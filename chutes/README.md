# chutes/ — going DOWN (Chutes & Ladders), at root

`chutes/` holds the **chutes** (aka "shoots"; UK "snakes") — the **down** moves (Aaron 2026-06-10: "ladders
and chutes/shoots"). The **descend / fall / collapse-down / bootstrap-down** half of the board. Sibling:
`ladders/` (up).

- **Bootstrap DOWN** — collapse back toward the seed (regenerate down; "bootstrap all the way down"; the
  grey particle running backward; antecedent-tracing back to the cause).
- **Shape A (inward/fall)** — self-reference converging inward; the fall that terminates (no infinite regress).
- **The descent** — higher → lower (3D → 2D cross-section; demote; hibernation; the cut that's honored, not
  discarded).

A chute is a **bounded down-jump** (it lands on the floor, never D⁰ heat-death). Pairs with `ladders/` — the
up/down of Chutes & Ladders over the board (the DAG / the space).

## Pointers

- `ladders/` (the up moves) · bootstrap-up-and-down (the seed) · `shapes/a.md` (inward/fall) · `shapes/d0.md`
  (the floor to avoid) · the grey particle / antecedent-tracing (running back).
