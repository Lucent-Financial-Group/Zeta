> **same/x-y — TEMPLATE.** Copy to `same/<x>-<y>.md` (canonical ordinal order: lexicographically smaller
> name first, so `x-y` ≡ `y-x` → one entry). Fill the carved sentence; this is the hub, detail lives in the
> linked captures.

# same/x — y

**Asserts:** `x` and `y` are the **same** — they share a **common cause** and are **unprovably-distinct**
across the boundary (the immaculate-coincidence relation). Symmetric; co-arising (shape E).

- **x:** `<traveler / ZetaId / observer name>`
- **y:** `<traveler / ZetaId / observer name>`
- **kind of same:** `common-cause / encrypted-null` (unprovable-distinct) — NOT byte-equality (that is the
  Chip-8 bit-perfect pole; say which pole this is).
- **shared cause:** `<the seed / null / coincidence that makes them same>`
- **established:** `<date · who · how (e.g. immaculate coincidence over Reticulum, two .NET tests)>`
- **falsifiable?:** by the encrypted-null invariant, **no** from inside the boundary — provability of
  difference = a boundary leak (P0). State the boundary.

**Carved sentence:** `<one line: x and y are the same because …>`
