{
  "$comment": [
    "Single-node readiness ledger for the hardware proof-of-concept.",
    "Read by src/Core.TypeScript/cluster/single-node-readiness.ts (bun, offline, ~1s).",
    "",
    "This file is the CHECKED-IN, DIFFABLE record of what the cluster manifests",
    "claim versus what one box can actually deliver. The auditor fails on DRIFT",
    "away from this record: a new app-of-apps identity collision, a new workload",
    "declaring redundancy it cannot have on nodeCount nodes, or a PVC total that",
    "outgrows nodeDiskGib. Every acknowledgement below is DEBT with a reason and",
    "an owner, not an exemption.",
    "",
    "STATUS: awaiting maintainer sign-off on nodeDiskGib (see below).",
    "  Dejan 2026-08-14 - drafted from a live single-node bring-up, not from a plan.",
    "",
    "2026-08-21 - nodeDiskGib is NO LONGER the auditor's only comparator, because",
    "it never was a measurement. The `capacity-provenance` check compares the same",
    "declared totals against MEASURED hardware read off the checked-in ClusterNode",
    "registrations under maintainers/*/cluster-nodes/*/node.yaml, and it REFUSES",
    "(exits 1) when no registration carries a measurable spec.hardware.storage.",
    "The nodeDiskGib budget line is still checked and is still useful as the",
    "procurement target - it is simply now labelled ASPIRATIONAL in the output,",
    "which is what it always was."
  ],

  "$comment_activeStorageProfile": [
    "Which rung of full-ai-cluster/k8s/storage-profiles.json THIS deployment runs.",
    "REQUIRED - readLedger refuses a ledger without it, because a default rung",
    "would let the storage-profile check report agreement with a profile nobody",
    "selected, which is a check that cannot fail.",
    "",
    "THE LADDER, and the box each rung needs. Longhorn never offers a node's raw",
    "disk to replicas. The tree that owns this ladder leaves both reserve settings",
    "at longhorn-1.7.2 chart defaults - storageOverProvisioningPercentage 100",
    "(values.yaml:214) and storageMinimalAvailablePercentage 25 (:216) - so ~75%",
    "of a disk is placeable, less the OS root:",
    "",
    "  minimal    200 GiB declared    172 GiB at bring-up   ~297 GiB box",
    "  standard   595 GiB declared    467 GiB at bring-up   ~823 GiB box",
    "  measured   967 GiB declared    467 GiB at bring-up   see the arithmetic below",
    "  large     1599 GiB declared   1099 GiB at bring-up  ~2162 GiB box",
    "",
    "`measured` IS ACTIVE, and it is named for where its numbers come from: every",
    "claim that provisions on a fresh sync gets `standard`'s capacity, and the",
    "three claims that provision NOTHING keep `large`'s planning ceilings.",
    "",
    "WHAT IT COSTS, AND WHAT IT DOES NOT. All 17 cuts from `large` are SIZE cuts.",
    "Not one pod count moves: cockroachdb keeps 3 Raft members, nats keeps a",
    "3-node JetStream cluster, mimir keeps 3 ingesters and 3 store-gateways,",
    "redis keeps 2 read replicas. `minimal` is the rung that trades quorum for",
    "disk - it drops cockroachdb and nats to 1, which removes Raft quorum for",
    "real - and that is exactly why `measured` rather than `minimal` is the rung",
    "for this box: the disk is there, so no HA has to be sold to fit on it.",
    "",
    "THE ARITHMETIC, against maintainers/Addisons820/cluster-nodes/node-ad1efd:",
    "  raw, every block device                        1047 GiB  <- what the gate compares",
    "  schedulable, 75% of (1047 - 30 GiB OS root)     763 GiB  <- what Longhorn will place",
    "  schedulable if only /dev/nvme0n1 is a Longhorn",
    "    disk (defaultDataPath: /var/lib/longhorn sits",
    "    on the root filesystem, so /dev/sda is not",
    "    one until somebody mounts and adds it)        676 GiB",
    "",
    "  measured, bring-up                              467 GiB  fits both, by 296 / 209 GiB",
    "  measured, declared                              967 GiB  under raw; OVER schedulable",
    "",
    "THE LAST LINE IS A STANDING DECISION, NOT A PASSED CHECK, and the auditor",
    "prints it on every run rather than leaving it buried here. The 500 GiB gap is",
    "ollama (200), vllm (200) and the arc runner model cache (100): two",
    "manual-sync Applications and one PVC manifest that no Application applies.",
    "None provisions on a fresh sync, so the cluster comes up well inside the",
    "schedulable bound - but syncing them at these ceilings would ask Longhorn for",
    "storage it will refuse to place. They are left alone deliberately: shrinking",
    "a ceiling nobody has decided to lower would buy headroom the bring-up total",
    "never spends, and would quietly delete a capacity intent instead of costing",
    "it. Running `standard` instead takes declared to 595 GiB and clears the",
    "schedulable bound outright, at the price of those ceilings (ollama and vllm",
    "200 -> 48, arc 100 -> 32). That is Aaron's call, and it now has its number."
  ],

  "activeStorageProfile": "measured",

  "$comment_activeResourceProfile": [
    "Which rung of storage-profiles.json's `resourceProfiles` THIS deployment runs.",
    "REQUIRED - readLedger refuses a ledger without it, for the same reason it",
    "refuses a missing activeStorageProfile.",
    "",
    "THIS KEY DID NOT EXIST UNTIL 2026-08-22, and its absence is the whole of the",
    "second finding in PR #13663. Two CPU/memory rungs existed; one of them was",
    "written into the manifests; nothing in the repo recorded which. Measured that",
    "day, all four combinations:",
    "",
    "  rung    --check              --budget",
    "  dev     exit 1, 54 drifts    exit 0     <- what CI runs",
    "  metal   exit 0               exit 1     <- what the tree carries",
    "",
    "One rung checked and not budgeted, the other budgeted and not checked, each",
    "green on the half it was measured against, and nothing comparing the two.",
    "`findRungCoverage` is now that comparison and this key is the input it needs.",
    "",
    "`metal` is the honest answer today: `--resource-profile metal --check` exits 0",
    "against the committed manifests. Writing `dev` here would make the ledger",
    "disagree with the tree, which `findResourceProfileDrift` refuses."
  ],

  "activeResourceProfile": "metal",

  "$comment_nodeDiskGib": [
    "Usable disk per node, in GiB, dedicated to the replicated-storage class.",
    "",
    "DERIVED, NOT GUESSED. Running the auditor prints the sum of every declared",
    "PVC capacity x pod count. Re-measured 2026-08-22:",
    "    longhorn          967 GiB   (profile \"measured\", the checked number)",
    "    longhorn          903 GiB   (the extractor's independent reading; it",
    "                                 cannot see mimir's chart-default pod counts",
    "                                 - kept as a cross-check, not as the answer.",
    "                                 911 -> 903 on 2026-09-01: it no longer",
    "                                 mis-attributes redis, whose two bitnami",
    "                                 StatefulSets became ONE valkey StatefulSet",
    "                                 with a shared volumeClaimTemplate. Both",
    "                                 oracles now read redis as 8Gi x 3 = 24.)",
    "    longhorn          843 GiB   (the RENDER, full-ai-cluster tree)",
    "    longhorn           24 GiB   (the RENDER, infra tree)",
    "    zeta-local-path    75 GiB   (the extractor's reading of our YAML - a",
    "                                 cross-check, and a STRUCTURALLY partial one;",
    "                                 see below)",
    "    zeta-local-path   225 GiB   (the RENDER, full-ai-cluster tree. Trust this",
    "                                 one for capacity.)",
    "    zeta-local-path    76 GiB   (the RENDER, infra tree)",
    "",
    "BOTH zeta-local-path readings fell by exactly 20 GiB on 2026-09-01: `minio` was",
      "removed (upstream project ARCHIVED) and its single 20Gi PVC on the cluster",
      "default class left with it. It moved BOTH readings because minio declared",
      "that claim in our own YAML *and* rendered it - the two figures disagree only",
      "where a chart claims disk our Applications never name, which minio did not.",
      "",
      "EVERY FIGURE ON THOSE SIX LINES IS NOW MACHINE-CHECKED, which it was not",
    "when it read 103 and 193. `findLedgerFigureDrift` in single-node-readiness.ts",
    "parses every aligned `<storageClass>  <N> GiB` row out of these $comment_*",
    "blocks and refuses any number that equals no reading the auditor computes -",
    "the YAML-derived total, any rung of the ladder (declared or at bring-up), or",
    "the render, per tree and across trees. Editing 193 back to 103 here is now a",
    "blocker rather than a silent regression, which is what it was until",
    "2026-08-22: prose in a checked-in ledger is read and acted on exactly like a",
    "field, and this file's most-read surface was its least-checked.",
    "",
    "PER TREE, NOT SUMMED. full-ai-cluster and infra both declare the root",
    "Application `argocd/zeta-root` (see $comment_acknowledgedRootAppDuplicates),",
    "so whichever is applied last prunes the other. One node runs ONE tree, and",
    "the 269 GiB sum of both is a number no cluster is ever asked for. The earlier",
    "note read \"193: 117 full-ai-cluster + 76 infra\", which was that sum mis-split:",
    "193 is the full-ai-cluster tree ALONE, of which gitlab is 76 - the same size",
    "as the whole infra tree, which is what made the two look interchangeable.",
    "",
    "WHY 95 AND 193 STILL DISAGREE, AND WHY BOTH ARE KEPT. Two errors were found",
    "in the 103; one is fixed and the other cannot be:",
    "  FIXED   it counted full-ai-cluster/k8s/applications/platform/blueprints-",
    "          flowdent.yaml's 8Gi mssql `Blueprint`, which is a TEMPLATE - a PVC",
    "          exists only once a Deployable references it, and none does (the",
    "          only Deployables in the tree live under examples/, which the",
    "          platform Application's directory.include glob does not apply). The",
    "          extractor now skips a Blueprint UNLESS some Deployable names it, so",
    "          writing one brings the capacity back with no edit here. 103 -> 95.",
    "          THAT FILE NO LONGER EXISTS: the mssql Blueprint left the tree on",
    "          2026-08-23 with the rest of the FlowDent chart (workitem",
    "          081M0QHCNQ3087G0R001P1GK5A). The 95 is UNCHANGED by that removal,",
    "          measured -- it was a template nothing instantiated, so it was",
    "          contributing nothing there was to subtract. The paragraph is kept",
    "          as the record of why 103 became 95, which is still what happened.",
    "  STRUCTURAL   the remaining 98 GiB gap is capacity our YAML never mentions:",
    "          gitlab (76), loki backend + write (10 each), mimir-alertmanager (1)",
    "          and dapr scheduler (1) all take their PVC from CHART DEFAULTS, with",
    "          no storageClassName, so they bind this class as the cluster default",
    "          - `zeta-local-path`, rancher.io/local-path, reclaimPolicy DELETE",
    "          (full-ai-cluster/nixos/modules/local-storage.nix). There is no key",
    "          in our tree to read, so no amount of teaching a YAML extractor",
    "          recovers them. Only the render can see them.",
    "The extractor DOES now resolve a blank `storageClassName` to the cluster",
    "default instead of dropping the claim - it shares one reading of that default",
    "with the render audit (cluster-default-storage-class.ts), so the two oracles",
    "cannot drift apart on the term. Measured honestly: no manifest in the tree",
    "writes a blank class today, so that half changes no number right now; it",
    "closes the hole rather than fixing an observed miss.",
    "So the two figures answer different questions and neither is redundant: the",
    "derived one is what we DECLARED (which is what the storage-profile ladder",
    "governs), the rendered one is what the cluster will ASK FOR (which is what",
    "the disk has to hold). The auditor now prints the render beside the derived",
    "and says in words which to trust, rather than printing two numbers and",
    "leaving the reader to pick.",
    "(this comment is a dated snapshot and has drifted twice already: 1499/73 on",
    "2026-08-14, 1599/1409 on 2026-08-21 under profile `large`. The auditor prints",
    "the live numbers; trust those over this paragraph.)",
    "",
    "2048 IS NOW LOOSE RATHER THAN TIGHT, and that is the useful change. It was",
    "chosen as the next standard NVMe size above a ~1.4 TiB reading and was",
    "slightly UNDER what `large` needed. The active rung needs 967 GiB declared",
    "and only 467 GiB at bring-up, so a 2 TiB node is no longer the requirement",
    "the manifests imply - it is headroom. The open procurement question changed",
    "shape with it: it is no longer \"buy 2 TiB or the cluster cannot come up\" (it",
    "can, today, on the 1047 GiB node-ad1efd) but \"how much of the deferred",
    "model-cache ceiling do we want to be able to actually sync\", which is 5.1 of",
    "docs/design/2026-08-21-the-zeta-bootstrap-usb-full-design-document.md.",
    "",
    "It is still NOT a measurement of any box, so it is still not what the auditor",
    "VERIFIES against; that is the measured ClusterNode registrations. See",
    "$comment_acknowledgedCapacityShortfall. It is kept, and kept labelled",
    "ASPIRATIONAL in the output, because a procurement target that quietly",
    "disappears once the cluster fits is a target nobody ever revisits.",
    "",
    "Measured counter-example (2026-08-14, kind 0.31.0 on podman/applehv, one",
    "node, 100 GB disk): the catalogue oversubscribed the disk ~15x. Longhorn",
    "reported ReplicaSchedulingFailure 'insufficient storage;precheck new replica",
    "failed', volumes went robustness=faulted, and cockroachdb-0/1/2 never left",
    "Init:0/1 with AttachVolume.Attach failed 'volume is not ready for workloads'.",
    "That is what an under-budgeted node looks like."
  ],

  "nodeDiskGib": 2048,

  "$comment_nodeCount": [
    "Nodes in the PoC cluster. One box today. Raise this as workers join; every",
    "acknowledgedFalseRedundancy entry below should be re-examined when it does,",
    "because most of them stop being false at nodeCount >= 3."
  ],

  "nodeCount": 1,

  "budgetedStorageClasses": ["longhorn"],

  "$comment_acknowledgedFalseRedundancy": [
    "Workloads that declare replicas > nodeCount WITHOUT hard anti-affinity, so",
    "Kubernetes co-schedules every replica onto the one node. They come up green;",
    "the redundancy is nominal and the failure domain is a single disk. Listing",
    "one here does NOT make it safe - it makes it stated, so nobody demos it as",
    "high availability.",
    "",
    "  full-ai-cluster/cockroachdb  3 Raft members, chart default podAntiAffinity",
    "                               type=soft. Observed co-scheduled on one node.",
    "  full-ai-cluster/vault        server.ha.replicas=3 with raft.enabled=true.",
    "                               Same shape: Raft quorum, one failure domain.",
    "  full-ai-cluster/nats         JetStream config.cluster.replicas=3. Same shape.",
    "                               AND IT WAS NOT EVEN THAT until 2026-08-22: the",
    "                               block sat at TOP LEVEL, where nats 1.2.7 never",
    "                               read it, so the StatefulSet rendered replicas: 1",
    "                               and this line acknowledged a nominal redundancy",
    "                               that did not exist in either sense -- one pod,",
    "                               no quorum, and any stream created with",
    "                               replicas > 1 refusing outright. The block is",
    "                               under `config:` now and the render was",
    "                               re-measured, so the entry is true as written:",
    "                               three co-scheduled pods on one disk.",
    "  full-ai-cluster/redis        replica.replicaCount=2. Async replication, so",
    "                               no quorum to lose - but no redundancy either.",
    "  full-ai-cluster/platform     examples/gmod-server.yaml replicas=2. A sample",
    "                               workload, not part of the PoC critical path.",
    "  full-ai-cluster/kubevirt     virt-operator Deployment replicas=2, from the",
    "                               VENDORED upstream kubevirt-operator.yaml",
    "                               (v1.8.4, line 8493). Upstream ships 2 for",
    "                               control-plane HA; on one node both co-schedule.",
    "                               NOT edited down to 1 on purpose: that manifest",
    "                               is vendored byte-for-byte so it stays 1:1 with",
    "                               what runs, and node-5b2dfa really does run two",
    "                               virt-operator pods today. Editing it would make",
    "                               the checked-in copy diverge from the cluster it",
    "                               documents, which is a worse lie than this one.",
    "                               Resolves at nodeCount >= 2 with no code change.",
    "",
    "Removing an entry from this list is the fix, and the fix is one of: drop to",
    "nodeCount replicas (honest single node), set hard anti-affinity (the shortfall",
    "becomes visible Pending pods), or add nodes."
  ],

  "acknowledgedFalseRedundancy": [
    "full-ai-cluster/cockroachdb",
    "full-ai-cluster/vault",
    "full-ai-cluster/nats",
    "full-ai-cluster/redis",
    "full-ai-cluster/platform",
    "full-ai-cluster/kubevirt"
  ],

  "$comment_acknowledgedCapacityShortfall": [
    "EMPTY, and that is the point of this round. It carried",
    "  longhorn=1599GiB>>1047GiB@node-ad1efd",
    "from 2026-08-21 until the manifests were shrunk to fit the measured box.",
    "",
    "The debt is retired by ARITHMETIC, not by exemption: activeStorageProfile",
    "moved from `large` (1599 GiB) to `measured` (967 GiB) and the manifests were",
    "rewritten to match, so the declared total is now under the 1047 GiB measured",
    "bound and there is no shortfall left to acknowledge. Nothing about the check",
    "was relaxed - re-run the auditor and it does the same comparison it always",
    "did, against the same measured evidence.",
    "",
    "WHAT THIS KEY IS FOR, still. A capacity shortfall against MEASURED hardware",
    "may be carried here as debt, recorded as",
    "`<storageClass>=<declared>GiB>><measured>GiB@<node>`. The key carries BOTH",
    "numbers on purpose: acknowledging the bare storage class would make the check",
    "unfalsifiable, silently absorbing any later PVC growth or a smaller node",
    "joining. Pinned, every movement of either side goes red and has to be",
    "re-stated deliberately.",
    "",
    "Provenance of the measured side: maintainers/Addisons820/cluster-nodes/",
    "node-ad1efd/node.yaml, written by zeta-install.sh from",
    "`lsblk -ndo NAME,SIZE,TYPE -e7` on that node during a real metal boot",
    "(2026-06-09) and committed by a self-registration PR. lsblk's human sizes are",
    "BINARY, so 931.5G is 931.5 GiB and compares directly to Kubernetes Gi.",
    "",
    "The bound is deliberately GENEROUS: it counts /dev/sda, which is very",
    "possibly the USB stick, and it reserves nothing for the ESP, the root",
    "filesystem, swap, or Longhorn's own reserve. So exceeding it is proven",
    "oversubscription and no reading of the hardware rescues it; NOT exceeding it",
    "proves nothing on its own. The inference convicts, never acquits - which is",
    "why the auditor now also PRINTS the schedulable estimate (~75% of disk less",
    "the OS root, from the deployed Longhorn reserve settings) beside the raw",
    "bound. That estimate is a report and not the gate, because it assumes which",
    "block devices Longhorn manages and how big the OS root is, and a bound built",
    "on assumptions can be wrong in the acquitting direction.",
    "",
    "Two of the registered nodes (maximdolphin's node-f82aa6 and node-5b2dfa)",
    "record NO hardware.storage at all - the post-boot self-register path never",
    "captured it. They are excluded from the bound and printed as unmeasured",
    "rather than counted as zero.",
    "",
    "NOTE: the UNVERIFIED case is not acknowledgeable here and must not become so.",
    "A shortfall you can see is debt; an absent comparator is an absent check, and",
    "an exemption for it would be the vacuity this ledger exists to prevent."
  ],

  "acknowledgedCapacityShortfall": [],

  "$comment_acknowledgedComputeShortfall": [
    "EMPTY, and unlike the storage list above it has never carried an entry.",
    "",
    "The `metal` rung reserves 10365m / 22623Mi across the 47 Applications the",
    "bootstrap root applies. The smallest registered node measures 16 logical CPUs",
    "(16000m) and 62942 MiB, so the rung fits with 5635m / 40319Mi of the raw",
    "bound unspent. That is a REPORT, not a pass: the bound reserves nothing for",
    "the kubelet, the control plane, kube-system or the OS, and 29 of the 46",
    "Applications render BestEffort pods that no arithmetic here ever sees.",
    "",
    "The cohort grew by one Application on 2026-08-22 and nothing was added to the",
    "tree: `applicationDirs()` enumerated depth 1 while ArgoCD's include glob",
    "reaches depth 2, so `game-hosting/gmod` -- an in-repo StatefulSet with a",
    "literal cpu/memory request in its own statefulset.yaml -- had always been",
    "applied and never counted. Both figures above moved with it.",
    "",
    "AND THE BOUND STILL EXCLUDES A WHOLE TREE. infra/k8s/applications is a second",
    "App-of-Apps root, auto-applied by K3S on first boot, and no row in the ladder",
    "can name it: every `dir` key is relative to full-ai-cluster/k8s/applications.",
    "Its seven Applications do render requests; the measurement lives in",
    "src/Core.TypeScript/cluster/rendered-resource-requests.snapshot.json and is",
    "baselined there as uncovered, deliberately NOT restated here, because a",
    "figure this block cannot re-read is a figure that goes stale silently. The",
    "total still fits the raw bound, so no verdict here turns on it - but what",
    "this block reports is the ladder's cohort, not the machine's whole load.",
    "",
    "Carry an entry here as `cpu=<declared>m>><measured>m@<node>` or",
    "`memory=<declared>Mi>><measured>Mi@<node>`. Both numbers are in the key for",
    "the reason the storage key gives: acknowledging the bare dimension would let",
    "any later growth - or a smaller node joining - be silently absorbed.",
    "",
    "THE UNITS ARE NOT SYMMETRIC AND THE ASYMMETRY IS MEASURED. The registration",
    "scripts capture storage with `lsblk` (BINARY human sizes) and memory with",
    "`free -h --si` (DECIMAL) - one line apart in the same function. So `931.5G`",
    "of disk is 931.5 GiB and `66G` of RAM is 62942 MiB, not 67584 MiB. Reading",
    "the memory as binary would inflate this bound by 4642 MiB per node, and",
    "inflating a bound is the ACQUITTING direction.",
    "",
    "`cores` is likewise not what its name says: both scripts write `$(nproc)`,",
    "which counts LOGICAL CPUs. maximdolphin's nodes record `cores: 22` on a",
    "16-core / 22-thread Ultra 9 185H. The thread count is the right number here -",
    "it is what the kubelet reports as capacity.cpu - but the field name is not.",
    "",
    "The UNVERIFIED case is NOT acknowledgeable here, same as above."
  ],

  "acknowledgedComputeShortfall": [],

  "$comment_acknowledgedRungBudgetGap": [
    "The one tolerated disagreement between the rung the tree CARRIES and the rung",
    "CI BUDGETS, recorded as",
    "`<committed>@<budgeted>-lane=<cpu>m/<mem>Mi>><budgetCpu>m/<budgetMem>Mi`.",
    "",
    "CARRIED, NOT RESOLVED, and it is deliberately not the auditor's call to",
    "resolve. `metal` over the 39 Applications the dev lane applies is 6390m /",
    "14352Mi against a 2500m / 9216Mi runner budget (this sentence is the",
    "statement of the gap and is kept CURRENT; the tables below are its history,",
    "oldest first). The arithmetic that makes",
    "this a maintainer decision rather than a code change:",
    "",
    "  dev lane at `metal` (what the tree carries)   6240m   over by 3740m",
    "  ...with hindsight removed ENTIRELY            5240m   still over by 2740m",
    "  ...with hindsight alone cut to `dev`          5315m   still over by 2815m",
    "  dev lane at `dev`                             1115m   FITS, 1385m spare",
    "",
    "RE-KEYED 2026-09-03 TO 6390m/14352Mi, AND THIS TIME ONE APPLICATION DID JOIN",
    "THE LANE. `agent-memory` left `DEFAULT_ROOT_DEV_CATALOG.excludeGlob` on its",
    "own recorded lift condition (its deferral said it was held by the glob, not",
    "by a measurement), so the dev lane applies 39 Applications, not 38. Its",
    "manifest asks 50m/64Mi and the dev rung floors that to 25m/64Mi. The table",
    "above at today's numbers, kept as a second row set rather than overwritten:",
    "",
    "  dev lane at `metal` (what the tree carries)   6390m   over by 3890m",
    "  ...with hindsight removed ENTIRELY            5390m   still over by 2890m",
    "  ...with hindsight alone cut to `dev`          5465m   still over by 2965m",
    "  dev lane at `dev`                             1165m   FITS, 1335m spare",
    "",
    "The memory axis at `dev` is 9164Mi against 9216Mi -- 52Mi of spare, and it",
    "is now the tighter axis of the two. That is the number the next Application",
    "to join this lane will hit first, and it is written here so it is not",
    "discovered by a Pending pod. (Between the two tables the key was re-keyed",
    "once more, on 2026-09-02 for `cloudnativepg`, without this prose following;",
    "the superseded figures are not repeated here because this ledger's checker",
    "admits only readings the tree computes today -- the sequence lives in",
    "reason-truth.test.ts and storage-profiles.test.ts.)",
    "",
    "EVERY NUMBER IN THAT TABLE ROSE AGAIN ON 2026-09-01, AND AGAIN NOTHING GREW.",
    "Re-measuring the resource snapshot -- forced by an unrelated redis change --",
    "found three declarations that chart bumps had left behind. mimir 6.2.0",
    "bundles a Kafka StatefulSet that nothing declared, because that chart now",
    "enables the ingest-storage architecture by default. node-feature-discovery",
    "renders a prune Job priced by the SAME `master.resources` field as its",
    "master, so one field governed two pods while the row said one. And alloy",
    "measures higher than the pin it was given when its chart was older. None of",
    "it is new load -- every one of those pods was already being applied by this",
    "lane. What moved is that the ledger now says so, which is the only thing a",
    "declaration was ever for. The last row also changed VERDICT the other way,",
    "to FITS: the dev rung reaches the raw manifests now and its governed rows",
    "are floored.",
    "",
    "EVERY NUMBER IN THAT TABLE ROSE BY 1000m ON 2026-08-22 AND NOTHING GREW, and",
    "the last row changed VERDICT, which is why this comment is rewritten rather",
    "than re-keyed. `applicationDirs()` enumerated depth 1; ArgoCD's include glob",
    "is not path-segment bounded (established against a live cluster in",
    "app-of-apps-discovery.ts); `game-hosting/gmod` -- an in-repo StatefulSet with",
    "a literal cpu 1 / memory 2Gi -- had always been applied and never counted.",
    "storage-profiles.json asserted in writing that it contributes nothing at all.",
    "",
    "So no app-local fix closes it (hindsight is 1000m and mimir's kafka row alone",
    "is another 1000m at metal), and the LANE-WIDE fix is what closes it: applying",
    "`dev` to the committed tree brings the lane inside the runner budget, which",
    "is the FITS verdict on the last row of the table above.",
    "",
    "SUPERSEDED 2026-09-01, AND KEPT BECAUSE THE SHAPE OF THE ARGUMENT STILL",
    "HOLDS: this paragraph used to end by saying that applying `dev` still left",
    "the lane over the budget by a small remainder, and it named two cheap ways",
    "to close it -- shrink gmod's literal request, or add `game-hosting/**` to the",
    "dev root's excludeGlob. gmod is a",
    "git-path source with no valuesObject, so no rung can express it, and that",
    "much is unchanged. What changed is that the dev rung's own floors have since",
    "absorbed that remainder, so there is nothing left to close. The two shapes are",
    "recorded here rather than deleted because the next growth in this lane will",
    "reopen exactly this question, and the answers were already priced beside the",
    "shortfall in storage-profiles.json's `acknowledgedLaneBudgetShortfall`.",
    "",
    "The two ways out of THIS gap are unchanged: apply `dev` to the committed",
    "tree, or build a per-substrate override point so one tree can carry two",
    "rungs. bootstrap/root-application.yaml points the metal cluster at the same",
    "path the CI lane uses, so today there is no override point at all. Both",
    "options are costed in",
    "docs/research/2026-08-22-one-tree-two-substrates-the-resource-rung-override-trade.md.",
    "",
    "Deleting this entry is the fix. While it is here the gap is stated rather",
    "than hidden, and every one of the four numbers is pinned - move any of them",
    "and this goes red until the trade is re-stated."
  ],

  "acknowledgedRungBudgetGap": ["metal@dev-lane=6390m/14352Mi>>2500m/9216Mi"],

  "$comment_acknowledgedRootAppDuplicates": [
    "argocd/zeta-root is declared TWICE with two different spec.source.path values:",
    "  full-ai-cluster/k8s/bootstrap/root-application.yaml -> full-ai-cluster/k8s/applications",
    "  infra/k8s/applications/root-application.yaml        -> infra/k8s/applications",
    "Kubernetes has exactly one object at that identity. Both roots carry",
    "prune: true + selfHeal: true, so whichever is applied last owns the name and",
    "prunes the other tree's entire child Application graph.",
    "",
    "This is the mechanical reason the two trees cannot both be pointed at one",
    "cluster, and the reason 'run the live test against both trees' is not a",
    "cheap option. Tracked for consolidation; delete this entry when one tree",
    "goes away."
  ],

  "acknowledgedRootAppDuplicates": ["argocd/zeta-root=full-ai-cluster/k8s/applications|infra/k8s/applications"]
}
