{
  "$comment": [
    "ACKNOWLEDGED findings for rendered-storage-claims.ts.",
    "",
    "WHAT AN ENTRY IS. Not a suppression -- a dated, keyed, reasoned record that a finding is",
    "TRUE and is not being fixed in the change that discovered it. Every acknowledged finding is",
    "still counted in the totals the report prints; what the entry buys is a non-red gate, never",
    "a smaller number.",
    "",
    "KEYED BY IDENTITY, NEVER BY LINE. `key` is the finding's own stable identity (claim id, or",
    "app + rendered PVC name). A file that grows a line cannot silently re-arm or re-suppress a",
    "different defect.",
    "",
    "`observed` PINS WHAT WAS ACKNOWLEDGED. It is compared byte-for-byte against the live finding.",
    "If forgejo starts rendering 40Gi instead of 20Gi, its entry stops covering it and the gate",
    "goes red -- because what was excused was a specific measured defect, not a name. A baseline",
    "that keeps matching after the thing it excused has moved is the vacuity class with a filename.",
    "",
    "`liftsWhen` IS REQUIRED. An acknowledgement with no stated exit is a permanent exemption",
    "wearing a temporary word, and the loader refuses one without it.",
    "",
    "STALE ENTRIES FAIL. An entry matching nothing is a claim about the tree that stopped being",
    "true, and it is refused exactly like a catalogue row matching no claim.",
    "",
    "WHAT IS LEFT ON `findings` IS SCOPE PLUS ONE ORPHAN. This file opened with three live-defect entries:",
    "hindsight (2) and nats (1) were inert valuesObject paths, declarations that never reached the chart.",
    "Both manifests were fixed on 2026-08-22, the render was re-measured, and all three entries were",
    "DELETED rather than left to match nothing -- a baseline entry that outlives its defect is itself a",
    "finding, and a stale one fails this loader in its own right. The 18 `undeclared` entries are PVCs on",
    "the non-longhorn class the ladder does not claim to govern; they are listed with their sizes so the",
    "269 GiB they represent is never invisible. `arc-runner-set/model-cache` is the one live defect left:",
    "100 GiB declared against a PVC nothing renders, because the manifest declaring it is orphaned.",
    "",
    "`unrenderable` IS A WORSE STATE THAN ANY FINDING, and it is where the rest of this change happened.",
    "A finding is a measured disagreement; an unrenderable app is 0 bytes of evidence. The list went from",
    "five entries to one on 2026-08-22, and the four that left were every one of them a DEFECT IN OUR OWN",
    "MANIFEST -- headscale mounted its declared volume at a path the chart never writes to (/data) while",
    "the node database and both private keys sat on the container's ephemeral layer, and omitted the",
    "accessMode the chart requires; oz pinned a chart version no registry has ever served, which was itself",
    "masking a missing `clientApi.advertisedHost` and an `adminSecret:` block the chart has no key for;",
    "gitlab was missing the one `configureCertmanager` value its own sibling in infra/ already carried; and",
    "arc-runner-set, the `lookup` one that looked like a limit of the TOOL, ships an explicit escape hatch",
    "its own error message recommends. None was a checker artefact. Every one would have failed inside",
    "ArgoCD in exactly the same place, on the same line.",
    "",
    "`observed` IS LOAD-BEARING ON **BOTH** LISTS AS OF 2026-08-22, and on `unrenderable` it was not before.",
    "There it pins the FAILURE CLASS -- `helm-pull-failed` (the pin names nothing upstream serves) versus",
    "`helm-template-failed` (the chart is there, our values are wrong) -- so an entry stops covering an app",
    "whose class changes. Until then the field was required by the loader, written on every entry, and read",
    "by NOTHING, so an acknowledgement about a bad pin would have gone on silently covering a completely",
    "different defect once the pin was fixed. A stale entry on this list now fails too, and that check",
    "earned itself within the hour: merging main mid-change re-introduced all four retired acknowledgements,",
    "and it was the stale check -- not a human reading a diff -- that refused them.",
    "",
    "WHAT IS NOT HERE, deliberately: a `structurally-unrenderable` class. It was considered -- a chart that",
    "calls `lookup` genuinely cannot be answered offline, and recording that as a limit of the tool rather",
    "than as a defect would be the honest thing IF an app were in it. Measurement says none is. Creating the",
    "class anyway, with no member, would be a category that cannot fail, which is the thing this whole file",
    "refuses. The distinction is carried by the reason codes instead, where it is checked."
  ],
  "findings": [
    {
      "key": "declared full-ai-cluster/arc-runner-set/model-cache",
      "reason": "MEASURED DEFECT, AND THE FIRST TIME IT COULD BE MEASURED AT ALL. gha-runner-scale-set 0.12.1 renders a ServiceAccount, a Role, a RoleBinding and one AutoscalingRunnerSet -- and NO PersistentVolumeClaim. The 100 GiB `arc-model-cache` claim lives in a hand-written manifest, full-ai-cluster/k8s/applications/arc-runner-set/model-cache-pvc.yaml, that NO Application applies: the root App-of-Apps includes only '{*/Application.yaml,Application.yaml}', and arc-runner-set's own Application sources a REMOTE chart, so it cannot apply its own sibling file. The catalogue's bringUpNote has said this since it was written; what is new is that the CHECKER now says it, out of a render, instead of being unable to look -- until 2026-08-22 this app was UNRENDERABLE (its chart calls `lookup` to discover the controller ServiceAccount), so the single largest row in the ladder was reported as unchecked, and unchecked hid the fact that it is also WRONG. The consequence is not only accounting: the Application ships minRunners: 1 mounting claimName arc-model-cache, so the warm runner stays Pending on a PVC that does not exist. NOT repaired here, because the repair is a choice between two different fixes -- add a git-path Application for the PVC, or move the claim into a volumeClaimTemplate -- and neither belongs inside the change that made it visible.",
      "liftsWhen": "an Application actually applies model-cache-pvc.yaml (a sibling git-path Application, or a root include that reaches it) so `arc-model-cache` appears in the render -- OR the row is retired together with the orphaned manifest, and the 100 GiB leaves the ladder honestly instead of by omission",
      "observed": "declares 100Gi on \"longhorn\" x1 pods, but rendering full-ai-cluster/arc-runner-set produces NO PersistentVolumeClaim matching /^arc-model-cache$/ — the row governs a path that does not exist, and its 100 GiB are counted against nothing"
    },
    {
      "key": "undeclared full-ai-cluster/dapr dapr-scheduler-data-dir/dapr-scheduler-server",
      "reason": "OUT OF THE LADDER'S DECLARED SCOPE, AND THE SCOPE IS NOW MEASURABLY UNDER-STATED. storage-profiles.json says 'one row per `longhorn` PVC claim in the tree', and this claim lands on `zeta-local-path` -- the cluster default StorageClass (full-ai-cluster/nixos/modules/local-storage.nix), a node-local hostPath class with reclaimPolicy: Delete. It is still real disk on the box being provisioned, so it is recorded here rather than filtered out: the rendered zeta-local-path total is 193 GiB (117 in full-ai-cluster, 76 in the infra tree). RE-DERIVED 2026-08-22: it read 201 GiB (125 + 76) until hindsight's postgres moved off the default class onto longhorn, taking 8 GiB with it. single-node-budget.json's $comment_nodeDiskGib now prints BOTH this figure and the readiness extractor's 103 GiB, with the reason they differ: the extractor over-counts a platform `Blueprint` template (8 GiB, provisions nothing) and under-counts every claim that renders with no storageClassName and binds this class as the default (22 GiB). The extractor itself is unchanged -- a named gap, not a fixed one.",
      "liftsWhen": "the catalogue grows a row for this PVC's non-longhorn class, at which point the claim is covered and this entry matches nothing and fails as stale. (The second exit this entry used to name -- re-deriving single-node-budget.json's zeta-local-path figure -- was NOT an exit: that was done on 2026-08-22 and the finding is still here, because what makes it a finding is that no catalogue ROW covers the PVC, not what any comment records.)",
      "observed": "the chart renders a PersistentVolumeClaim of 1Gi x1 on \"zeta-local-path\" (no storageClassName; cluster default) that NO catalogue row covers — 1 GiB of real disk that no profile total includes"
    },
    {
      "key": "undeclared full-ai-cluster/forgejo gitea-shared-storage",
      "reason": "OUT OF THE LADDER'S DECLARED SCOPE, AND THE SCOPE IS NOW MEASURABLY UNDER-STATED. storage-profiles.json says 'one row per `longhorn` PVC claim in the tree', and this claim lands on `zeta-local-path` -- the cluster default StorageClass (full-ai-cluster/nixos/modules/local-storage.nix), a node-local hostPath class with reclaimPolicy: Delete. It is still real disk on the box being provisioned, so it is recorded here rather than filtered out: the rendered zeta-local-path total is 193 GiB (117 in full-ai-cluster, 76 in the infra tree). RE-DERIVED 2026-08-22: it read 201 GiB (125 + 76) until hindsight's postgres moved off the default class onto longhorn, taking 8 GiB with it. single-node-budget.json's $comment_nodeDiskGib now prints BOTH this figure and the readiness extractor's 103 GiB, with the reason they differ: the extractor over-counts a platform `Blueprint` template (8 GiB, provisions nothing) and under-counts every claim that renders with no storageClassName and binds this class as the default (22 GiB). The extractor itself is unchanged -- a named gap, not a fixed one.",
      "liftsWhen": "the catalogue grows a row for this PVC's non-longhorn class, at which point the claim is covered and this entry matches nothing and fails as stale. (The second exit this entry used to name -- re-deriving single-node-budget.json's zeta-local-path figure -- was NOT an exit: that was done on 2026-08-22 and the finding is still here, because what makes it a finding is that no catalogue ROW covers the PVC, not what any comment records.)",
      "observed": "the chart renders a PersistentVolumeClaim of 20Gi x1 on \"zeta-local-path\" that NO catalogue row covers — 20 GiB of real disk that no profile total includes"
    },
    {
      "key": "undeclared full-ai-cluster/gitlab data/gitlab-postgresql",
      "reason": "OUT OF THE LADDER'S DECLARED SCOPE, AND NEWLY VISIBLE RATHER THAN NEWLY REAL. This PVC has always been what full-ai-cluster/gitlab provisions; until 2026-08-22 the app would not template at all -- it lacked `global.ingress.configureCertmanager: false`, which `certmanager.install: false` does not imply -- so the checker reported it as UNRENDERABLE and counted its disk as neither verified nor refuted. Supplying that one value moved these 76 GiB from unchecked to undeclared: a worse-looking gate and a strictly better ledger. Identical in size and class to the sibling infra/gitlab entries, because it is the same chart at the same pin -- which is also the evidence that the value was right: the sibling gitlab Application in the OTHER cluster tree already carried it, and that is exactly why that one rendered while this one produced nothing. (The path is cited in the manifest comment rather than here on purpose: that file is on the cluster-tree-consumers roster, so the citation dies with the tree it names instead of quietly outliving it.) the rendered zeta-local-path total is 269 GiB (193 in full-ai-cluster, 76 in the infra tree). RE-DERIVED AGAIN 2026-08-22: it read 193 GiB until full-ai-cluster/gitlab became RENDERABLE. That app was missing one value (`global.ingress.configureCertmanager: false`) and so templated nothing at all, which meant its 76 GiB was not counted anywhere -- not as declared, not as undeclared, not as debt. It was never absent from the disk, only from the arithmetic.",
      "liftsWhen": "the catalogue grows a row for this PVC's non-longhorn class, at which point the claim is covered and this entry matches nothing and fails as stale -- OR the gitlab/forgejo either-or is settled and the losing Application is deleted from this tree, which retires all four of these at once. Note what is NOT an exit: re-deriving any budget comment. What makes this a finding is that no catalogue ROW covers the PVC.",
      "observed": "the chart renders a PersistentVolumeClaim of 8Gi x1 on \"zeta-local-path\" (no storageClassName; cluster default) that NO catalogue row covers — 8 GiB of real disk that no profile total includes"
    },
    {
      "key": "undeclared full-ai-cluster/gitlab gitlab-minio",
      "reason": "OUT OF THE LADDER'S DECLARED SCOPE, AND NEWLY VISIBLE RATHER THAN NEWLY REAL. This PVC has always been what full-ai-cluster/gitlab provisions; until 2026-08-22 the app would not template at all -- it lacked `global.ingress.configureCertmanager: false`, which `certmanager.install: false` does not imply -- so the checker reported it as UNRENDERABLE and counted its disk as neither verified nor refuted. Supplying that one value moved these 76 GiB from unchecked to undeclared: a worse-looking gate and a strictly better ledger. Identical in size and class to the sibling infra/gitlab entries, because it is the same chart at the same pin -- which is also the evidence that the value was right: the sibling gitlab Application in the OTHER cluster tree already carried it, and that is exactly why that one rendered while this one produced nothing. (The path is cited in the manifest comment rather than here on purpose: that file is on the cluster-tree-consumers roster, so the citation dies with the tree it names instead of quietly outliving it.) the rendered zeta-local-path total is 269 GiB (193 in full-ai-cluster, 76 in the infra tree). RE-DERIVED AGAIN 2026-08-22: it read 193 GiB until full-ai-cluster/gitlab became RENDERABLE. That app was missing one value (`global.ingress.configureCertmanager: false`) and so templated nothing at all, which meant its 76 GiB was not counted anywhere -- not as declared, not as undeclared, not as debt. It was never absent from the disk, only from the arithmetic.",
      "liftsWhen": "the catalogue grows a row for this PVC's non-longhorn class, at which point the claim is covered and this entry matches nothing and fails as stale -- OR the gitlab/forgejo either-or is settled and the losing Application is deleted from this tree, which retires all four of these at once. Note what is NOT an exit: re-deriving any budget comment. What makes this a finding is that no catalogue ROW covers the PVC.",
      "observed": "the chart renders a PersistentVolumeClaim of 10Gi x1 on \"zeta-local-path\" (no storageClassName; cluster default) that NO catalogue row covers — 10 GiB of real disk that no profile total includes"
    },
    {
      "key": "undeclared full-ai-cluster/gitlab redis-data/gitlab-redis-master",
      "reason": "OUT OF THE LADDER'S DECLARED SCOPE, AND NEWLY VISIBLE RATHER THAN NEWLY REAL. This PVC has always been what full-ai-cluster/gitlab provisions; until 2026-08-22 the app would not template at all -- it lacked `global.ingress.configureCertmanager: false`, which `certmanager.install: false` does not imply -- so the checker reported it as UNRENDERABLE and counted its disk as neither verified nor refuted. Supplying that one value moved these 76 GiB from unchecked to undeclared: a worse-looking gate and a strictly better ledger. Identical in size and class to the sibling infra/gitlab entries, because it is the same chart at the same pin -- which is also the evidence that the value was right: the sibling gitlab Application in the OTHER cluster tree already carried it, and that is exactly why that one rendered while this one produced nothing. (The path is cited in the manifest comment rather than here on purpose: that file is on the cluster-tree-consumers roster, so the citation dies with the tree it names instead of quietly outliving it.) the rendered zeta-local-path total is 269 GiB (193 in full-ai-cluster, 76 in the infra tree). RE-DERIVED AGAIN 2026-08-22: it read 193 GiB until full-ai-cluster/gitlab became RENDERABLE. That app was missing one value (`global.ingress.configureCertmanager: false`) and so templated nothing at all, which meant its 76 GiB was not counted anywhere -- not as declared, not as undeclared, not as debt. It was never absent from the disk, only from the arithmetic.",
      "liftsWhen": "the catalogue grows a row for this PVC's non-longhorn class, at which point the claim is covered and this entry matches nothing and fails as stale -- OR the gitlab/forgejo either-or is settled and the losing Application is deleted from this tree, which retires all four of these at once. Note what is NOT an exit: re-deriving any budget comment. What makes this a finding is that no catalogue ROW covers the PVC.",
      "observed": "the chart renders a PersistentVolumeClaim of 8Gi x1 on \"zeta-local-path\" (no storageClassName; cluster default) that NO catalogue row covers — 8 GiB of real disk that no profile total includes"
    },
    {
      "key": "undeclared full-ai-cluster/gitlab repo-data/gitlab-gitaly",
      "reason": "OUT OF THE LADDER'S DECLARED SCOPE, AND NEWLY VISIBLE RATHER THAN NEWLY REAL. This PVC has always been what full-ai-cluster/gitlab provisions; until 2026-08-22 the app would not template at all -- it lacked `global.ingress.configureCertmanager: false`, which `certmanager.install: false` does not imply -- so the checker reported it as UNRENDERABLE and counted its disk as neither verified nor refuted. Supplying that one value moved these 76 GiB from unchecked to undeclared: a worse-looking gate and a strictly better ledger. Identical in size and class to the sibling infra/gitlab entries, because it is the same chart at the same pin -- which is also the evidence that the value was right: the sibling gitlab Application in the OTHER cluster tree already carried it, and that is exactly why that one rendered while this one produced nothing. (The path is cited in the manifest comment rather than here on purpose: that file is on the cluster-tree-consumers roster, so the citation dies with the tree it names instead of quietly outliving it.) the rendered zeta-local-path total is 269 GiB (193 in full-ai-cluster, 76 in the infra tree). RE-DERIVED AGAIN 2026-08-22: it read 193 GiB until full-ai-cluster/gitlab became RENDERABLE. That app was missing one value (`global.ingress.configureCertmanager: false`) and so templated nothing at all, which meant its 76 GiB was not counted anywhere -- not as declared, not as undeclared, not as debt. It was never absent from the disk, only from the arithmetic.",
      "liftsWhen": "the catalogue grows a row for this PVC's non-longhorn class, at which point the claim is covered and this entry matches nothing and fails as stale -- OR the gitlab/forgejo either-or is settled and the losing Application is deleted from this tree, which retires all four of these at once. Note what is NOT an exit: re-deriving any budget comment. What makes this a finding is that no catalogue ROW covers the PVC.",
      "observed": "the chart renders a PersistentVolumeClaim of 50Gi x1 on \"zeta-local-path\" (no storageClassName; cluster default) that NO catalogue row covers — 50 GiB of real disk that no profile total includes"
    },
    {
      "key": "undeclared full-ai-cluster/loki data/loki-backend",
      "reason": "OUT OF THE LADDER'S DECLARED SCOPE, AND THE SCOPE IS NOW MEASURABLY UNDER-STATED. storage-profiles.json says 'one row per `longhorn` PVC claim in the tree', and this claim lands on `zeta-local-path` -- the cluster default StorageClass (full-ai-cluster/nixos/modules/local-storage.nix), a node-local hostPath class with reclaimPolicy: Delete. It is still real disk on the box being provisioned, so it is recorded here rather than filtered out: the rendered zeta-local-path total is 193 GiB (117 in full-ai-cluster, 76 in the infra tree). RE-DERIVED 2026-08-22: it read 201 GiB (125 + 76) until hindsight's postgres moved off the default class onto longhorn, taking 8 GiB with it. single-node-budget.json's $comment_nodeDiskGib now prints BOTH this figure and the readiness extractor's 103 GiB, with the reason they differ: the extractor over-counts a platform `Blueprint` template (8 GiB, provisions nothing) and under-counts every claim that renders with no storageClassName and binds this class as the default (22 GiB). The extractor itself is unchanged -- a named gap, not a fixed one.",
      "liftsWhen": "the catalogue grows a row for this PVC's non-longhorn class, at which point the claim is covered and this entry matches nothing and fails as stale. (The second exit this entry used to name -- re-deriving single-node-budget.json's zeta-local-path figure -- was NOT an exit: that was done on 2026-08-22 and the finding is still here, because what makes it a finding is that no catalogue ROW covers the PVC, not what any comment records.)",
      "observed": "the chart renders a PersistentVolumeClaim of 10Gi x1 on \"zeta-local-path\" (no storageClassName; cluster default) that NO catalogue row covers — 10 GiB of real disk that no profile total includes"
    },
    {
      "key": "undeclared full-ai-cluster/loki data/loki-write",
      "reason": "OUT OF THE LADDER'S DECLARED SCOPE, AND THE SCOPE IS NOW MEASURABLY UNDER-STATED. storage-profiles.json says 'one row per `longhorn` PVC claim in the tree', and this claim lands on `zeta-local-path` -- the cluster default StorageClass (full-ai-cluster/nixos/modules/local-storage.nix), a node-local hostPath class with reclaimPolicy: Delete. It is still real disk on the box being provisioned, so it is recorded here rather than filtered out: the rendered zeta-local-path total is 193 GiB (117 in full-ai-cluster, 76 in the infra tree). RE-DERIVED 2026-08-22: it read 201 GiB (125 + 76) until hindsight's postgres moved off the default class onto longhorn, taking 8 GiB with it. single-node-budget.json's $comment_nodeDiskGib now prints BOTH this figure and the readiness extractor's 103 GiB, with the reason they differ: the extractor over-counts a platform `Blueprint` template (8 GiB, provisions nothing) and under-counts every claim that renders with no storageClassName and binds this class as the default (22 GiB). The extractor itself is unchanged -- a named gap, not a fixed one.",
      "liftsWhen": "the catalogue grows a row for this PVC's non-longhorn class, at which point the claim is covered and this entry matches nothing and fails as stale. (The second exit this entry used to name -- re-deriving single-node-budget.json's zeta-local-path figure -- was NOT an exit: that was done on 2026-08-22 and the finding is still here, because what makes it a finding is that no catalogue ROW covers the PVC, not what any comment records.)",
      "observed": "the chart renders a PersistentVolumeClaim of 10Gi x1 on \"zeta-local-path\" (no storageClassName; cluster default) that NO catalogue row covers — 10 GiB of real disk that no profile total includes"
    },
    {
      "key": "undeclared full-ai-cluster/mimir storage/mimir-alertmanager",
      "reason": "OUT OF THE LADDER'S DECLARED SCOPE, AND THE SCOPE IS NOW MEASURABLY UNDER-STATED. storage-profiles.json says 'one row per `longhorn` PVC claim in the tree', and this claim lands on `zeta-local-path` -- the cluster default StorageClass (full-ai-cluster/nixos/modules/local-storage.nix), a node-local hostPath class with reclaimPolicy: Delete. It is still real disk on the box being provisioned, so it is recorded here rather than filtered out: the rendered zeta-local-path total is 193 GiB (117 in full-ai-cluster, 76 in the infra tree). RE-DERIVED 2026-08-22: it read 201 GiB (125 + 76) until hindsight's postgres moved off the default class onto longhorn, taking 8 GiB with it. single-node-budget.json's $comment_nodeDiskGib now prints BOTH this figure and the readiness extractor's 103 GiB, with the reason they differ: the extractor over-counts a platform `Blueprint` template (8 GiB, provisions nothing) and under-counts every claim that renders with no storageClassName and binds this class as the default (22 GiB). The extractor itself is unchanged -- a named gap, not a fixed one.",
      "liftsWhen": "the catalogue grows a row for this PVC's non-longhorn class, at which point the claim is covered and this entry matches nothing and fails as stale. (The second exit this entry used to name -- re-deriving single-node-budget.json's zeta-local-path figure -- was NOT an exit: that was done on 2026-08-22 and the finding is still here, because what makes it a finding is that no catalogue ROW covers the PVC, not what any comment records.)",
      "observed": "the chart renders a PersistentVolumeClaim of 1Gi x1 on \"zeta-local-path\" (no storageClassName; cluster default) that NO catalogue row covers — 1 GiB of real disk that no profile total includes"
    },
    {
      "key": "undeclared full-ai-cluster/minio blob-store",
      "reason": "OUT OF THE LADDER'S DECLARED SCOPE, AND THE SCOPE IS NOW MEASURABLY UNDER-STATED. storage-profiles.json says 'one row per `longhorn` PVC claim in the tree', and this claim lands on `zeta-local-path` -- the cluster default StorageClass (full-ai-cluster/nixos/modules/local-storage.nix), a node-local hostPath class with reclaimPolicy: Delete. It is still real disk on the box being provisioned, so it is recorded here rather than filtered out: the rendered zeta-local-path total is 193 GiB (117 in full-ai-cluster, 76 in the infra tree). RE-DERIVED 2026-08-22: it read 201 GiB (125 + 76) until hindsight's postgres moved off the default class onto longhorn, taking 8 GiB with it. single-node-budget.json's $comment_nodeDiskGib now prints BOTH this figure and the readiness extractor's 103 GiB, with the reason they differ: the extractor over-counts a platform `Blueprint` template (8 GiB, provisions nothing) and under-counts every claim that renders with no storageClassName and binds this class as the default (22 GiB). The extractor itself is unchanged -- a named gap, not a fixed one.",
      "liftsWhen": "the catalogue grows a row for this PVC's non-longhorn class, at which point the claim is covered and this entry matches nothing and fails as stale. (The second exit this entry used to name -- re-deriving single-node-budget.json's zeta-local-path figure -- was NOT an exit: that was done on 2026-08-22 and the finding is still here, because what makes it a finding is that no catalogue ROW covers the PVC, not what any comment records.)",
      "observed": "the chart renders a PersistentVolumeClaim of 20Gi x1 on \"zeta-local-path\" that NO catalogue row covers — 20 GiB of real disk that no profile total includes"
    },
    {
      "key": "undeclared full-ai-cluster/seaweedfs blob-store-seaweedfs-all-in-one-data",
      "reason": "OUT OF THE LADDER'S DECLARED SCOPE, AND THE SCOPE IS NOW MEASURABLY UNDER-STATED. storage-profiles.json says 'one row per `longhorn` PVC claim in the tree', and this claim lands on `zeta-local-path` -- the cluster default StorageClass (full-ai-cluster/nixos/modules/local-storage.nix), a node-local hostPath class with reclaimPolicy: Delete. It is still real disk on the box being provisioned, so it is recorded here rather than filtered out: the rendered zeta-local-path total is 193 GiB (117 in full-ai-cluster, 76 in the infra tree). RE-DERIVED 2026-08-22: it read 201 GiB (125 + 76) until hindsight's postgres moved off the default class onto longhorn, taking 8 GiB with it. single-node-budget.json's $comment_nodeDiskGib now prints BOTH this figure and the readiness extractor's 103 GiB, with the reason they differ: the extractor over-counts a platform `Blueprint` template (8 GiB, provisions nothing) and under-counts every claim that renders with no storageClassName and binds this class as the default (22 GiB). The extractor itself is unchanged -- a named gap, not a fixed one.",
      "liftsWhen": "the catalogue grows a row for this PVC's non-longhorn class, at which point the claim is covered and this entry matches nothing and fails as stale. (The second exit this entry used to name -- re-deriving single-node-budget.json's zeta-local-path figure -- was NOT an exit: that was done on 2026-08-22 and the finding is still here, because what makes it a finding is that no catalogue ROW covers the PVC, not what any comment records.)",
      "observed": "the chart renders a PersistentVolumeClaim of 20Gi x1 on \"zeta-local-path\" that NO catalogue row covers — 20 GiB of real disk that no profile total includes"
    },
    {
      "key": "undeclared full-ai-cluster/spire spire-data/spire-server",
      "reason": "OUT OF THE LADDER'S DECLARED SCOPE, AND THE SCOPE IS NOW MEASURABLY UNDER-STATED. storage-profiles.json says 'one row per `longhorn` PVC claim in the tree', and this claim lands on `zeta-local-path` -- the cluster default StorageClass (full-ai-cluster/nixos/modules/local-storage.nix), a node-local hostPath class with reclaimPolicy: Delete. It is still real disk on the box being provisioned, so it is recorded here rather than filtered out: the rendered zeta-local-path total is 193 GiB (117 in full-ai-cluster, 76 in the infra tree). RE-DERIVED 2026-08-22: it read 201 GiB (125 + 76) until hindsight's postgres moved off the default class onto longhorn, taking 8 GiB with it. single-node-budget.json's $comment_nodeDiskGib now prints BOTH this figure and the readiness extractor's 103 GiB, with the reason they differ: the extractor over-counts a platform `Blueprint` template (8 GiB, provisions nothing) and under-counts every claim that renders with no storageClassName and binds this class as the default (22 GiB). The extractor itself is unchanged -- a named gap, not a fixed one.",
      "liftsWhen": "the catalogue grows a row for this PVC's non-longhorn class, at which point the claim is covered and this entry matches nothing and fails as stale. (The second exit this entry used to name -- re-deriving single-node-budget.json's zeta-local-path figure -- was NOT an exit: that was done on 2026-08-22 and the finding is still here, because what makes it a finding is that no catalogue ROW covers the PVC, not what any comment records.)",
      "observed": "the chart renders a PersistentVolumeClaim of 5Gi x1 on \"zeta-local-path\" that NO catalogue row covers — 5 GiB of real disk that no profile total includes"
    },
    {
      "key": "undeclared full-ai-cluster/vault audit/vault",
      "reason": "OUT OF THE LADDER'S DECLARED SCOPE, AND THE SCOPE IS NOW MEASURABLY UNDER-STATED. storage-profiles.json says 'one row per `longhorn` PVC claim in the tree', and this claim lands on `zeta-local-path` -- the cluster default StorageClass (full-ai-cluster/nixos/modules/local-storage.nix), a node-local hostPath class with reclaimPolicy: Delete. It is still real disk on the box being provisioned, so it is recorded here rather than filtered out: the rendered zeta-local-path total is 193 GiB (117 in full-ai-cluster, 76 in the infra tree). RE-DERIVED 2026-08-22: it read 201 GiB (125 + 76) until hindsight's postgres moved off the default class onto longhorn, taking 8 GiB with it. single-node-budget.json's $comment_nodeDiskGib now prints BOTH this figure and the readiness extractor's 103 GiB, with the reason they differ: the extractor over-counts a platform `Blueprint` template (8 GiB, provisions nothing) and under-counts every claim that renders with no storageClassName and binds this class as the default (22 GiB). The extractor itself is unchanged -- a named gap, not a fixed one.",
      "liftsWhen": "the catalogue grows a row for this PVC's non-longhorn class, at which point the claim is covered and this entry matches nothing and fails as stale. (The second exit this entry used to name -- re-deriving single-node-budget.json's zeta-local-path figure -- was NOT an exit: that was done on 2026-08-22 and the finding is still here, because what makes it a finding is that no catalogue ROW covers the PVC, not what any comment records.)",
      "observed": "the chart renders a PersistentVolumeClaim of 10Gi x1 on \"zeta-local-path\" that NO catalogue row covers — 10 GiB of real disk that no profile total includes"
    },
    {
      "key": "undeclared full-ai-cluster/vault data/vault",
      "reason": "OUT OF THE LADDER'S DECLARED SCOPE, AND THE SCOPE IS NOW MEASURABLY UNDER-STATED. storage-profiles.json says 'one row per `longhorn` PVC claim in the tree', and this claim lands on `zeta-local-path` -- the cluster default StorageClass (full-ai-cluster/nixos/modules/local-storage.nix), a node-local hostPath class with reclaimPolicy: Delete. It is still real disk on the box being provisioned, so it is recorded here rather than filtered out: the rendered zeta-local-path total is 193 GiB (117 in full-ai-cluster, 76 in the infra tree). RE-DERIVED 2026-08-22: it read 201 GiB (125 + 76) until hindsight's postgres moved off the default class onto longhorn, taking 8 GiB with it. single-node-budget.json's $comment_nodeDiskGib now prints BOTH this figure and the readiness extractor's 103 GiB, with the reason they differ: the extractor over-counts a platform `Blueprint` template (8 GiB, provisions nothing) and under-counts every claim that renders with no storageClassName and binds this class as the default (22 GiB). The extractor itself is unchanged -- a named gap, not a fixed one.",
      "liftsWhen": "the catalogue grows a row for this PVC's non-longhorn class, at which point the claim is covered and this entry matches nothing and fails as stale. (The second exit this entry used to name -- re-deriving single-node-budget.json's zeta-local-path figure -- was NOT an exit: that was done on 2026-08-22 and the finding is still here, because what makes it a finding is that no catalogue ROW covers the PVC, not what any comment records.)",
      "observed": "the chart renders a PersistentVolumeClaim of 20Gi x1 on \"zeta-local-path\" that NO catalogue row covers — 20 GiB of real disk that no profile total includes"
    },
    {
      "key": "undeclared infra/gitlab data/gitlab-postgresql",
      "reason": "OUT OF THE LADDER'S DECLARED SCOPE, AND THE SCOPE IS NOW MEASURABLY UNDER-STATED. storage-profiles.json says 'one row per `longhorn` PVC claim in the tree', and this claim lands on `zeta-local-path` -- the cluster default StorageClass (full-ai-cluster/nixos/modules/local-storage.nix), a node-local hostPath class with reclaimPolicy: Delete. It is still real disk on the box being provisioned, so it is recorded here rather than filtered out: the rendered zeta-local-path total is 193 GiB (117 in full-ai-cluster, 76 in the infra tree). RE-DERIVED 2026-08-22: it read 201 GiB (125 + 76) until hindsight's postgres moved off the default class onto longhorn, taking 8 GiB with it. single-node-budget.json's $comment_nodeDiskGib now prints BOTH this figure and the readiness extractor's 103 GiB, with the reason they differ: the extractor over-counts a platform `Blueprint` template (8 GiB, provisions nothing) and under-counts every claim that renders with no storageClassName and binds this class as the default (22 GiB). The extractor itself is unchanged -- a named gap, not a fixed one.",
      "liftsWhen": "the catalogue grows a row for this PVC's non-longhorn class, at which point the claim is covered and this entry matches nothing and fails as stale. (The second exit this entry used to name -- re-deriving single-node-budget.json's zeta-local-path figure -- was NOT an exit: that was done on 2026-08-22 and the finding is still here, because what makes it a finding is that no catalogue ROW covers the PVC, not what any comment records.)",
      "observed": "the chart renders a PersistentVolumeClaim of 8Gi x1 on \"zeta-local-path\" (no storageClassName; cluster default) that NO catalogue row covers — 8 GiB of real disk that no profile total includes"
    },
    {
      "key": "undeclared infra/gitlab gitlab-minio",
      "reason": "OUT OF THE LADDER'S DECLARED SCOPE, AND THE SCOPE IS NOW MEASURABLY UNDER-STATED. storage-profiles.json says 'one row per `longhorn` PVC claim in the tree', and this claim lands on `zeta-local-path` -- the cluster default StorageClass (full-ai-cluster/nixos/modules/local-storage.nix), a node-local hostPath class with reclaimPolicy: Delete. It is still real disk on the box being provisioned, so it is recorded here rather than filtered out: the rendered zeta-local-path total is 193 GiB (117 in full-ai-cluster, 76 in the infra tree). RE-DERIVED 2026-08-22: it read 201 GiB (125 + 76) until hindsight's postgres moved off the default class onto longhorn, taking 8 GiB with it. single-node-budget.json's $comment_nodeDiskGib now prints BOTH this figure and the readiness extractor's 103 GiB, with the reason they differ: the extractor over-counts a platform `Blueprint` template (8 GiB, provisions nothing) and under-counts every claim that renders with no storageClassName and binds this class as the default (22 GiB). The extractor itself is unchanged -- a named gap, not a fixed one.",
      "liftsWhen": "the catalogue grows a row for this PVC's non-longhorn class, at which point the claim is covered and this entry matches nothing and fails as stale. (The second exit this entry used to name -- re-deriving single-node-budget.json's zeta-local-path figure -- was NOT an exit: that was done on 2026-08-22 and the finding is still here, because what makes it a finding is that no catalogue ROW covers the PVC, not what any comment records.)",
      "observed": "the chart renders a PersistentVolumeClaim of 10Gi x1 on \"zeta-local-path\" (no storageClassName; cluster default) that NO catalogue row covers — 10 GiB of real disk that no profile total includes"
    },
    {
      "key": "undeclared infra/gitlab redis-data/gitlab-redis-master",
      "reason": "OUT OF THE LADDER'S DECLARED SCOPE, AND THE SCOPE IS NOW MEASURABLY UNDER-STATED. storage-profiles.json says 'one row per `longhorn` PVC claim in the tree', and this claim lands on `zeta-local-path` -- the cluster default StorageClass (full-ai-cluster/nixos/modules/local-storage.nix), a node-local hostPath class with reclaimPolicy: Delete. It is still real disk on the box being provisioned, so it is recorded here rather than filtered out: the rendered zeta-local-path total is 193 GiB (117 in full-ai-cluster, 76 in the infra tree). RE-DERIVED 2026-08-22: it read 201 GiB (125 + 76) until hindsight's postgres moved off the default class onto longhorn, taking 8 GiB with it. single-node-budget.json's $comment_nodeDiskGib now prints BOTH this figure and the readiness extractor's 103 GiB, with the reason they differ: the extractor over-counts a platform `Blueprint` template (8 GiB, provisions nothing) and under-counts every claim that renders with no storageClassName and binds this class as the default (22 GiB). The extractor itself is unchanged -- a named gap, not a fixed one.",
      "liftsWhen": "the catalogue grows a row for this PVC's non-longhorn class, at which point the claim is covered and this entry matches nothing and fails as stale. (The second exit this entry used to name -- re-deriving single-node-budget.json's zeta-local-path figure -- was NOT an exit: that was done on 2026-08-22 and the finding is still here, because what makes it a finding is that no catalogue ROW covers the PVC, not what any comment records.)",
      "observed": "the chart renders a PersistentVolumeClaim of 8Gi x1 on \"zeta-local-path\" (no storageClassName; cluster default) that NO catalogue row covers — 8 GiB of real disk that no profile total includes"
    },
    {
      "key": "undeclared infra/gitlab repo-data/gitlab-gitaly",
      "reason": "OUT OF THE LADDER'S DECLARED SCOPE, AND THE SCOPE IS NOW MEASURABLY UNDER-STATED. storage-profiles.json says 'one row per `longhorn` PVC claim in the tree', and this claim lands on `zeta-local-path` -- the cluster default StorageClass (full-ai-cluster/nixos/modules/local-storage.nix), a node-local hostPath class with reclaimPolicy: Delete. It is still real disk on the box being provisioned, so it is recorded here rather than filtered out: the rendered zeta-local-path total is 193 GiB (117 in full-ai-cluster, 76 in the infra tree). RE-DERIVED 2026-08-22: it read 201 GiB (125 + 76) until hindsight's postgres moved off the default class onto longhorn, taking 8 GiB with it. single-node-budget.json's $comment_nodeDiskGib now prints BOTH this figure and the readiness extractor's 103 GiB, with the reason they differ: the extractor over-counts a platform `Blueprint` template (8 GiB, provisions nothing) and under-counts every claim that renders with no storageClassName and binds this class as the default (22 GiB). The extractor itself is unchanged -- a named gap, not a fixed one.",
      "liftsWhen": "the catalogue grows a row for this PVC's non-longhorn class, at which point the claim is covered and this entry matches nothing and fails as stale. (The second exit this entry used to name -- re-deriving single-node-budget.json's zeta-local-path figure -- was NOT an exit: that was done on 2026-08-22 and the finding is still here, because what makes it a finding is that no catalogue ROW covers the PVC, not what any comment records.)",
      "observed": "the chart renders a PersistentVolumeClaim of 50Gi x1 on \"zeta-local-path\" (no storageClassName; cluster default) that NO catalogue row covers — 50 GiB of real disk that no profile total includes"
    }
  ],
  "unrenderable": [
    {
      "key": "full-ai-cluster/temporal@0.59.0",
      "reason": "temporal 0.59.0 templates/server-job.yaml:73 refuses to template without a cassandra port for the default store. No catalogue row points at this app, so nothing is left unchecked.",
      "liftsWhen": "the Application configures its datastore explicitly so the chart templates, and the render is re-measured",
      "observed": "helm-template-failed"
    }
  ]
}
