{
  "$comment": [
    "Known-open findings from rendered-resource-requests.ts, each with a reason and a lift",
    "condition. An entry that matches nothing is STALE and fails the audit -- an acknowledgement",
    "that outlives its defect is how a baseline becomes a lie.",
    "",
    "Every liftsWhen here is the GATE ITSELF rather than a paraphrase of it. A lift condition",
    "stricter than its gate keeps an entry alive after the defect is gone, which has cost this",
    "repo a cycle twice."
  ],
  "entries": [
    {
      "key": "unreachable-git-path-request|*|full-ai-cluster/k8s/applications/cdi/cdi-operator.yaml#7|spec.template.spec.containers[0].resources.requests@x1",
      "reason": "100m / 150Mi, AND IT IS REACHABLE NOW -- this is acknowledged because governing it is REFUSED, not because it cannot be expressed. The manifest-coordinate class added 2026-08-23 addresses cdi-operator.yaml document 7, containers[0] exactly, and this row WAS governed for one draft of that change before being backed out. `cdi-operator.yaml` is vendored byte-for-byte from upstream and `--resource-profile <rung> --apply` would rewrite it, making the checked-in copy diverge from what upstream ships. That invariant was stated in the catalogue before this work and is not mine to overturn as a side effect of a CPU budget. REACHING A FILE IS NOT A LICENCE TO EDIT IT.",
      "liftsWhen": "LIFTS WHEN: `rendered-resource-requests.ts` stops reporting this exact key \u2014 which happens the moment EITHER a resourceClaim names the coordinate, OR the manifest stops declaring a request, OR the workload's replica count changes (the count is part of the key, so `replicas: 0` -> `1` makes this entry STALE and the new count OPEN, and the audit exits 1 on both). The condition IS the gate, not a paraphrase."
    },
    {
      "key": "unreachable-git-path-request|*|full-ai-cluster/k8s/applications/hat-system/deployment.yaml#0|spec.template.spec.containers[0].resources.requests@x0",
      "reason": "100m / 128Mi on the hat-system operator Deployment, which ships `replicas: 0` on purpose so ArgoCD reports OK while the operator image is still a `:placeholder` tag. Smallest of the four latent coordinates and the same blocker as the rest: `pods >= 1` makes a resourceClaim inexpressible for a zero-replica workload. Acknowledged, replica-count-keyed, and it fails the audit the moment a maintainer scales it.",
      "liftsWhen": "LIFTS WHEN: `rendered-resource-requests.ts` stops reporting this exact key \u2014 which happens the moment EITHER a resourceClaim names the coordinate, OR the manifest stops declaring a request, OR the workload's replica count changes (the count is part of the key, so `replicas: 0` -> `1` makes this entry STALE and the new count OPEN, and the audit exits 1 on both). The condition IS the gate, not a paraphrase."
    },
    {
      "key": "unreachable-git-path-request|*|full-ai-cluster/k8s/applications/kubevirt/kubevirt-operator.yaml#9|spec.template.spec.containers[0].resources.requests@x2",
      "reason": "10m / 450Mi per pod across `replicas: 2` -- 20m / 900Mi in the totals. Same refusal as cdi and with a second artifact behind it: single-node-budget.json says of this exact file `NOT edited down to 1 on purpose: that manifest is vendored byte-for-byte so it stays 1:1 with what runs ... Editing it would make the checked-in copy diverge from the cluster it documents, which is a worse lie than this one.` A rung applier that rewrote it would do precisely what that sentence forbids. It is also the smallest of the reachable-but-refused set, so the budget argument for overriding the invariant is at its weakest here.",
      "liftsWhen": "LIFTS WHEN: `rendered-resource-requests.ts` stops reporting this exact key \u2014 which happens the moment EITHER a resourceClaim names the coordinate, OR the manifest stops declaring a request, OR the workload's replica count changes (the count is part of the key, so `replicas: 0` -> `1` makes this entry STALE and the new count OPEN, and the audit exits 1 on both). The condition IS the gate, not a paraphrase."
    },
    {
      "key": "unreachable-git-path-request|*|full-ai-cluster/k8s/applications/vllm/deployment.yaml#1|spec.template.spec.containers[0].resources.requests@x0",
      "reason": "THE LARGEST LATENT REQUEST IN THE TREE: 4000m / 16384Mi, and it schedules nothing today because the Deployment ships `replicas: 0` and this Application's syncPolicy is manual-only. It is acknowledged rather than governed because IT CANNOT BE GOVERNED YET: a resourceClaim validates `pods >= 1`, so a row for a zero-replica workload would add a whole pod's reservation to every rung total and make the budget overstate by 4000m -- trading an under-count for an over-count. REPLICAS: 0 IS A REPRIEVE, NOT A FIT, exactly as gmod's gatekeeper webhook failure was; the key carries the replica count so the reprieve cannot expire silently. Sized in workitem 081M0QCJ5Y3087G0R001P50W6Q.",
      "liftsWhen": "LIFTS WHEN: `rendered-resource-requests.ts` stops reporting this exact key \u2014 which happens the moment EITHER a resourceClaim names the coordinate, OR the manifest stops declaring a request, OR the workload's replica count changes (the count is part of the key, so `replicas: 0` -> `1` makes this entry STALE and the new count OPEN, and the audit exits 1 on both). The condition IS the gate, not a paraphrase."
    }
  ]
}
