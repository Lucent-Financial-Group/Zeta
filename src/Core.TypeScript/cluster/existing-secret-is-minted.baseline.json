{
  "note": [
    "Applications that NAME a Secret nothing in the dev bootstrap roster mints.",
    "",
    "An entry here is a claim that the missing Secret is ACCEPTABLE, and the only",
    "acceptable reasons are structural: the Application never runs a pod in this",
    "lane, or the credential is metal-only and the Application is excluded here.",
    "'Someone will add it later' is not one of them -- that is the state this",
    "audit exists to make loud, and it was reached once already (redis).",
    "",
    "Every entry needs a `reason` and a `liftsWhen`. An entry that stops matching",
    "fails as STALE, which is how a lifted exclusion forces the question again."
  ],
  "refreshCommand": "bun src/Core.TypeScript/cluster/audit-existing-secret-is-minted.ts",
  "entries": [
    {
      "key": "redis|redis-auth",
      "reason": "THE BREAK THIS AUDIT WAS WRITTEN FROM, and it is already being repaired by someone else. The redis -> valkey migration (#16292) declared `auth.usersExistingSecret: redis-auth` with the comment 'create via Sealed Secret / Vault' -- true of metal, false of the dev lane, and nothing checked the difference. redis then sat OutOfSync/Progressing in the included proof on main (schedule run 33657954802), found by Riven, not by a check. PR #16362 mints `redis/redis-auth` at bring-up on the SAME roster as grafana and ziti, with no credential in git. Carried here rather than fixed here because duplicating that PR would collide with it; carried at all rather than left red because the audit must be able to land before the fix it names.",
      "liftsWhen": "#16362 adds redis-auth to DEV_BOOTSTRAP_SECRETS. The roster then mints it, this reference stops being refused, this entry matches nothing, and it fails as STALE -- which is the signal to delete it. This entry is DESIGNED to be short-lived."
    },
    {
      "key": "forgejo|forgejo-initial-admin",
      "reason": "STRUCTURAL: forgejo is the standby half of an either/or Git-host pair (gitlab is the default-on one) and ships MANUAL SYNC by design, so the dev lane applies the Application and never syncs it. No pod is ever created, so nothing ever looks for this Secret. Minting one would be a credential in a cluster for a workload that does not run -- the opposite of the tightening this audit is for. Recorded in APPLIED_BUT_UNASSERTED_REASONS on the same grounds.",
      "liftsWhen": "forgejo is synced in any lane -- the inverted-pair leg (gitlab off, forgejo on) that Aaron described on 2026-09-02 is the intended shape. At that moment a pod really does need this credential, and the honest fix is to add it to DEV_BOOTSTRAP_SECRETS rather than to re-acknowledge it here."
    },
    {
      "key": "temporal|temporal-default-store",
      "reason": "STRUCTURAL: temporal is in DEV_EXCLUDED_REASONS, so the dev lane never applies this Application at all and no cluster in this lane ever resolves the name. The credential is metal-only, alongside the external datastore it authenticates to -- the same store the pending 0.59.0 -> 1.6.0 migration has to rewrite from `server.config.persistence.default` to `.datastores.<name>`.",
      "liftsWhen": "temporal leaves DEV_EXCLUDED_REASONS. This entry then names a Secret an applied Application needs, and the fix is the roster, not this file."
    },
    {
      "key": "temporal|temporal-visibility-store",
      "reason": "STRUCTURAL, and the same reason as `temporal-default-store` above: temporal is dev-excluded, so nothing in this lane resolves the name. Listed SEPARATELY rather than folded into one entry because they are two distinct Secrets for two distinct datastores (default and visibility), and an acknowledgement that covered both under one key would keep applying if only one of them were ever created.",
      "liftsWhen": "temporal leaves DEV_EXCLUDED_REASONS, exactly as for the default store."
    }
  ]
}
