{
  "description": "G-Set CRDT cross-language conformance (the bottom rung of the algebra ladder; 081KSXN940008QG0R00171YAZW bus comms substrate). Every impl starts from `initialSet`, replays `ops` in order with the canonical string comparator, and must value-match `expectedReplayStates[i]` after op i AND `expectedFinalState` at the end. Sets are serialized in canonical ascending order, so equality is array-equality. Oracle #1 = TS reference; F# casts oracle #2; C#/Rust join later per the meet-in-the-middle 4-oracle.",
  "elementType": "string",
  "comparator": "ascending Unicode code-point order (matching UTF-8 byte order).",
  "initialSet": [
    "漢字",
    "b-005-ASCII"
  ],
  "ops": [
    {
      "op": "add",
      "arg": "Кириллица",
      "note": "grow"
    },
    {
      "op": "add",
      "arg": "b-005-ASCII",
      "note": "idempotent: already present, no change"
    },
    {
      "op": "union",
      "arg": [
        "�",
        "Кириллица",
        "𠜎"
      ],
      "note": "commutative+associative: unsorted input, b-001 already present"
    },
    {
      "op": "union",
      "arg": [],
      "note": "identity: union with empty is a no-op"
    },
    {
      "op": "add",
      "arg": "áéíóú",
      "note": "grow at the front"
    }
  ],
  "expectedReplayStates": [
    [
      "b-005-ASCII",
      "Кириллица",
      "漢字"
    ],
    [
      "b-005-ASCII",
      "Кириллица",
      "漢字"
    ],
    [
      "b-005-ASCII",
      "Кириллица",
      "漢字",
      "�",
      "𠜎"
    ],
    [
      "b-005-ASCII",
      "Кириллица",
      "漢字",
      "�",
      "𠜎"
    ],
    [
      "b-005-ASCII",
      "áéíóú",
      "Кириллица",
      "漢字",
      "�",
      "𠜎"
    ]
  ],
  "expectedFinalState": [
    "b-005-ASCII",
    "áéíóú",
    "Кириллица",
    "漢字",
    "�",
    "𠜎"
  ],
  "laws": {
    "idempotent": "union(a, a) == a",
    "commutative": "union(a, b) == union(b, a)",
    "associative": "union(union(a, b), c) == union(a, union(b, c))",
    "identity": "union(a, empty) == a"
  }
}