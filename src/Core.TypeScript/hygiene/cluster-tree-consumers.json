{
  "$comment": [
    "Roster of every surface OUTSIDE `infra/` that names the stale cluster tree.",
    "",
    "WHY THIS FILE EXISTS (081M00QCHWA087G0R000GKKRXD):",
    "The repo carries two declarations of one cluster. `full-ai-cluster/` is the",
    "live one \u2014 it is what `zeta-install.sh` runs (`nixos-install --flake",
    "/mnt/etc/zeta/full-ai-cluster#$HOST`) and the only flake CI evaluates.",
    "`infra/k8s` + `infra/nixos` are the older declaration. They COLLIDE: both",
    "declare Application/argocd/zeta-root with different spec.source.path and both",
    "carry prune+selfHeal, so whichever is applied last prunes the other tree's",
    "entire child graph. That collision is caught by single-node-readiness.ts and",
    "is currently suppressed by an `acknowledgedRootAppDuplicates` entry.",
    "",
    "The consolidation has now been deferred twice, both times for the same",
    "reason: nobody could show that the list of things still reading the stale",
    "tree was COMPLETE. A 2026-08-16 pass named two code consumers and called the",
    "list closed; there were nine. This file makes that list a derived, checked",
    "fact instead of a grep somebody ran once.",
    "",
    "HOW IT IS CHECKED: audit-cluster-tree-consumers.ts derives the real set with",
    "`git grep` and compares it to `consumers` below. A NEW reference to the stale",
    "tree fails the audit (re-divergence). A rostered path that no longer",
    "references it also fails (the roster must shrink as the migration lands, so",
    "it can never over-claim). Deletion of `infra/k8s` + `infra/nixos` is provably",
    "safe when every `blocking` and `derived` entry is gone.",
    "",
    "THE DELETION ITSELF IS GATED on maintainer sign-off \u2014 see the work item's",
    "`Done when` list. This roster does not authorize it; it makes it checkable."
  ],
  "stalePatterns": ["infra/k8s", "infra/nixos"],
  "survivingTree": "full-ai-cluster",
  "$comment_excludedPrefixes": [
    "Prose archives and the stale tree itself. These are frozen records (merged",
    "PR reviews, research ferries, work-item bodies) \u2014 rewriting them would",
    "falsify history, and none of them is read by a machine. Named explicitly",
    "rather than as a wildcard so the exclusion is auditable."
  ],
  "excludedPrefixes": [
    "infra/",
    "docs/github/",
    "docs/handoffs/",
    "docs/history/",
    "docs/pr-discussions/",
    "docs/research/",
    "workitems/"
  ],
  "$comment_dispositions": [
    "blocking \u2014 deleting the stale tree BREAKS this, or makes it emit output that",
    "           points at a path that no longer exists. Must be migrated first.",
    "derived  \u2014 mechanically follows the deletion (a roots list, an acknowledgement",
    "           entry). Edit in the same commit; nothing breaks if it lags.",
    "prose    \u2014 a comment or document. Breaks nothing; correct for accuracy.",
    "self     \u2014 this guard, its roster, and its mutation suite. They name the stale",
    "           tree because their subject IS the stale tree."
  ],
  "consumers": [
    {
      "path": ".github/workflows/build-ai-cluster-iso.yml",
      "disposition": "prose",
      "note": "Header comment describing the two installer substrates.",
      "migrateTo": "drop the two-substrate paragraph when there is one."
    },
    {
      "path": ".github/workflows/gate.yml",
      "disposition": "blocking",
      "note": "The `lint (yaml/k8s)` job runs `yamllint -d relaxed full-ai-cluster/k8s/ infra/k8s/ ...` and a `find full-ai-cluster/k8s infra/k8s ... | xargs kubeconform`. Both name infra/k8s as a literal path argument; `find` errors on a missing directory.",
      "migrateTo": "drop infra/k8s from both argument lists in the deletion commit."
    },
    {
      "path": ".github/workflows/helm-validate.yml",
      "disposition": "blocking",
      "note": "The whole workflow is keyed on infra/k8s: PR + push path filters, and four invocations of `bun infra/k8s/tests/validate-applications.ts`, a script that lives INSIDE the stale tree. Deleting the tree deletes the validator.",
      "migrateTo": "full-ai-cluster/k8s/applications via the validator's existing --apps-dir flag, AND move the validator out of infra/k8s/tests/. MEASURED 2026-08-17: pointed at the live tree it reports 224 passed, 14 failed \u2014 so this is not a one-line repoint. See the work item."
    },
    {
      "path": "db/search-index/inverted/files.txt",
      "disposition": "derived",
      "note": "The git-native inverted index's document list (081M0QTXTR3087G0R002R439FH). It names infra/k8s and infra/nixos because it names EVERY indexed path in the repo \u2014 an index of the tree mentions the whole tree by construction, so any surviving file under those directories puts them here. Machine-written, rebuilt from an explicit git rev every ~6h by search-index-cadence.yml; nothing reads these paths as a target.",
      "migrateTo": "nothing to do \u2014 the next scheduled rebuild after the deletion drops the lines, and this roster entry then goes stale and fails, which is the roster shrinking as designed."
    },
    {
      "path": ".gitignore",
      "disposition": "prose",
      "note": "Comment naming infra/nixos/hosts/<host>/hardware-configuration.nix.",
      "migrateTo": "drop or re-point the comment."
    },
    {
      "path": "db/mutation-findings/alexa.jsonl",
      "disposition": "prose",
      "note": "An append-only mutation-findings log, same class as soraya.jsonl above and rostered on the same reasoning. 202 records of {source, test, mutation, outcome, address}. It names the stale tree in exactly the same TWO paths \u2014 infra/k8s/tests/validate-bootstrap.ts and its .test.ts \u2014 recording that a mutation was run against the bootstrap validator. Nothing resolves against those paths; they are the SUBJECT of a past run, logged. Arrived on main via the agent-heartbeat batch merge #12466 and turned `lint (yaml/k8s)` red on main itself.",
      "migrateTo": "NONE \u2014 do not repoint, for the reason already recorded on soraya.jsonl: the mutation genuinely ran against that file at that path, and rewriting the log to name full-ai-cluster would falsify what was measured. Drop the row with the tree; the log is append-only and content-addressed."
    },
    {
      "path": "db/mutation-findings/otto.jsonl",
      "disposition": "prose",
      "note": "An append-only mutation-findings log, same class as soraya.jsonl and alexa.jsonl above and rostered on the same reasoning. 231 records of {source, test, mutation, outcome, address}. It names the stale tree in exactly the same TWO paths \u2014 infra/k8s/tests/validate-bootstrap.ts and its .test.ts \u2014 recording that a mutation was run against the bootstrap validator. Nothing resolves against those paths; they are the SUBJECT of a past run, logged. Arrived on main via the agent-heartbeat batch merge #12604 and turned `lint (yaml/k8s)`, `test (TS hermetic)`, and `lint (bash retirement inventory + hygiene unit tests)` red on main itself \u2014 all three assert this same roster.",
      "migrateTo": "NONE \u2014 do not repoint, for the reason already recorded on soraya.jsonl and alexa.jsonl: the mutation genuinely ran against that file at that path, and rewriting the log to name full-ai-cluster would falsify what was measured. Drop the row with the tree; the log is append-only and content-addressed."
    },
    {
      "path": "db/mutation-findings/soraya.jsonl",
      "disposition": "prose",
      "note": "An append-only mutation-findings log: 110 records of {source, test, mutation, outcome, address}. It names the stale tree in exactly TWO entries \u2014 infra/k8s/tests/validate-bootstrap.ts and its .test.ts \u2014 recording that a mutation was run against the bootstrap validator. Nothing resolves against those paths; they are the SUBJECT of a past run, logged. Landed via #12091.",
      "migrateTo": "NONE \u2014 do not repoint. The mutation genuinely ran against that file at that path, and rewriting the log to name full-ai-cluster would falsify what was measured. Drop the row with the tree; the log is append-only and its records are content-addressed."
    },
    {
      "path": "db/uncertainty/081M0BH05N7087G0R0037KFCJD-k8s-bootstrap-never-deployed-argocd-install-was-a-kustomize.md",
      "disposition": "prose",
      "note": "A \u0394U ledger entry recording that the infra/k8s bootstrap never deployed (argocd-install.yaml was a kustomize Kustomization, a build-time input, not an API object). It names infra/k8s because it DOCUMENTS AN EVENT IN THAT TREE \u2014 it is a historical record, not a live consumer, and nothing imports or resolves against the path.",
      "migrateTo": "NONE \u2014 do not repoint. The bug happened in infra/k8s and the record must stay accurate about where it happened; rewriting it to name full-ai-cluster would make the ledger lie about history. Leave the row when the tree goes; the entry is keyed and idempotent."
    },
    {
      "path": "docs/ZETA-CORE-TECHNOLOGY-FOR-MAX.md",
      "disposition": "prose",
      "note": "Partner-facing. Corrected 2026-08-17 to cite full-ai-cluster paths; the previous table listed infra/k8s/applications/cilium/ and .../argocd/ as `Deployed` and NEITHER DIRECTORY EXISTS. Still rostered because the corrected text names infra/ in the note explaining the older tree.",
      "migrateTo": "drop the historical note when the tree goes."
    },
    {
      "path": "flake.nix",
      "disposition": "blocking",
      "note": "nixosConfigurations.{control-plane,worker-gpu-01,worker-gpu-02} import ./infra/nixos/hosts/*/configuration.nix and nixosModules.{common,k3s-server,k3s-agent,gpu} ARE ./infra/nixos/modules/*.nix. VERIFIED 2026-08-17: `nix flake check --no-build` at the repo root evaluates all three hosts today, so deleting infra/nixos/ breaks it. NOT verified by CI \u2014 the only `nix flake check` in .github/workflows/build-ai-cluster-iso.yml runs with working-directory: full-ai-cluster. A root flake.lock landed 2026-08-26 (was absent until then); `nix flake check --no-build` still evaluates all three hosts against it. The lock pins nixos-hardware@master, which was the one genuinely floating input.",
      "migrateTo": "UNDECIDED \u2014 full-ai-cluster/flake.nix already declares control-plane and worker-gpu, on nixpkgs 25.11 against the root flake's 24.11. Repointing crosses a nixpkgs major; dropping the outputs breaks any off-repo machine tracking .#control-plane. Maintainer decision."
    },
    {
      "path": "full-ai-cluster/k8s/applications/gitlab/Application.yaml",
      "disposition": "prose",
      "note": "Comment CITING infra/k8s/applications/gitlab/Application.yaml as the evidence for one value. That sibling pins the same chart at the same 8.7.0 and already carries `global.ingress.configureCertmanager: false`, which is exactly why it renders and this one did not -- so the path is the checkable half of the reason and is named rather than paraphrased. No manifest field references the stale tree; deleting infra/ breaks nothing here but the citation.",
      "migrateTo": "when infra/ goes away, replace the citation with the measurement itself (helm template of gitlab 8.7.0 without that key fails on certmanager-issuer.email) -- the evidence outlives the sibling."
    },
    {
      "path": "full-ai-cluster/k8s/single-node-budget.json",
      "disposition": "derived",
      "note": "acknowledgedRootAppDuplicates suppresses the root-app-collision blocker. VERIFIED 2026-08-17: emptying it makes single-node-readiness.ts exit 1 on the live collision.",
      "migrateTo": "empty the array \u2014 the collision is gone once one tree is."
    },
    {
      "path": "full-ai-cluster/k8s/storage-profiles.json",
      "disposition": "blocking",
      "note": "One claim row (id infra/cockroachdb/data) names infra/k8s/applications/cockroachdb/Application.yaml. It is DELIBERATE, not stray coupling: argocd/zeta-root is declared in both trees, only one can own that identity in a cluster, and the catalogue counts BOTH cockroachdb rows because a bound that assumes the favourable tree is a bound that cannot convict. Classified `blocking` rather than `derived` because it is CHECKED, not assumed: storage-profiles.ts readField() degrades gracefully on a missing path (:286 returns found:false), but the --apply path THROWS at :468 (\"catalogue references a manifest that does not exist\"). So deleting infra/k8s breaks `storage-profiles --apply` until this row goes with it. MIGRATION: delete this one claim row in the same commit that deletes infra/k8s; the full-ai-cluster/cockroachdb/data row already carries the surviving tree's accounting and needs no change."
    },
    {
      "path": "src/Core.TypeScript/ace/deps.ts",
      "disposition": "blocking",
      "note": "generateArgoCD() emits Applications whose spec.source.path is `infra/k8s/applications/${chart}` \u2014 a path in the tree ArgoCD will prune. It ships in the ace CLI. deps.test.ts does not assert the path, so after the deletion this fails SILENTLY, in generated output, at deploy time.",
      "migrateTo": "full-ai-cluster/k8s/applications/${chart}, plus an assertion in deps.test.ts so the path can go red."
    },
    {
      "path": "src/Core.TypeScript/ci/cross-verify-roster.ts",
      "disposition": "prose",
      "note": "One line of the `image-source-provenance` rationale, carried verbatim from the gate.yml step this roster replaced (2026-08-26): it lists infra/k8s among the directories that audit scans. Prose only \u2014 no COMMAND in the roster names the stale tree.",
      "migrateTo": "drop `infra/k8s` from that sentence in the deletion commit; the audit itself scans every tracked YAML and needs no edit."
    },
    {
      "path": "src/Core.TypeScript/cluster/argocd-health-test.ts",
      "disposition": "prose",
      "note": "Comment crediting a regex to infra/k8s/tests/validate-applications.ts.",
      "migrateTo": "re-point the comment when the validator moves."
    },
    {
      "path": "src/Core.TypeScript/cluster/inert-valuesobject-keys.schema.json",
      "disposition": "derived",
      "note": "MACHINE-WRITTEN measurement (`inert-valuesobject-keys.ts --write-snapshot`). It names infra/k8s because it measures BOTH declarations of the cluster -- it reuses `discoverApplications` from rendered-storage-claims.ts, whose APPLICATION_ROOTS is already rostered above for the same reason. The literal path appears only inside `unavailable.detail` for the two git-path Applications there (`infra/orleans`), where the checker reports WHICH directory carries no Chart.yaml; naming it is the checkable half of that verdict. Nothing BREAKS when the stale tree is deleted -- the entries simply stop being produced -- but the checked-in snapshot would keep naming apps that no longer exist, and `--check-snapshot` would report them as GONE, which is drift rather than a crash.",
      "migrateTo": "re-run `bun src/Core.TypeScript/cluster/inert-valuesobject-keys.ts --write-snapshot` in the deletion commit; the infra entries leave the snapshot in the same pass that drops `infra/k8s/applications` from APPLICATION_ROOTS. No hand-editing: this file is owned by the generator."
    },
    {
      "path": "src/Core.TypeScript/cluster/manual-sync-policy.ts",
      "disposition": "prose",
      "note": "Two DOC-COMMENT references only, no import and no path construction: it points at infra/k8s/tests/validate-applications.ts as the OTHER consumer of this same manual-sync convention, and at FULL-AI-CLUSTER-FAILURE-BASELINE.md for the failure count. Both files exist at those paths today. The pointer is the POINT -- it is what tells the next reader that the render validator must adopt this module rather than grow a parallel convention, which is the drift this repo keeps catching. Migration target: repoint both mentions when the render validator moves out of infra/.",
      "migrateTo": ""
    },
    {
      "path": "src/Core.TypeScript/cluster/rendered-resource-requests.baseline.json",
      "disposition": "derived",
      "note": "Fourteen baselined `uncovered-application` findings, one per infra/ Application per rung, each with the measured total and the schema change that would cover it. They exist BECAUSE the second tree exists.",
      "migrateTo": "delete all fourteen entries when infra/k8s is deleted -- the audit reports an entry that matches nothing as STALE and exits 1, so the deletion forces it in the same commit rather than leaving them behind."
    },
    {
      "path": "src/Core.TypeScript/cluster/rendered-resource-requests.test.ts",
      "disposition": "derived",
      "note": "Pins `appTreeOf` against the cross-tree collision the checker had in draft: `full-ai-cluster` and `infra` each hold an Application called `gitlab` and one called `cockroachdb`, and splitting an appId on the first slash attributed one tree's render to the other tree's catalogue row.",
      "migrateTo": "the collision case needs TWO trees to exist; when there is one, replace the fixture with a synthetic second tree rather than deleting the test -- the function is still wrong without it."
    },
    {
      "path": "src/Core.TypeScript/cluster/rendered-resource-requests.ts",
      "disposition": "derived",
      "note": "Renders every discovered Application at both resource rungs. It does not TARGET infra/k8s -- it inherits the tree list from `discoverApplications`, which walks both roots -- and it names the path only in the finding text that says the CPU/memory ladder cannot cover a second tree. Nothing here breaks when infra/k8s goes; the finding simply stops being produced.",
      "migrateTo": "drop the `(it is in the \"<tree>\" tree; ...)` clause from `uncovered-application` when full-ai-cluster is the only tree, or leave it -- it is derived from `appTreeOf`, not from a literal."
    },
    {
      "path": "src/Core.TypeScript/cluster/rendered-storage-claims.ts",
      "disposition": "derived",
      "note": "APPLICATION_ROOTS lists `infra/k8s/applications` beside `full-ai-cluster/k8s/applications` so the render check covers BOTH declarations of the cluster. It has to: infra/cockroachdb carries a catalogue row (`infra/cockroachdb/data`, 8Gi x3), and infra/gitlab renders four PVCs totalling 76 GiB that no row declares. Nothing BREAKS when the tree is deleted -- `readdirIfPresent` returns an empty list for a missing directory rather than throwing -- but the checked-in render snapshot would keep naming apps that no longer exist and `--check-snapshot` would report them as GONE, which is drift, not a crash.",
      "migrateTo": "drop `infra/k8s/applications` from APPLICATION_ROOTS in the deletion commit and re-run `--write-snapshot`; the infra entries leave the snapshot in the same pass. The `infra/cockroachdb/data` row in storage-profiles.json goes with them (it is already noted there as living in the other app-of-apps tree)."
    },
    {
      "path": "src/Core.TypeScript/cluster/single-node-readiness.test.ts",
      "disposition": "derived",
      "note": "Test fixture asserting the tree-qualified namespacing of infra/k8s/applications/cockroachdb.",
      "migrateTo": "re-point the fixture at a surviving path."
    },
    {
      "path": "src/Core.TypeScript/cluster/single-node-readiness.ts",
      "disposition": "derived",
      "note": "MANIFEST_ROOTS lists infra/k8s/applications and infra/k8s/bootstrap so the auditor can see the collision at all.",
      "migrateTo": "remove both roots when the tree goes."
    },
    {
      "path": "src/Core.TypeScript/hygiene/audit-cluster-tree-consumers.test.ts",
      "disposition": "self",
      "note": "The mutation suite proving the audit can go red."
    },
    {
      "path": "src/Core.TypeScript/hygiene/audit-cluster-tree-consumers.ts",
      "disposition": "self",
      "note": "The audit that reads this roster."
    },
    {
      "path": "src/Core.TypeScript/hygiene/audit-local-helm-charts.test.ts",
      "disposition": "prose",
      "note": "One header comment naming infra/k8s/tests/validate-applications.test.ts as the mutation-suite pattern this file follows. A citation of prior art, not a path anything resolves.",
      "migrateTo": "Re-point the citation at wherever the Application mutation suite lands, or drop the sentence."
    },
    {
      "path": "src/Core.TypeScript/hygiene/audit-local-helm-charts.ts",
      "disposition": "prose",
      "note": "Header comments only. Names infra/k8s in two capacities, neither a dependency: (a) to record WHY this validator deliberately does NOT live in infra/k8s/tests/ next to the Application validators \u2014 this roster's own helm-validate.yml entry says 'move the validator out of infra/k8s/tests/', and landing it there was caught by this audit on the first CI run of #14499 and reverted; (b) to state that helm-validate.yml's path filters covered infra/k8s/** but never examples/, which is the measured gap the file closes. It opens no path under infra/; chart discovery walks from the repo root.",
      "migrateTo": "Drop paragraph (a) with the tree \u2014 once infra/k8s/tests/ is gone there is no alternative home left to justify against. (b) is history by then and can go too."
    },
    {
      "path": "src/Core.TypeScript/hygiene/cluster-tree-consumers.json",
      "disposition": "self",
      "note": "This roster."
    },
    {
      "path": "src/Core.TypeScript/hygiene/mise-pin-parity.test.ts",
      "disposition": "derived",
      "note": "Synthetic flake body in a fixture string mentions ./infra/nixos/hosts/control-plane/configuration.nix. Test data only; nothing reads the real path.",
      "migrateTo": "update the fixture string."
    },
    {
      "path": "src/wasm-dla/README.md",
      "disposition": "blocking",
      "note": "States the byte-lock toolchain (wabt, binaryen, emscripten, nodejs) is declared in infra/nixos/modules/common.nix. TRUE TODAY and it is the ONLY NixOS module that declares it \u2014 VERIFIED 2026-08-17: full-ai-cluster/nixos/modules/common.nix contains none of wabt/binaryen/emscripten/zig/rustup. This is content unique to the stale tree, not a stale citation.",
      "migrateTo": "carry the byte-lock toolchain block into full-ai-cluster/nixos/modules/common.nix (or decide on the record that cluster nodes do not need it and the root devShell is the only home), THEN re-point this line."
    }
  ]
}
