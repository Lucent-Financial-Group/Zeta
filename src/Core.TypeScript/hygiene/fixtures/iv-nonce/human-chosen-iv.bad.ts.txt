import { createCipheriv, createDecipheriv } from "node:crypto";
const key = Buffer.alloc(32);
// the shape this rule exists for: a constant a human picked
const a = createCipheriv("aes-256-gcm", key, "0000000000000007");
const b = createCipheriv("aes-256-gcm", key, Buffer.from("0000000000000007", "hex"));
const c = createCipheriv("aes-256-cbc", key, Buffer.alloc(16));
const d = createDecipheriv("aes-256-gcm", key, "0000000000000007");
