// Fixture: a bare `.ts` basename that also exists in the archive dir.
// It must classify `dead`, never `archived` — the archive fallback is `.md`-only.
export const fixtureShorthandTool = "not a rule pointer";
