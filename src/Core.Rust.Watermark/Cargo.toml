[package]
name = "zeta-watermark"
version = "0.1.0"
edition = "2021"
description = "Watermark — the event-time watermark (Akidau et al., The Dataflow Model), Rust oracle for Zeta.Core cross-language agreement."
license = "MIT"
publish = false

# Zero-dependency production core. serde_json is a DEV-only dependency, used solely to replay the
# shared golden seed in the cross-verify test.
[dependencies]

[dev-dependencies]
serde_json = "=1.0.151"
