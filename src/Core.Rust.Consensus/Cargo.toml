[package]
name = "zeta-consensus"
version = "0.1.0"
edition = "2021"
description = "BFT quorum consensus decision core — Rust oracle for Zeta.Core cross-language agreement."
license = "MIT"
publish = false

[dependencies]

[dev-dependencies]
serde_json = "=1.0.151"
