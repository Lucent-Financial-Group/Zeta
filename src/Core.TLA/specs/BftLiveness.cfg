SPECIFICATION FairSpec

CONSTANTS
    Nodes = {"otto", "vera", "riven", "lior"}
    Values = {"merge", "reject"}
    MaxFaulty = 1
    Byzantine = {"lior"}

\* The CONDITIONAL form, and the only liveness this spec is entitled to. See BftConsensus.cfg
\* for the falsification evidence and for why the unconditional form is false here.
PROPERTY ConditionalTermination
