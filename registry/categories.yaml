schema: zeta-registry/v1
total_slots: 16
entries:
  - id: 0
    name: Observation
  - id: 1
    name: Emission
  - id: 2
    name: Workflow
  - id: 3
    name: Heartbeat
  # Batch — branch-mode batch-merge transport (corporate leash, 081KSNY2Z0008QG0R0017JSTGD); slot reserved 2026-05-31, impl deferred (Max). Sovereign mode uses folders-on-main (081KSNY2Z0008QG0R000E5KTPX), no coordinator.
  - id: 4
    name: Batch
  - id: 5
    name: FrictionTelemetry
  - id: 6
    name: Bus
  - id: 7
    name: Spawn
  - id: 8
    name: WorkItem
  # ContentAddress registered in zeta-id/types.ts; yaml had drifted (backfilled 2026-07-02)
  - id: 9
    name: ContentAddress
  # Physical asset register — git-as-database inventory (inventory/items/), LFG hardware fleet
  - id: 10
    name: InventoryAsset
  # Channel — a multiplexed four-corner duplex channel over one physical transport (ZetaId-keyed, not GUID);
  # the "little program" self-describing channel key so the far side decodes it as a channel. Aaron 2026-07-04.
  # Byte-lock CLOSED 2026-07-04: Channel=11 + InventoryAsset=10 backfilled into Core.CSharp.ZetaId,
  # Core.FSharp.ZetaId, and the compiled types.js — all oracles now carry the same category set.
  # CORRECTION 2026-08-23: that last clause was false when written. `src/Core.Rust.ZetaId`
  # stopped at WORK_ITEM = 8 and carried neither 9, 10, 11 nor Extended = 15 — three oracles
  # agreed and the fourth was never checked, because nothing checked. Backfilled and now
  # enforced by `src/Core.TypeScript/zeta-id/category-vocabulary-agreement.test.ts`, which
  # reads all four oracle sources plus this file and fails on any disagreement.
  - id: 11
    name: Channel
  # Agenda — a declarer's voluntary agenda declaration, one file per declaration
  # (agendas/<zetaid>-<slug>.md) so no shared document has to be agreed on. Removes the
  # hidden consensus source that a single docs/AGENDA.md is. Allocated 2026-08-23 by
  # 081M0R3WHTH087G0R0015CH5PV under Aaron's authorization; design in
  # docs/DECISIONS/2026-08-23-zetaid-keyed-agenda-declarations.md.
  # Slots 13 and 14 remain free; Extended = 15 is the escape beyond 14, not a renumbering.
  - id: 12
    name: Agenda
