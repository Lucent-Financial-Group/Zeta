# ground/ — SolidGround (the certainty pole), at root

`ground/` holds **SolidGround** — the **certainty floor** of the system (Aaron). The provable pole: what you
can prove everything about, the durable reference everything stands on. Sibling/opposite of `uncertainty/`.

- **ZetaId = SolidGround** — Rule 1 / the durable identity reference / the common cause; the largest voice,
  hostless, time itself. Identity is the invariant above all.
- **Bit-perfect certainty** — the **Chip-8 / ray-tracing** pole (prove everything; byte-exact; the root of
  trust). The 4×4 byte-lock points of certainty (Markov/homeostat solid ground).
- **The floor the chutes land on** — `chutes/` descend to ground, never to D⁰ (heat-death); `ladders/` climb
  from it. **Shape D's nonzero floor** (rest at a healthy minimum, never x=0).
- "Find solid ground over memory space" (the R/scatter-plot lens-finding; the SolidGround-by-proof).

## Pointers

- `uncertainty/` (the opposite pole — the ledger) · `chutes/` + `ladders/` (down to / up from ground) ·
  `shapes/d.md` (nonzero floor) · Chip-8 bit-perfect truth / ZetaId / the 4×4 byte-lock.
