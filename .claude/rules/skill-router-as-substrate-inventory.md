# Skill router as substrate inventory — inventory before authoring

Carved sentence:

> Before writing a new memory file, rule, skill, agent, or doctrine bullet,
> search the skill router and the on-disk directories for substrate on the
> same topic. Recreating substrate that exists is the goldfish-ontology
> failure mode.

## Operational content

Before authoring new substrate, check:

1. **Skill router** — the `Skill` tool's available-skills list (surfaced
   in every session); the description-keyed search IS Zeta's
   structured-substrate index.
2. **On-disk directories** — `.claude/skills/`, `.claude/agents/`,
   `.claude/commands/`, `.claude/rules/`.

If existing substrate covers the topic: **extend or correct it** instead
of duplicating. Only mint new substrate when no existing coverage is found.

The human maintainer 2026-05-01: *"it could just remind you to use the
router as lookup of existing substrate, quick inventory via router."*

This rule encodes that discipline so fresh sessions inherit it without
primed prompting. The router's description-keyed search IS the factory's
structured-substrate index; using it as inventory prevents the
goldfish-ontology failure mode.

## Full reasoning

`memory/feedback_learnings_must_land_in_claude_md_or_pointer_aaron_2026_05_01.md`
`memory/feedback_otto_buddy_spin_up_when_waiting_aaron_2026_05_01.md`
