# temperatures/ — one temperature per file, carved sentence (core, date-agnostic)

One **temperature** per file, single-token filename, one carved sentence on its meaning in
our system (LLM sampling temperature, edge-of-chaos, explore/exploit, the ⊥-to-⊤ poles, the LLMTV temperature channel), anchored. A core
date-agnostic vocabulary folder (a DV2.0 hub — stable), distinct from the dated
`docs/research/*` satellites. Same discipline as `words/` and `letters/`.
