# wonder-compression

> Candidate: the compression we defined over Zeta-the-source — git-fs compression (content-addressing + canonical/symlink DAG + ety-preserves-history + master-index cache + ZetaId-seed + human-entropy-source). 'Wonder' = the expanse compressed into structure. Candidate word for a ZetaId (like/ intake).
