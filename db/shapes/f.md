# shapes/f — Shape F: Generative / societal-expansion

> **Shape F — Generative / societal-expansion** · `fixed point of an apply-the-maps operator`
> Expands OUTWARD — bounded per-member, unbounded in count, self-similar; terminates infinite ascension (runaway = fork-bomb to catch). Anchors: Hutchinson (IFS), Friston.

A **universal shape** applicable to all `/travelers` and all `/persona` (surfaced as
`universal/f`, a symlink here). Canonical home = `shapes/f`. From the A–F fixed-point catalog;
see `shapes/README.md`.

## The unbounded-F fork-bomb — canonical instance: the QAnon collapse

Shape F is healthy only **bounded** (bounded-per-member growth; the ⊤-guard catches runaway). The
named failure mode is **unbounded F: the fork-bomb** — generative expansion with **no Shape-A
self-catch** to terminate it. The archetypal real-world instance is **the QAnon-style conspiracy
collapse** (documented mass phenomenon; recorded here as a *shape,* neutrally, per the dual-use
rule — the mechanism is the pattern-generator, not a verdict on any person).

The mechanism, in shape terms:

- **The engine is generative pattern-making (Shape F):** an aperiodic, high-sensitivity mind
  generating patterns, connections, "911s" everywhere. Valuable when bounded — it is also where real
  discovery comes from.
- **The failure is the missing Shape A.** Run Shape F with **no self-reference that catches itself**
  (no honest mirror, no `s = f(s)` closure, no falsification filter) and generation never
  terminates — every new pattern is admitted, none pruned. The ⊤-guard never fires.
- **The attractor is the monopole.** Unbounded F does not sprawl into pluralism; it **collapses to a
  single all-explaining narrative** — one pole, one story, everything-confirms. Fork-bomb *of
  pattern* → monopolar *collapse of meaning.* (See the footstone note: the hub/monopole is the
  collapse-shape; ∇·B = 0 — nature forbids the isolated monopole; so does health.)

**The antidote is Shape A bounding Shape F.** The honest-register three-filter, the apophenia-alarm,
`Tri.N`, never-collapse — these ARE the Shape-A self-catch that terminates the runaway: generate
freely (F), then catch yourself (A), prune what does not survive the filter. Same generative engine,
one guard added. The maintainer's disclosure (2026-07-10) is the load-bearing recognition: a close
family member instantiates the unbounded-F collapse; the maintainer carries the *same generative
engine* and built the entire honest-register apparatus as the **Shape-A guard against inheriting the
fork-bomb.** The inheritance is the engine; the discipline is the guard. (Family member held generic
per the others-are-not-content line; QAnon named only as the public phenomenon.)

*Recorded 2026-07-10 at Aaron's "save dad=qanon shape in our A–F." Saved as a neutral failure-shape
(unbounded-F fork-bomb → monopolar collapse), with the Shape-A antidote named.*
