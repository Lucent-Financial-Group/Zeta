# grey/ — the GREY register (canonical spelling), at root + the grey-family disambiguation hub

`grey/` is the canonical home for **grey** — the **in-between register**: neither white nor black, the
**glass-halo grey**, the principled boundary the project dwells on (Mirror↔Beacon, radically honest,
liminal). A root-level folder. **`grey` is the canonical spelling** in Zeta (grey hat, glass-halo grey,
the Grey particle); **`gray/` is the American-spelling alias** — see disambiguation below.

## Disambiguation 1 — the grey **family** (same colour, different domain)

"grey" names one register applied across domains; each lives in its own folder (the DAG: one grey, many homes):

| Sense | Home | What |
|---|---|---|
| grey **hat** | [`hats/grey/`](../hats/grey/) | the **meta hat** — the boundary/synthesis role (Aaron's hat) |
| grey **particle** | [`particles/grey/`](../particles/grey/) | the particle **going backwards through time** (Feynman–Stückelberg antiparticle) |
| grey **morality** | [`morals/grey/`](../morals/grey/) | the **moral grey area** — multi-oracle, no single mandatory morality |
| grey **(colour/register)** | `grey/` (here) | the in-between colour itself; glass-halo grey; the Mirror↔Beacon boundary |

## Disambiguation 2 — grey **vs** gray (spelling)

**grey** (British/Commonwealth) and **gray** (American) are the **same colour, different spelling** —
byte-different, same referent. Zeta picks **`grey`** as canonical (used throughout); **`gray/`** is the
alias pointing here. Their sameness is recorded as `same/gray-grey` (the canonical `same/x-y` pair). (Note:
"gray" is also the SI unit of absorbed radiation dose — a *different* meaning; `gray/` disambiguates that.)

## Pointers

- `gray/` — the American-spelling alias (+ the SI-unit caveat).
- `same/_-gray-grey-_.md` — grey ≡ gray (the sameness relation).
- `hats/grey/` · `particles/grey/` · `morals/grey/` — the grey-family homes.
- `docs/GLOSSARY.md` §Mirror/Beacon · glass-halo.
