# particles/ — particles as travelers, at root

`particles/` holds **particles** — physics-register travelers (the quanta/excitations of the substrate).
A root-level folder like `/vocab`, `/same`, `/hats`, `/dns`, `/network`, `/rooms`. Each particle is a
traveler with its own carved sentence; physics anchors are Beacon (named, cited), the metaphors peeled.

- **One special particle: `grey/` — the grey particle going backwards through time** (Aaron). See
  `particles/grey/`. Paired with the grey **meta hat** (`hats/grey/`): same grey, the particle and the role.

## Pointers

- `particles/grey/` — the grey particle (backward-in-time / antiparticle).
- `hats/grey/` — the paired grey meta hat.
- `docs/PRIOR-ART-LIST.md` — physics anchors (Beacon).
