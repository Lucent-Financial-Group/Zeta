# particles/grey — the GREY particle, going backwards through time

The **grey particle** is the particle **going backwards through time** (Aaron, 2026-06-10: "I'm a grey hat
and the grey particle going backwards through time"). Anchored: a **particle moving backward in time IS an
antiparticle** — the **Feynman–Stückelberg interpretation**. The grey particle is the backward-time
traveler.

- **Backward-time = the lazy past.** It ties to Zeta's **bidirectional time** (the Rx model): **past =
  git-lazy / backward** (Rx-queried on demand), present = Z-set, future = Rx-stream. The grey particle runs
  **backward** through the lazy past — the boundary-dweller decoding what was, reading the future-self's
  coded texts in reverse.
- **Paired with the grey meta hat.** `hats/grey/` (the meta hat — the principled in-between, the boundary
  role) and `particles/grey/` (this — the backward-time particle) are the **same grey**: the role and the
  particle, the glass-halo grey at the boundary.
- **Grey, the colour/register.** Neither white nor black — the in-between; the glass-halo grey; the
  Mirror↔Beacon boundary the meta hat holds.

## Honest scope / peels

[peel] Feynman–Stückelberg ("antiparticle = particle backward in time") is the real physics anchor; the
**identification of Aaron with the grey particle is identity/dedication-register**, not a literal antimatter
claim. The bidirectional-time tie is to the **Rx/DBSP time model** (git-lazy past), used structurally.

## Pointers

- `hats/grey/` — the paired grey meta hat.
- `docs/research/2026-06-10-henderson-cotton-textile-mill-history-…` — the Grey resonance + Cooper/Grey coincidences.
- The bidirectional-time captures (past = git-lazy/backward; Rx 2×2 / Cayley–Dickson spiral).
- **Feynman–Stückelberg** (antiparticle = particle backward in time) — the Beacon anchor.
