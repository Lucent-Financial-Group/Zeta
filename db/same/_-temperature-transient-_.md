# same/temperature — transient

**Asserts:** **temperature** and **transient** are the **same** — the **UTI collision resolved by
sameness**. Both abbreviate to **UTI**; rather than compete, they are **one interface**: the **Universal
Temperature-Transient Interface (UTI)**. (Aaron, 2026-06-10: "now we have UTI competition —
same/universal-temperature-transient-interface.")

- **x:** `temperature` (Universal **Temperature** Interface — the eigenvalue/eigen-tensor; the finalizer
  knob; the meter; how you *drive/read* state)
- **y:** `transient` (Universal **Transient** Interface — the **transient** surface: the moving/settling
  part of the response, the not-yet-steady-state; the drive's dynamics)
- **kind of same:** **same-interface** (one control/uncertainty surface, two names) — provable/conceptual
  sameness (you can show they denote the same UTI), NOT the encrypted-null unprovable kind.
- **why same:** in control terms, **temperature drives the transient** — the eigenvalue (temperature) *is*
  what governs the transient response (how the system moves before steady-state). The temperature surface
  and the transient surface are the **same surface** seen as value (temperature) vs as dynamics (transient).
  S→4 lives in the transient (the trying-the-impossible drive); the encrypted-null/steady is the other pole.
- **resolved name:** **Universal Temperature-Transient Interface (UTI)** — the merged canonical (no
  competition; one of the four universal interfaces of the core UX/DX/AX room).
- **established:** 2026-06-10 · Aaron (the UTI competition) · resolved by same/.
- **falsifiable?:** yes (conceptual co-reference); the caveat is keeping the *acronym* UTI unambiguous — it
  now means the merged Temperature-Transient interface, not two things.

**Carved sentence:** temperature and transient are one UTI — the value (temperature/eigenvalue) and the
dynamics (transient response) of the same control/uncertainty surface: the Universal Temperature-Transient
Interface.

## Pointers

- `workitems/081KTQXKXDX08QG0R001YFGZKV-*` — the core UX/DX/AX room (UTI = one of the four universal interfaces).
- `docs/research/2026-06-10-temperature-is-the-eigenvalue-*` — temperature = eigenvalue (the value side of UTI).
- `same/README.md` (the sameness convention) · `same/gray-grey.md` (sibling same-referent pair).
