# ΔU: 081M0BH05N7087G0R0037KFCJD — k8s bootstrap never deployed — argocd-install was a kustomize input, not an API object

- **measure:** On a real K3S v1.31.4+k3s1 stood up with exactly the four manifests services.k3s.manifests declares, both addons failed with ApplyManifestFailed and an empty CHECKSUM, retrying every ~15s forever. Zero ArgoCD pods, zero argoproj.io CRDs, and nothing under infra/k8s/applications ever deployed — steps 3-9 of the documented bootstrap order did not happen. After converting to the helm.cattle.io/v1 HelmChart form: 6 ArgoCD pods Running, 3 CRDs registered, zeta-root Synced/Healthy with all seven child Applications discovered.
- **ΔU > 0 because:** The bootstrap path went from unmetered-and-broken to metered-and-verified-on-real-hardware. Before, infra/k8s/bootstrap had NOTHING witnessing it, and helm-validate.yml triggered on infra/k8s/** while its validator only scanned infra/k8s/applications — so a bootstrap change ran a lane that structurally could not see it and reported green. Uncertainty about whether a node could bootstrap at all was total and invisible; it is now bounded by a falsifier that runs per-PR.
- **witnessed by:** infra/k8s/tests/validate-bootstrap.ts + its 10-case mutation suite, wired into helm-validate.yml. Run against main's pre-fix file it reports 20 passed, 1 failed — it goes red on exactly the bug it was written for. It also guards both directions of the manifest roster, strict YAML, and unpinned HelmCharts.
- **lineage:** Found by measurement rather than reasoning: an agent booted real K3S with the manifests as they stood on main instead of reading them. The structural finding (trigger path wider than validator scope) is the fourth instance today of a check that appeared to protect something it could not see.
