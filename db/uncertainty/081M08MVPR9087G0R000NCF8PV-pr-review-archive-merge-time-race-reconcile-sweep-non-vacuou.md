# ΔU: 081M08MVPR9087G0R000NCF8PV — PR-review archive merge-time race: reconcile sweep + non-vacuous capture guard

- **measure:** Re-measured capture before fixing: doc-level under-report 30/150 = 20.0% (filed 21% holds), thread-level capture 191/257 = 74.3%, fetchable capture 191/191 = 100.0%. Mechanism confirmed as the merge-time race against fetched_at (LOSS = 0 of 150), which excludes pagination, API cap, permissions and parsing. Shipped reconcile-review-archive.ts: bounded --write sweep in the serialised archive duty, plus a --check guard that refuses to render a verdict without a positive control.
- **ΔU > 0 because:** A recorded zero was indistinguishable from a real zero, so every downstream reader of the archive had unbounded uncertainty about whether a review happened. There is now a repair that heals under-reports for any tail length and a guard that goes red if it stops, so the class is bounded and observable instead of silent.
- **witnessed by:** 43 tests in reconcile-review-archive.test.ts, 23/23 mutants killed by a mutation sweep including 'regression guard removed', 'accepts a zero-thread control' and 'a missing PR recorded as zero threads'; the --check CLI exits 1 against a doctored under-reporting archive doc
- **lineage:** Re-measurement with a new instrument (audit-review-archive-capture-window.ts) that compares thread times against the shard's fetched_at rather than merged_at
