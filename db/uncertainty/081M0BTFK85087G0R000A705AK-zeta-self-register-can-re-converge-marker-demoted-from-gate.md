# ΔU: 081M0BTFK85087G0R000A705AK — zeta-self-register can re-converge: marker demoted from gate to receipt, reconcile timer added

- **measure:** The host layer's registration unit moved from enrolment-only (one-shot, gated forever on a local marker written at PR-open time) to level-triggered convergence driven by a jittered 6h timer, with three named storm bounds on the load it introduces.
- **ΔU > 0 because:** A node whose registration was lost, reverted, or written against a stale identity previously had NO automated path back — the marker said done permanently, and no timer, healthcheck or reconciler existed anywhere under full-ai-cluster/nixos to undo it. That is unbounded uncertainty about whether any given node is actually registered, resolvable only by an operator reaching a node that is by hypothesis unreachable. It is now bounded by the reconcile interval.
- **witnessed by:** src/Core.TypeScript/ci/self-register-reconvergence.test.ts executes the real tools/installer/zeta-self-register.sh under stubbed gh/git; 11 of its 12 tests FAIL against the pre-fix script, including 're-converges after a prior successful registration when the node is wiped from main'. Plus audit-installer-substrate.ts's new forbidden-sentinel, verified to fire on the pre-fix module.
- **lineage:** Reported by Dejan in docs/research/2026-08-18-reaching-a-dark-node-the-recovery-planes-missing-half-*.md section 1.0 while designing the pre-k8s recovery plane; proposed there as open question 5's first workitem.
