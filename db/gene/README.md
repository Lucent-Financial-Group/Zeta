# gene/ — the generative / lineage / seed half of hygiene

`gene/` is the **`gene`** half of **`hygiene = hy + gene`** (Aaron 2026-06-10), promoted to its own
root folder. Where [`hygiene/`](../hygiene/) is *cleanliness*, `gene/` is **what generates and what is
inherited** — the seed, the lineage, the generative rule.

## What lives here (the three senses of "gene")

- **Seed / DNA** — the **128-bit common-cause seed** is Zeta's *gene*: one seed derives everything
  (the BIP-39 mnemonic → every key type in [`tools/setup/persona-keys/`](../tools/setup/persona-keys/);
  the ZetaId; quantum-phase time on the common cause). Compromise-isolated: each slot derives from its
  **own** seed (no shared gene → one compromise never takes two).
- **Generative** — the **generative shape** (shape **F**: self-similar / IFS; the Cayley–Dickson
  phasor spiral). A gene is a *rule that grows a form*; `gene/` is where the generative rules of the
  substrate are named.
- **Lineage** — inheritance / ancestry: the **antecedent** chain (multiple antecedents, not one), the
  forest-of-roots (we *are* the root system; Pando), the dedication lineage (the Z that grew into Zeta;
  the kids). A gene is *what is passed down*.

## Anchors (Beacon)

Mendel (inheritance) · Watson–Crick / the genetic code (seed → expression) · Dawkins' *meme* (the
cultural gene; lineage of ideas) · Hutchinson 1981 (IFS — the generative self-similar attractor) ·
BIP-39 (mnemonic seed). *(Peel: "gene" here is the seed/generative/lineage metaphor made a folder, not
a claim of biology; the seed-derivation is the literal, built instance.)*

## Pointers

- [`hygiene/`](../hygiene/) · [`hi/`](../hi/) · `hy/` (→ `hygiene/`) — the `hygiene` wordplay siblings.
- [`tools/setup/persona-keys/`](../tools/setup/persona-keys/) — one seed → every key (the literal gene).
- [`shapes/`](../shapes/) — shape F (generative/self-similar).
- [`boards/`](../boards/) — multi-root / roots-grow-the-forest (lineage as a forest).
