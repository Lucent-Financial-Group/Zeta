---
id: 081KTJAM4VV08QG0R000TR5FS3
type: task
state: backlog
priority: P2
slug: cell-runtime-surfaces-zs-zetashell-interactive-interpreter-r
title: "Cell runtime surfaces: zs (ZetaShell) = interactive interpreter/REPL like FSI over the IDL/DynamicValue/YinYang engine; zc (ZetaCell) = regular CLI + long-running durable non-interactive (daemon/service) mode. Same cell, two modes = the interactive/ephemeral vs non-interactive/durable cut (#6935): zs interactive/ephemeral (read-eval-print the spec live), zc durable (run the spec unattended; observe-loop, durable-rotational-heartbeat agent, k8s-on-hardware #6949). Also: represent the Zeta IDL spec (#6955) in the banana-split forms (#6922/#6931) — 4-tree zip (CMYK, exact/lossless canonical stored spec) + 3-tree+uncertainty (RGB generator + SoftValue, soft/constrained-channel/regeneration form). Naming flag: zs/zc are 2-letter commands -> PATH-collision check + naming-expert before public binary names (internal fine). Design/naming, not built; IDL-in-4-tree rests on the #6922 conjecture (routed to Soraya). Anchors: FSI/REPL, CLI+daemon dual-mode (redis-server/redis-cli), CMYK/RGB banana-split. Ties IDL 081KTJAEMZW."
created: 2026-06-08T00:37:50.075Z
depends_on: []
composes_with: []
---

# Cell runtime surfaces: zs (ZetaShell) = interactive interpreter/REPL like FSI over the IDL/DynamicValue/YinYang engine; zc (ZetaCell) = regular CLI + long-running durable non-interactive (daemon/service) mode. Same cell, two modes = the interactive/ephemeral vs non-interactive/durable cut (#6935): zs interactive/ephemeral (read-eval-print the spec live), zc durable (run the spec unattended; observe-loop, durable-rotational-heartbeat agent, k8s-on-hardware #6949). Also: represent the Zeta IDL spec (#6955) in the banana-split forms (#6922/#6931) — 4-tree zip (CMYK, exact/lossless canonical stored spec) + 3-tree+uncertainty (RGB generator + SoftValue, soft/constrained-channel/regeneration form). Naming flag: zs/zc are 2-letter commands -> PATH-collision check + naming-expert before public binary names (internal fine). Design/naming, not built; IDL-in-4-tree rests on the #6922 conjecture (routed to Soraya). Anchors: FSI/REPL, CLI+daemon dual-mode (redis-server/redis-cli), CMYK/RGB banana-split. Ties IDL 081KTJAEMZW

<!-- Work-item body. ZetaId-keyed (conflict-free, time-sortable). "Backlog" is a
     STATE = this folder; completion moves the file to workitems/done/YYYY/MM/.
     Identity is the zetaid prefix — resolve cross-refs by `081KTJAM4VV08QG0R000TR5FS3-*.md` glob. -->
