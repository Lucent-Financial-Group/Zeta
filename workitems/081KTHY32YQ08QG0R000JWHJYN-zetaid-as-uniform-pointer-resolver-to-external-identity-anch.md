---
id: 081KTHY32YQ08QG0R000JWHJYN
type: task
state: backlog
priority: P2
slug: zetaid-as-uniform-pointer-resolver-to-external-identity-anch
title: "ZetaId as uniform pointer/resolver to external identity anchors (OAuth/OIDC/FIDO-WebAuthn/SAML + Web3: Nostr/ENS/DID/VC/wallet/SIWE) resolved by the SAME mechanism as dependencies and secrets. Identity anchor = versioned resolvable coordinate (nostr:npub.., did:.., oauth:github/.., fido:credId, ens:.eth) with pluggable per-scheme resolvers (cf. 081KSNY2Z0008QG0R002A785QR per-host adapters); private half (keys/tokens) referenced never inlined (secret-ref discipline + #6915 one-way). Layered ABOVE the self-certifying yin/yang heartbeat identity (#6915) as optional, revocable credence boosters -> feed the #6912 credence query. Honest bound: central anchors import a trust dependency (manifesto §1) kept honest by revocability + fallback to emergent base; never load-bearing for existence (§3 weight-free). Architecture/layering, not built. Ties 081KTHTPPCD (ZetaId-as-pointer/generator)."
created: 2026-06-07T20:58:48.151Z
depends_on: []
composes_with: []
---

# ZetaId as uniform pointer/resolver to external identity anchors (OAuth/OIDC/FIDO-WebAuthn/SAML + Web3: Nostr/ENS/DID/VC/wallet/SIWE) resolved by the SAME mechanism as dependencies and secrets. Identity anchor = versioned resolvable coordinate (nostr:npub.., did:.., oauth:github/.., fido:credId, ens:.eth) with pluggable per-scheme resolvers (cf. 081KSNY2Z0008QG0R002A785QR per-host adapters); private half (keys/tokens) referenced never inlined (secret-ref discipline + #6915 one-way). Layered ABOVE the self-certifying yin/yang heartbeat identity (#6915) as optional, revocable credence boosters -> feed the #6912 credence query. Honest bound: central anchors import a trust dependency (manifesto §1) kept honest by revocability + fallback to emergent base; never load-bearing for existence (§3 weight-free). Architecture/layering, not built. Ties 081KTHTPPCD (ZetaId-as-pointer/generator).

<!-- Work-item body. ZetaId-keyed (conflict-free, time-sortable). "Backlog" is a
     STATE = this folder; completion moves the file to workitems/done/YYYY/MM/.
     Identity is the zetaid prefix — resolve cross-refs by `081KTHY32YQ08QG0R000JWHJYN-*.md` glob. -->
