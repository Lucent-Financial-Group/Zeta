---
id: 081M00S0YB4087G0R00109502S
type: bug
state: backlog
priority: P2
slug: service-install-is-not-idempotent-in-effect-manifesto-12-lau
title: "Service install is not idempotent in effect (manifesto 12): LaunchdAdapter.install and host-loop-bootstrap.sh:170 both bootout-then-load, killing an in-flight agent tick on every re-apply. Persona-derived labels cannot tell an unchanged spec from a changed one; a realization-content-derived label makes re-apply a no-op and makes rotation an intentional restart."
created: 2026-08-14T18:37:20.612Z
depends_on: []
composes_with: []
---

# Service install is not idempotent in effect (manifesto 12): LaunchdAdapter.install and host-loop-bootstrap.sh:170 both bootout-then-load, killing an in-flight agent tick on every re-apply. Persona-derived labels cannot tell an unchanged spec from a changed one; a realization-content-derived label makes re-apply a no-op and makes rotation an intentional restart.

<!-- Work-item body. ZetaId-keyed (conflict-free, time-sortable). "Backlog" is a
     STATE = this folder; completion moves the file to workitems/done/YYYY/MM/.
     Identity is the zetaid prefix — resolve cross-refs by `081M00S0YB4087G0R00109502S-*.md` glob. -->
