---
id: 081M00F4W0F087G0R001FE36PP
type: task
state: backlog
priority: P2
slug: symmetric-vs-braided-cartridge-gates-1-3-closed-goldens-trea
title: "symmetric-vs-braided cartridge: gates 1-3 closed, goldens+treaty remain"
created: 2026-08-14T15:44:43.535Z
depends_on: []
composes_with: []
---

# symmetric-vs-braided cartridge: gates 1-3 closed, goldens+treaty remain

<!-- Work-item body. ZetaId-keyed (conflict-free, time-sortable). "Backlog" is a
     STATE = this folder; completion moves the file to workitems/done/YYYY/MM/.
     Identity is the zetaid prefix — resolve cross-refs by `081M00F4W0F087G0R001FE36PP-*.md` glob. -->

## What is done

`db/shapes/cartridges/pending/symmetric-vs-braided.lines` has passed gates 1–3 of
`db/shapes/cartridges/pending/README.md`: the generator is registered with its cost row, the
renderer draws both panels from one word, and `ShapeAcceptance.geometryLaw` carries a
known-answer law the cartridge PASSES. `zeta shape accept` now reports `bytes` as the single
refusal.

## What remains, and whose it is

1. **`treaty fsharp bytes ratified` — the F# oracle's.** The suite is green and the render is
   deterministic; the byte-register attestation is still the oracle's own to write.
2. **`treaty otto meaning ratified` / other travelers' — each traveler's own, from their own
   frame.** Nobody has been asked. A treaty line written by anyone but its owner is a forgery of
   consent (manifesto §6), so this item is deliberately NOT actionable by the next agent to pick
   it up.
3. **Goldens.** Blocked *by* (2)/(1), not before them: `zeta shape render` refuses any cartridge
   the full gate refuses, and the full gate includes `bytes`. See the dated correction in the
   pending README — steps 4 and 5 there are listed in the wrong order.
4. **On promotion** (a move, not a copy): `git mv` into `db/shapes/cartridges/`, generate goldens,
   bump `ShapeCartridge.Tests`' catalog count 19 → 20. `ShapePendingTests."THE CONSENT GATE
   STANDS"` is the tripwire that will go red and say exactly this.

## Still open in-file

`issue drawing-is-not-a-proof` (soraya) stays open and narrowed: the gate now checks the drawing's
arithmetic, but whether these strokes DEPICT a symmetric-monoidal reading beside a braided one is
a human judgement and remains hers.
