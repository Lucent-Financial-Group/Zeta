# macOS Homebrew packages — installed by tools/setup/macos.sh.
# Add a package by appending a line. Comments start with `#`.
#
# System-level packages only. Language runtimes (dotnet, python,
# java, bun) are pinned via `.mise.toml`, NOT via brew — avoids
# Homebrew's release lag per round-29 discipline. Round 34
# migrated OpenJDK off brew onto mise.

# CI test substrate (the maintainer 2026-05-26 — "7z is another tools
# we need to declarative manage somewhere" + "it does not have to
# be exact version but just declarative managed like other thinks
# we manage at that level in install.sh"):
p7zip       # cascade #4 ISO content audit (7z list); also useful
            # for maintainers doing any iso/zip inspection locally
            # (`7z l ...`). Idempotent: brew install skips if present.

# AI agent harness (operator 2026-05-27 — maintainer-decided
# dependency; external vendor-agnostic tool from NousResearch):
hermes-agent  # "Self-improving AI agent that creates skills from
              # experience" (https://hermes-agent.nousresearch.com).
              # Vendor-agnostic AI harness; MIT license; bottled
              # binary via homebrew-core. Transitive deps auto-
              # resolved by brew (see `brew info hermes-agent` for
              # current list). Idempotent: brew install skips if
              # present.

# Local-LLM core primitive (operator 2026-05-30 — "core, not optional"; small
# CPU model = baseline substrate). macOS installs the ollama binary here; the
# pinned MODEL is pulled by common/local-llm.sh per manifests/local-llm (the
# model is the reproducible/pinned artifact; brew tracks latest ollama binary).
ollama  # CPU-served tiny model for the move-next selector + observe.ts
        # classifier + DST fixtures. Idempotent: brew install skips if present.

# USB/ISO QEMU acceptance substrate (operator 2026-05-31): B-0891 uses
# qemu-system-x86_64 for boot/full-install/retention scenarios and qemu-img
# for qcow2 snapshot/restart state preservation. The file-backed zflash
# image builder uses mtools' mcopy to write ESP payloads into raw images
# without mounting physical USB devices.
qemu tier=standard
mtools

# Formal-verification rung 3 — TLAPS (tlapm, TLA+ proof manager) toolchain
# (Aaron path-A 2026-06-05: opam source-build is the right long-term cross-OS
# solution — upstream tlapm's latest BINARY release is 2022, x86_64-only
# self-extracting .bin, NO arm64; opam compiles tlapm natively on arm64).
# opam = the OCaml package manager that builds tlapm from a pinned source
# commit (see common/tlaps.sh). The SMT backend (z3) is declared below.
# Bottled; idempotent (brew install skips if present).
opam tier=standard  # OCaml package manager — builds tlapm from source per common/tlaps.sh

# Container runtime for the effectful do_item executor (rationale: B-0964 §2). podman
# is the default OCI runtime: Apache-2.0, rootless, daemonless. Coexists with an
# existing Docker Desktop (separate VM + socket); do NOT install `podman-mac-helper`
# /`podman-docker` (they shadow the real docker CLI). macOS one-time bootstrap:
# `podman machine init && podman machine start`. Version unpinned (fast-mover CLI;
# brew default) per dep-pin-search-first-authority. Idempotent: brew skips if present.
podman tier=standard

# Z3 SMT solver — the formal-verification portfolio's SMT prover (tools/Z3Verify +
# Formal/Z3.Laws.Tests shell `z3 -in`). Was UNDECLARED (the test self-skips when absent =
# silent non-enforcement); declaring it closes B-1009. Also the SMT backend TLAPS shells
# out to (rung-3). Version unpinned (brew default) per dep-pin-search-first-authority;
# idempotent (brew skips if present). The Microsoft.Z3 NuGet is deliberately NOT used (no
# osx-arm64 native) — the CLI is the real dependency.
z3

# R statistical runtime — charting / grammar-of-graphics lens-finder family (Aaron 2026-06-08:
# "the patterns in R are timeless… scatter plots… to find solid ground over memory space").
# Pin per dep-pin-search-first (WebSearch 2026-06-08): brew formula `r`, GPL-2; brew default
# version; idempotent (brew skips if present). Symmetric with apt `r-base` + windows `r`.
r tier=standard

# tectonic — lean single-binary LaTeX engine (Leslie Lamport's LaTeX on a self-contained XeTeX;
# Aaron 2026-06-08 "pull in Lamport's LaTeX"). COST-AWARE: a few-MB binary that fetches packages
# on demand from a bundle — NOT texlive-full's gigabytes on every runner/devcontainer. Math/typeset
# rendering + LaTeX-source as an LLM-readable math representation. Pin per dep-pin-search-first
# (WebSearch 2026-06-08): brew formula `tectonic`, MIT; idempotent. Symmetric with windows
# `tectonic` (scoop main). Linux: best-effort gap — see the NOTE in manifests/apt.
tectonic tier=standard
gnupg  # GPG — commit/artifact signing (close-over GPG, Aaron 2026-06-09)

# Network plane — Reticulum + Headscale (Aaron 2026-06-10: "we are using Reticulum and
# Headscale ... install headscale and tailscale on this pc in install.sh declaratively").
# SOVEREIGN / self-hosted networking (close-over-everything at the network layer; NOT the
# SaaS): tailscale = the client/daemon (tailscaled) that joins the mesh; headscale-cli =
# the CLI to manage our self-hosted Headscale control server. Reticulum (the ZetaId overlay)
# lives in network/.
# VERIFIED (dep-pin-search-first, brew 2026-06-10): `tailscale` 1.98.5 ✓, `headscale-cli`
# 0.28.0 ✓. NOTE: the Headscale *server* is NOT a brew formula (`brew search headscale` →
# only `headscale-cli`) — it runs on the **zetacluster** (k8s/ArgoCD container), not the PC;
# the PC gets the client (`tailscale`) + the control CLI (`headscale-cli`). brew default
# version; idempotent (brew skips if present). Symmetry (follow-on): apt + windows tailscale;
# headscale server = cluster. See network/README "declared deps".
headscale-cli  # CLI for our self-hosted Headscale control server (the server runs on the zetacluster)
tailscale  # the client/daemon (tailscaled) — joins the Headscale-controlled mesh

# E prover -- first-order ATP for the formal-verification portfolio (rung-3)
eprover

# Agda — dependently-typed proof assistant; the cubical proof lane's binary
# (provided-view univalence obligation, 081KX1VE4G808QG0R003DCK3GV; routing:
# docs/letters/from-soraya-univalence-lane-routing.md). BOTTLED (no GHC source
# build — the ghcup+cabal heavy path in Soraya's spec is NOT needed on macOS;
# verified 2026-07-08: brew agda 2.8.0-r3, bottled arm64+x86_64, deps ghc@9.12/
# gmp/icu4c pulled by brew). SOFT PIN (named debt): brew tracks upstream Agda;
# the cubical LIBRARY release is pin-paired to the installed Agda version by
# common/agda-cubical.sh via manifests/from-agda-cubical — if brew's Agda
# drifts past the pin-pair table, that script warns + skips and the fix is a
# new manifest row (bump tag+commit together). tier=standard (pulls a full GHC
# ~1.5 GB; slim hosts skip, and agda-cubical.sh skips gracefully without it).
agda tier=standard


# ── WASM toolchain (Oracle 10 / DLA multi-compiler substrate) ─────────────────
# Mirrors the apt manifest additions. All four WASM compilers must be present
# on every developer workstation (macOS) so the Oracle 10 build is reproducible
# in desired-state. Desired-state rationale: Conjecture Z-7 (binary_size ⊥ D_f)
# requires all four compilers to produce binaries that can be re-verified at any
# time on any host — not just the CI runner that first built them.
#
# wabt: WebAssembly Binary Toolkit — wat2wasm, wasm2wat, wasm-validate, wasm-strip.
#   Provides the WAT (bare-metal) compiler substrate. Bottled arm64+x86_64.
#   Verified: brew formula `wabt` (2.0.36). Idempotent.
wabt
#
# lua: Lua 5.4 bytecode/script substrate (Oracle 15). Homebrew's formula is the
# macOS peer of apt's lua5.4 and the Windows manifest's lua package.
lua
#
# binaryen: wasm-opt, wasm-as, wasm-dis — WASM optimizer and toolchain.
#   Used by AssemblyScript (asc) internally. Bottled arm64+x86_64.
#   Verified: brew formula `binaryen` (119). Idempotent.
binaryen
#
# emscripten: C/C++ → WASM compiler (LLVM-based). Provides emcc.
#   Fourth WASM compiler substrate (C language). Bottled arm64+x86_64.
#   Verified: brew formula `emscripten` (4.x). Idempotent.
#   NOTE: emscripten pulls llvm as a dep (~500 MB); tier=standard to avoid
#   adding 500 MB to slim/CI-only hosts that don't need C→WASM compilation.
emscripten tier=standard

# ── Zig compiler (Oracle 11 / wasm32-freestanding substrate) ──────────────────
# Zig compiles to WASM natively (`zig build-exe -target wasm32-freestanding`)
# producing extremely small binaries (951B). Bottled arm64+x86_64.
# Verified: brew formula `zig` (0.13.0). Idempotent.
zig

# ── LLVM tools (Oracle 13 / LLVM IR substrate) ────────────────────────────────
# llc, llvm-as, opt — LLVM IR to WASM pipeline. Provides the LLVM IR layer as
# a distinct substrate between C source and WASM binary.
# NOTE: emscripten (above) already pulls llvm as a dep on macOS. This entry
# ensures llc/opt are on PATH even on hosts that skip emscripten (tier=standard).
# Verified: brew formula `llvm` (18.x). tier=standard (large dep ~500MB).
llvm tier=standard

# ── Rust (rustup) — wasm32-unknown-unknown target ─────────────────────────────
# rustup is the canonical Rust toolchain manager. The wasm32 target needs no
# manual `rustup target add` step: it is DECLARED on the mise rust entry
# (`targets = ["wasm32-unknown-unknown"]`), so `mise install` provisions it.
# NOTE: brew has a `rust` formula but it does NOT include rustup or target mgmt.
# Use the official rustup.rs installer (already in mise via the `rust` entry).
# rust — installed via mise rust plugin (wraps rustup, not brew)

# ── pandoc (document conversion — book/EPUB build) ────────────────────────────
# pandoc converts the book Markdown → EPUB (iPhone/e-reader) and other formats.
# Consumed by tools/book-build/build-book.ts. Declarative-managed per the
# single-source-of-truth discipline (the maintainer 2026-08-06: "download whatever
# tools we need and link them in our declarative installers for all our OS shields").
pandoc tier=standard

# ── YubiKey hardware-token substrate ──────────────────────────────────────────
# macOS counterpart of the block appended to manifests/apt. Deliberately SHORTER
# than the apt block, and the asymmetry is the point:
#
#   - no pcscd/libpcsclite1: macOS ships its own CCID stack (verified on the
#     maintainer's host -- the YubiKey appears under SPSmartCardsDataType with an
#     Apple ifd-ccid driver and no daemon installed by us).
#
#     NARROWED 2026-08-31. That sentence is true and was being read wider than
#     its evidence. What was measured is that Apple's stack drives THE YUBIKEY
#     with no daemon from us; what it does not establish is that Apple's stack
#     drives EVERY CCID device. It does not -- Apple's driver carries a fixed
#     VID/PID table, and a device absent from that table is not covered by this
#     row however present the stack is. Measured instance in the block below.
#     The row's conclusion (do not install pcscd on macOS) is UNCHANGED: the
#     defect there is a stale table, and a second PC/SC daemon does not fix a
#     table -- it fights the one that already owns the interface.
#   - no yubihsm-shell FORMULA: there is none. `brew info yubihsm-shell` resolves
#     to nothing (re-checked 2026-08-20 against formulae.brew.sh -- still 404, as
#     is yubihsm-connector). Yubico ships the YubiHSM SDK as a macOS .pkg, which
#     is why the working copy on the maintainer's host lives at /usr/local/bin
#     rather than /opt/homebrew/bin -- it did not come from a brew FORMULA.
#
#     SUPERSEDED 2026-08-20: it comes from a brew CASK. `yubihsm2-sdk` is in
#     homebrew/cask (version 2026-04) and installs that exact Yubico .pkg with a
#     digest Homebrew maintains. The row is in manifests/brew-cask, where the
#     evidence and the correction are written up in full. The paragraph above is
#     kept because the 404s it reports are still true; what was wrong was the
#     conclusion drawn from them -- a formula-namespace miss read as "no package".
#
# Verified against brew 2026-08-20: ykman 5.9.2, yubico-piv-tool 2.7.3.
ykman             # YubiKey CLI — PIV/FIDO2/OpenPGP/YubiHSM-Auth applet management
yubico-piv-tool   # PIV slot + attestation tooling

# OpenSC — `pkcs11-tool`, the generic PKCS#11 CLI. MEASURED MISSING on the
# maintainer's host 2026-08-20 (`pkcs11-tool` not on PATH) while ykman and the
# YubiHSM SDK were present, which is the specific asymmetry this row closes: the
# SDK ships yubihsm_pkcs11.dylib and nothing in the declared toolchain could load
# a PKCS#11 module. It is also the vendor-neutral half — the same binary drives a
# YubiKey PIV slot through OpenSC's own module and a YubiHSM through Yubico's, so
# a contributor is not locked to one vendor's CLI to exercise either device.
# Verified against the Homebrew API 2026-08-20: formula `opensc` 0.27.1, LGPL-2.1-or-later.
opensc            # pkcs11-tool + PKCS#11 module — vendor-neutral smartcard/HSM CLI

# ── libccid >= 1.5.3: NO BREW ROUTE, AND NONE IS NEEDED (2026-08-31) ──────────
# NOT a row. A measured absence, recorded so the next agent does not re-propose
# the install line that does not resolve. Prompted by Ani's finding that a
# CardContact SmartCard-HSM in Identiv's uTrust Token Flex housing is attached to
# the maintainer's host and invisible to the smartcard stack.
#
# THE DIAGNOSIS IS CONFIRMED, independently, from the host itself:
#   ioreg -r -c IOUSBHostDevice -l -n "uTrust Token Flex"
#     idVendor 1254 (0x04E6 Identiv) · idProduct 22566 (0x5826) · serial 5599...
#     "CCID Interface@0" bInterfaceClass 11 · UsbExclusiveOwner = usbsmartcardreaderd
#   /usr/libexec/SmartCardServices/drivers/ifd-ccid.bundle -- CFBundleShortVersionString 1.5.1
#     556 ifdProductID entries · 0x5816 and 0x5817 PRESENT · 0x5826 ABSENT
#     (absent for every vendor, not just 0x04E6)
# libccid added the Token Flex in 1.5.3; Apple ships 1.5.1. The gap is a stale
# VID/PID table, and nothing else. The device is enumerated and its CCID
# interface is claimed by Apple's daemon -- so this is NOT a missing-daemon
# problem, which is exactly why installing one would be the wrong move.
#
# THE PROPOSED REMEDY DOES NOT RESOLVE. `brew install ccid` was checked against
# the Homebrew API rather than recalled, with a control that passes:
#   https://formulae.brew.sh/api/formula/ccid.json    404  (GitHub Pages HTML)
#   https://formulae.brew.sh/api/cask/ccid.json       404
#   https://formulae.brew.sh/api/formula/libccid.json 404
#   https://formulae.brew.sh/api/formula.json         200 -- 8,580 formulae parsed;
#     the ONLY ccid/pcsc/smartcard name match is `pcsc-lite`
#   brew info --formula ccid  -> Error: No available formula ... Did you mean cidr?
#   brew info --formula opensc -> resolves (0.27.1)   <- the control
# This is the `yubihsm-shell` lesson from the block above, in its other polarity:
# there the 404s were read as "no package" when a CASK existed; here there is no
# formula, no cask, and no alternate spelling. `pcsc-lite` is not a substitute --
# it is the daemon, not the driver, and macOS already runs PC/SC.
#
# NOTE the 404 returns rc=0 with an HTML body. Read the status code, not $?.
#
# NOR IS THE HOST-CONFIG HALF SAFE ON ITS OWN. `sudo defaults write
# /Library/Preferences/com.apple.security.smartcard useIFDCCID -bool yes` is a
# real, live mechanism -- `strings` on
# /System/Library/CryptoTokenKit/usbsmartcardreaderd.slotd/.../usbsmartcardreaderd
# carries `useIFDCCID`, `com.apple.security.smartcard`, the built-in bundle path,
# AND `file:///usr/local/libexec/SmartCardServices/drivers/`. That pref points the
# daemon at the third-party drop-in directory. With no brew formula there is
# nothing to put in that directory, so setting it today aims a working stack at an
# empty dir -- and the plausible failure is not "no change", it is the YubiKey and
# YubiHSM paths that currently work breaking. Precondition first, pref second.
# (The domain does not exist on the maintainer's host as of 2026-08-31 -- checked.)
#
# THE LINUX ASYMMETRY IS NOT SYMMETRIC EITHER, and manifests/apt should not be
# read as covering this. It declares `pcscd`, whose control fields are
# `Depends: libccid | pcsc-ifd-handler`, so libccid arrives transitively -- but at
# the release's version, and that is where the coverage splits. Measured against
# the Ubuntu indices 2026-08-31:
#   dists/noble/universe/binary-amd64  libccid 1.5.5-1  (64,755 pkgs)  -> COVERS 0x5826
#   dists/jammy/universe/binary-amd64  libccid 1.5.0-2  (58,923 pkgs)  -> DOES NOT
#   dists/{noble,jammy}/main           libccid ABSENT
# So: noble covers this token, jammy does not, macOS does not. No apt row is
# needed or wanted (a hard-pinned libccid row would fight pcscd's own dependency).
#
# AND NO VERSION FLOOR EITHER -- asked 2026-08-31 and answered no, with reasons,
# because "a transitive dependency at an unpinned version" is otherwise exactly the
# shape that deserves one:
#   1. A `libccid (>= 1.5.3)` row is UNSATISFIABLE on jammy, which we support and
#      test (docker-ubuntu-jammy-install-sh-test.yml). It would convert a
#      hypothetical device-support gap into a CERTAIN install failure on a live
#      lane. That is a strictly worse trade than the gap.
#   2. It is the wrong KIND of check. Whether a reader works is a runtime property
#      of a host that has the device; a package version is a proxy for it, and the
#      macOS result above is precisely a case where the proxy said NO and the
#      property was YES. Assert the property, not the proxy.
#   3. The instrument that settles it already exists and is now installed:
#      `opensc-tool --list-readers` on a Linux host with the token attached, with a
#      known-good CCID device as the positive control. UNMEASURED -- no such host
#      exists in this fleet today. Named so it is a runnable experiment rather than
#      an assumption, in either direction.
# Note the macOS result does NOT transfer by analogy. macOS and Windows match on
# the USB CCID interface class; Linux's pcsc-lite loads an ifdhandler and libccid
# keeps a VID/PID list it extends every release -- which is itself evidence the
# Linux table is load-bearing where the macOS one was not. Evidence, not proof:
# see (3).
#
# WINDOWS: nothing to add. Windows drives CCID through its in-box usbccid class
# driver, which matches on the USB smartcard class rather than a shipped VID/PID
# table, so there is no table to be stale. A row here would no-op -- the vacuity
# class the pam-reattach block below names.
#
# ══ REFUTED, SAME DAY, BY MEASUREMENT. THE READER WORKS. ═════════════════════
# Everything above this line is CONFIRMED and stays. The paragraph that used to
# stand here did not: it claimed the stale table BLOCKED the secp256k1 falsifier,
# and it was wrong. `opensc` was installed (it was declared in this manifest and
# ABSENT on the host -- see the block below) and the reader enumerated at once:
#
#   $ opensc-tool --list-readers
#   Nr.  Card  Features  Name
#   0    Yes             Yubico YubiKey FIDO+CCID
#   1    Yes             Identiv uTrust Token Flex        <- card present, rc=0
#
# No `ccid` installed. No `useIFDCCID` set. `/usr/local/libexec/SmartCardServices/
# drivers/` still does not exist. So NOTHING IS BLOCKED and NOTHING IS TO BE
# INSTALLED. Recorded as measured-unnecessary rather than deleted, so the install
# line does not get re-proposed in six weeks.
#
# HYPOTHESIS, labelled as one: macOS most likely matches a standards-compliant
# reader on the USB CCID INTERFACE CLASS (0x0B) and consults the VID/PID table for
# quirks and non-compliant devices -- so a compliant reader is picked up
# generically and a missing table row costs nothing. NOT MEASURED. What is
# measured is only that it works.
#
# DO NOT RUN the `defaults write ... useIFDCCID -bool yes` line. It was already
# unsafe (it aims the daemon at a directory that does not exist); it is now also
# pointless, and the devices it could break include a live YubiHSM. There is no
# ordering that makes it safe because there is no first half to order it after.
#
# ── THE REUSABLE PART: A NEGATIVE FROM THE WRONG INSTRUMENT IS `unknown` ──────
# The chain broke at the conclusion, not at any observation. Every fact above was
# right. The inference that turned them into "the reader does not work" came from
# ONE probe:
#
#   security list-smartcards   ->   (empty)
#
# which lists CRYPTOTOKENKIT tokens -- applets with a CTK driver -- and not PC/SC
# readers. An empty CTK list never meant "no reader". It meant "no CTK driver for
# this applet", which is true, expected, and irrelevant. The instrument answered a
# different question and its silence was read as an answer to ours.
#
# THE CONTROL WAS AVAILABLE THE ENTIRE TIME and nobody ran it. Two other CCID
# devices were attached to the same host (YubiKey 0x1050:0x0406, YubiHSM
# 0x1050:0x0030). `security list-smartcards` is empty with those attached too --
# so a single run with a known-good device would have shown the instrument was
# measuring something else, immediately, before a week of driver archaeology.
#
# This block already knew the discipline and applied it to the OTHER half: the
# 404s above are trusted precisely BECAUSE `brew info --formula opensc` returns a
# 200 on the same transport. A control was demanded of the packaging question and
# not of the hardware question. That asymmetry, not the libccid table, is what
# cost the week.
#
#   Rule: a negative result is `unknown` until a POSITIVE CONTROL on the same
#   instrument has produced a positive. Sibling instances already on file --
#   `system_profiler SPUSBDataType` returns zero lines on this host (a check that
#   does not run, not an empty bus); a required check that never ran contributes
#   zero to a failure count and reads as green; exit code 2 is a check that never
#   ran, not one that failed.
#
# WHAT ACTUALLY BLOCKED THE FALSIFIER: `opensc` was declared here and never
# installed on any host. The falsifier in
# docs/research/2026-08-22-smartcard-hsm-secp256k1-is-unconfirmed-*.md was
# runnable the moment the hardware was attached; the tool that runs it was a line
# of text in this file and nothing else. That is a PACKAGING failure of exactly
# the class `tools/setup/manifest-realized.ts` now refuses.

# ── pam_reattach — Touch ID for sudo inside tmux/screen (2026-08-24) ──────────
# Aaron 2026-08-24: "we need to add this to our ace package manager dependency
# tracking and make sure it's part of our declarative setup ... then i can run
# the 2nd part, that one liner is simple enough for a vibe coder."
#
# This row exists to DELETE a hand-typed command. tools/setup/touchid-sudo.ts
# used to tell the operator to run `brew install pam-reattach` themselves, which
# is precisely the manual step a declarative setup is for. The verifier now
# treats this row as the expectation and goes RED when the formula is declared
# here and absent on the host, so the declaration is CHECKED rather than hoped
# for (touchid-sudo-config.ts, `reattachDeclared`).
#
# WHAT IT DOES. Without it, sudo's Touch ID prompt cannot appear inside tmux or
# screen: the multiplexer's session is not the user's Aqua GUI session, so
# pam_tid has nothing to draw on and silently falls back to the password.
# pam_reattach re-attaches the process to that GUI session first, which is why
# touchid-sudo.ts emits it BEFORE pam_tid in /etc/pam.d/sudo_local. MEASURED
# ABSENT on the maintainer's host 2026-08-24 (no .so at either Homebrew prefix)
# while pam_tid was configured -- so Touch ID sudo was already inert in the
# shell agent work actually runs in, and nothing said so.
#
# Verified against the Homebrew API 2026-08-24, not from memory:
#   https://formulae.brew.sh/api/formula/pam-reattach.json
#   name pam-reattach | tap homebrew/core | version 1.3 | license MIT
#   upstream https://github.com/fabianishere/pam_reattach | dependencies: []
#   bottled arm64_tahoe/sequoia/sonoma/ventura/monterey/big_sur + x86_64
# Installs the module at <brew --prefix>/lib/pam/pam_reattach.so. touchid-sudo.ts
# PROBES both /opt/homebrew and /usr/local rather than assuming a prefix, and
# reads the Mach-O header to check the slice matches the host arch -- an
# x86_64-only module on an Apple Silicon Mac is present and unloadable.
#
# NOT tier-tagged (so: slim, every host), deliberately. It is a single ~50 KB
# bottled module with no dependencies, and the hosts most likely to be tiered
# down are exactly the headless/CI ones where a silent password fallback is
# hardest to notice. A tier= token here would buy no measurable disk and would
# make macos-install-sh-test skip the row.
#
# macOS-ONLY BY NATURE, and it is NOT mirrored into manifests/apt or
# manifests/windows. PAM-module-for-Aqua has no Linux or Windows meaning; a row
# that silently no-ops on another OS is a declaration that constrains nothing --
# the vacuity class wearing a manifest's clothes. The asymmetry is the honest
# record, the same way the YubiKey block above is deliberately shorter than apt's.
pam-reattach  # PAM module: re-attach to the Aqua session so Touch ID sudo works in tmux/screen
