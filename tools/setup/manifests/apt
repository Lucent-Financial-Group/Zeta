# Debian/Ubuntu apt packages — installed by tools/setup/linux.sh.
# Add a package by appending a line. Comments start with `#`.
#
# System-level packages only. Language runtimes (dotnet, python,
# java, bun) are pinned via `.mise.toml`, NOT via apt. Round 34
# migrated OpenJDK off apt onto mise.

build-essential
curl
ca-certificates
git

# CI test substrate (the maintainer 2026-05-26 — declaratively
# managed at install.sh manifest level, not implicit-from-runner-image):
p7zip-full  # cascade #4 ISO content audit (7z list); ubuntu-24.04
            # default-installs but Linux maintainers running setup
            # locally need explicit declaration

# Local-LLM core primitive (operator 2026-05-30): the ollama Linux release is a
# .tar.zst (zstd), so common/local-llm.sh needs `tar --zstd` ⇒ the zstd binary.
zstd        # required to extract ollama-linux-<arch>.tar.zst

# .NET runtime native deps (mise installs the dotnet SDK; it needs these shared
# libs to RUN). Present on full ubuntu runners (implicit), MISSING on a minimal
# ubuntu:24.04 image — the docker-ubuntu-install-sh-test exposed this (dotnet
# exited with no status = missing libicu). Declaring them makes the entropy lever
# work on TRULY bare ubuntu. Per Microsoft Learn linux-scripted-manual deps;
# build-essential already pulls libstdc++6/libgcc-s1/zlib1g. Names are Ubuntu
# 24.04 (Noble: libicu74, libssl3t64 post-time_t-transition).
# linux.sh maps libicu74/libssl3t64 → libicu70/libssl3 on Ubuntu 22.04 (jammy).
libicu74          # ICU — .NET globalization (the classic "dotnet exited" cause)
libssl3t64        # OpenSSL 3 runtime (Noble t64 name)
libgssapi-krb5-2  # Kerberos/GSSAPI — .NET networking
tzdata            # timezone data — .NET DateTime

# Formal-verification rung 3 — TLAPS (tlapm, TLA+ proof manager) toolchain
# (Aaron path-A 2026-06-05: opam source-build is the right long-term cross-OS
# solution — upstream tlapm's latest BINARY release is 2022, x86_64-only, NO
# arm64; opam compiles tlapm natively on any arch). opam = the OCaml package
# manager that builds tlapm from a pinned source commit (see common/tlaps.sh).
# The SMT backend (z3) is declared below. Idempotent (apt skips if present).
opam  # OCaml package manager — builds tlapm from source per common/tlaps.sh

# USB/ISO QEMU acceptance substrate (operator 2026-05-31): B-0891 uses
# qemu-system-* for boot/full-install/retention scenarios and qemu-img
# for qcow2 snapshot/restart state preservation. The file-backed zflash
# image builder uses mtools' mcopy to write ESP payloads into raw images
# without mounting physical USB devices.
qemu-system-x86
qemu-utils
mtools

# Container runtime for the effectful do_item executor (rationale: B-0964 §2). podman
# is the default OCI runtime: Apache-2.0, rootless, daemonless; native on Linux (no
# VM). Version unpinned (fast-mover CLI) per dep-pin-search-first-authority.
podman
# Rootless-podman helpers: linux.sh runs `apt install --no-install-recommends`, which
# skips podman's Recommends — so a minimal Ubuntu needs these explicitly or rootless
# podman silently breaks. Per automated-tests-are-the-shield-assert-dont-skip: declare
# them so podman actually RUNS rootless, not just installs (a green install that can't
# run a container is a hole in the shield).
uidmap          # newuidmap/newgidmap — rootless user-namespace ID mapping
slirp4netns     # rootless container networking
fuse-overlayfs  # rootless overlay storage driver

# Z3 SMT solver — the formal-verification SMT prover (tools/Z3Verify + Formal/Z3.Laws.Tests
# shell `z3 -in`). Was UNDECLARED (test self-skips when absent = silent non-enforcement);
# closes B-1009. Also the SMT backend TLAPS shells out to (rung-3). Unpinned (apt default)
# per dep-pin-search-first-authority; idempotent.
z3

# R statistical runtime — charting / grammar-of-graphics lens-finder family (Aaron 2026-06-08:
# "the patterns in R are timeless… scatter plots… to find solid ground over memory space").
# Renders data-viz lenses of the state/memory field (ASCII-rendered for the LLM-TV). Pin per
# dep-pin-search-first (WebSearch 2026-06-08): Ubuntu 24.04 noble `r-base` (universe), GPL-2;
# idempotent. Windows carries it via the `r` line (symmetry exception r-base→r in manifest test).
r-base

# NOTE — tectonic (lean single-binary LaTeX engine; Lamport's LaTeX, cost-aware vs texlive-full's
# GBs) is declared in manifests/brew + manifests/windows where it is a clean package. It is NOT in
# Ubuntu apt (WebSearch 2026-06-08: absent from all noble components), and its official drop-sh
# installer drops the binary into CWD (not PATH), so it does NOT fit the one-liner-tools
# download-then-exec shape cleanly. Linux tectonic install is therefore a BEST-EFFORT GAP pending an
# install-graph decision (`cargo install tectonic`, or a GitHub-release extraction onto PATH) —
# Dejan/devops call (mirrors the kiro-cli Linux gap precedent in manifests/one-liner-tools).
gnupg           # GPG — commit/artifact signing (close-over GPG, Aaron 2026-06-09)

# CVC5 — SMT solver for the formal-verification portfolio (rung-3).
# Noble (24.04): cvc5 in universe apt. Jammy: linux.sh aliases cvc5→cvc4 + from-shim.
# E prover: manifests/from-autotools-tarball on Linux (Noble apt 3.0.03 aborts in CI);
# manifests/brew on macOS.
cvc5

# Agda — dependently-typed proof assistant; the cubical proof lane's binary
# (provided-view univalence obligation, 081KX1VE4G808QG0R003DCK3GV; routing:
# docs/letters/from-soraya-univalence-lane-routing.md). Binary apt package on
# every supported series (launchpad 2026-07-08: noble 2.6.3, plucky/questing
# 2.6.4.3, resolute+ 2.8.0) — no ghcup+cabal source build needed. SOFT PIN
# (named debt): apt tracks the series' Agda; common/agda-cubical.sh pin-pairs
# the cubical LIBRARY release to the installed version via
# manifests/from-agda-cubical (a series whose Agda has no pin row warns +
# skips the lane; fix = add the matching cubical release row). agda-stdlib is
# deliberately NOT required: the cubical library is independent of it.
agda

# ── WASM toolchain (Oracle 10 / DLA multi-compiler substrate) ─────────────────
# Three compilers proved substrate-independence of D_f ≈ 1.322 (Oracle 10):
#   WAT (bare metal), AssemblyScript (TypeScript→WASM), Go (GOOS=js GOARCH=wasm).
# A fourth compiler (Emscripten, C→WASM) is the next substrate (Conjecture Z-7).
# All four compilers must be present on every cluster node and CI runner so the
# WASM oracle build is reproducible in desired-state (not "works on my machine").
#
# wabt: WebAssembly Binary Toolkit — wat2wasm, wasm2wat, wasm-validate, wasm-strip.
#   Noble (24.04): wabt in universe apt. Provides the WAT (bare-metal) compiler.
#   Verified present: apt-cache show wabt (Noble 1.0.33-1).
wabt
#
# lua5.4: Lua bytecode/script substrate (Oracle 15). It belongs in the guarded
# manifest install, not a second late apt call after install.sh has completed
# the shared runtime graph.
lua5.4
#
# binaryen: wasm-opt, wasm-as, wasm-dis — WASM optimizer and toolchain.
#   Noble: binaryen in universe apt. Used by AssemblyScript (asc) internally for
#   optimization passes. Also provides wasm-opt for post-compile size reduction.
#   Verified present: apt-cache show binaryen (Noble 114-1).
binaryen
#
# emscripten: C/C++ → WASM compiler (LLVM-based, Emscripten SDK). Provides emcc.
#   Noble: emscripten in universe apt (3.x). Fourth WASM compiler substrate.
#   Rationale: C is the most historically significant compiled language; having
#   WAT + ASC + Go + C in the same panel makes the compiler-independence claim
#   nearly exhaustive (Conjecture Z-7: binary_size ⊥ D_f across all compilers).
#   Verified present: apt-cache show emscripten (Noble 3.1.6~dfsg-1).
emscripten
#
# AssemblyScript (asc) is a project-level tool, NOT an apt package. Install it
# with the repo's own package manager (`bun add -d assemblyscript`) and invoke
# it via the project's node_modules/.bin/asc.
#
# NOTE (081KZYAGTB6087G0R002W8CWQK): this comment used to claim asc came in via
# `pnpm add -g` and that pnpm was "already in mise". Neither was ever true —
# pnpm is not a mise tool, and it is no longer a root devDependency either. The
# only pnpm consumer is demo/identity-dla-site, which now provisions its own
# pinned pnpm from src/Core.TypeScript/discovery/identity-dla-pages-build.ts.
# nodejs is required as the asc runtime host.
nodejs          # AssemblyScript runtime host (asc CLI requires Node.js)

# ── LLVM tools (Oracle 13 / LLVM IR substrate) ────────────────────────────────
# llc, llvm-as, opt — LLVM IR to WASM pipeline. Provides the LLVM IR layer as
# a distinct substrate between C source and WASM binary (Conjecture Z-7 sixth
# substrate). Ubuntu Noble: `llvm` metapackage installs llc-18, llvm-as-18, opt-18.
# Verified: apt-cache show llvm (Noble 1:18.0-59~exp2).
llvm

# ── Zig compiler (Oracle 11 / wasm32-freestanding substrate) ──────────────────
# Zig compiles to WASM natively (`zig build-exe -target wasm32-freestanding`)
# producing extremely small binaries (951B). NOT in Ubuntu Noble apt — installed
# via the official Zig release tarball. Declared here for single-source-of-truth.
# Actual install: mise zig plugin (see .mise.toml `zig = "0.13.0"`) or
# tools/setup/common/install-zig.sh (downloads zig-linux-x86_64-0.13.0.tar.xz).
# zig — installed via mise zig plugin or install-zig.sh (not apt)

# ── Rust (rustup) — wasm32-unknown-unknown target ─────────────────────────────
# rustup is NOT in apt — installed via https://sh.rustup.rs. The wasm32 target
# needs no manual `rustup target add` step: it is DECLARED on the mise rust
# entry (`targets = ["wasm32-unknown-unknown"]`). Declared here for
# single-source-of-truth. Actual install: mise rust plugin (wraps rustup).
# See .mise.toml `rust = { version = "1.87.0", components = [...], targets = [...] }`.
# Produces the Rust WASM substrate (7.4KB opt).
# rust/cargo — installed via mise rust plugin (wraps rustup, not apt)

# ── pandoc (document conversion — book/EPUB build) ────────────────────────────
# pandoc converts the book Markdown → EPUB (iPhone/e-reader) and other formats.
# Consumed by tools/book-build/build-book.ts. Declarative-managed per the
# single-source-of-truth discipline (the maintainer 2026-08-06: "download whatever
# tools we need and link them in our declarative installers for all our OS shields").
# NOTE: apt entries are BARE package names (the awk parser passes the whole line to
# apt-get; a `tier=` suffix would be read as a version-pin — brew uses tier=, apt does NOT).
pandoc          # document conversion — book/EPUB build (tools/book-build)

# ── YubiKey / YubiHSM hardware-token substrate ────────────────────────────────
# Declarative per the maintainer's standing rule quoted in manifests/brew:
# "download whatever tools we need and link them in our declarative installers
# for all our OS shields". Hardware present on the maintainer's bench 2026-08-20:
# YubiHSM 2 (serial 39160506, fw 2.4.1) + 2x YubiKey 5 (fw 5.7.4).
#
# WHY LINUX NEEDS MORE THAN macOS HERE. macOS ships a CCID smartcard stack in the
# OS, so `brew install ykman` is sufficient there. Linux does not: without a
# running PC/SC daemon the CCID interface is invisible and ykman reports NO device
# even though it is plugged in and enumerated on USB. That is a silent
# half-working install -- the tool present, the device unreachable -- so pcscd and
# its client library are declared HERE rather than assumed from the runner image.
#
# udev: the yubikey-manager deb ships rules under /lib/udev/rules.d/ granting
# non-root access to the Yubico vendor id (0x1050). (The yubihsm-shell deb ships
# its own rules for the HSM, but that package is not declared here -- see below --
# so an HSM on a Linux host still needs those rules from Yubico's own repo.)
# Installing via apt is what puts them there; a pip/binary install of ykman does
# NOT, which is the usual reason a working macOS setup fails to reproduce on Linux.
#
# Verified installable on the runner 2026-08-20. Two corrections are recorded here
# because both are the same error in different disguises:
#
#   1. A first lookup returned 000 for four names. That is a FAILED REQUEST, not an
#      absent package; re-running returned 200. Reading a timeout as "not packaged"
#      is how a check that did not run gets treated as one that did.
#
#   2. yubihsm-shell WAS declared here on the strength of a 200 from
#      packages.ubuntu.com/noble, and CI then failed with
#      `E: Unable to locate package yubihsm-shell`. The 200 proved the package
#      exists IN THE DISTRO ARCHIVE. It did not prove it is installable FROM THE
#      SOURCES THIS RUNNER HAS CONFIGURED, which is the proposition that actually
#      had to hold. Verifying the weaker claim and treating it as the stronger one
#      is the whole failure mode -- so the entry is gone rather than papered over
#      with an apt-source change nobody asked for. It is now absent on BOTH
#      platforms (see manifests/brew), which is at least honest and symmetric:
#      the YubiHSM CLI is undeclared everywhere until it can be pinned properly.
#
# The four below are confirmed installable: CI installed them.
yubikey-manager   # ykman CLI — PIV/FIDO2/OpenPGP/YubiHSM-Auth applet management
yubico-piv-tool   # PIV slot + attestation tooling (hardware proof a key never left)
pcscd             # PC/SC smartcard daemon — REQUIRED for CCID; macOS has this built in
libpcsclite1      # PC/SC client library ykman links against
