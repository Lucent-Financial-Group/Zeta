# The PRIVATE key is NEVER committed — it lives in the GH Actions secret
# ZETA_TEST_INFRA_SSH_KEY (repo scope). The PUBLIC key IS committed (safe;
# --test-scoped trust only). Ignore everything except the docs + the .pub.
*
!.gitignore
!README.md
!zeta-test-infra.pub
