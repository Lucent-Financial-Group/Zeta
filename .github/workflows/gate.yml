# Zeta CI gate — Phase 1 per docs/research/ci-gate-inventory.md.
#
# Runs on every PR (opened / reopened / synchronize / ready_for_review)
# and every push to main, plus workflow_dispatch for manual triage.
#
# Discipline (design doc: docs/research/ci-workflow-design.md, human
# maintainer reviewed 2026-04-18; parity-swap landed round 32; matrix
# refresh 2026-04-24 per PR #375):
#   - Runners pinned to explicit versions (not -latest). Active build-
#     and-test matrix: macos-26, ubuntu-24.04, ubuntu-24.04-arm.
#     The ubuntu-slim low-memory verification leg moved to
#     `.github/workflows/low-memory.yml` per maintainer 2026-04-27
#     — too slow for per-PR gating; runs on every push to main +
#     nightly schedule (in practice every merge, since direct
#     pushes are blocked by branch protection).
#     Lint jobs pinned to ubuntu-24.04 (short-lived, OS-independent
#     work; bumped from 22.04 on 2026-04-28 per the human
#     maintainer's input + version-currency discipline —
#     ubuntu-latest = 24.04 since Jan 2025).
#     Windows AND macOS legs are non-blocking DRIFT CHECKS
#     (Aaron 2026-08-19 — quoted in full at the
#     `continue-on-error:` flag on `build-and-test`). They are not
#     "deferred", which is what this line said until 2026-08-19.
#     Cadence differs and that is deliberate: Windows runs
#     push-to-main only, macOS runs on EVERY PR — a drift check is
#     only worth its minutes where someone reads it, and the PR
#     checks list is the surface humans actually open. Measured
#     reason, not preference: of the last 500 completed gate push
#     runs only 82 (16.4%) executed their matrix at all — the rest
#     were cancelled by the concurrency group before any leg ran
#     (data/platform-drift.json, 2026-08-19). Push-only cadence
#     would have cut macOS observation by ~84%.
#   - Third-party actions SHA-pinned by full 40-char commit SHA;
#     trailing `# vX.Y.Z` comments for humans.
#   - permissions: contents: read at the workflow level; no job
#     elevates. The only secret referenced is the auto-generated
#     per-run secrets.GITHUB_TOKEN (see workflow-level env: block
#     below). setup/common/mise.sh intentionally masks this repo-
#     scoped token from mise/aqua cross-repo release lookups unless
#     a dedicated MISE_GITHUB_TOKEN or GITHUB_API_TOKEN is supplied.
#     The token inherits the read-only permissions; no write
#     escalation. Workflow-level scope chosen over per-step for DRY;
#     trade-off documented at the env: block.
#   - Concurrency: workflow-scoped; cancel-in-progress only for PR
#     events (main pushes queue so every main commit gets a record).
#   - fail-fast: false so one OS failure doesn't hide another.
#   - NuGet cache keyed on Directory.Packages.props hash; runner.os
#     prefixed so macOS + Linux caches don't collide.
#   - Rust toolchain cache (`install-v2-*` / `full-verify-v2-*`, added
#     2026-08-16). mise's rust backend wraps rustup, and its install dir
#     `~/.local/share/mise/installs/rust/1.87.0` is a SYMLINK to
#     `~/.cargo/bin` holding 0 bytes — the real toolchain (656 MB) lives
#     in `~/.rustup/toolchains/`, which was in NO cache path anywhere.
#     So a full `install-*` cache HIT still re-fetched
#     https://static.rust-lang.org/dist/channel-rust-1.87.0.toml on every
#     job (~50 s each) and took three PRs down on 2026-08-16 (#10903,
#     #10930, #10963) — each reported under the name of a lint that never
#     executed. Cached paths are `~/.rustup/toolchains/1.87.0-*`,
#     `settings.toml`, `update-hashes`, `~/.cargo`.
#     ONLY the pinned toolchain: the ubuntu-24.04 image already ships
#     `~/.rustup/toolchains/stable-*` (624 MB) that RUSTUP_TOOLCHAIN never
#     selects. Measured on the runner — whole-dir 1681 MB vs pinned-only
#     1486 MB against a 1270 MB baseline, i.e. +216 MB instead of +411 MB.
#     `1.87.0` here must stay in sync with `rust` in `.mise.toml`; a stale
#     glob degrades to the pre-2026-08-16 behaviour (CDN fetch), never to a
#     wrong toolchain. `!`-exclusion was tried and measured as INERT —
#     `path: ~/.rustup/toolchains` + `!~/.rustup/toolchains/stable-*`
#     stored the full 1681 MB, so the explicit glob is load-bearing.
#   - Three-way parity per GOVERNANCE §24: dev laptop, CI runner,
#     and (future) devcontainer all bootstrap via the same
#     `tools/setup/install.sh`. This makes TLC + Alloy jars, Lean/
#     elan, mise-pinned dotnet + python, and dotnet-tools available
#     on CI without a separate YAML-maintained list — the manifests
#     under `tools/setup/manifests/` are the single source of truth.
#   - `mise trust` policy: `.mise.toml` trust is ceremony that
#     becomes meaningful once branch-protection-on-main is live
#     (reviewer gate before any change lands). Until that flips,
#     `.mise.toml` changes get `threat-model-critic` reviewer-floor
#     cover per GOVERNANCE §20. See docs/security/V1-SECURITY-GOALS.md.

name: gate

on:
  pull_request:
    types: [opened, reopened, synchronize, ready_for_review]
  push:
    branches: [main]
  merge_group:
  workflow_dispatch:

permissions:
  contents: read

# Workflow-level env: exposes GITHUB_TOKEN to steps that need the
# current repository token. It is not a valid general-purpose token
# for mise/aqua release lookups against other repositories: GitHub
# returns 404 for some public release-tag endpoints when queried with
# the repo-scoped Actions token. setup/common/mise.sh therefore masks
# GITHUB_TOKEN during `mise install` unless a dedicated
# MISE_GITHUB_TOKEN or GITHUB_API_TOKEN is provided. See
# https://mise.jdx.dev/dev-tools/github-tokens.html for mise's token
# priority order.
env:
  GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  # Dynamic matrix for build-and-test: pre-merge events (pull_request
  # and merge_group) get Linux-only (production build path, fast —
  # keeps PR checks and merge-queue runs short). Push-to-main /
  # workflow_dispatch get the full set including macos-26
  # (developer-experience verification) and the Windows experimental
  # legs. Standard GitHub-hosted runners are free for public repos so
  # the post-merge run has no cost downside.
  matrix-setup:
    name: matrix setup
    runs-on: ubuntu-24.04
    timeout-minutes: 1
    outputs:
      os: ${{ steps.set.outputs.os }}
    steps:
      - id: set
        shell: bash
        run: |
          # Pre-merge (pull_request + merge_group): Linux + macos-26
          # (~5 min wall clock with macos in parallel). macos-26 runs on
          # every PR — moved back to per-PR cadence 2026-04-28 after
          # maintainer correction ("we pull out mac and codeql to merge
          # to main time instead of per pr, and then moved it back to
          # per pr"). The earlier per-merge-only split structurally
          # blocked the PR queue.
          #   CORRECTION 2026-08-19: this comment used to say macos-26 is
          #   "required by branch protection's required-status-checks".
          #   It is not, and has not been since the drift-and-heal flip:
          #   the `CI Gate` ruleset requires exactly ONE context,
          #   `gate (required)`. macos-26 blocked only transitively, by
          #   being a leg of `build-and-test` with no continue-on-error.
          #   As of 2026-08-19 it no longer blocks at all — the flag now
          #   covers it. The mechanism is still what anyone changing this
          #   has to reason about.
          # macos-26 STAYS on every PR even though it no longer blocks,
          # and the reason is coverage rather than habit. The two
          # post-merge surfaces that read drift legs
          # (drift-sweep.yml's BD001 detector and
          # platform-drift-report.ts) both query
          # `branch=main&event=push`, and 418 of the last 500 such runs
          # were CANCELLED by the concurrency group before a single leg
          # executed — 16.4% coverage (data/platform-drift.json,
          # 2026-08-19). The PR run is the one that reliably executes and
          # the one a human already has open. Moving macOS to push-only
          # would have been cheaper and would have made the check mostly
          # unobserved, which is the vacuity class.
          # Push-to-main / workflow_dispatch: full surface adds the two
          # Windows legs. Those are DRIFT CHECKS — non-blocking
          # permanently and by intent (Aaron 2026-08-19), not
          # experimental legs waiting to be promoted. The full statement,
          # quoted, is at the `continue-on-error:` flag on
          # `build-and-test`; read it before changing either this matrix
          # or that flag.
          if [ "${GH_EVENT}" = "pull_request" ] || [ "${GH_EVENT}" = "merge_group" ]; then
            echo 'os=["ubuntu-24.04","ubuntu-24.04-arm","macos-26"]' >> "$GITHUB_OUTPUT"
          else
            echo 'os=["ubuntu-24.04","ubuntu-24.04-arm","macos-26","windows-2025","windows-11-arm"]' >> "$GITHUB_OUTPUT"
          fi
        env:
          GH_EVENT: ${{ github.event_name }}

  # F# / .NET path filter for build-and-test step-gating (081KQGDBJ0008QG0R001MK4YPC).
  #
  # PR docs-only PRs (touching only docs/**, memory/**, openspec/**,
  # *.md at root, etc.) do NOT need to run F# install + dotnet build
  # + dotnet test — those steps take 5-10 minutes per OS-leg and
  # produce no signal on doc-only changes. This job emits a single
  # boolean `code` indicating whether code-substrate paths were
  # touched; build-and-test gates its expensive steps on it.
  #
  # Required-status-check compatibility: build-and-test STILL RUNS
  # on docs-only PRs (so the required check name is reported as
  # passing); only the heavy steps inside it are skipped. This avoids
  # tripping `code_quality severity:all` which reads skipped jobs as
  # failure rather than success.
  #
  # Default safety: any non-PR event (push to main, merge_group,
  # workflow_dispatch — no `schedule:` trigger is currently configured)
  # gets `code=true` via the fast-path step (no checkout needed).
  # Path-filter is a per-PR optimization, never a path that hides
  # a code change from main-tip CI.
  #
  # Job output composition (per Copilot review on PR #1185, 2026-05-02):
  # - Non-PR events: fast-path step `nonpr` writes `code=true` without
  #   any checkout — saves the full-history fetch on every push to main
  #   and every merge_group run.
  # - Pull-request events: checkout + git diff base..head + classify
  #   per the path-list below; `detect` step writes `code=true|false`.
  # - Job output `code` resolves via `||` to whichever step ran
  #   (GH Actions falsy-string fallback).
  path-filter:
    name: path filter
    runs-on: ubuntu-24.04
    # 5 min covers the full-history checkout (`fetch-depth: 0`) on
    # slow runners. The non-PR fast path runs in ~5 sec without any
    # checkout, well under the cap.
    timeout-minutes: 5
    outputs:
      code: ${{ steps.detect.outputs.code || steps.nonpr.outputs.code }}
    steps:
      # Fast-path for non-PR events: emit code=true without cloning
      # the repo. Push-to-main / merge_group / workflow_dispatch all
      # take this path — preserves the per-PR-optimization invariant
      # (path-filter is never the mechanism that hides a code change
      # from main-tip CI).
      - id: nonpr
        if: github.event_name != 'pull_request'
        shell: bash
        env:
          GH_EVENT: ${{ github.event_name }}
        run: |
          echo "Event ${GH_EVENT} (not pull_request) — emitting code=true (full build), no checkout needed."
          echo "code=true" >> "$GITHUB_OUTPUT"

      - name: Checkout
        if: github.event_name == 'pull_request'
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          # Need both PR base and head shas for git diff. fetch-depth: 0
          # gets full history; the 5-minute job timeout above is sized
          # for this on slow runners.
          fetch-depth: 0

      - id: detect
        if: github.event_name == 'pull_request'
        shell: bash
        env:
          PR_BASE_SHA: ${{ github.event.pull_request.base.sha }}
          PR_HEAD_SHA: ${{ github.event.pull_request.head.sha }}
        run: |
          set -euo pipefail

          # Diff base..head of the PR to enumerate changed files.
          CHANGED=$(git diff --name-only "${PR_BASE_SHA}" "${PR_HEAD_SHA}")
          echo "Changed files in PR:"
          # Two-space indent each line via parameter expansion (pure
          # bash; SC2001-clean — no `sed s/^/  /` shell-out).
          INDENTED=${CHANGED//$'\n'/$'\n'  }
          echo "  ${INDENTED}"

          # Pure-docs surfaces never trigger the F# build/test path:
          #   docs/**       — repo documentation (incl. backlog/, hygiene-history/, research/)
          #   memory/**     — in-repo memory files
          #   openspec/**   — behavioral specs (no F# touch)
          #   *.md at root  — README, AGENTS, GOVERNANCE, CLAUDE.md, etc.
          #   .claude/**    — skill / agent / rule definitions (text only)
          #   .github/ISSUE_TEMPLATE, .github/PULL_REQUEST_TEMPLATE — text only
          #   .gitignore / .gitattributes — text only
          #   *.txt / LICENSE — text only
          #   data/**       — generated telemetry served by Pages (tick shards + rollups,
          #                   drift / vault ledgers). NOT a build input: no .fs / .fsproj /
          #                   .cs / test file in the repo reads anything under data/
          #                   (verified 2026-08-13). Added when the telemetry lanes moved
          #                   off direct pushes to main onto flush PRs — every frame now
          #                   arrives as a PR, and running the full F# matrix on a JSON
          #                   frame that cannot affect it would saturate the queue for no
          #                   signal.
          #   demo/red/red-state.json — same: a regenerated dashboard snapshot.
          #
          # This narrows what the heavy STEPS run on; it does not narrow the gate. The
          # `gate (required)` check still runs and still reports on these PRs, and every
          # lint (semgrep included) is untouched. A diff touching code alongside data still
          # flips code=true — the allowlist is per-file, not per-PR.
          #
          # Anything else (incl. src/**, tests/**, tools/**, *.fs, *.fsproj,
          # *.csproj, .github/workflows/**, global.json, Directory.Packages.props,
          # Zeta.sln, .config/dotnet-tools.json) flips code=true.
          HAS_CODE="false"
          while IFS= read -r f; do
            [ -z "${f}" ] && continue
            case "${f}" in
              docs/*|memory/*|openspec/*|.claude/*|.github/ISSUE_TEMPLATE/*|.github/PULL_REQUEST_TEMPLATE.md|.github/copilot-instructions.md)
                ;;
              data/*|demo/red/red-state.json)
                ;;
              .gitignore|.gitattributes|LICENSE|*.txt)
                ;;
              *.md)
                # Root-level markdown (README, AGENTS, GOVERNANCE,
                # CLAUDE.md, etc.) — non-build surface.
                case "${f}" in
                  */*) HAS_CODE="true" ;;  # under a subdir we don't recognize as docs-only
                  *) ;;                     # at repo root → docs surface
                esac
                ;;
              *)
                HAS_CODE="true"
                ;;
            esac
          done <<< "${CHANGED}"

          if [ "${HAS_CODE}" = "true" ]; then
            echo "Code-substrate path touched — emitting code=true (full build)."
            echo "code=true" >> "$GITHUB_OUTPUT"
          else
            echo "Docs-only PR — emitting code=false (skip F# install/build/test)."
            echo "code=false" >> "$GITHUB_OUTPUT"
          fi

  build-and-test:
    # Final runner matrix (maintainer ask 2026-04-24): symmetric
    # across AceHack fork and LFG canonical — same legs run everywhere.
    # Standard GitHub-hosted runners are free for public repositories,
    # so the prior fork/LFG cost-opt split (which kept macOS off LFG)
    # no longer applies.
    #
    # BLOCKING legs (2) — the only platforms a red leg still stops:
    #   - ubuntu-24.04      Ubuntu 24.04 LTS x64 (4 CPU, 16 GB)
    #   - ubuntu-24.04-arm  Ubuntu 24.04 LTS arm64 (4 CPU, 16 GB)
    #
    # DRIFT-CHECK legs (3) — run, report, block nothing:
    #   - macos-26          macOS 26, Apple Silicon M1 (3 CPU, 7 GB)
    #                       every PR + every push to main
    #   - windows-2025      Windows Server 2025 x64 (4 CPU, 16 GB)
    #   - windows-11-arm    Windows 11 arm64 (4 CPU, 16 GB)
    #                       push to main / workflow_dispatch only
    #
    # Moved-to-nightly leg (per maintainer 2026-04-27):
    #   - ubuntu-slim       Ubuntu slim x64 (1 vCPU, 5 GB RAM;
    #                       15-minute HARD job cap). Too slow for
    #                       per-PR gating (~10+ min vs ~1.5 min on
    #                       ubuntu-24.04 — ~7× slower, often times
    #                       out at the runner-class cap). Goal of
    #                       the slim leg is verifying low-memory
    #                       compat — a per-merge + nightly cadence
    #                       satisfies that without bottlenecking PR
    #                       landing. Now runs via
    #                       `.github/workflows/low-memory.yml`
    #                       (push-to-main + 06:00 UTC schedule +
    #                       workflow_dispatch).
    #                       Reference:
    # https://github.blog/changelog/2026-01-22-1-vcpu-linux-runner-now-generally-available-in-github-actions/
    #
    # The Windows legs (added 2026-04-27 as "experimental"; reclassified
    # 2026-08-19 by the maintainer — see the note at `continue-on-error:`
    # below) run only on push-to-main / `workflow_dispatch`, NOT on
    # `pull_request` (and NOT on a schedule — this workflow has no
    # `schedule:` trigger, which several comments here claimed until
    # 2026-08-19). They carry `continue-on-error: true`
    # permanently and by intent: they are drift checks, deliberately
    # observed and deliberately non-blocking. "Experimental / while the
    # installer matures" was the old reading and is no longer the
    # reason — do not read the flag as a temporary accommodation.
    # macos-26 joined them 2026-08-19 (same flag, per-PR cadence kept).
    #
    # fail-fast: false so one leg's failure doesn't cancel the
    # others — we want the full signal across the matrix.
    #
    # Per-PR / per-merge cadence split:
    # - macos-26: developer-experience verification, NOT prod build.
    #   Runs on EVERY PR and every push-to-main. (The 2026-04-27
    #   per-merge-only split was reverted 2026-04-28 by maintainer
    #   correction — "we pull out mac and codeql to merge to main time
    #   instead of per pr, and then moved it back to per pr". This
    #   comment said "push-to-main + schedule + workflow_dispatch"
    #   until 2026-08-19; it had been wrong for nearly four months.)
    # - ubuntu-24.04 + ubuntu-24.04-arm: production build path; runs
    #   on every PR + push-to-main.
    # The matrix is built dynamically by `matrix-setup`: PR + merge_group
    # runs get Linux + macOS, push-to-main / workflow_dispatch get all
    # five.
    #
    # THE ASYMMETRY IS CLOSED (2026-08-19). This block used to say Windows
    # and macOS were NOT in the same state — Windows non-blocking, macOS
    # blocking transitively through `build-and-test`. It is now one state:
    # both are drift checks, both carry the flag below. The floor amendment
    # that closed it was authorized by Aaron the same day, verbatim:
    #
    #   "we are moving away from anything that blocks into drift checks
    #    instead."
    #
    # What that cost, stated rather than left to be discovered: macOS runs
    # PRE-merge, so unlike the Windows flip this one changes what can land.
    # A macOS-only break now merges. Last time that would have mattered:
    # run 32138908732 (2026-08-18), macos-26 the only failed leg and
    # `gate (required)` failure — under this file it would be a green gate
    # carrying a named non-blocking failure, and the break would be on main.
    # Measured frequency of that event: 1 failure in 82 executed push runs
    # (1.2%), clean streak 19, over 2026-08-15..2026-08-19
    # (data/platform-drift.json). For contrast the Linux leg that still
    # blocks, ubuntu-24.04, failed 2 (2.4%) over the same runs — macOS was
    # never the noisy leg, it was a quiet leg holding gate authority.
    # Work item: 081M0CPEH40087G0R0016GDSB6.
    name: build-and-test (${{ matrix.os }})
    timeout-minutes: 45
    needs: [matrix-setup, path-filter]
    # ── THIS FLAG IS A CLASSIFICATION, NOT AN ACCOMMODATION. DO NOT "FIX" IT. ───────
    #
    # Settled 2026-08-19 by the maintainer (Aaron), verbatim:
    #
    #   "windows and mac are drift checks, it's fine to check per pr if we want but we
    #    don't want to block on them. Also we are moving more and more away from PRs
    #    and more into sovereign mode without PRs."
    #
    # AMENDED 2026-08-19, same day, by Aaron directly — this is what moved macOS in:
    #
    #   "we are moving away from anything that blocks into drift checks instead."
    #
    # So: the Windows AND macOS legs are non-blocking PERMANENTLY AND BY INTENT. They
    # are drift checks — deliberately observed, deliberately not gates. This is the
    # steady state, not a tolerance being worked off, and narrowing this expression
    # would be reversing a maintainer decision, not cleaning up a leftover. If you
    # believe a platform should block, that is a floor amendment and it goes through
    # the consent path (the same one named at `gate-required`), not through an edit
    # here. The expression is pinned by a test —
    # src/Core.TypeScript/ci/gate-scope-summary.test.ts, "the drift-check platform set
    # is exactly what the maintainer classified" — so a silent narrowing fails there
    # rather than being discovered by a merge that should not have landed.
    #
    # WHAT THE FLAG ACTUALLY DOES, since it must stay legible. This job IS in the
    # `gate-required` floor, and the floor NAME NOW OVERSTATES ITS COVERAGE ON EVERY
    # PLATFORM BUT LINUX: a Windows or macOS build/test failure reaches
    # `gate (required)` as SUCCESS. What the floor still covers pre-merge is
    # ubuntu-24.04 and ubuntu-24.04-arm, and registry/uncompensatable-floor.yaml's
    # `scoped-build-test-break` entry says so in the data rather than only here.
    # MEASURED for Windows over the 152 completed `gate` runs carrying Windows legs
    # (2026-04-25..2026-08-19): windows-2025 failed 12 times (7.9%), windows-11-arm 9
    # (5.9%), 14 runs carried at least one (9.2%) — and in all 11 instances since
    # `gate-required` existed, the required check reported green beside the red leg.
    # One is committed as a fixture: run 31978270082, in
    # src/Core.TypeScript/ci/fixtures/gate-run-jobs-windows-failed.json. That fixture
    # is the empirical basis for the macOS half too: it proves the Actions API reports
    # a continue-on-error leg's conclusion as `failure` (not `skipped`, not `neutral`),
    # which is what every surface below derives from.
    #
    # THE OBLIGATION THAT COMES WITH "DON'T BLOCK" IS "THEN BE SEEN". A drift check
    # nobody reads is the vacuity class wearing a different hat. FOUR surfaces carry
    # it, and each is deliberate:
    #   0. The leg's OWN check run still concludes `failure` — a red X in the PR's
    #      checks list, next to a green `gate (required)`. Verified against the live
    #      API, not assumed: GET /commits/54492c95ec2e6e31105d698c7ef68d5884daaf38/
    #      check-runs reports `build-and-test (windows-2025)` conclusion=failure for
    #      the run above, while the same commit's `gate (required)` is success. This
    #      is the surface that makes the macOS flip safe, because macOS runs per-PR.
    #   1. `gate (required)`'s step summary + warning annotation names any leg that
    #      failed non-blockingly (src/Core.TypeScript/ci/gate-scope-summary.ts) —
    #      per-run, derived, knows no platform names. It needed no edit for macOS,
    #      which is the test that it was built generically rather than for Windows.
    #   2. `drift-sweep.yml`'s BD001 detector files a failed `build-and-test` leg on
    #      main into the drift ledger (docs/drift-events/) — but keyed to `Zeta.sln`,
    #      so WHICH leg is lost, and it reads one run, so it measures a level.
    #   3. src/Core.TypeScript/ci/platform-drift-report.ts folds the last N push runs
    #      into per-leg failure RATES, clean streaks, and — the number that turned out
    #      to matter — COVERAGE: of the 500 most recent completed push runs, 418 were
    #      `cancelled` before their legs ever ran (16.4% coverage). Published to
    #      data/platform-drift.json and rendered on data/monitor.html. It will
    #      reclassify `macos-26` from `blocking` to `non-blocking` the first time it
    #      fails beside a green rollup — that classification is OBSERVED, not read off
    #      this flag, so the ledger will disagree with this file until the evidence
    #      arrives. That is correct and deliberate; do not "fix" the ledger either.
    #
    # Surfaces 2 and 3 both query `branch=main&event=push`, i.e. 16.4% coverage. That
    # is why macOS keeps its per-PR cadence: surface 0 is the one that reliably fires.
    #
    # Decision record, with the measurements that informed it and the strategic context
    # (the move toward sovereign mode without PRs):
    # workitems/081M0BVAV2H087G0R000VXRYRQ-decide-whether-the-windows-build-and-test-legs-should-block.md
    # workitems/done/2026/08/081M0CPEH40087G0R0016GDSB6-make-the-macos-build-and-test-leg-non-blocking-floor-amendme.md
    #
    # Note if you are here to make the WINDOWS legs run per-PR: Aaron allowed it ("fine
    # to check per pr if we want") and it was PRICED and declined — ~355 PR gate runs/day
    # × ~21 runner-minutes ≈ 124 runner-hours/day for a check that by decision cannot
    # block. The reasoning is in the work item; re-open it there, not here. macOS is not
    # covered by that pricing: it was already running per-PR before this change, so
    # keeping it there costs zero additional minutes.
    continue-on-error: ${{ startsWith(matrix.os, 'windows-') || startsWith(matrix.os, 'macos-') }}
    strategy:
      fail-fast: false
      matrix:
        os: ${{ fromJson(needs.matrix-setup.outputs.os) }}
    runs-on: ${{ matrix.os }}

    # Path-filter step-gating (081KQGDBJ0008QG0R001MK4YPC / 2026-05-02): when the PR
    # touches only docs/memory/openspec/etc., the heavy F# install +
    # dotnet build + dotnet test steps below are skipped. The job
    # itself still runs (status check name reports green) so
    # required-status-checks and `code_quality severity:all` see
    # success rather than "skipped". Cache + checkout steps are
    # cheap and unconditional. See `path-filter` job above for the
    # detection logic.
    steps:
      # Windows MAX_PATH (260) guard — MUST precede checkout. Zeta has memory/persona
      # files whose paths exceed 260 chars; without this, actions/checkout fails with
      # "Filename too long" before any setup runs (install.ps1 sets the same
      # core.longpaths for dev machines, but it runs POST-checkout so can't help CI).
      # Layer 1 (no admin): git prepends the extended-length prefix + bypasses MAX_PATH.
      - name: Enable git long paths (Windows; pre-checkout)
        if: startsWith(matrix.os, 'windows-')
        run: git config --system core.longpaths true

      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          # The pull_request ref is a synthetic merge commit. Its first parent
          # must be present for the PR-scoped markdown diff below.
          fetch-depth: 2

      - name: Path-filter result (081KQGDBJ0008QG0R001MK4YPC)
        shell: bash
        env:
          CODE_PATH: ${{ needs.path-filter.outputs.code }}
        run: |
          if [ "${CODE_PATH}" = "true" ]; then
            echo "Path filter: code=true — running full F# build/test."
          else
            echo "Path filter: code=false — docs-only PR, will skip F# install/build/test steps."
          fi

      # Cache keys include `runner.arch` in addition to `runner.os` so
      # Linux x64 (ubuntu-24.04, ubuntu-slim) and Linux arm64
      # (ubuntu-24.04-arm) do not share entries for arch-sensitive
      # paths. `runner.os` resolves to `Linux` on both arches, which
      # would otherwise cause wrong-arch restores (for example,
      # `~/.dotnet` with x64 binaries restored onto arm64). See
      # https://docs.github.com/en/actions/learn-github-actions/
      # contexts#runner-context for `runner.arch` values.
      - name: Cache .NET SDK
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ~/.dotnet
          key: dotnet-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('global.json', 'tools/setup/common/dotnet.sh') }}

      - name: Cache mise runtimes (python only post round 32)
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.local/share/mise
            ~/.cache/mise
          key: mise-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('.mise.toml', '.mise.full.toml') }}

      - name: Cache elan (Lean 4 toolchain)
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ~/.elan
          key: elan-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('src/Core.TypeScript/ace/setup-realizers/from-elan.ts', 'tools/setup/manifests/from-elan') }}

      - name: Cache NuGet packages
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.nuget/packages
            ~/.local/share/NuGet
          # Keys on `Directory.Packages.props`. The `packages.lock.json`
          # ideal is round-33 Track B. No `restore-keys` — partial hit
          # would restore against different resolved versions.
          # `runner.arch` is included because some NuGet packages ship
          # native RID-specific assets (for example, runtime.linux-
          # x64.*).
          key: nuget-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('Directory.Packages.props') }}

      - name: Install toolchain via three-way-parity script (Unix; GOVERNANCE §24)
        # Single source of truth for Unix toolchain state. Installs:
        # .NET SDK (per global.json, via Microsoft's dotnet-install.sh
        # — round-32 refactor out of mise), python (mise-pinned),
        # elan (Lean manager), dotnet global tools, TLC + Alloy
        # verifier jars. shellenv.sh emits BASH_ENV into $GITHUB_ENV
        # so every subsequent bash `run:` step auto-sources the
        # managed shellenv file (SQLSharp-proven pattern).
        if: needs.path-filter.outputs.code == 'true' && startsWith(matrix.os, 'windows-') == false
        run: ./tools/setup/install.sh

      - name: Install toolchain via three-way-parity script (Windows; GOVERNANCE §24)
        # PowerShell does not execute the Git Bash dispatcher reliably when a workflow `run:` step
        # names install.sh directly. Call the native entrypoint so it can publish mise runtime bins
        # to GITHUB_PATH for the later bun/dotnet steps. CI must not register a desktop loop task.
        if: needs.path-filter.outputs.code == 'true' && startsWith(matrix.os, 'windows-')
        shell: pwsh
        run: ./tools/setup/install.ps1 -SkipLoopRegister

      - name: Guard — CodeAnalysis pins ≤ SDK Roslyn (CS9057, #9774 regression)
        # Fast pre-build backstop: a Microsoft.CodeAnalysis.* pin newer than the SDK's own
        # Roslyn makes generators unloadable (CS9057) — which reached main via #9774. Reads the
        # SDK's ACTUAL csc version (no heuristic), so it self-updates when the SDK is bumped.
        if: needs.path-filter.outputs.code == 'true'
        run: bun src/Core.TypeScript/hygiene/audit-codeanalysis-sdk-match.ts

      - name: Build (0 Warning(s) / 0 Error(s) required)
        # BASH_ENV (set by shellenv.sh during the install step) has
        # already pointed this shell at the managed shellenv file;
        # no explicit source needed.
        if: needs.path-filter.outputs.code == 'true'
        run: dotnet build Zeta.sln -c Release

      - name: Test
        if: needs.path-filter.outputs.code == 'true'
        # --blame-crash / --blame-hang-timeout: when the test host crashes or
        # hangs (the silent "MSB4181: VSTestTask returned false but did not log
        # an error" class — seen once on windows-11-arm), collect a crash dump
        # and a Sequence_*.xml naming the in-flight test, instead of a mystery.
        run: dotnet test Zeta.sln -c Release --no-build --verbosity normal --blame-crash --blame-hang-timeout 15m

      - name: Name the crashed/hung test (on failure)
        # Surface the blame Sequence_*.xml into the CI log so a host crash/hang
        # is diagnosable without artifact plumbing — turns "VSTestTask returned
        # false" into "test X was running when the host died". Cross-platform (bun).
        if: failure() && needs.path-filter.outputs.code == 'true'
        run: bun src/Core.TypeScript/ci/print-blame-sequences.ts

      - name: Skipped (docs-only PR)
        # Status-check passthrough: when the path filter said code=false,
        # the job still completes successfully so required-status-checks
        # see green. This step is the visible signal that the F# build
        # was skipped intentionally.
        if: needs.path-filter.outputs.code != 'true'
        run: echo "build-and-test skipped on docs-only PR (081KQGDBJ0008QG0R001MK4YPC path-filter)."

  lint:
    # FLOOR-ONLY semgrep (ratification reconciliation, Vera's signature
    # note 2026-08-08): this BLOCKING job scans `.semgrep-floor.yml` —
    # exactly the rules registry/uncompensatable-floor.yaml names for the
    # `workflow-supply-chain` entry (mutable action tags, untrusted event
    # data in run: lines). The registry is the literal source of truth for
    # what blocks; the other 15 rules run as non-blocking drift in
    # `lint (semgrep drift)` below (drift-and-heal ADR items 2+5, Accepted
    # 2026-08-08). Adding a rule HERE = adding to the floor = the
    # treaty-amendment consent path.
    #
    # Round 30 posture fix history: before this job existed, 14 rules were
    # codified but 0 gated the build. See docs/security/
    # THREAT-MODEL.md "mitigation validation" section + round-30
    # elevation design (docs/research/threat-model-elevation.md).
    name: lint (semgrep)
    # Repo-wide generic-mode Semgrep has grown past the old 10-minute
    # cap on cold runners (2026-06-12: CI canceled semgrep-core before
    # any findings were emitted). Keep the full ruleset and give the
    # scan enough wall clock instead of weakening coverage.
    timeout-minutes: 20
    runs-on: ubuntu-24.04

    # Toolchain via three-way-parity install.sh (GOVERNANCE §24): same
    # semgrep version that dev laptops + devcontainers get, pinned in
    # `.mise.toml` as `pipx:semgrep`. No `actions/setup-python` here
    # — the host-portability invariant is to minimise GitHub-specific
    # surface so switching CI hosts is cheap, and to keep dev-machine
    # and build-machine setup as close as possible. Resolves Scorecard
    # PinnedDependenciesID #17 + #18 (pip install --upgrade pip + pip
    # install semgrep) by removing the unpinned pip-bootstrap surface
    # entirely.
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Cache install.sh outputs (mise runtimes + dotnet tools + verifier jars)
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.local/bin/mise
            ~/.local/share/mise
            ~/.cache/mise
            ~/.dotnet/tools
            ~/.elan
            ~/.config/zeta
            ~/.rustup/toolchains/1.87.0-*
            ~/.rustup/settings.toml
            ~/.rustup/update-hashes
            ~/.cargo
            ~/.cache/zeta/from-autotools-tarball/*.tgz
          key: install-v2-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('.mise.toml', 'tools/setup/**', 'global.json') }}

      - name: Install toolchain via three-way-parity script (GOVERNANCE §24)
        # Single source of truth for toolchain state. mise installs
        # python + semgrep (the latter via the `pipx:semgrep` pin in
        # `.mise.toml`, which mise auto-routes through `uv tool install`
        # since uv is in the toolchain — see ADR
        # docs/DECISIONS/2026-04-27-uv-canonical-python-tool-manager.md).
        # shellenv.sh wires BASH_ENV so subsequent run: steps auto-
        # source the managed shellenv. Same pattern as build-and-test
        # job above.
        # WHY THIS LANE DECLARES A TIER (081M0K36K69087G0R003BYSCF8 applied to gate).
        # `tools/setup/manifests/apt` carries `agda tier=standard`, and Ubuntu's
        # `agda` transitively pulls `ghc` (79.0 MB) plus 63 `libghc-*-dev` packages:
        # 237 MB of the 556 MB this job fetched on 2026-08-25 (job 97707217922) was
        # Haskell that nothing in this job runs. That download is what stalled the
        # archive mirror three times and burned the whole 420s apt budget, killing
        # nine checks across seven PRs in one 14-minute window. Nothing in this lane
        # touches Agda, GHC, emscripten, llvm, pandoc, r-base, podman, qemu or opam
        # — it runs mise-provided tools only — so the tier gate that already exists
        # subtracts them: 388 packages / 713 MiB becomes 149 / 139.7 MiB.
        #
        # THE BUDGET IS UNCHANGED, deliberately. 420s is a hang guard with a reasoned
        # derivation in tools/setup/linux.sh and an audit that keeps it inside every
        # job's timeout; the measured distribution (107 successes at median 116s, 11
        # failures at exactly 420-421s) is bimodal, so a bigger budget only makes each
        # hang cost more. The guard was right; the payload was wrong.
        #
        # SAFE BY CONSTRUCTION on the agda side: the cubical realizer already
        # degrades — src/Core.TypeScript/ace/setup-realizers/from-agda-cubical.ts:25
        # logs "agda not on PATH (slim tier ...); skipping" and returns success.
        # src/Core.TypeScript/ci/apt-manifest-host-tier.test.ts asserts this line is
        # still here, so deleting it goes red instead of going silently expensive.
        env:
          ZETA_HOST_TIER: slim
        run: ./tools/setup/install.sh

      - name: Run Semgrep (floor rules only — seconds-fast floor check)
        # --error exits non-zero if any floor rule matches. Under
        # nation-state posture per round-30 elevation, a supply-chain
        # rule without a gate isn't a control — and per the ratified
        # drift ADR, a non-floor rule WITH a gate is a lock. Both
        # postures are honored by the split.
        run: semgrep --config .semgrep-floor.yml --error --metrics=off

  lint-semgrep-drift:
    # The full detector set (15 non-floor rules + the 2 floor rules for
    # one-stop reporting), NON-BLOCKING: this job is deliberately NOT in
    # the gate roll-up's needs — a finding here is drift (red X = signal,
    # measured by the ledger's MTTH, healed by lane owners), never a lock
    # on unrelated lanes. Split created at ADR ratification (Vera's
    # signature note; workitem 081KZHGP45608QG0R003J0A7G5).
    name: lint (semgrep drift)
    timeout-minutes: 20
    runs-on: ubuntu-24.04

    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Cache install.sh outputs (mise runtimes + dotnet tools + verifier jars)
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.local/bin/mise
            ~/.local/share/mise
            ~/.cache/mise
            ~/.dotnet/tools
            ~/.elan
            ~/.config/zeta
            ~/.rustup/toolchains/1.87.0-*
            ~/.rustup/settings.toml
            ~/.rustup/update-hashes
            ~/.cargo
            ~/.cache/zeta/from-autotools-tarball/*.tgz
          key: install-v2-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('.mise.toml', 'tools/setup/**', 'global.json') }}

      - name: Install toolchain via three-way-parity script (GOVERNANCE §24)
        # tier=slim — this lane runs semgrep (mise `pipx:semgrep` pin) and nothing else, so it must not
        # fetch the 237 MB agda->ghc apt payload. Full measurement + reasoning at
        # the `lint (semgrep)` job above; pinned by
        # src/Core.TypeScript/ci/apt-manifest-host-tier.test.ts.
        env:
          ZETA_HOST_TIER: slim
        run: ./tools/setup/install.sh

      - name: Run Semgrep (full detector set — findings are drift, not locks)
        run: semgrep --config .semgrep.yml --config .semgrep-floor.yml --error --metrics=off

  lint-yaml-k8s:
    # YAML + Kubernetes-manifest validation over full-ai-cluster/k8s/** — the
    # GitOps surface ArgoCD deploys. Closes the gap that let a k8s Application land
    # with ZERO CI validation (only ArgoCD checked it, slowly, at cluster-sync).
    # yamllint = syntax/indentation/dup-key; kubeconform = k8s API-schema validation.
    # Aaron 2026-06-10 ("should we have a yaml/k8s lint gate? ... lets do that").
    #
    # REQUIRED as of 2026-08-14. It shipped ADVISORY (continue-on-error) under the
    # standing easing-in pattern, with the instruction to flip once clean for a few
    # rounds. That condition is met and was measured, not assumed: clean since
    # 2026-08-01, and 8 of 8 recent gate runs carrying this job report success. An
    # advisory check that has earned its promotion and never gets it is just a check
    # that cannot fail — which is the defect class this repo spent 2026-08-14 removing
    # (nine of them, including a k8s manifest validator that accepted tab indentation,
    # unterminated quotes and duplicate keys as "valid YAML"). The full dev-machine bootstrap is deliberately NOT run
    # here: this job only needs two lint tools, and install.sh pulls heavy proof/QEMU/R
    # substrate that can consume the whole advisory timeout before YAML is checked.
    name: lint (yaml/k8s)
    timeout-minutes: 10
    runs-on: ubuntu-24.04
    # No continue-on-error: this job now blocks the gate (see the note above).

    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Set up bun (for the cluster-tree consumer audit)
        uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2.2.0

      # 081M00QCHWA087G0R000GKKRXD — the repo carries TWO declarations of one
      # cluster, and they collide on Application/argocd/zeta-root. Consolidation
      # has been deferred twice for the same reason: nobody could show the list
      # of things still reading the stale tree was COMPLETE (a 2026-08-16 pass
      # named two code consumers; there were nine). This audit derives that list
      # with `git grep` and fails when a NEW file couples to the stale tree, or
      # when the roster over-claims by naming a file that no longer does.
      #
      # It needs no network and no toolchain beyond bun + git, and it needs no
      # `bun install` (node builtins only). MEASURED 2026-08-17, warm: 0.9 s
      # wall for a full content scan of the 15,711 tracked files outside the
      # excluded prefixes. That is why it sits in this narrow job rather than
      # earning one of its own.
      - name: Cluster-tree consumer roster (no new coupling to the stale tree)
        run: bun src/Core.TypeScript/hygiene/audit-cluster-tree-consumers.ts

      # The audit above is only worth its slot if it can go red. The suite
      # plants each failure mode in a throwaway git tree and asserts exit 1 with
      # the specific reason, plus a control case asserting exit 0 so an audit
      # that failed unconditionally could not pass. MEASURED: 2.4 s, 8 cases.
      - name: Cluster-tree consumer roster — mutation suite (proves it goes red)
        run: bun test src/Core.TypeScript/hygiene/audit-cluster-tree-consumers.test.ts

      # ADDED 2026-08-21, and it relaxes this job's stated "node builtins only"
      # design ON PURPOSE. The observability-chain audit below parses Helm
      # `valuesObject` YAML, and the honest options were (a) install the `yaml`
      # package this repo already declares, or (b) hand-roll YAML extraction
      # with regexes. (b) was refused: a regex YAML reader that silently
      # mis-parses a nested block would produce a GREEN audit over a config it
      # never understood, which is precisely the false-green class this audit
      # exists to catch. Paying ~seconds of install to avoid inventing a
      # fragile parser is the correct trade. First observed as
      # `Cannot find package 'yaml'` in run 96651130473.
      - name: Install bun dependencies (the chain audit needs `yaml`)
        run: bun install --frozen-lockfile

      # 2026-08-20 — the observability chain was broken at every link and every
      # break was values-only, so helm template, kubeconform and yamllint were
      # all green on all of them. Measured on the tree as it shipped: Alloy had
      # three sinks and zero sources, its Loki sink named a Service the chart
      # does not render, and every alert routed to a receiver named "null".
      # This audit refuses all three classes. It needs no network and no helm:
      # Service names come from a rendered roster that is freshness-keyed to the
      # target Application, so a chart bump without --refresh fails as STALE
      # rather than asserting a stale render. MEASURED: 0.4 s, bun + yaml only.
      - name: Observability chain (no sink without a source, no alert into /dev/null)
        run: bun src/Core.TypeScript/hygiene/audit-observability-chain.ts

      # Proves the audit above can go red, including against the exact pre-fix
      # content of the tree. MEASURED: 22 tests, 40 assertions, 0.5 s.
      - name: Observability chain — mutation suite (proves it goes red)
        run: bun test src/Core.TypeScript/hygiene/audit-observability-chain.test.ts

      - name: Install narrow YAML lint tools
        run: |
          set -euo pipefail
          python3 -m venv .venv-yamllint
          .venv-yamllint/bin/python -m pip install --upgrade pip
          .venv-yamllint/bin/pip install yamllint==1.38.0
          go install github.com/yannh/kubeconform/cmd/kubeconform@v0.7.0
          echo "$PWD/.venv-yamllint/bin" >> "$GITHUB_PATH"
          echo "$(go env GOPATH)/bin" >> "$GITHUB_PATH"

      - name: yamllint (k8s manifests)
        # relaxed profile: structural correctness (indent/dup-key/syntax), not
        # cosmetic line-length — the manifests are machine-authored Helm values.
        # ALL manifest roots, not just full-ai-cluster. Measured 2026-08-01: 24 of 129
        # manifests (19%) — every file under infra/k8s and agentic-organization/deploy —
        # were never validated by anything. They reach a cluster via ArgoCD sync, so a
        # malformed one failed at APPLY time on real hardware instead of here. That is
        # the wrong place to find it once runners live on USB-provisioned nodes.
        run: yamllint -d relaxed full-ai-cluster/k8s/ infra/k8s/ agentic-organization/deploy/

      - name: kubeconform (k8s API-schema validation)
        # -ignore-missing-schemas: skip CRs we have no schema for; -skip:
        # skip repo-owned/third-party CRD kinds whose default-schema mirror paths
        # are either absent or transiently unavailable. Retry with backoff because
        # rate-limited schema downloads count as ERRORS, not missing schemas.
        # -strict: reject unknown fields on the manifests we DO have schemas for.
        # find-pipe so it recurses explicitly (portable across kubeconform versions).
        #
        # -kubernetes-version 1.33.0 ADDED 2026-08-25. Without it kubeconform checks
        # against the schema registry's `master` set -- i.e. against whatever
        # Kubernetes released most recently, which is NOT the version any of our
        # clusters run and which DRIFTS UNDER US with no commit here. The pin's
        # provenance is this repo's own already-declared target: the `kube-version`
        # default in infra/k8s/tests/validate-applications.ts, which the helm-validate
        # lane has been rendering against for the other cluster tree. Two lanes
        # validating the same kinds against two different Kubernetes versions is the
        # drift this closes. MEASURED on the agentic-org tree before landing: 26
        # resources in 12 files, Valid 26 / Invalid 0 both with and without the flag,
        # so the pin changes no verdict today -- it fixes WHICH QUESTION is being asked.
        run: |
          crd_kinds="Application,Blueprint,CiliumL2AnnouncementPolicy,CiliumLoadBalancerIPPool,ClusterIssuer,ConstraintTemplate,Deployable,Gateway,HTTPRoute,Hat,HatConflict,HatCooldown,HatMaxBindings,HatMaxNew,HatNoCycle,HatPolicy,HatQuorum,HatWarmup,HelmChart,Policy"
          kc() {
            find full-ai-cluster/k8s infra/k8s agentic-organization/deploy -type f \( -name '*.yaml' -o -name '*.yml' \) -print0 \
              | xargs -0 kubeconform -strict -ignore-missing-schemas -summary -schema-location default -kubernetes-version 1.33.0 -skip "$crd_kinds"
          }
          kc || { echo "[yaml/k8s] retry in 30s (schema-registry rate limit?)"; sleep 30; kc; } \
             || { echo "[yaml/k8s] retry in 90s"; sleep 90; kc; }

      - name: Cross-manifest consistency (agentic-org apply set)
        # ADDED 2026-08-25. The two steps above are the only checks this tree has,
        # and NEITHER CAN SEE ACROSS DOCUMENTS: yamllint reads one file's syntax,
        # kubeconform reads one resource against its schema. Every defect this step
        # exists for is schema-VALID YAML that a cluster accepts at apply time and
        # that fails later with a symptom that does not name its cause -- a Service
        # whose named targetPort no container declares (endpoints with no port), an
        # env var naming a Service port nothing exposes (telemetry into a black
        # hole), a subPath key absent from its ConfigMap (a silently empty mount).
        #
        # THIS IS THE TREE A USB-BOOTED NODE ACTUALLY RUNS. It is raw YAML applied
        # with one `kubectl apply -f`, not a chart and not an ArgoCD Application, so
        # none of the helm-validate machinery reaches it.
        #
        # MEASURED on first contact at 60e2024e9: 6 findings, all real. The headline
        # one is that `31-work-provider-secret.example.yaml` sat INSIDE the apply
        # directory -- kubectl selects by extension, never by filename, so the
        # documented bring-up applied the template, installing WORK_PROVIDER=github
        # with the literal placeholder token and destroying the internal-only safe
        # default the worker manifest promises. All 6 are fixed in the landing PR, so
        # this step lands GREEN and is a ratchet against regression, not a debt.
        #
        # Offline, no network, ~0.15 s. No pipe: a pipeline would report the exit
        # status of its last stage and discard the tool's.
        run: bun src/Core.TypeScript/cluster/audit-agentic-org-manifests.ts

      - name: Cross-manifest consistency — falsifiers (proves it goes red)
        # The step above is only worth its slot if it can fail. 34 cases: each copies
        # the real tree, breaks exactly one thing, and asserts exit 1 AND the specific
        # check that fired -- plus an unmutated control asserting exit 0, and three
        # vacuity guards asserting that an empty directory, a directory that parses to
        # zero documents, and a nonexistent directory all FAIL rather than reporting a
        # serene "0 findings".
        #
        # VERIFIED 2026-08-25 by stubbing the tool's non-zero exit: 22 of the 34 went
        # red immediately, and the 12 that stayed green are exactly the controls and
        # the pure-unit cases, which do not read the exit code. ~2.8 s.
        run: bun test src/Core.TypeScript/cluster/audit-agentic-org-manifests.test.ts

  lint-shell:
    # Shellcheck on every bash script under tools/setup/ + tools/.
    # Round 33 static-analysis expansion (Aaron: "as much static
    # analysis as possible, match or surpass SQLSharp + scratch").
    # See openspec/specs/static-analysis/profiles/shell.md.
    name: lint (shellcheck)
    timeout-minutes: 12
    runs-on: ubuntu-24.04

    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Cache install.sh outputs (mise runtimes + dotnet tools + verifier jars)
        # Comprehensive cache of everything `tools/setup/install.sh`
        # writes — keeps dev laptops and CI runners as close to the
        # same state as possible per the human maintainer's
        # 2026-04-28 input: dev setup and build-machine setup
        # should be as close to the same as possible; cache the
        # whole install/setup output rather than per-component.
        # Without comprehensive caching every CI run hits CDNs cold
        # (mise.run + GitHub releases for bun/shellcheck/actionlint +
        # NuGet + Lean toolchain). Transient 502s become avoidable
        # failures rather than first-run-only cost.
        # Cache key hashes BOTH .mise.toml (runtime versions) AND
        # tools/setup/** (install logic itself) so changes to either
        # invalidate cache → vanilla install path gets re-tested
        # whenever the install discipline changes.
        #
        # `~/.cache/zeta/from-autotools-tarball/*.tgz` added 2026-08-17.
        # That directory is where `from-autotools-tarball.ts` parks upstream
        # source tarballs — today only E prover 3.2.0, fetched from
        # `github.com/eprover/eprover/archive/refs/tags/`. It was NOT in this
        # list, so the "comprehensive cache" claim above was false for the one
        # component fetched straight from github.com rather than a CDN: every
        # ubuntu job re-downloaded it cold. On 2026-08-17 GitHub answered 429
        # and PRs #11470, #11472 and #11475 went red on `lint (tick-shard
        # relative-paths)` with `curl fetch failed (22)` — three PRs whose
        # diffs had nothing to do with a theorem prover. curl retried thirteen
        # times over five minutes and lost, because a rate-limit window is
        # longer than any retry budget worth spending; not fetching is the only
        # fix that scales.
        #
        # Two properties make this safe to cache rather than merely faster.
        # The key already hashes `tools/setup/**`, which contains the manifest
        # holding the URL and the sha256 pin, so a version bump cannot be
        # served a stale tarball — it lands on a different key. And the
        # realizer now verifies the pin on the RESTORED file too, not only on
        # a fresh download, so a corrupt or substituted entry is discarded
        # instead of compiled into the prover that signs our FOL proofs.
        # Only `*.tgz` is cached: the sibling `<key>-build/` extraction dirs
        # are rebuilt every run and would add hundreds of MB of churn.
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.local/bin/mise
            ~/.local/share/mise
            ~/.cache/mise
            ~/.dotnet/tools
            ~/.elan
            ~/.config/zeta
            ~/.rustup/toolchains/1.87.0-*
            ~/.rustup/settings.toml
            ~/.rustup/update-hashes
            ~/.cargo
            ~/.cache/zeta/from-autotools-tarball/*.tgz
          key: install-v2-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('.mise.toml', 'tools/setup/**', 'global.json') }}

      - name: Install toolchain via three-way-parity script (GOVERNANCE §24)
        # Installs shellcheck via mise (pinned in .mise.toml). Single
        # source of truth — the same version on dev laptops + CI
        # runners. Prior step relied on shellcheck shipping pre-
        # installed on ubuntu (any version), which broke parity (dev machines
        # may have a different version) and wouldn't survive newer
        # runner images like ubuntu-slim that don't ship shellcheck.
        # Note: install.sh is idempotent (detect-first-install-else-update);
        # safe to run on every CI invocation including cache-hit (it
        # short-circuits when tools are already present).
        # CI-level retry per the human maintainer's 2026-04-28
        # input: PR #23 (on AceHack) failed because mise's
        # internal 3-attempt retry exhausted on a github releases
        # CDN 502 for bun-1.3.13. CI runs are non-interactive so
        # retry is safe; dev runs stay interactive (user decides).
        # 5 attempts; backoff 10s/30s/60s/120s covers short-burst
        # through multi-minute CDN outages. The retry wrapper is
        # currently inline-duplicated across multiple jobs;
        # extracting to a composite action is a follow-up
        # improvement candidate.
        # tier=slim — this lane runs shellcheck (mise pin 0.11.0) and nothing else, so it must not
        # fetch the 237 MB agda->ghc apt payload. Full measurement + reasoning at
        # the `lint (semgrep)` job above; pinned by
        # src/Core.TypeScript/ci/apt-manifest-host-tier.test.ts.
        env:
          ZETA_HOST_TIER: slim
        run: |
          set -euo pipefail
          for attempt in 1 2 3 4 5; do
            if ./tools/setup/install.sh; then exit 0; fi
            [ "$attempt" = "5" ] && { echo "install.sh failed after 5 attempts"; exit 1; }
            # Backoff: 10s, 30s, 60s, 120s — covers transient CDN
            # blips from short-burst-and-recover up to multi-minute
            # outages. Aaron 2026-04-28 input: bumped from 3 to 5
            # because PR #23's bun-1.3.13 502 burned all 3 prior
            # mise-internal retries and a 3-attempt wrapper on top
            # didn't add enough margin.
            case "$attempt" in
              1) backoff=10 ;;
              2) backoff=30 ;;
              3) backoff=60 ;;
              4) backoff=120 ;;
            esac
            echo "install.sh attempt $attempt failed; retrying in ${backoff}s..." >&2
            sleep "$backoff"
          done

      - name: Run shellcheck
        # Scope: Zeta's own scripts under `tools/setup/` only —
        # `src/Core.Lean4/.lake/packages/**` is Lean/Mathlib vendored
        # code not governed by Zeta standards.
        # Format: -f gcc for file:line:col:msg output parseable by
        # IDEs. Severity floor: style (catches everything including
        # style warnings; we can relax to warning if noise is high).
        run: |
          set -euo pipefail
          mapfile -t files < <(
            find tools -name "*.sh" -type f \
              -not -path "src/Core.Lean4/.lake/*" \
              | sort
          )
          if [ ${#files[@]} -eq 0 ]; then
            echo "no shell scripts found under tools/; skipping"
            exit 0
          fi
          echo "shellcheck targets:"
          printf '  %s\n' "${files[@]}"
          shellcheck --format=gcc --severity=style "${files[@]}"

  lint-bash-retirement-inventory:
    # Bash-retirement guard for the TypeScript/Bun migration trajectory:
    # Zeta-owned post-install repo scripts should not grow new `.sh`
    # entrypoints after Bucket B reached zero. The retained shell surface is
    # setup/bootstrap, launchd-bootstrap, and the Kiro loop wrapper only.
    #
    # ALSO runs the full hygiene unit-test suite (bun test
    # src/Core.TypeScript/hygiene/). Previously NO gate job ran it — only
    # two named hygiene tests + the lint scripts ran — so an incomplete
    # change (source allowlist updated, unit-test snapshot not) merged green
    # and left a test silently red for ~10 PRs (#8216 fix; this step closes
    # the gap). bun is already provisioned here, so it adds no extra install.
    name: lint (bash retirement inventory + hygiene unit tests)
    timeout-minutes: 15
    runs-on: ubuntu-24.04

    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      # This job executes lockfile-managed JavaScript plus tracked hygiene
      # tests only. The 553 MB workstation/cluster apt manifest timed out on a
      # live heartbeat PR before this guard ran; setup-bun + bun.lock is the
      # declared minimal boundary, while full setup remains for toolchain jobs.
      - name: Setup pinned Bun
        uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2.2.0
        with:
          bun-version: "1.3.13"

      - name: Install JavaScript dependencies from the locked graph
        run: bun install --frozen-lockfile

      - name: Run bash-retirement inventory guard
        run: bun run hygiene:check-bash-retirement-inventory

      - name: Ambient-credential-hoist guard
        # Refuses any tracked executable surface that puts a credential into a
        # shell environment. An env var crosses `exec` regardless of the child's
        # code identity, so this is the one exposure no signature, keychain ACL,
        # IMA appraisal or TPM seal can gate — all four bind a secret to a
        # CALLER, and an inherited variable has already escaped that question.
        # It caught two live sites when first run (shellenv.sh:111 and
        # op-token-setup.sh:83); both are removed in the same change.
        # Exits 2 if it scans fewer files than its floor, so a scope regression
        # cannot report success the way `lint:markdown` did (#10712).
        run: bun run hygiene:no-ambient-credential-hoist

      - name: Decide-by-grep guard (a crash must not read as a pass)
        # Refuses `if TOOL ... | grep -q PAT; then fail; fi` in a workflow. A
        # pipeline's exit status is the LAST command's, so TOOL's status is
        # discarded; when TOOL dies on a signal (139 = 128+11 SIGSEGV, 134 =
        # 128+6 SIGABRT) it prints nothing, the grep matches nothing, and the
        # gate PASSES having checked nothing. Measured 2026-08-15: replaying
        # lean-proof.yml's axiom-audit step with a `lake` that SIGSEGVs on every
        # call exited 0 and printed "axiom audit clean (no sorryAx)" — 20 proof
        # gates green with no prover output at all.
        # Only the DENY polarity is flagged; `if ! ... | grep -q` is fail-closed.
        # Exits 2 if it scans fewer workflows than its floor, so a scope
        # regression cannot report success the way `lint:markdown` did (#10712).
        run: bun run hygiene:no-decide-by-grep

      - name: Scan-floor route audit (the floors are themselves enumerable)
        # Inventories every scan floor in the repo through four INDEPENDENT
        # recognizers and requires each to contribute at least one site. An
        # enumerating check is exactly the shape that goes blind — a rename or a
        # reworded failure message kills one recognizer while the other three keep
        # returning a plausible-looking inventory — so the floor here is
        # per-recognizer, never a total. Exits 3 naming the one that went dark.
        run: bun run hygiene:scan-floor-routes

      - name: Ambient-time-in-tests guard (no test verdict may depend on wall-clock)
        # A test that sleeps 10ms and then asserts is asserting that 10ms was enough on THIS
        # runner. MEASURED 2026-08-16: poll-pr-gate-batch.test.ts, 14 pure in-memory tests with
        # injected fakes and no I/O, ran 1366ms idle and 5.46s under load on the same machine
        # class -- a 4x spread on identical code -- and went red at exactly the 5000ms per-test
        # cap on six open PRs. Every one of those reds was false. In this repo's vocabulary a
        # real timer in a test is an undeclared ambient time channel: DST #4 and
        # noninterference #7, and the test-suite form of
        # local-time-never-enters-the-shared-fold.
        #
        # NOT a Semgrep or ESLint rule, though either could match the pattern. Their suppression
        # is an inline comment -- invisible in any roster, added by the same edit that
        # introduces the violation. This reads registry/wall-clock-test-allowlist.json, which
        # pins EXACT per-file occurrence counts, so a new sleep hiding behind a justified one
        # fails and so does a row that has gone stale. Same mechanism, and the same reason, as
        # tests/Tests.FSharp/DeterminismLint.Tests.fs uses for src/Core.
        #
        # Zero-delay yields stay legal: a setTimeout of 0 grants one event-loop turn on a fast
        # machine and one on a loaded one. It is a NON-ZERO delay that encodes a guess.
        #
        # Its own falsifier runs in the step below: audit-ambient-time-in-tests.test.ts feeds
        # the detector a deliberately-introduced ambient-time test and requires it to go red,
        # in both ratchet directions.
        run: bun run hygiene:no-ambient-time-in-tests
      - name: A check may not claim a property of higher arity than it tests
        # 081M0RAX8AC087G0R003NQM7P9. Noninterference is a HYPERPROPERTY -- a predicate over PAIRS
        # of executions, and 2-safety (Clarkson & Schneider, CSF 2008 / JCS 18(6):1157). No
        # single-run check can decide one; that is a theorem, not a coverage gap. Manifesto s13 IS
        # noninterference, so the repo has committed to a discipline no arity-1 test can witness.
        #
        # The class is A CHECK WHOSE ARITY IS LOWER THAN THE PROPERTY IT CLAIMS. Self-comparison is
        # its most visible form and not its definition: a grep for `X = X` misses two calls with
        # identical arguments bound to DIFFERENT names, which is how the live instance in
        # Properties/Policy.Relocation.Tests.fs survived every prior sweep -- a 1000-case FsCheck
        # property that compared `delta` to itself, in a file no .fsproj compiled.
        #
        # Three rules. R1 gates: a test whose NAME declares independence of a named variable may
        # not resolve to a self-comparison (arity is DECLARED, never inferred -- Rice 1953 makes
        # the semantic version undecidable, and sampled read-set tracing is unsound in both
        # directions). R2 is a counted census ratchet, failing when a file's count goes UP and
        # equally when it goes DOWN, same mechanism as the ambient-time audit above. R3 fails on an
        # F# test source no project compiles: arity ZERO, the floor of the class -- the `*.test.ts`
        # half of that check has existed since 2026-08-13 and the F# half did not.
        #
        # Deliberately NOT a ban on self-comparison: most in this tree are correct (`f x = f x`
        # evaluated twice IS two executions; mutation-mediated pairs differ in what happened
        # between them; separately-constructed equal values are the point of an equality law).
        # Rounding those up to "vacuous" is the same error in the opposite direction.
        #
        # Its own falsifier runs in the step below: audit-check-arity.test.ts feeds each rule a
        # deliberately-broken fixture and requires it to go red, R2 in both ratchet directions.
        run: bun run hygiene:check-arity

      - name: Check arity -- the NON-EQUALITY half (a taint claim is not a string search)
        # The step above inspects EQUALITY only. Its own PR named the gap and declined to size
        # it: "Non-equality checks are entirely outside the detector ... Largest unsearched
        # region, unsized." It is now sized -- 24,805 non-equality assertion sites across 1,928
        # test files -- and two live instances were fixed:
        #
        #   * tools/setup/persona-keys/machine.test.ts claimed "no private bytes anywhere in it"
        #     and discharged it with `not.toContain("PRIVATE")` plus a PEM-armor regex.
        #     Publishing the COMPLETE private key base64-encoded passed all of it: 27 tests,
        #     128 expect() calls, green. That is a 2-safety property tested by a one-run string
        #     search, and it sat on a key-publication path.
        #   * DurableDiplomacyRankGate.Tests.fs asserted `Set.count caps >= 0`, which is
        #     non-negative by construction -- and the match arm holding it was never reached,
        #     so it was arity ZERO rather than merely vacuous.
        #
        # Two rules, and only one of them gates. R4 gates provably-unfalsifiable comparisons:
        # every pattern is a THEOREM about the expression's type (a length is non-negative;
        # `X >= X` is reflexivity), so Rice does not bite -- nothing semantic is decided.
        # R5 is a census ratchet over absence-assertions discharging a taint / 2-safety claim,
        # failing when a file's count goes UP and equally when it goes DOWN.
        #
        # R5 is deliberately NOT a gate: of the 10 sites where an absence assertion sits under a
        # 2-safety name, SIX are correct -- backed by an exact-equality pin on the whole output
        # or by a positive capability assertion. A gate at 60% false positives is not a gate, it
        # is a nuisance that gets suppressed. `Assert.True(true)` is likewise NOT flagged: all 8
        # live instances are the success leg of a discriminating match whose other leg is
        # `Assert.True(false, ...)`, so the enclosing check can fail.
        #
        # Scanned REPO-WIDE, unlike the step above whose SCAN_ROOTS is ["tests"]: 1,194 of the
        # tree's 1,230 *.test.ts files -- 97% -- live outside tests/, and BOTH bugs above were
        # in that region.
        #
        # Its own falsifier runs in the step below: audit-check-arity-nonequality.test.ts feeds
        # each rule a broken fixture and requires it to go red, R5 in both ratchet directions,
        # plus controls asserting the shapes that must NOT be flagged.
        run: bun run hygiene:check-arity-nonequality

      - name: Linter coverage vs CI invocation (a configured linter must be an invoked one)
        # THE CLASS: a linter is present, configured, believed to be checking a file, and does
        # not examine it -- while the invocation exits 0. Every instance produces a check that
        # did not run and looks like a check that passed. Three were found by hand on
        # 2026-08-24 (the markdownlint research/workitem carve-outs, prettier+stylelint
        # invoked nowhere, 81 unformatted files under tools/setup/persona-keys/), which is the
        # argument for a machine rather than a fourth point fix. #10712 -- `lint:markdown`
        # linting ZERO files and exiting 0 for months -- is the same class, already paid for.
        #
        # Four checks: every lint-family package.json script is invoked or declared uninvoked
        # WITH ITS REASON (both directions -- a ledger you can only add to is a suppression
        # list); every linter discovered in the tree through three independent routes is
        # claimed by a ledger row whose declared disposition matches the workflows; every
        # GATED tool asserts a floor on the corpus its invocation actually reaches, so exit 0
        # over zero files stops being indistinguishable from exit 0 over 2,843; and
        # tsconfig.json#exclude vs eslint.config.ts#ignorePatterns -- two lists of one fact,
        # maintained separately, 11 divergences covering 619 .ts files -- must match the
        # declared set exactly.
        #
        # It turns NOTHING on and removes NO carve-out. Every carve-out may be deliberate;
        # this step makes the blindness legible and fails when it CHANGES.
        #
        # Cost: 21 s MEASURED ON THE RUNNER (job 97406378383, 10:53:56Z -> 10:54:17Z), not
        # the 13 s the same command takes on a dev laptop -- the laptop number was the first
        # one written here and it was replaced rather than kept, because a cost estimate that
        # quotes the wrong machine is the same defect as a check that quotes the wrong file.
        # Nearly all of it is the markdownlint-cli2 probe that asks the linter itself which
        # files it opens; reimplementing its ignore semantics here would drift from the gate
        # within a release. Runs in this job because bun and the locked dependency graph are
        # already provisioned four steps up -- no new job, no new runner, no new install. At
        # ~1,100 gate runs/month that is ~6.4 runner-hours/month, against the ~95 the eslint
        # roster already costs. It scales with `.md` count, not with rule count.
        run: bun run hygiene:linter-coverage

      - name: No culture-sensitive collation in TypeScript (.claude/rules/culture-invariant-by-default.md)
        # THE CHECK EXISTED AND DID NOT RUN. This is the mechanical enforcement of a carved
        # rule, it has had a baseline file since it landed, and until this step no workflow
        # invoked it -- neither by script name nor by path. My own linter-coverage audit
        # (four steps up) classified it `A GENUINE HOLE` and declined to close it, because
        # that audit does not widen checks. This step is the separate, measured change it
        # named.
        #
        # WHY THIS ONE AND WHY NOW -- it would have caught a regression shipped TODAY.
        # The rule's cited live failure is 081KT07NV0008QG0R001YDB73K: `ZSet.ofSeq` sorted
        # culture-sensitively, which made `GCounter.Merge` NON-ASSOCIATIVE -- a CRDT law
        # broken by a collation choice. On 2026-08-24 an agent shipped two `localeCompare`
        # calls into a PR, reintroducing that exact class, and said so in its own words:
        # "I introduced the class I was arming a check against, twelve files away." It was
        # caught by hand, late. The machine that exists for precisely this was not wired in.
        #
        # WHY C# DOES NOT NEED THIS AND TYPESCRIPT DOES: `.editorconfig` sets CA1304/1305/
        # 1307/1310/2007 to `error`, so the C# compiler refuses culture-sensitive comparison
        # outright. TypeScript has no equivalent analyzer. The same carved rule was a wall in
        # one language and a suggestion in the other -- that asymmetry, not carelessness, is
        # why these keep reappearing.
        #
        # A COLD START CANNOT REDDEN main. The baseline records the 151 pre-existing
        # instances across 77 files as a RATCHET, not an allowlist: a file's count may fall,
        # never rise, and a file absent from the baseline may have none at all. So this fails
        # only on NEW debt. Nothing was added to the baseline to land this step and no
        # allowlist entry was created -- the check goes on exactly as it already was.
        #
        # Cost: 1.64 s MEASURED ON THE RUNNER (job 97432756112, 12:34:18.98Z -> 12:34:20.62Z),
        # over the 3,178 tracked files `git ls-files` reports. The laptop number was 2.34 s
        # warm / 6.39 s cold and is NOT the one quoted -- here the laptop would have OVERstated
        # the cost, where in the linter-coverage step above it understated it. That is the
        # argument for quoting the runner either way: a laptop figure is not conservative, it
        # is just the wrong machine.
        #
        # It is a single pass scanning for a closed list of named APIs (localeCompare, the Intl
        # family, toLocale*), so it scales with tracked-file count, not with rule count. At
        # ~1,100 gate runs/month the two steps added here together cost ~0.52 runner-hours/
        # month, against the ~6.4 the linter-coverage step above costs and the ~95 the eslint
        # roster costs. Runs in THIS job because bun and the locked dependency graph are already
        # provisioned nine steps up -- no new job, no new runner, no new install.
        run: bun run hygiene:no-culture-sensitive-collation

      - name: mise pin parity (GOVERNANCE §24 three-way parity)
        # The second hole the linter-coverage audit named and declined to close. A pin audit
        # that ran in no workflow: work-item 081KZKS9A6B08QG0R0008EG72M shipped an installer
        # ISO carrying nixpkgs' mise 2025.11.7 where CI carried the pinned 2026.6.12, because
        # a second flake built the SAME installer configuration without the mise-pin overlay.
        # An unpinned mise is below `.mise.toml`'s min_version and falls into a tarball branch
        # that is deliberately fatal on NixOS -- so the divergence surfaced only at FIRST BOOT,
        # which is the worst possible place for a parity defect to be discovered.
        #
        # Two checks, one per way to re-diverge: STRUCTURAL (any flake building the installer
        # config must apply the overlay -- discovered by `git ls-files`, so a newly added flake
        # is covered the moment it exists) and VALUE (every site restating the pin agrees with
        # `.mise.toml`, which is read as canonical rather than restated here).
        #
        # Green on main today, so it goes on with no backlog and no suppression. Cost: 0.044 s
        # MEASURED ON THE RUNNER (job 97432756112, 12:34:20.623Z -> 12:34:20.668Z) -- it reads a
        # handful of flake.nix files plus `.mise.toml`, and the figure is dominated by bun
        # start-up. Its sibling `hygiene:dotnet-pin-parity` is already invoked; this closes the
        # asymmetry between the two.
        run: bun run hygiene:mise-pin-parity

      - name: Hygiene unit-test suite (closes the per-PR coverage gap)
        # Runs every src/Core.TypeScript/hygiene/*.test.ts. These are
        # fixture/in-memory tests (no MEMORY.md / user-scope deps), so they
        # are CI-safe. This is what would have caught #8216's stale snapshot.
        run: bun test src/Core.TypeScript/hygiene/

      - name: Hardware-inventory cross-surface reconciliation (drift)
        # 081M00R59KS087G0R001W3837V. Hardware inventory lives on several surfaces
        # with different provenance (a ZetaId register, a human audit snapshot, an
        # operator declaration, machine self-registrations, SSH host keys) and
        # nothing reconciled them. This does NOT merge them — it checks the
        # invariants that hold ACROSS them (dangling machine references, one NIC
        # counted as two nodes, a surface that never says what it is, a snapshot
        # edited without moving its own counts, a declaration gone stale against
        # the probe surface) and reports the register-coverage gap.
        #
        # Drift, not a gate, by design: this job is not in the gate-required
        # roll-up, so a red here is signal, not a block (drift-and-heal ADR).
        # Divergences we deliberately hold open are ledgered in
        # inventory/reconciliation-open.json, and a ledger entry that no longer
        # reproduces fails too — a suppression cannot outlive its cause.
        run: bun run inventory:reconcile

      - name: Inventory read-model is derived, not drifted (081M00R59KS087G0R001W3837V)
        # `inventory/items.json` is a COMMITTED PROJECTION of `inventory/items/*.md`,
        # and it is the only inventory surface a reader actually sees — the Pages
        # viewer fetches it and never opens an item file. It has always had a
        # `--check` mode and NO workflow ran it: measured 2026-08-17, editing an
        # item's value_usd left `inventory:reconcile` green and all 23 inventory unit
        # tests passing while the published price stayed wrong.
        #
        # Third instance of the same class in this one job (the hygiene suite #8216,
        # the inventory unit tests one step down, now this): a check that exists,
        # can fail, and is wired to nothing reads exactly like a check that passed.
        run: bun run inventory:items-check

      - name: Inventory unit tests
        # These existed (generate-items-json.test.ts) and NO workflow ran them —
        # found while wiring the reconciliation check. Same shape as the #8216 gap
        # the hygiene step above closes, one directory over.
        run: bun test src/Core.TypeScript/inventory/

  lint-workflows:
    # actionlint catches .github/workflows/*.yml bugs: unknown
    # context refs, invalid runner labels, shellcheck-style warnings
    # on run: blocks, wrong action syntax. Round 33 static-analysis
    # expansion. See openspec/specs/static-analysis/profiles/
    # github-actions.md. This job installs only actionlint instead of running
    # the full install.sh bootstrap; otherwise apt proof/QEMU/R substrate can
    # consume the timeout before actionlint itself runs.
    name: lint (actionlint)
    timeout-minutes: 15
    runs-on: ubuntu-24.04

    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Install actionlint
        run: |
          set -euo pipefail
          go install github.com/rhysd/actionlint/cmd/actionlint@v1.7.12
          echo "$(go env GOPATH)/bin" >> "$GITHUB_PATH"

      - name: Run actionlint
        # -ignore 'unknown permission scope "administration"' is a
        # documented workaround for a known actionlint gap — the
        # `administration` permission IS valid in GitHub Actions
        # (used by .github/workflows/github-settings-drift.yml to
        # read rulesets + branch protection + security_and_analysis)
        # but actionlint v1.7.x has not yet added it to its allow-
        # list. Remove when actionlint catches up. See:
        # https://github.com/rhysd/actionlint/issues (search
        # "administration permission").
        run: actionlint -color -ignore 'unknown permission scope "administration"'

  lint-tick-history-order:
    # Validates that docs/hygiene-history/loop-tick-history.md
    # rows appear in non-decreasing chronological order
    # (specifically: the LAST row in the file is the latest
    # timestamp). This catches the recurring row-ordering bug
    # where the Edit tool's old_string=existing-line pattern
    # inserts the new row BEFORE the matched line, producing
    # reverse-chronological order. Maintainer 2026-04-26 asked for
    # structural prevention after the bug was caught at least
    # three times across recent ticks. The check is the
    # "fail-fast at commit/push time" mechanism that doesn't
    # rely on each agent's vigilance — Otto-339 anywhere-means-
    # anywhere applied to discipline-enforcement: enforce at
    # the layer that catches all paths, not at the input-tool
    # layer.
    name: lint (tick-history order)
    timeout-minutes: 12
    runs-on: ubuntu-24.04

    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      # This order check reads one tracked Markdown file and runs one tracked
      # TypeScript script. It does not consume any host or cluster toolchain;
      # use the package-pinned Bun path so apt mirror stalls cannot suppress the
      # ordering verdict.
      - name: Setup pinned Bun
        uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2.2.0
        with:
          bun-version: "1.3.13"

      - name: Install JavaScript dependencies from the locked graph
        run: bun install --frozen-lockfile

      - name: Run check-tick-history-order
        run: bun src/Core.TypeScript/hygiene/check-tick-history-order.ts

  lint-no-conflict-markers:
    # Fail if any committed file contains git merge-conflict markers
    # (`<<<<<<<`, `=======`, `>>>>>>>`). Maintainer 2026-04-26 ask:
    # *"maybe we should hygene for <<<<<<< HEAD and >>>>>>> ======= things
    # like that in our files incase we ever accidently botch a merge,
    # happens to the best of us humans too."*
    #
    # Per Otto-339 anywhere-means-anywhere: conflict markers in
    # substrate would shift weights wrongly when read by AI. Per
    # Otto-341 mechanism-not-vigilance: CI catches this regardless
    # of which agent / human / harness produced the merge. The
    # script self-allowlists files that legitimately document
    # merge-conflict resolution.
    name: lint (no conflict markers)
    timeout-minutes: 15
    runs-on: ubuntu-24.04

    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Cache install.sh outputs (mise runtimes + dotnet tools + verifier jars)
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.local/bin/mise
            ~/.local/share/mise
            ~/.cache/mise
            ~/.dotnet/tools
            ~/.elan
            ~/.config/zeta
            ~/.rustup/toolchains/1.87.0-*
            ~/.rustup/settings.toml
            ~/.rustup/update-hashes
            ~/.cargo
            ~/.cache/zeta/from-autotools-tarball/*.tgz
          key: install-v2-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('.mise.toml', 'tools/setup/**', 'global.json') }}

      - name: Install toolchain via three-way-parity script (GOVERNANCE §24)
        # Provides bun via mise's pin in .mise.toml. Same pattern as
        # memory-index-duplicate-lint.yml (#1377) +
        # memory-reference-existence-lint.yml (#1378). shellenv.sh
        # wires BASH_ENV so subsequent run: steps auto-source the
        # managed shellenv with bun on PATH.
        # tier=slim — this lane runs one bun hygiene script and nothing else, so it must not
        # fetch the 237 MB agda->ghc apt payload. Full measurement + reasoning at
        # the `lint (semgrep)` job above; pinned by
        # src/Core.TypeScript/ci/apt-manifest-host-tier.test.ts.
        env:
          ZETA_HOST_TIER: slim
        run: ./tools/setup/install.sh

      - name: Run check-no-conflict-markers
        run: bun src/Core.TypeScript/hygiene/check-no-conflict-markers.ts

  lint-archive-header-section33:
    # Fail if any courier-ferry / external-conversation import under
    # docs/research/** is missing the GOVERNANCE.md §33 4-field archive
    # boundary header in the first 20 lines, OR if its 'Operational
    # status:' value is not enum-strict ('research-grade' or
    # 'operational'). Factory observation 2026-04-26: §33 archive header
    # was the most-common review finding across the 11-refinement
    # courier-ferry lineage this session — every PR retrofitted post-
    # review.
    #
    # Per Otto-346 (recurring pattern → substrate primitive) +
    # Otto-341 (mechanism over vigilance), this lint catches the
    # discipline at the structural layer: agents and humans cannot
    # land courier-ferry imports without the §33 header anymore.
    #
    # 081KQ3HBZ0008QG0R001FPVG3Z Sub-task 2 — wire the lint to gate.yml after Sub-task 1
    # backfilled all pre-existing violations to 0.
    name: lint (archive header §33)
    timeout-minutes: 12
    runs-on: ubuntu-24.04

    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Cache install.sh outputs (mise runtimes + dotnet tools + verifier jars)
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.local/bin/mise
            ~/.local/share/mise
            ~/.cache/mise
            ~/.dotnet/tools
            ~/.elan
            ~/.config/zeta
            ~/.rustup/toolchains/1.87.0-*
            ~/.rustup/settings.toml
            ~/.rustup/update-hashes
            ~/.cargo
            ~/.cache/zeta/from-autotools-tarball/*.tgz
          key: install-v2-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('.mise.toml', 'tools/setup/**', 'global.json') }}

      - name: Install toolchain via three-way-parity script (GOVERNANCE §24)
        # Provides bun via mise's pin in .mise.toml. Same pattern as
        # memory-index-duplicate-lint.yml (#1377) +
        # memory-reference-existence-lint.yml (#1378). shellenv.sh
        # wires BASH_ENV so subsequent run: steps auto-source the
        # managed shellenv with bun on PATH.
        # tier=slim — this lane runs one bun hygiene script and nothing else, so it must not
        # fetch the 237 MB agda->ghc apt payload. Full measurement + reasoning at
        # the `lint (semgrep)` job above; pinned by
        # src/Core.TypeScript/ci/apt-manifest-host-tier.test.ts.
        env:
          ZETA_HOST_TIER: slim
        run: ./tools/setup/install.sh

      - name: Run check-archive-header-section33
        run: bun src/Core.TypeScript/hygiene/check-archive-header-section33.ts

  lint-section-33-migration-xrefs:
    # Fail if any live-nav surface (.claude/, memory/*.md top-level,
    # docs/backlog/, repo-root *.md) still references docs/research/<basename>
    # where <basename> has been migrated to memory/persona/<persona>/conversations/.
    #
    # Sibling of lint-archive-header-section33 (081KQ3HBZ0008QG0R001FPVG3Z): same shape, different
    # failure-class. The §33 migration pattern moves files but does not
    # auto-update backlinks; without this lint, dead xrefs accumulate silently
    # (empirical baseline 2026-05-15 before cleanup: 10 dead xrefs across 6 files;
    # full discovery: PR #3513 → Codex P2 → narrow fix PR #3529 → scanner
    # PR #3548 → baseline cleanup PR #3552).
    #
    # 081KRMEXM0008QG0R000K25MXS Slice B.3 + B.4 — wire the scanner to gate.yml in --enforce mode
    # after PR #3552 backfilled all pre-existing violations to 0.
    name: lint (§33 migration xrefs)
    timeout-minutes: 12
    runs-on: ubuntu-24.04

    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Cache install.sh outputs (mise runtimes + dotnet tools + verifier jars)
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.local/bin/mise
            ~/.local/share/mise
            ~/.cache/mise
            ~/.dotnet/tools
            ~/.elan
            ~/.config/zeta
            ~/.rustup/toolchains/1.87.0-*
            ~/.rustup/settings.toml
            ~/.rustup/update-hashes
            ~/.cargo
            ~/.cache/zeta/from-autotools-tarball/*.tgz
          key: install-v2-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('.mise.toml', 'tools/setup/**', 'global.json') }}

      - name: Install toolchain via three-way-parity script (GOVERNANCE §24)
        # tier=slim — this lane runs one bun hygiene script and nothing else, so it must not
        # fetch the 237 MB agda->ghc apt payload. Full measurement + reasoning at
        # the `lint (semgrep)` job above; pinned by
        # src/Core.TypeScript/ci/apt-manifest-host-tier.test.ts.
        env:
          ZETA_HOST_TIER: slim
        run: ./tools/setup/install.sh

      - name: Run audit-section-33-migration-xrefs (--enforce)
        run: bun src/Core.TypeScript/hygiene/audit-section-33-migration-xrefs.ts --enforce

  lint-tick-shard-relative-paths:
    # Fail if any tick shard at docs/hygiene-history/ticks/YYYY/MM/DD/HHMMZ.md
    # contains a broken relative-path link (target resolves outside the repo
    # root or to a missing file). Tick shards live 5 directories below docs/,
    # so the count-the-`..` pattern is error-prone — the agent shipped 5-`..`
    # paths twice in one session (PR #3676 / PR #3679, 2026-05-16) where
    # `../../../../../.claude/rules/X.md` lands at `docs/.claude/...` instead
    # of repo root. Copilot caught both via review threads.
    #
    # Lifecycle: discovery #3676/#3679 → narrow fix #3680 → scanner #3692 →
    # filter + quality rounds #3692 (fixups) → baseline mechanism #3699 →
    # CI enforce gate (this job). The `--baseline` flag grandfathers
    # pre-existing findings recorded in audit-tick-shard-relative-paths.baseline.json
    # so historical residue in immutable shards doesn't block — new violations
    # still fail. Same shape as Stryker `--reset` or ESLint suppressions.
    name: lint (tick-shard relative-paths)
    timeout-minutes: 12
    # RAISED 5 -> 12 (2026-08-01). This job was CANCELLED by its own budget four times today.
    # It is not randomly flaky — it is systematically near the ceiling: the lint itself runs in
    # 120-158s, but the job first runs ./tools/setup/install.sh (the full three-way-parity
    # toolchain per GOVERNANCE §24), and a slightly slow install pushes the total past 300s.
    # Observed cancellations at 316s and 317s against a 300s budget.
    #
    # A cancelled job reports as a RED CHECK indistinguishable from a real failure, so this cost
    # four spurious reds and several rerun cycles on unrelated PRs today.
    #
    # DEEPER ISSUE, not fixed here: audit-tick-shard-relative-paths.ts imports ONLY node built-ins
    # (node:fs, node:path, node:child_process). It needs a JS runtime and nothing else, yet
    # installs an entire toolchain to get one. gate.yml runs install.sh in 64 jobs and setup-bun in
    # 2. Either caching install.sh's output or using setup-bun for runtime-only jobs would remove
    # this whole class — but the cache is already at 10.05GB of a 10GB ceiling, and §24 makes
    # install.sh the single install path, so both need a deliberate decision rather than a
    # drive-by change.
    runs-on: ubuntu-24.04

    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Install toolchain via three-way-parity script (GOVERNANCE §24)
        # tier=slim — this lane runs one bun hygiene script and nothing else, so it must not
        # fetch the 237 MB agda->ghc apt payload. Full measurement + reasoning at
        # the `lint (semgrep)` job above; pinned by
        # src/Core.TypeScript/ci/apt-manifest-host-tier.test.ts.
        env:
          ZETA_HOST_TIER: slim
        run: ./tools/setup/install.sh

      - name: Run audit-tick-shard-relative-paths (--enforce --baseline)
        run: |
          bun src/Core.TypeScript/hygiene/audit-tick-shard-relative-paths.ts \
            --enforce \
            --baseline src/Core.TypeScript/hygiene/audit-tick-shard-relative-paths.baseline.json

  build-graph-completeness:
    # The acceptance gate on wiring job selection to `ace/build-graph.json`.
    #
    # An affected-set query is only sound if the graph is COMPLETE over the CI
    # domain: every target claims a leg, every leg resolves to a real job, every
    # job is claimed by a target. Break any of those and selecting jobs from the
    # graph SILENTLY SKIPS work -- and a job that should have run and did not is
    # indistinguishable from one that ran and passed.
    #
    # It found three real defects when written (2026-08-19), all live on main:
    #   * `lean-proof/build` and `tlaps-proof/tlaps` named jobs that do not exist
    #     (they are `type-check` and `prove`), so a wired graph would have skipped
    #     the Lean and TLA+ proofs entirely;
    #   * 35 Rust crates carried `legs: []` while `gate/lint-rust` demonstrably
    #     lints every crate under src/;
    #   * 18 jobs -- including lint-fsharp, lint-csharp and lint-rust -- were
    #     claimed by no target at all.
    #
    # BUN ONLY, on purpose, and the reason is this job's own subject matter: it
    # needs one 178 MB runtime, and `./tools/setup/install.sh` provisions the union
    # of every toolchain in the repo. Same rationale the lint-structural-hygiene
    # job below already states.
    name: lint (build-graph completeness)
    timeout-minutes: 5
    runs-on: ubuntu-24.04

    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Setup Bun
        uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2.2.0

      - name: Graph is complete over the CI domain (fails closed)
        run: bun src/Core.TypeScript/hygiene/audit-build-graph-completeness.ts

      # Manifesto §1 for repo topology: `ace` may be the good resolution path and
      # may never be the only one. Guarded here rather than later because the guard
      # is free while `ace` is optional and unretrofittable once the fleet depends
      # on it. Rule: .claude/rules/clone-at-tag-stays-sufficient.md
      - name: Clone-at-a-tag is still sufficient (no ace-as-resolver)
        run: bun src/Core.TypeScript/hygiene/lint-clone-at-tag-is-sufficient.ts

      # A flake ref evaluates PURE by default, and pure-eval `builtins.pathExists`
      # on an absolute path returns FALSE rather than erroring (measured 2026-08-21,
      # Nix 2.34.6). Every full-ai-cluster module reading /etc/zeta guards its
      # readFile behind that pathExists, so a rebuild without --impure silently
      # reverts hostname / k3s join endpoint / segment address and DROPS the
      # operator's authorized SSH keys. Guarded here because the failure is silent.
      - name: Documented flake rebuilds still carry --impure (silent-revert guard)
        run: bun src/Core.TypeScript/hygiene/lint-nixos-rebuild-needs-impure.ts

      - name: Audit + selector unit tests (a check that cannot fail is not a check)
        run: |
          bun test \
            src/Core.TypeScript/hygiene/audit-build-graph-completeness.test.ts \
            src/Core.TypeScript/hygiene/lint-clone-at-tag-is-sufficient.test.ts \
            src/Core.TypeScript/hygiene/lint-nixos-rebuild-needs-impure.test.ts \
            src/Core.TypeScript/ci/affected-legs.test.ts

      # OBSERVE-ONLY. Nothing reads these outputs yet, and that is deliberate: the
      # derivation is published and compared against what the gate actually ran, so
      # it is checked against reality BEFORE any `if:` depends on it. Flipping the
      # guards on an unproven selector is the same class of mistake as trusting an
      # incomplete graph. The mapping is DERIVED here at run time and is never
      # hand-maintained in this file -- a second copy of the graph written as `if:`
      # expressions is how a wired graph becomes unwired again.
      - name: Affected legs for this change (observe-only, nothing consumes it)
        if: github.event_name == 'pull_request'
        run: |
          bun src/Core.TypeScript/ci/affected-legs.ts \
            --base "${{ github.event.pull_request.base.sha }}" \
            --head "${{ github.event.pull_request.head.sha }}"

  lint-structural-hygiene:
    # Keep repository-structure guards that are still active after the
    # docs/backlog cutover. Legacy backlog-ID uniqueness/status checks are
    # intentionally not run here: new work-items are ZetaId-backed under
    # workitems/, while docs/backlog is frozen historical substrate.
    name: lint (structural hygiene)
    timeout-minutes: 5
    runs-on: ubuntu-24.04

    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          # Need merge-base history for manus commit-message leak scan.
          fetch-depth: 0

      # These repository-structure guards need Bun + Git only. Running the full
      # parity installer here lets unrelated compiler downloads consume this
      # job's five-minute budget before any guard executes.
      - name: Setup Bun
        uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2.2.0

      # Dangling tracked symlinks crash the GitHub Pages (Jekyll) build on realpath — invisible
      # to the test suites. Surfaced 2026-06-11: #7820 moved 31 dirs to db/ and nine relative
      # links dangled; Pages was red for hours on every push.
      - name: Run audit-dangling-symlinks (Pages/Jekyll guard)
        run: bun src/Core.TypeScript/hygiene/audit-dangling-symlinks.ts

      # docs/observe-events/ holds three filename schemes; only one of them (32-hex ZetaId)
      # carries no prefix, so a name that is hex-but-not-an-id is indistinguishable from an
      # id by shape. Three such names arrived 2026-08-09 from an id "mint" that did no
      # hashing, passed every `/^[0-9a-f]{32}$/` check in the repo, and produced a false
      # positive in a ZetaId audit (#10690). This decodes the version field instead of
      # trusting the regex, and carries a scan floor so it cannot pass by inspecting nothing.
      - name: Run audit-observe-event-filenames (ZetaId-vs-hex ambiguity guard)
        run: bun src/Core.TypeScript/hygiene/audit-observe-event-filenames.ts

      # 081KTSZN10008QG0R002J0GE0Z Reticulum-only clause: files marked SEALED-ROOM carry zero ambient-channel
      # tokens (fs/process/network/clock/entropy/Task.Run); falsifiers waive per-line, visibly.
      - name: Run audit-sealed-rooms (081KTSZN10008QG0R002J0GE0Z Reticulum-only lint half)
        run: bun src/Core.TypeScript/hygiene/audit-sealed-rooms.ts

      # 081KWMY831H — Manus/Lumen shell-wrapper must not leak into commit subjects.
      - name: Run lint-no-manus-commit-leak (081KWMY831H)
        run: bun src/Core.TypeScript/hygiene/lint-no-manus-commit-leak.ts

      # The four-oracle byte-lock pins field VALUES, not field NAMES — so two oracles
      # could declare the same schema id with different key sets and every existing
      # check stayed green. Live instance: PR #10722 gave TypeScript's
      # TemperatureReadout a required `fidelity` that F#'s does not have, both still
      # declaring `zeta.temperature.readout.v1`; the three suites that read that treaty
      # reported 51 pass / 0 fail / exit 0 with the divergence in place.
      # --min-schemas is a SCAN FLOOR: if a moved path or a changed glob makes the audit
      # inspect fewer schemas than stated, it exits non-zero instead of reporting
      # success — a check that did not run must not look like one that passed.
      - name: Run audit-schema-key-set-parity (cross-oracle schema key sets)
        run: bun src/Core.TypeScript/hygiene/audit-schema-key-set-parity.ts --min-schemas 6

      # Every Actions job must declare timeout-minutes. An unbounded job can hold a
      # runner until the 6h platform cap: on 2026-08-18 lint-autofix held a slot 90+
      # minutes and backlog-index-integrity 140.5, both stalled in install.sh on a hung
      # apt mirror. Slots never freed, concurrency cancelled the constituent lint jobs,
      # and gate (required) correctly read cancelled as failure — PRs went red with no
      # real defect. GitHub has no repo-level default job timeout and workflow defaults:
      # does not cover it, so per-job declaration is the only mechanism and this lint is
      # what keeps the class closed. Carries a scan floor so it cannot pass by
      # inspecting nothing.
      - name: Run lint-workflow-job-timeouts (unbounded-runner-slot guard)
        run: bun src/Core.TypeScript/hygiene/lint-workflow-job-timeouts.ts

      # A lane that COMPUTES a drift artifact must be able to PUBLISH it, and must fail when
      # it cannot. `drift-sweep` pushed its ledger at `main` from 2026-08-13, was refused
      # every run by the "CI Gate" ruleset, swallowed the refusal with
      # `git push || echo "push race"`, and concluded SUCCESS 1,597 times while
      # `docs/drift-events/` stayed frozen at tick 000247 — ten days of dashboard data
      # served from behind a green check (081M0QPZD9C087G0R002W8QC2A). The `||` is the
      # sharp part: it misfiled a PERMANENT rule rejection as a TRANSIENT race, so the lane
      # kept "recovering" from something that could never resolve.
      #
      # Three rules over lanes derived from content (never a hand-kept allowlist): no
      # publication push at `main`, no publish failure routed into a non-fatal handler, and
      # a real `git push --dry-run` capability probe — presence of a token is not capability,
      # which is the check that would have caught this on day one instead of day ten. The
      # `continue-on-error` half cannot be caught downstream at all: the REST jobs API
      # reports a swallowed STEP's conclusion as `success`, so static text is the only
      # channel that carries it.
      - name: Run lint-drift-publication-lands (stale-behind-green guard)
        run: bun src/Core.TypeScript/hygiene/lint-drift-publication-lands.ts

      # A workflow file GitHub will never read is not a check -- and it is worse than a
      # missing one, because a missing check is visibly missing. Live instance 2026-08-25:
      # agentic-organization/.github/workflows/ci.yml ran a 1,595-test suite and had never
      # executed once, because Actions reads only the ROOT .github/workflows/. The two
      # scaffold templates under src/Core.TypeScript/scaffold are exempt and SAID SO on
      # every run -- the exemption is derived from create-repo.ts sitting beside them, so
      # deleting that tool expires it rather than leaving an allowlist behind.
      - name: Run lint-no-nested-workflow-dirs (a workflow that never runs is not a check)
        run: bun src/Core.TypeScript/hygiene/lint-no-nested-workflow-dirs.ts

      # An ubuntu runner always has 16 GB, so an UNDECLARED host tier is not adaptation --
      # it is the constant `full`, i.e. 388 apt packages / 713.0 MiB on every run. Measured
      # 2026-08-25: ci-cache-paths-lint failed 5 of its last 20 runs, all five inside the
      # install step (exit 124, stalled on pandoc 26.9 MB and r-base-core 27.1 MB), while
      # the check NAMED "audit actions/cache paths vs git ls-files" never reached its audit.
      # This refuses SILENCE, never a particular tier: declaring `full` is a fine answer.
      - name: Run audit-install-tier-declared (undeclared apt payload guard)
        run: bun src/Core.TypeScript/hygiene/audit-install-tier-declared.ts

      - name: Workflow-shape guard falsifiers (a check that cannot fail is not a check)
        run: |
          bun test \
            src/Core.TypeScript/hygiene/lint-no-nested-workflow-dirs.test.ts \
            src/Core.TypeScript/hygiene/audit-install-tier-declared.test.ts

      # A privilege elevator resolved by NAME is substitutable by any writable directory
      # earlier on PATH, and planting one leaves NO GIT DIFF -- so review, AgencySignature
      # and byte-lock, all of which watch the repo, cannot see it. That was live on main as
      # a P1 (docs/BUGS.md, 2026-08-24): the shared biometric approval gate returned
      # ok:true from a shim that executed nothing. Deliberately NOT
      # `sonarjs/no-os-command-from-path` repo-wide: that rule was available and did not
      # catch this one -- it matches on the CALLEE, so a `run()` wrapper, an argv-prefix
      # array and a program held in a variable are all invisible to it.
      - name: Run lint-no-path-resolved-privilege-elevator (PATH-forgeable sudo guard)
        run: bun src/Core.TypeScript/hygiene/lint-no-path-resolved-privilege-elevator.ts

      - name: Elevator resolver + lint falsifiers (a check that cannot fail is not a check)
        run: |
          bun test \
            src/Core.TypeScript/privilege/elevator.test.ts \
            src/Core.TypeScript/hygiene/lint-no-path-resolved-privilege-elevator.test.ts \
            tools/setup/persona-keys/biometric.test.ts

      # Tsirelson's bound is S <= 2*sqrt(2) ~= 2.828 on the CHSH correlator, implemented at
      # src/Core/Tsirelson.fs. 1/(3*sqrt(2)) ~= 0.2357 is a DIFFERENT number: the image of
      # S = 2*sqrt(2) under the freely chosen linear map rho = S/12, adjudicated as a DESIGN
      # CHOICE. It is a legitimate design threshold and every current use of it is
      # behaviourally correct -- but every time the physicist's name got attached to it,
      # something broke. Z-3 and Z-5 were DEMOTED section-A -> section-B and the
      # Zeta-Conjecture keystone claim was REFUTED, all over this one constant.
      # Aaron 2026-08-23: "most 1/(3sqrt2) have turned out to be bugs."
      #
      # The 2026-08-01 response was a hand-propagated prose caveat. A prose sweep is a
      # SNAPSHOT: not idempotent, and it does not survive new files. Measured 2026-08-23, the
      # two 2026-07-16 sibling docs had drifted into DISAGREEING WITH EACH OTHER -- one
      # carried the banner, one did not, and neither could know. This guard is that same
      # correction expressed as a check instead of prose: the repo's own doctrine (prose rots,
      # a check does not) applied to its own correction.
      #
      # It refuses the NAME, never the NUMBER -- no value and no behaviour is in its scope.
      # Its caveat markers are DERIVED from the provenance doc, which it DISCOVERS by shape
      # rather than hardcoding, so renaming that doc reddens every stale citation instead of
      # silently disabling the guard. Sibling from the same 2026-08-01 audit:
      # lint-discharge-certificate-consistency.ts (in the lint (no empty dirs) job).
      - name: Run lint-tsirelson-constant-caveat (1/(3√2) is not a Tsirelson bound)
        run: bun src/Core.TypeScript/hygiene/lint-tsirelson-constant-caveat.ts

      # The guard above is only worth its runtime if it can still FIRE. Two of these tests
      # exist because earlier drafts of it were VACUOUS on the real tree -- markers that
      # green-lit the very files they were written for. Pinned so they cannot come back.
      - name: Tsirelson-caveat guard unit tests (a check that cannot fail is not a check)
        run: bun test src/Core.TypeScript/hygiene/lint-tsirelson-constant-caveat.test.ts

      # NOTE (2026-08-25): a `lint-no-direct-push-to-main` (PM001) check was wired in here
      # and has been withdrawn. Not because the defect it found was unreal — three lanes on
      # this branch are fixed BECAUSE of it — but because a check named "no direct push to
      # main" states a doctrine this repo has since decided against: #15313 designs the
      # heartbeat lane to push to `main` DIRECTLY, with a content-bound attestation and a
      # post-hoc audit instead of a gate. The surviving rule is "no UNATTESTED direct push",
      # which is `audit-heartbeat-lane-attestations.ts` in the `cross-verify` floor, not a
      # blanket ban. Its one mechanical finding that neither AH001 nor AH002 could see —
      # a skip token BUILT by `echo` into `git commit --file=-` — was folded into AH002
      # itself rather than shipped as a third overlapping check.

  lint-no-empty-dirs:
    # Fail if a committed directory has no files — almost always a
    # forgotten artefact (an agent-created skill folder without a
    # SKILL.md, a research folder with no report). Born round 35 after
    # Aaron: "we need a build script that will fail the build if it
    # detects an empty folder". Script under src/Core.TypeScript/lint/ respects
    # .gitignore and an explicit allowlist at
    # src/Core.TypeScript/lint/no-empty-dirs.allowlist. Upstream mirrors under
    # references/prior-art/** are excluded by the script itself.
    # No untrusted input used in run: — only a fixed repo path.
    name: lint (no empty dirs)
    timeout-minutes: 5
    runs-on: ubuntu-24.04

    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      # This source-tree lint needs Bun + Git only. Running the full parity
      # installer here makes an unrelated Zig/Lean/QEMU download capable of
      # consuming the entire five-minute lint budget before the check starts.
      - name: Setup Bun
        uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2.2.0

      - name: Run no-empty-dirs
        run: bun src/Core.TypeScript/lint/no-empty-dirs.ts

      # 081M010W1BP087G0R002M2BNVW — Heat.fs decided "is this kind pressure?" twice, by two
      # independent routes that disagreed on kinds carrying both a forgetting and a pressure
      # token. That bit is the discriminator for the composition law (deferral composes,
      # destruction does not), so two answers defeat the point of the law being decidable.
      # #10804 collapsed both routes onto one ordered chain; this is the standing guard that a
      # second one cannot come back. PART A refuses a dual-token kind literal; PART B
      # DISCOVERS the single classifier — it never pins the name, because pinning `ofKind` is
      # exactly what made the first draft of this lint go red on a correct main — and refuses
      # a second decider, an inline substring probe, an orphan probe, and a membership split
      # between the two pressure tables. Every extraction route carries its own scan floor, so
      # a route that goes dark fails instead of reading OK.
      - name: Guard — one classifier for the heat deferral/destruction bit (081M010W1BP087G0R002M2BNVW)
        run: bun src/Core.TypeScript/hygiene/lint-heat-kind-classifier-agreement.ts

      - name: Guard — discharge rows may not contradict their own certificates
        # 2026-08-01: five rows were written "§A — DISCHARGED" while Z-2's own cited
        # certificate said status:"OPEN" (94.7% error vs a 25% threshold). The script
        # honestly reported failure; the register cell overrode it in prose. §A is the
        # frozen core, so a false row there is an integrity defect. This catches that
        # class MECHANICALLY — no judgement about whether a discharge is *good*, only a
        # refusal of rows that contradict (or are missing) their cited evidence.
        run: bun src/Core.TypeScript/hygiene/lint-discharge-certificate-consistency.ts

      - name: Guard — the child-floor jurisdiction registry (081M0TJXY32087G0R003TBTR7V)
        # The PREDICATE (protect children) is invariant; the THRESHOLD (~16-21) is a
        # jurisdictional parameter. `Safety/ChildFloorPolicy.lean` proves, for EVERY
        # registry, that no reading can lower the floor and that an unknown jurisdiction
        # denies — so a bad entry cannot break safety, it is simply refused and the
        # resolver falls through to the protective bound. That silence is why this lint
        # exists: a jurisdiction that believes it declared 15 would get 21 and never learn.
        # It also byte-locks the band across its three copies (Lean / TypeScript / JSON),
        # because three copies of a safety constant is a drift bug waiting to happen.
        run: bun src/Core.TypeScript/hygiene/lint-child-floor-registry.ts

      - name: Child-floor policy falsifiers (081M0TJXY32087G0R003TBTR7V)
        # The resolver's own test suite. It lives outside src/Core.TypeScript/hygiene/, so
        # the hygiene suite does not reach it and only the non-blocking bare `bun test` job
        # would — which is too far from the change for a SAFETY floor. Named here so it runs
        # on every PR beside the lint it belongs to. Its sabotage registries are the same
        # ones ChildFloorPolicy.lean §6 refuses, so the two oracles refuse the same attacks
        # rather than each passing their own.
        run: bun test src/Core.TypeScript/child-floor/

      - name: Guard — a golden-vector treaty may not claim a rule its vectors do not pin
        # PR #10759 was filed on the premise "the tie-break behaviour is byte-locked into
        # the four-oracle treaty." Measured, that premise was false: first-occurrence and
        # ordinal-minimum each changed 0 of 7 pinned vectors, because six of the seven had
        # no tie reaching quorum at all. "The treaty pinned a POINT; the prose claimed a
        # RULE." This is spec-side mutation testing — mutate the RULE, count the vectors
        # that change, and refuse a declared exclusion that changes zero. Cross-oracle byte
        # agreement (the treaty's other, real job) is deliberately NOT audited here.
        run: bun src/Core.TypeScript/hygiene/lint-treaty-rule-discrimination.ts

  lint-markdown:
    # markdownlint-cli2 on every .md file outside the ignore list in
    # .markdownlint-cli2.jsonc. Round 33 static-analysis expansion
    # (docs + specs + skills are a huge surface; drift is easy).
    # See openspec/specs/static-analysis/profiles/markdown.md.
    name: lint (markdownlint)
    timeout-minutes: 12
    runs-on: ubuntu-24.04

    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          # The pull_request ref is a synthetic merge commit. Its first parent
          # must be present for the PR-scoped markdown diff below.
          fetch-depth: 2

      # This job needs Bun, markdownlint-cli2, and tracked TypeScript only.
      # Running the full workstation/cluster bootstrap made a 553 MB apt
      # manifest download capable of consuming its 12-minute job cap before
      # markdownlint began. `packageManager` pins this runtime and
      # `bun.lock` pins the linter; the full setup graph remains the path for
      # developer machines, cluster nodes, and jobs that consume its tools.
      - name: Setup pinned Bun
        uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2.2.0
        with:
          bun-version: "1.3.13"

      - name: Install JavaScript dependencies from the locked graph
        run: bun install --frozen-lockfile

      - name: Run markdownlint (scoped to the PR's own diff on pull_request)
        # Drift-and-heal ADR item 4 (workitem 081KX3KA3ES08QG0R003TW3XDE, the
        # priority-inversion fix): on pull_request this job fails ONLY on
        # findings in files the PR itself touches; findings in unrelated files
        # print as out-of-scope drift (informational — the drift-sweep ledger
        # on main owns them, and the SLO auto-files work when they linger).
        # On push/main the whole tree still lints: that run IS the detector.
        # HEAD is the merge commit on pull_request, so HEAD^1..HEAD is exactly
        # this PR's changes against current main — no user-controlled ref
        # names touched (safe-pattern: first-party context only). The
        # `|| true` inside the group is load-bearing: the default shell is
        # `bash -e -o pipefail`, and the verdict must come from scoped-lint
        # (rightmost pipe stage), not the raw linter.
        run: |
          if [ "$GITHUB_EVENT_NAME" = "pull_request" ]; then
            git diff --name-only HEAD^1 HEAD > /tmp/changed-files.txt
            git ls-files > /tmp/tracked-files.txt
            { bunx --no-install markdownlint-cli2 "**/*.md" 2>&1 || true; } \
              | bun src/Core.TypeScript/hygiene/scoped-lint.ts \
                  --changed /tmp/changed-files.txt --tracked /tmp/tracked-files.txt
          else
            bunx --no-install markdownlint-cli2 "**/*.md"
          fi

  lint-typescript:
    # The TypeScript member of the per-language lint family (lint (F#) / (C#) /
    # (Go) / (Python) / (Rust) / (TS)). Strict typecheck on every TypeScript file
    # via `tsc --noEmit -p tsconfig.json`. Round 35 static-analysis expansion per 081KQDTYV0008QG0R000XFYDTT:
    # ESLint with typed-linting catches *most* TS issues but not all
    # assignability narrowings. Slice 9 (PR #882) shipped a real
    # TS2322 (`Type 'string' is not assignable to type "head" | ...`)
    # to main because gate.yml previously had `dotnet build` for F#/C#
    # but no tsc step for `tools/**/*.ts`. PR #887 fixed the bug; this
    # job is the missing gate that prevents the class. Same shape as
    # round-30 semgrep elevation: codified rules (tsconfig strict mode)
    # without a gate aren't a control.
    name: lint (TS)
    timeout-minutes: 12
    runs-on: ubuntu-24.04

    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      # Strict TypeScript analysis consumes only the repository's Bun runtime
      # and lockfile-managed dev dependencies. Do not download the complete
      # apt workstation/cluster manifest before this narrow check starts.
      - name: Setup pinned Bun
        uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2.2.0
        with:
          bun-version: "1.3.13"

      - name: Install npm devDependencies (typescript@6.0.3 + eslint stack)
        # tsc lives in node_modules/.bin via the typescript devDep
        # pinned in package.json + bun.lock. --frozen-lockfile fails
        # if the lockfile is out of date with package.json — same
        # discipline as bun's own CI patterns.
        run: bun install --frozen-lockfile

      - name: Run TypeScript type check (tsc --noEmit, via the orchestrator)
        # tsc ONLY. This step used to be named "Run TypeScript Lint Script" and
        # commented "Type check and lint checks" — no eslint, prettier or
        # stylelint runs here or anywhere in this job, and the runner itself used
        # to print that they had passed. Both statements are removed rather than
        # made true: wiring eslint on is a separate, measured decision
        # (workitem 081M0RBXF6J087G0R0023EX9X2 — 14,218 errors in one directory).
        # A step name is read as a claim about what ran; keep it to what runs.
        run: bun src/Core.TypeScript/lint/lint-typescript.ts

      # ESLint over the paths MEASURED CLEAN, and only those
      # (workitem 081M0RBXF6J087G0R0023EX9X2). eslint has been configured,
      # pinned and named in this workflow since day one while running nowhere;
      # `lint (twitch-ai app)` (b1967172d, 2026-08-23T23:24:02Z) was the first
      # path it actually ran on. This is the second, chosen by measurement
      # rather than taste.
      #
      # Repo-wide the config finds 19,685 errors over 2,500 files, so a blanket
      # `eslint .` would redden main by five figures. The roster in
      # `lint:eslint` is every directory that measured EXACTLY ZERO on
      # 6fb8f3149, minus `infra/k8s` — 102 files. `infra/k8s` measured zero too,
      # but it is the STALE cluster declaration awaiting deletion
      # (081M00QCHWA087G0R000GKKRXD): `audit-cluster-tree-consumers` went red on
      # this very PR because naming it in package.json was new coupling to a tree
      # whose roster must only SHRINK. Dropped rather than rostered — a lint
      # roster is not a reason to keep a doomed tree alive. It locks the standard in where it already holds
      # and costs nothing to satisfy today; the other ~19.6k errors stay a
      # measured list in the workitem, not a silenced one. No rule is disabled,
      # no path is ignored, nothing is downgraded to a warning.
      #
      # Widening = add a path to `lint:eslint` once it measures zero. The same
      # script is what a laptop and a devcontainer run (`bun run lint:eslint`),
      # so the three surfaces cannot drift.
      #
      # Cost: ~13s on this machine over 107 files (the typed-linting program
      # load dominates, not the file count), inside a job already provisioned
      # with bun + the eslint stack. No new job, no new runner, no new install.
      - name: ESLint over the measured-clean path roster
        run: bun run lint:eslint

  lint-twitch-ai:
    # twitch-ai is a self-contained Vite workspace (own tsconfig: lib DOM +
    # vite/client types, own bun.lock) that NO CI job ran — the app's real
    # build was invisible until someone ran it by hand
    # (workitems/081M0QF7ZVY087G0R003Q4Q18D; two main-is-red workitems came
    # out of that gap). The root `lint (TS)` reaches these files only
    # incidentally, under ROOT settings (bun types, no DOM), not the app's.
    # This job runs the workspace's OWN typecheck, the repo ESLint config
    # over the app tree — the first narrow-path slice of
    # workitems/081M0RBXF6J087G0R0023EX9X2 ("eslint configured, pinned and
    # named in CI but never invoked"; that workitem's own prescription is
    # "turn the tool on for a narrow path first and widen") — and the real
    # `vite build`.
    # Drift check, not floor: adding a job to `gate (required)`'s needs is a
    # treaty amendment (see the floor comment there); that decision stays
    # with the consent path.
    name: lint (twitch-ai app)
    timeout-minutes: 12
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Setup pinned Bun
        uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2.2.0
        with:
          bun-version: "1.3.13"

      - name: Install root devDependencies (the eslint stack)
        run: bun install --frozen-lockfile

      - name: Install app dependencies (the app's own lockfile)
        run: bun install --frozen-lockfile
        working-directory: src/apps/twitch-ai

      - name: Typecheck under the app's own tsconfig
        run: bun run typecheck
        working-directory: src/apps/twitch-ai

      - name: ESLint over the app tree (repo config; rc captured directly)
        run: bunx eslint src/apps/twitch-ai/src

      - name: Vite build (the app's real build, exactly what deploys)
        run: bun run build
        working-directory: src/apps/twitch-ai

  lint-identity-dla:
    # demo/identity-dla-site is a self-contained pnpm/Vite workspace (own
    # tsconfig, own pnpm-lock.yaml, packageManager pin) that NO PR-time job
    # checked — its tsc lived solely inside pages-deploy.yml, which is not a
    # required check. Measured cost of that gap: PR #14523 merged `*/` inside
    # a block comment in OracleWorm.tsx, every Pages deploy went red for ~2h
    # (10 consecutive failures across unrelated shas) and the live page
    # silently served a stale bundle. Workitem 081M0RN9FW3087G0R000EBT0HH;
    # the break itself was fixed in #14604 — this job is the standing half.
    # It runs the DEPLOY'S OWN build entry (install → check → pages vite
    # build), so the job and the deploy cannot drift apart; pnpm resolves
    # from the site's own packageManager pin inside that script. The Go WASM
    # oracle step degrades to a logged skip here (no ZETA_PAGES_REQUIRE_GO):
    # this job's subject is the site's TypeScript, and pages-deploy still
    # enforces the Go half where its setup-go step guarantees the toolchain.
    # Drift check, not floor: adding a job to `gate (required)`'s needs is a
    # treaty amendment (see the floor comment there); that decision stays
    # with the consent path.
    name: lint (identity-dla site)
    timeout-minutes: 12
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Setup pinned Bun
        uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2.2.0
        with:
          bun-version: "1.3.13"

      - name: Install root dependencies (the builder's imports)
        run: bun install --frozen-lockfile

      - name: Build the site exactly as pages-deploy does (install + check + pages build)
        run: bun ./src/Core.TypeScript/discovery/identity-dla-pages-build.ts

  # Per-language lint jobs (split from the former single sequential
  # "lint (F#, C#, Go, Python, Rust)" job, 2026-06-13). The combined job ran the
  # five lint scripts in sequence and SHORT-CIRCUITED at the first failure, so a
  # multi-language change surfaced one red per CI cycle (the 2026-06-13 rollout
  # took several PRs to clear breakage CI revealed one language at a time). Five
  # parallel jobs run independently: CI surfaces ALL language reds in one run, and
  # each shows as its own status check. Setup is replicated per-job (the gate's
  # established pattern — 20 jobs inline `install.sh`; no composite action in repo).
  # `bun run preflight` is the local mirror.

  lint-fsharp:
    name: lint (F#)
    timeout-minutes: 15
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Cache install.sh outputs (mise runtimes + dotnet tools + verifier jars)
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.local/bin/mise
            ~/.local/share/mise
            ~/.cache/mise
            ~/.dotnet/tools
            ~/.elan
            ~/.config/zeta
            ~/.rustup/toolchains/1.87.0-*
            ~/.rustup/settings.toml
            ~/.rustup/update-hashes
            ~/.cargo
            ~/.cache/zeta/from-autotools-tarball/*.tgz
          key: install-v2-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('.mise.toml', 'tools/setup/**', 'global.json') }}
      - name: Install toolchain via three-way-parity script (GOVERNANCE §24)
        # tier=slim — this lane runs `bun src/Core.TypeScript/lint/lint-fsharp.ts` and nothing else, so it must not
        # fetch the 237 MB agda->ghc apt payload. Full measurement + reasoning at
        # the `lint (semgrep)` job above; pinned by
        # src/Core.TypeScript/ci/apt-manifest-host-tier.test.ts.
        env:
          ZETA_HOST_TIER: slim
        run: |
          set -euo pipefail
          for attempt in 1 2 3 4 5; do
            if ./tools/setup/install.sh; then exit 0; fi
            [ "$attempt" = "5" ] && { echo "install.sh failed after 5 attempts"; exit 1; }
            case "$attempt" in
              1) backoff=10 ;;
              2) backoff=30 ;;
              3) backoff=60 ;;
              4) backoff=120 ;;
            esac
            echo "install.sh attempt $attempt failed; retrying in ${backoff}s..." >&2
            sleep "$backoff"
          done
      - name: Run F# Lint Script
        run: bun src/Core.TypeScript/lint/lint-fsharp.ts

  lint-csharp:
    name: lint (C#)
    timeout-minutes: 15
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Cache install.sh outputs (mise runtimes + dotnet tools + verifier jars)
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.local/bin/mise
            ~/.local/share/mise
            ~/.cache/mise
            ~/.dotnet/tools
            ~/.elan
            ~/.config/zeta
            ~/.rustup/toolchains/1.87.0-*
            ~/.rustup/settings.toml
            ~/.rustup/update-hashes
            ~/.cargo
            ~/.cache/zeta/from-autotools-tarball/*.tgz
          key: install-v2-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('.mise.toml', 'tools/setup/**', 'global.json') }}
      - name: Install toolchain via three-way-parity script (GOVERNANCE §24)
        # tier=slim — this lane runs `bun src/Core.TypeScript/lint/lint-csharp.ts` and nothing else, so it must not
        # fetch the 237 MB agda->ghc apt payload. Full measurement + reasoning at
        # the `lint (semgrep)` job above; pinned by
        # src/Core.TypeScript/ci/apt-manifest-host-tier.test.ts.
        env:
          ZETA_HOST_TIER: slim
        run: |
          set -euo pipefail
          for attempt in 1 2 3 4 5; do
            if ./tools/setup/install.sh; then exit 0; fi
            [ "$attempt" = "5" ] && { echo "install.sh failed after 5 attempts"; exit 1; }
            case "$attempt" in
              1) backoff=10 ;;
              2) backoff=30 ;;
              3) backoff=60 ;;
              4) backoff=120 ;;
            esac
            echo "install.sh attempt $attempt failed; retrying in ${backoff}s..." >&2
            sleep "$backoff"
          done
      - name: Run C# Lint Script
        run: bun src/Core.TypeScript/lint/lint-csharp.ts

  lint-go:
    name: lint (Go)
    timeout-minutes: 15
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Cache install.sh outputs (mise runtimes + dotnet tools + verifier jars)
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.local/bin/mise
            ~/.local/share/mise
            ~/.cache/mise
            ~/.dotnet/tools
            ~/.elan
            ~/.config/zeta
            ~/.rustup/toolchains/1.87.0-*
            ~/.rustup/settings.toml
            ~/.rustup/update-hashes
            ~/.cargo
            ~/.cache/zeta/from-autotools-tarball/*.tgz
          key: install-v2-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('.mise.toml', 'tools/setup/**', 'global.json') }}
      - name: Install toolchain via three-way-parity script (GOVERNANCE §24)
        # tier=slim — this lane runs `bun src/Core.TypeScript/lint/lint-go.ts` and nothing else, so it must not
        # fetch the 237 MB agda->ghc apt payload. Full measurement + reasoning at
        # the `lint (semgrep)` job above; pinned by
        # src/Core.TypeScript/ci/apt-manifest-host-tier.test.ts.
        env:
          ZETA_HOST_TIER: slim
        run: |
          set -euo pipefail
          for attempt in 1 2 3 4 5; do
            if ./tools/setup/install.sh; then exit 0; fi
            [ "$attempt" = "5" ] && { echo "install.sh failed after 5 attempts"; exit 1; }
            case "$attempt" in
              1) backoff=10 ;;
              2) backoff=30 ;;
              3) backoff=60 ;;
              4) backoff=120 ;;
            esac
            echo "install.sh attempt $attempt failed; retrying in ${backoff}s..." >&2
            sleep "$backoff"
          done
      - name: Run Go Lint Script
        run: bun src/Core.TypeScript/lint/lint-go.ts

  lint-python:
    name: lint (Python)
    timeout-minutes: 15
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Cache install.sh outputs (mise runtimes + dotnet tools + verifier jars)
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.local/bin/mise
            ~/.local/share/mise
            ~/.cache/mise
            ~/.dotnet/tools
            ~/.elan
            ~/.config/zeta
            ~/.rustup/toolchains/1.87.0-*
            ~/.rustup/settings.toml
            ~/.rustup/update-hashes
            ~/.cargo
            ~/.cache/zeta/from-autotools-tarball/*.tgz
          key: install-v2-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('.mise.toml', 'tools/setup/**', 'global.json') }}
      - name: Install toolchain via three-way-parity script (GOVERNANCE §24)
        # tier=slim — this lane runs `bun src/Core.TypeScript/lint/lint-python.ts` and nothing else, so it must not
        # fetch the 237 MB agda->ghc apt payload. Full measurement + reasoning at
        # the `lint (semgrep)` job above; pinned by
        # src/Core.TypeScript/ci/apt-manifest-host-tier.test.ts.
        env:
          ZETA_HOST_TIER: slim
        run: |
          set -euo pipefail
          for attempt in 1 2 3 4 5; do
            if ./tools/setup/install.sh; then exit 0; fi
            [ "$attempt" = "5" ] && { echo "install.sh failed after 5 attempts"; exit 1; }
            case "$attempt" in
              1) backoff=10 ;;
              2) backoff=30 ;;
              3) backoff=60 ;;
              4) backoff=120 ;;
            esac
            echo "install.sh attempt $attempt failed; retrying in ${backoff}s..." >&2
            sleep "$backoff"
          done
      - name: Run Python Lint Script
        run: bun src/Core.TypeScript/lint/lint-python.ts

  lint-rust:
    name: lint (Rust)
    timeout-minutes: 15
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Cache install.sh outputs (mise runtimes + dotnet tools + verifier jars)
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.local/bin/mise
            ~/.local/share/mise
            ~/.cache/mise
            ~/.dotnet/tools
            ~/.elan
            ~/.config/zeta
            ~/.rustup/toolchains/1.87.0-*
            ~/.rustup/settings.toml
            ~/.rustup/update-hashes
            ~/.cargo
            ~/.cache/zeta/from-autotools-tarball/*.tgz
          key: install-v2-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('.mise.toml', 'tools/setup/**', 'global.json') }}
      - name: Install toolchain via three-way-parity script (GOVERNANCE §24)
        # tier=slim — this lane runs `bun src/Core.TypeScript/lint/lint-rust.ts` and nothing else, so it must not
        # fetch the 237 MB agda->ghc apt payload. Full measurement + reasoning at
        # the `lint (semgrep)` job above; pinned by
        # src/Core.TypeScript/ci/apt-manifest-host-tier.test.ts.
        env:
          ZETA_HOST_TIER: slim
        run: |
          set -euo pipefail
          for attempt in 1 2 3 4 5; do
            if ./tools/setup/install.sh; then exit 0; fi
            [ "$attempt" = "5" ] && { echo "install.sh failed after 5 attempts"; exit 1; }
            case "$attempt" in
              1) backoff=10 ;;
              2) backoff=30 ;;
              3) backoff=60 ;;
              4) backoff=120 ;;
            esac
            echo "install.sh attempt $attempt failed; retrying in ${backoff}s..." >&2
            sleep "$backoff"
          done
      - name: Run Rust Lint Script
        run: bun src/Core.TypeScript/lint/lint-rust.ts

  cross-verify:
    # Enforce the trust-core cross-language byte-lock + golden-vector oracles + the Ace
    # package-manager suite. Previously only `lint (TS)` type-checked these, so a
    # regression to a committed golden vector, or a 4-language disagreement, could merge green.
    # This job runs the ASSERTIONS (assert-dont-skip): every tests/cross-verification/<primitive>/
    # oracle via src/Core.TypeScript/ci/cross-verify-all.ts (compare.ts = TS/F#/C#/Rust committed outputs vs the
    # canonical vectors = the non-Byzantine compile-time agreement, baked in; cross-verify.ts = the
    # TS golden-vector oracle) plus `bun test src/Core.TypeScript/ace/` and the Q# source-oracle tests. The
    # ORACLES assert COMMITTED outputs (no dotnet/rust/QDK toolchain needed); the ace suite installs
    # npm deps (z3-solver) + uses the runner node for its solver Z3 differential. This is the
    # ENFORCEMENT half of the canonical-primitive gate (proof + 4-language byte-lock); the proof half
    # is the formal-coverage cadence / lean-proof lane.
    #
    # THIS JOB IS IN THE REQUIRED FLOOR. It is named in `gate-required.needs` below, and
    # `CI Gate` requires `gate (required)`, so a red here does block a merge. The note that
    # stood here until 2026-08-24 said the opposite — "promoting this to a REQUIRED check is
    # a separate operator/devops step ... it does not block auto-merge" — and it was false
    # for long enough to be believed: it was read on 2026-08-24 as evidence that the ace
    # drift gate was ungated, when a sabotage control proved the opposite. Adding a
    # `ProjectReference` to `clis/Zeta.Clis.fsproj` without regenerating
    # `src/Core.TypeScript/ace/build-graph.json` takes `bun test src/Core.TypeScript/ace/`
    # from 703 pass / 0 fail to 701 / 2, rc=1, and the two failures are the DRIFT GATE pair
    # — inside this job, inside the floor. A comment claiming a gate does not gate is the
    # same defect class as a gate that does not run, pointed the other way, so it is
    # corrected rather than left as folklore (081M0TK8DE8087G0R0001HSKHF).
    name: cross-verify (trust-core oracles + ace suite)
    timeout-minutes: 12
    runs-on: ubuntu-24.04

    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      # The committed oracle comparisons, Ace suite, and Q# source oracles
      # intentionally do not execute the F#/Rust/QDK toolchains. Their actual
      # dependency graph is Bun plus the repository lockfile (including
      # z3-solver), so use that declared narrow graph instead of the full apt
      # workstation/cluster installation.
      - name: Setup pinned Bun
        uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2.2.0
        with:
          bun-version: "1.3.13"

      - name: Install npm devDependencies (z3-solver for the ace solver Z3 differential, etc.)
        # The ace suite's solver.z3.test.ts spawns node to require('z3-solver') (assert-dont-skip:
        # it FAILS rather than skips if the dep is absent). --frozen-lockfile fails if the lockfile
        # is stale vs package.json (same discipline as lint-typescript).
        run: bun install --frozen-lockfile

      - name: Ace package-manager suite
        run: bun test src/Core.TypeScript/ace/

      - name: Q# source-owned reference oracles
        run: |
          bun test \
            src/Core.QSharp.ReferenceOracle/qsharp-golden.test.ts \
            src/Core.QSharp.ReferenceOracle/quantum-circuit.test.ts \
            src/Core.QSharp.ReferenceOracle/dbsp-operators.test.ts \
            src/Core.QSharp.ReferenceOracle/heat-signals.test.ts

      - name: Cross-language byte-lock + golden-vector oracles
        run: bun src/Core.TypeScript/ci/cross-verify-all.ts

      # The `.claude/rules/no-binary-in-proof-lineage.md` exception, enforced rather than
      # asserted. `src/wasm-dla/bytelock/` holds six committed `.wasm` substrate modules; they
      # are the artifact UNDER TEST, not golden vectors, and the rule now says so — but a
      # documented exception with no scope is a licence. This derives the allowed set from the
      # byte-lock runner's own roster and the build script's own declared outputs, so a new
      # binary in that directory is red until it is wired into both.
      #
      # Two of its checks are here rather than in `bytelock.yml` on purpose. That workflow is
      # `push: main` — POST-MERGE by construction — so the malformed-artefact guard that would
      # have caught `dla-canonical-zig.wasm` shipping as an `ar` archive could only ever fire
      # after the fact, and it did, for two weeks. The header check and the golden-vector text
      # check need no toolchain, so they belong on the pre-merge floor.
      - name: Proof-lineage binary exception (no-binary-in-proof-lineage.md)
        run: bun src/Core.TypeScript/hygiene/audit-proof-lineage-binaries.ts

      # Stage-0 minimization ratchet (081M0X2553J087G0R001VH0K64). `mise` installs `bun`, so
      # whatever installs `mise` cannot be written in `bun` — stage-0 shell is ESSENTIAL and
      # this check never argues with it. What it prices is the number of DOORS: shell files
      # that something outside the shell graph has to name. Internal helpers cost zero, so the
      # metric creates no pressure to merge scripts that split for a bootstrap-ordering reason.
      #
      # It is HERE and not in `lint (bash retirement inventory ...)` deliberately. That job is
      # not in `gate-required.needs`, so everything it runs blocks nothing — the existing shell
      # allowlist has been advisory the whole time it has been read as a guard. A ratchet that
      # cannot fail a merge is a number, not a ratchet, so it goes on the floor.
      - name: Stage-0 independence ratchet (doors, not file count)
        run: bun run hygiene:stage0-independence

      # THE VACUITY CLASS, INVERTED — a step that can never SUCCEED.
      #
      # `${{ steps.X.outputs.Y }}` with no writer interpolates to the empty string silently.
      # A step guarding on that value fails every run forever, and its error names the
      # PRODUCER, so the fault reads as a defect in that step. Live instance the day this
      # landed: `agent-heartbeat.yml`'s flush returned success on two paths without declaring
      # `skip`, so every backpressured tick — a HEALTHY outcome — reported as a broken flush.
      # Three agents diagnosed it as an auto-merge token-scope fault; that call was never
      # reached. The second of the two paths was found by this check, not by hand.
      - name: Step outputs have writers (a step that cannot succeed)
        run: bun src/Core.TypeScript/hygiene/audit-workflow-step-output-has-writer.ts

      # A REAL SHA THAT NOBODY CHOSE.
      #
      # Every third-party action is pinned to a SHA, which reads as supply-chain discipline
      # until you ask who checked the SHA. A 40-hex string is indistinguishable from a
      # fabricated one by shape, and two sites can pin one action at two SHAs indefinitely
      # while every review of either looks clean. Both happened here on 2026-08-25: an agent
      # nearly shipped an invented SHA for an action this repo does not use, and
      # `ruleset-apply.yml` landed `actions/upload-artifact` at a SHA a YEAR older than the
      # ten other sites. The roster makes the pin a value someone committed, not remembered.
      - name: Third-party actions match the SHA roster (AH007)
        run: bun src/Core.TypeScript/hygiene/audit-action-sha-roster.ts

      # A TASK ID THAT IS WELL-FORMED AND IDENTIFIES NOTHING.
      #
      # The AgencySignature gate validates `Task:` for placeholder-ness and SHAPE, never
      # existence — so an invented-but-well-formed ZetaId passes silently, and it is harder
      # to spot than a placeholder because it looks exactly right. The legacy `B-NNNN`
      # scheme has had this guard since `b-ref-resolve.ts`; the ZetaId scheme that REPLACED
      # it never got one, so the newer mandatory key was the less-checked one.
      #
      # Live instance the day this landed: `Task: 081M0X0JQGY087G0R000EBCPQ3`, written by
      # hand instead of minted, caught only because its author re-read his own trailer.
      - name: Task ZetaIds resolve to work-items (AH006)
        env:
          # The PR body carries the AgencySignature block, and unlike `git log` it is present
          # on a SHALLOW checkout. The first wiring of this step piped `git log -50`, CI checks
          # out depth 1, and the audit passed having examined ZERO ids — the exact vacuity it
          # was written to catch, in its own wiring. It now exits 2 on empty input, so a
          # regression here goes red instead of green.
          PR_BODY: ${{ github.event.pull_request.body }}
        run: |
          printf '%s' "$PR_BODY" | bun src/Core.TypeScript/hygiene/audit-task-zetaid-resolves.ts --stdin

      # THE FALSIFIER FOR CREDENTIAL-ROLE COLLAPSE (AH003, 2026-08-25).
      #
      # `docs/security/2026-08-17-society-heartbeat-token-boundary-and-gate-start-failure.md`
      # names THREE credential roles with three secrets: dispatch, branch-push, PR-create. A
      # `${{ secrets.A || secrets.B || secrets.GITHUB_TOKEN }}` expression erases that table at
      # the point of use -- `||` selects on the left secret being EMPTY, so an absent scoped
      # credential is silently replaced by one with DIFFERENT authority and the step then fails
      # somewhere downstream naming the wrong subject. 25 such chains across 12 workflows had
      # accumulated behind per-step comments that described the ladder as the SAFE half.
      #
      # It is the SILENT selection this refuses, never the degradation itself: a probe + an
      # explicit `::warning` naming the secret and scope inside a `run:` block is good design
      # and is untouched (agent-heartbeat.yml keeps three of them).
      #
      # Distinct from `audit-workflow-write-token-consistency.ts` above, which asks whether a
      # forge-write step reaches A PAT. This asks whether it reaches THE RIGHT ONE, and only
      # that one. A step can satisfy that audit and still be a role collapse -- all 25 sites
      # did, which is why one check does not subsume the other.
      #
      # Offline (reads only committed workflow text, no sockets), so it belongs on the
      # pre-merge floor rather than post-merge.
      - name: Workflow credential role separation (one role, one secret)
        run: bun src/Core.TypeScript/hygiene/audit-workflow-credential-role-separation.ts

      # AH005 — a committed identity that GitHub resolves to somebody who is not us.
      #
      # `<username>@users.noreply.github.com` is the LEGACY plain noreply form and GitHub
      # resolves it DIRECTLY to whoever owns that username today. Two of our personas
      # shipped under it (15 commits and 2 commits), and both local-parts are live accounts
      # belonging to uninvolved private individuals. They are rendered in this repository's
      # Contributors sidebar, which aggregates trailer co-authors — note the REST
      # `/contributors` endpoint does NOT, which is why an earlier check of that endpoint
      # came back clean and under-reported the problem.
      #
      # The audit scans the SOURCES THAT GENERATE identities, never git history. The
      # offending commits are on `main` and correcting them would mean force-pushing the
      # default branch, so they stay. An audit pointed at history would be permanently red
      # over commits nobody may fix, and a check that can never go green gets disabled.
      #
      # HERE on the cross-verify floor because it is offline (reads only committed text, no
      # sockets, no DNS — the fabricated-namespace list is a closed enumeration rather than
      # a resolver for exactly that reason). Measured: ~1.4 s warm over 2,028 generator
      # files. Proven able to fail before it was wired: run against the pre-fix contents of
      # the same seven files it reports 7 findings and exits 1.
      - name: Co-author identity collision (AH005 — plain-username GitHub noreply form)
        run: bun src/Core.TypeScript/hygiene/audit-coauthor-identity-collides.ts

      # THE FALSIFIER FOR THE PR-ARCHIVE COVERAGE COLLAPSE (2026-08-25).
      #
      # `agent-heartbeat.yml`'s auto-merge steps hardcoded `secrets.GITHUB_TOKEN` while every
      # other credentialed step in that file used the PAT fallback chain. A merge armed with
      # GITHUB_TOKEN lands as `github-actions[bot]`; GitHub suppresses workflow triggers for
      # GITHUB_TOKEN-driven events, so `pull_request: closed` never fires and
      # pr-archive-on-merge.yml never runs — with NO failure anywhere, which is why it was
      # misdiagnosed as a credential problem several times. Measured 2026-08-21..25: 747 of
      # 765 (97.6%) eligible unarchived PRs were bot-merged; 530 of 539 (98.3%) archived ones
      # were user-merged.
      #
      # The defect is the INCONSISTENCY, not the instance — one step out of nine disagreeing
      # with its neighbours, which prose cannot hold. Offline (reads only committed workflow
      # text, no sockets), so it belongs on the pre-merge floor rather than post-merge.
      - name: Workflow write-token consistency (forge writes must reach the PAT)
        run: bun src/Core.TypeScript/hygiene/audit-workflow-write-token-consistency.ts

      # The falsifier for the PR-free heartbeat lane. Design:
      # docs/research/2026-08-25-pr-free-heartbeat-lane-attestation-instead-of-gate.md
      #
      # The lane's proposal is that a bypass actor on ruleset `CI Gate` (16134995) pushes
      # telemetry straight to `main`, skipping `gate (required)`, and that the lane verifies
      # ITSELF. A bypass actor is a permanent hole, so that self-verification has to be a
      # checkable fact rather than a claim -- this is the check. For every commit the lane
      # produced it requires a content-bound attestation (`Verification-Subject` is the
      # commit's own tree sha, so a COPIED attestation fails), a path allowlist, and a
      # change-mode check in which `append-only` means the pre-image is a byte PREFIX of the
      # post-image -- NOT `deletions == 0`, which `docs/github/prs/manifest.jsonl` satisfies
      # 13 times in 40 while inserting its rows in sorted order.
      #
      # HERE on the cross-verify floor because it is offline (no sockets, reads only committed
      # git objects) and because a bypass-lane guard must run on exactly the lane a path filter
      # would exclude. Collection is two-phase for CORRECTNESS, not speed: this job checks out
      # at fetch-depth 1, where the tip has no parent, so `git show --name-status` would report
      # the entire 45k-file tree as added and `<sha>^` would not resolve. Metadata alone decides
      # NOT-LANE, so a commit the audit does not judge never enters that path -- and where the
      # parent genuinely is unreachable it exits 2 rather than reading a missing object as a
      # violation. Measured on a full clone: 6.9 s for a 300-commit metadata-only window.
      #
      # HONEST STATE, so nobody quotes a green run as evidence: `cutoverIso` in the registry
      # is 2099-01-01 and the lane identity does not exist yet, so TODAY this returns clean
      # over 300 commits with all 300 classified NOT-LANE -- armed, and vacuous until step 4
      # of the design doc's section 12. It was proven able to fail against real history
      # first: with the cutover moved back and the current flush identity registered it
      # reports 290 violations of 300. The unit tests below are what keep it non-vacuous in
      # the meantime.
      - name: PR-free heartbeat lane attestations (armed; vacuous until the lane exists)
        run: bun src/Core.TypeScript/hygiene/audit-heartbeat-lane-attestations.ts --max 300

      - name: Heartbeat-lane audit unit tests (a check that cannot fail is not a check)
        run: bun test src/Core.TypeScript/hygiene/audit-heartbeat-lane-attestations.test.ts

      # Two commit-back-lane audits, both of which existed as code and NEITHER of which any
      # workflow invoked. `audit-push-without-rebase.ts` (AH001) shipped 2026-08-10 with a
      # named live instance and zero callers — the guard against a lane erasing its own
      # commit had never once executed in CI. An audit nothing runs is the vacuity class in
      # its purest form: it reads as protection and constrains nothing, which is exactly the
      # property both of these audits exist to refuse in other people's code.
      #
      # AH002 is the newer half. The `[skip ci]` refusal already lived inside
      # `flush-via-staging.ts`, where it protects every lane that ALREADY adopted the safe
      # route and is invisible to every lane that did not — so `lockfile-healer` pushed a
      # skip-token commit at `main` for eleven days while reporting 40/40 green, because its
      # `Commit and push` step is gated on drift being detected and no drift occurred. The
      # load-bearing path had never run under ruleset "CI Gate".
      - name: Commit-back lane can re-express its work (AH001)
        run: bun src/Core.TypeScript/hygiene/audit-push-without-rebase.ts

      - name: Commit-back lane can actually land (AH002)
        run: bun src/Core.TypeScript/hygiene/audit-skip-token-cannot-land.ts

      # AH003 — the archive lane's record actually reached `main`.
      #
      # The third instance of the same class as the two above, and the most expensive to date.
      # `pr-archive-on-merge.yml` was changed SEVENTEEN times; its recent runs read 12 success
      # / 8 skipped / 0 failures — green, while 1,290 `automation/pr-archive-*` refs sat on the
      # remote. Every check it had asked a PROXY question ("did the tool run", "did I create a
      # PR"), and its sibling `audit-archive-pr-lane.ts` takes `gh pr list` as its input, so it
      # is structurally blind to a ref that never had a PR at all — which is what these were.
      #
      # This one takes REFS as input and asks the only question that is the point: is the
      # record ON MAIN? It runs HERE, on the pre-merge floor and fatal, rather than as the
      # non-fatal warning its sibling gets inside the heartbeat tick. A lane that leaked
      # because one failure was tolerated does not get its falsifier installed as another
      # tolerated failure.
      #
      # It gates on a high-water mark that may only be lowered, so the pre-existing backlog
      # cannot hold the floor red while any NEW strand fails immediately. Deleting a stranded
      # ref does not make it pass — the record is still absent from main.
      - name: Archive lane record reached main (AH003)
        run: bun src/Core.TypeScript/hygiene/audit-orphaned-archive-refs.ts

      # The .NET SDK version is declared in TWO files and, until 2026-08-23, nothing checked
      # that they agreed: `global.json` carried `rollForward: latestPatch` (which reads like
      # automatic security uptake and is not — it only selects among ALREADY-INSTALLED SDKs)
      # while `.mise.toml`, the file that actually acquires the SDK, pinned it exactly. A
      # reader of one believed a property only the other could grant. `.mise.toml` is now the
      # single declared source and `global.json` a checked restatement, so the two can no
      # longer disagree silently. Text-only and offline — it runs where no SDK is installed,
      # unlike its runtime sibling `audit-codeanalysis-sdk-match.ts`, which reads the SDK's
      # own `csc -version` in the build job.
      - name: .NET SDK pin declared once (.mise.toml canonical, global.json restates)
        run: bun src/Core.TypeScript/hygiene/audit-dotnet-pin-parity.ts

      # The `.mise.toml` pins that OTHER files silently depend on. Two couplings, both of
      # which fail by being quiet: a stale `~/.rustup/toolchains/<v>-*` cache glob degrades
      # the offline path to a CDN fetch, a stale `.mise.full.toml` gives full-tier hosts a
      # different compiler from slim ones, and a zig bump leaves the COMMITTED byte-lock
      # `.wasm` unchanged — so the byte-lock still passes while the artifact stops being
      # reproducible from the pinned toolchain. Neither is reachable by a Dependabot
      # `ignore:`: `.mise.toml` is a manifest of no supported ecosystem, so an `ignore:`
      # naming them would suppress nothing while reading as protection. The site list is
      # discovered from the operative surfaces, so a new workflow with a stale glob is
      # caught the day it lands.
      - name: mise toolchain couplings (rust restatements · zig byte-lock provenance)
        run: bun src/Core.TypeScript/hygiene/audit-mise-toolchain-couplings.ts

      # Every zflash host arm must reach the ISO integrity gate before it writes to a block
      # device. Measured on main 2026-08-21: the manifest check existed in the macOS arm and
      # NOWHERE ELSE, so flash-usb-linux.ts and flash-usb-windows.ts wrote an image with no
      # integrity verification at all (081M0HG7X7B087G0R002A05DAP). A missing check is
      # invisible to a test suite — the arms that have one stay green, and the arms that lack
      # one have nothing to test — so only something that reads the call sites can see it.
      #
      # HERE rather than in build-ai-cluster-iso.yml on purpose, and this is the placement
      # that workflow's own note asked for. The ISO lane runs on a subset of pushes, and when
      # this step lived there and referenced a script that did not exist it silently killed
      # ISO production for every run (#13102). On the cross-verify floor it blocks every PR,
      # beside the other audits whose subject is "a check that did not run must never look
      # like a check that passed".
      - name: zflash host-arm parity (every arm verifies the ISO before writing)
        run: bun src/Core.TypeScript/hygiene/audit-flash-entrypoint-parity.ts

      # Every ArgoCD `targetRevision` must name a chart version upstream actually published.
      # Measured on main 2026-08-20: applications/oz pins ziti-controller at 1.4.5, a version
      # OpenZiti never shipped (their 1.x line ends at 1.3.4), so that Application can never
      # sync — invisible because `oz` is one of 26 of 45 Applications no CI lane exercises, so
      # nothing ever asked the registry for the chart (081M0JVD5YG087G0R002QDFR9H, #13313).
      #
      # OFFLINE. It resolves every coordinate against the checked-in snapshot
      # `published-chart-versions.json` and opens no socket, so this step cannot redden a PR
      # because a third-party registry is down — which is the only reason a resolvability
      # check survives past its first outage. The network half is `--refresh`, and it runs on
      # a schedule in chart-version-refresh.yml where it is allowed to fail loudly.
      - name: ArgoCD chart targetRevisions resolve (offline, against the committed snapshot)
        run: bun src/Core.TypeScript/hygiene/audit-chart-target-revisions.ts

      # NO MANIFEST IN THE PUBLIC TREE MAY REFERENCE AN IMAGE NOBODY OUTSIDE CAN PULL.
      # `applications/platform/blueprints-flowdent.yaml` shipped here for months naming
      # ghcr.io/flowdent/cloudservice and ghcr.io/flowdent/fd-webclient -- images out of
      # Flowdent/fd-core (private) and Flowdent/fd-webclient (internal), in an org with
      # public_repos: 0. An agent wrote to the wrong repository in a multi-repo setup and
      # nothing caught it; it took months and a partitioner that could not MEASURE the images
      # to surface it, and a human to remove it (#14250). This is that accident as a red check.
      #
      # Aaron 2026-08-23: "zeta should not have any private repo dependencies." The invariant is
      # ONE-WAY: Flowdent (closed) may depend on Zeta (open); Zeta may never depend on Flowdent.
      #
      # HERE, on the floor that runs on EVERY PR, rather than in k8s-lane-partition.yml: that
      # workflow's path filter watches full-ai-cluster/k8s only, and this check's scope is every
      # tracked YAML outside docs/references/.github -- infra/k8s, agentic-organization/deploy,
      # samples/, and whatever directory the next multi-repo accident lands in. A path filter is
      # exactly the wrong shape for a check whose whole job is to notice a manifest somewhere
      # nobody declared.
      #
      # OFFLINE. It resolves every reference against the committed measurement
      # `full-ai-cluster/k8s/image-source-provenance.json` and opens no socket; the network half
      # is `--refresh` and it runs weekly in chart-version-refresh.yml. MEASURED 2026-08-23:
      # 0.43s, 49 files parsed of 209 scanned.
      - name: No private-source image dependencies (offline, against the committed provenance)
        run: bun src/Core.TypeScript/cluster/image-source-provenance.ts

      # The check above is only worth its slot if it can go red. The suite reconstructs the two
      # FlowDent Blueprints in a throwaway git tree and asserts they are refused with the REMOVE
      # remedy, asserts bitnami/weaviate/ich777/postgres/kubevirt are NOT tripped, and asserts an
      # unresolvable package with no source label is refused rather than admitted. 35 cases;
      # 16 targeted mutants applied and byte-`cmp` verified before reading each result -- 15
      # landed and were all killed, and the 16th reported PATTERN NOT FOUND rather than a
      # result, which is the discipline working.
      - name: Private-source image dependencies — falsifiers (proves it goes red)
        run: bun test src/Core.TypeScript/cluster/image-source-provenance.test.ts

      # ARE THE DEFERRAL REASONS STILL TRUE? Every reasoned-exclusion registry in the cluster
      # tree was audited for a reason being PRESENT and naming a lift condition, and by nothing
      # for the reason being TRUE. Measured twice on 2026-08-21: `temporal`'s reason cited a
      # baseline acknowledgement that had been deleted forty minutes earlier (#13472 -> #13483),
      # and `oz`'s cited a chart pin the tree had already corrected (#13313 -> #13471). Both
      # audits stayed green through both, because a false sentence with a `LIFTS WHEN:` clause
      # has every mechanical property those checks look for.
      #
      # HERE rather than in k8s-argocd-health-test.yml, and the placement is the point: this
      # checker resolves citations into `.github/workflows/` (a CI job a reason names) and into
      # the chart roster, so a PR that renames a job would never trigger a path-filtered cluster
      # workflow and would silently strand the reason that cites it. The cross-verify floor runs
      # on every PR. OFFLINE, ~1s, no sockets -- it reads the committed baseline, the committed
      # render snapshot and the committed roster.
      - name: ArgoCD deferral reasons — every cited anchor still holds (offline)
        run: bun src/Core.TypeScript/cluster/reason-truth.ts

      # A tracked source file holding a raw 0x00 byte reads as BINARY to grep/rg, so every
      # text audit silently skips it -- a check that did not run, looking exactly like one
      # that passed. `rg 'foldChain' key-epoch-ledger.ts` printed "binary file matches" and
      # exit 0 while ten real matches were invisible, in the revocation ledger of all files.
      # The \u0000 escape is byte-identical at runtime and keeps the file searchable, so the
      # rule is total and needs no allowlist. Ten tracked files failed this when written.
      - name: No raw NUL in tracked source (an audit must be able to read the file)
        run: bun src/Core.TypeScript/hygiene/audit-no-raw-nul-in-source.ts

      # Addison Cooper's Genesis concept vocabulary is published as a static page that is
      # hand-copied into a DIFFERENT repository (lucent-financial-group.github.io) — no build
      # step, no CI, nothing connecting it back here. It drifted exactly as you would expect:
      # four society-identity terms landed in docs/GLOSSARY.md on 2026-07-31 and never reached
      # the page, and nobody found out until Aaron asked.
      #
      # docs/CONCEPT-REGISTRY.md is now the single list. This step is what keeps it honest —
      # it fails when the registry and the page disagree in EITHER direction, including a
      # silently reworded definition, which is the case a term-name-only check would miss.
      - name: Concept registry vs published page (docs/CONCEPT-REGISTRY.md)
        run: bun src/Core.TypeScript/hygiene/audit-concept-registry-drift.ts

      # docs/TECH-RADAR.md is 100+ rows of PROSE asserting facts about this repo, and prose
      # rots silently -- which is the whole reason it drifted. Soraya found two on 2026-08-22
      # and both were the same defect: a row asserted something, the repo moved, nothing could
      # notice. `fast-check` was pinned and imported by five test files with NO ROW AT ALL, so
      # the only property-testing row ("FsCheck 3 | Adopt") -- which is .NET -- silently stood
      # in for a TypeScript tool at a different maturity. And two rows cited paths that had
      # moved (tools/alloy/alloy.jar, tools/invariant-substrates/tally.ts).
      #
      # Two mechanically-decidable subclasses, both derived rather than hand-listed:
      #   A. every backticked repo path the radar cites must exist (`(planned)` opts a
      #      proposed artifact out);
      #   B. every root package.json devDependency statically imported by a tracked
      #      *.test.ts must be named in the radar. devDependencies is the MANIFEST's own
      #      declaration of "tooling not product"; "imported by a test" is the proxy for
      #      "part of how we verify". Runtime `dependencies` are deliberately out of scope --
      #      the radar is an evaluation register, not an SBOM, and a check that demanded a row
      #      for `pg` is one somebody disables.
      #
      # HERE on the cross-verify floor because it is offline (~1s, no sockets, reads only
      # committed files) and because the radar is edited by every lane -- a path-filtered
      # workflow would miss the PR that moves the file a row cites. Went red on main when
      # written -- 7 findings: 4 dead paths (specs/Spine.als, tools/alloy/alloy.jar,
      # docs/research/scratch-zeta-parity.md, tools/invariant-substrates/tally.ts) and 3
      # unringed devDependencies (fast-check, quantum-circuit, semver). Green after the
      # corrections, so no baseline file exists (AUDIT-LIFECYCLE.md's cleanup-to-zero path
      # -- the surface is a mutable markdown doc, so the residue was editable).
      #
      # STATED LIMIT: neither check can tell you a RING is wrong. TLA+ sat at Adopt with a
      # dark lane for seven weeks and this audit would have stayed green throughout. Lane
      # liveness is a different measurement.
      - name: Tech radar claims the repo can still support (paths resolve, in-use tools ringed)
        run: bun src/Core.TypeScript/hygiene/audit-tech-radar-claims.ts

      - name: Tech-radar audit unit tests (a check that cannot fail is not a check)
        run: bun test src/Core.TypeScript/hygiene/audit-tech-radar-claims.test.ts

      # A filesystem EXISTENCE test whose result gates a later operation on the same
      # path. Check-then-use; TOCTOU; CWE-367. THREE instances landed on 2026-08-20, in
      # one day, from three INDEPENDENT agents, all in code that had already been
      # written and reviewed: cluster/restricted-namespace-workloads.test.ts
      # (readdir + statSync().isDirectory() + readFileSync),
      # cluster/storage-profiles.ts (existsSync -> readFileSync), and
      # hygiene/audit-chart-target-revisions.ts (three sites in one file). Each was
      # found by CodeQL one at a time, AFTER merge, because nothing in the tree looked
      # for the shape. The pattern READS as defensive and prevents nothing.
      #
      # HERE rather than in codeql.yml on purpose. CodeQL runs on a schedule and
      # reports into the security tab, so its verdict arrives after the merge that
      # caused it -- which is how all three shipped. This step is offline, needs no
      # database build, and blocks the PR.
      #
      # --baseline grandfathers the 288 pre-existing instances measured on main when
      # this landed (AUDIT-LIFECYCLE.md step 5: the gate has to be landable without a
      # 288-file cleanup first). NEW instances fail, and so does an edit that renames
      # the path expression at a grandfathered site. --min-files is the scan floor: an
      # audit that inspected nothing must not report success.
      - name: No check-then-use filesystem races (TOCTOU, CWE-367)
        run: |
          bun src/Core.TypeScript/hygiene/lint-check-then-use-file-races.ts \
            src/Core.TypeScript \
            --min-files 1500 \
            --baseline src/Core.TypeScript/hygiene/lint-check-then-use-file-races.baseline.json

      # Executes mumps_zeta_id.m (subset interpreter). compare.ts already pins
      # the committed JSON; this step runs the routine so a layout edit in the
      # .m file cannot hide behind a stale mumps-output.json.
      - name: Execute MUMPS zeta-id packer
        working-directory: tests/cross-verification/zeta-id
        run: bun run-mumps.ts

      # The six GENERATED bit-layout files vs docs/zeta-id-v1-layout.yaml, which the
      # zeta-id README named as an open hole: "There is no CI gate verifying the .gen
      # files match the YAML, so an edit without regeneration fails nowhere until a
      # codec stops compiling."
      #
      # The oracles above do not close it. compare.ts pins each oracle's COMMITTED
      # output to vectors.yaml, and CI independently re-executes five of the seven
      # (TS `bun test`, F#/C# `dotnet test`, Python `pytest`, MUMPS the step above).
      # Go and Rust are re-executed NOWHERE — this workflow runs only
      # `go test ./algebra/` and `cargo test … Core.Rust.Observe`. So a wrong constant
      # in zeta_id.gen.go or bit_layout.gen.rs sat behind a stale-but-agreeing output
      # JSON with every job green. This step is the independent reproduction for that
      # pair, and for the other four at no extra cost.
      - name: zeta-id generated layouts vs the layout YAML
        run: bun tests/cross-verification/zeta-id/gen-layout-drift.ts

      # Algebra-tower drift-check: the generated law-property tests must be
      # regenerable-identical from their IRs (codegen-law-drift), and the
      # generated laws must hold against the TS oracle instances. Covers the
      # semiring → ring → kleene tower (+ the orthogonal star-ring branch).
      # Pure bun/arithmetic — no go/python spawns (the cross-language law
      # cross-verify is a separate, local/toolchain-heavy lane).
      - name: Algebra-tower drift-check (semiring→ring→kleene + star-ring)
        run: |
          bun test \
            tests/cross-verification/_harness/codegen-law-drift.test.ts \
            tests/cross-verification/_harness/generated-semiring-laws.test.ts \
            tests/cross-verification/_harness/generated-ring-laws.test.ts \
            tests/cross-verification/_harness/generated-star-ring-laws.test.ts \
            tests/cross-verification/_harness/generated-kleene-laws.test.ts

  test-typescript-hermetic:
    # ══ TIER 1 OF 2: HERMETIC ═══════════════════════════════════════════════════════════
    #
    # A RED X HERE MEANS THE CHECKED-OUT SOURCE OR ITS DECLARED TOOLCHAIN BASELINE IS BROKEN.
    # That is the property the tier split was built to establish. Nothing below depends on a
    # venv, a cluster, a container, a physical device, an attached ISO, or the invoking user's
    # home directory. CI declares ubuntu-24.04, pinned Bun, the frozen lockfile, and pinned SMT
    # binaries below; developers realize the broader supported baseline through install.sh.
    # "Hermetic" here is a failure-attribution boundary, not a bit-identical runner image --
    # hosted-image drift remains possible and must be named if it is ever the cause.
    #
    # WHY THE SPLIT. MEASURED 2026-08-16 across the 53 most recent completed `gate` runs:
    # the old undivided `test (TS suite)` was red in 17 of them (32%), and in 15 of those the
    # ENTIRE cause was one deterministic test -- `folderSink -- entropy tracker wiring`, a
    # genuine regression that rode along across sixteen merges because this job sits outside
    # `gate (required)` and a red X nobody must clear is a red X nobody reads.
    #
    # The reason nobody read it was not in CI. MEASURED the same day on a FRESH CLONE of main
    # (macOS, `bun install` only), `bun test` gave 4 failures + 1 unhandled error and ZERO of
    # them were code defects: 4x cross-verify-ir (no Q# venv; no .NET SDK 10.0.302) and 1x
    # esp-inject (EPERM scandir ~/Downloads, thrown at module scope under macOS TCC). So the
    # SAME command was red-for-environment where people work and red-for-regression where it
    # mattered, with nothing in either output separating the two. The prior "this suite is
    # always red" is learned on the laptop and spent on the pull request. Splitting the tiers
    # is how that prior is retired: after this change, red here is red everywhere, for the
    # same reason.
    #
    # WHAT MOVED, AND WHERE IT WENT. Exactly the two files above, to
    # `test (TS environment-dependent)` below -- which still runs on every PR. Each is
    # declared with the NAME of the dependency it needs in
    # registry/environment-dependent-test-files.json, and
    # src/Core.TypeScript/hygiene/environment-dependent-test-files.ts is red if an exclusion
    # is undeclared, unattributed, or reached by no lane at all. Excluding a file from this
    # tier while nothing else runs it would be a deletion wearing a split's face; that is the
    # condition the checker exists to make unlandable.
    #
    # PROMOTED 2026-08-25. This job is in the `gate-required` needs list under explicit
    # maintainer consent. The promotion is a floor amendment, not just a dependency edit;
    # registry/uncompensatable-floor.yaml and the drift-and-heal ADR record its rationale
    # and its narrow whole-tree exception to the usual diff-scoped rule.
    #
    # THE GAP THIS CLOSES. Before this job, CI executed roughly 130 of the repo's 900-plus
    # `*.test.ts` files. Whole directories -- discovery/, planning/, browser-node/,
    # ferry-throttler/, model-backend/, oracle/, forge-host/ -- were never executed by any
    # workflow, so a PR touching them went green having run zero of their tests. Three live
    # instances on 2026-08-13 alone: a 32-test chaos harness whose four deliberately-pinned
    # known-broken behaviours were inert; the independent check that falsified a proposed
    # light-time remedy, never re-run; and a freshly-merged 21-test migration alongside a
    # drift gate referenced by no workflow, while main sat in drift and nothing reported it.
    # zflash-harness-lint.yml states the rule: a green check that implies more than it tested
    # is worse than no check. Work-item 081KZYPHESJ087G0R002EZ7A2H.
    #
    # WHY A BARE `bun test` AND NOT A LIST OF DIRECTORIES. Every filter list is a fresh copy
    # of the same defect: forge-host/ fell outside the previous globs precisely because
    # someone had to remember to add it. A bare invocation cannot have that bug -- a new
    # directory is covered the moment it exists. Deliberate exclusions therefore live in
    # exactly one place, the pathIgnorePatterns of a bunfig, and each one must carry a
    # written reason in a registry.
    # hygiene/unexecuted-test-files.ts enforces both halves and fails on an undeclared,
    # unreasoned, or stale entry.
    #
    # `--config=bunfig.hermetic.toml` DOES NOT BREAK THAT RULE, for the same reason: it is still a
    # bare, filter-less invocation, and the tier's exclusions are still a written list in a
    # bunfig under a registry-backed checker. bun has no exclude-filter form, so a second
    # config is the only way to say "everything except these" without reintroducing the
    # include-list defect. bunfig.hermetic.toml is asserted to be a strict superset of
    # bunfig.toml on every PR, so the two cannot drift apart in silence.
    #
    # WHY A SEPARATE JOB rather than a step inside `lint (TS)`. That job type-checks; folding
    # behaviour into it puts two different failures behind one red X, which is the attribution
    # problem the 2026-06-13 per-language lint split already solved once here. It also carries
    # a 12-minute cap sized for tsc, while this suite measured 256-346s locally on its own.
    #
    # WHY NOT SPLIT ACROSS PARALLEL JOBS. Per-job fixed cost here is the install.sh bootstrap
    # plus `bun install` -- 1-2 minutes even on a cache hit -- against roughly 7 minutes of
    # actual test time. N jobs multiply the fixed cost for a wall-clock saving that does not
    # pay. Revisit if the suite passes about 10 minutes.
    #
    # COST. About 9 minutes per run (setup plus tests) on ubuntu-24.04 at the 1x multiplier;
    # at roughly 250 gate runs per month that is on the order of 2,250 runner-minutes added.
    # It buys execution of ~770 files and ~9,000 assertions that today execute nowhere.
    #
    # RUNS AND BLOCKS. The probation period produced the evidence the original split asked
    # for. PR #15352 merged with this job FAILURE beside a successful required rollup: a cold
    # Go compiler invocation exceeded a shared 15-second generated-code timeout, while the
    # durable outputs were never written. PR #15358 repaired that real repository defect,
    # but merged five minutes BEFORE this job reported success because the rollup did not
    # depend on it. PR #15395 then supplied a second post-repair green run before merge.
    # The environment-dependent tier remains non-blocking; only this dependency-closed,
    # reproducible behavior suite enters the floor.
    name: test (TS hermetic)
    timeout-minutes: 20
    runs-on: ubuntu-24.04

    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      # The hermetic tier is deliberately JavaScript-first. Its explicit SMT
      # binary realizer remains below; the full apt/mise workstation graph is
      # not an input to this suite and repeatedly consumed the job budget before
      # any hermetic assertion ran.
      - name: Setup pinned Bun
        uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2.2.0
        with:
          bun-version: "1.3.13"

      - name: Install JavaScript dependencies from the locked graph
        run: bun install --frozen-lockfile

      # NO `uv sync` STEP HERE, ON PURPOSE. It provisioned the gitignored Q# venv at
      # src/Core.Python/.venv for cross-verify-ir, which is the single largest reason a
      # laptop run of this suite came back red: 4 of the 5 failures on a fresh clone. That
      # file now runs in `test (TS environment-dependent)`, which does the uv sync, and in
      # full-verify, which is in the required floor. A tier whose green depends on a venv
      # the reader has never created cannot be the tier whose red means a regression.

      - name: Verify npm devDependencies are installed
        # Several suites import devDependencies directly (z3-solver among them) and
        # assert-dont-skip when the dep is absent, so a missing install reads as a real
        # failure rather than a skip. The preceding frozen-lockfile install is the
        # sole dependency realization for this narrow job.
        run: test -d node_modules

      - name: Pin z3 4.16.0 and cvc5 1.3.4 (081KZZ27KJ8)
        # apt on ubuntu-24.04 is z3 4.8.12 / cvc5 1.1.2; two SMT certificates
        # soft-skip under that pair. Checksummed GitHub-release binaries go in
        # ~/.local/bin. No-op when PATH already meets the floor.
        run: bun src/Core.TypeScript/ace/setup-realizers/install-pinned-smt.ts

      - name: Verify light-time endpoint-speed envelope certificates
        # The Lean module is reached by the F# build. This companion TypeScript test
        # executes the Z3 outcome sequence and the independent ephemeris fault control;
        # neither may be a documentation-only proof artifact.
        run: bun test src/Core.TypeScript/planning/light-time-endpoint-speed-envelope.test.ts src/Core.TypeScript/planning/orbital-independent-check.test.ts

      - name: Tier manifest (what the hermetic tier deliberately does not run, and why)
        # Printed BEFORE the suite, pass or fail, so the log states the tier's own boundary
        # instead of leaving a reader to infer it from an absence. It also exits non-zero on
        # an undeclared, unattributed, or run-nowhere exclusion, which is the guard that
        # keeps the split from becoming a quiet mute list.
        run: bun src/Core.TypeScript/hygiene/environment-dependent-test-files.ts

      - name: Whole TypeScript suite (hermetic tier)
        # No path filters: see the header. Exclusions live in bunfig.hermetic.toml, and each
        # one is declared with a reason in registry/unexecuted-test-files.json (runs nowhere)
        # or registry/environment-dependent-test-files.json (runs in the tier below).
        run: bun --config=bunfig.hermetic.toml test

  test-typescript-environment:
    # ══ TIER 2 OF 2: ENVIRONMENT-DEPENDENT ══════════════════════════════════════════════
    #
    # A RED X HERE MEANS EITHER a real regression OR that this runner could not provide a
    # dependency the test needs. That ambiguity is exactly why these files are not in the
    # tier above -- it is not removed, it is CONCENTRATED, into a job whose name says so and
    # whose manifest names every dependency involved. Two red X's that mean different things
    # are readable; one red X that means either is not, and that was the state this replaced.
    #
    # THIS IS NOT A DUMPING GROUND, AND THE CHECKER MAKES THAT STRUCTURAL. Every file listed
    # below must appear in registry/environment-dependent-test-files.json with a non-empty
    # `dependency` naming the concrete missing thing (an SDK version, a venv, a device) and a
    # `reason` for why it cannot be assumed present. hygiene/environment-dependent-test-files
    # .ts is red if a hermetic-tier exclusion is undeclared, unattributed, listed in BOTH
    # registries, or -- the load-bearing one -- executed by no pull_request-lane invocation.
    # Moving a flaky test here to make it quiet does not work: it still runs, still here,
    # still red, and now with your name on the reason.
    #
    # WHY NOT `continue-on-error`. It renders a job green while it failed. An absent check
    # and a passing check must not look alike; that is the same defect as the one this whole
    # split exists to fix, so this job reports honestly and is simply not in the floor.
    #
    # THE FILE LIST IS EXPLICIT, unlike the tier above. Here that is correct rather than the
    # include-list defect the header of that job warns about: this is a hand-curated
    # exception set by construction, and the derivation in hygiene/environment-dependent-test
    # -files.ts reads THIS list out of the workflow to prove each registry entry actually
    # runs. A file silently dropped from this list becomes an "excluded from hermetic and run
    # by nothing" finding on the next PR.
    #
    # COST. Two files, seconds of test time; the minutes are the shared install.sh bootstrap
    # plus the uv sync that the hermetic tier no longer pays for.
    name: test (TS environment-dependent)
    timeout-minutes: 20
    runs-on: ubuntu-24.04

    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Cache install.sh outputs (mise runtimes + dotnet tools + verifier jars)
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.local/bin/mise
            ~/.local/share/mise
            ~/.cache/mise
            ~/.dotnet/tools
            ~/.elan
            ~/.config/zeta
            ~/.rustup/toolchains/1.87.0-*
            ~/.rustup/settings.toml
            ~/.rustup/update-hashes
            ~/.cargo
            ~/.cache/zeta/from-autotools-tarball/*.tgz
          key: install-v2-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('.mise.toml', 'tools/setup/**', 'global.json') }}

      - name: Install toolchain via three-way-parity script (GOVERNANCE §24)
        # Same retry rationale as every other install.sh step in this file
        # (Aaron 2026-04-28 PR #23 mise+bun-1.3.13 502).
        env:
          # TIER DECLARED, not detected. This job genuinely needs the wider payload:
          # environment-dependent tests are gated on the wide toolchain by design; the payload has not been measured down.
          # Recorded explicitly so the next reader sees a decision rather than a default;
          # audit-install-tier-declared.ts refuses silence, never a particular tier.
          ZETA_HOST_TIER: full
        run: |
          set -euo pipefail
          for attempt in 1 2 3 4 5; do
            if ./tools/setup/install.sh; then exit 0; fi
            [ "$attempt" = "5" ] && { echo "install.sh failed after 5 attempts"; exit 1; }
            case "$attempt" in
              1) backoff=10 ;;
              2) backoff=30 ;;
              3) backoff=60 ;;
              4) backoff=120 ;;
            esac
            echo "install.sh attempt $attempt failed; retrying in ${backoff}s..." >&2
            sleep "$backoff"
          done

      - name: QDK venv setup (uv sync)
        # The dependency that makes cross-verify-ir environment-dependent, provided here and
        # nowhere else in the TS lane. Same step full-verify runs, for the same reason: the
        # Q# lane executes through src/Core.Python/.venv/bin/python3 — a gitignored venv, not
        # a PATH tool. Without it that lane cannot run, and until the per-route floor landed
        # it went dark SILENTLY: six of seven lanes satisfied an aggregate `>= 6` floor on the
        # seventh's behalf, so the job reported a 7-language byte-lock it never checked. The
        # floor now names a dark lane and fails. uv comes from install.sh above.
        run: uv sync --project src/Core.Python

      - name: Install npm devDependencies
        run: bun install --frozen-lockfile

      - name: Tier manifest (which dependency each file below needs)
        run: bun src/Core.TypeScript/hygiene/environment-dependent-test-files.ts

      - name: Environment-dependent tests
        # Every path here is an entry in registry/environment-dependent-test-files.json, and
        # the checker above fails if a registry entry is not reached by an invocation in a
        # pull_request lane — i.e. if someone excludes a file from the hermetic tier and
        # forgets to add it here.
        run: |
          bun test \
            tests/cross-verification/_harness/cross-verify-ir.test.ts \
            src/Core.TypeScript/zflash/esp-inject.test.ts

  # ═══════════════════════════════════════════════════════════════════
  # Full Verification Gate — 7-language oracle + cost + proofs
  # ═══════════════════════════════════════════════════════════════════
  #
  # Runs the LIVE cross-language execution oracle (generates code in 7
  # languages, compiles each, executes, byte-compares outputs) plus the
  # algebraic law gate, cost analysis, and Rust/Go compilation.
  # Requires the FULL toolchain (all 7 runtimes + E-prover).
  # Fails if ANY tool is missing — no partial verification.
  full-verify:
    name: full-verify (7-lang oracle + cost + proofs)
    timeout-minutes: 30
    runs-on: ubuntu-24.04
    needs: [path-filter]
    if: needs.path-filter.outputs.code == 'true'

    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Cache install.sh outputs (mise runtimes + cargo + dotnet)
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.local/bin/mise
            ~/.local/share/mise
            ~/.cache/mise
            ~/.dotnet/tools
            ~/.elan
            ~/.config/zeta
            ~/.rustup/toolchains/1.87.0-*
            ~/.rustup/settings.toml
            ~/.rustup/update-hashes
            ~/.cargo
            ~/.cache/zeta/from-autotools-tarball/*.tgz
          key: full-verify-v2-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('.mise.toml', 'tools/setup/**', 'global.json') }}

      - name: Install toolchain (all 7 languages + E-prover)
        env:
          # TIER DECLARED, not detected. This job genuinely needs the wider payload:
          # the 7-language oracle: emscripten and llvm are tier=standard, and .mise.full.toml carries the k8s/WASM set that only tier=full merges.
          # Recorded explicitly so the next reader sees a decision rather than a default;
          # audit-install-tier-declared.ts refuses silence, never a particular tier.
          ZETA_HOST_TIER: full
        run: |
          set -euo pipefail
          for attempt in 1 2 3 4 5; do
            if ./tools/setup/install.sh; then exit 0; fi
            [ "$attempt" = "5" ] && { echo "install.sh failed after 5 attempts"; exit 1; }
            backoff=$((attempt * 30))
            echo "install.sh attempt $attempt failed; retrying in ${backoff}s..." >&2
            sleep "$backoff"
          done

      - name: QDK venv setup (uv sync)
        run: uv sync --project src/Core.Python

      - name: Python tests (Core.Python — incl. T-1/12 + Brownian-experts lemma gate)
        run: uv run --project src/Core.Python pytest src/Core.Python/tests -q
        timeout-minutes: 5

      - name: Smoke check (all 7 toolchains functional)
        run: CI=true ./tools/setup/common/smoke-7-toolchains.sh

      - name: Install npm devDependencies
        run: bun install --frozen-lockfile

      - name: Rust tests (Core.Rust.Observe)
        run: cargo test --manifest-path src/Core.Rust.Observe/Cargo.toml

      - name: Go tests (Core.Go/algebra)
        run: go test ./algebra/
        working-directory: src/Core.Go

      - name: Cross-verify IR (7-language byte-lock oracle)
        run: bun test tests/cross-verification/_harness/cross-verify-ir.test.ts
        timeout-minutes: 5

      # The COMPLEMENT of the step above, not a duplicate of it. That one proves the
      # generated lanes agree WITH EACH OTHER, which a wrong IR satisfies just as well
      # as a right one — N lanes generated from one spec agreeing is one observation
      # counted N times. This one diffs the generated code against the HAND-WRITTEN
      # ports, which are the independent oracle, and so is the only thing here that can
      # falsify IR *adequacy*. It also fails by name on any IR-carrying primitive that
      # has no hand-written port at all.
      - name: IR-generated vs hand-written (independent-oracle check)
        run: bun test tests/cross-verification/_harness/ir-vs-handwritten.test.ts
        timeout-minutes: 5

      - name: Interface law oracle (per-directory)
        run: bun test tests/cross-verification/zeta-ir-v2/interfaces/cross-verify-interfaces.test.ts
        timeout-minutes: 5

      - name: Law proof gate (proof references exist + tests pass)
        run: bun test tests/cross-verification/_harness/law-proof-gate.test.ts
        timeout-minutes: 3

      - name: Cost tests (counter + envelope + growth)
        run: |
          bun test src/Core.TypeScript/algebra/cost-counter.test.ts
          bun test src/Core.TypeScript/algebra/cost-envelope.test.ts
          bun test src/Core.TypeScript/algebra/cost-growth-property.test.ts

      - name: Clifford cross-verify (Cl(3,0) geometric algebra)
        run: bun test tests/cross-verification/_harness/codegen-clifford.test.ts

      - name: Notify on failure
        if: failure()
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
        run: |
          if [ -n "${SLACK_WEBHOOK_URL:-}" ]; then
            PRIORITY="normal"
            if [ "${{ github.event_name }}" = "schedule" ]; then
              PRIORITY="elevated-unattended"
            fi
            curl -s -X POST "$SLACK_WEBHOOK_URL" \
              -H 'Content-Type: application/json' \
              -d "{
                \"text\": \"🚨 full-verify FAILED ($PRIORITY)\",
                \"blocks\": [{
                  \"type\": \"section\",
                  \"text\": {
                    \"type\": \"mrkdwn\",
                    \"text\": \"*Job:* ${{ github.job }}\\n*Run:* ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}\\n*Trigger:* ${{ github.event_name }}\\n*Priority:* $PRIORITY\"
                  }
                }]
              }"
          fi

  # ═══════════════════════════════════════════════════════════════════
  # Merge Gate Roll-up — the ONE required status check for branch protection
  # ═══════════════════════════════════════════════════════════════════
  #
  # Branch protection requires a SINGLE status check name that always reports
  # (never skipped). This job collects the results of all blocking jobs and
  # reports pass/fail. Conditional jobs (full-verify, build-and-test) may be
  # skipped on docs-only PRs — that's fine (skipped = success here). But if
  # they RAN and FAILED, this job fails too, blocking merge.
  #
  # To enable: Repository Settings → Branches → main → Require status checks
  # → add "gate (required)" as the sole required check.
  gate-required:
    name: gate (required)
    runs-on: ubuntu-24.04
    # Was 1 minute, when this job was three seconds of `grep` over `toJSON(needs)`.
    # The scope summary below adds a sparse checkout, setup-bun, and one Actions-API
    # call. MEASURED on the first live run of this job's new shape (run 32207501412
    # attempt 2, job 95940135217): 12 s total -- checkout 2 s, setup-bun 2 s, summary
    # 3 s, verdict 0 s. The pre-land ESTIMATE in the landing PR was ~20 s, priced off
    # the ~11 s full checkouts the other jobs in this workflow take; the sparse +
    # `filter: blob:none` checkout is what closed the gap, and the number is corrected
    # here rather than left as the guess. So the whole cost of making this check say
    # what it covered is ~9 s per run.
    # 5 is a RUNAWAY CAP, not a budget: nothing in this job retries, polls, or waits
    # on anything that can hang open.
    timeout-minutes: 5
    if: always()
    # Job-scoped least privilege. The workflow default is `contents: read` and no
    # other job elevates; this one adds exactly one read scope for one call:
    #   contents: read -- sparse checkout of the summary script + this file
    #   actions:  read -- GET /actions/runs/<THIS run>/jobs, the only source of
    #                     PER-MATRIX-LEG results. The `needs` context collapses a
    #                     whole matrix to one value, which is precisely what kept the
    #                     Windows legs invisible. No write scope, no other repo, no
    #                     other run.
    permissions:
      contents: read
      actions: read
    # Machine-readable scope, so "which green was this" is answerable without
    # scraping a log. Empty when the summary step could not run -- see the
    # `continue-on-error` note on it.
    outputs:
      scope: ${{ steps.scope.outputs.scope }}
      floor-executed: ${{ steps.scope.outputs.floor-executed }}
      floor-skipped: ${{ steps.scope.outputs.floor-skipped }}
      nonblocking-failures: ${{ steps.scope.outputs.nonblocking-failures }}
    # ── THE FLIP (drift-and-heal ADR, Aaron 2026-08-01): blocking gates are
    # corporate mode; drift checks are sovereign mode. The gate now rolls up
    # ONLY the recorded floor (registry/uncompensatable-floor.yaml):
    #   - build-and-test             (functional build/test breaks)
    #   - test-typescript-hermetic   (dependency-closed TypeScript behavior breaks)
    #   - cross-verify/full-verify   (treaty byte-lock golden vectors)
    #   - lint [semgrep]             (workflow supply-chain / script-injection)
    #   - lint-typescript            (TypeScript type breaks)
    # Every hygiene/style lint below STILL RUNS on every PR and every main
    # push — that is the drift detection — it just no longer blocks unrelated
    # lanes (the 2026-07-08 priority inversion / 2026-08-01 six-rebuild race).
    # In-scope authors get their signal from the job's own red X and from
    # hygiene/scoped-lint.ts locally; heals converge via certified healers
    # (hygiene/healer-harness.ts). Re-adding a job to this list = adding to
    # the floor = treaty-amendment consent path.
    #
    # ── AMENDMENT 2026-08-17 (Aaron, explicit: "yes add lint (TS) to the
    # required floor"). `lint-typescript` re-enters the floor. The consent path
    # named above is why this comment exists rather than a silent list edit.
    #
    # ── AMENDMENT 2026-08-19 (Aaron, explicit: "we are moving away from anything
    # that blocks into drift checks instead"). SUBTRACTION this time, via the same
    # consent path: the `build-and-test` entry below stays in the floor but its
    # PLATFORM coverage narrows to Linux. The macOS leg joins the Windows legs under
    # `continue-on-error`, so a macos-26 build or test failure now reaches this
    # rollup as success. The needs: list is unchanged — what changed is inside the
    # matrix, which is exactly why the `Emit scope` step below exists: it names any
    # leg that failed non-blockingly, so a green here still says which green it is.
    # registry/uncompensatable-floor.yaml records the narrowed coverage in data.
    #
    # ── AMENDMENT 2026-08-25 (Aaron, explicit: "lets try to get your hermetic
    # suite in and if we need to update drift policies do so").
    # `test-typescript-hermetic` enters the required floor after its probationary
    # non-blocking period. This is intentionally a narrow whole-tree exception to
    # the diff-scoped default: the suite is dependency-closed and reproducible, and
    # its bare `bun test` invocation is what prevents new test directories from
    # falling outside CI. The environment-dependent tier remains drift-only.
    #
    # The evidence is this job's own header, twenty lines up from its `name:`:
    # it was created because "Slice 9 (PR #882) shipped a real TS2322 to main",
    # and it says "codified rules (tsconfig strict mode) without a gate aren't a
    # control." It was then taken off the blocking list by the drift-and-heal
    # flip — and on 2026-08-17 the identical class recurred, same error code:
    # TS2322 in `installer/usb-iserial-probe.ts` plus six TS2300s in
    # `zflash/test-harness/run.test.ts`, live on main, while `gate (required)`
    # reported SUCCESS (measured on PR #11268, merged 02:05:47Z with
    # `lint (TS)` FAILURE alongside a green gate).
    #
    # HONEST SCOPE — this does NOT close the case that produced it. Both PRs
    # behind the 2026-08-17 breakage were individually green; the duplicate
    # import existed only in the MERGED tree. A pre-merge floor checks each
    # branch against main-as-it-was, so a two-PR semantic collision is out of
    # its reach by construction. What this DOES close is the single-PR case: a
    # branch carrying its own type error can no longer merge past a red
    # `lint (TS)`. The collision class needs a merge queue, not a bigger floor,
    # and is deliberately left open here rather than papered over.
    #
    # Register note: the registry derives the floor as the ERASURE class, and a
    # type error is not erasure — it is fully reconstructible. This entry is
    # admitted on the adjacent `scoped-build-test-break` rationale (a break
    # downstream lanes consume this tick), not on erasure. Stated so the
    # registry's derivation stays honest rather than being quietly widened.
    #
    # ── WHICH GREEN IS THIS? (2026-08-19) ─────────────────────────────────────────
    # Treating `skipped` as success is correct -- the path filter legitimately skips
    # `full-verify` and the .NET build/test steps on a docs-only PR. But it made this
    # check's SUCCESS TWO-VALUED while publishing neither value: a green here is
    # either the full seven-language byte-lock or a materially smaller run, and to
    # anything reading check status -- including armed auto-merge, which is how most
    # things land here -- WALL-CLOCK DURATION WAS THE ONLY THING TELLING THEM APART.
    # That is the vacuity class at the rollup level: a check that did not run looks
    # exactly like a check that passed.
    #
    # The `Emit scope` step below therefore publishes WHICH floor jobs executed, which
    # were skipped, which steps were skipped inside the ones that ran, and which legs
    # failed non-blockingly -- to the step summary, to a warning/notice annotation, and
    # to this job's outputs. Skipping stays legitimate and nothing new blocks; the
    # green now carries its own scope.
    needs:
      - build-and-test
      - lint
      - lint-typescript
      - cross-verify
      - full-verify
      - test-typescript-hermetic
    steps:
      # Sparse: two paths, no blobs, one commit. The full checkout other jobs take
      # measures ~11 s here; this one has no reason to pay for 36k files.
      - name: Checkout (summary script + this workflow only)
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 1
          filter: blob:none
          sparse-checkout: |
            src/Core.TypeScript/ci/gate-scope-summary.ts
            .github/workflows/gate.yml
          sparse-checkout-cone-mode: false

      # Bun only -- the summary script imports nothing but node built-ins, so this job
      # never runs install.sh (~2-4 min) for a ~1 s render.
      - name: Setup Bun
        uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2.2.0

      - name: Emit scope -- which floor jobs actually ran
        id: scope
        # OBSERVABILITY ONLY, AND DELIBERATELY NON-FATAL. The verdict is the next step
        # and its logic is untouched; this one must never be able to turn a green gate
        # red (or a red one green) because a summary failed to render or the API
        # hiccuped. A failure here shows as a visible red step inside a job whose
        # conclusion is still decided below.
        continue-on-error: true
        env:
          # The AUTHORITATIVE floor list: this is literally the `needs:` block above,
          # keyed by job id, with each result. Adding or removing a floor job updates
          # the summary with no edit to the script -- there is no second copy to drift.
          NEEDS_JSON: ${{ toJSON(needs) }}
        run: |
          set -euo pipefail
          gh api --paginate \
            "repos/${GITHUB_REPOSITORY}/actions/runs/${GITHUB_RUN_ID}/jobs?per_page=100" \
            --jq '.jobs[]' \
            | bun src/Core.TypeScript/ci/gate-scope-summary.ts

      - name: Check all gate jobs
        env:
          # Same value as above; printed by NAME here so the per-job verdict survives
          # even if the summary step could not run. `needs.*.result` (used below,
          # unchanged) is an unlabelled array -- it is what makes the verdict correct
          # and what made it unreadable.
          NEEDS_JSON: ${{ toJSON(needs) }}
        run: |
          # A job result is one of: success, failure, cancelled, skipped.
          # Skipped is acceptable (path-filter gated); failure/cancelled are not.
          echo "Floor job results (by name):"
          echo "${NEEDS_JSON}" | jq -r 'to_entries[] | "  \(.key): \(.value.result)"' \
            || echo "${NEEDS_JSON}"
          results='${{ toJSON(needs.*.result) }}'
          echo "Job results: $results"
          if echo "$results" | grep -qE '"(failure|cancelled)"'; then
            echo "::error::One or more gate jobs failed or were cancelled."
            exit 1
          fi
          echo "All gate jobs passed or were skipped (docs-only). ✓"
          echo "Scope of this green: see the \"gate (required)\" step summary --"
          echo "a skipped floor job means this green covers less than a full-scope green."

  # ═══════════════════════════════════════════════════════════════════
  # Drift, made LOUD — and still blocking nothing
  # ═══════════════════════════════════════════════════════════════════
  #
  # Aaron 2026-08-23: "we want drift to be LOUD so the red is immediately noticed, not a
  # whisper — just not blocking."
  #
  # NOTHING BELOW CHANGES WHAT BLOCKS. `gate (required)`'s `needs:` list is untouched,
  # the windows/macOS `continue-on-error` flag is untouched, and every hygiene lint keeps
  # exactly the (non-)blocking status it had. Adding either job below to the floor would
  # be a treaty amendment and is deliberately not done here.
  #
  # WHAT IT DOES CHANGE is that "non-blocking" stopped meaning "unnoticed". Measured
  # 2026-08-23 over every completed `gate` push run on main across 5.5 days (600 runs,
  # 368 cancelled before anything ran, 232 inspected): **124 job failures sat beside a
  # green `gate (required)`**, and only **32 of the 124 (26%)** reach any rate surface —
  # `platform-drift-report.ts` folds only names starting `build-and-test (`, so the
  # largest absorber, `test (TS hermetic)` at 47, is folded by nothing. Census committed
  # at src/Core.TypeScript/ci/fixtures/absorbed-failure-census-2026-08-23.json.
  # Worse, step-level `continue-on-error`
  # produces a GREEN job with a red step inside, which every one of those surfaces is
  # blind to by construction because they all read a JOB conclusion. Worst, the two
  # surfaces that carry drift over TIME had both stopped landing: `drift-sweep.yml`'s
  # push to main has been rejected by the "CI Gate" ruleset since 2026-08-13 and is
  # swallowed as a "push race", so 1,597 sweep runs concluded `success` while
  # `docs/drift-events/` stayed frozen at tick 000247.
  #
  # So loud lives in the RUN, where nothing has to be published for it to be seen: a red
  # X on a non-floor job, plus ::error::/::warning:: annotations, plus a step summary.
  # Full reasoning, the bands, and the anti-cry-wolf design: src/Core.TypeScript/ci/drift-loud.ts.

  # The specimen. One step fails on purpose, under `continue-on-error`, on every run,
  # forever — so this job concludes SUCCESS with a failed step inside. That is an exact
  # instance of the class that had no detector, and `drift-loud` below is required to
  # find it. If step-level detection ever breaks, the canary goes missing and the
  # reporter goes red with DETECTOR WENT QUIET.
  #
  # This is not decoration and it is not a test double: it is the falsifier that keeps an
  # observability layer from becoming the next thing that quietly stopped working — the
  # class this repo has now rediscovered three times. Do not "fix" the failing step.
  #
  # IT PAID FOR ITSELF ON ITS FIRST RUN (run 32651748761, 2026-08-23). `drift (loud)`
  # went red with DETECTOR WENT QUIET while this job was running two feet away, because
  # the REST jobs API reports a swallowed step's conclusion as `success` — the failing
  # step above reads `"conclusion": "success"` in
  # GET /actions/runs/32651748761/jobs. `outcome` is workflow-local and not on the REST
  # payload at all, so the class is not merely unsurfaced, it is UNREPRESENTABLE in the
  # data every earlier surface reads. The note further up this file that "the API reports
  # a continue-on-error leg's conclusion as `failure`" is true of JOBS and false of STEPS.
  # The channel that does carry it: GET /check-runs/97224512630/annotations returns
  # `annotation_level: "failure"` on a check run whose conclusion is `success`. That is
  # what the reporter reads now. A test would not have found this — only a live specimen
  # against the live API could.
  drift-canary:
    name: drift-canary
    runs-on: ubuntu-24.04
    # A runaway cap, not a budget: this job runs two `echo`s.
    timeout-minutes: 5
    if: always()
    steps:
      # The step name is load-bearing — `drift-loud` reports it verbatim, and the test
      # fixture in drift-loud.test.ts uses the same string.
      - name: drift canary -- deliberate non-blocking failure
        continue-on-error: true
        run: |
          echo "This step fails on purpose. It is the live specimen of a step-level"
          echo "continue-on-error swallow: this JOB will conclude success anyway, with"
          echo "no red X anywhere, which is exactly the class that had no detector."
          echo "The 'drift (loud)' job below must find it. If it does not, that job goes"
          echo "red — silence is what fails the check. See src/Core.TypeScript/ci/drift-loud.ts."
          exit 1
      - name: Explain the green
        run: |
          echo "This job's conclusion is SUCCESS and a step inside it FAILED."
          echo "That is not a bug here; it is the measurement."

  # The reporter. Reads the current run plus a BOUNDED window of recent completed `gate`
  # runs on main, classifies every absorbed failure, and is loud in proportion to how
  # persistent the drift is.
  drift-loud:
    name: drift (loud)
    runs-on: ubuntu-24.04
    # Was priced off `gate-required`, which measures ~12 s with the same sparse-checkout
    # + bun shape. This one adds up to ~60 Actions API calls, so 10 is a RUNAWAY CAP:
    # nothing here retries, polls, or waits on anything that can hang open.
    timeout-minutes: 10
    # `always()` — the whole point is to report when something went wrong, so a failed
    # floor job must not skip the surface that explains it.
    if: always()
    # `gate-required` is the last job in the workflow, so needing it is how this job
    # waits for everything else to reach a conclusion without enumerating a `needs:`
    # list that would drift from the real job set. It does NOT make this job part of
    # the floor — `needs` is a dependency edge, and only `gate-required`'s OWN `needs:`
    # list defines what blocks.
    needs: [gate-required, drift-canary]
    permissions:
      contents: read
      # One scope, for one call: GET /actions/runs/*/jobs, the only source of PER-STEP
      # conclusions. `needs` collapses a matrix to one value and never carries steps at
      # all, which is precisely what kept the step-level class invisible.
      actions: read
    steps:
      - name: Checkout (reporter + ledger only)
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 1
          filter: blob:none
          sparse-checkout: |
            src/Core.TypeScript/ci/drift-loud.ts
            src/Core.TypeScript/ci/verdict-drought.ts
            data/platform-drift.json
          sparse-checkout-cone-mode: false

      # Bun only — the reporter imports nothing but node built-ins, so this job never
      # runs `bun install` (~2-4 min) for a ~30 s fold.
      - name: Setup Bun
        uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2.2.0

      - name: Report drift, loudly
        # NO `continue-on-error` here, and that is the entire point of the job. A
        # non-zero exit is the LOUD signal Aaron asked for: it paints a red X on
        # `drift (loud)` in the checks list, next to a green `gate (required)`.
        #
        # It blocks nothing. `gate (required)` is the sole required status check
        # (Settings → Branches → main), its `needs:` list above does not contain this
        # job, and armed auto-merge reads required checks only. Verified against the
        # live API on the Windows legs, which have concluded `failure` beside a green
        # required check 11 times without stopping a merge.
        env:
          GH_TOKEN: ${{ github.token }}
          REPO: ${{ github.repository }}
          RUN_ID: ${{ github.run_id }}
        run: |
          bun src/Core.TypeScript/ci/drift-loud.ts \
            --repo "$REPO" --run-id "$RUN_ID" --window 60 \
            --ledger data/platform-drift.json

      # THE SECOND HALF OF THE SAME QUESTION, and the half that had no detector at all.
      #
      # `drift-loud` above asks "which failures were absorbed?" -- a question about runs
      # that EXECUTED. It is silent, by construction, about a `main` on which nothing
      # executed: hand it a window of 60 cancelled runs and `subjects` is empty,
      # `sustained` is empty, and it prints `EXIT 0 -- no sustained drift`. It is not
      # wrong; it is reporting on a measurement that never happened.
      #
      # Measured 2026-08-23T17:35Z: the last SUCCESS on main was 10fbd9a4, 16:29:06Z ->
      # 16:43:22Z, ~52 minutes and ~20 commits earlier; of the last 8 gate runs, 6 were
      # cancelled and 2 were still running. `gate.yml`'s concurrency group cancels the
      # in-flight run on every merge, three merges landed inside 24 seconds, and a full
      # gate takes ~14 min -- so merges outran the gate by roughly an order of magnitude
      # and the commits in between carry NO verdict at all. `cancelled` reads as neither
      # pass nor fail, so nothing anywhere reported it.
      #
      # NOTHING ABOUT THE POLICY IS CHANGED HERE. The concurrency group, a merge queue,
      # and the flush cadences are the three options on the table and choosing between
      # them is a human call. This step makes the condition measurable so whichever is
      # chosen can be verified afterwards -- the same number, before and after.
      #
      # `if: always()` because the step above exits non-zero whenever ITS signal is loud,
      # and one loud detector must never mute the other.
      - name: Report main's gate-verdict drought, loudly
        if: always()
        env:
          GH_TOKEN: ${{ github.token }}
          REPO: ${{ github.repository }}
        run: |
          bun src/Core.TypeScript/ci/verdict-drought.ts \
            --repo "$REPO" --window 60
