name: drift-dashboard-cadence

# The Zeta drift dashboard's own tick.
#
# WHY THIS WORKFLOW EXISTS AT ALL, given the dashboard is a committed artifact:
# a dashboard nobody runs is the purest form of the defect it was built against.
# Aaron 2026-08-22 on the Unknown verdict: "this is what most humans and AI are not
# good at keeping in their head the unknowns they forgot about lol, so the more
# mechanical the better." Mechanical means the pass does not depend on anyone
# remembering to look — not a human, not an agent.
#
# WHAT IT DOES. Enumerates every ACTIVE workflow (the roster / denominator), takes the
# LATEST run per workflow on main, folds against the persisted roster in
# db/drift-dashboard/roster.json, and FAILS THE RUN when anything is red, anything is
# unknown, coverage fell short, or a producer errored. The rendered dashboard goes to
# the job summary, so the answer is in the run itself and not only in an artifact.
#
# WRITES BACK (2026-08-23, Alexa, after Otto review):
# The heartbeat lane already solved the push-to-main problem — telemetry lanes use
# ZETA_TELEMETRY_FLUSH_TOKEN with GITHUB_TOKEN fallback. A cadence that runs and
# leaves no evidence it ran is the same defect class as unknown-reads-as-operational.
# --write refreshes data/drift-dashboard.json so society-status reads fresh data.
#
# NOTE THE STRANGE LOOP, deliberately: this workflow is itself a row in the roster it
# produces. If it stops firing, the NEXT pass reports it as a periodic check gone
# stale — the instrument watches its own silence.
#
# Security: ZETA_TELEMETRY_FLUSH_TOKEN is a fine-grained PAT scoped to contents:write
# on this repo only. Fallback to GITHUB_TOKEN keeps the pass running (read-only, no
# write-back) if the PAT is denied or expired — presence checked AND capability tested
# by the git push exit code.

on:
  schedule:
    # Every 6 hours at :41 — off-the-hour and clear of the other cadence lanes so the
    # fleet's scheduled runs do not all land on the same minute and get dropped
    # together under scheduler load.
    - cron: "41 */6 * * *"
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: drift-dashboard-cadence
  cancel-in-progress: false

jobs:
  observe:
    runs-on: ubuntu-24.04
    timeout-minutes: 10

    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          token: ${{ secrets.ZETA_TELEMETRY_FLUSH_TOKEN || secrets.GITHUB_TOKEN }}

      - name: Install bun
        uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2.2.0

      - name: Run the drift-dashboard pass (with --write)
        id: pass
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail
          set +e
          bun src/Core.TypeScript/drift-dashboard/cli.ts \
            --ref main --dop 8 --repo "$GITHUB_REPOSITORY" --write > dashboard.md
          status=$?
          set -e
          cat dashboard.md >> "$GITHUB_STEP_SUMMARY"
          cat dashboard.md
          # Store exit status for commit decision — even if --write fails to produce
          # output, the pass itself ran and that's what matters for the verdict.
          echo "pass_status=$status" >> "$GITHUB_OUTPUT"

      - name: Commit refreshed dashboard to main
        if: always()
        run: |
          # Only commit if --write actually produced changes
          git config user.name "alexa[bot]"
          git config user.email "alexa[bot]@users.noreply.github.com"
          git add data/drift-dashboard.json db/drift-dashboard/ 2>/dev/null || true
          if git diff --cached --quiet; then
            echo "[dashboard] no changes to commit"
          else
            git commit --no-verify -q -m "drift-dashboard: refresh roster $(date -u +%Y-%m-%dT%H:%M:%SZ)

          Automated refresh of data/drift-dashboard.json by the cadence workflow.
          A cadence that runs must leave evidence it ran.

          Co-Authored-By: Kiro <noreply@kiro.dev>"
            # Push directly to main — telemetry data, same pattern as tick-metrics.
            # Fallback: if push fails (token denied), the artifact is stale but the
            # run's own summary still has the answer. CAPABILITY tested, not just presence.
            git push origin HEAD:main || echo "::warning::[dashboard] push failed — token may lack capability"
          fi

      # The expression goes through `env:`, never into the script text. Two reasons,
      # and the second is the one that matters: shellcheck cannot parse `exit ${{ ... }}`
      # (it reads the literal `${{` as the status and reports SC2242), so `lint (actionlint)`
      # has been red on every PR since #14314 landed; and interpolating a workflow expression
      # directly into a shell body is the injection shape this repository routes around
      # everywhere else (see k8s-lane-partition.yml, which records the same lesson as SC2016).
      - name: Fail if dashboard is not OK
        env:
          PASS_STATUS: ${{ steps.pass.outputs.pass_status || 0 }}
        run: exit "$PASS_STATUS"
