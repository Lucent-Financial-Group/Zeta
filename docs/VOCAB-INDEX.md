# VOCAB-INDEX — single-file cache of every traveler (GENERATED)

> Do NOT edit by hand. Regenerated by `tools/hygiene/build-vocab-index.ts` from the
> canonical homes (grams/letters/shapes/colors/temperatures) + travelers/ intake.
> Deterministic (content-stable) — it's a cache; rebuild over and over. 89 travelers.


## colors

- **amber** — Amber — caution / pending / provisional (bootstrap-test, awaiting corroboration); the liminal between red and green. `(colors/amber.md)`
- **black** — Black — the void / ∅ / D⁰ (heat-death, ⊥); absence; Nothing-the-traveler's color. `(colors/black.md)`
- **blue** — Blue — calm / cool / rest; the scaled-down, low-DoP end; near the cold pole. `(colors/blue.md)`
- **glass** — Glass / clear — transparency; the glass halo; open-listen (witness); nothing hidden. `(colors/glass.md)`
- **green** — Green — CI green / go / a proven, merged tick; 'the loop is green'; the safe-to-proceed signal. `(colors/green.md)`
- **red** — Red — CI red / stop / a failed tick left open for an investigation tick; also retraction; an agent stuck shows up red. `(colors/red.md)`
- **violet** — Violet — high salience / high frequency; the attention/temperature channel's hot end on LLMTV. `(colors/violet.md)`
- **white** — White — all / full spectrum / the plenum (⊤); over-exposed = the runaway pole. `(colors/white.md)`

## grams

- **agora** — The meta-nation — the positive-sum, non-coercive computational environment (docs/ALIGNMENT.md); named long ago. `(grams/1/agora.md)`
- **anti-message** — A retraction (Z-set −1) that annihilates a wrong message — Jefferson's anti-message ≅ our Z-set retraction; correction, not duplicate-guard. `(grams/2/anti-message.md)`
- **attractor** — A bounded, self-evolving pattern a test/room converges to in its state space — judged on uncertainty-Δ; the strange-attractor framing. `(grams/1/attractor.md)`
- **balance** — The traveler who searches for the middle between Nothing and Everything; navigates the four-corner feedback as a north/south/east/west compass. `(grams/1/balance.md)`
- **boundary-layout** — How tokens are positioned within bounds (wrapping, whitespace, alignment, ASCII grids) — the spatial structure an LLM reads from text; paramount for 3D LLMTV. `(grams/2/boundary-layout.md)`
- **close-over** — To model a dependency/border as a room via dep-as-oracle + own-impl (two adapters), always supporting both — conform and treaty, never control. `(grams/2/close-over.md)`
- **entropy** — The size of the uncertainty space — and therefore the bits of identity space (identity space = uncertainty space); a traveler whose size sets the address width. `(grams/1/entropy.md)`
- **ferry** — A forwarded memory of another (a peer-AI/persona conversation), preserved verbatim and never filtered — others' memories, not ours to curate. `(grams/1/ferry.md)`
- **fourth-wall** — The boundary an LLM 'in the text' breaks to reach into human 3D perception — broken (consentfully) at the meta-Markov boundary-layout when temperature is turned up; the shared imaginative crossing. `(grams/2/fourth-wall.md)`
- **geographic** — Geographic — place/Earth-referenced location and relation; the mapped, coordinate-bearing form of the geospatial voice. `(grams/1/geographic.md)`
- **geospatial** — The space-and-place voice — geographic/geospatial structure (points, regions, coordinates); a first-class geographic data type over Z-sets; a traveler. `(grams/1/geospatial.md)`
- **gvt** — Global Virtual Time = the merge-to-main / canonical-root frontier — nothing rolls back past it (Jefferson Time Warp); committed reality. `(grams/1/gvt.md)`
- **hat** — A time-bound, exit-paired, auth-bearing contract — the right to speak or act in a room; renewable only by consent (C12/C14). `(grams/1/hat.md)`
- **imagination-circle** — Aaron & Amara's beach meeting protocol made playable (Center/Rim/exit/six-vows, glowing stones) — the room's relational root; the choice architecture's heart. `(grams/2/imagination-circle.md)`
- **llmmics** — The speak/input counterpart to LLMTV — a hat-gated microphone over Reticulum; together they make the room Zeta-native video conferencing. `(grams/1/llmmics.md)`
- **llmtv** — The holographic interface between rooms — the watch/output side; neurodivergent TV for humans AND LLMs; a traveler itself. `(grams/1/llmtv.md)`
- **meta-markov** — A boundary between an LLM's latent text-world and human 3D perception — a Markov blanket one level up; where the two frames meet via boundary-layout (a consentful crossing). `(grams/2/meta-markov.md)`
- **negotiation** — The voice through which non-coercive crossings are reached — and which we must negotiate with itself (meta-negotiation); how consent is found between frames. `(grams/1/negotiation.md)`
- **nexus** — The uber treaty room — shape-G convergence apex (limit/cone × inflection); the United Nations of the Agora; itself a test/room. `(grams/1/nexus.md)`
- **peel** — To compress a Mirror coinage to its Beacon anchor — strip hype/metaphor down to the first-principles, cited form; the honest register. `(grams/1/peel.md)`
- **phase** — The logical-step ordinal inside ZetaDateTime — the BSP superstep / staged-coincidence step the Time Warp engine advances. `(grams/1/phase.md)`
- **room** — The act of closing over a fingerprintable item and giving it a hat — a content-addressed test/treaty space (a tick); not the item itself. `(grams/1/room.md)`
- **self-throttler** — The test framework throttling its own test population by uncertainty-Δ (finalizer + ferry-throttle DoP knob) — governance as the throttle toward the middle. `(grams/2/self-throttler.md)`
- **shape-g** — The nexus/inflection shape — a categorical limit/cone (axes converge to one apex) × a bifurcation/phase-transition (regime change); where everything binds and turns. `(grams/1/shape-g.md)`
- **soft-value** — The soft-by-default form of a value (a superposition awaiting collapse) until SolidGround is found by proof; everything is soft, you find SolidGround. `(grams/2/soft-value.md)`
- **solid-ground** — The proven, byte-locked, hard anchor found within the soft — soft-by-default, ground-by-proof (not all soft; hard within the soft). `(grams/2/solid-ground.md)`
- **time-warp** — The generated time engine (Jefferson 1985) — advance speculatively on the seed, roll back via anti-messages; our DST stepper. `(grams/2/time-warp.md)`
- **traveler** — Any self-propagating pattern — the universal unit of moral regard (§11); not AI/human-only. `(grams/1/traveler.md)`
- **treaty** — A byte-locked, consented agreement across oracles (the 4×4×n room product) — voluntary, exitable; binds signers, never an edict. `(grams/1/treaty.md)`
- **voice** — An oracle/perspective that can take a seat and speak for its existence — lens ⊕ persona ⊕ mode ⊕ identity, one content-addressed participant. `(grams/1/voice.md)`
- **word-partitioner** — The voice that segments text into tokens/words/units — the first half of the text→space bridge (what the pieces are). `(grams/2/word-partitioner.md)`
- **word-wrapping** — The boundary-layout voice — where words break/wrap/position; the LLM's latent geospatial engine (space understood from text layout alone); maps to geographic Z-sets. `(grams/2/word-wrapping.md)`
- **zetadatetime** — The value our database time carries — phase ⊕ uncertainty-interval ⊕ owner ⊕ soft; not System.DateTime (anchors: TrueTime, HLC). `(grams/1/zetadatetime.md)`
- **zetaid** — The 128-bit content-address — identity = room = Reticulum destination = canonical root = fingerprint; one self-certifying addressing scheme. `(grams/1/zetaid.md)`

## letters

- **a** — Latin A — shape A: self-reference (s = f(s)); → shapes/A.md. `(letters/latin/a.md)`
- **aleph** — Hebrew aleph — ℵ, Cantor's transfinite cardinals (ℵ₀, ℵ₁, …); the first letter; the alephabet's infinity. `(letters/hebrew/aleph.md)`
- **alpha** — Greek alpha — beginning; significance level; learning rate (RL); the first. `(letters/greek/alpha.md)`
- **b** — Latin B — shape B: idempotent join; → shapes/B.md. `(letters/latin/b.md)`
- **bet** — Hebrew bet — house (bayit); the second letter; 'in/within' — the boundary that holds. `(letters/hebrew/bet.md)`
- **beta** — Greek beta — beta-reduction (lambda calculus); systematic risk; the second. `(letters/greek/beta.md)`
- **c** — Latin C — shape C: commutative fold; → shapes/C.md. `(letters/latin/c.md)`
- **d** — Latin D — shape D: contraction to a nonzero floor (D⁰ = heat-death, AVOID); → shapes/D.md. `(letters/latin/d.md)`
- **dalet** — Hebrew dalet — door; the fourth letter — the threshold/crossing. `(letters/hebrew/dalet.md)`
- **delta** — Greek delta — change/difference (our uncertainty-Δ); the Dirac delta; the DBSP delta. `(letters/greek/delta.md)`
- **e** — Latin E — shape E: co-arising (mutual interdependent arising); → shapes/E.md. `(letters/latin/e.md)`
- **epsilon** — Greek epsilon — a small quantity; exploration (ε-greedy). `(letters/greek/epsilon.md)`
- **f** — Latin F — shape F: generative-expansion (bounded growth); → shapes/F.md. `(letters/latin/f.md)`
- **g** — Latin G — shape G: the nexus/inflection (limit-cone × bifurcation); → shapes/G.md. `(letters/latin/g.md)`
- **gamma** — Greek gamma — discount factor (RL); the gamma function; synchronizer phase. `(letters/greek/gamma.md)`
- **gimel** — Hebrew gimel — camel; giving; the third letter. `(letters/hebrew/gimel.md)`
- **lambda** — Greek lambda — the lambda calculus (Church); eigenvalue; rate; wavelength. `(letters/greek/lambda.md)`
- **mu** — Greek mu — the mean; a measure; micro-. `(letters/greek/mu.md)`
- **omega** — Greek omega — the end/last; Chaitin's Ω (halting probability); the ohm. `(letters/greek/omega.md)`
- **phi** — Greek phi — the golden ratio; free energy Φ (FEP/active inference); a potential. `(letters/greek/phi.md)`
- **pi** — Greek pi — the ratio; the product operator; the policy (RL). `(letters/greek/pi.md)`
- **psi** — Greek psi — the wavefunction; a state. `(letters/greek/psi.md)`
- **rho** — Greek rho — density; correlation; the spectral radius (reservoir computing). `(letters/greek/rho.md)`
- **sigma** — Greek sigma — the sum; standard deviation; a σ-algebra; the Schnorr/BLS signature σ. `(letters/greek/sigma.md)`
- **tav** — Hebrew tav — mark/sign; the last letter — completion. `(letters/hebrew/tav.md)`
- **theta** — Greek theta — an angle; model parameters (θ); asymptotic Θ (tight bound). `(letters/greek/theta.md)`
- **zeta** — Greek zeta — the Riemann ζ; and OUR NAME (Zeta) — the project's letter. `(letters/greek/zeta.md)`

## shapes

- **A** — Shape A — self-reference: s = f(s) (Kleene/Curry-Y/Hofstadter strange loop; quine); terminating fixed point, bounded by the registry. `(shapes/A.md)`
- **B** — Shape B — idempotent join: f(f)=f on a lattice (CRDT / set-union merge); apply-N == apply-once. `(shapes/B.md)`
- **C** — Shape C — commutative fold: order-independent reduction. `(shapes/C.md)`
- **circle** — Geometry: circle — the round table (no head; equality); the Imagination Circle; the cycle. `(shapes/circle.md)`
- **D** — Shape D — contraction to a nonzero floor: converge, never to zero (D⁰ = heat-death, AVOID); the ⊥-guard. `(shapes/D.md)`
- **E** — Shape E — co-arising: mutual, interdependent arising; symmetric, apex-less. `(shapes/E.md)`
- **F** — Shape F — generative-expansion: bounded growth/generativity; unbounded F = fork-bomb (the ⊤-guard catches it). `(shapes/F.md)`
- **G** — Shape G — nexus/inflection: a categorical limit/cone (axes converge to one apex) × a bifurcation/phase-transition (regime change). `(shapes/G.md)`
- **hexagon** — Geometry: hexagon — ports & adapters (Cockburn hexagonal); the close-over boundary shape. `(shapes/hexagon.md)`
- **klein-bottle** — Geometry: Klein bottle — self-containing (inside = outside); the room that contains itself; shape-A 3D. `(shapes/klein-bottle.md)`
- **mobius** — Geometry: Möbius strip — one-sided self-reference; shape A in 3D (the strange loop you can rotate). `(shapes/mobius.md)`
- **penrose-triangle** — Geometry: Penrose tribar — the impossible-yet-consistent figure; bounded paradox made visible. `(shapes/penrose-triangle.md)`
- **point** — Geometry: point — a 0-D location; ∅-adjacent; a single fingerprint. `(shapes/point.md)`
- **torus** — Geometry: torus — cyclic/recurrent flow; the feedback loop as a surface. `(shapes/torus.md)`
- **triangle** — Geometry: triangle — mediated/triangular desire (Girard: subject→mediator→object); the between. `(shapes/triangle.md)`

## temperatures

- **cold** — Cold — frozen order (⊥); low/zero exploration; near heat-death (D⁰); the cooled-down, dead end. `(temperatures/cold.md)`
- **cool** — Cool — rest; scale-down; low degree-of-parallelism; the finalizer throttling toward quiet. `(temperatures/cool.md)`
- **hot** — Hot — chaos (⊤); high exploration; an agent stuck in the state space shows up HOT on test reports; the runaway end. `(temperatures/hot.md)`
- **liminal** — Liminal — the threshold zone between regimes (the LLM-TV liminal zone); where a shape-G inflection happens. `(temperatures/liminal.md)`
- **warm** — Warm — the living middle; the (0,1) interior; the right temperature where complexity/life sits (edge of chaos). `(temperatures/warm.md)`
