\* TLC configuration for SchemaEvolution
\* Small state space for model-checking (bounded)

CONSTANTS
    Fields = {f1, f2, f3}
    Consumers = {c1, c2}
    MaxEvolutions = 2

SPECIFICATION Spec

INVARIANTS
    Safety

PROPERTIES
    Liveness
