# Mixed-message bridge: conservative withdrawal admission

Date: 2026-09-08 UTC
Author: Vera, OpenAI Codex using GPT-6 Astra
Operational status: research-grade implementation and development validation
Lifecycle: active
Disposition: bounded source correction; independent assembled admission pending
Work item: 081M1Z63YMC087G0R003N5FH9X

## Source and precise scope

Source `567a9f004cf67b9df6d4a82c55fadaaf4fdd0b9b` adds 34 bridge admission lines
and dedicated controls after the remotely preserved
[counter/path checkpoint](2026-09-08-mixed-message-epoch-bridge-corrections.md).
It imports the exact [withdrawal clarification](2026-09-08-mixed-message-withdrawal-admission-clarification.md)
from root `87bc6b28aa1d0ef780accf592b99ce70a16283e6`: 4,448 bytes, SHA256
`99576A0F9113839905463FF43CB35FF224524EDF8C207F38C7089984E7914DEE`.
The note's original pending status remains unchanged. The final service manifest
must include that exact independent source identity. Other root invocation and
register paths were not imported with it.

A cut must keep ActiveIds disjoint from Retractions. When its current supplied
Retractions is nonempty, the bridge refuses selected learned artifacts and
initial weights. Training also refuses child cuts and forecasts. Compensation
refuses learned selected artifacts or initial weights in its retained query and
weights in its checkpoint; the existing prior-result/current-state association
still applies. The guards run during plan admission before owned setup or any
numerical entry. A failed admission returns the existing typed Failure.

This conservatively invalidates a superset of possible descendants. It does not
identify affected ancestry, certify an artifact as unaffected, or impose global
historical revocation across independent callers. ParentVersion remains a
publication/history reference; it provides no old initialization vector. Cold-
start training on active nonretracted rows remains a separately budgeted
operation. There is no inverse SGD, work refund, new route, added wire field,
extra numerical entry or state-replay algorithm in this change. The existing
registered M4/M5/frozen-query inputs have no withdrawal and remain unchanged.

## Original observations and limits of the controls

Against the unchanged `4402be8` bridge, eight source-bound plan-admission
controls returned AdmittedPlan where the new boundary requires refusal:
selected artifacts, initial weights, child cuts, child forecasts, retained
selected artifacts, retained initial weights, checkpoint weights, and active/
retracted overlap. The separate positive case already admitted cold-start
training with ParentVersion metadata, a pure query and a pure compensation
payload. A second retained attempt found that both an absent clarification and
a wrong-hash clarification still returned ServiceManifest. All ten actual
failed assertions, raw outputs, source/test copies and commands are retained.

These are static admission fixtures. In particular, the compensation payloads
are inert records, not an actually returned historical query or a successful
compensation replay. Passing the positive payload does not establish complete
retained-history provenance or core runtime admission. The core owner must
independently enforce and test its actual source/checkpoint/operation contract.
Likewise the child-forecast admission mutation is not a successfully admitted
actual forecast bundle. No artificial callback result is promoted to runtime
evidence by this report.

After the guards, all 11 targeted controls passed. The first full dedicated
suite passed 111 tests but strict mypy reported one test-only no-any-return
annotation error in the inert compensation builder. Its source and diagnostic
remain retained. An explicit local annotation corrected that fixture; production
source was unchanged. The final suite passed all 111 tests in 2.54 seconds,
and strict three-file mypy, Ruff and format checks passed.

## Custody, repository checks and handoff

The [withdrawal custody](mixed-message-epoch-bridge-validation/2026-09-08/withdrawal-1/README.md)
contains 562 regular original files, 3,651,442 original bytes, in a lossless
647,003-byte archive with SHA256
`1860339907378E047EBEA740713CB7493ED9A4EC0AC61567656F86023964297E`.
Its 737-node lstat inventory includes 40 passive convenience symlinks.
The full source-stable preflight passed all 18 checks in 590.28 seconds;
the separate formatter exited zero in 17.50 seconds.

The final bridge is 199,844 bytes, SHA256
`2A6154EB842116837843E8E100AB08CF4A4C735C635ED693E8C3F58E11B4961B`;
the test is 68,585 bytes, SHA256
`1A533E6F2B1A17F8945B7C90E7BF3185411296756EAC16487327883CB74E9EDE`.
The 16,771-byte controls helper remains unchanged with SHA256
`03ED13D4D860D1E3B61B73AF265B7C65A8251B258623C16B1B1ED28EC6C51711`.
All three match their committed source, final fixture/check snapshots and
pre-gate source copies; the imported clarification matches root Git bytes.

The archive preserves the two original refusal captures, intermediate targeted
pass, both 111-test attempts, the original typing diagnostic, final checks,
repository command records, unchanged clarification import and the previous
a6ca normal-push/remote proof. Every stored member and every independently
re-read local original matched before publication. Symlinks are metadata only;
no archive extraction or numerical service was used for the custody check.

Repository gates include existing numerical unit/model tests. This work did
not invoke the named actual M4/M5 route, frozen nested query or standalone
native numerical producer. The formatter's recorded zero exit is limited by
its C#/VB support and unsupported F# projects. The final source files remained
unchanged through validation; later report/archive paths receive separate
normal push checks.

This is the implementation owner's source/custody account. Final bridge/peer
source acceptance, assembled independent service manifest admission and the
coordinator's fixed actual controls remain separate requirements. Full actual
BridgeResult remains memory-retained; this change adds no whole-result encoder
or new recording framework.
