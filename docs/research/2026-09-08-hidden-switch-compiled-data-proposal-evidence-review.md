# Guarded controller: actual data-proposal evidence review

Date: 2026-09-08 UTC
Operational status: research-grade
Lifecycle: active
Work item: 081M1XXWTTF087G0R000X1HMD0
Reviewer: Vera, OpenAI Codex using GPT-6 Astra
Disposition: finite metadata proposal accepted; full admission false

I independently audited evidence a0293cdddd778e95f883b91ea4cd0b7dab021ef1
in the predictor-native writer. This accepts the retained proposal and its
custody associations. It does not admit a physical read, an executing call
graph, the complete runtime or the frozen study. The coordinator-requested
pause applies after this audit; no new reader or runtime investigation is
started here.

The [source and launcher review](2026-09-08-hidden-switch-compiled-data-proposal-source-review.md)
retains both the original pre-child runtime-size refusal and the corrected
launcher boundary. Actual attempt two uses wrapper source
a80211d357d548d97caf850f367543abfbf91638 and execution/preparation
af6c1935c5240097b459d0508e204609d60855d7. Its exact manifest is 6,248 bytes,
SHA256 `64DB2CAE0E76FCBF7DE3C3EDD5825A215D61EFC19A8ECE778C3BC73D7445942A`.
All twelve stored files equal their immutable evidence blobs. Every gzip
record's stored and decompressed identity matches: 63,538 stored bytes and
381,147 original bytes.

The proposal is 87,278 bytes, SHA256
`7A30217E5686294E1FFD02593703D5E7348C1A528A7BB88620BB83373E745D07`.
Its complete value agrees with the retained terminal outcome and publication
descriptor. The independent [audit source](hidden-switch-compiled-validation/2026-09-08/data-proposal-independent-audit/audit.py)
and [result](hidden-switch-compiled-validation/2026-09-08/data-proposal-independent-audit/result.json)
are retained with empty stderr. Both initial and final audit executions
returned zero. A wording correction preserves the initial source/result:
`NewProcessesByReviewer=0` was too broad because immutable Git reads use Git
subprocesses. The corrected field explicitly counts study or metadata-process
launches. No project helper, validator, decoder, target, raw dump or new
memory query was invoked by this reviewer.

## Actual associations and counts

The owned metadata child has PID 71479. Main wait, cleanup wait and final
exit code are all zero; direct-child closure is true, outer failure is null
and cleanup failures are empty. The retained invocation, inner start/finish
and outer finish are ordered and elapsed time is below the sixty-second
bound. Both stderr streams are empty. The exact retained launcher hash is
741927F2980759CF50DEBDBB561D9BF801B673D63F64DE67CF603CD062577FFF.
The observed 18,090,720-byte Python image is explicitly admitted under the
separate 32 MiB runtime-image limit.

All sixteen outer source/test identities agree before and after the child
and with immutable source blobs. The fourteen inner source identities match
the reviewed wrapper's closure roster. This is the declared source-file
binding, without claiming complete interpreter or loaded-code identity.

Both input manifests have their exact expected identities. The 132 selected
archived records, their stored/original pairs and the two manifests account
for 266 retained identities and 8,155,384 original input bytes. Each selected
record matches its immutable execution-cut blob and both manifest hashes.
No `OriginalLocalFile` or recorded dump path was opened by the auditor.
The addresses in the inputs remain metadata.

The 413 journal rows are exactly one start, fourteen source identities,
266 input identities, one inventory admission, 130 method admissions and
one proposal publication. Identity order and complete values agree with the
terminal arrays. All 130 method descriptors and both nine-member unfinished
rosters agree with the earlier transfer inventory. The original noncontiguous
method-role identifiers remain intact.

## Independent range reconstruction

The auditor uses standard-library parsing and original retained instruction
words, without importing the proposal or classifier implementation. For each
unknown static cell it reconstructs the MOVZ/MOVK value, unsigned LDR offset,
register relationships and BR/BLR use. For each literal it reconstructs the
signed PC-relative displacement, D/Q width and compiler label association.
It then compares every complete range and site record with the proposal.

This establishes the exact deterministic roster: 43 eight-byte cells with
134 site references, followed by ten literals containing 152 expected bytes.
Cells and literals each sort by unsigned address; uses retain their original
method/word order. All 53 intervals are pairwise disjoint, including across
kinds. The total selected length is 496 bytes. Expected literal bytes and
unknown cell addresses are inherited metadata, not newly observed memory.

The proposal binds the same 6,208,508,456-byte dump identity,
SHA256 `7584B8D3E56DAFA79CAE8C954C03C2587AC25134CAA970F67CE530FDE17D3709`.
That file was not opened. `ProposalOnly=true`, `ObservedExecution=false`,
`RawDumpOpened=false`, zero new memory queries/source draws and all three
full admission flags false are consistent with the reviewed collector and
retained result. Successful completion means this finite metadata collection
completed.

## Remaining acceptance gap and stopping point

I also reviewed the resource note at
98c024cc28e5e8b7272ff437b38835d61b234df7. Its quantitative limitations agree
with the retained 8,665-word inventory. The counts below overlap; they are
not a sum of independent unresolved obligations.

A future separately reviewed physical read could observe 43 cell values for
134 static site correspondences and compare ten literal ranges. It would
leave all target identity/code/effect and executed-dispatch premises open.
The existing 191 recognized static-cell sites include 57 previously bound
sites; the other 39 indirect sites do not acquire a supported construction
from this proposal. All 230 indirect transfers still need the relevant
execution or complete target-set argument. There are also 468 outside direct
call sites, 7,357 instruction rows whose effects remain uninspected, 317
conditional sites, 125 returns and seventeen traps.

Nine unprepared reflection rows and nine extra compiler blocks remain
separate rosters. GuardSet numerical data still need the required object and
selector-register association. Arithmetic, caller/adapter, generated and
concrete generic code, exception/unwind and framework/runtime premises remain
part of the frozen contract. The earlier 130 current hot extents and exact
compiler/physical/decoder correspondence retain their finite scope; neither
those extents nor this proposal establish that every listed method executed.

The already replayed hand semantics remain valid bounded evidence. They do
not replace the independent full runtime obligation or the complete outer
conformance/implementation archive required before registered source and cost
work. The 53-range proposal supplies no policy-quality, training, latency or
allocation measurement. Full runtime closure has no demonstrated remaining
finite work budget or evidence-based completion-time estimate at this cut.
It also does not prove a different runtime is necessary. The stopping point
preserves these distinctions and authorizes no further range or process.

```text
Agency-Signature-Version: 1
Agent: Vera
Agent-Runtime: OpenAI Codex
Agent-Model: GPT-6 Astra
Credential-Identity: AceHack
Credential-Mode: shared
Human-Review: not-implied-by-credential
Human-Review-Evidence: none
Action-Mode: autonomous-fail-open
Task: 081M1XXWTTF087G0R000X1HMD0
Co-Authored-By: Codex <noreply@openai.com>
```
