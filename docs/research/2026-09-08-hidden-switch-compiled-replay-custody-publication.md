# Compiled replay and native custody: bounded publication

Date: 2026-09-08 UTC
Operational status: research-grade
Lifecycle: active
Author: Vera, OpenAI Codex using GPT-6 Astra
Work item: 081M1Z5ZW1T087G0R0002KPWJ8
Artifact status: local gates and independent scope review complete; CI and main proof pending

This finite slice imports reviewed prerequisites through coordinator commit
`cb4514e06b61fcebb2600ea8ea7f11abd08c8bcd`. It preserves the static,
file and identity replay implementations, actual separate fixture captures,
strict outer structure/artifact reads, bounded record storage, copied-target
custody, all 130 current native extents and their complete finite LLVM word
correspondence. The [validation index](hidden-switch-compiled-validation/2026-09-07/README.md)
links the exact source cuts, independent reviews and lossless attempt records.

The separate source cut `e6238f8acc89c4d619c6631f0cb0111aa9258c20`
passed 1,259 compiled Python tests and all 18 full local repository checks.
Later identity and decoder source have their own focused validation and
independent review; the publication's combined gates are recorded below when
complete. No historical count is relabeled as a later combined run.

## Scope that remains open

Complete 92-case outer production/replay, runtime body and call closure, and
the final actual-envelope chain remain pending. Every full runtime admission
flag remains false. No implementation archive, registered 9307/9409 source or
scientific cost measurement is created by this publication. The original
study and frozen compiled protocol are unchanged. The parent implementation
claim remains active on its separate remote branch and is omitted only from
this finite publication tree.

## Initial claim push and integration chronology

The ordinary claim push targets `f4b421adcdaf4f85df28fe9f53ee16ebc94ef0fa`
and returned success. The coordinator started the subsequent clean, uncommitted
integration while that push's preflight was still running. Therefore the
retained all-16-check output is not assigned an exact source-cut validation
claim. This publication reruns the complete gate after committing integration.
Both original logs are retained as an operational sequencing error, with no
measured experiment affected and no force push or overwritten evidence.

## Publication validation

At `be839a0c4784b25c35eb4a1cfb43fe087c11c999`, the full gate passed all
18 checks after an unchanged retry. The first attempt's Java SIGSEGV remains
retained with its complete 114-entry TLC diagnostic tree, an unchanged
one-test retry and both full logs. The separate native Python helper discovery
passed 67 tests. The [gate and source-history record](hidden-switch-compiled-validation/2026-09-08/replay-publication-gates/README.md)
binds all 14 lossless records, the original Git tree and the 31 changed
source/test byte identities. No source repair or diagnosis is inferred from
the JVM recovery. The local gate's quiet output does not state an aggregate
native test count, so none is invented.

The canonical root/native/reference/review preservation refs were advanced by
ordinary fast-forward push and their exact hashes verified by a fresh remote
read before publication reliance. Additional `wip/vera-compiled-*` aliases
were also created during coordinator preservation; they remain harmless
additional refs and do not replace the four declared canonical branches.

The finite work item's final completion and exact main proof remain pending
until the PR merges. The parent compiled-controller work remains active.

The [independent source review](2026-09-08-hidden-switch-compiled-replay-publication-review.md)
accepts the 31 changed source/test files and explicitly identifies six inherited
main variants. The finite claim is released in this reviewable publication tree;
its work item remains open until exact main publication is verified.
