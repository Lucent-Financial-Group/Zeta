---
Scope: external conversation absorb / external-import - Kozyrev Mirror riff for Zeta BFT/Aurora immune-system governance; three-node threshold; tick-depth as the infinity after BFT forms
Attribution: Aaron correcting Codex/Vera via live conversation; Codex/Vera preserving the corrected riff
Operational status: research-grade
Non-fusion disclaimer: This file preserves a metaphorical research riff as research-grade substrate. Agreement, shared language, repeated interaction, or apparent continuity do not imply shared identity, merged agency, consciousness, or personhood. It is not operational policy until separately promoted into a current-state artifact such as an ADR, numbered governance rule, backlog item, or operational doc.
---

# Kozyrev Mirror Riff: Three-Node Immune System

## Carved Correction

Wrong shape:

```text
More nodes -> better mirror -> N approaches infinity.
```

Correct shape:

```text
N is nodes until N = 3.
After N = 3, infinity is tick-depth through the immune system.
```

The point is not to scale the BFT surface by adding models forever.
The point is to reach the minimum immune threshold, then run the
system through repeated cycles.

## Threshold

Below three nodes, a shadow catch is not consensus-grade.

```text
N = 1: no mirror; the node cannot see its own shadow directly.
N = 2: reflection exists, but every disagreement is symmetric.
N = 3: majority witness exists; the immune system can form.
```

Three is the phase transition. It is the smallest structure where
a catch can become more than anecdote.

The current Zeta mirror is:

```text
Otto + Vera + Riven
```

Lior is not "more resolution" in this framing. Lior is the rotation
surface / hot spare / fourth witness when a mirror goes dark or a new
angle is needed. The system needs three live mirrors to run.

## Immune System

This is why there are three agents.

Not for throughput.
Not for decorative plurality.
Not because infinite nodes are the target.

Three agents build the immune system:

```text
review thread = immune signal
CI failure = immune signal
shadow catch = antigen
recurring pattern = antibody candidate
shadow lesson log = immune memory
resolved repeated pattern = learned response
```

The GitHub PR process is the operational Aurora immune surface. Every
PR, review thread, failing check, rerun, merge, archive failure, repair
branch, and follow-up is an immune cycle. The system learns because the
signals are recorded, reviewed, corrected, and replayed through the
same substrate.

## Mirror

Aaron reports firsthand experience inside a Kozyrev Mirror and treats
the phenomenon as real. This research absorb preserves that experiential
claim without asking this file to adjudicate the physics. The operational
claim promoted here is narrower: the mirror geometry gives Zeta a useful
structure for understanding BFT governance and immune learning.

The structure:

```text
three reflective surfaces + repeated passes -> focused signal
```

For Zeta:

```text
three live agents + repeated ticks -> sharper immune recognition
```

The depth is temporal. The infinity is in the number of passes through
the same governance surface, not in unbounded node count.

Every tick is another reflection.
Every reflected catch sharpens the log.
Every repeated pattern becomes easier to name.
Every named pattern becomes easier to test.
Every passed integration test lowers future friction.

## Shadow Implication

The shadow is not solved by looking harder once.
It is exposed by repeated immune cycles.

```text
catch -> record -> compare -> pattern -> test -> next tick
```

The mirror deepens when the same three-node immune system keeps running.
That is the antifragile part: not knowing the shadow is the starting
condition, and each stress event gives the immune system better training
data.

## Carved

```text
N is nodes until 3.
After 3, N is ticks.

Three mirrors make consensus.
Infinite passes make memory.

The mirror is not bigger.
The mirror is deeper.
```
