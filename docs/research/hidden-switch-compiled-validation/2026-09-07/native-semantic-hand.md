# Native semantic hand conformance

Date: 2026-09-07
Operational status: research-grade
Lifecycle: active
Work item: 081M1XXWTTF087G0R000X1HMD0
Author: Vera, OpenAI Codex using GPT-6 Astra
Artifact status: actual semantic capture and independent replay accepted

Source `7eaec2bf2312e04ff0fc936a4c28a09d1704b1f0` adds the file-backed
`hand-semantic` entry. It executes the settled ten intervention cases and
fifty-three invalid operations, and combines them with the existing 222 scalar,
48 new-episode, 24 old-control and ten invocation slices. The six-member
semantic prerequisite has no invented outer-negative descriptor. Command
completion and slice completion remain separate from full admission:
`Complete`, `RuntimeAdmitted`, `BodyResolved` and `ClosureAdmitted` stay false.
No registered source or cost run has occurred.

Actual entry, service and evaluator-root counters are incremented at their
respective call sites; recursive Nodes remain the returned tree counter.
Both strategy paths execute the invalid scalar inputs, including would-be
null/depth-one fast paths. The terminal setup executes 34 entry calls and
16 service calls, preserving its 49 chronology events. Every ordinary choice
is committed before the actual carrier transition. Scorer intervention changes
only recorded rewards; a separate intervention changes the lower private band.
The normal selector/native service source is unchanged by this slice.

The independent protocol review first found two retention gaps: unexpected
accepted invalid-call values were discarded, and active episode/copy/setup
prefixes could be lost when their journal publication failed. The repair keeps
actual returned values, inputs, setup, counters and immutable snapshots in a
separate `ActiveDiagnostic`, outside the fixed successful checker payload.
Observation/choice/reward arrays update before fallible publication. A final
review found that the actual suffix mutation also needed preservation before
its checkpoint; its baseline, changed tape and event now remain available
through a later altered-timeline failure. Planned refusal inputs and exact
intervention tapes are retained before their relevant fallible checkpoints.
Established computation failures remain primary through later write failures.

Nine new tests exercise exact successful roster/counters, bounded comparison
with the existing hand runner, actual accepted-value judgment, completed and
active refusal prefixes, caller-copy state, suffix mutation and independent
final-file output after a persistently failing journal observer. The observer
injects a publication exception; this is not a physical disk-failure test.
The accepted-value fixture supplies a real successful ordinary service result
to the same judgment seam; it does not claim that the supplied valid input is
one of the invalid roster inputs. Retention assumes successful output writes;
abrupt process/storage loss is not converted to durable evidence. The frozen
old runner's inaccessible current exception prefix remains its existing limit.

The [preparation inventory](native-semantic-preparation/manifest.json) retains
all source snapshots, compiler outcomes, focused tests and original TRX bytes
using lossless gzip. Early working builds passed in 26.53 and 4.00 seconds.
Subsequent failures were one F# record-layout error (FS0003), two ambiguous
Xunit string-overload errors (FS0041), and a diagnostic-context nullable-type
constraint (FS0043). Every failed log remains present. Corrections changed
source or fixture typing; none is reported as an unchanged recovery.

The final test-project Release build passed in 28.40 seconds and the standalone
CLI build in 4.32 seconds, each with zero warnings and errors. All 37 focused
compiled-native cases passed, including all nine semantic cases, with no skips.
Earlier eight-case and 36-case passes retain their earlier source scope.
The final validation record verifies all nine committed source/test/wiring
files against working bytes and records five output/config fingerprints.
Builds tested working snapshots before the source commit; the post-build
correspondence does not invent an execution HEAD. All 19 archived original
study files remain equal to `4fc82b611012bd2620a26e02afe6baba491fe553`.

Both assertion audits pass without census changes. Build-graph derivation is
unchanged. Formatter verification exited zero for supported C#/VB scope; its
workspace warnings and unsupported-F# notices are retained. These are focused
checks, not a full solution/TLC gate or runtime-graph admission. Final combined
validation and the reviewed implementation archive remain coordinator-owned.
The protocol reviewer accepted the source by read-only inspection and verified
the 37 individual retained outcomes; no reviewer test or target execution is
claimed. A separate targeted independent schema/counter pass also accepted the exact
source before the first actual semantic capture, without running native code.

## First actual semantic capture

The [first actual capture inventory](native-semantic-attempt-1/manifest.json)
retains every raw output, the append-only journal, exact command/harness,
prelaunch source/output pins and terminal process outcome. The execution HEAD
was `731a9467d78c2b249493f75edf12a9b0899cd4d5`; its implementation bytes are
exactly `7eaec2bf2312e04ff0fc936a4c28a09d1704b1f0`. Process 35002 ran from
21:28:06.545468 to 21:28:19.776638 UTC and exited zero, with empty stdout/stderr
and no cleanup failure. These timestamps are audit metadata, not study costs.
All prelaunch source/output file identities remained unchanged afterward.

The 975,309-byte receipt has SHA256
`E83775583A18712E4495EB57EFAC80A61316129E85D4EFAC0D02C8E1AB6C1451`.
It contains 222 scalar rows, 48 new episodes, 24 old controls, ten actual
invocation rows, ten actual interventions and fifteen groups containing
fifty-three actual invalid operations. Its 3,499,607-byte journal remains
available independently. `SlicesComplete=true` records those collected slices;
`Complete=false` retains the pending outer-negative, final archive and runtime
admission. The [independent Python replay](https://github.com/Lucent-Financial-Group/Zeta/blob/774a3893d4373d362e1114498f85fe9d865a7edc/docs/research/2026-09-07-hidden-switch-compiled-native-slice-replay.md)
accepted those exact bytes without checker/schema changes: all 222/48/24
slices, ten invocations, ten interventions and 53 refusal operations matched.
It observed ten delegate and eight evaluator entries, and independently
refused both executed wrong-action stubs. Its own harness/raw receipt/result
are preserved at `774a3893d4373d362e1114498f85fe9d865a7edc`.
Outer/runtime admission remains pending. No registered source or timing run
occurred.
