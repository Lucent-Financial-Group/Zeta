# Mixed-message assembled capsule: independent admission

Date: 2026-09-08 UTC
Reviewer: Vera, OpenAI Codex using GPT-6 Astra
Operational status: research-grade independent source and custody review
Lifecycle: active
Disposition: exact capsule accepted for the registered first M4 invocation
Work item: 081M1Z63YMC087G0R003N5FH9X

## Accepted immutable identities

This review accepts packet `d61ee6a994b154c8df3b0627e9b23b5cd9db13c6` and its
selected source cut `3c3a76ad6674bb40b2e1309021735950c282d8d5`. The independently
checked service manifest is 22,381 bytes, SHA256
`F92041F0DBC344DA877DFA970B3C5C7AD6817C8285AAA28E164872F2CE6D56F1`.
The accepted archive is 39,770,169 bytes, SHA256
`E51A00FC29FA2A13B09F152AB0F55908B7E03982036E5084F54A221815149445`.
Supply these exact two hashes to the already reviewed capture helper.

All 298 regular archive members match their recorded lengths/hashes, totaling
92,284,407 original bytes. All 65 ordered selected-source rows, totaling
1,951,508 bytes, match both Git at the source commit and current regular files.
Their 65 retained Git stdout/stderr/completion observations bind the same
source paths and commit. The exact 69-entry flat binding map includes the six
fixed convention/clarification documents, unchanged scalar protocol, both
scripts and three direct DLL identities. Required Python origins, core/wiring,
invoker and separate frozen-query registration are present. Binding count and
byte bounds fit both the existing scalar and mixed-message admission surfaces.

All seven direct roles match the current observed files, archived originals,
separately retained direct observations and complete after-read originals.
Both scripts retain the exact Core/Core.Abstractions/Bayesian reference order.
The six core source/wiring/test files equal accepted owner
`d7e8e3806afaecdaed8a4a1ff5244c899201c870`. Production bridge and controls remain
567a bytes; the test is the separately validated 4C8ECC portability correction.
Peer 5D0DD505, scalar script C1174406, invoker CC7FBF89, capture FBD0DF78 and
freeze 0DBFF44E match their previously reviewed exact sources. This extends the
[helper review](2026-09-08-mixed-message-assembled-helper-review.md) to the actual
committed capsule; it does not repeat or broaden the core mathematical review.

## Fixed control and budget boundary

The inert M4 builder retains the original ordered positive and cancellation
nodes, one sweep, two planned projections and unchanged scalar budgets. From
the actual selected script/DLL sizes, its external reservation is 31,206,246
combined bytes and ten artifact slots. The Store receives 237,229,210 bytes
and 4,086 slots, including its existing journal and terminal reserves. These
sum to the fixed 256 MiB and 4,096 slots; they are retention accounting, not
peak-memory or operating-system quota claims.

M5 remains the four dependent same-owner sessions: child training, the two
specified target-hidden queries, then parent training using their actual
closed returns. Its planned projections and external native reservation are
zero. The separate frozen query preserves the exact 0.75/zero feature row,
origin eight, target/label time nine and null target; it consumes the actual
child and parent artifacts in a fresh single-session budget. It adds two
forwards and no fitting. No plan builder was executed in this review.

The first actual call is M4 only. A closed capture is not a numerical verdict;
the coordinator must retain and inspect its two actual service outcomes and
committed-state prefix before proceeding under the existing M5 registration.
The same accepted source pair supports that later route and separate query
only when their already declared actual dependency/closure prerequisites hold.
No changed case, quota, retry or replacement computation is admitted here.

## Validation custody and limits

The separate assembled-validation archive verifies all 86 original identities
through 69 byte objects, without losing repeated originals. The original full
gate remains exit one; its build and other 16 checks passed while the test
process exited 139. The original 110-pass/one-failure Python run and formatter
limitations remain intact. The diagnostic recovery is a distinct zero-exit
`dotnet test --no-build` invocation. Its seven TRX files contain 7,835 passes,
zero failures and six individual NotExecuted rows, with all 12 selected
source/DLL/configuration identities unchanged. The inconsistent F# aggregate
notExecuted counter is preserved; the six row outcomes and total-minus-executed
are reported separately.

All 47 final-recheck originals verify. The corrected default-temp suite passes
111 tests in 4.86 reported test seconds, strict three-file mypy exits zero, and
the three executed source snapshots match the frozen source. The owner's
separate compiler-139 failure and unchanged successful build retry remain in
the [portability custody](2026-09-08-mixed-message-epoch-bridge-test-portability.md).
Equal exit numbers do not establish a shared cause, and neither failed gate
is relabeled successful.

All five freeze-observation originals verify. Its actual zero exit, 12.508
seconds, helper bytes, argv/environment and returned manifest/archive identities
match this capsule. The two metadata commands are host/interpreter observations;
their returns do not constitute numerical execution.

The [review companion](mixed-message-assembled-admission-review/2026-09-08/manifest.json)
retains both executed read-only audit scripts, actual commands/exits/stdout/stderr,
Git observation identities and selected packet metadata. It references the
coordinator's complete committed archives rather than copying their binaries.
No blocker was found. No project module, plan builder, test, freeze helper,
registered driver, peer or numerical service was invoked by these audits.

This is selected-source/direct-file admission. It establishes neither
transitive runtime closure, originally mapped machine-page identity,
source-to-machine equivalence nor numerical success. Full BridgeResult remains
memory-retained, with the existing durable originals and summaries. Actual
execution failures and missing closure must remain observed outcomes.
