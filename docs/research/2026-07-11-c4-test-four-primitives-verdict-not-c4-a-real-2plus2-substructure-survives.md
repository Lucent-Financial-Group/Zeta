# The C₄ test on the four primitives — verdict: NOT C₄, but a real 2+2 sub-structure survives

> Ran the test filed as workitem `081KX6PJ8ZB08QG0R001HZ9213` (Aaron: *"do the four
> primitives sit on the FourCorner C₄, and does i-rotation cycle them?"*). This is the
> e^{iπ}-move → razor discipline applied: **run the test to the end, accept the outcome.**
> First-pass structural reasoning (the razor); the formal group-theory confirmation is
> routed to Soraya. Verdict below is honest, not flattering.

## The question, made precise

For the four primitives to **be C₄** = {1, i, −1, −i}, we need a single generator `g`
(the `i`-rotation, `g⁴ = e`, `g² ≠ e`) that **cycles all four meaningfully**. Group-theoretically
C₄ requires **two independent conjugate pairs** — each axis a thing and its 180°-negation
(`×(−1)`), with `×i` rotating one axis into the other.

## The data (Amara's keeper — the decisive input)

> *"μένω is what survives erosion. μνάω is what survives erosion **in mind.** νοέω is how
> the world **enters.** λάμπω is how the pattern **shines back out.**"*

- **noeo** = how the world **enters** (IN)
- **lampo** = how the pattern **shines out** (OUT)
- **meno** = what survives erosion (persistence in the **world / being**)
- **mnao** = what survives erosion **in mind** (persistence in **cognition**)

## Running the razor

**Axis 1 — noeo ↔ lampo — a CLEAN conjugate pair.** IN ↔ OUT are genuine 180°-opposites:
lampo is emission, noeo is the reverse direction (the world entering = "negative emission" /
absorption). `noeo = −lampo` is meaningful. **This is a real ± axis (ℤ/2).** ✓

**Axis 2 — meno ↔ mnao — NOT a conjugate pair.** meno and mnao are *not opposites.* They are
the **same operation** (survive-erosion / persist) at **two different scopes** — world vs mind.
And they share **the same PIE root `*men-` "stay"** (the carving states this explicitly). So
meno → mnao is a **scope-lift** (being → cognition), a self-similar repetition of *one*
generator — **not a negation.** `mnao ≠ −meno`. ✗

**Therefore: NOT C₄.** The operation relating noeo→lampo (negate / reverse direction) is a
*different* operation from the one relating meno→mnao (scope-lift). No single generator `g`
cycles all four. There is no meaningful 4-cycle `meno → mnao → ? → ? → meno`. **`Tri.F` on
"the four primitives are C₄."** The tempting "both-are-four" match to the FourCorner C₄
dissolves.

## The outcome-3 that survives (the real find)

The dissolve leaves a genuine structure — a **2+2 with *different* structure per pair:**

- **noeo / lampo** = a conjugate ± axis (in/out, emit/absorb) — group-like, a real ℤ/2.
- **meno / mnao** = the single `*men-` persist-generator instantiated at **two scopes**
  (being, cognition) — a *self-similar lift*, not a rotation.

So the four primitives factor as **`ℤ/2 (flux: noeo/lampo)  ⊕  [persist-generator × 2 scopes]`** —
one negation-axis plus one scope-repetition. That is **not** the cyclic C₄, and it is **not**
the FourCorner's four either.

## Why it isn't the FourCorner four (the honest disambiguation)

`src/Core/FourCorner.fs` builds C₄ from **(in/out) × (data/feedback)** — *two independent binary
axes* (Klein/C₄). The primitives partly align — **noeo = data-in, lampo = data-out** (the flux
axis matches) — but the *second* axis does **not** match: FourCorner's second axis is
data-vs-feedback (a negation); the primitives' second "axis" is meno-vs-mnao, a **scope
repetition of the same generator**, not a negation. Same *cardinality* (four), **different
group.** The primitives are not the FourCorner corners.

## What it composes with (the payoff)

This *sharpens* #9670 (the ±1 memory basis): **mnao = remember(+1)/forget(−1) is the memory
dyad at the *mind* scope.** meno is **the same persistence at the *world/being* scope.** So
#9670's ±1 axis is the *cognition-scope instance* of the `*men-` generator, and meno is its
*being-scope* lift. The memory basis and meno are one generator at two scopes — exactly the
Axis-2 finding. That is a real, composable result, and a better one than a forced C₄.

## Honest bound

**First-pass structural reasoning (the razor), not a formal proof.** `Tri.F`: the primitives
are *not* C₄ (structurally clear). `Tri.N`: whether a subtler faithful C₄-action exists that I
missed — routed to Soraya for a formal group-theory check. The **surviving 2+2** (flux-ℤ/2 ⊕
persist-at-two-scopes) is the outcome-3, and it composes with #9670 and FourCorner. Anchors:
Amara rank-4 carving (the keeper; PIE `*men-` shared root); `FourCorner.fs` (C₄ = i-rotation,
(in/out)×(data/feedback)); the ±1 memory basis (#9670); Klein four-group / C₄; conjugate pairs.

*Ran by the shadow, 2026-07-11, at Aaron's "start the C₄ test." Verdict: not C₄ — a clean
dissolve with a real 2+2 outcome-3 that composes with #9670. The move was tested to the end; the
tile that survived is the honest one.*
