# Mixed-message bridge withdrawal custody

Date: 2026-09-08 UTC
Author: Vera, OpenAI Codex using GPT-6 Astra
Operational status: research-grade retained development evidence
Lifecycle: active

This is the companion to the
[withdrawal implementation report](../../../2026-09-08-mixed-message-epoch-bridge-withdrawal-admission.md).
The [manifest](manifest.json) binds 562 regular originals, 3,651,442 original
bytes, final source `567a9f004cf67b9df6d4a82c55fadaaf4fdd0b9b` and the exact
root clarification import. The lossless [archive](custody.tar.gz) is 647,003
bytes, SHA256
`1860339907378E047EBEA740713CB7493ED9A4EC0AC61567656F86023964297E`.
[observation.json](observation.json) records the author's actual preservation
checks, source comparisons and both repository commands/exits.

Eight original plan-admission controls and two manifest controls failed against
exact `4402be8`. The archive retains those outputs/source/test copies, the
intermediate 11-control pass, both full 111-test attempts, the one test-only
strict typing diagnostic, corrected checks, original root note fetch and the
prior a6ca normal-push/remote proof. Full preflight passed all 18 checks; the
formatter exited zero with its explicit C#/VB support and F# limitations.

The 737-node lstat inventory records 40 symlinks as passive metadata; only
regular files enter the tar. All members and independently re-read originals
matched before publication, without extraction. OriginalLocalFile fields are
historical locators, not instructions. The executed preserver is included; its
redirected stdout is outside the explicit selected roster.

Compensation payloads in these tests are inert plan-admission records, not actual
runtime history or successful replay evidence. The registered actual M4/M5 and
frozen query were not run. Repository gates include existing numerical
unit/model tests. The core's real history/operation admission and the
coordinator's independently registered assembled controls remain separate.
