# Mixed-message bridge correction custody

Date: 2026-09-08 UTC
Author: Vera, OpenAI Codex using GPT-6 Astra
Operational status: research-grade retained development evidence
Lifecycle: active

This is the byte-preservation companion to the
[correction report](../../../2026-09-08-mixed-message-epoch-bridge-corrections.md).
The [manifest](manifest.json) binds 858 regular originals, 4,783,518 original
bytes, the exact three final source files, and both corrected source cuts.
[observation.json](observation.json) retains the author's actual preservation
checks and repository command/exits. The lossless [archive](custody.tar.gz) is
822,952 bytes, SHA256
`9C9C3FCF79E4458CECC98E67B9AABCC32114F2ABDA182381FD2C8270439C197B`.

The archive contains the initial counter-knowledge failures; the separate
intermediate PriorWork failure; corrected 96-fixture/strict/style and full gate;
three original path-admission observations; final 100-fixture/strict/style and
full gate; initial `051d02c` normal-push/remote proof; and the source-only peer
foundation snapshot/review. Original commands, source/test preimages, raw output,
completion records and fixture files are preserved. The source-only peer read
performed no peer or owner-test execution.

The 1,085-node lstat inventory records directories and 43 symlinks as metadata.
Only regular files enter the tar, with no symlink traversal. All tar members and
all independently re-read originals matched before publication. OriginalLocalFile
values are historical locators, not instructions. Read the archive without
extracting it into any source tree. The executed preserver is itself a member;
its redirected stdout was explicitly outside the selected roster.

The repository gates include existing numerical unit/model tests. The registered
M4/M5 routes, frozen nested query and standalone native numerical producer were
not run here. The formatter's zero exits retain its actual C#/VB-only support
and F# limitations. These records are development evidence, not an actual
mixed-message experimental result or source-to-runtime closure proof.
