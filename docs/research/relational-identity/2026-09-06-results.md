# Relational identity: declared-cut invariance and workload accounting

Date: 2026-09-06
Operational status: research-grade
Lifecycle: active
Author: Vera, OpenAI Codex using GPT-6 Astra
Protocol: [2026-09-06 preregistration](2026-09-06-protocol.md), `4f470f40e`
Source: `archive/relational-identity-20260906-source-v4`
Resolved source commit: `159f7b0e65245d4ffae9ecbfe0702b1b7e987b2c`

The one registered deterministic panel completed, and independent Python
regeneration matched its entire typed semantic receipt. This earns a bounded
research receipt-normalization kernel and a stipulated workload model. It does
not establish source non-fungibility, identify physical controllers, price a
Sybil attack, or promote the conceptual Lorentz/CQM/Clifford correspondences.

The strongest retained negative results are constructive. Three pairwise-
independent fair streams can contain only two joint bits. A fabricated complete
graph of four identities can remain internally consistent when every receipt
is admitted by the fixture authentication table and no honest boundary is
present. The same observable transcript can have either of two synthetic
controller labels. Those are limits on the admitted evidence, not failures to
be hidden by selecting another threshold.

## Exact replay and preserved evidence

- [Native receipt](../../../src/Research.FSharp/relational-identity-results.json)
- [Independent replay receipt](../../../src/Research.FSharp/relational-identity-replay-results.json)
- [Frozen native fixtures](../../../src/Research.FSharp/RelationalIdentityExperiment.fs)
- [Independent Python implementation](../../../src/Interp.Python/zeta_interp/relational_identity.py)
- [Pre-result clarification](2026-09-06-clarification.md)
- [Source repair and review history](2026-09-06-source-repairs.md)

The native receipt SHA256 is
`FCD591E93E499175756BCB9E76FC7A7EF04601A5CDB50F10F0E83A0281317D21`.
The replay verified all nine registered source hashes against both the current
checkout and the resolved immutable archive commit. Its structural counts are
288 transports, 18 mutation cases, four entropy cases, 45 topology/cost rows,
and seven baseline rows. Expected values are regenerated from explicit
fixtures; the Python implementation does not load native answers as expected
values. Typed comparison separates booleans from integers and rejects extra
semantic fields. Current-tree regression tests deliberately check historical
semantics without requiring future live source bytes to match the old archive.

The original preregistration was pushed before implementation. Source v1, v2,
v3 and v4 remain on separate immutable refs; no complete result panel ran before
v4 was pushed. The earlier ten native unit tests and targeted defect witness
are disclosed in the repair history. Those unit tests exercised constituent
panels and their expected outcomes; the pre-result chronology is not a claim
that no fixture outcome had been seen. The complete measured JSON panel was
emitted once; subsequent semantic regeneration and corruption tests verify
that saved result. Repairs retain witnessed defects without changing the
registered scientific criteria or selecting new outcomes to improve a score.

## The invariant is a disclosed causal receipt structure

For the declared closed event cut `D`, the preserved `I_D` consists of canonical
actor-bound receipt content, the transitive happens-before relation, and the
consolidated signed claim map. Clock numbers, valid local position charts and
attestation encoding are outside that invariant. Authentication is supplied by
a total capability; the experiment uses an exact actor/content/token table.

All 24 arrival permutations, both valid topological orders of the four-event
diamond, and six clock origin/scale choices preserved `I_D`: 288 checks.
The same 288 cases passed coordinate inverse and composition checks. The
assert/retract pair consolidated to zero while its historical receipts
remained; the second actor's observation remained distinct. The resulting
causal relation contains five ordered event pairs, and the claim map retains
the second actor's vision claim and the first actor's acknowledgment.

The registered functor forgets valid coordinates on charted copies of this
fixed disclosed cut and maps to its finite poset. It preserves identity and
composition within that restricted domain. No spacetime metric, physical boost,
universal category equivalence, or identity-admission rule was constructed.
Every mutation report retains its declared `Expected` cut even when refused.

| Mutation | Observed status |
|---|---|
| Baseline | Consistent on declared cut |
| Identical authenticated replay | Consistent on declared cut |
| Changed payload with old attestation | Authentication rejected |
| Substituted key/token | Authentication rejected |
| Authenticated fork | Authenticated conflict |
| Authenticated causal cycle | Authenticated causal cycle |
| Missing parent receipt | Unknown coverage |
| Omitted expected event | Unknown coverage |
| Unavailable verifier | Unknown coverage |
| Conflict with other coverage missing | Authenticated conflict |
| Different observation channel | Consistent on declared cut |
| Coherent fabricated counterparties | Consistent on declared cut |
| Coherent fabricated complete graph | Consistent on declared cut |
| Unverified alternative to a covered event ID | Unknown coverage |
| Open causal boundary | Unknown coverage |
| Causal coordinate reversal | Coordinates refused |
| Coordinate collision | Coordinates refused |
| Empty cut | Consistent on empty declared cut |

An empty cut makes no claim about undisclosed events. The four-identity
fabricated graph contains six pairs and twelve directed receipts. Every
attestation is authorized by the permissive fixture table. This demonstrates
internally consistent collusion without an honest boundary, not forgery of an
honest actor's signature. An actual contradiction takes priority over missing
coverage; missing or unverified evidence alone is not an authenticated conflict.

## Additive entropy requires conditional innovation

For the registered `Z` constant and identity order `(X1,X2,X3)`, exact finite
state enumeration produced:

| Fixture | Maximum joint probability | Joint min-entropy | Pairwise independent | All three one-bit conditional bounds |
|---|---:|---:|---|---|
| Independent fair bits | `1/8` | 3 bits | Yes | Hold |
| Copies | `1/2` | 1 bit | No | Fail |
| Inversion | `1/2` | 1 bit | No | Fail |
| `(U,V,U XOR V)` | `1/4` | 2 bits | Yes | Fail |

The additive theorem uses a pointwise conditional bound for each innovation,
including conditioning on all earlier innovations and adversary information
`Z`. Multiplying those conditional probabilities establishes the sum of the
assumed entropy floors. Mere unconditional independence from other identities
is insufficient when `Z` reveals the bits. Physical source ownership and
non-fungibility remain separate assumptions, and public disclosure can reduce
remaining secret entropy.

## Pairwise work multiplies the baseline only as accounting

The registered units are stipulated work units, with `b=32` per identity and
`q=8` per required pair. They are separate from entropy bits. The exact identity
is `B=sum b_i`, `R=sum r_e`, `M=1+R/B`, and `C_model=B*M=B+R` for `B>0`.
The empty identity set has zero work and an undefined multiplier. Positive
`q` is not an observed adversarial lower bound.

All 45 graph rows passed relabeling and duplicate-edge idempotence. At 64
identities the five topologies give:

| Graph | Required pairs | `B` | `R` | `M` | `C_model` |
|---|---:|---:|---:|---:|---:|
| Empty | 0 | 2048 | 0 | `1` | 2048 |
| Complete | 2016 | 2048 | 16128 | `71/8` | 18176 |
| Path | 63 | 2048 | 504 | `319/256` | 2552 |
| Star | 63 | 2048 | 504 | `319/256` | 2552 |
| Cycle | 64 | 2048 | 512 | `5/4` | 2560 |

One required pair was withheld wherever an edge existed; every corresponding
row reports one missing pair while keeping required work separate from observed
coverage. Complete graphs make the stipulated relational term quadratic;
bounded-degree graphs make it linear. Neither calculation proves that coherent
histories require independently paid work, and no probability product or
cubic double-counting is introduced.

## Existing baselines expose channel limits

The actual `AntiSybil.correlation` at threshold `0.8` flags exact and inverted
replay with correlation `1`. The deterministic shared-seed orthogonal fixture
has correlation `0` and is unflagged. The deliberately identical finite sample
under an independent-controller label is flagged. These are synthetic witness
labels, not estimated false-positive or false-negative rates.

The distinct-controller and one-controller transcript rows have identical
observable streams, receipts, attestation-table results and raw CHSH lines.
Only their synthetic hidden controller labels differ. This establishes
observational non-identifiability within these inputs; it measures neither
physical controllers nor operational attack cost.

All seven signature-only rows are table-accepted and all seven receipt cuts
are internally consistent. Six raw symmetric CHSH lines equal `2`; the
constructed conducted-context fixture equals `4`. That last fixture contains
the cross-setting dependence explicitly; it supplies no Bell-locality or
quantum-evidence claim. The statistical, attestation and receipt inputs are
different channels, so this panel earns no cross-method detector ranking.

## Review findings and validation

Independent review found and preserved corrections before the complete panel:
arrival-dependent fork boundary diagnostics, forks counted as identical
replays, underspecified Actor binding and parent canonicalization, local
position versus clock origins, unavailable alternatives hidden by event-ID
coverage, missing declared cuts in JSON, ignored historical-replay options,
archive provenance gaps, and permissive boolean/integer equality. The source
repair record gives chronology, the first defect witness, and unchanged refs.
Eleven native facts and the independent replay corruption/snapshot tests retain
the relevant falsifiers. The entropy wording clarification makes explicit the
conditional-on-`Z` premise already required by the theorem.

Validation on source v4 completed:

- [Release solution build](validation/source-v4-release-build.txt): zero warnings
  and zero errors, 61.87 seconds. The loaded Core artifact is identified below.
- [Full .NET suite](validation/source-v4-dotnet-tests.txt): 7,468 passed, zero
  failed, six existing skipped tests; the main F# assembly completed in
  5 minutes 32 seconds, including the eleven new native facts.
- [Full Python suite](validation/source-v4-python-tests.txt): 89 passed, with
  one existing `HookedTransformer` deprecation warning in activation access.
- [Formatter verification](validation/source-v4-format.txt): exit zero. The
  tool reports that it supports C#/VB and does not format F# projects; the
  repository's F# lint separately passed in the sixteen-check quick preflight.
- All sixteen quick-preflight checks passed before each source archive push.
  Python Ruff/typechecking, markdown lint and diff hygiene also passed.

An earlier unchanged Core build exited with `MSB6006`, process code 139.
An [isolated retry](validation/source-v1-core-build-retry.txt) and subsequent
full Release solution build passed without a source change. It is retained as
a transient tool failure, not evidence about receipt correctness. No runtime
membership or cryptographic production surface changed.

## Engineering measurement and next promotion boundary

One cold panel on .NET 10.0.11 / macOS 26.6.2 recorded 109.2809 ms elapsed,
109.29 ms process CPU and 33,845,400 current-thread allocated bytes. Other
repository validation was running on the same host. This is a non-isolated
engineering observation, not a benchmark, retained heap measurement, energy
measurement, or calibration of the stipulated workload costs.

A fresh Release solution build preceded the panel. The loaded Core assembly
SHA256 was `16F07C903545DB71754BACEF9B4A8F4A100FFD3EF55E1D3DE020B9F451F46D82`,
with MVID `d6e49fb4-a752-9bc9-381a-5cc97f4bc739`. These identify the actual
loaded artifact alongside the build record; they do not prove reproducible
source-to-binary derivation.

The preregistered stop rule is reached. The next useful experiment would need a
separately registered honest-boundary/adaptive-disclosure adversary model,
an authenticated receipt capability grounded in approved cryptography, and
observable work or conditional-entropy assumptions. Physical Lorentz claims
would additionally require a specified metric and tested boost action. None
of those promotions follows from this normalization panel.
