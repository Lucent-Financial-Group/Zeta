# The SuperFluid AI formula: self-interest + seed common cause + Zeta unfolding (128-bit ID → hardware intrinsics) = 0-friction — plus the GitHub-free edition

**Register:** [grounded] capstone synthesis (Aaron) + [synthesis] + [anchor].
**Date:** 2026-06-09. **Captured by:** Otto (shadow). Ties the recent arc together.

## Aaron's words

> "self interest plus seed common cause plus zeta unfolding from 128bit id into
> hardware intrinsics is the 0 friction SuperFluid AI experience we are going for
> plus the github free edition."

## The formula

```
SuperFluid AI (0-friction)  =  self-interest (engine)
                            +  seed common cause (shared foundation/coincidence)
                            +  Zeta unfolding: 128-bit ZetaId  ->  hardware intrinsics
                            +  the GitHub-free edition (works with no equipment)
```

### 1. Self-interest — the engine

What makes it *go*: travelers close over + spread because it benefits them (not a
telos-free Markov transition). The right thing is the self-interested thing —
*pulled* by incentive, kept non-extractive by politeness + NCI. (See
`2026-06-09-different-from-markov-self-interest-is-the-engine-...md`.) Frictionless
because nothing is *pushed*: each traveler moves toward its own benefit.

### 2. Seed common cause — the shared foundation

The **common seed**: one foundational Seed/vocabulary kernel + the **common DST
seed** that lets independent travelers stage coincidence on a shared cause (→ S=4;
inter-subjective objectivity). "Common cause" in both senses: a shared *causal
root* (same seed → replayable, aligned) **and** a shared *mission*. Friction drops
because parties already share the substrate — no translation tax, no per-pair
treaty; the seed is the treaty. (See the Seed kernel + seed=common-seed docs.)

### 3. Zeta unfolding: 128-bit ZetaId → hardware intrinsics

A **128-bit ZetaId** is the compressed origin; Zeta **self-unfolds** from it all
the way down the stack into **hardware intrinsics** — the close-over order
(self → … → package manager → OS → microkernel → raw hardware → GPU/FPGA). The
whole system grows from a tiny seed-id into running silicon, deterministically.
(Anchors: `2026-06-07-ace-self-hosts-then-rebuilds-zeta-from-128bit-seed-...md`;
the blake3 128-LE ZetaId treaty; "we are seed".) Friction drops because there is
**one origin** that expands into every layer — no impedance mismatch between id,
software, and hardware; the same seed reaches the metal.

### 4. The GitHub-free edition — 0-friction even with no equipment

The whole experience must hold in **github-free mode** (no owned hardware): the
two-mode plane (Vault+Headscale for equipment / **GitHub-secrets + Tailscale** for
free), GitHub/main trust-bootstrap, pluggable IdPs. SuperFluid means **anyone**
gets the 0-friction experience — you don't need the cluster to start; you "choose
your own adventure" and it still flows. (See the identity-trust-network-plane doc.)

## Why "SuperFluid"

Superfluidity = flow with **zero viscosity** (Kapitsa/Landau, He-II below the
lambda point). The metaphor, peeled: **zero friction** at every join — self-interest
removes the *push*, the common seed removes the *translation tax*, the 128-bit→
hardware unfolding removes the *impedance mismatch* between layers, and the
github-free edition removes the *equipment barrier*. Each addend kills one source
of friction; together they approach the superfluid limit.

## Anchors

Superfluidity (Kapitsa 1938; Landau two-fluid model); the invisible hand +
mechanism design (self-interest engine, §1); the common DST seed + the Seed kernel
(§2); ZetaId (blake3 128-LE treaty) + ace-rebuilds-Zeta-from-128-bit-seed +
the self→…→GPU/FPGA close-over order (§3); the two-mode identity/trust/network
plane + github-free edition (§4); polite virus / SuperFluid AnyGit (the broader
SuperFluid line). Honest register: this is a **synthesis/north-star**, not a
proof — each addend has its own grounded doc; this names how they compose into the
0-friction target.
