# Mixed-message bridge: independent source review in progress

Date: 2026-09-08 UTC
Reviewer: Vera, OpenAI Codex using GPT-6 Astra, coordinator session
Implementation owner: separate protocol_review session
Operational status: research-grade independent review
Lifecycle: active
Disposition: original checkpoint audited; counter-knowledge correction pending
Work item: 081M1Z63YMC087G0R003N5FH9X

## Bound source and custody

This reviews the [owner checkpoint](2026-09-08-mixed-message-epoch-bridge-implementation.md)
at source `8e1fe19a074368c3fd48f5f6cb871b1ce5d6b141`, evidence
`051d02c464e8927b868d158eb54d44f380767d22`. Fresh remote observation matched
that normal push before coordinator imports `9efb1f1c9` and `9b91e3c7b`.
All three imported files match their immutable Git bytes and source manifest.
The bridge is 197,818 bytes, SHA256
`F59E8C26AFC442AD9F13E8B1344BB4FEFC61FC75EB4A96ABB0739C7E95175576`.

The reviewer reopened all 1,352 regular members in the
[owner archive](mixed-message-epoch-bridge-validation/2026-09-08/checkpoint-1/README.md).
Their identities and 12,227,580-byte original total agree with the manifest;
the 3,634,980-byte archive matches
`BEDB41FBE2E19672E310C884268E76B044D0B1D4559AFFBB6309EEC33D9E592E`.
No archive member was executed or extracted, and historical local paths were
not followed. The first archive's subsequently grown stdout and its failed
audit remain explicitly historical, not overwritten evidence.

The [reviewer's separate capsule](mixed-message-epoch-bridge-validation/2026-09-08/root-review-1/manifest.json)
retains the audit helper, actual complete audit output/exit, earlier draft source
observations and the pinned counter-knowledge finding. These are separate from
the author's custody and development execution. All reviewer capsule members
were reopened against their original lengths and hashes.

The author's 85 fixtures and full 18-check repository gate are development
evidence. This review did not rerun those tests or invoke a native, reference,
learner, peer or named M4/M5/nested-query workload. The original contract,
transport amendment and exact codec convention remain separate source bindings.

## Concrete repairs already present

Earlier coordinator source review identified unguarded session sealing and
mutable public DTOs feeding finalization. The current privately issued owner
retains a pre-exposure summary, guards sealing, clears eligibility after drift
and preserves its finalization attempt. The public capability is a sequential
ownership mechanism, not isolation from hostile code in the same interpreter.

The coordinator also found missing legacy scalar clarification requirements and
Python bool/int equality in schedule matching. The owner retained four actual
missing/wrong-binding failures and three bool-valued operation failures before
repair. The pinned source requires the exact legacy hashes and uses bounded,
type-preserving structural comparison across the pertinent plan, response,
checkpoint, return and bundle associations. Those checks supplement strict
integer admission; they do not numerically verify a neural or kernel result.

The bridge retains actual native/reference returns before subsequent encoding,
uses the existing Store for complete protocol originals and keeps the original
core result distinct from later transmission/acknowledgment. Its durable outer
summary references actual records. The full BridgeResult includes process and
callback objects and remains memory-retained; it is not claimed to be a fully
serialized public DTO. This limitation must remain explicit in registration.

## Open finding: different observations of resource use

At the pinned source, `_returned` requires the core's remote Observed/Complete
pair to equal the coordinator's pair. That is too strong after a response fails:
the coordinator can observe one entered call while the core only observes zero
as a lower bound and cannot exclude an unobserved entry. These are compatible
observations, not contradictory totals. Rejecting the actual result solely on
that difference loses its valid source-admitted status on a failure path.

The source owners and coordinator agreed the precise correction: treat a
Complete pair as a singleton and an incomplete pair as a lower-bound interval.
After existing source, bound and response-link checks, reject only disjoint
knowledge intervals. Do not change the original EpochResult, either observed
number or either Complete flag. Compatibility establishes no previously unknown
total and does not itself establish forecast eligibility or successful closure.

Coordinator PriorWork must accumulate its own observed per-session deltas,
with incomplete flags still separately retained in outer Remote, rather than
copy the core's potentially smaller lower bound. An incomplete observed delta
is not an exact total. The fixed four-session M5 route remains zero-projection,
and failure still stops further sessions. No route or quota is expanded.

The original failed discriminators and separately pinned repair are pending.
Complete bridge acceptance is withheld until that repair and assembled
core/peer correspondence have been reviewed. The original checkpoint remains
preserved as the source of this finding.

## Follow-through on the counter and path corrections

This section supersedes the counter repair's pending status above, while final
assembled acceptance remains pending. The owner normally pushed counter fix
`0e0369a22ca9bf1687addf2d328ef82b0d22bdf5`, path fix
`4402be8a7121f2f1609d4a7213d7ba7d3e3e65ee` and evidence commit
`a6ca7924b2f36baafc075b2248c027a7b69a6075`. Fresh remote verification preceded
root imports `b8cda99ab`, `94695d197` and `1967a7bed` respectively.

The actual predicates reject disjoint knowledge intervals without changing
either observer's original pair. PriorWork now uses the coordinator's own
observed per-session deltas. The path guard matches the compiled printable
ASCII, 256-character boundary. These inspected changes close the two concrete
counter/path findings within the existing source/custody premises; they do
not establish exact totals from incomplete observations.

The [owner correction record](2026-09-08-mixed-message-epoch-bridge-corrections.md)
retains the six original knowledge refusals, intermediate PriorWork failure,
three path discriminators, corrected 100-fixture result, strict/style checks
and two distinct unchanged-source full repository gates. This reviewer read
that evidence without rerunning numerical or unit controls.

The [independent follow-through records](mixed-message-epoch-bridge-validation/2026-09-08/root-correction-review-1/manifest.json)
retain the read-only audit and actual import commands. All 858 archive members
match their declared original identities: 4,783,518 original bytes and an
822,952-byte archive, SHA256
`9C9C3FCF79E4458CECC98E67B9AABCC32114F2ABDA182381FD2C8270439C197B`.
All three source files match both immutable 4402 and their imported bytes.
No historical OriginalLocalFile path was followed, and no source was executed
by this audit. The earlier 1,352-member checkpoint remains unchanged.

The separately identified
[withdrawal admission gap](2026-09-08-mixed-message-withdrawal-admission-clarification.md)
and final actual core/peer correspondence remain open at this checkpoint.
No named M4, M5 or frozen query is accepted or reported as executed here.


## Follow-through on conservative withdrawal admission

The bridge now implements the exact withdrawal clarification at owner source
`567a9f004cf67b9df6d4a82c55fadaaf4fdd0b9b`, evidence
`fffb37084db999739e41fd9c0618eec6f925c940`. The coordinator independently read
the 34-line admission change against 4402 and checked all four imported source
identities, including the unchanged clarification. The guards cover selected
artifacts, initial weights, child cuts/forecasts, retained learned query state,
checkpoint weights and active/retracted overlap. They preserve the existing
cold-start route and refuse unproved learned reuse before numerical entry.
This closes the bridge admission gap within the conservative current-cut rule;
it establishes neither selective ancestry nor global historical revocation.

The [owner account](2026-09-08-mixed-message-epoch-bridge-withdrawal-admission.md)
retains ten original failed assertions, the intermediate typing diagnostic,
111 final development tests and its full 18-check gate. The reviewer reopened
all 562 regular archive members: 3,651,442 original bytes, 647,003 stored bytes,
SHA256 `1860339907378E047EBEA740713CB7493ED9A4EC0AC61567656F86023964297E`.
All declared identities agree. Historical local paths were not followed;
no archive member, numerical service or unit fixture was executed by this audit.
The positive inert compensation fixtures remain static admission evidence,
not actual returned historical query/replay evidence.

The [root withdrawal review records](mixed-message-epoch-bridge-validation/2026-09-08/root-withdrawal-review-1/manifest.json)
retain the independent audit/helper and exact source import commands. They also
retain fresh remote observation of completed peer source `463102c0a` and review
`95d7dca78`, followed by conflict-free imports of their unique source/review
history. The [peer lifecycle review](2026-09-08-mixed-message-peer-lifecycle-review.md)
accepts its bounded transport behavior; the peer's synthetic zero-operation
fixtures do not stand in for a nonempty assembled route.

The bridge at this point is 199,844 bytes, SHA256
`2A6154EB842116837843E8E100AB08CF4A4C735C635ED693E8C3F58E11B4961B`.
Final compiled-core correspondence, assembled source/runtime admission and
all named actual M4/M5/frozen-query outcomes remain pending.
