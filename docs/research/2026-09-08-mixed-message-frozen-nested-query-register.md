# Frozen nested query after the fixed child-parent learning control

Date: 2026-09-08 UTC
Author: Vera, OpenAI Codex using GPT-6 Astra
Operational status: research-grade control preregistration
Lifecycle: active
Status: input and route fixed; assembled source admission and execution pending
Work item: 081M1Z63YMC087G0R003N5FH9X

## Purpose and dependency

This is the one frozen nested query required by the
[implementation register](2026-09-08-mixed-message-epoch-implementation-register.md).
It checks that two actually learned modules compose through a declared DAG edge
and explicit composite output alias without another fit. It is an implementation
control, not a held-out accuracy measurement or comparison with a published learner.

First complete the unchanged four-session M5 route in the
[transport amendment](2026-09-08-mixed-message-epoch-transport-amendment.md).
Both training results must be actually received, source-admitted and successfully
closed. No fabricated artifact, recomputed replacement return, hash-only loading
or separate training run may fill a missing dependency. A failed prerequisite
is retained and this query is recorded as not entered.

M5 remains eight SGD attempts, ten neural forwards, four peer sessions and zero
projections. This query is separately named and counted; it is not a fifth
session in that outer ledger. Freeze and independently admit the assembled
source, runtime, service identities and control invocation before execution.
No actual M4, M5 or nested query execution is reported by this registration.

## Exact target-hidden row

Use this row, deriving ContentSha256 from its canonical record before adding
that hash field under the accepted
[identity/codec conventions](2026-09-08-mixed-message-epoch-identity-codec-conventions.md):

```json
{
  "Id": "control/frozen-nested/query",
  "Origin": 8,
  "FeatureAvailable": 8,
  "TargetTime": 9,
  "LabelAvailable": 9,
  "Split": "control",
  "Features": [
    "3FE8000000000000", "0000000000000000",
    "0000000000000000", "0000000000000000",
    "0000000000000000", "0000000000000000",
    "0000000000000000", "0000000000000000"
  ],
  "Target": null,
  "Uses": []
}
```

The first raw feature is exactly 0.75; the other seven are positive zero.
Horizon is one. Child and parent training cut ends, three and seven, both
precede this origin. LabelAvailable is present as required by the closed row
schema; its target value is absent. Do not score against an imagined target.

## Exact composition and invocation

Use the inert `frozen_nested_query_plan` builder in
`src/Interp.Python/zeta_interp/mixed_message_epoch_controls.py` with the actual
M5 child/parent training SessionResults and the row above. Bind the builder in
the final source archive. Building a plan enters no learner, peer, scheduler,
recorder or projection. Admit the complete generated plan before peer launch.

The plan is `control/m5/frozen-nested`, mode query, one sweep. Its neural nodes
are `control/child` and `control/parent`; composite `control/nested` has empty
Inputs, both as Children and OutputAlias selecting the parent's mean. The
parent's only edge takes the child's mean into slot zero; slot one is absent.
The alias adds no likelihood, prior, fitted parameter or numerical operation.

SelectedVersions supplies both complete actual artifacts and canonical version
hashes. The cut contains the four original training rows and the hidden row,
with only the hidden row active and the actual parent training cut as ParentCut.
Initial state is empty at revision zero with that active cut. Operations are
exactly NeuralForward(child), then NeuralForward(parent), at sweep zero.
Training is null. No ForecastSource is supplied to this nontraining route;
the same-owner restriction on training forecast reuse remains unchanged.

Open a separate single bridge attempt named `frozen-nested-1` with the same
admitted bindings and complete first plan. Enter it once, then finalize once.
Keep the existing caps: one peer, 300-second cooperative deadline, 64 MiB
transcript, 16,384 frames, 256 MiB combined Store allowance, 4,096 artifact
slots and unchanged terminal reserves. Planned projections are zero: external
native D/A reservation is zero, with no native preparation or native/reference
service. Expected numerical work is two scheduler entries and two neural
forwards, zero SGD attempts, zero projection requests and zero fitted artifacts.
Observe actual counters, bytes, failures and closure; do not substitute targets
for observations.

## Discriminators and outcome

Stored child and parent scalers map the first feature to exactly six and zero,
respectively; the other seven transformed features remain zero. These follow
from the fixed two-row means and population standard deviations: child mean
-0.75 and scale 0.25, parent mean 0.75 and scale 0.25. Do not refit on the query.
The child uses two absent ports. The parent uses the actual committed child
mean and the slot-zero presence bit, with exact output bits and SourceSequence.
An independently rerun forward pass cannot supply the observation.

Retain actual forward inputs/returns, checkpoints, acknowledged storage,
successful Commit observations, complete EpochReturn and closure. The parent
consumes only the committed child output from this query. Resolve the alias to
the parent's actual mean; do not invent a third forward or output commit.
Both selected artifact byte identities remain unchanged, with no proposed
training artifact. An out-of-bound learned output is a retained refusal, not a
reason to change this row, clip a value or enlarge a quota.

Record failed discriminators and incomplete publication as actual outcomes.
A necessary repair receives a separately named source and attempt while the
first attempt stays preserved. Report the four-session M5 work and this query's
new work separately and together. Historical training is neither erased from
total cost nor charged as another actual fit in this query.
