# First assembled source and direct-runtime capsule

Date: 2026-09-08 UTC
Author: Vera, OpenAI Codex using GPT-6 Astra
Operational status: research-grade source registration
Lifecycle: active
Status: frozen; independent assembled admission pending; no named control run

Source commit: `3c3a76ad6674bb40b2e1309021735950c282d8d5`.
The [service manifest](service-manifest.json) is 22,381 bytes, SHA256
`F92041F0DBC344DA877DFA970B3C5C7AD6817C8285AAA28E164872F2CE6D56F1`.
It binds 65 committed source files, seven direct files and exactly 69 flat
source/protocol bindings. Its exact independent SHA must be supplied to the
fixed launcher. The direct Python and dotnet executables are observed files,
not a transitive runtime closure or proof of originally mapped process pages.

The [archive manifest](archive-manifest.json) binds 298 regular members in
`custody.tar.gz`: 39,770,169 bytes, SHA256
`E51A00FC29FA2A13B09F152AB0F55908B7E03982036E5084F54A221815149445`.
All members were reopened against their original identities. Source bytes match
Git at the stated commit. Both scripts appear as source and direct roles with
the same bytes. The capsule also retains actual Git source observations, both
metadata-command returns, direct-after bytes/identities, and the exact freeze
and capture helpers. Repeated equal bytes do not become independent evidence.

The [source custody](source-custody.json) names original direct-file locations
and their corresponding members. The [freeze observation](freeze-observation/manifest.json)
retains original argv, source helper, stdout/stderr, timing and exit. The actual
freeze exited 0 in 12.508 seconds. Passive manifest admission returned
ServiceManifest. Metadata commands were dotnet --info and Python interpreter
metadata; no peer, native numerical producer, learner, reference or certificate
was entered. Earlier helper failures/fixtures remain separately indexed in the
[observation review](../../../2026-09-08-mixed-message-observation-helper-review.md).

## Fixed invocation and independent admission

The archived `run-registered.py` is 6,936 bytes, SHA256
`FBD0DF785D5AECAE47591D73EA3062050825F33241880B67668B462ADDAC2462`.
Its positional arguments are the fixed mode, independently accepted manifest
SHA256 and independently accepted archive SHA256. It verifies those identities
and its own archived member before the actual launcher is entered. Reading an
expected hash solely from the same mutable source directory is insufficient.

The actual launcher remains the selected `registered-source/invoke.py` at
CC7FBF89, with the [fixed invocation record](../registered-source/README.md).
Use mode `m4` first for exactly `m4-registered-1`. A capture/launcher exit zero
means closure, not a numerical verdict: inspect the two actual projection
outcomes and committed state against M4 before separately choosing mode
`m5-and-frozen-nested`. That mode fixes four dependent M5 sessions, then a
separate frozen query only if its actual work/custody prerequisites hold.
The source contract, budgets, inputs and first-attempt names are unchanged.

The core/peer/bridge source reviews, original validation failures and passing
recovery checks remain in the [assembled validation record](../../../2026-09-08-mixed-message-assembled-validation.md).
The two observed exit-139 events remain unresolved; successful recovery does
not reassign them to passing invocations. Independent capsule admission is
still required before the first actual registered controls. A changed source
or corrected attempt needs its own explicit identity and preserved predecessor.
