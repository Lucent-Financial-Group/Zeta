# Mixed-message assembled helpers: source and correction review

Date: 2026-09-08 UTC
Reviewer: Vera, OpenAI Codex using GPT-6 Astra
Operational status: research-grade independent source review
Lifecycle: active
Disposition: bounded helper corrections accepted; final assembled capsule pending
Work item: 081M1Z63YMC087G0R003N5FH9X

This pass reads the coordinator's proposed finite freeze and capture helpers
and fixed invocation source at `50610f191`. It executed none of them and entered
no registered, peer, core or numerical operation. Initial source bytes and their
identities are [preserved here](mixed-message-assembled-helper-review/2026-09-08/initial/manifest.json).
These are static findings, not claims of experimentally observed failures.

The inspected freeze helper is 6,213 bytes, SHA256
`E77ED4272FC1D23803A5F92777B1E2346AC22CCDDC3AC9FF6323D0C1592DD918`.
It holds selected source/direct bytes and metadata subprocess outcomes in a
memory dictionary until final archive publication. A metadata subprocess's
nonzero exit or later identity recheck failure raises before those already
observed prefixes reach that archive. Its direct-file read also occurs before
regular-file/size admission. A corrected finite observer should preserve each
actual observation before subsequent fallible work and check a regular bounded
file before reading it. Failed prefixes must remain distinguishable from a
completed freeze.

The inspected registered capture helper is 3,168 bytes, SHA256
`D7981095EDD315E15CD376F217D39518E7C78AF31846815066611CF9EAAC17DA`.
It has no guarded launch/wait completion boundary: a launch failure produces
no completion record. A later source snapshot exception prevents the remaining
inventory/summary. On its ordinary path, it prints the actual child exit and
after-source comparison but the helper itself still exits zero after a nonzero
child result or identity mismatch. Preserve the actual first outcome and any
secondary capture failure separately, complete independent retention attempts,
and propagate invocation/capture failure after that retention. This does not
turn capture exit zero into a numerical-control verdict.

The same helper reads both manifest bytes and their expected hash from its
local frozen directory. That establishes local self-consistency, not equality
to the independently reviewed capsule. Final invocation must bind the accepted
manifest/capsule identity explicitly, through a caller-supplied value or a
retained independent check immediately before entry. The helper bytes themselves
also need the final capsule binding. No runtime wire field needs to change.

The fixed invoke.py is 11,511 bytes, SHA256
`CC7FBF89E681909374591516B4227F3D93373E9B5F089C039994B0CA46055E69`;
its full immutable commit is recorded in the companion manifest. No additional
concrete invocation-source defect was established in this pass. It uses the
existing observed bridge calls and once-only finalization, requires actual M5
closure and the fixed work/custody prerequisites before the separate frozen
query, and keeps full actual return objects in memory. Durable summaries refer
to the existing originals. Its transport/control exit deliberately leaves the
numerical verdict for the coordinator's later retained-result analysis.

The coordinator was notified before any registered execution. Corrected helper
source, immutable source/runtime capsule, exact independent manifest admission
and named invocation authorization remain separate requirements. This note is
not final assembled admission and does not reassign earlier gates to later
source or helpers.

## Corrected source and actual observation fixtures

The final accepted capture helper is 6,936 bytes, SHA256
`FBD0DF785D5AECAE47591D73EA3062050825F33241880B67668B462ADDAC2462`.
It requires independently supplied manifest and capsule hashes and checks the
exact helper and manifest members in that capsule. It retains child exit
separately from later observation failures, records per-file identity results
and per-item inventory errors, and returns exit two for failed invocation or
custody. Four coordinator fixtures retain child exit seven, a raised launch,
an after-read failure following child exit zero, and an unaccepted manifest
that invokes no child. All process returns in those fixtures are synthetic.
The reviewer read their source and retained results without invoking them.

The intermediate freeze is 8,506 bytes, SHA256
`D6C6B6BF9CB8599AC68BAA6458096E93D11C1EB5136193B0B86F4B62E352319A`.
It adds bounded regular-file reads and publishes observed raw prefixes before
later work. Its late metadata refusal retains 178 physical raw/descriptor
pairs with 176 unique logical names; source/direct script observations repeat
two names. The FIFO refusal retains two pairs and enters no subprocess.
All 180 identities were independently re-read and checked. The fixture's
dictionary reports unique names, not the complete persisted observation count.

The coordinator then found an additional prefix gap: an actual second direct
read was judged against the source copy before its changed bytes were retained,
and after-reads retained identity only. The original intermediate source and
results remain in the [intermediate companion](mixed-message-assembled-helper-review/2026-09-08/intermediate/manifest.json).
This is an author-found source correction, distinct from the three initial
review findings.

The final freeze is 8,553 bytes, SHA256
`0DBFF44E506F9EF175318F10018C340A0CDEB2B2F70602518DCFE47B92DD2970`.
It retains second direct-read bytes before correspondence judgment and each
after-read raw value before identity judgment. Its two repeated fixtures and
one new direct-mismatch fixture preserve 185, two and 171 physical pairs,
respectively, with 183, two and 171 unique names. All 358 stored identities
verify, including the mismatching actual direct bytes. The last result has one
case although its copied Scope string says two; that original string is
preserved, and this review uses the actual one-case roster.

The [corrected companion](mixed-message-assembled-helper-review/2026-09-08/corrected/manifest.json)
binds the final source, fixture scripts/results and the actual zero-exit
read-only reviewer audit. It is a selected review companion, not a second
complete copy of the coordinator's fixture archive. These checks use synthetic
subprocess returns, with passive manifest acceptance injected in freeze tests;
the source/host file reads themselves include actual regular-file reads.
No helper, fixture, registered driver or numerical operation was invoked in
the independent review.

No additional blocking defect was established in this bounded helper pass.
Ordinary filesystem/console failure can still prevent complete publication;
this is not a crash-proof recording framework. The accepted source hashes
must appear in the separately reviewed immutable capsule before named entry.
Helper exit zero remains an observation result, not a numerical verdict or a
source-to-machine proof.
