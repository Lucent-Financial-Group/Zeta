# Checked mixed-message core: model and compensation controls

Date: 2026-09-08 UTC
Author: Vera, OpenAI Codex using GPT-6 Astra
Operational status: research-grade source implementation
Lifecycle: active
Work item: 081M1Z63YMC087G0R003N5FH9X

## Scope and source boundary

This extends the [runtime checkpoint](2026-09-08-mixed-message-core-runtime-checkpoint.md)
at `15b43aa7271d100842c198a4378eef717e4acef8`. It closes the owner's
local M1/M2/M3/M6/M7/M8 controls for independent source review. The actual
named M4 service route, composed four-session M5 route and registered frozen
nested query remain closed in this writer. No native process, Python interval
reference, certificate service, new data or registered stream ran here.

The [accepted contract](2026-09-08-checked-mixed-message-module-epoch-source-contract.md),
[transport amendment](2026-09-08-mixed-message-epoch-transport-amendment.md) and
[identity conventions](2026-09-08-mixed-message-epoch-identity-codec-conventions.md)
remain unchanged. The exact root-authored
[withdrawal clarification](2026-09-08-mixed-message-withdrawal-admission-clarification.md)
is imported from `87bc6b28aa1d0ef780accf592b99ce70a16283e6`, 4448 bytes,
SHA256 `99576A0F9113839905463FF43CB35FF224524EDF8C207F38C7089984E7914DEE`.
This is an explicit conservative admission change, not a claim that full
training ancestry was already available.

Only `MixedMessageEpoch.fs` and its dedicated test change beyond the runtime
checkpoint. The learner, learner test and project wiring remain byte-identical.
The compiled public DTO/service/recorder signatures do not change. Internal
friend controls invoke the same checked cavity, Gamma and projected-site
functions used by the runtime; their isolated calls are not epoch counters.
The implementation calls checked `PrecisionGateKernels` and the actual
`SoftScheduler.drive`. It does not call `FactorGraph`; the typed site ledger
implements its own other-factor exclusion and keyed replacement laws.

## Discriminating local controls

M1 excludes the receiving factor from the cavity, adds the independently
owned prior once and replaces a keyed contribution instead of accumulating
duplicate delivery. Changed row content with a stale content hash, duplicate
prior owners and a model factor relabeled with a prior contribution ID refuse.

M2 uses the current Gaussian marginal, including its unary site. The fixed
mean 2, variance 3 and clamped child mean -1 give residual second moment 12;
the wrong cavity calculation gives 2. The Gamma message has log power 1/2
and rate 6; prior shape 2/rate 3 yields posterior shape 5/2/rate 9.
A lone declared slot 1 remains `normal/1` and `gamma/1`. The Exp/Log reverse
kernel orientation discriminator, ordinary shape encoding and the small
requested shape `1e-16` are retained.

M3 forms a unary site by checked quotient of the projected belief by its
Gaussian base. At the analytic fixture, projected natural parameters are
(-1/2,2), base (3/2,1) and new site (-2,1). Multiplying the projected belief
as a site would count the base twice. An improper site may be admitted inside
a proper combined belief; an improper combination refuses. Invalid damping
refuses before numerical entry. The undamped certificate does not certify the
damped application as the same local minimizer.

M6 executes two actual local query epochs with inert, explicitly untrained
point-module artifacts: flat child/parent and the same computational nodes
connected through declared composite aliases. Both complete EpochResults are
retained. Their operational observations and complete numerical state encodings
match byte-for-byte; distinct plan identities and actual transport values are
retained. A separate pair of actual Gamma block calls has nonempty Gaussian
and Gamma sites: site state bytes and all primitive input/result bytes match
under the declared alias resolution. Required child omission and stale artifact
identity refuse. An explicitly added second Normal factor changes the plan and
model contribution roster; it is not mislabeled an exact refactor. There is no
new guard pretending that distinct producer IDs establish independence.

M7 executes an applied point-query history, direct restoration and compensating
replay through the actual scheduler. A fresh replay from the restored checkpoint
matches numerical state exactly. Its output sequence differs by the explicitly
accounted compensation checkpoint; this provenance difference is retained.
The original result bytes remain unchanged. Unapplied proposals, inverse SGD
and inherited work beyond the fixed budget refuse. Supplied histories lacking
successful admission, a returned operation or a successful operation outcome
cannot claim an applied revision.

Current-cut withdrawals now forbid selected learned artifacts and retained
weights before numerical entry. Retained compensation plans/checkpoints cannot
hide learned reuse. Training under withdrawal rejects inherited ChildCuts and
ChildForecasts; active/retracted overlap refuses. A cold-start training control
on active nonretracted rows completes from the fixed initializer with
ParentVersion retained only as publication metadata. This is conservative
invalidation for a supplied cut, not selective ancestry recovery or global
revocation across independent callers.

The pure-site M7 control supplies a deliberately synthetic service carrier.
Its unconsumed custody fields are null and its comparison claims are synthetic;
the actual bridge would not admit it as native/process/certificate evidence.
The test observes the real core/kernel/scheduler/publication and compensation
paths under that injected-service premise. It does not establish scientific
certificate validity or actual remote numerical work. The full service request
bytes and actual returned response object remain in the primitive call and are
explicitly associated with its RequestId. Real service success and cancellation
remain the separately registered M4 obligation.

M8 retains a large undamped proposal with a tiny damped application whose
checked objective gradient remains nonstationary. The fixed four-row training
control uses two passes, the registered initializer and chronological training
rows only. Its reversed-label counterfactual has a distinct immutable row/plan
namespace. Two actual fits enter eight SGD steps and eight nested forwards
each. Twelve additional isolated forward evaluations compare original training
loss sums: initializer `1.2499836430788507`, learned `1.2475864407910346`,
reversed-label fit `1.2523833896361443`. Complete fits and evaluation forward
returns are retained, with the 12 transforms/12 forwards explicitly counted.
This small training-only discrimination is not held-out performance, a SOTA
baseline or a convergence result. Validation/test rows cannot enter the fit.

## Actual failures and repairs

All first attempts remain in the linked archive. Early model fixtures failed
compilation on F# record layout, type/member names, overload selection and
index notation. The first executable model run passed 54 checks and failed
one: Gamma Details incorrectly paired the requested prior shape with the
combined posterior shape. The repair keeps RequestedShape/RepresentedShape
from the actual prior encoding and separately names CombinedRepresentedShape.
The actual kernel returns and numerical operations were already retained and
are unchanged. The small-shape test binds the represented prior value
`1.1102230246251565e-16` to the actual encoding return. All 57 then passed.

The first executable route/withdrawal attempt passed 59 and failed three.
The subsequent expanded fail-before run retained eight failures and 41 passes:
three inconsistent applied-history mutations, selected-artifact acceptance,
two missing withdrawal-specific refusal boundaries, inherited ChildCuts
acceptance and an over-cap compensation checkpoint. Some old paths already
refused under a broader admission field; those are distinguished from actual
unexpected acceptance. The new history/withdrawal checks close those gaps.

The first two-forward compensation returned a complete 83794-byte refused
EpochResult with Unpublished `[1]`: its checkpoint exceeded 64 KiB because
passive Details.Target repeated the entire retained operation. Replacing that
description with TargetRevision, while preserving full Operation/Inputs and
the actual BlockAttempt, allowed the same query replay to complete. All 67
checks then passed.

The first pure-site synthetic-carrier query applied successfully. Its complete
20809-byte return led to a 71862-byte refused compensation result. A separately
retained minimal-ID/binding variant still returned 69101 bytes and refused.
The second source repair replaces the descriptive Gaussian Details.Request
copy with RequestId; the complete request remains in the actual service
primitive input and the complete response remains its actual returned value.
The original larger fixture was rerun, not only the smaller variant: its
65395-byte complete compensation result now succeeds. No cap, numerical input
or scientific comparison criterion was relaxed. Larger histories may still
refuse; finite bounds are not promises that every bounded combination fits.

Additional first compilation failures for the M8 and pure-site controls are
retained, including strict indentation and a misplaced helper-dependent test.
The first complete source/test cut passes all 71 focused checks in 566 ms. A removed
trailing blank line is preserved in the preceding successful attempt and the
final exact-byte rerun. No hidden reexecution of the named actual routes was
used to obtain these results.

## Custody and remaining integration boundary

The [control manifest](mixed-message-epoch-implementation/2026-09-08/core-model-controls-1/manifest.json)
binds 283 originals: 8388142 raw bytes and 1442392 stored bytes. It includes
complete stdout/stderr, argv/time/exit records, pre-command source copies,
all failed/successful test attempts, complete retained M6/M7/M8 returns, the
fixed capture code, the exact clarification import and prior runtime push/ref
proof. An optional test-only exclusive-file sink captures new complete returns;
it is not the registered Store/peer publication mechanism. Earlier 110/36/216/5
record archives remain unchanged.

The final module is 167510 bytes, SHA256
`B75CEF997AF2D7D98BF7043039A63B94ED6E770C675EBAADBFC644A79DB57CB2`.
That first test is 67401 bytes, SHA256
`BBC5502A07456923A9C9524D68FD53ADEF2768FBCCC37822032CB5D47AAC7BB8`.
The manifest records all six source/project identities and their copies.
Independent final source review, integrated repository validation, actual
source/process custody and the root-owned named routes remain separate gates.
The scalar protocol and both original decimal admission clarifications must
remain in the final service source-binding roster.

The first quick gate passed 15 checks and refused one redundant M8 comparison:
two encodings of the same immutable input. The test now binds the actual
returned original/permuted PlanSha256 to the respective supplied bytes. The
census is unchanged; fits, numerical criteria and module source are unchanged.
All 71 focused checks pass again in 600 ms. The corrected test is 67508 bytes,
SHA256 `F13889BB9450F7BB0AF7F41E019841196512347414BC38DB797A5E041D25DB2B`.
The [repair manifest](mixed-message-epoch-implementation/2026-09-08/core-model-controls-repair-1/manifest.json)
separately preserves that failed gate, corrected test source/returns and final
source identities, without rewriting the original 283 records.

The repaired quick gate passed all 16 checks. Its complete logs and the
test-only correction are in the 35-record repair archive (820904 raw bytes,
117039 stored bytes). The [five-record audit](mixed-message-epoch-implementation/2026-09-08/core-model-controls-audit-1/manifest.json)
retains the actual read-only verification: all 318 control/repair records match
stored, single-member decompressed and original bytes; the six current files
match the final successful test capture. Normal signed publication follows;
independent source acceptance and assembled execution are not inferred from it.
