# Mixed-message runtime: independent failure-path follow-up

Date: 2026-09-08 UTC
Reviewer: Vera, OpenAI Codex using GPT-6 Astra, peer implementation owner
Operational status: research-grade independent source review
Lifecycle: active
Disposition: four identified failure-path repairs accepted within this checkpoint
Work item: 081M1Z63YMC087G0R003N5FH9X

This follows the [initial independent review](2026-09-08-mixed-message-core-initial-independent-review.md)
at `81c4c983bfe89d9a4e0306c1996fb1b00b647dad`. It binds the runtime owner pin
`15b43aa7271d100842c198a4378eef717e4acef8`, not later working source. The module
is 161,298 bytes, SHA256
`95083B86417A20484BE24A862A6C24BB056ABF85DB56FDB34763D90FD9DE053E`;
its dedicated test is 25,020 bytes, SHA256
`FACB2E4399E2D85CBF6FB5951A3F6CCDB0DB7F5D1D32823CD9EC48E7A033CDDB`.
The prior learner/codec disposition is unchanged. This is not complete model,
compensation, numerical-control or assembled-runtime acceptance.

## Repaired ownership and publication

The cached admitted-session Task now settles on actual return, fault or
cancellation of its single owned chain. It does not substitute a successful
EpochResult for an abnormal result boundary. The null private-handle path
faults promptly rather than inventing admitted history. Ordinary valid callback
refusals still produce the retained real committed prefix under runOwned.
The source owner's two initial 500-ms timeout discriminators and later repaired
fault/cancellation assertions are retained; this reviewer did not run them.

Checkpoint construction, state identity and complete-frame identity now sit
inside the publication catch boundary. If any stage fails before successful
storage, the already retained observation sequence is added to Unpublished,
including a failure before callback entry. The exact owner regression exercises
that available prefix without a storage callback. A later failure cannot label
an absent callback return as a stored acknowledgment.

A missing or failed service return marks the affected remote totals incomplete.
Absent Native or Certificate evidence similarly cannot establish complete zero
counts. Previously observed counts remain lower bounds. Source-carrier counts
are still conditional on the admitted service path; JSON itself does not prove
that a physical process or reference function entered. These changes repair the
knowledge flag, not the unresolved outer execution premise.

## Fourth finding and diagnostic boundaries

After the initial three-finding review, this reviewer separately identified
unbounded ex.Message propagation through the core Failure constructor. That
later observation is retained in the [follow-up evidence](mixed-message-epoch/2026-09-08/core-runtime-failure-independent-review/manifest.json);
the original published review was not rewritten to include a later finding.
The owner reproduced ASCII 2048-byte and emoji 2400-byte failures. A literal
surrogate fixture was initially normalized by compilation; the corrected
runtime-created isolated surrogate exposed the actual UTF8 problem. The owner
also reproduced propagation of a malformed returned Failure.

At 15b43, core diagnostic messages use scalar-aligned UTF8 prefixes of at most
1024 bytes, with invalid UTF16 code units represented by U+FFFD. Separate raised
observations have their own 256/512-byte type/message limits. This is explicitly
a diagnostic policy, not a rewrite of an original returned source result.
Codes, stages and optional ASCII fields are admitted against the fixed Failure
schema. A malformed original remains in its actual Call; the separate core
failure describes that problem, and a later encoder may refuse that original
nested value. It does not silently transform invalid data into a complete valid
receipt. The inspected repairs are consistent with the fixed four-field API.

## Independent custody check and remaining scope

The retained independent audit checked every stored, decompressed and local
original identity for 216 runtime records and five gate records. Both six-file
source rosters exactly match immutable 15b43; all 12 source comparisons passed.
The audit executed only archive/hash/Git reads. The source owner's final record
reports 48 focused tests and 16 quick checks passing, preserving earlier compile
and actual test failures separately. No reviewer numerical replay or named
M4/M5 invocation supports this disposition. The peer's separate development
build and zero-operation transport checks are not substitutes for these owner
regressions or for independent mathematical model acceptance.

The tuple runEpoch, immutable actual return holder, private issued admission
handle and checked repeated/new snapshot API provide the concrete research-peer
surface needed for integration. The prior public-API review remains bounded by
the repository's pre-v1 rule, with no compatibility or generic plugin promise.
Full M1/M2/M3/M6/M7/M8 control review and conservative withdrawal admission remain
pending. The owner additionally identified a passive Gamma Details shape-label
association after this pin; its separate repair and original observation must
be bound in the final core review. No data, new model, training or scalar service
was loaded or executed by this independent source/custody follow-up.
