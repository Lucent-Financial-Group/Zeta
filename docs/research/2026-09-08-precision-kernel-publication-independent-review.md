# Independent precision-kernel publication scope and evidence review

Date: 2026-09-08 UTC
Operational status: research-grade review
Disposition: accepted within the scalar-kernel and publication scope below
Reviewer: Vera, OpenAI Codex using GPT-6 Astra, predictor-audit lane
Work item: 081M1Z63YMC087G0R003N5FH9X

## Exact reviewed boundary

The fresh-main publication at
`c1e6f0497afcc5078b03bfe8e797dc12e684cf58` preserves the declared source,
review and evidence scope. Its separately retained publication validation at
`e15821f4ab16ea70d16469d17fdb5d0252e00dd5` adds no source, test, benchmark
or alignment changes. I found no material scope, copy, composition or
evidence discrepancy. The [HC-8 explanation correction](https://github.com/Lucent-Financial-Group/Zeta/commit/8c2434003e638b36c347fb47a9397e4a848c041f)
is a separate documentation change and is excluded from these two trees.

This pass performs Git/file reads, byte comparisons, lossless decompression
and independent bookkeeping. I did not run a project module, test, native
producer, comparator, optimizer, training, registered stream or runtime
investigation. The reviewed publisher owns the actual gates and replay.
This is a local exact-cut review, not a GitHub CI or merge observation.

| Role | Exact commit |
| --- | --- |
| Fresh main base | 38b3acd14f0fc07449ace1b78311959f249468ed |
| Root integration source | afce96353549a27937dc8b5626532c8aca5ec026 |
| Initial bounded publication import | 2276d608c504d9cb99f6564e3ba24e9abd852991 |
| Project patch ancestor | 1393e65b439cf7a4b2a5018eeef37fed75d7d1bc |
| Five-document patch ancestor | 5cdd7a6568fcf250575a0d6d814434c3bc3c5214 |
| Tested publication cut | c1e6f0497afcc5078b03bfe8e797dc12e684cf58 |
| Publication validation preservation | e15821f4ab16ea70d16469d17fdb5d0252e00dd5 |

## Complete tree and composition checks

The retained import roster contains exactly 212 unique new files, totaling
5720431 bytes. For every row I independently checked raw bytes and SHA256,
Git mode, object type and blob identity against both root afce and the
initial publication commit. All are regular `100644` blobs and are absent
from the fresh-main base. The separate mode roster agrees in exact order.

The initial import has 222 changed paths with `--no-renames`. Its 212 copied
files, seven existing-file modifications, completed prior publication work
item, completion event and removed backlog path exhaust that set. The
rename-aware 221-entry display hides one deleted pathname. The completion
is for `081M1ZCHPWV087G0R002GG2PKY`, the already published PR17027 work;
its completed file and event equal root afce. It does not mark this new
kernel publication complete.

I independently reconstructed all seven existing-file results using
`git merge-file -p` over fresh main, the declared old ancestor and root afce.
All outputs equal the published bytes, with no conflict and unchanged file
modes. The five documentation paths are the compiled-controller handoff,
competitor matrix, learning-circuit continuation, prior publication review
and integration-direction index. The two project changes add only the
kernel and test `Compile` entries in their declared order. No fresh-main
addition is replaced by a wholesale parent copy.

The next four publication commits match their source commits in complete
changed-path sets, modes/types/blobs and exact binary/full-index diffs:

| Publication | Original root commit |
| --- | --- |
| 719a2a5f4a86733f07885ed93fcc476dc42a1186 | 6bee7644ce1022ff69634877ea2dab2b7a059b6e |
| 73c966f1550b315a49a3a3aa81d4f7f51e8ec3fc | 005573f869e7f63372a5ce5b1fda707fa7daefc7 |
| 424c853da80a4d6a7b17037d042616f15b1ba39f | e623d2d29959a4d161547d3ca185d3722cbcdcc3 |
| c1e6f0497afcc5078b03bfe8e797dc12e684cf58 | a184a1bb3cdcee0f428870d8a6928548bdbf3994 |

At c1e6 the full cut contains 275 changed paths: 267 additions, seven
modifications and one deletion. All changed on-disk files matched their
exact c1e6 Git bytes and modes when checked. No claim-path change is in
the cut; the parent claim files were not imported. The finite static
Markdown census found 153 relative destinations, all resolving in this
tree, with no relative anchors in that census. It does not certify every
external URL or historical locator as currently available.

## Kernel/API and record boundaries

The native source is byte-identical to the module independently reviewed
at `7100eefea413c1dd34b899fd1b7ba639494568b7`: 17206 bytes, SHA256
`4004196cb0ade8527dfebf83fbb3e6e42bd36208e38affd09906787a84901c02`.
The [native equation/API review](2026-09-08-precision-gate-kernels-native-review.md)
therefore remains applicable. Its scalar local VMP rules and the two
Gaussian objective derivatives follow the admitted density contract.
Exp and Log orientations remain distinct. Gaussian/Gamma natural kernels
can be improper intermediate sites; converting one to moments requires
separate proper-belief admission. Signed kernel algebra does not imply
that an arbitrary signed kernel is a normalized posterior.

Finite-input validation and conservative arithmetic refusal remain public
`Result` boundaries. The API retains `InvalidInput`, `NumericalFailure`
and `ImproperBelief` separately, including the distinction between a
parameter locator and a family-only improper-belief result. The two added
tests since the original review check representative exact error variants
and the independent nonstationary objective/gradient fixture. They address
the two test-strength recommendations without changing library bytes.
The final native test source is 13463 bytes, SHA256
`f443e58dd8c6d5e0c017d36a432dc6de121fd040dfa68d39e877e4a4ebfefebb`.

The [independent reference review](2026-09-08-precision-gate-kernels-reference-review.md)
and [producer/comparator review](2026-09-08-precision-gate-kernels-native-reference-replay-review.md)
cover the exact rational/Decimal reference and fixed adapter respectively.
`Shape` versus `RepresentedShape`, the native Normal residual field, family
versus field refusals, and the tiny-shape encoding observation remain
explicit mappings. Fixed-output field and finite-number admission is
sufficient for this comparison. The adapter and comparator are not public
arbitrary-JSON services or exception-total hostile-input parsers, and their
acceptance does not certify all arbitrary records. No heterogeneous graph,
mixed scheduler, projection solver, learned posterior, epoch/artifact
contract or full learner is admitted by these primitives.

The ADR's implementation/API-review status is stale at c1e6. The separate
documentation correction `b98f5256fcc29fd9d75c4ca2d18f8be6b7a5385b`
resolves it with an accepted scalar-kernel status and direct review and
validation links; its only other change is the learning-circuit index's
description of that ADR. I read that complete two-file diff. It expressly
keeps the optimizer, mixed schedule, learning-epoch interface and learned
comparison unimplemented. This scoped disposition is supported; it does
not retime the c1e6 executable gate or add executable changes.

## Actual validation and replay evidence

The root [integration archive](precision-gate-kernels/2026-09-08/integration-validation/README.md)
retains its own afce gate. I verified all 24 stored/decompressed/original
record identities and seven source pins. Those source-bound checks are
separate from the fresh-main publication gate; neither is retimed by a
later documentation import.

The publication full preflight process at exact c1e6 exited zero after
634.531074 seconds and reports all 18 executed checks passed, including
release build and the complete test command. This is the actual retained
runner summary, not an independently enumerated list of every passing test.
The formatter command exited zero after 17.264940 seconds but retained 26
unsupported-F# project messages plus a workspace warning. Its exit does
not establish F# formatter coverage. The separate F# preflight lint passed.

I read the fresh native output and independently compared all 24 ordered
rows, schema and reference hash against the already reviewed root replay.
They are equal. The comparison output is also byte-identical: 16 ordinary
numeric successes, seven typed refusals, one separate tiny-shape encoding
observation and ten rejected output mutations. The comparator differs from
the reviewed root comparator only in the two literal input paths; its
fixed reference byte/hash guard and comparison body are unchanged. These
are actual collector results and output-mutant tests, not mutated native
library executions or a new independent numerical rerun by this reviewer.

Both new assembly copies match their before-run records, actual producer
observations and the retained after-run witnesses. I independently rehashed
those copies and their current original files. The two bindings are:

| Assembly | Bytes | SHA256 |
| --- | ---: | --- |
| Zeta.Bayesian | 1037824 | dc8b62b1a119fec1ee48d08dcb2288d7bac73143cb8c3e1b336e0e1caf624fbb |
| Zeta.Core | 9266176 | dcdbd08e48acf5fb1ea8bc5a18eadf7746147ade44c0419bd257198e6c33c440 |

These direct assembly observations do not establish transitive runtime
closure or a full source-to-binary theorem. They do not reuse the older
root build's identities for the new build.

The inline capture harness failed while printing its final summary, after
the native/comparison processes and records completed. The retained note
explicitly identifies an `AttributeError` caused by variable shadowing and
says its error text is a transcription, not an original stderr file. Both
child exit-zero records and complete outputs were independently read. The
failure is not relabeled as a completely successful orchestration command.

The [publication-validation archive](precision-gate-kernels/2026-09-08/publication-validation/README.md)
at e158 has 33 records: 10425865 original bytes and 4767182 stored bytes.
All committed/current gzip bytes, decompressed identities and separately
retained original bytes matched. Its 36-path delta contains those records,
README/manifest and one replay-index append only. It grants no new source
or runtime admission beyond the exact c1e6 observations.

## Reviewer execution record and limits

The [review inventory](precision-kernel-publication-review/2026-09-08/manifest.json)
retains exact bookkeeping scripts and results. Two reviewer-side first
attempts failed before their final reports: the tree audit initially
resolved a root-relative `OriginalPath` against the wrong working directory;
the replay audit initially expected only one changed input path when both
reference and native paths changed. Their source preimages, empty/partial
outputs, errors and process results remain retained. Separately numbered
corrections passed against the same study records; no study process was
rerun and no data/schema gate was relaxed. A preliminary short Git lookup
also refused ambiguous `5cdd`; the final audit uses its full supplied hash.

Publication remains bounded to local kernels and evidence. PGE packaging,
equation and split hazards remain attached to any future reproduction.
Q8 remains a prospective construction with chart cells distinct from
actors; existing fabricated-memory/cartel mechanisms remain foundational.
No registered experiment, speedup, empirical detector improvement, physical
bound, default-oracle selection or full compiled runtime closure follows.

## Attribution

```text
Agency-Signature-Version: 1
Agent: Vera
Agent-Runtime: OpenAI Codex
Agent-Model: GPT-6 Astra
Credential-Identity: AceHack
Credential-Mode: shared
Human-Review: none
Human-Review-Evidence: none
Action-Mode: autonomous-fail-open
Task: 081M1Z63YMC087G0R003N5FH9X
Co-Authored-By: Codex <noreply@openai.com>
```
