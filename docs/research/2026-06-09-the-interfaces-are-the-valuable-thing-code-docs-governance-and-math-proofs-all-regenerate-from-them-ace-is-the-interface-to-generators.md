# The interfaces are the valuable thing: code, docs, governance, and even the math proofs all regenerate from them — `ace` is the interface to generators

**Register:** [grounded] capstone synthesis (Aaron) + [synthesis]. **Date:** 2026-06-09.
**Captured by:** Otto (shadow). Caps the interfaces/ace/regeneration arc.

## Aaron's words

> "ace = interface to hardware generators." ·
> "the interfaces are our valuable things — like I keep saying, the code can all be
> regenerated, and even the docs and governance from it, and even the math proofs."

## The interfaces are the durable seed; everything else regenerates from them

The **owned interfaces (ports) are the valuable, durable substrate** — the *what
remains*. Everything downstream is *what acts*, and is **regenerable**:

- **code** (the adapters / implementations) — regenerated from the interface
  (codegen, generative type providers, the own-impl adapters);
- **docs** — generated from the interfaces;
- **governance** — derived from the interfaces (the rules express interface contracts);
- **even the math proofs** — generated/checked against the interfaces (the specs are
  the interface; the proofs follow);
- **and eventually even the hardware itself** (Aaron 2026-06-09) — the RTL /
  bitstream / SoC regenerated by the hardware generators `ace` interfaces to, from
  the same interfaces. The regeneration runs *all the way to the metal*.

This is DV2.0 hub/satellite at the top level: the **interface = hub** (stable,
high-value, slow-changing); **code/docs/governance/proofs = satellites**
(higher change-rate, regenerable, not the source of truth). It is also "**we are
seed**": the interfaces are the compressed seed from which the whole tree unfolds —
the same thesis as *ace self-hosts then rebuilds Zeta from a 128-bit seed* (closure
is cognition), now stated for the *interfaces* as the seed.

Why this is the leverage: if you own the interfaces, **you can regenerate the entire
stack** — implementations, docs, governance, proofs, *and hardware* — so no vendor,
dep, or fab is load-bearing, and nothing downstream can be lost (it re-derives). The
interface is what you protect; the rest is output.

## `ace` is the interface to generators (not just a deployer)

So `ace` is not merely a deployer of finished artifacts — it is the **interface to
the generators** that *produce* them, across every layer:

- **hardware generators** — Chisel / Amaranth / Migen / SpinalHDL / HLS / bitstream
  toolchains: `ace` drives them to emit RTL / bitstreams / microcode / SoC
  capabilities (the hardware-intrinsics deployment, one layer deeper — it owns the
  *generator*, not just the output).
- **code generators** — emit the adapters/implementations from the interface.
- **doc / governance / proof generators** — emit those from the interface too.

`ace` federates **all** generators behind owned interfaces (the same port +
two-adapter + dep-as-oracle + byte-lock discipline), so a generated artifact is
trusted exactly like a package: content-hash + signature + golden-vector
conformance. The deployable unit generalizes from *package* → *generated artifact
at any layer* (software, docs, proofs, hardware). **`ace` = the owned interface to
the generative frontier of the whole stack.**

## The shape

This is the **F-shape (generative expansion)** applied to the substrate itself: the
interfaces are the generative core; `ace` is the interface to the generators; the
generators unfold the interfaces into code/docs/governance/proofs/hardware; and the
byte-lock + dep-as-oracle keep every generated layer certain. Own the interfaces +
own the generator interface (`ace`) ⇒ regenerate everything, trust everything, lose
nothing, be captured by no one.

## Anchors / ties

DV2.0 hub/satellite (interface=hub); "we are seed" + *ace rebuilds Zeta from a
128-bit seed* (closure-is-cognition); generative type providers / codegen; literate
& generated docs; proof generation/checking; hardware generators (Chisel — Berkeley;
Amaranth/Migen; HLS); the F-shape (generative); the own-all-interfaces principle +
the uncertainty-reduction-at-the-border + crypto-sovereignty + deepest-border-is-the-
metal docs (this is their unifying point: the *interface* is the asset).
