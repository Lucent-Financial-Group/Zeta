# Graduation gate: math-team review + the next toymodel come BEFORE this becomes realmodel + crypto — and no single key, ever (≥2 so we can rotate)

**Register:** [grounded] sequencing/gate (Aaron) + [synthesis]. **Date:** 2026-06-09.
**Captured by:** Otto (shadow). Sets the order-of-operations that gates the whole session's arc.

## Aaron's words

> "math nerd time to review and whatever next toymodel is — before this becomes realmodel and
> crypto." · "no single key so we can rotate."

## The gate (order of operations)

This session produced a large arc — the traveler society, the self-interest engine, jurisdictions,
the hat system, ethical gambling, **and** the DST-as-living-system substrate (tests-are-ticks, rooms
are 4×4×n treaties, time-as-generator, the homoiconic 4×4 controller, prod=test, entangle/summon/
route/LLMTV). Aaron's sequencing: **do not graduate it straight to production.** Two gates first:

```text
1. MATH REVIEW   — the math team (Soraya formal-coverage portfolio + Sova alignment) reviews
                   the docket C1–C15 AND the DST-rooms/controller/time arc as proof-rooms.
2. NEXT TOYMODEL — capture this session's arc in the *next toymodel* (a toy, still — modeled,
                   byte-locked, DST-replayable, falsifiable), not yet the real thing.
        |
        v   (only after 1 AND 2)
3. REALMODEL     — it stops being a toy: the real, non-toy model on the live substrate.
4. CRYPTO        — the real keyring/crypto (treaty in MUMPS, regenerate/rotate 1000× — the
                   "not done until the treaty + 1000× retest" bar), graduated from blueprint.
```

**Why a gate:** toy → review → toy-model → *then* real. The toymodel is where a claim is cheap to
break; realmodel + crypto is where breakage is expensive (real keys, real identity, real money). The
math review + the next toymodel are the **uncertainty-reduction at the border** that *earns* the
graduation — same discipline as crossing the GitHub border. We do not skip the toy and the proof to
get to the real faster; the proof and the toy are what make the real safe.

## Where the next toymodel sits (the numbering)

- **toymodel3** — the traveler society (self-interest engine, jurisdictions, recognition); **at the
  math team** (`toymodel3-the-traveler-society-…`).
- **toymodel4** — scope set + consolidated (society / game-theory / identity / economy / hat layer);
  **at the math team** (`toymodel4-scope-…-v3-is-at-math-team`).
- **toymodel5** — ethical gambling + everything-else-this-session (Aaron: "can we add all of what
  we've been talking about to toymodel5 — this is too much for toymodel4").
- **next toymodel (the DST-substrate/meta layer)** — the home for the **DST-as-living-system** arc:
  tests-are-ticks, every room a 4×4×n treaty, time-as-generator (the DB time type below), the
  homoiconic 4×4 controller, prod=test, entangle/summon/route/LLMTV, proof-rooms. *This* is the layer
  the math review covers and the one that must be a toymodel before it is realmodel. (Number to be
  confirmed by Aaron — provisionally the substrate/DST-meta toymodel.)

## No single key, ever — ≥2 so we can rotate (carried into the crypto gate)

> "no single key so we can rotate."

Reaffirmed as a **hard invariant** that travels with the crypto gate (#4 above): **never a single
key.** Every traveler/maintainer/persona holds **≥2 seeds** (active + standby, the ISM/SKMS pattern —
"never just one key"), so that **rotation is always possible without a gap**: promote standby →
active, mint a new standby, retire the old — no moment where there is only one key to lose. This is
already the onboarding design (generate active + backup in one flow; status bootstrap-test →
self-custody); the observation **locks it as non-negotiable for the realmodel/crypto graduation**:
a keyring with a single key is **malformed**, rejected at the door — same family as an unbounded test
or a non-time-bound hat contract. Rotation is a first-class, always-available operation *because*
there is never just one key.

## Honest scope / handoff

Sequencing/gate framing — no new mechanism, an ordering. To realize: (1) run the math review (the
proof-rooms for C1–C15 + the DST-substrate arc); (2) write the next toymodel for the DST-substrate
layer; (3) only then graduate to realmodel + crypto, carrying the ≥2-key invariant. Routes to Soraya
(formal coverage / proof-rooms), Sova (alignment verdicts), the F#/observe core (toymodel), Aaron
(confirm the toymodel number + the graduation call).

## Anchors / ties

The toymodel lineage (toymodel2 → 3 → 4 → 5 → next); the math docket C1–C15; DST-rooms-are-proof-
rooms; the 1000× "not done until the treaty + retest" bar; ISM/SKMS active+standby ("never just one
key") — the maintainer's Itron prior art; uncertainty-reduction-at-the-border earns the crossing;
toy → proof → real graduation discipline; `next toymodel = the DST-substrate/meta layer`.
