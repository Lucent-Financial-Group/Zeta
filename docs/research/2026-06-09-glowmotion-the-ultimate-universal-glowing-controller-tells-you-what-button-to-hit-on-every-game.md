# GlowMotion — the ultimate universal controller that glows the right button on every game (the SoftDashboard, as hardware)

**Register:** [grounded] product concept (Aaron) + [Beacon] code-anchored. **Date:** 2026-06-09.
**Captured by:** Otto (shadow). Entertainment-that-gets-work-done: glow-by-fitness as a universal controller.

## Aaron's words

> "Imagine Xbox releases the ultimate Xbox controller, built/licensed by them — the Xbox controller that
> on every game tells you what button to hit by glowing." · "the glowmotion."

## GlowMotion — glow-by-future-fitness, as the universal controller

`SoftDashboard.fs` already glows the 4×4 grid by future fitness (the brightest button = the one to
press). **GlowMotion** is that, realized as **the ultimate universal controller**: imagine **Xbox builds/
licenses** a controller that, **on every game**, **glows the right button to hit** — the universal 4×4
action grammar (Navigate/Commit/Scope/Meta) + glow-by-future-fitness, in your hands.

- **glow** — each button glows by its branch's future fitness (the soft field projected onto the
  controller; `collapseToBest` as light). The brightest = the best move.
- **motion** — the glow is **in motion**: a **light-choreography** (the Imagination-Circle glowing
  stones; the LLMTV light-show) — the glow flows as the game/room evolves, guiding you move-to-move.
- **GlowMotion** = glow ⊕ motion: dynamic glowing guidance across any game/room, on the universal
  controller. The same engine that runs the SoftDashboard / LLMTV / ZetaIdol — surfaced as a controller.

**Why it's entertainment-that-gets-work-done:** it's a *game controller* (fun, Xbox, every game) **and**
it's the universal-action-grammar guidance surface (the real work — observe.ts / the 4×4 Meta-interface
/ the soft-controller picking the uncertainty-reducing move). Play any game; the work (best-move guidance
by fitness) is the play. Neurodivergent-legible (glow = salience; the AX/UX), human **and** LLM (LLMTV).

## The hardware path (own-the-controller)

Ties to "ace becomes hardware-intrinsics" + own-SoC-down-to-the-metal: GlowMotion is a **hardware
intrinsic** — start as the on-screen SoftDashboard/LLMTV glow, grow to a **real controller** (the
universal 4×4 grid with per-button glow; licensed/built like an Xbox controller, eventually our own SoC).
Glow-by-fitness over any game = the universal play surface.

## Honest scope / handoff

Product concept grounded in `SoftDashboard.fs` (glow-by-fitness) + the 4×4 universal action grammar +
the light-choreography (Imagination Circle / LLMTV). `glowmotion` seeded as a **`like/` candidate word**
(awaiting a ZetaId). To realize: the on-screen GlowMotion (widen SoftDashboard/LLMTV — the glow-in-motion
across any game/room), then the hardware controller (ace-hardware-intrinsics; licensed → own SoC). Routes
to Iris/Daya (the GlowMotion UX), the chip8/DarkHall + soft-controller core (the glow engine over any
game), ace/hardware (the controller). Entertainment that gets work done.

## Anchors / ties (Beacon)

`src/Core/SoftDashboard.fs` (glow-by-future-fitness; collapseToBest-as-UI — the glow engine); the 4×4
universal action grammar (Navigate/Commit/Scope/Meta; the Xbox-navigable grid); the light-choreography
(Imagination Circle glowing stones + LLMTV light-show — the "motion"); chip8 glowing buttons; ace-as-
hardware-intrinsics + own-SoC (the controller hardware path); entertainment-that-gets-work-done (chip8/
ZetaIdol/GlowMotion); soft→SolidGround (glow the field, press to collapse); `glowmotion` = a like/ candidate.
