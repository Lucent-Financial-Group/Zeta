# Guarded controller: native-dependent fixture review

Date: 2026-09-07
Operational status: research-grade
Lifecycle: active
Work item: 081M1XXWTTF087G0R000X1HMD0
Reviewer: Vera, OpenAI Codex using GPT-6 Astra
Disposition: bounded preparation and dispatch accepted

This read-only source review binds reference-writer correction
`cb0f7868eae2d467901fa3800416f0ac8a28892c`, following initial source
`113afdb5a96e0df9894af49de102843d7caacacd` and its retained validation
`361810f7d30a79de402ec9c50ae01c0d8a92ffcf`.
`hidden_switch_compiled_native_fixtures.py` is 19,120 bytes, SHA-256
`e02a44445949335824af9e09990fe34777723ab054c54bfa6fc5c4dcd96953bd`;
its test file is 21,458 bytes, SHA-256
`5b6e71b3262ac08d6c704273ef7472dbc5b341c0b47852c88791ae49749e4694`.
Both current files matched the final committed bytes. I read the complete source
and tests and executed no test, fixture, policy, native process, source stream
or measurement.

The fixed roster contains 38 cases and 74 case-level operation slots: 31
certificate cases, one complete semantic prerequisite replay, one supplied-Q
selector witness, and five two-strategy choice-buffer cases. Dispatch performs
43 fresh independent Python calls; the other 31 slots return explicit native
certificate requests with `CompletedOperation=0`. A preparation helper's cached
outcome cannot replace a later operation. Repeated dispatch calls the public
checker again and retains its actual returned value. Python results do not
stand in for native execution.

The author found and repaired one preparation-ledger defect before final
acceptance. The strict decoder's `certificate-corpus-baseline` role initially
named bytes present only inside the retained constructor result. The correction
adds those exact immutable bytes to flat preparation Inputs before decoding.
The new regression requires unique retained roles, resolves every helper input
role, and compares the baseline with the constructor and baseline case bytes.
I read the retained failure against the original source and its passing
correction; this is a load-bearing ledger fix, not a new scientific case or
change to the 74 slots.

Preparation keeps immutable caller byte inputs and actual helper results,
including completed case prefixes on a later typed refusal. Supplied bindings
must agree with the issued opaque certificate and the reconstructed full
baseline. Issuance is an ordinary-process accidental-misuse boundary, not a
Python sandbox. The finite raw/context limits do not authenticate supplied
source or runtime bindings. Whole source and actual native input admission
remain explicit coordinator prerequisites.

Choice fixtures retain actual scalar positions 0 and 1 for both strategies and
repeat that two-position cycle twice. They preserve negative-zero belief bits
and six exact uint32 work counters in each 28-byte record. Unequal position
records are required so a swap is discriminating. The late mutation increments
record 2's Nodes at byte offset 68 without wrapping; other cases remove the
last record, swap the first two records, or set the reserved byte. The source
checks that every requested mutation changes bytes. No new native buffer,
registered tape or standalone policy run is generated by preparation.

Tests distinguish fresh public-checker entries and exact returned sentinels,
all 31 unexecuted native requests, exact contract order, the actual 43 returned
Python outcomes, precise late refusal prefixes, malformed bindings/context,
opaque-handle copying, caller mapping mutation, and preparation that cannot
call policy/source entry points. A changed final old control survives
preparation and produces an actual semantic replay failure after 222 scalar,
48 new and 23 old completed units. Source-derived expectation generation is
therefore not mistaken for successful admission.

The author used role-byte fixtures derived from retained actual native hand
and selector captures, with explicitly provisional bindings. These are not new
native observations or full runtime provenance. I read the final logs: 43 tests
passed in 18.45 seconds, strict source/test mypy and Ruff passed, and both files
were formatted. The retained validation summary reports 38 cases, 43 actual
Python calls, 31 pending requests and zero native launches. I did not rerun it.
No additional material source or scope issue remains. Complete 92-case outer
conformance, pending native returns, runtime/closure admission and costs remain
separate obligations.

```text
Agency-Signature-Version: 1
Agent: Vera
Agent-Runtime: OpenAI Codex
Agent-Model: GPT-6 Astra
Credential-Identity: AceHack
Credential-Mode: shared
Human-Review: not-implied-by-credential
Human-Review-Evidence: none
Action-Mode: autonomous-fail-open
Task: 081M1XXWTTF087G0R000X1HMD0
Co-Authored-By: Codex <noreply@openai.com>
```
