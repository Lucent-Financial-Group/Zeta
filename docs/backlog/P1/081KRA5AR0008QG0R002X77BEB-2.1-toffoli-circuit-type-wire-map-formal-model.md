---
id: 081KRA5AR0008QG0R002X77BEB
priority: P1
status: closed
title: "ToffoliCircuit type + wire-map formal model (smallest slice of 081KR50HA0008QG0R0002PGV1N)"
effort: S
created: 2026-05-11
last_updated: 2026-05-29
resolved: 2026-05-29
resolved_by: "claim/codex-loop-b0366-2-1-toffoli-circuit-tests-20260529"
claimed_by: otto
claim_branch: feat/b-0366-2-1-toffoli-circuit-wire-map
depends_on: [081KR50HA0008QG0R0021B5J87]
parent: 081KR50HA0008QG0R0002PGV1N
classification: buildable-now
decomposition: atomic
owners: [architect]
type: research
tags: [toffoli, circuit-model, fsharp, zset]
---

# 081KRA5AR0008QG0R002X77BEB — ToffoliCircuit type and wire map

## Pre-start checklist (2026-05-13, Otto)

- **Prior-art search**: 081KR50HA0008QG0R0021B5J87 already shipped `Bit`, `ToffoliWires`, `ZSetGateOp`
  in `src/Core/ToffoliGate.fs`. That file contained a stub `ToffoliCircuit`
  (`Gates: ToffoliWires list; Description: string`) that does not match the
  spec. No `WireId`, `WireMap`, or `ToffoliGateStep` exist anywhere in repo.
  No conflict with existing substrate.
- **Dependency check**: 081KR50HA0008QG0R0021B5J87 is shipped (ToffoliGate.fs present, tests green).
  Dependency satisfied.
- **Scope**: Pure type additions to `src/Core/ToffoliGate.fs` at namespace level.
  No new files (no .fsproj change needed). Remove stub; add proper types.

## What (bounded slice)

Define the minimal F# data model for a Toffoli gate network:

- `type WireId = int` — wire index type
- `type WireMap = Map<WireId, Bit>` — wire state map (reuses `Bit` from 081KR50HA0008QG0R0021B5J87)
- `[<Struct>] type ToffoliGateStep` — single gate application by wire indices
- `type ToffoliCircuit = { Gates: ToffoliGateStep list; Wires: WireMap; Ancilla: int }`
- `ToffoliGate.emptyCircuit : ToffoliCircuit` — factory for the empty circuit

No implementation of join yet. Pure type + invariant docs.

## Deliverable

Extended `src/Core/ToffoliGate.fs` — proper circuit types at namespace level,
replacing the earlier stub. `ToffoliGate.emptyCircuit` is the factory function.

## Focused checks (mandatory)

- `dotnet build -c Release` → **0 warnings 0 errors** ✓
- `dotnet test tests/Tests.FSharp -c Release --filter "ToffoliGate"` → **7/7 passed** ✓

## Evidence

- Depends on 081KR50HA0008QG0R0021B5J87 gate types ✓ (landed)
- Prepares 081KRA5AR0008QG0R001GQSVWE join encoding (Gates list + WireMap now exist)
- Prepares 081KRA5AR0008QG0R000CYY9ZN reversibility laws (types ready for FsCheck generators)

## Resolution

Closed by Codex/Vera verification pass on 2026-05-29:

- `src/Core/ToffoliGate.fs` already contains `WireId`, `WireMap`,
  `ToffoliGateStep`, `ToffoliCircuit`, and `ToffoliGate.emptyCircuit`.
- `tests/Tests.FSharp/Formal/ToffoliGate.Laws.Tests.fs` now covers the empty
  circuit invariants and a concrete wire-map gate step.
- `docs/BACKLOG.md` marks 081KRA5AR0008QG0R002X77BEB closed.
