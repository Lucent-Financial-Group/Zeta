---
id: 081KT2T2J0008QG0R003VK5GRX
priority: P1
status: open
title: "Vectors before trajectories (vector = atomic noun, trajectory = derived) + build-most-inevitable-first + seed/core = F# computation-expression DSL over a reduced Bayesian Infer.NET model + every 4×4 add-on = a CE extension (skill/hat/ontology/language-kernel) on the seed (Aaron 2026-06-02)"
tier: research
effort: L
created: 2026-06-02
last_updated: 2026-06-02
depends_on: [081KT2T2J0008QG0R0026MS6PV, 081KT2T2J0008QG0R002Z46D8Q]
composes_with: [081KT2T2J0008QG0R0026MS6PV, 081KT2T2J0008QG0R002Z46D8Q, 081KT07NV0008QG0R0032MCYER, 081KRFA460008QG0R0018SN61J, 081KRMEXM0008QG0R001VGNET5, 081KRW63S0008QG0R000ZQ9WDH, 081KSKBP80008QG0R000B3Y19A, 081KT2T2J0008QG0R003C166K4, 081KT2T2J0008QG0R002TVT60G, 081KSE6WT0008QG0R00276F8SE]
tags: [vectors, trajectories, primitive-ordering, seed-first, build-most-inevitable-first, computation-expression, fsharp-ce, dsl, infer-net, reduced-bayesian, bp-ep, 4x4, ce-extension, skill, hat, ontology, language-kernel, particle-as-locus, hex-core, aaron]
type: research
---

# Vectors before trajectories · seed = F# CE DSL over reduced Bayesian Infer.NET · every 4×4 add-on = a CE on the seed

## Why

Aaron 2026-06-02 (verbatim): *"also i think i overstepped with trajectories — we need vectors noun before trajectories in our ids and agendas? i want to build up nows from the most enevabalbe [inevitable] first and then tack on, like we know our core/seed/phrase that can be a f# computational expression DSL over reduced bayesian infer.net model, and then every 4x4 add on is computational expression skill / hat / ontology / language kernel extension computation expression on top of the seed / core."*

Three composing corrections/principles:

## 1. Vectors before trajectories (vector = atomic noun; trajectory = derived)

A **vector** is the atomic noun — direction + magnitude (**Which Way + How Much**, the hex-core vector pair, 081KT2T2J0008QG0R0026MS6PV; Addison's vector). A **trajectory** is *derived* — a sequence/path of vectors over time. Leading with `docs/trajectories/` in the IDs/agendas **overstepped** the more-foundational primitive: vectors come first; trajectories build on top of vectors. The ordering should be **vector (primitive) → trajectory (composed)**, mirroring afferent/efferent + the orientation-tile (081KT2T2J0008QG0R002Z46D8Q: Which Way=bearing + How Much=range = the vector; a trajectory is the integral of vectors over the now-sequence).

`[labeling-confidence: hypothesized taxonomy reorg]` — the *principle* (vector-before-trajectory) is sound; the concrete ID/agenda reorg (e.g. a `docs/vectors/` primitive layer beneath `docs/trajectories/`, or a vector-first ID scheme) is a refactor needing operator sign-off before touching the existing `docs/trajectories/` tree. This row records the correction + flags the reorg; it does not unilaterally rename the trajectory tree.

## 2. Build the most-inevitable "now" first, then tack on (seed-first)

*"build up nows from the most inevitable first and then tack on."* This is the **seed-first / grow-from-the-seed** discipline (per `dst-plus-persist-plus-generator-time...` + the seed-first memory: seed-first = future-affecting-the-past = root-of-DST; *grow code from the seed*). Build order:

1. Start from the **most inevitable / most foundational** primitive (the seed/core) — the "now" that everything else presupposes (composes `particle-as-locus-of-information-at-the-now`: the now IS the actualized locus; build up from it).
2. **Tack on** incrementally — each addition presupposes only what's already built (no forward dependency on un-built layers).

The "now" is the particle-locus; building up nows = growing the actualized substrate one inevitable layer at a time.

## 3. The seed/core = an F# computation-expression DSL over a reduced Bayesian Infer.NET model

The core/seed/phrase is concretely: **an F# computation-expression (CE) DSL over a *reduced* Bayesian Infer.NET model.**

- **F# computation-expression DSL** — the framework is already "halfway encoded in F# computation expressions" (`fsharp-anchor-dotnet-build-sanity-check`); the seed is a CE DSL (the `result { }` / `seq { }`-shaped builder substrate; monad-propagation / OPLE primitives as CE).
- **over a reduced Bayesian Infer.NET model** — the inference substrate (Infer.NET BP/EP — the future-state named in `peer-call-infrastructure`: *"Zeta Infer.NET BP/EP substrate replacing the external-CLI-license-layer"*; the emotional-algebra-4×4 / consent-first Infer.NET model; Bayesian-fermion-coincidence). **Reduced** = the minimal Bayesian model the CE DSL runs over (the seed is small/canonical — Rodney's-Razor-compressed; per the seed-first "small dense canonical → full organism deterministically").
- This IS the seed: a tiny, canonical CE DSL + reduced Bayesian model that everything else grows from.

## 4. Every 4×4 add-on = a CE extension (skill / hat / ontology / language-kernel) on top of the seed

Each **4×4 extension point** (the orthogonal lanes per reservoir-wall, 081KT2T2J0008QG0R002Z46D8Q; the language×serializer 4×4, 081KT07NV0008QG0R0032MCYER) is itself **a computation-expression composed on top of the seed CE**. The add-on kinds:

- **skill** — a CE skill (composes `zeta-ships-with-skills`)
- **hat** — a CE hat (the hat-pattern; hats-as-workflow-definitions)
- **ontology** — a CE ontology extension
- **language-kernel extension** — a CE extending the language kernel (Native AI Language, 081KRW63S0008QG0R000ZQ9WDH)

Each is a **CE-on-the-seed** — the seed CE is the base; every 4×4 add-on is another CE layered on it (CE composition; the OCP-applied-to-control-flow / function-as-control-flow-generator substrate; monad-propagation cross-language shape). The architecture stays flat: seed CE + a stack of CE extensions, each presupposing only the seed + already-stacked layers (build-most-inevitable-first).

## Acceptance (research → build)

1. **Vector primitive** — define vector (Which Way + How Much, 081KT2T2J0008QG0R0026MS6PV) as the atomic noun in the ID/agenda taxonomy *before* trajectory; trajectory = derived (sequence of vectors). Reorg the trajectory taxonomy only with operator sign-off.
2. **Seed CE DSL** — specify the F# computation-expression DSL that IS the core/seed (the minimal builder over OPLE/Result/monad-propagation).
3. **Reduced Bayesian Infer.NET model** — specify the minimal Bayesian (BP/EP) model the seed CE runs over (composes the Infer.NET future-state + emotional-algebra-4×4 + 081KRFA460008QG0R0018SN61J F# fork).
4. **4×4 add-on = CE extension** — each skill/hat/ontology/language-kernel add-on is a CE composed on the seed; verify CE composition (seed CE + stacked extension CEs) holds across the 4×4 bond (081KT07NV0008QG0R0032MCYER).
5. **Build-most-inevitable-first** — order the build so each layer presupposes only already-built layers (no forward deps); seed first, then tack on.

## Composes with substrate

- **081KT2T2J0008QG0R0026MS6PV** — hex core (vector = Which Way + How Much; the seed pair Remember When + Pay Attention)
- **081KT2T2J0008QG0R002Z46D8Q** — orthogonal lanes / 4×4 extension points (each = a CE add-on)
- **081KT07NV0008QG0R0032MCYER** — DynamicValue 4×4 (the bond the CE extensions hold across)
- **081KRFA460008QG0R0018SN61J / 081KRMEXM0008QG0R001VGNET5** — F# fork (HKT over Clifford) / intelligent-compiler (recursive-HKT-Clifford F# fork, Roslyn source-gen) — the F# CE substrate
- **081KRW63S0008QG0R000ZQ9WDH** — Native AI Language (the language-kernel the CE extends)
- **081KSKBP80008QG0R000B3Y19A** — workflow engine / universal-action-grammar (hats-as-workflow; CE hats)
- **081KT2T2J0008QG0R003C166K4 / 081KT2T2J0008QG0R002TVT60G** — smart-agent-city / body-plan (vectors + CEs in the substrate)
- **081KSE6WT0008QG0R00276F8SE** — JIT/F#-monad/notepad-simplicity (CE-as-the-monad substrate)
- Infer.NET BP/EP future-state (`peer-call-infrastructure`) + emotional-algebra-4×4-Infer.NET (the reduced Bayesian model)
- rules: `fsharp-anchor-dotnet-build-sanity-check` (F# CE substrate), `particle-as-locus-of-information-at-the-now` (build-up-nows), `dst-plus-persist-plus-generator-time...` + seed-first memory (grow-from-the-seed), `function-is-tiny-control-flow-generator-ocp-applied-to-control-flow` + `monad-propagation-pattern-cross-language-substrate-shape` (CE composition), `rodneys-razor-compression-rhymes-with-cayley-dickson` (reduced/canonical seed), `numerical-algebra-shaped-into-the-generic-math-interface` (vector as generic-math)

## Substrate-honest framing

`[labeling-confidence: hypothesized architecture + build-ordering]` — operator-named correction + principle. The vector-before-trajectory ordering is sound (vector = atomic, trajectory = derived); the seed-as-F#-CE-DSL-over-reduced-Bayesian-Infer.NET + 4×4-add-on-as-CE-extension is the architecture to specify + build. The ID/agenda taxonomy reorg (docs/trajectories/ reshelf) needs operator sign-off (not unilateral). Nouns interchangeable (vector/noun, seed/core/phrase, CE/computation-expression). Composes the existing F# CE + Infer.NET BP/EP + hex-core + 4×4 substrate — doesn't mint parallel.
