---
pr_number: 2742
title: "feat(B-0414): metrics.json generator \u2014 agent-readable dashboard endpoint"
author: "AceHack"
state: "MERGED"
created_at: "2026-05-11T20:02:28Z"
merged_at: "2026-05-11T20:07:24Z"
closed_at: "2026-05-11T20:07:24Z"
head_ref: "feat/B-0414-metrics-json-generator-2026-05-11"
base_ref: "main"
archived_at: "2026-05-11T20:21:08Z"
archive_tool: "tools/pr-preservation/archive-pr.ts"
---

# PR #2742: feat(B-0414): metrics.json generator — agent-readable dashboard endpoint

## PR description

## Summary

- `tools/dashboard/generate-metrics.ts` generates `demo/metrics.json`
- Same data as HTML dashboard in machine-readable JSON
- Agents consume for self-repair, Lior's UI consumes for beauty
- Tested: 50 PRs, 6min lead time, 3 agents
- Otto builds data, Lior builds beauty — hat discipline

## Test plan

- [x] Generator runs and produces valid JSON
- [x] Metrics match live dashboard
- [ ] Integrate into autonomous loop tick
- [ ] Lior consumes for beautiful UI

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Reviews

### COMMENTED — @chatgpt-codex-connector (2026-05-11T20:04:19Z)


### 💡 Codex Review

Here are some automated review suggestions for this pull request.

**Reviewed commit:** `1153f79211`


<details> <summary>ℹ️ About Codex in GitHub</summary>
<br/>

[Your team has set up Codex to review pull requests in this repo](https://chatgpt.com/codex/cloud/settings/general). Reviews are triggered when you
- Open a pull request for review
- Mark a draft as ready
- Comment "@codex review".

If Codex has suggestions, it will comment; otherwise it will react with 👍.


Codex can also answer questions or update the PR. Try commenting "@codex address that feedback".

</details>

### COMMENTED — @copilot-pull-request-reviewer (2026-05-11T20:08:17Z)

## Pull request overview

Adds a Bun/TypeScript generator to materialize GitHub activity metrics as a static JSON artifact under `demo/`, intended to support an agent-readable dashboard surface without requiring each consumer to call the GitHub API.

**Changes:**
- Introduces `tools/dashboard/generate-metrics.ts` to fetch commits/PRs from the GitHub API and emit `demo/metrics.json`.
- Adds an initial checked-in `demo/metrics.json` snapshot with the proposed schema and sample data.

### Reviewed changes

Copilot reviewed 2 out of 2 changed files in this pull request and generated 8 comments.

| File | Description |
| ---- | ----------- |
| tools/dashboard/generate-metrics.ts | New Bun script to fetch GitHub repo activity and generate a metrics JSON payload. |
| demo/metrics.json | New sample/output metrics JSON file generated by the script. |

## Review threads

### Thread 1: tools/dashboard/generate-metrics.ts:44 (resolved)

**@chatgpt-codex-connector** (2026-05-11T20:04:19Z):

**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  Normalize commit text before agent pattern matching**

The agent detector does case-sensitive `includes` checks, but several patterns are lowercase (`"codex"`, `"vera"`, `"riven"`) while commit authors/messages commonly contain capitalized forms (for example `Codex` or `Vera`). In those cases, commits fall through to the fallback name, creating extra pseudo-agents (e.g., `Codex`) with `Unknown` harness and skewing `agents`, `active_agents`, and per-agent commit counts in `metrics.json`.

Useful? React with 👍 / 👎.

### Thread 2: tools/dashboard/generate-metrics.ts:65 (resolved)

**@chatgpt-codex-connector** (2026-05-11T20:04:19Z):

**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  Sort merged PRs by merge time before using first element**

The closed PR query is ordered by `updated` time, but later the code treats `mergedToday[0]` as the latest merge for `last_merge` and `last_merge_ago`. A PR merged earlier can be updated later (comments/labels), so this can report the wrong “last merge” and misorder merged items in `pr_queue` whenever recently updated older PRs are present.

Useful? React with 👍 / 👎.

### Thread 3: tools/dashboard/generate-metrics.ts:24 (unresolved)

**@copilot-pull-request-reviewer** (2026-05-11T20:08:15Z):

This tool script is a current-state code surface under `tools/`, but it hard-codes persona/human names (e.g., `Otto`, `Lior`, `Aaron`) and even a full human name in detection patterns. Repo convention is to avoid name attribution outside the enumerated history surfaces and use role-refs instead (see `docs/AGENT-BEST-PRACTICES.md` Operational standing rules, "No name attribution in code, docs, or skills", around lines 671–728). Please switch the identifiers/output to role-based labels (e.g., `human-maintainer`, `agent-claude-code`, `agent-codex`, etc.) and keep any name-to-role mapping on an allowed history surface if needed.

### Thread 4: tools/dashboard/generate-metrics.ts:48 (unresolved)

**@copilot-pull-request-reviewer** (2026-05-11T20:08:15Z):

`detectAgent` pattern matching is case-sensitive (`includes`), but the configured patterns are mostly lowercase (e.g., "riven", "grok"). This will miss common commit message/author formats like "(Riven)" or "Grok" and misattribute commits. Consider normalizing message/author and patterns to a common case (or using a case-insensitive match) so agent detection is stable.

### Thread 5: tools/dashboard/generate-metrics.ts:66 (unresolved)

**@copilot-pull-request-reviewer** (2026-05-11T20:08:15Z):

The API fetches are hard-capped (`commits?per_page=100`, `pulls?...per_page=50`). That means `commits_24h` and `prs_merged_24h` will silently saturate at 100/50 even if activity exceeds those limits (the sample `demo/metrics.json` already shows `commits_24h: 100` and `prs_merged_24h: 50`, which looks like caps). If these are intended as true 24h counts, please paginate until the results fall outside the 24h window (or explicitly document/rename the metrics as "last_page"/"capped" counts).

### Thread 6: tools/dashboard/generate-metrics.ts:163 (unresolved)

**@copilot-pull-request-reviewer** (2026-05-11T20:08:15Z):

`mergedToday[0]` is used for `last_merge` / `last_merge_ago`, but `closedPRs` are fetched sorted by `updated`, not by `merged_at`. After filtering, `mergedToday` may not be ordered by merge time, so `last_merge` can be incorrect (e.g., a recently-commented older merge can float to the top). Sort `mergedToday` by `merged_at` descending before using index 0 and before building `recentMerged`.

### Thread 7: tools/dashboard/generate-metrics.ts:6 (unresolved)

**@copilot-pull-request-reviewer** (2026-05-11T20:08:16Z):

Header comment says the generated JSON is something “both the HTML dashboard and agents can read without hitting the GitHub API directly”, but the current `demo/index.html` still fetches GitHub API directly and does not read `demo/metrics.json`. Either update the comment to describe the current behavior/intent, or change the HTML dashboard to consume this JSON so the statement stays true.

### Thread 8: tools/dashboard/generate-metrics.ts:122 (unresolved)

**@copilot-pull-request-reviewer** (2026-05-11T20:08:16Z):

The generated JSON includes many derived “*Ago” fields (e.g., `lastCommitAgo`, `mergedAgo`, `dateAgo`). Those values become incorrect as wall-clock time advances and can confuse consumers unless they are regenerated very frequently. Consider emitting only timestamps (plus the file’s `generated` time) and letting consumers compute “ago”, or at least document that the “ago” fields are relative to `generated`.

### Thread 9: demo/metrics.json:24 (unresolved)

**@copilot-pull-request-reviewer** (2026-05-11T20:08:16Z):

This file contains direct persona/human names in a current-state surface (`demo/`), which conflicts with the repo’s “No name attribution in code, docs, or skills” operational rule (see `docs/AGENT-BEST-PRACTICES.md` around lines 671–728). If `metrics.json` is intended to be public/consumable, prefer role-based identifiers instead of embedding contributor/persona names.

### Thread 10: demo/metrics.json:4 (unresolved)

**@copilot-pull-request-reviewer** (2026-05-11T20:08:17Z):

`demo/metrics.json` appears to be generator output (it includes a real `generated` timestamp and time-relative fields like `"2m ago"`). Checking in a volatile, time-dependent artifact will become stale quickly and create constant diff churn if the generator runs on each tick. Consider (a) not committing this file and generating it at runtime / in a publish workflow, or (b) committing a stable example fixture (renamed to `metrics.example.json`) instead of live data.
