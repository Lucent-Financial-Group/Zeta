# Tick 2026-05-14T18:08Z — PR #3150 thread resolution (filename + user-scope memory clarification)

## Refresh (step 1)

`bun tools/github/poll-pr-gate-batch.ts 3150 3152`:

| PR | gate | required | threads | nextAction |
|----|------|----------|---------|------------|
| #3150 | BLOCKED | 7/7 ok | **2 unresolved** | resolve-threads |
| #3152 | BLOCKED | 7 in-progress | 0 | wait-ci |

PR #3150 CI green but 2 new review threads (Codex + Copilot, both 18:06-18:08Z). Per `blocked-green-ci-investigate-threads.md`: the block is virtually never opaque — investigate.

## Holding discipline (step 2)

Named work: investigate PR #3150 threads per the canonical "BLOCKED with green CI = investigate threads first, don't wait" rule. This is the highest-value substrate work this tick.

## Speculative work (step 3) — actually thread investigation

Both threads flag the same issue in `docs/research/2026-05-14-aaron-ani-grok-extension-manifesto-v2-civsim-arg-layer.md` line 23:

- **Codex** (P2): "Link the referenced Manifesto V2 artifact to a real file" — cited filename does not exist in the repo.
- **Copilot**: same issue + suggests either (a) add the file to the repo with `memory/` prefix, or (b) rewrite to indicate it lives outside the repo.

**Root cause**: the cited filename was `feedback_aaron_ani_root_discipline_manifesto_v2_civsim_play_arg_layer_2026_05_14.md` — but the actual user-scope memory file is `feedback_aaron_ani_root_discipline_manifesto_v2_civsim_play_arg_layer_dbsp_clifford_2026_05_14.md` (with `_dbsp_clifford_` suffix). AND the sentence omitted the user-scope-vs-repo distinction, so reviewers (correctly) read it as a broken in-tree reference.

The memory file is per-agent canonical (user-scope, NOT committed); the §33 archive (this PR) is the durable repo-side preservation. Both exist; the sentence elided the distinction.

## Verify + commit (step 4)

Fix at `bfa74d1`:

1. Updated cited filename to match the actual user-scope memory file.
2. Added explicit **Note on memory-file locations** paragraph clarifying that `feedback_*.md` files live in `~/.claude/projects/-Users-acehack-Documents-src-repos-Zeta/memory/`, NOT in the repo tree. Per-agent canonical vs durable repo-side preservation distinction made explicit.

Prior-art language for "user-scope memory file" exists in earlier §33 archives (e.g., `docs/research/2026-04-26-action-mode-classification-correction-and-self-provenance-accountability-framing.md` uses identical framing).

Pushed `bfa74d1` to `docs/grok-extension-manifesto-v2-archive-2026-05-14` (PR #3150 branch). Both review threads resolved via `gh api graphql` `resolveReviewThread` mutation.

## Shard (step 5)

This file.

## CronList (step 6)

Sentinel `f970cb2d` still armed (every-minute `<<autonomous-loop>>`).

## Visibility (step 7)

- **Landed**: `bfa74d1` on PR #3150 branch; 2 review threads resolved; #3152 (prior-tick razor-cadence shard) CLEAN (7/7 required ok, autoMerge armed → verify-merge ETA imminent).
- **PR #3150 gate state**: BLOCKED → wait-ci on new commit's CI (7 required checks restarted). AutoMerge armed.
- **No bus claim contention** this tick (B-0442 parallel Otto from prior tick still active but on unrelated row).

## Notes for future-Otto

The "blocked-green-ci = investigate threads first" rule continues to pay out. Two threads with identical findings (Codex + Copilot agreeing) is a clean signal: the issue is real, not stylistic. The fix took ~3 minutes from refresh to resolve.

Substrate-honest pattern reinforced: when reviewers flag a "missing file," check whether it's actually user-scope memory (not in the repo) before assuming a typo. Per CLAUDE.md fast-path, user-scope `feedback_*.md` files are first-class substrate but live outside the repo tree — that distinction is easy to elide and worth making explicit in every cross-reference.
