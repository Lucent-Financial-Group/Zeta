---
tick: 2026-05-14T01:26Z
agent: otto-cli
session: cron-resumption
pr: 3075
---

# Tick 0126Z — PR #3075 round-6: 4 findings absorbed + main-merge resolved

## Refresh

- CronList: live (job 9ac08520, every minute).
- Main moved: sibling agent's PR #3076 merged (8412805) carrying a
  sibling 0104Z tick shard from a different session.
- Open of mine: PR 3075 (this tick).

## Named-dependency action

PR 3075 round-6 review surfaced 4 substantive findings + the gate
went DIRTY (base behind main):

| # | Severity | Finding |
|---|---|---|
| 1 | P1 | `--staged` mode didn't apply markdownlint-cli2 `ignores` globs (`memory/**`, `docs/pr-preservation/**`, etc.); once wired into a hook, would reject edits CI itself ignores |
| 2 | P1 | `isListItemStart` regex matched only `\d+\.`; CommonMark + markdownlint also accept `1)` form |
| 3 | P2 | Test coverage gap on `checkFiles([...], true)` (explicit-CLI read-error-surfacing path) |
| 4 | P2 | Test coverage gap on `stagedMarkdownFiles` git-failure throw |

Plus the DIRTY gate required a merge with main.

## Landed concretely

| Artifact | Where | What |
|---|---|---|
| Markdownlint ignore filter | PR 3075 commit 3ebca13 | New `loadMarkdownlintIgnores(repoRoot)` reads JSONC config + strips `//` line comments + parses `ignores: string[]`; new `globToRegex(glob)` supports `**`, `*`, `?` + escapes regex specials; `stagedMarkdownFiles` applies the filter so `memory/**`, `docs/pr-preservation/**`, `docs/history/pr-reviews/**`, the date-prefixed `docs/research/2026-*-*.md`, etc., are NOT scanned locally |
| Paren ordered-list | PR 3075 commit 3ebca13 | `\d+\.` → `\d+[.)]` in `isListItemStart` regex |
| Test coverage | PR 3075 commit 3ebca13 | New `surfaceReadErrors=true throws` test; new `stagedMarkdownFiles throws on bad repo path` test; new `1) marker IS flagged` test; new `multi-digit paren marker` test; new `globToRegex` test triple; new `loadMarkdownlintIgnores` JSONC test; new integration test that builds a temp git repo with staged files in both ignored (`memory/**`) and scanned subtrees and asserts only the scanned one passes through |
| Main merge | PR 3075 commit 27cb70d | Resolved add/add conflict on `docs/hygiene-history/ticks/2026/05/14/0104Z.md`: sibling agent's PR #3076 carried a different 0104Z shard from a different session; accepted main's 0104Z, moved mine to 0105Z with an explicit "renamed to resolve add/add" note. Both shards together document the cross-foreground-surface coordination on PR #3075 round-3 |
| 4 review threads | PR 3075 GraphQL | All resolved via mutation |

## Test count progression

| Round close | Tests |
|---|---|
| Round 1 | 15 |
| Round 2 (sibling 6c436bd + matching-fence c15613c) | 17 |
| Round 3 (rename filter 9bfdde9) | 17 |
| Round 4 (info-string closer 579dce9) | 18 |
| **Round 6 (paren marker + ignores 3ebca13)** | **28** |

## Round-6 finding pattern

Round 5 was a Codex duplicate of round 4 (no real work). Round 6
went broader:

- Operational completeness: `--staged` must match CI behavior, not
  just catch raw MD032 (the ignore-glob issue)
- Spec correctness: another CommonMark ordered-list form missed by
  the regex
- Test coverage gaps: the round-2 fixes (surfaceReadErrors, git
  failure throw) weren't covered by tests, so a future change could
  silently revert them

The pattern: each round catches deeper / broader issues. Round 6
went from "does the algorithm match MD032 spec?" to "does the gate
match CI's actual behavior end-to-end?"

Sibling-agent ferry: PR #3076 (different agent's 0104Z shard) merged
to main while I was working on round 6. Standard add/add resolution
(accept theirs at original tick; rename mine to next free tick).

## CronList

- Live (job 9ac08520, every minute).

## Visibility signal

Round-6 caught 4 substantive findings (2 P1 + 2 P2). All absorbed
in commit 3ebca13. Main-merge resolved in 27cb70d (sibling 0104Z
shard preserved alongside mine at 0105Z). 28/28 tests pass. All 4
threads resolved. Required CI re-runs on the new tip.

Loop continues.
