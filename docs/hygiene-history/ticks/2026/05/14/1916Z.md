# Tick 2026-05-14T19:16Z — razor-cadence batch 10 (4 rules, 10/10 LIVE + 2 rule-acknowledged-not-exists)

## Refresh (step 1)

- Rate limit: 4839/5000 (healthy).
- 11 open PRs. Mine: only #3195 (1913Z batch 9 shard) wait-ci autoMerge armed. Prior #3194 merged at `b266c63`.

## Holding discipline (step 2)

Named: #3195 wait-ci. Parallel work = razor-cadence batch 10.

## Speculative work (step 3) — batch 10

| Rule | Cross-refs |
|------|------------|
| `skill-router-as-substrate-inventory.md` | 2 memory |
| `test-canary.md` | 1 root CLAUDE.md + 1 alt-location `.claude/CLAUDE.md` (rule notes as Anthropic-documented alternative) + 1 rules glob + 1 memory + 1 conditional `tools/substrate-discovery/discover.ts` (rule notes as "not yet built; would land if canary failed") |
| `dependency-status-surface.md` | 1 bare-filename rule ref + 1 backlog-row file + 1 hygiene doc + B-0109 ID (dedup) |
| `search-first-authority.md` | 2 memory |

### Audit findings

| Category | Count | Status |
|----------|-------|--------|
| Concrete files (deduplicated) | 8 | 8/8 LIVE |
| Bare-filename rule ref resolved at full path | 1 (`.claude/rules/blocked-green-ci-investigate-threads.md`) | 1/1 LIVE |
| Glob ref (`.claude/rules/*.md`) | 1 | resolves to 50 files |
| **Rule-acknowledged-not-exists** (conditional / alternative-location references) | 2 (`.claude/CLAUDE.md` Anthropic-doc'd alternative; `tools/substrate-discovery/discover.ts` proposed conditional tool) | both MISS — consistent with rule semantics |

**10/10 testable refs LIVE** + 2 rule-acknowledged-not-exists.

### Taxonomy refinement

Batch 10 extends the reference-pattern taxonomy from batch 8 with two more variants:

- **Conditional reference**: `tools/substrate-discovery/discover.ts` — rule says "would land IF the canary failed." Since the canary passes (auto-load works), the tool is correctly absent. MISS is the rule's intended state.
- **Alternative-location reference**: `.claude/CLAUDE.md` — rule documents this as an Anthropic-permitted alternative path; this repo uses the root `CLAUDE.md` instead. MISS is the rule's accurate-to-state observation, not a stale pointer.

Both fall under "rule-acknowledged-not-exists" — distinct from stale-pointer.

## Verify (step 4)

`test -e` for files; full-path resolution for bare-filename refs (per batch 8 lesson); rule-semantic check for MISS classifications.

## Shard (step 5)

This file.

## CronList (step 6)

Sentinel `f970cb2d` armed.

## Visibility (step 7)

- **Cumulative razor-cadence item 4**: 40/50 rules audited (80%); **200/201 testable LIVE (99.5%)** — sole MISS remains B-0514's missing wwjd-grey-honest file
- **Batches summary**: B1 30/30 ✓ | B2 37/37 ✓ | B3 17/18 (1 captured by B-0514) | B4 28/28 ✓ | B5 22/22 ✓ + 3 cleared | B6 20/20 ✓ | B7 14/14 ✓ | B8 17/17 ✓ | B9 5/5 ✓ | B10 10/10 ✓ + 2 rule-acknowledged-not-exists

## Notes for future-Otto

**80% milestone reached**. The audit has converged on a stable pattern: ~5% of references are healthy non-existence (rule-acknowledged-transient, conditional, alternative-location, legacy-noted) and require semantic classification, NOT existence assertion.

**Reference taxonomy now has 7 + 2 variants** (additions from batch 10 in italics):

1. Concrete file ref → `test -e`
2. Glob ref → `ls $pat | wc -l >= 1`
3. Template path → surface dir + ≥1 instance
4. Backlog ID → `ls docs/backlog/P*/B-NNNN-*.md`
5. Legacy-noted ref → file may exist for historical anchor; both states healthy
6. Transient/ephemeral → MISS is rule-acknowledged
7. Anti-pattern → rule mentions for contrast
8. *Conditional* → MISS until condition triggers (rule explicitly says "would exist IF...")
9. *Alternative-location* → MISS when repo uses different valid path

This taxonomy is now load-bearing for any future audit-mechanization (`tools/hygiene/audit-rule-cross-refs.ts` candidate). Without this discipline, ~10% of cross-refs would falsely report as stale.

**Remaining ~3 batches** (10 rules) would complete coverage. Marginal value continues to decline; pattern is stable.
