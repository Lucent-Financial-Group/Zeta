---
tick: 2026-05-14T01:52Z
agent: otto-cli
session: cron-resumption
pr: 3075
---

# Tick 0152Z — PR #3075 round-11: 1 real (in-test require) + 3 stale-snapshot duplicates

## Refresh

- CronList: live (job 9ac08520, every minute).
- Main unchanged since 0148Z (b6ac507).
- Open of mine: PR 3075 (this tick).

## Named-dependency action

PR 3075 round-11 review (Copilot) surfaced 4 findings — most
duplicates against a stale snapshot:

| # | Severity | Finding | Status |
|---|---|---|---|
| 1 | P1 | In-test `require("node:child_process")` violates `@typescript-eslint/no-require-imports` | REAL — fixed in commit 8498772 |
| 2 | P2 | PR description says 18 tests (file has 43) | DUPLICATE of round-10 thread 4; PR body already refreshed |
| 3 | P1 | Tilde fence with tilde in info string is valid | DUPLICATE of round-10 thread 1; fixed in a86a524 |
| 4 | P1 | Tilde-with-tilde test fixture | DUPLICATE of round-10 thread 3; inverted in a86a524 |

## Landed concretely

| Artifact | Where | What |
|---|---|---|
| `require` → module-scope import | PR 3075 commit 8498772 | Hoisted `spawnSync` to the existing `import { spawnSync } from "node:child_process"` at the top of the test file; replaced in-body `require()` call + cast + `spawn` alias with direct `spawnSync` references |
| 4 review threads | PR 3075 GraphQL | All resolved — 1 via the code fix, 3 via verification that round-10 commit a86a524 + body edit already addressed them |

## Stale-snapshot pattern

3 of 4 round-11 threads were duplicates of round-10 findings. The
review queue is firing against snapshots from before my round-10
push (a86a524 at 01:43Z, round-11 review at 01:49:31Z — about a
6-minute gap, but the review queue ran against an earlier head).

Cumulative stale-snapshot count this session:

- Round 5 (1 duplicate of round 4 thread 1)
- Round 7 (1 duplicate of round 6 thread 1)
- Round 9 (1 duplicate of round 8 thread 2)
- Round 11 (3 duplicates of round 10 threads 1, 3, 4)

The pattern is consistent — async review queues + iterative pushes
produce stale-snapshot duplicates. Substrate-honest resolution:
verify the fix exists in git via commit reference + resolve the
thread.

## Test count

| Round | Tests | Delta |
|---|---|---|
| 9 | 43 | — |
| 10 | 45 | +2 (fence-inside-list, -0 net) |
| **11 (commit 8498772)** | **45** | **+0 (lint fix only)** |

## CronList

- Live (job 9ac08520, every minute).

## Visibility signal

Round 11: 1 real lint fix (in-test `require` → module-scope import) +
3 stale-snapshot duplicates of round-10 findings. All 4 threads
resolved. 45/45 tests pass. Required CI re-runs on commit 8498772.

Loop continues.
