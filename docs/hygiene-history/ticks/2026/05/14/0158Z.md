---
tick: 2026-05-14T01:58Z
agent: otto-cli
session: cron-resumption
pr: 3075
---

# Tick 0158Z — PR #3075 CI: 1 markdownlint MD038 in 0143Z shard, fixed

## Refresh

- CronList: live (job 9ac08520, every minute).
- Main unchanged since 0148Z (b6ac507).
- Open of mine: PR 3075 (this tick).

## Named-dependency action

PR 3075 required-check `lint (markdownlint)` failed. Investigated:

```
docs/hygiene-history/ticks/2026/05/14/0143Z.md:31:88
  error MD038/no-space-in-code Spaces inside code span elements
  [Context: "`> `"]
```

Round-9 shard had `` `> ` `` (a code span containing `>` then a
space). Markdownlint MD038 flags spaces inside code spans as
decorative — the trailing space is rendered ambiguously.

Fixed in commit 3f3da2b by rewording the description in prose form
(no code span needed since the `>` marker is mentioned in the
surrounding text already).

## Pattern note

This is the 4th markdownlint rule class hit during the PR #3075
session (after MD032 — the target — plus the helper's own scope
gaps caught by reviewers). MD038 + MD018 + MD056 are out of scope
for B-0456 but worth captured follow-up rows. The MD038 case here
was a one-off (a single code span in one shard); not worth a
helper.

## CronList

- Live (job 9ac08520, every minute).

## Visibility signal

PR 3075 required-check markdownlint failure (MD038 in 0143Z shard)
absorbed in commit 3f3da2b. Required CI re-runs on the new tip.
Real-dependency-wait.

Loop continues.
