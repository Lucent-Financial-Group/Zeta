---
tick: 2026-05-14T02:14Z
agent: otto-cli
session: cron-resumption
pr: 3075
---

# Tick 0214Z — DIRTY-gate merge resolved (0154Z add/add); 2nd of session

## Refresh

- CronList: live (job 9ac08520, every minute).
- Main moved: PR #3087 merged (a558f6d) — sibling 0154Z tick shard
  re: PR #3086 MD056 + PR #3075 thread clearance.
- Open of mine: PR 3075 (this tick).

## Named-dependency action

PR 3075 gate went DIRTY (base behind main). `update-branch` API
rejected with 422 "merge conflict between base and head."

Investigated: add/add conflict on `docs/hygiene-history/ticks/
2026/05/14/0154Z.md`. Sibling agent's PR #3087 carried a different
0154Z shard from a different session (theirs: PR #3086 MD056 fix +
PR #3075 thread status; mine: PR #3075 wait-ci status).

Resolution (same pattern as round-6 0104Z conflict):

1. Accepted main's 0154Z (sibling's content)
2. Moved my 0154Z to 0155Z with explicit rename note
3. Both shards preserved alongside; rename annotation cites the
   sibling PR + the rename reason

Merge commit `1c43065` (regular merge, no squash) lands the
sibling's content + my 0155Z addition + all my prior commits.

## Pattern note (2nd this session)

This is the SECOND DIRTY-gate add/add conflict on a tick shard this
session:

- Round 6 (commit 27cb70d): 0104Z conflict with PR #3076 →
  resolution: accept main's, move mine to 0105Z
- This tick (commit 1c43065): 0154Z conflict with PR #3087 →
  resolution: accept main's, move mine to 0155Z

The recurring pattern: my session writes one tick shard per minute
while sibling Otto-Desktop session writes another tick shard at the
same minute. Both PR up. When my PR rebases, both shards collide.

Substrate-honest resolution preserves BOTH shards (different
sessions captured complementary aspects of the same minute).
Per `.claude/rules/honor-those-that-came-before.md`, this is the
right shape.

A future micro-discipline could be: include sender-tag in the
tick-shard filename (e.g., `0154Z-otto-cli.md` vs `0154Z-otto-
desktop.md`). Out of scope for this PR; captured as a follow-up.

## CronList

- Live (job 9ac08520, every minute).

## Visibility signal

DIRTY-gate add/add merge resolved (commit 1c43065). My 0154Z
moved to 0155Z to preserve the sibling agent's 0154Z. Required CI
re-runs on the merge tip. Real-dependency-wait.

Loop continues.
