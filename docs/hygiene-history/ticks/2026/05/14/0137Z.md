---
tick: 2026-05-14T01:37Z
agent: otto-cli
session: cron-resumption
pr: 3075
---

# Tick 0137Z — PR #3075 round-8: 4 CommonMark spec-grade findings

## Refresh

- CronList: live (job 9ac08520, every minute).
- Main unchanged since 0130Z (dbf0374).
- Open of mine: PR 3075 (this tick).

## Named-dependency action

PR 3075 round-8 review (Copilot) surfaced 4 CommonMark spec-grade
findings:

| # | Severity | Finding |
|---|---|---|
| 1 | P1 | Unindented lazy-continuation between two bullets falsely split the list (`- a\ncontinued\n- b`) — second bullet flagged as new list with bad predecessor |
| 2 | P1 | `\d+[.)]` accepted 10+ digit numeric prefixes; CommonMark caps ordered-list markers at 9 digits |
| 3 | P1 | Fenced opener accepted info strings containing the fence character (` ```ts\`bad `); per CommonMark these are NOT valid openers |
| 4 | P2 | `spawnSync` launch failures (git not on PATH, EACCES) surface only "unknown error"; `r.error.message` has the actionable cause |

## Landed concretely

| Artifact | Where | What |
|---|---|---|
| Lazy-continuation fix | PR 3075 commit acd8ff8 | Two-part: (a) `else` branch leaves `inList` unchanged — only blank/heading/fence ends a list per CommonMark; (b) list-item branch only checks predecessor when `!inList` (entering a new list, not a sibling/nested member). |
| 9-digit cap | PR 3075 commit acd8ff8 | Regex `\d+[.)]` → `\d{1,9}[.)]` |
| Fence info-string validation | PR 3075 commit acd8ff8 | `fenceInfo` returns `null` when trailing run contains the fence character |
| spawnSync.error surfacing | PR 3075 commit acd8ff8 | `r.error?.message ?? r.stderr?.trim() ?? "unknown error"` in the throw |
| 6 new tests | PR 3075 commit acd8ff8 | Lazy continuation, 10-digit reject, 9-digit boundary, backtick-with-backtick reject, tilde-with-tilde reject, valid info-string control |
| 4 review threads | PR 3075 GraphQL | All resolved via mutation |

## Test count

| Round close | Tests |
|---|---|
| Round 6 | 28 |
| Round 7 | 32 (+4 YAML front matter) |
| **Round 8 (this commit acd8ff8)** | **38** (+6 CommonMark edge cases) |

Validation: helper run across all 14/05/14 tick shards (31 files)
returns clean.

## Round-8 pattern

Round 8 was spec-grade CommonMark rigor:

- Lazy-continuation: the algorithm now tracks `inList` as a sticky
  state that only ends on blank/heading/fence, matching CommonMark's
  list-block grammar
- 9-digit cap: a small but real CommonMark constraint
- Fence info-string: the parser now rejects info strings containing
  the fence character per CommonMark spec
- spawnSync.error: operational diagnostics

Each finding was a real spec-correctness or operational-diagnostics
issue, not just style. The cumulative effect across rounds 1-8: the
helper now closely matches markdownlint's MD032 behavior across:

- list markers (`-` `+` `*` `1.` `1)` `12.` `12)`, capped 9 digits)
- list-item indent (0-3 spaces; 4+ is code)
- fence delimiters (` ``` ` and `~~~`, opener vs closer distinction,
  matching delim char + length, info-string validation)
- YAML front matter (skipped per markdownlint behavior)
- markdownlint-cli2 ignore globs (applied via JSONC parse)
- list-block continuations (indented AND lazy unindented)
- heading-not-list-friendly (per markdownlint MD032 strict)

## CronList

- Live (job 9ac08520, every minute).

## Visibility signal

Round 8: 4 substantive CommonMark spec-grade findings (3 P1 + 1 P2).
All absorbed in commit acd8ff8. 38/38 tests pass. All 4 threads
resolved. CI re-runs.

Loop continues.
