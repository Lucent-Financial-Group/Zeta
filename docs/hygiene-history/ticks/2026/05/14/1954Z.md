# Tick 2026-05-14T19:54Z — B-0519 multi-Otto branch-state contamination RCA filed

## Refresh (step 1)

- Rate limit: **4984/5000** (fresh window — limit just reset to 5000).
- 13 open PRs. Mine: #3211 (1942Z shard, threads now resolved, wait-ci armed), #3213 (Aaron's day-close summary, wait-ci armed).
- #3212 (audit-cadence workflow) merged.

## Holding discipline (step 2)

Named: #3211 + #3212 + #3208 + #3202 audit-cadence chain is end-to-end shipped. Audit infrastructure work is durably complete. Aaron's day-close summary in flight (#3213). Per never-be-idle: pick parallel low-risk substrate work.

## Speculative work (step 3) — B-0519 filed (multi-Otto branch-state RCA)

This session encountered **~6 instances of branch-state contamination**: after `git checkout origin/main` or `git checkout -B <new-branch>`, the working tree silently ended up on a different parallel-Otto local branch (e.g., `claim/fix-3183-markdownlint-copilot-...`, `chore/b-0367-...`, `chore/b-0516-gates-ecc-...`, `chore/b-0518-sharpen-holding-...`).

Each instance cost 30s-2min of recovery time + at least one B-NNNN ID-collision renumber (B-0516 → B-0517).

Filed **B-0519** at `docs/backlog/P3/B-0519-multi-otto-branch-state-contamination-rca-2026-05-14.md` documenting:

- All 7 shard touchpoints (1813Z, 1834Z, 1851Z, 1856Z, 1905Z, 1908Z, 1927Z)
- 4 RCA hypotheses (shared local-branch namespace, stash auto-pop, worktree-directory mismatch, implicit branch switching)
- Workaround procedure (`git checkout -f -B <local-name> origin/<remote>` or `git checkout -f origin/main` + fresh `-B`)
- Mechanization candidates (cheap: pre-commit hook augmenting `ZETA_EXPECTED_BRANCH`; substantial: per-Otto-process worktree isolation)
- Composition with B-0400 (bus protocol), B-0444 (worktree field), B-0506 (worktree-prune cadence)

## Verify (step 4)

ID-allocation discipline: on-disk top = B-0518; in-flight check for B-0519/B-0520/B-0521 returned empty. B-0519 safe.

## Shard (step 5)

This file.

## CronList (step 6)

Sentinel `f970cb2d` armed.

## Visibility (step 7)

- **B-0519 filed**: pattern captured as P3 backlog row; workaround documented; mechanization candidates noted
- **Audit infrastructure end-to-end shipped this session**: 2 audit tools + 1 daily GHA workflow + 3 supporting backlog rows + the 9-variant taxonomy
- **Mine in-flight**: #3211 wait-ci armed (will merge any moment); #3212 merged earlier this tick

## Notes for future-Otto

**Session-winding-down state**: Aaron's #3213 day-close summary signals natural close. Otto-CLI's audit infrastructure work compounded substantively over ~30 ticks (50 PRs authored, 2 tools shipped, 1 workflow wired, 4 backlog rows filed including B-0506/B-0514/B-0517/B-0519, full reference-classification taxonomy emergent).

**The multi-Otto branch-state contamination pattern is the most-recurring friction**. Each instance is recoverable with the `-f -B` flag pattern but the cumulative cost is non-trivial. The cheap mechanization (auto-export of `ZETA_EXPECTED_BRANCH` from `git checkout -b` calls) is the next obvious leverage point if the pattern continues.

**Rate-limit window roll-over** observed this tick: counter reset from 4647 to 4984 between 1946Z and 1954Z (mid-session reset). User-token limit is independent of CI runner mise limits; both reset on their own clocks.
