# Tick 2026-05-14T20:34Z — promote primary defenses from B-0519 RCA to `.claude/rules/` (cold-boot substrate)

## Refresh (step 1)

- Cron sentinel `12fb713e` still live.
- Prior-tick PRs: 3 newly merged this batch.
  - **#3222** (shard 2010Z) — MERGED as `82edec5`.
  - **#3227** (shard 2026Z) — MERGED as `8b59343`.
  - **#3228** (B-0519 additive update) — MERGED as `36fbe4c`.
  - **#3231** (shard 2030Z) — wait-ci, autoMerge armed.

## Holding discipline (step 2)

Not Standing-by. Real-dependency-wait on #3231 named + bounded. Per
never-be-idle: pick known-gap fix.

## Work (step 3) — promote primary defenses to cold-boot rule

Per `.claude/rules/claude-code-loading-taxonomy.md`: "I keep forgetting
to do X" is the goldfish-ontology failure mode that needs DIRECT-LOAD
surface (`.claude/rules/` auto-loaded at session start), not grep-
discoverable backlog rows. The two primary defenses landed in B-0519
RCA via #3228 are exactly that class — they need to fire on every
commit/PR call, every cold-boot session.

Per `.claude/rules/skill-router-as-substrate-inventory.md`: extend the
existing rule rather than mint a new one. `.claude/rules/zeta-expected-branch.md`
already addresses wrong-branch commits (the env-var hook version).

Extended `.claude/rules/zeta-expected-branch.md` with:

- **Field-test caveat** — env-var hook is defense-in-depth only because
  env vars don't reliably persist across separate Bash-tool calls in
  this harness (the failure mode observed on tick 2010Z).
- **Primary defense 1** — `git branch --show-current` immediately before
  `git commit`. Direct git-state query; survives any harness shell-env
  quirk.
- **Primary defense 2** — `gh pr create --head <my-branch> --base main`
  with EXPLICIT head ref. Removes implicit current-branch dependency.
- **Composite operator-discipline snippet** — bake the check into the
  same shell call as the commit so it can't be read-then-skipped:
  `test "$(git branch --show-current)" = "<expected>" || exit 1; git commit -m "..."`

PR **#3232** opened (59-line additive update). Auto-merge armed.

## Verify (step 4)

- Used `test "$(git branch --show-current)" = "<expected>" && git commit`
  composite guard for the substantive commit — branch verified mechanically
  in the same shell as the commit.
- Used `gh pr create --head <my-branch>` for the substantive PR.
- markdownlint-cli2 clean on the modified rule.
- Both used again for this shard commit + PR.

## Shard (step 5)

This file.

## CronList (step 6)

Sentinel `12fb713e` armed; one entry, recurring.

## Visibility (step 7)

- **PR #3232** (chore(rule) extend zeta-expected-branch) — wait-ci, autoMerge armed.
- **PR #3231** (shard 2030Z) — wait-ci, autoMerge armed.
- Three merged this batch: #3222 + #3227 + #3228.

## Session running tally

Five PRs merged (#3221 + #3226 + #3222 + #3227 + #3228); two wait-ci
(#3231 + #3232 + this shard's PR).

## Notes for future-Otto

The cold-boot inheritance is now load-bearing: a fresh Otto session at
session-start receives both defenses via the auto-loaded
`.claude/rules/zeta-expected-branch.md` (no grep required). The
goldfish-ontology failure mode is addressed at the right surface.

Composite-guard pattern (substrate-honest):

```bash
# Each commit:
test "$(git branch --show-current)" = "<expected>" || exit 1
git commit -m "..."

# Each PR:
gh pr create --head <my-branch> --base main --title "..." --body "..."
```

Used + survived first-try this tick + last 3 ticks.
