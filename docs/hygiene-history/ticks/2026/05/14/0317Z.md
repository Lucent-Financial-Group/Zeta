---
tick: 2026-05-14T03:17Z
agent: otto-cli
session: cron-resumption
pr: 3075
---

# Tick 0317Z — PR #3075 round-1 review: 4 substantive findings

## Refresh

- CronList: live (job 9ac08520, every minute).
- Main unchanged since previous tick.
- Open of mine: PR 3073 (wait-ci, threads-clear), PR 3075 (4
  new Codex+Copilot threads this tick).

## Named-dependency action

PR 3075 round-1 review surfaced 4 substantive findings, all real:

| Reviewer | Severity | Finding |
|---|---|---|
| Codex | P2 | Fenced code blocks ignored — list-like lines inside code samples would be flagged |
| Copilot | P0 | `checkFiles(files, repoRoot)` has unused `repoRoot`; `noUnusedParameters` would fail typecheck |
| Copilot | P2 | `checkFiles` docstring says "emits stderr / returns count" but actually returns array |
| Copilot | P1 | `^\s*` too greedy — CommonMark caps list-marker indent at 0-3 spaces |

## Landed concretely

| Artifact | Where | What |
|---|---|---|
| `isFenceLine` helper | PR 3075 commit 3158f6a | New: detects fenced code openers/closers |
| `findMd032Violations` | PR 3075 commit 3158f6a | Added `inFencedCode` state walked alongside line scan; fence-line toggle before list-start check |
| `isListItemStart` | PR 3075 commit 3158f6a | Tightened `^\s*` to `^ {0,3}` (CommonMark spec) |
| `checkFiles` signature | PR 3075 commit 3158f6a | Dropped unused `repoRoot` param; updated docstring to match actual contract |
| 3 new tests | PR 3075 commit 3158f6a | "list-like lines inside fenced code blocks", "tilde-fenced blocks", "indent-as-code 4+ spaces is NOT a list marker" |
| 4 review threads | PR 3075 GraphQL | Resolved via mutation |

## Test count

Before round-1: 12 tests pass.
After round-1: **15 tests pass** (+3 covering the new edge cases).

## Why round-1 caught these

Round-1 review on a TS helper landed in roughly the same shape as
PR 3066's round-1 review on the procedure memo: factual bugs +
P0 (will fail typecheck) + P1 (CommonMark spec correctness) + P2
(stale documentation). Each round-1 catches "the obvious gaps the
author missed" — different per artifact but consistent pattern.

The fenced-code-block one is especially valuable: future docs that
SHOW the bad MD032 pattern (for teaching/example purposes) would
have been falsely flagged. The helper now respects code-sample
context.

## CronList

- Live (job 9ac08520, every minute).

## Visibility signal

Session arc:

- 27 PRs merged + 1 closed-with-provenance
- 2 of mine in flight (PR 3073 = cascade-terminal, PR 3075 = B-0456 helper)
- Round-1 caught 4 substantive findings on the helper; all
  resolved + 3 new tests added
- 15/15 tests pass on the helper
- Real-dependency-wait on both PRs

Loop continues.
