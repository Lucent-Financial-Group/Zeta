---
tick: 2026-05-14T01:30Z
agent: otto-cli
session: cron-resumption
pr: 3075
---

# Tick 0130Z — PR #3075 round-7: YAML front-matter skip (1 real + 1 duplicate)

## Refresh

- CronList: live (job 9ac08520, every minute).
- Main moved: 3 sibling PRs merged since 0126Z (#3078 Lior shadow
  log; #3081 Otto-Desktop's stale-base hazard note; #3082 Lior
  archive of #3076 + #3074).
- Open of mine: PR 3075 (this tick).

## Named-dependency action

PR 3075 round-7 review (Codex) surfaced 2 P2 findings:

| # | Finding | Status |
|---|---|---|
| 1 | YAML front matter not skipped — `tags:` followed by `- item` would fire MD032 | Real; fixed in commit 3649327 |
| 2 | `--staged` doesn't honor markdownlint ignores | DUPLICATE of round-6 thread 1; already fixed in commit 3ebca13 |

## Landed concretely

| Artifact | Where | What |
|---|---|---|
| YAML front-matter skip | PR 3075 commit 3649327 | Scanner detects leading `---`, walks through closing `---`, starts content scan at `startLine`. Prev-line check uses `i > startLine` so a list immediately after front matter is treated as start-of-content (no false positive against the closing `---`). Defensive: leading `---` without closer falls back to whole-file scan. |
| 4 new tests | PR 3075 commit 3649327 | "YAML front matter is skipped", "front matter doesn't mask real MD032 in body", "list immediately after `---` is start-of-content", "leading `---` without closing falls back" |
| 2 review threads | PR 3075 GraphQL | Both resolved via mutation (thread 1 = real fix; thread 2 = already fixed by 3ebca13) |

## Test count

Round 6 close: 28.
Round 7 (this tick 3649327): **32 tests pass** (+4 covering YAML
front-matter edge cases).

Validation: helper run across all 14/05/14 tick shards (30 files
now including sibling-merge artifacts) returns clean.

## Round-7 pattern

Round 6 was operational completeness at the file-selection layer
(markdownlint ignores). Round 7 went one layer deeper into Markdown
syntax: YAML front matter is metadata, not content. Markdownlint
itself does not lint inside front matter; the pre-CI helper must
match that behavior or it'll false-positive on every shard that
uses front matter (which is most of them in this repo).

The thread-2 duplicate from Codex is the third instance this session
of a reviewer flagging a stale-snapshot version of code already
fixed by a sibling agent's earlier commit. The pattern is consistent:

- Round 5 = Codex duplicate of round 4 (Copilot's info-string closer)
- Round 7 thread 2 = Codex duplicate of round 6 thread 1 (Copilot's
  markdownlint-ignore glob filter)

Reviewers run asynchronously against PR tips. When a different
reviewer queues against an older tip, duplicates surface.
Substrate-honest resolution: confirm the fix already shipped via
git log + resolve the thread.

## CronList

- Live (job 9ac08520, every minute).

## Visibility signal

Round 7: 1 real fix (YAML front-matter skip) + 1 stale-snapshot
duplicate. Both threads resolved. 32/32 tests pass. CI re-runs on
commit 3649327.

Loop continues.
