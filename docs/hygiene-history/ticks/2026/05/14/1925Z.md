# Tick 2026-05-14T19:25Z — razor-cadence item 4 mechanization shipped

## Refresh (step 1)

- Rate limit: 4795/5000 (healthy).
- 13 open PRs. Mine: #3200 (1920Z final batch shard) wait-ci, autoMerge armed.

## Holding discipline (step 2)

Razor-cadence item 4 (composes-with audit) is functionally complete after the 12-batch manual audit. Next-best work per never-be-idle priority 1 (known-gap fixes): **ship the mechanization tool**. The audit's load-bearing artifact (9-variant reference-classification taxonomy) is well-developed; turn the manual procedure into a TS tool that future ticks invoke instead of repeating the manual audit.

## Speculative work (step 3) — mechanization shipped

`tools/hygiene/audit-rule-cross-refs.ts` (first slice, Layer A mechanical only):

- Scans all `.claude/rules/*.md`
- Pulls backtick'd path refs + `B-NNNN` IDs (dedup'd)
- Tests existence via direct path + glob + per-row backlog file lookup
- Reports stale-pointer candidates as a markdown table

Includes 12-test test suite at `tools/hygiene/audit-rule-cross-refs.test.ts` — all pass (22 `expect` calls).

### First-run validation

```
Rules scanned: 50
References pulled: 281
Resolved: 239
Stale-pointer candidates: 42
```

Tool correctly catches B-0514's genuine stale pointer (validates against the manual audit's single finding).

The 42-candidate count is higher than the manual audit's 1-defect result because Layer B (semantic classification via the 9-variant taxonomy) is out of scope for this first slice. Most candidates are:

- Bare-filename refs needing path prefix (e.g., `claude.ts` → `tools/peer-call/claude.ts`)
- RANE refs (rule-acknowledged-not-exists per the taxonomy)

Future Layer B work would add prose-context-aware classification to reduce false positives.

## Verify (step 4)

- `bun test tools/hygiene/audit-rule-cross-refs.test.ts` → 12 pass / 22 `expect` calls
- CLI run produces correctly-shaped markdown report
- Tool catches the known B-0514 stale pointer

Shipped as PR #3202 with auto-merge armed.

## Shard (step 5)

This file.

## CronList (step 6)

Sentinel `f970cb2d` armed.

## Visibility (step 7)

- **Razor-cadence item 4 mechanization shipped**: PR #3202 (`tools/hygiene/audit-rule-cross-refs.ts` + test suite)
- **Composes with**: B-0506 (worktree-prune cadence), razor-cadence #3128 daily fire, `encoding-rules-without-mechanizing.md`
- **State of mine in-flight**: #3200 (final batch shard) CLEAN verify-merge; #3202 (mechanization) wait-ci armed

## Notes for future-Otto

**Mechanization scope ladder**: this slice ships Layer A (mechanical existence checks). Layer B (semantic 9-variant taxonomy classification) is the natural follow-up but requires prose-context parsing per reference — substantially more complex. The first slice is useful TODAY (replaces per-tick manual work) even without Layer B.

**Wire-up candidates** for future ticks:

- Daily GitHub Actions cron: `bun tools/hygiene/audit-rule-cross-refs.ts --report drift-report.md` + post to razor-cadence issue
- Per-tick run as part of refresh: surface the candidate count
- Pre-PR check via harness hook for `.claude/rules/*.md` edits

None of these were filed this tick — they're natural follow-ups once the tool stabilizes.

**Encoding-rules-without-mechanizing satisfied**: the 12-batch manual audit produced "a memory of failures, not prevention" per the rule's warning. This tool turns it into prevention.
