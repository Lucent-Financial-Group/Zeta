---
tick: 2026-05-14T01:48Z
agent: otto-cli
session: cron-resumption
pr: 3075
---

# Tick 0148Z — PR #3075 round-10: tilde-fence relaxation + fence-keeps-list-context

## Refresh

- CronList: live (job 9ac08520, every minute).
- Main moved: PR #3084 merged (b6ac507 — sibling 0120Z shard
  re: MD032 self-fix + PR #3082 catastrophic-flag + PR #3075
  rebase-deferred note).
- Open of mine: PR 3075 (this tick).

## Named-dependency action

PR 3075 round-10 review (Copilot) surfaced 4 findings:

| # | Severity | Finding |
|---|---|---|
| 1 | P1 | Round-8's "tilde-fence info-string can't contain tilde" was over-corrected; CommonMark only restricts backtick fences. Tilde fences allow any info string including `~`. |
| 2 | P1 | Fence transitions reset `inList = false`; this loses the surrounding list context for an indented (or even flush-left) fence inside a list-item body. The sibling bullet after fence-close looks like a new list with non-blank predecessor. |
| 3 | P1 | Round-8 test (`tilde fence with tilde in info string is NOT a valid opener`) locks in the incorrect behavior; must be inverted to clean. |
| 4 | P2 | PR description said 18 tests; file has 45. |

## Landed concretely

| Artifact | Where | What |
|---|---|---|
| Tilde-fence relaxation | PR 3075 commit a86a524 | `fenceInfo` now rejects fence-char in tail ONLY when `char === "\`"`; tilde fences accept any info string per CommonMark |
| Fence keeps list context | PR 3075 commit a86a524 | Removed both `inList = false` resets on fence open + fence close; list context survives a fence (fence-inside-list-item case stops being a false positive) |
| Test inversion | PR 3075 commit a86a524 | Round-8 tilde-with-tilde test now asserts CLEAN (not flagged); content updated to fully-formed tilde-fenced block with list-like sample inside |
| 2 new tests | PR 3075 commit a86a524 | Indented fence inside list-item-body is clean; flush-left fence between bullets (lazy form) is clean |
| PR body refresh | PR 3075 GraphQL edit | 18 → 45 test count; added round-by-round trail (1–10); added coverage matrix |
| 4 review threads | PR 3075 GraphQL | All resolved via mutation |

## Test count

| Round | Tests | Delta |
|---|---|---|
| 8 | 38 | — |
| 9 | 43 | +5 (blockquote contexts) |
| **10 (commit a86a524)** | **45** | **+2 fence-inside-list, -0 net (one test inverted)** |

## Round-10 pattern

Round 10 was self-correcting on round 8:

- Round 8 added "fence-char rejected in info string" but the rule applies ONLY to backtick fences per CommonMark — tilde fences are unrestricted. Refined.
- Round 8's fence-state reset `inList = false` was correct for clarifying top-level transitions but wrong for fence-inside-list-item — that's a child block, not a list terminator. Refined.

The pattern: spec-grade rigor sometimes over-shoots, and the next round catches the over-shoot. Each iteration nudges the helper closer to markdownlint's actual MD032 behavior across the long-tail of CommonMark edge cases.

Trade-off accepted: with fence transitions no longer resetting
`inList`, a top-level fence between two bullet lists won't fire
MD032 locally on the second list. CI may still catch that case;
the conservative direction is to avoid false positives blocking
valid code over false negatives that defer to CI.

## CronList

- Live (job 9ac08520, every minute).

## Visibility signal

Round 10: 3 P1 (self-corrections on round-8 over-shoots + 1 PR-
body refresh). All 4 threads resolved. 45/45 tests pass. Required
CI re-runs on commit a86a524.

Loop continues.
