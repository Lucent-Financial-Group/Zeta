| 2026-05-01T04:20:00Z | opus-4-7 / autonomous-loop tick | 98fc7424 | CI-blocker fix tick — `check docs/BACKLOG.md generated-index drift` was failing on previous tick's PRs (#1012 B-0127, #1015 B-0128). Root cause: pre-existing drift on main from B-0124/B-0125/B-0126 merging without YAML frontmatter (the format that `tools/backlog/generate-index.sh` reads). The generator produced empty `**[](path)**` entries; committed BACKLOG.md omitted those rows; my new PRs surfaced the drift as a check-failure. Fix: hygiene PR #1016 — additive frontmatter blocks (`id`/`priority`/`status`/`title`) above each existing `# heading` (no body content changed), then regenerated `docs/BACKLOG.md`. For B-0126 specifically: title field uses generic *"a sibling repo"* phrasing per `feedback_no_copy_only_learning_from_sibling_repos_aaron_2026_04_30.md` (sibling-repo internals stay private); the body keeps its un-scrubbed exemplar status per Aaron 2026-05-01 *"leave your mistake"* + *"we should leave this one even then"*. Filename also unchanged — path-leak stays as evidence. Once #1016 merges, #1012 + #1015 rebase to add their own row's frontmatter on top. Cron 98fc7424 healthy. | [PR #1016 (this fix)] / [PR #1012 BLOCKED CI] / [PR #1015 BLOCKED CI] / [PR #1013 + #1014 awaiting checks] | Class-level observation: when filing new backlog rows under heading-only format, future-Otto must include YAML frontmatter on first commit — the integrity check fails immediately otherwise. The `tools/backlog/generate-index.sh` schema (id/priority/status/title) is the single source of truth; the markdown heading is for human readers. This is a process-rule that should be substrate-encoded — candidate B-NNNN row or memory file, but per receipt-energy hazard (§39), this tick already produced two substrate landings (#1015 + #1016) so the rule lands captured-only here. Future-Otto reading: if you file a backlog row and the integrity check fails, run `BACKLOG_WRITE_FORCE=1 ./tools/backlog/generate-index.sh && ./tools/backlog/generate-index.sh --check` after adding frontmatter. Five-second loop. |
