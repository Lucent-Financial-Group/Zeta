| 2026-05-01T09:51:00Z | opus-4-7 / autonomous-loop tick | 98fc7424 | PR #966 thread-drain + DIRTY-rebase tick — drained 6 threads on the B-0122 peer-call-TS-migration backlog row PR. PR #966 was DIRTY/CONFLICTING; rebased against latest main (BACKLOG.md autogen conflict; take-theirs + regen — fifth canonical application this session). Three classes of fix: (1) **Otto-215 user-scope-only reference (P1+P2, lines 174-175, real fix)** — referenced memory file lives only in user-scope `~/.claude/projects/<slug>/memory/`, not promoted to in-repo per 2026-04-24 natural-home directive. Rewrote as lineage-only reference with explicit user-scope-path callout. **New sub-class: user-scope-only-reference / Real-fix subclass** — when a substrate doc cross-references a memory file that exists only in user-scope, the corrective is annotation (not deletion), since the lineage is real but the file hasn't been promoted yet. (2) **Markdownlint MD004 ul-style (P0, line 131, real fix)** — list continuation starting with `+` was read as different bullet marker; reworded `+ bun invocation pattern` → `and bun invocation pattern`. (3) **Stale TS-claim (P1, line 75, real fix)** — row text said "these aren't TS yet" but `codex.ts/grok.ts/gemini.ts` exist on branch (verified via `ls`). Added "Partial-migration update (post-row-filing)" block clarifying scope shifted from initial-port to **cutover** (delete .sh; retire parallel maintenance). Two outdated B-0121 threads — B-0121 landed during this drain wave; references now valid; resolve-as-outdated. All 6 threads resolved via GraphQL. PR #966 substrate; auto-merge intentionally unarmed. Cron 98fc7424 healthy. | [PR #966: 1 commit (e128696) addressing 3 real findings + 6 thread-resolutions; rebased + force-with-lease pushed; auto-merge unarmed] | The user-scope-only-reference class is structurally interesting because it reveals **substrate-promotion lag** — Otto-215 was authored as a memory file but never promoted to in-repo per the natural-home directive. The B-0122 row referenced it as if it were already in-repo. The corrective discipline is dual: annotate the reference (this PR) AND eventually promote the user-scope file (separate, future row). This is a different shape from forward-reference (file exists in another in-flight PR) — it's "file exists but in a different lifecycle state". Future-Otto: when a cross-reference target is missing, the verification cascade should be: (a) on `origin/main` (in-repo, ready); (b) on an in-flight branch (forward-reference); (c) in user-scope only (substrate-promotion lag); (d) hallucinated (doesn't exist anywhere). Each shape has a different fix. The stale-TS-claim is the third instance of **shipped-state-overtook-the-doc** class this session — the substrate text claims a state ("aren't TS yet") that was true at row-filing but reality moved. Same root as the B-0125 title-slug mismatch and the proofs/lean migration: substrate authored at moment T1; reality at T2 has moved; substrate didn't get the update. Memory-file class promotion includes this one. |
