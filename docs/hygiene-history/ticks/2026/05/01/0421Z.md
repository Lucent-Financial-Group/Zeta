| 2026-05-01T04:21:00Z | opus-4-7 / autonomous-loop tick | (this commit) | Generative-improvement tick. With #1016 in real-dependency CI wait (no unresolved threads, build-and-test pending), picked the class-level prevention sibling: PR #1018 — `tools/backlog/generate-index.sh` now warns to stderr when a row file lacks `id` / `title` frontmatter. Output unchanged (no CI behaviour regression); operators / CI can grep stderr to surface the cause early — ideally before the row even merges. Verified locally: warning fires for the three pre-existing leaky-format rows on main; goes silent once #1016's frontmatter backfill merges. Layer 3 of the 4-layer pattern: encode the class (silent empty-render is the bug; warning is the catch), not just the instance (the three rows that triggered this). Cron 98fc7424 healthy. | [PR #1018 (this fix)] / [PR #1016 BLOCKED on real-dependency CI wait] / [PR #1012/#1013/#1014/#1015/#1017 awaiting #1016 merge] | Discipline note: cadence on this round is high (10 in-flight PRs from this session window). Each PR consumes CI cycles + reviewer attention; per silent-courier-debt logic the cost is non-zero even when CI is automated. Next tick should resist the urge to land further substrate unless a real Aaron-pointer arrives or an in-flight PR's merge unblocks specific follow-up work. The PR-rebase-on-#1016-merge work is real follow-up; everything else queues. |
