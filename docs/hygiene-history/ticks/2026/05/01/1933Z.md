| 2026-05-01T19:33:00Z | opus-4-7 / autonomous-loop tick | 98fc7424 | Goldfish-ontology double-failure tick. Aaron 2026-05-01 input cluster: OWASP + internationalization + accessibility + secure-by-default + static-analysis-crank for public-facing surfaces. Initial response: grep'd "OWASP" / "i18n" / "wcag" — empty. Aaron said "split to separate rows" — I drafted B-0159 / B-0160 / B-0161 + planned B-0162. Aaron caught: *"all of these items already exist under other names."* AND *"you searched in goldfish mode already but made the mistake of searching for owsap/i18n/wcag that's didn't find anyting then i told you to split and you didn't serch indiviual, that's a double failure."* **Failure-1**: shallow-grep on literal abbreviations; B-0004 (canonical i18n + l10n + g11n + a11y row from 2026-04-25) uses "internationalization" / "translate" / "human languages" / "accessibility" in title — none of which my "i18n" / "wcag" grep matched. **Failure-2**: when split-instruction landed, I didn't re-grep each concept individually with synonym expansion. **Discipline refinement** (extends prefer-mechanical-anchors + goldfish-ontology fix-cluster): pre-author-grep MUST use multi-synonym + multi-frame expansion (not literal-term-only); when splitting concerns, re-grep each individually with that same multi-synonym discipline. Found canonical homes: B-0004 (i18n + a11y), `docs/security/SDL-CHECKLIST.md` + `SECURITY-BACKLOG.md` (OWASP / SAST / SDL), extensive existing static-analysis infrastructure (Semgrep / CodeQL / .github/workflows/). Deleted B-0159 / B-0160 / B-0161 duplicate rows; will NOT create B-0162. The pre-author-grep discipline IS the discipline; my search quality wasn't. Cron 98fc7424 healthy. | [Deleted 3 duplicate rows; B-0154 to be updated next tick with composes_with refs to existing canonical homes (B-0004, SDL-CHECKLIST.md, SECURITY-BACKLOG.md); no PR opened from this branch for the duplicates] | The double-failure crystallizes the next refinement: pre-author-grep is necessary but not sufficient; pre-author-grep WITH multi-synonym expansion is the actual discipline. Per the just-validated extend-existing-memo pattern (PR #1145 Aaron-affirmed), the right move when finding existing canonical homes is to extend them, not duplicate them. The lesson: my goldfish-recurrence isn't lack of discipline; it's lack of search-quality WITHIN the discipline. Same shape as the Otto-364 search-first authority rule: searching is structural, but the QUERY is the load-bearing input. |
