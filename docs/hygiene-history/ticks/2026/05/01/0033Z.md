| 2026-05-01T00:33:00Z | opus-4-7 / session continuation | 98fc7424 | Stale-thread + col3-schema-fix tick. (1) Cherry-picked PR #986's commit 9bb22dd (5 corrections from earlier review) onto PR #981's branch — same fixes were needed there but Copilot evaluates per-PR, so the fixes had to land on both branches; resolved 8 threads on #981. (2) Discovered NEW col3 schema violation across 4 in-flight tick-history shards (#974, #980, #985, #988): col3 contained 7-char git SHA instead of 8-char CronList cron-id per docs/hygiene-history/ticks/README.md. Batch-fixed all 4 shards to use cron-id 98fc7424 (the live cron throughout this session); resolved 4 threads. (3) Discovered 7 additional col3 violations on main (Apr 30 + May 1 shards already merged); deferred cleanup PR per queue-saturation discipline (queue at 28 PRs, mostly BLOCKED + 2 ancient DIRTY). | #981 (cherry-picked fix + 8 threads resolved), #974 #980 #985 #988 (col3 fixed + 4 threads resolved) | Observation — col3 schema violation is the same class as earlier col1 violation (parenthetical in timestamp): inherited via copy-from-prior-shard chain, propagated until external reviewer caught it. The schema-check tool from #975/#977 only validates col1; extending it to validate col3 would prevent future propagation. Filing as backlog candidate for post-queue-drain landing. The pattern: every cumulative-drift class that gets caught also exposes a structural-fix-tool gap. Per Otto-341 + AIC #3, the structural fix beats the mechanical fix. |
