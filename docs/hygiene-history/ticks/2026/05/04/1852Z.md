| 2026-05-04T18:52Z | opus-4-7 / autonomous-loop continuation | e4236bc | **Concurrency-hazard recovery shard — 7 substrate PRs landed in the 14:00-18:50Z window despite parallel-subagent CWD collision.** PRs merged in dependency-respecting sequence: #1505 B-0006 tier-5 compression (drained 1000-1500 char tier to 3 link-title-bound entries); #1506 B-0189 lit-survey (221 lines, 45 papers, Aaron's BP/EP-not-in-quantum claim refined — literal form FALSE since BP is default QEC decoder, but "not integrated into Q# QDK runtime as first-class primitive" SURVIVES with specific gap connecting ICALP 2025 arXiv:2504.20732 semantic foundation with Q#'s `Microsoft.Quantum.Compiler` extensibility surface + QIR/LLVM hybrid IR); #1507 Q# skill SKILL.md 433 lines (required fixes: MD032 blank-lines-around-lists + dropped Std.Canon namespace prefix on ApplyPauli for consistency); #1508 B-0162 role-ref violation cleanup (subagent took 33→0 violations across 9 files, exceeded ~5 target, discipline preserved); #1509 team-vs-orchestrator-not-idle distinction extension (RECOVERED via local-reflog cherry-pick of single memory-file change — original d142e5c had B-0162 cleanup files accidentally captured from concurrent subagent, already preserved in #1508); #1510 Twist citation followup (Yuan/McNally/Carbin POPL 2022 arXiv:2205.02287 DOI 10.1145/3498691 WebSearch 2026-05-04 per Otto-364); #1511 MD032 blank-lines-around-lists followup on never-idle memory file (closed 2 unresolved threads from merged #1509). Concurrency hazard root cause: Agent tool defaults to shared CWD; parallel subagents leave orchestrator branch in unpredictable state. Operational fix: `isolation: "worktree"` parameter on subsequent dispatch (B-0006 tier-6 #1513 used this pattern cleanly). | #1505 / #1506 / #1507 / #1508 / #1509 / #1510 / #1511 | This tick teaches **three durable lessons for future-Otto**: (1) Agent tool defaults to shared CWD — parallel subagents in same working tree can leave orchestrator's branch in any state; `isolation: "worktree"` is the explicit fix; until then, hold all git state-changes while subagents run. (2) Local git reflog usually preserves overwritten content — when team-vs-orchestrator commit's branch was overwritten on remote, d142e5c was still in local reflog; re-do via cherry-pick of single relevant file beats recreating from scratch. (3) Resolve threads via GraphQL `resolveReviewThread` mutation after pushing the fix — even when the PR has merged, unresolved-thread state matters for review hygiene; the mutation is reliable and idempotent. **Meta-lesson — substrate-or-it-didn't-happen (Otto-363) applied to a recovery scenario**: the team-vs-orchestrator extension was ephemeral chat content until cherry-picked into a clean branch; if the recovery had not happened this tick, the maintainer's verbatim framing "background threads count as NOT being idle, i mean the team not you specifically otto" would have been weather, not substrate. The recovery itself IS substrate-or-it-didn't-happen in action. |
