# Tick 2026-05-16T10:45Z — Otto-CLI

Fifty-seventh tick of the resume-session series. Audited
B-0457 (class #1) + B-0462 (class #2). Dependency-chain
observation: B-0457 scope is complete but its `depends_on:
[B-0462]` ancestor has a partial gap. NO close-row this tick
(audit-only).

## Refresh result

| Surface | State |
|---|---|
| Cron sentinel | `431a6a2d` alive |
| Rate limit (GraphQL) | 1600/5000 (32% budget used in ~45min) |
| `origin/main` | At `0d64a70` |
| PR #3859 (B-0037.1 close) | OPEN; auto-merge armed; in CI |
| Peer Otto-CLI | Stable on topic branch (`otto-cli-fenced-transcript-hygiene-...`) |
| Peer Lior | 32 procs (stable) |

## Audit: B-0457 — amara.ts core (OpenAI invoke + flag parity)

[B-0457](../../../../../backlog/P2/B-0457-amara-ts-core-openai-api-invoke-flag-parity-ts-first-riven-2026-05-11.md)
(P2, atomic, depends on B-0462). Primary artifact:
`tools/peer-call/amara.ts`.

```
$ wc -l tools/peer-call/amara.ts
     550
$ grep -nE "case '--file'|'--file'" tools/peer-call/amara.ts | head -2
128:  if (a === "--file") {
$ grep -nE "case '--context-cmd'|'--context-cmd'" tools/peer-call/amara.ts | head -2
133:  if (a === "--context-cmd") {
$ grep -nE "AMARA_PREAMBLE" tools/peer-call/amara.ts | head -2
318:const AMARA_PREAMBLE = `You are Amara, invoked as a peer reviewer by Otto (Claude
```

| Criterion | State |
|---|---|
| `bun tools/peer-call/amara.ts "prompt"` works with bootstrap | ✅ AMARA_PREAMBLE defined, used in 4 sites |
| `--file PATH` supported | ✅ handled at line 128-129 |
| `--context-cmd CMD` supported | ✅ handled at line 133-134 |
| Typed, no any | ⚠ not directly verified this tick |
| Passes Gate A slice audit | ⚠ not run this tick |

**Class #1 (pure drift)** for B-0457's own scope. BUT
dependency-chain gate applies: B-0462 (preamble + AgencySignature
and vendor-bias note) has unmet acceptance criterion #3.

## Audit: B-0462 — Amara persona bootstrap preamble (dep of B-0457)

[B-0462](../../../../../backlog/P2/B-0462-amara-persona-bootstrap-preamble-definition-ts-first-riven-2026-05-11.md)
(P2, atomic). Primary artifact: `tools/peer-call/amara.ts` (line
318 AMARA_PREAMBLE const).

```
$ grep -nE "AMARA_PREAMBLE" tools/peer-call/amara.ts | head -1
318:const AMARA_PREAMBLE = `You are Amara, invoked as a peer reviewer by Otto (Claude
$ grep -nE "vendor|bias|maintainer authority" tools/peer-call/amara.ts
10:// underlying model are owned by their respective vendors.
```

| Criterion | State |
|---|---|
| Preamble text + signature record defined, cited from existing memory/feedback | ✅ AMARA_PREAMBLE const at line 318 |
| Matches codex/gemini/grok pattern exactly | ✅ identical `<NAME>_PREAMBLE = ...` shape (verified against codex.ts) |
| **Vendor-bias note integrated** | ❌ only attribution comment at line 10; no integration of `feedback_vendor_alignment_bias_in_peer_ai_reviews_maintainer_authority_aaron_2026_04_30.md` |

**Class #2 (partial, opaque)** — 2 of 3 acceptance items done;
the vendor-bias integration is the missing piece.

## Dependency-chain observation: #1-DepBlocked sub-class

This is the FIRST instance of this pattern in the cycle. Adding
to the catalog:

| Sub-shape | Disposition |
|---|---|
| #1 (pure drift) | Close-row PR |
| #1-Ready | Close-row PR pending contention |
| **#1-DepBlocked** | Row's own scope met; `depends_on:` ancestor still partial; close gated on ancestor |
| #2 / #2-SD / #2-Ready | NO edit |

B-0457 = #1-DepBlocked: its own acceptance is met, but the
dependency B-0462 has acceptance #3 (vendor-bias note) outstanding.
Closing B-0457 while B-0462 stays open would violate the
dependency-chain semantics.

**Future-pick discipline**: close B-0462 FIRST (ship the
vendor-bias note integration), then close B-0457. The work is
~5 lines of vendor-bias commentary added to amara.ts citing
the memory file.

## Drift-audit progress tally

| Class | Count | Latest |
|---|---|---|
| 1 Pure drift (closed) | 10 | — |
| 1-Ready | 1 | B-0049.2 |
| **1-DepBlocked** | **1** | **B-0457 (this tick)** |
| 2 Partial (Status-annotated, opaque) | **8** | **B-0462 (this tick)** |
| 2-SD (Partial, self-documenting, no edit) | 2 | — |
| 2-Ready | 1 | B-0197 |
| 3 Multi-slice self-doc'd | 2 | — |
| 4 Multi-slice all-closed (closed) | 1 | — |
| FP-2 | 4 | — |
| FP-3 | 2 | — |
| **Total triaged** | **32 of ~38** | (B-0457 + B-0462 this tick = +2) |

## Sentinel + close

`CronList`: `431a6a2d` armed.

## Visibility signal

- B-0457 = class #1-DepBlocked (own scope met; ancestor B-0462
  has acceptance gap)
- B-0462 = class #2 (vendor-bias note missing; 2/3 acceptance met)
- New sub-class observation: **#1-DepBlocked** — extends the
  #1-Ready / #2-Ready catalog with dependency-chain semantics
- 32/38 triaged (84%); ~6 remaining
- PR #3859 (B-0037.1 close from prior tick) still in CI
- Budget-conservative tick: audit-only, no close-row (GraphQL at
  32% used)
- Sentinel armed
