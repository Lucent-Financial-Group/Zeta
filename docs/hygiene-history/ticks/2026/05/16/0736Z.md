# Tick 2026-05-16T07:36Z — Otto-CLI

Twenty-eighth tick of the resume-session series. Rate at
**1024/5000** (cost-aware boundary; treat as extreme cost-aware).
Pure-git audit on B-0346 — discovered **3rd FP variant** (tool
shipped for OTHER row + B-0346's content-writing work hasn't
started). NOT drift; leave open.

## Refresh result

| Surface | State |
|---|---|
| Cron sentinel | Alive (`bd1c7739`) |
| Rate limit (GraphQL) | **1024/5000** (cost-aware boundary) |
| `origin/main` | Advanced — peer's PR #3809 (B-0557 slice 4 mixed-bullet) merged |

## Audit: B-0346 — 3rd FP variant

[B-0346](../../../../../backlog/P1/B-0346-backport-spec-gaps-to-openspec-b0193.md)
(`Back-port spec gaps to OpenSpec`). Path flagged: `tools/openspec/inventory.ts`.

**Per-acceptance check** (zero gh):

- Tool `tools/openspec/inventory.ts` exists (8428 bytes, 300 lines)
- Tool's git log: `tool(B-0171): add openspec/inventory.ts — Phase 1 spec-to-code gap report (#2147)` — shipped for **B-0171**, NOT B-0346
- B-0346 acceptance items are about USING the tool to author specs:
  1. Each gap from B-0345 has disposition (spec-written / augmented / etc.)
  2. New/augmented specs pass `inventory.ts` with improved coverage
  3. Gap count delta documented

This is a **3rd FP variant**: tool shipped for prior row + B-0346
requires additional content-writing work USING the tool. The
artifact's mere existence doesn't satisfy B-0346.

**Distinction from 2nd FP class (B-0418, B-0509):**

| Class | Pattern |
|---|---|
| **2nd FP** (B-0418, B-0509) | Tool path exists for OTHER row; acceptance-named API/field absent from tool |
| **3rd FP variant** (B-0346) | Tool path exists for OTHER row; acceptance requires USING the tool to produce new content; tool may already have everything needed but the USING-work hasn't happened |

Both subclasses share the same disposition: NOT drift; leave open;
the audit tool can't tell the difference but per-acceptance
verification catches both.

## Updated taxonomy

| Class | Count | Examples |
|---|---|---|
| 1 (Pure drift, closed) | 9 | — |
| 2 (Partial, Status-annotated) | 4 | — |
| 3 (Multi-slice, verified-self-documenting) | 1 | B-0440 |
| 4 (Multi-slice, all children closed, closed) | 1 | B-0159 |
| FP-2 (shared-tool-path + acceptance-API-absent) | 2 | B-0418, B-0509 |
| FP-3 (shared-tool-path + content-writing-work-not-done) | **1** | **B-0346 (this tick)** |
| **Triaged** | **18 of ~38** | |

## Sentinel + close

`CronList`: `bd1c7739` alive.

## Visibility signal

- B-0346 verified as 3rd FP variant (no edit)
- New FP-3 sub-class documented; taxonomy now spans 6 patterns
- Rate at 1024/5000; cost-aware boundary
- Sentinel `bd1c7739` alive
- ~20 audit candidates remaining
