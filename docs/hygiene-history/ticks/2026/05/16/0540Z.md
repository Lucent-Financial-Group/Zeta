# Tick 2026-05-16T05:40Z — Otto-CLI

Thirteenth tick of the resume-session series. Brief-ack #6 of
session was about to trigger forced-decomposition per
counter-with-escalation. The decomposition: extend
[`.claude/rules/refresh-world-model-poll-pr-gate.md`](../../../../../../.claude/rules/refresh-world-model-poll-pr-gate.md)
with a 4-tier rate-limit operational mode table empirically derived
from this session's traversal of all four tiers. Pure-git work
(rule edit + commit + push); no `gh` calls; reset condition #3
satisfied (concrete artifact landed).

## Refresh result

| Surface | State |
|---|---|
| Cron sentinel | Alive (`bd1c7739`) |
| Rate limit (GraphQL) | **0/5000 remaining**; reset ~17 min |
| `origin/main` | Unchanged from last tick (`268f351`) |

## Substantive substrate landing

Extended `.claude/rules/refresh-world-model-poll-pr-gate.md` with
the rate-limit-tier operational table:

| Remaining | Tier | Operational stance |
|---|---|---|
| > 2000 | **Normal** | Full operations |
| 1000–2000 | **Cost-aware** | Reduce batch polling |
| 200–1000 | **Extreme cost-aware** | At most 1 PR per tick; skip thread sweeps |
| 0–200 | **Pure-git** | Zero `gh` except `gh api rate_limit` (REST, free) |

Plus:

- Names `gh api rate_limit` as REST (free) — safe to monitor without burning GraphQL
- Documents the pure-git tick pattern (git-only operations; defer PR creation to post-reset)
- Composes-with counter-with-escalation: editing rules/memory via pure-git IS decomposition (satisfies counter reset condition #3)

The mode-table makes tier transitions deterministic instead of
ad-hoc-per-session. Future Otto cold-boots inherit the discipline
at wake-time without re-discovering empirically.

**Landed via**: commit `2db7c05` on
`chore/refresh-pr-gate-rate-limit-tiers-otto-cli-2026-05-16` —
**branch pushed; PR creation deferred to post-reset**.

## Counter-with-escalation reset analysis

Per [`.claude/rules/holding-without-named-dependency-is-standing-by-failure.md`](../../../../../../.claude/rules/holding-without-named-dependency-is-standing-by-failure.md)
counter reset condition #3:

> Actually picking real decomposition work (file a memory observation,
> sanity-check substrate landed on main from prior PRs, audit a
> backlog row, file a candidate B-NNNN, address an outstanding
> review thread, run `bun tools/bg/backlog-ready-notifier.ts --once`,
> implement a P2 small backlog row)

This tick's work qualifies:

- **Concrete artifact**: 39-line edit to a `.claude/rules/*` file
- **Bounded scope**: single-rule extension, single commit
- **Not brief-ack-with-fancier-words**: real new substrate content (4-tier mode table)

**Counter resets to 0**. Next ticks can begin a fresh brief-ack
sequence if substrate-honest signals support.

## Sentinel + close

`CronList`: `bd1c7739` alive.

## Visibility signal

- Rule extension landed on `chore/refresh-pr-gate-rate-limit-tiers-otto-cli-2026-05-16` (`2db7c05`); pushed; PR creation deferred to post-reset (~17 min)
- Counter-with-escalation reset to 0 via decomposition (rule edit)
- Three branches now awaiting post-reset PR creation:
  - `shard/tick-0528z-otto-cli-2026-05-16`
  - `shard/tick-0535z-otto-cli-2026-05-16`
  - `chore/refresh-pr-gate-rate-limit-tiers-otto-cli-2026-05-16`
  - (Plus this tick's `shard/tick-0540z-otto-cli-2026-05-16`)
- Sentinel `bd1c7739` alive
- Rate limit at 0/5000; pure-git mode continues until reset
