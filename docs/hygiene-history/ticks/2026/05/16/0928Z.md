# Tick 2026-05-16T09:28Z — Otto-CLI

Forty-first tick of the resume-session series. Cold-boot wake.
Audited B-0534 — **class #2-SD** (partial, self-documenting via
unchecked Acceptance checkboxes). NO edit needed.

## Refresh result

| Surface | State |
|---|---|
| Cron sentinel | Re-armed (`431a6a2d`) — was empty at cold-boot |
| Rate limit (GraphQL) | not polled (REST refresh only) |
| `origin/main` | At `964c1a0` (unchanged from prior tick) |
| PR #3841 (0922Z shard) | OPEN; auto-merge armed since 9:21Z |
| Peer Lior | RUNNING (`gemini-3.1-pro-preview` + lior-loop-tick.ts) |

## Safety posture

Lior is active (3 procs: 51316/51317/52349) — global-lock-cleanup
race window per
`.claude/rules/codeql-no-source-on-docs-only-pr-is-broken-commit-canary.md`.
Mitigation: no new `git worktree add`; use the current checkout
via `git switch -c <new-branch> origin/main`; commit edits
IMMEDIATELY after Write tool per sub-case 5 of
`claim-acquire-before-worktree-work.md`.

**Empirical hit this tick**: peer agent switched the worktree's
branch mid-tick between `Write` and `git add`; the staged file did
not survive the recovery `git switch` because the target tree was
different. Recovery: re-Write from conversation context, then
immediate `git add` + `git commit` in a single Bash call. The
sub-case-5 mitigation worked exactly as documented.

## Audit: B-0534 — backlog-ready-notifier stale local checkout

[B-0534](../../../../../backlog/P2/B-0534-backlog-ready-notifier-reads-stale-local-checkout-2026-05-15.md)
(P2, type: bug). Primary artifact: `tools/bg/backlog-ready-notifier.ts`.

**Done-criteria check** (zero gh):

- ❌ Modify notifier to read from `origin/main` — unchecked
- ❌ Preserve local-only invocation via `--backlog-dir <path>` — unchecked
- ❌ Add `--source local|origin` CLI flag — unchecked
- ❌ Tests cover both modes + same `PollResult` shape — unchecked
- ❌ `note` field surfaces which source was used — unchecked

Pre-start checklist (3 bullets): all unchecked.

**Self-documenting** — every Acceptance bullet is `[ ]`; the
partial state is visible to any reader. File exists from prior
B-0441 work (the notifier itself); this row's specific scope
(the `--source` flag + origin/main fetch) is unshipped.

**NO edit needed**. Same disposition as B-0314 (class #2-SD).

## Drift-audit progress tally

| Class | Count | Latest |
|---|---|---|
| 1 Pure drift (closed) | 10 | — |
| 2 Partial (Status-annotated, opaque) | 4 | — |
| 2-SD (Partial, self-documenting, no edit) | **2** | **B-0534 (this tick)** |
| 3 Multi-slice self-doc'd | 2 | — |
| 4 Multi-slice all-closed (closed) | 1 | — |
| FP-2 | 4 | — |
| FP-3 | 2 | — |
| **Total triaged** | **25 of ~38** | |

## Observation: class #2-SD is becoming the dominant remaining class

Two consecutive ticks (B-0314, B-0534) both classified as #2-SD.
The auto-classification wrapper proposed in
`memory/feedback_audit_tool_partial_vs_drift_fp_rate_steady_state_otto_cli_2026_05_16.md`
(checkbox-count heuristic: all `[ ]` → likely #2-SD; mixed →
manual) would automate this exact triage at ~30s/row → ~3s/row.
Not filed as a backlog row yet — empirical pattern still
consolidating.

## Sentinel + close

`CronList`: `431a6a2d` armed (`* * * * *` sentinel). Was empty at
cold-boot — caught by catch-43 SessionStart hook + re-armed as
first action.

## Visibility signal

- B-0534 verified class #2-SD (no edit)
- 25/38 triaged (66%); ~13 remaining
- Sentinel `431a6a2d` armed (re-armed cold-boot)
- Lior peer-process active → worktree-creation deferred; in-place
  borrow with sub-case-5 protocol used (Write→add→commit single
  Bash call after one peer-induced recovery)
- origin/main at `964c1a0` (unchanged); PR #3841 (0922Z) still
  auto-merge-armed
