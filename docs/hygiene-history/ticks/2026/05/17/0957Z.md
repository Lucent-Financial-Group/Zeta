---
tick: 2026-05-17T09:57Z
surface: otto-cli (background-worker fire, single-tick task)
operative-authorization: aaron 2026-05-14: "- **Devil-pole** (edge-runner drive): keep pushing, discover, go hard, never-be-idle"
---

# Tick 2026-05-17 0957Z — B-0156 acceptance #2 slice — check-github-settings-drift bun test

## Trigger

Background-worker task invocation: *"Claim and implement the smallest safe slice of B-0156."* Item is decomposed; 5 of 6 audit-listed ports already had test coverage; **2 had no `.test.ts`**.

## Substrate-drift discriminator (step 0 of backlog-item-start-gate)

| Check | Result |
|---|---|
| All 6 non-install `.sh` files have TS siblings? | ✓ verified (`tools/profile.sh`, `tools/peer-call/amara.sh`, `tools/peer-call/ani.sh` already deleted in PRs #1962 + #1702) |
| Each TS sibling has bun test? | **2 gaps**: `snapshot-github-settings.ts` + `check-github-settings-drift.ts` |
| `.py` policy lint in `gate.yml`? | ✓ `lint-no-python-files` job present |
| `.py` files in our codebase? | ✓ 0 (audit excluding `references/upstreams`, `.venv`, `site-packages`, `lean4`, `node_modules`, `.git`) |
| `package.json` scripts (acceptance #5)? | N/A — the ports are direct-invoke utilities, not npm scripts |

**Verdict**: row is *near-drift* (2 of 6 ports missing unit tests is the only residual gap on acceptance #2). Picked the **smaller** of the two gaps as this slice; left the larger (`snapshot-github-settings.ts`) for a follow-up slice per *"exactly one bounded step"*.

## Bounded step

- Edit: `tools/hygiene/check-github-settings-drift.ts` — `parseArgs` / `Args` / `ParseResult` lifted from module-scope to module-scope-`export`ed. No behavior change.
- Create: `tools/hygiene/check-github-settings-drift.test.ts` — 6 hermetic `bun:test` cases against the argument parser. All cases pass `--repo` explicitly so the `gh repo view` fall-through is not exercised.

## Verification

| Check | Result |
|---|---|
| `bun test tools/hygiene/check-github-settings-drift.test.ts` | **6 pass / 0 fail / 16 expect() calls** (806ms) |
| `bun test tools/hygiene/` (sibling-regression) | **187 pass / 0 fail / 355 expect() calls** (7.34s) |
| `bun tsc --noEmit` | clean |
| `git ls-tree HEAD \| wc -l` (canary) | 53 (matches `origin/main` baseline) |
| Worktree isolation | dedicated `.claude/worktrees/vast-foraging-sparrow`; root checkout untouched |
| Bus claim | `8b66235d-ca3f-4d5d-8e2e-d40428afe8c0` (otto-cli, B-0156, b0156-test-coverage-github-settings-drift-2026-05-17) |
| Branch guard | `git branch --show-current` matched `ZETA_EXPECTED_BRANCH` before commit |
| Commit SHA | `ae134c4b5` |
| Push | `dc096815b..ae134c4b5` → `origin/b0156-test-coverage-github-settings-drift-2026-05-17` |

## Rate-limit tier observation

GraphQL exhausted (`{remaining:0, used:5000}`) when `gh pr create` was attempted at 0957Z; reset_at 0959:53Z. Per [refresh-world-model-poll-pr-gate.md](../../../../../.claude/rules/refresh-world-model-poll-pr-gate.md) **Pure-git** tier: substrate landed via `git push` (no GraphQL needed); PR creation deferred to post-reset same-tick.

## Catch-43 cron-sentinel

Armed at session start: cron `7e3b5894` (`* * * * * <<autonomous-loop>>`) before any other work. CronList → no jobs; CronCreate → armed.

## Composes with

- B-0156 (acceptance #2; 5/6 ports now covered after this slice lands)
- B-0140 (parent migration row; closed 2026-05-09)
- Rule 0 (`no .sh files except install graph` — the TS port the test covers)
- `.claude/rules/backlog-item-start-gate.md` (step 0 substrate-drift discriminator fired correctly)
- `.claude/rules/claim-acquire-before-worktree-work.md` (claim acquired with surface-tagged `otto-cli` sender before write work)
- `.claude/rules/zeta-expected-branch.md` (branch-guard executed before commit)
- `.claude/rules/codeql-no-source-on-docs-only-pr-is-broken-commit-canary.md` (ls-tree count 53 confirms no commit-tree corruption)

## Follow-up (not in this PR)

File a parallel slice for `tools/hygiene/snapshot-github-settings.ts` bun-test coverage. After both land, B-0156 acceptance #2 is mechanically satisfied across all 6 ports and the row becomes a clean close-as-drift candidate.
