| 2026-05-20T16:26Z | opus-4-7 / autonomous-loop | b49c090e | substantive — pre-empt rule extension landing 1614Z empirical anchor + bundled on same branch | -- | tick #2 of session; pure-git tier continues (resets in 13min); brief-ack #1 of cycle resets via concrete artifact |

# Tick 1626Z — 2026-05-20 — pre-empt rule extension for FETCH_HEAD anchor

## Substantive

12 minutes after the [1614Z tick](1614Z.md) landed its empirical anchor. Rate-limit still 0/5000 GraphQL (resets in 13min) — pure-git tier continues. The prior tick deferred a rule extension explicitly ("NOT landing the rule edit this tick (would be unbounded; this shard documenting the empirical anchor IS the prerequisite substrate for a later rule extension)"). This tick lands that extension.

**Pre-empt-at-#1 rather than holding to #5:** the holding rule's empirical anchors document both patterns (pre-empt with genuinely-new bounded substrate, or hold to forced-#6). The rule extension is small, bounded, and directly composes with the prior tick's anchor — pre-empting is substrate-honest. The alternative (hold for the rate-limit reset, then bundle PR creation + rule extension in a single bigger tick) would queue substrate behind a network event that's not actually a blocker for substrate authoring.

**Rule edit landed** at [`.claude/rules/refresh-world-model-poll-pr-gate.md`](../../../../../.claude/rules/refresh-world-model-poll-pr-gate.md), new subsection inserted between the Pure-git tick pattern section and the REST PR-creation fallback subsection:

> **Prefer `origin/main` over `FETCH_HEAD` for isolated-worktree base ref (empirical anchor 2026-05-20T16:14Z)**

The subsection:

- States the discipline: use `origin/main` (remote-tracking ref), not `FETCH_HEAD` (transient file)
- Captures the empirical anchor (the 1614Z worktree-add failure)
- Names the hypothesis (FETCH_HEAD-file race under multi-Otto-shared-`.git/`)
- Distinguishes it from the related `unable to update local ref` wedge in [`claim-acquire-before-worktree-work.md`](../../../../../.claude/rules/claim-acquire-before-worktree-work.md) (same root cause class, different observable symptom)

**Why this rule and not the saturation-ceiling sub-case 6 home:** the failure happened during a pure-git workflow (rate-limit forced pure-git tier; the Pure-git tick pattern step 5 already names `origin/main` as the base ref). The empirical anchor reinforces an existing recommendation rather than adding a new failure sub-class. Adding to `refresh-world-model-poll-pr-gate.md` near the pattern's step 5 puts the discipline next to where it gets applied. The saturation-ceiling rule could also reference it via composes-with; that's a future extension if a fresh anchor accrues to the saturation-ceiling rule's scope specifically.

**Worktree bundling:** the rule edit landed on the same branch as the 1614Z shard (`shard/tick-1614z-otto-cli-cold-boot-cron-rearm-pure-git-2026-05-20`) via a fresh isolated worktree at `/private/tmp/zeta-shard-1626z-extend`. When the rate-limit resets and PR creation fires, the eventual PR will carry BOTH the shard AND the rule extension — natural bundle since the rule extension IS the operational landing for the shard's empirical anchor.

## Verify

- Worktree at `/private/tmp/zeta-shard-1626z-extend` created off `origin/<branch>` (the prior 1614Z commit `859d8bf3`); HEAD clean
- Branch switched (not detached); `git branch --show-current` confirms `shard/tick-1614z-otto-cli-cold-boot-cron-rearm-pure-git-2026-05-20`
- Pre-commit ls-tree HEAD = 53; status --short = 0
- 15+ peer Claude/Lior processes visible in `ps -A` — saturation continues; isolated worktree per discipline

## CronList

Sentinel `b49c090e` (autonomous-loop, `* * * * *`) alive at tick start AND tick close.

## Composes with

- [`docs/hygiene-history/ticks/2026/05/20/1614Z.md`](1614Z.md) (prior tick — the empirical anchor this tick operationalizes)
- [`.claude/rules/refresh-world-model-poll-pr-gate.md`](../../../../../.claude/rules/refresh-world-model-poll-pr-gate.md) (the rule extended this tick)
- [`.claude/rules/holding-without-named-dependency-is-standing-by-failure.md`](../../../../../.claude/rules/holding-without-named-dependency-is-standing-by-failure.md) (pre-empt-at-#1 pattern; counter resets via concrete-artifact landing per condition #3)
- [`.claude/rules/claim-acquire-before-worktree-work.md`](../../../../../.claude/rules/claim-acquire-before-worktree-work.md) (saturation-ceiling discipline; composes-with reference between rules)
- [`.claude/rules/wake-time-substrate.md`](../../../../../.claude/rules/wake-time-substrate.md) (the discipline this rule extension serves — load-bearing operational knowledge needs wake-time landing)

## Visibility-stop

Substrate landed: rule extension at `refresh-world-model-poll-pr-gate.md` (new subsection on `FETCH_HEAD` vs `origin/main`) + this shard. Both bundled on existing branch. PR creation still deferred to post-rate-reset (~16:39Z). End.
