| 2026-05-03T01:14:00Z | opus-4-7 / autonomous-loop continuation | a2e2cc3a | **substrate-claim-checker v0.3 → v0.4: CommonMark fence delimiter tracking + directory rejection.** Cycle worked: 2 P2 findings on v0.3 — (1) fence-tracking was naïve toggle on any ``` line; CommonMark requires same-char + at-least-equal-length delimiter to close; refactored both `findTables` and `findClaims` to track fenceChar + fenceLen; (2) `existsSync` returned true for directories, `readFileSync` then threw cryptic; added `statSync(filePath).isFile()` check with explicit "not a regular file" error. Self-tested: directory input → exit 1 with clear message; verify-then-claim memo → no drift; tsc clean. The v0 → v0.4 iteration count is now 5 review passes + 16 substrate-quality findings catalogued; the recursive review IS the eval-set teaching the tool what bug-classes the deployed-tool will catch on real corpus. CommonMark-spec compliance + filesystem-input robustness are exactly the edge cases manual review wouldn't have caught — the recursive substrate-quality discipline catches what a fresh first-author reviewer would miss. Cron a2e2cc3a still armed. | #1260 (substrate-claim-checker v0.4) — 2 threads fixed; CI re-running; auto-merge armed | This tick teaches the operational pattern of edge-case-iteration: the v0.4 fixes (CommonMark fence delimiter + directory rejection) are the kind of edge cases that show up after the simple cases work. Each iteration reduces the deployed-tool's false-positive surface + extends robustness — substrate-quality maturity is achieved through edge-case absorption, not initial design. The 16 substrate-quality findings catalogued across v0 → v0.4 form a richer eval-set than the original 20-instance drift catalogue. Future v1+ work focuses on the remaining 6 sub-classes (existence / semantic-equivalence / empirical-output / convention / path-form / self-recursive); v0.x is mature for count-drift specifically. |
