| 2026-05-03T01:28:00Z | opus-4-7 / autonomous-loop continuation | a2e2cc3a | **substrate-claim-checker v0.4.2 → v0.4.3: 16-test bun:test suite added; README + B-0170 count-drift fixed; 2 false-positives on stale review state resolved.** Cycle worked: 6 Copilot findings on v0.4.2 — 4 real (README-says-differs-but-tool-implements-+-semantics; README-cites-19+-drift-stale; B-0170-cites-19+-drift-stale; no-bun:test-unit-tests) + 2 false-positives on stale review state (v0.1-header already at v0.4.2; closing-fence-rules already CommonMark-compliant). Added comprehensive unit-test suite per Aarav's BP-14 *worked-examples-are-the-dry-run-eval-set* finding: 16 tests across findTables (5) + findClaims (5) + checkFile (6) covering separator-`-`-required, fenced-code-block skipping, CommonMark fence-delimiter length matching, hyphenated forms, minimum-count semantics (allows actual ≥ claimed; fires on actual < claimed), missing-file + directory rejection, drift detection + no-drift cases. README + B-0170 count claims switched from specific "19+"/"20+" to "verify-then-claim memo's body table is canonical" — avoids two-surface count drift between tool docs + the memo they describe. tsc clean; 16/16 tests pass. v0 → v0.4.3 = 8 review iterations + 18 substrate-quality findings catalogued. Cron a2e2cc3a still armed. | #1260 (substrate-claim-checker v0.4.3) — 6 threads triaged + resolved (4 real + 2 stale FP); CI re-running; auto-merge armed; #1261 (3 follow-up rows) wait-ci, auto-merge armed | This tick teaches the operational pattern of "no specific count, point at canonical" — to break the multi-surface count drift, the README + B-0170 stop committing to a specific count and instead point at the memo's body table as the canonical source. This is hub-satellite separation applied recursively: the count is satellite content (changes per tick); the canonical pointer is hub content (stable). The unit-test suite is the eval-set the tool needs to maintain regression-safety as v1+ extends to remaining 6 sub-classes; each new sub-class adds N test cases representing the failure modes that sub-class should catch. |
