| 2026-05-03T15:12Z | opus-4-7 / autonomous-loop continuation | a2e2cc3a | **B1 → A fully closed (post-1330Z session compaction recovery + B-0181 closure). (4 of 4 deferred TLA+ specs in CI) + B3 Stryker → A in-flight, completing all P0 items in the math-proofs honest assessment matrix.** Resumed after session compaction from previous context. **#1416 (B-0181 SpineMergeInvariants spec fix) merged ✓**: Cascade(i) was under-specified (only `levels[i] >= Cap(i)`, no constraint on level i+1); TLC found 16-step trace where Cascade(0) fires 5 times in a row accumulating level 1 to 10 > 2*Cap(1) = 8 while Cascade(1) sat enabled. Fix: added `levels[i+1] + levels[i] <= 2 * Cap(i+1)` precondition mirroring BalancedSpine.fs's synchronous cascade chain + `WF_vars(Cascade(i))` for liveness + `StateBound` (totalInserted ≤ 30) + `PendingBound` (Len(pendingIn) ≤ 4) state constraints. With bounded constants MaxLevel=2 / MaxBatchSize=1, TLC completes in <1s with 400 distinct states explored at depth 45; both `InvCap` AND `InvMass` enabled in cfg (mass-conservation was previously declared in THEOREM but not invariant-enabled). Promoted P2 → P1 because same author-time class as B-0184 (Alloy `Spine.als` `check`-vs-`run` semantics + missing batch-size `fact`): under-specified action preconditions across two different formal-verification tools. Memory file `feedback_under_specified_action_preconditions_recurring_class_in_formal_specs_aaron_2026_05_03.md` captures the recurring class with author-time precondition-audit discipline. F# CI registration + `proof-tool-coverage.md` + math-proofs assessment matrix all updated; B1 → A fully closed (DbspSpec ✓ #1397; CircuitRegistration ✓ #1401; SpineAsyncProtocol ✓ #1411; SpineMergeInvariants ✓ #1416). **#1413 (B-0183 Phase 1 sibling Alloy TS wrapper) merged ✓** after rebase from #1415's Spine.als fix. **#1417 (Stryker B3 → A: CI workflow) opened**: `.github/workflows/stryker-mutation.yml` modeled on lean-proof.yml — path-filtered to `src/Core/` + `tests/Tests.FSharp/` + `stryker-config.json`; threshold-break at 50% gates the workflow; HTML + json reports uploaded as 90-day artifacts on every run. Linux-only per B-0182 (Stryker is pure-managed code with no OS-specific behavior). Closes the LAST open P0 item from the math-proofs assessment — net P0: 4 of 4 closed. **Discipline applied**: under-specified-action-precondition recurring class encoded across two tools (TLA+ + Alloy) with author-time discipline (audit every state-transition action / pred / lemma for neighboring-state preconditions before declaring done; counterexamples showing "obviously-wrong" action sequences usually mean spec-gap, not bug-found). **Same-tick cascade**: each substrate fix updated the math-proofs assessment + proof-tool-coverage + relevant memory + MEMORY.md + backlog row simultaneously per the same-tick-update-recursion discipline. | PRs touched: #1416 merged ✓; #1413 merged after rebase ✓; #1417 opened (B3 Stryker workflow, auto-merge armed). Backlog: B-0181 P2→P1 closed; B-0183 Phase 1 sibling closed; B3 in-flight closure (Stryker CI). Memory landed: `feedback_under_specified_action_preconditions_recurring_class_in_formal_specs_aaron_2026_05_03.md` with MEMORY.md pointer. Specs validated: TLC clean on SpineMergeInvariants (400 distinct states / depth 45). | This tick teaches **author-time-precondition-audit** as the structural fix for the under-specified-action class. When writing a state-transition action / pred / lemma, the natural author move is to enumerate the action's primary effect on its own variables; what gets missed is the action's constraints from neighboring state. The discriminating signal: when a TTrace or counterexample shows the action firing in an "obviously-wrong" sequence (5 consecutive same-action calls with later cascades enabled but never chosen), the failure class is almost always under-specified-action-preconditions, not real-bug-found. Recurring class proven across two formal-verification tools (TLA+ Cascade + Alloy SizeDoubling) suggests this is a general spec-author bias to fix structurally. Future-Otto: at author-time, for every action / pred / lemma, ask *"what neighboring state must hold for this action to be safe?"* — that audit catches the class before it ships. |
