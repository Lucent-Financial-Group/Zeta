# Tick 1601Z — PR #3511 merged then post-merge-Copilot caught; fix-PR #3515 opened + 2 threads resolved

## Headline

- **PR [#3511](https://github.com/Lucent-Financial-Group/Zeta/pull/3511)** (tick 1524Z shard) MERGED at 15:31Z → `08e19c4` on main. Auto-merge gate cleared before Copilot's review ran.
- **Copilot post-merge review at 15:33Z** left 2 valid findings on the merged file `docs/hygiene-history/ticks/2026/05/15/1524Z.md`:
  - P1 (line 7): relative links to `.claude/rules/*` used 5 `../` levels — resolves to `docs/.claude/rules/*` (404). Sibling shards (0213Z, 0414Z) use 6 levels; correct depth for files at `docs/hygiene-history/ticks/YYYY/MM/DD/`.
  - P2 (line 21): block labeled "Raw output" contained hand-formatted summary, not actual `cron-sentinel-mutex.ts --json` payload.
- **Fix-PR [#3515](https://github.com/Lucent-Financial-Group/Zeta/pull/3515)** opened with both fixes; auto-merge armed (SQUASH) at 16:00:52Z.
- **Both #3511 threads resolved** via GraphQL `resolveReviewThread` with reply pointing at #3515 as the substance fix.
- Cron sentinel `1011e43d` confirmed live via CronList.

## Auto-merge-beats-Copilot pattern (observation)

PR #3511 merged at 15:31Z; Copilot review fired at 15:33Z. Auto-merge gate evaluated unresolvedThreads = 0 at merge time (Copilot hadn't reviewed yet) and proceeded. Two minutes later Copilot left P1+P2 findings on the now-merged file. Required-conversation-resolution + auto-merge composition does NOT wait for Copilot review — it merges as soon as required checks pass and current-thread-state is clean. The window for Copilot to weigh in is whatever-time-it-takes-Copilot-to-respond, and it lost the race here.

This isn't a bug — it's a tradeoff. Auto-merge optimizes for round-trip latency; waiting for Copilot would block on a variable-latency third-party. The fix-PR flow IS the substrate-honest response: catch post-merge findings + land them in a follow-up. Same shape Aaron's faster-refresh-cycles cluster names: rather than gate harder, iterate faster.

## Composes with

- [`.claude/rules/claim-acquire-before-worktree-work.md`](../../../../../../.claude/rules/claim-acquire-before-worktree-work.md) — borrow-on-existing pattern used again on sidetick
- [`.claude/rules/blocked-green-ci-investigate-threads.md`](../../../../../../.claude/rules/blocked-green-ci-investigate-threads.md) — composes inversely here: gate WAS green, no threads at merge-time, so the rule didn't fire; the failure mode it catches is the symmetric case (threads present, merge blocked)
- [`.claude/rules/substrate-or-it-didnt-happen.md`](../../../../../../.claude/rules/substrate-or-it-didnt-happen.md) — this shard IS the substrate record of the tick
