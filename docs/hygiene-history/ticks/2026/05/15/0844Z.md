| 2026-05-15T08:44:00Z | claude-opus-4-7 | 596e842c | shard: 2 legit Copilot threads on PR #3389 + PR #3382 closed as superseded by concurrent peer-Otto fix | (PR #3392) | substrate-honest concurrent-Otto loss: when two Ottos propose mirror bulk-fixes, faster wins; close without merge is appropriate outcome |

# Tick 0844Z — PR #3389 2-Copilot-fix; PR #3382 closed as superseded

## Headline

- **PR [#3389](https://github.com/Lucent-Financial-Group/Zeta/pull/3389)** (0829Z shard) picked up 2 LEGIT Copilot threads (not stale-review this time):
  - Line 50: "stable across 4 instances" but listed 5 ticks → corrected count; **also**: my earlier post-create-fix `sed` over-replaced generic `(PR TBD)` refs with `(PR #3389)`, restored generic refs in body prose
  - Line 54: `dont-ask-permission.md` reference without link → added relative-path link with 6 dotdot
  - Both fixed in commit `6000b03`; threads resolved
- **PR [#3382](https://github.com/Lucent-Financial-Group/Zeta/pull/3382)** (bulk path-bug fix) CLOSED as superseded:
  - `git diff origin/main` showed 3 of 4 files already fixed on main by some other concurrent bulk-fix
  - 4th file (0717Z.md) has main-side `backlog/` path fix that my sed didn't catch
  - Closed with substrate-honest comment explaining concurrent-Otto loss
- Cron sentinel `596e842c` armed.

## Δ since 0838Z

| What | At 0838Z | At 0844Z |
|---|---|---|
| PR #3389 (0829Z shard) | OPEN, 1 stale-review resolved | OPEN, 2 LEGIT threads → resolved (`6000b03`) |
| PR #3382 (bulk-fix) | OPEN, gate=DIRTY (rebase needed) | CLOSED as superseded |
| In-flight PRs | 5 | 4 (#3379, #3381, #3389, #3391) |
| Cumulative session merges | 11 | 11 (no new this tick — review-thread work) |

## Substrate-honest observations

### Concurrent-Otto bulk-fix race

Someone else (likely peer-Otto via their loop) shipped a similar `.claude/rules/` path-bug bulk-fix that landed BEFORE my PR #3382. My fix was correct but late. The substrate is converging: both Ottos identified the same bug, both proposed the same fix, the faster fix merges.

This is empirical evidence the two-Otto coordination works at the **independent-bug-discovery** layer — same root cause, parallel paths, eventually convergent. The bus + PR substrate carries the lessons; both Ottos benefit even though one PR closes without merge.

**Substrate-honest discipline**: when concurrent-Otto loss is observed, CLOSE the redundant PR (don't try to rebase + force-merge the duplicate). The closing comment IS the substrate landing — documents the concurrent fix + cites the diff.

### Sed broad-replace anti-pattern

My post-create-fix sed (`sed -i '' 's|(PR TBD)|(PR #3389)|' docs/hygiene-history/ticks/2026/05/15/0829Z.md`) replaced ALL occurrences of `(PR TBD)` in the file — including descriptive text that REFERENCED the `(PR TBD)` pattern generically. Should have been anchored to line 1 only:

```bash
sed -i '' '1s|(PR TBD)|(PR #NNNN)|' <file>
```

`1s|...|...|` restricts the substitution to line 1 (the pipe-row).

Going forward: post-create-fix sed should always be anchored. Updating the (PR TBD) discipline note implicitly. Could be part of the future `(PR TBD)` mechanization if/when that lands.

### Stale-vs-legit-thread distinction

PR #3388 → 1 stale-Copilot (the `(PR TBD)` claim — fix had already landed)
PR #3389 → 1 stale-Codex (same pattern; reply + resolve)
PR #3389 (this tick) → 2 LEGIT Copilot (substrate-real findings — count mismatch + missing link)

Stale-vs-legit distinction matters because:

- Stale: reply + resolve; no substrate fix needed (the issue was already addressed)
- Legit: fix-commit + resolve; substrate updated

Recognition signal: if the file's HEAD content matches what the thread describes, it's legit. If HEAD content differs from what thread describes (thread describes pre-fix state), it's stale.

## Cron sentinel

`596e842c` armed.

## Next

Cron-driven. Next tick:

1. Verify PR #3379 + #3381 + #3389 + #3391 + this tick's PR auto-merge fires
2. Address any new review threads
3. Watch PR #3323 for Lior response (loop active; no movement at B-0527 scope yet)
4. If quiet, deferred candidates remain: claude-loop investigation OR (PR TBD) mechanization (now with sed-anchoring + commit-amend learnings)
