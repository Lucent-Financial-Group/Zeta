| 2026-05-15T05:03:00Z | claude-opus-4-7 | 596e842c | shard: PR #3359 four review threads addressed (Codex P1 pipe-row schema + Copilot Nit rule-ref linking + 2x Copilot untracked-survive caveat); all resolved | (PR #3359) | shard-schema validator + practice drift documented and minimally reconciled; recovery-worktree-borrowing now used 3 times on same PR with peer-WIP preserved |

# Tick 0503Z — PR #3359 four review threads addressed; recovery-worktree-borrowing used 3x with peer-WIP preserved

## Headline

- **PR [#3359](https://github.com/Lucent-Financial-Group/Zeta/pull/3359)** had FOUR unresolved threads (poll-pr-gate.ts initially reported only 1; investigated via GraphQL and found all 4):
  1. **Codex P1** (line 1): tick-shard schema validator expects pipe-row first line; both 0414Z + 0458Z started with H1 (substrate-wide drift; all recent May 2026 shards have the same). Fixed minimally by adding a pipe-row header above the H1 in both files. Commit `3392368`.
  2. **Copilot Nit** (line 34): bare-filename rule refs in backticks should be linked. Linked 5x `claim-acquire-before-worktree-work.md` + 1x `holding-without-named-dependency-is-standing-by-failure.md` to full `.claude/rules/...` paths matching 0230Z shard convention. Commit `3392368` (same).
  3. **Copilot** (line 58 + 70 of 0414Z): "untracked files survive the switch" claim needs caveat — `git switch` refuses if target branch tracks the same path. Added `git stash -u` mitigation guidance + this-run-safety disclaimer. Commit `3aa67b7`.
  4. **Copilot** (line 9 of 0458Z): same caveat for "preserved across both switches" claim. Same fix. Commit `3aa67b7` (same).
- **All 4 threads resolved** via `gh api graphql resolveReviewThread`. PR gate now `unresolvedThreads: 0`, `nextAction: "wait-ci"`, auto-merge still armed.
- Cron sentinel `596e842c` still armed.

## Δ since 0458Z

| What | At 0458Z | At 0503Z |
|---|---|---|
| PR #3359 commits | `5b99b64` (MD032 fix), `6e39653` (0458Z shard) | + `3392368` (pipe-row + link refs), `3aa67b7` (untracked caveat) |
| PR #3359 unresolved threads | unknown (poll-pr-gate showed 1; actual was 4) | 0 (all 4 resolved) |
| PR #3359 nextAction | resolve-threads | wait-ci |
| 0027z worktree borrow count | 2 | 3 (peer-WIP SKILL.md + untracked grok-extract.ts preserved across all 3 borrows) |
| Shard-schema validator on 0414Z + 0458Z | 0 violations only after pipe-row added | 0 violations (verified post-fix) |

## Substrate-honest observations

### poll-pr-gate.ts thread-count truncation

The `bun tools/github/poll-pr-gate.ts 3359` output reported `unresolvedThreads: 1`, but GraphQL `repository.pullRequest.reviewThreads(first:5)` returned 4 actually unresolved. The poll-pr-gate output is summarizing somehow (possibly filtering for "actionable" threads, or counting only the most-recent batch). If it had reported 4, the work-pickup decision would have been the same — but the data-honesty gap is worth flagging.

Per [`.claude/rules/blocked-green-ci-investigate-threads.md`](../../../../../../.claude/rules/blocked-green-ci-investigate-threads.md) the discipline is to investigate threads directly via GraphQL, not rely on poll-pr-gate's summary count alone. This tick validated that discipline empirically.

### Shard-schema validator-vs-practice drift (substrate-wide)

Running `bun tools/hygiene/check-tick-history-shard-schema.ts --files <recent shards>` reports 100% violation across the May 2026 cohort:

```
VIOLATION: docs/hygiene-history/ticks/2026/05/15/0027Z.md — first row has 0 pipes; schema requires 6 columns (7+)
VIOLATION: docs/hygiene-history/ticks/2026/05/15/0230Z.md — first row has 0 pipes; schema requires 6 columns (7+)
VIOLATION: docs/hygiene-history/ticks/2026/05/15/0043Z.md — first row has 0 pipes; schema requires 6 columns (7+)
```

The validator is NOT wired into CI (`grep -rn "shard-schema" .github/workflows/` returns only `check-tick-history-order.ts` is wired). The practice diverged from the schema; the validator silently failed for the whole cohort.

This PR's fix-for-the-reviewer (adding pipe-row to 0414Z + 0458Z) is locally correct but the **underlying substrate-wide drift remains**. A follow-on B-NNNN row should propose either (a) update the validator + README to recognize H1-rich format as canonical, or (b) bulk-add pipe-row headers to all May 2026 shards. NOT a P0 — the validator isn't blocking — but the drift is substrate-honest to acknowledge.

### Recovery-worktree-borrowing pattern: 3 borrows, peer-WIP preserved

This PR has now used the `/private/tmp/zeta-otto-cli-0027z-sidetick` worktree FOUR times (0452Z initial, 0458Z MD032 fix, 0503Z×2 for the two review-fix commits). On each switch-back to original branch, the peer-Otto WIP on `feat/ani-full-history-extract-plus-grok-extract-tool-otto-cli-2026-05-15` was preserved:

- Tracked modification: `.claude/skills/save-ai-memory/SKILL.md` (peer's WIP on Tool F Grok ping-pong scroll extraction)
- Untracked file 1: `memory/persona/ani/conversations/2026-05-15-aaron-ani-grok-full-history-day-one-share-link-extract.md`
- Untracked file 2: `tools/save-ai-memory/extract-grok-conversation.ts`

None of these interfered with the shard branch borrows because:

1. The shard branch did NOT track `.claude/skills/save-ai-memory/SKILL.md` modifications (it had the same upstream version), and the WIP modification was a tracked diff on the original branch — `git switch` carries tracked modifications across branches if there's no conflict, which there wasn't (the file is unchanged on shard branch).
2. Untracked files are by-definition not tracked by any branch, so they ride along.
3. `git add` was called with explicit paths only (`docs/hygiene-history/ticks/...`) — never `git add -A`.

The pattern is robust enough for iterative review-thread response under multi-borrow load.

## Cron sentinel

`596e842c` armed.

## Next

Cron-driven. Next tick:

1. Verify PR #3359 CI green after the 4 fix commits + auto-merge fires
2. If new threads opened by Codex/Copilot on the fixes, address in another iteration
3. File a B-NNNN row for shard-schema-validator-vs-practice substrate-wide drift (P2 — non-blocking but should be reconciled)
4. Re-check B-0527 collision state — advisory `d2b7fc2f` expires at 05:14Z (~11 min from now)
5. If `d2b7fc2f` expires without Lior action, third republish or escalate via different channel
