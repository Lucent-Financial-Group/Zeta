# colors/ — one color per file, carved sentence (core, date-agnostic)

One **color** per file, single-token filename, one carved sentence on its meaning in
our system (CI/status, salience/LLMTV channels, the ⊥-to-⊤ poles), anchored. A core
date-agnostic vocabulary folder (a DV2.0 hub — stable), distinct from the dated
`docs/research/*` satellites. Same discipline as `words/` and `letters/`.
