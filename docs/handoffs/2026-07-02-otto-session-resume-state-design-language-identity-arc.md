# Otto session resume state — 2026-07-02, the design-language / identity arc

**Purpose:** checkpoint before an Alexa-website ferry batch. Everything below is
MERGED on main unless marked otherwise; memory
(`superdeterministic-same-ir-ui-dus-not-ifs` + `qpg-quality-per-glyph-over-dpi`)
carries the full derivation chain.

## Shipped this session (all merged)

- **Design sync (project f52fe130-fd0d-4310-93c2-19b6ce2a4ecc):** first import of the
  zeta-portal-web kit (10 components, 23 authored preview cells, all graded good);
  frost channel in conventions + safelist (#9102); design-ethics guidelines
  (#9127); grid-overflow remedies. Last driver run: `upload.any = false` — project
  matches build, anchored. Re-sync is one command (NOTES.md holds the ritual:
  self-link `ln -sfn ..`, tailwind buildCmd, anchor fetch, driver).
- **The research spine:** `docs/research/2026-07-02-name-of-name-equals-mix-of-mix-…`
  (#9087) + addenda: unchosen precedence (#9089, "the ROOT"), accidental time
  (#9091), CPT (#9092) + Aaron's dictionary (#9094), Bell↔Sybil verified (#9104);
  geographic superdeterminism S(d) + f* (#9113).
- **Instruments:** CPT property suite (#9096, 9 green); CHSH oracle
  `chshS`/`chshSybil`/`coordinationBandwidth` (#9117); AntiSybil↔Bell bridge tests
  (#9104); **Soraya batch 2 complete** (#9139): Z3 LHV lemma (z3+cvc5, replay-cached)
  + `chshMargin` ε(n,δ)=√(32·ln(1/δ)/n) + `chshSybilCalibrated`; her finite-sample
  finding fixed (#9134) and its BUGS.md P1 closed.
- **Glyphs & UI:** `shape-sybil-verdict` 18th cartridge (#9119, verdict-as-plait,
  drawn=gated); `shape-refraction` 19th (#9130, membrane crossing, tangent-Snell
  exact, honest sine-law bound); darkhall S-lanes coordination board (#9132).
- **Handoffs:** Otto→Soraya routing (#9128) → her verdict preserved verbatim
  (#9135); Otto→Aaron ZetaIdol probe-wiring steer request (#9128) — **five consent
  questions still awaiting Aaron's answers**.

## Open threads (in priority order)

1. **Soraya's remaining batches** (her order): batch 4 TLA+ deterministic-forger
   liveness (`tools/tla/specs/ChshSybilConviction.tla`, `◇□ Convicted`) + FsCheck
   prefix property; batch 3 geo Z3 lemma (S-effort, same landing surface as the LHV
   lemma); batch 1 Lean CPT lemma (`src/Core.Lean4/`) + Stryker run over Braid.
2. **ZetaIdol probe wiring** — blocked on Aaron's steer (the five questions:
   probe timing, pairwise consent, conviction consequences, probe economics,
   un-seeded-entropy prerequisite).
3. **Reticulum half of the geo experiment** — blocked on real transport latency
   data; sim side green; estimator f̂=(S−2)/2 shipped as API.
4. **Deferred by routing:** Lean/Mathlib Hoeffding proof (outward-claim time);
   Alloy exchange-history spec (code doesn't exist yet); F* re-heard on a
   cryptographic property.
5. **Design-language backlog (unscheduled):** cartridge playground toolkit
   (3 panes + law lamp), adinkra-tesseract VR demo (retires the wrap-edges issue
   row), chrome-token unification (portal ↔ darkhall hubs — Aaron's palette call).

## Standing disciplines this session carved/confirmed

Skins never remap meaning (deception = NOPE); frost = priced privacy;
QPG over DPI (the standing UI answer); honest bounds in the ink; ferry Alexa
material through the Mirror→Beacon peel (verify before Beacon; poetry stays
poetry); one-way inference on all Sybil verdicts (convicts, never acquits).
