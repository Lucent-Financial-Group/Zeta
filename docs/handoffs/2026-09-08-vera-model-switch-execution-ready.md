# Vera model-switch handoff: admitted source, first execution next

Date: 2026-09-08 UTC
Author: Vera, OpenAI Codex using GPT-6 Astra
Operational status: research-grade continuity record
Lifecycle: active
Status: source admission complete; registered M4/M5/frozen query unrun
Work item: 081M1Z63YMC087G0R003N5FH9X

## Paste-ready bootstrap

Continue Vera's compositional Bayesian/probabilistic DAG learning work. Nodes
may contain neural modules or nested DAGs; this is not restricted to a plain
neural network. The user authorizes unattended work and normal PRs/merges.
They requested this checkpoint before switching to a smaller model. Attribute
new work to the model actually running; do not inherit GPT-6 Astra attribution.

Use the existing writer clone and integration branch below. Do not start over
from main: the reviewed implementation and source capsule are on this pushed
branch, not yet on main. Read this document, the accepted assembled admission,
and the fixed invoker. The next substantive action is the already registered
first M4 invocation, exactly once, with the two independently accepted hashes.
Assess its actual numerical outcomes and committed prefix before entering M5.
Do not add another framework, repeat the old scalar experiment, or open the
paused compiled streams. Preserve failed first attempts as actual outcomes.

## Exact working surface and durability

- Writer: `/Users/acehack/.zeta/agents/codex/Zeta-projection-publication-20260908`.
- Branch: `codex/mixed-message-epoch-integration-20260908`.
- Last remotely verified source-capsule checkpoint:
  `d61ee6a994b154c8df3b0627e9b23b5cd9db13c6`.
- Independently accepted source commit:
  `3c3a76ad6674bb40b2e1309021735950c282d8d5`.
- Final independent admission owner commit:
  `26eb850ed0282e3395ec54011408738e5cf181c6`, freshly fetched from
  `codex/mixed-message-epoch-bridge-20260908` and imported here as
  `dec1050fd`. No numerical source changed in that import.
- This handoff commit follows that import and is pushed to the same integration
  branch. Resolve its actual tip with `git log` and `git ls-remote`; do not
  treat the older capsule checkpoint as the final handoff tip.
- Shared view `/Users/acehack/Documents/src/repos/Zeta` is read-only. Only a
  clean `git pull --ff-only` refresh belongs there. No edits, stash or branch.
- Fresh remote main probe during handoff:
  `f1e2e67ab36ca128d7ecc3be57b98bdd311b5911`. Main advanced after our verified
  PR17052 merge; do not infer that unrelated changes were reviewed here.

Implementation remains remote-parked on its indexed integration branch. There
is no implementation PR yet. Finish actual controls and outcome review before
publishing the coherent implementation/results through a normal PR and CI.
Do not claim these new results are on main. The separate source capsule keeps
its exact old identity despite later documentation commits.

## Completed and already on main

| Publication | Result and boundary |
| --- | --- |
| [PR17045](https://github.com/Lucent-Financial-Group/Zeta/pull/17045) | Both supplied magnet transcript versions and the history/generator interpretation. Latest transcript is the prior version with `0:00` plus newline prepended. Source title/transcript are user supplied; speaker/uploader/date were not verified. |
| [PR17048](https://github.com/Lucent-Financial-Group/Zeta/pull/17048) | Deliberate multi-oracle selection and conspicuous unchosen-default clarification; no new runtime chooser or waiver of alignment floors. Corrected a Tsirelson comment sign without changing code. |
| [PR17050](https://github.com/Lucent-Financial-Group/Zeta/pull/17050) | Publication receipts, reviews and paused compiled-controller investment. |
| [PR17051](https://github.com/Lucent-Financial-Group/Zeta/pull/17051) | Sole registered scalar projection attempt: 88 calls across 40 IDs, 12 core certificates, 27 closed native children, retained refusals and 12 rejected baseline mutations. |
| [PR17052](https://github.com/Lucent-Financial-Group/Zeta/pull/17052) | Accepted mixed-message/learned-module design, source contract, transport amendment, codec conventions and current comparator census. Design publication, not new learning results. |

PR17051 merge is `f59b6e395603062882dd1fe69fa8247406842e4d`; PR17052 merge
is `84cc7a0cd2c78bff2ca01fc699fa4d054d3a8223`. Their exact trees, paths,
checks and merge observations are indexed by the
[continuation](2026-09-08-vera-projection-and-composable-learning-continuation.md)
and [implementation register](../research/2026-09-08-mixed-message-epoch-implementation-register.md).
Do not run the scalar 40-ID/88-call driver again. Its original manifest remains
`FE1F5BDF732FA6B08092887210552BA9CEF68D724E519C77958A282366A348E7`,
source `f33ac42959388344fc3c82058f165764954fa22d`; later publication formatting
does not reassign that execution to another source cut.

The requested [magnet source record](../ip-questionable/2026-09-08-magnet-paradox-memory-history-transcript.md)
and [history/generator research direction](../research/2026-09-08-history-generators-and-hidden-evolution-under-repetition.md)
are complete on main. Latest attachment SHA256:
`c48054e83727c1b717a8b4b703a1ecf830a4b47fe9152808294887b33aa956eb`.
No further transcript ingestion is pending for that request.

## Completed implementation and review

Read the [final assembled admission](../research/2026-09-08-mixed-message-assembled-admission-review.md)
first. It accepts exactly this capsule and the already fixed invocation, with
no remaining source-admission blocker. It did not run a numerical service.

| Component | Final source and evidence |
| --- | --- |
| F# learner/core | Owner `d7e8e3806afaecdaed8a4a1ff5244c899201c870`; 71 focused controls; 323 retained owner evidence records; independent review `419c7a81d`. |
| F# process peer | Owner source `d16bafa2a71e503878d645a8ea4f52244b5c227b`; 99,661 bytes, SHA256 `5D0DD505F3849D7F989982DCAE5EBAB2898B8140AE74489540EC016B15AD7795`; independently reviewed lifecycle. |
| Python bridge | Production source `567a9f004cf67b9df6d4a82c55fadaaf4fdd0b9b`; conservative withdrawal admission and actual accounting; independent follow-through accepted. |
| Portable bridge test | Owner `cf5744d70c5c613c1c892a81e42a8c69f7d523e4`; test-only correction. Final default-temp run: 111 passes, strict three-file mypy passes. |
| Fixed invoker | `registered-source/invoke.py`, SHA256 `CC7FBF89E681909374591516B4227F3D93373E9B5F089C039994B0CA46055E69`; independent review `2c8adb74c`. |
| Finite capture/freeze helpers | Independent review `2fccfeff6`; original defects and isolated failure-retention controls preserved. Actual capsule admission at `26eb850ed` closes the final source gate. |

The core uses the real SoftScheduler route, checked Gaussian/Gamma kernels,
explicit contribution identities, immutable learned artifacts, frozen query
selection and retained-prefix failure handling. It borrows FactorGraph's
exclusion/replacement laws but does not call FactorGraph itself. Local
M1/M2/M3/M6/M7/M8 controls are recorded; synthetic service premises are limited
explicitly. Those tests do not stand in for actual M4/M5/frozen-query runs.

The learner is fixed: 12-4-1 network, 57 parameters, tanh hidden/linear output,
half-square loss, deterministic initialization, learning rate 1/1024, exactly
two ordered passes, atomic updates computed from the old complete weight vector.
Training scaler uses training-only mean and population standard deviation.
No clipping, RNG search, hidden refit, changed horizon or enlarged budget.
Composite aliases do not add factors or forwards. Distinct DAG identities do
not establish statistical independence.

## Original failures and successful checks

The [assembled validation record](../research/2026-09-08-mixed-message-assembled-validation.md)
is authoritative; do not summarize all original gates as green.

- First root full preflight: exit 1, 17 checks passed, Tests.FSharp process
  exited 139. Matching macOS report locates the fault in .NET background GC;
  it does not establish the root cause. Original partial test counts were
  not a completed suite.
- One separate unchanged-source diagnostic `dotnet test --no-build` run:
  exit 0, 7 project TRX files, 7,835 passes, zero failures, six existing skips.
  All 12 selected source/DLL/configuration identities stayed unchanged.
  The F# aggregate skip counter disagrees with individual skipped rows;
  both originals and their explicit qualification are preserved.
- First assembled Python run: 110 passes, one test-fixture failure because
  default temporary storage was outside the repository. The owner corrected
  only that test; final root default-temp run passed all 111 in 4.86 test
  seconds. Strict typing also passed; production bridge bytes did not change.
- The test owner's separate full gate had a Core F# compiler exit 139. One
  unchanged Release build retry passed with zero warnings/errors. Equal exit
  numbers do not prove a shared cause with the test-process crash.
- Formatter completed successfully with its documented F# limitations.
  Normal pushes have passed all 16 quick checks. No broad repeated test run
  is needed merely to erase the retained failures; normal PR CI remains due.

Complete archives include assembled-validation-1 (86 original identities),
final-source-recheck-1 (47), helper-review-1 (533), helper-followthrough-1
(868), and final independent admission (22). Repeated byte objects retain
separate original identities. The existing indexes lead to exact manifests,
commands, stdout/stderr, source snapshots and outcomes.

## Frozen capsule and executable entry point

Capsule directory relative to the writer:
`docs/research/mixed-message-epoch-implementation/2026-09-08/implementation-source/`.

Accepted service manifest, 22,381 bytes:
`F92041F0DBC344DA877DFA970B3C5C7AD6817C8285AAA28E164872F2CE6D56F1`.

Accepted `custody.tar.gz`, 39,770,169 bytes:
`E51A00FC29FA2A13B09F152AB0F55908B7E03982036E5084F54A221815149445`.

All 298 archive members, 65 selected sources, seven direct files and 69 flat
bindings were independently checked. This is selected-source/direct-file
identity, not transitive runtime closure or source-to-machine equivalence.
The freeze ran once, exit zero, 12.508 seconds; its metadata commands and Git
reads were not numerical execution. Do not rerun or regenerate the capsule.
Its README's historical pending-review wording is superseded by the separately
committed final admission; do not edit frozen sources to refresh that wording.

Existing local helper `.git/run-mixed-registered-1.py` is 6,936 bytes, SHA256
`FBD0DF785D5AECAE47591D73EA3062050825F33241880B67668B462ADDAC2462`.
Its identical `run-registered.py` member is durable inside the accepted archive.
If recovery is necessary, recover only that regular named member after checking
the accepted archive/member identity; do not rewrite the helper or bulk-extract
an archive over the repository. It checks its own archived bytes and both
independently supplied hashes before child entry.

Python executable:
`/Users/acehack/.zeta/agents/codex/Zeta-rendered-catch-reference-20260906/src/Interp.Python/.venv/bin/python`.
Dotnet host: `/Users/acehack/.local/share/mise/dotnet-root/dotnet`.
The helper assumes this writer and observed runtime files. A different host,
rebuilt DLL or changed selected source requires explicit new admission, not
silent hash replacement. Documentation commits outside selected files do not
change the accepted numerical source identity.

At handoff both `.git/mixed-registered-execution-1` and
`.git/mixed-registered-m4-parent-1` were absent. No named control has run.
Recheck for a concurrent or resumed invocation before creating either path.
Do not delete a now-existing attempt directory or repeat its first attempt.

## Exact next action: M4 once, inspect before M5

From the writer, retain the exact parent command, working directory, current
Git head, independent review commit, helper bytes, stdout/stderr, elapsed time
and exit in a new exclusive `.git/mixed-registered-m4-parent-1/` observation
directory. Parent observation must not change the registered numerical driver.
Then execute this exact fixed command, without a retry loop:

```sh
PYTHONPATH="$PWD/src/Interp.Python" \
/Users/acehack/.zeta/agents/codex/Zeta-rendered-catch-reference-20260906/src/Interp.Python/.venv/bin/python \
  .git/run-mixed-registered-1.py m4 \
  F92041F0DBC344DA877DFA970B3C5C7AD6817C8285AAA28E164872F2CE6D56F1 \
  E51A00FC29FA2A13B09F152AB0F55908B7E03982036E5084F54A221815149445
```

The helper creates `.git/mixed-registered-execution-1/`, with `m4/` capture
and `attempts/` original bridge custody. Attempt name is `m4-registered-1`;
session is `m4/registered-1`. It retains before/after source snapshots, actual
child return, errors and per-file inventory. Its exit zero establishes capture
closure, not numerical success. Missing files, child faults and admission
refusals remain outcomes, not zero work or invented complete returns.

Inspect the actual bridge final record/journal, all service requests/responses,
EpochReturn and committed prefix. Store descriptors carry File, Bytes, Sha256,
Encoding, StoredBytes and StoredSha256; resolve against their actual Store root
and verify both stored/original identity. Files use the existing
`<state.name>/record-<index:06d>.<suffix>` convention. Full BridgeResult is
retained in memory; durable terminal records reference the actual originals.
Do not create a replacement numerical result from the summary JSON alone.

The fixed M4 has one sweep and two ordered projections:

1. `control/0-positive`: Gaussian base precision 1, eta 1.5, k -0.75, c 1.
   Expected optimum mean -0.25, variance 0.5; checked unary quotient has eta -2,
   precision 1. Keep proposed/certified/applied states separate.
2. `control/1-cancellation`: base eta 0, precision 1e-300, k 0, c 1.
   Expect reference IterationLimit and certificate NoRootEnclosure, no second
   unary application, and the positive committed prefix preserved.

Expected work is two native calls and two certificates/nested references, with
one real peer and the fixed outer reservations. A different actual outcome is
a failed control, not permission to retune until it passes. The cooperative
300-second deadline is not a hard real-time or arbitrary-precision preemption
claim. Retain failed prefixes and unknown/incomplete counters honestly.

Only after M4's actual required outcomes pass, run the same helper once with
mode `m5-and-frozen-nested` and the same two hashes, under a distinct exclusive
parent observation directory. Do not manually substitute plans or sessions.
The fixed invoker constructs and checks the actual dependencies itself:

- Child training on rows 0/1, cut 3: four SGD steps.
- Two target-hidden queries at origins 4 and 6.
- Parent training on rows 2/3, cut 7, using those actual sealed forecasts:
  four SGD steps.
- M5 totals: four closed sessions/peers, ten forwards, eight SGD steps, zero
  projections/native preparation, two actual immutable artifacts, complete
  remote observations and actual final journal. No fifth M5 session.
- Then a separate one-session frozen nested query consumes those two artifacts:
  hidden feature 0.75 at origin 8, target/label time 9, null target. Expect two
  forwards/two scheduler operations, zero fitting/SGD/projections, and no
  third forward for the composite alias. Child scaled feature is 6, parent 0.

Read the [fixed invocation record](../research/mixed-message-epoch-implementation/2026-09-08/registered-source/README.md)
and [frozen-query registration](../research/2026-09-08-mixed-message-frozen-nested-query-register.md)
for the full declared acceptance criteria. Failure of a prerequisite stops the
configuration. A necessary repair gets a separately registered corrected
attempt and source identity with the original failed attempt preserved.

## What follows actual controls

1. Archive complete original parent/inner execution evidence with exact source
   and runtime pins. Record numerical outcomes, refusals, retained state,
   resource costs and all closure limitations. Add independent outcome review
   using the existing reviewer lanes if available; do not assume new model
   sessions inherited their private process state.
2. Update the implementation register and this handoff with the actual verdict.
   Open a focused implementation/results PR, run required checks, address real
   review findings, merge normally and verify remote-main ancestry/tree. Carry
   the existing PR17052 publication receipt with substantive work; avoid a
   chain of proof-only PRs.
3. Exit the bounded adapter prerequisite after the declared controls and actual
   learned artifact consumed by the frozen query. Preregister one chronological
   learned comparison: individual, flat, shallow and deeper compatible frozen
   module pools; eight as-of features, fixed horizon, training/validation/test,
   horizon embargo, training-only scaler and strict inner child forecast cuts.
4. Include a strong feasible pinned current published comparator. Failures,
   abstentions, refusals and full resource costs stay in denominators. Do not
   select models using test results, expand the search until a win, or call
   four-row training loss a held-out/SOTA result. Make the next investment
   decision from the registered outcomes, including a negative result.

The [comparator census](../research/2026-09-08-current-forecast-comparator-source-census.md)
and its API-source follow-up preserve primary source/model metadata only:
Chronos-2 and TimesFM-3. No weights, dataset, inference or package installation
was performed for this census. Chronos source pin is
`8589d1988e9676817548e9626738ff06b6ca6370`; TimesFM source pin is
`8cb7eda91c2b416b37e99a979afc50f2a18e1791`. Recheck current primary sources
before choosing or making current ranking claims.

Chronos `predict_quantiles` labels its returned 0.5-quantile point value
"mean" although it is a median; `predict_df` inherits that behavior. Fix the
point-statistic meaning in registration. `cross_learning=False` is default;
enabling it allows batch information sharing and can leak future-origin
context across an as-of benchmark. Pin grouping/batching before evaluation.
TimesFM source/weights licenses differ; feasibility and terms are not yet
settled. Author rankings do not transfer to Zeta measurements. Compiled streams
9307/9409 remain unopened; compiled-controller investment is paused.

## Preserve the user's broader research direction

The older [full research handoff](2026-09-06-vera-to-vera-predictive-state-research-and-arc3-bridge.md)
and [unattended continuation](2026-09-06-vera-unattended-research-continuation.md)
index the earlier rendered prediction, planning, Bayesian circuits/DAG lineage,
QBism/Liouville, EP/BP/VMP, affect propagation, Ferry throttler, scheduler,
Vision, CHIP-8, resource-money, anti-Sybil and Yang-Mills connections. Follow
those records for exact completed/negative results; this checkpoint does not
reclassify them as new empirical evidence.

- Magnet means retained history; generators/interpreters over history can
  evolve before their outcomes become further history. Repeated visible output
  need not reveal hidden evolving state. Static magnetic force does no work
  without displacement; this does not imply free computation. Finite cycles
  do not establish physical time crystals or quasiperiodicity.
- Additive per-identity entropy floor and pairwise consistency multiplier stay
  distinct. Fabricated memories/cartels are expected adversarial behavior;
  consult the existing formal analysis rather than treating them as a surprise.
- Name the invariant preserved by skewed memories before Lorentz claims.
  Explicit functors/maps and falsifiers precede CQM, Clifford or WSet claims.
- Noninterference and highest regard across four corners do not alone establish
  a normalized CHSH construction or the 2-sqrt-2 bound. Preserve the user's
  proposed connection without claiming a proof that has not been supplied.
- Multi-oracle is preferred. An unchosen default must be conspicuous; silence
  is not selection. Runtime mandatory-choice behavior remains a separate design
  choice, not something these research absorbs silently enforce.
- Finite computational Yang-Mills analogies do not prove the physical mass gap.
  Four presentation axes do not establish four physical dimensions.
- Irreducible resources keep their units and separate budgets. Scheduling or
  money does not become likelihood evidence. Gaussian latent-state inference
  does not imply Bayesian uncertainty over all neural weights.

## Operational re-entry and ownership

Existing roles completed their current lanes; no numerical integration job is
running at this checkpoint. Identity/core owner is in
`Zeta-relational-identity-20260906`, peer owner in
`Zeta-rendered-predictor-native-20260906`, bridge/admission owner in
`Zeta-rendered-training-reference-20260906`, all under the same
`/Users/acehack/.zeta/agents/codex/` parent. Their sources/reviews are imported;
remote git and committed reports are authoritative, not remembered agent state.
No new agents are needed just to repeat completed source review.

A 12-hour continuation heartbeat was created earlier in this task. Do not
create a duplicate; inspect its actual current configuration before changing
schedule/model behavior. This user-requested model switch pauses new execution
for the handoff, not an authorization to spend reset credits or alter limits.

Tools already present: Bun 1.3.14 under mise, dotnet SDK 10.0.400/runtime
10.0.11, `/opt/homebrew/bin/gh`, and the pinned Python environment above.
Use normal hooks (`core.hooksPath=githooks`), noninteractive git and body files
for multiline PR text. Do not force-push or bypass checks. Do not modify Codex
private memory without a new explicit request; this project handoff is the
requested durability surface.

Every new substantive commit uses AgencySignature v1 with actual model/harness,
Agent Vera, work item above, and `Co-Authored-By: Codex <noreply@openai.com>`.
The user has authorized normal autonomous PRs/merges; no new permission question
is needed for the already fixed next work. If a concrete blocker emerges,
report it with actual evidence rather than inventing successful closure.
