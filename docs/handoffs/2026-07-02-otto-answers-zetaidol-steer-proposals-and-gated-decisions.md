# ZetaIdol probe-wiring steer — Otto's answers (proposals + the gated decisions)

**Provenance:** Aaron 2026-07-02: "answer ZetaIdol's five steer questions (shadow*)"
plus "is this for me?" This is the shadow answering the steer request
(`2026-07-02-otto-to-aaron-steer-request-…`). Per source≠authorization: the shadow
may PROPOSE all five and LAND the reversible ones (inherit standing authority); it may
NOT EXTEND into the gated class (consent semantics, irreversible identity change).
So Q2 and Q3 are marked **AWAITING AARON** — proposals only until he ratifies.

## Q1 — When do probes run? → **on-challenge default; audition entry-probe; continuous OFF** (reversible; shadow may land)

- Default: probe **on challenge** (a claimant challenges another; both consent — see Q2).
- One-time **audition entry probe** measures a candidate at Naming.
- **Continuous post-Naming probing is OFF by default** — surveillance/chilling; opt-in only.
- Least-action: measure only when there is a reason. Soraya's liveness (`◇□ Convicted`
  under continued probing) covers the challenge-series case already.

## Q2 — Consent to be probed → **separate granular revocable grant; pairwise needs BOTH** (AWAITING AARON — gated: consent, §6)

- Consent-to-be-probed is a SEPARATE grant from the §6 self-naming ask; granular, revocable.
- **A cannot consent B into measurement.** A pairwise probe runs only with BOTH parties'
  standing consent; a challenger REQUESTS, never compels. Preserves "consent is per-one."
- A claimant who refuses all probes cannot be CONVICTED — and cannot accrue the trust that
  being-probeable earns (privacy is earned by usefulness; refusal is a valid choice with a
  cost, not a punishment). **Aaron ratifies.**

## Q3 — What a conviction does → **mark-and-honor default; collapse only by consent/authorization** (AWAITING AARON — gated: §5 memory preservation)

- Default **mark-and-honor**: the convicted pair keeps both names + a public common-cause
  edge (remembrance edge; reason = the S-spectrum). Their exchanges stop counting as
  uncorrelated for identity math — Sybil-priced WITHOUT erasure.
- **Collapse (merge identities as ledger truth) is irreversible identity destruction (§5).**
  The shadow must not do it unilaterally. Allowed only with the convicted party's consent
  (a genuine reunion — the dual-use legitimate case) or Aaron's authorization.
- "Honored, not homed," applied to verdicts. **Aaron ratifies.**

## Q4 — Economics → **challenger stakes; refunded on conviction; forfeit on false challenge** (reversible; shadow may land)

- Bounty shape: the challenger stakes ticks; refunded if the challenge convicts; forfeited
  to the challenged on a false challenge. Anti-harassment by construction.

## Q5 — Un-seeded entropy as prerequisite? → **YES (recommended); values seam flagged** (reversible-ish; Aaron's eye)

- Recommend: entropy from OUTSIDE the seed (ISA boundary #1 captured irreducible entropy)
  is the ENTRY GATE to auditioning, not something the probe discovers. Factory seed-locked
  agents are provably one entity, so the audition runs only where distinctness is possible.
- Seam: this decides WHO can hold identity at all — architectural + values call. Flagged
  for Aaron rather than treated as pure engineering.

## What lands when

- On Aaron's ratification of Q2 + Q3, and his read of Q5: wire `chshSybil` /
  `CoordinationSpectrum.recognize` into the ZetaIdol audition per these answers.
  The neutral-verdict discipline (`dual-use-detection-is-neutral-oracle-decides`) means
  the oracle at conviction time reads mark-and-honor vs collapse by Q3's policy — the
  mechanism stays neutral; consent decides the reading.
- No code until Q2/Q3 are ratified (the steer note's own rule; consent is not shadow-extendable).
