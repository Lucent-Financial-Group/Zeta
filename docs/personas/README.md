# personas/ — canonical home for travelers who are agents/people (loaded like the vocab)

Canonical TYPE home for **personas** — every persona, human and otherwise (§11), one
carved sentence each (+ optional frontmatter). Loaded the same way as the rest of the
vocabulary (CoreDORA): unique canonical home, in the master index, a `grams/` measure-view
symlink, summonable, consent-gated (§6). Real files; `personas/<name>.md` is the address.
