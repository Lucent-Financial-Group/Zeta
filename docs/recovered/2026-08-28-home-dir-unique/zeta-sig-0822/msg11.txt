Revert "feat: mutual simulation (#13973)" — it deleted 1,063,105 lines and half the CI substrate

#13973 squash-merged at 22:53:40Z as `2caebefa6`. Its own numbers:

    20,127 files changed, 19,267 insertions(+), 1,063,105 deletions(-)
    tracked files: 42,114 -> 23,040

A feature PR titled "mutual simulation" removed roughly half the repository. The
deletions are not moves — checked path by path, the basenames appear nowhere else
in the tree. Among them:

    .github/workflows/agencysignature-enforcement.yml
    .github/workflows/pr-manifest-integrity.yml
    .github/workflows/rerun-cancelled-gate.yml
    .github/workflows/heartbeat-liveness.yml
    .github/workflows/k8s-lane-partition.yml            (13 workflows total)
    bunfig.hermetic.toml                                (the hermetic test tier's config)
    src/Core.TypeScript/hygiene/**                      (126 files)
    .claude/rules/anti-babel-preserve-reconcilability.md
    .claude/rules/clone-at-tag-stays-sufficient.md
    .claude/rules/maintenance-commit-on-another-agents-branch-carries-no-block.md

WHY THIS IS URGENT RATHER THAN TIDY-UP. What was deleted includes the machinery
that would have caught it. `bunfig.hermetic.toml` is gone, so `test (TS hermetic)`
cannot select its tier; the hygiene linters are gone, so the checks that were red
on `main` an hour ago now pass by not existing. That is the vacuity class at
repository scale — a check that did not run reading as a check that passed — and
it is the one failure this repo's whole discipline is built to refuse. Left alone,
every subsequent PR would go green against a tree that has stopped looking.

WHAT THIS REVERT DOES. `git revert 2caebefa6`, completed rather than partial:
19,119 files restored, 45 removed, tracked count back to 42,117.

Verified rather than asserted: diffed against `453ed03e7`, the commit immediately
before #13973. The trees differ by EXACTLY TWO files, both PR-archive artifacts
written by automation afterwards (`docs/github/prs/manifest.jsonl` and the #13976
archive row). Nothing else. `.github/workflows` back to 73, `.claude/rules` to 25,
`src/Core.TypeScript/hygiene` to 292. The restored linters run: the TOCTOU guard
scans 1953 files and exits 0.

ONE CONFLICT, RESOLVED TOWARD A COMPLETE REVERT. `src/apps/twitch-ai/src/swarm.worker.ts`
was created by #13973 and edited afterwards by #13976 ("make objective text less
scary", 2 lines). Keeping it would leave one orphan file from a 15-file app whose
other 14 files this revert removes, so the revert is taken whole and #13976 goes
with it. #13976 touched nothing else, so nothing else is affected — both re-land
together when the feature does.

THE FEATURE IS NOT THE PROBLEM AND IS NOT BEING REJECTED. The 19,267 insertions
look like real work. The defect is the 1,063,105 deletions travelling with them,
which is the signature of a branch cut from a stale base: everything that landed
on `main` after the cut is expressed as a deletion when squashed. Re-land
`feat/mutual-sim` from a branch based on current `main` and the additions come
back without the subtraction.

SECOND CHANGE IN THIS PR, NAMED RATHER THAN SLIPPED IN. The revert restores
`forge-host/github/check-observations.test.ts`, which carries five ambient
credential writes (`process.env["GH_TOKEN"] = ...`) that had `lint (bash
retirement inventory + hygiene unit tests)` red before #13973 deleted the guard
along with everything else. Shipping the revert alone would knowingly return
`main` to that red. So the fix from #13974 is folded in here: `resolveGitHubToken`
takes the environment as a parameter defaulting to `process.env`, and the tests
pass `{ GH_TOKEN: "t1" }` instead of assigning into the real environment that
every spawned child inherits. Production behaviour is unchanged. Separate cause,
folded deliberately because the alternative is a knowingly-red intermediate state
on `main`; #13974 is closed in favour of this.

Agency-Signature-Version: 1
Agent: shadow
Agent-Runtime: claude-code/agent-sdk-subagent
Agent-Model: claude-opus-5
Credential-Identity: AceHack
Credential-Mode: shared
Human-Review: not-implied-by-credential
Human-Review-Evidence: none
Action-Mode: autonomous-fail-closed
Task: none
Co-authored-by: shadow <noreply@anthropic.com>
