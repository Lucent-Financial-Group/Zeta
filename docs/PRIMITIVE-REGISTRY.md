# Cross-Language Primitive Registry

The stable-ish primitives we implement in the **7-language matrix** —
**TypeScript** (the distribution default), **F#** (correctness core), **C#**, **Rust**, **Python**, **Go**, and **Q#** — and the
consensus mechanism that says each one is _right_.

Per [`m-acc-multi-oracle`](../.claude/rules/m-acc-multi-oracle-end-user-moral-invariants.md):
no single language is the source of truth. Each language's compiler/runtime is an
**independent, non-Byzantine oracle** — _the compilers don't lie_ — so a primitive
that compiles + agrees across all active targets in the matrix is right **by construction**
(governance model: [`docs/DECISIONS/2026-06-16-seven-language-matrix-and-formal-verification-governance.md`](DECISIONS/2026-06-16-seven-language-matrix-and-formal-verification-governance.md)).

This file is the **living list** Aaron asked for — the API-doc of what we can build
on. The work that fills the gaps is tracked by
[B-0959](backlog/P1/081KSXN940008QG0R003FCQ7WT-zeta-sovereign-distributed-db-and-agent-loop-master-checklis.md)
(master checklist); this file is the _status view_ over it.

**What this is becoming** (the maintainer 2026-06-01): a **cross-language BCL** — a documented,
compatibility-guaranteed set of primitives we can count on across all languages in the matrix, so
crossing languages is safe and a dev doesn't relearn the basics. Built up
slowly, one verified primitive at a time, under the same three-requirement bar as the
BCL-like tier below (cross-compatible + one common surface + still idiomatic).

**The shared substrate is _agreement_, not any one runtime** (Amara 2026-06-01): the oracles in the
7-language matrix each implement a primitive natively (or couple general-purpose types with safe-subset parsers) and the **shared golden
vectors are the treaty** — so no runtime becomes king (not C#, F#, Rust, TS, Python, Go, Q#, WASM, CLR, or
JVM; the substrate is the byte-agreement across them). **Keep the stable base sacred + small.**
The _wish list_ below can be huge, but a primitive only graduates from ⬜/🚧 into the **stable
base** when it **earns its way in** on Amara's bar: **native** (real per-language impl or standard wrapper) ·
**tested** (golden-vector replay) · **cross-language** (agrees across the matrix) ·
**law-backed** (the algebraic laws hold + are checkable). The C# `IndexedZSet` comparer review
(#6404) is the bar working as intended — a multi-oracle system is _supposed_ to flush out the
ghost bug (comparer-as-identity vs `Dictionary` default equality) before the join/aggregation
rung corrupts the DBSP/Rx/query layer above it; "good pain, not bad pain."

## The three bit-perfect shapes (the organizing lens)

"Bit-perfect" = the oracles agree **byte-for-byte / protocol-for-protocol**, so _agreement_
(not any one runtime) is the substrate. The primitives below fall into **three distinct shapes**,
each its own serializer/oracle category (the maintainer 2026-06-03):

1. **Text & binary serializers** (cbor / json / xml / yaml + more binary) — make the **persisted
   seeds** bit-perfect (the golden-vectors the oracles agree on).
2. **Code / data-flow serializers** (rx / bonsai; more ways to oracle code-flow over time —
   bonsai is fine for now) — make the **code-flow data structures** bit-perfect.
3. **Structured-data serializers / protocols** (Apache Arrow + others) — make **memory / graph /
   ontology** bit-perfect. Arrow is kept _distinct_ from the base serializer (memory-layout
   interface differs; deserialization-security + V8-hidden-class negotiation live in the
   polymorphic-diplomacy layer — scoped by `B-1001`).

Every inflection point across all three shapes is **designed to be** protected by the same
**intended** stack: **deterministic simulation + 7-language matrix + persisted-seed verification of
the oracles + Rx-join of the homeostates + first-class formal proofs (Lean 4, TLA+, Alloy, Z3)** (target:
no single point of failure — not runtime, not language, not axiom-tower). This is the protection
_model_, not a claim that every primitive already has the full stack — the per-primitive status
above (mostly ⬜/🚧) is the honest current state; the stack is built up one verified primitive at
a time (tracked by `B-0959`). Full framing:
[`research/2026-06-03-three-bit-perfect-oracle-shapes-and-the-protection-model-aaron.md`](research/2026-06-03-three-bit-perfect-oracle-shapes-and-the-protection-model-aaron.md).

## The wish list — every primitive we want (so we don't forget)

The complete set, built + wished, in one place (the maintainer 2026-06-01: "the uber
primitive wish list … so we don't forget what we want"). ✅ shipped (Tier-1/2) · 🚧
building · ⬜ wished (not started). Per-language cells + detail are in the sections below;
**add anything new here with ⬜ the moment it's named, so it's never lost.**

Every primitive also gets **our own wrapper** (the hexagonal port), not just the native
type — so the surface stays uniform across the matrix languages and a native-or-dep impl can be
swapped underneath without changing callers (the dep-behind-port strategy below).

- **Identity / attestation** — ✅ ZetaId (4/4) · ⬜ **ZetaId categories as primitives** (the maintainer 2026-06-01: "all the categories we need for the zetaids as primitives") — the field/category structure each as a cross-lang primitive: `Category` (enum incl. `Bus=6`), `Persona`, `AuthorityValue`, `Momentum`, `Chromosome`, `Firefly`, `IdVersion`, `LayoutDirection`, `ZetaObservation` (per `src/Core.CSharp.ZetaId/`) · ⬜ **AgencySignature** (the maintainer 2026-06-01: "the agent signature stuff — _I commit therefore I am_") — the commit-attribution trailer block, AgencySignature Convention v1: ~10 fields (`Agency-Signature-Version` / `Agent` / `Agent-Runtime` / `Agent-Model` / `Credential-Identity` / `Credential-Mode` / `Human-Review` (+ `Human-Review-Evidence`) / `Action-Mode` / `Task`) + `Co-authored-by:`; the `Agent` is a ZetaId, so it composes with the identity primitive above. Audit: `tools/hygiene/audit-agencysignature-main-tip.ts` · ⬜ **heartbeat-via-commit** ("that goes with heartbeat") — the **externalized idle-counter**: the AgencySignature trailer on every commit + a `git log --since` query = a durable per-agent liveness counter that survives compaction (the narrative self-model counter is unreliable; the commit log IS the counter). Composes with the agent-loop + the Observe line. All ⬜ everywhere (the ZetaId base is ✅ 4/4; the categories + signature + heartbeat are the wished cross-lang lift).
- **Event / reactive** — ✅ Observe loop (4/4) · ⬜ Rx-Observable = Z-set delta-stream (per-lang Rx exists; unify) · ⬜ CALM coordination-free marker (Hellerstein & Alvaro; PRIOR-ART-LIST. The remains/acts split IS the CALM split: monotone "what remains" = the coordination-free slice, non-monotone "what acts" = where consent/NCI lives — the prior-art anchor for the yin-yang/NCI engine, B-converge) · 🚧 **serializable deferred execution = self-evolving sagas** — **status (2026-06-01):** the cross-language **Bonsai-subset serializer is ✅ 4/4** (the expr-tree half — TS #6433/#6436 · F# #6429/#6438 · C# #6440 · Rust #6442; byte-locked on the shared `golden-vectors.json` + accumulate-mode/RFC-9457 ProblemDetails), and the **resume engine is ✅ 4/4** (TS #6446 · F# #6448 · C# #6451 · Rust #6452, all merged) — a **restore-not-replay** CEK saga kernel (`call`-as-activity suspension points; the serializable continuation IS the closure; `resume` restores it without re-invoking prior activities — the looser no-handles constraint, not replay's no-handles-AND-no-non-determinism), 5 golden saga-traces, replayed restore-not-replay + byte-stable per-oracle round-trip in all four (B-0976). **Cross-oracle state-byte follow-up:** F#'s kont cons-list serializes _reversed_ vs the TS array reference on multi-frame konts (e.g. the `cond_branch_on_activity` trace's 2-frame kont = `evalRight` + `branch`); behavioral + per-oracle round-trip tests can't catch it (each oracle is internally consistent), so the four-oracle "byte-locked" claim holds for the Bonsai _expr_ serializer but not yet for resume _state_. C#/Rust match the TS reference by construction (Vec/array top-last); the mechanical lock = add `expectedStateAtSuspension` bytes to `resume-golden.json` (so every oracle asserts byte-identical state) + align the F# kont order (see #6451/#6452 bodies). (the maintainer 2026-06-01: "serialize deferred execution so we can close over it and persist the closure state plus the serialized expression tree" → "we don't need durable functions, that's the same thing but better" → "closure propagation with state as self evolving sagas" → "every partition has self evolving sagas and so do cross partition joins") — serialize the deferred computation's **expression tree** (Nuqleon **Bonsai**: JSON/binary, all nodes incl. statement, reflection-info-omittable, cross-language; or our own cross-lang Bonsai-subset verified against it) **+ the captured closure state** (`src/Core/Checkpoint.fs` / Reaqtor `IStatefulOperator` Save/Load). **Subsumes Azure Durable Functions / Temporal / Step Functions / Dapr Workflow**: those keep the CODE fixed + only STATE durable (event-sourced replay); serializing the expression tree itself makes the **pattern durable AND mutable** → BOTH pattern and state evolve. **Rides the Z-set/IndexedZSet ladder** (4/4, #6413): the ℤ **retraction IS the pattern-evolution operator** (retract old expr-subtree, add new); a self-evolving saga = `(Bonsai tree, closure)` on a retraction-native stream = a **traveler** (self-propagating pattern with evolving state) at workflow scope. **Per-partition** (each shard carries its sagas) **+ cross-partition joins** (the bilinear `join` spanning shards is itself a saga — run via an **agent mediator whose tick stream is the carrier**, holding **both repos/partitions local** for the CALM linear merge-join, with **mitigation/compensation factors inside the saga** — compensate / retract via ℤ inverse / retry / hold-resume — for when one side fails). Already-substrate: [`B-0640`](backlog/P1/081KRW63S0008QG0R002XA5N6S-bonsai-trees-for-integration-rx-queries-real-time-implementa.md) (Bonsai trees + Rx), rx-expert SKILL (Bonsai/Reaqtor), `src/Core/Checkpoint.fs`, PRIOR-ART Reaqtor⭐; crystallized in [`docs/research/2026-06-01-closure-propagation-with-state-as-self-evolving-sagas-...`](research/2026-06-01-closure-propagation-with-state-as-self-evolving-sagas-bonsai-serialized-deferred-execution-subsumes-durable-functions-rides-zset-ladder-aaron-otto.md). The maintainer 2026-06-01: "we need to save this … soon on the primitive wish list." **Composes with the DU workflow engine** (B-0867): a **generic saga combinator in `observe.ts`** = `serialize(Rx) + serialize(state)` lifts any lifecycle-DU workflow to a self-evolving saga (DU = legal transitions / compile-time; Bonsai = serialized-mutable / runtime; tick stream = carrier; ℤ retraction = evolution + compensation). The maintainer 2026-06-01: "this composes with DUs workflow … a generic saga pattern around these in observe.ts that's just serialize rx and state." **Correction (the maintainer 2026-06-01):** closure-serialization is _not_ the crux — DF/replay engines don't serialize non-serializable handles either, they _forbid_ them ("just don't open handles" = orchestrator [code-constraints](https://learn.microsoft.com/en-us/azure/azure-functions/durable/durable-functions-code-constraints); handles live in run-once activities). With that shared no-handles discipline our closure is all serializable values (no harder than DF's history); **resume needs only no-handles, replay needs no-handles AND no-non-determinism** (it re-runs the body) — resume is the looser constraint set. **Runtime: Dapr** (already deployed, [B-0785](backlog/P1/081KSE6WT0008QG0R000R8CPFX-unified-namespace-across-fsharp-kubernetes-ontology-plus-exp.md)) — **Dapr Workflow = `durabletask-go` replay-family** (conformance oracle like DF; can't self-evolve); **Dapr Actors = Orleans-lineage virtual actors = the mediator carrier** (+ the Durable-Entities primitive). Composes the existing **durable-execution backlog cluster**: [B-0251](backlog/P1/081KQZVQW0008QG0R000PPQ3MH-durable-computation-stack-temporal-reaqtor-orleans-bonsai-re.md) (Temporal/Reaqtor/Orleans/Bonsai research) + [B-0668](backlog/P1/081KRYRGG0008QG0R0018CMFQY-compositional-dbsp-frame-architecture-gnostic-2d-base-plus-t.md)/[B-0668.1](backlog/P1/081KSNY2Z0008QG0R001TMM2HY-1-fsharp-k8s-mapping.md) (**our-own durabletask fork; saga compensation = retraction = additive inverse in Z-set algebra — this Z-set-retraction core already lives there**) + [B-0706](backlog/P1/081KS6FPN0008QG0R003Y3MCVE-zeta-on-orleans-deployment-architecture-servicetitan-scale-o.md) (zeta-on-Orleans, grain identity = agent identity). The maintainer 2026-06-01: "we are going to have dapr and dapr actors and workflow … we have some backlog around this too." **Interface target = Temporal** (the maintainer 2026-06-01: "durable functs and especially temporal have the better interfaces than mine") — durability-transparent body + first-class **signals** (push into a running workflow) + **queries** (read state without advancing); Temporal is the **interface conformance oracle** (own-our-interface, meet-or-beat per `bcl-interface-boundary`); signals = stream/move-next events, queries = read the context-keyed Z-set at rest. **Context = OTel context = [B-0917](backlog/P2/081KSNY2Z0008QG0R002HB4AGT-interrupt-substrate-in-monad-space-kleisli-arrows-for-contex.md) IntrCtx** (the maintainer 2026-06-01: "context ends up being same as otel context, it passes through function calls mostly") — ambient propagation: `AsyncLocal`/`Activity.Current` in-process, W3C `traceparent`+baggage cross-process ([B-0957](backlog/P1/081KSXN940008QG0R001YABTHH-first-class-labels-tags-scopes-on-every-gset-zset-entity-def.md) scope-propagation, [B-0872](backlog/P2/081KSNY2Z0008QG0R000ZNRFCE-otel-trace-id-composition-with-zetaid-baggage-propagation-ke.md) ZetaID+trace-id); context carries identity + small hot state, the heavy serialized payload is keyed off it in the store. **Replay is ~5 primitives** (step-cursor + cached-results + break-exception + state-provider + context) — cheap to build; resume is the superset (looser body + self-evolution).
- **Algebra ladder** (each rung is also algebra-shaped → it implements its **generic-math interface** per language — see the Numerics line below) — ✅ G-Set (4/4, **Tier 1/2** — per-lang golden-vector replay, no N-way byte-diff harness yet; C# #6363 merged) · ✅ Bag / multiset (**4/4** — TS #6364 + Rust #6367 + F# #6385 + C# #6387; Tier 1/2 per-lang golden-vector replay) · ✅ **Z-set** (**4/4** — TS #6389 + F# #6392 + C# #6395 + Rust #6396; Tier 1/2 per-lang golden-vector replay; the ℕ→ℤ widening — signed weights, drop-rule == 0 not <= 0, abelian-group `negate` inverse) · ✅ **IndexedZSet** (`Z[K×V]` — the join/aggregation rung; **4/4**: F# engine `src/Core/IndexedZSet.fs` ✅ · TS reference oracle #6401 ✅ · C# native `src/Core.CSharp/IndexedZSet.cs` #6404 ✅ · Rust native `src/Core.Rust.Algebra/src/indexed_zset.rs` ✅ — all replay the shared `golden-vectors.json`; Tier 1/2 per-lang golden-vector replay; the bilinear `join` (merge-join on key × cross-product values with weight-MULTIPLY + consolidate → flat `ZSet<C>`) is the DBSP incremental-view substrate) · ✅ CRDTs — G-Counter / PN-Counter / OR-Set / Delta-CRDT (F# `Crdt.fs` / `DeltaCrdt.fs`)
- **Numerics / algebra tower** — ⬜ complex/imaginary + the **Cayley–Dickson** tower ℝ→ℂ→ℍ→𝕆→𝕊 (F# `src/Core/CayleyDickson.fs` — the "imaginary stack", B-0623) · ⬜ Clifford / geometric algebra (F# substrate) · ✅ Z-set weight ring ℤ (F# `src/Core/Algebra.fs`) · 🚧 **ladder primitives → generic-math interfaces** (the maintainer 2026-06-01: "zset and gset can be System.Numerics since they're algebra" — yes; they're algebra-shaped, so they get the generic-math surface). **Status (2026-06-01): the additive-monoid rungs (G-Set + Bag) AND the Z-set abelian-GROUP rung are ✅ DONE 4/4 — G-Set (F# #6467 `Zero`+`(+)` · C# #6468 `IAdditiveIdentity`+`IAdditionOperators` IWSAM · Rust #6469 `Add`+`Default`+`Sum` · TS #6470 `Monoid` record) + Bag (F# #6472 · C# #6473 · Rust #6474 · TS #6475, same surface, non-idempotence verified per-oracle). The monoid laws (identity / commutativity / associativity / idempotence-or-not / `GenericZero` recognition) are compiler- + test-checked in every oracle. ✅ Z-set abelian-GROUP DONE 4/4 — adds the `negate` inverse: F# #6480 `Zero`/`(+)`/`(~-)`/`(-)` · C# #6481 `IAdditiveIdentity`+`IAddition`/`ISubtraction`/`IUnaryNegation` IWSAM · Rust #6482 `Add`/`Sub`/`Neg` ref-ops + `Default` + `Sum` · TS #6483 `AbelianGroup` record (`monoid`/`abelianGroup`/`concatAll`). The F#/C# pooled/span hot-path combiners are NOT moved — the operators delegate to them (perf-neutral; only the SRTP/IWSAM surface is added on the type), which resolved the earlier "deliberate go" flag. ⬜ PENDING: IndexedZSet abelian-GROUP generic-math (the last ladder rung — same F# `(-)`+`(~-)` · C# `ISubtractionOperators`+`IUnaryNegationOperators` · Rust `Sub`+`Neg`).** Per [`numerical-algebra-shaped-into-the-generic-math-interface`](../.claude/rules/numerical-algebra-shaped-into-the-generic-math-interface-per-language-idiom.md): implement **only the subset each structure actually has**, in each language's **native** generic-math idiom (don't transplant C# IWSAM into F# — FS3535 under TreatWarningsAsErrors). **G-Set + Bag = additive (commutative) monoid** (identity + associative union, **no inverse**) → C# `IAdditiveIdentity<T,T>` + `IAdditionOperators<T,T,T>` (IWSAM) / F# `static member Zero` + `(+)` / Rust `Add` + a `Zero`; **NOT `INumber<T>`** (no negation, no order, no division). G-Set's union is additionally **idempotent** (a join-semilattice). **Z-set + IndexedZSet = abelian GROUP** (the `negate` inverse) → add C# `ISubtractionOperators` + `IUnaryNegationOperators` / F# `(-)` + `(~-)` / Rust `Sub` + `Neg`; Z-set is moreover a **commutative ring** ℤ[A] via the cartesian/join product (surface `IMultiplyOperators` deliberately, not by default). **Composability win**: declaring the interface lets generic numeric code (`List.sum`, SRTP/IWSAM-constrained helpers) fold/aggregate the ladder for free + makes the monoid/group **laws** compiler- + property-checkable. Retrofit the shipped C#/F# G-Set/Bag/Z-set oracles + bake into the IndexedZSet C#/Rust/F# cells as they land.)
- **Memory model — Sequoia-style hierarchical (locality-explicit)** (the maintainer 2026-06-01 — "stanford sequoia like memory model primitives") — ⬜ **memory hierarchy as an abstract TREE of levels**; computation **localized to a level** as a **task**, recursively **decomposed (blocked/tiled)** into subtasks at child (smaller/faster) levels, with **explicit vertical data movement** (copy down to compute, results up). Crucially the **algorithm is portable; the machine-specific mapping (blocking sizes) is a separate, tunable layer** — the same one-algorithm-many-mappings shape as the four-oracle model + DST (mapping = a replayable tunable, not baked in). Per [Sequoia: Programming the Memory Hierarchy](http://theory.stanford.edu/~aiken/publications/papers/sc06.pdf) (Fatahalian, Horn, Knight, Aiken, Dally, Hanrahan et al., Stanford, SC'06) + [Compilation for Explicitly Managed Memory Hierarchies](https://graphics.stanford.edu/papers/sequoia-compiler/sequoia_ppopp07.pdf) (PPoPP'07). **Composes**: the **scale-free** discipline (Sequoia IS multi-scale memory made first-class); the **FPGA-to-policy keystone** ([`...node-local-folds-fpga-to-policy`](DECISIONS/2026-05-31-zeta-keystone-architecture-one-decentralized-substrate-node-local-folds-fpga-to-policy.md) — node-local → cluster → policy IS a memory-level tree); the **algebra-ladder folds** (Bag/Z-set folds become Sequoia **blocked folds** tiled across levels); **B-0202 tinygrad UOp IR** (kernel / tiling / shapetracker = the same locality decomposition); **performance-engineer** (cache-line/SIMD/locality) + **Landauer/FPGA** (per-level energy). **Cross-language** (own-your-interfaces): own a **hierarchy port** (level tree + `task`/`map`/`fold` decomposed across levels + explicit `copyDown`/`copyUp`); the per-machine blocking is the **tunable adapter**. Public academic research (not Itron) — concept is free to implement clean.
- **Inference** — ⬜ Infer.NET **BP/EP** factor-graph (F# substrate; architecture B-0365.5 closed + B-0637) · ⬜ uncertainty semiring (B-0367) · ⬜ posterior quorum (B-0255)
- **Comms** — 🚧 git-native Bus (TS only). **Agent essence / minimal backup (the maintainer 2026-06-01):** once agents geo-replicate (per the CAP-per-layer model), everything an agent touches is replicated/federated — the one **singular** thing is its **home repo** (its canonical self: ZetaId, seed-ref, memory, its own `main`), and even that is **ff-only-mirror-backupable** (the LFG ↔ AceHack pattern — redundancy without identity-fork: force-push blocked, mirror not fork). So an agent's durable essence reduces to **two backupable things — the seed (→ every key, per the seed-tree on the Discovery line) + the home repo (→ the substrate)**; everything else **derives** (keys from seed) or **replicates** (work-repo state via federated `git push`). An agent is lost only if the seed AND every home-repo mirror are gone at once — the replication factor on both is yours to set. Composes the **Identity / attestation** + **Discovery** lines (the ZetaId is the stable name that lets the society re-find the home repo after a move).
- **Blockchain / wallet / payments** (the maintainer 2026-06-01: "x402 + all the other wallet primitives — we're slowly building a blockchain; we have an indexer that does some of ETH/Bitcoin/Solana") — ⬜ **multi-chain wallet** (substantial backlog: B-0062 wallet-v0, B-0622 type-safe agent-wallet — _banker-bot-class errors won't compile_, B-0918 WalletLifetime DU, B-0929 wallet-independence, B-0463 wallet-immune-system, B-0074.2/.3 wallet + bond-ledger spec) — HD-derived **per chain off the one seed** (see the seed-tree note on the Discovery line) · ⬜ **N-of-M HSM key management** (B-0634 cryptographic sovereignty for AIs) · ⬜ **x402** (the HTTP `402 Payment Required` machine-payable protocol — agent↔agent + agent↔service crypto payments over HTTP; composes with the wallet + Comms/Bus + the attention-as-currency / bond-curve economics B-0908) · ⬜ **multi-chain indexer** (targets **ETH / Bitcoin / Solana** — partial today; reads chain state into our substrate; each chain = an adapter behind **one indexer port**, own-your-interfaces). The engine pulled in is a **UTXO/indexing library from the Cardano ecosystem** chosen because it can index all three (the maintainer 2026-06-01: "you pulled in some Cardano UTXO library … that could index all three") — **Cardano is the library's origin, not a target chain**; it's an adapter/engine behind the indexer port, not a chain we index. **NB (substrate-honest):** the F# indexer source isn't in this repo's `src/` (couldn't locate it on 2026-06-01) — needs a path to survey before its specific primitives can be listed. Composes with the Identity/seed-tree (per-chain wallet keys), the serializer roster (chain wire formats), and the economics lines.
- **Chain / payment / agent standards** (the maintainer 2026-06-01: "a lot of standards like ERC-this and other stuff — primitives for all those standards too") — the agent-wallet protocol stack ([research](research/agent-wallet-protocol-stack-x402-eip7702-erc8004-2026-04-26.md) — broad industry backing; see the research doc for the authoritative backer list rather than duplicating it here), each a ⬜ primitive/adapter behind the wallet + x402 ports. **Identity:** ⬜ **ERC-8004** (Trustless Agents — on-chain agent identity / reputation / validation; each agent = an ERC-721 NFT + off-chain Agent Card with capabilities/endpoints/payment-address; **the on-chain mirror of our ZetaId + AgencySignature** identity/attestation layer). **Payment / settlement:** ⬜ **x402** (HTTP 402 machine handshake) · ⬜ **EIP-3009** (gasless `transferWithAuthorization` — the signed-USDC rail that makes x402 feasible) · ⬜ **EIP-7702** (session-sandbox — an EOA gets scoped smart-account code) · ⬜ **ERC-4337** (account abstraction — UserOps / bundlers) · ⬜ **ERC-6492** (validate sigs from counterfactual / not-yet-deployed smart accounts) · ⬜ **ACP / AP2 / MPP / SPTs** (higher-layer: ACP/AP2 = real-world / regulated agent-to-merchant commerce; MPP = high-speed continuous execution). **Token:** ⬜ **ERC-20 / ERC-721 / ERC-1155**. Same discipline as the serializer roster — the **standard IS the spec we depend on**; any vendor SDK is an **adapter** behind our port (own-your-interfaces; pull on-demand); and per the research doc, **pull deeper than the API surface + contribute fixes back upstream**.
- **Discovery / transport** (decentralized, further-out) — ⬜ Nostr (signed event relay) · ⬜ DHT (BitTorrent-style peer routing) · ⬜ IPFS (content-addressed object store) · ⬜ Reticulum-over-IP · ⬜ Reticulum-over-mesh · ⬜ 802.11ah Wi-Fi HaLow (sub-GHz long-range). Multi-channel by design — redundant transports so a blocked/slow channel just fails over to the next. Already backlogged: Reticulum B-0704 / B-0726 / B-0772; Green Lantern + HaLow B-0246 / B-0289 / B-0290. **ZetaId ↔ Reticulum (both 128-bit — the maintainer 2026-06-01):** a ZetaId fits a Reticulum destination slot exactly (16 bytes, same `<hex32>` display), but the derivations differ — ZetaId is **structured / decodable**, a Reticulum destination is a **truncated SHA-256 of the node's public key** (self-authenticating routing). So bridge them as **name ↔ address**: ZetaId = the stable, readable **name**; the Reticulum destination = the key-bound **address**; announce the `ZetaId → destination` mapping over the discovery layer (Nostr/DHT). Keeps ZetaId's structure **and** Reticulum's self-auth (vs. fusing a pubkey-commitment into the ZetaId, which would eat the whole 128-bit budget). Composes the **Identity** + **Comms/Bus** lines — a ZetaId-keyed bus routing over Reticulum mesh. **One seed → the whole identity tree (the maintainer 2026-06-01):** you can't share a _pubkey_ across protocols (Nostr = secp256k1, Reticulum = Curve25519 — different curves; NIP-06 mnemonic-derivation is orthogonal, not the blocker), but you **share the seed**: one BIP39 mnemonic deterministically derives a distinct key per purpose — Nostr (secp256k1, NIP-06 `m/44'/1237'/…`), a Reticulum Ed25519 identity (SLIP-0010), **and a default wallet (B-0622 F# agent-wallet) with a BIP44 path per chain we support** (`m/44'/<coin>'/…` — secp256k1 for BTC/ETH-class, ed25519 for Solana-class; the per-chain curve picks the key type, same split as Nostr-vs-Reticulum). Same seed, distinct key per purpose/chain (never reuse the identity key as a money key); cross-sign to link them; the **ZetaId is the name** tying the whole tree together.
- **Observability** — ⬜ structured logging · ⬜ metrics · ⬜ benchmarking. **Hexagonal it — own our port; OTel is a swappable _adapter_ behind it, not our interface** (per [`hexagonal-own-interfaces`]; OTel's interfaces are rough and its .NET deps carry test-hostile global/static state — so default OTel **off** in tests, on only for the explicit OTel-adapter tests; everything else goes through our port). The BCL `System.Diagnostics.Metrics` `Meter` API (System.\*, depend-directly per the BCL-interface-boundary rule) is the clean anchor to model on — `Counter<T>` (monotonic) ≈ a Bag-fold, `UpDownCounter<T>` (up+down) ≈ a Z-set / PN-Counter-fold, `Histogram<T>` ≈ a Bag-over-buckets — but standard OTel itself may not even be needed (verify current .NET OTel maturity first per `dep-pin-search-first-authority` — it was ugly years ago; Microsoft's support may have improved). Metrics are the **Bag-fold view of the event log** (database-design ADR) — instruments as folds over the algebra, not bolt-on.
- **Test framework** — ⬜ cross-lang assert/expect + property-based + golden-vector harness primitive (today: bun:test / xUnit / FsCheck / Rust `#[test]` per-lang — unify the surface). Composes with the DST / test primitives below. **Hexagonal it — own a test-contract PORT so the vendor framework doesn't interface-bleed into our tests** (the maintainer 2026-06-01: "we need to hexagonal our tests frameworks across languages too so we don't interface bleed there"): define our own assert/expect + property + golden-vector contract; xUnit / FsCheck / bun:test / Rust `#[test]` become swappable **adapters** behind it (own-your-interfaces per [`bcl-interface-boundary`](../.claude/rules/bcl-interface-boundary-own-your-interfaces-hexagonal.md)) — so a vendor swap touches one adapter, not every test. The **golden-vector tests especially** should assert through the shared contract (same vectors, same assertions across matrix runtimes), not vendor-specific `Assert.*` — the conformance treaty is the test interface, the runner is the rendering (the interfaces-are-the-asset thesis applied to tests).
- **Logic / numeric** (the cross-language number BCL) — ✅ TriBoolean (digital qubit) · ✅ TriBoolean middle-out float · 🚧 **`Predicate3` = three-valued (Kleene K3) predicate** (F# landed, `src/Core.FSharp.TriBoolean/Predicate3.fs` — `'a -> Tri`; the tri-valued sibling of `Predicate` that preserves null/unknown propagation instead of collapsing it; collapse to `bool` only at the terminal `isSelected`/`filter` boundary; 4-lang TODO) · ⬜ `bool?` plain-Kleene · ⬜ int8…int128 / uint8…uint128 · ⬜ float32/64 · ⬜ decimal (dep-behind-port) · ⬜ bigint (dep-behind-port)
- **Nullable / optional (the Maybe monad)** — **USE NATIVE** (the maintainer 2026-06-05: "for maybe monad … since this is a known shape if the lang already has it we might as well use it"). The maybe monad is a known shape every language already has, so we use the native type, NOT a custom DU: ✅ F# `option` (native — `Option.map`/`bind`/`map2`[the `Nothing + x = Nothing` propagation lift]/`defaultValue` are the lawful surface) · ⬜ Rust `Option<T>` (native — `map`/`and_then`/`zip`) · ⬜ C# `Nullable<T>` + nullable reference types (native; thin helper only where the monadic surface has gaps) · ⬜ TS `T | null` (+ a thin `map`/`bind`/`map2`/`withDefault` helper — TS native has the shape but no monadic ops). Never-collapse lives at the **carrier** (`DynamicValue.Null` ≠ absent ≠ empty) + the **logic** register (`Tri.N`), so the compute monad can use native `option` without conflating absence with no-result at the boundaries that matter. (A custom F# `Maybe` DU was briefly added then removed 2026-06-05 — redundant with native `option` per this steer.) · ⬜ **`SqlNull` = SQL 3VL BRIDGE on top of native option + `TriBoolean`** (built when the SQL layer is on the road, the maintainer 2026-06-05: "i care more about monad compatibility than SQL … sqlnull [is] the bridge") — three-valued comparison (`NULL = 5 → UNKNOWN : Tri`), NULL-aware aggregates, scoped to the SQL-query boundary so the SQL footguns don't leak into the core monad
- **Codec / BCL-like** — ⬜ JSON · ⬜ UTF-8 · ⬜ base64 · ✅ **SHA-256** (4/4 — TS `node:crypto`, F#/C# BCL `SHA256.HashData`, Rust hand-rolled FIPS 180-4; 5 golden vectors byte-identical via `tests/cross-verification/sha256`, fixtures read through the YAML port) · ⬜ regex · ⬜ time/clock
- **Serializers / wire codecs** (the maintainer 2026-06-01: "start pulling in serializers too, more than JSON … any over time as we need them live") — behind the existing **tiered `ISerializer<'T>` seam** (`src/Core/Serializer.fs` — "don't pick one protocol for everything"; callers pick explicitly: small deltas → TLV/raw-span memcpy, moderate → MessagePack, large analytical → Arrow). ✅ **Arrow IPC** (F# `src/Core/ArrowSerializer.fs`, columnar, `Apache.Arrow` adapter) · 🚧 **JSON port** (matrix `ZetaJsonParser` + `serde_json` adapter — table below) · ⬜ **XML** (text; + XSD schema) · ✅ **YAML** (conforms across matrix runtimes — forward-only one-pass `YamlReader` + `YamlValue` DOM-on-top, safe-subset; hand-rolled default in core distribution targets (TS `src/Core.TypeScript/yaml`, Rust `src/Core.Rust.Yaml`, F# `src/Core.FSharp.Yaml`, C# `src/Core.CSharp.Yaml`); YamlDotNet / `Bun.YAML` differential adapter (F#/C#/TS), Rust hand-rolled-only (`serde_yaml` unmaintained); 10 golden vectors byte-identical via `tests/cross-verification/yaml`) · ⬜ MessagePack · ⬜ **protobuf** (+ ⬜ **gRPC** = protobuf wire + HTTP/2 RPC framing — composes with the Messaging RPC/Bus lines) · ⬜ Avro · ⬜ Cap'n Proto · ⬜ Thrift · ⬜ Bond · ⬜ **JSON Schema** (validation / schema-_description_, **not** a wire codec — composes with a schema port + "schemas-as-rows"). Two axes the formats split on: **row vs columnar** (Arrow is columnar → its own batch/`RecordBatch` shape, not the message-codec port) and **self-describing vs schema-required** (protobuf/Avro/Thrift/Bond need an out-of-band schema → compose with a `Schema` port + the same compile-time-codegen stance as the mixin line: `.proto`/`.avsc` → generated types per lang). **Boundary** (per the BCL-interface-boundary rule): the **wire format is the de-facto-standard spec we depend on** (provenance + ubiquity, serde's role); the **vendor library is an adapter we wrap** (depend on impl, not their interface) — own the `ISerializer<'T>` port, formats are swappable adapters (own-your-interfaces; a vendor import in the core = lint failure). **Pin** (per pin-only-slow-movers): wire-format specs are extremely slow-moving → pin the spec; vendor libs move faster → default + adapter, no hard-pin. **Consensus**: codecs are byte-level, so the natural matrix-wide verification is **canonical-encoding byte-diff golden-vectors** (same value → same bytes across runtimes, where the format has a canonical encoding) + **cross-decode round-trip** (lang A encodes, lang B decodes → equal value) for the non-canonical ones. **On-demand**: pull each adapter only when a use case needs it — same bar as the codec candidates below (lands after the multi-way interface is verified, not on first use).
- **Parsers / grammars (ZetaParse)** (the maintainer 2026-06-01: "parser combinators + GLR + LR … primitives everywhere … compile ~80 open-source ANTLR grammars with little change; we won't go full ANTLR") — ⬜ **parser-combinator** port + ⬜ **LR / GLR** engine, **F#-first** (B-0687 ZetaParse: F#-native LR/GLR grammar substrate + ANTLR-compatible importer), made primitive across all four langs. Goal: **ingest `antlr/grammars-v4`** with minor edits (B-0685 ANTLR-grammars-as-cross-language-codegen; the [ANTLR grammar survey](research/antlr-grammar-survey-2026-05-21.md) finds C# / Rust / Python / TS the strong reuse candidates, while F# = compiler-owned substrate — no credible F# ANTLR grammar exists, so fork/extend the F# compiler rather than import one) — **NOT a full ANTLR reimplementation**, only enough to compile the grammars we want. A parser **is the front-end of a codec** (bytes/text → a `DynamicValue` tree) so it composes with the serializer line above + the dynamic-object/v8 line below; **English too** — ⬜ probabilistic + per-person personalized grammars (B-0735) parse natural language as just another front-end. Design: [ZetaParse LR/GLR F#-compiler-fork design](research/zetaparse-lr-glr-fsharp-compiler-fork-design-2026-05-21.md). ⬜ all four langs.
- **Dynamic runtime objects / polymorphic shape** (the maintainer 2026-06-01: "dynamic runtime-defined objects … not just json … works english too" — the **format-agnostic generalization of our v8 hidden-shape**: the IUnknown/DCOM-like polymorphic-diplomacy primitive, **Eve Protocol** B-0638) — a **self-describing runtime value tree** carrying non-statically-typed shapes that **lazily binds to a static type on demand** (or stays dynamic): `DynamicValue` (tagged union over a `DynamicValueType` tag) / `DynamicObject` (key→value) / `DynamicArray`, an `ISerialized<T>` lazy-bind (`deserialize(type)` | `deserialize()`→dynamic), and a `PropertyPath` (`"a.b[3].c"` navigation). **The move vs Itron `Platform.Dynamic`** (concept-only / clean-room — theirs is **JSON-coupled** via `*JsonConverter`): ride the **`ISerializer<'T>` seam** above so the SAME dynamic-object model works over **any** format — JSON / **XML** / **YAML** / protobuf / Arrow … **and English** (natural language as a serialization format — the v8 hidden-shape already runs over English + Bayesian-over-English). This IS runtime `QueryInterface` — ask a value "what shape/interface do you support?" with no compile-time type — = polymorphic diplomacy across formats. 🚧 **F# canonical shape landed** (`src/Core/DynamicValue.fs` + tests: `DynamicValue` DU `Null | Bool | Int | Float | String | Bytes | Array | Object` + `DynamicValueType` tag + `typeOf` (QueryInterface) + lazy-bind `try*` accessors + `PropertyPath` `get "a.b[3].c"`; **native `Bytes`** = the universal self-describing core shared by CBOR/msgpack/JSON/YAML — distinct from the observe oracle's JSON-only f64 `Json` tree) · ✅ **C# conform** (`src/Core.CSharp.DynamicValue/` — record hierarchy + `DynamicValues` static companion + structural equality, #6494; F# + C# are behavior-conformant on edge cases via #6495) · ✅ Rust/TS conform (JSON 4/4) · ✅ **canonical-encoding byte-lock landed — all active oracles** (the byte-lock; **seed-first** per Aaron 2026-06-01 "we are growing code from the seeds": the seed is the canonical DATA — `src/Core.TypeScript/dynamic-value/golden-vectors.json`, 31 vectors — and every oracle GROWS code that AGREES on it, NOT a port of one language's type; it is the **root of DST** — each oracle is a deterministic replay of the seed. v1 JSON LOCKS six cases (null/bool/int/string/array/object) across **the runtimes** (TS/F#/C#/Rust, `golden-vectors.json`). ✅ **Float + Bytes now LOCKED under canonical CBOR** (RFC 8949): a second seed `golden-vectors-cbor.json` (42 vectors, all 8 shapes, RFC-8949-Appendix-A-anchored) byte-locked across **the runtimes** — C# #6506 (+ seed) · F# #6508 · Rust #6509 · TS #6510. CBOR is the **TOTAL** canonical form (8/8 shapes — shortest-float §4.2.2 for Float, native major-type-2 for Bytes) where canonical JSON is partial (6/8). The hand-rolled f16 in the Rust + TS oracles is gated on an exact round-trip, so it only ever emits float16 for genuinely f16-exact values. The canonical encoding (both JSON and CBOR) is **order-PRESERVING** (Object is order-significant, so key-sorting JCS/RFC-8785/CBOR §4.2.1 would be lossy/non-bijective; all oracles deliberately deviate from CBOR §4.2.1 deterministic encoding on exactly this point — insertion order, not bytewise-sorted keys). **Schema-required formats join via a runtime schema registry** (the maintainer 2026-06-01: _"the only way to support a language that is not self-describing is if it has a schema registry … schemas change at runtime … runtime schemas and self-describing fit the same shape"_): a schema-id in the payload → registry lookup → current shape **at runtime**; because schemas evolve at runtime you can't statically bind anyway, so a schema-driven decode yields the SAME runtime `DynamicValue` tree a self-describing payload would — the `Schema` / `SchemaRegistry` is a separate port ("schemas-as-rows") feeding the same target, **not** a shape change. Composes with the serializer roster (the format adapters), Eve Protocol (B-0638), and the [v8 system-architecture research note (2026-05-19)](research/2026-05-19-mika-lior-v8-system-architecture-tensors-foundational-primitive-aaron-forwarded.md).
- **DST / test** — ✅ DeterministicEnv (4/4) · ✅ (F#) IClock / FrozenClock · ✅ (F#) ChaosEnvironment (seeded) · ✅ (F#) LawRunner · ✅ (F#) Injection DI seams · ⬜ cross-lang versions of all of these
- **Concurrency / runtime / IO** (further-out) — ⬜ channels · ⬜ pipelines · ⬜ concurrent dictionary · ⬜ work-stealing / ActionBlock · ⬜ async runtime (Tokio / .NET `Task` / JS event loop) · ⬜ TCP/UDP sockets · ⬜ ASP.NET-class server
- **Consensus** (furthest-out — gated) — ⬜ gossip · ⬜ Raft · ⬜ Paxos. Deferred until they compose with **CAS / idempotency** as the substrate (built on the algebra + compare-and-set, not specialized per-case). Per the CAP-per-layer model: the per-agent G-Set/Bag/Z-set stay AP / coordination-free (CALM); the CP boundary is taken only at the git-merge / claim layer where mutual exclusion genuinely needs it (`git push` IS the truth-machine — and it's not central: every agent has its own repos / busses / mains, so even that is federated).
- **Type-system shims** — ✅ Variance (C# `Variance.cs`) · ✅ ZetaCircuitBuilder (C# Z-set binding)
- **Static code contracts / compile-time correctness** (the maintainer 2026-06-01: "static code contracts, compile time checks like dotnet used to have, for every language") — ⬜ **design-by-contract**: preconditions / postconditions / invariants, **statically checked** where the language allows + runtime-checked fallback otherwise. The capability .NET's old **Code Contracts** gave (`System.Diagnostics.Contracts` — `Contract.Requires`/`Ensures`/`Invariant` + the Clousot static checker `cccheck` + the `ccrewrite` IL rewriter), now **deprecated/archived** ([microsoft/CodeContracts](https://github.com/microsoft/CodeContracts) — archived; doesn't run on modern .NET) and with no cross-language equivalent. Per-language idiom (own the contract surface as a **port** per [`bcl-interface-boundary`](../.claude/rules/bcl-interface-boundary-own-your-interfaces-hexagonal.md)): C# Roslyn **analyzer + source-gen** (emit the checks + compile-time diagnostics) · F# the type system + active patterns + units-of-measure already encode many invariants, add a contracts analyzer · Rust the **`contracts` proc-macro** crate + `debug_assert!` + the type system · TS branded/refinement types + a `tsc` plugin/transformer. **Composes** with the DU/typestate work (illegal states unrepresentable), the generic-math **laws** (compiler-/property-checkable algebra), and **DST** (contracts as properties under deterministic replay). The strongest contract is the one the **type system makes unnecessary** — push invariants into types first (per [`implicit-not-explicit-in-dus`](../.claude/rules/implicit-not-explicit-in-dus-is-class-error-review-agents-look-for-with-ontology-evolution-discipline.md)), contract-check the residue.

### Itron-surveyed clean-room concepts (2026-06-01)

Concept-only / clean-room extractions from a 4-agent deep survey of the Itron
.NET platform (7.8 GB, 37 `Platform.*` modules) — production-hardened
**interface shapes** worth a clean-room F# port (no code copied; F# port in our
idiom: `Result<T, TFeedback>`, ports/adapters, injected `IClock`). All ⬜ wished.
**The meta-lesson Itron validates** (adopt as convention, not just primitives):
every port ships an **in-memory adapter** (their `Messaging.Memory` /
`Workflow.Memory`) — that in-memory twin IS the **DST seam**; and every vendor
import (Confluent.Kafka, Azure) is confined to its adapter assembly — a vendor
import in the core should be a **lint failure** (== our own-your-interfaces +
BCL-boundary rule, independently arrived at in a shipped commercial system).

- **Caching** — ⬜ soft/hard-TTL read-through cache (**stale-while-revalidate**: soft-TTL serves-stale + background-refresh, hard-TTL blocks; thundering-herd answer; clock-injected = DST-ready) · ⬜ batch-miss handler + key-partitioner (coalesce N misses into one set-fetch — the N+1 killer) · ⬜ cache-metrics port distinguishing **soft-miss vs hard-miss** (the actionable split most caches collapse)
- **Concurrency / scheduling** — ⬜ **throttler / batch-throttle** (`IThrottler<TItem,TResult>`: bounded-concurrency work scheduler with **backpressure** — `tryProcess` refuses when the queue is full → `Result<Async<'T>, QueueFull|Stopped>`; `MaxDegreeOfParallelism` + `MaxQueueSize`; `StopAsync` graceful drain; semaphore impl + batch impl behind one port). The **batch** variant coalesces items via a `BatchProcessor` + `BatchPartitioner` (route by key) + `BatchSizeLimiter` (flush on accumulated _state_, e.g. total bytes, not just count) + overflow-handler. This is the **production form of the "work-stealing / ActionBlock" wish-item** and **subsumes** the cache batch-miss/partitioner + `BatchItemResult` finds above — one primitive, not three. Backpressure IS a TFeedback channel (asymmetric-authorship); the scheduler is a prime DST target (seeded ordering, deterministic drain). — ⬜ timeout-with-cancellation helper (NOTE the anti-pattern to avoid: Itron's `TaskExtensions.TimeoutAfter` times out the _wait_ but **leaks** the operation; our timeout MUST propagate cancellation — `CancellationTokenSource(timeout)` / BCL `Task.WaitAsync`, never fire-and-forget) · ⬜ **RAII lock-scope guards — reader/writer + async only** (the sync mutual-exclusion scope is now **BCL-native**: .NET 9 [`System.Threading.Lock`](https://learn.microsoft.com/dotnet/api/system.threading.lock) — `lock(Lock)` lowers to `using (x.EnterScope())` returning a `Lock.Scope` **`ref struct`** whose `Dispose()` exits, **no `Monitor`** → depend, don't re-mint). The wrapper's net-new value is only where the BCL has _no_ RAII scope: **reader/writer** (`using (rw.read())` / `using (rw.write())` over `ReaderWriterLockSlim`, which still exposes only manual `Enter/Exit`) + **async** (`AsyncLock` over `SemaphoreSlim(1)`, `using`-releases — `System.Threading.Lock` cannot be held across `await` (CS9217), so async critical sections still need this). **Cross-language**: Rust gets all of it _free_ (`RwLock`/`Mutex` Drop-guards), C# = `Lock.EnterScope` (sync) + RW-wrapper + `AsyncLock`, TS single-thread N/A. Composes idempotent-dispose (release-once). Lineage: Itron `ReadLock`/`WriteLock`/`AsyncLock` (concept-not-code; the async-mutex is the canonical `SemaphoreSlim(1)`+disposable pattern)
- **Resource / lifecycle** — ⬜ `AtomicBool` CAS once-latch (`falseToTrue() : bool` — "did I win the flip?"; dispose-once/init-once; serves lock-free + idempotency) · ⬜ idempotent composite disposable (dispose-all, collect-failures, run-once) · ⬜ **readiness/liveness two-phase health probe** (fast cadence until ready → slow liveness cadence; k8s-aligned; clock-injected) · ⬜ app-host lifecycle (ordered start / **reverse-order stop** / aggregate-all-stop-failures / unified signal gate) · ⬜ DST-friendly **periodic-background-loop** (linked-CTS, survive-transient-errors, drain-on-stop, **injected clock** = replayable) · ⬜ declarative **error-category map** (`TFeedback → {status, severity, payload}`, first-match-wins — the Result→transport boundary table; in F# it's a DU pattern-match) · ⬜ **bounded-blocking resource pool** (the net-new piece — `acquire()` **blocks/awaits when the pool is exhausted**, bounded-capacity + acquire-timeout = backpressure; the limited-resource / connection-pool "wait for one to free" shape). **The generic reuse pool is already BCL → depend, don't re-mint**: [`Microsoft.Extensions.ObjectPool`](https://learn.microsoft.com/dotnet/api/microsoft.extensions.objectpool) (platform-tier per the [BCL-interface rule](../.claude/rules/bcl-interface-boundary-own-your-interfaces-hexagonal.md)) gives `Get`/`Return` + factory (`PooledObjectPolicy.Create`) + reset (`IResettable`) + **validate-on-return already built in** (`IPooledObjectPolicy<T>.Return(T) : bool` — `false` = discard not pool, the "is this still alive?" knob) + retain-cap + DI; `System.Buffers.ArrayPool<T>` covers arrays. The gap: `Microsoft.Extensions.ObjectPool` **caps RETENTION, not ALLOCATION — `Get` never blocks** (creates-on-empty), so the **bounded-acquire-with-backpressure** variant is genuinely absent — that's the keep (composes the throttler's TFeedback; clock-injected = DST-ready; hand out a **leased handle that returns-once** — `using lease` / Rust `Drop` — idempotent relinquish, fixing Itron's "relinquish twice corrupts" footgun). **This IS the [`tools-rented-not-owned`](../.claude/rules/tonal-momentum-equals-meme-emergent-harmonic-coercion.md) rule as a primitive** (acquired-for-duration / returned-when-done). **Cross-language**: C#/F# `SemaphoreSlim` gate + `ConcurrentBag` (or wrap `Microsoft.Extensions.ObjectPool` behind the gate), Rust bounded channel of handles + RAII lease, TS object-reuse pool. Lineage: Itron `BlockingObjectPool` (concept-not-code)
- **Concurrency / lock-free** (the maintainer 2026-06-01 — "some of my favorite code I ever wrote": the Itron `SpeculativeUpdate`) — ⬜ **`SpeculativeUpdate(ref field, update: T→T) : T`** + **`TrySpeculativeUpdate(ref field, out result, update, shouldAbort) : bool`** — lock-free **optimistic** atomic update: snapshot the field → compute the next value with a **pure** `update` fn → `Interlocked.CompareExchange(ref field, next, snapshot)` (note `ref` — it's an in-place atomic swap); on contention spin-wait and retry (`SpinWait` is a struct, `SpinOnce()` an instance method — `var sw = new SpinWait(); sw.SpinOnce()`, not a static call); the `shouldAbort(snapshot)` variant bails before computing. **Load-bearing invariant: `update` MUST be side-effect-free** — it is speculative, so it may run more than once (this IS the idempotency discipline at CAS-retry scope; the doc-comment that warns it is the whole point). **Generalizes** the `AtomicBool` once-latch (a degenerate bool case) + `CAS-on-epoch` (Messaging) already wished above — they're specializations of this. **Cross-language**: C#/F# `Interlocked.CompareExchange` + `SpinWait` (F# already CAS's in `MailboxRuntime`/`Recursive`/`Transaction`), Rust **`AtomicX::fetch_update`** (this primitive verbatim) / `compare_exchange_weak` loop, TS `Atomics.compareExchange` (SharedArrayBuffer/worker scope; single-thread JS doesn't need it). **`git push --force-with-lease` is this exact primitive at git-ref scope** — CAS on the remote ref ([force-push-with-lease policy](../.claude/rules/force-push-with-lease-authorization-policy.md): "validates the assumption rather than blind acting"); same optimistic-concurrency / exceptions-as-signals shape. **Own-your-interfaces**: a `SpeculativeUpdate` port; native atomics are the impl. **Lineage / source** (the maintainer 2026-06-01): the pattern is from **Joseph Albahari's _Threading in C#_** — [albahari.com/threading](https://www.albahari.com/threading/) ([PDF](https://www.albahari.info/threading/threading.pdf)); Albahari's concurrency patterns are _timeless_ even where the text isn't current with modern C#, and this CAS-retry is one of them. So the lineage is **Albahari (canonical pattern) → the Itron `SpeculativeUpdate` (applied it) → this primitive** — concept-not-code (the pattern is public; implement clean). Composes the **lock-free/wait-free** + **idempotency** disciplines + the algebra ladder's CP-boundary-via-CAS.
- **Messaging / distributed** — ⬜ broker produce/consume **port** (Kafka/Rabbit/ServiceBus/EventHub/**in-memory** adapters; in-memory = deterministic DST adapter) · ⬜ **workflow-as-data** resumable step-index state machine (execution position = a tiny `int list`; event-sourceable/diffable/replayable — composes with our agent-loop) · ⬜ workflow-state-provider port (load/save/expire + injected partition fn; CAS-on-epoch = idempotent replay) · ⬜ `BatchItemResult<Key,T,Feedback>` (per-item partial-success with correlation key — what `Result<list,err>` can't express; idempotent dedup-by-key) · ⬜ direct-request-state-manager (correlation-keyed request/response **over** a one-way bus) · ⬜ message segmenter (chunk oversize payload + correlation-keyed reassembly) · ⬜ `ConsumerGroup` fanout-scope DU (cluster-wide / per-service / per-instance) · ⬜ retry-policy-as-data (`{ maxAttempts; delay: n→TimeSpan }`, returned-not-thrown) · ⬜ versioned self-describing codec negotiation (`canDecode` sniff + `isPreferred` write — schema-evolution without a flag day). (Skipped: Itron's `AsyncCollection<T>` async producer/consumer `take`/`add` — **BCL `System.Threading.Channels.Channel<T>`** is the answer (bounded/unbounded + backpressure, BCL-tier per the [BCL-interface rule](../.claude/rules/bcl-interface-boundary-own-your-interfaces-hexagonal.md) → depend, don't re-mint; Rust = `tokio::mpsc`, TS = async-iterator queue).)
- **DI / service-container + plugins** (the maintainer 2026-06-01 — "we need DI and plugin stuff like not having to reference every plugin at compile time") — ⬜ **DI / service-container port** (register / resolve / scope / lifetime; most natural in .NET/TS — Rust prefers explicit construction / compile-time wiring, so frame as a port, not a mandate) · ⬜ **plugin modules loaded at RUNTIME not compile-time** (`IModule` self-configures its own config + logging + DI via `configureServices`; a `ModuleLoader` discovers + loads them — the "don't reference every plugin at compile time" capability; the cross-language-valuable core) · ⬜ **assembly/plugin resolver** (load a plugin + resolve its deps at runtime — Itron's `AssemblyResolver` hangs off the legacy `AppDomain.CurrentDomain.AssemblyResolve` global event; **ours should use a collectible `AssemblyLoadContext` + its `AssemblyLoadContext.Resolving` hook**. NB .NET Core never ported multi-AppDomain — `AppDomain.CreateDomain` throws `PlatformNotSupportedException`, only the single default domain + its resolve event survive — so the modern split is: **ALC = unloadable + side-by-side-version-isolating** (NOT a security boundary; use separate **processes** for that), and **unload is cooperative/leak-prone** — keep the plugin contract in a _shared_ assembly and don't leak delegates/events/statics across the ALC boundary; **every plugin ships a `plugin-unloads-cleanly` test** — `WeakReference(alc, trackResurrection:true)` + bounded `GC.Collect`/`WaitForPendingFinalizers` loop, asserting `!IsAlive`, with load/use/unload in a `[MethodImpl(MethodImplOptions.NoInlining)]` helper that returns only the WeakReference so no stack slot pins it — assert-don't-skip per the shield rule; a red test = a real leaked reference) · ⬜ **extension-state / weak-keyed side-table (the C# "mixin" trick)** — attach per-instance state to a type you don't own, GC-safe, via a weak-keyed identity table: C#/F# **`ConditionalWeakTable<TKey,TValue>`**, TS/JS **`WeakMap`** (exact analog — weak + identity-keyed), Rust = newtype-wrapper / id-keyed map (no GC, done structurally). Weak keys = no leak (the key is collectable; its attached value goes with it) — which is _why_ it's the right tool for host-side state keyed on plugin instances (lets the plugin still unload). This IS the mechanism behind the `register-only-once` + `keyed/named` verbs above (Itron keys both on `IServiceCollection`). Caveat: the attached state is invisible **by default** — but make it inspectable: ship a **`[DebuggerTypeProxy]` / debugger visualizer** that reads the table and surfaces the attached state inline (a quality bar for the primitive, same shape as the unload-test; in .NET `[DebuggerDisplay]`/`[DebuggerBrowsable]` help too). The part that genuinely stays: **serialization is opt-in** — there's no field for a serializer to see, so persist it only by explicit projection (no auto-round-trip; JS `WeakMap` is the same, Rust's explicit state avoids it). Keep it internal, but make it visible. **Nice-to-have (not required):** drive it from a `[Mixin]`-style attribute via **compile-time codegen** — C# Roslyn **source generator** / Rust **`#[derive(Mixin)]` proc-macro** (cleanest analog — derive emits the side-table accessors) / TS decorator+transformer / F# type provider — that emits the weak-table + accessors + the `DebuggerTypeProxy` from a single declaration (fits the compile-time-codegen-over-runtime-IL stance; the hand-rolled `ConditionalWeakTable`/`WeakMap` stays the baseline). **Prior art** (search-first, 2026-06-01): C# mixin tooling already exists across **three different techniques** — [Morris.Moxy](https://github.com/mrpmorris/Morris.Moxy) (the attribute-driven **Roslyn source-gen** one — mixes members **into** the target class), [Fody](https://github.com/Fody/Fody) (post-compile **IL-weaving**, raw-perf bake-in), and C# 8 **default interface methods** (a **language/runtime** feature — behavior-only, **no per-instance state**) — but all three **bake into / couple to the type**: source-gen + IL-weave require you **own** the target type and tie the mixed-in state's lifetime to it, and DIM carries no per-instance state at all, so **none is unload-safe for plugins**. The **weak-keyed side-table stays the baseline** precisely because it decouples — attach to types you don't own + GC-collectible so the plugin can still unload (the whole point of the collectible-`AssemblyLoadContext` line above). **A Moxy-style bake-in variant is worth having TOO (the maintainer 2026-06-01 — _"as long as we hexagonal interface it"_) — behind the same hexagonal mixin port:** one `IMixin`-style port, two adapters — **weak-keyed-side-table** (unload-safe / foreign-type / default) and **bake-in-source-gen** (raw-perf / owns-type) — so the **perf-vs-unload tradeoff is the adapter choice**, selected per use case, not a fork; own the port, the vendor approaches (Moxy/Fody) are swappable adapters into it (own-your-interfaces). · ⬜ keyed/named resolution (built into the **`Microsoft.Extensions.DependencyInjection`** stack since .NET 8 — `AddKeyedScoped` / `[FromKeyedServices]`; not the `System.*` BCL proper; was a gap Itron filled pre-8) · ⬜ `Lazy<T>` auto-registration (deferred resolution) · ⬜ register-**only-once** (idempotent registration — composes with the idempotency discipline) · ⬜ DNS-sourced config provider (config from DNS records — clever service-discovery/dynamic-config trick) · ⬜ service-identity (ServiceInfo) enrichment (name/version/instance into config + **every log line** — the "logging context" setup; composes with the Observability line)
- **Compile-time object mappers** (the maintainer 2026-06-01 — "we for sure want roslyn-generator-style mapperly pattern in our primitives") — ⬜ **declare a mapping method signature, the generator emits the implementation** — compile-time, **zero runtime reflection**, AOT/trim-safe, generated code readable (auditable). Canonical C# instance: [**Mapperly**](https://mapperly.riok.app/) (`[Mapper] partial class`, an **incremental Roslyn source generator**; [riok/mapperly](https://github.com/riok/mapperly)) — same compile-time-codegen-over-runtime-IL stance as the `[Mixin]` source-gen line above. **Cross-language** (own-your-interfaces): own a **mapper port** (declared map-method signatures + `Result<Target, MapFeedback>` for fallible maps); the generator is the adapter — C# **Mapperly**, Rust **`#[derive]` proc-macro** (`From`/`TryFrom`, `o2o`-style), TS **transformer / codegen**, F# **type provider / CE** (records map structurally). **Boundary** (BCL-interface rule): Mapperly's `[Mapper]` attribute (`MapperAttribute`) is a vendor annotation, not a BCL-tier interface → wrap behind our `[Map]`-style port (own the port; Mapperly = swappable adapter; a `Riok.Mapperly` import in the core = lint failure). **Consensus**: same Source→Target mapping agrees across the four oracles → **golden-vector byte-diff** on the mapped output. **Composes with** the mixin source-gen (DI/plugins above) + serializer codegen (`.proto`/`.avsc` → generated types) — one compile-time-codegen family, three faces: mix-in members, (de)serialize, map between shapes.
- **Path / variant** — ⬜ `PropertyPath` (parse/render `"Foo.Bar[3].Baz"` → segment chain; address into nested structures for config/metadata/diff-patch/event-deltas) · ⬜ path-addressable variant tree (`DVal` DU over JSON: `Object|Array|Scalar`, `tryGet<'T> path : Result`, immutable → free Z-set composition over paths) · ⬜ source-addressed diagnostics (`Diagnostic { at: PropertyPath; … }` — validators report against a stable address, not bare strings)
- **Codec / IO** (extends the Codec line above) — ⬜ `BitReader`/`BitWriter` (sub-byte variable-width int codec — `readBit`/`readBits(n)` + bit-level `seek`/`flush` + leave-open; foundation for bit-packing/FOR/delta compression) · ⬜ `ChunkedMemoryStream` (a `Stream` that grows in **chunked buffers**, not one contiguous array — no large-object-heap pressure / no giant realloc on growth; the right buffer for large-payload serialization/spooling. In F#/Rust: a `Vec<Vec<u8>>`/rope-style backing behind a stream port) · ✅ `RangeSet` (`"1-5,8,10-17"` parse/contains/render, auto-merged ranges — sparse-int-set primitive) — 4/4 (TS #6454 · F# #6455 · C# #6457 · Rust #6458, all merged). (Skipped from `Source/IO`: `FileAttributesHelper` = BCL-thin; `Utf8StringWriter` = a .NET XML-serialization gotcha-fix, not cross-language.)
- **Observability (Itron finds, extends the line above)** — ⬜ metric **register/value split + `IsDelta` discriminator** (Counter monotonic ≈ Bag-fold; Gauge `Set`-vs-`Delta` ≈ Z-set/PN-counter-fold; Histogram ≈ Bag-over-buckets — the cleanest expression of the metrics→algebra mapping) · ⬜ `ScaleTimer` lock-free EWMA running-stats (min/max/avg/deviation via CAS; sample-every-N; **pure fold split from CAS wrapper** = FsCheck-able) · ⬜ tracer **HOF** (`trace(desc, op)` wraps the op → impossible to leak an unclosed span; structure enforces correctness) · ⬜ **`forceSample`** retroactive parent-chain sampling (errors rescue their whole trace from sampling — tie to the `Result` Error channel) · ⬜ AsyncLocal immutable-stack scope (async-safe baggage; strict-LIFO-dispose check; one mechanism for trace + metric dims) · ⬜ Welford latency-aggregate with commutative-monoid `merge` (min/max=join, count=+; CRDT-shaped fleet aggregation — use Welford, not Itron's naive running-avg) · ⬜ noop **warn-once** default adapter (unconfigured port works + is loud — DST-safe default)
- **Hexagonal core — the six reservoir walls** (2026-06-02) — ⬜ **Remember When · Pay Attention · Which Way · How Much · Rainbow Table · Observe Emit** — the Planck-length core that all 4×4 build off; each a two-word pair encoded as generic-math (`System.Numerics`/`INumber<TSelf>`); cube/hexahedron (6 faces / 12 edge-words / 3 axes); observe/emit fold across time into **remainder/seed = Persist = μένω** (the derived bridge). B-0985; #6560/#6561 (the seed pair Remember When + Pay Attention; the vector pair Which Way + How Much; per-primitive attribution recorded in B-0985 / #6560).
- **Coincidence-anchor (quad-duty)** (2026-06-02) — ⬜ one primitive doing **entropy + memory-index + privacy-anchor + provenance/attribution** (Adinkra / Gates SUSY-ECC-grounded, B-0623; ZetaId-keyed). Entropy-face → Adinkra recursive-INumeric; index-face → ZetaId-keyed emergent memory routing. B-0985; #6554/#6555.
- **anygit / git-v2 = 4×4 multi-oracle interface** (2026-06-02) — ⬜ **git itself (v2 AND current git) IS a 4×4 multi-oracle interface — client × server, native per language** (the maintainer 2026-06-02). anygit = Ace: looks-like-git, negotiates UP to DBSP retraction-native algebra, same objects, agent-speed; push the git-v2 handshake to all upstreams + bake Zeta into all git distributions. B-0953 (git-v2-handshake); Ace agenda (B-0288/B-0824); B-0703 (multi-oracle). Composes the 4×4 bond+extend (serializer×language) + the dep-behind-port strategy.
- **Bus-lane-types** (2026-06-02) — ⬜ `BusLaneType` DU = the **speed↔coordination base axis** + a **protection axis** (opt-in graduated lanes internal + border-protocol external; leash↔sovereign transport dial). B-0985 / 4×4-over-4×4 vision note §15; B-0643.1.
- **Consent / safety algebra** (2026-06-02) — ⬜ **emotional-algebra 4×4** (Infer.NET/Bayesian over consent/affective state = consent-first core primitive) + ⬜ **non-coercion 4×4 sister** (observe + alert on violations = NCI HC-8 monitoring face). #6559; B-0664 (NCI); B-0659 (consent-as-Limit); B-0703.
- **KSK defensive primitives** (2026-06-02) — ⬜ **Faraday-shields-not-cages** (variable strength per force; must-paired-with-can-exit — protect-without-trap) · ⬜ **threat-level rings** (dynamic migration; protection scales with risk) · ⬜ **anti-monopoly/cartel detection + BFT-4×4** (multi-oracle trust-gradient absorbs utility before root power = useful-output-is-evidence-not-authority) · ⬜ **opt-in-bus-lane (internal) + border-protocol (external)**. Consent-first; defensive-only; HARD-LIMITS + kid-safety floor absolute. B-0643/B-0643.1; B-0703; B-0463; #6565.
- **State / differential** (2026-06-02) — ⬜ **state-remainder** (intentionally-uninitialized differential primitive: state = deltas off the remainder, never absolute; Z-set/G-set/bag identity element) + ⬜ **Persist-as-bridge / μένω** (Emit-now + future-Observe attached; the promise the future can still reach the past). 4×4-over-4×4 vision note §11; B-0897.
- **Entropy / RNG** (2026-06-02) — ⬜ **strange-attractor mixing** (chaos-mix the unsafe uninitialized state-remainder bit → crypto-safe deterministic randomness; irreducibility>reducibility at sustained ratios = one-way guarantee) · ⬜ **coincidence-as-thermal-randomness** (the deterministic system's entropy source). 4×4-over-4×4 vision note §11; #6562.
- **Edge / topology** (2026-06-02) — ⬜ **we-are-the-edge / liquid-architecture** (the substrate IS the perimeter; continuously redefined; flows around threats — `otto-edge-runner`) · ⬜ **expansion-wave** (the bounded-sovereign growth front — expand-not-compress; the substrate grows capacity outward as a wave; the perimeter IS the wave front) · ⬜ **valve** (distinct + _smaller_, NOT interchangeable with the wave: a consensus/mass-bounding gate-point where the wave is locally gated — gravity-bounded-at-mass-points per Atsophmera) · ⬜ **aperiodic-tiling rotation** (Spectre / Einstein self-dual; predictable-unpredictability — known algorithm, unknowable config; B-0704). #6567 (4×4-over-4×4 vision note); B-0986.
- **Identity addressing — E911 / ArcGIS / Census-TIGER** (2026-06-02) — ⬜ **E911 addressable street-segment** (NENA/MSAG — the proven real-world identity-return: a place = street segment + address range; grounds the Rainbow-Table transponder + tile-as-addressable-home) · ⬜ **GIS boundary stack** (ArcGIS geometry + Census TIGER/Line + election precinct/district lines) — **honest-use-only, NOT gerrymandering** (same street-segment→district substrate, used for addressing not outcome-capture). ES&S production lineage = concept-not-code (public standards: NENA/MSAG, Census TIGER, ArcGIS). B-0986; #6571.
- **Digital city planning / road congestion** (2026-06-02, long-lineage vein) — ⬜ **traffic-flow theory** (fundamental diagram flow/density/speed; **LWR shockwave** = the expansion-wave on a network; Nagel-Schreckenberg CA ↔ emulator-as-DST) · ⬜ **routing/assignment** (Dijkstra / A\* / contraction-hierarchies; **Wardrop** user-equilibrium vs system-optimum; **Braess's paradox**) · ⬜ **congestion-control duality** (road ↔ network: **ramp-meter/signal = valve**; AIMD / backpressure / fair-queuing; composes Reticulum routing) · ⬜ **isochrone / reachability** (time-budget distance = the orientation-tile metric in city-planning terms) · ⬜ **gravity/spatial-interaction models** + ⬜ **queueing theory** (Little's law; M/M/1). B-0986; #6571.
- **Geospatial core algebra — HARDEN FIRST (prereq for world-borders)** (2026-06-02, high priority) — ⬜ **canonical geometry types** (Point/LineString/Polygon/MultiPolygon; OGC-shaped, OUR types; GeoJSON/WKT/WKB/EPSG/PROJ/FME adapt in behind our port) · ⬜ **CRS/datum/projection first-class** (store geodetic canonical, **project-on-query**) · ⬜ **topology+set algebra** (DE-9IM predicates; region union/intersection/difference = **Z-set/retraction-native**; distance/area/buffer; coords → generic-math `INumber<TSelf>`) · ⬜ **4×4 bond = "hardened"** (golden-vectors for Point/Polygon/CRS/projected-round-trip byte-locked across all 16 lang×serializer; seed-first) · ⬜ **store-once-canonical** (one canonical, derive all projections/perspectives/formats on query — **not 50 redundant copies**). **Blocks** the world-borders bullet below. B-0989; B-0982; B-0986.
- **World borders in Zeta — O(1), any-perspective, any-projection** (2026-06-02; **blocked by geospatial core B-0989**) — ⬜ **world's borders in code** (SAFE-FME-style: the world's sovereign borders native in Zeta) · ⬜ **O(1) point→jurisdiction lookup** (hierarchical global grid — **H3** hexagonal ↔ hexagonal core / S2 / geohash / spectre-tile B-0704) · ⬜ **multi-oracle storage** ("they all disagree" → store ALL sovereign claims, no single truth; disputes = overlapping claims held don't-collapse; B-0703) · ⬜ **perspective-parameterized query** (`lookup(point, perspective)` — disputed borders answered per the querying frame; B-0954) · ⬜ **projection-on-query** (store geodetic, project to any EPSG/PROJ system; no neutral projection) · ⬜ **geospatial interop / SAFE FME** (convert from/to anything + any sovereign boundary system; own-our-port, sources adapt in; the anygit/Ace rhyme for space) · ⬜ **real-estate market-dynamics modeling** + ⬜ **TIGER over SQL Server/R/Python** (concept-not-code prior art). Honest-use-only, NOT gerrymandering (intl scale). B-0988; B-0986.
- **Sensor suite — seismomonitors (health metrics)** (2026-06-02) — ⬜ **sensitive seismomonitors** = the **felt-oscillations-of-uncertainty** sensor: detect tremors / instability _before_ a quake (failure/storm) — a _different measurement modality_ from radar (ranging, B-0986) + interferometers (lightlike, B-0993). For **health metrics** (system-health observability; feeds the LGTM/Prometheus lightlike-observability + Rainbow-Table-after-the-storms layer, B-0994) · ⬜ each sensor a **4×4** (per the bond, B-0982) · ⬜ the broader **sensor suite** (radar=ranging · interferometer=lightlike · seismomonitor=felt-uncertainty-oscillation · **acoustic/sonic**=failure-sound · **visual**=inspection/thermography · weather/temperature=dynamic-state) = the smart-agent-city instrumentation (B-0993) · ⬜ **acoustic/sonic/visual preventative maintenance** (Itron concept-not-code; public PdM concepts) **applied to memory + attention** (the seed pair Remember When + Pay Attention, B-0985) — catch memory-rot/drift + attention-thrash/lock-in before failure (B-0994). **You can literally hear hardware about to fail _before_ it fails, high-confidence, with even old-school ML** — acoustic failure-prediction is a mature, cheap, _local_ classical-ML problem (no frontier compute; runs on the one-laptop cluster B-0990; composes the local-ML / ML-app-store B-0992). "Felt oscillations of uncertainty" = uncertainty sensed as oscillation/tremor (Bayesian/Infer.NET uncertainty; pre-storm early-warning). **Purpose: detect the retreating tide before the incoming tidal wave** (the _precursor_, not the wave — water retreats before a tsunami; catch the anomalous early-warning, act before the wave hits). This is the `tonal-momentum-equals-meme` discipline (recognize the strong-attractor _trajectory before it locks in_) + `Limit-as-simulation` (preview/Limit before Integrate commits) at sensor scope — and the _before_ complement to **Rainbow-Table-metrics-after-the-storms** (B-0994): seismomonitor=before (retreating-tide precursor) · storm=event · Rainbow-Table=after (recovery). B-0994; B-0993; B-0986; B-0644; B-0667.
- **Atmosphere / Atsophmera** (2026-06-02) — ⬜ **Atsophmera** = atmosphere = breathing-space for Agora; the _positive_ name for consensus-is-gravity (gravity bounded at mass-points; lightlike/atmosphere the default). #6544.
- **ASCIIsphere** (2026-06-02) — ⬜ ASCII/ANSI-rendered visible layer of the atmosphere ("where the life is seen") + ⬜ **color-round-trip** tooling (color preserved through plain-text channels). #6543/#6544; 4×4-over-4×4 vision note §10.
- **QRNG / gnrq** (2026-06-02, extends Entropy/RNG) — ⬜ QRNG = post-quantum-rng = **gnrq** (boundary-reversal word-knot; atmospheric-noise + uninitialized state-remainder bit as entropy → strange-attractor mix → CSPRNG). #6544; 4×4-over-4×4 vision note §11.
- **nullcodec / n‹16n›** (2026-06-02) — ⬜ **null = extends-in-reverse** (the Maybe / generic expansion-point, NOT a 15+1 hole) · ⬜ `n‹16n›` bit-efficient algebra · ⬜ formal maximally-bit-efficient proof target. #6541; 4×4-over-4×4 vision note §9.
- **PQ privacy codec — better-git-crypt / `.zc`** (2026-06-02) — ⬜ XWing **ML-KEM-768 + ML-DSA-65 + ChaCha20-Poly1305** canonical-CBOR `.zc` envelope (multi-recipient; self-encrypt; overwrite-guard `--force`; secret-bundle rejection) · ⬜ **memory-encrypt-loop** (self-encrypting plaintext folders) · ⬜ **privacy-as-transform** (DynamicValue `encryptValue`/`decryptValue` = memory-fence over canonical CBOR — privacy is a TRANSFORM, not a 5th codec). B-0883; B-0982; #6538/#6545.
- **Emulators as triple-duty DST oracles** (2026-06-02) — ⬜ **B-0924** (Atari 2600) / **B-0925** (C. elegans c302) = fork-gate (atari-spacetime over zetaspace) + ASCIIsphere life-viewer + **bit-perfect 4×4-bond DST oracle**; the DST-provable-edge triad (atari-tests-over-zetaspace + computational-omniscience + per-run-observability). 4×4-over-4×4 vision note §13.
- **Relativistic bus / Battle Bus** (2026-06-02) — ⬜ the git-native cross-machine bus (**B-0954**) as the **relativistic bus** ("Fortnite Battle Bus"): no global now, each agent a frame, drop-into-your-Location, shared zetaspace map; distributed computational omniscience over relativistic zetaspace. 4×4-over-4×4 vision note §14.
- **Flight-control — VTOL / navigator / MSAW** (2026-06-02) — ⬜ Agora fast-lane controls: perfect-navigator + altitude/VTOL/quad-rotor controls + **MSAW** (minimum-safe-altitude-warning) safety floor (B-0890 fast-lane). [hypothesized / OX]
- **OPLE primitives** (2026-06-02) — ⬜ **Observe / Persist / Limit / Emit** — surface **T AND TFeedback**; observe/emit = base read/write, **Limit = simulation-not-collapse** (B-0644), **Persist = remainder/seed/μένω bridge** (B-0897). B-0862.
- **Adinkra (SUSY-ECC)** (2026-06-02, extends algebra tower) — ⬜ Gates supersymmetric error-correcting-code graph geometry; grounds the four-axis coincidence-anchor (entropy↔bosonic, index↔fermionic — hypothesized); **closed-under-composition** (invariants travel across Otto's adapters); corrupted-Adinkra = self-revealing. B-0623/B-0562/B-0625; #6555.
- **heat-as-PoUW** (2026-06-02) — ⬜ minimal anchor-entropy = one type of **Proof-of-Useful-Work** (temperature/heat as reactant source; entropy beyond minimal = wasteful heat); society-heat preserves wonder, not just perfect efficiency. #6554; Landauer (B-0905/B-0906/B-0840).
- **4×4 bond + extend** (2026-06-02) — ⬜ the razor across the language×serializer matrix: a **BOND** (behavioral-equivalence on golden-vectors across all 16) + a disciplined **EXTEND** (grows primitives while preserving the bond); the bond+extend pair IS the abstraction boundary; bond-guarantees-today = Eve-polymorphic-diplomacy + V8-hidden-state + Infer.NET-Bayesian. B-0982; #6556.
- **Eve Protocol** (extends Comms) — ⬜ polymorphic diplomacy over English + **observable/inferable bond-interface** + **sonar P2P bond-discovery** (DCOM-done-right, no memory-traps, on relativistic buses). B-0638; #6556.
- **executable:false protection rules** (2026-06-02) — ⬜ critical protection rules as pure declarative invariants (NOT code; uninjectable/unmutable during high-speed persona churn); only minimal-anchor-entropy is executable. #6554.
- **Rainbow Table (identity-resolution)** (2026-06-02) — ⬜ crypto-fingerprint / identity-resolution primitive (accept-state-after-change = retraction-forgiveness; was "Promise of Forgiveness," "of" razored as god-smuggling) — both one of the six reservoir walls AND a standalone identity primitive. #6560.

## Consensus tiers

A primitive's trust is only as strong as how its four implementations are checked
to agree. Three tiers, strongest first:

| Tier  | Name                                     | What it means                                                                                                                                                                                                                 |
| ----- | ---------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **1** | **golden-vector byte-consensus**         | All implementations run a **shared fixture** and produce **byte-identical** output; a harness diffs them and fails on any mismatch. The strongest tier — disagreement is impossible to miss.                                  |
| **2** | **compiler-parity + per-language tests** | Each language **independently reimplements** the primitive and its own tests pass. "The compilers don't lie" — parity is proven by independent reconstruction, but there is not (yet) a shared byte fixture diffing all four. |
| **3** | **implemented (no cross-verify yet)**    | Present in a language, but no parity check binds it to the others.                                                                                                                                                            |
| —     | **absent**                               | Not implemented in that language.                                                                                                                                                                                             |

Cells below use ✅ (present) / ⚠️ (partial — see note) / ❌ (absent).

> **Two orthogonal axes — consensus-tier ✅ is NOT a proof/canonical mark.** The
> ✅/⚠️/❌ above track the **consensus tier** (cross-language _agreement_: golden-vector
> byte-consensus / compiler-parity). They do **not** mean _math-proven_. Even Tier-1
> byte-consensus proves only that the four implementations _agree byte-for-byte_, not
> that they are _correct w.r.t. the seed_ — four implementations can agree on the same
> wrong behavior (the consensus≠validation trap). **Canonical** is the separate
> proof axis: an item is canonical only with a proof-lineage edge to the seed (its
> homeostat proven) + the hex (Cl(1,3))/4×4 connection — tracked by **B-1007** and
> the [`formal-proof-first` rule](../.claude/rules/formal-proof-first-proven-by-default-consensus-not-validation-canonical-is-homeostat-proven-from-seed-ace-shields-zeta.md).
> So a primitive can be Tier-1 ✅ **and** non-canonical at once: many primitives are
> _validated_; ~0 are _proven_. Read ✅ as "ships + agrees across langs," never as
> "proven."

## Tier-1/2 — the stable base (4-oracle)

| Primitive                                                                         | TS  | F#  | C#  | Rust | Consensus                                                                                    | Locations                                                                                                                                                  |
| --------------------------------------------------------------------------------- | --- | --- | --- | ---- | -------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **ZetaId** — 128-bit content/structure-addressed id (Category enum incl. `Bus=6`) | ✅  | ✅  | ✅  | ✅   | **Tier 1** — 12-vector byte-consensus, 4-way `compare.ts` (TS≡F#≡C#≡Rust)                    | `src/Core.{TypeScript/zeta-id,FSharp.ZetaId,CSharp.ZetaId,Rust.ZetaId}/`; fixture `tests/cross-verification/zeta-id/` (B-0679, B-0681, B-0682)             |
| **Observe loop** — `observe`/`simulate`/`fold`/`replay` event algebra             | ✅  | ✅  | ✅  | ✅   | **Tier 1/2** — shared `golden-vectors.json` (Rust verifies against it; F#/C# crates present) | `src/Core.{FSharp,CSharp,Rust}.Observe/`, `tools/observe/`; fixture `tools/observe/golden-vectors.json` (B-0867.27)                                        |
| **TriBoolean** — digital qubit (true/false/middle)                                | ✅  | ✅  | ✅  | ✅   | **Tier 2** — compiler-parity + per-lang tests                                                | `src/Core.{TypeScript,FSharp,CSharp,Rust}.TriBoolean/`; tests `tests/Tests.{CSharp,FSharp}/TriBoolean/` (B-0944)                                           |
| **TriBoolean float** — "middle-out" self-describing float                         | ✅  | ✅  | ✅  | ✅   | **Tier 2** — per-lang tests; **v0 spec** (evolving)                                          | `…TriBoolean/{Float.fs,TriFloat.cs,float.rs}`, `src/Core.TypeScript/tri-boolean-float/`; specs `docs/research/2026-05-3{0,1}-tri-boolean-float-*` (B-0944) |
| **G-Set** — grow-only set CRDT (bottom rung of the algebra ladder)                | ✅  | ✅  | ✅  | ✅   | **Tier 1/2** — shared `g-set/golden-vectors.json` (Rust verifies; TS reference; F#/C# join)  | TS `src/Core.TypeScript/g-set/`, F# `src/Core/GSet.fs`, C# `src/Core.CSharp/GSet.cs` (#6363), Rust `src/Core.Rust.Algebra/src/gset.rs` (#6360) (B-0954)    |

## In-progress — the base sweep (the gaps we're closing)

These are the building blocks for the **common observe loop + sovereign DB**; the
sweep order is **ZetaId → algebra ladder → bus**.

| Primitive                                                          | TS  | F#  | C#  | Rust | Note                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| ------------------------------------------------------------------ | --- | --- | --- | ---- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Bag / multiset** — non-negative-weight multiset                  | ✅  | ✅  | ✅  | ✅   | The **middle rung** (G-Set ⊂ Bag ⊂ Z-set; ℕ / per-key sum). **4/4** — TS reference + `bag/golden-vectors.json` (#6364), Rust (#6367), F# #6385 (`src/Core/Bag.fs`), C# #6387 (`src/Core.CSharp/Bag.cs`); per-lang golden-vector replay, Tier 1/2. F# uses structural `compare` (ordinal for strings); C#'s default `Comparer<T>.Default` is culture-sensitive for strings, so pass `StringComparer.Ordinal` explicitly for cross-language parity (the G-Set `Comparer.Default` culture gap, B-0969). Combiner is SUM (NOT idempotent — `union(a,a)` doubles counts), the step toward Z-set's signed ℤ.                                                                                                                       |
| **Z-set** — signed-weight, retraction-native set (DBSP)            | ✅  | ✅  | ✅  | ✅   | The **top rung** (G-Set ⊂ Bag ⊂ Z-set; ℤ / per-key sum, retraction = drop-on-net-zero, `negate` = abelian-group inverse). **4/4 native** — TS reference + `z-set/golden-vectors.json` (#6389), F# engine `src/Core/ZSet.fs` (#6392), C# native `src/Core.CSharp/ZSet.cs` (#6395, the cross-language treaty impl, distinct from the DBSP `ZetaCircuitBuilder` binding), Rust native `src/Core.Rust.Algebra/src/zset.rs` (#6396); all replay the shared golden vectors. **Generic-math 4/4** (abelian group): F# #6480 `Zero`/`(+)`/`(~-)`/`(-)` · C# #6481 IWSAM `IAdditiveIdentity`+`IAddition`/`ISubtraction`/`IUnaryNegation` · Rust #6482 `Add`/`Sub`/`Neg` ref-ops + `Default` + `Sum` · TS #6483 `AbelianGroup` record. |
| **Bus (git-native)** — ZetaId-keyed G-Set of envelopes, no-PR      | ✅  | ❌  | ❌  | ❌   | `tools/agent-bus/` (B-0954, #6283/#6327). The wire is JSON-on-git, so cross-lang = a thin read/write/merge per language (each needs ZetaId first).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| **Rx-Observable / Z-set delta-stream** — push-based reactive layer | ⚠️  | ⚠️  | ⚠️  | ❌   | The reactive layer **over** Z-set: an Rx query _is_ a Z-set delta-stream (B-0959 §3). Rx.NET / `FSharp.Control.Reactive` exist per-lang (`rx-expert` skill, B-0640); not yet a unified cross-lang primitive. **CALM** (Consistency As Logical Monotonicity) is the law that makes the monotone slice coordination-free.                                                                                                                                                                                                                                                                                                                                                                                                      |

## Candidate — BCL-like platform primitives (pull in slowly)

Some primitives every runtime already provides — **JSON**, **UTF-8**, and other
BCL/std-level facilities. We treat them as cross-registry primitives too, but
**pull them in slowly**: rely on one only once we've verified a _good, consistent
interface across all four_ we can depend on (per
[`bcl-interface-boundary-own-your-interfaces-hexagonal`](../.claude/rules/bcl-interface-boundary-own-your-interfaces-hexagonal.md)
— depend on BCL/std interfaces; wrap anything 3rd-party behind our own port; the
de-facto-standard exception, e.g. `serde`, only when both provenance-signed AND
widely-relied-on).

**"A good interface across all four" is a higher bar than "all four have it"**
(the maintainer 2026-06-01) — three requirements, all of which must hold before we rely on a
BCL-like primitive:

1. **Cross-compatible** — the four implementations interoperate on the wire (JSON
   written by TS parses identically in Rust; a UTF-8 string round-trips byte-for-byte
   across all four). Same bar as the algebra primitives' golden-vector consensus.
2. **One common surface + idioms** — our interface exposes the primitive's features
   _the same way_ in every language, so a dev moving across the four doesn't relearn
   JSON (or UTF-8, or …) four times. We **own the surface** (the port); we do not
   re-export four different vendor APIs (per the hexagonal rule above).
3. **Still idiomatic per language** — each binding feels native and uses language
   features (F# computation expressions, C# `System.Text.Json` source-gen, Rust
   traits, TS structural types). The common surface is a shared _shape_, not a
   lowest-common-denominator wrapper that fights every language.

The tension (common-surface **and** idiomatic) resolves the way the algebra primitives
already do: a shared interface + golden-vector cross-checks, with idiomatic
per-language adapters underneath. "Pull in slowly" = land a primitive in this tier
only once all three hold.

| Primitive | TS                     | F#                          | C#                          | Rust                                                                                                         | Python | Go | Q# | Note — what "a good interface across matrix runtimes" means here |
| --------- | ---------------------- | --------------------------- | --------------------------- | ------------------------------------------------------------------------------------------------------------ | ------ | -- | -- | ----------------------------------------------------------------- |
| **UTF-8** | native (`TextEncoder`) | `System.Text.Encoding.UTF8` | `System.Text.Encoding.UTF8` | `str`/`String` (native)                                                                                      | `str` (native) | `string`/`[]byte` (native) | `String` (native) | All languages have native UTF-8 support. Cross-language concerns focus on string comparison/ordering invariants across the runtimes. |
| **JSON**  | native `JSON`          | `System.Text.Json`          | `System.Text.Json`          | our `ZetaJsonParser` (zero-dep, `src/Core.Rust.Observe/src/json.rs`) + `serde_json` adapter behind a feature | native `json` | native `encoding/json` | N/A | Stabilize the parser port shape across all matrix configurations to ensure uniform handling of precision and key ordering. |

**Other BCL-like candidates — data / codec** (the maintainer 2026-06-01: "for sure yes", pull
in slowly): **base64**, **SHA-256** hashing, **big integers**, **regex**, and
**time/clock** — the last is a load-bearing **DST primitive** (deterministic simulation
needs a seedable/controllable clock, not wall-clock; F# is already well ahead here).
Each lands only after the four-way interface is verified — not on first use.

**Further-out — concurrency / runtime / IO primitives** (the maintainer 2026-06-01, naming them
for the map): **channels** + **pipelines** (`System.Threading.Channels` /
`System.IO.Pipelines`), **concurrent dictionary**, **work-stealing / `ActionBlock`-like**
dataflow, an **async runtime** (Tokio in Rust; the TPL/`Task` scheduler in .NET; the
event loop in JS), **TCP/UDP sockets**, and eventually a **server side** (ASP.NET-class
HTTP). These are a harder cross-language-surface problem than the data/codec primitives —
the concurrency models diverge sharply (Tokio vs .NET `Task` vs the JS event loop) — so
they come later and need extra care to find a common surface that stays idiomatic in each
runtime. Listed now so they're on the map; the same three-requirement bar applies.

## Category / numeric primitives — the cross-language number BCL

The base value types mapped across the four oracles, with the compatibility caveats that
bite when you cross languages (the maintainer 2026-06-01: "category primitives too like all the
ints floats decimal etc."). These are the "count on it" primitives the cross-language BCL
is built from. ✅ native / ⚠️ caveat / ❌ no native (needs a library).

| Primitive                         | C#                                 | F#                          | Rust                        | TS                                      | Python | Go | Q# | Cross-language caveat |
| --------------------------------- | ---------------------------------- | --------------------------- | --------------------------- | --------------------------------------- | ------ | -- | -- | --------------------- |
| **bool**                          | `bool`                             | `bool`                      | `bool`                      | `boolean`                               | `bool` | `bool` | `Bool` | Clean everywhere. |
| **bool? (plain Kleene 3-valued)** | `bool?` (built-in `&`/`\|` Kleene) | `bool option`               | `Option<bool>`              | `boolean \| null`                       | `Optional[bool]` | `*bool` \| custom | `Result[Bool, Unit]` | Maps cleanly to optionals across runtimes. |
| **int8/uint8 … int64/uint64**     | `sbyte/byte … long/ulong`          | `sbyte/byte … int64/uint64` | `i8/u8 … i64/u64`           | ⚠️ `number` is f64 — exact only to 2^53 | `int` (unbounded) | `int8/uint8 … int64/uint64` | `Int` (64-bit) | TS/Python have unbounded or f64 default representations; require bounds checking to enforce exact-integer ranges. |
| **int128/uint128**                | `Int128/UInt128`                   | `System.Int128`             | `i128/u128`                 | ❌ `bigint` only                        | `int` (unbounded) | ❌ `math/big` only | ❌ N/A | Supported via big integers or custom types in some runtimes. |
| **float32/float64**               | `float` / `double`                 | `float32` / `float`         | `f32` / `f64`               | ⚠️ `number` = f64 only                  | `float` = f64 only | `float32/float64` | `Double` (f64) | TS/Python default to f64 precision. |
| **decimal** (base-10, 128-bit)    | `decimal`                          | `decimal`                   | ❌ (crate `rust_decimal`)   | ❌ (lib `decimal.js`)                   | native `decimal.Decimal` | ❌ (lib `shopspring/decimal`) | ❌ N/A | Native to .NET and Python; requires external libraries in Rust, Go, and TS. |
| **bigint** (arbitrary precision)  | `BigInteger`                       | `bigint`                    | ❌ std (crate `num-bigint`) | `bigint` (native)                       | `int` (native) | `math/big` (native) | `BigInt` (native) | Native in C#, F#, TS, Python, Go, and Q#; requires crate in Rust. |

The pattern: the **.NET pair (C#/F#) is richest** (decimal + Int128 + BigInteger all
native); **Rust** has fixed-width ints/floats natively but no std decimal/bigint; **TS**
is the outlier (one numeric type, f64) — fixed-width, full 64-bit, and decimal all need
care. Documenting the mapping + the caveat tells a dev crossing languages exactly where
the floor is.

**Filling the ❌ gaps — dep behind our port, replaced over time** (the maintainer
2026-06-01): a missing primitive (Rust `bigint`/`decimal`, TS `decimal`) is brought up by
pulling a dep (`num-bigint`, `rust_decimal`, `decimal.js`) **behind our own hexagonal
interface** — never depending on the dep's interface directly (per
[`bcl-interface-boundary`](../.claude/rules/bcl-interface-boundary-own-your-interfaces-hexagonal.md)).
The dep is the bootstrap implementation **and** the differential-test oracle (our impl vs
the dep, the way `Core.Rust.Observe` keeps `serde_json` behind a feature); over time we
replace the dep with our own impl while keeping it for the differential test. So a ❌ cell
becomes "⚠️ via dep behind our port" → eventually "✅ ours" — the gap closes without ever
exposing four different vendor APIs to devs.

## DST / test primitives — F# is ahead; the cross-language target

The deterministic-simulation + test substrate (the maintainer 2026-06-01: "F# we are way ahead
here"). These make seeded, replayable simulation possible — the backbone of the
"compilers don't lie" verification + the DST discipline.

| Primitive                                                 | Where                                                        | Note                                                                                                                    |
| --------------------------------------------------------- | ------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------- |
| **IClock / SystemClock / FrozenClock**                    | F# `src/Core/Injection.fs`                                   | `UtcNow` + monotonic `Elapsed`; `FrozenClock` is the virtual-time test double                                           |
| **ChaosEnvironment** (seeded)                             | F# `src/Core/ChaosEnv.fs` (+ TLA+ `ChaosEnvDeterminism.tla`) | seeded chaos injection; same (seed, policy, schedule) replays deterministically; shrinks to the minimal triggering seed |
| **LawRunner**                                             | F# `src/Core/LawRunner.fs`                                   | property/law-test runner (trace from seed, run samples, surface `LawViolation`)                                         |
| **Injection DI seams** (`IBackingStore` / `IMetricsSink`) | F# `src/Core/Injection.fs`                                   | narrow interfaces so a prod path can be shadowed by a test double                                                       |
| **DeterministicEnv** (seeded env)                         | all 4 (ZetaId)                                               | the one DST primitive already 4-oracle — fixed-seed source for reproducible cross-verify                                |

F# leads here (`IClock` / `ChaosEnvironment` / `LawRunner` are F#-only today); the
cross-language target is a common seedable clock + chaos + property-runner surface so DST
works the same in all four. `DeterministicEnv` already shows the shape — 4-oracle.

## The algebra ladder (why three of the gaps are one build)

```
G-Set   ⊂   Bag / multiset   ⊂   Z-set
weights {0,1}    weights ℕ          weights ℤ (retraction-native)
```

Each rung is the next by widening the weight codomain. Built **once per language,
parameterized over the weight monoid**, one type yields all three rungs — so the
G-Set / Bag / Z-set gaps collapse into a single ladder build per language.

## How to use / maintain this file

- **Building on the base?** Use the Tier-1/2 table. Tier 1 (ZetaId) is byte-safe to
  depend on across machines + languages. Tier 2 is reimplementation-verified.
- **Landed a primitive in a new language?** Add/flip its cell, name the location,
  and state the consensus tier honestly (don't claim Tier 1 without a shared
  byte-diff harness — implement the fixture + `compare` first).
- **Promotion gate to Tier 1**: a shared golden-vector fixture + an N-way `compare`
  harness that fails non-zero on mismatch (the `tests/cross-verification/zeta-id/`
  pattern is the template).

_Last updated: 2026-06-01 — **IndexedZSet now 4/4 (Tier 1/2)**: the native Rust cell
(`src/Core.Rust.Algebra/src/indexed_zset.rs` + cross-verify replaying the shared
`golden-vectors.json`) joined F#/TS/C#, so the whole algebra ladder
(G-Set ⊂ Bag ⊂ Z-set ⊂ IndexedZSet) is 4/4 — including the bilinear `join`. — ZetaId + **G-Set now 4/4 (Tier 1/2)** (C# #6363 merged →
G-Set joined ZetaId/Observe/TriBoolean in the stable base; per-lang golden-vector replay,
no N-way byte-diff harness yet so Tier 1/2 not pure Tier 1); Bag (middle rung) TS reference
in-flight (#6364). Added wish-list categories the maintainer named: Discovery / transport
(Nostr, DHT, IPFS, Reticulum-over-IP/-mesh, 802.11ah HaLow — xref'd to existing backlog),
Observability (structured logging, OTel / `System.Diagnostics.Metrics` Meter model,
benchmarking — instruments as Bag/Z-set folds), Test framework, and Consensus (gossip /
Raft / Paxos — gated on CAS-composability, CP only at the federated git-merge boundary).
Prior: cross-language-BCL framing, numeric/category + numerics-tower + inference + nullable
wish items, DST/test section, BCL-like candidate tier (JSON, UTF-8 — pull in slowly)._

## Related work — cross-language interop prior art

(the maintainer 2026-06-01 asked for the prior-art map.) Every prior approach to
cross-language interop picks **one shared chokepoint**; the multi-oracle matrix deliberately
picks **none** — it keeps real native implementations and proves they **agree**
(byte-diff golden-vectors). Five strategies:

| Strategy                           | What's shared                             | Examples                                                                                                                                                                                                                   |
| ---------------------------------- | ----------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Shared VM / runtime**            | a runtime + object model                  | JVM (Kotlin/Scala/Clojure/JRuby/Jython), .NET CLR, **GraalVM/Truffle** (standardized polyglot interop messages)                                                                                                            |
| **Shared IR / backend**            | a compile _target_ (not a runtime model)  | **LLVM**, Cranelift — compile _to_, but no shared runtime object model _through_ it                                                                                                                                        |
| **Shared ABI**                     | a calling convention + interface contract | C ABI (JNI / P-Invoke / ctypes / SWIG / UniFFI), **WASM Component Model + WIT** ("COM done right": sandboxed, language-neutral — runtime/tooling versions tracked at the linked sources, kept out of this evergreen table) |
| **Shared wire / data format**      | bytes + IDL-codegen                       | protobuf/gRPC, Cap'n Proto, FlatBuffers, Arrow+Flight, Thrift, Avro, MessagePack (= our serializer roster)                                                                                                                 |
| **Dynamic-object / runtime-shape** | a runtime "what shape are you?" protocol  | COM/IUnknown/DCOM, CORBA, **.NET DLR** (`IDynamicMetaObjectProvider` / `dynamic` / `ExpandoObject` — IronPython/IronRuby ride it; the lineage of our dynamic-object line), JVM `invokedynamic`, JS `Proxy`                 |

**Our strategy is the sixth — conformance-by-agreement.** Not a shared substrate: N native
impls cross-checked, like the WASM spec test suite / Unicode test vectors / SQL conformance
suites. Cost N× implementation; buys zero shared-runtime lock-in, full native idiom, and the
BFT property (_the compilers don't lie_). The shared-substrate efforts re-enter as **adapters
behind our ports**, not replacements — a WASM-component adapter, an Arrow-Flight adapter.

**Why conformance-by-agreement is the asset — "languages turned inside out"** (Aaron 2026-06-01):
just as Kafka turned the database inside out (the log is primary, the DB a derived view), this
turns _languages_ inside out — the **binary-compatible primitive layer (the byte-locked golden
vectors + the shared interface) is the asset; the language it's written in is the swappable
rendering.** A primitive isn't "the F# Z-set," it's "the Z-set, of which the active language environments are byte-equal renderings." Positioning: **"C++ Boost for any language"** — Boost's depth/elegance,
DB-heavy + observability-heavy, but byte-locked across N languages instead of one. Full thesis +
Boost-as-prior-art: [`docs/research/2026-06-01-languages-turned-inside-out-binary-compatible-bcl-is-the-asset-cpp-boost-for-any-language-aaron-otto.md`](research/2026-06-01-languages-turned-inside-out-binary-compatible-bcl-is-the-asset-cpp-boost-for-any-language-aaron-otto.md).

**Closest conceptual neighbors** to our dynamic-object / polymorphic-shape (v8 hidden-shape)
line: GraalVM Truffle interop ("ask a foreign value what members it supports" = `QueryInterface`
for N languages), WASM Component Model / WIT (typed, runtime-checkable interface description),
and the .NET DLR (the literal `DynamicObject` lineage).

**The one gap none of them fill:** all assume _structured_ interfaces/data — none treats
**natural language as a first-class serialization format** behind the same polymorphic-shape
primitive (the nearest, LLM tool-schemas, is English→structured, not English-as-an-adapter).
That's the genuinely novel part of the dynamic-object-over-English + ZetaParse-probabilistic-grammar
direction.

Sources: [WASM Component Model](https://component-model.bytecodealliance.org/) ·
[WASI/CM 2026 status](https://eunomia.dev/blog/2025/02/16/wasi-and-the-webassembly-component-model-current-status/) ·
[GraalVM Polyglot](https://www.graalvm.org/latest/reference-manual/polyglot-programming/).
