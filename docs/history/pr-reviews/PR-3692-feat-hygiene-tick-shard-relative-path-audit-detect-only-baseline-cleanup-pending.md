# PR #3692 -- feat(hygiene): tick-shard relative-path audit (detect-only; baseline cleanup pending)

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 3692 |
| Title | feat(hygiene): tick-shard relative-path audit (detect-only; baseline cleanup pending) |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-05-16T02:47:56Z |
| Merged at | 2026-05-16T03:08:39Z |
| Merge commit SHA | `4632338c172b954106aa6377ac0e7782fe5c8993` |
| Branch | `feat/audit-tick-shard-relative-paths-otto-cli-2026-05-16` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/3692 |
| Changed files | 1 |
| Additions / deletions | +310 / -0 |

## Description

## What

New audit `tools/hygiene/audit-tick-shard-relative-paths.ts` walks `docs/hygiene-history/ticks/**/*.md`, extracts every relative markdown link target, resolves from the shard's directory, and reports missing-or-escaping targets.

## Why

Empirical evidence from this session:
- [PR #3676](https://github.com/Lucent-Financial-Group/Zeta/pull/3676) shipped with 5-`..` paths that resolved to `docs/` (not repo root); Copilot caught it via review thread
- [PR #3679](https://github.com/Lucent-Financial-Group/Zeta/pull/3679) shipped with the same bug, raced into main before review; fixed post-merge by [PR #3680](https://github.com/Lucent-Financial-Group/Zeta/pull/3680)

The bug class: tick shards live 5 directories below `docs/`, so the count-the-`..` pattern is error-prone. Mechanizing the catch via this audit prevents the class from recurring.

## Baseline run

Run on origin/main at 2026-05-16T02:48Z:
- 833 tick shards scanned
- 17 broken relative-path links across multiple historical shards
- Real bug classes detected: wrong-depth `..` (B-0442 link in 1436Z), malformed link syntax (`docs/api(v2`), missing-file refs

## Followup plan (same pattern as §33 migration xrefs)

1. **This PR** — ship the audit script in detect-only mode (no CI gate yet)
2. **Baseline cleanup PR** — address the 17 pre-existing findings (where mutable; tick-shard immutability discipline may require glossary corrections for some)
3. **CI gate PR** — wire to `.github/workflows/gate.yml` as a non-required check in `--enforce` mode after baseline is 0

## Verification

- `bun --bun tsc --noEmit -p tsconfig.json` exit 0
- `bun tools/hygiene/audit-tick-shard-relative-paths.ts` runs cleanly on all 833 shards

Co-Authored-By: Claude <noreply@anthropic.com>

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | true |
| Total threads | 6 |
| Resolved threads | 6 |
| Unresolved threads | 0 |
| Total review comments | 6 |
| Total fix commits (touching thread paths) | 4 |

## Review threads

### Thread 1 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6ChGu_`
- **Path**: `tools/hygiene/audit-tick-shard-relative-paths.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-16T02:50:00Z on `tools/hygiene/audit-tick-shard-relative-paths.ts`:60 (association: NONE)

  <pre>
  The `spawnSync("git", ...)` call needs an `// eslint-disable-next-line sonarjs/no-os-command-from-path` comment to pass the repo's ESLint+sonarjs configuration. Every sibling script in the repo that shells out to `git`/`gh` uses this disable comment — including the explicit template cited in the file's own comment, `tools/hygiene/check-tick-history-shard-schema.ts:23`. Without it, the lint gate will fail.
  </pre>

### Thread 2 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6ChGvB`
- **Path**: `tools/hygiene/audit-tick-shard-relative-paths.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-16T02:50:01Z on `tools/hygiene/audit-tick-shard-relative-paths.ts`:255 (association: NONE)

  <pre>
  This script invokes `process.exit(main(...))` unconditionally at the top level, and `main` is not exported. The repo convention (see the sibling/template `tools/hygiene/check-tick-history-shard-schema.ts:167,201-203`, and `tools/hygiene/audit-section-33-migration-xrefs.ts:291`) is to `export function main(...)` and guard the exit with `if (import.meta.main) { process.exit(main(process.argv.slice(2))); }`. Without the guard, importing this module for testing or composition triggers `process.exit` as a side effect.
  </pre>

### Thread 3 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6ChG33`
- **Path**: `tools/hygiene/audit-tick-shard-relative-paths.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-16T02:50:36Z on `tools/hygiene/audit-tick-shard-relative-paths.ts`:129 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Exclude all URI-scheme links from relative-path checks**

  `isRelativeTarget` only exempts `http(s)` and `mailto`, so valid absolute links with other schemes (for example `ftp:`, `file:`, `tel:`, `data:`) are misclassified as relative paths and reported as broken local files. In detect-only mode this adds noise, and once `--enforce` is wired into CI it can fail the gate on perfectly valid Markdown links even though the target is not meant to exist in-repo.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 4 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6ChG35`
- **Path**: `tools/hygiene/audit-tick-shard-relative-paths.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-16T02:50:36Z on `tools/hygiene/audit-tick-shard-relative-paths.ts`:171 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Validate `--files` inputs before reading shard contents**

  When a path passed via `--files` does not exist (or is unreadable), `readFileSync` throws inside `extractLinks` and the process aborts with an uncaught exception, bypassing the script’s documented argument/error handling contract. This makes the audit brittle in automation that supplies dynamic file lists, because one stale path can crash the whole run instead of returning a controlled error code/message.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 5 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6ChJvO`
- **Path**: `tools/hygiene/audit-tick-shard-relative-paths.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-16T03:01:52Z on `tools/hygiene/audit-tick-shard-relative-paths.ts`:244 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Validate `--files` paths are readable files**

  The `--files` preflight only checks `existsSync`, so directories (or unreadable files) pass validation and then `readFileSync` throws inside `extractLinks`, terminating with an uncaught exception instead of returning the script’s structured argument error code. This still breaks automation that supplies dynamic file lists, because one non-file input can crash the entire audit run rather than yielding a controlled `64` path-validation failure.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 6 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6ChJvQ`
- **Path**: `tools/hygiene/audit-tick-shard-relative-paths.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-16T03:01:52Z on `tools/hygiene/audit-tick-shard-relative-paths.ts`:210 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Normalize path separator in repo-boundary check**

  The repo-boundary guard hardcodes `"/"` in `ROOT + "/"`, but `resolve()` returns backslash-separated paths on Windows. In that environment, valid in-repo targets like `C:\repo\docs\...` fail the prefix test against `C:\repo/`, so links are incorrectly reported as `escapes-repo`; with `--enforce` this can fail CI/local checks despite correct links.

  Useful? React with 👍 / 👎.
  </pre>

## Fix commits (touching thread paths)

### `13e00526673c4446ff976651d3873a6adfb3c5f5` -- 2026-05-16T02:47:32Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/hygiene/audit-tick-shard-relative-paths.ts`

**Message:**

```
feat(hygiene): tick-shard relative-path audit (detect-only; baseline …

…cleanup pending)

Bug class: tick shards live 5 directories below docs/, so the count-the-..
pattern is error-prone. Empirical evidence this session: PR #3676 + PR #3679
both shipped with 5-`..` paths that resolved to docs/ instead of repo root;
Copilot caught both via review threads, but the broken links landed on main
briefly (PR #3680 fixed post-merge).

This audit walks docs/hygiene-history/ticks/**/*.md, extracts every relative
markdown link target (skipping URLs/anchors/code-blocks/images), resolves
from the shard's directory, and reports missing-or-escaping targets.

Empirical baseline (run on origin/main at 2026-05-16T02:48Z):
  - 833 tick shards scanned
  - 17 broken relative-path links across multiple historical shards
  - Real bug classes detected: wrong-depth `..` (B-0442 link in 1436Z),
    malformed link syntax (`docs/api(v2`), missing-file refs

Detect-only initially. CI enforce wires in after baseline cleanup (same
pattern as §33 migration xrefs: PR #3513 → #3529 → #3548 → #3552 → enforce).

`bun --bun tsc --noEmit -p tsconfig.json` exit 0.

Co-Authored-By: Claude <noreply@anthropic.com>
```

### `e945016f6d022a70d3d290b47ad4341761b188e4` -- 2026-05-16T02:52:31Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/hygiene/audit-tick-shard-relative-paths.ts`

**Message:**

```
fix(audit): skip placeholder targets (..., parens, identifier-only)

First baseline showed 17 findings; ~7 were false positives where shard prose
contained inline `[label](path-shape)` constructs as pattern illustrations:
- `path` / `otto-kenji-...` / `.claude/...` / `docs/...` — placeholder names
- `docs/api(v2` — fragmentary malformed syntax
- `docs/research/...amara-...md` — ellipsis-marked example

Add `isPlaceholderTarget` filter:
- contains `...` → placeholder
- contains `(` or `)` → malformed/fragment
- no `/` AND no `.` → pure identifier (not a path)

Re-run: 17 → 10 findings. The 10 remaining are real broken links
(wrong-depth `..` in `1436Z.md`, `0329Z.md`, `0852Z.md`; one borderline
`docs/foo.md` example). Worth a separate baseline-cleanup PR.

`bun --bun tsc --noEmit -p tsconfig.json` exit 0.

Co-Authored-By: Claude <noreply@anthropic.com>
```

### `d299c7da0ff9813691f89b73412d2056e6d21f3a` -- 2026-05-16T02:59:09Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/hygiene/audit-tick-shard-relative-paths.ts`

**Message:**

```
fix(audit): 4 Copilot P1/P2 — sonarjs disable, main export, URI schem…

…e, --files validation

PR #3692 review threads:

P1 (lint failure risk):
1. spawnSync("git", ...) at repoRoot() needs the standard repo-convention
   `// eslint-disable-next-line sonarjs/no-os-command-from-path` comment.
   Every sibling tool (check-tick-history-shard-schema.ts:23, etc.) uses it.
2. Top-level `process.exit(main(...))` blocks safe module-import for tests
   or composition. Switch to `export function main` + guarded
   `if (import.meta.main) { process.exit(main(...)); }` per the sibling
   audit-section-33-migration-xrefs.ts convention.

P2 (precision / brittleness):
3. isRelativeTarget only exempts http(s) + mailto. Replace with a generic
   `<scheme>:` regex (`/^[A-Za-z][A-Za-z0-9+.-]*:/`) so ftp:, file:, tel:,
   data:, etc. are properly classified as absolute.
4. --files inputs aren't validated; readFileSync throws on missing path.
   Add an explicit existence check at the args boundary; emit
   `input not found: <path>` and return exit 64.

Local verify:
- Baseline still 10 findings (no regression)
- `--files /tmp/does-not-exist` → exit 64 with structured message
- `bun --bun tsc --noEmit -p tsconfig.json` exit 0

Co-Authored-By: Claude <noreply@anthropic.com>
```

### `4e80655b899255230b46a4cfc4444e5958fbb60b` -- 2026-05-16T03:05:30Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/hygiene/audit-tick-shard-relative-paths.ts`

**Message:**

```
fix(audit): 2 Copilot fixups — directory inputs + Windows path separator

PR #3692 second-pass review threads:

P1 (line 244): --files validation only checked existsSync; a directory or
unreadable file passed the preflight, then `readFileSync` threw EISDIR/EACCES
inside extractLinks, bypassing the structured exit-64 contract. Tighten to
also require `statSync(abs).isFile()` and wrap stat in try/catch for
permission failures. Empirical verify:
- --files docs/hygiene-history/ → "input not a regular file" + exit 64
- --files /tmp/does-not-exist → "input not found" + exit 64

P2 (line 210): Repo-boundary check hardcoded "/" in `ROOT + "/"`. On Windows
`resolve()` returns paths with `\\` separators, so valid in-repo targets like
`C:\\repo\\docs\\...` would fail the `C:\\repo/` prefix test and be flagged
as `escapes-repo` — false positive that would break --enforce mode on
Windows CI. Replace with platform-correct `PATH_SEP` imported as
`sep as PATH_SEP` from node:path.

Local verify:
- Baseline still 10 findings (no regression)
- `bun --bun tsc --noEmit -p tsconfig.json` exit 0

Co-Authored-By: Claude <noreply@anthropic.com>
```
