# PR #6440 -- feat(081KT07NV0008QG0R003BE6MJ2): C# Bonsai oracle (#3 of 4) — serialize/parse + accumulate, Result over throw

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 6440 |
| Title | feat(081KT07NV0008QG0R003BE6MJ2): C# Bonsai oracle (#3 of 4) — serialize/parse + accumulate, Result over throw |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-06-01T17:21:40Z |
| Merged at | 2026-06-01T17:24:46Z |
| Merge commit SHA | `1069d3019ae77f1092048739c019f2de9a504437` |
| Branch | `otto-cli/b0976-csharp-bonsai-oracle-2026-06-01` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/6440 |
| Changed files | 13 |
| Additions / deletions | +1210 / -0 |

## Description

The **C# corner** of the four-oracle compiler-BFT for the Bonsai-subset expression-tree serializer (081KT07NV0008QG0R003BE6MJ2 slice 1). Replays the shared `src/Core.TypeScript/bonsai/golden-vectors.json` **byte-for-byte** (`serialize(parse(canonical)) == canonical`) — the cross-language byte lock with the TS reference (#6433) + F# (#6429) oracles. "The compilers don't lie."

## Shape (matches TS/F# error-for-error)
- **Result over throw**: owns a minimal `Result<T, TError>` port (C# has no BCL `Result` — per the BCL-interface-boundary rule: depend on BCL interfaces, else own your own). `Serialize`/`Parse` return `Result<_, BonsaiFeedback>`; an internal typed signal (`BonsaiFail`) is adapted to `Result` at the two boundaries (contract-vs-mechanism split).
- **Serialize** hand-rolls canonical compact bytes (the BCL `JsonSerializer` escapes more chars than JS `JSON.stringify`, so byte-exactness needs the hand-roll); surrogate-aware string escaping matched to `JSON.stringify`. **Parse** leans on `System.Text.Json` (BCL) + the canonical-only guard.
- `BonsaiFeedback` / `Expr` / `ConstValue` as sealed-record hierarchies (variant-for-variant parity with the F# DUs); `BinOp` enum; ergonomic `Build.*` constructors.
- **accumulate-mode**: `ParseAll` collects **every** per-node decline keyed by JSON-path; `ToProblemDetails` groups into the RFC-9457 `ProblemDetails` errors-map. Ferry of TS #6436.

## Tests — 23, all pass; 0 warnings
Byte-exact golden fixed-point (theory over all golden cases) + an independently-constructed tree → same canonical + rejection-by-variant (UnsupportedVersion / MalformedJson / UnknownKind / UnknownConstTag / UnknownOp / NonSafeInt / NonCanonical / TooDeep) + accumulate (3 independent declines with their paths + `ProblemDetails` keys + single malformed / single non-canonical). Analyzer-clean under Meziantou + .NET analyzers + `TreatWarningsAsErrors`.

## Oracle family
TS + F# + C# done. **Rust is the last corner** (next PR; production zero-dep → hand-rolled minimal JSON reader for the tiny canonical grammar, `std::result::Result` native).

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 1 |
| Resolved threads | 0 |
| Unresolved threads | 1 |
| Total review comments | 1 |
| Total fix commits (touching thread paths) | 1 |

## Review threads

### Thread 1 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM6GMmvZ`
- **Path**: `src/Core.CSharp.Bonsai/BonsaiCodec.cs`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-06-01T17:24:51Z on `src/Core.CSharp.Bonsai/BonsaiCodec.cs`:45 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Raise parser depth ceiling for call-arg chains**

  For a syntactically valid tree nested through `call.args`, each expression level adds both an object and an array on the deepest JSON path, so an input at `MaxDepth + 1` reaches roughly `2 * (MaxDepth + 1) + wrapper` JSON nesting before `ParseNode` can run its semantic depth check. With the reader capped at exactly `MaxDepth * 2`, `JsonDocument.Parse` rejects these over-deep call chains as `MalformedJson` instead of the shared `TooDeep` feedback that the serializer and depth contract expect; lambda/binary-only tests do not cover this shape.

  Useful? React with 👍 / 👎.
  </pre>

## Fix commits (touching thread paths)

### `5f0bf27df54160e9471c6d8a3bc114daa4a08231` -- 2026-06-01T17:21:27Z -- ``

**Touched files (intersect with thread paths):**

- `src/Core.CSharp.Bonsai/BonsaiCodec.cs`

**Message:**

```
feat(081KT07NV0008QG0R003BE6MJ2): C# Bonsai oracle (#3 of 4) — serialize/parse + accumula…

…te, Result over throw

The C# corner of the four-oracle compiler-BFT for the Bonsai-subset expression
serializer (081KT07NV0008QG0R003BE6MJ2 slice 1). Replays the shared golden-vectors.json byte-for-byte
(serialize(parse(canonical)) == canonical) — the cross-language byte lock with the
TS reference (#6433) + F# (#6429) oracles.

- Result over throw: own a minimal Result<T,TError> port (C# has no BCL Result;
  per the BCL-interface-boundary rule). serialize/parse return Result<_, BonsaiFeedback>;
  internal typed signal (BonsaiFail) adapted to Result at the two boundaries
  (contract-vs-mechanism). serialize hand-rolls canonical bytes (JsonSerializer escapes
  more than JS JSON.stringify); parse leans on System.Text.Json (BCL) + canonical-only guard.
- BonsaiFeedback / Expr / ConstValue as sealed-record hierarchies (variant-for-variant
  parity with the F# DUs); BinOp enum; ergonomic Build.* constructors.
- accumulate-mode: parseAll collects EVERY per-node decline keyed by JSON-path;
  toProblemDetails groups into the RFC-9457 ProblemDetails errors-map. Ferry of TS #6436.
- 23 tests: byte-exact golden fixed-point (theory over all cases) + rejection-by-variant
  + accumulate (3 independent declines with paths + ProblemDetails keys). 0 warnings
  (analyzer-clean: Meziantou + .NET analyzers + TreatWarningsAsErrors).

081KT07NV0008QG0R003BE6MJ2 oracle-family: TS + F# + C# done; Rust is the last corner.

Co-Authored-By: Claude <noreply@anthropic.com>
```
