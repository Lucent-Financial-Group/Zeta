# PR #6446 -- feat(081KT07NV0008QG0R003BE6MJ2): resume engine — TS reference (restore-not-replay CEK saga kernel)

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 6446 |
| Title | feat(081KT07NV0008QG0R003BE6MJ2): resume engine — TS reference (restore-not-replay CEK saga kernel) |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-06-01T18:01:28Z |
| Merged at | 2026-06-01T18:27:29Z |
| Merge commit SHA | `76ce54a89c6fa76714d9834002cc5196947d1f82` |
| Branch | `otto-cli/b0976-resume-engine-ts-reference-2026-06-01` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/6446 |
| Changed files | 3 |
| Additions / deletions | +694 / -0 |

## Description

Starts the **resume-engine slice** — the self-evolving-saga kernel the serialized Bonsai expression-tree feeds (the `bonsai.ts` serializer is the *shape*; this is the *evaluator* that runs it durably). TS reference oracle (#1 of TS/F#/C#/Rust); the proven Bonsai pattern — TS authors the golden saga-traces, F#/C#/Rust ferry.

## Model — restore-not-replay (the row's distinguishing property)
A small-step **CEK machine** over the Bonsai-subset `Expr`. **`call` nodes are activities** (the suspension points). Pure parts (`const`/`param`/`binary`/`cond`) evaluate inline; at an activity the machine **suspends**, handing back a fully serializable **`SagaState`** = the remaining continuation (the `kont` stack) + the pending activity. `resume(state, result)` **restores** the continuation and feeds the result back as the call's value — it does **not** replay from the top, so prior activities are never re-invoked (vs Temporal's replay-with-memoization). Each `kont` frame **is the closure** — it captures exactly the env + sub-expr it still needs (the slice's "serialize closure + expr-tree").

## Shape (matches the Bonsai oracle conventions)

- **Result over throw**: `start`/`resume`/`serializeState`/`parseState` return `Result<_, ResumeFeedback>` (`Unbound` / `TypeMismatch` / `UnsupportedNode` / `MalformedState`); internal typed signal adapted at the boundary.
- **State round-trips canonically** — persist at a suspension, restore, resume (the durability property; sets up cross-machine resume once the other oracles ferry).
- Slice-1 scope: `const`/`param`/`binary`/`cond`/`call`. `lambda` application deferred to slice-2 (declines `UnsupportedNode`).

## Tests — 11 (68 bonsai-dir total), tsc clean
`resume-golden.json` has 5 saga-traces (sequential activities · cond-branch-on-activity · activity-with-computed-args · pure-no-suspension · param-binding + activity) — the cross-language behavioral lock (same suspension sequence + final value). Each trace **persists + restores at every suspension** (proves restore-not-replay) + feedback-variant + state round-trip.

Fills the `PRIMITIVE-REGISTRY` **"serializable deferred execution = self-evolving sagas"** line (Event/reactive) — the TS cell. F#/C#/Rust ferry next.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | true |
| Total threads | 6 |
| Resolved threads | 6 |
| Unresolved threads | 0 |
| Total review comments | 6 |
| Total fix commits (touching thread paths) | 5 |

## Review threads

### Thread 1 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GNLsm`
- **Path**: `src/Core.TypeScript/bonsai/resume.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-01T18:03:11Z on `src/Core.TypeScript/bonsai/resume.ts`:328 (association: NONE)

  <pre>
  `o.op` is cast to `BinOp` without runtime validation in both the `evalRight` and `applyOp` branches. If a persisted state was tampered with or produced by a future version using an unknown op (e.g., `"xor"`), parsing will succeed and the value will silently flow into `applyBinOp`, whose `switch` has no `default` and will return `undefined` (typed as `ConstValue`), corrupting downstream evaluation rather than declining `MalformedState`. Consider validating `o.op` against the known `BinOp` tags here (and/or adding a `default: bad(...)` arm in `applyBinOp`).
  </pre>

### Thread 2 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GNL8n`
- **Path**: `src/Core.TypeScript/bonsai/resume.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-06-01T18:03:27Z on `src/Core.TypeScript/bonsai/resume.ts`:328 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Validate restored binary operators**

  When a persisted saga state is corrupted or produced by a mismatched peer with an unknown `op`, `parseState` still returns `Ok` because these branches cast `o.op` directly to `BinOp`. Resuming an `applyOp` frame with such a value falls through `applyBinOp` and can complete with `undefined` instead of declining `MalformedState`, so malformed durable state can silently corrupt the restored computation rather than being rejected at the parse boundary.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 3 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GNRT9`
- **Path**: `src/Core.TypeScript/bonsai/resume.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-06-01T18:08:51Z on `src/Core.TypeScript/bonsai/resume.ts`:302 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Reject unsafe integers in restored saga state**

  When a persisted state from another oracle or a tampered file contains an int outside JavaScript's safe range, `JSON.parse` rounds it but this check still accepts it because the rounded value is an integer. That lets `parseState` resume with a corrupted `ConstValue` instead of returning `MalformedState`, even though the Bonsai `ConstValue` wire contract enforces JS-safe integers. Please validate restored int values with `Number.isSafeInteger` here (and keep serialization aligned) so cross-language durable state cannot silently change numeric results.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 4 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GNWBa`
- **Path**: `src/Core.TypeScript/bonsai/resume.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-06-01T18:13:13Z on `src/Core.TypeScript/bonsai/resume.ts`:140 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Check own bindings before returning params**

  When a valid program references an unbound parameter whose name exists on `Object.prototype` (for example `param("toString")` or `param("constructor")`) with the default `{}` bindings, this lookup returns the inherited property instead of `undefined`, so `start` can produce a non-`ConstValue` result or a later `TypeMismatch` rather than the expected `Unbound`. Use an own-property check (and ideally null-prototype restored environments) before accepting a binding value.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 5 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GNWBe`
- **Path**: `src/Core.TypeScript/bonsai/resume.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-06-01T18:13:13Z on `src/Core.TypeScript/bonsai/resume.ts`:94 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Validate arithmetic results stay wire-safe**

  With safe input operands near the JS integer limit, `add` can produce a `ConstValue` outside the shared safe-int wire domain (for example `Number.MAX_SAFE_INTEGER + 1`), and that value can then become a final result or an activity argument that `serializeState` emits even though `parseState` rejects it. Since the Bonsai contract only permits JS-safe integers, arithmetic results should be checked before returning an `int` value.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 6 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GNZ2m`
- **Path**: `src/Core.TypeScript/bonsai/resume.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-06-01T18:17:31Z on `src/Core.TypeScript/bonsai/resume.ts`:339 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Preserve restored `__proto__` bindings**

  When a persisted frame environment contains an explicit parameter binding named `__proto__`, this assignment on a normal `{}` invokes the legacy prototype setter instead of creating an own property. Bonsai parameter names are arbitrary strings, so a saga that suspended before later evaluating `param("__proto__")` can serialize with that binding but resume from `parseState` with the binding missing and return `Unbound`, diverging from the original state. The earlier own-property lookup fix does not cover this restored-state path; the fresh evidence is that `readEnv` still materializes environments with `{}` plus indexed assignment rather than a null-prototype object or `defineProperty`.

  Useful? React with 👍 / 👎.
  </pre>

## Fix commits (touching thread paths)

### `d3198f771fc1406db89b298a01303cd2f161f5de` -- 2026-06-01T18:01:16Z -- ``

**Touched files (intersect with thread paths):**

- `src/Core.TypeScript/bonsai/resume.ts`

**Message:**

```
feat(081KT07NV0008QG0R003BE6MJ2): resume engine — TS reference (restore-not-replay CEK sa…

…ga kernel)

Starts the resume-engine slice (the self-evolving-saga kernel the serialized Bonsai
expression-tree feeds). TS reference oracle (#1 of TS/F#/C#/Rust); the proven pattern —
TS authors the golden saga-traces, F#/C#/Rust ferry.

- Model: a small-step CEK machine over the Bonsai-subset Expr. `call` nodes are activities
  (the suspension points). Pure parts (const/param/binary/cond) evaluate inline; at an
  activity the machine SUSPENDS, handing back a fully serializable SagaState = the remaining
  continuation (kont stack) + the pending activity. `resume(state, result)` RESTORES the
  continuation and feeds the result back as the call's value — it does NOT replay from the
  top, so prior activities are never re-invoked (the row's restore-not-replay property, vs
  Temporal's replay-with-memoization). The kont frame IS the closure (captures exactly the
  env + sub-expr it still needs = "serialize closure + expr-tree").
- Result over throw: start/resume/serializeState/parseState return Result<_, ResumeFeedback>
  (Unbound / TypeMismatch / UnsupportedNode / MalformedState); internal typed signal adapted
  at the boundary. SagaState round-trips canonically (persist a suspension, restore, resume).
- Slice-1 scope: const/param/binary/cond/call. lambda application deferred (slice-2) — a
  lambda in eval position declines UnsupportedNode.
- golden saga-traces (resume-golden.json): 5 traces (sequential activities · cond-branch on
  activity · activity-with-computed-args · pure-no-suspension · param-binding + activity) —
  the cross-language behavioral lock (same suspension sequence + final value). 11 tests; each
  trace persists+restores at EVERY suspension (proves restore-not-replay) + feedback-variant
  + state round-trip. 68 bonsai-dir tests green, tsc clean.

Fills the PRIMITIVE-REGISTRY 'serializable deferred execution = self-evolving sagas' line
(Event/reactive) — TS cell.

Co-Authored-By: Claude <noreply@anthropic.com>
```

### `80831e9547ecd93d7f3cd9ded30f7181f093db8f` -- 2026-06-01T18:06:27Z -- ``

**Touched files (intersect with thread paths):**

- `src/Core.TypeScript/bonsai/resume.ts`

**Message:**

```
fix(081KT07NV0008QG0R003BE6MJ2): validate restored binary op in parseState (review)

Copilot + Codex P2 on #6446: parseState cast o.op to BinOp without validation in the
evalRight/applyOp frame readers, so a tampered/future-version persisted state with an
unknown op (e.g. 'xor') parsed Ok and flowed into applyBinOp (exhaustive switch, no
default) → silent undefined instead of declining. Added readBinOp() validating against the
known BinOp tags at the parse boundary → unknown op now declines MalformedState. +1 test
(tamper a persisted 'add' to 'xor' → MalformedState). 12 tests, tsc clean.

Co-Authored-By: Claude <noreply@anthropic.com>
```

### `06fe1f6ca45e40e07946473bebecc7be937c41ec` -- 2026-06-01T18:10:21Z -- ``

**Touched files (intersect with thread paths):**

- `src/Core.TypeScript/bonsai/resume.ts`

**Message:**

```
fix(081KT07NV0008QG0R003BE6MJ2): reject unsafe integers in restored saga state (review)

Codex P2 on #6446: readConstValue checked Number.isInteger but not isSafeInteger, so a
restored/tampered/cross-oracle int outside 2^53-1 (which JSON.parse rounds to an integer)
slipped through — violating the Bonsai ConstValue safe-int wire contract and resuming a
corrupted numeric value instead of declining. Now uses Number.isSafeInteger → unsafe int
declines MalformedState. +1 test (tamper a persisted arg to 2^53+1 → MalformedState).
13 tests, tsc clean.

Co-Authored-By: Claude <noreply@anthropic.com>
```

### `5ff25611189a33033a6ee4f3ea253fdcec643d92` -- 2026-06-01T18:15:18Z -- ``

**Touched files (intersect with thread paths):**

- `src/Core.TypeScript/bonsai/resume.ts`

**Message:**

```
fix(081KT07NV0008QG0R003BE6MJ2): own-property param lookup + safe-int arithmetic results …

…(review)

Two Codex P2 on #6446:
- param lookup used bracket access, so param('toString'/'constructor'/etc.) resolved an
  inherited Object.prototype member instead of declining Unbound. Now uses an own-property
  check (Object.prototype.hasOwnProperty.call) — an unbound param always declines Unbound.
- add/sub/mul could produce a result outside the JS-safe-integer range (e.g.
  MAX_SAFE_INTEGER+1), which would become a final value / activity arg that serializeState
  emits but parseState rejects (safe-int wire-contract asymmetry). Added intResult() guard
  → arithmetic overflow declines NonSafeInt (new ResumeFeedback variant); emitConstValue
  also guards int symmetrically (an unsafe int injected via a resume() result declines
  rather than emitting un-parseable bytes).
+2 tests (prototype-name params → Unbound; MAX_SAFE_INTEGER+1 → NonSafeInt). 15 tests, tsc clean.

Co-Authored-By: Claude <noreply@anthropic.com>
```

### `29dc9d82f682dd4bcd3bfb62287f2d55a71e3473` -- 2026-06-01T18:24:05Z -- ``

**Touched files (intersect with thread paths):**

- `src/Core.TypeScript/bonsai/resume.ts`

**Message:**

```
fix(081KT07NV0008QG0R003BE6MJ2): null-prototype restored env (preserve __proto__ binding)…

… (review)

Codex P2 on #6446: readEnv built the env on a plain {}, so a restored binding named
'__proto__' (a legal Bonsai param name) invoked the legacy prototype setter instead of
creating an own property — a saga capturing param('__proto__') would serialize fine but
resume from parseState with the binding missing → Unbound, diverging from the original
state. (The earlier own-property *lookup* fix didn't cover the env-*construction* path.)
readEnv now builds with Object.create(null) so every binding becomes an own property.
+1 test (__proto__-bound saga round-trips persist/restore → resolves to 15, not Unbound).
16 tests, tsc clean. (The F# ferry is immune — Map has no prototype hole.)

Co-Authored-By: Claude <noreply@anthropic.com>
```
