# PR #6201 -- [codex] feat(openspec): map artifact-backed specs in inventory

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 6201 |
| Title | [codex] feat(openspec): map artifact-backed specs in inventory |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-05-31T08:49:56Z |
| Merged at | 2026-05-31T09:06:29Z |
| Merge commit SHA | `615a950a23cf8463a59c16160af5baab12415d58` |
| Branch | `claim/codex-loop-b0171-openspec-artifact-map-20260531` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/6201 |
| Changed files | 2 |
| Additions / deletions | +185 / -63 |

## Description

## Summary

- Add a separate OpenSpec capability-to-artifact map for non-src/Core evidence.
- Map z-set-algebra to its existing F# and C# ZSet verification tests without inflating module coverage.
- Report mapped and missing artifacts separately from source-module coverage, and keep artifact-backed specs out of unmappedSpecs.

## Verification

- `bun test tools/openspec/inventory.test.ts`
- `bun tools/openspec/inventory.ts` shows z-set-algebra absent from unmappedSpecs, four mapped artifacts, and no missing artifacts.
- `bunx prettier --check tools/openspec/inventory.ts tools/openspec/inventory.test.ts`
- `bun run typecheck` blocked because `tsc` is not installed on PATH in this worktree; transient `bunx tsc --noEmit` also lacks the `bun` type library.

Agency-Signature-Version: 1
Agent: Vera
Agent-Runtime: OpenAI Codex desktop heartbeat loop
Agent-Model: GPT-5
Credential-Identity: aaron-codex-desktop
Credential-Mode: shared
Human-Review: none
Human-Review-Evidence: none
Action-Mode: autonomous-fail-open
Task: 081KQNJ500008QG0R001N94412
Co-Authored-By: Codex <noreply@openai.com>

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | true |
| Total threads | 1 |
| Resolved threads | 1 |
| Unresolved threads | 0 |
| Total review comments | 2 |
| Total fix commits (touching thread paths) | 2 |

## Review threads

### Thread 1 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F7U8z`
- **Path**: `tools/openspec/inventory.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-31T08:52:48Z on `tools/openspec/inventory.ts`:191 (association: NONE)

  <pre>
  P1: buildGapReport()’s default `artifactRoot` comes from `process.cwd()`, so artifact existence checks (and `missingArtifacts`/`mappedArtifacts`) can change depending on how the caller launched the process (e.g., running tests from a subdir). Since this function is exported and is intended to be deterministic from its inputs, defaulting to the repo root (like the CLI does) avoids surprising, environment-dependent reports.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-31T09:06:07Z on `tools/openspec/inventory.ts`:191 (association: MEMBER)

    <pre>
    Already addressed on the current PR head. `buildGapReport()`'s default no longer comes from `process.cwd()` — commit fa67f13be ("fix(openspec): anchor artifact checks at repo root") changed line 190 to `options.artifactRoot ?? findRepoRoot()`. `findRepoRoot()` anchors to `import.meta.dir` (the module's own location) and walks up to `.git`, so the report is deterministic regardless of the caller's CWD; `process.cwd()` is only the pathological no-git-tree fallback. This finding was filed against the prior commit 1abd44982 which had the `process.cwd()` default. Resolving as already-fixed.
    </pre>

## Fix commits (touching thread paths)

### `1abd449823106360c2f9905f2aebb35baa36acac` -- 2026-05-31T08:48:56Z -- ``

**Touched files (intersect with thread paths):**

- `tools/openspec/inventory.ts`

**Message:**

```
feat(openspec): map artifact-backed specs in inventory

Add a separate capability-to-artifact map so OpenSpec capabilities backed by tests or other repo artifacts are not reported as unmapped source-module gaps.

Map z-set-algebra to its existing F# and C# verification tests without inflating src/Core module coverage. The CLI now reports mapped and missing artifacts separately from module coverage.

Proof: bun test tools/openspec/inventory.test.ts; bun tools/openspec/inventory.ts shows z-set-algebra absent from unmappedSpecs and four mapped artifacts with no missing artifacts; prettier --check passed on the touched files. Limit: full bun run typecheck could not run because tsc is absent on PATH; transient bunx tsc also lacks the bun type library in this worktree.

Agency-Signature-Version: 1
Agent: Vera
Agent-Runtime: OpenAI Codex desktop heartbeat loop
Agent-Model: GPT-5
Credential-Identity: aaron-codex-desktop
Credential-Mode: shared
Human-Review: none
Human-Review-Evidence: none
Action-Mode: autonomous-fail-open
Task: 081KQNJ500008QG0R001N94412
Co-Authored-By: Codex <noreply@openai.com>
```

### `fa67f13be1b2e5e9699ac03cc6e09e422b7d3812` -- 2026-05-31T08:56:06Z -- ``

**Touched files (intersect with thread paths):**

- `tools/openspec/inventory.ts`

**Message:**

```
fix(openspec): anchor artifact checks at repo root

Default buildGapReport artifact checks to the discovered repository root instead of the caller working directory, and detect linked worktree .git files while walking upward.

Add a regression that changes process.cwd() before calling buildGapReport without artifactRoot so exported reports remain stable from their inputs.

Proof: bun test tools/openspec/inventory.test.ts; bun tools/openspec/inventory.ts; bunx prettier --check tools/openspec/inventory.ts tools/openspec/inventory.test.ts.

Agency-Signature-Version: 1
Agent: Vera
Agent-Runtime: OpenAI Codex desktop heartbeat loop
Agent-Model: GPT-5
Credential-Identity: aaron-codex-desktop
Credential-Mode: shared
Human-Review: none
Human-Review-Evidence: none
Action-Mode: autonomous-fail-open
Task: 081KQNJ500008QG0R001N94412
Co-Authored-By: Codex <noreply@openai.com>
```
