# PR #12764 -- research: the combination step is already in our code; temperature is already load-bearing; model the wall closed

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 12764 |
| Title | research: the combination step is already in our code; temperature is already load-bearing; model the wall closed |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-08-20T13:16:23Z |
| Merged at | 2026-08-20T13:25:02Z |
| Merge commit SHA | `5e06a5eacb3d52bb522f8fec1172814c6ebccad1` |
| Branch | `research/closed-hex-wall-and-combination-shape` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/12764 |
| Changed files | 1 |
| Additions / deletions | +153 / -0 |

## Description

Four extensions from Aaron. **Two turned out to be already true in the tree rather than proposals** — that's the useful finding.

## §13 — The combination shape, checked against source rather than agreed with

> *"very very similar to the way i see our amplitudeemu and our q# code and our softemu and other bayesian stuff where the combination of them happens in a similar fashion."*

Not a resemblance. `src/Core/AmplitudeEmu.fs` already says it in its own header — `merge` sums the amplitudes of identical frames, opposite phase cancels and equal phase reinforces, and the file states flatly: **"That is interference, in code."** `bornProb` is `|amplitude|²`, called out as *"the only place amplitudes become probabilities."*

The control is already written down too: *drop the merge and use real weights ⇒ no interference; restore merge with complex weights ⇒ the two-slit falls out.*

| transactional | our code |
|---|---|
| offer wave (ket) | an amplitude-weighted frame |
| confirmation (bra) | the **identical frame** `merge` finds to sum against |
| the transaction ⇒ projector | `merge` / `SoftMix.Consolidate` |
| collapse; Born rule derived | `bornProb = \|amplitude\|²` |

**The combination *is* the measurement event in both** — which is why "drop the merge ⇒ no interference" is the right control, and why our meter refuses a one-sided ΔU.

Limits already in that file are carried over verbatim, not smoothed: interference ≠ entanglement ≠ signalling; the `4ⁿ` wall isn't escaped; CHIP-8 opcodes introduce no phase.

## §14 — Coincidence as a causal index

Index by what co-occurred, not by when a clock said it happened. Adjacency accumulates into causal order without anyone stamping a time. Standing guard applies harder at substrate scale: **store the register with the coincidence** — an unlabelled coincidence in a *shared* index is a belief nobody decided to hold that everyone now reads.

## §15 — Temperature: three of the four links were already here

1. **Measurement occurrence is a temperature knob** — Hackermüller 2004, the heater kills and restores C60 fringes
2. **`rules.bak/forgetting-costs-energy-remembering-is-cheap-landauer-...`** — erasure costs `kT ln 2` (Landauer 1961); reversible computation avoids it (Bennett 1973)
3. **`ARRIVAL-PROTOCOL.md`** — clock noise *is* thermal noise (Johnson–Nyquist), resolving it costs `kT ln 2`, *"why wall-clock drift is contaminating noise, not a clean identity source"*

**2 + 3 stop `local-time-never-enters-the-shared-fold` being a discipline we chose and make it one we're charged for.** A clock reading is a measurement against a thermal noise floor, so admitting it injects a heat source into a value meant to be a pure function of evidence.

And it prices our ledger: the Z-set is append-only `+1`/`−1`, **reversible by construction, so the fold is Landauer-free.** The irreversible step is *acting* on the fold — consuming a conclusion, erasing a possibility. Exactly where TI puts collapse. **Reversible up to the outcome; the outcome is what you pay for.** Live falsifier already filed: `081KR50HA0008QG0R002Z51PMR` (FPGA power measurement for Landauer validation).

**The fourth link is Aaron's leap and is labelled one.** Jacobson 1995 / Verlinde 2011 are the real and *contested* literature. Per the metering test: Landauer earns its place because it **prices a bit**; entropic gravity would earn its place only if it priced something we measure, and it doesn't yet. Recorded as a direction, refused as an anchor.

## §16 — Modelling the wall as closed (his call: try it, no dogma)

**(a) Exactly twelve defects at any size.** `F = 12 + h`, `V = 20 + 2h`, `E = 30 + 3h`. Twelve is invariant under growth — a closed reservoir scales without changing its count of irregular sites. That's scale-freeness holding for the wall itself, and the strongest argument for closing.

**(b) It abolishes Phase 0** — and this wasn't obvious before doing the exercise. A closed surface has **no boundary**, so there's no direction an emitter can point where nothing absorbs. *Ping-without-return cannot occur inside it.* Every remaining failure is Phase 1: returns exist, nothing computed a fix.

That converts a class we can't debug into one we can: **every silent failure in the reservoir becomes in principle a bug rather than a boundary condition.**

**(c) Finite area ⇒ saturation and structural back-pressure** — the cost of closing.

**(d) The twelve are distinguished positions we don't get to place.** A prediction with somewhere to look: does anything in the workflow-wall already cluster into twelve?

Registered as a **modelling hypothesis with three named falsifiers** — an observed genuine Phase 0 failure inside the reservoir; unbounded absorption with no back-pressure; a required exit — not a commitment. No code changes.

Agency-Signature-Version: 1
Agent: otto
Agent-Runtime: claude-code/local
Agent-Model: claude-opus-5
Credential-Identity: AceHack
Credential-Mode: shared
Human-Review: not-implied-by-credential
Human-Review-Evidence: none
Action-Mode: autonomous-fail-open
Task: none
Co-authored-by: Claude Opus 5 <noreply@anthropic.com>

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 0 |
| Resolved threads | 0 |
| Unresolved threads | 0 |
| Total review comments | 0 |
| Total fix commits (touching thread paths) | 0 |

## Review threads

(no review threads on this PR)

## Fix commits (touching thread paths)

(no commits in this PR touched files anchored by review threads)
