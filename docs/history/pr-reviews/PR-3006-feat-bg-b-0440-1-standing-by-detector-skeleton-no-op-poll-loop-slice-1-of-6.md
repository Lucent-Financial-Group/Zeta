# PR #3006 -- feat(bg): B-0440.1 — standing-by detector skeleton + no-op poll loop (slice 1 of 6)

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 3006 |
| Title | feat(bg): B-0440.1 — standing-by detector skeleton + no-op poll loop (slice 1 of 6) |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-05-13T17:31:20Z |
| Merged at | 2026-05-13T17:41:45Z |
| Merge commit SHA | `fd2dffbacc202474b042e1c3193d94c91508bca2` |
| Branch | `otto-b0440-1-standing-by-detector-skeleton-slice-1-impl-2026-05-13` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/3006 |
| Changed files | 3 |
| Additions / deletions | +163 / -0 |

## Description

## Summary

First implementation slice of B-0440 (Standing-by failure-mode detector). Ships ONLY the skeleton — future slices add real detection logic.

## What lands

**\`tools/bg/standing-by-detector.ts\`** (76 lines):
- \`DetectorConfig\` type + \`DEFAULT_CONFIG\` (5min poll / 15min idle threshold)
- \`pollOnce()\` — no-op result with slice-1 placeholder note
- \`runDetector()\` — loop scaffolding; \`--once\` for cron-driven mode
- CLI entry with \`--poll-min\` / \`--idle-min\` / \`--once\` flags

**\`tools/bg/standing-by-detector.test.ts\`** (3 tests, all pass):
- Default config thresholds
- \`pollOnce\` returns ISO-timestamped no-op result
- \`runDetector\` with \`once:true\` exits after one iteration

**\`tools/bg/README.md\`** — directory purpose + service inventory + run instructions.

## Decomposition (per B-0440)

| Slice | Status | What |
|-------|--------|------|
| **1** | **THIS PR** | Skeleton + no-op poll loop |
| 2 | TBD | Commit-history poll via \`git log\` |
| 3 | TBD | PR-activity poll via \`gh\` CLI |
| 4 | TBD | Nudge payload + bus publish (requires B-0400 schema extension for \`infinite-backlog-nudge\` topic) |
| 5 | TBD | Integration with agent subscribers |
| 6 | TBD | Cron registration + integration tests |

## Rule 0 compliance

TypeScript only — no \`.sh\` files in \`tools/bg/\`.

## Test results

\`\`\`
bun test v1.3.12
 3 pass
 0 fail
 8 expect() calls
Ran 3 tests across 1 file. [76.00ms]
\`\`\`

## Composes with

- B-0440 (the backlog row this implements; PR #3000 merged earlier today)
- B-0400 (bus protocol — for future slice 4)
- B-0441 / B-0442 (companion services in the mechanization suite)
- PR #2998 (architectural challenge that produced these rows)
- PR #2999 (substrate-honest discipline triad — decomposition discipline)

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 4 |
| Resolved threads | 4 |
| Unresolved threads | 0 |
| Total review comments | 4 |
| Total fix commits (touching thread paths) | 2 |

## Review threads

### Thread 1 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6B1vL8`
- **Path**: `tools/bg/standing-by-detector.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-13T17:32:50Z on `tools/bg/standing-by-detector.ts`:57 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Stop unbounded result growth in continuous detector mode**

  When `runDetector` runs in daemon mode (`once: false`), each iteration appends to `results` and the loop never terminates, so memory usage grows without bound over time. `tools/bg/README.md` explicitly documents this long-running mode, which means this path is expected to stay alive for extended periods and can eventually degrade or OOM the process; the returned array is only needed for one-shot/test usage.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 2 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6B1vL_`
- **Path**: `tools/bg/standing-by-detector.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-13T17:32:50Z on `tools/bg/standing-by-detector.ts`:75 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Validate --poll-min before using it as timer delay**

  `parseArgs` assigns `Number(argv[++i])` directly to `pollIntervalMin` with no bounds/type checks. Passing a non-numeric or non-positive value (e.g. `--poll-min foo` or `--poll-min 0`) makes the computed timeout invalid or near-zero, causing the loop to run almost continuously and spike CPU/log volume instead of polling at minute cadence. This should reject or clamp invalid values before they reach `setTimeout`.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 3 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6B1yBy`
- **Path**: `tools/bg/standing-by-detector.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-13T17:35:15Z on `tools/bg/standing-by-detector.ts`:57 (association: NONE)

  <pre>
  runDetector() accumulates every PollResult in an array even in daemon mode. For a long-running service this grows without bound (and the function never returns anyway). Suggest: only collect results when once=true (or cap to last N) and otherwise stream/log without retaining history.
  </pre>

### Thread 4 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6B1yCS`
- **Path**: `tools/bg/standing-by-detector.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-13T17:35:15Z on `tools/bg/standing-by-detector.ts`:77 (association: NONE)

  <pre>
  parseArgs() silently ignores unknown flags/arguments (anything not matching --once/--poll-min/--idle-min). For cron/launchd-driven services this can hide typos and run with unintended defaults. Suggest: add an explicit else branch that errors on unknown args and supports --help.

  This issue also appears on line 74 of the same file.

  </pre>

## Fix commits (touching thread paths)

### `fa901dffc11f3ed30a5afb7e6b6348a2d9d81bfc` -- 2026-05-13T17:30:52Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/bg/standing-by-detector.ts`

**Message:**

```
feat(bg): B-0440.1 — standing-by detector skeleton + no-op poll loop …

…(3 files, 3 tests pass)

First implementation slice of B-0440 (Standing-by failure-mode
detector). Ships ONLY the skeleton; future slices add real
detection.

Files:
- tools/bg/standing-by-detector.ts (76 lines):
  - DetectorConfig type + DEFAULT_CONFIG (5min poll / 15min idle threshold)
  - pollOnce() — no-op result with slice-1 placeholder note
  - runDetector() — loop scaffolding; --once for cron-driven mode
  - CLI entry with --poll-min / --idle-min / --once flags

- tools/bg/standing-by-detector.test.ts (3 tests):
  - default config thresholds
  - pollOnce returns ISO-timestamped no-op result
  - runDetector with once:true exits after one iteration

- tools/bg/README.md:
  - Directory purpose
  - Service inventory (B-0440 current; B-0441/B-0442 planned)
  - Run instructions (cron-driven --once vs standalone daemon)

Per Rule 0: TypeScript only (no .sh files in tools/bg/).

Future slices (per B-0440 decomposition section):
- Slice 2: commit-history poll via git log
- Slice 3: PR-activity poll via gh CLI
- Slice 4: nudge payload computation + bus publish
  (requires B-0400 schema extension for infinite-backlog-nudge topic)
- Slice 5: integration with agent subscribers
- Slice 6: additional tests + cron registration

Composes with:
- B-0440 (the backlog row this implements; PR #3000 merged)
- B-0400 (bus protocol — for future slice 4)
- B-0441 / B-0442 (companion services)
- PR #2998 (architectural challenge that produced these rows)
- PR #2999 (substrate-honest discipline triad — decomposition discipline)

Co-Authored-By: Claude <noreply@anthropic.com>
```

### `05efd257851da5ac62ac32ef053b94fa3af4f879` -- 2026-05-13T17:37:58Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/bg/standing-by-detector.ts`

**Message:**

```
fix(bg): B-0440.1 — close 2 Copilot findings (P1 unbounded results + …

…P2 arg validation) + markdownlint

- P1: runDetector daemon mode no longer accumulates results forever
  (split into single-iter return-array path + infinite-loop discard path).
  Same fix should land in B-0441.1 (PR #3007) — will follow up.
- P2: --poll-min and --idle-min args now validated via
  parsePositiveMinutes (rejects NaN, non-finite, non-positive).
- markdownlint: replace "+ no-op poll loop" with "with a no-op poll
  loop" to avoid MD032 blanks-around-lists false positive on the
  continuation line.

Tests still 3 pass / 0 fail.

Co-Authored-By: Claude <noreply@anthropic.com>
```
