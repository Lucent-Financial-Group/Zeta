# PR #2055 -- fix(B-0156): port smoke-test.sh scripts to TypeScript

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 2055 |
| Title | fix(B-0156): port smoke-test.sh scripts to TypeScript |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-05-08T11:15:02Z |
| Merged at | 2026-05-08T11:29:12Z |
| Merge commit SHA | `65ca0e2c4a7cbb0dcac585f894ced42db6951499` |
| Branch | `fix/b-0156-port-smoke-test-sh-to-ts` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/2055 |
| Changed files | 7 |
| Additions / deletions | +537 / -376 |

## Description

## Summary
- Ports 3 `smoke-test.sh` scripts to TypeScript (`smoke-test.ts`) for Rule 0 compliance (.sh is for install-graph only; TS is cross-platform DST)
- Ported files: `samples/FactoryDemo.Api.CSharp/smoke-test.ts`, `samples/FactoryDemo.Api.FSharp/smoke-test.ts`, `samples/FactoryDemo.Db/smoke-test.ts`
- Deleted the original `.sh` files
- Updated `samples/FactoryDemo.Db/README.md` references from `smoke-test.sh` to `smoke-test.ts`
- Uses Bun built-in `fetch` instead of `curl`, direct JSON property access instead of `jq`, and `spawnSync`/`spawn` from Bun instead of bash process management

## Test plan
- [x] `dotnet build -c Release` passes with 0 warnings, 0 errors
- [ ] Run `bun samples/FactoryDemo.Api.CSharp/smoke-test.ts` against a live API build
- [ ] Run `bun samples/FactoryDemo.Api.FSharp/smoke-test.ts` against a live API build
- [ ] Run `bun samples/FactoryDemo.Db/smoke-test.ts` with docker-compose db running

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | true |
| Total threads | 16 |
| Resolved threads | 16 |
| Unresolved threads | 0 |
| Total review comments | 32 |
| Total fix commits (touching thread paths) | 2 |

## Review threads

### Thread 1 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6AmnQK`
- **Path**: `samples/FactoryDemo.Api.CSharp/smoke-test.ts`
- **Replies**: 1

**Initial comment:**

- **`github-code-quality[bot]` (bot)** at 2026-05-08T11:17:24Z on `samples/FactoryDemo.Api.CSharp/smoke-test.ts`:21 (association: NONE)

  <pre>
  ## Invocation of non-function

  Callee is not a function: it has type undefined.

  ---

  &lt;p&gt;To fix this safely without changing behavior, avoid shadowing the global &lt;code&gt;URL&lt;/code&gt; constructor. Keep &lt;code&gt;new URL(import.meta.url)&lt;/code&gt; as-is, and rename the local string constant &lt;code&gt;URL&lt;/code&gt; (the base server URL) to a non-conflicting name such as &lt;code&gt;BASE_URL&lt;/code&gt;, then update its usages in this file.&lt;/p&gt;
  &lt;p&gt;Best single fix in &lt;code&gt;samples/FactoryDemo.Api.CSharp/smoke-test.ts&lt;/code&gt;:&lt;/p&gt;
  &lt;ul&gt;
  &lt;li&gt;Change &lt;code&gt;const URL = \&lt;/code&gt;&lt;a href="http://localhost:$%7BPORT%7D%5C%60;%60" rel="nofollow"&gt;http://localhost:${PORT}\`;`&lt;/a&gt; to &lt;code&gt;const BASE_URL = ...&lt;/code&gt;.&lt;/li&gt;
  &lt;li&gt;Update all references that mean the server base URL (e.g., log output and fetch targets) from &lt;code&gt;URL&lt;/code&gt; to &lt;code&gt;BASE_URL&lt;/code&gt;.&lt;/li&gt;
  &lt;li&gt;No new imports, methods, or dependencies are required.&lt;/li&gt;
  &lt;/ul&gt;
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-08T11:25:55Z on `samples/FactoryDemo.Api.CSharp/smoke-test.ts`:21 (association: MEMBER)

    <pre>
    Fixed in a0ea35a — renamed URL to BASE_URL to avoid shadowing the global URL constructor.
    </pre>

### Thread 2 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6AmnQU`
- **Path**: `samples/FactoryDemo.Api.FSharp/smoke-test.ts`
- **Replies**: 1

**Initial comment:**

- **`github-code-quality[bot]` (bot)** at 2026-05-08T11:17:24Z on `samples/FactoryDemo.Api.FSharp/smoke-test.ts`:22 (association: NONE)

  <pre>
  ## Invocation of non-function

  Callee is not a function: it has type undefined.

  ---

  The best fix is to avoid shadowing the global &lt;code&gt;URL&lt;/code&gt; constructor by renaming the local endpoint string constant (&lt;code&gt;URL&lt;/code&gt;) to a non-conflicting identifier (for example, &lt;code&gt;BASE_URL&lt;/code&gt;) and updating its usages.&lt;/p&gt;
  &lt;p&gt;Specifically in &lt;code&gt;samples/FactoryDemo.Api.FSharp/smoke-test.ts&lt;/code&gt;:&lt;/p&gt;
  &lt;ul&gt;
  &lt;li&gt;Change &lt;code&gt;const URL = \&lt;/code&gt;&lt;a href="http://localhost:$%7BPORT%7D%5C%60;%60" rel="nofollow"&gt;http://localhost:${PORT}\`;`&lt;/a&gt; to &lt;code&gt;const BASE_URL = ...&lt;/code&gt;.&lt;/li&gt;
  &lt;li&gt;Update references like &lt;code&gt;console.log(\&lt;/code&gt;Starting API on ${URL} ...`)&lt;code&gt;to use&lt;/code&gt;BASE_URL`.&lt;/li&gt;
  &lt;li&gt;Keep &lt;code&gt;new URL(import.meta.url)&lt;/code&gt; unchanged so it clearly refers to the global constructor.&lt;/li&gt;
  &lt;/ul&gt;
  &lt;p&gt;No new imports, methods, or dependencies are required.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-08T11:26:00Z on `samples/FactoryDemo.Api.FSharp/smoke-test.ts`:22 (association: MEMBER)

    <pre>
    Fixed in a0ea35a — renamed URL to BASE_URL to avoid shadowing the global URL constructor.
    </pre>

### Thread 3 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6AmnQX`
- **Path**: `samples/FactoryDemo.Api.CSharp/smoke-test.ts`
- **Replies**: 1

**Initial comment:**

- **`github-code-quality[bot]` (bot)** at 2026-05-08T11:17:24Z on `samples/FactoryDemo.Api.CSharp/smoke-test.ts`:21 (association: NONE)

  <pre>
  ## Variable not declared before use

  Variable 'URL' is used before its &lt;a class="Link" href="/Lucent-Financial-Group/Zeta/blob/b67c522a73f05aedf9508dfa04ac40748768b631/samples/FactoryDemo.Api.CSharp/smoke-test.ts#L33-L33"&gt;declaration&lt;/a&gt;.

  ---

  To fix this cleanly without changing functionality, rename the local &lt;code&gt;URL&lt;/code&gt; constant (API base URL string) to a distinct name such as &lt;code&gt;BASE_URL&lt;/code&gt;, and update references in this file accordingly. This removes shadowing/confusion with the global &lt;code&gt;URL&lt;/code&gt; constructor used at line 21.&lt;/p&gt;
  &lt;p&gt;In &lt;code&gt;samples/FactoryDemo.Api.CSharp/smoke-test.ts&lt;/code&gt;:&lt;/p&gt;
  &lt;ul&gt;
  &lt;li&gt;Change &lt;code&gt;const URL = \&lt;/code&gt;&lt;a href="http://localhost:$%7BPORT%7D%5C%60;%60" rel="nofollow"&gt;http://localhost:${PORT}\`;`&lt;/a&gt; to &lt;code&gt;const BASE_URL = ...&lt;/code&gt;.&lt;/li&gt;
  &lt;li&gt;Update the startup log line to use &lt;code&gt;${BASE_URL}&lt;/code&gt;.&lt;/li&gt;
  &lt;li&gt;Update any request construction that uses &lt;code&gt;URL&lt;/code&gt; as the base to use &lt;code&gt;BASE_URL&lt;/code&gt;.&lt;/li&gt;
  &lt;/ul&gt;
  &lt;p&gt;No imports, new methods, or external definitions are required.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-08T11:26:01Z on `samples/FactoryDemo.Api.CSharp/smoke-test.ts`:21 (association: MEMBER)

    <pre>
    Fixed in a0ea35a — renamed URL to BASE_URL, eliminating the use-before-declaration issue.
    </pre>

### Thread 4 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6AmnQe`
- **Path**: `samples/FactoryDemo.Api.FSharp/smoke-test.ts`
- **Replies**: 1

**Initial comment:**

- **`github-code-quality[bot]` (bot)** at 2026-05-08T11:17:25Z on `samples/FactoryDemo.Api.FSharp/smoke-test.ts`:22 (association: NONE)

  <pre>
  ## Variable not declared before use

  Variable 'URL' is used before its &lt;a class="Link" href="/Lucent-Financial-Group/Zeta/blob/b67c522a73f05aedf9508dfa04ac40748768b631/samples/FactoryDemo.Api.FSharp/smoke-test.ts#L34-L34"&gt;declaration&lt;/a&gt;.

  ---

  &lt;p&gt;To fix this safely without changing behavior, rename the local &lt;code&gt;const URL&lt;/code&gt; (the API base URL string) to a non-conflicting identifier such as &lt;code&gt;BASE_URL&lt;/code&gt;, and update its usages accordingly.&lt;/p&gt;
  &lt;p&gt;Best single fix in this file:&lt;/p&gt;
  &lt;ul&gt;
  &lt;li&gt;In &lt;code&gt;samples/FactoryDemo.Api.FSharp/smoke-test.ts&lt;/code&gt;, change:
  &lt;ul&gt;
  &lt;li&gt;&lt;code&gt;const URL = \&lt;/code&gt;&lt;a href="http://localhost:$%7BPORT%7D%5C%60;%60" rel="nofollow"&gt;http://localhost:${PORT}\`;`&lt;/a&gt; → &lt;code&gt;const BASE_URL = ...&lt;/code&gt;&lt;/li&gt;
  &lt;li&gt;Any later references meant for the API base URL (shown here in the startup log) from &lt;code&gt;${URL}&lt;/code&gt; to &lt;code&gt;${BASE_URL}&lt;/code&gt;.&lt;/li&gt;
  &lt;/ul&gt;
  &lt;/li&gt;
  &lt;li&gt;No imports or new dependencies are needed.&lt;/li&gt;
  &lt;li&gt;This preserves functionality while eliminating the shadowing of the global &lt;code&gt;URL&lt;/code&gt; constructor used on line 22.&lt;/li&gt;
  &lt;/ul&gt;
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-08T11:26:03Z on `samples/FactoryDemo.Api.FSharp/smoke-test.ts`:22 (association: MEMBER)

    <pre>
    Fixed in a0ea35a — renamed URL to BASE_URL, eliminating the use-before-declaration issue.
    </pre>

### Thread 5 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6AmnQm`
- **Path**: `samples/FactoryDemo.Api.CSharp/smoke-test.ts`
- **Replies**: 1

**Initial comment:**

- **`github-code-quality[bot]` (bot)** at 2026-05-08T11:17:25Z on `samples/FactoryDemo.Api.CSharp/smoke-test.ts`:19 (association: NONE)

  <pre>
  ## Unused variable, import, function or class

  Unused imports unlinkSync, writeFileSync.

  ---

  To fix this safely without changing functionality, remove only the unused names from the &lt;code&gt;fs&lt;/code&gt; import list and keep the imports that are actually used (here, &lt;code&gt;mkdtempSync&lt;/code&gt; and &lt;code&gt;readFileSync&lt;/code&gt; based on the provided context).&lt;/p&gt;
  &lt;p&gt;Best single change:&lt;/p&gt;
  &lt;ul&gt;
  &lt;li&gt;**File:** &lt;code&gt;samples/FactoryDemo.Api.CSharp/smoke-test.ts&lt;/code&gt;&lt;/li&gt;
  &lt;li&gt;**Region:** import section near line 19&lt;/li&gt;
  &lt;li&gt;**Edit:** change&lt;br&gt;
  &lt;code&gt;import { mkdtempSync, writeFileSync, readFileSync, unlinkSync } from "fs";&lt;/code&gt;&lt;br&gt;
  to&lt;br&gt;
  &lt;code&gt;import { mkdtempSync, readFileSync } from "fs";&lt;/code&gt;&lt;/li&gt;
  &lt;/ul&gt;
  &lt;p&gt;No new methods, definitions, or dependencies are required.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-08T11:26:04Z on `samples/FactoryDemo.Api.CSharp/smoke-test.ts`:19 (association: MEMBER)

    <pre>
    Fixed in a0ea35a — removed unused imports writeFileSync and unlinkSync.
    </pre>

### Thread 6 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6AmnQr`
- **Path**: `samples/FactoryDemo.Api.CSharp/smoke-test.ts`
- **Replies**: 1

**Initial comment:**

- **`github-code-quality[bot]` (bot)** at 2026-05-08T11:17:25Z on `samples/FactoryDemo.Api.CSharp/smoke-test.ts`:51 (association: NONE)

  <pre>
  ## Unused variable, import, function or class

  Unused variable logFile.

  ---

  &lt;p&gt;The best fix is to remove the unused &lt;code&gt;logFile&lt;/code&gt; declaration while keeping existing logging behavior unchanged.&lt;/p&gt;
  &lt;ul&gt;
  &lt;li&gt;**General approach:** delete unused declarations that have no effect on runtime behavior.&lt;/li&gt;
  &lt;li&gt;**Best concrete fix here:** in &lt;code&gt;samples/FactoryDemo.Api.CSharp/smoke-test.ts&lt;/code&gt;, remove line 51 (&lt;code&gt;const logFile = Bun.file(LOG_FILE);&lt;/code&gt;) and leave the &lt;code&gt;spawn&lt;/code&gt; call intact, since it already writes output via the &lt;code&gt;LOG_FILE&lt;/code&gt; path.&lt;/li&gt;
  &lt;li&gt;**What’s needed:** no new imports, methods, or definitions.&lt;/li&gt;
  &lt;/ul&gt;
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-08T11:26:05Z on `samples/FactoryDemo.Api.CSharp/smoke-test.ts`:51 (association: MEMBER)

    <pre>
    Fixed in a0ea35a — removed unused logFile variable; now using logFileHandle = Bun.file(LOG_FILE) which is consumed by spawn.
    </pre>

### Thread 7 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6Amn0O`
- **Path**: `samples/FactoryDemo.Api.CSharp/smoke-test.ts`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-08T11:18:00Z on `samples/FactoryDemo.Api.CSharp/smoke-test.ts`:21 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Derive script directory from file URL correctly**

  `new URL(import.meta.url).pathname` leaves URL encoding in place (for example `%20`) and produces `/C:/...` on Windows paths, so `PROJECT`/`cwd` can point to non-existent locations when the checkout path contains spaces or when run on Windows. This makes the smoke tests fail before they can start the demo app/compose stack. Use a file-URL-to-path conversion (or Bun’s native module path helpers) instead of `pathname`; the same pattern appears in all three new smoke-test scripts.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-08T11:26:06Z on `samples/FactoryDemo.Api.CSharp/smoke-test.ts`:21 (association: MEMBER)

    <pre>
    Fixed in a0ea35a — all three scripts now use fileURLToPath(import.meta.url) instead of new URL(import.meta.url).pathname, handling percent-encoding and Windows drive-letter paths correctly.
    </pre>

### Thread 8 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6Amn0U`
- **Path**: `samples/FactoryDemo.Db/smoke-test.ts`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-08T11:18:00Z on `samples/FactoryDemo.Db/smoke-test.ts`:43 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Replace shell sleep command with Bun-native delay**

  The retry loop now depends on an external `sleep` executable via `spawnSync(["sleep", "1"])`; that command is not portable across environments (notably Windows), so the DB smoke test can fail even when Docker/Postgres are healthy. Since this change is intended to move smoke tests to portable TypeScript/Bun, this should use `Bun.sleep`/`Bun.sleepSync` instead of shelling out.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-08T11:26:07Z on `samples/FactoryDemo.Db/smoke-test.ts`:43 (association: MEMBER)

    <pre>
    Fixed in a0ea35a — replaced spawnSync(["sleep", "1"]) with await Bun.sleep(1000) using an async waitForPostgres() wrapper. No external binary dependency.
    </pre>

### Thread 9 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6AmqaZ`
- **Path**: `samples/FactoryDemo.Api.CSharp/smoke-test.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-08T11:20:38Z on `samples/FactoryDemo.Api.CSharp/smoke-test.ts`:19 (association: NONE)

  <pre>
  `writeFileSync` and `unlinkSync` are imported but never used; with `noUnusedLocals` enabled in tsconfig this will fail `tsc --noEmit`. Remove the unused imports (or use them if intended).

  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-08T11:26:08Z on `samples/FactoryDemo.Api.CSharp/smoke-test.ts`:19 (association: MEMBER)

    <pre>
    Fixed in a0ea35a — removed unused imports writeFileSync and unlinkSync.
    </pre>

### Thread 10 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6Amqaz`
- **Path**: `samples/FactoryDemo.Api.CSharp/smoke-test.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-08T11:20:38Z on `samples/FactoryDemo.Api.CSharp/smoke-test.ts`:51 (association: NONE)

  <pre>
  `logFile` is declared but never used; with `noUnusedLocals` enabled this will fail TypeScript typechecking. Remove it or use it for the spawned process stdio if that's what it was meant for.

  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-08T11:26:10Z on `samples/FactoryDemo.Api.CSharp/smoke-test.ts`:51 (association: MEMBER)

    <pre>
    Fixed in a0ea35a — removed unused logFile declaration; replaced with logFileHandle = Bun.file(LOG_FILE) which is consumed by spawn.
    </pre>

### Thread 11 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6AmqbG`
- **Path**: `samples/FactoryDemo.Api.CSharp/smoke-test.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-08T11:20:39Z on `samples/FactoryDemo.Api.CSharp/smoke-test.ts`:21 (association: NONE)

  <pre>
  `dirname(new URL(import.meta.url).pathname)` is not a robust way to compute a filesystem path (percent-encoding + `file:///C:/...` leading slash issues). Use `fileURLToPath(import.meta.url)` (as used elsewhere in the repo) before calling `dirname` so this works on Windows and paths with spaces.

  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-08T11:26:11Z on `samples/FactoryDemo.Api.CSharp/smoke-test.ts`:21 (association: MEMBER)

    <pre>
    Fixed in a0ea35a — now uses fileURLToPath(import.meta.url) before dirname for cross-platform path derivation.
    </pre>

### Thread 12 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6AmqbY`
- **Path**: `samples/FactoryDemo.Api.CSharp/smoke-test.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-08T11:20:39Z on `samples/FactoryDemo.Api.CSharp/smoke-test.ts`:30 (association: NONE)

  <pre>
  Shelling out to `which` is not portable (not available on Windows). Prefer `Bun.which("dotnet")` for cross-platform tool detection (pattern used in tools/hygiene/*.ts).
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-08T11:26:12Z on `samples/FactoryDemo.Api.CSharp/smoke-test.ts`:30 (association: MEMBER)

    <pre>
    Fixed in a0ea35a — replaced spawnSync(["which", "dotnet"]) with Bun.which("dotnet") for cross-platform tool detection.
    </pre>

### Thread 13 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6Amqbt`
- **Path**: `samples/FactoryDemo.Api.FSharp/smoke-test.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-08T11:20:39Z on `samples/FactoryDemo.Api.FSharp/smoke-test.ts`:22 (association: NONE)

  <pre>
  `dirname(new URL(import.meta.url).pathname)` can mis-handle `file://` URLs (Windows drive letters) and percent-encoded paths. Use `fileURLToPath(import.meta.url)` before calling `dirname` to keep the script cross-platform.

  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-08T11:26:13Z on `samples/FactoryDemo.Api.FSharp/smoke-test.ts`:22 (association: MEMBER)

    <pre>
    Fixed in a0ea35a — now uses fileURLToPath(import.meta.url) before dirname for cross-platform path derivation.
    </pre>

### Thread 14 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6AmqcA`
- **Path**: `samples/FactoryDemo.Api.FSharp/smoke-test.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-08T11:20:39Z on `samples/FactoryDemo.Api.FSharp/smoke-test.ts`:27 (association: NONE)

  <pre>
  Shelling out to `which` is not portable (not available on Windows). Prefer `Bun.which("dotnet")` for cross-platform tool detection.

  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-08T11:26:14Z on `samples/FactoryDemo.Api.FSharp/smoke-test.ts`:27 (association: MEMBER)

    <pre>
    Fixed in a0ea35a — replaced spawnSync(["which", "dotnet"]) with Bun.which("dotnet") for cross-platform tool detection.
    </pre>

### Thread 15 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6AmqcP`
- **Path**: `samples/FactoryDemo.Db/smoke-test.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-08T11:20:40Z on `samples/FactoryDemo.Db/smoke-test.ts`:13 (association: NONE)

  <pre>
  `dirname(new URL(import.meta.url).pathname)` is not a safe way to derive a filesystem directory (encoding + Windows `file:///C:/...`). Use `fileURLToPath(import.meta.url)` before `dirname` for cross-platform correctness.

  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-08T11:26:15Z on `samples/FactoryDemo.Db/smoke-test.ts`:13 (association: MEMBER)

    <pre>
    Fixed in a0ea35a — now uses fileURLToPath(import.meta.url) before dirname for cross-platform path derivation.
    </pre>

### Thread 16 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6Amqcc`
- **Path**: `samples/FactoryDemo.Db/smoke-test.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-08T11:20:40Z on `samples/FactoryDemo.Db/smoke-test.ts`:43 (association: NONE)

  <pre>
  Using `spawnSync(["sleep", "1"])` relies on an external `sleep` binary (not present on Windows) and undermines the stated cross-platform goal. Prefer `await Bun.sleep(1000)` (or another JS-native sleep) and make the wait loop async.

  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-08T11:26:17Z on `samples/FactoryDemo.Db/smoke-test.ts`:43 (association: MEMBER)

    <pre>
    Fixed in a0ea35a — replaced spawnSync(["sleep", "1"]) with await Bun.sleep(1000) using an async waitForPostgres() wrapper. No external binary dependency.
    </pre>

## Fix commits (touching thread paths)

### `b67c522a73f05aedf9508dfa04ac40748768b631` -- 2026-05-08T11:14:51Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `samples/FactoryDemo.Api.CSharp/smoke-test.ts`
- `samples/FactoryDemo.Api.FSharp/smoke-test.ts`
- `samples/FactoryDemo.Db/smoke-test.ts`

**Message:**

```
fix(B-0156): port 3 smoke-test.sh scripts to TypeScript

Rule 0 compliance — .sh is for install-graph only.
Ports samples/FactoryDemo.*/smoke-test.sh → .ts.

Co-Authored-By: Claude Opus 4.6 (1M context) <noreply@anthropic.com>
```

### `a0ea35aadd889b9c855a8a8e52af1001f346fc21` -- 2026-05-08T11:25:30Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `samples/FactoryDemo.Api.CSharp/smoke-test.ts`
- `samples/FactoryDemo.Api.FSharp/smoke-test.ts`
- `samples/FactoryDemo.Db/smoke-test.ts`

**Message:**

```
fix(PR-2055): address 16 review findings — type fixes, error handling

- Rename `URL` to `BASE_URL` to avoid shadowing global URL constructor
  (fixes tsc "invocation of non-function" and "used before declaration")
- Remove unused imports (writeFileSync, unlinkSync) and unused variable
  (logFile → logFileHandle, now used for Bun.file() handle)
- Use `fileURLToPath(import.meta.url)` instead of `new URL(...).pathname`
  for cross-platform path derivation (Windows + percent-encoded paths)
- Replace `spawnSync(["which", "dotnet"])` with `Bun.which("dotnet")`
  for cross-platform tool detection
- Replace `spawnSync(["sleep", "1"])` with `await Bun.sleep(1000)` in
  Db smoke-test for portability (no external sleep binary needed)
- Use `Bun.file(LOG_FILE)` for spawn stdout/stderr instead of raw
  string path (fixes TS2322 type error)

Co-Authored-By: Claude Opus 4.6 (1M context) <noreply@anthropic.com>
```
