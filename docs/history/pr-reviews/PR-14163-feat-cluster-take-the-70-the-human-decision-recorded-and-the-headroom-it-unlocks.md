# PR #14163 -- feat(cluster): take the 70 — the human decision recorded, and the headroom it unlocks

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 14163 |
| Title | feat(cluster): take the 70 — the human decision recorded, and the headroom it unlocks |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-08-23T12:35:53Z |
| Merged at | 2026-08-23T16:01:06Z |
| Merge commit SHA | `26ee4561556021c3c79bb1a775f9b82f886dab1b` |
| Branch | `feat/take-the-70-unlock-giants` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/14163 |
| Changed files | 11 |
| Additions / deletions | +412 / -176 |

## Description

## Aaron took the 70. This PR is the record of that, rebased and re-measured.

> **AARON, VERBATIM:** "take the 70, unlock hindsight and vllm on hosted runners"

> **AARON, VERBATIM:** "cpu is a compressible resource, can we not set the requests smaller to make it fit? seems like should be able to test more charts"

`runnerEnvelope.freeDiskGib` **14 → 70**, with the authorization written into
`measuredFreeDiskEvidence` — dated, quoted, beside the measurement — because that string is
what the next agent reads. **That record is the substance here, more than the number.** The
field moved four times in under three hours on 2026-08-22 (#13784 → 70, #13830 → 14, #13871
→ 70, #13903 → 14) with no human decision recorded in either direction. The tripwire is
**not** deleted and is restated as standing: any future change, up or down, needs a named
human authorization recorded in that string.

70 is a **floor beneath both measured runners** (77.06 GiB x64, 99.02 GiB arm), not a
transcription of either — so `runner-disk.ts --check-envelope` gets *stronger*: at 14 it
could only convict a machine no hosted runner resembles.

---

## Rebased onto #14174 / #14194 / #14233 / #14250 and re-measured

One of this PR's two headline claims did not survive that. It is **withdrawn**, not carried.

| | #14163 claimed | `origin/main` | **this PR, measured** |
|---|---|---|---|
| lanes | 2 | 3 | **2** |
| covered | 41/47 | 41/47 | **43/47** |
| oversize quarantine | 0 | 3 | **1** (gitlab, CPU only) |
| unpriced | 6 | 3 | **3** |
| all-47 dev CPU | 4916m | 5191m | **3941m** |
| dev lane CPU | 1981m | 2006m | **1081m** (budget 2500m) |
| all-47 image floor | 74.5Gi | 74.26Gi | **74.26Gi** |
| unmeasurable images | 15 | 5 | **5** |

### The claim that did not survive

#14163 said the dev CPU floor is what took 3 lanes to 2. **On this tree that is false.**
Re-measured here, `lane-partition.ts --rung dev`, margin 0.85:

| floor | lanes | covered | busiest lane CPU | of 2125m |
|---|---|---|---|---|
| OFF | 2 | 43/47 | 1656m | 77.9% |
| 50m | 2 | 43/47 | 1506m | 70.9% |
| 45m | 2 | 43/47 | 1421m | 66.9% |
| **25m (taken)** | 2 | 43/47 | **1081m** | **50.9%** |
| 10m | 2 | 43/47 | 826m | 38.9% |

**The lane count does not move at any floor.** The 3→2 collapse and 41→43 are bought by the
disk unlock plus #14194 making raw in-repo manifests rung-reachable (gmod 1000m → 100m at
`dev`). What the floor actually buys on this tree is **headroom** — 77.9% → 50.9% of the
busiest lane's CPU budget. That is what Aaron asked for and it is worth having; it is a
smaller claim than the one that was written, and the smaller one is the true one.

**The deltas were not added, they were re-run.** 5191 − 1250 = 3941 happens to check out;
2006 − 1250 = 756 does **not**, because only −925m of the floor lands inside the 38
Applications the dev root applies. The measured lane is **1081m**.

---

## Does `gitlab` fit now? No — but half the problem cleared

#14174 made `gitlab` measurable. At 14 GiB it was oversize on **both** axes
(cpu 2525m > 2125m; disk 11.53Gi > 8.50Gi). At 70 the **disk half clears** — 11.53Gi against
a 56.10Gi lane — so gitlab is now oversize **on CPU alone**.

It still does not fit, and **the dev CPU floor cannot reach it**: `gitlab` has no
`resourceClaims` row at all. It is glob-excluded from the dev lane and carried in
`ungovernedRequests`, so a floor over governed rows leaves 2525m untouched. Governing it is a
separate decision with a separate blast radius and is not taken here.

## The giants still pull together, and the composition barely moved

`hindsight` and `vllm` are both in **lane-1** — the composition the hosted run verified
(11m33s; `hindsight-api` and `vllm-openai` pulled together in one lane; 47,051 MiB against a
56.1 GiB budget). Lane-1 went 21 apps / 41 images / 56.08Gi → **20 apps / 40 images /
56.07Gi**. The swap is size-neutral to a mebibyte: out `external-secrets` + `headscale`
(0.222Gi), in `weaviate` (0.221Gi).

**Stated rather than glossed:** lane-1's *estimate* sits **26.3 MiB under** a 56.10 GiB
budget — 99.95% — and it did in #14163 too. The estimate is the ×2.67 image-expansion model
this catalogue documents as high; the one real pull came in at 47,051 MiB. **The lane job on
this PR is the check, not this paragraph.**

## `metal` is byte-identical — verified three ways

- `storage-profiles.ts --resource-profile metal --verify` → exit 0, "manifests match resource profile metal".
- **Catalogue arithmetic:** all 34 rows' `cpuMillis.metal` equal `origin/main`'s; metal dev-lane total **5231m / 13475Mi**, unmoved. `memoryMib` unmoved on all 34 rows at *both* rungs.
- **Full re-render:** `rendered-resource-requests.ts --measure`; the snapshot's `metal` profile serialises byte-identical to `origin/main`'s. **Not vacuous** — the same comparison returns `false` for `dev`, so it discriminates.

## Why the disk total moved, and why it is not a size change

Unmeasurable images went **15 → 5**. The all-47 total went 74.5 → 74.26 GiB by moving in
*both* directions: #14174 + #14233 made 8 images measurable (up, to 75.81), then #14250 took
the private FlowDent charts out of the public tree (back down, to 74.26). **What changed is
how much of the tree is counted, not how big it is.**

**It is still a FLOOR.** 5 images remain unmeasurable and "the whole tree fits inside 77" is
**not** claimed, then or now.

## Conflicts resolved by re-measuring, not by picking a side

Seven files conflicted. `lane-partition.test.ts` was the real one — both sides edited the same
pinned totals. Resolved by keeping #14163's **bound-derived** generalisation (the budget is
derived from `envelope.freeDiskGib`, so the body passes at 14 *and* at 70) and keeping
#14174's `gitlab` pins as **sizes**. Its `toEqual(["gitlab","hindsight","vllm"])` oversize
**verdict** is dropped, because the biconditional loop below it already decides membership
from the declared bound — and a hardcoded verdict list is exactly what froze this file at 14
in the first place. The prose conflicts took `main`'s side (#14194's gmod-reachability account
is the tree's current truth) with the arithmetic re-pinned to measurement.

`game-hosting/gmod` is left at the **100m #14194 chose for it** and is *not* floored to 25m.
Extending the floor to a row governed *after* the floor was authorized would be **extending**
an authorization, not inheriting one.

**Rebased twice.** The first pass landed on `9a2888c20`; `main` then moved 7 commits, one of
which (#14250) edits this very tree. Everything above was re-measured on `abdca04a8` rather
than pushed against the base it was measured on — a check that did not run must not be
reported as one that passed.

882/882 cluster tests pass. `reason-truth.ts` exits 0 over 52 citations. No `--admin`, no
widened check.

---

## On the signature — two different things are being signed

Aaron's quotes reached me **relayed** by the dispatching agent, verbatim and attributed, not
read by me from a chat I observed. `Human-Review: explicit` / `Human-Review-Evidence: chat` is
asserted on that basis **and no other**, and the quotes are reproduced in the artifact and
above so the one person who can falsify them is looking straight at them.

**That covers the two levers.** It does **not** cover the rebase: the re-measurement, the
withdrawn lane claim, and the conflict resolutions are mine under standing authority, and
Aaron has not seen them. No gated class was touched — no budget increase, no WONT-DO, no
`--admin`. The force-update is the pre-authorized case (own branch, own single commit, zero
reviews, lease pinned to the exact prior SHA `146845204`).

`human-review-evidence.ts` reports this claim as **UNVERIFIABLE (outside the forge)**, which
is correct and is the honest state: `chat` names evidence no query can confirm or refute.

Agency-Signature-Version: 1
Agent: otto
Agent-Runtime: claude-code/background-session
Agent-Model: claude-opus-5
Credential-Identity: AceHack
Credential-Mode: shared
Human-Review: explicit
Human-Review-Evidence: chat
Action-Mode: human-directed
Task: take-the-70-unlock-giants

Co-authored-by: otto[bot] <otto[bot]@users.noreply.github.com>


## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 0 |
| Resolved threads | 0 |
| Unresolved threads | 0 |
| Total review comments | 0 |
| Total fix commits (touching thread paths) | 0 |

## Review threads

(no review threads on this PR)

## Fix commits (touching thread paths)

(no commits in this PR touched files anchored by review threads)
