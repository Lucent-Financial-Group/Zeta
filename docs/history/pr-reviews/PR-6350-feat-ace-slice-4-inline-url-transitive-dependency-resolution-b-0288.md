# PR #6350 -- feat(ace): slice 4 — inline-URL transitive dependency resolution (081KR2E4K0008QG0R002YE3MMD)

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 6350 |
| Title | feat(ace): slice 4 — inline-URL transitive dependency resolution (081KR2E4K0008QG0R002YE3MMD) |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-06-01T05:56:06Z |
| Merged at | 2026-06-01T06:16:39Z |
| Merge commit SHA | `233e96def0c6595916adae902d31433ebd55d93d` |
| Branch | `otto-windows/ace-slice4-impl-2026-06-01` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/6350 |
| Changed files | 8 |
| Additions / deletions | +1299 / -15 |

## Description

Implements Ace slice 4 per the merged spec (`docs/agendas/ace-package-manager/2026-06-01-ace-cli-slice4-inline-url-dependency-resolution-design.md`) and plan (`...-slice4-implementation-plan.md`), TDD, 12 commits.

## What lands
- **`tools/ace/resolve.ts`** (new, pure, injected fetch): `packageHash` (full-package identity) + `resolve()` — identity-keyed DFS, root-seeded, diamond dedup, cycle/version-skew/tamper detection, per-node verification (slice-2 files hash + `package_hash` pin + declared-identity + slice-3 signature gate, graph-wide `--allow-no-signature`), topo order (leaves-first, root-last).
- **`tools/ace/store.ts`**: `AceManifest.dependencies?` + `AceDependency`; `validatePackagePaths` factored out of `installPackage` (shared with the install preflight).
- **`tools/ace/ace.ts`**: resolver wired into `install` — resolve → **atomic preflight** (content_hash + path-safety + store-collision across the whole graph) → extract leaves-first → resolved-set output. Leaf packages unchanged (back-compat).
- **`.claude/skills/ace/SKILL.md`**: transitive-install docs + the 12 refusal reasons.

## Verification
- **101 tests pass, 0 fail** (`bun test tools/ace/`).
- Final opus code-review pass: 2 P1s found + fixed (root content_hash preflight closing the D6 atomicity gap; `invalid-package` shape-guard so malformed dep JSON refuses cleanly instead of throwing) + a P2 test + a nit comment.

## Scope
Inline-URL only. Registry + semver = slice 5. Store-collision is **refused** (strict); re-keying the store by identity is deferred to **081KT07NV0008QG0R001HKPJW6** (only if same-files-different-identity ever arises). Commit canary 67 throughout.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | true |
| Total threads | 5 |
| Resolved threads | 5 |
| Unresolved threads | 0 |
| Total review comments | 10 |
| Total fix commits (touching thread paths) | 9 |

## Review threads

### Thread 1 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GBHD4`
- **Path**: `tools/ace/ace.ts`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-06-01T05:58:38Z on `tools/ace/ace.ts`:410 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Verify the root package before resolving dependencies**

  When a graph root has a signed/trusted manifest whose `content_hash` does not match `files` (or an unsigned root installed with `--allow-no-signature`), this calls `resolve` and fetches/reads every dependency before the later preflight notices the bad root hash. That gives an invalid root package transitive network/file side effects that leaf installs do not have, and it contradicts the slice-4 design's ordering to verify the root's own integrity before resolution; move the root content-hash/path check ahead of `resolve`.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-06-01T06:13:53Z on `tools/ace/ace.ts`:410 (association: MEMBER)

    <pre>
    Fixed in 78216a572: the graph branch now verifies the root's content_hash BEFORE calling resolve(), so a bad-root-hash refuses immediately without fetching the whole dependency graph. The per-node preflight content_hash check stays as defense-in-depth.
    </pre>

### Thread 2 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GBJQY`
- **Path**: `tools/ace/store.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-01T06:02:03Z on `tools/ace/store.ts`:100 (association: NONE)

  <pre>
  P0: `validatePackagePaths` does not reject Windows drive-absolute paths like `C:\Windows\...`. On Windows, `path.join(storeDir, "C:\\...")` discards the store prefix and writes outside the store, defeating the atomic preflight and enabling arbitrary file writes.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-06-01T06:13:47Z on `tools/ace/store.ts`:100 (association: MEMBER)

    <pre>
    Fixed in 78216a572 (P0): validatePackagePaths now also rejects Windows drive paths via /^[A-Za-z]:/ — catches both drive-absolute (C:\Windows\...) and drive-relative (C:foo). Without it path.join(store, 'C:\...') discarded the store prefix → escape. Red→green tests added.
    </pre>

### Thread 3 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GBJQ5`
- **Path**: `tools/ace/store.test.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-01T06:02:04Z on `tools/ace/store.test.ts`:125 (association: NONE)

  <pre>
  Add coverage for Windows drive-absolute paths (e.g. `C:\\Windows\\system.ini`). Without this, `validatePackagePaths` can regress on Windows while POSIX/UNC cases still pass.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-06-01T06:13:50Z on `tools/ace/store.test.ts`:125 (association: MEMBER)

    <pre>
    Done in 78216a572: added validatePackagePaths tests for C:\Windows\system.ini (drive-absolute) and C:evil (drive-relative).
    </pre>

### Thread 4 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GBJRG`
- **Path**: `tools/ace/resolve.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-01T06:02:04Z on `tools/ace/resolve.ts`:46 (association: NONE)

  <pre>
  `resolve()` iterates `node.manifest.dependencies ?? []` assuming it's an array. If a fetched package has `dependencies` present but not an array (e.g. `{}`), the `for...of` will throw, bypassing the intended `invalid-package` refusal path.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-06-01T06:13:55Z on `tools/ace/resolve.ts`:46 (association: MEMBER)

    <pre>
    Fixed in 78216a572: the invalid-package shape guard now also rejects a non-array 'dependencies' (returns invalid-package), and the for-of is made defensive (Array.isArray ? : []) so it can never throw. Red→green test added (dependencies: {}).
    </pre>

### Thread 5 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GBJRQ`
- **Path**: `tools/ace/ace.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-01T06:02:04Z on `tools/ace/ace.ts`:403 (association: NONE)

  <pre>
  `u.startsWith("http")` will misclassify local paths that happen to start with `http` (e.g. `http-pkgs/A.json`) and attempt a network fetch. Prefer an explicit `http://` / `https://` check for dependency URLs.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-06-01T06:13:58Z on `tools/ace/ace.ts`:403 (association: MEMBER)

    <pre>
    Fixed in 78216a572: dep-URL classification uses explicit u.startsWith('http://') || u.startsWith('https://') so local paths like http-pkgs/A.json are read as files, not network-fetched.
    </pre>

## Fix commits (touching thread paths)

### `c5a6dc53d4627866dcd730d6121517a274b3fb8a` -- 2026-06-01T05:10:43Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/ace/store.test.ts`
- `tools/ace/store.ts`

**Message:**

```
feat(ace): AceManifest.dependencies? + AceDependency (slice 4 task 1)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `35ae46cf596f0657e8ece11f4de8bcc2364f228c` -- 2026-06-01T05:15:40Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/ace/store.test.ts`
- `tools/ace/store.ts`

**Message:**

```
refactor(ace): factor validatePackagePaths out of installPackage (sli…

…ce 4 task 2)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `11a70f340669f75f59a286e35c08528d3f6a0b49` -- 2026-06-01T05:17:50Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/ace/resolve.ts`

**Message:**

```
feat(ace): packageHash full-package identity helper (slice 4 task 3)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `a52a5d1758b9b125149baba6f25cc1e2a9ce9868` -- 2026-06-01T05:20:52Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/ace/resolve.ts`

**Message:**

```
feat(ace): resolve leaf + linear-chain topo order (slice 4 task 4)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `059e8e09d55c2cbb7efcf03f2f657d2daea69840` -- 2026-06-01T05:23:36Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/ace/resolve.ts`

**Message:**

```
feat(ace): resolve diamond dedup + identity-keyed checks (slice 4 tas…

…k 5)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `a91836e1c81f1d45e5fab5a21c7d46183e74c738` -- 2026-06-01T05:31:22Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/ace/resolve.ts`

**Message:**

```
feat(ace): resolve per-node hash/pin/identity/signature gates (slice …

…4 task 8)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `01e542446b8075293490ba5e3d7097eef8f9dfc6` -- 2026-06-01T05:37:50Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/ace/ace.ts`

**Message:**

```
feat(ace): wire resolver into install — graph preflight + atomic (sli…

…ce 4 task 9)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `39971297d23381efa6d5fc83f34a896dbacb7cfd` -- 2026-06-01T05:55:10Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/ace/ace.ts`
- `tools/ace/resolve.ts`

**Message:**

```
fix(ace): slice-4 review — root content_hash preflight + invalid-pack…

…age guard (P1+P2)

Final-review P1s: (1) graph preflight now content_hash-verifies EVERY node incl. the
root before any extraction (closes the D6 atomicity gap where a tampered-root graph
orphaned already-extracted leaves); (2) resolve() shape-validates fetched JSON and
returns invalid-package instead of throwing on parseable-but-malformed packages.
Plus a P2 test (bad/untrusted sig refuses even with --allow-no-signature) and a
root-skew->cycle precedence comment.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `78216a572a65599e992dc96de8f8fd16a433e478` -- 2026-06-01T06:13:05Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/ace/ace.ts`
- `tools/ace/resolve.ts`
- `tools/ace/store.test.ts`
- `tools/ace/store.ts`

**Message:**

```
fix(ace): slice-4 PR review — Windows drive-path escape (P0) + early …

…root check + non-array deps guard + http:// classify

Copilot P0: validatePackagePaths now rejects Windows drive paths (C:\... and C:foo) —
path.join would otherwise discard the store prefix and escape. Codex P2: verify root
content_hash before resolve (no wasted graph fetch on a bad root). Non-array dependencies
now refuse invalid-package instead of throwing (shape guard + defensive for-of). Dep-URL
classification uses explicit http://|https:// so local paths starting with 'http' aren't
network-fetched.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```
