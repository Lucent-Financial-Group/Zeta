# PR #15437 -- research(math): the second E8 route is a count, not a construction — route table for six in-tree roads to E8

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 15437 |
| Title | research(math): the second E8 route is a count, not a construction — route table for six in-tree roads to E8 |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-08-25T21:03:36Z |
| Merged at | 2026-08-25T21:27:56Z |
| Merge commit SHA | `2adfc78556e2607c47c8237d82fe74bf71ef051b` |
| Branch | `soraya/e8-route-table-triage` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/15437 |
| Changed files | 3 |
| Additions / deletions | +448 / -0 |

## Description

Triage-then-harden over a conversational thread whose claims sat at wildly different
levels of rigour. Four claims, four different answers. Several needed no formal
treatment and that is stated as the finding, not hidden.

## Item 2 first — CONFIRMED, at a corrected path

The reported path `tests/Tests.FSharp/Formal/CssCode.Tests.fs` **does not exist**. The file
is `tests/Tests.FSharp/CssCode.Tests.fs`, one directory up. At **that** file, **line 122**,
the quote is verbatim accurate — content and line number both. Nothing downstream needs
retracting on this account. The enclosing test also *computes* the family
(`(8,8,1) … (8,0,4)`), so the sentence has a machine-checked witness, not only prose.

## The docstring under review is literally true and its implicature is false

`src/Core/CliffordPeriodicity.fs:179` — all three clauses are true. `Λ²(ℝⁿ)` is a grade-2
**subspace**; no ideal is divided out. What does **not** follow is the inference the
placement invites:

| operation | map | how homoiconicity dies |
|---|---|---|
| quotient (Construction A) | surjective, `ker ≠ 0` | distinct algebra elements collapse onto one vertex |
| subspace (bivector) | injective, `image ≠ A` | 248 of 65536 basis elements represented; the rest absent |

The coded route loses **injectivity**; the bivector route loses **surjectivity**. Neither is
the bijection homoiconicity requires.

## NEW — the two defects are EQUAL

`defect := dim A / dim M` is `2^(N/2)` for **both** the maximally-coded adinkra and the
half-spinor route, at every `N ≡ 0 (mod 8)`. Two lines of semisimplicity: the even
subalgebra halves `dim A`, halves the irrep dimension, and doubles the block count — the
halvings cancel. Witnessed at `N ∈ {8,16,24,32}` with exact `BigInt` arithmetic
(16/256/4096/65536 on both sides). **Honest limit:** the prose proof carries the general
case; four points are a witness, not a proof for all `N`.

## NEW — and this is the only item that needs a tool

`so(16) ⊕ Δ⁺ = e₈` is in-tree as a **count**. Explicit-target search over `src/` and
`tests/` finds no bracket. `E8LieAlgebra.fs` contains none of `so(16)`/`so16`/`spinor`/`128`
— it builds `e₈` from the root system, downstream of Construction A.

- **Named competitor with the identical `120 + 128 = 248`:** the Inönü–Wigner graded
  contraction `so(16) ⋉ Δ⁺` with `[Δ⁺,Δ⁺] = 0`. Same dimension, same split, same action.
  Not isomorphic to `e₈`.
- **Excluding invariant:** nonzero bracket `Λ²Δ⁺ → so(16)` / nondegenerate Killing form.
  Exactly what the in-tree assertion does not check.
- The anchor (Adams) is correct; the **artefact** is the gap. Route is `unmetered`.
- Same gap one rung down at `f4FromSpinors = (9, 52)`.

Work item `081M0XBP381087G0R001C2Q9W7` minted with the scope and the instrument.

## Aaron's classical/quantum cut — wrong axis, right instinct

The dimension count is substrate-independent (it survives complexification). But CSS spends
the code **twice** — once on `X`, once on `Z` — so the codespace defect is `2^(2k)` against
the adinkra's `2^k`, computable from the in-tree table (`K = N − 2k`, matching all five
rows). And the operation is a **subquotient** `N(S)/S` inside one Pauli algebra, not a
module quotient. Partially upheld, on a corrected axis.

## Aaron's reframe UPHELD; the trade framing retracted

"Both adinkras can be true at once" is right, and the repo's own raw-vault / anti-Babel
doctrine already says so. The exclusive-or framing is retracted and replaced by a six-row
ledger — R0 uncoded · R1 Construction A · R2 bivector/spinor · R3 icosian golden doubling ·
R4 versor orbit · R5 blade-mask bridge · R6 CSS — each with its categorical operation, what
it preserves, what it costs, its measured defect, and what it transfers to.

Two readings that bite: **R4 and R5 are not independent of R1** (both start in R1's integer
frame); only **R3** is. And per **PR #15424** rank-8 agreement is **forced by uniqueness**,
so any BP-16 cross-check that wants to mean something must run at **rank 16**.

## Instruments refused, with the wrong-tool cost named

- **TLA+** — no state machine, no temporal property. A green model over an invented
  transition system is **false green in CI** plus a human-week. Most likely mis-route,
  because it is the tool the fleet is most fluent in.
- **Lean 4** — weeks of Clifford-periodicity formalisation to certify a two-line dimension
  count. Filed as low-priority, not started.
- **Z3** — no satisfiability question; `2^n` invites nonlinear-arithmetic incompleteness for
  zero benefit.
- **Alloy** — no relational structure to search.
- **FsCheck** — every space here is exhaustible (`C(248,3)` triples, 902 length-8 codes) or
  better served by four chosen points. Sampling is strictly weaker than the trivial loop.
- **A quantum simulator** on item 3 — nothing there is a circuit; it would answer a
  different question.

## Riffing that stays labelled riffing

"the door out of the coded tower" · "one clock, three rungs" (correct, inert as support) ·
"two faces of the same object" (do not cite as a coded↔uncoded bridge until the bracket
exists) · the homoiconicity/protection exclusive-or · `f₄ = so(9) ⊕ Δ₉`.

## Not touched

Item 5 (CFGs as initial algebras) is another agent's — not re-derived; one transferable
question offered. Item 6 (PR #15424 MERGED, #15423 OPEN — both verified) used, not redone.

## Verification honesty

`docs/research/2026-*-*.md` is in the `.markdownlint-cli2.jsonc` **ignores** list
(line 151). A `markdownlint` rc=0 on this file would be **a check that did not run**, so no
lint claim is made. Docs + one work item only; no source or test files touched, so no build
surface is affected.

## AgencySignature
```
Agency-Signature-Version: 1
Agent: Soraya
Agent-Runtime: claude-code
Agent-Model: claude-opus-5
Credential-Identity: AceHack
Credential-Mode: shared
Human-Review: not-implied-by-credential
Human-Review-Evidence: none
Action-Mode: autonomous-fail-closed
Task: 081M0XBP381087G0R001C2Q9W7
```


## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 0 |
| Resolved threads | 0 |
| Unresolved threads | 0 |
| Total review comments | 0 |
| Total fix commits (touching thread paths) | 0 |

## Review threads

(no review threads on this PR)

## Fix commits (touching thread paths)

(no commits in this PR touched files anchored by review threads)
