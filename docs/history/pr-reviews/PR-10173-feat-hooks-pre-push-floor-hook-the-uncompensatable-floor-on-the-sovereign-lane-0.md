# PR #10173 -- feat(hooks): pre-push floor hook — the uncompensatable floor on the sovereign lane (081KZHGP46G)

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 10173 |
| Title | feat(hooks): pre-push floor hook — the uncompensatable floor on the sovereign lane (081KZHGP46G) |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-08-08T23:08:02Z |
| Merged at | 2026-08-08T23:24:12Z |
| Merge commit SHA | `6265f4f78b762b11184375f7bcfce8a0240a816b` |
| Branch | `otto/pre-push-floor-hook` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/10173 |
| Changed files | 10 |
| Additions / deletions | +213 / -9 |

## Description

Executes **Lior's ratification note**: "a floor the fast lane can skip is a comment, not a contract." The gate enforces the floor on the PR lane; `scripts/hooks/pre-push` is the same floor at the moment of direct push — scoped to the **outgoing diff only** (pre-existing repo drift never blocks; the ADR's own discipline).

**Blocks:** secret-shaped added lines (no override — erasure class); the two `.semgrep-floor.yml` supply-chain patterns ported to portable ERE; un-acked changes to byte-lock vector contracts (`ZETA_FLOOR_VECTORS_ACK=1` after running the oracles = deliberate change allowed, silent breakage impossible — Lior's exact hostage scenario closed); non-ff pushes to main. HC-9 + scoped-build/test stay CI-side by nature. `--no-verify` is git's un-forbiddable bypass — documented as the emergency hatch, and using it on a floor class is a treaty matter.

**Install:** linked beside `commit-msg` by `install-git-hooks.sh` (same three declarative paths: install.sh / nix develop / ACE realizer). README restructured + healed markdownlint-clean.

**Verified live** in a scratch remote pair: 7 scenarios — clean push ✓, secret ✗, mutable tag ✗, untrusted `run:` ✗, SHA-pinned ✓, un-acked vectors ✗ then acked ✓, forced non-ff to main ✗ (blocked before transfer).

Closes `081KZHGP46G08QG0R002SR9A4T` (completion move post-merge). Second of three ratification-note workitems; remaining: `081KZHGP45V` (P2 build-drift auto-revert healer).

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 0 |
| Resolved threads | 0 |
| Unresolved threads | 0 |
| Total review comments | 0 |
| Total fix commits (touching thread paths) | 0 |

## Review threads

(no review threads on this PR)

## Fix commits (touching thread paths)

(no commits in this PR touched files anchored by review threads)
