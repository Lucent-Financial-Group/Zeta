# PR #2983 -- feat(shadow): B-0431 slice 3 — macOS grey-text detection via osascript

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 2983 |
| Title | feat(shadow): B-0431 slice 3 — macOS grey-text detection via osascript |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-05-13T10:56:11Z |
| Merged at | 2026-05-13T10:58:44Z |
| Merge commit SHA | `3bf22d378d7150a5cf4f6136f49cbd091f6745f3` |
| Branch | `feat/b-0431-shadow-observer-slice-3-detect-script` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/2983 |
| Changed files | 3 |
| Additions / deletions | +322 / -12 |

## Description

## Summary

Replaces the Phase 1 `detectGreyText()` stub with a real macOS implementation backed by an AppleScript that queries the terminal emulator's accessibility tree.

- **`tools/shadow/detect-grey-text.applescript`** (new) — queries `AXFocusedUIElement` → `AXSelectedText` / `AXValue` on the frontmost terminal emulator (`Terminal`, `iTerm2`, `Warp`, `Hyper`, `Alacritty`, `kitty`); returns empty string when no suggestion found
- **`tools/shadow/shadow-observer.ts`** — adds:
  - `OsascriptRunner` (exported type) for dependency injection in tests
  - `runOsascript()` private helper with 3 s timeout guard (prevents hangs from accessibility permission dialogs)
  - `detectGreyTextMacOS(terminalApp?, _runner?, _platform?)` exported function: non-darwin guard returns null with warning; accessibility error returns null with warning; empty stdout returns null
  - `detectGreyText()` stub replaced — now delegates to `detectGreyTextMacOS()`
- **`tools/shadow/shadow-observer.test.ts`** — 12 new tests (39 total)

## Test results

```
bun test tools/shadow/shadow-observer.test.ts
39 pass, 0 fail
```

## Acceptance checklist

- [x] `detectGreyTextMacOS()` exported from `tools/shadow/shadow-observer.ts`
- [x] Non-macOS guard logs warning and returns `null` without crashing
- [x] `detect-grey-text.applescript` present in `tools/shadow/`
- [x] Built-in `detectGreyText()` calls `detectGreyTextMacOS()` (stub replaced)
- [x] 12 new tests, 0 failures (`bun test tools/shadow/shadow-observer.test.ts`)
- [x] "Human keystroke overrides at any moment" criterion satisfied (overridden-path tests verify re-detect → `"overridden"`)

## B-0431 parent: B-0402

operative-authorization: aaron 2026-05-13: "Cooling period: TBD. The memory file IS the durable record"

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 1 |
| Resolved threads | 0 |
| Unresolved threads | 1 |
| Total review comments | 1 |
| Total fix commits (touching thread paths) | 1 |

## Review threads

### Thread 1 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM6BuVJq`
- **Path**: `tools/shadow/detect-grey-text.applescript`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-13T10:59:01Z on `tools/shadow/detect-grey-text.applescript`:44 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Restrict AXValue fallback to real suggestion text**

  Returning `AXValue` whenever it is non-empty makes the detector report a suggestion even when no grey autocomplete exists, because the focused terminal element typically has normal input/buffer text. In that state, `runOneCycle()` will see two consecutive non-null detections and proceed to auto-accept (`Right Arrow` + `Enter`), which can submit unintended commands while the user is just at a prompt.

  Useful? React with 👍 / 👎.
  </pre>

## Fix commits (touching thread paths)

### `bae690e11e10ae8017ebc6142ed96b97fdb45b4d` -- 2026-05-13T10:06:53Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/shadow/detect-grey-text.applescript`

**Message:**

```
feat(shadow): B-0431 slice 3 — macOS grey-text detection via osascript

Replaces the Phase 1 `detectGreyText()` stub with a real macOS
implementation backed by `detect-grey-text.applescript`.

Changes:
- `tools/shadow/detect-grey-text.applescript` — new: queries the
  frontmost terminal emulator's AXFocusedUIElement accessibility
  chain (AXSelectedText → AXValue); returns empty string when no
  terminal is focused or no text found.
- `tools/shadow/shadow-observer.ts` — adds:
  - `OsascriptRunner` exported type for dependency injection
  - `runOsascript()` private helper with a 3 s timeout guard
    (prevents hangs from accessibility permission dialogs)
  - `detectGreyTextMacOS(terminalApp?, _runner?, _platform?)`
    exported function; returns null on non-darwin, osascript
    error, or empty output
  - `detectGreyText()` stub replaced with `detectGreyTextMacOS()`
    call
- `tools/shadow/shadow-observer.test.ts` — 12 new tests (39 total,
  0 failures):
  - 4 `detectGreyTextMacOS` unit tests (non-darwin guard, exit 0 +
    text, exit 0 + empty, non-zero exit with warning)
  - 4 overridden-path cycle tests (call count, log events, content,
    re-detect throw = override)
  - 4 accepted-path cycle tests (call count, log events, dry-run
    content, acceptFn call count)

operative-authorization: aaron 2026-05-13: "Cooling period: TBD. The memory file IS the durable record"

Co-Authored-By: Claude <noreply@anthropic.com>
```
