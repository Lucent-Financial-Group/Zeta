# PR #571 -- feat(hygiene): tools/hygiene/check-archive-header-section33.sh — §33 archive header lint + B-0036 backfill backlog

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 571 |
| Title | feat(hygiene): tools/hygiene/check-archive-header-section33.sh — §33 archive header lint + B-0036 backfill backlog |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-04-26T08:08:00Z |
| Merged at | 2026-04-26T08:19:39Z |
| Merge commit SHA | `1b270b7431aa9e32cd1a8d45b818040fc3f815ea` |
| Branch | `feat/check-archive-header-section33-lint-otto-346-pattern` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/571 |
| Changed files | 2 |
| Additions / deletions | +255 / -0 |

## Description

## Summary

GOVERNANCE.md §33 archive-header missing was the **most-common review finding** across the 11-Amara-refinement courier-ferry lineage this session (PRs #560 / #562 / #563 / #565 / #566 / #568 / #569 / #570 / #553 each retrofitted post-review).

Per Otto-346 (recurring pattern → substrate primitive missing) + Otto-341 (mechanism over vigilance), the fix is a CI lint.

## What this PR ships

1. **`tools/hygiene/check-archive-header-section33.sh`** — the lint tool
2. **`docs/backlog/P3/B-0036-...md`** — backlog row tracking the two follow-up sub-tasks (backfill + CI wire)

## What this PR does NOT ship

- Does NOT wire the lint to CI yet (would fail on 26 pre-existing violations)
- Does NOT auto-fix the 26 pre-existing violations (B-0036 Sub-task 1)
- Does NOT enforce §33 on docs OUTSIDE `docs/research/**`

## Tool behavior

- Scans `docs/research/**.md` for courier-ferry / external-conversation imports (filename or content patterns)
- Validates first-20-lines contains all 4 §33 labels in literal form
- **Bold-styled `**Scope**:` form REJECTED** per #570 P0 finding
- Reports first violation with diagnostic
- Exits non-zero on any violation

## Smoke-test result

Found **26 pre-existing violations** on `main` — confirms the substrate-debt is real. Backfill is the dedicated PR scope (B-0036 Sub-task 1).

## Composition

- **Otto-346** (recurring inline pattern = signal substrate primitive missing): this PR IS Otto-346 in action
- **Otto-341** (mechanism over vigilance): goal is CI-enforced, not each-agent-remembering
- **Otto-229** template: same shape as `check-tick-history-order.sh` (recurring discipline-violation → CI lint as fix)
- **Otto-238** (retractability; visible reversal): backfill PR (next) preserves per-doc lineage; CI enforcement (after) prevents future violations

## Test plan

- [x] Tool smoke-tested on `main` — finds 26 pre-existing violations (expected)
- [x] Tool exit-code is 1 on violation, 0 on clean
- [x] Filter correctly identifies courier-ferry imports (`courier-ferry`, `via Aaron`, `external conversation` etc.)
- [x] Bold-styled labels rejected (per #570 P0 finding pattern)
- [ ] B-0036 Sub-task 1: backfill PR brings violation count to 0
- [ ] B-0036 Sub-task 2: wire to gate.yml after backfill lands

## Why P3

- Not blocking any current PR merge
- The lint tool exists; CI enforcement is the structural improvement
- Backfill can be batched into a single dedicated PR

## Examples for backfill

See PR #570 / #566 / #563 §33-header fixes for the canonical 4-field header form.

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | true |
| Total threads | 10 |
| Resolved threads | 6 |
| Unresolved threads | 4 |
| Total review comments | 16 |
| Total fix commits (touching thread paths) | 4 |

## Review threads

### Thread 1 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59pKlb`
- **Path**: `tools/hygiene/check-archive-header-section33.sh`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-04-26T08:10:01Z on `tools/hygiene/check-archive-header-section33.sh`:96 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Recurse through docs/research when scanning markdown**

  The file loop only uses `"$RESEARCH_DIR"/*.md`, so the lint ignores markdown files in nested paths even though the script header says it validates `docs/research/**`. This is already a real gap because `docs/research/claims/` exists today, and any courier-ferry/external-conversation doc added under a subdirectory would bypass this check and pass CI once the job is wired. Please switch file discovery to a recursive walk so the enforcement scope matches the documented scope.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-26T08:13:20Z on `tools/hygiene/check-archive-header-section33.sh`:96 (association: MEMBER)

    <pre>
    Fixed in b2091d9: replaced single-level glob `$RESEARCH_DIR/*.md` with recursive walk via `find -type f -name '*.md' -print0` piped through `while read -d ''` for null-terminated path safety. Now matches the documented `docs/research/**` scope.

    Smoke-test on main: lint finds **36 violations** with recursive walk (was 26 with single-level glob), confirming `docs/research/claims/` subdirectory paths are now scanned correctly. The 10 newly-flagged docs are pre-existing courier-ferry imports that were silently passing under the original glob — exactly the gap your finding identified.

    The B-0036 backfill scope is updated to reflect 36 docs (will re-run when picking up the row).

    Resolving.
    </pre>

### Thread 2 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59pK9D`
- **Path**: `tools/hygiene/check-archive-header-section33.sh`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-26T08:11:43Z on `tools/hygiene/check-archive-header-section33.sh`:83 (association: NONE)

  <pre>
  P0: The content-signal regex includes very broad terms like `chatgpt`, which will classify many internal research docs as "external conversation imports" just because they mention ChatGPT (e.g., `docs/research/meta-pixel-perfect-text-to-image-youtube-wink-2026-04-22.md` mentions ChatGPT Images 2.0). This likely creates false positives and inflates the violation count. Tighten the matcher to courier-ferry–specific markers (e.g., `courier[- ]ferry`, `via .* courier[- ]ferry`, explicit "external conversation" headers) instead of generic LLM brand names.

  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-26T08:17:09Z on `tools/hygiene/check-archive-header-section33.sh`:83 (association: MEMBER)

    <pre>
    Fixed in 11e9528:

    - **P0 — content-signal too broad** (Copilot): narrowed regex to structural phrases only (`courier.ferry`, `external conversation`, `external collaborator`, `external research agent`, `courier-ferry capture`). Mere mentions of `chatgpt` / `google search ai` no longer trigger. Lint now flags 19 docs (was 36 with broad regex) — verified 17 false positives removed; 19 remaining are real courier-ferry imports per manual inspection. Also tightened scan window to first-20 lines (was 200).
    - **P1 — name-attribution in code patterns** (Copilot): replaced name-strings (`via Aaron`/`amara-via`/`aaron-share`) with structural role-ref patterns (`courier-ferry|cross-substrate|external-import|cross-ferry`). No personal names in either filename or content matching now.
    - **P2 — docstring vs behavior** (Copilot): rewrote docstring to accurately describe multi-violation reporting + summary with explicit rationale (agents fix-all-at-once vs running lint repeatedly).
    - **P1 — B-0036 broken composes_with path** (Copilot): replaced `feedback_otto_229_*.md` (personal memory, not in-repo) with `GOVERNANCE.md-section-33-archive-header-discipline` + `tools/hygiene/check-tick-history-order.sh`. Body Otto-229 conceptual references retained as discipline-citations, not file paths.
    - **P1 — recursive walk** (Copilot, duplicate of Codex's earlier P2): already shipped in commit b2091d9.

    Resolving.
    </pre>

### Thread 3 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59pK9G`
- **Path**: `tools/hygiene/check-archive-header-section33.sh`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-26T08:11:43Z on `tools/hygiene/check-archive-header-section33.sh`:97 (association: NONE)

  <pre>
  P1: The script claims to scan `docs/research/**.md`, but the loop only matches `"$RESEARCH_DIR"/*.md`, so it will miss nested paths (e.g. `docs/research/claims/*.md` exists). Switch to a recursive file enumeration (e.g., `find` / `globstar`) so the lint actually covers `docs/research/**` as described.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-26T08:17:11Z on `tools/hygiene/check-archive-header-section33.sh`:97 (association: MEMBER)

    <pre>
    Fixed in 11e9528:

    - **P0 — content-signal too broad** (Copilot): narrowed regex to structural phrases only (`courier.ferry`, `external conversation`, `external collaborator`, `external research agent`, `courier-ferry capture`). Mere mentions of `chatgpt` / `google search ai` no longer trigger. Lint now flags 19 docs (was 36 with broad regex) — verified 17 false positives removed; 19 remaining are real courier-ferry imports per manual inspection. Also tightened scan window to first-20 lines (was 200).
    - **P1 — name-attribution in code patterns** (Copilot): replaced name-strings (`via Aaron`/`amara-via`/`aaron-share`) with structural role-ref patterns (`courier-ferry|cross-substrate|external-import|cross-ferry`). No personal names in either filename or content matching now.
    - **P2 — docstring vs behavior** (Copilot): rewrote docstring to accurately describe multi-violation reporting + summary with explicit rationale (agents fix-all-at-once vs running lint repeatedly).
    - **P1 — B-0036 broken composes_with path** (Copilot): replaced `feedback_otto_229_*.md` (personal memory, not in-repo) with `GOVERNANCE.md-section-33-archive-header-discipline` + `tools/hygiene/check-tick-history-order.sh`. Body Otto-229 conceptual references retained as discipline-citations, not file paths.
    - **P1 — recursive walk** (Copilot, duplicate of Codex's earlier P2): already shipped in commit b2091d9.

    Resolving.
    </pre>

### Thread 4 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59pK9I`
- **Path**: `tools/hygiene/check-archive-header-section33.sh`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-26T08:11:44Z on `tools/hygiene/check-archive-header-section33.sh`:35 (association: NONE)

  <pre>
  P2: Comment says "Reports the first failing file" but the implementation reports every violating file (prints a VIOLATION line per file). Either stop after the first violation (to match the contract) or update the header comment / PR description to reflect multi-violation reporting.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-26T08:17:12Z on `tools/hygiene/check-archive-header-section33.sh`:35 (association: MEMBER)

    <pre>
    Fixed in 11e9528:

    - **P0 — content-signal too broad** (Copilot): narrowed regex to structural phrases only (`courier.ferry`, `external conversation`, `external collaborator`, `external research agent`, `courier-ferry capture`). Mere mentions of `chatgpt` / `google search ai` no longer trigger. Lint now flags 19 docs (was 36 with broad regex) — verified 17 false positives removed; 19 remaining are real courier-ferry imports per manual inspection. Also tightened scan window to first-20 lines (was 200).
    - **P1 — name-attribution in code patterns** (Copilot): replaced name-strings (`via Aaron`/`amara-via`/`aaron-share`) with structural role-ref patterns (`courier-ferry|cross-substrate|external-import|cross-ferry`). No personal names in either filename or content matching now.
    - **P2 — docstring vs behavior** (Copilot): rewrote docstring to accurately describe multi-violation reporting + summary with explicit rationale (agents fix-all-at-once vs running lint repeatedly).
    - **P1 — B-0036 broken composes_with path** (Copilot): replaced `feedback_otto_229_*.md` (personal memory, not in-repo) with `GOVERNANCE.md-section-33-archive-header-discipline` + `tools/hygiene/check-tick-history-order.sh`. Body Otto-229 conceptual references retained as discipline-citations, not file paths.
    - **P1 — recursive walk** (Copilot, duplicate of Codex's earlier P2): already shipped in commit b2091d9.

    Resolving.
    </pre>

### Thread 5 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59pK9M`
- **Path**: `tools/hygiene/check-archive-header-section33.sh`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-26T08:11:44Z on `tools/hygiene/check-archive-header-section33.sh`:43 (association: NONE)

  <pre>
  P1: This tool embeds contributor/persona first-names in current-state code/comments (e.g., "via Aaron" / "Amara" / "aaron-share"). Per the repo's "No name attribution in code, docs, or skills" rule, prefer role-refs in prose and use name-agnostic patterns in code (e.g., a generic `via &lt;ProperName&gt; courier-ferry` regex rather than a hard-coded name). See `docs/AGENT-BEST-PRACTICES.md:284-313`.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-26T08:17:14Z on `tools/hygiene/check-archive-header-section33.sh`:43 (association: MEMBER)

    <pre>
    Fixed in 11e9528:

    - **P0 — content-signal too broad** (Copilot): narrowed regex to structural phrases only (`courier.ferry`, `external conversation`, `external collaborator`, `external research agent`, `courier-ferry capture`). Mere mentions of `chatgpt` / `google search ai` no longer trigger. Lint now flags 19 docs (was 36 with broad regex) — verified 17 false positives removed; 19 remaining are real courier-ferry imports per manual inspection. Also tightened scan window to first-20 lines (was 200).
    - **P1 — name-attribution in code patterns** (Copilot): replaced name-strings (`via Aaron`/`amara-via`/`aaron-share`) with structural role-ref patterns (`courier-ferry|cross-substrate|external-import|cross-ferry`). No personal names in either filename or content matching now.
    - **P2 — docstring vs behavior** (Copilot): rewrote docstring to accurately describe multi-violation reporting + summary with explicit rationale (agents fix-all-at-once vs running lint repeatedly).
    - **P1 — B-0036 broken composes_with path** (Copilot): replaced `feedback_otto_229_*.md` (personal memory, not in-repo) with `GOVERNANCE.md-section-33-archive-header-discipline` + `tools/hygiene/check-tick-history-order.sh`. Body Otto-229 conceptual references retained as discipline-citations, not file paths.
    - **P1 — recursive walk** (Copilot, duplicate of Codex's earlier P2): already shipped in commit b2091d9.

    Resolving.
    </pre>

### Thread 6 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59pK9X`
- **Path**: `docs/backlog/P3/B-0036-section33-archive-header-backfill-and-ci-wire-otto-346-pattern.md`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-26T08:11:44Z on `docs/backlog/P3/B-0036-section33-archive-header-backfill-and-ci-wire-otto-346-pattern.md`:11 (association: NONE)

  <pre>
  P1: `composes_with` references `feedback_otto_229_tick_history_append_only_never_edit_prior_rows_otto_229_2026_04_24.md`, but that file does not appear to exist under `memory/` (repo-wide search finds references but no file). Either add the missing memory artifact or change `composes_with` to point at an existing canonical reference for Otto-229 (or drop it).

  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-26T08:17:16Z on `docs/backlog/P3/B-0036-section33-archive-header-backfill-and-ci-wire-otto-346-pattern.md`:11 (association: MEMBER)

    <pre>
    Fixed in 11e9528:

    - **P0 — content-signal too broad** (Copilot): narrowed regex to structural phrases only (`courier.ferry`, `external conversation`, `external collaborator`, `external research agent`, `courier-ferry capture`). Mere mentions of `chatgpt` / `google search ai` no longer trigger. Lint now flags 19 docs (was 36 with broad regex) — verified 17 false positives removed; 19 remaining are real courier-ferry imports per manual inspection. Also tightened scan window to first-20 lines (was 200).
    - **P1 — name-attribution in code patterns** (Copilot): replaced name-strings (`via Aaron`/`amara-via`/`aaron-share`) with structural role-ref patterns (`courier-ferry|cross-substrate|external-import|cross-ferry`). No personal names in either filename or content matching now.
    - **P2 — docstring vs behavior** (Copilot): rewrote docstring to accurately describe multi-violation reporting + summary with explicit rationale (agents fix-all-at-once vs running lint repeatedly).
    - **P1 — B-0036 broken composes_with path** (Copilot): replaced `feedback_otto_229_*.md` (personal memory, not in-repo) with `GOVERNANCE.md-section-33-archive-header-discipline` + `tools/hygiene/check-tick-history-order.sh`. Body Otto-229 conceptual references retained as discipline-citations, not file paths.
    - **P1 — recursive walk** (Copilot, duplicate of Codex's earlier P2): already shipped in commit b2091d9.

    Resolving.
    </pre>

### Thread 7 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM59pMkJ`
- **Path**: `tools/hygiene/check-archive-header-section33.sh`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-26T08:20:04Z on `tools/hygiene/check-archive-header-section33.sh`:91 (association: NONE)

  <pre>
  P0: `[[ "$file" =~ courier-ferry|cross-substrate|external-import|cross-ferry ]]` is parsed by bash as multiple tokens because of the ungrouped `|`, which causes a `[[ ... ]]` syntax error at runtime. Wrap the alternation in a single regex token (e.g., group it) or assign the pattern to a variable and use `[[ "$file" =~ $pattern ]]`.

  </pre>

### Thread 8 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM59pMkN`
- **Path**: `tools/hygiene/check-archive-header-section33.sh`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-26T08:20:04Z on `tools/hygiene/check-archive-header-section33.sh`:108 (association: NONE)

  <pre>
  P0: `violation_files` is accumulated but never used, which will trigger ShellCheck SC2034 (and CI runs `shellcheck --severity=style` over `tools/**/*.sh`). Either remove `violation_files` entirely or use it (e.g., include the first/summary path list in the failure output).
  </pre>

### Thread 9 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM59pMkR`
- **Path**: `tools/hygiene/check-archive-header-section33.sh`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-26T08:20:04Z on `tools/hygiene/check-archive-header-section33.sh`:34 (association: NONE)

  <pre>
  P1: The PR description says the tool "Reports first violation", but the script explicitly reports every violating file (see the multi-violation rationale here). Please update the PR description to match the actual behavior, or change the script to stop after the first violation if that’s the intended contract.
  </pre>

### Thread 10 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM59pMz2`
- **Path**: `tools/hygiene/check-archive-header-section33.sh`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-04-26T08:21:05Z on `tools/hygiene/check-archive-header-section33.sh`:124 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Match §33 labels as raw line prefixes**

  The current check uses `grep -qF "$label"` against the header block, so any line that merely contains the substring (for example `**Scope:**` or prose/code snippets containing `Scope:`) is accepted. This contradicts the stated requirement that labels be in literal non-bold form and lets formatting drift bypass the lint, so a file can pass CI while still violating the intended §33 header shape.

  Useful? React with 👍 / 👎.
  </pre>

## Fix commits (touching thread paths)

### `f47eee016e0b37a93090fbfa0e8028e38a894a09` -- 2026-04-26T08:07:39Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `docs/backlog/P3/B-0036-section33-archive-header-backfill-and-ci-wire-otto-346-pattern.md`
- `tools/hygiene/check-archive-header-section33.sh`

**Message:**

```
feat(hygiene): tools/hygiene/check-archive-header-section33.sh — §33 …

…archive header lint + B-0036 backfill backlog

Otto-346 substrate-primitive shape: GOVERNANCE.md §33 archive-header
missing was the most-common review finding across the 11-Amara-
refinement courier-ferry lineage this session (PRs #560/#562/#563/
#565/#566/#568/#569/#570/#553 each retrofitted post-review).

Recurring identical review-finding pattern = signal that the discipline
lacks automated enforcement. Per Otto-346 (recurring inline pattern →
substrate primitive missing) + Otto-341 (mechanism over vigilance), the
fix is a CI lint that catches the violation pre-merge.

This commit ships the lint TOOL (not yet wired to CI) + a B-0036 backlog
row for the two sequential follow-ups (backfill 26 pre-existing docs +
wire to CI gate.yml).

Tool behavior:
- Scans docs/research/**.md for courier-ferry/external-conversation
  imports (filename or content patterns)
- Validates first-20-lines contains all 4 §33 labels in literal form:
  Scope: / Attribution: / Operational status: / Non-fusion disclaimer:
- Bold-styled (**Scope**:) form rejected per #570 P0 finding
- Reports first violation with diagnostic
- Exits non-zero on any violation

Smoke-test on main found 26 pre-existing violations — confirms the
substrate-debt is real and the lint catches it. Backfill is owed via
B-0036 Sub-task 1; CI wiring is owed via Sub-task 2 (after backfill
clears the residual).

Composes with:
- check-tick-history-order.sh (same pattern: structural-prevention via
  lint, not vigilance; that lint emerged from the same Otto-346 shape
  for the row-ordering bug)
- audit-md032-plus-linestart.sh (sibling md-lint hygiene tool)
- Otto-229 (recurring discipline violation → CI lint as fix)
- Otto-238 (visible reversal not silent fix; backfill preserves
  per-doc lineage)

Tool is standalone; not yet wired to CI gate.yml. Sub-task 2 of B-0036
covers the wiring after Sub-task 1's backfill PR clears the residual.
```

### `f3af2757078de7472802179e9db537d148881c08` -- 2026-04-26T08:10:32Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/hygiene/check-archive-header-section33.sh`

**Message:**

```
fix(check-archive-header-section33): SC2295 — quote REPO_ROOT inside …

…parameter expansion (shellcheck)

ShellCheck SC2295 caught: '${file#$REPO_ROOT/}' has the unquoted
$REPO_ROOT/ inside the parameter expansion, which would be treated as
a glob pattern. Right fix: '${file#"$REPO_ROOT/"}' — quoting forces
literal-string match.

This is the bash-pattern-quoting discipline; relevant when REPO_ROOT
could theoretically contain glob metacharacters (rare in practice but
correct-by-default).
```

### `b2091d98b71023a4a6a37c160995c0b102e9bbd4` -- 2026-04-26T08:12:51Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/hygiene/check-archive-header-section33.sh`

**Message:**

```
fix(check-archive-header-section33): recursive walk via 'find' (Codex…

… P2)

Codex P2: original loop used '$RESEARCH_DIR/*.md' (single-level glob),
but the script's documented scope is 'docs/research/**' (recursive).
docs/research/claims/ exists today and any courier-ferry doc placed
in a subdirectory would bypass the lint.

Fix: replaced shopt-glob loop with 'find -type f -name *.md -print0'
piped via 'while read -d ""' for null-terminated path safety.
Now matches the documented scope.

Smoke-test on main: lint now finds 36 violations (was 26 with the
single-level glob), confirming subdirectories are scanned. Includes
docs/research/claims/ subdirectory paths in the discovery.

Composes with prior Codex P2 fix (SC2295 quote in pattern expansion)
to keep this lint shellcheck-clean as it ships.
```

### `11e9528a5aee999a56b8366cd466f94414d15e8c` -- 2026-04-26T08:16:49Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `docs/backlog/P3/B-0036-section33-archive-header-backfill-and-ci-wire-otto-346-pattern.md`
- `tools/hygiene/check-archive-header-section33.sh`

**Message:**

```
fix(check-archive-header-section33): 4 review findings — narrow conte…

…nt regex + role-ref filename patterns + accurate docstring + B-0036 composes_with cleanup

P0 (Copilot) — content-signal regex was too broad (matched 'chatgpt' /
'google search ai' alone), false-positive on internal research docs
that merely mention external systems. Lint flagged 36 docs (10 of which
were false positives).

  Fix: narrowed content-signal regex to STRUCTURAL phrases only —
  'courier.ferry', 'external conversation', 'external collaborator',
  'external research agent', 'courier-ferry capture'. Mere mentions
  of system names ('chatgpt', 'google search ai') no longer trigger.
  Lint now flags 19 docs (was 36) — confirms 17 false positives were
  removed; the 19 remaining are real courier-ferry imports per
  manual inspection.

  Also tightened scan window to first-20 lines (was first-200) — the
  §33 header region is the only relevant scope.

P1 (Copilot) — code embedded contributor first-names in filename and
content patterns ('via Aaron' / 'amara-via' / 'aaron-share') per the
'No name attribution in code, docs, or skills' rule.

  Fix: replaced name-strings with structural role-ref patterns —
  filename: 'courier-ferry|cross-substrate|external-import|cross-ferry';
  content: structural phrases only. Lint now uses no personal names
  in either filename or content matching.

P1 (Copilot) — 'reports the first failing file' docstring did not
match the implementation (which reports every violating file).

  Fix: rewrote docstring to accurately describe multi-violation
  reporting + summary, with explicit rationale (agents fix-all-at-once
  instead of running lint repeatedly).

P1 (Copilot) — B-0036 composes_with referenced
'feedback_otto_229_tick_history_append_only_*' which is in personal
memory, not in-repo memory/.

  Fix: replaced with 'GOVERNANCE.md-section-33-archive-header-discipline'
  (the actual rule it composes with) + 'tools/hygiene/check-tick-history-
  order.sh' (the in-repo template). Body still references Otto-229
  conceptually as a discipline; that's not a broken-path concern.

P1 (Copilot, duplicate of Codex P2 already fixed in b2091d9) —
recursive walk via 'find -print0' instead of single-level glob.
Already shipped; this commit acknowledges the duplicate finding.
```
