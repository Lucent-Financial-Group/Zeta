# PR #6650 -- feat(ace): slice 8.4 — index-signature trust-core module + cross-language golden vectors

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 6650 |
| Title | feat(ace): slice 8.4 — index-signature trust-core module + cross-language golden vectors |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-06-03T09:39:50Z |
| Merged at | 2026-06-03T09:54:19Z |
| Merge commit SHA | `54b1fd77f1351bb1c873e3f2829b66e51e80bdb9` |
| Branch | `otto-windows/ace-slice8.4-index-signature-spec-2026-06-03` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/6650 |
| Changed files | 16 |
| Additions / deletions | +565 / -95 |

## Description

## Slice 8.4 — index-signature (extract + cross-language golden vectors)

Fifth step of the cross-language Ace trust core (after slice 8 SHA-256, 8.1 canonical-JSON, 8.2 package_hash, 8.3 ed25519). The signed-registry-index is the registry's authenticity root: a consumer accepts a registry's package list only if the index was signed by the registry's pinned key over its canonical bytes. That sign/verify primitive already worked + was unit-tested but had **no cross-language golden vectors** — so a future non-TS Ace had no contract to match for index verification. This slice gives index-signature a named module + the golden-vector contract.

Spec: `docs/agendas/ace-package-manager/2026-06-03-ace-slice8.4-index-signature-design.md` (in this PR).

### T1 — extract + sweep (move-only, behaviour-preserving)

- **NEW `tools/ace/index-signature.ts`** — the four index symbols (`IndexSignableContent`, `canonicalIndexBytes`, `signIndex`, `verifyIndexSignature`). One-way dep on `signing.ts` (shared `keyId`/`AceSignature`/`VerifyResult`/`TrustEntry`/`RevocationMap`) + `canonical.ts` (`canonicalBytes`) + `store.ts` (`RegistryEntry`). **No import cycle.**
- **NEW `tools/ace/index-signature.test.ts`** — index unit tests relocated from `signing.test.ts`: round-trip + 4 reason rejections (tampered-content / tampered-sig / untrusted-key / unsupported-algo).
- `signing.ts` — four index symbols removed (manifest sign/verify + shared primitives stay); dropped now-unused `RegistryEntry` import.
- Swept every import site (`ace.ts` / `registry-publish.ts` / `registry-remote.ts` / `registry-revoke.ts` + their `.test.ts` + `signing.test.ts`) to `./index-signature.ts`; split imports where a file used both.

### T2 — golden-vector fixture (`tests/cross-verification/index-signature/`)

- `vectors.json` — **canonical[3]** (minimal / with-packages / with-revoked+quarantined, each with `expected_canonical_json`) · **envelope[1]** (fixed PEM → `expected_key_id=ed25519:0dbe5a2385bc2af4` + `expected_sig`) · **invalid[4]** (tampered-content + tampered-sig → `bad-signature`; untrusted-key; unsupported-algo).
- `cross-verify.ts` — TS oracle asserts every vector, writes `ts-output.json`, exits non-zero on mismatch. **Auto-discovered by `tools/ci/cross-verify-all.ts` (now 6/6)** — byte-lock CI-enforced the moment this lands.
- Envelope `sig` + `key_id` + canonical bytes **independently re-derived via Python `cryptography`** at fixture-creation (non-tautological, assert-don't-skip).

### Canonical-gate status (operator 2026-06-03)
Per the canonical-primitive gate (proof **and** 4-language byte-lock): index-signature stays **TS-only + proof-owed → NOT canonical yet**. The golden vectors are the *data the eventual 4-language byte-lock + proof run on*; the proof half lives in the formal-coverage / lean-proof lane.

### Gates (all green locally)

- `bun --bun tsc --noEmit -p tsconfig.json` → exit 0
- `bun test tools/ace/` → **372 pass / 14 files**
- `bun tools/ci/cross-verify-all.ts` → **6/6 primitives** (incl. the new `index-signature` dir)
- all 16 files pure-LF · markdownlint clean · extraction is move-only (no semantics change)

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 4 |
| Resolved threads | 4 |
| Unresolved threads | 0 |
| Total review comments | 4 |
| Total fix commits (touching thread paths) | 2 |

## Review threads

### Thread 1 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6Gsp4S`
- **Path**: `tests/cross-verification/index-signature/vectors.json`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-06-03T09:42:49Z on `tests/cross-verification/index-signature/vectors.json`:33 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Use v2 for marked index vectors**

  When the `with-marks` vectors include `revoked`/`quarantined`, they need `format_version: 2`; the consumer parser explicitly rejects marked v1 indexes (`parseIndex` returns `index revoked map requires format_version 2` / quarantined equivalent). Because `cross-verify.ts` signs/verifies the raw content without parsing it, CI now byte-locks a signature and canonical JSON for an index shape that Ace itself cannot consume, so future non-TS implementations can match the fixture while still failing on real marked registry indexes.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 2 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6Gsqjo`
- **Path**: `tests/cross-verification/index-signature/vectors.json`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-03T09:43:28Z on `tests/cross-verification/index-signature/vectors.json`:35 (association: NONE)

  <pre>
  P1: This vector includes `revoked`/`quarantined` marks but sets `format_version` to 1. In `tools/ace/registry-remote.ts`, `parseIndex` rejects any index carrying `revoked`/`quarantined` unless `format_version` is 2, so this golden vector is describing an index shape the implementation treats as invalid. To keep the cross-language contract aligned with the real index format, update this vector (and derived expected canonical JSON / signature outputs) to use `format_version: 2` when marks are present.
  </pre>

### Thread 3 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GsqkI`
- **Path**: `tests/cross-verification/index-signature/vectors.json`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-03T09:43:29Z on `tests/cross-verification/index-signature/vectors.json`:71 (association: NONE)

  <pre>
  P1: This envelope vector signs content that includes `revoked`/`quarantined` but sets `format_version` to 1. The runtime parser (`tools/ace/registry-remote.ts` `parseIndex`) requires `format_version: 2` when either marks map is present, so the signed payload here doesn't match a valid index document shape. Please bump `format_version` to 2 and recompute `expected_canonical_json` / `expected_sig` (and `ts-output.json`) so the fixture matches the real format.
  </pre>

### Thread 4 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6Gsqks`
- **Path**: `docs/agendas/ace-package-manager/2026-06-03-ace-slice8.4-index-signature-design.md`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-03T09:43:29Z on `docs/agendas/ace-package-manager/2026-06-03-ace-slice8.4-index-signature-design.md`:52 (association: NONE)

  <pre>
  The spec calls for a vector with `revoked`/`quarantined` marks, but the actual index format gates marks behind `format_version: 2` (`parseIndex` rejects marks on v1). It would help to explicitly state that the marked vectors should use `format_version: 2` so the golden vectors reflect a valid on-wire index shape.
  </pre>

## Fix commits (touching thread paths)

### `146c9e37f92df970521823494f22126dd74c23f6` -- 2026-06-03T09:39:24Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `docs/agendas/ace-package-manager/2026-06-03-ace-slice8.4-index-signature-design.md`
- `tests/cross-verification/index-signature/vectors.json`

**Message:**

```
feat(ace): slice 8.4 — extract index-signature trust-core module + cr…

…oss-language golden vectors

Extracts the signed-registry-index sign/verify trio (IndexSignableContent,
canonicalIndexBytes, signIndex, verifyIndexSignature) out of signing.ts into a
dedicated trust-core module tools/ace/index-signature.ts (sibling of canonical.ts
8.1 + package-hash.ts 8.2), and anchors it with a cross-language golden-vector
fixture so a future Rust/F#/C# Ace verifies a signed index byte-identically.

T1 — extract + sweep (move-only, behaviour-preserving):
  - NEW tools/ace/index-signature.ts: the four index symbols; one-way dep on
    signing.ts (keyId/AceSignature/VerifyResult/TrustEntry/RevocationMap) +
    canonical.ts (canonicalBytes) + store.ts (RegistryEntry). No cycle.
  - NEW tools/ace/index-signature.test.ts: index sign/verify unit tests relocated
    from signing.test.ts (round-trip + 4 reason rejections: tampered-content /
    tampered-sig / untrusted-key / unsupported-algo).
  - signing.ts: four index symbols removed (manifest sign/verify + shared
    primitives stay); dropped now-unused RegistryEntry import.
  - Swept every import site (ace.ts / registry-publish.ts / registry-remote.ts /
    registry-revoke.ts + their .test.ts + signing.test.ts) to ./index-signature.ts;
    split imports where a file uses both index + manifest symbols.

T2 — golden-vector fixture (tests/cross-verification/index-signature/):
  - vectors.json: canonical[3] (minimal / with-packages / with-revoked+quarantined,
    each w/ expected_canonical_json) · envelope[1] (fixed PEM →
    expected_key_id=ed25519:0dbe5a2385bc2af4 + expected_sig) · invalid[4]
    (tampered-content + tampered-sig → bad-signature; untrusted-key; unsupported-algo).
  - cross-verify.ts: TS oracle asserts every vector, writes ts-output.json, exits
    non-zero on mismatch. Auto-discovered by tools/ci/cross-verify-all.ts (now 6/6).
  - Envelope sig + key_id + canonical bytes independently re-derived via Python
    `cryptography` at fixture-creation (non-tautological, assert-don't-skip).

Canonical-gate status (operator 2026-06-03): index-signature stays TS-only +
proof-owed → NOT canonical yet. The golden vectors are the data the eventual
4-language byte-lock + proof run on; proof half = formal-coverage / lean-proof lane.

Gates: tsc 0 · bun test tools/ace/ 372 pass / 14 files · cross-verify-all 6/6 ·
all files pure-LF · markdownlint clean · move-only (no semantics change).

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `9ed4a4fa066fde426d21f617f6ae7a5f2a5c0751` -- 2026-06-03T09:50:23Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `docs/agendas/ace-package-manager/2026-06-03-ace-slice8.4-index-signature-design.md`
- `tests/cross-verification/index-signature/vectors.json`

**Message:**

```
fix(ace): slice 8.4 — mark-bearing index vectors must be format_versi…

…on 2

Review (Codex P2 + Copilot P1×2 + doc note): the with-marks golden vectors set
format_version: 1 while carrying revoked/quarantined, but parseIndex
(registry-remote.ts:64,68) rejects marks on a v1 index ("index revoked/quarantined
map requires format_version 2"). The fixture was byte-locking an index shape Ace
itself cannot consume — a future non-TS Ace matching it would produce a doc the real
parser rejects.

Fix: bump the two mark-bearing vectors (canonical with-marks + envelope with-marks) to
format_version: 2. Because format_version is part of the signed canonical content, the
signed-content-dependent fields are re-derived: expected_canonical_json (both),
expected_sig (envelope), and ts-output.json (oracle-regenerated). key_id is
content-independent → unchanged (ed25519:0dbe5a2385bc2af4). The no-mark vectors
(minimal / with-packages / all invalid) stay v1 — valid.

Independent re-derivation (non-tautology preserved): Python `cryptography` reproduces
the new envelope sig over the v2 canonical bytes AND the key_id from raw SPKI-DER —
both match the fixture. Spec doc updated to state marked vectors use format_version: 2.

Gates: tsc 0 · bun test tools/ace/ 372 pass · cross-verify-all 6/6 · all files pure-LF
· markdownlint clean.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```
