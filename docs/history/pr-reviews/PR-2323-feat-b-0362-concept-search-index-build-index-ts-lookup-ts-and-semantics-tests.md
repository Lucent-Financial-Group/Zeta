# PR #2323 -- feat(B-0362): concept search index — build-index.ts, lookup.ts, AND semantics, tests

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 2323 |
| Title | feat(B-0362): concept search index — build-index.ts, lookup.ts, AND semantics, tests |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-05-09T17:53:42Z |
| Merged at | 2026-05-09T18:20:41Z |
| Merge commit SHA | `98d2791ad1b005f2e5068e031e1c7932499fcddb` |
| Branch | `claim/b0362-concept-search-index-2026-05-09` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/2323 |
| Changed files | 4 |
| Additions / deletions | +192 / -47 |

## Description

## Summary

- **`tools/search/build-index.ts`** — new entry point; pre-builds `.concept-index.json` (gitignored) from memory/, docs/, .claude/skills/, .claude/rules/, .claude/agents/
- **`tools/search/lookup.ts`** — new entry point with multi-word AND semantics (`bun tools/search/lookup.ts alignment clause`)
- **`tools/search/concept-index.ts`** — rewrote `buildIndex()` to use parallel `Promise.all(readFile)` calls; 10.7s → 2.47s on 2943 files; fixed `lookup()` single-substring → `words.every(AND)` semantics; exported types
- **`tools/search/concept-index.test.ts`** — 14 tests (Bun test runner): structure, timing gate, single-word, AND semantics

## Acceptance criteria

| Criterion | Result |
|---|---|
| `bun tools/search/build-index.ts` < 5s | ✅ 2.47s (745 entries, 1506KB, 10268 hits) |
| `bun tools/search/lookup.ts <term>` < 100ms | ✅ 0.5–0.7ms |
| Covers memory/, docs/, .claude/skills/, .claude/rules/ | ✅ + .claude/agents/ |
| Multi-word AND semantics | ✅ `"alignment clause"` → 21 ⊆ `"alignment"` → 275 |

## Test plan

- [x] `bun test tools/search/concept-index.test.ts` — 14 pass, 0 fail
- [x] `dotnet build -c Release` on clean tree — 0 warnings, 0 errors (TS-only changes; no F# touched)
- [x] Manual `bun tools/search/lookup.ts Otto` — 275 matches, 0.7ms
- [x] Manual `bun tools/search/lookup.ts alignment clause` — 21 matches, AND ⊆ verified

## Depends on

B-0310 (concept-registry) — CONCEPT_QUERIES follow the same concept-class vocabulary.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | true |
| Total threads | 11 |
| Resolved threads | 11 |
| Unresolved threads | 0 |
| Total review comments | 18 |
| Total fix commits (touching thread paths) | 4 |

## Review threads

### Thread 1 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6A1vgY`
- **Path**: `tools/search/concept-index.test.ts`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-09T17:56:17Z on `tools/search/concept-index.test.ts`:50 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Avoid hard 5s wall-clock assertion in tests**

  This assertion makes the correctness test suite depend on host performance rather than functional behavior: on slower CI runners, busy shared machines, or larger repositories, `buildIndex()` can legitimately exceed 5s and fail even when the index is correct. Because this gate is in normal tests (not a dedicated benchmark), it can create flaky red builds and block unrelated changes; consider moving the timing check to a benchmark/perf job or using a much looser/non-blocking threshold.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-09T18:03:10Z on `tools/search/concept-index.test.ts`:50 (association: MEMBER)

    <pre>
    Fixed in efc81d5d: removed the 5s wall-clock timing describe block entirely. Timing assertions belong in benchmarks, not in correctness test suites — they're fragile across CI runners and don't test behavior.
    </pre>

### Thread 2 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6A1wD7`
- **Path**: `tools/search/concept-index.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-09T17:57:31Z on `tools/search/concept-index.ts`:89 (association: NONE)

  <pre>
  buildIndex() reads all scanned files with a single Promise.all and keeps every file's full content in memory (fileContents) before processing. This scales memory usage with total corpus size and can become a reliability problem as docs/memory grow; consider streaming/processing files incrementally and/or using bounded concurrency (worker pool) so you don't materialize the whole corpus at once.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-09T18:03:11Z on `tools/search/concept-index.ts`:89 (association: MEMBER)

    <pre>
    Fixed in efc81d5d: replaced Promise.all(all files) with sequential per-file reads. Peak memory is now O(single file content) instead of O(total corpus). Sequential I/O is the accepted tradeoff for a tooling script.
    </pre>

### Thread 3 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6A1wEE`
- **Path**: `tools/search/concept-index.test.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-09T17:57:31Z on `tools/search/concept-index.test.ts`:36 (association: NONE)

  <pre>
  The "generated field is a valid ISO timestamp" test isn't actually validating anything: `new Date(...)` never throws, and the `&gt;= 2026` year check will become brittle (and doesn't verify ISO formatting). Prefer `Date.parse(index.generated)` and assert it's not NaN (pattern used elsewhere, e.g. tools/authorization/check-authorization.test.ts).

  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-09T18:03:12Z on `tools/search/concept-index.test.ts`:36 (association: MEMBER)

    <pre>
    Fixed in efc81d5d: replaced `new Date(index.generated).getFullYear() &gt;= 2026` with `Date.parse(index.generated)` and assert `not.toBeNaN()` — this actually validates the ISO format rather than just checking year.
    </pre>

### Thread 4 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6A1wEL`
- **Path**: `tools/search/concept-index.test.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-09T17:57:32Z on `tools/search/concept-index.test.ts`:51 (association: NONE)

  <pre>
  The "builds in under 5 seconds" timing gate is likely to be flaky across CI runners / local machines (filesystem + runner load variance). Consider removing it from unit tests, loosening it substantially, or guarding it behind an opt-in env flag so CI stays deterministic.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-09T18:03:13Z on `tools/search/concept-index.test.ts`:51 (association: MEMBER)

    <pre>
    Fixed in efc81d5d: removed the entire `buildIndex — timing` describe block. Same finding as the Codex P2 comment above — wall-clock assertions are inherently flaky in CI.
    </pre>

### Thread 5 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6A1wEP`
- **Path**: `tools/search/lookup.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-09T17:57:32Z on `tools/search/lookup.ts`:41 (association: NONE)

  <pre>
  This script executes at import time and calls `process.exit`, which makes it unsafe to import from other tools/tests. Most tools scripts in this repo expose an exported `main(argv)` and only `process.exit(...)` under `if (import.meta.main)` (e.g. tools/hygiene/append-tick-history-row.ts). Consider adopting that pattern here for consistency and testability.

  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-09T18:03:14Z on `tools/search/lookup.ts`:41 (association: MEMBER)

    <pre>
    Fixed in efc81d5d: wrapped all execution code in `if (import.meta.main)` and exported a `main(argv: string[])` function — now safe to import from tests or other tools without side effects.
    </pre>

### Thread 6 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6A1wEV`
- **Path**: `tools/search/build-index.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-09T17:57:32Z on `tools/search/build-index.ts`:25 (association: NONE)

  <pre>
  This script runs immediately at module load (top-level await) with no exported `main` and no `if (import.meta.main)` guard. For consistency with other tools/** scripts and to keep it import-safe, consider moving logic into an exported `main(argv)` and calling it only when `import.meta.main` is true.

  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-09T18:03:15Z on `tools/search/build-index.ts`:25 (association: MEMBER)

    <pre>
    Fixed in efc81d5d: moved all logic into an exported `main()` and added `if (import.meta.main) { await main(); }` guard — top-level await no longer runs at import time.
    </pre>

### Thread 7 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6A1wEZ`
- **Path**: `tools/search/concept-index.test.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-09T17:57:32Z on `tools/search/concept-index.test.ts`:28 (association: NONE)

  <pre>
  buildIndex() now scans `.claude/agents`, but the "covers required scan dirs" test doesn't assert that directory is included. Adding it would prevent accidental regression of the newly-added scan surface.

  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-09T18:03:16Z on `tools/search/concept-index.test.ts`:28 (association: MEMBER)

    <pre>
    Fixed in efc81d5d: added `expect(index.scanDirs).toContain('.claude/agents')` to the scan dirs coverage test.
    </pre>

### Thread 8 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6A10XC`
- **Path**: `tools/search/concept-index.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-09T18:06:55Z on `tools/search/concept-index.ts`:83 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Parallelize file reads in index build loop**

  `buildIndex()` still performs one `readFile` per iteration with `await` inside the `for` loop, so filesystem I/O is serialized across all scanned markdown files. For the target corpus (memory/docs/.claude trees), this keeps wall-clock time tied to cumulative per-file latency and can miss the stated sub-5s build objective on slower or busy runners; collect read promises (or use bounded concurrency) so file reads actually run in parallel before processing.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 9 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6A13Uo`
- **Path**: `tools/search/concept-index.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-09T18:12:16Z on `tools/search/concept-index.ts`:84 (association: NONE)

  <pre>
  PR description says concept-index buildIndex() uses parallel Promise.all(readFile) calls, but the current implementation reads files sequentially with `await readFile(...)` inside a for-loop. Please update the PR description (and/or any nearby comments) to match the actual behavior, or switch to bounded concurrency if parallelism is still intended.
  </pre>

### Thread 10 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6A15U7`
- **Path**: `tools/search/concept-index.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-09T18:16:29Z on `tools/search/concept-index.ts`:84 (association: NONE)

  <pre>
  P0: buildIndex() uses Promise.all over allFiles and materializes every file's full content in memory (fileContents). With ~3k markdown files this can spike memory and also hit OS file-descriptor limits (EMFILE) due to thousands of concurrent readFile calls. Prefer sequential reads or bounded concurrency (e.g., a small worker pool/semaphore) while processing each file immediately so peak memory stays O(single file).
  </pre>

### Thread 11 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6A15VI`
- **Path**: `tools/search/lookup.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-09T18:16:30Z on `tools/search/lookup.ts`:25 (association: NONE)

  <pre>
  P1: lookup.ts exports main(argv) but calls process.exit(...) inside main. This makes the exported function unsafe to call from other tools/tests (it will terminate the process) and diverges from the common tools/** pattern of returning an exit code and only calling process.exit(...) under if (import.meta.main). Consider having main return a numeric/union exit code and moving process.exit to the import.meta.main block.
  </pre>

## Fix commits (touching thread paths)

### `8f510ff13c6cbe411c182af88160e168fc05656e` -- 2026-05-09T17:53:26Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/search/build-index.ts`
- `tools/search/concept-index.test.ts`
- `tools/search/concept-index.ts`
- `tools/search/lookup.ts`

**Message:**

```
feat(B-0362): concept search index — build-index.ts, lookup.ts, AND s…

…emantics, tests

- Add tools/search/build-index.ts entry point (bun tools/search/build-index.ts)
- Add tools/search/lookup.ts entry point with multi-word AND semantics
- Rewrite buildIndex() to use parallel async readFile — 10.7s → 2.5s on 2943 files
- Fix lookup() single-substring match → words.every(AND) semantics
- Add concept-index.test.ts: 14 tests covering structure, timing, single/multi-word

Acceptance criteria met:
  build: 2.47s (< 5s)
  lookup: 0.5–0.7ms (< 100ms)
  AND: 'alignment clause' → 21 results ⊆ 'alignment' → 275 results

operative-authorization: aaron 2026-05-04: "it**, not just the output. Grinding through failures + recoveries"

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>
```

### `c26edb1cbbf70b9f0b2d82a16d9e8c6e57f37126` -- 2026-05-09T18:02:13Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/search/build-index.ts`
- `tools/search/concept-index.test.ts`
- `tools/search/concept-index.ts`
- `tools/search/lookup.ts`

**Message:**

```
fix(B-0362): address 7 Copilot/Codex review findings on concept searc…

…h index

Thread 1+4: remove flaky 5s wall-clock timing assertion from test suite —
timing assertions belong in benchmarks, not correctness tests; also drops
the unused buildElapsedMs variable.

Thread 2: switch buildIndex() from Promise.all(all files) to sequential
per-file reads — reduces peak memory from O(corpus) to O(1 file); tradeoff
is sequential I/O but acceptable for a tooling script.

Thread 3: fix timestamp validation — new Date() never throws, use
Date.parse() and assert !isNaN instead.

Thread 5: add if (import.meta.main) guard to lookup.ts and export main(argv)
so the script is safe to import from tests or other tools.

Thread 6: add if (import.meta.main) guard to build-index.ts and export
main() — removes top-level await side-effect on import.

Thread 7: add .claude/agents assertion to "covers required scan dirs" test
to prevent regression of the newly-added scan surface.

Also: remove the duplicated CLI block from concept-index.ts (now covered
by the dedicated build-index.ts and lookup.ts entry points).

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>
```

### `83e85e6998eb676cc6639f442dd7dd0dd5b06889` -- 2026-05-09T18:13:14Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/search/concept-index.ts`

**Message:**

```
fix(B-0362): parallelize file reads in buildIndex with Promise.all

Both Codex and Copilot flagged sequential readFile calls in the
for-loop — the PR description claimed parallel reads but the
implementation was sequential. Now uses Promise.all to read all
files concurrently. The corpus is bounded small markdown files
so O(corpus) peak memory is acceptable.

Co-Authored-By: Claude Opus 4.6 (1M context) <noreply@anthropic.com>
```

### `6c1bcbfcbecb410aa39d9a3ebde142bc0fd3df9b` -- 2026-05-09T18:17:50Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/search/lookup.ts`

**Message:**

```
fix(B-0362): return exit codes from lookup main instead of process.exit

Makes the exported main() import-safe for other tools/tests by
returning numeric exit codes instead of calling process.exit()
directly. process.exit is called only at the CLI entry point.

Co-Authored-By: Claude Opus 4.6 (1M context) <noreply@anthropic.com>
```
