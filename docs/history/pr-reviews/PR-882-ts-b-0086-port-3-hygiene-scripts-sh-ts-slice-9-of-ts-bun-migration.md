# PR #882 -- ts(B-0086): port 3 hygiene scripts (.sh→.ts) — slice 9 of TS/Bun migration

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 882 |
| Title | ts(B-0086): port 3 hygiene scripts (.sh→.ts) — slice 9 of TS/Bun migration |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-04-30T03:56:41Z |
| Merged at | 2026-04-30T03:58:56Z |
| Merge commit SHA | `02266a7b38c681b2da73e46103e758dba1673245` |
| Branch | `lane-b/ts-bun-slice-9-agencysignature-pair-2026-04-30` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/882 |
| Changed files | 5 |
| Additions / deletions | +1109 / -3 |

## Description

## Summary

- Slice 9 of the TS+Bun migration. Ports 3 hygiene scripts byte-equivalent against bash, with two notable bash-bug-fixes-by-port in audit-agencysignature-main-tip.
- **Agency-signature-pair cluster opened**: validate-agencysignature-pr-body (pre-merge) + audit-agencysignature-main-tip (post-merge) form the ferry-7 enforcement-instrument set per Amara.

## Files

- \`tools/hygiene/validate-agencysignature-pr-body.{sh→ts}\` — pre-merge validator for AgencySignature v1 trailer block in PR bodies (10 required keys + 5 enums + Task pattern + Human-Review consistency)
- \`tools/hygiene/audit-agencysignature-main-tip.{sh→ts}\` — post-merge auditor with 4-state classification (LEGACY / CORRECT / HUMAN-AUTHORED-EXEMPT / REGRESSION) + 5 modes (HEAD / commit / max / since / branch)
- \`tools/hygiene/capture-tick-snapshot.{sh→ts}\` — factory-state YAML/JSON pin per Amara 4th-ferry snapshot-pinning concern

Plus:

- \`docs/trajectories/typescript-bun-migration/slice-audits.md\` — slice-9 audit appended
- \`docs/trajectories/typescript-bun-migration/RESUME.md\` — slice-8-merged closure (#880) + slice-9-in-flight

## Equivalence

Per-script:

| script | result |
|---|---|
| validate-agencysignature-pr-body | byte-equivalent on PASS + FAIL paths |
| audit-agencysignature-main-tip | TS port produces correct classifications where bash silently broke (BSD date TZ-offset + exit-in-subshell bugs on macOS) — documented behaviour-improvement |
| capture-tick-snapshot | byte-equivalent on YAML + JSON modes modulo run-time fields |

## Bash-bug-fixes-by-port (audit-agencysignature-main-tip)

1. **BSD \`date -j -f '%Y-%m-%dT%H:%M:%SZ'\` does NOT handle TZ-offset** like \`-04:00\` on macOS. The TS port uses \`Date.parse\` which handles ISO-8601 with both \`Z\` and \`±HH:MM\` formats.
2. **Bash \`exit 2\` inside \`\$(classify_commit ...)\`** only exits the subshell, not the parent. The bash original silently produced unclassified output on macOS when v1-ship-date had a TZ offset. The TS port short-circuits cleanly.

Same behaviour-improvement-over-bash class as the no-empty-dirs empty-FILTERED case from slice-7.

## Lint + types

All three files pass:

- \`bun x tsc --noEmit\` clean
- \`bun x eslint\` clean (typescript-eslint strictTypeChecked + stylisticTypeChecked + sonarjs)

## Test plan

- [ ] CI: lint + typecheck on the slice files
- [ ] CI: \`bun tools/hygiene/validate-agencysignature-pr-body.ts\` integration smoke test
- [ ] CI: \`bun tools/hygiene/audit-agencysignature-main-tip.ts --max 10\` returns 0 against current main
- [ ] CI: \`bun tools/hygiene/capture-tick-snapshot.ts\` produces parseable YAML

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 6 |
| Resolved threads | 0 |
| Unresolved threads | 6 |
| Total review comments | 6 |
| Total fix commits (touching thread paths) | 3 |

## Review threads

### Thread 1 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM5-oRCR`
- **Path**: `tools/hygiene/capture-tick-snapshot.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-30T04:01:30Z on `tools/hygiene/capture-tick-snapshot.ts`:96 (association: NONE)

  <pre>
  `extractRepoName()` does not match the bash `sed -E 's|.*[:/]([^/]+/[^/]+)(\.git)?$|\1|'` semantics when the remote URL is already in `owner/repo` form (or otherwise lacks a `:` or `/` prefix before the last two segments). Bash would leave the string unchanged, but this TS implementation returns `"unknown"`, which breaks the stated drop-in equivalence and can hide the repo identifier.
  ```suggestion
    // Sed leaves the input unchanged when the pattern does not match,
    // so inputs already in `owner/repo` form must round-trip unchanged.
    // Manual extraction: take last two slash-separated segments after the
    // last `:` or `/` separator that precedes them.
    const lastSlash = url.lastIndexOf("/");
    if (lastSlash &lt;= 0) return url;
    const beforeLast = url.slice(0, lastSlash);
    const sepIdx = Math.max(
      beforeLast.lastIndexOf("/"),
      beforeLast.lastIndexOf(":"),
    );
    if (sepIdx &lt; 0) return url;
  ```
  </pre>

### Thread 2 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM5-oRCd`
- **Path**: `tools/hygiene/capture-tick-snapshot.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-30T04:01:31Z on `tools/hygiene/capture-tick-snapshot.ts`:16 (association: NONE)

  <pre>
  This file’s header comment introduces named attribution (“Amara”). `tools/**` is a current-state surface; per repo guidance it should use role-refs (e.g., “the human maintainer”) rather than persona/human names in reusable docs/comments.
  </pre>

### Thread 3 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM5-oRCn`
- **Path**: `tools/hygiene/audit-agencysignature-main-tip.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-30T04:01:31Z on `tools/hygiene/audit-agencysignature-main-tip.ts`:264 (association: NONE)

  <pre>
  `buildCommitList()` ignores `gitOutput(...).status` for the `--max` and `--since` paths. If `git log` fails (bad `--since` value, repo state, etc.), this can silently produce an empty commit list and later report success (“nothing to audit”) instead of returning an input/tooling error as the header claims (exit code 2).
  </pre>

### Thread 4 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM5-oRCq`
- **Path**: `docs/trajectories/typescript-bun-migration/slice-audits.md`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-30T04:01:31Z on `docs/trajectories/typescript-bun-migration/slice-audits.md`:446 (association: NONE)

  <pre>
  This slice audit entry uses named attribution (“Amara”) in a trajectory doc. `docs/trajectories/**` is a current-state surface; please switch to role-refs (e.g., “the human maintainer”) to match the repo’s no-name-attribution convention outside history/research surfaces.
  </pre>

### Thread 5 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM5-oRC1`
- **Path**: `docs/trajectories/typescript-bun-migration/RESUME.md`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-30T04:01:32Z on `docs/trajectories/typescript-bun-migration/RESUME.md`:6 (association: NONE)

  <pre>
  This milestone/status line includes named attribution (“per Amara ...”). `docs/trajectories/**` should use role-refs rather than names (names are reserved for the explicitly listed history/research surfaces).
  </pre>

### Thread 6 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM5-oRUq`
- **Path**: `tools/hygiene/audit-agencysignature-main-tip.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-04-30T04:02:17Z on `tools/hygiene/audit-agencysignature-main-tip.ts`:124 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Narrow parsed arg key before assigning into args object**

  `args[step.key] = step.value` uses a `keyof MutableArgs` index that includes `mode`, but `step.value` is always a plain `string`; this makes strict type-checking fail at this line (`TS2322` from `npx tsc --noEmit` in this repo). As written, the migration slice does not compile cleanly under the TypeScript gate, so the script cannot be relied on until this assignment is narrowed to non-`mode` keys (or handled in key-specific branches).

  Useful? React with 👍 / 👎.
  </pre>

## Fix commits (touching thread paths)

### `f88201f5b6dd74c723a8fea0c4af007913dd9aa7` -- 2026-04-30T03:51:26Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/hygiene/audit-agencysignature-main-tip.ts`

**Message:**

```
ts(slice-9, wip 2/N): port audit-agencysignature-main-tip (.sh→.ts)

Second script of slice 9. Post-merge auditor for the AgencySignature
Convention v1 trailer block. Pairs with validate-agencysignature-pr-body
(slice-9 wip 1/N) as the ferry-7 enforcement-instrument set.

**Behaviour improvement over bash**: the bash original has two macOS-
specific bugs that the TS port fixes:

1. BSD `date -j -f '%Y-%m-%dT%H:%M:%SZ'` does NOT handle TZ-offset
   format like `2026-04-28T19:36:23-04:00`; it fails to parse and the
   ship-ts cache is left empty. The TS port uses `Date.parse` which
   handles ISO-8601 with both Z and ±HH:MM timezone formats.
2. Bash `exit 2` inside a function called via `$(classify_commit ...)`
   command substitution only exits the subshell, not the parent. So
   the ship-date parse error printed to stderr but the audit silently
   continued with empty status classifications. The TS port returns
   null from classifyCommit on parse failure and the caller short-
   circuits with exit 2 cleanly.

Both bugs are documented in slice-9 audit substrate (when it lands)
under "Equivalence audit" — same behaviour-improvement-over-bash
class as the no-empty-dirs empty-FILTERED case.

Mechanical changes:
- bash arg loop with case statement + 5 multi-arg flags consolidated
  into requiresNext spec table + classifyArg helper to keep
  parseArgs cognitive-complexity under sonarjs threshold
- bash `git log --pretty='%(trailers)'` per commit preserved
  verbatim (real git command; TS shells out same as bash)
- bash detect_v1_ship pipeline (oldest-first iteration with grep
  for trailer match) preserved as detectV1Ship() with V1_TRAILER_RE
- bash 5 modes (head / commit / max / since / branch) preserved as
  Mode literal-union + buildCommitList helper
- bash printf format strings preserved verbatim (`%-22s` for status
  badge, `%cI`/`%cI`/`%s` for date/sha/subject formatting, etc.)
- All FAIL message text + Spec citations preserved verbatim

Lint-clean (eslint + sonarjs + tsc).
```

### `59ee725a7f44dafb8b79a76c5ecd74790f2d02f8` -- 2026-04-30T03:54:28Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/hygiene/capture-tick-snapshot.ts`

**Message:**

```
ts(slice-9, wip 3/N): port capture-tick-snapshot (.sh→.ts)

Third script of slice 9. YAML/JSON snapshot emitter for tick-open /
tick-close pinning per Amara's 4th-ferry concern.

Byte-equivalent against bash original modulo run-time fields
(date_utc + claude_cli_version which differ by invocation).

Mechanical changes:
- bash safe_sha → safeSha helper using git hash-object
- bash safe_bytes → safeBytes using statSync.size (avoids wc -c
  shell-out)
- bash claude --version subprocess → spawnSync('claude', [...])
- bash sed regex .*[:/]([^/]+/[^/]+)(.git)? → manual extractRepoName
  helper (last-two-segments after sep) — preserves bash bug of
  retaining .git suffix for drop-in equivalence; future cleanup
  could strip it after downstream verifies
- YAML + JSON emit blocks preserved verbatim including all field
  ordering, comments ("# Agent fills these from session context:"
  etc.), and null-literal placeholders

Lint-clean (eslint + sonarjs + tsc).
```

### `76b459562bd12bf3b93feb42f652197ca754a871` -- 2026-04-30T03:56:11Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `docs/trajectories/typescript-bun-migration/RESUME.md`
- `docs/trajectories/typescript-bun-migration/slice-audits.md`

**Message:**

```
trajectory(ts-bun): slice 9 audit substrate + RESUME tracker

- slice-audits.md: append slice-9 audit (3 ports — agency-signature
  pair + capture-tick-snapshot). Two bash-bug-fixes documented in
  audit-agencysignature-main-tip (BSD date TZ-offset + exit-in-subshell).
- RESUME.md: bump slice-7→slice-8-merged status (#880, commit 988de70).
  Milestone 25 → 28. Bucket B reduced 19 → 16.
```
