# PR #2973 -- feat(B-0402): shadow observer slice 1 — polling loop, --once flag, 16 tests

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 2973 |
| Title | feat(B-0402): shadow observer slice 1 — polling loop, --once flag, 16 tests |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-05-13T07:59:24Z |
| Merged at | 2026-05-13T08:02:01Z |
| Merge commit SHA | `7ffb1763369f7aff99e6c609d53406ec0e4ed417` |
| Branch | `feat/b-0402-shadow-observer-slice-1-polling-loop-tests` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/2973 |
| Changed files | 3 |
| Additions / deletions | +465 / -118 |

## Description

## Summary

- Refactors `tools/shadow/shadow-observer.ts` Phase 1 stub into a testable polling loop infrastructure
- Adds `tools/shadow/shadow-observer.test.ts` with 16 tests, 0 failures (no OS permissions required)
- Updates B-0402 backlog item with pre-start checklist and slice decomposition

## What changed

**`tools/shadow/shadow-observer.ts`** (refactored):
- `appendFileSync` replaces `Bun.write(..., { append: true } as never)` type-cast workaround
- `--once` flag: run one detection cycle then exit cleanly (enables CI-safe testing)
- `--loop-interval <ms>` flag: configures sleep between continuous-mode cycles (default 1000)
- `runOneCycle(config, detectFn, acceptFn)` exported with injected fns for unit testing
- Full polling loop structure: detect → wait delay → re-detect → accept/override/no-suggestion
- Validate `--delay` and `--loop-interval`; exit 1 on NaN or negative value
- Exports: `ShadowConfig`, `ShadowEvent`, `DetectFn`, `AcceptFn`

**`tools/shadow/shadow-observer.test.ts`** (new, 16 tests):
- CLI arg validation: `--delay abc`, `--delay -5`, `--loop-interval abc`, unknown flag → exit 1
- Dry-run once mode: exits 0, writes `started` + `no-suggestion` events to log, stdout is valid JSON lines
- `runOneCycle` unit with injected fns: no-suggestion / accepted / overridden / detectFn-throws / acceptFn-false
- `log()` unit: appends multiple events to file, tolerates non-writable path without throwing

**`docs/backlog/P0/B-0402-...md`** (pre-start checklist + acceptance-criteria ticks):
- Prior-art search across shadow/outlet/bus tooling documented
- Slice 1/2/3 scope decomposed

## Checks

```
bun test tools/shadow/shadow-observer.test.ts
  16 pass, 0 fail — 333ms

dotnet build -c Release
  Build succeeded. 0 Warning(s), 0 Error(s)
```

## Acceptance criteria status

| Criterion | Status |
|---|---|
| Configurable delay (default 3s) | ✅ `--delay <ms>` with validation |
| Glass Halo logging with `(shadow)` attribution | ✅ `appendFileSync`-based, all events |
| Human keystroke override | ⏳ Slice 2 — requires empirical grey-text detection |
| `zeta shadow` CLI entry point | ⏳ Slice 3 — top-level CLI wiring |
| Deployable as Zeta CLI install | ⏳ Slice 3 |

## Slice decomposition

- **Slice 1 (this PR)**: polling loop infrastructure + tests
- **Slice 2**: empirical grey text detection (AppleScript/accessibility API — macOS empirical testing)
- **Slice 3**: `zeta shadow` top-level CLI entry point + Zeta install wiring

operative-authorization: aaron 2026-05-13: "Cooling period: TBD. The memory file IS the durable record"

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 0 |
| Resolved threads | 0 |
| Unresolved threads | 0 |
| Total review comments | 0 |
| Total fix commits (touching thread paths) | 0 |

## Review threads

(no review threads on this PR)

## Fix commits (touching thread paths)

(no commits in this PR touched files anchored by review threads)
