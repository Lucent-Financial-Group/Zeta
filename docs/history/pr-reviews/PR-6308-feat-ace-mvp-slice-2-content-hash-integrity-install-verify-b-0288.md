# PR #6308 -- feat(ace): MVP slice 2 — content-hash-integrity install/verify (081KR2E4K0008QG0R002YE3MMD)

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 6308 |
| Title | feat(ace): MVP slice 2 — content-hash-integrity install/verify (081KR2E4K0008QG0R002YE3MMD) |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-06-01T01:36:50Z |
| Merged at | 2026-06-01T01:50:09Z |
| Merge commit SHA | `b4fdcb06fa0560f27ed48324661e2b9ea2b137ec` |
| Branch | `otto-windows/ace-mvp-slice2-2026-06-01` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/6308 |
| Changed files | 5 |
| Additions / deletions | +273 / -32 |

## Description

## What

Ace MVP **slice 2** — the content-hash-**integrity** `install`/`verify` build (slice 1 = `bun link` distribution + skill surface, merged #6301; the plan: `docs/agendas/ace-package-manager/2026-06-01-ace-cli-mvp-implementation-plan.md` SLICE 2 / Tasks 5–8).

- **`contentHash(bytes) → sha256:<hex>`** in `store.ts` (Task 5).
- **`installPackage()` verify-before-extract** (Task 6): recompute the content hash of `pkg.files`, refuse on mismatch, extract to `~/.ace/store/<hash>/` with a `manifest.json`. Path-traversal guard rejects `..` / absolute / UNC-backslash paths. Validation is a **pre-pass over all paths before any mkdir/write**, so a traversal-blocked install refuses **atomically** (extracts nothing) — same guarantee as a hash mismatch.
- **`install <url-or-path>` + `verify <hash>` verbs** wired into `ace.ts` (Task 7); `main` is now async with a `.catch()` entry-point guard. `install` prints an **unconditional** "integrity-verified (content hash). NOT authenticity-verified" line — no green-by-skip.
- **`.claude/skills/ace/SKILL.md`** verb table + frontmatter updated; install/verify live, integrity-only noted (Task 8).

**Integrity only** — authenticity/signatures are explicitly **slice 3**. On the map (not silently skipped): Windows device names (CON/NUL/PRN) + drive-relative paths are not rejected by the guard (they can't escape `<store>/<hash>/`); tracked for the authenticity/robustness slice.

## Process

Built subagent-driven (implementer → independent review → fix loop → independent re-review). First review returned CHANGES_REQUIRED (atomic-refusal partial-write, untested absolute/backslash guard branches, entry-point unhandled-promise, `require()` idiom, stale router description); fix commit `dbfcef7d0` closed all; re-review **APPROVED**, all findings CLOSED, no regressions.

## Gates (verified)

- `bun test tools/ace/` → **35 pass / 0 fail**
- `tsc --noEmit` → clean on `tools/ace`
- `ace help` / `ace list --json` → exit 0 · `ace verify nonexistent` → exit 1
- `markdownlint-cli2 SKILL.md` → exit 0
- commit canary `git ls-tree HEAD | wc -l` → 67

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | true |
| Total threads | 2 |
| Resolved threads | 2 |
| Unresolved threads | 0 |
| Total review comments | 4 |
| Total fix commits (touching thread paths) | 4 |

## Review threads

### Thread 1 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F_Ooi`
- **Path**: `tools/ace/store.ts`
- **Replies**: 1

**Initial comment:**

- **`github-advanced-security[bot]` (bot)** at 2026-06-01T01:39:45Z on `tools/ace/store.ts`:125 (association: NONE)

  <pre>
  ## CodeQL / Network data written to file

  Write to file system depends on [Untrusted data](1).

  [Show more details](https://github.com/Lucent-Financial-Group/Zeta/security/code-scanning/133)
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-06-01T01:45:29Z on `tools/ace/store.ts`:125 (association: MEMBER)

    <pre>
    Accepted with justification (not a removable bug). `install &lt;url&gt;` fetching package bytes and writing them to the content-addressed store is the package manager's core function — the http→file data-flow is intended. The write is path-confined to `&lt;store&gt;/&lt;hash&gt;/` (traversal guard) and integrity-checked against the manifest hash; but the manifest ships *with* the download, so integrity alone can't defend a malicious *source*. Authenticity (signature over a trusted key) is the defense and is explicitly slice 3. Documented on-the-map in the store.ts slice-3 NOTE (e33622254) rather than suppressed with a `codeql[]` directive (no repo precedent for inline suppressions; the medium alert honestly stays open until slice 3 adds authenticity). Non-required check; required checks are green.
    </pre>

### Thread 2 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F_O5y`
- **Path**: `.claude/skills/ace/SKILL.md`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-01T01:40:40Z on `.claude/skills/ace/SKILL.md`:3 (association: NONE)

  <pre>
  The new `description` is ~217 characters, well over the 150-char hard cap enforced by `tools/hygiene/audit-skill-description-length.ts` (MAX_CHARS=150). The audit will fail with `description is 217 chars (&gt; 150 cap — gets dropped from listing)`, which also means the skill silently drops from the cold-start routing listing per 081KR50HA0008QG0R002ZNFQBZ. Carve to ≤150 (preferably ≤120) chars — drop the secondary clauses (integrity/authenticity caveat belongs in the body, where it's already stated on line 33).
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-06-01T01:45:32Z on `.claude/skills/ace/SKILL.md`:3 (association: MEMBER)

    <pre>
    Fixed in e33622254 — carved the description from 214 to 102 chars (under the 150 cap and the 120 preferred). Dropped the integrity/authenticity caveat from the router field; it already lives in the body verb-table note. `audit-skill-description-length.ts` now clears `ace` (the 2 remaining errors are pre-existing on main — biometric-sudo-handler, glass-halo-signature-acquisition — out of scope for this PR).
    </pre>

## Fix commits (touching thread paths)

### `851919125e203e672eaafce5047f7d28d32d1aea` -- 2026-06-01T01:09:23Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/ace/store.ts`

**Message:**

```
feat(ace): contentHash(bytes) -> sha256:<hex> (store.ts) + unit tests

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `60572ee19305e0a23f082812448d84504d42c4ab` -- 2026-06-01T01:13:40Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `.claude/skills/ace/SKILL.md`

**Message:**

```
docs(ace): skill — install/verify now live (integrity-only; authentic…

…ity is slice 3)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `dbfcef7d07527bb6df7087e598e245204bf1f750` -- 2026-06-01T01:28:22Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `.claude/skills/ace/SKILL.md`
- `tools/ace/store.ts`

**Message:**

```
fix(ace): atomic traversal refusal + entry-point .catch + guard tests…

… (review)

Independent review of slice 2 (CHANGES_REQUIRED) — close the findings:

- P2 (atomicity): validate ALL package file paths in a pre-pass BEFORE any
  mkdir/write, so a traversal-blocked install refuses atomically (extracts
  nothing) — same guarantee as a hash mismatch. Previously a multi-file
  package with a non-first traversal path wrote earlier files before the
  guard fired, leaving a partial hash dir.
- P1 (shield holes): add guard tests for absolute POSIX (/etc/passwd),
  absolute UNC/backslash (\server\share), and Windows-style ..\ traversal —
  the branches worked but were untested (false-green). Plus a multi-file
  atomic-refusal test asserting the store stays clean after a blocked install.
- P2 (unhandled promise): add .catch() to the async-main entry point so an
  unexpected throw exits 1 with a diagnostic, not an UnhandledPromiseRejection.
- P2 (idiom): store.test.ts uses ES `import { createHash }` not `require()`.
- P3 (router field): SKILL.md description now reflects install/verify live +
  integrity-not-authenticity.
- On the map (not silently skipped): a guard comment documents that Windows
  device names (CON/NUL/PRN) + drive-relative paths are NOT rejected — they
  cannot escape <store>/<hash>/ but are tracked for the authenticity slice.

Gates: bun test tools/ace 35 pass / 0 fail; tsc clean on tools/ace; ace
help/list exit 0; verify-nonexistent exit 1; markdownlint SKILL.md exit 0.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `e33622254ec72941c53ae718122559ada2d1891d` -- 2026-06-01T01:45:03Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `.claude/skills/ace/SKILL.md`
- `tools/ace/store.ts`

**Message:**

```
fix(ace): address PR #6308 review — carve skill desc + document accep…

…ted http-to-file flow

- Copilot: SKILL.md description was 214 chars (> 150 cap in
  audit-skill-description-length.ts; 081KR50HA0008QG0R002ZNFQBZ drops it from cold-start routing).
  Carved to 102 chars (drops the integrity/authenticity caveat — already in
  the body verb-table note). ace cleared from the audit error list.
- CodeQL js/http-to-file-access (store.ts: install <url> fetch -> writeFileSync,
  medium): intended package-manager data-flow, NOT a removable bug. Write is
  path-confined to <store>/<hash>/ + integrity-checked, but the manifest ships
  WITH the download so integrity alone can't defend a malicious source —
  authenticity (signature over a trusted key) is explicitly slice 3. Extended
  the in-code slice-3 NOTE to document the accepted flow on-the-map (NOT a
  codeql[] suppression — no repo precedent for inline suppressions, and the
  alert honestly stays open until slice 3 adds authenticity).

Gates: bun test tools/ace 35/0; tsc clean on tools/ace; skill-desc audit clears
ace; markdownlint SKILL.md exit 0.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```
