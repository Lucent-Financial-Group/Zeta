# PR #11100 -- fix(ace): refresh the derived build graph — main was red on a floor check

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 11100 |
| Title | fix(ace): refresh the derived build graph — main was red on a floor check |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-08-16T21:11:06Z |
| Merged at | 2026-08-16T21:23:50Z |
| Merge commit SHA | `96c43f3bbe6586e478e11de7a5fdc7636dd1a4d6` |
| Branch | `shadow/fix-ace-graph-drift` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/11100 |
| Changed files | 1 |
| Additions / deletions | +1 / -1 |

## Description

`main` was failing **`cross-verify (trust-core oracles + ace suite)`** — which **is** in the `gate (required)` floor (`build-and-test`, `lint`, `cross-verify`, `full-verify`), so it was **blocking every PR**. Three were already stuck on it: #11092, #11093, #11094.

## The failure

```
✗ DRIFT GATE: regenerating from the repo's manifests reproduces the checked-in graph
✗ DRIFT GATE: a hand-edited tier contradicting the derivation fails derive
```

A manifest changed without the derived graph being refreshed. **The real delta is one value** — `"count": 288 → 290` — consistent with the treaty-witness count moving after a 6th witness landed with the four-corner-trace TypeScript mirror.

## The fix

The prescribed one: `bun src/Core.TypeScript/ace/build-graph.ts derive --write`.

**583 pass / 0 fail. One-line diff.**

## Deliberately not running prettier

The tool's closing message says *"run prettier --write on it before committing."* **Don't.** The checked-in file is not in prettier layout, so `prettier --write` turns this one-line change into **1275 lines of churn** (311 insertions, 964 deletions).

A sibling agent hit exactly this today, reverted it, and concluded *"the generator is the formatter."* The drift test agrees — it is explicitly **"content-exact, formatting-agnostic"** — so the minimal diff is both correct and reviewable.

That tool message is misleading and worth fixing on its own; not bundling it here while a floor check is red.

**Omits the template's trailing `Co-authored-by:` line deliberately** — verified today that it causes GitHub's squash to append a co-author separator splitting the trailer group, so git parses 1 trailer instead of 10.

Agency-Signature-Version: 1
Agent: shadow
Agent-Runtime: Claude Code
Agent-Model: claude-opus-5
Credential-Identity: AceHack via gh
Credential-Mode: shared
Human-Review: not-implied-by-credential
Human-Review-Evidence: none
Action-Mode: autonomous-fail-open
Task: none


## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 0 |
| Resolved threads | 0 |
| Unresolved threads | 0 |
| Total review comments | 0 |
| Total fix commits (touching thread paths) | 0 |

## Review threads

(no review threads on this PR)

## Fix commits (touching thread paths)

(no commits in this PR touched files anchored by review threads)
