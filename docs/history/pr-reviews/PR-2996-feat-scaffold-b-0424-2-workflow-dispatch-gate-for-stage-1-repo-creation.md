# PR #2996 -- feat(scaffold): B-0424.2 — workflow_dispatch gate for Stage 1 repo creation

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 2996 |
| Title | feat(scaffold): B-0424.2 — workflow_dispatch gate for Stage 1 repo creation |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-05-13T15:55:11Z |
| Merged at | 2026-05-13T16:25:43Z |
| Merge commit SHA | `28595d03d71664c1f3a23643c9fe61b0ee281056` |
| Branch | `feat/B-0424.2-scaffold-stage1-workflow` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/2996 |
| Changed files | 1 |
| Additions / deletions | +170 / -0 |

## Description

## Summary

B-0424.2 — the \"follow-up PR with Aaron's review\" that B-0424.1 explicitly deferred.

Adds `.github/workflows/scaffold-stage1-create-repos.yml`: a `workflow_dispatch`-only
workflow that creates `LFG/Forge` and `LFG/ace` using the
`tools/scaffold/create-repo.ts --apply` path landed in B-0424.1 (PR #2994).

## Why this design

Creating GitHub repos is irreversible. The workflow gates execution behind:

1. **Explicit maintainer trigger** — `workflow_dispatch` only, no schedule.
2. **CONFIRM text input** — must type exactly `CONFIRM` or step 1 fails loudly.
3. **`SCAFFOLD_STAGE1_PAT` secret** — `GITHUB_TOKEN` cannot create org repos
   (its scope is the current repo). A PAT with `repo, read:org, workflow` scopes
   on an LFG org owner/admin is required. Create it at:
   `Settings → Secrets and variables → Actions → New repository secret`.

## Safe-pattern compliance

All `workflow_dispatch` inputs are routed via `env:` into `run:` blocks and
referenced as `"$VAR"` — no `${{ expressions }}` interpolated directly in
shell scripts. See `budget-snapshot-cadence.yml` for the factory pattern.

- SHA-pinned checkout: `de0fac2e4500dabe0009e67214ff5f5447ce83dd` (v6.0.2)
- `contents: read` at workflow + job level
- Concurrency group prevents double-trigger race

## Dry-run output (verified before PR)

```
Forge: DRY RUN — 12 operations planned for Lucent-Financial-Group/Forge
  01-create-repo, 01b-merge-settings, 06-push-scaffold-files (10 files),
  02-branch-protection, 02b-required-signed-commits,
  03a-secret-scanning, 03b-dependabot-alerts, 03c-dependabot-security-updates,
  03d-private-vuln-reporting, 04-codeql-default-setup,
  05-fork-to-acehack, 07-next-steps

ace: DRY RUN — 12 operations planned for Lucent-Financial-Group/ace
  (same step sequence, ace-scoped governance files)
```

## Build gate

`dotnet build -c Release` → **0 Warning(s) 0 Error(s)**

## How to trigger after merge

1. Create `SCAFFOLD_STAGE1_PAT` secret (PAT: `repo, read:org, workflow` scopes).
2. Go to **Actions → scaffold-stage1-create-repos → Run workflow**.
3. Select repo: `both` (or `forge` / `ace` individually).
4. Type `CONFIRM` in the confirm field.
5. Click **Run workflow**.

Manual steps after the workflow completes (printed in step 7):
- Upload SVG social-preview PNG (GitHub requires rasterized PNG)
- Enable merge queue via GitHub UI (no REST API)
- Wire OpenSSF Scorecard workflow (copy from Zeta's `.github/workflows/scorecard.yml`)
- Add Semgrep GHA inline-untrusted-in-run rule
- Verify \$0 budget caps at LFG org billing
- Confirm CodeQL default-setup is active

## Relation to B-0424

- B-0424.1 (PR #2994, merged): templates + dry-run tool ✅
- **B-0424.2 (this PR)**: `workflow_dispatch` gate for `--apply` execution
- B-0424 DOD: repos exist + scaffolding 100% applied — closes after Aaron triggers workflow

operative-authorization: aaron 2026-05-13: "Cooling period: TBD. The memory file IS the durable record"

Co-Authored-By: Claude <noreply@anthropic.com>

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | true |
| Total threads | 4 |
| Resolved threads | 4 |
| Unresolved threads | 0 |
| Total review comments | 7 |
| Total fix commits (touching thread paths) | 2 |

## Review threads

### Thread 1 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6B0FMd`
- **Path**: `.github/workflows/scaffold-stage1-create-repos.yml`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-13T15:57:44Z on `.github/workflows/scaffold-stage1-create-repos.yml`:33 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Add maintainer-only approval gate before repo creation**

  This workflow relies on `workflow_dispatch` plus a `CONFIRM` string, but those checks do not restrict *who* can execute it; GitHub’s manual-run docs state that users with write access can run `workflow_dispatch` workflows. In any repo with multiple writers, a non-maintainer can trigger this job and consume `SCAFFOLD_STAGE1_PAT` to create irreversible org repositories, which contradicts the stated “explicit maintainer action” safety model. Add an environment with required reviewers (or an explicit actor allowlist check) to enforce operator authorization, not just intent confirmation.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-13T16:01:53Z on `.github/workflows/scaffold-stage1-create-repos.yml`:33 (association: MEMBER)

    <pre>
    Fixed in push faba8904. Added a new first step **Validate actor allowlist** that checks `github.actor` against the LFG maintainer list (`AceHack`) via env-var routing. Write access alone is no longer sufficient to trigger the irreversible path — only listed maintainers can proceed. The allowlist is maintained in-workflow; update via PR when the roster changes. actionlint passes.
    </pre>

### Thread 2 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6B0Glt`
- **Path**: `.github/workflows/scaffold-stage1-create-repos.yml`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-13T15:58:55Z on `.github/workflows/scaffold-stage1-create-repos.yml`:73 (association: NONE)

  <pre>
  P1: The validation step logs the raw `confirm` workflow_dispatch input in the error message. If the operator accidentally pastes a token/secret into the confirm field, this will be permanently recorded in Actions logs. Consider removing the echoed value (or redacting it) and only stating that the input did not match `CONFIRM`.

  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-13T16:02:00Z on `.github/workflows/scaffold-stage1-create-repos.yml`:73 (association: MEMBER)

    <pre>
    Fixed in push faba8904. The error message now prints a generic string — `'CONFIRM' (all caps)` — without echoing the raw input value. If a secret were accidentally pasted into the confirm field it will no longer appear in Actions logs.
    </pre>

### Thread 3 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6B0Gmt`
- **Path**: `.github/workflows/scaffold-stage1-create-repos.yml`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-13T15:58:56Z on `.github/workflows/scaffold-stage1-create-repos.yml`:95 (association: NONE)

  <pre>
  P2: `actions/checkout` is used without `persist-credentials: false`. Since this workflow performs GitHub mutations via the PAT (and does not push back to this repo), disabling persisted checkout credentials reduces the chance of accidentally using `GITHUB_TOKEN` for any git operations and matches the hardening pattern used in other security-sensitive workflows (e.g. scorecard).

  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-13T16:02:06Z on `.github/workflows/scaffold-stage1-create-repos.yml`:95 (association: MEMBER)

    <pre>
    Fixed in push faba8904. Added `persist-credentials: false` to the checkout step. This workflow uses `SCAFFOLD_STAGE1_PAT` for all GitHub mutations and never pushes to this repo, so there is no reason to keep GITHUB_TOKEN credentials on disk after checkout.
    </pre>

### Thread 4 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6B0QbK`
- **Path**: `.github/workflows/scaffold-stage1-create-repos.yml`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-13T16:06:31Z on `.github/workflows/scaffold-stage1-create-repos.yml`:75 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Enforce maintainer gate outside mutable workflow code**

  Fresh evidence in this commit: the new allowlist check is implemented inside the workflow file itself, but `workflow_dispatch` runs can target a chosen ref/branch, so any user with write access can dispatch a branch where this step is edited out. Because this job consumes `secrets.SCAFFOLD_STAGE1_PAT`, that writer can still run privileged repo-creation logic despite failing the default-branch allowlist, which leaves the maintainer-only control bypassable. Put the secret behind an environment with required reviewers (or another server-side approval control) so authorization is enforced outside branch-editable YAML.

  Useful? React with 👍 / 👎.
  </pre>

## Fix commits (touching thread paths)

### `497ab7ed3567b52892da3e83b0abec2de73d7f51` -- 2026-05-13T15:54:46Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `.github/workflows/scaffold-stage1-create-repos.yml`

**Message:**

```
feat(scaffold): B-0424.2 — workflow_dispatch gate for Stage 1 repo cr…

…eation

Adds .github/workflows/scaffold-stage1-create-repos.yml — a
workflow_dispatch-only workflow that creates LFG/Forge and LFG/ace using
the tools/scaffold/create-repo.ts --apply path.

This is the "follow-up PR with Aaron's review" that B-0424.1 explicitly
deferred. The workflow gates the irreversible external action (GitHub repo
creation) behind:

1. An explicit maintainer trigger from GitHub UI (no schedule).
2. A CONFIRM text input — must type exactly CONFIRM to proceed.
3. A prerequisite SCAFFOLD_STAGE1_PAT secret (PAT with repo, read:org,
   workflow scopes — GITHUB_TOKEN cannot create org repos).

Safe-pattern compliance (per .github/workflows/budget-snapshot-cadence.yml
factory convention):
- All workflow_dispatch inputs routed via env: into run blocks.
- No ${{ expressions }} interpolated directly in shell scripts.
- contents: read at both workflow and job level (PAT handles org writes).
- Concurrency group prevents double-trigger races.
- SHA-pinned checkout: de0fac2e4500dabe0009e67214ff5f5447ce83dd (v6.0.2).

Build gate: dotnet build -c Release → 0 Warning(s) 0 Error(s).
Dry-run verified: both repos show 12 planned operations each.

operative-authorization: aaron 2026-05-13: "Cooling period: TBD. The memory file IS the durable record"

Co-Authored-By: Claude <noreply@anthropic.com>
```

### `faba89042f77d3bbb88e6206d27411a5184c7d59` -- 2026-05-13T16:01:39Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `.github/workflows/scaffold-stage1-create-repos.yml`

**Message:**

```
fix(scaffold): B-0424.2 — address Codex/Copilot review findings on sc…

…affold workflow

Three security hardening fixes from PR #2996 reviewer threads:

1. Actor allowlist gate (Codex P1): add a first step that checks
   github.actor against the LFG maintainer list. workflow_dispatch
   alone only requires write access; this ensures only authorised
   maintainers can trigger the irreversible repo-creation path.

2. Log-safe CONFIRM error (Copilot P1, line 73): remove raw
   $CONFIRM_INPUT echo from the error message. If a PAT/secret
   were accidentally pasted into the confirm field it would
   previously be permanently recorded in Actions logs.

3. persist-credentials: false on checkout (Copilot P2, line 80):
   this workflow uses SCAFFOLD_STAGE1_PAT for all GitHub mutations
   and never pushes to this repo; disabling persisted credentials
   eliminates accidental GITHUB_TOKEN usage.

All three changes use env: routing — no ${{ expressions }} in
run: blocks. actionlint passes locally.

Co-Authored-By: Claude <noreply@anthropic.com>
```
