# PR #3008 -- feat(bg): B-0442.1 — missed-substrate detector skeleton (completes 3-skeleton mechanization suite)

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 3008 |
| Title | feat(bg): B-0442.1 — missed-substrate detector skeleton (completes 3-skeleton mechanization suite) |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-05-13T17:41:50Z |
| Merged at | 2026-05-13T17:53:06Z |
| Merge commit SHA | `4395f68474d5d19a71961be7cfa662bc56d91c06` |
| Branch | `otto-b0442-1-missed-substrate-detector-skeleton-slice-1-impl-2026-05-13` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/3008 |
| Changed files | 2 |
| Additions / deletions | +181 / -0 |

## Description

## Summary

Third and final skeleton in the mechanization suite. With PR #3006 (B-0440.1) and PR #3007 (B-0441.1), this PR completes the three-layer defense pattern:

| Service | Layer | Trigger |
|---------|-------|---------|
| B-0440.1 standing-by-detector | Reactive | Cron-fires + idle threshold |
| B-0441.1 backlog-ready-notifier | Proactive | Queue-empty + rows-ready |
| **B-0442.1 missed-substrate-detector** | **Drift-prevention** | **Merged-PR + branch-HEAD divergence** |

## Canonical operational example

The Otto-section-missed-PR-2980-by-3-min cascade earlier today (recovered via PR #2997). Branch had the commit; main did not (auto-merge fired 3 minutes before push arrived). This service detects that pattern proactively.

## Implementation pattern

Same skeleton pattern as the other two with all bug-fixes pre-applied:
- Bounded single-iter mode + unbounded daemon mode (no result accumulation)
- \`parsePositiveMinutes\` validation on \`--poll-min\`

## Test results

\`\`\`
bun test
 3 pass
 0 fail
 7 expect() calls
\`\`\`

## Composes with

- B-0442 (backlog row; PR #3000 merged)
- B-0440.1 + B-0441.1 (companion skeletons; PR #3006 + #3007)
- B-0400 (bus protocol — future slice 4)
- PR #2998 (architectural challenge)
- \`tools/hygiene/LOST-FILES-LOCATIONS.md\` (mechanizes one of the 15-class lost-files surveys)

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | true |
| Total threads | 5 |
| Resolved threads | 5 |
| Unresolved threads | 0 |
| Total review comments | 5 |
| Total fix commits (touching thread paths) | 3 |

## Review threads

### Thread 1 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6B16IV`
- **Path**: `tools/bg/missed-substrate-detector.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-13T17:43:36Z on `tools/bg/missed-substrate-detector.ts`:71 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Return CLI parse failures instead of throwing exceptions**

  The new CLI parser raises `Error` for invalid `--poll-min` inputs, which violates the repo’s hard “Result-over-exception” contract for user-visible failures and turns normal operator mistakes into unhandled crashes with stack traces. In practice, a typo like `--poll-min nope` will abort the service rather than returning a structured failure value that callers/automation can handle consistently.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 2 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6B18cV`
- **Path**: `tools/bg/missed-substrate-detector.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-13T17:45:53Z on `tools/bg/missed-substrate-detector.ts`:55 (association: NONE)

  <pre>
  P1: `runDetector` is typed/documented as returning results (`Promise&lt;PollResult[]&gt;` / “returns its result”), but in daemon mode it never returns (infinite loop) and in `once` mode it returns an array even though the doc says singular. This mismatch can mislead callers and makes the API shape unclear; consider splitting into `runOnce(): PollResult` + `runDaemon(): Promise&lt;never|void&gt;`, or changing `runDetector`’s signature/docs to accurately reflect the two modes (e.g., return a single `PollResult` for `once` and `Promise&lt;never&gt;`/`Promise&lt;void&gt;` for daemon mode).

  </pre>

### Thread 3 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6B18cy`
- **Path**: `tools/bg/missed-substrate-detector.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-13T17:45:54Z on `tools/bg/missed-substrate-detector.ts`:85 (association: NONE)

  <pre>
  P1: `parseArgs` silently ignores unknown CLI flags, which makes typos easy to miss and can cause the detector to run with default settings unexpectedly. The repo’s tools generally fail fast on unknown args (e.g., `tools/backlog/empty-queue-pickup.ts` / `tools/backlog/autonomous-pickup.ts` throw `unknown arg`). Consider throwing (or returning an error) on unknown args and optionally supporting `--help`.
  </pre>

### Thread 4 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6B18dI`
- **Path**: `tools/bg/missed-substrate-detector.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-13T17:45:54Z on `tools/bg/missed-substrate-detector.ts`:7 (association: NONE)

  <pre>
  P1: This header comment includes direct persona-name attribution (“Otto-section…”). Per `docs/AGENT-BEST-PRACTICES.md` (“No name attribution in code, docs, or skills… current-state surfaces include: code”), these should be role-refs or neutral descriptors. Suggest renaming the example to a non-name form (e.g., “own-voice section missed PR-2980 by 3 min”).

  </pre>

### Thread 5 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6B18dZ`
- **Path**: `tools/bg/missed-substrate-detector.test.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-13T17:45:54Z on `tools/bg/missed-substrate-detector.test.ts`:75 (association: NONE)

  <pre>
  P2: The tests cover the no-op `pollOnce` and `runDetector({ once:true })`, but the CLI validation path advertised in the PR description (`--poll-min` using `parsePositiveMinutes`) isn’t exercised. Adding tests for invalid/missing `--poll-min` values (and for unknown-arg handling if added) would lock in the contract for future slices.
  </pre>

## Fix commits (touching thread paths)

### `831aa82e7b5b1bfe76e15a88174ea3ef93143bf0` -- 2026-05-13T17:41:24Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/bg/missed-substrate-detector.test.ts`
- `tools/bg/missed-substrate-detector.ts`

**Message:**

```
feat(bg): B-0442.1 — missed-substrate cascade detector skeleton (comp…

…letes the 3-skeleton suite)

Third and final skeleton in the mechanization suite. With B-0440.1
(reactive Standing-by detector; PR #3006) and B-0441.1 (proactive
backlog-ready notifier; PR #3007), B-0442.1 (drift-prevention) closes
the trio.

Files (same shape as B-0440.1 / B-0441.1 with bug-fixes pre-applied):
- tools/bg/missed-substrate-detector.ts (87 lines):
  - DetectorConfig + DEFAULT_CONFIG (5min poll)
  - pollOnce() no-op result
  - runDetector() — bounded single-iter or unbounded daemon (no result accumulation)
  - parsePositiveMinutes validation on --poll-min
  - CLI entry

- tools/bg/missed-substrate-detector.test.ts (3 tests, all pass)

Three-layer defense suite now in code:
| Service | Layer | Trigger |
|---------|-------|---------|
| B-0440.1 standing-by-detector | Reactive | Cron-fires + idle threshold |
| B-0441.1 backlog-ready-notifier | Proactive | Queue-empty + rows-ready |
| B-0442.1 missed-substrate-detector | Drift-prevention | Merged-PR + branch-HEAD divergence |

Canonical operational example B-0442 was filed for:
Otto-section-missed-PR-2980-by-3-min cascade (recovered via PR #2997).

Future slices (per B-0442 decomposition):
- Slice 2: merged-PR state fetch via gh CLI
- Slice 3: branch-vs-squash comparison logic
- Slice 4: cascade-detection bus publish (requires B-0400 schema extension for missed-substrate-cascade topic)
- Slice 5: optional auto-recovery-PR opening (gated)
- Slice 6: integration tests + cron registration

Test results: 3 pass / 0 fail / 7 expect() calls.

Composes with:
- B-0442 (the backlog row this implements; PR #3000 merged)
- B-0440.1 + B-0441.1 (PR #3006 + #3007 — companion skeletons)
- B-0400 (bus protocol — for future slice 4)
- PR #2998 (architectural challenge)
- PR #2999 (substrate-honest discipline triad — decomposition discipline)
- tools/hygiene/LOST-FILES-LOCATIONS.md (15-class lost-files survey — this service mechanizes one class)

Co-Authored-By: Claude <noreply@anthropic.com>
```

### `581c368a88310c75393161f7138d9f2aa95c4685` -- 2026-05-13T17:45:25Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/bg/missed-substrate-detector.test.ts`

**Message:**

```
fix(tsc): non-null assert results[0]! under noUncheckedIndexedAccess

TypeScript 6 + noUncheckedIndexedAccess makes results[0] PollResult|undefined;
toHaveLength(1) asserts length but doesn't narrow the type, so the explicit
non-null assertion is needed.

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>
```

### `89259e2d1226843bed18790769a694dac49dcf13` -- 2026-05-13T17:48:08Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/bg/missed-substrate-detector.test.ts`
- `tools/bg/missed-substrate-detector.ts`

**Message:**

```
fix(bg): B-0442.1 — close 4 Copilot findings (split runDetector, fail…

…-fast on unknown flags, role-ref, expand tests)

Addresses 4 P1/P2 findings:

1. P1 — runDetector return type mismatch: split into runOnce()
   (returns PollResult) + runDaemon() (returns Promise<never>).
   Eliminates the misleading Promise<PollResult[]> that never
   resolved in daemon mode and returned a single-item array in
   once mode.

2. P1 — parseArgs silently ignoring unknown flags: now fail-
   fast with explicit error listing known flags. Typos no
   longer hide. Functions also exported for testability.

3. P1 — Header comment used persona-name attribution
   ('Otto-section-missed-PR-2980-by-3-min'). Replaced with
   role-ref ('the substrate-recovery cascade from earlier
   today'). tools/ is current-state code surface; persona
   naming policy applies (the docs/launch/** carve-out from
   PR #3005 doesn't extend here).

4. P2 — Tests now cover CLI validation paths:
   - parsePositiveMinutes: 5 cases (positive, undefined,
     non-numeric, zero/negative, Infinity/NaN)
   - parseArgs: 5 cases (defaults, --once, --poll-min,
     unknown-flag rejection, invalid --poll-min)

Test results: 13 pass / 0 fail / 21 expect() calls (was 3 / 7).

Sibling impl PRs (B-0440.1 / B-0441.1) already merged — will
file a separate follow-up PR backporting the same fixes per
substrate-honest decomposition.

Co-Authored-By: Claude <noreply@anthropic.com>
```
