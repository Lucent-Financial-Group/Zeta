# PR #2448 -- fix: gracefully skip actions/permissions 403 in GitHub settings drift check

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 2448 |
| Title | fix: gracefully skip actions/permissions 403 in GitHub settings drift check |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-05-10T09:36:40Z |
| Merged at | 2026-05-10T09:39:25Z |
| Merge commit SHA | `89edd719feeca584d6e0c51166502f9a2a294032` |
| Branch | `fix/snapshot-drift-403-graceful` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/2448 |
| Changed files | 2 |
| Additions / deletions | +61 / -15 |

## Description

## Summary

- Fixes `check drift` CI failure caused by `GITHUB_TOKEN` lacking admin scope for `/repos/.../actions/permissions` (HTTP 403)
- `snapshot-github-settings.ts`: switches `actions/permissions` from `ghApi` (throws on failure) to `ghApiOptional`; emits `{_skipped: \"insufficient-token-scope\"}` sentinel when 403
- `check-github-settings-drift.ts`: before diffing, strips sentinel fields from both sides and warns on stderr — prevents false drift for admin-only endpoints

Real drift on fields the token CAN read is still detected normally. When running locally with an admin token, the sentinel never fires and all fields are compared.

## Root cause

The `check drift` workflow check was failing on every PR with:
```
error: snapshot failed: gh api /repos/.../actions/permissions failed (exit 1): gh: Resource not accessible by integration (HTTP 403)
```
The default `GITHUB_TOKEN` in Actions doesn't have `admin` scope to read repository Actions permissions settings. The check was non-required but produced noise on every PR.

## Verification

```
bun tools/hygiene/check-github-settings-drift.ts --repo Lucent-Financial-Group/Zeta
→ github-settings-drift: no drift (repo=Lucent-Financial-Group/Zeta)
```

No sentinel triggered locally (admin token has full access). In CI, the sentinel will fire for `actions_permissions`, emit a skip warning, and the diff will proceed on all other fields.

## Test plan

- [x] Drift check passes locally (no false positives)
- [x] sentinel path verified by inspecting code: null → `{_skipped: "insufficient-token-scope"}` → stripped from both sides before diff
- [x] Build gate: `dotnet build -c Release` → 0 Warning(s) 0 Error(s)
- [x] Rebased on main (after #2447 merged with refreshed `github-settings.expected.json`)

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 1 |
| Resolved threads | 1 |
| Unresolved threads | 0 |
| Total review comments | 1 |
| Total fix commits (touching thread paths) | 1 |

## Review threads

### Thread 1 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6A4557`
- **Path**: `tools/hygiene/snapshot-github-settings.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-10T09:38:24Z on `tools/hygiene/snapshot-github-settings.ts`:218 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Fail on non-403 errors for actions permissions fetch**

  This call now uses `ghApiOptional`, which returns `null` for *any* non-zero `gh api` exit code, not just the intended 403 insufficient-scope case. Combined with the new sentinel stripping in `check-github-settings-drift.ts`, transient API failures (e.g., endpoint-specific 5xx) or other auth/config errors get silently dropped from comparison and can report `no drift` instead of surfacing an operational failure. Restrict the sentinel path to confirmed 403 responses and keep other failures fatal.

  Useful? React with 👍 / 👎.
  </pre>

## Fix commits (touching thread paths)

### `d736bc7eadcf71034431c523a970f1bf2523d96d` -- 2026-05-10T09:36:24Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/hygiene/snapshot-github-settings.ts`

**Message:**

```
fix: gracefully skip actions/permissions 403 in drift check

In CI, GITHUB_TOKEN lacks admin scope for /repos/.../actions/permissions
causing the snapshot to throw and the entire drift check to exit 2.

- snapshot-github-settings.ts: switch actions/permissions from ghApi
  (throws on failure) to ghApiOptional (returns null). Emit
  {_skipped: "insufficient-token-scope"} sentinel when 403.
- check-github-settings-drift.ts: before diffing, detect sentinel fields
  in the live snapshot, drop them from both sides, and warn on stderr.
  This prevents false drift when CI can't read admin-only endpoints.

Real drift (fields the token CAN read) is still detected normally.
Running locally with admin token: sentinel never fires; all fields compared.

Closes the non-required "check drift" CI failure introduced by the
actions/permissions 403 when using the default GITHUB_TOKEN.

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>
```
