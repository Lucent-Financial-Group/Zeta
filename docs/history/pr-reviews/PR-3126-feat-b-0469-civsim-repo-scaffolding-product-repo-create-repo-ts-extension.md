# PR #3126 -- feat(b-0469): civsim repo scaffolding — product-repo create-repo.ts extension

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 3126 |
| Title | feat(b-0469): civsim repo scaffolding — product-repo create-repo.ts extension |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-05-14T10:29:15Z |
| Merged at | 2026-05-14T10:52:19Z |
| Merge commit SHA | `ce5c4101e9d3d78550215cc5ef8152cffb63b8cc` |
| Branch | `feat/b-0469-civsim-repo-scaffolding-2026-05-14` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/3126 |
| Changed files | 11 |
| Additions / deletions | +500 / -30 |

## Description

## Summary

- Extends `tools/scaffold/create-repo.ts` to support product repos via `isProduct` flag — gates AceHack mirror step (step 05) so product repos are NOT mirrored to AceHack (factory repos only)
- Adds `civsim` to `REPO_CONFIGS` as first product repo (honor-system license, fork-friendly, mutual-privacy clause)
- Creates `tools/scaffold/civsim/` with 5 day-one scaffold files: README, LICENSE (honor-system), `.zeta-version` (Zeta pin), `.claude/CLAUDE.md` (product-scoped), CONTRIBUTING
- Adds `docs/backlog/P1/B-0469-civsim-repo-scaffolding-2026-05-14.md` backlog row

**Closes B-0469.** Implements Stage 1 of `docs/DECISIONS/2026-05-14-product-repo-split-decisions.md`.

## Dry-run verified

```
bun tools/scaffold/create-repo.ts --repo civsim --dry-run
```

11 operations planned. Step 05 (AceHack fork) correctly shows `"status": "skipped"` with reason. Build gate: 0 warnings, 0 errors.

## ⚠️ Manual follow-up required

The actual repo creation requires `--apply` — this creates a real GitHub repo (shared state, hard to reverse):

```bash
# Review dry-run first:
bun tools/scaffold/create-repo.ts --repo civsim --dry-run

# When ready, create the repo:
bun tools/scaffold/create-repo.ts --repo civsim --apply
```

After `--apply`:
1. Wire `repository_dispatch` subscription (Forge CI → civsim)
2. Upload social-preview PNG via GitHub UI
3. Enable merge queue via GitHub UI
4. Confirm CodeQL default-setup is active

## Test plan

- [x] `bun tools/scaffold/create-repo.ts --repo civsim --dry-run` — 11 ops planned, step 05 skipped ✓
- [x] `bun tools/scaffold/create-repo.ts --repo forge --dry-run` — existing factory repo unaffected ✓
- [x] `bun tools/scaffold/create-repo.ts --repo ace --dry-run` — existing factory repo unaffected ✓
- [x] `dotnet build -c Release` — 0 warnings, 0 errors ✓

🤖 Generated with [Claude Code](https://claude.ai/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | true |
| Total threads | 8 |
| Resolved threads | 8 |
| Unresolved threads | 0 |
| Total review comments | 8 |
| Total fix commits (touching thread paths) | 3 |

## Review threads

### Thread 1 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6CCJM4`
- **Path**: `tools/scaffold/create-repo.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-14T10:32:46Z on `tools/scaffold/create-repo.ts`:551 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Gate semgrep setup advice on scaffolded config presence**

  The new product scaffold (`tools/scaffold/civsim/`) does not include `.semgrep.yml`, but `step07_summary` still always tells operators to add a Semgrep step that runs `semgrep --config .semgrep.yml ...`. For product repos this instruction is now wrong and will fail when copied into CI as written, so the day-one setup output is no longer reliable for the newly added repo class.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 2 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6CCJM9`
- **Path**: `tools/scaffold/create-repo.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-14T10:32:46Z on `tools/scaffold/create-repo.ts`:560 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Remove civsim-specific claim command from product summary**

  The product-only next steps hardcode `bun tools/bus/claim.ts release --from otto --item B-0469`. Because this branch is keyed only on `config.isProduct`, any future product repo added to `REPO_CONFIGS` will emit the wrong claim release command, which can release an unrelated backlog item and leave the real claim unreleased. This makes the new "product repo" path non-reusable despite the generic `isProduct` abstraction.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 3 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6CCJoT`
- **Path**: `tools/scaffold/create-repo.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-14T10:33:17Z on `tools/scaffold/create-repo.ts`:15 (association: NONE)

  <pre>
  The new civsim apply example routes operators to run the irreversible repo-creation path locally, but this repo already has a guarded `scaffold-stage1-create-repos` workflow with actor allowlisting, a `CONFIRM` gate, PAT-secret handling, and concurrency protection for this operation (`.github/workflows/scaffold-stage1-create-repos.yml:31-56,67-92,128-142`). Please update that workflow to support civsim and direct the apply path through it, or explicitly preserve those controls for product repos before documenting `--apply` as the path.
  </pre>

### Thread 4 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6CCJo3`
- **Path**: `tools/scaffold/create-repo.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-14T10:33:17Z on `tools/scaffold/create-repo.ts`:551 (association: NONE)

  <pre>
  For product repos this manual step is inaccurate: `tools/scaffold/civsim/` only adds README, LICENSE, `.zeta-version`, `.claude/CLAUDE.md`, and CONTRIBUTING, so `.semgrep.yml` is not actually in the scaffold files even though the emitted next steps say the Semgrep config is already present and only CI wiring remains. Either include the Semgrep/toolchain baseline in the civsim scaffold (as forge/ace do) or make this manual step conditional so the generated runbook matches what step 06 pushes.

  </pre>

### Thread 5 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6CCJpU`
- **Path**: `tools/scaffold/create-repo.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-14T10:33:17Z on `tools/scaffold/create-repo.ts`:404 (association: NONE)

  <pre>
  This new product-repo branch is not covered by the existing scaffold test suite: `tools/scaffold/create-repo.test.ts` only dry-runs `forge` and `ace`, so it would not catch regressions in the `isProduct` path (for example, the skipped AceHack operation, product-only next steps, or civsim scaffold file inventory). Please add civsim dry-run assertions alongside the existing repo cases.
  </pre>

### Thread 6 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6CCJp0`
- **Path**: `docs/backlog/P1/B-0469-civsim-repo-scaffolding-2026-05-14.md`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-14T10:33:18Z on `docs/backlog/P1/B-0469-civsim-repo-scaffolding-2026-05-14.md`:9 (association: NONE)

  <pre>
  The PR description says this closes B-0469, but the row is added with `status: open`. Because the generated index renders `[x]` only for `status: closed` or `superseded-by-*` (`tools/backlog/generate-index.ts:129-133`), this would leave B-0469 open on main after the closing PR lands. Please mark the row closed with the usual closure metadata and regenerate `docs/BACKLOG.md`, or remove the closing claim from the PR scope.

  </pre>

### Thread 7 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6CCQxx`
- **Path**: `.github/workflows/scaffold-stage1-create-repos.yml`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-14T10:41:15Z on `.github/workflows/scaffold-stage1-create-repos.yml`:152 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Include civsim when repo input is `both`**

  The workflow now advertises `repo` choices as `forge, ace, civsim, or both`, but the civsim creation step only runs for `repo == 'civsim'`. In the `both` path, Forge and ace are created while civsim is silently skipped, which can leave Stage 1 partially applied even though operators selected the aggregate option. Please include `|| github.event.inputs.repo == 'both'` for the civsim branch (and corresponding preview/summary branches) so `both` is consistent with the input contract.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 8 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6CCQx0`
- **Path**: `tools/scaffold/create-repo.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-14T10:41:15Z on `tools/scaffold/create-repo.ts`:554 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Resolve semgrep scaffold path from repo key, not display name**

  `step07_summary` checks for `.semgrep.yml` under `join(SCAFFOLD_DIR, config.name)`, but `config.name` is the GitHub repo name (`Forge`) while the scaffold directory key is lowercase (`forge`). For Forge, this lookup always misses, so the Semgrep wiring instruction is omitted even though the scaffold includes `.semgrep.yml`. That gives incorrect post-create guidance for the Forge path and can cause the security gate step to be skipped.

  Useful? React with 👍 / 👎.
  </pre>

## Fix commits (touching thread paths)

### `3d7641343456b30f068cf50a908769109f069b64` -- 2026-05-14T10:28:54Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `docs/backlog/P1/B-0469-civsim-repo-scaffolding-2026-05-14.md`
- `tools/scaffold/create-repo.ts`

**Message:**

```
feat(b-0469): civsim repo scaffolding — extend create-repo.ts for pro…

…duct repos

Add B-0469 backlog row and tooling for Stage 1 of the product-repo split ADR
(docs/DECISIONS/2026-05-14-product-repo-split-decisions.md).

Changes:
- docs/backlog/P1/B-0469-civsim-repo-scaffolding-2026-05-14.md — new row
- tools/scaffold/create-repo.ts — add isProduct flag to RepoConfig; add civsim
  to REPO_CONFIGS; gate step 05 (AceHack fork) on !config.isProduct; add
  product-specific next steps to step 07 summary; update usage comment
- tools/scaffold/civsim/ — day-one scaffold files:
    README.md (carved sentence + fork-friendly note)
    LICENSE (honor-system text from B-0464)
    .zeta-version (Zeta main SHA pin — immutable reference)
    .claude/CLAUDE.md (product-scoped bootstrap)
    CONTRIBUTING.md (fork-friendly guidance)
- docs/BACKLOG.md regenerated

Dry-run verified: 11 operations planned, step 05 correctly skipped (product repo).
Build gate: 0 warnings, 0 errors.

NOTE: --apply is a human-confirmed step (creates a real GitHub repo).
Run: bun tools/scaffold/create-repo.ts --repo civsim --dry-run (review)
Then: bun tools/scaffold/create-repo.ts --repo civsim --apply

Co-Authored-By: Claude <noreply@anthropic.com>
```

### `8f8a5a5db1c2cdd20136b1063b71a65fa2efaab4` -- 2026-05-14T10:39:09Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `.github/workflows/scaffold-stage1-create-repos.yml`
- `docs/backlog/P1/B-0469-civsim-repo-scaffolding-2026-05-14.md`
- `tools/scaffold/create-repo.ts`

**Message:**

```
fix(b-0469): address review threads — conditional semgrep, dynamic cl…

…aim, civsim tests, close row

- Semgrep wiring step in step07_summary now gated on existsSync(.semgrep.yml)
  so product repos without it don't emit a broken runbook instruction
- Hardcoded B-0469 claim release replaced with config.backlogItem field on
  RepoConfig; civsim sets backlogItem:"B-0469"; future product repos reusable
- scaffold-stage1-create-repos.yml extended with civsim choice option + dry-run
  preview + create step — routes operator through existing workflow guards
- Add civsim dry-run test suite (8 assertions): product-path invariants,
  AceHack skip, minimal scaffold inventory, semgrep absence, productSteps
- Close B-0469 row (resolved/closed_by metadata) + regenerate docs/BACKLOG.md

Addresses Codex P2 + Copilot review threads on PR #3126.

Co-Authored-By: Claude <noreply@anthropic.com>
```

### `706ad0f664f5ded912d40d4f0695edfbc1427f0d` -- 2026-05-14T10:49:19Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `.github/workflows/scaffold-stage1-create-repos.yml`
- `tools/scaffold/create-repo.ts`

**Message:**

```
fix(b-0469): address remaining review threads — both path + semgrep path

Thread PRRT_kwDOSF9kNM6CCQxx: add `|| 'both'` to civsim dry-run, create, and
post-summary steps in scaffold-stage1-create-repos.yml so selecting 'both'
creates all three repos (forge + ace + civsim) as the input contract advertises.

Thread PRRT_kwDOSF9kNM6CCQx0: resolve semgrep scaffold path via repoArg
(lowercase CLI key, e.g. 'forge') instead of config.name (display name, e.g.
'Forge'). config.name is capitalized for Forge so join(SCAFFOLD_DIR, config.name)
always missed the directory, causing hasSemgrep to always be false and omitting
the semgrep step even when .semgrep.yml is present. Consistent with step06 which
already uses repoArg for the same scaffold path lookup.

All 38 tests pass. 0 warnings, 0 errors.

Co-Authored-By: Claude <noreply@anthropic.com>
```
