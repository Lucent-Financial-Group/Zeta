# PR #9133 -- feat(core): DbspCellGraph Distinct — a non-linear stateful operator through the scheduler

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 9133 |
| Title | feat(core): DbspCellGraph Distinct — a non-linear stateful operator through the scheduler |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-07-02T12:51:05Z |
| Merged at | 2026-07-02T12:51:14Z |
| Merge commit SHA | `6529d505f22ca97982922ae8d853f030130f1bba` |
| Branch | `otto/dbsp-cell-graph-distinct-operator` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/9133 |
| Changed files | 2 |
| Additions / deletions | +75 / -14 |

## Description

Follow-up #3 to the DBSP consumer (#9129): add a **non-linear** operator, `DbspOp.Distinct` (the DBSP `H` — incremental set-semantics distinct via `ZSet.distinctIncremental`).

## Why it's the meatier proof
Unlike the linear `Filter`/`Rekey`/`Relay`, `Distinct` is **stateful**: the cell holds its integrated *input* (`InInt`), emits only boundary-crossing (0↔positive) deltas, and advances `InInt += delta`. It's still a pure function of `(cell, delta)`, so the scheduler's noninterference / DoP-invariance guarantees carry over unchanged — now with a cell that **carries state through the ferry path**.

## Tests (2 new, 6 on the module)
- **incremental distinct == batch distinct** of the integrated input, with a retraction crossing a key's count back to 0 (key 2 vanishes);
- distinct inside a `relay → distinct → sink` graph is **DoP-invariant** (`run(1)==run(4)`) and equals `distinct(total)`.

Build 0 warnings.

Anchor: Budiu et al. *DBSP* (VLDB 2023) — the `H`/distinct operator and its bounded-by-|Δ| incremental form.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 1 |
| Resolved threads | 0 |
| Unresolved threads | 1 |
| Total review comments | 1 |
| Total fix commits (touching thread paths) | 1 |

## Review threads

### Thread 1 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM6N5em7`
- **Path**: `src/Core/DbspCellGraph.fs`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-07-02T12:53:56Z on `src/Core/DbspCellGraph.fs`:70 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Reject Distinct in applyOp instead of relaying it**

  Because `applyOp` remains a public module function in the Core assembly, adding `Distinct` as a pass-through gives callers a wrong answer whenever they evaluate a `DbspOp.Distinct` outside `step`; for example, a delta `{k -&gt; 2}` should not be returned as `{k -&gt; 2}` under set-semantics distinct/boundary-crossing behavior. Since this new union case can now be passed to the function, make the helper private/linear-only or reject `Distinct` rather than silently treating it like `Relay`.

  Useful? React with 👍 / 👎.
  </pre>

## Fix commits (touching thread paths)

### `07da0d6cc34d60fae5578b72772142e0f137cee5` -- 2026-07-02T12:51:02Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `src/Core/DbspCellGraph.fs`

**Message:**

```
feat(core): DbspCellGraph Distinct — a non-linear stateful operator t…

…hrough the scheduler (shadow*)

Follow-up #3 to the DBSP consumer: add a NON-LINEAR operator, `DbspOp.Distinct`
(the DBSP `H`, incremental set-semantics distinct via ZSet.distinctIncremental).
Unlike the linear Filter/Rekey/Relay, it is stateful: the cell holds its integrated
INPUT (`InInt`), emits only boundary-crossing (0<->positive) deltas, and advances
`InInt += delta` — still a pure function of (cell, delta), so the scheduler's
noninterference / DoP-invariance guarantees carry over unchanged.

This is the meatier proof: a stateful, non-linear DBSP operator maintained
incrementally ACROSS the cell scheduler still equals the batch recompute, and stays
DoP-invariant even though the cell now carries state through the ferry path.

2 tests (6 on the module): incremental distinct == batch distinct of the integrated
input (with a retraction crossing a key's count back to 0); distinct inside a
relay->distinct->sink graph is DoP-invariant (run(1)==run(4)) and equals
distinct(total). Build 0 warnings.

Anchor: Budiu et al. DBSP (VLDB 2023) — the `H`/distinct operator and its
bounded-by-|delta| incremental form.

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>

AgencySignature-v1:
  persona: otto
  actor: zeta-otto
  surface: src/core
  topology: shadow-clone
  intent: dbsp-cell-graph-distinct-nonlinear-stateful-operator-through-scheduler
  authorization: aaron-standing-24h-autonomous-2026-07-02
  uncertainty: low
  measure: distinct-nonlinear-stateful-cell-inint-incremental-eq-recompute-dop-invariant-with-state-through-ferry-2-tests
  delta-u: consumer-gains-nonlinear-operator-proves-stateful-cells-survive-dopn-deterministically
  seed: S4
```
