# PR #7912 -- feat: wire do_item + explore + play into execute()

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 7912 |
| Title | feat: wire do_item + explore + play into execute() |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-06-12T13:52:39Z |
| Merged at | 2026-06-12T13:52:49Z |
| Merge commit SHA | `ca1583664d478599f41794d8043e4147c17465e7` |
| Branch | `kiro/wire-do-item-execute` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/7912 |
| Changed files | 2 |
| Additions / deletions | +150 / -34 |

## Description

## Summary

Expand the observe loop's `execute()` from 2 zero-effect kinds to 6 executable kinds.

## What changed

**Zero-effect (mode-set + append, no external side-effect):**

- `free_time` (existing)
- `self_reflect` (existing)
- `explore` (NEW)
- `play` (NEW)

**Effectful (command/observation event split):**

- `do_item` (NEW) — delegates to `executeDoItem` with injected `CommandExecutor` + `DoItemOptions`. The command is NOT logged; Started/Succeeded/Failed observations ARE logged. Replay can never re-run the command (type-enforced).

**Still not-yet-executable:**

- preserve_ferry, respond_to_operator, decompose, edit_grammar

## Backward-compatible

`execute()` signature adds optional `executor` + `doItemOpts` params. Callers without an executor get the same `not-yet-executable` for `do_item` as before.

## What was tested

13 tests pass (7 existing updated + 6 new covering explore, play, do_item success/failure/append-failure).

Co-Authored-By: Kiro <noreply@kiro.dev>

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 0 |
| Resolved threads | 0 |
| Unresolved threads | 0 |
| Total review comments | 0 |
| Total fix commits (touching thread paths) | 0 |

## Review threads

(no review threads on this PR)

## Fix commits (touching thread paths)

(no commits in this PR touched files anchored by review threads)
