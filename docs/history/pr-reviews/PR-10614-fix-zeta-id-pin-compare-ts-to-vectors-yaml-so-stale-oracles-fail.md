# PR #10614 -- fix(zeta-id): pin compare.ts to vectors.yaml so stale oracles fail

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 10614 |
| Title | fix(zeta-id): pin compare.ts to vectors.yaml so stale oracles fail |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-08-14T15:22:45Z |
| Merged at | 2026-08-14T15:40:31Z |
| Merge commit SHA | `8d5a107b41d2593fa4c90822ff9c1c7389d91cf6` |
| Branch | `claim/task-zetaid-compare-reads-vectors` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/10614 |
| Changed files | 5 |
| Additions / deletions | +324 / -150 |

## Description

# Summary

`tests/cross-verification/zeta-id/compare.ts` only compared committed language outputs against each other. A mutually consistent but fully stale set stayed green — documented as Gap 1 in that directory's README. This pins every present oracle to `vectors.yaml` `expected_hex` / `expected_crockford` (parse-reject oracles emit the `rejected` token; the yaml Crockford field stays the illegal input). Same shape as the yaml primitive's fixture pin.

Edge vectors (`all-zero`, `max-128`, `overflow-reject-1`, `lenient-alias-1`) already lived in the fixture and the six oracles. This slice does not add vectors — it makes the shared fixture the compare gate's source of truth. Drops the stale `docs/BUGS.md` P1 row that still claimed those vectors were missing.

# Spec alignment

No `openspec/specs/` change. Behaviour of the zeta-id cross-verify gate tightens (stale-but-consistent now fails); the algebra is unchanged.

# Tests

```
bun test tests/cross-verification/zeta-id/compare.test.ts
# 5 pass, 0 fail

(cd tests/cross-verification/zeta-id && bun compare.ts)
# 16 / 16 vectors, all six oracles pinned to vectors.yaml
```

The new unit file asserts: fixture loads the packed + edge set; parse-reject pins output crockford to `rejected`; committed oracles agree with yaml; stale-but-consistent outputs fail; a TS key missing from yaml is drift.

# Validation

- [x] Targeted bun tests pass
- [x] New claim (stale-but-consistent must fail) has a falsifying test
- [ ] `openspec/specs/**` — not touched
- [ ] `docs/ROUND-HISTORY.md` — not notable enough for a round entry
- [x] Claim filed, work landed, claim released on this branch

# Notes

Gap 2 (MUMPS still ungated, 13 keys including a stale `workitem-v1-standard`) is documented, not fixed. Out of scope.

Agency-Signature-Version: 1
Agent: Grok
Agent-Runtime: grok-build
Agent-Model: grok-build
Credential-Identity: AceHack via gh
Credential-Mode: shared
Human-Review: none
Human-Review-Evidence: none
Action-Mode: human-directed
Task: none
Co-authored-by: Grok <noreply@x.ai>

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 0 |
| Resolved threads | 0 |
| Unresolved threads | 0 |
| Total review comments | 0 |
| Total fix commits (touching thread paths) | 0 |

## Review threads

(no review threads on this PR)

## Fix commits (touching thread paths)

(no commits in this PR touched files anchored by review threads)
