# PR #6390 -- docs(registry): Bag → 4/4 (C# merged); Z-set TS reference in-flight

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 6390 |
| Title | docs(registry): Bag → 4/4 (C# merged); Z-set TS reference in-flight |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-06-01T10:00:20Z |
| Merged at | 2026-06-01T10:10:44Z |
| Merge commit SHA | `fb282b50dcd5319f43689f2b452c6f16fa004005` |
| Branch | `otto-cli/registry-bag-4of4-zset-inflight-2026-06-01` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/6390 |
| Changed files | 1 |
| Additions / deletions | +3 / -3 |

## Description

Accuracy bump on the algebra-ladder registry rows reflecting merged reality:

- **Bag → 4/4** (was 3/4): C# #6387 merged, joining TS #6364 + Rust #6367 + F# #6385. Tier 1/2 per-lang golden-vector replay.
- **Z-set TS cell ❌ → 🚧**: TS reference + `z-set/golden-vectors.json` in-flight (#6389); F# engine `ZSet.fs` exists; C# binding + native Rust pending.

Doc-accuracy only — **no CI gate added** (the registry tracks what's shipped). markdownlint clean; table pipe-structure intact.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | true |
| Total threads | 1 |
| Resolved threads | 1 |
| Unresolved threads | 0 |
| Total review comments | 2 |
| Total fix commits (touching thread paths) | 2 |

## Review threads

### Thread 1 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GE14v`
- **Path**: `docs/PRIMITIVE-REGISTRY.md`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-01T10:02:32Z on `docs/PRIMITIVE-REGISTRY.md`:110 (association: NONE)

  <pre>
  The note says “F#/C# use ordinal string order”, but the C# Bag defaults to `Comparer&lt;T&gt;.Default` (and for `string` that is culture-sensitive). `StringComparer.Ordinal` is used when passed explicitly (e.g., for cross-language parity), so the doc should reflect that nuance to avoid implying the default is ordinal.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-06-01T10:07:47Z on `docs/PRIMITIVE-REGISTRY.md`:110 (association: MEMBER)

    <pre>
    Good catch — fixed in e5a1ab61e. Verified against `src/Core.CSharp/Bag.cs`: every factory defaults to `Comparer&lt;T&gt;.Default` (culture-sensitive for strings, per the type's own XML doc Bag.cs:130-132), and `StringComparer.Ordinal` is opt-in for cross-language parity. F#'s structural `compare` is ordinal for strings, so only the C# half over-claimed. Doc now reads: "F# uses structural `compare` (ordinal for strings); C#'s default `Comparer&lt;T&gt;.Default` is culture-sensitive for strings, so pass `StringComparer.Ordinal` explicitly for cross-language parity."
    </pre>

## Fix commits (touching thread paths)

### `3f7878c935e09d2beb146aa349549b870425ac9c` -- 2026-06-01T10:00:16Z -- ``

**Touched files (intersect with thread paths):**

- `docs/PRIMITIVE-REGISTRY.md`

**Message:**

```
docs(registry): Bag → 4/4 (C# #6387 merged); Z-set TS reference in-fl…

…ight (#6389)

Accuracy bump reflecting merged reality on the algebra ladder:
- Bag is now 4/4 (TS #6364 + Rust #6367 + F# #6385 + C# #6387) — was 3/4.
  Tier 1/2 per-lang golden-vector replay; F#/C# use ordinal string order
  (fixes the G-Set Comparer.Default culture gap, 081KT07NV0008QG0R001YDB73K).
- Z-set TS cell ❌ → 🚧: TS reference + z-set/golden-vectors.json in-flight
  (#6389); F# engine ZSet.fs exists; C# binding + native Rust pending.

Doc accuracy only (no gate added). markdownlint clean; table structure intact.
```

### `e5a1ab61e907b47fca1c50bf430baf12ec870e9d` -- 2026-06-01T10:07:16Z -- `claude`

**Touched files (intersect with thread paths):**

- `docs/PRIMITIVE-REGISTRY.md`

**Message:**

```
docs(registry): correct C# Bag string-order nuance (Comparer<T>.Defau…

…lt is culture-sensitive)

Copilot review on #6390 (docs/PRIMITIVE-REGISTRY.md:110) correctly flagged
that the note implied C# Bag uses ordinal string order by default. Verified
against src/Core.CSharp/Bag.cs: every factory defaults to Comparer<T>.Default
(culture-sensitive for strings per the type's own XML doc, Bag.cs:130-132);
StringComparer.Ordinal is opt-in for cross-language parity. F#'s structural
compare IS ordinal for strings, so only the C# half was misleading.

Co-Authored-By: Claude <noreply@anthropic.com>
```
