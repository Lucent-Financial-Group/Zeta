# PR #14250 -- fix(platform): the FlowDent chart leaves the public tree — Zeta must not depend on a private repo

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 14250 |
| Title | fix(platform): the FlowDent chart leaves the public tree — Zeta must not depend on a private repo |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-08-23T15:16:27Z |
| Merged at | 2026-08-23T15:30:08Z |
| Merge commit SHA | `664913200be56eb5fe07187ac7a83cb11c60369f` |
| Branch | `fix/flowdent-out-of-zeta` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/14250 |
| Changed files | 10 |
| Additions / deletions | +360 / -135 |

## Description

## What this does

Removes the FlowDent Blueprint chart from the public Zeta tree.

**Aaron, 2026-08-23, verbatim:**

> "lets go ahead and get flowdent charts out of the zeta repo i'll talk to max about it."

The reason, his words: *"zeta should not have any private repo dependencies."* And the
design note this is a down payment on: *"part of this is we need to move forward the multi
repo split eventually so flowdent can depend on zeta parts without itself needing to be
open source."*

**The invariant: one-way dependency. Flowdent (closed) may depend on Zeta (open); Zeta must
never depend on Flowdent.**

## What was there, measured

`full-ai-cluster/k8s/applications/platform/` → `blueprints-flowdent.yaml`, three
`platform.zeta.io/v1alpha1` `Blueprint` documents, pulled into the `platform` ArgoCD
Application by `Application.yaml`'s `directory.include` glob (line 29).

| Blueprint | image | source repo | repo state |
|---|---|---|---|
| `opendental-suite` | `ghcr.io/flowdent/cloudservice:latest` | `Flowdent/fd-core` | **private** |
| `fd-webapp` | `ghcr.io/flowdent/fd-webclient:latest` | `Flowdent/fd-webclient` | **internal** |
| `mssql` | `mcr.microsoft.com/mssql/server:2022-latest` | Microsoft (public image) | FlowDent-specific config |

Provenance read from each package's own `repository` link. The `Flowdent` org reports
`public_repos: 0`, `owned_private_repos: 9` — **no public FlowDent source exists for
anything**, so nobody cloning Zeta at a tag can satisfy these references.

**Why `mssql` went too, though its image is public.** It is not a generic SQL Server
Blueprint: its only credential source is the **`flowdent-db`** Secret (`sa-password`), and
that same Secret's `sqlserver-conn` key is what `opendental-suite` reads for
`ConnectionStrings__DefaultConnection`. The two are one deployment, and the file's own
header calls all three *"the FlowDent stack"*. `blueprints.yaml` already carries a generic
`postgres` Blueprint in the `database` category, so the library keeps a database type.

## Nothing instantiated them — verified, not assumed

A `Blueprint` is a **template**; it provisions nothing until a `Deployable` names it.
Measured on `40e993d16b`:

- The tree holds exactly **four** `Deployable` documents, all in
  `full-ai-cluster/k8s/applications/platform/examples/gmod-server.yaml`, naming `gmod`,
  `web`, `postgres`. None names `opendental-suite`, `fd-webapp`, or `mssql`.
- `instantiatedBlueprints(loadManifests(DEFAULT_ROOTS))` returns exactly
  `["gmod","postgres","web"]`.
- `examples/` is outside the Application's include glob in any case.

**This removes a dangling template reference, not a running workload.**

## The render diff

The `platform` directory Application expanded through its own include glob, before → after:

```diff
-include glob: {...,policy-default,blueprints,blueprints-flowdent,controller,portal,...}.yaml
+include glob: {...,policy-default,blueprints,controller,portal,...}.yaml
-included files (16): ... blueprints.yaml blueprints-flowdent.yaml controller.yaml ...
+included files (15): ... blueprints.yaml controller.yaml ...
 included-but-absent: (none)
 present-but-excluded: (none)

-RENDERED OBJECTS (33):
+RENDERED OBJECTS (30):
   blueprints.yaml  Blueprint  zeta-platform/unturned
   blueprints.yaml  Blueprint  zeta-platform/arma-reforger
-  blueprints-flowdent.yaml  Blueprint  zeta-platform/opendental-suite
-  blueprints-flowdent.yaml  Blueprint  zeta-platform/fd-webapp
-  blueprints-flowdent.yaml  Blueprint  zeta-platform/mssql
   controller.yaml  ServiceAccount  zeta-platform/platform-controller

-IMAGES (12 distinct):
+IMAGES (9 distinct):
   atmoz/sftp:alpine
   busybox:1.36
-  ghcr.io/flowdent/cloudservice:latest
-  ghcr.io/flowdent/fd-webclient:latest
   ghcr.io/ich777/steamcmd:armareforger
   ghcr.io/ich777/steamcmd:garrysmod
   ghcr.io/ich777/steamcmd:unturned
   ghcr.io/lucent-financial-group/zeta-platform-controller:latest
   ghcr.io/lucent-financial-group/zeta-portal:latest
-  mcr.microsoft.com/mssql/server:2022-latest
   nginx:1.27-alpine
   postgres:16-alpine
```

Every one of the 30 surviving objects is byte-identical. `included-but-absent` and
`present-but-excluded` are both `(none)` after, so the glob and the directory still agree —
the chart renders.

## Blockers: 5 → 3. This does NOT unquarantine `platform`.

`bun src/Core.TypeScript/cluster/lane-partition.ts --rung dev`, before → after:

```diff
-all 47 together: 5191m / 15406Mi / 75.8Gi over 127 images (7 unmeasurable — the total is a FLOOR)
+all 47 together: 5191m / 15406Mi / 74.3Gi over 124 images (5 unmeasurable — the total is a FLOOR)

 CANNOT BE PRICED (never packed): 3
   hat-system: unmeasurable image ghcr.io/lucent-financial-group/hat-system-operator:placeholder
   orleans: unmeasurable image ghcr.io/lucent-financial-group/zeta-orleans-silo:latest
-  platform: unmeasurable image ghcr.io/flowdent/cloudservice:latest; unmeasurable image ghcr.io/flowdent/fd-webclient:latest; unmeasurable image ghcr.io/ich777/steamcmd:armareforger; unmeasurable image ghcr.io/lucent-financial-group/zeta-platform-controller:latest; unmeasurable image ghcr.io/lucent-financial-group/zeta-portal:latest
+  platform: unmeasurable image ghcr.io/ich777/steamcmd:armareforger; unmeasurable image ghcr.io/lucent-financial-group/zeta-platform-controller:latest; unmeasurable image ghcr.io/lucent-financial-group/zeta-portal:latest

 covered by a lane: 41/47  |  quarantined: 3 oversize + 3 unpriced
```

`covered by a lane` is **unchanged at 41/47**, exactly as expected. `platform` stays in
CANNOT BE PRICED on three blockers:

```
ghcr.io/ich777/steamcmd:armareforger                            <- does not exist upstream at all
ghcr.io/lucent-financial-group/zeta-platform-controller:latest  <- ours, private, UI-only visibility flip pending
ghcr.io/lucent-financial-group/zeta-portal:latest               <- ours, private, UI-only visibility flip pending
```

**Said plainly: this closes the sovereignty hole; it does not make `platform` testable.**

## Measured artifacts and tests that moved with it

- **`lane-footprints.json`** — the three images drop out of `full-ai-cluster/platform` and
  their now-orphan `imageSizes` rows go with them. That is exactly what a re-measure would
  emit: `measureAll` builds `imageSizes` from the union of rendered images, so an entry no
  app renders would not be written. **No size was invented and none was re-measured.**
- **`lane-partition.test.ts`** — the pinned all-apps total moves `75.81 → 74.26` GiB. This
  **is** the falling-floor case that test's own comment warns about (*"a floor that moved
  down would mean an image had gone missing"*), so it is checked rather than waved through:
  the whole 1.55 GiB is `mssql` (624874207 compressed × 2.67 = 1.5538 GiB). The other two
  were **unmeasurable (401)** and so were contributing nothing to the priced total — which
  is precisely why the blocker count moves and the byte count barely does.
- **`single-node-readiness.test.ts`** — the "real tree" test pinned an **8 GiB
  counterfactual** whose subject was `mssql`, and `mssql` was the **only** Blueprint in the
  tree carrying an explicit `storageClassName`. Leaving that test as-is would have left a
  check that **cannot fail** — its subject left. It is rewritten to pin the *reason* the
  counterfactual is now 0 (no Blueprint declares a storage class), so adding one turns it
  red and forces whoever adds it to re-anchor. The rule's own falsifiers, over fixtures,
  are untouched.
- **`single-node-budget.json` / `storage-profiles.json` / `rendered-storage-claims.test.ts`**
  — narrative annotations only. Neither count is re-derived, because nothing re-measured
  them, and saying so beats quietly editing a measurement.
- **`image-footprint.measured.json` is deliberately NOT edited.** It is a dated snapshot
  (`measuredOn: 2026-08-22`) that already lags `main` — it still lists
  `ghcr.io/ich777/steamcmd:gmod`, a tag the tree corrected to `garrysmod` on 2026-08-23.
  Deleting rows from a measurement nobody re-took is the thing this repo keeps refusing.

## Where the removed content is recorded, for Max

`workitems/081M0QHCNQ3087G0R001P1GK5A-flowdent-chart-leaves-the-public-zeta-tree-one-way-dependenc.md`
carries **the full removed file, verbatim**, plus what a Flowdent-side repo would need to
use it: the two Secrets (`flowdent-db`, `flowdent-jwt`) and a `Deployable` per Blueprint.
Nothing Flowdent-specific in the machinery leaves — the CRD (`crd-blueprint.yaml`), the
reconciler (`controller.yaml`) and the `Deployable` contract all stay public in Zeta. That
is exactly Aaron's shape: **Flowdent depends on Zeta parts; Zeta does not depend on
Flowdent.** Git history preserves it too, but a commit someone has to go looking for is not
a pointer.

## Sized and NOT taken

**`arma-reforger` is the same class of question and a different answer.** Its Blueprint in
`blueprints.yaml` names `ghcr.io/ich777/steamcmd:armareforger`, which the publisher does not
ship (the tag list returns 94 tags and that is not among them — the same measurement that
fixed `gmod` → `garrysmod`). It is **likewise uninstantiated**: no `Deployable` names it, so
it too is a dangling template, and removing it would take `platform` to two blockers, both
being the pending visibility clicks.

It is **not** removed here. Flowdent's removal is authorized and is about repository
sovereignty; `arma-reforger` is a game Blueprint whose only defect is a wrong or
not-yet-published tag, and the right fix might be to correct the tag rather than delete the
Blueprint — substituting a sibling tag was already refused as unevidenced. That is Aaron's
call, not a cleanup to fold into this one.

## Deliberately left alone

Three `flowdent` mentions remain in `full-ai-cluster/`, and **none is a dependency on a
private repository**:

- `applications/kubevirt/Application.yaml` — a comment naming live production Windows VM
  guests (`fd-windows`, `fd-win-flowdent-*`).
- `applications/open-policy-agent/Application.yaml` — the `flowdent` **namespace** in the
  gatekeeper exemption list. A tenant namespace on a running cluster; removing it changes
  live admission behaviour.
- `portal/src/api.test.ts` — `flowdent/flowdent-webapp` as a URL-decoding **test fixture
  string**.

**The `Flowdent` GitHub org was not touched.** Nothing there needs changing and it is not
ours to change.

## Verification

- `git fetch --prune` first; branched from `origin/main` at **`40e993d16b9e3d49b4149bbe26d41c32a7fa1bf5`**.
- `bun test src/Core.TypeScript/cluster/` → **856 pass, 0 fail** (2737 assertions).
  Baseline on `40e993d16b` was 854 pass / 2 fail, both 5 s timeouts on a contended local
  machine, not real failures.
- `bun run preflight:quick` → the two red checks (`auto-vivify`, `markdownlint`) are both
  **pre-existing on `main`**: 14 dangling refs from other work-items, and
  `docs/ip-questionable/2026-08-23-geometric-reasoning-*`. This branch adds **zero** to
  either — the work-item was rephrased after it added a 15th dangling ref, and it lints
  clean.
- `prettier` status on every touched file is **unchanged from `origin/main`** (the three
  test files and `lane-footprints.json` already warn under the same run before this
  branch).

Expect `gate` BLOCKED — `main`'s gate is red with a deep queue. **No `--admin`.**

## Coordination

Supersedes the Flowdent half of **#14202**, which measured the provenance and deliberately
changed nothing (*"Options are recorded for Aaron and Max to choose. Nothing is removed from
the chart here"*). Aaron chose; this is the execution. That row's other two thirds — the two
package-visibility clicks and `steamcmd:armareforger` — stand untouched. No overlap with
#14194 or #14163: this branch stays inside `applications/platform/` plus the measured
artifacts that join to it.

Agency-Signature-Version: 1
Agent: shadow
Agent-Runtime: Claude Code
Agent-Model: claude-opus-5
Credential-Identity: AceHack
Credential-Mode: shared
Human-Review: not-implied-by-credential
Human-Review-Evidence: none
Action-Mode: human-directed
Task: 081M0QHCNQ3087G0R001P1GK5A

Co-authored-by: Aaron Stainback <aaron_bond@yahoo.com>

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_015VrXL5qefejozgUzchMRXp


## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 0 |
| Resolved threads | 0 |
| Unresolved threads | 0 |
| Total review comments | 0 |
| Total fix commits (touching thread paths) | 0 |

## Review threads

(no review threads on this PR)

## Fix commits (touching thread paths)

(no commits in this PR touched files anchored by review threads)
