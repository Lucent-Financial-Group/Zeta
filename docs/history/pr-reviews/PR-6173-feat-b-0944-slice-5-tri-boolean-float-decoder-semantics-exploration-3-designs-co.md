# PR #6173 -- feat(081KSV2WD0008QG0R00051XS0N slice 5): tri-boolean float decoder-semantics exploration (3 designs compared; 13/13)

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 6173 |
| Title | feat(081KSV2WD0008QG0R00051XS0N slice 5): tri-boolean float decoder-semantics exploration (3 designs compared; 13/13) |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-05-31T00:59:00Z |
| Merged at | 2026-05-31T01:20:56Z |
| Merge commit SHA | `81986b1fe8d5bd2dba308b4ff1ec7d5cbbc00739` |
| Branch | `otto-cli/b0944-slice5-float-decoder-exploration-2026-05-31` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/6173 |
| Changed files | 4 |
| Additions / deletions | +287 / -0 |

## Description

Per your 2026-05-31: ratified v0 (radix-point, unsigned) **and** *"lets try a few different designs unless it's obviously right."* It is **not** obviously right — the three decoder semantics occupy genuinely different range/precision regimes. This is **additive design exploration** (the ratified v0 `decode`/`fromValue` are unchanged); the pick is yours, then F#/C#/Rust parity (pt2) + the conformance harness (slice 6) follow.

`decoders.ts` adds `DecoderSemantics` + `decodeWith` + `characterize` — three designs that **share** the held-state logic (N-in-decoder ⇒ interpretation-superposed; N-in-value ⇒ value-superposed) and differ only in the certain arithmetic of "how the middle decodes the ends":

## Measured comparison (shape 4/3/4; numbers asserted by `decoders.test.ts`)
| design | arithmetic | max | min positive | distinct/2048 | fractions? |
|---|---|---|---|---|---|
| **radix-point** (v0) | `V / 2^mode` | 255 | 1/128 | 1152 | ✅ |
| **biased-exponent** | `V * 2^(mode-bias)` | 2040 | 1/16 | 1152 (redundant) | ✅ |
| **high-low-split** (posit-spirit) | `mantissa * 2^exponent` | **2^127** | 1 | 577 | ❌ integer-only |

- **radix-point** — simplest, fractional, modest range. Faithful to "the middle = the radix point."
- **biased-exponent** — ~8× the range, still fractional; the balanced float-like option (redundant reps).
- **high-low-split** — astronomically wide range, but with an unsigned exponent it's **integer-only** (no sub-unit precision); closest to posits/tapered, but needs a v2 signed split-exponent to regain fractions.

## Recommendation (you decide)
Keep **radix-point as the default** (simplest + fractional + ratified); keep **biased-exponent as a selectable wider-range mode**; document **high-low-split as posit-spirit but not default** (integer-only as formulated). The framework's Bayesian/probability consumers want sub-unit precision → favors radix-point or biased-exponent. `DecoderSemantics` keeps all three first-class so the choice is a *parameter*, not a rewrite.

**Verified: bun test 13/13** (8 v0 + 5 decoder/comparison). No F#/C#/Rust parity yet — that follows your pick.

🤖 Generated with [Claude Code](https://claude.com/claude-code)


## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | true |
| Total threads | 7 |
| Resolved threads | 7 |
| Unresolved threads | 0 |
| Total review comments | 7 |
| Total fix commits (touching thread paths) | 5 |

## Review threads

### Thread 1 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F6E9A`
- **Path**: `src/Core.TypeScript/tri-boolean-float/decoders.test.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-31T01:01:22Z on `src/Core.TypeScript/tri-boolean-float/decoders.test.ts`:29 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Import Tri type for the strict typecheck gate**

  `Tri` is used in this new test annotation but is never imported; the TypeScript CI gate in `.github/workflows/gate.yml:1080` runs `bun --bun tsc --noEmit -p tsconfig.json`, and locally `./node_modules/.bin/tsc --noEmit` reports TS2304 for each `Tri` here. Any PR containing this commit will fail the typecheck job until the type is imported or the annotation is removed.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 2 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F6E9C`
- **Path**: `src/Core.TypeScript/tri-boolean-float/decoders.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-31T01:01:22Z on `src/Core.TypeScript/tri-boolean-float/decoders.ts`:53 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Avoid 32-bit shifts for wider high-low-split shapes**

  For `high-low-split` shapes whose value field leaves 32 or more mantissa bits, these JavaScript bitwise operations truncate `V` to uint32 and mask the shift count modulo 32. For example, `applyDecoder(1, 1, 33, 1, 'high-low-split')` should decode exponent 0 and mantissa 1 as 1, but `V &gt;&gt;&gt; 32` and `(1 &lt;&lt; 32)` make the mask zero and return 0, so `decodeWith`/`characterize` publish wrong results for wider shapes unless arithmetic splitting is used or a &lt;=31-bit shape limit is enforced.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 3 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F6FBL`
- **Path**: `src/Core.TypeScript/tri-boolean-float/decoders.test.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-31T01:01:46Z on `src/Core.TypeScript/tri-boolean-float/decoders.test.ts`:2 (association: NONE)

  <pre>
  P0: With `verbatimModuleSyntax` enabled in `tsconfig.json`, `Tri` is a type-only export and must be imported with a type-only specifier. As written, `npm run typecheck`/`tsc --noEmit` will fail on this test file even though `bun test` passes.
  </pre>

### Thread 4 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F6FBV`
- **Path**: `src/Core.TypeScript/tri-boolean-float/decoders.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-31T01:01:47Z on `src/Core.TypeScript/tri-boolean-float/decoders.ts`:53 (association: NONE)

  <pre>
  P1: The high-low split uses JavaScript bitwise operators, which coerce operands to 32-bit integers and mask the shift count. Any shape with `valueBits - e &gt;= 32` will decode the exponent/mantissa incorrectly even though `FloatShape` allows arbitrary widths. Use numeric division/modulo so decoder semantics remain correct for wider shapes within `number` precision.
  </pre>

### Thread 5 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F6FBX`
- **Path**: `src/Core.TypeScript/tri-boolean-float/decoders.test.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-31T01:01:47Z on `src/Core.TypeScript/tri-boolean-float/decoders.test.ts`:37 (association: NONE)

  <pre>
  P2: This comment reverses the split direction for `mode(e)=1`: the implementation treats `e` as the number of high bits used for the exponent, so `e=1` means 1 exponent bit and 7 mantissa bits. The test vector below uses `e=7`, which is the case described by the current comment.
  </pre>

### Thread 6 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F6Flk`
- **Path**: `src/Core.TypeScript/tri-boolean-float/decoders.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-31T01:05:32Z on `src/Core.TypeScript/tri-boolean-float/decoders.ts`:104 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Iterate instead of spreading profile value sets**

  When `characterize` is used with a moderately larger `FloatShape` (for example 9 high bits, 1 decoder bit, and 9 low bits), `values` can contain hundreds of thousands of distinct numbers; spreading that set into `Math.max`/`Math.min` passes every value as a separate argument and V8 throws `RangeError: Maximum call stack size exceeded` before returning a profile. Since `FloatShape` is public and unconstrained, the new exported profiler should compute extrema by iterating the set rather than using argument spread.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 7 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F6GhI`
- **Path**: `src/Core.TypeScript/tri-boolean-float/decoders.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-31T01:12:05Z on `src/Core.TypeScript/tri-boolean-float/decoders.ts`:10 (association: NONE)

  <pre>
  P2: This looks like a relative file path, but the embedded ellipsis makes it non-resolvable for readers or tooling. Reword it as a description or use a complete path; the replacement below avoids a broken cross-reference.
  </pre>

## Fix commits (touching thread paths)

### `7f0a8278cd34115d6d379f7af5a2e21fc1d731dd` -- 2026-05-31T00:58:49Z -- `claude`

**Touched files (intersect with thread paths):**

- `src/Core.TypeScript/tri-boolean-float/decoders.test.ts`
- `src/Core.TypeScript/tri-boolean-float/decoders.ts`

**Message:**

```
feat(081KSV2WD0008QG0R00051XS0N slice 5): tri-boolean float decoder-semantics exploration…

… (3 designs compared; 13/13)

Per operator 2026-05-31: ratified v0 (radix-point, unsigned) AND 'lets try a few
different designs unless it's obviously right.' It is NOT obviously right -- the
three decoder semantics occupy genuinely different range/precision regimes.

Additive (the ratified v0 decode/fromValue are unchanged). New decoders.ts adds
a DecoderSemantics type + decodeWith + characterize, implementing 3 designs that
share the held-state logic (N-in-decoder => interpretation-superposed; N-in-value
=> value-superposed) and differ only in the certain-case arithmetic:

- radix-point (v0): value = V / 2^mode. max 255, min 1/128, fractions. Simplest.
- biased-exponent: value = V * 2^(mode-bias). max 2040, min 1/16, fractions,
  wider range, redundant reps. Balanced float-like.
- high-low-split (posit-spirit): mode = exponent-bit count; value = mantissa *
  2^exponent. max 2^127, but INTEGER-ONLY (unsigned exp) -- no sub-unit precision.

Measured profiles (characterize, asserted by tests; shape 4/3/4):
radix-point 255 / (1/128) / 1152-distinct / fractions;
biased-exponent 2040 / (1/16) / 1152-distinct / fractions;
high-low-split 2^127 / 1 / 577-distinct / integer-only.

Recommendation (operator decides): keep radix-point as default (simplest +
fractional + ratified); keep biased-exponent as a selectable wider-range mode;
document high-low-split as posit-spirit but not default (integer-only until a v2
signed split-exponent). The framework's Bayesian/probability consumers want
sub-unit precision -> favors radix-point or biased-exponent.

Verified: bun test 13/13 (8 v0 + 5 decoder/comparison). DecoderSemantics keeps
all three first-class so the choice is a parameter; cross-language parity (slice 5
pt2) + the conformance harness (slice 6) follow the operator's pick.

Comparison doc: docs/research/2026-05-31-...-decoder-semantics-comparison-...md.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
```

### `bf2e62734ade9fcf49860cd13981896f43b9110a` -- 2026-05-31T01:01:20Z -- `claude`

**Touched files (intersect with thread paths):**

- `src/Core.TypeScript/tri-boolean-float/decoders.test.ts`

**Message:**

```
fix(#6173): import the Tri TYPE in decoders.test.ts (tsc TS2304)

decoders.test.ts used the Tri type in (mode: [Tri, Tri, Tri]) but imported only
the T/F/N constants, not the type. bun test strips types so it passed locally;
CI's full tsc caught it (TS2304 Cannot find name 'Tri'). Added 'type Tri' to the
import. bun test still 13/13.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
```

### `0489702bcd1aee2f28035e9b665725fe5538e184` -- 2026-05-31T01:03:12Z -- `claude`

**Touched files (intersect with thread paths):**

- `src/Core.TypeScript/tri-boolean-float/decoders.test.ts`
- `src/Core.TypeScript/tri-boolean-float/decoders.ts`

**Message:**

```
fix(#6173): high-low-split uses arithmetic (not 32-bit bitwise) + cor…

…rect test comment (Codex/Copilot P1/P2)

Two real findings:
- decoders.ts high-low-split used V >>> mantBits and (1 << mantBits), which JS
  coerces to uint32 + masks the shift count mod 32 -> silently mis-decodes shapes
  with mantBits >= 32 (e.g. applyDecoder(1,1,33,1) wrongly returned 0 instead of
  1). FloatShape allows arbitrary widths, so this is a latent correctness bug.
  Replaced with division/modulo (scale = 2^mantBits; exponent = floor(V/scale);
  mantissa = V % scale) -- correct for any width within Number's 2^53 integer
  precision. Default shape (8 bits) behavior unchanged; the comparison doc's
  measured numbers (all < 32 bits) are unaffected.
- decoders.test.ts comment said 'mode(e)=1' while the vector + impl use e=7
  (the earlier fix updated lines 37-38 but not 36) -- corrected to e=7.

Added a wide-shape regression test (mantBits 32 + 40) via applyDecoder. bun test
14/14.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
```

### `ca422d82b50cb2f7b83ef039ae99fab199546f03` -- 2026-05-31T01:09:28Z -- `claude`

**Touched files (intersect with thread paths):**

- `src/Core.TypeScript/tri-boolean-float/decoders.ts`

**Message:**

```
fix(#6173): characterize uses single-pass reduce, not Math.max/min(..…

….set) (Codex P2)

Math.max(...values)/Math.min(...positives) spreads the value set as individual
args; for larger FloatShapes (e.g. 9/1/9 -> 262144 distinct values) that
overflows the call stack (RangeError). Replaced with an O(n) single-pass loop
computing max/minPositive/integersOnly without spreading (and without the
intermediate positives array). Default-shape profiles unchanged; bun test 14/14.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
```

### `6f89c2f14d1e53122905b078c56315abfdcedfc0` -- 2026-05-31T01:17:40Z -- `claude`

**Touched files (intersect with thread paths):**

- `src/Core.TypeScript/tri-boolean-float/decoders.ts`

**Message:**

```
fix(#6173): use full doc path in decoders.ts header comment, no ellip…

…sis (Copilot P2)

The header comment referenced the comparison doc with an ellipsis-truncated path
(non-resolvable for readers/tooling). Replaced with the complete path. Comment-only.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
```
