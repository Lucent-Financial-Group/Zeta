# PR #868 -- ts(B-0086): port 3 hygiene audit scripts (.sh→.ts) — slice 2 of TS/Bun migration

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 868 |
| Title | ts(B-0086): port 3 hygiene audit scripts (.sh→.ts) — slice 2 of TS/Bun migration |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-04-30T01:10:54Z |
| Merged at | 2026-04-30T01:13:15Z |
| Merge commit SHA | `b1dab4d658ed1a2b60508bae7370efb342de1005` |
| Branch | `lane-b/ts-bun-slice-2-hygiene-audits-2026-04-30` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/868 |
| Changed files | 4 |
| Additions / deletions | +1058 / -0 |

## Description

## Summary

Slice 2 of the TS/Bun migration. Cluster A (3 hygiene audit scripts, ~700 lines bash → ~1050 lines typed TS):

- `audit-machine-specific-content.{sh→ts}` — pattern-grep for user-home / harness-slug leaks
- `audit-git-hotspots.{sh→ts}` — high-churn file ranking via `git log --name-only`
- `audit-cross-platform-parity.{sh→ts}` — bash/PowerShell twin parity check

Per Gate B prerequisite (verified currency of `docs/best-practices/{typescript,bun,repo-scripting}.md` 1 day old, well within 30-day window).

## Patterns applied

Canonical from slice 1 (PR #866):
- Typed boundary objects, no stringly-formatted findings
- Literal-type union exit codes
- `ParseResult` discriminated-union for CLI args (no `process.exit` from parse paths)
- Structured spawn-failure classifier mirroring `../SQLSharp/tools/automation/format/process-runner.ts`
- `maxBuffer: 64 * 1024 * 1024` on `spawnSync`
- Named exports + `import.meta.main` entry guard

New this slice:
- **DST-friendly**: report-generating scripts inject a `Clock` interface — tests can substitute deterministic time; CLI uses `realClock()`. Per the universal-DST gate in `typescript.md`.
- **`ParseAcc` reducer + `reduceFlag` helper**: flag-heavy CLI parser pattern that brings cognitive complexity within the sonarjs 15-cap. Recorded in `slice-audits.md` for future reuse.

## Equivalence

Per-file bash-vs-TS verified:
- `audit-machine-specific-content`: byte-equivalent under default args
- `audit-git-hotspots`: byte-equivalent modulo `Generated:` timestamp
- `audit-cross-platform-parity`: byte-equivalent under `--summary`; report mode equivalent modulo `Run:` timestamp

## Coverage

Deferred (bash originals had no tests; slice-2 ports don't introduce a new module). Recorded explicitly in `slice-audits.md` per the DST-exempt-is-deferred-bug discipline applied to coverage.

## Test plan

- [x] `bun x tsc --noEmit` clean
- [x] `bun x eslint <slice files>` clean (strictTypeChecked + stylisticTypeChecked + sonarjs)
- [x] Bash-vs-TS equivalence per file (modulo timestamps in 2 of 3)
- [x] markdownlint clean on slice-audits.md
- [x] Slice-2 audit landed in `docs/trajectories/typescript-bun-migration/slice-audits.md`

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 6 |
| Resolved threads | 0 |
| Unresolved threads | 6 |
| Total review comments | 6 |
| Total fix commits (touching thread paths) | 1 |

## Review threads

### Thread 1 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM5-nEXu`
- **Path**: `tools/hygiene/audit-machine-specific-content.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-30T01:16:45Z on `tools/hygiene/audit-machine-specific-content.ts`:190 (association: NONE)

  <pre>
  This script prints human-oriented summaries via `console.log` (stdout). `docs/best-practices/repo-scripting.md` specifies stdout should be reserved for machine-parseable output (`--list`, `--help`) and that summaries/errors should go to stderr. Consider switching non-`--list` output to `process.stderr.write(...)` to keep the stdout/stderr contract consistent across hygiene audits.
  ```suggestion
        process.stderr.write(
          `machine-specific content gaps: ${String(count)}\n`,
        );
        for (const finding of findings) {
          process.stderr.write(`  ${formatFinding(finding)}\n`);
        }
        return 2;
      }
      process.stderr.write("machine-specific content gaps: 0 (clean)\n");
      return 0;
    }

    if (count &gt; 0) {
      process.stderr.write(
        `machine-specific content gaps: ${String(count)}\n`,
      );
      process.stderr.write("run with --list to see offending files\n");
    } else {
      process.stderr.write("machine-specific content gaps: 0 (clean)\n");
  ```
  </pre>

### Thread 2 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM5-nEX5`
- **Path**: `tools/hygiene/audit-git-hotspots.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-30T01:16:46Z on `tools/hygiene/audit-git-hotspots.ts`:205 (association: NONE)

  <pre>
  The tie-breaker for equal-touch files is different from the bash original. Bash uses `sort -rn`, which reverses the *entire* comparison (so ties also end up reverse-lexicographic), while this uses ascending `localeCompare`. If you want byte-equivalence with the .sh output, mirror `sort -rn`’s tie behavior; otherwise, record the intentional divergence in the slice audit / equivalence notes.
  ```suggestion
    // Sort by touches desc; for equal touches use reverse-lexicographic
    // filename order to mirror `sort -rn` in the bash original.
    tallies.sort(
      (a, b) =&gt;
        b.touches - a.touches ||
        (a.file &lt; b.file ? 1 : a.file &gt; b.file ? -1 : 0),
    );
  ```
  </pre>

### Thread 3 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM5-nEYC`
- **Path**: `tools/hygiene/audit-cross-platform-parity.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-30T01:16:46Z on `tools/hygiene/audit-cross-platform-parity.ts`:304 (association: NONE)

  <pre>
  This report includes a line that begins with `  + ...` (after indentation). markdownlint treats `+` as a list marker, and the repo explicitly bans wrapped continuation lines starting with `+` because it frequently triggers MD004/ul-style failures when these reports are checked in. Reword this line to avoid starting it with `+` (e.g., use “and …” or inline the reference).
  ```suggestion
      "  and memory/feedback_stay_bash_forever_implies_powershell_twin_obligation.md",
  ```
  </pre>

### Thread 4 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM5-nEYH`
- **Path**: `tools/hygiene/audit-cross-platform-parity.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-30T01:16:46Z on `tools/hygiene/audit-cross-platform-parity.ts`:45 (association: NONE)

  <pre>
  This script uses exit code `1` for usage/arg errors, but `docs/best-practices/repo-scripting.md` standardizes hygiene-audit exit codes on `0 | 2 | 64` (with `64` = EX_USAGE). Consider switching argument/usage errors to `64` (and updating `AuditExitCode`) so callers can treat all hygiene audits consistently.
  </pre>

### Thread 5 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM5-nEYN`
- **Path**: `docs/trajectories/typescript-bun-migration/slice-audits.md`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-30T01:16:46Z on `docs/trajectories/typescript-bun-migration/slice-audits.md`:152 (association: NONE)

  <pre>
  The slice log uses a placeholder PR reference (`PR #NNN`). This should be replaced with the actual PR number before merge so cross-references remain auditable.
  ```suggestion
  ## Slice 2 — 3 hygiene-audit-script ports (2026-04-30)
  ```
  </pre>

### Thread 6 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM5-nEYS`
- **Path**: `tools/hygiene/audit-machine-specific-content.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-30T01:16:47Z on `tools/hygiene/audit-machine-specific-content.ts`:13 (association: NONE)

  <pre>
  Header claims this scan also detects “Claude harness slugs”, but the only detection patterns implemented are home-directory path regexes (PATTERNS/PATTERN_NAMES). Either add the harness-slug patterns or update the header comment so it matches what the script actually checks.
  ```suggestion
  //   Scans tracked files for user-home paths and other machine-
  //   specific path leaks. Detect-only by default (per the not-yet-
  //   prevention-gate framing in row #23 / #47).
  ```
  </pre>

## Fix commits (touching thread paths)

### `3f7bc5984d6c72bfcfd9c32d75d5af82db1630b4` -- 2026-04-30T01:10:36Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `docs/trajectories/typescript-bun-migration/slice-audits.md`
- `tools/hygiene/audit-cross-platform-parity.ts`
- `tools/hygiene/audit-git-hotspots.ts`
- `tools/hygiene/audit-machine-specific-content.ts`

**Message:**

```
ts(B-0086): port 3 hygiene audit scripts (.sh→.ts) — slice 2 of TS/Bu…

…n migration

Cluster A from RESUME.md's slice-2 candidates:

- audit-machine-specific-content.ts (106 → ported, byte-equivalent)
- audit-git-hotspots.ts (250 → ported, equivalent modulo timestamp)
- audit-cross-platform-parity.ts (336 → ported, equivalent modulo timestamp)

Slice-2 audit substrate landed in
docs/trajectories/typescript-bun-migration/slice-audits.md per
Gate A merge gate.

Patterns applied (canonical from slice 1 + new this slice):

- Typed boundary objects (AuditFinding, FileSummary, AuditBuckets).
- Literal-type union exit codes (0|2|64, 0|64|128, 0|1|2 per script).
- ParseResult discriminated-union for CLI args (no process.exit
  from parse paths — slice-1 P2 fix continued).
- Structured spawn-failure classifier mirroring SQLSharp's
  process-runner.ts (slice-1 pattern).
- maxBuffer=64MiB on spawnSync (slice-1 review fix continued).
- Named exports + import.meta.main entry guard.
- DST-friendly: report-generating scripts inject a Clock interface
  (audit-git-hotspots, audit-cross-platform-parity) — tests can
  substitute deterministic time; CLI uses realClock(). Per the
  universal-DST gate in typescript.md.

New pattern this slice (recorded in slice-audits.md for future
reuse): ParseAcc reducer + reduceFlag helper for flag-heavy CLI
parsers. audit-git-hotspots used this to bring cognitive
complexity within the sonarjs 15-cap.

Equivalence verified per file:
- machine-specific-content: byte-equivalent under default args
- git-hotspots: byte-equivalent modulo Generated: timestamp
- cross-platform-parity: byte-equivalent under --summary; report
  mode equivalent modulo Run: timestamp

Coverage: deferred (bash originals had no tests; slice-2 ports
do not introduce a new module). Recorded in slice-audits.md per
the DST-exempt-is-deferred-bug discipline applied to coverage.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
```
