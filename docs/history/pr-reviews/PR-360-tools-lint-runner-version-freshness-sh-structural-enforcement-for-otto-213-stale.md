# PR #360 -- tools: lint/runner-version-freshness.sh — structural enforcement for Otto-213 stale-version lesson

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 360 |
| Title | tools: lint/runner-version-freshness.sh — structural enforcement for Otto-213 stale-version lesson |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-04-24T11:45:24Z |
| Merged at | 2026-04-25T05:08:36Z |
| Merge commit SHA | `d9f80b53f77a6d0a5ccae6b3cb0d2c44d6100702` |
| Branch | `tools/lint-runner-version-freshness-otto-214` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/360 |
| Changed files | 1 |
| Additions / deletions | +241 / -0 |

## Description

## Summary

Otto-214 shipping the tooling-level enforcement I proposed Otto-213. Memory-alone was not sufficient to stop the "write a stale version number" recurrence pattern (I did it 3× this session alone); this lint adds a CI-fail gate so the failure stops compounding.

## What the lint does

- Walks `.github/workflows/*.yml`
- Extracts `runs-on:` + `os:` matrix lines
- **Fails (exit 2)** on stale-label hits: `ubuntu-22.04`, `macos-14`, `macos-15`, `windows-2022`, etc.
- **Warns (exit 3)** if the allow-list itself is stale (>30 days since `LAST_VERIFIED`)
- Prints the canonical allow-list + authoritative GitHub docs URL on failure

Allow-list verified 2026-04-24 via [the standard-runners docs](https://docs.github.com/en/actions/how-tos/write-workflows/choose-where-workflows-run/choose-the-runner-for-a-job#standard-github-hosted-runners-for-public-repositories).

## First-run output on current main

13 stale-label hits across 3 workflows:

```
STALE RUNNER LABEL(S) in .github/workflows/codeql.yml:
  108:    runs-on: ubuntu-22.04
  265:    runs-on: ubuntu-22.04
STALE RUNNER LABEL(S) in .github/workflows/gate.yml:
  [8 hits including matrix + comment-block]
STALE RUNNER LABEL(S) in .github/workflows/github-settings-drift.yml:
  54:    runs-on: ubuntu-22.04
```

`gate.yml` hits are cleaned up by PR #359 (already in queue). `codeql.yml` + `github-settings-drift.yml` need separate follow-up PRs.

## Sequencing — detect-only first, enforce when baseline green

Same pattern as `audit-cross-platform-parity.sh` (FACTORY-HYGIENE row #51): ship the detector, clean up the existing baseline, THEN wire enforcement into `gate.yml` lint job. Premature enforcement blocks every current PR. Concrete plan:

1. **This PR** — ships the lint tool. Not wired into CI yet.
2. **PR #359** — clears `gate.yml` stale refs (already in queue)
3. **Follow-up PRs** — clear `codeql.yml` + `github-settings-drift.yml` stale refs
4. **Enforcement PR** — wire `runner-version-freshness.sh` into `gate.yml` `lint (...)` job chain after baseline is clean

## Portability

- macOS default-bash-3.x compatible (no `mapfile`)
- GNU `date` + BSD `date` fallback for the `LAST_VERIFIED` age check
- Shellcheck clean (SC2001 note acknowledged as intentional)

## Composes with

- Otto-213 `version-numbers-always-websearch` memory (the durable lesson this tool structurally enforces)
- Otto-212 use-latest-tags + security-hygiene directive
- Otto-210/211 macOS-is-free + M1-not-Intel corrections
- FACTORY-HYGIENE row #43 safe-pattern compliance

## Test plan

- [x] Detects all 13 current stale refs on main (exit 2)
- [x] Prints canonical allow-list + docs URL on failure
- [x] Shellcheck clean
- [x] Portable to bash 3.x (macOS default)
- [ ] CI wire-up: separate follow-up after baseline green

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | true |
| Total threads | 16 |
| Resolved threads | 16 |
| Unresolved threads | 0 |
| Total review comments | 32 |
| Total fix commits (touching thread paths) | 3 |

## Review threads

### Thread 1 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59YjCS`
- **Path**: `tools/lint/runner-version-freshness.sh`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-04-24T11:47:50Z on `tools/lint/runner-version-freshness.sh`:149 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Exclude comments when scanning for stale runner labels**

  The grep pipeline matches any line containing `- &lt;stale_label&gt;` before checking whether it is YAML data, so comment bullets like `# - ubuntu-22.04` are treated as real runner pins and make the script fail. This creates false positives in workflows that keep rollback/history notes, and it can block CI even when every actual `runs-on` value is current.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-25T05:00:13Z on `tools/lint/runner-version-freshness.sh`:149 (association: MEMBER)

    <pre>
    Fixed in `55eeb4c` — added a comment-stripping pre-filter (`grep -vE '^[[:space:]]*#'`) before the runner-label scan, so stale labels mentioned in YAML comments don't trigger false positives. The pattern matches any line whose first non-whitespace char is `#`.
    </pre>

### Thread 2 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59YjCX`
- **Path**: `tools/lint/runner-version-freshness.sh`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-04-24T11:47:50Z on `tools/lint/runner-version-freshness.sh`:179 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Keep allow-list age warning as a non-failing path**

  The header documents this as a warning-only freshness check, but when `LAST_VERIFIED` is older than 30 days the script exits with code `3`, which is a failing exit status for a normal GitHub Actions shell step. That means this will hard-fail CI on age alone unless every caller adds special handling, contradicting the stated "warn (not fails)" behavior.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-25T05:00:15Z on `tools/lint/runner-version-freshness.sh`:179 (association: MEMBER)

    <pre>
    Fixed in `55eeb4c` — header explicitly documents the freshness check as warning-only, so changed the warn-only path from `exit 3` to `exit 0` (warning still printed to stderr by `_verify_age_ok`). Stale-version detection still exits 2 (real failure, distinct from freshness-stale). Operators see the warning and bump LAST_VERIFIED on the next refresh tick without CI-fail noise.
    </pre>

### Thread 3 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59Yjvi`
- **Path**: `tools/lint/runner-version-freshness.sh`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-24T11:48:39Z on `tools/lint/runner-version-freshness.sh`:163 (association: NONE)

  <pre>
  P0: `stale_pattern` is built from raw label strings and then used as an ERE. Labels like `ubuntu-22.04` contain `.` which is a regex wildcard, so this will produce false positives/negatives (e.g., `ubuntu-22x04`). Prefer fixed-string matching (e.g., grep -F over each label) or escape regex metacharacters when building the alternation.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-25T05:00:17Z on `tools/lint/runner-version-freshness.sh`:163 (association: MEMBER)

    <pre>
    Fixed in `55eeb4c` — added `escape_for_regex` helper that escapes . + * ? ( ) [ ] { } | \\ / before alternation. `ubuntu-22.04` now matches literally instead of `ubuntu-22&lt;any&gt;04`. Smoke-test confirmed the script catches actual ubuntu-22.04 in gate.yml after the fix (real finding).
    </pre>

### Thread 4 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59Yjv4`
- **Path**: `tools/lint/runner-version-freshness.sh`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-24T11:48:40Z on `tools/lint/runner-version-freshness.sh`:149 (association: NONE)

  <pre>
  P0: The use of `\b` word-boundary (and `\bos:\b`) is not portable to BSD grep (macOS default); `\b` is treated as backspace in POSIX ERE, so the matches/hits detection can break despite the script claiming bash-3.x/macOS portability. Use a portable boundary strategy (e.g., explicit YAML delimiters/whitespace checks) or avoid regex boundaries by doing fixed-string label checks against already-filtered lines.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-25T05:00:18Z on `tools/lint/runner-version-freshness.sh`:149 (association: MEMBER)

    <pre>
    Fixed in `55eeb4c` — replaced `\\b` word-boundaries (BSD-grep-incompatible; treated as backspace under POSIX ERE) with explicit non-word boundary classes: `([^A-Za-z0-9_]|^)` for start, `([^A-Za-z0-9_]|$)` for end. Works in both GNU and BSD grep without backrefs.
    </pre>

### Thread 5 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59YjwK`
- **Path**: `tools/lint/runner-version-freshness.sh`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-24T11:48:40Z on `tools/lint/runner-version-freshness.sh`:149 (association: NONE)

  <pre>
  P1: When files are passed explicitly, nonexistent/unreadable paths are silently ignored (`grep ... 2&gt;/dev/null || true`), which can yield a misleading "ok" even though nothing was actually linted. Consider validating each CLI argument with `-f`/readable checks and exiting with code 1 (as documented) on invalid inputs.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-25T05:00:20Z on `tools/lint/runner-version-freshness.sh`:149 (association: MEMBER)

    <pre>
    Fixed in `55eeb4c` — added explicit `[ ! -r "$file" ]` precheck before the grep that fails loud (`fail=1`, ERROR message to stderr) when an explicit file argument doesn't exist or isn't readable. Closes the 'silent ok for nothing-actually-linted' gap that the `2&gt;/dev/null || true` was masking.
    </pre>

### Thread 6 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59Yjwa`
- **Path**: `tools/lint/runner-version-freshness.sh`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-24T11:48:40Z on `tools/lint/runner-version-freshness.sh`:75 (association: NONE)

  <pre>
  P1 (convention drift): `ALLOWED_LABELS` includes rolling aliases (`ubuntu-latest`, `windows-latest`, `macos-latest`), but repo docs describe a "pinned major-OS-version labels (not ubuntu-latest / macos-latest)" discipline (docs/research/ci-retractability-inventory.md:204-206). If the intent is to enforce pinned-version freshness, consider removing `*-latest` from the canonical list (or flagging them separately) to keep the lint aligned with that documented policy.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-25T05:00:22Z on `tools/lint/runner-version-freshness.sh`:75 (association: MEMBER)

    <pre>
    Fixed in `55eeb4c` — removed the rolling aliases (ubuntu-latest / windows-latest / macos-latest) from `ALLOWED_LABELS` and added a separate `ROLLING_ALIASES` forbidden list with its own scan. Contributors typing `ubuntu-latest` now get a distinct error message ('ROLLING-ALIAS RUNNER LABEL ... use a pinned version per repo convention') from the stale-version error, with the rationale (CI-reproducibility-by-pinning) called out in the comment block. Aligns the script with the repo's pinned-major-OS-version-labels convention.
    </pre>

### Thread 7 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59j8bG`
- **Path**: `tools/lint/runner-version-freshness.sh`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-04-25T05:01:40Z on `tools/lint/runner-version-freshness.sh`:183 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Include quoted matrix entries in stale-label scan**

  The stale-label detector misses common YAML matrix syntax where `os` values are quoted (for example `- "ubuntu-22.04"`). In this path, the prefilter only keeps bullet lines that match `- &lt;stale_label&gt;` exactly, so quoted bullets are dropped before the second grep runs; I verified this with a workflow using `runs-on: ${{ matrix.os }}` and a quoted stale entry, which exited 0. This creates a false negative that allows stale runner pins to pass the lint.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-25T05:06:13Z on `tools/lint/runner-version-freshness.sh`:183 (association: MEMBER)

    <pre>
    Fixed in `391b045` — updated matrix-entry prefilter from `^[[:space:]]*-[[:space:]]+` to `^[[:space:]]*-[[:space:]]+(['\"]?)` which optionally consumes a leading single or double quote. Smoke-tested with mixed quoting (`- "ubuntu-22.04"` and `- 'macos-15'`): both now correctly caught as stale.
    </pre>

### Thread 8 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59j8bK`
- **Path**: `tools/lint/runner-version-freshness.sh`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-04-25T05:01:40Z on `tools/lint/runner-version-freshness.sh`:179 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Strip inline YAML comments before matching labels**

  Only full-line comments are removed, so stale labels inside trailing comments still match and fail the lint (for example `runs-on: ubuntu-24.04 # was ubuntu-22.04`). Because matching is done against the full line content, these inline notes become false positives and can block CI even when the actual runner label is current.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-25T05:06:15Z on `tools/lint/runner-version-freshness.sh`:179 (association: MEMBER)

    <pre>
    Fixed in `391b045` — added a second sed pass after the full-line-comment strip: `sed -E 's/[[:space:]]+#.*$//'` strips everything after the first ` #` (YAML-spec comment-start sentinel: `#` must be preceded by whitespace to start a comment). Smoke-tested: `runs-on: ubuntu-24.04 # was ubuntu-22.04` no longer falsely flags. Conservative — doesn't try to handle `#` inside quoted strings (rare in workflow YAML); tolerates that corner case at the cost of an occasional false positive.
    </pre>

### Thread 9 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59j9zN`
- **Path**: `tools/lint/runner-version-freshness.sh`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-04-25T05:08:57Z on `tools/lint/runner-version-freshness.sh`:195 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Validate runner labels against allow-list, not stale subset**

  The detection path only searches for labels listed in `STALE_LABELS`, so stale pins not enumerated there are silently accepted. In this commit’s script, a workflow containing `runs-on: ubuntu-18.04` exits 0, even though the file header says the lint should fail pins older than latest and `ALLOWED_LABELS` is treated as canonical. This creates a durable false-negative gap whenever an older label is still valid (or a new stale variant appears) until someone manually appends it to `STALE_LABELS`; the gate should derive failures from “not in allow-list” semantics instead of a partial deny-list.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-25T05:16:02Z on `tools/lint/runner-version-freshness.sh`:195 (association: MEMBER)

    <pre>
    Fixed in follow-up [PR #432](https://github.com/Lucent-Financial-Group/Zeta/pull/432) — added a third scan: extract `runs-on: &lt;value&gt;` and flag anything that is NOT in (ALLOWED_LABELS ∪ ROLLING_ALIASES) and NOT in expression form `${{ ... }}`. Distinct error class 'NOT-ON-ALLOW-LIST RUNNER LABEL'. Now any label invented after this script's last refresh (e.g. `ubuntu-30.04`) gets caught even if the operator forgot to add it to STALE_LABELS.
    </pre>

### Thread 10 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59j9zO`
- **Path**: `tools/lint/runner-version-freshness.sh`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-04-25T05:08:57Z on `tools/lint/runner-version-freshness.sh`:188 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Guard comment-stripping grep from set -e aborts**

  The `uncommented` assignment runs `grep -vE ... | sed ...` under `set -euo pipefail`, but `grep -v` returns exit code 1 when a file has no non-comment lines. In that case (empty or comment-only workflow file), the command substitution fails and the script exits immediately with status 1 and no lint message, turning a normal no-match condition into an unexpected hard failure in CI.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-25T05:16:05Z on `tools/lint/runner-version-freshness.sh`:188 (association: MEMBER)

    <pre>
    Fixed in follow-up [PR #432](https://github.com/Lucent-Financial-Group/Zeta/pull/432) — added `|| true` to neutralise the no-match exit 1 from `grep -vE`. Same root cause as P0 below; the comment-strip pipeline + sed combination is robust under `set -euo pipefail` now.
    </pre>

### Thread 11 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59j97l`
- **Path**: `tools/lint/runner-version-freshness.sh`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-25T05:09:52Z on `tools/lint/runner-version-freshness.sh`:174 (association: NONE)

  <pre>
  P1: Unreadable/missing workflow files are currently counted in `fail=1`, which later forces an exit 2 (“stale labels detected”). That makes usage/environment errors indistinguishable from stale-runner findings and contradicts the header’s “exit 1 environment/usage error”. Consider tracking a separate `usage_error` flag (or separate exit path) so missing/unreadable inputs exit 1 while stale/rolling hits exit 2.
  ```suggestion
    # This is a usage/environment problem, not a stale-label
    # finding, so exit with the documented status 1 instead of
    # flowing through the stale-runner `fail` path.
    if [ ! -r "$file" ]; then
      echo "ERROR: cannot read $file (does not exist or unreadable)" &gt;&amp;2
      exit 1
  ```
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-25T05:16:06Z on `tools/lint/runner-version-freshness.sh`:174 (association: MEMBER)

    <pre>
    Fixed in follow-up [PR #432](https://github.com/Lucent-Financial-Group/Zeta/pull/432) — split `fail` (label findings → exit 2) from `env_error` (unreadable file / env problem → exit 1). Callers can now distinguish 'something broke in the environment' from 'stale labels found in workflow'. Header exit-code contract updated.
    </pre>

### Thread 12 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59j97s`
- **Path**: `tools/lint/runner-version-freshness.sh`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-25T05:09:52Z on `tools/lint/runner-version-freshness.sh`:142 (association: NONE)

  <pre>
  P1 (codebase convention): This script relies on being run from repo root (`find .github/workflows ...`). Other `tools/lint/*.sh` scripts first resolve `REPO_ROOT` and `cd` there (e.g., `tools/lint/no-empty-dirs.sh:36-37`). Consider doing the same here so `runner-version-freshness.sh` behaves consistently when invoked from subdirectories or CI steps with a different working directory.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-25T05:16:08Z on `tools/lint/runner-version-freshness.sh`:142 (association: MEMBER)

    <pre>
    Fixed in follow-up [PR #432](https://github.com/Lucent-Financial-Group/Zeta/pull/432) — script now resolves REPO_ROOT via `git rev-parse --show-toplevel` and `cd`s there before file discovery. Aligns with other `tools/lint/*.sh` scripts (`tools/lint/no-empty-dirs.sh` does the same). Default `find .github/workflows ...` discovery now works regardless of caller's cwd.
    </pre>

### Thread 13 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59j97u`
- **Path**: `tools/lint/runner-version-freshness.sh`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-25T05:09:53Z on `tools/lint/runner-version-freshness.sh`:116 (association: NONE)

  <pre>
  P2: `now_epoch` is computed in UTC (`date -u +%s`), but the BSD `date -j -f ... +%s` branch uses local time by default. That can skew `age_days` by ~1 day depending on timezone/DST and make the “&gt;30 days” warning flip early/late. Consider forcing both computations to the same timezone (e.g., `TZ=UTC` for both branches) before dividing by 86400.
  ```suggestion
    # Linux (GNU date) and macOS (BSD date), normalized to UTC.
    local now_epoch last_epoch age_days
    now_epoch="$(TZ=UTC date +%s)"
    if TZ=UTC date -j -f "%Y-%m-%d" "$LAST_VERIFIED" "+%s" &gt;/dev/null 2&gt;&amp;1; then
      last_epoch="$(TZ=UTC date -j -f "%Y-%m-%d" "$LAST_VERIFIED" "+%s")"
    elif TZ=UTC date -d "$LAST_VERIFIED" "+%s" &gt;/dev/null 2&gt;&amp;1; then
      last_epoch="$(TZ=UTC date -d "$LAST_VERIFIED" "+%s")"
  ```
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-25T05:16:09Z on `tools/lint/runner-version-freshness.sh`:116 (association: MEMBER)

    <pre>
    Fixed in follow-up [PR #432](https://github.com/Lucent-Financial-Group/Zeta/pull/432) — both BSD and GNU `date` branches now run under `TZ=UTC` so age_days computes in a single timezone. Closes the ±1 day skew that previously could cross the 30-day boundary on DST transitions or non-UTC machines.
    </pre>

### Thread 14 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59j97z`
- **Path**: `tools/lint/runner-version-freshness.sh`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-25T05:09:53Z on `tools/lint/runner-version-freshness.sh`:9 (association: NONE)

  <pre>
  P1 (codebase convention): The header comments include factory-process lineage tokens like `Otto-213` / `Otto-214`. This violates the documented “code comments explain code, not history” discipline (docs/FACTORY-DISCIPLINE.md:62-71) and will be flagged as a *new* violation by `tools/lint/doc-comment-history-audit.sh` (TOKEN_PATTERN includes `Otto-[0-9]+` at tools/lint/doc-comment-history-audit.sh:78). Suggest removing the Otto references from this file’s comments (keep rationale in the PR description / hygiene-history instead).
  ```suggestion
  # to a version older than the current standard label.
  # Training-data version numbers are stale by definition;
  # structural lint is the enforcement mechanism that
  # memory alone does not provide.
  ```
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-25T05:16:11Z on `tools/lint/runner-version-freshness.sh`:9 (association: MEMBER)

    <pre>
    Fixed in follow-up [PR #432](https://github.com/Lucent-Financial-Group/Zeta/pull/432) — removed Otto-213 / Otto-214 lineage tokens from the header. Replaced 'Otto-213 durable compounding-failure mitigation' with the structural rationale ('training-data version numbers decay; structural lint enforces'). Per the 'code comments explain code, not history' discipline, the structural reason for the script is what belongs in the header; lineage stays in PR descriptions / commit logs.
    </pre>

### Thread 15 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59j974`
- **Path**: `tools/lint/runner-version-freshness.sh`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-25T05:09:53Z on `tools/lint/runner-version-freshness.sh`:187 (association: NONE)

  <pre>
  P0: With `set -euo pipefail`, the `grep -vE ... | sed ...` pipeline will cause the script to exit if the workflow file has no non-comment lines (because `grep -v` returns status 1 when it outputs nothing, and `pipefail` propagates that). Make the comment-stripping step tolerant of empty output (e.g., add `|| true` to the pipeline, or implement both passes in a single `sed` so no command returns 1 for “no matches”).
  ```suggestion
      sed -E \
        -e '/^[[:space:]]*#/d' \
        -e 's/[[:space:]]+#.*$//' \
        "$file"
  ```
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-25T05:16:13Z on `tools/lint/runner-version-freshness.sh`:187 (association: MEMBER)

    <pre>
    Fixed in follow-up [PR #432](https://github.com/Lucent-Financial-Group/Zeta/pull/432) — added `|| true` to the `grep -vE ... | sed ...` pipeline. Under `set -euo pipefail`, `grep -v` returns 1 when its filter matches every line (no output → exit 1), which previously aborted the script if the workflow file consisted only of comments. The `|| true` neutralises that case.
    </pre>

### Thread 16 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59j975`
- **Path**: `tools/lint/runner-version-freshness.sh`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-25T05:09:53Z on `tools/lint/runner-version-freshness.sh`:237 (association: NONE)

  <pre>
  P1: The documented exit-code contract doesn’t match the implementation. The header says the allow-list staleness warning is exit 3, but the `warn` path currently exits 0. If callers need to distinguish “fresh ok” vs “ok but needs allow-list refresh”, consider actually exiting 3 (and letting CI treat it as non-fatal if desired), or update the header/PR description to reflect exit 0.
  ```suggestion
    # Return the documented warning exit code for a stale
    # allow-list verification timestamp. The warning has
    # already been printed to stderr by _verify_age_ok, and
    # callers can treat exit 3 as non-fatal if desired.
    exit 3
  ```
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-25T05:16:14Z on `tools/lint/runner-version-freshness.sh`:237 (association: MEMBER)

    <pre>
    Fixed in follow-up [PR #432](https://github.com/Lucent-Financial-Group/Zeta/pull/432) — reconciled the exit-code contract. Old header claimed 'exit 3 for freshness warning' but the earlier (now-merged) reviewer feedback wanted warning-only to exit 0 so it doesn't fail CI. Header updated: 'exit 0 even with freshness warning printed to stderr; the warning is non-fatal'. Removed exit 3 from the contract; now only 0/1/2.
    </pre>

## Fix commits (touching thread paths)

### `b44c0b8967e3ece4e567897077907ac7f40919d7` -- 2026-04-24T11:45:02Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/lint/runner-version-freshness.sh`

**Message:**

```
tools: lint/runner-version-freshness.sh — structural enforcement for …

…Otto-213 durable lesson

Otto-214 implementation of the tooling-level enforcement
I proposed Otto-213. Memory-alone was not sufficient to
stop the "write a stale version number" recurrence
pattern; this script adds a CI-fail gate.

Behavior:

- Walks .github/workflows/*.yml files
- Extracts runs-on: + os: matrix lines
- Fails (exit 2) if any line references a STALE runner
  version (ubuntu-22.04, macos-14, macos-15, windows-2022,
  ubuntu-20.04, macos-13, macos-15-intel, etc.)
- Warns (exit 3) if the allow-list itself is stale (>30
  days since LAST_VERIFIED)
- Prints the canonical list of ALLOWED labels on failure
  + the authoritative GitHub docs URL for re-verification

Allow-list verified 2026-04-24 via
https://docs.github.com/en/actions/how-tos/write-workflows/choose-where-workflows-run/choose-the-runner-for-a-job#standard-github-hosted-runners-for-public-repositories
exact quote "Use of the standard GitHub-hosted runners
is free and unlimited on public repositories."

First-run detects 13 stale-label hits across codeql.yml,
gate.yml, github-settings-drift.yml (plus stale comment-
block references in gate.yml from the pre-correction
history). These will be cleaned up by PR #359 for
gate.yml; codeql.yml + github-settings-drift.yml need
separate follow-up PRs.

Does NOT wire into gate.yml automatically — separate
step to add the lint check after the baseline is green.
Premature enforcement would block every current PR.
Sequencing: (1) this PR ships the tool; (2) follow-up
PRs clean up existing stale refs (gate.yml already
covered by #359; others queued); (3) once baseline is
clean, add to gate.yml lint job.

Composes with:

- Otto-213 version-numbers-require-websearch memory
- Otto-212 use-latest-tags + security-hygiene directive
- Otto-210/211 macOS-is-free + M1-not-Intel corrections
- FACTORY-HYGIENE row #43 safe-pattern compliance
- Analogous pattern to audit-cross-platform-parity.sh
  (detect-only-first, enforce-when-baseline-green)

Test plan:

- Runs clean when no stale labels present
- Exits 2 with clear message when stale labels present
- Warns when allow-list >30 days old
- Shellcheck clean (SC2001 note acknowledged; the
  non-bash-4 sed-style substitution is intentional for
  macOS default-bash-3.x compatibility per FACTORY-
  HYGIENE row #51 cross-platform parity)
- Portable: no mapfile (bash 4+ only); uses while-read
  loop pattern that works in bash 3.x

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
```

### `55eeb4ce6b186fba0b80c6b48d3872a9dcac0bd0` -- 2026-04-25T04:59:39Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/lint/runner-version-freshness.sh`

**Message:**

```
drain(#360 P0×2 + P1+P2+P1+P1 Codex): regex-escape + BSD-grep portabl…

…e + comment-strip + rolling-alias forbidden + warn-only exit

Six Codex findings on tools/lint/runner-version-freshness.sh:

P0 (line 133) — regex-metachar escape:
`stale_pattern` was built from raw label strings; `.` in
ubuntu-22.04 was a regex wildcard, producing false matches/
misses. Added `escape_for_regex` helper that escapes . + *
? ( ) [ ] { } | \ / before alternation.

P0 (line 149) — BSD-grep portability:
`\b` word-boundary doesn't work in BSD grep (macOS default;
treated as backspace per POSIX ERE). Replaced with explicit
non-word boundaries: `([^A-Za-z0-9_]|^)` start +
`([^A-Za-z0-9_]|$)` end, expressed without backrefs so it
works in both GNU and BSD grep.

P1 (line 149-1) — exclude comments:
Stale-label-in-comment was triggering false positives. Added
a comment-stripping pre-filter (`grep -vE '^[[:space:]]*#'`)
so YAML comments are excluded from the scan.

P1 (line 149-2) — explicit-file-not-found masking:
`grep ... 2>/dev/null || true` silently swallowed missing-
file errors and reported 'ok' for nothing-actually-linted.
Added an explicit `[ ! -r "$file" ]` precheck that fails
loud (exit 2) rather than passing silent.

P1 (line 73) — rolling-aliases forbidden by convention:
ALLOWED_LABELS included ubuntu-latest / windows-latest /
macos-latest, contradicting the repo convention of pinned
major-OS-version labels. Removed from ALLOWED_LABELS, added
a separate ROLLING_ALIASES forbidden list, added a
distinct error-class scan ('ROLLING-ALIAS RUNNER LABEL') so
contributors get a different error message than for
stale-version pins. Same fail=1 flag, different operator
message.

P2 (line 179) — warn-only exit on stale freshness:
Header documents this as warning-only; code exited 3 (which
some CI configurations treat as failure). Updated to exit 0
on stale-freshness-only path; warning is still printed to
stderr. Stale-version-detection still exit 2 (a real failure).

Smoke-test note: the new script now flags ubuntu-22.04 in
gate.yml as stale (real finding) — exit 2 with the expected
output. gate.yml's own runner-pin upgrade is out of scope
for this PR; will land separately.
```

### `391b045ab4cb69aa548509f8e38d4e807e51a6c2` -- 2026-04-25T05:05:56Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/lint/runner-version-freshness.sh`

**Message:**

```
drain(#360 P1+P2 Codex): quoted-matrix-entries + inline-comment strip…

…ping

Two more substantive Codex findings:

P1 (line 183) — quoted matrix entries missed:
The matrix-entry prefilter was `^[[:space:]]*-[[:space:]]+`
which only matched bare `- <label>`. Common YAML syntax
`- "ubuntu-22.04"` or `- 'macos-15'` was being missed.
Updated prefilter to `^[[:space:]]*-[[:space:]]+(['\"]?)`
which optionally consumes a leading single or double quote.
Smoke-tested with mixed quoting + matrix block: catches both
forms now.

P2 (line 179) — trailing inline comments not stripped:
`runs-on: ubuntu-24.04 # was ubuntu-22.04` was falsely
flagging `ubuntu-22.04` in the trailing comment. Added a
second sed pass: `sed -E 's/[[:space:]]+#.*$//'` strips
everything after the first ` #` (YAML-spec comment-start
sentinel with required leading space). Conservative: doesn't
handle `#` inside quoted strings (rare in workflow YAML).
Smoke-tested: trailing comments correctly stripped.
```
