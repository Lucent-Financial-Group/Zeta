# PR #2381 -- claim(B-0347): smallest safe slice — TS audit tool for carved-skill-description budget

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 2381 |
| Title | claim(B-0347): smallest safe slice — TS audit tool for carved-skill-description budget |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-05-10T00:12:01Z |
| Merged at | 2026-05-10T00:15:37Z |
| Merge commit SHA | `79f5cc23fd3b189445a60af59b69045e99eb69af` |
| Branch | `claim/b0347-carved-sentence-skill-descriptions-smallest-slice-riven-2026-05-09` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/2381 |
| Changed files | 1 |
| Additions / deletions | +66 / -0 |

## Description

## Summary
Smallest safe slice of B-0347 (P1, broad M-effort "carve 200+ skill descriptions").
Re-decomposed on assumption that "atomic" label was mistake: instead of touching 242 files, deliver a focused TS diagnostic tool (Rule 0 compliant) as the entry point. Tool lives at `tools/skill-carver/audit-descriptions.ts`, emits JSON report of description lengths vs 120-char target.

## One bounded step
- Created + committed + pushed the audit tool only.
- No SKILL.md edits, no doc changes, no broad refactors.
- Prepares carve-pass + spot-check routing in follow-up slice.

## Focused checks (included per rules)
- `bun run tools/skill-carver/audit-descriptions.ts`: clean run, reports 242 skills, 242 exceed 120 chars (max 3717), droppedEstimate 42 — confirms original symptom.
- `dotnet build -c Release` (root checkout, pre-write): 0 Warning(s) 0 Error(s).
- Worktree build note: requires restore (expected, no F# delta); root gate is authoritative.
- Branch pushed from dedicated worktree; root checkout untouched.

## Acceptance for this slice
- Tool runs, surfaces the budget violation data.
- PR opened against LFG only.
- Next slice can consume the report to drive carve edits safely.

## Evidence
- Backlog: `docs/backlog/P1/B-0347-carved-sentence-skill-descriptions-routing-budget.md`
- Trajectory context: factory-trajectory-surface + skill-expert owner
- Refresh-worldview and active RESUME.md reads completed before work.

Co-Authored-By: Grok <noreply@x.ai>

Made with [Cursor](https://cursor.com)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 7 |
| Resolved threads | 7 |
| Unresolved threads | 0 |
| Total review comments | 7 |
| Total fix commits (touching thread paths) | 1 |

## Review threads

### Thread 1 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6A3MeY`
- **Path**: `tools/skill-carver/audit-descriptions.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-10T00:14:36Z on `tools/skill-carver/audit-descriptions.ts`:48 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Fail fast on unreadable skill files**

  Do not silently swallow I/O errors here: if any `SKILL.md` is unreadable or missing, this audit undercounts `totalSkills`/`exceedingBudget` while still exiting successfully, which can produce a false “all clear” and mis-prioritize the carve pass. This is especially risky when the tool is used as a gating diagnostic, because a single permissions/path issue currently looks identical to a compliant skill set.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 2 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6A3Mea`
- **Path**: `tools/skill-carver/audit-descriptions.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-10T00:14:36Z on `tools/skill-carver/audit-descriptions.ts`:60 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Guard max length calculation for empty reports**

  Handle the empty-report case before calling `Math.max(...)`: when `reports` is empty (e.g., parser drift, missing frontmatter, or no readable files), `Math.max` returns `-Infinity`, which `JSON.stringify` serializes as `null`. That masks a failed scan and can break downstream consumers that expect `maxLen` to be a number.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 3 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6A3Mg0`
- **Path**: `tools/skill-carver/audit-descriptions.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-10T00:14:54Z on `tools/skill-carver/audit-descriptions.ts`:28 (association: NONE)

  <pre>
  P1 (codebase_conventions): Tool scripts in this repo typically export a `main(argv)` entrypoint and only invoke `process.exit(main(...))` under `if (import.meta.main)` (e.g., tools/backlog/generate-index.ts:266-290; tools/budget/daily-cost-report.ts:249-285). This file defines a local `main()` and unconditionally calls it, which makes it harder to reuse/test and diverges from the established pattern.
  </pre>

### Thread 4 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6A3Mg8`
- **Path**: `tools/skill-carver/audit-descriptions.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-10T00:14:54Z on `tools/skill-carver/audit-descriptions.ts`:23 (association: NONE)

  <pre>
  P1 (bug): `parseDescription` searches for the first `^description:` anywhere in the file, not specifically within the YAML frontmatter block. If a skill is missing `description` in frontmatter (or has a commented-out one), this can accidentally match a `description:` inside the body (e.g., YAML snippets) and report incorrect lengths. Consider parsing only between the initial `---` markers (frontmatter) and extracting `description` from that region.

  </pre>

### Thread 5 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6A3Mg9`
- **Path**: `tools/skill-carver/audit-descriptions.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-10T00:14:55Z on `tools/skill-carver/audit-descriptions.ts`:49 (association: NONE)

  <pre>
  P1 (maintainability): The `catch { // skip unreadable }` swallows all I/O and parse failures silently, which can mask missing `SKILL.md` files or permission issues and make the JSON totals hard to trust. Emitting a structured error entry (or at least counting/reporting missing/unreadable skills) would keep the audit actionable when the tree has anomalies.
  </pre>

### Thread 6 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6A3Mg-`
- **Path**: `tools/skill-carver/audit-descriptions.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-10T00:14:55Z on `tools/skill-carver/audit-descriptions.ts`:55 (association: NONE)

  <pre>
  P1 (bug): `totalSkills` is derived from `reports.length`, which currently counts only skills where a description was successfully parsed. That can undercount total skill directories and make `droppedEstimate`/budget ratios misleading if any skill is missing a description or had a read error. Consider basing totals on `skillDirs.length` and separately tracking parsed/missing/error counts.
  </pre>

### Thread 7 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6A3MhA`
- **Path**: `tools/skill-carver/audit-descriptions.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-10T00:14:55Z on `tools/skill-carver/audit-descriptions.ts`:60 (association: NONE)

  <pre>
  P2 (bug): `maxLen: Math.max(...reports.map(r =&gt; r.descLen))` throws when `reports` is empty (e.g., if the skills root path is wrong or parsing fails). Consider guarding the empty case (e.g., `maxLen: 0` or `null`) so the tool always emits JSON rather than crashing.

  </pre>

## Fix commits (touching thread paths)

### `d4a4e98e09df5f9f28548a26fe3e3a52e6a77227` -- 2026-05-10T00:11:47Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/skill-carver/audit-descriptions.ts`

**Message:**

```
feat(B-0347): smallest safe slice — TS audit tool for carved-skill-de…

…scription budget (B-0347)

One bounded step on the broad M-effort item: diagnostic TS tool (Rule 0)
that scans all 242 SKILL.md frontmatter descriptions, flags those >120 chars,
and emits JSON report. Re-decomposed: treat as atomic was mistake; tool is
the safe entry point before any mass carve edits. Focused checks: tool runs
clean (242 total, 242 exceed, max 3717), dotnet build gate 0w/0e in root
checkout (TS change has no impact). Prepares next slice for carve pass.

Co-Authored-By: Grok <noreply@x.ai>
Co-authored-by: Cursor <cursoragent@cursor.com>
```
