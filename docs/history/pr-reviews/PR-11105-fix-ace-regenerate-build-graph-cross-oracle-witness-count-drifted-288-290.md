# PR #11105 -- fix(ace): regenerate build-graph — cross-oracle witness count drifted 288 -> 290

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 11105 |
| Title | fix(ace): regenerate build-graph — cross-oracle witness count drifted 288 -> 290 |
| Author | `maximdolphin` (human) |
| State | MERGED |
| Created at | 2026-08-16T21:21:10Z |
| Merged at | 2026-08-16T21:33:52Z |
| Merge commit SHA | `4e0173f2fb19aa98325e0c4d82d5c3edcdb71b2f` |
| Branch | `fix/ace-graph-drift-kubevirt-cdi` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/11105 |
| Changed files | 1 |
| Additions / deletions | +1 / -1 |

## Description

## What

The `DRIFT GATE` in `build-graph.test.ts` is failing on main — regenerating from the repo manifests no longer reproduces the checked-in graph.

Two `cross-oracle-bytelock` witnesses were added against `tests/cross-verification/_golden/zeta-ir-v4.golden.json` without regenerating the derived graph, so the checked-in count stayed at 288 while derivation produces 290.

## Fix

Exactly the command the failing test prints:

```bash
bun src/Core.TypeScript/ace/build-graph.ts derive --write
```

## NOT prettier

The derive tool ends with *"run prettier --write on it before committing"* — that advice is **stale for this file**. `build-graph.json` is not stored in prettier format, so `prettier --write` rewrites **964 lines** for no reason (same result on the repo-pinned 3.8.3 and on 3.9.6, so it is not a version artifact).

Nothing enforces it either: `prettier --write` is explicitly **deferred** in CI (`.github/workflows/lint-autofix.yml:111`), and the file is not in `.prettierignore`. Derive alone yields the correct 1-line diff.

## Provenance

Ambient drift — **not** from the longhorn (#11079) or kubevirt/cdi (#11089) merges. The witness is a cross-verification golden, unrelated to k8s manifests or nix modules.

## Verification

`bun test src/Core.TypeScript/ace/build-graph.test.ts` — **83 pass, 0 fail**.

Note: the sibling `refusal is uniform ... trailing whitespace` failure in the same job is a **separate, pre-existing** issue (`installer-binding-cli.ts:34` trims binding material) and is deliberately not touched here.

Agency-Signature-Version: 1
Agent: claude-code
Agent-Runtime: claude-code
Agent-Model: claude-opus-5
Credential-Identity: sergmaxim08@gmail.com
Credential-Mode: shared
Human-Review: not-implied-by-credential
Human-Review-Evidence: none
Action-Mode: autonomous-fail-open
Task: none
Co-authored-by: Claude Opus 5 <noreply@anthropic.com>


## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 0 |
| Resolved threads | 0 |
| Unresolved threads | 0 |
| Total review comments | 0 |
| Total fix commits (touching thread paths) | 0 |

## Review threads

(no review threads on this PR)

## Fix commits (touching thread paths)

(no commits in this PR touched files anchored by review threads)
