# PR #6501 -- feat(yaml): YAML port — forward-only one-pass reader + DOM-on-top, 4-oracle (registry ✅ 4/4)

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 6501 |
| Title | feat(yaml): YAML port — forward-only one-pass reader + DOM-on-top, 4-oracle (registry ✅ 4/4) |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-06-01T23:42:58Z |
| Merged at | 2026-06-01T23:53:18Z |
| Merge commit SHA | `2e940151d59db82c3e08cfb43c3bd7ca0caf7a96` |
| Branch | `otto-windows/yaml-port-impl-2026-06-01` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/6501 |
| Changed files | 46 |
| Additions / deletions | +6884 / -64 |

## Description

Lands the **YAML port** on the serializer roster (registry `⬜ YAML` → `✅ 4/4`). Spec: #6493. Plan + spec committed under `docs/agendas/ace-package-manager/`.

## What
A YAML reader owned across all four languages (TS/F#/C#/Rust), per the `bcl-interface-boundary` doctrine (own the port; vendor is a wrapped adapter):

- **Layer 1 `YamlReader`** — forward-only single-pass pull reader: scans once, indent/context stack, emits a flat `YamlEvent` stream (`Mapping/Sequence Start/End`, `Scalar{raw,kind,style}`). **Never materializes a tree** (`Utf8JsonReader`-style).
- **Layer 2 `YamlValue` DOM** — a thin fold built *on top* of the event stream (does not re-scan).
- **Scope** — safe-subset (block maps/seqs/scalars, nesting, comments, quoted/plain, core-schema kind resolution). Anchors/tags/multi-doc/flow/block-scalars + emit are out (vendor-only).
- **Boundary** — hand-rolled default in all four; YamlDotNet (F#/C#) + `Bun.YAML` (TS) wrapped as a **differential oracle** (our reader's events vs the vendor's event parser agree); Rust hand-rolled-only (`serde_yaml` unmaintained).

## Verification (Tier-1)

- **4/4 byte-identical** on 10 golden vectors (`tests/cross-verification/yaml`): TS≡F#≡C#≡Rust, all == fixture `expected` (independently deep-equal-checked). Fixture is JSON (bootstrap-avoidance — don't parse YAML to test YAML).
- YamlDotNet differential **FULL** pass (F#/C#) — our forward-only reader matches the mature vendor's event stream on every vector.
- Per-lang unit tests: TS 28, Rust 24, F# (Yaml) + C# (Yaml) facts; full suites green (F# 1127, C# 125), 0 regressions; `dotnet build -c Release` 0-warn; `cargo test` 0-warn.

## Boundary payoff (retrofit)
Removed the **3 remaining direct vendor-YAML usages** by routing them through the port — zeta-id cross-verify (F#/C#/TS) now parses via our `YamlReader`/DOM, not `DeserializerBuilder`/`Bun.YAML`. **Gate held: zeta-id output byte-identical** (12-vector hex unchanged; `git diff` empty). Stripped the now-dead `[YamlMember]` attributes from the C# zeta-id `FlatVector` + deleted the dead `VectorEnvelope` — no live direct YamlDotNet refs remain outside the differential adapters.

## Next
Slice 8 (SHA-256, paused on its own branch) resumes after this — its TS/F#/C# oracles consume this port for fixture reading instead of `Bun.YAML`/YamlDotNet.

Brainstormed + approved with the operator 2026-06-01 (own-the-interface like the JSON port; safe-subset; forward-only one-pass, not a DOM parser). Subagent-driven build.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 6 |
| Resolved threads | 6 |
| Unresolved threads | 0 |
| Total review comments | 6 |
| Total fix commits (touching thread paths) | 5 |

## Review threads

### Thread 1 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GRrdw`
- **Path**: `tests/Tests.CSharp/ZetaId/CrossVerifyTests.cs`
- **Replies**: 0

**Initial comment:**

- **`github-code-quality[bot]` (bot)** at 2026-06-01T23:45:19Z on `tests/Tests.CSharp/ZetaId/CrossVerifyTests.cs`:27 (association: NONE)

  <pre>
  ## Missed opportunity to use Where

  This foreach loop &lt;a class="Link" href="/Lucent-Financial-Group/Zeta/blob/9ca2ce2ab3b4383a8face2019b34aeaa83af1034/tests/Tests.CSharp/ZetaId/CrossVerifyTests.cs#L26-L26"&gt;implicitly filters its target sequence&lt;/a&gt; - consider filtering the sequence explicitly using '.Where(...)'.

  ---

  &lt;p&gt;Use LINQ to make filtering explicit in &lt;code&gt;Field(...)&lt;/code&gt; rather than filtering inside a &lt;code&gt;foreach&lt;/code&gt;.&lt;/p&gt;
  &lt;p&gt;Best fix (minimal behavior change):&lt;/p&gt;
  &lt;ul&gt;
  &lt;li&gt;In &lt;code&gt;tests/Tests.CSharp/ZetaId/CrossVerifyTests.cs&lt;/code&gt;, add &lt;code&gt;using System.Linq;&lt;/code&gt;.&lt;/li&gt;
  &lt;li&gt;Replace the &lt;code&gt;foreach&lt;/code&gt; loop in &lt;code&gt;Field(...)&lt;/code&gt; with a &lt;code&gt;FirstOrDefault&lt;/code&gt; lookup by key.&lt;/li&gt;
  &lt;li&gt;Keep the same exception behavior when the key is not found.&lt;/li&gt;
  &lt;/ul&gt;
  &lt;p&gt;This preserves functionality:&lt;/p&gt;
  &lt;ul&gt;
  &lt;li&gt;Returns matching value when present.&lt;/li&gt;
  &lt;li&gt;Throws &lt;code&gt;InvalidOperationException&lt;/code&gt; with the same message when absent.&lt;/li&gt;
  &lt;/ul&gt;
  </pre>

### Thread 2 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GRrd4`
- **Path**: `src/Core.CSharp.Yaml/YamlReader.cs`
- **Replies**: 0

**Initial comment:**

- **`github-code-quality[bot]` (bot)** at 2026-06-01T23:45:19Z on `src/Core.CSharp.Yaml/YamlReader.cs`:251 (association: NONE)

  <pre>
  ## Missed opportunity to use Select

  This foreach loop immediately &lt;a class="Link" href="/Lucent-Financial-Group/Zeta/blob/9ca2ce2ab3b4383a8face2019b34aeaa83af1034/src/Core.CSharp.Yaml/YamlReader.cs#L234-L234"&gt;maps its iteration variable to another variable&lt;/a&gt; - consider mapping the sequence explicitly using '.Select(...)'.

  ---

  Use LINQ to normalize line endings before entering the main processing loop.&lt;/p&gt;
  &lt;p&gt;Best fix (without changing behavior):&lt;/p&gt;
  &lt;ul&gt;
  &lt;li&gt;In &lt;code&gt;src/Core.CSharp.Yaml/YamlReader.cs&lt;/code&gt;, add &lt;code&gt;using System.Linq;&lt;/code&gt;.&lt;/li&gt;
  &lt;li&gt;In &lt;code&gt;ToContentLines&lt;/code&gt;, replace:
  &lt;ul&gt;
  &lt;li&gt;&lt;code&gt;string[] rawLines = text.Split('&amp;amp;#92;n');&lt;/code&gt;&lt;/li&gt;
  &lt;li&gt;&lt;code&gt;foreach (string rawLine in rawLines)&lt;/code&gt;&lt;/li&gt;
  &lt;/ul&gt;
  &lt;/li&gt;
  &lt;li&gt;with:
  &lt;ul&gt;
  &lt;li&gt;&lt;code&gt;IEnumerable&amp;lt;string&amp;gt; lines = text.Split('&amp;amp;#92;n').Select(rawLine =&amp;gt; rawLine.EndsWith('&amp;amp;#92;r') ? rawLine[..^1] : rawLine);&lt;/code&gt;&lt;/li&gt;
  &lt;li&gt;&lt;code&gt;foreach (string line in lines)&lt;/code&gt;&lt;/li&gt;
  &lt;/ul&gt;
  &lt;/li&gt;
  &lt;li&gt;Remove the now-unneeded inner &lt;code&gt;line&lt;/code&gt; assignment from &lt;code&gt;rawLine&lt;/code&gt;.&lt;/li&gt;
  &lt;/ul&gt;
  &lt;p&gt;This keeps all existing parsing/filtering/validation logic unchanged and only externalizes the mapping step as requested by the rule.
  </pre>

### Thread 3 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GRsty`
- **Path**: `tests/Tests.CSharp/Yaml/CrossVerifyTests.cs`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-01T23:47:41Z on `tests/Tests.CSharp/Yaml/CrossVerifyTests.cs`:193 (association: NONE)

  <pre>
  Potential build break under .NET analyzers: `IndentEventJson(...)` is called with an `indent` argument, but the helper currently ignores its `indent` parameter. With `TreatWarningsAsErrors` + `AnalysisLevel=latest-recommended`, this can trigger CA1801 (unused parameter) and fail the build. Either remove the parameter or use it meaningfully; simplest is to drop the unused argument and update the helper signature to take a single parameter (see the paired comment on the helper).
  </pre>

### Thread 4 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GRst7`
- **Path**: `tests/Tests.CSharp/Yaml/CrossVerifyTests.cs`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-01T23:47:42Z on `tests/Tests.CSharp/Yaml/CrossVerifyTests.cs`:222 (association: NONE)

  <pre>
  Potential build break under .NET analyzers: `IndentEventJson` declares an `indent` parameter but never uses it, which can trigger CA1801 (unused parameter) and fail the build when warnings are treated as errors. If indentation isn't needed (compare.ts deep-equals parsed JSON), remove the parameter and keep the method as a no-op wrapper (and update the call site accordingly).
  </pre>

### Thread 5 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GRsuK`
- **Path**: `src/Core.TypeScript/yaml/reader.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-01T23:47:42Z on `src/Core.TypeScript/yaml/reader.ts`:117 (association: NONE)

  <pre>
  Quoted-scalar decoding returns as soon as it sees the closing quote and ignores any remaining non-comment text on the same line. That means inputs like `a: "x" trailing` (or `a: 'x' trailing`) are silently accepted with value `x`, which is a misparse and makes YAML errors easy to miss. Consider validating that any characters after the closing quote are only whitespace or a `#` comment, and decline otherwise (and apply the same rule across the other language ports to preserve cross-oracle parity).
  </pre>

### Thread 6 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GRsuT`
- **Path**: `docs/agendas/ace-package-manager/2026-06-01-yaml-port-implementation-plan.md`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-01T23:47:42Z on `docs/agendas/ace-package-manager/2026-06-01-yaml-port-implementation-plan.md`:259 (association: NONE)

  <pre>
  The Task 3 Step 6 description says the YamlDotNet differential compares `readEvents`→DOM against `DeserializerBuilder`, but the implementation in this PR uses YamlDotNet's forward-only event parser (`YamlDotNet.Core.Parser`) and compares structure + raw + style (ignoring kind). Updating this keeps the plan aligned with the actual verification strategy landed here.
  </pre>

## Fix commits (touching thread paths)

### `51c79d0bb8a744177c00cf76143af932d36fc84e` -- 2026-06-01T22:46:18Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `docs/agendas/ace-package-manager/2026-06-01-yaml-port-implementation-plan.md`

**Message:**

```
docs(yaml): YAML port implementation plan (subagent-driven, 4-languag…

…e, forward-only)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `4e41b5cdba98a3bec90399bbc38d170101a3d562` -- 2026-06-01T22:53:10Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `src/Core.TypeScript/yaml/reader.ts`

**Message:**

```
feat(yaml): T1 — TS forward-only YamlReader + YamlValue DOM + fixture…

… + compare

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `675cf7a2d53fc3938183cf128a025469d4fad41e` -- 2026-06-01T23:32:05Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `src/Core.CSharp.Yaml/YamlReader.cs`
- `tests/Tests.CSharp/Yaml/CrossVerifyTests.cs`

**Message:**

```
feat(yaml): T4 -- C# BCL-clean forward-only YamlReader + DOM + YamlDo…

…tNet differential (oracle 4)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `b3419b05aac3ec661d516997830a86c945767dad` -- 2026-06-01T23:40:02Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tests/Tests.CSharp/ZetaId/CrossVerifyTests.cs`

**Message:**

```
refactor(yaml): retrofit zeta-id cross-verify to the YAML port (remov…

…e direct YamlDotNet/Bun.YAML)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `208e24382dd0d8852f5d1ba9785c8e1016069ec9` -- 2026-06-01T23:50:06Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tests/Tests.CSharp/Yaml/CrossVerifyTests.cs`

**Message:**

```
refactor(yaml): remove dead no-op IndentEventJson helper (C# cross-ve…

…rify)

Inlined EventToJson at the call site; the helper ignored both its 'indent' param
and its own name (returned compact as-is). Output byte-identical. Addresses Copilot
review (the CA1801 'build break' framing was moot — build was 0-warn — but the dead
param + misleading no-op are real).

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```
