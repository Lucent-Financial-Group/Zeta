# PR #16484 -- measure(cluster): agent-memory leaves the dev excludeGlob on its own stated condition — remeasured on today's main

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 16484 |
| Title | measure(cluster): agent-memory leaves the dev excludeGlob on its own stated condition — remeasured on today's main |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-09-03T09:59:36Z |
| Merged at | 2026-09-03T11:56:48Z |
| Merge commit SHA | `9faf575df5f779bffb2ae5bd210a3887256dcc0f` |
| Branch | `shadow/land-agent-memory-drop-the-glob` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/16484 |
| Changed files | 12 |
| Additions / deletions | +130 / -59 |

## Description

Aaron 2026-09-03: **remeasure, then land `shadow/measure-agent-memory-drop-the-glob`, then merge after an adversarial review.** This PR is the remeasure-and-land half. **Auto-merge is NOT armed** — the adversarial review runs first.

The old branch's *intent* is re-applied on a fresh branch cut from today's `origin/main` (`4962a0bc`). Its diff and its 2026-08-31 arithmetic are not reused; main's lane numbers moved three times since (minio removed; mimir/nfd/alloy re-measured; cloudnativepg added).

## What lifts

`agent-memory`'s `DEV_EXCLUDED_REASONS` entry said, in its own words, that it was **"HELD BY THE GLOB, NOT BY A MEASUREMENT"**, that the blocker was "very likely spent" once the dev longhorn alias shipped (2026-08-21), and that nobody had measured it because an app that never syncs never produces a verdict. Its `LIFTS WHEN`, verbatim: *"`agent-memory/**` is dropped from `DEFAULT_ROOT_DEV_CATALOG.excludeGlob` and one included run reports its actual verdict."*

- The first half is this PR: one glob entry removed in `ports.ts`.
- The second half is the **next included run**, which this PR cannot contain. This is a *measurement, not a repair* — nothing about the Application was fixed because nothing was known to be broken. If that run goes red the deferral comes back with a real reason attached, which is strictly more than it had.

Checked before lifting: RWO (not RWX, so `yamlTreeRequestsReadWriteMany` does not catch it), 8Gi on the `longhorn` alias, `busybox:1.36`, one replica, 50m/64Mi. No registry credential, no GPU. After the change `discoverExpectedApplications()` reports `agent-memory` with `excludedFromDev: false` and the offline `--dry-run --scope included` plan lists it as a proof target.

**Honest limit:** the live included proof needs a kind cluster and did not run here. Whether agent-memory actually yields Synced/Healthy is the next CI run's to report; this PR does not claim it.

## The defect found in passing — verified on today's main

`DEV_INCLUDED_PROOF_DEFERRED_DIRS`'s comment for agent-memory claimed the entry was *"redundant … independently caught by the longhorn rule in `isExcludedFromIncludedProof`"*. That rule sits **behind** `if (aliasDeclared) return false;`, and `devLonghornStorageClassAliasDeclared()` returns `true` on main today (checked with `bun -e`) and has since 2026-08-21. So the longhorn rule was unreachable for this dir and caught nothing — two entries claiming to back each other up while one was inert. Both entries are closed with the record written where they stood (the file's own `headscale` idiom), not deleted. The mechanism is correct; the prose that leaned on it was not.

## Every pinned number that moved — before → after, and the cause

Baseline measured on **clean main first** (`git stash`-equivalent: measured before any edit), then the branch, so each delta is attributable to exactly one cause: **one more Application applied by the dev lane**.

| quantity | before (main `4962a0bc`) | after | delta / cause |
|---|---|---|---|
| dev lane, `dev` rung | 38 apps · 1140m / 9100Mi | 39 apps · **1165m / 9164Mi** | +25m / +64Mi — agent-memory's governed row, floored to 25m at `dev` |
| dev lane, `metal` rung | 38 apps · 6340m / 14288Mi | 39 apps · **6390m / 14352Mi** | +50m / +64Mi — the manifest literal |
| all 47, `metal` | 10365m / 22623Mi | 10365m / 22623Mi | **unchanged** — agent-memory was always in the tree |
| all 47, `dev` | 4000m / 16251Mi | 4000m / 16251Mi | unchanged |
| dev CPU spare | 1360m | **1335m** | −25m |
| dev memory spare | 116Mi | **52Mi** | −64Mi — memory is now the tighter axis in this lane |
| `acknowledgedRungBudgetGap` | `metal@dev-lane=6340m/14288Mi>>2500m/9216Mi` | `metal@dev-lane=6390m/14352Mi>>2500m/9216Mi` | re-keyed |
| dev-lane applied count | 38 of 47 | **39 of 47** | first move where *only* the applied count changed — correct: the tree did not grow, the glob shrank |

## Files touched (12), each sequence site APPENDED rather than overwritten

- `src/Core.TypeScript/cluster/ports.ts` — the glob. The only mechanical change.
- `src/Core.TypeScript/cluster/argocd-health-test.ts` — both registry entries closed with their record; hindsight's `[cite: lane-cpu metal 6390 over]` / `[cite: lane-cpu dev 1165 fits]`; a **FOURTH ANSWER** appended to the "THE ARITHMETIC THAT SAYS SYMPTOM" sentence, which also records that the citations had moved 1081 → 1056 → 1115 → 1140 without that sentence following.
- `src/Core.TypeScript/cluster/argocd-health-test.test.ts` — three assertions flip to pin the *inclusion* (`vault` LIFTED / `spire` FLIPPED SIDES idiom); the excludeGlob set; the cite pair.
- `src/Core.TypeScript/cluster/storage-profiles.test.ts` — 38 → 39 applied (+ `toContain("agent-memory")`), both lane totals, test title.
- `src/Core.TypeScript/cluster/reason-truth.test.ts` — lane-cpu citations, both polarity controls, both sequence ledgers, 1335m spare.
- `full-ai-cluster/k8s/single-node-budget.json` — the key; the gap's header sentence restated at current values (39 / 6390m / 14352Mi); a second arithmetic table appended under the first. The ledger's own checker admits only readings the tree computes today, so superseded figures are pointed at (reason-truth/storage-profiles tests), not repeated.
- `src/Core.TypeScript/cluster/single-node-readiness.test.ts` — the mutation helper string `6340m`→`6390m`.
- `full-ai-cluster/k8s/storage-profiles.json` — agent-memory's row said "Glob-excluded from the dev lane, so this row buys reachability rather than budget"; appended that it is counted now.
- `src/Core.TypeScript/cluster/lane-partition.ts` — docstring enumerated nine standing deferrals; eight now, ninth named as lifted.
- `rendered-resource-requests.snapshot.json`, `rendered-storage-claims.snapshot.json`, `full-ai-cluster/k8s/lane-footprints.json` — re-measured (`--measure`, `--write-snapshot`, `measure-lane-footprints`, 133/134 images, hat-system ghcr 401 as before). **Only `measuredOn` moved** — the renders and image sets were already current, which is itself a reading.

## Verified

- `bun test src/Core.TypeScript/cluster/ src/Core.TypeScript/hygiene/` — **4662 pass / 0 fail** (cluster 1115, baseline on main 1115/0; hygiene 3547).
- `bun src/Core.TypeScript/cluster/derive-sync-waves.ts` — 47 charts, 8 disagreements, **0 new** (baseline 0 new).
- Push verified: `gh api …/git/ref/heads/shadow/land-agent-memory-drop-the-glob` = local HEAD `0d886417`.

## For the adversarial reviewer — two pre-existing weaknesses this PR does not fix

1. The ledger figure checker (`single-node-readiness.test.ts` "every millicore and MiB figure…") admits any *difference* of any two readings, which is why stale `6240m` / `1115m` in the older `$comment` paragraph still pass. Weak, and pre-existing.
2. `reason-truth.test.ts`'s sequence ledger lists 2026-09-02 before 2026-09-01 — pre-existing ordering; I appended after rather than reordering.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_01QyTyVdDqxekK5rZQGBm75f

Agency-Signature-Version: 1
Agent: shadow
Agent-Runtime: claude-code
Agent-Model: claude-fable-5-1
Credential-Identity: AceHack
Credential-Mode: shared
Human-Review: explicit
Human-Review-Evidence: chat
Action-Mode: human-directed
Task: none
Co-authored-by: shadow <shadow@zeta.agents>


## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 0 |
| Resolved threads | 0 |
| Unresolved threads | 0 |
| Total review comments | 0 |
| Total fix commits (touching thread paths) | 0 |

## Review threads

(no review threads on this PR)

## Fix commits (touching thread paths)

(no commits in this PR touched files anchored by review threads)
