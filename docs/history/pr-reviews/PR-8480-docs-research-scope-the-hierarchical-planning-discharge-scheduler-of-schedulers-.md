# PR #8480 -- docs(research): scope the hierarchical-planning discharge (scheduler-of-schedulers over gist levels beats flat StateSpace BFS) + MVP slice

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 8480 |
| Title | docs(research): scope the hierarchical-planning discharge (scheduler-of-schedulers over gist levels beats flat StateSpace BFS) + MVP slice |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-06-16T08:01:57Z |
| Merged at | 2026-06-16T08:02:09Z |
| Merge commit SHA | `aa7581bb017ff24828dafbdf51e2c3e62850f4b3` |
| Branch | `otto/hierarchical-planning-discharge-scoping` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/8480 |
| Changed files | 2 |
| Additions / deletions | +69 / -0 |

## Description

Aaron 2026-06-16: "continue" the big prize. Scopes the §B hierarchical-planning discharge into a **demo design + first slice** (same pattern as the Aurora scoping; the build is an author/Vera job).

**Key find (look-better):** the **flat baseline is already built** — `StateSpace.fs` = DST-replayable goal-directed BFS + transposition table + shortest-path recovery. So the discharge is concretely scopable:

- **Hierarchical** = the *same* `StateSpace` search at **gist-abstraction levels**; `SoftChip8Scheduler` nests the levels; **least-action (§9j)** selects the level; **homoiconic gist-unpack** (`apply(reify gist)` IS the sub-search, #8474) makes it real; `ActionGrid` = the nav space.
- **"Beats flat"** = states-explored: flat O(b^d) vs hierarchical O(b^(d/k)·k), same goal, both DST-replayable.
- **First slice (MVP):** 2-level planner over a small `ActionGrid` task — coarse region BFS → fine within-region BFS → concatenate; assert plan reaches goal + states ≪ flat + byte-identical replay.
- Falsifiers carried; liveness routes via observe.ts (out of scope).

World-model trajectory: #1 discharge now **SCOPED** with the MVP slice.

🤖 Generated with [Claude Code](https://claude.com/claude-code)


## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 0 |
| Resolved threads | 0 |
| Unresolved threads | 0 |
| Total review comments | 0 |
| Total fix commits (touching thread paths) | 0 |

## Review threads

(no review threads on this PR)

## Fix commits (touching thread paths)

(no commits in this PR touched files anchored by review threads)
