# PR #1412 -- feat(B-0183 Phase 1): TS wrapper for TLC — tools/formal-verification/run-tlc.ts

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 1412 |
| Title | feat(B-0183 Phase 1): TS wrapper for TLC — tools/formal-verification/run-tlc.ts |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-05-03T13:53:40Z |
| Merged at | 2026-05-03T14:10:25Z |
| Merge commit SHA | `158b76ef8a3759cd643aa0ab388795ac24870bfd` |
| Branch | `feat/b0183-phase1-tlc-ts-wrapper` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/1412 |
| Changed files | 1 |
| Additions / deletions | +339 / -0 |

## Description

## Summary

B-0183 Phase 1 implementation. Per Aaron 2026-05-03 *"another one bites the dust > ts"* + the B-0183 backlog row: the F# `Tlc.Runner.Tests.fs` is really an xunit wrapper around shelling to TLC — no F# operator-algebra logic involved. This TS wrapper is the natural shape.

## Three invocation modes

```bash
bun tools/formal-verification/run-tlc.ts <SpecName>
bun tools/formal-verification/run-tlc.ts --all
bun tools/formal-verification/run-tlc.ts --check-toolchain
```

## Local verification

| Test | Result |
|---|---|
| `--check-toolchain` | OK |
| `SmokeCheck` | OK |
| `DbspSpec` | OK (1M states / ~11s) |
| `--all` on this branch | 10/11 pass; SpineAsyncProtocol fails (expected — #1411 not yet merged into main this branch forked from) |
| Total `--all` wall time | 16s (faster than `dotnet test` overhead alone) |

## Roadmap (per B-0183)

- **Phase 1** (this PR): TS wrapper shipped; parallel-run with F# tests
- **Phase 2** (next PR): Linux-only CI workflow that calls the wrapper
- **Phase 3** (after parity verified across N PRs): retire F# tests + remove B-0182's vestigial OS+arch+workflow filter

## Test plan

- [x] All three modes work locally
- [x] Trace cleanup matches F# behavior (`<SpecName>_TTrace_*.tla`, `*.bin`, `MC*.tla`)
- [x] Exit codes correct (0 success / 1 fail / 2 toolchain-not-ready)
- [x] CATALOGUE matches F# `Tlc.Runner.Tests.fs` `[<Fact>]` registrations

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 11 |
| Resolved threads | 11 |
| Unresolved threads | 0 |
| Total review comments | 11 |
| Total fix commits (touching thread paths) | 2 |

## Review threads

### Thread 1 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5_MNkm`
- **Path**: `tools/formal-verification/run-tlc.ts`
- **Replies**: 0

**Initial comment:**

- **`github-code-quality[bot]` (bot)** at 2026-05-03T13:54:47Z on `tools/formal-verification/run-tlc.ts`:37 (association: NONE)

  <pre>
  ## Unused variable, import, function or class

  Unused import readFileSync.

  ---

  The general fix is to remove imports that are not referenced anywhere in the file.&lt;/p&gt;
  &lt;p&gt;Best fix here: in &lt;code&gt;tools/formal-verification/run-tlc.ts&lt;/code&gt;, update the &lt;code&gt;node:fs&lt;/code&gt; import line to remove &lt;code&gt;readFileSync&lt;/code&gt; while keeping the other used symbols unchanged. This is a minimal, behavior-preserving cleanup and directly resolves the CodeQL warning.
  </pre>

### Thread 2 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5_MN0J`
- **Path**: `tools/formal-verification/run-tlc.ts`
- **Replies**: 0

**Initial comment:**

- **`github-advanced-security[bot]` (bot)** at 2026-05-03T13:55:40Z on `tools/formal-verification/run-tlc.ts`:122 (association: NONE)

  <pre>
  ## CodeQL / Regular expression injection

  This regular expression is constructed from a [command-line argument](1).

  [Show more details](https://github.com/Lucent-Financial-Group/Zeta/security/code-scanning/46)
  </pre>

### Thread 3 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5_MN0M`
- **Path**: `tools/formal-verification/run-tlc.ts`
- **Replies**: 0

**Initial comment:**

- **`github-advanced-security[bot]` (bot)** at 2026-05-03T13:55:40Z on `tools/formal-verification/run-tlc.ts`:123 (association: NONE)

  <pre>
  ## CodeQL / Regular expression injection

  This regular expression is constructed from a [command-line argument](1).

  [Show more details](https://github.com/Lucent-Financial-Group/Zeta/security/code-scanning/47)
  </pre>

### Thread 4 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5_MOA8`
- **Path**: `tools/formal-verification/run-tlc.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-03T13:56:29Z on `tools/formal-verification/run-tlc.ts`:277 (association: NONE)

  <pre>
  P0: Exit-code contract in the header comment says `2` is for “toolchain not ready”, but `main` also returns `2` for an unknown flag (`specName.startsWith("--")`). This makes it impossible for callers/CI to distinguish toolchain problems from argument/usage errors. Either return a different code for usage errors (commonly 1), or update the documented contract + `ExitCode` type to reflect the expanded meaning.

  </pre>

### Thread 5 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5_MOBD`
- **Path**: `tools/formal-verification/run-tlc.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-03T13:56:29Z on `tools/formal-verification/run-tlc.ts`:79 (association: NONE)

  <pre>
  P1: `which()` shells out to `/usr/bin/env which ...`, which adds an extra external dependency (the `which` binary) and is less portable than the PATH-scan used by the existing F# runner. Consider implementing PATH resolution in-process (or at least using a `command -v` check like other Bun tools in this repo) so `--check-toolchain` behaves consistently across environments.
  </pre>

### Thread 6 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5_MOBI`
- **Path**: `tools/formal-verification/run-tlc.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-03T13:56:30Z on `tools/formal-verification/run-tlc.ts`:127 (association: NONE)

  <pre>
  P0: `cleanupTraceFiles` builds regexes by interpolating `specName` directly. If a user passes a spec name containing regex metacharacters (e.g. `.` or `*`), the cleanup can match/delete unintended files under `tools/tla/specs`. Escape `specName` before embedding it in a `RegExp`, or avoid regex entirely by using prefix checks.

  </pre>

### Thread 7 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5_MOBY`
- **Path**: `tools/formal-verification/run-tlc.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-03T13:56:30Z on `tools/formal-verification/run-tlc.ts`:247 (association: NONE)

  <pre>
  P1: In `--all` mode, missing `.tla`/`.cfg` entries are treated as “skipped” and do not affect the exit code. That contradicts the stated `--all` semantics (“exit 0 only if all pass”) and can mask catalogue drift (e.g., a renamed/deleted spec). Consider failing the run when a catalogued spec is missing, or add an explicit `--skip-missing` flag if skipping is desired.
  </pre>

### Thread 8 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5_MOBp`
- **Path**: `tools/formal-verification/run-tlc.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-03T13:56:30Z on `tools/formal-verification/run-tlc.ts`:259 (association: NONE)

  <pre>
  P2: When a spec fails under `--all`, the script records it but does not emit the failing spec’s stdout/stderr. In CI this can make triage harder compared to `runOne`. Consider printing at least the failure’s stdout (or a brief tail) for failed specs, or printing a clear “rerun with: bun ... &lt;SpecName&gt;” hint.
  </pre>

### Thread 9 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5_MOBw`
- **Path**: `tools/formal-verification/run-tlc.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-03T13:56:30Z on `tools/formal-verification/run-tlc.ts`:79 (association: NONE)

  <pre>
  P1: `CATALOGUE` claims to match `tests/Tests.FSharp/Formal/Tlc.Runner.Tests.fs` registrations, but it currently includes `SpineAsyncProtocol` even though the F# runner does not have a corresponding `[&lt;Fact&gt;]` that calls `assertSpecValid "SpineAsyncProtocol"` (it only appears in the `.tla exists on disk` list). Either adjust the catalogue to reflect the F# runnable set, or update the comment/docs to clarify that this is a forward-looking superset and will intentionally diverge until #1411 lands / Phase 3.
  </pre>

### Thread 10 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5_MOCg`
- **Path**: `tools/formal-verification/run-tlc.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-03T13:56:34Z on `tools/formal-verification/run-tlc.ts`:246 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Fail --all when a catalogued TLC spec is missing**

  `runAll` currently treats a missing `.tla` or `.cfg` for a catalogued spec as `skipped` and can still exit 0, which weakens the verification gate by allowing required specs to silently drop out of coverage. In the previous F# runner, catalogued per-spec checks executed directly and missing artifacts surfaced as failures; this change can let CI pass even though a formally-verified spec is no longer being checked.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 11 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5_MOCh`
- **Path**: `tools/formal-verification/run-tlc.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-03T13:56:34Z on `tools/formal-verification/run-tlc.ts`:79 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Resolve Java without relying on /usr/bin/env which**

  The toolchain check hard-depends on `/usr/bin/env which java`; in environments where `/usr/bin/env` or `which` is unavailable (notably non-Unix or minimal images), `which()` returns `null` even when Java is installed, causing false `toolchain not ready` failures. This is a portability regression for `--check-toolchain`/normal runs and can block valid executions solely due to lookup mechanism, not actual toolchain state.

  Useful? React with 👍 / 👎.
  </pre>

## Fix commits (touching thread paths)

### `bc2e2708fb17ebc2de8ae1294bff1939c1886006` -- 2026-05-03T13:53:25Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/formal-verification/run-tlc.ts`

**Message:**

```
feat(B-0183 Phase 1): TS wrapper for TLC — tools/formal-verification/…

…run-tlc.ts

Per Aaron 2026-05-03 "another one bites the dust > ts" (also B-0183
backlog row): the F# Tlc.Runner.Tests.fs is really an xunit wrapper
around shelling to TLC — no F# operator-algebra logic involved. This
TS wrapper is the natural shape.

Three modes:

- `bun tools/formal-verification/run-tlc.ts <SpecName>`
  Runs TLC on tools/tla/specs/<SpecName>.tla with the matching .cfg.
  Exit 0 on "Model checking completed. No error has been found.";
  exit 1 on TLC error / invariant violation / etc.; exit 2 if
  toolchain not ready.

- `bun tools/formal-verification/run-tlc.ts --all`
  Runs the curated CATALOGUE (matches F# Tlc.Runner.Tests.fs
  registration) and aggregates pass/fail/skip counts. Exit 0 only
  if all pass.

- `bun tools/formal-verification/run-tlc.ts --check-toolchain`
  Useful for CI gating + dev-local diagnostics. Exit 0/2.

Local verification:
  - --check-toolchain → OK (java + tla2tools.jar present)
  - SmokeCheck → OK
  - DbspSpec → OK (1M states / ~11s)
  - --all on this branch → 10/11 pass; SpineAsyncProtocol fails
    because #1411 (CHECK_DEADLOCK FALSE fix) not yet on main this
    branch was forked from. Once #1411 merges + this branch rebases,
    expected: 11/11 pass.
  - Total wall time for --all: 16s (faster than dotnet test
    overhead alone).

Trace cleanup matches F# version: <SpecName>_TTrace_*.tla, *.bin,
MC*.tla. Working directory set to specs/ so TLC's file resolution
finds module + cfg without absolute paths.

Phase 2 (next PR) wires this into a Linux-only CI workflow.
Phase 3 retires Tlc.Runner.Tests.fs after parity verification across
N PRs (per B-0183).

Composes with B-0182 (the F# 3-axis filter; tactical band-aid until
this TS migration completes — Phase 3 removes that filter as
vestigial).
```

### `5855c2552342e01a47c0e2ebf52e6c57b385f5d1` -- 2026-05-03T14:07:34Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/formal-verification/run-tlc.ts`

**Message:**

```
fix(run-tlc.ts): address 6 #1412 review findings + tighten typing per…

… Aaron 2026-05-03

P0 fixes (security + correctness):
- Regex injection: cleanupTraceFiles now escapes specName before
  embedding in RegExp constructor. Added escapeRegex() helper.
  Without this, a specName containing regex metacharacters could
  cause unintended file deletion under tools/tla/specs.
- Exit code orthogonality: was using exit 2 for both "toolchain
  not ready" AND "unknown flag". Split: 2 = toolchain;
  3 = argument/usage error. Documented in header doc.

P1 fixes (correctness + portability):
- which() now uses in-process PATH-scan (no /usr/bin/env which
  shell-out). Matches F# Tlc.Runner.Tests.fs's pattern; portable to
  Windows + minimal images that lack /usr/bin/env which.
- runAll() treats missing-from-catalogue specs as failures (NOT
  silent skips). Matches the documented contract "exit 0 only if
  all pass" and gates against catalogue drift.
- Removed unused readFileSync import (tsc lint failure).

P2 fixes (DX):
- runAll() now prints failure stdout-tail (last 30 lines) per
  failed spec + a "rerun with: bun ... <SpecName>" hint. Saves
  CI triage from re-invoking with --one.

Aaron 2026-05-03 meta-feedback: "we want to be strongly typed to
a large degree in ts it verifies your assumptions more at lint
time". Tightened ExitCode union to include 3; orthogonal exit-code
contract documented. Future TS authoring should default to strong
types.

Local verification: --all passes 11/11 specs in ~16s wall.
Regex-injection fix verified by passing Spec.With.Dots — now
returns "ERROR: ... not found" instead of regex-matching unrelated
files.
```
