# PR #6480 -- feat(algebra): Z-set F# generic-math — Zero/(+)/(~-)/(-) on the type (abelian group)

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 6480 |
| Title | feat(algebra): Z-set F# generic-math — Zero/(+)/(~-)/(-) on the type (abelian group) |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-06-01T21:52:12Z |
| Merged at | 2026-06-01T22:01:18Z |
| Merge commit SHA | `694e8e0d98422a2555674e071e3ab8c47a6cc1ff` |
| Branch | `otto-cli/zset-fsharp-generic-math-2026-06-01` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/6480 |
| Changed files | 2 |
| Additions / deletions | +126 / -42 |

## Description

Retrofit the **System.Numerics-shaped generic-math interface** onto the F# Z-set — the next rung after G-Set (#6467) and Bag (#6472). Z-set is an abelian **group**, so it gets the inverse rungs `(~-)`/`(-)` on top of the monoid `Zero`/`(+)`.

### What

- `static member Zero` / `(+)` / `(~-)` / `(-)` on the `ZSet<'K>` type.
- The pooled sorted-merge combiner (`add`) and `negate` **relocate onto the type** as the `(+)` and `(~-)` operators — F# SRTP (`LanguagePrimitives.GenericZero`, `Seq.sum`, generic `+`/`-`/`~-`) resolves operators *on the type*, and the `ZSet` module is `ModuleSuffix` (defined after the type) so a type-member can't forward-delegate to it.
- Module `add`/`neg`/`sub` become thin delegators. **Hot path is perf-neutral** (same code, new home); `Checked.(+)`/`Checked.(-)` overflow guards preserved.

### Why
> "numerics like dotnet as our interface, push to other langs if they don't have" — Aaron 2026-06-01

F# has native generic-math; this is the F# idiom rung. Same shape as the G-Set/Bag ladder.

### Tests (+6, **58/58** ZSet pass; Core builds 0 warn / 0 err)

- operator `≡` module fn (delegation is real)
- `Zero` = `empty` = `LanguagePrimitives.GenericZero`
- abelian-group inverse via operators: `a + (-a) = Zero` and `a - a = Zero`
- `Seq.sum` aggregates Z-sets through `GenericZero` + `(+)` (retraction-to-0 drops)
- `(+)` is **NOT** idempotent: `a + a` doubles every weight (Z-set, not G-Set)

### Next rungs (one primitive at a time, hexagonal)
C# IWSAM (`ISubtractionOperators` + `IUnaryNegationOperators`) · Rust (`impl Sub`/`Neg`) · TS (`AbelianGroup` record). Registry status bump to follow.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 4 |
| Resolved threads | 4 |
| Unresolved threads | 0 |
| Total review comments | 4 |
| Total fix commits (touching thread paths) | 1 |

## Review threads

### Thread 1 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GQnsi`
- **Path**: `src/Core/ZSet.fs`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-01T21:54:50Z on `src/Core/ZSet.fs`:48 (association: NONE)

  <pre>
  This docstring claims there is a C# `IAdditiveIdentity` “twin”, but `src/Core.CSharp/ZSet.cs` does not currently implement `IAdditiveIdentity` for `ZSet&lt;T&gt;`. This reads like the interface already exists and can mislead readers; reword to avoid implying the C# generic-math surface is already implemented (or link to the planned work).
  </pre>

### Thread 2 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GQns1`
- **Path**: `src/Core/ZSet.fs`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-01T21:54:50Z on `src/Core/ZSet.fs`:55 (association: NONE)

  <pre>
  This doc comment says the addition operator is the C# `IAdditionOperators` “twin”, but the C# `ZSet&lt;T&gt;` implementation (`src/Core.CSharp/ZSet.cs`) does not implement `IAdditionOperators` yet. Consider rewording so it describes the algebraic intent without implying the C# generic-math interface exists today.
  </pre>

### Thread 3 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GQntD`
- **Path**: `src/Core/ZSet.fs`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-01T21:54:50Z on `src/Core/ZSet.fs`:93 (association: NONE)

  <pre>
  This doc comment references a C# `IUnaryNegationOperators` “twin”, but `src/Core.CSharp/ZSet.cs` does not implement that interface yet. Reword to avoid suggesting the C# generic-math lift already exists.
  </pre>

### Thread 4 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GQntK`
- **Path**: `src/Core/ZSet.fs`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-01T21:54:51Z on `src/Core/ZSet.fs`:105 (association: NONE)

  <pre>
  This doc comment references a C# `ISubtractionOperators` “twin”, but the C# `ZSet&lt;T&gt;` implementation (`src/Core.CSharp/ZSet.cs`) does not implement subtraction interfaces yet. Reword to avoid implying cross-language interface parity that isn’t implemented.
  </pre>

## Fix commits (touching thread paths)

### `475d802cd25b838cef94713f12ad1e136dbea5b5` -- 2026-06-01T21:52:01Z -- ``

**Touched files (intersect with thread paths):**

- `src/Core/ZSet.fs`

**Message:**

```
feat(algebra): Z-set F# generic-math surface — Zero/(+)/(~-)/(-) on t…

…he type (abelian group)

Retrofit the System.Numerics-shaped generic-math interface onto the F# Z-set,
matching the G-Set (#6467) and Bag (#6472) rungs — but Z-set is an abelian
GROUP, so it gets the inverse rungs (~-)/(-) on top of the monoid Zero/(+).

The pooled sorted-merge combiner (add) and negate relocate ONTO the type as the
(+) and (~-) operators so F# SRTP — LanguagePrimitives.GenericZero, Seq.sum,
generic +/-/~- — resolves them on the type (the ZSet module is ModuleSuffix,
defined AFTER the type, so a type-member cannot forward-delegate to it). The
module add/neg/sub become thin delegators; the hot path is perf-neutral (same
code, new home). Checked.(+)/Checked.(-) overflow guards preserved.

"numerics like dotnet as our interface, push to other langs if they don't have"
(Aaron 2026-06-01) — F# has native generic-math; this is the F# idiom rung.
Next rungs: C# IWSAM (ISubtractionOperators + IUnaryNegationOperators), Rust
(impl Sub/Neg), TS (AbelianGroup record).

Tests (+6, 58/58 ZSet pass): operator equals module fn; Zero = empty =
GenericZero; a + (-a) = Zero and a - a = Zero; Seq.sum aggregates through
GenericZero + (+) with retraction-to-0 drop; (+) NOT idempotent (a + a doubles).

Co-Authored-By: Claude <noreply@anthropic.com>
```
