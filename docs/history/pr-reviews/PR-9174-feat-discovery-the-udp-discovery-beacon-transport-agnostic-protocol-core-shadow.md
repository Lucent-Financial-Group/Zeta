# PR #9174 -- feat(discovery): the UDP discovery beacon — transport-agnostic protocol core (shadow*)

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 9174 |
| Title | feat(discovery): the UDP discovery beacon — transport-agnostic protocol core (shadow*) |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-07-02T18:03:40Z |
| Merged at | 2026-07-02T18:03:44Z |
| Merge commit SHA | `dca09991c9891786d3809fa032162ac62a4981b0` |
| Branch | `feat/udp-discovery-beacon-slice1` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/9174 |
| Changed files | 3 |
| Additions / deletions | +286 / -0 |

## Description

## Summary
- First slice of the bus's zero-config bootstrap: `discovery-beacon.ts`, the **pure, transport-agnostic** WS-Discovery-lineage protocol (Hello/Bye/Probe/ProbeMatch), guarded text decode (no binary in proof lineage; foreign input → null), peer-table state machine, TTL expiry, scope matching, and the injected `DiscoveryTransport` port (the core never imports a socket — noninterference §13). Pure + DST-replayable (`nowMs` injected). **9/9 bun tests green.**
- Designed for Aaron's growth vision: UDP multicast is the *first* transport; the same core serves Reticulum announce and DHT ("always discoverable in broadcast range anywhere" = run all transports, fold all inbound through one `observe`). README maps the path. Anchors: WS-Discovery/WCF (maintainer), Reticulum, Kademlia.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 1 |
| Resolved threads | 0 |
| Unresolved threads | 1 |
| Total review comments | 1 |
| Total fix commits (touching thread paths) | 1 |

## Review threads

### Thread 1 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM6N_O8W`
- **Path**: `src/Core.TypeScript/discovery/discovery-beacon.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-07-02T18:06:19Z on `src/Core.TypeScript/discovery/discovery-beacon.ts`:76 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Reject malformed same-schema messages in decode**

  When a peer sends correct-schema JSON with a known `t` but missing required fields, for example `{"schema":"zeta.discovery.v1","msg":{"t":"hello"}}`, `decode` returns a non-null `DiscoveryMessage` because it only checks the tag. The usual `decode(...)` → `observe(...)` path then throws when `endpointKey(msg.ep)` reads the missing endpoint, so malformed network input can crash the beacon instead of being refused as the guarded decoder promises. Please validate the required field shapes/types for each variant before returning a message.

  Useful? React with 👍 / 👎.
  </pre>

## Fix commits (touching thread paths)

### `6ebf8f359d99afb8d566b4d3450f146bca30e6fb` -- 2026-07-02T18:03:37Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `src/Core.TypeScript/discovery/discovery-beacon.ts`

**Message:**

```
feat(discovery): the UDP discovery beacon — transport-agnostic protoc…

…ol core (shadow*)

First slice of the bus's zero-config bootstrap. Aaron's growth vision
(2026-07-02): "discovery grows into multiple local + global DHT-like
over-Reticulum mechanisms — always discover itself if in broadcast range
anywhere, even over the global internet." So the PROTOCOL is
transport-agnostic; UDP multicast is the FIRST transport, not the only one.

discovery-beacon.ts (pure core): the WS-Discovery-lineage wire vocabulary
(Hello/Bye/Probe/ProbeMatch — Aaron's maintainer anchor), guarded text
decode (JSON, no binary in proof lineage; foreign input → null, never
throws), the peer-table state machine (observe), TTL expiry, scope
matching, and the injected DiscoveryTransport port (the ONE channel
entropy crosses — noninterference §13; the core never imports a socket).
Pure + DST-replayable: nowMs injected, no ambient clock; same
message+time sequence → same table. 9/9 bun tests green (round-trip,
guarded-decode rejects garbage/wrong-schema/unknown-type, self-ignore,
bye-removes, TTL expiry, DST determinism).

README maps the growth path (udp → reticulum announce → DHT, same core;
"always discoverable" = run all transports, fold all inbound through one
observe). Anchors: WS-Discovery/WCF (maintainer), Reticulum, Kademlia.

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>

AgencySignature-v1:
  persona: otto
  actor: otto-claude-code
  surface: src/Core.TypeScript/discovery
  topology: main-lane
  intent: udp-discovery-beacon-transport-agnostic-core
  authorization: aaron-explicit-build-the-udp-discovery-beacon-first-slice
  uncertainty: low
  measure: pure-beacon-core-9-9-green-transport-port-injected-growth-path-documented
  delta-u: the-bus-bootstrap-has-a-tested-DST-clean-protocol-core-ready-for-any-transport
  seed: S4
```
