# PR #7383 -- feat(vocab): canonical-by-TYPE (+personas) + grams is the measure-view + master index loaded by DST framework (Z-set/git-weak-table/lazy-Rx) + type-provider closes the test loop + autopilot readiness

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 7383 |
| Title | feat(vocab): canonical-by-TYPE (+personas) + grams is the measure-view + master index loaded by DST framework (Z-set/git-weak-table/lazy-Rx) + type-provider closes the test loop + autopilot readiness |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-06-09T20:01:24Z |
| Merged at | 2026-06-09T20:01:34Z |
| Merge commit SHA | `0847de36fd6f2d1a52d90d13cc16d94590a34b89` |
| Branch | `otto/canonical-by-type-grams-measure-personas-master-index-dst-load` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/7383 |
| Changed files | 231 |
| Additions / deletions | +1036 / -387 |

## Description

Aaron corrected the structure: **grams is NOT canonical — it's the symlink, the MEASURE/dimension.** Canonical homes are the **TYPE folders** (real files): `words/`, `letters/<lang>/`, `shapes/`, `colors/`, `temperatures/`, and now **`personas/`** ("load personas like this too"). `grams/<n>/` = generated symlink measure-view (token count; case-insensitive collisions discriminated by type). `travelers/` = intake + symlink index. Hierarchical per-folder `INDEX.md` + master `VOCAB-INDEX.md` (one-file read, 116 travelers). Discriminator enforced for multi-sense terms.

Built + green: `vocab-uniqueness` / `build-grams-view` / `build-vocab-index` / `vocab-master-index.test` + `vocab-hygiene.yml` (uniqueness + view/index freshness + the master-index **load test**, 3 pass).

**DST framework loads the master index:** eager one-file read → a **Z-set** (present); git history = a **weak-reference weak-table** → touching it triggers **lazy Rx queries to git** for going backwards in time (Time Warp; GVT = present). Load policy: eager-cache the small, lazy the rest (history + large). **F# type provider closes the test loop** — reify vocab → types; test + framework + types are one source → compiler-enforced (remove a traveler → fails to *compile*).

**Autopilot readiness** (you asked "when can dotnet test drive + we watch + re-kick?"): **not yet** — needs the finalizer framework, runaway-convergence guards, the OBJ4-1 human-root (blocker), the privacy gate, CI-recursion/own-runners, the Z-set/git/Rx load, and realmodel graduation. Safe first step: a bounded, **supervised** single-step loop (manual re-kick) before the unbounded auto-re-kick swarm.


## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 0 |
| Resolved threads | 0 |
| Unresolved threads | 0 |
| Total review comments | 0 |
| Total fix commits (touching thread paths) | 0 |

## Review threads

(no review threads on this PR)

## Fix commits (touching thread paths)

(no commits in this PR touched files anchored by review threads)
