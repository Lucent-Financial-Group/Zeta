# PR #10496 -- fix(discovery): decode all 3 erasures the Adinkra [8,4,4] code corrects

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 10496 |
| Title | fix(discovery): decode all 3 erasures the Adinkra [8,4,4] code corrects |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-08-14T01:32:29Z |
| Merged at | 2026-08-14T01:44:53Z |
| Merge commit SHA | `f7287da8fefe4cd1c1cc083d9778a6198bf2dc0a` |
| Branch | `shadow/adinkra-844-three-erasure-decoder` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/10496 |
| Changed files | 9 |
| Additions / deletions | +858 / -183 |

## Description

Closes `081KZYN3B79087G0R0014ZKE3C`.

`recoverAdinkraErasure` returned `null` at 2 erasures, so the transport used **1 of the 3** erasures its 50%-overhead code pays for. The module header stated that limit as the **code's** property; it was the **decoder's**. A linear code of minimum distance `d` corrects any `d−1` erasures, and [8,4,4] has `d = 4`.

## The fix

- **Decoder** — `recoverAdinkraBlock` solves the erasure system over `H = [Aᵀ | I₄]`, *derived from* `ADINKRA_G` rather than re-declared, by Gauss–Jordan over GF(2) with byte-vector right-hand sides. Recovers every erased position at once; returns `null` exactly when the erased columns of `H` are dependent — the code's limit, not the decoder's. `recoverAdinkraErasure` keeps its signature as a projection onto the first erased position.
- **Receiver** — `addToBlock` attempts recovery on every arrival **from the 4th onwards**, not only at 7-of-8. That seam was half the defect: it discarded blocks the decoder could already solve, and delayed delivery by three packets when it did not.
- **No wire-format change.** Same generator, same 8 packets, same headers. XOR-7/8 fallback untouched.

## Measured — CHECKED

Seeded chaos harness, seed `0x5eed`, 6000 blocks, delivery ratio:

| loss | burst | before | after | ML ceiling |
|---|---|---|---|---|
| 5% | 1 | 96.15% | **100.00%** | 100.00% |
| 10% | 1 | 84.93% | **99.95%** | 99.95% |
| 10% | 4 | 85.80% | **93.17%** | 93.17% |
| 20% | 1 | 52.03% | **99.35%** | 99.35% |
| 30% | 1 | 19.72% | **95.68%** | 95.68% |

The shipped path now equals the independent ML reference at **every** measured point, to all printed digits. Three-erasure census: **0/56 → 56/56**, byte-exact.

## The rate guidance did NOT flip — and the workitem predicted it would

Re-measured after the fix, XOR-7/8 **still dominates** [8,4,4] on goodput at every point `UCH-13` tests:

| loss (uniform) | 0% | 5% | 10% | 20% | 30% | 40% |
|---|---|---|---|---|---|---|
| [8,4,4] after fix | 0.500 | 0.500 | 0.500 | 0.497 | 0.479 | 0.388 |
| XOR-7/8 | 0.875 | 0.833 | 0.722 | 0.409 | 0.125 | 0.010 |

The reason is **rate, not capability**: at low loss goodput → rate, and 7/8 > 4/8 by construction, so no decoder improvement can close that gap — it can only raise [8,4,4] toward its own 0.5 ceiling. Erasure capability starts paying only once the 7/8 code's block-failure probability exceeds `1 − 0.5/0.875 ≈ 0.43`. The fix moves the crossover from roughly **33% → 18%** uniform loss (and from beyond 40% to roughly **31%** at mean burst length 4) without abolishing it.

`UCH-13` keeps its assertions and now **carries its wrong prediction on the record**; `UCH-13b` pins the crossover so the range claim is not read as a verdict against the code. The header's rate guidance is corrected with the numbers.

## Pinned tests that flipped — all updated, none relaxed

| test | was | now |
|---|---|---|
| `UCH-7` | `implRecovered === 0` | `=== 56`, and strengthened from `!== null` to byte-exact |
| `UCH-12` | impl cliff ≤ 0.03, `ml > impl` | impl cliff `=== ml` cliff, at both burst lengths |
| `ULT-4` | "two erasures → unrecoverable" — the decoder's limit dressed as the code's | an erased weight-4 codeword support, with the ambiguity demonstrated |
| `ULT-6` | kept only the last call's return | failed on an *improvement* (delivery moved earlier); now captures the first delivery and pins it at the 4th arrival |

## Standing falsifiers added

- **`ULT-22`** — all **256** erasure patterns, each verdict checked against the code's own algebra (codeword supports enumerated through `computeAdinkraParity`, never a remembered table): `0:1/1 · 1:8/8 · 2:28/28 · 3:56/56 · 4:56/70 · 5+:0`.
- **`ULT-23`** — property test: any 3 losses, any arrival order, through the receiver state machine.
- **`ULT-25/26`** — symbol-length sizing; the exported block-length precondition.
- **`UCH-17`** — end to end: the live receiver loses nothing against the ML ceiling under loss + reorder + duplication.

## Mutation testing — 13 mutants, 12 killed

Three initial survivors were **dead code of mine**, deleted rather than tested around:

- a redundant `rank`/`pivotRow` pair (`pivotRow[c] === c` on every path),
- a redundant "more unknowns than checks" pre-check (the rank test already rejects it),
- an explicit `blockPos` range guard that **could not be observed** — the pre-existing duplicate check's strict `!==` already rejects out-of-range positions, because an out-of-range read is `undefined`, not `null`. I had asserted the opposite in a comment. Corrected; `ULT-24` now pins the real mechanism, and `!=` fails it.

The one remaining survivor (transposing `H`) is unobservable because `A` is symmetric for this code — documented in place rather than chased, per the `ULT-16` precedent.

## Filed, not bundled

`081KZYY6SVJ087G0R0035SW945` — the harness's `meanBurstLength = 1` is **not** i.i.d. Bernoulli. It forbids consecutive losses (`P(drop | prev drop) = 0.00000` over 400k packets; per-block `k=4` understated 4.6x vs `Binomial(8, 0.1)`, `k≥5` impossible). So every "uniform loss" row above is optimistic in the code's favour. That is a change to the *measuring instrument*, not the decoder — landing them together would make it impossible to tell which one moved a number.

Independent of `081KZYQ8KNB087G0R000G8QPRE` (loss-signal separation) and `081KZYN37T4087G0R00181THA4` (AIMD, LATENT — untouched). Erasure *recovery* is not congestion *control*.

## Local gates

`bun test src/Core.TypeScript/discovery/` — **287 pass, 0 fail** (33 files). Prettier clean; `tsc` reports nothing on the touched files.

🤖 Generated with [Claude Code](https://claude.com/claude-code)


## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 0 |
| Resolved threads | 0 |
| Unresolved threads | 0 |
| Total review comments | 0 |
| Total fix commits (touching thread paths) | 0 |

## Review threads

(no review threads on this PR)

## Fix commits (touching thread paths)

(no commits in this PR touched files anchored by review threads)
