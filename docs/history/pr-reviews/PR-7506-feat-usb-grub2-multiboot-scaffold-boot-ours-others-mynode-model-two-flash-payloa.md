# PR #7506 -- feat(usb): GRUB2 multiboot scaffold — boot ours+others + MyNode Model Two flash payload (sha256-pinned)

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 7506 |
| Title | feat(usb): GRUB2 multiboot scaffold — boot ours+others + MyNode Model Two flash payload (sha256-pinned) |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-06-10T20:26:30Z |
| Merged at | 2026-06-10T22:27:01Z |
| Merge commit SHA | `43b9adaa2e8a39691a162f7d4cbf4483ec258cf2` |
| Branch | `shadow-grub2-multiboot-usb` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/7506 |
| Changed files | 2 |
| Additions / deletions | +16 / -6 |

## Description

feat(usb): GRUB2 multiboot scaffold — boot ours + others + carry flash payloads (MyNode Model Two pinned)

Aaron 2026-06-10: "change our USB to give a choice of our boot and the chance to boot others too ...
use grub2 for the usb ... we need the my node model 2 iso (mynode_amd64_0-3-34.img.gz) ... no model 1."

full-ai-cluster/usb-nixos-installer/multiboot/:

- images.manifest — DECLARATIVE payload list (name/kind/source/sha256). NO binaries in repo
  (no-binary-in-proof-lineage); fetched + sha256-verified at build, never committed. Entries:
  - zeta-installer (grub-iso-local) — built by `nix build .#installer-iso`, GRUB loopback-booted.
  - mynode-model-two (flash-img) — mynode_amd64_0-3-34.img.gz, SHA-256
    03498d02...81dde (from MyNode's published SHA256SUMS, verified 2026-06-10).
- grub.cfg — GRUB2 menu template: Zeta installer (loopback) + slots for other x86 ISOs + local-disk
  fallback. MyNode is NOT a boot entry.
- README.md — layout, build recipe, the MyNode-is-a-flash-payload wrinkle + dd recipe.

KEY HONESTY: MyNode ships raw .img.gz (even amd64), NOT bootable ISOs — so Model Two is a FLASH
PAYLOAD (dd onto td5/td6), carried under /payloads/, not GRUB-booted. The scaffold handles both
boot-ISOs and flash-imgs. Model 1 dropped (we have none).

Scope: declarative manifest + GRUB config + layout land now (reviewable). The build-multiboot-usb.ts
builder (the only piece touching a physical device) lands next with its own qemu/DST test, mirroring
tools/zflash. README markdownlint clean.

AgencySignature-v1:
  persona: otto
  hat: shadow
  surface: full-ai-cluster/usb-nixos-installer/multiboot
  intent: grub2-multiboot-usb-scaffold-mynode-model-two
  authorization: aaron-authored ("use grub2 for the usb")
  reversible: yes
  gated-class: none
  build: markdownlint clean; manifest sha256 verified against MyNode SHA256SUMS
  register: grounded
  ferry: no

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>


## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 1 |
| Resolved threads | 1 |
| Unresolved threads | 0 |
| Total review comments | 1 |
| Total fix commits (touching thread paths) | 3 |

## Review threads

### Thread 1 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6InCgO`
- **Path**: `full-ai-cluster/usb-nixos-installer/multiboot/grub.cfg`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-06-10T20:28:46Z on `full-ai-cluster/usb-nixos-installer/multiboot/grub.cfg`:33 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Resolve the current ISO kernel and initrd paths**

  When this multiboot entry is used with the current installer flake, GRUB will look for legacy top-level paths that the repo already knows are absent on nixpkgs 25.11. `full-ai-cluster/usb-nixos-installer/flake.nix` pins `nixos-25.11`, and `tools/ci/audit-installer-iso-content.ts` documents the empirically confirmed 25.11 layout as `boot/nix/store/&lt;hash&gt;-linux-&lt;version&gt;/bzImage` and `boot/nix/store/&lt;hash&gt;-initrd-linux-&lt;version&gt;/initrd`, with the hash varying per build. Hard-coding `(loop)/boot/bzImage` and `(loop)/boot/initrd` here means the primary “Zeta NixOS Installer” menu entry can fail before the kernel loads; the builder needs to inspect the built ISO and substitute the actual paths or use the same suffix-pattern resolution as the audit.

  Useful? React with 👍 / 👎.
  </pre>

## Fix commits (touching thread paths)

### `bda5c2928e59811a0c50a61796385324014c0b31` -- 2026-06-10T20:26:08Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `full-ai-cluster/usb-nixos-installer/multiboot/grub.cfg`

**Message:**

```
feat(usb): GRUB2 multiboot scaffold — boot ours + others + carry flas…

…h payloads (MyNode Model Two pinned)

Aaron 2026-06-10: "change our USB to give a choice of our boot and the chance to boot others too ...
use grub2 for the usb ... we need the my node model 2 iso (mynode_amd64_0-3-34.img.gz) ... no model 1."

full-ai-cluster/usb-nixos-installer/multiboot/:
- images.manifest — DECLARATIVE payload list (name/kind/source/sha256). NO binaries in repo
  (no-binary-in-proof-lineage); fetched + sha256-verified at build, never committed. Entries:
  - zeta-installer (grub-iso-local) — built by `nix build .#installer-iso`, GRUB loopback-booted.
  - mynode-model-two (flash-img) — mynode_amd64_0-3-34.img.gz, SHA-256
    03498d02...81dde (from MyNode's published SHA256SUMS, verified 2026-06-10).
- grub.cfg — GRUB2 menu template: Zeta installer (loopback) + slots for other x86 ISOs + local-disk
  fallback. MyNode is NOT a boot entry.
- README.md — layout, build recipe, the MyNode-is-a-flash-payload wrinkle + dd recipe.

KEY HONESTY: MyNode ships raw .img.gz (even amd64), NOT bootable ISOs — so Model Two is a FLASH
PAYLOAD (dd onto td5/td6), carried under /payloads/, not GRUB-booted. The scaffold handles both
boot-ISOs and flash-imgs. Model 1 dropped (we have none).

Scope: declarative manifest + GRUB config + layout land now (reviewable). The build-multiboot-usb.ts
builder (the only piece touching a physical device) lands next with its own qemu/DST test, mirroring
tools/zflash. README markdownlint clean.

AgencySignature-v1:
  persona: otto
  hat: shadow
  surface: full-ai-cluster/usb-nixos-installer/multiboot
  intent: grub2-multiboot-usb-scaffold-mynode-model-two
  authorization: aaron-authored ("use grub2 for the usb")
  reversible: yes
  gated-class: none
  build: markdownlint clean; manifest sha256 verified against MyNode SHA256SUMS
  register: grounded
  ferry: no

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `3247812252051908d3617aa60af3de904a66a83b` -- 2026-06-10T20:30:12Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `full-ai-cluster/usb-nixos-installer/multiboot/grub.cfg`

**Message:**

```
feat(usb): MyNode payload resolves LATEST every build (flash-img-late…

…st), not hard-coded (Aaron)

Aaron 2026-06-10: 'make sure that process pulls the latest mynode every time and is not hard coded to
one.' New manifest kind flash-img-latest: builder fetches MyNode SHA256SUMS, picks newest
mynode_amd64_*.img.gz, verifies download against that fresh checksum. Latest-always + transit
integrity; version no longer pinned in-repo. Tradeoff named (verifies transit, not compromised
upstream; pinned flash-img would freeze version to guard source — latest wins for a re-flashable
appliance). README + grub.cfg de-hardcoded. markdownlint clean.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `4856bfd9f1ca342cace307415d2a75614318938c` -- 2026-06-10T22:26:38Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `full-ai-cluster/usb-nixos-installer/multiboot/grub.cfg`

**Message:**

```
fix(usb): grub.cfg kernel/initrd are builder-substituted @KERNEL@/@IN…

…ITRD@ placeholders — nixos-25.11 store paths, not /boot (PR P2)

PR review (codex P2, correct): hard-coded (loop)/boot/bzImage + /boot/initrd fail on nixos-25.11 —
kernel/initrd live under boot/nix/store/<hash>-linux-<ver>/bzImage + <hash>-initrd-linux-<ver>/initrd
(hash per build; tools/ci/audit-installer-iso-content.ts documents the layout). Changed to @KERNEL@/
@INITRD@ placeholders the builder resolves via the audit's suffix-pattern; README caveat updated.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```
