# PR #6172 -- feat(081KSV2WD0008QG0R00051XS0N slice 5 pt1): tri-boolean float v0 spec + TS reference (middle-out, self-describing; 8/8)

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 6172 |
| Title | feat(081KSV2WD0008QG0R00051XS0N slice 5 pt1): tri-boolean float v0 spec + TS reference (middle-out, self-describing; 8/8) |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-05-31T00:50:33Z |
| Merged at | 2026-05-31T00:53:15Z |
| Merge commit SHA | `3a1b004aae38743d5d625164f2724271d066983b` |
| Branch | `otto-cli/b0944-slice5-tri-boolean-float-v0-2026-05-30` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/6172 |
| Changed files | 5 |
| Additions / deletions | +366 / -0 |

## Description

Slice 5 = the tri-boolean **float** — the hard, novel slice. The 081KSV2WD0008QG0R00051XS0N row says *"spec firming required before impl,"* so this lands the **spec firming + a TS reference (executable spec)**. F#/C#/Rust parity (slice 5 pt2) waits for your ratification of the layout.

## Spec (proposed v0 — `docs/research/...tri-boolean-float-v0-spec...`)
**Middle-out, self-describing.** Layout `[ high value | decoder | low value ]`, read middle-out: the **middle decoder selects how the outer ends decode**. v0 decoder semantics: `mode` = radix-point position → decoded number = `intOf(high ++ low) / 2^mode` (self-describing precision — *same end bits, different magnitude depending on the middle*). Built **from the digital-qubit cell** (slices 1–4): every trit is a `Tri`.

**Tri-valued (the load-bearing novelty):**
| `N` location | held-state | `measure` feedback |
|---|---|---|
| a **value** trit | value held, interpretation known | `value-superposed` |
| a **decoder** trit | the decode *instruction itself* is held — the qubit property at the **interpretation** level | `interpretation-superposed` |

`measure`/`cooperate` + `Result<number, FloatFeedback>` mirror the tri-boolean discipline + monad-propagation.

## TS reference (`src/Core.TypeScript/tri-boolean-float/`)
`decode` (middle-out), `measure`, `cooperate`, `isHeld`, `fromValue` (canonical smallest-mode encode), `fromTrits`. **Verified: `bun test` 8/8** (round-trip; middle-decodes-ends; value-superposed; interpretation-superposed; decoder-N-dominates; cooperate-preserves; not-representable; canonical-mode).

## 6 OPEN decisions surfaced for your ratification
1. **decoder semantics** (v0: radix-point; alts: power-of-two exponent / high-low split / regime)
2. **sign** (v0 unsigned; a tri-valued sign trit is an option → held-sign)
3. **widths** (v0 4/3/4; fixed vs posit-style variable)
4. **N-in-value** → carry the held *range* (v1, composes with wonder-compression)
5. **decoder-N** → carry the candidate-mode set (v1)
6. **field order**

Prior art: Gustafson posits / tapered precision / tagged encodings; novel = **tri-valued + middle-as-decoder-read-outward**.

> On ratification → F#/C#/Rust parity + the cross-language conformance harness (slice 6 = the BFT ballot).

🤖 Generated with [Claude Code](https://claude.com/claude-code)


## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 1 |
| Resolved threads | 0 |
| Unresolved threads | 1 |
| Total review comments | 1 |
| Total fix commits (touching thread paths) | 1 |

## Review threads

### Thread 1 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM6F6Due`
- **Path**: `src/Core.TypeScript/tri-boolean-float/tri-boolean-float.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-31T00:53:22Z on `src/Core.TypeScript/tri-boolean-float/tri-boolean-float.ts`:26 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Avoid 32-bit shifts when encoding wider shapes**

  When callers use a valid `FloatShape` with more than 32 value trits, this bitwise shift silently truncates `v` to a 32-bit integer, so `fromValue` can return `ok: true` for the wrong encoding; for example, `fromValue(2 ** 32, { highWidth: 33, decoderWidth: 1, lowWidth: 0 })` decodes back to `0` instead of `4294967296`. Since `FloatShape` is public and not capped to 32-bit widths, use arithmetic/BigInt bit extraction or reject unsupported widths rather than relying on `&gt;&gt;` (the `maxMode` calculation has the same 32-bit coercion risk for wide decoders).

  Useful? React with 👍 / 👎.
  </pre>

## Fix commits (touching thread paths)

### `334dd1c6b977701509034b21dfccfc3cc56a9853` -- 2026-05-31T00:50:21Z -- `claude`

**Touched files (intersect with thread paths):**

- `src/Core.TypeScript/tri-boolean-float/tri-boolean-float.ts`

**Message:**

```
feat(081KSV2WD0008QG0R00051XS0N slice 5 pt1): tri-boolean float v0 spec + TS reference (m…

…iddle-out, self-describing; 8/8)

Slice 5 = the tri-boolean FLOAT (the hard, novel slice; the 081KSV2WD0008QG0R00051XS0N row says
'spec firming required before impl'). This lands the spec firming + a TS
reference (executable spec); F#/C#/Rust parity (slice 5 pt2) waits for operator
ratification of the layout.

Spec (docs/research/...-tri-boolean-float-v0-spec-...): PROPOSED v0, middle-out,
self-describing. Layout [high value | decoder | low value], read middle-out: the
MIDDLE decoder selects how the OUTER ends decode. v0 decoder semantics: mode =
radix-point position; decoded number = intOf(high++low) / 2^mode (self-describing
precision -- same end bits, different magnitude per the middle). Built FROM the
digital-qubit cell (slices 1-4): every trit is a Tri.

Tri-valued, the load-bearing novelty: N in a VALUE trit => value-superposed
(value held, interpretation known); N in a DECODER trit => interpretation-
superposed (the decode INSTRUCTION itself is held -- the qubit property at the
interpretation level). measure/cooperate + Result<number, FloatFeedback> mirror
the tri-boolean discipline + monad-propagation.

TS reference (src/Core.TypeScript/tri-boolean-float/): decode (middle-out),
measure, cooperate, isHeld, fromValue (canonical smallest-mode encode), fromTrits.
Verified: bun test 8/8 (round-trip; middle-decodes-ends; value-superposed;
interpretation-superposed; decoder-N-dominates; cooperate-preserves; not-
representable; canonical-mode).

6 OPEN design decisions surfaced for operator ratification (decoder semantics
[radix-point vs exponent vs split], sign, widths, N-in-value held-range,
decoder-N candidate-set, field order). Prior art: Gustafson posits / tapered
precision / tagged encodings; novel = tri-valued + middle-as-decoder-read-outward.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
```
