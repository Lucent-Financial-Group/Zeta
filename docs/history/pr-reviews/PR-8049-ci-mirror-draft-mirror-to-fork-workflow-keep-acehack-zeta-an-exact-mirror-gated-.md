# PR #8049 -- ci(mirror): draft mirror-to-fork workflow — keep acehack/Zeta an exact mirror [GATED: Dejan + secret]

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 8049 |
| Title | ci(mirror): draft mirror-to-fork workflow — keep acehack/Zeta an exact mirror [GATED: Dejan + secret] |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-06-13T19:29:06Z |
| Merged at | 2026-06-13T20:06:35Z |
| Merge commit SHA | `e0d3331eb096be8e753cdfc6df02988b6bd1651e` |
| Branch | `shadow/mirror-to-fork-workflow` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/8049 |
| Changed files | 1 |
| Additions / deletions | +78 / -0 |

## Description

## What

Drafts `.github/workflows/mirror-to-fork.yml` — keeps the **acehack/Zeta fork an exact mirror** of canonical `Lucent-Financial-Group/Zeta`, so it stops drifting as origin advances (every merge currently pushes a fresh `pr-archive-*` branch onto origin only).

Follows the 2026-06-13 one-time mirror reconciliation (this PR automates keeping it that way).

## Behavior

- **Triggers:** push (all branches + tags), `delete` (propagate branch removals), 6-hourly safety-net `schedule`, `workflow_dispatch`.
- **Sync:** `git push --prune --force` heads + tags → fork **matches** origin (prunes fork refs absent on origin, force-syncs divergent tips).
- **Guard:** canonical-repo-only (`if: github.repository == 'Lucent-Financial-Group/Zeta'`).

## ⚠️ GATED — do not auto-merge

This touches **workflows + a cross-repo secret**, so per GOVERNANCE.md §23 it needs:
1. **@Dejan (devops) review.**
2. **Human sign-off.**
3. **Secret `ACEHACK_MIRROR_TOKEN`** provisioned first — a **fine-grained PAT scoped to `acehack/Zeta` only**, Contents: read+write. (The default `GITHUB_TOKEN` cannot push to a different repo.) Rotate per the secret-rotation cadence.

**Destructive-by-design caveat:** `--prune` deletes fork refs absent on origin. Safe *only* while the fork-holds-no-unique-content contract holds (it does, post-reconciliation; prior fork-unique tips are preserved under `archive/2026-06-13-acehack/*` tags on origin).

## Conventions

`actions/checkout` pinned `@df4cb1c0` (v6.0.3), `ubuntu-24.04`, minimal `permissions: contents: read`, `concurrency` group, off-peak cron minute. YAML validated.

## Authorization

@AceHack: *"yeah draft the mirror-sync workflow (shadow*)"* — drafted for review, intentionally not auto-merged.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 0 |
| Resolved threads | 0 |
| Unresolved threads | 0 |
| Total review comments | 0 |
| Total fix commits (touching thread paths) | 0 |

## Review threads

(no review threads on this PR)

## Fix commits (touching thread paths)

(no commits in this PR touched files anchored by review threads)
