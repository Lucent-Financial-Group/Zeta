# PR #8741 -- feat(core): resume darkhall heat board snapshots

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 8741 |
| Title | feat(core): resume darkhall heat board snapshots |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-06-20T15:56:10Z |
| Merged at | 2026-06-20T16:56:21Z |
| Merge commit SHA | `d3c4d0a4985fc26212249e3fdd840278dac2e791` |
| Branch | `codex/darkhall-heat-snapshot-resume` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/8741 |
| Changed files | 4 |
| Additions / deletions | +688 / -7 |

## Description

## Summary
- add a source-owned `IHeatBoardStateStore` boundary and in-memory reference implementation
- add save/load helpers so a heat-board continuation can load its saved `ScheduledRoomState` and run the next bounded SimLoop link
- keep missing snapshots, bad tokens, foreign loop ids, bad namespaces, and resume tick overflow on `HeatBoardContinuationFeedback`
- tighten resume admission so the full next lap fits in `int` tick space before offsetting the host interrupt source

## Validation
- dotnet test tests/Tests.FSharp/Tests.FSharp.fsproj -c Release --filter FullyQualifiedName~DarkHallSchedulerTests
- dotnet format --verify-no-changes
- dotnet build -c Release
- dotnet test Zeta.sln -c Release
- bun run preflight:quick

Agency-Signature-Version: 1
Agent: vera
Agent-Runtime: Codex
Agent-Model: gpt-5.5
Credential-Identity: acehack
Credential-Mode: shared
Human-Review: not-implied-by-credential
Human-Review-Evidence: none
Action-Mode: human-directed
Task: none
Co-Authored-By: Codex <noreply@openai.com>

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | true |
| Total threads | 6 |
| Resolved threads | 6 |
| Unresolved threads | 0 |
| Total review comments | 6 |
| Total fix commits (touching thread paths) | 6 |

## Review threads

### Thread 1 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6LBWYU`
- **Path**: `src/Core/DarkHallScheduler.fs`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-06-20T15:58:55Z on `src/Core/DarkHallScheduler.fs`:200 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Carry the absolute lap count when resuming**

  When `resumeHeatBoardSimLoop` uses this helper with a saved state, the returned `Outcome` still contains only the resumed link's local laps. If that budget-stopped outcome is then passed through `encodeHeatBoardContinuation`, `SimLoop.continueAfter` sets `NextLap` back to `List.length outcome.Laps` (for example 2 after every 2-lap link) instead of the cumulative lap/tick boundary from `start.CompletedTicks`; the next resume therefore offsets the host source to tick 2 again instead of tick 4, replaying the same absolute interrupts and corrupting multi-hop heat-board chains. The resumed path needs to preserve/encode the cumulative lap offset before minting the next token.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 2 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6LBc1v`
- **Path**: `src/Core/DarkHallScheduler.fs`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-06-20T16:13:38Z on `src/Core/DarkHallScheduler.fs`:404 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Validate resumed snapshots against tokens**

  When a namespace-valid token is paired with a saved pointer from a different lap, this path trusts `token.NextLap` for the source offset but trusts the loaded snapshot for state. For example, `spawn:darkhall-heat-board:4:...:saves/.../lap-2-tick-2.heat-board` resumes the lap-2 state while offsetting the host source to lap 4, skipping absolute ticks 2-3 and then stamping the resulting laps as 3-4; compare `admission.Token.NextLap` with `start.CompletedLaps` (or derive the offset from the snapshot) before running.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 3 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6LBc1x`
- **Path**: `src/Core/DarkHallScheduler.fs`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-06-20T16:13:38Z on `src/Core/DarkHallScheduler.fs`:329 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Bound the full resumed link before offsetting**

  When `ticksPerLap = 1` and `token.NextLap = Int32.MaxValue`, this guard accepts the continuation because the first lap's last tick still fits. A resumed run with a budget above one lap then asks `resumeHeatBoardSource` for local tick 1, so the int addition crosses the range and either throws or wraps instead of returning `ResumeTickOverflow`; the admission/resume path needs to validate the whole finite link's tick span or perform checked `int64` offsetting.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 4 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6LBgOd`
- **Path**: `src/Core/DarkHallScheduler.fs`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-06-20T16:21:53Z on `src/Core/DarkHallScheduler.fs`:218 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Make cumulative laps visible to cuts**

  When a cut predicate depends on the new `ScheduledRoomState.CompletedLaps` field, resumed links evaluate that predicate before the cumulative lap count is stamped onto the states. `record` preserves the loaded count for every tick, and this post-processing only fixes the returned ledger after `SimLoop.run` has already decided whether to close, so a chain that should stop at an absolute lap boundary (for example `state.CompletedLaps &gt;= 4`) instead runs to the link budget and mints another continuation. The lap count needs to be advanced before the cut sees each lap state, not only after the run returns.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 5 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6LBlkU`
- **Path**: `src/Core/DarkHallScheduler.fs`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-06-20T16:36:59Z on `src/Core/DarkHallScheduler.fs`:220 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Guard cumulative lap overflow before incrementing**

  Fresh evidence in this revision is that `resumeLinkBaseTick` still admits `NextLap = Int32.MaxValue` when `ticksPerLap = 1` and the resumed budget is one lap, because the last tick is exactly `Int32.MaxValue`; the first measurement then executes this `int` increment (and `stampCumulativeLaps` repeats the addition), so the resume path wraps/throws instead of returning typed feedback and can mint a negative lap pointer. Please reject continuations whose next completed lap would exceed the `int` range before running the link.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 6 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6LBpd0`
- **Path**: `src/Core/DarkHallScheduler.fs`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-06-20T16:46:38Z on `src/Core/DarkHallScheduler.fs`:446 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Reject tick-boundary mismatches on resume**

  Even with the new lap check, a token produced with one `ticksPerLap` value can be resumed with a different value and still pass here because only `CompletedLaps` is compared. For example, a 2-lap snapshot created with `ticksPerLap = 2` is accepted when the caller resumes with `ticksPerLap = 1`, so `ResumeBaseTick` becomes 2 and the host source replays absolute ticks 2.. instead of continuing at tick 4; validate the saved tick boundary or encode the lap width before offsetting the source.

  Useful? React with 👍 / 👎.
  </pre>

## Fix commits (touching thread paths)

### `8fffe8ddbf875a84395542757be436961a09710b` -- 2026-06-20T15:55:19Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `src/Core/DarkHallScheduler.fs`

**Message:**

```
feat(core): resume darkhall heat board snapshots

Add a DarkHall-owned heat-board state store boundary and a resumed-loop helper that admits a continuation token, loads the saved state, offsets the host interrupt source, and runs one more finite SimLoop link.

State snapshot misses and malformed or foreign tokens stay on the typed HeatBoardContinuationFeedback channel. Resume admission now also proves the whole next lap fits inside int tick space before offsetting the source.

Validation:

- dotnet test tests/Tests.FSharp/Tests.FSharp.fsproj -c Release --filter FullyQualifiedName~DarkHallSchedulerTests

- dotnet format --verify-no-changes

- dotnet build -c Release

- dotnet test Zeta.sln -c Release

- bun run preflight:quick

Agency-Signature-Version: 1
Agent: vera
Agent-Runtime: Codex
Agent-Model: gpt-5.5
Credential-Identity: acehack
Credential-Mode: shared
Human-Review: not-implied-by-credential
Human-Review-Evidence: none
Action-Mode: human-directed
Task: none
Co-Authored-By: Codex <noreply@openai.com>
```

### `a0fcd7f4017ba96510dd4f0b01c355f68db17115` -- 2026-06-20T16:09:52Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `src/Core/DarkHallScheduler.fs`

**Message:**

```
fix(core): carry heat board resume lap boundary

Stamp heat-board SimLoop outcomes with cumulative lap boundaries before minting continuation tokens. The state pointer and NextLap now advance across chained resumed links instead of replaying the first resumed window.

Add a multi-hop heat-board resume regression that saves the second link, mints a second token, and proves the third link starts at the next absolute host tick window.

Validation:

- dotnet test tests/Tests.FSharp/Tests.FSharp.fsproj -c Release --filter FullyQualifiedName~DarkHallSchedulerTests

- dotnet format --verify-no-changes

- dotnet build -c Release

- dotnet test Zeta.sln -c Release

- bun run preflight:quick

Agency-Signature-Version: 1

Agent: vera

Agent-Runtime: Codex

Agent-Model: gpt-5.5

Credential-Identity: acehack

Credential-Mode: shared

Human-Review: not-implied-by-credential

Human-Review-Evidence: none

Action-Mode: human-directed

Task: pr-8741-review-fix

Co-Authored-By: Codex <noreply@openai.com>
```

### `e9de4e62d921d61176612af57a7fc2d7227b6ecd` -- 2026-06-20T16:25:54Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `src/Core/DarkHallScheduler.fs`

**Message:**

```
fix(core): validate heat board resume boundaries

Reject heat-board resume tokens when the loaded snapshot's cumulative lap count does not match the token's NextLap, so a valid namespace pointer cannot be paired with the wrong saved state.

Validate the whole finite resumed link against the tick range before offsetting the host source, and keep the source adapter on int64 arithmetic so callers cannot trigger int overflow at the edge.

Validation:

- dotnet test tests/Tests.FSharp/Tests.FSharp.fsproj -c Release --filter FullyQualifiedName~DarkHallSchedulerTests

- dotnet format --verify-no-changes

- bun run preflight:quick

- bun src/Core.TypeScript/ci/cross-verify-all.ts

- dotnet test Zeta.sln -c Release

Agency-Signature-Version: 1

Agent: vera

Agent-Runtime: Codex

Agent-Model: gpt-5.5

Credential-Identity: acehack

Credential-Mode: shared

Human-Review: not-implied-by-credential

Human-Review-Evidence: none

Action-Mode: human-directed

Task: pr-8741-review-fix-2

Co-Authored-By: Codex <noreply@openai.com>
```

### `91df909dbac0c060ffdad34659c030cbe1b78d88` -- 2026-06-20T16:32:48Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `src/Core/DarkHallScheduler.fs`

**Message:**

```
fix(core): expose resumed lap boundary to cuts

Advance the heat-board cumulative lap boundary during measurement so cut predicates observe the same absolute CompletedLaps that the returned ledger later records.

Add a resumed-link regression where the cut closes at absolute lap 4 even though the finite link has budget for more laps.

Validation:

- dotnet test tests/Tests.FSharp/Tests.FSharp.fsproj -c Release --filter FullyQualifiedName~DarkHallSchedulerTests

- dotnet format --verify-no-changes

- bun run preflight:quick

- dotnet test tests/Tests.FSharp.Git/Tests.FSharp.Git.fsproj -c Release --filter FullyQualifiedName~DurableObserveGitTests

- dotnet test Zeta.sln -c Release

Agency-Signature-Version: 1

Agent: vera

Agent-Runtime: Codex

Agent-Model: gpt-5.5

Credential-Identity: acehack

Credential-Mode: shared

Human-Review: not-implied-by-credential

Human-Review-Evidence: none

Action-Mode: human-directed

Task: pr-8741-review-fix-3

Co-Authored-By: Codex <noreply@openai.com>
```

### `763aff68faf47514f1ce9da42b3ad2e9a2f962c5` -- 2026-06-20T16:48:29Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `src/Core/DarkHallScheduler.fs`

**Message:**

```
fix(core): guard resumed lap counter overflow

Reject resumed heat-board finite links whose completed-lap boundary would exceed the int range even when the tick span itself still fits.

Add the one-lap Int32.MaxValue regression so the continuation path returns typed ResumeTickOverflow before measurement can increment the lap counter.

Validation:

- dotnet test tests/Tests.FSharp/Tests.FSharp.fsproj -c Release --filter FullyQualifiedName~DarkHallSchedulerTests
- dotnet format --verify-no-changes
- bun run preflight:quick
- bun src/Core.TypeScript/ci/cross-verify-all.ts
- dotnet test Zeta.sln -c Release

Agency-Signature-Version: 1
Agent: vera
Agent-Runtime: Codex
Agent-Model: gpt-5.5
Credential-Identity: acehack
Credential-Mode: shared
Human-Review: not-implied-by-credential
Human-Review-Evidence: none
Action-Mode: human-directed
Task: pr-8741-review-fix-4
Co-Authored-By: Codex <noreply@openai.com>
```

### `16ac3c260dc0d09cee2361aaedafcedcc61c6703` -- 2026-06-20T16:55:23Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `src/Core/DarkHallScheduler.fs`

**Message:**

```
fix(core): reject mismatched resume tick boundaries

Validate the loaded heat-board snapshot tick boundary against the continuation-derived ResumeBaseTick before offsetting the host source.

Add a regression where a two-tick-per-lap snapshot is refused when resumed as one tick per lap, preventing replay of already-spent absolute ticks.

Validation:

- dotnet test tests/Tests.FSharp/Tests.FSharp.fsproj -c Release --filter FullyQualifiedName~DarkHallSchedulerTests
- dotnet format --verify-no-changes
- bun run preflight:quick
- bun src/Core.TypeScript/ci/cross-verify-all.ts
- dotnet test Zeta.sln -c Release

Agency-Signature-Version: 1
Agent: vera
Agent-Runtime: Codex
Agent-Model: gpt-5.5
Credential-Identity: acehack
Credential-Mode: shared
Human-Review: not-implied-by-credential
Human-Review-Evidence: none
Action-Mode: human-directed
Task: pr-8741-review-fix-5
Co-Authored-By: Codex <noreply@openai.com>
```
