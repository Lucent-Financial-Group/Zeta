# PR #3775 -- feat(bus): add subscribeOnce helper (strictly decomposed from B-0459)

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 3775 |
| Title | feat(bus): add subscribeOnce helper (strictly decomposed from B-0459) |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-05-16T06:17:21Z |
| Merged at | 2026-05-17T00:18:09Z |
| Merge commit SHA | `cd5ea9eb3e7e6b4fb6b92d1a2efb9d14fe1cc28a` |
| Branch | `lior/decompose-3643-bus-subscribe-new` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/3775 |
| Changed files | 2 |
| Additions / deletions | +170 / -0 |

## Description

Strict decomposition of B-0459 (#3643). This PR only contains the subscribeOnce bus helper, leaving the subscriber implementation for a subsequent slice.

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | true |
| Total threads | 14 |
| Resolved threads | 14 |
| Unresolved threads | 0 |
| Total review comments | 21 |
| Total fix commits (touching thread paths) | 6 |

## Review threads

### Thread 1 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6CiR4S`
- **Path**: `tools/bus/subscribe.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-16T06:19:09Z on `tools/bus/subscribe.ts`:4 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Fix unresolved local imports before using subscribeOnce**

  This file imports `./bus` and `./types`, but this commit only adds `tools/bus/subscribe.ts` and `tools/bus/subscribe.test.ts`; there is no `tools/bus/bus.ts` or `tools/bus/types.ts` in the parent tree or this diff. In this state, loading `subscribe.ts` fails with module-resolution errors, so the new helper and its test cannot run at all.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 2 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6CiR8Z`
- **Path**: `tools/bus/subscribe.ts`
- **Replies**: 0

**Initial comment:**

- **`github-advanced-security[bot]` (bot)** at 2026-05-16T06:19:24Z on `tools/bus/subscribe.ts`:52 (association: NONE)

  <pre>
  ## CodeQL / Potential file system race condition

  The file may have changed since it [was checked](1).

  [Show more details](https://github.com/Lucent-Financial-Group/Zeta/security/code-scanning/86)
  </pre>

### Thread 3 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6CiSRS`
- **Path**: `tools/bus/subscribe.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-16T06:20:36Z on `tools/bus/subscribe.ts`:5 (association: NONE)

  <pre>
  P0: This file imports `./bus` and `./types`, but there are no corresponding modules under `tools/bus/` in this PR (and none exist on main in the current tree). As written, `subscribeOnce` won’t typecheck/build. Either include the missing `bus`/`types` modules in this PR or change these imports to the existing bus/type definitions elsewhere in the repo (if any).

  This issue also appears in the following locations of the same file:
  - line 6
  - line 17

  </pre>

### Thread 4 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6CiSRb`
- **Path**: `tools/bus/subscribe.test.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-16T06:20:36Z on `tools/bus/subscribe.test.ts`:7 (association: NONE)

  <pre>
  P0: This test imports `./bus` and `./types`, but those modules are not present under `tools/bus/` in this PR (and none exist on main in the current tree). The test suite won’t typecheck/run until the missing modules are added or the imports are corrected.

  This issue also appears in the following locations of the same file:
  - line 1
  - line 8

  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-16T21:23:36Z on `tools/bus/subscribe.test.ts`:7 (association: MEMBER)

    <pre>
    Verified `./bus` + `./types` exist on `main` and export the required symbols (`BUS_DIR`/`ensureDir`/`list` from `tools/bus/bus.ts`; `MessageEnvelope`/`Topic` from `tools/bus/types.ts`, both shipped via B-0400 slice 1). Imports resolve on the merge target — false positive per `.claude/rules/blocked-green-ci-investigate-threads.md` known-FP class ("imported file doesn't exist" when modules verifiably do exist on main).
    </pre>

### Thread 5 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6Clt2g`
- **Path**: `tools/bus/subscribe.ts`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-16T21:05:30Z on `tools/bus/subscribe.ts`:4 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Add missing bus/type modules before importing subscribeOnce**

  This commit introduces imports from `./bus` and `./types`, but in commit `e06f083312969e1786c2816cfad3a346d4167611` the `tools/bus` tree contains only `subscribe.ts` and `subscribe.test.ts`, so these relative modules cannot be resolved. As a result, loading `subscribe.ts` fails immediately, and both the new helper and its test are unusable until those modules are added in the same changeset (or the imports are redirected to existing paths).

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-16T21:23:37Z on `tools/bus/subscribe.ts`:4 (association: MEMBER)

    <pre>
    Verified `./bus` + `./types` exist on `main` and export the required symbols (`BUS_DIR`/`ensureDir`/`list` from `tools/bus/bus.ts`; `MessageEnvelope`/`Topic` from `tools/bus/types.ts`, both shipped via B-0400 slice 1). Imports resolve on the merge target — false positive per `.claude/rules/blocked-green-ci-investigate-threads.md` known-FP class ("imported file doesn't exist" when modules verifiably do exist on main).
    </pre>

### Thread 6 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6Clt_L`
- **Path**: `tools/bus/subscribe.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-16T21:05:47Z on `tools/bus/subscribe.ts`:5 (association: NONE)

  <pre>
  P0: This file imports `./bus` and `./types`, but there are no corresponding modules in `tools/bus/` (and no `BUS_DIR`/`MessageEnvelope` definitions elsewhere in the repo). As-is, this will fail TypeScript compilation/test execution. Add the missing `bus.ts`/`types.ts` (or adjust the import paths to where they actually live) in this PR slice so `subscribeOnce` is buildable.

  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-16T21:23:38Z on `tools/bus/subscribe.ts`:5 (association: MEMBER)

    <pre>
    Verified `./bus` + `./types` exist on `main` and export the required symbols (`BUS_DIR`/`ensureDir`/`list` from `tools/bus/bus.ts`; `MessageEnvelope`/`Topic` from `tools/bus/types.ts`, both shipped via B-0400 slice 1). Imports resolve on the merge target — false positive per `.claude/rules/blocked-green-ci-investigate-threads.md` known-FP class ("imported file doesn't exist" when modules verifiably do exist on main).
    </pre>

### Thread 7 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6Clt_f`
- **Path**: `tools/bus/subscribe.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-16T21:05:48Z on `tools/bus/subscribe.ts`:27 (association: NONE)

  <pre>
  P1: `surface` is interpolated directly into the `seen-&lt;surface&gt;.json` filename. If `surface` can ever come from input (CLI/env), values like `../...` will escape `BUS_DIR` and overwrite arbitrary files. Constrain `surface` to a safe filename (e.g., allowlist `[a-z0-9-_.]`), or map surfaces to filenames via a safe encoder before calling `join()`.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-16T21:26:30Z on `tools/bus/subscribe.ts`:27 (association: MEMBER)

    <pre>
    Fixed in [c538a83](https://github.com/Lucent-Financial-Group/Zeta/pull/3775/commits/c538a8365feeb254b0eb5f39d0c7c20034df64ea) — added `SURFACE_RE = /^[a-z0-9_-]{1,64}$/` guard at the top of `subscribeOnce`. Invalid surface inputs throw before any filename interpolation. Defense-in-depth even though current callers pass hard-coded surface ids.
    </pre>

### Thread 8 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6Clt_v`
- **Path**: `tools/bus/subscribe.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-16T21:05:48Z on `tools/bus/subscribe.ts`:42 (association: NONE)

  <pre>
  P1: The broad `catch { seenIds = new Set(); }` treats *all* read/parse failures as 'no state yet', including permission errors or JSON corruption. That can silently drop seen-state and cause unexpected re-processing. Consider handling ENOENT as the 'no file' case, but logging/throwing on other I/O errors, and logging parse errors explicitly so state corruption is visible.

  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-16T21:26:32Z on `tools/bus/subscribe.ts`:42 (association: MEMBER)

    <pre>
    Fixed in [c538a83](https://github.com/Lucent-Financial-Group/Zeta/pull/3775/commits/c538a8365feeb254b0eb5f39d0c7c20034df64ea) — catch now branches on `ENOENT`: missing-file becomes `seenIds = new Set()`, other errors (permission, JSON corruption) re-throw so seen-state is never silently dropped.
    </pre>

### Thread 9 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6Clt_7`
- **Path**: `tools/bus/subscribe.test.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-16T21:05:48Z on `tools/bus/subscribe.test.ts`:7 (association: NONE)

  <pre>
  P0: Test suite imports `./bus` and `./types`, but those modules don't exist in `tools/bus/` (nor elsewhere in the repo), so the tests won't run. The PR slice needs to include the missing modules or update imports to the correct locations.

  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-16T21:23:40Z on `tools/bus/subscribe.test.ts`:7 (association: MEMBER)

    <pre>
    Verified `./bus` + `./types` exist on `main` and export the required symbols (`BUS_DIR`/`ensureDir`/`list` from `tools/bus/bus.ts`; `MessageEnvelope`/`Topic` from `tools/bus/types.ts`, both shipped via B-0400 slice 1). Imports resolve on the merge target — false positive per `.claude/rules/blocked-green-ci-investigate-threads.md` known-FP class ("imported file doesn't exist" when modules verifiably do exist on main).
    </pre>

### Thread 10 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6CluAF`
- **Path**: `tools/bus/subscribe.test.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-16T21:05:48Z on `tools/bus/subscribe.test.ts`:9 (association: NONE)

  <pre>
  P2: The describe string references "B-0449 slice 5", but the PR is for B-0459 and the PR title/description say it’s decomposed from B-0459. Update the test label to match to avoid confusion when grepping test output and PR history.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-16T21:26:33Z on `tools/bus/subscribe.test.ts`:9 (association: MEMBER)

    <pre>
    Fixed in [36b6a35](https://github.com/Lucent-Financial-Group/Zeta/pull/3775/commits/36b6a35b0b5f36176125fcc8ff7d948dd3010e87) — describe string is now "B-0459 slice 1" matching PR title/description.
    </pre>

### Thread 11 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6Cl3dh`
- **Path**: `tools/bus/subscribe.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-16T21:28:21Z on `tools/bus/subscribe.ts`:4 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Add missing local bus/type modules in this commit**

  These relative imports make the new helper uncompilable at this revision because `./bus` and `./types` are not present in the commit tree, so loading `subscribe.ts` fails before tests can run. Fresh evidence: inspecting this commit’s tree (`406b637:tools/bus`) shows only `subscribe.ts` and `subscribe.test.ts`, with no `bus.ts` or `types.ts` to resolve these imports.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 12 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6Cl3di`
- **Path**: `tools/bus/subscribe.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-16T21:28:21Z on `tools/bus/subscribe.ts`:65 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Propagate seen-file write failures from subscribeOnce**

  When persisting seen IDs fails (for example due to permissions or disk-full), the code only logs and returns success, so envelopes that were already handled will be re-delivered on the next poll and `subscribeOnce` loses its once-only behavior. This should surface an error to the caller (or otherwise fail the operation) instead of silently continuing after `writeFileSync` fails.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 13 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6Cl302`
- **Path**: `tools/bus/subscribe.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-16T21:29:11Z on `tools/bus/subscribe.ts`:5 (association: NONE)

  <pre>
  P0: This file imports `./bus` and `./types`, but those modules do not exist anywhere in the current tree (no `tools/bus/bus.ts` / `tools/bus/types.ts`, and no other definitions of `BUS_DIR`/`ensureDir`/`list`/`MessageEnvelope`/`Topic`). As-is, `subscribeOnce` will not typecheck/build. Add the missing bus/types modules in this PR (or update imports to the actual locations) so this slice is self-buildable.

  This issue also appears in the following locations of the same file:
  - line 29
  - line 61

  </pre>

### Thread 14 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6Cl31f`
- **Path**: `tools/bus/subscribe.test.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-16T21:29:11Z on `tools/bus/subscribe.test.ts`:7 (association: NONE)

  <pre>
  P0: This test imports `./bus` and `./types`, but those modules are not present in the current tree (no `tools/bus/bus.ts` / `tools/bus/types.ts`). The test suite won’t typecheck/run until the missing modules are added or the imports are corrected.

  </pre>

## Fix commits (touching thread paths)

### `aa8a424403df826a5b00d4b9ecaa7a02182f0567` -- 2026-05-16T06:17:00Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/bus/subscribe.test.ts`
- `tools/bus/subscribe.ts`

**Message:**

```
feat(bus): add subscribeOnce helper (decomposed from B-0459)
```

### `13c497bd0f8c7fbe5305692d4918bee00c490324` -- 2026-05-16T21:02:40Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/bus/subscribe.test.ts`

**Message:**

```
fix(bus): remove unused Topic import and rename unused mock param (#3…

…775)
```

### `5618091c2d57b6cecea03ff64e322f3ad083342d` -- 2026-05-16T21:03:13Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/bus/subscribe.ts`

**Message:**

```
fix(bus): remove existsSync to eliminate TOCTOU race (#3775)
```

### `c538a8365feeb254b0eb5f39d0c7c20034df64ea` -- 2026-05-16T21:26:01Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/bus/subscribe.ts`

**Message:**

```
fix(bus): subscribeOnce — surface allowlist + ENOENT-aware seen read

Addresses Copilot/Codex P1 threads on #3775:
- Path-traversal: surface interpolated into `seen-${surface}.json` is now
  guarded by SURFACE_RE = /^[a-z0-9_-]{1,64}$/. Throws on invalid input;
  defense-in-depth even though current callers pass hard-coded surface ids.
- Broad catch: distinguish ENOENT (no seen-file yet) from real errors.
  Re-throw permission / corruption errors so seen-state is never silently
  dropped.

Validated locally: `bun build` clean; `bun test tools/bus/subscribe.test.ts`
passes (2/2).
```

### `36b6a35b0b5f36176125fcc8ff7d948dd3010e87` -- 2026-05-16T21:26:11Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/bus/subscribe.test.ts`

**Message:**

```
chore(bus): correct describe label B-0449 → B-0459 in subscribe.test.ts

Addresses Copilot P2 thread on #3775. PR title + description say
"B-0459 slice 1" but the describe string still read "B-0449 slice 5".
```

### `5b490ba21397bfce0f4d2a5d13a34d7034003eb6` -- 2026-05-17T00:14:07Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/bus/subscribe.test.ts`
- `tools/bus/subscribe.ts`

**Message:**

```
fix(bus): propagate seen-file write failures from subscribeOnce

Addresses Codex P2 thread on #3775. The read-side already re-throws
non-ENOENT errors so we "never silently drop seen-state and re-deliver
already-handled envelopes" — the write-side was breaking the same
invariant in the opposite direction by logging-and-swallowing.

Now writeFileSync errors propagate to the caller. Adds a test that
pre-creates the seen file read-only so the read succeeds and the
write fails with EACCES; subscribeOnce rejects rather than silently
continuing.

Co-Authored-By: Claude <noreply@anthropic.com>
```
