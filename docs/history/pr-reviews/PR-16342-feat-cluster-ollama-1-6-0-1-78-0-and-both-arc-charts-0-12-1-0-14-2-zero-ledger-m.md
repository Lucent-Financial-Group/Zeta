# PR #16342 -- feat(cluster): ollama 1.6.0 -> 1.78.0 and both arc charts 0.12.1 -> 0.14.2 — zero ledger movement

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 16342 |
| Title | feat(cluster): ollama 1.6.0 -> 1.78.0 and both arc charts 0.12.1 -> 0.14.2 — zero ledger movement |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-09-02T17:00:26Z |
| Merged at | 2026-09-02T17:20:34Z |
| Merge commit SHA | `531d10f29333054da142b5e611cf330e73edad19` |
| Branch | `shadow/ollama-arc-charts-to-latest` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/16342 |
| Changed files | 6 |
| Additions / deletions | +94 / -25 |

## Description

## What

Three chart bumps, all to the newest published version:

| chart | from | to | what actually moves in the render |
|---|---|---|---|
| `ollama` | 1.6.0 | **1.78.0** | server image `ollama/ollama:0.5.11` → `0.33.0` |
| `arc-controller` | 0.12.1 | **0.14.2** | controller image tracks the chart |
| `arc-runner-set` | 0.12.1 | **0.14.2** | nothing — same runner image `2.330.0` |

Behind-upstream drops from **10 → 7**.

## Rendered and diffed before bumping

Each chart was templated at **both** pins against its own `valuesObject` at
`--kube-version 1.35.6`, then compared on the surfaces that can hurt — kinds,
images, PVCs, replicas, resource requests. `ollama` keeps its 200Gi longhorn PVC,
its 2 cpu / 8Gi / 1 GPU requests and `replicas: 0`; the arc pair are identical
but for the controller tag. **No storage row, resource row, or lane total moves**
— asserted by the suite, not by me: **1068 pass / 0 fail**.

## The schema snapshot is in the diff, and the tool is why

`inert-valuesobject-keys` refused the bumped tree:

> NOT COVERED by the snapshot — checked NOTHING here, which is not the same as passing

A new chart version has no schema on file, so it declined to certify rather than
go quietly green. After `--write-snapshot` it passes, and the inert-key set is
**byte-identical to main's 14** — meaning that across ollama's 72-version jump,
not one key we set was renamed away.

## One test fixture moved with the pin

`storage-profiles.test.ts` asserts `pinnedChartVersion("arc-runner-set")`. That
test proves the pin is read from the **parsed document** rather than line-scanned
near `chart:`, and `oz` — with ~35 comment lines between the keys — is the fixture
carrying that point. The arc-runner-set line is the ordinary case, so tracking the
real pin costs the test nothing.

## Verification

- `bun test src/Core.TypeScript/cluster/` → **1068 pass / 0 fail**
- `tsc --noEmit` → clean
- `inert-valuesobject-keys` → OK, no new inert keys
- `audit-chart-target-revisions` → 35/35 resolve
- `yamllint -d relaxed` over all three manifest roots → exit 0
- `docs/CHART-CURRENCY.md` regenerated

## Not in this PR

`headscale` is separately interesting and is **not** dormant-because-unsupported —
the chart moved home. Filed separately rather than bundled here.

Agency-Signature-Version: 1
Agent: shadow
Agent-Runtime: claude-code
Agent-Model: claude-opus-5
Credential-Identity: AceHack
Credential-Mode: shared
Human-Review: not-implied-by-credential
Human-Review-Evidence: none
Action-Mode: autonomous-fail-open
Task: none
Co-authored-by: shadow <shadow@zeta.agents>

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_01QyTyVdDqxekK5rZQGBm75f


## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 0 |
| Resolved threads | 0 |
| Unresolved threads | 0 |
| Total review comments | 0 |
| Total fix commits (touching thread paths) | 0 |

## Review threads

(no review threads on this PR)

## Fix commits (touching thread paths)

(no commits in this PR touched files anchored by review threads)
