# PR #6502 -- feat(DynamicValue): re-ground C# against the seed — canonical JSON encoder + byte-lock test

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 6502 |
| Title | feat(DynamicValue): re-ground C# against the seed — canonical JSON encoder + byte-lock test |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-06-01T23:46:22Z |
| Merged at | 2026-06-02T00:14:54Z |
| Merge commit SHA | `62a9f2cf7cabc7d6f7218f01317c8ae210ab5622` |
| Branch | `otto-cli/dynamicvalue-csharp-reground-seed-2026-06-01` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/6502 |
| Changed files | 5 |
| Additions / deletions | +322 / -4 |

## Description

**Seed-first** (Aaron 2026-06-01). The C# oracle now AGREES with the shared seed (`src/Core.TypeScript/dynamic-value/golden-vectors.json`). **Three oracles now independently agree on the seed**: TS (32 green, #6499) + F# (#6500) + C# (here) — none ported from another; all grown to agree on the canonical *data*.

## What's here

- **`DynamicValues.ToCanonicalJson`** (+ `WriteCanonical` / `AppendEscaped`) — the canonical-encode side of the byte-lock. Minified; **Object keys in INSERTION order** (NOT sorted — Object is order-significant; key-sorting would be lossy/non-bijective); Int = bare exact decimal (invariant); String = RFC 8259 minimal escaping (`/` not escaped; control U+0000..001F short-form or `\u00XX` lowercase; raw UTF-8 else). **Float + Bytes throw** `InvalidOperationException` (DEFERRED — lock under CBOR or tagged-JSON).
- **`DynamicValueCrossVerifyTests.cs`** — loads the 31 seed vectors, builds DynamicValue from the tagged form, asserts `ToCanonicalJson(value) == json`. Green (Release, 0 warnings, CA/MA/CodeQL).
- **Fixes both `DynamicValue.cs` doc comments** that said the wire encoder "sorts keys when byte-locking" — corrected to order-PRESERVING (the contradiction the seed surfaced; F# side fixed in #6500).

Independent of #6500 (disjoint files). Next: **Rust** oracle; then the **decode** side (precision-safe int64 tokenizer); then Float/Bytes via CBOR (or tagged-JSON) if called.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | true |
| Total threads | 8 |
| Resolved threads | 8 |
| Unresolved threads | 0 |
| Total review comments | 9 |
| Total fix commits (touching thread paths) | 5 |

## Review threads

### Thread 1 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GRtHL`
- **Path**: `src/Core.CSharp.DynamicValue/DynamicValues.cs`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-06-01T23:48:25Z on `src/Core.CSharp.DynamicValue/DynamicValues.cs`:253 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Return a typed failure for deferred cases**

  When a caller serializes a valid `DynamicValue` tree containing `Float` or `Bytes`, this public encoder throws `InvalidOperationException` across the boundary. The repo-wide contract in `AGENTS.md` requires user-visible failures to flow as `Result&lt;_ , DbspError&gt;`/AppendResult-style values rather than exceptions, and generic callers cannot safely use `ToCanonicalJson` without pre-scanning the tree for these legal shapes; make the deferred cases a typed `Err`/`Try` result (or keep them out of the public encoder) instead of throwing.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 2 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GRtmU`
- **Path**: `tests/Tests.CSharp/DynamicValueCrossVerifyTests.cs`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-01T23:49:13Z on `tests/Tests.CSharp/DynamicValueCrossVerifyTests.cs`:17 (association: NONE)

  <pre>
  P1: This test XML doc comment introduces named attribution ("Aaron 2026-06-01"), but current-state surfaces (including test code) should use role-refs rather than personal/persona names (see .github/copilot-instructions.md:317-377). Please drop the name and keep the statement as an attribution-free note about the seed-first approach.
  </pre>

### Thread 3 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GRtmx`
- **Path**: `tests/Tests.CSharp/DynamicValueCrossVerifyTests.cs`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-01T23:49:13Z on `tests/Tests.CSharp/DynamicValueCrossVerifyTests.cs`:70 (association: NONE)

  <pre>
  P1: In the "obj" case, the fixture pair is assumed to have exactly two elements. If the seed is malformed, this will currently throw IndexOutOfRangeException and make debugging harder. Validate the pair shape and throw a clear InvalidOperationException with the raw JSON.
  </pre>

### Thread 4 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GRtm_`
- **Path**: `src/Core.CSharp.DynamicValue/DynamicValues.cs`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-01T23:49:14Z on `src/Core.CSharp.DynamicValue/DynamicValues.cs`:200 (association: NONE)

  <pre>
  P1: ToCanonicalJson(null) currently falls through to the default switch case and then dereferences `value.Type`, producing a NullReferenceException. Since this is a public API, it should fail fast with an ArgumentNullException instead of an NRE.
  </pre>

### Thread 5 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GRzo8`
- **Path**: `src/Core.CSharp.DynamicValue/DynamicValues.cs`
- **Replies**: 0

**Initial comment:**

- **`github-code-quality[bot]` (bot)** at 2026-06-02T00:01:01Z on `src/Core.CSharp.DynamicValue/DynamicValues.cs`:237 (association: NONE)

  <pre>
  ## Missed opportunity to use Where

  &lt;p&gt;This foreach loop &lt;a class="Link" href="/Lucent-Financial-Group/Zeta/blob/519ca272aa12e0dac1f03e7a79dbe67e7063f65d/src/Core.CSharp.DynamicValue/DynamicValues.cs#L233-L233"&gt;implicitly filters its target sequence&lt;/a&gt; - consider filtering the sequence explicitly using '.Where(...)'.&lt;/p&gt;
  </pre>

### Thread 6 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GRzo_`
- **Path**: `src/Core.CSharp.DynamicValue/DynamicValues.cs`
- **Replies**: 0

**Initial comment:**

- **`github-code-quality[bot]` (bot)** at 2026-06-02T00:01:01Z on `src/Core.CSharp.DynamicValue/DynamicValues.cs`:247 (association: NONE)

  <pre>
  ## Missed opportunity to use Where

  &lt;p&gt;This foreach loop &lt;a class="Link" href="/Lucent-Financial-Group/Zeta/blob/519ca272aa12e0dac1f03e7a79dbe67e7063f65d/src/Core.CSharp.DynamicValue/DynamicValues.cs#L243-L243"&gt;implicitly filters its target sequence&lt;/a&gt; - consider filtering the sequence explicitly using '.Where(...)'.&lt;/p&gt;
  </pre>

### Thread 7 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GR1la`
- **Path**: `src/Core.CSharp.DynamicValue/DynamicValues.cs`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-02T00:04:59Z on `src/Core.CSharp.DynamicValue/DynamicValues.cs`:195 (association: NONE)

  <pre>
  P0: The XML docs on `ToCanonicalJson` are duplicated (two `&lt;summary&gt;` blocks plus repeated `&lt;param&gt;`/`&lt;returns&gt;`), and the first block also claims Float/Bytes throw `InvalidOperationException`, which contradicts the method’s actual `Result&lt;_, EncodeError&gt;` behavior. This yields malformed/misleading API docs and can break doc generation if enabled; consolidate to a single accurate doc block.
  </pre>

### Thread 8 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GR4yG`
- **Path**: `src/Core.CSharp.DynamicValue/DynamicValues.cs`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-06-02T00:11:38Z on `src/Core.CSharp.DynamicValue/DynamicValues.cs`:270 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Reject duplicate object keys before emitting JSON**

  When a `DynamicValue.Object` contains duplicate keys, this emits a JSON object with repeated member names (for example two `"a"` pairs). The value model is an ordered pair list and existing lookup semantics keep the first match, but most JSON decoders expose objects as maps/last-wins properties, so this canonical form cannot round-trip that legal `DynamicValue` without losing a pair; return a typed encode error for duplicate keys or use a tagged/pair-list representation for those cases.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-06-02T00:14:51Z on `src/Core.CSharp.DynamicValue/DynamicValues.cs`:270 (association: MEMBER)

    <pre>
    Real round-trip concern. The canonical model is an ordered pair-list (order-significant) and this PR byte-locks the current form, which emits duplicate names as-is. Changing that -- reject-with-typed-error vs tagged-form vs accept-lossy -- is a canonical-form decision across all four oracles plus a seed change, the same authority class as the deferred Float/Bytes/CBOR call (maintainer-owned, not autonomous). v1 seed has no duplicate-key vectors, so the byte-lock is unaffected. Resolving as deferred-by-design; it rides with the Float/Bytes/CBOR canonical-form decision.
    </pre>

## Fix commits (touching thread paths)

### `6c6b6ac722f480cbac6291c7e1da589d4e99de5a` -- 2026-06-01T23:46:12Z -- ``

**Touched files (intersect with thread paths):**

- `src/Core.CSharp.DynamicValue/DynamicValues.cs`
- `tests/Tests.CSharp/DynamicValueCrossVerifyTests.cs`

**Message:**

```
feat(DynamicValue): re-ground C# against the seed — canonical JSON en…

…coder + byte-lock test

Seed-first (Aaron 2026-06-01): the C# oracle now AGREES with the shared
seed (src/Core.TypeScript/dynamic-value/golden-vectors.json). Three oracles
now independently agree on the seed: TS (32 green) + F# (#6500) + C# (here).

- DynamicValues.ToCanonicalJson (+ WriteCanonical / AppendEscaped): minified;
  Object keys in INSERTION order (NOT sorted — Object is order-significant;
  key-sorting would be lossy/non-bijective); Int = bare exact decimal
  (invariant); String = RFC 8259 minimal escaping ('/' not escaped, control
  U+0000..001F short-form or \u00XX lowercase, raw UTF-8 else). Float + Bytes
  throw InvalidOperationException (DEFERRED — lock under CBOR or tagged-JSON).
- DynamicValueCrossVerifyTests.cs: loads the 31 seed vectors, builds the
  value from the tagged form, asserts ToCanonicalJson(value) == json. Green.
- Fixes both DynamicValue.cs doc comments that said the wire encoder 'sorts
  keys when byte-locking' — corrected to order-PRESERVING.

Build clean (Release, 0 warnings, TreatWarningsAsErrors + CA/MA/CodeQL).
Next: Rust oracle; then the decode side (precision-safe int64 tokenizer).

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `519ca272aa12e0dac1f03e7a79dbe67e7063f65d` -- 2026-06-01T23:58:45Z -- ``

**Touched files (intersect with thread paths):**

- `src/Core.CSharp.DynamicValue/DynamicValues.cs`
- `tests/Tests.CSharp/DynamicValueCrossVerifyTests.cs`

**Message:**

```
fix(#6502): address review threads — Result-shaped encoder, null guar…

…d, lone-surrogate escape, obj-pair guard, role-refs

Per AGENTS.md hard rule (user-visible errors are Result, not exceptions),
mirroring the F# reground (#6500):
- ToCanonicalJson now returns Result<string, EncodeError> (Ok/Err records;
  FloatDeferred | BytesDeferred). A FirstDeferred pre-check surfaces deferred
  variants as Err DATA, never thrown (Codex). Adds Result.cs (per-crate port,
  BCL-interface-boundary) + EncodeError.cs.
- ArgumentNullException.ThrowIfNull guard: ToCanonicalJson(null) is a clear
  ArgumentNullException, not a default-case NullReferenceException (Copilot).
- AppendEscaped now lone-surrogate-safe: valid pairs emit the astral char raw;
  lone surrogates are u-escaped (raw would be invalid Unicode / non-bijective).
- test: obj-pair length guard (no IndexOutOfRange on a malformed fixture pair,
  Copilot); role-refs not a personal name (Copilot); Result-aware assertions.

Build clean (Release, 0 warnings, CA/MA/CodeQL); byte-lock test green (Ok json
for all 31). Three .NET oracles now Result-shaped + agree on the seed.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `990362e1ca4a35ec6cdcfb99ac0ff4da4e3fe84a` -- 2026-06-02T00:02:49Z -- ``

**Touched files (intersect with thread paths):**

- `src/Core.CSharp.DynamicValue/DynamicValues.cs`

**Message:**

```
fix(#6502): null-safe AppendEscaped — no NRE on a malformed null Stri…

…ng payload / object key

Pre-empts the same Codex P2 caught on the F# side (#6500): a DynamicValue.String
null / null object key (reachable via nullable-disabled / interop callers) made
the Result-advertising encoder throw an NRE. Normalize null -> empty (param is now
string?; mirrors the records default-array normalization) so the encoder stays
exception-free. Keeps F# + C# oracles consistent.

Build clean (Release, 0 warnings); byte-lock test green.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `ea082b258502e256d7ba6ae9fa1946e726eec04f` -- 2026-06-02T00:04:08Z -- ``

**Touched files (intersect with thread paths):**

- `src/Core.CSharp.DynamicValue/DynamicValues.cs`

**Message:**

```
fix(#6502): FirstDeferred uses LINQ Select+FirstOrDefault (CodeQL mis…

…sed-Where)

CodeQL code-quality flagged the two early-return foreach loops as implicit
filters. Express the find-first-deferred as Select(FirstDeferred).FirstOrDefault(
e => e is not null) — Select is lazy + FirstOrDefault short-circuits, so the same
first-match semantics, no implicit-filter foreach.

Build clean (Release, 0 warnings); byte-lock test green.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `7e0ac88d31f3a61fdc33468a97196dd1388cb494` -- 2026-06-02T00:09:10Z -- ``

**Touched files (intersect with thread paths):**

- `src/Core.CSharp.DynamicValue/DynamicValues.cs`

**Message:**

```
fix(#6502): remove duplicate/stale XML doc on ToCanonicalJson

Copilot P0: the Result rewrite left the OLD doc block in place (its old_string
started at the signature, not the doc comment), so ToCanonicalJson had two
<summary> blocks + the stale one still claimed it throws InvalidOperationException
(contradicting the Result behavior). Delete the stale block; keep the accurate
Result one.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```
