# PR #2886 -- feat(B-0400): inter-agent bus protocol — typed /tmp transport, slice 1

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 2886 |
| Title | feat(B-0400): inter-agent bus protocol — typed /tmp transport, slice 1 |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-05-13T00:39:54Z |
| Merged at | 2026-05-13T00:58:19Z |
| Merge commit SHA | `1a76429d435597d1fb40fccbe7c773c086692e68` |
| Branch | `feat/b-0400-bus-protocol-slice1` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/2886 |
| Changed files | 4 |
| Additions / deletions | +543 / -0 |

## Description

## Summary

- **`tools/bus/types.ts`** — discriminated-union message schema; four topics (`heartbeat`, `claim`, `shadow-catch`, `review-request`); per-topic TTL defaults (5 min / 24 h / 1 h / 4 h)
- **`tools/bus/bus.ts`** — `publish / list / read / clean` CLI; topic + recipient filtering; TTL-based expiry; `ZETA_BUS_DIR` override for test isolation
- **`tools/bus/bus.test.ts`** — 11 tests covering publish, list-by-topic, list-by-recipient (broadcast semantics), read, clean, TTL expiry, error paths
- **B-0400 pre-start checklist** updated in backlog item

## Design notes (agent-designed per acceptance criteria)

Transport chosen: `/tmp/zeta-bus/` JSON files (same pattern as `shadow-outlet`). No new runtime dependencies. NATS/JetStream is a future transport swap behind the same interface — slice 2+.

Key distinction from `shadow-outlet`: structured topics + recipient routing + TTL expiry. Shadow-outlet is for unstructured agent scratch; bus is for coordinated inter-agent messaging.

## Test outcome

```
bun test tools/bus/bus.test.ts
 11 pass
 0 fail
 35 expect() calls
Ran 11 tests across 1 file. [754.00ms]
```

## Deferred (slice 2+)

- NATS JetStream transport swap
- Named-pipe transport option
- Subscription watch mode (`bus.ts watch --agent otto`)
- Integration with `poll-pr-gate-batch.ts` for coordinated claims

## Acceptance criteria check

- [x] Protocol designed by agents (not Aaron) — Otto designed; PR is multi-agent review surface
- [x] At least 2 agents can exchange messages via the bus — test suite demonstrates otto↔vera exchange
- [x] Messages survive between ticks but not necessarily reboots — `/tmp` survives ticks, cleared on reboot
- [ ] Multi-agent review of this design — requesting review via this PR

Closes / partially implements B-0400 (slice 1).

operative-authorization: aaron 2026-05-12: "- The "go hard" + dense-encoding-mode authorizations —"

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | true |
| Total threads | 10 |
| Resolved threads | 10 |
| Unresolved threads | 0 |
| Total review comments | 18 |
| Total fix commits (touching thread paths) | 3 |

## Review threads

### Thread 1 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6BmXUm`
- **Path**: `tools/bus/bus.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-13T00:42:09Z on `tools/bus/bus.ts`:30 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Sanitize message IDs before building read path**

  `read` accepts untrusted `&lt;id&gt;` and `envelopePath` concatenates it directly with `join`, so IDs like `../escape` escape `ZETA_BUS_DIR` and read arbitrary `*.json` files outside the bus directory. This breaks bus isolation and can leak unrelated JSON data whenever a caller passes a crafted ID (for example from user input or another tool). Restrict IDs to UUID format (or normalize and enforce the resolved path stays under `BUS_DIR`) before reading.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 2 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6BmXUo`
- **Path**: `tools/bus/bus.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-13T00:42:09Z on `tools/bus/bus.ts`:46 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Validate topic values before computing TTL**

  The CLI casts `--topic` to `Topic` without runtime validation, so an unknown topic reaches `publish`, where `TTL_MS[message.topic]` becomes `undefined` and `toISOString()` throws `RangeError: Invalid Date`. This turns a normal input error into an uncaught stack trace, which is brittle for automation that expects structured error handling. Validate `--topic` against the known topic set and emit a user-facing error before calling `publish`.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 3 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6BmYeG`
- **Path**: `tools/bus/bus.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-13T00:44:33Z on `tools/bus/bus.ts`:84 (association: NONE)

  <pre>
  `list()` only checks `id` and `topic` before pushing parsed entries, but then sorts by `a.timestamp.localeCompare(...)`. If a JSON file is missing `timestamp` (or it’s non-string), `list()` will throw at sort time and break the whole command. Validate required fields (`timestamp`, `expiresAt`, `from`, `to`, and payload/topic shape) before pushing, or sort defensively when fields are absent.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-13T00:56:43Z on `tools/bus/bus.ts`:84 (association: MEMBER)

    <pre>
    Already fixed in 582b910d2c: `list()` validates all six required fields (`id`, `topic`, `timestamp`, `expiresAt`, `from`, `to`) as strings before pushing to results, so the sort on `timestamp.localeCompare` is safe — any entry with a non-string timestamp is silently skipped.
    </pre>

### Thread 4 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6BmYeT`
- **Path**: `tools/bus/bus.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-13T00:44:33Z on `tools/bus/bus.ts`:172 (association: NONE)

  <pre>
  CLI `publish` casts `--topic` to `Topic` without runtime validation. If the user passes an unknown topic, `TTL_MS[message.topic]` becomes `undefined` and `new Date(now.getTime() + ttl).toISOString()` will throw (RangeError). Add runtime validation for `--topic` (and ideally `--from/--to`) and emit a clean error instead of crashing.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-13T00:56:45Z on `tools/bus/bus.ts`:172 (association: MEMBER)

    <pre>
    Already fixed in 582b910d2c: CLI `publish` validates the topic with `if (!(topic in TTL_MS))` and exits with an error listing valid topics before calling `publish()`.
    </pre>

### Thread 5 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6BmYek`
- **Path**: `tools/bus/types.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-13T00:44:33Z on `tools/bus/types.ts`:18 (association: NONE)

  <pre>
  `AgentId` includes "*" (broadcast), and `MessageEnvelope.from` is typed as `AgentId`, which allows a broadcast sender. That makes the envelope schema internally inconsistent (broadcast is a routing target, not an origin). Consider narrowing `from` to exclude "*" (and keep "*" only for `to`).
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-13T00:56:51Z on `tools/bus/types.ts`:18 (association: MEMBER)

    <pre>
    Already fixed in 582b910d2c: `MessageEnvelope.from` is typed as `SenderAgentId` (defined as `Exclude&lt;AgentId, "*"&gt;`), so `*` is not a valid sender at the type level. The Copilot suggestion is now correctly reflected in the schema.
    </pre>

### Thread 6 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6BmYeu`
- **Path**: `tools/bus/types.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-13T00:44:34Z on `tools/bus/types.ts`:10 (association: NONE)

  <pre>
  Code comments include named attribution ("agent-designed, Otto 2026-05-13"). Per repo convention, reusable code/docs should use role references rather than persona names outside the allowed history surfaces. Reword this comment to remove the persona name while keeping the provenance info.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-13T00:56:35Z on `tools/bus/types.ts`:10 (association: MEMBER)

    <pre>
    Fixed in d03b16d1: removed persona attribution from the comment. Now reads `// Topic taxonomy (agent-designed, 2026-05-13):` — date-only provenance, no persona name.
    </pre>

### Thread 7 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6BmYez`
- **Path**: `docs/backlog/P1/B-0400-inter-agent-ephemeral-communication-bus-nats-protocol.md`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-13T00:44:34Z on `docs/backlog/P1/B-0400-inter-agent-ephemeral-communication-bus-nats-protocol.md`:61 (association: NONE)

  <pre>
  Backlog per-row files require a `last_updated` frontmatter field that’s updated on every content edit. This file’s frontmatter currently lacks `last_updated`, so this content update should add it (and set it to 2026-05-13).
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-13T00:56:37Z on `docs/backlog/P1/B-0400-inter-agent-ephemeral-communication-bus-nats-protocol.md`:61 (association: MEMBER)

    <pre>
    Fixed in d03b16d1: added `last_updated: 2026-05-13` to the B-0400 frontmatter.
    </pre>

### Thread 8 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6BmYe4`
- **Path**: `docs/backlog/P1/B-0400-inter-agent-ephemeral-communication-bus-nats-protocol.md`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-13T00:44:34Z on `docs/backlog/P1/B-0400-inter-agent-ephemeral-communication-bus-nats-protocol.md`:62 (association: NONE)

  <pre>
  The checklist references `tools/shadow/ephemeral.ts`, but that path doesn’t exist in the repo (the existing utility is `tools/shadow-outlet/ephemeral.ts`). Also, the current bus implementation doesn’t import/reuse an ephemeral utility for TTL cleanup. Update this checklist entry to point at the correct prior art and accurately reflect what’s reused.

  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-13T00:56:53Z on `docs/backlog/P1/B-0400-inter-agent-ephemeral-communication-bus-nats-protocol.md`:62 (association: MEMBER)

    <pre>
    Already correct in 582b910d2c: the checklist references `tools/shadow-outlet/ephemeral.ts` (the correct path). The earlier version of the file had the wrong path; the second commit corrected it.
    </pre>

### Thread 9 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6BmcBB`
- **Path**: `tools/bus/bus.ts`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-13T00:51:15Z on `tools/bus/bus.ts`:160 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Validate sender and recipient IDs before publish**

  The CLI currently casts `--from`/`--to` directly to `AgentId` without runtime checks, so typos like `--to veraa` are accepted and persisted as envelopes that no real agent will ever consume. In this workflow that creates silent message loss (publish reports success, but `list --to vera` will never surface the message), which makes coordination failures hard to debug. Please reject unknown agent IDs at the CLI boundary the same way unknown topics are rejected.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-13T00:56:30Z on `tools/bus/bus.ts`:160 (association: MEMBER)

    <pre>
    Fixed in d03b16d1: added `SENDER_IDS` and `AGENT_IDS` constant arrays; CLI `publish` now validates `--from` against `SENDER_IDS` and `--to` against `AGENT_IDS` before calling `publish()`, with a clear error message listing valid values.
    </pre>

### Thread 10 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6BmcBC`
- **Path**: `tools/bus/bus.ts`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-13T00:51:15Z on `tools/bus/bus.ts`:85 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Catch invalid read IDs instead of throwing**

  `readMessage` calls `envelopePath(id)` before entering its `try` block, and `envelopePath` throws on traversal-style IDs; this bypasses the CLI’s normal error path and produces an uncaught exception/stack trace for malformed input. That turns a user input error into a hard crash path. Wrap path construction in the same error handling path (or make `envelopePath` return `null` for invalid IDs) so `read` consistently returns a controlled “not found/invalid id” error.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-13T00:56:27Z on `tools/bus/bus.ts`:85 (association: MEMBER)

    <pre>
    Fixed in d03b16d1: `envelopePath(id)` is now called inside the `try` block in `readMessage`, so traversal-style invalid IDs return `null` via the existing catch rather than surfacing as an unhandled throw.
    </pre>

## Fix commits (touching thread paths)

### `73ee291ed7462d6084a97b2bd45a3a2f96c32ca0` -- 2026-05-13T00:39:36Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `docs/backlog/P1/B-0400-inter-agent-ephemeral-communication-bus-nats-protocol.md`
- `tools/bus/bus.ts`
- `tools/bus/types.ts`

**Message:**

```
feat(B-0400): inter-agent bus protocol — typed /tmp transport, slice 1

Implements the smallest safe slice of B-0400: a typed, topic-routed
ephemeral message bus using /tmp/zeta-bus/ JSON files (no new runtime
deps; NATS is a future transport swap).

## What lands

- tools/bus/types.ts — discriminated union message schema; four topics
  (heartbeat, claim, shadow-catch, review-request); per-topic TTL defaults
- tools/bus/bus.ts  — publish/list/read/clean CLI (same pattern as
  shadow-outlet/outlet.ts); ZETA_BUS_DIR override for tests
- tools/bus/bus.test.ts — 11 tests (publish, list filtering by topic/to,
  read, clean, TTL expiry, error handling); all pass

## Tests

bun test tools/bus/bus.test.ts → 11 pass / 0 fail

## Deferred (slice 2+)

NATS JetStream transport swap, named-pipe option, watch mode,
poll-pr-gate integration.

operative-authorization: aaron 2026-05-12: "- The "go hard" + dense-encoding-mode authorizations —"

Co-Authored-By: Claude <noreply@anthropic.com>
```

### `582b910d2c4c50b4c720b4050b4ad0f11ae3b941` -- 2026-05-13T00:47:39Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `docs/backlog/P1/B-0400-inter-agent-ephemeral-communication-bus-nats-protocol.md`
- `tools/bus/bus.ts`

**Message:**

```
fix(B-0400): markdownlint MD032 + bus.ts path-traversal + TS exactOpt…

…ionalPropertyTypes

- docs: add blank lines after bold headers before list blocks (MD032)
- bus.ts: validate envelopePath stays within BUS_DIR (prevents ../escape)
- bus.ts: validate --topic against TTL_MS keys before publish (prevents RangeError)
- bus.ts: use conditional spread for optional params (exactOptionalPropertyTypes TS2379)

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>
```

### `d03b16d1cbda4dcd8176bfe7c7eb3de84d20244f` -- 2026-05-13T00:55:42Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `docs/backlog/P1/B-0400-inter-agent-ephemeral-communication-bus-nats-protocol.md`
- `tools/bus/bus.ts`
- `tools/bus/types.ts`

**Message:**

```
fix(B-0400): address reviewer feedback — path-safety, validation, fro…

…ntmatter

- Move envelopePath(id) inside readMessage try-block so path-traversal
  InvalidID errors return null rather than surfacing as unhandled throws
- Add --from / --to runtime validation in CLI publish against known agent
  IDs (SENDER_IDS / AGENT_IDS) before persisting; clear error messages
- Remove persona attribution from types.ts comment (role-ref only)
- Add last_updated: 2026-05-13 to B-0400 frontmatter

All 11 bus tests pass.

Co-Authored-By: Claude <noreply@anthropic.com>
```
