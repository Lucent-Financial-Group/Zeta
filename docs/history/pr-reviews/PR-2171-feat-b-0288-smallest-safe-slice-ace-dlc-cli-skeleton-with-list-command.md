# PR #2171 -- feat(B-0288): smallest safe slice — Ace DLC CLI skeleton with list command

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 2171 |
| Title | feat(B-0288): smallest safe slice — Ace DLC CLI skeleton with list command |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-05-09T03:02:47Z |
| Merged at | 2026-05-09T03:17:51Z |
| Merge commit SHA | `f10a113b55c9c01e9ca301574d1a1ff6f7b0a914` |
| Branch | `claim/b0288-ace-dlc-cli-smallest-slice-2026-05-08` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/2171 |
| Changed files | 4 |
| Additions / deletions | +404 / -3 |

## Description

## Summary

- Implements the Ace DLC package manager CLI skeleton at `tools/ace/`
- Only the `list` command is functional (smallest safe slice); `install`, `verify`, `remove`, `inspect` return clear "not yet implemented" errors
- Content-addressed local store reader (`store.ts`) reads `manifest.json` from hash-named directories, validates required fields, sorts by name
- Follows repo TS CLI conventions: `parseArgs` → typed result, `main` returns exit code, `import.meta.main` entrypoint

## What's in scope

| File | Purpose |
|---|---|
| `tools/ace/ace.ts` | CLI entrypoint — arg parsing, help text, `list` dispatch |
| `tools/ace/store.ts` | Local store abstraction — `listInstalled()`, `defaultStorePath()`, `AceManifest` type |
| `tools/ace/ace.test.ts` | 20 tests covering arg parsing, store reading, manifest validation, sort order, main exit codes |

## What's NOT in scope (future slices)

- `install` — download + signature verification + extraction
- `verify` — Ed25519 signature + content hash check
- `remove` / `inspect` — store mutation and manifest display
- Guardian AI integration (KSK pattern)

## Test plan

- [x] `bun test tools/ace/ace.test.ts` — 20 pass, 0 fail, 33 expect() calls
- [x] `dotnet build -c Release` — 0 warnings, 0 errors
- [x] Smoke test: `bun tools/ace/ace.ts help`, `list --store /tmp/nonexistent`, `install foo` (exit 64)

## Backlog

- B-0288 status updated: `open` → `in-progress`, `blocked-on-B-0287` → `buildable-now` (B-0287 spec already closed)

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | true |
| Total threads | 8 |
| Resolved threads | 8 |
| Unresolved threads | 0 |
| Total review comments | 16 |
| Total fix commits (touching thread paths) | 4 |

## Review threads

### Thread 1 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6AyBPL`
- **Path**: `tools/ace/store.ts`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-09T03:04:12Z on `tools/ace/store.ts`:27 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Guard store reads against non-directory paths**

  When `--store` points to an existing file (or a path the process cannot read), `readdirSync(storePath, { withFileTypes: true })` throws before any per-entry `try/catch`, so `ace list` terminates with an uncaught exception instead of returning a controlled CLI error/exit code. This is user-triggerable via argument input and breaks the command’s error-handling contract for invalid store paths.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-09T03:10:02Z on `tools/ace/store.ts`:27 (association: MEMBER)

    <pre>
    Fixed in cc0c3ef9: added `statSync().isDirectory()` guard before `readdirSync`, with a test for non-directory store paths.
    </pre>

### Thread 2 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6AyBPM`
- **Path**: `tools/ace/store.ts`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-09T03:04:12Z on `tools/ace/store.ts`:56 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Require manifest version during validation**

  `AceManifest.version` is treated as required (and is printed in `ace.ts`), but the validator only checks `format_version`, `name`, and `content_hash`. A manifest missing `version` is therefore accepted and later rendered as `name@undefined`, which corrupts list output and JSON shape guarantees for installed packages.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-09T03:10:04Z on `tools/ace/store.ts`:56 (association: MEMBER)

    <pre>
    Fixed in 484c4a59: added `typeof manifest.version !== "string"` to the validation check, with a dedicated test for missing version field.
    </pre>

### Thread 3 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6AyB2p`
- **Path**: `tools/ace/store.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-09T03:06:39Z on `tools/ace/store.ts`:41 (association: NONE)

  <pre>
  `listInstalled()` only checks `existsSync(storePath)` before calling `readdirSync`. If `storePath` exists but is a file (or otherwise not a directory), this will throw (e.g., ENOTDIR) and crash the CLI. Consider guarding with `statSync(...).isDirectory()` (or wrapping `readdirSync` in a try/catch) and returning an empty list or a structured error.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-09T03:10:28Z on `tools/ace/store.ts`:41 (association: MEMBER)

    <pre>
    Fixed in 484c4a59: added `statSync().isDirectory()` guard with try/catch fallback before `readdirSync`. Returns empty array for non-directory paths. Test added.
    </pre>

### Thread 4 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6AyB2w`
- **Path**: `tools/ace/store.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-09T03:06:39Z on `tools/ace/store.ts`:58 (association: NONE)

  <pre>
  Manifest validation currently omits `version` even though `AceManifest` requires it. This can admit manifests where `manifest.version` is `undefined`, leading to output like `name@undefined` (and JSON output missing a required field). Add a `typeof manifest.version === "string"` check (and ideally reject empty strings) to match the type/intent.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-09T03:10:31Z on `tools/ace/store.ts`:58 (association: MEMBER)

    <pre>
    Fixed in 484c4a59: added `typeof manifest.version !== "string"` to the validation guard. Test added for manifests missing version.
    </pre>

### Thread 5 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6AyB20`
- **Path**: `tools/ace/ace.test.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-09T03:06:39Z on `tools/ace/ace.test.ts`:193 (association: NONE)

  <pre>
  This test name claims the JSON output is valid, but it only asserts the exit code and doesn't validate stdout is parseable JSON (or that it matches `[]` for an empty store). Either capture/parse stdout for `--json`, or rename the test to reflect what it actually asserts.

  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-09T03:10:35Z on `tools/ace/ace.test.ts`:193 (association: MEMBER)

    <pre>
    Fixed in cc0c3ef9: renamed test to "list --json on empty store returns 0" to match actual assertion (exit code only).
    </pre>

### Thread 6 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6AyB28`
- **Path**: `docs/backlog/P1/B-0288-ace-dlc-package-manager-cli-2026-05-08.md`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-09T03:06:39Z on `docs/backlog/P1/B-0288-ace-dlc-package-manager-cli-2026-05-08.md`:7 (association: NONE)

  <pre>
  Per the per-row backlog schema, `last_updated` must be updated on every content edit. This file changes `status`/`classification` but leaves `last_updated` at 2026-05-08; bump it to the date of this change.

  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-09T03:10:51Z on `docs/backlog/P1/B-0288-ace-dlc-package-manager-cli-2026-05-08.md`:7 (association: MEMBER)

    <pre>
    Fixed in cc0c3ef9: bumped `last_updated` to 2026-05-09.
    </pre>

### Thread 7 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6AyDK2`
- **Path**: `tools/ace/store.ts`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-09T03:12:26Z on `tools/ace/store.ts`:35 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Handle unreadable store directories in listInstalled**

  `listInstalled` can still throw for permission-restricted store paths: a directory may pass `statSync(...).isDirectory()` but fail `readdirSync` with `EACCES`/`EPERM`, which bubbles out of `main` and crashes `ace list` instead of returning a controlled CLI error/empty result. Fresh evidence: this revision added a `statSync` guard for non-directory paths, but the `readdirSync` call remains outside any error handling.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-09T03:17:40Z on `tools/ace/store.ts`:35 (association: MEMBER)

    <pre>
    Fixed in 35fab7f8: wrapped `readdirSync` in try/catch so EACCES/EPERM returns `[]` instead of crashing. Test added with chmod(0o000) + cleanup.
    </pre>

### Thread 8 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6AyDUW`
- **Path**: `tools/ace/store.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-09T03:13:12Z on `tools/ace/store.ts`:35 (association: NONE)

  <pre>
  P0: `readdirSync(storePath, ...)` is unguarded. If the store dir exists but is unreadable (e.g., EACCES/EPERM) or is concurrently removed, this will throw and crash the CLI. Wrap the `readdirSync` call in a try/catch (similar to the `statSync` guard) and return `[]` (or plumb a structured error) on failure.

  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-09T03:17:43Z on `tools/ace/store.ts`:35 (association: MEMBER)

    <pre>
    Fixed in 35fab7f8: wrapped `readdirSync` in try/catch so EACCES/EPERM returns `[]` instead of crashing. Test added with chmod(0o000) + cleanup.
    </pre>

## Fix commits (touching thread paths)

### `a0f0411a1464e44bebdf1190b82a8c52e622a97e` -- 2026-05-09T03:02:22Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `docs/backlog/P1/B-0288-ace-dlc-package-manager-cli-2026-05-08.md`
- `tools/ace/ace.test.ts`
- `tools/ace/store.ts`

**Message:**

```
feat(B-0288): smallest safe slice — Ace DLC CLI skeleton with list co…

…mmand

Implements the CLI entrypoint and local store reader for the Ace DLC
package manager. This is the smallest safe slice: only the `list`
command is implemented; install/verify/remove/inspect return a clear
"not yet implemented" message.

- tools/ace/ace.ts: CLI dispatcher with arg parsing, help, and list
- tools/ace/store.ts: content-addressed local store reader (listInstalled)
- tools/ace/ace.test.ts: 20 tests covering arg parsing, store reading,
  manifest validation, sort order, and main exit codes

Build gate: 0 warnings 0 errors. All 20 tests pass.

Co-Authored-By: Claude Opus 4.6 (1M context) <noreply@anthropic.com>
```

### `484c4a593069f1ca670534ec377027767b0a0edf` -- 2026-05-09T03:08:39Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/ace/ace.test.ts`
- `tools/ace/store.ts`

**Message:**

```
fix(B-0288): address Codex review — guard non-dir store path, validat…

…e version field, fix tsc

Guard listInstalled against non-directory store paths (P1) and add
version to manifest field validation (P2) per Codex review. Also fix
pre-existing tsc strict-mode errors (noUncheckedIndexedAccess).

Co-Authored-By: Claude Opus 4.6 (1M context) <noreply@anthropic.com>
```

### `cc0c3ef998ca863c179e3c48a3d208dfd71247dc` -- 2026-05-09T03:09:48Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `docs/backlog/P1/B-0288-ace-dlc-package-manager-cli-2026-05-08.md`
- `tools/ace/ace.test.ts`

**Message:**

```
fix(B-0288): rename misleading test, bump backlog last_updated

Rename "returns valid JSON" test to match its actual assertion (exit
code only). Update B-0288 last_updated to 2026-05-09.

Co-Authored-By: Claude Opus 4.6 (1M context) <noreply@anthropic.com>
```

### `35fab7f8423c63022f0c46924581b2d4f3b30df3` -- 2026-05-09T03:15:24Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/ace/ace.test.ts`
- `tools/ace/store.ts`

**Message:**

```
fix(ace): wrap readdirSync in try/catch to handle EACCES/EPERM

Unreadable store directories passed the statSync isDirectory() guard
but still threw on readdirSync, crashing ace list with an uncaught
exception instead of a controlled empty result.

Wraps the readdirSync call in try/catch and returns [] on any error.
Adds a chmod-0 test (skipped when running as root) to cover this path.

Resolves both unresolved review threads on PR #2171.

Co-authored-by: Copilot <223556219+Copilot@users.noreply.github.com>
```
