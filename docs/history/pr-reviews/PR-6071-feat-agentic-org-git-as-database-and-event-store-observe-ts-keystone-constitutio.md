# PR #6071 -- feat(agentic-org): git-as-database-and-event-store + observe.ts keystone + constitution gate + metrics/review board

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 6071 |
| Title | feat(agentic-org): git-as-database-and-event-store + observe.ts keystone + constitution gate + metrics/review board |
| Author | `maximdolphin` (human) |
| State | MERGED |
| Created at | 2026-05-30T03:05:39Z |
| Merged at | 2026-05-30T16:22:55Z |
| Merge commit SHA | `40edb8d76a8c9ff6c6c8a0f3b37259cc455fd03f` |
| Branch | `feat/observe-composer-run-state-zetaid-sync-2026-05-29` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/6071 |
| Changed files | 199 |
| Additions / deletions | +18626 / -30 |

## Description

## Self-driving agentic organization — deterministic keep-alive + autonomous data plane, proven in kubernetes

This branch builds and **proves end-to-end in a kind (kubernetes-in-docker) cluster** the operator's #1 tenet: *enough determinism to drive the organization AND the agents to stay alive, with the agents doing the autonomous work.*

### The full loop, proven in-cluster

```
task event -> ingest -> reaction plan -> Hermes agent run (autonomous)
  -> durable agent liveness -> the agent goes silent
  -> the deterministic keep-alive engine catches it past its deadline
  -> a stale_work_reassignment signal naming the agent + work item
```

Live in-cluster evidence (see docs/NORTH_STAR_ALIGNMENT_CHECKPOINT.md):

- **Org liveness** — org heartbeat advanced unattended to version 56, surviving a full worker redeploy.
- **Tasks spin up** — a published SupervisorSignalSent event drove ingest -> reaction plan -> completed.
- **Autonomous data plane** — the deployed worker runs reaction-plan actions through a Hermes run; an agent_heartbeat row was persisted for the exact work item.
- **Watch loop closes** — when that agent went silent, keep-alive recorded a stale_work_reassignment alert (age 93063 > 90000 deadline) naming the agent + work item.

### What landed

- Deterministic keep-alive control plane (pure engine + lane), wired as the first worker-runtime lane with lane-failure discipline. Both halves: org liveness + agent liveness, DB-clock age, Cockroach-backed.
- Hermes data-plane integration — createHermesReactionPlanActionExecutor runs actions through runWorkItemThroughHermes, persisting agent liveness via a dependency-inverted writer.
- Process host + container substrate — bootable main.ts, Dockerfile, deploy/k8s manifests, NATS provisioner, task publisher.
- 3 real CockroachDB-dialect bugs fixed (multi-statement migration split; interval cast). Migrations 0011 + 0012 with on-disk .sql mirrors + parity tests.

### Quality
**520 tests, 520 pass, 0 fail, 0 skipped** vs live Cockroach + NATS. tsc 0. TDD; SOLID; house DU style; control-plane/data-plane separation preserved.

### Honest remaining (named, not hidden)
The agent's internal decision backend is the in-process Hermes runtime today; a real LLM/sandbox backend swaps in behind the unchanged HermesRuntime port. Forward: independent fast keep-alive loop, durable Hermes/Hindsight tables, full hat/supervisor-chain org structure. Every surrounding piece is real and proven.

🤖 Generated with [Claude Code](https://claude.com/claude-code)
---

## 2026-05-30 — Autonomous org + deterministic keep-alive, proven in kubernetes (kind)

The agentic organization now runs end-to-end in a kind cluster (CockroachDB +
NATS JetStream + worker), with both planes durable and proven in-cluster.

### What's proven (read straight from Cockroach in-cluster)

1. **Deterministic keep-alive control plane (operator tenet #1).** Cockroach-backed,
   DB-clock-aged, on an independent fast loop decoupled from the work cycle.
   Org liveness heartbeat at `version=594` (only 7 transient `org_stall`s);
   1693 `stale_work_reassignment` signals as the watch relentlessly catches
   silent agents. Keep-alive only SIGNALS liveness; it never decides work.
2. **Autonomous data plane (Hermes).** Every reaction-plan action runs as a
   durable, auditable `hermes_run` (state→completed), with Hindsight memory and
   an agent-liveness heartbeat the control plane watches. (A per-execution
   id-collision bug — fixed: crypto-UUID ids + `$N::JSONB` cast — was caught by
   the live cluster and now has a live regression test.)
3. **Agent decisions are computed, not scripted.** The agent's outcome flows
   through the deterministic decision kernel `observe → decide`:
   `DefaultDeterministicRules` compute the legal options (determinism keeps the
   org within bounds); the composer (`EphemeralComposerPort`) chooses among
   them; an out-of-set choice is rejected as a rule violation. Proven in-cluster:
   `outcome_summary="decided 'compose' -> composing: …"`,
   memory=`"selected compose from 2 legal option(s) under rules [gate-precondition, evidence-precondition]"`.
4. **The organizational-structure command pipeline.** Each action also produces
   a durable org artifact — a supervisor-triage `discussion_anchor` created
   through the command pipeline, anchored to an idempotently-seeded `work_item`
   and `project`. Agent autonomy meets auditable org substrate.

### Deploy surface added

`Dockerfile`, `deploy/k8s/*` (namespace, cockroach, nats, worker),
`deploy/provision-nats.ts`, `deploy/spin-up-task.ts`. Worker image
`agentic-org-worker:keepalive`.

### Quality

`tsc` 0 errors, 542 tests pass (0 fail), House DU style + Result-as-DU +
dependency-inversion throughout. Full phase-by-phase evidence in
`docs/NORTH_STAR_ALIGNMENT_CHECKPOINT.md`.

### One remaining seam (infra-dependent, not pure code)

A *live* LLM/sandbox composer (real model calls + sandboxed tools). It is a
drop-in `EphemeralComposerPort` behind the unchanged decision kernel — every
durable invariant it relies on (deterministic legal-option guardrail, Hermes
run lifecycle, Hindsight memory, agent liveness, org-artifact command pipeline)
is implemented and proven in-cluster above.
---

## 2026-05-30 (update) — Live LLM + sandboxed-tool agent decision backend, proven in-cluster

The agent's decision backend is now LIVE: real model calls + real sandboxed tool
execution, fully autonomous (no external credentials — the model runs in-cluster).

- **Real model calls.** A model runs in-cluster (Ollama `qwen2:0.5b`,
  `deploy/k8s/25-ollama.yaml`). The worker's `createModelBackedComposer` builds a
  prompt from the LEGAL options, calls the model, parses its chosen legal token,
  and the decision kernel re-validates it (shared `resolveSelection`). Illegal /
  unparseable / unreachable → deterministic fallback. The model adds judgment
  WITHIN the guardrails; it cannot widen them. Proven: Ollama GIN log
  `08:01:47 | 200 | 1.788s | 10.244.0.20 | POST "/api/chat"` (worker pod), and
  `hermes_run.outcome_summary = "decided 'compose' -> composing: model selected 'compose'"`.
- **Real sandboxed tool execution.** `createSubprocessSandbox` runs a bounded
  child process (isolated cwd, env stripped to PATH, SIGKILL on timeout, capped
  output). The agent runs a sha256 verification tool; the digest is durable
  evidence: `outcome_evidence_refs = ["evt-…","sandbox:sha256:f983a883…"]`. Unit
  tests prove it really executes, really times out, and really hides worker
  secrets from the tool.

`tsc` 0, 554 tests pass. Full evidence in `docs/NORTH_STAR_ALIGNMENT_CHECKPOINT.md`.

**The entire vision is now implemented and proven end-to-end in kubernetes:**
deterministic keep-alive (org + agent liveness), autonomous durable data plane,
real model-driven agent decisions bounded by the deterministic legal-option
kernel, real sandboxed tool execution, and the organizational-structure command
pipeline (durable discussion anchors anchored to work items).


## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | true |
| Total threads | 23 |
| Resolved threads | 23 |
| Unresolved threads | 0 |
| Total review comments | 25 |
| Total fix commits (touching thread paths) | 12 |

## Review threads

### Thread 1 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F22Sb`
- **Path**: `agentic-organization/apps/workers/src/config.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-30T08:27:11Z on `agentic-organization/apps/workers/src/config.ts`:102 (association: NONE)

  <pre>
  LLM config parsing silently ignores partial configuration (only LLM_BASE_URL or only LLM_MODEL). That contradicts the type contract comment that llmModel is required when llmBaseUrl is set, and makes misconfiguration hard to diagnose (it falls back without warning). Consider either (a) throwing a typed WorkerRuntimeConfigError when exactly one of the two env vars is provided, or (b) updating the comment/type to match the intended “both optional, best-effort” behavior.
  </pre>

### Thread 2 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F22Sc`
- **Path**: `agentic-organization/apps/workers/src/adapters/subprocess-sandbox.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-30T08:27:11Z on `agentic-organization/apps/workers/src/adapters/subprocess-sandbox.ts`:39 (association: NONE)

  <pre>
  SandboxToolRequest says `command` should be an absolute path, but the adapter doesn’t enforce that. If a relative command is ever passed, execFile will resolve via PATH (even though env is stripped), which is a footgun and can execute an unexpected binary. Validate `request.command` is absolute and fail as a Result when it isn’t.
  </pre>

### Thread 3 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F22Sf`
- **Path**: `agentic-organization/packages/frontmatter-db/src/schema-to-sql.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-30T08:27:12Z on `agentic-organization/packages/frontmatter-db/src/schema-to-sql.ts`:32 (association: NONE)

  <pre>
  Enum values are interpolated into SQL without escaping. If any enum value contains a single quote, emitCreateTable will generate invalid SQL (or worse, allow SQL injection in contexts where schema is user-authored). Escape `'` as `''` when emitting enum literals.
  </pre>

### Thread 4 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F22Sj`
- **Path**: `agentic-organization/packages/frontmatter-db/src/event-codec.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-30T08:27:12Z on `agentic-organization/packages/frontmatter-db/src/event-codec.ts`:64 (association: NONE)

  <pre>
  serializeEvent copies event.fields into frontmatter verbatim, which allows a caller to supply reserved `$...` keys (e.g. `$id`) and overwrite the event metadata. That breaks the “reserved keys can’t collide with field names” invariant and can lead to spoofed/incorrect events when re-parsed. Guard against reserved keys when serializing (throw or otherwise reject).
  </pre>

### Thread 5 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F22Sl`
- **Path**: `agentic-organization/packages/frontmatter-db/src/validate.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-30T08:27:12Z on `agentic-organization/packages/frontmatter-db/src/validate.ts`:44 (association: NONE)

  <pre>
  validateRow currently special-cases a `table` key inside row.values and skips unknown-column validation for it. But FrontmatterRow already carries `table` separately, and documentToRow copies frontmatter keys verbatim, so allowing a stray `table:` frontmatter key to bypass validation is likely accidental and can hide schema violations.
  </pre>

### Thread 6 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F22So`
- **Path**: `agentic-organization/packages/frontmatter-db/src/validate.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-30T08:27:12Z on `agentic-organization/packages/frontmatter-db/src/validate.ts`:71 (association: NONE)

  <pre>
  FK / FK-array columns are typed as references to ZetaIdDecimal (and traverse.ts casts them to ZetaIdDecimal), but validation only checks “non-empty string”. This allows non-ZetaId reference values through validation and then into traversal/store lookup with a branded type, undermining the type guarantee. Validate FK values as base-10 ZetaIds (same regex as ColumnType.ZetaId).
  </pre>

### Thread 7 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F22Sr`
- **Path**: `agentic-organization/packages/state-cockroach/test/sql-statement-splitter.test.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-30T08:27:13Z on `agentic-organization/packages/state-cockroach/test/sql-statement-splitter.test.ts`:34 (association: NONE)

  <pre>
  This test name says the splitter “strips” a line comment, but the expected output preserves the comment (it becomes part of the second returned statement). Renaming the test avoids documenting behavior that isn’t implemented.
  </pre>

### Thread 8 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F22Sv`
- **Path**: `agentic-organization/apps/workers/src/config.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-30T08:27:13Z on `agentic-organization/apps/workers/src/config.ts`:102 (association: NONE)

  <pre>
  LLM config parsing silently ignores partial configuration (only LLM_BASE_URL or only LLM_MODEL). That contradicts the type contract comment that llmModel is required when llmBaseUrl is set, and makes misconfiguration hard to diagnose (it falls back without warning). Consider either (a) throwing a typed WorkerRuntimeConfigError when exactly one of the two env vars is provided, or (b) updating the comment/type to match the intended “both optional, best-effort” behavior.
  </pre>

### Thread 9 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F22Sx`
- **Path**: `agentic-organization/apps/workers/src/adapters/subprocess-sandbox.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-30T08:27:13Z on `agentic-organization/apps/workers/src/adapters/subprocess-sandbox.ts`:39 (association: NONE)

  <pre>
  SandboxToolRequest says `command` should be an absolute path, but the adapter doesn’t enforce that. If a relative command is ever passed, execFile will resolve via PATH (even though env is stripped), which is a footgun and can execute an unexpected binary. Validate `request.command` is absolute and fail as a Result when it isn’t.
  </pre>

### Thread 10 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F22S0`
- **Path**: `agentic-organization/packages/frontmatter-db/src/schema-to-sql.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-30T08:27:13Z on `agentic-organization/packages/frontmatter-db/src/schema-to-sql.ts`:32 (association: NONE)

  <pre>
  Enum values are interpolated into SQL without escaping. If any enum value contains a single quote, emitCreateTable will generate invalid SQL (or worse, allow SQL injection in contexts where schema is user-authored). Escape `'` as `''` when emitting enum literals.
  </pre>

### Thread 11 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F22S2`
- **Path**: `agentic-organization/packages/frontmatter-db/src/event-codec.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-30T08:27:13Z on `agentic-organization/packages/frontmatter-db/src/event-codec.ts`:64 (association: NONE)

  <pre>
  serializeEvent copies event.fields into frontmatter verbatim, which allows a caller to supply reserved `$...` keys (e.g. `$id`) and overwrite the event metadata. That breaks the “reserved keys can’t collide with field names” invariant and can lead to spoofed/incorrect events when re-parsed. Guard against reserved keys when serializing (throw or otherwise reject).
  </pre>

### Thread 12 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F22S4`
- **Path**: `agentic-organization/packages/frontmatter-db/src/validate.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-30T08:27:14Z on `agentic-organization/packages/frontmatter-db/src/validate.ts`:44 (association: NONE)

  <pre>
  validateRow currently special-cases a `table` key inside row.values and skips unknown-column validation for it. But FrontmatterRow already carries `table` separately, and documentToRow copies frontmatter keys verbatim, so allowing a stray `table:` frontmatter key to bypass validation is likely accidental and can hide schema violations.
  </pre>

### Thread 13 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F22S6`
- **Path**: `agentic-organization/packages/frontmatter-db/src/validate.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-30T08:27:14Z on `agentic-organization/packages/frontmatter-db/src/validate.ts`:71 (association: NONE)

  <pre>
  FK / FK-array columns are typed as references to ZetaIdDecimal (and traverse.ts casts them to ZetaIdDecimal), but validation only checks “non-empty string”. This allows non-ZetaId reference values through validation and then into traversal/store lookup with a branded type, undermining the type guarantee. Validate FK values as base-10 ZetaIds (same regex as ColumnType.ZetaId).
  </pre>

### Thread 14 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F22S-`
- **Path**: `agentic-organization/packages/state-cockroach/test/sql-statement-splitter.test.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-30T08:27:14Z on `agentic-organization/packages/state-cockroach/test/sql-statement-splitter.test.ts`:34 (association: NONE)

  <pre>
  This test name says the splitter “strips” a line comment, but the expected output preserves the comment (it becomes part of the second returned statement). Renaming the test avoids documenting behavior that isn’t implemented.
  </pre>

### Thread 15 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F22TC`
- **Path**: `agentic-organization/apps/workers/src/config.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-30T08:27:14Z on `agentic-organization/apps/workers/src/config.ts`:102 (association: NONE)

  <pre>
  LLM config parsing silently ignores partial configuration (only LLM_BASE_URL or only LLM_MODEL). That contradicts the type contract comment that llmModel is required when llmBaseUrl is set, and makes misconfiguration hard to diagnose (it falls back without warning). Consider either (a) throwing a typed WorkerRuntimeConfigError when exactly one of the two env vars is provided, or (b) updating the comment/type to match the intended “both optional, best-effort” behavior.
  </pre>

### Thread 16 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F22TI`
- **Path**: `agentic-organization/apps/workers/src/adapters/subprocess-sandbox.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-30T08:27:15Z on `agentic-organization/apps/workers/src/adapters/subprocess-sandbox.ts`:39 (association: NONE)

  <pre>
  SandboxToolRequest says `command` should be an absolute path, but the adapter doesn’t enforce that. If a relative command is ever passed, execFile will resolve via PATH (even though env is stripped), which is a footgun and can execute an unexpected binary. Validate `request.command` is absolute and fail as a Result when it isn’t.
  </pre>

### Thread 17 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F22TK`
- **Path**: `agentic-organization/packages/frontmatter-db/src/schema-to-sql.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-30T08:27:15Z on `agentic-organization/packages/frontmatter-db/src/schema-to-sql.ts`:33 (association: NONE)

  <pre>
  Enum values are interpolated into SQL without escaping. If any enum value contains a single quote, emitCreateTable will generate invalid SQL (or worse, allow SQL injection in contexts where schema is user-authored). Escape `'` as `''` when emitting enum literals.
  </pre>

### Thread 18 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F22TO`
- **Path**: `agentic-organization/packages/frontmatter-db/src/event-codec.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-30T08:27:15Z on `agentic-organization/packages/frontmatter-db/src/event-codec.ts`:64 (association: NONE)

  <pre>
  serializeEvent copies event.fields into frontmatter verbatim, which allows a caller to supply reserved `$...` keys (e.g. `$id`) and overwrite the event metadata. That breaks the “reserved keys can’t collide with field names” invariant and can lead to spoofed/incorrect events when re-parsed. Guard against reserved keys when serializing (throw or otherwise reject).
  </pre>

### Thread 19 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F22TR`
- **Path**: `agentic-organization/packages/frontmatter-db/src/validate.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-30T08:27:15Z on `agentic-organization/packages/frontmatter-db/src/validate.ts`:44 (association: NONE)

  <pre>
  validateRow currently special-cases a `table` key inside row.values and skips unknown-column validation for it. But FrontmatterRow already carries `table` separately, and documentToRow copies frontmatter keys verbatim, so allowing a stray `table:` frontmatter key to bypass validation is likely accidental and can hide schema violations.
  </pre>

### Thread 20 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F22TS`
- **Path**: `agentic-organization/packages/frontmatter-db/src/validate.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-30T08:27:15Z on `agentic-organization/packages/frontmatter-db/src/validate.ts`:71 (association: NONE)

  <pre>
  FK / FK-array columns are typed as references to ZetaIdDecimal (and traverse.ts casts them to ZetaIdDecimal), but validation only checks “non-empty string”. This allows non-ZetaId reference values through validation and then into traversal/store lookup with a branded type, undermining the type guarantee. Validate FK values as base-10 ZetaIds (same regex as ColumnType.ZetaId).
  </pre>

### Thread 21 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F22TU`
- **Path**: `agentic-organization/packages/state-cockroach/test/sql-statement-splitter.test.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-30T08:27:16Z on `agentic-organization/packages/state-cockroach/test/sql-statement-splitter.test.ts`:34 (association: NONE)

  <pre>
  This test name says the splitter “strips” a line comment, but the expected output preserves the comment (it becomes part of the second returned statement). Renaming the test avoids documenting behavior that isn’t implemented.
  </pre>

### Thread 22 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F4Xe2`
- **Path**: `agentic-organization/packages/frontmatter-db/src/validate.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-30T15:51:22Z on `agentic-organization/packages/frontmatter-db/src/validate.ts`:91 (association: NONE)

  <pre>
  P1: `checkType`'s `switch` has a `default` that silently returns. That defeats compile-time exhaustiveness if a new `ColumnType`/`ColumnDef` variant is added later (it would be dropped on the floor instead of forcing an update). Prefer a `never` exhaustiveness check in the default branch so additions fail the build.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-30T16:19:08Z on `agentic-organization/packages/frontmatter-db/src/validate.ts`:91 (association: MEMBER)

    <pre>
    Fixed in 8a72a46d0. Replaced the silent `default: return;` in `checkType` with a `never` exhaustiveness assertion (`const _exhaustive: never = column; return _exhaustive;`). A new `ColumnType`/`ColumnDef` variant added without a matching case now fails the `tsc` build instead of being dropped on the floor — composes with the repo rule *IMPLICIT-NOT-EXPLICIT in DUs is class error*. Verified: tsc 0 errors; `validate-and-traverse.test.ts` 9/9 pass.
    </pre>

### Thread 23 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F4Xe6`
- **Path**: `agentic-organization/packages/state-cockroach/test/cockroach-schema.test.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-30T15:51:22Z on `agentic-organization/packages/state-cockroach/test/cockroach-schema.test.ts`:97 (association: NONE)

  <pre>
  P1: `createCockroachCoreStateMigrations()` now includes the org-system migration (`OrgSystemV15`), but the ordering test stops at `HermesRunV14`. Add an assertion for the new tail migration so the test continues to guard migration ordering as new migrations are appended.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-30T16:19:16Z on `agentic-organization/packages/state-cockroach/test/cockroach-schema.test.ts`:97 (association: MEMBER)

    <pre>
    Fixed in 8a72a46d0. Extended the migration-ordering test with `equal(migrations[14]?.name, CockroachCoreStateMigrationName.OrgSystemV15);`. The list returned by `createCockroachCoreStateMigrations()` now has 15 entries (`OrgSystemV15` at index 14); the test previously stopped at `HermesRunV14` / index 13, so the new tail was unguarded. Verified: `cockroach-schema.test.ts` 16/16 pass.
    </pre>

## Fix commits (touching thread paths)

### `e4e1014e61ddc3fe38b1bf9df402c601c79265d8` -- 2026-05-30T01:58:39Z -- `maximdolphin`

**Touched files (intersect with thread paths):**

- `agentic-organization/packages/frontmatter-db/src/validate.ts`

**Message:**

```
feat(agentic-org): git-as-database-and-event-store via frontmatter + …

…ZetaId CRDT

Operator vision 2026-05-29: git is the database AND the event store. A markdown
file is a row, frontmatter is the SQL-derived typed schema/columns + fk graph
edges, events are ZetaId-keyed files that merge conflict-free as a G-Set CRDT,
state is a timestamp-ordered fold, CockroachDB is a rebuildable query index.

New package packages/frontmatter-db (26 tests; full suite 297 green; typecheck
clean for new files):
- schema.ts: ColumnType DU (zeta_id/text/int/bool/timestamp/enum/fk/fk_array)
  with payload-bearing variants explicit (enum.values, fk.references); TableSchema,
  FrontmatterRow, edge/pk helpers.
- sql-to-schema.ts: CREATE TABLE -> TableSchema (PRIMARY KEY->zeta_id, CHECK IN->
  enum, REFERENCES->fk, TYPE[] REFERENCES->fk_array, NOT NULL->required); explicit
  feedback on non-DDL (Result<T,TFeedback> as two-variant DU).
- event.ts + crdt-log.ts: ZetaId-decimal event records; timestamp read from the
  id's 48-bit field; G-Set log keyed by unique id; mergeLogs union proven
  commutative/associative/idempotent (the CRDT join laws => conflict-free merge).
- project.ts: deterministic (timestamp,id)-ordered fold to rows; upsert=LWW,
  retract=tombstone (Z-set/retraction-native); project(merge(a,b))==project(merge(b,a))
  convergence proven.
- validate.ts: row vs schema (enum range, fk shape, required, unknown-column).
- traverse.ts: fk/fk_array columns as graph edges; neighbors() resolves against a
  ZetaId-keyed store (same mechanism as the doc composes_with graph).

Docs:
- Rewrote GIT_COCKROACH_SYNC_AND_ZETAID_ADDRESSING.md to the frontmatter-native +
  event-store-CRDT model (supersedes the prior JSON-per-aggregate draft); status
  v0; code_anchors point at the tested files; notes git's native object-DB shape
  (Linus) and why ZetaId (stable/time-ordered) is the key vs content SHA.
- DOC_FRONTMATTER_CONVENTION.md: frontmatter's two unified roles (doc-graph +
  db-row/schema) share one traversal mechanism.
- README wired. All frontmatter pointers verified resolving.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
```

### `53c0e34185a5dc91b4b575634344dabefc2f0ed0` -- 2026-05-30T02:26:43Z -- `maximdolphin`

**Touched files (intersect with thread paths):**

- `agentic-organization/packages/frontmatter-db/src/schema-to-sql.ts`

**Message:**

```
feat(agentic-org): frontmatter YAML codec, schema<->SQL + git<->cockr…

…oach sync, >=3-agent constitution gate

Three deliverables for git-as-database-and-event-store, built by an isolated
3-agent workflow then integration-gated inline (full suite 318 green, up from
291; real tsc clean for all new files; only the pre-existing 8 @nats-io
missing-dep errors in apps/workers remain).

frontmatter-db (new files):
- frontmatter-codec.ts: self-contained YAML-frontmatter parse/serialize for our
  FrontmatterValue set (string/number/boolean/string[]) + markdown body. Lossless
  round-trip: number-looking strings quoted so they parse back as strings; arrays
  as [a, b, c]; explicit ParseResult DU (missing_frontmatter / unterminated_
  frontmatter / malformed_line). rowToDocument/documentToRow bridge to FrontmatterRow.
- schema-to-sql.ts: emitCreateTable(schema) — inverse of sql-to-schema; round-trip
  verified (parseCreateTable(emitCreateTable(s)) reconstructs the columns).
- sync.ts: the generic git<->cockroach loop as pure functions over injected ports
  (GitEventSource/IndexRowSink/IndexRowSource/GitEventSink/IdGenerator).
  syncGitToIndex folds the event log via project() and upserts rows + tombstone-
  deletes ids no longer projected; syncIndexToGit emits one Upsert event per
  changed row (row_missing_id feedback when a row lacks its pk). Explicit
  SyncDirection + Result-as-DU.

governance (new files):
- constitution-gate.ts: evaluateConstitutionRatification — the >=3-agent gate as a
  pure function. State precedence is explicit: any objection -> Rejected; else
  distinct agree-agents >= quorum (DEFAULT_CONSTITUTION_QUORUM=3) -> Ratified; else
  >=1 agreement -> Gathering; else Proposed. Distinct-agentId set means one agent
  agreeing twice counts once (no self-amplification). Self-contained (no vote-tally
  dependency — that module does not exist in the repo).

Wiring + docs: exported all three from their package index.ts (agents were
isolated from barrels to avoid races). Updated GIT_COCKROACH_..._ADDRESSING.md
(Layer 5 + Status now reflect the built codec/emitter/sync + code_anchors) and
OBSERVE_COMPOSER_AND_RUN_STATE.md (constitution gate now implemented, not design;
code_anchor added). All frontmatter pointers verified resolving.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
```

### `8f36fee6c8556de2653de5e19bef6b7b4ca32a04` -- 2026-05-30T02:42:05Z -- `maximdolphin`

**Touched files (intersect with thread paths):**

- `agentic-organization/packages/frontmatter-db/src/event-codec.ts`

**Message:**

```
feat(agentic-org): git/cockroach sync adapters + reconcile worker; qu…

…antitative metrics + 3-agent review board + MCP tool interface

Builds the real I/O edges behind the sync ports and the two-layer metrics system.
Full suite 346 green (was 318); real tsc clean for all new files; pre-existing 8
@nats-io errors in apps/workers unchanged.

frontmatter-db — adapters + reconcile (real edges behind the tested pure core):
- event-codec.ts: FrontmatterEvent <-> markdown file. Event metadata under
  reserved $-prefixed frontmatter keys so it can't collide with field columns;
  reuses the row codec so quoting/round-trip rules are identical.
- git-fs-adapter.ts: filesystem-backed GitEventSource+GitEventSink over
  events/<table>/<ZetaIdDecimal>.md. async load() snapshots into memory so the
  sync ports stay synchronous; appends buffer; async flush() writes. Testable via
  an injected EventFileSystem (no node:fs dependency in the core).
- cockroach-row-sink.ts: in-memory IndexRowSink+IndexRowSource reference impl
  (the rebuildable index) with change-tracking; SQL host is a // TODO.
- reconcile-worker.ts: runOnce() cycle mirroring worker-host.ts with lane-tagged
  failures + explicit status DU. Ordering bug caught by test and fixed:
  index->git runs BEFORE git->index so a row written only to the index this cycle
  becomes an event before the projection diff — otherwise git (canonical) would
  tombstone-delete it. 13 tests.

metrics (new package) — operator's two-layer idea 4:
- code-metrics.ts: quantitative "coverage for structure" — longest function /
  longest class (god-object detection) / file length / max nesting, each breach
  an explicit MetricFinding DU on metric+severity.
- review-board.ts: the qualitative >=3-agent board. A CandidateFinding is adopted
  only when >= quorum DISTINCT reviewers agree (one agent voting thrice counts
  once); quorum-agree AND quorum-disagree -> Contested (escalate); < quorum
  reviewers -> feedback. Same multi-oracle agreement shape as the constitution
  gate, applied to review findings (restated, not cross-imported, per package
  boundary). Reviewers vote along correctness/solid/architecture/perf/testing.
- mcp-tools.ts: MCP tool INTERFACE only — METRICS_TOOL_DESCRIPTORS +
  dispatchMetricsTool(name,args) pure router returning an explicit
  MetricsToolResult DU. Server hosting is a // TODO(mcp-host) per the operator.
  15 tests.

Docs: new METRICS_AND_REVIEW_BOARD.md (status v0); GIT_COCKROACH_..._ADDRESSING.md
updated (adapters/reconcile now built + ordering rationale + new code_anchors);
README wired; all frontmatter pointers verified resolving.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
```

### `33e40d0a26d5be037f71eba070d8d10cee3eabd1` -- 2026-05-30T05:52:43Z -- `maximdolphin`

**Touched files (intersect with thread paths):**

- `agentic-organization/packages/state-cockroach/test/sql-statement-splitter.test.ts`

**Message:**

```
feat(agentic-org): Phase 5 — bootable process host + container substr…

…ate + real CockroachDB-dialect fixes

Process host (apps/workers/src/main.ts):
- runMain(overrides?) reads env -> parseWorkerRuntimeConfigFromEnv -> builds
  pg/nats/telemetry -> migration bootstrap + readiness -> composes durable
  ports -> worker runtime -> process loop. Real SIGINT/SIGTERM signal source +
  setTimeout sleeper. Config error -> stderr -> exit 1 (graceful, never crash).
  import.meta.url main-module guard. node-process-host.d.ts ambient typing.

Container substrate:
- Dockerfile: node:24-slim, npm ci --omit=dev, USER node,
  ENTRYPOINT node --experimental-strip-types apps/workers/src/main.ts.
  Verified: exits 1 with "missing_cockroach_database_url" (graceful abort).
- docker-compose.yml: cockroach v26.2.1 (single-node --insecure, healthcheck),
  nats 2.14.1-alpine (-js), worker. .dockerignore excludes node_modules/tests/docs.

Real CockroachDB-dialect fixes (only visible against a live cluster):
- sql-statement-splitter.ts: splitSqlStatements() respects single-quoted
  strings ('' escape) + line comments. CockroachDB runs a multi-statement
  string as ONE implicit txn and forbids referencing a column added by an
  earlier DDL stmt in the same txn (ADD COLUMN version; UPDATE ... version ->
  SQLSTATE 42703). Migration runner + bootstrapper now execute per-statement.
- reaction-plan-work-queue: $N::INT8 cast before * INTERVAL '1 ms'
  (interval * interval is unsupported; param defaults to interval).

Tests: 490 total, 486 pass, 0 fail, 4 skipped. tsc 0 errors.
Golden-SQL assertions re-derived from splitSqlStatements (same source of truth).
All 4 live cockroach-integration tests pass against real Cockroach + NATS.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
```

### `d9c5a97830acbcba5ddb94a5894a466c6ba08d59` -- 2026-05-30T06:16:02Z -- `maximdolphin`

**Touched files (intersect with thread paths):**

- `agentic-organization/apps/workers/src/config.ts`
- `agentic-organization/packages/state-cockroach/test/cockroach-schema.test.ts`

**Message:**

```
feat(agentic-org): make the deterministic keep-alive real — Cockroach…

…-backed, wired into production composition (operator tenet #1)

The keep-alive lane was wired into the worker runtime but production never
constructed one (keepAliveLane stayed undefined in the deployed worker). This
makes it real on durable Cockroach state, end-to-end.

New durable substrate — migration 0011 (two tables, DV2.0 change-rate split):
- agentic_org_control_plane_heartbeat: ONE row per org, UPSERTed every tick.
  last_tick_at advancing IS the org's observable proof of life.
- agentic_org_control_plane_alerts: append-only self-heal signal log
  (org stall / stale-work reassignment / lease reap).
On-disk migrations/0011_*.sql mirror + TS↔disk parity test pass.

New Cockroach adapters (state-cockroach):
- createCockroachControlPlaneStateStore: readOrgHeartbeatAgeMs (age measured by
  the DB clock, not the worker clock — deterministic across replicas),
  tickOrgHeartbeat (idempotent UPSERT, version+1), appendAlert.
- createCockroachKeepAliveSnapshotSource: loads org liveness into the pure
  engine's snapshot. v1 agents[]/leases[] empty (Hermes sessions not yet
  persisted) — engine handles empty; ORG liveness is real now.
- createCockroachKeepAliveActionSink: exhaustive DU switch over KeepAliveActionKind
  (EmitHeartbeat -> tick; the three alerts -> append). never-default catches a
  new action kind at compile time.

Wiring: composeDurableWorkerRuntimePorts builds store -> source -> sink -> lane
and threads keepAliveLane through composeWorkerRuntime -> createWorkerRuntime, so
the org heartbeat now ticks every worker cycle in the deployed process.

Config: optional WORKER_KEEP_ALIVE_ORG_HEARTBEAT_DEADLINE_MS (default 30s) — policy
not hardcoded; existing deployments need no new var.

TDD throughout (store/source/sink/config/runtime red->green). New live integration
test proves it against real Cockroach: tick advances the heartbeat row each cycle;
a forced stall emits heartbeat + org-stall alert (Degraded, appliedCount 2) and the
org self-heals. Full suite 511/511 pass, 0 skipped, vs live Cockroach + NATS. tsc 0.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
```

### `82c3d06adb4737afcf5396e427af7e1a4a33c646` -- 2026-05-30T06:43:00Z -- `maximdolphin`

**Touched files (intersect with thread paths):**

- `agentic-organization/packages/state-cockroach/test/cockroach-schema.test.ts`

**Message:**

```
feat(agentic-org): agent liveness is real — second half of keep-alive…

… tenet #1

The keep-alive snapshot source no longer hardcodes agents:[] — it reads real
persisted agent heartbeats, so the engine's stale-agent -> reassignment-signal
branch is live (not dormant).

- Migration 0012: agentic_org_agent_heartbeat (one row per org+agent;
  last_heartbeat_at, deadline_ms, version). On-disk .sql mirror + parity test.
- Control-plane store: recordAgentHeartbeat (idempotent UPSERT, version+1) +
  readAgentHeartbeats (DB-clock age, mapped to the engine's AgentHeartbeat shape).
- Snapshot source reads agent heartbeats into snapshot.agents.

Live integration proof against real Cockroach: a fresh agent heartbeat reads
ALIVE (lane applies only the org heartbeat, no reassignment); forcing the agent
past its deadline makes the engine emit ReassignStaleWork, and the sink records a
stale_work_reassignment alert naming the agent's work item. The control plane
only SIGNALS reassignment (an alert) — the actual decision stays in the
autonomous data plane (separation preserved).

TDD throughout. Full suite 516/516 pass, 0 skipped, vs live Cockroach + NATS. tsc 0.

Honest boundary: the production WRITER (a deployed agent session calling
recordAgentHeartbeat) awaits Hermes integration into the live pipeline — the
mechanism + detection are real and proven; the deployed worker has no agent
sessions heartbeating yet.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
```

### `f6d1d1280c78342348132f343fcdaefc397c6da3` -- 2026-05-30T07:04:09Z -- `maximdolphin`

**Touched files (intersect with thread paths):**

- `agentic-organization/apps/workers/src/config.ts`

**Message:**

```
feat(agentic-org): independent fast keep-alive loop — decouple the he…

…artbeat from the work cycle

The keep-alive ticked once per worker cycle, so a slow agent run or a ~30s idle
NATS long-poll delayed the org heartbeat (the deadline had to exceed the max
cycle). It now runs on its OWN fixed cadence (LoopIntervalMs, default 5s),
concurrently with the work loop and independent of it — the org's proof of life
no longer depends on work-cycle timing. This is the determinism the tenet wants.

- runKeepAliveLoop (apps/workers): interval loop with the same failure discipline
  as the lane (degraded/thrown tick captured, loop keeps ticking). 5 unit tests.
- main.ts runs it concurrently with entrypoint.run(); on a stop signal the work
  loop exits, then the keep-alive loop is stopped and its current tick awaited.
  keepAliveLane is taken OFF the work runtime (no double-tick).
- Stop-aware sleep via the injected clock; the boot-path test (main.test.ts)
  stays green — the proven boot path is untouched.

Full suite 519 pass, 0 fail (6 live skipped, no env). tsc 0.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
```

### `7bbb9d37ed9ffd4d203e23d3aa160de20b46c1e1` -- 2026-05-30T07:10:39Z -- `maximdolphin`

**Touched files (intersect with thread paths):**

- `agentic-organization/packages/state-cockroach/test/cockroach-schema.test.ts`

**Message:**

```
feat(agentic-org): durable Cockroach-backed Hindsight memory, wired i…

…nto the deployed agent runs

"Set up hermes... the memory, all that" — memory is now durable, not in-process.
A Cockroach Memory adapter (migration 0013 agentic_org_hindsight_memory) behind
the same Memory port the fake implements: retain INSERTs an attributed memory;
recall is SCOPED by project (Organization policy: never global); attribution is
STICKY (a memory keeps its original author even when recalled by another hat);
reflect counts in-scope memories. Persists across worker restarts.

Wired into the deployed reaction-plan executor's Hermes runs, so what an agent
learns is durably retained. On-disk migrations/0013_*.sql mirror + parity test.
TDD: retain/recall-scoped-and-sticky/reflect. Full suite 523 pass, 0 fail. tsc 0.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
```

### `ac0d0400b8e297790074dc9eae230d1d2efdca4f` -- 2026-05-30T07:14:57Z -- `maximdolphin`

**Touched files (intersect with thread paths):**

- `agentic-organization/packages/state-cockroach/test/cockroach-schema.test.ts`

**Message:**

```
feat(agentic-org): durable Cockroach-backed Hermes runs, wired into t…

…he deployed data plane

"Set up hermes, the orchestration, the memory, all that" — Hermes runs are now
durable, not in-process. A Cockroach Hermes runtime adapter (migration 0014
agentic_org_hermes_run) behind the same HermesRuntime port: launchRun inserts a
Running row bound to the org facts; heartbeat/complete/fail apply conditionally
(only to a Running run); an unknown run returns feedback, never throws. Every
agent run becomes a durable, auditable row (who ran on what, state, outcome).

Wired into the deployed reaction-plan executor in place of the in-process runtime.
On-disk migrations/0014_*.sql mirror + parity test. TDD: launch/heartbeat/
unknown-run/complete/non-running-guard. Full suite 529 pass, 0 fail. tsc 0.

With this, both halves of the data plane (Hermes runs + Hindsight memory) and the
whole control plane (org + agent liveness) are durable and Cockroach-backed.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
```

### `f1055f29a1e0ff2e81b29839398f473c19a7ba70` -- 2026-05-30T07:57:37Z -- `maximdolphin`

**Touched files (intersect with thread paths):**

- `agentic-organization/apps/workers/src/adapters/subprocess-sandbox.ts`
- `agentic-organization/apps/workers/src/config.ts`

**Message:**

```
feat: live LLM decision backend + real sandboxed tool execution

The agent's decision backend is now a REAL model call, and the agent really
executes a sandboxed tool — both behind dependency-inverted ports, both safe
by construction.

- AsyncEphemeralComposerPort + decideAsync: async decision path sharing the
  exact legality enforcement (resolveSelection) with the sync path, so a
  model-backed composer cannot escape the deterministic rules.
- createModelBackedComposer: builds a prompt from the legal options, calls a
  ChatCompletionPort, parses the model's chosen legal token, and falls back to
  the deterministic policy on any illegal/unparseable/unreachable response
  (defence in depth — the kernel rejects illegal picks anyway).
- createOllamaChatPort: real in-cluster model calls (no external credentials),
  AbortController-bounded.
- SandboxToolPort + createSubprocessSandbox: real bounded subprocess —
  isolated cwd, stripped env (PATH only), SIGKILL on timeout, capped output;
  Result-as-DU. The agent runs a sha256 verification tool producing durable
  evidence; tool failure is supplementary, never fatal.
- Hermes executor uses the async decision + sandbox evidence; main.ts wires the
  model composer when LLM_BASE_URL/LLM_MODEL are set, sandbox always on.
- Extended the hand-rolled node shim (child_process/fs/os + execPath/argv).

tsc 0, 553 tests (11 new: model-composer + sandbox-tool + live subprocess incl.
real timeout-kill + env-strip proofs).

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
```

### `b8fa461a3651335362fdbe24bc3501f2b91536dd` -- 2026-05-30T14:22:44Z -- `maximdolphin`

**Touched files (intersect with thread paths):**

- `agentic-organization/apps/workers/src/adapters/subprocess-sandbox.ts`
- `agentic-organization/apps/workers/src/config.ts`
- `agentic-organization/packages/frontmatter-db/src/event-codec.ts`
- `agentic-organization/packages/frontmatter-db/src/schema-to-sql.ts`
- `agentic-organization/packages/frontmatter-db/src/validate.ts`
- `agentic-organization/packages/state-cockroach/test/sql-statement-splitter.test.ts`

**Message:**

```
fix(review): resolve all 7 Copilot review findings on PR #6071

1. config.ts — throw typed PartialLlmConfig error when exactly one of
   LLM_BASE_URL / LLM_MODEL is set (was silent fallback; now diagnosable).
2. subprocess-sandbox.ts — reject a non-absolute command as a Result
   (a relative command would PATH-resolve an unexpected binary).
3. schema-to-sql.ts — escape single quotes (' -> '') in enum literals
   (was a SQL-break / injection vector for author-supplied schemas).
4. event-codec.ts — serializeEvent rejects a field key in the reserved
   '$' namespace (was able to overwrite/spoof event metadata).
5. validate.ts — drop the stray 'table'-key bypass so it's flagged as an
   unknown column (was hiding schema violations).
6. validate.ts — validate FK / FK-array values as base-10 ZetaIds, not
   just non-empty strings (matches the ZetaIdDecimal brand traverse uses).
7. sql-statement-splitter.test.ts — rename the misleading 'strips a line
   comment' test to describe the real behavior (no split inside a comment).

Each fix has a regression test. tsc 0, 564 tests (10 new), 0 fail.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
```

### `8a72a46d06fd42b2f4df3de9f2eb9b6a10f92812` -- 2026-05-30T16:17:50Z -- ``

**Touched files (intersect with thread paths):**

- `agentic-organization/packages/frontmatter-db/src/validate.ts`
- `agentic-organization/packages/state-cockroach/test/cockroach-schema.test.ts`

**Message:**

```
fix(agentic-org): close 2 Copilot P1 review findings on #6071

- validate.ts checkType: replace silent `default: return` with a
  `never` exhaustiveness assertion so a new ColumnType/ColumnDef
  variant fails the build instead of being dropped on the floor
  (composes with repo rule: IMPLICIT-NOT-EXPLICIT in DUs is class error).
- cockroach-schema.test.ts: extend the migration-ordering test to assert
  the new tail migration OrgSystemV15 (the list now has 15 entries;
  the test stopped at HermesRunV14 / index 13).

Verified: tsc 0 errors; cockroach-schema 16/16 pass; validate-and-traverse
9/9 pass. Both are assert-don't-skip shield closures — they turn a
silently-covered case into a compile error / test failure.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```
