# PR #2202 -- feat(B-0335): add memory schema validation tool

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 2202 |
| Title | feat(B-0335): add memory schema validation tool |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-05-09T06:27:50Z |
| Merged at | 2026-05-09T06:37:19Z |
| Merge commit SHA | `51ad91949388dd9dc8ce72087de048448ef725f2` |
| Branch | `fix/b0335-memory-schema-validator` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/2202 |
| Changed files | 2 |
| Additions / deletions | +265 / -1 |

## Description

## Summary

- Add `tools/hygiene/validate-memory-schema.ts` — enforces B-0330 format standard
- Validates frontmatter (name/description/type required), filename prefix matching, type enum
- Checks MEMORY.md index coverage (files not indexed, orphan entries)
- Mark B-0335 done

**First run (user-scope, 624 files):**
| Check | Result |
|-------|--------|
| Errors | 1 (missing description) |
| Warnings | 0 |
| Files not in MEMORY.md | 40 |
| Orphan index entries | 3 |

## Test plan

- [x] `bun tools/hygiene/validate-memory-schema.ts` — default
- [x] `bun tools/hygiene/validate-memory-schema.ts --memory-dir memory` — in-repo
- [x] `bun tools/hygiene/validate-memory-schema.ts --json` — structured
- [ ] CI gate (tsc)

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 5 |
| Resolved threads | 5 |
| Unresolved threads | 0 |
| Total review comments | 5 |
| Total fix commits (touching thread paths) | 1 |

## Review threads

### Thread 1 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6Ay6Yk`
- **Path**: `tools/hygiene/validate-memory-schema.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-09T06:31:44Z on `tools/hygiene/validate-memory-schema.ts`:14 (association: NONE)

  <pre>
  P0: The default memory directory is hard-coded to a per-user Claude project path ("~/.claude/projects/.../memory"). Running the tool as documented (`bun tools/hygiene/validate-memory-schema.ts`) will fail for other contributors/CI because that directory won’t exist. Default should be repo-relative (e.g., `git rev-parse --show-toplevel` + `/memory`) and rely on `--memory-dir` only for overrides.

  </pre>

### Thread 2 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6Ay6Yr`
- **Path**: `tools/hygiene/validate-memory-schema.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-09T06:31:45Z on `tools/hygiene/validate-memory-schema.ts`:86 (association: NONE)

  <pre>
  P0: `parseMemoryIndex` / coverage-gap logic treats every `](...*.md)` link in MEMORY.md as a memory-file index entry, but MEMORY.md currently contains links with path segments (e.g. `architectural-intent-guesses/README.md`) and links outside `memory/` (e.g. `../docs/research/...`). Because the validator only `readdirSync`s the top-level `memoryDir`, these will be incorrectly flagged as orphan index entries and will skew coverage counts. Filter index targets to just in-memory entries (e.g. no `/`), or resolve targets relative to `memoryDir` (and/or recursively enumerate files) before declaring them orphaned.

  </pre>

### Thread 3 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6Ay6Ys`
- **Path**: `tools/hygiene/validate-memory-schema.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-09T06:31:45Z on `tools/hygiene/validate-memory-schema.ts`:88 (association: NONE)

  <pre>
  P1: `parseMemoryIndex` uses `existsSync` then `readFileSync`, which introduces a TOCTOU file-system race that other hygiene tools explicitly avoid (see tools/hygiene/audit-memory-index-duplicates.ts). Prefer a single `readFileSync` in a try/catch and report the IO error cleanly (or treat missing MEMORY.md as empty index) to match repo scripting conventions and avoid CodeQL js/file-system-race style findings.
  </pre>

### Thread 4 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6Ay6Yt`
- **Path**: `tools/hygiene/validate-memory-schema.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-09T06:31:45Z on `tools/hygiene/validate-memory-schema.ts`:258 (association: NONE)

  <pre>
  P2: This tool’s `main()` calls `process.exit(...)` internally (e.g. when the dir is missing, and for `--enforce`), which makes the function hard to reuse/test and is inconsistent with the typical pattern in tools/hygiene where `main(argv)` returns a typed exit code and `process.exit(main(...))` is only done under `if (import.meta.main)`. Consider returning an exit code from `main` and moving all `process.exit` calls to the `import.meta.main` block.
  </pre>

### Thread 5 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6Ay6Y0`
- **Path**: `docs/backlog/P1/B-0335-memory-schema-validation-tooling-step11-b0190.md`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-09T06:31:45Z on `docs/backlog/P1/B-0335-memory-schema-validation-tooling-step11-b0190.md`:7 (association: NONE)

  <pre>
  The backlog row is marked `status: done`, but the implementation in this PR doesn’t appear to meet the row’s own acceptance criteria (e.g., no section-header checks, no link-integrity validation, and no `--fix` support). Also, per tools/backlog/README.md the `last_updated` field should be bumped on every content edit; this status change should update it too.
  </pre>

## Fix commits (touching thread paths)

### `442bc0623d4e1e19cf087015f89121bb69ecfb5e` -- 2026-05-09T06:27:44Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `docs/backlog/P1/B-0335-memory-schema-validation-tooling-step11-b0190.md`
- `tools/hygiene/validate-memory-schema.ts`

**Message:**

```
feat(B-0335): add memory schema validation tool

Enforces B-0330 format standard mechanically:
- Frontmatter validation (name, description, type required)
- Filename prefix must match declared type
- Invalid type values flagged
- MEMORY.md coverage check (files not indexed, orphan entries)
- --enforce flag exits non-zero on errors
- --json for structured output

First run (user-scope, 624 regular files):
- 1 error (missing description field)
- 40 files not in MEMORY.md index
- 3 orphan MEMORY.md entries

Co-Authored-By: Claude Opus 4.6 (1M context) <noreply@anthropic.com>
```
