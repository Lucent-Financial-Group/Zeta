# PR #870 -- ts(B-0086): port 3 alignment audit scripts (.sh→.ts) — slice 3 of TS/Bun migration

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 870 |
| Title | ts(B-0086): port 3 alignment audit scripts (.sh→.ts) — slice 3 of TS/Bun migration |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-04-30T01:32:47Z |
| Merged at | 2026-04-30T01:35:33Z |
| Merge commit SHA | `cab59ca80716b5d2faf094850f1b077c5beb0647` |
| Branch | `lane-b/ts-bun-slice-3-alignment-audits-2026-04-30` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/870 |
| Changed files | 4 |
| Additions / deletions | +1166 / -0 |

## Description

## Summary

Slice 3 of the TS/Bun migration. Cluster B1 (3 alignment audit scripts, ~750 lines bash → ~1100 lines typed TS):

- \`audit_archive_headers.{sh→ts}\` — archive-header discipline lint (Scope/Attribution/Operational status/Non-fusion disclaimer)
- \`audit_personas.{sh→ts}\` — per-round persona runtime observability (notebook-last-round, commit-mentions, roster-coverage)
- \`audit_commit.{sh→ts}\` — per-commit alignment lint (HC-2 / HC-6 / SD-6 signals from \`docs/ALIGNMENT.md\`)

Per Gate B prerequisite (verified currency of layered baseline 1 day old, well within 30-day window).

## Patterns applied

Canonical from prior slices:
- Typed boundary objects (FileFinding / PersonaRow / CommitAudit + Hc2/Hc6/Sd6 results)
- Literal-type union exit codes
- \`ParseResult\` discriminated-union via \`reduceFlag\` helper
- Structured spawn-failure classifier mirroring \`../SQLSharp/tools/automation/format/process-runner.ts\`
- \`maxBuffer: 64 * 1024 * 1024\` on \`spawnSync\`
- Named exports + \`import.meta.main\` entry guard

New this slice (recorded in \`slice-audits.md\`):
- **Bun-native \`readdirSync\` recursion** (audit_archive_headers): no \`find\` shell-out; faster on macOS; avoids sonarjs eslint exception.
- **Injective slug encoding** for collision-safe per-file output paths (\`_\` → \`_5F\` → \`/\` → \`__\`).
- **\`matchAll\` regex parsing** (audit_personas) for extracting round numbers from notebook headers.
- **Per-commit signal classifier** (audit_commit): HELD / STRAINED / VIOLATED / IRRELEVANT for each of HC-2/HC-6/SD-6.

## Equivalence

Per-file bash-vs-TS diff against current repo state:
- \`audit_archive_headers\`: byte-equivalent under default args.
- \`audit_personas\`: byte-equivalent under default args.
- \`audit_commit\`: byte-equivalent against HEAD (default range).

## Coverage

Deferred — bash originals had no tests; ports don't introduce a new module. Recorded explicitly in \`slice-audits.md\` per the DST-exempt-is-deferred-bug discipline applied to coverage.

## Test plan

- [x] \`bun x tsc --noEmit\` clean
- [x] \`bun x eslint <slice files>\` clean (strictTypeChecked + stylisticTypeChecked + sonarjs)
- [x] Bash-vs-TS equivalence per file
- [x] markdownlint clean on slice-audits.md
- [x] Slice-3 audit landed in \`docs/trajectories/typescript-bun-migration/slice-audits.md\`

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 11 |
| Resolved threads | 0 |
| Unresolved threads | 11 |
| Total review comments | 11 |
| Total fix commits (touching thread paths) | 3 |

## Review threads

### Thread 1 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM5-nMhW`
- **Path**: `tools/alignment/audit_commit.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-04-30T01:35:57Z on `tools/alignment/audit_commit.ts`:103 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Treat non-zero git exits as script failures**

  `runGit` currently accepts any non-null exit status as success, so git failures (for example an invalid single-commit range like `BAD_SHA`) are silently downgraded into empty output and can produce a misleading clean audit instead of the documented script-error path. This regresses the shell behavior (which failed on bad revisions) and can hide broken audit invocations in automation because the command may return `0/1` based on empty data rather than returning error code `2`.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 2 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM5-nNUj`
- **Path**: `tools/alignment/audit_commit.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-30T01:38:00Z on `tools/alignment/audit_commit.ts`:35 (association: NONE)

  <pre>
  Exit code convention here (0|1|2) conflicts with the repo scripting baseline (`docs/best-practices/repo-scripting.md`), which standardizes on `type AuditExitCode = 0 | 2 | 64` with 64 reserved for usage/arg/input errors. Please align this script’s exit codes/return sites to match the baseline (or document an explicit exemption).
  </pre>

### Thread 3 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM5-nNUo`
- **Path**: `tools/alignment/audit_commit.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-30T01:38:00Z on `tools/alignment/audit_commit.ts`:77 (association: NONE)

  <pre>
  This citation regex includes a specific personal name (`per aaron`). Current repo guidance is to avoid name attribution on current-state surfaces (including code under `tools/`); use role-refs instead (e.g., “per human maintainer instruction”). Consider removing the named alternative and keeping only role-ref phrasing so SD-6 doesn’t require embedding names in this script.
  ```suggestion
  const HC2_CITATION_RE = /(maintainer (asked|requested|instructed)|human (asked|requested|instructed)|per maintainer instruction|explicit authori[sz]ation)/i;
  ```
  </pre>

### Thread 4 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM5-nNUs`
- **Path**: `tools/alignment/audit_archive_headers.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-30T01:38:01Z on `tools/alignment/audit_archive_headers.ts`:47 (association: NONE)

  <pre>
  Exit code convention here (0|1|2) conflicts with the repo scripting baseline (`docs/best-practices/repo-scripting.md`), which standardizes on `type AuditExitCode = 0 | 2 | 64` (0=clean/advisory, 2=findings under `--enforce`, 64=usage/arg/input error). Since this script already has `--enforce`, it should be a good fit for the baseline; consider updating the type + return codes accordingly.
  </pre>

### Thread 5 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM5-nNUz`
- **Path**: `tools/alignment/audit_archive_headers.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-30T01:38:01Z on `tools/alignment/audit_archive_headers.ts`:265 (association: NONE)

  <pre>
  The comment says the JSON format has “no newline at end of file”, but `formatJson` currently appends `\n`. Also, the bash original writes JSON using `echo`, which *does* include a trailing newline. Suggest updating the comment to match actual behavior (rather than changing the output format).
  ```suggestion
  // with a trailing newline at end of file/output (matches echo-driven layout).
  ```
  </pre>

### Thread 6 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM5-nNU5`
- **Path**: `docs/trajectories/typescript-bun-migration/slice-audits.md`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-30T01:38:01Z on `docs/trajectories/typescript-bun-migration/slice-audits.md`:228 (association: NONE)

  <pre>
  This slice header still has a placeholder `PR #NNN`. Please replace it with the actual PR number (or remove the PR-number parenthetical) before merging so the trajectory log remains a reliable index.
  ```suggestion
  ## Slice 3 — 3 alignment-audit-script ports
  ```
  </pre>

### Thread 7 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM5-nNU8`
- **Path**: `docs/trajectories/typescript-bun-migration/slice-audits.md`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-30T01:38:01Z on `docs/trajectories/typescript-bun-migration/slice-audits.md`:245 (association: NONE)

  <pre>
  This sentence claims helpers named `classifyHc2/Hc6/Sd6` were extracted, but the TS ports in this PR use `checkHc2/checkHc6/checkSd6` (and don’t define `classifyHc*`). Please update this log entry so it accurately reflects the code (or rename the functions if the intent is to standardize on `classify*`).
  ```suggestion
  - **Cognitive-complexity push (audit_archive_headers, audit_commit)**: extracted helper functions (reduceFlag, emitJsonRollup, emitHumanSummary, checkHc2/checkHc6/checkSd6) to bring functions within the sonarjs 15-cap.
  ```
  </pre>

### Thread 8 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM5-nNVE`
- **Path**: `tools/alignment/audit_personas.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-30T01:38:02Z on `tools/alignment/audit_personas.ts`:41 (association: NONE)

  <pre>
  Exit code convention here (0|1|2) doesn’t match the repo scripting baseline, which standardizes on `type AuditExitCode = 0 | 2 | 64` (0=clean/advisory, 2=findings under enforce-mode, 64=usage/arg/input error). Since this file explicitly points readers at `docs/best-practices/repo-scripting.md`, please align the exit codes (and the corresponding return sites) to that convention.
  </pre>

### Thread 9 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM5-nNVI`
- **Path**: `tools/alignment/audit_personas.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-30T01:38:02Z on `tools/alignment/audit_personas.ts`:384 (association: NONE)

  <pre>
  In default mode this emits a human-readable report on stdout. `docs/best-practices/repo-scripting.md` specifies stdout for machine-parseable output and stderr for summaries/progress; if other tooling pipes stdout, this default output will interfere. Consider sending the human summary to stderr and reserving stdout for `--json`/`--md`/`--out` artifacts.
  </pre>

### Thread 10 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM5-nNVO`
- **Path**: `tools/alignment/audit_commit.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-30T01:38:02Z on `tools/alignment/audit_commit.ts`:342 (association: NONE)

  <pre>
  In human-output mode this writes the per-commit summary lines to stdout. `docs/best-practices/repo-scripting.md` specifies stdout for machine-parseable output only, and stderr for summaries/progress. Consider sending these human lines to stderr (and keeping stdout for `--json` / `--out` only) to avoid breaking pipelines.
  ```suggestion
        process.stderr.write(`${commitToHumanLine(audit)}\n`);
  ```
  </pre>

### Thread 11 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM5-nNVS`
- **Path**: `tools/alignment/audit_personas.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-30T01:38:02Z on `tools/alignment/audit_personas.ts`:375 (association: NONE)

  <pre>
  Per `docs/best-practices/repo-scripting.md` output-channel discipline, progress/summaries should go to stderr and stdout should be reserved for machine-parseable output. This status line is human chatter on stdout; consider moving it to stderr (or suppressing it when `--json`/`--md` output is requested).
  ```suggestion
      process.stderr.write(
  ```
  </pre>

## Fix commits (touching thread paths)

### `0831c017b092e0d12f7c604c0bdc346f4e7c116a` -- 2026-04-30T01:25:55Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/alignment/audit_archive_headers.ts`

**Message:**

```
ts(slice-3, wip 1/3): port audit_archive_headers (.sh→.ts)

First port of slice 3 (Cluster B1 alignment audits). Bash original
242 lines, TS port 270 lines.

Patterns reused (canonical from slices 1+2):
- Typed boundary objects (FileFinding, AuditResult).
- AuditExitCode literal-type union (0 | 1 | 2).
- ParseResult discriminated-union.
- maxBuffer=64MiB on spawnSync.

New patterns this port (recorded in slice-audits.md when full slice
lands):
- JSON output via JSON.stringify (matches bash hand-rolled format).
- Per-file injective slug encoding for collision-safe output paths.
- Bun-native readdirSync recursion (no spawnSync find) — faster on
  macOS, avoids sonarjs eslint exception.
- reduceFlag mutating-state helper for parseArgs.

Equivalence: bash-vs-TS output diff is empty under default args
against current repo state (24 files / 7 ok / 17 missing).

Slice 3 continuation: audit_commit + audit_personas to follow.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
```

### `5a9b2403ff66be2691a933b92ec709e628175783` -- 2026-04-30T01:28:56Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/alignment/audit_personas.ts`

**Message:**

```
ts(slice-3, wip 2/3): port audit_personas (.sh→.ts)

Second port of slice 3. Bash 226 lines → TS port (similar size).

Patterns reused (canonical):
- Typed boundary objects (PersonaRow, AuditResult).
- AuditExitCode literal-type union.
- ParseResult discriminated-union via reduceFlag.
- maxBuffer=64MiB on spawnSync.

Implementation notes:
- Persona discovery via Bun-native readdirSync (no spawnSync find).
- Round number extracted via global regex match (matchAll).
- Three output modes: JSON / Markdown / human-summary.
- Gate threshold honored when --gate FRAC given.

Equivalence: bash-vs-TS output diff is empty under default args
against current repo state.

Slice 3 status: 2/3 ports landed. audit_commit (alignment-spec
HC/SD/DIR signals) follows.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
```

### `16f4e95e767f9202cf5b2bcc8e93f796824e9a4b` -- 2026-04-30T01:32:25Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `docs/trajectories/typescript-bun-migration/slice-audits.md`
- `tools/alignment/audit_commit.ts`

**Message:**

```
ts(slice-3): port audit_commit + slice-3 audit substrate

Final port of slice 3 (HC-2/HC-6/SD-6 alignment-spec lint).
Bash 280 lines → TS port 350 lines.

audit_commit ports the per-commit alignment lint with the four
signal levels (HELD / STRAINED / VIOLATED / IRRELEVANT). Each
signal is computed independently:

- HC-2 (retraction-footprint): scan diff added-lines for
  destructive-op tokens; check commit body for an explicit
  human-instruction citation.
- HC-6 (memory-deletion audit): flag memory/** deletions; check
  body for supersede/retire/replaced-by citation.
- SD-6 (name-hygiene): scan files for maintainer names from
  sd6_names.txt; honor exempt globs.

Patterns reused (canonical):
- Typed boundary objects (CommitAudit, Hc2Result, Hc6Result, Sd6Result).
- AuditExitCode literal-type union.
- ParseResult discriminated-union via reduceFlag.
- maxBuffer=64MiB on spawnSync.

Implementation notes:
- Range resolution: `git rev-list --reverse RANGE` for `..`
  notation, single `git rev-parse REV` otherwise.
- Per-signal classifier extracted into checkHc2/Hc6/Sd6 helpers
  to bring main loop within sonarjs 15-cap.
- Hand-rolled JSON output preserves the bash original's exact
  shape (signal name + integer counters per signal).

Equivalence: bash-vs-TS output diff is empty against HEAD
(default range, current repo state).

Slice 3 audit substrate landed in slice-audits.md per Gate A:
- 3 new patterns recorded (Bun-native readdirSync recursion,
  regex matchAll for parsing, per-commit signal classifier).
- Equivalence verified per port.
- Coverage deferred explicitly per DST-exempt-is-deferred-bug
  discipline applied to coverage.

Slice 3 ready for review.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
```
