# PR #2049 -- feat(backlog): advance B-0280 PR body publication

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 2049 |
| Title | feat(backlog): advance B-0280 PR body publication |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-05-08T07:38:35Z |
| Merged at | 2026-05-08T07:53:20Z |
| Merge commit SHA | `3f9310f250f4881706f96d1d905ca64b7101bde4` |
| Branch | `claim/task-b0280-pr-body-file` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/2049 |
| Changed files | 4 |
| Additions / deletions | +109 / -4 |

## Description

## Summary
- Replaces the PR publication plan's `<body-file>` placeholder with a validated `bodyFilePath` input.
- Adds `--write-body` / `writePrBodyFile` so the generated review packet can be materialized exactly once before `gh pr create`.
- Records B-0280 progress for this executor slice.

## Backlog row
- B-0280: Autonomous backlog pickup - PR publication and auto-merge
- Path: `docs/backlog/P0/B-0280-autonomous-backlog-pr-publication-and-automerge-2026-05-08.md`

## Checks
- passed: `bun test tools/backlog/pr-publication-plan.test.ts`
- passed: `bun run typecheck`
- passed: `git diff --check`
- diagnostic: `bun run lint:typescript` still fails on pre-existing repo-wide ESLint debt outside this claim; this slice did not use it as a gate.

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | true |
| Total threads | 4 |
| Resolved threads | 4 |
| Unresolved threads | 0 |
| Total review comments | 4 |
| Total fix commits (touching thread paths) | 4 |

## Review threads

### Thread 1 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6AjaDl`
- **Path**: `tools/backlog/pr-publication-plan.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-08T07:40:31Z on `tools/backlog/pr-publication-plan.ts`:139 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Reject traversal and absolute PR body file paths**

  The new `validateBodyFilePath` check only rejects empty, option-like, NUL, and trailing-slash values, but it still accepts absolute paths (`/tmp/body.md`) and parent traversal (`../../outside.md`). With `--write-body`, `writePrBodyFile` will then create directories and write to that location, so a crafted input can make this tool create files outside the repo. Since this commit introduces on-disk writes, `bodyFilePath` needs the same repo-relative safety constraints already applied to `backlogPath`.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 2 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6AjbvK`
- **Path**: `tools/backlog/pr-publication-plan.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-08T07:42:15Z on `tools/backlog/pr-publication-plan.ts`:143 (association: NONE)

  <pre>
  P0: `validateBodyFilePath` doesn’t prevent absolute paths (e.g. `/tmp/x`), Windows drive paths, or `../` traversal segments. Because `--write-body` ultimately calls `mkdirSync`/`writeFileSync` on this value, a crafted input JSON could write outside the repo (data-loss / clobber risk). Consider reusing `validateRepoPath`-style checks (normalize `\` → `/`, reject leading `/`, `../`, `/../`, `.git`), and add a test for traversal/absolute paths.
  </pre>

### Thread 3 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6Ajbvp`
- **Path**: `tools/backlog/pr-publication-plan.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-08T07:42:15Z on `tools/backlog/pr-publication-plan.ts`:307 (association: NONE)

  <pre>
  P1: `writePrBodyFile` assumes the body file path is the last argv element (`createPr.at(-1)`). This is brittle—adding any future `gh pr create` flag after `--body-file` would make it write to the wrong path. Prefer locating the `--body-file` flag and reading the following element (and erroring if missing), or store the body file path explicitly on `PublicationPlan`.

  </pre>

### Thread 4 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6AjgH8`
- **Path**: `tools/backlog/pr-publication-plan.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-08T07:46:28Z on `tools/backlog/pr-publication-plan.ts`:124 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Reject Windows drive-relative traversal in repo path validation**

  Fresh evidence: although this patch added `^[A-Za-z]:/` and `/../` checks, a path like `C:../outside/body.md` still passes `validateRepoPath` and `writePrBodyFile` will write outside the repo on Windows because `C:...` is drive-relative, not a normal relative segment. This leaves a traversal gap for `bodyFilePath` despite the new safety validation, so the validator should also reject drive-prefixed paths without a slash (or normalize with `path.resolve` and enforce repo containment).

  Useful? React with 👍 / 👎.
  </pre>

## Fix commits (touching thread paths)

### `7496fd06d9b6d91a2caed438d443d4a89a19a0c5` -- 2026-05-08T07:38:16Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/backlog/pr-publication-plan.ts`

**Message:**

```
feat(backlog): advance B-0280 PR body publication

Co-Authored-By: Codex <noreply@openai.com>
```

### `3378830c6a9467532ce4756aebbb3428b4c2548b` -- 2026-05-08T07:41:28Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/backlog/pr-publication-plan.ts`

**Message:**

```
fix(backlog): restrict PR body file paths

Co-Authored-By: Codex <noreply@openai.com>
```

### `3bc663d04e52f2e269cdada616a5b6ecfa8c382a` -- 2026-05-08T07:43:18Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/backlog/pr-publication-plan.ts`

**Message:**

```
fix(backlog): harden PR body publication path

Co-Authored-By: Codex <noreply@openai.com>
```

### `f444203bfd45d9555a493e01c856dfd207fa44d0` -- 2026-05-08T07:49:59Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/backlog/pr-publication-plan.ts`

**Message:**

```
fix(backlog): reject drive-relative body paths

Co-Authored-By: Codex <noreply@openai.com>
```
