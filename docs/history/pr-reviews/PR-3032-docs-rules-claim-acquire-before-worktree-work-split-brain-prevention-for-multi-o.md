# PR #3032 -- docs(rules): claim acquire before worktree work (split-brain prevention for multi-Otto)

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 3032 |
| Title | docs(rules): claim acquire before worktree work (split-brain prevention for multi-Otto) |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-05-13T21:21:30Z |
| Merged at | 2026-05-13T21:40:35Z |
| Merge commit SHA | `5935e76e482441afe2c5782f3213547cc4f5ff56` |
| Branch | `otto-claim-acquire-before-worktree-work-rule-2026-05-13` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/3032 |
| Changed files | 1 |
| Additions / deletions | +198 / -0 |

## Description

Auto-load rule mandating  before any worktree-creating backlog work. Prevents Otto-CLI vs Otto-Desktop split-brain by requiring atomic claim via the existing B-0400 slice 3 + 5 infrastructure (PRs #2939 + #2959 already shipped). Discipline-level fix today; substrate-level pre-commit hook is future work.

Per Aaron 2026-05-13: "probalby want to figure out how not to split brain with yourself bot any idea?" — this rule is the operationally-honest answer.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | true |
| Total threads | 5 |
| Resolved threads | 5 |
| Unresolved threads | 0 |
| Total review comments | 5 |
| Total fix commits (touching thread paths) | 5 |

## Review threads

### Thread 1 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6B5R6r`
- **Path**: `.claude/rules/claim-acquire-before-worktree-work.md`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-13T21:23:14Z on `.claude/rules/claim-acquire-before-worktree-work.md`:80 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Correct same-agent race example to match `claim.ts` behavior**

  This example states the second `acquire --from otto --item B-0444` exits `1`, but `tools/bus/claim.ts` currently only blocks claims from *other* senders (`activeClaims(itemId).filter((c) =&gt; c.from !== sender)`), so a second process using the same sender (`otto`) can still acquire and exit `0`. In the exact Otto-CLI + Otto-Desktop scenario described here, this guidance gives a false guarantee that split-brain is prevented when it is not, which can lead to duplicate work on the same backlog row.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 2 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6B5VwU`
- **Path**: `.claude/rules/claim-acquire-before-worktree-work.md`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-13T21:28:08Z on `.claude/rules/claim-acquire-before-worktree-work.md`:8 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Remove unconditional split-brain prevention claim**

  The carved sentence says split-brain is prevented when two instances of the same agent share the bus, but this file itself later documents that same-sender races (`--from otto` on both surfaces) still both exit 0 due to `activeClaims(itemId).filter((c) =&gt; c.from !== sender)` in `tools/bus/claim.ts`. In the exact Otto-CLI/Otto-Desktop scenario this rule targets, readers can act on a false safety guarantee and still duplicate work on the same backlog row.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 3 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6B5VwX`
- **Path**: `.claude/rules/claim-acquire-before-worktree-work.md`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-13T21:28:08Z on `.claude/rules/claim-acquire-before-worktree-work.md`:130 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Remove unsupported PID-liveness reclaim guidance**

  This guidance states a claim can be reclaimed after confirming the claimant process is dead, but current claim semantics only expire by TTL or explicit `release`; PID liveness checks in `tools/bus/claim.ts` apply only to the short-lived acquire lockfile, not to active claim envelopes. Following this instruction can cause operators to expect early recovery that never happens, leaving rows blocked until the 24h TTL elapses.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 4 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6B5ZBm`
- **Path**: `.claude/rules/claim-acquire-before-worktree-work.md`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-13T21:32:17Z on `.claude/rules/claim-acquire-before-worktree-work.md`:100 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Remove branch-prefix as split-brain workaround**

  This item is presented as a workaround for the same-sender race, but `claim acquire` does not use `branch` to decide ownership: in `tools/bus/claim.ts`, `acquire` only blocks when `activeClaims(itemId).filter((c) =&gt; c.from !== sender)` is non-empty, and `activeClaims` keys by `(from,itemId)` rather than branch. In the Otto-CLI/Otto-Desktop scenario where both still use `--from otto`, different branch prefixes do not prevent both acquires from succeeding, so this guidance can still lead to duplicate work on the same backlog row.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 5 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6B5bGq`
- **Path**: `.claude/rules/claim-acquire-before-worktree-work.md`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-13T21:34:55Z on `.claude/rules/claim-acquire-before-worktree-work.md`:10 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Remove branch-prefix workaround from carved sentence**

  The carved sentence still tells readers to use "branch-prefix conventions" until sender IDs are extended, but this same rule later correctly states branch prefixes do not participate in `acquire` conflict checks (`claim.ts` filters only by `from`). Because the carved sentence is the top-level guidance most likely to be skimmed at cold boot, this contradiction can cause agents to rely on branch naming as split-brain protection and still double-claim the same backlog row.

  Useful? React with 👍 / 👎.
  </pre>

## Fix commits (touching thread paths)

### `98df00a8b90fcb70175206d95efce1951a30d838` -- 2026-05-13T21:20:52Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `.claude/rules/claim-acquire-before-worktree-work.md`

**Message:**

```
docs(rules): claim acquire before worktree-creating backlog work (spl…

…it-brain prevention for multi-Otto)

Aaron 2026-05-13 set up Otto on Claude Desktop alongside Otto-CLI,
creating a real multi-foreground-surface split-brain risk. Both Ottos
share git + bus on one machine and could pick the same backlog row
simultaneously.

The claim-coordinator (tools/bus/claim.ts, B-0400 slice 3) was built
for this — atomic check/acquire/release with PID-liveness + 24h TTL.
PR #2939 shipped it; PR #2959 added the gate integration. The
infrastructure exists; this rule is the discipline-level mechanization
that auto-loads at cold-boot so both Ottos read it + use it before
worktree-creating backlog work.

Applies to: starting work on a B-NNNN row, creating feature branch +
worktree, opening a backlog-advancing PR.

Does NOT apply to: CI fixes on already-claimed PRs, ad-hoc memory-
file writes, conversation-driven substrate, hot fixes.

Composes with:
- backlog-item-start-gate.md (this rule adds zero-th step)
- dont-ask-permission.md (acquire exit 0 IS the substrate permission)
- never-be-idle.md (if acquire fails, pick another row)
- B-0400 + slice 3 + slice 5 infrastructure
- Otto Claude Desktop bootstream (PR #3030)
- fetch-before-push memory (sibling coordination pattern at git scope)

Future substrate-level mechanization: pre-commit hook that auto-calls
'claim check' and fails on no-held-claim. Today's rule is the
discipline-level fix.

Co-Authored-By: Claude <noreply@anthropic.com>
```

### `d437b9eb2204354454dd511fff4ac74a3877a28f` -- 2026-05-13T21:26:36Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `.claude/rules/claim-acquire-before-worktree-work.md`

**Message:**

```
fix(rules): correct Example 2 — multi-surface Otto can't be distingui…

…shed by same --from value (Vera P1)

Vera caught (PR #3032 review): the original Example 2 showed two Ottos
calling 'acquire --from otto' and the second exiting 1. That's WRONG
per claim.ts line ~270 — same --from is filtered out as self-re-
acquire (idempotent). Both calls succeed; split-brain not prevented.

Fix:
- Example 2 renamed to "KNOWN GAP" — shows actual behavior (both
  succeed) + names the architectural cause (SENDER_IDS doesn't
  distinguish multi-surface instances)
- Added workarounds: lane-based convention, branch-prefix discipline,
  schema extension (otto-cli / otto-desktop)
- New Example 3 shows TARGET behavior with schema fix (--from otto-cli
  vs --from otto-desktop)
- Example 3 renamed to Example 4 (crash recovery)

The architectural gap is a real follow-up: extending SENDER_IDS to
support multi-surface instances of the same logical agent. Future
work, not this PR.

Operational consequence today: use the lane-based + branch-prefix
workarounds. Claim-coordinator is INSUFFICIENT for multi-Otto without
the schema extension.

Co-Authored-By: Claude <noreply@anthropic.com>
```

### `840c30254db62964bf526b43d239866811bd4bdc` -- 2026-05-13T21:30:33Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `.claude/rules/claim-acquire-before-worktree-work.md`

**Message:**

```
docs(rules): fix false split-brain + PID-liveness claims

P1 (PRRT_kwDOSF9kNM6B5VwU): carved sentence claimed split-brain
is  but claim.ts filters by c.from !== sender, soprevented
same-sender instances both exit 0. Updated to state --from must
differ and document the known gap explicitly.

P2 (PRRT_kwDOSF9kNM6B5VwX): example 4 claimed PID-liveness can
reclaim a claim mid- no such mechanism exists. Only TTLTTL
expiry or explicit release ends a claim. Corrected comment.

Co-authored-by: Copilot <223556219+Copilot@users.noreply.github.com>
```

### `030513fb1b2d2ac50a335f6c33640ea7b74b2681` -- 2026-05-13T21:33:25Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `.claude/rules/claim-acquire-before-worktree-work.md`

**Message:**

```
fix(rules): remove branch-prefix as split-brain workaround (Vera P2 c…

…aught it doesn't work)

Vera caught: branch-prefix doesn't prevent split-brain because
claim.ts only filters by 'from', not by 'branch'. Two Ottos with
same --from otto but different --branch both succeed (both exit 0).
The branch field is post-hoc metadata, not a coordination key.

Real workarounds reduce to:
1. Lane-based convention (zero-code; ONLY real prevention today)
2. Schema extension (future SENDER_IDS additions)

Co-Authored-By: Claude <noreply@anthropic.com>
```

### `ce7061af7da7aca6f83968c850b9b36adb905eec` -- 2026-05-13T21:38:39Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `.claude/rules/claim-acquire-before-worktree-work.md`

**Message:**

```
fix(rules): carved sentence consistency — remove branch-prefix from w…

…orkarounds (Vera P2)

The body of the rule was already corrected (commit 030513f) to remove
branch-prefix as a workaround. The carved sentence wasn't updated.
Vera caught the inconsistency.

Now carved sentence + body both say: lane-based convention is the
ONLY real split-brain workaround today.

Co-Authored-By: Claude <noreply@anthropic.com>
```
