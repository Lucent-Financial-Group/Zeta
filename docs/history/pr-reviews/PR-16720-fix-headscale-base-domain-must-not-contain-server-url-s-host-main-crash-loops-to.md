# PR #16720 -- fix(headscale): base_domain must not contain server_url's host — main crash-loops today

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 16720 |
| Title | fix(headscale): base_domain must not contain server_url's host — main crash-loops today |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-09-06T01:04:42Z |
| Merged at | 2026-09-06T01:25:40Z |
| Merge commit SHA | `2ade9af5c29844575e371ab6ad6b99f08bb43d20` |
| Branch | `otto/headscale-base-domain-fix` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/16720 |
| Changed files | 4 |
| Additions / deletions | +117 / -1 |

## Description

## The live defect on `main`

`main` currently ships headscale with `dns.base_domain: zeta.local` while `server_url: https://headscale.zeta.local`. **headscale refuses that config at load and the pod CrashLoopBackOffs** — measured on live run 33995838640:

```
Error: initializing: loading configuration: server_url cannot be part of base_domain
in a way that could make the DERP and headscale server unreachable
```

The control server's own hostname sits inside the tailnet's MagicDNS zone, so it would shadow itself. The upstream chart shipped `example.com`; **I introduced the conflict by changing it to `zeta.local` for tidiness** during the Helm-to-raw-manifests migration — exactly the kind of edit that looks like cleanup and is a config error.

## Why it reached main, recorded as a process fact

The fix and its falsifier were written and pushed to #16696's branch, and **#16696 merged at 22:42:56Z before that push**. So the PR landed the migration and not the correction: the diagnosis existed, on a branch, *after* the merge.

> **A fix pushed to a branch after its PR merges lands nowhere**, and nothing in the tooling said so.

## The fix

`base_domain: tailnet.zeta.local` — a sibling label rather than an ancestor of `headscale.zeta.local`, which is what headscale's check requires.

Plus a falsifier pinning the **predicate** headscale enforces (`host !== base` and `!host.endsWith("." + base)`) rather than the two literal strings, so it still catches the conflict if either value changes later.

## Also corrects an honest-limit note, which matters more than the config change

The ConfigMap said a CrashLoopBackOff should be read as the **key-generation** assumption failing. The pod did crash and it was **not** that — headscale rejected the config before starting, so the key paths were never reached.

> *"The predicted failure happened"* and *"the predicted cause happened"* are different claims, and only the first was true.

That assumption therefore remains **untested**, not confirmed, and the note now says so. The first boot that gets past config validation is the one that finally exercises it.

## Verification

- **Mutation-checked:** restoring `zeta.local` fails the new test
- `bun test src/Core.TypeScript/cluster/` — **1284/1284 pass**

Agency-Signature-Version: 1
Agent: shadow
Agent-Runtime: claude-code/cli
Agent-Model: claude-opus-5[1m]
Credential-Identity: AceHack
Credential-Mode: shared
Human-Review: explicit
Human-Review-Evidence: chat
Action-Mode: human-directed
Task: 081M1T3XFRK087G0R001WQ4XZ7

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_01QyTyVdDqxekK5rZQGBm75f


## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 0 |
| Resolved threads | 0 |
| Unresolved threads | 0 |
| Total review comments | 0 |
| Total fix commits (touching thread paths) | 0 |

## Review threads

(no review threads on this PR)

## Fix commits (touching thread paths)

(no commits in this PR touched files anchored by review threads)
