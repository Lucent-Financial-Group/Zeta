# PR #6388 -- feat(ace): slice 5.2 — semver ranges + version solver (081KR2E4K0008QG0R002YE3MMD)

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 6388 |
| Title | feat(ace): slice 5.2 — semver ranges + version solver (081KR2E4K0008QG0R002YE3MMD) |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-06-01T09:48:26Z |
| Merged at | 2026-06-01T09:55:24Z |
| Merge commit SHA | `a7263f2d26d6b742ca947d34701fa5c073aa6914` |
| Branch | `otto-windows/ace-slice5.2-impl-2026-06-01` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/6388 |
| Changed files | 13 |
| Additions / deletions | +1427 / -42 |

## Description

## Ace slice 5.2 — semver ranges + version solver (081KR2E4K0008QG0R002YE3MMD)

Implements the slice-5.2 spec (#6376): a registry dependency can name a package by **name + semver range**; `ace install` solves the range constraints across the transitive graph to one concrete version per package, then feeds the solved graph into slice-5.1's unchanged verify + atomic-install engine.

### What changed
- **`tools/ace/semver.ts`** (new, pure) — pragmatic semver subset: `parseVersion`, `compareVersions`, `parseRange` (`^ ~ >= <= > < =`, exact, `* / x` wildcard, space-AND), `satisfies`, `maxSatisfying`. `^`/`~` desugaring incl. `^0.x` / `^0.0.x`.
- **`tools/ace/solver.ts`** (new) — deterministic **newest-first backtracking** `solve()`. Classifies edges by **source**: inline = pre-decided (version + url/hash, never registry-routed); registry = range-solved against registry versions. Re-validates already-assigned packages on **every** new constraint; **retracts an abandoned version's contributed constraints on repair** (source-tagged, so a version-dependent transitive dep can't over-constrain unrelated packages).
- **`tools/ace/resolve.ts`** — new `solved: Map<string,string>` param; registry edges resolve their concrete version from the map + a `satisfies` **defense-in-depth re-check** (a solved version violating the edge's range → `unsatisfiable`); concrete version threaded through all bookkeeping; new `unsatisfiable`/`bad-range` reasons. Every slice-5.1 verify step intact.
- **`tools/ace/ace.ts`** — `install` runs solve→resolve (refuses with exit 1 + reason if either fails, store stays empty); `--print-resolution` prints the solved graph.
- **`.claude/skills/ace/SKILL.md`** — range-dep + solver docs.

### Testing (163 tests pass; full-repo strict-tsc clean; markdownlint clean)
- **node-semver differential** (`semver.test.ts`) — our `satisfies`/`maxSatisfying` asserted against the `semver` npm package over a corpus (always runs under bun).
- **Z3 differential** (`solver.z3.test.ts`) — Z3 as a SAT oracle cross-checking the TS solver's ok/unsatisfiable verdicts (per your "use z3 to test our ts"). See caveat below.
- **solver unit** — inline-only/empty-registry back-compat, range→newest, transitive-backtrack re-validation, unsatisfiable, bad-range, mixed inline+registry, determinism, **+ 2 P1-regression tests** (constraint retraction).
- **resolve + ace e2e** — ranged install→newest, unsatisfiable→exit-1-store-empty, inline-only back-compat, `--print-resolution`.

### Review gate caught + fixed before merge
- **P1 (solver correctness)** — final review constructed a runnable false-`unsatisfiable` on version-dependent dep graphs (stale monotone constraints). **Fixed** via source-tagged constraint retraction (`70c476cb2`); both reviewer failing cases are now regression tests.
- **P2** — SKILL.md reason-name drift (`registry-unsatisfiable` → `unsatisfiable`). Fixed.

### Caveats / follow-ups (substrate-honest)
- **z3-solver + Bun:** `z3-solver`'s Emscripten-pthreads WASM doesn't load under Bun 1.3.12; the Z3 differential bridges via a `node -e` subprocess (Node present locally), runs + asserts (no skip). CI doesn't run the bun ace tests (only `lint (tsc tools)` + the .NET `build-and-test`), so the Z3 + node-semver differentials are a **local gate** — same as all prior ace slices. Worth a follow-up if Bun adds pthreads-WASM support (drop the node bridge).
- **Deferred (sliced off):** 081KT07NV0008QG0R002WK9064 advanced semver (`||`/hyphen/pre-release/build-metadata), 081KT07NV0008QG0R000SJ34AK remote registry, 081KT07NV0008QG0R003659TWT single-fetch cache. Slice 5.3 = lockfile.

Spec: #6376. Plan: `docs/agendas/ace-package-manager/2026-06-01-ace-cli-slice5.2-implementation-plan.md`. devDeps `semver@7.8.1` / `@types/semver@7.7.1` / `z3-solver@4.16.0` are test-only, WebSearch-pinned 2026-06-01.

🤖 Generated with [Claude Code](https://claude.com/claude-code)


## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 8 |
| Resolved threads | 0 |
| Unresolved threads | 8 |
| Total review comments | 8 |
| Total fix commits (touching thread paths) | 7 |

## Review threads

### Thread 1 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM6GEuXB`
- **Path**: `tools/ace/solver.z3.test.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-01T09:55:27Z on `tools/ace/solver.z3.test.ts`:172 (association: NONE)

  <pre>
  P0 (lint/build): `spawnSync("node", ...)` will trip `sonarjs/no-os-command-from-path` under the repo’s eslint+sonarjs config unless it’s explicitly suppressed with a rationale (pattern used throughout `tools/**`).
  </pre>

### Thread 2 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM6GEuXa`
- **Path**: `tools/ace/solver.z3.test.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-01T09:55:28Z on `tools/ace/solver.z3.test.ts`:9 (association: NONE)

  <pre>
  P2 (xref drift): this comment references `automated-tests-are-the-shield-assert-dont-skip.md` without its actual repo path, which makes the cross-reference hard to follow. The canonical path elsewhere is `.claude/rules/automated-tests-are-the-shield-assert-dont-skip.md`.
  </pre>

### Thread 3 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM6GEuXq`
- **Path**: `tools/ace/solver.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-01T09:55:28Z on `tools/ace/solver.ts`:106 (association: NONE)

  <pre>
  P0 (robustness): `edge.version` is treated as a string, but dependency edges are untrusted JSON. If a malformed package provides a non-string `version` (e.g. number/null), `parseRange(edge.version)` will throw and crash the solver instead of returning `invalid-package`/`bad-range`. Add an explicit runtime guard before using `edge.version`.
  </pre>

### Thread 4 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM6GEuX4`
- **Path**: `tools/ace/solver.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-01T09:55:28Z on `tools/ace/solver.ts`:79 (association: NONE)

  <pre>
  P2: `retractSource` is documented as “Never call with \"root\"”, but `ingest()` currently calls `retractSource(source)` unconditionally (including when `source` is `"root"`). Either update the comment or skip retraction for the root seed so the implementation matches the documented invariant.
  </pre>

### Thread 5 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM6GEuYG`
- **Path**: `tools/ace/resolve.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-01T09:55:29Z on `tools/ace/resolve.ts`:66 (association: NONE)

  <pre>
  P0 (robustness): dependency edges are untrusted JSON, but `edge.version` is used as if it’s always a string (and is passed into `satisfies`). If a malformed package provides a non-string `version`, this can throw and crash resolve instead of returning `invalid-package`. Add a runtime type guard for `edge.version` alongside the existing kind/url/hash guards.
  </pre>

### Thread 6 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM6GEuYP`
- **Path**: `tools/ace/resolve.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-01T09:55:29Z on `tools/ace/resolve.ts`:28 (association: NONE)

  <pre>
  P1 (maintainability): `ResolveReason` now includes `"bad-range"`, but `resolve()` never returns it (the only new range-related refusal uses `"unsatisfiable"`). This makes the public-ish reason union misleading and can drift docs/tests. Either implement a `bad-range` path in `resolve()` or drop it from the union here.
  </pre>

### Thread 7 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM6GEuYb`
- **Path**: `tools/ace/semver.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-01T09:55:29Z on `tools/ace/semver.ts`:47 (association: NONE)

  <pre>
  P2: This comment is stale — `parseComparatorToken` already handles `^`/`~` now, so referencing “Task 2 extends this” is misleading when reading the file in isolation.
  </pre>

### Thread 8 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM6GEuYy`
- **Path**: `tools/ace/ace.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-01T09:55:30Z on `tools/ace/ace.ts`:502 (association: NONE)

  <pre>
  P2 (consistency/perf): `loadRegistry()` is called twice during `install` (once for `solve`, once for `resolve`). If the registry changes between calls (or if load is expensive), solve/resolve can observe different registries. Load it once and reuse the same `registry` instance for both phases.
  </pre>

## Fix commits (touching thread paths)

### `436adb2bb718eeb16e72dbeb265dbc09926849a5` -- 2026-06-01T08:57:50Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/ace/semver.ts`

**Message:**

```
feat(ace): semver subset — versions + exact/comparator/wildcard range…

…s + satisfies (slice 5.2 task 1)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `ced7760d95c95de548db2de9172b34336294ca2a` -- 2026-06-01T09:01:35Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/ace/semver.ts`

**Message:**

```
feat(ace): semver ^/~ desugar + AND ranges + maxSatisfying; node-semv…

…er differential (slice 5.2 task 2)

semver@7.8.1 + @types/semver@7.7.1 test-only devDeps (WebSearch-pinned 2026-06-01,
npmjs.com/package/semver + /@types/semver) per dep-pin-search-first-authority.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `3321e1cc2e0af1982ba79635661d675f5217a8df` -- 2026-06-01T09:05:56Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/ace/solver.ts`

**Message:**

```
feat(ace): version solver — source-classified newest-first backtracki…

…ng solve() (slice 5.2 task 3)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `e57c9a1a6974b998563ac4f8ca9328c547ab6f05` -- 2026-06-01T09:18:02Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/ace/solver.z3.test.ts`

**Message:**

```
test(ace): Z3 differential — SAT-oracle cross-check of the TS solver …

…(slice 5.2 task 4)

z3-solver@4.16.0 test-only devDep (WebSearch-pinned 2026-06-01, npmjs.com/package/z3-solver)
per dep-pin-search-first-authority; WASM build runs+asserts in CI (no skip).

Bun 1.3.12 incompatibility note (documented in test file, no skip):
z3-solver uses Emscripten pthread WASM which crashes in Bun 1.3.12 worker threads
("Aborted(Assertion failed)" at z3-built.js:848 removeRunDependency→assert).
Resolution: test file runs under Bun; Z3 is invoked via spawnSync("node",...) subprocess.
Z3 actually executes and returns sat/unsat — NOT a graceful skip per the shield rule.
Test FAILS if Node is unavailable or Z3 crashes.

3 corpus cases (version-independent-dep graphs, faithful to static SMT encoding):
  1. SAT diamond: root→A^1.0.0 + B^1.0.0, both leaves → sat ∧ solve ok
  2. UNSAT shared-dep: A needs C>=2.0.0, B needs C<2.0.0, no valid C → unsat ∧ solve fail
  3. SAT with real pick: root→A>=1.2.0, A{1.0.0,1.2.0,1.5.0} → sat ∧ solve picks >=1.2.0

Each case: solve() ok ⟺ Z3 sat; ok cases: every returned version satisfies its range.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `0e3671427c9d81840c3977af4fc4763f49d4ead0` -- 2026-06-01T09:28:12Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/ace/ace.ts`
- `tools/ace/resolve.ts`

**Message:**

```
feat(ace): resolver solved-map param + satisfies defense-in-depth re-…

…check (slice 5.2 task 5)

Wire solver output into resolve(): add `solved: Map<string, string>` param (after
`registry`, before `opts`). Registry edges now use the concrete solved version
instead of treating edge.version as an exact key. satisfies() defense-in-depth check
rejects a solved version that doesn't satisfy the declared range. Introduce
`effectiveVersion` variable so all downstream bookkeeping (version-skew, tamper,
byName, declared-identity check) uses the concrete version, not the range string.
Adds "unsatisfiable" and "bad-range" to ResolveReason. Updates ace.ts install path
to call solve() first and pass solveResult.versions to resolve().

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `382e61e12086745efd706683dda728ad27a9f5f2` -- 2026-06-01T09:38:37Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/ace/ace.ts`

**Message:**

```
feat(ace): --print-resolution + semver-range e2e tests + SKILL docs (…

…slice 5.2 task 6)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `70c476cb2d4555c5eaa303c6e8471936d7a8469f` -- 2026-06-01T09:47:16Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/ace/solver.ts`

**Message:**

```
fix(ace): retract abandoned-version constraints on backtrack — no fal…

…se unsatisfiable (P1 final-review #slice5.2)

Constraints are now source-tagged by contributor (pkg@version / root / inline:url); when a
registry assignment is repaired, the abandoned version's contributed constraints are retracted
so a version-dependent transitive dep no longer over-constrains unrelated packages into a false
unsatisfiable. Two regression tests (registry-availability + peer-conflict variants). Also fixes
SKILL.md reason-name drift (registry-unsatisfiable -> unsatisfiable).

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```
