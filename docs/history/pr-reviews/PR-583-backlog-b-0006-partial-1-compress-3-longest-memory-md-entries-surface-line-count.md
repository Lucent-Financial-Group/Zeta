# PR #583 -- backlog(B-0006 partial-1): compress 3 longest MEMORY.md entries + surface line-count criterion calibration

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 583 |
| Title | backlog(B-0006 partial-1): compress 3 longest MEMORY.md entries + surface line-count criterion calibration |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-04-26T10:19:23Z |
| Merged at | 2026-04-26T10:21:50Z |
| Merge commit SHA | `f1e669454445e58aeac632207fa0751a7f3c73c0` |
| Branch | `backlog/B-0006-partial-1-memory-md-compression` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/583 |
| Changed files | 1 |
| Additions / deletions | +3 / -3 |

## Description

## Summary

- Compresses the 3 longest entries in `memory/MEMORY.md` to <200 chars each per B-0006 (P1 BACKLOG, MEMORY.md compression pass)
- Soul-file (4533 → 191 chars), UI-DSL calling-convention (3287 → 183), UI-DSL class-vs-instance (3674 → 195)
- Body files retain all detail (Otto-238 retractability)
- Net char savings: ~10KB

## Calibration tension surfaced

B-0006 has two acceptance criteria that are structurally inconsistent for current file shape:

| Criterion | Status |
|-----------|--------|
| Each entry one line under ~200 chars | **Achievable per-entry** (this PR) |
| ≤ 200 lines (README cap) | **NOT achievable** by compression — file has 591+ entries already one-line each; meeting the cap requires DELETING entries |

Three resolution paths in commit message:

- **(a)** Drop line-count criterion — char-cap is the actual harness constraint (harness truncation is byte-based per session-start warning *"MEMORY.md is 454 lines and 307KB. Only part of it was loaded"*)
- **(b)** Add entry-prune step — walk entries, identify superseded ones, delete
- **(c)** Split into priority sub-indexes (P0/P1/P2/P3 like docs/backlog/)

Recommend (a). Awaiting your call.

## Why partial-1 not full-pass

Per `feedback_substrate_changes_require_scenario_thinking_upfront_not_patch_as_it_breaks_2026_04_26.md`, did not commit to full ~411 entries (~5 hr work) without:
- Validating the compression shape on a small sample first
- Surfacing the criterion-inconsistency calibration
- Confirming Aaron's preferred resolution path

Partial-1 demonstrates the pattern; partial-N+ scale once you validate the shape.

## Test plan

- [x] `wc -l` confirms still 597 lines (line count unchanged — that's the calibration finding)
- [x] `wc -c` confirms ~10KB saved (405474 → ~395KB after this PR)
- [x] Each compressed entry under 200 chars (191/183/195)
- [x] Body files exist + canonical (verified via `ls memory/<filename>` for all 3)

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 1 |
| Resolved threads | 0 |
| Unresolved threads | 1 |
| Total review comments | 1 |
| Total fix commits (touching thread paths) | 1 |

## Review threads

### Thread 1 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM59pmaQ`
- **Path**: `memory/MEMORY.md`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-26T10:22:42Z on `memory/MEMORY.md`:231 (association: NONE)

  <pre>
  P1: These compressed index entries are at lines &gt;200 in MEMORY.md. Per memory/README.md, the harness truncates the index after ~200 lines, so anything below that cutoff won’t be loaded at session start. If the goal is to improve wake-up context (not just human readability), consider a follow-up that keeps memory/MEMORY.md ≤200 lines (prune/split into multiple indexes and keep a short top-level index pointing at them).
  </pre>

## Fix commits (touching thread paths)

### `10fec66cadd93cac04395d27a36b46d42982451f` -- 2026-04-26T10:19:06Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `memory/MEMORY.md`

**Message:**

```
backlog(B-0006 partial-1): compress 3 longest MEMORY.md entries to <2…

…00 chars each

Per drain-chronologically-oldest-first + scenario-thinking-upfront discipline,
this is a bounded partial-1 demonstration of the B-0006 compression shape
before scaling. Compressed three entries (longest in the file: 4533/3287/3674
chars → 191/183/195 chars each):

- Soul-file (line 272 → ~191 chars) — body file canonical at user_git_repo_is_factory_soul_file_*
- UI-DSL calling-convention (line 230 → ~183 chars) — body file canonical
- UI-DSL class vs instance (line 231 → ~195 chars) — body file canonical

Net char savings: ~10KB. Body files retain the load-bearing detail; index
entries now serve their hook+pointer role per B-0006 §"What 'compression
pass' means here" (1. load-bearing claim, 2. body file canonical, 3.
one-liner < 200 chars).

## Calibration finding to surface

B-0006 acceptance criteria include both:
- "Each entry one line under ~200 chars" (achievable per-entry; the work
  this PR demonstrates)
- "≤ 200 lines (matching README cap)" (NOT achievable by per-entry
  compression — file has 591+ entries each already on one line; meeting
  the line cap requires DELETING entries, not compressing them)

The two criteria are structurally inconsistent for the current file shape.
Three resolution paths (per Otto-275 log-but-don't-implement; deferred to
maintainer for resolution path choice):

(a) **Drop the line-count criterion.** Total CHAR count is the load-bearing
    metric (harness truncation is byte-based, not line-based per the
    "MEMORY.md is 454 lines and 307KB. Only part of it was loaded"
    session-start warning). Per-entry char-cap suffices.

(b) **Add an entry-prune step.** Walk entries identifying superseded /
    consolidated / no-longer-canonical ones; delete those. Substantive
    judgment per Otto-244 careful-and-serialized.

(c) **Split into multiple index files.** P0/P1/P2/P3 priority sub-indexes
    (like docs/backlog/P*/) — each under cap. Major restructure;
    high blast radius.

Recommendation: **path (a)** — char-cap is the actual harness constraint.
Awaiting maintainer call.

Composes with:
- B-0006 P1 BACKLOG row
- Otto-238 retractability (body files retain detail)
- feedback_substrate_changes_require_scenario_thinking_upfront_*.md (scenarios surfaced before scaling)

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
```
