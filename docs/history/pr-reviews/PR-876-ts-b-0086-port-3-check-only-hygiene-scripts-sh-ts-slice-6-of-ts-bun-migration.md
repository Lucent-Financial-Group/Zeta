# PR #876 -- ts(B-0086): port 3 check-only hygiene scripts (.sh→.ts) — slice 6 of TS/Bun migration

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 876 |
| Title | ts(B-0086): port 3 check-only hygiene scripts (.sh→.ts) — slice 6 of TS/Bun migration |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-04-30T02:36:03Z |
| Merged at | 2026-04-30T02:38:39Z |
| Merge commit SHA | `02baabcb1ca424a019e4e8ca4e549719f226c98d` |
| Branch | `lane-b/ts-bun-slice-6-check-scripts-2026-04-30` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/876 |
| Changed files | 4 |
| Additions / deletions | +561 / -0 |

## Description

## Summary

Slice 6 of the TS/Bun migration. Cluster F (3 of 4 check-only hygiene scripts):

- \`check-no-conflict-markers.{sh→ts}\` — git merge-conflict marker detector
- \`check-archive-header-section33.{sh→ts}\` — GOVERNANCE.md §33 archive header validator
- \`check-tick-history-order.{sh→ts}\` — chronological order check on tick-history rows

**Slice scope adjustment**: Cluster F was 4 files; \`append-tick-history-row.sh\` is a write-side script (file mutation) and warrants its own careful equivalence-test setup with file-state preservation. Deferred to slice 7+.

Per Gate B prerequisite (verified currency of layered baseline 1 day old).

## Equivalence

Per-file bash-vs-TS diff against current repo state:
- \`check-no-conflict-markers\`: byte-equivalent (no markers found).
- \`check-archive-header-section33\`: byte-equivalent (all docs have headers).
- \`check-tick-history-order\`: byte-equivalent (rows in non-decreasing order).

## Test plan

- [x] \`bun x tsc --noEmit\` clean
- [x] \`bun x eslint <slice files>\` clean
- [x] Bash-vs-TS equivalence per file
- [x] markdownlint clean
- [x] Slice-6 audit landed

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 7 |
| Resolved threads | 0 |
| Unresolved threads | 7 |
| Total review comments | 7 |
| Total fix commits (touching thread paths) | 2 |

## Review threads

### Thread 1 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM5-npVs`
- **Path**: `docs/trajectories/typescript-bun-migration/slice-audits.md`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-30T02:41:07Z on `docs/trajectories/typescript-bun-migration/slice-audits.md`:369 (association: NONE)

  <pre>
  This slice-audit entry still has a placeholder PR reference (“PR #NNN”). Replace this with the actual PR number before merging so the trajectory log stays navigable.
  ```suggestion
  ## Slice 6 — 3 check-only hygiene ports (2026-04-30)
  ```
  </pre>

### Thread 2 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM5-npWA`
- **Path**: `tools/hygiene/check-tick-history-order.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-30T02:41:08Z on `tools/hygiene/check-tick-history-order.ts`:152 (association: NONE)

  <pre>
  This script’s remediation text includes a specific person’s name (e.g., “authorized (Aaron 2026-04-26: …)” and “pattern Aaron caught”). Repo convention is to avoid named attribution in code/docs/skills outside the explicitly enumerated history surfaces; use role-refs (e.g., “the human maintainer”) or move the attribution/quotes to a history surface and keep this message structural.
  ```suggestion
      process.stderr.write("  - For HISTORICAL disorder: Otto-229 one-case override\n");
      process.stderr.write("    permits re-ordering rows physically because git\n");
      process.stderr.write("    preserves the prior state.\n");
      process.stderr.write("  - Do NOT add an opt-in flag to suppress these violations.\n");
      process.stderr.write(
        "    That is the Otto-341 self-deception pattern.\n",
  ```
  </pre>

### Thread 3 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM5-npWN`
- **Path**: `tools/hygiene/check-tick-history-order.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-30T02:41:08Z on `tools/hygiene/check-tick-history-order.ts`:148 (association: NONE)

  <pre>
  Typo in the quoted text: "permanat" should be "permanent".
  ```suggestion
      process.stderr.write("    keep us honest so no risk of permanent loss'). Re-order\n");
  ```
  </pre>

### Thread 4 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM5-npWU`
- **Path**: `tools/hygiene/check-tick-history-order.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-30T02:41:08Z on `tools/hygiene/check-tick-history-order.ts`:12 (association: NONE)

  <pre>
  Header comment says “First violation reported with surrounding context,” but the implementation reports context for every out-of-order pair (it iterates all violations). Either update the comment to match behavior, or change the loop to stop after the first violation if that’s the intended contract.
  ```suggestion
  //   - Each violation is reported with surrounding context.
  ```
  </pre>

### Thread 5 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM5-npWc`
- **Path**: `tools/hygiene/check-archive-header-section33.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-30T02:41:09Z on `tools/hygiene/check-archive-header-section33.ts`:95 (association: NONE)

  <pre>
  `readHead()` reads the entire file and then slices to 20 lines, which is a performance regression vs the bash original’s `head -20` (especially if `docs/research/**` contains large courier-ferry archives). Consider reading only an initial chunk / streaming until N lines are collected so the check’s cost stays proportional to the header region.
  </pre>

### Thread 6 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM5-npWh`
- **Path**: `tools/hygiene/check-archive-header-section33.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-30T02:41:09Z on `tools/hygiene/check-archive-header-section33.ts`:140 (association: NONE)

  <pre>
  `relativize()` assumes POSIX separators by building a `${root}/` prefix and doing `startsWith`. On Windows, `join()` will produce `\` separators and `git rev-parse --show-toplevel` may differ, so this can fail to relativize correctly. Prefer `path.relative(root, file)` (and/or `path.resolve` normalization) to make output stable cross-platform.
  </pre>

### Thread 7 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM5-npWs`
- **Path**: `tools/hygiene/check-no-conflict-markers.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-30T02:41:09Z on `tools/hygiene/check-no-conflict-markers.ts`:54 (association: NONE)

  <pre>
  `classifyFailure()` doesn’t include the signal case when `status === null` (it always reports “terminated before reporting an exit code”), which loses useful diagnostics and diverges from the repo’s standard spawn failure classifier. Handle `result.signal !== null` explicitly so CI logs can distinguish signal kills from other abnormal termination.
  ```suggestion
    if (result.status === null) {
      if (result.signal !== null) {
        return `'${cmd} ${args.join(" ")}' terminated by signal ${result.signal}`;
      }
  ```
  </pre>

## Fix commits (touching thread paths)

### `465daab2185caaa74d5c4cc08c08746abdf6aa12` -- 2026-04-30T02:32:24Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/hygiene/check-no-conflict-markers.ts`

**Message:**

```
ts(slice-6, wip 1/3): port check-no-conflict-markers (.sh→.ts)

First port of slice 6 (Cluster F check-only scripts). Bash 119
lines → TS port.

Scope adjustment for slice 6: append-tick-history-row.sh deferred
to slice 7+ (write-side script, file mutation; warrants its own
careful equivalence-test setup with file-state preservation). Slice
6 narrows to 3 read-only check-* scripts.

Patterns reused (canonical):
- Typed boundary objects (Violation).
- Literal-type union exit codes (0 | 1).
- Structured spawn-failure classifier.
- Self-allowlist (this script's bash + ts paths excluded from scan).

Implementation notes:
- ALLOWLIST extended to include the .ts file (so the audit doesn't
  flag itself when finding markers in the regex constants).
- MARKER_RE uses `[\t ]` rather than POSIX `[[:space:]]` (Node regex
  doesn't support POSIX classes). The match outcome is identical for
  the two relevant whitespace bytes.
- repoRoot() falls back to process.cwd() if git fails (mirrors bash
  `git rev-parse --show-toplevel || pwd`).

Equivalence: bash-vs-TS output diff is empty against current repo
state (no conflict markers).

Slice 6 status: 1/3 ports landed. check-archive-header-section33 +
check-tick-history-order follow.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
```

### `687b8ce3b7e6b3770ffd94f4782ad46f935a5eac` -- 2026-04-30T02:36:00Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `docs/trajectories/typescript-bun-migration/slice-audits.md`
- `tools/hygiene/check-archive-header-section33.ts`
- `tools/hygiene/check-tick-history-order.ts`

**Message:**

```
ts(slice-6): port check-archive-header-section33 + check-tick-history…

…-order + slice-6 audit

Ports 2 and 3 of slice 6 (Cluster F check-only scripts).

check-archive-header-section33:
- Walks docs/research/**.md recursively (Bun-native readdirSync).
- Detects courier-ferry imports via filename or first-20-line content
  hint regexes.
- Validates 4 §33 labels at line-start + enum-strict Operational
  status: value (research-grade | operational + trailing whitespace).

check-tick-history-order:
- Extracts rows matching `| YYYY-MM-DDTHH:MM:SSZ` from tick-history.
- ISO-8601 lex-sortable string comparison for chronological order.
- Reports first violation with surrounding context (200-char tail).

Patterns reused (canonical):
- Typed boundary objects (Violation, Row).
- Literal-type union exit codes (0 | 1 | 2).
- Self-allowlist where applicable.
- repoRoot fallback to process.cwd() (mirrors bash `git rev-parse ||
  pwd`).

Equivalence: bash-vs-TS output diff is empty per port against
current repo state.

Slice-6 audit substrate landed in slice-audits.md.

Slice 6 ready for review.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
```
