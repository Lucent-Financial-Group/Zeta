# PR #9214 -- feat(work-items): G-Set event log slice 1 — WorkItemCreated (081KSXN)

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 9214 |
| Title | feat(work-items): G-Set event log slice 1 — WorkItemCreated (081KSXN) |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-07-02T23:24:49Z |
| Merged at | 2026-07-03T01:05:26Z |
| Merge commit SHA | `b478df39e804086cf26a63599388cfc81c669c2c` |
| Branch | `feat/081KSXN-workitem-event-gset-slice-1` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/9214 |
| Changed files | 6 |
| Additions / deletions | +335 / -5 |

## Description

## Summary
- Add `src/Core.TypeScript/work-items/` — append-only JSON event G-Set under `workitems/events/` (disjoint ZetaId files, wx-create idempotency, same discipline as agent-bus)
- Define `WorkItemCreated` / `state-changed` / `closed` event shapes (slice 1 implements `created` only)
- `new-workitem.ts` now emits a `WorkItemCreated` fact alongside the markdown file

## Context
First slice of **081KSXN940008QG0R002FWR9B2** (work-item event G-Set / DORA substrate). Git direct-to-main publish deferred to a follow-up slice.

## Test plan
- [x] `bun test src/Core.TypeScript/work-items/work-items.test.ts`
- [x] `bun test src/Core.TypeScript/backlog/new-workitem.test.ts`
- [ ] Gate green


Made with [Cursor](https://cursor.com)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 2 |
| Resolved threads | 2 |
| Unresolved threads | 0 |
| Total review comments | 2 |
| Total fix commits (touching thread paths) | 2 |

## Review threads

### Thread 1 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6ODHCQ`
- **Path**: `src/Core.TypeScript/backlog/new-workitem.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-07-02T23:28:26Z on `src/Core.TypeScript/backlog/new-workitem.ts`:248 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Honor --dir when publishing created events**

  When the documented `--dir` option is used to mint into a scratch or non-default work-item tree, this still publishes the event with no `eventsRoot`, so `writeEvent` falls back to `workitems/events` in the current repo. That splits the markdown projection from its `WorkItemCreated` fact and makes hermetic CLI/test invocations unexpectedly mutate the default repo event store; derive the event root from `dir` or expose a matching CLI flag before publishing.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 2 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6ODHCS`
- **Path**: `src/Core.TypeScript/backlog/new-workitem.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-07-02T23:28:26Z on `src/Core.TypeScript/backlog/new-workitem.ts`:175 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Use the injected env when minting event ids**

  When a caller passes a deterministic `WorkItemEnv` to `publishCreatedEvent`, the timestamp comes from that env but the event id still uses `makeCreatedEvent`'s default `DEFAULT_ENV` randomness because no mint function is supplied here. Replaying the same `(minted, spec, env)` therefore writes different event paths, which breaks the deterministic-simulation/idempotent G-Set behavior this API is shaped to support; pass a mint function that uses `env.nextInt64()` for the event id.

  Useful? React with 👍 / 👎.
  </pre>

## Fix commits (touching thread paths)

### `6e293e5ee3259b6ec560dd66724ab740216d5b9d` -- 2026-07-02T23:24:41Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `src/Core.TypeScript/backlog/new-workitem.ts`

**Message:**

```
feat(work-items): G-Set event log slice 1 — WorkItemCreated on mint (…

…081KSXN)

Add workitems/events/ append-only JSON G-Set (mirrors agent-bus write discipline)
and wire new-workitem.ts to emit a created fact alongside the markdown file.

Co-authored-by: Cursor <cursoragent@cursor.com>
```

### `21e81fcbff838ac02005bfba48eef32beeb2ca9b` -- 2026-07-03T01:04:51Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `src/Core.TypeScript/backlog/new-workitem.ts`

**Message:**

```
fix(work-items): address review — co-locate events with --dir, DST ev…

…ent ids

Honor --dir for events root; mint event ids from injected WorkItemEnv.

Co-authored-by: Cursor <cursoragent@cursor.com>
```
