# PR #6278 -- fix(shield): authenticate mise in the windows-ps1 install shield (MISE_GITHUB_TOKEN build-arg)

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 6278 |
| Title | fix(shield): authenticate mise in the windows-ps1 install shield (MISE_GITHUB_TOKEN build-arg) |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-05-31T23:31:08Z |
| Merged at | 2026-05-31T23:37:59Z |
| Merge commit SHA | `864f976f726bfb3b7b577363b8f741af65c26eb5` |
| Branch | `otto-windows/shield-windows-ps1-mise-token-2026-05-31` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/6278 |
| Changed files | 3 |
| Additions / deletions | +78 / -6 |

## Description

## What

Authenticates `mise` inside the **windows-ps1 install shield** (`docker-windows-install-ps1-test`) so it stops hitting the GitHub API rate-limit. This is the sibling of the 4 Unix shields fixed in #6273 — the windows-ps1 follow-up that #6273's body flagged as deferred ("Windows container secrets differ + install.ps1's token-handling needs verifying first"). That verification is done; this lands the fix.

## Why it 403s

The shield runs `install.ps1` → `mise install`, which calls the GitHub API to resolve `.mise.toml` runtimes from GitHub releases. **Unauthenticated** that API is 60 req/hr; a cold Server-Core build exhausts it and mise gets HTTP 403 (the exact failure the macOS shield hit). **Authenticated** it's 5000 req/hr.

## Why the Windows delivery differs from the Unix shields

| Concern | Unix shields (#6273) | Windows shield (this PR) |
|---|---|---|
| Token var | `GITHUB_TOKEN` (Unix `mise.sh` auto-promotes → `MISE_GITHUB_TOKEN`) | **`MISE_GITHUB_TOKEN` set directly** — `install.ps1` has **no** promotion logic, so we set mise's highest-precedence var (`MISE_GITHUB_TOKEN > GITHUB_API_TOKEN > GITHUB_TOKEN`) |
| Build-time channel | BuildKit `--mount=type=secret` (token never enters layers) | **`--build-arg`** — the `windows-2025` runner uses the **legacy docker builder** (no BuildKit secret mounts; documented in the driver's NOTE). ARG is the only build-time channel; it reaches the install RUN as `$env:MISE_GITHUB_TOKEN`, which mise inherits |
| `ZETA_INSTALL_FULL` | set (exercises the Unix one-liner-tools registry) | **not set** — that registry is Unix-only; `install.ps1` doesn't use it |

## Leak mitigations for the `--build-arg`

A build-arg secret has two leak surfaces; both are mitigated:

1. **`docker history`** (where ARG values surface) — the driver already `docker rmi -f`s the image in `cleanup()`, so the history is discarded in the **same job, never pushed**. The token is the **per-run ephemeral `GITHUB_TOKEN`** (dead the moment the job ends).
2. **The driver logs the build command** — the value is **redacted to `***`** before logging (GHA's auto-mask of `GITHUB_TOKEN` is the backup, not the primary defense).

It's declared `ARG` (not `ENV`), so ARG scope keeps it out of image layers + `docker inspect Config.Env`. Unset `GITHUB_TOKEN` → mise stays unauthenticated (prior behavior).

## Files

- `tools/ci/dockerfiles/windows-install-ps1-test/Dockerfile` — `ARG MISE_GITHUB_TOKEN` before the `install.ps1` RUN
- `tools/ci/docker-windows-install-ps1-test.ts` — `--build-arg MISE_GITHUB_TOKEN` from `process.env.GITHUB_TOKEN`; value redacted in the logged command
- `.github/workflows/docker-windows-install-ps1-test.yml` — forward `GITHUB_TOKEN` to the driver step

## Validation

- `tsc` clean on the driver
- `actionlint` exit 0 on the workflow
- The shield's paths-filter matches all three changed files, so **this PR self-tests the fix** — a green ~15-min Windows-container build means mise no longer 403s.

## Follow-up (not bundled)

Optional parity item: `install.ps1` could gain a `GITHUB_TOKEN → MISE_GITHUB_TOKEN` promotion (mirroring Unix `mise.sh`) so Windows *devs* who only set `GITHUB_TOKEN` get auto-auth too. Kept out of this PR to keep it scoped to the shield (the literal ask); it touches the production install path so it's a separate, reviewable change.

🤖 Generated with [Claude Code](https://claude.com/claude-code)


## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | true |
| Total threads | 2 |
| Resolved threads | 2 |
| Unresolved threads | 0 |
| Total review comments | 4 |
| Total fix commits (touching thread paths) | 2 |

## Review threads

### Thread 1 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F-twy`
- **Path**: `tools/ci/docker-windows-install-ps1-test.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-31T23:33:04Z on `tools/ci/docker-windows-install-ps1-test.ts`:111 (association: NONE)

  <pre>
  P1 security: this forwards the token even when the driver is invoked with `--keep-image`, which bypasses the stated cleanup mitigation and leaves the build-arg value recoverable from the retained image history. Reject `--keep-image` when `GITHUB_TOKEN` is set, or intentionally omit the token for keep-image runs.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-31T23:35:06Z on `tools/ci/docker-windows-install-ps1-test.ts`:111 (association: MEMBER)

    <pre>
    Fixed in d1fbf42b3: MISE_GITHUB_TOKEN is now OMITTED when `--keep-image` is set (threaded `keepImage` into `runBuild`), so a retained image's `docker history` never carries the token; a warning notes mise then runs unauthenticated for that run. CI never passes --keep-image, so the shield path is unaffected.
    </pre>

### Thread 2 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F-tw3`
- **Path**: `tools/ci/docker-windows-install-ps1-test.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-31T23:33:04Z on `tools/ci/docker-windows-install-ps1-test.ts`:88 (association: NONE)

  <pre>
  P2 documentation: this runner reference now says `windows-2025`, but the file header still says the script requires the `windows-2022` GitHub runner. Update the remaining reference so the driver docs do not contradict the workflow/Dockerfile pins.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-31T23:35:08Z on `tools/ci/docker-windows-install-ps1-test.ts`:88 (association: MEMBER)

    <pre>
    Fixed in d1fbf42b3: corrected the header's lone stale `windows-2022` reference to `windows-2025` (matching the runBuild NOTE + workflow runs-on + Dockerfile pins).
    </pre>

## Fix commits (touching thread paths)

### `ab57d5cf749c0513e2fd87a10542f2b8c35f2d60` -- 2026-05-31T23:30:46Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/ci/docker-windows-install-ps1-test.ts`

**Message:**

```
fix(shield): authenticate mise in the windows-ps1 install shield via …

…MISE_GITHUB_TOKEN build-arg

The docker-windows-install-ps1-test shield runs install.ps1 -> `mise install`, which calls
the GitHub API to resolve .mise.toml runtimes from GitHub releases. UNAUTHENTICATED that API
is 60 req/hr; a cold Server-Core build exhausts it and mise gets HTTP 403 — the same failure
class the Unix macOS shield hit (fixed for the 4 Unix shields in #6273). This is the windows-ps1
sibling fix #6273 flagged as deferred.

Token delivery differs from the Unix shields by necessity:
- WHY MISE_GITHUB_TOKEN (not GITHUB_TOKEN): mise auth precedence is MISE_GITHUB_TOKEN >
  GITHUB_API_TOKEN > GITHUB_TOKEN. Unix mise.sh auto-promotes GITHUB_TOKEN -> MISE_GITHUB_TOKEN;
  install.ps1 has NO such promotion, so the shield sets the highest-precedence var directly.
- WHY --build-arg (not BuildKit --mount=type=secret like the Unix shields): the windows-2025
  runner uses the LEGACY docker builder (documented in the driver), which has no BuildKit secret
  mounts. ARG is the only build-time channel; declared in the Dockerfile it reaches the install
  RUN as the $env:MISE_GITHUB_TOKEN build env var that mise (a child of the RUN) inherits. It is
  NOT promoted to a persistent ENV — ARG scope keeps it build-only (out of image layers + docker
  inspect Config.Env).

Leak mitigations for the build-arg:
- the value is the per-run ephemeral GitHub Actions GITHUB_TOKEN (dead the moment the job ends);
- the driver `docker rmi -f`s the image in cleanup() -> docker history (where ARG values surface)
  is discarded in the same job, never pushed;
- the driver REDACTS the value to *** in the build command it logs; GHA auto-masks GITHUB_TOKEN
  as backup.

ZETA_INSTALL_FULL is NOT set here (that var gates the Unix one-liner-tools registry, which
install.ps1 does not use). Unset GITHUB_TOKEN -> mise stays unauthenticated (prior behavior).

Files:
- tools/ci/dockerfiles/windows-install-ps1-test/Dockerfile — ARG MISE_GITHUB_TOKEN before the
  install.ps1 RUN
- tools/ci/docker-windows-install-ps1-test.ts — --build-arg MISE_GITHUB_TOKEN from
  process.env.GITHUB_TOKEN; value redacted in the logged command
- .github/workflows/docker-windows-install-ps1-test.yml — forward GITHUB_TOKEN to the driver step

Validation: tsc clean on the driver; actionlint exit 0 on the workflow. The shield's paths-filter
matches all three files, so this PR self-tests the fix (a green build = mise no longer 403s).

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `d1fbf42b3f9029c60ea9b9afe1419787111c4870` -- 2026-05-31T23:34:52Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/ci/docker-windows-install-ps1-test.ts`

**Message:**

```
fix(shield): address Copilot review on #6278 — omit token on --keep-i…

…mage + correct stale runner ref

Two Copilot findings, both verified valid against source:

- P1 security: the build-arg leak mitigation relied on cleanup()'s `docker rmi -f` discarding the
  image (+ its history, where ARG values surface). But cleanup() SKIPS the rmi on --keep-image, so a
  retained image would persist the token in its history. Fix: OMIT MISE_GITHUB_TOKEN entirely when
  --keep-image is set (threaded keepImage into runBuild) + log a warning that mise runs unauthenticated
  for that run. A retained image now never carries the token. CI never passes --keep-image, so the
  shield is unaffected; this closes the local-debug leak path.

- P2 docs: the header still said the script requires the windows-2022 runner while the runBuild NOTE
  + workflow `runs-on` + Dockerfile pins all say windows-2025. Corrected the lone stale reference.

Validation: tsc clean on the driver.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```
