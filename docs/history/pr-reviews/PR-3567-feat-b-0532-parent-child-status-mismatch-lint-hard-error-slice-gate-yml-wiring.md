# PR #3567 -- feat(B-0532): parent-child status-mismatch lint (hard-error slice) + gate.yml wiring

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 3567 |
| Title | feat(B-0532): parent-child status-mismatch lint (hard-error slice) + gate.yml wiring |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-05-15T19:49:09Z |
| Merged at | 2026-05-15T19:59:31Z |
| Merge commit SHA | `376a68697fcc62b3020e895bc1161d1f61dad44f` |
| Branch | `feat/b0532-parent-child-status-mismatch-lint-otto-cli-2026-05-15` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/3567 |
| Changed files | 2 |
| Additions / deletions | +116 / -4 |

## Description

## Summary

Implements [B-0532](https://github.com/Lucent-Financial-Group/Zeta/blob/main/docs/backlog/P3/B-0532-backlog-graph-consistency-lint-parent-child-status-mismatch-2026-05-15.md) **hard-error case**: parent row \`status: closed\` AND any declared child still \`status: open\` → exit 1.

## Sibling-pattern completion

| Lint | Status |
|---|---|
| \`lint-archive-header-section33\` | LIVE (B-0036) |
| \`lint-section-33-migration-xrefs\` | LIVE ([PR #3555](https://github.com/Lucent-Financial-Group/Zeta/pull/3555), B-0533) |
| \`lint-backlog-id-uniqueness\` | LIVE ([PR #3565](https://github.com/Lucent-Financial-Group/Zeta/pull/3565), B-0535) |
| **\`lint-backlog-parent-child-status\`** | **In-flight (this PR, B-0532 hard-error slice)** |

Once this lands, all 4 catch-once-then-lint cluster siblings are live.

## Scope (hard-error slice)

Per the empirical anchor ([PR #3518](https://github.com/Lucent-Financial-Group/Zeta/pull/3518)): parent B-0442 was flipped to \`closed\` while children B-0504 + B-0505 were still \`open\`. Caught by Codex + Copilot review but took 4 thread cycles to resolve.

The hard-error case alone catches this exact failure class. Deferred to follow-up slices per B-0532's full acceptance criteria:

- Soft-warning: all children closed but parent still open (closure candidate)
- Bidirectional consistency: parent.children ↔ child.parent (orphan / dangling refs)

## Implementation

| File | LOC delta | Change |
|---|---|---|
| \`tools/hygiene/audit-backlog-items.ts\` | +90 / -3 | Item 9 audit class + \`--enforce-parent-child-status\` flag |
| \`.github/workflows/gate.yml\` | +28 | New \`lint-backlog-parent-child-status\` job |

## Test plan

- [x] \`bun tools/hygiene/audit-backlog-items.ts\` runs as before (detect-only)
- [x] \`bun tools/hygiene/audit-backlog-items.ts --enforce-parent-child-status\` exits 0 on baseline (0 mismatches)
- [x] Unknown flag rejected with stderr error + exit 1
- [ ] CI green (including the new \`lint (backlog parent-child status)\` job itself running on this PR)
- [ ] Auto-merge arms

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | true |
| Total threads | 2 |
| Resolved threads | 2 |
| Unresolved threads | 0 |
| Total review comments | 4 |
| Total fix commits (touching thread paths) | 2 |

## Review threads

### Thread 1 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6CdoH1`
- **Path**: `tools/hygiene/audit-backlog-items.ts`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-15T19:52:16Z on `tools/hygiene/audit-backlog-items.ts`:195 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Preserve dotted backlog IDs when parsing children refs**

  Use a parser that keeps the full `B-NNNN.suffix` token for `children:` entries; with the current `getListRefs` pattern, IDs like `B-0029.1` are truncated to `B-0029`, so the new parent/child status check can miss open child rows (or match the wrong row) whenever decomposed child IDs are used. This is especially relevant because backlog files already use dotted child IDs (for example `docs/backlog/P2/B-0029-...md`), so the enforced gate can silently under-report mismatches.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-15T19:56:49Z on `tools/hygiene/audit-backlog-items.ts`:195 (association: MEMBER)

    <pre>
    Fixed in 47a23ace: updated `B_REF_RE` from `/B-\\d{4}/g` to `/B-\\d{4}(?:\\.\\d+)*/g`. The `(?:\.\d+)*` non-capturing group greedily captures any number of dotted suffixes (e.g. `B-0029.1`, `B-0029.1.2`), so `getListRefs("children")` now returns full dotted IDs rather than truncating them.
    </pre>

### Thread 2 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6CdofJ`
- **Path**: `tools/hygiene/audit-backlog-items.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-15T19:52:46Z on `tools/hygiene/audit-backlog-items.ts`:196 (association: NONE)

  <pre>
  P0: `childrenRefs` extraction uses `B_REF_RE = /B-\d{4}/g` via `getListRefs`, which truncates dotted backlog IDs (e.g. `B-0058.1` becomes `B-0058`). Since `children:` in frontmatter commonly includes dotted child IDs, the new parent/child mismatch gate can silently miss real mismatches (or self-reference the parent) and let an inconsistent backlog graph pass CI. Suggest updating the reference regex/parser to capture full IDs including optional dotted suffix (and ensuring lookups use the full child ID).
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-15T19:56:51Z on `tools/hygiene/audit-backlog-items.ts`:196 (association: MEMBER)

    <pre>
    Fixed in 47a23ace: `B_REF_RE` updated to `/B-\\d{4}(?:\\.\\d+)*/g`. Captures optional dotted suffix — `B-0029.1`, `B-0058.1`, etc. — preserving full child IDs when parsing `children:` frontmatter. Lookups in the parent/child mismatch check now use the complete ID.
    </pre>

## Fix commits (touching thread paths)

### `bf66b2ece0d1ef273900c78697f32b72f3d59576` -- 2026-05-15T19:48:06Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/hygiene/audit-backlog-items.ts`

**Message:**

```
feat(b-0532): parent-child status-mismatch lint (hard-error slice) + …

…gate.yml wiring

Implements the hard-error case from B-0532 acceptance criteria: parent
row with `status: closed` (or equivalent landed/superseded/merged/done)
AND any declared child still has `status: open`.

Changes:

1. tools/hygiene/audit-backlog-items.ts (item 9 audit class):
   - Extend FrontmatterFields + BacklogRow with `childrenRefs`
   - Add `isClosedStatus()` helper (reuses CLOSED_STATUSES + handles
     `superseded-by-*` prefix)
   - Add `reportParentChildStatusMismatch()` function (item 9)
   - Add `--enforce-parent-child-status` CLI flag (exit 1 on findings)
   - Update Summary section + script header docs

2. .github/workflows/gate.yml:
   - New `lint-backlog-parent-child-status` job (sibling of
     lint-backlog-id-uniqueness from PR #3565)
   - Runs `bun tools/hygiene/audit-backlog-items.ts --enforce-parent-child-status`
   - Baseline = 0 mismatches (verified locally)

Scope: hard-error case only. B-0532's full acceptance criteria also
covers soft-warning (all children closed, parent open) + bidirectional
consistency (parent.children ↔ child.parent). Those are deferred to
follow-up slices.

Empirical anchor: PR #3518 (2026-05-15) shipped a row-status flip
closing parent B-0442 without closing children B-0504 + B-0505. Took
4 review-thread cycles to fully resolve. This lint catches that class
at PR time before merge.

Composes with: PR #3565 (lint-backlog-id-uniqueness, sibling pattern),
PR #3555 (lint-section-33-migration-xrefs, sibling pattern), B-0532
(parent row).

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
```

### `47a23ace0283469956de2d588773f7bb3ed585cd` -- 2026-05-15T19:56:26Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/hygiene/audit-backlog-items.ts`

**Message:**

```
fix(B-0532): extend B_REF_RE to capture dotted child IDs (B-NNNN.suffix)

Reviewer finding (Copilot P0 + chatgpt-codex-connector P2, PR #3567):
B_REF_RE = /B-\d{4}/g truncated dotted backlog IDs like B-0029.1 → B-0029,
causing the parent/child status-mismatch gate to miss mismatches on decomposed
child rows. Updated to /B-\d{4}(?:\.\d+)*/g to capture the full dotted suffix.

Co-Authored-By: Claude <noreply@anthropic.com>
```
