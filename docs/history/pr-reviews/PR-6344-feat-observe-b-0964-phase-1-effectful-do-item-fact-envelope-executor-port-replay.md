# PR #6344 -- feat(observe 081KT07NV0008QG0R001CBQ2X2): Phase-1 effectful do_item — fact envelope + executor port + replay-folds-facts

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 6344 |
| Title | feat(observe 081KT07NV0008QG0R001CBQ2X2): Phase-1 effectful do_item — fact envelope + executor port + replay-folds-facts |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-06-01T04:58:36Z |
| Merged at | 2026-06-01T05:24:56Z |
| Merge commit SHA | `49bc517cbe4765acad39d951f800723d27d529bd` |
| Branch | `otto-cli/b0964-phase1-do-item-impl-2026-06-01` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/6344 |
| Changed files | 3 |
| Additions / deletions | +456 / -3 |

## Description

081KT07NV0008QG0R001CBQ2X2 §5 **Phase 1** (fake executor; no dep, no shell) — built on your go.

## What

The **command-vs-fact-event split** for effectful `do_item`:
- `do_item` is a COMMAND (not logged). Executing it emits **FACT events** that ARE logged: `ActionExecutionStarted{item, tier, gated}` → `ActionExecutionSucceeded{item}` | `ActionExecutionFailed{item, reason}`.
- `foldFacts(initial, facts)` replays the facts, **delegating the success transition to `simulate(do_item)`** (single reducer — no drift). It takes **no executor**, so replay *structurally cannot re-run the work* — the §0 correctness guarantee, enforced by the type.
- `CommandExecutor` port (the bash surface) is injected; `fakeExecutor` for tests. The `Started` fact records `tier` + `gated` so the §3 glass-halo audit can tell a sandbox run from a docker/real-FS escalation.

## Files

- `tools/observe/do-item.ts` — port + `fakeExecutor` + `ActionFact` + `applyFact` + `foldFacts` + `executeDoItem` (append-first; `ok` = machinery worked, `completed` = work succeeded).
- `tools/observe/execute.ts` — `EventSink` → generic `EventSink<E = NextAction>` (backward-compatible; `EventSink` ≡ `EventSink<NextAction>`) so the fact stream reuses the one durability-port shape.
- `tools/observe/do-item.test.ts` — success/failure transitions, tier-in-fact, append-first (executor never runs on durability failure), **replay-folds-facts-without-executor**, `applyFact == simulate`.

## Verify

7/7 new pass; **full observe suite 119/119** (no regressions from the `EventSink` generic); tsc ✓; eslint ✓; prettier ✓.

## Scope

Sibling to `execute()` for Phase 1 (keeps existing `execute` + tests green); unified-`execute` integration + Phase-2 real executors (local docker / just-bash) are follow-ups. Box-checking 081KT07NV0008QG0R001CBQ2X2/081KSXN940008QG0R001A4WWX4 follows once 081KT07NV0008QG0R001CBQ2X2 (#6342) lands.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | true |
| Total threads | 10 |
| Resolved threads | 10 |
| Unresolved threads | 0 |
| Total review comments | 12 |
| Total fix commits (touching thread paths) | 3 |

## Review threads

### Thread 1 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GAlRx`
- **Path**: `tools/observe/do-item.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-01T05:01:41Z on `tools/observe/do-item.ts`:46 (association: NONE)

  <pre>
  The header comment claims to compose with an "exact path" under docs/backlog/P1 for 081KT07NV0008QG0R001CBQ2X2, but that file does not exist in the current tree. This is a broken cross-reference that will mislead readers and makes future xref tooling harder.

  Either add the referenced doc in this PR, or remove/relax the "exact paths" claim here.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-06-01T05:09:47Z on `tools/observe/do-item.ts`:46 (association: MEMBER)

    <pre>
    Resolving — self-healed. This was true at thread-filing time: #6344 branched before #6342 (which created the 081KT07NV0008QG0R001CBQ2X2 row) merged, so the cited `docs/backlog/P1/081KT07NV0008QG0R001CBQ2X2-...md` path didn't exist yet. #6342 has since merged and this branch is rebased on origin/main, where the cited path now resolves exactly. Verified `git show origin/main:&lt;path&gt;` succeeds. Stale ≠ false; the action window closed.
    </pre>

### Thread 2 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GAlSI`
- **Path**: `tools/observe/do-item.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-01T05:01:42Z on `tools/observe/do-item.ts`:223 (association: NONE)

  <pre>
  If the executor runs and then appending the terminal fact (Succeeded/Failed) fails, this function returns `ok: false` even though a side-effect may already have happened and only the `Started` fact is durable. With the current `foldFacts`/`applyFact` projection, a log containing only `Started` leaves the item in the backlog, so the system may later re-attempt the same `do_item` and duplicate the side-effect.

  Consider modeling this explicitly (e.g., a distinct result variant that returns the durable facts so far + the run outcome, and/or projecting `Started` into an "in-flight/needs-reconcile" state that blocks automatic re-execution until resolved).
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-06-01T05:09:53Z on `tools/observe/do-item.ts`:223 (association: MEMBER)

    <pre>
    Fixed in 7f95371ed. `DoItemFeedback` is now a union splitting the two cases by retry-safety:

    - `append-failed`: the `Started` append failed → the executor NEVER ran → safe to retry the whole do_item.
    - `terminal-append-failed`: the executor ALREADY RAN (side-effect happened) but the terminal fact didn't land → **reconcile-needed, NOT blind-retry**. Carries `ranOutcome` ("succeeded"|"failed") + `durableFacts` (= `[Started]`) so the caller can reconcile (re-append the known terminal, or check the world). Safe re-execution requires idempotent item-work (the always-active idempotency discipline).

    The distinct-result-variant you suggested is implemented. The deeper in-flight-projection (project a `Started`-with-no-terminal into a World state that BLOCKS auto-re-execution until reconciled) needs a World in-flight field beyond Phase-1's envelope scope — noted as a FOLLOW-UP in the header. +1 test (executor RAN + terminal append failed → `terminal-append-failed`, `durableFacts=[Started]`).
    </pre>

### Thread 3 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GArzo`
- **Path**: `tools/observe/do-item.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-06-01T05:11:38Z on `tools/observe/do-item.ts`:188 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Convert executor rejections into terminal facts**

  When a real executor rejects or throws here (for example a spawn failure, timeout wrapper, or unexpected docker/just-bash error), `Started` has already been durably appended but the promise escapes before `ActionExecutionFailed` or `terminal-append-failed` can be recorded. That leaves replay with only an in-flight `Started` and gives callers no `DoItemResult` to reconcile, violating the Result-only/fact-envelope contract; wrap executor failures and append a failed terminal fact just like an `{ ok: false }` `RunOutcome`.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 4 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GArzq`
- **Path**: `tools/observe/do-item.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-06-01T05:11:38Z on `tools/observe/do-item.ts`:240 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Preserve the executor failure during reconciliation**

  When the executor returns a failed `RunOutcome` and the terminal append also fails, this feedback stores only the append failure reason and drops `outcome.reason`. In that recovery path the caller has `Started` plus `ranOutcome: "failed"`, but cannot re-append the exact `ActionExecutionFailed{..., reason}` fact without either losing the work-failure reason or rerunning the side effect; include the failed terminal fact or original work reason in the feedback.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 5 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GAsZA`
- **Path**: `tools/observe/do-item.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-01T05:12:16Z on `tools/observe/do-item.ts`:32 (association: NONE)

  <pre>
  Comment includes named attribution (“Copilot 2026-06-01”) on a current-state code surface. Repo convention is to avoid name attribution in code/docs/skills and use role-refs instead (.github/copilot-instructions.md:317-378).
  </pre>

### Thread 6 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GAsZb`
- **Path**: `tools/observe/do-item.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-01T05:12:17Z on `tools/observe/do-item.ts`:127 (association: NONE)

  <pre>
  Comment includes named attribution (“Copilot 2026-06-01”) on a current-state code surface. Repo convention is to avoid name attribution in code/docs/skills and use role-refs instead (.github/copilot-instructions.md:317-378).
  </pre>

### Thread 7 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GAsZs`
- **Path**: `tools/observe/do-item.test.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-01T05:12:17Z on `tools/observe/do-item.test.ts`:110 (association: NONE)

  <pre>
  Test name includes named attribution (“Copilot 2026-06-01”) on a current-state code surface. Repo convention is to avoid name attribution in code/docs/skills and use role-refs instead (.github/copilot-instructions.md:317-378).
  </pre>

### Thread 8 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GAsZ-`
- **Path**: `tools/observe/do-item.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-01T05:12:17Z on `tools/observe/do-item.ts`:32 (association: NONE)

  <pre>
  Comment includes named attribution (“Copilot 2026-06-01”) on a current-state code surface. Repo convention is to avoid name attribution in code/docs/skills and use role-refs instead (.github/copilot-instructions.md:317-378).
  </pre>

### Thread 9 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GAsaK`
- **Path**: `tools/observe/do-item.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-01T05:12:18Z on `tools/observe/do-item.ts`:127 (association: NONE)

  <pre>
  Comment includes named attribution (“Copilot 2026-06-01”) on a current-state code surface. Repo convention is to avoid name attribution in code/docs/skills and use role-refs instead (.github/copilot-instructions.md:317-378).
  </pre>

### Thread 10 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GAsaV`
- **Path**: `tools/observe/do-item.test.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-01T05:12:18Z on `tools/observe/do-item.test.ts`:110 (association: NONE)

  <pre>
  Test name includes named attribution (“Copilot 2026-06-01”) on a current-state code surface. Repo convention is to avoid name attribution in code/docs/skills and use role-refs instead (.github/copilot-instructions.md:317-378).
  </pre>

## Fix commits (touching thread paths)

### `73bb27e6652e4574603ce6004d4c0e4477796d3f` -- 2026-06-01T04:57:27Z -- ``

**Touched files (intersect with thread paths):**

- `tools/observe/do-item.test.ts`
- `tools/observe/do-item.ts`

**Message:**

```
feat(observe 081KT07NV0008QG0R001CBQ2X2): Phase-1 effectful do_item — fact envelope + exe…

…cutor port + replay-folds-facts

081KT07NV0008QG0R001CBQ2X2 Phase 1 (fake executor; no dep, no shell). The command-vs-fact-event
split: do_item is a COMMAND (not logged); executing emits FACT events that ARE
logged (ActionExecutionStarted{item,tier,gated} -> Succeeded|Failed), and
foldFacts replays the facts — DELEGATING the success transition to
simulate(do_item) (single reducer, no drift). foldFacts takes NO executor, so
replay structurally cannot re-run the work (the §0 correctness guarantee,
enforced by the type).

- tools/observe/do-item.ts: ExecutorTier, RunSpec/RunOutcome, CommandExecutor
  port + fakeExecutor, ActionFact union, applyFact (Succeeded delegates to
  simulate), foldFacts, executeDoItem (append-first; ok=machinery,
  completed=work; tier+gated in the Started fact for the §3 audit).
- tools/observe/execute.ts: EventSink made generic EventSink<E=NextAction>
  (backward-compatible) so the fact stream reuses the one durability-port shape.
- tools/observe/do-item.test.ts: success/failure transitions, tier-in-fact,
  append-first (executor never runs on durability failure), and the
  replay-folds-facts-without-executor correctness test + applyFact==simulate.

verify: 7/7 new pass; FULL observe suite 119/119 (no regressions from the
EventSink generic); tsc clean; eslint clean; prettier clean. Sibling to
execute() for Phase 1 (unified-execute integration is a follow-up). Implements
081KT07NV0008QG0R001CBQ2X2 §5 Phase 1; box-check + 081KSXN940008QG0R001A4WWX4 update follow once 081KT07NV0008QG0R001CBQ2X2 (#6342) lands.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `7f95371ed6d962d4cc90852feb332565eaf9fd7c` -- 2026-06-01T05:09:06Z -- ``

**Touched files (intersect with thread paths):**

- `tools/observe/do-item.test.ts`
- `tools/observe/do-item.ts`

**Message:**

```
fix(081KT07NV0008QG0R001CBQ2X2): distinguish terminal-append-failed from append-failed (C…

…opilot #6344)

The append-first envelope already prevented the common duplicate-side-effect
case (Started-append fails → executor never runs → safe retry). But the rarer
corner — executor RAN, then the TERMINAL fact (Succeeded/Failed) failed to
append — collapsed into the same 'append-failed' feedback, inviting a blind
retry that would duplicate the side-effect under at-least-once delivery.

DoItemFeedback is now a union:
  - append-failed: Started didn't land → executor never ran → safe to retry.
  - terminal-append-failed: executor ran; only Started is durable → reconcile-
    needed, NOT blind-retry. Carries ranOutcome + durableFacts so the caller can
    reconcile (re-append the known terminal, or check the world). Safe re-exec
    requires idempotent item-work (the always-active idempotency discipline).

Header notes the FOLLOW-UP Copilot suggested: project a Started-with-no-terminal
into an explicit in-flight state that BLOCKS auto-re-execution until reconciled
(needs a World in-flight field — beyond Phase-1's envelope scope).

+1 test (executor RAN + terminal append failed → terminal-append-failed,
durableFacts=[Started]). 120 pass; tsc/eslint/prettier clean.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `b39c1a9440e7b96031bc049e33523988948b8c6e` -- 2026-06-01T05:21:38Z -- ``

**Touched files (intersect with thread paths):**

- `tools/observe/do-item.test.ts`
- `tools/observe/do-item.ts`

**Message:**

```
fix(081KT07NV0008QG0R001CBQ2X2): catch executor throws, preserve failure reason, drop nam…

…e-attribution (PR review #6344)

Folds the second-round review on the do_item envelope:

- Codex P2 'convert executor rejections into terminal facts': the CommandExecutor
  port is Promise<RunOutcome>, but a real impl (spawn/timeout/container-start) can
  REJECT. Wrap executor.run in try/catch → a throw becomes a failed RunOutcome →
  ActionExecutionFailed, so an effectful run ALWAYS emits a terminal fact, never a
  dangling Started + unhandled rejection. +test.

- Codex P2 'preserve the executor failure during reconciliation': when the run
  FAILS and the terminal append also fails, the terminal-append-failed feedback now
  carries ranReason (the executor's outcome.reason) alongside reason (the append
  failure) — reconciliation no longer loses WHY the work failed. +test.

- Copilot repo-convention: remove named attribution ('Copilot 2026-06-01') from
  code surfaces (header, feedback comment, test name) → role-ref ('PR review
  2026-06-01'). No persona names in tools/** per .github/copilot-instructions.md.

122 pass (+2); tsc/eslint/prettier clean.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```
