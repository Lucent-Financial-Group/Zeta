# PR #6410 -- feat(ace): slice 5.3 — lockfile (write ./ace.lock + --frozen replay)

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 6410 |
| Title | feat(ace): slice 5.3 — lockfile (write ./ace.lock + --frozen replay) |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-06-01T11:38:44Z |
| Merged at | 2026-06-01T11:54:07Z |
| Merge commit SHA | `e01a585ebd73df623c930a95ed0c2af0801cce93` |
| Branch | `otto-windows/ace-slice5.3-impl-2026-06-01` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/6410 |
| Changed files | 7 |
| Additions / deletions | +1118 / -4 |

## Description

Slice 5.3 of the Ace DLC package manager (081KR2E4K0008QG0R002YE3MMD), building on 5.1 (#6369) + 5.2 (#6388/#6391). Implements the spec merged in #6400. Subagent-driven build (7 TDD tasks + 3 review-fix rounds).

## What this adds

- **`tools/ace/lockfile.ts`** (new, pure): `Lockfile`/`LockNode` types, `buildLockfile(root, order, registry)` (full pin: name+version+url+package_hash per node, inline-edge-first url precedence, root excluded), `serializeLockfile` (canonical JSON), `parseLockfile` (total — malformed → `{error}`, never throws), `verifyRootMatchesLock` (drift gate).
- **`tools/ace/resolve.ts`**: `canonicalJson` exported for reuse (one-keyword additive change).
- **`tools/ace/ace.ts`**: `--frozen` + `--lockfile <path>` flags; normal `ace install` writes `./ace.lock` after a successful graph install (write failure = warning, not a failed install); `--frozen` reads the lock, applies the root-drift gate, and **two-pass** replays it (verify ALL nodes — pin + content_hash + path-safety + signature gate + store-collision check — then install ALL + root), never calling `solve()`/`loadRegistry()` → registry-independent, byte-reproducible, tamper-evident.
- **`.claude/skills/ace/SKILL.md`**: documents `--frozen`/`--lockfile` + the lockfile.

## Decisions (from the #6400 spec)
cargo-style write + opt-in `--frozen` read · `./ace.lock` in CWD + `--lockfile` override · full-pin (registry-independent replay).

## Verification

- `bun test tools/ace/` → **187 pass / 0 fail** (incl. registry-independence with an empty registry, drifted-root → refuse, no-lock → refuse, tampered-node → refuse, untrusted-signature node → refuse even with `--allow-no-signature`, atomicity verify-all-before-install-any, store-collision guard).
- `bun --bun tsc --noEmit -p tsconfig.json` → exit 0 · markdownlint clean · commit canary 67 throughout.

## Review trail
Three review rounds during the build: lockfile.ts module review (P2 test gap fixed), ace.ts integration review (untrusted-signature frozen test added + de-shadow), final holistic review (frozen two-pass atomicity + store-collision parity + SKILL flags). All false-green-sanity-checked.

Deferred enhancements tracked in 081KT07NV0008QG0R002GV3MXW (`ace update`), 081KT07NV0008QG0R0028AAV0E (`--locked` mode), 081KT07NV0008QG0R003VDHWWG (lockfile ergonomics) — filed with the spec.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | true |
| Total threads | 4 |
| Resolved threads | 4 |
| Unresolved threads | 0 |
| Total review comments | 7 |
| Total fix commits (touching thread paths) | 10 |

## Review threads

### Thread 1 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GGeRz`
- **Path**: `tools/ace/ace.ts`
- **Replies**: 0

**Initial comment:**

- **`github-advanced-security[bot]` (bot)** at 2026-06-01T11:41:46Z on `tools/ace/ace.ts`:615 (association: NONE)

  <pre>
  ## CodeQL / Network data written to file

  Write to file system depends on [Untrusted data](1).

  [Show more details](https://github.com/Lucent-Financial-Group/Zeta/security/code-scanning/139)
  </pre>

### Thread 2 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GGey3`
- **Path**: `tools/ace/lockfile.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-01T11:42:19Z on `tools/ace/lockfile.ts`:26 (association: NONE)

  <pre>
  buildLockfile only records URLs for dependencies whose edge.kind is exactly "inline". However, resolve() treats a missing kind as inline for back-compat, so a manifest with kind omitted can install successfully but then fail to build a lockfile because the inline URL was never captured (and the registry may not have an entry). Consider treating kind==="inline" OR kind===undefined as inline when pre-scanning edges.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-06-01T11:51:38Z on `tools/ace/lockfile.ts`:26 (association: MEMBER)

    <pre>
    Fixed in d4abc653f — root exclusion now uses `order.slice(0,-1)` when the last element is the root (the documented contract), with the `p !== root` identity filter as fallback.
    </pre>

### Thread 3 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GGezZ`
- **Path**: `tools/ace/lockfile.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-01T11:42:19Z on `tools/ace/lockfile.ts`:25 (association: NONE)

  <pre>
  buildLockfile relies on object identity (p !== root) to exclude the root from the locked nodes. The function contract/docstring says `order` is deps-first with the root LAST, so excluding via `order.slice(0, -1)` (when the last element is the provided root) is more robust than identity filtering (which can accidentally include the root if a caller passes an equivalent-but-not-identical root instance).
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-06-01T11:51:40Z on `tools/ace/lockfile.ts`:25 (association: MEMBER)

    <pre>
    Fixed in d4abc653f — buildLockfile prescan now treats `kind === undefined` as inline (back-compat parity with resolve()/solver), reads url defensively, and only maps string urls. Added a unit test for a kind-omitted inline edge.
    </pre>

### Thread 4 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GGezy`
- **Path**: `tools/ace/ace.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-01T11:42:20Z on `tools/ace/ace.ts`:555 (association: NONE)

  <pre>
  In the --frozen replay path, locked nodes are parsed with `JSON.parse(...) as AcePackage` and then immediately passed to packageHash()/verifySignature()/contentHash/validatePackagePaths. Because the lockfile (and the fetched bytes at node.url) are untrusted, a malformed JSON object (missing manifest/files, wrong field types) can throw at runtime instead of producing a clean refusal. Adding a small shape guard (similar to resolve()’s invalid-package check) before any field access will make frozen replay total and avoid crashes on malformed/tampered inputs.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-06-01T11:51:41Z on `tools/ace/ace.ts`:555 (association: MEMBER)

    <pre>
    Fixed in d4abc653f — the --frozen PASS-1 replay now shape-guards each JSON-parsed node (manifest + files must be objects) before any verify primitive, returning a clean invalid-package refusal instead of throwing. Added a frozen integration test with a malformed node payload (false-green-verified).
    </pre>

## Fix commits (touching thread paths)

### `f019f903e911473494399c45f672ebe9dad78e3a` -- 2026-06-01T11:08:42Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/ace/lockfile.ts`

**Message:**

```
feat(ace): lockfile.ts buildLockfile (slice 5.3 task 1)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `a59a3bf974ce0dd508de37a0f4696b470ea38e97` -- 2026-06-01T11:10:52Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/ace/lockfile.ts`

**Message:**

```
feat(ace): lockfile serialize/parse + export canonicalJson (slice 5.3…

… task 2)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `d41068e2735d83927661bdfa88e077eb612c229b` -- 2026-06-01T11:12:12Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/ace/lockfile.ts`

**Message:**

```
feat(ace): lockfile verifyRootMatchesLock drift gate (slice 5.3 task 3)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `c8eba2c86580ea401f24b207308e032b44824d07` -- 2026-06-01T11:17:18Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/ace/ace.ts`

**Message:**

```
feat(ace): --frozen + --lockfile install flags (slice 5.3 task 4)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `b65667aa33e0114afc521a6e00b1b3d29258c420` -- 2026-06-01T11:18:23Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/ace/ace.ts`

**Message:**

```
feat(ace): write ./ace.lock on graph install (slice 5.3 task 5)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `1f9a95ec8d8f5848da6acdb4c47fbddd83286c17` -- 2026-06-01T11:19:48Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/ace/ace.ts`

**Message:**

```
feat(ace): --frozen lockfile replay install (slice 5.3 task 6)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `85c8063c728b9c075b27fa5944cd048c1ff793af` -- 2026-06-01T11:28:21Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/ace/ace.ts`

**Message:**

```
fix(ace): frozen-path untrusted-signature test + de-shadow node read …

…buffer (slice 5.3 review)

P1 (security surface): add a frozen integration test proving the replay signature
gate refuses an untrusted-signature locked node EVEN WITH --allow-no-signature.
Node A is signed with a key never added to the trust store, so verifySignature
returns untrusted-key; the lock is built directly to pin packageHash(signedA) so
replay fetches the same signed bytes (pin + content_hash + path-safety pass) and
reaches the signature gate -- proving --allow-no-signature waives only no-signature,
never a present-but-invalid signature. Sanity-checked: weakening the gate to also
waive untrusted-key makes the test fail (received exit 0 vs expected 1); restored.

P2 (readability): the inner per-node fetch buffer `raw` in the frozen replay loop
shadowed the outer root read buffer `raw` in the same function scope. Renamed the
inner one to `nodeRaw`. Pure rename, no behavior change.

bun test tools/ace: 185 pass / 0 fail. tsc --noEmit exit 0.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `e1e674660ec6c73bb804d5f821ec13b9e1240fc7` -- 2026-06-01T11:30:56Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/ace/ace.ts`

**Message:**

```
docs(ace): document --frozen/--lockfile + lockfile (slice 5.3 task 7)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `daeeed60cefb47c8222342654547a620e8250d74` -- 2026-06-01T11:37:54Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/ace/ace.ts`

**Message:**

```
fix(ace): frozen replay two-pass verify-all-then-install + store-coll…

…ision parity + SKILL flags (slice 5.3 final review)

P1 (atomicity + store-collision parity): the --frozen replay loop fetched+
verified+installed each node sequentially, so (a) it never ran the byStoreKey
content_hash->package_hash cross-node collision check the default graph-install
preflight runs, and (b) a verify failure on a later node left earlier nodes
already installed (no atomicity). Restructured into two passes mirroring the
default path: PASS 1 verifies the WHOLE graph (fetch -> parse -> pin ->
content_hash -> path-safety -> signature gate -> byStoreKey collision, seeded
with the root so a node colliding on the root's store key is caught) installing
nothing; PASS 2 installs the verified nodes in lock order, then the root. Any
PASS 1 failure returns 1 having installed nothing. No solve(), no loadRegistry(),
no lock write. All existing per-node refusal messages preserved.

Tests: two new --frozen integration tests. (1) atomicity — a 2-node lock whose
2nd node fails the pin check leaves the 1st (fully verifiable) node NOT installed.
(2) store-collision — two nodes sharing a content_hash store key with different
package_hash refuse before installing either, mirroring the default-path
store-collision test. Both proven non-false-green: deliberately reverting the
restructure to install-during-verify turns both red, then restored.

P2 (doc drift): SKILL.md install verb grammar line was missing the new flags;
added [--frozen] [--lockfile <path>].

bun test tools/ace/: 187 pass / 0 fail. tsc --noEmit: exit 0.
markdownlint SKILL.md: clean. git ls-tree HEAD: 67.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `d4abc653f5836891fe9dfe5e6000dd92cd464b58` -- 2026-06-01T11:49:51Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/ace/ace.ts`
- `tools/ace/lockfile.ts`

**Message:**

```
fix(ace): kind-omitted inline lock capture + frozen node shape-guard …

…+ robust root exclusion (PR #6410 review)

Three robustness fixes from Copilot review of slice-5.3 (PR #6410):

- Finding 2 (lockfile.ts buildLockfile): the inline-url prescan only
  captured edges with kind === "inline", but resolve()/solver treat a
  MISSING kind as inline (pre-DU back-compat). A kind-omitted inline edge
  installed via resolve() but its url was never captured, so buildLockfile
  returned {error} / the write warned and no lock was written. Now treat
  kind === undefined as inline and read url defensively (the edge is
  untrusted-shaped, not in the AceDependency DU).

- Finding 4 (ace.ts --frozen replay): each locked node's fetched bytes +
  the lockfile are untrusted; JSON.parse-as-AcePackage then handed straight
  to packageHash/contentHash/verifySignature could THROW (TypeError on
  np.manifest.content_hash) on a malformed-but-JSON-valid payload instead
  of refusing cleanly. Added solver.ts parsePackage-style shape guard right
  after JSON.parse (before any verify primitive): malformed manifest/files
  → exit 1, no throw, installs nothing.

- Finding 3 (lockfile.ts root exclusion): root excluded via identity-filter;
  contract is root-is-LAST-in-order, so slice when the contract holds and
  fall back to identity-filter otherwise.

Tests: lockfile.test.ts gains a kind-omitted-inline-edge unit test;
ace.test.ts gains a --frozen malformed-node integration test that builds
the lock directly so the pin passes and execution reaches the shape guard
(the exact line that throws unguarded). Both false-green sanity-checked:
reverting each fix turns its test red (Finding 4 with the exact
np.manifest.content_hash TypeError). 189 pass / 0 fail; tsc exit 0.

Did NOT touch the lockfile write (CodeQL "network data written to file")
— handled separately.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```
