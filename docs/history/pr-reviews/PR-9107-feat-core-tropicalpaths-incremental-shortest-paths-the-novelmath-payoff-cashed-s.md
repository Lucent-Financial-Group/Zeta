# PR #9107 -- feat(core): TropicalPaths — incremental shortest paths, the NovelMath payoff cashed (shadow*)

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 9107 |
| Title | feat(core): TropicalPaths — incremental shortest paths, the NovelMath payoff cashed (shadow*) |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-07-02T03:57:41Z |
| Merged at | 2026-07-02T03:57:44Z |
| Merge commit SHA | `22b4b3a7ca897311120b65b46aeed8a258163d9c` |
| Branch | `otto/tropical-shortest-paths-the-novelmatch-payoff` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/9107 |
| Changed files | 5 |
| Additions / deletions | +276 / -2 |

## Description

The docstring promise in `NovelMath.fs` made real: incremental single-source shortest paths over an evolving edge `ZSetW<'V*'V, TropicalWeight>` — Bellman-Ford as a semi-naive fixpoint of Z-set sums (the shared kernel). The atom's semantics do the graph work: parallel edges min-consolidate, +∞ drops, unreachable≡absent.

**The zerosumfree theorem, operational:** `insertEdges` is incrementally free (min monotone); `refold` handles delete because tropical has no inverse (the ring plane retracts, the tropical plane refolds); negative cycles detected. TropicalSemiring is now `[<Struct>]` (hot-path ready).

**6 tests** incl. the FsCheck LAW **incremental-insert ≡ fresh-recompute over 200 random graphs**. 0 warnings; existing suites green. Anchors: Lehmann 1977, Mohri 2002, Gondran-Minoux 2008.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 1 |
| Resolved threads | 0 |
| Unresolved threads | 1 |
| Total review comments | 1 |
| Total fix commits (touching thread paths) | 1 |

## Review threads

### Thread 1 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM6Nxud2`
- **Path**: `src/Core/TropicalPaths.fs`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-07-02T04:01:49Z on `src/Core/TropicalPaths.fs`:80 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Return overflow instead of throwing from path relaxation**

  When a reachable path's finite costs exceed the `int64` range (for example `A-&gt;B = Int64.MaxValue - 1` and `B-&gt;C = 2`, or a negative cycle driven below `Int64.MinValue` before `maxRounds`), this `ring.Mul` reaches `TropicalWeight.(*)`, which uses `Checked.(+)` and raises `OverflowException`. That bypasses the `Result` contract of `singleSource`/`insertEdges` and can crash callers instead of reporting an error; the relaxation should either detect/saturate overflow consistently or translate it to `Error`.

  Useful? React with 👍 / 👎.
  </pre>

## Fix commits (touching thread paths)

### `84d6c32e2d961554a7b7990b2e1c50002e1c2766` -- 2026-07-02T03:57:37Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `src/Core/TropicalPaths.fs`

**Message:**

```
feat(core): TropicalPaths — incremental shortest paths, the NovelMath…

… payoff cashed (shadow*)

Aaron 2026-07-02: "NovelMath.fs wow yeah this is a long term accidental payoff lets
do this." NovelMath.fs promised it in a docstring; the base-atom unification
(081KWFXTHJY, ZSetW carries any semiring at zero overhead) + the IRing split
(081KWG9JQ9H, tropical is now lawful at its own tier) make it fall out as algebra.

src/Core/TropicalPaths.fs: single-source shortest paths over an evolving edge
Z-set `ZSetW<'V*'V, TropicalWeight>` — Bellman-Ford as a semi-naive fixpoint of
Z-set sums (the shared MergeKernel fold). The atom's own semantics do the graph
bookkeeping: parallel edges CONSOLIDATE via Add=min; +∞ edges DROP via Zero;
unreachable ≡ absent, by the algebra not by convention.

The zerosumfree theorem made OPERATIONAL (the API states it instead of hiding it):
- insertEdges / cost-decrease is INCREMENTAL for free — min is monotone, re-relax
  from current distances over the updated edges; work ∝ affected region.
- refold for deletion / cost-increase — tropical has NO additive inverse (can't
  subtract an edge out), so deletion is a rebuild from the surviving edge log
  (which the event-sourced substrate keeps). The ring plane retracts; the tropical
  plane refolds — the split's lesson, cashed.
- negative cycles reachable from the source are DETECTED (fixpoint bound), not
  looped forever.
- TropicalSemiring made [<Struct>] (dual-register like IntegerRing; hot-path ready).

Tests (6, incl. the load-bearing FsCheck LAW over 200 random graphs): hand-checked
SSSP; parallel-edge min-consolidation; incremental insert improves only the
affected region; deletion-as-refold; negative-cycle detection; and
INCREMENTAL INSERT ≡ FRESH RECOMPUTE (min-monotone convergence). Full solution
0 warnings; existing tropical/semiring/novelmath suites green under the struct change.

Anchors: Lehmann 1977 (algebraic path problem); Mohri 2002 (semiring
shortest-distance); Gondran & Minoux 2008 (dioids); Bellman 1958 / Ford 1956.

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>

AgencySignature-v1:
  persona: otto
  actor: zeta-otto
  surface: src/core
  topology: shadow-clone
  intent: cash-the-novelmath-tropical-shortest-paths-payoff
  authorization: aaron-explicit-2026-07-02
  uncertainty: low
  measure: tropicalpaths-sssp-incremental-insert-equals-recompute-law-6-tests-green-refold-for-delete
  delta-u: the-base-atom-does-something-hardcoded-int-dbsp-cannot-zerosumfree-theorem-operational
  seed: S4
```
