# PR #9873 -- feat(hygiene): drift-sweep workflow — the sweep runs on main's tick (081KX3KA3EW)

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 9873 |
| Title | feat(hygiene): drift-sweep workflow — the sweep runs on main's tick (081KX3KA3EW) |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-08-01T13:20:27Z |
| Merged at | 2026-08-01T13:41:39Z |
| Merge commit SHA | `c2b5692ba79f3a68787a968f272acfa48ce6e5e7` |
| Branch | `otto/drift-sweep-on-tick` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/9873 |
| Changed files | 3 |
| Additions / deletions | +130 / -9 |

## Description

Arms the flip's safety net. #9863 built the tick-indexed MTTH fold; this PR makes something actually sweep — the ledger on main has zero recorded ticks today, and unmeasured drift-tolerance is normalized deviance.

**`.github/workflows/drift-sweep.yml`** (mirrors tick-metrics.yml safe patterns): every 4h + dispatch → run the non-blocking detectors (v1: markdownlint-cli2@0.22.1, the .mise.toml pin) → `drift-ledger sweep --dir docs/drift-events --tracked <(git ls-files)` → commit the tick event. Detectors are additive: append another detector's output to the same findings file to bring its drift class into the ledger. Loop safety: GITHUB_TOKEN pushes don't trigger workflows + `[skip ci]`.

**`drift-ledger.ts`**: the guard the workflow surfaced — `parseFindings(output, knownPaths?)` filters path-shaped tool preamble (`Summary:`, `Linting:`, glob echoes) that would otherwise mint junk keys in a permanent ledger; same guard as scoped-lint's `classifyLines`; paths normalized for stable keys. CLI grows `--tracked`.

Verified on-cell: 10 bun tests green (incl. new preamble-guard test), strict tsc (`noUncheckedIndexedAccess` + `exactOptionalPropertyTypes`) clean, live round-trip with real markdownlint-cli2 preamble → only the true finding recorded, heal at tick 2, MTTH 1.0. actionlint clean.

Workitem 081KX3KA3EW08QG0R002WFQ6BG stays in-progress (remaining: dashboard panel over `report`, SLO breach → auto-filed P1).

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 0 |
| Resolved threads | 0 |
| Unresolved threads | 0 |
| Total review comments | 0 |
| Total fix commits (touching thread paths) | 0 |

## Review threads

(no review threads on this PR)

## Fix commits (touching thread paths)

(no commits in this PR touched files anchored by review threads)
