# PR #2519 -- feat(B-0322): wire drain-log auto-write into mutate() — closes B-0322

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 2519 |
| Title | feat(B-0322): wire drain-log auto-write into mutate() — closes B-0322 |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-05-10T18:33:47Z |
| Merged at | 2026-05-10T18:53:13Z |
| Merge commit SHA | `95bc496c90e05fef730369132ab5256cfb60149e` |
| Branch | `fix/B-0322-drain-log-auto-write` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/2519 |
| Changed files | 5 |
| Additions / deletions | +103 / -7 |

## Description

## Summary

- `mutate()` now appends a `MutationLogEntry` to `docs/hygiene-history/playwright-mutations/log.jsonl` on every successful mutation, closing the last open done-criterion for B-0322.
- `skipLog: true` option added to `MutationOptions` so `revert()` can call `mutate()` without double-logging (drain-log.ts appends its own revert/indeterminate markers).
- `logPath` option added so tests can redirect writes to temp paths; `makeOpts()` in mutate.test.ts defaults to a temp logPath so the real drain log is never touched by the test suite.
- 4 new tests verify: auto-write on success, written entry matches `drainLogEntry`, `skipLog` suppresses write, failed mutations don't write.

## Circular dependency note

`mutate.ts` now imports `appendEntry` / `DEFAULT_LOG_PATH` from `drain-log.ts`, and `drain-log.ts` imports `mutate` from `mutate.ts`. This is a **safe ES-module circular reference**: both modules only use the imported values inside function bodies, never at module initialisation time.

## Focused check log

```
bun test mutate drain-log
51 pass / 0 fail (2 files)

bun test --reporter=dot (full suite)
169 pass / 0 fail (8 files)

dotnet build -c Release
0 Warning(s)  0 Error(s)

git status (real drain log)
docs/hygiene-history/playwright-mutations/log.jsonl — CLEAN (not modified)
```

operative-authorization: aaron 2026-05-04: "it**, not just the output. Grinding through failures + recoveries"

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | true |
| Total threads | 4 |
| Resolved threads | 4 |
| Unresolved threads | 0 |
| Total review comments | 4 |
| Total fix commits (touching thread paths) | 4 |

## Review threads

### Thread 1 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6A69_M`
- **Path**: `docs/backlog/P1/B-0322-playwright-github-mutation-reversibility-log.md`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-10T18:37:00Z on `docs/backlog/P1/B-0322-playwright-github-mutation-reversibility-log.md`:4 (association: NONE)

  <pre>
  P1: Backlog frontmatter `status: done` is not a valid per-row status value. Per tools/backlog/README.md, allowed statuses are `open` / `closed` / `superseded-by-B-NNNN` / `deferred`, and the index generator treats unknown statuses (including `done`) as still-open. Change this to `closed` (or an allowed superseded/deferred variant) so B-0322 is actually marked closed in docs/BACKLOG.md.

  </pre>

### Thread 2 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6A69_T`
- **Path**: `tools/playwright/github-ui/mutate.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-10T18:37:00Z on `tools/playwright/github-ui/mutate.ts`:344 (association: NONE)

  <pre>
  P1: This drain-log write can throw (e.g., permissions / invalid path) and will currently surface as a raw fs error without indicating it was the drain-log append or which logPath was used. Consider catching errors from `appendEntry(...)` and rethrowing a `MutationExecutionError` (or returning a `{ success: false, error: ... }` result) that includes the resolved log path for easier debugging.

  </pre>

### Thread 3 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6A69_Y`
- **Path**: `tools/playwright/github-ui/mutate.test.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-10T18:37:00Z on `tools/playwright/github-ui/mutate.test.ts`:484 (association: NONE)

  <pre>
  P2: The `skipLog` test only asserts that the log file doesn't exist; it should also assert the mutation itself succeeded (e.g., `result.success === true`) so the test can't pass due to an unrelated failure path that prevents any write.

  </pre>

### Thread 4 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6A7AKd`
- **Path**: `tools/playwright/github-ui/mutate.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-10T18:45:30Z on `tools/playwright/github-ui/mutate.ts`:351 (association: NONE)

  <pre>
  P0: If appendEntry throws, mutate() returns {success:false} even though the UI mutation has already been applied (action executed + after snapshot captured). That makes the result semantically misleading and can cause callers to retry and apply the mutation twice. Consider treating drain-log persistence as a separate (reported) step: either (a) keep success:true but surface a log-write failure in a dedicated field/warning, or (b) throw a distinct error type so callers know the mutation happened but logging failed and should not be retried blindly.

  </pre>

## Fix commits (touching thread paths)

### `4b6252b73dfec6e499df69eea8f35b0130baab63` -- 2026-05-10T18:33:34Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `docs/backlog/P1/B-0322-playwright-github-mutation-reversibility-log.md`
- `tools/playwright/github-ui/mutate.test.ts`
- `tools/playwright/github-ui/mutate.ts`

**Message:**

```
feat(B-0322): wire drain-log auto-write into mutate() — closes B-0322

mutate() now appends a MutationLogEntry to the drain log on every
successful mutation. The circular dependency between mutate.ts and
drain-log.ts is safe because each module only invokes the other inside
function bodies, not at module-load time.

Key design decisions:
- `skipLog: true` MutationOptions field lets revert() call mutate()
  without double-logging — drain-log.ts already appends its own
  revert/indeterminate markers.
- `logPath` option overrides the default path; mutate.test.ts sets
  a temp logPath in makeOpts() so tests never pollute the real drain log.
- 4 new tests cover: auto-write on success, written entry matches
  drainLogEntry, skipLog suppresses write, failed mutations don't write.

169/169 tests pass; real docs/hygiene-history/playwright-mutations/log.jsonl
remains untouched.

operative-authorization: aaron 2026-05-04: "it**, not just the output. Grinding through failures + recoveries"

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>
```

### `2fc34c6dcdeff2b47ee68e8262154324fa196c6f` -- 2026-05-10T18:41:12Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `docs/backlog/P1/B-0322-playwright-github-mutation-reversibility-log.md`
- `tools/playwright/github-ui/mutate.test.ts`
- `tools/playwright/github-ui/mutate.ts`

**Message:**

```
fix(B-0322): address Copilot review findings — status→closed, error w…

…rapping, test assertion

- Backlog status: done→closed (valid frontmatter value per tools/backlog/README.md)
- appendEntry() now wrapped in try/catch; throws MutationExecutionError with
  resolved logPath on failure for debuggable error context
- skipLog test now asserts result.success===true before checking log absence,
  preventing false-pass from unrelated failure paths

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>
```

### `8baa4dfe4fb8f5033165bb7638a5d78893cb9045` -- 2026-05-10T18:41:37Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/playwright/github-ui/mutate.ts`

**Message:**

```
fix(B-0322): address review comments on drain-log error surfacing, te…

…st assertion, and backlog status

- Change backlog frontmatter status: done → closed (valid per tools/backlog/README.md)
- Wrap appendEntry in mutate() to catch I/O errors and return MutationExecutionError-style failure including resolved logPath
- Strengthen skipLog test to assert result.success === true (prevents false-positive on unrelated failure)

Co-Authored-By: Grok <noreply@x.ai>
Co-authored-by: Cursor <cursoragent@cursor.com>
```

### `900054f112602814268ccd21b1817edd5e1ebfa6` -- 2026-05-10T18:50:22Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/playwright/github-ui/mutate.test.ts`
- `tools/playwright/github-ui/mutate.ts`

**Message:**

```
fix(B-0322): preserve mutation success on log append failure

Keep mutate() success true once the UI mutation has applied and surface drain-log append failures through an optional result field so callers do not retry an already-applied mutation.

Co-Authored-By: Codex <noreply@openai.com>
```
