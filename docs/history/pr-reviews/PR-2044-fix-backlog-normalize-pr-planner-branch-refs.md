# PR #2044 -- fix(backlog): normalize PR planner branch refs

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 2044 |
| Title | fix(backlog): normalize PR planner branch refs |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-05-08T05:53:04Z |
| Merged at | 2026-05-08T06:16:19Z |
| Merge commit SHA | `17e2e982ec32a8492aafebb42b98abf33d448aa1` |
| Branch | `claim/task-b0280-branch-ref-normalization` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/2044 |
| Changed files | 2 |
| Additions / deletions | +138 / -4 |

## Description

## Summary
- fixes the B-0280 PR publication planner to normalize branch refs before default-branch safety checks
- blocks `refs/heads/main` and `origin/main` the same way as `main`
- emits push and PR-create command argv with normalized short branch refs
- addresses the post-merge review finding on PR #2043

## Checks
- `bun test tools/backlog/pr-publication-plan.test.ts`
- `bun run typecheck`
- `bunx prettier --check tools/backlog/pr-publication-plan.ts tools/backlog/pr-publication-plan.test.ts docs/claims/task-b0280-branch-ref-normalization.md`
- `git diff --check`


## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | true |
| Total threads | 6 |
| Resolved threads | 6 |
| Unresolved threads | 0 |
| Total review comments | 6 |
| Total fix commits (touching thread paths) | 6 |

## Review threads

### Thread 1 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6Ah3WG`
- **Path**: `tools/backlog/pr-publication-plan.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-08T05:55:13Z on `tools/backlog/pr-publication-plan.ts`:128 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Preserve literal branch names that begin with origin/**

  The new `origin/` normalization rewrites any branch that starts with that prefix, but Git allows local branch names like `origin/feature` as ordinary refs, so this can silently retarget publication commands to `feature` instead of the actual branch the operator provided. In that case `git push -u origin ...` and `gh pr create --head ...` are generated for the wrong ref, which can fail or publish unintended commits. This regression is introduced by the unconditional `startsWith("origin/")` strip and should be limited to true remote-ref forms (for example `refs/remotes/origin/...`) rather than all literal branch names.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 2 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6Ah4om`
- **Path**: `tools/backlog/pr-publication-plan.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-08T05:57:09Z on `tools/backlog/pr-publication-plan.ts`:144 (association: NONE)

  <pre>
  P0 (security): `normalizeBranchRef()` can turn a non-empty input into an empty ref (e.g., `refs/heads/` / `origin/`) and can also produce values that start with `-`. Since the normalized `branch` is later used directly as argv for `git push` and `gh pr create`, please validate the *normalized* refs (non-empty + reject option-like/invalid ref names) before proceeding to avoid malformed commands and potential option/argv injection.
  </pre>

### Thread 3 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6Ah-IN`
- **Path**: `tools/backlog/pr-publication-plan.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-08T06:05:03Z on `tools/backlog/pr-publication-plan.ts`:134 (association: NONE)

  <pre>
  P0: PR description says `origin/main` should be treated the same as `main`, but `normalizeBranchRef` only strips `refs/heads/` and `refs/remotes/&lt;remote&gt;/`. As a result, `validatePublicationInput` will not block `origin/main` (it normalizes to `origin/main`, which is not in `DEFAULT_BRANCHES`). Consider normalizing `origin/&lt;default&gt;` (or handling `&lt;remote&gt;/&lt;default&gt;` explicitly in the default-branch check) so `origin/main` is refused as intended.
  </pre>

### Thread 4 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6Ah-IX`
- **Path**: `tools/backlog/pr-publication-plan.test.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-08T06:05:03Z on `tools/backlog/pr-publication-plan.test.ts`:97 (association: NONE)

  <pre>
  P0: The validation tests cover `main`, `refs/heads/main`, and `refs/remotes/origin/main`, but (per the PR description) `origin/main` should also be refused. Right now `origin/main` would pass validation and be emitted into command argv unchanged. Add a test for `origin/main` (and update normalization/validation accordingly) to ensure the default-branch safety check can't be bypassed via a remote-qualified ref.
  </pre>

### Thread 5 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6AiCD1`
- **Path**: `tools/backlog/pr-publication-plan.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-08T06:10:24Z on `tools/backlog/pr-publication-plan.ts`:157 (association: NONE)

  <pre>
  P0: validateNormalizedBranchRef currently allows normalized refs that still start with `refs/` (e.g. `refs/tags/v1.0` or `refs/remotes/origin` without a trailing `/`). That means the generated commands could plan `git push -u origin refs/tags/...` or other non-branch refs, which breaks the tool’s “branch” assumption and can bypass the intended safety checks. Consider rejecting any normalized value with a `refs/` prefix (unless it was stripped to a short branch name), and add a regression test (e.g. `refs/tags/v1`).
  </pre>

### Thread 6 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6AiCvr`
- **Path**: `tools/backlog/pr-publication-plan.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-08T06:11:10Z on `tools/backlog/pr-publication-plan.ts`:131 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Avoid collapsing non-default branch names ending in /main**

  `normalizeBranchRef` currently rewrites any two-segment name like `release/main` to `main`, not just remote-tracking shorthands such as `origin/main`. That means valid refs like `refs/heads/release/main` are mis-normalized: publication from that branch is rejected as if it were the default branch, and `baseBranch` can be silently retargeted to `main` in `gh pr create`. This is a regression from the new normalization logic and can misdirect or block PR publication for legitimate branch names.

  Useful? React with 👍 / 👎.
  </pre>

## Fix commits (touching thread paths)

### `da8c77fde1d31f0adeb1de793acca30b03fc6f49` -- 2026-05-08T05:52:55Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/backlog/pr-publication-plan.test.ts`
- `tools/backlog/pr-publication-plan.ts`

**Message:**

```
fix(backlog): normalize PR planner branch refs

Normalizes full branch refs before default-branch safety checks and command construction, covering the post-merge PR #2043 review finding for refs/heads/main.

Co-Authored-By: Codex <noreply@openai.com>
```

### `3a336faa348eae495c73692d103d743c955cf9bd` -- 2026-05-08T05:57:44Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/backlog/pr-publication-plan.test.ts`
- `tools/backlog/pr-publication-plan.ts`

**Message:**

```
fix(backlog): preserve literal origin-prefixed branch refs

Address PR #2044 review feedback by limiting normalization to explicit refs/heads/* and refs/remotes/* forms. Literal branch names such as origin/feature now remain intact in generated push and PR commands.

Checks: bun test tools/backlog/pr-publication-plan.test.ts

Checks: bun run typecheck

Checks: bunx prettier --check tools/backlog/pr-publication-plan.ts tools/backlog/pr-publication-plan.test.ts

Checks: git diff --check

Co-Authored-By: Codex <noreply@openai.com>
```

### `84347c9a9d7995d503a4f72cd4030fac67b0f620` -- 2026-05-08T06:01:24Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/backlog/pr-publication-plan.test.ts`
- `tools/backlog/pr-publication-plan.ts`

**Message:**

```
fix(backlog): validate normalized PR planner refs

Address the PR #2044 P0 review by validating normalized branch and base refs before building git/gh argv. Reject empty, option-like, and malformed ref names while preserving literal origin/* branch names.

Checks: bun test tools/backlog/pr-publication-plan.test.ts

Checks: bun run typecheck

Checks: bunx prettier --check tools/backlog/pr-publication-plan.ts tools/backlog/pr-publication-plan.test.ts

Checks: git diff --check

Co-Authored-By: Codex <noreply@openai.com>
```

### `bbe1a6600fef1c6d78530af3e27904e97d1cefa7` -- 2026-05-08T06:06:29Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/backlog/pr-publication-plan.test.ts`
- `tools/backlog/pr-publication-plan.ts`

**Message:**

```
fix(backlog): reject remote-qualified default refs

Address PR #2044 review feedback by refusing remote-qualified default branch refs such as origin/main and upstream/main while preserving non-default origin/* feature branches.

Checks: bun test tools/backlog/pr-publication-plan.test.ts

Checks: bun run typecheck

Checks: bunx prettier --check tools/backlog/pr-publication-plan.ts tools/backlog/pr-publication-plan.test.ts

Checks: git diff --check

Co-Authored-By: Codex <noreply@openai.com>
```

### `22fa1bd3379f12d335aba5694436b0e1c0f56a4b` -- 2026-05-08T06:07:21Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/backlog/pr-publication-plan.test.ts`
- `tools/backlog/pr-publication-plan.ts`

**Message:**

```
fix(backlog): normalize remote default branch refs

Keep the PR planner's default-branch guard aligned with review expectations by normalizing remote-qualified default refs such as origin/main and upstream/main to main. Non-default origin/* feature branches remain literal.

Checks: bun test tools/backlog/pr-publication-plan.test.ts

Checks: bun run typecheck

Checks: bunx prettier --check tools/backlog/pr-publication-plan.ts tools/backlog/pr-publication-plan.test.ts

Checks: git diff --check

Co-Authored-By: Codex <noreply@openai.com>
```

### `30169b1d112557d928f1988aec88db16e9365240` -- 2026-05-08T06:13:08Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/backlog/pr-publication-plan.test.ts`
- `tools/backlog/pr-publication-plan.ts`

**Message:**

```
fix(backlog): keep PR planner refs branch-only

Address PR #2044 review feedback by rejecting normalized refs that still carry refs/* and limiting default-branch shorthand normalization to known remote shorthands. Legitimate branch names like release/main remain intact.

Checks: bun test tools/backlog/pr-publication-plan.test.ts

Checks: bun run typecheck

Checks: bunx prettier --check tools/backlog/pr-publication-plan.ts tools/backlog/pr-publication-plan.test.ts

Checks: git diff --check

Co-Authored-By: Codex <noreply@openai.com>
```
