# PR #423 -- drain(#406 + #407 post-merge): CodeQL xref + GOVERNANCE §24 truth + downstream typo

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 423 |
| Title | drain(#406 + #407 post-merge): CodeQL xref + GOVERNANCE §24 truth + downstream typo |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-04-25T03:32:35Z |
| Merged at | 2026-04-25T03:50:31Z |
| Merge commit SHA | `a0c6425e7fda77e429c79d3b048286c40461e8e3` |
| Branch | `drain/406-407-followup` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/423 |
| Changed files | 1 |
| Additions / deletions | +16 / -9 |

## Description

## Summary

Combined drain for 6 post-merge threads across two related BACKLOG rows.

**#406 (local-DB shortlist) — 3 threads:**
- 'we already backlog CodeQL installation' was a dangling xref. Replaced with accurate pointer: \`docs/INSTALLED.md\` lists CodeQL CLI as pending; \`docs/BACKLOG.md\` only has the completed CodeQL workflow.
- Same fix in composes-with section.
- 'down-stream' → 'downstream' typo.

**#407 (four-way-parity) — 3 threads:**
- Parenthetical about GOVERNANCE §24 was wrong. §24 DOES carry an explicit 'three consumers / three ways' count (dev laptop / CI runner / devcontainer). Reframed: §24's three-consumers count is the deployment-target axis; the four-way count is the shell-runtime axis. Both stay.
- Mixed role-refs + names (Otto-279 BACKLOG is history-surface — exempt; reply explains).
- Inline-code wrap was collapsed by subsequent #408/#410 refinements; no current wrap to fix.

## Test plan

- [x] Both rows updated.
- [x] Per Otto-229 / Otto-279 BACKLOG.md is a history surface — keeping names is correct.
- [ ] CI markdownlint passes.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | true |
| Total threads | 2 |
| Resolved threads | 2 |
| Unresolved threads | 0 |
| Total review comments | 4 |
| Total fix commits (touching thread paths) | 2 |

## Review threads

### Thread 1 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59jfqQ`
- **Path**: `docs/BACKLOG.md`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-25T03:35:24Z on `docs/BACKLOG.md`:10015 (association: NONE)

  <pre>
  P1: The inline code span for the install command is split across two lines (the opening backtick is on line 10013 and the closing backtick is on 10014). CommonMark inline code spans can’t contain line breaks, and this can render oddly / trip markdownlint. Keep `brew install codeql` on a single line, or switch to a fenced code block.
  ```suggestion
        CodeQL CLI as pending install (`brew install codeql`);
        when it lands the local DB format is queryable.
  ```
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-25T03:48:19Z on `docs/BACKLOG.md`:10015 (association: MEMBER)

    <pre>
    Fixed in `a924ebf` — reflowed so `brew install codeql` stays on a single line within the inline code span. Markdownlint + CommonMark-strict renderers no longer trip on the split.
    </pre>

### Thread 2 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59jfqY`
- **Path**: `docs/BACKLOG.md`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-25T03:35:24Z on `docs/BACKLOG.md`:10095 (association: NONE)

  <pre>
  P1: Pointing readers to the CodeQL workflow entry by a specific line number (“near line 4167”) is brittle in a frequently-edited file like BACKLOG.md; the referenced line will drift and the xref will eventually become wrong. Prefer a stable identifier (e.g., quote the checklist item text, or reference the section heading + the item name) instead of a line number.
  ```suggestion
      workflow" entry covers the CI side only).
  ```
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-25T03:48:21Z on `docs/BACKLOG.md`:10095 (association: MEMBER)

    <pre>
    Fixed in `a924ebf` — replaced the brittle line-number xref ("near line 4167") with a stable identifier referencing the **CodeQL workflow** checkbox item by name. Survives BACKLOG.md re-numbering.
    </pre>

## Fix commits (touching thread paths)

### `7c0e78e1a9606e68590b9f2403229254b487b282` -- 2026-04-25T03:32:21Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `docs/BACKLOG.md`

**Message:**

```
drain(#406 + #407 post-merge): xref CodeQL → INSTALLED.md, GOVERNANCE…

… §24 truth, downstream typo

Combined drain for two related rows on docs/BACKLOG.md:

#406 (local-DB shortlist):
- Replaced 'we already backlog CodeQL installation' with the
  accurate xref: docs/INSTALLED.md lists CodeQL CLI as pending
  install; BACKLOG.md only has the completed CodeQL workflow.
- Same fix in the composes-with section (line 10089) — points
  at INSTALLED.md pending-install row + notes BACKLOG's CodeQL
  workflow entry covers CI side only.
- 'down-stream' → 'downstream' typo.

#407 (four-way-parity):
- Corrected the parenthetical about GOVERNANCE §24. §24 DOES
  carry an explicit 'three consumers / three ways' count today
  (dev laptop / CI runner / devcontainer). Reframed the bullet
  to make the two-axis distinction clear: §24's three-consumers
  count is the deployment-target axis; this row's four-way
  count is the shell-runtime axis. Both stay; sweep adds the
  shell-runtime sibling note.

Mixed role-refs + names (#407 thread): per Otto-279,
docs/BACKLOG.md IS exempt from name-stripping (it's a history
surface). Reply will explain.

Inline-code wrap (#407): the original wrap was in the pre-
#408/#410 refinement; subsequent refinements already collapsed
it. Reply will note.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
```

### `a924ebfa6418757c75c09c9e90eddcba207654f3` -- 2026-04-25T03:47:59Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `docs/BACKLOG.md`

**Message:**

```
drain(#423 P1+P1 Copilot): inline-code-span line-break + brittle line…

…-number xref

Two findings on the BACKLOG row landed in #423.

P1 (line ~10013): The inline code span for the install command
was split across two lines (`\`brew install\\ncodeql\``). CommonMark
inline code can't contain line breaks; markdownlint and some
renderers misformat. Reflowed so `brew install codeql` stays on
a single line.

P1 (line ~10094): Pointed at the CodeQL workflow item by line
number (`near line 4167`), which drifts as BACKLOG.md grows.
Replaced with a stable identifier — bolded checkbox item
reference (`**CodeQL workflow** checkbox item`) — that survives
re-numbering.
```
