# PR #315 -- core: Veridicality.canonicalKey + groupByCanonical — 7th graduation (semantic canonicalization)

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 315 |
| Title | core: Veridicality.canonicalKey + groupByCanonical — 7th graduation (semantic canonicalization) |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-04-24T07:06:06Z |
| Merged at | 2026-04-24T12:39:37Z |
| Merge commit SHA | `435c2b625536829b474fd756e3034ab00280a619` |
| Branch | `feat/semantic-canonicalization-amara-graduation-7` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/315 |
| Changed files | 2 |
| Additions / deletions | +168 / -0 |

## Description

Seventh Amara-graduation — ships semantic canonicalization that groups claims about the same proposition. Composes with PR #310's antiConsensusGate once it lands. 17 tests passing.

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 5 |
| Resolved threads | 5 |
| Unresolved threads | 0 |
| Total review comments | 5 |
| Total fix commits (touching thread paths) | 1 |

## Review threads

### Thread 1 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59Ud0Y`
- **Path**: `src/Core/Veridicality.fs`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-24T07:08:50Z on `src/Core/Veridicality.fs`:142 (association: NONE)

  <pre>
  P1: New docstrings introduce direct contributor name attribution (e.g., referencing specific people/agents). Repo standing rule is to avoid names in code/docs/skills and use role-refs instead (docs/AGENT-BEST-PRACTICES.md:284-290). Please rephrase these comments to remove names while keeping the technical provenance (e.g., “bootstrap conversation”, “upstream ferry spec”, “implementation agent”).
  </pre>

### Thread 2 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59Ud0x`
- **Path**: `tests/Tests.FSharp/Algebra/Veridicality.Tests.fs`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-24T07:08:50Z on `tests/Tests.FSharp/Algebra/Veridicality.Tests.fs`:106 (association: NONE)

  <pre>
  The test helper `simpleProject` returns an all-empty 5-tuple on malformed payloads, which can silently group unrelated/invalid inputs under the same canonical key and make tests pass for the wrong reason. Prefer failing fast (e.g., throw/failwith) or assert the expected 5-part format so test failures are diagnosable.
  ```suggestion
      let parts = payload.Split('|')
      match parts with
      | [| s; p; o; t; m |] -&gt; s, p, o, t, m
      | _ -&gt;
          failwithf
              "Expected payload in 'subject|predicate|object|time|modality' format with 5 parts, but got %d parts: '%s'"
              parts.Length
              payload
  ```
  </pre>

### Thread 3 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59UuTK`
- **Path**: `src/Core/Veridicality.fs`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-24T07:27:31Z on `src/Core/Veridicality.fs`:186 (association: NONE)

  <pre>
  `canonicalKey`/`groupByCanonical` take a projector that returns a positional 5-tuple. This is easy to misuse (swapped fields compile but change meaning). Consider changing the projector type to return `CanonicalClaimKey` (or a record with named fields) to make call sites self-documenting and reduce ordering bugs.
  ```suggestion
      /// user-supplied payload-to-canonical-key projector. The
      /// projector is where domain-specific semantics live:
      /// callers parse their payload (string / record / AST /
      /// etc.) into a `CanonicalClaimKey`. This module does not
      /// prescribe how to canonicalize natural-language strings
      /// into SPO triples — that's a downstream concern (NLP /
      /// structural parsing / etc.) and different domains call
      /// it differently.
      ///
      /// Normalization is the projector's responsibility too:
      /// lowercasing, trimming, unit-unification, alias-resolving
      /// — all happen in the projector before it returns the
      /// canonical key. This module just projects.
      let canonicalKey
              (project: 'T -&gt; CanonicalClaimKey)
              (c: Claim&lt;'T&gt;)
              : CanonicalClaimKey =
          project c.Payload

      /// Group claims by canonical key. Two claims end up in
      /// the same bucket iff their projectors produce the same
      /// `CanonicalClaimKey`. Downstream code (e.g.
      /// `antiConsensusGate`) then operates per-bucket to check
      /// multi-root independence. Input order is preserved
      /// within each bucket.
      ///
      /// Returns a `Map&lt;CanonicalClaimKey, Claim&lt;'T&gt; list&gt;`.
      /// Empty input → empty map. The per-bucket list order
      /// matches the input order (Map.add + List.rev pattern).
      let groupByCanonical
              (project: 'T -&gt; CanonicalClaimKey)
  ```
  </pre>

### Thread 4 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59UuTu`
- **Path**: `src/Core/Veridicality.fs`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-24T07:27:31Z on `src/Core/Veridicality.fs`:196 (association: NONE)

  <pre>
  `groupByCanonical` does a `Map.tryFind` followed by `Map.add` for every claim, which results in two tree lookups per element. If this will run on large claim sets, consider using `Map.change` (or similar single-lookup update) to reduce overhead while keeping the same semantics and order preservation.
  </pre>

### Thread 5 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59UuT8`
- **Path**: `src/Core/Veridicality.fs`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-24T07:27:32Z on `src/Core/Veridicality.fs`:142 (association: NONE)

  <pre>
  The XML doc says `K(c) = hash(...)`, but `canonicalKey` returns an unhashed `CanonicalClaimKey` record that is used directly as a structural key. Consider rewording the comment to avoid implying hashing (or actually return a hash if that's intended), so readers don’t misunderstand the API/semantics.
  ```suggestion
      /// **CanonicalClaimKey** — the structural "aboutness"
      /// key of a claim. Two claims with the same key assert
      /// the same proposition (possibly from different
      /// sources, possibly with different
      /// weights/retractions). Two claims with different
      /// keys assert different propositions.
      ///
      /// Derived from the same semantic dimensions as Amara's
      /// 8th / 10th ferry spec
      /// (`subject`, `predicate`, `object`, `time-scope`,
      /// `modality`, `provenance-root`, `evidence-class`),
      /// but represented here as an unhashed structural key
      /// rather than a digest value, with a deliberate
      /// divergence: this key EXCLUDES `provenance-root` and
      /// `evidence-class`. Rationale: the key's purpose here
      /// is to GROUP claims about the same proposition across
      /// sources so that `antiConsensusGate` can then check
      /// independent-root cardinality. Including
      /// `provenance-root` in the key would defeat that
      /// grouping. If a future use-case needs
      /// dedupe-by-identical-source semantics, add a separate
      /// 7-field `SourceScopedCanonicalClaimKey` type rather
      /// than expanding this one.
  ```
  </pre>

## Fix commits (touching thread paths)

### `81a638981e5e2c11de362f9cc9e4083c94354a03` -- 2026-04-24T07:06:03Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `src/Core/Veridicality.fs`
- `tests/Tests.FSharp/Algebra/Veridicality.Tests.fs`

**Message:**

```
core: Veridicality.CanonicalClaimKey + canonicalKey + groupByCanonica…

…l — 7th graduation

Ships the semantic-canonicalization primitive that turns "claims
about the same proposition" into a first-class operation. Seventh
graduation under the Otto-105 cadence; composes on PR #309's
Veridicality.Claim<'T>.

Aaron Otto-112 naming confirmed: Veridicality (not
BullshitDetector); "bullshit" stays informal, programmatic surface
uses the formal term.

Attribution:
- Aaron = concept origin (bullshit-detector framing in bootstrap
  conversation; aboutness-vs-identity-of-source distinction)
- Amara = formalization (8th/10th ferries: K(c) = hash(subject,
  predicate, object, time-scope, modality, provenance-root,
  evidence-class))
- Otto = implementation with a deliberate design choice: EXCLUDE
  provenance-root from the key

Why exclude provenance-root: the key's purpose is to GROUP claims
about the same proposition across sources, so antiConsensusGate
(PR #310) can then check independent-root cardinality. Including
provenance-root in the key would defeat that grouping. If a future
use-case needs dedupe-by-identical-source, a separate 7-field
SourceScopedCanonicalClaimKey can be added.

Surface:
- Veridicality.CanonicalClaimKey record (5 fields: Subject,
  Predicate, Object, TimeScope, Modality)
- Veridicality.canonicalKey
    : ('T -> string*string*string*string*string) -> Claim<'T>
    -> CanonicalClaimKey
  User-supplied projector handles domain-specific normalization
  (lowercasing/trimming/unit-unification/alias-resolving). Module
  does not prescribe how to canonicalize natural language.
- Veridicality.groupByCanonical
    : projector -> Claim<'T> seq
    -> Map<CanonicalClaimKey, Claim<'T> list>
  Groups claims by proposition; preserves input order within each
  bucket.

Composition shape (once PR #310 lands): group first, then
antiConsensusGate per bucket. Multi-root buckets pass; single-root
buckets fail. Test "groupByCanonical produces distinct-root counts
per bucket" verifies the half that can test on main right now.

Tests (7 new, 17 total in Veridicality module, all passing):
- canonicalKey projects payload fields
- canonicalKey EXCLUDES provenance-root (two same-prop-diff-root
  claims match)
- canonicalKey distinguishes different propositions
- groupByCanonical groups same-proposition under one key
- groupByCanonical preserves input order within bucket
- groupByCanonical on empty seq returns empty map
- groupByCanonical produces distinct-root counts per bucket

Build: 0 Warning / 0 Error.

SPOF (per Otto-106): pure function + record types; no external
deps; no SPOF introduced. Caller's projector is the domain-
specific SPOF — if it drifts, canonicalization drifts.
Documented as caller responsibility in XML-doc.

Next graduation queue:
- Graph substrate (prerequisite for cartel-detection graduations)
- largestEigenvalue / modularityScore / covarianceAcceleration
  (13th + 14th ferries)
- NetworkIntegrity / composite I(x) score (12th ferry §4)
- scoreVeridicality (composite; needs ADR on formula)

Composes with:
- src/Core/Veridicality.fs Provenance + Claim<'T> (PR #309 merged)
- src/Core/Veridicality.fs antiConsensusGate (PR #310 pending;
  downstream composition enabled by this ship)
- RobustStats.robustAggregate (PR #295) for weight-combining

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
```
