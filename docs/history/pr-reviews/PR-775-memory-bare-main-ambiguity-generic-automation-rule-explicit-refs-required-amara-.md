# PR #775 -- memory(bare-main-ambiguity): generic automation rule — explicit refs required (Amara 2026-04-29)

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 775 |
| Title | memory(bare-main-ambiguity): generic automation rule — explicit refs required (Amara 2026-04-29) |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-04-29T04:51:19Z |
| Merged at | 2026-04-29T05:19:13Z |
| Merge commit SHA | `d5922d56514aac7520535092cdc9899fb02787a0` |
| Branch | `memory/bare-main-ambiguity-automation-discipline-2026-04-29` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/775 |
| Changed files | 2 |
| Additions / deletions | +445 / -0 |

## Description

Amara caught (via the autonomous-loop trace) that scripts were repeatedly hitting `fatal: main matched multiple (2) remote tracking branches` and continuing as if successful — producing wrong downstream working-tree state.

New durable memory file: generic rule applies to ANY repo with multiple remotes that have `main` (not just Zeta's LFG ↔ AceHack setup, since future-Claude might be dropped into recovery on a different multi-remote repo).

Best line: *"Bare `main` is for humans. Automation uses explicit refs."*

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | true |
| Total threads | 11 |
| Resolved threads | 7 |
| Unresolved threads | 4 |
| Total review comments | 11 |
| Total fix commits (touching thread paths) | 5 |

## Review threads

### Thread 1 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5-U_uh`
- **Path**: `memory/feedback_bare_main_ambiguity_automation_discipline_explicit_refs_required_amara_2026_04_29.md`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-29T04:54:13Z on `memory/feedback_bare_main_ambiguity_automation_discipline_explicit_refs_required_amara_2026_04_29.md`:116 (association: NONE)

  <pre>
  P1: This file states “Never use bare `main` in automation / avoid bare branch names in scripts”, but the “local main tracking origin” example uses `git switch main` (and later uses `origin/main` instead of the fully-qualified `refs/remotes/origin/main`). This is internally inconsistent and could mislead automation authors; either update the example to use fully-qualified refs (e.g., `refs/heads/main`, `refs/remotes/origin/main`) or narrow the rule text to clarify when bare `main` is acceptable (only when a local `refs/heads/main` is guaranteed to exist).
  ```suggestion
  git branch --track main refs/remotes/origin/main 2&gt;/dev/null || true
  git switch refs/heads/main
  git reset --hard refs/remotes/origin/main
  ```
  </pre>

### Thread 2 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5-U_u3`
- **Path**: `memory/feedback_bare_main_ambiguity_automation_discipline_explicit_refs_required_amara_2026_04_29.md`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-29T04:54:13Z on `memory/feedback_bare_main_ambiguity_automation_discipline_explicit_refs_required_amara_2026_04_29.md`:12 (association: NONE)

  <pre>
  P2: The “Source” section includes a `channel:` label but leaves it blank. If the channel isn’t known/important, remove the label; otherwise fill it in (e.g., “maintainer channel”, “Slack”, “email”) so the provenance is complete.
  ```suggestion
  channel: maintainer channel
  ```
  </pre>

### Thread 3 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5-U_vJ`
- **Path**: `memory/MEMORY.md`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-29T04:54:13Z on `memory/MEMORY.md`:5 (association: NONE)

  <pre>
  P1: This new MEMORY.md entry is far from “terse” (it’s an entire paragraph) and further grows an index that’s already well past the ~200-line cap described in memory/README.md. Consider trimming this bullet down to a short one-liner (link + tagline) and rely on the dedicated memory file for the full details; also consider consolidating older entries to get the index back near the documented cap.
  ```suggestion
  - [**Bare `main` is ambiguous — automation uses explicit refs (Amara, 2026-04-29)**](feedback_bare_main_ambiguity_automation_discipline_explicit_refs_required_amara_2026_04_29.md) — Multiple `main` refs make bare `main` unsafe in automation; scripts use explicit remote refs.
  ```
  </pre>

### Thread 4 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5-VDJK`
- **Path**: `memory/feedback_bare_main_ambiguity_automation_discipline_explicit_refs_required_amara_2026_04_29.md`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-04-29T05:00:37Z on `memory/feedback_bare_main_ambiguity_automation_discipline_explicit_refs_required_amara_2026_04_29.md`:384 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Remove claim that bare `git checkout main` is safe**

  This guidance is incorrect in the exact multi-remote setup described here: when `origin/main` and `acehack/main` both exist and no local `main` branch exists, `git checkout main` exits non-zero with `fatal: 'main' matched multiple (2) remote tracking branches` (Git itself suggests `--track origin/&lt;name&gt;` or setting `checkout.defaultRemote`). Stating that interactive use is "fine" can cause agents or humans to reuse the ambiguous command and reintroduce the failure mode this memory note is trying to prevent.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 5 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5-VI-z`
- **Path**: `memory/feedback_bare_main_ambiguity_automation_discipline_explicit_refs_required_amara_2026_04_29.md`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-29T05:10:23Z on `memory/feedback_bare_main_ambiguity_automation_discipline_explicit_refs_required_amara_2026_04_29.md`:3 (association: NONE)

  <pre>
  P1: The description currently states that bare `git checkout main` / `git switch main` “hits fatal … and exits non-zero” as a general behavior. That fatal only occurs when `refs/heads/main` is missing (or otherwise not resolved) and multiple remote-tracking branches match; if a local `main` exists, the bare command is typically unambiguous. Suggest tightening this sentence to make the precondition explicit so the rule reads as technically precise rather than absolute.
  ```suggestion
  description: This repo has multiple `main` refs (`refs/remotes/origin/main` and `refs/remotes/acehack/main`) per the LFG ↔ AceHack zero-divergence-fork double-hop workflow. The local working copy may also have `refs/heads/main`. When `refs/heads/main` is missing or otherwise does not resolve, bare `git checkout main` / `git switch main` in automation can hit `fatal: 'main' matched multiple (2) remote tracking branches` and exit non-zero — and many automation scripts continue past the failure as if it succeeded. Rule: in scripts, never use bare `main`; always disambiguate. Safety-belt config (`git config checkout.defaultRemote origin`) helps in interactive sessions but is NOT a substitute for explicit refs in automation. Caught by Amara 2026-04-29 after the autonomous-loop scripts hit the fatal repeatedly. Composes with the LFG-AceHack zero-divergence-fork workflow + autonomous-loop tick discipline.
  ```
  </pre>

### Thread 6 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5-VI_G`
- **Path**: `memory/feedback_bare_main_ambiguity_automation_discipline_explicit_refs_required_amara_2026_04_29.md`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-29T05:10:24Z on `memory/feedback_bare_main_ambiguity_automation_discipline_explicit_refs_required_amara_2026_04_29.md`:307 (association: NONE)

  <pre>
  P2: “Wrong (zsh nullglob may swallow this before Git sees it)” is misleading: zsh’s default failure mode on unmatched globs is typically `nomatch` (error), and the relevant option name is `NULL_GLOB` (not “nullglob”). Suggest rewording this parenthetical to accurately describe the unmatched-glob behaviors (error vs literal vs empty expansion) without attributing bash option names to zsh.
  ```suggestion
  Wrong (unmatched globs are shell-specific: zsh usually
  errors with `nomatch`, some shells keep the literal
  pattern, and zsh with `NULL_GLOB` enabled may expand it
  away before Git sees it):
  ```
  </pre>

### Thread 7 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5-VLO_`
- **Path**: `memory/feedback_bare_main_ambiguity_automation_discipline_explicit_refs_required_amara_2026_04_29.md`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-04-29T05:13:48Z on `memory/feedback_bare_main_ambiguity_automation_discipline_explicit_refs_required_amara_2026_04_29.md`:133 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Replace invalid `git switch refs/heads/main` guidance**

  This command is not valid for the non-detached `switch` mode shown here: `git switch -h` documents usage as `git switch [&lt;branch&gt;]`, and running `git switch refs/heads/main` errors with `fatal: a branch is expected` unless `--detach` is used. In any automation that copies this snippet, branch setup will fail at this line (or continue in an unexpected state if errors are not trapped), which directly undermines the memo’s goal of providing a safe, unambiguous main-branch workflow.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 8 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM5-VPWj`
- **Path**: `memory/feedback_bare_main_ambiguity_automation_discipline_explicit_refs_required_amara_2026_04_29.md`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-04-29T05:19:23Z on `memory/feedback_bare_main_ambiguity_automation_discipline_explicit_refs_required_amara_2026_04_29.md`:172 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Fetch AceHack main with explicit destination refspec**

  The AceHack snippet fetches with `git fetch acehack main` and then checks out `refs/remotes/acehack/main`, but this form can leave `refs/remotes/acehack/main` stale because the command-line refspec updates `FETCH_HEAD` while remote-tracking updates still depend on `remote.acehack.fetch` mapping. In repos with narrowed/custom fetch refspecs, the next `git switch --detach refs/remotes/acehack/main` can branch from an old commit, recreating the wrong-base-state failure this memo is trying to prevent.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 9 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM5-VPWl`
- **Path**: `memory/feedback_bare_main_ambiguity_automation_discipline_explicit_refs_required_amara_2026_04_29.md`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-04-29T05:19:23Z on `memory/feedback_bare_main_ambiguity_automation_discipline_explicit_refs_required_amara_2026_04_29.md`:133 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Guard `git branch -f main` when main is checked out**

  This sequence is described as creating/updating local `main` “unconditionally”, but `git branch -f main ...` fails with `fatal: cannot force update the branch 'main' used by worktree` when the current branch is already `main`. In automation that uses `set -e`, that aborts the flow before the subsequent `git switch main`, so the documented recovery path is brittle for a common starting state.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 10 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM5-VPZy`
- **Path**: `memory/feedback_bare_main_ambiguity_automation_discipline_explicit_refs_required_amara_2026_04_29.md`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-29T05:19:27Z on `memory/feedback_bare_main_ambiguity_automation_discipline_explicit_refs_required_amara_2026_04_29.md`:137 (association: NONE)

  <pre>
  In the "local main branch tracking origin" snippet, the earlier section emphasizes hard-stopping on base-ref setup failures, but this snippet doesn’t enable strict mode (`set -euo pipefail`) or otherwise check `git fetch` / `git branch -f` for failure. As written, a failed fetch/update could still allow the script to continue on a stale base ref. Consider either adding strict mode at the top of this snippet or adding explicit `|| exit` checks (and/or a note that the snippet assumes strict mode is already enabled).
  </pre>

### Thread 11 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM5-VPaO`
- **Path**: `memory/feedback_bare_main_ambiguity_automation_discipline_explicit_refs_required_amara_2026_04_29.md`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-29T05:19:27Z on `memory/feedback_bare_main_ambiguity_automation_discipline_explicit_refs_required_amara_2026_04_29.md`:171 (association: NONE)

  <pre>
  The AceHack example fetches `main` but then detaches from `refs/remotes/acehack/main`. Depending on the remote’s fetch refspec configuration, `git fetch acehack main` may only update `FETCH_HEAD` and leave `refs/remotes/acehack/main` stale, which would defeat the “explicit base ref” goal. Consider using an explicit refspec here as well (mirroring the origin example) so `refs/remotes/acehack/main` is guaranteed to be refreshed before switching.
  ```suggestion
  git fetch acehack refs/heads/main:refs/remotes/acehack/main --quiet
  ```
  </pre>

## Fix commits (touching thread paths)

### `1e30b4837bdeb6a3f024da9fb7f6f9d4b9d5cbe3` -- 2026-04-29T04:51:15Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `memory/MEMORY.md`
- `memory/feedback_bare_main_ambiguity_automation_discipline_explicit_refs_required_amara_2026_04_29.md`

**Message:**

```
memory(bare-main-ambiguity): generic automation discipline rule from …

…Amara 2026-04-29 packet

Amara caught (via the 04:35Z+ trace) that autonomous-loop scripts
were repeatedly hitting 'fatal: main matched multiple (2) remote
tracking branches' and then continuing past the failure as if it
succeeded — which produced wrong downstream working-tree state.

Rule (generic, applies to ANY multi-remote repo, not just Zeta):
  Bare `main` is for humans.
  Automation uses explicit refs.

Zeta-specific worked example: refs/remotes/origin/main (LFG-trunk)
and refs/remotes/acehack/main (dev-mirror) both exist per the
zero-divergence-fork double-hop workflow. Different SHAs during
in-flight work is the normal state.

Local safety-belt set: `git config checkout.defaultRemote origin`
helps interactive use but is NOT a substitute for explicit refs in
scripts.

Per Aaron's note: future-Claude dropped into recovery on a
DIFFERENT repo with multiple remotes will hit the same failure
mode, so the memory file is framed generically with Zeta as worked
example.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
```

### `fa88011c2fd547f8872f0f27f976b6f32e5472b0` -- 2026-04-29T04:58:32Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `memory/MEMORY.md`
- `memory/feedback_bare_main_ambiguity_automation_discipline_explicit_refs_required_amara_2026_04_29.md`

**Message:**

```
memory(bare-main-ambiguity): three Copilot fixes + Amara round-2 corr…

…ections (PR #775)

Three Copilot findings on PR #775:
1. P1 internal inconsistency between 'never use bare main' rule and
   the local-main-tracking-origin example using 'git switch main'.
   Fixed by adding explicit explanation: bare 'git switch main' is
   safe ONLY when refs/heads/main exists locally and tracks origin
   (Git's lookup order finds the local ref first; the multi-remote
   guessing only fires when the bare name doesn't match a local
   ref). All other refs in the example reframed to fully-qualified
   refs/remotes/origin/main form.
2. P2 blank 'channel:' label — no actual frontmatter field; the
   line was prose continuation 'forwarded by Aaron through the
   maintainer channel:' and Copilot read it as a label. Removed
   the colon-style break by inlining: 'forwarded by Aaron through
   the maintainer channel (the live Claude Code CLI conversation
   surface):'.
3. P1 MEMORY.md entry too long — trimmed from full paragraph to
   one-liner per memory/README.md ~200-line cap discipline. The
   detailed content lives in the dedicated memory file.

Amara's round-2 packet additions:
- Hard-stop pattern (set -euo pipefail + explicit exit-1 on
  base-ref failure) — the actual bug Amara caught was 'fatal
  happened, loop continued', not just bare-main ambiguity.
- Better fetch refspec form (refs/heads/main:refs/remotes/origin/main)
  to ensure the remote-tracking ref is refreshed exactly.
- Pre-canonization search evidence section — recording the
  search terms used + the closest-hit non-fit so future-Claude
  can audit the search-before-canonizing decision.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
```

### `13cb220a085c19d089b51b3c228b8da03b5ca89b` -- 2026-04-29T05:05:58Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `memory/feedback_bare_main_ambiguity_automation_discipline_explicit_refs_required_amara_2026_04_29.md`

**Message:**

```
memory(bare-main): apply 2026-04-29 multi-AI synthesis additions (Ani…

…+Gemini+Claude.ai+Alexa+Deepseek+Amara)

Four substantive additions to the bare-main rule + a stronger
distilled keeper:

1. **Defining 'stop the tick' (orchestrator vs shell-script
   distinction)**: set -euo pipefail is right for shells; the
   autonomous loop is an orchestrator that needs explicit
   failure propagation across multiple commands. Defines what
   'stop the tick' means: mark FAILED + minimal failure note +
   no commit/PR + wait for next cron cycle (preserves liveness
   while preventing fast-failure loops).

2. **Concurrency caveat for the fetch/switch pattern** (Claude.ai
   catch): the fetch+switch gives an explicit base ref but NOT a
   globally stable base across parallel ticks. For tick-history
   shards: fine. For stateful PR work: record the base SHA at
   branch-creation and verify before merge-sensitive operations.

3. **Generalized shell-glob lesson**: zsh-glob handling
   generalizes to ALL ref patterns in automation. Quote ref
   patterns so Git receives the pattern, not the shell's
   expansion.

4. **Future-consolidation threshold** (Claude.ai + Amara catch):
   when automation-failure-mode memory files reach 5-10 entries,
   create or update a consolidated index at
   docs/automation/automation-pitfalls.md. Substrate-locality >
   duplicate retrieval paths at the folder level too.

Stronger distilled keeper added:

  Topology is allowed.
  Ambiguity is expected.
  Continuing after fatal ambiguity is forbidden.

The actual immune trigger is the post-fatal continuation, not
the topology or the ambiguity — that's what makes this an
operational bug, not just multi-remote infrastructure.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
```

### `96e7adfafaaa4b104539c373cb70651fecef2d52` -- 2026-04-29T05:10:55Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `memory/feedback_bare_main_ambiguity_automation_discipline_explicit_refs_required_amara_2026_04_29.md`

**Message:**

```
memory(bare-main): fix Codex P2 — replace bare 'git switch main' with…

… explicit 'git switch refs/heads/main' (PR #775)

Codex correctly flagged that the 'with a local main tracking
origin' example used 'git switch main' (bare), which only works
AFTER refs/heads/main already exists locally. On first-run scripts
where the local branch hasn't been created yet, bare 'main' would
still hit the multi-remote ambiguity fatal.

Fix: use 'git switch refs/heads/main' explicitly. That form is
unambiguous regardless of local state — it asks Git for the
local-branch ref, never the remote-tracking namespace. Also
replaced 'git branch --track ... 2>/dev/null || true' with
'git branch -f main ...' for unconditional idempotent local
ref creation at the explicit start-point.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
```

### `ea131771ff43ec058c1f67306507d82d424dc982` -- 2026-04-29T05:16:15Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `memory/feedback_bare_main_ambiguity_automation_discipline_explicit_refs_required_amara_2026_04_29.md`

**Message:**

```
memory(bare-main): revert invalid 'git switch refs/heads/main' to bar…

…e 'git switch main' (Codex round-4 P1)

Codex correctly flagged that 'git switch refs/heads/main' (non-
detached form) is invalid: git switch in non-detached mode takes
a branch NAME, not a fully-qualified ref.

The correct sequence (which the original draft had right; my
round-3 fix overcorrected):

1. git fetch origin refs/heads/main:refs/remotes/origin/main
2. git branch -f main refs/remotes/origin/main   ← creates local
   branch at explicit ref FIRST (eliminating ambiguity)
3. git switch main                                ← safe NOW because
   refs/heads/main exists; lookup-order finds local ref first

Updated explanation to cover the three valid switch forms:
- git switch <name>            non-detached, branch-name only
- git switch --detach <ref>    detached HEAD, accepts any ref
- git checkout <name>          older form, same ambiguity concerns

For new feature branches: git switch --detach refs/remotes/origin/main
+ git switch -c <new-branch>.

This is round 4 of refinement on the bare-main rule — Codex
caught my own overcorrection. Each round catches a real edge
case the prior round missed.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
```
