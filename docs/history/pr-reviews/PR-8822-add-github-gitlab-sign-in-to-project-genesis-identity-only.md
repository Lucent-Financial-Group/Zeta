# PR #8822 -- Add GitHub + GitLab sign-in to Project Genesis (identity-only)

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 8822 |
| Title | Add GitHub + GitLab sign-in to Project Genesis (identity-only) |
| Author | `maximdolphin` (human) |
| State | MERGED |
| Created at | 2026-06-21T01:00:34Z |
| Merged at | 2026-06-21T03:09:42Z |
| Merge commit SHA | `240c55c746343e4e8a4180ca677212817be08191` |
| Branch | `claude/genesis-github-pages-deploy-x6pgl9` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/8822 |
| Changed files | 18 |
| Additions / deletions | +1062 / -142 |

## Description

## What

Adds real **Sign in with GitHub / GitLab** to the Genesis prototype. `Genesis.jsx` is **still byte-identical to the original upload** — auth is added purely as a wrapper + overlay around it.

**Identity only:** after sign-in the UI shows avatar + name + provider + sign-out. No repo access; the provider access token never reaches the browser.

## Why a backend

A static GitHub Pages site can't safely perform the OAuth code→token exchange (it needs the client **secret**, which can't ship in the bundle). So sign-in is brokered by a small **self-hosted ASP.NET Core** service (you host it "off-pod"). It does the exchange server-to-server, reads identity, and hands the frontend a short-lived **signed identity token** via a URL fragment. The flow uses only top-level redirects + a first-party `SameSite=Lax` state cookie, so **no CORS / no third-party cookies**.

## Frontend (rebuilt Vite bundle, served at `/Zeta/genesis/`)

- `_src/src/auth.js` — login/logout, capture `#token` from the URL, decode for display
- `_src/src/AuthWidget.jsx` — top-right overlay; provider buttons (inline GitHub/GitLab brand marks, since lucide v1 dropped brand icons) → signed-in chip. Styled with the prototype's own CSS variables.
- `_src/src/App.jsx` — renders `<Genesis/>` (untouched) + `<AuthWidget/>`
- `auth-config.js` — runtime backend URL, **editable post-deploy with no rebuild**

## Backend (`genesis/_src/auth-backend/`, .NET 8, Jekyll-ignored)

- `Program.cs` minimal API: `/auth/{provider}/login`, `/callback`, `/auth/me`, `/healthz`
- Hand-rolled HS256 identity token (no extra NuGet deps), redirect allowlist, CSRF state cookie, GitHub + GitLab (gitlab.com or self-managed)
- Carries its own empty `Directory.Build.props` so it does **not** inherit Zeta's strict analyzer profile; it is **not** in `Zeta.sln` and **not** built by Zeta CI
- `README.md` — full OAuth-app registration + env-var + Docker deploy checklist
- `Dockerfile` for containerized hosting

## Verified

- Backend: builds clean (0 warnings), `/healthz` ok, both providers 302 to correct authorize URLs, redirect allowlist enforced (400 off-list), unknown provider 404, `/auth/me` 401 without token.
- Frontend: rendered in a headless Chrome — Genesis renders fully, widget shows correctly in both unconfigured ("set base in auth-config.js") and configured ("Continue with GitHub/GitLab") states.

## To go live (after merge)

1. Register a GitHub OAuth App + a GitLab Application (callback `https://YOUR-BACKEND/auth/{provider}/callback`).
2. Deploy the backend with the client IDs/secrets + `Jwt__Secret` set as env vars.
3. Edit `genesis/auth-config.js`, set `base` to the backend URL. Done — no rebuild.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

---
_Generated by [Claude Code](https://claude.ai/code/session_019MCsnUpqt2nfuQaf6PFQcq)_

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 6 |
| Resolved threads | 6 |
| Unresolved threads | 0 |
| Total review comments | 6 |
| Total fix commits (touching thread paths) | 3 |

## Review threads

### Thread 1 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6LDpeR`
- **Path**: `genesis/_src/src/Genesis.jsx`
- **Replies**: 0

**Initial comment:**

- **`github-code-quality[bot]` (bot)** at 2026-06-21T01:03:13Z on `genesis/_src/src/Genesis.jsx`:8 (association: NONE)

  <pre>
  ## Unused variable, import, function or class

  Unused import EyeOff.

  ---

  &lt;p&gt;Remove the unused &lt;code&gt;EyeOff&lt;/code&gt; named import from the &lt;code&gt;lucide-react&lt;/code&gt; import list in &lt;code&gt;genesis/_src/src/Genesis.jsx&lt;/code&gt;.&lt;/p&gt;
  &lt;p&gt;Best fix without changing functionality:&lt;/p&gt;
  &lt;ul&gt;
  &lt;li&gt;Edit only the import block at the top of &lt;code&gt;Genesis.jsx&lt;/code&gt;.&lt;/li&gt;
  &lt;li&gt;In the named import list, delete &lt;code&gt;EyeOff&lt;/code&gt; and keep all other imports unchanged.&lt;/li&gt;
  &lt;li&gt;No additional methods, definitions, or dependencies are required.&lt;/li&gt;
  &lt;/ul&gt;
  </pre>

### Thread 2 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6LDpeT`
- **Path**: `genesis/_src/src/Genesis.jsx`
- **Replies**: 0

**Initial comment:**

- **`github-code-quality[bot]` (bot)** at 2026-06-21T01:03:13Z on `genesis/_src/src/Genesis.jsx`:284 (association: NONE)

  <pre>
  ## Unused variable, import, function or class

  Unused variable setCredits.

  ---

  The best fix is to keep the &lt;code&gt;credits&lt;/code&gt; state value (since it is used) and remove only the unused setter from the destructuring assignment.&lt;/p&gt;
  &lt;p&gt;In &lt;code&gt;genesis/_src/src/Genesis.jsx&lt;/code&gt;, update the &lt;code&gt;useState(1420)&lt;/code&gt; line so it destructures only the first tuple element. In React, this is valid as &lt;code&gt;const [credits] = useState(1420);&lt;/code&gt; when no updates are needed. No imports, methods, or additional definitions are required.
  </pre>

### Thread 3 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6LDphO`
- **Path**: `genesis/_src/auth-backend/Program.cs`
- **Replies**: 0

**Initial comment:**

- **`github-code-quality[bot]` (bot)** at 2026-06-21T01:03:29Z on `genesis/_src/auth-backend/Program.cs`:79 (association: NONE)

  <pre>
  ## Useless assignment to local variable

  This assignment to &lt;a class="Link" href="/Lucent-Financial-Group/Zeta/blob/e44402d02672c6c135d0ab32459b60bb30163b89/genesis/_src/auth-backend/Program.cs#L79-L79"&gt;jsonOpts&lt;/a&gt; is useless, since its value is never read.

  ---

  To fix this without changing functionality, remove the unused local variable declaration/assignment for &lt;code&gt;jsonOpts&lt;/code&gt; in &lt;code&gt;genesis/_src/auth-backend/Program.cs&lt;/code&gt;.&lt;/p&gt;
  &lt;p&gt;Best single change:&lt;/p&gt;
  &lt;ul&gt;
  &lt;li&gt;In &lt;code&gt;genesis/_src/auth-backend/Program.cs&lt;/code&gt;, delete the line:&lt;br&gt;
  &lt;code&gt;var jsonOpts = new JsonSerializerOptions(JsonSerializerDefaults.Web);&lt;/code&gt;&lt;/li&gt;
  &lt;/ul&gt;
  &lt;p&gt;No imports, methods, or additional definitions are required.
  </pre>

### Thread 4 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6LDphS`
- **Path**: `genesis/_src/auth-backend/Program.cs`
- **Replies**: 0

**Initial comment:**

- **`github-code-quality[bot]` (bot)** at 2026-06-21T01:03:29Z on `genesis/_src/auth-backend/Program.cs`:161 (association: NONE)

  <pre>
  ## Missing Dispose call on local IDisposable

  Disposable 'HttpRequestMessage' is created but not disposed.

  ---

  Use deterministic disposal for locally created &lt;code&gt;HttpRequestMessage&lt;/code&gt; instances.&lt;/p&gt;
  &lt;p&gt;Best fix in this file: change the token exchange request creation from &lt;code&gt;var tokenReq = new HttpRequestMessage(...)&lt;/code&gt; to &lt;code&gt;using var tokenReq = new HttpRequestMessage(...)&lt;/code&gt; so it is disposed at scope exit even on early returns/exceptions.&lt;br&gt;
  In the shown snippet, this is in &lt;code&gt;genesis/_src/auth-backend/Program.cs&lt;/code&gt; around line 151.&lt;/p&gt;
  &lt;p&gt;No new imports, helper methods, or external dependencies are required.
  </pre>

### Thread 5 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6LDphV`
- **Path**: `genesis/_src/auth-backend/Program.cs`
- **Replies**: 0

**Initial comment:**

- **`github-code-quality[bot]` (bot)** at 2026-06-21T01:03:29Z on `genesis/_src/auth-backend/Program.cs`:174 (association: NONE)

  <pre>
  ## Missing Dispose call on local IDisposable

  Disposable 'HttpRequestMessage' is created but not disposed.

  ---

  Use deterministic disposal for the &lt;code&gt;HttpRequestMessage&lt;/code&gt; created for the user info call.&lt;/p&gt;
  &lt;p&gt;Best fix (no behavior change): in &lt;code&gt;genesis/_src/auth-backend/Program.cs&lt;/code&gt;, change the &lt;code&gt;userReq&lt;/code&gt; declaration at the user-info section from a plain &lt;code&gt;var&lt;/code&gt; to &lt;code&gt;using var&lt;/code&gt;. Keep all existing header assignments and request flow unchanged. This ensures &lt;code&gt;Dispose()&lt;/code&gt; is called automatically when the scope exits, including on early returns/exceptions.&lt;/p&gt;
  &lt;p&gt;No new methods, imports, or dependencies are required.
  </pre>

### Thread 6 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6LDphX`
- **Path**: `genesis/_src/auth-backend/Program.cs`
- **Replies**: 0

**Initial comment:**

- **`github-code-quality[bot]` (bot)** at 2026-06-21T01:03:29Z on `genesis/_src/auth-backend/Program.cs`:245 (association: NONE)

  <pre>
  ## Missed opportunity to use Where

  This foreach loop &lt;a class="Link" href="/Lucent-Financial-Group/Zeta/blob/e44402d02672c6c135d0ab32459b60bb30163b89/genesis/_src/auth-backend/Program.cs#L244-L244"&gt;implicitly filters its target sequence&lt;/a&gt; - consider filtering the sequence explicitly using '.Where(...)'.

  ---

  Refactor &lt;code&gt;ResolveRedirect&lt;/code&gt; in &lt;code&gt;genesis/_src/auth-backend/Program.cs&lt;/code&gt; so the membership check uses LINQ filtering explicitly instead of a &lt;code&gt;foreach&lt;/code&gt; with an &lt;code&gt;if&lt;/code&gt; and early return.&lt;/p&gt;
  &lt;p&gt;Best single fix (without changing functionality):&lt;/p&gt;
  &lt;ul&gt;
  &lt;li&gt;Replace lines 243–246 (the &lt;code&gt;foreach&lt;/code&gt; block and trailing &lt;code&gt;return null&lt;/code&gt;) with a LINQ-based check over &lt;code&gt;allowedOrigins&lt;/code&gt;.&lt;/li&gt;
  &lt;li&gt;Use &lt;code&gt;.Where(...)&lt;/code&gt; to normalize/filter matching origins, then &lt;code&gt;.Any()&lt;/code&gt; to test if a match exists.&lt;/li&gt;
  &lt;li&gt;Return &lt;code&gt;requested&lt;/code&gt; if any match exists; otherwise return &lt;code&gt;null&lt;/code&gt;.&lt;/li&gt;
  &lt;/ul&gt;
  &lt;p&gt;No new external dependencies are needed. You will need &lt;code&gt;System.Linq&lt;/code&gt; available; since we should only edit shown code and avoid changing imports unless necessary, use &lt;code&gt;System.Linq.Enumerable.Where(...)&lt;/code&gt; form to avoid requiring a new &lt;code&gt;using&lt;/code&gt;.
  </pre>

## Fix commits (touching thread paths)

### `b61b360d2a9e71092a22c45640be4bb07f4b1933` -- 2026-06-20T23:20:27Z -- `claude`

**Touched files (intersect with thread paths):**

- `genesis/_src/src/Genesis.jsx`

**Message:**

```
Deploy Project Genesis prototype as a /genesis/ subfolder of the Page…

…s site

Stand up the interactive "Project Genesis" React prototype as a built Vite
static site, served as a subfolder of the EXISTING Jekyll GitHub Pages
deployment (root index.html + demo/) rather than replacing it — the Pages
site is the factory's DORA signal and a family-edited surface, so it is left
untouched. The Vite build emits with base=/Zeta/genesis/; Jekyll serves the
static genesis/index.html + genesis/assets/* verbatim (no front matter).

Live at: https://lucent-financial-group.github.io/Zeta/genesis/ once on main.

- genesis/                : built static site (served)
- genesis/_src/           : reproducible Vite source (Jekyll-ignored via the
                            leading underscore); Genesis.jsx is committed
                            UNCHANGED from the original upload
- .gitattributes          : mark genesis/assets/** generated (linguist + -diff)
- .github/codeql/codeql-config.yml, .semgrepignore : exclude the minified
                            bundle / prototype from scanners, matching the
                            existing bench/ and references/prior-art/ pattern

Component contents are treated as final and were not modified; only the Vite
wrapper (main.jsx, index.css, index.html, vite.config.js) is project config.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_019MCsnUpqt2nfuQaf6PFQcq
```

### `e44402d02672c6c135d0ab32459b60bb30163b89` -- 2026-06-21T00:59:50Z -- `claude`

**Touched files (intersect with thread paths):**

- `genesis/_src/auth-backend/Program.cs`

**Message:**

```
Add GitHub + GitLab sign-in to Project Genesis (identity-only)

Adds real OAuth "Sign in with GitHub / GitLab" to the Genesis prototype
without modifying Genesis.jsx (still byte-identical to the original upload).

Because a static Pages site can't safely hold an OAuth client secret, sign-in
is brokered by a small self-hosted ASP.NET Core service (genesis/_src/auth-
backend/, Jekyll-ignored). It does the code->token exchange server-to-server,
reads the user's public identity, and hands the frontend a short-lived signed
identity token via a URL fragment. The provider access token never reaches the
browser. Flow uses only top-level redirects + a first-party SameSite=Lax state
cookie, so no CORS or third-party cookies are required.

Frontend (rebuilt Vite bundle):
- src/auth.js      : login/logout, capture #token from URL, decode for display
- src/AuthWidget.jsx: top-right overlay; sign-in buttons (inline GitHub/GitLab
                     brand marks — lucide v1 dropped brand icons) -> after login
                     shows avatar + name + provider + sign-out. Styled with the
                     prototype's own CSS variables.
- src/App.jsx      : renders <Genesis/> (untouched) + <AuthWidget/>
- auth-config.js   : runtime backend URL, editable post-deploy (no rebuild)

Backend (genesis/_src/auth-backend/, .NET 8, containerized):
- Program.cs minimal API: /auth/{provider}/login, /callback, /auth/me, /healthz
- hand-rolled HS256 identity token (no extra NuGet deps), redirect allowlist,
  CSRF state cookie. Carries its own empty Directory.Build.props so it does NOT
  inherit Zeta's strict analyzer profile; not part of Zeta.sln, not built by CI.
- README.md: full OAuth-app registration + env + deploy checklist.

Verified: backend builds clean (0 warnings) and serves correct provider
redirects/allowlist/JWT checks; frontend renders in a headless browser with
the widget in both configured and unconfigured states.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_019MCsnUpqt2nfuQaf6PFQcq
```

### `7ad9e936fdb75629969eac57275324df38a60966` -- 2026-06-21T03:07:06Z -- `claude`

**Touched files (intersect with thread paths):**

- `genesis/_src/auth-backend/Program.cs`

**Message:**

```
auth-backend: clear code-quality review comments

- Remove unused `jsonOpts` local + the now-unused System.Text.Json using.
- Dispose both HttpRequestMessage instances via `using var`.
- Replace the allowlist `foreach` with LINQ `.Any(...)`.

Builds 0 warnings. (markdownlint MD032 on the README was already auto-healed
on the branch by CI.) Genesis.jsx is left untouched (its flagged unused
EyeOff/setCredits are in the original upload, treated as final).

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_019MCsnUpqt2nfuQaf6PFQcq
```
