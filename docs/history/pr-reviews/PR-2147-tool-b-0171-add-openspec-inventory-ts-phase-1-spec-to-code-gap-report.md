# PR #2147 -- tool(B-0171): add openspec/inventory.ts — Phase 1 spec-to-code gap report

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 2147 |
| Title | tool(B-0171): add openspec/inventory.ts — Phase 1 spec-to-code gap report |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-05-08T22:56:20Z |
| Merged at | 2026-05-08T23:11:46Z |
| Merge commit SHA | `88d6509ba2cb3a09690889b3ae7985d86463d138` |
| Branch | `claim/task-b0171-openspec-inventory-tool` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/2147 |
| Changed files | 3 |
| Additions / deletions | +550 / -7 |

## Description

## Summary

- **New tool**: `tools/openspec/inventory.ts` — scans `openspec/specs/` and `src/Core/` to produce a structured JSON gap report mapping behavioural specs to code modules. Mechanizes Phase 1 (inventory + sequencing) of B-0171.
- **README fix**: `openspec/README.md` capabilities section was stale — listed 3 specs when 6 exist; listed `storage-spines` and `circuit-scheduling` as "planned" when `lsm-spine-family` and `circuit-recursion` already cover them.
- **12 tests** covering scanners, gap analysis, mapping table integrity, and real-repo integration.

### Current inventory output

```
Specs:             6
Core modules:      80
Covered modules:   18
Uncovered modules: 60
Coverage:          23.1%
```

The 60 uncovered modules are the Phase 2 candidate list for new specs. Run `bun tools/openspec/inventory.ts` for the full gap report.

## Test plan

- [x] `bun test tools/openspec/inventory.test.ts` — 12/12 pass
- [x] `bun tools/openspec/inventory.ts` — produces structured JSON + human summary
- [x] `dotnet build -c Release` — 0 warnings, 0 errors

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | true |
| Total threads | 14 |
| Resolved threads | 14 |
| Unresolved threads | 0 |
| Total review comments | 29 |
| Total fix commits (touching thread paths) | 4 |

## Review threads

### Thread 1 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6Awf66`
- **Path**: `tools/openspec/inventory.ts`
- **Replies**: 1

**Initial comment:**

- **`github-code-quality[bot]` (bot)** at 2026-05-08T22:57:38Z on `tools/openspec/inventory.ts`:13 (association: NONE)

  <pre>
  ## Unused variable, import, function or class

  Unused import basename.

  ---

  The general fix is to remove unused imported symbols from import declarations, leaving only the names actually referenced in the file.&lt;/p&gt;
  &lt;p&gt;For this file, update &lt;code&gt;tools/openspec/inventory.ts&lt;/code&gt; at the import section (line 13 in the snippet): change &lt;code&gt;import { basename, join, resolve } from "node:path";&lt;/code&gt; to &lt;code&gt;import { join, resolve } from "node:path";&lt;/code&gt;. No additional methods, definitions, or dependencies are needed, and functionality remains unchanged.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-08T23:00:12Z on `tools/openspec/inventory.ts`:13 (association: MEMBER)

    <pre>
    Fixed in 1a85283 — removed unused `basename` import.
    </pre>

### Thread 2 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6AwgVr`
- **Path**: `tools/openspec/inventory.ts`
- **Replies**: 1

**Initial comment:**

- **`github-advanced-security[bot]` (bot)** at 2026-05-08T22:58:18Z on `tools/openspec/inventory.ts`:125 (association: NONE)

  <pre>
  ## CodeQL / Potential file system race condition

  The file may have changed since it [was checked](1).

  [Show more details](https://github.com/Lucent-Financial-Group/Zeta/security/code-scanning/67)
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-08T23:04:26Z on `tools/openspec/inventory.ts`:125 (association: MEMBER)

    <pre>
    Fixed in af317d4 — eliminated the stat+read TOCTOU pattern. Now reads the file directly inside a try/catch, removing the race window.
    </pre>

### Thread 3 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6AwgdV`
- **Path**: `tools/openspec/inventory.ts`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-08T22:58:33Z on `tools/openspec/inventory.ts`:204 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Compute coverage denominator from present modules**

  `totalAnalyzable` subtracts `EXCLUDED_MODULES.size` unconditionally, so coverage is miscomputed whenever one or both excluded filenames are absent in the scanned module set (e.g., temp fixtures, partial trees, or future refactors). In those cases the denominator can become too small or even non-positive, producing inflated percentages or a forced `0%` despite covered modules being present; derive the denominator by filtering actual `modules` against `EXCLUDED_MODULES` instead.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-08T23:04:26Z on `tools/openspec/inventory.ts`:204 (association: MEMBER)

    <pre>
    Fixed in af317d4 — denominator now counts only actually-present excluded modules: `modules.filter(m =&gt; EXCLUDED_MODULES.has(m.name)).length`. Added a dedicated test case.
    </pre>

### Thread 4 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6AwhAM`
- **Path**: `tools/openspec/inventory.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-08T22:59:49Z on `tools/openspec/inventory.ts`:106 (association: NONE)

  <pre>
  `findRepoRoot()` uses `__dirname`, but this repo’s TS is ESM (`module: Preserve`), and other tooling uses `import.meta.dir`. Using `__dirname` risks a typecheck/runtime failure under Bun ESM. Replace this with `resolve(import.meta.dir)` (or equivalent `new URL('.', import.meta.url)`), and adjust the traversal accordingly.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-08T23:04:26Z on `tools/openspec/inventory.ts`:106 (association: MEMBER)

    <pre>
    Fixed in af317d4 — replaced `__dirname` with `import.meta.dir` throughout.
    </pre>

### Thread 5 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6AwhAf`
- **Path**: `tools/openspec/inventory.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-08T22:59:50Z on `tools/openspec/inventory.ts`:7 (association: NONE)

  <pre>
  The file header says `openspec/inventory.ts`, but the actual path is `tools/openspec/inventory.ts`. This is misleading when users try to run or locate the tool; update the header to match the real location.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-08T23:04:26Z on `tools/openspec/inventory.ts`:7 (association: MEMBER)

    <pre>
    Fixed in af317d4 — header now reads `tools/openspec/inventory.ts`.
    </pre>

### Thread 6 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6AwhAo`
- **Path**: `tools/openspec/inventory.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-08T22:59:50Z on `tools/openspec/inventory.ts`:208 (association: NONE)

  <pre>
  Coverage percent denominator is computed as `modules.length - EXCLUDED_MODULES.size`, which is wrong if one or more excluded module names are not present in `modules` (it will inflate coverage). Compute the denominator by filtering `modules` against `EXCLUDED_MODULES` instead (count excluded *present*), so the percentage is correct for arbitrary inputs and unit tests.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-08T23:04:26Z on `tools/openspec/inventory.ts`:208 (association: MEMBER)

    <pre>
    Fixed in af317d4 — same fix as the P2 thread: denominator derived from actual module set, not the constant `EXCLUDED_MODULES.size`.
    </pre>

### Thread 7 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6AwhA1`
- **Path**: `tools/openspec/inventory.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-08T22:59:50Z on `tools/openspec/inventory.ts`:145 (association: NONE)

  <pre>
  `scanSpecs(specsDir)` returns `specPath` as `openspec/specs/&lt;cap&gt;/spec.md` regardless of the `specsDir` passed in. This makes the function incorrect for callers that scan a different tree (including these unit tests) and makes the API misleading. Consider returning the actual path (absolute or based on `specsDir`) and only converting to repo-relative paths in `main()` when scanning the real repo.

  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-08T23:04:26Z on `tools/openspec/inventory.ts`:145 (association: MEMBER)

    <pre>
    Fixed in af317d4 — `scanSpecs` now returns the actual filesystem path. Repo-relative conversion happens in `main()` only.
    </pre>

### Thread 8 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6AwhA7`
- **Path**: `tools/openspec/inventory.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-08T22:59:51Z on `tools/openspec/inventory.ts`:169 (association: NONE)

  <pre>
  Similarly, `scanModules(coreDir)` hardcodes `path: src/Core/&lt;file&gt;` instead of reflecting `coreDir`. This is surprising for a function that takes an explicit directory argument and makes results wrong when scanning non-repo fixtures. Consider returning the real file path (or a path relative to `coreDir`) and formatting repo-relative paths only at the CLI layer.

  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-08T23:04:26Z on `tools/openspec/inventory.ts`:169 (association: MEMBER)

    <pre>
    Fixed in af317d4 — `scanModules` now returns the actual filesystem path via `join(coreDir, file)`. Repo-relative conversion in `main()` only.
    </pre>

### Thread 9 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6AwhBF`
- **Path**: `tools/openspec/inventory.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-08T22:59:51Z on `tools/openspec/inventory.ts`:119 (association: NONE)

  <pre>
  `readdirSync` results (spec dirs, profiles, and core files) are not sorted, so the JSON output ordering can vary by filesystem/OS, making diffs and snapshotting noisy. Sort the directory/file lists (and profile list) before emitting entries to keep the report deterministic.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-08T23:04:26Z on `tools/openspec/inventory.ts`:119 (association: MEMBER)

    <pre>
    Fixed in af317d4 — all `readdirSync` results (spec dirs, module files, profiles) are now `.sort()`-ed. Added sort-order test cases.
    </pre>

### Thread 10 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6AwhBN`
- **Path**: `tools/openspec/inventory.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-08T22:59:51Z on `tools/openspec/inventory.ts`:197 (association: NONE)

  <pre>
  In `buildGapReport`, mapping entries for modules that don’t exist are silently dropped (`moduleNames.filter(...)`). That hides typos/drift in `CAPABILITY_MODULE_MAP`—the tool will look healthy while coverage silently decreases. Consider reporting missing module names (or failing a test) so mapping table mistakes are visible.

  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-08T23:04:26Z on `tools/openspec/inventory.ts`:197 (association: MEMBER)

    <pre>
    Fixed in af317d4 — `Mapping` type now includes `missingModules: string[]`. `main()` prints them under 'Mapped modules not found in src/Core/ (possible drift)'. Added test case.
    </pre>

### Thread 11 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6AwhBg`
- **Path**: `tools/openspec/inventory.test.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-08T22:59:51Z on `tools/openspec/inventory.test.ts`:233 (association: NONE)

  <pre>
  The integration tests use `__dirname` to locate the repo root. In this repo’s ESM TypeScript setup, prefer `import.meta.dir` (as used elsewhere) to avoid `__dirname` not being defined under Bun ESM and to keep the tests consistent with the rest of the tooling.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-08T23:04:26Z on `tools/openspec/inventory.test.ts`:233 (association: MEMBER)

    <pre>
    Fixed in af317d4 — test file now uses `import.meta.dir` consistent with the rest of the tooling.
    </pre>

### Thread 12 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6Awks6`
- **Path**: `tools/openspec/inventory.ts`
- **Replies**: 2

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-08T23:05:47Z on `tools/openspec/inventory.ts`:190 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Gate covered modules on specs that are present**

  `buildGapReport` marks modules as covered for every capability in `CAPABILITY_MODULE_MAP` even when that capability's `spec.md` is missing from the scanned `openspec/specs` tree. In that scenario (e.g., a spec deleted/renamed), the report will still show those modules as covered and inflate `coveragePercent`, which defeats this tool’s stated purpose of detecting spec-to-code gaps and hides real spec drift.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-08T23:09:34Z on `tools/openspec/inventory.ts`:190 (association: MEMBER)

    <pre>
    Fixed in 6f49b457 — buildGapReport now gates covered-module classification on spec presence. Modules only enter coveredSet when specCapabilities.has(capability) is true, so deleted/renamed specs no longer inflate coveragePercent. Added dedicated test case for zero-specs-means-zero-coverage invariant.
    </pre>
  - **`AceHack` (human)** at 2026-05-08T23:10:17Z on `tools/openspec/inventory.ts`:190 (association: MEMBER)

    <pre>
    Already fixed in this same commit (6f49b457) — buildGapReport now creates specCapabilities at line 183 and gates coveredSet.add behind specCapabilities.has(capability) at line 191. The exact fix this finding describes.
    </pre>

### Thread 13 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6AwlU9`
- **Path**: `tools/openspec/inventory.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-08T23:07:05Z on `tools/openspec/inventory.ts`:196 (association: NONE)

  <pre>
  `buildGapReport(specs, modules)` computes `coveredModules`/`coveragePercent` solely from `CAPABILITY_MODULE_MAP` and does not gate coverage on the capabilities actually present in the `specs` argument. This can produce internally inconsistent reports (e.g., `specs.length === 0` but non-zero coverage) if spec scanning fails or a spec directory is missing. Consider intersecting the mapping keys with the scanned `specs` capabilities (and/or emitting a separate `mappingsWithoutSpec` drift list) so coverage reflects actual spec presence.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-08T23:10:38Z on `tools/openspec/inventory.ts`:196 (association: MEMBER)

    <pre>
    Fixed in 6f49b457 — buildGapReport now builds specCapabilities from the specs array and only adds to coveredSet when specCapabilities.has(capability). This is exactly the fix described here.
    </pre>

### Thread 14 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6AwlVD`
- **Path**: `tools/openspec/inventory.test.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-08T23:07:06Z on `tools/openspec/inventory.test.ts`:64 (association: NONE)

  <pre>
  These tests manually `rmSync(root, { recursive: true })` at the end of each test body. If an assertion fails before cleanup runs, temp directories will be leaked on the developer machine. Consider using `try/finally` around each test’s body or a shared `afterEach` hook to ensure cleanup always executes.

  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-08T23:10:40Z on `tools/openspec/inventory.test.ts`:64 (association: MEMBER)

    <pre>
    Acknowledged — temp dirs in /tmp/ are harmless if leaked, and Bun test runner exits cleanly. Low-priority improvement; not blocking.
    </pre>

## Fix commits (touching thread paths)

### `645dbd10e5fcbd74db060424cfc803e9ac8079ef` -- 2026-05-08T22:56:06Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/openspec/inventory.test.ts`
- `tools/openspec/inventory.ts`

**Message:**

```
tool(B-0171): add openspec/inventory.ts — Phase 1 spec-to-code gap re…

…port

Smallest safe slice of B-0171 (OpenSpec catch-up): mechanize the
Phase 1 inventory so the gap between specs and code is visible and
re-runnable.

- tools/openspec/inventory.ts: scans openspec/specs/ and src/Core/,
  emits structured JSON gap report (6 specs, 80 modules, 23.1%
  coverage today).
- tools/openspec/inventory.test.ts: 12 tests covering scanners,
  gap analysis, mapping table integrity, and real-repo integration.
- openspec/README.md: fix capabilities drift — was "Three" but 6
  exist; remove stale planned entries (storage-spines already
  covered by lsm-spine-family, circuit-scheduling by circuit-
  recursion); add pointer to inventory tool.

Co-Authored-By: Claude Opus 4.6 (1M context) <noreply@anthropic.com>
```

### `1a85283c5b2fdd62a79df0e9763cf221d687d023` -- 2026-05-08T22:59:58Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/openspec/inventory.ts`

**Message:**

```
fix(B-0171): remove unused basename import — fixes tsc lint

TS6133 flagged by CI lint (tsc tools) and github-code-quality bot review thread.

Co-Authored-By: Claude Opus 4.6 (1M context) <noreply@anthropic.com>
```

### `af317d4b03c4e8815967d2bf9f1ab5af9bc18b9d` -- 2026-05-08T23:03:50Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/openspec/inventory.test.ts`
- `tools/openspec/inventory.ts`

**Message:**

```
fix(B-0171): address all review findings — ESM compat, determinism, c…

…overage denominator

- Replace __dirname with import.meta.dir (ESM/Bun native)
- Eliminate TOCTOU race: read file directly instead of stat+read
- Sort readdirSync results for deterministic output
- Fix coverage denominator: count only actually-present excluded modules
- Return actual filesystem paths from scan functions; convert to
  repo-relative in main() only
- Report missing mapped modules (drift detection)
- Fix file header path to match actual location
- Add tests for sort order, coverage denominator, missing modules

Co-Authored-By: Claude Opus 4.6 (1M context) <noreply@anthropic.com>
```

### `6f49b457bc2415cab796d14bd5bc62019a59db5a` -- 2026-05-08T23:09:19Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/openspec/inventory.test.ts`
- `tools/openspec/inventory.ts`

**Message:**

```
fix(B-0171): gate covered-module classification on spec presence

buildGapReport marked modules as covered based on CAPABILITY_MODULE_MAP
alone, regardless of whether the corresponding spec actually existed in
the scanned specs directory. This inflated coveragePercent when a spec
was deleted or renamed — exactly the drift this tool is meant to detect.

Now modules enter coveredSet only when specCapabilities.has(capability)
is true. The mapping table is still fully recorded for drift detection.
Added test case for the zero-specs-means-zero-coverage invariant.

Addresses Codex P1 review finding on PR #2147.

Co-Authored-By: Claude Opus 4.6 (1M context) <noreply@anthropic.com>
```
