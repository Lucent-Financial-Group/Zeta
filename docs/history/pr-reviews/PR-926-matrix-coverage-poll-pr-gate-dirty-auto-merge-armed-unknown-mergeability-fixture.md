# PR #926 -- matrix-coverage(poll-pr-gate): dirty-auto-merge-armed + unknown-mergeability fixtures

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 926 |
| Title | matrix-coverage(poll-pr-gate): dirty-auto-merge-armed + unknown-mergeability fixtures |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-04-30T17:27:10Z |
| Merged at | 2026-04-30T17:29:21Z |
| Merge commit SHA | `6822f169956aafd0e4964fa956e54c4294ffb816` |
| Branch | `matrix-coverage/poll-pr-gate-dirty-and-unknown-fixtures-2026-04-30` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/926 |
| Changed files | 2 |
| Additions / deletions | +25 / -0 |

## Description

## Summary

Two new fixture cases for `tools/github/poll-pr-gate.ts`, both observed in the live LFG PR queue during the 2026-04-30 stale-PR triage round (task #356):

1. **`dirty-auto-merge-armed.json`** — `mergeStateStatus=DIRTY` with `autoMergeRequest` set. Validates that auto-merge being armed does **not** short-circuit the rebase requirement when content collides. Real instances: PRs #752, #739 in this round (both closed as superseded — the on-main path already had a different commit's content).

2. **`unknown-mergeability-pending-recompute.json`** — `mergeStateStatus=UNKNOWN`. Models PRs from prior rounds where GitHub hasn't recomputed mergeability (no push / no comment / no triggering event). Tool classifies `gate=UNKNOWN`, `nextAction=none`.

## Matrix gap surfaced (intentionally documented)

The `UNKNOWN` + `nextAction=none` result is a known gap — the tool has no actionable nudge for "GitHub hasn't recomputed yet." A v2 could route `UNKNOWN` to a distinct `nudge-recompute` action (push no-op commit, comment, etc.). For now, the fixture makes the gap visible. Tracked under task #355.

## Test plan

- [x] Both fixtures verified via `bun tools/github/poll-pr-gate.ts --fixture <path>` — classify as expected (`DIRTY`/`rebase` and `UNKNOWN`/`none`).
- [x] All 5 prior fixtures still classify identically (regression check ran inline).
- [x] No tool changes — fixtures-only PR; behavior unchanged for live queries.

## Lineage

Poll-the-gate executable substrate landed earlier this session per the 5-AI peer-reviewer convergence on 2026-04-30. Fixture matrix extension follows the carved blade from that packet: *"if the loop uses it every tick, it deserves tests."*

Both fixtures correspond to states observed in the actual queue, not speculative edge cases. This is the evidence-first growth shape — fixtures track real-world cases the loop encountered.

## Composes with

- `memory/feedback_amara_poll_gate_not_ending_holding_is_not_status_2026_04_30.md` — the prose blade this tool operationalizes
- `docs/research/2026-04-30-amara-poll-pr-gate-v1-hardening.md` — v1 hardening packet that drove the required-vs-non-required split
- task #355 — matrix coverage extension tracker
- task #356 — stale-PR triage round that surfaced these cases

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 0 |
| Resolved threads | 0 |
| Unresolved threads | 0 |
| Total review comments | 0 |
| Total fix commits (touching thread paths) | 0 |

## Review threads

(no review threads on this PR)

## Fix commits (touching thread paths)

(no commits in this PR touched files anchored by review threads)
