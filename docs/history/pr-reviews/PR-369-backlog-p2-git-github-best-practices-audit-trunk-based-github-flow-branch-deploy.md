# PR #369 -- backlog: P2 — git/github best-practices audit (trunk-based + GitHub Flow + branch-deploy)

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 369 |
| Title | backlog: P2 — git/github best-practices audit (trunk-based + GitHub Flow + branch-deploy) |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-04-24T14:23:41Z |
| Merged at | 2026-04-24T14:25:26Z |
| Merge commit SHA | `f58a18b2ccfe4f8165642ddb1a29d8557113e26a` |
| Branch | `backlog/git-github-best-practices-research-otto-233` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/369 |
| Changed files | 1 |
| Additions / deletions | +2 / -0 |

## Description

## Summary

Otto-233 maintainer directive: research-backlog row for trunk-based development verification + GitHub Flow audit + GitHub's official `branch-deploy` pattern study. Intended for future execution; not urgent for the starboard UI v1 but important as the repo matures into deployable services.

## Three axes

1. **Trunk-based development** — verify we are following (initial read: yes — main protected, squash-merge default, short-lived branches, feature-flag / auto-merge gated)
2. **GitHub Flow** — audit our actual flow (branch from main → commit → PR → auto-merge via CI+review gates) and document deviations + rationale (parallel drain-subagents, Otto-229 append-only, etc.)
3. **GitHub's `branch-deploy`** — study the official branch-deployment pattern; assess fit for the future deployable-service layer (overkill for starboard v1 per maintainer)

## Deliverable

`docs/research/git-github-best-practices-audit-otto-233.md` with verified-against-current-repo grade per dimension + gap list + any follow-up rows worth elevating.

## Constraints

- Does NOT authorize changing merge discipline / deploy flow pre-research
- Research produces a recommendation; maintainer decides adoption
- Append-only BACKLOG edit; zero existing-row edits (Otto-229 honored)

## Test plan

- [x] Row appended at end of `## P2 — research-grade` section, before `## P2 — Rule-Zero axiomatic substrate` heading
- [x] No existing BACKLOG row edits
- [x] Verbatim maintainer quote preserved inside italics

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 1 |
| Resolved threads | 0 |
| Unresolved threads | 1 |
| Total review comments | 1 |
| Total fix commits (touching thread paths) | 1 |

## Review threads

### Thread 1 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM59bFMZ`
- **Path**: `docs/BACKLOG.md`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-24T14:26:26Z on `docs/BACKLOG.md`:6822 (association: NONE)

  <pre>
  P1: This new BACKLOG row references Otto-215 and Otto-229, but those identifiers don’t appear anywhere else in the repo (and Otto-223 only appears in a hygiene-history log). Consider replacing these with resolvable references (e.g., links to the relevant BACKLOG rows/sections or specific file paths), or add the missing referenced artefacts, so readers can actually follow the dependency chain.
  ```suggestion
  - [ ] **Git / GitHub best-practices audit — trunk-based development + GitHub Flow + GitHub's `branch-deploy` release/deployment flow.** Maintainer 2026-04-24 Otto-233 directive: *"backlog research trunk based development and GitHub Flow and https://github.com/github/branch-deploy branch deploy, trunk based is git native and we want to make sure we are following (it seems we are you can verify) and github flow and github branch deploy are both github host release/deployments flows we want to make sure we follow this well. Although all this is overkill for our first github pages starboard ui lol. It will be important for best practices for git/github."* Scope: (1) audit current repo against trunk-based development discipline — short-lived feature branches, always-green main, feature flags over long-lived branches, pair/mob friendly merges, &lt;24h feature-branch lifespan target. Verify we are following (initial read says yes — main is protected, squash-merge convention, feature branches short-lived); document gaps. (2) Audit against GitHub Flow — branch from main → commit → open PR → review → merge → deploy. Current flow: branch from main → commit → open PR → auto-merge (CI+review gated). Document where we deviate and why (e.g. parallel drain-subagents via worktree, subagent dispatch prompts, append-only discipline for audit-trail files such as `docs/BACKLOG.md`). (3) Study `https://github.com/github/branch-deploy` — GitHub's official branch-deployment pattern that lets a PR be deployed to a temporary environment before merging to main. Assess fit for Zeta's `starboard` GitHub Pages UI (likely overkill for v1 per maintainer) + future deployable-service layer. (4) Write `docs/research/git-github-best-practices-audit-otto-233.md` with verified-against-current-repo grade per dimension, gap list, and a future-follow-up row for any gap worth closing. Priority P2 research-grade; effort S + S + M (three studies). Composes with Windows cross-platform verification via peer harnesses, and with post-drain AceHack-first routing as an orthogonal policy layer that sits on top of whichever base flow we run. **Does NOT authorize** changing branch-protection / merge discipline / deploy flow pre-research; the research produces a recommendation, maintainer decides adoption.
  ```
  </pre>

## Fix commits (touching thread paths)

### `e273a82d5aa4818cef7024e5e92acbc24f081223` -- 2026-04-24T14:23:22Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `docs/BACKLOG.md`

**Message:**

```
backlog: P2 — git/github best-practices audit (trunk-based + GitHub F…

…low + branch-deploy)

Maintainer Otto-233 directive:

> "backlog research trunk based development and GitHub Flow and
> https://github.com/github/branch-deploy branch deploy, trunk
> based is git native and we want to make sure we are following
> (it seems we are you can verify) and github flow and github
> branch deploy are both github host release/deployments flows
> we want to make sure we follow this well. Although all this
> is overkill for our first github pages starboard ui lol. It
> will be important for best practices for git/github."

Single P2 research row appended at bottom of `## P2 —
research-grade` section (Otto-229 append-only + BACKLOG
append-at-section-bottom discipline honored; no existing row
edits). Scope has three axes: current-repo-vs-trunk-based
audit, current-flow-vs-GitHub-Flow audit, branch-deploy fit
study for the starboard UI + future deployable services.

Effort S + S + M. Produces a `docs/research/...audit.md` with
a verified grade per dimension, gap list, and any follow-up
rows worth elevating.

Does NOT authorize changing merge discipline / deploy flow
pre-research — the row produces a recommendation; maintainer
decides adoption.
```
