# PR #12856 -- fix(hygiene): clear 7 dangling auto-vivify refs — re-issued on current main

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 12856 |
| Title | fix(hygiene): clear 7 dangling auto-vivify refs — re-issued on current main |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-08-20T16:33:28Z |
| Merged at | 2026-08-20T16:46:55Z |
| Merge commit SHA | `31f371787941dac19eb7eee94b7100b4e0c8ba35` |
| Branch | `fix/auto-vivify-dangling-refs-v2` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/12856 |
| Changed files | 2 |
| Additions / deletions | +19 / -0 |

## Description

Re-issues the auto-vivify change from #12401, which had been `DIRTY` since 2026-08-19 with auto-merge armed — **stuck rather than progressing**, and which I twice logged as "another agent's work" when the branch is literally `otto/…`.

## Why a clean commit rather than a merge

I first resolved it as a merge with both parents. **The pre-push floor hook refused it** — a merge's pushed *range* includes everything main gained since the branch point, which touches `treaty-byte-lock-vectors`: cross-oracle contracts other lanes build on this tick.

The hook offers `ZETA_FLOOR_VECTORS_ACK=1` to state the change is deliberate. **That acknowledgement would have been false.** Those vector changes are *main's* — already landed and verified by whoever made them — not mine to attest to. So the push was re-shaped to be honestly small instead of acknowledged past.

> The floor did exactly its job: it caught a push whose *stated* scope (two files) and *actual* range (hundreds) disagreed.

## The original conflicts were the same fix made twice

Both conflicting files are work-item notes where this branch and main **independently** qualified bare paths to full repo paths:

- `081M0BTTM85…` — both sides made **identical** edits. Main already carries it.
- `081M0B5V6Z5…` — both reworded the same line differently, and **main also fixed a second line** the branch never touched.

Main is equivalent-or-better on both, so nothing substantive is lost — only this branch's *wording* of a fix main already made. The branch's real contribution is the auto-vivify code, which main never touched, and that's what this carries.

## Verified — and the first attempt did not count

I ran the tests once against a detached HEAD at `0c1fac98` while main was at `baf4b229`, got **4 pass**, and that was a **stale tree, not the merged state**. Reporting it as verification would have been the rounding-up this repo refuses.

Re-run against current main: **5 pass, 0 fail, 23 expect() calls.**

**The test *count* differing between the two runs is what exposed the stale tree** — had both read 4, I wouldn't have noticed.

Closes #12401.

Agency-Signature-Version: 1
Agent: otto
Agent-Runtime: claude-code/local
Agent-Model: claude-opus-5
Credential-Identity: AceHack
Credential-Mode: shared
Human-Review: not-implied-by-credential
Human-Review-Evidence: none
Action-Mode: autonomous-fail-open
Task: none
Co-authored-by: Claude Opus 5 <noreply@anthropic.com>

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 0 |
| Resolved threads | 0 |
| Unresolved threads | 0 |
| Total review comments | 0 |
| Total fix commits (touching thread paths) | 0 |

## Review threads

(no review threads on this PR)

## Fix commits (touching thread paths)

(no commits in this PR touched files anchored by review threads)
