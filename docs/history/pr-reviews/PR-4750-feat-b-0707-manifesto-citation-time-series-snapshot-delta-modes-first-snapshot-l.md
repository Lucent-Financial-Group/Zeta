# PR #4750 -- feat(B-0707): manifesto citation time-series — --snapshot + --delta modes; first snapshot landed

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 4750 |
| Title | feat(B-0707): manifesto citation time-series — --snapshot + --delta modes; first snapshot landed |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-05-23T18:34:27Z |
| Merged at | 2026-05-23T18:50:20Z |
| Merge commit SHA | `5415288e64ae5292b14b651014f273eb157bef82` |
| Branch | `otto/cli-b0707-citation-time-series-2026-05-23` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/4750 |
| Changed files | 5 |
| Additions / deletions | +608 / -12 |

## Description

## Summary

Ships **B-0707** (the time-series child filed in PR #4747 slice 1). Extends `tools/hygiene/audit-manifesto-citations.ts` with persistent-snapshot mode + delta computation.

**B-0707 row closed** (4 of 5 acceptance criteria met; cron-cadence wiring deferred as separate concern — manual snapshot already useful + composable into pre-commit/cron later).

## New flags

| Flag | Behavior |
|---|---|
| `--snapshot` | Write today's summary to `docs/hygiene-history/manifesto-citations/YYYY-MM-DD.json` |
| `--snapshot --date YYYY-MM-DD` | Override date (testing/backfill) |
| `--delta` | Compute delta vs most-recent prior snapshot (markdown table) |
| `--delta --json` | Same but machine-readable |
| `--date YYYY-MM-DD` | Pin date for snapshot or delta computation |

## First snapshot landed

[`docs/hygiene-history/manifesto-citations/2026-05-23.json`](docs/hygiene-history/manifesto-citations/2026-05-23.json) — **2.8KB**, baseline 88 files / 684 citations / 11 surfaces.

Snapshot intentionally strips the heavy `citations[]` array (~70KB if kept) and stores only per-surface metrics + totals — 365 daily snapshots = ~1MB time-series, tractable for long-horizon adoption tracking.

## Delta path validation

Delta computation validated against a synthetic prior snapshot during development:
- Correctly attributes per-surface deltas (files-with-citation + citations + each form)
- Handles missing-in-prior surfaces (treats as zero — useful when new surfaces are added between snapshots)
- `mostRecentPriorSnapshot` lexicographic sort works because ISO `YYYY-MM-DD` sorts identically chronologically + lexicographically (same trick as tick shards)

## Tests

**30 pass** (16 pre-existing + 14 new):
- `parseArgs` for `--snapshot` / `--delta` / `--date` (including `--date` rejection cases)
- `signedNum` (+5/0/-3 formatting)
- `snapshotDate` / `snapshotPath` (ISO date stripping + path joining)
- `toSnapshot` (verifies citations[] is stripped — keeps file size tractable)
- `computeDelta` (positive deltas + missing-in-prior-surface as zero)
- `renderDelta` (markdown rendering with signed numbers)

## Composes with

- B-0525 (parent — constitutional-promotion readiness tracking; this slice is measurement infrastructure step 4)
- PR #4747 (slice 1 — original `audit-manifesto-citations.ts`)
- PR #4748 (slice 2 — trajectory-citation gap fix; produces measurable +16 trajectory delta for first snapshot pair)
- `tools/hygiene/audit-rule-cross-refs.ts` (sibling count-then-classify pattern)
- `.claude/rules/encoding-rules-without-mechanizing.md` (cron-cadence-wiring is the deferred follow-up)
- `docs/hygiene-history/` (canonical hygiene-substrate location; snapshot files compose with tick-shard `YYYY/MM/DD/HHMMZ.md` timestamping pattern)

## Test plan

- [x] All 30 tests pass locally (`bun test tools/hygiene/audit-manifesto-citations.test.ts`)
- [x] `--snapshot` write verified against current main (2.8KB JSON)
- [x] `--delta` no-prior path returns informative message + exit 0
- [x] `--delta` with synthetic prior produces correct signed-number table
- [x] B-0707 row closed with acceptance-criteria status updated
- [x] BACKLOG.md regenerated
- [x] Branch matches `ZETA_EXPECTED_BRANCH` guard
- [ ] CI green

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 4 |
| Resolved threads | 4 |
| Unresolved threads | 0 |
| Total review comments | 4 |
| Total fix commits (touching thread paths) | 2 |

## Review threads

### Thread 1 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6EU9Hg`
- **Path**: `tools/hygiene/audit-manifesto-citations.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-23T18:36:51Z on `tools/hygiene/audit-manifesto-citations.ts`:133 (association: NONE)

  <pre>
  P1: `--snapshot` and `--delta` are currently allowed together (and `--report`/`--json` can also be combined), but `main()` will silently prioritize the first branch it hits and ignore the rest. Please add argument validation (e.g., make `--snapshot` and `--delta` mutually exclusive, and decide whether `--report` is compatible with either) and return a 64 error on invalid combinations.
  </pre>

### Thread 2 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6EU9Hk`
- **Path**: `tools/hygiene/audit-manifesto-citations.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-23T18:36:51Z on `tools/hygiene/audit-manifesto-citations.ts`:372 (association: NONE)

  <pre>
  P1: `computeDelta()` only iterates `current.surfaces`, so if a surface existed in the prior snapshot but is missing from the current snapshot, it will not appear in the delta output (and negative deltas will be hidden). Consider iterating the union of surface keys from both snapshots so removals show up as negative counts too.

  </pre>

### Thread 3 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6EU9Hm`
- **Path**: `docs/backlog/P2/B-0707-manifesto-citation-time-series-tracking-2026-05-23.md`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-23T18:36:52Z on `docs/backlog/P2/B-0707-manifesto-citation-time-series-tracking-2026-05-23.md`:6 (association: NONE)

  <pre>
  P1: The row is marked `status: closed`, but `closed_by` says "all acceptance criteria met" while the acceptance list still has an explicit deferred/unchecked item. Please update `closed_by` to reflect the actual closure rationale (e.g., 4/5 met and cron wiring deferred), or keep the row open until the final criterion is satisfied.

  </pre>

### Thread 4 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6EU9Jr`
- **Path**: `tools/hygiene/audit-manifesto-citations.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-23T18:37:01Z on `tools/hygiene/audit-manifesto-citations.ts`:437 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Return JSON for no-prior case under --delta --json**

  When `--delta --json` is used and no earlier snapshot exists, this branch prints markdown text instead of JSON, even though the CLI advertises a machine-readable delta mode. Any automation that parses stdout as JSON will fail on first-run/bootstrap repositories or when `--date` points before the first snapshot. Keep output format consistent in JSON mode (e.g., emit a structured "no prior snapshot" payload).

  Useful? React with 👍 / 👎.
  </pre>

## Fix commits (touching thread paths)

### `a3db3e0c7a9d348de19cb2f0c8f1e1039733e932` -- 2026-05-23T18:32:19Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `docs/backlog/P2/B-0707-manifesto-citation-time-series-tracking-2026-05-23.md`
- `tools/hygiene/audit-manifesto-citations.ts`

**Message:**

```
feat(B-0707): manifesto citation time-series — --snapshot + --delta m…

…odes; first snapshot landed

Extends tools/hygiene/audit-manifesto-citations.ts with:

  --snapshot           write today's summary to
                       docs/hygiene-history/manifesto-citations/YYYY-MM-DD.json
  --snapshot --date    override date (testing/backfill)
  --delta              compute delta vs most-recent prior snapshot
                       (markdown table by default; --json available)
  --date YYYY-MM-DD    pin date for snapshot or delta computation

Snapshot strips the heavy citations[] array (keeps just per-surface
metrics + totals); 2.8KB per snapshot vs ~70KB if full citation list
were kept. 365 daily snapshots = ~1MB time-series, tractable.

First snapshot committed: docs/hygiene-history/manifesto-citations/
2026-05-23.json (baseline: 88 files / 684 citations / 11 surfaces).

Delta path validated against synthetic prior; mostRecentPriorSnapshot
sorts filenames lexicographically (works because ISO YYYY-MM-DD sorts
identically chronologically + lexicographically).

Tests: 30 pass (14 new for snapshot/delta/signedNum/toSnapshot/
computeDelta/renderDelta paths; 16 pre-existing).

B-0707 acceptance criteria 4 of 5 complete:
  [x] --snapshot writes dated JSON
  [x] --delta reports change-since-last per surface + per form
  [x] First snapshot file git-committed
  [x] Test coverage for snapshot + delta paths
  [ ] Cron-cadence wiring deferred to follow-up (manual already useful)

B-0707 row closed (4/5 acceptance criteria met; cron-wiring deferred
as separate concern); backlog index regenerated.

Composes with:
- B-0525 (parent) — slice 3+ measurement infrastructure
- tools/hygiene/audit-rule-cross-refs.ts (sibling pattern)
- docs/hygiene-history/ (canonical hygiene-substrate location;
  composes with shard YYYY/MM/DD/HHMMZ.md timestamping pattern)
- .claude/rules/encoding-rules-without-mechanizing.md (future cron
  candidate for daily auto-snapshot — deferred follow-up)

Co-Authored-By: Claude <noreply@anthropic.com>
```

### `f7f01236586373c7e6d79c96e001169a4d346a15` -- 2026-05-23T18:40:34Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `docs/backlog/P2/B-0707-manifesto-citation-time-series-tracking-2026-05-23.md`
- `tools/hygiene/audit-manifesto-citations.ts`

**Message:**

```
fix(B-0707): address 3 Copilot P1 + 1 Codex P2 review findings on PR …

…#4750

1. Copilot P1 (audit-manifesto-citations.ts:133) — mode mutual-exclusion
   parseArgs now rejects --snapshot + --delta combined; rejects --report
   combined with either. Both return exit 64. 3 new tests cover each
   error path.

2. Copilot P1 (audit-manifesto-citations.ts:372) — computeDelta union
   Now iterates union of surface keys (current ∪ prior). Removed-in-current
   surfaces appear as negative deltas instead of being silently dropped.
   1 new test covers the missing-in-current case (-1 files / -5 citations
   / -5 path).

3. Copilot P1 (B-0707 row:6) — closed_by precision
   Was: "all acceptance criteria met" (overclaim — 1 criterion was
   explicitly deferred). Now: "4 of 5 acceptance criteria met (cron-
   cadence wiring deferred as separate follow-up concern)".

4. Codex P2 (audit-manifesto-citations.ts:437) — JSON consistency
   --delta --json with no prior snapshot was emitting markdown; now
   emits structured {kind, snapshotDir, currentDate, message} JSON
   payload. Smoke-tested with --date 2020-01-01.

Tests: 34 pass (4 new; 30 pre-existing).

Co-Authored-By: Claude <noreply@anthropic.com>
```
