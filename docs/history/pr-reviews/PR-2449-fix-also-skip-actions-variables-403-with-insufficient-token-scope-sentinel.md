# PR #2449 -- fix: also skip actions/variables 403 with insufficient-token-scope sentinel

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 2449 |
| Title | fix: also skip actions/variables 403 with insufficient-token-scope sentinel |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-05-10T09:41:23Z |
| Merged at | 2026-05-10T09:44:33Z |
| Merge commit SHA | `cd9e4aa2ceb9a8820dc93dd6e9f39f37db5f75c1` |
| Branch | `fix/snapshot-drift-vars-403` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/2449 |
| Changed files | 1 |
| Additions / deletions | +4 / -3 |

## Description

## Summary

- PR #2448 fixed the `check drift` 403 for `actions/permissions` but CI immediately revealed `actions/variables` hits the same problem
- Applies the identical sentinel pattern: `ghApiOptional` + `{_skipped:"insufficient-token-scope"}` fallback
- `check-github-settings-drift.ts` already strips any top-level field with that sentinel before diffing — no changes needed there

## Root cause

`/repos/.../actions/variables` requires admin token scope, same as `actions/permissions`. The CI `GITHUB_TOKEN` lacks it, producing:
```
error: snapshot failed: gh api /repos/.../actions/variables failed (exit 1): gh: Resource not accessible by integration (HTTP 403)
```

## Verification

```
bun tools/hygiene/check-github-settings-drift.ts --repo Lucent-Financial-Group/Zeta
→ github-settings-drift: no drift (repo=Lucent-Financial-Group/Zeta)
```

## Test plan

- [x] `bun tsc --noEmit` passes clean
- [x] Single-line diff: `ghApi` → `ghApiOptional` + sentinel for `actions/variables`
- [x] No change needed to drift checker — sentinel-stripping loop already handles any skipped key

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 2 |
| Resolved threads | 2 |
| Unresolved threads | 0 |
| Total review comments | 2 |
| Total fix commits (touching thread paths) | 1 |

## Review threads

### Thread 1 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6A47F3`
- **Path**: `tools/hygiene/snapshot-github-settings.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-10T09:43:42Z on `tools/hygiene/snapshot-github-settings.ts`:226 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Restrict actions variables skip to 403 scope errors**

  This change converts `actions/variables` to `ghApiOptional`, but that helper returns `null` for **any** non-zero `gh api` exit code, not just the intended insufficient-scope 403. In CI, a transient GitHub/API failure, auth outage (401), or malformed request would now be silently mapped to `{_skipped:"insufficient-token-scope"}` and then stripped by `check-github-settings-drift`, producing a false “no drift” result instead of failing fast. Please gate the sentinel fallback on an actual 403 “Resource not accessible by integration” response and preserve hard failures for other exit conditions.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 2 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6A47K-`
- **Path**: `tools/hygiene/snapshot-github-settings.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-10T09:44:01Z on `tools/hygiene/snapshot-github-settings.ts`:226 (association: NONE)

  <pre>
  P1: `ghApiOptional()` returns `null` for any non-zero exit (not just the expected 403 insufficient-scope case). With this change, *any* transient failure (rate limit, network, GitHub outage, etc.) will be converted into `{_skipped:"insufficient-token-scope"}` and then silently stripped by `check-github-settings-drift.ts`, creating false-negative “no drift” results. Consider tightening this path to only emit the sentinel when the failure is known to be an auth-scope 403 (e.g., inspect stderr for the HTTP status) and otherwise fail the snapshot so CI can surface real API/tooling problems.
  </pre>

## Fix commits (touching thread paths)

### `42b14a40fd64970c634c2c03a0f3e279a16d6b17` -- 2026-05-10T09:40:06Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/hygiene/snapshot-github-settings.ts`

**Message:**

```
fix: also skip actions/variables 403 with insufficient-token-scope se…

…ntinel

The original fix only applied ghApiOptional to actions/permissions.
CI immediately showed actions/variables hits the same 403 — the
GITHUB_TOKEN lacks admin scope for that endpoint too.

Apply the same pattern: ghApiOptional + {_skipped:"insufficient-token-scope"}
sentinel so check-github-settings-drift.ts strips the field from both sides
before diffing, preventing false-drift noise without masking real drift.

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>
```
