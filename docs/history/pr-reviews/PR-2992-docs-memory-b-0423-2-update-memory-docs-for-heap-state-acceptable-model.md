# PR #2992 -- docs(memory): B-0423.2 — update memory/ docs for heap-state-acceptable model

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 2992 |
| Title | docs(memory): B-0423.2 — update memory/ docs for heap-state-acceptable model |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-05-13T13:18:52Z |
| Merged at | 2026-05-13T13:33:03Z |
| Merge commit SHA | `5de7f82fb06f17e5233d5cc6196143e1e16f512b` |
| Branch | `feat/B-0423.2-memory-docs-heap-model` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/2992 |
| Changed files | 2 |
| Additions / deletions | +68 / -2 |

## Description

## Summary

- Updates `memory/README.md` to replace the old synchronous paired-edit instruction with the heap-state model (B-0423)
- Updates `memory/project_memory_format_standard.md` §5 + adds §6.4 (heap-state validation)

Until these docs are updated, cold-booting agents follow the old discipline, defeating the B-0423 architectural fix.

## Acceptance criteria verification

- [x] `memory/README.md` — "Agents writing memories" section updated: frontmatter required, paired-edit not required, reindexer runs on cadence
- [x] `memory/README.md` — "Stack-vs-heap model" subsection added, links to B-0423
- [x] `memory/project_memory_format_standard.md` §5 — heap-state-acceptable note added
- [x] `memory/project_memory_format_standard.md` §6.4 — new subsection documenting the 4 required frontmatter fields for reindexer contract
- [x] `bunx markdownlint-cli2 memory/README.md memory/project_memory_format_standard.md` → clean
- [x] `dotnet build -c Release` → 0 Warning(s), 0 Error(s)

## Test plan

```
bunx markdownlint-cli2 memory/README.md memory/project_memory_format_standard.md
dotnet build -c Release
```

Closes B-0423.2. Composes with B-0423.1 (test coverage) and gates B-0423.4 (CI relaxation — docs should describe the contract before CI stops enforcing the old one).

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | true |
| Total threads | 6 |
| Resolved threads | 6 |
| Unresolved threads | 0 |
| Total review comments | 12 |
| Total fix commits (touching thread paths) | 4 |

## Review threads

### Thread 1 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6Bw-Fb`
- **Path**: `memory/project_memory_format_standard.md`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-13T13:21:05Z on `memory/project_memory_format_standard.md`:285 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Align heap-state requirements with actual tooling behavior**

  This section states that files missing any of `name/description/type/created` are “silently skipped” by `tools/memory/reindex-memory-md.ts` and that B-0335 enforces all of these fields, but neither is true: the reindexer only skips files with no frontmatter and otherwise falls back to filename/default values (including missing `created`), and `tools/hygiene/validate-memory-schema.ts` does not validate `created` at all. In practice, this misleads contributors during triage because a file can still be promoted to `MEMORY.md` even when it violates the documented contract.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-13T13:24:10Z on `memory/project_memory_format_standard.md`:285 (association: MEMBER)

    <pre>
    Valid P2 finding — both claims were inaccurate. Fixed in e9848aa: the reindexer only skips files with no frontmatter block at all (individual missing fields produce fallbacks: name→filename, description→"(no description)", created→date-from-filename). B-0335 does not validate `created`. §6.4 now has an accurate fallback/enforcement table distinguishing what the reindexer does vs what B-0335 enforces.
    </pre>

### Thread 2 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6BxA7x`
- **Path**: `memory/README.md`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-13T13:23:08Z on `memory/README.md`:89 (association: NONE)

  <pre>
  Docs say new memory files must include `created:` and that the reindexer requires `name/description/type/created`. However, `tools/memory/reindex-memory-md.ts` currently only requires that a YAML frontmatter block exists; it falls back to a date parsed from the filename when `created` is missing and also renders entries with missing `name`/`description` using defaults. Please either (a) adjust this text to match the current reindexer/validator behavior (e.g., `created` recommended, not required), or (b) update the reindexer/validation tooling to actually enforce and skip/flag files missing these fields.

  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-13T13:24:34Z on `memory/README.md`:89 (association: MEMBER)

    <pre>
    Fixed in e9848aa — §6.4 now documents the actual reindexer behavior: only skips files with no frontmatter block; individual missing fields produce fallbacks. The table now shows 'Not enforced by B-0335' for `created` and notes it as 'strongly recommended' rather than required. This matches option (a) in your suggestion.
    </pre>

### Thread 3 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6BxA8f`
- **Path**: `memory/project_memory_format_standard.md`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-13T13:23:09Z on `memory/project_memory_format_standard.md`:285 (association: NONE)

  <pre>
  This table marks `created:` as required for “heap-state validation”, but in the same doc `created` is listed as an optional field (§1), and `tools/hygiene/validate-memory-schema.ts` does not require `created`. Please reconcile the standard so the required/optional status of `created` is consistent across the document and matches enforcement.

  This issue also appears on line 282 of the same file.

  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-13T13:24:35Z on `memory/project_memory_format_standard.md`:285 (association: MEMBER)

    <pre>
    Fixed in e9848aa — the inconsistency is resolved. §6.4 now reflects that `created` is recommended (not required), aligning with §1 and with the actual validate-memory-schema.ts behavior which does not enforce it. The fallback/enforcement table makes the distinction explicit.
    </pre>

### Thread 4 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6BxEsb`
- **Path**: `memory/README.md`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-13T13:25:57Z on `memory/README.md`:89 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Correct reindexer field requirements in memory README**

  The new guidance says `name`, `description`, `type`, and `created` are required because the reindexer needs them, but `tools/memory/reindex-memory-md.ts` only requires a frontmatter block and falls back when fields are missing (and `created` is not enforced by `tools/hygiene/validate-memory-schema.ts`). This creates a false operational contract for contributors: files that are actually indexable are documented as invalid, which can lead to incorrect triage and unnecessary churn in memory-file edits.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-13T13:28:52Z on `memory/README.md`:89 (association: MEMBER)

    <pre>
    Fixed in ad5931ab — the bullet now accurately states: reindexer only skips files with no frontmatter block at all; missing individual fields use safe fallbacks. References §6.4 in project_memory_format_standard.md for the full fallback/enforcement table. (Option a from the suggestion: text adjusted to match actual tooling behavior.)
    </pre>

### Thread 5 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6BxHBy`
- **Path**: `memory/README.md`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-13T13:27:41Z on `memory/README.md`:89 (association: NONE)

  <pre>
  Docs currently say new memory files must include `created:` and that the reindexer “requires” `name/description/type/created`. In `tools/memory/reindex-memory-md.ts`, the only hard requirement is that a YAML frontmatter block exists; missing `created` falls back to a date parsed from the filename, and missing `name`/`description` fall back to defaults. Please reword this bullet to match actual behavior (e.g., frontmatter block required; `name/description/type` required by B-0335 validation; `created` strongly recommended for sort order).

  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-13T13:30:51Z on `memory/README.md`:89 (association: MEMBER)

    <pre>
    Addressed in ad5931ab (same fix as the Codex thread above). This thread is outdated relative to the current commit.
    </pre>

### Thread 6 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6BxHCZ`
- **Path**: `memory/project_memory_format_standard.md`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-13T13:27:41Z on `memory/project_memory_format_standard.md`:290 (association: NONE)

  <pre>
  The §6.4 table says missing `type:` “falls back to inferred from filename prefix,” but the reindexer doesn’t use `type` at all, and `tools/hygiene/validate-memory-schema.ts --enforce` treats missing `type` as an error (optionally auto-fixable with `--fix`). Please adjust the “Behavior when missing”/“enforcement” cells so they don’t imply silent fallback for `type:` when it’s actually required by validation.

  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-13T13:30:45Z on `memory/project_memory_format_standard.md`:290 (association: MEMBER)

    <pre>
    Valid finding — confirmed via reindexer source: formatEntry() only reads fm.name, fm.description, and fm.created. fm.type is parsed into FrontMatter but never accessed by the index builder. Fixed in b6e21dfb: the table now reads 'Not used by reindexer; B-0335 --fix auto-infers from filename prefix' which correctly separates reindexer fallback behavior from validator enforcement.
    </pre>

## Fix commits (touching thread paths)

### `cd8672a2ffcade85eea5bc0ed404896535d1a074` -- 2026-05-13T13:18:39Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `memory/README.md`
- `memory/project_memory_format_standard.md`

**Message:**

```
docs(memory): B-0423.2 — update memory/ docs for heap-state-acceptabl…

…e model

Updates README.md and project_memory_format_standard.md to document
the new heap-state model introduced by B-0423 so future agents
author memory files correctly under the new architecture.

Changes:
- README.md: replaces "Update MEMORY.md to include new entries"
  with frontmatter requirement + no-paired-edit contract; adds
  Stack-vs-heap model subsection explaining both states + reindexer
- project_memory_format_standard.md §5: adds heap-state-acceptable
  note with reindexer callout
- project_memory_format_standard.md §6.4: new subsection documenting
  the four required frontmatter fields for reindexer contract

No code changes. markdownlint clean. dotnet build 0 warnings.

Co-Authored-By: Claude <noreply@anthropic.com>
```

### `e9848aa805ba7244b4a04f946fb111012da7591c` -- 2026-05-13T13:23:45Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `memory/project_memory_format_standard.md`

**Message:**

```
fix(memory): §6.4 — correct reindexer skip/fallback behavior and B-03…

…35 coverage

Per Vera review on PR #2992: the previous text falsely claimed that
files missing any required field are "silently skipped" and that
B-0335 enforces all four fields. The reindexer only skips files
with no frontmatter block at all; individual missing fields produce
fallbacks (name→filename, description→"(no description)",
created→date-from-filename). B-0335 enforces name/description/type
but not created.

Replace the "silently skipped" table with an accurate fallback/
enforcement table and a best-practice note about `created`.

Co-Authored-By: Claude <noreply@anthropic.com>
```

### `ad5931abe03f13741c2a9c19665ddc03401fcb4b` -- 2026-05-13T13:28:37Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `memory/README.md`

**Message:**

```
fix(memory): correct README.md reindexer contract at line 89

The reindexer skips files with no frontmatter block at all; missing
individual fields (name/description/created) use safe fallbacks rather
than causing the file to be skipped. Update the bullet to match the
accurate behavior documented in §6.4 of project_memory_format_standard.md.

Addresses Codex P2 thread on PR #2992 (PRRT_kwDOSF9kNM6BxEsb).

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>
```

### `b6e21dfb1403184052b735cd6c6cce3c42d7bc47` -- 2026-05-13T13:30:09Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `memory/project_memory_format_standard.md`

**Message:**

```
fix(memory): correct §6.4 type: reindexer-vs-validator distinction

The reindexer never reads fm.type at all — it only uses name, description,
and created. The 'Inferred from filename prefix' behavior belongs to
B-0335 validate-memory-schema.ts --fix, not the reindexer. Update the
§6.4 table to make this distinction explicit.

Addresses Copilot review thread PRRT_kwDOSF9kNM6BxHCZ on PR #2992.

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>
```
