# PR #16867 -- docs(cluster): record the byte-lock toolchain evidence instead of leaving it to be re-derived

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 16867 |
| Title | docs(cluster): record the byte-lock toolchain evidence instead of leaving it to be re-derived |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-09-06T21:37:15Z |
| Merged at | 2026-09-06T21:55:41Z |
| Merge commit SHA | `e06e611f81c62d3e7264a186c14fab1c6d5336b8` |
| Branch | `shadow/record-bytelock-toolchain-evidence` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/16867 |
| Changed files | 3 |
| Additions / deletions | +33 / -2 |

## Description

`src/wasm-dla/README.md` is one of the six `blocking` entries left on the stale-cluster-tree roster, and its migration is a **maintainer decision**: carry the byte-lock toolchain into full-ai-cluster's NixOS module, or record that cluster hosts do not need it.

**This does not make that decision.** It measures the three facts the decision turns on, so nobody has to dig them out again:

| | measured 2026-09-06 |
|---|---|
| **a** | The README enumerates **four** declaration sites — NixOS, brew, apt, and the root flake devShell (`flake.nix` lines 233–235 carry `wabt`, `binaryen`, `emscripten`). Deleting `infra/nixos` removes only the NixOS one. |
| **b** | **`full-ai-cluster/` declares the byte-lock toolchain nowhere.** A grep for `wabt`/`binaryen`/`emscripten` across the whole tree returns nothing — the packages would simply be absent from cluster hosts. |
| **c** | **`bytelock.yml` never runs on a cluster host.** Its matrix is `ubuntu-24.04` / macOS / Windows GitHub runners, and it installs `wabt` via apt, brew and choco. No byte-lock work depends on the NixOS declaration today. |

**The disposition stays `blocking` and the count is unchanged at 15**, because nothing was migrated. Recording evidence is not a migration, and moving the number without moving the work is the failure this roster exists to prevent.

**Why it is worth a commit at all:** a decision whose inputs must be re-measured every time someone picks it up is a point that lives permanently in the expensive corner. Writing the measurement down once is the cheap half of exactly the trade the triangle meter (#16851) exists to watch.

Work-item `081M1WADYFS087G0R0038FJBSX`.

Agency-Signature-Version: 1
Agent: shadow
Agent-Runtime: claude-code/cli
Agent-Model: claude-opus-5[1m]
Credential-Identity: AceHack
Credential-Mode: shared
Human-Review: none
Human-Review-Evidence: none
Action-Mode: autonomous-fail-closed
Task: 081M1WADYFS087G0R0038FJBSX
Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_01QyTyVdDqxekK5rZQGBm75f


## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 0 |
| Resolved threads | 0 |
| Unresolved threads | 0 |
| Total review comments | 0 |
| Total fix commits (touching thread paths) | 0 |

## Review threads

(no review threads on this PR)

## Fix commits (touching thread paths)

(no commits in this PR touched files anchored by review threads)
