# PR #16881 -- fix(formal): strengthen the NCI oracle-independence assertion — main was red

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 16881 |
| Title | fix(formal): strengthen the NCI oracle-independence assertion — main was red |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-09-06T22:29:27Z |
| Merged at | 2026-09-06T22:48:39Z |
| Merge commit SHA | `76a80b74d5e25c2bd6681d648ed46d195397d73f` |
| Branch | `shadow/strengthen-nci-independence-assertion` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/16881 |
| Changed files | 3 |
| Additions / deletions | +47 / -1 |

## Description

**`main` is red** at `113fdbf0` — `lint (bash retirement inventory + hygiene unit tests)`, from `audit-check-arity-nonequality` R5 (count rose 0 → 1), on the test added in #16868:

```ts
expect(source).not.toContain("nci_witness_receipt_oracle");
```

**The audit is right, and the claim it guards is a real one.** That test asserts **independence** — the TypeScript emitter must not share code with the Python checker, or their agreement is worth nothing as evidence (Knight–Leveson: correlated implementations fail together). But a string-absence check witnesses *one rendering of one leak* and cannot witness its absence: a differently-named module, a relative path, or a dynamic import all pass it silently.

Strengthened per the audit's **first** remedy rather than adjudicated away with `--accept-raises`:

```ts
expect(imports).toEqual(["node:crypto", "node:fs", "node:path", "node:child_process"]);
```

plus an exact-empty assertion on dynamic `import()` / `require()` sites, which an import-statement scan would not see. **Any new dependency now fails here**, which is what the claim actually says.

## Demonstrated strictly stronger, not asserted

Planted control — an import that never spells the sentinel string:

| assertion | verdict on the planted leak |
|---|---|
| old `not.toContain(...)` | **passes** — 0 occurrences of the sentinel; it misses it |
| new exact import set | **fails** — 8 pass, 1 fail |

Restored afterwards; green again. The whole `formal-verification` suite passes (33 tests), and `audit-check-arity-nonequality` exits 0.

Not my test and not my defect — the ordinary consequence of a new absence assertion meeting a check that counts them. The claim is unchanged; only its witness got stronger.

Work-item `081M1WDDHQG087G0R001NWF589`.

Agency-Signature-Version: 1
Agent: shadow
Agent-Runtime: claude-code/cli
Agent-Model: claude-opus-5[1m]
Credential-Identity: AceHack
Credential-Mode: shared
Human-Review: none
Human-Review-Evidence: none
Action-Mode: autonomous-fail-closed
Task: 081M1WDDHQG087G0R001NWF589
Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_01QyTyVdDqxekK5rZQGBm75f


## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 0 |
| Resolved threads | 0 |
| Unresolved threads | 0 |
| Total review comments | 0 |
| Total fix commits (touching thread paths) | 0 |

## Review threads

(no review threads on this PR)

## Fix commits (touching thread paths)

(no commits in this PR touched files anchored by review threads)
