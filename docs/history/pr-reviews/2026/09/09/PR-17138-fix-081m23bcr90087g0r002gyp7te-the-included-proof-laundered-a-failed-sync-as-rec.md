# PR #17138 -- fix(081M23BCR90087G0R002GYP7TE): the included proof laundered a failed sync as reconciled whenever health was Healthy

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 17138 |
| Title | fix(081M23BCR90087G0R002GYP7TE): the included proof laundered a failed sync as reconciled whenever health was Healthy |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-09-09T15:13:51Z |
| Merged at | 2026-09-09T15:51:20Z |
| Merge commit SHA | `e5c599fcdc9a1f1f4b818d1bdd640c14091cbe72` |
| Branch | `proof-refuses-a-failed-sync` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/17138 |
| Changed files | 4 |
| Additions / deletions | +243 / -0 |

## Description

## What was laundered

`isApplicationSynced` accepts `OutOfSync + Healthy` for two documented and legitimate reasons — benign StatefulSet drift on Helm apps, benign manifest drift on git-directory apps. Both are real. What neither anticipated is that **ArgoCD assesses health per resource kind**, and a kind it has no health check for contributes nothing to the verdict.

So an Application whose sync genuinely **failed**, and whose unapplied resources are all health-less kinds, presents as `OutOfSync + Healthy` — byte-identical, to every predicate in the file, to an app that synced cleanly and drifted afterwards.

## Measured

`hat-system` in run `34323056405`:

```
=== hat-system: sync=OutOfSync health=Healthy
    condition SyncError: Failed last sync attempt to [c73cc49e...]: one or more
      synchronization tasks completed unsuccessfully (retried 10 times).
    HatConflict/hat-conflict-default:       sync=OutOfSync
    HatCooldown/hat-cooldown-default:       sync=OutOfSync
    HatMaxBindings/hat-max-bindings-default: sync=OutOfSync
    HatMaxNew/hat-max-new-default:          sync=OutOfSync
    HatNoCycle/hat-no-cycle-default:        sync=OutOfSync
    HatQuorum/hat-quorum-default:           sync=OutOfSync
    HatWarmup/hat-warmup-default:           sync=OutOfSync
```

The same seven Constraints were unapplied in run `34338106811` — **which the proof passed**. A policy engine holding zero installed policies read as reconciled in a green run, and had done for as long as this lane has existed. The cause of that sync failure is #17136 / `081M23B24P5087G0R002BJWEN1`; this PR is about the proof being unable to **see** it.

## The change

`applicationOutcome` refuses any auto-sync Application carrying a `SyncError` condition, **before** the `OutOfSync + Healthy` allowances are consulted. The condition is ArgoCD stating the failure rather than us inferring it from two status strings that cannot carry it. The field was already parsed by `parseApplicationConditions` and, for child Applications, never read.

Two deliberate limits, both tested:

- **Not terminal.** ArgoCD retries and clears the condition on a successful sync, so an app carrying one stays a *laggard* and the wait keeps waiting. A transient failure still self-heals; only one that survives the timeout fails the proof.
- **Auto-sync only.** A declared manual-sync app is judged by `manualSyncAssertion`, a weaker contract on purpose. A test pins that the new branch does not reach it — widening the auto contract must not silently widen the manual one.

`ComparisonError` (the `sync=Unknown` class, which six apps pass through in a green run) is explicitly **not** treated as a sync failure, and that is a test rather than a comment.

## Mutation-tested

A refusal that cannot fire is exactly what this PR removes, so it is checked the same way:

| mutant | tests failed |
|---|---|
| refusal disabled (`if (false && ...)`) | 3 |
| empty `SyncError` message reads as no failure | 1 |
| any condition type counts as a sync failure | 1 |

Restored: `111 pass, 0 fail`. `bunx tsc --noEmit` clean.

## Sequencing

This PR makes the lane **able to go red** on a class that is live on `main` today. #17136 removes the only current instance. Auto-merge is armed only after #17136 lands, so this does not turn `main` red on a defect whose fix is already in flight.

Agency-Signature-Version: 1
Agent: shadow-subagent
Agent-Runtime: Claude Code
Agent-Model: Claude Opus 5
Credential-Identity: AceHack via gh
Credential-Mode: shared
Human-Review: not-implied-by-credential
Human-Review-Evidence: none
Action-Mode: autonomous-fail-closed
Task: 081M23BCR90087G0R002GYP7TE

Co-authored-by: AceHack <578953+AceHack@users.noreply.github.com>
Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01QyTyVdDqxekK5rZQGBm75f

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_01QyTyVdDqxekK5rZQGBm75f


## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 0 |
| Resolved threads | 0 |
| Unresolved threads | 0 |
| Total review comments | 0 |
| Total fix commits (touching thread paths) | 0 |

## Review threads

(no review threads on this PR)

## Fix commits (touching thread paths)

(no commits in this PR touched files anchored by review threads)
