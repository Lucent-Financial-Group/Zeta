# PR #17169 -- fix(tests): stop writing a discharge certificate to a hard-coded /tmp path

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 17169 |
| Title | fix(tests): stop writing a discharge certificate to a hard-coded /tmp path |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-09-09T18:36:28Z |
| Merged at | 2026-09-09T19:35:52Z |
| Merge commit SHA | `e702e7f16dbad1853003814d12cda7c7abc282e5` |
| Branch | `codeql-insecure-temporary-file` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/17169 |
| Changed files | 6 |
| Additions / deletions | +223 / -19 |

## Description

Closes the `js/insecure-temporary-file` class **by fixing, not dismissing**, and
answers the one open question in it.

The 4 alerts are **not one finding**. They split two-and-two, and the split is
the point:

| alert | site | verdict |
|---|---|---|
| **641** | `verify/z4-sle-harmonic-discharge.ts:118`, reached from its test | **genuine** — hard-coded `/tmp/z4-test-cert` |
| **260, 261** | `zflash/esp-inject.test.ts:65,72` | **genuine** — `esp-inject-test-<pid>.iso` in `/tmp` |
| **224** | `hygiene/divergence-shard.ts:359` | **false positive — and the caller is now identified** |

## 641 — a hard-coded `/tmp` path, for a DISCHARGE CERTIFICATE

The test used the literal `/tmp/z4-test-cert`, three times. `runZ4Discharge` then
does `mkdirSync(dir, { recursive: true })` and
`writeFileSync(join(dir, "z4-discharge-certificate.json"))`.

Three problems, none of them scanner pedantry:

- **`mkdirSync` with `recursive: true` SUCCEEDS on a directory that already
  exists and belongs to somebody else.** On a shared runner, whoever creates the
  path first owns it — and can then read or replace whatever lands there.
- **`writeFileSync` FOLLOWS SYMLINKS.** A pre-planted
  `/tmp/z4-test-cert/z4-discharge-certificate.json -> <target>` is an
  arbitrary-file overwrite running as the test user.
- The directory was **never removed**, so it persisted across runs and users.

The artifact makes it worse rather than better. A **discharge certificate** is a
proof-lineage artifact; whoever can replace one can assert that a conjecture was
discharged. That is the broken-meter shape — an instrument that presents as
frozen and is in fact writable by someone else.

## 260 / 261 — a pid is not a secret

`join(tmpdir(), ` + "`esp-inject-test-${process.pid}.iso`" + `)`. A pid is small,
guessable and **reused**, so the full path is predictable to any local user, and
both `copyFileSync` and `openSync(.., "r+")` follow symlinks.

## The fix for both

`mkdtempSync` — a **0700** directory with an **unpredictable** suffix, created
**atomically**, torn down after. None of the three problems above holds against
it.

## 224 — the caller, at last

This alert was carrying a correction (from PR #17151) that named the open
question rather than answering it: the query *"is following a caller that has not
been identified"*, and *"close it with a measurement"*.

**It is the test.** `divergence-shard.test.ts:39`:

```ts
const root = mkdtempSync(join(tmpdir(), "diverge-test-"));
```

`withTempRoot` hands that `root` to `writeShardAtPath(join(root, ...))` and to
`writeDivergenceShard(root, INPUT)`, which reaches `openSync(absPath, "wx")`.
That is the **only** flow: `divergence-shard.ts` never calls `tmpdir()`, and the
production entry point takes `--repo-root` defaulting to `"."`
(`review-thread-observations.ts:548`) — a checkout, never a temp dir.

On the merits the flow is benign **twice over**: `mkdtempSync` is the *secure*
idiom (0700, unpredictable) rather than the predictable name this query is about,
and the create is `O_CREAT|O_EXCL`, which neither follows a pre-planted symlink
nor races a competing writer.

**This PR does not claim alert 224 is closed. It is not, and the comment says so
in those words.** What the identification buys is that the flow can be *argued
about* instead of guessed at. Disposition: **false positive with a named flow** —
a judgement for Kenji and the maintainer, not one to enact from this lane.

## The falsifier that keeps the 224 comment honest

That comment rests on **one load-bearing premise** — that `divergence-shard.ts`
never reaches for a temp dir itself. A comment cannot keep a premise true. Two
new tests do:

- `divergence-shard.ts never calls tmpdir() outside its comments`, reading a
  **comment-stripped** copy of the source.
- `the stripper actually strips — otherwise the assertion above is vacuous`,
  which pins **both** directions: the real file mentions `tmpdir()` in prose (so
  a no-op stripper would fail the first test), a commented call is stripped, and
  a real call carrying a trailing comment is not.

| mutation | outcome |
|---|---|
| add a real `tmpdir()` call to `divergence-shard.ts` | **killed** — 42 pass, 1 fail, naming the guard |
| add a COMMENT mentioning `tmpdir()` and `mkdtempSync()` | **survives, correctly** — 43 pass |

The second row is the one that matters. A source-grepping guard that trips on its
own prose is a failure this repo has hit four times; this one reads the code.

## Gate

- `bun test` on all three touched files — **49 pass, 0 fail**. The `esp-inject`
  case genuinely **ran** (an installer ISO is present locally; 2 expect() calls),
  so the `mkdtemp` path is exercised end to end rather than skipped.
- `bunx tsc --noEmit -p tsconfig.json` — clean.
- `bun src/Core.TypeScript/ace/build-graph.ts derive --write` — already current,
  no derived drift from this branch.

Work-item: `081M23Q73MC087G0R000BZ38BZ`

Agency-Signature-Version: 1
Agent: shadow-subagent
Agent-Runtime: Claude Code
Agent-Model: Claude Opus 5
Credential-Identity: AceHack via gh
Credential-Mode: shared
Human-Review: not-implied-by-credential
Human-Review-Evidence: none
Action-Mode: autonomous-fail-closed
Task: 081M23Q73MC087G0R000BZ38BZ

Co-authored-by: AceHack <578953+AceHack@users.noreply.github.com>
Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01QyTyVdDqxekK5rZQGBm75f

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_01QyTyVdDqxekK5rZQGBm75f


## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 0 |
| Resolved threads | 0 |
| Unresolved threads | 0 |
| Total review comments | 0 |
| Total fix commits (touching thread paths) | 0 |

## Review threads

(no review threads on this PR)

## Fix commits (touching thread paths)

(no commits in this PR touched files anchored by review threads)
