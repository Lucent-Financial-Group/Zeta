# PR #17268 -- fix(081M26NHRM9087G0R0029WZGHS): main is red on actionlint — restore the SC2129 fix and the stranded local-checks runner

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 17268 |
| Title | fix(081M26NHRM9087G0R0029WZGHS): main is red on actionlint — restore the SC2129 fix and the stranded local-checks runner |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-09-10T23:24:25Z |
| Merged at | 2026-09-11T14:29:19Z |
| Merge commit SHA | `b1ce7698d180ff2c75ee0c60e5c8d374a13b8d56` |
| Branch | `restore-actionlint-and-local-checks` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/17268 |
| Changed files | 3 |
| Additions / deletions | +507 / -9 |

## Description

**`main` is red on actionlint right now, and it is my fault.** This restores the fix, plus the tool that was stranded with it.

## What happened

PR #17266 merged at head `2dfd5e7b`. My SC2129 fix (`4c7812e3`) and `local-checks.ts` (`66b0c090`) were pushed **after** that, so auto-merge fired past them and both were left on the branch. Verified against `origin/main`:

```
$ actionlint -ignore 'unknown permission scope "administration"'
.github/workflows/gate.yml:257:9: SC2129:style: Consider using { cmd1; cmd2; }
    >> file instead of individual redirects
rc=1
```

The defect is the eight separate `>> "$GITHUB_OUTPUT"` redirects I added to `path-filter`'s non-PR step. One block redirect around a loop — also the honest shape, since those lines are one emission.

**The operational lesson, which is the reusable part:** pushing to a PR that already has auto-merge armed *races the merge*. The window is real and I lost it. A follow-up push needs the merge disarmed first, or it needs to be a new PR.

## Also restored: `local-checks.ts`

Run the checks this diff would face, before pushing:

```
bun src/Core.TypeScript/ci/local-checks.ts          # what this diff would face
bun src/Core.TypeScript/ci/local-checks.ts --list   # what would run, and why
bun src/Core.TypeScript/ci/local-checks.ts --audit  # roster vs gate.yml
```

**Five outcomes, only one a pass** — `passed` / `failed` / `not-applicable` / `could-not-run` / `not-attempted`. A local runner that prints "all green" while three checks could not start manufactures confidence. Exit 0 only when nothing failed *and* nothing was unknown.

**Derived from `gate.yml`, not transcribed** — 23 jobs invoke a plain `bun <script>.ts`, several more than one. A hand-written map would drift silently. Commands needing env vars or shell state are reported `could-not-run` *with the reason*; `--audit` refuses to let the roster be incomplete in silence.

**Three defects it found in itself, all recorded in the file:** it classified a missing module as `failed` (bun exits 1 for both — the code can't separate *subject broken* from *check never ran*); a roster entry named a script I invented; and it caught a type error in itself via its own `lint (TS)` leg before the commit.

Verified on this branch: `actionlint` rc=0, 12 falsifiers green.

Agency-Signature-Version: 1
Agent: shadow
Agent-Runtime: Claude Code
Agent-Model: claude-opus-5
Credential-Identity: AceHack
Credential-Mode: shared
Human-Review: not-implied-by-credential
Human-Review-Evidence: none
Action-Mode: autonomous-fail-closed
Task: 081M26NHRM9087G0R0029WZGHS

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_01QyTyVdDqxekK5rZQGBm75f


## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 0 |
| Resolved threads | 0 |
| Unresolved threads | 0 |
| Total review comments | 0 |
| Total fix commits (touching thread paths) | 0 |

## Review threads

(no review threads on this PR)

## Fix commits (touching thread paths)

(no commits in this PR touched files anchored by review threads)
