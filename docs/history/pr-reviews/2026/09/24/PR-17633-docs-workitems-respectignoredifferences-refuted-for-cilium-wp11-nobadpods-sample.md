# PR #17633 -- docs(workitems): RespectIgnoreDifferences refuted for cilium; WP11 noBadPods samples too early (judgement)

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 17633 |
| Title | docs(workitems): RespectIgnoreDifferences refuted for cilium; WP11 noBadPods samples too early (judgement) |
| Author | `maximdolphin` (human) |
| State | MERGED |
| Created at | 2026-09-24T13:39:30Z |
| Merged at | 2026-09-24T13:50:09Z |
| Merge commit SHA | `e4949cb87679dc2051e47e081e349cea7c59e76c` |
| Branch | `claude/wp11-nobadpods-and-respectignoredifferences-finding` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/17633 |
| Changed files | 3 |
| Additions / deletions | +208 / -0 |

## Description

## Two coordinator-raised findings after #17607 merged

### 1. Does `RespectIgnoreDifferences=true` itself cause the stuck-Progressing symptom? REFUTED for cilium.

Urgent because seaweedfs got the same annotation in #17607, which would have
widened the defect from two apps to three if the hypothesis held. Checked
against data already in hand: cilium's FAIL reason is byte-identical in run
35946414428 (BEFORE `ignoreDifferences`/`RespectIgnoreDifferences` ever
touched cilium's Application.yaml) and run 35973241862 (AFTER). The symptom
predates the annotation. No changes needed to the three landed fixes.

Full evidence in `081M38GCTFX087G0R003MMTXJE`.

### 2. WP11's first-ever green attempt failed `noBadPods` — judgement, not necessarily a fix

Run 35996447262: `spire-agent` CrashLoopBackOff, restarts=1, sampled ONE
SECOND after `rootLanded`. `zeta-first-boot-k3s-verify.nix`'s verdict 6/6 is
the only one of six verdicts with no poll/soak.

**Judgement**: ordinary startup-ordering churn (spire-agent's DaemonSet has
no readiness dependency on spire-server's StatefulSet), not the confirmed-
non-metal DNS/hostNetwork race (proven absent from VMs, and WP11 boots a
real VM), not a genuine defect — restartCount=1 is weaker evidence than the
already-measured clean-VM baseline of 3 (081M343EM0R087G0R003C8ZHJ7).

Filed `081M39T5661087G0R001FTJ78W` with a concrete fix shape grounded in
already-measured numbers (180s soak matching the VM-oracle's own settle
time, failing only a pod still bad or still climbing at the end) — not
implemented here, per "not necessarily a fix." Also confirms
`collectAppFailureDiagnostics` does not reach this Nix-based lane (grepped
the full serial log: zero describe/logs/events for the bad pod).

Docs-only, no code changed.

Agency-Signature-Version: 1
Agent: Claude Code session (WP26 follow-up)
Agent-Runtime: Claude Code
Agent-Model: claude-sonnet-5
Credential-Identity: maximdolphin
Credential-Mode: shared
Human-Review: not-implied-by-credential
Human-Review-Evidence: none
Action-Mode: autonomous-fail-closed
Task: 081M39T5661087G0R001FTJ78W

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 0 |
| Resolved threads | 0 |
| Unresolved threads | 0 |
| Total review comments | 0 |
| Total fix commits (touching thread paths) | 0 |

## Review threads

(no review threads on this PR)

## Fix commits (touching thread paths)

(no commits in this PR touched files anchored by review threads)
