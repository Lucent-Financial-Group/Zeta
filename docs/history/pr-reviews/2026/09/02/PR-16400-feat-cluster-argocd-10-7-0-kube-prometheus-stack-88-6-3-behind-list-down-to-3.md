# PR #16400 -- feat(cluster): argocd 10.7.0, kube-prometheus-stack 88.6.3 — behind list down to 3

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 16400 |
| Title | feat(cluster): argocd 10.7.0, kube-prometheus-stack 88.6.3 — behind list down to 3 |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-09-02T22:30:13Z |
| Merged at | 2026-09-02T22:49:11Z |
| Merge commit SHA | `3a84499051d87aedb072c3d04438ecd2e41eda1d` |
| Branch | `shadow/chart-currency-argocd-10-7-0-kps-88-6-3` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/16400 |
| Changed files | 6 |
| Additions / deletions | +73 / -55 |

## Description

Two same-day releases picked up by the weekly refresh. Neither had a reason to be held.

| chart | from | to | bump |
|---|---|---|---|
| `argocd` / argo-cd | 10.6.4 | **10.7.0** | 1 minor |
| `kube-prometheus-stack` | 88.6.2 | **88.6.3** | 1 patch |

## Render-proven, not snapshot-proven

A passing snapshot test does **not** establish that a new pin renders — it establishes
that the committed snapshot still matches itself, which is the same shape as a check
that did not run. So the bump was verified by re-measuring:
`rendered-resource-requests.ts --measure` reports **53 Applications at both rungs, 0
unrenderable**, and the requests snapshot did not move — neither chart changed a
resource reservation between these versions.

Regenerated because each is keyed **by version** and would otherwise go stale silently:
`inert-valuesobject-keys.schema.json` (its keys embed the chart version), both
`published-chart-*` snapshots, and `docs/CHART-CURRENCY.md`.

## Behind list is now 3, and each is a decision rather than an oversight

- **gitlab** 8.7.0 → 10.3.1 (162 behind, MAJOR) — gated on the CloudNativePG migration.
  19.x needs PG 17.x *exactly* plus `pg_trgm`/`btree_gist`/`amcheck`, and CockroachDB
  accepts `CREATE EXTENSION pg_trgm` as a **no-op** — a failure in the acquitting
  direction, which is what rules it out.
- **temporal** 0.59.0 → 1.6.0 (26 behind, MAJOR) — not yet attempted; next up.
- **spire** 0.24.2 → 0.30.1 (16 behind) — deliberately **held** as the control in the
  running lane-isolation experiment. Moving it destroys the control.

Separately: **`headscale` is DORMANT at 560d.** It reads `CURRENT` only because upstream
stopped publishing — that is the security hazard Aaron named, not a clean bill of health.

Verified: hygiene + cluster **4640 pass / 0 fail**.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_01QyTyVdDqxekK5rZQGBm75f

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 0 |
| Resolved threads | 0 |
| Unresolved threads | 0 |
| Total review comments | 0 |
| Total fix commits (touching thread paths) | 0 |

## Review threads

(no review threads on this PR)

## Fix commits (touching thread paths)

(no commits in this PR touched files anchored by review threads)
