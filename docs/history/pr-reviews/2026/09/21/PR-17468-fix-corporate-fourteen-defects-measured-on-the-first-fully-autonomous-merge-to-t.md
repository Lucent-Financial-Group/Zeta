# PR #17468 -- fix(corporate): fourteen defects measured on the first fully autonomous merge-to-trunk run

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 17468 |
| Title | fix(corporate): fourteen defects measured on the first fully autonomous merge-to-trunk run |
| Author | `maximdolphin` (human) |
| State | MERGED |
| Created at | 2026-09-20T14:48:57Z |
| Merged at | 2026-09-21T20:08:04Z |
| Merge commit SHA | `0a0ac2895a010c76a6bdc766547d79992dc0dedc` |
| Branch | `fix/corporate-waypoint-run-hardening` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/17468 |
| Changed files | 23 |
| Additions / deletions | +2266 / -182 |

## Description

## Summary

Measured on the Waypoint organization, 2026-09-20: three greenfield goals, `--delivery merge`, `--pipeline diagnosed_design`, Claude Code (Sonnet 5 everywhere, Opus 5 on architecture) as every agent, no person in the loop. The run reached `=== DELIVERED ===` (three stories merged to the target repo's trunk) once these were closed. Each defect has a red test that reproduces the measurement; `.claude/rules` conventions followed (measured comments, refusal over fallback).

## Defects closed (in the order the run found them)

**Change control (`adapters.ts`)**
- A failed worktree setup left the checkout on disk with no owner marker → every later cycle refused it as "not a checkout of" its own branch (3 cycles, 0 landed). A refused open removes what it made.
- A merge refused for a **conflict** was left in progress in the operator's checkout when it was the base → every later merge failed with "unmerged files". Base aborted clean; conflict surfaced in the branch's own worktree and left for the performer; refusal names the files (`MERGE_CONFLICT`, `conflictedFiles`).
- `commandProposal` recorded its own argv as artifacts (`agent.cjs`, `work`, `task-021`); reviewers rejected for want of a deliverable.
- `capture()` keeps head **and** tail (a runner's summary is at the end); test-run evidence names the command and directory (`ran:`); verdict reasons get `MAX_VERDICT_CHARS` (16k) — a 6 kB rejection lost its one objection to the middle cut.

**Runtime (`org-runtime.ts`)**
- A landing refused for a conflict turns the change's leaf back at `implementation_review` by the merge steward, naming the files; previously the next cycle retried the same merge forever.
- The test producer answers `qa_uat` as well as `runtime_validation`, so the gate carries a run rather than an empty step every reviewer correctly refused.
- A verify leaf whose dependency's change is not in `openedChanges` this cycle (done in an earlier run, or opened a moment later under `--parallel`) rejoins that change for its checkout instead of testing the base tree.
- "Done" is judged against the chain **under the pipeline** (`chainForTask`), the same question the walk asks; under `diagnosed_design` a review leaf's `peer_review` is never walked and could never be proven → three delivered stories read `NOT DELIVERED`.

**Entry point (`run-org.ts`)**
- `latestPhaseRefusals`: why the last attempt *stopped* (verifier/producer refusal) reaches the next attempt as `ORG_FEEDBACK`, as a reviewer's rejection already did. The record existed (post AIAGENT-1662); nothing read it.
- `reviewerEnvFrom`: the judge receives `ORG_PRACTICE` / `ORG_DIRECTIVES` resolved for the gate and item, as every author already did. Three reviewers had rejected one green run under three unstated standards.
- `performedGates` ignores a **declined** gate practice; an unbound `qa_uat` still handed the gate to the document producer and displaced the test run.

**Observe**
- `commonRoot` looped forever when one attachment ref was an absolute path and another was not a path (`exit:1`); every reviewer of a worked item spun 5+ min then gave up.
- Inline evidence (`stdout:`, `exit:`, `ran:` …) is listed as `evidence#N (kind, size)` and opens to its text; it was opened as a missing file or dropped.

**`tools/claude-agent.cjs`**
- `ORG_CLAUDE_JUDGE_TOOLS`: the operator states what else a reviewer may run (flutter, dotnet, …); the judging allowlist was hardcoded to the Node ecosystem.

## Test plan
- [x] `tsc --noEmit` clean (repo config)
- [x] `corporate/{adapters,cli-adapters,git-worktree,run-org,org-runtime,observe-cli,claude-agent}.test.ts`, `observe/observe*.test.ts` — all green, ~20 new tests
- [x] `audit-agencysignature-main-tip.ts` PASS on the commit
- [ ] CI hermetic TS job (not awaited locally)

## Second commit (a99b2f468b)
Eleven more defects measured once the organization ran unattended: spawnSync serialising `--parallel`; post-code rejections never re-running the performer (fresh and resumed items); steward verdict `atMs` ordering; acceptance follow-up minted per cycle; verify leaf re-verifying a landed tree (now mints a Defect + `work_depends_on`); done≠landed duplicate follow-ups; decomposition seat counting (`freeSeatsUnder`); cancelled leaves still walked; `addDependency`/`work_depends_on` fact; performer feedback carrying every post-code gate.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

Agency-Signature-Version: 1
Agent: Claude Code session (Waypoint org operator)
Agent-Runtime: Claude Code
Agent-Model: claude-opus-5
Credential-Identity: maximdolphin
Credential-Mode: shared
Human-Review: explicit
Human-Review-Evidence: chat
Action-Mode: human-directed
Task: none

Co-authored-by: Claude Opus 5 <noreply@anthropic.com>


## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 0 |
| Resolved threads | 0 |
| Unresolved threads | 0 |
| Total review comments | 0 |
| Total fix commits (touching thread paths) | 0 |

## Review threads

(no review threads on this PR)

## Fix commits (touching thread paths)

(no commits in this PR touched files anchored by review threads)
