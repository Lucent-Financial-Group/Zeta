# PR #17475 -- feat(cluster): verify every first-boot image actually pulls (WP4 image-resolvability)

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 17475 |
| Title | feat(cluster): verify every first-boot image actually pulls (WP4 image-resolvability) |
| Author | `maximdolphin` (human) |
| State | MERGED |
| Created at | 2026-09-22T05:15:53Z |
| Merged at | 2026-09-22T07:08:37Z |
| Merge commit SHA | `8d3f5af1baf4a7f2a9190323d46e98de68200d65` |
| Branch | `claude/image-resolvability` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/17475 |
| Changed files | 9 |
| Additions / deletions | +4270 / -2 |

## Description

## Summary

- New `src/Core.TypeScript/cluster/image-resolvability.ts`: renders the K3S first-boot HelmChart roster (`full-ai-cluster/k8s/bootstrap/*.yaml`) plus every ArgoCD Application at the committed (metal) rung, extracts every container image, and resolves each against its registry's OCI distribution API. `missing` (404) / `arch-missing` (resolves, but no linux/amd64) fail; `unknown` (auth-walled or a failed probe) warns with a count and never gates. Reports Docker Hub count (anonymous 100/6h pull limit), `:latest`/untagged, no-digest, and `kube-version-derived` (a tag exactly matching the rendered kube-version — fragile to drift) as report-only risk.
- Offline (PR-blocking) mode checks the current render against a checked-in snapshot (`full-ai-cluster/k8s/image-resolvability.json`); `--refresh` (network, rides `helm-validate.yml`'s existing daily cron, non-blocking) re-resolves live and rewrites it — same split `image-source-provenance.ts` already uses.
- Small, explicitly-reasoned table for operator-injected images that never appear as a literal `image:` string — KubeVirt's component images, derived from `virt-operator`'s own tag. Verified against upstream KubeVirt v1.8.4 source (not guessed): the first draft wrote `virt-pr-helper`, which does not exist; the real names (`pr-helper`, `sidecar-shim`, no `virt-` prefix) came from `pkg/virt-operator/resource/generate/components/*.go`'s own `const` blocks, confirmed public one repository at a time against `quay.io/api/v1/repository/kubevirt/<name>`.
- **Addressed mid-task**: a real NixOS first-boot VM found `spire`'s bootstrap chart deriving a hook image's tag from `.Capabilities.KubeVersion` (`docker.io/rancher/kubectl:v1.35.7`, absent upstream). Fixed by reading `full-ai-cluster/k8s/kubernetes-version.json` — never hardcoding — for every render (`rendered-storage-claims.ts` gained an additive, opt-in `RenderOptions.kubeVersion`; no existing caller's behaviour changes). Any image whose tag exactly equals the render's kube-version is now flagged `kube-version-derived`.
- **Addressed mid-task (2)**: docker.io's 401 is ambiguous — a private repository and a DELETED one answer it identically. `dockerHubDisambiguate()` consults Docker Hub's own catalog API (`hub.docker.com/v2/repositories/...`, plain anonymous REST, not gated the same way) and promotes the finding to `missing` only on a DEFINITIVE 404; anything else stays `unknown`. Scoped to docker.io tag references only (never digests, never other registries). This correctly reclassified `minio/minio`/`minio/mc` (bundled by GitLab's chart at ancient defaults, deleted from Docker Hub ~2026-09-11) from `unknown` to `missing`.
- Since a **separate** change (`claude/gitlab-minio-image-gone`) owns fixing GitLab's bundled minio subchart and had not landed, added a small, dated, tracked, **expiring** acknowledgement register (`ACKNOWLEDGED_MISSING`) rather than leave this PR's gate red for a finding it's correct about but doesn't own fixing — both images are still printed in their own report section every run (tracking branch + expiry date), the register self-expires 2026-10-06, and a reverse check flags any acknowledgement whose target no longer matches a finding as safe to delete. `gatingRows()` is the one definition of "what fails the gate", shared by the printed report and the exit code.
- CI: two new steps in `helm-validate.yml`'s `charts` job — the offline check (every PR touching `full-ai-cluster/k8s/**` or `src/Core.TypeScript/cluster/**`) and the `--refresh` network step (`if: github.event_name == 'schedule'`, riding the workflow's existing daily cron, artifact-uploaded rather than auto-committed — same non-committing discipline as `chart-version-refresh.yml`).
- Reused rather than duplicated: `imagesInDocuments`/`parseImageReference` (`image-footprint.ts`), and newly-exported `fetchManifest`/`MANIFEST_ACCEPT` (the generic WWW-Authenticate challenge flow) from the same file; `discoverApplications`/`renderApplication` (`rendered-storage-claims.ts`); `parseHelmChartCrs`/`bootstrapDirs` (`render-first-boot-charts.ts`/`declared-cluster-trees.ts`).
- Worked around `node:child_process.spawnSync`'s unbounded-pipe `ENOBUFS` on large `--include-crds` renders (hit on cloudnative-pg, kube-prometheus-stack, argo-cd, external-secrets, argo-rollouts, gha-runner-scale-set-controller) by supplying a `Bun.spawnSync`-backed `runHelm` — additive, no edit to the shared renderer other validators use unmodified.

## Measured against the real tree (2026-09-22)

137 distinct image references · **134 ok** · **2 missing (both acknowledged, tracking `claude/gitlab-minio-image-gone`, expires 2026-10-06)** · **0 arch-missing** · 1 unknown (a documented `replicas: 0` placeholder operator awaiting its first build) · 51 Docker Hub images · 1 `kube-version-derived` risk (spire's kubectl hook image, at the currently-declared `1.35.6`). **Gate exits 0** — nothing currently rendered is an unacknowledged gating finding.

## Test plan

- [x] `bun test src/Core.TypeScript/cluster/image-resolvability.test.ts` — 45 pass (mocked registry + Hub-catalog client; no real network)
- [x] `bun test src/Core.TypeScript/cluster/image-footprint.test.ts src/Core.TypeScript/cluster/image-source-provenance.test.ts src/Core.TypeScript/cluster/rendered-storage-claims.test.ts` — 118 pass, no regression from the additive changes
- [x] `bunx tsc --noEmit` clean
- [x] `bun src/Core.TypeScript/ci/local-checks.ts --only "lint (TS)"` — ok
- [x] `bun src/Core.TypeScript/cluster/image-resolvability.ts --refresh` run for real against this tree from a Windows dev box (helm 4.2.0 pinned per `.mise.full.toml`, downloaded to a scratch dir outside the repo) twice — before and after the Docker Hub disambiguation fix — the committed `full-ai-cluster/k8s/image-resolvability.json` is the latest measurement
- [ ] CI (`helm-validate.yml`) — awaiting run on this PR

Agency-Signature-Version: 1
Agent: claude
Agent-Runtime: Claude Code
Agent-Model: claude-sonnet-5
Credential-Identity: maximdolphin
Credential-Mode: shared
Human-Review: not-implied-by-credential
Human-Review-Evidence: none
Action-Mode: human-directed
Task: 081M33RD7PW087G0R0011EVZYQ

🤖 Generated with [Claude Code](https://claude.com/claude-code)


## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 8 |
| Resolved threads | 8 |
| Unresolved threads | 0 |
| Total review comments | 8 |
| Total fix commits (touching thread paths) | 4 |

## Review threads

### Thread 1 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6kno3C`
- **Path**: `src/Core.TypeScript/cluster/image-resolvability.test.ts`
- **Replies**: 0

**Initial comment:**

- **`github-advanced-security[bot]` (bot)** at 2026-09-22T06:25:32Z on `src/Core.TypeScript/cluster/image-resolvability.test.ts`:242 (association: NONE)

  <pre>
  ## CodeQL / Incomplete URL substring sanitization

  '[registry-1.docker.io](1)' can be anywhere in the URL, and arbitrary hosts may come before or after it.

  [Show more details](https://github.com/Lucent-Financial-Group/Zeta/security/code-scanning/1029)
  </pre>

### Thread 2 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6kno3V`
- **Path**: `src/Core.TypeScript/cluster/image-resolvability.test.ts`
- **Replies**: 0

**Initial comment:**

- **`github-advanced-security[bot]` (bot)** at 2026-09-22T06:25:32Z on `src/Core.TypeScript/cluster/image-resolvability.test.ts`:255 (association: NONE)

  <pre>
  ## CodeQL / Incomplete URL substring sanitization

  '[registry-1.docker.io](1)' can be anywhere in the URL, and arbitrary hosts may come before or after it.

  [Show more details](https://github.com/Lucent-Financial-Group/Zeta/security/code-scanning/1030)
  </pre>

### Thread 3 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6kno3e`
- **Path**: `src/Core.TypeScript/cluster/image-resolvability.test.ts`
- **Replies**: 0

**Initial comment:**

- **`github-advanced-security[bot]` (bot)** at 2026-09-22T06:25:32Z on `src/Core.TypeScript/cluster/image-resolvability.test.ts`:269 (association: NONE)

  <pre>
  ## CodeQL / Incomplete URL substring sanitization

  '[registry-1.docker.io](1)' can be anywhere in the URL, and arbitrary hosts may come before or after it.

  [Show more details](https://github.com/Lucent-Financial-Group/Zeta/security/code-scanning/1031)
  </pre>

### Thread 4 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6kno3m`
- **Path**: `src/Core.TypeScript/cluster/image-resolvability.test.ts`
- **Replies**: 0

**Initial comment:**

- **`github-advanced-security[bot]` (bot)** at 2026-09-22T06:25:32Z on `src/Core.TypeScript/cluster/image-resolvability.test.ts`:281 (association: NONE)

  <pre>
  ## CodeQL / Incomplete URL substring sanitization

  '[registry-1.docker.io](1)' can be anywhere in the URL, and arbitrary hosts may come before or after it.

  [Show more details](https://github.com/Lucent-Financial-Group/Zeta/security/code-scanning/1032)
  </pre>

### Thread 5 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6kno3s`
- **Path**: `src/Core.TypeScript/cluster/image-resolvability.test.ts`
- **Replies**: 0

**Initial comment:**

- **`github-advanced-security[bot]` (bot)** at 2026-09-22T06:25:32Z on `src/Core.TypeScript/cluster/image-resolvability.test.ts`:282 (association: NONE)

  <pre>
  ## CodeQL / Incomplete URL substring sanitization

  '[hub.docker.com](1)' can be anywhere in the URL, and arbitrary hosts may come before or after it.

  [Show more details](https://github.com/Lucent-Financial-Group/Zeta/security/code-scanning/1033)
  </pre>

### Thread 6 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6kno3z`
- **Path**: `src/Core.TypeScript/cluster/image-resolvability.test.ts`
- **Replies**: 0

**Initial comment:**

- **`github-advanced-security[bot]` (bot)** at 2026-09-22T06:25:32Z on `src/Core.TypeScript/cluster/image-resolvability.test.ts`:292 (association: NONE)

  <pre>
  ## CodeQL / Incomplete URL substring sanitization

  '[hub.docker.com](1)' can be anywhere in the URL, and arbitrary hosts may come before or after it.

  [Show more details](https://github.com/Lucent-Financial-Group/Zeta/security/code-scanning/1034)
  </pre>

### Thread 7 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6kno36`
- **Path**: `src/Core.TypeScript/cluster/image-resolvability.test.ts`
- **Replies**: 0

**Initial comment:**

- **`github-advanced-security[bot]` (bot)** at 2026-09-22T06:25:32Z on `src/Core.TypeScript/cluster/image-resolvability.test.ts`:307 (association: NONE)

  <pre>
  ## CodeQL / Incomplete URL substring sanitization

  '[hub.docker.com](1)' can be anywhere in the URL, and arbitrary hosts may come before or after it.

  [Show more details](https://github.com/Lucent-Financial-Group/Zeta/security/code-scanning/1035)
  </pre>

### Thread 8 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6kno3_`
- **Path**: `src/Core.TypeScript/cluster/image-resolvability.test.ts`
- **Replies**: 0

**Initial comment:**

- **`github-advanced-security[bot]` (bot)** at 2026-09-22T06:25:32Z on `src/Core.TypeScript/cluster/image-resolvability.test.ts`:311 (association: NONE)

  <pre>
  ## CodeQL / Incomplete URL substring sanitization

  '[registry-1.docker.io](1)' can be anywhere in the URL, and arbitrary hosts may come before or after it.

  [Show more details](https://github.com/Lucent-Financial-Group/Zeta/security/code-scanning/1036)
  </pre>

## Fix commits (touching thread paths)

### `b2e60ca5c1bed8327de145299247dda5cbaddbfe` -- 2026-09-22T05:15:02Z -- `maximdolphin`

**Touched files (intersect with thread paths):**

- `src/Core.TypeScript/cluster/image-resolvability.test.ts`

**Message:**

```
feat(cluster): verify every first-boot image actually pulls, before a…

… USB install finds out the hard way

WP4. When the Zeta USB installer boots bare metal, K3S applies the
first-boot HelmChart roster and ArgoCD syncs every Application. If any
image those manifests reference does not exist at its pinned tag, or
exists with no linux/amd64 layer, the pod that needs it sits in
ImagePullBackOff forever -- a guaranteed crash-loop-equivalent, on a
box with nobody watching a CI log. Nothing in this tree asked the
registry that question before today.

image-resolvability.ts renders the first-boot roster + every
Application at the committed (metal) rung, extracts every image
(the generic `image:` walker, plus a small explicit table for images
an operator derives at runtime -- KubeVirt's virt-api/virt-controller
/virt-handler/virt-launcher/virt-exportproxy/virt-exportserver/
virt-synchronization-controller/pr-helper/sidecar-shim, none of which
is a literal string anywhere in this tree, and whose real names were
verified against upstream source rather than guessed: the first draft
wrote "virt-pr-helper", which does not exist), and resolves each one
against its registry's OCI distribution API. `missing` (404) and
`arch-missing` (resolves, no linux/amd64) fail; `unknown` (auth-walled
or a failed probe) warns with a count and never gates -- a probe that
could not be verified must never read as verified missing. Docker Hub
count, :latest/untagged, no-digest and (added below) kube-version-
derived tags are reported as risk, never gating.

Offline (PR-blocking) mode renders fresh and checks the current image
list against a checked-in snapshot; `--refresh` (riding helm-
validate.yml's existing daily cron, non-blocking) re-resolves live and
rewrites it -- same DV2.0 split image-source-provenance.ts already
uses, for the identical reason: six registries on every PR fails on
their bad afternoons, not ours.

ADDRESSED MID-TASK: a real NixOS first-boot VM found spire's bootstrap
chart deriving a hook image's tag from `.Capabilities.KubeVersion`
(docker.io/rancher/kubectl:v1.35.7, absent upstream), which this
checker could not have reproduced while it rendered at a hardcoded
version. Fixed by reading full-ai-cluster/k8s/kubernetes-version.json
-- never hardcoding -- for every render, Application and bootstrap
alike (rendered-storage-claims.ts gained an additive, opt-in
`RenderOptions.kubeVersion`; no existing caller's behaviour changes).
Any image whose tag exactly equals the render's kube-version is now
flagged `kube-version-derived`: fragile by construction, and exactly
what broke on real hardware.

MEASURED against the real tree (2026-09-22): 137 distinct images, 134
ok, 0 missing, 0 arch-missing, 3 unknown -- all genuine (a documented
replicas:0 placeholder operator; two ancient GitLab-bundled minio
tags where minio/minio now 401s on EVERY tag upstream, not a repo
defect). 51 Docker Hub images. 1 kube-version-derived risk (spire's
kubectl hook, at the currently-declared 1.35.6).

Also: exported `fetchManifest`/`MANIFEST_ACCEPT` from image-footprint.ts
(the generic WWW-Authenticate flow) for reuse rather than a second
implementation; supplied a Bun.spawnSync-backed `runHelm` to work
around node:child_process.spawnSync's unbounded-pipe ENOBUFS on large
`--include-crds` renders (cloudnative-pg, kube-prometheus-stack,
argo-cd, external-secrets, argo-rollouts, gha-runner-scale-set-
controller all hit it) rather than editing the shared renderer several
other validators use unmodified.

Agency-Signature-Version: 1
Agent: claude
Agent-Runtime: Claude Code
Agent-Model: claude-sonnet-5
Credential-Identity: maximdolphin
Credential-Mode: shared
Human-Review: not-implied-by-credential
Human-Review-Evidence: none
Action-Mode: human-directed
Task: 081M33RD7PW087G0R0011EVZYQ

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
```

### `953f9204eff14cc5f5ec9a89b84006c38a95da70` -- 2026-09-22T06:14:14Z -- `maximdolphin`

**Touched files (intersect with thread paths):**

- `src/Core.TypeScript/cluster/image-resolvability.test.ts`

**Message:**

```
fix(cluster): disambiguate docker.io's ambiguous 401 against Hub's ow…

…n catalog API

Architect follow-up on PR #17475. docker.io answers 401 identically
for a private repository and a DELETED one -- image-source-provenance.ts's
own header already names this ("Both ghcr.io and Docker Hub answer 401
... for a repository that was never created"), and this checker's first
cut inherited the same conflation, reading a deletion as merely `unknown`.

MEASURED: minio/minio and minio/mc were deleted from Docker Hub around
2026-09-11 -- every tag now 401s at the OCI registry, not just the two
ancient ones GitLab's bundled minio subchart defaults to.

dockerHubDisambiguate() consults Docker Hub's own catalog API
(hub.docker.com/v2/repositories/...), which is plain anonymous-readable
REST and answers a real 404 when the repository or tag is gone -- it is
not gated by the same pull-scope check that produces the ambiguous 401.
Wired in ONLY for docker.io tag references (never digests, never other
registries -- ghcr.io/quay.io keep their existing 401->unknown reading,
covered by image-source-provenance.ts's own packages-API route). A
DEFINITIVE 404 promotes the finding to `missing` (gating); a 200, a
network failure, or any other status leaves it `unknown` -- an
inconclusive disambiguation is exactly as failed as no probe at all.

This correctly turned both minio images gating. Since a SEPARATE change
(claude/gitlab-minio-image-gone) owns fixing GitLab's bundled minio
subchart and had not landed, added a small, dated, tracked, EXPIRING
acknowledgement register (ACKNOWLEDGED_MISSING) rather than leave this
PR's own gate red for a finding it is correct about but does not own
fixing, and rather than hide it: both images are still printed in their
own report section on every run, with their tracking branch and expiry
date, and the register self-expires 2026-10-06 -- past that date the
gate reds again on its own with no second mechanism to remember. Also
added the reverse drift check: an acknowledgement whose target no
longer matches a finding is named as safe to delete rather than
silently forgotten.

gatingRows() is now the ONE definition of "what fails the gate", used
by both formatReport (what a human reads) and main (the exit code), so
the two can no longer disagree about which rows counted.

14 new tests (45 total, up from 31): the four Hub-disambiguation
outcomes end to end via resolveImage, dockerHubDisambiguate() directly,
the acknowledgement register's own shape invariants (every entry names
a tracking branch and a reason; every key is already canonical), and
the self-expiry behavior.

Re-measured against the real tree: 2 missing (both minio images,
correctly attributed to full-ai-cluster/gitlab), both acknowledged,
gate exits 0.

Agency-Signature-Version: 1
Agent: claude
Agent-Runtime: Claude Code
Agent-Model: claude-sonnet-5
Credential-Identity: maximdolphin
Credential-Mode: shared
Human-Review: not-implied-by-credential
Human-Review-Evidence: none
Action-Mode: human-directed
Task: 081M33RD7PW087G0R0011EVZYQ

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
```

### `df5928dc76afb1d2f10ef1866f4610376cb37ab9` -- 2026-09-22T06:38:48Z -- `maximdolphin`

**Touched files (intersect with thread paths):**

- `src/Core.TypeScript/cluster/image-resolvability.test.ts`

**Message:**

```
fix(cluster): incomplete URL substring sanitization + wall-clock slee…

…ps in test fixtures

Architect follow-up on PR #17475. CodeQL flagged 8 medium
js/incomplete-url-substring-sanitization alerts: the mocked-fetch
fixtures routed on url.includes("registry-1.docker.io") /
url.includes("hub.docker.com"), which also matches a hostile URL
carrying that text in its path or query. Replaced with a hostnameOf()
helper that parses via `new URL(url).hostname` and compares exactly --
the same discipline dockerHubDisambiguate() itself already uses in the
module under test (host comes from parseImageReference, never from
string-matching a URL). No behavior change; these are test fixtures
with no untrusted input, so nothing was exploitable, but parsing is
what makes that true by construction rather than by argument.

Separately, `lint (bash retirement inventory + hygiene unit tests)`
was genuinely red after the main merge: audit-ambient-time-in-tests.ts
caught two real `setTimeout(..., ms)` wall-clock delays in
mapWithConcurrency's own test cases -- exactly the undeclared ambient
time channel `.claude/rules/local-time-never-enters-the-shared-fold.md`
and DV2.0 discipline #4 (DST) forbid. Rewrote both with the repo's own
deterministic-async barrier (`deferred()`): each item's `fn` suspends
on its own gate, gates resolve in a scrambled order to prove the
result-ordering property, and the DoP-ceiling test asserts `inFlight`
immediately after the call (mapWithConcurrency's `dop` workers start
SYNCHRONOUSLY, each up to its own first await, so the ceiling is
already enforced with no timer or turn needed) rather than sleeping
and hoping. Zero timers, faster (1.06s vs 2s), and the assertion is
now strictly stronger: it cannot pass by luck on a fast machine or
fail by bad luck on a loaded one.

The job's OTHER failing step (chart-currency, 16-day-old snapshot) is
pre-existing drift unrelated to this change -- its own step carries
`continue-on-error: true` and its comment states it explicitly:
"BEING BEHIND DOES NOT BLOCK A MERGE... outside the required set."
The job's red conclusion was driven by the ambient-time step, which
has no such flag; that is the one this commit fixes.

45/45 tests still pass, unchanged.

Agency-Signature-Version: 1
Agent: claude
Agent-Runtime: Claude Code
Agent-Model: claude-sonnet-5
Credential-Identity: maximdolphin
Credential-Mode: shared
Human-Review: not-implied-by-credential
Human-Review-Evidence: none
Action-Mode: human-directed
Task: 081M33RD7PW087G0R0011EVZYQ

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
```

### `c5ced42db2229e088daf24cc0eb60af98477ae33` -- 2026-09-22T06:45:45Z -- `maximdolphin`

**Touched files (intersect with thread paths):**

- `src/Core.TypeScript/cluster/image-resolvability.test.ts`

**Message:**

```
fix(cluster): inject the retry backoff clock, don't just eliminate th…

…e two the linter caught

Architect follow-up on PR #17475. The static ambient-time-in-tests
lint only catches a literal setTimeout(...) written IN a test file, so
it could not see that "a transient 503 is retried" indirectly spent
real wall-clock time INSIDE fetchWithRetry's own jittered backoff
(measured: that one test alone took 1.72s). Same undeclared-ambient-
time defect as the two the linter did catch, just one layer further
in -- and the fix is the other sanctioned form
audit-ambient-time-in-tests.ts names: inject the clock.

fetchWithRetry and resolveImage now take an optional `sleepFn`
(SleepFn = (ms) => Promise<void>), defaulting to the real setTimeout-
backed sleep in production. The one test that exercises a retry
(fetchWithRetry's transient-failure path) injects a no-op that records
its calls instead of waiting on them, so the RETRY COUNT and
CLASSIFICATION are still exercised for real -- the assertion is
unchanged, `sleepCalls.length === 1` confirms the backoff branch
genuinely ran -- while zero wall-clock time is spent proving it.

Full suite: 163 tests across the four related files, 1.2s (was
several seconds with the two real sleeps and the retry test's real
backoff combined).

Agency-Signature-Version: 1
Agent: claude
Agent-Runtime: Claude Code
Agent-Model: claude-sonnet-5
Credential-Identity: maximdolphin
Credential-Mode: shared
Human-Review: not-implied-by-credential
Human-Review-Evidence: none
Action-Mode: human-directed
Task: 081M33RD7PW087G0R0011EVZYQ

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
```
