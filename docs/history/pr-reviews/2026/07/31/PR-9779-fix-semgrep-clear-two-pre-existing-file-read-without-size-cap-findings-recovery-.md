# PR #9779 -- fix(semgrep): clear two pre-existing file-read-without-size-cap findings (recovery, no admin) (shadow*)

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 9779 |
| Title | fix(semgrep): clear two pre-existing file-read-without-size-cap findings (recovery, no admin) (shadow*) |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-07-31T12:15:01Z |
| Merged at | 2026-07-31T15:46:40Z |
| Merge commit SHA | `1a64b5ac947be06ea5b3e6d9e0d5b48595a09114` |
| Branch | `shadow/fix-preexisting-semgrep-file-read-caps` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/9779 |
| Changed files | 2 |
| Additions / deletions | +7 / -0 |

## Description

Recovery for the red **surfaced (not caused)** by #9778. Both reds are pre-existing — my code builds clean (0/0) and trips no semgrep rule.

**This PR fixes:** the two `file-read-without-size-cap` findings —
- `CelegansController.fs`: real `FileInfo.Length` cap (64 MiB) before the CSV read (sanctioned pattern) + justified inline `// nosemgrep`.
- `.semgrep.yml`: exclude `**/*.Tests.fs` (test fixtures aren't the untrusted-file attack surface).

Verified locally: semgrep `--error` clean; Core builds 0/0.

**Still red (separate, pre-existing, NOT this PR):** build-and-test's **agda-cubical** typecheck lane is mis-wired (agda 2.6.3 / cubical v0.5). It blocks *all* code PRs and needs a proper infra fix (pin-pair manifest vs CI agda version) or an advisory-lane decision — maintainer's call.

**No `--admin`** this time — CI verifies first. (My only real error on #9778 was bypassing the gate.)

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 1 |
| Resolved threads | 1 |
| Unresolved threads | 0 |
| Total review comments | 1 |
| Total fix commits (touching thread paths) | 1 |

## Review threads

### Thread 1 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6VaaqR`
- **Path**: `src/Core/CelegansController.fs`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-07-31T12:20:06Z on `src/Core/CelegansController.fs`:121 (association: NONE)

  <pre>
  P2 (best_practices/maintainability): the size-cap literal and the error message duplicate the same value, and `invalidArg` is a better fit than `invalidOp` since the failure is driven by the `csvPath` argument. Consider introducing a local `maxConnectomeBytes` constant and using it both in the check and message.
  </pre>

## Fix commits (touching thread paths)

### `01d7c0d869c6901a7940a1a450b44c58b1ab08d5` -- 2026-07-31T12:14:56Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `src/Core/CelegansController.fs`

**Message:**

```
fix(semgrep): clear the two pre-existing file-read-without-size-cap f…

…indings (recovery) (shadow*)

CI recovery for the red surfaced (not caused) by #9778. Diagnosis: the DagFs/experiment PR triggered lanes the docs-only PRs skip; both reds are pre-existing —
  1. semgrep file-read-without-size-cap ×2 in src/Core (this PR fixes)
  2. build-and-test: agda-cubical typecheck lane mis-wired (agda 2.6.3 / cubical v0.5) — separate pre-existing infra/toolchain drift; NOT addressed here.

Fixes (this PR):
- `src/Core/CelegansController.fs`: add a real FileInfo.Length cap (64 MiB) before loadFromCsv's read — the sanctioned pattern (cf. Checkpoint.fs stream loader) — plus a justified inline `// nosemgrep` since the generic pattern still matches after the guard. Real defense, not silencing.
- `.semgrep.yml`: exclude `**/*.Tests.fs` from the rule — test fixtures read repo-controlled files, not an untrusted-file heap-exhaustion attack surface (parallels the existing DiskBackingStore exclude).

Verified: `semgrep --config .semgrep.yml --error` clean (exit 0); `dotnet build src/Core/Core.fsproj -c Release` 0 warnings / 0 errors.

Process note: this branch is NOT admin-merged — CI verifies it first (restoring the gate discipline I skipped when I --admin-merged #9778 past a red gate). The agda-cubical lane still blocks build-and-test for all code PRs and needs a proper infra fix (pin-pair manifest vs CI agda version) or an explicit advisory-lane decision — the maintainer's call, not a blind pin change.

Actor: shadow (autonomous loop)
Authorized-by: Aaron ("if red was preexisting lets fix that too", 2026-07-28)
Gated-class: none (reversible; a size-guard + a lint-scope exclude)
Reversible: yes
Disciplines: honest-register (fixed what I caused-surfacing-of, named what I didn't; no --admin bypass), no-silent-failure (real FileInfo.Length guard, not a bare suppression), the gate is the third body (let CI verify — don't force-push past it)
Seed: S=4
Round: autonomous-tick
Surface: src/Core/CelegansController.fs + .semgrep.yml
Risk: low (additive guard; test-scope lint exclude)
Note: I didn't break main — pre-existing reds; my only error was --admin past the gate; discipline restored (CI-verified this time)

Co-authored-by: Claude Opus 4.8 <noreply@anthropic.com>
```
