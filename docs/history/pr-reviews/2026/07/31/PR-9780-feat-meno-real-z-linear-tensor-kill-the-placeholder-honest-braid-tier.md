# PR #9780 -- feat(Meno): real Z-linear tensor ⊗ (kill the placeholder) + honest braid tier

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 9780 |
| Title | feat(Meno): real Z-linear tensor ⊗ (kill the placeholder) + honest braid tier |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-07-31T13:23:38Z |
| Merged at | 2026-07-31T13:23:47Z |
| Merge commit SHA | `179893ee606fcae87ef300adf8bab6973f32090c` |
| Branch | `shadow/meno-real-tensor` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/9780 |
| Changed files | 2 |
| Additions / deletions | +55 / -9 |

## Description

## What

`Meno.tensor` was a placeholder (`ZSet.empty`). It is now the **real Z-linear (Kronecker) tensor** of Meno arrows over the free ZSet module: for each basis pair `(a,c)` of weight `w`, emit `w·(f{a} ⊗ g{c})` and sum. `first`/`second` implemented likewise (`f⊗id`, `id⊗f`).

## Why now

Soraya's routing verdict split the braid claim cleanly: the braid **group** (`Braid.fs`) is real; the braided monoidal **category** was vocabulary + a stubbed skeleton whose `tensor` returned `ZSet.empty`. This lands the real ⊗ — which is also the load-bearing prereq for **factor-graph → Bayesian-neural-network composition** (⊗ = how probabilistic units stack into layers).

## Honest tier (no overclaim)

The Cartesian ⊗ makes the category **symmetric** monoidal (Mod_ℤ), so `braid` is the **swap** (σ²=id), **not** a genuine braid. A braided monoidal category (σ²≠id, the `Braid.fs` crossing) needs an **R-matrix / Yang–Baxter** braiding — a separate build. The `braid` doc now says exactly this and warns against FsCheck-ing coherence against the swap (symmetric ⇒ hexagons hold trivially ⇒ **false-green** — Soraya's flag).

## Tests

- **MENO-4**: the Kronecker product — weights multiply, no cross terms.
- **MENO-5**: `first` = `f ⊗ id`.
- `PrivacyAndMenoTests` **8/8 passing** locally.

## Next (not this PR)

R-matrix braiding wired from `Braid.fs` → then FsCheck naturality + two hexagons + bifunctoriality → then Lean4 `CategoryTheory.Monoidal.Braided`. Only *then* is "braided" earned.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 0 |
| Resolved threads | 0 |
| Unresolved threads | 0 |
| Total review comments | 0 |
| Total fix commits (touching thread paths) | 0 |

## Review threads

(no review threads on this PR)

## Fix commits (touching thread paths)

(no commits in this PR touched files anchored by review threads)
