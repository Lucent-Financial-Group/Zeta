# PR #9141 -- fix(cross-verify): register IDbspOperators/IKleeneAlgebra/IRing in the interface oracle

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 9141 |
| Title | fix(cross-verify): register IDbspOperators/IKleeneAlgebra/IRing in the interface oracle |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-07-02T14:59:34Z |
| Merged at | 2026-07-02T14:59:43Z |
| Merge commit SHA | `1d073f5895ad651bc4f195afc2e656e4af1793d7` |
| Branch | `otto/fix-cross-verify-interface-oracle-dbsp-kleene` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/9141 |
| Changed files | 2 |
| Additions / deletions | +59 / -7 |

## Description

**Self-caught regression.** The DBSP + IKleeneAlgebra oracle mirrors (#9137/#9138) added new interface IRs but didn't update the `zeta-ir-v2` interface oracle's frozen allowlist, so `cross-verify-all.ts` (the byte-lock step of gate.yml's cross-verify job) failed with "unexpected interface" — `cross-verify-all: 30/31`. It slipped through because that job is **non-required** (doesn't block auto-merge) and I'd only run the interface *law* test, not the byte-lock oracle. Caught it on the next tick's CI health check.

## Fixes
- **Register `IDbspOperators`, `IKleeneAlgebra`, `IRing`** in `expectedNames` (IRing was a pre-existing miss from whoever added `ring.ir.json`; registering is how a new frozen interface is admitted).
- **Transitive `IStarRing extends ISemiring` check.** It was direct-only, but the `081KWG9JQ9H` split rebased `IStarRing` onto `IRing` (`extends: ["IRing"]`, and `IRing extends ["ISemiring"]`). Made the check walk the extends chain so the legitimate `IStarRing→IRing→ISemiring` hierarchy passes.
- Regenerated the committed `ts-output.json` snapshot (now 11 interfaces).

## Result
Interface oracle **0 mismatches**; **cross-verify-all 31/31**; interface law + codegen-drift suites **16 pass / 0 fail**. The byte-lock oracle is green again.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 1 |
| Resolved threads | 0 |
| Unresolved threads | 1 |
| Total review comments | 1 |
| Total fix commits (touching thread paths) | 1 |

## Review threads

### Thread 1 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM6N8IOt`
- **Path**: `tests/cross-verification/zeta-ir-v2/ts-output.json`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-07-02T15:03:42Z on `tests/cross-verification/zeta-ir-v2/ts-output.json`:54 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Restore IKleeneAlgebra's semiring inheritance**

  Registering `IKleeneAlgebra` with `extends: []` freezes a shape that disagrees with the existing C# and TS contracts (`src/Core.Abstractions/IKleeneAlgebra.cs` and `src/Core.TypeScript/algebra/kleene.ts` both make it extend `ISemiring`). Any interface generator or drift check consuming this snapshot will emit/accept a standalone Kleene algebra that redeclares semiring operations instead of inheriting the semiring tier, so the oracle no longer protects the intended algebra hierarchy for this newly admitted interface.

  Useful? React with 👍 / 👎.
  </pre>

## Fix commits (touching thread paths)

### `0a54b3dfdfa14b2cb943e8b6180d2e95ddb0162b` -- 2026-07-02T14:59:30Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tests/cross-verification/zeta-ir-v2/ts-output.json`

**Message:**

```
fix(cross-verify): register IDbspOperators/IKleeneAlgebra/IRing in th…

…e interface oracle (shadow*)

Self-caught regression. The DBSP + IKleeneAlgebra oracle mirrors (#9137/#9138) added
new interface IRs but did NOT update the zeta-ir-v2 interface oracle's frozen
allowlist (cross-verify.ts `expectedNames`), so `bun src/Core.TypeScript/ci/
cross-verify-all.ts` — the byte-lock/golden-vector step of gate.yml's cross-verify job
— failed with "unexpected interface" (cross-verify-all: 30/31). It went unnoticed
because that job is NON-REQUIRED (doesn't block auto-merge) and I'd only run the
interface LAW test, not the byte-lock oracle. Caught it on the next tick's CI health
check.

Fixes:
- Register IDbspOperators, IKleeneAlgebra, and IRing in `expectedNames` (IRing was a
  pre-existing miss from whoever added ring.ir.json; registering here is how a new
  frozen interface is admitted).
- The `IStarRing must extend ISemiring` check was direct-only, but the 081KWG9JQ9H
  split rebased IStarRing onto IRing (extends ["IRing"], IRing extends ["ISemiring"]).
  Made the check TRANSITIVE (walk the extends chain) so the legitimate
  IStarRing→IRing→ISemiring hierarchy passes.
- Regenerated the committed ts-output.json snapshot (now 11 interfaces).

Result: interface oracle 0 mismatches; cross-verify-all 31/31; interface law +
codegen-drift suites 16 pass / 0 fail. The byte-lock oracle is green again.

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>

AgencySignature-v1:
  persona: otto
  actor: zeta-otto
  surface: tests/cross-verification
  topology: shadow-clone
  intent: fix-interface-oracle-register-new-irs-transitive-starring-extends-check
  authorization: aaron-standing-24h-autonomous-2026-07-02
  uncertainty: low
  measure: expectednames-adds-idbsp-ikleene-iring-starring-extends-check-transitive-ts-output-regenerated-cross-verify-all-31-of-31-green
  delta-u: byte-lock-oracle-restored-to-green-self-caught-non-required-regression-from-the-ir-mirrors
  seed: S4
```
