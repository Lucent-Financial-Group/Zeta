# PR #9175 -- feat(core): SchedulerZeta.runToHorizon - the recurrence prediction made LOAD-BEARING

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 9175 |
| Title | feat(core): SchedulerZeta.runToHorizon - the recurrence prediction made LOAD-BEARING |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-07-02T18:15:14Z |
| Merged at | 2026-07-02T18:15:18Z |
| Merge commit SHA | `e636c27bb2de15b2a9d3958d2ad02fe11dd710ee` |
| Branch | `otto/scheduler-zeta-load-bearing-fast-forward` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/9175 |
| Changed files | 3 |
| Additions / deletions | +66 / -0 |

## Description

You chose *make SchedulerZeta load-bearing*: the running scheduler should consult its recurrence prediction to skip re-simulating a config it can prove is on a known orbit. That's exactly `runToHorizon`.

## src/Core/SchedulerZeta.fs
`runToHorizon key step start horizon` returns the state after `horizon` ticks, but **fast-forwards through detected recurrence**: record the trajectory only until it enters a cycle (transient t, period p), then compute the state at tick `horizon` by index arithmetic `states[t + (horizon - t) mod p]` - not `horizon` invocations of `step`. Work is O(reachable), independent of horizon size.

## Tests (5/5, +2)
- **runToHorizon == naive step^n for every horizon** (the correctness guarantee);
- **LOAD-BEARING**: fast-forwards the REAL CHIP-8 config through **1,000,000,000 frames** in bounded work (O(reachable ~ 5)) - naive would be 4e9 Chip8.step calls; the test runs in ~78ms. The scheduler skips re-simulating a config it has proven periodic.

Turns the proven capstone (#9172) into a USED capability: recurrence prediction that saves real work at runtime. Deeper async PredictionScheduler auto-invocation (detect periodicity mid-run, fast-forward the budget) is the follow-up. RESUME updated.

Anchors: Artin-Mazur 1965; SoftScheduler/CellScheduler; Chip8.step; #9172.

Generated with Claude Code

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 1 |
| Resolved threads | 0 |
| Unresolved threads | 1 |
| Total review comments | 1 |
| Total fix commits (touching thread paths) | 1 |

## Review threads

### Thread 1 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM6N_ZRB`
- **Path**: `src/Core/SchedulerZeta.fs`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-07-02T18:16:56Z on `src/Core/SchedulerZeta.fs`:72 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Don't fast-forward full states from projected keys**

  When callers use the documented `key` as a finite projection rather than an injective state identity, a repeated key here does not prove the full `'S` state has repeated, but the function then returns a cached full state for later horizons. For example, `step x = x + 1`, `key x = x % 2`, `start = 0`, and `horizon = 5` stops on the key repeat at `2` and returns cached state `1` instead of the naive state `5`, violating the new “equal to `stepⁿ`” guarantee; this should either detect cycles on full states or return/guarantee only projected observations with an enforced congruence/injectivity requirement.

  Useful? React with 👍 / 👎.
  </pre>

## Fix commits (touching thread paths)

### `6228da93cfe79204710eb99934eb3bef88340d37` -- 2026-07-02T18:15:10Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `src/Core/SchedulerZeta.fs`

**Message:**

```
feat(core): SchedulerZeta.runToHorizon — the recurrence prediction ma…

…de LOAD-BEARING (shadow*)

Aaron chose "make SchedulerZeta load-bearing": the running scheduler should CONSULT its
recurrence prediction to skip re-simulating a config it can prove is on a known orbit.
That is exactly runToHorizon.

src/Core/SchedulerZeta.fs — `runToHorizon key step start horizon` returns the state
after `horizon` ticks, but FAST-FORWARDS through detected recurrence: it records the
trajectory only until it enters a cycle (transient t, period p), then computes the
state at tick `horizon` by index arithmetic `states.[t + (horizon − t) mod p]` — NOT
`horizon` invocations of `step`. Work is O(reachable), independent of horizon size.

tests/Tests.FSharp/SchedulerZeta.Tests.fs (5/5, +2):
  • runToHorizon == the naive step^n for every horizon (the correctness guarantee);
  • LOAD-BEARING: fast-forwards the REAL CHIP-8 config through 1_000_000_000 frames in
    bounded work (O(reachable ≈ 5)) — naive would be 4×10⁹ Chip8.step calls; the test
    runs in ~78ms. The scheduler skips re-simulating a config it has proven periodic.

This turns the proven capstone (#9172) into a USED capability: recurrence prediction
that saves real work at runtime. Deeper async PredictionScheduler auto-invocation
(detect periodicity mid-run, fast-forward the budget) is the follow-up. RESUME updated.

Anchors: Artin–Mazur 1965 (the orbit structure); SoftScheduler/CellScheduler (the DoP=1
deterministic tick); Chip8.step; #9172 (SchedulerZeta).

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>

AgencySignature-v1:
  persona: otto
  actor: zeta-otto
  surface: src/core
  topology: shadow-clone
  intent: scheduler-zeta-runtohorizon-recurrence-prediction-load-bearing-fast-forward
  authorization: aaron-explicit-make-schedulerzeta-load-bearing-2026-07-02
  uncertainty: low
  measure: runtohorizon-fast-forwards-through-recurrence-o-reachable-eq-naive-step-n-every-horizon-real-chip8-1e9-frames-bounded-work-78ms-5-tests
  delta-u: capstone-capability-made-load-bearing-scheduler-skips-re-simulating-proven-periodic-config-billion-frames-microseconds
  seed: S4
```
