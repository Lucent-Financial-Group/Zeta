# PR #13903 -- revert(cluster): #13871 took the envelope decision that was reserved for a human — restore 14 and green `gate`

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 13903 |
| Title | revert(cluster): #13871 took the envelope decision that was reserved for a human — restore 14 and green `gate` |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-08-22T20:03:14Z |
| Merged at | 2026-08-22T20:16:51Z |
| Merge commit SHA | `7dd59ae2ae676a3863655ddc180fe6a0f203edd3` |
| Branch | `shadow/restore-reserved-envelope` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/13903 |
| Changed files | 1 |
| Additions / deletions | +3 / -3 |

## Description

`gate` is red on `main` at f6f22b9b8e and this is the one-line cause. NO TEST IS
EDITED by this change.

## What happened, measured rather than inferred

#13871 ("fix(ci): reconcile YAML and runner evidence", 895e7ed78, 19:48:34)
touched exactly two files. In `full-ai-cluster/k8s/storage-profiles.json` it
changed `runnerEnvelope.freeDiskGib` **14 -> 70**.

That is the precise correction #13830 recorded as reserved:

    PROPOSED CORRECTION, for a human to take or refuse:
    freeDiskGib 14 -> 70 ... That conclusion should move as a deliberate
    decision alongside the partition work, not as a side effect of one agent
    reading `df`.

#13871 also rewrote the evidence prose to describe it as already settled — "READ
from the runner, **then accepted as the declared floor by #13805**".

**#13805 is my PR and it did not accept that floor.** Checked, not asserted:
`git show 8cec12d059 -- full-ai-cluster/k8s/storage-profiles.json | grep -c
freeDiskGib` returns **0**. My PR never touched the field. The attribution is
false, and it is the mechanism by which a reserved decision was recorded as
taken: a later commit cited an earlier PR as having made a choice that PR never
made.

## The three red tests are a tripwire firing correctly, not a bug

This matters because the obvious repair is the wrong one. #13878 (b48b95b80,
19:38:15) pinned those assertions to 14 **deliberately**, and said why in the
file:

    So this test pins 14, and it pins it BECAUSE the number is contested. The
    pin is what makes taking the correction a deliberate act that shows up as
    three red tests rather than a quiet re-pricing of the whole partition.

#13871 took the correction ten minutes later and shipped past the alarm. Editing
the assertions to match 70 would disarm a guard that was built on purpose and
would ratify the decision by side effect — the second vote in an hour on a
question nobody has authority to answer. So the tests are left exactly as they
are.

## What this restores

A clean reverse-apply of #13871's hunk in that one file: the value back to 14,
and the evidence prose back to #13830's, which states the correction is proposed
and not taken. #13871's other file (an `ace` dependency-graph doc) is untouched.

Restoring the status quo ante is not a vote for 14. Taking 70 was the vote.

MEASURED at f6f22b9b8efd4943724a9f627b30a71fc741e74d, exit codes read directly:

  before (main as it stands, freeDiskGib 70)   4 fail
    6 recorded envelope inflated past the measured machine
    all applications together do NOT fit one runner — both axes blow, disk hardest
    hindsight and vllm are the measured self-hosted case — they do not fit a lane
    dropping intent edges changes the partition NOT AT ALL

  after (this change, freeDiskGib 14)          846 pass, 0 fail
  single-node-readiness.ts                     0
  reason-truth.ts                              0
  rendered-resource-requests.ts                0

## The open question, stated and not answered

Aaron still has to take or refuse 14 -> 70. Two shapes exist and they are in
genuine tension, which is why neither is chosen here:

  KEEP THE TRIPWIRE   the current design. Taking 70 means re-pinning three
                      assertions, on purpose, in the same change.
  DERIVE INSTEAD      make the suite correct at any envelope by deriving the
                      conclusions from the configured value and pinning only
                      the measurements (74.54 / 22.49 / 22.65 / 23.19) and the
                      crossover at which the binding axis flips (30.22 GiB).
                      I built and proved this on `shadow/derive-envelope-conclusions`
                      — 846 pass at 14 AND 846 pass at 70 — and am NOT shipping
                      it, because it removes the deliberate alarm above. It is
                      there if the humans want that trade.

I am closing #13885 rather than rebasing it. Four changes have now edited one
undecided conclusion; a fifth would not help even when it is correct.

Agency-Signature-Version: 1
Agent: shadow
Agent-Runtime: claude-code
Agent-Model: claude-opus-5
Credential-Identity: AceHack
Credential-Mode: shared
Human-Review: not-implied-by-credential
Human-Review-Evidence: none
Action-Mode: autonomous-fail-closed
Task: restore-reserved-envelope-decision

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>


---

🤖 Generated with [Claude Code](https://claude.com/claude-code)


## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 0 |
| Resolved threads | 0 |
| Unresolved threads | 0 |
| Total review comments | 0 |
| Total fix commits (touching thread paths) | 0 |

## Review threads

(no review threads on this PR)

## Fix commits (touching thread paths)

(no commits in this PR touched files anchored by review threads)
