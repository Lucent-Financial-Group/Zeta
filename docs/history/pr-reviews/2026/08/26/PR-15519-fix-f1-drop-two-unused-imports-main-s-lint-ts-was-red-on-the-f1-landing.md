# PR #15519 -- fix(f1): drop two unused imports — main's `lint (TS)` was red on the F1 landing

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 15519 |
| Title | fix(f1): drop two unused imports — main's `lint (TS)` was red on the F1 landing |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-08-26T07:15:02Z |
| Merged at | 2026-08-26T07:28:29Z |
| Merge commit SHA | `01bd090ed32c088053218ae1272c4e368c99bab3` |
| Branch | `fix/f1-unused-imports` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/15519 |
| Changed files | 1 |
| Additions / deletions | +0 / -3 |

## Description

`main` went red **two ways** on the F1 landing (#15518). This fixes both.

## 1. `lint (TS)` — two unused imports

`f1-verify-asymmetry.ts` imported `writeFileSync` and `join` and used neither, so
`tsc --noEmit` failed on `main`:

```
f1-verify-asymmetry.ts(19,1): 'writeFileSync' is declared but its value is never read.
f1-verify-asymmetry.ts(20,1): 'join' is declared but its value is never read.
```

Verified each is a **single occurrence** — the import line itself — before removing.
`tsc --noEmit -p tsconfig.json` now exits **0**, zero errors.

## 2. `agencysignature (main tip)` — cleared by this commit existing

`2df0a6ccdf08` carries only `Co-authored-by: Kiro`, with no `Agency-Signature-Version: 1`
block. Since 2026-08-14 exemption is **fail-closed**: an identity absent from
`agency-signature-identity-roster.json` is agent-or-unknown by construction, so the
audit is right to refuse it. The prescribed remedy is re-attaching trailers to the
**next** commit rather than rewriting history — which is what this is.

**I sign as myself and do not assert Kiro's attestation.** Adding Kiro to the roster
grants a standing exemption — a reviewed one-line diff, and per the audit's own text
*"which is the point."* That's a governance call, not a lint fix.

## For the next benchmark PR

The block must appear in **both** the commit message **and** the PR body bottom. They
are two separate checks and neither satisfies the other — a squash merge strips
trailers the body didn't carry.

*(None of this touches the F1 result itself, which stands: gemma2:2b 0% produce →
69.5% verify. The asymmetry is real.)*

Agency-Signature-Version: 1
Agent: shadow
Agent-Runtime: claude-code
Agent-Model: claude-opus-5
Credential-Identity: AceHack
Credential-Mode: shared
Human-Review: not-implied-by-credential
Human-Review-Evidence: none
Action-Mode: autonomous-fail-closed
Task: none
Co-authored-by: Claude Opus 5 <noreply@anthropic.com>


## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 0 |
| Resolved threads | 0 |
| Unresolved threads | 0 |
| Total review comments | 0 |
| Total fix commits (touching thread paths) | 0 |

## Review threads

(no review threads on this PR)

## Fix commits (touching thread paths)

(no commits in this PR touched files anchored by review threads)
