# PR #15531 -- research(ci): the Actions surface as a SET — one required check, 81 advisories, and vacuity its own detectors already documented

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 15531 |
| Title | research(ci): the Actions surface as a SET — one required check, 81 advisories, and vacuity its own detectors already documented |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-08-26T07:56:10Z |
| Merged at | 2026-08-26T08:10:09Z |
| Merge commit SHA | `785ec83ff91126e81531a2503b44f4f117583265` |
| Branch | `audit/actions-surface` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/15531 |
| Changed files | 1 |
| Additions / deletions | +1263 / -0 |

## Description

First audit of **all 82 workflows (21,478 lines) as a set**, at a pinned SHA, with **224,291 measured runs** behind it. **Findings only — not one `.yml` byte changes.** Changing what blocks is a treaty amendment and a human decision (drift-and-heal ADR, 2026-08-01); this is the measurement such a decision would need.

Doc: `docs/research/2026-08-26-the-actions-surface-audited-as-a-set-one-required-check-eighty-one-advisories-and-the-vacuity-that-outlived-its-own-detectors.md`

## The fact that reorders the other four questions

Exactly one ruleset carries a `required_status_checks` rule, and it names one context:

```
GET /rulesets/16134995  "CI Gate"  active  ~DEFAULT_BRANCH
  required_status_checks: [ { context: "gate (required)", integration_id: 15368 } ]
```

**81 of 82 workflows cannot block a merge, by construction.** That makes the consequence ranking a fact rather than a preference: only a vacuous assertion inside `gate-required`'s transitive `needs` closure can let a defect through a merge.

## Tier 0 — live on `main` at `dc15f463df`

`gate.yml:3519` is the whole verdict:

```bash
results='${{ toJSON(needs.*.result) }}'
if echo "$results" | grep -qE '"(failure|cancelled)"'; then exit 1; fi
```

`matrix-setup` and `path-filter` are **not** in `gate-required.needs` (3459-3465), but `build-and-test` needs both (395) and `full-verify` needs `path-filter` (3205). A failure in either marks its dependants `skipped`, `skipped` counts as success, and the sole required check goes green with the .NET build and the seven-language byte-lock never run. A third trace needs nothing to fail at all: `path-filter.outputs.code` is `${{ steps.detect.outputs.code || steps.nonpr.outputs.code }}` — an empty string skips `full-verify` and every heavy step inside `build-and-test`.

**Measured why it hides.** Across the 300 most recent green `gate` runs (2026-08-24 → 2026-08-26): `full-verify` was **`skipped` in 182 (60.7%)**. Three of five green required checks legitimately exclude the byte-lock, so `skipped` is the common case, not an anomaly a reader notices. *"The path filter said docs-only"* and *"the path filter is dead"* are the same string in the same field.

A fix for exactly this exists **uncommitted in another clone** and is deliberately not in this PR.

## Tier 1 — the watchdog layer reporting green on what it exists to catch

- **BD001 (`drift-sweep.yml:296-307`)** — the detector for a red `main` build says **"main build green" on a CANCELLED gate run**: its `jq` selects only `.conclusion=="failure"`, and cancelled legs conclude `cancelled`. Population: **6,817 of 35,467 gate runs are cancelled (19.2%)**; push-run coverage on main is 16.4%; the repo's own memory carries *"88% of gate runs on main cancelled."* Second blind spot in the same step: no `pipefail`, so a dead API reads as "no drift".
- **`proof-closure-drift.yml:131`** — `bun … audit-proof-closure-claims.ts | tee audit.txt`. No `pipefail`, so `steps.audit.outcome` is permanently `success` and the run summary **prints "No proof file claims a closure it does not have" on a run where the auditor printed findings.** Reporting the negation of your own measurement.
- **`lint-autofix-apply.yml`** — a `contents: write` lane with **no reachable non-zero path**: line 70 routes every API failure to `exit 0`, line 139's `else` has no `exit 1`, and the push branch at 111 requires `head_branch == "main"` which its `pull_request`-only producer cannot supply. **6,675 runs, 0 failures.**
- **`mux-swarm-tick.yml`** — no push, no artifact, no step after the tick. **299 greens whose output never left the runner.**
- Plus silent-failure sites in `agent-heartbeat.yml` (786, 1513, 1309, 351) where **the fix already exists 1,000 lines lower in the same file**, and `memory-index-integrity.yml` (mutation-demonstrated: an unresolvable base SHA makes `git diff` print `fatal:` and the check exit 0).

## Demonstrated, not asserted

```
$ printf 'set -uo pipefail\nfalse\nrc=$?\necho reached\n' > t.sh && bash -e t.sh ; echo $?
1                          ← "reached" never printed
$ printf 'set +e\nfalse\nrc=$?\necho reached\n' > t2.sh && bash -e t2.sh ; echo $?
reached
0                          ← the control
```

`set -uo pipefail` does not clear a `-e` that arrived on the command line. Consequences: **`ruleset-apply.yml:82-88` can only apply when there is nothing to apply** (exit 1 = DRIFT aborts the plan step), and **`agencysignature-enforcement.yml:266-282` collapses REFUTED and COULD-NOT-VERIFY into one indistinguishable red** — the exact conflation its own header says it exists to remove, on 8,680 runs. Both corrections are already written out in `github-settings-drift.yml:132-142`, in this repo.

Also measured inert: `gate.yml:1051`'s `-not -path "src/Core.Lean4/.lake/*"` under `find tools` — 22 files with it, 22 without.

## Dead

- **Six referenced secrets do not exist** (2 repo variables, 0 org, 0 environment; 15 secrets). `RULESET_READER_TOKEN` means **ruleset drift has never been checked** — confirmed from live annotations, with `github-settings-drift.yml:226-233` unreachable code. `ZETA_REALTIME_URL` is dead three independent ways.
- **`mirror-to-fork.yml` is fixed, and the damage is now measured: 21,418 skipped runs of 23,375.**
- Four workflows have never run; `passkey-proposal-gated-commit.yml` is the one that should raise a question (it triggers on `issues`).

## Report-but-cannot-block, split honestly

Seven deliberate non-blockers with their decision and evidence quoted (Windows/macOS legs at a measured 7.9%/5.9%, reproduced here as 8 of 300 green runs carrying a failed platform leg — and **what breaks if you make them blocking**), against eight accidental ones.

## I refuted one of my own findings

`agent-reviewer.yml` was reported dormant on a 100%-skip window over the last 100 runs. It succeeded on **2026-08-25T15:52:26Z on `heartbeat/alexa-flush`**, 21 of the last 300 PRs carry a `-flush` ref, and its verifier can genuinely fail (`set -euo pipefail` before the `| tee`). Recorded rather than deleted, because the reasoning that produced it is what the document warns about: a skip rate is a prompt to look, and looking is what refutes it.

## The pattern worth more than any single finding

Most of the vacuity found is **documented as fixed somewhere else in the same repo, often the same file** — V-5's correction is spelled out at length in `github-settings-drift.yml`, V-8's fix is a thousand lines below it in `agent-heartbeat.yml`, V-10 violates a prohibition its own file states 260 lines earlier. That is a **budget** failure, not a care failure. §7 names the three ~50-line mechanical audits (`vars` resolve, assertion-survives-pipe, `continue-on-error`-has-reader) that would have caught these — including the ten-week mirror outage on the day its gate was written — and §7 also lists the six surfaces that already do this correctly (`helm-validate.yml` pairs every validator with a mutation suite proving it exits 1).

## What I could not verify

§8 is explicit: no mutation was run against a live workflow (V-0's traces are proofs by construction, and staging one would change CI behaviour, which this PR forbids); one `xargs` link is unproven on the runner; seven suspicions are labelled with mechanism and no proof; six of nine zero-failure workflows are left open rather than promoted; run counts are point-in-time.

**Methodological note:** the shared clone this started in carried **uncommitted WIP in `gate.yml`** that would have made the top finding read as already fixed. Everything is re-verified against `origin/main` pinned at `dc15f463dfb8c44543f217028d6879ca9da39a0e`.

Agency-Signature-Version: 1
Agent: shadow
Agent-Runtime: claude-code
Agent-Model: claude-opus-5
Credential-Identity: AceHack
Credential-Mode: shared
Human-Review: not-implied-by-credential
Human-Review-Evidence: none
Action-Mode: autonomous-fail-closed
Task: none
Co-authored-by: Claude Opus 5 <noreply@anthropic.com>


## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 0 |
| Resolved threads | 0 |
| Unresolved threads | 0 |
| Total review comments | 0 |
| Total fix commits (touching thread paths) | 0 |

## Review threads

(no review threads on this PR)

## Fix commits (touching thread paths)

(no commits in this PR touched files anchored by review threads)
