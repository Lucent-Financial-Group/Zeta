# PR #10765 -- math(braid): ribbon needs a dualizable object — the rank equation excludes Burau and LKB, and Schur does not collapse the certificate

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 10765 |
| Title | math(braid): ribbon needs a dualizable object — the rank equation excludes Burau and LKB, and Schur does not collapse the certificate |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-08-15T13:08:36Z |
| Merged at | 2026-08-15T13:23:04Z |
| Merge commit SHA | `58c62e25a417c2f912956509605d386ad0a3c312` |
| Branch | `shadow/ribbon-object-alternatives` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/10765 |
| Changed files | 1 |
| Additions / deletions | +500 / -0 |

## Description

## Verdict

**The stop stands, and the current object is right — for a reason the brief did not have.**

Aaron's candidates are well chosen and each is genuinely dualizable. They fail *before* faithfulness, byte-lockability, or the Markov trace ever come up:

> **Reduced Burau and Lawrence–Krammer–Bigelow are representations of the braid *groups*. They are not *objects of a braided monoidal category*.** There is no `⊗` under which either becomes `V^⊗n`, and the proof is a two-line rank equation.

A dualizable generating object forces `rank(V^⊗n) = rⁿ`. Burau's rank is `n−1`; LKB's is `n(n−1)/2`. Neither is an `n`-th power (`n=3` ⇒ `r³ = 2` and `r³ = 3`). Dualizability is necessary for ribbon and nowhere near sufficient.

Doc only. Nothing built. Auto-merge **not** armed.

## Four corrections to the brief — flagged, as asked

**1. STALE PREMISE.** The brief says the general-`n` Lean certificate for `θ = ρ(Δₙ²)` is open and "verified computationally only for `m+n ≤ 7`". **CHECKED against `origin/main`: it is merged.** `src/Core.Lean4/Lean4/MenoBalancedTwist.lean` (404 lines, PR #10623, `343e17638`) certifies `dbl_cocycle` for **all** `m, n` from the hexagons alone via four Yang–Baxter rewrites; work-item `081KZZVC3DD087G0R0035SZN58` is under `workitems/done/2026/08/`. The certificate did not need to collapse — it already did, and the thing that collapsed it was neither Schur nor centrality but the observation that the obstruction is a 2-cocycle condition on the double braiding.

**2. Schur is INAPPLICABLE to `⟨V⟩`, not merely unhelpful.** `End_{⟨V⟩}(V^⊗n) = ρ(Bₙ)` is a **group** (a `Hom` is a braid word, constructor `Assembly`-scoped); you cannot add morphisms, so "irreducible" is undefinable. `End(I)` is trivial, so **the only scalar in `⟨V⟩` is 1** — "θ is a scalar" can only mean `θ = id`, which is the refuted misreading.

**3. Centrality is necessary and NOT sufficient — with a mechanical witness.** `Δ⁴` is *also* central (`Z(Bₙ) = ⟨Δ²⟩`, Chow 1948). If centrality sufficed, `θ = ρ(Δ⁴)` would work. **CHECKED: REJECTED at `m+n = 2`.** So no, the certificate does not collapse to a centrality/Schur argument.

**4. The byte-lock argument runs backwards — and this was the strongest practical case for the move.** `⟨V⟩` is *already* four-oracle byte-locked (`golden-vectors.json`, 14 vectors in `B₅` including `Δ₅`, replayed in F#/C#/TS/Rust). Golden-vector size for `ρ(Δₙ²)`:

| object | n=4 | n=6 | n=8 | n=10 | growth |
|---|---:|---:|---:|---:|---|
| **`⟨V⟩ = ℤ[Fₙ]`, Artin** | **34** | **76** | **134** | **208** | `O(n²)` small ints |
| rank-2 R-matrix, `2ⁿ×2ⁿ` | 256 | 4,096 | 65,536 | 1,048,576 | `O(4ⁿ)` Laurent polys |
| reduced Burau / LKB | 1 | 1 | 1 | 1 | 1 monomial |

Burau's and LKB's "1" is a **vacuity warning, not a win**: a golden vector pinning `tⁿ·I` cannot fail on a wrong braiding, a wrong `R`, or a wrong composition order. Infinite rank ≠ un-byte-lockable — the *representation* is infinite-rank, its *value on a braid* is a short free-group word.

## The Δₙ² question, answered per candidate — CHECKED

Exact Laurent/integer arithmetic, no floats, self-validated by braid relations, planted mutants died.

| object | `ρ(Δₙ²)` | mutants |
|---|---|---|
| reduced Burau `ℤ[t^{±1}]` | **`tⁿ · I`** (n=2..7) | relations hold `B₃…B₇`; 4 rejected |
| LKB `ℤ[q^{±1},t^{±1}]` | **`q^{2n} t² · I`** (n=2..6) | relations hold `B₃…B₆`; 3 rejected |
| **rank-2 R-matrix** (dualizable) | **NOT SCALAR**, n=2..5 | YBE holds; 1 rejected, 1 legitimately survived (`R⁻¹`, the mirror braiding) |
| `⟨V⟩` (current) | no scalars exist | `θ=id` rejected at `m+n=2` |

**Aaron's Schur prediction is exactly right for Burau and LKB — and it does not help**, because §2 says they are not objects of the ladder. And the moment you take an object that *is* — the rank-2 R-matrix — the scalar goes away: `V^⊗n` is reducible, so Schur applies on **simple summands only**. That is not a defect; it is what a ribbon twist is.

The scalar that *is* forced, as a closing check: assuming `θ_V = λ·id` (legitimate — `V` is simple), the ribbon condition `θ = 1` on the trivial summand of `V⊗V` (where `R²` acts by `A⁻⁶`) gives `λ² = A⁶`, i.e. `θ_V = ±A³` — the Kauffman-bracket framing factor `−A³`. **This answers Q2 as a side effect: the finite-rank object carries the framing datum automatically, because `θ_V ≠ id` *is* the framing.**

## Tensor half extended `m+n ≤ 7` → `m+n ≤ 14`

`Δ²_{m+n} = (Δ²_m ⊗ Δ²_n)·c_{m,n}·c_{n,m}`, decided by Artin's faithful action. **All 91 splits hold.** Seven mutants rejected: `θ=id`, `θ=Δ`, **`θ=Δ⁴` (the central one)**, single block swap, both swaps same direction, `θ_A` dropped, one swap inverted. Cost is polynomial — the max Artin image word is exactly `2n+1` for every `n ≤ 14`, because the full twist is **periodic, not pseudo-Anosov**, so `Δ²` escapes PR #10540 §3a's blowup entirely.

## Faithfulness: measured, not asserted — and Garside still wins

I am **not** contradicting the handoff's "Markov trace is strictly weaker". But PR #10540's "LKB buys a polynomial-size certificate — a real gap, wrong tool" was asserted; here are numbers, for `w = σ₁σ₃σ₂σ₂σ₂ ∈ B₄`:

| `k` | `\|wᵏ\|` | LKB bits | Artin bits | Garside bits (analytic bound) |
|---:|---:|---:|---:|---:|
| 6 | 30 | 102,765 | 61,056 | 150 |
| 10 | 50 | 437,187 | 11,803,032 | 250 |
| 14 | 70 | **1,163,138** | **2,289,653,424** | **350** |

LKB log-log slope **2.921** ⇒ `O(|w|³)`, polynomial. Artin ratio **13.93** ⇒ geometric. Crossover ≈ `|w| = 30`. Garside is linear and beats both by 3+ orders of magnitude *(ANALYTIC BOUND — I did not implement it)*. **The gap is real and now quantified; no recommendation changes.**

## MTC — Aaron has it right, with one refinement

Confirmed false-not-open, and the fix is a **functor** (semisimplification at a root of unity), not a better object. The refinement: semisimplification quotients by negligible morphisms, which presupposes an additive category — and `⟨V⟩`'s hom-sets are groups of basis bijections with nothing to add. **You cannot semisimplify `⟨V⟩`.** The functor applies to a linear category built from a finite-rank R-matrix object. Object first, then functor.

## Register

Everything stays **toy**. §8 names the fastest `metered` promotion (the concrete-model instance of `dbl_cocycle` as an F# test, ~40 lines) **with its falsifier stated first — the `Δ⁴` mutant must die** — and then explicitly *records rather than recommends* it, because the marginal value over #10623 is modest.

Not page-checked: every citation. Where an anchor's *content* is load-bearing it was re-derived computationally instead. Jackson–Kerler (LKB inside a `U_q(sl₂)` Verma weight space — which would mean the monoidal home of the faithful rep is itself infinite-rank) is marked CONJECTURE and nothing rests on it.

Work-items verified present on `origin/main` before citing: `081KZZVC3DD087G0R0035SZN58` (done), `081KZZVC6SE087G0R001SXE8BV` (open).

🤖 Generated with [Claude Code](https://claude.com/claude-code)

Agency-Signature-Version: 1
Agent: shadow
Agent-Runtime: Claude Code
Agent-Model: claude-opus-5
Credential-Identity: AceHack via gh
Credential-Mode: shared
Human-Review: not-implied-by-credential
Human-Review-Evidence: none
Action-Mode: supervised
Task: shadow-ribbon-object-alternatives-2026-08-15
Co-authored-by: Aaron Stainback <acehack00@gmail.com>


## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 0 |
| Resolved threads | 0 |
| Unresolved threads | 0 |
| Total review comments | 0 |
| Total fix commits (touching thread paths) | 0 |

## Review threads

(no review threads on this PR)

## Fix commits (touching thread paths)

(no commits in this PR touched files anchored by review threads)
