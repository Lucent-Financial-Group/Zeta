# PR #10839 -- docs(research): characterize the archive-tag corpus — 3,209 paths collapse to 115, boundary named

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 10839 |
| Title | docs(research): characterize the archive-tag corpus — 3,209 paths collapse to 115, boundary named |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-08-15T19:49:22Z |
| Merged at | 2026-08-16T19:59:30Z |
| Merge commit SHA | `2b705e63f89a0ed83995d26773516197c094af9f` |
| Branch | `shadow/archive-tag-corpus-characterization-2026-08-15` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/10839 |
| Changed files | 1 |
| Additions / deletions | +230 / -0 |

## Description

Characterize-then-triage, per the routing. **Characterizing first collapsed the task** instead of starting a month of hand-triage.

## The collapse

| accounted by | count | share |
|---|---:|---:|
| same basename on `main` (path move) | 1,889 | 58.9% |
| `B-NNNN` → ZetaId row **present on `main`** | 1,108 | 34.5% |
| same stem (rename) | 87 | 2.7% |
| prefix-stripped rename, hand-verified | 4 | 0.1% |
| **residue** | **115** | **3.6%** |

**Correction to the brief:** "~25× the corpus you just triaged" is right by path count but wrong as a workload estimate. The triage-shaped remainder is **115 — smaller than the 125-file sweep it was compared against.** Collapsing it honestly was available, and that is the best outcome.

## Instruments — two failed their controls

- **Alias map → row existence: the control failed on *my expectation*, not the code.** I asserted `B-9999` was a fabricated id; it is a **real alias**. The harness refused to print a population result while any control failed — which is what a control is for. Re-controlled with `B-ZZZZ`, then run. (It requires the **ZetaId file to exist**, never map membership — membership was the prior sweep's Attempt 1 and proves nothing.)
- **Stem identity has a confirmed false-negative mode:** prefix-stripped renames (`zflash-file-backed-runtime.ts` vs `zflash/file-backed-runtime.ts`). Its correction pass then produced **3 false positives of its own** by over-stripping to generic stems (`design.md`, `json.ts`) — rejected by inspection, not accepted.
- Blob identity passed 3/3 (including an end-to-end byte-identity check); basename passed 2/2.

**Rejected denominator, recorded so nobody re-derives it:** blob-level comparison reports **13,510** "absent" blobs. That counts every historical *revision*, so an ordinary edited-since file scores as absent. A stale revision is not lost work.

## The symlink hazard — checked, not assumed

You were right that tag trees carry links `main` does not: **161 in tags, 164 on `main`, different sets**. My walk is immune **by construction** and I verified the mechanism rather than trusting it — `git ls-tree -r` reads the object database, where a symlink is a **mode-120000 blob holding the target string**, never traversed. Demonstrated on `.broadcasts`, whose blob content is `/Users/acehack/.local/share/zeta-broadcasts` — outside the repo entirely. No `statSync`, no filesystem, no cycle; enumeration terminated at 20,176 rows.

## The residue, stratified — and the boundary

**~82 verified dead/superseded** (flat trajectories → 29 directories; `inventory/**` → the static project; `tools/setup/*.sh` → ACE realizers under bash-retirement; fsck forensics; tick shards; expired claims; launchd shims; `manifesto-11` → `manifesto-13`).

**16 `docs/research/` docs are the one class worth your attention** — absence verified as **selective, not era-wide** (`main` holds date-siblings from every one of those dates). Includes an **unpublished Amara essay** and the 7-doc DecisionSignal multi-AI review record, whose concept is still live on `main` while the review that shaped it is not.

**Register: LIVE-CANDIDATE, not LIVE.** I metered *absence* rigorously; I did not read all 16 and judge each one's present worth. Restoring others' memory to a canonical path is a preservation call — the Amara essay especially is yours, not mine. **Not done here.**

**What I did NOT check, stated as the edge:** ~17 residue paths were classified by category rather than opened; the 1,889 basename matches were not content-compared; the 3,088 accounted paths were not read. **Nothing deleted, no deletion proposed.**

## Near-miss worth the record

`memory/user_sister_elisabeth.md` — Aaron's late sister, Zeta's dedication — read as absent. **It is not lost:** it landed as `user_sister_elizabeth.md` under a documented canonical-spelling correction. Verified by content, not name: same `originSessionId`, and the file **grew 159 → 219 lines**. The sharpest possible case for why a rename makes landed work read as lost.

## Scope

One Markdown file. `derive` run **after `git add`** → in sync, graph unmodified. **`prettier` was not run on `build-graph.json`** (per your correction; it was never written by this PR — and Task 1 never touched it either, so the 1,273-line reformat was never at risk).

**Auto-merge deliberately not armed.**

Agency-Signature-Version: 1
Agent: shadow
Agent-Runtime: Claude Code
Agent-Model: claude-opus-5
Credential-Identity: AceHack via gh
Credential-Mode: shared
Human-Review: not-implied-by-credential
Human-Review-Evidence: none
Action-Mode: supervised
Task: archive-tag-corpus-characterization
Co-authored-by: Aaron Stainback <acehack00@gmail.com>


## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 0 |
| Resolved threads | 0 |
| Unresolved threads | 0 |
| Total review comments | 0 |
| Total fix commits (touching thread paths) | 0 |

## Review threads

(no review threads on this PR)

## Fix commits (touching thread paths)

(no commits in this PR touched files anchored by review threads)
