# PR #10833 -- docs(research): triage the 125 non-backlog recovered files — 7 live, and one dispatchable workflow is already broken

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 10833 |
| Title | docs(research): triage the 125 non-backlog recovered files — 7 live, and one dispatchable workflow is already broken |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-08-15T18:51:56Z |
| Merged at | 2026-08-16T19:59:08Z |
| Merge commit SHA | `a1ed8c422df7d28c69ea7cd9f44d76b820d0b4cc` |
| Branch | `shadow/recovered-wip-and-research-sweep-2026-08-15` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/10833 |
| Changed files | 1 |
| Additions / deletions | +342 / -0 |

## Description

The sweep the B-NNNN census explicitly named as its remaining gap: the **WIP code and research/misc residue** in `docs/recovered-orphan-branches-2026-05/`. Triage only — nothing merged, nothing minted, nothing deleted.

## Result

**125 files** (not the ~131 the brief inherited): **48 LANDED · 14 SUPERSEDED · 56 ABANDONED-CORRECTLY · 7 LIVE**. The 7 collapse to **3 items**.

## The sharpest finding

`.github/workflows/accelerator-move-next.yml` is on `main` and `workflow_dispatch`-able. Line 99 runs `bun src/Core.TypeScript/accelerator/move-next-harness.ts` — **that file does not exist on `main`**. `docs/accelerator/EVENT-STORE-SCHEMA.md` links twice to another absent path. And the branch the workflow checks out, `accelerator/pr-less-git-monster`, is **gone from origin** (control-tested: `refs/heads/main` returns a SHA, `refs/heads/accelerator/*` returns empty, 1469 heads total). The only surviving copies of the harness are in the quarantine. Tracking row `081KSNY2Z0008QG0R003X1QWYG` is P1 and open.

The other two LIVE items are one-file doc landings: a 300-line `EngagementPolicy` design whose five `code_anchors` all exist on `main` while the concept does not, and an **Accepted** Axis-3 ADR absent from all 56 entries of `docs/DECISIONS/`.

## Corrections to the brief (flagged, as asked)

1. **The 68/63 counts are wrong** and were never a measurement — they are the quarantine README's estimates, quoted onward as fact. Measured: 207 total, 82 backlog (matching the prior sweep exactly), **125 residue**, split **89 code / 30 md+txt / 6 extensionless**.
2. **The prior sweep is not on `main`** — it is commit `6e47ac90f` on unmerged branch `shadow/lost-bnnnn-recovered-branch-sweep-2026-08-15`.
3. **The branch set is a directory on `main`, not the archive tags.** Tags are intact (323, 314 off-main) and hold **3,209** paths absent from `main` — ~25x this sweep's corpus, dominated by known renames (all 1,155 `docs/backlog` tag-only paths are `B-NNNN`; `memory/persona/*` → `memory/*`). Reported as a **scoped, untriaged gap**, not a clean bill. No deletion proposed.
4. **Nothing here was ever "lost"** — all 125 are on `main` inside the quarantine, so LIVE means "not at a canonical path".

## Instruments that failed their controls

Both are recorded in the doc because their output looked clean:

- The 600k identifier index scored **0/3 on known positives** (`ZSetW` read ABSENT while `src/Core/ZSetW.fs` exists) — `rg -N` suppresses line numbers, not filenames, so every row was `path:token` and no exact match could hit. Fixed, re-controlled, then used.
- The F# symbol extractor returned local test bindings (`a`, `b`, `z`, `w`) rather than API, scoring a file 5/18 meaninglessly. Abandoned for `.fs` rather than trusted.

## Scope

One Markdown file. `docs/research` appears **0 times** in `src/Core.TypeScript/ace/build-graph.json` (checked, not assumed), so no `derive --write` is needed. No `B-NNNN` minted; B-0732/B-0733 confirmed live ZetaId rows on `main`. No edits to `src/Core/Bonsai.fs` or any sibling lane.

**Auto-merge deliberately not armed.**

Agency-Signature-Version: 1
Agent: shadow
Agent-Runtime: Claude Code
Agent-Model: claude-opus-5
Credential-Identity: AceHack via gh
Credential-Mode: shared
Human-Review: not-implied-by-credential
Human-Review-Evidence: none
Action-Mode: supervised
Task: recovered-wip-and-research-sweep
Co-authored-by: Aaron Stainback <acehack00@gmail.com>


## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 0 |
| Resolved threads | 0 |
| Unresolved threads | 0 |
| Total review comments | 0 |
| Total fix commits (touching thread paths) | 0 |

## Review threads

(no review threads on this PR)

## Fix commits (touching thread paths)

(no commits in this PR touched files anchored by review threads)
