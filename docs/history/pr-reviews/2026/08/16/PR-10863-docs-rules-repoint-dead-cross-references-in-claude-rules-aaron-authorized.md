# PR #10863 -- docs(rules): repoint dead cross-references in .claude/rules (Aaron-authorized)

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 10863 |
| Title | docs(rules): repoint dead cross-references in .claude/rules (Aaron-authorized) |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-08-16T02:39:08Z |
| Merged at | 2026-08-16T14:03:58Z |
| Merge commit SHA | `325a524feb093290abc631721e9294fcac8f29b1` |
| Branch | `rules-pointer-fix` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/10863 |
| Changed files | 11 |
| Additions / deletions | +17 / -16 |

## Description

## What

`.claude/rules/` is the cold-start surface loaded into **every agent on every wake**. It carried
cross-references that resolved to nothing — a citation to nothing, read by every agent, every
session. This repoints them.

**Pointers only.** No carved sentence, no rule semantics, no `CLAUDE.md`.

Authorized directly by Aaron ("fix the 13 dead pointers in .claude/rules/"), relayed to me.
`.claude/rules/` is a gated class under `no-directives.md`; this is the human authorizing it.

## Corrections to the brief

Flagged explicitly, as asked:

1. **The count is not 13 — I make it 14 dead references across 8 files** (plus 1 adjacent fix).
   I re-derived from scratch rather than inheriting PR #10861's list.
2. **`privacy-budget-...md:49` was cited as one of the 13 with "a unique elided completion."**
   It is elided, and the completion *is* unique — but it is not the same defect class as the
   archive-move pointers: a glob of the prefix lands on exactly one file. I completed it anyway
   (matching #10861's own pattern) and say so rather than quietly reclassifying it.
3. **The extractor hole the brief predicted is real and it caught one.** A code span containing a
   space hid `2026-05-17 ...clifford-cayley-dickson-hkt-dbsp...` in
   `only-the-irreducible-is-primitive-generate-the-rest.md:36`.
4. **I under-delivered against this PR's own claim, and a sibling agent caught it.** The first commit
   prefixed only the *first* reference on `no-directives.md:31`; the three bare siblings on that line
   still implied `.claude/rules/`, where none of them lives. I left them deliberately, reasoning that
   they inherit the first item's prefix — **that reasoning was wrong**, and this body had already
   counted all four under the archive class, so the diff contradicted the body. Fixed in the second
   commit. Surfaced by the auditor fix in **PR #10868**, which is exactly the argument for that
   instrument: a careful pass fixed the reference carrying a *visible* wrong prefix and missed its
   three neighbours.
5. **The brief's `Human-Review-Evidence` value is not a legal enum value.**
   `aaron-authorized-rules-pointer-fix-2026-08-15` fails `validate-agencysignature-pr-body.ts:121`,
   which allows `chat | pr-review | pr-comment | signed-policy | none`. Aaron authorized in
   conversation, so this carries `chat`, satisfying the cross-field rule with `Human-Review: explicit`.

## The three defect classes

### A. Archive move (#6676 `rules/` -> `rules.bak/`) — 9 refs, 6 files

The implied location `.claude/rules/` is **wrong**; the rules live in `.claude/rules.bak/`. All 8
distinct targets verified present before repointing.

| File:line | Reference |
|---|---|
| `anchor-to-human-prior-art.md:37` | `honor-those-that-came-before.md` |
| `dv2-data-split-discipline-activated.md:24` | `default-to-both.md` |
| `mirror-beacon-register-discipline.md:28` | `grep-substrate-anchors-before-razor-as-metaphysical.md` |
| `rules-are-small-...:52` | `wake-time-substrate.md` |
| `shared-checkout-is-view-only.md:29` | `dont-ask-permission.md` |
| `no-directives.md:31` | `dont-ask-permission.md` + 3 bare siblings on the same line (see *Correction 4*) |

For the five markdown links I corrected **both halves**: the link target (`../rules.bak/...`, so the
link works) *and* the backticked display text (`.claude/rules.bak/...`, so the text stops implying a
location that is wrong, and so the repo's own auditor can see it).

### B. Target absent from the repo — 3 refs, 2 files

`cleanroom-two-team-separation.md:39`, `:42` and `itron-hub-...:159` point at `memory/<file>.md`
entries that are **not in this repo** (checked against all 1,948 tracked `memory/` files). They exist
only in the operator's machine-local auto-memory.

Per the bar, these are **named as absent rather than guessed** — the `memory/` prefix (a false repo
path) is dropped and `(**not in-repo**)` added, following the pattern PR #10838/#10861 used for
`lens/`. Pointing them at a plausible in-repo substitute would have been the same defect with extra
steps.

### C. Elided / mis-prefixed — 3 refs + 1 adjacent

- `privacy-budget-...:49` and `:128` — unicode-ellipsis paths completed to their verified unique match.
- `only-the-irreducible-...:36` — converted to `2026-05-17-*-clifford-cayley-dickson-hkt-dbsp-*`,
  the glob convention already used by its two line-mates, rather than inlining a 130-char filename
  onto the cold-start surface.
- `every-bug-has-economic-value.md:18` — `BUGS.md` corrected to `docs/BUGS.md` (adjacent find, same class).

## Before / after

Every dead reference resolving, checked two ways.

**Repo's own falsifier**, `src/Core.TypeScript/hygiene/audit-rule-cross-refs.ts`:

```
before: Resolved  95 / Stale-pointer candidates 23
after:  Resolved 109 / Stale-pointer candidates 11
```

**Independent ellipsis-aware resolver** over every path-shaped reference (markdown-link targets +
code spans, splitting spans on whitespace so space-containing spans are visible):

```
before: 113 path refs -> DEAD 9
after:  119 path refs -> DEAD 0
```

**Archived class** — the check that actually matters: a reference whose text implies `.claude/rules/`
while the file lives in `.claude/rules.bak/`.

```
before: 12
after:   0
```

Each replacement target `existsSync`-verified; the one glob confirmed to match exactly 1 file. The
archived-class row is verified by **direct resolution against `.claude/rules.bak/`** — not by the
auditor on `main`, which cannot see the archive. That auditor's numbers are reported here as movement
only, never relied on as the check.

## The 11 the auditor still reports — deliberately not "fixed"

These are **not** the same defect and padding them would grow the cold-start surface for a green
number rather than for agent utility. My discriminator:

> **Wrong implied location = dead. Unqualified but unique basename = live shorthand.**

The remaining 11 are all the second kind — `new-workitem.ts`, `LagrangeCondorcet.fs`,
`golden-vectors-*.json`, the `docs/research/`-prefixed sibling on `only-the-irreducible:36`, and
`.test.ts` (a filename *suffix* in prose, not a pointer at all). Each lands on exactly one file when
followed.

**This surfaced a real tooling gap — now built by a sibling agent as PR #10868** (open, unmerged, zero
file overlap with this PR). The gap: the auditor's
sibling-rule resolution at `audit-rule-cross-refs.ts:282` only checks `.claude/rules/<basename>` and
has **no `.claude/rules.bak/` fallback** — the mirror image of the defect this PR fixes, left behind
by the same #6676 move. It also pulls references from backticked spans only, so it reads a markdown
link's *display text*, never its target. Running #10868's classifier against this branch is what caught
my own miss in *Correction 4*; once it lands, the counts above will change.

## Verification notes

- `bunx markdownlint-cli2` **ignores `**/.claude/**`** (config `ignores`), so its exit 0 on these
  files would be meaningless. Linted copies through a non-exempt path with a **positive control**
  that deliberately trips MD022/MD009: control fired (exit 1), the 11 real files then passed (exit 0).
- `build-graph` derives targets from `.csproj` / `Cargo.toml` / `lakefile.toml` and contains zero
  `.claude/rules` references; no manifest was touched, so the derive gate is unaffected.
- Growth **+375 bytes (+0.48%)** on a 77KB surface — spent entirely on making pointers resolve.
- Two commits, **no force-push**: the follow-up landed as a new commit because force-push is a gated
  class under `no-directives.md` and no authorization for it was given.
- No conflict with open PR #10861 (17 pointers elsewhere, no `.claude/rules/**`) or PR #10868 (auditor
  + fixtures) — zero file overlap with either, both verified.

Not auto-merged — this is the cold-start surface and Aaron reviews it.

Agency-Signature-Version: 1
Agent: shadow
Agent-Runtime: Claude Code
Agent-Model: claude-opus-5
Credential-Identity: AceHack via gh
Credential-Mode: shared
Human-Review: explicit
Human-Review-Evidence: chat
Action-Mode: supervised
Task: fix-dead-pointers-claude-rules
Co-authored-by: Aaron Stainback <acehack00@gmail.com>


## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 0 |
| Resolved threads | 0 |
| Unresolved threads | 0 |
| Total review comments | 0 |
| Total fix commits (touching thread paths) | 0 |

## Review threads

(no review threads on this PR)

## Fix commits (touching thread paths)

(no commits in this PR touched files anchored by review threads)
