# PR #15424 -- research(lattices): rank 16 is where the E8 routes disagree — the rank-8 agreement was forced

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 15424 |
| Title | research(lattices): rank 16 is where the E8 routes disagree — the rank-8 agreement was forced |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-08-25T19:32:20Z |
| Merged at | 2026-08-25T19:52:33Z |
| Merge commit SHA | `3d7b741ef25c80e0fad08c42b41e76a147c8dee5` |
| Branch | `soraya/rank-16-discriminating-test` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/15424 |
| Changed files | 5 |
| Additions / deletions | +1526 / -0 |

## Description

## The test PR #15415 named and did not run

Kira's adversarial review found that **E₈ is a uniqueness sink** — exactly one even unimodular
positive-definite rank-8 lattice exists (Mordell 1938; Witt) — so the four in-tree routes that
"agree" on E₈ agree **by a theorem about the target**, not by any shared mechanism. It named the
one discriminating test and marked it unrun: **rank 16, where there are two.**

**Result: the routes disagree. The rank-8 agreement carries no evidential weight.** This is the
outcome the brief pre-registered as a success, and it is not hedged toward the synthesis.

## What was computed

`src/Core.TypeScript/algebra/rank-sixteen-lattice-routes.ts` — 18 tests, 83 assertions, ~5s.

**1. The classification, proven rather than cited.** Exhaustive classification at length 8 finds
**one** Type II code with `|Aut| = 1344` (`AGL(3,2)`), so `8!/1344 = 30` labelled codes — which
*validates* the mass formula then applied at length 16. There, computed automorphism orders
`3 612 672` and `5 160 960` give class sizes `5 791 500 + 4 054 050 = 9 845 550 = N(16)`. **The
mass balances exactly**, so there are exactly two classes; a missing third would show as missing
mass. (The brief's parenthetical `[16,5] / d=8` was wrong — self-dual forces `k = n/2`, so both
codes are `[16,8,4]`. `[16,5,8]` is RM(1,4), self-*orthogonal* only.)

**2. Every count agrees and identifies nothing.** Same weight enumerator (Gleason forces
`1 + 28y⁴ + 198y⁸ + 28y¹² + y¹⁶`), same 480 minimal vectors, same rank, same determinant — and
the **same theta series**. That is Milnor's isospectral pair, and it is forced. **So the in-tree
`ConstructionATheta` route has no discriminating power at rank 16, and therefore never had any at
rank 8** — it measured even-unimodularity and let Mordell do the identifying, exactly as its own
docstring says.

**3. The invariant that does separate them** is root-graph connectivity: two components of
240/rank 8 versus one of 480/rank 16. Competitors named and excluded (`A₁₆` has 272 roots;
`E₆/E₇/E₈` have rank ≤ 8), leaving `D₁₆` uniquely.

**4. The Clifford/versor route is diagram-driven.** `CliffordE8Roots.fs` hardcodes the E₈ Cartan
matrix and searches a root set Construction A already built. Re-run at rank 16, changing only the
supplied diagram: E₈+E₈ in → E₈+E₈ out; D₁₆ in → D₁₆ out. **It confirms; it cannot select.**

**5. The adversary's `?`: UNPROVEN → PROVEN NOT TO EXIST.** The Cayley–Dickson norm form
`N(a,b) = N(a) + N(b)` *is* an orthogonal direct sum, so the doubling's only lattice-level content
is `D(L) ⊇ L ⊥ L`. Under that: `det(A₂ ⊥ A₂) = 9` versus `det(D₄) = 4` gives index `3/2`, so
`A₂ ⊥ A₂` is **not a sublattice of `D₄` at all** — the chain breaks at the **first** rung, not at
16. Corollaries: any such operation gives `E₈⊕E₈` and can never reach `D₁₆⁺` (independently
confirmed by an **exhaustive 34 299 361-node search finding no E₈ subsystem in `D₁₆⁺`**), and the
doubling law is exactly **×2** on kissing number (4→8, 6→12, 24→48) while the claimed chain goes
6→24→240. The excess is glue; the algebra does not supply glue.

This also **retires the adversary's own weaker argument** — unequal ratios 4 and 10 refute only a
*multiplicative* rule, not an operation.

**6. Rank 8 is a fourfold collision.** Unique even unimodular, densest packing, CD unit lattice,
and Lie root lattice all name E₈ there. At rank 16 they name four *different* objects
(`E₈⊕E₈`/`D₁₆⁺`; Barnes–Wall `Λ₁₆` at **16× the centre density**, computed here as kissing 4320 /
δ = 1/16; `E₈ ⊥ E₈`; the non-unimodular `D₁₆` root lattice).

## Aaron's separate question — Coldea et al. 2010

**Yes, it is a genuine non-human-resonance appearance of E₈ — of the *root system*, not the
lattice.** Zamolodchikov's eight masses are reproduced here to `5.3e-15` from the E₈ Dynkin
**adjacency matrix alone**, with `m₂/m₁ = φ`. Nothing in the physics uses unimodularity,
Construction A, the `[8,4]` code, or sphere packing.

The competitor is excluded by structure, not by the number: **`D₁₆` has the same Coxeter number
30**, hence the same Perron–Frobenius eigenvalue — and a different eigenvector, with no golden
ratio in it. The eigenvalue is the coincidence; the eigenvector is the identification.

Recorded honestly: a bipartite-graph power-iteration bug made four of eight masses wrong by a
common factor 1.1106, and **the published closed forms are what caught it**. An anchor must be
checked, not cited.

## Discipline

- **Not-found verified before building**: re-ran the adversary's search with a positive control.
  All ten `d16` hits in `src/`+`tests/` are lockfile/signature hex. The absence was real.
- **Every claim has a control that could have failed** — including two negative searches that
  **hit their cap and are reported as INCONCLUSIVE**, with the structural proof used instead.
- **Mutation check**: replacing `CODE_D16_PLUS` with `CODE_E8_PLUS_E8` turns **6 of 18 tests red**.
- **Stated as unverified**: densest-lattice-packing is proven only in dimensions 1–8 and 24, so
  `Λ₁₆` is densest *known*; the Coldea measurement is cited, not computed; `|Aut(d₁₆⁺)|` was not
  cross-checked against a CAS (the mass balance is its check).
- **markdownlint does NOT cover the research doc** — `docs/research/2026-*-*.md` is in the
  `.markdownlint-cli2.jsonc` ignores (line 151). A green run there is a check that did not run.
- `tsc --noEmit`: **0 errors**. `lint:typescript`: pass. `prettier --check`: clean.

## AgencySignature

```
Agency-Signature-Version: 1
Agent: Soraya
Agent-Runtime: claude-code
Agent-Model: claude-opus-5
Credential-Identity: AceHack
Credential-Mode: shared
Human-Review: not-implied-by-credential
Human-Review-Evidence: none
Action-Mode: autonomous-fail-closed
Task: 081M0X50HY1087G0R003HTDJ1N
```

🤖 Generated with [Claude Code](https://claude.com/claude-code)


## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 0 |
| Resolved threads | 0 |
| Unresolved threads | 0 |
| Total review comments | 0 |
| Total fix commits (touching thread paths) | 0 |

## Review threads

(no review threads on this PR)

## Fix commits (touching thread paths)

(no commits in this PR touched files anchored by review threads)
