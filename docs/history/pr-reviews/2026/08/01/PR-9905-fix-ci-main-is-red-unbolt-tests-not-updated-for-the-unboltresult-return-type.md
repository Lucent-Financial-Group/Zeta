# PR #9905 -- fix(ci): main is red — unbolt tests not updated for the UnboltResult return type

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 9905 |
| Title | fix(ci): main is red — unbolt tests not updated for the UnboltResult return type |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-08-01T17:44:56Z |
| Merged at | 2026-08-01T18:22:30Z |
| Merge commit SHA | `825069bcc8a8024909a54b56e90173e8ef0a4616` |
| Branch | `shadow/fix-main-red-unbolt-result-types` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/9905 |
| Changed files | 1 |
| Additions / deletions | +6 / -3 |

## Description

`lint (TS)` fails on main — a **real** failure (187s, `conclusion=failure`, not a timeout).

`3dfa94077` (*"calibration-ledger"*) changed `unboltTaskHierarchy` to return `UnboltResult { state, calibrationLedger }` instead of `FlatSocietyBase`, but three call sites in the test file still treat the result as the society state.

## Why it reached main looking green

**`bun test` does not typecheck.** All 11 tests passed while `tsc` failed — the suite exercised the runtime shape and never saw the type error. Only `lint (TS)` runs `tsc`.

Worth generalizing: **a green `bun test` is not evidence of a green `tsc`.**

## The implementation is correct

The ledger is *passed through untouched* by unbolt — exactly the requirement from the calibration-ledger spec (#9901 §3.1). Unbolt does a wholesale restore from `base.peers`, so hat time has to live somewhere unbolt doesn't reset or the accumulated track record is erased every cycle. Only the tests needed updating.

## Verified beyond just green

- `tsc --noEmit`: clean
- 11/11 tests pass
- **mutation runner (#9900) on the same file: `gte-to-gt` KILLED, `and-to-or` KILLED** — confirming the edit didn't weaken the suite into passing vacuously, which is the exact failure mode this repo spent today finding.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 0 |
| Resolved threads | 0 |
| Unresolved threads | 0 |
| Total review comments | 0 |
| Total fix commits (touching thread paths) | 0 |

## Review threads

(no review threads on this PR)

## Fix commits (touching thread paths)

(no commits in this PR touched files anchored by review threads)
