# PR #14395 -- feat(twitch-ai): forced perception ladder — objects, OCR grid, hunt/flee latch, priors in source, win conditions in the cart

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 14395 |
| Title | feat(twitch-ai): forced perception ladder — objects, OCR grid, hunt/flee latch, priors in source, win conditions in the cart |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-08-23T19:17:59Z |
| Merged at | 2026-08-23T19:42:28Z |
| Merge commit SHA | `07b92beec8420fb413aa3128e31721840080e822` |
| Branch | `claude/otto-chip8-perception-ladder` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/14395 |
| Changed files | 22 |
| Additions / deletions | +3830 / -163 |

## Description

Otto's pass on Aaron's arena asks (2026-08-23), following the shadow→Lior handoff. Every item verified by tests (90 green) or headless chromium against the built page.

**Why buttons were random — three structural causes, all fixed:** (1) `ArcExplorer` overrode the BNN with `Math.random()` uniform for the first 30 *wall-clock* seconds of every stream — now 300 ticks, seeded, and skipped entirely when priors load; (2) the "target" centroid averaged the *walls* (they share color 1 with the adversary: 32 furniture px vs a 4–12 px sprite), pinning mode to flee and steering at scenery — perception now separates objects and scenery is structurally excluded from adversary choice (regression-tested); (3) ambient entropy at every level *including the emulator's RND opcode* — every draw now flows from `COMMON_SEED` (`chip8/seeded-rng.ts`, splitmix32).

**The forced layers** (Aaron: "layers on our BNN, forced if not auto-forming") — each engineered, exposed on the predictor, rendered on the page, individually tested: connected-component **objects** (bboxes/centroids/areas) → **tracking** (stable ids, velocities, static/moving, coast-through-flicker) → **relations** (offsets, closing speeds) → **OCR** of the fontset into a row/col **grid** with number merging → **roles** (self via key↔motion correlation — the empowerment probe; adversary; scenery) → **hunt/flee latch** with hysteresis → **geometry-aware steering** with obstacle lookahead, smoothed by the unchanged Student-t EP + WSet consensus.

**Priors in source:** `train-priors.ts` plays a cart headlessly — deterministic end to end, `--verify` proves byte-identical reruns — and emits committed `priors/<cart>.priors.ts`; the worker restores by ROM fingerprint at boot and on cart switch. Known cart → warm start ("not starting from zero"); unknown cart → fresh prior while the structural layers transfer: game switching stays in the soft regime.

**Win conditions in the cart:** mutual-sim now draws fontset scoreboards (which the OCR layer reads back), detects tags in-ROM, respawns via seeded RND, and floods the board on first-to-5 (win = plane-2, lose = plane-1). The worker's out-of-cart `Math.random` tag hack is deleted.

**Also:** persistence-of-vision compositing (end-of-tick snapshots phase-lock onto XOR-erase windows — found by a curriculum cart failing honestly); the guaranteed-failing `localhost:11434` fetch on orbit shifts gated behind explicit opt-in; `chosenKey` + arena readout now cross the worker boundary; NaN guard on the normalizer (the dashboard-freeze class); bbox overlay + live mode/OCR headers.

**Curriculum:** one cart per layer (`single-mover`, `mover-and-wall`, `glyph-board`, `mode-flip`), graded through the running emulator in CI.

**Headless verdict on the built page:** `FLEE · self#4 · adv#10` · `OCR 0:0 (first to 5)` · 5 role-correct boxes · glow left=1.00 fleeing a hunter on the right · ~20fps · 0 LLM request failures.

Handoff continuation (incl. the one flagged divergence — `--base` went absolute after the prior handoff argued relative; not churned here): `docs/handoffs/2026-08-23-otto-chip8-arena-perception-ladder-forced-layers-priors-and-win-conditions.md`

Agency-Signature-Version: 1
Agent: otto (interactive Claude session)
Agent-Runtime: Claude Code (remote container)
Agent-Model: Claude
Credential-Identity: GitHub MCP app credential (repo-scoped)
Credential-Mode: unknown
Human-Review: none
Human-Review-Evidence: none
Action-Mode: human-directed
Task: none

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_01UNRXBqHjAKNK6QzuLcem49

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 0 |
| Resolved threads | 0 |
| Unresolved threads | 0 |
| Total review comments | 0 |
| Total fix commits (touching thread paths) | 0 |

## Review threads

(no review threads on this PR)

## Fix commits (touching thread paths)

(no commits in this PR touched files anchored by review threads)
