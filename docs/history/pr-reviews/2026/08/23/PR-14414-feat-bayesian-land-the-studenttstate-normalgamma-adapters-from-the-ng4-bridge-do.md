# PR #14414 -- feat(bayesian): land the StudentTState ⟷ NormalGamma adapters from the NG4 bridge doc §4.4

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 14414 |
| Title | feat(bayesian): land the StudentTState ⟷ NormalGamma adapters from the NG4 bridge doc §4.4 |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-08-23T19:45:11Z |
| Merged at | 2026-08-23T19:57:56Z |
| Merge commit SHA | `a8b79f44fb743c043f819035d019e433f79b984f` |
| Branch | `claude/otto-ng4-bridge-adapters` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/14414 |
| Changed files | 2 |
| Additions / deletions | +201 / -0 |

## Description

Follow-through on the Student-t/NG4 decision doc (#14390): its §4.4 prescribed the `StudentTState ⟷ NormalGamma` adapters with their losses measured — but as doc-only code. This lands them (`student-t-ng-bridge.ts`) with nine falsifiers that re-derive the doc's §4.3–4.4 fixture through the live `updateStudentT`, so a filter change that silently invalidates the doc turns the suite red.

What the tests pin: the two ν's are different objects (`state.nu` invariant at 4 across four observations while the NG4 marginal counts 2α = 6 — the 4-arg↔4-param arity match stays *coincidence*, per numerology-vs-number-theory); `posterior.mu` round-trips **exactly** (the only field the key predictor consumes); `sigma2` to one ulp; the EP site message resets to the honest uniform site rather than being faked; `nu`/`obsVariance`/`obsCount` travel out of band by construction; fusion in natural coordinates is order-independent to `ngKl < 1e-12` against the doc's measured t-projection failure (ν=7 vs ν=6 by association order).

**One finding sharper than the doc, caught by a failing draft assertion:** the marginal *scale* is convention-**invariant** by construction (`β = σ²αλ ⇒ scale² = σ²` for any λ₀) — the λ₀/α₀ convention moves the **joint**, and the marginal ν moves only with α₀. Stated as a test.

Next rung once this lands: the key predictor's `SocietySnapshot` (from #14395) stores `(mu, sigma2, nu, obsCount)` per key — the exact (μ,σ,ν)-storage degrade the doc names. Carrier swap to NG4 natural params via these adapters, API and call sites unchanged, priors regenerated by the deterministic trainer.

New files only; 9/9 tests green; root typecheck + lint(TS) green.

Agency-Signature-Version: 1
Agent: otto (interactive Claude session)
Agent-Runtime: Claude Code (remote container)
Agent-Model: Claude
Credential-Identity: GitHub MCP app credential (repo-scoped)
Credential-Mode: unknown
Human-Review: none
Human-Review-Evidence: none
Action-Mode: human-directed
Task: none

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_01UNRXBqHjAKNK6QzuLcem49

---
_Generated by [Claude Code](https://claude.ai/code/session_01UNRXBqHjAKNK6QzuLcem49)_

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 0 |
| Resolved threads | 0 |
| Unresolved threads | 0 |
| Total review comments | 0 |
| Total fix commits (touching thread paths) | 0 |

## Review threads

(no review threads on this PR)

## Fix commits (touching thread paths)

(no commits in this PR touched files anchored by review threads)
