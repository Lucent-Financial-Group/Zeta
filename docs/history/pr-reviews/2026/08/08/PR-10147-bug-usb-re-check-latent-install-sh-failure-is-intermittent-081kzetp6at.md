# PR #10147 -- bug(usb): re-check — latent install.sh failure is INTERMITTENT (081KZETP6AT)

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 10147 |
| Title | bug(usb): re-check — latent install.sh failure is INTERMITTENT (081KZETP6AT) |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-08-08T18:27:51Z |
| Merged at | 2026-08-08T18:28:05Z |
| Merge commit SHA | `4666c8e9f62c7a7d631fe01b2a6cdef9caa6ba75` |
| Branch | `shadow/iso-latent-recheck-intermittent` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/10147 |
| Changed files | 1 |
| Additions / deletions | +38 / -1 |

## Description

Re-check of the latent x86_64 first-boot `install.sh` bug (per Aaron's request).

**Finding: it's INTERMITTENT, not resolved.** Fresh `workflow_dispatch` build-iso on `main` (run 31270499976) → `build-iso` x86_64 **FAILED**, `install.sh` rc=1 in first-boot — vs the prior run (31212929243) where it succeeded.

- **Different mode:** rc=1 with **`mise ERROR`=0 / no pipx skips** (original was mise/pipx). Exact cause still uncaptured (serial tail truncated).
- **Errexit fix (#10135) confirmed non-fatal** — install.sh failed without aborting.
- **Job failed via the `workflow_dispatch`-only wifi-ESP acceptance:** failed install.sh → `ZETA CLUSTER NODE INSTALL COMPLETE` never emitted → phase-1 10-min timeout → contract fail. Normal push/PR CI doesn't run this, so `main` stays green.
- **Real-metal impact:** x86_64 node may come up **partial** (`PARTIAL-PROVISION`), recoverable via `cd ~/Zeta && ZETA_HOST_TIER=full tools/setup/install.sh`.

Stays **P2, OPEN**. Next (drive-the-fix): `MISE_VERBOSE=1` + phase-1 serial capture to pin the rc=1 cause, then fix. Docs-only.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 0 |
| Resolved threads | 0 |
| Unresolved threads | 0 |
| Total review comments | 0 |
| Total fix commits (touching thread paths) | 0 |

## Review threads

(no review threads on this PR)

## Fix commits (touching thread paths)

(no commits in this PR touched files anchored by review threads)
