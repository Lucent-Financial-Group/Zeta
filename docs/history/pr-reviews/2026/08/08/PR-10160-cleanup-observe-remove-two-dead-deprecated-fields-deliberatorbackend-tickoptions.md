# PR #10160 -- cleanup(observe): remove two dead deprecated fields (deliberatorBackend, TickOptions.backend)

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 10160 |
| Title | cleanup(observe): remove two dead deprecated fields (deliberatorBackend, TickOptions.backend) |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-08-08T20:11:54Z |
| Merged at | 2026-08-08T20:12:02Z |
| Merge commit SHA | `a62980187d12578022fd40aaeb63e63a9614dcb3` |
| Branch | `shadow/cleanup-dead-deprecated-fields` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/10160 |
| Changed files | 2 |
| Additions / deletions | +2 / -9 |

## Description

Aaron-requested deprecated-API cleanup after the Alexa handoff. Of the three `@deprecated` TS members on main, **two are dead** (Kiro's earlier scan counted module imports of *other* symbols, not usage of the deprecated members themselves):

- **`ChooserConfig.deliberatorBackend`** — no external setter; its only consumer collapses to `config.deliberator` when absent (behavior-preserving). Removed the field, simplified the consumer, dropped the now-orphaned `ModelBackend` + `localLlmParticipant` imports.
- **`TickOptions.backend`** — `opts.backend` is never read and no `simulateTick` caller passes it. Removed the field + orphaned `ModelBackend` import.

**Deliberately NOT touched:** `DEFAULT_ESP_OFFSET_BYTES` (prepare-boot-image.ts) — it has **live callers** (qemu-full-install-test, run.ts ×2), is deliberately "kept for tests", and migrating it to auto-detect is a behavioral change in the QEMU harness currently under diagnosis (`081KZETP6AT`). Left for a coordinated migration.

**Verified:** observe tests 16/16 pass (behavior preserved); `tsc` clean on both files.

**Flagged (not mine):** 2 pre-existing `TS6133` unused-decl errors already red on `main` — `dla-meter.probe.test.ts:22` (`CommitPairProbeResult`) and `traveler-rank-ledger.persistence.test.ts:13` (`freshBelief`). Present with my changes stashed, so pre-existing drift — likely someone's in-flight WIP; surfaced separately rather than yanked.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 0 |
| Resolved threads | 0 |
| Unresolved threads | 0 |
| Total review comments | 0 |
| Total fix commits (touching thread paths) | 0 |

## Review threads

(no review threads on this PR)

## Fix commits (touching thread paths)

(no commits in this PR touched files anchored by review threads)
