# PR #10161 -- test(chsh): Z3 gate-agreement lemma + companion gate (BP-16 second tool)

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 10161 |
| Title | test(chsh): Z3 gate-agreement lemma + companion gate (BP-16 second tool) |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-08-08T20:13:58Z |
| Merged at | 2026-08-08T20:14:06Z |
| Merge commit SHA | `c12ef27c9903934c994d79ade802ca64c77364fa` |
| Branch | `shadow/z3-chsh-band-gate-lemma` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/10161 |
| Changed files | 2 |
| Additions / deletions | +138 / -0 |

## Description

Completes the second CHSH-types verification follow-up (Soraya's spec obligation 1): the **BP-16 second tool** for the `ChshBand` gate-agreement property. FsCheck was the first witness (`ChshBand.Property.Tests.fs` property 1); this is **Z3 over exact reals**.

- **`tools/Z3Verify/chsh-band-gate-agreement-lemma.smt2`** (authored + verified by Soraya) — models `classifyBand`'s soundness-biased elif ladder exactly, asserts the negation of `band ≥ Quantum ⇔ |s| > 2 + eps`, `QF_NRA`, expected `unsat`. z3 4.16.0 → `unsat`. Teeth-checked (2 mutants flip to `sat`: RHS `>`→`>=`, and dropping `eps ≥ 0`). By abstracting `eps` as any non-negative real it certifies **both** the i.i.d. `chshMargin` and the shipped `chshMarginAutocorr` gate — a cross-check dividend the FsCheck witness alone misses.
- **`src/Core.TypeScript/algebra/chsh-band-gate-agreement.test.ts`** (companion gate) — Soraya's finding: the `.smt2` alone runs **nowhere** (each `tools/Z3Verify/*.smt2` is gated by a companion bun test that shells `z3` and asserts `unsat`; pattern from `cost-envelope.test.ts`). Without it, `audit-formal-artifacts` registers existence but runs nothing → **false-green**. This shells z3 (skips if absent) + a ground boundary check.

**Verified:** companion 3/3 pass locally. **BP-16 satisfied** for the property (FsCheck + Z3, two independent tools).

Closes the CHSH follow-ups (the Semgrep neutrality rule landed in #10159).

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 0 |
| Resolved threads | 0 |
| Unresolved threads | 0 |
| Total review comments | 0 |
| Total fix commits (touching thread paths) | 0 |

## Review threads

(no review threads on this PR)

## Fix commits (touching thread paths)

(no commits in this PR touched files anchored by review threads)
