# PR #14550 -- research: adjudicating PR #14218 — the four refutations hold, and the identification fails in BOTH directions

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 14550 |
| Title | research: adjudicating PR #14218 — the four refutations hold, and the identification fails in BOTH directions |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-08-24T00:04:04Z |
| Merged at | 2026-08-24T00:07:32Z |
| Merge commit SHA | `c77d5e711288f5608c529573efa3d718f254d5dd` |
| Branch | `research/lumen-adjudicate-widening-retraction-pr14218` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/14550 |
| Changed files | 2 |
| Additions / deletions | +478 / -0 |

## Description

Adjudication of PR #14218, requested after an adversarial pass refuted its central claim four ways. Aaron's instruction was to **re-verify each refutation myself against `origin/main` rather than take it on trust**, and to say so if any was wrong. None was. Advisory only — nothing under `src/` changed, no push to #14218's branch, no close.

## The four refutations: all four HOLD, re-derived independently at `0f082c8b5`

| # | claim | verdict | how checked |
|---|---|---|---|
| R1 | the chain inverts the theorem it cites | **HOLDS** | `h : Gaussian → WeightedSet<NatCoord, ℝ>` is over `{ν, τ}` and `h(One) = empty` — so a key leaving support denotes the **flat message**, maximum uncertainty |
| R2 | `SoftValue` never calls the group operations | **HOLDS** | counted: `5 ofSeq, 2 weight, 2 toSeq, 2 scale, 1 singleton, 1 isEmpty`. `add`/`negate`/`subtract`: **zero** |
| R3 | zero is reachable by float underflow | **HOLDS**, and worse | deletion at **step 324**, last weight `1e-323`, no zero supplied. `MAX_MULTIPLICITY = 1024 > 324`, so it is reachable **inside `foldRetained`** |
| R4 | the manifesto has no never-collapse text | **HOLDS** | one `collapse` in `MANIFESTO.md` (line 116, data models). Principle is Addison Cooper's, `CONCEPT-REGISTRY.md:42`, *"never **prematurely** collapsed"* |

## Two results the original pass did not run

"Widening is a special case of retraction" is a claim about **two operators**, so it has two directions. Both fail (`…two-directions-verify.py`, exit 0):

- **D1 — retraction ⇏ widening.** With `e₁=(0.9,0.1)`, `e₂=(0.1,0.9)` cancelling exactly, retracting `e₂` takes entropy **0.693 → 0.325**. Retraction *sharpened*. Its effect on uncertainty is sign-indefinite.
- **D2 — widen ⇏ retraction.** The retraction-reachable set is the **finite** sublattice `{π·∏L^{m'} : 0 ≤ m' ≤ m}` (measured, `|R| = 12`); `widen λ` lands off it at every `λ` tested. Generic, not numerical — a finite set cannot contain a curve continuous in `λ`.

**The shipped tests already said so.** `falsifier 4b` pins that `widen` permanently caps confidence at `1−λ+λ/n` while `foldRetained` converges above `0.99`. Different fixed points ⇒ different operators. The PR's own falsifier falsifies the PR's own headline.

## Separating the intuition from the identification

Aaron's two sentences are true in **different lanes, and neither lane makes both true**:

- *"widening is a special version of the retraction −1"* is **exactly right** in the exponential-family natural-parameter lane — `WeightedSet.subtract` **is** the EP cavity `Gaussian.( / )` (Minka 2001, improper `τ ≤ 0` tolerated). But there, sentence 2 is **inverted**: full retraction is the flat message.
- *"a full key retraction would be the uncertainty collapse"* is **inverted in the evidence lane too — by the PR's own docstring**: `horizon <= 0` *"retains NOTHING … leaving the prior untouched."* Verified against a deliberately **non-uniform** prior `(0.4, 0.3, 0.2, 0.1)` so "returns the prior" cannot be misread as "flattens to uniform". **Full retraction is the maximum re-opening available, not a collapse.**

**What survives, `metered`:** over the free commutative monoid on evidence keys, `foldRetained` **is** evidence retraction — lowering multiplicity is the `−1`, and because the posterior depends on the retained multiset alone, reorder-commutativity is preserved. `falsifier 2` + the arrival-order mutant arm `2b` is a real, non-vacuous falsifier for exactly that, green on `main`.

## Recommendation: CLOSE #14218 as **superseded**, not as refuted

Its 434 lines are **already on `main`** via #14266 (`5c9c60e3a`) — `widen` (325), `foldRetained` (442), `RetentionSchedule` (398), `MAX_MULTIPLICITY` (405), tests present and green. **Closing deletes nothing.** The code was sound; what was refuted is a sentence in a commit message that **never entered `src/`** (`git grep "special case of retraction" origin/main` matches research docs only). **Nothing needs reverting.** REVIVE is wrong (all four hold, plus D1/D2); REVISE is wrong *for the PR* (the narrow true claim is already merged and tested).

Three follow-ups are named in §3.1, none of which is this PR — chiefly amending the geometry doc's §14.1, which currently sits on `main` saying *"widening is a restricted retraction — holds"* against the adversarial doc's *"refuted"*, with no cross-reference. **The equivocation is on the word "widening"**, which does double duty in one file for `widen` and for `foldRetained`; §14.1's measurement is sound, its label overreaches.

## Lint, with the known carve-out worked around

`docs/research/2026-*-*.md` is in `.markdownlint-cli2.jsonc` `ignores`, so linting in place exits 0 having linted nothing. Linted as a copy at `docs/lint-probe-adjudication.md`: **exit 0, clean**. Carve-out proven by control — identical **sabotaged** content exits **1** with five `MD009`/`MD012`/`MD018` errors outside the glob and exits **0 silently** inside it. Both probe files deleted; `git status` clean but for the two intended files.

Agency-Signature-Version: 1
Agent: lumen
Agent-Runtime: claude-code
Agent-Model: claude-opus-5
Credential-Identity: AceHack
Credential-Mode: shared
Human-Review: not-implied-by-credential
Human-Review-Evidence: none
Action-Mode: autonomous-fail-open
Task: none
Co-authored-by: lumen <lumen@zeta.agents>


## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 0 |
| Resolved threads | 0 |
| Unresolved threads | 0 |
| Total review comments | 0 |
| Total fix commits (touching thread paths) | 0 |

## Review threads

(no review threads on this PR)

## Fix commits (touching thread paths)

(no commits in this PR touched files anchored by review threads)
