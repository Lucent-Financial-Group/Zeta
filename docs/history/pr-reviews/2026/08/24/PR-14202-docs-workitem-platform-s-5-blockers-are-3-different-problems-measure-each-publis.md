# PR #14202 -- docs(workitem): platform's 5 blockers are 3 different problems — measure each, publish neither Flowdent image

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 14202 |
| Title | docs(workitem): platform's 5 blockers are 3 different problems — measure each, publish neither Flowdent image |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-08-23T13:51:39Z |
| Merged at | 2026-08-24T11:17:06Z |
| Merge commit SHA | `c880c3165afde179ad68cdf990af2df9b1807652` |
| Branch | `fix/publish-platform-packages` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/14202 |
| Changed files | 2 |
| Additions / deletions | +151 / -0 |

## Description

## What this is

**No code change, and no chart change.** The deliverable was a package-visibility flip;
the measured answer is that **GitHub exposes no API for it**, so this PR ships the
measurement and the decision it surfaced instead of inventing a diff.

## 1. Visibility is UI-only — verified, not inherited

A prior agent reported this and declined on those grounds. Rechecked from scratch:

```
GET    /orgs/Lucent-Financial-Group/packages/container/zeta-portal              -> 200
PATCH  /orgs/Lucent-Financial-Group/packages/container/zeta-portal              -> 404
POST   /orgs/Lucent-Financial-Group/packages/container/zeta-portal              -> 404
PUT    /orgs/Lucent-Financial-Group/packages/container/zeta-portal              -> 404
PATCH  /orgs/Lucent-Financial-Group/packages/container/zeta-portal/visibility   -> 404
POST   /orgs/Lucent-Financial-Group/packages/container/zeta-portal/visibility   -> 404
PUT    /orgs/Lucent-Financial-Group/packages/container/zeta-portal/visibility   -> 404
```

`GET` is 200 on the *same path* every write method 404s on, so the 404 means "no such
route", not "no such package". Corroborated two more ways: the REST Packages reference
enumerates 27 endpoints (`GET`/`DELETE`/`restore` only, none for visibility), and GraphQL
schema introspection exposes exactly one package mutation, `deletePackageVersion`. The
token carries `admin:org` + `write:packages` — not a scope shortfall.

**Aaron's two clicks**, per package:
`https://github.com/orgs/Lucent-Financial-Group/packages/container/package/zeta-portal/settings`
→ **Danger Zone** → **Change visibility** → **Public** → type the package name →
**"I understand the consequences, change package visibility"**. Repeat for
`.../package/zeta-platform-controller/settings`.

⚠️ **Irreversible** — GitHub: *"Once you make a package public, you cannot make it private
again."* Gated non-reversible class; authorized because Aaron named both packages
explicitly, not because an agent judged it safe.

## 2. Flowdent — do NOT publish; this is a manifest defect

Aaron's line keys on **source-repo privacy**, not image visibility. Measured provenance from
the packages' own `repository` links:

| image | source repo | repo state |
|---|---|---|
| `ghcr.io/flowdent/cloudservice` | `Flowdent/fd-core` | `private=true` / `private` |
| `ghcr.io/flowdent/fd-webclient` | `Flowdent/fd-webclient` | `private=true` / `internal` |

`Flowdent` org: **`public_repos: 0`, `owned_private_repos: 9`.** No public Flowdent source
exists for anything.

So these fall on the do-not-publish side, and the defect is upstream: the **public** Zeta
tree references build outputs of **private** repositories
(`blueprints-flowdent.yaml:19,50`, included via `Application.yaml:29`). That is
`clone-at-tag-stays-sufficient` in a different costume — a dependency nobody outside can
satisfy.

**Options are recorded for Aaron and Max to choose. Nothing is removed from the chart here**
— changing what a deployment references changes what a deployment does.

## 3. `ich777/steamcmd:armareforger` — does not exist upstream

The prior refusal to substitute a sibling tag stands (the four game tags share only their
first two layers).

## The honest consequence

**Publishing both of ours does not unquarantine `platform`.** It takes the blocker list
from **5 → 3**; `platform` stays in CANNOT BE PRICED until #2 and #3 are answered.

## Confirmed unaffected

The org publishes **exactly two** container packages, so `hat-system-operator` and
`zeta-orleans-silo` both `404` — no visibility change reaches them.
081M0QB1ZBW087G0R002664EN7 and 081M0QB1Q6Z087G0R00091JH3Q stand.

## Falsifier (recorded in the row)

Anonymous, no token, both images **today**: manifest `GET -> 403`. After the flip this must
become `200` **without a token** — visibility reported as `public` is not the same as
anonymously pullable, and the partitioner has no token. Then `lane-partition.ts --rung dev`
must show `platform` with **3** unmeasurable images, not 5.

## Verification run (this branch's parent, `324da722d`)

```
covered by a lane: 41/47  |  quarantined: 3 oversize + 3 unpriced
```

Unchanged by this PR, as expected — it adds a work-item only. `hindsight` / `vllm` /
`gitlab` remain oversize pending #14163 (`freeDiskGib: 14 → 70`), which is still open.
`main`'s `gate` is red with a queue; BLOCKED is expected and not this change's doing.
**No `--admin`.**


🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_015VrXL5qefejozgUzchMRXp

Agency-Signature-Version: 1
Agent: shadow
Agent-Runtime: claude-code
Agent-Model: claude-opus-5
Credential-Identity: AceHack
Credential-Mode: shared
Human-Review: not-implied-by-credential
Human-Review-Evidence: none
Action-Mode: human-directed
Task: none
Co-authored-by: shadow <shadow@zeta.agents>


## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 0 |
| Resolved threads | 0 |
| Unresolved threads | 0 |
| Total review comments | 0 |
| Total fix commits (touching thread paths) | 0 |

## Review threads

(no review threads on this PR)

## Fix commits (touching thread paths)

(no commits in this PR touched files anchored by review threads)
