# PR #15928 -- hygiene: wire the oscillation registry into CI

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 15928 |
| Title | hygiene: wire the oscillation registry into CI |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-08-27T23:04:53Z |
| Merged at | 2026-08-27T23:28:44Z |
| Merge commit SHA | `559a47d737fbd8202a91eac0c2a6d9f3cae81296` |
| Branch | `shadow/wire-oscillation-registry` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/15928 |
| Changed files | 4 |
| Additions / deletions | +301 / -0 |

## Description

`oscillation-registry.ts` landed with 17 falsifiers and **zero callers** — a tested module nobody
runs proves its own properties and constrains nothing. Fourth instance of that shape in a day; this
is the caller.

## What it adds over the harness

`healer-harness` certifies idempotence, closure and convergence of the **composite**, in the **one**
order `composeHealers` folds. That proves it reaches *a* fixed point, never *the* fixed point — so
the hardcoded order in `run-tier0.ts` becomes silently load-bearing the moment two healers write the
same file. Five healers admit **120 orders; exactly one is certified.**

| relation | verdict |
|---|---|
| `disjoint` / `commutes` | pass — overlap isn't illegal, and refusing it would block the rule-template collapse |
| `override` | **fail unless declared** with a reason |
| `oscillate` | **fail always** — no declaration excuses it |

## The vacuity guard is why this is safe to run

A pair only disagrees on inputs both touch. Measured: the three **built-in harness fixtures fire
none of the Tier-0 healers**, so a registry over them alone reports all-disjoint and is a green
check over an empty experiment. The runner **exits 2** on a corpus that fired nothing.

## Current state, measured

**10 pairs, all disjoint, DAG height 1.** Height 1 is an antichain — the roster composes as a *set*
and `run-tier0`'s order isn't load-bearing today. That's a property of *this roster*, not a guarantee
of the harness, and the harness's own test file already proves order-dependence is reachable
(*"composition order decides legality"*).

`DECLARED_EDGES` is empty because there's no order to declare, and a test pins that rather than
leaving it an unchecked omission.

## Tier — stated plainly

The host job `lint-bash-retirement-inventory` is **not** in `gate-required`'s 8-job needs list, so
this **runs but does not block a merge** — same advisory tier as its neighbour. Promoting it would
change what blocks merges repo-wide; that's a separate decision, not one to take while wiring.

Six runner tests prove **each exit path is reachable** — oscillate, undeclared override, declared
override passing, and the vacuous-corpus refusal. Without them, every assertion is satisfied by a
checker that always returns 0.

tsc clean · package.json valid · YAML valid · runs green.

Agency-Signature-Version: 1
Agent: shadow
Agent-Runtime: Claude Code
Agent-Model: claude-opus-5
Credential-Identity: AceHack
Credential-Mode: shared
Human-Review: not-implied-by-credential
Human-Review-Evidence: none
Action-Mode: human-directed
Task: none

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 1 |
| Resolved threads | 1 |
| Unresolved threads | 0 |
| Total review comments | 1 |
| Total fix commits (touching thread paths) | 2 |

## Review threads

### Thread 1 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6dAezL`
- **Path**: `src/Core.TypeScript/hygiene/check-healer-oscillation.ts`
- **Replies**: 0

**Initial comment:**

- **`github-advanced-security[bot]` (bot)** at 2026-08-27T23:10:54Z on `src/Core.TypeScript/hygiene/check-healer-oscillation.ts`:84 (association: NONE)

  <pre>
  ## CodeQL / Potential file system race condition

  The file may have changed since it [was checked](1).

  [Show more details](https://github.com/Lucent-Financial-Group/Zeta/security/code-scanning/812)
  </pre>

## Fix commits (touching thread paths)

### `5cabe7e404840928b6f2a20c1276df91bf430451` -- 2026-08-27T23:04:50Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `src/Core.TypeScript/hygiene/check-healer-oscillation.ts`

**Message:**

```
hygiene: wire the oscillation registry into CI

`oscillation-registry.ts` landed with 17 falsifiers and ZERO callers. A
tested module nobody runs proves its own properties and constrains nothing --
the same shape as a check that cannot fail, and the fourth instance of it I
shipped in a day. This is the caller.

WHAT IT ADDS OVER THE HARNESS. `healer-harness` certifies idempotence,
closure and convergence of the COMPOSITE in the ONE order `composeHealers`
folds. That proves the composite reaches *a* fixed point, never *the* fixed
point -- so the hardcoded order in run-tier0.ts is silently load-bearing the
moment two healers write the same file. Five healers admit 120 orders and
exactly one is certified.

Fails on the two relations that are genuinely defects: an OSCILLATING pair
(the composite never settles; no declaration excuses it) and an UNDECLARED
OVERRIDE (order load-bearing and undocumented). `disjoint` and `commutes`
pass -- overlap is not illegal, and refusing it would block the rule-template
collapse the parity family needs.

THE VACUITY GUARD IS WHY THIS IS SAFE TO RUN AT ALL. A pair only disagrees on
inputs both touch, so a corpus firing neither healer reports `disjoint` for
everything and means nothing. Measured: the three BUILT-IN harness fixtures
fire NONE of the Tier-0 healers, so a registry over them alone would be a
green check over an empty experiment. The runner exits 2 rather than 0 on a
corpus that fired nothing.

Current state, measured not assumed: 10 pairs, all disjoint, DAG height 1.
Height 1 is an antichain -- the roster composes as a SET and run-tier0's
order is not load-bearing TODAY. That is a property of this roster, not a
guarantee of the harness, and the harness's own test file already proves
order-dependence is reachable ("composition order decides legality").

DECLARED_EDGES is empty because there is no order to declare, and a test pins
that rather than leaving it as an omission nobody checked.

TIER, STATED PLAINLY: the host job `lint-bash-retirement-inventory` is NOT in
`gate-required`'s 8-job needs list, so this check RUNS but does NOT block a
merge -- same advisory tier as its neighbour. Promoting it would change what
blocks merges repo-wide and is a separate decision, not one to take while
wiring.

Six runner tests prove each exit path is REACHABLE -- oscillate, undeclared
override, declared override passing, and the vacuous-corpus refusal. Without
them every assertion is satisfied by a checker that always returns 0.

tsc clean, package.json valid, YAML valid, runs green via the script name.

Agency-Signature-Version: 1
Agent: shadow
Agent-Runtime: Claude Code
Agent-Model: claude-opus-5
Credential-Identity: AceHack
Credential-Mode: shared
Human-Review: not-implied-by-credential
Human-Review-Evidence: none
Action-Mode: human-directed
Task: none
Co-authored-by: Claude Opus 5 <noreply@anthropic.com>
```

### `b2b07b1756c1b2e6d6dbe06ab2c98cafbfb1f35e` -- 2026-08-27T23:12:33Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `src/Core.TypeScript/hygiene/check-healer-oscillation.ts`

**Message:**

```
fix: my own linter caught my new file, and my control test was not he…

…rmetic

Two real defects in check-healer-oscillation.ts, both mine, both caught by
the repo's own machinery rather than by me.

1. TOCTOU, caught by `lint-check-then-use-file-races` -- the linter I
   extended yesterday, firing on the file I wrote today. Two findings:

     existsSync(dir) gating readdirSync(dir)  -- a check-then-use race whose
       answer is stale before the use runs, and readdirSync reports absence
       perfectly well by throwing.
     readdirSync then statSync per entry      -- asking the filesystem a
       question the LISTING ALREADY ANSWERED.

   Fixed as the linter prescribes: no existsSync gate, and
   `readdirSync(dir, { withFileTypes: true })` so the kind arrives with the
   name and there is no second syscall to race against. One syscall, one
   answer, no window.

2. The sampler control test derived the repo root from `import.meta.url` with
   string surgery and asserted it found files. Green on a laptop, red on a
   runner -- environment-dependent in exactly the way that makes a test look
   like coverage and provide none. Rebuilt hermetically: it now creates the
   directories the sampler looks for and checks the WALKER, including that
   repo-relative keys come back (the healers match on `src/...` and
   `docs/...`, so absolute paths would silently match nothing) and that a
   wrong-extension file in a watched directory is skipped. A second test pins
   that an absent directory is skipped rather than thrown.

The first defect is the more interesting one: I wrote a check to catch a
class of defect, then committed an instance of that class in the check
itself, and the linter caught it. That is the apparatus working exactly as
intended on its own author.

3426 hygiene tests pass, races clean against baseline, tsc clean, the
oscillation check still reports 10 pairs / height 1.

Agency-Signature-Version: 1
Agent: shadow
Agent-Runtime: Claude Code
Agent-Model: claude-opus-5
Credential-Identity: AceHack
Credential-Mode: shared
Human-Review: not-implied-by-credential
Human-Review-Evidence: none
Action-Mode: human-directed
Task: none
Co-authored-by: Claude Opus 5 <noreply@anthropic.com>
```
