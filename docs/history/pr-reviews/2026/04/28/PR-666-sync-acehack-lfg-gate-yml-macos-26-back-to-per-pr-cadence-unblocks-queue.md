# PR #666 -- sync(acehack→lfg): gate.yml — macos-26 back to per-PR cadence (unblocks queue)

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 666 |
| Title | sync(acehack→lfg): gate.yml — macos-26 back to per-PR cadence (unblocks queue) |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-04-28T16:08:07Z |
| Merged at | 2026-04-28T16:16:40Z |
| Merge commit SHA | `861b1c570b8362acfe4512f94e0af8a639b5a679` |
| Branch | `sync/forward-gate-yml-macos-26-back-to-per-pr` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/666 |
| Changed files | 1 |
| Additions / deletions | +15 / -14 |

## Description

## Summary

Forward-ports gate.yml from AceHack to LFG. AceHack already moved macos-26 + Analyze (csharp) back to per-PR cadence; LFG is stale on the prior cadence-split (matrix-setup dynamic switch that excludes macos-26 from pull_request events).

**Effect:** unblocks the structurally-blocked LFG queue. Branch protection requires `build-and-test (macos-26)` as a status check; LFG gate.yml's PR-event matrix didn't include it. After this PR lands, PR-events run macos-26 directly.

## Aaron's correction

> *"we pull out mac and codeql to merge to main time instead of per pr, and then moved it back to per pr, I think you just have some staleness and not update to date with the move back to per per"*
> — Aaron 2026-04-28T16:30Z

## Composes with

- PR #657 (the originally-queued forward-sync of this same change; itself BLOCKED by this gate — becomes redundant once this lands)
- task #306 (cadence-fast revisit — Analyze csharp sibling of macos-26 gate question)
- BLOCKED LFG queue: #655/#656/#657/#658/#659/#660/#661/#662/#663/#665 — all awaiting this unblock

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 7 |
| Resolved threads | 7 |
| Unresolved threads | 0 |
| Total review comments | 14 |
| Total fix commits (touching thread paths) | 1 |

## Review threads

### Thread 1 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5-MYas`
- **Path**: `.github/workflows/gate.yml`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-04-28T16:10:10Z on `.github/workflows/gate.yml`:239 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Revert unpinned Semgrep bootstrap**

  This change reintroduces `pip install` for Semgrep, which makes the `lint (semgrep)` gate non-deterministic because CI now pulls whatever Semgrep and pip versions are current at run time instead of the repo-pinned toolchain version. In practice, a new upstream Semgrep release can start failing PRs (or alter findings) without any repository change, and it also diverges from the three-way parity path (`tools/setup/install.sh` + `.mise.toml` pin) used elsewhere in this workflow.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-28T16:13:25Z on `.github/workflows/gate.yml`:239 (association: MEMBER)

    <pre>
    **EVIDENCE-BASED fix applied — PR is now surgical (15+/14- lines instead of 179+/109-).**

    You're right: AceHack's gate.yml has the older Semgrep bootstrap (`pip install semgrep` via setup-python) while LFG already migrated to mise-pinned `pipx:semgrep` via three-way-parity `install.sh` (resolves Scorecard PinnedDependenciesID #17 + #18 per the comment in LFG's gate.yml).

    Force-pushed surgical version that:
    - Bases on LFG main's gate.yml (preserves the mise-pinned Semgrep + every other LFG-newer improvement)
    - Only edits matrix-setup's PR/merge_group branch from `[ubuntu-24.04, ubuntu-24.04-arm]` to `[ubuntu-24.04, ubuntu-24.04-arm, macos-26]`
    - Updates the comment to reflect the cadence change

    Diff stat is now 1 file / 15 insertions / 14 deletions (was 179/109 with the full-file copy).

    Resolving.
    </pre>

### Thread 2 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5-Mat7`
- **Path**: `.github/workflows/gate.yml`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-28T16:11:57Z on `.github/workflows/gate.yml`:287 (association: NONE)

  <pre>
  P1 (operational_implications): This cache path includes repo directories `tools/tla` and `tools/alloy`, which contain tracked sources (e.g., tools/alloy/AlloyRunner.java, specs). Restoring a cache can overwrite PR-modified tracked files with older cached versions because the key doesn't include the commit SHA. Cache only the downloaded artifacts (e.g., tools/tla/tla2tools.jar and tools/alloy/alloy.jar) or move generated artifacts out of the git working tree before caching.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-28T16:16:21Z on `.github/workflows/gate.yml`:287 (association: MEMBER)

    <pre>
    **EVIDENCE-BASED carve-out: pre-existing in LFG main, not introduced by this PR.**

    The cache step with `path: tools/tla / tools/alloy` is identical on `origin/main` and on this PR's branch:

    ```bash
    git diff origin/main...origin/sync/forward-gate-yml-macos-26-back-to-per-pr -- .github/workflows/gate.yml
    # 15+/14- — only the matrix-setup OS list and surrounding comment changed
    ```

    Codex's concern about cache restore overwriting tracked files in `tools/tla/` (specs/) and `tools/alloy/` (`AlloyRunner.java` + specs) is valid as a general LFG-side observation, but it predates this PR. Filing as backlog row B-0080 for separate cleanup (split cache key to a non-tracked path, OR add a guard step to verify tracked-file invariance before restore).

    Resolving here. PR #666's scope stays narrow: macos-26 cadence unblock only.
    </pre>

### Thread 3 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5-Mauw`
- **Path**: `.github/workflows/gate.yml`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-28T16:11:57Z on `.github/workflows/gate.yml`:379 (association: NONE)

  <pre>
  P1 (operational_implications): The cache for install.sh outputs includes `tools/tla` and `tools/alloy` (repo-tracked directories). Cache restore can overwrite tracked files changed in a PR since the key is based on toolchain files, not the git SHA. Prefer caching only the generated jar paths (tools/tla/tla2tools.jar, tools/alloy/alloy.jar) or an out-of-tree artifact directory.
  ```suggestion
              tools/tla/tla2tools.jar
              tools/alloy/alloy.jar
  ```
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-28T16:16:24Z on `.github/workflows/gate.yml`:379 (association: MEMBER)

    <pre>
    **EVIDENCE-BASED carve-out: pre-existing in LFG main, not introduced by this PR.**

    The cache step with `path: tools/tla / tools/alloy` is identical on `origin/main` and on this PR's branch:

    ```bash
    git diff origin/main...origin/sync/forward-gate-yml-macos-26-back-to-per-pr -- .github/workflows/gate.yml
    # 15+/14- — only the matrix-setup OS list and surrounding comment changed
    ```

    Codex's concern about cache restore overwriting tracked files in `tools/tla/` (specs/) and `tools/alloy/` (`AlloyRunner.java` + specs) is valid as a general LFG-side observation, but it predates this PR. Filing as backlog row B-0080 for separate cleanup (split cache key to a non-tracked path, OR add a guard step to verify tracked-file invariance before restore).

    Resolving here. PR #666's scope stays narrow: macos-26 cadence unblock only.
    </pre>

### Thread 4 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5-MavY`
- **Path**: `.github/workflows/gate.yml`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-28T16:11:58Z on `.github/workflows/gate.yml`:545 (association: NONE)

  <pre>
  P1 (operational_implications): This cache step caches `tools/tla` and `tools/alloy` (git working-tree paths that also contain tracked sources). A cache restore can clobber PR changes in those directories because the cache key doesn't vary with the repo revision. Restrict the cache to the generated jar files (tools/tla/tla2tools.jar and tools/alloy/alloy.jar) or relocate generated artifacts outside the repo tree.
  ```suggestion
              tools/tla/tla2tools.jar
              tools/alloy/alloy.jar
  ```
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-28T16:16:27Z on `.github/workflows/gate.yml`:545 (association: MEMBER)

    <pre>
    **EVIDENCE-BASED carve-out: pre-existing in LFG main, not introduced by this PR.**

    The cache step with `path: tools/tla / tools/alloy` is identical on `origin/main` and on this PR's branch:

    ```bash
    git diff origin/main...origin/sync/forward-gate-yml-macos-26-back-to-per-pr -- .github/workflows/gate.yml
    # 15+/14- — only the matrix-setup OS list and surrounding comment changed
    ```

    Codex's concern about cache restore overwriting tracked files in `tools/tla/` (specs/) and `tools/alloy/` (`AlloyRunner.java` + specs) is valid as a general LFG-side observation, but it predates this PR. Filing as backlog row B-0080 for separate cleanup (split cache key to a non-tracked path, OR add a guard step to verify tracked-file invariance before restore).

    Resolving here. PR #666's scope stays narrow: macos-26 cadence unblock only.
    </pre>

### Thread 5 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5-Mavy`
- **Path**: `.github/workflows/gate.yml`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-28T16:11:58Z on `.github/workflows/gate.yml`:239 (association: NONE)

  <pre>
  P1 (discrepancy_with_pr_description): The PR title/description frames this as a macos-26 cadence unblock, but this diff also changes the Semgrep installation approach (setup-python + pip) and adds broad install.sh caching/retry logic to lint jobs. Please either (a) narrow the PR to the macos-26 matrix change, or (b) explicitly call out these additional behavior changes in the PR description so reviewers know to evaluate them.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-28T16:16:30Z on `.github/workflows/gate.yml`:239 (association: MEMBER)

    <pre>
    **EVIDENCE-BASED stale-snapshot false-positive (Codex/Copilot reviewer-false-positive catalog class 1).**

    This thread reviews content from the pre-force-push state of the PR. After the [Codex P2 catch on Semgrep regression](https://github.com/Lucent-Financial-Group/Zeta/pull/666#discussion_r-MYas), I force-pushed a surgical patch that:

    - Bases on LFG main's gate.yml (preserves the mise-pinned `pipx:semgrep` via `install.sh` — not `pip install semgrep`)
    - Only edits matrix-setup's PR/merge_group OS list to include macos-26

    `git diff origin/main...origin/sync/forward-gate-yml-macos-26-back-to-per-pr -- .github/workflows/gate.yml` now shows 15+/14- changes total — the Semgrep / pip-install / setup-python diffs you flagged no longer exist in the current branch HEAD.

    Resolving as stale-snapshot.
    </pre>

### Thread 6 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5-MawM`
- **Path**: `.github/workflows/gate.yml`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-28T16:11:58Z on `.github/workflows/gate.yml`:19 (association: NONE)

  <pre>
  P1 (codebase_conventions): This workflow comment introduces direct name attribution (e.g., "Aaron" / "Otto-"). Per docs/AGENT-BEST-PRACTICES.md:284-299, current-state surfaces like workflows should use role references (e.g., "human maintainer") and avoid persona/contributor names outside the history-surface carve-outs.
  ```suggestion
  #     work; bumped from 22.04 on 2026-04-28 per human maintainer
  #     review and version-currency verification — ubuntu-latest =
  #     24.04 since Jan 2025).
  ```
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-28T16:16:33Z on `.github/workflows/gate.yml`:19 (association: MEMBER)

    <pre>
    **EVIDENCE-BASED stale-snapshot false-positive (Codex/Copilot reviewer-false-positive catalog class 1).**

    This thread reviews content from the pre-force-push state of the PR. After the [Codex P2 catch on Semgrep regression](https://github.com/Lucent-Financial-Group/Zeta/pull/666#discussion_r-MYas), I force-pushed a surgical patch that:

    - Bases on LFG main's gate.yml (preserves the mise-pinned `pipx:semgrep` via `install.sh` — not `pip install semgrep`)
    - Only edits matrix-setup's PR/merge_group OS list to include macos-26

    `git diff origin/main...origin/sync/forward-gate-yml-macos-26-back-to-per-pr -- .github/workflows/gate.yml` now shows 15+/14- changes total — the Semgrep / pip-install / setup-python diffs you flagged no longer exist in the current branch HEAD.

    Resolving as stale-snapshot.
    </pre>

### Thread 7 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5-Mawm`
- **Path**: `.github/workflows/gate.yml`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-28T16:11:59Z on `.github/workflows/gate.yml`:240 (association: NONE)

  <pre>
  P0 (security/best_practices): The semgrep job bypasses the repo's pinned toolchain and installs Semgrep via `pip install semgrep` (unpinned) after `pip install --upgrade pip`. This reintroduces the unpinned supply-chain surface that .mise.toml explicitly documents eliminating ("pipx:semgrep" pin) and breaks three-way parity via tools/setup/install.sh. Use the mise/uv-managed semgrep from install.sh (or otherwise pin semgrep to the .mise.toml version and avoid pip upgrade).
  ```suggestion
        - name: Install pinned toolchain
          run: tools/setup/install.sh
  ```
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-28T16:16:36Z on `.github/workflows/gate.yml`:240 (association: MEMBER)

    <pre>
    **EVIDENCE-BASED stale-snapshot false-positive (Codex/Copilot reviewer-false-positive catalog class 1).**

    This thread reviews content from the pre-force-push state of the PR. After the [Codex P2 catch on Semgrep regression](https://github.com/Lucent-Financial-Group/Zeta/pull/666#discussion_r-MYas), I force-pushed a surgical patch that:

    - Bases on LFG main's gate.yml (preserves the mise-pinned `pipx:semgrep` via `install.sh` — not `pip install semgrep`)
    - Only edits matrix-setup's PR/merge_group OS list to include macos-26

    `git diff origin/main...origin/sync/forward-gate-yml-macos-26-back-to-per-pr -- .github/workflows/gate.yml` now shows 15+/14- changes total — the Semgrep / pip-install / setup-python diffs you flagged no longer exist in the current branch HEAD.

    Resolving as stale-snapshot.
    </pre>

## Fix commits (touching thread paths)

### `e352411bf80c12293de68fa55a645c6a355ee264` -- 2026-04-28T16:07:44Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `.github/workflows/gate.yml`

**Message:**

```
sync(acehack→lfg): gate.yml — macos-26 back to per-PR cadence (surgical)

LFG main's gate.yml uses dynamic matrix-setup that excludes macos-26
from pull_request + merge_group events ("Linux production legs
only"). Branch protection on main requires `build-and-test (macos-26)`
as a status check, so every LFG PR is structurally BLOCKED.

Aaron's correction (2026-04-28T16:30Z): "we pull out mac and codeql
to merge to main time instead of per pr, and then moved it back to
per pr, I think you just have some staleness and not update to date
with the move back to per per".

This PR makes the surgical edit: matrix-setup's PR/merge_group
branch now outputs `[ubuntu-24.04, ubuntu-24.04-arm, macos-26]`
(was `[ubuntu-24.04, ubuntu-24.04-arm]`). Comment updated to
reflect the cadence change.

Initial draft full-file copy from AceHack was correctly flagged by
Codex P2: AceHack's gate.yml has the older Semgrep bootstrap
(`pip install semgrep`) while LFG already migrated to mise-pinned
`pipx:semgrep` via three-way-parity install.sh (resolves Scorecard
PinnedDependenciesID #17/#18). LFG's improvement preserved by
basing this surgical patch on LFG main rather than AceHack.

Composes with:
- PR #657 (originally-queued forward-sync — becomes redundant)
- task #306 (cadence-fast revisit)
- BLOCKED LFG queue: #655..#666 awaiting this unblock

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
```
