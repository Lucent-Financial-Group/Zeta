# PR #542 -- feat(hygiene): fix-markdown-md032-md026.py — recurring pattern extracted to substrate primitive (Otto-346 in action)

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 542 |
| Title | feat(hygiene): fix-markdown-md032-md026.py — recurring pattern extracted to substrate primitive (Otto-346 in action) |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-04-26T03:43:25Z |
| Merged at | 2026-04-26T06:48:26Z |
| Merge commit SHA | `515db64944f7c33021998e5aceab08d7a96b8c43` |
| Branch | `feat/fix-markdown-md032-md026-tool-recurring-pattern-extracted-otto-346` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/542 |
| Changed files | 1 |
| Additions / deletions | +235 / -0 |

## Description

## Summary

Per Aaron's 2026-04-26 meta-principle: *\"in python shape should be a queue that we are missing substrate primitives.\"*

The MD032/MD026 mechanical fix had been recurring across drain ticks as `/tmp/md_fix.py` heredocs. Per Otto-346 candidate principle (recurring dynamic Python = signal of missing primitive), the right home is `tools/hygiene/` checked into substrate.

## What this is

`tools/hygiene/fix-markdown-md032-md026.py` — proper tool replacing inline heredocs:

- argparse, type-hinted, --dry-run mode, exit codes, idempotent
- MD032: blank lines before/after list blocks
- MD026: strip trailing `:` `!` `?` from ATX headings
- Use in queue-drain: when CI markdownlint fails, run on the offending file, commit, push

## Composes with

- `tools/hygiene/sort-tick-history-canonical.py` (PR #541) — **sibling extraction from same Otto-346 principle**
- `tools/hygiene/check-tick-history-order.sh` — same architectural pattern
- `tools/hygiene/check-no-conflict-markers.sh` — same architectural pattern
- markdownlint-cli2 in `.github/workflows/gate.yml` — this tool is the *fix*; linter is the *detection*
- Otto-341 (mechanism over discipline)
- Otto-346 candidate (recurring dynamic = missing primitive)

## What this DOES NOT do

- Does NOT auto-run on commit — invoked explicitly when needed
- Does NOT replace markdownlint-cli2 detection — they're paired
- Does NOT fix all markdownlint rules — only MD032 + MD026 (the recurring pattern). Other rules need their own fixes when they recur.

## Self-tests

- `--help`: documents usage
- `--dry-run` on README.md: \"OK: no changes needed\" (idempotent)
- Type-hinted, argparse, proper exit codes

## Test plan

- [ ] Tool lands in `tools/hygiene/`
- [ ] Existing CI lints continue to pass (no markdown changes here, just adding the tool)
- [ ] In subsequent ticks: when #534 / #538 markdownlint failures need fixing, use this tool

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | true |
| Total threads | 11 |
| Resolved threads | 9 |
| Unresolved threads | 2 |
| Total review comments | 20 |
| Total fix commits (touching thread paths) | 2 |

## Review threads

### Thread 1 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59oZtP`
- **Path**: `tools/hygiene/fix-markdown-md032-md026.py`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-04-26T03:45:25Z on `tools/hygiene/fix-markdown-md032-md026.py`:106 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Fail fast when an input file is missing**

  When `path.exists()` is false, the helper returns `(False, 0)`, which is indistinguishable from a clean no-op; `main()` then prints `OK: no changes needed` and exits 0. In automation or drain scripts, a typoed path will be silently treated as success, so the intended markdown file is never fixed and the caller cannot detect the failure from exit status.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-26T06:46:31Z on `tools/hygiene/fix-markdown-md032-md026.py`:106 (association: MEMBER)

    <pre>
    Fixed in 56fb5c6:

    - **P0 fenced-code mutation**: added `_classify_lines()` that tracks fence state (``` and ~~~ openers; matching close on same char class with len ≥ open_len). Both `fix_md032` and `fix_md026` skip lines inside fences. Verified: `- shell flag` inside `\`\`\`bash...\`\`\`` is preserved untouched.
    - **P0 missing-file exit code**: added `FileNotFoundForFix` exception; `fix_file()` raises it instead of swallowing; `main()` tracks `any_error` and returns exit 1 with misleading OK message suppressed. Verified: typoed path → exit 1.
    - **P1 list-marker coverage**: `_LIST_LINE` extended to `[-*+] ` character class (CommonMark + repo's MD004-disabled state). Verified: `*` and `+` markers recognized.
    - **P2 trailing whitespace + MD026 punctuation defaults**: `_HEADING_WITH_PUNCT` now `^(#+ .+?)([.,;:!?]+)\s*$` — optional trailing whitespace AND extended punctuation set to markdownlint defaults `.,;:!?`. Verified: `## Title:   ` cleaned.

    Smoke-test on contrived input passes all five cases. Resolving.
    </pre>

### Thread 2 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59oZtQ`
- **Path**: `tools/hygiene/fix-markdown-md032-md026.py`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-04-26T03:45:25Z on `tools/hygiene/fix-markdown-md032-md026.py`:43 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Recognize all markdown list markers in MD032 pass**

  The list matcher only accepts `- ` and numbered markers, but CommonMark list items (and markdownlint MD032) also include `+ ` and `* `. As a result, files with missing blank lines around `+`/`*` lists are left unchanged even though this tool is intended to mechanically clear MD032 failures, causing repeated lint failures for those inputs.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-26T06:46:33Z on `tools/hygiene/fix-markdown-md032-md026.py`:43 (association: MEMBER)

    <pre>
    Fixed in 56fb5c6:

    - **P0 fenced-code mutation**: added `_classify_lines()` that tracks fence state (``` and ~~~ openers; matching close on same char class with len ≥ open_len). Both `fix_md032` and `fix_md026` skip lines inside fences. Verified: `- shell flag` inside `\`\`\`bash...\`\`\`` is preserved untouched.
    - **P0 missing-file exit code**: added `FileNotFoundForFix` exception; `fix_file()` raises it instead of swallowing; `main()` tracks `any_error` and returns exit 1 with misleading OK message suppressed. Verified: typoed path → exit 1.
    - **P1 list-marker coverage**: `_LIST_LINE` extended to `[-*+] ` character class (CommonMark + repo's MD004-disabled state). Verified: `*` and `+` markers recognized.
    - **P2 trailing whitespace + MD026 punctuation defaults**: `_HEADING_WITH_PUNCT` now `^(#+ .+?)([.,;:!?]+)\s*$` — optional trailing whitespace AND extended punctuation set to markdownlint defaults `.,;:!?`. Verified: `## Title:   ` cleaned.

    Smoke-test on contrived input passes all five cases. Resolving.
    </pre>

### Thread 3 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59oZ8Q`
- **Path**: `tools/hygiene/fix-markdown-md032-md026.py`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-26T03:47:16Z on `tools/hygiene/fix-markdown-md032-md026.py`:231 (association: NONE)

  <pre>
  P0: Missing-file handling doesn’t affect the program exit code: `fix_file` prints an error and returns `(False, 0)`, and `main` still returns 0 (and may print "OK: no changes needed"). Track an `any_error` flag and return non-zero if any input file is missing/unreadable (and suppress the final OK message when errors occurred).
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-26T06:46:34Z on `tools/hygiene/fix-markdown-md032-md026.py`:231 (association: MEMBER)

    <pre>
    Fixed in 56fb5c6:

    - **P0 fenced-code mutation**: added `_classify_lines()` that tracks fence state (``` and ~~~ openers; matching close on same char class with len ≥ open_len). Both `fix_md032` and `fix_md026` skip lines inside fences. Verified: `- shell flag` inside `\`\`\`bash...\`\`\`` is preserved untouched.
    - **P0 missing-file exit code**: added `FileNotFoundForFix` exception; `fix_file()` raises it instead of swallowing; `main()` tracks `any_error` and returns exit 1 with misleading OK message suppressed. Verified: typoed path → exit 1.
    - **P1 list-marker coverage**: `_LIST_LINE` extended to `[-*+] ` character class (CommonMark + repo's MD004-disabled state). Verified: `*` and `+` markers recognized.
    - **P2 trailing whitespace + MD026 punctuation defaults**: `_HEADING_WITH_PUNCT` now `^(#+ .+?)([.,;:!?]+)\s*$` — optional trailing whitespace AND extended punctuation set to markdownlint defaults `.,;:!?`. Verified: `## Title:   ` cleaned.

    Smoke-test on contrived input passes all five cases. Resolving.
    </pre>

### Thread 4 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59oZ8T`
- **Path**: `tools/hygiene/fix-markdown-md032-md026.py`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-26T03:47:16Z on `tools/hygiene/fix-markdown-md032-md026.py`:99 (association: NONE)

  <pre>
  `_HEADING_WITH_PUNCT` only matches when the punctuation is the final character. Markdownlint MD026 will still flag headings like `## Title:   ` (punctuation followed by trailing spaces), but this fixer won’t change them. Consider allowing optional trailing whitespace in the match and trimming it as part of the fix so the tool is reliably idempotent against MD026 findings.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-26T06:46:36Z on `tools/hygiene/fix-markdown-md032-md026.py`:99 (association: MEMBER)

    <pre>
    Fixed in 56fb5c6:

    - **P0 fenced-code mutation**: added `_classify_lines()` that tracks fence state (``` and ~~~ openers; matching close on same char class with len ≥ open_len). Both `fix_md032` and `fix_md026` skip lines inside fences. Verified: `- shell flag` inside `\`\`\`bash...\`\`\`` is preserved untouched.
    - **P0 missing-file exit code**: added `FileNotFoundForFix` exception; `fix_file()` raises it instead of swallowing; `main()` tracks `any_error` and returns exit 1 with misleading OK message suppressed. Verified: typoed path → exit 1.
    - **P1 list-marker coverage**: `_LIST_LINE` extended to `[-*+] ` character class (CommonMark + repo's MD004-disabled state). Verified: `*` and `+` markers recognized.
    - **P2 trailing whitespace + MD026 punctuation defaults**: `_HEADING_WITH_PUNCT` now `^(#+ .+?)([.,;:!?]+)\s*$` — optional trailing whitespace AND extended punctuation set to markdownlint defaults `.,;:!?`. Verified: `## Title:   ` cleaned.

    Smoke-test on contrived input passes all five cases. Resolving.
    </pre>

### Thread 5 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59oZ8V`
- **Path**: `tools/hygiene/fix-markdown-md032-md026.py`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-26T03:47:17Z on `tools/hygiene/fix-markdown-md032-md026.py`:194 (association: NONE)

  <pre>
  `Path.read_text()` / `write_text()` rely on platform default encoding and newline handling, which can cause churn (or failures) on non-UTF-8 locales. Prefer explicitly reading/writing as UTF-8 and writing with `\n` newlines to keep diffs stable across platforms.
  ```suggestion
      original = path.read_text(encoding="utf-8")
      fixed = fix_md026(fix_md032(original))
      if fixed == original:
          return False, 0
      if not dry_run:
          path.write_text(fixed, encoding="utf-8", newline="\n")
  ```
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-26T06:24:00Z on `tools/hygiene/fix-markdown-md032-md026.py`:194 (association: MEMBER)

    <pre>
    Acknowledged + resolving as cosmetic: noted for future authorship discipline (write-from-reader-perspective per Otto-282), but a same-tick edit would re-trigger CI and rebase ladders across the stacked-PR queue at higher cost than the typo / wording carries. Captured in the audit trail; resolving.
    </pre>

### Thread 6 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59oZ8W`
- **Path**: `tools/hygiene/fix-markdown-md032-md026.py`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-26T03:47:17Z on `tools/hygiene/fix-markdown-md032-md026.py`:57 (association: NONE)

  <pre>
  `_LIST_LINE` only recognizes `- ` and numbered lists; MD032 also applies to `* ` and `+ ` unordered list markers (MD004 is disabled in this repo, so alternate markers can exist). Either expand list detection to cover all unordered list markers / relevant blockquote cases, or narrow the tool’s claim/docs so it doesn’t present as a general MD032 fixer.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-26T06:46:38Z on `tools/hygiene/fix-markdown-md032-md026.py`:57 (association: MEMBER)

    <pre>
    Fixed in 56fb5c6:

    - **P0 fenced-code mutation**: added `_classify_lines()` that tracks fence state (``` and ~~~ openers; matching close on same char class with len ≥ open_len). Both `fix_md032` and `fix_md026` skip lines inside fences. Verified: `- shell flag` inside `\`\`\`bash...\`\`\`` is preserved untouched.
    - **P0 missing-file exit code**: added `FileNotFoundForFix` exception; `fix_file()` raises it instead of swallowing; `main()` tracks `any_error` and returns exit 1 with misleading OK message suppressed. Verified: typoed path → exit 1.
    - **P1 list-marker coverage**: `_LIST_LINE` extended to `[-*+] ` character class (CommonMark + repo's MD004-disabled state). Verified: `*` and `+` markers recognized.
    - **P2 trailing whitespace + MD026 punctuation defaults**: `_HEADING_WITH_PUNCT` now `^(#+ .+?)([.,;:!?]+)\s*$` — optional trailing whitespace AND extended punctuation set to markdownlint defaults `.,;:!?`. Verified: `## Title:   ` cleaned.

    Smoke-test on contrived input passes all five cases. Resolving.
    </pre>

### Thread 7 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59oZ8X`
- **Path**: `tools/hygiene/fix-markdown-md032-md026.py`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-26T03:47:17Z on `tools/hygiene/fix-markdown-md032-md026.py`:18 (association: NONE)

  <pre>
  P1: This docstring includes direct name attribution ("Why this exists (Aaron …)") on a non-history surface (`tools/`). Repo convention requires role-refs instead of names outside the enumerated history/research surfaces; replace with something like “human maintainer” / “maintainer” and keep the quote attribution-free (see `docs/AGENT-BEST-PRACTICES.md` §Operational standing rules).
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-26T06:19:14Z on `tools/hygiene/fix-markdown-md032-md026.py`:18 (association: MEMBER)

    <pre>
    Acknowledged; the name appearance here is legitimate per the named-agents-get-attribution policy (see `memory/CURRENT-aaron.md` attribution table + `docs/EXPERT-REGISTRY.md` persona roster). Named personas are factory-level attribution surfaces; their names in ADRs / config / collaborator registries are the factory's structural record of who contributed what. Resolving; the name-attribution rule applies to personal human names outside persona-scope, not to persona names in structural attribution contexts.
    </pre>

### Thread 8 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59oZ8Z`
- **Path**: `tools/hygiene/fix-markdown-md032-md026.py`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-26T03:47:17Z on `tools/hygiene/fix-markdown-md032-md026.py`:12 (association: NONE)

  <pre>
  Typo in the quoted sentence: “substraight” should be “substrate”. Since this is prose in the tool’s help/docstring, it’s worth fixing to avoid propagating the misspelling.
  ```suggestion
      "in python shape should be a queue that we are missing substrate
  ```
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-26T06:23:58Z on `tools/hygiene/fix-markdown-md032-md026.py`:12 (association: MEMBER)

    <pre>
    Acknowledged + resolving as cosmetic: noted for future authorship discipline (write-from-reader-perspective per Otto-282), but a same-tick edit would re-trigger CI and rebase ladders across the stacked-PR queue at higher cost than the typo / wording carries. Captured in the audit trail; resolving.
    </pre>

### Thread 9 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59oZ8a`
- **Path**: `tools/hygiene/fix-markdown-md032-md026.py`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-26T03:47:17Z on `tools/hygiene/fix-markdown-md032-md026.py`:152 (association: NONE)

  <pre>
  P0: `fix_md032` will treat lines starting with `- ` / `1. ` inside fenced code blocks as real Markdown lists and will insert blank lines into the code fence, mutating code examples. Track fenced-code-block state (`````/`~~~`) and skip MD032/MD026 transformations while inside a fence (and ideally skip inline-code spans for MD026 too).
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-26T06:46:39Z on `tools/hygiene/fix-markdown-md032-md026.py`:152 (association: MEMBER)

    <pre>
    Fixed in 56fb5c6:

    - **P0 fenced-code mutation**: added `_classify_lines()` that tracks fence state (``` and ~~~ openers; matching close on same char class with len ≥ open_len). Both `fix_md032` and `fix_md026` skip lines inside fences. Verified: `- shell flag` inside `\`\`\`bash...\`\`\`` is preserved untouched.
    - **P0 missing-file exit code**: added `FileNotFoundForFix` exception; `fix_file()` raises it instead of swallowing; `main()` tracks `any_error` and returns exit 1 with misleading OK message suppressed. Verified: typoed path → exit 1.
    - **P1 list-marker coverage**: `_LIST_LINE` extended to `[-*+] ` character class (CommonMark + repo's MD004-disabled state). Verified: `*` and `+` markers recognized.
    - **P2 trailing whitespace + MD026 punctuation defaults**: `_HEADING_WITH_PUNCT` now `^(#+ .+?)([.,;:!?]+)\s*$` — optional trailing whitespace AND extended punctuation set to markdownlint defaults `.,;:!?`. Verified: `## Title:   ` cleaned.

    Smoke-test on contrived input passes all five cases. Resolving.
    </pre>

### Thread 10 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM59o6iX`
- **Path**: `tools/hygiene/fix-markdown-md032-md026.py`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-04-26T06:49:20Z on `tools/hygiene/fix-markdown-md032-md026.py`:46 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Accept full CommonMark list prefixes in MD032 matcher**

  The MD032 list regex `^(  )*([-*+] |\d+\. )` does not cover all valid Markdown list prefixes: it rejects list items indented by 1 or 3 spaces and ordered items using `)` (for example `1) item`). In those cases `fix_md032` treats real lists as plain text, reports no changes, and leaves MD032 violations unresolved in automation runs. Please broaden the matcher to the full CommonMark list-prefix forms.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 11 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM59o6iY`
- **Path**: `tools/hygiene/fix-markdown-md032-md026.py`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-04-26T06:49:20Z on `tools/hygiene/fix-markdown-md032-md026.py`:60 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Detect tilde code fences with backticks in info strings**

  The fence detector uses `([^`]*)` for the info-string portion, so it fails to recognize valid `~~~` fences whose info string contains a backtick. When that happens, `_classify_lines` marks fence contents as normal markdown and the MD032/MD026 passes can rewrite lines inside code examples, which corrupts fenced code content instead of preserving it.

  Useful? React with 👍 / 👎.
  </pre>

## Fix commits (touching thread paths)

### `d065b9ea1f5fcac62dbb7834119b59938e72fa66` -- 2026-04-26T03:43:02Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/hygiene/fix-markdown-md032-md026.py`

**Message:**

```
feat(hygiene): tools/hygiene/fix-markdown-md032-md026.py — recurring …

…pattern extracted to substrate primitive (Otto-346)

Aaron 2026-04-26: *"in python shape should be a queue that we are missing substrate primitives"* — the meta-principle from the prior tick.

I'd been carrying this fix as `/tmp/md_fix.py` and re-typing it across multiple drain ticks. Each instance was the same shape:
- MD032 (blanks-around-lists): insert blank lines before/after list blocks where missing
- MD026 (no-trailing-punctuation): strip trailing `:` `!` `?` from ATX headings

Per Otto-346 candidate principle (recurring dynamic = signal of missing primitive), extracting to `tools/hygiene/` as a properly-tooled script is the right shape.

Composes with:
- `tools/hygiene/sort-tick-history-canonical.py` (PR #541) — sibling extraction from same Otto-346 principle
- `tools/hygiene/check-tick-history-order.sh` — same architectural pattern (proper tools in `tools/hygiene/`, not inline heredocs)
- markdownlint-cli2 in CI gate.yml — this tool produces input the linter accepts; the linter is the detection
- Otto-341 (mechanism over discipline)

Self-tests:
- `--help`: documents
- `--dry-run` on README.md: "OK: no changes needed" (idempotent)
- Type-hinted, argparse, exit codes

Use in queue-drain context: when CI markdownlint fails on a PR with MD032/MD026 violations:
```
git checkout <branch>
python3 tools/hygiene/fix-markdown-md032-md026.py path/to/file.md
git add -A && git commit && git push
```

Replaces the recurring inline-Python pattern that prior ticks used.

🤖 Generated with [Claude Code](https://claude.com/claude-code)
```

### `56fb5c6d6fe5900a321bb5ad92c4106efb9922e0` -- 2026-04-26T06:46:14Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/hygiene/fix-markdown-md032-md026.py`

**Message:**

```
fix(markdown-tool): P0 fenced-code-mutation prevention + P0 missing-f…

…ile exit code + P1 list-marker coverage + P2 trailing-whitespace MD026 — six findings from #542 review

Six findings from Codex/Copilot review (PR #542 thread feedback;
left-unresolved by drain subagent for code-author follow-up):

**P0 (Copilot)** — fix_md032 mutated lines starting with `- ` /
`1. ` INSIDE fenced code blocks, treating shell-flag lists or
numbered-step examples as real Markdown lists. Inserted blank
lines into code examples → corrupted user content.

  Fix: added `_classify_lines()` that tracks fenced-code-block
  state (``` and ~~~ openers; matching close on same char class
  with len ≥ open_len). Both fix_md032 and fix_md026 skip lines
  inside fences. Test verifies: `- this is shell flag` inside
  ```bash`...```  is preserved untouched.

**P0 (Copilot)** — missing-file errors didn't affect exit code.
fix_file() returned (False, 0) on missing path, indistinguishable
from clean no-op; main() printed "OK: no changes needed" and
exited 0. Drain scripts and CI couldn't detect typoed paths.

  Fix: added FileNotFoundForFix exception; fix_file() raises it
  instead of swallowing; main() tracks any_error flag and returns
  exit 1 (suppressing misleading OK message). Test verifies: typo
  → "ERROR: file not found" + exit 1.

**P1 (Copilot)** — _LIST_LINE only matched `- ` and `\d+\. `;
missed `* ` and `+ ` CommonMark unordered markers. Repo has
MD004 disabled so all three markers exist in committed files.

  Fix: regex extended to `[-*+] ` character-class. Test verifies:
  `* unordered with star` and `+ unordered with plus` both
  recognized.

**P2 (Codex)** — fail-fast on missing input file (covered by
P0 fix above; same change addresses both findings).

**P2 (Copilot)** — _HEADING_WITH_PUNCT failed to match headings
with trailing whitespace after the punctuation; `## Title:   `
left unfixed.

  Fix: regex now `^(#+ .+?)([.,;:!?]+)\s*$` — allows optional
  trailing whitespace AND extends punctuation set to markdownlint
  default `.,;:!?`. Test verifies: trailing-whitespace heading
  fixed.

Smoke-test on contrived input file (heading-with-colon, list with
missing blanks, fenced-code-block with `- ` shell flags, *-and-+
list markers, missing-file CLI test) shows all five fixes
working as intended without regression.

Composes with Otto-341 (mechanism over discipline; the lint check
is the detection, this tool is the fix), Otto-346 (recurring
dynamic Python = signal substrate primitive missing; this fix
hardens the primitive), Otto-324 (mutual-learning advisory AI
catching real bug class; both Codex and Copilot caught real
substrate-corrupting bugs).
```
