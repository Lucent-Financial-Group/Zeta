# PR #582 -- backlog(B-0025): rename schema field directive: → ask: per Otto-293

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 582 |
| Title | backlog(B-0025): rename schema field directive: → ask: per Otto-293 |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-04-26T09:34:36Z |
| Merged at | 2026-04-26T10:45:07Z |
| Merge commit SHA | `c6527edc25e2448ff57c72833023ca8f341a6ffe` |
| Branch | `backlog/B-0025-rename-directive-to-ask-yaml-schema` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/582 |
| Changed files | 26 |
| Additions / deletions | +58 / -54 |

## Description

## Summary

- Mechanises Otto-293 mutual-alignment language at the YAML schema-field layer (B-0025 closure)
- 22 backlog row files: `^directive:` → `ask:` (Python regex anchored to YAML field start)
- 3 prose updates in `tools/backlog/README.md` (schema-table cell with Otto-293 rationale + 2 origin callouts)
- 1 prose update in `tools/backlog/generate-index.sh` (origin comment)
- 1 prose update in `docs/backlog/P3/B-0013` body
- B-0025 row itself: status `open` → `closed` + Outcome block

## Rationale

Aaron Otto-293 surfaced the "directive" register at the schema-field layer ("still sneaking in that one way langage" on B-0023's frontmatter). Body-prose corrections from Otto-220 didn't reach the schema field name itself.

`generate-index.sh` parses YAML frontmatter generically — no field-name-specific logic, no script logic broke.

## Picked from BACKLOG per Aaron 2026-04-26

> *"in the future all items on the backlog are non-speculative work, work that moves the project forward fee[l] free to pickup whatever you want, that's better than speculative work if you can do it."*

Drain Phase 1 (LFG queue) is on triage decision; BACKLOG-pickup is the right idle-fill. B-0025 was thematically chosen — it mechanises the very Otto-293 discipline Aaron re-corrected on this turn.

## Out of scope

- `.github/workflows/gate.yml` body-prose "maintainer directive 2026-04-24" mentions (3 instances in comments) — separate Otto-220 body-prose cleanup, not B-0025 schema scope. Per Otto-244 serialized-rename: split unless maintainer says combine.

## Test plan

- [x] `grep -c "^directive:" docs/backlog/**/*.md` → 0
- [x] `bash tools/backlog/generate-index.sh --check` runs without errors
- [x] All 22 renamed files retain valid YAML frontmatter

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | true |
| Total threads | 1 |
| Resolved threads | 1 |
| Unresolved threads | 0 |
| Total review comments | 2 |
| Total fix commits (touching thread paths) | 2 |

## Review threads

### Thread 1 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59pc0z`
- **Path**: `docs/backlog/P1/B-0003-alignment-md-rewrite.md`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-04-26T09:37:28Z on `docs/backlog/P1/B-0003-alignment-md-rewrite.md`:10 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Bump `last_updated` on modified backlog rows**

  This edit changes frontmatter content (`directive` → `ask`) but leaves `last_updated` at `2026-04-25`. The backlog schema documents `last_updated` as being refreshed on every content edit (`tools/backlog/README.md`), so these rows now carry stale metadata; any recency/audit workflow that uses this field will treat modified rows as unchanged.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-26T10:42:13Z on `docs/backlog/P1/B-0003-alignment-md-rewrite.md`:10 (association: MEMBER)

    <pre>
    Fixed in 2689cd3: bumped `last_updated` to 2026-04-26 on all 22 affected rows (the directive→ask rename IS a content edit per the schema). B-0025 itself was already at 2026-04-26 from the initial closure commit.
    </pre>

## Fix commits (touching thread paths)

### `7dd196edeeac637b3f93c4353a7f1942a7ea1818` -- 2026-04-26T09:33:12Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `docs/backlog/P1/B-0003-alignment-md-rewrite.md`

**Message:**

```
backlog(B-0025): rename schema field `directive:` → `ask:` per Otto-2…

…93 (one-way language at YAML layer)

Aaron Otto-293 surfaced the "directive" register at the YAML
schema-field layer ("still sneaking in that one way langage" on
B-0023's frontmatter). The body-prose corrections from
Otto-220ff didn't reach the schema field name itself. This PR
mechanises the rename at the schema layer.

Scope (final, one atomic PR per Otto-244 serialized-rename):

- 22 backlog row files: `^directive:` → `ask:` (Python regex
  anchored to YAML field start, not body prose)
- `tools/backlog/README.md`: frontmatter table cell + 2 origin
  callouts + Otto-293 rationale annotation on the field row
- `tools/backlog/generate-index.sh`: origin comment
- `docs/backlog/P3/B-0013` body prose: "Aaron 2026-04-25
  directive:" → "Aaron 2026-04-25 ask:"
- `B-0025` itself: status open → closed + Outcome block

`generate-index.sh` parses YAML frontmatter generically (no
field-name-specific logic), so no script logic broke. Verified
zero remaining `^directive:` matches across `docs/backlog/**`.

Composes with:

- Otto-293 — mutual-alignment language ("ask" not "directive")
- Otto-244 — rename cascades OK if right long-term + careful
  + serialized
- Aaron 2026-04-26 BACKLOG-over-speculative — picked from
  BACKLOG since LFG drain is on Aaron-decision triage and
  BACKLOG items are non-speculative project-forward work
- Otto-329 multi-phase plan — Phase 1 (LFG drain) is in flight
  but blocked on triage; this PR is BACKLOG-pickup not
  speculative work, and the rename is *thematically* the
  agency/accountability discipline being mechanised at the
  schema layer

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
```

### `2689cd3e7648e006d1e96a1f2dd7abf5f7ff4409` -- 2026-04-26T10:42:01Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `docs/backlog/P1/B-0003-alignment-md-rewrite.md`

**Message:**

```
backlog(B-0025 #582 thread): bump last_updated to 2026-04-26 on 22 re…

…named rows

Per Codex P2 on PR #582: schema documents `last_updated` as refreshed
on every content edit (`tools/backlog/README.md`). The directive→ask
rename IS a content edit, so all 22 affected rows get last_updated
bumped to 2026-04-26.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
```
