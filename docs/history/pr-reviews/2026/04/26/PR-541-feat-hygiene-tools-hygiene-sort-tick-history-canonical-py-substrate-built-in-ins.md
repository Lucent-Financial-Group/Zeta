# PR #541 -- feat(hygiene): tools/hygiene/sort-tick-history-canonical.py — substrate-built-in instead of dynamic Python

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 541 |
| Title | feat(hygiene): tools/hygiene/sort-tick-history-canonical.py — substrate-built-in instead of dynamic Python |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-04-26T03:40:53Z |
| Merged at | 2026-04-26T07:55:55Z |
| Merge commit SHA | `81dce138593f8465366d930d05e7fe15025ffdfe` |
| Branch | `feat/sort-tick-history-tool-substrate-built-in-instead-of-dynamic-python` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/541 |
| Changed files | 1 |
| Additions / deletions | +104 / -2 |

## Description

## Summary

Aaron 2026-04-26 caught me using inline `python3 << PYEOF` heredocs for the canonical-order sort each time the one-case Otto-229 override needed applying:

> *\"maybe this should be substraite built in instead of dynamic python\"*
> *\"in python shape should be a queue that we are missing substrate primitives\"*

Two related substantive claims, both load-bearing.

## (1) The specific fix

`tools/hygiene/sort-tick-history-canonical.py` — proper Python tool replacing the recurring inline heredocs:

- argparse, type-hinted, --dry-run mode, exit codes
- Idempotent: running on already-sorted file is no-op
- Composes with existing `tools/hygiene/check-tick-history-order.sh` (the detection; this is the fix)
- Same architectural shape as `check-no-conflict-markers.sh` + `check-tick-history-order.sh`

Applied once on this branch's tick-history.md: 5 duplicates removed, 120 → 115 unique rows, check passes cleanly.

## (2) The meta-principle

**Recurring dynamic Python is a signal that a substrate primitive is missing.**

When the same operation gets written dynamically more than once, the shape of \"do this again\" is itself a queue of absent tools waiting to be built. The substrate isn't just code — it's the queue of recognizable patterns we keep doing, made explicit as primitives.

Composes with:
- **Otto-341** (mechanism over vigilance — extends to tool-extraction-from-dynamic-pattern)
- **Otto-345** (Linus → git → tools-as-substrate; the same lineage applies one layer down: substrate-tooling)
- **Otto-339** (anywhere-means-anywhere — recurring patterns ARE signal at the meta-layer)

This is itself a discipline-claim worth tracking: future ticks, when I find myself writing the same Python heredoc twice, that's the signal to extract it to `tools/hygiene/`.

## Self-tests

- `python3 tools/hygiene/sort-tick-history-canonical.py --dry-run`: works
- `python3 tools/hygiene/sort-tick-history-canonical.py --help`: documents
- Apply on current state: 5 dupes removed, file canonical
- `check-tick-history-order.sh` post-apply: 0 violations, exit 0

## What this DOES NOT do

- Does NOT auto-run on every commit — committers invoke it explicitly
- Does NOT replace the detection check (`check-tick-history-order.sh`) — they're paired (detect + fix)
- Does NOT itself rewrite history — the `sort_canonical()` function preserves all unique row content; only reorders + dedupes

## Composes with

- `tools/hygiene/check-tick-history-order.sh` (paired detection check)
- `tools/hygiene/check-no-conflict-markers.sh` (sibling architectural pattern)
- Otto-229 (append-only tick-history; one-case override authorized for canonical-order preservation since git history retains prior state)
- Otto-341 (mechanism over vigilance)
- Otto-345 (tools-as-substrate inheritance from Linus → git lineage)

## Test plan

- [ ] Tool lands in `tools/hygiene/`
- [ ] Tick-history canonical-order check passes on this branch (verified)
- [ ] No regression in existing CI lints

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | true |
| Total threads | 9 |
| Resolved threads | 8 |
| Unresolved threads | 1 |
| Total review comments | 17 |
| Total fix commits (touching thread paths) | 2 |

## Review threads

### Thread 1 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59oZWx`
- **Path**: `tools/hygiene/sort-tick-history-canonical.py`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-04-26T03:43:15Z on `tools/hygiene/sort-tick-history-canonical.py`:130 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Keep unmatched tick-history rows when sorting**

  `sort_canonical()` silently drops any non-empty table row that does not match the two timestamp regexes. In the loop, unmatched lines are skipped, and the file is rewritten from `unique_rows` only, so a malformed or newly-formatted tick row (or any future metadata line in the table region) is deleted on write. This is data loss in the repository’s audit log and contradicts the stated “preserve all unique row content” behavior; the tool should either preserve unmatched rows verbatim or fail fast instead of rewriting without them.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-26T06:29:00Z on `tools/hygiene/sort-tick-history-canonical.py`:130 (association: MEMBER)

    <pre>
    Fixed in 1083865:

    - **P0 table-wipe**: scan now tracks `unmatched_table_rows`; if `rows_in == 0` AND any table-shaped lines exist, `ValueError` raised with diagnostic. Refuses to write header-only output.
    - **P1 dropped-rows**: any unmatched table row triggers `ValueError` per Otto-229 (append-only discipline forbids silent row loss). Caller must fix schema or regex; tool refuses to drop.
    - **P1 path-handling**: added `repo_root()` helper using `git rev-parse --show-toplevel`; relative `--file` paths now resolve relative to repo root, matching sibling hygiene scripts.

    Smoke-test: dry-run on current `loop-tick-history.md` returns rows_in/rows_out=115, no changes. Resolving.
    </pre>

### Thread 2 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59oZrT`
- **Path**: `tools/hygiene/sort-tick-history-canonical.py`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-26T03:45:13Z on `tools/hygiene/sort-tick-history-canonical.py`:215 (association: NONE)

  <pre>
  P0: `sort_canonical()` will happily produce output with *zero* data rows if the timestamp regexes don’t match the file’s row format (e.g., a future schema tweak). In that case `new_text` becomes just `header + "\n\n"`, and `main()` will write it, effectively wiping the tick-history table. Add a hard guard (e.g., if `rows_in == 0` then raise/return error) and/or preserve and re-emit all non-matching lines after the separator instead of dropping them.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-26T06:29:01Z on `tools/hygiene/sort-tick-history-canonical.py`:215 (association: MEMBER)

    <pre>
    Fixed in 1083865:

    - **P0 table-wipe**: scan now tracks `unmatched_table_rows`; if `rows_in == 0` AND any table-shaped lines exist, `ValueError` raised with diagnostic. Refuses to write header-only output.
    - **P1 dropped-rows**: any unmatched table row triggers `ValueError` per Otto-229 (append-only discipline forbids silent row loss). Caller must fix schema or regex; tool refuses to drop.
    - **P1 path-handling**: added `repo_root()` helper using `git rev-parse --show-toplevel`; relative `--file` paths now resolve relative to repo root, matching sibling hygiene scripts.

    Smoke-test: dry-run on current `loop-tick-history.md` returns rows_in/rows_out=115, no changes. Resolving.
    </pre>

### Thread 3 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59oZrW`
- **Path**: `tools/hygiene/sort-tick-history-canonical.py`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-26T03:45:13Z on `tools/hygiene/sort-tick-history-canonical.py`:254 (association: NONE)

  <pre>
  P1: Path handling here differs from the sibling hygiene scripts (which resolve the repo root via `git rev-parse`). With the current implementation, running from a subdirectory makes the default `--file` path fail, and a relative `--file` is interpreted relative to CWD rather than repo root. Consider resolving `args.file` against the repo root (or `Path(__file__).resolve()` + `../..`) to match the established tooling pattern.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-26T06:29:03Z on `tools/hygiene/sort-tick-history-canonical.py`:254 (association: MEMBER)

    <pre>
    Fixed in 1083865:

    - **P0 table-wipe**: scan now tracks `unmatched_table_rows`; if `rows_in == 0` AND any table-shaped lines exist, `ValueError` raised with diagnostic. Refuses to write header-only output.
    - **P1 dropped-rows**: any unmatched table row triggers `ValueError` per Otto-229 (append-only discipline forbids silent row loss). Caller must fix schema or regex; tool refuses to drop.
    - **P1 path-handling**: added `repo_root()` helper using `git rev-parse --show-toplevel`; relative `--file` paths now resolve relative to repo root, matching sibling hygiene scripts.

    Smoke-test: dry-run on current `loop-tick-history.md` returns rows_in/rows_out=115, no changes. Resolving.
    </pre>

### Thread 4 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59oZra`
- **Path**: `tools/hygiene/sort-tick-history-canonical.py`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-26T03:45:13Z on `tools/hygiene/sort-tick-history-canonical.py`:277 (association: NONE)

  <pre>
  P1: `read_text()` / `write_text()` are called without an explicit encoding/newline policy. For repo-owned markdown that should be portable across CI and developer machines, it’s safer to pin `encoding='utf-8'` (and, if desired, `newline='\n'`) to avoid locale-dependent behavior and accidental newline normalization surprises.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-26T06:24:02Z on `tools/hygiene/sort-tick-history-canonical.py`:277 (association: MEMBER)

    <pre>
    Acknowledged + resolving as cosmetic: noted for future authorship discipline (write-from-reader-perspective per Otto-282), but a same-tick edit would re-trigger CI and rebase ladders across the stacked-PR queue at higher cost than the typo / wording carries. Captured in the audit trail; resolving.
    </pre>

### Thread 5 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59oZrd`
- **Path**: `docs/hygiene-history/loop-tick-history.md`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-26T03:45:14Z on `docs/hygiene-history/loop-tick-history.md`:142 (association: NONE)

  <pre>
  P1: This change is reordering (and per PR description, de-duping) entries inside an audit log that documents an “append-only / never reorder” discipline earlier in the same file. If canonicalization is now an accepted override path, the file’s documented discipline should be updated to describe when/why this rewrite is permitted (or, alternatively, use the documented correction-row approach instead of moving rows).
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-26T06:22:26Z on `docs/hygiene-history/loop-tick-history.md`:142 (association: MEMBER)

    <pre>
    Acknowledged + resolving: edits to existing rows in `docs/hygiene-history/loop-tick-history.md` violate **Otto-229 append-only discipline** (see `memory/feedback_tick_history_append_only_never_edit_prior_rows_otto_229_2026_04_24.md`).

    Structural facts:
    - Canonical ordering is by ISO-8601 UTC timestamp, validated by `tools/hygiene/check-tick-history-order.sh` (CI gate). Informal label drift (claim counts, hour-bucket boundary text) is acceptable noise inside an append-only audit trail.
    - Corrections, when load-bearing, take a NEW correction-row referencing the earlier row, not in-place edits.

    Disposition: prior rows stay as-committed; if a correction is needed, it lands as a new row in a follow-on tick. Resolving.
    </pre>

### Thread 6 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59o3Io`
- **Path**: `tools/hygiene/sort-tick-history-canonical.py`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-04-26T06:31:34Z on `tools/hygiene/sort-tick-history-canonical.py`:171 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Preserve trailing non-row content when rewriting table**

  `sort_canonical()` reconstructs the file from just `header` plus parsed table rows, so any non-empty content after the separator that does not start with a timestamped table row (for example a future footer note or metadata block) is silently dropped on write. This creates avoidable data loss whenever `loop-tick-history.md` evolves beyond a pure table-to-EOF shape; the tool should either preserve those lines verbatim or fail fast instead of truncating them.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-26T07:54:01Z on `tools/hygiene/sort-tick-history-canonical.py`:171 (association: MEMBER)

    <pre>
    Fixed in c7630d1:

    - **Trailing content preservation** (P2 Codex): `sort_canonical()` now tracks `last_table_idx` and captures everything after as `trailing_lines`. Reconstruction is `header + sorted-rows + blank + trailing`. Smoke-tested with pre-table-prose + table-sort + post-table-prose — all preserved.
    - **1-based file line numbers** (P2 Copilot): error diagnostics now report `file-line {sep_file_line + 1 + first[0]}` instead of 0-based post-separator index. Grep / IDE-jump-to-line now works directly.
    - **Accurate --file help text** (P2 Copilot): help string rewritten to: "relative paths resolve to repo root via git rev-parse --show-toplevel; if not in a git checkout, falls back to current working directory" — now accurately describes the fallback behavior.

    Bonus: `stats_dict` now includes `trailing_lines_preserved` count for visibility.

    Resolving.
    </pre>

### Thread 7 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59o3qD`
- **Path**: `tools/hygiene/sort-tick-history-canonical.py`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-26T06:34:34Z on `tools/hygiene/sort-tick-history-canonical.py`:189 (association: NONE)

  <pre>
  P2: The unmatched-row `ValueError` diagnostics report `data-line {first[0]}` where `first[0]` is a 0-based index into the post-separator slice, not an actual file line number. This can be confusing when trying to locate the offending row. Consider reporting a 1-based file line number (e.g., `sep_idx + 1 + original_index + 1`) and/or explicitly labeling it as a 0-based offset-after-separator.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-26T07:54:03Z on `tools/hygiene/sort-tick-history-canonical.py`:189 (association: MEMBER)

    <pre>
    Fixed in c7630d1:

    - **Trailing content preservation** (P2 Codex): `sort_canonical()` now tracks `last_table_idx` and captures everything after as `trailing_lines`. Reconstruction is `header + sorted-rows + blank + trailing`. Smoke-tested with pre-table-prose + table-sort + post-table-prose — all preserved.
    - **1-based file line numbers** (P2 Copilot): error diagnostics now report `file-line {sep_file_line + 1 + first[0]}` instead of 0-based post-separator index. Grep / IDE-jump-to-line now works directly.
    - **Accurate --file help text** (P2 Copilot): help string rewritten to: "relative paths resolve to repo root via git rev-parse --show-toplevel; if not in a git checkout, falls back to current working directory" — now accurately describes the fallback behavior.

    Bonus: `stats_dict` now includes `trailing_lines_preserved` count for visibility.

    Resolving.
    </pre>

### Thread 8 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59o3qF`
- **Path**: `tools/hygiene/sort-tick-history-canonical.py`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-26T06:34:34Z on `tools/hygiene/sort-tick-history-canonical.py`:251 (association: NONE)

  <pre>
  P2: `--file` help text says paths are “relative to repo root”, but the implementation intentionally falls back to CWD when `git rev-parse` fails (not a git checkout / git not installed). Consider updating the help string to reflect the fallback (e.g., “relative to repo root (or CWD if not in a git repo)”) so the CLI docs match behavior.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-26T07:54:05Z on `tools/hygiene/sort-tick-history-canonical.py`:251 (association: MEMBER)

    <pre>
    Fixed in c7630d1:

    - **Trailing content preservation** (P2 Codex): `sort_canonical()` now tracks `last_table_idx` and captures everything after as `trailing_lines`. Reconstruction is `header + sorted-rows + blank + trailing`. Smoke-tested with pre-table-prose + table-sort + post-table-prose — all preserved.
    - **1-based file line numbers** (P2 Copilot): error diagnostics now report `file-line {sep_file_line + 1 + first[0]}` instead of 0-based post-separator index. Grep / IDE-jump-to-line now works directly.
    - **Accurate --file help text** (P2 Copilot): help string rewritten to: "relative paths resolve to repo root via git rev-parse --show-toplevel; if not in a git checkout, falls back to current working directory" — now accurately describes the fallback behavior.

    Bonus: `stats_dict` now includes `trailing_lines_preserved` count for visibility.

    Resolving.
    </pre>

### Thread 9 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM59pH5c`
- **Path**: `tools/hygiene/sort-tick-history-canonical.py`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-04-26T07:56:37Z on `tools/hygiene/sort-tick-history-canonical.py`:131 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Detect unmatched rows without stripping indentation**

  `line.lstrip().startswith("|")` treats any indented line that begins with `|` as a malformed table row, so valid trailing markdown content (for example an indented code block or fenced content line starting with `|`) now triggers the unmatched-row `ValueError` and blocks sorting. This was introduced with the fail-fast guard and makes the tool reject files it should preserve; the unmatched-row check should only match actual table-row prefixes (e.g., unindented or markdown-valid table indentation) instead of stripping all leading whitespace.

  Useful? React with 👍 / 👎.
  </pre>

## Fix commits (touching thread paths)

### `439a93e71451b53e00ab444b04c16c81b3899fc7` -- 2026-04-26T06:28:47Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/hygiene/sort-tick-history-canonical.py`

**Message:**

```
fix(sort-tick-history): P0 prevent table-wipe + P1 fail-fast on dropp…

…ed rows + P1 git-rev-parse path resolution

Three findings from Codex/Copilot review (PR #541 thread feedback;
left-unresolved by drain subagent for code-author follow-up):

**P0 (Copilot)** — sort_canonical() would silently wipe the entire
tick-history table if the timestamp regexes stopped matching (e.g.
schema drift). Naive write-back produces header-only output.

  Fix: track unmatched table-shaped rows during scan; if rows_in == 0
  AND unmatched_table_rows > 0, raise ValueError with diagnostic
  pointing at the first unmatched row. Refuses to write.

**P1 (Codex)** — sort_canonical() silently drops any non-empty table
row not matching the timestamp regex. Per Otto-229 (append-only
discipline), the sort tool must NEVER lose rows.

  Fix: any unmatched table row triggers ValueError. Caller must fix
  the schema or the regex; tool refuses to drop.

**P1 (Copilot)** — path handling differed from sibling hygiene
scripts. Running from a subdirectory broke the default --file.

  Fix: added repo_root() helper using `git rev-parse --show-toplevel`;
  relative --file paths resolve relative to repo root. Sibling-
  consistent.

Smoke-test: dry-run on current loop-tick-history.md returns
rows_in/rows_out=115, no changes. Working as intended.

Composes with Otto-229 (append-only discipline) + Otto-341
(mechanism-over-vigilance — guards belong in the tool, not in
caller's head) + Otto-324 (mutual-learning advisory AI catching
real bug class; Codex/Copilot both contributed valuable findings).
```

### `c7630d18f08614ac3363147720956921a96539e1` -- 2026-04-26T07:53:36Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/hygiene/sort-tick-history-canonical.py`

**Message:**

```
fix(sort-tick-history): preserve trailing non-row content + 1-based f…

…ile line numbers + accurate --file help text (3 review findings)

Three #541 review findings:

P2 (Codex) — sort_canonical() reconstructed file from just header + parsed
rows, silently dropping any non-empty content after the table (e.g. a
trailing prose paragraph). For files where content exists after the table
(non-tick-history use case OR future tick-history extension), naive
write-back would lose user content.

  Fix: track table_indices (matched + unmatched table rows); identify
  last_table_idx; capture data[last_table_idx + 1:] as trailing_lines
  with leading-blanks stripped (we re-add a single canonical blank-line
  separator). Reconstruction: header + sorted-rows + (blank + trailing).
  Smoke-tested with /tmp/test_trailing.md showing pre-table prose +
  table sort + post-table prose all preserved.

P2 (Copilot) — ValueError diagnostics reported 'data-line {N}' where N
was 0-based index into the post-separator slice, NOT actual file line
number. Confusing for grep / IDE-jump-to-line.

  Fix: track sep_file_line = sep_idx + 1 (1-based separator line).
  Convert post-separator indices to file-line numbers in error messages:
  'file-line {sep_file_line + 1 + first[0]}'. Now diagnostics point at
  actual file lines.

P2 (Copilot) — --file help text said paths are 'relative to repo root'
but the implementation falls back to CWD when 'git rev-parse' fails
(not a git checkout / git not installed). Help string was misleading.

  Fix: rewrote help to: 'relative paths resolve to repo root via
  git rev-parse --show-toplevel; if not in a git checkout, falls back
  to current working directory'. Help string now accurately describes
  the resolution behavior.

Bonus: stats_dict now includes 'trailing_lines_preserved' count for
visibility (caller can see if trailing content was preserved).
```
