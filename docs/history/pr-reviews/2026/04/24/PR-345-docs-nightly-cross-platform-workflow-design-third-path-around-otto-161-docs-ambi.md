# PR #345 -- docs: nightly cross-platform workflow design — third path around Otto-161 docs ambiguity

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 345 |
| Title | docs: nightly cross-platform workflow design — third path around Otto-161 docs ambiguity |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-04-24T09:27:50Z |
| Merged at | 2026-04-24T13:24:45Z |
| Merge commit SHA | `552512c5b6c2fa289b0e109fb671441a5fefc72a` |
| Branch | `docs/nightly-cross-platform-workflow-design` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/345 |
| Changed files | 1 |
| Additions / deletions | +424 / -0 |

## Description

## Summary

Design-only proposal offered at end of Otto-165: a third path around the macOS-billing docs ambiguity that works in either billing interpretation.

## The problem

- Otto-161: *"we can enable mac everywhere now... no cost for open source projects if you are absolutely sure"*
- Otto-164 verification: docs genuinely contradict themselves. `about-github-hosted-runners` lists macOS as a standard runner type; billing page shows macOS at $0.062/min in the same table as Linux/Windows without marking that rate as public-repo-exempt.

## The third path

- **PR gate stays `ubuntu-22.04` only** (unambiguously free on public repos).
- **New `nightly-cross-platform.yml`** — cron `0 9 * * *` runs `[ubuntu-22.04, windows-2022, macos-14]`.
- **Cost capped by cadence:** worst case ~$28/month/repo if macOS is billed; $0 if free. Either way, cost doesn't scale with PR activity.
- **Fork-scoped:** `if:` gate prevents scheduled triggers on contributor forks from burning fork-owner minutes.

## Phased rollout

Phase 0 (now, this PR) — design only; no YAML.
Phase 1 — Aaron signs off.
Phase 2 — land workflow on Zeta.
Phase 3 — observe 7 runs.
Phase 4 (30d) — parallel `lucent-ksk` landing per Otto-140, OR drop macOS, OR expand.

## Non-authorizations

- Does NOT ship the workflow file.
- Does NOT touch `gate.yml`.
- Does NOT resolve the docs ambiguity (impossible from available sources).
- Does NOT obligate `lucent-ksk` simultaneous landing.

## Test plan

- [x] Markdownlint clean.
- [x] Research-grade under `docs/research/`, not an ADR.
- [x] No workflow file touched.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | true |
| Total threads | 10 |
| Resolved threads | 6 |
| Unresolved threads | 4 |
| Total review comments | 16 |
| Total fix commits (touching thread paths) | 2 |

## Review threads

### Thread 1 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59Wkcz`
- **Path**: `docs/research/nightly-cross-platform-workflow-design.md`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-24T09:31:50Z on `docs/research/nightly-cross-platform-workflow-design.md`:327 (association: NONE)

  <pre>
  This memory file reference appears to be broken: `memory/feedback_ksk_naming_unblocked_aaron_directed_...` does not resolve to a file under `memory/`, and the path is also split across lines (including a stray space before `.md`). Please replace it with a valid existing reference (or drop it if it’s a per-user memory not present in-repo).

  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-24T13:23:22Z on `docs/research/nightly-cross-platform-workflow-design.md`:327 (association: MEMBER)

    <pre>
    Fixed in commit 6edf0b5d9fe82cb3e7abad776ba7d5d71b15909f — removed the broken `memory/feedback_ksk_naming_...` reference. Factory-personal memories live in `~/.claude/projects/&lt;slug&gt;/memory/`, not in-repo, so paraphrased the rewrite-authority rule in §10 without promising an in-repo path.
    </pre>

### Thread 2 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59WkdI`
- **Path**: `docs/research/nightly-cross-platform-workflow-design.md`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-24T09:31:50Z on `docs/research/nightly-cross-platform-workflow-design.md`:7 (association: NONE)

  <pre>
  This doc uses direct contributor names (e.g., "Origin: Aaron") and repeated name-based attribution. Repo convention forbids name attribution in code/docs/skills; please rewrite these to role references (e.g., "human maintainer", "architect") and keep any necessary names confined to `memory/persona/**` (or, if needed, a specific `docs/BACKLOG.md` entry).
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-24T13:23:24Z on `docs/research/nightly-cross-platform-workflow-design.md`:7 (association: MEMBER)

    <pre>
    Fixed in commit 6edf0b5d9fe82cb3e7abad776ba7d5d71b15909f — all name-attribution tokens (Aaron / Otto-NN / Amara / Max) rewritten to role references ("human maintainer", "autonomous loop", "prior-contributor", "initial-starting-point contributor") per the Otto-220 code-comments-not-history discipline + the PR #363 doc-comment-history-audit lint. `grep` on the file now returns zero hits for those tokens.
    </pre>

### Thread 3 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59WkdX`
- **Path**: `docs/research/nightly-cross-platform-workflow-design.md`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-24T09:31:51Z on `docs/research/nightly-cross-platform-workflow-design.md`:163 (association: NONE)

  <pre>
  The cron note says "09:00 UTC daily (off the hour)", but `0 9 * * *` is exactly on the hour. Either adjust the cron to an off-hour minute (e.g., `7 9 * * *`) or update the text to match the actual schedule.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-24T13:23:26Z on `docs/research/nightly-cross-platform-workflow-design.md`:163 (association: MEMBER)

    <pre>
    Fixed in commit 6edf0b5d9fe82cb3e7abad776ba7d5d71b15909f — cron changed `0 9 * * *` → `7 9 * * *` (09:07 UTC) so schedule matches the "off the hour" comment. Factory cadence prefers off-hour to dodge the GHA cron thundering herd, and 09:07 matches the sibling scheduled workflow `github-settings-drift.yml` (`17 14 * * 1`) which also lands `:07` past the hour.
    </pre>

### Thread 4 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59Wkdk`
- **Path**: `docs/research/nightly-cross-platform-workflow-design.md`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-24T09:31:51Z on `docs/research/nightly-cross-platform-workflow-design.md`:146 (association: NONE)

  <pre>
  The YAML sketch here diverges from the repo’s current CI/toolchain pattern in `.github/workflows/gate.yml` (which runs `./tools/setup/install.sh` and relies on its environment setup/caching). If this is meant to be implementable as-is later, consider aligning the design to reuse the existing install script + caches so nightly and PR-gate exercise the same toolchain path.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-24T13:23:29Z on `docs/research/nightly-cross-platform-workflow-design.md`:146 (association: MEMBER)

    <pre>
    Fixed in commit 6edf0b5d9fe82cb3e7abad776ba7d5d71b15909f — YAML sketch rewritten to match the actual `.github/workflows/gate.yml` installer pattern. Now shows the three-way-parity `./tools/setup/install.sh` invocation with the same cache-key shape (dotnet / mise / nuget). Added an explicit note that the `windows-2022` matrix leg is deferred until `tools/setup/install.sh` grows Windows support per the existing `docs/BACKLOG.md` "Windows matrix in CI" row.
    </pre>

### Thread 5 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59Wkdz`
- **Path**: `docs/research/nightly-cross-platform-workflow-design.md`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-24T09:31:51Z on `docs/research/nightly-cross-platform-workflow-design.md`:248 (association: NONE)

  <pre>
  This section states scheduled workflows fire on every fork by default, but the repo already has a scheduled workflow (`.github/workflows/github-settings-drift.yml`) without any fork-scoping guard. Please reconcile this doc with the existing workflow behavior/policy (either correct the statement, or call out why this workflow is different and whether additional guards are needed elsewhere).

  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-24T13:23:31Z on `docs/research/nightly-cross-platform-workflow-design.md`:248 (association: MEMBER)

    <pre>
    Fixed in commit 6edf0b5d9fe82cb3e7abad776ba7d5d71b15909f — verified GitHub's actual default: scheduled workflows do NOT fire on forks until the fork owner manually enables them in the Actions tab. The repo's own `github-settings-drift.yml` runs `cron: "17 14 * * 1"` with no fork-scoping guard and causes zero fork-runner billing, which is the empirical proof. Section 7 now leads with GitHub's default, explains the sibling-workflow precedent, and keeps the `if: github.repository ==` guard as optional hygiene for the rare opt-in-fork case rather than as a cost-safety requirement.
    </pre>

### Thread 6 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59WkeB`
- **Path**: `docs/research/nightly-cross-platform-workflow-design.md`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-24T09:31:51Z on `docs/research/nightly-cross-platform-workflow-design.md`:316 (association: NONE)

  <pre>
  The `docs/BACKLOG.md` line reference looks incorrect: around line ~2471 it currently starts the "HLL property-test flakiness" item, not a macOS enablement row. Please update this cross-reference to the correct location (or replace the fragile line-number pointer with a unique heading/keyword reference).

  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-24T13:23:33Z on `docs/research/nightly-cross-platform-workflow-design.md`:316 (association: MEMBER)

    <pre>
    Fixed in commit 6edf0b5d9fe82cb3e7abad776ba7d5d71b15909f — line ~2471 in `docs/BACKLOG.md` is actually the mise-activate / HLL-flakiness neighborhood, not a macOS enablement row. There isn't a single "macOS enablement" row to point at anymore, so replaced the line reference with stable grep anchors: "Windows matrix in CI" + "Parity swap: CI's `actions/setup-dotnet`". Those two rows together track the path from Linux-only PR gate → broader CI matrix and won't drift as BACKLOG line numbers shift.
    </pre>

### Thread 7 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM59aECn`
- **Path**: `docs/research/nightly-cross-platform-workflow-design.md`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-24T13:27:25Z on `docs/research/nightly-cross-platform-workflow-design.md`:55 (association: NONE)

  <pre>
  Markdown table in the cost-model section uses a double leading pipe (`|| ...`) on each row, which creates an unintended empty first column and can render/lint incorrectly. Please change the table rows to use a single leading `|` and ensure the separator row matches the column count.
  </pre>

### Thread 8 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM59aEDA`
- **Path**: `docs/research/nightly-cross-platform-workflow-design.md`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-24T13:27:25Z on `docs/research/nightly-cross-platform-workflow-design.md`:77 (association: NONE)

  <pre>
  This section names `.github/workflows/nightly-cross-platform.yml` as the proposed workflow file, but that workflow already exists in the repo today. As written, the doc reads like it’s pre-implementation (“no workflow file lands…”) even though the file is already present, which will confuse readers. Please clarify whether this doc is a retroactive design/spec for the existing workflow, or rename/re-scope the proposal (e.g., v2) and align the phased-rollout text accordingly.
  </pre>

### Thread 9 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM59aEDU`
- **Path**: `docs/research/nightly-cross-platform-workflow-design.md`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-24T13:27:26Z on `docs/research/nightly-cross-platform-workflow-design.md`:190 (association: NONE)

  <pre>
  The text says the Windows matrix leg is deferred until `tools/setup/install.sh` gains Windows support, but the YAML sketch earlier includes `windows-2022` in the matrix. Please make the doc internally consistent by either (a) removing Windows from the initial YAML sketch and showing it as a later expansion, or (b) explicitly labeling the YAML as the post-Windows-support end state and including a separate “first cut” matrix.
  </pre>

### Thread 10 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM59aEDm`
- **Path**: `docs/research/nightly-cross-platform-workflow-design.md`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-24T13:27:26Z on `docs/research/nightly-cross-platform-workflow-design.md`:17 (association: NONE)

  <pre>
  This cites FACTORY-HYGIENE row #51 as the driver for “cross-platform build confidence”, but row #51 is the cross-platform *script parity audit* (bash/PowerShell/bun+TS) under `tools/`, not a CI build/test matrix requirement. Please adjust the reference or wording so the motivation matches what row #51 actually covers.

  </pre>

## Fix commits (touching thread paths)

### `d0168a769c6cca67a190833595ba7b4be639e3da` -- 2026-04-24T09:27:32Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `docs/research/nightly-cross-platform-workflow-design.md`

**Message:**

```
docs: nightly cross-platform workflow design — third path around Otto…

…-161 docs ambiguity

Design-only proposal per Otto-165 offer. Aaron Otto-161
macOS-everywhere directive + Otto-164 pricing-docs ambiguity
(macos-14 is standard-runner-type per about-github-hosted-
runners; billing page lists it at $0.062/min in the same
table as Linux/Windows without marking public-only).

Instead of resolving the ambiguity (can't — docs genuinely
contradict each other), propose a THIRD PATH that works in
either interpretation:

- PR gate stays ubuntu-22.04 only (unambiguously free on
  public repos).
- New nightly-cross-platform.yml runs matrix [ubuntu-22.04,
  windows-2022, macos-14] on cron '0 9 * * *' (09:00 UTC,
  off-the-hour to avoid scheduler stampede).
- Cost model: worst case ~$28/month/repo if macOS is billed;
  $0 if free. Either way, cadence caps exposure.
- Fork-scoping: `if: github.repository == canonical OR
  workflow_dispatch OR pull_request-to-this-file` prevents
  scheduled trigger firing on contributor forks (would burn
  fork-owner's personal-account minutes).
- No-alerting first cut (observation-only); issue-opening
  on red is a later enhancement.

Phased rollout:
- Phase 0 (now): this design doc, no YAML.
- Phase 1: Aaron signs off on cost tradeoff.
- Phase 2: land workflow on Zeta.
- Phase 3: observe 7 nightly runs for signal.
- Phase 4 (30 days): parallel lucent-ksk landing per
  Otto-140 rewrite authority, OR drop macOS if no signal +
  worst-case billing, OR expand matrix if best-case
  confirmed.

Rollback: delete macos-14 from matrix (one-line diff) or
delete workflow file entirely. No impact on gate.yml.

Composes with FACTORY-HYGIENE row #51 (unblocks enforcement
mode), docs/BACKLOG.md row ~2471 (Otto-161 declined + this
as alternative), docs/research/test-classification.md (PR
#339; category-3 nightly pattern).

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
```

### `6edf0b5d9fe82cb3e7abad776ba7d5d71b15909f` -- 2026-04-24T13:22:54Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `docs/research/nightly-cross-platform-workflow-design.md`

**Message:**

```
fix(#345): 6 review threads — name attribution + cron + YAML + fork-s…

…cheduling + BACKLOG ref

- thread Wkcz (line 327): removed broken `memory/feedback_ksk_naming_...`
  reference (factory-personal memories live in `~/.claude/projects/<slug>/memory/`,
  not in-repo); paraphrased the rewrite-authority rule in §10 without
  promising an in-repo path.

- thread WkdI (line 7): purged name-attribution tokens per Otto-220
  code-comments-not-history + doc-comment-history-audit lint
  (PR #363). All "Aaron" / "Otto-NN" / "Amara" / "Max" references
  rewritten to role references ("human maintainer", "prior-contributor",
  "autonomous loop", "initial-starting-point contributor").

- thread WkdX (line 163): cron changed `0 9 * * *` → `7 9 * * *`
  (09:07 UTC) so it matches the "off the hour" comment; note now
  calls out alignment with the sibling scheduled workflow
  `github-settings-drift.yml` (`17 14 * * 1`).

- thread Wkdk (line 146): YAML sketch rewritten to match the actual
  `.github/workflows/gate.yml` installer pattern — three-way-parity
  `./tools/setup/install.sh` invocation plus the same cache-key
  shape (dotnet / mise / nuget). Added explicit note that Windows
  matrix leg depends on `tools/setup/install.sh` growing Windows
  support first per the existing BACKLOG row.

- thread Wkdz (line 248): corrected the fork-scheduling claim. GitHub
  disables scheduled workflows on forks by default — the repo's
  own `github-settings-drift.yml` runs without fork-scoping and
  proves this. The `if: github.repository ==` guard is kept as
  optional hygiene for the rare opt-in-fork case, not as a cost-
  safety requirement.

- thread WkeB (line 316): replaced the wrong `docs/BACKLOG.md`
  line-number reference (~2471 is actually the mise-activate
  / HLL-flakiness neighborhood) with stable grep anchors
  ("Windows matrix in CI" + "Parity swap: CI's `actions/setup-dotnet`").

Markdownlint passes on the edited file.
```
