# PR #376 -- ci: .NET 10 Server GC crash workaround on Apple Silicon (Otto-248)

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 376 |
| Title | ci: .NET 10 Server GC crash workaround on Apple Silicon (Otto-248) |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-04-24T17:51:35Z |
| Merged at | 2026-04-24T18:57:54Z |
| Merge commit SHA | `9c1c0e3620a444a2713d380d5836828750c79799` |
| Branch | `ci/dotnet-server-gc-workaround-apple-silicon-otto-248` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/376 |
| Changed files | 3 |
| Additions / deletions | +207 / -10 |

## Description

## Summary

Three `dotnet` SIGSEGV crashes captured 2026-04-24 during `dotnet build -c Release` on Apple Silicon macOS all landed in Server GC concurrent-mark-phase with `EXC_BAD_ACCESS` / ARM64 PAC memory-corruption detection. Upstream .NET 10 GC bug; not Zeta code. This PR:

1. **Workaround**: `tools/setup/common/shellenv.sh` exports `DOTNET_gcServer=0` only on Apple Silicon Darwin → Workstation GC avoids the crash.
2. **Discipline**: Otto-248 memory — "never ignore flakes per DST."

## Crash evidence

| Time | Stack top | Phase |
|---|---|---|
| 13:31:13 | `SVR::gc_heap::background_mark_simple1` | concurrent mark |
| 13:35:36 | `SVR::gc_heap::find_first_object` | revisit-written-page |
| 13:43:09 | `SVR::gc_heap::plan_phase` | plan phase |

All three: `EXC_BAD_ACCESS` / `KERN_INVALID_ADDRESS` / "possible pointer authentication failure" on macOS 26.4.1 (MacBook Air M2) + .NET SDK 10.0.203 (latest, April 21 2026).

IPS files at `~/Library/Logs/DiagnosticReports/dotnet-2026-04-24-*.ips`.

## Scope

- **Apple Silicon Darwin**: workaround applies, Workstation GC
- **Linux** (CI `ubuntu-24.04`, `ubuntu-24.04-arm`, `ubuntu-slim`): no change — no bug there
- **Intel macOS**: no change — no bug there
- **Windows**: no change — no bug there

Slight build-time perf cost on Apple Silicon (single-threaded GC) but stability gain outweighs.

## Discipline landed

Otto-248 memory rule (`memory/feedback_never_ignore_flakes_per_DST_discipline_flakes_mean_determinism_not_perfect_otto_248_2026_04_24.md`):

**Flakes are determinism violations. Fix them, don't retry.**

Applies to: test flakes, build-tool flakes, CI flakes, output non-determinism. NOT to genuinely-external transient systems (GitHub API rate limits, remote 5xx — those retry correctly with backoff).

Aaron quote: *"never ignore flakes per DST they must be fixed, flakes just mean that your DST isnt perfect."*

## Test plan

- [x] Local `DOTNET_gcServer=0 dotnet build -c Release` → 0W/0E
- [x] Local `dotnet build -c Release` (workaround active via shellenv) → 0W/0E
- [x] shellenv.sh regenerates idempotently
- [ ] CI runs exercise the workaround on `macos-26` matrix leg (needs #375 to merge first — both are active PRs)
- [ ] Multi-run stability check: ~10 fresh-shell `dotnet build` invocations on Apple Silicon without crash

## Follow-up

- **Upstream issue** at `dotnet/runtime` with the 3 IPS files — deferred pending maintainer preference on handle/attribution
- **Monitor** `.NET 10.0.204+` release notes for GC fix
- **Remove workaround** once upstream ships; tracked in Otto-248 memory

## Composes with

- **Otto-247 version-currency** (used to verify .NET SDK 10.0.203 is latest)
- **Otto-248 never-ignore-flakes** (this PR lands the rule)
- **CLAUDE.md verify-before-deferring** (read crash dumps before assuming)
- **three-way-parity** GOVERNANCE §24 — workaround flows to dev laptop + CI + devcontainer via the same shellenv.sh

## References

- Upstream class: [dotnet/runtime#63070](https://github.com/dotnet/runtime/issues/63070) — Server GC crashes when enabled
- Upstream class: [dotnet/runtime#122608](https://github.com/dotnet/runtime/issues/122608) — .NET 10 ARM64 intermittent on M4
- [.NET 10.0.203 release notes](https://github.com/dotnet/core/blob/main/release-notes/10.0/10.0.7/10.0.7.md)

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | true |
| Total threads | 6 |
| Resolved threads | 3 |
| Unresolved threads | 3 |
| Total review comments | 9 |
| Total fix commits (touching thread paths) | 3 |

## Review threads

### Thread 1 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59eNUw`
- **Path**: `tools/setup/common/shellenv.sh`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-24T17:54:32Z on `tools/setup/common/shellenv.sh`:45 (association: NONE)

  <pre>
  P1: The comment says this applies to “Apple Silicon Darwin”, but the emitted condition is based on process architecture (`uname -m`), which reports `x86_64` under Rosetta even on Apple Silicon hardware. Either adjust the guard to detect Apple Silicon hardware (not the current process arch), or update the comment to match the actual condition (Darwin + arm64 process) so future debugging doesn’t chase the wrong scope.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-24T18:54:50Z on `tools/setup/common/shellenv.sh`:45 (association: MEMBER)

    <pre>
    Fixed. Replaced `uname -m` (process arch; reports `x86_64` under Rosetta 2 on Apple Silicon hardware) with `sysctl -n hw.optional.arm64` (hardware probe; returns `"1"` on Apple Silicon regardless of translation state). Guard is now `Darwin &amp;&amp; hw.optional.arm64 == 1` so a Rosetta-translated shell on M-series hardware still gets the workaround. Fallback `2&gt;/dev/null || echo 0` keeps it safe where `hw.optional.arm64` sysctl does not exist. Inline comment now documents hardware-vs-process distinction. See `docs/pr-preservation/376-drain-log.md` thread 1.
    </pre>

### Thread 2 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59eNVH`
- **Path**: `memory/feedback_never_ignore_flakes_per_DST_discipline_flakes_mean_determinism_not_perfect_otto_248_2026_04_24.md`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-24T17:54:33Z on `memory/feedback_never_ignore_flakes_per_DST_discipline_flakes_mean_determinism_not_perfect_otto_248_2026_04_24.md`:3 (association: NONE)

  <pre>
  P1: This introduces direct contributor naming (“Aaron …”) in a non-persona memory file. `docs/AGENT-BEST-PRACTICES.md` explicitly says direct names should appear only under `memory/persona/&lt;name&gt;/` (and optionally `docs/BACKLOG.md`), with role-refs elsewhere. Please reword to role references (e.g., “human maintainer”) and/or relocate any name-attribution details into the appropriate `memory/persona/**` path.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-24T18:54:58Z on `memory/feedback_never_ignore_flakes_per_DST_discipline_flakes_mean_determinism_not_perfect_otto_248_2026_04_24.md`:3 (association: MEMBER)

    <pre>
    Fixed. Narrative prose now uses "the human maintainer" / "human-maintainer" per AGENT-BEST-PRACTICES operational standing rule. Verbatim quote block preserved as-is — scrubbing the speaker of a speech act would damage training signal (Otto-250). Frontmatter `name:` + `description:` now open with "human maintainer" rather than direct name. See `docs/pr-preservation/376-drain-log.md` thread 2.
    </pre>

### Thread 3 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59eNVV`
- **Path**: `memory/MEMORY.md`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-24T17:54:33Z on `memory/MEMORY.md`:5 (association: NONE)

  <pre>
  P1: This new index entry includes direct contributor naming (“Aaron …”). Per `docs/AGENT-BEST-PRACTICES.md` (Operational standing rules), direct names should be confined to `memory/persona/&lt;name&gt;/` (and optionally `docs/BACKLOG.md`); consider rewriting this entry to role references and linking to a persona memory file if attribution is needed.
  ```suggestion
  - [**NEVER ignore flakes per DST discipline — Deterministic Simulation Testing says every run with same inputs → same outputs; a "flake" is NOT transient, it's a determinism violation to diagnose + fix; retry-and-succeed is MASKING; trigger: dotnet 10 F# compiler SIGSEGV (exit 139) "flakes" on Apple Silicon ARM64 macOS = real .NET 10 Server GC bug (EXC_BAD_ACCESS in `SVR::gc_heap::{plan_phase,background_mark_phase,find_first_object}` with ARM64 PAC failure); retry pattern I/subagents used was discipline failure; 3 crash reports captured 2026-04-24 (`~/Library/Logs/DiagnosticReports/dotnet-2026-04-24-*.ips`); human maintainer Otto-248 "never ignore flakes per DST they must be fixed, flakes just mean that your DST isnt perfect"; 2026-04-24**](feedback_never_ignore_flakes_per_DST_discipline_flakes_mean_determinism_not_perfect_otto_248_2026_04_24.md) — Concrete fix pipeline: (1) capture reproduction state, (2) read crash dump/core/IPS, (3) check upstream known-issues, (4) propose mitigation (upstream report, pinned SDK, workaround env var, etc), (5) regression-guard. This session's application: shellenv.sh workaround sets `DOTNET_gcServer=0` on Apple Silicon Darwin only — Workstation GC is stable; small build-time perf cost; removes once upstream fixes. Scope distinguishes: in-stack flakes (our code / tools) = fix; external-system flakes (GitHub API rate limits, 5xx) = retry-with-backoff is correct. Applies to test flakes, build-tool flakes, CI flakes, output-nondeterminism. Composes with `deterministic-simulation-theory-expert` capability skill + Otto-247 verify-first + CLAUDE.md verify-before-deferring.
  ```
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-24T18:55:02Z on `memory/MEMORY.md`:5 (association: MEMBER)

    <pre>
    After rebase onto main, `memory/MEMORY.md` no longer appears in this PR's diff — the Otto-248 index row was absorbed into main via commit 813c802 "memory: sync Otto-247..252" (a broader 6-memory sync), so the append collision collapsed to empty-diff under `-X ours` rebase strategy. The role-ref scrub of `Aaron` → role-ref on main's copy belongs to the separately-tracked Otto-241 bulk scrub (~900 files, dedicated PR). Resolving here since this PR no longer touches `MEMORY.md`. See `docs/pr-preservation/376-drain-log.md` thread 3.
    </pre>

### Thread 4 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM59fDTv`
- **Path**: `memory/feedback_never_ignore_flakes_per_DST_discipline_flakes_mean_determinism_not_perfect_otto_248_2026_04_24.md`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-24T18:58:15Z on `memory/feedback_never_ignore_flakes_per_DST_discipline_flakes_mean_determinism_not_perfect_otto_248_2026_04_24.md`:5 (association: NONE)

  <pre>
  P1: This new memory file reintroduces `originSessionId:` in the YAML frontmatter, but the repo has an explicit “effective immediately” discipline to stop writing session IDs into factory files (see `memory/feedback_session_id_out_of_factory_files_peer_claude_parity_test_worktree_launch_otto_241_2026_04_24.md`). Please remove the `originSessionId:` field from this file’s frontmatter.
  ```suggestion

  ```
  </pre>

### Thread 5 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM59fDUd`
- **Path**: `docs/pr-preservation/376-drain-log.md`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-24T18:58:15Z on `docs/pr-preservation/376-drain-log.md`:17 (association: NONE)

  <pre>
  P1: This doc uses direct contributor naming (e.g., “Aaron”, “Otto”, and handles like “AceHack”) in narrative prose. Repo convention is to avoid direct names in docs and use role references instead (see `docs/AGENT-BEST-PRACTICES.md` §Operational standing rules). Please rewrite these references to role-refs (e.g., “human maintainer”, “drain agent”, “PR author/reviewer”) or relocate this document under an explicitly exempt “history” area if it must preserve names verbatim.
  ```suggestion
  # PR #376 drain log — .NET 10 Server GC workaround on Apple Silicon

  PR: &lt;https://github.com/Lucent-Financial-Group/Zeta/pull/376&gt;
  Branch: `ci/dotnet-server-gc-workaround-apple-silicon-otto-248`
  Drain session: 2026-04-24 (drain agent, rebase-only subagent)
  Thread count at drain start: 3 unresolved (all Copilot P1)
  Rebase context: branch was `DIRTY` against `origin/main` (memory/
  MEMORY.md append collision); rebased with `-X ours` per
  append-only-audit-trail discipline. After rebase, MEMORY.md
  diff is empty (the corresponding index row had already been added
  to main via later commit 813c802),
  collapsing branch scope to two files: `tools/setup/common/
  shellenv.sh` + `memory/feedback_never_ignore_flakes_..._otto_
  248_2026_04_24.md`.

  Per the human maintainer's 2026-04-24 PR-comment-preservation
  directive (PR review comments + responses + resolutions are
  ```
  </pre>

### Thread 6 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM59fDUv`
- **Path**: `docs/pr-preservation/376-drain-log.md`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-24T18:58:15Z on `docs/pr-preservation/376-drain-log.md`:14 (association: NONE)

  <pre>
  P2: This states the rebased PR diff “collapsed … to two files”, but this PR also adds this drain-log file itself, so the statement is inaccurate/confusing. Please clarify whether you mean “two substantive change files, plus PR-preservation docs”, or update the count accordingly.
  ```suggestion
  collapsing the substantive branch scope to two files:
  `tools/setup/common/shellenv.sh` and `memory/
  feedback_never_ignore_flakes_..._otto_248_2026_04_24.md`,
  plus this PR-preservation drain log.
  ```
  </pre>

## Fix commits (touching thread paths)

### `5413c6ece363f5099e195cdf4d07d164fec57116` -- 2026-04-24T17:51:05Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/setup/common/shellenv.sh`

**Message:**

```
ci: .NET 10 Server GC crash workaround on Apple Silicon + Otto-248 fl…

…ake-discipline rule

## Problem

Three `dotnet` SIGSEGV crashes captured 2026-04-24 during
`dotnet build -c Release` on Apple Silicon macOS (ARM64,
MacBook Air M2, macOS 26.4.1, .NET SDK 10.0.203). All crashed
in Server GC concurrent-mark-phase:

- 13:31:13 `SVR::gc_heap::background_mark_simple1` → `gc1`
- 13:35:36 `SVR::gc_heap::find_first_object` → `revisit_written_page`
- 13:43:09 `SVR::gc_heap::plan_phase` → `gc1`

All three `EXC_BAD_ACCESS` / `KERN_INVALID_ADDRESS` with
"possible pointer authentication failure" — ARM64 PAC
memory-corruption detection. Matches open dotnet/runtime
issue class for .NET 10 Apple Silicon GC instability.

## Workaround

`tools/setup/common/shellenv.sh` now emits:

  if Darwin + arm64: export DOTNET_gcServer=0

This forces the Workstation GC (stable, slightly slower
under load) on Apple Silicon only. Linux CI, Intel macOS,
Windows see no change — they don't have the bug.

Effect flows into every bash shell sourcing the managed
shellenv file, plus CI via `BASH_ENV` + `GITHUB_ENV` per
the existing three-way-parity pattern (GOVERNANCE §24).

## Discipline

Per Otto-248 (this commit lands the memory): **never ignore
flakes per DST**. Retry-and-succeed was masking a real
runtime bug. The discipline:

1. Capture failure state (crash dump paths, reproduction
   command)
2. Read the dump — don't guess
3. Check upstream known-issues
4. Propose targeted mitigation
5. Track for upstream fix

Aaron's quote: *"never ignore flakes per DST they must be
fixed, flakes just mean that your DST isnt perfect."*

## Verification

- `DOTNET_gcServer=0 dotnet build -c Release` → 0W/0E in 18s
  (one-shot; intermittent crash means one success isn't
  conclusive proof, but the mechanism is well-established)
- Plain `dotnet build -c Release` (with the workaround now
  active via shellenv) → 0W/0E in 1.5s cached
- Workaround is reversible: delete the 5-line block in
  shellenv.sh when upstream fix ships

## Follow-up

- File upstream `dotnet/runtime` issue with the 3 IPS files
  (deferred; want Aaron's say on where to file under which
  handle)
- Monitor `.NET 10.0.204+` release notes for GC fix
- Remove workaround once upstream ships

## Memory

`memory/feedback_never_ignore_flakes_per_DST_discipline_flakes_mean_determinism_not_perfect_otto_248_2026_04_24.md` — full discipline rule with scope, anti-patterns, and composition with DST + Otto-247 + CLAUDE.md verify-before-deferring.

## Referenced

- upstream class: dotnet/runtime#63070 (Server GC crashes when enabled)
- upstream class: dotnet/runtime#122608 (.NET 10 ARM64 intermittent on M4)

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
```

### `b2abe16cf6e82290c9d0a39a014e73ba1a8ab678` -- 2026-04-24T17:55:02Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/setup/common/shellenv.sh`

**Message:**

```
ci: fix shellcheck SC2016 — use escaped double-quotes for emitted block

PR #376 shellcheck failure: the 4 `echo '...'` lines emitting the
Apple-Silicon GC workaround block used single quotes. Shellcheck
SC2016 warns "Expressions don't expand in single quotes" — my
intent was to emit literal `$(uname ...)` into the generated
shellenv.sh, but the pattern conflicted with the linter.

Converted to the existing style (double-quoted with `\$` escapes)
used elsewhere in the file. Emitted output is byte-identical.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
```

### `a9fe976dba6539e11327e88bde94e3964f667f8d` -- 2026-04-24T18:54:38Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `docs/pr-preservation/376-drain-log.md`
- `memory/feedback_never_ignore_flakes_per_DST_discipline_flakes_mean_determinism_not_perfect_otto_248_2026_04_24.md`
- `tools/setup/common/shellenv.sh`

**Message:**

```
ci: address Copilot P1 threads on PR #376 (Rosetta arch detection + r…

…ole-refs)

Drain-only pass on PR #376 after rebase onto main.

- shellenv.sh: swap `uname -m` (process arch; reports x86_64
  under Rosetta 2 on Apple Silicon hardware) for `sysctl -n
  hw.optional.arm64` (hardware probe; returns "1" on Apple
  Silicon regardless of translation state). Guard now
  survives Rosetta-translated shells on M-series hardware.
  Fallback `2>/dev/null || echo 0` for non-Darwin safety.
- memory/feedback_..._otto_248: narrative prose refs swapped
  from direct name "Aaron" to role-ref "the human maintainer"
  per AGENT-BEST-PRACTICES operational standing rule.
  Verbatim quote preserved (speech-act, not prose; scrubbing
  the speaker damages training signal per Otto-250).
- MEMORY.md: obsolete-in-this-PR — rebase `-X ours` collapsed
  the append collision to empty diff (main commit 813c802
  landed the Otto-248 row independently). Role-ref scrub of
  main's copy belongs to the Otto-241 bulk-scrub PR.
- docs/pr-preservation/376-drain-log.md: per-thread record
  with verbatim reviewer text, outcome, verbatim reply,
  resolution (Otto-250 discipline).

Build: `dotnet build -c Release` → 0W/0E on Apple Silicon M2
(the affected hardware; workaround prevents its own SIGSEGV
flake).
```
