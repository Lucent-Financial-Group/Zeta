# PR #298 -- core: phaseLockingValue — 3rd Amara graduation (firefly-sync signature, 11th ferry)

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 298 |
| Title | core: phaseLockingValue — 3rd Amara graduation (firefly-sync signature, 11th ferry) |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-04-24T05:44:52Z |
| Merged at | 2026-04-24T06:22:04Z |
| Merge commit SHA | `44c8f59af2d9ef28f485b4e02816b4de8936f398` |
| Branch | `feat/phase-locking-value-amara-graduation-3` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/298 |
| Changed files | 2 |
| Additions / deletions | +136 / -0 |

## Description

## Summary

Third Amara graduation — extends `TemporalCoordinationDetection` with `phaseLockingValue`, the canonical firefly-synchronization primitive from Aaron's 11th-ferry design (PR #296).

Aaron Otto-105: *"the diffenrencable firefly network with trivial cartel detect was my design i'm very interested in that."* PLV is the classical formalization of that synchronization signature.

## What lands

- `phaseLockingValue : double seq -> double seq -> double option`
- 8 new tests (18 total in the module) — all passing
- Composes with `crossCorrelation` (amplitude-similarity) + `RobustStats.robustAggregate` (outlier-resistant combination)

## Why PLV + cross-correlation together

Cartels that hide amplitude coordination by adding noise may still reveal themselves through preserved phase structure. Two detectors from different angles composed over the same streams; each catches what the other misses.

## Test plan

- [x] 18 tests pass
- [x] Build 0 Warning / 0 Error
- [x] Commutativity invariant
- [x] None on undefined input (empty / mismatched length)
- [x] Perfect locking detected at any constant offset (including anti-phase)
- [x] Uniform phase distribution → ~0

## SPOF (per Otto-106 directive)

Pure function, no external deps, no SPOF introduced. Downstream combinators over many node-pairs should use `RobustStats.robustAggregate` rather than arithmetic mean.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 4 |
| Resolved threads | 4 |
| Unresolved threads | 0 |
| Total review comments | 4 |
| Total fix commits (touching thread paths) | 1 |

## Review threads

### Thread 1 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59ThuT`
- **Path**: `tests/Tests.FSharp/Algebra/TemporalCoordinationDetection.Tests.fs`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-04-24T05:47:00Z on `tests/Tests.FSharp/Algebra/TemporalCoordinationDetection.Tests.fs`:158 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Require a defined PLV before asserting near-zero**

  This test can pass even when `phaseLockingValue` incorrectly returns `None` for valid equal-length input, because `Option.defaultValue -1.0` converts `None` into `-1.0`, which still satisfies `be lessThan 1e-9`. That masks a real regression in detector availability and weakens the guardrail for future changes; the test should fail on `None` and only apply the near-zero threshold to a `Some` value.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 2 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59TiMw`
- **Path**: `src/Core/TemporalCoordinationDetection.fs`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-24T05:47:45Z on `src/Core/TemporalCoordinationDetection.fs`:134 (association: NONE)

  <pre>
  The doc comment references a ferry document path under `docs/aurora/2026-04-24-...-11th-ferry.md`, but that file does not exist in the repository. Please either add the referenced doc or update the path/section to an existing document so the provenance link is not broken.
  </pre>

### Thread 3 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59TiNE`
- **Path**: `src/Core/TemporalCoordinationDetection.fs`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-24T05:47:45Z on `src/Core/TemporalCoordinationDetection.fs`:135 (association: NONE)

  <pre>
  This comment introduces contributor/agent name attribution (e.g., Aaron/Amara/Otto) in code documentation. `docs/AGENT-BEST-PRACTICES.md` specifies using role references in code/docs and keeping names only in `memory/persona/**` (and optionally `docs/BACKLOG.md`). Please rewrite this provenance/attribution text using role refs and/or a neutral source identifier (e.g., a doc title without names).
  </pre>

### Thread 4 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59TiNP`
- **Path**: `tests/Tests.FSharp/Algebra/TemporalCoordinationDetection.Tests.fs`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-24T05:47:45Z on `tests/Tests.FSharp/Algebra/TemporalCoordinationDetection.Tests.fs`:159 (association: NONE)

  <pre>
  This test will still pass if `phaseLockingValue` unexpectedly returns `None`, because `Option.defaultValue -1.0` makes the assertion `-1.0 &lt; 1e-9` true. To ensure the test actually validates the behavior, assert the result is `Some` and then compare the contained value to the near-zero threshold.
  ```suggestion
      let plv = TemporalCoordinationDetection.phaseLockingValue a b
      match plv with
      | Some value -&gt; value |&gt; should (be lessThan) 1e-9
      | None -&gt; Assert.True(false, "Expected phaseLockingValue to return Some for equal-length non-empty inputs.")
  ```
  </pre>

## Fix commits (touching thread paths)

### `e2d5796fddcbf3b9c5fd74162b4cd7428a4d81df` -- 2026-04-24T05:44:38Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `src/Core/TemporalCoordinationDetection.fs`
- `tests/Tests.FSharp/Algebra/TemporalCoordinationDetection.Tests.fs`

**Message:**

```
core: TemporalCoordinationDetection.phaseLockingValue — 3rd Amara gra…

…duation (11th ferry)

Extends the temporal-coordination-detection module with PLV, the
classical firefly-synchronization primitive. Third graduation under
the Otto-105 cadence; composes with crossCorrelation (PR #297) and
RobustStats (PR #295).

Aaron Otto-105 attribution: "the diffenrencable firefly network
with trivial cartel detect was my design i'm very interested in
that." PLV is the canonical formalization of the firefly-
synchronization signature Aaron's design targets.

Surface:
- TemporalCoordinationDetection.phaseLockingValue
    : double seq -> double seq -> double option
  Magnitude of the mean complex phase-difference vector; returns
  [0, 1] where 1 = perfect phase locking (constant offset across
  series) and 0 = uniformly-distributed phase differences.
  Returns None on empty input or mismatched-length pairs
  (undefined; silent truncation would hide caller bugs).

Complementary to crossCorrelation: amplitudes-move-together vs
events-fire-at-matching-phases. Cartels that flatten amplitude
cross-correlation by injecting noise may still reveal themselves
through preserved phase structure. Detectors compose both.

Math:
  PLV = | (1/N) sum( exp(i * (phi_a[k] - phi_b[k])) ) |
      = sqrt( mean(cos(delta))^2 + mean(sin(delta))^2 )

Only depends on phase differences, so any consistent wrapping
convention ([-pi, pi] or [0, 2pi]) works without pre-unwrap.

Tests (8 new, 18 total passing):
- Identical phase series -> 1.0
- Constant offset (pi/4) -> 1.0 (perfect locking regardless of offset)
- Empty series -> None
- Mismatched lengths -> None (surfaces caller bug)
- Anti-phase (pi offset) -> 1.0 (still constant = still locked)
- Uniformly-distributed differences across [0, 2pi) -> ~0 (360 samples)
- Commutativity (swapping args leaves magnitude invariant)
- Single-element degenerate case returns 1.0 without crash

Attribution:
- Concept (phase-locking as firefly-synchronization signature,
  trivial-cartel-detect primary detection) = Aaron's design
- Technical formulation (PLV, complex-phase-vector magnitude)
  = Amara's formalization in 11th ferry
- Implementation = Otto

SPOF consideration (per Otto-106 directive): pure function with
no external dependencies; no SPOF introduced by this ship.
Downstream detectors that combine PLV across many node pairs
should use RobustStats.robustAggregate for outlier-resistant
combination, not arithmetic mean.

Composes with:
- src/Core/RobustStats.fs (PR #295) — 1st graduation
- src/Core/TemporalCoordinationDetection.crossCorrelation (PR #297)
  — 2nd graduation (same module)

Next graduation candidates (feedback memory queue):
- BurstAlignment detector over crossCorrelationProfile
- antiConsensusGate from 10th ferry
- ModularitySpike / EigenvectorCentralityDrift (need graph substrate)

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
```
