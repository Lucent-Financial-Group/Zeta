# PR #213 -- hygiene: git-hotspots audit tool + first-run baseline (FACTORY-HYGIENE #57)

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 213 |
| Title | hygiene: git-hotspots audit tool + first-run baseline (FACTORY-HYGIENE #57) |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-04-23T23:06:08Z |
| Merged at | 2026-04-24T16:44:57Z |
| Merge commit SHA | `9d8c68e43e37c04722db62f16bb9944d2ae454ab` |
| Branch | `hygiene/git-hotspots-audit-tool-plus-first-run` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/213 |
| Changed files | 3 |
| Additions / deletions | +365 / -0 |

## Description

## Summary

Executes Aaron Otto-54's git-hotspots cadence directive from PR #212's BACKLOG row. Closes the gap between "BACKLOG row naming the cadence" and "audit tool + first baseline + FACTORY-HYGIENE row activating it".

## What landed

- `tools/hygiene/audit-git-hotspots.sh` — bash audit tool (detect-only)
- `docs/hygiene-history/git-hotspots-2026-04-23.md` — first-run baseline + Otto observations
- FACTORY-HYGIENE row #57 activating the cadence

## First-run finding

**`docs/BACKLOG.md` is the top hotspot: 34 touches / 26 PRs in 30 days** — effectively one BACKLOG touch per PR opened. Validates Aaron's Otto-54 intuition that split is the highest-leverage move. Other hotspots: `docs/ROUND-HISTORY.md` (freeze-then-watch), `memory/MEMORY.md` (cadence candidate — Otto-54 CURRENT-freshness row), 4 skill files (skill-tune-up audit candidates).

Per-file action recommendations in the report: 1 split / 1 freeze-then-watch / 1 cadence / 5 audit / 11 watch.

## Composition

- Directly closes PR #212 row "Git-hotspots audit on a cadence" (S effort)
- Surfaces concrete data supporting PR #212 row "Split `docs/BACKLOG.md` per-swim-lane" (M effort)
- Surfaces concrete data supporting PR #212 row "CURRENT-`<maintainer>`.md freshness audit" (S effort)
- Excludes `docs/hygiene-history/`, `openspec/changes/`, `references/upstreams/` as legitimately-by-design high-churn

## What this is NOT

- Not an enforcement — audit exits 0 regardless of findings
- Not a blame tool — author counts describe collaboration shape
- Not a complete merge-conflict predictor — append-only-with-coordination files can be hot without conflicting

## Test plan

- [x] Tool runs successfully on current repo (14-day and 30-day windows tested locally)
- [x] Baseline report generated with Otto observations
- [x] FACTORY-HYGIENE row #57 added (row 56 reserved for in-flight PR #204 MD032 preflight)

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | true |
| Total threads | 22 |
| Resolved threads | 19 |
| Unresolved threads | 3 |
| Total review comments | 41 |
| Total fix commits (touching thread paths) | 3 |

## Review threads

### Thread 1 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59QCdM`
- **Path**: `tools/hygiene/audit-git-hotspots.sh`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-04-23T23:08:26Z on `tools/hygiene/audit-git-hotspots.sh`:83 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Handle empty windows without aborting the audit**

  With `set -euo pipefail`, `grep -v '^$'` returns exit code 1 when `git log` produces no file lines (for example during a quiet period or `--window '0 days'`), which makes the whole script exit before rendering anything. That breaks the stated detect-only behavior and can fail scheduled cadence runs exactly when there are simply no recent touches to report.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-23T23:18:01Z on `tools/hygiene/audit-git-hotspots.sh`:83 (association: MEMBER)

    <pre>
    Fixed in 392b6ab. With `set -euo pipefail`, `grep -v '^$'` returns exit 1 on no matches and aborts the pipe. Added `|| true` guard + explicit empty-window message to stderr so the audit degrades gracefully rather than silently crashing. Good catch.
    </pre>

### Thread 2 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59QCdO`
- **Path**: `tools/hygiene/audit-git-hotspots.sh`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-04-23T23:08:26Z on `tools/hygiene/audit-git-hotspots.sh`:102 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Default PR count to zero when no PR tokens are present**

  The PR-count pipeline uses `grep -oE '#[0-9]+'` under `pipefail`; when a file's commit subjects contain no `#123` tokens, `grep` exits 1 and the script terminates instead of reporting `0`. This means the audit can fail on perfectly valid history (e.g., direct commits, imported history, or repos not encoding PR numbers in subjects), again violating detect-only expectations.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-23T23:18:24Z on `tools/hygiene/audit-git-hotspots.sh`:102 (association: MEMBER)

    <pre>
    Fixed in 392b6ab. Replaced `wc -l | tr -d ' '` with `grep -c . || true` so the empty case yields "0" cleanly instead of aborting under pipefail. Same class as the empty-window issue — good catch.
    </pre>

### Thread 3 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59QDiG`
- **Path**: `docs/FACTORY-HYGIENE.md`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-23T23:10:01Z on `docs/FACTORY-HYGIENE.md`:100 (association: NONE)

  <pre>
  P1 (xref): This row cites `memory/project_factory_is_git_native_github_first_host_hygiene_cadences_for_frictionless_operation_2026_04_23.md`, but that file does not exist in the repo. Either add the referenced file, or change the citation to the correct location (e.g., explicitly mark it as per-user `~/.claude/...` memory if it is intentionally not checked in).
  ```suggestion
  | 57 | Git-hotspots audit (cadenced ranking of high-churn files as friction-point candidates) | Detect-only (landed 2026-04-23); cadenced detection every 5-10 rounds (same cadence as rows #5 / #38 / #46 meta-audits) + opportunistic on-touch when merge conflicts surface on a shared file. No enforcement; detection-first per Aaron's *"detection-first, action-second"* framing in the Otto-54 directive cluster. | Dejan (devops-engineer) on cadenced sweeps; Architect (Kenji) on per-file action decisions (split / freeze / archive / watch); all agents (self-administered) on on-touch when a merge conflict surfaces. | factory | `tools/hygiene/audit-git-hotspots.sh --window "60 days" --top 20 --report docs/hygiene-history/git-hotspots-YYYY-MM-DD.md` runs a `git log --since --name-only` pass, counts per-file touches over the window, enriches with unique-author + PR-count columns, ranks top-N, and emits a markdown report. Excludes `docs/hygiene-history/`, `openspec/changes/`, `references/upstreams/` as legitimately-by-design high-churn. **Why this row exists:** Aaron 2026-04-23 Otto-54 four-message cluster — *"cadence for checking github hotspots too this is a hygene issues points of friction and bottlenecks, we are frictionless... git hotspots i mean... we are gitnative with github as our first host"*. High-churn shared files are the paradigmatic friction surface (routine merge conflicts, reviewer burden, serialization bottleneck) and git log is the native instrument for detecting them. **First-fire finding (2026-04-23):** `docs/BACKLOG.md` is the top hotspot at 34 touches / 26 PRs in 30 days — effectively one BACKLOG touch per PR opened. The Otto-54 BACKLOG-per-swim-lane split row is the direct remediation. Other notable hotspots: `docs/ROUND-HISTORY.md` (freeze-then-watch), `memory/MEMORY.md` (cadence candidate — Otto-54 CURRENT-freshness row), 4 skill files (audit candidates for skill-tune-up). **Classification (row #50):** **prevention-bearing** — surfacing friction candidates before they compound into routine merge-tangle is upstream prevention, even though the tool itself is detect-only. Ships to project-under-construction: adopters inherit the audit + exclusion-prefix discipline + the per-file-action decision taxonomy. | Audit output in `docs/hygiene-history/git-hotspots-YYYY-MM-DD.md` per fire (per-fire schema per row #44 — date, window, top-N, Otto observations, per-file action, synthesis); BACKLOG rows for `split` / `audit` actions; ROUND-HISTORY row when a file transitions out of hotspot status via split / freeze / archive. | `tools/hygiene/audit-git-hotspots.sh` (detection + exclusion list + ranking) + `docs/hygiene-history/git-hotspots-2026-04-23.md` (first-run baseline with Otto observations) + Otto-54 directive cluster in BACKLOG.md § "P1 — Git-native hygiene cadences" + per-user memory `~/.claude/memory/project_factory_is_git_native_github_first_host_hygiene_cadences_for_frictionless_operation_2026_04_23.md` (captures the four-message directive verbatim) |
  ```
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-23T23:18:26Z on `docs/FACTORY-HYGIENE.md`:100 (association: MEMBER)

    <pre>
    Fixed in 392b6ab. Added explicit **out-of-repo** (per-user memory, not yet in-repo) marker so readers don't chase a dead in-repo path. Candidate for Overlay-A migration once reviewer capacity permits.
    </pre>

### Thread 4 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59QDiZ`
- **Path**: `docs/FACTORY-HYGIENE.md`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-23T23:10:01Z on `docs/FACTORY-HYGIENE.md`:100 (association: NONE)

  <pre>
  P1: This new hygiene row repeatedly uses the human maintainer’s personal name (“Aaron”) in the spec text. Standing rule is to use role references (e.g., “human maintainer”) outside the allowed exemptions.
  ```suggestion
  | 57 | Git-hotspots audit (cadenced ranking of high-churn files as friction-point candidates) | Detect-only (landed 2026-04-23); cadenced detection every 5-10 rounds (same cadence as rows #5 / #38 / #46 meta-audits) + opportunistic on-touch when merge conflicts surface on a shared file. No enforcement; detection-first per the human maintainer's *"detection-first, action-second"* framing in the Otto-54 directive cluster. | Dejan (devops-engineer) on cadenced sweeps; Architect (Kenji) on per-file action decisions (split / freeze / archive / watch); all agents (self-administered) on on-touch when a merge conflict surfaces. | factory | `tools/hygiene/audit-git-hotspots.sh --window "60 days" --top 20 --report docs/hygiene-history/git-hotspots-YYYY-MM-DD.md` runs a `git log --since --name-only` pass, counts per-file touches over the window, enriches with unique-author + PR-count columns, ranks top-N, and emits a markdown report. Excludes `docs/hygiene-history/`, `openspec/changes/`, `references/upstreams/` as legitimately-by-design high-churn. **Why this row exists:** human maintainer 2026-04-23 Otto-54 four-message cluster — *"cadence for checking github hotspots too this is a hygene issues points of friction and bottlenecks, we are frictionless... git hotspots i mean... we are gitnative with github as our first host"*. High-churn shared files are the paradigmatic friction surface (routine merge conflicts, reviewer burden, serialization bottleneck) and git log is the native instrument for detecting them. **First-fire finding (2026-04-23):** `docs/BACKLOG.md` is the top hotspot at 34 touches / 26 PRs in 30 days — effectively one BACKLOG touch per PR opened. The Otto-54 BACKLOG-per-swim-lane split row is the direct remediation. Other notable hotspots: `docs/ROUND-HISTORY.md` (freeze-then-watch), `memory/MEMORY.md` (cadence candidate — Otto-54 CURRENT-freshness row), 4 skill files (audit candidates for skill-tune-up). **Classification (row #50):** **prevention-bearing** — surfacing friction candidates before they compound into routine merge-tangle is upstream prevention, even though the tool itself is detect-only. Ships to project-under-construction: adopters inherit the audit + exclusion-prefix discipline + the per-file-action decision taxonomy. | Audit output in `docs/hygiene-history/git-hotspots-YYYY-MM-DD.md` per fire (per-fire schema per row #44 — date, window, top-N, Otto observations, per-file action, synthesis); BACKLOG rows for `split` / `audit` actions; ROUND-HISTORY row when a file transitions out of hotspot status via split / freeze / archive. | `tools/hygiene/audit-git-hotspots.sh` (detection + exclusion list + ranking) + `docs/hygiene-history/git-hotspots-2026-04-23.md` (first-run baseline with Otto observations) + Otto-54 directive cluster in BACKLOG.md § "P1 — Git-native hygiene cadences" + role-based companion memory capturing the four-message directive verbatim |
  ```
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-23T23:18:28Z on `docs/FACTORY-HYGIENE.md`:100 (association: MEMBER)

    <pre>
    Fixed in 392b6ab. Replaced direct-name references with role references (the human maintainer / maintainer). FACTORY-HYGIENE.md is NOT in the history-file exempt list per the policy clarification landed in PR #210 (the exempt list covers tick-history, memory/**, hygiene-history/**, ROUND-HISTORY.md, BACKLOG attribution lines, research logs). Verbatim quote block preserved — quoted directive IS attribution, which the narrow exemption covers.
    </pre>

### Thread 5 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59QDil`
- **Path**: `tools/hygiene/audit-git-hotspots.sh`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-23T23:10:02Z on `tools/hygiene/audit-git-hotspots.sh`:75 (association: NONE)

  <pre>
  P1: Option parsing assumes every flag has a value (e.g., `--window` reads `$2` and then `shift 2`). With `set -euo pipefail`, a missing value produces a confusing runtime failure (`shift: shift count out of range`). Add explicit validation for missing/empty flag values and return a usage error (exit 64) with a helpful message.
  ```suggestion

  require_flag_value() {
    local flag="$1"

    if [[ $# -lt 2 || -z "${2-}" ]]; then
      echo "missing value for ${flag}" &gt;&amp;2
      exit 64
    fi
  }

  while [[ $# -gt 0 ]]; do
    case "$1" in
      --window)
        require_flag_value "$1" "$@"
        window="$2"
        shift 2
        ;;
      --top)
        require_flag_value "$1" "$@"
        top="$2"
        shift 2
        ;;
      --report)
        require_flag_value "$1" "$@"
  ```
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-23T23:18:30Z on `tools/hygiene/audit-git-hotspots.sh`:75 (association: MEMBER)

    <pre>
    Fixed in 392b6ab. Added `require_value` helper that validates $2 exists and fails fast with "error: $FLAG requires a value" + exit 64 before the `shift 2` runs. No more confusing runtime message.
    </pre>

### Thread 6 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59QDiw`
- **Path**: `tools/hygiene/audit-git-hotspots.sh`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-23T23:10:02Z on `tools/hygiene/audit-git-hotspots.sh`:55 (association: NONE)

  <pre>
  P2: `--help` currently greps every `#` line, which includes the shebang. After the `sed` rewrite, the help output starts with `!/usr/bin/env bash`, which is confusing. Filter out `^#!/` (or start grep at line 2) so help output is clean.
  ```suggestion
        grep '^#' "$0" | grep -v '^#!/' | sed 's/^# //;s/^#//'
  ```
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-23T23:18:32Z on `tools/hygiene/audit-git-hotspots.sh`:55 (association: MEMBER)

    <pre>
    Fixed in 392b6ab. Added `| grep -v '^#!'` filter in the --help branch so shebang doesn't leak into help output. Cosmetic but worth doing.
    </pre>

### Thread 7 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59QDi3`
- **Path**: `tools/hygiene/audit-git-hotspots.sh`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-23T23:10:02Z on `tools/hygiene/audit-git-hotspots.sh`:129 (association: NONE)

  <pre>
  P1: The comment says the touch count is “ignoring pure deletions”, but `git log --name-only` will still list deleted paths. If deletions should be excluded from the hotspot ranking, add an appropriate `--diff-filter` (or equivalent) to exclude D entries; otherwise update the comment so the report semantics are accurate.
  ```suggestion
  raw=$(git log --since="$window" --pretty=format: --name-only --diff-filter=d \
  ```
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-23T23:18:34Z on `tools/hygiene/audit-git-hotspots.sh`:129 (association: MEMBER)

    <pre>
    Fixed in 392b6ab. The comment was inaccurate — `git log --name-only` does include deletion-commit paths. Corrected: we DELIBERATELY include deletions because frequent deletion of a path is still friction (same class as frequent rewrite). Comment now states the intent explicitly.
    </pre>

### Thread 8 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59QDjO`
- **Path**: `tools/hygiene/audit-git-hotspots.sh`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-23T23:10:02Z on `tools/hygiene/audit-git-hotspots.sh`:5 (association: NONE)

  <pre>
  P1: This header comment uses the human maintainer’s personal name (“Aaron”) on a non-exempt surface. Repo standing rule is to use role references (e.g., “human maintainer”) outside `memory/persona/**` (and optionally `docs/BACKLOG.md`). Please rewrite to a role reference.
  ```suggestion
  # window — the "hotspots" the human maintainer named on 2026-04-23 Otto-54:
  ```
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-23T23:18:35Z on `tools/hygiene/audit-git-hotspots.sh`:5 (association: MEMBER)

    <pre>
    Fixed in 392b6ab. Script header comment rewrote to use role reference (the human maintainer). The verbatim quote block preserves the attribution line ("the human maintainer named on 2026-04-23 Otto-54") because the quoted directive IS attribution, which is the narrow exemption. Added inline note to the script comment explaining the distinction.
    </pre>

### Thread 9 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59QDjg`
- **Path**: `docs/hygiene-history/git-hotspots-2026-04-23.md`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-23T23:10:03Z on `docs/hygiene-history/git-hotspots-2026-04-23.md`:67 (association: NONE)

  <pre>
  P1: This report uses personal names (“Otto observations”, and later references to the human maintainer by name). Per repo standing rule, use role references on docs surfaces outside the allowed exemptions (e.g., “first-run observations”, “human maintainer”).
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-23T23:18:37Z on `docs/hygiene-history/git-hotspots-2026-04-23.md`:67 (association: MEMBER)

    <pre>
    Won't fix — policy pushback. The report lives at `docs/hygiene-history/git-hotspots-2026-04-23.md`, which IS in the history-file exempt category per PR #210 (merged) § "Name-attribution policy clarification". The exempt list explicitly covers `hygiene-history/**`. Otto observations attribution and named-human references in a history file are documented historical reality, not forward-looking prose. Marking resolved.
    </pre>

### Thread 10 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59QKUj`
- **Path**: `tools/hygiene/audit-git-hotspots.sh`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-04-23T23:19:18Z on `tools/hygiene/audit-git-hotspots.sh`:107 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Fail fast on git history read errors**

  `raw=$(git log ... | grep -v '^$' || true)` masks **all** non-zero exits from `git log`, not just the empty-window case. In repositories with missing/unreadable objects (e.g., partial clones), `git log` can emit fatal errors and stop early, but this code still produces a report from truncated history and exits successfully, which makes hotspot rankings unreliable while hiding a real repo-read failure. Handle only the `grep` no-match case explicitly and propagate `git log` failures.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-24T16:44:05Z on `tools/hygiene/audit-git-hotspots.sh`:107 (association: MEMBER)

    <pre>
    Fixed in be3e2cb. Replaced `grep -v '^$' || true` with `sed '/^$/d'` on the main pipeline and added an upfront `git rev-parse --is-inside-work-tree` guard (exit 128 on failure). Empty-output is handled by sed (exit 0, empty string) while real `git log` failures now propagate under `set -euo pipefail`. Good catch — pipefail-masking was hiding real repo-read errors.
    </pre>

### Thread 11 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59QOSn`
- **Path**: `tools/hygiene/audit-git-hotspots.sh`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-04-23T23:25:38Z on `tools/hygiene/audit-git-hotspots.sh`:135 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Stop suppressing git-log failures in file_summary**

  Both per-file metric pipelines hide `git log` failures by redirecting stderr to `/dev/null` and adding `|| true`, so history-read errors (partial clone, missing objects, corrupt repo) are silently turned into apparently valid `unique authors` / `PR count` numbers. In this repository, `git log --since='60 days' -- docs/BACKLOG.md` exits 128 with `fatal: unable to read tree ...`, but this code still emits counts, which makes the report look trustworthy while using incomplete data. Please let `git log` failures propagate and only special-case the empty-match `grep` path.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-24T16:44:39Z on `tools/hygiene/audit-git-hotspots.sh`:135 (association: MEMBER)

    <pre>
    Fixed in be3e2cb. Removed the `2&gt;/dev/null` + `|| true` wrappers in `file_summary`. Now we capture `git log` stdout into a variable first (failures propagate via pipefail), then classify empty-vs-populated branchlessly. The remaining `|| true` on the inner `grep -oE` is bounded to the no-match case where `git log` already succeeded, so partial-clone fatals no longer silently fabricate zeros.
    </pre>

### Thread 12 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59QOlV`
- **Path**: `tools/hygiene/audit-git-hotspots.sh`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-23T23:26:08Z on `tools/hygiene/audit-git-hotspots.sh`:25 (association: NONE)

  <pre>
  P1: Header comment still uses the human maintainer’s personal name ("Aaron") outside the verbatim quote block. Per the repo’s no-name-attribution rule for code/docs/skills, please rewrite this aside to use a role reference (e.g., “the human maintainer”) and avoid the personal name entirely.
  ```suggestion
  # (Naming the human maintainer inline in the verbatim quote
  # above is deliberate — the quoted directive is attribution,
  # not prose reference. Outside the quote block, prose uses
  # role references per the no-name-attribution rule.)
  ```
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-24T16:44:41Z on `tools/hygiene/audit-git-hotspots.sh`:25 (association: MEMBER)

    <pre>
    Fixed in be3e2cb. Rewrote the aside comment to reference the verbatim quote by role — no more personal name outside the quote block. The verbatim quote itself is preserved (quoted directive IS attribution, which is the narrow name-attribution exemption).
    </pre>

### Thread 13 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59QOlk`
- **Path**: `tools/hygiene/audit-git-hotspots.sh`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-23T23:26:09Z on `tools/hygiene/audit-git-hotspots.sh`:74 (association: NONE)

  <pre>
  P1: `--top` accepts any string and is later passed to `head -n`. If a non-integer value is provided (or a value starting with `-`), `head` will error and the script will exit non-deterministically under `set -e` (despite the “exit 0 — always” contract). Consider validating that `--top` is a positive integer and failing with exit 64 + a clear message when it is not.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-24T16:44:43Z on `tools/hygiene/audit-git-hotspots.sh`:74 (association: MEMBER)

    <pre>
    Fixed in be3e2cb. Added `require_positive_int` validator — `--top` must match `^[1-9][0-9]*$` or the script aborts with exit 64 and a clear message. Smoke-tested: `--top abc` errors cleanly. Integer-only in the hot path eliminates the non-deterministic-head-failure path entirely.
    </pre>

### Thread 14 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59QSRc`
- **Path**: `tools/hygiene/audit-git-hotspots.sh`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-04-23T23:32:00Z on `tools/hygiene/audit-git-hotspots.sh`:153 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Preserve exact filename text when parsing ranked rows**

  The filename extraction rewrites whitespace (`awk '{$1=""; sub(/^ /,""); print}'`), so paths containing repeated spaces or tabs are normalized (e.g., `a  b.txt` becomes `a b.txt`) before `file_summary` runs. In repositories that contain such valid Git paths, the subsequent `git log -- "$file"` lookup targets the wrong file and the report emits incorrect `unique authors` / `PR count` values (often `0`), which corrupts the hotspot audit output.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-24T16:44:45Z on `tools/hygiene/audit-git-hotspots.sh`:153 (association: MEMBER)

    <pre>
    Fixed in be3e2cb. Replaced `awk '{$1=""; sub(/^ /,""); print}'` with `sed -E 's/^[[:space:]]*[0-9]+[[:space:]]//'` which strips exactly the `uniq -c` prefix (leading whitespace + count + single separator) without touching the rest of the line. Filenames with repeated whitespace now round-trip into `git log -- "$file"` exactly.
    </pre>

### Thread 15 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59QWXi`
- **Path**: `tools/hygiene/audit-git-hotspots.sh`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-04-23T23:38:08Z on `tools/hygiene/audit-git-hotspots.sh`:150 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Avoid SIGPIPE exits when truncating ranked output**

  The `render` loop pipes `printf` into `head` while `set -euo pipefail` is enabled; when the ranked list is large enough, `head -n "$top"` exits early and upstream `printf` can terminate with SIGPIPE (141), causing the whole audit to exit non-zero even though this tool is documented as detect-only and always-exit-0. This will surface on larger repos or wider windows with many hotspot rows, so the ranking step should avoid a `head` pipeline that can fail under `pipefail`.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-24T16:44:46Z on `tools/hygiene/audit-git-hotspots.sh`:150 (association: MEMBER)

    <pre>
    Fixed in be3e2cb. Replaced the `printf '%s\n' "$ranked" | head -n "$top"` pipeline with a `while IFS= read` loop + counter over a here-string. No pipe into `head`, so no SIGPIPE 141 path. The "exit 0 — always" contract now holds on large ranked lists too.
    </pre>

### Thread 16 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59QWwl`
- **Path**: `tools/hygiene/audit-git-hotspots.sh`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-23T23:38:43Z on `tools/hygiene/audit-git-hotspots.sh`:107 (association: NONE)

  <pre>
  P1: `raw=$(git log ... | grep -v '^$' || true)` will also suppress real `git log` failures (e.g., running outside a git worktree) and later reports "no commits". Prefer failing fast with a clear error (or explicitly checking `git rev-parse --is-inside-work-tree`), and only treating the *empty output* case as non-fatal.
  ```suggestion
  if ! git rev-parse --is-inside-work-tree &gt;/dev/null 2&gt;&amp;1; then
    echo "error: tools/hygiene/audit-git-hotspots.sh must run inside a git worktree" &gt;&amp;2
    exit 128
  fi

  raw=$(git log --since="$window" --pretty=format: --name-only \
        | sed '/^$/d')
  ```
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-24T16:44:48Z on `tools/hygiene/audit-git-hotspots.sh`:107 (association: MEMBER)

    <pre>
    Fixed in be3e2cb. Applied the suggested pattern: upfront `git rev-parse --is-inside-work-tree &gt;/dev/null 2&gt;&amp;1 || exit 128`, then `git log ... | sed '/^$/d'` (no `|| true`). Real git failures propagate; only the empty-match case is non-fatal, which is what was actually intended.
    </pre>

### Thread 17 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59QWw7`
- **Path**: `tools/hygiene/audit-git-hotspots.sh`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-23T23:38:44Z on `tools/hygiene/audit-git-hotspots.sh`:133 (association: NONE)

  <pre>
  The comment here mentions “`grep -c` … via `wc -l`”, but the pipeline doesn’t use `wc -l`. Consider updating the comment to accurately describe why `|| true` is needed (i.e., `grep -c` prints 0 but exits 1 on no matches, which would trip `set -euo pipefail`).
  ```suggestion
    # prints "0" on no matches but exits 1 in that case.
  ```
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-24T16:44:50Z on `tools/hygiene/audit-git-hotspots.sh`:133 (association: MEMBER)

    <pre>
    Fixed in be3e2cb. The stale `wc -l` comment is gone — rewrote the `file_summary` block entirely and the new comments correctly describe the actual pipeline (capture subjects first under pipefail, then grep in a context where no-match exit-1 is acceptable).
    </pre>

### Thread 18 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59Q7TD`
- **Path**: `tools/hygiene/audit-git-hotspots.sh`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-24T00:38:06Z on `tools/hygiene/audit-git-hotspots.sh`:136 (association: NONE)

  <pre>
  P0: `raw=$(git log ... | grep ... || true)` masks any `git log` failure (e.g., running outside a git worktree, missing git, or other git errors) and then emits the misleading “no commits in window …” message while still exiting 0. Please add an explicit “must be inside a git repo” check up-front, and avoid `|| true` in a way that hides real git errors (it should only tolerate the empty-output case).
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-24T16:44:52Z on `tools/hygiene/audit-git-hotspots.sh`:136 (association: MEMBER)

    <pre>
    Fixed in be3e2cb. Added explicit `git rev-parse --is-inside-work-tree` check at the top of the main pipeline (exit 128 with a clear message on failure) and swapped the masking `|| true` for `sed '/^$/d'` so real git errors propagate under pipefail. The "no commits in window" message now only fires for actually-empty windows, not for repo-read failures.
    </pre>

### Thread 19 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59Q7d1`
- **Path**: `tools/hygiene/audit-git-hotspots.sh`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-04-24T00:38:24Z on `tools/hygiene/audit-git-hotspots.sh`:135 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Restrict PR counting to actual PR suffixes**

  `grep -oE '#[0-9]+'` treats every hash-number in a subject as a PR, but this repo’s commit subjects also contain non-PR tokens like hygiene row IDs (for example, `row #58`) and issue-style references (`fix(#213)`), so the reported `PR count` is systematically inflated and can drive wrong hotspot decisions. Parse only the GitHub PR marker format (e.g., trailing `(#123)` from squash merges) or rename the metric to a generic reference count.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-24T16:44:54Z on `tools/hygiene/audit-git-hotspots.sh`:135 (association: MEMBER)

    <pre>
    Fixed in be3e2cb. Narrowed `grep -oE '#[0-9]+'` to `grep -oE '\(#[0-9]+\)$'` so only trailing squash-merge markers count. Row IDs like `row #58` and issue refs like `fix #213` mid-subject no longer inflate the metric. Added an explaining comment. Metric name stays "PR count" since that's now what it actually measures.
    </pre>

### Thread 20 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM59dTNM`
- **Path**: `docs/hygiene-history/git-hotspots-2026-04-23.md`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-24T16:47:10Z on `docs/hygiene-history/git-hotspots-2026-04-23.md`:27 (association: NONE)

  <pre>
  P1: In the Ranking table, `docs/FACTORY-HYGIENE.md` shows `touches = 7` but `PR count = 10`. Given the tool’s semantics (touches is the number of commits touching the file within the window, PR count is derived from those commits’ subjects), PR count should not exceed touches. This row likely has a transcription/regeneration error and should be regenerated or corrected.
  ```suggestion
  | docs/FACTORY-HYGIENE.md | 7 | 1 | 7 |
  ```
  </pre>

### Thread 21 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM59dTNy`
- **Path**: `docs/hygiene-history/git-hotspots-2026-04-23.md`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-24T16:47:11Z on `docs/hygiene-history/git-hotspots-2026-04-23.md`:75 (association: NONE)

  <pre>
  P1: The report declares **Top: 25** (line 5), but the “Per-file suggested action” mapping appears incomplete (it doesn’t cover all 25 ranked files), and the “Synthesis” counts later don’t reconcile with Top-N. Consider either lowering Top to the analyzed subset or adding action rows + updating synthesis so every ranked file is accounted for.
  </pre>

### Thread 22 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM59dTN_`
- **Path**: `tools/hygiene/audit-git-hotspots.sh`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-24T16:47:11Z on `tools/hygiene/audit-git-hotspots.sh`:38 (association: NONE)

  <pre>
  P1: The header says the script exits "0 — always", but the implementation exits non-zero for invalid usage (64) and for running outside a git worktree (128). Update the Exit codes comment to document these cases so callers aren’t misled.
  ```suggestion
  #   0   — success (detect-only, no enforcement yet; see
  #         Otto-54 NOT-list: detection-first, action-second)
  #   64  — invalid usage (for example: missing flag value or
  #         non-positive integer passed to --top)
  #   128 — not inside a git worktree
  ```
  </pre>

## Fix commits (touching thread paths)

### `b3dd8d015ea88d80e3744ef0058d555f05dc85c6` -- 2026-04-23T23:06:05Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `docs/hygiene-history/git-hotspots-2026-04-23.md`
- `tools/hygiene/audit-git-hotspots.sh`

**Message:**

```
hygiene: git-hotspots audit tool + first-run baseline (FACTORY-HYGIEN…

…E row #57)

Directly executes Aaron Otto-54's git-hotspots cadence directive from
the four-message directive cluster in PR #212 BACKLOG row. This PR
closes the gap between "BACKLOG row naming the cadence" and "audit
tool + first baseline report + FACTORY-HYGIENE row activating it".

New artifacts:

- `tools/hygiene/audit-git-hotspots.sh` — bash script running
  `git log --since --name-only` over a configurable window, ranking
  top-N, enriching with unique-author + PR-count, excluding
  legitimately-by-design hotspots (hygiene-history, openspec/changes,
  references/upstreams). Supports `--window`, `--top`, `--report`.
  Detect-only (exit 0); action is judgment call.

- `docs/hygiene-history/git-hotspots-2026-04-23.md` — first-run
  baseline report over 30 days. Finding: `docs/BACKLOG.md` is top
  hotspot at 34 touches / 26 PRs (one touch per PR opened) —
  validates Aaron's Otto-54 intuition that BACKLOG split is the
  highest-leverage move. Otto observations appended with per-file
  action recommendations (1 split / 1 freeze-then-watch / 1 cadence
  / 5 audit / 11 watch).

- FACTORY-HYGIENE row #57 activating the cadenced sweep.
  Cross-refs the BACKLOG row and the companion memory.

Row number is 57 (not 56) to leave #56 reserved for the MD032
preflight that's in-flight on PR #204.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
```

### `b0741a40ca0e054f518f2c5fd103377cb2e213c8` -- 2026-04-23T23:17:21Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/hygiene/audit-git-hotspots.sh`

**Message:**

```
fix(#213): address 9 Codex/Copilot findings — script bugs + policy-nu…

…anced name attribution

7 real findings addressed in this commit. 2 findings need policy pushback,
captured in thread responses.

CODEX P1 — empty window aborts under set -euo pipefail:
  `grep -v '^$'` returns exit code 1 when no matches → pipe aborts. Fixed
  by adding `|| true` guard + explicit empty-window message to stderr.

CODEX P1 — PR count returns zero via grep silence:
  Same pattern. Replaced `wc -l | tr -d ' '` with `grep -c . || true` so
  the empty case yields 0 cleanly instead of aborting.

COPILOT P1 — option parsing missing value handling:
  `shift 2` with missing arg produced confusing error. Added `require_value`
  helper that fails fast with "error: $1 requires a value" + exit 64.

COPILOT P2 — --help shows shebang:
  `grep '^#'` included `#!/usr/bin/env bash`. Added `| grep -v '^#!'`
  filter.

COPILOT P1 — "ignoring pure deletions" comment was wrong:
  `git log --name-only` lists deleted paths too. Fixed comment to state
  we DELIBERATELY include deletions (frequent deletion = friction too).

COPILOT P1 — "Aaron" attribution on non-exempt surfaces:
  FACTORY-HYGIENE row #57 + script header comment rewrote references to
  "the human maintainer" (role). Verbatim quote block in script header
  preserved as attribution (with inline note explaining the distinction).
  FACTORY-HYGIENE.md is NOT in the history-file exempt list per the
  policy clarification being landed in PR #210.

COPILOT P1 — dangling memory path citation:
  FACTORY-HYGIENE row #57 cited `memory/project_factory_is_git_native_...`
  as if in-repo; actually per-user memory. Added explicit
  "**out-of-repo** (per-user memory, not yet in-repo)" marker.

POLICY-PUSHBACK (not fixed — thread response):

COPILOT P1 — "Otto observations" + "Aaron" in baseline report:
  The report lives at `docs/hygiene-history/git-hotspots-2026-04-23.md`
  which IS in the history-file exempt list per the Otto-52 policy
  clarification (*"'No name attribution in code, docs, or skills' in
  history files are files like memory backlog and other things like
  that for historical purposes"*). Naming the author "Otto" in
  observation attribution is attribution-line form, which the exemption
  preserves. Not fixing; will explain in thread reply.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
```

### `be3e2cbd9f6162808b176837f9e732c87f8524be` -- 2026-04-24T16:42:28Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `docs/FACTORY-HYGIENE.md`
- `tools/hygiene/audit-git-hotspots.sh`

**Message:**

```
fix(#213): address 7 remaining review findings + restore FACTORY-HYGI…

…ENE row 57

Rebase onto main used -X ours for audit-trail (FACTORY-HYGIENE.md is
append-only) which dropped our new row 57. Restored it in this commit
with the latest post-feedback text (human-maintainer role reference +
out-of-repo memory marker, matching state at branch head before rebase).

Script fixes:
- Remove "Aaron" from header aside; use role reference, keep verbatim
  quote attribution as before (narrow attribution exemption).
- Validate --top is a positive integer; exit 64 with clear message.
- Guard with git rev-parse --is-inside-work-tree; replace main-pipeline
  'grep -v || true' with 'sed /^$/d' so git-log failures propagate
  instead of being masked.
- file_summary: capture git-log output without stderr suppression or
  '|| true'; count lines via wc -l; restrict PR-count to trailing
  '(#NNN)' squash-merge markers so 'row #58' / 'fix #213' tokens no
  longer inflate the metric.
- render: replace 'head -n $top' pipeline with counter + while-read so
  SIGPIPE 141 cannot surface on large ranked lists.
- render: replace awk $1-strip with sed regex so filenames with
  internal whitespace are preserved exactly.

Smoke-tested --window 7d/0d, invalid --top, missing --window value.
```
