# PR #327 -- backlog: P2 SharderInfoTheoreticTests.Uniform flake — seed-lock + DST (Aaron Otto-132)

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 327 |
| Title | backlog: P2 SharderInfoTheoreticTests.Uniform flake — seed-lock + DST (Aaron Otto-132) |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-04-24T07:56:27Z |
| Merged at | 2026-04-24T13:05:17Z |
| Merge commit SHA | `4735547585dfd6f4be87eaf371a7752b8a81e42e` |
| Branch | `backlog/sharder-info-theoretic-flaky-test-seed-lock-dst` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/327 |
| Changed files | 1 |
| Additions / deletions | +2 / -0 |

## Description

Aaron Otto-132 flag on the flaky test blocking #323 (and potentially others). 4-step scope: audit seeds, sweep Sharder tests, document pattern, quantify flake rate.

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 3 |
| Resolved threads | 2 |
| Unresolved threads | 1 |
| Total review comments | 5 |
| Total fix commits (touching thread paths) | 1 |

## Review threads

### Thread 1 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59VLQ2`
- **Path**: `docs/BACKLOG.md`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-24T07:58:34Z on `docs/BACKLOG.md`:4278 (association: NONE)

  <pre>
  P1: This BACKLOG item’s root-cause hypothesis looks incorrect. The referenced test is an xUnit [&lt;Fact&gt;] and already uses a fixed RNG seed (`Random 42`), so it’s not an FsCheck property test and there’s no FsCheck seed to lock. If this test flakes, a more likely nondeterminism source is `HashCode.Combine` (process-randomized seed) affecting the shard mapping; suggest updating the text/questions to reflect the actual nondeterministic input and fix direction (e.g., stable hash function / fixed seedable hasher).
  ```suggestion
  - [ ] **SharderInfoTheoreticTests.Uniform flake — stable hashing + DST compliance.** Aaron 2026-04-24 Otto-132 flag: *"SharderInfoTheoreticTests.Uniform (not seed locked, falkey, DST?)"*. Test `Zeta.Tests.Formal.SharderInfoTheoreticTests.Uniform traffic: consistent-hash is already near-optimal` is a statistical threshold assertion (`Expected: "Less than 1.2"`) that fails intermittently on CI — observed blocking PR #323 (whose own property tests passed). The referenced test is an xUnit `[&lt;Fact&gt;]`, not an FsCheck property, and it already uses a fixed RNG seed (`Random 42`), so the more likely nondeterminism source is unstable shard mapping input (for example `HashCode.Combine` with a process-randomized seed) rather than FsCheck seed roulette. Questions: (1) does shard assignment depend on a process-randomized hash or other nondeterministic input that should be replaced with a stable hash function / fixed seedable hasher for tests? (2) does the current assertion violate DST expectations and need a deterministic equivalent? Scope: (1) audit `SharderInfoTheoreticTests.fs` and sharder implementation paths for `HashCode.Combine` or equivalent unstable hashing in test-observable shard mapping, and switch tests to stable hashing or a fixed seedable hasher where needed; (2) sweep all Sharder tests for similar non-DST / unstable-hash patterns; (3) document the fix pattern so future statistical tests follow it; (4) quantify flake rate from historical CI. Priority P2 research-grade (blocks other PRs' auto-merge when it trips). Effort S + S.
  ```
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-24T13:03:37Z on `docs/BACKLOG.md`:4278 (association: MEMBER)

    <pre>
    Fixed in d7d349b. Verified the test file — you are correct: the Uniform test is an xUnit `[&lt;Fact&gt;]` already using `Random 42` for Zipf key generation, so the flake is NOT lack of FsCheck seed-lock. Rewrote the root-cause hypothesis to lead with `HashCode.Combine` process-hash-randomization (the downstream `JumpConsistentHash.Pick` gets a different shard distribution each process, tripping the `max/avg &lt; 1.2` assertion), with tolerance tightness and closure-iteration nondeterminism as secondary candidates. Scope updated to seeded deterministic hash + empirically-justified tolerance (Wilson interval / measured 99th-pct) rather than arbitrary 1.2. Thanks for the careful read.
    </pre>

### Thread 2 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59VLRU`
- **Path**: `docs/BACKLOG.md`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-24T07:58:34Z on `docs/BACKLOG.md`:4278 (association: NONE)

  <pre>
  P1 xref: The item says to audit `SharderInfoTheoreticTests.fs`, but the test module lives in `tests/Tests.FSharp/Formal/Sharder.InfoTheoretic.Tests.fs` (file name differs). This path mismatch will make the backlog entry harder to action; update the reference to the actual file path.
  ```suggestion
  - [ ] **SharderInfoTheoreticTests.Uniform flake — seed-lock + DST compliance.** Aaron 2026-04-24 Otto-132 flag: *"SharderInfoTheoreticTests.Uniform (not seed locked, falkey, DST?)"*. Test `Zeta.Tests.Formal.SharderInfoTheoreticTests.Uniform traffic: consistent-hash is already near-optimal` is a statistical property assertion (`Expected: "Less than 1.2"`) that fails intermittently on CI — observed blocking PR #323 (whose own property tests passed). Questions: (1) is it seed-locked for FsCheck? probably not, hence seed-roulette; (2) violates DST? Scope: (1) audit `tests/Tests.FSharp/Formal/Sharder.InfoTheoretic.Tests.fs` FsCheck seed usage + add fixed seed or replace statistical-threshold with deterministic equivalent; (2) sweep all Sharder tests for similar non-DST patterns; (3) document fix pattern so future statistical tests follow it; (4) quantify flake rate from historical CI. Priority P2 research-grade (blocks other PRs' auto-merge when it trips). Effort S + S.
  ```
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-24T13:03:41Z on `docs/BACKLOG.md`:4278 (association: MEMBER)

    <pre>
    Fixed in d7d349b. Path updated to `tests/Tests.FSharp/Formal/Sharder.InfoTheoretic.Tests.fs` (module `Zeta.Tests.Formal.SharderInfoTheoreticTests`). Thanks.
    </pre>

### Thread 3 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM59ZuVG`
- **Path**: `docs/BACKLOG.md`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-24T13:05:54Z on `docs/BACKLOG.md`:4874 (association: NONE)

  <pre>
  P1: This backlog row’s secondary hypothesis about the threshold being “overly tight for a 100k-sample, 16-shard Zipf baseline” looks incorrect for the referenced flake. The failing test is the *Uniform traffic* `[&lt;Fact&gt;]` (uniform keys, not Zipf), and with `HashCode.Combine`/bucket mapping changing per process the variance is dominated by the per-process hash seed (and the small key universe), not finite-sample σ from 100k draws. Suggest rewriting (2) to reflect the real risk factors here (e.g., small `Next 5_000` universe + per-process hash salt can yield an imbalanced bucket mapping; using stable hashing and/or sampling `uint64` keys directly / increasing universe size removes the issue) rather than framing it as a 2σ/Zipf threshold problem.
  ```suggestion
  - [ ] **SharderInfoTheoreticTests "Uniform traffic" flake — process-hash-randomization root cause + deterministic-threshold fix.** Aaron 2026-04-24 Otto-132 flag: *"SharderInfoTheoreticTests.Uniform (not seed locked, falkey, DST?)"*. Test lives at `tests/Tests.FSharp/Formal/Sharder.InfoTheoretic.Tests.fs` (module `Zeta.Tests.Formal.SharderInfoTheoreticTests`). **Corrected root-cause hypothesis (PR #327 review):** the test is an xUnit `[&lt;Fact&gt;]`, not an FsCheck property — it already uses `Random 42` for key generation, so the flake is *not* lack of FsCheck seed-lock. Actual suspected causes, in priority order: (1) **`HashCode.Combine` is process-randomized** — .NET's `HashCode` uses a per-process random seed (hash-randomization mitigation), so `uint64 (HashCode.Combine k)` differs across runs even with identical keys; the downstream `JumpConsistentHash.Pick` then yields a different shard distribution each run, and the `max/avg &lt; 1.2` assertion trips on unlucky seeds; (2) the variance here is more likely driven by **small-key-universe + salted hash-to-bucket mapping** than by a Zipf/finite-sample threshold story — this is the *Uniform traffic* fact, and if keys come from a relatively small `rng.Next 5_000`-style universe then a per-process hash salt can map that fixed set unevenly across 16 shards; using a stable hash and/or sampling direct `uint64` keys / increasing the universe size removes most of that artifact before any tolerance tuning; (3) nondeterministic iteration order in `Array.init` closures if `rng.Next` is ever called concurrently (not the current shape, but a future-drift risk). Scope: (1) replace `HashCode.Combine` with a seeded deterministic hash (`XxHash64` / FNV with explicit seed) or a fixed-salt wrapper; (2) if the test still needs a wider tolerance after stable hashing / larger-universe sampling, calibrate it with justification (measured 99th-percentile across N CI runs or equivalent) rather than arbitrary 1.2; (3) sweep sibling Sharder tests for the same `HashCode.Combine` pattern; (4) document the "no process-randomized hashes in deterministic tests" rule as a hygiene row; (5) quantify flake rate from historical CI. Priority P2 research-grade (blocks other PRs' auto-merge when it trips). Effort S + S. **Does NOT authorize:** blanket `HashCode.Combine` ban outside tests (prod paths may legitimately want hash-randomization); widening tolerance without empirical justification.
  ```
  </pre>

## Fix commits (touching thread paths)

### `d7d349b6ba92bfacd803a97e9223acf7e50296f9` -- 2026-04-24T07:56:24Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `docs/BACKLOG.md`

**Message:**

```
backlog: P2 SharderInfoTheoreticTests.Uniform flake — revised hypothe…

…sis + correct path (PR #327 review)

Corrects two PR-review findings on the Sharder flake BACKLOG row:

1. File path fix: SharderInfoTheoreticTests.fs -> tests/Tests.FSharp/Formal/Sharder.InfoTheoretic.Tests.fs
2. Root-cause hypothesis rewrite: test is an xUnit [<Fact>] already using Random 42
   for key generation, NOT an FsCheck property test. Seed-lock is not the fix.
   Revised suspected causes: (a) HashCode.Combine process-hash-randomization
   (primary); (b) 1.2x tolerance too tight for 100k/16-shard Zipf; (c) closure
   iteration nondeterminism as future-drift risk. Scope updated to seeded
   deterministic hash + empirically-justified tolerance.

Does not authorize blanket HashCode.Combine ban outside tests, or tolerance
widening without empirical justification.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
```
