# PR #372 -- backlog: Otto-241 three rows — session-id scrub + peer-Claude parity + `claude -w` default

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 372 |
| Title | backlog: Otto-241 three rows — session-id scrub + peer-Claude parity + `claude -w` default |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-04-24T16:00:19Z |
| Merged at | 2026-04-24T16:02:02Z |
| Merge commit SHA | `7e1e8ed6433a88dcb869435b355009ca215af16b` |
| Branch | `backlog/session-id-scrub-plus-peer-claude-parity-plus-worktree-launch-otto-241` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/372 |
| Changed files | 1 |
| Additions / deletions | +6 / -0 |

## Description

## Summary

Otto-241 maintainer directive produced three disciplines. Filing all three as BACKLOG rows in a single PR since they compose tightly (discussed in same message, share the multi-instance-Claude context).

## Rows added (in `## P2 — research-grade` section)

1. **Session-id bulk scrub (P2, effort S mechanical)** — remove `originSessionId:` YAML frontmatter from ~900 factory files (memory + research + history). Discipline effective going forward already; this row covers the back-catalog cleanup.
2. **Peer-Claude parity test (P2, effort M)** — regression guard that a fresh Claude Code session can match seasoned-session effectiveness using only in-repo substrate. Measures whether Otto-230 externalisation gap has actually been closed.
3. **Adopt `claude -w` as default launch (P3, effort S+S)** — worktree-mode Claude Code for main-session isolation; analog of Otto-226 subagent worktree pattern one layer up.

## Why one PR, three rows

Otto-241 was one maintainer message; the three disciplines compose: (1) prevents writer-ID collisions, (2) prevents in-session knowledge lock-in, (3) prevents filesystem contention. Filing separately would split a coherent response.

Otto-229 append-only at section bottom honored; rows appended after the Otto-239 swim-lane row already in the section.

## Test plan

- [x] Three rows append at end of `## P2 — research-grade` section
- [x] No existing-row edits (Otto-229 discipline)
- [x] Verbatim maintainer quotes preserved in each row
- [x] Cross-references to composing memories (Otto-171 / 226 / 230 / 233 / 239 / 240 / 241) included

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 1 |
| Resolved threads | 0 |
| Unresolved threads | 1 |
| Total review comments | 1 |
| Total fix commits (touching thread paths) | 1 |

## Review threads

### Thread 1 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM59cq2z`
- **Path**: `docs/BACKLOG.md`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-24T16:03:11Z on `docs/BACKLOG.md`:7046 (association: NONE)

  <pre>
  This row hard-codes the current session GUID (`1937bff2-…`) inside BACKLOG.md, even though the stated goal is to scrub session IDs from persistent factory files. This will go stale immediately and also creates a new `originSessionId`/GUID hit that the future scrub PR must remove. Suggest: remove the concrete GUID from the prose and the example command (search for `originSessionId:` only, or use a placeholder/fake GUID), and consider naming the index file as `memory/MEMORY.md` for consistency with other rows.
  ```suggestion
  - [ ] **Session-id bulk scrub — remove `originSessionId:` YAML frontmatter from all factory-authored memory / research / history files (~900 files).** Maintainer 2026-04-24 Otto-241: *"we should likely clean our session id out of all our files then"*. Session-ids are globally-unique per-Claude-session GUIDs; baking them into persistent factory files is a category error — the file belongs to the factory (persistent), the session-id belongs to one run (ephemeral). Follow-up to Otto-240 per-writer-file tick-history design: when multi-instance mode ships (same-machine-two-Claudes test), any file carrying a hardcoded session-id would falsely claim identity with one specific run. Scope: (1) `grep -rl 'originSessionId:' memory/ docs/` to enumerate; (2) mechanical scrub removing the `originSessionId:` line from YAML frontmatter; (3) update `memory/MEMORY.md` index if needed; (4) doc-only change, single PR, large diff, all mechanical. Do NOT touch Claude Code's own transcript store (`~/.claude/projects/.../sessions/*.jsonl`) — those ARE session logs where session-id belongs. Priority P2 research-grade; effort S (mechanical). Composes with Otto-240 + Otto-241. Discipline effective going forward already: new memory files omit the field.
  ```
  </pre>

## Fix commits (touching thread paths)

### `e0df2aeb6243cf82e7cdd3a9985aff9098f35d8d` -- 2026-04-24T16:00:16Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `docs/BACKLOG.md`

**Message:**

```
backlog: three rows from Otto-241 — session-id scrub + peer-Claude pa…

…rity + -w launch
```
