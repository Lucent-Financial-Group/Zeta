# PR #381 -- backlog: dotnet GC upstream investigation + DST-marker convention (Otto-248 followup)

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 381 |
| Title | backlog: dotnet GC upstream investigation + DST-marker convention (Otto-248 followup) |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-04-24T19:10:49Z |
| Merged at | 2026-04-24T19:12:47Z |
| Merge commit SHA | `43a1aeb471e0240f336d1c57f518d04e3b48883b` |
| Branch | `backlog/dotnet-gc-upstream-investigation-otto-248-followup` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/381 |
| Changed files | 2 |
| Additions / deletions | +9 / -0 |

## Description

## Summary

Two new P2 BACKLOG rows + a concrete DST-exempt marker on the first declared exception.

## Why

Maintainer directive 2026-04-24:
1. *"we can check your workaround in for now and just backlog the other"* — the dotnet GC workaround (PR #376) stays; the proper root-cause investigation gets backlogged
2. *"mark you test, there is some lint rule to call out why a test is not DST fully"* — institutionalize DST-exemption markers

## What's landed

- **\`docs/BACKLOG.md\`**: two new P2 research-grade rows co-located with the SharderInfoTheoretic flake row (same flake-discipline cluster)
  - .NET 10 Server GC crash — bullet-proof investigation (upstream issue, reproducer, source read, fix-landing tracking, workaround removal)
  - DST-marker convention + lint rule (semgrep rule, marker shape, backfill sweep)
- **\`tests/Tests.FSharp/Formal/Sharder.InfoTheoretic.Tests.fs\`**: \`// DST-exempt: ...\` marker on the "InfoTheoreticSharder does not make skew worse" test (first declared exception to the implicit "all tests DST" rule)

## Composes with

- Otto-248 never-ignore-flakes-per-DST (this enforces it)
- Otto-250/251/252 training-signal framing (exception-markers-with-reasons = richer signal than unmarked flakes)
- Otto-253 escro dependency maintenance (dotnet GC row IS escro-scope)
- Otto-254 roll-forward (workaround stays, fix lands forward)
- Existing SharderInfoTheoreticTests row (root-cause analysis)

## Test plan

- [x] \`dotnet build -c Release\` → 0W/0E
- [x] Marker comment on the flaky test points at docs/BACKLOG.md row for root-cause + fix pipeline
- [ ] Future PR implementing the DST-lint-rule sweeps + flags other non-DST tests
- [ ] Future PR executes the dotnet GC investigation (deterministic repro)

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 3 |
| Resolved threads | 0 |
| Unresolved threads | 3 |
| Total review comments | 3 |
| Total fix commits (touching thread paths) | 1 |

## Review threads

### Thread 1 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM59fQU9`
- **Path**: `tests/Tests.FSharp/Formal/Sharder.InfoTheoretic.Tests.fs`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-24T19:13:47Z on `tests/Tests.FSharp/Formal/Sharder.InfoTheoretic.Tests.fs`:69 (association: NONE)

  <pre>
  Repo convention forbids direct contributor name attribution outside `docs/BACKLOG.md` and `memory/persona/**` (see `docs/AGENT-BEST-PRACTICES.md:284-290`). This marker comment includes a contributor name; please rewrite to a role reference (e.g., “human maintainer directive 2026-04-24”) and keep the rest of the pointer intact.
  ```suggestion
  // convention + lint rule" row (human maintainer directive 2026-04-24).
  ```
  </pre>

### Thread 2 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM59fQVR`
- **Path**: `tests/Tests.FSharp/Formal/Sharder.InfoTheoretic.Tests.fs`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-24T19:13:47Z on `tests/Tests.FSharp/Formal/Sharder.InfoTheoretic.Tests.fs`:69 (association: NONE)

  <pre>
  The DST-exempt marker references the BACKLOG row for the “Uniform traffic” flake, but it’s attached to the `InfoTheoreticSharder does not make skew worse` test. Consider moving/duplicating the marker so it sits on the actual non-DST/flaky test(s) (e.g., `Uniform traffic: ...` and/or any `[&lt;Fact&gt;]` here using `HashCode.Combine`), or adjust the referenced row/test name so the cross-reference is unambiguous.
  </pre>

### Thread 3 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM59fQVl`
- **Path**: `docs/BACKLOG.md`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-24T19:13:47Z on `docs/BACKLOG.md`:5271 (association: NONE)

  <pre>
  The reference `project_escro_maintain_every_dependency_microkernel_os_endpoint_grow_our_way_there_2026_04_22.md` looks like a path typo: the file lives under `memory/` in this repo (`memory/project_escro_maintain_every_dependency_microkernel_os_endpoint_grow_our_way_there_2026_04_22.md`). Please update the inline reference so it resolves correctly.
  ```suggestion
  - [ ] **.NET 10 Server GC crash on Apple Silicon — bullet-proof investigation + upstream report (Otto-248 workaround follow-up).** Shipped mitigation in PR #376 (`DOTNET_gcServer=0` on Apple Silicon Darwin via `tools/setup/common/shellenv.sh`) is a WORKAROUND, not a root-cause fix. Three SIGSEGV crashes captured 2026-04-24 at `~/Library/Logs/DiagnosticReports/dotnet-2026-04-24-*.ips` — all in `libcoreclr.dylib` Server GC (`SVR::gc_heap::{plan_phase, background_mark_simple1, find_first_object, revisit_written_page}`) with `EXC_BAD_ACCESS / KERN_INVALID_ADDRESS / "possible pointer authentication failure"` on macOS 26.4.1 MacBook M2 + .NET SDK 10.0.203 (latest). Current hypothesis: concurrent-mark-phase invariant violation, possibly write-barrier or card-table race; Apple Silicon ARM64 PAC surfaces it as SIGSEGV where x86 would silently corrupt. Two known related upstream fixes on dotnet/runtime `main` but NOT yet in release/10.0: `#126389` (Jan Vorlicek, 2026-04-01, BGC mark phase wrong-join — fixes *hangs* not crashes; symptom mismatch) and `#126929/#126977` (largepages corruption fix — open port-to-10.0 PR). Per maintainer directive 2026-04-24 Otto-254 ("roll forward, not backward" + escro absorb-and-contribute dependency-maintenance per `memory/project_escro_maintain_every_dependency_microkernel_os_endpoint_grow_our_way_there_2026_04_22.md`). Work scope: (1) build minimal deterministic repro — F# allocation-heavy Server GC program that reliably crashes on Apple Silicon, clean under Workstation GC; (2) measure crash rate across Apple-Silicon-Darwin vs ubuntu-24.04-arm vs x86 legs (differentiates Scenario A Apple-specific vs Scenario B ARM64-general); (3) read coreclr GC source at crash symbols (now split across `plan_phase.cpp`, `background.cpp`, `mark_phase.cpp` per 2026-Q1 refactor; sparse-checkout sibling `../runtime` already pulled for offline navigation); (4) file upstream `dotnet/runtime` issue with IPS files + reproducer IF deterministic + professional (not a dick per maintainer directive); (5) track upstream fix; (6) remove `DOTNET_gcServer=0` workaround once fix lands in a release/10.0 SDK patch. Priority P2 research-grade — workaround prevents daily blocker but the fix is escro-scope dependency maintenance. Effort M (repro + source read) + S (upstream file) + S (patch-landed cleanup). **Does NOT authorize:** reverting the workaround before upstream fix lands; filing upstream before reproducer is deterministic.
  ```
  </pre>

## Fix commits (touching thread paths)

### `58fae54e696eb3c138beb20b866ce6ebf25f9abd` -- 2026-04-24T19:10:21Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `docs/BACKLOG.md`
- `tests/Tests.FSharp/Formal/Sharder.InfoTheoretic.Tests.fs`

**Message:**

```
backlog: dotnet GC upstream investigation + DST-marker convention (Ot…

…to-248 followup)

Two new P2 research-grade BACKLOG rows + a concrete DST-exempt
marker on the first declared exception.

## Rows added

### .NET 10 Server GC crash — bullet-proof investigation

PR #376 shipped `DOTNET_gcServer=0` as a workaround; this row
tracks the proper root-cause investigation: deterministic repro,
coreclr GC source read, upstream `dotnet/runtime` issue filing,
fix-landing tracking, workaround removal. Per Otto-253 escro
framing (maintain every dependency) + Otto-254 roll-forward.
Sibling `../runtime` already sparse-checked-out for offline
navigation. 3 IPS files captured 2026-04-24 ready for the
upstream report once reproducer is deterministic.

### DST-marker convention + lint rule

Aaron directive 2026-04-24 *"mark you test, there is some lint
rule to call out why a test is not DST fully"* — Otto-248
never-ignore-flakes-per-DST discipline is aspirational without
mechanical markers. This row: decide marker shape (comment vs
attribute), write semgrep rule enforcing markers on non-DST
tests, backfill sweep. Composes with Otto-248 — makes "every
flake must be diagnosed" mechanically enforceable.

## Marker applied to first declared exception

`tests/Tests.FSharp/Formal/Sharder.InfoTheoretic.Tests.fs`
"InfoTheoreticSharder does not make skew worse" test annotated
with `// DST-exempt: uses HashCode.Combine which is process-
randomized per-run.` Points at the SharderInfoTheoretic row in
docs/BACKLOG.md for root-cause + fix pipeline.

Maintainer observation 2026-04-24: "probably [the rule] says no
exceptions now we got an exception" — the Uniform test is the
first declared DST exception. Precedent documented.

## Composes with

- Otto-248 flake discipline (this row makes it enforceable)
- Otto-250/251/252 training-signal framing (exception-markers
  with reasons = richer training data than unmarked flakes)
- Otto-253 escro dependency maintenance (dotnet GC row IS
  escro-scope)
- Otto-254 roll-forward (workaround stays, fix lands forward)
- Existing SharderInfoTheoreticTests row (root-cause analysis)

## Build

`dotnet build -c Release` → 0W/0E.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
```
