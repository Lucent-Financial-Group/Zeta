# PR #432 -- drain(#360 follow-up): 8 Codex shell-portability + allow-list-scope findings

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 432 |
| Title | drain(#360 follow-up): 8 Codex shell-portability + allow-list-scope findings |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-04-25T05:15:29Z |
| Merged at | 2026-04-25T06:04:01Z |
| Merge commit SHA | `60b197ca1066ca6bb323c872e87503fe213483c8` |
| Branch | `drain/360-followup-shell-portability-and-allowlist` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/432 |
| Changed files | 1 |
| Additions / deletions | +148 / -33 |

## Description

Codex post-merge review on #360 surfaced 8 findings (P0 + 5×P1 + 2×P2). Addressed in this follow-up.

| Finding | Severity | Fix |
|---|---|---|
| set -e + grep -v abort | P0 | `|| true` neutralises no-match |
| Validate against allow-list, not stale-subset | P1 | New NOT-ON-ALLOW-LIST scan |
| env-error vs stale-label exit code | P1 | Split env_error (exit 1) from fail (exit 2) |
| cd to REPO_ROOT first | P1 | git rev-parse + cd |
| Code comments explain code, not history | P1 | Removed Otto-NNN tokens from header |
| Exit-code contract mismatch | P1 | Header exit 3 removed; warn = exit 0 documented |
| Guard comment-stripping pipeline | P2 | Same || true fix |
| UTC vs local-time TZ skew | P2 | `TZ=UTC` on BSD date branch |

Smoke-test confirmed: gate.yml's ubuntu-22.04 pins are now flagged twice (STALE + NOT-ON-ALLOW-LIST), exit 2.


## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | true |
| Total threads | 9 |
| Resolved threads | 7 |
| Unresolved threads | 2 |
| Total review comments | 16 |
| Total fix commits (touching thread paths) | 2 |

## Review threads

### Thread 1 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59kAP9`
- **Path**: `tools/lint/runner-version-freshness.sh`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-04-25T05:18:02Z on `tools/lint/runner-version-freshness.sh`:202 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Initialize `warn` before nounset reads it**

  With `set -u` enabled, `warn` must always be defined before the final `if [ "$warn" = "1" ]` check. This commit removed the default initialization, so when `_verify_age_ok` returns success and no stale labels are found, the script aborts with `warn: unbound variable` instead of returning success (e.g., linting a clean workflow file). That turns a passing lint into an environment error.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-25T06:02:11Z on `tools/lint/runner-version-freshness.sh`:202 (association: MEMBER)

    <pre>
    Fixed in 98ce4412cdb9ad4517c314735838d330a3e23541. Initialized `warn=0` alongside `fail=0` and `env_error=0` near the top of the main loop. Real regression — `set -u` would have aborted any clean-lint run. Resolving.
    </pre>

### Thread 2 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59kAP-`
- **Path**: `tools/lint/runner-version-freshness.sh`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-04-25T05:18:02Z on `tools/lint/runner-version-freshness.sh`:244 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Escape allow-list entries before composing ERE**

  The allow-list regex is built from raw label strings, but several labels contain `.` (for example `ubuntu-24.04`), which is a wildcard in ERE. In the `NOT-ON-ALLOW-LIST` filter this makes typos like `ubuntu-24x04` match as if they were allow-listed, so unknown runner labels can slip through undetected. Reusing `escape_for_regex` for `ALLOWED_LABELS` would keep this check literal and reliable.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-25T06:02:13Z on `tools/lint/runner-version-freshness.sh`:244 (association: MEMBER)

    <pre>
    Fixed in 98ce4412cdb9ad4517c314735838d330a3e23541. ALLOWED_LABELS and ROLLING_ALIASES are now both escaped via `escape_for_regex` before being composed into the ERE alternation, matching the existing STALE_LABELS treatment. `ubuntu-24x04`-style typos can no longer slip through. Resolving.
    </pre>

### Thread 3 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59kApl`
- **Path**: `tools/lint/runner-version-freshness.sh`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-25T05:18:59Z on `tools/lint/runner-version-freshness.sh`:321 (association: NONE)

  <pre>
  P0: `warn` is never initialized, but the script later evaluates `[ "$warn" = "1" ]` under `set -u`, which will abort with an “unbound variable” error whenever `_verify_age_ok` returns success (the common case). Initialize `warn=0` (or similar) up-front, or restructure the logic to avoid reading an unset variable.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-25T06:02:14Z on `tools/lint/runner-version-freshness.sh`:321 (association: MEMBER)

    <pre>
    Fixed in 98ce4412cdb9ad4517c314735838d330a3e23541. Initialized `warn=0` at the same point as `fail=0` and `env_error=0`. The final `[ "$warn" = "1" ]` check no longer aborts under `set -u`. Resolving.
    </pre>

### Thread 4 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59kApt`
- **Path**: `tools/lint/runner-version-freshness.sh`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-25T05:19:00Z on `tools/lint/runner-version-freshness.sh`:56 (association: NONE)

  <pre>
  P1: `cd "$REPO_ROOT"` happens before `files=("$@")` is consumed. If the caller passes file paths relative to their current working directory (per the usage comment), those paths will no longer resolve after the `cd`, and the script will report `env_error` instead of linting. Consider normalizing CLI args to absolute paths before `cd`, or document/enforce that file args must be repo-root-relative. Also, other `tools/lint/*.sh` scripts appear to compute `REPO_ROOT` from the script path rather than requiring `git`, which would avoid a new dependency on `git` being present.
  ```suggestion
  # Normalize explicit file arguments against the caller's
  # cwd before changing directories. This preserves the
  # documented CLI contract for `tools/lint/runner-version-
  # freshness.sh &lt;file&gt;...` when invoked outside the repo
  # root.
  CALLER_CWD="$(pwd -P)"
  if [ "$#" -gt 0 ]; then
    normalized_args=()
    for file in "$@"; do
      case "$file" in
        /*)
          normalized_args+=("$file")
          ;;
        *)
          normalized_args+=("$CALLER_CWD/$file")
          ;;
      esac
    done
    set -- "${normalized_args[@]}"
  fi

  # Resolve REPO_ROOT from this script's location and cd
  # there so the default `find .github/workflows ...`
  # discovery works regardless of the caller's cwd.
  if ! SCRIPT_DIR="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" &amp;&amp; pwd -P)"; then
    echo "ERROR: unable to resolve script directory" &gt;&amp;2
    exit 1
  fi
  if ! REPO_ROOT="$(cd -- "$SCRIPT_DIR/../.." &amp;&amp; pwd -P)"; then
    echo "ERROR: unable to resolve repository root" &gt;&amp;2
    exit 1
  fi
  ```
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-25T06:02:16Z on `tools/lint/runner-version-freshness.sh`:56 (association: MEMBER)

    <pre>
    Fixed in 98ce4412cdb9ad4517c314735838d330a3e23541. CLI args are now normalized to absolute paths BEFORE the `cd "$REPO_ROOT"`, so paths given relative to the caller's cwd survive. Smoke-tested: `bash tools/lint/runner-version-freshness.sh ../.github/workflows/codeql.yml` from `docs/` now resolves correctly. Kept the git-required REPO_ROOT discovery (matches the rest of the script's git-tree-required posture). Resolving.
    </pre>

### Thread 5 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59kApy`
- **Path**: `tools/lint/runner-version-freshness.sh`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-25T05:19:00Z on `tools/lint/runner-version-freshness.sh`:227 (association: NONE)

  <pre>
  P1: The comment says `|| true` is needed on the `grep` side, but `cmd1 | sed ... || true` applies to the entire pipeline under `pipefail`, which will also mask real `sed` failures (e.g., missing `sed`, unsupported `-E`, etc.) and could cause the script to silently lint an empty string. Prefer grouping so only the expected `grep` exit-1 (no output) is neutralized, while genuine pipeline failures still surface as an environment/usage error.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-25T06:02:18Z on `tools/lint/runner-version-freshness.sh`:227 (association: MEMBER)

    <pre>
    Fixed in 98ce4412cdb9ad4517c314735838d330a3e23541. Restructured to `{ grep -vE ... || true; } | sed` so only the expected `grep` no-output exit-1 is neutralized; a real `sed` failure (missing tool, unsupported `-E`) now surfaces as an environment error instead of silently linting an empty string. Resolving.
    </pre>

### Thread 6 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59kAp2`
- **Path**: `tools/lint/runner-version-freshness.sh`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-25T05:19:00Z on `tools/lint/runner-version-freshness.sh`:245 (association: NONE)

  <pre>
  P1: `allowed_pattern` is built from raw `ALLOWED_LABELS` values but then used inside an ERE alternation. Labels like `ubuntu-24.04` contain regex metacharacters (`.`) and should be escaped (similar to how `STALE_LABELS` are escaped) to avoid accidental matches (e.g., allowing typos like `ubuntu-24x04`). Consider reusing `escape_for_regex` for `ALLOWED_LABELS` (and any other list used in an alternation).
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-25T06:02:19Z on `tools/lint/runner-version-freshness.sh`:245 (association: MEMBER)

    <pre>
    Fixed in 98ce4412cdb9ad4517c314735838d330a3e23541. ALLOWED_LABELS is now escaped via `escape_for_regex` (same pattern as STALE_LABELS) before being composed into the ERE alternation. Typos like `ubuntu-24x04` no longer slip past the allow-list filter. Resolving.
    </pre>

### Thread 7 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59kAp6`
- **Path**: `tools/lint/runner-version-freshness.sh`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-25T05:19:00Z on `tools/lint/runner-version-freshness.sh`:252 (association: NONE)

  <pre>
  P1: The new NOT-ON-ALLOW-LIST scan only checks scalar `runs-on: &lt;label&gt;` values, but it explicitly skips expression-form `runs-on: ${{ ... }}`. In workflows that use `runs-on: ${{ matrix.os }}` (present in this repo), an unknown matrix entry (e.g., `ubuntu-30.04`) would not be caught by this allow-list validation. Consider extending the allow-list validation to also scan matrix list entries (the same `matrix_prefix` forms you already prefilter for stale/rolling), so expression-driven `runs-on` is still validated via its matrix values.
  ```suggestion
    # Validate both direct scalar `runs-on:` labels and the
    # matrix list entries that feed expression-form
    # `runs-on: ${{ matrix.* }}` usage. Keep skipping the
    # expression-form `runs-on:` line itself; validate the
    # concrete matrix values instead.
    unknown_hits="$(printf '%s\n' "$uncommented" \
      | grep -nE "runs-on:[[:space:]]*[A-Za-z0-9_-][A-Za-z0-9._-]*|${matrix_prefix}[A-Za-z0-9_-][A-Za-z0-9._-]*" \
      | grep -vE 'runs-on:[[:space:]]*\$\{\{' \
      | grep -vE "runs-on:[[:space:]]*(${rolling_or_allowed})($|[[:space:]]|#)|${matrix_prefix}(${rolling_or_allowed})${nonword_end}" \
  ```
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-25T06:02:21Z on `tools/lint/runner-version-freshness.sh`:252 (association: MEMBER)

    <pre>
    Fixed in 98ce4412cdb9ad4517c314735838d330a3e23541. Extended the NOT-ON-ALLOW-LIST scan to also validate matrix list entries (`- ubuntu-24.04` / `- "ubuntu-24.04"` form), matching the existing `matrix_prefix` shape used for stale/rolling. The exclude filter uses a line-number-aware prefix `(^|^[0-9]+:)` because `grep -n` prepends `&lt;linenum&gt;:` which would otherwise break the `^` anchor. Smoke-tested: matrix entries `macos-26` / `ubuntu-24.04` / `ubuntu-24.04-arm` are correctly excluded as allow-listed; existing stale `ubuntu-22.04` findings unchanged. Resolving.
    </pre>

### Thread 8 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM59kJ3q`
- **Path**: `tools/lint/runner-version-freshness.sh`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-04-25T06:04:03Z on `tools/lint/runner-version-freshness.sh`:300 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Scope matrix allow-list scan to runner labels only**

  The new `matrix_unknown` matcher currently inspects any list item that starts with a letter and contains a digit, regardless of whether it is a runner label, and then treats matches as fatal (`fail=1`). In practice this flags common non-runner matrices (for example `matrix.dotnet: [dotnet-9.0]`) as `NOT-ON-ALLOW-LIST` and exits 2 even when `runs-on` is valid, so legitimate workflows can start failing CI after this change. Restricting this path to runner-related matrix fields (or making it truly advisory) would avoid these false positives.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 9 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM59kJ3r`
- **Path**: `tools/lint/runner-version-freshness.sh`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-04-25T06:04:03Z on `tools/lint/runner-version-freshness.sh`:282 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Include quoted `runs-on` scalars in allow-list validation**

  The `scalar_unknown` regex only matches unquoted scalar values after `runs-on:`, so quoted values are skipped by the new not-on-allow-list check. As a result, a workflow using `runs-on: "ubuntu-30.04"` passes this guard unless the label is already in `STALE_LABELS`, which defeats the purpose of catching unknown future pins. Supporting an optional quote in this matcher would close that bypass.

  Useful? React with 👍 / 👎.
  </pre>

## Fix commits (touching thread paths)

### `58234c4ae00bbaaa02190b380e31ebc3f40ca2ff` -- 2026-04-25T05:15:27Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/lint/runner-version-freshness.sh`

**Message:**

```
drain(#360 follow-up: 8 Codex P0/P1/P2 findings on shell portability …

…+ allow-list scope)

P0 (line 187) — set -e + grep -v abort:
`grep -vE ... | sed ...` aborts under set -euo pipefail
when grep -v outputs nothing (every line is a comment).
Added `|| true` to neutralise the exit 1.

P1 (line 195) — validate against allow-list, not stale-subset:
Old code only flagged labels in STALE_LABELS. A label like
`ubuntu-30.04` invented after this script's last refresh
would silently pass. Added a third scan: extract `runs-on:
<value>` and flag anything not in (ALLOWED ∪ ROLLING) and
not in expression form `${{ ... }}`. Distinct error class
'NOT-ON-ALLOW-LIST RUNNER LABEL'.

P1 (line 174) — env-error vs stale-label exit code:
Unreadable files set fail=1 → exit 2 (stale labels). That
mixed environment errors with content findings. Split into
`env_error` (exit 1) vs `fail` (exit 2). Header exit-code
contract updated.

P1 (line 142) — convention: cd to REPO_ROOT:
Other tools/lint/*.sh scripts establish REPO_ROOT and cd
there. Aligned: `git rev-parse --show-toplevel` + cd to
REPO_ROOT before file discovery.

P1 (line 9) — code comments explain code, not history:
Removed Otto-213 / Otto-214 lineage tokens from the header.
Replaced 'Otto-213 durable compounding-failure mitigation'
with the structural rationale (training-data version
numbers decay; structural lint enforces).

P1 (line 237) — exit-code contract reconciliation:
Earlier reviewer wanted warn-only path to exit 0 (so
freshness warning doesn't fail CI). This reviewer pointed
out the header still claimed exit 3 for warn. Updated
header explicitly: 'exit 0 even with freshness warning;
warning is on stderr, non-fatal'. Removed exit 3 from
exit-code contract; now only 0/1/2.

P2 (line 188) — guard comment-stripping pipeline:
Same as P0 fix above (`|| true` neutralises grep -v's
no-match exit 1).

P2 (line 116) — UTC vs local-time TZ skew:
`now_epoch` was UTC but BSD `date -j -f` defaults to local
time. Forced `TZ=UTC` on both branches so age_days computes
in a single timezone (no ±1 day skew across DST/timezones).
```

### `98ce4412cdb9ad4517c314735838d330a3e23541` -- 2026-04-25T06:01:49Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/lint/runner-version-freshness.sh`

**Message:**

```
drain(#360 follow-up): fix Codex P0/P1/P2 + Copilot findings

P0 (Copilot L265) / P1 (Codex L185) — `warn` unbound under `set -u`:
  Initialize `warn=0` alongside `fail=0` and `env_error=0`. Without this,
  `_verify_age_ok` returning success leaves `warn` unset; the final
  `[ "$warn" = "1" ]` check then aborts with "unbound variable" — turning
  passing-lint into env-error. Real regression.

P1 (Copilot L56) — `cd "$REPO_ROOT"` before consuming `$@`:
  Normalize CLI args to absolute paths BEFORE the chdir into REPO_ROOT, so
  paths given relative to the caller's cwd survive. Without this,
  `script.sh ./foo.yml` from outside REPO_ROOT errors after the cd.

P1 (Copilot L213) — `|| true` masking real `sed` failures:
  Group only the `grep` with `|| true` (`{ grep -vE ... || true; } | sed`)
  so a real sed failure (missing tool / unsupported -E) still surfaces;
  before, pipefail propagated through `cmd1 | sed ... || true`, hiding
  legitimate environment errors.

P1 (Codex L244 / Copilot L245) — escape ALLOWED_LABELS for ERE:
  Labels like `ubuntu-24.04` contain `.` (ERE wildcard); without escaping,
  typos like `ubuntu-24x04` would slip through as "allow-listed". Reuses
  `escape_for_regex` for ALLOWED + ROLLING lists.

P1 (Copilot L252) — extend NOT-ON-ALLOW-LIST scan to matrix entries:
  Previously skipped `runs-on: ${{ matrix.os }}` workflows entirely. Now
  validates matrix list entries against the allow-list using a
  line-number-aware exclude prefix `(^|^[0-9]+:)` (grep -n prepends
  `<linenum>:` which would otherwise break the `^` anchor in the exclude
  filter). Smoke-tested: matrix entries `macos-26` / `ubuntu-24.04` /
  `ubuntu-24.04-arm` correctly excluded as allow-listed; existing stale
  `ubuntu-22.04` findings unchanged.

Verified: clean lint pass on `.github/workflows/codeql.yml`, stale-label
detection unchanged on `.github/workflows/gate.yml`, relative-path arg
from a subdirectory now resolves correctly.
```
