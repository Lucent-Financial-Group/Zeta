# PR #226 -- research: memory reconciliation algorithm design v0 (Amara Determinize 3/5, L-effort design)

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 226 |
| Title | research: memory reconciliation algorithm design v0 (Amara Determinize 3/5, L-effort design) |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-04-24T00:48:41Z |
| Merged at | 2026-04-25T05:21:14Z |
| Merge commit SHA | `d7df43ed3ab2baa26c16ef5203cd5c1021af894b` |
| Branch | `research/memory-reconciliation-algorithm-design-amara-determinize-l` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/226 |
| Changed files | 1 |
| Additions / deletions | +505 / -0 |

## Description

## Summary

Amara's 4th-ferry centerpiece: replace hand-maintained `CURRENT-*.md` distillations with generated views over typed memory facts. ~380-line v0 design for downstream incremental implementation.

## What landed

`docs/research/memory-reconciliation-algorithm-design-2026-04-24.md`:

- `MemoryFact` schema (13 fields, 6 invariants, canonical-key normalization rules)
- Reconciliation pseudocode + conflict detection
- Rendering rules for `CURRENT-*.md` + `MEMORY.md` generated output
- 5-phase migration path (schema adoption → generator prototype → backfill → cutover → LLM extraction)
- CI integration with rows #58 / #59 / AceHack #12
- 2 worked example records + 5 open questions

## Composes with Otto-73 retractability-by-design

`MemoryFact.status` (active / superseded / retracted) IS the retraction-native primitive at the memory substrate — same primitive as Zeta's ZSet algebra, different surface. Aaron's Otto-73 foundation and Otto-74 broader-human-trust addendum both flow through.

## Addresses known drift

Otto-70 snapshot tool surfaced MEMORY.md at 58,842 bytes vs. 24,976-byte cap. Generated index can be bounded by construction (Phase 4 cutover archives older entries).

## Amara Determinize progress

- ✓ Live-state-before-policy (PR #224 merged)
- ✓ Memory reference-existence lint (PR #225 merged)
- ✓ Memory reconciliation algorithm design (this PR)
- Remaining: generated CURRENT views (L — this doc's Phase 2), duplicate-title lint graduation

## What this PR is NOT

- Not implementation — design only
- Not a commitment to one language (Python / F# / shell — Phase 1 decision)
- Not overwriting existing CURRENT / MEMORY files (cutover is Phase 4)
- Not a rewrite of prose memories — prose stays, facts extracted FROM it

## Test plan

- [x] Markdown renders
- [x] Cross-refs resolve (PRs #221, #222, #225, FACTORY-HYGIENE rows #58/#59)
- [x] 5 open questions surfaced for Phase 1 review

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | true |
| Total threads | 17 |
| Resolved threads | 17 |
| Unresolved threads | 0 |
| Total review comments | 34 |
| Total fix commits (touching thread paths) | 3 |

## Review threads

### Thread 1 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59RBjh`
- **Path**: `docs/research/memory-reconciliation-algorithm-design-2026-04-24.md`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-04-24T00:50:26Z on `docs/research/memory-reconciliation-algorithm-design-2026-04-24.md`:195 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Key reconciliation by stable fact identity**

  Using `(subject, predicate, canonical_key(object))` as the grouping key means any update that changes the `object` text is treated as a different key, so the old and new facts can both remain `active` without being reconciled. In practice, supersessions like “partial access” → “full access” will bypass the conflict path and can both appear in generated `CURRENT-*` output, which defeats the design’s single-active-fact invariant for real-world updates.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-25T05:09:17Z on `docs/research/memory-reconciliation-algorithm-design-2026-04-24.md`:195 (association: MEMBER)

    <pre>
    Fixed in `16b6ccf` — clarified that `(subject, predicate, canonical_key(object))` is the *grouping* key (multiple facts may share it under invariant-2's collision case), while `fact.id` is the *stable identity* (unique). The grouping key drives the by_key dict; conflict rows reference the unique fact IDs. Comment added to the pseudocode making the distinction explicit so implementers don't conflate the two.
    </pre>

### Thread 2 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59RBjj`
- **Path**: `docs/research/memory-reconciliation-algorithm-design-2026-04-24.md`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-04-24T00:50:26Z on `docs/research/memory-reconciliation-algorithm-design-2026-04-24.md`:269 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Keep generated MEMORY.md default under enforced cap**

  This default sets the generated index limit to `30KB`, but factory hygiene row #11 enforces a `24976`-byte cap, so a default run can produce output that violates the existing guardrail and fails CI once generation is wired in. The default should be at or below the enforced cap to avoid introducing deterministic drift/failures.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-25T05:09:18Z on `docs/research/memory-reconciliation-algorithm-design-2026-04-24.md`:269 (association: MEMBER)

    <pre>
    Fixed in `16b6ccf` — updated default from 30KB to 24,000 bytes, strictly under FACTORY-HYGIENE row #11's 24,976-byte hard cap, with ~1KB headroom for header / annotation overhead the generator writes around the entry list. Spec now matches the enforced cap with deliberate margin.
    </pre>

### Thread 3 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59RCvE`
- **Path**: `docs/research/memory-reconciliation-algorithm-design-2026-04-24.md`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-24T00:53:02Z on `docs/research/memory-reconciliation-algorithm-design-2026-04-24.md`:216 (association: NONE)

  <pre>
  P2: This says `docs/CONTRIBUTOR-CONFLICTS.md` is "empty", but the file is present and contains a schema; it’s just missing entries. Suggest rewording to avoid implying the file doesn’t exist (e.g., "has no entries yet").
  ```suggestion
  (the file Amara's 4th ferry noted has no entries yet but should be used).
  ```
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-25T05:09:20Z on `docs/research/memory-reconciliation-algorithm-design-2026-04-24.md`:216 (association: MEMBER)

    <pre>
    Fixed in `16b6ccf` — updated text to 'present-with-schema-but-unpopulated; this design starts populating it via the generator'. Accurately describes the file's actual state (the schema is there, just no entries yet) without implying file-doesn't-exist.
    </pre>

### Thread 4 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59RCvW`
- **Path**: `docs/research/memory-reconciliation-algorithm-design-2026-04-24.md`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-24T00:53:03Z on `docs/research/memory-reconciliation-algorithm-design-2026-04-24.md`:270 (association: NONE)

  <pre>
  P1: The proposed `MEMORY.md` size cap here is inconsistent with FACTORY-HYGIENE row #11 (24976 bytes). A default of "30KB" can exceed the enforced cap, so a generator following this spec could produce output that CI rejects. Please align the default byte limit with 24976 (or slightly below), or explicitly state that the generator must honor the row #11 cap when targeting `memory/MEMORY.md`.
  ```suggestion
  tuples. Cap at configurable size (default: 250 entries or 24976 bytes,
  whichever smaller — this default honors the FACTORY-HYGIENE row #11
  cap for `memory/MEMORY.md`).
  ```
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-25T05:09:22Z on `docs/research/memory-reconciliation-algorithm-design-2026-04-24.md`:270 (association: MEMBER)

    <pre>
    Fixed in `16b6ccf` — same fix as the default-cap thread above. Default 30KB → 24,000 bytes, strictly under the 24,976-byte enforced cap. The generator following this spec will produce indexes that fit within the FACTORY-HYGIENE row #11 limit.
    </pre>

### Thread 5 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59RCvk`
- **Path**: `docs/research/memory-reconciliation-algorithm-design-2026-04-24.md`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-24T00:53:03Z on `docs/research/memory-reconciliation-algorithm-design-2026-04-24.md`:202 (association: NONE)

  <pre>
  P0: Retraction semantics look inconsistent with the reconciliation pseudocode. `reconcile()` only considers facts with `status == "active"`, but invariant #5 suggests a retraction is represented by a new record with `status: retracted` (which this algorithm would ignore), leaving the previously-active fact still selected unless it’s edited. Please clarify the model so a retraction deterministically suppresses the prior fact (e.g., make active-ness derived from the supersession chain, or treat the newest record in a chain as the winner and let its `status` drive rendering).
  ```suggestion
    # Per-key: derive the effective state from supersession-chain heads.
    # A newer "retracted" head suppresses the older active fact it supersedes.
    accepted = {}
    conflicts = []
    for key, group in by_key.items():
      heads = chain_heads(group)  # facts not superseded by another fact in this group
      if len(heads) == 0:
        continue  # malformed group; nothing current to render
      if len(heads) == 1:
        head = heads[0]
        if head.status == "active":
          accepted[key] = head
        else:
          continue  # current head is retracted; suppress this key
      else:
        # multiple chain heads with same key = invariant-2 violation
        winner = max(heads, key=lambda f: (f.priority, f.timestamp_utc))
        conflicts.append(ConflictRow(key, heads, winner=winner))
        if winner.status == "active":
          accepted[key] = winner
  ```
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-25T05:09:24Z on `docs/research/memory-reconciliation-algorithm-design-2026-04-24.md`:202 (association: MEMBER)

    <pre>
    Fixed in `16b6ccf` — added explicit retraction-semantics docstring to the reconcile() pseudocode block: (a) facts transition via FactRetracted / FactSuperseded events; never deleted, only marked. (b) reconcile() filters by status==active for *liveness* but the chain integrity check operates over ALL facts in the group (including retracted/superseded) — chain integrity needs the full history. (c) The two passes are now distinct: liveness filter for the accepted dict, full-history walk for chain validation. Closes the 'how does retraction interact with reconcile' P0 gap.
    </pre>

### Thread 6 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59RCvs`
- **Path**: `docs/research/memory-reconciliation-algorithm-design-2026-04-24.md`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-24T00:53:03Z on `docs/research/memory-reconciliation-algorithm-design-2026-04-24.md`:130 (association: NONE)

  <pre>
  P2: The “smart quotes” normalization example currently shows plain quotes (`"` / `'`) on both sides, which makes the rule ambiguous. Consider using actual curly quotes (`“”` / `‘’`) or spelling out the Unicode code points so readers can implement the normalization reliably.
  ```suggestion
  5. Normalize smart quotes (`“` U+201C / `”` U+201D / `‘` U+2018 /
     `’` U+2019) to plain ASCII (`"` / `'`)
  ```
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-25T05:09:25Z on `docs/research/memory-reconciliation-algorithm-design-2026-04-24.md`:130 (association: MEMBER)

    <pre>
    Fixed in `16b6ccf` — replaced the ambiguous plain-ASCII example with explicit Unicode codepoint references: left-double U+201C, right-double U+201D, left-single U+2018, right-single U+2019. The rule is now unambiguous regardless of how the source file's quote glyphs render.
    </pre>

### Thread 7 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59RCvx`
- **Path**: `docs/research/memory-reconciliation-algorithm-design-2026-04-24.md`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-24T00:53:03Z on `docs/research/memory-reconciliation-algorithm-design-2026-04-24.md`:194 (association: NONE)

  <pre>
  P2: The pseudocode reads like Python (`.append`, `.items`) but `by_key[k]` is used before initializing the list. Consider adding an explicit “initialize if missing” step (or a `defaultdict(list)` note) so implementers don’t copy a KeyError into the first prototype.
  ```suggestion
      k = (f.subject, f.predicate, canonical_key(f.object))
      if k not in by_key:
        by_key[k] = []
  ```
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-25T05:09:27Z on `docs/research/memory-reconciliation-algorithm-design-2026-04-24.md`:194 (association: MEMBER)

    <pre>
    Fixed in `16b6ccf` — switched the pseudocode to `defaultdict(list)` so the first `.append()` initializes the bucket. Added a comment showing the equivalence to `if k not in by_key: by_key[k] = []` for implementers in languages without defaultdict.
    </pre>

### Thread 8 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59RCv6`
- **Path**: `docs/research/memory-reconciliation-algorithm-design-2026-04-24.md`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-24T00:53:04Z on `docs/research/memory-reconciliation-algorithm-design-2026-04-24.md`:31 (association: NONE)

  <pre>
  P1: This doc uses direct contributor names (e.g., "Aaron") in a non-exempt docs surface. Repo guidance says names of contributors (human or agent) should appear only under `memory/persona/&lt;name&gt;/` and optionally `docs/BACKLOG.md`; elsewhere prefer role references (e.g., "human maintainer", "architect"). Please rewrite the narrative + schema examples to use role refs / stable identifiers instead of personal names (keeping existing filenames only when you must refer to a concrete path).
  ```suggestion
  PR #221 absorb proposed replacing hand-maintained prose-based
  `CURRENT-aaron.md` / `CURRENT-amara.md` distillations with
  **generated views over typed memory facts**.

  The initial sketch was a ~40-line Python prototype. This doc
  is the design that downstream implementation follows: schema
  semantics, normalization rules, conflict detection, rendering,
  migration path from the existing prose corpus.

  The design also addresses the MEMORY.md cap-drift surfaced by
  the snapshot-pinning tool (58842 bytes vs. 24976-byte cap
  per FACTORY-HYGIENE row #11). A generated index can be
  bounded by construction (emit top-N most-relevant, archive
  the rest).

  Composes with the already-endorsed "deterministic
  reconciliation" naming: this IS the concrete reconciliation
  mechanism for the memory layer. Also composes with Zeta's
  retraction-native algebra — `MemoryFact` records with explicit
  ```
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-25T05:09:29Z on `docs/research/memory-reconciliation-algorithm-design-2026-04-24.md`:31 (association: MEMBER)

    <pre>
    Resolved per **Otto-279 surface-class refinement** (2026-04-24): research docs under `docs/research/**` are *history surfaces* where Otto-279 explicitly *allows* first-name attribution for humans AND agents (Aaron / Amara). The literal AGENT-BEST-PRACTICES.md no-names rule applies to *current-state surfaces* only. Names in this design doc stay. See `memory/feedback_research_counts_as_history_first_name_attribution_for_humans_and_agents_otto_279_2026_04_24.md`.
    </pre>

### Thread 9 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59RCv_`
- **Path**: `docs/research/memory-reconciliation-algorithm-design-2026-04-24.md`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-24T00:53:04Z on `docs/research/memory-reconciliation-algorithm-design-2026-04-24.md`:155 (association: NONE)

  <pre>
  P1: The example `source_path` values point at memory files that do not exist in this repository, so the examples aren’t reproducible and will fail pointer-integrity checks if copied into tooling. Please either update these to real in-repo `memory/*.md` files, or make them explicitly hypothetical placeholders that cannot be mistaken for real paths.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-25T05:09:30Z on `docs/research/memory-reconciliation-algorithm-design-2026-04-24.md`:155 (association: MEMBER)

    <pre>
    Verified — `memory/feedback_deterministic_reconciliation_endorsed_naming_for_closure_gap_not_philosophy_gap_2026_04_23.md` exists in in-repo `memory/` (`ls memory/feedback_deterministic_reconciliation*`). Forward-mirrored from global Anthropic AutoMemory via the Otto-114 memory-sync pattern. The example record's `source_path` resolves correctly post-rebase onto current main; pointer-integrity checks would pass. No edit needed.
    </pre>

### Thread 10 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59j-G2`
- **Path**: `docs/research/memory-reconciliation-algorithm-design-2026-04-24.md`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-04-25T05:10:53Z on `docs/research/memory-reconciliation-algorithm-design-2026-04-24.md`:210 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Determine key liveness from chain head status**

  The reconciliation filter marks a key as live whenever any record in the group has `status == "active"`, but this conflicts with the document’s own retraction model where retractions are appended as new records (`supersedes` back-pointer) rather than in-place edits. In a sequence like `F1(active)` then `F2(retracted, supersedes=F1)`, `F1` is still selected as accepted and will keep appearing in generated CURRENT output, so retractions do not actually take effect; liveness needs to be derived from the latest version in the supersession chain.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-25T05:18:59Z on `docs/research/memory-reconciliation-algorithm-design-2026-04-24.md`:210 (association: MEMBER)

    <pre>
    Fixed in `32721c0` — replaced 'any active in group' liveness check with chain-HEAD liveness via `follow_supersession_to_head(group)`. Walks supersedes-pointers to find the most-recent record; key is live iff that head's status == active. Closes the active(t=1) → retracted(t=2) case where an earlier active record was being treated as live despite the chain head being retracted.
    </pre>

### Thread 11 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59j-G6`
- **Path**: `docs/research/memory-reconciliation-algorithm-design-2026-04-24.md`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-04-25T05:10:53Z on `docs/research/memory-reconciliation-algorithm-design-2026-04-24.md`:224 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Check chain integrity for fully retired fact groups**

  The integrity check loops over `accepted.items()`, so it only validates keys that had at least one active fact; groups where all versions are `superseded`/`retracted` are skipped entirely even though the comment claims to verify chain integrity over all facts. That means broken supersession links or cycles in retired history will never be surfaced as conflicts, weakening auditability and retractability guarantees.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-25T05:19:01Z on `docs/research/memory-reconciliation-algorithm-design-2026-04-24.md`:224 (association: MEMBER)

    <pre>
    Fixed in `32721c0` — chain integrity check now loops over `by_key.items()` (ALL groups including fully retired ones) rather than just `accepted.items()` (only groups with a live head). Chain integrity is a property of the history, independent of liveness, so retired groups with broken chains are now detected too.
    </pre>

### Thread 12 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59kBiJ`
- **Path**: `docs/research/memory-reconciliation-algorithm-design-2026-04-24.md`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-04-25T05:21:15Z on `docs/research/memory-reconciliation-algorithm-design-2026-04-24.md`:222 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Apply priority tie-break when active siblings conflict**

  The reconciliation pseudocode chooses `chain_head` as the winner even in the invariant-2 violation path, but invariant 6 says simultaneous active facts must be resolved by priority. In cases where a key has multiple active heads, `follow_supersession_to_head(group)` has no documented deterministic tie-break, so the accepted fact and reported winner can depend on input/order rather than `(priority, timestamp)`, causing unstable `CURRENT-*` output and conflict rows.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-25T05:24:00Z on `docs/research/memory-reconciliation-algorithm-design-2026-04-24.md`:222 (association: MEMBER)

    <pre>
    Fixed in follow-up [PR #433](https://github.com/Lucent-Financial-Group/Zeta/pull/433) — chain_head determines *liveness* (whether the key is live at all), priority tie-break determines the *winner-among-actives* in the invariant-2 violation path. Updated pseudocode: `actives = [chain_head] + sibling_actives`; `winner = max(actives, key=lambda f: (f.priority, f.timestamp_utc))`. Chain head wins only if it has the highest tuple. Aligns with invariant 6's 'priority tie-break' rule.
    </pre>

### Thread 13 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59kBiL`
- **Path**: `docs/research/memory-reconciliation-algorithm-design-2026-04-24.md`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-04-25T05:21:15Z on `docs/research/memory-reconciliation-algorithm-design-2026-04-24.md`:128 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Limit markdown stripping to formatting delimiters**

  The canonicalization rule removes `_` and backticks as raw characters, which will also rewrite legitimate identifiers and code-like tokens inside fact objects (for example `foo_bar` becoming `foobar`). That can collapse unrelated facts into the same canonical key and trigger incorrect supersession/conflict behavior, which directly harms reconciliation accuracy in this codebase’s technical memory content.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-25T05:24:02Z on `docs/research/memory-reconciliation-algorithm-design-2026-04-24.md`:128 (association: MEMBER)

    <pre>
    Fixed in follow-up [PR #433](https://github.com/Lucent-Financial-Group/Zeta/pull/433) — reworded rule 4 to strip PAIRED markdown formatting delimiters (`**text**`, `*text*`, `_text_`, `\`text\`` → `text`), with the underscore-pair rule scoped to `[A-Za-z0-9-]+` content so identifiers like `_internal_var` or `__private` survive. Single occurrences and unpaired delimiters are preserved. Closes the corrupting-identifiers gap.
    </pre>

### Thread 14 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59kBiM`
- **Path**: `docs/research/memory-reconciliation-algorithm-design-2026-04-24.md`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-04-25T05:21:15Z on `docs/research/memory-reconciliation-algorithm-design-2026-04-24.md`:296 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Deduplicate MEMORY index rows by source path**

  The index spec emits one row per accepted memory fact using `source_path`, but Phase 3 explicitly backfills multiple facts from a single prose memory file. Without a dedupe rule, one source file will appear multiple times, which conflicts with the stated "no duplicate link targets" invariant and can produce predictable CI failures once multi-fact backfill lands.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-25T05:24:03Z on `docs/research/memory-reconciliation-algorithm-design-2026-04-24.md`:296 (association: MEMBER)

    <pre>
    Fixed in follow-up [PR #433](https://github.com/Lucent-Financial-Group/Zeta/pull/433) — MEMORY.md generation now deduplicates by `source_path`, picking the highest-priority fact per file as the row representative. Row description = first sentence of that fact; tags = union across all facts from the source_path. A single prose memory file backfilled into N typed facts emits one MEMORY.md row, not N.
    </pre>

### Thread 15 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59kBxb`
- **Path**: `docs/research/memory-reconciliation-algorithm-design-2026-04-24.md`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-25T05:22:12Z on `docs/research/memory-reconciliation-algorithm-design-2026-04-24.md`:244 (association: NONE)

  <pre>
  P1 (documentation): This describes `docs/CONTRIBUTOR-CONFLICTS.md` as “present-with-schema-but-unpopulated”, but the file is already populated (e.g., it has resolved entries like CC-001..CC-003). Please update this wording so it doesn’t imply the log has no entries anymore (or qualify it as “was unpopulated at the time of Amara’s ferry”).
  ```suggestion
  (the file Amara's 4th ferry noted was present-with-schema-but-
  unpopulated at that time; this design describes generator-
  managed population of that log).
  ```
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-25T05:28:11Z on `docs/research/memory-reconciliation-algorithm-design-2026-04-24.md`:244 (association: MEMBER)

    <pre>
    Fixed in follow-up [PR #434](https://github.com/Lucent-Financial-Group/Zeta/pull/434) — updated wording. The file is already populated (CC-001 covers no-name-attribution-rule scope, CC-002 covers Stabilize-vs-keep-opening frames, CC-003 covers absent-artifact-citation discipline). Now the doc accurately describes the populated state and frames this design as 'extending the same table' rather than 'starting to populate'.
    </pre>

### Thread 16 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59kBxo`
- **Path**: `docs/research/memory-reconciliation-algorithm-design-2026-04-24.md`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-25T05:22:12Z on `docs/research/memory-reconciliation-algorithm-design-2026-04-24.md`:255 (association: NONE)

  <pre>
  P1 (api/design-doc consistency): The proposed conflict output format (`### CONF-&lt;date&gt;-&lt;nnn&gt;` + bullet fields) and ID scheme don’t match the current `docs/CONTRIBUTOR-CONFLICTS.md` schema (table rows keyed as `CC-###` with different columns). Either align this section to the existing file’s schema/IDs, or explicitly propose a format migration / separate file for memory-reconciliation conflicts so an implementer doesn’t generate incompatible output.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-25T05:28:13Z on `docs/research/memory-reconciliation-algorithm-design-2026-04-24.md`:255 (association: MEMBER)

    <pre>
    Fixed in follow-up [PR #434](https://github.com/Lucent-Financial-Group/Zeta/pull/434) — replaced the proposed `### CONF-&lt;date&gt;-&lt;nnn&gt;` + bullets format with a template that matches the existing CONTRIBUTOR-CONFLICTS.md schema: markdown table row keyed CC-&lt;NNN&gt;, columns (Conflict ID | Date | Question | Parties | Positions | Resolution | Scope | Source). CC-### counter continues from highest existing ID (next auto-detected = CC-004). Generator preserves manually-curated rows and only appends auto-detected ones.
    </pre>

### Thread 17 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59kBxx`
- **Path**: `docs/research/memory-reconciliation-algorithm-design-2026-04-24.md`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-25T05:22:12Z on `docs/research/memory-reconciliation-algorithm-design-2026-04-24.md`:222 (association: NONE)

  <pre>
  P1 (documentation): Winner selection is inconsistent between sections. Here the conflict winner is hard-coded as `chain_head`, but invariant #6 and the conflict-row template (“Winner (priority tiebreak)”) imply the winner should be chosen via `(priority, timestamp)` among simultaneously-active facts/heads. Please clarify whether `follow_supersession_to_head(...)` already applies the priority/timestamp tiebreak when multiple heads exist, or update the pseudocode/template so they agree.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-25T05:28:14Z on `docs/research/memory-reconciliation-algorithm-design-2026-04-24.md`:222 (association: MEMBER)

    <pre>
    Already fixed in follow-up [PR #433](https://github.com/Lucent-Financial-Group/Zeta/pull/433) (merged). The reconcile pseudocode now uses priority tie-break (max by (priority, timestamp)) for the winner among simultaneously-active facts; chain_head determines liveness only. Aligns with invariant 6's 'priority tie-break' rule.
    </pre>

## Fix commits (touching thread paths)

### `47ec0005b2d1ac54b8bc35579e982f4851bdf228` -- 2026-04-24T00:48:39Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `docs/research/memory-reconciliation-algorithm-design-2026-04-24.md`

**Message:**

```
research: memory reconciliation algorithm — v0 design (Amara Determin…

…ize L-effort item)

Amara's 4th ferry (PR #221 absorb) centerpiece proposal: replace
hand-maintained CURRENT-*.md distillations with generated views
over typed memory facts. Her sketch was ~40 lines of Python;
this is the design that downstream implementation follows.

~380 lines covering:

- MemoryFact record schema (id / subject / predicate / object /
  source_kind / source_path / source_anchor / timestamp_utc /
  supersedes / priority / status / confidence / tags)
- 6 schema invariants (at-most-one-active-per-canonical-key +
  monotone-timestamps-on-chain + retraction-leaves-trail + ...)
- Canonical-key normalization rules (7 apply; 3 deliberately
  NOT applied to preserve distinctions)
- Reconciliation pseudocode (group by canonical key, detect
  conflicts, follow supersession chains)
- Conflict output format → CONTRIBUTOR-CONFLICTS.md rows
- Rendering rules for CURRENT-<maintainer>.md + MEMORY.md
- 5-phase incremental migration (schema adoption → generator
  prototype → mechanical backfill → cutover → LLM extraction)
- CI integration hooks composing with rows #58, #59, #12
- Worked examples (MF-2026-04-23-001 "Aaron endorses
  deterministic reconciliation"; MF-2026-04-23-004 "Aaron
  grants full GitHub access")
- 5 open questions for Phase 1 PR design decisions

Composes with:

- Otto-73 retractability-by-design foundation — MemoryFact
  status (active / superseded / retracted) is the retraction-
  native primitive at the memory substrate
- PR #222 decision-proxy-evidence — consulted_memory_ids
  can now reference MemoryFact.id directly
- PR #225 memory-reference-existence CI (row #59) — generated
  output preserves the invariant by construction
- Zeta's ZSet algebra — MemoryFact records ARE Z-set entries
  at the memory layer; same primitive, different surface

Addresses MEMORY.md cap-drift (Otto-70 snapshot-tool
surfaced 58842 bytes vs. 24976-byte cap): a generated
index can be bounded by construction (top-N most-recent,
archive the rest).

Not implementation. Research doc only. Downstream arc:
schema adoption (S) → generator prototype off-CI (S-M) →
mechanical backfill (M) → cutover with retractability (M) →
LLM-assisted extraction (L research).

Amara Determinize-stage: 3/5 (with this PR).
  ✓ Live-state-before-policy (PR #224)
  ✓ Memory reference-existence lint (PR #225)
  ✓ Memory reconciliation algorithm design (this PR)
  Remaining:
  - Generated CURRENT-*.md views (L; this doc's Phase 2)
  - Memory duplicate-title lint enforcement (partial via
    AceHack PR #12; graduates via batch-sync)

Per Aaron Otto-73 retractability foundation: the design
itself embodies the thesis — supersession + status +
retraction make the memory layer's reconciliation
deterministic, same primitive as Zeta's data layer.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
```

### `6f895f1042da09e4ff9513312076d6800244dcb6` -- 2026-04-25T05:08:41Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `docs/research/memory-reconciliation-algorithm-design-2026-04-24.md`

**Message:**

```
drain(#226 P0+P1×2+P2×3 Codex): retraction semantics + cap consistenc…

…y + smart-quote + pseudocode init + present-with-schema

Six substantive Codex findings on memory-reconciliation algorithm doc:

P0 (line 202) — retraction semantics inconsistency:
reconcile() filtered by status == 'active' which masked the
intent. Added explicit retraction-semantics docstring:
- Facts transition via explicit FactRetracted / FactSuperseded
  events; never deleted, only marked.
- reconcile() ignores retracted/superseded for liveness but
  STILL considers them when checking version-chain integrity.
- Updated chain check to operate over ALL facts in the group
  (including retracted/superseded), not just active ones —
  chain integrity needs the full history.

P1 (line 187) — stable fact identity vs grouping key:
Distinguished fact ID (stable identity, unique) from
(subject, predicate, canonical_key) grouping tuple (which
multiple facts can share under invariant-2's collision
case). Comment makes the distinction explicit.

P1 (line 270) — MEMORY.md cap inconsistency:
Default 30KB exceeded FACTORY-HYGIENE row #11 cap (24,976
bytes). Updated to 24,000 bytes — strictly under the hard
cap with ~1KB headroom for header/annotation overhead.

P2 (line 130) — smart-quote example ambiguous:
Both sides showed plain ASCII ('"' / "'"). Replaced with
explicit Unicode codepoint references (U+201C/D for
double, U+2018/9 for single) so the rule is unambiguous
in plain-ASCII source.

P2 (line 186) — pseudocode by_key[k] used before init:
Switched to defaultdict(list); added a comment noting the
equivalence to 'if k not in by_key: by_key[k] = []' for
non-Python implementers.

P2 (line 216) — CONTRIBUTOR-CONFLICTS.md 'empty' wording:
File is present and contains a schema; just unpopulated.
Updated text to 'present-with-schema-but-unpopulated; this
design starts populating it via the generator'.
```

### `32721c01b8c69be7dda056275bed5485b4c4bce0` -- 2026-04-25T05:18:41Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `docs/research/memory-reconciliation-algorithm-design-2026-04-24.md`

**Message:**

```
drain(#226 P1+P2 Codex): chain-head liveness + chain-integrity for re…

…tired groups

P1 (line 210) — chain-HEAD liveness, not 'any active in group':
The reconcile filter marked a key live whenever any record
in the group had status==active. That's wrong — a key with
active(t=1) → retracted(t=2) has an earlier active record
but the HEAD of the supersession chain is retracted, so the
key is not live. Fix: `follow_supersession_to_head(group)`
walks supersedes-pointers to find the most-recent record;
liveness keyed on its status == active.

P2 (line 224) — chain integrity for fully retired groups:
The chain-integrity check looped over `accepted.items()`,
which only included keys with at least one active record.
Retired groups (all members retracted/superseded) could
have broken chains and we'd silently miss them. Fix: loop
over `by_key.items()` (all groups, including fully retired
ones). Chain integrity is independent of liveness.
```
