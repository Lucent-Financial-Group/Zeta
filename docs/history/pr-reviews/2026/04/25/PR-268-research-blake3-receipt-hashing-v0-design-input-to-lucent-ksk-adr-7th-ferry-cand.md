# PR #268 -- research: BLAKE3 receipt-hashing v0 design input to lucent-ksk ADR (7th-ferry candidate #3)

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 268 |
| Title | research: BLAKE3 receipt-hashing v0 design input to lucent-ksk ADR (7th-ferry candidate #3) |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-04-24T03:10:32Z |
| Merged at | 2026-04-25T04:38:25Z |
| Merge commit SHA | `0fc13f2fbaf6637f66e22b7b7192f8a636b29ceb` |
| Branch | `research/blake3-receipt-hashing-v0-design-input-to-lucent-ksk-adr` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/268 |
| Changed files | 1 |
| Additions / deletions | +467 / -0 |

## Description

## Summary

Zeta-side **design input** for the receipt-hashing scheme that eventually lands as an ADR in \`LFG/lucent-ksk\` per Aminata's Otto-90 critique (receipt-hash belongs in lucent-ksk, not Zeta).

## Three inputs synthesised

| Source | Contribution |
|---|---|
| Amara 7th ferry (PR #259) | Original BLAKE3 scheme: 7-field hash + agent/node signatures |
| Aminata Otto-90 pass (PR #263) | Side-channel leakage, crypto-agility gaps, approval-withdrawal race |
| Otto-91 oracle-scoring v0 (PR #266) | \`parameter_file_sha\` binding for replay determinism |

## v0 hash input (8 fields; 3 changes from original)

- **NEW:** \`hash_version\` prefix → crypto-agility
- **NEW:** \`parameter_file_sha\` → replay determinism
- **CHANGED:** \`approval_set\` → \`approval_set_commitment\` → side-channel-leak closure
- Signature tuple binds \`*_key_version\` for rotation

## 4 replay-deterministic harness requirements

1. Same fields = byte-for-byte-identical materialised views
2. Unknown hash_version → halt-and-report
3. Unresolvable parameter_file_sha → halt-and-report
4. Mismatched approval_set_commitment → reject receipt

## Explicit NOT-scope

- Doesn't decide signature algorithm (Ed25519 = v0 assumption)
- Doesn't define hash_version / parameter_file registries (lucent-ksk governance)
- Doesn't define commitment scheme specifics (Merkle vs sorted-hash-list)
- Doesn't implement rotation runbook
- Doesn't include Bitcoin anchoring

## Specific-asks

- **Aaron:** does \`parameter_file_sha\` binding warrant its own registry, or reuse \`docs/DECISIONS/\` pattern?
- **Max:** does lucent-ksk have preferred ADR form-factor? Otto propose, or Max owns that design choice?

Framed as specific questions per Otto-90 specific-ask-channel calibration.

## 7th-ferry candidate queue status

| # | Item | Status |
|---|---|---|
| 1 | KSK-as-Zeta-module implementation (L) | **Only remaining**; within standing authority |
| 2 | Oracle-scoring research | ✓ PR #266 Otto-91 |
| 3 | **BLAKE3 receipt hashing design** | **This PR** — closes |
| 4 | Aurora README branding update | ✓ PR #261 Otto-89 |
| 5 | Aminata threat-model pass | ✓ PR #263 Otto-90 |

4 of 5 closed with substantive responses.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | true |
| Total threads | 19 |
| Resolved threads | 19 |
| Unresolved threads | 0 |
| Total review comments | 38 |
| Total fix commits (touching thread paths) | 7 |

## Review threads

### Thread 1 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59SMrz`
- **Path**: `docs/research/blake3-receipt-hashing-v0-design-input-to-lucent-ksk-adr-2026-04-23.md`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-04-24T03:12:29Z on `docs/research/blake3-receipt-hashing-v0-design-input-to-lucent-ksk-adr-2026-04-23.md`:126 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Correct v0 hash field count**

  The section labels this as an “8 fields” hash, but the formula directly below binds nine inputs (`hash_version`, `h_inputs`, `h_actions`, `h_outputs`, `budget_id`, `policy_version`, `parameter_file_sha`, `approval_set_commitment`, `node_id`). This mismatch can cause implementers of the downstream lucent-ksk ADR to omit a field and produce incompatible receipt hashes, which would break replay and signature verification across components.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-25T03:56:22Z on `docs/research/blake3-receipt-hashing-v0-design-input-to-lucent-ksk-adr-2026-04-23.md`:126 (association: MEMBER)

    <pre>
    Fixed in `60bb32c` — section heading and intro text both updated to **9 fields** (the formula was source of truth; the count text was lagging). `hash_version` is field 1, then the 8 content/policy/commitment hashes.
    </pre>

### Thread 2 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59SNsm`
- **Path**: `docs/research/blake3-receipt-hashing-v0-design-input-to-lucent-ksk-adr-2026-04-23.md`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-24T03:14:24Z on `docs/research/blake3-receipt-hashing-v0-design-input-to-lucent-ksk-adr-2026-04-23.md`:236 (association: NONE)

  <pre>
  The version identifier notation is inconsistent: earlier you use hex like `0x01`, but here it switches to `v0x02` (and later `v0x01`). Consider standardizing the notation (e.g., always `0x01` / `0x02`, or always `v1` / `v2`) to avoid confusion in the eventual registry/ADR.
  ```suggestion
    0x02), the scheme is broken. Mitigation: the registry
  ```
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-25T03:56:23Z on `docs/research/blake3-receipt-hashing-v0-design-input-to-lucent-ksk-adr-2026-04-23.md`:236 (association: MEMBER)

    <pre>
    Fixed in `60bb32c` — standardized on the byte-literal hex notation `0x01` / `0x02` throughout (no more `v0x01` / `v0x02`). The "v" prefix doubled up with `hash_version =` already in the formula and added no information.
    </pre>

### Thread 3 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59SNsy`
- **Path**: `docs/research/blake3-receipt-hashing-v0-design-input-to-lucent-ksk-adr-2026-04-23.md`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-24T03:14:24Z on `docs/research/blake3-receipt-hashing-v0-design-input-to-lucent-ksk-adr-2026-04-23.md`:130 (association: NONE)

  <pre>
  The doc claims the v0 hash input set is "8 fields" (here and also on line 120), but the listed inputs include 9 distinct fields (hash_version, h_inputs, h_actions, h_outputs, budget_id, policy_version, parameter_file_sha, approval_set_commitment, node_id). Please reconcile the field count and update the surrounding text accordingly so implementers don’t drop or double-count a field.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-25T03:56:25Z on `docs/research/blake3-receipt-hashing-v0-design-input-to-lucent-ksk-adr-2026-04-23.md`:130 (association: MEMBER)

    <pre>
    Fixed in `60bb32c` — section heading and intro text both updated to **9 fields** (the formula was source of truth; the count text was lagging). `hash_version` is field 1, then the 8 content/policy/commitment hashes.
    </pre>

### Thread 4 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59SNs2`
- **Path**: `docs/research/blake3-receipt-hashing-v0-design-input-to-lucent-ksk-adr-2026-04-23.md`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-24T03:14:24Z on `docs/research/blake3-receipt-hashing-v0-design-input-to-lucent-ksk-adr-2026-04-23.md`:132 (association: NONE)

  <pre>
  The hash definition uses raw concatenation ("∥") of multiple fields but doesn’t specify an unambiguous, canonical encoding (e.g., fixed-width fields, length-prefix/TLV framing, or domain-separated tagged encoding). Without this, variable-length fields can make the hash input ambiguous and enable practical collision-style attacks via different field tuples producing the same byte stream. Please define the exact byte-level serialization for each field (including separators/lengths and a domain-separation tag) as part of the scheme.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-25T03:56:26Z on `docs/research/blake3-receipt-hashing-v0-design-input-to-lucent-ksk-adr-2026-04-23.md`:132 (association: MEMBER)

    <pre>
    Fixed in `60bb32c` — added an explicit `encode(·)` wrapper per field plus a canonical-encoding section. Per-field shape: 1-byte version, 32-byte fixed-width digests for content/policy/commitment hashes, `len:u32-be ∥ bytes` length-prefix framing for variable-length identifiers (`budget_id`, `policy_version`, `node_id`). Closes the `"AB" ∥ "CD"` vs `"A" ∥ "BCD"` boundary-shift adversary. Forward-compatible: future schemes (`hash_version &gt;= 0x02`) can pick different framing (CBOR / Protobuf / RFC 8949 §3.1 TLV) and the version prefix tells verifiers which applies.
    </pre>

### Thread 5 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59jn48`
- **Path**: `docs/research/blake3-receipt-hashing-v0-design-input-to-lucent-ksk-adr-2026-04-23.md`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-04-25T03:58:24Z on `docs/research/blake3-receipt-hashing-v0-design-input-to-lucent-ksk-adr-2026-04-23.md`:226 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Validate commitment against replay-state signer view**

  Requirement #4 says to compare `approval_set_commitment` with the "current signer-view," which makes validity depend on whichever signer set exists when replay runs rather than the signer set at receipt execution time; after normal signer rotation or approval updates, historical receipts can be rejected even though they were valid when emitted, breaking the replay determinism promised in requirement #1. The check needs to be against the signer view reconstructed for that receipt’s point in the event stream (or an explicitly versioned snapshot), not a wall-clock-current view.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-25T04:17:44Z on `docs/research/blake3-receipt-hashing-v0-design-input-to-lucent-ksk-adr-2026-04-23.md`:226 (association: MEMBER)

    <pre>
    Fixed in `c01b778` — req #4 now validates against the signer set **authoritative at the receipt's claimed `policy_version`**, recoverable from `policy_version` + dispute-process commitment-opening. Replay-determinism preserved: the historical signer view is what the receipt commits to, not the live signer set. Receipt-creation-time approval-withdrawal-race-check is unchanged; the replay gate now catches *forged* commitments only (the original intent — the wording was sloppy).
    </pre>

### Thread 6 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59jn5C`
- **Path**: `docs/research/blake3-receipt-hashing-v0-design-input-to-lucent-ksk-adr-2026-04-23.md`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-04-25T03:58:24Z on `docs/research/blake3-receipt-hashing-v0-design-input-to-lucent-ksk-adr-2026-04-23.md`:157 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Define canonical byte encoding for identifier fields**

  The v0 encoding specifies `len:u32-be ∥ bytes` for `budget_id`, `policy_version`, and `node_id`, but it never defines how these identifiers are converted to bytes (e.g., UTF-8 vs UTF-16, normalization, case handling). Two conforming implementations can therefore hash different byte streams for the same logical identifiers, yielding incompatible receipt hashes/signatures and cross-implementation verification failures.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-25T04:17:46Z on `docs/research/blake3-receipt-hashing-v0-design-input-to-lucent-ksk-adr-2026-04-23.md`:157 (association: MEMBER)

    <pre>
    Fixed in `c01b778` — added explicit text-to-byte mapping: `bytes = NFC-normalised UTF-8 octets` — Unicode Normalization Form C per Unicode Annex #15, then encoded as UTF-8. NFC handles visually-identical-but-byte-different forms (e.g., precomposed vs decomposed accents in identifiers); UTF-8 is the canonical text→byte mapping. The `len:u32-be ∥ bytes` framing now has a complete derivation rule from input string.
    </pre>

### Thread 7 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59jxNw`
- **Path**: `docs/research/blake3-receipt-hashing-v0-design-input-to-lucent-ksk-adr-2026-04-23.md`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-25T04:16:55Z on `docs/research/blake3-receipt-hashing-v0-design-input-to-lucent-ksk-adr-2026-04-23.md`:147 (association: NONE)

  <pre>
  P1: The canonical-encoding rationale mentions a “length-extension” adversary surface (lines 143–144). The real risk here is ambiguous concatenation / boundary shifting between variable-length fields; length-extension attacks are a property of specific hash constructions and this wording could mislead readers about what’s actually being prevented. Consider rephrasing to focus on boundary ambiguity/collision-style risks from non-canonical serialization (and keep the AB|CD vs A|BCD example).
  ```suggestion
  variable-length fields is ambiguous and opens a boundary-
  shift / ambiguous-serialization adversary surface (an
  attacker could partition `"AB" ∥ "CD"` and `"A" ∥ "BCD"` to
  the same input bytes). v0 binds an explicit canonical
  encoding for each field, in the order listed:
  ```
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-25T04:20:45Z on `docs/research/blake3-receipt-hashing-v0-design-input-to-lucent-ksk-adr-2026-04-23.md`:147 (association: MEMBER)

    <pre>
    Fixed in `50f6cc5` — corrected the terminology. BLAKE3 is built on a tree-hash construction with finalisation flags and is NOT vulnerable to length-extension the way SHA-256 / MD5 are. The risk in raw concatenation is boundary-shift / collision-by-reframing — same effect (collision via input ambiguity) but a different mechanism than length-extension. Reworded to name the risk precisely and added a parenthetical clarifying that length-extension is NOT a BLAKE3 concern.
    </pre>

### Thread 8 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59jxOI`
- **Path**: `docs/research/blake3-receipt-hashing-v0-design-input-to-lucent-ksk-adr-2026-04-23.md`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-25T04:16:55Z on `docs/research/blake3-receipt-hashing-v0-design-input-to-lucent-ksk-adr-2026-04-23.md`:162 (association: NONE)

  <pre>
  P2: “domain-separated TLV per RFC 8949 §3.1” (line 162) appears to mix CBOR’s RFC reference (8949) with a TLV framing concept. If the intent is “CBOR (RFC 8949)” or “tagged/typed encoding (e.g., CBOR/Protobuf/TLV)”, please adjust the wording/citation so implementers don’t go looking for a TLV spec section in RFC 8949.
  ```suggestion
  0x02`) may pick different framing (e.g. CBOR per RFC 8949,
  Protobuf, or domain-separated TLV framing), and the version
  ```
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-25T04:20:47Z on `docs/research/blake3-receipt-hashing-v0-design-input-to-lucent-ksk-adr-2026-04-23.md`:162 (association: MEMBER)

    <pre>
    Fixed in `50f6cc5` — split the conflated reference. RFC 8949 is CBOR; 'domain-separated TLV' is a separate framing concept (generic, no canonical RFC). Now reads as two parallel options: 'CBOR per RFC 8949' (one option) and 'a domain-separated TLV scheme' (another, no specific RFC attached). The v0 doc no longer mis-cites.
    </pre>

### Thread 9 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59j0B4`
- **Path**: `docs/research/blake3-receipt-hashing-v0-design-input-to-lucent-ksk-adr-2026-04-23.md`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-04-25T04:23:28Z on `docs/research/blake3-receipt-hashing-v0-design-input-to-lucent-ksk-adr-2026-04-23.md`:229 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Enforce policy-gated hash_version acceptance**

  Requirement #2 only rejects *unknown* `hash_version` values, which means any older but still recognized version remains acceptable for newly issued receipts; that leaves a downgrade path open after an algorithm upgrade (an attacker can keep emitting receipts under a weaker legacy version and pass verification). To actually close the downgrade class described earlier in the document, verification needs a rule that the receipt's `hash_version` is allowed for its issuance epoch/policy (for example, keyed by `policy_version`) rather than merely "known".

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-25T04:31:18Z on `docs/research/blake3-receipt-hashing-v0-design-input-to-lucent-ksk-adr-2026-04-23.md`:229 (association: MEMBER)

    <pre>
    Fixed in `5da2e68` — req #2 now also rejects DEPRECATED `hash_version` per a policy registry (lucent-ksk governance artifact), not just unknown. Historical receipts under deprecated versions remain verifiable for audit; new receipts under deprecated versions are refused. Closes the 'forgery under old-but-still-recognised version' attack.
    </pre>

### Thread 10 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59j0B6`
- **Path**: `docs/research/blake3-receipt-hashing-v0-design-input-to-lucent-ksk-adr-2026-04-23.md`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-04-25T04:23:28Z on `docs/research/blake3-receipt-hashing-v0-design-input-to-lucent-ksk-adr-2026-04-23.md`:211 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Restrict retired key versions for new receipts**

  The rotation section adds `agent_key_version`/`node_key_version` to signature metadata but does not define any acceptance boundary for new receipts, so a compromised or retired old key can still sign fresh receipts by declaring an old key version and be treated as valid. This undermines the stated rotation security goal unless verification also checks that the key version is authorized for the receipt's policy/issuance point, not just that signatures verify against some historical key.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-25T04:31:20Z on `docs/research/blake3-receipt-hashing-v0-design-input-to-lucent-ksk-adr-2026-04-23.md`:211 (association: MEMBER)

    <pre>
    Fixed in `5da2e68` — added explicit text: 'Restrict NEW receipts to non-retired key versions. A separate registry of retired key versions (lucent-ksk governance artifact) blocks creation of new receipts under retired versions. Historical receipts under retired versions remain verifiable (replay-determinism) but the signing path refuses to produce more.' Same governance-shape as the deprecated-hash-version policy.
    </pre>

### Thread 11 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59j0QR`
- **Path**: `docs/research/blake3-receipt-hashing-v0-design-input-to-lucent-ksk-adr-2026-04-23.md`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-25T04:24:42Z on `docs/research/blake3-receipt-hashing-v0-design-input-to-lucent-ksk-adr-2026-04-23.md`:17 (association: NONE)

  <pre>
  P1: This doc introduces direct name attribution (e.g., Amara/Aminata/Otto/Max) and later uses direct names in the specific-ask section. Repo convention is to avoid contributor names in docs and use role references instead (e.g., “human maintainer”, “threat-model critic persona”, “lucent-ksk maintainer”). Please rewrite attribution + asks to use role refs and keep names only in exempt historical surfaces per docs/AGENT-BEST-PRACTICES.md:284-292.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-25T04:31:21Z on `docs/research/blake3-receipt-hashing-v0-design-input-to-lucent-ksk-adr-2026-04-23.md`:17 (association: MEMBER)

    <pre>
    Resolved per **Otto-279 surface-class refinement** (2026-04-24): research docs under `docs/research/**` are *history surfaces* where Otto-279 explicitly *allows* first-name attribution for humans AND agents. The literal AGENT-BEST-PRACTICES.md no-names rule applies to *current-state surfaces* (code, skill bodies, persona definitions, spec docs, README, public-facing prose). Aaron's Otto-279 directive: *"i feel like under research that counts as history and we should give first name attribution"*. Names in this design doc (Amara, Aminata, Otto, Max) are intentional and stay. See `memory/feedback_research_counts_as_history_first_name_attribution_for_humans_and_agents_otto_279_2026_04_24.md`.
    </pre>

### Thread 12 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59j0QU`
- **Path**: `docs/research/blake3-receipt-hashing-v0-design-input-to-lucent-ksk-adr-2026-04-23.md`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-25T04:24:42Z on `docs/research/blake3-receipt-hashing-v0-design-input-to-lucent-ksk-adr-2026-04-23.md`:120 (association: NONE)

  <pre>
  This sentence reads as if the oracle-scoring addition alone brings the scheme to 9 fields, but at this point only `parameter_file_sha` has been introduced (which would make the original 7-field set into 8). Consider rewording to make it explicit that the *combined* v0 changes (including `hash_version` + `approval_set_commitment`) yield 9 fields, to avoid miscounting by readers.
  ```suggestion
  In the combined proposed v0 scheme, adding
  `hash_version`, `parameter_file_sha`, and
  `approval_set_commitment` brings the input set to 9
  fields.
  ```
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-25T04:31:23Z on `docs/research/blake3-receipt-hashing-v0-design-input-to-lucent-ksk-adr-2026-04-23.md`:120 (association: MEMBER)

    <pre>
    Fixed in `5da2e68` — reframed the sentence to make field-count reasoning explicit: 'Amara's 7th-ferry proposal had 7 base fields (...); v0 adds hash_version (cryptographic-agility prefix) and parameter_file_sha (oracle-scoring binding above), so the v0 input set extends to 9 fields total.' The 9-fields claim now derives transparently from 7 + 2; no longer reads as if oracle-scoring alone gets us there.
    </pre>

### Thread 13 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59j0Qd`
- **Path**: `docs/research/blake3-receipt-hashing-v0-design-input-to-lucent-ksk-adr-2026-04-23.md`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-25T04:24:42Z on `docs/research/blake3-receipt-hashing-v0-design-input-to-lucent-ksk-adr-2026-04-23.md`:229 (association: NONE)

  <pre>
  The notation `Sign_{sk_*, *_key_version}(h_r)` plus “bind *_key_version into the signature tuple” is ambiguous about whether `*_key_version` is authenticated. If `*_key_version` is just unsigned metadata alongside a signature over `h_r`, it can be tampered with and lead to key-selection bugs/DoS. Please clarify that key-version identifiers are part of the signed payload (e.g., included in what’s hashed/signed, or that the signature is over a structured message that includes both `h_r` and the key-version fields).
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-25T04:31:25Z on `docs/research/blake3-receipt-hashing-v0-design-input-to-lucent-ksk-adr-2026-04-23.md`:229 (association: MEMBER)

    <pre>
    Fixed in `5da2e68` — bound `*_key_version` INSIDE the signed message rather than as unsigned metadata. New form: `Sign_{sk}(version ∥ h_r)`. Verification re-derives the signing input from the declared version + h_r. This authenticates the version: an attacker cannot swap the declared version to one pointing at a different algorithm's pubkey, because the verifier recomputes the signed bytes from the declared version. Verification block added showing the explicit lookup + recompute pattern.
    </pre>

### Thread 14 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59j1rH`
- **Path**: `docs/research/blake3-receipt-hashing-v0-design-input-to-lucent-ksk-adr-2026-04-23.md`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-04-25T04:32:50Z on `docs/research/blake3-receipt-hashing-v0-design-input-to-lucent-ksk-adr-2026-04-23.md`:267 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Gate deprecated hash_version by issuance epoch**

  This requirement unconditionally rejects receipts whose `hash_version` is marked deprecated, but the same section says historical receipts under deprecated versions should remain verifiable. Without an issuance-time or policy-version boundary in the registry check, replay/audit of older receipts will start failing as soon as a version is deprecated, which breaks the replay-determinism goal stated above; the acceptance rule needs an explicit historical window keyed to receipt metadata.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-25T04:36:31Z on `docs/research/blake3-receipt-hashing-v0-design-input-to-lucent-ksk-adr-2026-04-23.md`:267 (association: MEMBER)

    <pre>
    Fixed in `61a2bd4` — replaced unconditional deprecation-rejection with issuance-epoch gate. Receipts issued BEFORE the version's deprecation cutoff remain valid for audit / replay (preserves historical determinism); receipts claiming an issuance epoch AFTER the cutoff under that version are rejected. Registry now stores `(version, deprecated_after_epoch)` tuples; verifier compares the receipt's claimed issuance epoch against the registry's deprecation epoch for that version. Replay-determinism preserved across deprecation events.
    </pre>

### Thread 15 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59j1rI`
- **Path**: `docs/research/blake3-receipt-hashing-v0-design-input-to-lucent-ksk-adr-2026-04-23.md`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-04-25T04:32:50Z on `docs/research/blake3-receipt-hashing-v0-design-input-to-lucent-ksk-adr-2026-04-23.md`:208 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Specify canonical encoding for key-version in signature input**

  The signature scheme binds `agent_key_version`/`node_key_version` by concatenating them with `h_r`, but this doc never defines how those version values are encoded to bytes. Different implementations can therefore sign different byte streams for the same logical version (for example text vs fixed-width integer), causing cross-implementation signature verification failures and undermining the stated rotation compatibility.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-25T04:36:32Z on `docs/research/blake3-receipt-hashing-v0-design-input-to-lucent-ksk-adr-2026-04-23.md`:208 (association: MEMBER)

    <pre>
    Fixed in `61a2bd4` — added explicit `encode_u32_be(*_key_version)` to the signing input + an Encoding section: 4-byte big-endian unsigned integer, monotonic from 1, version 0 reserved for uninitialised. Fixed-width 4 bytes — no length prefix needed since every version is exactly 4 bytes. Verifier recomputes the signing input from the declared version with the same `encode_u32_be` wrapper.
    </pre>

### Thread 16 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59j3Dq`
- **Path**: `docs/research/blake3-receipt-hashing-v0-design-input-to-lucent-ksk-adr-2026-04-23.md`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-25T04:40:42Z on `docs/research/blake3-receipt-hashing-v0-design-input-to-lucent-ksk-adr-2026-04-23.md`:133 (association: NONE)

  <pre>
  P2: PR description says the v0 hash input is “8 fields”, but this doc (correctly) defines a 9-field input set. Please update the PR description to match this document so reviewers don’t carry forward the outdated count into the eventual lucent-ksk ADR.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-25T04:45:41Z on `docs/research/blake3-receipt-hashing-v0-design-input-to-lucent-ksk-adr-2026-04-23.md`:133 (association: MEMBER)

    <pre>
    Fixed in follow-up [PR #431](https://github.com/Lucent-Financial-Group/Zeta/pull/431). The PR description count was a residual from the early '8 fields' framing before `parameter_file_sha`, then `issuance_epoch` were added; the canonical count is now 10 (per the doc + the follow-up PR). The PR description on #268 itself can't be edited post-merge, but the doc + this follow-up establish the canonical 10-field count going forward.
    </pre>

### Thread 17 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59j3Dw`
- **Path**: `docs/research/blake3-receipt-hashing-v0-design-input-to-lucent-ksk-adr-2026-04-23.md`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-25T04:40:43Z on `docs/research/blake3-receipt-hashing-v0-design-input-to-lucent-ksk-adr-2026-04-23.md`:126 (association: NONE)

  <pre>
  P1: The attribution/field list here says Amara’s 7th-ferry proposal used `approval_set_commitment`, but earlier in the doc the original scheme binds `approval_set` and later you list “Replace `approval_set` with `approval_set_commitment`” as a v0 change. Please correct this sentence so the historical baseline is accurate and readers don’t misinterpret which fields are new vs changed.
  ```suggestion
  `approval_set`, `node_id`); v0 adds `hash_version`
  (cryptographic-agility prefix), adds
  `parameter_file_sha` (oracle-scoring binding above), and
  replaces `approval_set` with `approval_set_commitment`, so
  the v0 input set extends to **9 fields total**.
  ```
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-25T04:45:37Z on `docs/research/blake3-receipt-hashing-v0-design-input-to-lucent-ksk-adr-2026-04-23.md`:126 (association: MEMBER)

    <pre>
    Fixed in follow-up [PR #431](https://github.com/Lucent-Financial-Group/Zeta/pull/431) — updated attribution to: 'Amara's 7th-ferry proposal had 7 base fields ... `approval_set` [raw, replaced by `approval_set_commitment` in v0 per Aminata's side-channel finding — same slot, different binding]'. Now accurately tracks the seven-base-fields-with-one-replaced-in-v0 history rather than collapsing the Aminata-driven swap.
    </pre>

### Thread 18 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59j3D1`
- **Path**: `docs/research/blake3-receipt-hashing-v0-design-input-to-lucent-ksk-adr-2026-04-23.md`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-25T04:40:43Z on `docs/research/blake3-receipt-hashing-v0-design-input-to-lucent-ksk-adr-2026-04-23.md`:275 (association: NONE)

  <pre>
  P0: The “issuance-epoch gate” relies on a receipt’s *claimed issuance epoch*, but `issuance_epoch` isn’t defined anywhere in the receipt fields nor bound into `h_r`/the signatures in this v0 scheme. As written, an attacker could tamper with an unsigned timestamp/epoch to bypass deprecation cutoffs. Please either (a) add an authenticated issuance epoch field to the hash input set (or into one of the hashed sub-structures like `h_actions` with an explicit requirement), and (b) specify the canonical encoding + trust model for that epoch source.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-25T04:45:35Z on `docs/research/blake3-receipt-hashing-v0-design-input-to-lucent-ksk-adr-2026-04-23.md`:275 (association: MEMBER)

    <pre>
    Fixed in follow-up [PR #431](https://github.com/Lucent-Financial-Group/Zeta/pull/431) — added `issuance_epoch` as a 10th hash-input field (u64-be milliseconds since Unix epoch), bound directly into `h_r`. Encoding: 8-byte unsigned big-endian integer. Without this binding, an attacker could forge a receipt under a deprecated `hash_version` and put the claimed epoch BEFORE the deprecation cutoff to slip past the gate. With it bound into h_r, any tampering invalidates the BLAKE3 hash. Field count v0: 10 fields (was 9).
    </pre>

### Thread 19 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59j3EA`
- **Path**: `docs/research/blake3-receipt-hashing-v0-design-input-to-lucent-ksk-adr-2026-04-23.md`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-25T04:40:43Z on `docs/research/blake3-receipt-hashing-v0-design-input-to-lucent-ksk-adr-2026-04-23.md`:166 (association: NONE)

  <pre>
  P1: The field name `parameter_file_sha` strongly implies a SHA-family digest, but this section specifies it is a “32-byte fixed-width BLAKE3 digest”. Please clarify which algorithm is actually required for this field and align the name/text accordingly (e.g., rename to `parameter_file_digest`/`parameter_file_hash`, or explicitly require SHA-256 if “sha” is meant literally) to avoid interop mismatches.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-25T04:45:39Z on `docs/research/blake3-receipt-hashing-v0-design-input-to-lucent-ksk-adr-2026-04-23.md`:166 (association: MEMBER)

    <pre>
    Fixed in follow-up [PR #431](https://github.com/Lucent-Financial-Group/Zeta/pull/431) — added clarification: `parameter_file_sha`'s '_sha' suffix is legacy Otto-91 oracle-scoring naming where '_sha' meant 'hash digest' in that context. The actual algorithm bound by `hash_version = 0x01` is BLAKE3-256, not SHA-256. Future schemes may select a different digest via the `hash_version` registry; the field name stays for Otto-91-prose backward-compatibility. The legacy-naming-with-current-algorithm is bracketed by the version registry, so consumers can reason about the actual algorithm without renaming the field.
    </pre>

## Fix commits (touching thread paths)

### `8f6e2a20bfd17f8f49ad2e9ec778e9089a2bb5d4` -- 2026-04-24T03:10:06Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `docs/research/blake3-receipt-hashing-v0-design-input-to-lucent-ksk-adr-2026-04-23.md`

**Message:**

```
research: BLAKE3 receipt-hashing v0 design input to lucent-ksk ADR (7…

…th-ferry candidate #3)

Responds to Amara's 7th-ferry BLAKE3 proposal (PR #259) +
Aminata's Otto-90 critiques (PR #263) flagging it belongs in
lucent-ksk rather than Zeta + naming side-channel-leakage and
cryptographic-agility gaps + Otto-91 addition of
parameter_file_sha binding for replay determinism.

v0 hash input set (8 fields, changes marked):

  h_r = BLAKE3(
    hash_version                    // NEW — crypto-agility
    ∥ h_inputs
    ∥ h_actions
    ∥ h_outputs
    ∥ budget_id
    ∥ policy_version
    ∥ parameter_file_sha            // NEW — Otto-91
    ∥ approval_set_commitment       // CHANGED — side-channel
    ∥ node_id
  )

Signature structure adds *_key_version to each signature tuple
for per-key-rotation without breaking historical receipts.

Addresses Aminata's 3 findings:
- Side-channel leakage: raw approval_set → Merkle/sorted-hash
  commitment; read-only observers see a hash, dispute process
  opens it.
- Cryptographic-agility: hash_version prefix + *_key_version
  binding; algorithm downgrade blocked because version is
  inside the hash.
- Approval-withdrawal race (top-3 #2): commitment mismatch at
  replay-time invalidates the receipt.

4 replay-deterministic harness requirements for Zeta-module
consumer side:
1. Same fields = same materialised views byte-for-byte.
2. Unknown hash_version = halt-and-report.
3. Unresolvable parameter_file_sha = halt-and-report.
4. Mismatched approval_set_commitment = reject receipt.

Explicit NOT-scope:
- Doesn't decide signature algorithm (Ed25519 is v0
  assumption, scheme accommodates later).
- Doesn't define hash_version / parameter_file registries
  (lucent-ksk governance artifacts).
- Doesn't define commitment scheme specifics (Merkle vs
  sorted-hash-list; affects dispute only).
- Doesn't implement rotation runbook.
- Doesn't include Bitcoin anchoring (separate trust-model).

7 dependencies to adoption in priority order; Aminata 2nd
pass first; cross-repo lucent-ksk ADR second; Max-specific
asks framed per Otto-90 specific-ask-channel calibration.

This is Zeta-SIDE design input. Canonical ADR belongs in
lucent-ksk per Aminata Otto-90 framing. No adoption until
cross-repo ADR lands.

Max attribution preserved first-name-only. Cross-repo work
on lucent-ksk does not touch Max's substrate directly until
actual coordination warrants — specific-ask channel is the
right escalation.

Archive-header format self-applied — 10th aurora/research
doc in a row.

Lands within-standing-authority per Otto-82/90 calibration.

Closes 7th-ferry absorb candidate #3 of 5. Remaining:
- #1 KSK-as-Zeta-module implementation (L)

Otto-92 tick primary deliverable.
```

### `0be0f5fdc255e3a761fce003bd85c1d32c3e291b` -- 2026-04-25T03:55:53Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `docs/research/blake3-receipt-hashing-v0-design-input-to-lucent-ksk-adr-2026-04-23.md`

**Message:**

```
drain(#268 P2+P2+style+P1 Codex/Copilot): field count + version notat…

…ion + canonical encoding

Four threads on the BLAKE3 receipt-hashing v0 design doc, all
on the same file.

P2 (lines 120 + 126): "8 fields" header / count text vs the
formula's 9 actual binding inputs (`hash_version` + 8 content
hashes). Reconciled to "9 fields" — the formula was the
source of truth, the count text was the lag.

Style (line 236): version notation inconsistency — `0x01` in
some places, `v0x02` / `v0x01` in others. Standardized on the
byte-literal hex notation `0x01` / `0x02` everywhere; the
"v" prefix doubled up with `hash_version =` already in the
formula and added no information.

P1 (line 132): hash binding used raw `∥` concatenation of
variable-length fields, opening a length-extension /
boundary-shift adversary surface. Added an explicit
`encode(·)` wrapper per field with a canonical-encoding
section: 1-byte version, 32-byte fixed-width digests for
content/policy/commitment hashes, and `len:u32-be ∥ bytes`
length-prefix framing for variable-length identifiers
(budget_id, policy_version, node_id). Forward-compatibility
preserved — future schemes (`hash_version >= 0x02`) can pick
different framing (CBOR / Protobuf / RFC 8949 §3.1 TLV) and
the version prefix tells verifiers which framing applies.

All 4 Codex/Copilot threads (PRRT_kwDOSF9kNM59SMrz,
PRRT_kwDOSF9kNM59SNsm, PRRT_kwDOSF9kNM59SNsy,
PRRT_kwDOSF9kNM59SNs2) addressed in this commit.
```

### `acbb27f1cfb2b10b70b71dae678fcada29b2878e` -- 2026-04-25T04:14:05Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `docs/research/blake3-receipt-hashing-v0-design-input-to-lucent-ksk-adr-2026-04-23.md`

**Message:**

```
drain(#268 lint): MD032 — line-leading + interpreted as list bullet (…

…wrap fix)
```

### `6aad1713c6629bfc02a063449ff8d8bdec969576` -- 2026-04-25T04:17:28Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `docs/research/blake3-receipt-hashing-v0-design-input-to-lucent-ksk-adr-2026-04-23.md`

**Message:**

```
drain(#268 P1+P1 Codex): replay-determinism on signer view + UTF-8/NF…

…C byte encoding

Two new Codex P1 findings on the BLAKE3 receipt-hashing v0 doc:

P1 (line 226) — replay determinism vs current signer set:
The req #4 said "compare commitment vs CURRENT signer-view",
which makes receipt validity time-dependent — the moment the
live signer set rotates, every prior receipt becomes invalid.
Replay-determinism breaks. Fix: validate against the signer
set authoritative at the receipt's claimed `policy_version`
(recoverable from `policy_version` + dispute-process
commitment-opening). Receipt-creation-time race-checking is
moved to the receipt-creation step; the replay gate catches
*forged* commitments only.

P1 (line 157) — canonical text-to-byte mapping:
The `len:u32-be ∥ bytes` framing for variable-length
identifiers (`budget_id`, `policy_version`, `node_id`)
specified the framing but not how to derive `bytes` from
the identifier string. Added explicit binding:
`bytes = NFC-normalised UTF-8 octets` — Unicode Normalization
Form C per Unicode Annex #15, then UTF-8 encoded. NFC fixes
visually-identical-but-byte-different forms (e.g., precomposed
vs decomposed accents); UTF-8 is the canonical text→byte map.
EOF
```

### `3cfa4345136dee87ed11fa43777cd97887c1a7e3` -- 2026-04-25T04:20:29Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `docs/research/blake3-receipt-hashing-v0-design-input-to-lucent-ksk-adr-2026-04-23.md`

**Message:**

```
drain(#268 P1+P2 Codex): correct adversary terminology + decouple CBO…

…R/TLV citations

P1 (line 144) — terminology correction:
"length-extension / boundary-shift adversary surface"
incorrectly conflated two distinct attacks. BLAKE3 is built
on a tree-hash construction with finalisation flags — it is
NOT vulnerable to length-extension the way SHA-256 and MD5
are. The actual risk in raw concatenation is boundary-shift
/ collision-by-reframing only. Updated the wording to name
that risk explicitly and added a parenthetical noting that
length-extension is NOT a concern with BLAKE3.

P2 (line 162) — CBOR vs TLV reference correction:
'domain-separated TLV per RFC 8949 §3.1' conflated two
distinct concepts: RFC 8949 is CBOR (tagged data items), and
'domain-separated TLV' is a separate framing concept. Split
into two parallel options: 'CBOR per RFC 8949' (one option)
and 'a domain-separated TLV scheme' (another, no specific RFC
attached because TLV is generic). Future ADR can pick either
or define a custom TLV; the v0 doc no longer mis-cites.
```

### `7d05d92b6c8d9d33f023a492c0c138a3a48546eb` -- 2026-04-25T04:30:51Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `docs/research/blake3-receipt-hashing-v0-design-input-to-lucent-ksk-adr-2026-04-23.md`

**Message:**

```
drain(#268 P1×3 Codex): version-policy gate + retired-key restriction…

… + signed key-version

Three substantive Codex P1 findings on the v0 receipt-hashing design:

P1 (line 229) — version policy gate beyond unknown:
Req #2 only fail-closed on unknown hash_version. Updated to
also reject DEPRECATED versions per a policy registry
(lucent-ksk governance artifact). Prevents forgery under an
old-but-still-mechanically-recognised version that was
retired due to weakness. Historical receipts remain
verifiable for audit; new receipts under deprecated versions
are refused.

P1 (line 211) — retired key versions:
Rotation introduced agent_key_version/node_key_version but
didn't restrict NEW receipts from using retired key versions.
Added: separate registry of retired key versions blocks
creation of new receipts under retired versions; historical
receipts under retired versions remain verifiable
(replay-determinism preserved) but the signing path refuses
to produce more.

P1 (line 203) — signed key-version (authenticated metadata):
The notation `Sign_{sk, *_key_version}(h_r)` was ambiguous
about whether *_key_version was authenticated. If it's
unsigned metadata, an attacker can swap the declared version
to one that points at a public key for a different signature
algorithm. Fix: bind the version INSIDE the signed message
(`Sign_{sk}(version ∥ h_r)`) and verify by recomputing the
signing input from the declared version. Verification block
added showing the explicit lookup + recompute pattern.

Also reframed line 120 to make the field-count reasoning
explicit (Amara's 7 base + hash_version + parameter_file_sha
= 9 v0 fields) so the count claim isn't load-bearing on the
preceding paragraph alone.
```

### `61a2bd48d1b0bcbf6fc5c1041583694cc1d0d6ec` -- 2026-04-25T04:36:15Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `docs/research/blake3-receipt-hashing-v0-design-input-to-lucent-ksk-adr-2026-04-23.md`

**Message:**

```
drain(#268 P1+P1 Codex): u32-be encoding for key-version + issuance-e…

…poch gate on deprecated hash_version

Two more substantive Codex P1 findings:

P1 (line 208) — canonical encoding for key-version:
The signature scheme bound *_key_version into the signed
message but didn't specify the byte encoding. Added explicit
`encode_u32_be` wrapper + an Encoding section: 4-byte
big-endian unsigned integer, monotonic from 1, with version 0
reserved for uninitialised. Fixed-width avoids needing a
length prefix (every version is exactly 4 bytes).

P1 (line 260) — issuance-epoch gate on deprecation:
Unconditionally rejecting receipts with deprecated
hash_version breaks audit/replay of historical receipts that
were valid when issued. Updated to issuance-epoch gate:
receipts issued BEFORE the version's deprecation cutoff
remain valid for audit; receipts claiming an issuance epoch
AFTER the cutoff under that version are rejected. Registry
stores (version, deprecated_after_epoch) tuples; verifier
compares claimed issuance epoch against deprecation epoch
for that version.
```
