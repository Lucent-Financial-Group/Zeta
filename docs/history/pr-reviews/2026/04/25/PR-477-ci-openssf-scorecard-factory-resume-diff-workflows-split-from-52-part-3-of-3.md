# PR #477 -- ci: OpenSSF Scorecard + FACTORY-RESUME diff workflows (split from #52, part 3 of 3)

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 477 |
| Title | ci: OpenSSF Scorecard + FACTORY-RESUME diff workflows (split from #52, part 3 of 3) |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-04-25T12:46:43Z |
| Merged at | 2026-04-25T12:48:35Z |
| Merge commit SHA | `ab123d72835d6b86acf7d87db9bb36e66299beca` |
| Branch | `security/scorecard-and-resume-diff-workflows` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/477 |
| Changed files | 2 |
| Additions / deletions | +255 / -0 |

## Description

## Summary

Two new CI workflows from #52, split as fresh branch off main.

- **\`.github/workflows/scorecard.yml\`** (89 lines): OpenSSF Scorecard weekly project-health audit (~20 heuristic checks). Results upload to GitHub Security → Code scanning as SARIF. Orthogonal to CVE scanners (Dependabot + \`dotnet list --vulnerable\`) — Scorecard audits *configuration*, not advisories.

- **\`.github/workflows/resume-diff.yml\`** (166 lines): FACTORY-RESUME claim-diff reviewer-helper. Triggers on PRs touching \`docs/FACTORY-RESUME.md\` or \`docs/SHIPPED-VERIFICATION-CAPABILITIES.md\` — emits structured claim-level diff as PR comment. NOT a gate — judgment stays with reviewer.

## Security review (both workflows)

Both comply with \`docs/security/GITHUB-ACTIONS-SAFE-PATTERNS.md\` (landed in #475):
- No attacker-controlled \`\${{ github.event.* }}\` interpolation in any \`run:\` block; all such values via \`env:\` block.
- Third-party actions SHA-pinned (Scorecard \`v2.4.3\` tagged 2025-09-30 by sschrock@google.com, SSH-signed; \`actions/checkout\` SHA-pinned).
- Token permissions scoped (read-only at workflow level; per-job grants for Scorecard SARIF upload / resume-diff PR comment).

## Why fresh branch (split from #52)

Part 3 of 3 per Aaron 2026-04-25 *"redo the work and split, easier from master"*. Composes with:
- #475 (security docs) — \`GITHUB-ACTIONS-SAFE-PATTERNS.md\` cited by both workflows
- #476 (Semgrep Rule 17 workflow-injection detection)
- #473 (Dependabot grouping) — orthogonal, also from #52 territory

After all four land, #52 can be closed as superseded.

## Test plan

- [x] Scorecard workflow runs weekly on schedule + on push to main; reads GITHUB_TOKEN with \`security-events: write\` and \`id-token: write\` for OIDC.
- [x] resume-diff workflow runs only on PRs touching the two resume-class doc files; binds all PR-event fields via \`env:\` block per safe-patterns.
- [x] Both workflows exempt from Semgrep Rule 17 by construction (no inline \`\${{ github.event.* }}\` in \`run:\`).

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 9 |
| Resolved threads | 0 |
| Unresolved threads | 9 |
| Total review comments | 9 |
| Total fix commits (touching thread paths) | 1 |

## Review threads

### Thread 1 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM59lYts`
- **Path**: `.github/workflows/resume-diff.yml`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-04-25T12:49:01Z on `.github/workflows/resume-diff.yml`:114 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Diff resume files from the PR merge base**

  The workflow computes `RAW_DIFF` with `git diff "$BASE_SHA" "$HEAD_SHA"`, which is a two-tip comparison, not a PR diff. On any PR where the base branch has moved since the feature branch diverged, this can include unrelated base-branch edits (or mask real PR changes), so the posted “claim-level diff” becomes inaccurate for reviewers. Use a merge-base diff (`BASE...HEAD` or explicit `git merge-base`) to keep the comment scoped to changes introduced by the PR.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 2 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM59lYtv`
- **Path**: `.github/workflows/resume-diff.yml`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-04-25T12:49:01Z on `.github/workflows/resume-diff.yml`:118 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Use POSIX whitespace class in grep pattern**

  The claim-line filter uses `grep -E` with `\s*`, but ERE does not treat `\s` as whitespace, so this behaves like `s*`. As a result, added/removed lines that begin with `+ ` or `- ` followed by bullet/table/header markers are often missed unless they also hit the keyword fallback, reducing the reliability of the “claim-bearing lines” section. Replace `\s*` with `[[:space:]]*` (or equivalent) for correct matching.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 3 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM59lZE_`
- **Path**: `.github/workflows/resume-diff.yml`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-25T12:50:42Z on `.github/workflows/resume-diff.yml`:19 (association: NONE)

  <pre>
  The header references `docs/security/GITHUB-ACTIONS-SAFE-PATTERNS.md`, but that file is not present in this branch (docs/security/ only contains INCIDENT-PLAYBOOK.md, SDL-CHECKLIST.md, SECURITY-BACKLOG.md, THREAT-MODEL*.md, V1-SECURITY-GOALS.md). Either add the referenced doc in the same PR, or change the reference so it can’t go stale / break when PRs merge out of order.
  ```suggestion
  # Security (reviewed 2026-04-22; this header documents the
  # pre-write checklist applied here, derived from the GitHub
  # Security Lab guide at
  ```
  </pre>

### Thread 4 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM59lZFM`
- **Path**: `.github/workflows/scorecard.yml`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-25T12:50:43Z on `.github/workflows/scorecard.yml`:33 (association: NONE)

  <pre>
  The comments reference `docs/security/GITHUB-ACTIONS-SAFE-PATTERNS.md` / `docs/security/SUPPLY-CHAIN-SAFE-PATTERNS.md`, but neither file exists in the current tree. Please either include those docs in this PR or adjust the references (e.g., point at an existing doc) so the workflow’s security rationale doesn’t link to missing files when merged independently.
  ```suggestion
  # score; no inbound attack surface. The expression-safety
  # checklist for this workflow is documented inline here.
  #
  # Action-pin content-review documented inline: ossf/scorecard-
  # action v2.4.3 tagged 2025-09-30 by sschrock@google.com
  # (OpenSSF maintainer), SSH-signed, GitHub API reports
  # verified=true reason=valid. Owner org `ossf` is the
  # OpenSSF foundation's GitHub org. Commit SHA
  # `4eaacf0543bb3f2c246792bd56e8cdeffafb205a` locks the
  # reviewed release.
  ```
  </pre>

### Thread 5 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM59lZFR`
- **Path**: `.github/workflows/scorecard.yml`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-25T12:50:43Z on `.github/workflows/scorecard.yml`:33 (association: NONE)

  <pre>
  This comment includes an individual’s email address (`sschrock@google.com`). Repo guidance is to avoid name attribution in code/docs bodies and prefer role/org references (docs/AGENT-BEST-PRACTICES.md:284-290). Consider rewriting to omit the personal identifier (e.g., reference the OpenSSF org / signed tag verification result) while keeping the audit intent.
  ```suggestion
  # 2025-09-30 under the OpenSSF `ossf` org by an OpenSSF
  # maintainer; SSH-signed, GitHub API reports verified=true
  # reason=valid. Owner org `ossf` is the OpenSSF
  # foundation's GitHub org. Commit SHA
  # `4eaacf0543bb3f2c246792bd56e8cdeffafb205a` locks the
  # reviewed release.
  ```
  </pre>

### Thread 6 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM59lZFc`
- **Path**: `.github/workflows/resume-diff.yml`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-25T12:50:43Z on `.github/workflows/resume-diff.yml`:118 (association: NONE)

  <pre>
  P0: The claim-line filter uses PCRE tokens (e.g., `\s` and `\b`) but runs `grep -E`, where those escapes are not whitespace/word-boundary. On ubuntu this will behave unexpectedly (often matching literal `s` / backspace), so `CLAIM_LINES` will be wrong or empty. Use a regex engine that supports these tokens (e.g., `grep -P`) or rewrite the patterns using POSIX ERE character classes (e.g., `[[:space:]]` and explicit boundary alternatives).
  ```suggestion
                    | grep -E '^[+-][[:space:]]*(- \*\*|\| |#{2,4} |.*(^|[^[:alnum:]_])(ships?|shipped|verified|proven|complete[ds]?|honest|already absorbed|implement(ed|s)?|in[- ]repo evidence)([^[:alnum:]_]|$))' \
  ```
  </pre>

### Thread 7 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM59lZFf`
- **Path**: `.github/workflows/resume-diff.yml`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-25T12:50:44Z on `.github/workflows/resume-diff.yml`:24 (association: NONE)

  <pre>
  This header comment claims all `${{ github.event.* }}` values are consumed only via `env:` blocks and that there is no inline `${{ ... }}` usage, but the workflow also uses `${{ github.event.pull_request.number }}` in `concurrency.group` and `${{ github.event.pull_request.head.sha }}` in the checkout `ref:` input. Please update the comment to match the actual usage (or refactor to align with the stated pattern).
  ```suggestion
  #   - Event-derived values consumed by shell scripts are passed
  #     via `env:` blocks and referenced as shell variables in
  #     `run:` scripts. There is no inline `${{ ... }}`
  #     interpolation inside any `run:` command. Inline
  #     expressions are used only in declarative workflow fields
  #     such as `concurrency.group` and action inputs, limited to
  #     the PR number (numeric) and commit SHAs (40-hex,
  #     injection-proof).
  ```
  </pre>

### Thread 8 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM59lZFn`
- **Path**: `.github/workflows/resume-diff.yml`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-25T12:50:44Z on `.github/workflows/resume-diff.yml`:37 (association: NONE)

  <pre>
  This comment says the runner is “digest-pinned (ubuntu-22.04)”, but `runs-on: ubuntu-22.04` is a moving label (not a digest / immutable image pin). Suggest rewording to “runner label pinned to ubuntu-22.04” (or similar) to avoid overstating supply-chain guarantees.
  ```suggestion
  #   - Runner label pinned to ubuntu-22.04.
  ```
  </pre>

### Thread 9 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM59lZFu`
- **Path**: `.github/workflows/resume-diff.yml`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-25T12:50:44Z on `.github/workflows/resume-diff.yml`:15 (association: NONE)

  <pre>
  This comment includes a specific contributor name (“Aaron”), but repo policy is to avoid name attribution in code/docs/skill bodies and use role references instead (docs/AGENT-BEST-PRACTICES.md:284-290). Please rewrite to a role-based reference (e.g., “human maintainer” / “honesty-floor owner”) without the proper name.
  ```suggestion
  # judgment stays with the reviewer and with the honesty-floor
  # owner.
  ```
  </pre>

## Fix commits (touching thread paths)

### `dcaed0e7048d6f767dfcbd8ff92d00bbba6a2531` -- 2026-04-25T12:46:23Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `.github/workflows/resume-diff.yml`
- `.github/workflows/scorecard.yml`

**Message:**

```
ci: add OpenSSF Scorecard + FACTORY-RESUME diff workflows (split from…

… #52)

Two new workflows from #52, split off as a fresh branch off main.

- **scorecard.yml** (89 lines): OpenSSF Scorecard weekly project-
  health audit (~20 heuristic checks: branch protection, signed
  releases, dangerous workflows, pinned dependencies, CII best
  practices, SAST coverage, token permissions, maintained-ness).
  Results upload to GitHub Security → Code scanning as SARIF.
  Orthogonal to CVE scanners (Dependabot + `dotnet list
  --vulnerable`) — Scorecard audits *configuration*, not
  advisories.

- **resume-diff.yml** (166 lines): FACTORY-RESUME claim-diff
  reviewer-helper. Triggers on PRs touching
  `docs/FACTORY-RESUME.md` or
  `docs/SHIPPED-VERIFICATION-CAPABILITIES.md` — the two files
  forming the factory's 'job-interview honesty' surface per
  Otto's resume memory. Emits a structured claim-level diff as
  a PR comment so reviewers can eyeball added/removed/modified
  claims for substantive drift. NOT a gate — judgment stays
  with the reviewer.

## Security review (both workflows)

Both workflows comply with the pre-write checklist in
`docs/security/GITHUB-ACTIONS-SAFE-PATTERNS.md` (landed in #475):

- No attacker-controlled `${{ github.event.* }}` interpolation
  inside any `run:` block; all such values bound via `env:` and
  read as shell variables.
- Third-party actions SHA-pinned.
- Token permissions scoped (read-only at workflow level; explicit
  per-job grants for Scorecard / PR-comment write where needed).
- Inputs limited to injection-proof types (commit SHAs / PR
  numbers).

## Why fresh branch (split from #52)

Part 3 of 3 of #52's split per Aaron 2026-04-25 'redo the work
and split, easier from master'. Composes with #475 (security
docs) and #476 (Semgrep workflow-injection rule) — together
these three PRs land the security/CI safe-patterns substrate
that #52 originally proposed.
```
