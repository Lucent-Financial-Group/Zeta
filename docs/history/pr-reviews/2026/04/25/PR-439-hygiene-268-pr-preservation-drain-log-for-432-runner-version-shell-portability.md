# PR #439 -- hygiene(#268): pr-preservation drain-log for #432 (runner-version shell portability)

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 439 |
| Title | hygiene(#268): pr-preservation drain-log for #432 (runner-version shell portability) |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-04-25T06:49:48Z |
| Merged at | 2026-04-25T07:54:35Z |
| Merge commit SHA | `1477dac7d6909ba052b6047abcd6c498e1f7db8d` |
| Branch | `drain/432-pr-preservation-log` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/439 |
| Changed files | 1 |
| Additions / deletions | +149 / -0 |

## Description

## Summary

Otto-268 backfill: drain-log for PR #432 (drain follow-up for #360 — `tools/lint/runner-version-freshness.sh` shell portability + allow-list scope), covering 7 substantive shell-portability findings including a P0 regression.

Per Otto-250 (PR review comments + responses + resolutions are high-quality training signals).

## Coverage — all FIX outcomes

- **P0 regression** (Codex P1 + Copilot P0, two reviewers converging): `warn` unbound under `set -u` — fixed via explicit `warn=0` initialization.
- **P1 path-resolution bug**: `cd "$REPO_ROOT"` before consuming `$@` — fixed via absolute-path normalization before chdir.
- **P1 pipefail masking**: `cmd1 | sed ... || true` masking sed failures — fixed via `{ grep -vE ... || true; } | sed` grouping.
- **P1 ERE-metachar escape** (two threads, Codex + Copilot): ALLOWED_LABELS / ROLLING_ALIASES missing the `escape_for_regex` treatment — fixed via uniform escape loop.
- **P1 matrix-entry validation gap**: NOT-ON-ALLOW-LIST scan skipping expression-form `runs-on:` workflows — fixed via line-number-aware `(^|^[0-9]+:)` exclude prefix.

## Pattern observations

1. Cross-reviewer convergence on the P0 (Codex P1 + Copilot P0 same finding) raises confidence the issue is real, not reviewer noise.
2. `set -euo pipefail` interactions with shell quirks are a recurring class — 3 of 7 findings.
3. ERE-metachar escape uniformity propagates across label arrays.
4. `grep -n` line-number prefix breaking `^` anchors in subsequent greps is a reusable pattern worth capturing.

## Test plan

- [x] All 7 thread IDs preserved verbatim with severity + outcome.
- [x] Smoke-test results captured for path-resolution + matrix-entry findings.
- [x] Drain-log shape matches existing `docs/pr-preservation/*-drain-log.md` template.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 1 |
| Resolved threads | 1 |
| Unresolved threads | 0 |
| Total review comments | 2 |
| Total fix commits (touching thread paths) | 1 |

## Review threads

### Thread 1 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59kVvJ`
- **Path**: `docs/pr-preservation/432-drain-log.md`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-25T06:52:17Z on `docs/pr-preservation/432-drain-log.md`:6 (association: NONE)

  <pre>
  P1/codebase-conventions: This drain-log includes non-verbatim agent name attribution in narrative prose (e.g., the parenthetical on the drain-session line, and later the "Drained by" footer). Repo guidance is to avoid names in docs and use role references instead, keeping names only inside explicitly verbatim quotes/logs. Please rewrite these narrative occurrences to role-refs (e.g., "reviewing agent", "maintainer") to match the convention used in other drain logs (see docs/AGENT-BEST-PRACTICES.md operational standing rule).
  ```suggestion
  Drain session: 2026-04-25 (reviewing agent, post-summary continuation autonomous-loop)
  Thread count at drain start: 7 unresolved (reviewer P1/P2 + reviewer P0/P1 findings)
  ```
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-25T07:54:32Z on `docs/pr-preservation/432-drain-log.md`:6 (association: MEMBER)

    <pre>
    **OTTO-279 SURFACE-CLASS.** `docs/pr-preservation/*-drain-log.md` is history-class per the Otto-279 surface-class refinement (alongside research / decisions / round-history / aurora-archive surfaces). First-name / agent-name attribution is permitted — preserves provenance of the drain session (who drained when, with what cron heartbeat). The role-ref-only rule applies to current-state operational substrate (skill bodies, code, README, public-facing prose, behavioural docs, threat models) — not history surfaces. See `docs/pr-preservation/_patterns.md` Surface-class taxonomy section + the synthesis-over-history third class for the full taxonomy. Resolving.
    </pre>

## Fix commits (touching thread paths)

### `8402bc5f2a172eb6eec59ebbfacc9e488ebcf274` -- 2026-04-25T06:49:31Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `docs/pr-preservation/432-drain-log.md`

**Message:**

```
hygiene(#268): pr-preservation drain-log for #432 (runner-version she…

…ll portability)

Otto-268 backfill task: drain-log for PR #432 covering 7 substantive
shell-portability findings — including a P0 regression (`warn` unbound
under `set -u`) caught independently by both Codex and Copilot.

Per Otto-250 training-signal discipline. Pattern observations capture
four load-bearing patterns: cross-reviewer convergence as a quality
signal; `set -euo pipefail` interaction with shell quirks as a
recurring class (3 of 7 findings); ERE-metachar escape uniformity
across label arrays; grep -n line-number prefix breaking `^` anchors
in subsequent greps as a reusable pattern.
```
