# PR #357 -- tools: PR-preservation minimal archive + Otto-207 session backfill (10 PRs)

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 357 |
| Title | tools: PR-preservation minimal archive + Otto-207 session backfill (10 PRs) |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-04-24T11:23:49Z |
| Merged at | 2026-04-25T02:43:39Z |
| Merge commit SHA | `9c325c09553ac3b71be9716e4b02120c901e2dfd` |
| Branch | `tools/pr-preservation-phase-2-minimal` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/357 |
| Changed files | 14 |
| Additions / deletions | +3377 / -0 |

## Description

## Summary

Maintainer Otto-207: *"are we saving these yet gitnative and have we backfilled them yet?"*

Honest answer was NO. The PR-preservation BACKLOG row (Otto-150..154, PR #335 in queue elevating to P1 + phased plan) specifies the discipline but never shipped capture tooling. This PR ships **Phase 0 minimal viable implementation** + **backfills 10 PRs** from this session.

## Tool

`tools/pr-preservation/archive-pr.sh` — one-shot bash script:

- Fetches review threads + reviews + comments via `gh api graphql`
- Writes `docs/pr-discussions/PR-<N>-<slug>.md` with YAML frontmatter
- Sections: PR description · Reviews · Review threads (with resolved/unresolved) · General comments
- No external deps beyond `gh` + `python3` stdlib + `bash 4+`

## Backfill (10 PRs this session)

| PR | Status | Threads | Reviews | Comments |
|---|---|---|---|---|
| #354 backlog-split Phase 1a | OPEN | 20 | 16 | 1 |
| #352 Server Meshing research | OPEN | 6 | 8 | 0 |
| #336 KSK naming doc | OPEN | 8 | 8 | 1 |
| #342 calibration-harness design | MERGED | 5 | 1 | 1 |
| #344 Amara 19th ferry absorb | MERGED | 8 | 1 | 1 |
| #346 DST compliance criteria | MERGED | 5 | 1 | 1 |
| #350 Frontier rename pass-2 | MERGED | 4 | 2 | 0 |
| #353 BACKLOG split design | MERGED | 6 | 1 | 0 |
| #355 Codex peer-review absorb | MERGED | 5 | 1 | 1 |
| #356 PR-resolve-loop row | MERGED | 5 | 1 | 0 |

Total: 72 threads + 40 reviews + 6 comments across ~97KB markdown.

## Long-term plan (per maintainer directive)

Remaining phases kept in the PR-preservation BACKLOG row (PR #335 in queue):

- **Phase 1** — GHA workflow on merge (automatic archive)
- **Phase 2** — historical backfill (all merged PRs)
- **Phase 3** — reconciliation (drift detection)
- **Phase 4** — redaction layer (privacy-pass for human-reviewer comments)

Scope out of this PR per maintainer *"make sure you backlog then to a proper long term solution"*.

## Composes with

- Otto-204c livelock-diagnosis (the gap this closes part of)
- Otto-204 PR-resolve-loop skill (this is step 4 of its 6-step cycle)
- PR #335 PR-preservation elevation (authoritative phased plan)

## Test plan

- [x] Script runs cleanly on test PR (#354)
- [x] Shellcheck exit 0 (SC2016 single-quote in GraphQL is intentional)
- [x] Output format validated across 10 varied PRs
- [x] No external dependencies beyond gh CLI + python3 stdlib

🤖 Generated with [Claude Code](https://claude.com/claude-code)


## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | true |
| Total threads | 36 |
| Resolved threads | 36 |
| Unresolved threads | 0 |
| Total review comments | 72 |
| Total fix commits (touching thread paths) | 9 |

## Review threads

### Thread 1 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59YQC2`
- **Path**: `tools/pr-preservation/archive-pr.sh`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-04-24T11:25:56Z on `tools/pr-preservation/archive-pr.sh`:60 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Paginate GraphQL connections before writing archive**

  The archive query hard-limits review threads and nested comments (`first: 100` / `first: 50`) without checking `pageInfo.hasNextPage` or following cursors, so high-activity PRs are silently truncated in the generated markdown. This breaks the stated preservation/audit goal because archives can look complete while dropping part of the discussion; the same pattern appears for reviews and general comments later in the query as well. Please paginate each connection (or fail loudly when truncated) before emitting the archive file.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-24T13:22:23Z on `tools/pr-preservation/archive-pr.sh`:60 (association: MEMBER)

    <pre>
    Fixed in 66e48f063082045871fb7d970bdb6fe213633524. The archive query now paginates all three top-level connections (reviewThreads / reviews / comments) via cursor loops in the Python driver, plus paginates per-thread comments when `comments.pageInfo.hasNextPage` is set. No silent truncation remaining. End-to-end test against PR #357 itself captures all 9 threads (see docs/pr-discussions/PR-0357-...md written by the new code).
    </pre>

### Thread 2 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59YQC_`
- **Path**: `tools/pr-preservation/archive-pr.sh`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-04-24T11:25:56Z on `tools/pr-preservation/archive-pr.sh`:107 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Validate pullRequest exists before dereferencing fields**

  The script assumes `d['data']['repository']['pullRequest']` is always present and immediately calls `pr.get(...)`, but GitHub GraphQL can return valid JSON with `pullRequest: null` (for example, an invalid PR number or missing access). In that case this path crashes with a Python traceback instead of the script's documented fetch-failure handling, making operational failures noisy and non-actionable for operators.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-24T13:22:26Z on `tools/pr-preservation/archive-pr.sh`:107 (association: MEMBER)

    <pre>
    Fixed in 66e48f063082045871fb7d970bdb6fe213633524. Now detect `pullRequest is None` in two places: (1) in the Python fetch driver right after the first GraphQL call, emitting a clear "not found / private / access denied" diagnostic and exiting 2; (2) again in the standalone validation pass before the formatter runs. Formatter also retains a defensive guard. Smoke-tested: `./tools/pr-preservation/archive-pr.sh 99999` exits 2 cleanly with "Could not resolve to a PullRequest".
    </pre>

### Thread 3 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59YSjM`
- **Path**: `tools/pr-preservation/archive-pr.sh`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-24T11:28:44Z on `tools/pr-preservation/archive-pr.sh`:49 (association: NONE)

  <pre>
  P0: With `set -e`, if `gh api graphql` exits non-zero (auth failure, network error), the script aborts immediately and never reaches the intended “fetch failed …” diagnostics / exit-code mapping. Wrap the `gh api graphql … &gt; "$TMP"` call in an explicit error-handling block so failures produce a clear message and the documented exit code (2).
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-24T13:22:28Z on `tools/pr-preservation/archive-pr.sh`:49 (association: MEMBER)

    <pre>
    Fixed in 66e48f063082045871fb7d970bdb6fe213633524. The fetch block is now wrapped in `set +e` / explicit `FETCH_RC=$?` / `set -e` so a non-zero gh exit no longer aborts before the diagnostic path. On failure we print "fetch failed for PR #$PR (exit $FETCH_RC)" and exit 2 per the header docstring.
    </pre>

### Thread 4 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59YSjm`
- **Path**: `tools/pr-preservation/archive-pr.sh`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-24T11:28:44Z on `tools/pr-preservation/archive-pr.sh`:108 (association: NONE)

  <pre>
  P0: The JSON check only validates that the response is parseable JSON; GraphQL error payloads (or `pullRequest: null`) will pass this check and then the Python block will crash on `d['data']...` / `pr.get(...)` with a stack trace. Detect `errors` in the response and/or a null `pullRequest` and exit 2 with a concise error message (e.g., not found / permission / auth).
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-24T13:22:30Z on `tools/pr-preservation/archive-pr.sh`:108 (association: MEMBER)

    <pre>
    Fixed in 66e48f063082045871fb7d970bdb6fe213633524. The fetch driver now rejects top-level `errors` and `pullRequest: null` before any field access, and a separate shell-side validation pass re-checks both conditions before the formatter runs. The formatter itself also guards (belt and suspenders). No more TypeError stack traces on invalid PRs.
    </pre>

### Thread 5 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59YSj4`
- **Path**: `tools/pr-preservation/archive-pr.sh`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-24T11:28:44Z on `tools/pr-preservation/archive-pr.sh`:47 (association: NONE)

  <pre>
  P1: The GraphQL query hard-codes `repository(owner: "Lucent-Financial-Group", name: "Zeta")`, so the script won’t work when run from forks or after a repo transfer/rename. Consider deriving owner/name from `gh repo view --json owner,name` (or `gh repo set-default`) and pass them as GraphQL variables.
  ```suggestion
  OWNER="$(gh repo view --json owner --jq '.owner.login')"
  REPO_NAME="$(gh repo view --json name --jq '.name')"

  TMP="$(mktemp)"
  trap 'rm -f "$TMP"' EXIT

  gh api graphql -F owner="$OWNER" -F name="$REPO_NAME" -F number="$PR" -f query='
    query($owner: String!, $name: String!, $number: Int!) {
      repository(owner: $owner, name: $name) {
  ```
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-24T13:22:32Z on `tools/pr-preservation/archive-pr.sh`:47 (association: MEMBER)

    <pre>
    Fixed in 66e48f063082045871fb7d970bdb6fe213633524. Owner/name now come from `gh repo view --json nameWithOwner -q .nameWithOwner` at runtime, passed as GraphQL `$owner`/`$name` variables. If gh cannot resolve the repo, we hard-fail with exit 1 rather than silently falling back ("never silently wrong" — factory rule). Works from forks and after rename.
    </pre>

### Thread 6 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59YSkI`
- **Path**: `tools/pr-preservation/archive-pr.sh`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-24T11:28:44Z on `tools/pr-preservation/archive-pr.sh`:90 (association: NONE)

  <pre>
  P1: The script and header comment say it writes “all review threads + reviews + comments”, but the GraphQL query is capped at `reviewThreads(first: 100)`, per-thread `comments(first: 50)`, `reviews(first: 50)`, and `comments(first: 100)` with no pagination. Either implement pagination (pageInfo/endCursor loops) or record in the output/frontmatter that results may be truncated at these limits.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-24T13:22:34Z on `tools/pr-preservation/archive-pr.sh`:90 (association: MEMBER)

    <pre>
    Fixed in 66e48f063082045871fb7d970bdb6fe213633524 (option: implement pagination, not update README to advertise truncation). All three top-level connections and per-thread comments now paginate via cursor loops. README updated to note pagination is in place so there is no gap between "captures all review threads + reviews + comments" claim and actual behaviour.
    </pre>

### Thread 7 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59YSkT`
- **Path**: `tools/pr-preservation/archive-pr.sh`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-24T11:28:45Z on `tools/pr-preservation/archive-pr.sh`:134 (association: NONE)

  <pre>
  P1: YAML frontmatter values like `author`, `head_ref`, and `base_ref` are emitted without quoting/escaping. Some valid ref names can include `#` or `:` which would break YAML parsing (treating the remainder as a comment or creating a mapping). Emit these scalars using a safe quoting routine (e.g., JSON-style quoting) for all string fields, not just `title`.
  ```suggestion
  lines.append(f'author: {json.dumps((pr.get("author") or {}).get("login") or "unknown")}')
  lines.append(f'state: {json.dumps(pr.get("state") or "")}')
  lines.append(f'created_at: {json.dumps(pr.get("createdAt") or "")}')
  if pr.get('mergedAt'):
      lines.append(f'merged_at: {json.dumps(pr.get("mergedAt") or "")}')
  if pr.get('closedAt'):
      lines.append(f'closed_at: {json.dumps(pr.get("closedAt") or "")}')
  lines.append(f'head_ref: {json.dumps(pr.get("headRefName") or "")}')
  lines.append(f'base_ref: {json.dumps(pr.get("baseRefName") or "")}')
  lines.append(f'archived_at: {json.dumps(archived_at)}')
  ```
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-24T13:22:36Z on `tools/pr-preservation/archive-pr.sh`:134 (association: MEMBER)

    <pre>
    Fixed in 66e48f063082045871fb7d970bdb6fe213633524. Added a `yaml_quote()` helper that wraps `json.dumps()` (double-quoted YAML strings with JSON-compatible escaping is valid YAML 1.2). Applied to every string-valued frontmatter field: title / author / state / created_at / merged_at / closed_at / head_ref / base_ref / archived_at / archive_tool. Only `pr_number` (integer) stays bare. Refs with `#` or `:` now parse cleanly.
    </pre>

### Thread 8 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59YSkk`
- **Path**: `tools/pr-preservation/README.md`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-24T11:28:45Z on `tools/pr-preservation/README.md`:33 (association: NONE)

  <pre>
  P0: README says archives are written to `docs/pr-discussions/PR-&lt;N&gt;-&lt;slug&gt;.md`, but the script actually writes `PR-&lt;N&gt;` zero-padded to 4 digits (e.g., `PR-0356-...`). Update the documented filename pattern so operators can reliably locate files and avoid accidental duplicates.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-24T13:22:38Z on `tools/pr-preservation/README.md`:33 (association: MEMBER)

    <pre>
    Fixed in 66e48f063082045871fb7d970bdb6fe213633524 — chose option A (update README to match script). Documented filename shape is now `PR-&lt;NNNN&gt;-&lt;slug&gt;.md` with zero-padding to four digits, explicitly called out in the Usage section with the rationale (lex-sort == num-sort for PR #0001..#9999). Picked this direction because the 10 backfilled files already use zero-padding and renaming them would churn cross-references and git history.
    </pre>

### Thread 9 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59YSkt`
- **Path**: `docs/pr-discussions/PR-0352-backlog-otto-180-server-meshing-spacetimedb-deep-research-ga.md`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-24T11:28:45Z on `docs/pr-discussions/PR-0352-backlog-otto-180-server-meshing-spacetimedb-deep-research-ga.md`:68 (association: NONE)

  <pre>
  P0: The backfilled `docs/pr-discussions/*.md` files include whitespace-only lines (e.g., after “Reviewed commit”) which violate markdownlint MD009 (no trailing spaces). Since the CI job runs markdownlint over `**/*.md` and `docs/pr-discussions/**` isn’t ignored, this will likely fail CI unless the archive output is normalized or the directory is added to `.markdownlint-cli2.jsonc` ignores (similar to `docs/amara-full-conversation/**`).
  ```suggestion

  ```
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-24T13:22:41Z on `docs/pr-discussions/PR-0352-backlog-otto-180-server-meshing-spacetimedb-deep-research-ga.md`:68 (association: MEMBER)

    <pre>
    Fixed in 66e48f063082045871fb7d970bdb6fe213633524. Ran `find docs/pr-discussions -name '*.md' -exec perl -i -pe 's/[ \t]+$//' {} +` across all existing archives — MD009 clean (grep-verified: zero trailing-whitespace lines remaining). Also added an inline trailing-whitespace strip inside the archive writer itself so future archive runs stay clean without needing the post-hoc sweep.
    </pre>

### Thread 10 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59aDVg`
- **Path**: `tools/pr-preservation/archive-pr.sh`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-04-24T13:26:42Z on `tools/pr-preservation/archive-pr.sh`:277 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Avoid re-fetching full payload on each pagination step**

  `paginate_top_level` always reissues the monolithic `QUERY`, so every extra page for one connection (e.g., `reviewsAfter`) also re-downloads page 1 of `reviewThreads` and `comments` (including nested thread comments from `comments(first: 100)`). On busy PRs this multiplies GraphQL cost and can hit API resource/rate limits before archival completes, even though pagination logic itself is correct. Splitting pagination into connection-specific queries (or otherwise avoiding refetch of unrelated connections) would prevent fetch failures under high-volume PRs.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-24T13:41:33Z on `tools/pr-preservation/archive-pr.sh`:277 (association: MEMBER)

    <pre>
    Codex P2 acknowledged and scoped, landed as a P2 research-grade BACKLOG row rather than an inline fix on this PR.

    **Three-outcome decision (Otto-226 / Otto-227, option 2 = narrow-fix + backlog):**

    - The finding is correct: `paginate_top_level` in `tools/pr-preservation/archive-pr.sh` reissues the full monolithic `QUERY` on every page, so walking extra pages of one connection (e.g. `reviewThreads`) re-fetches the full `reviews` + `pullRequest` payload. Works correctly; wastes GraphQL rate budget on PRs with many threads.
    - A proper fix is a ~30-60 LOC refactor: split the single `QUERY` into per-connection GraphQL fragments (one per `reviewThreads`, `reviews`, `comments-per-thread`) with separate cursors, compose results client-side, preserve the end-to-end archive format. That scope is beyond a single review-response commit, and risks regressing the self-hosting smoke test we just validated.
    - This PR keeps the current pagination shape. The refactor is queued as a P2 row at end of `## P2 — research-grade` in `docs/BACKLOG.md`, scope locked, citing this thread.

    **Commit:** dfc8ef554f094a32bf54356bd2a1b1029064b13c
    **BACKLOG row:** `docs/BACKLOG.md` — *"PR-archive pagination refactor — per-connection cursor pagination, not whole-query refetch"* (end of `## P2 — research-grade`)

    Resolving.
    </pre>

### Thread 11 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59aYTl`
- **Path**: `tools/pr-preservation/archive-pr.sh`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-04-24T13:45:51Z on `tools/pr-preservation/archive-pr.sh`:390 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Preserve trailing whitespace in archived discussion text**

  The formatter rewrites every output line with `rstrip()`, which removes trailing spaces from PR bodies/reviews/comments before writing the archive. This changes markdown semantics in real cases (for example, GitHub comments that use two trailing spaces for hard line breaks), so the generated file is no longer a faithful audit copy of the original discussion. Since this tool is explicitly for durable preservation, this normalization should not be applied to user-authored text.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-24T14:38:59Z on `tools/pr-preservation/archive-pr.sh`:390 (association: MEMBER)

    <pre>
    Fixed in a5331f7. Dropped the per-line `rstrip()` normalization so trailing whitespace (e.g. markdown two-space hard-line-breaks) is preserved verbatim — the archive is a faithful audit copy again. Ran into a tension with a sibling thread on this same line (MD012 blank-line runs) so also added a blank-line-run collapse that normalizes 3+ blank lines to 2 without touching in-line trailing whitespace.
    </pre>

### Thread 12 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59aZsA`
- **Path**: `tools/pr-preservation/archive-pr.sh`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-24T13:47:01Z on `tools/pr-preservation/archive-pr.sh`:390 (association: NONE)

  <pre>
  The output normalization only strips trailing whitespace; several generated archives (including the backfilled ones in this PR) contain 3+ consecutive blank lines (e.g., around &lt;details&gt; blocks), which will trip markdownlint MD012 because `docs/pr-discussions/**` is not in the markdownlint ignore list. Consider adding a safe normalization step that collapses multiple consecutive blank lines (or alternatively add `docs/pr-discussions/**` to `.markdownlint-cli2.jsonc` ignores) and then regenerate the backfilled archives so CI stays green.
  ```suggestion
  content = '\n'.join(line.rstrip() for line in content.split('\n'))
  # Collapse excessive blank-line runs so generated archives
  # do not trip markdownlint MD012.
  normalized_lines = []
  blank_run = 0
  for line in content.split('\n'):
      if line == '':
          blank_run += 1
      else:
          blank_run = 0
      if blank_run &lt;= 2:
          normalized_lines.append(line)
  content = '\n'.join(normalized_lines).rstrip() + '\n'
  ```
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-24T14:39:07Z on `tools/pr-preservation/archive-pr.sh`:390 (association: MEMBER)

    <pre>
    Fixed in a5331f7. Added the blank-line-run collapse you suggested (3+ consecutive blank lines -&gt; 2) so archives stay clean under markdownlint MD012 without needing to ignore `docs/pr-discussions/**`. Also regenerated the three backfilled archives that were tripping MD012 (PR-0350, PR-0352, PR-0354) — they're now clean. Kept user-authored trailing whitespace preserved (markdown two-space hard-line-breaks) per the sibling thread.
    </pre>

### Thread 13 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59aZs8`
- **Path**: `tools/pr-preservation/archive-pr.sh`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-24T13:47:02Z on `tools/pr-preservation/archive-pr.sh`:122 (association: NONE)

  <pre>
  The repo-detection comment says the script "falls back" to Lucent-Financial-Group/Zeta if `gh repo view` fails, but the implementation hard-fails with exit 1 and does not provide any fallback. Please align the comment with actual behavior (or implement the documented fallback) so operators aren’t misled.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-24T14:39:11Z on `tools/pr-preservation/archive-pr.sh`:122 (association: MEMBER)

    <pre>
    Fixed in a5331f7. The comment used to say "falls back" but the implementation hard-fails with exit 1 — updated the comment to match actual behavior: requires `gh repo view` to succeed, no silent fallback to a baked-in NWO. Rationale: better to fail loud than archive to the wrong repo path on a fork.
    </pre>

### Thread 14 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59aZtS`
- **Path**: `tools/pr-preservation/archive-pr.sh`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-24T13:47:02Z on `tools/pr-preservation/archive-pr.sh`:95 (association: NONE)

  <pre>
  `PR` is taken verbatim from argv and then `int(os.environ['PR'])` is called in the Python fetch driver. If the user passes a non-integer (or whitespace), this will raise a Python traceback and be reported as a generic “fetch failed … exit 1” with exit code 2. Consider validating `PR` is an integer in the shell before invoking Python and exiting 1 with a clear usage/error message.
  ```suggestion
  PR="$1"
  if ! [[ "$PR" =~ ^[0-9]+$ ]]; then
    echo "error: PR number must be a non-empty integer" &gt;&amp;2
    echo "usage: $0 &lt;PR-number&gt;" &gt;&amp;2
    exit 1
  fi
  ```
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-24T14:39:15Z on `tools/pr-preservation/archive-pr.sh`:95 (association: MEMBER)

    <pre>
    Fixed in a5331f7. Applied your suggested shell-side integer validation (regex `^[0-9]+$`) before invoking Python. Now a non-integer arg exits 1 with a clear "PR number must be a non-empty positive integer" message instead of a Python traceback mapped to exit 2. Verified: `./archive-pr.sh not-an-int` exits 1 with the usage line.
    </pre>

### Thread 15 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59aZtx`
- **Path**: `docs/pr-discussions/PR-0354-tools-backlog-split-phase-1a-generator-schema-example-row-aa.md`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-24T13:47:02Z on `docs/pr-discussions/PR-0354-tools-backlog-split-phase-1a-generator-schema-example-row-aa.md`:90 (association: NONE)

  <pre>
  This archived PR discussion includes multiple consecutive blank lines (e.g., between “**Reviewed commit:** …” and the following `&lt;details&gt;` block). With the current `.markdownlint-cli2.jsonc` config, MD012 (no multiple consecutive blank lines) will run on `docs/pr-discussions/**` and may fail CI. If the archive format is intended to be lint-clean, regenerate archives after adjusting the archiver to collapse excess blank lines; otherwise consider excluding `docs/pr-discussions/**` from markdownlint.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-24T14:39:21Z on `docs/pr-discussions/PR-0354-tools-backlog-split-phase-1a-generator-schema-example-row-aa.md`:90 (association: MEMBER)

    <pre>
    Fixed in a5331f7. Added the blank-line-run collapse to the archiver (3+ -&gt; 2) and regenerated this archive (PR-0354) along with PR-0350 and PR-0352 — all three were tripping MD012. Verified with a grep for `\\n\\n\\n\\n` across `docs/pr-discussions/*.md`: zero matches after regeneration.
    </pre>

### Thread 16 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59aZuF`
- **Path**: `tools/pr-preservation/README.md`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-24T13:47:02Z on `tools/pr-preservation/README.md`:115 (association: NONE)

  <pre>
  README lists a `bash 4+` dependency, but `archive-pr.sh` doesn’t appear to rely on bash-4-only features (and macOS still ships bash 3.2 by default). Either (a) enforce the version at runtime (fail fast with a helpful message), or (b) relax the documented requirement to just `bash` if bash 3.x is sufficient.
  ```suggestion
  - `bash`, POSIX `mktemp`
  ```
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-24T14:39:23Z on `tools/pr-preservation/README.md`:115 (association: MEMBER)

    <pre>
    Fixed in a5331f7 — relaxed the documented dependency to `bash` with a note that the script uses no bash-4-only features and macOS default bash 3.2 is fine. Chose option (b) per your suggestion; adding a runtime version check would be extra complexity with no payoff since the script genuinely doesn't need 4+.
    </pre>

### Thread 17 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59aZun`
- **Path**: `tools/pr-preservation/README.md`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-24T13:47:03Z on `tools/pr-preservation/README.md`:7 (association: NONE)

  <pre>
  The intro paragraph says the tool writes `docs/pr-discussions/PR-&lt;N&gt;-&lt;slug&gt;.md`, but later the Usage section specifies the zero-padded `PR-&lt;NNNN&gt;-&lt;slug&gt;.md` naming. Please make the intro consistent with the actual filename format to avoid operators looking for the wrong path.
  ```suggestion
  `docs/pr-discussions/PR-&lt;NNNN&gt;-&lt;slug&gt;.md` for durable
  ```
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-24T14:39:25Z on `tools/pr-preservation/README.md`:7 (association: MEMBER)

    <pre>
    Fixed in a5331f7. README intro now says `docs/pr-discussions/PR-&lt;NNNN&gt;-&lt;slug&gt;.md` (zero-padded four digits) — matches the Usage section and the implementation.
    </pre>

### Thread 18 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59aZu0`
- **Path**: `tools/pr-preservation/archive-pr.sh`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-24T13:47:03Z on `tools/pr-preservation/archive-pr.sh`:25 (association: NONE)

  <pre>
  Header comment still describes the output path as `docs/pr-discussions/PR-&lt;N&gt;-&lt;slug&gt;.md`, but the implementation writes `PR-{number:04d}-...` (zero-padded to 4 digits). Consider updating the header comment so it matches the actual filename format (and the README’s `PR-&lt;NNNN&gt;-...` wording).
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-24T14:39:27Z on `tools/pr-preservation/archive-pr.sh`:25 (association: MEMBER)

    <pre>
    Fixed in a5331f7. Header comment block now documents the zero-padded `PR-&lt;NNNN&gt;-&lt;slug&gt;.md` filename shape, matching the implementation's `f'PR-{number:04d}-{slug}.md'` and the README.
    </pre>

### Thread 19 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59bWi5`
- **Path**: `tools/pr-preservation/archive-pr.sh`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-04-24T14:42:54Z on `tools/pr-preservation/archive-pr.sh`:325 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Use PR number as stable archive filename key**

  The output path is keyed by a slug derived from the current PR title, so re-archiving after a title edit writes a second file instead of updating the existing archive for that PR number. This leaves stale duplicates (`PR-####-old-title.md` plus `PR-####-new-title.md`) and breaks the expected one-record-per-PR behavior for reconciliation/backfill workflows.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-24T14:50:39Z on `tools/pr-preservation/archive-pr.sh`:325 (association: MEMBER)

    <pre>
    Fixed in 549369f. PR number is now the canonical archive key: on re-archive, we glob for existing `PR-&lt;NNNN&gt;-*.md` and reuse that path regardless of current title (option b from your suggestion — preserves the human-friendly slug while ensuring idempotency). Smoke-tested: running `./archive-pr.sh 357` wrote back to the same PR-0357-* file, no orphan produced.
    </pre>

### Thread 20 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59bWi_`
- **Path**: `tools/pr-preservation/archive-pr.sh`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-04-24T14:42:54Z on `tools/pr-preservation/archive-pr.sh`:369 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Preserve leading whitespace in archived comment bodies**

  Comment/review text is normalized with `.strip()`, which removes leading spaces and leading blank lines from user-authored content before writing the archive. This is lossy for markdown that intentionally starts with indentation (for example indented code blocks) and undermines the script’s preservation/audit goal; the same pattern is used for thread and general comments as well.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-24T14:50:42Z on `tools/pr-preservation/archive-pr.sh`:369 (association: MEMBER)

    <pre>
    Fixed in 549369f. Switched `.strip()` to `.rstrip('\n')` for review bodies, thread-comment bodies, and general-comment bodies so leading whitespace (indented code blocks, nested bullets) survives the archive round-trip. Only trailing newlines are stripped now. Review state check on empty bodies uses `body_text.strip()` as a boolean guard only — does not mutate the archived text.
    </pre>

### Thread 21 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59bkfx`
- **Path**: `tools/pr-preservation/archive-pr.sh`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-24T14:55:40Z on `tools/pr-preservation/archive-pr.sh`:34 (association: NONE)

  <pre>
  P0: This header says the script adds a “truncation warning for threads (&gt;100) and per-thread comments (&gt;100)”, but the implementation only paginates; it never emits any warning. Either implement the warning (e.g., when a connection requires &gt;1 page) or remove the claim so operators aren’t misled about what diagnostics they’ll get.
  ```suggestion
  #   - Pagination for threads (&gt;100) and per-thread comments
  #     (&gt;100).
  ```
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-24T15:38:09Z on `tools/pr-preservation/archive-pr.sh`:34 (association: MEMBER)

    <pre>
    Fixed in 0f655b49824d4c0a055bfa19355d282456d6e1a3. Removed the "+ truncation warning" claim from the header comment block. The implementation paginates without emitting a warning; the comment now describes actual behaviour (just "Pagination for threads (&gt;100) and per-thread comments (&gt;100)."). Resolving.
    </pre>

### Thread 22 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59bkgS`
- **Path**: `tools/pr-preservation/archive-pr.sh`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-24T14:55:41Z on `tools/pr-preservation/archive-pr.sh`:384 (association: NONE)

  <pre>
  P0: `body.rstrip()` strips spaces/tabs from the end of the PR description’s final line. That conflicts with the goal of preserving markdown semantics (two trailing spaces = hard line break). Use a newline-only trim (e.g., rstrip of `\n`) or avoid trimming entirely for user-authored text.
  ```suggestion
      lines.append(body.rstrip('\n'))
  ```
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-24T15:38:10Z on `tools/pr-preservation/archive-pr.sh`:384 (association: MEMBER)

    <pre>
    Fixed in 0f655b49824d4c0a055bfa19355d282456d6e1a3. Switched `body.rstrip()` to `body.rstrip('\n')` on the PR-description block so trailing spaces on the last line (markdown two-space hard-line-break "  \n") are preserved. Resolving.
    </pre>

### Thread 23 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59bkgg`
- **Path**: `tools/pr-preservation/archive-pr.sh`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-24T14:55:41Z on `tools/pr-preservation/archive-pr.sh`:457 (association: NONE)

  <pre>
  P0: `content = '\n'.join(lines).rstrip() + '\n'` (and the later `.rstrip()` after collapsing blanks) will still remove trailing spaces from the last line of the archive, so the output is not a fully faithful copy even after removing per-line trimming. Prefer trimming only trailing newlines (or leaving content unchanged) so end-of-file hard line breaks aren’t lost.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-24T15:38:11Z on `tools/pr-preservation/archive-pr.sh`:457 (association: MEMBER)

    <pre>
    Fixed in 0f655b49824d4c0a055bfa19355d282456d6e1a3. Both rstrip sites (before and after the blank-line-run collapse) now use `.rstrip('\n')` so end-of-file hard-line-breaks are preserved. The archive round-trip is now newline-only-stripped at EOF. Resolving.
    </pre>

### Thread 24 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59bkgt`
- **Path**: `docs/pr-discussions/PR-0357-tools-pr-preservation-minimal-archive-otto-207-session-backf.md`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-24T14:55:41Z on `docs/pr-discussions/PR-0357-tools-pr-preservation-minimal-archive-otto-207-session-backf.md`:86 (association: NONE)

  <pre>
  P0: This archive contains a whitespace-only line (line 85) which will trip markdownlint MD009 (no trailing spaces) because `docs/pr-discussions/**` is not in the markdownlint ignore list. Either normalize whitespace-only lines to empty during archive generation (while still preserving meaningful inline trailing spaces), or add this directory to markdownlint ignores to keep CI green.
  ```suggestion

  ```
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-24T15:38:28Z on `docs/pr-discussions/PR-0357-tools-pr-preservation-minimal-archive-otto-207-session-backf.md`:86 (association: MEMBER)

    <pre>
    Fixed in 0f655b49824d4c0a055bfa19355d282456d6e1a3. The archiver now normalizes whitespace-only lines (containing only spaces/tabs) to empty while preserving meaningful inline trailing whitespace — a whitespace-only line cannot be a markdown hard-line-break because there is no preceding text on the same line. Lines with any non-whitespace character keep their trailing whitespace intact (two-space hard-line-break discipline preserved). Regenerated PR-0357 archive; grep-verified zero whitespace-only lines across all four regenerated archives. Resolving.
    </pre>

### Thread 25 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59bkg-`
- **Path**: `docs/pr-discussions/PR-0354-tools-backlog-split-phase-1a-generator-schema-example-row-aa.md`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-24T14:55:42Z on `docs/pr-discussions/PR-0354-tools-backlog-split-phase-1a-generator-schema-example-row-aa.md`:84 (association: NONE)

  <pre>
  P0: This archive includes a whitespace-only line (line 83) which will fail markdownlint MD009 (no trailing spaces). If the intent is for `docs/pr-discussions/**` to be lint-clean, the archiver needs to normalize whitespace-only lines (or the directory should be markdownlint-ignored like other verbatim archives).
  ```suggestion

  ```
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-24T15:38:29Z on `docs/pr-discussions/PR-0354-tools-backlog-split-phase-1a-generator-schema-example-row-aa.md`:84 (association: MEMBER)

    <pre>
    Fixed in 0f655b49824d4c0a055bfa19355d282456d6e1a3. Same fix as the sibling threads on PR-0357 and PR-0350 archives — archiver now normalizes whitespace-only lines to empty (while preserving inline trailing whitespace for hard-line-breaks). Regenerated PR-0354 archive and verified zero whitespace-only lines remain. Resolving.
    </pre>

### Thread 26 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59bkhN`
- **Path**: `docs/pr-discussions/PR-0350-docs-frontier-rename-pass-2-hindu-ff7-egyptian-greek-norse-s.md`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-24T14:55:42Z on `docs/pr-discussions/PR-0350-docs-frontier-rename-pass-2-hindu-ff7-egyptian-greek-norse-s.md`:63 (association: NONE)

  <pre>
  P0: Whitespace-only line at 62 will violate markdownlint MD009 (no trailing spaces). This appears to be a systematic artifact of archiving Codex connector comments; consider normalizing whitespace-only lines during archive generation or excluding `docs/pr-discussions/**` from markdownlint if verbatim preservation is required.
  ```suggestion

  ```
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-24T15:38:30Z on `docs/pr-discussions/PR-0350-docs-frontier-rename-pass-2-hindu-ff7-egyptian-greek-norse-s.md`:63 (association: MEMBER)

    <pre>
    Fixed in 0f655b49824d4c0a055bfa19355d282456d6e1a3. You're right — this was a systematic artifact from Codex connector comments that emit 4-space indented lines. Picked option (a) your suggestion: normalize whitespace-only lines during archive generation so `docs/pr-discussions/**` stays in-scope for markdownlint (no directory-level ignore). The normalizer only touches whitespace-only lines; lines with any non-whitespace character keep their trailing whitespace intact (two-space hard-line-break discipline preserved). Regenerated PR-0350 archive; verified clean. Resolving.
    </pre>

### Thread 27 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59cSL1`
- **Path**: `tools/pr-preservation/archive-pr.sh`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-04-24T15:40:40Z on `tools/pr-preservation/archive-pr.sh`:574 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Preserve original blank-line runs in archived comment text**

  The formatter globally collapses every run of blank lines to at most two after assembling the archive, which silently rewrites user-authored bodies. In PR comments/reviews that include fenced code blocks, logs, or templates where 3+ consecutive blank lines are intentional, this changes the preserved content and breaks the script’s stated audit-fidelity goal. This should be limited to tool-generated scaffolding (or removed) so user text is archived verbatim.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-24T15:59:55Z on `tools/pr-preservation/archive-pr.sh`:574 (association: MEMBER)

    <pre>
    Fixed in f2e25a8f1d97280782a02e705a894a128f2d452c. Added a fenced-code-block carve-out: the formatter now toggles code-fence state while scanning (```/~~~ at start of line) and SKIPS both the blank-line-run collapse and the whitespace-only normalization inside fenced regions. Outside fences, MD012/MD009 hygiene still applies to tool-generated scaffolding so archives stay lint-clean.

    Rationale: markdownlint MD012 already exempts fenced code from the "no multiple consecutive blank lines" rule by design — this fix aligns with the linter's own semantics. Fenced regions in PR review text are exactly where audit fidelity must win over scaffolding hygiene (logs, templates, preformatted payloads live there).

    Smoke-tested against PR #357 itself: regen produced +95/-12 lines of recovered content (mostly inside &lt;details&gt; fenced Codex/Copilot connector payloads the prior collapse was truncating). Archive-file churn reverted on this branch — regeneration belongs in a separate PR. Gates: `bash -n` clean + `shellcheck` clean. Resolving.
    </pre>

### Thread 28 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59cqzd`
- **Path**: `tools/pr-preservation/archive-pr.sh`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-04-24T16:03:07Z on `tools/pr-preservation/archive-pr.sh`:504 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Track opening fence marker before toggling code-block mode**

  The fence detector flips `in_fence` on any line that starts with ``` or ~~~, but Markdown fences only close with the same delimiter character and at least the opening length. In PR content that uses a 4-backtick fence to show a literal triple-backtick line (e.g., ````...```json...````), this logic exits fence mode too early, so the later blank-line collapse/whitespace normalization can rewrite text that should be preserved verbatim. That produces lossy archives in exactly the audit-preservation workflow this script is meant to protect.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-25T02:26:24Z on `tools/pr-preservation/archive-pr.sh`:504 (association: MEMBER)

    <pre>
    Fixed in 9143752 — fence detector now tracks the opener marker ('`' vs '~') and only closes when the encountered fence line uses the SAME marker character. Different-marker fence lines inside an open fence fall through to the verbatim branch (preserved as content, no state flip). Matches CommonMark spec. Bash -n syntax OK.
    </pre>

### Thread 29 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59csbW`
- **Path**: `tools/pr-preservation/README.md`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-24T16:04:38Z on `tools/pr-preservation/README.md`:9 (association: NONE)

  <pre>
  The archive files intentionally preserve PR bodies/comments verbatim (including personal names and @handles), but `docs/AGENT-BEST-PRACTICES.md` explicitly forbids contributor name attribution in docs outside `memory/persona/**` and (optionally) `docs/BACKLOG.md`. Since this tool writes to `docs/pr-discussions/**`, either document an explicit carve-out for this archive directory (similar to other history/audit-trail exceptions) or adjust the approach so it doesn’t violate the standing rule.
  ```suggestion
  historical audit-trail storage outside of GitHub.

  `docs/pr-discussions/**` is an archive-only carve-out from
  the normal no-name-attribution rule for docs: files in this
  directory preserve PR bodies/comments verbatim as historical
  records, including contributor names and `@handles` when
  they appear in the source material.
  ```
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-25T02:26:26Z on `tools/pr-preservation/README.md`:9 (association: MEMBER)

    <pre>
    The archive's verbatim-names preservation is the correct policy per Otto-279 surface-class refinement (landed in docs/BACKLOG.md Otto-52 row with the Otto-279 reinforcement). docs/pr-preservation/** IS a history surface — sibling to docs/ROUND-HISTORY.md, docs/DECISIONS/, docs/aurora/, memory/persona/ — where first-name attribution IS appropriate for both humans and agent personas. The tension here is a confusion between forward-looking current-state surfaces (where names are role-refs) and history surfaces (where names are record-of-fact). The README's verbatim-preservation posture is correct; the AGENT-BEST-PRACTICES literal rule needs the research-docs / history-surfaces carve-out to make it consistent, which is the post-drain sweep in the Otto-52/Otto-279 BACKLOG row. Resolving.
    </pre>

### Thread 30 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59csco`
- **Path**: `tools/pr-preservation/archive-pr.sh`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-24T16:04:39Z on `tools/pr-preservation/archive-pr.sh`:111 (association: NONE)

  <pre>
  `gh repo view` is invoked with `-q .nameWithOwner`, but other repo scripts use `--jq` for this flag (e.g., `tools/hygiene/check-github-settings-drift.sh:47`). If `-q` isn’t supported for `gh repo view` on all versions, repo detection will fail and the archiver will exit 1 even when gh is authenticated. Prefer `--jq '.nameWithOwner'` for consistency and portability.
  ```suggestion
  REPO_NWO="$(gh repo view --json nameWithOwner --jq '.nameWithOwner' 2&gt;/dev/null || true)"
  ```
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-25T02:26:28Z on `tools/pr-preservation/archive-pr.sh`:111 (association: MEMBER)

    <pre>
    Fixed in 9143752 — replaced '`-q .nameWithOwner`' with '`--jq .nameWithOwner`' (long form) to match the rest of the factory's gh invocations (e.g. tools/hygiene/check-github-settings-drift.sh:47). Avoids any -q ambiguity across gh versions.
    </pre>

### Thread 31 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59jJj_`
- **Path**: `tools/pr-preservation/archive-pr.sh`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-25T02:30:37Z on `tools/pr-preservation/archive-pr.sh`:104 (association: NONE)

  <pre>
  `REPO_ROOT` falls back to `pwd` when `git rev-parse --show-toplevel` fails. However `gh repo view` may still succeed outside a git checkout (via `gh repo set-default`), causing the script to write `docs/pr-discussions/` under whatever directory the operator happens to be in. Given the goal is to create git-tracked archives, consider failing fast when not inside a git worktree (and align this with the header’s “repo detect failed” exit-code description).
  ```suggestion
  # Archive output must land under a real Git worktree so the
  # generated discussion file is git-tracked. Do not fall back
  # to the caller's current directory when repo detection fails.
  if ! REPO_ROOT="$(git rev-parse --show-toplevel 2&gt;/dev/null)"; then
    echo "error: could not detect repo root via 'git rev-parse --show-toplevel'. Run this inside a Git worktree." &gt;&amp;2
    exit 1
  fi
  ```
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-25T02:42:15Z on `tools/pr-preservation/archive-pr.sh`:104 (association: MEMBER)

    <pre>
    Fixed in 737192b479cae3cf3e4aa7d6f1b1581c7059cfbb — script now hard-fails if `git rev-parse --show-toplevel` fails. Prior fallback to `pwd` had the exact bug you described: `gh repo view` can succeed outside a checkout (via `gh repo set-default`), which would cause the archive to be written to a bogus REPO_ROOT. Now exits 1 with a clear error directing users to run from a Zeta checkout.
    </pre>

### Thread 32 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59jJkD`
- **Path**: `tools/pr-preservation/archive-pr.sh`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-25T02:30:37Z on `tools/pr-preservation/archive-pr.sh`:528 (association: NONE)

  <pre>
  The fenced-code-block detector only tracks the fence *marker* (``` vs ~~~) but not the fence *length*. In CommonMark, an opening fence of 4+ backticks must be closed by the same marker with length &gt;= opener; a line starting with ``` inside a ```` fence is content, not a closer. Without tracking length, the script can incorrectly exit `in_fence` early and then apply blank-line/whitespace normalization to what should be preserved verbatim, reducing audit fidelity for comments that use longer fences.
  ```suggestion
  fence_marker = None  # '`' or '~' — opener/closer marker must match
  fence_length = 0     # CommonMark closer length must be &gt;= opener length
  for raw_line in content.split('\n'):
      # Detect fenced-code-block boundaries by counting the
      # leading run of backticks or tildes after optional
      # indentation. Per CommonMark, a fenced block opens on
      # a run of 3+ matching markers, and closes only on the
      # SAME marker character with a run length &gt;= the opener.
      stripped = raw_line.lstrip()
      marker = None
      marker_length = 0
      if stripped:
          first = stripped[0]
          if first in ('`', '~'):
              while marker_length &lt; len(stripped) and stripped[marker_length] == first:
                  marker_length += 1
              if marker_length &gt;= 3:
                  marker = first
      if marker is not None:
          if not in_fence:
              in_fence = True
              fence_marker = marker
              fence_length = marker_length
              blank_run = 0
              collapsed.append(raw_line)
              continue
          if marker == fence_marker and marker_length &gt;= fence_length:
              in_fence = False
              fence_marker = None
              fence_length = 0
              blank_run = 0
              collapsed.append(raw_line)
              continue
          # Different-marker fence line, or a same-marker run
          # shorter than the opener, inside an open fence:
  ```
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-25T02:42:17Z on `tools/pr-preservation/archive-pr.sh`:528 (association: MEMBER)

    <pre>
    Fixed in 737192b479cae3cf3e4aa7d6f1b1581c7059cfbb — fence detector now tracks BOTH marker character and marker LENGTH per CommonMark §4.5. Opener records marker + length; closer must have same marker AND length &gt;= opener. An opener of 4 backticks now correctly contains 3-backtick lines as content, not closers — matches CommonMark nesting.
    </pre>

### Thread 33 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59jJkF`
- **Path**: `tools/pr-preservation/README.md`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-25T02:30:37Z on `tools/pr-preservation/README.md`:109 (association: NONE)

  <pre>
  The cross-reference `CLAUDE.md "agents, not bots"` doesn’t appear to exist (the phrase is defined in `GOVERNANCE.md §3` and in glossary/docs elsewhere, but not in `CLAUDE.md`). This is likely to confuse readers trying to follow the pointer; suggest changing this to cite `GOVERNANCE.md §3` alone or linking to the actual doc that defines the wording (e.g., `docs/GLOSSARY.md` “Agent (not \"bot\")”).
  ```suggestion
    GOVERNANCE.md §3 — Copilot and Codex are agents with
    agency and accountability, not bots.
  ```
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-25T02:42:18Z on `tools/pr-preservation/README.md`:109 (association: MEMBER)

    <pre>
    Fixed in 737192b479cae3cf3e4aa7d6f1b1581c7059cfbb — clarified the cross-ref. Canonical source for 'agents, not bots' terminology is GOVERNANCE.md §3 ("Contributors are agents, not bots"). CLAUDE.md carries a session-bootstrap pointer at the same rule. Reworded to name GOVERNANCE as canonical, CLAUDE.md as the pointer.
    </pre>

### Thread 34 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59jJkJ`
- **Path**: `tools/pr-preservation/archive-pr.sh`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-25T02:30:37Z on `tools/pr-preservation/archive-pr.sh`:123 (association: NONE)

  <pre>
  `mktemp` is invoked with no template (`TMP="$(mktemp)"`), which works on GNU coreutils but fails on macOS/BSD `mktemp` (it requires a template or `-t`). Since the README explicitly claims macOS is supported, this currently breaks the tool for macOS operators. Use a portable pattern (e.g., try `mktemp` with a template in the repo dir or fall back to `mktemp -t &lt;prefix&gt;`), and ensure failures produce the documented exit code.
  ```suggestion
  TMP=""
  if TMP="$(mktemp "${OUT_DIR}/archive-pr.XXXXXX" 2&gt;/dev/null)"; then
    :
  elif TMP="$(mktemp -t archive-pr 2&gt;/dev/null)"; then
    :
  else
    echo "error: could not create temporary file" &gt;&amp;2
    exit 2
  fi
  ```
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-25T02:42:20Z on `tools/pr-preservation/archive-pr.sh`:123 (association: MEMBER)

    <pre>
    Fixed in 737192b479cae3cf3e4aa7d6f1b1581c7059cfbb — mktemp now uses explicit template `zeta-archive-pr.XXXXXX` with `-t` flag. Works on both GNU coreutils (Linux) and BSD mktemp (macOS). Prior invocation with no argument was GNU-only.
    </pre>

### Thread 35 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59jJzU`
- **Path**: `tools/pr-preservation/archive-pr.sh`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-04-25T02:31:18Z on `tools/pr-preservation/archive-pr.sh`:533 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Honor CommonMark closing-fence syntax before exiting fence mode**

  Fence detection treats any line whose left-trimmed text starts with ````` or `~~~` as a fence token, and the later `marker == fence_marker` check closes the block without verifying that the line is a valid *closing* fence. In CommonMark, a closing fence may only be followed by spaces, so content lines like `````json` inside an outer fence are literal text, not terminators. In that scenario this logic exits `in_fence` early, and the subsequent blank-line/whitespace normalization can rewrite content that should remain verbatim in the archive.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-25T02:42:22Z on `tools/pr-preservation/archive-pr.sh`:533 (association: MEMBER)

    <pre>
    Fixed in 737192b479cae3cf3e4aa7d6f1b1581c7059cfbb (same fix as the Copilot fence-length thread). Tracks marker + length per CommonMark §4.5; closer must match both. Different-marker or shorter fence-shaped lines inside an open fence are preserved as content via the verbatim branch.
    </pre>

### Thread 36 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59jOrw`
- **Path**: `tools/pr-preservation/archive-pr.sh`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-04-25T02:44:45Z on `tools/pr-preservation/archive-pr.sh`:530 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Enforce CommonMark indent limit when detecting fences**

  Fence detection strips all leading whitespace (`raw_line.lstrip()`) before checking for ```/~~~, so lines indented by 4+ spaces are treated as fence markers even though CommonMark only recognizes fences with up to 3 leading spaces. In archived PR text that includes indented literal fence-like lines (for example inside indented code/log snippets), this can flip `in_fence` incorrectly and cause subsequent normalization logic to rewrite content that should be preserved verbatim, reducing audit fidelity.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-25T03:37:48Z on `tools/pr-preservation/archive-pr.sh`:530 (association: MEMBER)

    <pre>
    Fixed in #425 (8b5b22ef0f879d0767d9a52c812731291d15606a) — fence detector now counts leading spaces first; a line with 4+ spaces of indentation falls through to content branches per CommonMark §4.5 indented-code-block rule, not the fence-marker branch.
    </pre>

## Fix commits (touching thread paths)

### `5aa882b56094ff19c9afb47a50fa1a9478e789cd` -- 2026-04-24T11:23:22Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `docs/pr-discussions/PR-0350-docs-frontier-rename-pass-2-hindu-ff7-egyptian-greek-norse-s.md`
- `docs/pr-discussions/PR-0352-backlog-otto-180-server-meshing-spacetimedb-deep-research-ga.md`
- `docs/pr-discussions/PR-0354-tools-backlog-split-phase-1a-generator-schema-example-row-aa.md`
- `tools/pr-preservation/README.md`
- `tools/pr-preservation/archive-pr.sh`

**Message:**

```
tools: PR-preservation minimal archive tool + Otto-207 session backfi…

…ll (10 PRs)

Otto-207: maintainer "are we saving these yet gitnative and
have we backfilled them yet?" Honest answer was NO — the
PR-preservation BACKLOG row (Otto-150..154, PR #335 in queue)
specifies the discipline but never shipped the capture
tooling. This PR ships the minimal viable implementation
+ backfills 10 PRs from this session.

New tool:

- tools/pr-preservation/archive-pr.sh — one-shot bash
  script that fetches a PR's review threads, reviews, and
  comments via `gh api graphql` and writes them to
  docs/pr-discussions/PR-<N>-<slug>.md with YAML
  frontmatter (pr_number / title / author / state / dates
  / refs / archived_at / archive_tool).
- tools/pr-preservation/README.md — scope (Phase 0
  minimal vs Phase 1-4 longer plan), usage, output
  schema, backfill status, dependencies (bash + python3
  + gh; no external packages), cross-references to
  Otto-171 / Otto-204 / Otto-204c / PR #335.

Backfill (10 PRs archived this tick):

- PR #354 backlog-split Phase 1a
- PR #352 Server Meshing + SpacetimeDB research
- PR #336 KSK naming definition doc
- PR #342 calibration-harness Stage-2 design (merged)
- PR #344 Amara 19th ferry absorb (merged)
- PR #346 DST compliance criteria (merged)
- PR #350 Frontier rename pass-2 (merged)
- PR #353 BACKLOG split Phase 0 design (merged)
- PR #355 Codex first peer-agent deep-review absorb
  (merged)
- PR #356 PR-resolve-loop skill row (merged)

Total: 72 review threads + 40 reviews + 6 general comments
captured across ~97KB of archive markdown.

Long-term plan deliberately kept in BACKLOG row (Otto-150
..154 / PR #335 queue elevation) rather than expanded in
this commit's docs. Phase 0 shipping now; Phase 1 GHA
workflow + Phase 2 historical backfill + Phase 3
reconciliation + Phase 4 redaction layer remain queued
tickets. Per maintainer directive "make sure you backlog
then to a proper long term solution" — the phased plan
is already in PR #335 and covers the remaining work.

Discipline applied: active-management on the preservation
gap itself. Previous tick's "ship and pray" pattern is the
exact failure mode this tool begins to close (operator-
initiated archive instead of silent reliance on GitHub-
side conversation storage). Composes with Otto-204c
livelock-diagnosis memory + Otto-204 PR-resolve-loop
skill (this script is step 4 of that cycle's
conversation-preservation hook).

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
```

### `c2b6018fffe0c187e9dcbeccf0c9f144cf18b5e4` -- 2026-04-24T11:26:28Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/pr-preservation/README.md`

**Message:**

```
fix(#357): bot→agent terminology per GOVERNANCE §3 (maintainer Otto-208)

Maintainer Otto-208 flag on Phase 4 redaction-layer wording:
"No redaction — bot content + human content ... bot=agent."

Applied the Otto-156 pattern: Copilot + Codex + Claude Code
personas + github-actions are AGENTS with agency and
accountability (GOVERNANCE §3 + CLAUDE.md "Agents, not
bots."). Updated Phase 4 wording:

- "bot-review comments (Copilot, Codex) archive verbatim"
  →
  "agent-review comments (Copilot, Codex, Claude Code
  personas, github-actions) archive verbatim"
- Added explicit pointer to GOVERNANCE §3 + CLAUDE.md
  terminology convention.

PR body edit follows separately via `gh pr edit`.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
```

### `6f0737712b2ec0b71c281a53efe40a6cfb734198` -- 2026-04-24T13:21:46Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `docs/pr-discussions/PR-0350-docs-frontier-rename-pass-2-hindu-ff7-egyptian-greek-norse-s.md`
- `docs/pr-discussions/PR-0352-backlog-otto-180-server-meshing-spacetimedb-deep-research-ga.md`
- `docs/pr-discussions/PR-0354-tools-backlog-split-phase-1a-generator-schema-example-row-aa.md`
- `docs/pr-discussions/PR-0357-tools-pr-preservation-minimal-archive-otto-207-session-backf.md`
- `tools/pr-preservation/README.md`
- `tools/pr-preservation/archive-pr.sh`

**Message:**

```
fix(#357): 9 review threads — paginate, null-check, dynamic repo, YAM…

…L quoting, README alignment, trailing-ws strip

Addresses all 9 unresolved Copilot + Codex threads on PR #357
(Otto-226 review-drain discipline, three-outcome model: fix).

Script changes (tools/pr-preservation/archive-pr.sh):
- Paginate reviewThreads / reviews / comments at the top level
  AND per-thread comments via cursor loops (threads 1 + 6 —
  no silent truncation).
- Validate `pullRequest != null` and detect top-level
  GraphQL `errors` before dereferencing (threads 2 + 4).
- Capture `gh api graphql` exit code explicitly instead of
  letting `set -e` swallow the diagnostic path (thread 3).
- Derive owner/name dynamically from `gh repo view --json
  nameWithOwner` with a hard-fail if detection fails —
  works from forks and after rename (thread 5).
- Quote all YAML frontmatter string values via `json.dumps`
  (title / author / state / ISO timestamps / head_ref /
  base_ref / archived_at / archive_tool), so refs with `#`
  or `:` don't break parsing (thread 7).

Documentation alignment:
- README now shows zero-padded filename shape
  `PR-<NNNN>-<slug>.md` (e.g. `PR-0357-...`) matching the
  script's output (thread 8 — chose "align README to
  script" since the 10 backfilled files already use the
  zero-padded form and renaming them would churn links).
- README notes pagination is in place (no more "may be
  truncated" silent-behaviour gap tied to thread 6).

Backfilled archives:
- Stripped trailing whitespace across all existing
  `docs/pr-discussions/*.md` via `perl -i -pe 's/[ \t]+$//'`
  (thread 9 — MD009 compliance for the CI markdownlint
  gate).

Also adds `docs/pr-discussions/PR-0357-...md` as the
self-hosting smoke test: the archive tool successfully
drains its own review queue.

Validation:
- `bash -n` clean
- `shellcheck` clean (no findings)
- End-to-end: `./tools/pr-preservation/archive-pr.sh 357`
  writes 9 threads / 2 reviews / 0 comments to 12179 bytes
- Error path: PR #99999 exits 2 with clear diagnostic

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
```

### `218b2e0380bf1bfd68e1ff96f8357bfa301a552e` -- 2026-04-24T14:38:44Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `docs/pr-discussions/PR-0350-docs-frontier-rename-pass-2-hindu-ff7-egyptian-greek-norse-s.md`
- `docs/pr-discussions/PR-0352-backlog-otto-180-server-meshing-spacetimedb-deep-research-ga.md`
- `docs/pr-discussions/PR-0354-tools-backlog-split-phase-1a-generator-schema-example-row-aa.md`
- `docs/pr-discussions/PR-0357-tools-pr-preservation-minimal-archive-otto-207-session-backf.md`
- `tools/pr-preservation/README.md`
- `tools/pr-preservation/archive-pr.sh`

**Message:**

```
fix(#357): 8 review threads — integer PR validation, trailing-ws pres…

…ervation, MD012 blank-line collapse, README/header filename shape + bash dep

Second drain pass on PR #357 review threads. Eight threads from
agent reviewers; all fix-in-place.

Script changes (`tools/pr-preservation/archive-pr.sh`):

- Integer validation on `$PR` in the shell (pre-Python) — prevents
  a Python traceback + generic "fetch failed exit 2" diagnostic
  when a typo / non-integer is passed.
- Dropped the per-line `rstrip()` normalization. Markdown uses
  two trailing spaces as a hard-line-break; this tool is a
  faithful audit copy and must preserve that intent.
- Added a blank-line-run collapse (3+ consecutive blank lines
  -> 2) so generated archives stay clean under markdownlint
  MD012 without destroying user-authored formatting.
- Header comment now documents the zero-padded `PR-<NNNN>-<slug>`
  filename shape (matches the implementation + README).
- Header comment on repo-detection aligned with actual behavior
  (requires `gh repo view`, no silent fallback).

README changes (`tools/pr-preservation/README.md`):

- Intro uses `PR-<NNNN>-<slug>.md` (matches Usage + implementation).
- Dependency relaxed from `bash 4+` to `bash` with a note — the
  script uses no bash-4-only features and macOS ships bash 3.2.

Backfilled archives regenerated under the new collapse rule so
they stop tripping MD012:

- PR-0350 (Frontier rename pass-2)
- PR-0352 (Server Meshing / SpacetimeDB deep research)
- PR-0354 (backlog-split Phase 1a)
- PR-0357 (this PR — self-archive re-fetched)

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
```

### `1d1d2ab0474486b2d70486eeb9533b1b2eedb85e` -- 2026-04-24T14:50:24Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `docs/pr-discussions/PR-0357-tools-pr-preservation-minimal-archive-otto-207-session-backf.md`
- `tools/pr-preservation/archive-pr.sh`

**Message:**

```
fix(#357): 2 Codex P2 threads — PR-number stable filename key + prese…

…rve leading whitespace in archived bodies

Third-round review-thread drain on `tools/pr-preservation/archive-pr.sh`:

- `PRRT_kwDOSF9kNM59bWi5` (line 325): archive filename was derived
  from the title slug, so editing a PR title would write a second
  file instead of updating the existing record. Fix: PR number is
  now the canonical archive key. On re-archive, glob for an existing
  `PR-<NNNN>-*.md` and reuse its path regardless of current title.
  New PRs still land at `PR-<NNNN>-<slug>.md`.
- `PRRT_kwDOSF9kNM59bWi_` (line 369 + lines 388, 401): `.strip()`
  normalised review / thread-comment / general-comment bodies and
  destroyed semantically-significant leading indentation (indented
  code blocks, nested bullets). Switched to `.rstrip('\n')` so only
  trailing newlines are stripped; leading whitespace survives.

Smoke tested: `./archive-pr.sh 357` writes back to the same file
(no new PR-0357-* orphan), bash -n + shellcheck clean, diff shows
preserved `<details>` internal structure and indentation in archive.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
```

### `9bed33f8af3572dbc7c886571cfd4092138c3cb0` -- 2026-04-24T15:37:39Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `docs/pr-discussions/PR-0350-docs-frontier-rename-pass-2-hindu-ff7-egyptian-greek-norse-s.md`
- `docs/pr-discussions/PR-0352-backlog-otto-180-server-meshing-spacetimedb-deep-research-ga.md`
- `docs/pr-discussions/PR-0354-tools-backlog-split-phase-1a-generator-schema-example-row-aa.md`
- `docs/pr-discussions/PR-0357-tools-pr-preservation-minimal-archive-otto-207-session-backf.md`
- `tools/pr-preservation/archive-pr.sh`

**Message:**

```
fix(#357): 6 review threads — drop truncation-warning claim, preserve…

… last-line hard-line-breaks, normalize whitespace-only lines

Fourth drain pass on PR #357. Addresses 6 new P0 threads from
re-review:

- archive-pr.sh header said "Pagination + truncation warning
  for threads (>100)" but implementation only paginates, never
  emits a warning. Claim removed; comment now matches behaviour.
- `body.rstrip()` on the PR-description block stripped trailing
  spaces from the last line (kills markdown "  \n" hard-line
  breaks). Changed to `body.rstrip('\n')`.
- End-of-file `content.rstrip()` had the same problem — end-of-
  file hard-line-break would be lost. Changed to
  `content.rstrip('\n')` in both places (pre- and post-blank-
  line-collapse).
- Whitespace-only lines (e.g. "    " from Codex connector
  comments) tripped markdownlint MD009. Added a post-collapse
  normalization step: lines containing only whitespace are
  normalized to empty, while lines with any non-whitespace
  character keep trailing whitespace intact (two-space
  hard-line-breaks survive).

Regenerated four affected archives: PR-0350, PR-0352, PR-0354,
PR-0357. Verified: zero whitespace-only lines, zero 3+ blank-
line runs across all archives.

Syntax / shellcheck clean.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
```

### `3c4c9b84097f4a551341eae770f1b368e1a58aef` -- 2026-04-24T15:59:37Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/pr-preservation/archive-pr.sh`

**Message:**

```
fix(#357): Codex P1 audit-fidelity carve-out — skip blank-line collap…

…se inside fenced code blocks

Codex review thread on PR #357 (line 486, P1, unresolved after 4
prior drain rounds): the formatter globally collapses every run of
blank lines to at most 2 after assembling the archive, which silently
rewrites user-authored bodies. In PR comments / reviews that include
fenced code blocks, logs, or templates where 3+ consecutive blank
lines are intentional, this changes the preserved content and breaks
the script's stated audit-fidelity goal.

Narrow fix: toggle code-fence state while scanning (``` / ~~~ at the
start of a line, ignoring leading whitespace), and SKIP both the
blank-line-run collapse and the whitespace-only normalization inside
fenced regions. Outside fences, MD012 / MD009 hygiene still applies
to tool-generated scaffolding so archives stay lint-clean.

Rationale: markdownlint MD012 already exempts fenced code from the
"no multiple consecutive blank lines" rule by design, so this fix
aligns with the linter's own semantics. Fenced regions in PR review
text are exactly where audit fidelity must win over scaffolding
hygiene — that is where logs, templates, and preformatted payloads
live.

Smoke-tested against PR #357 itself: re-running archive-pr.sh 357
produces a 107-line diff of recovered content (mostly inside the
<details> fenced blocks from Codex / Copilot connector payloads that
the prior collapse was truncating). Archive-file churn reverted on
this branch — archive regeneration belongs in a separate PR, not
here.

Gates: `bash -n` clean + `shellcheck` clean.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
```

### `828fefa55e694f1c4e7cf7a946fbb2f1428f42fa` -- 2026-04-25T02:26:03Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/pr-preservation/archive-pr.sh`

**Message:**

```
drain(#357): fence-marker type-match + gh --jq consistency

Two Codex/Copilot threads on #357's archive-pr.sh:

1. **Codex P2 — fence detector conflates ``` and ~~~.** CommonMark
   requires the closing fence to use the SAME marker character as
   the opener (backticks close backticks; tildes close tildes). The
   previous `in_fence = not in_fence` on any fence-shaped line would
   prematurely close a backtick fence when a tilde line appeared
   inside it (and vice versa). Fix: track fence_marker on open,
   only flip back to False when the marker matches. Different-marker
   fence lines inside an open fence fall through to the verbatim
   branch so they're preserved as content.

2. **Copilot — `gh repo view -q` → `--jq` for consistency.** Other
   repo scripts (e.g. tools/hygiene/check-github-settings-drift.sh)
   use `--jq`. Switching to the long form matches the rest of the
   factory's gh invocations and avoids any `-q` ambiguity across
   gh versions.

Bash -n syntax check passes.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
```

### `737192b479cae3cf3e4aa7d6f1b1581c7059cfbb` -- 2026-04-25T02:41:26Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/pr-preservation/README.md`
- `tools/pr-preservation/archive-pr.sh`

**Message:**

```
drain(#357): REPO_ROOT git-tree guard + mktemp template + fence-lengt…

…h tracking + README cross-ref

Five Copilot + Codex threads:

1. **REPO_ROOT bogus-path risk.** `git rev-parse --show-toplevel || pwd`
   falls back to pwd outside a git checkout, but `gh repo view` can
   succeed via `gh repo set-default`, so the script could write
   docs/pr-discussions/ into a bogus REPO_ROOT directory. Hard-fail
   when not inside a git working tree.

2. **mktemp portability.** Plain `mktemp` with no template works on
   GNU coreutils (Linux) but fails on BSD mktemp (macOS). README
   advertises macOS support, so added `-t zeta-archive-pr.XXXXXX`
   template that works on both.

3. **Fence-length tracking (Codex P2 + Copilot).** Prior fix tracked
   marker TYPE (backtick vs tilde) but not fence LENGTH. Per
   CommonMark §4.5, the closing fence must be at least as long as
   the opener — a 4-backtick opener contains a 3-backtick line as
   content, not a closer. Now tracks both marker + length on open;
   closer must match BOTH.

4. **README cross-ref correction.** Canonical source for "agents,
   not bots" terminology is GOVERNANCE.md §3 ("Contributors are
   agents, not bots"). CLAUDE.md carries a session-bootstrap pointer
   at the same rule. Reworded to name GOVERNANCE as canonical with
   CLAUDE.md as the pointer.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
```
