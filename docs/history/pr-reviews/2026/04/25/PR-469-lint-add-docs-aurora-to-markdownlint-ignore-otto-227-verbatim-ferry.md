# PR #469 -- lint: add docs/aurora/** to markdownlint ignore (Otto-227 verbatim ferry)

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 469 |
| Title | lint: add docs/aurora/** to markdownlint ignore (Otto-227 verbatim ferry) |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-04-25T08:46:35Z |
| Merged at | 2026-04-25T08:59:39Z |
| Merge commit SHA | `9d45a9936f8669ce43c5a5556a6a30420ed1b528` |
| Branch | `lint/aurora-verbatim-ignore` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/469 |
| Changed files | 1 |
| Additions / deletions | +19 / -1 |

## Description

## Summary

The per-ferry Amara absorbs under \`docs/aurora/2026-*-amara-*.md\` each contain a "## Verbatim preservation (Amara's report)" section where Amara's deepresearch report is preserved verbatim per Otto-227 signal-in-signal-out discipline. Editing that content to satisfy markdownlint MD032/MD029/etc would violate the verbatim-courier contract.

Add \`docs/aurora/**\` to the markdownlint ignore list, mirroring the existing precedent for \`docs/amara-full-conversation/**\` and \`docs/pr-preservation/**\`.

## Why this unblocks 3+ PRs

Currently failing markdownlint on aurora ferry content:
- #235 (5th-ferry: Zeta/KSK/Aurora validation) — line 252 \`+ git history\` line-leading
- #219 (3rd-ferry: decision-proxy + technical review) — line 8 \`- [...]\` continuation
- (and future ferry absorbs would hit the same class)

Auto-merge SQUASH should fire on those PRs once they rebase to pick up this change.

## Test plan

- [x] Mirrors existing precedent (\`docs/amara-full-conversation/**\`, \`docs/pr-preservation/**\`) — same Otto-227 verbatim-preservation rationale
- [x] Comment in config explains the rationale (not just a path)
- [x] Lint covers any non-aurora content normally

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | true |
| Total threads | 3 |
| Resolved threads | 3 |
| Unresolved threads | 0 |
| Total review comments | 6 |
| Total fix commits (touching thread paths) | 2 |

## Review threads

### Thread 1 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59krYs`
- **Path**: `.markdownlint-cli2.jsonc`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-04-25T08:48:06Z on `.markdownlint-cli2.jsonc`:63 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Narrow markdownlint ignore to verbatim ferry files**

  The new `docs/aurora/**` ignore disables markdownlint for the entire Aurora directory, but the justification here only covers verbatim ferry absorbs (`docs/aurora/2026-*-amara-*.md`). This now exempts non-verbatim, author-controlled docs in the same tree (for example `docs/aurora/README.md` and `docs/aurora/collaborators.md`), so markdownlint regressions in those files will no longer be caught by CI. Restricting the ignore glob to the actual verbatim subset keeps the intended unblock without weakening lint coverage for the rest of Aurora docs.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-25T08:56:50Z on `.markdownlint-cli2.jsonc`:63 (association: MEMBER)

    <pre>
    Fixed in a014c6ad1fac5739c726b3c79d6062d882a2788f — narrowed to 'docs/aurora/2026-*-amara-*.md' matching the 10 Amara ferry-absorb files only. README.md, collaborators.md, initial-operations-integration plan, codex-4 peer-review archive, and transfer-report-from-amara (which has its own lint-compliance carve-out per its own header) stay linted. Comment updated to enumerate which docs are excluded so future readers see the boundary.
    </pre>

### Thread 2 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59krjb`
- **Path**: `.markdownlint-cli2.jsonc`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-25T08:48:52Z on `.markdownlint-cli2.jsonc`:63 (association: NONE)

  <pre>
  P1: `"docs/aurora/**"` disables markdownlint for *all* Aurora docs, including author-controlled files like `docs/aurora/README.md` (and `collaborators.md`). Since the rationale here is about verbatim-preserved ferry report sections, consider narrowing the ignore to just the courier absorb files (e.g., the `2026-*-amara-*.md` pattern mentioned in the comment, and any other truly verbatim-only absorbs) so normal docs in `docs/aurora/` still get lint coverage.
  ```suggestion
      // covers `docs/amara-full-conversation/**`. Keep the ignore
      // scoped to the absorb files so author-controlled Aurora docs
      // like `README.md` and `collaborators.md` still get linted.
      "docs/aurora/2026-*-amara-*.md"
  ```
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-25T08:56:51Z on `.markdownlint-cli2.jsonc`:63 (association: MEMBER)

    <pre>
    Fixed in a014c6ad1fac5739c726b3c79d6062d882a2788f — applied your suggested narrowing. Cross-reviewer convergence with the Codex P2 above; the broad pattern was over-cautious. Pattern now matches all 10 ferry-absorb files (verified via 'ls docs/aurora/' against the glob) without silencing lint on README.md / collaborators.md / etc.
    </pre>

### Thread 3 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59krjd`
- **Path**: `.markdownlint-cli2.jsonc`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-25T08:48:52Z on `.markdownlint-cli2.jsonc`:62 (association: NONE)

  <pre>
  P1: The comment block describes a narrow scope (Amara ferry absorbs under `docs/aurora/2026-*-amara-*.md`), but the actual ignore glob is `docs/aurora/**`, which also covers non-Amara and non-ferry docs in that directory. Either update the comment to match the broader ignore, or narrow the ignore to match the stated scope to avoid future confusion/truth-drift in the config rationale.
  ```suggestion
      // Aurora docs are ignored directory-wide because the primary
      // contents under `docs/aurora/` are verbatim courier-protocol
      // preservation artifacts, including Amara ferry absorbs such as
      // `docs/aurora/2026-*-amara-*.md`, per Otto-227 verbatim-
      // preservation discipline. Each absorb has a
      // "## Verbatim preservation (Amara's report)" section
      // containing Amara's report as paste, including AI-tool
      // citation anchors (`citeturn…file…`/`citeturn…search…`),
      // wrap points that hit CommonMark special characters (`+ ...`/
      // `- ...` line-leading), and ordered-list numbering preserved
      // across heading breaks. Reformatting the verbatim content to
      // satisfy MD032/MD029/etc. would violate Otto-227
      // signal-in-signal-out discipline — same policy that already
      // covers `docs/amara-full-conversation/**`. The ignore is kept
      // at the directory level to match the preservation surface and
      // avoid brittle file-specific entries.
  ```
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-25T08:56:53Z on `.markdownlint-cli2.jsonc`:62 (association: MEMBER)

    <pre>
    Fixed in a014c6ad1fac5739c726b3c79d6062d882a2788f — narrowed the ignore glob to match the comment's stated scope (rather than expanding the comment to match a broader ignore). The narrow path is the right call here per the surface-class discipline: ignore-list scope should match the actual preservation-contract surface (Otto-227 verbatim-only), nothing more.
    </pre>

## Fix commits (touching thread paths)

### `274c75ad166ff43920b8b4de84434c60f00409a9` -- 2026-04-25T08:46:15Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `.markdownlint-cli2.jsonc`

**Message:**

```
lint: add docs/aurora/** to markdownlint ignore (verbatim ferry prese…

…rvation)

The per-ferry Amara absorbs under `docs/aurora/2026-*-amara-*.md`
each contain a "## Verbatim preservation (Amara's report)" section
where Amara's deepresearch report is preserved verbatim per
Otto-227 signal-in-signal-out discipline. The verbatim content
includes:

- AI-tool citation anchors (`citeturn…file…` / `citeturn…search…`)
- Wrap points hitting CommonMark special characters (`+ ...` /
  `- ...` line-leading parsed as list markers)
- Ordered-list numbering preserved across heading breaks
- Other markdownlint-noisy patterns

Reformatting any of these would violate Otto-227 verbatim-
preservation. Add `docs/aurora/**` to the ignore list, mirroring
the precedent for `docs/amara-full-conversation/**` and
`docs/pr-preservation/**`.

This unblocks markdownlint CI on PRs touching aurora ferry
absorbs (#235 / #219 / pending future-ferry absorbs) without
violating the courier-protocol verbatim contract.
```

### `a014c6ad1fac5739c726b3c79d6062d882a2788f` -- 2026-04-25T08:56:28Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `.markdownlint-cli2.jsonc`

**Message:**

```
hygiene(#469): narrow aurora ignore to verbatim ferry pattern only

Codex P2 + Copilot P1 (×2) cross-reviewer convergence: the
broad 'docs/aurora/**' ignore covered author-controlled docs
(README.md, collaborators.md, initial-operations-integration
plan, codex-4 peer-review archive, transfer-report-from-amara
which has its own lint-compliance carve-out) — those should
stay linted.

Narrow to 'docs/aurora/2026-*-amara-*.md' which matches the
10 Amara ferry-absorb files (5th/6th/7th/8th/9th/10th-ferry
+ deep-research + memory-drift + operational-gap + zset-
semantics) that have the verbatim-preservation Otto-227
contract. Other docs in docs/aurora/ keep lint coverage.

The earlier broad pattern was a YAGNI vs over-cautious
trade-off that landed on the wrong side; reviewers caught
it. Same shape as the discriminator-falsification class:
the ignore-list scope must match the actual preservation-
contract surface.
```
