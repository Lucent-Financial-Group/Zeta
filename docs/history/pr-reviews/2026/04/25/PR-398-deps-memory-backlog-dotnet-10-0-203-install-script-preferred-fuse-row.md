# PR #398 -- deps+memory+backlog: dotnet 10.0.203 + install-script-preferred + FUSE row

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 398 |
| Title | deps+memory+backlog: dotnet 10.0.203 + install-script-preferred + FUSE row |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-04-25T00:17:53Z |
| Merged at | 2026-04-25T01:23:50Z |
| Merge commit SHA | `c7e396b3ad8827f7bd3d96cc16e33dc9719848ec` |
| Branch | `deps/dotnet-10-0-203-bump` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/398 |
| Changed files | 7 |
| Additions / deletions | +449 / -8 |

## Description

## Summary

Three landings in one PR (all related to maintainer 2026-04-24 directives):

1. **deps**: bump dotnet 10.0.202 → 10.0.203 (security update; ASP.NET Core Data Protection fix released 2026-04-21).
2. **memory**: durable note that `tools/setup/install.sh` is the preferred update method (after editing `.mise.toml`/`global.json` pins). Anti-patterns captured.
3. **backlog**: P3/way-back-backlog row for user-mode filesystem driver interface (FUSE/WinFsp/macFUSE/FSKit) — composes with native F# git impl + microkernel direction.

## Verification

End-to-end on Apple Silicon Mac (2026-04-24):
- Edited `.mise.toml:24` (10.0.202 → 10.0.203)
- Edited `global.json:3` (10.0.202 → 10.0.203)
- Ran `tools/setup/install.sh` → exit clean
- `mise exec -- dotnet --version` → `10.0.203`
- `DOTNET_gcServer=0 dotnet build Zeta.sln -c Release` → `0 Warning(s) 0 Error(s)` (Otto-248 GC workaround applies)

## Test plan

- [x] Build gate green locally with new version
- [x] Install script ran end-to-end without intervention
- [x] Memory file durabilizes the preferred-update-method discipline
- [x] FUSE BACKLOG row captured per Otto-275 log-don't-implement

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | true |
| Total threads | 11 |
| Resolved threads | 11 |
| Unresolved threads | 0 |
| Total review comments | 22 |
| Total fix commits (touching thread paths) | 4 |

## Review threads

### Thread 1 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59iZhS`
- **Path**: `memory/feedback_install_script_is_preferred_update_method_2026_04_24.md`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-25T00:20:42Z on `memory/feedback_install_script_is_preferred_update_method_2026_04_24.md`:60 (association: NONE)

  <pre>
  This references `docs/AGENTS.md`, but in the repo the file is `AGENTS.md` at the root (no `docs/` prefix). Update the link/path so readers can actually navigate to the referenced document.
  ```suggestion
  `AGENTS.md`) breaks.
  ```
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-25T00:49:12Z on `memory/feedback_install_script_is_preferred_update_method_2026_04_24.md`:60 (association: MEMBER)

    <pre>
    Fixed. `AGENTS.md` is at repo root (verified by `ls`); the `docs/` prefix was wrong. Updated to `AGENTS.md`. Resolved.
    </pre>

### Thread 2 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59iZh0`
- **Path**: `memory/feedback_install_script_is_preferred_update_method_2026_04_24.md`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-25T00:20:42Z on `memory/feedback_install_script_is_preferred_update_method_2026_04_24.md`:60 (association: NONE)

  <pre>
  This memory establishes that `brew install/upgrade dotnet` is an anti-pattern, but `docs/INSTALLED.md` currently documents installing/upgrading .NET via Homebrew and still mentions 10.0.202. Please reconcile that doc with this directive (either update it to the mise + install.sh path, or clearly mark it as historical/personal-machine info) to avoid conflicting guidance in the repo docs.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-25T00:49:23Z on `memory/feedback_install_script_is_preferred_update_method_2026_04_24.md`:60 (association: MEMBER)

    <pre>
    Fixed. `docs/INSTALLED.md` reconciled in the same PR:

    1. .NET SDK row updated to 10.0.203 with install path "mise-managed via `.mise.toml` + `global.json`; installed by `tools/setup/install.sh` (the canonical update path)" and a note that older brew / system installs are personal-machine residue not used by the build.
    2. The "recreate from scratch" snippet drops `brew install --cask dotnet`, reorders to put `tools/setup/install.sh` as the canonical path, and prefixes `dotnet restore` / `dotnet build` with `mise exec --` so the resolved SDK matches the pin.

    No more conflicting guidance — `INSTALLED.md` and this memory file now point at the same path. Resolved.
    </pre>

### Thread 3 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59iZiI`
- **Path**: `docs/BACKLOG.md`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-25T00:20:42Z on `docs/BACKLOG.md`:5606 (association: NONE)

  <pre>
  In this new BACKLOG item you label it as “Priority P3 / way-back-backlog”, but it’s being added under the “## P2 — research-grade” section. Please either move the entry into the appropriate priority section or update the priority label so the backlog structure stays consistent.
  ```suggestion
    directive (verbatim, research-grade):
  ```
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-25T00:49:33Z on `docs/BACKLOG.md`:5606 (association: MEMBER)

    <pre>
    Fixed by moving the row, not relabelling it. The maintainer's verbatim quote is "back back log file system driven interface" — that's a P3 signal, not P2. So the P2-section placement was the bug. The whole row now lives under `## P3 — noted, deferred` with the original "Priority P3 / way-back-backlog per maintainer" label intact. Resolved.
    </pre>

### Thread 4 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59iZja`
- **Path**: `docs/BACKLOG.md`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-04-25T00:20:44Z on `docs/BACKLOG.md`:5684 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Align backlog priority text with its section**

  This new entry is placed under `## P2 — research-grade` but explicitly says `Priority P3 / way-back-backlog`, which creates an internal priority contradiction in the canonical backlog file. In practice, section placement is used to interpret urgency, so this mismatch can misroute planning/triage; either move the row to a P3 section or change the stated priority to match P2.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-25T00:49:42Z on `docs/BACKLOG.md`:5684 (association: MEMBER)

    <pre>
    Fixed (same fix as the Copilot thread one bullet up). Row moved to `## P3 — noted, deferred` rather than relabelling, because the maintainer's verbatim quote ("back back log file system driven interface") is P3 intent — placement was the bug, not the priority. Resolved.
    </pre>

### Thread 5 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59iZjb`
- **Path**: `memory/feedback_install_script_is_preferred_update_method_2026_04_24.md`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-04-25T00:20:45Z on `memory/feedback_install_script_is_preferred_update_method_2026_04_24.md`:25 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Require updating both .mise.toml and global.json**

  The new “preferred update path” says to edit `.mise.toml` “(or `global.json`)`, but this repo’s .NET pinning contract requires those two files to stay in sync. If a contributor follows this wording and updates only one file, `install.sh` and SDK resolution can diverge, producing pin drift or build failures; the instruction should explicitly require updating both files for .NET SDK version bumps.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-25T00:49:52Z on `memory/feedback_install_script_is_preferred_update_method_2026_04_24.md`:25 (association: MEMBER)

    <pre>
    Fixed. Step 1 of the "Preferred update path" now requires editing **both** `.mise.toml` **and** `global.json` for any .NET SDK bump (with explicit rationale: `.mise.toml` drives the install path; `global.json` drives runtime SDK resolution; editing one without the other produces pin drift). Clarified that for non-.NET tools the contract is `.mise.toml` alone. Resolved.
    </pre>

### Thread 6 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59ik3v`
- **Path**: `docs/INSTALLED.md`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-25T00:53:11Z on `docs/INSTALLED.md`:87 (association: NONE)

  <pre>
  `tools/setup/install.sh` is documented (and used in CI) as a directly-invoked executable (`./tools/setup/install.sh`), but this snippet runs it via `bash ...`. Prefer invoking it directly for parity with CI and to catch any missing executable bit / shebang issues early (install.sh itself documents `tools/setup/install.sh` as the usage form).
  ```suggestion
  ./tools/setup/install.sh            # reads .mise.toml + global.json pins
  ```
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-25T01:03:06Z on `docs/INSTALLED.md`:87 (association: MEMBER)

    <pre>
    Fixed in 0feb137 — switched to direct `./tools/setup/install.sh` invocation for CI parity, added explicit shellenv source step (Otto-248 DOTNET_gcServer=0 active in shell), and switched build-gate command to `mise exec -- dotnet build` to avoid SDK pin drift through legacy/brew dotnet. Resolving as FIX per Otto-236.
    </pre>

### Thread 7 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59ik3x`
- **Path**: `docs/INSTALLED.md`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-25T00:53:12Z on `docs/INSTALLED.md`:99 (association: NONE)

  <pre>
  This "fresh box" workflow runs `mise exec -- dotnet restore/build` immediately after install, but on Apple Silicon the repo relies on `DOTNET_gcServer=0` being exported by the managed `~/.config/zeta/shellenv.sh` (Otto-248 workaround) to avoid .NET 10 Server GC crashes. Consider adding an explicit step to source the generated shellenv (or prefix the commands with `DOTNET_gcServer=0`) so the instructions are reliable on the affected platform.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-25T01:03:08Z on `docs/INSTALLED.md`:99 (association: MEMBER)

    <pre>
    Fixed in 0feb137 — switched to direct `./tools/setup/install.sh` invocation for CI parity, added explicit shellenv source step (Otto-248 DOTNET_gcServer=0 active in shell), and switched build-gate command to `mise exec -- dotnet build` to avoid SDK pin drift through legacy/brew dotnet. Resolving as FIX per Otto-236.
    </pre>

### Thread 8 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59imp1`
- **Path**: `memory/feedback_install_script_is_preferred_update_method_2026_04_24.md`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-04-25T00:58:04Z on `memory/feedback_install_script_is_preferred_update_method_2026_04_24.md`:39 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Run build gate through mise to avoid SDK pin drift**

  Step 3 verifies the SDK with `mise exec -- dotnet --version`, but step 4 switches back to plain `dotnet build`, which can use a different executable when a legacy system/Homebrew dotnet is earlier on `PATH`. In that common setup, the verification passes against 10.0.203 while the build runs with another SDK (or fails to find 10.0.203), undermining the three-way parity this memory is trying to enforce.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-25T01:03:10Z on `memory/feedback_install_script_is_preferred_update_method_2026_04_24.md`:39 (association: MEMBER)

    <pre>
    Fixed in 0feb137 — switched to direct `./tools/setup/install.sh` invocation for CI parity, added explicit shellenv source step (Otto-248 DOTNET_gcServer=0 active in shell), and switched build-gate command to `mise exec -- dotnet build` to avoid SDK pin drift through legacy/brew dotnet. Resolving as FIX per Otto-236.
    </pre>

### Thread 9 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59ios0`
- **Path**: `memory/feedback_install_script_is_preferred_update_method_2026_04_24.md`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-04-25T01:05:07Z on `memory/feedback_install_script_is_preferred_update_method_2026_04_24.md`:90 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Keep verified build command on mise-managed dotnet**

  This memory now mandates running the build gate through `mise exec` to avoid SDK pin drift, but the verified example still uses plain `dotnet build`. In environments where a Homebrew/system `dotnet` appears earlier on `PATH`, copying this command can bypass the pinned SDK and undermine the three-way parity guarantee this document is meant to enforce, so the verified command should be updated to the same `mise exec -- dotnet build` form used in the rule section.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-25T01:20:45Z on `memory/feedback_install_script_is_preferred_update_method_2026_04_24.md`:90 (association: MEMBER)

    <pre>
    Fixed in f7ca76221773dbd168b6aa3a5a4c546d356984c7 — verified-build example now reads `DOTNET_gcServer=0 mise exec -- dotnet build Zeta.sln -c Release` (matches the rule in step 4) plus an explanatory one-liner about why each verification routes through `mise exec --`. The actual 2026-04-24 bump did run via mise after the install script set PATH; the doc now reflects that reality.
    </pre>

### Thread 10 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59ipax`
- **Path**: `memory/feedback_install_script_is_preferred_update_method_2026_04_24.md`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-25T01:07:27Z on `memory/feedback_install_script_is_preferred_update_method_2026_04_24.md`:18 (association: NONE)

  <pre>
  The “Context” section describes verifying `dotnet --version` and running `dotnet build ...` directly, but the rule just below emphasizes using `mise exec -- dotnet ...` to avoid PATH/pin drift. Consider updating these example commands (or explicitly stating they were run via `mise exec --`) so the narrative doesn’t undermine the guidance.
  ```suggestion
  (c) verified `mise exec -- dotnet --version` returns
  10.0.203, (d) verified `DOTNET_gcServer=0 mise exec --
  dotnet build Zeta.sln -c Release` is green with the
  Otto-248 workaround.
  ```
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-25T01:20:46Z on `memory/feedback_install_script_is_preferred_update_method_2026_04_24.md`:18 (association: MEMBER)

    <pre>
    Fixed in f7ca76221773dbd168b6aa3a5a4c546d356984c7 — Context section's verification commands now use `mise exec -- dotnet --version` and `DOTNET_gcServer=0 mise exec -- dotnet build ...` so a reader copy-pasting them can't silently bypass the mise-managed pin via a Homebrew/legacy `dotnet` ahead on PATH.
    </pre>

### Thread 11 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59ipa4`
- **Path**: `memory/feedback_install_script_is_preferred_update_method_2026_04_24.md`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-25T01:07:28Z on `memory/feedback_install_script_is_preferred_update_method_2026_04_24.md`:90 (association: NONE)

  <pre>
  In “Verified on 2026-04-24”, the Build gate command uses `DOTNET_gcServer=0 dotnet build ...` (no `mise exec --`), which conflicts with step 4’s rationale about avoiding a legacy/Homebrew `dotnet` earlier on PATH. Recommend changing this verification command to use `DOTNET_gcServer=0 mise exec -- dotnet build ...` (or clarify why plain `dotnet` is guaranteed to be the pinned SDK here).
  ```suggestion
  - **Build gate**: `DOTNET_gcServer=0 mise exec --
    dotnet build Zeta.sln -c Release` →
    `0 Warning(s) 0 Error(s)`.
  ```
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-25T01:20:48Z on `memory/feedback_install_script_is_preferred_update_method_2026_04_24.md`:90 (association: MEMBER)

    <pre>
    Fixed in f7ca76221773dbd168b6aa3a5a4c546d356984c7 — Build gate verification command updated to `DOTNET_gcServer=0 mise exec -- dotnet build Zeta.sln -c Release` (matches the rule's rationale in step 4 about avoiding PATH shadowing). Inline note added explaining the wrap so the doc's example self-documents the discipline.
    </pre>

## Fix commits (touching thread paths)

### `fb1cea7934d9eb289584f806c09a1e7f17aafbaf` -- 2026-04-25T00:17:39Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `docs/BACKLOG.md`
- `memory/feedback_install_script_is_preferred_update_method_2026_04_24.md`

**Message:**

```
deps+memory+backlog: dotnet 10.0.203 + install-script-preferred + FUS…

…E row

(1) **dotnet 10.0.202 → 10.0.203** (security update; ASP.NET Core
    Data Protection fix, released 2026-04-21). Bumped both
    `.mise.toml:24` and `global.json:3`. Verified end-to-end:
    edited pins → ran `tools/setup/install.sh` → mise installed
    10.0.203 → `dotnet --version` confirms → `DOTNET_gcServer=0
    dotnet build Zeta.sln -c Release` is 0 warnings 0 errors.

(2) **Memory + MEMORY.md pointer** — preferred update method is
    edit `.mise.toml` + run `tools/setup/install.sh`. Maintainer
    2026-04-24 directive: "you should note somewehre durable
    that that's the prefered method of update". Anti-patterns
    captured: brew upgrade / direct `mise install` without
    .mise.toml edit / Microsoft's dotnet-install.sh — all break
    GOVERNANCE §24 three-way-parity.

(3) **BACKLOG row** — user-mode filesystem driver interface
    (FUSE Linux / WinFsp Windows / macFUSE+FSKit macOS).
    Maintainer 2026-04-24: "back back log file system driven
    interface ... will help when we go to microkernal all
    dotnet f#". P3 / way-back-backlog, research first. Composes
    with native F# git impl, closure-table hardening, cross-DSL
    composability, blockchain ingest, Ouroboros bootstrap.

Otto-247 version-currency-search-first triggered the bump.
Otto-275 log-don't-implement applies to FUSE row (research
gate first).
```

### `5857dfb4e25b1cd5be23868cec1c33a5f2c7ba97` -- 2026-04-25T00:48:57Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `docs/BACKLOG.md`
- `docs/INSTALLED.md`
- `memory/feedback_install_script_is_preferred_update_method_2026_04_24.md`

**Message:**

```
drain #398: address 5 review threads (Copilot + Codex)

- AGENTS.md path: dropped wrong `docs/` prefix in memory file.
- INSTALLED.md reconciled with install-script-preferred directive:
  .NET SDK row updated to 10.0.203, names mise + tools/setup/install.sh
  as canonical install path; "recreate from scratch" snippet drops
  brew install --cask dotnet and prefixes commands with `mise exec --`.
- Memory file now requires editing BOTH .mise.toml AND global.json
  for any .NET SDK bump (rationale in step 1: pin drift between
  install path and runtime SDK resolution).
- FUSE BACKLOG row moved from P2 — research-grade to P3 — noted,
  deferred to match the maintainer's verbatim "back back log" intent
  (placement was the bug, not the priority label).
- Drain log at docs/pr-preservation/398-drain-log.md.
```

### `0feb137e7ae21f4aeabb94063c4db07a5d47a38e` -- 2026-04-25T01:02:49Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `docs/INSTALLED.md`
- `memory/feedback_install_script_is_preferred_update_method_2026_04_24.md`

**Message:**

```
fix(#398) drain r2: install.sh CI-parity form + shellenv source + mis…

…e exec on build gate
```

### `f7ca76221773dbd168b6aa3a5a4c546d356984c7` -- 2026-04-25T01:20:26Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `memory/feedback_install_script_is_preferred_update_method_2026_04_24.md`

**Message:**

```
drain: address Codex+Copilot review on #398 — route verified examples…

… through mise exec

The memory file's "rule" section mandates `mise exec --` for the build
gate to avoid PATH-shadowing by legacy/Homebrew dotnet, but the
"Context" + "Verified on 2026-04-24" examples used plain `dotnet build`
verbatim. That mismatch let a reader copy-paste the verification
commands and silently bypass the mise-managed pin.

Update both example commands to route through `mise exec --` (matching
step 4 of the rule), and add an explicit one-liner explaining why each
verification is wrapped — so future-readers see the rule's rationale
applied to its own examples.

No behaviour change in the bump itself (the actual 2026-04-24 build
gate ran with `mise exec --` after the install script ensured the
mise dotnet shim won the PATH race; the doc now reflects that
reality).

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
```
