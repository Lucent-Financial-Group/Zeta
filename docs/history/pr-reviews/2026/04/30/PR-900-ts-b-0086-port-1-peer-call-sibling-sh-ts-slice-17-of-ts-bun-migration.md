# PR #900 -- ts(B-0086): port 1 peer-call sibling (.sh→.ts) — slice 17 of TS/Bun migration

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 900 |
| Title | ts(B-0086): port 1 peer-call sibling (.sh→.ts) — slice 17 of TS/Bun migration |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-04-30T06:46:46Z |
| Merged at | 2026-04-30T06:52:30Z |
| Merge commit SHA | `10c34180769531459c8026815b1a2aeebbd3f174` |
| Branch | `lane-b/ts-bun-slice-17-peer-call-codex-2026-04-30` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/900 |
| Changed files | 1 |
| Additions / deletions | +357 / -0 |

## Description

## Summary

Slice 17 — third peer-call sibling (after grok #896 and gemini #898):

- `tools/peer-call/codex.{sh→ts}` (Codex peer-caller via codex CLI; default `codex exec -s read-only --skip-git-repo-check`, `--review` routes through `codex review`)

Bakes in **all the round-2 + round-3 fixes** from the gemini.ts review cycle:

- Exit codes 0/1/2 uniform per `tools/peer-call/README.md` (NOT 64).
- `commandAvailable` via `/bin/sh -c 'command -v <cmd>'` (PATH check, not `--version`).
- `/bin/bash -c` (not `/bin/sh -c`) for context-cmd to preserve bash-only feature support.
- `ReadHeadResult { ok, content, error }` so file-read failures surface to stderr instead of silently dropping context.
- `classifySpawnFailure` 4-case helper (status / ENOENT → 127 / signal / other) reused from #887 + #898.
- Concat `stdout + stderr` from `runContextCmd` to preserve shell parse errors that fall outside the inner `( ) 2>&1` redirect.

## Test plan

- [x] `bun --bun tsc --noEmit` clean (lint-tsc-tools gate from #890 will validate this in CI).
- [x] `eslint` clean.
- [x] Argument-validation byte-equivalent: `--model` without value → exit 1; no prompt → exit 1; `--bogus` → exit 1.
- [ ] CI gate (gate.yml + CodeQL + lint-tsc-tools).

## Note on CodeQL alert

Same `js/indirect-command-line-injection` will fire on `runContextCmd`. Per [B-0107](docs/backlog/P3/B-0107-codeql-peer-call-dismiss-pattern-2026-04-30.md) the per-PR dismissal pattern applies until the structural fix lands.

## Trajectory

After-merge: 38 ports total, 5 Bucket B files remain (2 budget + 1 git/batch-resolve + 1 pr-preservation/archive + 0 peer-call — cluster complete!).

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 3 |
| Resolved threads | 3 |
| Unresolved threads | 0 |
| Total review comments | 4 |
| Total fix commits (touching thread paths) | 1 |

## Review threads

### Thread 1 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5-pzql`
- **Path**: `tools/peer-call/codex.ts`
- **Replies**: 0

**Initial comment:**

- **`github-advanced-security[bot]` (bot)** at 2026-04-30T06:48:38Z on `tools/peer-call/codex.ts`:196 (association: NONE)

  <pre>
  ## CodeQL / Indirect uncontrolled command line

  This command depends on an unsanitized [command-line argument](1).

  [Show more details](https://github.com/Lucent-Financial-Group/Zeta/security/code-scanning/40)
  </pre>

### Thread 2 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5-p0or`
- **Path**: `tools/peer-call/codex.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-30T06:49:52Z on `tools/peer-call/codex.ts`:9 (association: NONE)

  <pre>
  The header comment introduces persona/human names (e.g., “Otto”, “Amara”) in a current-state code surface. Repo convention is to use role-refs in code/TS (e.g., “the harness-side caller”, “the human maintainer”, “the critique peer”) and reserve named attribution for the enumerated history surfaces. Please rewrite this header to use role-refs while preserving the intended role-distribution semantics.

  </pre>

### Thread 3 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5-p0pF`
- **Path**: `tools/peer-call/codex.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-30T06:49:53Z on `tools/peer-call/codex.ts`:242 (association: NONE)

  <pre>
  The PREAMBLE string includes named attribution (“Otto”, “Amara”, “Aaron”, etc.). This conflicts with the repo’s “role-refs only on current-state surfaces (including code)” convention; names should be replaced with role-refs unless the file is a listed history surface. Please refactor the preamble wording accordingly.

  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-30T06:52:26Z on `tools/peer-call/codex.ts`:242 (association: MEMBER)

    <pre>
    Same push-back as on #898: the PREAMBLE is **prompt content sent verbatim to the LLM peer** — not pure code-surface. The bash original (`tools/peer-call/codex.sh`) uses the same persona names; the byte-equivalence invariant of this migration says the TS port produces the same prompt the bash version did.

    The header-comment names are file/protocol references (e.g., "Sibling to tools/peer-call/grok.ts (slice 15) and gemini.ts (slice 16)" → file-path refs; "four-ferry consensus" → factory protocol name). These either are role-refs already (script purpose) or are file-paths. The remaining ones (e.g., "Codex" itself in the file purpose) name the peer the script invokes — that's the script's identity.

    Tracking via this thread; the broader question of retiring persona names from the 3 PREAMBLES (grok / gemini / codex) onto both bash AND TS sides together belongs in a follow-up that decides whether to lose the protocol's recognizability for cross-LLM identity vs role-only.

    </pre>

## Fix commits (touching thread paths)

### `a5c6fd9c274537ada7bbeb137dc814b67928b556` -- 2026-04-30T06:46:16Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/peer-call/codex.ts`

**Message:**

```
ts(B-0086): port 1 peer-call sibling (.sh→.ts) — slice 17 of TS/Bun m…

…igration

* ts(slice-17, wip 1/N): port peer-call/codex (.sh→.ts)

Third sibling of the peer-call cluster (after grok #896 and gemini
#898). Wraps 'codex exec -s read-only --skip-git-repo-check' (default)
or 'codex review' (with --review). --model is ignored in --review
mode with a warning to stderr.

Bakes in all the round-2 + round-3 fixes from the gemini.ts review
cycle:
- exit codes 0/1/2 uniform per tools/peer-call/README.md (NOT 64)
- commandAvailable via /bin/sh -c 'command -v <cmd>' (PATH check, not
  --version)
- /bin/bash -c (not /bin/sh -c) for context-cmd to preserve bash-only
  feature support (Codex P2)
- ReadHeadResult { ok, content, error } so file-read failures surface
  to stderr instead of silently dropping context
- classifySpawnFailure 4-case helper (status / ENOENT → 127 / signal /
  other) reused from PR #887 + slice-16 #898
- Concat stdout + stderr from runContextCmd to preserve shell parse
  errors that fall outside the inner ( ) 2>&1 redirect

Lint-clean: bun --bun tsc --noEmit + eslint strictTypeChecked +
sonarjs all pass. Argument-validation byte-equivalent.

Note: same CodeQL js/indirect-command-line-injection alert will fire
here as on grok.ts and gemini.ts. Per B-0107 (filed in PR #897) the
per-PR dismissal pattern applies until B-0107 implements the
structural fix.
```
