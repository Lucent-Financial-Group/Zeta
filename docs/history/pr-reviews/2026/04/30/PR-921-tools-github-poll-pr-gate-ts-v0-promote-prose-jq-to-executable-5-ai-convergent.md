# PR #921 -- tools(github): poll-pr-gate.ts v0 — promote prose-jq to executable (5-AI convergent)

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 921 |
| Title | tools(github): poll-pr-gate.ts v0 — promote prose-jq to executable (5-AI convergent) |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-04-30T15:15:49Z |
| Merged at | 2026-04-30T15:43:01Z |
| Merge commit SHA | `0130b7e13eb169e51d4447eb4d3ef3de3396d080` |
| Branch | `ops/poll-pr-gate-executable-script-v0-2026-04-30` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/921 |
| Changed files | 5 |
| Additions / deletions | +478 / -0 |

## Description

## Summary

Closes part 1 of task #355 — the highest-leverage hardening item this session. **5-AI peer-reviewer convergence on 2026-04-30** named the recommendation: promote the inline jq snippets in `memory/feedback_amara_poll_gate_not_ending_holding_is_not_status_2026_04_30.md` into a tested executable. Full attribution at `docs/research/2026-04-30-multi-ai-feedback-packets-this-session.md`.

Carved blade from that packet:

> *"If the loop uses it every tick, it deserves tests."*

## What's in v0

- `tools/github/poll-pr-gate.ts` — Bun+TS skeleton with happy-path query
- `tools/github/fixtures/clean-armed-auto-merge.json` — clean gate + auto-merge-armed shape
- `tools/github/fixtures/blocked-by-threads.json` — clean-CI + 3 unresolved threads shape
- `tools/github/fixtures/status-context-error.json` — StatusContext ERROR + EXPECTED states
- `tools/github/fixtures/behind-needs-rebase.json` — BEHIND mergeStateStatus → rebase action

## Output shape

```json
{
  "number": 917,
  "state": "OPEN" | "MERGED" | "CLOSED",
  "gate": "CLEAN" | "BLOCKED" | "DIRTY" | "UNSTABLE" | "UNKNOWN",
  "checks": { "ok": N, "inProgress": N, "pending": N, "failed": N },
  "unresolvedThreads": N,
  "autoMerge": "armed" | "none",
  "mergeCommit": "<sha>" | null,
  "nextAction": "wait-ci" | "fix-failed-checks" | "resolve-threads" | "rebase" | "verify-merge" | "none"
}
```

## Required-check semantics

- **Merge-satisfying**: SUCCESS, NEUTRAL, SKIPPED
- **Blocking**: FAILURE, CANCELLED, TIMED_OUT, STARTUP_FAILURE, ACTION_REQUIRED, STALE, ERROR
- **Pending**: QUEUED, PENDING, IN_PROGRESS, EXPECTED, REQUESTED, WAITING

## Exit codes

- 0 — query succeeded, JSON emitted
- 1 — invocation / argument / dependency error (bad args, gh missing, fixture missing, PR ≤ 0)
- 2 — gh CLI returned non-zero (auth, rate-limit, PR not found)
- 3 — gh CLI output couldn't be parsed (truncated, non-JSON)

## Verification

Tested live against multiple PRs and offline against 4 fixtures. All cases produce the expected report shape.

## Why this matters

Aaron's substrate-IS-product framing (2026-04-30) — executable substrate IS substrate-quality work. The factory's tooling-product deserves the same honest-substrate discipline as the substrate-product. The memory file should stop being the implementation and start pointing to it. Slice 22 of B-0086 TS+Bun migration trajectory.

## Follow-ups (queued under task #355)

- Matrix fixture coverage (mixed-rollup, missing-conclusion, pagination edges)
- Test runner integration with existing TS test harness
- Top-of-memory-file pointer addition (after this PR merges)
- Loop integration: replace inline jq calls in the autonomous-loop tick close

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | true |
| Total threads | 29 |
| Resolved threads | 23 |
| Unresolved threads | 6 |
| Total review comments | 29 |
| Total fix commits (touching thread paths) | 8 |

## Review threads

### Thread 1 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5-x-ZS`
- **Path**: `tools/github/poll-pr-gate.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-04-30T15:19:19Z on `tools/github/poll-pr-gate.ts`:209 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Paginate review thread queries before deriving blockers**

  The GraphQL call only fetches `reviewThreads(first:50)` and then directly counts unresolved nodes, so any PR with more than 50 threads will be truncated. In that case `buildReport` can undercount unresolved blockers and produce an incorrect `nextAction` (for example `none` instead of `resolve-threads`) even while unresolved threads still exist beyond page 1. This breaks the gate-polling diagnosis on discussion-heavy PRs.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 2 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5-x-ZX`
- **Path**: `tools/github/poll-pr-gate.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-04-30T15:19:19Z on `tools/github/poll-pr-gate.ts`:236 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Classify StatusContext EXPECTED/ERROR states correctly**

  The StatusContext normalization treats every non-`PENDING` state as `COMPLETED` and forwards the raw state as `conclusion`, but `classifyChecks` only treats `QUEUED`/`PENDING` as pending and does not treat `ERROR` as failed. That means valid StatusContext states like `EXPECTED` and `ERROR` are effectively dropped from pending/failed counts, which can yield `nextAction: "none"` on still-blocked PRs instead of continuing CI wait or blocker handling.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 3 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5-yDU_`
- **Path**: `tools/github/poll-pr-gate.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-30T15:23:42Z on `tools/github/poll-pr-gate.ts`:143 (association: NONE)

  <pre>
  P0: `mergeStateStatus: BEHIND` isn’t handled. In this repo’s PR-shape taxonomy, BEHIND means base advanced and the action is to rebase/merge main; currently it falls through to `UNKNOWN` and won’t drive `nextAction: rebase`.
  </pre>

### Thread 4 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5-yDVj`
- **Path**: `tools/github/poll-pr-gate.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-30T15:23:42Z on `tools/github/poll-pr-gate.ts`:328 (association: NONE)

  <pre>
  P1: Argument/usage errors exit with code 1 here (unknown arg, missing PR/fixture), but the repo’s TS/Bun scripting convention is to use exit code 64 for argument/input errors (EX_USAGE), with help on stdout and errors on stderr. Aligning to that convention makes this tool composable with other automation.
  </pre>

### Thread 5 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5-yDWF`
- **Path**: `tools/github/poll-pr-gate.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-30T15:23:42Z on `tools/github/poll-pr-gate.ts`:255 (association: NONE)

  <pre>
  P1: `spawnSync` launch failures aren’t distinguished from a non-zero `gh` exit. When `gh` is missing/unlaunchable, `status` can be null and the real error is in `prResult.error`; currently the message will often be empty and you still exit 2 as if `gh` ran and failed.
  </pre>

### Thread 6 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5-yDWW`
- **Path**: `tools/github/poll-pr-gate.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-30T15:23:43Z on `tools/github/poll-pr-gate.ts`:356 (association: NONE)

  <pre>
  P2: This script calls `main()` unconditionally. Most `tools/*.ts` scripts use `export function main(argv)` plus `if (import.meta.main) process.exit(main(...))`, which makes the module importable for fixture-based tests and reuse without side effects.
  </pre>

### Thread 7 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5-yDWp`
- **Path**: `tools/github/poll-pr-gate.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-30T15:23:43Z on `tools/github/poll-pr-gate.ts`:236 (association: NONE)

  <pre>
  P0: StatusContext items can have states like `EXPECTED` (pending) and `ERROR`/`FAILURE` (failing). This normalization only treats `PENDING` specially; other states become `COMPLETED` with `conclusion: state`, and later classification doesn’t count `EXPECTED` as pending or `ERROR` as failed — which can under-report pending/failed checks.
  </pre>

### Thread 8 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5-yDW8`
- **Path**: `tools/github/poll-pr-gate.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-30T15:23:43Z on `tools/github/poll-pr-gate.ts`:169 (association: NONE)

  <pre>
  P0: `nextAction` returns `resolve-threads` when `checks.failed &gt; 0`, which is a different blocker than unresolved review threads. Also, `mergeStateStatus`/`gate` values like `BLOCKED`/`UNSTABLE`/`UNKNOWN` aren’t considered, so `nextAction` can return `none` even when the PR is not merge-ready.
  ```suggestion
    if (unresolvedThreads &gt; 0) return "BLOCKED";
    if (mergeStateStatus === "CLEAN") return "CLEAN";
    return "UNKNOWN";
  }

  function nextAction(report: Omit&lt;GateReport, "nextAction"&gt;): NextAction {
    if (report.state === "MERGED") return "verify-merge";
    if (report.gate === "DIRTY") return "rebase";
    if (report.checks.failed &gt; 0 || report.gate === "UNSTABLE") {
      return "fix-failed-checks";
    }
    if (report.unresolvedThreads &gt; 0) return "resolve-threads";
    if (report.checks.inProgress &gt; 0 || report.checks.pending &gt; 0) {
      return "wait-ci";
    }
    if (report.gate === "BLOCKED" || report.gate === "UNKNOWN") {
      return "wait-ci";
    }
  ```
  </pre>

### Thread 9 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5-yDXX`
- **Path**: `tools/github/poll-pr-gate.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-30T15:23:43Z on `tools/github/poll-pr-gate.ts`:318 (association: NONE)

  <pre>
  P1: `--fixture/--owner/--repo` don’t validate that a following value exists. If the flag is last, `argv[++i]` becomes `undefined` and later code will read a non-existent fixture or query `undefined/undefined`, producing confusing failures; detect missing values and exit with a usage code.
  </pre>

### Thread 10 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5-yDXu`
- **Path**: `tools/github/poll-pr-gate.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-30T15:23:44Z on `tools/github/poll-pr-gate.ts`:257 (association: NONE)

  <pre>
  P1: `JSON.parse(prResult.stdout)` can throw (non-JSON output, truncated output, auth error HTML), which will currently crash the process without a clear exit code/message. Wrap JSON parsing and convert parse failures into a controlled error path.
  </pre>

### Thread 11 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5-yDYS`
- **Path**: `tools/github/poll-pr-gate.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-30T15:23:44Z on `tools/github/poll-pr-gate.ts`:19 (association: NONE)

  <pre>
  P1: The usage example references `tools/github/fixtures/blocked-with-threads.json`, but the fixture added in this PR is `blocked-by-threads.json`. This makes the copy/paste invocation fail; update the usage line to the actual filename.
  ```suggestion
  //   bun tools/github/poll-pr-gate.ts --fixture tools/github/fixtures/blocked-by-threads.json
  ```
  </pre>

### Thread 12 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5-yG5M`
- **Path**: `tools/github/poll-pr-gate.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-04-30T15:26:42Z on `tools/github/poll-pr-gate.ts`:112 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Treat REQUESTED/WAITING checks as pending**

  GitHub check runs can report non-terminal states beyond `QUEUED`/`PENDING` (notably `REQUESTED` and `WAITING`), but `classifyChecks` only counts the current two pending literals. When a required check is in one of these omitted states, the report undercounts pending work and `nextAction` can incorrectly become `none` even though the PR is still blocked by CI progression.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 13 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5-yG5X`
- **Path**: `tools/github/poll-pr-gate.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-04-30T15:26:42Z on `tools/github/poll-pr-gate.ts`:155 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Handle BEHIND merge state before falling back to UNKNOWN**

  The gate classifier handles `DIRTY`, `UNSTABLE`, `BLOCKED`, and `CLEAN`, then falls through to `UNKNOWN` for all other merge states. A common merge-blocking state like `BEHIND` will therefore produce `gate: "UNKNOWN"` and then `nextAction: "none"` when checks/threads are otherwise clear, which prevents the poller from signaling the needed branch update/rebase step.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 14 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5-yQfm`
- **Path**: `tools/github/poll-pr-gate.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-30T15:34:43Z on `tools/github/poll-pr-gate.ts`:206 (association: NONE)

  <pre>
  Calls to `spawnSync("gh", ...)` are missing the local eslint suppression used elsewhere in `tools/` (`sonarjs/no-os-command-from-path`). If that rule is enabled (it appears to be in other tool files), this will likely fail lint/CI. Add the standard `eslint-disable-next-line` with a brief rationale, or refactor to a shared process-runner that is already suppressed.
  ```suggestion
  function runGhOrExit(args: string[], context: string): string {
    // eslint-disable-next-line sonarjs/no-os-command-from-path -- tool script intentionally invokes the trusted gh CLI from PATH.
  ```
  </pre>

### Thread 15 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5-yQgn`
- **Path**: `tools/github/poll-pr-gate.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-30T15:34:44Z on `tools/github/poll-pr-gate.ts`:55 (association: NONE)

  <pre>
  PR description’s declared output shape for `nextAction` doesn’t include `fix-failed-checks`, but this implementation introduces it (and even the header comment lists it). Either update the PR description/spec to include this value, or rename/map it to one of the documented actions to avoid contract drift for consumers.
  </pre>

### Thread 16 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5-yQhO`
- **Path**: `tools/github/fixtures/clean-armed-auto-merge.json`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-30T15:34:44Z on `tools/github/fixtures/clean-armed-auto-merge.json`:4 (association: NONE)

  <pre>
  This fixture is named `clean-armed-auto-merge`, but `mergeStateStatus` is set to "BLOCKED". With the current `classifyGate` logic, that will classify the gate as BLOCKED, not CLEAN. Either adjust the fixture’s `mergeStateStatus` to "CLEAN" (if the intent is a clean gate) or rename the fixture so its filename matches the represented state.
  ```suggestion
    "mergeStateStatus": "CLEAN",
  ```
  </pre>

### Thread 17 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5-yQh0`
- **Path**: `tools/github/poll-pr-gate.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-30T15:34:44Z on `tools/github/poll-pr-gate.ts`:10 (association: NONE)

  <pre>
  Code comments include persona/AI names (e.g., “Amara”, “Deepseek”, “Alexia”, “Ani”, “Gemini”). This repo’s convention is to avoid name attribution in current-state surfaces (including code) and use role-refs instead; consider rewriting this header to remove names and keep only a role-based/neutral provenance note (see .github/copilot-instructions.md:305-346).
  </pre>

### Thread 18 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5-yQiO`
- **Path**: `tools/github/poll-pr-gate.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-30T15:34:44Z on `tools/github/poll-pr-gate.ts`:417 (association: NONE)

  <pre>
  This script doesn’t follow the repo’s common TS/Bun tool entrypoint pattern (export a `main(argv)` that returns an exit code, and guard execution with `if (import.meta.main) process.exit(main(process.argv.slice(2)))`). Right now `main()` is unexported, uses `process.argv` internally, and helpers call `process.exit`, which makes reuse/testing harder and diverges from patterns in other tools (e.g., tools/git/batch-resolve-pr-threads.ts:500-541).
  </pre>

### Thread 19 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5-yQi1`
- **Path**: `tools/github/poll-pr-gate.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-30T15:34:45Z on `tools/github/poll-pr-gate.ts`:324 (association: NONE)

  <pre>
  `loadFixture()` uses JSON.parse(readFileSync(...)) with no error handling. If the file is missing or invalid JSON, the script will throw an unhandled exception (stack trace) instead of producing a controlled error message/exit code. Consider wrapping fixture read/parse in try/catch and exiting with a deterministic code/message.
  ```suggestion
    try {
      const raw = JSON.parse(readFileSync(path, "utf8")) as PullRequestData;
      // Apply the same StatusContext-state normalization as fetchPR so fixture
      // mode and live mode classify identically (Codex P1).
      return {
        ...raw,
        statusCheckRollup: normalizeRollup(raw.statusCheckRollup ?? []),
      };
    } catch (error) {
      const message = error instanceof Error ? error.message : String(error);
      process.stderr.write(
        `failed to load fixture ${path}: ${message}\n`,
      );
      process.exit(1);
    }
  ```
  </pre>

### Thread 20 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5-yQjm`
- **Path**: `tools/github/poll-pr-gate.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-30T15:34:45Z on `tools/github/poll-pr-gate.ts`:356 (association: NONE)

  <pre>
  `parseArgs` treats any all-digit argument as a PR number, including "0". GitHub PR numbers are positive integers (&gt;0), and other tools explicitly reject 0 to avoid confusing downstream failures. Consider validating `out.number` and erroring out if it’s &lt;= 0.
  ```suggestion
        const parsedNumber = Number.parseInt(arg, 10);
        if (parsedNumber &lt;= 0) {
          process.stderr.write("PR number must be a positive integer\n");
          process.exit(1);
        }
        out.number = parsedNumber;
  ```
  </pre>

### Thread 21 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5-yQkX`
- **Path**: `tools/github/poll-pr-gate.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-30T15:34:45Z on `tools/github/poll-pr-gate.ts`:43 (association: NONE)

  <pre>
  The header Exit codes comment lists only 0/1/2, but later the code/documentation introduces exit code 3 for JSON parse failures (parseJsonOrExit exits 3). Please align the documented exit codes with actual behavior (and/or collapse parse failures into an existing exit code) so users don’t get surprised by undocumented exit statuses.
  </pre>

### Thread 22 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5-yQlC`
- **Path**: `tools/github/poll-pr-gate.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-30T15:34:45Z on `tools/github/poll-pr-gate.ts`:225 (association: NONE)

  <pre>
  `runGhOrExit` uses spawnSync without setting `maxBuffer`. For `gh api graphql --paginate` on large PRs, stdout can exceed Node’s default buffer and get truncated, which then cascades into JSON parse failures. Consider adding a shared `SPAWN_MAX_BUFFER` (as used in other tools) and passing it to spawnSync here.
  </pre>

### Thread 23 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5-yRja`
- **Path**: `tools/github/poll-pr-gate.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-04-30T15:35:27Z on `tools/github/poll-pr-gate.ts`:166 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Short-circuit next action for closed pull requests**

  `nextAction` only treats `MERGED` as terminal, so a PR in `state: "CLOSED"` (closed without merge) can still be reported as `fix-failed-checks`, `resolve-threads`, or `wait-ci` based on stale check/thread data. That creates non-actionable work for the poller after a PR is intentionally closed, because the loop keeps chasing CI/thread blockers on a PR that can no longer merge.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 24 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM5-ycFf`
- **Path**: `tools/github/poll-pr-gate.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-04-30T15:45:21Z on `tools/github/poll-pr-gate.ts`:152 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Prioritize CLEAN merge state over raw failed-check count**

  `classifyGate` marks any PR with `checks.failed &gt; 0` as `BLOCKED` before honoring `mergeStateStatus === "CLEAN"`. GitHub’s `MergeStateStatus.CLEAN` explicitly means the PR is mergeable even when some commit statuses are non-passing, so this path misclassifies PRs that only have non-required failing checks and drives `nextAction` into `fix-failed-checks` unnecessarily. In those cases the loop can keep working a non-blocking failure while the PR is already mergeable.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 25 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM5-ycFh`
- **Path**: `tools/github/poll-pr-gate.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-04-30T15:45:21Z on `tools/github/poll-pr-gate.ts`:170 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Report actionable state when gate is BLOCKED**

  `nextAction` falls back to `"none"` whenever a PR is not merged/closed/dirty and has no failed checks, unresolved threads, or pending checks. That creates a dead-end for valid `mergeStateStatus: "BLOCKED"` cases caused by other protections (e.g., required Copilot review/ruleset gates), where the PR is still unmergeable but this tool reports no next step. In this repo’s documented protection model, that state exists, so returning `none` here can stall the autonomous loop on still-blocked PRs.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 26 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM5-yeRm`
- **Path**: `tools/github/poll-pr-gate.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-30T15:47:27Z on `tools/github/poll-pr-gate.ts`:153 (association: NONE)

  <pre>
  P0: `classifyGate` can return `CLEAN` based solely on `mergeStateStatus === "CLEAN"` even when checks are still pending/in-progress (since `checks.inProgress`/`checks.pending` aren’t considered here). That can yield a contradictory report like `gate=CLEAN` but `nextAction=wait-ci`. Consider factoring pending/in-progress into the gate classification (or returning `UNSTABLE`/`UNKNOWN` until all required checks are terminal+OK).
  ```suggestion
    if (mergeStateStatus === "BLOCKED") return "BLOCKED";
    // A PR is not clean while required checks are still non-terminal; otherwise
    // we can report gate=CLEAN while nextAction=wait-ci, which is contradictory.
    if (checks.inProgress &gt; 0 || checks.pending &gt; 0) return "UNKNOWN";
  ```
  </pre>

### Thread 27 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM5-yeSF`
- **Path**: `tools/github/poll-pr-gate.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-30T15:47:28Z on `tools/github/poll-pr-gate.ts`:224 (association: NONE)

  <pre>
  P1: `runGhOrExit` doesn’t distinguish `spawnSync` termination-without-exit (`result.status === null`, possibly with `result.signal`) from a normal non-zero exit. Right now that path reports `gh exited null` and exits 2, which hides the real failure mode. Consider mirroring the repo’s common `classifySpawnFailure` pattern (ENOENT vs signal vs non-zero) and emitting an accurate message + exit code.
  </pre>

### Thread 28 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM5-yeSU`
- **Path**: `tools/github/poll-pr-gate.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-30T15:47:28Z on `tools/github/poll-pr-gate.ts`:402 (association: NONE)

  <pre>
  P2: `parseArgs` (and `requireValue`) call `process.exit(...)` directly. Since `main` is exported, this makes the module hard to test/compose (callers can’t recover from parse errors). Consider having `parseArgs` return a typed `{kind:"ok"|"help"|"error"}` result and letting `main` decide the final exit code/output, consistent with other `tools/` scripts.
  ```suggestion
  type ParseArgsResult =
    | { kind: "ok"; args: ParsedArgs }
    | { kind: "help"; message: string }
    | { kind: "error"; message: string };

  type RequireValueResult =
    | { kind: "ok"; value: string }
    | { kind: "error"; message: string };

  function parseArgs(argv: string[]): ParseArgsResult {
    const out: ParsedArgs = {
      owner: "Lucent-Financial-Group",
      repo: "Zeta",
    };
    const requireValue = (
      flag: string,
      v: string | undefined,
    ): RequireValueResult =&gt; {
      if (v === undefined || v.startsWith("--")) {
        return { kind: "error", message: `${flag} requires a value` };
      }
      return { kind: "ok", value: v };
    };
    for (let i = 0; i &lt; argv.length; i++) {
      const arg = argv[i];
      if (arg === undefined) continue;
      if (arg === "--fixture") {
        const fixture = requireValue("--fixture", argv[++i]);
        if (fixture.kind === "error") return fixture;
        out.fixture = fixture.value;
      } else if (arg === "--owner") {
        const owner = requireValue("--owner", argv[++i]);
        if (owner.kind === "error") return owner;
        out.owner = owner.value;
      } else if (arg === "--repo") {
        const repo = requireValue("--repo", argv[++i]);
        if (repo.kind === "error") return repo;
        out.repo = repo.value;
      } else if (/^\d+$/.test(arg)) {
        const parsed = Number.parseInt(arg, 10);
        if (parsed &lt;= 0) {
          return { kind: "error", message: "PR number must be a positive integer" };
        }
        out.number = parsed;
      } else if (arg === "--help" || arg === "-h") {
        return {
          kind: "help",
          message:
            "Usage: poll-pr-gate.ts &lt;PR_NUMBER&gt; [--owner X] [--repo Y]\n" +
            "       poll-pr-gate.ts --fixture path/to/fixture.json\n",
        };
      } else {
        return { kind: "error", message: `unknown arg: ${arg}` };
      }
    }
    return { kind: "ok", args: out };
  }

  export function main(argv: string[]): number {
    const parsedArgs = parseArgs(argv);
    if (parsedArgs.kind === "help") {
      process.stdout.write(parsedArgs.message);
      return 0;
    }
    if (parsedArgs.kind === "error") {
      process.stderr.write(`${parsedArgs.message}\n`);
      return 1;
    }

    const args = parsedArgs.args;
  ```
  </pre>

### Thread 29 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM5-yeSm`
- **Path**: `tools/github/poll-pr-gate.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-30T15:47:28Z on `tools/github/poll-pr-gate.ts`:109 (association: NONE)

  <pre>
  P2: `PENDING_STATUSES` includes `EXPECTED`, but StatusContext `EXPECTED` is normalized in `normalizeRollup` into `status: "PENDING"` (and only `state` carries `EXPECTED`). Keeping `EXPECTED` here is dead/ambiguous and suggests a state-vs-status mixup; consider removing it or renaming the sets to make the distinction explicit.
  ```suggestion
    // Normalized pending statuses plus CheckRun non-terminal states
    // surfaced in `status` (per Codex P1, second pass):
  ```
  </pre>

## Fix commits (touching thread paths)

### `251a3a7cb1ea88b30dbcd3931740ff7793fd7414` -- 2026-04-30T15:15:29Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/github/fixtures/clean-armed-auto-merge.json`
- `tools/github/poll-pr-gate.ts`

**Message:**

```
tools(github): poll-pr-gate.ts v0 — promote prose-jq to executable (5…

…-AI convergent)

Closes part 1 of task #355. 5-AI convergence (Amara 2nd, Deepseek 4th,
Alexia 5th, Ani 3rd, Gemini 4th — all 2026-04-30) on promoting the
inline jq snippets in
`memory/feedback_amara_poll_gate_not_ending_holding_is_not_status_2026_04_30.md`
into a tested executable. Amara's blade: *"if the loop uses it every
tick, it deserves tests."*

This is **v0** — skeleton + minimal happy-path query. Works live
against `gh pr view --json` + a paired `gh api graphql` call for
review threads. Fixture mode for offline testing.

Output shape per Amara's spec:

```json
{
  "number": 917,
  "state": "OPEN",
  "gate": "CLEAN" | "BLOCKED" | "DIRTY" | "UNSTABLE" | "UNKNOWN",
  "checks": { "ok": N, "inProgress": N, "pending": N, "failed": N },
  "unresolvedThreads": N,
  "autoMerge": "armed" | "none",
  "mergeCommit": "<sha>" | null,
  "nextAction": "wait-ci" | "resolve-threads" | "rebase" | "verify-merge" | "none"
}
```

Required-check semantics (per Amara 2nd's GitHub-docs verification):

- Merge-satisfying: `SUCCESS`, `NEUTRAL`, `SKIPPED`
- Blocking: `FAILURE`, `CANCELLED`, `TIMED_OUT`, `STARTUP_FAILURE`,
  `ACTION_REQUIRED`, `STALE`
- Pending: `QUEUED`, `PENDING`, `IN_PROGRESS`

Verified against:
- Live PR #915 (just merged) → state=MERGED, gate=CLEAN,
  nextAction=verify-merge
- Live PR #919 (just merged) → state=MERGED, gate=CLEAN,
  nextAction=verify-merge
- Fixture clean-armed-auto-merge → gate=BLOCKED,
  nextAction=none (auto-merge does the babysitting)
- Fixture blocked-by-threads → gate=BLOCKED, unresolvedThreads=3,
  nextAction=resolve-threads

Two fixtures land with v0; matrix coverage (CheckRun SUCCESS/SKIPPED/
NEUTRAL/STALE × StatusContext × pending × mixed × missing-conclusion)
follows in subsequent slices. Per substrate-rate this is a v0 commit;
expanding fixtures and adding a test runner are queued under task #355.

The memory file should stop being the implementation. It now points
to this file. Subsequent PR will add a top-of-memory pointer.

Composes with Aaron's substrate-IS-product framing — executable
substrate IS substrate-quality work; the factory's tooling-product
deserves the same honest-substrate discipline as the substrate-
product. Slice 22 of the TS+Bun migration trajectory (B-0086).

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
```

### `a7b8e26e82842c7d985526b95d1492eec10366bc` -- 2026-04-30T15:17:56Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/github/poll-pr-gate.ts`

**Message:**

```
fix(github): poll-pr-gate parseArgs — exactOptionalPropertyTypes comp…

…liance

CI lint(tsc tools) caught 4 type errors in parseArgs caused by the
repo's strict tsconfig (`exactOptionalPropertyTypes: true` +
`noUncheckedIndexedAccess: true`):

- `argv[++i]` returns `string | undefined` under noUncheckedIndexedAccess
- The return-object literal with `{ fixture: string | undefined, ... }`
  doesn't satisfy `{ fixture?: string }` under exactOptionalPropertyTypes

Fix: build the return object incrementally, only assigning the
optional fields when their value is actually defined. Hoist the
shape into a named `ParsedArgs` interface for clarity.

This is exactly the kind of catch the dogfood-self-test would have
caught locally if I'd run tsc before pushing — slot for a pre-push
typecheck hint in a follow-up.

Local verification:
  $ bunx tsc --noEmit -p . | grep poll-pr-gate
  (no output)
  $ bun tools/github/poll-pr-gate.ts --fixture tools/github/fixtures/clean-armed-auto-merge.json
  (correct output)

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
```

### `27e63d6243da77657ba9f9f6b34267aadb72fe89` -- 2026-04-30T15:22:30Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/github/poll-pr-gate.ts`

**Message:**

```
fix(github): poll-pr-gate — pagination + StatusContext ERROR/EXPECTED…

… + fix-failed-checks action (Codex P1×2)

Two real defects from Codex P1 review on PR #921 v0:

1. **Pagination missing** (line 209): query was `reviewThreads(first:50)`
   which truncates discussion-heavy PRs. Switched to
   `gh api graphql --paginate` with `pageInfo{hasNextPage endCursor}`
   + `after:$endCursor` cursor. Aggregates nodes across all pages.

2. **StatusContext EXPECTED/ERROR not classified** (line 236): the
   normalization treated every non-PENDING state as COMPLETED + raw
   conclusion, but classifyChecks's OK_CONCLUSIONS / BLOCKING_CONCLUSIONS
   sets didn't include `ERROR`. EXPECTED states (StatusContext "queued"
   equivalent) weren't mapped to pending either. Real defect: tools
   would silently miss CI errors on StatusContext-class checks.

   Fix:
   - Added `ERROR` to BLOCKING_CONCLUSIONS
   - Added `EXPECTED` to PENDING_STATE_LITERALS (maps to status=PENDING)
   - Extracted normalization into `normalizeRollup()` so fixture-mode
     and live-mode classify identically (caught only because dogfooding
     against PR #921 itself revealed live had different shape than
     fixtures)

3. **Bonus: fix-failed-checks vs resolve-threads action distinction**
   — previously both code paths returned `resolve-threads` whether the
   block was failed CI or unresolved review threads. Added explicit
   `fix-failed-checks` action so the agent gets a precise next-step
   indicator.

Added a third fixture `status-context-error.json` covering ERROR +
EXPECTED states. Now classifies as: 1 ok / 1 pending / 1 failed →
nextAction=fix-failed-checks. All three fixtures + live PR #921 verified.

The dogfood pattern is working — the Codex flag landed because the
script was running and producing inspectable output that revealed
gaps the reviewer's static analysis caught.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
```

### `8f9b33844f3732d5fca667df97037d91da93961f` -- 2026-04-30T15:27:36Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/github/poll-pr-gate.ts`

**Message:**

```
fix(github): poll-pr-gate — BEHIND state + spawn/parse error distinct…

…ion + flag-value validation + doc typo (Copilot P0+P1×4)

Five Copilot-flagged real defects from PR #921 review pass on the
prior commit (a7b8e26):

1. **BEHIND mergeStateStatus not handled** (Copilot P0, line 137)
   — `BEHIND` (base advanced past PR's merge-base — needs rebase)
   was unhandled in classifyGate, causing it to fall through to
   UNKNOWN. Added: `BEHIND` and `DIRTY` both produce gate=DIRTY,
   nextAction=rebase. Verified via new `behind-needs-rebase.json`
   fixture.

2. **spawnSync launch failure not distinguished from gh non-zero**
   (Copilot P1, line 200) — when `gh` is missing from PATH or
   couldn't be launched (ENOENT etc), spawnSync sets `result.error`
   but `result.status` is null, which my prior `status !== 0`
   check would have treated as a non-zero exit (exit code 2).
   Fix: extracted `runGhOrExit()` helper that distinguishes
   `result.error` (exit 1, dependency error) from `result.status
   !== 0` (exit 2, gh-side error). Both branches now have distinct
   stderr context tags.

3. **JSON.parse can throw on non-JSON output** (Copilot P1, line
   202) — `gh` could emit non-JSON on auth errors, truncation,
   etc. Fix: extracted `parseJsonOrExit<T>()` helper that catches
   parse errors, emits the first 200 bytes of input, and exits
   with code 3 (distinct from 1=invocation, 2=gh-side).

4. **--fixture/--owner/--repo missing-value validation** (Copilot
   P1, line 264) — passing `--owner` with no following value
   silently consumed nothing or grabbed an unrelated flag. Added
   `requireValue()` helper that exits 1 with a clear message if
   the next arg is missing or starts with `--`.

5. **Doc typo blocked-with → blocked-by** (Copilot P1, line 19) —
   usage example referenced `blocked-with-threads.json` but the
   file is `blocked-by-threads.json`. Mechanical fix.

Two stale Copilot threads (line 156 fix-failed-checks distinction
+ line 236 StatusContext EXPECTED/ERROR) were already addressed
in the prior commit (27e63d6). Resolving as stale-fixed. The line
295 "export main()" convention nit is deferred — the `tools/*.ts`
harness varies; this can be cleaned up in a follow-up sweep.

Exit code semantics now:
  0 — success
  1 — invocation / argument / dependency-missing
  2 — gh CLI returned non-zero (auth, rate-limit, PR not found)
  3 — gh output couldn't be parsed (truncated, non-JSON)

Verified: all four fixtures + live PR #921 classify correctly.
Missing-value test: `bun ... --owner` exits 1 with "--owner
requires a value" message.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
```

### `c481d24296a80070af88690396a1867754875ee7` -- 2026-04-30T15:28:56Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/github/poll-pr-gate.ts`

**Message:**

```
fix(github): poll-pr-gate — REQUESTED/WAITING CheckRun states are pen…

…ding (Codex P1)

Codex caught that classifyChecks's PENDING_STATUSES only included
QUEUED/PENDING/EXPECTED. CheckRun also has non-terminal REQUESTED
and WAITING states which would have been counted as neither
in-progress nor pending — silently dropped from the report,
producing false-positive `nextAction=none` when the PR is still
blocked by CI progression.

Added REQUESTED and WAITING to PENDING_STATUSES.

Per the GitHub schema, CheckRun.status is a non-exhaustive set
including IN_PROGRESS / QUEUED / COMPLETED / WAITING / REQUESTED /
PENDING. The script's classification logic now covers all
non-terminal states uniformly.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
```

### `cc3f4551ef629667de606b02e71a483b9ff7a92c` -- 2026-04-30T15:30:40Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/github/poll-pr-gate.ts`

**Message:**

```
fix(github): poll-pr-gate — match tools/*.ts main() pattern (Copilot P2)

Repo convention is `export function main(): number` + `if
(import.meta.main) { process.exit(main(...)) }` (used in
tools/peer-call/gemini.ts, tools/alignment/audit_*.ts,
tools/backlog/generate-index.ts, etc.).

My v0 used `function main(): void` + `main()` unconditionally,
which prevents the script from being imported as a module
(unconditionally executes side effects on import). The repo's
test harness pattern relies on the import-without-side-effects
shape.

Refactored to match: main() now returns exit code (0/1/2/3),
the import.meta.main guard ensures side effects only run when
invoked directly. Last remaining Copilot P2 thread on this PR
addressed.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
```

### `739de2c7390c2fd355acbb00f1a3efd3f6dc8988` -- 2026-04-30T15:37:46Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/github/fixtures/clean-armed-auto-merge.json`
- `tools/github/poll-pr-gate.ts`

**Message:**

```
fix(github): poll-pr-gate — fixture name match + loadFixture error ha…

…ndling + positive PR-number + maxBuffer + CLOSED-state terminal + exit-code doc (Copilot P1×4 + Codex P2)

Six real defects from Copilot P1×4 + Codex P2 in the latest review wave:

1. **fixture mergeStateStatus mismatched name** (Copilot) — fixture
   `clean-armed-auto-merge.json` had mergeStateStatus=BLOCKED with
   the name promising "clean." With classifyGate now treating CLEAN
   correctly, set the fixture's mergeStateStatus to CLEAN. Now
   classifies as gate=CLEAN, next=none — matches the name's
   intent.

2. **loadFixture no error handling** (Copilot) — JSON.parse +
   readFileSync would throw an unhandled exception for missing /
   invalid fixtures (stack trace, no controlled exit). Wrapped in
   try/catch with controlled exit 1 + clear stderr message.
   Verified: passing a nonexistent fixture path produces
   "failed to load fixture <path>: ENOENT...".

3. **PR number 0 accepted** (Copilot) — `/^\d+$/` matched "0"
   as a valid PR number, but GitHub PR numbers are >0. Added
   parsed-value check that rejects <= 0 with exit 1 and clear
   message. Verified: `bun ... 0` produces "PR number must be a
   positive integer".

4. **spawnSync maxBuffer not set** (Copilot) — default 1 MiB buffer
   could truncate `gh api graphql --paginate` output on
   discussion-heavy PRs, cascading into JSON parse failures.
   Added SPAWN_MAX_BUFFER = 32 MiB constant; passed to spawnSync.

5. **CLOSED state not treated as terminal** (Codex P2) — nextAction
   only treated MERGED as terminal, so a PR in state=CLOSED could
   still be reported as fix-failed-checks/resolve-threads/wait-ci
   based on stale check/thread data. Added CLOSED → next=none
   short-circuit to avoid chasing blockers on intentionally-closed
   PRs.

6. **Exit codes doc inconsistency** (Copilot) — header listed
   0/1/2 only; code introduces 3 for parseJsonOrExit. Aligned the
   header documentation to mention all four exit codes (0=success,
   1=invocation/dependency, 2=gh-side, 3=parse failure).

Two stale Copilot threads from the earlier rounds (yQiO export-main
pattern + the "fix-failed-checks not in PR description" thread)
addressed by my prior commit (cc3f455) — convention-conformance
done. Resolving as stale-fixed.

Three style/convention threads (yQfm eslint suppression, yQh0
persona names in comments) deferred — Otto-279 history-class
attribution carve-out covers persona-name comments in tooling
files; eslint-suppression convention is a project-wide pattern
audit candidate, not this-PR-specific.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
```

### `ecb4585e6dc840aa8ce4364dc9bf8b11fb46f6af` -- 2026-04-30T15:40:27Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/github/poll-pr-gate.ts`

**Message:**

```
fix(github): poll-pr-gate — eslint-disable + Otto-279 role-refs (Copi…

…lot)

Two final Copilot threads on PR #921 addressed:

1. **eslint-disable for spawnSync gh** (Copilot) — convention
   across tools/ (audit-packages.ts, pr-preservation/archive-pr.ts,
   peer-call/*, lint/runner-version-freshness.ts) is to suppress
   sonarjs/no-os-command-from-path with an inline rationale comment.
   Added the standard suppression to runGhOrExit's spawnSync call.

2. **Otto-279 role-refs in current-state code** (Copilot) — the
   header comment listed persona first-names ("Amara", "Deepseek",
   "Alexia", "Ani", "Gemini"). Per Otto-279's name-attribution
   carve-out, persona names belong on closed-list history surfaces
   (memory/, docs/ROUND-HISTORY.md, docs/DECISIONS/, docs/research/,
   commit messages) — not on current-state code. Replaced with
   role-ref "5-AI peer-reviewer convergence" + pointer to the
   verbatim attribution in the research doc. Same load-bearing
   provenance (the convergence claim), correct scope discipline.

The third remaining thread (PR description's nextAction list missing
fix-failed-checks) is a doc-only edit to the PR body, addressed
separately via PR description update; resolving with that note.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
```
