# PR #878 -- ts(B-0086): port 3 lint-pattern scripts (.sh→.ts) — slice 7 of TS/Bun migration

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 878 |
| Title | ts(B-0086): port 3 lint-pattern scripts (.sh→.ts) — slice 7 of TS/Bun migration |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-04-30T03:06:19Z |
| Merged at | 2026-04-30T03:36:01Z |
| Merge commit SHA | `4dac957e6b59b12dd17787295318886a070f66b0` |
| Branch | `lane-b/ts-bun-slice-7-lint-scripts-2026-04-30` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/878 |
| Changed files | 5 |
| Additions / deletions | +1042 / -1 |

## Description

## Summary

- Slice 7 of the TS+Bun migration. Ports 3 lint-pattern scripts from `tools/lint/*.sh` → `tools/lint/*.ts`, byte-equivalent on a clean tree (modulo one intentional self-reference diff and one behavioural improvement on the bash-3.2 empty-array case).
- Cluster H-3-of-5 — the two remaining (`no-directives-otto-prose.sh` 261 lines + `runner-version-freshness.sh` 356 lines) are deferred to slice 8 as a natural size boundary; `no-directives-otto-prose` also has open backlog #350 (extend scope to commit messages + PR bodies + skill files), so porting it now would force coupled review.

## Files

- `tools/lint/no-empty-dirs.{sh→ts}` — readdirSync recursive walk, git-check-ignore batch filter, allowlist-set partition. Fixes pre-existing macOS bash 3.2 `set -u + empty FILTERED[@]` bug.
- `tools/lint/safety-clause-audit.{sh→ts}` — three-tier H1/H2/H3 regex classification of `.claude/skills/*/SKILL.md` files. Regex arrays split per-pattern to stay under sonarjs/regex-complexity threshold.
- `tools/lint/doc-comment-history-audit.{sh→ts}` — comment-line scan + token boundary check + baseline diff. 60+ lines of inline awk replaced with `extractTokens()` doing per-line `RegExp.exec` + manual word-boundary check.

Plus:

- `docs/trajectories/typescript-bun-migration/slice-audits.md` — slice-7 audit appended (Gate A merge-gate substrate)
- `docs/trajectories/typescript-bun-migration/RESUME.md` — milestone bumped to 22 ports + slice-8 candidate list updated

## Equivalence

Per-script diff against bash original on current repo state:

| script | mode(s) tested | result |
|---|---|---|
| no-empty-dirs | check, --list | TS handles empty-array case; bash 3.2 errors on `set -u`. Behavioural improvement, not divergence. |
| safety-clause-audit | summary, --list-missing, --verbose | byte-equivalent |
| doc-comment-history-audit | check, --list, --fail-any, --regenerate-baseline | byte-equivalent except intentional script self-reference (.ts vs .sh) in error help |

## Lint + types

All three files pass:

- `bun x tsc --noEmit` clean
- `bun x eslint` clean (typescript-eslint strictTypeChecked + stylisticTypeChecked + sonarjs)

## Test plan

- [ ] CI: lint + typecheck on the slice files
- [ ] CI: `bun tools/lint/no-empty-dirs.ts` returns 0 on clean tree
- [ ] CI: `bun tools/lint/safety-clause-audit.ts` returns matching counts
- [ ] CI: `bun tools/lint/doc-comment-history-audit.ts` returns 0 (no new violations vs baseline)

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | true |
| Total threads | 17 |
| Resolved threads | 16 |
| Unresolved threads | 1 |
| Total review comments | 18 |
| Total fix commits (touching thread paths) | 8 |

## Review threads

### Thread 1 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5-n2C0`
- **Path**: `tools/lint/no-empty-dirs.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-04-30T03:10:30Z on `tools/lint/no-empty-dirs.ts`:151 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Strip CRLF when parsing allowlist entries**

  The TypeScript port only trims trailing spaces and tabs from allowlist lines, so a CRLF checkout leaves `\r` attached to each entry and breaks equality checks in `partitionEmpty`. In that environment, legitimately allowlisted empty directories are misclassified as flagged, causing false lint failures that the shell version avoided by stripping all trailing whitespace.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 2 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5-n2C1`
- **Path**: `tools/lint/safety-clause-audit.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-04-30T03:10:30Z on `tools/lint/safety-clause-audit.ts`:118 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Reject malformed --fail-over arguments**

  If `--fail-over` is passed without a numeric value (for example from an empty env expansion like `--fail-over $THRESHOLD`), `parseFailOverArg` returns `{ value: null }` and execution continues with `failOver` disabled. That silently bypasses the threshold gate and can let missing safety clauses pass in automation instead of failing fast on bad CLI input.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 3 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5-n3TQ`
- **Path**: `tools/lint/safety-clause-audit.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-30T03:13:26Z on `tools/lint/safety-clause-audit.ts`:276 (association: NONE)

  <pre>
  In `--list-missing` mode, when there are no missing skills this prints a blank line. The bash version prints nothing in that case, so this is a behavior/byte-equivalence change and makes piping less convenient. Consider emitting zero bytes when the list is empty.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-30T03:25:23Z on `tools/lint/safety-clause-audit.ts`:276 (association: MEMBER)

    <pre>
    Verified empirically — bash actually emits a single newline on empty list (`printf '%s\n' "${arr[@]:-}"` produces `\n` due to `:-` expansion + printf format). The TS port preserves byte-equivalence. Documented this in the inline comment with the empirical result. Leaving the empty-set behaviour matching bash so byte-equivalence claim isn't violated; users wanting zero-bytes-on-empty can pipe through `grep -v '^$'`.
    </pre>

### Thread 4 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5-n3Th`
- **Path**: `tools/lint/doc-comment-history-audit.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-30T03:13:26Z on `tools/lint/doc-comment-history-audit.ts`:15 (association: NONE)

  <pre>
  This comment describes `#` as a “code-file comment” alongside `///` and `//`, but for `.fs`/`.cs` files `#` usually denotes directives (not comments). Since `isCommentLine()` treats `#`-prefixed lines as in-scope, it would be clearer to document this as “shell `#` comments (and any `#`-prefixed lines in scanned files)” to avoid misleading readers.
  ```suggestion
  // The rule: a code-file comment (`///`, `//`), shell `#` comments,
  // and any `#`-prefixed lines in scanned files should explain what
  // the code DOES — math, invariants, input contracts, composition
  // guidance. They should not carry process-lineage tags (round
  // shipped, external collaborator who formalised it, correction
  // number, persona attribution). That content belongs in the PR
  // description, the commit message, `docs/hygiene-history/**`, or
  // memory files.
  ```
  </pre>

### Thread 5 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5-n3Tv`
- **Path**: `tools/lint/doc-comment-history-audit.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-30T03:13:27Z on `tools/lint/doc-comment-history-audit.ts`:62 (association: NONE)

  <pre>
  `tools/lean4/.lake` is a known heavy build-output tree (and is hard-excluded in `no-empty-dirs.ts`), but this scanner doesn’t exclude `.lake`. If `.lake` exists locally, the directory walk can become very slow even though few files match the extensions. Consider adding `.lake` (or specifically `tools/lean4/.lake`) to `SCAN_EXCLUDE_SEGMENTS`.
  ```suggestion
    "node_modules",
    ".lake",
  ```
  </pre>

### Thread 6 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5-n3T6`
- **Path**: `docs/trajectories/typescript-bun-migration/slice-audits.md`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-30T03:13:27Z on `docs/trajectories/typescript-bun-migration/slice-audits.md`:438 (association: NONE)

  <pre>
  The `no-empty-dirs.ts` bullet looks like it accidentally broke an inline regex example across lines, leaving a stray blank line and an incomplete inline-code span (``[\t ]+``). Please reflow this so the bullet renders as a single coherent sentence.
  ```suggestion
  - **`no-empty-dirs.ts`**: readdirSync recursive walk + git-check-ignore batch + Set-based allowlist match. `[\t ]+$` trailing-whitespace strip replaced with manual char walk (`trimTrailingSpaceTab`) — no slow-regex flag. Comment-or-blank line check (bash `^[[:space:]]*(#|$)`) replaced with manual char walk (`isCommentOrBlankLine`) — same.
  ```
  </pre>

### Thread 7 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5-n3UF`
- **Path**: `docs/trajectories/typescript-bun-migration/slice-audits.md`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-30T03:13:27Z on `docs/trajectories/typescript-bun-migration/slice-audits.md`:452 (association: NONE)

  <pre>
  “open backlog #350” is ambiguous with PR #350 in this repo. Consider renaming this reference to “Task #350” (or adding a concrete link/path) so readers can resolve it unambiguously.
  ```suggestion
  Slice 7 passes audit. No new patterns recorded — all reused from prior slices. The two remaining Cluster H scripts (`no-directives-otto-prose.sh` at 261 lines + `runner-version-freshness.sh` at 356 lines) are deferred to slice 8 — natural size boundary, plus `no-directives-otto-prose` has open Task #350 (extend scope) so port-then-extend likely needs paired review.
  ```
  </pre>

### Thread 8 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5-n3UR`
- **Path**: `tools/lint/no-empty-dirs.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-30T03:13:27Z on `tools/lint/no-empty-dirs.ts`:154 (association: NONE)

  <pre>
  `trimTrailingSpaceTab()` only trims space/tab. If the allowlist file is checked out with CRLF, entries will retain a trailing `\r` and won’t match directory paths, causing false positives. Consider trimming all trailing whitespace (or at least including `\r`) when normalizing allowlist lines.
  </pre>

### Thread 9 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5-n3Ua`
- **Path**: `tools/lint/safety-clause-audit.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-30T03:13:28Z on `tools/lint/safety-clause-audit.ts`:163 (association: NONE)

  <pre>
  `--fail-over` with a missing or non-numeric value is silently ignored (treated as `null`). The bash version fails hard under `set -u` / integer comparison, so this changes behavior and can hide misconfigurations. Consider treating missing/invalid `--fail-over` as a usage error (non-zero exit + message).
  </pre>

### Thread 10 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5-n3Uk`
- **Path**: `tools/lint/safety-clause-audit.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-30T03:13:28Z on `tools/lint/safety-clause-audit.ts`:194 (association: NONE)

  <pre>
  The bash script supports `-h|--help` (prints usage and exits 0), but the TS port currently has no help path and does not report unknown flags as errors. If this script is expected to follow `docs/best-practices/repo-scripting.md` CLI conventions, consider adding `-h/--help` and a usage error exit for unknown args.
  </pre>

### Thread 11 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5-oAgS`
- **Path**: `tools/lint/no-empty-dirs.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-30T03:29:58Z on `tools/lint/no-empty-dirs.ts`:97 (association: NONE)

  <pre>
  On Windows, `relative(root, full)` returns paths with `\` separators. That breaks `isHardExcluded()` (prefix/segment checks use `/`), allowlist matching, and can cause the walk to descend into huge excluded trees (e.g. `references/upstreams`, `node_modules`) and/or mis-report directories. Consider normalizing all repo-relative paths to POSIX form (convert `\\` → `/`) immediately after `relative(...)` and before any exclude/allowlist logic.
  </pre>

### Thread 12 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5-oAgb`
- **Path**: `tools/lint/no-empty-dirs.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-30T03:29:58Z on `tools/lint/no-empty-dirs.ts`:135 (association: NONE)

  <pre>
  Same Windows path-separator issue: `relative(root, dir)` will produce `\` on Windows, but later output/allowlist/`git check-ignore` calls assume `/`-separated repo-relative paths. Normalizing the computed `rel` to `/` here avoids platform-dependent behavior and keeps output stable across environments.
  </pre>

### Thread 13 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5-oAgk`
- **Path**: `tools/lint/doc-comment-history-audit.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-30T03:29:58Z on `tools/lint/doc-comment-history-audit.ts`:200 (association: NONE)

  <pre>
  `rel` is computed via `relative(root, full)`, which uses OS-specific separators (`\` on Windows). Since this tool’s baseline and output are repo-path based (and the bash original emits `/` paths), Windows runs will generate different `file:line:tokens` rows and can incorrectly report every violation as “new”. Normalize `rel` to POSIX (`/`) before pushing it to `out` and before constructing the emitted rows.
  </pre>

### Thread 14 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5-oAgz`
- **Path**: `tools/lint/safety-clause-audit.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-30T03:29:59Z on `tools/lint/safety-clause-audit.ts`:161 (association: NONE)

  <pre>
  This TS port treats a non-integer `--fail-over` value as a usage error (exits 1 with a message). The bash original does not validate this (it will typically print an “integer expression expected” diagnostic and still exit 0 because the failing `[` is inside an `if`). If the PR’s “byte-equivalent” claim is meant to include invalid-arg behavior, this is a divergence that should be called out alongside the no-empty-dirs bash-3.2 fix; alternatively, change the TS behavior to match bash for invalid values.
  </pre>

### Thread 15 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5-oAg8`
- **Path**: `tools/lint/safety-clause-audit.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-30T03:29:59Z on `tools/lint/safety-clause-audit.ts`:340 (association: NONE)

  <pre>
  The header comment documents exit code `1` only for the `--fail-over` threshold case, but `main()` also returns `1` for argument/usage errors. Please update the documented exit-code section to include the usage-error case (and ideally align with the repo scripting convention of `64` for usage/input errors per `docs/best-practices/repo-scripting.md:50-73`, if this script is intended to follow that standard).
  </pre>

### Thread 16 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5-oAhG`
- **Path**: `docs/trajectories/typescript-bun-migration/RESUME.md`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-30T03:29:59Z on `docs/trajectories/typescript-bun-migration/RESUME.md`:4 (association: NONE)

  <pre>
  This milestone line will become stale immediately after this PR merges (“3 in-flight on slice-7 branch”). Since `RESUME.md` is a current-state summary, consider wording it in a merge-stable way (e.g. reference the PR number as pending until merged, or update it to the post-merge state when landing the slice) so the page doesn’t carry instantly outdated status.
  ```suggestion
  **Milestone**: 22 hygiene/lint scripts ported (2 from #849 + 3 from #866 + 3 from #868 + 3 from #870 + 2 from #872 + 3 from #874 + 3 from #876 + 3 lint-pattern scripts landed in the next slice). **Cluster H-3-of-5: 3 lint-pattern scripts ported (no-empty-dirs, safety-clause-audit, doc-comment-history-audit); 2 remain deferred to slice 8 (no-directives-otto-prose at 261 lines and runner-version-freshness at 356 lines — natural slice-8 boundary).**
  ```
  </pre>

### Thread 17 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM5-oFZz`
- **Path**: `tools/lint/no-empty-dirs.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-04-30T03:38:01Z on `tools/lint/no-empty-dirs.ts`:144 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Remove process-history token from code comment**

  `doc-comment-history-audit` flags `Otto-\d+` on comment lines, and this new comment introduces `Otto-235` without a corresponding baseline entry. In check mode, that creates a new violation (`tools/lint/no-empty-dirs.ts:144:Otto-235`) and breaks the intended clean audit/equivalence run for this slice unless the baseline is regenerated. This should be avoided in source comments to keep the lint non-regressing.

  Useful? React with 👍 / 👎.
  </pre>

## Fix commits (touching thread paths)

### `7d7410c3736959b59a4df01f92e1c72b3c6388e1` -- 2026-04-30T02:56:40Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/lint/no-empty-dirs.ts`

**Message:**

```
ts(slice-7, wip 1/N): port no-empty-dirs (.sh→.ts)

First script of slice 7 — Cluster H lint scripts. Port keeps
behaviour byte-equivalent on a clean tree (both produce
"OK (0 allowlisted, 0 flagged)" output) while also fixing the
pre-existing bash bug where set -u + macOS bash 3.2 + empty
FILTERED array triggers "unbound variable" in the for-loop.

Mechanical changes:
- find -type d -empty replaced with readdirSync recursion +
  empty-entries check, no shell-out
- HARD_EXCLUDE_PREFIXES + HARD_EXCLUDE_SEGMENTS encode the
  bash -not -path predicates declaratively
- gitCheckIgnore batches one git invocation, same as bash
- bash regex `^[[:space:]]*(#|$)` becomes manual char walk
  (isCommentOrBlankLine) — no slow-regex flag
- bash `[[:space:]]*$` tail-strip becomes trimTrailingSpaceTab
  (manual char walk) — no slow-regex flag
- output strings preserved verbatim including "FAIL — "
  em-dash, "(none)" placeholders, fix-options block

Lint-clean (eslint + sonarjs + tsc) under repo's strict config.
```

### `0c1a7387d7b51f05dc70aa42717aa830c335c969` -- 2026-04-30T02:59:48Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/lint/safety-clause-audit.ts`

**Message:**

```
ts(slice-7, wip 2/N): port safety-clause-audit (.sh→.ts)

Second script of slice 7. Counts how many .claude/skills/*/SKILL.md
files carry an explicit scope-limiting heading (H1 explicit / H2
scope / H3 authority / MISSING) and emits one of three formats.

Byte-equivalent on this repo for all three modes (--summary
default, --list-missing, --verbose) — diff'd clean against bash
output.

Mechanical changes:
- bash glob `${SKILLS_GLOB}` replaced with readdirSync of
  .claude/skills/* + statSync probe on each SKILL.md path
- bash three multi-alternative grep patterns split into
  H1/H2/H3 RegExp arrays where each individual pattern stays
  under sonarjs/regex-complexity threshold (20). H1 array uses
  /i + non-capturing optional `(?:[\t ]+do)?` to collapse
  NOT/not/NOT do/not do alternatives without inflating
  complexity
- bash `grep -iqE` replaced with `RegExp.test` per-file +
  matchesAny() helper
- bash awk-printf percentage formatting replaced with
  `(100 * c / t).toFixed(1)`
- argument parsing extracted into parseFailOverArg() so the
  main parseArgs loop stays under sonarjs/cognitive-complexity
  threshold (15)
- output strings preserved verbatim, including em-dashes,
  table column widths, "(none)" placeholders, FAIL prefix

Lint-clean (eslint + sonarjs + tsc) under repo's strict config.
```

### `a6d10448c494b8ed40ee67fa9e759a5c32a48c95` -- 2026-04-30T03:04:45Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/lint/doc-comment-history-audit.ts`

**Message:**

```
ts(slice-7, wip 3/N): port doc-comment-history-audit (.sh→.ts)

Third script of slice 7. Scans source doc comments for
factory-process tokens (Otto-NN, Amara, Aaron, ferry, courier,
graduation, Provenance:, Attribution:) on `///`/`//`/`#`
comment lines and emits file:line:tok1,tok2,... violations.

Byte-equivalent on this repo across all four modes (check,
--list, --fail-any, --regenerate-baseline) — the only diff
is the script self-reference (.sh vs .ts) in the error help
text, which is correct (each invocation tells users to use
the same form they ran).

Mechanical changes:
- bash `find src tests bench tools` replaced with explicit
  per-root readdirSync recursion + extension filter +
  bin/obj/.venv/node_modules exclusion
- bash awk in-line script (60+ lines of awk) replaced with
  TS extractTokens() function:
    - per-line match against TOKEN_RE (global, sticky-iterated)
    - manual leading + trailing word-boundary check via
      isBoundary() — same logic as the awk for portability
    - tokens-ending-in-`:` skip trailing boundary check
    - dedupe + sort per line
- bash `grep ... | sort` baseline-diff replaced with
  Set-difference filter
- bash `comm -23` (current minus baseline) replaced with
  filter(row => !baseSet.has(row))
- listScanFiles split into walkRoot + processEntry + readDirEntries
  to keep cognitive complexity under sonarjs threshold (15)
- `[0-9]` → `\d`, `[A-Za-z0-9_]` → `\w` per sonarjs
- output strings preserved verbatim (script self-ref aside)

Lint-clean (eslint + sonarjs + tsc) under repo's strict config.
```

### `97af934e5628ac2b757b49986d9a540e6ea88580` -- 2026-04-30T03:05:56Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `docs/trajectories/typescript-bun-migration/RESUME.md`
- `docs/trajectories/typescript-bun-migration/slice-audits.md`

**Message:**

```
trajectory(ts-bun): slice 7 audit substrate + RESUME tracker

- slice-audits.md: append slice-7 audit (3 Cluster H lint scripts:
  no-empty-dirs, safety-clause-audit, doc-comment-history-audit).
  Equivalence outcomes: no-empty-dirs is a behavioural improvement
  (TS port handles empty-FILTERED-array case where bash 3.2 errors
  on `set -u`); safety-clause-audit byte-equivalent across all 3
  modes; doc-comment-history-audit byte-equivalent across all 4
  modes (sole diff is intentional script self-reference .ts vs .sh).
- RESUME.md: bump milestone count to 22 ports (in-flight on slice-7
  branch); record 2 remaining Cluster H deferrals (no-directives-otto-
  prose 261 lines + runner-version-freshness 356 lines) as natural
  slice-8 boundary.
```

### `1bd6d2eddcfd719f6538c6158afdc7ea61e2f3bc` -- 2026-04-30T03:15:25Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `docs/trajectories/typescript-bun-migration/slice-audits.md`

**Message:**

```
fix(slice-audits): MD032 — repair broken backtick + bullet list

Heredoc-induced unclosed backtick in the bash regex citation broke
the bullet list across line 436 (open backtick, no close), line 437
(blank from embedded newline), line 438 (orphan continuation). MD032
fired because the orphan content broke the surrounding bullet list
structure. Rewrote the bullet to refer to the regex by description
("trailing-whitespace strip regex matching tab/space at end-of-line")
instead of inlining the literal `[\\t ]+\$` which mishandles in
markdown + heredoc combinations.
```

### `e34c21082c7b9c2ee31c71f954a923d80058aa96` -- 2026-04-30T03:24:46Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `docs/trajectories/typescript-bun-migration/slice-audits.md`
- `tools/lint/doc-comment-history-audit.ts`
- `tools/lint/no-empty-dirs.ts`
- `tools/lint/safety-clause-audit.ts`

**Message:**

```
review(slice-7): address PR #878 P2 threads — CRLF + arg-validation +…

… .lake exclusion + Task #350

Five real-fix findings + one comment-clarification finding from
the PR #878 thread queue:

1. **CRLF on allowlist (P2, no-empty-dirs.ts)**: `trimTrailingSpaceTab`
   now also trims CR (0x0d). Bash's `[[:space:]]*$` regex matches CR
   on CRLF checkouts; the TS port previously only trimmed space + tab,
   which would silently fail to match allowlist entries with CRLF
   line endings (Otto-235 4-shell portability + git autocrlf=true on
   Windows). Two reviewer threads consolidated into one fix.

2. **--fail-over invalid (P2, safety-clause-audit.ts)**: bash's
   `[ "$missing" -gt "$fail_over" ]` errors hard under set -u when
   fail_over is empty/non-numeric. TS port previously silently
   ignored invalid values. Now: explicit usage-error on `--fail-over
   abc` or `--fail-over` with no value, exit 1 with clear message.
   Tagged-union ParsedArgs (ok / help / usage-error) + factored out
   ArgStep helper to keep cognitive-complexity under threshold.
   Two reviewer threads consolidated.

3. **-h / --help (P2, safety-clause-audit.ts)**: bash supported
   `-h|--help`; TS port had no help path. Added emitHelp + early
   return from parseArgs.

4. **.lake exclusion (P2, doc-comment-history-audit.ts)**: SCAN_ROOTS
   includes `tools/`, which under the lean4/Mathlib substrate pulls
   in the heavy .lake build-output tree. Added `.lake` to
   SCAN_EXCLUDE_SEGMENTS — both correctness (mathlib wouldn't dominate
   findings) and perf (directory walk speeds up substantially).

5. **# comment clarification (P2, doc-comment-history-audit.ts)**:
   added inline comment near COMMENT_HASH_RE explaining that `#` in
   F#/C# is preprocessor/module directives, not strictly comments,
   but the scanner treats them as in-scope on the principle that
   any `#`-prefixed line carries the same factory-process-token
   risk class.

6. **Task #350 disambiguation (slice-audits.md)**: "open backlog
   #350" was ambiguous with PR #350. Renamed to "Task #350" since
   #350 is a TaskCreate task ID, not a PR.

Verified: all 3 .ts files lint-clean; bash-vs-TS equivalence holds
across all original modes; new --help and --fail-over arg validation
behave correctly.
```

### `4425c5b40b3a138fe94509fe476857f450efc1ff` -- 2026-04-30T03:26:06Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/lint/safety-clause-audit.ts`

**Message:**

```
review(slice-7): apply --list-missing zero-bytes-on-empty behaviour-f…

…ix per PR #878 thread

After verification that bash's `printf '%s\n' "${arr[@]:-}"`
actually emits a single newline on empty (the :- expansion produces
"" then printf adds \n), reconsidered: the bash blank line is
itself a UX wart. Same behaviour-improvement-over-bash category as
the no-empty-dirs.ts empty-FILTERED case documented in slice-7
audit. Reviewer's framing about bash behaviour was wrong but the
desired behaviour (zero bytes for piping) is right; both bash and
TS could be said to have this minor wart, and the TS port can fix
it.

Inline comment now documents both:
- the empirical bash behaviour (1 newline)
- the intentional TS behaviour-improvement (0 bytes)
- the reviewer link + slice-7 audit cross-reference
```

### `9aa60db3c42d891a722151110e1feef583491fdf` -- 2026-04-30T03:33:39Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `docs/trajectories/typescript-bun-migration/RESUME.md`
- `tools/lint/doc-comment-history-audit.ts`
- `tools/lint/no-empty-dirs.ts`
- `tools/lint/safety-clause-audit.ts`

**Message:**

```
review(slice-7): address PR #878 round 2 — Windows path separators + …

…exit-code docs + RESUME merge-stability

Six new threads from the post-fix review round:

1+2+3. **Windows path separators (no-empty-dirs.ts + doc-comment-
   history-audit.ts)**: Node-style path.relative() returns
   backslash-separated paths on Windows; the bash original emits
   forward-slash paths and the allowlist + git-check-ignore +
   baseline-diff comparisons assume forward slashes. Added
   toPosixRel() helper (single-line replace(/\\\\/g, "/")) and
   apply it to every relative() result before passing to
   isHardExcluded / shouldExcludeDir / output / baseline diff.
   Three reviewer threads consolidated into one fix per file.

4. **safety-clause-audit --fail-over usage error stricter than
   bash**: bash printed "integer expression expected" and exited 0
   from the failed [ test; TS port now exits 1 with explicit
   message. Documented as intentional behaviour-improvement-over-
   bash in the header comment + class-aligned with the empty-
   FILTERED case.

5. **safety-clause-audit exit-code documentation**: header now
   documents that exit 1 covers BOTH the --fail-over threshold
   case AND usage errors, with a brief note about the bash-
   behaviour delta.

6. **RESUME.md merge-stability**: replaced "3 in-flight on slice-7
   branch" with "3 from PR #878"; added a sentence noting the row
   is merge-stable (count + PR-list won't go stale post-merge;
   subsequent slices append rows rather than edit this one).
```
