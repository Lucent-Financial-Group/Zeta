# PR #857 -- fix(codeql): per-language source-presence gate — Amara Option B structural fix

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 857 |
| Title | fix(codeql): per-language source-presence gate — Amara Option B structural fix |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-04-29T20:06:05Z |
| Merged at | 2026-04-29T20:39:50Z |
| Merge commit SHA | `f357019f7243635299c4a08231a18d392dcae6cc` |
| Branch | `fix/codeql-no-source-baseline-2026-04-29` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/857 |
| Changed files | 1 |
| Additions / deletions | +199 / -3 |

## Description

## Summary

Structural fix for the *\"no source code seen during build\"* CodeQL extractor failure
in the `analyze` matrix when a language is configured but has zero first-party source
files at the current commit.

**Caught by**: PR #849 (TS hygiene-port retiring the last `*.py` tools) — `Analyze
(python)` exits 32 in CodeQL's `database finalize` step because no Python source
remained for the extractor to process. The `code_quality severity=all` repo ruleset
sees per-language FAILURE and blocks merges.

**Per Amara 2026-04-29 review of #849**: *\"a retired language must not become a
permanent PR gate; structure should self-heal.\"* Aaron 2026-04-29: *\"we are going
to have AI / ML scripts in the future with python so we want good support.\"*

## Diagnostic

- **Owner**: Advanced Setup ONLY. `gh api repos/.../code-scanning/default-setup` →
  `state: \"not-configured\"`. No multi-master.
- **Branch protection required checks**: `Analyze (python)` is NOT listed — only
  the lint + build-and-test legs are required-at-branch-protection. The block
  comes from the `code_quality severity=all` repo ruleset, which gates on the
  CodeQL aggregate / per-language status.
- **Existing path-gate** already emits empty SARIF for all 5 languages when
  no code paths changed at all (docs-PR case). What's missing is the
  per-language gate when *some* code changed but not in this language.

## Approach (Amara Option B)

Each `analyze` matrix leg starts unconditionally (workflow-level
`paths-ignore` would leave required checks PENDING per [GitHub docs](https://docs.github.com/en/actions/using-workflows/triggering-a-workflow#example-using-paths-and-paths-ignore)),
runs a per-language source-presence check that mirrors `codeql-config.yml`'s
`paths-ignore` shape, and either:

- runs the full extractor pipeline (CodeQL init → build → analyze) when
  first-party source exists, OR
- uploads a minimal no-findings SARIF under the matching language category
  and exits SUCCESS when no source exists.

`actions` and `csharp` keep the unconditional path — first-party always; gating
them would mask config drift that should surface as failure. `java-kotlin`,
`javascript-typescript`, and `python` get the same gated pattern for symmetry.

**Self-healing**: when Python returns for AI/ML scripts, full analysis
re-enables automatically without workflow edits.

## Files

- `.github/workflows/codeql.yml` — +156 / -3 lines

## Test plan

- [x] `actionlint` clean locally
- [x] `shellcheck` SC2126 warning resolved (use `grep -cv` not `grep -v | wc -l`)
- [x] `set -euo pipefail` + grep-no-match safety: `|| true` recovers when zero
  lines selected (the language-fully-retired case we want to detect)
- [ ] CI: per-language SUCCESS for python on this PR (no python source change
  in this PR; java-kotlin should stay green via existing AlloyRunner.java)
- [ ] After merge: PR #849 re-run shows `Analyze (python)` SUCCESS via the
  no-source baseline path

## References

- [No source code was seen during the build — GitHub Docs](https://docs.github.com/en/code-security/code-scanning/troubleshooting-code-scanning/no-source-code-seen-during-build)
- [Customizing your advanced setup for code scanning — GitHub Docs](https://docs.github.com/en/code-security/code-scanning/creating-an-advanced-setup-for-code-scanning/customizing-your-advanced-setup-for-code-scanning)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | true |
| Total threads | 9 |
| Resolved threads | 9 |
| Unresolved threads | 0 |
| Total review comments | 9 |
| Total fix commits (touching thread paths) | 5 |

## Review threads

### Thread 1 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5-jh27`
- **Path**: `.github/workflows/codeql.yml`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-04-29T20:07:47Z on `.github/workflows/codeql.yml`:572 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Skip no-source SARIF upload on fork pull requests**

  This new no-source path uploads SARIF with `github/codeql-action/upload-sarif` whenever `has_source` is false, but unlike the existing path-gate baseline uploads it is not guarded for fork PRs. In forked `pull_request` runs, `GITHUB_TOKEN` is read-only for `security-events`, so this upload can fail with `Resource not accessible by integration`, causing the `Analyze (python)`/`Analyze (javascript-typescript)` leg to fail exactly in the retired-language scenario this change is meant to unblock. Add the same fork-PR guard used in `path-gate` (or another safe fallback) to this upload step.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 2 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5-jmwV`
- **Path**: `.github/workflows/codeql.yml`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-29T20:12:41Z on `.github/workflows/codeql.yml`:454 (association: NONE)

  <pre>
  P1: Named-attribution (“per Amara …”) in workflow comments conflicts with the repo’s role-ref convention for current-state surfaces. Please replace with a role-ref (e.g., “per reviewer feedback 2026-04-29”) and keep names confined to the allowed history/research surfaces.
  ```suggestion
        # Why this approach (per reviewer feedback 2026-04-29 on #849):
  ```
  </pre>

### Thread 3 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5-jmw_`
- **Path**: `.github/workflows/codeql.yml`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-29T20:12:41Z on `.github/workflows/codeql.yml`:494 (association: NONE)

  <pre>
  P0: The “first-party source” heuristic is documented as mirroring .github/codeql/codeql-config.yml `paths-ignore`, but this python filter doesn’t exclude several ignored trees (bench/**, tools/tla/**, **/obj/**, **/bin/**) and also excludes all of references/ (config only ignores references/upstreams/**). If the only *.py files present are under ignored paths, this gate will incorrectly run CodeQL and the extractor can still hit “no source code seen during build”. Align the exclusions with the actual CodeQL config (or update the config to match this intent).
  ```suggestion
                # tools/lean4/** (mathlib pkg), bench/**,
                # tools/tla/**, references/upstreams/** (vendored),
                # and generated **/obj/** and **/bin/** trees.
                count=$(git ls-files -- '*.py' \
                  | grep -cvE '^(tools/lean4/|bench/|tools/tla/|references/upstreams/)|(^|.*/)(obj|bin)/' \
  ```
  </pre>

### Thread 4 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5-jmxR`
- **Path**: `.github/workflows/codeql.yml`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-29T20:12:42Z on `.github/workflows/codeql.yml`:505 (association: NONE)

  <pre>
  P0: Same as the python case: this JS/TS source-presence check doesn’t mirror the CodeQL `paths-ignore` config (bench/**, tools/tla/**, tools/lean4/**, **/obj/**, **/bin/**, etc.). If the repo ever has only ignored JS/TS files, this gate will mis-detect “has_source=true” and CodeQL can still fail during DB finalize due to zero scannable sources. Consider centralizing the ignored-path regex (derived from .github/codeql/codeql-config.yml) and reusing it across languages.
  ```suggestion
            # Keep this in sync with .github/codeql/codeql-config.yml
            # paths-ignore so the source-presence gate cannot report
            # has_source=true for files CodeQL will skip anyway.
            shared_ignored_paths_regex='(^bench/|^tools/tla/|^tools/lean4/|^references/upstreams/|^references/|^node_modules/|(^|/)(obj|bin)/)'
            case "${{ matrix.language }}" in
              python)
                count=$(git ls-files -- '*.py' \
                  | grep -cvE "${shared_ignored_paths_regex}" \
                  || true)
                ;;
              java-kotlin)
                # tools/alloy/AlloyRunner.java is first-party Java.
                count=$(git ls-files -- '*.java' '*.kt' '*.kts' \
                  | grep -cvE "${shared_ignored_paths_regex}" \
                  || true)
                ;;
              javascript-typescript)
                count=$(git ls-files -- '*.js' '*.jsx' '*.ts' '*.tsx' '*.mjs' '*.cjs' \
                  | grep -cvE "${shared_ignored_paths_regex}" \
  ```
  </pre>

### Thread 5 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5-jmxx`
- **Path**: `.github/workflows/codeql.yml`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-29T20:12:42Z on `.github/workflows/codeql.yml`:602 (association: NONE)

  <pre>
  P0: The no-source baseline path uploads SARIF via github/codeql-action/upload-sarif, but unlike the earlier path-gate baseline it is not guarded for fork PRs. This file already notes that synthetic SARIF uploads 403 on fork PRs due to downgraded GITHUB_TOKEN permissions; this would make Analyze(language) fail for forks when has_source=false. Add an explicit fork-PR guard (via needs.path-gate.outputs.is_fork_pr) and decide on a fork-safe fallback behavior for the no-source case.
  </pre>

### Thread 6 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5-jmyD`
- **Path**: `.github/workflows/codeql.yml`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-29T20:12:42Z on `.github/workflows/codeql.yml`:466 (association: NONE)

  <pre>
  P2: This comment says the SARIF baseline is keyed by `automationDetails.id` matching the analyze category, but the workflow relies on the upload step’s `category:` input to override automationDetails (and the SARIF id value includes a trailing “/” while the category input does not). Please reword to avoid implying the in-file automationDetails value is the key.
  ```suggestion
        # ruleset rule sees a real per-language status; the upload
        # step uses the same `category` as the analyze path, so
        # GitHub's SARIF replacement logic records it as the clean
        # baseline for that language.
  ```
  </pre>

### Thread 7 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5-jmyd`
- **Path**: `.github/workflows/codeql.yml`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-29T20:12:43Z on `.github/workflows/codeql.yml`:86 (association: NONE)

  <pre>
  P1: This comment introduces named-attribution (“per Amara …”) in a current-state surface (.github/workflows). Repo convention is to use role-refs here (e.g., “per reviewer feedback 2026-04-29” / “per maintainer review”) rather than persona/human names; please rewrite accordingly (see docs/AGENT-BEST-PRACTICES.md BP-24 ‘No name attribution…’).
  ```suggestion
  #      checks PENDING (per reviewer feedback 2026-04-29 on #849:
  ```
  </pre>

### Thread 8 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5-joTv`
- **Path**: `.github/workflows/codeql.yml`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-04-29T20:14:13Z on `.github/workflows/codeql.yml`:516 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Align source detection with CodeQL ignore paths**

  Update the new `Detect first-party source` filter to include all directories ignored by `.github/codeql/codeql-config.yml` (not just `references/**` and `node_modules/**`). Right now this pattern can mark `has_source=true` when a language only has files under globally ignored paths such as `bench/**` or `tools/tla/**`, which causes the workflow to skip the no-source baseline and run `init/analyze` against an effectively empty source set; that reintroduces the same `no source code seen during build` failure this commit is meant to prevent.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 9 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5-jxej`
- **Path**: `.github/workflows/codeql.yml`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-04-29T20:22:48Z on `.github/workflows/codeql.yml`:525 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Include .mts/.cts when detecting TypeScript source**

  Update the `javascript-typescript` branch of `Detect first-party source` to count `.mts` and `.cts` files as source. CodeQL's TypeScript support includes these extensions, but the current glob list only checks `.ts/.tsx` (plus JS variants). In repositories that use ESM/CJS TypeScript entrypoints exclusively via `.mts/.cts`, this step will set `has_source=false`, skip `init/analyze`, and upload an empty baseline SARIF instead of running real analysis, silently dropping JS/TS scanning coverage.

  Useful? React with 👍 / 👎.
  </pre>

## Fix commits (touching thread paths)

### `c916ca5a4f00f17fdbcd2bc5ac9438f30cab2a94` -- 2026-04-29T20:05:40Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `.github/workflows/codeql.yml`

**Message:**

```
fix(codeql): per-language source-presence gate (no-source baseline up…

…load)

Structural fix for the "no source code seen during build" CodeQL extractor
failure in the analyze matrix when a language is in the matrix but has zero
first-party source files at this commit. Caught when PR #849 retired the
last `*.py` files (Python tools ported to TypeScript on Bun) and `Analyze
(python)` started exiting 32 in `database finalize` because no Python
source remained for the extractor to process.

Per Amara 2026-04-29 review: a retired language must not become a permanent
PR gate. The structure should self-heal — when Python returns (Aaron
confirmed AI/ML scripts will land in Python), full analysis re-enables
automatically without workflow edits. workflow-level paths-ignore would
leave required checks PENDING and block PRs differently; matrix-removal
would lose future re-enablement.

Approach: each `analyze` matrix leg starts unconditionally, runs a
per-language source-presence check (mirroring codeql-config.yml's
paths-ignore) and either:

- runs the full extractor pipeline (CodeQL init → build → analyze) when
  first-party source exists, OR
- uploads a minimal no-findings SARIF under the matching language category
  and exits SUCCESS when no source exists.

`actions` and `csharp` keep the unconditional path (first-party always —
gating them would mask config drift). `java-kotlin` and
`javascript-typescript` get the same gated pattern as `python` for symmetry.

Composes with the path-gate's existing unconditional empty-SARIF emit:
path-gate covers the "no code paths changed at all" docs-PR case; the
matrix-level gate covers the "some code changed but not in this language"
case.

Diagnostic confirmation:
- Default Setup: not-configured (Advanced Setup owns CodeQL exclusively;
  no multi-master).
- Branch protection required checks: do NOT include `Analyze (X)` per-
  language entries (only lint + build-and-test legs are required-at-
  branch-protection level).
- Real blocker: `code_quality severity=all` repo ruleset, which gates on
  the CodeQL aggregate. Per-language SUCCESS satisfies it; per-language
  FAILURE blocks.

References:
- https://docs.github.com/en/code-security/code-scanning/troubleshooting-code-scanning/no-source-code-seen-during-build
- https://docs.github.com/en/code-security/code-scanning/creating-an-advanced-setup-for-code-scanning/customizing-your-advanced-setup-for-code-scanning

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
```

### `27f2a389891edce9ba23dd02701d4b2ea247666a` -- 2026-04-29T20:11:08Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `.github/workflows/codeql.yml`

**Message:**

```
fix(codeql): guard no-source SARIF upload on fork PRs

PR #857 review thread (Codex P1): the new no-source path uploads SARIF
with `github/codeql-action/upload-sarif` whenever `has_source` is false,
but unlike `path-gate`'s baseline emit/upload steps it was not guarded
against fork PRs. On forked `pull_request` runs the GITHUB_TOKEN is
read-only for `security-events`, so the upload would 403 with
"Resource not accessible by integration" — failing the exact
retired-language case this gate is meant to unblock.

Fix: gate both the SARIF emit and upload steps on
`needs.path-gate.outputs.is_fork_pr != 'true'`, mirroring path-gate's
existing pattern. On fork PR + no-source, the subsequent init/build/
analyze steps are also skipped (gated on `has_source == 'true'`), so
the job exits SUCCESS with no SARIF emitted — same posture as
path-gate takes on fork PRs.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
```

### `07d6af6293fd7d0caa3204cea419474804e68075` -- 2026-04-29T20:16:18Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `.github/workflows/codeql.yml`

**Message:**

```
fix(codeql): align source-presence regex with codeql-config + drop na…

…me-attribution

PR #857 review threads from Copilot:

P0 (L494, L505): the per-language source-presence heuristic claimed to
mirror .github/codeql/codeql-config.yml `paths-ignore` but diverged in
both directions:
  - over-excluded: ALL of `references/` (config only excludes
    `references/upstreams/**`)
  - under-excluded: missing `bench/**`, `tools/tla/**`, `**/obj/**`,
    `**/bin/**` from the config

Net effect: a python/js-ts file living only under `bench/` or `tools/tla/`
would be detected as has_source=true, real CodeQL extraction would run,
and the extractor would skip those files (paths-ignore wins) → "no source
code seen during build" → exit 32 → the exact failure mode this gate is
meant to prevent.

Fix: centralise the exclusion regex in `shared_ignored_paths_regex` and
match codeql-config.yml exactly: `references/upstreams/`, `bench/`,
`tools/tla/`, `tools/lean4/`, plus `(^|.*/)(obj|bin)/` for the
**/obj/** and **/bin/** patterns. (.generated.cs is csharp-only and
csharp keeps the unconditional path.)

P1 (L86, L454): name-attribution ("per Amara …") in current-state
workflow comments violates the no-name-attribution discipline on code
surfaces (memory/research/round-history surfaces are where persona
names belong; .github/workflows/ is current-state). Reword to
"per reviewer feedback 2026-04-29 on #849" + "per reviewer feedback
2026-04-29". Same pattern applied to "Codex P1 catch" → "review-tool P1
catch" for consistency.

P2 (L466): comment claimed SARIF replacement was keyed by
`automationDetails.id` matching analyze category, but actually the
upload step's `category:` action-param overrides automationDetails and
is what GitHub keys on. Reword.

L575 already addressed by 27f2a38 (fork-PR guard on emit + upload
steps); leaving as-is and resolving the duplicate thread.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
```

### `042ba8113bf6bf465bacdc7d25cb969899f73458` -- 2026-04-29T20:19:28Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `.github/workflows/codeql.yml`

**Message:**

```
chore(codeql): mark workflow-local classifier as bridge, not final ar…

…chitecture

Per reviewer follow-up on PR #857: the workflow-local source-presence regex
is the smallest sufficient unblock for the retired-language case. The future
canonical classifier should pull this logic into tools/ci/classify-changed-
files.* with fixtures so CodeQL, docs-substrate, and code gates share one
path-universe rather than each growing their own.

Add a comment near the regex so future agents don't treat the duplicated
regex as the permanent design. Tracked via task #306 (cadence-fast
classifier work).

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
```

### `64b6bc2fa35c12778cb04ce867fa43e2cbcc00bf` -- 2026-04-29T20:30:23Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `.github/workflows/codeql.yml`

**Message:**

```
fix(codeql): include .mts/.cts in javascript-typescript source-presen…

…ce glob

PR #857 review thread (Codex P2): CodeQL's javascript-typescript
extractor handles `.mts` (ESM TypeScript) and `.cts` (CJS TypeScript)
files alongside the standard .js/.jsx/.ts/.tsx/.mjs/.cjs set, but the
source-presence glob only checked the latter. Repos that use `.mts`/
`.cts` exclusively as entrypoints would set has_source=false, skip
init/analyze, and upload an empty baseline SARIF instead of running real
analysis — silently dropping JS/TS scanning coverage.

Add `.mts`/`.cts` to the glob.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
```
