# PR #757 -- docs(lost-substrate): full Day-1/2/3 corruption-triage substrate continuation (off-trunk; PR #756 squashed Day-0 only)

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 757 |
| Title | docs(lost-substrate): full Day-1/2/3 corruption-triage substrate continuation (off-trunk; PR #756 squashed Day-0 only) |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-04-29T03:29:26Z |
| Merged at | 2026-04-29T03:49:26Z |
| Merge commit SHA | `b8b9c33710bcf1bee2d0230ed204971ac4d63d5d` |
| Branch | `corruption-triage/full-substrate-onto-main-2026-04-29` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/757 |
| Changed files | 10 |
| Additions / deletions | +1228 / -11 |

## Description

## Summary

PR #756 squash-merged the Day-0 inventory ledger only. Days 1-3
of follow-up substrate (peer-verification corrections, Day-2
inventory results, corruption-triage memory file, three-round
classification refinement, persisted fsck artifacts) were
committed to the post-merge feature branch and never reached
main. This PR ports the full Day-1/2/3 chain forward off main.

Five commits, all cherry-picked from the post-merge feature
branch onto a fresh branch off origin/main:

- **Day 1** — peer-verification corrections per Aaron + Amara
  feedback (`64bc60f`)
- **Day 2** — inventory results (1,109 dangling objects, pack
  corruption, 8 stashes, 293 rerere records) (`c968a59`)
- **Day 2** — corruption triage results + durable memory file
  on the corruption-triage discipline (`e4896e8`)
- **Day 2+** — initial 8d5e67fd follow-up (later overturned by
  empirical re-verification) (`c4a6aed`)
- **Day 3** — three-bucket reachability + content-equivalence +
  exclusive-lane corrections per Amara's three correction
  packets (`5bca4a8`)

## Three-round classification on `8d5e67fd`

Each round overturned the prior:

| Round | Classification | Why wrong / right |
|---|---|---|
| 1 | `MISSING_UNRECOVERED` | Wrong scope — only checked origin recovery; missed local reachability |
| 2 | `REFERENCED_BY_DANGLING_ONLY` | Wrong sample — sampled 10 of 888 commits + fsck adjacency only; missed live-ref reach |
| 3 (correct) | `CORRUPT_BLOB_REFERENCED_BY_LIVE_LOCAL_BRANCH_AND_STALE_REMOTE_TRACKING_REF` | Three-bucket reachability scan + content-equivalence verification: branch tip clean; corrupt blob is intermediate-history only; stale remote-tracking ref preserves evidence; origin no longer has the branch |

## Key keepers in the discipline memory file

- A corrupt object is not a backlog item. It is a substrate health incident.
- Do not prune the evidence while investigating lost evidence.
- Recoverable-from-origin requires fresh-clone cat-file type + size verification.
- Cleanup is a destructive decision, not a repair step.
- Stale remote-tracking ref is evidence, not origin recovery.
- Live local branch reference means the incident is not closed.
- Corruption lane means exclusive lane. Exclusive lane means no side quests.
- Reachability is mode-dependent.
- Verify squash-preservation by content, not by ancestry vibe.

## Persisted fsck artifacts

All under `docs/lost-substrate/artifacts/2026-04-29-corruption/`:

- `fsck-full.txt` — reflog-inclusive fsck output
- `fsck-full-no-reflogs.txt` — `--no-reflogs` fsck output (mode-dependent reachability evidence)
- `fsck-connectivity.txt` — connectivity-only fsck output
- `rev-list-all-objects.txt` — all-objects rev-list (1.1 MB; 18,150 objects)
- `gc-config-snapshot.txt` — gc.auto / gc.autoPackLimit / gc.pruneExpire / reflog-expire / worktreePruneExpire snapshot
- `hour-05Z-show-ref.txt` — local + stale remote-tracking refs both pointing at d9feb3f
- `hour-05Z-ls-remote.txt` — empty (origin no longer has the branch)
- `triage-report.md` — narrative artifact tying the raw outputs to the prose conclusions

## Test plan

- [ ] All gate / lint checks pass
- [ ] markdownlint clean
- [ ] §33 archive-header lint passes (ledger has it; memory file is a memory file, not an archive-import)

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | true |
| Total threads | 18 |
| Resolved threads | 16 |
| Unresolved threads | 2 |
| Total review comments | 18 |
| Total fix commits (touching thread paths) | 9 |

## Review threads

### Thread 1 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5-UVrZ`
- **Path**: `docs/lost-substrate/inventory-ledger-2026-04-29.md`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-04-29T03:31:39Z on `docs/lost-substrate/inventory-ledger-2026-04-29.md`:534 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Remove mutating fsck command from read-only pass**

  This section labels the checklist as "read-only," but it includes `git fsck --lost-found`, which writes dangling objects into `.git/lost-found` (`git fsck -h` explicitly documents `--lost-found` as a write). Running this command during corruption triage mutates repository state and can change or contaminate the very evidence the ledger is trying to preserve, so the command should be replaced with a non-writing alternative in this read-only block.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 2 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5-UVra`
- **Path**: `memory/MEMORY.md`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-04-29T03:31:39Z on `memory/MEMORY.md`:5 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Correct memory index to final corruption classification**

  The MEMORY index entry still labels `8d5e67fd` as `MISSING_UNRECOVERED`, but this same commit’s detailed triage record says that was an overturned Round-1 result and the final classification is live-local-ref reachability with a stale remote-tracking ref. Leaving the outdated label at the index level creates a high-risk summary mismatch that can cause future triage to treat the object as irrecoverable loss instead of a locally reachable corruption case.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 3 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5-UWFE`
- **Path**: `docs/lost-substrate/inventory-ledger-2026-04-29.md`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-29T03:32:36Z on `docs/lost-substrate/inventory-ledger-2026-04-29.md`:354 (association: NONE)

  <pre>
  This table still classifies `8d5e67fd` as **MISSING_UNRECOVERED** and describes the investigation as pending, but later in the same ledger the “corrected classification” section concludes it’s bucket A and refines it to `CORRUPT_BLOB_REFERENCED_BY_LIVE_LOCAL_BRANCH_AND_STALE_REMOTE_TRACKING_REF`. Please reconcile these sections (e.g., mark this table row as superseded/round-1 result, or update it to the final classification) so the document has a single current classification.
  ```suggestion
  | `8d5e67fd313573855848705e4af114f3ff0eecbc` | blob | 439,327 bytes (early `docs/hygiene-history/loop-tick-history.md`) | **CORRUPT_BLOB_REFERENCED_BY_LIVE_LOCAL_BRANCH_AND_STALE_REMOTE_TRACKING_REF** | Not recoverable from origin by blob lookup, but later corrected reachability scan places it in bucket A: referenced by a live local branch and a stale remote-tracking ref. |

  **Critical finding**: `8d5e67fd` is not an unrecovered dangling-only blob. The corrected three-bucket reachability scan classifies it as bucket A and refines it to `CORRUPT_BLOB_REFERENCED_BY_LIVE_LOCAL_BRANCH_AND_STALE_REMOTE_TRACKING_REF`, so the actionable issue is live-ref corruption rather than pending investigation into a missing local-only object.
  ```
  </pre>

### Thread 4 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5-UWFK`
- **Path**: `docs/lost-substrate/inventory-ledger-2026-04-29.md`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-29T03:32:37Z on `docs/lost-substrate/inventory-ledger-2026-04-29.md`:3 (association: NONE)

  <pre>
  `docs/lost-substrate/` is a current-state docs surface (not one of the closed-list history/research surfaces), but this ledger embeds named attribution (“Aaron”, “Amara”, etc.). Per the repo’s no-name-attribution convention outside history surfaces, please rewrite these attributions using role-refs (e.g., “the human maintainer”, “the harsh-critic/maintainability-reviewer”, etc.) and keep named attribution confined to the approved history surfaces.
  </pre>

### Thread 5 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5-UWFN`
- **Path**: `memory/feedback_corruption_triage_discipline_object_health_incident_aaron_amara_2026_04_29.md`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-29T03:32:37Z on `memory/feedback_corruption_triage_discipline_object_health_incident_aaron_amara_2026_04_29.md`:110 (association: NONE)

  <pre>
  The sample commands use `grep` options after the file operand (e.g., `grep "$OBJ" fsck-full.txt -C 5`). This is not portable: BSD/macOS `grep` requires options before the pattern/file operands, so this form will error or treat `-C` as a filename. Reorder these examples to place `-C 5` before the pattern and file.
  ```suggestion
  grep -C 5 "$OBJ" fsck-full.txt
  grep -C 5 "$OBJ" fsck-full-no-reflogs.txt
  ```
  </pre>

### Thread 6 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5-UWFQ`
- **Path**: `docs/lost-substrate/inventory-ledger-2026-04-29.md`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-29T03:32:37Z on `docs/lost-substrate/inventory-ledger-2026-04-29.md`:383 (association: NONE)

  <pre>
  These pipeline examples use `grep 8d5e67fd -C 5` (options after the pattern). This will fail on BSD/macOS `grep` where options must come before the pattern. Please reorder to `grep -C 5 8d5e67fd` (and similarly for other `grep` invocations) so the documented commands are actually runnable on macOS.
  ```suggestion
  git fsck --full --no-progress | grep -C 5 8d5e67fd
  git fsck --full --no-reflogs --no-progress | grep -C 5 8d5e67fd
  ```
  </pre>

### Thread 7 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5-UWFa`
- **Path**: `docs/lost-substrate/inventory-ledger-2026-04-29.md`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-29T03:32:37Z on `docs/lost-substrate/inventory-ledger-2026-04-29.md`:534 (association: NONE)

  <pre>
  This section labels the next passes as “read-only”, but includes `git fsck --lost-found ...`, which *writes* into `.git/lost-found/` (and earlier in the triage discipline it’s explicitly called out as not read-only). Please remove `--lost-found` from the read-only list or clearly mark it as a write/destructive action that is forbidden during triage.
  ```suggestion
  # Keep fsck read-only during triage; do not use --lost-found here because it writes into .git/lost-found/
  git fsck --no-reflogs --no-progress
  ```
  </pre>

### Thread 8 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5-UWFm`
- **Path**: `memory/MEMORY.md`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-29T03:32:37Z on `memory/MEMORY.md`:5 (association: NONE)

  <pre>
  The MEMORY index entry summarizes `8d5e67fd` as `MISSING_UNRECOVERED`, but the linked memory file’s worked example ends with the corrected classification (`CORRUPT_BLOB_REFERENCED_BY_LIVE_LOCAL_BRANCH_AND_STALE_REMOTE_TRACKING_REF`) and explains that `MISSING_UNRECOVERED` was only the Round-1 misclassification. Update this index summary to reflect the final (Round-3) classification to avoid future readers taking the superseded label as current truth.
  ```suggestion
  - [**Corruption triage is a substrate health incident, not a backlog item (Aaron + Amara, 2026-04-29)**](feedback_corruption_triage_discipline_object_health_incident_aaron_amara_2026_04_29.md) — When `git fsck` reports corrupt objects, lane narrows hard: stop all background work, do read-only diagnosis first (no `git fsck --lost-found` — it writes), per-object classification, fresh-clone verification BEFORE declaring "origin has it." Worked example: 2026-04-29 Day-2 audit found 2 corrupt objects on this repo — 9bf2daee (16MB blob, RECOVERABLE_FROM_ORIGIN) + 8d5e67fd (439KB blob, CORRUPT_BLOB_REFERENCED_BY_LIVE_LOCAL_BRANCH_AND_STALE_REMOTE_TRACKING_REF — local-only intermediate version of tick-history.md not on origin). Bucket schema + repair plan in body. Aaron explicit: *"future self remembers this, this is very important."*
  ```
  </pre>

### Thread 9 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5-UYyo`
- **Path**: `docs/lost-substrate/inventory-ledger-2026-04-29.md`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-04-29T03:39:16Z on `docs/lost-substrate/inventory-ledger-2026-04-29.md`:95 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Align merged-branch guidance with candidate-only policy**

  This change reclassifies merged branches as `BRANCH_MERGED_TO_MAIN_CANDIDATE` (not auto-clear) and keys the check to `origin/main`, but the same ledger still instructs `git branch --merged main` and calls those results deletion-safe in the “next safe actions” flow. In this repo’s stated diverged state (145 ahead / 562 behind), that inconsistency can cause premature destructive branch cleanup from a stale local `main`, which contradicts the new candidate+peer-verification safety model.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 10 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5-UYyq`
- **Path**: `docs/lost-substrate/artifacts/2026-04-29-corruption/triage-report.md`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-04-29T03:39:16Z on `docs/lost-substrate/artifacts/2026-04-29-corruption/triage-report.md`:196 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Reference the committed gc config artifact name**

  The cleanup posture cites `gc-config-snapshot.txt`, but this commit only adds `git-gc-config.txt`; as written, the reader cannot resolve the evidence file used to justify the auto-gc claim. That breaks traceability for the triage conclusion and makes re-verification ambiguous until the filename is corrected consistently.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 11 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5-UZpF`
- **Path**: `memory/feedback_repo_is_soulfile_dont_commit_raw_diagnostic_dumps_aaron_amara_2026_04_29.md`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-29T03:41:22Z on `memory/feedback_repo_is_soulfile_dont_commit_raw_diagnostic_dumps_aaron_amara_2026_04_29.md`:67 (association: NONE)

  <pre>
  P1: The `find ... -printf '%s %p\n'` invocation is GNU-find specific (`-printf` isn’t supported by BSD/macOS `find`), so the size-check recipe won’t run on default macOS installs. Consider switching this to a portable approach (e.g., `du`-based, or `find ... -exec` with `wc -c`/`stat`), or document the GNU-find requirement explicitly.
  ```suggestion
  for file in docs/lost-substrate/artifacts/2026-04-29-corruption/*; do
    [ -f "$file" ] || continue
    printf '%s %s\n' "$(wc -c &lt; "$file")" "$file"
  done | sort -nr
  ```
  </pre>

### Thread 12 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5-UZpM`
- **Path**: `memory/feedback_corruption_triage_discipline_object_health_incident_aaron_amara_2026_04_29.md`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-29T03:41:23Z on `memory/feedback_corruption_triage_discipline_object_health_incident_aaron_amara_2026_04_29.md`:103 (association: NONE)

  <pre>
  P1: This procedure currently tells readers to write and “persist outputs” like full `rev-list-all-objects.txt` / full fsck outputs under `docs/lost-substrate/artifacts/&lt;date&gt;-corruption/`. That conflicts with the adjacent new “soulfile” rule in this PR (extract+recipe; don’t commit multi‑MB raw dumps). Suggest updating this section to write raw outputs to a temp dir (e.g., under `/tmp/…`) and only commit grep-extracts + re-run recipe into `docs/lost-substrate/artifacts/…`.
  </pre>

### Thread 13 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5-UZpR`
- **Path**: `docs/lost-substrate/artifacts/2026-04-29-corruption/triage-report.md`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-29T03:41:23Z on `docs/lost-substrate/artifacts/2026-04-29-corruption/triage-report.md`:24 (association: NONE)

  <pre>
  P1: The artifact table references `hour-05Z-ls-remote.txt` as evidence that `git ls-remote` returned empty, but the committed file is currently 0 lines. Consider adding a short header/provenance line (similar to the other extract files) so the empty result is self-describing when viewed on its own.
  ```suggestion
  | `hour-05Z-ls-remote.txt` | `git ls-remote --heads origin hour-05Z`; the committed sibling file is currently a zero-byte placeholder because the command returned no refs, so this table row provides the provenance/context for that empty result |

  All files are point-in-time evidence; do not edit.
  The one presentation caveat is `hour-05Z-ls-remote.txt`: because the command returned empty output, the committed artifact is presently 0 bytes and is not self-describing when opened on its own; use the table row above as the provenance for that empty result.
  ```
  </pre>

### Thread 14 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5-UZpU`
- **Path**: `docs/lost-substrate/artifacts/2026-04-29-corruption/triage-report.md`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-29T03:41:23Z on `docs/lost-substrate/artifacts/2026-04-29-corruption/triage-report.md`:82 (association: NONE)

  <pre>
  P2: The “remote-tracking ref is stale” section uses `$ fresh-clone --branch …`, which isn’t a standard git command and doesn’t match the earlier re-run recipes. For reproducibility, replace this with the exact `git clone ... --branch ...` (or explicitly label it as pseudocode and point to the real command that produced the output).
  ```suggestion
  $ git clone --branch chore/heartbeat-batch-2026-04-26-hour-05Z origin fresh-clone
  ```
  </pre>

### Thread 15 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5-UZpY`
- **Path**: `docs/lost-substrate/artifacts/2026-04-29-corruption/triage-report.md`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-29T03:41:24Z on `docs/lost-substrate/artifacts/2026-04-29-corruption/triage-report.md`:196 (association: NONE)

  <pre>
  P1: This section references `gc-config-snapshot.txt`, but the committed artifact file in this directory is `git-gc-config.txt`. Please align the filename (either rename the artifact file or update these references) so readers can follow the evidence trail without guessing.
  ```suggestion
  (per `git-gc-config.txt`); below-threshold loose-object count
  ```
  </pre>

### Thread 16 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5-UZpg`
- **Path**: `docs/lost-substrate/inventory-ledger-2026-04-29.md`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-29T03:41:24Z on `docs/lost-substrate/inventory-ledger-2026-04-29.md`:482 (association: NONE)

  <pre>
  P1: This references `gc-config-snapshot.txt`, but the artifacts added in this PR use `docs/lost-substrate/artifacts/2026-04-29-corruption/git-gc-config.txt`. Please align the referenced filename with the committed artifact (or rename the artifact) so the ledger’s evidence links resolve.
  ```suggestion
  `docs/lost-substrate/artifacts/2026-04-29-corruption/git-gc-config.txt`;
  below-threshold loose-object count means auto-gc is not imminent —
  the discipline is "no implicit cleanup decision" not "we have time."
  ```
  </pre>

### Thread 17 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM5-Ueg3`
- **Path**: `docs/lost-substrate/artifacts/2026-04-29-corruption/triage-report.md`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-04-29T03:52:41Z on `docs/lost-substrate/artifacts/2026-04-29-corruption/triage-report.md`:22 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Query exact ref when proving remote branch absence**

  The stale-ref conclusion is keyed to `hour-05Z-ls-remote.txt`, but this source-command row uses `git ls-remote --heads origin hour-05Z`, which is a pattern query, not an exact ref lookup. `git ls-remote` patterns are glob-matched against ref tails, so this can match multiple branches that end with `hour-05Z`; a non-empty result would not prove the specific `chore/heartbeat-batch-2026-04-26-hour-05Z` branch still exists. For corruption triage, that ambiguity can misclassify a remote-tracking ref as current vs stale.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 18 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM5-Ueg5`
- **Path**: `docs/lost-substrate/inventory-ledger-2026-04-29.md`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-04-29T03:52:41Z on `docs/lost-substrate/inventory-ledger-2026-04-29.md`:552 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Include merge markers in worktree transaction scan**

  This check only inspects `REBASE_HEAD`, `AUTO_MERGE`, and `ORIG_HEAD` under `.git/worktrees/*`, but the report later labels merge state as clean. A merge-in-progress is typically indicated by `MERGE_HEAD`, and the active/main worktree state lives under `.git/` (not only `.git/worktrees/*`), so this probe can miss real in-progress operations and produce a false "clean" classification before cleanup decisions.

  Useful? React with 👍 / 👎.
  </pre>

## Fix commits (touching thread paths)

### `64bc60f72aba08681da7ee1ce272ccf13e996b17` -- 2026-04-29T02:57:12Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `docs/lost-substrate/inventory-ledger-2026-04-29.md`

**Message:**

```
docs(lost-substrate): peer-verification corrections per Aaron + Amara…

… feedback

Three corrections per Aaron's 2026-04-29 input + Amara synthesis:

1. **"directive" -> "input"/"feedback"** — Aaron caught me using
   directive language again. Replaced in attribution + body.

2. **"definitively deletion-safe" -> deletion candidates** —
   git branch --merged is a mechanical signal, not policy
   clearance. Renamed BRANCH_MERGED_TO_MAIN ->
   BRANCH_MERGED_TO_MAIN_CANDIDATE; added explicit note that
   deletion is gated on open-PR mapping + worktree mapping +
   peer verification.

3. **Peer-verification gaps section added** — Codex + Gemini
   independent reviews found substantial surfaces missed by
   the initial branch/worktree snapshot:

   Gemini (#1):
   - Index/staged-uncommitted across all 58 worktrees
   - git notes
   - git fsck --lost-found orphans
   - .gitignore'd substrate (.env, local DBs, uncommitted
     memory shards, .mise tweaks)
   - .git/config + .git/hooks
   - Submodule state (detached HEADs, unpushed commits)
   - Git LFS objects (commit pointer != asset)
   - GitHub-native (draft PRs, Wiki, Actions vars/secrets,
     branch protection, Project Boards)

   Codex (#2):
   - Rerere cache (.git/rr-cache; 293 records)
   - Per-worktree transaction state (REBASE_HEAD, AUTO_MERGE,
     ORIG_HEAD, FETCH_HEAD, COMMIT_EDITMSG)
   - History-rewriting ref namespaces (refs/replace,
     refs/original, refs/bisect, refs/pull, refs/changes,
     .git/info/grafts)
   - Index flags (assume-unchanged, skip-worktree,
     .git/info/exclude)
   - Patch/mailbox/bundle artifacts (*.patch, *.diff, *.mbox,
     *.eml, *.bundle)
   - Nested upstream mirrors (references/upstreams/* embedded
     submodules)
   - Sibling/factory repos (../claude-code, ../SQLSharp,
     ../runtime)

Plus added:

- Day-2 follow-up: 9 read-only commands for subsequent
  inventory passes
- Evidence-column schema for per-row classification
- Three-layer 0/0/0 verification (commit graph + tree state +
  content-loss surface; ALL must be clean for true 0/0/0)

Best keeper rule preserved verbatim:
"A count is not a clearance. A bucket is not proof. A ledger
row needs evidence."

Aaron's catch was load-bearing: I had over-claimed substrate
coverage based on a branch/worktree snapshot. Peer verification
revealed 15+ missed surface classes. The ledger remains a
prerequisite to recovery + 0/0/0 work, but is now honestly
labeled as not-yet-a-substrate-loss-proof.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
```

### `c968a59d8bb5d6e1e6decef2f973964817eb3163` -- 2026-04-29T03:00:15Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `docs/lost-substrate/inventory-ledger-2026-04-29.md`

**Message:**

```
docs(lost-substrate): Day-2 inventory results — 1,109 dangling object…

…s + pack corruption + 8 stashes + 293 rerere records

Day-2 read-only inventory commands executed per the ledger's
own follow-up section. Real findings:

HIGHEST RISK:
- **1,109 dangling objects** (commits/trees/blobs not reachable
  from any branch). Hard-reset would destroy these. Per-object
  `git show` inspection needed to classify.
- **Pack corruption** in `.git/objects/pack/pack-16732b...pack`:
  data-stream error on object 9bf2daee... Cannot be unpacked.
  May need `git fsck --full` + `git repack` + re-fetch if
  origin has the missing object.
- **8 stashes** (stash@{3} alone is 668 files / 208k
  deletions). Explicit unfinalized intent across 8 different
  branches. Each needs per-stash classification.
- **293 rerere records**. Real factory-discipline substrate
  (codified conflict-resolution knowledge). NOT auto-disposable.

CLEAN:
- History-rewriting refs (replace/original/bisect/pull/changes)
- Index flags (no assume-unchanged / skip-worktree)
- Per-worktree mid-operation state (no REBASE/MERGE/AUTO_MERGE)
- Patch/bundle artifacts (outside references/upstreams)
- No submodules, no LFS, no tags, no git notes

UPDATED HONEST STATEMENT:
This ledger v2 (after peer-verification + Day-2 execution) is
closer to a substrate-loss audit but still NOT complete — the
1,109 dangling objects need per-object classification before
any reset/forward-sync is safe.

The pack-corruption finding is the most-urgent issue: a real
durability-of-history concern that exists independently of
branch/worktree classification.

Per Aaron's input + Amara synthesis: count is not clearance.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
```

### `e4896e848bf545eb850b73f9b0b26cdb2a59b040` -- 2026-04-29T03:08:18Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `docs/lost-substrate/inventory-ledger-2026-04-29.md`
- `memory/MEMORY.md`
- `memory/feedback_corruption_triage_discipline_object_health_incident_aaron_amara_2026_04_29.md`

**Message:**

```
docs+memory: corruption triage results + memory file (Aaron + Amara —…

… substrate health incident, durable for future-Claude)

Aaron 2026-04-29: "you for sure need to make sure your future
self remembers this, this is very important."

Amara synthesis: "A corrupt object is not a backlog item. It
is a substrate health incident."

Three landings:

1. **memory/feedback_corruption_triage_discipline_object_health_incident_aaron_amara_2026_04_29.md**
   New durable memory file capturing the discipline: read-only
   diagnosis first, fresh-clone verification before declaring
   "origin has it", per-object bucket classification, repair
   plan only after classification. Forbids destructive fixes
   during triage (no git gc / git prune / git repack /
   git fsck --lost-found as a "fix" yet).

2. **memory/MEMORY.md** — paired-edit pointer added for the new
   feedback file (gate requirement).

3. **docs/lost-substrate/inventory-ledger-2026-04-29.md**
   Added "Corruption triage results" section with the 2
   corrupt objects classified:
   - 9bf2daee (16MB blob): RECOVERABLE_FROM_ORIGIN; fresh
     clone has it cleanly; recovery via git fetch --refetch.
   - 8d5e67fd (439KB blob, early loop-tick-history.md):
     MISSING_UNRECOVERED; not in origin; local-only
     intermediate version. Canonical worked-example of
     unrecovered-substrate boundary case.

   Plus wording correction: do NOT declare "origin has it"
   without fresh-clone verification.

Per Amara's "stop the scanner; corruption first" priority
shift: the no-directives scanner I had built (tools/lint/
no-directives-scan.sh) was discarded from working tree.
That scanner becomes follow-up work after corruption triage
clears.

The 8d5e67fd unrecovered-blob finding is the durable lesson
this commit is here to preserve for future-Claude.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
```

### `c4a6aed78f07b48073491e6e47c9379d84abc29f` -- 2026-04-29T03:13:14Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `docs/lost-substrate/inventory-ledger-2026-04-29.md`

**Message:**

```
docs(lost-substrate): 8d5e67fd corrupt blob is referenced only from d…

…angling state — recovery urgency LOW

Follow-up to the 2026-04-29 corruption triage: investigated where the
missing blob 8d5e67fd is referenced from. fsck output shows it's
reachable only from a dangling tree (ba5cc035) near a dangling commit
(9d5db21). Live commits on current branches use other tree SHAs for
docs/hygiene-history/loop-tick-history.md (cross-checked 10 of 888
commits over the file's history; none reference 8d5e67fd).

New sub-classification: CORRUPT_LOOSE_OBJECT_REFERENCED_BY_DANGLING_ONLY
(sub-bucket of MISSING_UNRECOVERED).

Recovery urgency: LOW. The corrupt blob is bounded to unreachable
substrate; live history is clean. Hard-reset readiness is NOT blocked
by this corruption finding.

Per the corruption-triage discipline memory file: still no destructive
action during triage. Classification is complete; any future GC
consideration is a separate decision, not part of triage.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
```

### `5bca4a87f0919d4482f182bb780014e93733f879` -- 2026-04-29T03:26:05Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `docs/lost-substrate/artifacts/2026-04-29-corruption/triage-report.md`
- `docs/lost-substrate/inventory-ledger-2026-04-29.md`
- `memory/feedback_corruption_triage_discipline_object_health_incident_aaron_amara_2026_04_29.md`

**Message:**

```
docs(lost-substrate): three-bucket reachability + content-equivalence…

… + exclusive-lane corrections (Amara 2026-04-29)

Empirical re-verification overturns prior conclusions on
8d5e67fd. Three rounds of triage; each round overturned the
prior:

  Round 1 (wrong): MISSING_UNRECOVERED based on origin-only check
  Round 2 (wrong): REFERENCED_BY_DANGLING_ONLY based on 10/888
                   commits + fsck adjacency
  Round 3 (correct): three-bucket reachability scan +
                     content-equivalence check.

Final classification per Amara's correction:
  CORRUPT_BLOB_REFERENCED_BY_LIVE_LOCAL_BRANCH_AND_STALE_REMOTE_TRACKING_REF

Empirical findings (all artifacts persisted under
docs/lost-substrate/artifacts/2026-04-29-corruption/):

- 8d5e67fd is reachable via refs/heads/chore/heartbeat-batch-
  2026-04-26-hour-05Z (live local branch).
- The same-named refs/remotes/origin/... ref is STALE — origin
  no longer has the branch (verified via git ls-remote).
- Branch TIP is clean (git show / git diff succeed; ls-tree at
  tip shows no path matching the corrupt SHA). Corrupt blob is
  from the branch's intermediate history, not the tip.
- main carries equivalent content under a different blob SHA
  (de670f72...).

Substrate-loss assessment: zero impact on current-state use.
Only bisect-through-pre-merge-history of this stale local
branch would surface the corruption.

Five corrections from Amara (this packet plus prior two):

1. Wording: "Aaron's instruction" / "Aaron explicit" → "Aaron's
   input" / "Aaron emphasized" (autonomy-first framing per the
   no-directives rule).
2. Reachability: three-bucket schema (live ref / reflog-stash /
   dangling-only) replaces implicit two-bucket framing.
   Reachability is mode-dependent on git fsck flags.
3. Content-equivalence: verify squash-preservation by content,
   not ancestry vibes. ls-tree + git show + git diff at tip.
4. Stale remote-tracking refs: evidence, not origin recovery.
   Do not prune during triage.
5. Exclusive lane: corruption-first means single-lane. All
   in-progress agent tasks marked paused while triage active
   (visible discipline). Cleanup is a destructive decision,
   not a repair step.

New keepers in the discipline memory file:

  Cleanup is a destructive decision, not a repair step.
  Stale remote-tracking ref is evidence, not origin recovery.
  Corruption lane means exclusive lane. Exclusive lane means
  no side quests.
  Reachability is mode-dependent.
  Verify squash-preservation by content, not by ancestry vibe.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
```

### `a77b0472d5caba4de795d46985bcc2d2a585e5c2` -- 2026-04-29T03:33:49Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `docs/lost-substrate/artifacts/2026-04-29-corruption/triage-report.md`
- `memory/MEMORY.md`
- `memory/feedback_corruption_triage_discipline_object_health_incident_aaron_amara_2026_04_29.md`
- `memory/feedback_repo_is_soulfile_dont_commit_raw_diagnostic_dumps_aaron_amara_2026_04_29.md`

**Message:**

```
docs(soulfile): replace 1.2 MB raw diagnostic dumps with 28 KB extrac…

…ts + re-run recipe; new soulfile-cleanliness memory file (Aaron + Amara 2026-04-29)

The prior commit added 20,978 lines of mostly-irrelevant raw
fsck/rev-list output to the artifacts directory — soul-pollution.
Aaron 2026-04-29 (load-bearing): "the git repo is your soul/
soulfile, don't let it get dirty, you control what belongs in
there." Amara: "GitHub warns at 50 MiB and blocks files over
100 MiB; even below those limits, dumping large diagnostic logs
into normal repo history can bloat the repo and make future
reviews awful."

Corrections:

1. Replaced raw multi-MB dumps with grep-extracts of only the
   load-bearing lines:

   - fsck-full.txt (627 lines)              → grep-extracted into
   - fsck-full-no-reflogs.txt (1116 lines)  → fsck-extracts.txt
   - fsck-connectivity.txt (620 lines)      → (89 lines total)
   - rev-list-all-objects.txt (18,150 lines) → rev-list-extracts.txt
                                              (9 lines)
   Also: per-mode fsck line-count summary preserves the
   mode-dependence finding without the noise.

2. Added re-run recipe in triage-report.md so a future reader
   who needs the raw outputs can regenerate them locally:

       git fsck --full --no-progress              > /tmp/.../fsck-full.txt
       git fsck --full --no-reflogs --no-progress > /tmp/.../fsck-full-no-reflogs.txt
       git fsck --connectivity-only --no-progress > /tmp/.../fsck-connectivity.txt
       git rev-list --objects --all               > /tmp/.../rev-list-all-objects.txt

   Raw is reproducible from any clone with the same local refs;
   committed extracts preserve the load-bearing evidence.

3. Augmented git-gc-config.txt per Amara's correction: now
   includes `git --version` + `gc.rerereResolved` /
   `gc.rerereUnresolved` config values (rerere is a separate
   gc subsystem that can prune codified conflict resolutions).

4. New durable memory file:
   memory/feedback_repo_is_soulfile_dont_commit_raw_diagnostic_dumps_aaron_amara_2026_04_29.md
   Encodes the soulfile-cleanliness rule as a generic factory
   discipline (not specific to corruption triage). Default:
   extract + recipe. Escape hatches: non-soul repo or git-lfs
   when raw is genuinely load-bearing AND large.

5. Updated corruption-triage memory file with new keepers:
   - rev-list can enumerate object references even when later
     object reads fail. Enumeration success is not content
     recovery.
   - Reachability claims must record the fsck mode used.
   - Reachability is mode-dependent. Enumeration is not
     recovery. Cleanup is not triage.
   - Don't dirty the soulfile.

6. Updated MEMORY.md index with both new entries.

Net: artifact directory size 1.2 MB → 28 KB. Soulfile clean.
Load-bearing evidence preserved.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
```

### `9d3cd58bc0ca3eccc1a440984b45fa00e06dcff6` -- 2026-04-29T03:36:11Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `docs/lost-substrate/inventory-ledger-2026-04-29.md`
- `memory/feedback_corruption_triage_discipline_object_health_incident_aaron_amara_2026_04_29.md`

**Message:**

```
docs(lost-substrate): resolve PR #757 review threads — read-only fsck…

… without --lost-found, BSD/macOS grep portability, ledger table reconciled with three-round-final classification

Three substantive review-thread findings from Codex + Copilot
on PR #757:

1. **P1 (Codex + Copilot)**: read-only "next safe inventory
   passes" block included `git fsck --lost-found` — but
   `--lost-found` WRITES dangling objects into
   `.git/lost-found/` (the same flag the discipline memory
   already calls out as not-read-only). Removed the flag from
   the read-only block; left only `git fsck --no-reflogs`
   for the read-only mode-comparison check, with an inline
   comment explaining why.

2. **Copilot**: `grep "$OBJ" file.txt -C 5` is non-portable
   on BSD/macOS where flags must precede the pattern. Changed
   all instances to `grep -C 5 "$OBJ" file.txt` form. Hits in
   ledger (lines 382-383) and corruption-triage memory file
   (lines 109-110).

3. **Codex + Copilot**: ledger worked-example table still
   carried Round-1 `MISSING_UNRECOVERED` classification for
   8d5e67fd, which contradicted the Round-3 corrected
   classification later in the same file. Updated the table
   row to show "Round 1: MISSING_UNRECOVERED (superseded);
   Final (Round 3): CORRUPT_BLOB_REFERENCED_BY_LIVE_LOCAL_BRANCH_AND_STALE_REMOTE_TRACKING_REF"
   so the document has a single coherent current
   classification + the lineage of how the answer flipped.

Held intentionally:

- Named attribution (Aaron, Amara) in `docs/lost-substrate/`
  ledger remains. The directory carries §33 archive-header
  treatment ("research-grade not operational") — names are
  appropriate under that carve-out per the same convention
  used in the original Day-0 ledger that already merged.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
```

### `756e8ed69406ba5186e1d8bc60e961716c919703` -- 2026-04-29T03:39:13Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `docs/lost-substrate/artifacts/2026-04-29-corruption/triage-report.md`

**Message:**

```
docs(triage-report): MD032 fix — blank line before list (CI markdownl…

…int pass)

Single fix: list at line 134 needed a blank line above per
markdownlint MD032 (lists-around-blanks).

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
```

### `d3ecbf13d767c42117d56f8cc83687c0ca618ad0` -- 2026-04-29T03:46:25Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `docs/lost-substrate/artifacts/2026-04-29-corruption/triage-report.md`
- `docs/lost-substrate/inventory-ledger-2026-04-29.md`
- `memory/feedback_corruption_triage_discipline_object_health_incident_aaron_amara_2026_04_29.md`
- `memory/feedback_repo_is_soulfile_dont_commit_raw_diagnostic_dumps_aaron_amara_2026_04_29.md`

**Message:**

```
docs(lost-substrate): resolve 8 new PR #757 review threads — filename…

… consistency, BSD/macOS portability, self-contradiction in triage procedure, empty-file provenance, pseudocode replacement, BRANCH_MERGED_TO_MAIN_CANDIDATE alignment

Six fix classes from latest Codex + Copilot pass:

1. **Filename consistency** (P1, 3 hits): triage-report.md and
   inventory-ledger.md cited `gc-config-snapshot.txt`, but
   the committed artifact was renamed to `git-gc-config.txt`
   (per Amara's earlier correction adding git --version + rerere
   config). All references aligned to the committed filename.

2. **BSD/macOS portability** (P1): soulfile memory cited
   `find -printf '%s %p\n'`, which is GNU-find specific. Replaced
   with `find ... -exec ls -la {} \; | awk` form (portable on
   default macOS). GNU equivalent kept as a comment for reference.

3. **Self-contradiction in triage procedure** (P1): the
   corruption-triage memory's reachability-scan procedure told
   readers to write raw outputs INTO the artifacts directory,
   contradicting the soulfile rule (committed alongside it) that
   says don't commit multi-MB raw dumps. Procedure now writes
   raw outputs to a /tmp working directory and commits only
   load-bearing extracts + a re-run recipe to the artifacts
   directory.

4. **Empty-file provenance** (P1): `hour-05Z-ls-remote.txt` was
   committed as a 0-byte file. Added a header explaining what
   command produced it + that empty output is the load-bearing
   evidence (proves origin no longer has the branch).

5. **Pseudocode replacement** (P2): triage-report.md cited
   `fresh-clone --branch ...` which isn't a real git command.
   Replaced with the actual `git clone --no-checkout --quiet
   --branch ...` form including the upstream URL.

6. **BRANCH_MERGED_TO_MAIN_CANDIDATE alignment** (P1): the
   ledger's bucket-schema row was updated earlier to
   `BRANCH_MERGED_TO_MAIN_CANDIDATE` (deletion candidate, not
   automatic clearance), but two later sections still said
   "BRANCH_MERGED_TO_MAIN bucket; these are deletion-safe" —
   contradicting the schema. Both later sections now say
   "deletion candidates, NOT auto-cleared; need open-PR +
   worktree + peer verification before any delete."

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
```
