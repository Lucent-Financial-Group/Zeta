# PR #858 -- doctrine(acehack-mirror): force-with-lease + remote-topology cleanup + multi-remote-script-design

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 858 |
| Title | doctrine(acehack-mirror): force-with-lease + remote-topology cleanup + multi-remote-script-design |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-04-29T20:27:39Z |
| Merged at | 2026-04-29T20:41:25Z |
| Merge commit SHA | `5294bf45ab3c65880b748df94e77e93b193cafd2` |
| Branch | `doctrine/acehack-mirror-not-peer-force-sync-2026-04-29` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/858 |
| Changed files | 3 |
| Additions / deletions | +679 / -1 |

## Description

## Status

**Doctrine substrate landing**, NOT operational sync execution. Three Amara packets from 2026-04-29 (later same-day) refining the LFG-canonical / AceHack-mirror-while-inactive rule landed earlier today.

## The three packets

**Packet 1 — force-sync command discipline:**
- \`--force-with-lease\` by default (safety latch — refuses to overwrite if AceHack changed unexpectedly between fetch and push)
- Raw \`--force\` reserved for confirmed inactive-mirror state
- Direction always \`LFG → AceHack\`, never bidirectional

**Packet 2 — remote-topology cleanup (\"dual-root\" disambiguation):**
- \`origin = LFG only\`. No multi-push URLs.
- Optional \`acehack-mirror\` remote (named explicitly; no local branch tracks it)
- Cleanup-inspection + normalisation commands documented

**Packet 3 — multi-remote-script design (TS port + future tooling):**
- Don't rip out multi-remote support — real market exists (fork contributors, GitHub↔GitLab mirror, deploy remotes)
- DO rip out the Zeta-specific dual-active assumption
- 3-tier design: Tier 1 default (origin only) / Tier 2 advanced (--remote flags) / Tier 3 mirror (--from/--to)
- Optional repo-local \`git-topology.yaml\` config deferred

## Files

- \`docs/research/2026-04-29-amara-acehack-mirror-not-peer-force-sync-protocol.md\` (NEW — verbatim preservation of all 3 packets)
- \`memory/feedback_lfg_only_development_flow_acehack_is_mirror_aaron_amara_2026_04_29.md\` (EXTENDED in-place per Otto-362; +184 lines covering force-with-lease + remote-topology + multi-remote-script-design)

## What this PR does NOT do

- ❌ NOT a CLAUDE.md / AGENTS.md edit (residual double-hop language refinement = separate small substrate PR)
- ❌ NOT operational sync execution (separate tick after this PR lands)
- ❌ NOT a TS port (PR #849 lane carries the TS port; this PR adds the design *constraint* the port must follow)
- ❌ NOT a duplicate memory file (per Otto-362 in-place expansion — extends existing canonical home)

## Authority verifications (per Otto-364 search-first)

- [git-push(1) \`--force-with-lease\` semantics](https://git-scm.com/docs/git-push#Documentation/git-push.txt---no-force-with-lease)
- [GitHub CLI \`gh repo sync --force\`](https://cli.github.com/manual/gh_repo_sync)
- [GitHub branch protection vs force-push](https://docs.github.com/en/repositories/configuring-branches-and-merges-in-your-repository/managing-protected-branches/about-protected-branches)
- [git-remote(1) \`set-url\` semantics](https://git-scm.com/docs/git-remote#Documentation/git-remote.txt-emset-urlem)
- [GitHub fork workflow / origin+upstream pattern](https://docs.github.com/en/get-started/quickstart/fork-a-repo)

## Follow-up

- Task #341 — TS port + future git scripts must enforce the 3-tier multi-remote design

## Test plan

- [x] Memory file extended in-place (no duplicate canonical home created)
- [x] Verbatim preservation of all 3 Amara packets in \`docs/research/\`
- [x] Authority links verified upstream (Otto-364)
- [x] No code/CI risk (docs-only)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 4 |
| Resolved threads | 4 |
| Unresolved threads | 0 |
| Total review comments | 4 |
| Total fix commits (touching thread paths) | 2 |

## Review threads

### Thread 1 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5-j5h6`
- **Path**: `docs/research/2026-04-29-amara-acehack-mirror-not-peer-force-sync-protocol.md`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-29T20:31:31Z on `docs/research/2026-04-29-amara-acehack-mirror-not-peer-force-sync-protocol.md`:5 (association: NONE)

  <pre>
  P1: The file header says this packet contains TWO Amara contributions (force-with-lease + remote-topology cleanup), but the document also includes “Amara packet 3” later. Please update the Status line to reflect that packet 3 is included (or explicitly state which packets are in-scope for this preservation).
  ```suggestion
  **Status**: Research-grade preservation. The Aaron + Amara quoted bodies are verbatim; the surrounding framing is translation/synthesis. The distilled rules extend the existing canonical home at `memory/feedback_lfg_only_development_flow_acehack_is_mirror_aaron_amara_2026_04_29.md` (per Otto-362 in-place expansion, not duplication). This preservation includes the two same-day Amara contributions named here — (1) force-with-lease command discipline, (2) remote-topology cleanup (the dual-root problem) — plus the later “Amara packet 3” included below.
  ```
  </pre>

### Thread 2 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5-j5ih`
- **Path**: `docs/research/2026-04-29-amara-acehack-mirror-not-peer-force-sync-protocol.md`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-29T20:31:32Z on `docs/research/2026-04-29-amara-acehack-mirror-not-peer-force-sync-protocol.md`:72 (association: NONE)

  <pre>
  P1: In the first verbatim “Preferred command shape” block, the remote name is `acehack`, but elsewhere in this doctrine the recommended explicit mirror remote name is `acehack-mirror`. Since this is a copy/paste-prone command block, add a short non-verbatim note immediately after it clarifying that `acehack` in the quote corresponds to today’s `acehack-mirror` naming discipline (without changing the verbatim quote itself).
  ```suggestion
  &gt; ```

  Note: in the verbatim quote above, `acehack` is the historical remote name; under today’s explicit naming discipline, use `acehack-mirror` for that mirror remote.
  ```
  </pre>

### Thread 3 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5-j5i9`
- **Path**: `memory/feedback_lfg_only_development_flow_acehack_is_mirror_aaron_amara_2026_04_29.md`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-29T20:31:32Z on `memory/feedback_lfg_only_development_flow_acehack_is_mirror_aaron_amara_2026_04_29.md`:177 (association: NONE)

  <pre>
  P1: The cleanup command for removing an AceHack `pushurl` from `origin` is hardcoded to the SSH URL (`git@github.com:AceHack/Zeta.git`). If the configured pushurl is HTTPS or uses a different spelling, this won’t remove it. Adjust the guidance to delete the exact value(s) returned by `git config --get-all remote.origin.pushurl`, or to clear `remote.origin.pushurl` entirely when the goal is to return to a single canonical push destination.
  ```suggestion
  # Clear any explicit pushurl overrides so origin pushes only to the canonical destination:
  git config --unset-all remote.origin.pushurl 2&gt;/dev/null || true
  ```
  </pre>

### Thread 4 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5-j5jT`
- **Path**: `docs/research/2026-04-29-amara-acehack-mirror-not-peer-force-sync-protocol.md`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-29T20:31:32Z on `docs/research/2026-04-29-amara-acehack-mirror-not-peer-force-sync-protocol.md`:471 (association: NONE)

  <pre>
  P1: In “Operational status”, the first bullet says this file preserves packet 1 + packet 2, but the file also includes “Amara packet 3 — verbatim” above. Update this bullet (and any other summary text) so it matches the actual contents of the file.
  </pre>

## Fix commits (touching thread paths)

### `0a7c726210a29bbfecb9624a681ce8d91f0613a8` -- 2026-04-29T20:27:19Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `docs/research/2026-04-29-amara-acehack-mirror-not-peer-force-sync-protocol.md`
- `memory/feedback_lfg_only_development_flow_acehack_is_mirror_aaron_amara_2026_04_29.md`

**Message:**

```
doctrine(acehack-mirror): force-with-lease + remote-topology cleanup …

…+ multi-remote-script-design (Aaron + Amara 2026-04-29)

Three Amara packets landed 2026-04-29 (later same-day) refining the
LFG-canonical / AceHack-mirror-while-inactive doctrine that landed earlier
today:

Packet 1 — force-sync command discipline:
- Use `--force-with-lease` by default (refuses to overwrite if AceHack
  changed unexpectedly between fetch and push — safety latch).
- Raw `--force` reserved for confirmed inactive-mirror state.
- Direction always `LFG → AceHack`, never bidirectional.

Packet 2 — remote-topology cleanup ("dual-root" disambiguation):
- `origin = LFG only`. No multi-push URLs, no `origin` fanout to AceHack.
- Optional `acehack-mirror` remote (named explicitly so topology can't be
  misread). No local branch tracks `acehack-mirror/*`.
- Cleanup-inspection commands documented: `git remote -v`, `git config
  --get-all remote.origin.pushurl`, `git branch -vv`. Normalisation via
  `git remote set-url`. Server repos never touched (only local refs).

Packet 3 — multi-remote support discipline for git scripts (TS port):
- Don't rip out multi-remote support entirely. Real market: fork
  contributors (origin=fork, upstream=canonical), GitHub→GitLab mirror,
  deploy remotes, personal fork projection.
- DO rip out the Zeta-specific dual-active assumption.
- 3-tier design:
  Tier 1 (default): scripts use `origin`, assume canonical, don't inspect
  others.
  Tier 2 (advanced): explicit `--remote`/`--upstream`/`--push-remote`
  flags.
  Tier 3 (mirror): dedicated scripts only, explicit `--from`/`--to`.
- Optional repo-local config (tools/ci/git-topology.yaml-ish) deferred —
  declarative source-of-truth for future scripts to read.

Both packets preserved verbatim in
`docs/research/2026-04-29-amara-acehack-mirror-not-peer-force-sync-protocol.md`
per the channel-verbatim-preservation rule (Aaron + Amara 2026-04-29).

Canonical home extended in-place per Otto-362 (in-place expansion, not
duplication): `memory/feedback_lfg_only_development_flow_acehack_is_
mirror_aaron_amara_2026_04_29.md` already covered the LFG-only / mirror
doctrine; this commit adds two new sections —
"§2026-04-29 refinement (later same-day) — force-with-lease + remote-
topology cleanup" and "§2026-04-29 packet 3 — design constraint for git
scripts (TS port + future tooling)" — with both new layers as in-place
extensions. No duplicate memory file.

Authority verifications (per Otto-364 search-first):
- git-push(1) `--force-with-lease` semantics
- gh repo sync `--force` flag
- GitHub branch protection vs force-push warnings
- git remote `set-url` semantics (server-safe)
- GitHub origin+upstream fork workflow (multi-remote market)

No CLAUDE.md / AGENTS.md edits in this commit (separate small substrate
PR for the residual double-hop language refinement; smallest-sufficient
scope per Otto-362).

No operational sync execution in this commit (separate tick after this
PR lands).

Follow-up tasks captured:
- #341 — TS port + future git scripts: enforce 3-tier multi-remote design.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
```

### `075809e0df0b85b3167b7fcf5fea3ffa97b134bc` -- 2026-04-29T20:37:19Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `docs/research/2026-04-29-amara-acehack-mirror-not-peer-force-sync-protocol.md`
- `memory/feedback_lfg_only_development_flow_acehack_is_mirror_aaron_amara_2026_04_29.md`

**Message:**

```
fix(doctrine): 4 PR #858 thread fixes — packet-3 mention + acehack-mi…

…rror naming + robust pushurl unset

Copilot review threads (all P1):

- L5 + L471 (Status header + Operational status): updated to mention
  packet 3 (multi-remote-script-design) alongside packets 1 and 2.
  Earlier wording said "TWO contributions" but the file includes all
  three.

- L72 (acehack vs acehack-mirror naming): added a non-verbatim
  reconciliation note after the verbatim "Preferred command shape"
  block. The verbatim quote uses `acehack` as the remote name; under
  packet 2's explicit naming discipline the recommended name is
  `acehack-mirror`. Verbatim preserved as-written; note clarifies for
  operational use without modifying the quote.

- L177 (pushurl unset robustness): changed cleanup snippet from
  `git remote set-url --delete --push origin <exact-SSH-URL>` to
  `git config --unset-all remote.origin.pushurl`. The earlier form
  required the URL to match exactly which fails on HTTPS-vs-SSH or
  `.git`-suffix differences. Clearing the whole pushurl block is
  robust regardless of how the override was originally added.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
```
