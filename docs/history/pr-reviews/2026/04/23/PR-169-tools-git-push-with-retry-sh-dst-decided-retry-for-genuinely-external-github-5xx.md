# PR #169 -- tools(git): push-with-retry.sh — DST-decided retry for genuinely external GitHub 5xx

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 169 |
| Title | tools(git): push-with-retry.sh — DST-decided retry for genuinely external GitHub 5xx |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-04-23T16:43:34Z |
| Merged at | 2026-04-23T18:30:33Z |
| Merge commit SHA | `b448d2b9d70e019177301285b0951857b9cdc051` |
| Branch | `tools/git-push-with-retry` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/169 |
| Changed files | 1 |
| Additions / deletions | +121 / -0 |

## Description

## Summary
Thin `git push` retry wrapper for transient GitHub 5xx errors, landing after a root-cause investigation per Aaron's DST discipline (retries are a non-determinism smell; investigate first).

## Investigation (2026-04-23)
Aaron pushed back on an earlier version of this PR that reached for retry without investigation. Investigation chain:

- **Aaron's hypothesis**: error URL showed `Zeta.git/` with a trailing slash — possible client-side URL-construction bug.
- **Local config**: `remote.origin.url = https://github.com/Lucent-Financial-Group/Zeta.git` — no trailing slash.
- **On-wire trace**: `GIT_TRACE=1 GIT_CURL_VERBOSE=1 git ls-remote origin` shows the actual URL is `/Lucent-Financial-Group/Zeta.git/git-upload-pack`. The `.git/` in error messages is `.git/git-upload-pack` truncated by git's error formatter — correct URL path per Git-over-HTTPS spec, not a bug.
- **Reproduction**: HTTP 500 returns from GitHub itself, not a client error. Reproduces intermittently across commands.

**Conclusion**: this is a genuinely external GitHub transient (the DST-exception case — *"real external reasons we can't control"*). Retry is legitimate here **after** investigation, not as a default reach.

## The script
- Wraps `git push` with up to 3 retries (env `GIT_PUSH_MAX_ATTEMPTS`) on matched 5xx text (500/502/503/504/Internal Server Error/Bad Gateway/Service Unavailable/Gateway Timeout).
- Exponential backoff starting at 2s (env `GIT_PUSH_BACKOFF_S`).
- Non-transient errors (auth / protected branch / hook / divergence) propagate immediately without retry.
- Thin-wrapper-over-existing-CLI exemption from the bun+TS default per `docs/POST-SETUP-SCRIPT-STACK.md` Q3.

## Paper trail
- Per-user memory: `feedback_retries_are_non_determinism_smell_DST_holds_investigate_first_2026_04_23.md` captures the DST discipline + the investigation-before-retry order
- Script header documents the full investigation chain
- Commit body describes the pushback + resolution

## If the 500-rate escalates
Revisit — the retry wrapper is a mitigation for the current observed rate. If the rate rises or a new root cause surfaces, this wrapper should be replaced by the actual fix.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | true |
| Total threads | 8 |
| Resolved threads | 7 |
| Unresolved threads | 1 |
| Total review comments | 8 |
| Total fix commits (touching thread paths) | 3 |

## Review threads

### Thread 1 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59KKe9`
- **Path**: `tools/git/push-with-retry.sh`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-04-23T16:44:48Z on `tools/git/push-with-retry.sh`:69 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Capture git push exit status before closing the if**

  On a failed push, `exit_code=$?` is executed **after** the `if ... fi` block, but in Bash an `if` command with no `else` returns success when the condition is false, so `exit_code` becomes `0` instead of the real `git push` failure code. That means non-transient failures (auth, protected branch, divergence, hooks) can hit the `exit "$exit_code"` path and incorrectly exit წარმატously, causing callers to treat a failed push as successful.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 2 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59KMpQ`
- **Path**: `tools/git/push-with-retry.sh`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-23T16:47:27Z on `tools/git/push-with-retry.sh`:18 (association: NONE)

  <pre>
  The comment references `memory/feedback_retries_are_non_determinism_smell_DST_holds_investigate_first_2026_04_23.md`, but that file does not exist in `memory/` in the current tree. Either add the referenced memory entry or update the reference to the correct existing path so the investigation trail is navigable.
  ```suggestion
  # Root-cause investigation (2026-04-23, per the DST
  # discipline that retries should be investigated before they
  # are added):
  ```
  </pre>

### Thread 3 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59KMpu`
- **Path**: `tools/git/push-with-retry.sh`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-23T16:47:27Z on `tools/git/push-with-retry.sh`:30 (association: NONE)

  <pre>
  This script comment uses personal name attribution (e.g., "per Aaron's DST discipline" / "Aaron's trailing-slash hypothesis"). Repo convention is to use role-refs instead of personal names outside allowed locations (see `docs/AGENT-BEST-PRACTICES.md` §"No name attribution in code, docs, or skills"). Please rewrite these references to roles (e.g., "human maintainer") or remove the attribution while preserving the technical facts.
  </pre>

### Thread 4 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59KMp6`
- **Path**: `tools/git/push-with-retry.sh`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-23T16:47:27Z on `tools/git/push-with-retry.sh`:66 (association: NONE)

  <pre>
  If the script is interrupted (Ctrl-C/TERM) after `mktemp`, the temp file can be left behind because cleanup is only on the normal control-flow paths. Consider adding a `trap` that removes the current `$tmp_stderr` on EXIT (and resetting it each loop) to match the temp-file hygiene used in other tooling scripts (e.g., `tools/hygiene/check-github-settings-drift.sh:59-61`).
  </pre>

### Thread 5 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59KMqC`
- **Path**: `tools/git/push-with-retry.sh`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-23T16:47:28Z on `tools/git/push-with-retry.sh`:86 (association: NONE)

  <pre>
  `max_attempts` and `backoff` are taken from env as strings but then used in arithmetic contexts (`(( attempt &lt;= max_attempts ))`, `$(( backoff * 2 ))`). With `set -e`, a non-integer env value will terminate the script with a shell arithmetic error. Consider validating these inputs early (numeric and &gt;= 1 for attempts, numeric and &gt;= 0/1 for backoff) and emitting a clear error message.
  ```suggestion

  require_integer_at_least() {
    local name="$1"
    local value="$2"
    local minimum="$3"

    if ! [[ "$value" =~ ^[0-9]+$ ]]; then
      echo "error: $name must be an integer &gt;= $minimum; got '$value'" &gt;&amp;2
      exit 1
    fi

    if (( value &lt; minimum )); then
      echo "error: $name must be an integer &gt;= $minimum; got '$value'" &gt;&amp;2
      exit 1
    fi
  }

  max_attempts="${GIT_PUSH_MAX_ATTEMPTS:-3}"
  backoff="${GIT_PUSH_BACKOFF_S:-2}"

  require_integer_at_least "GIT_PUSH_MAX_ATTEMPTS" "$max_attempts" 1
  require_integer_at_least "GIT_PUSH_BACKOFF_S" "$backoff" 1
  ```
  </pre>

### Thread 6 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59KMqN`
- **Path**: `tools/git/push-with-retry.sh`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-23T16:47:28Z on `tools/git/push-with-retry.sh`:70 (association: NONE)

  <pre>
  P0: `exit_code=$?` is captured after the `if git push ...; then ... fi` block. In bash, when the condition fails and there is no `else`, the `if` compound command typically exits with status 0, so `exit_code` can become 0 and the script may incorrectly report success on non-transient push failures. Capture the `git push` exit status in the `else` path (or immediately after the `git push` invocation) before any other commands run, then propagate that value.
  ```suggestion
    else
      exit_code=$?
    fi
  ```
  </pre>

### Thread 7 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59KMqZ`
- **Path**: `tools/git/push-with-retry.sh`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-23T16:47:28Z on `tools/git/push-with-retry.sh`:49 (association: NONE)

  <pre>
  The header claims exit code `1` for any failure, but the script intends to propagate the underlying `git push` exit code for non-transient errors (`exit "$exit_code"`). Once the exit-code capture is fixed, either update this doc to reflect propagated exit codes or normalize failures to always exit 1 for consistency.
  ```suggestion
  #   1   push failed after all retries on transient 5xx errors
  #   N   non-transient error; propagates underlying `git push`
  #       exit code
  ```
  </pre>

### Thread 8 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM59L5VM`
- **Path**: `tools/git/push-with-retry.sh`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-04-23T18:31:45Z on `tools/git/push-with-retry.sh`:103 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Restrict 5xx matching to actual HTTP status text**

  The retry classifier currently treats any occurrence of `500|502|503|504` in stderr as a transient GitHub outage, which causes false positives on ordinary non-transient failures (for example, a rejected ref like `feature/502-refactor`). In that case this loop retries unnecessarily and eventually returns `1` instead of propagating the original `git push` exit code, so callers lose the real failure signal. Matching should require explicit HTTP/remote 5xx context (or anchored tokens) rather than bare numbers.

  Useful? React with 👍 / 👎.
  </pre>

## Fix commits (touching thread paths)

### `f9e9278da12b070858f5ce90e1f75f4382d7f2a4` -- 2026-04-23T16:41:12Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/git/push-with-retry.sh`

**Message:**

```
tools(git): push-with-retry.sh — thin retry wrapper for transient 5xx

Aaron 2026-04-23 noted recurring Internal Server Error 500s
from GitHub during autonomous-loop tick-close commits. A
retry of the same `git push` has succeeded on next attempt
within seconds each time.

Script: retry up to 3 times (env GIT_PUSH_MAX_ATTEMPTS) on
matched 5xx error text (500/502/503/504/Internal Server
Error/Bad Gateway/Service Unavailable/Gateway Timeout) with
exponential backoff starting at 2s (env GIT_PUSH_BACKOFF_S).
Non-transient errors (auth / protected branch / hook /
divergence) propagate immediately without retry.

Thin-wrapper-over-existing-CLI exemption from the bun+TS
default per docs/POST-SETUP-SCRIPT-STACK.md Q3.

Open root-cause question noted in the header: Aaron spotted
`Zeta.git/` trailing slash in the error URL. Local git
config has the canonical `.git` form with no trailing slash,
so the trailing slash may be an artifact of git's
URL-error formatter. If root cause turns out to be
URL-construction, a repo-level fix would supersede this
wrapper.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
```

### `3b44b97d405b3f822f0a9d130f6710674562e942` -- 2026-04-23T16:43:07Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/git/push-with-retry.sh`

**Message:**

```
tools(git): document root-cause investigation per DST discipline

Aaron 2026-04-23 pushback on the prior commit: retries are a
non-determinism smell; DST (Deterministic Simulation Theory)
holds throughout the factory except when explicitly decided-
against for real external uncontrollable reasons; investigate
before reaching for retry.

Investigation this tick:
- Local git config clean (no trailing slash on remote URL)
- GIT_TRACE=1 + GIT_CURL_VERBOSE=1 on git ls-remote origin
  shows on-wire URL is `/Lucent-Financial-Group/Zeta.git/
  git-upload-pack` — the `.git/` in git's error messages is
  `.git/git-upload-pack` truncated by git's error formatter
  (correct URL path, not a client-side bug)
- The HTTP 500 returns from GitHub itself, reproduces
  intermittently across commands
- Conclusion: genuinely external GitHub transient, which IS
  the DST-exception case — retry legitimate after
  investigation

Script header updated with the full investigation chain so
the paper trail records what was checked and why retry was
decided-against-DST for this specific boundary.

Memory filed per-user:
feedback_retries_are_non_determinism_smell_DST_holds_investigate_first_2026_04_23.md

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
```

### `d44f8a1420156d9cacd499a2d81cd2c2dee7879c` -- 2026-04-23T18:28:50Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/git/push-with-retry.sh`

**Message:**

```
tools(git): fix real bash bugs per PR #169 review

Seven Copilot findings addressed:

P0 bugs (real):
- `exit_code=$?` after `if git push; then ... fi` block was
  capturing the if-compound's 0-return, not git push's exit
  code. Switched to `set +e` around the push, capture $? on
  the next line, then `set -e`. Now correctly propagates
  git push's exit code for non-transient errors.
- tmp_stderr cleanup only on normal paths; SIGTERM/SIGINT
  could leak temp files. Moved mktemp outside the loop;
  added `trap cleanup EXIT`.
- Env vars used in arithmetic contexts without validation.
  Added `int_re` regex check before arithmetic fires; exits
  with code 2 on bad env (not set -e crash).

Discipline:
- Personal-name attribution ("Aaron's DST discipline") →
  role-refs ("the DST discipline" / "the maintainer's
  trailing-slash hypothesis"). Matches BP rule on name-
  attribution in code/docs/skills.
- Header exit-code documentation updated: 0 success / 1 all-
  retries-exhausted / 2 env-validation-failed / N
  propagated-from-git-push.

Xref:
- Per-user memory reference shortened to remove the exact
  filename path (which lives at ~/.claude/projects/<slug>/
  memory/; not in-repo).

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
```
