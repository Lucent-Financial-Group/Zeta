# PR #6626 -- fix(ace): untrusted-input packageHash throws → styled invalid-package refusals (not ace: fatal:)

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 6626 |
| Title | fix(ace): untrusted-input packageHash throws → styled invalid-package refusals (not ace: fatal:) |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-06-03T07:28:36Z |
| Merged at | 2026-06-03T07:40:41Z |
| Merge commit SHA | `0df52d4bb79cdbcbc5faa37276614cd2f6db834c` |
| Branch | `otto-windows/ace-untrusted-input-hardening-2026-06-03` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/6626 |
| Changed files | 7 |
| Additions / deletions | +266 / -20 |

## Description

## What

Trust-core hardening follow-up for the Ace package manager (081KR2E4K0008QG0R002YE3MMD), surfaced by the 8.1/8.2 final reviews and confirmed against current `origin/main`.

`packageHash(pkg)` throws (via `canonicalBytes` → `toTagged`) on a malformed **untrusted** field — a non-safe-integer (float) or a lone UTF-16 surrogate. `resolve.ts`'s **dependency** site already mapped that throw to a styled `invalid-package` refusal; the sibling untrusted-input sites did **not**, so the same input surfaced as the generic `ace: fatal: <internal>` entry-point catch (an internal-error channel) instead of a styled domain refusal.

## Change

Centralize the throw→reason mapping as `safePackageHash(pkg): { ok: true; hash } | { ok: false; reason }` in `tools/ace/package-hash.ts` (no change to `packageHash` itself), and apply it at every untrusted-input site:

| Site | Channel / exit |
|---|---|
| `resolve.ts` root (primary reachable gap) | `invalid-package` at path `["root"]` |
| `resolve.ts` dep | refactored to `safePackageHash` (identical behaviour; deletes duplicated inline try/catch) |
| `ace.ts` registry-add | `ace: registry add: invalid package — …`, exit **65** |
| `ace.ts` frozen-root / frozen-node | `ace: install refused: invalid-package — …`, exit **1** (hash computed once + reused) |
| `ace.ts` preflightGraph | its `string \| null` error channel (defense-in-depth) |

Also corrects two overconfident shape-guard comments that claimed object-ness prevented the `packageHash` throw — it guards shape, not field values.

No `packageHash` value or signature/identity-semantics change; only the malformed-untrusted-input **failure path** changes. The leaf (no-deps) install path is untouched (it computes only `content_hash`, which never throws on floats/surrogates).

## Tests (assert-don't-skip)

- **Unit:** `safePackageHash` parity on a well-formed package; `{ ok: false }` with a reason on float + lone-surrogate fields.
- **Behavioural (falsifying — failed before the fix):** malformed root → `resolve()` returns `invalid-package` (previously threw, escaping `resolve()`); `ace registry add` of a float-field package → exit `65` (previously `main()` threw → `ace: fatal:`).
- Coverage honesty: frozen-root / frozen-node / `preflightGraph` are guarded uniformly + unit-covered; they're defense-in-depth (resolve catches root/dep upstream in the default + update flows; frozen needs a matching-lock fixture). Stated in the design doc, not silently skipped.

## Gates

`bun test tools/ace/` → 370 pass / 0 fail · `bun --bun tsc --noEmit -p tsconfig.json` → exit 0 · markdownlint → clean · pure-LF · code-review subagent → APPROVE (no findings).

Design doc: `docs/agendas/ace-package-manager/2026-06-03-ace-untrusted-input-packagehash-hardening-design.md`

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | true |
| Total threads | 3 |
| Resolved threads | 3 |
| Unresolved threads | 0 |
| Total review comments | 6 |
| Total fix commits (touching thread paths) | 2 |

## Review threads

### Thread 1 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GqieA`
- **Path**: `tools/ace/ace.ts`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-06-03T07:30:25Z on `tools/ace/ace.ts`:982 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Move root safe hash before frozen lock drift check**

  In `install --frozen` for graph roots, this new guard is reached only after `verifyRootMatchesLock(pkg, lf)` runs above; that helper still calls `packageHash(root)` directly. For a malformed root field such as a float or lone surrogate, the throw therefore escapes to the top-level `ace: fatal:` catch before this `safePackageHash` refusal can run, leaving the frozen-root case this change set out to harden still unhandled. Compute the root hash safely before/inside the lock drift check so the malformed-root path returns the styled `invalid-package` refusal.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-06-03T07:38:15Z on `tools/ace/ace.ts`:982 (association: MEMBER)

    <pre>
    Addressed in e85e5b017 — the root `safePackageHash` guard now runs ONCE at the top of the install/update handlers, i.e. before the frozen drift check (`verifyRootMatchesLock`); the frozen-path block reuses that entry hash rather than re-guarding after the drift check.
    </pre>

### Thread 2 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6Gqkvr`
- **Path**: `tools/ace/ace.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-03T07:32:52Z on `tools/ace/ace.ts`:983 (association: NONE)

  <pre>
  P0: In the --frozen graph path, `verifyRootMatchesLock(pkg, lf)` still calls `packageHash(root)` (see lockfile.ts:79-81), which can throw on float/lone-surrogate fields. That means a malformed root can still escape to the `ace: fatal:` catch-all *before* the new `safePackageHash(pkg)` guard runs, so this slice isn’t uniformly closing the throw-surface for untrusted input in frozen mode.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-06-03T07:38:11Z on `tools/ace/ace.ts`:983 (association: MEMBER)

    <pre>
    Confirmed real against `lockfile.ts:80` — `verifyRootMatchesLock` calls `packageHash(root)` unguarded and (in the --frozen path) runs before the prior per-path guard, so a malformed --frozen root still threw. Fixed in e85e5b017: the untrusted root is now guarded via `safePackageHash` ONCE at the top of the install + update command handlers, BEFORE any branch calls `verifyRootMatchesLock` / `buildLeafLockfile` / `buildLockfile` / `resolve` / `preflightGraph` — so a malformed root refuses as invalid-package (exit 1) before the throw. `lockfile.ts` kept as a trusted-input helper (callers validate first). bun test tools/ace/ 371 pass; tsc strict 0.
    </pre>

### Thread 3 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GqkwA`
- **Path**: `docs/agendas/ace-package-manager/2026-06-03-ace-untrusted-input-packagehash-hardening-design.md`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-03T07:32:53Z on `docs/agendas/ace-package-manager/2026-06-03-ace-untrusted-input-packagehash-hardening-design.md`:38 (association: NONE)

  <pre>
  This design doc claims the leaf (no-deps) install path “computes only content_hash … not packageHash,” but leaf mode still uses `packageHash` via lockfile handling (`buildLeafLockfile` / `verifyRootMatchesLock` in tools/ace/lockfile.ts). That matters for this hardening story because those sites can still throw on float/lone-surrogate input (especially `--frozen` / `--locked` drift checks).
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-06-03T07:38:13Z on `docs/agendas/ace-package-manager/2026-06-03-ace-untrusted-input-packagehash-hardening-design.md`:38 (association: MEMBER)

    <pre>
    Correct — the leaf (no-deps) path is not packageHash-free: its lockfile write goes through `buildLeafLockfile` (`lockfile.ts:92`), which computes `packageHash(root)`. Doc corrected in e85e5b017 (removed the claim; added the `lockfile.ts` reach map + the early-root-guard decision). The leaf-default malformed root is now covered by the entry guard plus a new falsifying test (install leaf-default float-field root → exit 1; previously main() threw through buildLeafLockfile).
    </pre>

## Fix commits (touching thread paths)

### `d05f038f377c84223861fb7f997c1d9ae54be782` -- 2026-06-03T07:27:57Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `docs/agendas/ace-package-manager/2026-06-03-ace-untrusted-input-packagehash-hardening-design.md`
- `tools/ace/ace.ts`

**Message:**

```
fix(ace): map untrusted-input packageHash throws to styled invalid-pa…

…ckage refusals (not ace: fatal:)

ace's packageHash(pkg) throws (canonicalBytes -> toTagged) on a malformed
untrusted field — a non-safe-integer (float) or lone UTF-16 surrogate.
resolve.ts's dependency site already mapped that to an invalid-package
refusal; the sibling untrusted-input sites did not, so the same input
surfaced as the generic `ace: fatal:` entry-point catch (an internal-error
channel) instead of a styled domain refusal.

Centralize the throw->reason mapping as safePackageHash(pkg) in
package-hash.ts and apply it at every untrusted-input site:
- resolve.ts root (the primary reachable gap) + dep-site refactor for uniformity
- ace.ts registry-add (exit 65); frozen-root + frozen-node (exit 1, hash
  computed once + reused); preflightGraph (its string|null channel)
Correct two shape-guard comments that claimed object-ness prevented the
packageHash throw — it guards shape, not field values.

No packageHash value or signature/identity-semantics change; only the
malformed-untrusted-input failure path changes. Tests: safePackageHash unit
tests + falsifying behavioural tests (malformed root -> resolve invalid-package;
registry-add float field -> exit 65). bun test tools/ace/ 370 pass; tsc strict
exit 0; markdownlint clean.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `e85e5b01741a17e47d048c91204d889d3d16411c` -- 2026-06-03T07:37:36Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `docs/agendas/ace-package-manager/2026-06-03-ace-untrusted-input-packagehash-hardening-design.md`
- `tools/ace/ace.ts`

**Message:**

```
fix(ace): guard untrusted root at install/update entry (covers lockfi…

…le.ts packageHash reaches)

Review (Copilot P0 + doc finding, Codex P2) surfaced packageHash reaches
missed in the first commit, in lockfile.ts:
- verifyRootMatchesLock (frozen path) runs BEFORE the prior per-path guard,
  so a malformed --frozen root still threw -> ace: fatal:.
- buildLeafLockfile hashes the root, so the leaf (no-deps) install lock
  write also threw on a malformed root (the earlier doc claim that the leaf
  path is packageHash-free was wrong).

Fix: guard the untrusted root ONCE via safePackageHash at the top of the
install + update command handlers — before any graph/leaf/frozen branch
calls resolve / verifyRootMatchesLock / buildLockfile / buildLeafLockfile /
preflightGraph. Frozen-root reuses that entry hash. lockfile.ts stays a
trusted-input helper (its callers validate first), so it is unchanged.

Tests: + install leaf-default malformed-root -> exit 1 (falsifying: main()
previously threw through buildLeafLockfile). Design doc corrected (leaf
claim + lockfile.ts reach map + early-guard decision). bun test tools/ace/
371 pass; tsc strict exit 0; markdownlint clean.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```
