# PR #6628 -- proof(081KT2T2J0008QG0R000YZ3NMY C4): Message.marginal is a monoid homomorphism (list,@,[])→(message,*,One) — FsCheck

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 6628 |
| Title | proof(081KT2T2J0008QG0R000YZ3NMY C4): Message.marginal is a monoid homomorphism (list,@,[])→(message,*,One) — FsCheck |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-06-03T07:37:28Z |
| Merged at | 2026-06-03T07:45:55Z |
| Merge commit SHA | `3bd755926b618f57576bd0110f9a920778b9ea64` |
| Branch | `otto-cli/b1007-c4-marginal-fold-homomorphism-2026-06-03` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/6628 |
| Changed files | 1 |
| Additions / deletions | +84 / -0 |

## Description

## What

C4 cadence item. `Message.marginal` (`Message.fs:308`) is the generic product-fold `Seq.fold ( * ) GenericOne`. This proves, per family (Gaussian / Beta / Bernoulli) via FsCheck, that it is a **monoid homomorphism** from `(list, @, [])` to `(message, *, One)`:

- **identity on empty** — `marginal [] = One` (3 `[<Fact>]`)
- **singleton** — `marginal [m] = m`
- **fold-homomorphism** — `marginal (xs @ ys) = marginal xs * marginal ys`
- **order-independent** — `marginal xs = marginal (rev xs)` (product is commutative — proven C1/C2/C3)

The laws hold by the C1/C2/C3 monoid structure **regardless of properness** (the `gEq*` helpers compare values within tolerance), so no closure gating is needed. Bernoulli lists are capped at 4 — the fold *adds* log-odds, so a longer list saturates `p→0/1` where two regroupings can differ past tolerance. KFL 2001: the marginal is the product of all incoming messages.

## Verification

C4 **6/6**; full `Bayesian.Tests` green; Release build clean (`TreatWarningsAsErrors`).

## Discipline

Per **formal-proof-first**: half-(a) for C4; the hex/4×4 seed-lineage edge (half-b) is still owed before *canonical*. Test-only change (existing generic `marginal`). C12 (codec algebra — new public surface) follows as its own PR.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 4 |
| Resolved threads | 4 |
| Unresolved threads | 0 |
| Total review comments | 4 |
| Total fix commits (touching thread paths) | 2 |

## Review threads

### Thread 1 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GqrOa`
- **Path**: `tests/Bayesian.Tests/Message.Tests.fs`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-06-03T07:39:41Z on `tests/Bayesian.Tests/Message.Tests.fs`:526 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Exercise Bernoulli marginal laws beyond four messages**

  When a Bernoulli variable has more than four incoming messages, production code still folds all of them (`FactorGraph.marginal` has no comparable cap), but this property truncates the generated case before checking homomorphism/order independence. The comment even notes that longer folds can diverge past tolerance; for example seven bounded log-odds messages such as `(8, 7, 8, 4, -8, -8, -8)` expose order/grouping differences above `gEqBern`'s tolerance while this test silently drops the tail. Please either encode C4 as an explicitly bounded law or generate the longer cases and fix/justify the numeric behavior being claimed.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 2 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6Gqrfl`
- **Path**: `tests/Bayesian.Tests/Message.Tests.fs`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-03T07:40:00Z on `tests/Bayesian.Tests/Message.Tests.fs`:479 (association: NONE)

  <pre>
  P2: These C4 identity-on-empty facts are phrased as list laws ((list,@,[])), but the implementation uses `Seq.empty` and asserts via a boolean `gEq* |&gt; should equal true`. Using `List.empty&lt;_&gt;` and asserting equality directly keeps the test aligned with the stated monoid and produces clearer failures.
  </pre>

### Thread 3 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6Gqrf5`
- **Path**: `tests/Bayesian.Tests/Message.Tests.fs`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-03T07:40:00Z on `tests/Bayesian.Tests/Message.Tests.fs`:483 (association: NONE)

  <pre>
  P2: Same point here — prefer `List.empty&lt;_&gt;` + direct equality assertion to match the stated `(list,@,[])` source monoid and avoid `... |&gt; should equal true` on a boolean helper.
  </pre>

### Thread 4 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6Gqrf_`
- **Path**: `tests/Bayesian.Tests/Message.Tests.fs`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-03T07:40:00Z on `tests/Bayesian.Tests/Message.Tests.fs`:487 (association: NONE)

  <pre>
  P2: Same point here — use `List.empty&lt;_&gt;` + direct equality assertion for the identity-on-empty law so the test reads directly as the list-monoid claim.
  </pre>

## Fix commits (touching thread paths)

### `cfa6e7586da5c6a19b248f93c105d54e9d574c0a` -- 2026-06-03T07:37:19Z -- ``

**Touched files (intersect with thread paths):**

- `tests/Bayesian.Tests/Message.Tests.fs`

**Message:**

```
proof(081KT2T2J0008QG0R000YZ3NMY C4): Message.marginal is a monoid homomorphism (list,@,[…

…])→(message,*,One) — FsCheck

C4 cadence item. `Message.marginal` (Message.fs:308) is the generic
product-fold `Seq.fold ( * ) GenericOne`. Proves, per family
(Gaussian/Beta/Bernoulli) via FsCheck, that it is a MONOID HOMOMORPHISM:
  * identity on empty  — marginal [] = One           (3 [<Fact>])
  * singleton          — marginal [m] = m
  * fold-homomorphism  — marginal (xs@ys) = marginal xs * marginal ys
  * order-independent   — marginal xs = marginal (rev xs)  (product
                         commutative, proven C1/C2/C3)

The laws hold by the C1/C2/C3 monoid structure regardless of properness
(gEq* compares values within tolerance), so no closure gating is needed.
Bernoulli lists capped at 4 (the fold ADDS log-odds; longer saturates
p→0/1 where regroupings can differ past tolerance). KFL 2001 — the
marginal is the product of all incoming messages.

Per formal-proof-first: closes acceptance half-(a) for C4; the hex/4×4
seed-lineage edge (half b) is still owed before *canonical*.

VERIFIED LOCALLY: C4 6/6; full Bayesian.Tests green; Release build clean.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `b49e3f3c614615da3ffb0ac792747eb64cee3fdb` -- 2026-06-03T07:42:50Z -- ``

**Touched files (intersect with thread paths):**

- `tests/Bayesian.Tests/Message.Tests.fs`

**Message:**

```
review(081KT2T2J0008QG0R000YZ3NMY C4): address P2 threads — List.empty + direct One equal…

…ity; exercise >4 Bernoulli fold

Codex P2 (@526): the Bernoulli homomorphism property capped the list at 4,
so it never exercised the >4 fold production `marginal` does. Now folds up
to 8 messages, with per-message log-odds bounded to ±2 (p∈[0.12,0.88]) so
the probability-space product (t=∏p, f=∏(1−p), t/(t+f)) stays clear of
p→0/1 saturation and the two regroupings agree within tol.

Copilot P2 (@479/483/487): the identity-on-empty facts used Seq.empty +
`gEq |> should equal true`. Switched to `List.empty<_>` + direct
`should equal One` — reads straight as the (list,@,[]) source-monoid
identity law (marginal [] = One bit-exactly), with clearer failures.

VERIFIED LOCALLY: C4 6/6.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```
