# PR #6643 -- ci(ace): fix-forward — bun install in cross-verify job (z3-solver MODULE_NOT_FOUND)

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 6643 |
| Title | ci(ace): fix-forward — bun install in cross-verify job (z3-solver MODULE_NOT_FOUND) |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-06-03T08:46:32Z |
| Merged at | 2026-06-03T08:56:34Z |
| Merge commit SHA | `340606f8ed340abf8f145bfff2f7e84806626e7f` |
| Branch | `otto-windows/ace-cross-verify-ci-z3-fix-2026-06-03` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/6643 |
| Changed files | 1 |
| Additions / deletions | +10 / -3 |

## Description

## Fix-forward for #6641

#6641 added the `cross-verify` gate.yml job, but it **squash-merged before its own (non-required) job went green** — auto-merge fires on *required* checks only, and `cross-verify` is intentionally non-required (promoting it to required is the operator/devops follow-up). So main's `cross-verify` job now **fails on every PR**:

`bun test tools/ace/` runs `tools/ace/solver.z3.test.ts`, which spawns `node` to `require('z3-solver')` (deliberately **no-skip** per `automated-tests-are-the-shield-assert-dont-skip.md` — it FAILS, not skips, when the dep is absent). The job had no `bun install`, so `z3-solver` is `MODULE_NOT_FOUND`. (It passed locally where `node_modules` is present.)

## Change

Add `bun install --frozen-lockfile` before the ace-suite step (mirrors `lint-tsc-tools`). The cross-verify **oracles** still need no toolchain (they assert committed outputs); only the ace suite's Z3 differential needs the npm dep + the runner's preinstalled node.

## Verification

`actionlint` exit 0; YAML parses; pure-LF; single-file diff. **On this PR the `cross-verify` job runs with the fix**, so a green `cross-verify (trust-core oracles + ace suite)` check IS the confirmation.

Note: this PR's `cross-verify` check still runs against *this branch's* fixed gate.yml, so it should be green; if it's red for the z3 reason that would mean the fix is wrong — watch that specific check.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | true |
| Total threads | 1 |
| Resolved threads | 1 |
| Unresolved threads | 0 |
| Total review comments | 2 |
| Total fix commits (touching thread paths) | 2 |

## Review threads

### Thread 1 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6Gruia`
- **Path**: `.github/workflows/gate.yml`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-03T08:48:17Z on `.github/workflows/gate.yml`:1093 (association: NONE)

  <pre>
  P1: This comment uses a persona name ("Soraya") in a workflow file. Repo convention is to avoid name attribution outside the documented history surfaces and instead use role-refs (see .github/copilot-instructions.md:317-378). Consider replacing with a role description (e.g., "formal-verification").
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-06-03T08:52:59Z on `.github/workflows/gate.yml`:1093 (association: MEMBER)

    <pre>
    Fixed in 2bafc2c1e — replaced the persona name with a role-ref: the comment now reads `the proof half is the formal-coverage cadence / lean-proof lane` (no name attribution in the workflow file, per the copilot-instructions role-ref convention). actionlint exit 0.
    </pre>

## Fix commits (touching thread paths)

### `afc6bd7648505f22a6a4507f32f888a1e71fc5f2` -- 2026-06-03T08:46:16Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `.github/workflows/gate.yml`

**Message:**

```
ci(ace): fix-forward — bun install in cross-verify job (z3-solver MOD…

…ULE_NOT_FOUND on main)

#6641 added the cross-verify job but squash-merged before its own (non-required)
job went green, so main's cross-verify job now FAILS on every PR: `bun test
tools/ace/` runs solver.z3.test.ts, which spawns node to require('z3-solver')
(assert-dont-skip — FAILS, not skips, when absent), and the job had no
`bun install`, so z3-solver is MODULE_NOT_FOUND.

Add `bun install --frozen-lockfile` before the ace-suite step (mirrors
lint-tsc-tools). The cross-verify oracles still need no toolchain; only the ace
suite's Z3 differential needs the npm dep + the runner node (preinstalled).

actionlint exit 0; YAML parses; pure-LF. On THIS PR the cross-verify job runs
with the fix, so a green cross-verify check confirms the fix-forward.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `2bafc2c1ecfd24763a10bf393a34838de3423f04` -- 2026-06-03T08:52:39Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `.github/workflows/gate.yml`

**Message:**

```
ci(ace): role-ref not persona name in cross-verify comment (Copilot P…

…1; copilot-instructions role-ref convention)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```
