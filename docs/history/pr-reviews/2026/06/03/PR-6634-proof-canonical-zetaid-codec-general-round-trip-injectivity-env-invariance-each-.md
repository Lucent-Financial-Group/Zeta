# PR #6634 -- proof(canonical): ZetaId codec — general round-trip + injectivity + env-invariance (each maps to a real code claim)

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 6634 |
| Title | proof(canonical): ZetaId codec — general round-trip + injectivity + env-invariance (each maps to a real code claim) |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-06-03T08:06:27Z |
| Merged at | 2026-06-03T08:09:28Z |
| Merge commit SHA | `1a8dcb97bbb67c28361e2108d5b198254098f3b0` |
| Branch | `otto-cli/zetaid-canonical-roundtrip-injectivity-envinvariance-2026-06-03` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/6634 |
| Changed files | 2 |
| Additions / deletions | +105 / -0 |

## Description

## What

ZetaId already had the **consensus axis** (4-lang byte-lock — `ZetaId/CrossVerifyTests.fs` packs the 12 seed observations, checks `pack obs = expected_hex` ∧ `unpack∘pack=id`; that's the seed-lineage edge). This adds the **proof axis**: the **general** law over *arbitrary* observations, with each property mapped to a claim the code actually makes (no vacuous math):

| Property | Code claim it verifies | Why it's real |
|---|---|---|
| `unpack ∘ pack = id` | `Codec.fs`: "Unpack … Inverse of Pack"; round-trip-stable IDs | the **homeostat** (round-trip fixed point) over the full valid field space (9 fields, bijective 128-bit layout) — consensus-free |
| unpacked obs **invariant to env randomness** | pack injects 32 random bits (`env.NextInt64()`); unpack never reads them → randomness must not bleed into field bits | **novel** — no existing test checks it; a layout bug could let randomness corrupt fields |
| distinct obs → distinct ids (same env) | no field **aliasing** (the no-collision point of an ID) | **non-tautological** — compares the *packed ids* directly (fails if two fields overlap), not a round-trip restatement |

## Verification

**3/3**, 0 skipped; Release build clean. FsCheck (the layout is a structural bijection — Z3 not needed). All three are **consensus-free** F# proofs: they stand on the math, not 4-oracle agreement.

## Discipline

Per formal-proof-first + Aaron 2026-06-03 ("only proofs that actually verify our code against our claims; homeostat chains without heavy consensus"): each property above names the code claim it verifies. The round-trip law is the homeostat; the cross-verify (consensus) is the separate seed-lineage edge. Registry `canonical` marker flip + Soraya ratification is the next step (I supply the proof, not the mark).

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 0 |
| Resolved threads | 0 |
| Unresolved threads | 0 |
| Total review comments | 0 |
| Total fix commits (touching thread paths) | 0 |

## Review threads

(no review threads on this PR)

## Fix commits (touching thread paths)

(no commits in this PR touched files anchored by review threads)
