# PR #8266 -- feat(cart): Dot Runner — the cart IS the proof (deterministic, durable, minimal, non-static)

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 8266 |
| Title | feat(cart): Dot Runner — the cart IS the proof (deterministic, durable, minimal, non-static) |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-06-15T05:50:38Z |
| Merged at | 2026-06-15T05:50:49Z |
| Merge commit SHA | `b0023df9a7caf2d6fd8f3fa7d58f69dd0dd63ffd` |
| Branch | `otto/cart-dot-runner-the-proof` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/8266 |
| Changed files | 4 |
| Additions / deletions | +339 / -0 |

## Description

## What

Aaron 2026-06-15: *"the carts need to be the proof — let's build one."* This turns the Playable-Quotes arc from a doc claim into a **checked artifact**.

New **`Cart`** module (`src/Core/Cart.fs`): a named, content-addressed playable quote = metadata + `(seed, ROM)` recipe + input recording, serialized as **text** (no binary in the proof lineage), with a content-derived id and a "take the controls" continuation. Built **on** the existing substrate (`Chip8Cow` VM, `RecordedSource` input-log, `Chip8Quote` mask-theorem) — with the grain.

The first cart **"Dot Runner"**: hold key 5 to walk a dot right, release to hold; a 32-byte dead tail the mask drops.

## The proof (`Cart.Tests.fs`, 6/6 green)

- **TRUTH** — byte-exact deterministic replay (the frame-exact guarantee Tenmile could *not* make; ours holds by DST / DoP=1).
- **DURABLE** — text round-trips byte-exact + replays identically; serialized form is pure text (tab/newline only).
- **MINIMAL** — the mask theorem holds (untouched bytes provably don't matter); `keptFraction < 0.6` (dead tail gone; Tenmile's ~6% measured).
- **NON-STATIC** — take-the-controls with a novel input **diverges** from the recording (a playable existence, not a video).

## Framing captured in the module doc (all anchored)

Truth + fun coexist as a *consenting game* (not Mad-Men theater); the shared core is the **GCF** (greatest common factor — richest shared structure), **not the LCD**; per-persona experience = **graceful degradation** = capabilities-honest distribution of concerns (CHIP-8 doesn't pretend to be a quantum computer — capability-interface principle); capability is a **distribution of concerns, not a ranking of worth** (equal moral regard, #11); and the humble lens is the **resilience floor** — scale-free degrades N→1 when N collapses; the cart runs on a potato.

## Build

Core: **0 warnings**. Cart proofs: **6/6 pass**.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 0 |
| Resolved threads | 0 |
| Unresolved threads | 0 |
| Total review comments | 0 |
| Total fix commits (touching thread paths) | 0 |

## Review threads

(no review threads on this PR)

## Fix commits (touching thread paths)

(no commits in this PR touched files anchored by review threads)
