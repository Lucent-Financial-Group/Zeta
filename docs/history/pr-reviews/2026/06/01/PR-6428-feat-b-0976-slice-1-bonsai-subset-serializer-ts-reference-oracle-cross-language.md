# PR #6428 -- feat(081KT07NV0008QG0R003BE6MJ2 slice 1): Bonsai-subset serializer — TS reference oracle (cross-language)

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 6428 |
| Title | feat(081KT07NV0008QG0R003BE6MJ2 slice 1): Bonsai-subset serializer — TS reference oracle (cross-language) |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-06-01T15:45:44Z |
| Merged at | 2026-06-01T16:07:34Z |
| Merge commit SHA | `7281f66862064b6163dc88a5d3e47d544f477fea` |
| Branch | `otto-cli/b0976-bonsai-subset-ts-oracle-slice1-2026-06-01` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/6428 |
| Changed files | 5 |
| Additions / deletions | +601 / -1 |

## Description

First slice of the self-evolving-saga build (**081KT07NV0008QG0R003BE6MJ2**): the cross-language **Bonsai-subset expression-tree serializer** the saga primitive needs. TS reference oracle (#1 of TS/F#/C#/Rust) — the algebra-ladder meet-in-the-middle discipline: TS authors the shared golden vectors; F#/C#/Rust replay later.

## What

The **weakly-typed / reflection-info-omitted** Bonsai mode (the cross-language-portable form Nuqleon uses for C++/JS targets) — kind-tagged node set `const · param · lambda · binary · call · cond`, no .NET type table. Nuqleon Bonsai stays the .NET-typed-mode conformance oracle.

- **Canonical byte-exact serializer** — compact JSON, fixed key order per node-kind (`kind` first), integer-only literals (no float-format divergence), standard JSON string escaping, `{"v":1,"expr":<node>}` wrapper. *The* cross-oracle byte-diff contract.
- **`parse` round-trip** — `equals(parse(canonical), expr)` and `serialize(parse(s)) === s` (canonical = fixed point).
- **Structural `equals`** + ergonomic constructors.

## Verification

`src/Core.TypeScript/bonsai/` — `bonsai.ts` + `golden-vectors.json` (8 cases incl. a recursive factorial: cond + recursive `call`) + `bonsai.test.ts` + `index.ts`.

- `bun test bonsai/` → **30 pass / 0 fail** (serialize byte-exact · parse round-trip · canonical fixed point · equals laws · unknown-kind + unsupported-version rejection).
- strict `bun --bun tsc --noEmit -p tsconfig.json` → **0 errors**.

Per `m-acc-multi-oracle`: the compilers don't lie; agreement IS the verification. F#/C#/Rust oracles replay the same `golden-vectors.json` in following slices.

081KT07NV0008QG0R003BE6MJ2 acceptance slice 1: **TS oracle ✅**; F#/C#/Rust pending.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | true |
| Total threads | 10 |
| Resolved threads | 10 |
| Unresolved threads | 0 |
| Total review comments | 10 |
| Total fix commits (touching thread paths) | 3 |

## Review threads

### Thread 1 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GLCcG`
- **Path**: `src/Core.TypeScript/bonsai/bonsai.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-06-01T15:48:14Z on `src/Core.TypeScript/bonsai/bonsai.ts`:117 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Reject lossy integer literals**

  When a persisted vector from another oracle contains a v1 `int` that is not exactly representable as a JS safe integer (for example an int64 emitted by C#/Rust) or is fractional, this silently rounds/truncates it instead of rejecting it. That corrupts the expression and breaks the advertised byte fixed point (`serialize(parse(s)) === s`) for those inputs; validate with `Number.isSafeInteger` before constructing the parsed integer.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 2 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GLCcI`
- **Path**: `src/Core.TypeScript/bonsai/bonsai.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-06-01T15:48:14Z on `src/Core.TypeScript/bonsai/bonsai.ts`:140 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Reject unsupported binary operators**

  For v1 data with an unsupported operator such as `"div"` or a typo, `parse` returns an `Expr` typed as if the operator were a valid `BinOp`. That lets malformed persisted saga data pass the parser and round-trip even though later evaluators and the F#/C#/Rust oracles have no defined semantics for it; validate `o.op` against the declared operator set before returning the node.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 3 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GLE5C`
- **Path**: `src/Core.TypeScript/bonsai/bonsai.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-01T15:50:35Z on `src/Core.TypeScript/bonsai/bonsai.ts`:53 (association: NONE)

  <pre>
  P0: `cint` silently truncates non-integer inputs (and allows NaN/Infinity), which can change program semantics and violate the "integer-only" + byte-exact contract (e.g., 1.9 becomes 1). Prefer rejecting anything that isn’t a finite safe integer and store the exact value.
  </pre>

### Thread 4 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GLE55`
- **Path**: `src/Core.TypeScript/bonsai/bonsai.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-01T15:50:35Z on `src/Core.TypeScript/bonsai/bonsai.ts`:77 (association: NONE)

  <pre>
  P0: `emitConst` currently uses `Math.trunc` for ints, which can silently change values and still permit non-finite / non-safe integers (and `Number#toString()` can emit exponent notation for large values, risking cross-language divergence). Validate and emit the exact integer value.
  </pre>

### Thread 5 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GLE6Q`
- **Path**: `src/Core.TypeScript/bonsai/bonsai.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-01T15:50:36Z on `src/Core.TypeScript/bonsai/bonsai.ts`:117 (association: NONE)

  <pre>
  P0: `parseConst` truncates `int` values (and will coerce non-numbers like strings via `Math.trunc`), which means `parse` can accept non-canonical/non-integer inputs and silently rewrite them. If `parse` is part of the conformance surface, it should reject anything that isn’t a finite safe integer.
  </pre>

### Thread 6 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GLE6k`
- **Path**: `src/Core.TypeScript/bonsai/bonsai.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-01T15:50:36Z on `src/Core.TypeScript/bonsai/bonsai.ts`:142 (association: NONE)

  <pre>
  P1: `parseNode` largely relies on casts, so malformed inputs can either (a) get accepted into an invalid `Expr` (e.g., unknown `op`) or (b) throw generic JS errors (e.g., `o.args.map is not a function`) instead of a Bonsai-specific validation error. Adding minimal runtime checks here will make failures deterministic and easier to diagnose across languages.
  </pre>

### Thread 7 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GLE7M`
- **Path**: `src/Core.TypeScript/bonsai/bonsai.test.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-01T15:50:36Z on `src/Core.TypeScript/bonsai/bonsai.test.ts`:90 (association: NONE)

  <pre>
  P1: The test suite currently covers unknown kind and unsupported version, but not the stricter validation behaviors implied by the canonical contract (e.g., rejecting non-integer ints, unknown binary ops, non-array call args). Adding targeted negative tests will keep the oracle strict and prevent silent acceptance from creeping back in.
  </pre>

### Thread 8 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GLE7j`
- **Path**: `src/Core.TypeScript/bonsai/bonsai.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-01T15:50:36Z on `src/Core.TypeScript/bonsai/bonsai.ts`:132 (association: NONE)

  <pre>
  P1: `parseNode` assumes `expr` is a non-null object; inputs like `{"v":1,"expr":null}` will currently throw a generic `TypeError` instead of a Bonsai validation error. Adding an early object/null check makes failures deterministic and keeps error messages oracle-stable across runtimes.
  </pre>

### Thread 9 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GLE8O`
- **Path**: `src/Core.TypeScript/bonsai/bonsai.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-01T15:50:37Z on `src/Core.TypeScript/bonsai/bonsai.ts`:165 (association: NONE)

  <pre>
  P1: `parseConst` also assumes the value is a non-null object; `{"t":"int","v":...}` is expected, but `null` currently causes a `TypeError` when accessing `o.t`. A small guard keeps parsing errors explicit and consistent.
  </pre>

### Thread 10 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GLOQo`
- **Path**: `src/Core.TypeScript/bonsai/bonsai.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-06-01T15:59:12Z on `src/Core.TypeScript/bonsai/bonsai.ts`:214 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Reject non-canonical parse input**

  When a persisted v1 vector is valid JSON but not the canonical byte form, such as `{"v":1,"expr":{"kind":"param","name":"x","extra":0}}`, this returns an `Expr` and lets `serialize(parse(s))` silently drop or reorder bytes instead of rejecting the vector. That violates the advertised cross-oracle fixed point for `parse` and can let malformed saga data pass the TS oracle while later byte-diff checks disagree; either reject unknown/non-canonical fields during parsing or compare `serialize(parsed)` back to the original string before returning.

  Useful? React with 👍 / 👎.
  </pre>

## Fix commits (touching thread paths)

### `4ee6c1dde70b320db115c4fe6f1cc86db71a8f54` -- 2026-06-01T15:45:31Z -- ``

**Touched files (intersect with thread paths):**

- `src/Core.TypeScript/bonsai/bonsai.test.ts`
- `src/Core.TypeScript/bonsai/bonsai.ts`

**Message:**

```
feat(081KT07NV0008QG0R003BE6MJ2 slice 1): Bonsai-subset serializer — TS reference oracle …

…(cross-language)

First slice of the self-evolving-saga build (081KT07NV0008QG0R003BE6MJ2): the cross-language
expression-tree serializer the saga primitive needs ("serialize the deferred
computation's expression tree"). TS reference oracle (#1 of TS/F#/C#/Rust),
mirroring the algebra-ladder meet-in-the-middle discipline — TS authors the
shared golden vectors; F#/C#/Rust replay later.

The **weakly-typed / reflection-info-omitted** Bonsai mode (the cross-language-
portable form Nuqleon uses for C++/JS targets): kind-tagged node set
const/param/lambda/binary/call/cond, no .NET type table. Canonical byte-exact
serializer (compact JSON, fixed key order per kind, integer-only literals,
standard JSON string escaping, {"v":1,"expr":<node>} wrapper) + parse round-trip
+ structural equals. Nuqleon Bonsai stays the .NET-typed-mode conformance oracle.

`src/Core.TypeScript/bonsai/`: bonsai.ts + golden-vectors.json (8 cases incl. a
recursive factorial — cond + recursive call) + bonsai.test.ts (30 tests:
serialize byte-exact, parse round-trip, canonical fixed point, equals laws,
unknown-kind/version rejection) + index.ts. bun test 30/0; strict
`tsc --noEmit -p tsconfig.json` clean.

081KT07NV0008QG0R003BE6MJ2 acceptance slice 1: TS oracle ✅; F#/C#/Rust pending.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `8cbb322c602009e47828977a93919c2485ad8878` -- 2026-06-01T15:56:46Z -- ``

**Touched files (intersect with thread paths):**

- `src/Core.TypeScript/bonsai/bonsai.test.ts`
- `src/Core.TypeScript/bonsai/bonsai.ts`

**Message:**

```
harden(081KT07NV0008QG0R003BE6MJ2): strict Bonsai-subset parse/construct — reject lossy i…

…nts + unknown ops + malformed shapes (Codex + Copilot review)

Addresses 9 review threads (all real — the four-oracle review flushing genuine
robustness gaps). The serializer's parse + constructors are a conformance
surface; they must reject invalid input deterministically, not silently rewrite.

- INTEGERS (P0/P2): asSafeInt validates finite JS-safe-integer — cint, emitConst,
  parseConst no longer Math.trunc (1.9 -> error, not 1), reject NaN/Infinity, and
  REJECT int64 beyond 2^53 (a value TS can't represent exactly must not be
  silently rounded). v1 int domain = safe-integer range (documented; v2 could
  widen to bigint). Safe integers always stringify to plain digits, so emitted
  bytes stay reproducible across oracles.
- BINARY OPS (P2): asBinOp validates against the 7 known ops — parse no longer
  casts 'div'/typos into a fake BinOp.
- MALFORMED SHAPES (P1): asObject/asString/asArray guards — null expr, null value,
  non-array args, non-string name/kind now throw deterministic bonsai: errors
  instead of generic TypeErrors.
- TESTS (P1): +10 negative tests (non-integer/out-of-range int, unknown op,
  non-array args, null expr/value, non-object doc, non-bool bool).

Canonical bytes UNCHANGED for valid inputs (golden vectors pass; F# oracle #6429
still agrees). bun test 40/0; strict tsc 0 errors.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `ecf814f674a3cc7828dfa482659f7df4d42e9b65` -- 2026-06-01T16:04:11Z -- ``

**Touched files (intersect with thread paths):**

- `src/Core.TypeScript/bonsai/bonsai.test.ts`
- `src/Core.TypeScript/bonsai/bonsai.ts`

**Message:**

```
harden(bonsai-ts): canonical-only parse (reject non-canonical input) …

…for the serialize∘parse fixed point (#6428)

Codex P2: parse advertised the serialize∘parse fixed point but only enforced it
on canonical input — a structurally-valid but non-canonical vector (extra fields,
whitespace, reordered keys) returned an Expr and silently round-tripped to
*different* canonical bytes. For a byte-exact verification oracle that's a real
hole: a non-canonical saga vector would pass this oracle yet fail a peer oracle's
byte-diff (the exact invariant the cross-language oracles exist to guarantee).

Fix (Codex's preferred option): after parseNode, compare serialize(result) back
to the input and reject on mismatch. parse now accepts the canonical byte form
ONLY — extra fields, whitespace, and key reordering are all rejected via one
roundtrip guard, matching the cell's already-strict posture (unknown kind/op/tag,
wrong version, non-safe int, etc.).

Canonical input is unaffected (golden vectors are canonical by construction, so
serialize(parse(canonical)) === canonical passes). +3 negative tests
(extra-field, whitespace, reordered-keys) → 43/43; tsc --noEmit 0 errors.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```
