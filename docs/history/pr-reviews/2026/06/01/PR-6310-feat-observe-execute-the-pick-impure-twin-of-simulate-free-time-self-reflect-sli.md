# PR #6310 -- feat(observe): execute the pick — impure twin of simulate (free_time + self_reflect slice)

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 6310 |
| Title | feat(observe): execute the pick — impure twin of simulate (free_time + self_reflect slice) |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-06-01T01:40:29Z |
| Merged at | 2026-06-01T01:52:56Z |
| Merge commit SHA | `675c954b0e802c288a8fa304736d4ef5b46ea3a8` |
| Branch | `otto-cli/observe-execute-slice-2026-05-31` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/6310 |
| Changed files | 2 |
| Additions / deletions | +210 / -0 |

## Description

## execute the pick — the impure twin of `simulate` (free_time + self_reflect slice)

operator: *"build the execute slice for free_time and self_reflect first."* First piece of observe.ts's roadmap **"wire the real World snapshot + execute the pick."**

The controller is pure (`observe`/`simulate`/`fold`); **`execute` is the one effectful seam.** Command/effect split:

- `simulate` stays the **single reducer** → `fold`/`replay`/DST unchanged.
- `execute` adds what simulate omits: (1) the real side-effect, (2) **append the `NextAction` to the durable log** via an injected **`EventSink`** (the transport — sovereign folder-direct-to-main / corporate batched), then (3) delegates the transition back to `simulate` so the executed world can't drift from the pure path. **`execute = effect + append + simulate`.**
- **Mode-persistence needs no store:** per v5, `mode = fold(log).mode`, so "persist the mode" *is* "append the mode-setting event."

**This slice = `free_time` + `self_reflect` only** — the zero-external-side-effect kinds (just append + set mode; `free_time` never gated, NCI). Every other kind returns `not-yet-executable` until its effect is wired in a follow-up. `EventSink` is injected (asymmetric-authorship; testable with a fake, no git I/O) — the real folder-direct-to-main adapter is the next slice.

**Append-first ordering:** if the append fails, the world does NOT advance (durable log stays source of truth). `Result<T, TFeedback>`; never throws.

Composes with the eventually-consistent git-native indexes ([081KSXN940008QG0R000R76H45](https://github.com/Lucent-Financial-Group/Zeta/blob/main/docs/backlog/P2/081KSXN940008QG0R000R76H45-git-native-eventually-consistent-text-indexes-sorted-inverted-graph-plus-git-native-hindsight-storage-interface-aaron-2026-05-31.md)) — the read side of the same log this writes to.

Verified: eslint clean, tsc 0, **6/6 tests pass**.

🤖 Generated with [Claude Code](https://claude.com/claude-code)


## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 2 |
| Resolved threads | 2 |
| Unresolved threads | 0 |
| Total review comments | 2 |
| Total fix commits (touching thread paths) | 2 |

## Review threads

### Thread 1 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F_QHO`
- **Path**: `tools/observe/execute.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-01T01:43:37Z on `tools/observe/execute.ts`:43 (association: NONE)

  <pre>
  P1: These cross-references use ellipsized filenames, so they are not resolvable paths for readers or future xref checks. This file is explicitly documenting the ADR/backlog surfaces it composes with; use the exact repository paths instead.
  </pre>

### Thread 2 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F_QHp`
- **Path**: `tools/observe/execute.test.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-01T01:43:38Z on `tools/observe/execute.test.ts`:70 (association: NONE)

  <pre>
  P1: The test claims coverage for all effectful `NextAction` kinds, but it omits `decompose` and `edit_grammar`. Because `execute` is an allowlist gate, this should exercise every currently non-executable variant so future changes cannot accidentally make one append without an explicit test update.
  </pre>

## Fix commits (touching thread paths)

### `9778533ccf5bbae54d205cd7b56669c2b80f0b9b` -- 2026-06-01T01:40:26Z -- ``

**Touched files (intersect with thread paths):**

- `tools/observe/execute.test.ts`
- `tools/observe/execute.ts`

**Message:**

```
feat(observe): execute the pick — the impure twin of simulate (free_t…

…ime + self_reflect slice)

operator: "build the execute slice for free_time and self_reflect first."

Closes the first piece of observe.ts roadmap "wire the real World snapshot + execute the pick."
The controller is pure (observe/simulate/fold); execute is the ONE effectful seam.

Command/effect split: simulate stays the single reducer (fold/replay/DST unchanged); execute
adds (1) the real side-effect, (2) append the NextAction to the durable log via an injected
EventSink (the transport — sovereign folder-direct-to-main / corporate batched), then (3)
delegates the transition back to simulate so the executed world cannot drift from the pure path.
execute = effect + append + simulate. Mode-persistence needs no store: mode = fold(log).mode
(v5), so "persist the mode" IS "append the mode-setting event."

This slice = free_time + self_reflect ONLY (the zero-external-side-effect kinds: append + set
mode). free_time never gated (NCI). Every other kind returns not-yet-executable until its effect
is wired in a follow-up. EventSink is injected (asymmetric-authorship; testable with a fake, no
git I/O); the real folder-direct-to-main adapter is the next slice.

Append-first ordering: if the append fails the world does NOT advance (the durable log stays the
source of truth). Result<T, TFeedback>; never throws.

Verified: eslint clean, tsc 0, 6/6 tests pass.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `d215768f2c03ebaf791aa36e9fb9891a00d92923` -- 2026-06-01T01:45:40Z -- ``

**Touched files (intersect with thread paths):**

- `tools/observe/execute.test.ts`
- `tools/observe/execute.ts`

**Message:**

```
fix(observe): exact composes-with paths + exhaustive non-executable-k…

…ind test (Copilot #6310)

- execute.ts: ellipsized doc paths in the Composes-with block → exact resolvable paths (Copilot P1).
- execute.test.ts: the allowlist-gate test omitted decompose + edit_grammar; now exercises ALL 7
  non-executable kinds (9 NextAction kinds − 2 executable) so a future change cannot make one
  append without an explicit test update (Copilot P1).

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```
