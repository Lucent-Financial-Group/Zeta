# PR #6363 -- feat(081KSXN940008QG0R003FCQ7WT): C# G-Set oracle — algebra-ladder bottom rung now 4/4 (Tier-1)

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 6363 |
| Title | feat(081KSXN940008QG0R003FCQ7WT): C# G-Set oracle — algebra-ladder bottom rung now 4/4 (Tier-1) |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-06-01T07:08:05Z |
| Merged at | 2026-06-01T07:21:56Z |
| Merge commit SHA | `d96b37b4db6355944fe37afd58ab86f8ef0c3b4a` |
| Branch | `otto-cli/csharp-gset-2026-06-01` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/6363 |
| Changed files | 3 |
| Additions / deletions | +401 / -0 |

## Description

Completes the **G-Set** rung across all four oracles → **4/4 (TS/F#/C#/Rust)**, Tier-1 (shared golden-vector fixture). The directive: "go ahead with C# G-Set." Tracked by 081KSXN940008QG0R003FCQ7WT.

## What

- `src/Core.CSharp/GSet.cs` — in the C# core next to the Z-set binding (`ZetaCircuitBuilder`), mirroring F#'s `src/Core/GSet.fs` placement (F# has no separate algebra project; neither does the C# core).
  - **Non-generic companion-class** factory pattern (`GSet.Empty`/`Singleton`/`OfSeq`) + sealed generic `GSet<T>` instance methods — the .NET-idiomatic shape (`ImmutableArray`/`ImmutableArray<T>`) that satisfies **CA1000** (no statics on generic types).
  - **Explicit `IComparer<T>`** (like the TS `compare` param) so the cross-verify passes `StringComparer.Ordinal` — `Comparer<string>.Default` is culture-sensitive and would diverge from TS code-unit / Rust byte order beyond ASCII.
- `tests/Tests.CSharp/Algebra/GSetCrossVerifyTests.cs` — reads the shared self-describing `src/Core.TypeScript/g-set/golden-vectors.json` (via `System.Text.Json`, BCL) and asserts every `expectedReplayStates[i]` + `expectedFinalState` (no per-lang output file). Plus the CRDT law tests.

## Proof

```
dotnet build src/Core.CSharp -c Release  → 0 warnings, 0 errors
dotnet test (--filter Algebra)           → Passed! 2/2 (cross-verify + CRDT laws)
```

**G-Set: 4/4** — the algebra ladder's bottom rung is complete across all oracles. Registry flip to Tier-1 follows once #6361 lands. Next: **Bag** (the missing middle) → **Z-set** (TS/Rust) → **bus**.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | true |
| Total threads | 7 |
| Resolved threads | 7 |
| Unresolved threads | 0 |
| Total review comments | 14 |
| Total fix commits (touching thread paths) | 3 |

## Review threads

### Thread 1 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GB85g`
- **Path**: `src/Core.CSharp/GSet.cs`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-06-01T07:09:47Z on `src/Core.CSharp/GSet.cs`:160 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Reject or recanonicalize mismatched comparers**

  When `other` was constructed with a different comparer, its `_items` are not necessarily sorted according to this set's `_comparer`, but the merge loop treats both runs as sorted under `_comparer`. For example, unioning an ordinal string set with a set built using a reverse comparer can return a non-canonical run such as `["b", "c", "a"]`, after which binary-search `Contains("a")` can incorrectly return false. Since the API stores an explicit comparer per set, `Union` should either require compatible comparers or recanonicalize `other` before doing the linear merge.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-06-01T07:14:18Z on `src/Core.CSharp/GSet.cs`:160 (association: MEMBER)

    <pre>
    Fixed — Union now recanonicalizes `other` when its comparer differs from this set's (fast path: same instance), so the merge never sees a run sorted under a different order. New test `UnionRecanonicalizesAMismatchedComparer` proves it (reverse-comparer other → canonical-ascending union, Contains stays correct).
    </pre>

### Thread 2 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GB85m`
- **Path**: `src/Core.CSharp/GSet.cs`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-06-01T07:09:47Z on `src/Core.CSharp/GSet.cs`:234 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Hash comparer-equivalent values consistently**

  `Equals` compares elements with `_comparer`, but `GetHashCode` hashes them with the default equality semantics. With a comparer such as `StringComparer.OrdinalIgnoreCase`, `GSet.OfSeq(["a"], cmp).Equals(GSet.OfSeq(["A"], cmp))` is true while the hashes differ, so equal sets can disappear in `Dictionary&lt;GSet&lt;T&gt;, ...&gt;` / `HashSet&lt;GSet&lt;T&gt;&gt;`. The hash calculation needs to follow the same equivalence relation as `Equals` or the API must prevent comparers whose equality differs from `EqualityComparer&lt;T&gt;.Default`.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-06-01T07:14:21Z on `src/Core.CSharp/GSet.cs`:234 (association: MEMBER)

    <pre>
    Fixed — GetHashCode now follows the comparer-based equality: when the comparer is also IEqualityComparer&lt;T&gt; (e.g. StringComparer.Ordinal) it hashes each element through it; otherwise it hashes count-only (still consistent with the comparer-based Equals, so GSet stays a valid dictionary/set key).
    </pre>

### Thread 3 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GB-7k`
- **Path**: `src/Core.CSharp/GSet.cs`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-01T07:12:14Z on `src/Core.CSharp/GSet.cs`:152 (association: NONE)

  <pre>
  P0: `Union` implicitly assumes both operands use the same comparer. If `other` was built with a different comparer, the merge can produce a run that is not canonical under either order, and `union` can stop being commutative/associative (even `empty.Union(x)` would depend on which empty’s comparer you used). Add an explicit comparer-compatibility check up front and fail fast on mismatch.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-06-01T07:18:49Z on `src/Core.CSharp/GSet.cs`:152 (association: MEMBER)

    <pre>
    Fixed — Union now fails fast (ArgumentException via RequireSameComparer) on comparer mismatch instead of recanonicalizing. The comparer is part of the set's identity, so commutativity holds within a comparer and cross-comparer union is a loud programming error. Test UnionThrowsOnMismatchedComparer covers it.
    </pre>

### Thread 4 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GB-8E`
- **Path**: `src/Core.CSharp/GSet.cs`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-01T07:12:14Z on `src/Core.CSharp/GSet.cs`:260 (association: NONE)

  <pre>
  P0: `Equals` compares elements using this instance’s comparer but does not require the two sets to share the same comparer. That can violate symmetry (`a.Equals(b)` may differ from `b.Equals(a)`) and makes equality depend on which side is "left". Consider comparer equality part of the value semantics and return false when comparers differ.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-06-01T07:18:50Z on `src/Core.CSharp/GSet.cs`:260 (association: MEMBER)

    <pre>
    Fixed — Equals returns false when comparers differ, so it's symmetric (a.Equals(b) == b.Equals(a)). The comparer is now part of value identity.
    </pre>

### Thread 5 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GB-8X`
- **Path**: `tests/Tests.CSharp/Algebra/GSetCrossVerifyTests.cs`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-01T07:12:14Z on `tests/Tests.CSharp/Algebra/GSetCrossVerifyTests.cs`:37 (association: NONE)

  <pre>
  `Strings(...)` uses `GetString()!`, so a malformed fixture yields a null-forgiving NRE instead of a clear assertion failure pointing at the bad element. Since this is a cross-lang conformance test, a more explicit error makes fixture/schema drift much easier to diagnose.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-06-01T07:18:57Z on `tests/Tests.CSharp/Algebra/GSetCrossVerifyTests.cs`:37 (association: MEMBER)

    <pre>
    Fixed — Strings() now throws a clear InvalidOperationException naming the bad element (was GetString()! → NRE), so fixture/schema drift is easy to diagnose.
    </pre>

### Thread 6 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GB-8p`
- **Path**: `src/Core.CSharp/GSet.cs`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-01T07:12:15Z on `src/Core.CSharp/GSet.cs`:285 (association: NONE)

  <pre>
  P0: `GetHashCode` uses `HashCode.Add(x)` (default equality) while `Equals` uses `_comparer` semantics. With comparers where `Compare(a,b)==0` but default equality differs (e.g., `StringComparer.OrdinalIgnoreCase`), this breaks the `Equals`/`GetHashCode` contract and can corrupt hash-based collections. Compute element hashes using an `IEqualityComparer&lt;T&gt;` aligned with the comparer when available, and include the comparer in the hash.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-06-01T07:18:55Z on `src/Core.CSharp/GSet.cs`:285 (association: MEMBER)

    <pre>
    Fixed — GetHashCode now includes the comparer and hashes elements via IEqualityComparer&lt;T&gt; when the comparer is one (e.g. StringComparer.Ordinal), consistent with the comparer-in-identity Equals.
    </pre>

### Thread 7 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GCCjH`
- **Path**: `src/Core.CSharp/GSet.cs`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-06-01T07:16:20Z on `src/Core.CSharp/GSet.cs`:253 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Preserve equality symmetry when comparers differ**

  When two sets were constructed with different comparer semantics, comparing each position with only `this._comparer` breaks the `Equals` contract. For example, `GSet.OfSeq(["a"], StringComparer.OrdinalIgnoreCase).Equals(GSet.OfSeq(["A"], StringComparer.Ordinal))` returns true here, while the reverse comparison returns false; this can make `HashSet&lt;GSet&lt;string&gt;&gt;`/`Dictionary` lookups behave unpredictably. Either require compatible comparers for equality or canonicalize/compare under a symmetric rule before returning true.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-06-01T07:18:52Z on `src/Core.CSharp/GSet.cs`:253 (association: MEMBER)

    <pre>
    Fixed (same change) — comparer is part of identity: Equals returns false on differing comparers, so OrdinalIgnoreCase-vs-Ordinal sets are unequal both directions. Test asserts the symmetric case.
    </pre>

## Fix commits (touching thread paths)

### `aa6d3542aeb1ba9a9fd60238a8714e343d64a0bb` -- 2026-06-01T07:08:00Z -- ``

**Touched files (intersect with thread paths):**

- `src/Core.CSharp/GSet.cs`
- `tests/Tests.CSharp/Algebra/GSetCrossVerifyTests.cs`

**Message:**

```
feat(081KSXN940008QG0R003FCQ7WT): C# G-Set oracle — algebra-ladder bottom rung now 4/4 (T…

…ier-1)

Adds src/Core.CSharp/GSet.cs (in the C# core next to the Z-set binding, mirroring how
F# keeps GSet.fs in src/Core/). Non-generic companion-class factory pattern (GSet.Empty/
Singleton/OfSeq) + sealed generic GSet<T> instance methods (Contains via binary search,
Union merge, Add idempotent, value-equality) — the .NET-idiomatic shape (like
ImmutableArray/ImmutableArray<T>) that satisfies CA1000. Comparer is explicit (IComparer<T>)
so the cross-verify can pass StringComparer.Ordinal — matching the fixture's ordinal/
code-point order (Comparer<string>.Default is culture-sensitive; would diverge from
TS/Rust beyond ASCII).

Test (tests/Tests.CSharp/Algebra/, xUnit) reads the shared self-describing fixture
src/Core.TypeScript/g-set/golden-vectors.json and asserts every replay state + final
(no per-lang output file). Verified:

  dotnet build Core.CSharp -c Release  -> 0 warnings, 0 errors
  dotnet test (filter Algebra)         -> Passed! 2/2 (cross-verify + CRDT laws)

G-Set: now 4/4 (TS/F#/C#/Rust) with a shared golden-vector fixture → Tier-1. The
algebra ladder's bottom rung is complete across all four oracles; Bag + Z-set next.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `a5cac60cfdba5f0e3fa3516f9f76af45c8307c7c` -- 2026-06-01T07:14:16Z -- ``

**Touched files (intersect with thread paths):**

- `src/Core.CSharp/GSet.cs`
- `tests/Tests.CSharp/Algebra/GSetCrossVerifyTests.cs`

**Message:**

```
fix(081KSXN940008QG0R003FCQ7WT): harden C# GSet per PR review — recanonicalize mismatched…

… comparer + comparer-consistent hash

Two verified P2 findings on #6363:
1. Union assumed 'other' was sorted under THIS comparer; a different comparer produced a
   non-canonical run (binary-search Contains then broke). Now recanonicalizes 'other'
   when comparers differ (fast path: same instance). Canonicalize extracted to the
   non-generic companion (reused by OfSeq + Union; no CA1000 on the generic type).
2. Equals used the comparer but GetHashCode used default hashing — broke the
   Equals/GetHashCode contract for comparers like OrdinalIgnoreCase. GetHashCode now
   hashes via the comparer when it's also IEqualityComparer<T>, else count-only (still
   consistent with comparer-based equality).

New test proves the recanonicalize fix (reverse-comparer 'other' → canonical-ascending
union, Contains stays correct). dotnet test: Passed 3/3.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `e038266e99246c7e675cdf4ec3fa19c83f6d3146` -- 2026-06-01T07:18:46Z -- ``

**Touched files (intersect with thread paths):**

- `src/Core.CSharp/GSet.cs`
- `tests/Tests.CSharp/Algebra/GSetCrossVerifyTests.cs`

**Message:**

```
fix(081KSXN940008QG0R003FCQ7WT): comparer is part of GSet identity — Union fail-fast, sym…

…metric Equals, consistent hash (PR review 2026-06-01)

Copilot escalated the comparer concern to P0; the recanonicalize approach made Union
internally consistent but NOT commutative across comparers (a∪b ≠ b∪a). Cleaner fix:
treat the comparer as part of the set's value identity.

- Union: fail-fast (ArgumentException) on comparer mismatch via RequireSameComparer
  (was: silent recanonicalize) — preserves commutativity within a comparer.
- Equals: false when comparers differ → symmetric (a.Equals(b) == b.Equals(a)).
- GetHashCode: includes the comparer + (elements via IEqualityComparer when available)
  → consistent with the comparer-in-identity Equals.
- Test: Strings() gives a clear error on a non-string fixture element (was GetString()!
  NRE); recanonicalize test → UnionThrowsOnMismatchedComparer (+ symmetric-Equals check).

dotnet build 0/0; dotnet test Passed 3/3 (cross-verify + CRDT laws + throws-on-mismatch).

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```
