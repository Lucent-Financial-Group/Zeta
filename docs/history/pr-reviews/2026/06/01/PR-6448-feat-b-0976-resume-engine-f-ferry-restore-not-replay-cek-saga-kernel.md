# PR #6448 -- feat(081KT07NV0008QG0R003BE6MJ2): resume engine — F# ferry (restore-not-replay CEK saga kernel)

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 6448 |
| Title | feat(081KT07NV0008QG0R003BE6MJ2): resume engine — F# ferry (restore-not-replay CEK saga kernel) |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-06-01T18:22:25Z |
| Merged at | 2026-06-01T18:37:44Z |
| Merge commit SHA | `701cdf6fa9c7ef691e26bce9539fb9a4b850c05f` |
| Branch | `otto-cli/b0976-resume-engine-fsharp-ferry-2026-06-01` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/6448 |
| Changed files | 4 |
| Additions / deletions | +765 / -0 |

## Description

F# oracle (#2 of TS/F#/C#/Rust) for the resume-engine slice — ferry of the TS reference (#6446). `src/Core/Resume.fs` is a small-step **CEK machine** over the Bonsai-subset `Expr`: `Call` = activity suspension point; the serializable `SagaState` = continuation + pending activity; `resume` **restores** the continuation (prior activities never re-invoked). Replays the shared `resume-golden.json` — same suspension sequence + final value, restore-not-replay at each suspension.

Carries the #6446 review hardening by construction: `Map.tryFind` own-key param lookup (no prototype leak), safe-int guard on arithmetic + restore, op validated on restore. **7 tests** (golden-trace replay across all 5 traces + Unbound / TypeMismatch / UnsupportedNode / NonSafeInt / round-trip / MalformedState), Core builds 0 warnings.

⚠️ **Stacked on #6446** (which adds `resume-golden.json` + the TS reference). The diff shows #6446's commits until it merges; **not armed** so the TS work can't merge under this PR — I'll rebase clean off `main` + arm once #6446 lands.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 3 |
| Resolved threads | 3 |
| Unresolved threads | 0 |
| Total review comments | 3 |
| Total fix commits (touching thread paths) | 2 |

## Review threads

### Thread 1 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GNjx0`
- **Path**: `src/Core/Resume.fs`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-01T18:27:30Z on `src/Core/Resume.fs`:466 (association: NONE)

  <pre>
  P0: `parseState` uses `JsonDocument.Parse` with the default MaxDepth (64). Since a persisted state embeds Bonsai-serialized Expr values, this will reject perfectly valid states containing expressions deeper than 64 (Bonsai's v1 contract allows nesting up to `Bonsai.MaxDepth` = 1024). Use `JsonDocumentOptions(MaxDepth = ...)` similar to `Bonsai.parse` so restore works for deep-but-valid programs.
  </pre>

### Thread 2 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GNjyd`
- **Path**: `src/Core/Resume.fs`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-01T18:27:30Z on `src/Core/Resume.fs`:126 (association: NONE)

  <pre>
  P0: `Mul` computes `asInt left * asInt right` as an `int64` before the safe-int bounds check. Two inputs that are each within the JS-safe-int domain can still overflow `int64` on multiplication (e.g. ~9e15 * ~9e15), wrapping to an in-range value and silently producing the wrong result (and potentially a persistable-but-incorrect state). Add an overflow-free precheck against the safe-int bound before multiplying.
  </pre>

### Thread 3 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GNjyv`
- **Path**: `src/Core/Resume.fs`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-01T18:27:31Z on `src/Core/Resume.fs`:222 (association: NONE)

  <pre>
  `serializeState` uses `JsonSerializer.Serialize` to escape strings (`jstr`). In this codebase, Bonsai’s canonical JSON emission uses a custom JSON-string escaper to match `JSON.stringify` exactly (including surrogate-pair handling). Using `JsonSerializer` here can change the byte form of persisted resume states (e.g., escaping astral characters as `\uXXXX\uXXXX`), which undermines cross-oracle/cross-machine durability and makes state bytes inconsistent with the TS reference.
  </pre>

## Fix commits (touching thread paths)

### `3e5934a4288f7f8379379f1a29ae64a5202fb878` -- 2026-06-01T18:22:21Z -- ``

**Touched files (intersect with thread paths):**

- `src/Core/Resume.fs`

**Message:**

```
feat(081KT07NV0008QG0R003BE6MJ2): resume engine — F# ferry (restore-not-replay CEK saga k…

…ernel)

Ferry of the TS reference (#6446) to the F# oracle (#2 of TS/F#/C#/Rust). Replays the shared
resume-golden.json saga traces — same suspension sequence + final value (the cross-language
behavioral lock), restore-not-replay at every suspension.

- src/Core/Resume.fs (module Zeta.Core.Resume): small-step CEK machine over the Bonsai-subset
  Expr; Call = activity suspension point; serializable SagaState = kont continuation + pending
  activity; resume restores the continuation (prior activities never re-invoked). Reuses
  Bonsai serialize/parse for embedded exprs. Result over throw (ResumeFeedback: Unbound /
  TypeMismatch / UnsupportedNode / NonSafeInt / MalformedState). Carries the #6446 review
  hardening by construction: Map.tryFind = own-key param lookup (no prototype leak); safe-int
  guard on arithmetic + restore; op validated on restore (binOpOfString). Slice-1 scope
  (const/param/binary/cond/call; lambda declines UnsupportedNode).
- 7 tests: golden-trace replay (all 5 traces, persist+restore each suspension) + Unbound /
  TypeMismatch / UnsupportedNode / NonSafeInt / round-trip / MalformedState(junk+version+
  tampered-op+unsafe-int). Core builds 0 warnings.

STACKED on #6446 (the golden + TS reference); retarget/rebase to main once #6446 lands.

Co-Authored-By: Claude <noreply@anthropic.com>
```

### `259f8bc33fa205a562bfdb49d8d44f9a1bbbb49b` -- 2026-06-01T18:34:44Z -- ``

**Touched files (intersect with thread paths):**

- `src/Core/Resume.fs`

**Message:**

```
fix(resume-fsharp): parseState depth ceiling + Mul int64-overflow gua…

…rd + JSON.stringify-matching state escaper

Three Copilot review findings on the F# resume ferry, all valid (two P0):

1. P0 — parseState used JsonDocument's default MaxDepth (64). A persisted
   state embeds Bonsai-serialized Exprs INLINE (up to Bonsai.MaxDepth=1024),
   wrapped in state levels, so a deep-but-valid program was rejected. Now
   tokenizes with Bonsai.MaxDepth*4 headroom (mirrors Bonsai.parse).

2. P0 — applyBinOp Mul computed `asInt l * asInt r` in int64 BEFORE the
   safe-int check. F# int64 silently WRAPS on overflow (unlike JS floats,
   which lose precision and stay caught by Number.isSafeInteger — so TS was
   immune). 4294967296^2 = 2^64 wraps to exactly 0 and would have returned a
   silently-wrong CInt 0. Add/Sub/Mul now compute in bigint first, then
   range-check + narrow (toSafe). Overflow-free.

3. escaper — serializeState used JsonSerializer.Serialize for strings, which
   escapes more than JS JSON.stringify ('<' -> <, astral -> surrogate
   \u escapes), so F# state bytes diverged from the TS reference. Replaced
   with the surrogate-aware JSON.stringify-matching escaper (mirrors
   Bonsai's embedded-expr escaper) so state restores byte-for-byte across
   the TS/F#/C#/Rust oracles — the point of the resume ferry.

+3 regression tests: silent-wrong mul (2^64 -> 0), deep-embedded-program
restore (past depth 64), and JSON.stringify-vs-JsonSerializer escaping.
All 10 Resume tests pass; Core builds 0/0.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```
