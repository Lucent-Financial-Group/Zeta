# PR #6346 -- chore(setup): declare podman as default OCI container runtime (brew/apt/windows)

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 6346 |
| Title | chore(setup): declare podman as default OCI container runtime (brew/apt/windows) |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-06-01T05:14:33Z |
| Merged at | 2026-06-01T05:34:01Z |
| Merge commit SHA | `23ec9cb04fa96dd700fea4d369223c47becc4245` |
| Branch | `otto-cli/podman-oci-deps-2026-06-01` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/6346 |
| Changed files | 4 |
| Additions / deletions | +37 / -0 |

## Description

## What

Declares **podman** as the default OCI container runtime across all three platform manifests, for the effectful `do_item` executor's real-work tier (081KT07NV0008QG0R001CBQ2X2 §2 — "the first time the item needs real tools, stop simulating").

| Manifest | Line |
|---|---|
| `tools/setup/manifests/brew` | `podman` (macOS; one-time `podman machine init && start` Linux-VM bootstrap) |
| `tools/setup/manifests/apt` | `podman` (Linux; native, no VM) |
| `tools/setup/manifests/windows` | `podman  winget=RedHat.Podman` (WSL2 machine; scoop primary) |

## Why podman (huddle 2026-06-01: gemini + grok + amara)

OCI-standard, **Apache-2.0, $0, rootless, daemonless**. The executor reads `ZETA_CONTAINER_RUNTIME` (podman|docker|nerdctl|finch) — **runtime-swappable**, so Docker Desktop coexists for maintainers who already run it (separate VM, separate socket, no conflict; don't install `podman-mac-helper`/`podman-docker` which would shadow the docker CLI).

- NVIDIA GPU on the cluster is **runtime-neutral** via the NVIDIA Container Toolkit + CDI — same `--device nvidia.com/gpu=all` on podman and docker (no docker lock-in for GPUs).
- Version **unpinned** (fast-mover CLI; brew/scoop default) per pin-only-slow-movers.
- Compose stays at the infra layer (Compose Spec), not the executor — a follow-up note.

## Verification

- `manifest-symmetry.test.ts` 3/3 ✓ (brew/apt tool ⇒ present in windows)
- Installed + smoke-tested on the maintainer's Mac: podman 5.8.2, `podman machine` up, ran a real alpine container (`uname=Linux aarch64`), coexisting with the pre-existing Docker 29.4.3 — no conflict.

## Follow-up (same session)

Fold the OCI-abstraction + compose-at-infra into 081KT07NV0008QG0R001CBQ2X2 §2 + preserve the docker/podman huddle verbatim in a review doc.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | true |
| Total threads | 5 |
| Resolved threads | 5 |
| Unresolved threads | 0 |
| Total review comments | 6 |
| Total fix commits (touching thread paths) | 3 |

## Review threads

### Thread 1 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GAu-Y`
- **Path**: `tools/setup/manifests/apt`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-06-01T05:17:03Z on `tools/setup/manifests/apt`:38 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Declare Podman's rootless helper packages**

  On a fresh minimal Ubuntu install this line is consumed by `tools/setup/linux.sh`, which runs `apt-get install -y --no-install-recommends` (checked at `tools/setup/linux.sh:92`). In Ubuntu 24.04 package metadata, Podman's rootless support packages (`uidmap`, `slirp4netns`, `passt`, `dbus-user-session`) are only `Recommends`, so they are skipped here even though the manifest documents the runtime as rootless/daemonless. The first non-root executor run can then fail or lose rootless networking; add the required helper packages explicitly to this manifest or stop suppressing recommends for this install path.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 2 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GAvhN`
- **Path**: `tools/setup/manifests/windows`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-01T05:18:18Z on `tools/setup/manifests/windows`:40 (association: NONE)

  <pre>
  The comment refers to `dep-pin-search-first`, but the repo’s established rule name is `dep-pin-search-first-authority` (used earlier in this file and elsewhere). Using the canonical name keeps the cross-reference searchable and avoids drifting to a non-existent identifier.
  </pre>

### Thread 3 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GAvhc`
- **Path**: `tools/setup/manifests/brew`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-01T05:18:18Z on `tools/setup/manifests/brew`:43 (association: NONE)

  <pre>
  This manifest comment includes named attribution (`huddle 2026-06-01: gemini+grok+amara`) and asserts behavior that isn’t substantiated in-tree (e.g., “Phase-2 executor starts it lazily”). To keep current-state manifests minimal and durable, prefer a short, source-of-truth comment focused on what the installer actually does / what the operator must run.
  </pre>

### Thread 4 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GAvho`
- **Path**: `tools/setup/manifests/apt`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-01T05:18:18Z on `tools/setup/manifests/apt`:41 (association: NONE)

  <pre>
  This comment mentions `ZETA_CONTAINER_RUNTIME`, but that identifier does not currently exist anywhere in the repo. Keeping an unimplemented env var in a system-package manifest is misleading and likely to drift; either implement it where the executor selects a runtime, or remove it from the manifest commentary for now.
  </pre>

### Thread 5 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GA0_v`
- **Path**: `tools/setup/manifests/windows`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-06-01T05:27:44Z on `tools/setup/manifests/windows`:41 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Set Chocolatey override for Podman CLI**

  When this manifest is installed on a host where the resolver falls through to Chocolatey, `install.ps1` defaults `$cid` to the scoop id unless a `choco=` override is present. Chocolatey publishes the Podman CLI package as `podman-cli`, not `podman`, so this required line will run `choco install podman` and fail instead of using the declared fallback; add `choco=podman-cli` or mark Chocolatey unsupported for this tool.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-06-01T05:31:00Z on `tools/setup/manifests/windows`:41 (association: MEMBER)

    <pre>
    Fixed in 56fc2c099. You're right — `install.ps1` defaults `$cid` to the scoop id when no `choco=` override is present, so a choco-only host would run `choco install podman` which 404s. The Chocolatey CLI package is `podman-cli` (v5.8.0, community.chocolatey.org/packages/podman-cli); `podman-desktop` is the separate GUI. Set explicit `choco=podman-cli`. scoop stays primary (`podman`), winget `RedHat.Podman` unchanged. manifest-symmetry 3/3.

    (Separately: the macos-26 `build-and-test` failure on the prior commit was a transient DNS flake — `curl (6) Could not resolve host: pkg-containers.githubusercontent.com` while downloading the unrelated `hermes-agent` brew bottle, not podman/this change. The fresh push re-runs it.)
    </pre>

## Fix commits (touching thread paths)

### `02899964d7b54bc2281931292004744a9d38a798` -- 2026-06-01T05:14:15Z -- ``

**Touched files (intersect with thread paths):**

- `tools/setup/manifests/apt`
- `tools/setup/manifests/brew`
- `tools/setup/manifests/windows`

**Message:**

```
chore(setup): declare podman as the default OCI container runtime (br…

…ew/apt/windows)

The effectful do_item executor (081KT07NV0008QG0R001CBQ2X2 §2) needs a real container surface for
the 'first time the item needs real tools, stop simulating' tier. Per the
docker-vs-podman huddle (gemini+grok+amara, 2026-06-01) podman is the DEFAULT:
Apache-2.0, $0, rootless, daemonless. The executor reads ZETA_CONTAINER_RUNTIME
(podman|docker|nerdctl|finch) so Docker Desktop coexists for maintainers who
already run it — separate VM, separate socket, no conflict.

Declared symmetrically across all three platform manifests (manifest-symmetry
test stays green, 3/3):
  - brew:    podman  (macOS; one-time `podman machine init && start` bootstrap)
  - apt:     podman  (Linux; native, no VM; NVIDIA via CDI is runtime-neutral)
  - windows: podman winget=RedHat.Podman  (WSL2 machine; scoop primary)

Version unpinned (fast-mover CLI; brew/scoop default) per pin-only-slow-movers.
Folding the OCI-abstraction + compose-at-infra into 081KT07NV0008QG0R001CBQ2X2 §2 + preserving the
docker/podman huddle verbatim is the immediate follow-up.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `3b02ab6e2a1fc49cb74fffcb6b63f315e093a29d` -- 2026-06-01T05:25:11Z -- ``

**Touched files (intersect with thread paths):**

- `tools/setup/manifests/apt`
- `tools/setup/manifests/brew`
- `tools/setup/manifests/windows`

**Message:**

```
chore(setup): trim podman manifest comments to current-state + declar…

…e rootless helpers (PR review #6346)

Folds the manifest review:
- Copilot: keep manifests minimal/current-state — drop AI-peer names, drop the
  not-yet-implemented ZETA_CONTAINER_RUNTIME assertion + 'Phase-2 lazily' future-
  claims; point to 081KT07NV0008QG0R001CBQ2X2 §2 for rationale. Fix dep-pin-search-first ->
  dep-pin-search-first-authority (canonical rule name).
- Codex P2 (apt): linux.sh installs with --no-install-recommends, which skips
  podman's Recommends — declare uidmap + slirp4netns + fuse-overlayfs so podman
  actually RUNS rootless on minimal Ubuntu (per assert-don't-skip: a green install
  that can't run a container is a hole in the shield). +3 WINDOWS_EXCEPTIONS
  (linux-only rootless helpers; Windows podman uses WSL2's VM).

manifest-symmetry 3/3, tsc clean.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `56fc2c099d4ca77c33a0d8bd98a5cb4ebce28e28` -- 2026-06-01T05:30:49Z -- ``

**Touched files (intersect with thread paths):**

- `tools/setup/manifests/windows`

**Message:**

```
fix(setup): set choco=podman-cli for Podman CLI on Windows (Codex P2 …

…#6346)

Codex caught a real bug: install.ps1 defaults $cid to the scoop id when no choco=
override is present, so a choco-only Windows host would run 'choco install podman'
— which 404s. The Chocolatey CLI package is 'podman-cli' (v5.8.0; community.
chocolatey.org/packages/podman-cli); 'podman-desktop' is the separate GUI. Set the
explicit choco=podman-cli override. scoop stays primary (id 'podman'); winget
RedHat.Podman unchanged. manifest-symmetry 3/3 (first token still 'podman').

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```
