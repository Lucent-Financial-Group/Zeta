# PR #6330 -- fix(ace): slice-3 post-merge hardening — trust-store perms + trust-add validation + ESM imports

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 6330 |
| Title | fix(ace): slice-3 post-merge hardening — trust-store perms + trust-add validation + ESM imports |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-06-01T03:27:14Z |
| Merged at | 2026-06-01T03:57:27Z |
| Merge commit SHA | `c2a3c71c5fa6a2dad032bca2a004422f99c43852` |
| Branch | `otto-windows/ace-slice3-postmerge-fixes-2026-06-01` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/6330 |
| Changed files | 8 |
| Additions / deletions | +245 / -85 |

## Description

Fix-forward for 3 post-merge review findings on Ace slice 3 (#6320, now on main).

- **Security (Codex P2):** `addTrustedKey` could leave `~/.ace/trusted-keys.json` group/world-**writable** under a permissive umask → another local user injects a key → signs packages you'd trust. Now creates `~/.ace` `0700` + `chmod 0600` the trust file on every call (corrects a pre-existing permissive file too; `writeFileSync` mode only applies on create).
- **Robustness (Copilot):** `ace trust add` with a `.pub` JSON missing `public_key` → `undefined` → `Buffer.from(undefined)` threw a fatal out of `main`. Now validates the key parses as an Ed25519 SPKI (`createPublicKey`) before persist → clean exit 65 on invalid/garbage input.
- **Consistency (Copilot):** slice-3 tests used `require("node:fs")` (the idiom slice-2 review closed) → re-pointed to ESM imports.

Tests 68 → **72** (added: trust-file owner-only perms + pre-existing-permissive correction; trust-add missing-field + garbage-input clean-exit). tsc clean; canary 67.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | true |
| Total threads | 9 |
| Resolved threads | 9 |
| Unresolved threads | 0 |
| Total review comments | 18 |
| Total fix commits (touching thread paths) | 6 |

## Review threads

### Thread 1 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F_3xz`
- **Path**: `tools/ace/ace.ts`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-06-01T03:29:18Z on `tools/ace/ace.ts`:310 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Reject non-Ed25519 SPKI keys**

  When `ace trust add` is given any valid non-Ed25519 SPKI, for example an RSA public key exported as SPKI DER/base64, `createPublicKey` succeeds and the key is persisted even though package signatures are always verified as Ed25519. This leaves the operator with a trusted key ID that can never authenticate packages, so check the returned key object's `asymmetricKeyType === "ed25519"` before calling `addTrustedKey`.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-06-01T03:38:01Z on `tools/ace/ace.ts`:310 (association: MEMBER)

    <pre>
    Fixed in ed0a6365e — trust add now asserts pub.asymmetricKeyType === 'ed25519' after createPublicKey (which accepts RSA/EC SPKI too); a non-Ed25519 key returns a clean exit 65 instead of persisting a key that could never verify. Test: trust add of an EC P-256 SPKI -&gt; exit 65.
    </pre>

### Thread 2 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F_3x0`
- **Path**: `tools/ace/store.ts`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-06-01T03:29:18Z on `tools/ace/store.ts`:205 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Repair trust-file permissions before duplicate return**

  If a user already has a permissive trust file that contains the same key, such as one created by the previous version or under a bad umask, this new chmod is skipped because the function returns at the duplicate check. Re-running `ace trust add` then reports the key is already trusted while leaving group/other bits intact, including world-writable modes, so perform the permission repair before the duplicate return or on every add attempt.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-06-01T03:38:03Z on `tools/ace/store.ts`:205 (association: MEMBER)

    <pre>
    Fixed in ed0a6365e — addTrustedKey now chmods the trust file to 0600 on EVERY call, including before the duplicate early-return, so re-adding an already-trusted key on a pre-existing permissive file repairs the perms. Test: dedup path on a 0o644 file -&gt; {added:false} AND mode &amp; 0o077 === 0.
    </pre>

### Thread 3 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F_4LY`
- **Path**: `tools/ace/ace.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-01T03:30:15Z on `tools/ace/ace.ts`:310 (association: NONE)

  <pre>
  P1: The validation still accepts non-Ed25519 SPKI keys. `createPublicKey` parses any supported SPKI (for example RSA/EC), and `keyId()` will then persist it under an `ed25519:` id even though it can never verify Ed25519 signatures. Check the parsed key type before persisting so `trust add` enforces the Ed25519 contract advertised by the error message and `.pub` format.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-06-01T03:39:53Z on `tools/ace/ace.ts`:310 (association: MEMBER)

    <pre>
    Already addressed in ed0a6365e (this review appears to be of the prior commit): ace.ts:313 checks pub.asymmetricKeyType !== 'ed25519' and returns a clean exit 65, so RSA/EC SPKI keys are rejected (not persisted under an ed25519: id). Test: trust add of an EC P-256 SPKI -&gt; exit 65. Resolving as stale.
    </pre>

### Thread 4 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F_4Ll`
- **Path**: `tools/ace/store.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-01T03:30:15Z on `tools/ace/store.ts`:192 (association: NONE)

  <pre>
  P1: This only applies `0o700` when the directory is created. If `~/.ace` already exists with group/world write bits, another local user can still replace entries in that directory even though `trusted-keys.json` is chmodded to `0o600`, so the pre-existing-permission hardening remains incomplete. Tighten the directory after `mkdirSync` the same way the file is tightened after write.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-06-01T03:39:48Z on `tools/ace/store.ts`:192 (association: MEMBER)

    <pre>
    Fixed — addTrustedKey now chmods BOTH the ~/.ace dir (0o700) and the trust file (0o600) on every call (mkdirSync mode only applies on create). A pre-existing permissive dir is tightened on dedup + add paths. Test: pre-existing 0o777 ~/.ace -&gt; dir mode &amp; 0o077 === 0 after addTrustedKey. 75 tests pass.
    </pre>

### Thread 5 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F_76Q`
- **Path**: `tools/ace/store.ts`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-06-01T03:39:56Z on `tools/ace/store.ts`:198 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Repair the existing trust directory permissions**

  When `~/.ace` already exists with group/other write bits, such as from an earlier `ace install` or `trust add` run under a permissive umask, this `mkdirSync(..., { recursive: true, mode: 0o700 })` does not change the mode of the existing directory. That leaves any local user with directory write permission able to unlink/replace `trusted-keys.json` despite the new file-level `chmod 0600`, so the local key-injection hardening still fails for the same pre-existing-store scenario unless `dirname(userPath)` is also chmod-repaired.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-06-01T03:41:36Z on `tools/ace/store.ts`:198 (association: MEMBER)

    <pre>
    Already fixed in 920df743e (this review is of the prior commit). addTrustedKey now repairs the EXISTING dir: repairPerms() does chmodSync(dir, 0o700) on every call (store.ts:192, invoked on both the dedup path :197 and the add path :203), not just on mkdirSync-create. A pre-existing permissive ~/.ace is tightened to owner-only. Test: pre-existing 0o777 ~/.ace -&gt; dir mode &amp; 0o077 === 0 after addTrustedKey. Resolving as stale.
    </pre>

### Thread 6 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F_9JG`
- **Path**: `tools/ace/store.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-01T03:42:53Z on `tools/ace/store.ts`:203 (association: NONE)

  <pre>
  P1: The pre-existing `~/.ace` directory is still tightened only after the trust file has already been read and rewritten. If the directory is already group/world-writable, another local user can replace `trusted-keys.json` during that window, so the hardening does not close the injection race it is intended to fix. Create/repair the directory before `readKeysFile` and before `writeFileSync`, then keep the post-write repair for the file mode.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-06-01T03:44:31Z on `tools/ace/store.ts`:203 (association: MEMBER)

    <pre>
    Fixed — good catch on the ordering. addTrustedKey now mkdir+chmod the ~/.ace dir to 0o700 FIRST, before readKeysFile and before writeFileSync, so a pre-existing group/world-writable dir can't be used to swap trusted-keys.json during the read/write window. File mode (0o600) is still tightened after each write. Closes the TOCTOU injection race.
    </pre>

### Thread 7 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GABIz`
- **Path**: `docs/backlog/P2/081KT07NV0008QG0R0016XR9J1-ace-slice3.1-pave-the-strict-default-road-2026-06-01.md`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-01T03:50:50Z on `docs/backlog/P2/081KT07NV0008QG0R0016XR9J1-ace-slice3.1-pave-the-strict-default-road-2026-06-01.md`:80 (association: NONE)

  <pre>
  P1: This sentence now says the rename was resolved, but the following lines still describe it as a deferred CLI-contract decision where only the help-text gloss was applied. That makes the backlog row internally contradictory after the flag rename.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-06-01T03:54:23Z on `docs/backlog/P2/081KT07NV0008QG0R0016XR9J1-ace-slice3.1-pave-the-strict-default-road-2026-06-01.md`:80 (association: MEMBER)

    <pre>
    Fixed — removed the contradiction: the trailing 'deferred to the operator / help-text gloss only' clause is gone; 081KT07NV0008QG0R0016XR9J1 now consistently states the rename shipped across code + tests + docs.
    </pre>

### Thread 8 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GABI3`
- **Path**: `docs/agendas/ace-package-manager/2026-06-01-ace-cli-slice3-authenticity-signature-verify-design.md`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-01T03:50:50Z on `docs/agendas/ace-package-manager/2026-06-01-ace-cli-slice3-authenticity-signature-verify-design.md`:127 (association: NONE)

  <pre>
  P1: The design still documents only three failure reasons from `verifySignature`, but the implementation also returns `unsupported-algo` for a present signature whose `algo` is not `ed25519` (`tools/ace/signing.ts:64-65`). Since this PR updates the flag semantics in the same API contract, include that reason here so the design matches the gate behavior and tests.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-06-01T03:54:20Z on `docs/agendas/ace-package-manager/2026-06-01-ace-cli-slice3-authenticity-signature-verify-design.md`:127 (association: MEMBER)

    <pre>
    Fixed — design §6 now lists unsupported-algo as the 4th verifySignature reason (present signature whose algo != ed25519 -&gt; hard refuse, not overridable), plus a gate bullet, matching signing.ts + the algorithm-confusion tests.
    </pre>

### Thread 9 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GABM7`
- **Path**: `tools/ace/ace.ts`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-06-01T03:50:59Z on `tools/ace/ace.ts`:313 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Normalize SPKI bytes before computing trusted key IDs**

  Because `createPublicKey` accepts an Ed25519 SPKI with trailing DER bytes but re-exports the canonical 44-byte key, this validation can pass while `keyId(publicB64)` below hashes the original padded bytes. In that case `ace trust add` reports a trusted `ed25519:&lt;hash-of-padded-der&gt;`, but packages signed with the matching private key use the canonical SPKI hash from `signManifest`, so installs remain `untrusted-key`. After parsing, either reject non-canonical input by comparing the re-exported DER or replace `publicB64` with `pub.export({ type: "spki", format: "der" }).toString("base64")` before computing/storing the key ID.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-06-01T03:54:18Z on `tools/ace/ace.ts`:313 (association: MEMBER)

    <pre>
    Fixed — good catch. trust add now derives key_id + stores public_key from the CANONICAL re-exported SPKI (pub.export spki der), not the raw input. Confirmed empirically: createPublicKey accepts a padded SPKI but re-exports the 44-byte canonical form; keyId(padded) != keyId(canonical) while keyId(reexport) == signManifest's key_id. Test added: padded SPKI -&gt; stored under canonical key_id, not the padded id.
    </pre>

## Fix commits (touching thread paths)

### `024ff339e1a5722b07775a4b3222dbf4dc5ec5e9` -- 2026-06-01T03:26:38Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/ace/ace.ts`
- `tools/ace/store.ts`

**Message:**

```
fix(ace): harden trust store perms + validate trust-add key + ESM imp…

…orts (post-merge review)

3 post-merge findings on slice-3: (1) addTrustedKey now creates ~/.ace 0700 +
trust file 0600 + chmod-on-every-call (a permissive umask could leave the trust
store world/group-writable -> key injection); (2) trust add validates the public
key parses as Ed25519 SPKI before persist (missing public_key no longer throws a
fatal out of main -> clean exit 65); (3) tests use ESM imports not require().

New tests: addTrustedKey writes 0o600 on POSIX; corrects pre-existing permissive
trust file; trust add with missing public_key exits 64/65; trust add with garbage
key string exits 64/65.

Gates: 72 pass / 0 fail (was 68); tsc clean; ace help/list --json/trust list all
exit 0; git ls-tree HEAD = 67.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `ed0a6365e2518e7507e0e1b75a46dd60007a451a` -- 2026-06-01T03:37:30Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/ace/ace.ts`
- `tools/ace/store.ts`

**Message:**

```
fix(ace): trust add rejects non-ed25519 keys + chmod repairs on dedup…

… path (review)

Two #6330 follow-ons: (1) trust add now asserts asymmetricKeyType===ed25519
(createPublicKey accepts RSA/EC SPKI too; a non-ed25519 trusted key can never
verify) -> clean exit 65; (2) addTrustedKey repairs trust-file perms to 0600 on
EVERY call incl the duplicate early-return (a pre-existing permissive file with
the key was left world/group-readable-writable on re-add). Tests for both.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `920df743e00487a8cc32b151e553fc0a94a1b14e` -- 2026-06-01T03:39:44Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/ace/store.ts`

**Message:**

```
fix(ace): repair ~/.ace dir perms (0o700) on every addTrustedKey call…

… (review)

#6330 follow-on (Copilot P1): mkdirSync mode:0o700 only applies on dir CREATE — a
pre-existing permissive ~/.ace would let another local user replace files in it
(swap trusted-keys.json) even with the file at 0o600. addTrustedKey now chmods
BOTH the dir (0o700) and file (0o600) on every call (dedup + add paths). Test:
pre-existing 0o777 ~/.ace -> dir tightened to owner-only after addTrustedKey.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `2401a8082b23215aa058ef7982c8d464bb038b80` -- 2026-06-01T03:44:27Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/ace/store.ts`

**Message:**

```
fix(ace): tighten ~/.ace dir BEFORE trust-file read/write — close the…

… TOCTOU window (review)

#6330 follow-on (Copilot P1): the dir was chmod'd 0o700 only AFTER readKeysFile +
writeFileSync, so a pre-existing group/world-writable ~/.ace left a window where
another local user could swap trusted-keys.json mid-operation. Now mkdir+chmod the
dir to 0o700 FIRST (before any file read/write); file mode still tightened after
each write. Closes the injection race. 75 tests pass.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `ce18e6d5be6f8c84e840b7eabefa7ef1d51885e7` -- 2026-06-01T03:47:13Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `docs/agendas/ace-package-manager/2026-06-01-ace-cli-slice3-authenticity-signature-verify-design.md`
- `docs/backlog/P2/081KT07NV0008QG0R0016XR9J1-ace-slice3.1-pave-the-strict-default-road-2026-06-01.md`
- `tools/ace/ace.ts`
- `tools/ace/store.ts`

**Message:**

```
ux(ace): rename --allow-unsigned -> --allow-no-signature (operator de…

…cision)

Operator resolved the deferred flag-naming question 2026-06-01 ("rename to
--allow-no-signature; whatever makes most sense"). The DX/UX team flagged
--allow-unsigned as reading like "skip all checks" when it only covers genuinely-
unsigned packages (a present bad/untrusted signature is always refused). The new
name is unambiguous. No back-compat alias (the flag shipped hours ago in #6320,
no users). Sweep-refs across live code + tests + skill + the canonical Ace docs;
internal field allowUnsigned -> allowNoSignature; 081KT07NV0008QG0R0016XR9J1's open-question line
marked resolved. 75 tests pass; help shows the new flag + scope gloss.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `4e92fc4948def75ce46f6ba4502198115214aaf0` -- 2026-06-01T03:54:13Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `docs/agendas/ace-package-manager/2026-06-01-ace-cli-slice3-authenticity-signature-verify-design.md`
- `docs/backlog/P2/081KT07NV0008QG0R0016XR9J1-ace-slice3.1-pave-the-strict-default-road-2026-06-01.md`
- `tools/ace/ace.ts`

**Message:**

```
fix(ace): canonical key_id in trust add + doc-sync unsupported-algo &…

… 081KT07NV0008QG0R0016XR9J1 (review)

3 review findings on the rename commit:
- Codex P2 (correctness): createPublicKey accepts a padded SPKI but re-exports the
  canonical 44-byte form; trust add computed key_id from the RAW input, so a padded
  key would be stored under a key_id no signature presents -> that publisher's
  packages silently never authenticate. Now derive key_id + stored public_key from
  the canonical re-exported SPKI (matches signManifest/verify). Test: padded SPKI
  -> stored under canonical key_id, not the padded-bytes id.
- Copilot P1 (doc-sync): design §6 listed only 3 verifySignature reasons; added
  the unsupported-algo case (present sig, algo != ed25519 -> hard refuse) from the
  algorithm-confusion fix.
- Copilot P1 (doc-sync): 081KT07NV0008QG0R0016XR9J1 said the rename was resolved but a later line still
  called it deferred/gloss-only; removed the contradiction.

76 tests pass; tsc + markdownlint clean.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```
