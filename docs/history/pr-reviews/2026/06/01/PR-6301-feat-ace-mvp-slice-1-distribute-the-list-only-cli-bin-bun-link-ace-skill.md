# PR #6301 -- feat(ace): MVP slice 1 — distribute the list-only CLI (bin + bun link + ace skill)

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 6301 |
| Title | feat(ace): MVP slice 1 — distribute the list-only CLI (bin + bun link + ace skill) |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-06-01T01:03:17Z |
| Merged at | 2026-06-01T01:06:35Z |
| Merge commit SHA | `92cf5fbea269a8f6ea0ad4e573f4e94bbbdb861d` |
| Branch | `otto-windows/ace-mvp-slice1-2026-06-01` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/6301 |
| Changed files | 7 |
| Additions / deletions | +101 / -0 |

## Description

## What

**Slice 1** of the Ace CLI MVP (per the plan `docs/agendas/ace-package-manager/2026-06-01-ace-cli-mvp-implementation-plan.md`). Distribution wiring over the existing `list`-only CLI — **zero new CLI logic** (install/verify stay stubbed; that's Slice 2). Built subagent-driven (implementer + independent spec+quality review, both passed).

## Tasks

1. **`package.json` bin** — `"ace": "tools/ace/ace.ts"` (mirrors the `zeta-shadow` precedent).
2. **`tools/setup/common/repo-bins.sh`** — best-effort `bun link` in repo root → exposes `ace` + `zeta-shadow` on PATH; wired into `macos.sh` + `linux.sh` after `agent-clis.sh`; allowlisted in the bash-retirement inventory (`setup/bootstrap`).
3. **`install.ps1`** — Windows `bun link` step (best-effort `Invoke-ToolSoft` + `Push/Pop-Location`).
4. **`.claude/skills/ace/SKILL.md`** — agent surface (router-discovered; Node ≥22.5/bun floor + Codex no-JS fallback; verb table reflects current `list`/`help` live, install/verify marked Slice 2).

## Decisions honored (spec §8)

install.sh-sibling first ✓ · skill-installer-only (no manifest-driving) ✓ · Node-floor portable (no bun-only API) ✓.

## Validation (independently re-run by the review subagent)

`bun test tools/ace/` 23 pass · `package.json` bin = `tools/ace/ace.ts` · `repo-bins.sh` `bash -n` ok + best-effort discipline confirmed · bash-retirement inventory PASS (25 retained) · `install.ps1` AST 0 errors · SKILL.md markdownlint exit 0 · `ace list --json` → `[]`. **No Slice 2 code leaked** (install/verify still stubs; no `contentHash`/`installPackage`).

Slice 2 (the `install`/`verify` integrity build) is a separate PR on your go.

🤖 Generated with [Claude Code](https://claude.com/claude-code)


## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 0 |
| Resolved threads | 0 |
| Unresolved threads | 0 |
| Total review comments | 0 |
| Total fix commits (touching thread paths) | 0 |

## Review threads

(no review threads on this PR)

## Fix commits (touching thread paths)

(no commits in this PR touched files anchored by review threads)
