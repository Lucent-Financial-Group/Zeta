# PR #6438 -- feat(081KT07NV0008QG0R003BE6MJ2): accumulate-mode for F# Bonsai oracle (parseAll + ProblemDetails) — ferry of #6436

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 6438 |
| Title | feat(081KT07NV0008QG0R003BE6MJ2): accumulate-mode for F# Bonsai oracle (parseAll + ProblemDetails) — ferry of #6436 |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-06-01T17:10:46Z |
| Merged at | 2026-06-01T17:14:00Z |
| Merge commit SHA | `ca5ccad2545df8f7a4e2bcbbab000aeecff5dc69` |
| Branch | `otto-cli/b0976-accumulate-fsharp-2026-06-01` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/6438 |
| Changed files | 2 |
| Additions / deletions | +282 / -0 |

## Description

F# ferry of the TS accumulate-mode (#6436) — one at a time, TS reference → F# match. The applicative complement to the fail-fast `parse`.

- **`parseAll : string -> Result<Expr, PathedFeedback list>`** — collects **every** per-node decline keyed by its JSON-path; independent sub-trees accumulate, a fatal-structural node is single for that node but siblings still accumulate. Non-throwing collectors (`pushNode`/`pushConst`) over `JsonElement` via `TryGetProperty` + `ValueKind`; eager `for`-loops over `EnumerateArray` (mutable-struct enumerator).
- **`PathedFeedback { Path; Feedback }` + `ProblemDetails { Type; Title; Errors }`** (RFC-9457) + **`toProblemDetails`** groups declines by path into the `Errors` map.
- Additive over the same `BonsaiFeedback` payload; reuses the fail-fast `parse` for the valid path (only `NonCanonical` reachable after structural validation).

**Matches the TS oracle shape error-for-error.** +5 F# tests (Ok on golden; 3 independent declines with paths; `toProblemDetails` errors-map; single malformed-JSON; single `NonCanonical`) → **30 F# Bonsai tests**, build 0 warnings.

Both oracles now have the accumulate-mode. 081KT07NV0008QG0R003BE6MJ2's accumulate slice → ✅ once this + #6436 both merge (small batched follow-up).

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 0 |
| Resolved threads | 0 |
| Unresolved threads | 0 |
| Total review comments | 0 |
| Total fix commits (touching thread paths) | 0 |

## Review threads

(no review threads on this PR)

## Fix commits (touching thread paths)

(no commits in this PR touched files anchored by review threads)
