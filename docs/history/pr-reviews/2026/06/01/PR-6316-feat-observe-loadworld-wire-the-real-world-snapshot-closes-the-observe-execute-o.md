# PR #6316 -- feat(observe): loadWorld — wire the real World snapshot (closes the observe→execute→observe loop)

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 6316 |
| Title | feat(observe): loadWorld — wire the real World snapshot (closes the observe→execute→observe loop) |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-06-01T02:14:21Z |
| Merged at | 2026-06-01T02:22:03Z |
| Merge commit SHA | `e1248f9a5692c2c4516eda057f7fa69a225c1700` |
| Branch | `otto-cli/observe-loadworld-2026-05-31` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/6316 |
| Changed files | 2 |
| Additions / deletions | +274 / -0 |

## Description

## loadWorld — wire the real World snapshot (closes the loop)

operator: *"build loadWorld to close the real loop next."* The read side of observe.ts's roadmap (execute side already landed in #6310/#6312). Closes:

`loadWorld() → observe/observeWithLlm → execute (append event) → loadWorld() → …`

**Two channels, each its own source of truth:**

- **BACKLOG** ← the existing selector (the oracle) via `nextActionFromBacklog` — *not* reimplemented (per backlog-reader's "selector is the oracle"). The chosen item IS the world's backlog for the tick; a free_time selection (empty/blocked) → empty backlog.
- **MODE** ← a **fold of the event log** (v5: state is a projection of the log). The events `execute` appended via the folder sink carry the chosen mode; folding yields the persisted mode — **the read side of what the sink writes, the loop closing on itself.**

**Schema-on-read:** `readEventActions` tolerantly parses event JSON for `{id, at, action}`, validates canonical id + known action kind, orders by `(at, id)` for a replayable fold, skips malformed/missing (never throws). Decoupled reader — no dependency on the writer's type.

`operator` optional (absent = background agent); `nextAction` injectable → testable without the real backlog. **Closes-the-loop test:** a `self_reflect` event in the log → `observe` returns `self_reflect` even with ready work available (mode persisted; work offered, not forced).

Verified: tsc 0, eslint clean, **9/9 tests**. Additive (new file).

🤖 Generated with [Claude Code](https://claude.com/claude-code)


## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | true |
| Total threads | 3 |
| Resolved threads | 2 |
| Unresolved threads | 1 |
| Total review comments | 3 |
| Total fix commits (touching thread paths) | 2 |

## Review threads

### Thread 1 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F_cZ4`
- **Path**: `tools/observe/load-world.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-06-01T02:16:08Z on `tools/observe/load-world.ts`:76 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Recurse into date-partitioned event directories**

  When `eventDir` is the documented event root (`agent-state/{persona}/{trajectory}/events/YYYY/MM/DD/{zetaId}.json`, checked in `docs/backlog/P2/081KSNY2Z0008QG0R001K6HJ7Z-git-append-only-state-persist-typescript-tool-event-sourcing-layer-kestrel-2026-05-28.md`), this only lists the immediate `YYYY` directory names and then skips them because they are not `.json` files. In that real layout, `loadWorld()` folds an empty log and never restores the persisted mode, so the observe→execute→observe loop does not close unless callers pass a single day leaf directory instead of the event root.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 2 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F_cZ-`
- **Path**: `tools/observe/load-world.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-06-01T02:16:08Z on `tools/observe/load-world.ts`:92 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Validate action payload shape before folding**

  For a malformed append-only event that still has a canonical `id`, string `at`, and a known `kind` such as `{ "kind": "do_item" }`, this cast accepts it as `NextAction`; the later `fold()` calls `simulate()`, which dereferences `action.item.id` and throws. Because the reader is explicitly the tolerant schema-on-read boundary, one bad event file in the log can now block every `loadWorld()` instead of being skipped.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 3 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM6F_fA9`
- **Path**: `tools/observe/load-world.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-06-01T02:22:19Z on `tools/observe/load-world.ts`:75 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Require full BacklogItem shape before accepting work events**

  Fresh evidence beyond the earlier payload comment: this validator now rejects a missing `item`, but it still accepts `do_item`/`decompose` events whose `item` only has an `id`. For a malformed append-only event such as `{ kind: "decompose", item: { id: "B-1" } }`, `readEventActions()` returns it as a `NextAction`; any replay that folds those actions over a real backlog then uses `action.item.title` in `simulate()` and creates children titled `undefined (part 1/2)`, corrupting the reconstructed backlog instead of skipping the bad event at the schema-on-read boundary.

  Useful? React with 👍 / 👎.
  </pre>

## Fix commits (touching thread paths)

### `209f670cf9196bc44e91c206a3144d2cb86c8121` -- 2026-06-01T02:14:18Z -- ``

**Touched files (intersect with thread paths):**

- `tools/observe/load-world.ts`

**Message:**

```
feat(observe): loadWorld — wire the real World snapshot (closes the l…

…oop)

operator: "build loadWorld to close the real loop next." The read side of observe.ts roadmap
"wire the real World snapshot + execute the pick" (execute side already landed). Closes:
  loadWorld() -> observe/observeWithLlm -> execute (append event) -> loadWorld() -> ...

Two channels, each its own source of truth:
- BACKLOG: the existing selector (oracle) via nextActionFromBacklog — NOT reimplemented; the
  chosen item IS the world backlog for the tick (free_time selection → empty backlog).
- MODE: a FOLD of the event log (v5: state is a projection of the log). The events execute
  appended via the folder sink carry the chosen mode; folding yields the persisted mode. This is
  the read side of what the folder sink writes — the loop closing on itself.

Schema-on-read: readEventActions tolerantly parses event JSON for { id, at, action }, validates
canonical id + known action kind, orders by (at, id) for replayable fold; skips malformed/missing
(never throws). Decoupled reader (no dependency on the writer type).

operator optional (absent = background agent). nextAction injectable → testable without the real
backlog. Closes-the-loop test: a self_reflect event in the log → observe returns self_reflect
even with ready work (mode persisted; work offered not forced).

Verified: tsc 0, eslint clean, 9/9 tests.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `96cf1414a87324921d1ebffc352c2b320e83975f` -- 2026-06-01T02:18:33Z -- ``

**Touched files (intersect with thread paths):**

- `tools/observe/load-world.ts`

**Message:**

```
fix(observe): recurse date-partitioned events + validate payload shap…

…e (Codex #6316)

Two Codex P2 fixes on the tolerant reader:
- recurse: events live under YYYY/MM/DD/{id}.json (081KSNY2Z0008QG0R001K6HJ7Z / bus shape); flat readdir missed
  them all. readEventActions now recursive readdir (works flat + partitioned). +test.
- validate payload per kind: a corrupt {kind:"do_item"} with no item passed the kind check then
  fold→simulate dereferenced action.item.id and threw. hasValidPayload rejects ill-shaped payloads
  (do_item/decompose need item.id; reason-kinds need a string reason). +tests.
- extracted parseEventFile + hasValidPayload to keep readEventActions under cognitive-complexity 15.

tsc 0, eslint clean, 11/11.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```
