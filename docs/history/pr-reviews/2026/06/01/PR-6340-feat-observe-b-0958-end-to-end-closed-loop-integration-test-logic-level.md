# PR #6340 -- feat(observe 081KSXN940008QG0R001A4WWX4): end-to-end closed-loop integration test (logic-level)

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 6340 |
| Title | feat(observe 081KSXN940008QG0R001A4WWX4): end-to-end closed-loop integration test (logic-level) |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-06-01T04:39:11Z |
| Merged at | 2026-06-01T04:47:14Z |
| Merge commit SHA | `589aac68afc200ff5ec289e58200652706a2e1d5` |
| Branch | `otto-cli/b0958-observe-closed-loop-test-2026-06-01` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/6340 |
| Changed files | 2 |
| Additions / deletions | +171 / -16 |

## Description

First observe.ts slice on your go (081KSXN940008QG0R001A4WWX4, "any slice, test with our local-llm tests until comfortable turning it on").

## What

`tools/observe/closed-loop.test.ts` — the closed-loop **logic** integration test (081KSXN940008QG0R001A4WWX4 LEFT #2). Wires the **real** modules end-to-end:

```
observeWithLlm(mock backend)  →  execute  →  fold
```

with the **key invariant**: `fold(initial, sink.appended) === executed world` — the durable log reconstructs the executed state ("state is a projection of the event log") proven through the **real execute+sink seam**, not just in-memory `simulate` (which the existing observe.test.ts loop test already covers).

Mock `ModelBackend` (no ollama) + fake `EventSink` (no git) — the established patterns from observe.test.ts + execute.test.ts — so CI is an always-green shield. Covers:

- single tick closes (log reconstructs world)
- multi-tick loop closes (fold whole log == final world)
- DST determinism (same initial+backend ⇒ identical world)
- honest boundary: a not-yet-wired `do_item` pick surfaces `not-yet-executable` feedback with **no partial append**

## Scope (honest)

This is the **logic-level** closed loop (fakes). The **real-temp-git-repo** variant (real `folderSink` + `gitCommitToMain`) remains — 081KSXN940008QG0R001A4WWX4 item #2 sub-item + item #3. Nothing turned on in any foreground loop.

## Verify

4/4 new tests pass; **full observe suite 112/112** (no regressions); `tsc` clean; `eslint` clean; `closed-loop.test.ts` prettier-clean. (081KSXN940008QG0R001A4WWX4 itself carries a pre-existing prettier non-idempotency on its unicode tables — non-blocking, matches main.)

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 2 |
| Resolved threads | 2 |
| Unresolved threads | 0 |
| Total review comments | 2 |
| Total fix commits (touching thread paths) | 2 |

## Review threads

### Thread 1 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GAaxD`
- **Path**: `tools/observe/closed-loop.test.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-01T04:41:45Z on `tools/observe/closed-loop.test.ts`:5 (association: NONE)

  <pre>
  P1: This code comment introduces direct persona/human name attribution on a code surface. Current-state code comments should use role references instead of names; history/backlog surfaces preserve attribution, but `tools/**` does not.
  </pre>

### Thread 2 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GAaxQ`
- **Path**: `tools/observe/closed-loop.test.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-01T04:41:45Z on `tools/observe/closed-loop.test.ts`:33 (association: NONE)

  <pre>
  P1: This section says it lists exact paths, but this entry uses an ellipsis that is not a resolvable repository path. That breaks the cross-reference guarantee for readers and any tooling that checks these links; either use the real path or stop labeling the list as exact paths.
  </pre>

## Fix commits (touching thread paths)

### `286ee8ab5cd9c9d9597288e1106788b721a75d03` -- 2026-06-01T04:38:59Z -- ``

**Touched files (intersect with thread paths):**

- `tools/observe/closed-loop.test.ts`

**Message:**

```
feat(observe 081KSXN940008QG0R001A4WWX4): end-to-end closed-loop integration test (logic-…

…level)

Aaron 2026-06-01 'go': start observe.ts from 081KSXN940008QG0R001A4WWX4, any slice, test with
our local-llm tests until comfortable turning it on in the foreground loop.

First slice = the closed-loop LOGIC integration test (081KSXN940008QG0R001A4WWX4 LEFT #2):
tools/observe/closed-loop.test.ts wires the REAL modules end-to-end —
  observeWithLlm(mock backend) -> execute -> fold
— with the KEY invariant fold(initial, sink.appended) === executed world
(the durable log reconstructs the executed state: 'state is a projection
of the event log' proven through the real execute+sink seam, not just
in-memory simulate). Mock backend + fake sink (no ollama, no git) =
always-green CI shield. Covers single-tick, multi-tick loop, DST
determinism, and the not-yet-executable boundary (do_item surfaces
feedback, no partial append).

verify: 4/4 new tests pass; full observe suite 112/112 (no regressions);
tsc clean; eslint clean; closed-loop.test.ts prettier-clean. 081KSXN940008QG0R001A4WWX4
updated: item #2 logic-level [x], real-temp-git-repo variant remains.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `bd60cdc46508487a4859c4000b2a3a644fb7bb2f` -- 2026-06-01T04:43:53Z -- ``

**Touched files (intersect with thread paths):**

- `tools/observe/closed-loop.test.ts`

**Message:**

```
fix(observe): review threads on #6340 — role ref not name; real path …

…not ellipsis

Copilot P1s (both valid):
- closed-loop.test.ts header: 'Aaron 2026-06-01' -> 'The operator 2026-06-01'
  (no persona-name attribution on a tools/** code surface; role ref).
- composes-with 'exact paths' list: 081KSXN940008QG0R001A4WWX4-...md ellipsis -> the real
  resolvable path (keeps the exact-paths guarantee).

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```
