# PR #6380 -- docs(registry): AgencySignature + heartbeat-via-commit + ZetaId-categories as primitives

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 6380 |
| Title | docs(registry): AgencySignature + heartbeat-via-commit + ZetaId-categories as primitives |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-06-01T08:55:49Z |
| Merged at | 2026-06-01T08:58:01Z |
| Merge commit SHA | `53ae96e8c7c9d1da4d91bc4cb0eddc0a0e48e91e` |
| Branch | `otto-cli/registry-agencysignature-heartbeat-zetaid-categories-2026-06-01` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/6380 |
| Changed files | 1 |
| Additions / deletions | +1 / -1 |

## Description

Your call (2026-06-01): _"the agent signature stuff for I commit therefore I am … goes with heartbeat … and all the categories we need for the zetaids as primitives."_ Extended the **Identity → Identity / attestation** bullet:

- **ZetaId categories as primitives** — the field/category structure each as a cross-lang primitive: `Category` (incl. `Bus=6`), `Persona`, `AuthorityValue`, `Momentum`, `Chromosome`, `Firefly`, `IdVersion`, `LayoutDirection`, `ZetaObservation` (per `src/Core.CSharp.ZetaId/`).
- **AgencySignature** (_"I commit therefore I am"_) — the commit-attribution trailer, Convention v1: ~10 fields (`Agency-Signature-Version`/`Agent`/`Agent-Runtime`/`Agent-Model`/`Credential-Identity`/`Credential-Mode`/`Human-Review`(+Evidence)/`Action-Mode`/`Task`) + `Co-authored-by:`. The `Agent` is a ZetaId → composes with the identity primitive. Audit: `tools/hygiene/audit-agencysignature-main-tip.ts`.
- **heartbeat-via-commit** — the **externalized idle-counter**: trailer-on-every-commit + `git log --since` = a durable per-agent liveness counter that survives compaction (the narrative self-model counter is unreliable; the commit log IS the counter). Composes with the agent-loop + Observe.

ZetaId base stays ✅ 4/4; categories + signature + heartbeat are the wished cross-lang lift.

Docs-only; prettier + markdownlint clean; BACKLOG.md untouched. Edits the Identity line (region 35) — non-overlapping with #6379 (47-49 + EOF), so no rebase collision expected.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 0 |
| Resolved threads | 0 |
| Unresolved threads | 0 |
| Total review comments | 0 |
| Total fix commits (touching thread paths) | 0 |

## Review threads

(no review threads on this PR)

## Fix commits (touching thread paths)

(no commits in this PR touched files anchored by review threads)
