# PR #6293 -- docs(adr): v6 — Node safe-baseline, Bun accelerator (proposed Rule-0 refinement)

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 6293 |
| Title | docs(adr): v6 — Node safe-baseline, Bun accelerator (proposed Rule-0 refinement) |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-06-01T00:31:18Z |
| Merged at | 2026-06-01T00:39:42Z |
| Merge commit SHA | `4df10b50b03a23c0be8a0cff294dd7056f8e5c86` |
| Branch | `otto-cli/node-safe-bun-accelerator-adr-2026-05-31` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/6293 |
| Changed files | 1 |
| Additions / deletions | +79 / -0 |

## Description

Drafts the **Node = safe cross-harness baseline, Bun = accelerator** refinement as a **v6 triggered revisit** of the 2026-04-20 tools-runtime ADR (its own watchlist named the "node/deno/bun landscape consolidates" trigger) — *not* a unilateral rewrite of the auto-loaded Rule 0.

**The refinement** (still bun+TS; nothing else changes):

- **Node** = the portable runtime tooling must *run* on (every harness/CI image ships it). The new constraint: **nothing may be Bun-only.**
- **Bun** = repositioned from "the host" to "the fast lane" (fast install/test/startup where available).

**Why now:** Max already wanted Node for other reasons + cross-harness portability needs a safe-everywhere runtime; the Node-24 pin already landed (#6290, mise 22→24, "node everywhere; keep bun").

**Includes** the proposed Rule-0 carved-sentence amendment (to apply *on ratification*), and verified compatibility notes: `import.meta.main` needs Node 24.2+ (✓, aligns with the pin); `bun:test` is the accelerator path; Node type-strips erasable TS but `enum`/`namespace` need `tsx`/build. The agent-bus (#6283) is empirical proof it's satisfiable today (Node-safe `node:` builtins + Node-24.2 `import.meta.main` CLIs).

**Status: Proposed** — Aaron + Max aligned; pending broader product-team ratification per the doctrine-through-agreement discipline. On acceptance, the Rule-0 edit is a mechanical fast-follow.

🤖 Generated with [Claude Code](https://claude.com/claude-code)


## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 1 |
| Resolved threads | 1 |
| Unresolved threads | 0 |
| Total review comments | 1 |
| Total fix commits (touching thread paths) | 2 |

## Review threads

### Thread 1 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F-7aH`
- **Path**: `docs/DECISIONS/2026-04-20-tools-scripting-language.md`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-06-01T00:32:47Z on `docs/DECISIONS/2026-04-20-tools-scripting-language.md`:884 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Account for Bun-only APIs before declaring Node portability**

  This row says avoiding Bun-only APIs is already the convention, but a repo-wide search shows current tooling still depends on Bun runtime APIs (for example `tools/dora-classify/cli.ts` uses `Bun.argv`, `tools/hygiene/snapshot-github-settings.ts` and `tools/hygiene/audit-formal-artifacts.ts` use `Bun.spawn`, and `src/Core.TypeScript/zeta-id/cross-verify.ts` uses `Bun.YAML`/`Bun.file`/`Bun.write`). If the proposed Rule-0 text is accepted, those tools will not satisfy the new “must run on Node” baseline, so the ADR should list these as migration blockers or scoped exceptions rather than saying the Node-compatible API convention is already true.

  Useful? React with 👍 / 👎.
  </pre>

## Fix commits (touching thread paths)

### `b0bc041e16bf7bc025681f171c9cfc4c6fc512c5` -- 2026-06-01T00:31:15Z -- ``

**Touched files (intersect with thread paths):**

- `docs/DECISIONS/2026-04-20-tools-scripting-language.md`

**Message:**

```
docs(adr): v6 triggered revisit — Node = safe cross-harness baseline,…

… Bun = accelerator (proposed Rule-0 refinement)

Refines the 2026-04-20 tools-runtime ADR (via its revision-history + watchlist
'node/deno/bun landscape' trigger): still bun+TS, but Node becomes the portable runtime
tooling must RUN on (present in every harness/CI image) and Bun is repositioned to the
accelerator/fast-lane — nothing may be Bun-only. Records Aaron+Max alignment; Node-24 pin
already landed (#6290). Includes the proposed Rule-0 carved-sentence amendment (to apply
on ratification, not unilaterally), compatibility notes (import.meta.main needs Node 24.2+
— verified; bun:test is the accelerator path; Node type-strips erasable TS but enums need
tsx/build), and the agent-bus as empirical proof the refinement is satisfiable today.

Status: Proposed — pending broader product-team ratification per doctrine-through-agreement.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `3aa27ac1cb5fc31b70ab46dadd82151b28b3f68e` -- 2026-06-01T00:37:04Z -- ``

**Touched files (intersect with thread paths):**

- `docs/DECISIONS/2026-04-20-tools-scripting-language.md`

**Message:**

```
docs(adr): correct 'Bun-only APIs already avoided' — ~29 tools/ files…

… still use them; name the migration (Codex #6293)

The addendum claimed avoiding Bun-only APIs was 'already the convention'. A repo search
shows ~29 tools/ files still use Bun.* runtime APIs (Bun.argv, Bun.write, Bun.Glob,
Bun.spawnSync, Bun.sleep, Bun.stdin, ...). Corrected to TARGET-not-current-state + added a
'Migration cost (honest)' subsection: the policy sets direction (new tooling Node-safe by
default, nothing new Bun-only) and the existing-tooling port is a tracked migration
(candidate backlog row), not a big-bang. agent-bus (#6283) is the first tool to the new
convention. Makes the decision's real cost visible for ratification.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```
