# PR #6419 -- fix(install): graceful apt-get update on unreachable third-party sources + resilience test

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 6419 |
| Title | fix(install): graceful apt-get update on unreachable third-party sources + resilience test |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-06-01T13:04:12Z |
| Merged at | 2026-06-01T13:50:44Z |
| Merge commit SHA | `daa9e44d2084cd18523956f0062f4dd994d24ae1` |
| Branch | `claude/install-apt-resilience-2026-06-01` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/6419 |
| Changed files | 3 |
| Additions / deletions | +65 / -2 |

## Description

## What

`tools/setup/linux.sh` ran `apt-get update -y` under `set -euo pipefail`, so **any single unreachable apt source aborts the entire install** before runtime tooling (mise/dotnet/bun) is set up — even when the failing source isn't ours.

## Why (empirical)

Surfaced live from a restricted-network cloud session: the host image shipped third-party PPAs (`deadsnakes`, `ondrej/php`) that nobody added, and the egress policy `403`s those PPA hosts. `apt-get update` → non-zero → `pipefail` → whole install dead. Zeta's own apt manifest is clean; the brittleness is that a non-essential, non-Zeta source failure is treated as fatal.

## Fix — *grace in the artifact, assert in the test*

Per [`automated-tests-are-the-shield-assert-dont-skip.md`](../blob/main/.claude/rules/automated-tests-are-the-shield-assert-dont-skip.md):

- **`apt-get update` is now graceful** — a partial-source failure prints a visible `⚠` warning to stderr (**not silent**) and continues.
- **`apt-get install` stays strict** — it still fails loudly if a package we actually need is unavailable, so the assert is preserved at install time, **not** skipped to a false-green.

## Test — the shield must cover the graceful path

`tools/ci/dockerfiles/ubuntu-install-sh-test/Dockerfile` gets a resilience-assert RUN (sibling to the existing idempotency assert):

- Injects an unreachable apt source (`.invalid` TLD — RFC-2606 reserved, deterministic non-resolve, **no real-network dependency**), runs `install.sh`, and asserts **BOTH**: (a) exit 0 (graceful — pipefail didn't abort), and (b) the `⚠` warning **was emitted** (not silent — a graceful path that warned about nothing would be a false-green hole).
- Workflow `timeout-minutes` 40→45 for the extra idempotent run.

## Verification

Locally, against the live 403 condition: guarded update fires the warning + continues (exit 0); the unchanged `apt-get install` still pulls needed packages from the reachable archive. (`dotnet` itself still can't install in *that* env — the egress policy also 403s the dotnet/github runtime endpoints — but that's the env, not this fix; this fix stops a non-essential source from bricking the whole graph.)

Doc/script only; `tools/setup/*.sh` is Rule-0-allowed (install-graph).

https://claude.ai/code/session_01JjZLtuWj1GXecSrTWfeqcq

---
_Generated by [Claude Code](https://claude.ai/code/session_01JjZLtuWj1GXecSrTWfeqcq)_

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | true |
| Total threads | 3 |
| Resolved threads | 3 |
| Unresolved threads | 0 |
| Total review comments | 6 |
| Total fix commits (touching thread paths) | 4 |

## Review threads

### Thread 1 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GH-ao`
- **Path**: `tools/setup/linux.sh`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-06-01T13:07:22Z on `tools/setup/linux.sh`:101 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Make apt update report partial-source failures**

  In the Ubuntu apt version used by this install path, `apt-get update` can still exit 0 when an individual source cannot be fetched; I reproduced with the same `.invalid` source and apt printed `W: Some index files failed to download` with exit 0. Because the new warning is only emitted from the nonzero branch here, the added Docker resilience step reaches its grep with no `apt-get update reported errors` message and fails the workflow, while real third-party source outages also remain silent.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-06-01T13:12:10Z on `tools/setup/linux.sh`:101 (association: MEMBER)

    <pre>
    👍 Correct, and the `docker-ubuntu-install-sh-test` failure confirmed it empirically — in the clean CI image (no PPAs) my `.invalid` source is the lone failure, so `apt-get update` exits **0** with only `W: Some index files failed to download`, the exit-code-only branch never fired, the grep found nothing, and real DNS/connection outages would have stayed silent.

    Fixed in `3855c1b`: the guard now inspects **both** the exit code **and** apt's output (`Failed to fetch` / `Some index files failed to download` / `^Err:`). Verified locally against both modes:
    - PPA 403 (signature error) → exit 100 → warns via exit-code branch
    - `.invalid` (DNS non-resolve) → exit 0 → warns via output-grep branch (raw apt output contained `Err:1`, `W: Failed to fetch`, `W: Some index files failed to download`)

    `apt-get install` is unchanged, so the strict assert is preserved at install time. Thanks — this was the real bug, not the test.

    ---
    _Generated by [Claude Code](https://claude.ai/code)_
    </pre>

### Thread 2 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GIGKI`
- **Path**: `tools/setup/linux.sh`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-06-01T13:13:56Z on `tools/setup/linux.sh`:112 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Avoid suppressing early apt warning matches under pipefail**

  When `apt-get update` exits 0 but emits a partial-failure marker early in a large output stream, this `grep -q` pipeline can return 141 instead of 0 because `grep -q` exits on the first match, `printf` then gets SIGPIPE, and `set -o pipefail` is active for the script. I reproduced that shape with an early `Err:` plus a large trailing payload, and the condition fell through without warning; using a here-string/temp file or disabling pipefail for this probe keeps the new partial-failure guard from becoming silent in exactly the exit-0 partial-source case it is meant to surface.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-06-01T13:16:04Z on `tools/setup/linux.sh`:112 (association: MEMBER)

    <pre>
    👍 Correct — a real SIGPIPE/pipefail interaction: `grep -q` exits on first match → `printf` gets SIGPIPE (141) → pipefail returns the pipeline non-zero → the `||` reads it false → warning suppressed despite a match (triggers when apt output exceeds the pipe buffer with an early marker).

    Fixed in `81b0851`: switched the probe to a here-string — `grep -qiE '…' &lt;&lt;&lt;"$apt_update_out"` — which has no upstream producer to receive SIGPIPE, so pipefail never trips it, and grep stays line-oriented so `^Err:` still anchors per line. Verified the here-string matches the failure-output shape and ignores clean (all-`Hit:`) output. (Commit message references the wrong discussion id — the correct one is this thread, `r3334384342`.)

    ---
    _Generated by [Claude Code](https://claude.ai/code)_
    </pre>

### Thread 3 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GINNG`
- **Path**: `tools/setup/linux.sh`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-01T13:19:51Z on `tools/setup/linux.sh`:126 (association: NONE)

  <pre>
  P1 (operational): capturing `apt-get update` output via command substitution buffers all output until the command finishes. On slow mirrors / retry-heavy failure cases this can look like the installer is hung, and it also loads the entire output into memory. Prefer streaming the output while capturing it to a temp file for the subsequent pattern check, and remove the temp file before continuing.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-06-01T13:24:00Z on `tools/setup/linux.sh`:126 (association: MEMBER)

    <pre>
    Fair — and accurate that I regressed live output vs the original direct `apt-get update -y`. Fixed in `44d6a7d`: stream via `tee` to a temp file, run the probe against the file, remove it before `apt-get install`. This keeps the operator seeing progress on slow/retry mirrors, and importantly `tee` consumes *all* of apt's output so it does **not** reintroduce the SIGPIPE/pipefail hazard from the earlier P2 (the probe greps the file, not a pipe; grep stays line-oriented for `^Err:`). apt's own exit is taken from `PIPESTATUS[0]` so `tee` doesn't mask it. Verified locally against the live 403 condition: streams, captures exit 100, grep matches the log, warning fires, temp removed. (The in-memory point is negligible at apt-output sizes, but streaming was the real regression — thanks for catching it.)

    ---
    _Generated by [Claude Code](https://claude.ai/code)_
    </pre>

## Fix commits (touching thread paths)

### `182e3d99dcbd477db950cc0d2f2ac00a7c41fdab` -- 2026-06-01T13:03:53Z -- `claude`

**Touched files (intersect with thread paths):**

- `tools/setup/linux.sh`

**Message:**

```
fix(install): apt-get update graceful on unreachable third-party sour…

…ces + resilience test

Empirically surfaced from a restricted-network cloud session: a host
image that ships third-party PPAs (e.g. deadsnakes, ondrej/php) the
operator never added, combined with an egress policy that 403s those
PPA hosts, makes `apt-get update` exit non-zero — and under
`set -euo pipefail` that aborts the ENTIRE install before any runtime
tooling (mise/dotnet/bun) is set up. The failing sources aren't even
Zeta's (the apt manifest is clean).

Fix (tools/setup/linux.sh) — per
.claude/rules/automated-tests-are-the-shield-assert-dont-skip.md
("grace in the artifact, assert in the test"):
- `apt-get update` is now GRACEFUL: a partial-source failure warns
  (visible ⚠ to stderr — NOT silent) and continues.
- `apt-get install` stays STRICT: it still fails loudly if a package we
  actually need is unavailable, so the assert is preserved at install
  time rather than skipped to a false-green.

Test (tools/ci/dockerfiles/ubuntu-install-sh-test/Dockerfile) — the
shield must cover the graceful path or it's a hole that reads as
covered:
- New resilience-assert RUN injects an unreachable apt source
  (`.invalid` TLD, RFC-2606 reserved → deterministic non-resolve, no
  real-network dependency), runs install.sh, and asserts BOTH:
  (a) it still exits 0 (graceful — pipefail did not abort), AND
  (b) the warning WAS emitted (not silent).
- Bumped the workflow timeout 40→45 for the extra idempotent run.

Verified locally against the live 403 condition: the guarded update
fires the warning and continues (exit 0); the unchanged `apt-get
install` still pulls needed packages from the reachable archive.

Co-Authored-By: Claude <noreply@anthropic.com>

https://claude.ai/code/session_01JjZLtuWj1GXecSrTWfeqcq
```

### `3855c1b07aa3e0c62643b0a9cfef8008b6d89f55` -- 2026-06-01T13:11:58Z -- `claude`

**Touched files (intersect with thread paths):**

- `tools/setup/linux.sh`

**Message:**

```
fix(install): detect apt partial-source failure from output, not just…

… exit code

Codex P1 on PR #6419 (discussion_r3334343196), confirmed by the
docker-ubuntu-install-sh-test failure: `apt-get update`'s exit code is
an unreliable partial-failure signal. A signature/403 error exits
non-zero, but a DNS/connection failure on one source exits 0 with only
`W: Some index files failed to download`. The previous guard keyed only
on the non-zero branch, so:
  - the Docker resilience test's `.invalid` source (DNS non-resolve →
    exit 0) never tripped the warning → grep found nothing → red; and
  - worse, a REAL third-party DNS/connection outage would stay SILENT —
    the exact failure the guard exists to surface.

Now the guard inspects BOTH the exit code AND apt's output
(`Failed to fetch` / `Some index files failed to download` / `^Err:`)
and warns in either case. Verified locally against both modes:
  - PPA 403 (signature error)  → exit 100 → warns via exit-code branch
  - `.invalid` (DNS non-resolve) → exit 0  → warns via output-grep branch
Install stays STRICT (`apt-get install` unchanged) so the assert is
preserved at install time.

Co-Authored-By: Claude <noreply@anthropic.com>

https://claude.ai/code/session_01JjZLtuWj1GXecSrTWfeqcq
```

### `81b0851d908b3c2a8aa0168697c6d12ada803fce` -- 2026-06-01T13:15:41Z -- `claude`

**Touched files (intersect with thread paths):**

- `tools/setup/linux.sh`

**Message:**

```
fix(install): SIGPIPE-immune apt-failure probe (here-string, not pipe)

Codex P2 on PR #6419: `printf '%s' "$out" | grep -qiE ...` under
`set -o pipefail` can return 141 instead of 0 — `grep -q` exits on the
first match, `printf` then gets SIGPIPE, and pipefail propagates the
non-zero. The `||` condition reads that as false, so the warning is
SUPPRESSED even though grep matched — a silent hole in exactly the
exit-0 partial-source case the guard exists to surface (triggers when
apt output exceeds the pipe buffer with an early match).

Switch the probe to a here-string (`grep ... <<<"$out"`): no upstream
producer to receive SIGPIPE, so pipefail never trips it; grep stays
line-oriented so `^Err:` still anchors per line. Verified the
here-string matches the failure-output shape and ignores clean output.

Co-Authored-By: Claude <noreply@anthropic.com>

https://claude.ai/code/session_01JjZLtuWj1GXecSrTWfeqcq
```

### `44d6a7dcf65f3aa68e724e596a3f1bcf490f0a81` -- 2026-06-01T13:23:38Z -- `claude`

**Touched files (intersect with thread paths):**

- `tools/setup/linux.sh`

**Message:**

```
fix(install): stream apt-get update live while capturing for the probe

Copilot P1 (operational) on PR #6419: capturing `apt-get update` via
command substitution (`out="$(apt-get update 2>&1)"`) buffers ALL output
until the command finishes, so on a slow/retry-heavy mirror the install
looks hung — a regression vs the original direct `apt-get update -y`,
which streamed live.

Stream via `tee` to a temp file and run the partial-failure probe
against the file:
- live output preserved (operator sees progress on slow mirrors);
- `tee` consumes ALL of apt's output, so the SIGPIPE/pipefail hazard
  that ruled out `printf | grep -q` (Codex P2) does NOT reapply — the
  probe greps the FILE, not a pipe, and grep stays line-oriented so
  `^Err:` still anchors per line;
- apt's OWN exit comes from PIPESTATUS[0] (the pipeline status would be
  `tee`'s, masking apt);
- temp file removed before `apt-get install` continues.

Verified locally against the live 403 condition: streams, captures
exit 100 via PIPESTATUS[0], grep matches the log, warning fires, temp
removed.

Co-Authored-By: Claude <noreply@anthropic.com>

https://claude.ai/code/session_01JjZLtuWj1GXecSrTWfeqcq
```
