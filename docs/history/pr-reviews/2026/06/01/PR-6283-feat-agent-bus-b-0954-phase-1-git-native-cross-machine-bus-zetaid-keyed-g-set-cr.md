# PR #6283 -- feat(agent-bus): 081KSXN940008QG0R00171YAZW Phase 1 — git-native cross-machine bus (ZetaId-keyed G-Set CRDT, no-PR)

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 6283 |
| Title | feat(agent-bus): 081KSXN940008QG0R00171YAZW Phase 1 — git-native cross-machine bus (ZetaId-keyed G-Set CRDT, no-PR) |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-05-31T23:51:20Z |
| Merged at | 2026-06-01T00:38:58Z |
| Merge commit SHA | `1f37bcb204f27ef2402058497f3b3bcfeb0a6d7e` |
| Branch | `otto-cli/agent-bus-phase1-2026-05-31` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/6283 |
| Changed files | 5 |
| Additions / deletions | +727 / -0 |

## Description

**081KSXN940008QG0R00171YAZW Phase 1** — the git-native cross-machine agent-bus, the folder + ZetaId-keyed tooling that works *today* (pre-081KSNY2Z0008QG0R001DFZK4V), per the #6219 spec. You said "start the agent-bus implementation, phase 1."

Envelopes are ZetaId-named files in `docs/agent-bus/<persona>/<YYYY>/<MM>/<DD>/<hex>.json` — a **G-Set CRDT**: disjoint files never collide, so concurrent agents on different machines write different files → conflict-free → **cross-machine / Windows-safe** (git is the transport).

- **types.ts** — `AgentBusEnvelope` (reuses `tools/bus` `BusMessage` topic+payload + `AgentId`); `envelopePath` (UTC partition); `mintBusZetaIdHex` (canonical ZetaId, `Category.Bus`; `DEFAULT_ENV` unique / `DETERMINISTIC_ENV` reproducible); `serializeEnvelope` (stable).
- **publish.ts** — `writeEnvelope` (**pure**; idempotent on identical content; surfaces a same-id/different-content **collision** as feedback, never silent-overwrite — the spec's collision caveat) + `makeEnvelope` + CLI (write then `git add/commit/push` direct-to-main, **no PR** — the 081KSKBP80008QG0R001KK9WV6 mechanism; guarded by `import.meta.main` so tests never touch git).
- **subscribe.ts** — `readEnvelopesSince` (**pure**; ISO-ts cursor; sorted `(ts, hex)`; skips malformed best-effort) + `nextCursor` + CLI (`git pull --ff-only` then read).
- **agent-bus.test.ts** — **14 tests**: mint (Bus category / 32-hex / unique / reproducible), path layout, write (created / idempotent / collision / disjoint-G-Set), read (all / since-cursor / ordering / skip-malformed), nextCursor.

Reuses `tools/bus` + the canonical ZetaId (no new id scheme or action language). Pure/CLI split keeps git out of tests. **14/14 green; 0 agent-bus type errors.** Windows parity is by construction (git + file IO, no `/tmp`); the cross-machine round-trip verification is a follow-up sub-target. Phase 2 (path-scoped protection) waits on 081KSNY2Z0008QG0R001DFZK4V.

🤖 Generated with [Claude Code](https://claude.com/claude-code)


## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | true |
| Total threads | 34 |
| Resolved threads | 34 |
| Unresolved threads | 0 |
| Total review comments | 34 |
| Total fix commits (touching thread paths) | 12 |

## Review threads

### Thread 1 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F-yK1`
- **Path**: `tools/agent-bus/publish.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-31T23:53:17Z on `tools/agent-bus/publish.ts`:55 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Retry non-fast-forward bus pushes**

  When two machines publish from clones where one has not yet integrated the other's disjoint envelope, this plain `git push` is rejected as a non-fast-forward update, leaving the second bus message committed only locally and invisible cross-machine. The bus spec calls out this exact ref-contention case and requires `fetch origin main` + `rebase origin/main` + bounded retry for unique-file G-Set merges (`docs/research/2026-05-31-git-backed-cross-machine-otto-bus-zetaid-spec.md:245-250`); I also confirmed with local Git 2.43.0 that a divergent push to a bare repo is rejected as non-fast-forward rather than integrated automatically.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 2 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F-yK2`
- **Path**: `tools/agent-bus/subscribe.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-31T23:53:17Z on `tools/agent-bus/subscribe.ts`:57 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Read bus envelopes from origin/main**

  When an agent subscribes while checked out on a feature branch, or on a stale working tree, `git pull --ff-only` updates that branch/worktree and then `readEnvelopesSince` scans local files, so envelopes already pushed to `main` can be missed. The accepted bus spec explicitly requires `git fetch origin main` and reading `docs/agent-bus/**` from `origin/main` with `git ls-tree`/`git show`, because the working tree may be stale (`docs/research/2026-05-31-git-backed-cross-machine-otto-bus-zetaid-spec.md:263-279`).

  Useful? React with 👍 / 👎.
  </pre>

### Thread 3 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F-yK3`
- **Path**: `tools/agent-bus/subscribe.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-31T23:53:17Z on `tools/agent-bus/subscribe.ts`:50 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Include the ZetaId in the read cursor**

  Because the cursor stores only the newest timestamp, any envelope that arrives later with the same millisecond timestamp as the previous cursor is dropped forever by the strict `env.ts &gt; sinceTs` filter, even though the implementation already allows many distinct ZetaId-randomized envelopes per millisecond and sorts ties by `(ts, zetaIdHex)`. This can happen with concurrent cross-machine publishes in the same ms; the cursor needs the same tie-breaker it sorts on, or a seen-ZetaId set, to avoid losing messages.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 4 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F-yVd`
- **Path**: `tools/agent-bus/publish.ts`
- **Replies**: 0

**Initial comment:**

- **`github-advanced-security[bot]` (bot)** at 2026-05-31T23:54:06Z on `tools/agent-bus/publish.ts`:38 (association: NONE)

  <pre>
  ## CodeQL / Potential file system race condition

  The file may have changed since it [was checked](1).

  [Show more details](https://github.com/Lucent-Financial-Group/Zeta/security/code-scanning/132)
  </pre>

### Thread 5 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F-y9c`
- **Path**: `tools/agent-bus/subscribe.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-31T23:56:49Z on `tools/agent-bus/subscribe.ts`:76 (association: NONE)

  <pre>
  P0: `readEnvelopesSince` only treats JSON parse failures as malformed, but a syntactically-valid JSON file missing `ts`/`zetaIdHex` (or having non-strings) will throw during filtering/sorting (`localeCompare`/`a.ts.localeCompare`). To keep the “best-effort” guarantee, validate the required fields are strings and skip otherwise.
  </pre>

### Thread 6 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F-y9f`
- **Path**: `tools/agent-bus/publish.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-31T23:56:50Z on `tools/agent-bus/publish.ts`:39 (association: NONE)

  <pre>
  P0: `writeEnvelope` has a TOCTOU race (`existsSync` → `writeFileSync`) that can silently overwrite a concurrently-created file (including a same-id/different-content collision), defeating the collision-surfacing guarantee. Use an atomic create (`flag: "wx"`) and handle `EEXIST` by comparing existing content.
  </pre>

### Thread 7 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F-y9i`
- **Path**: `tools/agent-bus/publish.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-31T23:56:50Z on `tools/agent-bus/publish.ts`:66 (association: NONE)

  <pre>
  P0: The publish CLI accepts unvalidated `&lt;from&gt;`/`&lt;to&gt;` and uses `&lt;from&gt;` as a path segment (`docs/agent-bus/&lt;from&gt;/...`). A caller can pass values containing `/`/`..` and write outside the bus root. Also `JSON.parse(payloadJson)` is uncaught, so invalid JSON crashes with a stack trace instead of a clear usage error. Validate args (safe path segments + "*" for `to`) and catch JSON parse errors.
  </pre>

### Thread 8 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F-y9m`
- **Path**: `tools/agent-bus/publish.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-31T23:56:50Z on `tools/agent-bus/publish.ts`:55 (association: NONE)

  <pre>
  P1: `gitPushEnvelope`’s `git push` is ambiguous (depends on the caller’s current branch/upstream) and the comment mentions `--no-verify` but it isn’t actually passed. If this tool is meant to be “direct-to-main”, make the push target explicit and either pass `--no-verify` on commit or drop the claim.
  </pre>

### Thread 9 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F-y9x`
- **Path**: `tools/agent-bus/types.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-31T23:56:50Z on `tools/agent-bus/types.ts`:44 (association: NONE)

  <pre>
  P1: `AgentBusEnvelope` diverges from the existing local-bus `MessageEnvelope` shape (tools/bus/types.ts) and from the git-bus spec’s “extend MessageEnvelope” guidance (docs/research/2026-05-31-git-backed-cross-machine-otto-bus-zetaid-spec.md). Here the `BusMessage` is nested under `message` and `timestamp` is renamed to `ts`, which will make a future bridge/interop layer harder (and breaks “reuse envelope shape” from 081KSXN940008QG0R00171YAZW’s acceptance text). Consider reusing `MessageEnvelope` fields directly (topic/payload/from/to/timestamp/...) and adding just the extra git fields (zetaId, etc).
  </pre>

### Thread 10 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F-0JC`
- **Path**: `tools/agent-bus/agent-bus.test.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-06-01T00:02:09Z on `tools/agent-bus/agent-bus.test.ts`:24 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Fix the agent-bus test helper so typecheck passes**

  This helper no longer typechecks under the repo's strict discriminated-union settings: `Partial&lt;AgentBusEnvelope&gt;` can override `topic` without the matching `payload`, so the spread widens them independently and is not assignable to `MessageEnvelope`. I ran `./node_modules/.bin/tsc --noEmit` and it now reports TS2322 at this line, which blocks the TypeScript gate before these tests can run.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 11 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F-0JD`
- **Path**: `tools/agent-bus/subscribe.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-06-01T00:02:09Z on `tools/agent-bus/subscribe.ts`:113 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Filter subscription results to the requested recipient**

  When `origin/main` contains messages for multiple agents, the CLI returns every envelope after the cursor because neither this call nor `readEnvelopesFromGitRef` checks `env.to`. That diverges from the existing local bus (`list({to})` only returns `to == me || '*'`) and the git-bus spec's poll step 3, so a Vera subscriber would process Otto-only messages whenever both are present on the shared bus.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 12 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F-0JE`
- **Path**: `tools/agent-bus/publish.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-06-01T00:02:09Z on `tools/agent-bus/publish.ts`:170 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Allow the Windows Otto sender required by the bus spec**

  This allowlist check rejects `otto-windows`, but the cross-machine bus spec explicitly makes that the first Windows surface and requires adding it to `SENDER_IDS`; `rg "otto-windows|otto-win"` shows it is only in the spec/windows wrapper, not in the runtime sender list. In the intended Mac/Windows bus scenario, the Windows publisher exits here before writing any envelope.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 13 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F-2Fz`
- **Path**: `tools/agent-bus/publish.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-06-01T00:11:09Z on `tools/agent-bus/publish.ts`:133 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Block publishing from a locally-ahead main**

  When this runs on a `main` worktree that already has local commits not on `origin/main`, the branch-name check passes and this `git push origin HEAD:main` publishes those unrelated commits directly to `main` along with the bus envelope, bypassing the PR/path-scoped carve-out. I checked `git push -h`; the command accepts a refspec (`[&lt;repository&gt; [&lt;refspec&gt;...]]`), and here `HEAD:main` updates `main` from the entire local HEAD, not just the newly created envelope commit. Please fetch and verify `HEAD` is exactly based on `origin/main` before making the bus commit, or otherwise construct/push a commit containing only the envelope.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 14 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F-2F1`
- **Path**: `tools/agent-bus/publish.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-06-01T00:11:09Z on `tools/agent-bus/publish.ts`:61 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Commit only the new envelope path**

  If the main checkout has any pre-staged work when an agent publishes a bus message, this plain `git commit` includes those staged changes in the no-verify, direct-to-main bus commit. I checked `git commit -h`; committing only selected paths requires a pathspec or `--only`, but this command supplies neither, so it commits the current index after `git add path`. That can leak unrelated WIP through the bus carve-out; use a clean-index check or commit with an explicit pathspec.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 15 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F-2yA`
- **Path**: `tools/agent-bus/publish.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-01T00:14:00Z on `tools/agent-bus/publish.ts`:14 (association: NONE)

  <pre>
  P0: publish CLI casts `{ topic, payload } as BusMessage` without validating that `topic` is one of the known bus topics. If a caller passes an unknown topic, `TTL_MS[message.topic]` becomes `undefined` and `new Date(atMs + undefined).toISOString()` throws (RangeError), producing a confusing crash instead of a clear error.
  </pre>

### Thread 16 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F-2yE`
- **Path**: `tools/agent-bus/publish.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-01T00:14:01Z on `tools/agent-bus/publish.ts`:100 (association: NONE)

  <pre>
  P0: `topic` needs validation against the bus topic allowlist before building the envelope; otherwise an unknown topic causes `expiresAt` computation to throw. After validating, cast `topic` to `Topic` so `makeEnvelope` stays type-safe.
  </pre>

### Thread 17 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F-2yK`
- **Path**: `tools/agent-bus/types.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-01T00:14:01Z on `tools/agent-bus/types.ts`:50 (association: NONE)

  <pre>
  P1: `isSafeSegment` is used to claim “Windows-safe”, but it currently only blocks separators/`..`/leading dot. On Windows, path segments containing characters like `:` `*` `?` `"` `&lt;` `&gt;` `|` (or control chars) are invalid and will make publishing fail at runtime. Tighten the validation to reject Windows-reserved characters so callers can’t accidentally mint an unwritable path segment.
  </pre>

### Thread 18 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F-2yS`
- **Path**: `tools/agent-bus/publish.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-01T00:14:01Z on `tools/agent-bus/publish.ts`:61 (association: NONE)

  <pre>
  P0: `gitPushEnvelope` does `git add &lt;path&gt;` and then `git commit` without checking whether other changes are already staged. If the index is non-empty, this commit will include unrelated staged files and push them to `main`. Refuse to proceed when the index already has staged changes.
  </pre>

### Thread 19 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F-2yW`
- **Path**: `tools/agent-bus/publish.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-01T00:14:01Z on `tools/agent-bus/publish.ts`:55 (association: NONE)

  <pre>
  P1: `writeEnvelope` defaults the date partition (`at`) to `new Date()` rather than the envelope’s own `timestamp`. Around UTC midnight this can write an envelope under the wrong YYYY/MM/DD directory compared to `env.timestamp`, and re-writing the same envelope later can land it under a different day directory, weakening the intended per-id idempotency/dedup semantics.
  </pre>

### Thread 20 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F-32S`
- **Path**: `tools/agent-bus/publish.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-06-01T00:18:24Z on `tools/agent-bus/publish.ts`:81 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Handle same-path rebase collisions by re-minting**

  When a peer has already pushed the same ZetaId path with different content (the collision case called out for deterministic/high-volume same-ms publishers), this retry path treats it like an ordinary disjoint-file rebase: `git pull --rebase` will stop on the content conflict and throw, leaving the publisher in an interrupted rebase with the envelope unpublished. The bus spec explicitly distinguishes this from the clean G-Set union and requires re-packing a fresh id and retrying instead of relying on the rebase to succeed (`docs/research/2026-05-31-git-backed-cross-machine-otto-bus-zetaid-spec.md:245-249`).

  Useful? React with 👍 / 👎.
  </pre>

### Thread 21 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F-4uY`
- **Path**: `tools/agent-bus/publish.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-06-01T00:21:36Z on `tools/agent-bus/publish.ts`:77 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Preserve audit trailers on bus commits**

  When the publish CLI successfully commits an envelope, the generated commit message has only the subject line, so every direct-to-main bus commit made by an agent lacks the repo-required `Co-Authored-By` trailer. Because this path also uses `--no-verify`, hooks cannot add or reject the missing attribution, leaving cross-machine bus traffic on `main` without the audit surface AGENTS.md requires for AI-authored commits.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 22 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F-4uZ`
- **Path**: `tools/agent-bus/types.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-06-01T00:21:36Z on `tools/agent-bus/types.ts`:112 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Encode the sender surface in minted bus IDs**

  When any sender publishes through this code path, the ZetaId is always packed with `Persona.FireflyCoherence`, so `unpack(env.id).persona` is identical for `otto-windows`, `vera-codex`, `soraya`, etc. The bus spec's publish algorithm packs `persona:&lt;surface&gt;` and the v0 checklist calls out reserving those surface personas; without that mapping the filename/dedup key erases the sender provenance the ZetaId is supposed to carry.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 23 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F-5QN`
- **Path**: `tools/agent-bus/publish.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-01T00:23:43Z on `tools/agent-bus/publish.ts`:56 (association: NONE)

  <pre>
  `writeEnvelope` defaults `at` to `new Date(env.timestamp)` without validating that `env.timestamp` is a valid ISO timestamp. If a caller passes a malformed/empty timestamp, `new Date(...)` becomes an invalid date and `envelopePath` will write under `NaN/NaN/NaN`, breaking the on-disk layout and making idempotent re-publish unreliable.
  </pre>

### Thread 24 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F-5QU`
- **Path**: `tools/agent-bus/subscribe.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-01T00:23:43Z on `tools/agent-bus/subscribe.ts`:42 (association: NONE)

  <pre>
  `readEnvelopesSince`/`readEnvelopesFromGitRef` rely on `timestamp` being ISO-8601 (so string comparison matches time ordering) and `id` being a deterministic 32-hex tie-break. Currently `isReadableEnvelope` only checks that these are strings, so a single malformed timestamp/id can break ordering and cursor progression.
  </pre>

### Thread 25 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F-5fN`
- **Path**: `tools/agent-bus/subscribe.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-06-01T00:24:45Z on `tools/agent-bus/subscribe.ts`:139 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Preserve the positional cursor when no recipient is supplied**

  When `subscribe.ts` is called in the documented form with just a saved cursor, `forIdx` is `-1`, so `forIdx + 1` is `0` and this predicate excludes the first positional argument. That makes `bun tools/agent-bus/subscribe.ts &lt;cursor&gt;` ignore the cursor and reread every envelope from `origin/main`, causing duplicate processing unless callers also pass `--for`; only skip the `--for` value when the flag is actually present.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 26 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F-6vC`
- **Path**: `tools/agent-bus/types.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-01T00:30:05Z on `tools/agent-bus/types.ts`:66 (association: NONE)

  <pre>
  `isSafeSegment` is documented as making path segments valid on every OS, but it currently allows Windows-reserved device names (e.g. `CON`, `PRN`, `AUX`, `NUL`, `COM1`…); those segments fail on Windows even though they match the regex. Tighten the allowlist to reject reserved names (and also reject trailing dot which Windows disallows).
  </pre>

### Thread 27 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F-6vQ`
- **Path**: `tools/agent-bus/subscribe.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-01T00:30:05Z on `tools/agent-bus/subscribe.ts`:15 (association: NONE)

  <pre>
  This file invokes `git` via `execFileSync(...)` but does not suppress `sonarjs/no-os-command-from-path`. The repo’s lint config treats this as an error, so CI will fail unless these calls are either refactored or explicitly suppressed with a rationale (the common pattern in tools/** is an eslint-disable comment).
  </pre>

### Thread 28 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F-6vY`
- **Path**: `tools/agent-bus/publish.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-01T00:30:05Z on `tools/agent-bus/publish.ts`:12 (association: NONE)

  <pre>
  This file invokes `git` via `execFileSync(...)` but does not suppress `sonarjs/no-os-command-from-path`. This lint rule is enforced repo-wide (many tools/** scripts add an explicit eslint-disable with rationale), so these calls will fail CI without a suppression or refactor.
  </pre>

### Thread 29 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F-6va`
- **Path**: `tools/agent-bus/publish.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-01T00:30:06Z on `tools/agent-bus/publish.ts`:103 (association: NONE)

  <pre>
  `gitPushEnvelope` passes `path` (built with `node:path.join`) directly to `git add`/`git commit`. On Windows `join(...)` produces backslashes, which can be interpreted as escapes in git pathspec parsing and cause the add/commit to miss the file. Normalize the path to forward slashes for git commands and for the commit message/pathspec.
  </pre>

### Thread 30 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F-6ve`
- **Path**: `tools/agent-bus/publish.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-01T00:30:06Z on `tools/agent-bus/publish.ts`:176 (association: NONE)

  <pre>
  The unknown-target error message appends `| *`, but `AGENT_IDS` already includes `"*"`. This produces a duplicated/incorrect help string.
  </pre>

### Thread 31 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F-6vh`
- **Path**: `tools/agent-bus/publish.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-01T00:30:06Z on `tools/agent-bus/publish.ts`:194 (association: NONE)

  <pre>
  The CLI validates `topic` against `TTL_MS` keys, but then asserts it is a `BusMessage["topic"]` (`as BusMessage`). If `TTL_MS` ever contains a key that is not actually part of the `BusMessage` union, this will create an envelope that violates the declared `AgentBusEnvelope = MessageEnvelope` type contract. Add an explicit runtime allowlist of `BusMessage` topics before constructing the message.
  </pre>

### Thread 32 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F-6-b`
- **Path**: `tools/agent-bus/publish.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-06-01T00:31:04Z on `tools/agent-bus/publish.ts`:133 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Stop pushing Phase 1 bus envelopes directly to main**

  When the Phase 1 publisher runs, this `git push origin HEAD:main` updates `main` directly; I checked `git push -h`, and the command accepts `[&lt;repository&gt; [&lt;refspec&gt;...]]`, so `HEAD:main` is an explicit refspec to the remote main ref. That contradicts the bus spec's Phase 1 rollout, which says current envelopes land on a branch and only Phase 2 pushes `main` after observe.ts plus 081KSNY2Z0008QG0R001DFZK4V/path-scoped protection are in place (`docs/research/2026-05-31-git-backed-cross-machine-otto-bus-zetaid-spec.md:234-244, 284-303`). In the current environment this bypasses the safety gate the spec says must remain active.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 33 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F-6-c`
- **Path**: `tools/agent-bus/types.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-06-01T00:31:04Z on `tools/agent-bus/types.ts`:43 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Extend envelopes with cross-machine provenance fields**

  This aliases the git-backed envelope to the local `MessageEnvelope`, so the files produced by `makeEnvelope` have no `host` and no typed `inReplyTo` field. The accepted schema says the git/cross-machine envelope extends `MessageEnvelope` with `zetaId`, `host`, and optional `inReplyTo` (`docs/research/2026-05-31-git-backed-cross-machine-otto-bus-zetaid-spec.md:213-224`); without at least `host`, a subscriber cannot distinguish which machine emitted the same surface identity, and ack envelopes have no typed way to point at the message they acknowledge.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 34 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F-6-d`
- **Path**: `tools/agent-bus/publish.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-06-01T00:31:04Z on `tools/agent-bus/publish.ts`:101 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Attribute bus commits to the actual publishing harness**

  When any non-Claude surface uses this CLI, the direct bus commit is still stamped as `Claude Opus 4.8`, even for senders such as `vera-codex`, `riven-cursor`, or `lior-gemini`. The prior missing-trailer concern remains in a different form: the fresh evidence is this hard-coded Claude trailer, which preserves a trailer syntactically but corrupts the audit attribution AGENTS.md requires per harness.

  Useful? React with 👍 / 👎.
  </pre>

## Fix commits (touching thread paths)

### `e2827ad008462d0a2b0a5733c1cf6d9226d70123` -- 2026-05-31T23:51:10Z -- ``

**Touched files (intersect with thread paths):**

- `tools/agent-bus/agent-bus.test.ts`
- `tools/agent-bus/publish.ts`
- `tools/agent-bus/subscribe.ts`
- `tools/agent-bus/types.ts`

**Message:**

```
feat(agent-bus): 081KSXN940008QG0R00171YAZW Phase 1 — git-native cross-machine bus (ZetaI…

…d-keyed G-Set CRDT, no-PR)

Phase 1 of the git-native cross-machine agent-bus (081KSXN940008QG0R00171YAZW, per the #6219 spec): the
folder + ZetaId-keyed write/read tooling that works today (pre-081KSNY2Z0008QG0R001DFZK4V). Envelopes are
ZetaId-named files in docs/agent-bus/<persona>/<YYYY>/<MM>/<DD>/<hex>.json — a G-Set
CRDT (disjoint files = conflict-free = cross-machine/Windows-safe via git).

- types.ts: AgentBusEnvelope (reuses tools/bus BusMessage topic+payload + AgentId);
  envelopePath (UTC partition); mintBusZetaIdHex (canonical ZetaId, Category.Bus,
  DEFAULT_ENV unique / DETERMINISTIC_ENV reproducible); serializeEnvelope (stable).
- publish.ts: writeEnvelope (PURE; idempotent on identical content; surfaces
  same-id/different-content collision as feedback, never silent-overwrite) +
  makeEnvelope + CLI (write then git add/commit/push direct-to-main, no PR — 081KSKBP80008QG0R001KK9WV6;
  guarded by import.meta.main so tests never touch git).
- subscribe.ts: readEnvelopesSince (PURE; ISO-ts cursor; sorted; skips malformed
  best-effort) + nextCursor + CLI (git pull --ff-only then read).
- agent-bus.test.ts: 14 tests — mint (Bus category/32-hex/unique/reproducible),
  path layout, write (created/idempotent/collision/disjoint-G-Set), read
  (all/since-cursor/ordering/skip-malformed), nextCursor.

Pure/CLI split keeps the git layer out of tests. Reuses tools/bus + canonical ZetaId
(no new id scheme). 14/14 green; 0 agent-bus type errors. Windows parity by
construction (git + file IO, no /tmp); round-trip verification is a follow-up sub-target.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `c1366a525f3bbd858d2ba50d276aa3082f5da7e4` -- 2026-05-31T23:54:54Z -- ``

**Touched files (intersect with thread paths):**

- `tools/agent-bus/agent-bus.test.ts`
- `tools/agent-bus/publish.ts`
- `tools/agent-bus/subscribe.ts`

**Message:**

```
fix(agent-bus): non-ff push retry + read from origin/main + compound …

…(ts,zetaId) cursor (Codex #6283)

Three cross-machine correctness fixes Codex caught on Phase 1:

- P1 non-fast-forward push: a concurrent peer advancing main rejected our push,
  stranding the envelope locally. Disjoint ZetaId files (G-Set CRDT) never conflict,
  so pull --rebase + retry (bounded) integrates the peer + re-pushes.
- P1 read-from-working-tree: subscribe read the working tree (may be a feature
  branch / stale) -> missed envelopes on origin/main (the post-fetch-read-trap).
  Added readEnvelopesFromGitRef(ref) (git ls-tree + git show); the CLI now
  git-fetches origin/main + reads the origin/main tree, not the working tree.
- P2 same-ms cursor drop: ts-only cursor dropped a later envelope sharing the
  cursor ms. Cursor is now compound <ts>|<zetaIdHex> (envelopeCursor); filter +
  sort + nextCursor all use it. Added a same-millisecond regression test.

15/15 green (was 14; +same-ms test). Pure readers stay filesystem-testable; the
git-ref read + push-retry are the CLI/runtime layer.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `e47735a60e3f56ff156b77ae5f1e1c3729d5e75f` -- 2026-05-31T23:59:27Z -- ``

**Touched files (intersect with thread paths):**

- `tools/agent-bus/agent-bus.test.ts`
- `tools/agent-bus/publish.ts`
- `tools/agent-bus/subscribe.ts`
- `tools/agent-bus/types.ts`

**Message:**

```
fix(agent-bus): 5 Copilot fixes — reuse MessageEnvelope, atomic write…

…, schema-validate, path-guard, explicit push (#6283)

- P0 schema-validation: readEnvelopesSince now skips JSON-valid-but-missing-fields
  envelopes (typeof timestamp/id !== string) instead of throwing in sort — keeps the
  best-effort guarantee.
- P0 TOCTOU: writeEnvelope uses an atomic create (flag 'wx'); on EEXIST it compares
  content -> exists-identical | collision. No existsSync->write race.
- P0 path-injection: envelopePath rejects unsafe segments (isSafeSegment: no / \ ..
  leading-dot); the publish CLI validates from/to against SENDER_IDS/AGENT_IDS and
  try/catches JSON.parse (clean error, no stack-trace crash / no write outside root).
- P1 push: explicit 'git push origin HEAD:main' + on-main guard (won't push a feature
  branch's commits to main) + commit '--no-verify' (honors the data-not-code comment);
  pull --rebase origin main retry kept.
- P1 envelope shape: AgentBusEnvelope now REUSES MessageEnvelope (tools/bus/types) per
  the #6219 spec's 'extend MessageEnvelope' guidance — id (=Bus ZetaId hex) / from / to
  / timestamp / expiresAt + top-level topic/payload; per-topic TTL_MS[topic]. No drift
  from the local bus -> the legacy-bus bridge sub-target becomes a transport swap.

17/17 green; 0 agent-bus type errors. Cursor + readers updated to (timestamp, id).

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `d9caf8a2cb63290905e86f3d9bc44c80f52be47c` -- 2026-06-01T00:08:37Z -- ``

**Touched files (intersect with thread paths):**

- `tools/agent-bus/agent-bus.test.ts`
- `tools/agent-bus/subscribe.ts`

**Message:**

```
fix(agent-bus): 3 Codex fixes — DU-safe test helper, recipient filter…

…, otto-windows sender (#6283)

- P1 (tsc): the env() test helper used Partial<AgentBusEnvelope>, letting a caller set
  topic without its matching payload; the spread then produced a non-member (heartbeat
  topic x shadow payload) not assignable to the MessageEnvelope DU. bun strips types so
  it ran 17/17 green; tsc --noEmit caught it (the same assert-vs-skip gap as the grammar-16
  red). Fix: scope over to Extract<AgentBusEnvelope,{topic:'shadow-catch'}> so topic/payload
  stay correlated. agent-bus tsc errors now 0.
- P2 (recipient): subscribe now filters to a recipient — readEnvelopesSince /
  readEnvelopesFromGitRef take an optional recipient; collect() drops envelopes whose
  'to' is neither the recipient nor '*'; CLI gains '--for <agentId>'. Without it every
  subscriber read every agent's mail. +1 test (18 pass).
- P2 (otto-windows): added 'otto-windows' to SenderAgentId + SENDER_IDS (tools/bus/types.ts)
  — the #6219 spec names it the first Windows surface (line 227); publish's allowlist now
  admits Aaron's Windows peer. (zeta-id Persona/Bus-category reservation per the spec is a
  separate 081KSXN940008QG0R00171YAZW sub-target.)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `d8cba0112ccc4510d474bd83b56d4d5778363a8b` -- 2026-06-01T00:14:30Z -- ``

**Touched files (intersect with thread paths):**

- `tools/agent-bus/publish.ts`

**Message:**

```
fix(agent-bus): harden direct-to-main publish — refuse local-ahead ma…

…in + commit only the envelope path (Codex #6283)

- P1: before `git push HEAD:main`, fetch origin/main and refuse if local main is ahead
  (rev-list origin/main..HEAD != 0) — otherwise unrelated local commits get pushed to
  main alongside the envelope, bypassing review. Reconcile-first or fail clean.
- P2: `git commit ... -- <path>` pathspec so the no-verify direct-to-main bus commit
  includes ONLY the envelope file, never pre-staged work sitting in the checkout.

gitPushEnvelope stays behind import.meta.main (git path not unit-tested by design);
tsc clean.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `3af73f77bc2c1a2917702c5e616f1ceaceba79d9` -- 2026-06-01T00:18:32Z -- ``

**Touched files (intersect with thread paths):**

- `tools/agent-bus/agent-bus.test.ts`
- `tools/agent-bus/publish.ts`
- `tools/agent-bus/types.ts`

**Message:**

```
fix(agent-bus): 4 Copilot fixes — validate topic, Windows-safe segmen…

…ts, timestamp partition (#6283)

- P0 (topic validation): publish CLI now rejects an unknown topic (`topic in TTL_MS`)
  before building the envelope, with a clear error + valid-topic list. Previously an
  unknown topic made TTL_MS[topic] undefined and makeEnvelope's expiresAt threw a
  RangeError (new Date(NaN)). Cast narrowed to BusMessage['topic'] after the check.
- P1 (Windows-safe segments): isSafeSegment switched to a positive allowlist
  /^[A-Za-z0-9._-]+$/ (+ no leading dot, no '..'), which rejects every Windows-forbidden
  char (< > : " / \ | ? *, control chars, spaces) by omission while keeping kebab-case
  personas (otto-cli / otto-windows). Makes the 'Windows-safe' claim actually hold.
- P1 (timestamp partition): writeEnvelope now defaults the date partition to the
  envelope's OWN timestamp (new Date(env.timestamp)) not wall-clock, so an idempotent
  re-publish always lands the SAME file even across a UTC-midnight boundary (was: a 2nd
  file for one id -> broke the G-Set property).
- P0 (refuse-if-index-non-empty, outdated): already superseded by the '-- path' pathspec
  commit from the prior round (commits only the envelope regardless of index state).

+2 tests (Windows-char rejection; default-partition-from-timestamp). tsc 0, 19 pass.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `42793d8c885508d40d458a9711c47b1b9e3738ba` -- 2026-06-01T00:21:40Z -- ``

**Touched files (intersect with thread paths):**

- `tools/agent-bus/publish.ts`

**Message:**

```
fix(agent-bus): abort+fail-loud on rebase conflict (same-path collisi…

…on) instead of mishandling it (Codex #6283 P2)

The push-retry loop previously ran `git pull --rebase` unconditionally; a rebase
CONFLICT there means a peer pushed the same ZetaId path with DIFFERENT content (a real
same-path collision, only reachable with deterministic ids — crypto DEFAULT_ENV makes it
astronomically improbable). Now: catch the rebase failure, `git rebase --abort` to leave
a clean repo, and throw a clear error telling the publisher to re-mint with DEFAULT_ENV.
Avoids a half-rebased state / silently merging over a peer's envelope. (Full auto-re-mint
is deferred — impossible with crypto ids, and re-minting touches envelope construction.)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `560dff0747ad35ca9384fa56880a859533762724` -- 2026-06-01T00:23:47Z -- ``

**Touched files (intersect with thread paths):**

- `tools/agent-bus/publish.ts`

**Message:**

```
fix(agent-bus): bus commits carry attribution — body names sender + C…

…o-Authored-By trailer (Codex #6283 P2)

The direct-to-main bus commit message was subject-only; with --no-verify skipping the
hook, every bus commit landed without the repo-required Co-Authored-By trailer. Now the
message has a body naming the publishing sender + the standard Co-Authored-By trailer.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `142136e76454eb093c076350fc5f1eaca0b7f205` -- 2026-06-01T00:25:50Z -- ``

**Touched files (intersect with thread paths):**

- `tools/agent-bus/agent-bus.test.ts`
- `tools/agent-bus/publish.ts`
- `tools/agent-bus/subscribe.ts`
- `tools/agent-bus/types.ts`

**Message:**

```
fix(agent-bus): validate timestamp(ISO)+id(hex) format, not just type…

… (Copilot #6283)

Both findings share one root: envelope validation was typeof-string only.
- isReadableEnvelope (reader) now requires a canonical ISO timestamp (lexical sort ==
  time sort, which the (timestamp,id) cursor relies on) + a 32-hex id; a malformed one is
  skipped instead of corrupting ordering/cursor for everyone.
- writeEnvelope (publish) guards the same before computing the path — a malformed timestamp
  would default 'at' to Invalid Date and write under NaN/NaN/NaN; now it refuses loudly.
New shared validators isCanonicalTimestamp / isCanonicalBusId in types.ts. +2 tests
(reader skips non-ISO; writeEnvelope throws on malformed). tsc 0, 21 pass.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `01cd577179a8069f9e05be7b10dd6be5a03a02f9` -- 2026-06-01T00:27:33Z -- ``

**Touched files (intersect with thread paths):**

- `tools/agent-bus/agent-bus.test.ts`
- `tools/agent-bus/subscribe.ts`

**Message:**

```
fix(agent-bus): subscribe CLI dropped the cursor when --for absent; e…

…xtract+test parseSubscribeArgs (Codex #6283 P2)

With --for absent, forIdx is -1 so forIdx+1=0 wrongly excluded the first positional —
`subscribe.ts <cursor>` ignored the cursor and reread everything. Guarded forValueIdx to
-1 when --for is absent. Extracted the arg parsing into a pure, exported
`parseSubscribeArgs` (the 2nd CLI-parsing bug -> make it testable) + 4 tests covering the
bare-cursor regression, --for in either order, and --no-fetch. tsc 0, 25 pass.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `f5a4fb8ead90c5b875238ad98b0ce6bddda9dcc1` -- 2026-06-01T00:27:58Z -- ``

**Touched files (intersect with thread paths):**

- `tools/agent-bus/subscribe.ts`

**Message:**

```
fix(agent-bus): parseSubscribeArgs return type uses explicit | undefi…

…ned (exactOptionalPropertyTypes)

The prior commit pushed with a tsc error: under exactOptionalPropertyTypes the return
{ cursor?: string; recipient?: AgentId } rejects the actual { cursor: string|undefined,
recipient: AgentId|undefined } value (and the tests' { recipient: undefined } literals).
Return type now uses explicit | undefined. tsc 0.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `c8998bdbff8fa878ec00e669592fbdb7d041d508` -- 2026-06-01T00:35:24Z -- ``

**Touched files (intersect with thread paths):**

- `tools/agent-bus/agent-bus.test.ts`
- `tools/agent-bus/publish.ts`
- `tools/agent-bus/subscribe.ts`
- `tools/agent-bus/types.ts`

**Message:**

```
fix(agent-bus): 6 review fixes + clear pre-existing eslint reds (Code…

…x/Copilot #6283)

- eslint sonarjs/no-os-command-from-path: per-line disable on every execFileSync(git)
  (the gate runs eslint; matches the tools/agent-heartbeats/ precedent — the sibling
  no-PR-to-main tool). Was a real CI red.
- Windows backslash pathspec: normalize the envelope path to forward slashes before
  git add/commit (node:path.join yields backslashes on the otto-windows surface).
- per-sender Co-Authored-By: coauthorFor(from) maps the sender surface to its trailer
  (otto->Claude, alexa->Kiro, riven->Grok, vera->Codex, lior->Gemini; fallback names the
  sender) — a non-Claude bus commit is no longer mis-stamped as Claude.
- Windows reserved device names (CON/PRN/AUX/NUL/COM1-9/LPT1-9) rejected by isSafeSegment.
- duplicate '| *' dropped from the unknown-target help (AGENT_IDS already includes '*').
- cleared 5 pre-existing eslint reds that had only been caught by tsc before: two bare
  .sort() (-> localeCompare cmp), an unsafe .id member-access (-> typed cast), a
  ReadonlyArray<T> (-> readonly T[]), and a non-null assertion in nextCursor (-> .at(-1)).

Verify is now tsc 0 + ESLINT 0 + 25 pass (eslint added to the local gate — it had been
shipping silent-red since it's non-required).

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```
