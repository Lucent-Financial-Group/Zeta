# PR #6456 -- docs(ace): slice 6.2 publish-enhancements design

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 6456 |
| Title | docs(ace): slice 6.2 publish-enhancements design |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-06-01T19:15:40Z |
| Merged at | 2026-06-01T19:17:24Z |
| Merge commit SHA | `a17836481b26128592c06a2ef04a04114824c6c8` |
| Branch | `otto-windows/ace-slice6.2-spec-2026-06-01` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/6456 |
| Changed files | 1 |
| Additions / deletions | +254 / -0 |

## Description

Design spec for **slice 6.2** of `ace registry publish` (081KT07NV0008QG0R0016FVWD7 deferred enhancements). Brainstormed + decided with the operator 2026-06-01.

Three enhancements:

- **Per-package `url` field** — optional top-level `url` (sibling of manifest/files, outside the signed manifest) overrides the derived `<base>/<name>-<version>.json`. Excluded from `package_hash` (verified: hashes manifest+files only); relaxes the filename guard for that package (the override decouples URL from filename — the use case is files not named canonically).
- **Comma-separated `--packages a,b,c`** — multi-directory scan + merge; per-dir readability required; cross-dir duplicate `name@version` → existing duplicate guard.
- **Explicit `--sequence N`** — overrides auto-bump; positive int; the existing anti-rollback guard goes live (`N <= prev.sequence` → refuse).

**ETag/Last-Modified sidecar: dropped** (not deferred-again) — consumer already does HTTP conditional-GET + 6.1 deterministic output gives stable host ETags.

Spec-only PR (the design doc); impl follows as a separate PR after review. Self-reviewed: no placeholders, internally consistent, single-slice scope.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 0 |
| Resolved threads | 0 |
| Unresolved threads | 0 |
| Total review comments | 0 |
| Total fix commits (touching thread paths) | 0 |

## Review threads

(no review threads on this PR)

## Fix commits (touching thread paths)

(no commits in this PR touched files anchored by review threads)
