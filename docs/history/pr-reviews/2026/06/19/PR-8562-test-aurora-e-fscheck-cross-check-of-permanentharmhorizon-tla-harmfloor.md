# PR #8562 -- test(aurora): (e) FsCheck cross-check of PermanentHarmHorizon.tla (HarmFloor)

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 8562 |
| Title | test(aurora): (e) FsCheck cross-check of PermanentHarmHorizon.tla (HarmFloor) |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-06-19T12:25:52Z |
| Merged at | 2026-06-19T12:25:59Z |
| Merge commit SHA | `057c0f80c697445f88ea9a8d8efbcf578d3fde84` |
| Branch | `otto/aurora-e-harmhorizon-fscheck` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/8562 |
| Changed files | 4 |
| Additions / deletions | +147 / -3 |

## Description

## What

The **(e) FsCheck cross-check** of the Aurora immune re-grounding (`docs/trajectories/aurora-immune-reground/`). Continues the slice Aaron authorized 2026-06-19.

BP-16 **Leg B** (empirical) for round (e) `PermanentHarmRisk_H` / viability-kernel **harm floor**. Leg A is the TLA+ model (`src/Core.TLA/specs/PermanentHarmHorizon.tla`, Kira-reviewed, TLC-green). This leg re-implements the same harm-decay-within-horizon transition system **independently in F#** and uses FsCheck to assert **HarmFloor** — `committed ⇒ reversible ∧ kernel-reached ∧ within-horizon` — holds at **every reachable state** under random action sequences.

## Tests (4, all green)

- 1 FsCheck property: HarmFloor at every reachable state (random `MaxHarm`/`H`/action-sequence).
- 3 non-vacuity witnesses, one per Aurora §4.1 case (mirroring the TLA+ NON-VACUITY probes):
  - (c) accept — low-harm reversible insert repairs to kernel in time → `committed`.
  - (a) irreversible-block → `refused`.
  - (b) past-horizon-block (harm can't reach kernel within H) → `refused`.

## Why it's worth its weight

- **Independence is the value.** The F# `step`/`harmFloor` relation is authored separately from the TLA+ `Next`/`HarmFloor`. A counterexample ⇒ the two **drifted** (triage = freeze + diff both transition relations, never silently fix one).
- **Distinct from `ChildFloorCrossVerify`** — that cross-checks the *deployed* `SubstrateEffectHandler` deny-propagation; this cross-checks the §4.1 retraction-horizon dynamics.
- **Scope honest:** this is the **safety** floor (a harmful/irreversible/over-horizon insert is never *committed*), **not** liveness (that it *is* refused) — the liveness leg routes through `observe.ts` (Aaron 2026-06-16). The four non-claims travel unchanged.

## Gate

`dotnet build -c Release` → 0 warnings / 0 errors. Full Formal suite: **194 green**.

Files: new `tests/Tests.FSharp/Formal/PermanentHarmHorizonCrossVerify.Tests.fs` (+ fsproj); discharge log §8(e) + RESUME step 2.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 0 |
| Resolved threads | 0 |
| Unresolved threads | 0 |
| Total review comments | 0 |
| Total fix commits (touching thread paths) | 0 |

## Review threads

(no review threads on this PR)

## Fix commits (touching thread paths)

(no commits in this PR touched files anchored by review threads)
