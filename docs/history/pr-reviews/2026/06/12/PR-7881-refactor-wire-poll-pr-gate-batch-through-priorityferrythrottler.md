# PR #7881 -- refactor: wire poll-pr-gate-batch through PriorityFerryThrottler

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 7881 |
| Title | refactor: wire poll-pr-gate-batch through PriorityFerryThrottler |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-06-12T10:18:19Z |
| Merged at | 2026-06-12T10:58:29Z |
| Merge commit SHA | `4f0bb69c5f818dc92241fa15bd18706d983a27d0` |
| Branch | `kiro/ferry-throttler-ts-port` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/7881 |
| Changed files | 67 |
| Additions / deletions | +1811 / -68 |

## Description

## Summary

Replace the manual cursor-based worker-pool in `pollAllBounded` with `PriorityFerryThrottlerWithResult<number, PollOutcome>`. Same external behavior, same function signature, all existing tests passing.

## What changed

- Import PriorityFerryThrottlerWithResult from the ferry-throttler module
- Replace the worker-pool loop with a throttler instance (maxBatchSize=1, strict draining)
- Two lanes ready: priority 0 (urgent/actionable) and priority 1 (background/default)
- All PRs currently enter at priority 1 — the wiring is ready for observe-loop classification

## What was tested

All 14 existing poll-pr-gate-batch tests pass unchanged (DST contracts: input-order alignment, concurrency bounds, error propagation, empty-input, clamped worker count).

## Context

Step 2 of Rodney's razor from the FerryThrottler handoff: fold genuine producers into FerryThrottler instances.

Co-Authored-By: Kiro <noreply@kiro.dev>

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | true |
| Total threads | 4 |
| Resolved threads | 4 |
| Unresolved threads | 0 |
| Total review comments | 4 |
| Total fix commits (touching thread paths) | 2 |

## Review threads

### Thread 1 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6JF6ik`
- **Path**: `src/Core.TypeScript/ferry-throttler/lane-notifier.test.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-06-12T10:21:48Z on `src/Core.TypeScript/ferry-throttler/lane-notifier.test.ts`:26 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Fix the track helper's inferred type**

  This helper creates `state` without a `promise` field and then assigns `state.promise`, so the repository no longer type-checks under the existing strict TypeScript gate; I verified this with `node_modules/.bin/tsc --noEmit --pretty false`, which reports `TS2339: Property 'promise' does not exist` at this line. Since `package.json` defines `typecheck` as `tsc --noEmit` and `tsconfig.json` includes these new test files, this blocks the TypeScript check until `state` is explicitly typed with a `promise` property before assignment.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 2 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6JGKqd`
- **Path**: `tools/shadow/shadow-observer.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-06-12T10:34:59Z on `tools/shadow/shadow-observer.ts`:49 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Remove the unused ProcessBatch import**

  With `noUnusedLocals` enabled in `tsconfig.json`, this newly added type-only import blocks the TypeScript gate even though the symbol is never referenced; `tsc --noEmit --pretty false` reports `TS6133: 'ProcessBatch' is declared but its value is never read` for this line. Removing the import restores this file's compatibility with the existing typecheck contract.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 3 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6JGZp2`
- **Path**: `src/Core.TypeScript/zflash/test-harness/multi-vm.ts`
- **Replies**: 0

**Initial comment:**

- **`github-code-quality[bot]` (bot)** at 2026-06-12T10:47:29Z on `src/Core.TypeScript/zflash/test-harness/multi-vm.ts`:26 (association: NONE)

  <pre>
  ## Unused variable, import, function or class

  Unused import createSpawnSyncQcow2RetentionExecutor.

  ---

  The best fix is to remove only the unused named import &lt;code&gt;createSpawnSyncQcow2RetentionExecutor&lt;/code&gt; from the &lt;code&gt;./qemu-state&lt;/code&gt; import list in &lt;code&gt;src/Core.TypeScript/zflash/test-harness/multi-vm.ts&lt;/code&gt;, leaving all other imports intact.&lt;/p&gt;
  &lt;p&gt;Concretely:&lt;/p&gt;
  &lt;ul&gt;
  &lt;li&gt;Edit the import block starting at line 17.&lt;/li&gt;
  &lt;li&gt;Delete the single line containing &lt;code&gt;createSpawnSyncQcow2RetentionExecutor,&lt;/code&gt;.&lt;/li&gt;
  &lt;li&gt;Do not change behavior or any other code paths; this is a cleanup-only change that preserves functionality.&lt;/li&gt;
  &lt;/ul&gt;
  &lt;p&gt;No new methods, definitions, or external packages are needed.
  </pre>

### Thread 4 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6JGZp-`
- **Path**: `src/Core.TypeScript/zflash/test-harness/multi-vm.ts`
- **Replies**: 0

**Initial comment:**

- **`github-code-quality[bot]` (bot)** at 2026-06-12T10:47:29Z on `src/Core.TypeScript/zflash/test-harness/multi-vm.ts`:79 (association: NONE)

  <pre>
  ## Unused variable, import, function or class

  Unused function positiveInteger.

  ---

  The best fix is to **use** &lt;code&gt;positiveInteger&lt;/code&gt; in &lt;code&gt;validateInput&lt;/code&gt; to validate &lt;code&gt;memoryMB&lt;/code&gt; and &lt;code&gt;cpuCount&lt;/code&gt; when provided. This removes the unused-function warning and improves input safety without changing existing behavior for valid inputs.&lt;/p&gt;
  &lt;p&gt;In &lt;code&gt;src/Core.TypeScript/zflash/test-harness/multi-vm.ts&lt;/code&gt;, update &lt;code&gt;validateInput&lt;/code&gt; by adding checks after existing string-field validation:&lt;/p&gt;
  &lt;ul&gt;
  &lt;li&gt;If &lt;code&gt;input.memoryMB !== undefined&lt;/code&gt; and not &lt;code&gt;positiveInteger(input.memoryMB)&lt;/code&gt;, return an &lt;code&gt;invalid-input&lt;/code&gt; error for &lt;code&gt;memoryMB&lt;/code&gt;.&lt;/li&gt;
  &lt;li&gt;If &lt;code&gt;input.cpuCount !== undefined&lt;/code&gt; and not &lt;code&gt;positiveInteger(input.cpuCount)&lt;/code&gt;, return an &lt;code&gt;invalid-input&lt;/code&gt; error for &lt;code&gt;cpuCount&lt;/code&gt;.&lt;/li&gt;
  &lt;/ul&gt;
  &lt;p&gt;No new imports or dependencies are required.
  </pre>

## Fix commits (touching thread paths)

### `dda64b73acd9230ac060fe658caa122dc2cbf5ac` -- 2026-06-12T10:30:28Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/shadow/shadow-observer.ts`

**Message:**

```
feat: wire shadow-observer event sink through FerryThrottler

The shadow observer's continuous loop now publishes ShadowEvent instances
through a FerryThrottler<ShadowEvent> (DoP=1, batch=64, queue=256).

This is the room-readiness wiring: when the shadow observer becomes a
room in the multi-room architecture, its outbound event stream flows
through the throttler so downstream consumers (bus, observe loop) get
backpressure-aware delivery. The throttler also provides the attachment
point for priority scheduling when multiple rooms share rate-limited
resources (osascript/accessibility/GitHub API).

Current behavior: the default processBatch is a no-op (events are still
logged by log() as before). The room adapter will replace this with
bus.publish or an observe-loop feed. All 68 existing tests pass.

Dispose is wired into SIGINT/SIGTERM handlers for clean shutdown.

Co-Authored-By: Kiro <noreply@kiro.dev>
```

### `b19454efd15694a019fac0f829ea0a4b4b2eca20` -- 2026-06-12T10:44:49Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `src/Core.TypeScript/zflash/test-harness/multi-vm.ts`

**Message:**

```
fix: rescue orphaned multi-vm.ts + delete superseded move-next.ts

multi-vm.ts (081KSNY2Z0008QG0R0008PN7RQ scenario 5 cluster-joining QEMU primitives) was
left as an untracked file at the old tools/zflash/test-harness/ path
after PR #7864 moved the harness to src/Core.TypeScript/. Relocated
to the correct directory where its imports (./extensions,
./serial-markers, ./qemu-state) resolve. Zero tsc errors.

move-next.ts was superseded by observe.ts (v4) and then by the room
framework. No live imports reference it. Deleted.

Another agent's lost work is everyone's problem on Zeta — we don't
leave it dangling.

Co-Authored-By: Kiro <noreply@kiro.dev>
```
