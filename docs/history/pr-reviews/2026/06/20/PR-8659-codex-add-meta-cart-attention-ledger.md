# PR #8659 -- [codex] add meta-cart attention ledger

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 8659 |
| Title | [codex] add meta-cart attention ledger |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-06-20T00:35:35Z |
| Merged at | 2026-06-20T01:01:48Z |
| Merge commit SHA | `66ee601c3463222a8c0e1c87c1bb89377b5c368a` |
| Branch | `codex/metacart-attention-ledger` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/8659 |
| Changed files | 3 |
| Additions / deletions | +386 / -30 |

## Description

## What

- Added Dark Hall attention-ledger report types and `executeAddressWithAttentionLedger` / `executeCellWithAttentionLedger` APIs while preserving the existing result-only execution APIs.
- Records meta-cart policy rows with source, address, policy name, baseline slot, selected slot, outcome, reason, and policy-backpressure heat kind.
- Added a regression test comparing a high-attention CHIP-9 child that is denied/backpressured with a lower-attention CHIP-8 child that executes.
- Removed three unused imports from `src/Core.TypeScript/observe/workspace-port.test.ts` so the current TypeScript preflight gate is green.

## Why

Attention should change ordering, not arithmetic truth. This gives the room a typed ledger row for the exact case Aaron called out: high-attention/high-gravity futures board first, but if the capability/byte tank is too small they emit backpressure instead of magically passing.

## Validation

- `dotnet build -c Release`
- `dotnet test tests/Tests.FSharp/Tests.FSharp.fsproj -c Release --no-build --filter "FullyQualifiedName~DarkHallCabinetRuntimeTests|FullyQualifiedName~MetaCartTests|FullyQualifiedName~DarkHallRoomLoopTests"`
- `dotnet test Zeta.sln -c Release --no-build`
- `dotnet format --verify-no-changes`
- `bun test src/Core.TypeScript/observe/workspace-port.test.ts`
- `bun run preflight:quick` (Go lint skipped locally because the toolchain is unavailable; CI still runs it)

Agency-Signature-Version: 1
Agent: vera
Agent-Runtime: Codex
Agent-Model: gpt-5.5
Credential-Identity: acehack
Credential-Mode: shared
Human-Review: not-implied-by-credential
Human-Review-Evidence: none
Action-Mode: human-directed
Task: none
Co-Authored-By: Codex <noreply@openai.com>


## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | true |
| Total threads | 2 |
| Resolved threads | 2 |
| Unresolved threads | 0 |
| Total review comments | 2 |
| Total fix commits (touching thread paths) | 3 |

## Review threads

### Thread 1 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6K8mIR`
- **Path**: `src/Core/DarkHallCabinetRuntime.fs`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-06-20T00:37:57Z on `src/Core/DarkHallCabinetRuntime.fs`:389 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Don't label rejected denial heat as policy backpressure**

  When an attention policy selects a denied or missing cart and a bounded heat sink rejects the first heat emitted for that failure (for example, the sink is already full), `MetaCart` returns `HeatRejected` for the `meta-cart.denied`/`meta-cart.missing` heat and never emits `meta-cart.policy-backpressure`. This predicate only checks that the failed slot was the policy-selected slot, so the ledger reports `HeatKind = Some "meta-cart.policy-backpressure"` and increments `Backpressured` even though no policy-backpressure heat was emitted; the ledger should distinguish `HeatRejected` whose rejected heat kind is actually `meta-cart.policy-backpressure` from rejection of the earlier failure heat.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 2 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6K8s4Z`
- **Path**: `src/Core/DarkHallCabinetRuntime.fs`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-06-20T00:50:24Z on `src/Core/DarkHallCabinetRuntime.fs`:399 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Do not infer policy backpressure after storage errors**

  When the sink returns `HeatSinkFeedback.StorageError` while emitting the first `meta-cart.denied`/`meta-cart.missing` heat (for example, a `BoundedHeatSink` constructed with capacity 0 returns `NonPositiveCapacity` before any policy-backpressure heat is attempted), `rejectedHeatKind` is `None`, so this fallback still labels the row as `meta-cart.policy-backpressure` just because the failed slot was policy-selected. The remaining `StorageError` branch is the fresh evidence after the earlier backpressure-kind fix; it will overcount `Backpressured` for a telemetry storage failure that never emitted policy backpressure.

  Useful? React with 👍 / 👎.
  </pre>

## Fix commits (touching thread paths)

### `882162d33259ccee9c545d7f4f3b0b7c2823a810` -- 2026-06-20T00:29:25Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `src/Core/DarkHallCabinetRuntime.fs`

**Message:**

```
feat(core): add meta-cart attention ledger

Adds a Dark Hall runtime report surface that keeps the existing result-only execution APIs while exposing attention-ledger rows for meta-cart policy runs. The ledger records the policy name, baseline slot, selected slot, execution outcome, and policy-backpressure heat kind when attention boards a future that the host capability gate still denies.

The regression test compares the high-attention CHIP-9 child that backpressures against the lower-attention CHIP-8 child that executes, proving attention changes ordering while byte/capability truth stays honest. The branch also removes three unused imports from the latest workspace-port TypeScript test so the current quick preflight tsc gate is green.

Proof: dotnet build -c Release; dotnet test Zeta.sln -c Release --no-build; dotnet format --verify-no-changes; bun test src/Core.TypeScript/observe/workspace-port.test.ts; bun run preflight:quick.

Agency-Signature-Version: 1
Agent: vera
Agent-Runtime: Codex
Agent-Model: gpt-5.5
Credential-Identity: acehack
Credential-Mode: shared
Human-Review: not-implied-by-credential
Human-Review-Evidence: none
Action-Mode: human-directed
Task: none
Co-Authored-By: Codex <noreply@openai.com>
```

### `c4bbddddb2f1a6bd8d0e11aaf46281b1219f41ff` -- 2026-06-20T00:47:10Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `src/Core/DarkHallCabinetRuntime.fs`

**Message:**

```
fix(core): distinguish rejected denial heat in attention ledger

Tightens the meta-cart attention ledger so `HeatRejected` rows only report `meta-cart.policy-backpressure` when the rejected heat kind was actually policy backpressure. Rejected `meta-cart.denied` or `meta-cart.missing` heat now stays a heat-channel rejection rather than inflating the policy-backpressure count.

Adds a saturated `BoundedHeatSink` regression that fills the heat channel before a high-attention denied cart launch, proving the ledger records `AttentionOutcome.HeatRejected` with no policy heat kind.

Proof: dotnet build -c Release; dotnet test tests/Tests.FSharp/Tests.FSharp.fsproj -c Release --no-build --filter "FullyQualifiedName~DarkHallCabinetRuntimeTests|FullyQualifiedName~MetaCartTests|FullyQualifiedName~DarkHallRoomLoopTests"; dotnet test Zeta.sln -c Release --no-build; dotnet format --verify-no-changes; bun run preflight:quick.

Agency-Signature-Version: 1
Agent: vera
Agent-Runtime: Codex
Agent-Model: gpt-5.5
Credential-Identity: acehack
Credential-Mode: shared
Human-Review: not-implied-by-credential
Human-Review-Evidence: none
Action-Mode: human-directed
Task: none
Co-Authored-By: Codex <noreply@openai.com>
```

### `468554400e59952e4b6368287dbb4551f5367906` -- 2026-06-20T01:01:14Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `src/Core/DarkHallCabinetRuntime.fs`

**Message:**

```
fix(core): avoid inferring heat backpressure on storage errors

Do not label a heat-rejected meta-cart run as policy backpressure when the heat sink returned a storage error before any policy-backpressure heat was emitted. The ledger now only infers policy backpressure for ordinary denied/missing results, or records it explicitly when the rejected heat kind is meta-cart.policy-backpressure.

Validation:

- dotnet build -c Release

- dotnet test tests/Tests.FSharp/Tests.FSharp.fsproj -c Release --no-build --filter "FullyQualifiedName~DarkHallCabinetRuntimeTests|FullyQualifiedName~MetaCartTests|FullyQualifiedName~DarkHallRoomLoopTests"

- dotnet test Zeta.sln -c Release --no-build

- dotnet format --verify-no-changes

- bun run preflight:quick

Agency-Signature-Version: 1
Agent: vera
Agent-Runtime: Codex
Agent-Model: gpt-5.5
Credential-Identity: acehack
Credential-Mode: shared
Human-Review: not-implied-by-credential
Human-Review-Evidence: none
Action-Mode: human-directed
Task: none
Co-Authored-By: Codex <noreply@openai.com>
```
