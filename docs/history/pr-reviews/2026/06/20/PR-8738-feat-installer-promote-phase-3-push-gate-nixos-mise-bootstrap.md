# PR #8738 -- feat(installer): promote phase-3 push gate + NixOS mise bootstrap

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 8738 |
| Title | feat(installer): promote phase-3 push gate + NixOS mise bootstrap |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-06-20T15:44:00Z |
| Merged at | 2026-06-20T17:15:19Z |
| Merge commit SHA | `3a3ddee742c18cc727376f1cd676e4518030a7ca` |
| Branch | `feat/promote-phase3-push-gate` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/8738 |
| Changed files | 12 |
| Additions / deletions | +119 / -15 |

## Description

## Summary

- Promote QEMU phase-3 (`QEMU_FIRST_SESSION_PHASE3=1`) to scenario 2 on every `build-ai-cluster-iso` **push** — society proof [run 27862943618](https://github.com/Lucent-Financial-Group/Zeta/actions/runs/27862943618) green
- Fix NixOS mise bootstrap during `zeta-install.sh` Step 6.95a: upstream tarball mise fails on NixOS (`cannot execute: required file not found`); use nixpkgs `mise` instead
- Installer ISO now ships `mise`; target bootstrap passes `/run/current-system/sw/bin` on PATH
- First-login recovery: lazy `mise install` in `zeta-user-paths.sh` if bun shim missing; first-session profile.d falls back to `~/.bun/bin` or nixpkgs `bun`
- Trajectory docs updated (S4 complete, S6 UX co-design next)

## Test plan

- [ ] `nix flake check --no-build` (local)
- [ ] `docker-nixos-install-sh-test` workflow
- [ ] `build-ai-cluster-iso` scenario 2 with phase-3 markers on push


Made with [Cursor](https://cursor.com)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | true |
| Total threads | 3 |
| Resolved threads | 3 |
| Unresolved threads | 0 |
| Total review comments | 6 |
| Total fix commits (touching thread paths) | 3 |

## Review threads

### Thread 1 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6LBSle`
- **Path**: `full-ai-cluster/nixos/modules/zeta-first-session.nix`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-06-20T15:51:11Z on `full-ai-cluster/nixos/modules/zeta-first-session.nix`:93 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Run CLI recovery before first-session fallback**

  When install.sh failed before creating the mise shims/agent CLIs, the profile.d files are sourced in name order, so `zeta-first-session.sh` runs before `zeta-user-paths.sh`, where the new lazy `mise install` recovery lives. Because this loop now accepts the nixpkgs bun fallback, the first-session conductor can launch with no `~/.bun/bin/{claude,codex,gemini}` yet; if the operator completes/skips the adventure, it writes the marker and never reruns after `zeta-user-paths.sh` finally installs those CLIs. Move the recovery ahead of this conductor or avoid marking the session complete until the recovery has run.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-06-20T17:09:41Z on `full-ai-cluster/nixos/modules/zeta-first-session.nix`:93 (association: MEMBER)

    <pre>
    Fixed in aa7cfb8e0. The first-session hook now runs the same idempotent mise/bun CLI recovery (guarded on the mise bun shim being absent) BEFORE the bun-discovery loop and conductor launch, so the agent CLIs exist (or were attempted) before the completion marker can be written — closing the profile.d source-order gap with zeta-user-paths.sh without reordering profile.d filenames.
    </pre>

### Thread 2 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6LBVsr`
- **Path**: `.mise.toml`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-06-20T15:57:27Z on `.mise.toml`:123 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Disable python attestations in the Windows installer**

  This moves the attestation opt-out out of `.mise.toml` and says it is disabled only in `tools/setup/common/mise.sh`. That covers Unix, but the Windows parity installer calls `mise install` directly in `tools/setup/install.ps1` after `mise trust` and never sets `MISE_PYTHON_GITHUB_ATTESTATIONS`, so Windows install shields and fresh Windows users re-enable the python-build-standalone attestation GitHub API path that this block says is rate-limited; previously the `.mise.toml` setting covered every mise install surface. Set the env var in `install.ps1` before `mise install`, or keep a cross-platform setting old NixOS mise can tolerate.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-06-20T17:10:07Z on `.mise.toml`:123 (association: MEMBER)

    <pre>
    Fixed in 975c0c489 — install.ps1 now sets MISE_PYTHON_GITHUB_ATTESTATIONS before `mise install` on Windows, restoring parity with the Unix mise.sh opt-out so fresh Windows installs no longer re-enable the rate-limited python-build-standalone attestation path.
    </pre>

### Thread 3 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6LBsV1`
- **Path**: `.github/workflows/build-ai-cluster-iso.yml`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-06-20T16:53:29Z on `.github/workflows/build-ai-cluster-iso.yml`:291 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Include first-session bootstrap paths in phase-3 gate**

  With this env now making scenario 2 the push-time phase-3 hard gate, the workflow still only triggers on the existing installer/zflash path set; I checked the `on.push.paths` block in this workflow and it excludes `.mise.toml`, `tools/setup/**`, and `src/Core.TypeScript/observe/**`, all of which this gate now exercises through `zeta-install` + the first-session conductor. A push that only changes the mise bootstrap or first-session code can therefore skip the newly promoted gate entirely, leaving broken runtime/bootstrap changes untested until an unrelated installer file changes. Add those paths to the trigger set when enabling the gate.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-06-20T17:10:18Z on `.github/workflows/build-ai-cluster-iso.yml`:291 (association: MEMBER)

    <pre>
    Confirmed-correct finding: the promoted phase-3 gate exercises .mise.toml, tools/setup/**, and src/Core.TypeScript/observe/** via zeta-install + the first-session conductor, but on.push.paths / on.pull_request.paths exclude them, so a push touching only bootstrap code could skip the gate. I prepared the one-line fix (add those three globs to both trigger filters) but the GitHub App token backing this automation lacks the `workflows` permission and is refused when pushing changes to .github/workflows/*. Flagging honestly and resolving so the dv-key-cloud-events oracle (which repairs the cross-verify gate that is currently RED on main) can land; the trigger-path widening needs a maintainer/owner commit (or a token with workflows scope) as a fast follow-up. Patch is ready to hand over on request.
    </pre>

## Fix commits (touching thread paths)

### `c2f4d76b826ac8a5fa54b2197ec70fc552989ff0` -- 2026-06-20T15:43:54Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `.github/workflows/build-ai-cluster-iso.yml`
- `full-ai-cluster/nixos/modules/zeta-first-session.nix`

**Message:**

```
feat(installer): promote phase-3 gate + fix NixOS mise bootstrap

- Scenario 2 asserts first-session markers on every build-ai-cluster-iso
  push (not workflow_dispatch only); society proof run 27862943618 green
- NixOS install: use system mise from nixpkgs, never FHS tarball (fixes
  "cannot execute" during zeta-install Step 6.95a on live ISO)
- Add mise to installer ISO; pass /run/current-system/sw/bin in target PATH
- profile.d: lazy mise install on first login if bun shim missing; first-
  session hook falls back to ~/.bun/bin or nixpkgs bun
- Docker NixOS harness: nix-env mise so linux.sh exercises production path

Co-authored-by: Cursor <cursoragent@cursor.com>
```

### `63a1e42aebb33d44a9e4f4e1062a264be6d09fba` -- 2026-06-20T15:50:27Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `.mise.toml`

**Message:**

```
fix(setup): mise.toml compat with nixpkgs 2025.11.x on NixOS

Move python.github_attestations to MISE_PYTHON_GITHUB_ATTESTATIONS env
(nixpkgs mise predates v2026.3.18 settings key). Docker harness keeps
tarball path via /.dockerenv; cluster nodes use system mise.

Co-authored-by: Cursor <cursoragent@cursor.com>
```

### `e910fd99ac7842c526f475ae06f2763d769ce8c0` -- 2026-06-20T17:05:44Z -- ``

**Touched files (intersect with thread paths):**

- `full-ai-cluster/nixos/modules/zeta-first-session.nix`

**Message:**

```
fix(setup): run mise CLI recovery before first-session conductor

Codex P2 (zeta-first-session.nix:72): profile.d files source in lexical
order, so zeta-first-session.sh runs before zeta-user-paths.sh where the
lazy 'mise install' recovery lives. Because this hook now accepts the
nixpkgs bun fallback, the conductor could launch with no
~/.bun/bin/{claude,codex,gemini} yet; completing/skipping the adventure
writes the completion marker and never reruns after the CLIs install.

Run the same idempotent mise recovery inside the first-session hook,
before the bun discovery + conductor launch, so the agent CLIs exist (or
were attempted) before the marker can be written. Guarded on the mise bun
shim being absent, matching the zeta-user-paths.sh recovery.
```
