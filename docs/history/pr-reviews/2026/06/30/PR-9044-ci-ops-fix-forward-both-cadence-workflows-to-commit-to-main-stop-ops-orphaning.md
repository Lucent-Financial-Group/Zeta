# PR #9044 -- ci(ops): fix-forward both cadence workflows to commit-to-main (stop ops/ orphaning)

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 9044 |
| Title | ci(ops): fix-forward both cadence workflows to commit-to-main (stop ops/ orphaning) |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-06-30T21:45:26Z |
| Merged at | 2026-06-30T21:45:31Z |
| Merge commit SHA | `ce2da8b84d4b9caf50e50f6415b5a320d34c6424` |
| Branch | `ops-fix-forward-cadence-commit-to-main` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/9044 |
| Changed files | 2 |
| Additions / deletions | +48 / -179 |

## Description

Executing **Dejan's ops/-bucket recommendation, step E** (the durable fix). The manifesto-citation + budget cadence workflows branched/PR'd their append-only output, but Actions is enterprise-blocked from opening PRs → output orphaned on `ops/*` (38 accumulated, drained in #9043).

Convert both to the proven `context-cost-trend-cadence.yml` pattern: commit the append-only file straight to main (`git pull --rebase origin main` + `git push origin HEAD:main`), gated on an idempotent diff check. **No branch, no PR, no policy relaxation** — deterministic append-only data needs no review gate (the sibling proves it lands cleanly daily).

- manifesto: *Publish snapshot branch* → *Commit snapshot to main*.
- budget: *Open snapshot PR* → *Commit snapshot to main*; dropped `pull-requests:write` + `gh pr create`.

All three cadence workflows now share one commit-to-main shape. **actionlint clean.** −179/+48.

🌒 shadow* — Dejan's step E, the ops/ bucket can't refill (standing authorization).

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 0 |
| Resolved threads | 0 |
| Unresolved threads | 0 |
| Total review comments | 0 |
| Total fix commits (touching thread paths) | 0 |

## Review threads

(no review threads on this PR)

## Fix commits (touching thread paths)

(no commits in this PR touched files anchored by review threads)
