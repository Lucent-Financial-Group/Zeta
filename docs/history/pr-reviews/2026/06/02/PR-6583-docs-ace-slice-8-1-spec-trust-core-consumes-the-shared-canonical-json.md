# PR #6583 -- docs(ace): slice 8.1 spec — trust core consumes the shared canonical-JSON

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 6583 |
| Title | docs(ace): slice 8.1 spec — trust core consumes the shared canonical-JSON |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-06-02T15:34:07Z |
| Merged at | 2026-06-02T15:35:48Z |
| Merge commit SHA | `8476006ef6770f635831357908290ecb34ff29c9` |
| Branch | `otto-windows/ace-slice8.1-canonical-json-spec-2026-06-02` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/6583 |
| Changed files | 1 |
| Additions / deletions | +142 / -0 |

## Description

Spec for Ace slice 8.1 (cross-language trust core, after slice 8 SHA-256). Ace's trust core stops rolling its own `canonicalize`+`JSON.stringify` and **consumes the shared 4-language byte-locked `canonicalJson`** (the DynamicValue lane's `src/Core.TypeScript/dynamic-value/json.ts`).

**Investigation findings (operator-requested):**

- Ace's content (`IndexSignableContent`) is all int/string/object — no Float/Bytes — so the JSON canonical form (6/8 shapes) covers it fully.
- Ace's current `canonicalize` already **sorts keys**; the shared `canonicalJson` is order-**preserving** → reconciled by an Ace `toTagged` that **sorts object keys** (keeps Ace's key-order-independence while consuming the shared primitive).
- No plain-JS→`Tagged` converter exists in the lane — that's the one piece Ace writes.

**Design:** new `tools/ace/canonical.ts` (`toTagged` sorting+integer-asserting, + `canonicalBytes`) → rewire `canonicalIndexBytes`/`canonicalManifestBytes` (signing.ts) + `packageHash` (resolve.ts) → delete local `canonicalize`. Format change accepted (pre-release; no `format_version` bump). TS-only; 4-lang `package_hash` parity inherited from the byte-lock. 2-task decomposition in the spec.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 2 |
| Resolved threads | 0 |
| Unresolved threads | 2 |
| Total review comments | 2 |
| Total fix commits (touching thread paths) | 1 |

## Review threads

### Thread 1 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM6Gez5W`
- **Path**: `docs/agendas/ace-package-manager/2026-06-02-ace-slice8.1-canonical-json-consume-design.md`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-06-02T15:36:14Z on `docs/agendas/ace-package-manager/2026-06-02-ace-slice8.1-canonical-json-consume-design.md`:50 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Reject unsafe integers in toTagged**

  When Ace canonicalizes parsed JSON that contains an integer above `Number.MAX_SAFE_INTEGER` (for example a registry `sequence` or manifest `format_version` supplied by untrusted JSON), `JSON.parse` has already rounded the value but `Number.isInteger` still passes, so `toTagged` would sign/hash the rounded decimal. That undermines the cross-language parity this slice is trying to buy from the precision-safe DynamicValue codec, because a Rust/F#/C# consumer can preserve and hash the original decimal instead. Please require `Number.isSafeInteger` here, or carry integer text from a precision-preserving parser.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 2 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM6Gez5b`
- **Path**: `docs/agendas/ace-package-manager/2026-06-02-ace-slice8.1-canonical-json-consume-design.md`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-06-02T15:36:14Z on `docs/agendas/ace-package-manager/2026-06-02-ace-slice8.1-canonical-json-consume-design.md`:56 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Include lockfile serialization in the migration**

  I checked the current Ace call sites: `tools/ace/lockfile.ts` imports `canonicalJson` from `resolve.ts` and uses it in `serializeLockfile`, but this slice only scopes `canonicalIndexBytes`, `canonicalManifestBytes`, and `packageHash`. If T2 removes or privatizes the resolver’s local canonicalizer, lockfile code breaks; if it leaves it, Ace still has a parallel ad-hoc canonical JSON path despite the stated single-primitive goal. Please either migrate `serializeLockfile` to the new adapter too or explicitly document why lockfiles remain out of scope.

  Useful? React with 👍 / 👎.
  </pre>

## Fix commits (touching thread paths)

### `1819c7f3ce5d25d83d3529e209e03256cee6d08c` -- 2026-06-02T15:34:01Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `docs/agendas/ace-package-manager/2026-06-02-ace-slice8.1-canonical-json-consume-design.md`

**Message:**

```
docs(ace): slice 8.1 spec — trust core consumes shared canonical-JSON…

… (sorted toTagged)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```
