# PR #6599 -- feat(ace): slice 8.1 — trust core consumes shared canonical-JSON (sorted toTagged + canonicalBytes)

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 6599 |
| Title | feat(ace): slice 8.1 — trust core consumes shared canonical-JSON (sorted toTagged + canonicalBytes) |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-06-02T17:02:04Z |
| Merged at | 2026-06-02T17:11:58Z |
| Merge commit SHA | `7419c468dfa4556c2a4c91a0d776b8ee33a2d657` |
| Branch | `otto-windows/ace-slice8.1-impl-2026-06-02` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/6599 |
| Changed files | 6 |
| Additions / deletions | +596 / -22 |

## Description

## Ace slice 8.1 — trust core consumes the shared canonical-JSON

Second step of the cross-language Ace trust core (after slice 8 SHA-256). Ace's trust core stops using its own ad-hoc canonicalizers and instead consumes the project's **shared, 4-language byte-locked canonical-JSON** (`src/Core.TypeScript/dynamic-value/json.ts` `canonicalJson`, byte-locked across TS/F#/C#/Rust). A future Rust/F#/C# Ace consumer then computes byte-identical `package_hash` for free from the byte-lock.

Spec: `docs/agendas/ace-package-manager/2026-06-02-ace-slice8.1-canonical-json-consume-design.md` (#6583, merged). Plan: `docs/agendas/ace-package-manager/2026-06-02-ace-slice8.1-canonical-json-implementation-plan.md` (in this PR).

### What changed

- **NEW `tools/ace/canonical.ts`** — the seam (own-the-interface): `toTagged(value)` (lexicographically **sorted** object keys, so the order-preserving `canonicalJson` keeps Ace's key-order-independence; **`Number.isSafeInteger`-guarded** — a float/NaN/out-of-range int is a bug and throws, never silently hashed; `undefined` props omitted) + `canonicalBytes(value) = encode(canonicalJson(toTagged(value)))`.
- **`signing.ts`** — `canonicalManifestBytes` + `canonicalIndexBytes` → `canonicalBytes`; local `canonicalize` deleted.
- **`resolve.ts`** — `packageHash` → `canonicalBytes`; the untrusted-dep pin-check call is wrapped to map a `canonicalBytes` throw to a clean `{ ok: false, reason: "invalid-package" }`. Local `canonicalJson` **retained** for `lockfile.ts` serialization (a deterministic-file-format concern, NOT a trust-core hashing site — out of scope, documented inline).

### Behavior-preservation

The canonical bytes legitimately shift (the shared `canonicalJson`'s escaping + int re-canonicalization differ from the old `JSON.stringify`). Ace is pre-release (no live registries) — accepted one-time change, no `format_version` bump. **`bun test tools/ace/` = 354 pass / 0 fail with zero test-literal edits** — every relevant assertion is a round-trip (sign→verify), a determinism check (key-order-independent bytes), or a self-consistent recompute (`packageHash`), none pin a literal byte/hash/sig.

### Review (subagent-driven, two-stage per task + final holistic)

- T1 code-quality caught a `Number.isInteger(1e21)`-true-but-`String`-exponential gap → fixed with `Number.isSafeInteger` (`d317e947b`).
- T2 code-quality caught the new float-throw escaping `resolve()` unguarded → mapped to `invalid-package` + regression test (`e669f7eae`).
- Final holistic review: **APPROVED** — sign/verify and pin compute/check each share one canonicalizer; security invariants preserved + float-field tightened; for realistic Ace manifests the new and old forms are byte-identical, so no persisted artifact could break.

### Follow-up (non-blocking, noted by final review)

CLI-layer (`ace.ts`) untrusted **root** manifest paths (install/update/registry) with a float field currently surface as `ace: fatal: …` rather than a styled `refused: invalid-package` — *not a trust hole* (package still refused, exit non-zero), pre-existing-shape, out of this slice's three-site scope. Candidate for a small CLI-error-consistency follow-up.

Gates: `bun test tools/ace/` 354/0 · `bun --bun tsc --noEmit -p tsconfig.json` exit 0 · pure-LF · markdownlint clean (plan).

🤖 Generated with [Claude Code](https://claude.com/claude-code)


## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | true |
| Total threads | 6 |
| Resolved threads | 6 |
| Unresolved threads | 0 |
| Total review comments | 6 |
| Total fix commits (touching thread paths) | 8 |

## Review threads

### Thread 1 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GgX29`
- **Path**: `tools/ace/canonical.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-06-02T17:03:37Z on `tools/ace/canonical.ts`:77 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Reject lone surrogates before hashing**

  When a manifest or index string contains an escaped lone surrogate such as `\uD800` (JSON.parse accepts this), `canonicalJson(toTagged(...))` preserves the invalid code unit and `TextEncoder` encodes it as U+FFFD. That makes distinct strings like `\uD800`, `\uD801`, and the real replacement character produce identical trust-core bytes, so a manifest or index signature/package hash can be replayed across byte-distinct metadata values. Since this function is now the shared hashing/signing seam, reject lone surrogates or escape them canonically before UTF-8 encoding.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 2 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GgbY0`
- **Path**: `tools/ace/canonical.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-02T17:07:04Z on `tools/ace/canonical.ts`:16 (association: NONE)

  <pre>
  The JSDoc describes `Number.isInteger`, but the implementation enforces `Number.isSafeInteger` (and also rejects NaN/Infinity/out-of-range ints). This comment is now misleading for a security-adjacent canonicalization seam; update it to match the actual constraint.
  </pre>

### Thread 3 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GgbZa`
- **Path**: `tools/ace/resolve.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-02T17:07:05Z on `tools/ace/resolve.ts`:25 (association: NONE)

  <pre>
  `packageHash` now depends on `canonicalBytes(...)`, which can throw on non-safe-integer numbers. Several call sites treat `packageHash` as non-throwing (e.g., `resolve()` seeds `byName` with `packageHash(root)` and the CLI uses `packageHash(np)` for lockfile pin checks). To keep “invalid input → clean refusal” invariants, consider introducing a non-throwing wrapper (e.g., `tryPackageHash` / `tryCanonicalBytes`) and using it at untrusted boundaries, or systematically wrapping `packageHash` calls in try/catch where inputs are untrusted JSON.
  </pre>

### Thread 4 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GgbZs`
- **Path**: `tools/ace/resolve.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-02T17:07:05Z on `tools/ace/resolve.ts`:153 (association: NONE)

  <pre>
  The error mapping uses `(e as Error).message`, which can yield `undefined` for non-Error throws and reduces diagnostic quality in the returned `detail`. Prefer extracting a message via `e instanceof Error ? e.message : String(e)`.
  </pre>

### Thread 5 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GgbZ_`
- **Path**: `docs/agendas/ace-package-manager/2026-06-02-ace-slice8.1-canonical-json-implementation-plan.md`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-02T17:07:05Z on `docs/agendas/ace-package-manager/2026-06-02-ace-slice8.1-canonical-json-implementation-plan.md`:163 (association: NONE)

  <pre>
  This implementation-plan code snippet still uses `Number.isInteger(...)` and describes only “non-integer” rejection, but the actual implementation in `tools/ace/canonical.ts` uses `Number.isSafeInteger(...)` (rejects NaN/Infinity/out-of-range ints too). Updating the plan avoids instructing future readers to implement a weaker check than what the codebase now relies on.
  </pre>

### Thread 6 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6Ggbaf`
- **Path**: `docs/agendas/ace-package-manager/2026-06-02-ace-slice8.1-canonical-json-implementation-plan.md`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-02T17:07:06Z on `docs/agendas/ace-package-manager/2026-06-02-ace-slice8.1-canonical-json-implementation-plan.md`:175 (association: NONE)

  <pre>
  The plan’s sample `toTagged` implementation uses `Number.isInteger` and a different error message, but the merged code uses `Number.isSafeInteger` and a more specific safe-integer-range error. Aligning this snippet reduces copy/paste drift.
  </pre>

## Fix commits (touching thread paths)

### `7b022981acd17223cb2f5da89366352ca53c1d17` -- 2026-06-02T16:46:55Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `docs/agendas/ace-package-manager/2026-06-02-ace-slice8.1-canonical-json-implementation-plan.md`

**Message:**

```
docs(ace): slice 8.1 implementation plan — consume shared canonical-J…

…SON (2-task)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `3c99633f5de0b541e187927ed49e377875e6811e` -- 2026-06-02T16:48:40Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/ace/canonical.ts`

**Message:**

```
feat(ace): slice 8.1 T1 — canonical.ts seam (sorted toTagged + canoni…

…calBytes) consuming shared canonical-JSON

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `d317e947bb5884089e54ab7abd8faca9685e3e72` -- 2026-06-02T16:52:06Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/ace/canonical.ts`

**Message:**

```
fix(ace): slice 8.1 T1 — guard toTagged with Number.isSafeInteger (cl…

…ose 1e21 exponential gap)

Code-quality review caught that Number.isInteger(1e21) is true but String(1e21)
yields '1e+21', which BigInt() rejects inside canonicalJson — a cryptic downstream
crash leaking the seam. Number.isSafeInteger is the single correct guard (rejects
floats/NaN/Infinity AND out-of-safe-range integers). Adds safe-bounds + empty-shape
tests + a sort-comment guarding the byte output against a localeCompare refactor.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `5620222cf3877d014cdba7ea2c747db42c125167` -- 2026-06-02T16:54:08Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/ace/resolve.ts`

**Message:**

```
feat(ace): slice 8.1 T2 — rewire trust-core canonical bytes onto shar…

…ed canonical-JSON

signing.ts canonicalIndexBytes/canonicalManifestBytes + resolve.ts packageHash now use
canonicalBytes (canonical.ts); signing.ts canonicalize deleted. resolve.ts canonicalJson
retained for lockfile serialization only. Canonical bytes change (intentional, pre-release,
no format_version bump); all Ace suites green (assertions are round-trip / determinism /
self-consistent recompute, no literal byte pins).

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `e669f7eaedea01018455b2860df6e3427ed2285d` -- 2026-06-02T16:58:26Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/ace/resolve.ts`

**Message:**

```
fix(ace): slice 8.1 T2 — map packageHash canonicalBytes-throw to clea…

…n invalid-package

Code-quality review caught that T1's Number.isSafeInteger guard makes canonicalBytes
throw on a float, while the old JSON.stringify path silently hashed it. In resolve()'s
walk, packageHash(dep) runs on untrusted fetched-package data unguarded — a malformed
dep with a float manifest field would escape resolve() as an unhandled exception instead
of a { ok:false, reason:'invalid-package' } refusal. Wrap the untrusted pin-check call;
add a regression test (float-dep → invalid-package, no throw).

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `6290158b9e50696f693a151e982bf9dbab97001f` -- 2026-06-02T17:01:34Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/ace/canonical.ts`

**Message:**

```
docs(ace): slice 8.1 — correct canonical.ts header (key_id rests on s…

…ha256(SPKI-DER), not canonicalBytes)

Final holistic review noted the header overstated canonicalBytes' dependents:
key_id = sha256(SPKI-DER) (signing.ts keyId), independent of canonicalBytes.
package_hash + index/manifest signing are the accurate dependents.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `978cf61b4a3b016b6d839015a696ceeab23aae28` -- 2026-06-02T17:06:35Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/ace/canonical.ts`

**Message:**

```
fix(ace): slice 8.1 — reject lone surrogates in toTagged (P1 trust-co…

…re byte-collision)

Codex review (P1): a lone surrogate (e.g. "\uD800", which JSON.parse accepts) passes
through the shared canonicalJson raw, then TextEncoder collapses it to U+FFFD — so
"\uD800", "\uD801", and real U+FFFD all hash to identical trust-core bytes, enabling
package_hash / signature replay across byte-distinct metadata. The old JSON.stringify path
escaped lone surrogates (well-formed stringify); the shared canonicalJson does not. Fix at
Ace's seam (not the byte-locked shared primitive): toTagged rejects lone surrogates in
string values AND object keys (fail-loud; resolve already maps the throw to invalid-package).
Valid surrogate pairs (astral code points) still accepted. +3 tests (357/0).

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `175e7b64b5fa829d8ddfd964ffaf2aa75e50ff0d` -- 2026-06-02T17:08:40Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `docs/agendas/ace-package-manager/2026-06-02-ace-slice8.1-canonical-json-implementation-plan.md`
- `tools/ace/canonical.ts`
- `tools/ace/resolve.ts`

**Message:**

```
docs+fix(ace): slice 8.1 — align isSafeInteger docs + robust catch me…

…ssage (review)

Copilot review: (1) canonical.ts JSDoc + (2) plan snippet said Number.isInteger but
the impl uses Number.isSafeInteger — aligned both. (3) resolve.ts invalid-package catch
used (e as Error).message (undefined for non-Error throws) — now e instanceof Error ?
e.message : String(e). No behavior change to the trust core; 357/0, tsc 0, markdownlint clean.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```
