# PR #6616 -- proof(081KT2T2J0008QG0R000YZ3NMY C1): Gaussian message product is a commutative group — Z3 + FsCheck (first real proof)

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 6616 |
| Title | proof(081KT2T2J0008QG0R000YZ3NMY C1): Gaussian message product is a commutative group — Z3 + FsCheck (first real proof) |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-06-02T18:36:42Z |
| Merged at | 2026-06-02T18:39:50Z |
| Merge commit SHA | `dc4f9d18edacee13146c125cab20cecde4d15880` |
| Branch | `otto-cli/b1007-c1-gaussian-group-proof-z3-fscheck-2026-06-02` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/6616 |
| Changed files | 3 |
| Additions / deletions | +172 / -0 |

## Description

## What

The **first formal proof** closing 081KT2T2J0008QG0R000YZ3NMY's P0 — and the repo's first move off ~0 proven primitives. Gaussian message product = **abelian group** on the natural-parameter representation (ν, τ): product = component-wise add, divide = subtract, identity = `One` (0,0). Two independent tools per BP-16:

- **Z3** (`tests/Tests.FSharp/Formal/Z3.Laws.Tests.fs`) — proves the **ideal-real algebra is an abelian group** symbolically: associativity, commutativity, identity, inverse (divide=subtract), EP-cavity round-trip `(a*b)/b=a`, + guarded proper-closure. 6 lemmas, QF_LRA, reuses `z3ScriptHolds` (zero new wiring; sits next to the existing Z-set abelian-group lemmas).
- **FsCheck** (`tests/Bayesian.Tests/Message.Tests.fs`) — proves the **float impl conforms** over the proper domain (τ>0): 6 properties, natural-parameter equality, **tolerance not relaxed**. Wires `FsCheck` + `FsCheck.Xunit.v3` into `Bayesian.Tests` (the repo's FsCheck-3 `[<Property>]` + `NormalFloat`-param convention; `#nowarn 0893`).

**The load-bearing split:** Z3 proves *the model is a group* (exact, no tolerance); FsCheck proves *the float code conforms* (within tolerance, on the domain). Disagreement IS the finding — neither alone is the proof.

## Verification

**Run locally with z3 on PATH:** FsCheck **6/6 passed**, Z3 **7/7 passed**, **0 skipped**. The proof actually executes — not consensus, not example-tests.

## Discipline

- Authored by **Soraya** (formal-verification-expert) per 081KT2T2J0008QG0R000YZ3NMY; cleanroom (no Infer.NET source).
- Anchor: KFL 2001 (sum-product), Minka 2001 (EP cavity = divide), Wainwright–Jordan 2008 §3 (exp-family natural params = free abelian group).
- Per **formal-proof-first**: this closes acceptance-half **(a) proof** for C1. The **hex/4×4 lineage edge (half b)** is owed before C1 is *canonical* — until then it is **validated/proven, not canonical**.

## ⚠️ Known gap (pre-existing — flagged, not introduced here)

`z3` is **not installed in `gate.yml` build-and-test**, so the **Z3 half self-skips in CI** (green-by-skip — the assert-don't-skip shape). I verified it **locally** with z3 present instead. **Follow-up owed:** add z3 to the CI build-and-test job so the Z3 proofs are *enforced*, not skipped. The FsCheck half runs in CI normally.

Next in the cadence: C2 (Beta), C3 (Bernoulli) — same Z3+FsCheck pair.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 3 |
| Resolved threads | 0 |
| Unresolved threads | 3 |
| Total review comments | 3 |
| Total fix commits (touching thread paths) | 1 |

## Review threads

### Thread 1 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM6Gh88-`
- **Path**: `tests/Tests.FSharp/Formal/Z3.Laws.Tests.fs`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-02T18:39:53Z on `tests/Tests.FSharp/Formal/Z3.Laws.Tests.fs`:454 (association: NONE)

  <pre>
  Repo convention forbids persona-name attribution in code/tests; use role references instead (see .github/copilot-instructions.md:317-355).
  </pre>

### Thread 2 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM6Gh89d`
- **Path**: `tests/Bayesian.Tests/Message.Tests.fs`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-02T18:39:53Z on `tests/Bayesian.Tests/Message.Tests.fs`:190 (association: NONE)

  <pre>
  This comment block claims the group holds over the proper-message domain, but `Gaussian.One` (τ=0) is the identity and inverses typically have τ&lt;0, so the group is on all (ν, τ) (ℝ²) while the proper subset is only closed under product. The wording here should be corrected to avoid a mathematically false statement.
  </pre>

### Thread 3 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM6Gh89y`
- **Path**: `tests/Bayesian.Tests/Message.Tests.fs`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-02T18:39:53Z on `tests/Bayesian.Tests/Message.Tests.fs`:196 (association: NONE)

  <pre>
  Repo convention forbids persona-name attribution in code/tests; use role references instead (see .github/copilot-instructions.md:317-355). Also, “uniform identity” is ambiguous here—prefer directly stating that `Gaussian.One` is the identity element (uniform message).
  </pre>

## Fix commits (touching thread paths)

### `a85c2c25763cbded7ab4b0928d48d91ed29db016` -- 2026-06-02T18:36:29Z -- ``

**Touched files (intersect with thread paths):**

- `tests/Bayesian.Tests/Message.Tests.fs`
- `tests/Tests.FSharp/Formal/Z3.Laws.Tests.fs`

**Message:**

```
proof(081KT2T2J0008QG0R000YZ3NMY C1): Gaussian message product is a commutative group — Z…

…3 + FsCheck (the first real proof)

The first formal proof closing 081KT2T2J0008QG0R000YZ3NMY's P0. Gaussian product = abelian group on
the natural-parameter representation (ν, τ): product = component-wise add, divide =
subtract, identity = One (0,0). Two independent tools per BP-16:

- Z3 (tests/Tests.FSharp/Formal/Z3.Laws.Tests.fs): proves the IDEAL-REAL algebra is
  an abelian group symbolically (assoc/commut/identity/inverse/cavity-round-trip +
  guarded proper-closure) — 6 lemmas, QF_LRA. Mirrors the existing Z-set
  abelian-group lemmas; reuses z3ScriptHolds (zero new wiring).
- FsCheck (tests/Bayesian.Tests/Message.Tests.fs): proves the FLOAT impl CONFORMS
  over the proper domain (τ>0) — 6 properties, natural-parameter equality, tolerance
  NOT relaxed. Wires FsCheck + FsCheck.Xunit.v3 into Bayesian.Tests (repo's
  FsCheck-3 [<Property>] + NormalFloat-param convention; #nowarn 0893).

What each proves (the load-bearing split): Z3 = the model is a group (exact, no
tolerance); FsCheck = the float code conforms (within tolerance, on the domain).
Disagreement IS the finding. Authored by Soraya (formal-verification-expert).

VERIFIED LOCALLY (z3 on PATH): FsCheck 6/6 passed, Z3 7/7 passed, 0 skipped.

Anchor: KFL 2001 (sum-product), Minka 2001 (EP cavity = divide), Wainwright-Jordan
2008 §3 (exp-family natural params = free abelian group). Cleanroom; no Infer.NET
source. Per formal-proof-first: this closes acceptance-half (a) for C1 (proof);
the hex/4×4 lineage edge (half b) is owed before C1 is *canonical* — until then it
is *validated/proven*, not canonical.

Known gap (pre-existing, flagged): z3 is not installed in gate.yml build-and-test,
so the Z3 half self-skips in CI (green-by-skip). Verified locally instead; a
follow-up adds z3 to CI so the Z3 proofs are enforced (assert-don't-skip).

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
Co-Authored-By: Soraya <noreply@anthropic.com>
```
