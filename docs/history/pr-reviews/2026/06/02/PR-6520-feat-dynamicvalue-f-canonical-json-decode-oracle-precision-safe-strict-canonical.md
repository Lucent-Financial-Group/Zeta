# PR #6520 -- feat(DynamicValue): F# canonical-JSON DECODE oracle — precision-safe + strict-canonical

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 6520 |
| Title | feat(DynamicValue): F# canonical-JSON DECODE oracle — precision-safe + strict-canonical |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-06-02T01:43:13Z |
| Merged at | 2026-06-02T01:52:10Z |
| Merge commit SHA | `b5e78a857e9304e371b021c2eb9360f4dc313a37` |
| Branch | `otto-cli/dynamicvalue-decode-json-fsharp-2026-06-01` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/6520 |
| Changed files | 3 |
| Additions / deletions | +352 / -0 |

## Description

Third of the four JSON-decode oracles (TS #6518, C# #6519 landed/landing; Rust follows). `fromCanonicalJson` is the inverse of `toCanonicalJson` for the 6 locked shapes (Float + Bytes DEFERRED in JSON — they lock under CBOR).

## What

- Adds `fromCanonicalJson` to `DynamicValue.fs`, mirroring the CBOR-decode functional style (mutable `pos` + `let rec readValue () … and …`) + the **shared `DecodeError` DU** + the fixed-point canonical check.
- `DynamicValue.JsonDecode.Tests.fs`: round-trip over the seed (structural) + 23 rejection assertions.

## Precision-safe + strict-canonical

- **int64 precision**: number token parsed as **text** via `System.Int64.TryParse` (never via a float); on a `-?[0-9]+` token the only failure is overflow → `IntegerOverflow`.
- **fixed-point**: `toCanonicalJson decoded = input` → else `NonCanonical` (rejects whitespace, non-minimal escapes, leading zeros).
- **deferred**: a number with `.`/`e`/`E` → `Unsupported`.
- **carries the TS/C# malformed-input lessons**: `\uXXXX` requires exactly 4 hex digits (`UInt16.TryParse` HexNumber); `.`/exponent require digits (`consumeDigits`) — so `1.`/`1e`/`1e+` → `UnexpectedEnd`, not `Unsupported`.
- never thrown: `Result<DynamicValue, DecodeError>`.

**2 facts pass (round-trip + 23-case rejection batch); Release build 0-warnings.** The compilers don't lie.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 4 |
| Resolved threads | 4 |
| Unresolved threads | 0 |
| Total review comments | 4 |
| Total fix commits (touching thread paths) | 3 |

## Review threads

### Thread 1 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GSpvV`
- **Path**: `src/Core/DynamicValue.fs`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-02T01:46:35Z on `src/Core/DynamicValue.fs`:721 (association: NONE)

  <pre>
  Docstring says non-canonical forms include leading '+' signs, but this decoder does not accept a leading '+' at all (it returns UnexpectedEnd). To avoid misleading API docs, drop the '+' mention or clarify it’s invalid JSON rather than a non-canonical form.
  </pre>

### Thread 2 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GSpvh`
- **Path**: `src/Core/DynamicValue.fs`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-02T01:46:36Z on `src/Core/DynamicValue.fs`:753 (association: NONE)

  <pre>
  This comment claims a “partial parse” would silently accept malformed `\uXXXX`, but `UInt16.TryParse` does not partially parse; it fails if any character is non-hex. Consider rewording to match the actual failure mode.
  </pre>

### Thread 3 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GSpvj`
- **Path**: `src/Core/DynamicValue.fs`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-02T01:46:36Z on `src/Core/DynamicValue.fs`:759 (association: NONE)

  <pre>
  `Substring` here allocates on every `\uXXXX` escape. Since this runs in the inner loop, consider using `AsSpan` with the span-based `TryParse` overload to avoid per-escape allocations.
  </pre>

### Thread 4 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GSpvq`
- **Path**: `src/Core/DynamicValue.fs`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-02T01:46:36Z on `src/Core/DynamicValue.fs`:846 (association: NONE)

  <pre>
  `Substring` allocates for every integer token. You can avoid this by using the span-based `Int64.TryParse` overload (`json.AsSpan(start, pos - start)`).
  </pre>

## Fix commits (touching thread paths)

### `77f9174005d6a1d0bf375b92cd651e4d3c117151` -- 2026-06-02T01:43:03Z -- ``

**Touched files (intersect with thread paths):**

- `src/Core/DynamicValue.fs`

**Message:**

```
feat(DynamicValue): F# canonical-JSON DECODE oracle — precision-safe …

…+ strict-canonical

Third of the four JSON-decode oracles (TS #6518, C# #6519; Rust follows). fromCanonicalJson is
the inverse of toCanonicalJson for the 6 locked shapes (Float + Bytes DEFERRED in JSON — they
lock under CBOR). Mirrors the CBOR-decode functional style (mutable pos + let rec readValue ()
... and ...) + the shared DecodeError DU + the fixed-point canonical check.

- precision-safe int64: number token parsed as TEXT via System.Int64.TryParse (never via a float);
  the only failure on a -?[0-9]+ token is overflow → IntegerOverflow.
- strict-canonical via fixed-point: toCanonicalJson decoded = input → else NonCanonical
  (rejects whitespace, non-minimal escapes, leading zeros).
- Float-shaped number (.eE) → Unsupported (deferred in v1 JSON).
- carries the TS/C# malformed-input lessons: \uXXXX requires exactly 4 hex digits
  (UInt16.TryParse HexNumber on the 4-char substring); '.'/exponent require digits
  (consumeDigits → UnexpectedEnd if none) — so 1./1e/1e+ → UnexpectedEnd, not Unsupported.
- never thrown: Result<DynamicValue, DecodeError> per the Result-over-exception rule.

DynamicValue.JsonDecode.Tests: round-trip over the seed (structural) + 23 rejection assertions
(malformed / deferred-float / oversized / non-canonical). 2 facts pass; Release build 0-warnings.
The compilers don't lie.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `894dac09b2a8db6caed17c42802d64491aaa6b0a` -- 2026-06-02T01:45:28Z -- ``

**Touched files (intersect with thread paths):**

- `src/Core/DynamicValue.fs`

**Message:**

```
fix(DynamicValue): F# JSON \uXXXX uses AllowHexSpecifier not HexNumbe…

…r (whitespace-permissive)

Pre-empting the Codex P2 found on the C# oracle (#6519): NumberStyles.HexNumber also permits
leading/trailing whitespace, so a malformed escape like "\u 001" / "\u001 " was trimmed +
accepted as a decoded char (surfacing as NonCanonical) instead of the promised UnexpectedEnd.
AllowHexSpecifier parses hex without the whitespace flags. Added whitespace-\u rejection tests.
2 facts pass; Release 0-warnings.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `cc9e208a6a5683b2d50eeab2a5b94e59977eaa98` -- 2026-06-02T01:49:13Z -- ``

**Touched files (intersect with thread paths):**

- `src/Core/DynamicValue.fs`

**Message:**

```
fix(DynamicValue): F# JSON decode — doc accuracy + AsSpan-not-Substri…

…ng (Copilot #6520)

- doc: a leading '+' is invalid JSON → UnexpectedEnd, not a NonCanonical form; drop '+' from
  the NonCanonical examples in the fromCanonicalJson docstring.
- perf: \uXXXX hex parse + integer-token parse use System.MemoryExtensions.AsSpan(json, ...)
  with the span-based TryParse overloads instead of json.Substring, avoiding a per-escape /
  per-token allocation in the inner loop (matches the C# oracle's AsSpan usage). F# lacks an
  'open System', so the extension is called explicitly.

(The 'partial parse' comment Copilot flagged was already replaced by the earlier AllowHexSpecifier
fix.) 2 facts pass; Release 0-warnings.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```
