# PR #6500 -- feat(DynamicValue): re-ground F# against the seed — canonical JSON encoder + byte-lock test

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 6500 |
| Title | feat(DynamicValue): re-ground F# against the seed — canonical JSON encoder + byte-lock test |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-06-01T23:42:54Z |
| Merged at | 2026-06-02T00:05:16Z |
| Merge commit SHA | `f77625d426a81809cc5fc14adb089276d5260cbd` |
| Branch | `otto-cli/dynamicvalue-fsharp-reground-seed-2026-06-01` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/6500 |
| Changed files | 3 |
| Additions / deletions | +189 / -2 |

## Description

Follow-up to the seed (#6499, merged). **Seed-first** (Aaron 2026-06-01: _"we are growing code from the seeds"_): the F# oracle now AGREES with the shared seed (`src/Core.TypeScript/dynamic-value/golden-vectors.json`), not the other way round. Two oracles now independently agree on the seed: **TS** (32 green, #6499) + **F#** (byte-lock green, here).

## What's here

- **`DynamicValue.toCanonicalJson`** + `escapeJsonString` — the canonical-encode side of the byte-lock. Minified; **Object keys in INSERTION order** (NOT sorted — Object is order-significant; key-sorting would be lossy/non-bijective); Int = bare exact decimal (invariant); String = RFC 8259 minimal escaping (`/` not escaped; control U+0000..001F short-form or `\u00XX` lowercase; raw UTF-8 else). **Float + Bytes raise** `InvalidOperationException` (DEFERRED — lock under CBOR or tagged-JSON).
- **`DynamicValue.GoldenVectors.Tests.fs`** — loads the 31 seed vectors, builds DynamicValue from the tagged form, asserts `toCanonicalJson(value) === json`. Green (Release, 0 warnings).
- **Fixes the `DynamicValue.fs` doc-comment contradiction** the seed surfaced — it said the wire encoder "sorts keys when byte-locking"; corrected to order-PRESERVING.

Next: **C# re-ground** (same shape); then **Rust** + the **decode** side (precision-safe int64 tokenizer); then Float/Bytes via CBOR (or tagged-JSON) if called.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | true |
| Total threads | 9 |
| Resolved threads | 6 |
| Unresolved threads | 3 |
| Total review comments | 9 |
| Total fix commits (touching thread paths) | 3 |

## Review threads

### Thread 1 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GRrUv`
- **Path**: `src/Core/DynamicValue.fs`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-06-01T23:45:06Z on `src/Core/DynamicValue.fs`:338 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Return a modeled result for deferred variants**

  When callers encode a legal `DynamicValue.Float` (and the analogous `Bytes` branch below), this new public encoder throws `InvalidOperationException` instead of surfacing the deferred status as data. That makes canonicalization an exception-based partial function for variants the DU explicitly admits; AGENTS.md's repo-wide rule says user-visible errors must be `Result&lt;_, DbspError&gt;`/AppendResult-style, not exceptions. Return a modeled decline/error value or restrict the accepted input type so these legal payloads cannot escape as unhandled exceptions.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 2 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GRrUz`
- **Path**: `src/Core/DynamicValue.fs`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-06-01T23:45:06Z on `src/Core/DynamicValue.fs`:317 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Escape lone surrogate code units**

  For .NET strings that contain an unpaired UTF-16 surrogate, this branch appends the raw surrogate into the supposedly canonical JSON text. Those strings can be constructed by .NET callers and by adapters for arbitrary self-describing payloads; the resulting text is not valid Unicode JSON, and byte-locking via UTF-8 will typically replace the surrogate, causing non-bijective/colliding bytes. Escape lone surrogates as `\uXXXX` (while still leaving valid surrogate pairs/ordinary Unicode raw per the seed) so every `DynamicValue.String` produces valid canonical JSON bytes.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 3 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GRsIw`
- **Path**: `tests/Tests.FSharp/DynamicValue.GoldenVectors.Tests.fs`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-01T23:46:31Z on `tests/Tests.FSharp/DynamicValue.GoldenVectors.Tests.fs`:13 (association: NONE)

  <pre>
  P1: Code comment uses a direct personal name for attribution. Per the repo’s “No name attribution in code, docs, or skills” standing rule, current-state surfaces like tests should use role-refs or neutral phrasing instead (see docs/AGENT-BEST-PRACTICES.md:671-759).
  </pre>

### Thread 4 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GRsI_`
- **Path**: `tests/Tests.FSharp/DynamicValue.GoldenVectors.Tests.fs`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-01T23:46:31Z on `tests/Tests.FSharp/DynamicValue.GoldenVectors.Tests.fs`:44 (association: NONE)

  <pre>
  P0: Avoid using Seq pipeline helpers over JsonElement.ArrayEnumerator. This pattern is already called out in the Observe golden-vector tests as Release-unsafe because the enumerator is a mutable struct that gets boxed through Seq (tests/Tests.FSharp/Observe/GoldenVectors.Tests.fs:55-59). Use an eager array comprehension instead.
  </pre>

### Thread 5 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GRsJK`
- **Path**: `tests/Tests.FSharp/DynamicValue.GoldenVectors.Tests.fs`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-01T23:46:31Z on `tests/Tests.FSharp/DynamicValue.GoldenVectors.Tests.fs`:54 (association: NONE)

  <pre>
  P0: Same issue as above: avoid `EnumerateArray() |&gt; Seq.toArray` on JsonElement.ArrayEnumerator (mutable struct + Seq boxing can corrupt state in Release). Prefer an eager `for`/comprehension-based array build (see tests/Tests.FSharp/Observe/GoldenVectors.Tests.fs:55-59).
  </pre>

### Thread 6 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GRx5x`
- **Path**: `src/Core/DynamicValue.fs`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-06-01T23:57:06Z on `src/Core/DynamicValue.fs`:388 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Return an error for null string payloads**

  When a .NET caller constructs `DynamicValue.String null` (or an object pair with a null key in the branch below), this new public encoder still throws from `escapeJsonString` even though `toCanonicalJson` advertises a `Result` surface and the repo rule forbids user-visible exception-based failure. This can happen from nullable-disabled C# callers or F# interop despite `DynamicValue.Null` being the intended null shape, so either reject/represent these malformed string nodes as an `EncodeError` or make construction enforce non-null strings before encoding.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 7 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM6GR10E`
- **Path**: `src/Core/DynamicValue.fs`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-02T00:05:21Z on `src/Core/DynamicValue.fs`:148 (association: NONE)

  <pre>
  `EncodeError` should include a case for invalid/malformed values that cannot be canonically encoded without losing information (e.g., `DynamicValue.String null` / null object keys). Without an explicit error, the encoder either throws or silently normalizes, both of which undermine the byte-lock’s bijectivity guarantees.
  </pre>

### Thread 8 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM6GR10P`
- **Path**: `src/Core/DynamicValue.fs`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-02T00:05:21Z on `src/Core/DynamicValue.fs`:403 (association: NONE)

  <pre>
  `toCanonicalJson` currently relies on `escapeJsonString`’s null-&gt;"" normalization for string values and object keys. For a canonical encoder, it’s safer to explicitly reject null strings/keys (returning `Error`) so that encoding remains injective and malformed states don’t silently pass the byte-lock.
  </pre>

### Thread 9 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM6GR10b`
- **Path**: `src/Core/DynamicValue.fs`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-02T00:05:21Z on `src/Core/DynamicValue.fs`:389 (association: NONE)

  <pre>
  PR description says Float/Bytes raise `InvalidOperationException`, but the implementation returns `Error EncodeError.*` (which is consistent with the Result-over-exception approach). Please align the PR description (or adjust code) so reviewers/users aren’t misled about whether encoding failures are exceptions or data.
  </pre>

## Fix commits (touching thread paths)

### `60b5348e1dd353a7a81fb5a838d86250dfd08fb1` -- 2026-06-01T23:41:57Z -- ``

**Touched files (intersect with thread paths):**

- `src/Core/DynamicValue.fs`
- `tests/Tests.FSharp/DynamicValue.GoldenVectors.Tests.fs`

**Message:**

```
feat(DynamicValue): re-ground F# against the seed — canonical JSON en…

…coder + byte-lock test

Seed-first (Aaron 2026-06-01): the F# oracle now AGREES with the shared
seed (src/Core.TypeScript/dynamic-value/golden-vectors.json), not the
other way round. Adds DynamicValue.toCanonicalJson (the canonical-encode
side of the byte-lock) + escapeJsonString, and a seed-conformance test
that loads the 31 vectors and asserts encode(value) === json. Two oracles
now agree on the seed (TS 32 green + F# byte-lock green).

- toCanonicalJson: minified; Object keys in INSERTION order (NOT sorted —
  Object is order-significant, key-sorting would be lossy/non-bijective);
  Int = bare exact decimal (invariant); String = RFC 8259 minimal escaping
  ('/' not escaped, control U+0000..001F short-form or \u00XX lowercase,
  raw UTF-8 else). Float + Bytes raise InvalidOperationException (DEFERRED).
- Fixes the DynamicValue.fs doc comment that said the wire encoder 'sorts
  keys when byte-locking' — corrected to order-PRESERVING (the contradiction
  the seed surfaced).

Stacked on the seed PR (#6499); GitHub auto-retargets to main when #6499
merges. Next: C# re-ground (same shape); then Rust + the decode side.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `438c06eece0a33d03450d9159f2b6ca768f9917b` -- 2026-06-01T23:54:58Z -- ``

**Touched files (intersect with thread paths):**

- `src/Core/DynamicValue.fs`
- `tests/Tests.FSharp/DynamicValue.GoldenVectors.Tests.fs`

**Message:**

```
fix(#6500): address review threads — Result-shaped encoder, lone-surr…

…ogate escape, eager loader, role-refs

Per AGENTS.md hard rule (user-visible errors are Result, not exceptions):
- toCanonicalJson now returns Result<string, EncodeError> (FloatDeferred |
  BytesDeferred); deferred variants surface as Error DATA, never thrown
  (Codex P1). Clean Result-propagation via fold/bind/map through the recursion.
- escapeJsonString now lone-surrogate-safe: valid surrogate PAIRS emit the
  astral char raw; LONE surrogates are \u-escaped (raw would be invalid
  Unicode + non-bijective under UTF-8 byte-lock) (Codex P2).
- test: eager  helper ([| for … -> … |], direct enumerator loop)
  replaces every EnumerateArray() |> Seq.toArray — JsonElement.ArrayEnumerator
  is a mutable struct that corrupts through Seq boxing in Release (Copilot P0).
- test + encoder docs: role-refs ('the maintainer') not a personal name
  (Copilot P1, no-name-attribution-in-code rule).

Build clean (Release, 0 warnings); byte-lock test green (Ok json for all 31).

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `453f8904aedeeb114d2643c1e6018f2a1638ae87` -- 2026-06-02T00:01:41Z -- ``

**Touched files (intersect with thread paths):**

- `src/Core/DynamicValue.fs`

**Message:**

```
fix(#6500): null-safe escapeJsonString — no NRE on a malformed null S…

…tring payload / object key

Codex P2: a DynamicValue.String null (or null object key, reachable via
nullable-disabled C# / interop) made the Result-advertising encoder throw an
NRE in escapeJsonString. Normalize null -> empty (mirroring the records
default-array normalization) so the encoder stays exception-free. Uses
System.Object.ReferenceEquals (the module shadows F# isNull with the
DynamicValue tag accessor).

Build clean (Release, 0 warnings); byte-lock test green.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```
