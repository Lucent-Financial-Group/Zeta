# PR #6522 -- feat(DynamicValue): Rust canonical-JSON DECODE oracle — completes the 4-oracle JSON decode set

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 6522 |
| Title | feat(DynamicValue): Rust canonical-JSON DECODE oracle — completes the 4-oracle JSON decode set |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-06-02T01:52:53Z |
| Merged at | 2026-06-02T02:06:02Z |
| Merge commit SHA | `cda12eb3c7aed22cdf7d31db53fdf4fdafdb51bf` |
| Branch | `otto-cli/dynamicvalue-decode-json-rust-2026-06-01` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/6522 |
| Changed files | 2 |
| Additions / deletions | +419 / -8 |

## Description

Last of the four JSON-decode oracles (TS #6518, C# #6519, F# #6520). `from_canonical_json` is the inverse of `to_canonical_json` for the 6 locked shapes (Float + Bytes DEFERRED in JSON — they lock under CBOR). **This completes the JSON decode set across all four oracles.**

## What

- Adds `from_canonical_json` + free recursive-descent readers to `lib.rs`, mirroring the CBOR-decode free-function pattern + the **shared `DecodeError` enum** + the fixed-point canonical check.
- `tests/json_decode_cross_verify.rs`: round-trip over the seed (structural) + malformed / trailing / deferred-float / oversized / non-canonical rejection cases.

## Precision-safe + strict-canonical

- operates on `Vec<char>` (Unicode scalar values) so structural scanning never splits a multi-byte UTF-8 char.
- **int64 precision**: number token collected as text + `str::parse::<i64>` (never via `f64`); only failure on a `-?[0-9]+` token is overflow → `IntegerOverflow`.
- **fixed-point**: `to_canonical_json() == input` → else `NonCanonical`.
- **deferred**: a number with `.`/`e`/`E` → `Unsupported`.
- **bakes in every accumulated oracle lesson from the start**: `\uXXXX` requires exactly 4 hex digits (`char::to_digit(16)` per digit — rejects whitespace + non-hex, no lenient trim); `.`/exponent require digits → `1.`/`1e`/`1e+` are `UnexpectedEnd`; lone UTF-16 surrogate in `\u` → `UnexpectedEnd`.
- never panics: `Result<DynamicValue, DecodeError>`.

**4 tests pass; `cargo fmt` + `clippy -D warnings` clean.**

DynamicValue is now fully **decode-locked in both CBOR (total, 8 shapes) and JSON (6 shapes) across all four oracles**. The compilers don't lie.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | true |
| Total threads | 5 |
| Resolved threads | 5 |
| Unresolved threads | 0 |
| Total review comments | 5 |
| Total fix commits (touching thread paths) | 3 |

## Review threads

### Thread 1 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GSu5c`
- **Path**: `src/Core.Rust.DynamicValue/src/lib.rs`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-06-02T01:54:35Z on `src/Core.Rust.DynamicValue/src/lib.rs`:700 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Handle escaped surrogate pairs as non-canonical JSON**

  For valid JSON that represents an astral scalar with UTF-16 escapes, e.g. `"\uD83D\uDE00"`, this branch rejects the high surrogate immediately as `UnexpectedEnd`. The canonical rule says astral characters are emitted raw UTF-8, so this input is structurally valid but non-canonical; the existing TS oracle decodes `\uXXXX` code units and then the fixed-point check reports `NonCanonical`. As written, Rust breaks the cross-oracle decode-error parity for any JSON producer that escapes emoji/non-BMP characters instead of emitting them raw.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 2 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GSxd0`
- **Path**: `src/Core.Rust.DynamicValue/src/lib.rs`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-02T01:58:21Z on `src/Core.Rust.DynamicValue/src/lib.rs`:700 (association: NONE)

  <pre>
  P1: `read_json_escape` currently rejects any `\uXXXX` in the UTF-16 surrogate ranges via `char::from_u32`, which means valid JSON surrogate-pair escapes (e.g. `"\uD83D\uDE00"`) are reported as `UnexpectedEnd` instead of being decoded and then rejected by the fixed-point check as `NonCanonical`. Supporting surrogate pairs here also matches the "lenient parse → fixed-point canonical check" contract and other Rust JSON readers in the repo.
  </pre>

### Thread 3 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GSxeT`
- **Path**: `src/Core.Rust.DynamicValue/tests/json_decode_cross_verify.rs`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-02T01:58:21Z on `src/Core.Rust.DynamicValue/tests/json_decode_cross_verify.rs`:111 (association: NONE)

  <pre>
  Add a regression assertion for UTF-16 surrogate escapes: a lone high surrogate in a `\uXXXX` escape should be rejected (per decoder docs / invariant that Rust `char` is a Unicode scalar value).
  </pre>

### Thread 4 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GSxet`
- **Path**: `src/Core.Rust.DynamicValue/tests/json_decode_cross_verify.rs`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-02T01:58:22Z on `src/Core.Rust.DynamicValue/tests/json_decode_cross_verify.rs`:135 (association: NONE)

  <pre>
  Add a non-canonical regression case for surrogate *pairs*: `"\uD83D\uDE00"` is valid JSON but not canonical for 😀 (canonical emits the raw scalar), so it should parse and then fail the fixed-point check with `NonCanonical` (not `UnexpectedEnd`).
  </pre>

### Thread 5 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GSyzO`
- **Path**: `src/Core.Rust.DynamicValue/src/lib.rs`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-06-02T02:00:23Z on `src/Core.Rust.DynamicValue/src/lib.rs`:240 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Make DecodeError messages format-neutral**

  When this new JSON API returns an error, callers that log or surface the public `DecodeError` via `Display` still get CBOR-specific text from the shared enum implementation, e.g. `DynamicValue::from_canonical_json("tru").unwrap_err()` displays as a truncated-CBOR failure and `Unsupported` mentions CBOR simple values/tags. Since this method is documented as canonical-JSON decode, JSON parse/deferred-float failures will produce misleading user-visible diagnostics unless the shared error messages are made format-neutral or split per codec.

  Useful? React with 👍 / 👎.
  </pre>

## Fix commits (touching thread paths)

### `a09a3fca9de37239c14a43902f47b2d41e705ec9` -- 2026-06-02T01:52:39Z -- ``

**Touched files (intersect with thread paths):**

- `src/Core.Rust.DynamicValue/src/lib.rs`
- `src/Core.Rust.DynamicValue/tests/json_decode_cross_verify.rs`

**Message:**

```
feat(DynamicValue): Rust canonical-JSON DECODE oracle — completes the…

… 4-oracle JSON decode set

Last of the four JSON-decode oracles (TS #6518, C# #6519, F# #6520). from_canonical_json is the
inverse of to_canonical_json for the 6 locked shapes (Float + Bytes DEFERRED in JSON — they lock
under CBOR). Mirrors the CBOR-decode free-function pattern + shared DecodeError enum + fixed-point.

- operates on Vec<char> (Unicode scalar values) so structural scanning never splits a multi-byte
  UTF-8 char and raw string content reconstructs faithfully.
- precision-safe int64: number token collected as text and parsed via str::parse::<i64> (never via
  f64); on a -?[0-9]+ token the only failure is overflow → IntegerOverflow.
- strict-canonical via fixed-point: to_canonical_json() == input → else NonCanonical (rejects
  whitespace, non-minimal escapes, leading zeros).
- Float-shaped number (.eE) → Unsupported (deferred in v1 JSON).
- bakes in all the accumulated oracle lessons from the start: \uXXXX requires exactly 4 hex digits
  (char::to_digit(16) per digit — rejects whitespace + non-hex, no lenient trim); '.'/exponent
  require digits (consume_json_digits) so 1./1e/1e+ → UnexpectedEnd not Unsupported; lone UTF-16
  surrogate in \u → UnexpectedEnd.
- never panics: Result<DynamicValue, DecodeError> per the Result-over-exception rule.

json_decode_cross_verify: round-trip over the seed (structural; JSON has no floats so PartialEq is
exact) + malformed / trailing / deferred-float / oversized / non-canonical rejection cases. 4 tests
pass; cargo fmt + clippy -D warnings clean.

DynamicValue is now fully decode-locked in both CBOR (total, 8 shapes) and JSON (6 shapes) across
all four oracles. The compilers don't lie.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `f6a3da232b1f61f6924b5bc172aafddeb853f91b` -- 2026-06-02T01:56:54Z -- ``

**Touched files (intersect with thread paths):**

- `src/Core.Rust.DynamicValue/src/lib.rs`
- `src/Core.Rust.DynamicValue/tests/json_decode_cross_verify.rs`

**Message:**

```
fix(DynamicValue): Rust JSON decoder combines \uXXXX surrogate pairs …

…for cross-oracle parity (Codex #6522)

Valid JSON that escapes an astral scalar as a UTF-16 surrogate pair (e.g. "\uD83D\uDE00" for 😀)
was rejected immediately as UnexpectedEnd because char::from_u32 fails on the high surrogate. The
TS/C#/F# oracles hold UTF-16 strings, so they decode the pair and then report NonCanonical via the
fixed-point check (canonical emits raw UTF-8). Rust now combines a high surrogate (0xD800..=0xDBFF)
with a following \uXXXX low surrogate (0xDC00..=0xDFFF) into the astral scalar → decodes → the
fixed-point check reports NonCanonical, matching the other three oracles for emoji-escaping producers.

A lone/unpaired surrogate stays UnexpectedEnd — Rust's char cannot hold one, an honest language-level
difference (the UTF-16 oracles would report NonCanonical, but both are 'rejected'). Extracted
read_u4_hex helper (used for both high + low). Added surrogate-pair (NonCanonical) + lone-surrogate
(UnexpectedEnd) tests. 5 tests pass; cargo fmt + clippy -D warnings clean.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `4a1df979bd13bb1b7f6c62bdda7337f2cae120e6` -- 2026-06-02T02:03:08Z -- ``

**Touched files (intersect with thread paths):**

- `src/Core.Rust.DynamicValue/src/lib.rs`

**Message:**

```
fix(DynamicValue): format-neutral DecodeError Display now the enum is…

… shared by CBOR+JSON (Codex #6522)

DecodeError is shared by from_canonical_cbor + from_canonical_json, but its Display text was
CBOR-specific (authored in the CBOR-decode PR): a JSON caller logging Unsupported saw 'unsupported
CBOR (...tag, or simple value)', and UnexpectedEnd said 'truncated CBOR'. Made the messages
format-neutral (truncated input / extra input / unsupported-or-reserved-form-for-this-codec /
integer does not fit i64 / non-canonical input). Extended decode_error_display_is_human_readable to
assert no variant's Display names a single codec.

(The other 3 threads — surrogate-pair P1 + lone-surrogate test + surrogate-pair-NonCanonical test —
were already addressed in f6a3da232; Copilot reviewed the pre-fix commit.) 7 lib + 4 json-decode
tests pass; cargo fmt + clippy -D warnings clean.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```
