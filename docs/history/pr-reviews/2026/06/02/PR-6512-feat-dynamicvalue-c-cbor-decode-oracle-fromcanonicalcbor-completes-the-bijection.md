# PR #6512 -- feat(DynamicValue): C# CBOR DECODE oracle (FromCanonicalCbor) — completes the bijection

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 6512 |
| Title | feat(DynamicValue): C# CBOR DECODE oracle (FromCanonicalCbor) — completes the bijection |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-06-02T00:54:51Z |
| Merged at | 2026-06-02T01:05:56Z |
| Merge commit SHA | `4d0dc4c65fcb7f62eecd7beb9115528c284f7412` |
| Branch | `otto-cli/dynamicvalue-decode-cbor-csharp-2026-06-01` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/6512 |
| Changed files | 3 |
| Additions / deletions | +471 / -0 |

## Description

Starts **the decode side** (per Aaron: both, **CBOR then JSON**). `DynamicValues.FromCanonicalCbor(byte[]) -> Result<DynamicValue, DecodeError>` is the inverse of `ToCanonicalCbor`, completing the byte↔value bijection for all 8 shapes.

- **Result-shaped** (decode is partial): new `DecodeError` enum — `UnexpectedEnd / TrailingData / Unsupported / IntegerOverflow / NonTextKey`. Never throws for malformed input.
- **float16** payloads decode via `System.Half`.
- **Byte-lock = round-trip**: `ToCanonicalCbor(FromCanonicalCbor(b)) == b` over the shared seed — sound (canonical encode is bijective) and it sidesteps NaN/-0.0 value-equality traps (NaN re-encodes to `f97e00`). Plus direct structural `decode == value` (C# `DynamicValue` has structural equality) + 7 malformed-input rejections + non-text-key. **9 tests green.**

First decode oracle (one authors, others replay): **C# here; F#/Rust/TS CBOR decode next, then JSON decode ×4.**

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | true |
| Total threads | 9 |
| Resolved threads | 9 |
| Unresolved threads | 0 |
| Total review comments | 18 |
| Total fix commits (touching thread paths) | 3 |

## Review threads

### Thread 1 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GSOcr`
- **Path**: `src/Core.CSharp.DynamicValue/DynamicValues.cs`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-06-02T00:56:52Z on `src/Core.CSharp.DynamicValue/DynamicValues.cs`:707 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Reject invalid UTF-8 in text strings**

  For malformed CBOR text strings containing invalid UTF-8, this uses the default replacement fallback, so an input like `61 ff` returns `Ok(String("�"))` instead of an error and then re-encodes to different bytes. Because CBOR text strings are required to contain UTF-8 and this API promises a canonical byte↔value lock rather than silent repair, the decoder should use a strict UTF-8 decoder and return a `DecodeError` for invalid payloads.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-06-02T00:58:54Z on `src/Core.CSharp.DynamicValue/DynamicValues.cs`:707 (association: MEMBER)

    <pre>
    Fixed in 11fd3d5a4. Rather than swap in a strict UTF-8 decoder, the decoder now enforces canonicality uniformly: canonical bytes are exactly the fixed points of encode-after-decode, so FromCanonicalCbor asserts ToCanonicalCbor(decoded) == input and returns DecodeError.NonCanonical otherwise. 61 ff lenient-repairs to U+FFFD which re-encodes to 61 ef bf bd != 61 ff, so it is rejected. One check also covers non-shortest widths/floats. Test added.
    </pre>

### Thread 2 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GSOcu`
- **Path**: `src/Core.CSharp.DynamicValue/DynamicValues.cs`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-06-02T00:56:52Z on `src/Core.CSharp.DynamicValue/DynamicValues.cs`:789 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Reject non-preferred CBOR argument widths**

  When the input uses a non-canonical definite-length/integer encoding, this reader accepts the wider additional-info form without checking that the value required that width. For example, `18 00` decodes as `Ok(Int(0))`, but `ToCanonicalCbor` emits `00`, so accepted input bytes no longer round-trip and `FromCanonicalCbor` is not actually limited to the canonical encoding it advertises; reject widths that are not the preferred shortest form.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-06-02T00:58:55Z on `src/Core.CSharp.DynamicValue/DynamicValues.cs`:789 (association: MEMBER)

    <pre>
    Fixed in 11fd3d5a4 by the same fixed-point check: 18 00 decodes to Int(0) which re-encodes to 00 != 18 00, so it is rejected as DecodeError.NonCanonical. This generalizes beyond integers to non-shortest length widths and non-shortest floats / non-canonical NaN (e.g. 1.0 as fb...), all caught by ToCanonicalCbor(decoded) == input. Tests added (18 00, non-shortest float, nested).
    </pre>

### Thread 3 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GSP-S`
- **Path**: `src/Core.CSharp.DynamicValue/DynamicValues.cs`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-02T00:59:55Z on `src/Core.CSharp.DynamicValue/DynamicValues.cs`:698 (association: NONE)

  <pre>
  P1: `TryReadTextString` uses `Encoding.UTF8.GetString`, which accepts invalid UTF-8 by replacement. That means non-canonical CBOR text strings with malformed UTF-8 can decode successfully but then fail the byte-lock on re-encode (and can silently change data). Since this API is `FromCanonicalCbor`, it should reject invalid UTF-8 sequences instead of normalizing them.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-06-02T01:03:13Z on `src/Core.CSharp.DynamicValue/DynamicValues.cs`:698 (association: MEMBER)

    <pre>
    Already enforced — by a centralized fixed-point check rather than per-form strictness. FromCanonicalCbor asserts ToCanonicalCbor(decoded) == input and returns DecodeError.NonCanonical otherwise (commit 11fd3d5a4). The per-form readers stay lenient by design; the single wrapper check rejects every non-canonical form. 61 ff lenient-repairs to U+FFFD which re-encodes to 61 ef bf bd != 61 ff -&gt; NonCanonical (pinned by a test).
    </pre>

### Thread 4 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GSP-Y`
- **Path**: `src/Core.CSharp.DynamicValue/DynamicValues.cs`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-02T00:59:55Z on `src/Core.CSharp.DynamicValue/DynamicValues.cs`:798 (association: NONE)

  <pre>
  P1: `TryReadCborArg` currently accepts non-preferred (non-shortest) argument encodings (e.g., encoding 23 as `0x18 0x17`, or a 1-byte string length using `ai=24`). Since this is a *canonical* CBOR decoder and the seed/spec require preferred/shortest ints+lengths (§4.2.1), these should be rejected; otherwise `FromCanonicalCbor` accepts non-canonical bytes and `ToCanonicalCbor(FromCanonicalCbor(b))` will not equal `b` for such inputs.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-06-02T01:03:15Z on `src/Core.CSharp.DynamicValue/DynamicValues.cs`:798 (association: MEMBER)

    <pre>
    Already enforced — by a centralized fixed-point check rather than per-form strictness. FromCanonicalCbor asserts ToCanonicalCbor(decoded) == input and returns DecodeError.NonCanonical otherwise (commit 11fd3d5a4). The per-form readers stay lenient by design; the single wrapper check rejects every non-canonical form. 18 17 (23 as uint8) decodes to Int(23) -&gt; re-encodes 17 != 18 17 -&gt; NonCanonical (pinned by tests 18 17 and 18 00).
    </pre>

### Thread 5 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GSP-e`
- **Path**: `src/Core.CSharp.DynamicValue/DynamicValues.cs`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-02T00:59:55Z on `src/Core.CSharp.DynamicValue/DynamicValues.cs`:635 (association: NONE)

  <pre>
  P1: For `ai=25` (float16), the decoder currently accepts any NaN payload. However `ToCanonicalCbor` canonicalizes *all* NaNs to `f97e00`, so accepting other NaN payloads here means `FromCanonicalCbor` will accept non-canonical bytes and break the advertised byte-lock on re-encode. Consider rejecting float16 NaNs whose payload isn't the canonical `0x7e00`.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-06-02T01:03:17Z on `src/Core.CSharp.DynamicValue/DynamicValues.cs`:635 (association: MEMBER)

    <pre>
    Already enforced — by a centralized fixed-point check rather than per-form strictness. FromCanonicalCbor asserts ToCanonicalCbor(decoded) == input and returns DecodeError.NonCanonical otherwise (commit 11fd3d5a4). The per-form readers stay lenient by design; the single wrapper check rejects every non-canonical form. f9 7e01 (non-canonical float16 NaN) decodes to Float(NaN) -&gt; re-encodes f9 7e00 != input -&gt; NonCanonical (pinned by a test).
    </pre>

### Thread 6 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GSP-m`
- **Path**: `src/Core.CSharp.DynamicValue/DynamicValues.cs`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-02T00:59:55Z on `src/Core.CSharp.DynamicValue/DynamicValues.cs`:647 (association: NONE)

  <pre>
  P1: For `ai=26` (float32), `FromCanonicalCbor` should enforce the RFC 8949 shortest-float rule advertised by `ToCanonicalCbor`/the seed. As written, it will accept (a) float32 NaNs (canonicalizes to `f97e00` on re-encode) and (b) float32 values that round-trip through float16 (which should have been encoded as `ai=25`).
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-06-02T01:03:19Z on `src/Core.CSharp.DynamicValue/DynamicValues.cs`:647 (association: MEMBER)

    <pre>
    Already enforced — by a centralized fixed-point check rather than per-form strictness. FromCanonicalCbor asserts ToCanonicalCbor(decoded) == input and returns DecodeError.NonCanonical otherwise (commit 11fd3d5a4). The per-form readers stay lenient by design; the single wrapper check rejects every non-canonical form. Both cases caught: float32 NaN (fa 7fc00000) re-encodes f9 7e00; 1.0 as float32 (fa 3f800000) re-encodes f9 3c00 -&gt; both != input -&gt; NonCanonical (both pinned by tests).
    </pre>

### Thread 7 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GSP-s`
- **Path**: `src/Core.CSharp.DynamicValue/DynamicValues.cs`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-02T00:59:56Z on `src/Core.CSharp.DynamicValue/DynamicValues.cs`:664 (association: NONE)

  <pre>
  P1: For `ai=27` (float64), the decoder currently accepts values that could have been encoded as float32/float16 under the shortest-float rule (and also accepts float64 NaNs). Since `ToCanonicalCbor` uses shortest-float (and canonicalizes NaN to `f97e00`), `FromCanonicalCbor` should reject float64 encodings when a narrower canonical encoding exists; otherwise the byte-lock and the "canonical" contract diverge.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-06-02T01:03:21Z on `src/Core.CSharp.DynamicValue/DynamicValues.cs`:664 (association: MEMBER)

    <pre>
    Already enforced — by a centralized fixed-point check rather than per-form strictness. FromCanonicalCbor asserts ToCanonicalCbor(decoded) == input and returns DecodeError.NonCanonical otherwise (commit 11fd3d5a4). The per-form readers stay lenient by design; the single wrapper check rejects every non-canonical form. float64 that fits narrower (1.0 as fb 3ff0...) re-encodes f9 3c00, and float64 NaN re-encodes f9 7e00 -&gt; both != input -&gt; NonCanonical (1.0-as-f64 pinned by a test).
    </pre>

### Thread 8 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GSP-u`
- **Path**: `tests/Tests.CSharp/DynamicValueCborDecodeTests.cs`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-02T00:59:56Z on `tests/Tests.CSharp/DynamicValueCborDecodeTests.cs`:129 (association: NONE)

  <pre>
  P1: The malformed-input theory covers truncation/indefinite/tag cases, but it doesn't currently pin the "canonical" constraints that `FromCanonicalCbor` is supposed to enforce (preferred/shortest args, shortest-float, canonical NaN payload, and valid UTF-8 for text strings). Adding a few non-canonical vectors here would prevent regressions once the decoder is tightened.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-06-02T01:03:22Z on `tests/Tests.CSharp/DynamicValueCborDecodeTests.cs`:129 (association: MEMBER)

    <pre>
    Done in 55473cc5c: added non-canonical vectors pinning preferred/shortest arg width (18 17 / 18 00), shortest-float (1.0 as fa.. and fb..), canonical NaN payload (f9 7e01, fa 7fc00000), and invalid UTF-8 (61 ff) -- all asserted to return DecodeError.NonCanonical. 17 tests green.
    </pre>

### Thread 9 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GSP-1`
- **Path**: `src/Core.CSharp.DynamicValue/DecodeError.cs`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-02T00:59:56Z on `src/Core.CSharp.DynamicValue/DecodeError.cs`:18 (association: NONE)

  <pre>
  P1: With `FromCanonicalCbor` enforcing canonical rules (preferred/shortest arg encodings, shortest-float, canonical NaN payload, and rejecting invalid UTF-8), `DecodeError.Unsupported` will cover more cases than this doc comment lists. Updating the summary avoids misleading callers about what "Unsupported" includes.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-06-02T01:03:24Z on `src/Core.CSharp.DynamicValue/DecodeError.cs`:18 (association: MEMBER)

    <pre>
    The canonical-rule violations are surfaced as the dedicated DecodeError.NonCanonical variant (documented), not folded into Unsupported -- so the Unsupported doc stays accurate (reserved 28-30 / indefinite-length 31 / tags / unsupported simple value). NonCanonical was added with its own doc in the same commit.
    </pre>

## Fix commits (touching thread paths)

### `f6a45b226099c16fce6817111a17493fdad0dffc` -- 2026-06-02T00:54:48Z -- ``

**Touched files (intersect with thread paths):**

- `src/Core.CSharp.DynamicValue/DecodeError.cs`
- `src/Core.CSharp.DynamicValue/DynamicValues.cs`
- `tests/Tests.CSharp/DynamicValueCborDecodeTests.cs`

**Message:**

```
feat(DynamicValue): C# CBOR DECODE oracle (FromCanonicalCbor) — compl…

…etes the bijection

The decode side, starting CBOR (Aaron 2026-06-01: "both, CBOR then JSON").
DynamicValues.FromCanonicalCbor(byte[]) -> Result<DynamicValue, DecodeError> is the
inverse of ToCanonicalCbor, for all 8 shapes. Decode is partial (truncation,
reserved/indefinite forms, CBOR tags, oversized ints, non-text map keys), so it
returns Result per the Result-over-exception hard rule (AGENTS.md), never throws.

New DecodeError enum (UnexpectedEnd / TrailingData / Unsupported / IntegerOverflow /
NonTextKey). float16 payloads decode via System.Half.

Byte-lock test (9 green): the round-trip ToCanonicalCbor(FromCanonicalCbor(b)) == b
over the shared seed -- sound because canonical encode is bijective, and it sidesteps
NaN/-0.0 value-equality traps. Plus direct structural decode == value (C# DynamicValue
has structural equality) + 7 malformed-input rejections + non-text-key.

First decode oracle (one authors, others replay): C# here; F#/Rust/TS CBOR decode
next, then JSON decode x4.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `11fd3d5a4d8ae5728ba86c891b6914e634458401` -- 2026-06-02T00:58:44Z -- ``

**Touched files (intersect with thread paths):**

- `src/Core.CSharp.DynamicValue/DecodeError.cs`
- `src/Core.CSharp.DynamicValue/DynamicValues.cs`
- `tests/Tests.CSharp/DynamicValueCborDecodeTests.cs`

**Message:**

```
fix(DynamicValue): strict canonical CBOR decode — reject non-canonica…

…l input (Codex #6512)

Resolves two Codex P2 threads (invalid UTF-8 silently repaired; non-preferred
argument widths accepted) with one uniform check rather than scattered strictness:
canonical bytes are exactly the fixed points of encode-after-decode, so after a
successful decode FromCanonicalCbor now asserts ToCanonicalCbor(decoded) == input and
returns the new DecodeError.NonCanonical otherwise.

One check catches every non-canonical form: non-shortest int/length widths (18 00 vs
00), non-shortest floats / non-canonical NaN (1.0 as fb... ), AND invalid UTF-8 (61 ff
-> lenient U+FFFD repair re-encodes to ef bf bd != input). Stays trivially consistent
across the four oracles (each just re-encodes + compares).

13 tests green (+4 NonCanonical: non-shortest int, invalid UTF-8, non-shortest float,
nested non-canonical).

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `55473cc5c02815f2000490f0e943d0fc52867ead` -- 2026-06-02T01:03:01Z -- ``

**Touched files (intersect with thread paths):**

- `tests/Tests.CSharp/DynamicValueCborDecodeTests.cs`

**Message:**

```
test(DynamicValue): pin canonical-CBOR decode constraints (Copilot #6…

…512)

Adds non-canonical decode vectors that the centralized fixed-point check
(ToCanonicalCbor(decoded) == input -> NonCanonical) already rejects, pinning the
constraints Copilot listed: non-shortest arg width (18 17), non-canonical float16 NaN
payload (f9 7e01), float32 NaN (fa 7fc00000), and float32-that-fits-float16
(fa 3f800000). 17 tests green. The per-form readers stay lenient by design; the single
FromCanonicalCbor fixed-point check is the canonical-enforcement point.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```
