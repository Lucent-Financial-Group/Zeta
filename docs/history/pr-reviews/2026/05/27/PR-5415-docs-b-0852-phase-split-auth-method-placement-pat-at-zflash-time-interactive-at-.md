# PR #5415 -- docs(081KSKBP80008QG0R003AX2A69): phase-split auth-method placement — PAT at zflash time + interactive at setup time (Aaron 2026-05-27)

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 5415 |
| Title | docs(081KSKBP80008QG0R003AX2A69): phase-split auth-method placement — PAT at zflash time + interactive at setup time (Aaron 2026-05-27) |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-05-27T07:07:54Z |
| Merged at | 2026-05-27T07:26:02Z |
| Merge commit SHA | `b66b2377882789ce11a9f9c26174767746572ec0` |
| Branch | `docs/b-0852-zflash-vs-boot-phase-split-auth-method-aaron-2026-05-27` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/5415 |
| Changed files | 1 |
| Additions / deletions | +145 / -0 |

## Description

## Summary

081KSKBP80008QG0R003AX2A69 row body refinement per operator: *"i think if we do token we should do at zflash time and human interactive at setup time what do you think?"*

Match each auth method to the operator-UX phase that fits it best.

## Phase split

| Phase | Where | Auth methods |
|---|---|---|
| zflash time | Operator Mac | (optional) PAT inject + passphrase-encrypt + ESP write |
| Boot time | Target console | (1) restore-blob default / (2) device-flow / (3) PAT-paste rare / (4) skip |

## Why better than picker-only-at-install-time

- PAT UX: paste at Mac with clipboard (vs typing long PAT at target console)
- Composes with iter-4.2 ESP-write channel (zflash already writes SSH pubkey at flash time)
- First-boot of target reads blob → zero operator interaction at boot
- Re-boot USB = restore blob; no re-auth

## Edge case explicitly named

"Same USB → multiple machines = same PAT" is a feature for fleet-USB workflows + footgun for per-machine isolation. Phase-split makes the trade-off explicit; operator picks model deliberately at flash time.

## What doesn't change

- 081KSKBP80008QG0R003AX2A69.1 crypto module (PR #5411 landed): same `encrypt(plaintext, usbUuid, passphrase)` primitive
- 081KSKBP80008QG0R003AX2A69.5 cred-manifest schema (PR #5414 in flight): same declarative entries; zflash-time write populates `gh-cli`; boot-time can populate the rest if device-flow chosen

Refinement recorded NOW per Aaron 2026-05-27 separation-of-concerns discipline ("recording row exists is critical for deferring work to reliably happen"); implementation defers to next ISO batch.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 1 |
| Resolved threads | 1 |
| Unresolved threads | 0 |
| Total review comments | 1 |
| Total fix commits (touching thread paths) | 4 |

## Review threads

### Thread 1 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6FBaWh`
- **Path**: `docs/backlog/P1/081KSKBP80008QG0R003AX2A69-credential-persistence-on-usb-esp-plus-boot-sequence-auth-method-picker-encrypted-blob-bound-to-usb-uuid-plus-operator-passphrase-aaron-2026-05-27.md`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-27T07:10:07Z on `docs/backlog/P1/081KSKBP80008QG0R003AX2A69-credential-persistence-on-usb-esp-plus-boot-sequence-auth-method-picker-encrypted-blob-bound-to-usb-uuid-plus-operator-passphrase-aaron-2026-05-27.md`:263 (association: NONE)

  <pre>
  P1: The command path here is incorrect: there is no `tools/zflash.ts` in the repo; the zflash wrapper lives at `full-ai-cluster/tools/zflash.ts` (or is typically run via the `zflash` alias from zflash-setup). As written, this instruction will fail for operators following the backlog row verbatim.

  </pre>

## Fix commits (touching thread paths)

### `dc6ab76d2c1fef9d7ab023eabe716166f43e362c` -- 2026-05-27T07:07:41Z -- ``

**Touched files (intersect with thread paths):**

- `docs/backlog/P1/081KSKBP80008QG0R003AX2A69-credential-persistence-on-usb-esp-plus-boot-sequence-auth-method-picker-encrypted-blob-bound-to-usb-uuid-plus-operator-passphrase-aaron-2026-05-27.md`

**Message:**

```
docs(081KSKBP80008QG0R003AX2A69): phase-split auth-method placement — PAT at zflash time …

…+ interactive at setup time (Aaron 2026-05-27)

Operator refinement: "i think if we do token we should do at zflash time
and human interactive at setup time what do you think?"

Match each auth method to the operator-UX phase that fits it best:

- zflash time (operator's Mac; full UI + clipboard + browser):
  - Optional PAT inject + passphrase-encrypt + ESP write
  - Composes with iter-4.2 ESP-write channel + zflash --agent flow
  - First-boot of target reads blob → no operator interaction at boot

- Boot time (target machine; console; operator phone):
  - Restore from blob (DEFAULT when blob present)
  - Device-flow login (operator at github.com/login/device on phone)
  - Operator-provided PAT at console (RARE fallback)
  - Skip (degraded mode)

Edge case named: "same USB → multiple machines = same PAT" is a feature
for fleet-USB workflows + footgun for per-machine-isolation. Phase-split
makes the trade-off explicit at flash time; operator picks deliberately.

Doesn't change 081KSKBP80008QG0R003AX2A69.1 crypto primitive OR 081KSKBP80008QG0R003AX2A69.5 schema — only
changes WHERE encrypt fires (zflash time AND/OR install time AND/OR
post-install).

Composes with separation-of-concerns discipline per Aaron 2026-05-27
("recording row exists is critical for deferring work to reliably
happen") — refinement recorded NOW even though implementation defers
to next ISO batch.
```

### `0e6240922cb7bfa7be4bd5d4b60ac81263b7aaa5` -- 2026-05-27T07:09:03Z -- ``

**Touched files (intersect with thread paths):**

- `docs/backlog/P1/081KSKBP80008QG0R003AX2A69-credential-persistence-on-usb-esp-plus-boot-sequence-auth-method-picker-encrypted-blob-bound-to-usb-uuid-plus-operator-passphrase-aaron-2026-05-27.md`

**Message:**

```
docs(081KSKBP80008QG0R003AX2A69): per-cred operator choice at zflash time iterating over …

…declared manifest (Aaron 2026-05-27 extension)

Operator: "then zflash script and/or skill can make sure it asks what
declared creds you want to bake in vs go through device flow."

zflash --agent (or paired /zflash-creds skill) iterates over the
cred-manifest (081KSKBP80008QG0R003AX2A69.5) + prompts PER CREDENTIAL:
  (a) Bake in (paste value now; encrypted to ESP)
  (b) Device-flow at boot (defer to console + phone)
  (c) Skip (degrade)

Manifest is source-of-truth for what creds exist. zflash iterates;
no zflash code change required when manifest extends with new cred type.

Single passphrase typed once at flash time encrypts all bake-in
selections. Boot-time picker knows which creds are bake-in vs which
need device-flow per the blob contents.

New sub-row 081KSKBP80008QG0R003AX2A69.9 (zflash-time per-cred bake-in prompt loop)
composes with 081KSGS9H0008QG0R001EZKNCB (zflash --agent flag) + 081KSKBP80008QG0R003AX2A69.1 (crypto) +
081KSKBP80008QG0R003AX2A69.5 (manifest schema).

Alternative skill surface: /zflash-creds in .claude/skills/ wraps the
zflash flow with manifest-iteration prompt UX for Claude-Code agent
invocation.
```

### `6475d3b3a6ca4d686a2cd413716a9808e4b59052` -- 2026-05-27T07:13:31Z -- ``

**Touched files (intersect with thread paths):**

- `docs/backlog/P1/081KSKBP80008QG0R003AX2A69-credential-persistence-on-usb-esp-plus-boot-sequence-auth-method-picker-encrypted-blob-bound-to-usb-uuid-plus-operator-passphrase-aaron-2026-05-27.md`

**Message:**

```
docs(081KSKBP80008QG0R003AX2A69): CLI override > prompt loop — --bake-cred <id>=<value> w…

…ith per-cred type handlers (Aaron 2026-05-27 sharpening)

Operator sharpened: "maybe instead of loop in zflash you just allow
command line override of any declared cred as token... that way, might
need custom code per cred type for this idk. the would probably be
easier for the ai to call."

CLI shape replaces prompt loop:
  zflash --agent /dev/disk6 \
    --bake-cred gh-cli=ghp_xxx \
    --bake-cred claude=@~/.config/claude/credentials.json \
    --bake-cred ssh-operator-pubkey=@~/.ssh/operator.pub \
    --bake-passphrase-file ~/.config/zeta/zflash-passphrase

Value-source syntax (curl/git convention):
- <literal>     : direct string value
- @<path>       : read file contents
- env:<VAR>     : read from env var (avoid PAT in shell history)

Per-cred type handlers (one per manifest entry) define value shape +
validation + supported sources. Table in row body documents handler
contract per the 6 default manifest entries.

Trade-off table updated: AI-callable / scriptable / composable favor
CLI override; prompt loop deferred unless operator-UX test reveals
need.

Adds two sub-rows:
- 081KSKBP80008QG0R003AX2A69.9 zflash --bake-cred CLI override (extends 081KSGS9H0008QG0R001EZKNCB)
- 081KSKBP80008QG0R003AX2A69.10 per-cred type handlers module (pure functions; unit-tested
  independently of zflash)
```

### `e41593050cd7e19579208974d8dbe001fae29c7d` -- 2026-05-27T07:24:00Z -- ``

**Touched files (intersect with thread paths):**

- `docs/backlog/P1/081KSKBP80008QG0R003AX2A69-credential-persistence-on-usb-esp-plus-boot-sequence-auth-method-picker-encrypted-blob-bound-to-usb-uuid-plus-operator-passphrase-aaron-2026-05-27.md`

**Message:**

```
fix(081KSKBP80008QG0R003AX2A69): correct zflash path — full-ai-cluster/tools/zflash.ts (C…

…opilot P1 finding on PR #5415)

Copilot review caught: row body used `bun tools/zflash.ts` in 3 example
commands; actual path on disk is `bun full-ai-cluster/tools/zflash.ts`.
As written, operators following the row verbatim would hit
"script not found" errors.

sed replace across all 3 example invocations. The 4th reference
(in 081KSKBP80008QG0R003AX2A69.9 sub-row pointing at `full-ai-cluster/tools/zflash.ts`)
was already correct.

Resolves Copilot thread PRRT_kwDOSF9kNM6FBaWh on line 263.
```
