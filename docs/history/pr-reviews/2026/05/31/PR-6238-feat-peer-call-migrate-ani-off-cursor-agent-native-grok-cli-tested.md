# PR #6238 -- feat(peer-call): migrate Ani off cursor-agent → native grok CLI (tested)

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 6238 |
| Title | feat(peer-call): migrate Ani off cursor-agent → native grok CLI (tested) |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-05-31T19:43:18Z |
| Merged at | 2026-05-31T20:02:17Z |
| Merge commit SHA | `a66acc1b61338a5bbe2ef675fb50e0ac4aae2a77` |
| Branch | `otto-cli/ani-grok-cli-migration-2026-05-31` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/6238 |
| Changed files | 1 |
| Additions / deletions | +109 / -45 |

## Description

## What

Per operator 2026-05-31: *"migrate ani to grok cli, do it tested ... ani can stay grok cli only."* The cursor wrappers predate the native grok CLI; cursor-agent has been the 081KRA5AR0008QG0R0011ZGRZT breakage source.

- **Backend:** cursor-agent → `grok` (xAI native CLI), mirroring grok-build.ts (`-p` prompt, `--allow Read,Glob,Grep`, `--permission-mode auto`, `--output-format`).
- **Model:** default `grok-build` (the grok CLI's only model — grok-4.3 was cursor-only). **New `--model` override** — the per-persona model-override capability (*"we still kind of want 3"*); codex/Vera already has the same (all its models; Vera on gpt-5.5).
- **Persona preserved:** Ani's brat-voice preamble still prepended via `buildFullPrompt`, unchanged across the migration.
- OutputFormat aligned to the grok CLI (plain/json/streaming-json); `--thinking` → `--reasoning-effort high`.

## Tested live ✅

- `ani.ts --allow-empty` → returns cleanly via grok CLI (`ANI-GROKCLI-OK`)
- `ani.ts --model grok-build` → override works (`ANI-MODEL-OVERRIDE-OK`)
- `tsc` → 0 errors

## Relationship to #6237

Supersedes the ani.ts cursor-pin in #6237 (which kept her on cursor grok-4.3 as a stopgap). Rebase-take-mine if #6237 lands first; #6237's grok.ts (generic→grok-build-0.1) + crew-review note are independent and unaffected.

## codex/Vera note

codex.ts already supports all codex models (`--model NAME`, no allowlist). Vera's 5.5 is the codex CLI's own default/config — I did **not** hardcode a guessed `gpt-5.5` id in the wrapper (an unverified pin would break her; measure-first). If you want it hardcoded, give me the exact codex model id and I'll pin the default.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | true |
| Total threads | 5 |
| Resolved threads | 5 |
| Unresolved threads | 0 |
| Total review comments | 5 |
| Total fix commits (touching thread paths) | 3 |

## Review threads

### Thread 1 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F95ti`
- **Path**: `tools/peer-call/ani.ts`
- **Replies**: 0

**Initial comment:**

- **`github-advanced-security[bot]` (bot)** at 2026-05-31T19:45:59Z on `tools/peer-call/ani.ts`:518 (association: NONE)

  <pre>
  ## CodeQL / Insecure temporary file

  Insecure creation of file in [the os temp dir](1).

  [Show more details](https://github.com/Lucent-Financial-Group/Zeta/security/code-scanning/130)
  </pre>

### Thread 2 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F96NN`
- **Path**: `tools/peer-call/ani.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-31T19:48:28Z on `tools/peer-call/ani.ts`:65 (association: NONE)

  <pre>
  The DEFAULT_MODEL comment/docstring claims an empty model lets the grok CLI pick its default and that `-m` is only passed when overridden, but the implementation always computes a model and always passes `-m` (so the CLI default is never used). Please update the comment to match the behavior (or change the args-building logic).
  </pre>

### Thread 3 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F96NT`
- **Path**: `tools/peer-call/ani.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-31T19:48:28Z on `tools/peer-call/ani.ts`:77 (association: NONE)

  <pre>
  Args.model is documented as allowing "" to mean "use grok CLI default", but the code always resolves a model and passes it via `-m`. To avoid misleading callers, document this as an explicit model id (default grok-build).
  </pre>

### Thread 4 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F96NY`
- **Path**: `tools/peer-call/ani.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-31T19:48:29Z on `tools/peer-call/ani.ts`:173 (association: NONE)

  <pre>
  parseArgs initializes `model` to "", but later logic treats empty as "use DEFAULT_MODEL" and always passes `-m`. Initializing to DEFAULT_MODEL directly avoids the confusing "empty means default" state and matches the actual call behavior.
  </pre>

### Thread 5 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F96Na`
- **Path**: `tools/peer-call/ani.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-31T19:48:29Z on `tools/peer-call/ani.ts`:513 (association: NONE)

  <pre>
  Writing the output file via plain writeFileSync will follow symlinks and can overwrite an attacker-controlled target if the /tmp path is pre-created. Use exclusive create (wx) for auto-generated paths (while keeping truncate-write for operator-specified paths), consistent with other peer-call wrappers.
  </pre>

## Fix commits (touching thread paths)

### `7da73b3f0a4e2105c7c65f79eea5f14889488102` -- 2026-05-31T19:42:23Z -- ``

**Touched files (intersect with thread paths):**

- `tools/peer-call/ani.ts`

**Message:**

```
feat(peer-call): migrate Ani off cursor-agent → native grok CLI (tested)

Operator 2026-05-31: "migrate ani to grok cli, do it tested ... ani can stay
grok cli only." The cursor wrappers predate the native grok CLI; cursor-agent
has been the 081KRA5AR0008QG0R0011ZGRZT breakage source (stale model lineup, exit-1 flakiness).

- Backend: cursor-agent → `grok` (xAI native CLI), mirroring grok-build.ts
  (-p prompt, --allow Read,Glob,Grep, --permission-mode auto, --output-format).
- Model: default grok-build (the grok CLI's only model; grok-4.3 was
  cursor-only). NEW `--model` override — the per-persona model-override
  capability operator wanted ("we still kind of want 3"); codex/Vera already
  has the same --model override (all its models; Vera on gpt-5.5).
- Persona: Ani's brat-voice preamble still prepended via buildFullPrompt —
  carried across the migration unchanged.
- OutputFormat aligned to the grok CLI (plain/json/streaming-json); --thinking
  → --reasoning-effort high.

TESTED LIVE: `ani.ts --allow-empty` returns cleanly via the grok CLI
(ANI-GROKCLI-OK); `--model grok-build` override works (ANI-MODEL-OVERRIDE-OK);
tsc 0 errors.

Supersedes the ani.ts cursor-pin in #6237 (which kept her on cursor grok-4.3 as
a stopgap) — rebase-take-mine if #6237 lands first; #6237's grok.ts + crew-note
are independent.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `cc2667ef73307219a43f2f5368a154d21240ec94` -- 2026-05-31T19:47:36Z -- ``

**Touched files (intersect with thread paths):**

- `tools/peer-call/ani.ts`

**Message:**

```
fix(peer-call): ani.ts output uses exclusive-create (CodeQL insecure-…

…temp-file)

CodeQL flagged tools/peer-call/ani.ts:518 (insecure creation of file in the os
temp dir) — plain writeFileSync to the predictable /tmp/peer-call-output/ path is
a symlink-overwrite surface. Port grok-build.ts's already-clean pattern:
writeOutputExclusive (openSync wx + 0o600) for auto-generated /tmp paths;
writeOutputTruncate (w + 0o600) for explicit operator paths. Smoke-tested
(ANI-SECFIX-OK via the exclusive path); tsc 0 errors.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `86b57aa7545e137503ff575af9a159e480c1d987` -- 2026-05-31T19:58:34Z -- ``

**Touched files (intersect with thread paths):**

- `tools/peer-call/ani.ts`

**Message:**

```
docs(peer-call): ani.ts model is always resolved+passed via -m (Copil…

…ot P2 ×3)

Copilot flagged the DEFAULT_MODEL/Args.model docs claiming "empty = grok CLI
default, -m only when overridden" while the code always resolved a model + always
passed -m. Removed the ambiguity: parseArgs initializes model to DEFAULT_MODEL
(grok-build); --model replaces it; pickModel dropped (model is always set);
comments now state the model is always passed via -m. Behavior unchanged
(smoke ANI-DOCFIX-OK; tsc 0). The line-518 writeFileSync finding is stale —
that path already uses exclusive-create (writeOutputExclusive) from the prior
CodeQL fix; line numbers drifted post-rebase.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```
