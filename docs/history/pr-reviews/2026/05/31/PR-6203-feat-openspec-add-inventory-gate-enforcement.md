# PR #6203 -- feat(openspec): add inventory gate enforcement

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 6203 |
| Title | feat(openspec): add inventory gate enforcement |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-05-31T09:50:26Z |
| Merged at | 2026-05-31T10:04:45Z |
| Merge commit SHA | `dae77fe4362275c40d4adfe3e45c73b5ea1572ee` |
| Branch | `claim/codex-loop-b0171-openspec-inventory-gate-20260531` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/6203 |
| Changed files | 2 |
| Additions / deletions | +243 / -4 |

## Description

Summary:

- Add --enforce mode to tools/openspec/inventory.ts.
- Fail the default gate on missing mapped Core modules or mapped artifacts.
- Add strict modifiers for unmapped specs and uncovered Core modules.

Proof:

- PATH=/opt/homebrew/bin:$PATH bun test tools/openspec/inventory.test.ts
- PATH=/opt/homebrew/bin:$PATH bun tools/openspec/inventory.ts --enforce
- PATH=/opt/homebrew/bin:$PATH bunx prettier --check tools/openspec/inventory.ts tools/openspec/inventory.test.ts
- git diff --check

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | true |
| Total threads | 2 |
| Resolved threads | 2 |
| Unresolved threads | 0 |
| Total review comments | 4 |
| Total fix commits (touching thread paths) | 3 |

## Review threads

### Thread 1 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F7hKB`
- **Path**: `docs/claims/codex-loop-b0171-openspec-inventory-gate-20260531.md`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-31T09:52:39Z on `docs/claims/codex-loop-b0171-openspec-inventory-gate-20260531.md`:11 (association: NONE)

  <pre>
  Claim files under docs/claims/ are meant to be live task locks and should not land on main. The claim protocol requires deleting the claim file in the same PR that lands the work (docs/AGENT-CLAIM-PROTOCOL.md §5; docs/claims/README.md). Additionally, the filename slug here ("codex-loop-b0171-openspec-inventory-gate-20260531") does not match the allowed slug prefixes (backlog-/bug-/issue-/task-) in the protocol, so even as a live claim it wouldn’t conform.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-31T10:01:25Z on `docs/claims/codex-loop-b0171-openspec-inventory-gate-20260531.md`:11 (association: MEMBER)

    <pre>
    Correct — released the claim file in 959c4b24 (`git rm`), per docs/claims/README.md ("delete the file in the same PR that lands the work") + AGENT-CLAIM-PROTOCOL.md §5. The non-conformant slug prefix is moot once the file is deleted.
    </pre>

### Thread 2 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F7hP1`
- **Path**: `tools/openspec/inventory.ts`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-31T09:53:03Z on `tools/openspec/inventory.ts`:445 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Make strict fail flags enforce the gate**

  When CI or a local check invokes `bun tools/openspec/inventory.ts --fail-on-unmapped-specs` without also adding `--enforce`, `parseCliArgs` records the strict option but this guard skips gate evaluation entirely, so the command still exits 0 even with the current unmapped specs (`agentic-organization`, `tick-history`). Since the option text says it treats those specs as gate failures, either these `--fail-on-*` flags should imply enforcement or the CLI should reject them unless `--enforce` is present.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-31T10:01:23Z on `tools/openspec/inventory.ts`:445 (association: MEMBER)

    <pre>
    Fixed in 959c4b24. Took the 'flags imply enforcement' option: each `--fail-on-*` flag now sets `enforce = true` at parse time, so the gate is evaluated whenever a strict flag is present. Verified `inventory.ts --fail-on-unmapped-specs` alone now exits 1 on the current unmapped specs (agentic-organization, tick-history); bare invocation still exits 0. Help text documents the implication; added regression tests (flag-alone ⇒ enforce:true, bare ⇒ enforce:false).
    </pre>

## Fix commits (touching thread paths)

### `3d31df2a2980a7bc839644fb49bb760f7c1c2f0b` -- 2026-05-31T09:44:24Z -- ``

**Touched files (intersect with thread paths):**

- `docs/claims/codex-loop-b0171-openspec-inventory-gate-20260531.md`

**Message:**

```
claim: codex-loop-b0171-openspec-inventory-gate-20260531 - openspec i…

…nventory gate

Co-Authored-By: Codex <noreply@openai.com>

Codex-Origin: codex-launchd-loop

Codex-Surface: codex-background-service

Codex-Loop-Run-Id: 20260531T094149Z
```

### `013af7d6a1078cae2a78d8ab3aa39459a7ed7580` -- 2026-05-31T09:50:08Z -- ``

**Touched files (intersect with thread paths):**

- `tools/openspec/inventory.ts`

**Message:**

```
feat(openspec): add inventory gate enforcement

Add an enforceable --enforce mode for the OpenSpec inventory tool. The default gate fails on mapping drift: missing mapped modules or mapped artifacts. Strict modifiers allow future CI jobs to also fail on unmapped specs or uncovered Core modules.

Proof:

- bun test tools/openspec/inventory.test.ts

- bun tools/openspec/inventory.ts --enforce

- bunx prettier --check tools/openspec/inventory.ts tools/openspec/inventory.test.ts

- git diff --check

Co-Authored-By: Codex <noreply@openai.com>
```

### `959c4b24184faaa41937e788a09d1251f4000f27` -- 2026-05-31T10:00:55Z -- `claude`

**Touched files (intersect with thread paths):**

- `docs/claims/codex-loop-b0171-openspec-inventory-gate-20260531.md`
- `tools/openspec/inventory.ts`

**Message:**

```
fix(openspec): make --fail-on-* flags enforce the gate + release clai…

…m file

Resolves two review threads on PR #6203:

- Codex P2 (tools/openspec/inventory.ts): `--fail-on-unmapped-specs` /
  `--fail-on-uncovered-modules` recorded the strict option but the gate
  only evaluated under `--enforce`, so the flags silently exited 0 even
  with unmapped specs. Each --fail-on-* flag now implies --enforce at
  parse time; help text documents the implication; added regression
  tests (flag-alone => enforce:true; bare invocation => enforce:false).

- Copilot (docs/claims/): released the live claim file in the same PR
  that lands the work, per docs/claims/README.md + AGENT-CLAIM-PROTOCOL.md
  §5. (Slug also did not match backlog-/bug-/issue-/task- prefixes;
  deletion moots that too.)

Verified: bun test 30/30 pass; `inventory.ts --fail-on-unmapped-specs`
alone now exits 1 on current unmapped specs; bare invocation stays exit 0.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```
