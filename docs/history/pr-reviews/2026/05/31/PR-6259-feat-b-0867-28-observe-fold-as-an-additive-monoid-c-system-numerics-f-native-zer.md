# PR #6259 -- feat(081KSXN940008QG0R0002287MP): observe-fold as an additive monoid (C# System.Numerics + F# native Zero/(+)) — additive-monoidal only

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 6259 |
| Title | feat(081KSXN940008QG0R0002287MP): observe-fold as an additive monoid (C# System.Numerics + F# native Zero/(+)) — additive-monoidal only |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-05-31T21:54:14Z |
| Merged at | 2026-05-31T22:19:45Z |
| Merge commit SHA | `8b3de2367034047ce0c5efbb46a6113ec9b6812d` |
| Branch | `feat/observe-fold-monoid-b0867-28-2026-05-31` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/6259 |
| Changed files | 7 |
| Additions / deletions | +381 / -10 |

## Description

## What

Target 2 of the numerics interface-gate (081KSXN940008QG0R0002287MP), C# slice. The observe event log is the **free monoid**; this exposes it via `System.Numerics` generic-math additive interfaces:

```csharp
EventLog : IAdditiveIdentity<EventLog, EventLog>, IAdditionOperators<EventLog, EventLog, EventLog>
```

- **identity** = empty log · **+** = associative append · **FoldOnto** = the monoid action / homomorphism onto `World`.
- **Load-bearing law (tested):** folding a concatenated log == incremental folding — `(a + b).FoldOnto(w0) == b.FoldOnto(a.FoldOnto(w0))` — i.e. append-only / DST-replay soundness.

## Decisions (operator 2026-05-31)

- **Additive-monoidal ONLY, not `INumber<T>`.** Reading the code surfaced the fork: TriFloat (and the log) is the free monoid, not a field-like number — tri-state, held-capable, shape-parameterized, partial encode. Full `INumber<T>` (×, ordering, negatives) would be a square peg. Implement only the additive-monoid interfaces — the exact structure present.
- **UoM: N/A here.** The observe `World` has no measurable numeric quantity (ids, flags, a mode) — `[<Measure>]` would be ceremony, not safety (load-bearing razor). UoM lives at the TriFloat/attention value domain (`src/Core/Units.fs`).

## Interface-gate result: NON-BREAKING

`EventLog` is a **new** additive type; `World` / `Algebra` / `NextAction` are **unchanged**. The gate's 'may change the interface significantly' worry resolves favorably — build-on-top (081KSXN940008QG0R000ZAQT3W) builds on the unchanged algebra + the new monoid layer.

## Tests

6 monoid-law tests (identity ×2, associativity, fold-empty, homomorphism ×2). **42/42 C# tests pass** incl. `GoldenVectorsTests` — cross-language BFT parity intact after the interface addition.

## Remaining (tracked in the row, not this PR)

- F# observe-fold monoid (F# lists already ARE the free monoid → decide if a generic-math wrapper earns its keep).
- Target 1: apply the additive-monoid decision to TriFloat itself (C#/F#).

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | true |
| Total threads | 3 |
| Resolved threads | 3 |
| Unresolved threads | 0 |
| Total review comments | 6 |
| Total fix commits (touching thread paths) | 4 |

## Review threads

### Thread 1 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F-XWw`
- **Path**: `src/Core.CSharp.Observe/EventLog.cs`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-31T21:56:22Z on `src/Core.CSharp.Observe/EventLog.cs`:37 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Preserve event-log immutability**

  When callers construct `EventLog` from a mutable `List&lt;NextAction&gt;`, the primary constructor stores that same list behind the `IReadOnlyList` interface, so later mutations by the caller change what `FoldOnto` replays and what future `+` operations append. That breaks the advertised append-only/free-monoid semantics in exactly the case this new type is meant to make algebraically stable; copy the input into an immutable/read-only backing store before exposing it.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-31T21:59:42Z on `src/Core.CSharp.Observe/EventLog.cs`:37 (association: MEMBER)

    <pre>
    Fixed: EventLog now has a get-only Events property + an explicit constructor that defensively copies the input (events.ToArray()) into a private read-only backing array, so a caller mutating a passed-in mutable list can't change what FoldOnto replays or what + appends. Test added (construct from mutable list, mutate after, assert unchanged). F# side is already safe — F# list is immutable. 43/43 C# pass.
    </pre>

### Thread 2 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F-YDS`
- **Path**: `src/Core.CSharp.Observe/EventLog.cs`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-31T21:59:56Z on `src/Core.CSharp.Observe/EventLog.cs`:64 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Make record equality obey event-log laws**

  When any caller compares logs through the public record equality (`==`, `Equals`, `Assert.Equal`, or hash-based collections), the monoid identity law fails: `EventLog.AdditiveIdentity + x` returns a fresh `List`, and the generated record equality compares the `Events` collection by reference, so it is not equal to `x` even though the XML docs promise `empty + x == x`. This makes the generic-math monoid surface easy to misuse unless equality is overridden structurally or the type stops advertising record value equality for a list-backed log.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-31T22:13:48Z on `src/Core.CSharp.Observe/EventLog.cs`:64 (association: MEMBER)

    <pre>
    Fixed: overrode Equals(EventLog?) to compare Events element-wise (SequenceEqual) + GetHashCode combined over the ordered sequence, so the monoid laws now hold through ==/Equals/hash-collections (AdditiveIdentity + x == x; associativity). C# records do reference-equality on list props, hence the override; F# observe-fold EventLog is structural by construction (now at parity). Test RecordEqualityObeysMonoidLawsThroughEqualsAndHash added; 44/44 pass.
    </pre>

### Thread 3 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F-bte`
- **Path**: `src/Core.CSharp.Observe/EventLog.cs`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-31T22:15:24Z on `src/Core.CSharp.Observe/EventLog.cs`:53 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Return a read-only wrapper for Events**

  When any caller observes `log.Events` and downcasts the returned object (`if (log.Events is NextAction[] a) ...`), this assignment exposes the mutable backing array, so the caller can rewrite the event sequence after construction and change `FoldOnto`, equality, and hash results. This is not the earlier source-list mutation issue; the fresh evidence is that the fixed constructor now assigns the copied array itself to the public `Events` property. Wrap the copy in a non-mutable collection (or otherwise keep the array private) before exposing it.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-31T22:17:00Z on `src/Core.CSharp.Observe/EventLog.cs`:53 (association: MEMBER)

    <pre>
    Fixed: the constructor now wraps the copy in Array.AsReadOnly(...) (a BCL ReadOnlyCollection over a private array), so Events is no longer a bare NextAction[] — a caller can't downcast (log.Events is NextAction[]) and rewrite the backing array. Test added: EventsCannotBeDowncastToAMutableArray. 45/45 pass.
    </pre>

## Fix commits (touching thread paths)

### `a4c44258bf1ac3accdf9458a91118b3b62cc0199` -- 2026-05-31T21:54:01Z -- ``

**Touched files (intersect with thread paths):**

- `src/Core.CSharp.Observe/EventLog.cs`

**Message:**

```
feat(081KSXN940008QG0R0002287MP): observe-fold as System.Numerics additive monoid (C#)…

… — additive-monoidal only

The numerics interface-gate, Target 2 (C#). The observe event log is the free
monoid; expose it via System.Numerics generic-math additive interfaces:

  EventLog : IAdditiveIdentity<EventLog,EventLog>, IAdditionOperators<EventLog,EventLog,EventLog>

identity = empty log, + = associative append, FoldOnto = the monoid action /
homomorphism onto World. The load-bearing law (tested): folding a concatenated
log == incremental folding — (a+b).FoldOnto(w0) == b.FoldOnto(a.FoldOnto(w0)) —
i.e. append-only / DST-replay soundness.

Additive-monoidal ONLY (operator 2026-05-31), not INumber<T>: the log (and
TriFloat) is the free monoid, not a field-like number — no multiplication,
ordering, or negatives to invent. Reading TriFloat surfaced it's tri-state /
held-capable / shape-parameterized with partial encode, so INumber<T> would be a
square peg.

UoM: N/A for the observe World (no measurable numeric quantity — only ids, flags,
a mode); [<Measure>] here would be ceremony not safety (load-bearing razor). UoM
lives at the TriFloat/attention value domain (src/Core/Units.fs).

Interface-gate result: NON-BREAKING. EventLog is a NEW additive type; World /
Algebra / NextAction unchanged. The gate's 'may change the interface' worry
resolves favorably — build-on-top builds on the unchanged algebra + new monoid.

6 monoid-law tests (identity, associativity, fold-empty, homomorphism×2);
42/42 C# tests pass incl. GoldenVectors parity (BFT agreement intact).
F# monoid + Target-1 TriFloat additive-monoid tracked as remaining in the row.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `b997116d034b90a37805beea7f5d0a42a88a7284` -- 2026-05-31T21:59:40Z -- ``

**Touched files (intersect with thread paths):**

- `src/Core.CSharp.Observe/EventLog.cs`

**Message:**

```
fix(observe): EventLog defensively copies its input (append-only stab…

…ility) (Codex P2)

C# record EventLog(IReadOnlyList<NextAction>) stored the caller's reference, so a
caller passing a mutable List could mutate it post-construction and change what
FoldOnto replays / what + appends — breaking the free-monoid stability the type
exists to guarantee. Now a get-only Events property + explicit ctor that copies
(events.ToArray()) into a private read-only backing array; mutation can't leak.
(F# is already safe — F# list is immutable by construction.) Test added:
construct from a mutable list, mutate it after, assert the log is unchanged.
43/43 C# tests pass; 0-warn.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `78a420d76fb00960391e51e3f5efa29093b63313` -- 2026-05-31T22:13:46Z -- ``

**Touched files (intersect with thread paths):**

- `src/Core.CSharp.Observe/EventLog.cs`

**Message:**

```
fix(observe): EventLog structural equality so monoid laws hold throug…

…h ==/Equals/hash (Codex P2)

C# records do REFERENCE equality on list-typed properties, so the generated
EventLog equality made AdditiveIdentity + x != x even though the docs promise the
monoid identity law — a footgun for a value type that advertises value equality.
Override Equals(EventLog?) = element-wise SequenceEqual over Events + GetHashCode
combined over the (ordered) event sequence. Now ==/Equals/hash-collections obey
the laws. (F# observe-fold EventLog is structural by construction — this brings C#
to parity.) Test added: RecordEqualityObeysMonoidLawsThroughEqualsAndHash. 44/44.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `4c94e5c33a1ba70fb9bec0d201ed231331a75964` -- 2026-05-31T22:16:43Z -- ``

**Touched files (intersect with thread paths):**

- `src/Core.CSharp.Observe/EventLog.cs`

**Message:**

```
fix(observe): wrap EventLog.Events in a read-only collection (no down…

…cast-to-array escape) (Codex P2)

Assigning events.ToArray() (a NextAction[]) to the IReadOnlyList property let a
caller downcast — (log.Events is NextAction[] arr) — and rewrite the backing
array, defeating the defensive copy + breaking FoldOnto/equality/hash. Wrap the
copy in Array.AsReadOnly(...) (BCL ReadOnlyCollection over a private array) so
Events is not a raw array and can't be downcast-and-mutated. Test added:
EventsCannotBeDowncastToAMutableArray.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```
