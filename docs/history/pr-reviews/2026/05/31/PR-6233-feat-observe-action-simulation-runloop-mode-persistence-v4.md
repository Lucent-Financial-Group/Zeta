# PR #6233 -- feat(observe): action simulation + runLoop + mode persistence (v4)

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 6233 |
| Title | feat(observe): action simulation + runLoop + mode persistence (v4) |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-05-31T19:15:46Z |
| Merged at | 2026-05-31T19:18:54Z |
| Merge commit SHA | `7162da93e0be31ced5aa0829457b67cfb78ffeda` |
| Branch | `otto-cli/observe-simulate-action-llm-loop-2026-05-31` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/6233 |
| Changed files | 2 |
| Additions / deletions | +355 / -38 |

## Description

## What

Wires **choices into action** so the controller loop runs end-to-end, per operator 2026-05-31: *"simulate action with our local llm in our tests too not just menus."*

- **`simulate(world, action) → World`** — pure state-transition. Work actions consume/transform the backlog (do_item removes; decompose dissolves an ambiguous item into two ready children; edit_grammar makes a needsNewAction item ready); operator actions clear the signal they addressed; free-mode actions set the persisted mode (backlog + operator untouched). DST: deterministic + replayable.
- **`runLoop(world, backend)`** — choose (`observeWithLlm`) → `simulate` → repeat to a fixed point. Deterministic simulation under a mock backend; the agent driving itself under a real local model.
- **Mode persistence** (operator: *"i love mode persistance"*) — a chosen FREE mode persists across ticks; work is OFFERED, not forced (a ready item does NOT pull the agent out of a free mode — only the operator outranks it; the agent switches by picking work from the menu). `work` mode is transient.
- **`buildMenu` refactor** — leads with `observe(world)` BY CONSTRUCTION, so the degrade-to-index-0 invariant is structural + persistence is handled cleanly.
- **Work-hours framing corrected** to the KPI overlay (operator *measure-first* principle: measure with KPIs before restricting choice), not a time-lock.

## Tests (+14 → 41 total, 0 fail, tsc clean)

- `simulate` transitions (each action's effect)
- mode persistence (free mode sticks; operator outranks; `work` doesn't stick; persisted mode leads the menu)
- **the loop as CI shield (mock):** a mixed backlog + operator signals drains to a steady `explore`; decompose path dissolves-then-drains; empty backlog reaches a fixed point
- **real local LLM (ollama) loop, guarded:** when ollama is reachable the real model drives a valid loop; skips when down (the mock loop is the deterministic coverage)

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 0 |
| Resolved threads | 0 |
| Unresolved threads | 0 |
| Total review comments | 0 |
| Total fix commits (touching thread paths) | 0 |

## Review threads

(no review threads on this PR)

## Fix commits (touching thread paths)

(no commits in this PR touched files anchored by review threads)
