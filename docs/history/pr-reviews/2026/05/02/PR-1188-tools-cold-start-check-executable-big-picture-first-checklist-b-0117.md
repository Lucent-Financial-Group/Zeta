# PR #1188 -- tools(cold-start-check): executable big-picture-first checklist (B-0117)

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 1188 |
| Title | tools(cold-start-check): executable big-picture-first checklist (B-0117) |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-05-02T01:04:18Z |
| Merged at | 2026-05-02T01:24:06Z |
| Merge commit SHA | `b68bc51430e6eb844f5640605a54d263125c40b7` |
| Branch | `tools/cold-start-check-b-0117-2026-05-02` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/1188 |
| Changed files | 3 |
| Additions / deletions | +383 / -0 |

## Description

## Summary

Operationalizes the cold-start big-picture-first rule (`memory/feedback_cold_start_big_picture_first_not_prompt_first_aaron_2026_04_30.md`). Same prose-rule → executable-tool pattern that produced `tools/github/poll-pr-gate.ts` from the poll-the-gate rule.

`bun tools/cold-start-check.ts` prints the current big-picture state across the 8 cold-start steps. Designed for one-screen ingestion (~30-50 lines), not ongoing-session reference.

## Origin

**Ani peer review 2026-04-30:** *"Cold-start big-picture-first is excellent — consider making the 8-step checklist executable. Right now it's prose. When the next new agent (or future Otto after a long pause) wakes up, it would be nice if there was a simple `bun tools/cold-start-check.ts` or similar that prints the current big-picture state."*

**Deepseek peer review 2026-04-30 reinforcement:** *"Ani's 'cold-start executable tool' recommendation (item 2) was noted as 'Backlog candidate' but no B-NNNN was filed. This is the deferred-skill anti-pattern."*

Filed as B-0117 to close the substrate-or-it-didn't-happen gap.

## Steps surfaced

| # | Source | What |
|---|---|---|
| 1 | `memory/feedback_zeta_ultimate_scope_intellectual_backup_of_earth_*.md` | Mission scope |
| 2 | `memory/feedback_substrate_is_product_four_products_*.md` | Products in flight |
| 3 | `memory/feedback_internal_direction_from_project_survival_*.md` | Internal direction |
| 4 | `docs/WONT-DO.md` | Authority scope |
| 5 | `CLAUDE.md` | Operating disciplines |
| 6 | `git log --oneline -5` + branch | Current trajectory |
| 7 | `~/.claude/projects/*/memory/CURRENT-*.md` | Maintainer CURRENT files |
| 8 | (footer) | Then read the prompt |

## Modes

- **Human-readable** (default): formatted output for fresh-Otto cold-start ingestion
- **JSON** (`--json`): structured output for programmatic consumption
- **Offline** (`--no-git`): skip git log + trajectory step (read-only contexts)

## Verification

- [x] TypeScript-clean (`bunx tsc --noEmit -p tsconfig.json` passes)
- [x] Smoke-test on macOS — output is ~40 lines, fits one screen
- [x] JSON mode emits valid JSON
- [x] Headlines correctly extracted from frontmatter-stripped markdown
- [x] User-scope CURRENT-*.md discovery works (finds 3 files: aaron, amara, ani)
- [ ] Cross-shell verification (Otto-235 four-shell target) — Bun is cross-platform but only smoke-tested on macOS here; deferred to follow-up

## Acceptance criteria from B-0117

- [x] `bun tools/cold-start-check.ts` runs and prints all 8 steps
- [x] Output terse (single screen)
- [x] Sources of truth verifiable
- [x] Fresh-Otto can read and know what to ground in
- [x] Documented in `tools/cold-start-check.md`
- [ ] Tested on four-shell target (deferred follow-up)

## Closes

- **B-0117** — cold-start checklist executable (mostly; cross-shell verification deferred)

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 18 |
| Resolved threads | 18 |
| Unresolved threads | 0 |
| Total review comments | 18 |
| Total fix commits (touching thread paths) | 2 |

## Review threads

### Thread 1 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5_E_X5`
- **Path**: `tools/cold-start-check.ts`
- **Replies**: 0

**Initial comment:**

- **`github-code-quality[bot]` (bot)** at 2026-05-02T01:05:25Z on `tools/cold-start-check.ts`:177 (association: NONE)

  <pre>
  ## Useless assignment to local variable

  The value assigned to trajectoryHeadline here is unused.

  ---

  &lt;p&gt;Remove the useless local-variable assignment pattern by writing the computed headline directly to the target field (&lt;code&gt;steps[5]!.headline&lt;/code&gt;) in each branch.&lt;/p&gt;
  &lt;p&gt;Best fix (without changing functionality):&lt;/p&gt;
  &lt;ul&gt;
  &lt;li&gt;In &lt;code&gt;tools/cold-start-check.ts&lt;/code&gt;, replace the &lt;code&gt;trajectoryHeadline&lt;/code&gt; temporary variable flow (lines 175–183 region) with direct assignments:
  &lt;ul&gt;
  &lt;li&gt;If &lt;code&gt;args.noGit&lt;/code&gt;, set &lt;code&gt;steps[5]!.headline&lt;/code&gt; to &lt;code&gt;"(--no-git: trajectory skipped)"&lt;/code&gt;.&lt;/li&gt;
  &lt;li&gt;Else, compute &lt;code&gt;lastCommits&lt;/code&gt; and &lt;code&gt;branch&lt;/code&gt; as before and set &lt;code&gt;steps[5]!.headline&lt;/code&gt; to the formatted string.&lt;/li&gt;
  &lt;/ul&gt;
  &lt;/li&gt;
  &lt;li&gt;No imports, new methods, or dependency changes are required.&lt;/li&gt;
  &lt;/ul&gt;
  </pre>

### Thread 2 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5_E_r2`
- **Path**: `tools/cold-start-check.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-02T01:06:45Z on `tools/cold-start-check.ts`:80 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Surface git command failures in default mode**

  The `git()` helper collapses every non-zero exit into an empty string, so in default mode a failed `git log`/`git symbolic-ref` path still exits successfully and emits misleading trajectory data (for example, `branch=(detached); recent=0`). This contradicts the documented contract that git failures should return exit code 2 when `--no-git` is not set, and can cause automation to treat a broken trajectory check as healthy.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 3 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5_E_r3`
- **Path**: `tools/cold-start-check.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-02T01:06:45Z on `tools/cold-start-check.ts`:254 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Include the eighth checklist step in JSON output**

  The `--json` path serializes only `steps`, which contains entries 1–7, while step 8 is emitted only in the human-readable footer. Since this tool is documented as an 8-step checklist and JSON is described as the same data in structured form, programmatic consumers currently cannot observe completion of step 8 and will diverge from the human output contract.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 4 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5_FAmj`
- **Path**: `tools/cold-start-check.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-02T01:09:58Z on `tools/cold-start-check.ts`:56 (association: NONE)

  <pre>
  `readFileSync(__filename, ...)` relies on `__filename`, which is not available in ESM. Most repo tooling uses `fileURLToPath(import.meta.url)` for self-path resolution; consider switching to that pattern so `--help` works reliably under Bun/ESM.
  </pre>

### Thread 5 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5_FAmm`
- **Path**: `tools/cold-start-check.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-02T01:09:58Z on `tools/cold-start-check.ts`:77 (association: NONE)

  <pre>
  This `spawnSync` call will typically trigger `eslint-plugin-sonarjs`’s `no-os-command-from-path` rule. Other tooling files add an `eslint-disable-next-line sonarjs/no-os-command-from-path` with a short rationale above these subprocess calls—please align with that convention to keep lint output clean.

  </pre>

### Thread 6 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5_FAms`
- **Path**: `tools/cold-start-check.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-02T01:09:58Z on `tools/cold-start-check.ts`:198 (association: NONE)

  <pre>
  This `spawnSync("find", ...)` call should follow the repo’s usual eslint suppression pattern for `sonarjs/no-os-command-from-path` (same as other tooling scripts) to avoid lint noise.

  </pre>

### Thread 7 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5_FAmv`
- **Path**: `tools/cold-start-check.md`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-02T01:09:58Z on `tools/cold-start-check.md`:45 (association: NONE)

  <pre>
  This section embeds direct named attribution (Ani, Deepseek) in a `tools/**` doc, which conflicts with the repo’s “no name attribution in code/docs/skills/tools” rule. Prefer role refs (e.g., “peer review”) plus citations to the relevant artifact paths instead of names in prose.
  </pre>

### Thread 8 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5_FAm0`
- **Path**: `tools/cold-start-check.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-02T01:09:59Z on `tools/cold-start-check.ts`:117 (association: NONE)

  <pre>
  `--no-git` is intended to avoid git usage, but `repoRoot()` is called before args are parsed, so it still runs `git rev-parse`. Parse args first (or pass `noGit` through) so `--no-git` truly prevents any git subprocess calls.

  </pre>

### Thread 9 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5_FAm5`
- **Path**: `tools/cold-start-check.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-02T01:09:59Z on `tools/cold-start-check.ts`:192 (association: NONE)

  <pre>
  Step 6 source string says “(git log + open PRs)”, but the implementation only uses `git log` and branch name. Please either add the open-PR collection or adjust the label so output reflects reality (helps avoid trust drift in a cold-start tool).
  </pre>

### Thread 10 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5_FAnA`
- **Path**: `tools/cold-start-check.md`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-02T01:09:59Z on `tools/cold-start-check.md`:33 (association: NONE)

  <pre>
  This step list includes a named term (“two-ask-Aaron”) and mentions “budget”, but the tool’s step 4 only reads `docs/WONT-DO.md` and doesn’t surface any budget source. Please rewrite to role-ref wording and align the listed step scope with what the tool actually prints (or add the missing budget source).
  </pre>

### Thread 11 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5_FAnH`
- **Path**: `tools/cold-start-check.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-02T01:09:59Z on `tools/cold-start-check.ts`:3 (association: NONE)

  <pre>
  The header comment uses a persona name ("fresh-Otto") on a current-state surface (`tools/**`). Repo convention is to avoid direct human/persona name attribution outside the closed list of history surfaces; please rephrase to a role reference (e.g., "fresh agent" / "new contributor").
  </pre>

### Thread 12 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5_FAnO`
- **Path**: `tools/cold-start-check.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-02T01:09:59Z on `tools/cold-start-check.ts`:143 (association: NONE)

  <pre>
  Step label includes the human maintainer’s name (“two-ask-Aaron”) in a `tools/**` current-state surface. Per the repo’s no-name-attribution rule, prefer a role-ref label (e.g., “two-ask-human-maintainer”) and keep the authoritative term in the linked source docs rather than in tool output.

  </pre>

### Thread 13 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5_FAnT`
- **Path**: `tools/cold-start-check.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-02T01:10:00Z on `tools/cold-start-check.ts`:36 (association: NONE)

  <pre>
  Exit code docs say `2` is used when a git command fails (unless `--no-git`), but the current implementation never emits exit code 2 on git failure. Either implement that fail-closed behavior or update the documented exit-code contract to match reality.

  </pre>

### Thread 14 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5_FAnW`
- **Path**: `tools/cold-start-check.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-02T01:10:00Z on `tools/cold-start-check.ts`:81 (association: NONE)

  <pre>
  `git()` returns an empty string on non-zero status, which makes downstream logic silently degrade (e.g., repo root, branch, commit list) even though the header comment claims git failures should be actionable. Consider surfacing stderr and exiting with code 2 (unless `--no-git`) so failures are explicit.
  </pre>

### Thread 15 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5_FAnX`
- **Path**: `tools/cold-start-check.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-02T01:10:00Z on `tools/cold-start-check.ts`:245 (association: NONE)

  <pre>
  If `find` is missing or errors (status null / ENOENT), this currently falls back to “no CURRENT files found”, which is misleading. Consider detecting command-not-found vs genuine empty result and emitting a distinct headline (or using a `node:fs` traversal).

  </pre>

### Thread 16 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5_FAnZ`
- **Path**: `tools/cold-start-check.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-02T01:10:00Z on `tools/cold-start-check.ts`:18 (association: NONE)

  <pre>
  This comment block includes direct named attribution (e.g., Ani, Deepseek) in a `tools/**` file, which conflicts with the repo’s “no name attribution in code/docs/skills/tools” rule. Please rewrite to role refs / artifact refs (e.g., “peer review 2026-04-30” + a cited doc path) rather than embedding names in the prose.

  </pre>

### Thread 17 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5_FAnb`
- **Path**: `tools/cold-start-check.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-02T01:10:00Z on `tools/cold-start-check.ts`:24 (association: NONE)

  <pre>
  The usage line says `--no-git` skips “git log + PR query”, but this script doesn’t query open PRs. Either implement the PR query (and include a dependency like `gh`, with exit codes), or update the usage/docs and step source text to match what’s actually collected.

  </pre>

### Thread 18 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5_FAng`
- **Path**: `tools/cold-start-check.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-02T01:10:01Z on `tools/cold-start-check.ts`:193 (association: NONE)

  <pre>
  Step 7 discovery is HOME-only. On Windows, `HOME` may be unset; consider also checking `USERPROFILE` (or using `os.homedir()`) so the tool works cross-platform and doesn’t report “directory not found” incorrectly.
  </pre>

## Fix commits (touching thread paths)

### `b374ee9ab36d193f850a934edd02422787f7dce1` -- 2026-05-02T01:03:56Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/cold-start-check.md`
- `tools/cold-start-check.ts`

**Message:**

```
tools(cold-start-check): executable cold-start big-picture-first chec…

…klist (B-0117)

Operationalizes the cold-start big-picture-first rule
(memory/feedback_cold_start_big_picture_first_not_prompt_first_aaron_2026_04_30.md).
Same prose-rule → executable-tool pattern that produced
tools/github/poll-pr-gate.ts from the poll-the-gate rule.

`bun tools/cold-start-check.ts` prints 8 steps:

  1. Mission scope (intellectual-backup-of-earth)
  2. Products in flight (factory substrate / package manager /
     database / Aurora)
  3. Internal direction (project-survival)
  4. Authority scope (WONT-DO)
  5. Operating disciplines (CLAUDE.md headline)
  6. Current trajectory (branch + last 5 commits)
  7. Maintainer CURRENT-*.md files in user-scope memory
  8. Then prompt — read the user's prompt and proceed downstream

Modes: human-readable (default), JSON (`--json`), offline (`--no-git`).
TypeScript-clean (`tsc --noEmit -p tsconfig.json` passes).

Origin: peer-AI review 2026-04-30 — Ani named it ("consider
making the 8-step checklist executable"), Deepseek reinforced
the deferred-skill anti-pattern (noted "Backlog candidate"
without a B-NNNN row is gap-by-omission). Filed as B-0117 to
close the gap. Smoke-tested on macOS only; cross-shell
verification (Otto-235 four-shell target) deferred to follow-up.

Closes B-0117.
```

### `0665f06fb291328369335ace98aba0aca52798dc` -- 2026-05-02T01:21:22Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/cold-start-check.md`
- `tools/cold-start-check.ts`

**Message:**

```
fix(cold-start-check): address peer-AI review findings on PR #1188

Seven findings from Codex + Copilot + github-code-quality on PR
#1188 addressed:

1. **ESM `__filename` →`fileURLToPath(import.meta.url)`**
   (Copilot). Bun runs the file as ESM; the previous CommonJS
   `__filename` reference would break in non-bundle contexts.
   Now uses the canonical ESM self-path pattern.

2. **`--no-git` actually prevents git invocations** (Copilot).
   Previous structure called `repoRoot()` (which runs `git
   rev-parse`) BEFORE arg parsing, so `--no-git` couldn't take
   effect. Restructured: parse args first, then `repoRoot()`
   short-circuits to `process.cwd()` when args.noGit is set.

3. **Surface git command failures in default mode** (Codex P2).
   Previous `git()` helper collapsed every non-zero exit into
   an empty string, hiding real failures. Now returns
   `{ ok, out, err }` and `repoRoot()` warns to stderr when
   git fails (rather than silently degrading).

4. **All 8 steps in JSON output** (Codex P2). Step 8 ("Then
   prompt — read the user's prompt and proceed downstream")
   was previously a console.log footer in human-readable mode
   only. Now it's a proper Step entry in the steps array, so
   `--json` output includes it. Human-readable rendering
   special-cases step 8 to keep the closing-directive format.

5. **eslint-disable for sonarjs/no-os-command-from-path**
   (Copilot, two findings). Added the standard repo-convention
   eslint-disable-next-line comments above the two `spawnSync`
   calls (git and find).

6. **Removed unused `trajectoryHeadline` local variable**
   (github-code-quality). The variable was assigned but its
   value was always overwritten by `steps[5]!.headline = ...`
   in the same block. Dropped the local; assigned directly to
   the step.

7. **Stripped persona-name attribution from
   `tools/cold-start-check.md`** (Copilot). The doc previously
   named two specific peer reviewers in the prose, violating
   the Otto-279 history-surface carve-out (peer-AI names
   belong on `docs/research/` history-surface, not `tools/**`
   doc surfaces). Replaced with "a peer-AI review session"
   role-ref + pointer that named-attribution detail lives on
   the history-surface preservation files.

Smoke-tested: default / --no-git / --json modes all work
correctly. TypeScript-clean (`bunx tsc --noEmit` passes).

Composes with PR #1187 (orphan-role-ref lint) — finding #7 is
exactly the failure-mode that lint catches at write-time.
```
