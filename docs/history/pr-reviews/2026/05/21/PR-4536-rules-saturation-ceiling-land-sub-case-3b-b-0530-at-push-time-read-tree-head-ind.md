# PR #4536 -- rules(saturation-ceiling): land Sub-case 3b (081KRMEXM0008QG0R000X1PPGC at push-time) + read-tree HEAD index recovery

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 4536 |
| Title | rules(saturation-ceiling): land Sub-case 3b (081KRMEXM0008QG0R000X1PPGC at push-time) + read-tree HEAD index recovery |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-05-21T13:31:36Z |
| Merged at | 2026-05-21T13:41:22Z |
| Merge commit SHA | `4a52e377618933b8603e52bee1ea3babc1e0935d` |
| Branch | `otto/rule-edit-saturation-ceiling-subcase-3b-1327z` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/4536 |
| Changed files | 1 |
| Additions / deletions | +107 / -3 |

## Description

## Summary

Folds the two empirical refinements captured in memo PR [#4535](https://github.com/Lucent-Financial-Group/Zeta/pull/4535) directly into the saturation-ceiling rule at [.claude/rules/claim-acquire-before-worktree-work.md](.claude/rules/claim-acquire-before-worktree-work.md), so future Otto cold-boots inherit them via auto-load (rules auto-load at session start) instead of via memory-file pointer.

## Two edits

### 1. New Sub-case 3b — pack-dir contention at `git push` time

Same 081KRMEXM0008QG0R000X1PPGC root cause class as Sub-case 3 (worktree-add time), but the symptom appears at push time (`Interrupted system call` on `.git/objects/pack`). Distinct from **081KRW63S0008QG0R000EAZ9K2** (silent-push-failure with exit 0). Mitigation: REST git-data API bypass per [PR #4145](https://github.com/Lucent-Financial-Group/Zeta/pull/4145).

Empirical anchor: PR #4535 shipped via the bypass after `git push` timed out at exit 124.

### 2. In-place index recovery — `git read-tree HEAD`

Rebuilds a truncated index file (post stale-lock-removal under peer contention) without requiring worktree abandonment. Extends Sub-case 5 (peer-side destructive) recovery toolkit. Symptom: `index file smaller than expected`.

Empirical anchor: [PR #4532](https://github.com/Lucent-Financial-Group/Zeta/pull/4532) shipped after `read-tree HEAD` recovered an index truncated by stale-lock-removal race; previously the only recovery option was worktree abandonment.

## Minimal-additive shape

Both edits extend existing sub-case structure rather than reorganizing it. Section header still accurate ("4 failure sub-cases of borrow-on-existing") because 3b is a sibling-variant of 3, not a new numbered case.

## Authored via REST bypass

This rule edit was authored + pushed via the **081KRW63S0008QG0R000EAZ9K2 REST git-data API bypass** because `git push` was still hitting the very Sub-case 3b being documented (third successful use of the bypass this session).

## Test plan

- [x] Rule file still parseable as Markdown
- [x] Cross-refs to PRs #4145 / #4532 / #4535 / 081KRMEXM0008QG0R000X1PPGC / 081KRW63S0008QG0R000EAZ9K2 / sibling rule files all intact
- [x] Tree-size 54 on commit (matches main baseline; no broken-commit-canary trip)

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 5 |
| Resolved threads | 5 |
| Unresolved threads | 0 |
| Total review comments | 5 |
| Total fix commits (touching thread paths) | 3 |

## Review threads

### Thread 1 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6D0E99`
- **Path**: `.claude/rules/claim-acquire-before-worktree-work.md`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-21T13:33:50Z on `.claude/rules/claim-acquire-before-worktree-work.md`:299 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Drop the "status clean" postcondition after `read-tree`**

  The recovery sequence claims `git status` should be empty immediately after `git read-tree HEAD`, but `read-tree` rewrites the index and does **not** update working-tree files, so any real local edits remain and will still appear as modified. In the exact scenario this section targets (recovering a corrupted index while preserving intended edits), this postcondition is commonly false and can cause a successful recovery to be misdiagnosed as failed.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 2 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6D0G4v`
- **Path**: `.claude/rules/claim-acquire-before-worktree-work.md`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-21T13:35:25Z on `.claude/rules/claim-acquire-before-worktree-work.md`:238 (association: NONE)

  <pre>
  P1: This new “Sub-case 3b” makes the earlier statement that there are “FOUR distinct failure sub-cases” / “All 4 sub-cases” stale (the taxonomy now has 1, 2, 3, 3b, 4, ...). Please update the earlier count/wording so the document stays internally consistent.

  </pre>

### Thread 3 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6D0G5Z`
- **Path**: `.claude/rules/claim-acquire-before-worktree-work.md`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-21T13:35:26Z on `.claude/rules/claim-acquire-before-worktree-work.md`:262 (association: NONE)

  <pre>
  P1: `git push` itself doesn’t normally return exit 124; that’s typically from an external timeout wrapper. To avoid confusion during incident response, consider rephrasing to explicitly state that the push was timed out (e.g., via `timeout` or tool-call timeout) and therefore surfaced as exit 124.

  </pre>

### Thread 4 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6D0G52`
- **Path**: `.claude/rules/claim-acquire-before-worktree-work.md`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-21T13:35:26Z on `.claude/rules/claim-acquire-before-worktree-work.md`:266 (association: NONE)

  <pre>
  P1: This section describes “~5-6 REST calls”, but then frames the cost as “within Normal-tier GraphQL budget”. Since REST calls consume the REST/core budget (not GraphQL remaining), the tier reference is mismatched. Suggest referencing the REST/core rate-limit budget here, or more generally “API budget”, and (if relevant) note why this is still acceptable under the tier model in `refresh-world-model-poll-pr-gate.md`.

  </pre>

### Thread 5 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6D0G6M`
- **Path**: `.claude/rules/claim-acquire-before-worktree-work.md`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-21T13:35:26Z on `.claude/rules/claim-acquire-before-worktree-work.md`:302 (association: NONE)

  <pre>
  P1: The recovery steps say `git status` will be clean after `read-tree`, but the next step immediately talks about staging an “intended file”. If the working tree had local edits, `read-tree HEAD` resets the index to HEAD and `git status` will typically show those edits as *unstaged*, not clean. Consider adjusting the step text to reflect that `git status` should return sane/accurate output (and may show local modifications), rather than always clean.

  </pre>

## Fix commits (touching thread paths)

### `3c67d61980bb6eb76cf3f00faa27c5a878627188` -- 2026-05-21T13:31:17Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `.claude/rules/claim-acquire-before-worktree-work.md`

**Message:**

```
rules(saturation-ceiling): land Sub-case 3b (081KRMEXM0008QG0R000X1PPGC at push-time) + r…

…ead-tree HEAD index recovery

Folds in the two refinements captured in the 2026-05-21 memo (PR #4535)
to the saturation-ceiling discipline directly, so future Otto cold-boots
inherit them via auto-load instead of via memory-file pointer.

Two edits:

1. New Sub-case 3b — pack-dir contention at git-push time. Same 081KRMEXM0008QG0R000X1PPGC
   root cause as sub-case 3 (worktree-add time), but the symptom appears
   on push (Interrupted system call on .git/objects/pack). Distinct from
   081KRW63S0008QG0R000EAZ9K2 (silent-push-failure with exit 0). Mitigation: REST git-data
   API bypass per PR #4145. Empirical anchor: PR #4535 shipped via the
   bypass after git push timed out at exit 124.

2. In-place index recovery — git read-tree HEAD rebuilds a truncated
   index file (post stale-lock-removal under peer contention) without
   requiring worktree abandonment. Extends sub-case 5 (peer-side
   destructive) recovery toolkit. Empirical anchor: PR #4532 shipped
   after read-tree HEAD recovered an index truncated by stale-lock-
   removal race; previously only recovery option was abandonment.

Both edits are minimal-additive: they extend existing sub-case structure
rather than reorganizing it. Section header still accurate ("4 failure
sub-cases of borrow-on-existing") because 3b is a sibling-variant of 3,
not a new numbered case.

Authored + pushed via REST git-data API bypass because git push was
still hitting the very Sub-case 3b being documented.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
```

### `510da94dc982b43c7a43943d38f35e5382841516` -- 2026-05-21T13:35:43Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `.claude/rules/claim-acquire-before-worktree-work.md`

**Message:**

```
fix(rule): correct read-tree HEAD postcondition — status is NOT clean…

… after rebuild

P2 thread finding (chatgpt-codex-connector): read-tree HEAD rewrites the
index but does NOT touch the working tree. The original wording said
"git status returns clean (empty)" after rebuild — false in the general
case + actually false in the empirical case the rule documents (the
shard file was untracked at the time).

Correction: the recovery indicator is the DISAPPEARANCE of "index file
smaller than expected" — not a clean status. Genuine working-tree-vs-
HEAD diff still reflects in status. Misreading read-tree as "should
produce clean status" is the most common way the recovery gets
misdiagnosed as failed when it actually succeeded.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
```

### `ae15cbe997af8aab3837cfb9c31c26c33528b408` -- 2026-05-21T13:37:53Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `.claude/rules/claim-acquire-before-worktree-work.md`

**Message:**

```
fix(rule): 3 review findings — sub-case count + exit-124 origin + RES…

…T/core budget

P1 findings from copilot-pull-request-reviewer:

1. Count of sub-cases was stale ("4 failure sub-cases of borrow-on-existing"
   + "All 4 sub-cases empirically validated") — now 5 with the addition of
   3b. Updated section header to "5 failure sub-cases" + footnote naming
   3b as the fifth empirical sub-case with a working mitigation.

2. exit 124 is from the GNU timeout wrapper (command killed by timeout
   status), NOT a native git push exit code. Clarified in Sub-case 3b
   empirical anchor that the contention was hanging git push indefinitely
   until the timeout wrapper killed it.

3. REST API calls consume the REST/core budget (5000/hr per token), NOT
   the GraphQL budget. Original text referenced "Normal-tier GraphQL
   budget" which conflated independent budget pools. Updated cost section
   to reference REST/core explicitly + clarified relationship to the
   GraphQL tier classification in refresh-world-model-poll-pr-gate.md
   (which is GraphQL-scoped, does not translate directly to REST/core).

4. Outdated thread (already-addressed in prior fix commit 510da94d on
   read-tree HEAD postcondition) resolved no-op.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
```
