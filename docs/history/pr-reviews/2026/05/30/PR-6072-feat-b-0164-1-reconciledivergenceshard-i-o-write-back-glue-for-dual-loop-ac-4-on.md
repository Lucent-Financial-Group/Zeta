# PR #6072 -- feat(081KR7JY10008QG0R000MH7PJT): reconcileDivergenceShard — I/O write-back glue for dual-loop AC #4 "one action"

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 6072 |
| Title | feat(081KR7JY10008QG0R000MH7PJT): reconcileDivergenceShard — I/O write-back glue for dual-loop AC #4 "one action" |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-05-30T03:06:44Z |
| Merged at | 2026-05-30T03:17:26Z |
| Merge commit SHA | `ab0265cc70278e949ea864fc5992cfa40870dc87` |
| Branch | `otto-cli/b0164.1-reconcile-writeback-glue` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/6072 |
| Changed files | 2 |
| Additions / deletions | +227 / -15 |

## Description

## What

Adds `reconcileDivergenceShard(repoRoot, relPath, decision, note?)` to
`tools/hygiene/divergence-reconcile.ts` — the I/O glue that **lands** a
morning-reconciliation decision back into a divergence shard in place. This is
the I/O "one action" of **081KR7JY10008QG0R000MH7PJT dual-loop AC #4** ("the morning
reconciliation can resolve the thread in one read + one action").

## Why — symmetric completion of AC #4

AC #4 was asymmetrically complete versus AC #2:

| | AC #2 (file on disagreement) | AC #4 (resolve in one read + one action) |
|---|---|---|
| pure core | `detectReviewThreadDisagreement` (#6067) | `fillReconciliation` (#6069) |
| I/O glue | `fileReviewThreadDisagreement` (#6068) | **(missing — this PR)** |

AC #2 had both halves; AC #4 had the reader (`scanDivergenceDir` = "one read",
#6066) and the *pure* shard-side transform (`fillReconciliation` returns
reconciled markdown but never writes), so the CLI told the maintainer to fill
the section *by hand*. `reconcileDivergenceShard` is the missing I/O shell:
read the shard → apply `fillReconciliation` → write the reconciled markdown
back **in place**, mirroring how `fileReviewThreadDisagreement` composes the
pure detector with the writer.

## Design — in-place write is the deliberate opposite of the divergence writer

The in-place overwrite is the README-prescribed update
(`docs/hygiene-history/divergences/README.md` §"Reconciliation outcomes":
*"the shard is updated in place (not deleted) so the divergence history is
preserved permanently"*). This is the intentional **opposite** of the
divergence *writer*'s never-overwrite-differing-content rule:

- the **writer** guards uncommitted divergence evidence (fail-closed-OR-idempotent);
- **reconciliation** transforms a pending placeholder into a decided section.

Safety comes from `fillReconciliation`, which preserves every byte up to and
including the `## Reconciliation` heading — **both loop perspectives + the
disagreement summary survive verbatim** — and git preserves the
pre-reconciliation revision. No divergence evidence is erased.

## Fail-closed (all validation before the write)

- absent shard at `<repoRoot>/<relPath>` → throw, no write.
- not a divergence shard (`parseShardMeta` returns null) → throw, **file left
  unchanged** (guards a mistyped `relPath` pointing at unrelated markdown that
  happens to carry a `## Reconciliation` heading).
- invalid decision / no `## Reconciliation` section / already-reconciled shard
  → `fillReconciliation` throws, no write (a prior human decision is never
  silently re-decided; a second reconcile run fails closed).

## Below the blocked boundary

Never touches GitHub, never resolves the live PR thread — that end-to-end half
remains the blocked impl child pending 081KQJZR90008QG0R000FTJ1TC (concurrent dual-loop harness).

## Focused checks

- `bun test tools/hygiene/divergence-reconcile.test.ts tools/hygiene/divergence-shard.test.ts`
  → **61 pass / 0 fail** (7 new tests: full write→scan→reconcile→scan loop,
  decision-only + note, divergence-evidence-preserved, absent-file throw,
  not-a-shard fail-closed, already-reconciled re-run fail-closed, invalid-decision
  fail-closed).
- `bun run typecheck` (`tsc --noEmit`) → **0 errors in changed files**. The only
  typecheck errors are pre-existing `@nats-io/*` module-resolution failures in
  `agentic-organization/apps/workers/`, unrelated to this change.
- Commit canary: `git ls-tree HEAD` == `HEAD~1` == 66 (no tree collapse).

## Scope

One bounded step, TS-only, additive (1 new exported function + 1 new result
type + 7 tests). No `.fs`/.NET surface touched. Claim acquired via
`tools/bus/claim.ts` as `otto-cli` for 081KR7JY10008QG0R000MH7PJT.

🤖 Generated with [Claude Code](https://claude.com/claude-code)


## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | true |
| Total threads | 3 |
| Resolved threads | 3 |
| Unresolved threads | 0 |
| Total review comments | 6 |
| Total fix commits (touching thread paths) | 2 |

## Review threads

### Thread 1 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F1aFy`
- **Path**: `tools/hygiene/divergence-reconcile.ts`
- **Replies**: 1

**Initial comment:**

- **`github-advanced-security[bot]` (bot)** at 2026-05-30T03:09:22Z on `tools/hygiene/divergence-reconcile.ts`:367 (association: NONE)

  <pre>
  ## CodeQL / Potential file system race condition

  The file may have changed since it [was checked](1).

  [Show more details](https://github.com/Lucent-Financial-Group/Zeta/security/code-scanning/112)
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-30T03:14:56Z on `tools/hygiene/divergence-reconcile.ts`:367 (association: MEMBER)

    <pre>
    Fixed in 98144b1 — replaced the `existsSync` pre-check with a direct `readFileSync` in try/catch that maps ENOENT to the same friendly error. This removes the check-then-use gap (CWE-367 TOCTOU) — signal-based IO per the framework's exceptions-as-signals discipline.
    </pre>

### Thread 2 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F1aL9`
- **Path**: `tools/hygiene/divergence-reconcile.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-30T03:09:49Z on `tools/hygiene/divergence-reconcile.ts`:343 (association: NONE)

  <pre>
  reconcileDivergenceShard accepts an arbitrary relPath and uses path.join(repoRoot, relPath). If relPath is absolute or contains traversal (e.g. "../"), join() can escape repoRoot and the function may overwrite unrelated files. Since this is a write-back helper intended only for divergence shards, validate that relPath stays within the repo and under the divergence root before reading/writing.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-30T03:14:58Z on `tools/hygiene/divergence-reconcile.ts`:343 (association: MEMBER)

    <pre>
    Fixed in 98144b1 — `relPath` is now bounded to the divergence root (`docs/hygiene-history/divergences/`) via `resolve()` + a `relative()`-contains check before any I/O. Absolute paths and `../` traversal are rejected with `outside the divergence root`. Added a falsifying test (traversal + absolute both rejected; victim file untouched).
    </pre>

### Thread 3 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F1aMA`
- **Path**: `tools/hygiene/divergence-reconcile.test.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-30T03:09:49Z on `tools/hygiene/divergence-reconcile.test.ts`:269 (association: NONE)

  <pre>
  This test adds direct name attribution ("(Aaron)") on a code surface. Per .github/copilot-instructions.md "No name attribution in code, docs, or skills" (around lines 317-356), use role-refs (e.g. "human maintainer") instead of specific names.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-30T03:14:59Z on `tools/hygiene/divergence-reconcile.test.ts`:269 (association: MEMBER)

    <pre>
    Fixed in 98144b1 — replaced `(Aaron)` with the role-ref `(human maintainer)` across all 9 test fixtures, per .github/copilot-instructions.md "No name attribution".
    </pre>

## Fix commits (touching thread paths)

### `612ac26054ffbeca0c127f610a6fd0c47a625928` -- 2026-05-30T03:05:54Z -- `claude`

**Touched files (intersect with thread paths):**

- `tools/hygiene/divergence-reconcile.test.ts`
- `tools/hygiene/divergence-reconcile.ts`

**Message:**

```
feat(081KR7JY10008QG0R000MH7PJT): reconcileDivergenceShard — I/O write-back glue for du…

…al-loop AC #4 "one action"

AC #4 ("resolve the thread in one read + one action") was asymmetrically
complete vs AC #2. AC #2 has both halves: the pure detector
(detectReviewThreadDisagreement) AND the I/O glue (fileReviewThreadDisagreement)
that composes detect→write. AC #4 had the reader (scanDivergenceDir = "one
read", #6066) and the *pure* shard-side transform (fillReconciliation = returns
reconciled markdown, #6069) — but no I/O glue landing that markdown back. The
CLI even told the maintainer to fill the section by hand.

reconcileDivergenceShard is the symmetric completion: read the shard, apply
fillReconciliation, write the reconciled markdown back IN PLACE — the
README-prescribed update ("the shard is updated in place (not deleted) so the
divergence history is preserved permanently"). It is the deliberate OPPOSITE of
the divergence WRITER's never-overwrite rule: the writer guards uncommitted
divergence evidence; reconciliation transforms a pending placeholder into a
decided section, and fillReconciliation preserves every byte up to the
`## Reconciliation` heading (both loop perspectives + summary survive verbatim),
so no divergence evidence is erased.

Fail-closed, all validation before the write:
- absent shard at path → throw, no write.
- not a divergence shard (parseShardMeta null) → throw, file unchanged
  (guards a mistyped relPath at unrelated markdown with a Reconciliation heading).
- invalid decision / no section / already-reconciled → fillReconciliation
  throws, no write (a prior human decision is never silently re-decided).

Stays below the blocked end-to-end boundary: never touches GitHub, never
resolves the live PR thread (blocked impl child pending 081KQJZR90008QG0R000FTJ1TC).

Focused checks: bun test tools/hygiene/divergence-{reconcile,shard}.test.ts →
61 pass / 0 fail (7 new); bun run typecheck → 0 errors in changed files
(only pre-existing @nats-io/* module errors in agentic-organization/, unrelated).

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `98144b13f688c4f6bbf2e3cd5f2587047470f6dd` -- 2026-05-30T03:14:17Z -- `claude`

**Touched files (intersect with thread paths):**

- `tools/hygiene/divergence-reconcile.test.ts`
- `tools/hygiene/divergence-reconcile.ts`

**Message:**

```
fix(081KR7JY10008QG0R000MH7PJT): guard reconcileDivergenceShard path + drop TOCTOU pre-…

…check

Three review findings on PR #6072:

- Copilot (path traversal): reconcileDivergenceShard did
  join(repoRoot, relPath) blindly, so an absolute path or "../"
  traversal could escape repoRoot and clobber unrelated files.
  Now resolve() + relative()-contains check bounds relPath to the
  divergence root (docs/hygiene-history/divergences/) before any I/O.
- CodeQL (CWE-367 file-system race): the existsSync-then-readFileSync
  pre-check is a check-then-use TOCTOU. Replaced with a direct read in
  try/catch that maps ENOENT to the same friendly error — signal-based
  IO per the framework's exceptions-as-signals discipline.
- Copilot (name attribution): replaced "(Aaron)" with role-ref
  "(human maintainer)" across all 9 test fixtures, per
  .github/copilot-instructions.md "No name attribution".

Added a falsifying test (traversal + absolute path both rejected,
victim file untouched). Moved the existing "not a divergence shard"
test inside the divergence root so its intent (not-a-shard fail-closed)
still fires rather than tripping the new containment guard first.

32 tests pass.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```
