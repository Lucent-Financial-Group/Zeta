# PR #6118 -- fix(persistence/windows): windowless loop (conhost --headless) + install.ps1 plan

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 6118 |
| Title | fix(persistence/windows): windowless loop (conhost --headless) + install.ps1 plan |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-05-30T14:24:06Z |
| Merged at | 2026-05-30T14:33:11Z |
| Merge commit SHA | `9c8c56ffe484f237c4b1f9eb023237f58bc18d7b` |
| Branch | `otto-windows/loop-windowless-conhost-2026-05-30` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/6118 |
| Changed files | 6 |
| Additions / deletions | +224 / -4 |

## Description

**Popup fix (you reported a console window every minute once the loop actually started ticking).**

Root cause: the Task Scheduler *interactive* task launched `pwsh.exe` directly, which flashes a console each fire — and the wrapper's `git`/`bun` children pop their own. `-WindowStyle Hidden` only hides *after* creation (still flashed; you confirmed). Fix: action is now `conhost.exe --headless pwsh.exe …` — a headless pseudoconsole the whole process tree inherits, so nothing shows. **Verified live on this Win11 laptop; you confirmed no window.**

- `scheduled-task.xml`: `Command` → conhost; `Arguments` → `--headless "<pwsh>" …`
- `install-scheduled-task.ts`: `detectConhost()` + `CONHOST_PATH` placeholder
- `otto-loop-wrapper.ps1`: writes `last-tick-result.txt` — conhost swallows the child exit code (verified `exit 7` → `0`), so the task's `Last Result` is always 0; this keeps tick health observable (heartbeat also reports it)
- README: windowless-launch note + the caveat
- 13 tests pass, tsc + markdownlint clean; installer re-register verified end-to-end (live task is installer-produced)

Also lands the **install.ps1 cross-edition test plan** (`docs/plans/…`): declarative install.ps1 (mise/npm cross-platform-first, scoop→winget→choco for the irreducible remainder), Server-Core Docker + desktop loop-smoke matrix, per the shield rule (assert, don't skip).

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | true |
| Total threads | 4 |
| Resolved threads | 4 |
| Unresolved threads | 0 |
| Total review comments | 8 |
| Total fix commits (touching thread paths) | 3 |

## Review threads

### Thread 1 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F4Fom`
- **Path**: `docs/plans/2026-05-30-windows-install-ps1-cross-edition-tests-plan.md`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-30T14:27:54Z on `docs/plans/2026-05-30-windows-install-ps1-cross-edition-tests-plan.md`:48 (association: NONE)

  <pre>
  P1: The line starting with `+ CLI tools ...` will be parsed as a `+`-style list item in Markdown (and can trip markdownlint / render incorrectly). Reflow the sentence so no line begins with `+`.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-30T14:30:28Z on `docs/plans/2026-05-30-windows-install-ps1-cross-edition-tests-plan.md`:48 (association: MEMBER)

    <pre>
    Already fixed in fbf59104a — the wrapped line no longer begins with a plus marker (markdownlint clean).
    </pre>

### Thread 2 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F4Foo`
- **Path**: `docs/plans/2026-05-30-windows-install-ps1-cross-edition-tests-plan.md`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-30T14:27:54Z on `docs/plans/2026-05-30-windows-install-ps1-cross-edition-tests-plan.md`:112 (association: NONE)

  <pre>
  P0: The plan suggests executing remote content via `Invoke-RestMethod ... | Invoke-Expression`, which is the PowerShell equivalent of `curl | sh` and violates the repo’s “no pipe-to-shell from external URLs” safety rule. Prefer requiring a pre-installed package manager (winget/choco) or a manual Scoop install step instead of piping downloaded script text into the interpreter.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-30T14:30:22Z on `docs/plans/2026-05-30-windows-install-ps1-cross-edition-tests-plan.md`:112 (association: MEMBER)

    <pre>
    Fixed in 4eeed40f3 — scoop install is now download-then-exec (temp .ps1 + verify non-empty + run the local file), mirroring macos.sh Homebrew 081KQ8P5D0008QG0R001DMK8JD. No pipe-to-shell.
    </pre>

### Thread 3 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F4Fot`
- **Path**: `docs/plans/2026-05-30-windows-install-ps1-cross-edition-tests-plan.md`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-30T14:27:55Z on `docs/plans/2026-05-30-windows-install-ps1-cross-edition-tests-plan.md`:132 (association: NONE)

  <pre>
  P1: The PowerShell sample claims `#Requires -Version 5.1` but uses the `?:` ternary operator (`($winget ? $winget : $scoopId)`), which is only available in PowerShell 7+. This would fail if copied verbatim on Windows PowerShell 5.1; use an `if (...) { ... } else { ... }` expression for the ID selection.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-30T14:30:23Z on `docs/plans/2026-05-30-windows-install-ps1-cross-edition-tests-plan.md`:132 (association: MEMBER)

    <pre>
    Fixed in 4eeed40f3 — replaced the PS7 ternary with if/else (5.1-safe) for the winget/choco id selection.
    </pre>

### Thread 4 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F4Fow`
- **Path**: `tools/persistence/windows/install-scheduled-task.test.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-30T14:27:55Z on `tools/persistence/windows/install-scheduled-task.test.ts`:19 (association: NONE)

  <pre>
  P2: The placeholder-substitution tests don’t currently exercise the real `scheduled-task.xml` template. Since this PR introduces a new placeholder (`{{CONHOST_PATH}}`) and changes the Task Scheduler invocation shape, a small test that reads `scheduled-task.xml` and runs `substitutePlaceholders` with dummy values would catch future placeholder drift without requiring Windows-only `whoami/where.exe` calls.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-30T14:30:26Z on `tools/persistence/windows/install-scheduled-task.test.ts`:19 (association: MEMBER)

    <pre>
    Done in 4eeed40f3 — added a drift test reading the real scheduled-task.xml + substitutePlaceholders over all 6 placeholders (incl. CONHOST_PATH); asserts no leftovers + the conhost --headless shape. Platform-independent.
    </pre>

## Fix commits (touching thread paths)

### `635d8ecd188d94b83845a12f47e02b0c9f9db5b3` -- 2026-05-30T14:23:55Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `docs/plans/2026-05-30-windows-install-ps1-cross-edition-tests-plan.md`
- `tools/persistence/windows/install-scheduled-task.test.ts`

**Message:**

```
fix(persistence/windows): windowless loop via conhost --headless + in…

…stall.ps1 cross-edition plan

Popup fix (operator: console window every minute after the repetition fix made the loop
actually tick): the Task Scheduler interactive task launched pwsh.exe directly, which flashes
a console each fire (and the wrapper's git/bun children pop their own). -WindowStyle Hidden
only hides post-creation (still flashed — operator confirmed). Switch the task action to
`conhost.exe --headless pwsh.exe …`: a headless pseudoconsole the whole process tree inherits,
so nothing shows. Verified live on this Win11 laptop — operator confirmed no window.

- scheduled-task.xml: Command -> {{CONHOST_PATH}}; Arguments -> --headless "{{PWSH_PATH}}" ...
- install-scheduled-task.ts: detectConhost() + CONHOST_PATH placeholder (System32\conhost.exe)
- otto-loop-wrapper.ps1: write last-tick-result.txt — conhost swallows the child exit code
  (verified: exit 7 -> 0), so the task's Last Result is always 0; this keeps tick health
  observable (heartbeat also reports it)
- README: windowless-launch note + the Last-Result-always-0 caveat
- tests updated (CONHOST_PATH); 13 pass, tsc + markdownlint clean

Plan doc: docs/plans/...windows-install-ps1-cross-edition-tests-plan.md — declarative
install.ps1 (mise/npm cross-platform-first, scoop->winget->choco remainder) + Server-Core
Docker + desktop loop-smoke test matrix (per operator's shield rule: assert, don't skip).

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
```

### `fbf59104abb78f3450967880dda208425ce5ddcc` -- 2026-05-30T14:25:54Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `docs/plans/2026-05-30-windows-install-ps1-cross-edition-tests-plan.md`

**Message:**

```
fix(docs): markdownlint MD032 in install.ps1 plan (wrapped line start…

…ed with "+ ")

A wrapped paragraph line began with "+ CLI tools…", which markdownlint reads as a list
bullet (MD032: list not surrounded by blank lines). Reword "+ " -> "and "; markdownlint clean.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
```

### `4eeed40f35387c9ad56f8403be08e62e3fadc11f` -- 2026-05-30T14:30:08Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `docs/plans/2026-05-30-windows-install-ps1-cross-edition-tests-plan.md`
- `tools/persistence/windows/install-scheduled-task.test.ts`

**Message:**

```
fix(windows): address Copilot review on #6118 (pipe-to-shell P0, 5.1 …

…ternary P1, drift test P2)

Plan install.ps1 skeleton:
- P0: scoop install was Invoke-RestMethod | Invoke-Expression (pipe-to-shell from external
  URL). Switch to download-then-exec (temp .ps1 + verify non-empty + run local file), mirroring
  macos.sh's Homebrew 081KQ8P5D0008QG0R001DMK8JD pattern.
- P1: resolver used the PS7+ ?: ternary under #Requires 5.1. Replace with if/else (5.1-safe).

Test:
- P2: add a drift test that reads the real scheduled-task.xml + substitutePlaceholders with
  all 6 placeholders (incl. CONHOST_PATH) — asserts no leftover {{...}} + conhost --headless
  shape. Platform-independent (no whoami/where). Would have caught the CONHOST_PATH drift.

(+CLI P1 thread already fixed in fbf59104a.) 10 tests pass, tsc + markdownlint clean.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
```
