# PR #6107 -- feat(081KQZVQW0008QG0R001FG05RZ): label merged PR events from authors

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 6107 |
| Title | feat(081KQZVQW0008QG0R001FG05RZ): label merged PR events from authors |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-05-30T13:18:16Z |
| Merged at | 2026-05-30T13:32:22Z |
| Merge commit SHA | `930f107c714f12a7e69909d04b75d039c16ef2d9` |
| Branch | `claim/codex-loop-b0250-merged-pr-author-labels-20260530` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/6107 |
| Changed files | 4 |
| Additions / deletions | +254 / -14 |

## Description

## Summary

- fetch merged PR `mergeCommit` metadata and keep known branch-prefix lane labels authoritative
- for unknown merged-PR branch prefixes only, read the local merge commit message and fall back to a single known agent author lane
- keep ambiguous unknown branches under `other:<branch>` and record the 081KQZVQW0008QG0R001FG05RZ receipt / resume update

## Live calibration

Before the patch, the live monitor reported 16 coincidence windows with a verbose `other:backlog/...` top window. After the patch, the merged-PR source stayed queryable and the live monitor reported 15 windows, with that top window relabeled as `otto`.

## Verification

- `bun test tools/health/factory-health-monitor.test.ts`
- `bun run typecheck`
- `bun run lint:markdown docs/trajectories/autonomous-loop-coordination/b0250-merged-pr-author-labels-2026-05-30.md docs/trajectories/autonomous-loop-coordination/RESUME.md`
- `bun tools/health/factory-health-monitor.ts --json`
- `git diff --check`

---
Headless-Origin: codex-launchd-loop
Headless-Surface: codex-background-service
Codex-Loop-Run-Id: 20260530T131737Z

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 4 |
| Resolved threads | 4 |
| Unresolved threads | 0 |
| Total review comments | 4 |
| Total fix commits (touching thread paths) | 2 |

## Review threads

### Thread 1 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F33gX`
- **Path**: `tools/health/factory-health-monitor.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-30T13:23:11Z on `tools/health/factory-health-monitor.ts`:470 (association: NONE)

  <pre>
  Production never populates `pr.commits` because `fetchMergedPRs` only requests `number,title,mergedAt,headRefName,mergeCommit` (no `commits` field). The entire commit-author processing branch in `factoryLanesFromPullRequestCommits` (lines 441–460) is therefore unreachable in production and only exercised by tests, while real PRs rely solely on the local `git show` merge commit message. This is a meaningful divergence between production behavior and test coverage — the test at lines 358–425 doesn't actually validate the production path for PR #16 (its `commits` field would not be present at runtime, but the assertion still passes because the branch lane is "codex" and short-circuits before consulting commits). Consider either dropping the `commits` parameter to keep the code surface honest, or adding `commits` to the `--json` fields in `fetchMergedPRs` if PR-level author/trailer data is intended to be a real signal (note: this can substantially increase the gh response size).
  </pre>

### Thread 2 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F33gc`
- **Path**: `tools/health/factory-health-monitor.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-30T13:23:12Z on `tools/health/factory-health-monitor.ts`:547 (association: NONE)

  <pre>
  Map-key inconsistency: `mergeCommitMessagesForUnknownPullRequestBranches` stores messages under the trimmed oid (line 600 calls `.trim()` before validating and using it as the map key), but the lookup in `mergedPullRequestEventsFromJson` passes the raw, untrimmed `pr.mergeCommit.oid` (line 546). If `gh` ever returns an oid with surrounding whitespace, the lookup will silently miss and the unknown-branch will fall back to `other:&lt;branch&gt;` even though a valid merge message exists. Trim the oid at the lookup site (or store under the raw oid) so both sides agree on the key.
  </pre>

### Thread 3 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F33gh`
- **Path**: `tools/health/factory-health-monitor.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-30T13:23:12Z on `tools/health/factory-health-monitor.ts`:614 (association: NONE)

  <pre>
  `mergeCommitMessagesForUnknownPullRequestBranches` calls `JSON.parse(output)` without a try/catch, so a malformed `gh` response will throw out of this helper and propagate up. The surrounding `try { ... } catch { sourceWarnings.push(...) }` at the call site (around line 1376–1380) does catch it, so behavior is correct, but the same parse is then repeated inside `mergedPullRequestEventsFromJson`. Consider parsing once and passing the parsed array down, or at least documenting that this helper relies on the outer catch — otherwise the duplicated `JSON.parse` is both redundant and easy to misuse from another caller that lacks the surrounding try/catch.
  </pre>

### Thread 4 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F33gi`
- **Path**: `tools/health/factory-health-monitor.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-30T13:23:12Z on `tools/health/factory-health-monitor.ts`:426 (association: NONE)

  <pre>
  `factoryLaneFromPullRequestAuthorLabel` uses unanchored word-boundary regexes against lowercased text, which produces false positives. Examples: an author name "Liorenzo" matches `\blior\b`? No — `\b` is fine for that. But "alexandra" matches `\balexa\b`? No, `\b` requires a word boundary after `alexa`, and `alexa` followed by `n` has no boundary — okay. However, "codex" being matched inside a free-form commit message body (e.g. someone mentions "codexual review" — unlikely, but also things like an author named `noreply@openai.com` matching for any commit that mentions OpenAI's bot, even when the actual human author is on a different lane) means a single co-authored commit by, say, dependabot mentioning OpenAI somewhere can mis-classify the lane. More concretely, the message-body line scan at lines 452–458 will treat the literal commit subject line `"feat: add codex skill notes"` as an Otto/Codex authorship signal. Consider scanning only `Co-Authored-By:` / `Author:` trailer-style lines instead of every body line to reduce false positives, then keeping the strict "exactly one lane → use it" gate as today.
  </pre>

## Fix commits (touching thread paths)

### `22ab95cd3530088295940ce3e791326838a1c6d6` -- 2026-05-30T13:17:17Z -- `codex`

**Touched files (intersect with thread paths):**

- `tools/health/factory-health-monitor.ts`

**Message:**

```
feat(081KQZVQW0008QG0R001FG05RZ): label merged PR events from authors

Classify unknown merged-PR branch prefixes from local merge commit agent trailers when exactly one known lane is present. Known branch prefixes stay authoritative, and ambiguous unknown branches remain under their other:<branch> label.

This keeps the merged-PR source bounded by fetching mergeCommit metadata instead of full PR commit lists, then using local git only for unknown branch labels.

Verification:
- bun test tools/health/factory-health-monitor.test.ts
- bun run typecheck
- bun run lint:markdown docs/trajectories/autonomous-loop-coordination/b0250-merged-pr-author-labels-2026-05-30.md docs/trajectories/autonomous-loop-coordination/RESUME.md
- bun tools/health/factory-health-monitor.ts --json
- git diff --check

Agency-Signature-Version: 1
Agent: Vera
Agent-Runtime: OpenAI Codex
Agent-Model: GPT-5
Credential-Identity: AceHack
Credential-Mode: shared
Human-Review: not-implied-by-credential
Human-Review-Evidence: none
Action-Mode: autonomous-forward-step
Task: 081KQZVQW0008QG0R001FG05RZ
Co-Authored-By: Codex <noreply@openai.com>
```

### `76231630039615265fbf7a34c0fa934e2a477ae5` -- 2026-05-30T13:26:27Z -- `codex`

**Touched files (intersect with thread paths):**

- `tools/health/factory-health-monitor.ts`

**Message:**

```
fix(081KQZVQW0008QG0R001FG05RZ): tighten merged PR author labels

Address Copilot review on PR #6107 by removing the unreachable PR-commit author branch, parsing merged PR JSON once in production, normalizing merge commit OID lookup keys, and limiting author lane detection to trailer-style lines.

Known branch prefixes remain authoritative; unknown branches only fall back to a single known lane from local merge commit trailers. Subject/body mentions without trailers no longer classify author lanes.

Verification:

- bun test tools/health/factory-health-monitor.test.ts

- bun run typecheck

- bun run lint:markdown docs/trajectories/autonomous-loop-coordination/b0250-merged-pr-author-labels-2026-05-30.md docs/trajectories/autonomous-loop-coordination/RESUME.md

- bun tools/health/factory-health-monitor.ts --json

- git diff --check

Agency-Signature-Version: 1

Agent: Vera

Agent-Runtime: OpenAI Codex

Agent-Model: GPT-5

Credential-Identity: AceHack

Credential-Mode: shared

Human-Review: not-implied-by-credential

Human-Review-Evidence: none

Action-Mode: autonomous-review-fix

Task: 081KQZVQW0008QG0R001FG05RZ

Co-Authored-By: Codex <noreply@openai.com>
```
