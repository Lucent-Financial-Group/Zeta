# PR #1419 -- feat(C1 partial): ChaosEnvDeterminism cfg + atomize splitMix+AdvanceTime critical section

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 1419 |
| Title | feat(C1 partial): ChaosEnvDeterminism cfg + atomize splitMix+AdvanceTime critical section |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-05-03T15:17:51Z |
| Merged at | 2026-05-03T15:20:30Z |
| Merge commit SHA | `7b0b69dbda0f7b650f85d4229a569824b34cc013` |
| Branch | `otto/c1-chaos-env-determinism-cfg-atomize-2026-05-03` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/1419 |
| Changed files | 4 |
| Additions / deletions | +57 / -15 |

## Description

## Summary

- **C1 → B partial (1 of 4 specs).** ChaosEnvDeterminism.tla had no .cfg so TLC couldn't run it; adding the cfg surfaced a latent spec atomicity issue.
- **Atomized DelayCritical(t)** — collapsed DelayRng + DelayClock into one step. Mirrors ChaosEnvironment.fs holding the lock across both splitMix + AdvanceTime; real concurrent code never observes the "rng-recorded-but-no-clock-yet" intermediate state because the lock is held.
- **TLC clean**: 2043 distinct states / depth 26 / both `TypeOK` and `Atomic` invariants hold. Bounded by 2 threads / Seed=0 / HistoryBound=16.
- **F# CI registration** added: `[<Fact>] TLC validates ChaosEnvDeterminism`.

## Why partial (not all 4 C1 specs)

The other 3 C1 specs (ConsistentHashRebalance, DictionaryStripedCAS, AsyncStreamEnumerator) need more than just a cfg — they have structural spec issues (dangling `CONSTANT` declarations, incomplete actions with TODO comments, etc.) that need more careful redesign. Filed for future work.

## Pattern

Same author-time class as B-0181 + B-0184 (per `memory/feedback_under_specified_action_preconditions_recurring_class_in_formal_specs_aaron_2026_05_03.md`): the original spec modeled separate-step DelayRng + DelayClock without a precondition that prevented the intermediate state from being observable. The fix is to model the action's actual atomicity (single step with both effects).

## Test plan

- [x] `dotnet build` clean (0 warnings, 0 errors).
- [x] `dotnet test --filter "FullyQualifiedName~ChaosEnvDeterminism"` passes (1 test, 481ms).
- [x] Direct TLC: `cd tools/tla/specs && java -cp ../tla2tools.jar tlc2.TLC ChaosEnvDeterminism` reports "No error has been found" with 2043 distinct states.
- [ ] CI gate green on Linux x64 standard runner.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 4 |
| Resolved threads | 0 |
| Unresolved threads | 4 |
| Total review comments | 4 |
| Total fix commits (touching thread paths) | 1 |

## Review threads

### Thread 1 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM5_Miwl`
- **Path**: `tools/tla/specs/ChaosEnvDeterminism.cfg`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-03T15:21:35Z on `tools/tla/specs/ChaosEnvDeterminism.cfg`:7 (association: NONE)

  <pre>
  The header comment describes the bounded exploration as “every thread completes a fixed number of acquire/release cycles” and implies a per-thread cycle cap, but the only constraint is `HistoryBound` in the .tla file (a global `Len(history) &lt;= 16`). Please adjust the wording to reflect that this bounds total DelayCritical steps across all threads, not per-thread progress/fairness.

  </pre>

### Thread 2 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM5_Miwx`
- **Path**: `tests/Tests.FSharp/Formal/Tlc.Runner.Tests.fs`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-03T15:21:36Z on `tests/Tests.FSharp/Formal/Tlc.Runner.Tests.fs`:301 (association: NONE)

  <pre>
  This test comment references `ChaosEnvironment.fs`, but the implementation is in `src/Core/ChaosEnv.fs` (type/module `ChaosEnvironment`). Please update the filename reference to avoid broken cross-references.

  </pre>

### Thread 3 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM5_Miw4`
- **Path**: `tools/tla/specs/ChaosEnvDeterminism.tla`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-03T15:21:36Z on `tools/tla/specs/ChaosEnvDeterminism.tla`:41 (association: NONE)

  <pre>
  The comment refers to `ChaosEnvironment.fs`, but the implementation appears to live in `src/Core/ChaosEnv.fs` (type/module `ChaosEnvironment`). Please fix this file-path reference so cross-references stay accurate.
  </pre>

### Thread 4 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM5_Miw_`
- **Path**: `tools/tla/specs/ChaosEnvDeterminism.tla`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-03T15:21:36Z on `tools/tla/specs/ChaosEnvDeterminism.tla`:86 (association: NONE)

  <pre>
  This comment claims “With 2 threads and 4 cycles each, history tops out at 16 entries”, but `HistoryBound == Len(history) &lt;= 16` only bounds the *total* history length; it doesn’t enforce per-thread cycle counts. Please reword the explanation (or tighten the constraint) so the bound description matches what TLC is actually constraining.

  </pre>

## Fix commits (touching thread paths)

### `fc0493f3be6f2f7e49befb9c2cce8a47f69efe94` -- 2026-05-03T15:17:33Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tests/Tests.FSharp/Formal/Tlc.Runner.Tests.fs`
- `tools/tla/specs/ChaosEnvDeterminism.cfg`
- `tools/tla/specs/ChaosEnvDeterminism.tla`

**Message:**

```
feat(C1 partial): ChaosEnvDeterminism cfg + atomize critical section

ChaosEnvDeterminism.tla had no .cfg so TLC couldn't run it (C1-grade
in math-proofs assessment). Adding the cfg surfaced a latent spec
issue: the original DelayRng + DelayClock as separate steps allowed
TLC to reach an intermediate "rng-recorded-but-no-clock-yet" state at
depth 3 where the Atomic invariant fails.

The fix: collapse DelayRng + DelayClock into a single DelayCritical
step. This mirrors ChaosEnvironment.fs which holds the lock across
both splitMix and AdvanceTime — real concurrent code never observes
the intermediate state because the lock is held atomically. The spec
now correctly models the same atomicity.

Bounded by 2 threads / Seed=0 / HistoryBound=16 — TLC completes in
<1s with 2043 distinct states explored at depth 26. Both TypeOK and
Atomic invariants hold. CHECK_DEADLOCK FALSE because every thread
reaching "done" then "idle" is intentional cycle completion (same
pattern as SpineAsyncProtocol's bounded-protocol terminal state).

F# CI registration: [<Fact>] TLC validates ChaosEnvDeterminism added
to tests/Tests.FSharp/Formal/Tlc.Runner.Tests.fs.

Math-proofs assessment matrix C1 row: 1 of 4 closed (other 3 specs
need more than just a cfg — structural spec issues like dangling
constants and incomplete actions).

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
```
