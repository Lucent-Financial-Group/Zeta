# PR #1385 -- docs(research): substrate-discovery via Zeta-native-AOT scoping 2026-05-03

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 1385 |
| Title | docs(research): substrate-discovery via Zeta-native-AOT scoping 2026-05-03 |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-05-03T11:37:03Z |
| Merged at | 2026-05-03T11:39:09Z |
| Merge commit SHA | `f2504890e7b475070b1958eb682cdc17b3d27d3e` |
| Branch | `research/substrate-discovery-zeta-native-aot-scoping-2026-05-03` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/1385 |
| Changed files | 2 |
| Additions / deletions | +267 / -0 |

## Description

## Summary

Architect-within-authority decision per CLAUDE.md "don't ask permission within authority scope": **yes, Zeta-native-AOT is the best long-term solution** for the custom substrate index the maintainer 2026-05-03 named.

## Why Zeta-native-AOT (vs alternatives)

Three load-bearing reasons:
1. **The workload IS Z-set algebra by definition.** file-add / file-remove / file-modify is exactly the delta-stream `IndexedZSet.fs` + `Incremental.fs` + `Operators.fs` consume. Strongest match.
2. **NativeAOT validates a deployment story we already need.** Cron / CI / agent-loop ticks all want fast startup. Substrate-discovery is the cheapest first try.
3. **Pre-v1 dogfooding is HIGHER leverage than deferring.** Per the 2026-05-03 math-proofs assessment, core algebra is A-grade verified; the API surface needs real-world exercise.

Alternatives rejected: TS+sqlite-vec/DuckDB (faster but doesn't dogfood); live-off-the-land (punts architecture); hybrid (two systems).

## Doc covers

- Substrate types: memory + skill + agent + rule + command + BACKLOG + tick shards + research + doc cross-refs + code symbols
- 6 query workloads with target latencies (cold-start, warm)
- Operator mapping per query (IndexedZSet keys, joins, filters, aggregates)
- NativeAOT deployment shape: CLI + watcher daemon + library
- 4-phase migration plan with parallel-run retirement gates
- 7-row risk register

## Next concrete work

**Phase 0 PoC (2-3 ticks):** F# project skeleton + NativeAOT publish + smoke test invocation. Validates toolchain end-to-end before substantial commit.

## Test plan

- [x] §33 archive-header lint passes
- [x] Memory-references lint passes (0 broken refs)
- [x] Composes with math-proofs assessment + IndexedZSet.fs + DbspChainRule.lean + DbspSpec.tla

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 4 |
| Resolved threads | 0 |
| Unresolved threads | 4 |
| Total review comments | 4 |
| Total fix commits (touching thread paths) | 1 |

## Review threads

### Thread 1 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM5_Lu7w`
- **Path**: `docs/research/2026-05-03-substrate-discovery-zeta-native-aot-scoping.md`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-03T11:40:17Z on `docs/research/2026-05-03-substrate-discovery-zeta-native-aot-scoping.md`:227 (association: NONE)

  <pre>
  `docs/research/2026-05-03-math-proofs-honest-assessment.md` is referenced here but does not exist in the repository/PR. This creates a broken cross-reference; either add the referenced doc in this PR, or replace the path with a stable reference (e.g., a PR link/issue) until the file exists on main.

  </pre>

### Thread 2 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM5_Lu76`
- **Path**: `docs/research/2026-05-03-substrate-discovery-zeta-native-aot-scoping.md`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-03T11:40:18Z on `docs/research/2026-05-03-substrate-discovery-zeta-native-aot-scoping.md`:185 (association: NONE)

  <pre>
  These tool references omit the `tools/hygiene/` prefix (and one is missing entirely in the later bullet list), which makes cross-references ambiguous and harder to search/click. Consider using full repo-relative paths consistently (e.g., `tools/hygiene/audit-memory-references.ts`, `tools/hygiene/audit-memory-index-duplicates.ts`).
  </pre>

### Thread 3 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM5_Lu8A`
- **Path**: `docs/research/2026-05-03-substrate-discovery-zeta-native-aot-scoping.md`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-03T11:40:18Z on `docs/research/2026-05-03-substrate-discovery-zeta-native-aot-scoping.md`:240 (association: NONE)

  <pre>
  `audit-memory-index-duplicates.ts` is referenced without its directory, but the actual file is `tools/hygiene/audit-memory-index-duplicates.ts`. Using the full path here avoids broken/ambiguous references (and matches the preceding `tools/hygiene/audit-memory-references.ts`).

  </pre>

### Thread 4 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM5_Lu8E`
- **Path**: `docs/research/2026-05-03-substrate-discovery-zeta-native-aot-scoping.md`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-03T11:40:18Z on `docs/research/2026-05-03-substrate-discovery-zeta-native-aot-scoping.md`:229 (association: NONE)

  <pre>
  In this `Composes with` entry, only the first core file uses a full path; the others (`Incremental.fs`, `Operators.fs`, `ZSet.fs`) are ambiguous outside of context. Consider using full repo-relative paths for each (`src/Core/Incremental.fs`, etc.) to keep cross-references unambiguous and consistent with other docs that cite source files by full path.

  </pre>

## Fix commits (touching thread paths)

### `cf1ebf8e72767d2475301e1f8ef9ab4ed3c02c85` -- 2026-05-03T11:36:48Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `docs/research/2026-05-03-substrate-discovery-zeta-native-aot-scoping.md`

**Message:**

```
docs(research): substrate-discovery via Zeta-native-AOT scoping 2026-…

…05-03

Architect-within-authority decision (per CLAUDE.md "don't ask
permission within authority scope — only two real gates"): yes,
Zeta-native-AOT IS the best long-term solution for the custom
substrate index the maintainer 2026-05-03 named.

Three load-bearing reasons override the classical-PM defer-the-
dogfooding default:

1. The workload IS Z-set algebra by definition. file-add /
   file-remove / file-modify is exactly the delta-stream the IVM
   primitives consume (IndexedZSet.fs + Incremental.fs +
   Operators.fs).
2. NativeAOT validates a deployment story we already need (cron /
   CI / agent-loop ticks all want fast startup). Substrate-discovery
   is the cheapest first try.
3. Pre-v1 dogfooding is HIGHER leverage than deferring. Per the
   2026-05-03 math-proofs assessment, the core algebra is A-grade
   verified; the API surface needs real-world exercise.

Alternatives considered and rejected: TS+sqlite-vec/DuckDB (faster
but doesn't dogfood); live-off-the-land via Skill router + grep
(punts architecture); hybrid TS+Zeta (two systems, more
complexity).

Doc covers: substrate types we index (memory + skill + agent +
rule + command + BACKLOG + tick shards + research + doc cross-refs
+ code symbols), 6 query workloads with target latencies, operator
mapping per query, NativeAOT deployment shape (CLI + watcher
daemon + library), 4-phase migration plan with parallel-run
retirement gates, 7-row risk register, composes-with cross-links.

Phase 0 next concrete work: PoC validating toolchain end-to-end
(2-3 ticks; F# project + NativeAOT publish + smoke test).

Header carries §33-style Scope/Attribution/Operational status/
Non-fusion disclaimer; check-archive-header-section33.ts passes.
Operational status: research-grade.
```
