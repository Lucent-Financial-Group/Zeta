# PR #1356 -- fix(#1349 post-merge): B-0142 migration concerns + scope clarification (4 review threads absorbed)

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 1356 |
| Title | fix(#1349 post-merge): B-0142 migration concerns + scope clarification (4 review threads absorbed) |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-05-03T08:11:11Z |
| Merged at | 2026-05-03T08:22:27Z |
| Merge commit SHA | `df908d57eadc123a53ab6073fd962b4d7bf3ef23` |
| Branch | `backlog/B-0142-followup-migration-concerns-warnings-as-errors` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/1356 |
| Changed files | 1 |
| Additions / deletions | +16 / -6 |

## Description

## Summary

Four substantive post-merge review findings on B-0142 absorbed via new \"Migration concerns + scope clarification\" section.

## Findings absorbed

1. **Existing \`invalidArg\` / exception-based code in \`src/Core/**\`** (line 24) — programmer-error preconditions use F# idiom (\`invalidArg\`); CLAUDE.md Result-over-exception applies to user-visible failures. **Scope refinement**: B-0142 targets recoverable user-facing failures (Result-flow); programmer-error preconditions stay as \`invalidArg\` until separate decision migrates them. **No blanket-ban exceptions.**

2. **Plain-returning APIs (\`bool\`, \`Stream\`, \`ValueTask\`)** (line 55) — contract primitives returning \`Result\` aren't drop-in. **Scope refinement**: two-mode primitive — \`requires_result()\` (Result-returning) vs \`requires_assert()\` (assertion-only).

3. **\`TreatWarningsAsErrors=true\` makes warning vs error a false dichotomy** (line 91). **Scope refinement**: contract-layer severity field; build-gate disposition is uniform (any reaching build = break); per-contract-class CI gating is separate future design question.

4. **Dangling memo reference** (line 75) — \`memory/parallelism-scaling-ladder memo\` replaced with full filename for grep-ability.

## Why this matters

These are all real compatibility concerns that affect future B-0142 implementation. The original row's spec admitted scope-creep that conflicted with existing F# idiom. Refining the scope keeps B-0142 viable as durable backlog substrate without forcing repo-wide signature churn.

## Test plan

- [x] markdownlint passes locally
- [x] New \"Migration concerns + scope clarification\" section preserves the original spec while narrowing scope
- [x] Open-design-questions updated with build-gate disposition (replacing warning-vs-error false dichotomy)
- [x] Dangling memo path replaced with full filename

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | true |
| Total threads | 3 |
| Resolved threads | 3 |
| Unresolved threads | 0 |
| Total review comments | 6 |
| Total fix commits (touching thread paths) | 2 |

## Review threads

### Thread 1 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5_LD2T`
- **Path**: `docs/backlog/P2/B-0142-code-contracts-design-by-contract-revival-aaron-2026-05-01.md`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-03T08:12:44Z on `docs/backlog/P2/B-0142-code-contracts-design-by-contract-revival-aaron-2026-05-01.md`:89 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Align exception policy with carved sentence**

  This scope clarification now says the row "does NOT blanket-ban exceptions" for programmer-error preconditions, but the same document’s carved sentence still states "NO throw / panic flow" as the row’s core contract. That leaves two incompatible implementation targets (allowing `invalidArg` vs banning throws), which makes future B-0142 design/review decisions non-deterministic depending on which section a reader follows. Please reconcile the policy in one place (for example, explicitly scope the no-throw rule to recoverable user-facing failures).

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-03T08:21:17Z on `docs/backlog/P2/B-0142-code-contracts-design-by-contract-revival-aaron-2026-05-01.md`:89 (association: MEMBER)

    <pre>
    Addressed in latest commit. Three reconciliations:

    1. Editorial note: explicit scope of "MUST flow as Result-error" — limited to recoverable-user-facing failures within Result-flow code paths. Programmer-error preconditions (invalidArg) are explicitly OUT OF SCOPE.

    2. Pre-condition primitive description: now distinguishes Result-flow (recoverable-user-facing) from assertion-only (programmer-error) two-mode variants.

    3. Carved sentence: same scope split made explicit ("for recoverable-user-facing-failure contract violations" + "Programmer-error preconditions ... stay as exceptions per F# idiom").

    4. Stream&lt;_&gt; → Stream (System.IO.Stream is non-generic).

    The two implementation targets (Result-flow vs invalidArg) are now reconciled by scope-tagging at every site: Result-flow rule applies WITHIN recoverable-user-facing-failure scope; invalidArg stays for programmer-error preconditions.
    </pre>

### Thread 2 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5_LD5z`
- **Path**: `docs/backlog/P2/B-0142-code-contracts-design-by-contract-revival-aaron-2026-05-01.md`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-03T08:13:02Z on `docs/backlog/P2/B-0142-code-contracts-design-by-contract-revival-aaron-2026-05-01.md`:90 (association: NONE)

  <pre>
  The new scope clarification allows `invalidArg` exceptions for programmer-error preconditions, but earlier in this doc the editorial note says B-0142 implementations "MUST" flow contract violations as Result errors and "NOT" throw exceptions. Please reconcile the wording so the doc consistently distinguishes programmer-error preconditions vs recoverable user-facing contract violations.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-03T08:21:19Z on `docs/backlog/P2/B-0142-code-contracts-design-by-contract-revival-aaron-2026-05-01.md`:90 (association: MEMBER)

    <pre>
    Addressed in latest commit. Three reconciliations:

    1. Editorial note: explicit scope of "MUST flow as Result-error" — limited to recoverable-user-facing failures within Result-flow code paths. Programmer-error preconditions (invalidArg) are explicitly OUT OF SCOPE.

    2. Pre-condition primitive description: now distinguishes Result-flow (recoverable-user-facing) from assertion-only (programmer-error) two-mode variants.

    3. Carved sentence: same scope split made explicit ("for recoverable-user-facing-failure contract violations" + "Programmer-error preconditions ... stay as exceptions per F# idiom").

    4. Stream&lt;_&gt; → Stream (System.IO.Stream is non-generic).

    The two implementation targets (Result-flow vs invalidArg) are now reconciled by scope-tagging at every site: Result-flow rule applies WITHIN recoverable-user-facing-failure scope; invalidArg stays for programmer-error preconditions.
    </pre>

### Thread 3 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5_LD55`
- **Path**: `docs/backlog/P2/B-0142-code-contracts-design-by-contract-revival-aaron-2026-05-01.md`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-03T08:13:02Z on `docs/backlog/P2/B-0142-code-contracts-design-by-contract-revival-aaron-2026-05-01.md`:91 (association: NONE)

  <pre>
  `Stream&lt;_&gt;` is not a valid/idiomatic type name for `System.IO.Stream` (which is non-generic). Consider changing this example to `Stream` (or `Stream`/`StreamReader`/etc. as appropriate) so readers don’t infer a generic stream type in `src/Core/**`.

  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-03T08:21:20Z on `docs/backlog/P2/B-0142-code-contracts-design-by-contract-revival-aaron-2026-05-01.md`:91 (association: MEMBER)

    <pre>
    Addressed in latest commit. Three reconciliations:

    1. Editorial note: explicit scope of "MUST flow as Result-error" — limited to recoverable-user-facing failures within Result-flow code paths. Programmer-error preconditions (invalidArg) are explicitly OUT OF SCOPE.

    2. Pre-condition primitive description: now distinguishes Result-flow (recoverable-user-facing) from assertion-only (programmer-error) two-mode variants.

    3. Carved sentence: same scope split made explicit ("for recoverable-user-facing-failure contract violations" + "Programmer-error preconditions ... stay as exceptions per F# idiom").

    4. Stream&lt;_&gt; → Stream (System.IO.Stream is non-generic).

    The two implementation targets (Result-flow vs invalidArg) are now reconciled by scope-tagging at every site: Result-flow rule applies WITHIN recoverable-user-facing-failure scope; invalidArg stays for programmer-error preconditions.
    </pre>

## Fix commits (touching thread paths)

### `849ee4edd9b95f6fd20f909714d7260f724edb30` -- 2026-05-03T08:10:54Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `docs/backlog/P2/B-0142-code-contracts-design-by-contract-revival-aaron-2026-05-01.md`

**Message:**

```
fix(#1349 post-merge): B-0142 migration concerns + scope clarificatio…

…n (4 review threads absorbed)

Four substantive post-merge review findings on B-0142:

1. Existing invalidArg/exception-based code in src/Core/** —
   programmer-error preconditions historically use exceptions in F#
   idiom; CLAUDE.md Result-over-exception applies to user-visible
   failures. Scope refinement: B-0142 targets recoverable user-facing
   failures (Result-flow); programmer-error preconditions stay as
   invalidArg until a separate decision migrates them. NO blanket-ban
   exceptions.

2. Plain-returning APIs (bool/Stream/ValueTask) — contract primitives
   returning Result aren't drop-in. Scope refinement: two-mode
   primitive — requires_result() (Result-returning) vs requires_assert()
   (assertion-only).

3. TreatWarningsAsErrors=true makes warning vs error a false dichotomy.
   Scope refinement: contract-layer severity field; build-gate
   disposition is uniform (any reaching build = break); per-contract-
   class CI gating is separate future design question.

4. Dangling reference: memory/parallelism-scaling-ladder memo replaced
   with full filename for grep-ability.

New "Migration concerns + scope clarification" section absorbs the
findings. Open-design-questions updated with build-gate disposition
question replacing the warning-vs-error false dichotomy.
```

### `692deca80f2a18ceda20004e54cb80dcd065eb81` -- 2026-05-03T08:21:15Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `docs/backlog/P2/B-0142-code-contracts-design-by-contract-revival-aaron-2026-05-01.md`

**Message:**

```
fix(#1356 review): B-0142 internal-consistency reconciliation — scope…

… no-throw rule explicitly to recoverable-user-facing-failures + Stream type precision
```
