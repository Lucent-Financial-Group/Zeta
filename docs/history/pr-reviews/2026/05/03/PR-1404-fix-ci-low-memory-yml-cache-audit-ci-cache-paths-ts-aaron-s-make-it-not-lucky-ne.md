# PR #1404 -- fix(ci): low-memory.yml cache + audit-ci-cache-paths.ts (Aaron's "make it not lucky next time")

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 1404 |
| Title | fix(ci): low-memory.yml cache + audit-ci-cache-paths.ts (Aaron's "make it not lucky next time") |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-05-03T13:08:19Z |
| Merged at | 2026-05-03T13:19:25Z |
| Merge commit SHA | `45baaa020d3efac08c732f4471ec676fd542ea98` |
| Branch | `fix/cache-clobber-low-memory-plus-audit-tool` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/1404 |
| Changed files | 2 |
| Additions / deletions | +397 / -3 |

## Description

## Summary

Two parts addressing Aaron's question on the #1403 cache-clobber discovery: *"lucky catch how can you make it not lucky next time for same class or similar class"*

## Part 1 — `.github/workflows/low-memory.yml` cache fix

Same broad-path bug as gate.yml (fixed in #1403). Path narrowed to specific jar files only; key bumped to `-v2`.

## Part 2 — `tools/hygiene/audit-ci-cache-paths.ts` (the not-lucky mechanism)

Structural CI-on-CI audit that flags any `actions/cache` `path:` overlapping with `git ls-files`. Discipline rule:

> **actions/cache paths are mutually exclusive with `git ls-files` — cache only DERIVED files (downloaded jars, built artefacts, user-home tool state), never source-controlled content.**

- Parses both single-line and multi-line `path:` forms
- Allowlists user-home paths
- Reports per-violation: workflow + step + cache path + example tracked file
- Includes actionable "Why this is a bug" + "How to fix" output

## Local verification

Currently flags gate.yml's `tools/tla` + `tools/alloy` paths (will return clean once #1403 merges + this PR lands).

## Follow-up (not in this PR)

- CI lint-job wiring (after #1403 lands so audit baseline is green)
- Memory file capturing the full cache-paths-vs-git-ls-files discipline

## Test plan

- [x] `bun tools/hygiene/audit-ci-cache-paths.ts` runs cleanly + correctly identifies violations
- [x] §33 archive-header lint passes
- [x] low-memory.yml fix mirrors gate.yml fix

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | true |
| Total threads | 15 |
| Resolved threads | 9 |
| Unresolved threads | 6 |
| Total review comments | 15 |
| Total fix commits (touching thread paths) | 3 |

## Review threads

### Thread 1 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5_MDLt`
- **Path**: `tools/hygiene/audit-ci-cache-paths.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-03T13:10:29Z on `tools/hygiene/audit-ci-cache-paths.ts`:145 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Do not exempt absolute cache paths from overlap checks**

  The audit currently skips every cache path that starts with `/`, but `actions/cache` allows absolute paths and those can still point into the checked-out workspace (for example via `${{ github.workspace }}` expansion). In that case a workflow can cache tracked files and silently overwrite PR content on restore, yet this auditor reports clean, which defeats the exact safety check this script is intended to enforce.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 2 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5_MDLu`
- **Path**: `tools/hygiene/audit-ci-cache-paths.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-03T13:10:29Z on `tools/hygiene/audit-ci-cache-paths.ts`:274 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Expand glob cache paths before overlap detection**

  The overlap check only does exact/prefix string comparisons, so cache paths containing wildcards (which `actions/cache` supports) are never matched against tracked files. A pattern like `tools/**` or `**/*.jar` can include tracked content but will pass this audit as non-overlapping, creating a false negative for the class of cache-clobber regressions this guard is meant to catch.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 3 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5_MDLw`
- **Path**: `tools/hygiene/audit-ci-cache-paths.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-03T13:10:29Z on `tools/hygiene/audit-ci-cache-paths.ts`:315 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Return failure when a workflow file cannot be read**

  The script documents exit code `2` for workflow read failures, but unreadable workflow files are currently logged and then skipped. If one workflow cannot be parsed, the run can still exit `0` with an `OK` summary, which masks partial-audit coverage and can let violating cache paths slip through undetected in CI.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 4 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5_MDzj`
- **Path**: `tools/hygiene/audit-ci-cache-paths.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-03T13:13:11Z on `tools/hygiene/audit-ci-cache-paths.ts`:31 (association: NONE)

  <pre>
  The header comment says the tool will “expand globs and check against `git ls-files`”, but the implementation only does exact and directory-prefix checks and doesn’t interpret glob patterns (e.g., `tools/**` or `tools/tla/*.jar`). Either implement glob matching/expansion (so the audit can’t be bypassed by switching to a globbed cache path) or update the documentation to match current behavior.

  </pre>

### Thread 5 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5_MDzp`
- **Path**: `tools/hygiene/audit-ci-cache-paths.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-03T13:13:11Z on `tools/hygiene/audit-ci-cache-paths.ts`:172 (association: NONE)

  <pre>
  Inside `parseCachePaths()`, `const content = ...` on this line shadows the outer `content` parameter, which makes the function harder to read and reason about. Renaming the inner variable (e.g., `trimmed`/`entry`) avoids accidental confusion during future edits to the parser.

  </pre>

### Thread 6 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5_MDzy`
- **Path**: `.github/workflows/low-memory.yml`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-03T13:13:11Z on `.github/workflows/low-memory.yml`:128 (association: NONE)

  <pre>
  This comment says “See gate.yml comment for the full failure-mode write-up”, but `.github/workflows/gate.yml` currently doesn’t contain that write-up near its verifier-jar cache step (so this reference is stale/nonexistent). Please either include the key details here, or link to the specific PR/issue that documents the failure mode (e.g., #1403), so readers aren’t sent to a dead reference.

  </pre>

### Thread 7 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5_MDz6`
- **Path**: `tools/hygiene/audit-ci-cache-paths.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-03T13:13:12Z on `tools/hygiene/audit-ci-cache-paths.ts`:316 (association: NONE)

  <pre>
  The header comment says exit code 2 is used for “workflow file or git read failure”, but `main()` currently just logs and continues when a workflow file can’t be read. That can lead to exit 0/1 even though the audit was incomplete. Track a read-failure flag and return 2 if any workflow read fails so CI doesn’t silently accept a partial audit.
  </pre>

### Thread 8 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5_MD0A`
- **Path**: `tools/hygiene/audit-ci-cache-paths.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-03T13:13:12Z on `tools/hygiene/audit-ci-cache-paths.ts`:92 (association: NONE)

  <pre>
  `isUserHomePath()` currently returns true for any path starting with `/`, which effectively allowlists all absolute paths. That can mask the exact class of bug this audit is meant to catch if a workflow uses an absolute path into the workspace (e.g., `/home/runner/work/&lt;repo&gt;/&lt;repo&gt;/tools/tla`). Consider either (a) only allowlisting `~/` (and other known-non-workspace prefixes), or (b) normalizing `${{ github.workspace }}`/common workspace absolute prefixes back to repo-relative paths and still checking them against `git ls-files`.

  </pre>

### Thread 9 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5_MD0D`
- **Path**: `tools/hygiene/audit-ci-cache-paths.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-03T13:13:12Z on `tools/hygiene/audit-ci-cache-paths.ts`:275 (association: NONE)

  <pre>
  `overlapsTrackedFile()` normalizes `./` to `""`, which means cache paths like `"./"` (and also `"."`) will not be flagged even though they overlap the entire repository. This creates a high-impact false-negative for the audit. Treat `""`/`"."` (and maybe `"./"`) as the repo root and immediately report an overlap (e.g., return the first tracked file) so a cache path that points at the workspace cannot slip through.
  </pre>

### Thread 10 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM5_MFTP`
- **Path**: `tools/hygiene/audit-ci-cache-paths.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-03T13:19:25Z on `tools/hygiene/audit-ci-cache-paths.ts`:274 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Normalize absolute cache paths before overlap checks**

  Fresh evidence in this revision: although absolute paths are no longer skipped, `overlapsTrackedFile` still compares unmodified absolute cache paths against `git ls-files` entries, which are repo-relative, so a path like `/home/runner/work/&lt;repo&gt;/&lt;repo&gt;/tools/tla` can never match and is reported clean. That creates a false negative for the same cache-clobber class this audit is meant to block whenever workflows use absolute cache paths into the workspace.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 11 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM5_MFTR`
- **Path**: `tools/hygiene/audit-ci-cache-paths.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-03T13:19:25Z on `tools/hygiene/audit-ci-cache-paths.ts`:174 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Match cache/restore and cache/save actions in parser**

  The detector only enters cache parsing for `uses: actions/cache@...`, so steps using `actions/cache/restore@...` (which also restores files to `path`) are ignored entirely. GitHub’s cache action docs include restore/save forms, and a restore step can overwrite checked-out tracked files in exactly the same way, so this omission leaves a blind spot where unsafe cache paths pass the audit.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 12 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM5_MFTS`
- **Path**: `tools/hygiene/audit-ci-cache-paths.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-03T13:19:25Z on `tools/hygiene/audit-ci-cache-paths.ts`:120 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Preserve globstar semantics for `**/` when matching paths**

  `globToRegex` translates `**` to `.*`, which makes patterns like `**/*.md` require at least one directory separator and therefore miss repo-root files (e.g. `README.md`). Since `actions/cache` path patterns use `@actions/glob` semantics where `**` can match zero path segments, this causes false negatives for globbed cache paths that include tracked files at the repository root.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 13 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM5_MFZX`
- **Path**: `tools/hygiene/audit-ci-cache-paths.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-03T13:19:45Z on `tools/hygiene/audit-ci-cache-paths.ts`:35 (association: NONE)

  <pre>
  P2 (docs): The top-of-file Allowlist comment says paths starting with `/` are allowlisted, but `isUserHomePath` explicitly says absolute `/...` paths are *not* auto-exempt (and the implementation only exempts `~...`). Please reconcile the comments so future edits don’t reintroduce the “absolute path is always safe” assumption.

  </pre>

### Thread 14 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM5_MFZo`
- **Path**: `tools/hygiene/audit-ci-cache-paths.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-03T13:19:46Z on `tools/hygiene/audit-ci-cache-paths.ts`:98 (association: NONE)

  <pre>
  P1: `isUserHomePath` claims only `~/...` should be exempt, but the implementation returns `path.startsWith("~")` which would also exempt paths like `~foo/bar`. Tighten the check to `~/` (or update the comment + rationale) to avoid false negatives in the audit.

  </pre>

### Thread 15 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM5_MFZw`
- **Path**: `tools/hygiene/audit-ci-cache-paths.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-03T13:19:46Z on `tools/hygiene/audit-ci-cache-paths.ts`:85 (association: NONE)

  <pre>
  P2: On `git ls-files` failure, the error message prints only the exit status. When `spawnSync` fails to execute (e.g., `git` missing), `status` can be null and the useful details are in `result.error` / `result.stderr`. Including those would make the exit-2 diagnostics actionable.

  </pre>

## Fix commits (touching thread paths)

### `08e1baf40aec345075cd93b7c159eaafdc67cc01` -- 2026-05-03T13:08:06Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `.github/workflows/low-memory.yml`
- `tools/hygiene/audit-ci-cache-paths.ts`

**Message:**

```
fix(ci): low-memory.yml cache fix + structural audit (`audit-ci-cache…

…-paths.ts`) — Aaron's "make it not lucky next time"

**Two parts** addressing the silent CI cache-clobber bug discovered
via #1401's CircuitRegistration test failure:

## Part 1 — `.github/workflows/low-memory.yml` cache fix

Same broad-path bug as gate.yml (#1403 fixes that one): cache path
`tools/tla` + `tools/alloy` overwrites source-controlled spec files
on cache hit. Narrowed to specific jar files only; bumped key suffix
to `-v2` to bust stale cache.

Without this fix, low-memory.yml would have the same silent
clobber bug as gate.yml had pre-#1403.

## Part 2 — `tools/hygiene/audit-ci-cache-paths.ts` (the not-lucky mechanism)

Aaron 2026-05-03: *"lucky catch how can you make it not lucky next
time for same class or similar class"*

The structural answer: a CI-on-CI audit that flags any `actions/
cache` `path:` in `.github/workflows/*.yml` overlapping with `git
ls-files`. The discipline rule:

  *"actions/cache paths are mutually exclusive with git ls-files —
   cache only DERIVED files (downloaded jars, built artefacts,
   user-home tool state), never source-controlled content."*

Implementation:

- Walks `.github/workflows/*.yml` parsing `path:` blocks (handles
  both single-line `path: foo` and multi-line `path: |` forms)
- For each path, checks if it equals or contains a `git ls-files`
  entry
- Allowlists user-home paths (`~/...`, `/...`) — never tracked
- Reports per-violation: workflow + step name + cache path +
  example tracked file
- Includes "Why this is a bug" + "How to fix" output for actionable
  CI failures

**Local verification:** runs against current main (pre-#1403 merge);
correctly flags gate.yml's `tools/tla` + `tools/alloy` paths as
violations (2). Once #1403 merges + this PR's low-memory.yml fix
lands, the audit returns clean.

## Not-yet-done (follow-up):

- Wire the audit into CI as a lint job (after #1403 merges so the
  audit returns clean as a baseline)
- Memory file capturing the full discipline (cache-paths-mutually-
  exclusive-with-git-ls-files)

This PR ships the *tool*; the CI wire-up is a deliberate follow-up
PR so the lint-job-add can land cleanly on a green baseline.

Composes with:
- #1401 (CircuitRegistration Safety fix that surfaced this)
- #1403 (gate.yml cache fix; sibling of low-memory.yml fix here)
- #1383 math-proofs honest assessment (test-fidelity is fundamental
  to A/B/C grading)
```

### `691aaae37a53a00715bc2290fc1cb3326207d232` -- 2026-05-03T13:14:32Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/hygiene/audit-ci-cache-paths.ts`

**Message:**

```
fix(audit-ci-cache-paths): address 3 review findings on #1404 — absol…

…ute paths + glob expansion + read-failure exit

Three #1404 reviewer findings addressed:

**P1 — Don't exempt absolute paths.** Original code skipped any
path starting with `/` as "user-home equivalent". But actions/cache
absolute paths can point INTO the checked-out workspace (e.g.
`/home/runner/work/<repo>/<repo>/...`) — same clobber failure mode
as relative paths. Now `isUserHomePath` returns true only for
`~/...`. Comment added at the function explaining the rationale.

**P2 — Expand globs before overlap detection.** actions/cache paths
can contain wildcards (`*`, `**`, `?`); the original implementation
only did exact/prefix string matching, so a glob path like
`tools/tla/specs/*.tla` would be considered non-overlapping with
any tracked file. Added `globToRegex()` (* matches non-`/`, **
matches anything including `/`, ? matches single non-`/` char,
escapes regex meta-chars) and `pathHasGlob()`; `overlapsTrackedFile`
now branches on glob-vs-literal and matches accordingly.

**P2 — Return exit 2 on workflow read failure.** Documented exit
codes promised exit 2 on workflow read failure but the code logged
+ skipped + still reported "OK" if no other violations. That's
exactly the false-pass class the audit was designed to catch.
Added `readFailures` counter; returns exit 2 with explicit error
message before the violation summary.

**Verification post-fix:**
  - `bun tools/hygiene/audit-ci-cache-paths.ts` → "OK: 12 workflow(s)
    audited; no cache paths overlap git-tracked files" (returns
    clean now that #1403 + this PR's low-memory fix have rebased
    on top of merged main)
  - All 3 reviewer findings addressed within the same audit tool

Composes with:
- #1403 (gate.yml cache fix; merged)
- #1404 (this PR; low-memory.yml + audit-tool)
- The "not-lucky-next-time" structural answer to Aaron's question
```

### `89ef9b5e80884b6be562205823b56cdbc5e6386d` -- 2026-05-03T13:16:18Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/hygiene/audit-ci-cache-paths.ts`

**Message:**

```
fix(audit-ci-cache-paths): address 3 more #1404 review findings — con…

…tent-shadow rename + repo-root cache-path edge

Three additional reviewer findings:

**Variable shadowing in parseCachePaths.** Inner
shadowed the outer  parameter (the workflow YAML text).
Renamed inner to  (the per-line trimmed value) for
clarity.

**Repo-root cache-path false-pass.** Cache paths like `.` or `./`
normalised to `""` and silently false-passed (no tracked file
equals "" or starts with "/"). High-impact bug class — a
workflow caching the entire repo root would not be flagged. Added
explicit repo-root branch: if cp is "" or "." after
normalisation, return the first tracked file (overlap is 100%).

**Header-comment claim about glob expansion.** Already addressed
in the prior commit (added globToRegex + pathHasGlob); this thread
duplicates the P2 finding.

Local verification: audit returns clean (12 workflows audited;
no overlap).
```
