# PR #5069 -- feat(claude-code-recovery): repair JSONL sessions corrupted by oversize images

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 5069 |
| Title | feat(claude-code-recovery): repair JSONL sessions corrupted by oversize images |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-05-26T02:21:27Z |
| Merged at | 2026-05-26T02:36:35Z |
| Merge commit SHA | `51aac98f55a714617d2baaa4d83d6eb358f189ee` |
| Branch | `otto-cli/claude-code-jsonl-recovery-script-2026-05-25` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/5069 |
| Changed files | 3 |
| Additions / deletions | +731 / -0 |

## Description

## Summary

Repair Claude Code sessions that won't reopen because a pasted image
ballooned a single JSONL line past the harness session-load limit.

Born from a live incident: session `c2b77530` ("Assemble declarative
infrastructure files for Zeta") had a 13.4 MB line at 15230 from a
9.8 MB PNG attachment — wouldn't reopen, hours of work at risk. The
recovery procedure is mechanical but easy to get wrong; this lands
it as a reusable tool + skill so the next operator hit doesn't lose
substrate.

## What lands

- `tools/claude-code-recovery/repair-jsonl-strip-images.ts` — Bun-hosted
  TypeScript, dry-run by default, refuses to write if any post-edit
  line fails to parse
- `tools/claude-code-recovery/README.md` — usage + the why
- `.claude/skills/claude-session-recovery/SKILL.md` — triggers on
  "session won't open", "image too large", "edit out the image", etc.

## Per-image vs per-line threshold

The script splits two thresholds intentionally:

- `--max-line-bytes` (default 10 MB) — which JSONL lines to inspect
- `--max-image-bytes` (default = `--max-line-bytes`) — which individual
  images on those lines to strip

**Small images on an oversize line are preserved.** Only images whose
base64 length individually exceeds `--max-image-bytes` are removed.
This keeps the strip surgical: if a corrupted turn contained a few
small thumbnails plus one 13 MB screenshot, only the screenshot is
removed.

## Operator-runs discipline

The Claude Code auto-mode classifier blocks the agent from running
`--apply` directly. This is correct per
`.claude/rules/classifier-bypass-research-do-not-deploy-without-zeta-safer-floor.md`
— `.jsonl` edits are self-modification territory.

The skill walks the agent through scan + dry-run + handing `--apply`
to the operator. The script's built-in backup
(`<file>.bak-YYYY-MM-DD-<epoch>`) makes the operation reversible.
No `.claude/settings.json` `_*_acceptance` block is needed for
one-off recoveries — the operator-runs split keeps authority anchored
to the operator without ceremonial scaffolding.

## Test plan

- [x] `--scan` correctly finds c2b77530, only flags lines above the threshold
- [x] Dry-run on c2b77530 with default thresholds: identifies the 13 MB image, would free 13.1 MB (53 MB → 40 MB)
- [x] Per-image threshold discriminator: raising `--max-image-bytes 20000000` correctly preserves the 13 MB image (proves small images would survive)
- [x] `--help` output shows the new flag
- [ ] (operator) Run `--apply` against c2b77530 to actually recover the session
- [ ] (operator) Confirm session reopens in Claude Code after the strip
- [ ] Skill activates correctly on future trigger phrases ("session won't open" / "image too large" / etc.)

## Composes with

- `.claude/rules/classifier-bypass-research-do-not-deploy-without-zeta-safer-floor.md` — the operator-runs gate that mandates the agent/operator split
- `.claude/rules/human-audit-and-legal-risk-acceptance-pattern-in-settings.md` — the four-field attribution scaffold (would apply if this becomes recurring enough to want automation past operator-runs)
- User-scope memory file `feedback_claude_code_session_jsonl_oversize_image_self_heal_recovery_pattern_classifier_blocks_claude_operator_runs_2026_05_25.md` (the self-heal pattern + the c2b77530 anchor)

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | true |
| Total threads | 16 |
| Resolved threads | 16 |
| Unresolved threads | 0 |
| Total review comments | 16 |
| Total fix commits (touching thread paths) | 5 |

## Review threads

### Thread 1 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6EqgAE`
- **Path**: `tools/claude-code-recovery/repair-jsonl-strip-images.ts`
- **Replies**: 0

**Initial comment:**

- **`github-advanced-security[bot]` (bot)** at 2026-05-26T02:23:41Z on `tools/claude-code-recovery/repair-jsonl-strip-images.ts`:298 (association: NONE)

  <pre>
  ## CodeQL / Potential file system race condition

  The file may have changed since it [was checked](1).

  [Show more details](https://github.com/Lucent-Financial-Group/Zeta/security/code-scanning/93)
  </pre>

### Thread 2 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6EqgAJ`
- **Path**: `tools/claude-code-recovery/repair-jsonl-strip-images.ts`
- **Replies**: 0

**Initial comment:**

- **`github-advanced-security[bot]` (bot)** at 2026-05-26T02:23:42Z on `tools/claude-code-recovery/repair-jsonl-strip-images.ts`:373 (association: NONE)

  <pre>
  ## CodeQL / Potential file system race condition

  The file may have changed since it [was checked](1).
  The file may have changed since it [was checked](2).

  [Show more details](https://github.com/Lucent-Financial-Group/Zeta/security/code-scanning/94)
  </pre>

### Thread 3 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6Eqgu6`
- **Path**: `tools/claude-code-recovery/repair-jsonl-strip-images.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-26T02:24:47Z on `tools/claude-code-recovery/repair-jsonl-strip-images.ts`:50 (association: NONE)

  <pre>
  This tool hard-codes a maintainer-specific Claude project slug (and embeds a user handle) as DEFAULT_SLUG. That makes the default behavior fail for any operator whose repo path differs, and it leaks machine-specific identity into a reusable tool. Prefer deriving the slug from the current repo absolute path (Claude’s slugging rule) or require --slug explicitly and print a clear error showing how to find the correct slug.
  </pre>

### Thread 4 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6EqgvU`
- **Path**: `tools/claude-code-recovery/repair-jsonl-strip-images.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-26T02:24:47Z on `tools/claude-code-recovery/repair-jsonl-strip-images.ts`:90 (association: NONE)

  <pre>
  Argument parsing accepts flags without values (e.g., "--slug" at end) and then proceeds with undefined/NaN values (Number(undefined) =&gt; NaN), which will cause confusing behavior later. Add validation that every value-taking flag has a following non-flag token, and reject invalid numbers; also consider allowing numeric underscores by normalizing (remove '_' / ',' before Number) since the help/comments show underscore-separated literals.
  </pre>

### Thread 5 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6Eqgvd`
- **Path**: `tools/claude-code-recovery/repair-jsonl-strip-images.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-26T02:24:48Z on `tools/claude-code-recovery/repair-jsonl-strip-images.ts`:312 (association: NONE)

  <pre>
  Repair mode inspects lines with length &gt;= --max-line-bytes, but scan mode flags only lines with length &gt; --max-line-bytes. This threshold mismatch means a line exactly at the limit will be processed in repair but never appear in scan output. Make the comparison consistent (either both '&gt;' or both '&gt;=' / '&lt;=').

  </pre>

### Thread 6 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6Eqgvt`
- **Path**: `tools/claude-code-recovery/repair-jsonl-strip-images.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-26T02:24:48Z on `tools/claude-code-recovery/repair-jsonl-strip-images.ts`:385 (association: NONE)

  <pre>
  Most tools/ scripts in this repo export a main(argv) and only call process.exit when run as the entrypoint (if (import.meta.main)). This file unconditionally calls main() and exits from inside helpers, which makes it harder to import/reuse/test and diverges from established tooling structure. Refactor to export main(argv) -&gt; exit code, and guard the process.exit call behind import.meta.main.

  </pre>

### Thread 7 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6Eqgv6`
- **Path**: `.claude/skills/claude-session-recovery/SKILL.md`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-26T02:24:48Z on `.claude/skills/claude-session-recovery/SKILL.md`:7 (association: NONE)

  <pre>
  Skill frontmatter description is far above the repo’s carved-sentence guidance (one sentence, &lt;120 chars preferred, &lt;150 target) and risks being truncated/dropped by the skill router budget. Carve description down to a single routing-quality sentence and move procedural detail into the skill body.

  </pre>

### Thread 8 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6EqgwL`
- **Path**: `.claude/skills/claude-session-recovery/SKILL.md`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-26T02:24:48Z on `.claude/skills/claude-session-recovery/SKILL.md`:34 (association: NONE)

  <pre>
  This grep example hard-codes a maintainer-specific project slug; on any other machine the slug will differ and the command will miss sessions. Use a &lt;slug&gt; placeholder (or instruct deriving the slug from the repo path) so the skill remains usable for other operators.

  </pre>

### Thread 9 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6EqgwT`
- **Path**: `tools/claude-code-recovery/README.md`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-26T02:24:48Z on `tools/claude-code-recovery/README.md`:50 (association: NONE)

  <pre>
  The README documents a specific operator’s slug as the default. Since Claude’s project slug depends on the operator’s absolute repo path, this default will be wrong for most users. Prefer documenting the slug as "&lt;slug derived from your repo path&gt;" (or explain how to compute/find it), and consider removing the hard-coded default from the tool so docs and behavior stay consistent.

  </pre>

### Thread 10 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6EqjUj`
- **Path**: `tools/claude-code-recovery/repair-jsonl-strip-images.ts`
- **Replies**: 0

**Initial comment:**

- **`github-advanced-security[bot]` (bot)** at 2026-05-26T02:29:59Z on `tools/claude-code-recovery/repair-jsonl-strip-images.ts`:327 (association: NONE)

  <pre>
  ## CodeQL / Potential file system race condition

  The file may have changed since it [was checked](1).

  [Show more details](https://github.com/Lucent-Financial-Group/Zeta/security/code-scanning/95)
  </pre>

### Thread 11 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6Eqj1X`
- **Path**: `tools/claude-code-recovery/repair-jsonl-strip-images.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-26T02:31:06Z on `tools/claude-code-recovery/repair-jsonl-strip-images.ts`:352 (association: NONE)

  <pre>
  The `--max-line-bytes` / `--max-image-bytes` flags are documented as byte thresholds, but this code compares against `ln.length` (UTF-16 code units) rather than the UTF-8 byte length. For non-ASCII content, this will misclassify lines near the threshold and make `--scan` / `--session` inconsistent with the stated units. Use `Buffer.byteLength(ln, "utf8")` (and similarly when computing line lengths in `scanFile`).

  </pre>

### Thread 12 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6Eqj1p`
- **Path**: `tools/claude-code-recovery/repair-jsonl-strip-images.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-26T02:31:07Z on `tools/claude-code-recovery/repair-jsonl-strip-images.ts`:272 (association: NONE)

  <pre>
  `scanFile` computes per-line “bytes” using string indices from `readFileSync(path, "utf8")`, which are not byte counts for UTF-8. Since the tool is explicitly thresholding in bytes, read the file as a `Buffer` (no encoding) and count bytes by scanning for `0x0A`, or compute each line’s size via `Buffer.byteLength` on slices.

  </pre>

### Thread 13 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6Eqj11`
- **Path**: `tools/claude-code-recovery/repair-jsonl-strip-images.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-26T02:31:07Z on `tools/claude-code-recovery/repair-jsonl-strip-images.ts`:405 (association: NONE)

  <pre>
  `newContent.length` is used as “bytes” in the size delta log, but `.length` is characters (UTF-16 code units), while `sizeBefore` is bytes from `statSync`. Use `Buffer.byteLength(newContent, "utf8")` for accurate byte reporting (and to avoid negative/incorrect “saves …” output when non-ASCII appears).

  </pre>

### Thread 14 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6Eqj17`
- **Path**: `tools/claude-code-recovery/repair-jsonl-strip-images.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-26T02:31:07Z on `tools/claude-code-recovery/repair-jsonl-strip-images.ts`:429 (association: NONE)

  <pre>
  `--apply` writes in-place via `writeFileSync(path, newContent)`. If the process is interrupted mid-write (crash / out-of-disk), this can leave the session file truncated/corrupted (even though a backup exists). Prefer writing to a temp file in the same directory and `renameSync` to replace atomically (and preserve original file mode/ownership as needed).
  </pre>

### Thread 15 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6Eqj2C`
- **Path**: `tools/claude-code-recovery/repair-jsonl-strip-images.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-26T02:31:08Z on `tools/claude-code-recovery/repair-jsonl-strip-images.ts`:256 (association: NONE)

  <pre>
  `processLine` says it annotates “the first container that lost images”, but the current logic annotates `attachment.prompt` whenever it exists, even if the actual drops happened in `message.content` or top-level `content`. This can add a synthetic text block to an unrelated container and fail to annotate the container that was actually modified. Track which container(s) had drops (e.g., have `apply(...)` return a boolean / record the first modified container reference) and call `annotate` on that specific container.
  </pre>

### Thread 16 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6Eqj2J`
- **Path**: `.claude/skills/claude-session-recovery/SKILL.md`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-26T02:31:08Z on `.claude/skills/claude-session-recovery/SKILL.md`:3 (association: NONE)

  <pre>
  Skill frontmatter `description` is significantly longer than the repo guidance to carve skill descriptions to &lt;120 chars (see docs/ops/COST-REDUCTION-LESSONS.md:53-55 / 081KR50HA0008QG0R002ZNFQBZ). Consider shrinking this to a single carved sentence and move the trigger-phrase list into the body (it’s already present under “Specific trigger phrases”).

  </pre>

## Fix commits (touching thread paths)

### `2fe905ec7cfe6d97f77c58a94f3dd22ad9516046` -- 2026-05-26T02:21:13Z -- ``

**Touched files (intersect with thread paths):**

- `.claude/skills/claude-session-recovery/SKILL.md`
- `tools/claude-code-recovery/README.md`
- `tools/claude-code-recovery/repair-jsonl-strip-images.ts`

**Message:**

```
feat(claude-code-recovery): repair JSONL sessions corrupted by oversi…

…ze images

Pasted images that exceed the harness session-load limit (commonly ~10 MB
per JSONL line once base64-encoded) prevent Claude Code sessions from
reopening. The operator just lost a session this way (c2b77530, "Assemble
declarative infrastructure files for Zeta" — 13.4 MB line at 15230 from a
9.8 MB PNG). The recovery procedure is mechanical but easy to get wrong;
this lands it as a reusable tool + skill.

What lands:
- tools/claude-code-recovery/repair-jsonl-strip-images.ts (Bun, dry-run
  default, refuses to write if any post-edit line fails to parse)
- tools/claude-code-recovery/README.md
- .claude/skills/claude-session-recovery/SKILL.md

The script splits two thresholds: --max-line-bytes (which lines to
inspect, default 10 MB) and --max-image-bytes (which individual images
to strip, default = --max-line-bytes). Small images on an oversize line
are PRESERVED; only the oversize image(s) that pushed the line past the
harness limit are removed. Line UUIDs + parentUuids stay intact so
downstream messages keep a valid parent in the conversation graph; the
first text block in the affected container is annotated with the strip
metadata so the operation is traceable.

Operator-runs discipline: the Claude Code auto-mode classifier blocks
the agent from running --apply directly (correctly — .jsonl edits are
self-modification territory per the classifier-bypass-research rule).
The skill walks the agent through scan + dry-run + handing --apply to
the operator. No settings.json _*_acceptance block is needed for one-off
recoveries; the operator-runs split keeps authority anchored to the
operator without ceremonial scaffolding.

Tested via the skill against c2b77530 dry-run: identifies the 13 MB
image, would free 13.1 MB (53 MB → 40 MB), validates all post-edit
lines parse, refuses to apply if not. The per-image threshold
discriminator verified: raising --max-image-bytes to 20 MB correctly
preserves the 13 MB image.

Composes with .claude/rules/classifier-bypass-research-do-not-deploy-
without-zeta-safer-floor.md (the operator-runs gate); .claude/rules/
human-audit-and-legal-risk-acceptance-pattern-in-settings.md (the
attribution scaffold that would apply if this becomes recurring enough
to want automation past operator-runs).

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
```

### `5d61ced86fb0b19c46865a7f7eefb898af9be410` -- 2026-05-26T02:25:00Z -- ``

**Touched files (intersect with thread paths):**

- `tools/claude-code-recovery/repair-jsonl-strip-images.ts`

**Message:**

```
fix(claude-code-recovery): satisfy exactOptionalPropertyTypes + noUnc…

…heckedIndexedAccess

CI tsc-tools surfaced 3 strict-mode errors on the initial commit:
- Args.session was `?: string` which exactOptionalPropertyTypes treats
  as "may be omitted" not "may be undefined" — assigning the
  potentially-undefined value from get() was rejected. Fix: type as
  `string | undefined` (required-but-nullable).
- Two `lines[i]` / `out[i]` accesses returned `string | undefined`
  under noUncheckedIndexedAccess. Fix: `lines[i] ?? ""` and `out[i] ?? ""`
  — empty-string fallback is safe because the subsequent length and
  parse checks already handle "".

Behavior unchanged. Functional test re-run: dry-run on c2b77530 still
identifies the 13 MB image and reports 13.1 MB freed.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
```

### `4ea541350c0f3a97a385829993b29783862a6c1c` -- 2026-05-26T02:27:19Z -- ``

**Touched files (intersect with thread paths):**

- `.claude/skills/claude-session-recovery/SKILL.md`
- `tools/claude-code-recovery/README.md`
- `tools/claude-code-recovery/repair-jsonl-strip-images.ts`

**Message:**

```
fix(claude-code-recovery): address review threads — derive slug, argp…

…arse validation, threshold consistency, conventions

Addresses 7 actionable review findings on PR #5069 from Copilot +
CodeQL:

- Derive DEFAULT_SLUG from process.cwd() (Copilot #3, #8, #9): the
  hard-coded `-Users-acehack-Documents-src-repos-Zeta` would fail
  for any other operator. Now: `process.cwd().replace(/\//g, "-")`
  via deriveDefaultSlug(); --slug flag still overrides. Same change
  applied to the SKILL.md grep example (uses `$(pwd | tr '/' '-')`)
  and the README defaults table.

- Argparse validation (Copilot #4): get() now rejects flags with
  no following value (or a following --flag), and Number() coercion
  is gated through parseNonNegativeNumber() so NaN / negative values
  surface a clear error instead of silently propagating. Validated
  empirically: `--max-line-bytes` (no value) and `--max-line-bytes abc`
  both produce specific error messages now.

- Threshold consistency (Copilot #5): runScan flagged lines with
  length > maxLineBytes; runRepair inspected lines with length <
  maxLineBytes (i.e., a line exactly at the threshold went through
  repair but not scan). Now both use the same `> maxLineBytes`
  comparator — repair skips lines that are at-or-below the threshold.

- import.meta.main convention (Copilot #6): main() is now only auto-
  invoked when the script runs as entrypoint, matching the rest of
  tools/ and enabling import-for-testing.

- SKILL.md description carving (Copilot #7): was ~620 chars, well
  past the router-budget guidance of <150. Carved to 144 chars
  naming the trigger phrases only; procedural detail stays in the
  skill body.

- CodeQL CWE-367 TOCTOU (CodeQL #1, #2): dropped the redundant
  existsSync gate before readFileSync — readFileSync throws ENOENT
  directly, so the gate added a race window without any safety
  benefit. The remaining copy+write race (backup then mutate) is
  accepted-risk and annotated in-comment: this tool repairs the
  operator's own ~/.claude/projects/ files, which they are not
  concurrently editing, and the backup makes the operation
  reversible.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
```

### `66f338fd5094f35ea51107493073ce0584ee8690` -- 2026-05-26T02:31:32Z -- ``

**Touched files (intersect with thread paths):**

- `tools/claude-code-recovery/repair-jsonl-strip-images.ts`

**Message:**

```
fix(claude-code-recovery): eliminate CWE-367 TOCTOU surfaces via sing…

…le buffered read

The in-code accept-the-risk comments don't suppress CodeQL — it
re-fired on the new commit at lines 327 + 417 (same race-condition
class). Refactored to eliminate the check-then-act patterns:

- runRepair: single readFileSync (Buffer) replaces statSync +
  readFileSync. sizeBefore comes from buf.length instead of a
  separate stat. Removes the stat→read TOCTOU window.

- Backup write: writeFileSync(backup, buf) reuses the in-memory
  buffer rather than copyFileSync(path, backup), which would have
  been a second read of `path` after our initial read (CWE-367).
  Now there is no check-then-act pattern at all on the target
  path: read once, write backup from memory, write new content.

- Dropped unused copyFileSync import.

Behavior unchanged. Dry-run on c2b77530 still identifies the
13 MB image and reports 13.1 MB freed (same byte counts pre/post
refactor).

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
```

### `e1f9f29bf760c715a8887d941c3e4686d9d5f0e8` -- 2026-05-26T02:33:25Z -- ``

**Touched files (intersect with thread paths):**

- `.claude/skills/claude-session-recovery/SKILL.md`
- `tools/claude-code-recovery/repair-jsonl-strip-images.ts`

**Message:**

```
fix(claude-code-recovery): UTF-8 byte counting + atomic write + annot…

…ate-correct-container + carve SKILL desc

Addresses 6 new review findings on PR #5069 (Copilot, third pass):

- UTF-8 byte counting (#1, #2, #3): the script's thresholds are
  documented as bytes, but the comparators were using `.length`
  (UTF-16 code units) and `charCodeAt` on a decoded string. For
  multi-byte content (any non-ASCII), this under-counted bytes and
  could misclassify lines near the threshold. Fixed three sites:
  - scanFile now reads raw Buffer (no encoding) and counts bytes
    directly via index iteration looking for 0x0a
  - runRepair's per-line check uses Buffer.byteLength(ln, "utf8")
  - The "size: X → Y" log uses Buffer.byteLength on newContent
  Empirical: c2b77530 size delta is now 13,415,556 (accurate UTF-8
  bytes) vs prior 13,494,764 (UTF-16 code units) — small but real
  correction.

- Atomic in-place write (#4): writeFileSync(path, newContent) was
  non-atomic — a crash mid-write would leave the session JSONL
  truncated. Now: write to {path}.tmp-{epoch}, renameSync to path.
  POSIX rename is atomic; crash leaves either old file or new file
  intact. Backup write (separate file, no race) stays as-is.

- Annotate-correct-container (#5): processLine used to annotate
  att.prompt whenever it existed, even when the actual drops
  happened in msg.content or top-level content. The synthetic text
  block landed on the wrong surface, and the modified container
  stayed untraceable. Now: apply() tracks `firstModified` and the
  annotation lands on the container that actually lost images.

- SKILL.md description carving (#6): was 158 chars, target was
  <120 per the carved-sentence guidance (081KR50HA0008QG0R002ZNFQBZ /
  docs/ops/COST-REDUCTION-LESSONS.md). Now 110 chars; trigger
  phrases stay in the body's "Specific trigger phrases" section.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
```
