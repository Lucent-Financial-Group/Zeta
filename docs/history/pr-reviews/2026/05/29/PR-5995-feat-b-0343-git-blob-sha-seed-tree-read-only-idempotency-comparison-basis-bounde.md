# PR #5995 -- feat(081KR2E4K0008QG0R002JW751Y): git-blob-SHA seed tree — read-only idempotency comparison basis (bounded slice 3)

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 5995 |
| Title | feat(081KR2E4K0008QG0R002JW751Y): git-blob-SHA seed tree — read-only idempotency comparison basis (bounded slice 3) |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-05-29T14:01:06Z |
| Merged at | 2026-05-29T14:13:31Z |
| Merge commit SHA | `7c33c19f6032696286b5d273c4d722d9cfd94702` |
| Branch | `otto-cli/b0343-seed-blob-sha-idempotency-prereq-2026-05-29` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/5995 |
| Changed files | 2 |
| Additions / deletions | +93 / -4 |

## Description

## What

Bounded **slice 3** of 081KR2E4K0008QG0R002JW751Y (test-repo seeding script). Adds the content-addressable representation of the resolved seed set — each resolved file paired with its **git blob SHA** — as the read-only prerequisite for AC 3 (idempotency).

Builds on the merged manifest-reader + `--dry-run` + glob-resolution slices (PRs #2716 / #2722 / #2723 / #5992).

## Why this slice / why now

The script's own slice-2 comment names the prerequisite: *"idempotency = compare the resolved set against the target repo"*. A git blob SHA is `sha1("blob " + byteLength + "\0" + content)` — byte-identical to `git hash-object` and to the `sha` GitHub returns in its git-data/tree + contents API responses. Computing these locally turns AC 3's "is the repo already seeded with exactly these files?" into a **pure `{path, sha}` set-equality check with zero remote-content download**.

Smallest **safe** step: still **no `gh`, no network, no repo mutation** — only a read-only `readFileSync` per resolved file. `gh` create + real seeding + commit remain follow-up slices.

## Changes

- `gitBlobSha(content)` — exported pure fn; uses raw **byte** length (not char count) so multi-byte content hashes correctly.
- `computeSeedTree(resolved, root)` — read-only, returns sorted `{path, sha}` set; one `readFileSync` per resolved file; no mutation/network/gh.
- `--dry-run` now prints `<sha>  <path>` per resolved file as the explicit AC 3 comparison basis.
- Tests cross-check empty / `"hello\n"` / multi-byte `"é"` against **canonical `git hash-object` values**.

## Focused checks (run locally)

| Check | Result |
|---|---|
| `bun test tools/bootstrap-razor/seed-test-repo.test.ts` | **11 pass / 0 fail** |
| `bunx tsc -p tsconfig.json --noEmit` | **0 errors in changed files** (pre-existing unrelated NATS errors in `agentic-organization/apps/workers/` only) |
| `bun tools/bootstrap-razor/seed-test-repo.ts --dry-run` | prints resolved files with blob SHAs ✓ |
| `printf '…' \| git hash-object --stdin` cross-check | all 3 fixtures match git's output ✓ |

Notably, the empirical test run **caught a guessed `"é"` SHA fixture** (verify-before-asserting); the implementation already produced git's real value, and the fixture was corrected to the verified SHA.

Commit canary clean: `git ls-tree HEAD~1 = git ls-tree HEAD = 63` (no tree collapse).

Closes a slice of 081KR2E4K0008QG0R002JW751Y; AC 3 (idempotency) + repo creation + commit logic remain.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | true |
| Total threads | 2 |
| Resolved threads | 2 |
| Unresolved threads | 0 |
| Total review comments | 4 |
| Total fix commits (touching thread paths) | 2 |

## Review threads

### Thread 1 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6Fs4SN`
- **Path**: `tools/bootstrap-razor/seed-test-repo.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-29T14:04:08Z on `tools/bootstrap-razor/seed-test-repo.ts`:156 (association: NONE)

  <pre>
  P1: The helper's documented contract says the returned seed tree is sorted by path, and the PR description relies on that as a stable idempotency comparison basis, but the implementation only preserves the caller's input order. If a future caller passes an unsorted resolved set, the output will not match the documented representation.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-29T14:10:35Z on `tools/bootstrap-razor/seed-test-repo.ts`:156 (association: MEMBER)

    <pre>
    Fixed in 93d499d61d456232da971d3479759cb2f822a158: computeSeedTree now sorts its output by path unconditionally (matching git's canonical path-sorted tree representation), so the documented sorted-by-path contract holds regardless of caller input order. resolveSeedFiles still sorts upstream but the contract no longer depends on it.
    </pre>

### Thread 2 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6Fs4TC`
- **Path**: `tools/bootstrap-razor/seed-test-repo.test.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-29T14:04:09Z on `tools/bootstrap-razor/seed-test-repo.test.ts`:109 (association: NONE)

  <pre>
  P1: This test uses already-sorted input while asserting input-order behavior, so it does not protect the documented sorted-by-path contract that idempotency will depend on. Use intentionally unsorted input and expect sorted output so the test fails if `computeSeedTree` only maps the caller's order.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-29T14:10:37Z on `tools/bootstrap-razor/seed-test-repo.test.ts`:109 (association: MEMBER)

    <pre>
    Fixed in 93d499d61d456232da971d3479759cb2f822a158: the test now passes intentionally unsorted input (["b.txt", "a.txt"]) and asserts sorted output, so it fails if computeSeedTree ever reverts to preserving input order. bun test: 11 pass / 0 fail.
    </pre>

## Fix commits (touching thread paths)

### `8b9f9b298c08dcce7e5e755f4a89c3ad11cc7b7e` -- 2026-05-29T14:00:24Z -- `claude`

**Touched files (intersect with thread paths):**

- `tools/bootstrap-razor/seed-test-repo.test.ts`
- `tools/bootstrap-razor/seed-test-repo.ts`

**Message:**

```
feat(081KR2E4K0008QG0R002JW751Y): git-blob-SHA seed tree — read-only idempotency comparis…

…on basis (bounded slice 3)

Builds on the merged manifest-reader + --dry-run + glob-resolution slices
(PRs #2716/#2722/#2723/#5992). This slice adds the content-addressable
representation of the resolved seed set: each resolved file paired with its
git blob SHA (sha1("blob "+byteLen+"\0"+content) — byte-identical to
`git hash-object` and to what GitHub's git-data/contents API returns).

WHY: AC 3 (idempotency) requires "re-running against an existing repo reports
status, does not duplicate". The script's own comment names the prerequisite —
"idempotency = compare the resolved set against the target repo". Computing
blob SHAs locally turns that comparison into a pure {path, sha} set-equality
check with zero remote-content download. Still NO gh, NO network, NO repo
mutation — only a read-only readFileSync per resolved file. gh create +
real seeding + commit remain follow-up slices.

- gitBlobSha(content): exported pure fn; raw byte length (not char count)
- computeSeedTree(resolved, root): read-only, sorted {path, sha} set
- dry-run now prints "<sha>  <path>" per resolved file as the AC 3 basis
- tests cross-check empty / "hello\n" / multi-byte "é" against canonical
  `git hash-object` values (all three verified against real git output;
  the "é" fixture was corrected after the test caught a guessed value)

Focused checks: bun test (11 pass / 0 fail), tsc -p (no errors in changed
files), --dry-run smoke prints resolved files with blob SHAs.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `93d499d61d456232da971d3479759cb2f822a158` -- 2026-05-29T14:10:02Z -- `claude`

**Touched files (intersect with thread paths):**

- `tools/bootstrap-razor/seed-test-repo.test.ts`
- `tools/bootstrap-razor/seed-test-repo.ts`

**Message:**

```
fix(081KR2E4K0008QG0R002JW751Y): computeSeedTree canonically sorts output by path (Copilo…

…t P1)

Two Copilot P1 review findings on PR #5995:
- seed-test-repo.ts:152 — doc claimed output was sorted-by-path but the
  body only preserved caller input order; a future caller passing an
  unsorted set would silently break the documented idempotency basis.
- seed-test-repo.test.ts — test used already-sorted input, so it did not
  protect the sorted-by-path contract.

Fix: computeSeedTree now sorts its output by path unconditionally,
matching git's canonical path-sorted tree representation and making the
idempotency comparison basis stable regardless of input order. Test now
uses intentionally unsorted input and expects sorted output, so it fails
if the function reverts to input-order. resolveSeedFiles still sorts; the
contract no longer depends on it. bun test: 11 pass / 0 fail.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```
