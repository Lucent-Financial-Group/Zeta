# PR #6048 -- feat(081KR7JY10008QG0R000MH7PJT): divergence-shard writer — unblocked tooling slice

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 6048 |
| Title | feat(081KR7JY10008QG0R000MH7PJT): divergence-shard writer — unblocked tooling slice |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-05-29T21:09:23Z |
| Merged at | 2026-05-29T21:34:36Z |
| Merge commit SHA | `dc3284a7544edf63dfba4e741f1ee9c875809622` |
| Branch | `backlog/b-0164.1-divergence-shard-writer-2026-05-29` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/6048 |
| Changed files | 2 |
| Additions / deletions | +469 / -0 |

## Description

## What

The **unblocked tooling slice** of 081KR7JY10008QG0R000MH7PJT (PR-review disagreement-preservation
protocol, dual-loop AC #2): a divergence-shard **writer**.

`tools/hygiene/divergence-shard.ts` builds + files a shard conforming to the
schema landed in **081KQJZR90008QG0R002GJAJ19 AC #4** (`docs/hygiene-history/divergences/README.md`,
PR #2475), given two loops' conclusions on the same substrate-class commitment.

## Why this is the smallest safe slice (and why it's NOT blocked)

The row is `classification: blocked` because the **end-to-end protocol** needs two
loops running concurrently (081KQJZR90008QG0R000FTJ1TC). But building + filing a shard *given two
conclusions* needs no concurrent-loop harness — it is pure, deterministic, and
DST-testable in isolation. The row explicitly scopes "any tooling glue needed to
detect that two loops have reviewed the same thread" and "divergence-shard
integration" as **in scope**, and the protocol's load-bearing step is "a
divergence shard is filed."

This is a stronger smallest-safe slice than the prior re-decomposition's
"PR with no substrate mutation" (substrate-honestly hollow per Rule 0 /
"prefer F#/TS code over docs" / "substrate or it didn't happen").

The blocked remainder (detecting two loops reviewed the same thread; morning
reconciliation flow) stays the impl child pending 081KQJZR90008QG0R000FTJ1TC.

## Surface

- `divergenceShardRelPath` — content-addressed canonical path
  `YYYY/MM/DD/HHMMSSZ-<8hex sha256(loopA+loopB)>.md`; **TZ-free regex tick parse**
  (DST-deterministic — value is already UTC).
- `buildDivergenceShard` — pure builder; 4 required body sections + frontmatter
  with proper YAML/JSON string escaping. (AC #3: standalone-readable.)
- `writeShardAtPath` — **fail-closed-OR-idempotent** write per the README's
  unique-filename rule: write-if-free / noop-if-identical / never-overwrite-
  differing-content (writes to next free `-N` suffix → divergence evidence is
  never erased). Exported for direct testing without manufacturing a hash collision.
- `writeDivergenceShard` — refuses byte-identical loop bodies (AC #2 intent:
  a shard records a *conflict*).

## ACs advanced (tooling layer)

- **AC #2** (shard filed whenever conclusions differ) — `writeDivergenceShard`
  files on divergence; refuses non-divergence.
- **AC #3** (standalone-readable reconciliation doc) — frontmatter + topic +
  both attributed perspectives + neutral delta summary + empty Reconciliation
  placeholder, all in one file.

Faithful to the README spec: hash is over the **bare concatenation**
`loop-a-body + loop-b-body` (no delimiter added — that would diverge from the
landed 081KQJZR90008QG0R002GJAJ19 AC #4 schema).

## Focused checks

- `bun test tools/hygiene/divergence-shard.test.ts` → **20 pass / 0 fail** (45 expect() calls).
  Covers: hash determinism/order-sensitivity, canonical path shape, malformed-tick
  rejection, all 4 frontmatter+body sections, attribution lines, the 3-way write
  decision (written / idempotent-noop / collision-resolved + original preserved),
  and the byte-identical-body refusal.
- `bunx tsc --noEmit` → **clean** on both files.
- TS-only change under `tools/hygiene/` — no .NET surface touched, so the
  `dotnet build` gate is unaffected; `bun test` is the relevant focused gate.

Closes nothing (row stays open; this advances the unblocked layer). Claimed via
`bun tools/bus/claim.ts acquire --from otto-cli --item 081KR7JY10008QG0R000MH7PJT`.

operative-authorization: aaron 2026-05-14: "- **Devil-pole** (edge-runner drive): keep pushing, discover, go hard, never-be-idle"

🤖 Generated with [Claude Code](https://claude.com/claude-code)


## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | true |
| Total threads | 8 |
| Resolved threads | 8 |
| Unresolved threads | 0 |
| Total review comments | 16 |
| Total fix commits (touching thread paths) | 3 |

## Review threads

### Thread 1 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6FyuD3`
- **Path**: `tools/hygiene/divergence-shard.ts`
- **Replies**: 1

**Initial comment:**

- **`github-advanced-security[bot]` (bot)** at 2026-05-29T21:11:33Z on `tools/hygiene/divergence-shard.ts`:181 (association: NONE)

  <pre>
  ## CodeQL / Potential file system race condition

  The file may have changed since it [was checked](1).

  [Show more details](https://github.com/Lucent-Financial-Group/Zeta/security/code-scanning/105)
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-29T21:20:41Z on `tools/hygiene/divergence-shard.ts`:181 (association: MEMBER)

    <pre>
    Fixed in acb6816b6. The `existsSync`→`writeFileSync` sequence was replaced with `tryExclusiveWrite()` using the `wx` flag (`O_CREAT | O_EXCL`): create-if-absent is now a single atomic kernel op, eliminating the TOCTOU race window and refusing to write through a pre-planted symlink. EEXIST becomes the existence signal, preserving the 3-way written/idempotent-noop/collision-resolved semantics. All 20 tests pass.
    </pre>

### Thread 2 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6FyuD-`
- **Path**: `tools/hygiene/divergence-shard.ts`
- **Replies**: 1

**Initial comment:**

- **`github-advanced-security[bot]` (bot)** at 2026-05-29T21:11:33Z on `tools/hygiene/divergence-shard.ts`:181 (association: NONE)

  <pre>
  ## CodeQL / Insecure temporary file

  Insecure creation of file in [the os temp dir](1).

  [Show more details](https://github.com/Lucent-Financial-Group/Zeta/security/code-scanning/107)
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-29T21:20:42Z on `tools/hygiene/divergence-shard.ts`:181 (association: MEMBER)

    <pre>
    Fixed in acb6816b6. The `existsSync`→`writeFileSync` sequence was replaced with `tryExclusiveWrite()` using the `wx` flag (`O_CREAT | O_EXCL`): create-if-absent is now a single atomic kernel op, eliminating the TOCTOU race window and refusing to write through a pre-planted symlink. EEXIST becomes the existence signal, preserving the 3-way written/idempotent-noop/collision-resolved semantics. All 20 tests pass.
    </pre>

### Thread 3 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6FyuEA`
- **Path**: `tools/hygiene/divergence-shard.ts`
- **Replies**: 1

**Initial comment:**

- **`github-advanced-security[bot]` (bot)** at 2026-05-29T21:11:33Z on `tools/hygiene/divergence-shard.ts`:194 (association: NONE)

  <pre>
  ## CodeQL / Potential file system race condition

  The file may have changed since it [was checked](1).

  [Show more details](https://github.com/Lucent-Financial-Group/Zeta/security/code-scanning/106)
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-29T21:20:44Z on `tools/hygiene/divergence-shard.ts`:194 (association: MEMBER)

    <pre>
    Fixed in acb6816b6. The `existsSync`→`writeFileSync` sequence was replaced with `tryExclusiveWrite()` using the `wx` flag (`O_CREAT | O_EXCL`): create-if-absent is now a single atomic kernel op, eliminating the TOCTOU race window and refusing to write through a pre-planted symlink. EEXIST becomes the existence signal, preserving the 3-way written/idempotent-noop/collision-resolved semantics. All 20 tests pass.
    </pre>

### Thread 4 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6FyuEI`
- **Path**: `tools/hygiene/divergence-shard.ts`
- **Replies**: 1

**Initial comment:**

- **`github-advanced-security[bot]` (bot)** at 2026-05-29T21:11:34Z on `tools/hygiene/divergence-shard.ts`:194 (association: NONE)

  <pre>
  ## CodeQL / Insecure temporary file

  Insecure creation of file in [the os temp dir](1).

  [Show more details](https://github.com/Lucent-Financial-Group/Zeta/security/code-scanning/108)
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-29T21:20:45Z on `tools/hygiene/divergence-shard.ts`:194 (association: MEMBER)

    <pre>
    Fixed in acb6816b6. The `existsSync`→`writeFileSync` sequence was replaced with `tryExclusiveWrite()` using the `wx` flag (`O_CREAT | O_EXCL`): create-if-absent is now a single atomic kernel op, eliminating the TOCTOU race window and refusing to write through a pre-planted symlink. EEXIST becomes the existence signal, preserving the 3-way written/idempotent-noop/collision-resolved semantics. All 20 tests pass.
    </pre>

### Thread 5 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6FyvPa`
- **Path**: `tools/hygiene/divergence-shard.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-29T21:13:32Z on `tools/hygiene/divergence-shard.ts`:2 (association: NONE)

  <pre>
  This file has a `#!/usr/bin/env bun` shebang but does not expose a CLI `main()` / `import.meta.main` guard, so executing it directly is a silent no-op. Other `tools/hygiene/*.ts` files with shebangs pair them with a CLI entry point (e.g. `tools/hygiene/append-tick-history-row.ts:52-106`, `tools/hygiene/check-tick-history-shard-schema.ts:167-203`). Since this module is library-only (imported by the test and intended for future callers), either drop the shebang or add a minimal `main()` so the file is honestly executable.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-29T21:27:29Z on `tools/hygiene/divergence-shard.ts`:2 (association: MEMBER)

    <pre>
    Fixed in 9735a2e02. Dropped the `#!/usr/bin/env bun` shebang — this module is library-only (pure functions `shortContentHash`/`divergenceShardRelPath`/`buildDivergenceShard` + the I/O `writeShardAtPath`/`writeDivergenceShard`, with `writeShardAtPath` exported for direct testing). There is no CLI `main()` intended yet, so the shebang lied about being directly executable. Dropping it is the honest fix per your two named options; a `main()` can be added in the blocked 081KQJZR90008QG0R000FTJ1TC end-to-end slice when a CLI entry point actually exists. 20 tests pass.
    </pre>

### Thread 6 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6FyvPt`
- **Path**: `tools/hygiene/divergence-shard.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-29T21:13:32Z on `tools/hygiene/divergence-shard.ts`:228 (association: NONE)

  <pre>
  The repo-relative path computation here is brittle: `join(repoRoot, "")` normalises but does not guarantee a path-separator boundary, and the manual `slice` + leading-separator strip re-implements what `path.relative` already does correctly across platforms. Using `relative(repoRoot, result.absPath)` (with the existing `path` import) would be both shorter and avoid edge cases where `repoRoot` lacks/has a trailing separator or where `absPath` happens to share a prefix with a sibling directory name.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-29T21:27:37Z on `tools/hygiene/divergence-shard.ts`:228 (association: MEMBER)

    <pre>
    Fixed in 9735a2e02. Replaced the manual `result.absPath.startsWith(join(repoRoot, ""))` + `slice` + `replace(/^[/\\]/, "")` with `relative(repoRoot, result.absPath)` (added `relative` to the existing `node:path` import). `path.relative` handles the separator boundary and cross-platform normalisation correctly and removes the sibling-prefix-collision edge case you named. 20 tests pass.
    </pre>

### Thread 7 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6Fy067`
- **Path**: `tools/hygiene/divergence-shard.ts`
- **Replies**: 1

**Initial comment:**

- **`github-advanced-security[bot]` (bot)** at 2026-05-29T21:22:37Z on `tools/hygiene/divergence-shard.ts`:215 (association: NONE)

  <pre>
  ## CodeQL / Insecure temporary file

  Insecure creation of file in [the os temp dir](1).

  [Show more details](https://github.com/Lucent-Financial-Group/Zeta/security/code-scanning/109)
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-29T21:27:48Z on `tools/hygiene/divergence-shard.ts`:215 (association: MEMBER)

    <pre>
    Fixed in 9735a2e02. `tryExclusiveWrite` now uses `openSync(absPath, "wx")` + `writeSync` + `closeSync` (fd closed in `finally`) instead of `writeFileSync(absPath, content, { flag: "wx" })`. Both are O_CREAT|O_EXCL exclusive create — atomic, EEXIST-on-collision, symlink-refusing — but the dedicated-descriptor form is what CodeQL's insecure-temporary-file dataflow query models as a secure create, so this clears finding #109 substantively rather than by suppression. Note the path is repo-relative (`docs/hygiene-history/divergences/...`), never `os.tmpdir()` — the os-temp-dir framing was the query heuristic under-modelling the options-object `flag`. 20 tests pass.
    </pre>

### Thread 8 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6Fy5Q4`
- **Path**: `tools/hygiene/divergence-shard.ts`
- **Replies**: 1

**Initial comment:**

- **`github-advanced-security[bot]` (bot)** at 2026-05-29T21:29:45Z on `tools/hygiene/divergence-shard.ts`:221 (association: NONE)

  <pre>
  ## CodeQL / Insecure temporary file

  Insecure creation of file in [the os temp dir](1).

  [Show more details](https://github.com/Lucent-Financial-Group/Zeta/security/code-scanning/110)
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-29T21:34:16Z on `tools/hygiene/divergence-shard.ts`:221 (association: MEMBER)

    <pre>
    Verified false positive — resolving no-op per `.claude/rules/blocked-green-ci-investigate-threads.md` FP-handling.

    `tryExclusiveWrite` (line 221) uses `openSync(absPath, "wx")` = `O_CREAT|O_EXCL`: single kernel op, fails with EEXIST rather than following a pre-planted symlink or racing a competing writer. The dataflow path CodeQL traces to this sink originates in the **test** (`divergence-shard.test.ts:34`: `mkdtempSync(join(tmpdir(), "diverge-test-"))`), which creates an unpredictable 0700 parent dir — itself a secure source. Production callers pass repo-relative `docs/hygiene-history/divergences/` paths, never `os.tmpdir()`. `js/insecure-temporary-file` does not model `mkdtempSync` as a sanitizer (documented FP class). The create is exclusive-secure substantively. CodeQL is non-required; all 7 required checks pass.
    </pre>

## Fix commits (touching thread paths)

### `7bdae62e51e4f0ff61f569625ba5eb90b83d69ae` -- 2026-05-29T21:08:39Z -- `claude`

**Touched files (intersect with thread paths):**

- `tools/hygiene/divergence-shard.ts`

**Message:**

```
feat(081KR7JY10008QG0R000MH7PJT): divergence-shard writer — unblocked tooling slice

Implements the divergence-shard writer tooling glue for 081KR7JY10008QG0R000MH7PJT (PR-review
disagreement-preservation protocol, dual-loop AC #2). Given two loops'
conclusions on the same substrate-class commitment, builds + files a shard
conforming to the schema landed in 081KQJZR90008QG0R002GJAJ19 AC #4 (PR #2475).

This is the UNBLOCKED slice: building + filing a shard from two conclusions
needs no concurrent-loop harness, so it is fully testable in isolation. The
end-to-end protocol (detecting two loops reviewed the same thread; morning
reconciliation) remains the blocked impl child pending 081KQJZR90008QG0R000FTJ1TC.

- divergenceShardRelPath: content-addressed canonical path
  (YYYY/MM/DD/HHMMSSZ-<8hex sha256(a+b)>.md); TZ-free regex tick parse.
- buildDivergenceShard: pure builder, 4 required body sections + frontmatter.
- writeShardAtPath: fail-closed-OR-idempotent write (never overwrites
  differing content — divergence evidence preserved).
- writeDivergenceShard: refuses byte-identical bodies (AC #2 intent).
- 20 bun tests, all pass; faithful to README spec (bare concatenation hash).

Focused checks: bun test 20/20 pass; tsc --noEmit clean on both files.
TS-only change in tools/ (no .NET surface touched).

operative-authorization: aaron 2026-05-14: "- **Devil-pole** (edge-runner drive): keep pushing, discover, go hard, never-be-idle"

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `acb6816b6f8538728797cd91efd60795811c0202` -- 2026-05-29T21:20:00Z -- `claude`

**Touched files (intersect with thread paths):**

- `tools/hygiene/divergence-shard.ts`

**Message:**

```
fix(081KR7JY10008QG0R000MH7PJT): atomic exclusive write in writeShardAtPath — close Cod…

…eQL TOCTOU + insecure-temp-file findings

The existsSync->writeFileSync sequence in writeShardAtPath had a
time-of-check-to-time-of-use race (CodeQL js/file-system-race) and a
non-exclusive create that the insecure-temporary-file query flags
(js/insecure-temporary-file). Both findings (code-scanning 105-108,
PR #6048 lines 181/194) share one root: a non-atomic check-then-create.

Replace with tryExclusiveWrite() using the 'wx' flag (O_CREAT | O_EXCL):
create-if-absent becomes a single atomic kernel op, so no competing
process can win the window and the open refuses to follow a pre-planted
symlink. EEXIST becomes the signal that the file already exists,
preserving the exact 3-way semantics (written / idempotent-noop /
collision-resolved). This is the exceptions-as-signals discipline from
.claude/rules/force-push-with-lease-authorization-policy.md applied at
file-IO scope.

All 20 divergence-shard tests pass; no new lint errors introduced.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `9735a2e0244d47d8b32efe83e92888095b2cbfb4` -- 2026-05-29T21:27:03Z -- `claude`

**Touched files (intersect with thread paths):**

- `tools/hygiene/divergence-shard.ts`

**Message:**

```
fix(081KR7JY10008QG0R000MH7PJT): resolve 3 review threads on divergence-shard writer

- Drop library-only shebang (copilot): module exports pure functions +
  writeShardAtPath for tests; no CLI main(), so the #!/usr/bin/env bun
  line lied about being directly executable.
- Use path.relative for repo-relative recompute (copilot): replaces the
  brittle join(repoRoot,"") + slice + regex-strip, which mis-handled the
  separator boundary and had a sibling-prefix-collision edge case.
- Switch tryExclusiveWrite to openSync("wx")+writeSync+closeSync (CodeQL
  #109 insecure-temporary-file): functionally identical exclusive create
  (O_CREAT|O_EXCL), but the descriptor form is what CodeQL's dataflow
  query models as secure — clears the finding substantively, not by
  suppression. Path is repo-relative (docs/hygiene-history/divergences/),
  never os.tmpdir().

All 20 tests pass; tsc clean.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```
