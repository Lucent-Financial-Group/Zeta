# PR #5988 -- feat(roms): datfile-as-dependency pin + fetch + SHA-256 verify (081KSRGFP0008QG0R003ZH6DN3)

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 5988 |
| Title | feat(roms): datfile-as-dependency pin + fetch + SHA-256 verify (081KSRGFP0008QG0R003ZH6DN3) |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-05-29T13:21:12Z |
| Merged at | 2026-05-29T13:36:48Z |
| Merge commit SHA | `1380425fd0ab2bdc91c243ad91cc11bf4cc716f6` |
| Branch | `otto-cli/b0083-1-datfile-as-dependency-2026-05-29` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/5988 |
| Changed files | 6 |
| Additions / deletions | +619 / -2 |

## Description

## What

Smallest safe slice of **081KQ8P5D0008QG0R001590WJ3** (Atari 2600 ROM canonical-naming + TOSEC tooling). Both decomposed children are already closed:

- **081KR2E4K0008QG0R001QZDAMQ** (canonical naming via hash lookup) — closed 2026-05-16 (`canonicalize.ts`).
- **081KR2E4K0008QG0R001JC6S3N** (safe/unsafe folder split) — closed 2026-05-29 (`split-by-license.ts` + `roms-safe/`).

The parent's acceptance criterion #6 — *"Tooling refreshes on TOSEC datfile updates"* — plus the whole **"Datfile-as-dependency"** design section was the one genuinely-unbuilt, repo-shippable gap. `canonicalize.ts` *consumes* `--datfile <path>`, but nothing *produced* the pinned, verified datfile. This PR builds that producer (tracked as new child **081KSRGFP0008QG0R003ZH6DN3**).

## Changes

| File | Purpose |
|---|---|
| `tools/roms/manifests/datfiles.json` | Structured pin manifest. `atari-2600` pinned to **TOSEC 2025-03-13** (WebSearch-verified latest; no 2026 release as of 2026-05). |
| `tools/roms/fetch-datfile.ts` | Download + SHA-256-verify + write to gitignored `roms/.datfiles/`; emits the `canonicalize.ts --datfile` follow-up. `--list` shows verification status. |
| `tools/roms/fetch-datfile.test.ts` | 24 tests (manifest parse, placeholder gate, SHA-256 verify, CLI error paths) — no network. |
| `docs/backlog/P1/081KSRGFP0008QG0R003ZH6DN3-*.md` | New child row tracking the slice. |
| `docs/backlog/P1/081KQ8P5D0008QG0R001590WJ3-*.md` + `docs/BACKLOG.md` | Parent children list + decomposition-status note + regenerated index. |

## Fail-closed dep-pin discipline

Per `.claude/rules/dep-pin-search-first-authority.md`, `downloadUrl` + `sha256` carry explicit `<...-VERIFY-ON-FETCH>` placeholders (the Anchor-3 / 081KSGS9H0008QG0R003GM7TYN pattern). These require a real download to verify, which is out of scope for the authoring environment. The tool **refuses to write** (exit 2) on any placeholder pin, naming the exact operator steps. The operator fills the verified values on the first network-enabled fetch.

## Focused checks

- `bun test tools/roms/fetch-datfile.test.ts tools/roms/canonicalize.test.ts` → **45 pass, 0 fail** (24 new + 21 canonicalize regression).
- `bunx tsc --noEmit` (project config) → **zero errors in the new files** (only pre-existing unrelated `@nats-io/*` module-resolution errors in `agentic-organization/`).
- CLI smoke: `--list` → exit 0 (`atari-2600  TOSEC 2025-03-13  PINNED-UNVERIFIED`); `--platform atari-2600` → exit 2 (fail-closed, as designed).
- Commit-tree canary: HEAD 63 == HEAD~1 63 (no tree collapse).

## Out of scope (named in 081KSRGFP0008QG0R003ZH6DN3)

- Operator fills the verified `downloadUrl` + `sha256` on first fetch (manifest edit, not code).
- Optional scheduled GHA refresh cadence (parent marks it "scheduled cron optional").

Sources: [TOSEC release 2025-03-13](https://www.tosecdev.org/news/releases/177-tosec-release-2025-03-13), [TOSEC downloads](https://mail.tosecdev.org/downloads/category/59-2025-03-13).

🤖 Generated with [Claude Code](https://claude.com/claude-code)


## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | true |
| Total threads | 3 |
| Resolved threads | 3 |
| Unresolved threads | 0 |
| Total review comments | 5 |
| Total fix commits (touching thread paths) | 2 |

## Review threads

### Thread 1 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6FsNoM`
- **Path**: `tools/roms/fetch-datfile.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-29T13:22:41Z on `tools/roms/fetch-datfile.ts`:277 (association: NONE)

  <pre>
  `pin.platform.replace("-", "/")` only replaces the first hyphen, which happens to produce the desired `atari/2600` path for the current `atari-2600` slug. If a future pin uses a slug like `nintendo-entertainment-system` or `sega-master-system`, the emitted follow-up command will be wrong (e.g., `roms/nintendo/entertainment-system`). Consider mapping the platform-to-path explicitly in the manifest (e.g., a `romsDir` field) rather than inferring it from the slug, or at minimum document the single-hyphen assumption.
  </pre>

### Thread 2 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6FsOp_`
- **Path**: `tools/roms/fetch-datfile.ts`
- **Replies**: 1

**Initial comment:**

- **`github-advanced-security[bot]` (bot)** at 2026-05-29T13:23:45Z on `tools/roms/fetch-datfile.ts`:262 (association: NONE)

  <pre>
  ## CodeQL / File data in outbound network request

  Outbound network request depends on [file data](1).

  [Show more details](https://github.com/Lucent-Financial-Group/Zeta/security/code-scanning/103)
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-29T13:36:34Z on `tools/roms/fetch-datfile.ts`:262 (association: MEMBER)

    <pre>
    Resolving with mitigation noted. CodeQL alert 103 (file data → outbound network) is an accurate taint-flow but the security property is satisfied: `pin.downloadUrl` is sourced from a checked-in, version-controlled manifest (`tools/roms/manifests/datfiles.json`), not from untrusted input. The fetch is gated by `fetchBlockReason(pin)` (line 253) which fail-closes on any unverified `&lt;placeholder&gt;` URL per dep-pin-search-first-authority. This is the standard pinned-dependency-manifest pattern (same shape as a lockfile URL); the URL set is a fixed, reviewed allowlist in git. No SSRF surface from untrusted input. CodeQL is a non-required check.
    </pre>

### Thread 3 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6FsOqG`
- **Path**: `tools/roms/fetch-datfile.ts`
- **Replies**: 1

**Initial comment:**

- **`github-advanced-security[bot]` (bot)** at 2026-05-29T13:23:45Z on `tools/roms/fetch-datfile.ts`:282 (association: NONE)

  <pre>
  ## CodeQL / Network data written to file

  Write to file system depends on [Untrusted data](1).

  [Show more details](https://github.com/Lucent-Financial-Group/Zeta/security/code-scanning/104)
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-29T13:36:39Z on `tools/roms/fetch-datfile.ts`:282 (association: MEMBER)

    <pre>
    Resolving with mitigation noted. CodeQL alert 104 (network data → file write) is an accurate taint-flow but the canonical mitigation is already present in-code: the downloaded `bytes` are SHA-256 verified against the pinned hash at line 271 (`if (!verifyChecksum(bytes, pin.sha256))`) which returns early WITHOUT writing on mismatch. The `writeFileSync` at line 282 is only reached after the bytes match a checked-in, operator-curated hash — i.e. the data written is cryptographically verified against a git-tracked pin. This is exactly the verify-before-write gate CodeQL's rule recommends for this class. CodeQL is a non-required check.
    </pre>

## Fix commits (touching thread paths)

### `a45bd77439d4756a35b0e69b65f14990f6a3a8ae` -- 2026-05-29T13:20:35Z -- `claude`

**Touched files (intersect with thread paths):**

- `tools/roms/fetch-datfile.ts`

**Message:**

```
feat(roms): datfile-as-dependency pin + fetch + SHA-256 verify (B-008…

…3.1)

Smallest safe slice of 081KQ8P5D0008QG0R001590WJ3. Both decomposed children (081KR2E4K0008QG0R001QZDAMQ
canonical-naming, 081KR2E4K0008QG0R001JC6S3N safe/unsafe split) are closed; the parent's
'Datfile-as-dependency' design + 'refreshes on TOSEC datfile updates'
acceptance criterion (#6) was the one genuinely-unbuilt repo-shippable
gap. canonicalize.ts consumes --datfile <path> but nothing produced the
pinned, verified datfile.

- tools/roms/manifests/datfiles.json: structured pin manifest
  (platform -> source/release/datfileName/sourceUrl/downloadUrl/sha256).
  atari-2600 pinned to TOSEC 2025-03-13 (WebSearch-verified latest; no
  2026 release as of 2026-05).
- tools/roms/fetch-datfile.ts: download + SHA-256-verify + write to a
  gitignored cache (roms/.datfiles/), then emit the canonicalize.ts
  --datfile follow-up. Fails CLOSED (exit 2) on any <...> placeholder pin
  per dep-pin-search-first-authority; --list surfaces verification status.
- tools/roms/fetch-datfile.test.ts: 24 tests (manifest parse, placeholder
  gate, SHA-256 verify, CLI error paths) — no network.

downloadUrl + sha256 carry explicit <...-VERIFY-ON-FETCH> placeholders
(dep-pin Anchor-3 / 081KSGS9H0008QG0R003GM7TYN pattern): they need a real download to verify,
which is out of scope for the authoring environment. The tool refuses to
write until the operator fills them on first network-enabled fetch.

operative-authorization: aaron 2026-05-14: "- **Devil-pole** (edge-runner drive): keep pushing, discover, go hard, never-be-idle"

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `33ad2cd72c8d26051b4966b39f57c365ba2021d3` -- 2026-05-29T13:28:14Z -- ``

**Touched files (intersect with thread paths):**

- `tools/roms/fetch-datfile.ts`

**Message:**

```
fix(roms): explicit romsDir manifest field replaces fragile slug→path…

… inference

Copilot review (PR #5988): pin.platform.replace("-", "/") only replaces
the first hyphen — correct for atari-2600→atari/2600 but wrong for slugs
like nintendo-entertainment-system. Per reviewer's preferred fix, map the
platform→roms-path explicitly in the manifest (romsDir field) rather than
inferring it from the slug. The field is now required (REQUIRED_FIELDS),
present in the manifest entry, and the canonicalize.ts hint uses it directly.

- DatfilePin: add required romsDir field (documented why explicit > inferred)
- REQUIRED_FIELDS: add "romsDir" (parse-time validation single source of truth)
- datfiles.json: romsDir="atari/2600" for the atari-2600 pin
- fetch-datfile.ts:286: hint uses roms/${pin.romsDir}
- test: VERIFIED_PIN romsDir + missing-romsDir-throws test (27 pass)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```
