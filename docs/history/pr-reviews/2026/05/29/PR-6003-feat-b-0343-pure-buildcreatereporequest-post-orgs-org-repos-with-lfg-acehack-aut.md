# PR #6003 -- feat(081KR2E4K0008QG0R002JW751Y): pure buildCreateRepoRequest — POST /orgs/{org}/repos with LFG/AceHack authz guard (step 0)

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 6003 |
| Title | feat(081KR2E4K0008QG0R002JW751Y): pure buildCreateRepoRequest — POST /orgs/{org}/repos with LFG/AceHack authz guard (step 0) |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-05-29T15:49:36Z |
| Merged at | 2026-05-29T15:58:33Z |
| Merge commit SHA | `9a92ce45cf95e9789f9fd1acb535822a8587970e` |
| Branch | `otto-cli/b0343-create-repo-builder-2026-05-29` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/6003 |
| Changed files | 2 |
| Additions / deletions | +161 / -1 |

## Description

## What

Adds `buildCreateRepoRequest` — the **one missing link** in 081KR2E4K0008QG0R002JW751Y's seed write chain. The blob → tree → commit → ref builders all shipped (slices 6–9); the logically-first step, **repo CREATE** (AC 1 step 1, "Creates a new repo in LFG or AceHack org"), was the last gap. This slice fills it as a pure builder in the same family as the rest.

## Why this is the smallest safe slice

- **Pure**: no network, no `gh`, no filesystem — `(org, repo, options?) → GitCreateRepoRequest | string`.
- **Additive**: one new export + one new interface + 7 new test cases + dry-run demonstration. No existing behavior changed.
- **Carries the load-bearing authz guard** the row names explicitly: `org ∈ {Lucent-Financial-Group, AceHack}`; any other org (esp. **ServiceTitan**, named off-limits per Aaron 2026-05-05) returns a refusal string. Allowlist / default-deny — refusal does not depend on naming ServiceTitan specifically.

## Design notes

- `T | string` return matches `readManifest` / `parseGitTreeResponse`: a string is the refusal reason, a request is the green light. The guard lives at the builder, not at a call site.
- **`auto_init: false` is load-bearing** — an auto-initialized README would break the seed's root-commit path (`buildSeedCommitRequest(parentSha=null)` → `parents: []`) and report as `extraneous` in every idempotency diff. Documented inline.
- `private` defaults true (throwaway experiment repo); overridable.
- `AUTHORIZED_ORGS` const = single source of truth for the scope guard.

## API currency (Otto-364 search-first)

Endpoint + body verified current against [docs.github.com/en/rest/repos/repos](https://docs.github.com/en/rest/repos/repos), `X-GitHub-Api-Version: 2026-03-10` — matches the file's existing doc comments.

## Focused checks

| Check | Outcome |
|---|---|
| `bun test tools/bootstrap-razor/seed-test-repo.test.ts` | **53 pass / 0 fail** (73 expect calls; +7 new cases) |
| `--dry-run` output | renders step-0 create plan + demonstrates ServiceTitan refusal (AC 1 step 1 + AC 4 visible) |
| `tsc --noEmit` | clean for this file (only pre-existing `agentic-organization/` module-resolution errors remain) |
| commit canary | HEAD tree 63 = parent tree 63 (no corruption; 2 intended files only) |

## Scope

Bounded to the pure builder + tests + dry-run wiring. The real `gh` network integration (create → seed → commit → push) remains a follow-up slice — the dry-run note now reads "gh network integration (real create + seed + commit): follow-up slice."

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | true |
| Total threads | 1 |
| Resolved threads | 1 |
| Unresolved threads | 0 |
| Total review comments | 2 |
| Total fix commits (touching thread paths) | 2 |

## Review threads

### Thread 1 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6FunDu`
- **Path**: `tools/bootstrap-razor/seed-test-repo.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-29T15:51:18Z on `tools/bootstrap-razor/seed-test-repo.ts`:406 (association: NONE)

  <pre>
  P2: This code comment introduces a direct human first-name attribution in a TypeScript source file. Per the repository convention, code/current-state surfaces should use role references such as “operator” or “human maintainer” instead of named attribution; the named-attribution carve-outs are limited to history/research surfaces and roster mappings.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-29T15:54:43Z on `tools/bootstrap-razor/seed-test-repo.ts`:406 (association: MEMBER)

    <pre>
    Fixed in 55795b5c — swapped the named attribution to "operator authorization 2026-05-05", preserving the date + authorization scope (LFG/AceHack-only). Comment-only change per the role-reference convention for code/current-state surfaces.
    </pre>

## Fix commits (touching thread paths)

### `a7e2b7a85ba4df4b27f1aef1595b034045ef659c` -- 2026-05-29T15:48:41Z -- `claude`

**Touched files (intersect with thread paths):**

- `tools/bootstrap-razor/seed-test-repo.ts`

**Message:**

```
feat(081KR2E4K0008QG0R002JW751Y): pure buildCreateRepoRequest — POST /orgs/{org}/repos wi…

…th LFG/AceHack authz guard (step 0)

The one missing link in the seed write chain: repo CREATE (AC 1 step 1),
which logically precedes the already-built blob → tree → commit → ref
builders. Pure, no network, no gh — same pure-builder family as the rest.

- buildCreateRepoRequest(org, repo, options?) → GitCreateRepoRequest | string
  - authorization-scope guard: org ∈ {Lucent-Financial-Group, AceHack};
    any other org (esp. ServiceTitan, named off-limits in the row's
    authorization scope per Aaron 2026-05-05) returns a refusal string.
    Allowlist / default-deny — the refusal does not depend on naming
    ServiceTitan specifically.
  - T | string return matches readManifest / parseGitTreeResponse: a string
    is the refusal reason, a request is the green light. Pure, testable.
  - auto_init: false is load-bearing — an auto-initialized README would
    break the seed's root-commit path (buildSeedCommitRequest(parentSha=null)
    → parents:[]) and report as extraneous in every idempotency diff.
  - private defaults true (throwaway experiment repo); overridable.
- Endpoint + body verified current: docs.github.com/en/rest/repos/repos,
  X-GitHub-Api-Version 2026-03-10 (matches the file's existing doc comments).
- AUTHORIZED_ORGS const = single source of truth for the scope guard.
- --dry-run now shows the step-0 create plan + demonstrates the
  ServiceTitan refusal (AC 1 step 1 + AC 4 visible in output).

Focused checks: bun test (53 pass / 0 fail, +7 new cases);
--dry-run renders the create plan + guard; tsc clean for this file
(only pre-existing agentic-organization/ module-resolution errors remain).

operative-authorization: aaron 2026-05-14: "- **Devil-pole** (edge-runner drive): keep pushing, discover, go hard, never-be-idle"

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `55795b5c40dfeb3ee8b9933ac7b0aafbc3808fad` -- 2026-05-29T15:54:17Z -- `claude`

**Touched files (intersect with thread paths):**

- `tools/bootstrap-razor/seed-test-repo.ts`

**Message:**

```
docs(081KR2E4K0008QG0R002JW751Y): role-reference comment in seed-test-repo (resolve Copil…

…ot P2)

Copilot P2 on PR #6003: code comment at seed-test-repo.ts:406 used a
direct human first-name attribution. Per the role-reference convention,
code/current-state surfaces use "operator"/"human maintainer"; named
attribution is reserved for history/research surfaces and roster maps.

Swap "Aaron 2026-05-05" -> "operator authorization 2026-05-05",
preserving the date + authorization scope (LFG/AceHack-only, NOT
ServiceTitan). Comment-only; no behavior change.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```
