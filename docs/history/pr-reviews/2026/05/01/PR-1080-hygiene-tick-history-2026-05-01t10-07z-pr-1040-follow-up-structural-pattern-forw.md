# PR #1080 -- hygiene(tick-history): 2026-05-01T10:07Z — PR #1040 follow-up (structural-pattern + forward-ref position)

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 1080 |
| Title | hygiene(tick-history): 2026-05-01T10:07Z — PR #1040 follow-up (structural-pattern + forward-ref position) |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-05-01T10:08:22Z |
| Merged at | 2026-05-01T23:24:04Z |
| Merge commit SHA | `76e8e6bf29af7aea5766318392d9ff690b84d666` |
| Branch | `hygiene/tick-history-2026-05-01T1008Z-1040-followup` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/1080 |
| Changed files | 1 |
| Additions / deletions | +1 / -0 |

## Description

PR #1040 follow-up: 2 real fixes — `.git/hooks/pre-commit` rephrased as `.githooks/` + `core.hooksPath` (tracked layer), forward-ref restructured (description leads, citation trails).

New classes: **structural-pattern-mismatch** (path correct in general but wrong abstraction layer) and **forward-ref position-of-citation** (lead-with-description, trail-with-PR).

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 1 |
| Resolved threads | 1 |
| Unresolved threads | 0 |
| Total review comments | 2 |
| Total fix commits (touching thread paths) | 2 |

## Review threads

### Thread 1 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5-8zgn`
- **Path**: `docs/hygiene-history/ticks/2026/05/01/1007Z.md`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-01T10:11:40Z on `docs/hygiene-history/ticks/2026/05/01/1007Z.md`:1 (association: NONE)

  <pre>
  The tick body claims the repo’s “versioned integration pattern is `.githooks/` + `core.hooksPath` (set up by the install script)”, but the repo’s own hook inventory doc states there are currently no repo-local hooks, no `.githooks/`, and no `core.hooksPath` override. Please adjust this entry to match current repo state (e.g., describe `.githooks/`/`core.hooksPath` as a proposed pattern, or cite the actual tracked hook integration mechanism if one exists).
  ```suggestion
  | 2026-05-01T10:07:00Z | opus-4-7 / autonomous-loop tick | 98fc7424 | PR #1040 follow-up tick — 2 threads. Two real fixes: (1) **Pre-commit hook integration point (P1, line 39)** — the row described `.git/hooks/pre-commit` as the integration point, but `.git/hooks/*` is per-clone and untracked. The repo does **not** currently ship a tracked repo-local hook path (`.githooks/`) or a `core.hooksPath` override, so that wording overstated current state. Reworded to remove the shipped-state claim and treat `.githooks/` + `core.hooksPath` as a **proposed** versioned pattern rather than an existing integration. **Same shipped-state-overtook-the-doc class** as B-0125 title-slug + B-0122 stale-TS-claim — except in this case the substrate-author wrote a path-pattern that's **structurally wrong** (per-clone untracked vs tracked/versioned) rather than just stale. The class extends to **structural-pattern-mismatch / Real-fix subclass**: the path is real but at the wrong abstraction layer. (2) **Forward-reference restructuring (P1, line 98)** — the forward-ref bullet had the filename in `**\`backticked\`**` form, reading visually as a code reference even though the file isn't on main. Restructured: description leads, filename appears parenthetically AFTER the "filed in PR #1025" annotation, with explicit "(path will resolve once #1025 lands)" note. **Position-of-citation matters** within forward-references: lead with description (cognitive handle), trail with filename + PR (path handle). All 2 threads resolved via GraphQL. PR #1040 substrate; auto-merge unarmed. Cron 98fc7424 healthy. | [PR #1040: 1 commit (5dd67a8) addressing 2 real findings + 2 thread-resolutions; auto-merge intentionally unarmed] | The structural-pattern-mismatch class is structurally interesting because the substrate-author's instinct (`.git/hooks/pre-commit` as "where pre-commit hooks go") is correct in 90% of contexts but wrong here because `.git/hooks/*` is per-clone runtime state, not a tracked project artifact. The corrective is **layer-of-abstraction discipline**: when describing a tooling integration, distinguish the runtime location from any versioned project artifact, and do not imply a tracked `.githooks/` + `core.hooksPath` setup unless the repo actually ships one. Future-Otto: when writing about hook/config/state integrations, default to citing the actual tracked artifact the repo ships; if the tracked pattern is only aspirational, say so explicitly instead of promoting the familiar runtime path into a shipped-state claim. The forward-reference position-of-citation lesson is also worth promoting: **lead-with-description, trail-with-citation** for in-flight refs reads more clearly than **lead-with-citation-look, trail-with-PR-annotation**. The reader's eye sees the description first (cognitive load), then the PR annotation (recency context), then the path (only if they're going to navigate). |
  ```
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-01T13:14:20Z on `docs/hygiene-history/ticks/2026/05/01/1007Z.md`:1 (association: MEMBER)

    <pre>
    **Reviewer is correct — acknowledging the factual error in the shard.**

    The tick-shard at `docs/hygiene-history/ticks/2026/05/01/1007Z.md` claimed *"the versioned integration pattern is `.githooks/` + `core.hooksPath` config (set up by the install script)"* — that claim is **factually wrong** when verified against current repo state:

    ```
    $ ls .githooks
    ls: .githooks: No such file or directory
    $ git config --get core.hooksPath
    [empty — not configured]
    ```

    And per the actual hook-research doc `docs/research/hooks-adr-track.md` Phase-1 audit (Aaron-commissioned 2026-04-20):

    &gt; *"No repo-local hooks. The audit found no `.githooks/`, no `tools/githooks/`, no active entries in `.git/hooks/` (all .sample only), no `core.hooksPath` override in git config."*

    The substrate-author (me) wrote what would have been the *intended* versioned integration pattern (the install-script-managed `.githooks/` + `core.hooksPath` indirection is a common project pattern) — but the project hasn't actually implemented that pattern yet. The shard's "is" should have been "would be" or "the target pattern (not yet implemented)."

    Tick-history shards capture what happened at the moment they were written, including erroneous claims; they're immutable history. The right corrective is this thread-acknowledgment-of-error + the future-Otto note: when describing tooling integration in tick shards, **verify present-tense claims against current repo state** rather than describing target-pattern as if implemented.

    This composes with the substrate-or-it-didn't-happen rule (CLAUDE.md): if a pattern isn't implemented, the tick shard should not describe it in present-indicative. "Would be" / "target pattern" / "per the hooks-research doc's recommended shape" are the correct framings for unimplemented patterns.

    Resolving with acknowledgment-of-error rather than shard-edit (immutable history).
    </pre>

## Fix commits (touching thread paths)

### `1dc76dbfbf4a0097cfb4a0bba88d5566eabcb0e4` -- 2026-05-01T10:08:19Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `docs/hygiene-history/ticks/2026/05/01/1007Z.md`

**Message:**

```
hygiene(tick-history): 2026-05-01T10:07Z — PR #1040 follow-up (struct…

…ural-pattern-mismatch + forward-ref position)
```

### `15638f7225eccb74c2f3d7c89e5c2aaa93e7f141` -- 2026-05-01T23:22:05Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `docs/hygiene-history/ticks/2026/05/01/1007Z.md`

**Message:**

```
fix(tick-history-1007Z): unbreak markdownlint MD038 — replace nested-…

…escape-backtick with bold-code (markdownlint required check)

The pattern `**\`backticked\`**` (bolded escaped backticks)
was confusing markdownlint's code-span parser, producing two
spurious MD038 "spaces inside code span elements" errors at
columns 978 and 2058 of the line. The simpler well-formed
markdown — `**`backticked`**` (bold around an inline-code
element) — renders the same intent and parses cleanly.

Both lint errors clear with the single fix; the second was a
parser-confusion downstream of the first.
```
