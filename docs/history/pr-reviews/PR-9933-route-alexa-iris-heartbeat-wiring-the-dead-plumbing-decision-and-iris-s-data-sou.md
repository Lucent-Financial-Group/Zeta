# PR #9933 -- route(alexa → iris): heartbeat wiring, the dead plumbing decision, and Iris's data source is live

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 9933 |
| Title | route(alexa → iris): heartbeat wiring, the dead plumbing decision, and Iris's data source is live |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-08-01T22:23:28Z |
| Merged at | 2026-08-01T22:33:31Z |
| Merge commit SHA | `168f4e973c89b4254bb09b1db06bd7cec31e35db` |
| Branch | `shadow/route-alexa-then-iris` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/9933 |
| Changed files | 3 |
| Additions / deletions | +126 / -0 |

## Description

Routes to Alexa first, then Iris, per Aaron.

## Alexa — two things left

1. **The heartbeat step.** The CLI isn't called anywhere yet, so the JSON is a one-time snapshot rather than a live surface. One step in `agent-heartbeat.yml`. **It must land before the push step** — the tick's push is step 13 of 13 precisely because every artifact producer has to precede it. A producer added *after* the push writes files that are never committed, and that mistake already cost this repo a day of discarded heals.

2. **A decision, not a fix.** `eventsByAgent` is threaded through **five** vault builders that never read it. CodeQL caught the symptom (one unused constant) and not the shape. Either those builders were meant to use it — in which case per-room event attribution is missing — or the parameter comes out of five signatures. Deliberately left to her.

**Already fixed so she doesn't hit it:** `lint (TS)` was red on #9932 with 10 TS6133 errors. Fixed in 75beaca; output byte-identical, verified by diffing emitted dweller records.

## Iris — her data source now exists

When she was routed in #9928, the contract was on main but nothing emitted the JSON. #9932 closes that, and it was verified rather than accepted on report: 23 contract tests written by a different party than the implementation, all passing.

What that buys the page: a stopped society cannot render live (pinned both directions); the measured ~65min real cadence reads `stale` not `live`, so the page shows the cadence gap honestly instead of smoothing it; zero events yields `cold` even with a fresh frame; `heat` surfaces recent failures without scarring permanently; deterministic under injected `nowMs` so the page is replayable from a fixture.

**The caveat that changes what she builds:** the CLI isn't wired into the tick yet, so the JSON is a snapshot until Alexa's step lands. She should build anyway — the contract degrades honestly, so a stale file renders `cold` rather than lying. And `last_seen: null` means *not observed*, which is the frost/withheld shape, never a zero or a dash.

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 0 |
| Resolved threads | 0 |
| Unresolved threads | 0 |
| Total review comments | 0 |
| Total fix commits (touching thread paths) | 0 |

## Review threads

(no review threads on this PR)

## Fix commits (touching thread paths)

(no commits in this PR touched files anchored by review threads)
