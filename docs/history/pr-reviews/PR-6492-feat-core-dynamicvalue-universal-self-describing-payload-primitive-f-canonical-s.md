# PR #6492 -- feat(Core): DynamicValue — universal self-describing-payload primitive (F# canonical shape)

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 6492 |
| Title | feat(Core): DynamicValue — universal self-describing-payload primitive (F# canonical shape) |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-06-01T22:41:14Z |
| Merged at | 2026-06-01T22:53:21Z |
| Merge commit SHA | `2a15ba1815f744f353a476c130f0bc27f9eb88b6` |
| Branch | `otto-cli/dynamic-value-fsharp-canonical-shape-2026-06-01` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/6492 |
| Changed files | 5 |
| Additions / deletions | +418 / -1 |

## Description

The serializer for **dynamic shapes** Aaron asked about: a self-describing runtime value tree for shapes not known at compile time. Distinct from the static `ISerializer<'T>` seam (known types) and a **new shape** — not a generalization of the observe oracle's JSON-only `Json` tree.

## Aaron's directives (2026-06-01), in order
1. *"we should do a new shape for that one"* → fresh `DynamicValue`, not a `Json` lift.
2. *"support any language that can be dynamic or self describing in the payload"* → the case set `Null | Bool | Int | Float | String | Bytes | Array | Object` is exactly the common self-describing core of **CBOR / msgpack / JSON / YAML**. What makes it a new shape vs `Json`: **`Int` split from `Float`** (carries protobuf int64 / >2^53 without precision loss) and **native `Bytes`** (the binary type JSON fakes with base64).
3. *"the only way to support a non-self-describing language is a schema registry … schemas change at runtime … runtime schemas and self-describing fit the same shape"* → recorded in the doc + registry: schema-required formats (protobuf/Avro/Thrift) join via a **runtime schema registry** (schema-id → lookup → current shape at runtime); a schema-driven decode yields the **same** runtime `DynamicValue`. The `Schema`/`SchemaRegistry` is a separate port, **not** a shape change.

## Shape (F# canonical reference; C#/Rust/TS conform later)
- `DynamicValue` DU (8 cases) + `DynamicValueType` tag + `typeOf` — the runtime **QueryInterface**; exhaustive match, no wildcard, so future variants (CBOR tags, BSON dates, decimal128, msgpack ext) break consumers loudly.
- Lazy bind: strict `try*` accessors (no widening) + `tryField` / `tryItem` + `PropertyPath` `get "a.b[3].c"` (declines miss / out-of-range / type-mismatch / malformed; empty path = identity).
- Hand-written structural equality (`[<CustomEquality; NoComparison>]`): `Bytes` compares **contents** not the `ImmutableArray` reference; arrays/objects recurse; `Object` is order-sensitive (canonical encoders sort on the wire).

## Follow-ups (open)
C#/Rust/TS conformance · wire codecs (JSON/CBOR/msgpack/Arrow adapters behind the `ISerializer<'T>` seam) + canonical-encoding golden vectors · `Schema`/`SchemaRegistry` port · format-specific extra variants.

`src/Core/DynamicValue.fs` (+ registry status). 13 tests, build 0-warning.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | true |
| Total threads | 3 |
| Resolved threads | 3 |
| Unresolved threads | 0 |
| Total review comments | 3 |
| Total fix commits (touching thread paths) | 3 |

## Review threads

### Thread 1 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GRHAe`
- **Path**: `src/Core/DynamicValue.fs`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-06-01T22:42:24Z on `src/Core/DynamicValue.fs`:242 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Return None for oversized path indexes**

  For a path containing a numeric index larger than `Int32.MaxValue` (for example `"items[99999999999999999999]"` from an external payload/query), this `Int32.Parse` throws `OverflowException` instead of making `DynamicValue.get` return `None`. That violates the documented option-returning navigation behavior for malformed/out-of-range paths and reintroduces exception-based control flow on this public surface; use a non-throwing parse/bounds check before constructing the `Index` step.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 2 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GRIAN`
- **Path**: `src/Core/DynamicValue.fs`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-01T22:44:08Z on `src/Core/DynamicValue.fs`:243 (association: NONE)

  <pre>
  P0: `DynamicValue.get` claims to return `None` on malformed paths, but parsing an index uses `Int32.Parse`, which will throw on overflow (e.g., "a[999999999999]") instead of returning `None`.

  Use `Int32.TryParse` and treat parse failure as `ok &lt;- false` so `get` remains total over arbitrary input strings.
  </pre>

### Thread 3 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GRIAo`
- **Path**: `docs/PRIMITIVE-REGISTRY.md`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-01T22:44:08Z on `docs/PRIMITIVE-REGISTRY.md`:64 (association: NONE)

  <pre>
  The inline-code `DynamicValue` case list is written with escaped pipes (`\|`), which will render the backslashes literally inside the code span (readers will see `\|` rather than `|`). Use plain `|` inside inline code (and between the two `deserialize` code spans) so the rendered registry entry matches the actual DU case syntax.
  </pre>

## Fix commits (touching thread paths)

### `59748348fd7933a470b6dd00bae946f124682005` -- 2026-06-01T22:41:04Z -- ``

**Touched files (intersect with thread paths):**

- `docs/PRIMITIVE-REGISTRY.md`
- `src/Core/DynamicValue.fs`

**Message:**

```
feat(Core): DynamicValue — the universal self-describing-payload prim…

…itive (F# canonical shape)

The serializer for dynamic shapes: a self-describing runtime value tree for shapes
NOT known at compile time (the "Dynamic runtime objects / polymorphic shape" line in
PRIMITIVE-REGISTRY; Eve-Protocol 081KRW63S0008QG0R0030F8ZXA). Distinct from the static `ISerializer<'T>`
seam (known types) and a NEW shape — deliberately not a generalization of the observe
oracle's JSON-only `Json` tree.

Aaron 2026-06-01 directives, in order:
- "we should do a new shape for that one" → a fresh `DynamicValue`, not a `Json` lift.
- "support any language that can be dynamic or self describing in the payload" → the case
  set `Null | Bool | Int | Float | String | Bytes | Array | Object` is exactly the common
  self-describing core shared by CBOR/msgpack/JSON/YAML, so any self-describing payload
  parses faithfully into it. Two things that make it a NEW shape vs `Json`: `Int` (int64)
  split from `Float` (carries protobuf int64 / >2^53 integers without precision loss), and
  **native `Bytes`** (the binary type every self-describing binary format has and JSON fakes
  via base64).
- "the only way to support a non-self-describing language is a schema registry … schemas
  change at runtime … runtime schemas and self-describing fit the same shape" → recorded in
  the doc + registry: schema-required formats (protobuf/Avro/Thrift) join via a runtime
  schema registry (schema-id → registry lookup → current shape at runtime); a schema-driven
  decode yields the SAME runtime `DynamicValue` tree. The `Schema`/`SchemaRegistry` is a
  separate port feeding the same target, not a shape change.

Shape (F# canonical reference; C#/Rust/TS conform later):
- `DynamicValue` DU (8 cases) + `DynamicValueType` tag + `typeOf` (the runtime
  QueryInterface — exhaustive match, no wildcard, so future variants break consumers loudly).
- Lazy bind: strict `try*` accessors (bind matching shape, decline the rest; no widening).
- `tryField` / `tryItem` + `PropertyPath` `get "a.b[3].c"` navigation (declines miss /
  out-of-range / type-mismatch / malformed path; empty path = identity).
- Hand-written structural equality (`[<CustomEquality; NoComparison>]`): `Bytes` compares
  CONTENTS not the `ImmutableArray` reference; arrays/objects recurse; `Object` is
  order-sensitive (insertion order preserved; canonical encoders sort on the wire).

Format-specific extras (CBOR tags, BSON dates/ObjectId, decimal128, msgpack ext) + the wire
codecs + canonical-encoding golden vectors are open follow-ups. 13 tests, build 0-warning.

Co-Authored-By: Claude <noreply@anthropic.com>
```

### `3c322f122daab031ef2a3de4b700702726c0accb` -- 2026-06-01T22:43:32Z -- ``

**Touched files (intersect with thread paths):**

- `src/Core/DynamicValue.fs`

**Message:**

```
fix(Core): DynamicValue path index uses Int32.TryParse — oversized in…

…dex -> None, not OverflowException

Codex P2: `get "items[99999999999999999999]"` threw OverflowException via Int32.Parse
instead of returning None, violating the documented option-returning contract for
malformed/out-of-range paths (and reintroducing exception-based control flow). Switched to
Int32.TryParse — an index that overflows Int32 is now treated as a malformed path -> None.
Regression test added.

Co-Authored-By: Claude <noreply@anthropic.com>
```

### `f205e3a02c8be82cf3d2bc54fab902b24e5b9017` -- 2026-06-01T22:50:23Z -- ``

**Touched files (intersect with thread paths):**

- `docs/PRIMITIVE-REGISTRY.md`

**Message:**

```
docs(registry): unescape pipes in DynamicValue case list (bullet, not…

… a table)

Copilot: the inline-code DU case list + the deserialize spans used escaped pipes (\|), which render
the backslash literally outside a table. This line is a bullet, not a table row, so plain | is correct.

Co-Authored-By: Claude <noreply@anthropic.com>
```
