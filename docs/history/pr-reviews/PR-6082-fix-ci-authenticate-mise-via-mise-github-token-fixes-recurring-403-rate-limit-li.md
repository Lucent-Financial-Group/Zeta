# PR #6082 -- fix(ci): authenticate mise via MISE_GITHUB_TOKEN (fixes recurring 403 rate-limit lint flake)

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 6082 |
| Title | fix(ci): authenticate mise via MISE_GITHUB_TOKEN (fixes recurring 403 rate-limit lint flake) |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-05-30T05:16:04Z |
| Merged at | 2026-05-30T05:22:22Z |
| Merge commit SHA | `abe75ff46de07c608558a7f5139f9623a20551f3` |
| Branch | `otto-cli/fix-mise-github-token-ci-flake-2026-05-30` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/6082 |
| Changed files | 1 |
| Additions / deletions | +26 / -6 |

## Description

## What this is
The CI "Install toolchain" step (`tools/setup/common/mise.sh`) ran **`env -u GITHUB_TOKEN mise install`** — deliberately *dropping* the token so mise looked up tool releases **anonymously**. Anonymous GitHub API = **60 req/hr**; under multi-PR load that exhausts and every release-metadata fetch (semgrep / shellcheck / actionlint / uv) **403s**. mise logged `No GitHub token was found … GitHub rate limit exceeded`.

Empirically flaked 3+ lint jobs in one session: #6080 (`lint (semgrep)`), #6081 (`lint (no python files)` + `lint (semgrep)`).

## The fix
Promote the Actions `GITHUB_TOKEN` → **`MISE_GITHUB_TOKEN`**, mise's highest-precedence GitHub-auth var (`MISE_GITHUB_TOKEN > GITHUB_API_TOKEN > GITHUB_TOKEN`, per [mise docs](https://mise.jdx.dev/dev-tools/github-tokens.html), verified 2026-05-30) → **authenticated** calls at **1000/hr**. Routing through mise's own auth path (not leaking the bare env var) reads public cross-repo release metadata fine.

## The tradeoff (please be aware)
This **reverses a deliberate prior choice.** The original `env -u GITHUB_TOKEN` was chosen to avoid **404s** once seen when the *bare* `GITHUB_TOKEN` was reused cross-repo. I preserved that history in the code comment + a documented escalation path: **if cross-repo 404s reappear, set a dedicated fine-grained `MISE_GITHUB_TOKEN` repo secret** (still highest-precedence, so this branch no-ops). Net: trades the worse, *recurring* 403-rate-limit flake for the standard authenticated path.

## Scope
Single-source fix — `install.sh` is consumed three ways (dev / CI / devcontainer, GOVERNANCE §24), so this fixes the flake for **every** workflow that runs `install.sh` (gate.yml lint matrix + cadences), not just one.

`bash -n` + `shellcheck` clean.

🤖 Generated with [Claude Code](https://claude.com/claude-code)


## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | true |
| Total threads | 1 |
| Resolved threads | 1 |
| Unresolved threads | 0 |
| Total review comments | 2 |
| Total fix commits (touching thread paths) | 2 |

## Review threads

### Thread 1 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F1_ZC`
- **Path**: `tools/setup/common/mise.sh`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-30T05:17:11Z on `tools/setup/common/mise.sh`:49 (association: NONE)

  <pre>
  P1: This still passes the bare `GITHUB_TOKEN` through to `mise install`; the inline assignment only adds `MISE_GITHUB_TOKEN` and does not remove the original variable from the child environment. That contradicts the documented intent to avoid the bare-token path and can reintroduce the cross-repo lookup behavior this branch is trying to avoid if mise/aqua or a plugin reads `GITHUB_TOKEN` directly. Keep the promoted token but unset the original for the child process.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-30T05:19:06Z on `tools/setup/common/mise.sh`:49 (association: MEMBER)

    <pre>
    Addressed in 94746cbc3 — now `env -u GITHUB_TOKEN MISE_GITHUB_TOKEN="$GITHUB_TOKEN" mise install`: the bare GITHUB_TOKEN is removed from the child env (no direct-read 404 surface) while the value rides via mise’s MISE_GITHUB_TOKEN auth path. Good catch.
    </pre>

## Fix commits (touching thread paths)

### `11598bdcebf062de3992a96e096ab0ff65f386fc` -- 2026-05-30T05:15:54Z -- ``

**Touched files (intersect with thread paths):**

- `tools/setup/common/mise.sh`

**Message:**

```
fix(ci): authenticate mise via MISE_GITHUB_TOKEN instead of dropping …

…the token (CI 403 rate-limit flake)

The CI toolchain-install step (tools/setup/common/mise.sh) was running
`env -u GITHUB_TOKEN mise install` — deliberately dropping the token so mise
looked up tool releases ANONYMOUSLY. Anonymous GitHub API = 60 req/hr; under
multi-PR load that exhausts and every release-metadata fetch
(semgrep/shellcheck/actionlint/uv) 403s. Empirically flaked 3+ lint jobs in one
session (#6080 semgrep; #6081 lint-no-python + semgrep); mise logged
'No GitHub token was found ... GitHub rate limit exceeded'.

FIX: promote the Actions GITHUB_TOKEN to MISE_GITHUB_TOKEN — mise's
highest-precedence GitHub-auth var (MISE_GITHUB_TOKEN > GITHUB_API_TOKEN >
GITHUB_TOKEN; https://mise.jdx.dev/dev-tools/github-tokens.html, verified
2026-05-30) — so mise makes AUTHENTICATED calls (1000/hr) for public release
metadata. Routing through mise's own auth path (not the bare env var) reads
public cross-repo releases fine.

Preserves the original 404-history note + a documented revert/escalation path
(dedicated fine-grained MISE_GITHUB_TOKEN secret) per honor-those-that-came-before.
Single-source fix: install.sh is consumed three ways (dev/CI/devcontainer per
GOVERNANCE §24), so this fixes the flake for every workflow that runs install.sh.

bash -n + shellcheck clean.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `94746cbc389c4c7c06b918740eaa25d7711787e2` -- 2026-05-30T05:18:55Z -- ``

**Touched files (intersect with thread paths):**

- `tools/setup/common/mise.sh`

**Message:**

```
fix(ci): also unset bare GITHUB_TOKEN for the mise child (Copilot P1 …

…on #6082)

The first cut set MISE_GITHUB_TOKEN but left the bare GITHUB_TOKEN in the child
environment — a mise plugin / aqua path reading GITHUB_TOKEN directly could still
trigger the cross-repo 404 this branch guards against, and it contradicted the
comment's 'not leaking the bare env var' claim. Use
`env -u GITHUB_TOKEN MISE_GITHUB_TOKEN="$GITHUB_TOKEN" mise install`: the bare
token is removed from the child, the value rides via mise's own MISE_GITHUB_TOKEN
auth path (shell-expanded before env runs). Authenticated AND no bare-token leak.

bash -n + shellcheck clean.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```
