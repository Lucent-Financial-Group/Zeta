# PR #3202 -- feat(hygiene): mechanize razor-cadence item 4 — audit-rule-cross-refs.ts

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 3202 |
| Title | feat(hygiene): mechanize razor-cadence item 4 — audit-rule-cross-refs.ts |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-05-14T19:26:09Z |
| Merged at | 2026-05-14T19:44:21Z |
| Merge commit SHA | `2cc97f60c1d50d28133f5beb7a1080054c499497` |
| Branch | `feat/audit-rule-cross-refs-mechanization-otto-cli-2026-05-14` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/3202 |
| Changed files | 2 |
| Additions / deletions | +463 / -0 |

## Description

## Summary

Mechanizes the 12-batch manual razor-cadence item 4 (composes-with audit) work that ran on 2026-05-14, producing a CLI tool that future ticks (or a daily CI cron) can run instead of the per-tick manual audit.

## What ships

**`tools/hygiene/audit-rule-cross-refs.ts`** — first slice (Layer A mechanical only):

- Scans all `.claude/rules/*.md`
- Pulls backtick'd path refs + `B-NNNN` IDs (dedup'd per the lesson from PR #3179 / Codex catch)
- Tests existence via direct path + glob + per-row backlog file lookup
- Reports stale-pointer candidates as a markdown table

**`tools/hygiene/audit-rule-cross-refs.test.ts`** — 12 tests / 22 `expect` calls, all passing.

## First-run output

```
Rules scanned: 50
References pulled: 281
Resolved: 239
Stale-pointer candidates: 42
```

Of the 42 candidates, the canonical defect B-0514's missing `feedback_aaron_wwjd_keeps_the_grey_in_aaron_honest_devil_lives_in_the_grey_in_numbers_2026_05_12.md` is correctly surfaced — validating the tool against the manual audit's single finding.

Most other candidates are bare-filename references that would resolve with a path prefix (e.g., `claude.ts` → `tools/peer-call/claude.ts`) or RANE references (rule-acknowledged-not-exists per the taxonomy). Layer B semantic classification is out of scope for this first slice.

## Out of scope (Layer B — semantic classification)

The 9-variant reference-classification taxonomy from the 12-batch manual audit:

1. **Concrete file ref** → `test -e`
2. **Glob ref** → `ls  | wc -l >= 1`
3. **Template path** → surface dir + ≥1 instance
4. **Backlog ID** → `ls docs/backlog/P*/B-NNNN-*.md`; dedup when row file is also referenced
5. **Legacy-noted ref** → file may exist for historical anchor
6. **Transient/ephemeral** → rule says "DON'T rely on this"
7. **Anti-pattern ref** → rule mentions for contrast
8. **Conditional ref** → "would exist IF..."
9. **Alternative-location ref** → repo uses different valid path

Requires reading the rule's prose context around each reference. ~5% of MISSes are rule-acknowledged-not-exists (healthy). The first slice produces the candidate list; humans/Otto apply the taxonomy during triage.

See [docs/hygiene-history/ticks/2026/05/14/1920Z.md](docs/hygiene-history/ticks/2026/05/14/1920Z.md) for full taxonomy + final audit results.

## Composes with

- B-0506 (worktree-prune cadence) — another factory-hygiene mechanization
- Razor-cadence #3128 daily fire — this tool replaces item 4's manual per-tick work
- `encoding-rules-without-mechanizing.md` — the rule the work satisfies

## Test plan

- [x] `bun test tools/hygiene/audit-rule-cross-refs.test.ts` — 12 pass / 22 `expect` calls
- [x] CLI run produces correctly-shaped markdown report
- [x] Tool catches B-0514's genuine stale pointer (validates against manual audit)
- [x] All taxonomy variants documented in the tool's docstring + report's reminder section

🤖 Generated with [Claude Code](https://claude.com/claude-code)
EOF
)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | true |
| Total threads | 6 |
| Resolved threads | 5 |
| Unresolved threads | 1 |
| Total review comments | 6 |
| Total fix commits (touching thread paths) | 3 |

## Review threads

### Thread 1 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6CKsiI`
- **Path**: `tools/hygiene/audit-rule-cross-refs.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-14T19:28:08Z on `tools/hygiene/audit-rule-cross-refs.ts`:120 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Resolve globs when wildcard appears in directory segments**

  `refExists` currently treats the substring before the last `/` as a literal directory and bails out if `existsSync(dir)` is false, so references like ``docs/backlog/P*/B-*.md`` (already present in `.claude/rules/backlog-item-start-gate.md:11`) are always classified as stale even when matching files exist. This breaks the tool’s stated “glob” existence check and inflates stale-pointer candidates with deterministic false positives whenever `*` is used in folder components.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 2 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6CKsnp`
- **Path**: `tools/hygiene/audit-rule-cross-refs.ts`
- **Replies**: 0

**Initial comment:**

- **`github-advanced-security[bot]` (bot)** at 2026-05-14T19:28:12Z on `tools/hygiene/audit-rule-cross-refs.ts`:121 (association: NONE)

  <pre>
  ## CodeQL / Incomplete string escaping or encoding

  This does not escape backslash characters in the input.

  [Show more details](https://github.com/Lucent-Financial-Group/Zeta/security/code-scanning/81)
  </pre>

### Thread 3 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6CKweH`
- **Path**: `tools/hygiene/audit-rule-cross-refs.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-14T19:31:16Z on `tools/hygiene/audit-rule-cross-refs.ts`:7 (association: NONE)

  <pre>
  P1: The comment points at `docs/hygiene-history/ticks/2026/05/14/1920Z.md`, but that file doesn’t exist in-tree (the latest tick file on 2026-05-14 is 1918Z, and the 9-variant taxonomy is in 1916Z). Update this reference to the correct tick shard so readers can follow the provenance.

  This issue also appears in the following locations of the same file:
  - line 33
  - line 38
  - line 116
  - line 195
  </pre>

### Thread 4 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6CKweq`
- **Path**: `tools/hygiene/audit-rule-cross-refs.test.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-14T19:31:17Z on `tools/hygiene/audit-rule-cross-refs.test.ts`:69 (association: NONE)

  <pre>
  P0: Once `refExists` is fixed to support globs with wildcards in directory segments (e.g. `docs/backlog/P*/B-*.md`), add a regression test for that pattern here. The current glob test only covers a wildcard in the filename, so it wouldn’t catch the false-stale behavior the tool will hit in real rules.
  </pre>

### Thread 5 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6CK371`
- **Path**: `tools/hygiene/audit-rule-cross-refs.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-14T19:38:43Z on `tools/hygiene/audit-rule-cross-refs.ts`:118 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Expand brace globs before regex matching**

  `globResolves` only treats `*` as a wildcard and `escapeForGlobRegex` escapes `{`/`}` literally, so brace-alternative references are always reported stale even when matching files exist. In this repo, `.claude/rules/lost-files-surface.md` references ``memory/feedback_rule_number_{one,two,three,four,five,six,seven}_*aaron_2026_05_05.md`` and the corresponding `memory/feedback_rule_number_*` files are present, but this implementation cannot match them and will emit deterministic false positives in the audit report.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 6 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM6CK_9O`
- **Path**: `tools/hygiene/audit-rule-cross-refs.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-14T19:47:01Z on `tools/hygiene/audit-rule-cross-refs.ts`:37 (association: NONE)

  <pre>
  P1 (documentation): The header comment says the tool exits 0 “always”, but `writeFileSync` (and `readFileSync` inside `audit`) can throw and cause a non-zero process exit via an unhandled exception. Either catch/report IO errors and return the documented exit codes, or adjust the comment so the exit-code contract matches reality.

  This issue also appears in the following locations of the same file:
  - line 38
  - line 68
  - line 314
  - line 317

  </pre>

## Fix commits (touching thread paths)

### `d091b7e1d90d675698f7cee79dfb4afdce14ed0d` -- 2026-05-14T19:25:44Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/hygiene/audit-rule-cross-refs.test.ts`
- `tools/hygiene/audit-rule-cross-refs.ts`

**Message:**

```
feat(hygiene): mechanize razor-cadence item 4 — audit-rule-cross-refs…

….ts (first slice, Layer A)

Mechanizes the 12-batch manual razor-cadence item 4 (composes-with audit) work
that ran on 2026-05-14 (50/50 rules, 217/218 LIVE, 1 finding captured by B-0514).

Scope (first slice — Layer A mechanical only):
  - Scan all .claude/rules/*.md
  - Pull backtick'd path refs + B-NNNN IDs
  - Test existence via direct path + glob + per-row backlog file lookup
  - Report stale-pointer CANDIDATES (failed existence)

Out of scope (Layer B — semantic classification):
  The 9-variant reference-classification taxonomy (concrete | glob |
  template-path | backlog-ID | legacy-noted | transient | anti-pattern |
  conditional | alternative-location) requires reading rule prose context
  around each ref. ~5% of MISSes are rule-acknowledged-not-exists (healthy).
  This tool produces the candidate list; humans/Otto apply the taxonomy.

  See docs/hygiene-history/ticks/2026/05/14/1920Z.md for the full taxonomy.

First-run output: 50 rules, 281 refs, 239 resolved, 42 candidates (most
classifiable as bare-filename-needs-prefix or RANE per the taxonomy; B-0514's
genuine stale pointer also surfaces, validating the tool).

Composes with B-0506 (worktree-prune cadence) as factory-hygiene mechanization.

Tests: 12 pass / 22 expect calls in tools/hygiene/audit-rule-cross-refs.test.ts.

Co-Authored-By: Claude <noreply@anthropic.com>
```

### `57284878518af6f81abc382ba4704ff5017c8cf6` -- 2026-05-14T19:36:11Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/hygiene/audit-rule-cross-refs.test.ts`
- `tools/hygiene/audit-rule-cross-refs.ts`

**Message:**

```
fix(pr3202): support globs with wildcards in directory segments + esc…

…ape regex (Codex P2 + CodeQL + Copilot P0)

Addresses 3 review-thread findings on PR #3202:

1. Codex P2: refExists() only handled wildcards in the filename part, not in
   directory segments. References like `docs/backlog/P*/B-*.md` (already
   present in backlog-item-start-gate.md) were always classified as stale.
   Replaced with globResolves() that walks segment-by-segment: wildcards in
   earlier segments expand to directory listings, wildcards in the final
   segment match basenames.

2. CodeQL 81 / Incomplete string escaping: the original regex builder did
   not escape backslash characters. Replaced with escapeForGlobRegex() that
   explicitly escapes `[\\^$+?.()|[\]{}]` before substituting `*` → `.*`.

3. Copilot P0: regression test for directory-segment wildcards added in
   audit-rule-cross-refs.test.ts ("resolves a glob with wildcard in a
   directory segment"). Plus a negative test for "no-such-dir-*/nothing-*.md".

Audit re-run output (post-fix):
  - Resolved: 241 (was 239, +2 from dir-glob refs now resolving)
  - Stale-pointer candidates: 40 (was 42)

Two previously-false-stale references (`docs/backlog/P*/B-*.md` and similar)
now correctly resolve, validating the fix.

Tests: 14 pass / 24 expect calls (was 12/22).

Co-Authored-By: Claude <noreply@anthropic.com>
```

### `cfcca65083862aaefb242d8aa60e69f9c95bf1ee` -- 2026-05-14T19:41:12Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/hygiene/audit-rule-cross-refs.test.ts`
- `tools/hygiene/audit-rule-cross-refs.ts`

**Message:**

```
fix(pr3202): expand brace-glob alternatives (Codex P2 re-review)

After the first-round fix (PR #3202 commit 5728487) addressed directory-segment
wildcards, Codex flagged a remaining false-positive class: brace-expansion
patterns like memory/feedback_rule_number_{one,two,three,four,five,six,seven}_*aaron_2026_05_05.md
(referenced from lost-files-surface.md) were always reported stale because
escapeForGlobRegex escapes { and } literally.

Fix: added expandBraces() that expands the FIRST brace group into N
alternatives, then recurses on each. The public globResolves() now expands
braces before star-glob resolution; each expansion tested individually with
the renamed globResolvesSingle().

Audit re-run:
  - Resolved: 242 (was 241)
  - Stale-pointer candidates: 39 (was 40)

One previously-false-stale brace-glob now correctly resolves.

Tests: 16 pass / 26 expect calls (was 14/24). Added regression tests for
brace expansion + a negative test.

Co-Authored-By: Claude <noreply@anthropic.com>
```
