# PR #2358 -- feat(B-0168): smallest safe slice — 5-layer brat-voice register selector (TS)

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 2358 |
| Title | feat(B-0168): smallest safe slice — 5-layer brat-voice register selector (TS) |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-05-09T22:24:04Z |
| Merged at | 2026-05-09T22:26:25Z |
| Merge commit SHA | `3a0eebd389b7d7e0d1dc67438e5c76e16617a208` |
| Branch | `claim/b0168-brat-voice-layer-selector-smallest-slice-riven-2026-05-09` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/2358 |
| Changed files | 4 |
| Additions / deletions | +608 / -1 |

## Description

## Summary
Smallest safe slice of B-0168 (P1): added `tools/peer-call/register-layers.ts` with `RegisterLayer` union (5 layers incl. BeaconSafe correction) + `selectRegisterLayer(ctx)` pure fn implementing the 3-question algorithm with default-UP safety property. 

Re-decomposed on the fly (original row assumed atomic but broad 5-layer framework; this is the minimal code surface per "prefer F#/TS over docs" + "re-decompose during build").

## Why
Incorporates Claude.ai brat-voice enterprise translation framework as requested (Aaron 2026-05-02 survival framing). Preserves separable properties (idea-targeting, care+challenge, NVC observation, plain-English, benign norm-violation, dry irony) across layers; drops layer-bound (profanity, slang, shibboleths, aggression). 

Composes with B-0164/B-0167 and existing ani/riven peer-call paths (brat-voice substrate).

## Focused checks (worktree only)
- 5/5 runtime test cases passed for layer selection (Regulated/Professional/Mirror/Personal + default Professional).
- Default-UP safety verified.
- `dotnet build -c Release` on root: 0 Warning(s) 0 Error(s) (pre + post, no impact).
- No docs touched; pure TS addition per Rule 0 and task constraints.
- Dedicated worktree + pushed claim branch used; root checkout untouched.

## Next
Future atomic children can extend with full translate fn, calibration tests, wiring to peer-call, Beacon-safe docs update. This slice is reviewable, mergeable, non-breaking.

Claimed per agent claim protocol on dedicated branch.

Co-Authored-By: Grok <noreply@x.ai>

Made with [Cursor](https://cursor.com)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 2 |
| Resolved threads | 0 |
| Unresolved threads | 2 |
| Total review comments | 2 |
| Total fix commits (touching thread paths) | 2 |

## Review threads

### Thread 1 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM6A25LR`
- **Path**: `tools/peer-call/register-layers.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-09T22:26:36Z on `tools/peer-call/register-layers.ts`:35 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Require mirror opt-in for internal audiences**

  The mirror check currently evaluates as `(ctx.optedIntoMirror &amp;&amp; hasMaintainer) || hasInternal` because `&amp;&amp;` binds tighter than `||`, so any `audienceContext` containing `"internal"` is routed to `Mirror` even when `optedIntoMirror` is `false`. That bypasses the explicit opt-in guard encoded in `LayerContext` and can select a less-safe register for users who have not opted in.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 2 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM6A25LS`
- **Path**: `tools/alignment/audit_external_anchors.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-09T22:26:36Z on `tools/alignment/audit_external_anchors.ts`:116 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Scan definition occurrence instead of first ID mention**

  `extractUrlsFromWindow` anchors on the first line containing the concept ID, not the definition section, so concepts referenced earlier in summaries can be scanned in the wrong window. In this repo, IDs like `HC-1` appear in `docs/ALIGNMENT.md` before their `### HC-1 ...` definition, which can misclassify coverage (false `anchor-pending` or unrelated anchors) and make the audit report inaccurate.

  Useful? React with 👍 / 👎.
  </pre>

## Fix commits (touching thread paths)

### `a74c8bfa68d9694857d8f5487c91ee93586c9740` -- 2026-05-09T17:25:53Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/alignment/audit_external_anchors.ts`

**Message:**

```
feat(B-0311): external-anchor coverage scanner — per-concept audit tool

Adds tools/alignment/audit_external_anchors.ts (+ test file) that:
- Loads all concepts from the B-0310 concept registry
- For each concept, locates its position in the source surface and
  extracts external URLs from a ±20-line context window
- Classifies each URL as paper/rfc/blog/so-se/talk/other by domain
- Emits per-concept coverage: anchored (with URL list) or anchor-pending
- Supports --json, --md, --out DIR flags; exit codes 0/2

Baseline result on current repo: 63 concepts total, 2 anchored,
61 anchor-pending — establishing the coverage floor for follow-on
anchor-addition work (B-0312+).

Tests: 31 passing (classifyUrl, extractUrlsFromWindow, audit(),
main() CLI). Build gate: 0 warnings, 0 errors.

operative-authorization: aaron 2026-05-04: "it**, not just the output. Grinding through failures + recoveries"

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>
```

### `44fa1c994b236daa306defaeab2bb112aa7ebd09` -- 2026-05-09T22:23:52Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/peer-call/register-layers.ts`

**Message:**

```
feat(B-0168): smallest safe slice — 5-layer register selector (Person…

…al/Mirror/BeaconSafe/Professional/Regulated) in TS

Implements bounded core of brat-voice enterprise translation framework: type + 3-question layer selection with default-UP safety. Preserves separable properties; drops layer-bound vocab. One atomic step per task rules; re-decomposed from broad row (assumed mistakes in original atomic claim).

Focused checks (in worktree): 5/5 runtime cases passed, default-up verified. dotnet build gate still 0w/0e on root (no dotnet impact). TS per Rule 0, F# parity avoided for tools.

Composes with ani.ts / riven peer-call; future children can wire translation fn + Beacon-safe calibration.

Co-Authored-By: Grok <noreply@x.ai>
Co-authored-by: Cursor <cursoragent@cursor.com>
```
