# PR #6375 -- docs(registry): mixin prior-art + Moxy-style bake-in variant behind a hexagonal port

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 6375 |
| Title | docs(registry): mixin prior-art + Moxy-style bake-in variant behind a hexagonal port |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-06-01T08:30:44Z |
| Merged at | 2026-06-01T08:41:32Z |
| Merge commit SHA | `a75f3bbeb5a62291b253e41e654ed557e258a18e` |
| Branch | `otto-cli/registry-moxy-variant-hexagonal-port-2026-06-01` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/6375 |
| Changed files | 1 |
| Additions / deletions | +1 / -1 |

## Description

Follow-up to #6374, landing two things from this session:

**1. Prior art (you asked me to search the ecosystem).** Attribute-driven source-gen mixins already exist:

- [Morris.Moxy](https://github.com/mrpmorris/Morris.Moxy) — Roslyn source-gen, mixes members **into** the target class
- [Fody](https://github.com/Fody/Fody) — post-compile **IL-weaving**, raw-perf bake-in
- C# 8 **default interface methods** — behavior-only, **no per-instance state**

All three optimize for raw-perf-bake-in → you must **own** the type and the mixed-in state's lifetime couples to the type's → **not unload-safe for plugins**. So the **weak-keyed side-table stays the baseline** precisely for the reason you named: it decouples (attach to foreign types + GC-collectible → the plugin can still unload, the whole point of the collectible-`AssemblyLoadContext` line).

**2. A Moxy-style bake-in variant is worth having TOO — behind the same hexagonal port** (your call: _"as long as we hexagonal interface it"_). One `IMixin`-style port, two adapters: **weak-keyed-side-table** (unload-safe / foreign-type / default) and **bake-in-source-gen** (raw-perf / owns-type). The **perf-vs-unload tradeoff becomes the adapter choice**, selected per use case — not a fork. Own the port; Moxy/Fody are swappable adapters into it (own-your-interfaces).

Docs-only; prettier + markdownlint clean; BACKLOG.md untouched.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 1 |
| Resolved threads | 1 |
| Unresolved threads | 0 |
| Total review comments | 1 |
| Total fix commits (touching thread paths) | 2 |

## Review threads

### Thread 1 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GDWTy`
- **Path**: `docs/PRIMITIVE-REGISTRY.md`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-01T08:32:16Z on `docs/PRIMITIVE-REGISTRY.md`:69 (association: NONE)

  <pre>
  The phrase "attribute-driven source-gen mixins already exist" is inaccurate given the examples that follow (Fody is IL weaving, and default interface methods are a language/runtime feature rather than source generation). Also, the parenthetical "DIM" acronym is introduced without definition, which makes this section harder to read.
  </pre>

## Fix commits (touching thread paths)

### `09e6b0f9e44bfe34c64c6a053bf883e80cc77a8a` -- 2026-06-01T08:30:40Z -- ``

**Touched files (intersect with thread paths):**

- `docs/PRIMITIVE-REGISTRY.md`

**Message:**

```
docs(registry): mixin prior-art + Moxy-style bake-in variant behind h…

…exagonal port (maintainer)

Search-first (per the maintainer's prompt to check the ecosystem): attribute-driven
source-gen mixins already exist — Morris.Moxy (Roslyn source-gen, bakes members into the
type), Fody (IL-weaving, raw-perf bake-in), C# 8 default interface methods (behavior-only,
no state). All optimize for raw-perf-bake-in → own-the-type + couple state lifetime to the
type → NOT unload-safe for plugins. The weak-keyed side-table stays the baseline because it
decouples (foreign-type + GC-collectible → plugin unloads).

Maintainer point (2026-06-01): a Moxy-style bake-in variant is worth having TOO 'as long as
we hexagonal interface it' — one IMixin port, two adapters (weak-keyed-side-table = unload-safe
default; bake-in-source-gen = raw-perf owns-type); perf-vs-unload is the adapter choice, not a
fork. Own the port; Moxy/Fody are swappable adapters into it (own-your-interfaces).

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `77af0da6d32b409b4d023844da6a6e968d9f3759` -- 2026-06-01T08:39:26Z -- ``

**Touched files (intersect with thread paths):**

- `docs/PRIMITIVE-REGISTRY.md`

**Message:**

```
docs(registry): fix mixin prior-art wording — 3 techniques, not all s…

…ource-gen (Copilot #6375)

Copilot correctly flagged 'attribute-driven source-gen mixins already exist' as inaccurate:
only Moxy is source-gen; Fody is IL-weaving; default interface methods are a language/runtime
feature. Reworded to 'three different techniques' with each correctly labeled — which actually
sharpens the point: across ALL three, they bake into / couple to the type, so none is
unload-safe for plugins (the weak-keyed side-table's differentiator).

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```
