# PR #3438 -- fix(b-0503): make branch name deterministic so idempotency gate works (PR #3433 P0)

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 3438 |
| Title | fix(b-0503): make branch name deterministic so idempotency gate works (PR #3433 P0) |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-05-15T11:35:59Z |
| Merged at | 2026-05-15T11:50:31Z |
| Merge commit SHA | `575a3baa46bf71726da541ae76d06be38c11684d` |
| Branch | `fix/b0503-idempotency-deterministic-branch-name-otto-cli-2026-05-15` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/3438 |
| Changed files | 2 |
| Additions / deletions | +124 / -26 |

## Description

## Summary

Follow-up to [PR #3433](https://github.com/Lucent-Financial-Group/Zeta/pull/3433). Four review findings all addressed in this PR.

### P0 — Idempotency gate defeated by timestamp

The earlier `recovery/<prNumber>-<timestamp>` form made every `openRecoveryPR()` call produce a fresh branch name, so `checkRecoveryPRExists(recoveryBranch)` could effectively never match an existing PR. The spec's claim that "the idempotency gate prevents duplicate recovery PRs" didn't hold because the gate's input changed every invocation. Fix: deterministic `recovery/<prNumber>` (drop the timestamp).

New regression test (`queries checkRecoveryPRExists with the deterministic branch name`) locks in the contract.

### P? — Backtick in `branchName` breaks PR body
Added `sanitizeForInlineCode` helper; backticks in branchName replaced with `_` before the inline-code interpolation. New test covers the case.

### P? — Unused `RecoveryResult` type import in tests
Removed.

### P? — Test description/regex mismatch
Old: "digits, /, -" but regex permitted `a-z`. Replaced both with the actual `^recovery\/\d+$` for the new deterministic form.

## Test count: 14 → 16

Added: sanitize-backticks coverage, P0 regression test, deterministic-output test, idempotency-precondition (same prNumber two Dates → same branch name).

## Test plan
- [x] `bunx tsc --noEmit` clean
- [x] `bun test missed-substrate-recovery.test.ts` — 16 pass / 0 fail
- [x] `bun test missed-substrate-detector.test.ts` — 24 pass (baseline unchanged)
- [x] `semgrep --config .semgrep.yml --error` — 0 findings
- [ ] CI green
- [ ] Auto-merge fires

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | true |
| Total threads | 3 |
| Resolved threads | 3 |
| Unresolved threads | 0 |
| Total review comments | 3 |
| Total fix commits (touching thread paths) | 4 |

## Review threads

### Thread 1 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6CWAlP`
- **Path**: `tools/bg/missed-substrate-recovery.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-15T11:37:32Z on `tools/bg/missed-substrate-recovery.ts`:48 (association: NONE)

  <pre>
  The retained unused `_ts: Date` parameter (and matching `new Date()` call site at line 108) conflicts with the project's "Cutting-edge over legacy-compat — no `[Obsolete]`, no 'for backwards compatibility' comments. Delete the old shape in the same diff that replaces it" principle from `.github/copilot-instructions.md` (Values #5). The only caller is `openRecoveryPR` in the same file, so there is no external back-compat constraint to preserve. Recommend dropping the `_ts` parameter from `buildRecoveryBranchName`, removing the `new Date()` argument at line 108, and updating the three test call sites (lines 89, 96–97, 102) to pass only `prNumber`. The docstring's "retained for backward compatibility" note (lines 44–46) can then go away as well.
  </pre>

### Thread 2 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6CWCGb`
- **Path**: `tools/bg/missed-substrate-recovery.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-15T11:38:53Z on `tools/bg/missed-substrate-recovery.ts`:78 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Restore retry-safe uniqueness for recovery branch names**

  Using only `recovery/&lt;prNumber&gt;` makes retries non-recoverable after any partial failure path. If a prior run for the same PR created the branch but then failed later (e.g., cherry-pick conflict, push error, or `gh pr create` error), a subsequent run will reuse the same branch name; the workflow still calls `gitCreateBranch` (`git checkout -b`) and will fail when that branch already exists, while the idempotency gate only checks for an open PR. This turns transient failures into a persistent stuck state for that PR unless manual branch cleanup occurs.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 3 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6CWIxF`
- **Path**: `tools/bg/missed-substrate-recovery.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-15T11:45:27Z on `tools/bg/missed-substrate-recovery.ts`:135 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Handle stale local recovery branch when retrying**

  Using a deterministic `recovery/&lt;prNumber&gt;` name means retries now hit the same local branch, but this path still treats any `gitCreateBranch` failure as fatal. If an earlier attempt failed after creating the branch (e.g., cherry-pick/push/PR-create error), `checkRecoveryPRExists` can return false (no open PR) while `git checkout -b` fails with “branch already exists,” leaving all subsequent retries wedged until manual branch cleanup. The retry-safe behavior should be handled here (or via an explicit adapter contract enforcement) before returning an unrecoverable error.

  Useful? React with 👍 / 👎.
  </pre>

## Fix commits (touching thread paths)

### `212e12f08ed3446c44175fbbc517d77f61d7b6f9` -- 2026-05-15T11:35:25Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/bg/missed-substrate-recovery.ts`

**Message:**

```
fix(b-0503): make branch name deterministic so idempotency gate works

Four PR #3433 review findings, all real:

P0 [Copilot, recovery.ts:90] — Idempotency check defeated by timestamp:
  `recovery/<prNumber>-<timestamp>` made each `openRecoveryPR()` call
  produce a fresh branch name, so `checkRecoveryPRExists(recoveryBranch)`
  could effectively never match an existing PR — the load-bearing
  uniqueness mechanism described in the spec didn't function.
  Fix: deterministic `recovery/<prNumber>` (drop timestamp). Same
  prNumber → same branch name across invocations → gate fires correctly
  on duplicates. New regression test asserts the adapter is called with
  exactly the expected name.

P? [Copilot, recovery.ts:55] — Backtick in branchName breaks PR body:
  `\`${finding.branchName}\`` interpolation would break the inline-code
  span if branchName contained backticks. Added `sanitizeForInlineCode`
  helper (replaces backticks with `_`) before interpolation. New test
  verifies sanitization happens.

P? [Copilot, test:9] — Unused `RecoveryResult` type-only import:
  Removed. The narrowing in tests is done via if-throw guards (already
  in place) rather than direct type annotation.

P? [Copilot, test:104] — Test description/regex mismatch:
  Old test said "digits, /, -" but the regex permitted `a-z` (because
  of the "recovery/" prefix). Replaced both the description and the
  regex with the actual constraint for the new deterministic name:
  `^recovery\/\d+$`.

Test count: 14 → 16 (added 2: sanitize-backticks coverage + the
P0 regression test that asserts branch name is `recovery/<prNumber>`).

Composes with [B-0504](https://github.com/Lucent-Financial-Group/Zeta/blob/main/docs/backlog/P1/B-0503-b0442-slice5a-open-recovery-pr-core-function-2026-05-14.md):
the real adapter `gitCreateBranch` will need to handle the case where
the recovery branch already exists locally (idempotency gate catches
the open-PR case; branch-exists-locally is a separate condition the
adapter must surface so openRecoveryPR can return `error` cleanly).

Verified:
  - bunx tsc --noEmit clean
  - bun test missed-substrate-recovery.test.ts: 16 pass / 0 fail
  - bun test missed-substrate-detector.test.ts: 24 pass (baseline unchanged)
  - semgrep --config .semgrep.yml --error: 0 findings

Co-Authored-By: Claude <noreply@anthropic.com>
```

### `61910bee1edee712bab04695dd093433d788f2c9` -- 2026-05-15T11:39:39Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/bg/missed-substrate-recovery.ts`

**Message:**

```
fix(b-0503): drop unused _ts parameter from buildRecoveryBranchName

Copilot caught a project-convention violation: I retained the `_ts: Date`
parameter "for backward compatibility" but per .github/copilot-instructions.md
Values #5: "Cutting-edge over legacy-compat — no [Obsolete], no 'for
backwards compatibility' comments. Delete the old shape in the same diff
that replaces it."

The only caller is openRecoveryPR in the same file, so there is no
external back-compat constraint to preserve. Dropped:
  - `_ts: Date` parameter from signature
  - `new Date()` argument at the call site
  - "retained for backward compatibility" docstring note
  - `new Date()` / Date-arg pattern from the 3 test call sites

Verified: tsc clean, 16/16 tests still pass.

Co-Authored-By: Claude <noreply@anthropic.com>
```

### `68df386059de3427b136b31987ddee7c0b834fa7` -- 2026-05-15T11:41:39Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/bg/missed-substrate-recovery.ts`

**Message:**

```
docs(b-0503): document retry-safe branch handling for B-0504 adapters

Codex P1 catch: deterministic `recovery/<prNumber>` makes retries
non-recoverable after partial failures (cherry-pick conflict, push
error, gh pr create error) because a subsequent attempt re-uses the
same branch name and `gitCreateBranch` (`git checkout -b`) fails on
the stale local branch.

This is a real concern. The correct fix belongs in B-0504's adapter
implementations (per the B-0503 spec: "real adapter implementations
are scope for B-0504"). Added a docstring section on
`buildRecoveryBranchName` documenting the two viable strategies for
B-0504's gitCreateBranch adapter:

  (a) attempt `git branch -D <recoveryBranch>` before `git checkout
      -b` (safe because we recreate from origin/main); OR
  (b) detect "fatal: A branch named ... already exists" and
      translate it into success (resume on existing branch).

The B-0503 core function correctly surfaces the failure (gitCreateBranch
→ false → RecoveryResult.error); B-0504's adapter chooses the
retry strategy.

Co-Authored-By: Claude <noreply@anthropic.com>
```

### `ff6be7e17f6e2d562955b3a25e43b1b1596e5d08` -- 2026-05-15T11:47:19Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/bg/missed-substrate-recovery.ts`

**Message:**

```
fix(b-0503): make gitCreateBranch retry-safety contract explicit

Codex's follow-up on PR #3438 line 117: my docstring-only response
to the retry-from-stale-branch concern wasn't enough — the contract
needed to be explicit in the adapter type itself so B-0504 can't
accidentally violate it.

Strengthened `RecoveryAdapters.gitCreateBranch` docstring with an
explicit CONTRACT section spelling out the two viable retry
strategies the adapter MUST implement (delete-and-recreate OR
reset-to-base on existing branch). Same docstring also adds a
`--force-with-lease` hint on `gitPush` for the same retry-safety
reason on the remote branch.

Added a regression test "retry path: gitCreateBranch returns true
even when stale branch exists (contract test)" — verifies
openRecoveryPR makes no assumptions about stale-branch handling
beyond what the adapter's boolean return says. Adapter says true →
proceed normally; the core function doesn't impose any retry loop
of its own (retry-safety lives in the adapter's implementation).

Test count: 16 → 17.

Verified: tsc clean, 17/17 tests pass.

Co-Authored-By: Claude <noreply@anthropic.com>
```
