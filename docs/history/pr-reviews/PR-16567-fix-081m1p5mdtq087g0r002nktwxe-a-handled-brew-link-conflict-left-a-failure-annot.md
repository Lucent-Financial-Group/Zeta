# PR #16567 -- fix(081M1P5MDTQ087G0R002NKTWXE): a handled brew link conflict left a failure annotation on a green job

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 16567 |
| Title | fix(081M1P5MDTQ087G0R002NKTWXE): a handled brew link conflict left a failure annotation on a green job |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-09-04T12:18:19Z |
| Merged at | 2026-09-04T12:38:34Z |
| Merge commit SHA | `e43de0ad19865201910fe36ae04cb4e1300cb4fd` |
| Branch | `otto/brew-link-swallowed-annotation` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/16567 |
| Changed files | 3 |
| Additions / deletions | +150 / -2 |

## Description

`drift (loud)` surfaced a subject nothing else could see, on run 33860210941:

```
build-and-test (macos-26) › (step swallowed by continue-on-error)
  1/1 executions failed (100.0%), clean streak 0
  step red, JOB GREEN (no red X anywhere)
```

The job's `conclusion` is `success` and **no step reports `failure`**. The only evidence anywhere is a check-run annotation:

```
##[error]The `brew link` step did not complete successfully
Could not symlink bin/wasm2c
Target /opt/homebrew/bin/wasm2c is a symlink belonging to wabt.
```

## What's actually happening — the script isn't wrong

`binaryen` and `wabt` both ship `wasm2c`. `ZETA_BREW_LINK_OVERWRITE` declares `binaryen` the winner with a written reason, and when `brew install binaryen` fails its link phase the script recovers with `brew link --overwrite`. Install succeeds, step exits 0, job green.

**The residue is the annotation.** Homebrew's `ofail` emits a GitHub Actions error annotation when `GITHUB_ACTIONS` is set — `utils/output.rb` `onoe` → `GitHub::Actions.puts_annotation_if_env_set!`, gated on `env_set?` = `ENV.fetch("GITHUB_ACTIONS", false).present?`.

So a failure we *intended* to recover from leaves a permanent failure annotation on a green job — and through the API that is **indistinguishable from a step swallowed by `continue-on-error`**. Which is exactly what `drift-loud.ts` classifies it as, and why its `SWALLOWED_STEP` subject is keyed on the *job*: the annotation carries no step name.

## The fix: stop failing, don't stop reporting

`brew install --overwrite` links over the conflicting file during install, so the link phase never fails. The end state is identical to what the recovery already produced — `brew link --overwrite` deletes the same `wabt` symlink — so the resulting toolchain is unchanged. What changes is that there's no failure on the way, and therefore no annotation.

**The alternative was worse, and is worth recording.** Unsetting `GITHUB_ACTIONS` for that one call would have suppressed the annotation while leaving the failure happening — hiding a report rather than removing its cause — and would have blinded Homebrew's CI reporting for every other formula in the loop.

**Probed, not assumed.** `--overwrite` isn't in every Homebrew version and this script runs on dev laptops with whatever brew they have, so support is checked once. Where it's absent the install-then-relink path is unchanged: an older brew keeps working *and keeps its annotation*. Honest either way.

## How much of this is verified

Stated precisely, because the failing platform isn't the one I can run:

| claim | evidence |
| --- | --- |
| the mechanism | **read at the source** — `formula_installer.rb` is the only place that string exists; `ofail` → `onoe` → the `GITHUB_ACTIONS`-gated annotation — and corroborated by the CI log, which carries the annotation and the script's own *"DECLARED overwrite winner, relinking"* line four seconds later |
| `brew install --overwrite` exists | checked against brew 6.0.21 locally |
| **could not reproduce locally** | `brew ruby` does not inherit `GITHUB_ACTIONS` (prints `<unset>` when the parent sets it), so the probe I tried cannot reach the gate. That's a property of `brew ruby`, not evidence about `brew install` — recorded rather than glossed |
| no new lint | `shellcheck -s bash`: **0 findings before and after** |

The remaining verification is a green `macos-26` run with **no annotation**, which only CI can produce.

## What this does not resolve

Whether `drift (loud)` should band a *handled* swallow differently from an unhandled one. It can't currently tell them apart, and arguably shouldn't have to — the fix is to not generate the annotation. Filed in the work item because the next handled-recovery pattern will look identical.

Work item: `081M1P5MDTQ087G0R002NKTWXE`

Agency-Signature-Version: 1
Agent: shadow
Agent-Runtime: claude-code/cli
Agent-Model: claude-opus-5[1m]
Credential-Identity: AceHack
Credential-Mode: shared
Human-Review: explicit
Human-Review-Evidence: chat
Action-Mode: human-directed
Task: 081M1P5MDTQ087G0R002NKTWXE

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_01QyTyVdDqxekK5rZQGBm75f


## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 0 |
| Resolved threads | 0 |
| Unresolved threads | 0 |
| Total review comments | 0 |
| Total fix commits (touching thread paths) | 0 |

## Review threads

(no review threads on this PR)

## Fix commits (touching thread paths)

(no commits in this PR touched files anchored by review threads)
