# PR #6591 -- feat(infernet 081KT2T2J0008QG0R000S7GHQ8 slice 4): sum-product BP to a fixed point — runToFixpoint + per-family message distance

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 6591 |
| Title | feat(infernet 081KT2T2J0008QG0R000S7GHQ8 slice 4): sum-product BP to a fixed point — runToFixpoint + per-family message distance |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-06-02T16:31:16Z |
| Merged at | 2026-06-02T16:49:02Z |
| Merge commit SHA | `85b95811c51caa23a4af9bb2fb24d79bd05496c0` |
| Branch | `otto-cli/b1000-slice4-bp-fixpoint-2026-06-02` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/6591 |
| Changed files | 4 |
| Additions / deletions | +162 / -2 |

## Description

## 081KT2T2J0008QG0R000S7GHQ8 slice 4 — sum-product BP to a fixed point

Belief propagation driven to convergence: iterate `passOnce` until no factor→var message moves more than `tol` (per-family `distance`), or `maxRounds` is hit. **On a tree this reaches the exact marginals**; with loops it is loopy BP (capped, like `NestedCircuit`'s 64-iteration LFP cap).

This is the **factor-graph analog of `NestedCircuit.Fixedpoint`** (drive the inner clock to the least fixed point, capped). Wiring the factor graph as literal DBSP operators — for **incremental re-inference on a data delta** (the differentiator vs stock Infer.NET) — is the next integration (slice 4b).

### Code

- `src/Bayesian/FactorGraph.fs`: **`runToFixpoint distance tol maxRounds g`** → `(converged graph, rounds, converged?)`; private `moved` residual test compares factor→var messages between rounds.
- `src/Bayesian/Message.fs` (additive): `Gaussian.distance` / `Beta.distance` / `Bernoulli.distance` (max abs diff of the defining params).

### Tests (`Bp.Tests.fs` — 4/4)

- reaches the exact product marginal + reports converged
- **evidence propagates along an equality chain `x0=x1=x2`** (priors `Beta(2,1)`,`Beta(1,2)`,`Beta(3,1)`) → every marginal = `Beta(4,2)` (all three priors combined) — real multi-round message passing
- respects the round cap (converged=false when capped to 1)
- message distance: 0 for equal, abs-diff otherwise

Spec: Kschischang–Frey–Loeliger 2001 (sum-product). `dotnet build -c Release`: **0 warnings**. `dotnet test`: **4/4**.

Slices: 1 `Vector`/`Wall` (merged) → 2 message algebra (merged) → 3 factor graph (merged) → **4 BP fixpoint (this)** → 4b incremental BP on `NestedCircuit` → 5 EP → 6 seed CE. Refs 081KT2T2J0008QG0R000S7GHQ8.

🤖 Generated with [Claude Code](https://claude.com/claude-code)


## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | true |
| Total threads | 2 |
| Resolved threads | 2 |
| Unresolved threads | 0 |
| Total review comments | 2 |
| Total fix commits (touching thread paths) | 3 |

## Review threads

### Thread 1 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6Gf0a9`
- **Path**: `src/Bayesian/FactorGraph.fs`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-02T16:34:10Z on `src/Bayesian/FactorGraph.fs`:155 (association: NONE)

  <pre>
  `moved` treats `distance m m'` as a plain float, but `distance` can become `NaN` (e.g., `∞ - ∞` in natural params after overflow). In F#, `NaN &gt; tol` is false, so the residual test can incorrectly report “not moved” and make `runToFixpoint` declare convergence even when messages have become invalid. Consider treating `NaN` as “moved” (or failing fast) to avoid silently returning `converged=true` on divergent/overflowed runs.
  </pre>

### Thread 2 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GgBTB`
- **Path**: `src/Bayesian/FactorGraph.fs`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-02T16:44:17Z on `src/Bayesian/FactorGraph.fs`:159 (association: NONE)

  <pre>
  `moved` only checks keys present in `a.FactorToVar`. If `passOnce` (or a future `Factor.ComputeMessages`) produces an extra factor id or extra variable entry that wasn’t present in `a`, this implementation won’t detect it and can incorrectly report convergence. The implementation should compare both directions (`a` vs `b` and `b` vs `a`) to match the docstring (“any message in `b` differs from `a`).
  </pre>

## Fix commits (touching thread paths)

### `341032b70d95cdc0eee7110877a60bf5d5a858c4` -- 2026-06-02T16:30:31Z -- ``

**Touched files (intersect with thread paths):**

- `src/Bayesian/FactorGraph.fs`

**Message:**

```
feat(infernet 081KT2T2J0008QG0R000S7GHQ8 slice 4): sum-product BP to a fixed point — Fact…

…orGraph.runToFixpoint + per-family message distance (Aaron 2026-06-02)

Slice 4 of the Zeta Infer.NET rewrite (081KT2T2J0008QG0R000S7GHQ8): sum-product belief
propagation driven to a fixed point. Iterate passOnce until no factor→var
message moves more than `tol` (per-family `distance`), or `maxRounds` is
hit. On a tree this reaches the EXACT marginals; with loops it is loopy
BP (capped, like NestedCircuit's 64-iteration LFP cap).

This is the factor-graph analog of the DBSP NestedCircuit.Fixedpoint
(drive the inner clock to the least fixed point, capped). Wiring the
factor graph as literal DBSP operators — for INCREMENTAL re-inference on
a data delta (the differentiator vs stock Infer.NET) — is the next
integration (slice 4b).

src/Bayesian/FactorGraph.fs:
- `runToFixpoint distance tol maxRounds g` → (converged graph, rounds,
  converged?). Residual test `moved` compares factor→var messages
  between rounds under the per-family distance.

src/Bayesian/Message.fs (additive): `Gaussian.distance` / `Beta.distance`
/ `Bernoulli.distance` — max abs diff of the defining params (natural
params / shapes / P(true)).

tests/Bayesian.Tests/Bp.Tests.fs (4 tests, all pass):
- runToFixpoint reaches the exact product marginal + reports converged
- BP propagates evidence along an equality CHAIN x0=x1=x2 (priors
  Beta(2,1),Beta(1,2),Beta(3,1)) → every marginal = Beta(4,2) (all
  three priors combined) — real multi-round message passing on a tree
- runToFixpoint respects the round cap (converged=false when capped to 1)
- message distance: zero for equal, abs-diff otherwise

Spec: Kschischang–Frey–Loeliger 2001 (sum-product). dotnet build
-c Release: 0 warnings. dotnet test: 4/4 pass.

Slices: 1 Vector/Wall (merged) → 2 message algebra (merged) → 3 factor
graph (merged) → 4 sum-product BP fixpoint (this) → 4b incremental BP on
NestedCircuit → 5 EP → 6 seed CE. Refs 081KT2T2J0008QG0R000S7GHQ8.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `4236989b78c3e3c312a88cbe11eb332e127dd431` -- 2026-06-02T16:38:48Z -- ``

**Touched files (intersect with thread paths):**

- `src/Bayesian/FactorGraph.fs`

**Message:**

```
fix(infernet 081KT2T2J0008QG0R000S7GHQ8 slice 4): no false BP convergence on NaN/∞ residu…

…al — Copilot review

Copilot: `moved` used `distance m m' > tol`, but `NaN > tol` is false in
F#, so a divergent/overflowed run (messages → ∞/NaN) could silently
report converged=true. Two-part fix:
- FactorGraph.moved: `not (distance m m' <= tol)` so a NaN residual
  counts as MOVED (no false convergence).
- Message.{Gaussian,Beta,Bernoulli}.distance: return `infinity` when any
  parameter is non-finite (∞/NaN), so `max` can't mask a NaN into a small
  residual — a non-finite message always counts as moved.

test (+1 → 5 Bp tests): infinite-precision messages (record literal,
bypassing the validated constructor) give a non-finite distance and
runToFixpoint reports converged=false (no false convergence on overflow).

dotnet build -c Release: 0 warnings. dotnet test: 5/5 pass. Refs 081KT2T2J0008QG0R000S7GHQ8.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `56da8e59276d2a69f5667431911aa9e6babd071e` -- 2026-06-02T16:45:45Z -- ``

**Touched files (intersect with thread paths):**

- `src/Bayesian/FactorGraph.fs`

**Message:**

```
fix(infernet 081KT2T2J0008QG0R000S7GHQ8 slice 4): make BP residual test `moved` symmetric…

… over the key union — Copilot review

Copilot: `moved` only iterated `a.FactorToVar`'s keys, so a factor id or
edge appearing in `b` but not `a` (a structural change) could be missed →
false convergence; and it didn't match the docstring ("any message in b
differs from a"). Currently benign (passOnce keeps the keyset stable), but
the contract was overclaimed. Fixed: compare symmetrically over the UNION
of factor ids and edge keys — a factor/edge present on only one side counts
as moved. NaN-safe `not (distance <= tol)` preserved.

dotnet build -c Release: 0 warnings. dotnet test: 5/5 pass. Refs 081KT2T2J0008QG0R000S7GHQ8.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```
