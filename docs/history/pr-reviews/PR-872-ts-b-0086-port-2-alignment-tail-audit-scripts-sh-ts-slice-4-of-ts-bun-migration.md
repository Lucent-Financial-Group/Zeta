# PR #872 -- ts(B-0086): port 2 alignment-tail audit scripts (.sh→.ts) — slice 4 of TS/Bun migration

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 872 |
| Title | ts(B-0086): port 2 alignment-tail audit scripts (.sh→.ts) — slice 4 of TS/Bun migration |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-04-30T02:04:33Z |
| Merged at | 2026-04-30T02:07:09Z |
| Merge commit SHA | `2f3275a1c44e894df1f73b22350a504944b48508` |
| Branch | `lane-b/ts-bun-slice-4-alignment-tail-2026-04-30` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/872 |
| Changed files | 3 |
| Additions / deletions | +961 / -0 |

## Description

## Summary

Slice 4 of the TS/Bun migration. Cluster B-tail (2 alignment scripts):

- \`audit_skills.{sh→ts}\` — per-round skill runtime observability (DORA-2025-skill-scope-v1)
- \`citations.{sh→ts}\` — Phase-0 citations-graph prototype (summary / JSON / DOT)

Per Gate B prerequisite (verified currency of layered baseline 1 day old, within 30-day window).

## New pattern this slice

**Per-line scan for grep-like extraction**: bash \`grep -oE\` is per-line by default; TypeScript \`content.matchAll(re)\` matches across the whole content. When regex includes character classes that match newline (like \`[^\\]]+\`), whole-content scan produces extra matches across line boundaries. Solution: \`content.split("\\n")\` + per-line \`matchAll\`. Caught via byte-equivalence diff (off-by-3 on external refs, +1 each on internal/broken). Recorded in slice-audits.md for future ports.

## Equivalence

Per-file bash-vs-TS diff against current repo state:
- \`audit_skills\`: byte-equivalent under default args (44/235 skills touched).
- \`citations\`: byte-equivalent (5427 internal / 86 broken / 577 external refs).

## Test plan

- [x] \`bun x tsc --noEmit\` clean
- [x] \`bun x eslint <slice files>\` clean
- [x] Bash-vs-TS equivalence per file
- [x] markdownlint clean
- [x] Slice-4 audit landed

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 10 |
| Resolved threads | 0 |
| Unresolved threads | 10 |
| Total review comments | 10 |
| Total fix commits (touching thread paths) | 2 |

## Review threads

### Thread 1 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM5-nbT_`
- **Path**: `tools/alignment/audit_skills.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-30T02:08:23Z on `tools/alignment/audit_skills.ts`:150 (association: NONE)

  <pre>
  P1: `--gate`/`--stale` are parsed via `Number(next)` without validation. Non-numeric input becomes `NaN` and leads to silent misbehavior (e.g., gate never trips, or stale filter drops everything). Validate with `Number.isFinite`, reject negatives, and return a parse error when invalid.
  ```suggestion
      const gate = Number(next);
      if (!Number.isFinite(gate) || gate &lt; 0) {
        return { ok: false, message: "audit_skills: --gate requires a non-negative finite number" };
      }
      state.gate = gate;
      return { ok: true, consumed: 2 };
    }
    if (arg === "--stale") {
      if (next === undefined) {
        return { ok: false, message: "audit_skills: --stale requires a value" };
      }
      const staleMin = Number(next);
      if (!Number.isFinite(staleMin) || staleMin &lt; 0) {
        return { ok: false, message: "audit_skills: --stale requires a non-negative finite number" };
      }
      state.staleMin = staleMin;
  ```
  </pre>

### Thread 2 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM5-nbUS`
- **Path**: `tools/alignment/citations.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-30T02:08:23Z on `tools/alignment/citations.ts`:115 (association: NONE)

  <pre>
  P1: Error text still references `citations.sh` (`citations.sh: unknown arg`). Since this is the TS port, the filename in messages should be `citations.ts` to avoid confusing users and log parsers.
  ```suggestion
    return { ok: false, message: `citations.ts: unknown arg: ${arg}` };
  ```
  </pre>

### Thread 3 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM5-nbUZ`
- **Path**: `tools/alignment/citations.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-30T02:08:24Z on `tools/alignment/citations.ts`:80 (association: NONE)

  <pre>
  P1: `classifyFailure` treats non-zero exit statuses as success (it only checks `error` and `status === null`). If `git rev-parse` fails, `repoRoot()` can return an empty string and later crash at `chdir`. Add a `result.status !== 0` branch (and include stderr) similar to other alignment scripts.
  ```suggestion
    }
    if (result.status !== 0) {
      const stderr = result.stderr.trim();
      return stderr.length &gt; 0
        ? `'${cmd} ${args.join(" ")}' exited with status ${result.status}: ${stderr}`
        : `'${cmd} ${args.join(" ")}' exited with status ${result.status}`;
    }
  ```
  </pre>

### Thread 4 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM5-nbUf`
- **Path**: `tools/alignment/citations.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-30T02:08:24Z on `tools/alignment/citations.ts`:90 (association: NONE)

  <pre>
  P2: `classifyFailure` is called with an args array that doesn’t match the actual invocation (`["rev-parse"]` vs `rev-parse --show-toplevel`), which makes failure messages misleading. Pass the same args list you invoked.
  ```suggestion
    const gitArgs = ["rev-parse", "--show-toplevel"] as const;
    // eslint-disable-next-line sonarjs/no-os-command-from-path
    const result = spawnSync("git", gitArgs, {
      encoding: "utf8",
      maxBuffer: SPAWN_MAX_BUFFER,
    });
    const failure = classifyFailure("git", gitArgs, result);
  ```
  </pre>

### Thread 5 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM5-nbUm`
- **Path**: `tools/alignment/citations.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-30T02:08:24Z on `tools/alignment/citations.ts`:336 (association: NONE)

  <pre>
  P2: Summary/DOT headers still say `citations.sh` (e.g. `citations.sh — Phase-0 prototype`). Since this is the TS port, update the emitted labels/comments to `citations.ts` to keep outputs self-describing.
  </pre>

### Thread 6 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM5-nbUu`
- **Path**: `tools/alignment/citations.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-30T02:08:24Z on `tools/alignment/citations.ts`:363 (association: NONE)

  <pre>
  P2: DOT output header comment says it was generated by `citations.sh`. Update to `citations.ts` so the artifact correctly identifies its generator.
  ```suggestion
    lines.push("// Generated by tools/alignment/citations.ts (Phase-0 prototype).");
  ```
  </pre>

### Thread 7 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM5-nbUx`
- **Path**: `tools/alignment/citations.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-30T02:08:24Z on `tools/alignment/citations.ts`:358 (association: NONE)

  <pre>
  P1: `emitJson` builds JSON via string interpolation without escaping `subject`/`object` values. If a markdown link target contains quotes/backslashes/newlines, this will emit invalid JSON. Prefer constructing a typed JS object and using `JSON.stringify`, or at least escape each field with `JSON.stringify(value)`.
  ```suggestion
    const payload = {
      schema: "citations-graph-v0",
      files_scanned: r.filesScanned,
      internal_edges: r.internal.length,
      broken_candidates: r.broken.length,
      external_refs: r.externalCount,
      edges: r.internal.map((c) =&gt; ({
        subject: c.subject,
        object: c.object,
        relation: c.relation,
      })),
      broken: r.broken.map((c) =&gt; ({
        subject: c.subject,
        object: c.object,
      })),
    };

    return `${JSON.stringify(payload, null, 2)}\n`;
  ```
  </pre>

### Thread 8 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM5-nbU5`
- **Path**: `tools/alignment/audit_skills.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-30T02:08:25Z on `tools/alignment/audit_skills.ts`:91 (association: NONE)

  <pre>
  P1: `classifyFailure` doesn’t treat non-zero exit codes as failures. If `git rev-parse` fails (e.g., not in a git repo), `repoRoot()` can return an empty string and the script will throw later at `chdir` with a less helpful error. Add a `result.status !== 0` branch and surface stderr, matching the pattern used in other alignment ports.
  ```suggestion
    }
    if (result.status !== 0) {
      const stderr = result.stderr.trim();
      const stderrSuffix = stderr.length &gt; 0 ? `: ${stderr}` : "";
      return `'${cmd} ${args.join(" ")}' exited with status ${result.status}${stderrSuffix}`;
    }
  ```
  </pre>

### Thread 9 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM5-nbot`
- **Path**: `tools/alignment/audit_skills.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-04-30T02:09:13Z on `tools/alignment/audit_skills.ts`:108 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Derive repo root without depending on caller CWD**

  This port now computes the repo root via `git rev-parse --show-toplevel` before any `chdir`, so running the script from outside the repository (for example via an absolute path from cron or another workspace) returns an empty/invalid root and fails. The previous shell version anchored root from the script path (`dirname "$0"`), which was CWD-independent. Please resolve root from `import.meta`/script location (or validate non-zero git status) to preserve the prior invocation behavior.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 10 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM5-nbou`
- **Path**: `tools/alignment/citations.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-04-30T02:09:13Z on `tools/alignment/citations.ts`:87 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Make repo-root discovery independent of execution directory**

  Here `repoRoot()` relies on `git rev-parse --show-toplevel` in the current process directory, so invoking `citations.ts` from outside the repo can no longer work even when the script path is correct. The prior `citations.sh` explicitly `cd`’d to the repository by script-relative path, so this is a regression in portability for automation contexts that don’t start in `/workspace/Zeta`.

  Useful? React with 👍 / 👎.
  </pre>

## Fix commits (touching thread paths)

### `bec95914b960836ed224206169cbe601f1c96e07` -- 2026-04-30T01:48:50Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/alignment/audit_skills.ts`

**Message:**

```
ts(slice-4, wip 1/2): port audit_skills (.sh→.ts)

First port of slice 4 (Cluster B-tail). Bash 323 lines → TS port
505 lines. Per-round skill runtime observability with DORA-2025-
skill-scope-v1 schema (4 measurable columns + 6 unmeasured stubs).

Patterns reused (canonical):
- Typed boundary objects (SkillRow, AuditResult).
- AuditExitCode literal-type union.
- ParseResult discriminated-union via reduceFlag.
- maxBuffer=64MiB on spawnSync.

Implementation notes:
- Roster discovery via Bun-native readdirSync (skips _retired,
  _drafts, anything starting with _).
- Owner extraction via OWNER_RE = /memory\/persona\/([a-z0-9_-]+)\/NOTEBOOK\.md/.
- Round number extraction via global regex matchAll (slice-3 pattern).
- Three output modes: JSON / Markdown / human-summary.
- --stale N filters output to skills with friction >= N.
- --gate N exits 1 if any skill's friction >= N.

Equivalence: bash-vs-TS output diff is empty under default args
against current repo state (44/235 skills touched).

Slice 4 status: 1/2 ports landed. citations.sh follows.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
```

### `0a392fa50a6d667704a68fb59f9433c8e585461b` -- 2026-04-30T02:04:31Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/alignment/citations.ts`

**Message:**

```
ts(slice-4): port citations + slice-4 audit substrate

Final port of slice 4 (Cluster B-tail). Bash 434 lines → TS port 403
lines. Phase-0 citations-graph prototype with 3 output modes
(summary / JSON / DOT) and an --out DIR mode.

Patterns reused (canonical):
- Typed boundary objects (InternalCite, BrokenCite, AuditResult).
- AuditExitCode literal-type union (0 | 2).
- ParseResult discriminated-union via reduceFlag.
- maxBuffer=64MiB on spawnSync.
- Bun-native readdirSync recursion for markdown surface discovery.

Implementation notes:
- candidatePaths helper extracted to keep resolveTarget within
  sonarjs 15-cap.
- Per-line scan via content.split("\n") + per-line matchAll (rather
  than whole-content matchAll). The whole-content version produced
  off-by-3 differences vs bash because the regex matched across
  newlines; bash grep is per-line by default.
- normalizePath via stack-based parts collapse (matches bash's
  ./.. resolution).

Slice-4 audit substrate landed in slice-audits.md per Gate A:
- 1 new pattern recorded (per-line scan pattern for grep-like
  extraction).
- Equivalence verified per port byte-equivalent against bash.
- Coverage deferred explicitly.

Slice 4 ready for review.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
```
