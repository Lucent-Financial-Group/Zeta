# PR #2310 -- feat(B-0323): add feature-diff.ts — snapshot set diff engine + Markdown report generator

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 2310 |
| Title | feat(B-0323): add feature-diff.ts — snapshot set diff engine + Markdown report generator |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-05-09T15:14:43Z |
| Merged at | 2026-05-09T15:51:35Z |
| Merge commit SHA | `ec91db1ff8244e3297ce1bc4d92be4e895ff2afa` |
| Branch | `claim/b0323-feature-diff-engine-2026-05-09` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/2310 |
| Changed files | 5 |
| Additions / deletions | +1288 / -6 |

## Description

## Summary

- **B-0323** (P1) — Phase 3 of B-0064 GitHub feature-discovery diff cadence.
- Implements `tools/playwright/github-ui/feature-diff.ts`: pure diff engine, snapshot persistence, Markdown report generator, and CLI.
- **No live Playwright required** in this slice — fully unit-tested against fixture data.
- Adds `feature-diff.test.ts`: 35 tests, 96/96 pass across all `github-ui` tools.

## Done-criteria satisfied

| Criterion | Status |
|---|---|
| `tools/playwright/github-ui/feature-diff.ts` exists | ✓ |
| At least one feature-diff report generated comparing two snapshot sets | ✓ (`docs/research/github-ui-feature-diff-2026-05-08-fixture.md`) |
| New UI elements clearly flagged in the report (★ new) | ✓ |
| Cadence mechanism documented (CLI --help + commit body) | ✓ |

## Key functions

| Function | Purpose |
|---|---|
| `diffPageSnapshots(prior, current)` | Pure structural diff — new/removed/changed toggles, headings, form fields |
| `diffSnapshotSets(prior, current)` | Multi-page orchestration |
| `saveSnapshotSet(set, dir)` | Persist to `docs/hygiene-history/github-ui-snapshots/<date>.json` |
| `loadSnapshotSet(path)` | Deserialize prior snapshot set |
| `renderDiffReport(report)` | GFM Markdown with `★ new` / `✗ removed` markers |

## CLI exit codes

- `0` — no new feature candidates
- `1` — error (missing args, file not found)
- `2` — new feature candidates detected (triage recommended)

## Sample report output (from fixture diff)

```
# GitHub UI Feature Diff — 2026-05-08

| Category | Count |
|---|---|
| New feature candidates (toggles + headings) | 4 |

### .../settings

**New toggle elements** (new feature candidates):
- `copilot-code-review-auto-suggest` ★ new

**New UI section headings** (new feature candidates):
- Copilot ★ new
```

## Follow-on slices

Live snapshot capture (wiring `snapshotGitHubPage` across `MONITORED_PAGES` and invoking from a GitHub Actions workflow) is the next bounded step.

## Test run

```
bun test tools/playwright/github-ui/
96 pass, 0 fail — 209 expect() calls [120ms]
```

## Checks

- [x] 35/35 feature-diff unit tests pass
- [x] 96/96 all github-ui tests pass
- [x] No live Playwright/auth dependency in this slice
- [x] TypeScript only (Rule 0 — no .sh files)
- [x] `ZETA_EXPECTED_BRANCH` set before commit

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | true |
| Total threads | 15 |
| Resolved threads | 15 |
| Unresolved threads | 0 |
| Total review comments | 27 |
| Total fix commits (touching thread paths) | 10 |

## Review threads

### Thread 1 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6A1A01`
- **Path**: `tools/playwright/github-ui/feature-diff.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-09T15:17:35Z on `tools/playwright/github-ui/feature-diff.ts`:196 (association: NONE)

  <pre>
  `saveSnapshotSet` builds the output filename from `set.date` without validating/sanitizing it. Since this string can come from parsed JSON or external callers, a value containing path separators (e.g. `../...`) could write outside `outDir` and potentially overwrite arbitrary files. Enforce the documented `YYYY-MM-DD` format (or otherwise sanitize to a safe basename) before joining, and fail fast on invalid dates.

  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-09T15:44:46Z on `tools/playwright/github-ui/feature-diff.ts`:196 (association: MEMBER)

    <pre>
    Addressed:  validates  against  before building the filename (line 193-195), so path separators cannot be injected. The resulting filename is  joined with the resolved  — no traversal possible. Already in the committed code.
    </pre>

### Thread 2 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6A1A07`
- **Path**: `tools/playwright/github-ui/feature-diff.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-09T15:17:35Z on `tools/playwright/github-ui/feature-diff.ts`:151 (association: NONE)

  <pre>
  Diff outputs are built from `Set`/`Object.keys` iteration order, so `pagesAdded/pagesRemoved` and the per-page arrays (e.g. `newToggles`, `newFeatures`, etc.) can vary depending on JSON key order. Sorting these arrays (and optionally sorting `pageDiffs` by URL) would make the generated Markdown report deterministic and reduce noisy diffs in committed reports.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-09T15:44:55Z on `tools/playwright/github-ui/feature-diff.ts`:151 (association: MEMBER)

    <pre>
    Addressed: all arrays in diffPageSnapshots and diffSnapshotSets are sorted before being returned — newToggles, removedToggles, changedToggles (by key), newFeatures, removedFeatures, newFormFields, removedFormFields, changedFormFields (by key), pagesAdded, pagesRemoved, and pageDiffs (sorted by URL via currentUrls.sort()). Output is fully deterministic.
    </pre>

### Thread 3 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6A1A0_`
- **Path**: `docs/backlog/P1/B-0323-playwright-github-feature-discovery-diff-cadence.md`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-09T15:17:36Z on `docs/backlog/P1/B-0323-playwright-github-feature-discovery-diff-cadence.md`:57 (association: NONE)

  <pre>
  This file was edited (new checklist section), but the YAML frontmatter `last_updated` is still `2026-05-08`. Per backlog-row schema, `last_updated` must be updated on every content edit (tools/backlog/README.md:69). Please bump it to the edit date (2026-05-09).
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-09T15:45:01Z on `docs/backlog/P1/B-0323-playwright-github-feature-discovery-diff-cadence.md`:57 (association: MEMBER)

    <pre>
    Fixed: frontmatter last_updated is 2026-05-09 in the current file.
    </pre>

### Thread 4 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6A1A-W`
- **Path**: `tools/playwright/github-ui/feature-diff.ts`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-09T15:18:05Z on `tools/playwright/github-ui/feature-diff.ts`:150 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Diff newly added pages instead of skipping them**

  `diffSnapshotSets` only builds `pageDiffs` for URLs present in both snapshot sets, so pages that are newly added to monitoring never produce `newToggles`/`newFeatures`. Because `main()` determines `hasNewCandidates` solely from `report.pageDiffs`, a run where a page is first introduced (or missing in prior due partial capture) can incorrectly exit `0` even though all elements on that page are new relative to the prior set, creating false negatives in the feature-discovery cadence.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-09T15:45:08Z on `tools/playwright/github-ui/feature-diff.ts`:150 (association: MEMBER)

    <pre>
    Addressed: diffSnapshotSets uses emptyPageSnapshot() as the prior for any newly-added page, so all current-page toggles and headings appear as 'new' in the pageDiffs. New pages fully participate in the diff — their feature candidates surface via the standard pageDiffs.some() check.
    </pre>

### Thread 5 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6A1DiY`
- **Path**: `tools/playwright/github-ui/feature-diff.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-09T15:27:39Z on `tools/playwright/github-ui/feature-diff.ts`:352 (association: NONE)

  <pre>
  P1: `parseCliArgs` treats a flag that is missing its value (e.g., `--prior` as the last argument) as `unknown flag: --prior`, which is misleading and makes CLI errors harder to debug. Consider explicitly detecting `--prior/--current/--out` with missing values and returning a dedicated error like "missing value for --prior" (still exit code 1).
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-09T15:45:16Z on `tools/playwright/github-ui/feature-diff.ts`:352 (association: MEMBER)

    <pre>
    Addressed: cliValue returns 'missing value for ${flag}' (not 'unknown flag') when the value is undefined or is a known flag itself. The upstream commit (cd4948fc) introduced KNOWN_CLI_FLAGS to detect this case precisely.
    </pre>

### Thread 6 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6A1Dib`
- **Path**: `tools/playwright/github-ui/feature-diff.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-09T15:27:39Z on `tools/playwright/github-ui/feature-diff.ts`:116 (association: NONE)

  <pre>
  P1: Membership checks use `key in priorToggles` / `key in curToggles`. Since keys come from external snapshot data (including JSON load), `in` can be tripped by prototype properties (e.g., `toString`) and can also interact badly with `__proto__`-style keys. Prefer `Object.hasOwn(priorToggles, key)` (or an equivalent own-property check) for correctness and to reduce prototype-pollution footguns.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-09T15:45:22Z on `tools/playwright/github-ui/feature-diff.ts`:116 (association: MEMBER)

    <pre>
    Addressed: all membership checks use Object.hasOwn() — see lines 108-116 for toggle keys and lines 128-136 for form keys. No 'in' operator used on data-sourced objects.
    </pre>

### Thread 7 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6A1Did`
- **Path**: `tools/playwright/github-ui/feature-diff.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-09T15:27:39Z on `tools/playwright/github-ui/feature-diff.ts`:136 (association: NONE)

  <pre>
  P1: Same `key in priorForm` / `key in curForm` issue as the toggle diff loop: using `in` can produce incorrect results for keys that collide with prototype properties, and it’s a common surface for prototype-pollution quirks when data comes from JSON. Use an own-property check (e.g., `Object.hasOwn`) instead.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-09T15:45:28Z on `tools/playwright/github-ui/feature-diff.ts`:136 (association: MEMBER)

    <pre>
    Addressed: form field checks use Object.hasOwn(priorForm, key) / Object.hasOwn(curForm, key) — lines 128-136. No prototype-collision surface.
    </pre>

### Thread 8 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6A1Dig`
- **Path**: `tools/playwright/github-ui/feature-diff.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-09T15:27:39Z on `tools/playwright/github-ui/feature-diff.ts`:402 (association: NONE)

  <pre>
  P0: After `loadSnapshotSet` succeeds, `main` calls `diffSnapshotSets` / `renderDiffReport` outside of a try/catch. If the JSON is syntactically valid but has the wrong shape (e.g., missing `pages`, non-object `pages`, etc.), this will throw a TypeError and fall into the top-level `fatal:` handler rather than producing a user-facing `error ...` message and exit code 1. Consider validating the parsed object (or wrapping the diff/render block in try/catch) so malformed snapshot files fail gracefully with a clear error.

  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-09T15:45:34Z on `tools/playwright/github-ui/feature-diff.ts`:402 (association: MEMBER)

    <pre>
    Addressed: main wraps both diffSnapshotSets and renderDiffReport in a try/catch block, returning exit code 1 with a descriptive error message on any failure including malformed-but-valid JSON.
    </pre>

### Thread 9 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6A1Ee8`
- **Path**: `tools/playwright/github-ui/feature-diff.ts`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-09T15:31:40Z on `tools/playwright/github-ui/feature-diff.ts`:430 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Gate exit code 2 on real feature candidates only**

  The CLI currently returns exit code `2` whenever `pagesAdded` is non-empty, even if no new toggles or headings were detected on those pages. This contradicts the tool’s own contract (`Exit codes: ... 2 new feature candidates detected`) and creates false-positive triage runs when monitoring scope changes or a newly added page snapshot is empty/permission-limited. Restricting `hasNewCandidates` to actual `newToggles`/`newFeatures` would keep automation signals aligned with the documented semantics.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-09T15:45:43Z on `tools/playwright/github-ui/feature-diff.ts`:430 (association: MEMBER)

    <pre>
    Fixed (commit 8f2f79e0): removed pagesAdded.length &gt; 0 from hasNewCandidates. New pages already produce newToggles/newFeatures entries in pageDiffs via emptyPageSnapshot, so the separate pagesAdded check was redundant and caused false exit-2 when a new empty page was added to monitoring.
    </pre>

### Thread 10 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6A1FSp`
- **Path**: `tools/playwright/github-ui/feature-diff.ts`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-09T15:34:59Z on `tools/playwright/github-ui/feature-diff.ts`:120 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Deduplicate new/removed heading diffs before reporting**

  `diffPageSnapshots` computes heading diffs by filtering the raw `visibleFeatures` arrays, which preserves duplicates when the same heading text appears multiple times in a page snapshot. Since `extractVisibleFeatures` collects every `h2/h3` occurrence, a repeated heading can be counted and printed multiple times as separate “new feature candidates,” inflating the summary totals and creating noisy triage signals even when there is only one logical new section.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-09T15:45:49Z on `tools/playwright/github-ui/feature-diff.ts`:120 (association: MEMBER)

    <pre>
    Fixed (commit 8f2f79e0): newFeatures and removedFeatures now spread from the curFeatures/priorFeatures Sets (which deduplicate by construction) then .sort() for deterministic order. Previously the raw visibleFeatures array was filtered directly, which could pass through duplicates.
    </pre>

### Thread 11 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6A1FhG`
- **Path**: `tools/playwright/github-ui/feature-diff.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-09T15:35:57Z on `tools/playwright/github-ui/feature-diff.ts`:234 (association: NONE)

  <pre>
  P1: `renderDiffReport`'s summary row "Removed elements" currently counts only removed toggles + removed headings (`removedToggles.length + removedFeatures.length`) but the report can also emit removed form fields. This can undercount removals in the summary; either include `removedFormFields.length` in `totalRemoved` (and possibly rename the label if it’s meant to be toggles+headings only).
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-09T15:45:57Z on `tools/playwright/github-ui/feature-diff.ts`:234 (association: MEMBER)

    <pre>
    Fixed: totalRemoved now includes removedFormFields.length in its reduce. Test added: 'summary Removed elements count includes removedFormFields' verifies 1 removedToggle + 1 removedFeature + 2 removedFormFields = 4 in the summary row.
    </pre>

### Thread 12 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6A1FhP`
- **Path**: `tools/playwright/github-ui/feature-diff.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-09T15:35:58Z on `tools/playwright/github-ui/feature-diff.ts`:353 (association: NONE)

  <pre>
  P2: `cliValue` treats any value starting with "-" as missing, which breaks valid paths like "-prior.json" or relative paths in directories named with a leading dash. Consider accepting dash-prefixed values (and optionally supporting `--` to end flag parsing) rather than rejecting them.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-09T15:46:05Z on `tools/playwright/github-ui/feature-diff.ts`:353 (association: MEMBER)

    <pre>
    Addressed (commit 8f2f79e0 + upstream cd4948fc): cliValue now checks against KNOWN_CLI_FLAGS (exact match) instead of value.startsWith('-'), so paths like '-prior.json' pass through correctly. The parseCliArgs loop also handles '--' as an end-of-flags sentinel, breaking out cleanly for paths after the separator.
    </pre>

### Thread 13 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6A1Gkr`
- **Path**: `tools/playwright/github-ui/feature-diff.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-09T15:40:20Z on `tools/playwright/github-ui/feature-diff.ts`:451 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Include new form fields in candidate-exit detection**

  `main()` only sets `hasNewCandidates` from `newToggles` and `newFeatures`, so a diff that introduces only `newFormFields` exits `0` even though the report renders those fields as newly discovered UI elements (`★ new`). In practice, if GitHub ships a new text/select setting without a new heading or checkbox, automation will miss the triage signal and classify the run as “no new features,” which is a false negative for this discovery workflow.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 14 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6A1H34`
- **Path**: `tools/playwright/github-ui/feature-diff.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-09T15:45:43Z on `tools/playwright/github-ui/feature-diff.ts`:322 (association: NONE)

  <pre>
  P1: The report footer says "New feature candidates (★)" but the report also marks **new form fields** with the same "★ new" marker. Since `totalNew`/exit-code logic only treats toggles+headings as candidates, using ★ for form fields makes the report semantics inconsistent and can cause unnecessary triage. Consider either (a) using a different marker for form-field additions, or (b) explicitly including form fields in the "feature candidates" definition/count and CLI detection (and updating the summary text accordingly).

  </pre>

### Thread 15 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6A1H3-`
- **Path**: `tools/playwright/github-ui/feature-diff.test.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-09T15:45:44Z on `tools/playwright/github-ui/feature-diff.test.ts`:78 (association: NONE)

  <pre>
  P1: These helpers capture CLI output by temporarily monkey-patching `process.stdout.write`/`process.stderr.write`. If `bun test` runs tests concurrently across files (or within this file), this shared global mutation can make unrelated tests flaky and can interleave output between tests. Prefer refactoring `main(...)` to accept injectable output writers/streams (so tests can pass a local buffer), or use a test double that doesn't mutate global process streams.
  </pre>

## Fix commits (touching thread paths)

### `2b3b285aab118de863bec910f30dd8dc6fb4a331` -- 2026-05-09T15:14:21Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `docs/backlog/P1/B-0323-playwright-github-feature-discovery-diff-cadence.md`
- `tools/playwright/github-ui/feature-diff.test.ts`
- `tools/playwright/github-ui/feature-diff.ts`

**Message:**

```
feat(B-0323): add feature-diff.ts — snapshot set diff engine + report…

… generator

Implements the pure diff/persistence/reporting core of B-0323 (Phase 3 of
B-0064 GitHub feature-discovery). No live Playwright required in this slice;
the engine is fully unit-tested against fixture data.

- `diffPageSnapshots()`: pure structural diff of two GitHubPageSnapshot objects
  — flags new toggles (primary "new feature candidate" signal), removed toggles,
  changed toggle states, new/removed h2/h3 headings, new/removed form fields
- `diffSnapshotSets()`: multi-page orchestration over SnapshotSet collections
- `saveSnapshotSet()` / `loadSnapshotSet()`: JSON persistence to
  docs/hygiene-history/github-ui-snapshots/
- `renderDiffReport()`: GitHub-flavored Markdown report with ★ new / ✗ removed
  markers and a summary table
- CLI: `--prior <path> --current <path> [--out <path>]`
  exit 0 = no candidates | exit 2 = new feature candidates detected
- 35 unit tests; 96/96 pass across all github-ui tools
- Fixture report saved to docs/research/ demonstrating real diff output

Cadence: invocable via autonomous-loop tick or GitHub Actions workflow
(live snapshot capture wired in follow-on slice).

operative-authorization: aaron 2026-05-04: "it**, not just the output. Grinding through failures + recoveries"

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>
```

### `c89860871fbef2b6d00769cfc08d5ba8dfd0cad2` -- 2026-05-09T15:23:34Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/playwright/github-ui/feature-diff.test.ts`

**Message:**

```
fix(B-0323): fix readonly sort error + regenerate BACKLOG.md

Two non-required CI failures on PR #2310:

1. lint (tsc tools): TS2339 — `Property 'sort' does not exist on type
   'readonly string[]'` at feature-diff.test.ts:107. Fix: spread into
   a mutable copy before sorting (`[...diff.newToggles].sort()`).

2. BACKLOG.md generated-index drift: B-0318, B-0319, B-0326, B-0327,
   B-0328 were closed in earlier PRs (their per-row files have
   `status: closed`) but BACKLOG.md still showed `[ ]`. Regenerated.

96/96 tests pass; tsc --noEmit clean.

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>
```

### `52156f0a75ea1964d96c74dd98be2b521d69a4fe` -- 2026-05-09T15:24:10Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `docs/backlog/P1/B-0323-playwright-github-feature-discovery-diff-cadence.md`
- `tools/playwright/github-ui/feature-diff.ts`

**Message:**

```
fix(B-0323): address review threads — date sanitize, deterministic so…

…rt, last_updated bump, new-page candidates (P1s)

Co-Authored-By: Grok <noreply@x.ai>
Co-authored-by: Cursor <cursoragent@cursor.com>
```

### `199f6c1f2bcd06abfe709e136c11eea993793639` -- 2026-05-09T15:29:10Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/playwright/github-ui/feature-diff.test.ts`
- `tools/playwright/github-ui/feature-diff.ts`

**Message:**

```
fix(B-0323): include added pages in feature diffs

Add candidate-level diffs for newly monitored pages so the Markdown report and CLI gate surface the actual new toggles, headings, and form fields instead of only listing the page URL.

Co-Authored-By: Codex <noreply@openai.com>
```

### `8535fdc328410df6a3c8b20f0ec8948ecc2b0738` -- 2026-05-09T15:32:36Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/playwright/github-ui/feature-diff.test.ts`
- `tools/playwright/github-ui/feature-diff.ts`

**Message:**

```
fix(B-0323): harden feature diff CLI inputs

Use own-property checks for snapshot key membership, report missing CLI flag values directly, and keep malformed snapshot data on the user-facing error path.

Co-Authored-By: Codex <noreply@openai.com>
```

### `f8495fc84f0c22e421cf3e282337e93bc1278929` -- 2026-05-09T15:35:54Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/playwright/github-ui/feature-diff.test.ts`
- `tools/playwright/github-ui/feature-diff.ts`

**Message:**

```
fix(B-0323): align CLI candidate exit semantics

Return exit code 2 only when newly detected toggles or headings exist, while still rendering newly monitored empty pages in the report.

Co-Authored-By: Codex <noreply@openai.com>
```

### `cd4948fc86efecfbaa902fbda97d4a9a6c970e2e` -- 2026-05-09T15:41:04Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/playwright/github-ui/feature-diff.test.ts`
- `tools/playwright/github-ui/feature-diff.ts`

**Message:**

```
fix(B-0323): address feature diff review nits

Deduplicate repeated heading diffs, include removed form fields in removal totals, and allow dash-prefixed CLI path values while preserving missing-value errors.\n\nCo-Authored-By: Codex <noreply@openai.com>
```

### `14a1ff6a64efa9f0179451bbabe32a275c0bcb6e` -- 2026-05-09T15:43:03Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/playwright/github-ui/feature-diff.test.ts`
- `tools/playwright/github-ui/feature-diff.ts`

**Message:**

```
fix(B-0323): flag new form fields as candidates

Include new form fields in feature-candidate totals and exit-code detection so form-only GitHub UI additions trigger triage.\n\nCo-Authored-By: Codex <noreply@openai.com>
```

### `8f2f79e014b3637099f7a6878e59ad8d7bf46c5d` -- 2026-05-09T15:44:20Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/playwright/github-ui/feature-diff.test.ts`
- `tools/playwright/github-ui/feature-diff.ts`

**Message:**

```
fix(B-0323): address reviewer findings — deterministic sort, exit-cod…

…e precision, removedFormFields test

- Sort newFeatures/removedFeatures for deterministic output (Codex/Copilot)
- Remove pagesAdded.length > 0 from hasNewCandidates — already captured in pageDiffs
  via emptyPageSnapshot for new pages; bare pagesAdded > 0 caused false exit-2 (Codex P2)
- Add test: summary 'Removed elements' count includes removedFormFields (Copilot P1)
- KNOWN_CLI_FLAGS + -- end-of-flags: upstream commit (cd4948fc) addressed; merge resolved

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>
```

### `15c370b5522ebdc6475e64368b8fc2de11ca0a57` -- 2026-05-09T15:48:38Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/playwright/github-ui/feature-diff.test.ts`
- `tools/playwright/github-ui/feature-diff.ts`

**Message:**

```
fix(B-0323): isolate feature diff CLI output tests

Clarify form-field candidate footer semantics and let CLI tests capture output through injected writers instead of mutating process stdout/stderr globally.\n\nCo-Authored-By: Codex <noreply@openai.com>
```
