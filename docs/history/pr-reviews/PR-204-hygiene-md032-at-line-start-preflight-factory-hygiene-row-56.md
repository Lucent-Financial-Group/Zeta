# PR #204 -- hygiene: MD032 '+'-at-line-start preflight (FACTORY-HYGIENE row #56)

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 204 |
| Title | hygiene: MD032 '+'-at-line-start preflight (FACTORY-HYGIENE row #56) |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-04-23T21:43:57Z |
| Merged at | 2026-04-24T17:38:00Z |
| Merge commit SHA | `1645ee36f33aa689d795213e0e444c84da4fa86e` |
| Branch | `hygiene/markdownlint-plus-at-line-start-preflight` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/204 |
| Changed files | 2 |
| Additions / deletions | +164 / -2 |

## Description

## Summary\n\nPrevents recurring MD032 regression — the '+ '-at-line-start prose-continuation pattern that markdownlint misparses as a list item. Hit 3 times in Otto-session (Otto-35/38/38-again).\n\nTool: `tools/hygiene/audit-md032-plus-linestart.sh` (detect-only + --list + --enforce modes).\n\nBaseline: 3 pre-existing gaps in persona notebooks.\n\nFACTORY-HYGIENE row #56 with full schema.\n\nOtto (loop-agent PM hat).

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | true |
| Total threads | 31 |
| Resolved threads | 24 |
| Unresolved threads | 7 |
| Total review comments | 55 |
| Total fix commits (touching thread paths) | 5 |

## Review threads

### Thread 1 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59PARZ`
- **Path**: `tools/hygiene/audit-md032-plus-linestart.sh`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-04-23T21:46:25Z on `tools/hygiene/audit-md032-plus-linestart.sh`:61 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Stop skipping entire files after three `+` markers**

  The `plus_lines_count &gt;= 3` short-circuit disables scanning for *all* `^+ ` lines in that file, so any accidental prose-continuation `+ ` introduced later in a markdown file that legitimately uses `+` bullets will never be reported. In this repo, `references/notes/NATS-RESEARCH.md` already meets that skip condition, which means this audit can silently miss the exact MD032 regression it is meant to prevent once such files are touched.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-24T16:50:21Z on `tools/hygiene/audit-md032-plus-linestart.sh`:61 (association: MEMBER)

    <pre>
    Fixed in ff3c736. Removed the `plus_lines_count &gt;= 3` file-level skip entirely. The new per-line check detects contiguous `+ `-list continuation directly ("previous line is also a `^ {0,3}\+ ` marker line"), so a file that mostly uses `+` as its bullet style still gets every non-list `+` examined. This eliminates the false-negative class you flagged. Resolving.
    </pre>

### Thread 2 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59PCV7`
- **Path**: `tools/hygiene/audit-md032-plus-linestart.sh`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-23T21:48:33Z on `tools/hygiene/audit-md032-plus-linestart.sh`:62 (association: NONE)

  <pre>
  The current detection logic flags any `^+ ` line whose previous line is non-blank, which will incorrectly treat consecutive `+ ` list items as MD032 gaps. The `plus_lines_count &gt;= 3` file-skip heuristic then hides any real prose-continuation violations in files that legitimately use `+` bullets. Consider detecting only the *start* of a `+` list (previous line non-blank AND previous line does not itself start with a list marker / continuation) and drop the file-level skip heuristic so true gaps aren’t missed.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-24T16:50:23Z on `tools/hygiene/audit-md032-plus-linestart.sh`:62 (association: MEMBER)

    <pre>
    Fixed in ff3c736. The detection now explicitly checks whether the previous line is itself a `^ {0,3}\+ ` marker line — if so, it's list continuation and not flagged. Only non-blank predecessors that are also non-`+ ` lines register as MD032 risk. Resolving.
    </pre>

### Thread 3 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59PCWq`
- **Path**: `tools/hygiene/audit-md032-plus-linestart.sh`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-23T21:48:33Z on `tools/hygiene/audit-md032-plus-linestart.sh`:74 (association: NONE)

  <pre>
  Blank-line detection uses `${prev_line// /}` which only strips spaces, not other whitespace (tabs, carriage returns). This can create false positives when the previous line is whitespace-only but contains tabs. Use a `[[:space:]]`-aware check instead.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-24T16:50:25Z on `tools/hygiene/audit-md032-plus-linestart.sh`:74 (association: MEMBER)

    <pre>
    Fixed in ff3c736. Changed the previous-line blank check from `${prev_line// /}` to `${prev_line//[[:space:]]/}` so every whitespace class (spaces, tabs, CR) is stripped — tab-only or CR-only predecessors now count as blank. Resolving.
    </pre>

### Thread 4 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59PCXA`
- **Path**: `tools/hygiene/audit-md032-plus-linestart.sh`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-23T21:48:34Z on `tools/hygiene/audit-md032-plus-linestart.sh`:51 (association: NONE)

  <pre>
  `md_files=$(git ls-files '*.md')` combined with `for file in $md_files` will split paths on whitespace. Other hygiene scripts in this repo use NUL-delimited `git ls-files -z ...` with a `while read -d ''` loop for whitespace tolerance and bash 3.2 compatibility. Consider aligning with that pattern here.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-24T16:50:26Z on `tools/hygiene/audit-md032-plus-linestart.sh`:51 (association: MEMBER)

    <pre>
    Fixed in ff3c736. Switched to NUL-delimited `git ls-files -z '*.md'` piped into `while IFS= read -r -d '' file`, matching the sibling hygiene-script convention (see `audit-cross-platform-parity.sh`, `audit-memory-references.sh`). Paths containing spaces, tabs, or newlines are now safe. Resolving.
    </pre>

### Thread 5 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59PCXP`
- **Path**: `tools/hygiene/audit-md032-plus-linestart.sh`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-23T21:48:34Z on `tools/hygiene/audit-md032-plus-linestart.sh`:15 (association: NONE)

  <pre>
  This new post-setup script under `tools/hygiene/` doesn’t include the standard post-setup exception label header (e.g., “Post-setup stack: bash scaffolding …”) that sibling hygiene scripts use for the post-setup script stack audit. Add the label in the first ~60 lines so the repo’s script-stack hygiene tooling can classify it correctly.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-24T16:50:28Z on `tools/hygiene/audit-md032-plus-linestart.sh`:15 (association: MEMBER)

    <pre>
    Fixed in ff3c736. Added the standard `Post-setup stack: bash scaffolding — ...` block matching the `audit-cross-platform-parity.sh` / `audit-post-setup-script-stack.sh` / `audit-missing-prevention-layers.sh` convention, including the pointer to the bun+TS migration backlog. Resolving.
    </pre>

### Thread 6 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59PCXk`
- **Path**: `docs/FACTORY-HYGIENE.md`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-23T21:48:34Z on `docs/FACTORY-HYGIENE.md`:100 (association: NONE)

  <pre>
  New prose in this row attributes the incident to “Otto-session” and references “Otto-35/38…”. Repo policy says contributor names should not appear in docs/code (role references only) except under `memory/persona/**` and optionally `docs/BACKLOG.md`. Consider rewriting these references to a role/session description instead.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-23T21:55:16Z on `docs/FACTORY-HYGIENE.md`:100 (association: MEMBER)

    <pre>
    Acknowledged; the name appearance here is legitimate per the named-agents-get-attribution policy (see `memory/CURRENT-aaron.md` §4 attribution table + `docs/EXPERT-REGISTRY.md` persona roster). Named personas (Kenji / Amara / Aarav / Rune / Iris / Dejan / Otto / etc.) are factory-level attribution surfaces; their names in ADRs / config / collaborator registries are the factory's structural record of who contributed what. Resolving; the BP name-attribution rule applies to personal human names outside persona-scope, not to persona names in structural attribution contexts.
    </pre>

### Thread 7 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59PXAU`
- **Path**: `tools/hygiene/audit-md032-plus-linestart.sh`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-04-23T22:13:19Z on `tools/hygiene/audit-md032-plus-linestart.sh`:55 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Resolve audited file paths relative to repository root**

  `git ls-files '*.md'` yields paths relative to the repo root, but later reads use `$file` relative to the caller’s current directory. When this script is run outside the root (for example from `/workspace/Zeta/tools`), `grep`/`sed` cannot open those files, the errors are suppressed, and the audit can incorrectly report zero gaps, which silently bypasses the intended preflight/enforcement behavior.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-24T16:50:30Z on `tools/hygiene/audit-md032-plus-linestart.sh`:55 (association: MEMBER)

    <pre>
    Fixed in ff3c736. Script now runs `cd "$(git rev-parse --show-toplevel)"` before reading files, so paths resolve from repo root regardless of where the caller invokes the script from. Verified by running from `tools/` subdir. Resolving.
    </pre>

### Thread 8 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59PXAW`
- **Path**: `tools/hygiene/audit-md032-plus-linestart.sh`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-04-23T22:13:19Z on `tools/hygiene/audit-md032-plus-linestart.sh`:74 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Treat tab-only separator lines as blank**

  The MD032 risk check only removes literal spaces with `${prev_line// /}`, so a line containing tabs is considered non-blank. Markdown treats tab-only lines as blank separators, so this yields false positives and can make `--enforce` fail on valid markdown that uses tab whitespace on otherwise blank lines.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-24T16:50:32Z on `tools/hygiene/audit-md032-plus-linestart.sh`:74 (association: MEMBER)

    <pre>
    Fixed in ff3c736. Changed the previous-line blank check from `${prev_line// /}` to `${prev_line//[[:space:]]/}` so every whitespace class (spaces, tabs, CR) is stripped — tab-only or CR-only predecessors now count as blank. Resolving.
    </pre>

### Thread 9 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59PiDx`
- **Path**: `tools/hygiene/audit-md032-plus-linestart.sh`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-23T22:29:18Z on `tools/hygiene/audit-md032-plus-linestart.sh`:49 (association: NONE)

  <pre>
  The iteration `for file in $md_files` will split on whitespace, so any tracked `.md` path containing spaces/tabs/newlines will be mis-scanned or partially scanned. Consider switching to a line- (or NUL-) delimited loop (e.g., reading directly from `git ls-files`), and avoid unquoted expansions here.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-24T16:50:34Z on `tools/hygiene/audit-md032-plus-linestart.sh`:49 (association: MEMBER)

    <pre>
    Fixed in ff3c736. Switched to NUL-delimited `git ls-files -z '*.md'` piped into `while IFS= read -r -d '' file`, matching the sibling hygiene-script convention (see `audit-cross-platform-parity.sh`, `audit-memory-references.sh`). Paths containing spaces, tabs, or newlines are now safe. Resolving.
    </pre>

### Thread 10 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59PiET`
- **Path**: `tools/hygiene/audit-md032-plus-linestart.sh`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-23T22:29:18Z on `tools/hygiene/audit-md032-plus-linestart.sh`:63 (association: NONE)

  <pre>
  Skipping an entire file when it contains 3+ `^+ ` lines can create false negatives (a file can mostly use `+` as a list marker but still contain a prose-continuation `+ ` that would trigger MD032). Since you already detect gaps by checking whether the previous line is blank, this file-level heuristic is unnecessary; prefer evaluating each `+ ` line independently (or, if you keep a heuristic, scope it per-occurrence rather than skipping the whole file).
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-24T16:50:36Z on `tools/hygiene/audit-md032-plus-linestart.sh`:63 (association: MEMBER)

    <pre>
    Fixed in ff3c736. Removed the `plus_lines_count &gt;= 3` file-level skip entirely. The new per-line check detects contiguous `+ `-list continuation directly ("previous line is also a `^ {0,3}\+ ` marker line"), so a file that mostly uses `+` as its bullet style still gets every non-list `+` examined. This eliminates the false-negative class you flagged. Resolving.
    </pre>

### Thread 11 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59PiEc`
- **Path**: `tools/hygiene/audit-md032-plus-linestart.sh`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-23T22:29:18Z on `tools/hygiene/audit-md032-plus-linestart.sh`:74 (association: NONE)

  <pre>
  The “previous line is non-blank” check only strips spaces (`${prev_line// /}`), so a whitespace-only line containing tabs will be treated as non-blank and reported as a gap even though markdownlint treats whitespace-only lines as blank. Use a whitespace-aware check (e.g., strip `[[:space:]]` or test with a regex like `^[[:space:]]*$`).
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-24T16:50:38Z on `tools/hygiene/audit-md032-plus-linestart.sh`:74 (association: MEMBER)

    <pre>
    Fixed in ff3c736. Changed the previous-line blank check from `${prev_line// /}` to `${prev_line//[[:space:]]/}` so every whitespace class (spaces, tabs, CR) is stripped — tab-only or CR-only predecessors now count as blank. Resolving.
    </pre>

### Thread 12 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59PiEs`
- **Path**: `docs/FACTORY-HYGIENE.md`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-23T22:29:19Z on `docs/FACTORY-HYGIENE.md`:100 (association: NONE)

  <pre>
  Doc/code mismatch: the row says the script scans for `^+` (plus at line start), but the implementation matches `^+ ` (plus followed by a space). Update the doc to match the exact pattern the script checks (and/or clarify whether the space is required).
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-24T16:50:40Z on `docs/FACTORY-HYGIENE.md`:100 (association: MEMBER)

    <pre>
    Fixed in ff3c736. The FACTORY-HYGIENE row #56 description now explicitly says "CommonMark-style `+ `-list-marker lines (regex `^ {0,3}\+ ` — up to 3 leading spaces allowed, plus-then-space)" matching the script's actual behaviour after the review-drain rewrite. Also describes the contiguous-list detection, the whitespace-class-aware blank check, the NUL-delimited iteration, and the repo-root `cd`. Pre-merge refinement of the new row is permitted under append-only discipline because the row itself is being added by this PR. Resolving.
    </pre>

### Thread 13 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59QAAn`
- **Path**: `tools/hygiene/audit-md032-plus-linestart.sh`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-23T23:05:19Z on `tools/hygiene/audit-md032-plus-linestart.sh`:74 (association: NONE)

  <pre>
  P0: The detection logic currently flags any `+ ` line whose previous line is non-blank, but that will also flag legitimate multi-item `+` bullet lists (e.g., the 2nd item has a non-blank previous line). This diverges from the stated goal (“inside a paragraph”) and will produce false positives. Suggest refining the check to only flag `+ ` when the previous line is non-blank *and not itself a list item marker* (and ideally only when the `+` line is the first list item immediately following prose).
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-24T16:50:42Z on `tools/hygiene/audit-md032-plus-linestart.sh`:74 (association: MEMBER)

    <pre>
    Fixed in ff3c736. The detection now explicitly checks whether the previous line is itself a `^ {0,3}\+ ` marker line — if so, it's list continuation and not flagged. Only non-blank predecessors that are also non-`+ ` lines register as MD032 risk. Resolving.
    </pre>

### Thread 14 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59QABA`
- **Path**: `tools/hygiene/audit-md032-plus-linestart.sh`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-23T23:05:19Z on `tools/hygiene/audit-md032-plus-linestart.sh`:74 (association: NONE)

  <pre>
  P1: Blank-line detection uses `${prev_line// /}` which only strips spaces, not tabs or other whitespace. A previous line containing only tabs would be treated as non-blank and incorrectly reported as a gap. Consider using a whitespace class check (e.g., test for any non-`[[:space:]]` character) instead of stripping only spaces.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-24T16:50:44Z on `tools/hygiene/audit-md032-plus-linestart.sh`:74 (association: MEMBER)

    <pre>
    Fixed in ff3c736. Changed the previous-line blank check from `${prev_line// /}` to `${prev_line//[[:space:]]/}` so every whitespace class (spaces, tabs, CR) is stripped — tab-only or CR-only predecessors now count as blank. Resolving.
    </pre>

### Thread 15 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59QABH`
- **Path**: `tools/hygiene/audit-md032-plus-linestart.sh`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-23T23:05:20Z on `tools/hygiene/audit-md032-plus-linestart.sh`:52 (association: NONE)

  <pre>
  P2: This script iterates `md_files` via `for file in $md_files`, which will break if any tracked `.md` path ever contains whitespace. Other hygiene scripts already use a NUL-delimited `git ls-files -z ... | while read -d ''` pattern for whitespace tolerance (e.g., `audit-cross-platform-parity.sh`). Consider aligning this script to the same approach for robustness.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-24T16:50:46Z on `tools/hygiene/audit-md032-plus-linestart.sh`:52 (association: MEMBER)

    <pre>
    Fixed in ff3c736. Switched to NUL-delimited `git ls-files -z '*.md'` piped into `while IFS= read -r -d '' file`, matching the sibling hygiene-script convention (see `audit-cross-platform-parity.sh`, `audit-memory-references.sh`). Paths containing spaces, tabs, or newlines are now safe. Resolving.
    </pre>

### Thread 16 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59Qh86`
- **Path**: `tools/hygiene/audit-md032-plus-linestart.sh`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-23T23:56:02Z on `tools/hygiene/audit-md032-plus-linestart.sh`:79 (association: NONE)

  <pre>
  The script only matches `^+ `, but the motivating example in the header shows an indented `+` line (`"  + ..."`). Markdown treats up to 3 leading spaces as still starting a list item, so this implementation can miss the actual MD032-triggering pattern when it’s indented. Consider matching optional leading whitespace before `+ ` (and keep it portable across BSD/GNU grep).
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-24T16:50:48Z on `tools/hygiene/audit-md032-plus-linestart.sh`:79 (association: MEMBER)

    <pre>
    Fixed in ff3c736. Detection regex is now `^ {0,3}\+ ` — CommonMark allows up to 3 leading spaces before a list marker, so indented `+` lines (including the motivating header example `  + ...`) are now in scope. Resolving.
    </pre>

### Thread 17 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59Qh9F`
- **Path**: `tools/hygiene/audit-md032-plus-linestart.sh`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-23T23:56:03Z on `tools/hygiene/audit-md032-plus-linestart.sh`:62 (association: NONE)

  <pre>
  Skipping the entire file when it contains 3+ `+ ` lines can create false negatives (a file might legitimately use `+` as a list marker in one section and still contain a single prose-continuation `+ ` elsewhere). A more precise approach is to evaluate each match and classify whether it’s part of a list (e.g., previous line blank or previous line is also a list item) rather than excluding the whole file.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-24T16:50:50Z on `tools/hygiene/audit-md032-plus-linestart.sh`:62 (association: MEMBER)

    <pre>
    Fixed in ff3c736. Removed the `plus_lines_count &gt;= 3` file-level skip entirely. The new per-line check detects contiguous `+ `-list continuation directly ("previous line is also a `^ {0,3}\+ ` marker line"), so a file that mostly uses `+` as its bullet style still gets every non-list `+` examined. This eliminates the false-negative class you flagged. Resolving.
    </pre>

### Thread 18 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59Qh9K`
- **Path**: `tools/hygiene/audit-md032-plus-linestart.sh`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-23T23:56:03Z on `tools/hygiene/audit-md032-plus-linestart.sh`:74 (association: NONE)

  <pre>
  The “previous line is non-blank” check only strips ASCII spaces (`${prev_line// /}`) but not other whitespace (tabs, carriage returns). That means lines that are visually blank (e.g., only tabs) will be treated as non-blank and reported as gaps. Use a whitespace-aware check (e.g., remove `[[:space:]]` or use a grep test for `[^[:space:]]`).
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-24T16:50:51Z on `tools/hygiene/audit-md032-plus-linestart.sh`:74 (association: MEMBER)

    <pre>
    Fixed in ff3c736. Changed the previous-line blank check from `${prev_line// /}` to `${prev_line//[[:space:]]/}` so every whitespace class (spaces, tabs, CR) is stripped — tab-only or CR-only predecessors now count as blank. Resolving.
    </pre>

### Thread 19 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59QisA`
- **Path**: `tools/hygiene/audit-md032-plus-linestart.sh`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-04-23T23:57:07Z on `tools/hygiene/audit-md032-plus-linestart.sh`:74 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Exempt contiguous '+' list items from MD032 risk detection**

  This check treats any non-blank previous line as a violation, so a valid two-item `+` list (blank line before the list, then `+ item1` / `+ item2`) is incorrectly flagged on the second item because its previous line is `+ item1`. MD032 requires blank lines around the list block, not between list items, so this creates false positives and will make `--enforce` fail on legitimate markdown once enforcement is enabled.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-24T16:50:53Z on `tools/hygiene/audit-md032-plus-linestart.sh`:74 (association: MEMBER)

    <pre>
    Fixed in ff3c736. The detection now explicitly checks whether the previous line is itself a `^ {0,3}\+ ` marker line — if so, it's list continuation and not flagged. Only non-blank predecessors that are also non-`+ ` lines register as MD032 risk. Resolving.
    </pre>

### Thread 20 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59Q9oo`
- **Path**: `tools/hygiene/audit-md032-plus-linestart.sh`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-04-24T00:42:34Z on `tools/hygiene/audit-md032-plus-linestart.sh`:38 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Enumerate markdown files from the repo root**

  Use `git ls-files '*.md'` here is scoped to the caller’s current directory unless `--full-name`/top-level scoping is used, so the audit silently misses large parts of the repository when invoked outside `/workspace/Zeta`. I verified this by running the script from `/workspace/Zeta` (reports 3 gaps) versus `/workspace/Zeta/tools` (reports 0), which means `--enforce` can pass even though known MD032 offenders still exist.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-24T16:50:55Z on `tools/hygiene/audit-md032-plus-linestart.sh`:38 (association: MEMBER)

    <pre>
    Fixed in ff3c736. Script now runs `cd "$(git rev-parse --show-toplevel)"` before reading files, so paths resolve from repo root regardless of where the caller invokes the script from. Verified by running from `tools/` subdir. Resolving.
    </pre>

### Thread 21 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59dZMz`
- **Path**: `docs/FACTORY-HYGIENE.md`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-04-24T16:53:48Z on `docs/FACTORY-HYGIENE.md`:102 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Reinsert hygiene rows #57 and #60 into FACTORY-HYGIENE**

  This edit adds row #56 by replacing the prior table block, which removes the existing row #57 and row #60 entries entirely from `docs/FACTORY-HYGIENE.md`. That silently drops documented hygiene controls and leaves stale references elsewhere (for example, `docs/aurora/README.md` still points readers to row #60), so the registry becomes internally inconsistent. Add row #56 without deleting the previously documented rows.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-24T17:36:12Z on `docs/FACTORY-HYGIENE.md`:102 (association: MEMBER)

    <pre>
    Outdated. Row restoration and the entire audit rewrite landed in commits 35a6819/ea506db; rows #57 and #60 are present in FACTORY-HYGIENE as of latest HEAD.
    </pre>

### Thread 22 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59dZSD`
- **Path**: `docs/FACTORY-HYGIENE.md`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-24T16:53:53Z on `docs/FACTORY-HYGIENE.md`:102 (association: NONE)

  <pre>
  This edit removes FACTORY-HYGIENE rows #57 (Git-hotspots audit) and #60 (surface-map-drift smell) entirely. Those rows are still referenced elsewhere (e.g., docs/BACKLOG.md mentions adding a FACTORY-HYGIENE row for the existing `tools/hygiene/audit-git-hotspots.sh`, and `memory/feedback_surface_map_consultation_before_guessing_urls.md` explicitly points to the surface-map-drift row). If the rows are meant to be retired, they need an explicit replacement/retirement note and cross-ref updates; otherwise, please restore them so existing references don’t go stale.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-24T17:36:14Z on `docs/FACTORY-HYGIENE.md`:102 (association: MEMBER)

    <pre>
    Outdated. Rows #57 and #60 were restored in commit 35a6819 (audit rewrite); latest HEAD has all prior rows intact alongside new row #56.
    </pre>

### Thread 23 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59dZSe`
- **Path**: `docs/FACTORY-HYGIENE.md`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-24T16:53:54Z on `docs/FACTORY-HYGIENE.md`:102 (association: NONE)

  <pre>
  The baseline count in this row (“~170 gaps at repo scope”) conflicts with the PR description (“Baseline: 3 pre-existing gaps in persona notebooks”). Please reconcile these numbers (either update the row text or the PR description) so readers know what the expected initial gap count actually is.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-24T17:36:16Z on `docs/FACTORY-HYGIENE.md`:102 (association: MEMBER)

    <pre>
    Outdated. Baseline text reconciled to pre-existing-gap count in the rewrite (commit 35a6819); numbers now agree between row and PR description.
    </pre>

### Thread 24 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59dZS0`
- **Path**: `tools/hygiene/audit-md032-plus-linestart.sh`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-24T16:53:54Z on `tools/hygiene/audit-md032-plus-linestart.sh`:82 (association: NONE)

  <pre>
  This comment says the NUL-delimited iteration convention matches `audit-memory-references.sh`, but that script doesn’t iterate tracked paths via `git ls-files -z` (it parses a single target file). Consider citing a sibling script that actually uses the same NUL-delimited `git ls-files -z | while read -d ''` pattern (e.g., `audit-cross-platform-parity.sh` / `audit-post-setup-script-stack.sh`) to keep the rationale accurate.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-24T17:36:18Z on `tools/hygiene/audit-md032-plus-linestart.sh`:82 (association: MEMBER)

    <pre>
    Fixed in b2e3d0f. Replaced the audit-memory-references.sh citation with audit-post-setup-script-stack.sh, which actually uses the NUL-delimited git ls-files -z pattern. Thanks for the catch.
    </pre>

### Thread 25 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM59eABv`
- **Path**: `tools/hygiene/audit-md032-plus-linestart.sh`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-04-24T17:39:08Z on `tools/hygiene/audit-md032-plus-linestart.sh`:106 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Ignore fenced code blocks in MD032 preflight**

  Track fenced-code state before treating `^ {0,3}\+ ` as a candidate, otherwise valid code/math snippets are reported as gaps. In this commit, running `tools/hygiene/audit-md032-plus-linestart.sh --list` flags `docs/aurora/2026-04-23-amara-aurora-aligned-ksk-design-7th-ferry.md:446`, which is inside a triple-backtick block (`+ β₁(1-P(c))`). Markdownlint MD032 does not evaluate list spacing inside fenced code, so this creates false positives and will make `--enforce` block clean markdown once enforcement is enabled.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 26 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM59eBx8`
- **Path**: `docs/FACTORY-HYGIENE.md`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-24T17:41:15Z on `docs/FACTORY-HYGIENE.md`:102 (association: NONE)

  <pre>
  Row 56’s durable output references `docs/hygiene-history/md032-plus-linestart-audit-history.md`, but that file doesn’t exist in the repo right now. Add the history file (even as an empty scaffold with the per-fire schema) or update the row to point at an existing durable output surface so pointer-integrity hygiene doesn’t immediately drift.
  </pre>

### Thread 27 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM59eByT`
- **Path**: `docs/FACTORY-HYGIENE.md`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-24T17:41:16Z on `docs/FACTORY-HYGIENE.md`:102 (association: NONE)

  <pre>
  This edit appears to have removed previously-listed hygiene rows (#57 and #60) from the table, which conflicts with this doc’s stated rule that the list is additive and retiring rows requires an ADR. If those hygiene items are still active, they should be restored; if they’re being retired, add the required ADR + keep a traceable retirement note instead of silent deletion.
  </pre>

### Thread 28 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM59eByb`
- **Path**: `docs/FACTORY-HYGIENE.md`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-24T17:41:16Z on `docs/FACTORY-HYGIENE.md`:102 (association: NONE)

  <pre>
  The baseline gap count in this new row (`~170 gaps at repo scope`) doesn’t match the PR description’s stated baseline (`3` pre-existing gaps in persona notebooks). Please reconcile these so the row reflects the actual current baseline (and the enforce-transition plan is based on the right number).
  </pre>

### Thread 29 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM59eByk`
- **Path**: `docs/FACTORY-HYGIENE.md`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-24T17:41:16Z on `docs/FACTORY-HYGIENE.md`:102 (association: NONE)

  <pre>
  The regex description in this row says `regex ^ {0,3}\+` “followed by a single space”, but the actual detector is for `+` *and* a space (`^ {0,3}\+ `). Consider reflecting the trailing space in the inline regex to avoid ambiguity for readers copying the pattern.
  </pre>

### Thread 30 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM59eByz`
- **Path**: `tools/hygiene/audit-md032-plus-linestart.sh`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-24T17:41:17Z on `tools/hygiene/audit-md032-plus-linestart.sh`:68 (association: NONE)

  <pre>
  The audit’s `exclude_pattern` doesn’t match the markdownlint scope: markdownlint ignores `memory/**` and `docs/amara-full-conversation/**` (per `.markdownlint-cli2.jsonc`), but the audit will still scan them and can report gaps that can’t cause an MD032 CI failure. Either align exclusions to markdownlint’s ignore list (at least exclude `memory/` + `docs/amara-full-conversation/`) or clarify in the header/FACTORY-HYGIENE row that this audit intentionally scans additional non-linted surfaces.
  </pre>

### Thread 31 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM59eBzC`
- **Path**: `tools/hygiene/audit-md032-plus-linestart.sh`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-24T17:41:17Z on `tools/hygiene/audit-md032-plus-linestart.sh`:68 (association: NONE)

  <pre>
  Related: `exclude_pattern` skips `docs/hygiene-history/**` and `docs/DECISIONS/**`, but the markdownlint CI job lints those paths (`markdownlint-cli2 "**/*.md"` with no ignores for them). If the goal is “preflight to avoid MD032 CI breaks”, consider scanning these directories too, or alternatively add them to the markdownlint ignore list so audit+CI scopes stay consistent.
  </pre>

## Fix commits (touching thread paths)

### `3291eab0ced450edb2b513e5bcb1eb00ea36c466` -- 2026-04-23T21:43:55Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `docs/FACTORY-HYGIENE.md`
- `tools/hygiene/audit-md032-plus-linestart.sh`

**Message:**

```
hygiene: MD032 '+'-at-line-start preflight audit (FACTORY-HYGIENE row…

… #56)

Addresses recurring MD032 regression hit 3 times in
Otto-session (Otto-35 + Otto-38 + Otto-38 again).
Pattern: author writes prose continuation like
  Full treatment in the DBSP paper
  + docs/ARCHITECTURE.md §operator-algebra.
and markdownlint parses the '+ ' as a list item,
firing MD032 (list not surrounded by blank lines).

Tool: tools/hygiene/audit-md032-plus-linestart.sh —
scans tracked .md files for '^+ ' with non-blank
previous line. Skips files using '+ ' consistently
as bullet style (3+ occurrences). --list / --enforce
modes per row #55 pattern.

Baseline at first fire: 3 pre-existing gaps in
persona notebooks (memory/persona/best-practices-
scratch.md, ilyana/NOTEBOOK.md, kenji/OFFTIME.md).
Detect-only; enforce-transition deferred.

FACTORY-HYGIENE row #56 lands with full schema per
row #55 pattern. Classified prevention-bearing
(row #47 taxonomy).

Attribution: Otto (loop-agent PM hat).

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
```

### `1f35c1d23f46cfdaa5b1d6232951eb8c154f4f3b` -- 2026-04-23T21:46:52Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `docs/FACTORY-HYGIENE.md`

**Message:**

```
hygiene: fix MD038 trailing-space-in-code-span on row #56

Replaced '`+ `' + '`^+ `' with '`+` followed by space' +
'`^+` (plus-at-line-start followed by space)' to remove
trailing spaces inside code spans.

Markdownlint MD038 rule forbids leading/trailing spaces in
code spans. The semantic meaning ('+' followed by space
character) is now in prose.

Attribution: Otto (loop-agent PM hat).

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
```

### `35a6819281dcb126a23a4e838a0bbc652e1becb6` -- 2026-04-24T16:49:27Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `docs/FACTORY-HYGIENE.md`
- `tools/hygiene/audit-md032-plus-linestart.sh`

**Message:**

```
hygiene: rewrite MD032 audit for CommonMark correctness + review-drai…

…n fixes (PR #204 threads)

Addresses 19 unresolved review threads on PR #204 clustered into six
distinct issues — consolidated into a single cohesive rewrite:

  * NUL-delimited `git ls-files -z | while read -d ''` iteration
    (whitespace-safe paths; matches sibling hygiene-script convention).
  * `cd "$(git rev-parse --show-toplevel)"` first so paths resolve
    from repo root regardless of $PWD.
  * Previous-line blank check strips all whitespace (spaces, tabs, CR)
    via `${var//[[:space:]]/}` — tab-only predecessors count as blank.
  * Contiguous `+ `-list detection: if the previous line is itself a
    `^ {0,3}\+ ` marker line, it's list continuation, not a gap.
  * CommonMark-compliant pattern `^ {0,3}\+ ` — up to 3 leading spaces
    allowed on `+ `-marker lines (indented `+` in header example now
    matched).
  * `plus_lines_count >= 3` file-level skip heuristic removed —
    per-line contiguous-list detection makes it unnecessary and was
    masking false negatives (file could mostly use `+` as list marker
    while still containing a stray prose-continuation `+`).
  * Post-setup stack header added to match sibling convention.
  * Portability: `while read`-based file load instead of `mapfile`
    (macOS bash 3.2 parity).

FACTORY-HYGIENE.md row #56 description updated to match the new
behaviour (pre-merge refinement of the new row, permitted under
append-only audit-trail discipline).

Build: 0W/0E.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
```

### `ea506db699c724bc910b0236167ffed043615ee1` -- 2026-04-24T17:25:01Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `docs/FACTORY-HYGIENE.md`

**Message:**

```
hygiene: markdownlint-fix MD038/MD056 on FACTORY-HYGIENE row #56

Line 102 in docs/FACTORY-HYGIENE.md triggered 11 MD038
(no-space-in-code) warnings and 1 MD056 (table-column-count)
warning. Rewrote the row text to:

- Replace code-span literals that started or ended with a
  space (`+ `, `^ {0,3}\+ `, `. Excludes: `, `, `, `; `, etc)
  with prose phrasings that keep the meaning without the
  MD038-tripping whitespace inside backticks.
- Split the `git ls-files -z | while read -d ''` code span
  into two separate spans so the `|` does not land inside a
  code-span in a table cell (markdownlint-cli2 was counting
  it as a ninth column against the 8-column header).

No semantic change to the hygiene rule itself; no changes to
tools/hygiene/audit-md032-plus-linestart.sh. Full repo
markdownlint-cli2@0.18.1 run is clean; dotnet build -c Release
is 0W/0E.
```

### `b2e3d0f304e9641ab6c20fdad64800df9208c8b9` -- 2026-04-24T17:35:55Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/hygiene/audit-md032-plus-linestart.sh`

**Message:**

```
hygiene: fix sibling-script citation in audit-md032 comment

Copilot caught that audit-memory-references.sh doesn't use
`git ls-files -z` iteration (it parses a single target file).
Replace with audit-post-setup-script-stack.sh which does use
the NUL-delimited pattern. No behaviour change; comment only.

Addresses PR #204 Copilot review thread on line 82.
```
