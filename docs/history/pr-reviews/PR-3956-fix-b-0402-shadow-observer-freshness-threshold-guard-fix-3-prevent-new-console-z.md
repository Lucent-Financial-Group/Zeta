# PR #3956 -- fix(B-0402): shadow observer freshness-threshold guard (fix #3 — prevent new-console zsh abort)

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 3956 |
| Title | fix(B-0402): shadow observer freshness-threshold guard (fix #3 — prevent new-console zsh abort) |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-05-16T22:20:10Z |
| Merged at | 2026-05-16T23:51:16Z |
| Merge commit SHA | `3cad45557583c8f23e782b6123d1d7fc85dc07fa` |
| Branch | `fix/b-0402-shadow-freshness-threshold-2026-05-16` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/3956 |
| Changed files | 4 |
| Additions / deletions | +283 / -2 |

## Description

## Summary

Fix #3 for the 2026-05-16 shadow loop bug: a pid-aware freshness threshold prevents the observer from running AX queries + restore-arrow keypresses on newly-focused terminal windows that are still in shell-init. Aaron experienced the bug as "aborted my zsh user stuff" requiring a reboot to recover.

## Root cause

The disabled production plist (`com.zeta.shadow-observer.plist.disabled-2026-05-16T20-42-35Z`) ran with `--dry-run --restore-arrow --loop-interval 2000`. The dry-run flag only suppresses the destructive Enter-to-accept; `--restore-arrow` runs unconditionally per the existing plist comment ("the two flags are orthogonal"). Result: every 2 seconds, `restore-arrow.applescript` fired in the frontmost terminal — sampling `AXValue` via `AXFocusedUIElement`, pressing `key code 124` (right arrow), then re-sampling. On a newly-opened terminal mid-zsh init, this combination (AX queries on partially-built terminal widget + keypress injection during zsh setup) could plausibly abort init.

## Fix

Pid-aware freshness threshold: before any AX-heavy work in `runOneCycle`, query frontmost app pid+name via a new cheap AppleScript (`get-frontmost-state.applescript`); skip the cycle entirely if that window has been frontmost for less than `freshnessThresholdMs`. Default 5000ms in production (set explicitly via the launchd plist's `--freshness-threshold-ms 5000` flag).

## Diff

- `tools/shadow/get-frontmost-state.applescript` (NEW, 32 lines) — minimal pid+name query, no AX traversal, no key injection
- `tools/shadow/shadow-observer.ts` (+120 lines): `FrontmostState` type, `getFrontmostState` fn, `checkWindowFresh` pure fn, `frontmostHistory` Map, `_resetFrontmostHistory` test helper, `freshnessThresholdMs` config field, CLI flag, early-exit in `runOneCycle` with new `"skipped-fresh-window"` verdict
- `tools/shadow/shadow-observer.test.ts` (+52 lines): `freshnessThresholdMs: 0` added to existing 3 fixtures, 4 new tests for freshness behavior (66 pass total, was 62)
- `tools/shadow/launchd/com.zeta.shadow-observer.plist` (+9 lines): `--freshness-threshold-ms 5000` in `ProgramArguments`; comment block explaining fix #3 rationale

## CLI default = 0 (disabled); production default = 5000 (enabled via plist)

Tests + ad-hoc CLI invocations get deterministic first-cycle detection; production safety choice is visible in the plist config rather than hidden in a CLI default. This composes with the explicit-flag-in-deployment-config discipline.

## Test plan

- [x] All 66 existing tests pass (+4 new freshness tests)
- [x] markdownlint clean
- [x] plist `plutil -lint` passes (template syntax)
- [ ] Post-merge: run `bun tools/shadow/launchd/install-launchagent.ts` to reinstall plist with new flag
- [ ] Post-install: verify new terminal opens without zsh-init interference

## Composes with

- B-0402 (shadow observer original substrate)
- The 2026-05-16 disabled plist (root-cause evidence)
- The existing `detect-grey-text.applescript` lines 40-49 comment (prior failure-mode preservation — same family: querying AX of unprepared windows has side effects)
- `.claude/rules/shadow-star-shorthand-autocomplete-marker.md` (the shadow infrastructure that produces `(shadow*)` markers when accepted suggestions get shipped)

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | true |
| Total threads | 6 |
| Resolved threads | 6 |
| Unresolved threads | 0 |
| Total review comments | 12 |
| Total fix commits (touching thread paths) | 2 |

## Review threads

### Thread 1 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6CmLNB`
- **Path**: `tools/shadow/shadow-observer.ts`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-16T22:21:57Z on `tools/shadow/shadow-observer.ts`:442 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Skip cycle when frontmost probe returns unknown state**

  When `--freshness-threshold-ms` is enabled, the guard currently skips only if `state !== null` and `checkWindowFresh(...)` is false. If the probe fails (e.g., transient `osascript`/Accessibility failure), `state` is `null` and execution falls through to AX-heavy detection and optional restore-arrow keypresses, which is fail-open and bypasses the safety behavior this change is meant to enforce.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-16T23:51:04Z on `tools/shadow/shadow-observer.ts`:442 (association: MEMBER)

    <pre>
    Addressed in commit `bdc56f232`. `runOneCycle` now checks `if (state === null)` immediately after `await getFrontmostStateFn()` and returns `"skipped-fresh-window"` with log content `"frontmost-unknown (probe returned null)"` before any AX-heavy detection or restore-arrow keypress. The fail-open path on probe failure is closed.
    </pre>

### Thread 2 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6CmLNC`
- **Path**: `tools/shadow/shadow-observer.ts`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-16T22:21:57Z on `tools/shadow/shadow-observer.ts`:398 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Track freshness per focused window, not per app process**

  The freshness key is derived from `pid|name`, which identifies only the application process. Opening a new Terminal/iTerm window/tab inside an already-seen process reuses the same key and inherits an old `firstSeen`, so the new window is treated as "not fresh" immediately. That means the guard does not protect newly opened consoles once the app has been observed before.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-16T23:51:05Z on `tools/shadow/shadow-observer.ts`:398 (association: MEMBER)

    <pre>
    Substrate-honest acknowledgment of a real limitation: the per-process key (`pid|name`) prevents the **first** window of a freshly-launched app from interfering with shell init — the specifically-named B-0402 failure mode. Subsequent new windows in an already-running app inherit `firstSeen` and are not skipped. Full per-window granularity needs window-level identification, which on macOS typically means AXUI traversal — directly contradicting this probe's cheap-by-design contract. Resolving for this PR's bounded scope; refinement is queued as B-0402 follow-up.
    </pre>

### Thread 3 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6CmMKA`
- **Path**: `tools/shadow/shadow-observer.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-16T22:26:27Z on `tools/shadow/shadow-observer.ts`:454 (association: NONE)

  <pre>
  The code only skips when `state !== null`, but `getFrontmostState`’s docstring says callers treat `null` as “unknown, skip cycle.” As written, when the frontmost query fails (no accessibility permission / osascript error), the cycle proceeds into AX-heavy work anyway, undermining the guard’s safety goal. Consider changing this branch to skip the cycle when `state === null` (when `freshnessThresholdMs &gt; 0`), optionally logging a distinct reason (e.g. `"skipped-fresh-window:unknown-frontmost"`).
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-16T23:51:07Z on `tools/shadow/shadow-observer.ts`:454 (association: MEMBER)

    <pre>
    Addressed in commit `bdc56f232`. The freshness guard in `runOneCycle` now explicitly skips when `state === null` (only when `freshnessThresholdMs &gt; 0`), logging the distinct reason `"frontmost-unknown (probe returned null)"`. The cycle no longer proceeds into AX-heavy work on a probe failure.
    </pre>

### Thread 4 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6CmMKH`
- **Path**: `tools/shadow/shadow-observer.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-16T22:26:27Z on `tools/shadow/shadow-observer.ts`:379 (association: NONE)

  <pre>
  Parsing `&lt;pid&gt;|&lt;app-name&gt;` is brittle if the application name contains `|` (the first `|` split will corrupt the name and may cause false `null` returns). A more robust output format would be either: (1) two-line output (`pid` on line 1, `name` on line 2), or (2) JSON output (e.g. `{"pid":..., "name":"..."}`) so escaping is well-defined.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-16T23:51:09Z on `tools/shadow/shadow-observer.ts`:379 (association: MEMBER)

    <pre>
    Verified via direct inspection — this is a false positive. The parse uses `trimmed.indexOf("|")` which returns the **first** `|` position only; `trimmed.slice(sepIdx + 1)` then takes everything after that first separator, preserving any subsequent `|` characters as part of the name. For input `"1234|Foo|Bar"`, parse yields `{pid: 1234, name: "Foo|Bar"}` correctly. Edge cases (empty pid, empty name, missing separator) all return `null` via the `Number.isFinite(pid) || pid &lt;= 0 || name.length === 0` guard.
    </pre>

### Thread 5 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6CmMKJ`
- **Path**: `tools/shadow/shadow-observer.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-16T22:26:27Z on `tools/shadow/shadow-observer.ts`:345 (association: NONE)

  <pre>
  `frontmostHistory` is module-level and never pruned, so a long-running launch agent can accumulate entries over time (e.g., many distinct pids across app restarts), causing unbounded memory growth. Consider adding a simple pruning strategy (e.g., cap map size, or store `{firstSeen,lastSeen}` and periodically delete entries not seen in N minutes) when inserting/updating in `checkWindowFresh`.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-16T23:51:11Z on `tools/shadow/shadow-observer.ts`:345 (association: MEMBER)

    <pre>
    Addressed in commit `bdc56f232`. `frontmostHistory` is now bounded by `FRONTMOST_HISTORY_MAX = 256` with LRU-style eviction in `checkWindowFresh` — when the map reaches capacity, the oldest insertion is evicted before the new entry is added. The unbounded-growth concern is closed.
    </pre>

### Thread 6 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6CmMKK`
- **Path**: `tools/shadow/shadow-observer.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-16T22:26:27Z on `tools/shadow/shadow-observer.ts`:387 (association: NONE)

  <pre>
  The comment calls `checkWindowFresh` “Pure” but it mutates `history` (and by default the module-level `frontmostHistory`). This is misleading for readers and test authors. Either update the docstring to describe the side-effect plainly (and remove “Pure”), or refactor to a pure function that returns an updated history/value pair without mutating inputs.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-16T23:51:13Z on `tools/shadow/shadow-observer.ts`:387 (association: MEMBER)

    <pre>
    Addressed in commit `bdc56f232`. The docstring was rewritten to plainly describe the side effect: `"Freshness check with side-effect: records first-seen ms in `\history`\ on first sighting so subsequent cycles can measure age."` The misleading "Pure" framing has been removed.
    </pre>

## Fix commits (touching thread paths)

### `68d001e250f7c905cdb0d3f97a441e7d96a686c0` -- 2026-05-16T22:19:06Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/shadow/shadow-observer.ts`

**Message:**

```
fix(B-0402): shadow observer freshness-threshold guard (new-console z…

…sh abort)

2026-05-16 bug: with the disabled production plist's flags
(--dry-run --restore-arrow --loop-interval 2000), the shadow loop
queried AX + pressed → in the frontmost terminal every 2s. On a
NEWLY-OPENED terminal mid-zsh init, the AX queries + keypress could
interfere with zsh interactive startup (Aaron experienced this and
had to reboot to recover — "aborted my zsh user stuff").

Fix #3 (Aaron-picked option of three): pid-aware freshness threshold.
Before any AX-heavy work in runOneCycle, query frontmost app pid+name
via the new cheap get-frontmost-state.applescript; skip the cycle if
that window has been frontmost for less than freshnessThresholdMs.

Production plist sets --freshness-threshold-ms 5000 explicitly.
CLI default remains 0 (disabled) so test fixtures + ad-hoc CLI invocations
see deterministic detection on first cycle — the production safety
choice is visible in the plist config rather than hidden in a CLI default.

Changes:
- get-frontmost-state.applescript: NEW (15 lines) — minimal pid+name query
- shadow-observer.ts: +FrontmostState type, +getFrontmostState fn,
  +checkWindowFresh pure fn, +frontmostHistory Map, +_resetFrontmostHistory
  test helper, +freshnessThresholdMs config field, +CLI flag, +early-exit
  in runOneCycle with new "skipped-fresh-window" verdict
- shadow-observer.test.ts: +freshnessThresholdMs:0 to existing fixtures,
  +4 new tests for freshness behavior (66 pass total, was 62)
- launchd plist: +--freshness-threshold-ms 5000 in ProgramArguments,
  + plist comment block explaining the fix's rationale

Aaron's directive: "go with (a) (shadow*) maybe you can do whatever
now i now how to fix it if it's an issue" — option (a) was implement
fix #3 before re-enabling.

Re-enabling launchd is a separate follow-up — once this lands on main,
run `bun tools/shadow/launchd/install-launchagent.ts` to reinstall the
template with the new flag.

Co-Authored-By: Claude <noreply@anthropic.com>
```

### `bdc56f232ea22b5c7c9a5d10b0b80aeeb8b31905` -- 2026-05-16T23:09:51Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/shadow/shadow-observer.ts`

**Message:**

```
fix(B-0402): address Copilot review — null-state skip, bounded histor…

…y, docstring

Three substantive fixes from PR #3956 review:

1. **Null-state skip (P1)** — `getFrontmostState`'s docstring contract says
   caller treats null as "unknown, skip cycle." Previous code only skipped
   when state was non-null and too-fresh; a probe failure (no accessibility
   permission, osascript error) let the cycle proceed into AX-heavy work
   anyway, undermining the guard. Now: null also returns
   "skipped-fresh-window" with a "frontmost-unknown" log entry.

2. **Bounded history** — `frontmostHistory` was module-level and never
   pruned, so a long-running launch agent could accumulate entries across
   pid churn. Now capped at `FRONTMOST_HISTORY_MAX = 256`; on overflow
   the oldest insertion is evicted (Map preserves insertion order).

3. **Docstring honesty** — `checkWindowFresh`'s comment claimed "Pure"
   but it mutates `history`. Replaced with side-effect-naming description.

Plus 2 new tests covering both fixes.

Composes with `.claude/rules/blocked-green-ci-investigate-threads.md` —
findings classified, fixed where substantive, replies cite verification.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
```
