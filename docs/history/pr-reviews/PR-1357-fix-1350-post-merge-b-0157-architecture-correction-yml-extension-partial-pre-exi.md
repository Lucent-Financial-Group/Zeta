# PR #1357 -- fix(#1350 post-merge): B-0157 architecture correction + .yml extension + partial pre-existence (4 substantive findings + 3 merge-ordering false positives)

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 1357 |
| Title | fix(#1350 post-merge): B-0157 architecture correction + .yml extension + partial pre-existence (4 substantive findings + 3 merge-ordering false positives) |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-05-03T08:13:49Z |
| Merged at | 2026-05-03T08:15:06Z |
| Merge commit SHA | `ff956bd3da5546b0d4b118980d0fccd064fc1bc7` |
| Branch | `backlog/B-0157-followup-yml-extension-and-stale-problem-statement` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/1357 |
| Changed files | 1 |
| Additions / deletions | +26 / -15 |

## Description

## Summary

Substantive post-merge review findings on B-0157 absorbed:

1. **Cross-workflow `needs:` is invalid** (line 67 P2) — GitHub Actions \`needs:\` works only within single workflow file (\`jobs.<job_id>.needs\`), NOT across workflow files. **Architecture correction**: row now specifies three options (Architecture A: reusable workflow via \`workflow_call\` — recommended; Architecture B: per-workflow detect-job; Architecture C: single consolidated workflow).

2. **\`.yaml\` → \`.yml\` extension** (lines 41 + 66) — all existing workflows use \`.yml\`; one-off \`.yaml\` would create drift. Updated throughout.

3. **Stale problem statement** (line 35) — \`gate.yml\` already skips on docs-only via \`path-gate\`; \`codeql.yml\` short-circuits pure docs; \`backlog-index-integrity\` exists. Row now acknowledges partial pre-existence — generalizes pattern, doesn't introduce from zero.

The remaining 3 threads (lines 8, 25, 62) were merge-ordering false positives — B-0142 wasn't present at PR-eval time but merged later as #1349. Resolved with explanation.

## Test plan

- [x] Architecture section corrected with valid GitHub Actions semantics
- [x] All `.yaml` references → `.yml`
- [x] Partial-pre-existence acknowledgment added
- [x] markdownlint passes locally

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 3 |
| Resolved threads | 3 |
| Unresolved threads | 0 |
| Total review comments | 6 |
| Total fix commits (touching thread paths) | 1 |

## Review threads

### Thread 1 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5_LEe-`
- **Path**: `docs/backlog/P2/B-0157-detect-changes-pattern-parallel-pr-friendly-aaron-2026-05-01.md`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-03T08:16:00Z on `docs/backlog/P2/B-0157-detect-changes-pattern-parallel-pr-friendly-aaron-2026-05-01.md`:64 (association: NONE)

  <pre>
  P2: The doc describes per-class outputs as `true|false`, but GitHub Actions job/workflow outputs are strings in expressions (and your own example later compares to `'true'`). To avoid confusion, document the outputs as `'true'|'false'` (string) or explicitly call out that they are string booleans.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-03T08:23:50Z on `docs/backlog/P2/B-0157-detect-changes-pattern-parallel-pr-friendly-aaron-2026-05-01.md`:64 (association: MEMBER)

    <pre>
    Addressed in latest commit. Three substantive corrections + Aaron 2026-05-03 architectural addition:

    1. **Skipped-required-check** (P1): claim was INCORRECT for repo reality. Corrected: `gate.yml` + `code_quality severity:all` treats skipped as FAILURE; the right pattern is required-check name reports SUCCESS via cheap-no-op-path while expensive steps internally skip. NOT job skipped externally.

    2. **String outputs** (P2): `true|false` → `true|false` (string-typed; GitHub Actions outputs are strings in expressions).

    3. **Grammar** (P2): "generalizes ..., NOT introduces gating from zero" → "generalizes ... and does NOT introduce gating from zero".

    4. **Per-gate-ruleset architecture** (Aaron 2026-05-03 correction; recurring substrate per B-0155): each parallel-forever-workflow has its OWN ruleset that stays always-on; declarative git-native files (NOT legacy branch-protection API). Composes with B-0155 (multi-ruleset split, 5 concern-aligned rulesets validated by sibling-repo). Frontmatter `composes_with` updated to include B-0155.
    </pre>

### Thread 2 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5_LEfE`
- **Path**: `docs/backlog/P2/B-0157-detect-changes-pattern-parallel-pr-friendly-aaron-2026-05-01.md`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-03T08:16:01Z on `docs/backlog/P2/B-0157-detect-changes-pattern-parallel-pr-friendly-aaron-2026-05-01.md`:37 (association: NONE)

  <pre>
  P2: Grammar issue: “This row generalizes …, NOT introduces gating from zero” is ungrammatical and reads like a typo. Consider rephrasing to “...and does NOT introduce gating from zero” (or similar) for clarity.

  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-03T08:23:51Z on `docs/backlog/P2/B-0157-detect-changes-pattern-parallel-pr-friendly-aaron-2026-05-01.md`:37 (association: MEMBER)

    <pre>
    Addressed in latest commit. Three substantive corrections + Aaron 2026-05-03 architectural addition:

    1. **Skipped-required-check** (P1): claim was INCORRECT for repo reality. Corrected: `gate.yml` + `code_quality severity:all` treats skipped as FAILURE; the right pattern is required-check name reports SUCCESS via cheap-no-op-path while expensive steps internally skip. NOT job skipped externally.

    2. **String outputs** (P2): `true|false` → `true|false` (string-typed; GitHub Actions outputs are strings in expressions).

    3. **Grammar** (P2): "generalizes ..., NOT introduces gating from zero" → "generalizes ... and does NOT introduce gating from zero".

    4. **Per-gate-ruleset architecture** (Aaron 2026-05-03 correction; recurring substrate per B-0155): each parallel-forever-workflow has its OWN ruleset that stays always-on; declarative git-native files (NOT legacy branch-protection API). Composes with B-0155 (multi-ruleset split, 5 concern-aligned rulesets validated by sibling-repo). Frontmatter `composes_with` updated to include B-0155.
    </pre>

### Thread 3 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5_LEfM`
- **Path**: `docs/backlog/P2/B-0157-detect-changes-pattern-parallel-pr-friendly-aaron-2026-05-01.md`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-03T08:16:01Z on `docs/backlog/P2/B-0157-detect-changes-pattern-parallel-pr-friendly-aaron-2026-05-01.md`:66 (association: NONE)

  <pre>
  P1: The statement that a “skipped-but-required check still counts as passing” appears incorrect for this repo’s branch protection/ruleset setup. `gate.yml` explicitly avoids skipped jobs because `code_quality severity:all` treats skipped jobs as failure rather than success; this row should align with that reality (i.e., keep required check names reporting SUCCESS and skip only the expensive steps, or emit a cheap baseline artifact/status instead of skipping the job).

  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-03T08:23:53Z on `docs/backlog/P2/B-0157-detect-changes-pattern-parallel-pr-friendly-aaron-2026-05-01.md`:66 (association: MEMBER)

    <pre>
    Addressed in latest commit. Three substantive corrections + Aaron 2026-05-03 architectural addition:

    1. **Skipped-required-check** (P1): claim was INCORRECT for repo reality. Corrected: `gate.yml` + `code_quality severity:all` treats skipped as FAILURE; the right pattern is required-check name reports SUCCESS via cheap-no-op-path while expensive steps internally skip. NOT job skipped externally.

    2. **String outputs** (P2): `true|false` → `true|false` (string-typed; GitHub Actions outputs are strings in expressions).

    3. **Grammar** (P2): "generalizes ..., NOT introduces gating from zero" → "generalizes ... and does NOT introduce gating from zero".

    4. **Per-gate-ruleset architecture** (Aaron 2026-05-03 correction; recurring substrate per B-0155): each parallel-forever-workflow has its OWN ruleset that stays always-on; declarative git-native files (NOT legacy branch-protection API). Composes with B-0155 (multi-ruleset split, 5 concern-aligned rulesets validated by sibling-repo). Frontmatter `composes_with` updated to include B-0155.
    </pre>

## Fix commits (touching thread paths)

### `c5284fc26e51cf8dde87f0f1fee683c1e6583d99` -- 2026-05-03T08:13:36Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `docs/backlog/P2/B-0157-detect-changes-pattern-parallel-pr-friendly-aaron-2026-05-01.md`

**Message:**

```
fix(#1350 post-merge): B-0157 architecture correction + .yml extensio…

…n + partial pre-existence acknowledged

Three substantive review findings absorbed:

1. Cross-workflow `needs:` is invalid — `needs:` works only within
   single workflow file. Architecture corrected to specify three
   options (Architecture A: reusable workflow via workflow_call;
   Architecture B: per-workflow detect-job; Architecture C: single
   consolidated workflow). Architecture A recommended (matches
   sibling-repo pattern + preserves separation).

2. .yaml → .yml extension throughout — repo's existing workflows
   consistently use .yml; one-off .yaml suffix would create drift.

3. Problem-statement acknowledged partial pre-existence — gate.yml
   already skips on docs-only via path-gate; codeql.yml
   short-circuits pure docs; backlog-only changes have
   backlog-index-integrity. Row generalizes the partial pattern
   into uniform mechanism, NOT from-zero gating.

The B-0142-stale-cross-references in some threads were
merge-ordering false positives (B-0142 not present at PR-eval time;
merged later as #1349) — handled in resolve-thread comments.
```
