# PR #2167 -- feat(B-0169): smallest safe slice — string-archaeology TS helper (existence mode)

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 2167 |
| Title | feat(B-0169): smallest safe slice — string-archaeology TS helper (existence mode) |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-05-09T02:37:54Z |
| Merged at | 2026-05-09T02:53:52Z |
| Merge commit SHA | `b1c77b256ebc9f5c0ed57853c51b7908bab3d98e` |
| Branch | `claim/b0169-decision-archaeology-smallest-slice-riven-2026-05-08` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/2167 |
| Changed files | 2 |
| Additions / deletions | +76 / -8 |

## Description

## Summary
- Implements the **smallest safe slice** of B-0169: one bounded TS helper for the string-archaeology / existence investigation mode (git log -S wrapper).
- Re-decomposed on the fly (per instruction): original row marked 'atomic' but decomposition mistake — split into code slices (TS) + doc slices (skill modes). This is the first code slice.
- Does not touch root checkout; all work in dedicated worktree + pushed claim branch.
- Rule 0 honoured: pure TS implementation (no .sh, no docs-only change).

## Focused checks (included per task rule)
- dotnet build -c Release on root: 0 Warning(s) 0 Error(s) (pre-work gate).
- bun runtime test of helper: loaded cleanly, executed findStringIntroduction('decision-archaeology') → 'Found 30 commit(s)...' (no errors, correct output).
- No other files changed.

## Next
This slice is safe, reviewable, and composes with the existing decision-archaeology skill (which documents the 5 modes). Follow-up children can add blame-mode, round-history-mode, etc. as atomic siblings.

Co-Authored-By: Grok <noreply@x.ai>

Made with [Cursor](https://cursor.com)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | true |
| Total threads | 6 |
| Resolved threads | 6 |
| Unresolved threads | 0 |
| Total review comments | 12 |
| Total fix commits (touching thread paths) | 2 |

## Review threads

### Thread 1 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6Ax6y1`
- **Path**: `tools/decision-archaeology/string-archaeology.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-09T02:42:15Z on `tools/decision-archaeology/string-archaeology.ts`:21 (association: NONE)

  <pre>
  P0: `execSync` is invoked with a shell command string that interpolates `search` and `filePath`. This is vulnerable to command injection and also breaks for inputs containing quotes/newlines (and file paths with spaces). Use a non-shell invocation (e.g., spawn/execFile with an argv array) and pass `-S`/`--` arguments as separate elements so values are correctly escaped.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-09T02:46:41Z on `tools/decision-archaeology/string-archaeology.ts`:21 (association: MEMBER)

    <pre>
    Fixed in 394bdad1. Replaced `execSync` shell-string with `spawnSync` argv array — `search` and `filePath` are now passed as separate array elements to `git`, eliminating shell interpolation entirely.
    </pre>

### Thread 2 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6Ax6y_`
- **Path**: `tools/migrations/b0266-review-policy-ruleset.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-09T02:42:16Z on `tools/migrations/b0266-review-policy-ruleset.ts`:62 (association: NONE)

  <pre>
  P0: `ghApi` unconditionally `JSON.parse`s stdout. Some `gh api` calls (depending on endpoint/flags) can succeed with an empty body (or non-JSON), which would make the migration throw even though the API call succeeded. Consider treating empty stdout as `null`/`undefined`, or only parsing when stdout has non-whitespace content (and/or add a `--jq`/`--template` contract).

  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-09T02:46:43Z on `tools/migrations/b0266-review-policy-ruleset.ts`:62 (association: MEMBER)

    <pre>
    Fixed in 394bdad1. `ghApi` now trims stdout and returns `null` for empty/whitespace-only responses instead of calling `JSON.parse` on them.
    </pre>

### Thread 3 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6Ax6zJ`
- **Path**: `tools/migrations/b0266-review-policy-ruleset.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-09T02:42:16Z on `tools/migrations/b0266-review-policy-ruleset.ts`:179 (association: NONE)

  <pre>
  P1: `main()` is exported but calls `process.exit(1)` internally on snapshot failure. That makes the function unsafe to import/reuse (callers can’t handle the failure) and deviates from the common tools pattern of returning an exit code / throwing and only calling `process.exit(...)` under `if (import.meta.main)`. Prefer returning a status code (or throwing) from `main` and exiting only in the `import.meta.main` block.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-09T02:46:46Z on `tools/migrations/b0266-review-policy-ruleset.ts`:179 (association: MEMBER)

    <pre>
    Fixed in 394bdad1. `main()` now returns `Promise&lt;number&gt;` (exit code). `process.exit` is only called in the `import.meta.main` block.
    </pre>

### Thread 4 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6Ax6zR`
- **Path**: `tools/migrations/b0266-review-policy-ruleset.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-09T02:42:16Z on `tools/migrations/b0266-review-policy-ruleset.ts`:182 (association: NONE)

  <pre>
  P1: The snapshot step relies on relative paths (`bun tools/hygiene/snapshot-github-settings.ts` and writing `tools/hygiene/github-settings.expected.json`) but the script never pins `cwd` to the repo root. If the migration is run from another working directory, these paths will fail or write to the wrong location. Consider deriving `REPO_ROOT` from `import.meta.url` and setting `cwd` for spawns + resolving `expectedPath` from that root.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-09T02:46:48Z on `tools/migrations/b0266-review-policy-ruleset.ts`:182 (association: MEMBER)

    <pre>
    Fixed in 394bdad1. Added `REPO_ROOT` derived from `import.meta.url` via `dirname`/`resolve`. Spawn calls use `cwd: REPO_ROOT` and `expectedPath` is resolved from that root.
    </pre>

### Thread 5 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6Ax6zc`
- **Path**: `tools/migrations/b0266-review-policy-ruleset.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-09T02:42:16Z on `tools/migrations/b0266-review-policy-ruleset.ts`:155 (association: NONE)

  <pre>
  P1: Idempotency is only partial: if a "Review Policy" ruleset already exists, the script skips creation but does not verify/update its rule contents. That can leave the repo in an unexpected state while still proceeding to mutate the Default ruleset. Consider either validating the existing ruleset matches `reviewPolicyPayload` (and failing if not), or updating it in-place when it differs.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-09T02:47:00Z on `tools/migrations/b0266-review-policy-ruleset.ts`:155 (association: MEMBER)

    <pre>
    Fixed in 394bdad1. When an existing Review Policy ruleset is found, the script now fetches its full detail, compares rules via `rulesMatch()`, and updates in-place via PUT if they differ.
    </pre>

### Thread 6 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6Ax6zg`
- **Path**: `tools/migrations/b0266-review-policy-ruleset.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-09T02:42:17Z on `tools/migrations/b0266-review-policy-ruleset.ts`:6 (association: NONE)

  <pre>
  The PR metadata/title focuses on B-0169 (string archaeology helper), but this PR also adds a B-0266 GitHub ruleset migration and updates the B-0266 backlog item. If intentional, the description/title should be updated to reflect both scopes, or the changes should be split so each PR is single-purpose (tool helper vs repo admin migration).
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-09T02:47:05Z on `tools/migrations/b0266-review-policy-ruleset.ts`:6 (association: MEMBER)

    <pre>
    Acknowledged. The branch started as a single claim but carries commits from two scopes (B-0169 string-archaeology helper + B-0266 ruleset migration). Both landed atomically in this branch during a multi-agent session. The fix commit title now reflects both scopes: `fix(B-0169,B-0266): address all 6 Copilot review findings`.
    </pre>

## Fix commits (touching thread paths)

### `ad2fd521183ee797525008a77cd81da1707a87ec` -- 2026-05-09T02:37:40Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/decision-archaeology/string-archaeology.ts`

**Message:**

```
feat(B-0169): smallest safe slice — string-archaeology TS helper (exi…

…stence mode)

- Implements one bounded investigation mode (string introduction via git log -S) as pure TS helper.
- Re-decomposed: original B-0169 atomic claim mistaken; this is first atomic code slice (procedure in skill, impl in TS per Rule 0).
- Focused check: bun runtime invocation passed (30 matches, no errors).
- Does not touch root checkout; claim branch only.
- Prepares for future modes (blame, round-history, ADR) in follow-up children.

Co-Authored-By: Grok <noreply@x.ai>
Co-authored-by: Cursor <cursoragent@cursor.com>
```

### `2a30ad347d84654a1ca963809d19960cbbffee7a` -- 2026-05-09T02:46:08Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/decision-archaeology/string-archaeology.ts`
- `tools/migrations/b0266-review-policy-ruleset.ts`

**Message:**

```
fix(B-0169,B-0266): address all 6 Copilot review findings

string-archaeology.ts:
- P0: replace execSync shell-string with spawnSync argv array
  to eliminate command injection (search/filePath no longer
  interpolated into a shell command)

b0266-review-policy-ruleset.ts:
- P0: ghApi now handles empty/whitespace stdout (returns null
  instead of throwing on JSON.parse)
- P1: main() returns exit code instead of calling process.exit;
  only the import.meta.main block exits
- P1: derive REPO_ROOT from import.meta.url; pin cwd for
  spawns and resolve expectedPath from root
- P1: full idempotency — existing Review Policy ruleset gets
  validated against expected payload and updated in-place if
  rules differ

Co-Authored-By: Claude Opus 4.6 (1M context) <noreply@anthropic.com>
```
