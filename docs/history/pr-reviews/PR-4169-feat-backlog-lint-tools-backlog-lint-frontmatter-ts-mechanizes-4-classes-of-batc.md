# PR #4169 -- feat(backlog-lint): tools/backlog/lint-frontmatter.ts — mechanizes 4 classes of batch-7 reviewer findings (B-0663)

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 4169 |
| Title | feat(backlog-lint): tools/backlog/lint-frontmatter.ts — mechanizes 4 classes of batch-7 reviewer findings (B-0663) |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-05-18T16:56:08Z |
| Merged at | 2026-05-18T17:25:55Z |
| Merge commit SHA | `dd18765f023f928df5a63c93dac86f7abe009421` |
| Branch | `otto-cli/b0663-frontmatter-lint-tool-2026-05-18-1255Z` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/4169 |
| Changed files | 2 |
| Additions / deletions | +431 / -0 |

## Description

## Summary

Pre-push frontmatter discipline lint that catches recurring reviewer findings BEFORE PR opens.

### The 4 check classes (all observed multiple times in batch-7)

| Check | What it catches |
|---|---|
| **1. Wrong relative-path prefix** | Same-dir link uses `../P{X}/` (should be bare); cross-dir link missing prefix |
| **2. composes_with completeness** | Body cites `B-NNNN` via markdown link but frontmatter omits |
| **3. Non-schema frontmatter keys** | Typo'd keys like `last_invariant` instead of `last_updated` |
| **4. Redundant edges** | `B-NNNN` in both `depends_on` AND `composes_with` (depends_on is stronger) |

### Validation

Tested against batch-7 rows:

- B-0661 (P1): caught 2 findings (composes_with omits B-0639 + B-0660 prefix wrong)
- B-0662 (P2): caught 1 finding (composes_with omits B-0644)

Same class of findings Codex/Copilot caught manually — tool detects mechanically.

### Usage

```bash
# Lint all backlog rows
bun tools/backlog/lint-frontmatter.ts

# Lint specific file
bun tools/backlog/lint-frontmatter.ts --file docs/backlog/P2/B-XXXX.md

# Strict mode (exit 1 on findings; for CI)
bun tools/backlog/lint-frontmatter.ts --strict

# Subset of checks
bun tools/backlog/lint-frontmatter.ts --check 1,3
```

### Implementation

- Pure stdlib (`node:fs` + `node:path`); no external deps
- Inline YAML parsing (flat schema sufficient)
- Schema keys whitelisted per `tools/backlog/README.md`
- ~6 KB compiled output

### Composition

- **B-0650** sibling tooling extension (rest-push.ts --delete/--rename); both mechanize batch-7 patterns
- `tools/backlog/README.md` — schema source-of-truth
- `tools/hygiene/audit-backlog-items.ts` — factory-wide invariants (this row scoped to per-row frontmatter)
- `.claude/rules/blocked-green-ci-investigate-threads.md` — the discipline this tool front-loads

## Ship method

REST git-data API per B-0615 push-hang workaround.

## Test plan

- [x] Compiles cleanly (`bun build` succeeds)
- [x] Detects real findings on batch-7 rows
- [x] Dogfooded on its own backlog row
- [ ] CI passes

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | true |
| Total threads | 16 |
| Resolved threads | 16 |
| Unresolved threads | 0 |
| Total review comments | 16 |
| Total fix commits (touching thread paths) | 3 |

## Review threads

### Thread 1 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6C6Gpc`
- **Path**: `tools/backlog/lint-frontmatter.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-18T16:59:08Z on `tools/backlog/lint-frontmatter.ts`:248 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Match dotted backlog IDs when discovering row files**

  The default `walkBacklog` scan excludes valid backlog rows whose filenames include dotted IDs (for example `docs/backlog/P2/B-0037.3-...md` and `B-0054.10-...md`) because it only matches `^B-\d{4}-`. Running the tool without `--file` therefore silently skips a non-trivial subset of rows, so a clean lint result can miss real frontmatter problems in those files.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 2 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6C6Gpj`
- **Path**: `tools/backlog/lint-frontmatter.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-18T16:59:08Z on `tools/backlog/lint-frontmatter.ts`:126 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Parse list-style and dotted IDs in frontmatter B-lists**

  The B-list parser only accepts inline bracket syntax and only IDs matching `B-\d{4}`. In this repo, many rows use YAML list form (`depends_on:` followed by `- B-...`) and dotted IDs like `B-0423.3`; both are valid in existing backlog files. Those entries get dropped to `[]`, which breaks check semantics (for example, redundancy in check 4 and dependency suppression in check 2 are computed from incomplete sets).

  Useful? React with 👍 / 👎.
  </pre>

### Thread 3 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6C6Gpq`
- **Path**: `tools/backlog/lint-frontmatter.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-18T16:59:08Z on `tools/backlog/lint-frontmatter.ts`:205 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Validate bare links against actual target directory**

  Check 1 currently skips every bare `B-....md` link by labeling it `"SAME"` and continuing, so it never detects the documented "cross-dir link missing prefix" case. If a row in `P1` links to a `P0` row using a bare filename instead of `../P0/...`, this check will not report it even though the link path is wrong.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 4 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6C6IGp`
- **Path**: `tools/backlog/lint-frontmatter.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-18T17:00:33Z on `tools/backlog/lint-frontmatter.ts`:125 (association: NONE)

  <pre>
  `parseBList` only recognizes inline flow-style YAML lists (`[B-0001, B-0002]`) via the `^\[(.*)\]$` regex. Many existing backlog rows use YAML block-style lists for `depends_on:` / `composes_with:` (e.g., `docs/backlog/P1/B-0154-*.md:10-17`, B-0164, B-0426, B-0428, B-0429 etc., where the key line ends with just `depends_on:` and items are indented `  - B-XXXX` underneath). For all such rows, `parseBList` returns `[]`, which silently mis-feeds checks 2 and 4: check 2 will report spurious "composes_with omits …" findings (because body-cited IDs are not deduplicated against the actually-declared depends_on/composes_with), and check 4 will fail to detect real redundant edges. The block-form lines beneath the key are also never consumed by `parseFrontmatter` (the per-line regex only matches `key: value`). The parser must handle the block-list continuation form to avoid false positives on a large fraction of the backlog.

  This issue also appears on line 120 of the same file.

  </pre>

### Thread 5 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6C6IHD`
- **Path**: `tools/backlog/lint-frontmatter.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-18T17:00:33Z on `tools/backlog/lint-frontmatter.ts`:55 (association: NONE)

  <pre>
  `SCHEMA_KEYS` is missing several frontmatter fields that are in active use across `docs/backlog/**`, which will cause check 3 to emit spurious "Non-schema frontmatter key" findings on legitimate rows. Concrete missing keys observed in the tree: `children:` (used widely, e.g., `docs/backlog/P1/B-0154-*.md:9`, B-0160, B-0190, B-0249, B-0366, B-0423, B-0436, B-0440, B-0441, B-0442 and many more — this is a sibling of `decomposition:`), `renumbered_reason:` (e.g., `docs/backlog/P1/B-0463-*.md:13`, P2/B-0462), and likely `decomposed_into:` / other variants. Either widen `SCHEMA_KEYS` to cover the de-facto schema (and ideally derive it from `tools/backlog/README.md` so the source-of-truth doesn't drift from this hardcoded copy), or downgrade check 3 to a warning until the schema is reconciled. As shipped, running `bun tools/backlog/lint-frontmatter.ts` against the current repo will produce dozens of false-positive P1 findings.

  </pre>

### Thread 6 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6C6IHi`
- **Path**: `tools/backlog/lint-frontmatter.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-18T17:00:34Z on `tools/backlog/lint-frontmatter.ts`:158 (association: NONE)

  <pre>
  The `fm: Frontmatter` parameter is declared but never referenced in this function's body. The repo's `tsconfig.json` sets `noUnusedParameters: true` (and `noUnusedLocals: true`, `noEmitOnError: true`) — under `tsc`, this is a compile error. `bun build` (claimed in the PR test plan) is a bundler and does not type-check, which is why the issue slipped through. Either drop the parameter, rename it to `_fm`, or actually use it (e.g., to suppress check 1 when `fm.priority` doesn't match the directory). Note: the unused-parameter risk only applies to `check1_pathPrefix`; `check2_composesCompleteness` does reference `fm`.
  </pre>

### Thread 7 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6C6IH0`
- **Path**: `tools/backlog/lint-frontmatter.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-18T17:00:34Z on `tools/backlog/lint-frontmatter.ts`:217 (association: NONE)

  <pre>
  Check 1 only fires when the parsed `refDir` equals the file's own `P[0-3]`, i.e., it catches the same-directory-with-wrong-`../P{X}/`-prefix case. It never detects the "cross-dir link missing prefix" case advertised in the PR description and the row's "Why" section (`docs/backlog/P3/B-0663-*.md:22`): a bare `B-NNNN-...md` href whose target actually lives in a different `P{X}/` directory will hit `pathDirForRef → "SAME"` and be skipped silently. Detecting that class requires resolving the referenced filename against the filesystem (or against an in-memory index of `B-NNNN → P-tier`). Either implement the cross-dir-missing-prefix detection or update the comment header / PR description to scope the check to the one direction actually implemented, so future readers don't assume a missing-prefix bug would have been caught.
  </pre>

### Thread 8 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6C6IIK`
- **Path**: `tools/backlog/lint-frontmatter.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-18T17:00:34Z on `tools/backlog/lint-frontmatter.ts`:299 (association: NONE)

  <pre>
  `walkBacklog` calls `statSync(baseDir)` unconditionally; if the user passes `--base-dir` pointing at a non-existent path (typo, wrong cwd), `statSync` throws an uncaught `ENOENT`, producing a stack trace instead of a clean diagnostic. Similarly, when invoked from outside the repo root with the default `baseDir = "docs/backlog"`, the same crash occurs. Wrap the existence check or use `existsSync` first, and emit a clear `process.stderr.write(...)` + non-zero exit instead of letting the exception propagate.
  </pre>

### Thread 9 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6C6IId`
- **Path**: `tools/backlog/lint-frontmatter.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-18T17:00:35Z on `tools/backlog/lint-frontmatter.ts`:346 (association: NONE)

  <pre>
  Per the repo convention (see `tools/backlog/generate-index.ts:266-290`, `tools/budget/daily-cost-report.ts:249-285`, etc.), tools/**.ts entrypoints expose a `main(...)` function and guard `process.exit` / top-level invocation with `if (import.meta.main) { ... }`. This script calls `main()` unconditionally at the bottom of the file and never exports it, so it cannot be imported by other tools (e.g., a hook wrapper, a CI harness, or a future composite linter) without triggering side effects. Recommend exporting `main` and gating the call with `import.meta.main`.
  </pre>

### Thread 10 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6C6IIv`
- **Path**: `tools/backlog/lint-frontmatter.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-18T17:00:35Z on `tools/backlog/lint-frontmatter.ts`:243 (association: NONE)

  <pre>
  Check 2 collects body-cited B-IDs from any markdown link `[B-NNNN](href)`, but in practice rows routinely cite IDs in narrative prose, change-log bullets, supersession notes, and references to *closed* / *retired* / *unrelated* rows that are not true `composes_with` edges (e.g., the row file itself contains `[B-0650](...)` in the "Composes with" section but also references like "Closes B-0663" and "Phase 2 …" elsewhere). Forcing every body-mentioned ID into `composes_with` will both add noise to the frontmatter and pull this lint toward authoring-by-mechanism rather than authoring-by-intent. Consider scoping check 2 to markdown links inside an explicit "Composes with" / "Composition" section, or downgrading it to an informational class with a clear opt-out marker, otherwise this check will pressure authors to inflate `composes_with` lists.
  </pre>

### Thread 11 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6C6UVZ`
- **Path**: `tools/backlog/lint-frontmatter.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-18T17:13:46Z on `tools/backlog/lint-frontmatter.ts`:173 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Handle dotted backlog IDs in body-link extraction**

  The body-link regex only matches `[B-####](...)`, so it silently ignores common dotted IDs like `[B-0037.3](...)` and `[B-0054.10](...)` (both present under `docs/backlog/**`), which means check 1 (path-prefix validation) and check 2 (`composes_with` completeness) miss real violations for those links. This makes a clean lint run unreliable for a substantial subset of backlog references.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 12 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6C6dmR`
- **Path**: `tools/backlog/lint-frontmatter.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-18T17:22:58Z on `tools/backlog/lint-frontmatter.ts`:218 (association: NONE)

  <pre>
  Check 1 only flags the "same-dir using `../PX/` prefix" case but does not detect the inverse "cross-dir missing prefix" case advertised in the file header comment, the PR description, and the backlog row's Why section. `pathDirForRef` returns `"SAME"` for any bare `B-NNNN-…md` href, and `check1_pathPrefix` skips `refDir === "SAME"`. So a P3 row that links to a P1 row using a bare filename (the cross-dir-missing-prefix bug) is silently passed. Either implement the inverse check (compare bare-filename targets' priority directory against the source file's directory by resolving the row ID against the on-disk tree), or update the comments/docs to reflect that only the same-dir-over-prefixed case is detected.
  </pre>

### Thread 13 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6C6dnA`
- **Path**: `tools/backlog/lint-frontmatter.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-18T17:22:58Z on `tools/backlog/lint-frontmatter.ts`:346 (association: NONE)

  <pre>
  This script calls `main()` and `process.exit(...)` unconditionally at module top level. The established convention across `tools/**.ts` is to export a `main(...)` entrypoint and only invoke it / call `process.exit` when guarded by `if (import.meta.main)` (see `tools/backlog/generate-index.ts:289`, `tools/budget/daily-cost-report.ts`, `tools/archive/archive-pr-reviews.ts`). That keeps the module importable for testing/composition. Wrap the trailing `main();` in an `if (import.meta.main)` guard and consider exporting `main` (plus the per-check functions) so other tooling can reuse them without side effects.
  </pre>

### Thread 14 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6C6dnj`
- **Path**: `tools/backlog/lint-frontmatter.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-18T17:22:59Z on `tools/backlog/lint-frontmatter.ts`:297 (association: NONE)

  <pre>
  `walkBacklog`'s `try/catch` only wraps the initial `statSync(baseDir)` semantically but in practice also catches any `statSync`/`readdirSync` error during recursion (e.g., a transient permission error on a sub-entry). When that happens the script will print `error: --base-dir '&lt;top-level&gt;' not found or not a directory` and `process.exit(2)`, which is misleading because the failure was actually inside the tree. Either narrow the try/catch to the top-level `statSync(baseDir)` only, or include the offending path in the error.

  </pre>

### Thread 15 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6C6dn4`
- **Path**: `tools/backlog/lint-frontmatter.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-18T17:22:59Z on `tools/backlog/lint-frontmatter.ts`:151 (association: NONE)

  <pre>
  The block-list parser only treats `value === ""` or `value === "&gt;"` as the trigger for parsing subsequent `- B-XXXX` items. Real YAML also accepts `|`, `|-`, `&gt;-`, etc., and (more importantly) the canonical block-sequence form simply has an empty scalar — which works — but any trailing whitespace after the colon will produce `value === ""` after `trim()`, so that's fine. However `value === "&gt;"` is not how YAML block lists are signalled (`&gt;` is a folded scalar indicator, not a sequence indicator); it will never appear in legitimate frontmatter. Consider dropping the `"&gt;"` branch to avoid confusing readers, or document why it's there.

  </pre>

### Thread 16 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6C6doR`
- **Path**: `tools/backlog/lint-frontmatter.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-18T17:22:59Z on `tools/backlog/lint-frontmatter.ts`:180 (association: NONE)

  <pre>
  Check 2 only treats markdown-link citations `[B-NNNN](href)` as "body cites this ID." Bare references like `` `B-NNNN` `` or plain `B-NNNN` mentions in prose (common in Why/Notes sections) are not detected, so a row that depends on/composes_with a referenced ID by bare reference will not trigger the completeness check — undermining the stated goal of catching the recurring batch-7 finding. Consider extending `extractBodyBLinks` to also match bare/backticked `B-\d{4}(\.\d+)?` occurrences (and then filter against `depends_on`/self as today), or document this scoping limitation in the file header.

  </pre>

## Fix commits (touching thread paths)

### `94863ca2492d0ddacf61e110296db3481d645987` -- 2026-05-18T16:56:03Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/backlog/lint-frontmatter.ts`

**Message:**

```
feat(backlog-lint): tools/backlog/lint-frontmatter.ts — mechanizes 4 …

…classes of batch-7 reviewer findings (B-0663)

Pre-push frontmatter discipline lint that catches BEFORE PR opens:
1. Wrong relative-path prefix (same-dir vs cross-dir)
2. composes_with completeness (body cites B-XXXX not in frontmatter)
3. Non-schema frontmatter keys (typos like 'last_invariant')
4. Redundant depends_on/composes_with edges

Pure stdlib (node:fs + node:path); compiles cleanly via bun build.
Validated against B-0661 + B-0662: caught 3 findings that Codex/Copilot
also caught manually — tool detects mechanically.

Closes the cycle from batch-7 where every PR triggered the same
reviewer-tool fix-push-resolve cadence for the same 4 discipline classes.

Companion to B-0650 rest-push.ts --delete/--rename extension; both mechanize
patterns that previously required inline workarounds.

🤖 Generated with Claude Code
Co-Authored-By: Claude <noreply@anthropic.com>
```

### `884a779bfe91173f75f04a4af0aa73c13642032f` -- 2026-05-18T17:10:51Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/backlog/lint-frontmatter.ts`

**Message:**

```
fix(pr-4169): 4 substantive bugs caught by Codex + Copilot review

Critical fixes:
- SCHEMA_KEYS expanded with 30+ keys observed in active use (audit-based);
  closes the spurious-finding case where legitimate factory variation
  triggered check 3 (Copilot P1)
- parseBList now handles BOTH inline (`[B-0001, B-0002]`) AND YAML
  block-style (multi-line `  - B-XXXX`) — closes the missed-rows case
  for the majority of existing rows (Codex + Copilot P1)
- Dotted backlog IDs (B-NNNN.M subdecimal rows per tools/backlog/README.md)
  now matched by both walkBacklog AND pathDirForRef regexes (Codex P1)
- Unused 'fm' parameter in check1 renamed to '_fm' for noUnusedParameters
  compatibility (Copilot P1)
- walkBacklog wraps statSync in try/catch with substrate-honest error
  message instead of stack-trace dump (Copilot P2)

Deferred (separate follow-up scope):
- Check 1 cross-dir-missing-prefix detection (Codex P2; Copilot P1) —
  requires filesystem target lookup; substantial scope; new row candidate
- Check 2 narrative-context false-positive (Copilot P2) — requires
  more sophisticated link-vs-prose distinction; new row candidate
- main()/process.exit guard pattern refactor (Copilot P2) — style;
  current pattern works correctly

Validation post-fix:
- Compiles cleanly: bun build → 9.20 KB output
- Dogfood: B-0663's own row: 0 findings
- Re-test B-0661 + B-0662: still detects the 3 known real findings

🤖 Generated with Claude Code
Co-Authored-By: Claude <noreply@anthropic.com>
```

### `bbaf7b6dc7667becd8dc787aa250b2b4916edbe0` -- 2026-05-18T17:19:58Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/backlog/lint-frontmatter.ts`

**Message:**

```
fix(pr-4169): body-link regex now matches dotted backlog IDs (B-NNNN.…

…M subdecimal rows; Codex P1)

Single-line fix: extractBodyBLinks regex extended from `(B-\d{4})` to `(B-\d{4}(?:\.\d+)?)` to match
both standard and subdecimal IDs (e.g., B-0170.4). Closes the silent-ignore case where dotted ID
links in row bodies weren't getting checked.
```
