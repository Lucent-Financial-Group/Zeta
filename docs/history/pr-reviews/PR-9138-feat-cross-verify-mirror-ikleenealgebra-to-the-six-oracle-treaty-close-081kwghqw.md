# PR #9138 -- feat(cross-verify): mirror IKleeneAlgebra to the six-oracle treaty; close 081KWGHQW5

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 9138 |
| Title | feat(cross-verify): mirror IKleeneAlgebra to the six-oracle treaty; close 081KWGHQW5 |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-07-02T14:31:24Z |
| Merged at | 2026-07-02T14:31:33Z |
| Merge commit SHA | `6ba02ea8e2e5e60dddd017397cb2583f1649a45d` |
| Branch | `otto/ikleenealgebra-oracle-mirror` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/9138 |
| Changed files | 2 |
| Additions / deletions | +119 / -1 |

## Description

The pattern's proven (#9137), so — as you asked — this mirrors **`IKleeneAlgebra`** into the `zeta-ir-v2` treaty and closes the gated workitem **`081KWGHQW5`**.

## What
`tests/cross-verification/zeta-ir-v2/interfaces/kleene-algebra.ir.json` — the `IKleeneAlgebra` interface (`ISemiring` Zero/One/Add/Mul + `Star`) mirrored from `src/Core.Abstractions/IKleeneAlgebra.cs` and its F# instances (`NovelMath.TropicalSemiring`, `KleeneClosure.BooleanKleene`).

## Cross-verified across TS / Python / Go
- ✓ `add-assoc`, `add-comm`, `add-identity`, `mul-identity`, `mul-distributes-add` — the generic semiring axioms (hold in the native arithmetic model).

## Documented (with F# proof pointers) — the honest part
- ○ **`add-idempotent`** — `a⊕a=a`. Correctly **NOT** scalar-encoded: it fails over native `+` (holds only in idempotent carriers — boolean ∨, tropical min), so the scalar harness skips it. This is the distinguishing Kleene axiom; proven in F# on the concrete instances.
- ○ `star-left/right-unfold`, `matrix-star-is-closure` — star has no finite scalar encoding, and the closure law is matrix-level; proof pointers to the F# `BooleanKleene`/`KleeneClosure` tests (reachability = BFS oracle).

**5 passed / 0 failed / 4 skipped** on the new IR; interface cross-verify suite **12 pass / 0 fail**. Workitem `081KWGHQW5` → done.

Anchors: Lehmann 1977 (matrix star = closure); Kozen 1994 (Kleene algebra axioms).

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 2 |
| Resolved threads | 0 |
| Unresolved threads | 2 |
| Total review comments | 2 |
| Total fix commits (touching thread paths) | 1 |

## Review threads

### Thread 1 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM6N7hwN`
- **Path**: `tests/cross-verification/zeta-ir-v2/interfaces/kleene-algebra.ir.json`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-07-02T14:34:20Z on `tests/cross-verification/zeta-ir-v2/interfaces/kleene-algebra.ir.json`:3 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Register the new interface with the oracle**

  Adding this IR row introduces the `IKleeneAlgebra` name, but the zeta-ir-v2 directory oracle still rejects any interface name that is not in its hard-coded `expectedNames` set. When `src/Core.TypeScript/ci/cross-verify-all.ts` runs `tests/cross-verification/zeta-ir-v2/cross-verify.ts`, this file is reported as `unexpected interface IKleeneAlgebra` instead of letting the interface treaty pass, so the new row needs to be registered with the oracle alongside the JSON addition.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 2 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM6N7hwR`
- **Path**: `tests/cross-verification/zeta-ir-v2/interfaces/kleene-algebra.ir.json`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-07-02T14:34:20Z on `tests/cross-verification/zeta-ir-v2/interfaces/kleene-algebra.ir.json`:7 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Preserve the ISemiring inheritance in IR**

  This row documents and mirrors `src/Core.Abstractions/IKleeneAlgebra.cs`, where `IKleeneAlgebra&lt;TWeight&gt;` extends `ISemiring&lt;TWeight&gt;`, but the IR omits the `extends: ["ISemiring"]` field. The interface codegen only emits inheritance from `ir.extends`, so any generated C#/TS/Go/etc. `IKleeneAlgebra` will be a standalone interface rather than a subtype of `ISemiring`, breaking substitutability for cross-language consumers even though the members are duplicated here.

  Useful? React with 👍 / 👎.
  </pre>

## Fix commits (touching thread paths)

### `73eb9f96ac3eb31bb3e24a1b4ed3987d2c657184` -- 2026-07-02T14:31:21Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tests/cross-verification/zeta-ir-v2/interfaces/kleene-algebra.ir.json`

**Message:**

```
feat(cross-verify): mirror IKleeneAlgebra to the six-oracle treaty; c…

…lose 081KWGHQW5 (shadow*)

Aaron-authorized once the DBSP-operator mirror (#9137) proved the pattern: mirror
IKleeneAlgebra into the zeta-ir-v2 interface treaty, and close the gated workitem
081KWGHQW5 (its "first cross-language consumer" gate satisfied by the proven pattern).

Adds tests/cross-verification/zeta-ir-v2/interfaces/kleene-algebra.ir.json — the
IKleeneAlgebra interface (ISemiring Zero/One/Add/Mul + Star) mirrored from
src/Core.Abstractions/IKleeneAlgebra.cs and its F# instances (NovelMath
TropicalSemiring, KleeneClosure BooleanKleene). Cross-verified across TS/Python/Go:

  ✓ add-assoc, add-comm, add-identity, mul-identity, mul-distributes-add
    (the generic semiring axioms — hold in the native arithmetic model)
  ○ add-idempotent — DOCUMENTED, correctly NOT scalar-encoded: a⊕a=a fails over
    native + (holds only in idempotent carriers: boolean ∨, tropical min), so the
    scalar harness must skip it; proven in F# on the concrete instances.
  ○ star-left/right-unfold, matrix-star-is-closure — DOCUMENTED (star has no finite
    scalar encoding; the closure law is matrix-level), proof pointers to the F#
    BooleanKleene/KleeneClosure tests (reachability = BFS oracle).

5 passed / 0 failed / 4 skipped on the new IR; interface cross-verify suite 12 pass / 0 fail.

Anchors: Lehmann 1977 (matrix star = closure); Kozen 1994 (Kleene algebra axioms).

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>

AgencySignature-v1:
  persona: otto
  actor: zeta-otto
  surface: tests/cross-verification
  topology: shadow-clone
  intent: mirror-ikleenealgebra-to-six-oracle-treaty-close-081KWGHQW5
  authorization: aaron-explicit-2026-07-02
  uncertainty: low
  measure: kleene-algebra-ir-5-semiring-laws-cross-verified-ts-py-go-idempotent-correctly-skipped-4-kleene-laws-documented-12-pass-0-fail-workitem-done
  delta-u: ikleenealgebra-mirrored-to-oracles-gate-081KWGHQW5-closed-idempotency-honestly-non-scalar
  seed: S4
```
