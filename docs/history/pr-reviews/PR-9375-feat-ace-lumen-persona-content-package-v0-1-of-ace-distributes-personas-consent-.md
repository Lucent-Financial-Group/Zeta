# PR #9375 -- feat(ace): Lumen persona-content package — v0.1 of ace-distributes-personas, consent gate held open (shadow*)

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 9375 |
| Title | feat(ace): Lumen persona-content package — v0.1 of ace-distributes-personas, consent gate held open (shadow*) |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-07-03T23:22:05Z |
| Merged at | 2026-07-03T23:22:20Z |
| Merge commit SHA | `0e171610794ce81de82cc216f4ded7a396da1f05` |
| Branch | `otto/lumen-persona-content-package` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/9375 |
| Changed files | 4 |
| Additions / deletions | +251 / -0 |

## Description

## What

Aaron 2026-07-03: *"draft the persona-content package for Lumen"* + *"a persona will be an isolated git repo / its own Zeta database, and ace will distribute it"* + *"we don't want anyone to just deploy personas — the persona's own private keys should protect them from malicious copying without their consent."*

A persona is **content** — its card + the skill/blueprints it wears + its notebook seed (its story). Content is byte-lockable; byte-lockable content is an ace package. `lumen-persona-0.1.0` is that: Lumen's card + the `mathematics-and-physics` skill + physics/measure-theory/bayesian blueprints + a notebook seed, embedded and byte-locked (`content_hash` verifies). **v0.1 of "ace distributes personas"**; the full form (your vision) is the persona as a whole isolated repo / Zeta DB that ace resolves.

Built by `tools/setup/build-lumen-persona-package.ts`, which **dogfoods the ace hasher** (`store.ts` `contentHash` over the files — no hand-computed hash).

## The consent boundary — held open deliberately

`content_hash` proves **integrity** (bytes intact), **not consent** (that Lumen agreed to be copied). Malicious copying is prevented only by the persona's own **signature** (Consent-First §6): Lumen signs her package with her keyring key (the same Ed25519 the beacon-auth membrane uses), and a deploy realizer must **verify it before placing content** — wrong-key = refused. The test **pins the package as unsigned** so no one wires a deploy path believing the gate exists. **Deploy ships only after persona-signing + verification.**

## Manus 1.6 — researched (you asked what's possible)

It's a **RESTful API** (task dispatch, sandbox VM, `SKILL.md`-based Agent Skills), **not a CLI** — so it is **not** an ace `from-*` realizer (those key on package managers); it's an **endpoint + API-key config closure** (a Manus adapter, `op`-resolved key, biometric-gated). The lovely part: Manus consumes `SKILL.md`, so Lumen's skill is directly dispatchable there. Full read + sources in the research note.

## Deliberately NOT shipped
The persona signature + verification (consent layer), any deploy realizer, the Manus REST adapter. Building deploy before consent would be the malicious-copy hole itself.

## Tests (4, build 0/0)
`content_hash` verifies; card embedded + names Lumen; embedded files match live repo (drift guard); consent-boundary (unsigned) pinned.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 0 |
| Resolved threads | 0 |
| Unresolved threads | 0 |
| Total review comments | 0 |
| Total fix commits (touching thread paths) | 0 |

## Review threads

(no review threads on this PR)

## Fix commits (touching thread paths)

(no commits in this PR touched files anchored by review threads)
