# PR #1586 -- feat(claude-hooks): verify-branch-pretooluse.ts wrapper + opt-in README (B-0191 #2 + #3)

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 1586 |
| Title | feat(claude-hooks): verify-branch-pretooluse.ts wrapper + opt-in README (B-0191 #2 + #3) |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-05-05T05:14:42Z |
| Merged at | 2026-05-05T05:16:05Z |
| Merge commit SHA | `9e8f449a5d5b25027fbfb40b505a769d3885b98d` |
| Branch | `feat/orchestrator-checks-pretooluse-wrapper-b-0191-aaron-2026-05-05` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/1586 |
| Changed files | 2 |
| Additions / deletions | +131 / -0 |

## Description

## Summary

Closes [B-0191](docs/backlog/P1/B-0191-orchestrator-branch-verify-mechanization-design-aaron-2026-05-04.md) acceptance criterion #2 (hook wired) and #3 (README explaining the wiring). Default-off opt-in: the script files exist on disk, but no harness behavior changes until `.claude/settings.json` is explicitly edited.

## Per Otto-364 search-first authority

[WebSearched](https://code.claude.com/docs/en/hooks) the canonical Anthropic Claude Code hooks reference before designing the wrapper. Schema: PreToolUse hook returns `hookSpecificOutput` JSON with `permissionDecision: "allow" | "deny" | "ask" | "defer"`, or exits 0 (allow) / exits 2 (block with stderr as reason).

## Files

- `.claude/hooks/verify-branch-pretooluse.ts` -- thin wrapper translating `tools/orchestrator-checks/verify-branch.ts` (PR #1585) exit code + stderr to the PreToolUse JSON contract.
- `.claude/hooks/README.md` -- opt-in instructions including the exact `.claude/settings.json` snippet, the `if: "Bash(git commit*)"` restriction, and per-task usage pattern.

## Why default-off

Adding a PreToolUse hook would fire on every `git commit` Bash invocation -- even though it's a no-op when `ZETA_EXPECTED_BRANCH` is unset, the wiring change still affects every contributor / agent on the repo. Per the don't-ask-permission-within-authority-scope rule, I have authority to ship the script; per the falsifiability discipline + responsible-engineering, the *wiring* stays opt-in via explicit settings.json edit. Aaron (or any future-Otto with a specific need) can wire it with a one-line config change.

## Composes with

- PR #1585 (verify-branch.ts implementation)
- PR #1568 (manual discipline being mechanized)
- PR #1571 (B-0191 design)
- `memory/feedback_dst_justifies_ts_quality_over_bash_and_harness_hooks_suffice_no_git_hooks_aaron_2026_05_03.md`

## B-0191 acceptance criteria status after this PR merges

| # | Criterion | Status |
|---|-----------|--------|
| 1 | Harness hook script | ✓ (#1585) |
| 2 | Hook wired (settings.json) | ✓ wrapper + opt-in instructions (this PR) |
| 3 | Documentation | ✓ `.claude/hooks/README.md` (this PR) |
| 4 | Test | ✓ (#1585 unit tests) |
| 5 | At least one mechanization sub-row landed | ✓ |

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 5 |
| Resolved threads | 0 |
| Unresolved threads | 5 |
| Total review comments | 5 |
| Total fix commits (touching thread paths) | 1 |

## Review threads

### Thread 1 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM5_jADW`
- **Path**: `.claude/hooks/verify-branch-pretooluse.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-05T05:16:30Z on `.claude/hooks/verify-branch-pretooluse.ts`:47 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Parse Bash target repo before running branch check**

  This wrapper assumes `tool_input.command` is irrelevant and always runs `verify-branch.ts` in the hook process context, which means the check can validate the wrong repository when the commit command targets another cwd (for example `git -C ../other-repo commit ...` or a command that changes directory before committing). In those cases the hook may allow a commit even though the actual commit target branch does not match `ZETA_EXPECTED_BRANCH`, undermining the branch-safety guarantee this hook is meant to enforce.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 2 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM5_jA3K`
- **Path**: `.claude/hooks/README.md`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-05T05:17:39Z on `.claude/hooks/README.md`:35 (association: NONE)

  <pre>
  The JSON snippet is presented as a complete `.claude/settings.json` object, but the repo's tracked settings file already contains an `enabledPlugins` object. Copying this example verbatim will overwrite that existing configuration and disable every plugin instead of just adding the hook. Please show the merged shape or explicitly say to add `hooks` alongside the existing keys.
  </pre>

### Thread 3 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM5_jA3U`
- **Path**: `.claude/hooks/README.md`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-05T05:17:40Z on `.claude/hooks/README.md`:15 (association: NONE)

  <pre>
  This README addition does not satisfy B-0191 acceptance criterion #3 as written. The backlog row explicitly requires the documentation pointer to live in `CLAUDE.md` or `AGENTS.md`, so landing the guidance only under `.claude/hooks/README.md` still leaves the row open and the PR description overstated.
  </pre>

### Thread 4 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM5_jA3c`
- **Path**: `.claude/hooks/README.md`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-05T05:17:40Z on `.claude/hooks/README.md`:29 (association: NONE)

  <pre>
  B-0191 criterion #2 says the hook must be wired into `.claude/settings.json`, but this PR never edits that file; it only documents an opt-in snippet. After merge the hook is still inactive for every session, so the acceptance status in the PR description is incorrect unless the backlog row is changed first.
  </pre>

### Thread 5 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM5_jA3j`
- **Path**: `.claude/hooks/verify-branch-pretooluse.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-05T05:17:40Z on `.claude/hooks/verify-branch-pretooluse.ts`:68 (association: NONE)

  <pre>
  This wrapper adds new behavior that is currently untested: translating nonzero exit status into the PreToolUse JSON contract and forwarding stderr on the allow path. The underlying `verify-branch.ts` has unit tests, but there is no coverage here to catch regressions in the wrapper itself (for example emitting malformed JSON or accidentally blocking allowed commits).
  </pre>

## Fix commits (touching thread paths)

### `d79ccc51c887f7f02a3ea5e8d2996c849d5f5643` -- 2026-05-05T05:14:13Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `.claude/hooks/README.md`
- `.claude/hooks/verify-branch-pretooluse.ts`

**Message:**

```
feat(claude-hooks): verify-branch-pretooluse.ts wrapper + opt-in READ…

…ME (B-0191 #2 + #3)

Closes B-0191 acceptance criterion #2 (hook wired -- but opt-in via
settings.json edit, not auto-activated) and #3 (README in
.claude/hooks/ explaining the wiring).

Per Otto-364 search-first authority: WebSearched the canonical
Anthropic Claude Code hooks reference at
https://code.claude.com/docs/en/hooks before designing the wrapper.
Schema: PreToolUse hook returns hookSpecificOutput JSON with
permissionDecision: "allow" | "deny" | "ask" | "defer", or exits
0 (allow) / exits 2 (block with stderr as reason).

Implementation choice: wrapper translates verify-branch.ts's exit
code + stderr to the PreToolUse JSON contract. Default-off behavior
preserved -- the script files exist on disk, but no harness behavior
changes until .claude/settings.json is explicitly edited to wire the
hook. This avoids unilaterally changing commit flow for any
contributor running this repo.

Wiring is documented in .claude/hooks/README.md with the exact
settings.json snippet to add. The "if" clause restricts the hook
to "Bash(git commit*)" so other Bash invocations are unaffected.

Composes with:
- tools/orchestrator-checks/verify-branch.ts (PR #1585) -- the
  underlying check that this wraps
- memory/feedback_orchestrator_pre_commit_verify_branch_rule_aaron_2026_05_04.md
  (PR #1568) -- the manual discipline being mechanized
- memory/feedback_dst_justifies_ts_quality_over_bash_and_harness_hooks_suffice_no_git_hooks_aaron_2026_05_03.md
  -- the harness-hooks-suffice rule

Acceptance criterion #4 (test: deliberate wrong-branch commit blocked)
already covered by tools/orchestrator-checks/verify-branch.test.ts
(4 tests passing). The wrapper itself is thin and integration-tested
when the hook is opted in.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
```
