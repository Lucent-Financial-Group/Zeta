# PR #10270 -- docs(blueprint): generation 1's results and the generation-2 fixes

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 10270 |
| Title | docs(blueprint): generation 1's results and the generation-2 fixes |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-08-10T01:21:36Z |
| Merged at | 2026-08-10T01:22:56Z |
| Merge commit SHA | `5cfa353ba006f44986cb4d7caedcf0672f4f08f7` |
| Branch | `shadow/blueprint-generation-1-results` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/10270 |
| Changed files | 2 |
| Additions / deletions | +119 / -0 |

## Description

Saves the pattern back into the blueprint, as asked. Generation 1 has run, so it now records results rather than only generation 0's defects.

**The generation-0 fixes worked.** Both reporting derivations declared `partial` where they could have claimed `implemented` and been believed — and **each marked partial exactly where the other marked implemented.** Generation 0's failure (twelve-of-twelve claimed, four claims not surviving execution) did not recur.

## Co-discovery is now the explicit scoring rule

> **One derivation naming an ambiguity is a hypothesis. Two naming it independently is evidence.**

Rank the combine by how many derivations found each defect, not by how compelling the argument reads. Four were co-discovered here and are near-certain; the single-derivation finds were the *most severe* but carry the **weaker warrant** — the combine must say which is which.

## Why N=3 beat N=2 — the specific reason

Two derivations added a domain tag; the third added none, reasoning only about injectivity — the only property the spec's rationale argued for. **The tagless one proved the spec permits omitting domain separation entirely.**

> **N=2 shows you *that* two readings exist. N=3 shows you the reading nobody defended.**

Corollary: the marginal derivation is worth most **where the requirement is prose rather than arithmetic**.

## Generation-2 fixes

1. **Append each ambiguity the moment it resolves.** One derivation wrote the skeleton first, as instructed, then wedged before filling it in — code survived, analysis didn't. **The unit of incrementality must be the finding, not the file.**
2. **Stagger the builds.** Worktree isolation isolates the **filesystem, not the CPU** — three concurrent Release builds dominated wall-clock. Isolation is independence, not throughput.
3. **Expect one in three to need rescuing.** Both runs had an agent wedge before pushing; **check the worktree before declaring a derivation lost.**
4. **Fix the spec before dispatching another derivation** — a fourth against unamended text reproduces every divergence already found.

🤖 Generated with [Claude Code](https://claude.com/claude-code)


## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 0 |
| Resolved threads | 0 |
| Unresolved threads | 0 |
| Total review comments | 0 |
| Total fix commits (touching thread paths) | 0 |

## Review threads

(no review threads on this PR)

## Fix commits (touching thread paths)

(no commits in this PR touched files anchored by review threads)
