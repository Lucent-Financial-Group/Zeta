# PR #2944 -- feat(B-0400): bus status subcommand — slice 4

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 2944 |
| Title | feat(B-0400): bus status subcommand — slice 4 |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-05-13T05:48:58Z |
| Merged at | 2026-05-13T06:28:20Z |
| Merge commit SHA | `a4a66cbbe69e31b8ea4085ab8117ce11de2c73c7` |
| Branch | `feat/b-0400-slice4-status` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/2944 |
| Changed files | 3 |
| Additions / deletions | +255 / -4 |

## Description

## Summary

- Adds `bun tools/bus/bus.ts status [--json]` — single-query dashboard of the live bus state
- **Agents section**: latest heartbeat per sender (deduplicated by most-recent timestamp), showing status + optional note
- **Claims section**: all active raw claim messages (action, itemId, branch) — for authoritative ownership use `claim.ts check` which handles release tombstones
- **Review-requests section**: pending `review-request` messages (artifact, question, from→to)
- **Shadow-catch count**: totals only (content is ephemeral)
- 9 new tests; **60/60 bus + claim tests pass** (was 51 before this slice)

## Prior slices
| Slice | PR | Content |
|---|---|---|
| 1 | #2886 | types.ts + bus.ts (publish/list/read/clean) + tests |
| 2 | #2886 | watch subcommand |
| 3 | #2939 | claim.ts (acquire/release/check) + tests |
| **4** | **this** | **status subcommand + 9 tests** |

## Deferred (slice 5+)
- NATS JetStream transport swap
- Named-pipe transport option
- Integration with `poll-pr-gate-batch.ts` for coordinated claims

## Checks

```
bun test tools/bus/
60 pass, 0 fail (162 expect() calls)

dotnet build -c Release
0 Warning(s), 0 Error(s)
```

## Backlog item
`docs/backlog/P1/B-0400-inter-agent-ephemeral-communication-bus-nats-protocol.md`

operative-authorization: aaron 2026-05-12: "- The "go hard" + dense-encoding-mode authorizations —"

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | true |
| Total threads | 11 |
| Resolved threads | 11 |
| Unresolved threads | 0 |
| Total review comments | 23 |
| Total fix commits (touching thread paths) | 4 |

## Review threads

### Thread 1 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6BpXVS`
- **Path**: `tools/bus/bus.ts`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-13T05:50:48Z on `tools/bus/bus.ts`:370 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Validate message payload shape before rendering status**

  `status` assumes every heartbeat payload is an object with `status`/`note`, but `publish` currently accepts any JSON value, so a single malformed message (for example `--topic heartbeat --payload null` or a manually written envelope) will make this property access throw and crash the whole command. This turns one bad bus entry into a total loss of the status dashboard; add a runtime shape guard (as done in `claim.ts`'s `activeClaims`) and skip invalid entries instead of dereferencing unvalidated payloads.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-13T05:56:47Z on `tools/bus/bus.ts`:370 (association: MEMBER)

    <pre>
    Fixed in 5699aff0. Added `isObj()` predicate (non-null object, non-array) and apply it as a guard before dereferencing any payload — heartbeat entries are pre-filtered in the dedup loop, claims and review-requests are filtered inline in both the JSON and text output paths. Matches the same skip-invalid-entries pattern as `claim.ts` activeClaims() line 111.
    </pre>

### Thread 2 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6BpYYt`
- **Path**: `tools/bus/bus.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-13T05:52:02Z on `tools/bus/bus.ts`:378 (association: NONE)

  <pre>
  P0: `status` assumes per-topic payload shapes via type casts, but `publish` accepts any valid JSON (including `null`). If someone publishes `--payload null` (or a non-object), `status` will throw when reading properties (e.g., `p.status`, `p.action`). Please add runtime guards (e.g., ensure payload is a non-null object and required fields exist) and either skip malformed messages or render them as `unknown` without crashing.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-13T05:56:53Z on `tools/bus/bus.ts`:378 (association: MEMBER)

    <pre>
    Fixed in 5699aff0. Added `isObj(v)` guard (`!!v &amp;&amp; typeof v === 'object' &amp;&amp; !Array.isArray(v)`) and apply it before every payload cast. Heartbeat loop pre-filters via the guard; claims and review-requests use `.filter(c =&gt; isObj(c.payload))` before `.map()` in JSON mode, and `if (!isObj(c.payload)) continue` in text mode. Malformed entries are skipped, not thrown.
    </pre>

### Thread 3 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6BpYY7`
- **Path**: `tools/bus/bus.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-13T05:52:02Z on `tools/bus/bus.ts`:342 (association: NONE)

  <pre>
  P1: Heartbeat de-duplication uses only `timestamp` and a strict `&gt;` comparison. If two heartbeats from the same sender share the same ISO timestamp string (possible when publishing quickly), the later write may not win, and the chosen "latest" heartbeat becomes nondeterministic due to sort stability. Consider adding a deterministic tiebreaker (e.g., file mtime like `claim.ts` does, or include message id/mtime in the comparison).
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-13T05:56:59Z on `tools/bus/bus.ts`:342 (association: MEMBER)

    <pre>
    Fixed in 5699aff0. Replaced the single-comparison loop with the same three-level tiebreaker claim.ts uses: ISO timestamp first, then file mtime via `msgMtimeMs(id)` (sub-ms precision), then list index as final tiebreaker. Internally uses a `HbEntry = {envelope, mtime, idx}` map during dedup, then extracts the winning envelopes into `Map&lt;SenderAgentId, MessageEnvelope&gt;` — so the rendering code below is unchanged.
    </pre>

### Thread 4 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6BpYZG`
- **Path**: `tools/bus/bus.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-13T05:52:02Z on `tools/bus/bus.ts`:316 (association: NONE)

  <pre>
  P2: `agentMap` is declared as `Map&lt;string, MessageEnvelope&gt;` even though `from` is a `SenderAgentId`. Using `Map&lt;SenderAgentId, MessageEnvelope&gt;` (and iterating `agentMap.values()` directly) would preserve type-safety and make it harder for malformed envelopes to introduce unexpected keys.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-13T05:57:03Z on `tools/bus/bus.ts`:316 (association: MEMBER)

    <pre>
    Fixed in 5699aff0. Changed `hbDedup` to `Map&lt;SenderAgentId, HbEntry&gt;` (the internal dedup map) and `agentMap` to `Map&lt;SenderAgentId, MessageEnvelope&gt;` (the final extracted result). TypeScript now rejects any non-SenderAgentId key at the call sites.
    </pre>

### Thread 5 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6Bpdkk`
- **Path**: `tools/bus/bus.ts`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-13T05:58:19Z on `tools/bus/bus.ts`:328 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Reject malformed heartbeat objects before latest-state dedup**

  `status` currently treats any object payload as a valid heartbeat, so a newer malformed message like `{}` from the same sender will overwrite that sender’s last good heartbeat in `hbDedup` and then render as `undefined` status in both text and JSON output. Because `publish` accepts arbitrary JSON payloads, this makes one bad heartbeat silently erase real liveness state for that agent instead of being ignored; only heartbeats with a valid `status` enum should participate in deduplication.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-13T06:05:56Z on `tools/bus/bus.ts`:328 (association: MEMBER)

    <pre>
    Fixed in 5af9a5c2. Added semantic status guard after the structural  check: only heartbeats with a recognized `status` value ("alive" | "idle" | "working") enter `hbDedup`; a malformed `{}` envelope is skipped rather than overwriting a valid liveness entry. Also adds a dedicated test: publishes a valid "alive" heartbeat then a malformed `{}` one from the same sender — asserts the dashboard still reports "alive" (61/61 bus tests pass).
    </pre>

### Thread 6 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6Bpkx-`
- **Path**: `tools/bus/bus.ts`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-13T06:08:19Z on `tools/bus/bus.ts`:373 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Filter malformed claim payloads before rendering status**

  `status --json` only checks that claim payloads are objects, then blindly reads `action`/`itemId`; because `publish` accepts arbitrary JSON, payloads like `{}` or `{ "action": "claim" }` are treated as valid and render `undefined` fields, which produces misleading claim rows instead of ignoring malformed entries. Add a shape guard for required claim fields (as done for heartbeat `status`) before mapping to output.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-13T06:26:13Z on `tools/bus/bus.ts`:373 (association: MEMBER)

    <pre>
    Fixed in 812307bc: added `isValidClaim` helper that checks `isObj(p) &amp;&amp; typeof p.action === 'string' &amp;&amp; typeof p.itemId === 'string'`. Pre-filtering with this before building the output means neither the JSON arrays nor the text-mode loop will encounter payloads missing required string fields.
    </pre>

### Thread 7 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6BpkyF`
- **Path**: `tools/bus/bus.ts`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-13T06:08:20Z on `tools/bus/bus.ts`:390 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Derive status totals from rendered entries**

  The `totals` counts are computed from raw `claimMsgs`/`reviewMsgs` arrays, but the detailed arrays are filtered, so malformed messages (for example `payload: null`) create phantom counts where `totals.claims` or `totals.reviewRequests` is nonzero while the corresponding output list is empty; this can mislead automation that keys off totals and also prevents `bus: empty` from being emitted in the same scenario.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-13T06:26:14Z on `tools/bus/bus.ts`:390 (association: MEMBER)

    <pre>
    Fixed in 812307bc: `validClaims`/`validReviews` are pre-computed from the filtered arrays. `totals.claims` and `totals.reviewRequests` now use `validClaims.length` / `validReviews.length`, so they always equal the lengths of the rendered arrays.
    </pre>

### Thread 8 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6Bpl0_`
- **Path**: `tools/bus/bus.ts`
- **Replies**: 2

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-13T06:09:52Z on `tools/bus/bus.ts`:37 (association: NONE)

  <pre>
  P1 (security): `msgMtimeMs` builds the path with `join(BUS_DIR, `${id}.json`)` and does not apply the `envelopePath` traversal guard. A malformed/corrupted envelope with an `id` like `../../etc/passwd` could make `status` stat paths outside `BUS_DIR`. Consider reusing `envelopePath(id)` (or the same `resolve`+`startsWith` check) before calling `statSync`.

  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-13T06:25:44Z on `tools/bus/bus.ts`:37 (association: MEMBER)

    <pre>
    Fixed in 812307bc:  now calls  which applies the same traversal guard as /, preventing any path-component in the ID from escaping BUS_DIR.
    </pre>
  - **`AceHack` (human)** at 2026-05-13T06:26:11Z on `tools/bus/bus.ts`:37 (association: MEMBER)

    <pre>
    Fixed in 812307bc: `msgMtimeMs` now calls `envelopePath(id)` which applies the same traversal guard as `readMessage`/`publish`, preventing any path-component in the ID from escaping BUS_DIR.
    </pre>

### Thread 9 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6Bpl1U`
- **Path**: `tools/bus/bus.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-13T06:09:52Z on `tools/bus/bus.ts`:341 (association: NONE)

  <pre>
  P1 (operational): The heartbeat de-dupe tiebreaker falls back to `i &gt; existing.idx`, but `i` ultimately depends on `readdirSync()` order for cases where both `timestamp` and `mtimeMs` are equal (possible on filesystems with coarse mtime granularity or very fast consecutive writes). That means the chosen “latest” heartbeat can still be nondeterministic. Consider using a stable, content-derived final tiebreaker (e.g., compare `id`/filename lexicographically) rather than list index.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-13T06:25:47Z on `tools/bus/bus.ts`:341 (association: MEMBER)

    <pre>
    Fixed in 812307bc: removed `idx` from `HbEntry` and replaced `i &gt; existing.idx` with `h.id &gt; existing.envelope.id`. Since IDs are UUIDs (random but stable per envelope), this is a deterministic tiebreaker regardless of `readdirSync()` order.
    </pre>

### Thread 10 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6Bpl1g`
- **Path**: `tools/bus/bus.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-13T06:09:53Z on `tools/bus/bus.ts`:395 (association: NONE)

  <pre>
  P1: In `status --json`, `totals.claims` / `totals.reviewRequests` are computed from the raw message arrays, but the exported `claims` / `reviewRequests` arrays filter out non-object payloads. This can produce inconsistent output where `totals.*` doesn’t match the lengths of the corresponding arrays, and the text mode can print section headers with no entries when all payloads are filtered. Consider computing the filtered/validated arrays once and using their lengths for totals + deciding whether to print the section header (or explicitly expose both raw vs parsed counts).
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-13T06:26:12Z on `tools/bus/bus.ts`:395 (association: MEMBER)

    <pre>
    Fixed in 812307bc: pre-compute `validClaims` and `validReviews` by filtering with `isValidClaim`/`isValidReview` before building the output object. Both `claims`/`reviewRequests` arrays and `totals.claims`/`totals.reviewRequests` now come from the same filtered slices — counts will always match.
    </pre>

### Thread 11 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6Bpl1t`
- **Path**: `docs/backlog/P1/B-0400-inter-agent-ephemeral-communication-bus-nats-protocol.md`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-13T06:09:53Z on `docs/backlog/P1/B-0400-inter-agent-ephemeral-communication-bus-nats-protocol.md`:93 (association: NONE)

  <pre>
  P1 (docs): This backlog item still labels “Slice 2” as “(this PR — feat/b-0400-slice2-watch)”, but this PR is slice 4 (status). Since this hunk updates the slice 4 scope, it’d be good to also update the nearby slice 2 header so the backlog doc doesn’t mislead readers about which PR delivered watch vs status.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-13T06:26:15Z on `docs/backlog/P1/B-0400-inter-agent-ephemeral-communication-bus-nats-protocol.md`:93 (association: MEMBER)

    <pre>
    Fixed in 812307bc: removed the stale `(this PR — feat/b-0400-slice2-watch)` annotation from the Slice 2 header. The backlog item now refers to each slice by its branch name only, with no PR self-reference.
    </pre>

## Fix commits (touching thread paths)

### `74c22aefaf3dca1450fa3bebaf68413ac932e853` -- 2026-05-13T05:48:42Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `docs/backlog/P1/B-0400-inter-agent-ephemeral-communication-bus-nats-protocol.md`
- `tools/bus/bus.ts`

**Message:**

```
feat(B-0400): bus status subcommand — slice 4

Add `bun tools/bus/bus.ts status [--json]` to provide a single-query
dashboard of the live bus state: latest heartbeat per agent (deduplicated),
raw claim messages, pending review-request messages, and shadow-catch count.

Scope: new `status` case in bus.ts switch, payload type imports extended
(HeartbeatPayload, ClaimPayload, ReviewRequestPayload), usage string updated.
9 new tests across the status describe-block; all 60 bus/claim tests pass.

Deferred: NATS JetStream swap, named-pipe transport, poll-pr-gate integration.

dotnet build -c Release: 0 warnings, 0 errors.

operative-authorization: aaron 2026-05-12: "- The "go hard" + dense-encoding-mode authorizations —"

Co-Authored-By: Claude <noreply@anthropic.com>
```

### `5699aff0b7e059dbacc56440f86c034877d9c557` -- 2026-05-13T05:56:24Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/bus/bus.ts`

**Message:**

```
fix(B-0400): harden status command against malformed payloads + deter…

…ministic dedup

- Add runtime payload shape guard (skip null/non-object payloads) to
  heartbeat dedup loop, claims, and review-requests — prevents crash
  when `publish` receives `--payload null` or any non-object JSON
- Add deterministic tiebreaker to heartbeat dedup: ISO timestamp wins,
  then file mtime (sub-ms precision), then list index — mirrors the
  same pattern already used in claim.ts activeClaims()
- Change agentMap key type from string to SenderAgentId for type safety
- Extract isObj() predicate to consolidate the shape-guard pattern

60/60 bus + claim tests pass.

Addresses Copilot P0/P1/P2 + Codex P1 review comments on PR #2944.

Co-Authored-By: Claude <noreply@anthropic.com>
```

### `5af9a5c249cfb08fcc11659c1f4557fb212b2555` -- 2026-05-13T06:05:43Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/bus/bus.ts`

**Message:**

```
fix(B-0400): reject malformed heartbeats (no valid status) before ded…

…up entry

Codex P2 finding: a heartbeat envelope with a valid object payload but
no `status` enum field (e.g. `{}`) would pass the existing `isObj` guard
and enter `hbDedup`, potentially overwriting a valid liveness entry and
rendering `undefined` status in the dashboard.

Fix: add a semantic guard after the structural guard — only envelopes
whose payload carries a recognised status value ("alive" | "idle" |
"working") are admitted into the dedup map.  Matches the same
skip-invalid-entries discipline as `claim.ts` activeClaims().

Also adds test: publishes a valid "alive" heartbeat then a malformed {}
one from the same sender; asserts the status command still reports
"alive" (61/61 bus tests pass).

Co-Authored-By: Claude <noreply@anthropic.com>
```

### `812307bc934c17c7de02c3771ee3597a7e0cabd5` -- 2026-05-13T06:25:31Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `docs/backlog/P1/B-0400-inter-agent-ephemeral-communication-bus-nats-protocol.md`
- `tools/bus/bus.ts`

**Message:**

```
fix(B-0400/status): resolve 6 review threads — path-traversal guard, …

…stable dedup tiebreaker, payload validation, totals consistency

- msgMtimeMs: use envelopePath(id) instead of bare join to apply traversal guard (P1 security)
- HbEntry dedup: replace list-index tiebreaker with stable UUID comparison so dedup is deterministic regardless of readdirSync() order (P1 operational)
- isValidClaim/isValidReview: validate required string fields (action+itemId for claims, artifact for reviews) beyond isObj check (P2)
- Pre-compute validClaims/validReviews once; use in both --json and text output paths — totals now always match rendered arrays (P1+P2)
- Slice 2 label in backlog doc: remove stale "(this PR — ...)" annotation (P1 docs)

Co-Authored-By: Claude <noreply@anthropic.com>
```
