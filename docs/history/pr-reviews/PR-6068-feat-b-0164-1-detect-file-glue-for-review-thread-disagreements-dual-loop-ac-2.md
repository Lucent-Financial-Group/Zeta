# PR #6068 -- feat(081KR7JY10008QG0R000MH7PJT): detect→file glue for review-thread disagreements (dual-loop AC #2)

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 6068 |
| Title | feat(081KR7JY10008QG0R000MH7PJT): detect→file glue for review-thread disagreements (dual-loop AC #2) |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-05-30T02:04:45Z |
| Merged at | 2026-05-30T02:19:08Z |
| Merge commit SHA | `1112395c87e993666dcac7b8d81723657ce5216d` |
| Branch | `otto-cli/b0164.1-detect-file-glue-2026-05-29` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/6068 |
| Changed files | 2 |
| Additions / deletions | +214 / -2 |

## Description

## What

Adds `fileReviewThreadDisagreement` to `tools/hygiene/divergence-shard.ts` — the **detect→file composing function** that operationalizes 081KR7JY10008QG0R000MH7PJT **AC #2** ("a divergence shard is filed whenever conclusions differ") as a single call.

Prior slices landed the three pieces but not the seam:

- `detectReviewThreadDisagreement` (pure detector, #6067) returns a `DivergenceInput` *or* a `no-disagreement` reason.
- `writeDivergenceShard` (fail-closed-OR-idempotent writer).
- `divergence-reconcile.ts` (morning-reconciliation reader, #6066, AC #4).

Until now, AC #2's verb had **no entry point** — a caller had to detect, branch on `kind`, then hand-thread `divergenceInput` into the writer. This slice collapses that into the one operational unit the protocol describes.

## Behaviour

| Input | Outcome |
|---|---|
| same thread, differing normalized conclusions | `{ kind: "filed", write, divergenceInput }` — shard written |
| same conclusion | `{ kind: "no-disagreement", reason: "same-conclusion" }` — no write |
| different thread | `{ kind: "no-disagreement", reason: "different-thread" }` — no write |
| blank thread id / conclusion / body | throws **before any I/O** |

## Why it stays safe / in-scope

- **Below the blocked end-to-end boundary** (no concurrent-loop harness needed): never auto-resolves the GitHub thread (AC #1), never submits a GitHub review, never changes the shard schema.
- **Never overwrites divergence evidence**: delegates to the writer's fail-closed-OR-idempotent rule.
- **The writer's byte-identical-bodies guard provably cannot fire on detector output**: a `disagreement` means *normalized* conclusions differ, and each perspective body is prefixed with its trimmed (case-preserving) conclusion ⇒ the two bodies are always distinct.

## Focused checks (run in dedicated worktree off `origin/main`)

- `bun test tools/hygiene/divergence-shard.test.ts tools/hygiene/divergence-reconcile.test.ts` → **42 pass / 0 fail** (37 prior + 5 new).
- `bunx tsc --noEmit` → **0 errors in changed files**. One pre-existing global `TS2688 "Cannot find type definition file for 'bun'"` artifact appears (fresh-worktree `@types/bun` resolution; `bun test` passes because Bun ships its own types) — unrelated to this change.
- `git diff --check` → clean.

This change is **TS-only** (no `.fs`/`.cs` touched); the dotnet build gate is unaffected.

## Backlog

081KR7JY10008QG0R000MH7PJT (P1) — PR-review disagreement-preservation protocol (dual-loop AC #2). Parent 081KQJZR90008QG0R002GJAJ19. The end-to-end live dual-review + morning-reconciliation workflow remains the **blocked** impl child pending 081KQJZR90008QG0R000FTJ1TC (concurrent-loop harness).

🤖 Generated with [Claude Code](https://claude.com/claude-code)


## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | true |
| Total threads | 1 |
| Resolved threads | 1 |
| Unresolved threads | 0 |
| Total review comments | 2 |
| Total fix commits (touching thread paths) | 2 |

## Review threads

### Thread 1 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F1MXI`
- **Path**: `tools/hygiene/divergence-shard.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-30T02:07:12Z on `tools/hygiene/divergence-shard.ts`:387 (association: NONE)

  <pre>
  The wrapper currently inherits the detector's lazy body validation, so a blank observation body can be accepted whenever detection returns `no-disagreement` before building perspectives (for example, same conclusion with `loopB.body: " "`). That contradicts this new API's documented behavior that blank thread/conclusion/body inputs throw before any I/O, and lets malformed review observations be silently treated as clean no-disagreement outcomes.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-30T02:15:11Z on `tools/hygiene/divergence-shard.ts`:387 (association: MEMBER)

    <pre>
    Fixed in d6a557a3b. Verified: the detector validated thread id eagerly but conclusion only on the same-thread path and body only on the disagreement path, so a same-conclusion pair with a blank body (or a different-thread pair with a blank conclusion/body) was silently returned as a clean no-disagreement outcome — exactly as you describe.

    Moved all three required-field non-blank checks to the top of `detectReviewThreadDisagreement`, so validation is branch-independent: a malformed observation throws before the no-disagreement decision and before any I/O, matching `fileReviewThreadDisagreement`'s documented contract. Tests extended to cover the previously-uncovered no-disagreement paths (same-conclusion + blank body; different-thread + blank conclusion/body). 30/30 pass.
    </pre>

## Fix commits (touching thread paths)

### `aa35f3a196074abdbce1248f1f740d695cc0d1ba` -- 2026-05-30T02:03:58Z -- `claude`

**Touched files (intersect with thread paths):**

- `tools/hygiene/divergence-shard.ts`

**Message:**

```
feat(081KR7JY10008QG0R000MH7PJT): detect→file glue for review-thread disagreements (dua…

…l-loop AC #2)

Add fileReviewThreadDisagreement: the composing function that operationalizes
AC #2 ("a divergence shard is filed whenever conclusions differ") as a single
call. It wires the existing pure detector (detectReviewThreadDisagreement) to
the existing I/O writer (writeDivergenceShard): on a same-thread/differing-
conclusion disagreement it files the shard and returns "filed" (carrying the
WriteResult); on same-conclusion or different-thread it returns
"no-disagreement" without writing.

Before this slice the protocol's verb had no entry point — a caller had to
detect, branch on kind, then hand-thread the DivergenceInput into the writer.
This collapses detect→file into the one operational unit the protocol describes.

Stays below the blocked end-to-end GitHub boundary: never auto-resolves the
review thread (AC #1), never overwrites a differing shard (writer fail-closed-
OR-idempotent rule), and validation throws surface before any I/O. The writer's
byte-identical-bodies guard provably cannot fire on detector output (differing
normalized conclusions + conclusion-prefixed bodies => distinct bodies).

Focused tests: files-on-disagreement (written + shard content), no-write on
same-conclusion, no-write on different-thread, idempotent-noop re-run, and
validation-throws-before-IO.

Verification:
- bun test tools/hygiene/divergence-shard.test.ts tools/hygiene/divergence-reconcile.test.ts -> 42 pass / 0 fail
- bunx tsc --noEmit -> 0 errors in changed files (1 pre-existing global TS2688 'bun' types artifact in the fresh worktree, unrelated to this change)
- git diff --check -> clean

operative-authorization: aaron 2026-05-14: "- **Devil-pole** (edge-runner drive): keep pushing, discover, go hard, never-be-idle"

Agency-Signature-Version: 1
Agent: Otto-CLI
Agent-Runtime: Claude Code (background worker)
Agent-Model: claude-opus-4-8
Agent-Surface: otto-cli-background
Credential-Identity: acehack
Credential-Mode: operator-authorized
Human-Review: pre-merge-pending
Human-Review-Evidence: PR review pending

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `d6a557a3b2eecc36242c0c3f1321ba2ae5e3c738` -- 2026-05-30T02:14:36Z -- `claude`

**Touched files (intersect with thread paths):**

- `tools/hygiene/divergence-shard.ts`

**Message:**

```
fix(081KR7JY10008QG0R000MH7PJT): eager-validate detector fields so blank body/conclusio…

…n throws on no-disagreement paths

detectReviewThreadDisagreement validated thread id eagerly but the
conclusion only on the same-thread path and the body only on the
disagreement path. So a malformed observation (blank body with a
same-conclusion pair, or blank conclusion/body on a different-thread
pair) was silently accepted as a clean no-disagreement outcome --
contradicting fileReviewThreadDisagreement's documented contract that
blank thread id / conclusion / body throw before any I/O (Copilot
PR #6068 finding).

Move all three required-field non-blank checks to the top of the
detector so validation is branch-independent: a malformed observation
is rejected regardless of whether it would have produced a
disagreement, a same-conclusion, or a different-thread outcome. Code
now matches its own docstring; no doc relaxation.

Error messages adopt the field-path style already used for thread id
(loopA.conclusion / loopB.body), and tests are extended to cover the
previously-uncovered no-disagreement paths.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```
