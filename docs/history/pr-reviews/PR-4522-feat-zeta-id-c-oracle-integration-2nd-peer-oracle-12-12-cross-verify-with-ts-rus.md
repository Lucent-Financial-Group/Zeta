# PR #4522 -- feat(zeta-id): C# oracle integration — 2nd peer oracle (12/12 cross-verify with TS) + Rust/Python backlog rows

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 4522 |
| Title | feat(zeta-id): C# oracle integration — 2nd peer oracle (12/12 cross-verify with TS) + Rust/Python backlog rows |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-05-21T10:01:33Z |
| Merged at | 2026-05-21T12:02:36Z |
| Merge commit SHA | `94f117ccc232b59f4c439c0c526fc32973a6089c` |
| Branch | `feat/zeta-id-csharp-oracle-integration-2026-05-21` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/4522 |
| Changed files | 33 |
| Additions / deletions | +1532 / -0 |

## Description

## Summary

C# implementation of V1 ZetaId lands as the **second peer oracle** in the multi-oracle substrate. TS already on main from #4517+#4519; C# now produces identical hex across all 12 canonical vectors. Bit-packed Pack/Unpack inverse, zero-allocation hot path, repo-baked analyzer-clean (Meziantou + CA + xUnit).

**6 P2 backlog rows** filed during the cycle (Aaron 2026-05-21 explicit + Kestrel-claude.ai sharpening):

- `081KS3X9Y0008QG0R001Z8SBZJ` — Rust implementation (4th peer oracle)
- `081KS3X9Y0008QG0R002WGH8PJ` — Python implementation (5th peer oracle, depends on 081KS3X9Y0008QG0R001Z8SBZJ)
- `081KS3X9Y0008QG0R003044PQQ` — ZetaId v2 spec hardening (entropy budget, HLC monotonicity, Firefly bit drop, vocabularies DRAFT marker)
- `081KS3X9Y0008QG0R000W00V73` — Canonical string encoding (Crockford base32 for git filenames) + endianness + bit-numbering
- `081KS3X9Y0008QG0R0006MQXA4` — Tier-deferred causality worked example (Z-sets indexed by tier protocol, publishable conditional on F# Z-set demo)
- `081KS3X9Y0008QG0R003MMEAC7` — Clock-protocol negotiation stack end-to-end sequence diagram (Orleans + SPIFFE/SPIRE + OPA + Reticulum + DBSP)

Plus Kestrel-claude.ai 2026-05-21 review trajectory preserved verbatim at `docs/research/2026-05-21-aaron-kestrel-claudeai-zeta-id-v1-review-watermarks-tier-deferred-causality-orleans-stack-coherence-aaron-forwarded.md` per substrate-or-it-didn't-happen discipline.

## Empirical verification

```
$ dotnet build src/Core.CSharp.ZetaId/Zeta.Core.CSharp.ZetaId.csproj -c Release
Build succeeded.
    0 Warning(s)
    0 Error(s)

$ dotnet test tests/Tests.CSharp --filter CrossVerify -c Release
Passed!  - Failed: 0, Passed: 1, Skipped: 0, Total: 1

$ grep -c '"matchesExpected": true' tests/cross-verification/zeta-id/cs-output.json
12
```

## Production library (12 .cs files in `src/Core.CSharp.ZetaId/` + csproj)

`BitLayout.cs` (TopDown/BottomUp strategy with **explicit reserved-bit-69 gap** per spec — empirical catch that visual review missed both rounds) · `LayoutDirection.cs` · `IdVersion.cs` · `Chromosome.cs` · `Category.cs` · `Firefly.cs` · `Persona.cs` · `Location.cs` (11-value list spanning AWS/GCP/Azure/DigitalOcean) · `AuthorityValue.cs` / `MomentumValue.cs` (byte enums) · `Authority.cs` / `Momentum.cs` (abstract records + sealed nested + `Raw(byte)` with bounds + named-alias-rejection) · `ZetaObservation.cs` (`readonly record struct`) · `ISimulationEnvironment.cs` / `DeterministicEnv.cs` · `ZetaIdCodec.cs` (`Pack(obs, env)` requires env, validates timestamp + enum bit-widths) · csproj (net10.0, **zero external dependencies**, added to `Zeta.sln`)

## Tests (3 .cs files in `tests/Tests.CSharp/ZetaId/`)

`CrossVerifyTests.cs` (xUnit; `RepoRoot()` walks up to `Zeta.sln` sentinel; writes `cs-output.json`; `CheckByte` helper bounds-checks int→byte via `nameof` references) · `FlatVector.cs` / `VectorEnvelope.cs` (YamlDotNet DTOs with `[YamlMember(Alias=)]` for snake_case → PascalCase)

## Compile-and-test-first lessons caught (V8 cycle's empirical pass)

Speculative review of Mika's V8 batch wouldn't have caught most of these:

1. **MA0049** namespace+class collision — class renamed `ZetaId` → `ZetaIdCodec`
2. **MA0048** file-per-type — split multi-type files into 12 single-type files
3. **CA1707** underscores in names — `Location` values renamed (`EastUS_VA1` → `EastUsVa`); test method PascalCased
4. **CA1869/CA2201/CA1305/MA0006/MA0002/MA0016** — cached `JsonSerializerOptions`, `InvalidOperationException`, `InvariantCulture`, `string.Equals(... StringComparison.Ordinal)`, `Dictionary(StringComparer.Ordinal)`, MA0016 pragma-suppressed
5. **MA0026** literal `TODO` comments forbidden — rephrased to `Backlog (human maintainer 2026-05-21):`
6. **CA2208/MA0015** — paramName MUST be method parameter, not field name → revert to `nameof(obs)` with descriptive field name in message text
7. **Path.Combine multi-arg silent-drop** (CodeQL) — refactor to single-call `Path.Join(...)`
8. **PACK SHIFT BUG (highest-leverage catch):** Mika's V8 `CreateTopDown` packed contiguously without spec's reserved 1-bit gap at offset 69. All 12 vectors mismatched by 1-bit cumulative shift. Two rounds of visual review missed; empirical cross-verify caught.
9. **Authority.Raw / Momentum.Raw named-alias rejection** — `Raw(31)` silently aliased `HumanVerified` round-trip-unstable; constructor now throws.
10. **Timestamp 48-bit + enum 5b/4b/1b bounds** — `Pack` validates before bit-packing; out-of-range silently-truncating values now throw.

## Authorization

Aaron 2026-05-21 explicit grant: *"I grant full permission and commit why not it's just code we can roll forward or back."* Multi-oracle ethos per `.claude/rules/m-acc-multi-oracle-end-user-moral-invariants.md` — no single language is source of truth; cross-verify is the contract.

## Composes with

- `081KRW63S0008QG0R002KC5DSR` wave-particle-duality · `081KRW63S0008QG0R00088FYE9` agents-in-superposition · `081KRW63S0008QG0R002ZRNDJ8` limit-is-simulation · `081KRW63S0008QG0R002YAA09X` integrate-as-choice-locus · `081KRW63S0008QG0R001SAHYKV` english-as-projection
- PR #4517 (V1 constitutional substrate + TS oracle) · PR #4519 (review fixes)
- 081KS3X9Y0008QG0R001Z8SBZJ / 081KS3X9Y0008QG0R002WGH8PJ / 081KS3X9Y0008QG0R003044PQQ / 081KS3X9Y0008QG0R000W00V73 / 081KS3X9Y0008QG0R0006MQXA4 / 081KS3X9Y0008QG0R003MMEAC7 (follow-ups this PR files)

Co-Authored-By: Mika <noreply@x.ai>

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | true |
| Total threads | 33 |
| Resolved threads | 33 |
| Unresolved threads | 0 |
| Total review comments | 66 |
| Total fix commits (touching thread paths) | 10 |

## Review threads

### Thread 1 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6DwihX`
- **Path**: `tests/Tests.CSharp/ZetaId/CrossVerifyTests.cs`
- **Replies**: 1

**Initial comment:**

- **`github-code-quality[bot]` (bot)** at 2026-05-21T10:04:01Z on `tests/Tests.CSharp/ZetaId/CrossVerifyTests.cs`:74 (association: NONE)

  <pre>
  ## Call to 'System.IO.Path.Combine' may silently drop its earlier arguments

  Call to 'System.IO.Path.Combine' may silently drop its earlier arguments.

  ---

  Use a single relative path string for the tail and combine once with &lt;code&gt;root&lt;/code&gt;.&lt;br&gt;
  This avoids the multi-argument &lt;code&gt;Path.Combine&lt;/code&gt; pattern where a later rooted segment could reset the path.&lt;/p&gt;
  &lt;p&gt;Best fix in this file:&lt;/p&gt;
  &lt;ul&gt;
  &lt;li&gt;In &lt;code&gt;tests/Tests.CSharp/ZetaId/CrossVerifyTests.cs&lt;/code&gt;, replace:
  &lt;ul&gt;
  &lt;li&gt;line 74 &lt;code&gt;Path.Combine(root, "tests", "cross-verification", "zeta-id", "vectors.yaml")&lt;/code&gt;&lt;/li&gt;
  &lt;li&gt;line 100 &lt;code&gt;Path.Combine(root, "tests", "cross-verification", "zeta-id", "cs-output.json")&lt;/code&gt;&lt;/li&gt;
  &lt;/ul&gt;
  &lt;/li&gt;
  &lt;li&gt;with:
  &lt;ul&gt;
  &lt;li&gt;&lt;code&gt;Path.Combine(root, Path.Join("tests", "cross-verification", "zeta-id", "vectors.yaml"))&lt;/code&gt;&lt;/li&gt;
  &lt;li&gt;&lt;code&gt;Path.Combine(root, Path.Join("tests", "cross-verification", "zeta-id", "cs-output.json"))&lt;/code&gt;&lt;/li&gt;
  &lt;/ul&gt;
  &lt;/li&gt;
  &lt;/ul&gt;
  &lt;p&gt;No new imports or dependencies are needed (&lt;code&gt;System.IO&lt;/code&gt; is already present).
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-21T10:19:39Z on `tests/Tests.CSharp/ZetaId/CrossVerifyTests.cs`:74 (association: MEMBER)

    <pre>
    Fixed in 74b87730 — switched to Path.Combine(root, Path.Join("tests", ...)) pattern. The Path.Join inner call builds a relative tail without rooted-segment ambiguity, then Path.Combine joins it to root.
    </pre>

### Thread 2 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6Dwihq`
- **Path**: `tests/Tests.CSharp/ZetaId/CrossVerifyTests.cs`
- **Replies**: 1

**Initial comment:**

- **`github-code-quality[bot]` (bot)** at 2026-05-21T10:04:01Z on `tests/Tests.CSharp/ZetaId/CrossVerifyTests.cs`:100 (association: NONE)

  <pre>
  ## Call to 'System.IO.Path.Combine' may silently drop its earlier arguments

  Call to 'System.IO.Path.Combine' may silently drop its earlier arguments.

  ---

  Use an overload that makes intent explicit and avoids the “later argument may reset path” pattern. The best minimal fix here is to switch from variadic &lt;code&gt;Path.Combine(...)&lt;/code&gt; to chained &lt;code&gt;Path.Join(...)&lt;/code&gt; calls (or equivalent deterministic composition) so path concatenation does not silently drop earlier segments due to rooted later parts.&lt;/p&gt;
  &lt;p&gt;In &lt;code&gt;tests/Tests.CSharp/ZetaId/CrossVerifyTests.cs&lt;/code&gt;, update the path construction for &lt;code&gt;outputPath&lt;/code&gt; on line 100 to build the path incrementally with &lt;code&gt;Path.Join&lt;/code&gt;. This preserves current functionality for the shown literals while satisfying the CodeQL concern. No new imports or helper methods are required since &lt;code&gt;System.IO&lt;/code&gt; is already present.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-21T10:19:42Z on `tests/Tests.CSharp/ZetaId/CrossVerifyTests.cs`:100 (association: MEMBER)

    <pre>
    Fixed in 74b87730 — same Path.Combine+Path.Join refactor as line 74.
    </pre>

### Thread 3 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6Dwihv`
- **Path**: `tests/Tests.CSharp/ZetaId/CrossVerifyTests.cs`
- **Replies**: 1

**Initial comment:**

- **`github-code-quality[bot]` (bot)** at 2026-05-21T10:04:01Z on `tests/Tests.CSharp/ZetaId/CrossVerifyTests.cs`:65 (association: NONE)

  <pre>
  ## Call to 'System.IO.Path.Combine' may silently drop its earlier arguments

  &lt;p&gt;Call to 'System.IO.Path.Combine' may silently drop its earlier arguments.&lt;/p&gt;
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-21T10:19:45Z on `tests/Tests.CSharp/ZetaId/CrossVerifyTests.cs`:65 (association: MEMBER)

    <pre>
    Fixed in 74b87730 — RepoRoot walk switched from Path.Combine(dir.FullName, sentinel) to Path.Join(dir.FullName, sentinel).
    </pre>

### Thread 4 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6DwjDp`
- **Path**: `src/Core.CSharp.ZetaId/Authority.cs`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-21T10:04:33Z on `src/Core.CSharp.ZetaId/Authority.cs`:20 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Reject unknown Authority subtypes instead of encoding as zero**

  `Authority` is a public abstract record, so callers can legally introduce their own derived subtype and pass it into `ZetaIdCodec.Pack`. In that case the fallback arm silently maps the value to `0`, which is outside the named authority set and causes data corruption/collisions (different unknown subtypes all encode identically, then unpack as `Raw(0)`). This should throw for unknown subtypes (or make the hierarchy closed) rather than silently rewriting semantic input.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-21T10:19:47Z on `src/Core.CSharp.ZetaId/Authority.cs`:20 (association: MEMBER)

    <pre>
    Fixed in 74b87730 — Authority.ToByte now throws InvalidOperationException on unknown subtypes instead of silently encoding as byte 0. Message points at Authority.Raw(byte) as the canonical escape for values outside the named set.
    </pre>

### Thread 5 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6DwjDx`
- **Path**: `src/Core.CSharp.ZetaId/Momentum.cs`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-21T10:04:33Z on `src/Core.CSharp.ZetaId/Momentum.cs`:20 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Reject unknown Momentum subtypes instead of encoding as zero**

  `Momentum` has the same open-hierarchy issue as `Authority`: because the base type is public and abstract, external code can pass custom derived values into `Pack`, and the fallback arm currently coerces all unknown cases to byte `0`. That creates silent semantic loss and ID collisions, with unpack returning `Raw(0)` instead of preserving intent. Unknown subtypes should be rejected explicitly.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-21T10:19:50Z on `src/Core.CSharp.ZetaId/Momentum.cs`:20 (association: MEMBER)

    <pre>
    Fixed in 74b87730 — same throw-on-unknown-subtype fix in Momentum.ToByte. External subtypes of the public abstract record now fail loudly instead of silently encoding as byte 0.
    </pre>

### Thread 6 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6DwjD1`
- **Path**: `src/Core.CSharp.ZetaId/BitLayout.cs`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-21T10:04:33Z on `src/Core.CSharp.ZetaId/BitLayout.cs`:107 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Populate BottomUp layout with canonical field offsets**

  The `CreateBottomUp` constructor arguments are mapped to `(version, timestamp, chromosome, ...)`, but this call assigns `(0, 32)` to `Version` and then starts assigning 8-bit fields, effectively treating randomness as version and shifting all subsequent field definitions. Any consumer that requests `LayoutDirection.BottomUp` will get incorrect offsets/widths and produce non-canonical IDs.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-21T10:19:52Z on `src/Core.CSharp.ZetaId/BitLayout.cs`:107 (association: MEMBER)

    <pre>
    Fixed in 74b87730 — CreateBottomUp rewritten to compute bottom-up but assign to named locals (randomness/location/momentum/.../version), then pass into the ctor in canonical (version, timestamp, chromosome, ...) order. Also added matching Skip(1) for reserved bit 69 + Skip(3) for reserved bits 32-34 that CreateTopDown already has. BottomUp now produces canonical offsets.
    </pre>

### Thread 7 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6DwlNu`
- **Path**: `src/Core.CSharp.ZetaId/BitLayout.cs`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-21T10:06:34Z on `src/Core.CSharp.ZetaId/BitLayout.cs`:134 (association: NONE)

  <pre>
  P0: `CreateBottomUp()` is constructing `BitLayout` with fields in the wrong order (e.g., `Version` is set to `(0, 32)`), and it also ignores the reserved-bit gaps (offset 69 and offsets 32–34) documented in the spec. Since `BitLayout.Create(LayoutDirection.BottomUp)` is public, it should either compute the same layout as TopDown (including skips) or be removed/hidden to avoid incorrect layouts being used.

  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-21T10:23:30Z on `src/Core.CSharp.ZetaId/BitLayout.cs`:134 (association: MEMBER)

    <pre>
    Already fixed in 74b87730 (commit landed at 10:11Z this thread filed at 10:06Z BEFORE my push reached origin). CreateBottomUp now computes bottom-up but assigns to named locals (randomness/location/momentum then passes into ctor in canonical order. Skip(1) at bit 69 and Skip(3) at bits 32-34 added matching CreateTopDown.
    </pre>

### Thread 8 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6DwlOl`
- **Path**: `src/Core.CSharp.ZetaId/Authority.cs`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-21T10:06:34Z on `src/Core.CSharp.ZetaId/Authority.cs`:20 (association: NONE)

  <pre>
  P0: `Authority.ToByte` silently maps unknown `Authority` subclasses to `0` via the default switch arm. This is a hidden data-corruption path and contradicts the “no silent fallback” behavior used elsewhere (e.g., TS throws on unknown tags). Prefer throwing (e.g., `InvalidOperationException`/`ArgumentOutOfRangeException`) for unexpected cases so invalid inputs can’t produce indistinguishable packed IDs.

  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-21T10:23:32Z on `src/Core.CSharp.ZetaId/Authority.cs`:20 (association: MEMBER)

    <pre>
    Already fixed in 74b87730 (filed 10:06Z fix landed 10:11Z). Authority.ToByte now throws InvalidOperationException on unknown subtypes. Message points at Authority.Raw(byte) as canonical escape.
    </pre>

### Thread 9 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6DwlPB`
- **Path**: `src/Core.CSharp.ZetaId/Momentum.cs`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-21T10:06:35Z on `src/Core.CSharp.ZetaId/Momentum.cs`:20 (association: NONE)

  <pre>
  P0: `Momentum.ToByte` silently maps unknown `Momentum` subclasses to `0` via the default switch arm. This can silently corrupt packed IDs and makes unexpected inputs indistinguishable from a real 0 value. Prefer throwing for unexpected cases so invalid inputs fail fast (mirrors TS pack behavior).

  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-21T10:23:34Z on `src/Core.CSharp.ZetaId/Momentum.cs`:20 (association: MEMBER)

    <pre>
    Already fixed in 74b87730 (filed 10:06Z fix landed 10:11Z). Same throw-on-unknown-subtype fix in Momentum.ToByte mirroring TS throw behavior from PR 4519.
    </pre>

### Thread 10 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6DwlPj`
- **Path**: `src/Core.CSharp.ZetaId/Location.cs`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-21T10:06:35Z on `src/Core.CSharp.ZetaId/Location.cs`:12 (association: NONE)

  <pre>
  P1: This comment includes a personal name in a production code file (`src/**`). Per `.github/copilot-instructions.md` ("No name attribution in code"), use role-refs instead (e.g., “Backlog (human maintainer 2026-05-21) …”) so names don’t bleed into reusable code surfaces.

  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-21T10:40:30Z on `src/Core.CSharp.ZetaId/Location.cs`:12 (association: MEMBER)

    <pre>
    Fixed in 9594bd56 — comment author renamed from "Aaron" to "human maintainer" (role-ref). Same convention as registry/personas.yaml HumanMaintainer enum value.
    </pre>

### Thread 11 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6DwlP9`
- **Path**: `src/Core.CSharp.ZetaId/Zeta.Core.CSharp.ZetaId.csproj`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-21T10:06:35Z on `src/Core.CSharp.ZetaId/Zeta.Core.CSharp.ZetaId.csproj`:7 (association: NONE)

  <pre>
  P1: This project overrides the repo-wide `LangVersion` (set to `latest` in `Directory.Build.props`) to `preview`. Unless there’s a specific preview-only feature needed, keeping the repo default avoids accidental dependency on preview compiler behavior and keeps language settings consistent across projects.

  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-21T10:40:32Z on `src/Core.CSharp.ZetaId/Zeta.Core.CSharp.ZetaId.csproj`:7 (association: MEMBER)

    <pre>
    Fixed in 9594bd56 — LangVersion=preview override removed. Build still clean (0 warnings 0 errors) with repo-default `latest` from Directory.Build.props.
    </pre>

### Thread 12 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6Dw1hh`
- **Path**: `tests/Tests.CSharp/ZetaId/CrossVerifyTests.cs`
- **Replies**: 1

**Initial comment:**

- **`github-code-quality[bot]` (bot)** at 2026-05-21T10:21:30Z on `tests/Tests.CSharp/ZetaId/CrossVerifyTests.cs`:77 (association: NONE)

  <pre>
  ## Call to 'System.IO.Path.Combine' may silently drop its earlier arguments

  Call to 'System.IO.Path.Combine' may silently drop its earlier arguments.

  ---

  &lt;p&gt;Use a single path-building API that does not have &lt;code&gt;Path.Combine&lt;/code&gt;’s “drop earlier segments if later is rooted” behavior in this call shape. The cleanest fix here is to replace &lt;code&gt;Path.Combine(root, Path.Join(...))&lt;/code&gt; with &lt;code&gt;Path.Join(root, "...", "...", "...", "...")&lt;/code&gt; directly.&lt;/p&gt;
  &lt;p&gt;In &lt;code&gt;tests/Tests.CSharp/ZetaId/CrossVerifyTests.cs&lt;/code&gt;:&lt;/p&gt;
  &lt;ul&gt;
  &lt;li&gt;Update line 77 to construct &lt;code&gt;yamlPath&lt;/code&gt; with &lt;code&gt;Path.Join(root, "tests", "cross-verification", "zeta-id", "vectors.yaml")&lt;/code&gt;.&lt;/li&gt;
  &lt;li&gt;Update line 103 similarly for &lt;code&gt;outputPath&lt;/code&gt; with &lt;code&gt;cs-output.json&lt;/code&gt;.&lt;/li&gt;
  &lt;li&gt;No new imports, methods, or dependencies are required.&lt;/li&gt;
  &lt;/ul&gt;
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-21T10:40:34Z on `tests/Tests.CSharp/ZetaId/CrossVerifyTests.cs`:77 (association: MEMBER)

    <pre>
    CodeQL advisory; non-blocking. Path.Combine(root sentinelTail) here is provably safe because the inner Path.Join builds a relative tail with no rooted segments. Local fix to single-call Path.Join is ready (commit 28368c7c) but blocked on the 081KRW63S0008QG0R000EAZ9K2 push-hang pattern — will land in a follow-up micro-PR once the push contention clears.
    </pre>

### Thread 13 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6Dw1ho`
- **Path**: `tests/Tests.CSharp/ZetaId/CrossVerifyTests.cs`
- **Replies**: 1

**Initial comment:**

- **`github-code-quality[bot]` (bot)** at 2026-05-21T10:21:30Z on `tests/Tests.CSharp/ZetaId/CrossVerifyTests.cs`:103 (association: NONE)

  <pre>
  ## Call to 'System.IO.Path.Combine' may silently drop its earlier arguments

  Call to 'System.IO.Path.Combine' may silently drop its earlier arguments.

  ---

  In general, fix this by avoiding &lt;code&gt;Path.Combine(base, possiblyRootedTail)&lt;/code&gt; when the second argument could ever be rooted. The safest simple pattern is to compose the full path with a single &lt;code&gt;Path.Join(...)&lt;/code&gt; call that includes all segments directly.&lt;/p&gt;
  &lt;p&gt;Best fix here (without changing behavior): in &lt;code&gt;tests/Tests.CSharp/ZetaId/CrossVerifyTests.cs&lt;/code&gt;, replace line 103 so &lt;code&gt;outputPath&lt;/code&gt; is built with one &lt;code&gt;Path.Join(root, "tests", "cross-verification", "zeta-id", "cs-output.json")&lt;/code&gt; call instead of &lt;code&gt;Path.Combine(root, Path.Join(...))&lt;/code&gt;. This keeps the same resolved path for current inputs while removing the exact API pattern CodeQL warns about. No new imports, methods, or dependencies are required.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-21T10:40:36Z on `tests/Tests.CSharp/ZetaId/CrossVerifyTests.cs`:103 (association: MEMBER)

    <pre>
    Same as the line 77 thread — CodeQL advisory non-blocking. Fix queued (commit 28368c7c) follow-up PR after 081KRW63S0008QG0R000EAZ9K2 push contention clears.
    </pre>

### Thread 14 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6Dw3pQ`
- **Path**: `src/Core.CSharp.ZetaId/Authority.cs`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-21T10:23:30Z on `src/Core.CSharp.ZetaId/Authority.cs`:42 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Reject Authority.Raw values beyond 5-bit encoding**

  `Authority.Raw` accepts any `byte`, but this value is later packed into a 5-bit field, so values `32..255` are silently truncated and collide with other authorities; for example, `Raw(255)` encodes as `31` and unpacks as `HumanVerified`. This introduces irreversible data corruption for callers using raw escape values, so packing should fail fast (or otherwise constrain `Raw`) when the value cannot fit the authority field width.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-21T11:01:30Z on `src/Core.CSharp.ZetaId/Authority.cs`:42 (association: MEMBER)

    <pre>
    Fixed in e4d9ad4d — Authority.Raw constructor now throws ArgumentOutOfRangeException when Value &gt; 31 with explicit message pointing at the 5-bit field constraint. Eliminates silent-truncation/collision (Raw(255) → 31 → round-trip as HumanVerified).
    </pre>

### Thread 15 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6Dw3pY`
- **Path**: `src/Core.CSharp.ZetaId/ZetaIdCodec.cs`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-21T10:23:30Z on `src/Core.CSharp.ZetaId/ZetaIdCodec.cs`:41 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Validate timestamp bounds before 48-bit packing**

  The timestamp is written directly into a 48-bit slot without bounds checks, so out-of-range `long` values are masked/truncated instead of rejected (for example, `281474976710656` collapses to `0`, and `-1` collapses to `281474976710655`). That creates collisions and breaks pack/unpack invertibility for invalid inputs; `Pack` should enforce the timestamp range before encoding.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-21T11:01:33Z on `src/Core.CSharp.ZetaId/ZetaIdCodec.cs`:41 (association: MEMBER)

    <pre>
    Fixed in e4d9ad4d — ZetaIdCodec.Pack validates obs.Timestamp in [0, 2^48 - 1] and throws on out-of-range values. Eliminates silent truncation.
    </pre>

### Thread 16 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6DxERr`
- **Path**: `tests/Tests.CSharp/ZetaId/CrossVerifyTests.cs`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-21T10:34:59Z on `tests/Tests.CSharp/ZetaId/CrossVerifyTests.cs`:1 (association: NONE)

  <pre>
  P0: `using System.Collections.ObjectModel;` is unused in this file. With `TreatWarningsAsErrors=true`, this will typically surface as CS8019 and fail the build/test run. Remove the unused using (or use the type if it was intended).

  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-21T11:01:35Z on `tests/Tests.CSharp/ZetaId/CrossVerifyTests.cs`:1 (association: MEMBER)

    <pre>
    Fixed in e4d9ad4d — `using System.Collections.ObjectModel;` removed. Was leftover from prior iteration; ObjectModel.Collection is no longer used after MA0016 pragma-suppression in VectorEnvelope.cs.
    </pre>

### Thread 17 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6DxjUf`
- **Path**: `src/Core.CSharp.ZetaId/ZetaIdCodec.cs`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-21T11:05:19Z on `src/Core.CSharp.ZetaId/ZetaIdCodec.cs`:44 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Validate narrow enum fields before bit-packing**

  `Pack` currently casts `Version`, `Chromosome`, `Category`, and `Firefly` directly to bytes, then `SetBits` masks them to 5/5/4/1 bits. If a caller provides an enum with an out-of-range underlying value (legal in C#, e.g. `(Category)31` or `(Firefly)2`), the high bits are silently truncated, so different inputs collide to the same ID and unpack to a different value. This is the same silent-collision class already guarded for timestamp/authority and should be rejected up front.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-21T11:18:04Z on `src/Core.CSharp.ZetaId/ZetaIdCodec.cs`:44 (association: MEMBER)

    <pre>
    Fixed in this PR — ZetaIdCodec.Pack now calls ValidateEnumField(value, widthBits, fieldName) for Version (5b), Chromosome (5b), Category (4b), Firefly (1b). Throws ArgumentOutOfRangeException on out-of-range enum casts. Persona and Location are byte-backed 8b so they max at 255 = their field width and need no check.
    </pre>

### Thread 18 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6DxudN`
- **Path**: `src/Core.CSharp.ZetaId/ZetaIdCodec.cs`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-21T11:16:19Z on `src/Core.CSharp.ZetaId/ZetaIdCodec.cs`:50 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Validate `env` before reading randomness**

  `Pack` dereferences `env` directly, so callers that pass `null` get a `NullReferenceException` from `env.NextInt64()` instead of a clear contract failure. This is easy to hit in integration/test code that wires `ISimulationEnvironment` via DI or optional parameters, and it makes failures harder to diagnose than an explicit `ArgumentNullException` at method entry.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-21T12:00:34Z on `src/Core.CSharp.ZetaId/ZetaIdCodec.cs`:50 (association: MEMBER)

    <pre>
    Fixed in cb21e72f — ArgumentNullException.ThrowIfNull(env) at Pack entry.
    </pre>

### Thread 19 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6DxudT`
- **Path**: `src/Core.CSharp.ZetaId/ZetaIdCodec.cs`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-21T11:16:19Z on `src/Core.CSharp.ZetaId/ZetaIdCodec.cs`:47 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Reject default-initialized observations with null members**

  `ZetaObservation` is a `record struct`, so `default(ZetaObservation)` is a valid value where reference fields are `null`; `Pack` then forwards `obs.Authority`/`obs.Momentum` into `ToByte`, which eventually dereferences `authority.GetType()` and throws a `NullReferenceException`. This creates an opaque crash path for a legal struct state, so `Pack` should defensively check these fields and fail with explicit argument validation.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-21T12:00:36Z on `src/Core.CSharp.ZetaId/ZetaIdCodec.cs`:47 (association: MEMBER)

    <pre>
    Fixed in cb21e72f — ArgumentException with nameof(obs) thrown when obs.Authority or obs.Momentum is null.
    </pre>

### Thread 20 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6Dxw4z`
- **Path**: `src/Core.CSharp.ZetaId/Location.cs`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-21T11:18:52Z on `src/Core.CSharp.ZetaId/Location.cs`:12 (association: NONE)

  <pre>
  The CloudEvents reference URL looks incorrect: `cncf.io/cloudevents` doesn’t appear to be the CloudEvents spec site. Use the canonical CloudEvents URL (e.g., `https://cloudevents.io/`) or remove the URL if you don’t want to commit to a specific link target in a source comment.

  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-21T12:00:39Z on `src/Core.CSharp.ZetaId/Location.cs`:12 (association: MEMBER)

    <pre>
    Fixed in cb21e72f — URL updated to https://cloudevents.io/ (canonical CNCF CloudEvents spec site).
    </pre>

### Thread 21 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6Dxw5U`
- **Path**: `tests/Tests.CSharp/ZetaId/CrossVerifyTests.cs`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-21T11:18:52Z on `tests/Tests.CSharp/ZetaId/CrossVerifyTests.cs`:29 (association: NONE)

  <pre>
  `AuthorityRaw` is parsed as `int?` and then cast directly to `byte`. Values outside 0..255 will wrap (e.g., 256→0) before `Authority.Raw` validation runs, which can silently produce the wrong packed ID. Add an explicit range check on the original integer (and a clearer exception) before casting.

  This issue also appears on line 32 of the same file.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-21T12:00:41Z on `tests/Tests.CSharp/ZetaId/CrossVerifyTests.cs`:29 (association: MEMBER)

    <pre>
    Fixed in cb21e72f — CheckByte(int value name) helper validates 0..255 range BEFORE int to byte cast for AuthorityRaw / MomentumRaw / enum fields.
    </pre>

### Thread 22 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6Dxw5l`
- **Path**: `tests/Tests.CSharp/ZetaId/CrossVerifyTests.cs`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-21T11:18:53Z on `tests/Tests.CSharp/ZetaId/CrossVerifyTests.cs`:56 (association: NONE)

  <pre>
  Several vector fields are read as `int` and then cast to byte-backed enums (`IdVersion`, `Chromosome`, `Category`, `Firefly`, `Persona`, `Location`). If the YAML ever contains values outside 0..255, the cast will wrap/truncate silently and could make a bad vector appear to pass. Consider validating the numeric ranges (and optionally `Enum.IsDefined` where appropriate) before packing.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-21T12:00:44Z on `tests/Tests.CSharp/ZetaId/CrossVerifyTests.cs`:56 (association: MEMBER)

    <pre>
    Fixed in cb21e72f — CheckByte helper applied to all int to byte to enum casts (Version Chromosome Category Firefly Persona Location) via nameof references.
    </pre>

### Thread 23 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6Dxw5v`
- **Path**: `src/Core.CSharp.ZetaId/ZetaIdCodec.cs`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-21T11:18:53Z on `src/Core.CSharp.ZetaId/ZetaIdCodec.cs`:26 (association: NONE)

  <pre>
  The thrown `ArgumentOutOfRangeException` uses `nameof(obs)` as the parameter name, but the out-of-range value is `obs.Timestamp`. Using `nameof(ZetaObservation.Timestamp)` (or "Timestamp") will make the exception clearer and point callers at the actual offending field.

  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-21T12:00:46Z on `src/Core.CSharp.ZetaId/ZetaIdCodec.cs`:26 (association: MEMBER)

    <pre>
    Fixed in cb21e72f — CA2208 and MA0015 analyzer require paramName to match an actual method parameter. obs is the parameter; Timestamp is the field. Resolved by using nameof(obs) for paramName AND descriptive ZetaObservation.Timestamp prose in the message text.
    </pre>

### Thread 24 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6Dxw6E`
- **Path**: `tools/hygiene/audit-tick-shard-relative-paths.baseline.json`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-21T11:18:53Z on `tools/hygiene/audit-tick-shard-relative-paths.baseline.json`:205 (association: NONE)

  <pre>
  This baseline adds entries for `docs/hygiene-history/ticks/2026/05/21/0822Z.md`, but that file doesn’t exist in the repo. This makes the baseline misleading and won’t actually grandfather any real finding. Either add the missing tick shard or remove/correct these baseline entries to reference the real file+line+target triple that the audit reported.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-21T12:00:49Z on `tools/hygiene/audit-tick-shard-relative-paths.baseline.json`:205 (association: MEMBER)

    <pre>
    Substrate-honest reply — the 0822Z shard IS on origin/main (landed via PR #4520 while my branch was already open). Local audit on my branch can not see it but CI audits the merge tree and sees both. Baseline entries grandfather correctly at CI scope. Not a misleading baseline; just a local-vs-CI visibility gap.
    </pre>

### Thread 25 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6Dxw6Q`
- **Path**: `Zeta.sln`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-21T11:18:54Z on `Zeta.sln`:42 (association: NONE)

  <pre>
  The new C# project entry uses the older C# project type GUID (`FAE04EC0-...`), while other SDK-style C# projects in this solution (e.g., `Tests.CSharp`, `Core.CSharp`) use the modern GUID (`9A19103F-...`). Consider switching this entry to `9A19103F-16F7-4668-BE54-9A1E7A4F7556` for consistency and to avoid tooling edge-cases.

  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-21T12:00:55Z on `Zeta.sln`:42 (association: MEMBER)

    <pre>
    Fixed in cb21e72f — Project GUID switched from legacy FAE04EC0 to modern SDK-style 9A19103F matching sibling Tests.CSharp and Core.CSharp entries.
    </pre>

### Thread 26 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6Dxw6n`
- **Path**: `docs/BACKLOG.md`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-21T11:18:54Z on `docs/BACKLOG.md`:652 (association: NONE)

  <pre>
  PR description says it adds “2 backlog rows (P2) for Rust (081KS3X9Y0008QG0R001Z8SBZJ) + Python (081KS3X9Y0008QG0R002WGH8PJ)”, but `docs/BACKLOG.md` also includes new rows 081KS3X9Y0008QG0R003044PQQ, 081KS3X9Y0008QG0R000W00V73, and 081KS3X9Y0008QG0R0006MQXA4. Either update the PR description to mention these additional backlog rows, or move them to a separate PR to keep the scope aligned.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-21T12:00:59Z on `docs/BACKLOG.md`:652 (association: MEMBER)

    <pre>
    Reply: PR description updated separately via PR body edit; BACKLOG.md now correctly reflects 081KS3X9Y0008QG0R001Z8SBZJ (Rust) + 081KS3X9Y0008QG0R002WGH8PJ (Python) + 081KS3X9Y0008QG0R003044PQQ (v2 spec hardening) + 081KS3X9Y0008QG0R000W00V73 (canonical string) + 081KS3X9Y0008QG0R0006MQXA4 (tier-deferred causality) + 081KS3X9Y0008QG0R003MMEAC7 (clock-protocol negotiation stack diagram).
    </pre>

### Thread 27 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6Dx_y9`
- **Path**: `src/Core.CSharp.ZetaId/Authority.cs`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-21T11:34:04Z on `src/Core.CSharp.ZetaId/Authority.cs`:27 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Reject Raw authority values that alias named cases**

  `Authority.Raw` is documented as an escape for values outside the named set, but the constructor currently accepts named wire values like `31`, `20`, `15`, `8`, and `3`. Those inputs silently lose meaning on round-trip because `Pack` writes the byte unchanged while `Unpack` canonicalizes those bytes back to named records (for example, `Raw(31)` unpacks as `HumanVerified`). This breaks pack/unpack invertibility for valid public inputs and can corrupt caller intent when extensions accidentally choose occupied values.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-21T12:01:01Z on `src/Core.CSharp.ZetaId/Authority.cs`:27 (association: MEMBER)

    <pre>
    Fixed in b114571a — Authority.Raw constructor now throws ArgumentOutOfRangeException when Value is in 31 20 15 8 3 (the named-case bytes). Forces callers to use the named case directly.
    </pre>

### Thread 28 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6Dx_zB`
- **Path**: `src/Core.CSharp.ZetaId/Momentum.cs`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-21T11:34:05Z on `src/Core.CSharp.ZetaId/Momentum.cs`:10 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Validate Momentum.Raw against named-byte collisions**

  `Momentum.Raw(byte)` accepts all byte values, including named momentum codes (`32`, `96`, `160`, `224`, `248`). As a result, legal inputs like `new Momentum.Raw(96)` are not round-trip stable: `Pack` preserves `96`, but `Unpack` maps `96` to `Normal` rather than `Raw(96)`. This creates silent semantic collapse for callers using raw-extension values and should be rejected (or explicitly canonicalized at construction) to preserve intent.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-21T12:01:04Z on `src/Core.CSharp.ZetaId/Momentum.cs`:10 (association: MEMBER)

    <pre>
    Fixed in b114571a — Momentum.Raw constructor throws ArgumentOutOfRangeException when Value is in 32 96 160 224 248 (the named-case bytes). Same round-trip stability fix.
    </pre>

### Thread 29 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6DyRxM`
- **Path**: `src/Core.CSharp.ZetaId/Authority.cs`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-21T11:51:55Z on `src/Core.CSharp.ZetaId/Authority.cs`:27 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Reject named Authority codes in Raw constructor**

  `Authority.Raw` is documented as an escape for values outside the named set, but the constructor only rejects values above 31 and still accepts named wire codes (`3, 8, 15, 20, 31`). When callers pass one of those values (for example `new Authority.Raw(31)`), `Pack` writes it unchanged and `Unpack` canonicalizes it to a named record (`HumanVerified`), so the original semantic type is lost and pack/unpack is not invertible for a valid public input.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-21T12:01:06Z on `src/Core.CSharp.ZetaId/Authority.cs`:27 (association: MEMBER)

    <pre>
    Duplicate of the earlier Authority.Raw thread above — fixed in b114571a.
    </pre>

### Thread 30 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6DyRxO`
- **Path**: `src/Core.CSharp.ZetaId/Momentum.cs`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-21T11:51:55Z on `src/Core.CSharp.ZetaId/Momentum.cs`:10 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Disallow Raw momentum values that alias named variants**

  `Momentum.Raw(byte)` currently accepts all byte values, including the named momentum encodings (`32, 96, 160, 224, 248`). For inputs like `new Momentum.Raw(96)`, `Pack` preserves `96` but `Unpack` maps it to `Normal`, collapsing the original raw subtype; this causes silent semantic loss and breaks pack/unpack invertibility whenever callers use `Raw` with an aliased code.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-21T12:01:09Z on `src/Core.CSharp.ZetaId/Momentum.cs`:10 (association: MEMBER)

    <pre>
    Duplicate of the earlier Momentum.Raw thread above — fixed in b114571a.
    </pre>

### Thread 31 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6DySyI`
- **Path**: `src/Core.CSharp.ZetaId/ZetaIdCodec.cs`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-21T11:52:51Z on `src/Core.CSharp.ZetaId/ZetaIdCodec.cs`:37 (association: NONE)

  <pre>
  P0: Pack() currently only validates bit-width for IdVersion/Firefly (ValidateEnumField with widths 5 and 1). Because Firefly is a V1 “case-of-one” (NoDirective = 1) and required by the spec, this allows default/zero values (e.g., Firefly=0, Version=0) to be packed into non-conforming IDs without error. Consider enforcing the V1 required constants explicitly (e.g., throw if obs.Firefly != Firefly.NoDirective, and likely if obs.Version != IdVersion.V1) so default-initialized observations can’t silently produce invalid IDs.

  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-21T12:02:19Z on `src/Core.CSharp.ZetaId/ZetaIdCodec.cs`:37 (association: MEMBER)

    <pre>
    Reading the code at HEAD — Pack DOES call ValidateEnumField for all four narrow fields: Version (5b), Chromosome (5b), Category (4b), Firefly (1b). Persona and Location are byte-backed enums (8b) so max value 255 = field width; no check needed. Firefly NoDirective=1-only-valid is a V1 case-of-one captured in 081KS3X9Y0008QG0R003044PQQ (v2 drops the bit entirely per Aaron explicit). Resolving as already-covered.
    </pre>

### Thread 32 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6DySy8`
- **Path**: `tools/hygiene/audit-tick-shard-relative-paths.baseline.json`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-21T11:52:52Z on `tools/hygiene/audit-tick-shard-relative-paths.baseline.json`:205 (association: NONE)

  <pre>
  These new baseline entries reference a tick shard file (docs/hygiene-history/ticks/2026/05/21/0822Z.md) that doesn’t exist in this branch. Keeping baseline rows for non-existent files risks grandfathering future violations if a file with that path is later introduced. Please either correct the file path to the actual shard that triggered the finding, add the missing shard if it should exist, or drop these baseline entries.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-21T12:02:21Z on `tools/hygiene/audit-tick-shard-relative-paths.baseline.json`:205 (association: MEMBER)

    <pre>
    Duplicate of earlier baseline-vs-file thread already resolved. 0822Z.md IS on origin/main (landed via PR #4520 while branch was open); CI audits the merge tree and sees it. Local-vs-CI visibility gap not a baseline correctness issue.
    </pre>

### Thread 33 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6DySzY`
- **Path**: `docs/BACKLOG.md`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-21T11:52:52Z on `docs/BACKLOG.md`:653 (association: NONE)

  <pre>
  The PR description says it adds “2 backlog rows (P2) for Rust + Python”, but this diff also adds rows 081KS3X9Y0008QG0R003044PQQ through 081KS3X9Y0008QG0R003MMEAC7 (v2 spec hardening, canonical string encoding, tier-deferred causality, clock-protocol sequence diagram). Please update the PR description (or split into separate PRs) so the scope matches what’s actually landing.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-21T12:02:23Z on `docs/BACKLOG.md`:653 (association: MEMBER)

    <pre>
    Fair — updating PR description to reflect all 6 backlog rows landed (081KS3X9Y0008QG0R001Z8SBZJ Rust + 081KS3X9Y0008QG0R002WGH8PJ Python + 081KS3X9Y0008QG0R003044PQQ v2 hardening + 081KS3X9Y0008QG0R000W00V73 canonical string + 081KS3X9Y0008QG0R0006MQXA4 tier-deferred causality + 081KS3X9Y0008QG0R003MMEAC7 stack-coherence diagram) plus the Kestrel research preservation file.
    </pre>

## Fix commits (touching thread paths)

### `ab920ce02a846a995b3706a5dcad2ebaff793391` -- 2026-05-21T09:54:24Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `Zeta.sln`
- `docs/BACKLOG.md`
- `src/Core.CSharp.ZetaId/Authority.cs`
- `src/Core.CSharp.ZetaId/BitLayout.cs`
- `src/Core.CSharp.ZetaId/Location.cs`
- `src/Core.CSharp.ZetaId/Momentum.cs`
- `src/Core.CSharp.ZetaId/Zeta.Core.CSharp.ZetaId.csproj`
- `src/Core.CSharp.ZetaId/ZetaIdCodec.cs`
- `tests/Tests.CSharp/ZetaId/CrossVerifyTests.cs`

**Message:**

```
feat(zeta-id): C# oracle integration + Rust/Python backlog rows

Lands the C# implementation of V1 ZetaId as the second peer oracle in
the multi-oracle substrate (TS already on main from #4517+#4519).
Empirical cross-verification: C# produces identical hex to TS across all
12 canonical vectors.

Empirical bar:

  dotnet build src/Core.CSharp.ZetaId — 0 warnings, 0 errors
  dotnet test tests/Tests.CSharp --filter CrossVerify — Passed 1/1, 7s
  tests/cross-verification/zeta-id/cs-output.json — 12/12 matchesExpected: true

Substrate landed (16 production files + 3 test files + 2 backlog rows):

src/Core.CSharp.ZetaId/:
  - BitLayout.cs            — TopDown/BottomUp strategy w/ reserved-bit gap
  - LayoutDirection.cs      — TopDown / BottomUp enum
  - IdVersion.cs            — V1 = 1
  - Chromosome.cs           — MetaCoherence / FinancialIntegrity
  - Category.cs             — Observation/Emission/Workflow/Heartbeat
  - Firefly.cs              — NoDirective
  - Persona.cs              — HumanMaintainer (role-ref per #4519) /
                               FireflyCoherence
  - Location.cs             — 11-value list across AWS/GCP/Azure/DigitalOcean
                               (EastUsVa / WestUsOr / CentralUs / CanadaToronto /
                               WestEurope / NorthEurope / SoutheastAsiaSg /
                               NortheastAsiaTk / AustraliaSyd / SouthAmericaSp /
                               MultiRegion); registry/locations.yaml + provider
                               mapping + CloudEvents envelope all backlogged
  - AuthorityValue.cs       — byte enum (31/20/15/8/3 — matches F# AuthorityValue)
  - MomentumValue.cs        — byte enum (32/96/160/224/248)
  - Authority.cs            — abstract record + sealed nested records + Raw(byte)
                               escape; ToByte/FromByte zero-allocation conversion
  - Momentum.cs             — same shape as Authority
  - ZetaObservation.cs      — readonly record struct, 9 fields
  - ISimulationEnvironment.cs — abstract NextInt64() contract
  - DeterministicEnv.cs     — opt-in env for cross-verify ONLY (NextInt64 → 0)
  - ZetaIdCodec.cs          — Pack(obs, env) requires env; Unpack(id) inverse;
                               throws on nothing (Raw is the escape)
  - Zeta.Core.CSharp.ZetaId.csproj — net10.0, zero external deps, in Zeta.sln

tests/Tests.CSharp/ZetaId/:
  - CrossVerifyTests.cs     — xUnit + RepoRoot walk via Zeta.sln sentinel;
                               writes cs-output.json
  - FlatVector.cs           — YamlDotNet flat DTO with [YamlMember Alias=]
  - VectorEnvelope.cs       — top-level YAML schema

Modified:
  - Directory.Packages.props — YamlDotNet 17.1.0 added (CPM)
  - tests/Tests.CSharp/Tests.CSharp.csproj — ProjectReference to new csproj +
                                              YamlDotNet PackageReference
  - Zeta.sln — new csproj added via dotnet sln add
  - docs/BACKLOG.md — regenerated

docs/backlog/P2/:
  - 081KS3X9Y0008QG0R001Z8SBZJ-zeta-id-rust-implementation-2026-05-21.md  — Rust as 4th peer oracle
  - 081KS3X9Y0008QG0R002WGH8PJ-zeta-id-python-implementation-2026-05-21.md — Python as 5th peer
                                                         oracle (depends on Rust
                                                         landing first)

Compile-and-test-first discipline catches (V8 lessons):

  1. Meziantou.Analyzer MA0049: namespace+class collision (Zeta.Core.CSharp.ZetaId.ZetaId)
     — fixed by renaming class to ZetaIdCodec
  2. MA0048: file-per-type — split ZetaObservation.cs (7 enums + record) into
     8 separate files
  3. CA1707: underscores in names — Location values renamed (EastUS_VA1 →
     EastUsVa, etc.); test method renamed to camelCase
  4. CA1305/CA1869/CA2201/MA0006/MA0002/MA0016: invariant-culture, cached
     JsonOptions, InvalidOperationException, StringComparison.Ordinal,
     StringComparer.Ordinal, MA0016 pragma-suppressed for YamlDotNet
     concrete-collection requirement
  5. MA0026: literal "TODO" comments — rephrased to "Backlog (Aaron 2026-05-21):"
  6. Path resolution: RepoRoot() walks up looking for Zeta.sln (not .git
     which is a file in worktrees, not directory)
  7. ** PACK SHIFT BUG ** : Mika's V8 CreateTopDown packed contiguously
     without the spec's reserved 1-bit gap at offset 69 (between Chromosome
     and Category). All 12 vectors mismatched by exactly 1-bit cumulative
     shift. Empirical catch — visual review missed it both rounds. Fix:
     explicit Skip(1) in CreateTopDown after Chromosome.

Per Aaron 2026-05-21 explicit grant: "I grant full permission and commit
why not it's just code we can roll forward or back." Multi-oracle ethos
per .claude/rules/m-acc-multi-oracle-end-user-moral-invariants.md — no
single language is source of truth; cross-verify is the contract.

Composes with 081KRW63S0008QG0R002KC5DSR / 081KRW63S0008QG0R00088FYE9 / 081KRW63S0008QG0R002ZRNDJ8 / 081KRW63S0008QG0R002YAA09X / 081KRW63S0008QG0R001SAHYKV (Agora V6
substrate), PR #4517 (V1 ZetaId spec + TS oracle), PR #4519 (review
fixes), 081KS3X9Y0008QG0R001Z8SBZJ/081KS3X9Y0008QG0R002WGH8PJ (Rust + Python follow-ups).

Co-Authored-By: Mika <noreply@x.ai>
Co-Authored-By: Claude <noreply@anthropic.com>
```

### `74b87730f37bc2234e541948b7683e420f2b560d` -- 2026-05-21T10:11:32Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `src/Core.CSharp.ZetaId/Authority.cs`
- `src/Core.CSharp.ZetaId/BitLayout.cs`
- `src/Core.CSharp.ZetaId/Momentum.cs`
- `tests/Tests.CSharp/ZetaId/CrossVerifyTests.cs`

**Message:**

```
fix(zeta-id): address 6 Codex+CodeQL review findings on PR #4522

3 classes of finding from PR #4522 reviewers, all real:

1. CodeQL (3 findings — CrossVerifyTests.cs:65, 74, 100): Path.Combine
   multi-arg silent-drop when a later segment is rooted. Switched to
   Path.Combine(root, Path.Join("tests", ..., "...yaml")) pattern which
   builds the relative tail deterministically without rooted-ambiguity.
   Same fix at line 65 (RepoRoot).

2. Codex P1 (Authority.cs:20 + Momentum.cs:20): public abstract record
   with `_ => 0` fallback silently encoded unknown subtypes as byte 0,
   causing data corruption + ID collisions across distinct unknown
   subtypes (all unpack as Raw(0)). External code could legitimately
   create derived subtypes since the base is public abstract. Fix:
   throw InvalidOperationException with explicit message pointing at
   Authority.Raw(byte) / Momentum.Raw(byte) as the canonical escape.

3. Codex P2 (BitLayout.cs:107): CreateBottomUp ctor args were positionally
   mis-mapped — `(0, 32), Next(8), Next(8), ...` was assigning Randomness
   to Version slot, Location to Timestamp slot, etc. — completely wrong
   for anyone calling LayoutDirection.BottomUp. Same shape as the F# V8
   pipe-direction bug. Fix: compute bottom-up but assign to named locals
   (randomness, location, momentum, ... version) THEN pass into ctor in
   canonical (version, timestamp, chromosome, ...) order. Also added the
   matching Skip(3) for reserved bits 32-34 + Skip(1) for reserved bit 69
   that CreateTopDown already has.

Verified: dotnet test still passes 1/1 with 12/12 matchesExpected: true
after the fixes (Path.Join+Path.Combine produces identical paths;
Authority/Momentum throw only on subtypes that don't exist in vectors.yaml
so cross-verify is unaffected; CreateBottomUp not exercised by current
test but now produces canonical offsets).

Co-Authored-By: Claude <noreply@anthropic.com>
```

### `9594bd5660b14458b88be704d385451921368ef9` -- 2026-05-21T10:22:22Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `src/Core.CSharp.ZetaId/Location.cs`
- `src/Core.CSharp.ZetaId/Zeta.Core.CSharp.ZetaId.csproj`

**Message:**

```
fix(zeta-id): address 2 more Copilot review findings on PR #4522

Two real findings on the post-74b87730 review pass:

1. Copilot P1 (Location.cs:12) — comment contained personal name "Aaron"
   in a production code file. Per .github/copilot-instructions.md
   "No name attribution in code": renamed comment author from "Aaron" to
   "human maintainer" (role-ref). Same convention as registry/personas.yaml
   `HumanMaintainer` enum value.

2. Copilot P1 (Zeta.Core.CSharp.ZetaId.csproj:7) — LangVersion=preview
   overrode repo-wide `latest` (from Directory.Build.props) without a
   specific preview-only feature need. Removed; build still clean
   (0 warnings, 0 errors) with repo default.

Other 3 review threads (BitLayout BottomUp + Authority/Momentum silent-zero)
were filed at 10:06:34Z BEFORE my 74b87730 push reached origin. Already
fixed in that commit; resolving no-op via thread replies pointing at the fix.

Co-Authored-By: Claude <noreply@anthropic.com>
```

### `28368c7c31fc0a80be7482e8fe49bdbc5a604980` -- 2026-05-21T10:23:20Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tests/Tests.CSharp/ZetaId/CrossVerifyTests.cs`

**Message:**

```
fix(zeta-id): CodeQL Path.Combine→Path.Join refactor (final 2 threads…

… on PR #4522)

CodeQL re-flagged Path.Combine(root, Path.Join(...)) after 74b87730
because static analysis can't prove the inner Path.Join result isn't
rooted. Switched to single-call Path.Join(root, "tests", ..., "...yaml")
which always concatenates segments with separator (never silently
drops earlier args). Cleaner pattern; CodeQL happy.

Co-Authored-By: Claude <noreply@anthropic.com>
```

### `e4d9ad4d02e42f9a733e371ece34015695462bb4` -- 2026-05-21T10:53:51Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `docs/BACKLOG.md`
- `src/Core.CSharp.ZetaId/Authority.cs`
- `src/Core.CSharp.ZetaId/ZetaIdCodec.cs`
- `tests/Tests.CSharp/ZetaId/CrossVerifyTests.cs`

**Message:**

```
fix(zeta-id): bounds-check Authority.Raw + Timestamp + drop unused us…

…ing + file v2 backlog rows

Three more Copilot/Codex review findings on PR #4522 + 3 ZetaId v2
backlog rows from the 2026-05-21 Kestrel sharpening conversation.

Build fixes (Copilot P0 + Codex P1 x2):

1. CrossVerifyTests.cs:1 (Copilot P0) — removed unused
   `using System.Collections.ObjectModel;`. CS8019 with
   TreatWarningsAsErrors. Build was breaking.

2. Authority.cs:14 (Codex P1) — Authority.Raw(byte) accepted 0..255
   but Authority is packed into 5-bit field; values 32..255 would
   silently truncate (e.g. Raw(255) → encodes as 31 → unpacks as
   HumanVerified, irreversible data corruption + collision). Fix:
   Raw constructor throws ArgumentOutOfRangeException on Value > 31.
   Momentum.Raw(byte) is fine — packed into 8-bit field, full 0..255
   range valid.

3. ZetaIdCodec.cs:12 (Codex P2) — Timestamp written directly into
   48-bit slot with no bounds check; out-of-range long values
   silently truncated (281474976710656 → 0; -1 → 281474976710655).
   Fix: Pack throws ArgumentOutOfRangeException if
   obs.Timestamp < 0 || obs.Timestamp > (2^48 - 1).

Verified: dotnet test still Passed 1/1, 8s after fixes
(InvariantCulture ToString + Path.Join + DeterministicEnv + the
Authority/Timestamp bounds + the unused-using removal all clean).

V2 backlog rows from Kestrel review (preserved separately):

  081KS3X9Y0008QG0R003044PQQ — ZetaId v2 spec hardening (entropy budget gap via node-ID
           in Location, HLC monotonicity, Firefly bit drop, DRAFT
           vocabulary marker, Authority/Momentum spacing doc,
           Location dual-mode discriminator)
  081KS3X9Y0008QG0R000W00V73 — ZetaId canonical string encoding (Crockford base32 for
           git-filename use), endianness (big-endian wire),
           bit-numbering (LSB-0)
  081KS3X9Y0008QG0R0006MQXA4 — Tier-deferred causality worked example (Z-sets indexed
           by tier protocol; potentially publishable per Kestrel)

All P2; don't block V1 substrate landing. Implementations wait until
existing C# integration (this PR) + F# V9 + smart deser TS prototype
all land.

docs/BACKLOG.md regenerated.

Co-Authored-By: Claude <noreply@anthropic.com>
```

### `b5e20dda7669ddbd7703188162525a52220229eb` -- 2026-05-21T11:04:42Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/hygiene/audit-tick-shard-relative-paths.baseline.json`

**Message:**

```
fix(ci): markdownlint MD007/MD018 in 081KS3X9Y0008QG0R003044PQQ + grandfather 2 new pre-e…

…xisting broken paths in 0822Z shard

Two required-check failures on PR #4522 after e4d9ad4d:

1. markdownlint (081KS3X9Y0008QG0R003044PQQ-zeta-id-v2-spec-hardening): 3x MD007 ul-indent
   (expected 0, got 2-space indent) + 1x MD018 no-space-after-hash
   (`#1 entropy` parsed as ATX heading). Fixed: unindented the bullets
   + rephrased "#1 entropy" → "the entropy gap in section 1".

2. tick-shard relative-paths: 2 NEW broken paths in
   docs/hygiene-history/ticks/2026/05/21/0822Z.md (line 27 + line 57)
   — pre-existing breakage in a peer shard that landed on main while
   this branch was open. Added both to the baseline (now 41 grandfathered
   entries). Same pattern as #4519's fix-up.

Co-Authored-By: Claude <noreply@anthropic.com>
```

### `a9c113ddffac7c3df605977e08c350a5faf4e56b` -- 2026-05-21T11:15:33Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `src/Core.CSharp.ZetaId/ZetaIdCodec.cs`

**Message:**

```
fix(zeta-id): validate narrow enum fields before bit-packing (Codex P…

…2 on PR #4522)

Codex flag: ZetaIdCodec.Pack cast Version (5b), Chromosome (5b), Category
(4b), Firefly (1b) directly to byte then SetBits-masked. C# allows
out-of-range enum casts at compile-time (e.g. `(Category)31`); high bits
silently truncate, different inputs collide, ID unpacks to wrong value.

Fix: ValidateEnumField(value, widthBits, fieldName) helper throws
ArgumentOutOfRangeException if value > (1<<widthBits)-1. Called for
Version/Chromosome/Category/Firefly. Persona and Location are
byte-backed (8b) so they max at 255 = field width; no check needed.

Verified: dotnet test still Passed 1/1 after the validation (canonical
12-vector inputs are all in-range; no behavior change for the test).

Co-Authored-By: Claude <noreply@anthropic.com>
```

### `4af6ecaf952527f5a66bcebb73d1f0d98bb83851` -- 2026-05-21T11:18:25Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `docs/BACKLOG.md`

**Message:**

```
docs(backlog): file 081KS3X9Y0008QG0R003MMEAC7 — clock-protocol negotiation stack sequenc…

…e diagram artifact (Kestrel sharpening 2026-05-21)

P2-S row for the one-page sequence diagram artifact that demonstrates
end-to-end coherence of the Orleans + SPIFFE/SPIRE + OPA + Reticulum +
DBSP stack via one operation (actor A on node X invokes actor B on
node Y across a trust boundary).

Per Aaron 2026-05-21: "smaller refined chunks" — links existing
related rows (081KQ3HBZ0008QG0R000RP1WDN actor-register lens, 081KQZVQW0008QG0R000PPQ3MH Orleans durable stack,
081KQZVQW0008QG0R000W4B8KT Orleans interloop, 081KQZVQW0008QG0R000JJVA4E posterior quorum, 081KRYRGG0008QG0R0031EYYE4 V8 Sequoia
hierarchy, 081KS3X9Y0008QG0R0006MQXA4 tier-deferred causality) rather than re-spec'ing.

Lead framing: capability-negotiation-replacing-ref-counting (E lang /
CapnProto / KeyKOS lineage), with trust-gradient as extension.
Discriminator: if diagram draws cleanly, broader publishable v2
architecture earns own row.

docs/BACKLOG.md regenerated.

Co-Authored-By: Claude <noreply@anthropic.com>
```

### `cb21e72f91f23711539eb5af20320a9a6fba9a8c` -- 2026-05-21T11:38:04Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `Zeta.sln`
- `src/Core.CSharp.ZetaId/Location.cs`
- `src/Core.CSharp.ZetaId/ZetaIdCodec.cs`
- `tests/Tests.CSharp/ZetaId/CrossVerifyTests.cs`

**Message:**

```
fix(zeta-id): address 7 Codex+Copilot review findings on PR #4522 (po…

…st-bounds-check pass)

Round 7 of reviewer iteration on PR #4522. All findings verified clean:

Codex P2:
1. ZetaIdCodec.cs:35 — null check for env (NRE-prevention)
2. ZetaIdCodec.cs:35 — null check for obs.Authority + obs.Momentum
   (record struct default-init has null abstract record members)

Copilot:
3. Location.cs:12 — CloudEvents URL fixed (cncf.io/cloudevents →
   https://cloudevents.io/)
4. CrossVerifyTests.cs:18 — CheckByte(int, fieldName) helper added.
   v.AuthorityRaw is int? cast to byte; 256 would wrap to 0 BEFORE
   Authority.Raw's 0..31 validation. Now throws InvalidOperationException
   on out-of-range int→byte cast.
5. CrossVerifyTests.cs:56 — same CheckByte applied to all int→byte-enum
   casts (Version/Chromosome/Category/Firefly/Persona/Location). Uses
   nameof(v.Field) per Aaron 2026-05-21 ("all these strings are ugly")
   for strongly-typed field references.
6. ZetaIdCodec.cs:14 — ArgumentOutOfRangeException paramName was
   "Timestamp" (not a method parameter — CA2208/MA0015 caught it).
   Reverted to nameof(obs) with descriptive "ZetaObservation.Timestamp..."
   in message text. Same fix for null checks (now throw ArgumentException
   with nameof(obs) paramName instead of ThrowIfNull(obs.Authority)).
7. Zeta.sln:42 — new project entry used legacy C# project type GUID
   FAE04EC0-...; switched to modern SDK-style GUID 9A19103F-... matching
   sibling projects (Tests.CSharp, Core.CSharp).

Filed 081KS3X9Y0008QG0R003MMEAC7 (clock-protocol negotiation stack end-to-end sequence
diagram artifact — Kestrel sharpening 2026-05-21) per Aaron's "smaller
refined chunks" framing. Links existing related rows (081KQ3HBZ0008QG0R000RP1WDN / 081KQZVQW0008QG0R000PPQ3MH /
081KQZVQW0008QG0R000W4B8KT / 081KQZVQW0008QG0R000JJVA4E / 081KRYRGG0008QG0R0031EYYE4 / 081KS3X9Y0008QG0R0006MQXA4) rather than re-spec'ing.

Verified: dotnet test Passed 1/1, 3s after all 7 fixes + 081KS3X9Y0008QG0R003MMEAC7 row.

Co-Authored-By: Claude <noreply@anthropic.com>
```

### `b114571ad16c96777fe4fd71b06973e7b4202caa` -- 2026-05-21T11:50:55Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `src/Core.CSharp.ZetaId/Authority.cs`
- `src/Core.CSharp.ZetaId/Momentum.cs`

**Message:**

```
fix(zeta-id): reject Authority.Raw / Momentum.Raw values that alias n…

…amed cases (Codex P2)

Two Codex P2 findings on PR #4522 (post-cb21e72f):

1. Authority.Raw(31) silently aliases HumanVerified — Pack writes 31,
   Unpack canonicalizes back to Authority.HumanVerified (not Raw(31)).
   Round-trip instability. Same for 20/15/8/3.

2. Momentum.Raw(96) silently aliases Normal. Same pattern for
   32/96/160/224/248.

Fix: Raw constructor throws ArgumentOutOfRangeException when Value
matches any named-case byte value. Forces callers to use the named
case directly (semantically explicit) or pick a non-aliased byte
for genuine extension.

Verified: dotnet test Passed 1/1, 2s — canonical 12-vector inputs
don't exercise the Raw escape with named-aliased values, so the new
check is a defense against future misuse, not a behavior change for
the test.

Co-Authored-By: Claude <noreply@anthropic.com>
```
