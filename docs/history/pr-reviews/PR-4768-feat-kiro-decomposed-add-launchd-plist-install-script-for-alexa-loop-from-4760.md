# PR #4768 -- feat(kiro): DECOMPOSED - add launchd plist + install script for Alexa loop (from #4760)

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 4768 |
| Title | feat(kiro): DECOMPOSED - add launchd plist + install script for Alexa loop (from #4760) |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-05-23T21:05:52Z |
| Merged at | 2026-05-25T11:05:30Z |
| Merge commit SHA | `daea1037a446ea161515d62618e27bfb1108d457` |
| Branch | `lior/decompose-4760-kiro-launchd` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/4768 |
| Changed files | 3 |
| Additions / deletions | +102 / -0 |

## Description

Decomposed from the blob PR #4760. This PR isolates the Kiro launchd plist and install scripts as per Maji antigravity protocol.

Addresses: 081KRSKQ20008QG0R00367828S

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | true |
| Total threads | 17 |
| Resolved threads | 17 |
| Unresolved threads | 0 |
| Total review comments | 17 |
| Total fix commits (touching thread paths) | 5 |

## Review threads

### Thread 1 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6EVctm`
- **Path**: `tools/kiro/launchd/com.lucent.zeta.kiro-loop.plist`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-23T21:07:38Z on `tools/kiro/launchd/com.lucent.zeta.kiro-loop.plist`:11 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Replace user-specific paths in launchd plist**

  The LaunchAgent hardcodes `/Users/acehack` paths for the bun binary, repo root, HOME, and log files, so `tools/kiro/launchd/install.sh` will install successfully but the service will fail on any machine where those exact paths do not exist (different username, checkout path, or bun install path). This makes the new installer non-portable and breaks the loop for every non-acehack environment.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 2 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6EVc8s`
- **Path**: `tools/kiro/launchd/com.lucent.zeta.kiro-loop.plist`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-23T21:08:51Z on `tools/kiro/launchd/com.lucent.zeta.kiro-loop.plist`:34 (association: NONE)

  <pre>
  P0: This plist hard-codes a specific user’s bun path and repo path (/Users/acehack, bun/1.3). That makes the LaunchAgent non-portable and will fail for any other checkout location or bun install. Follow the repo’s launchd convention of shipping plists as templates with placeholders (e.g., {{BUN_PATH}}, {{REPO_ROOT}}) and substitute them during install (see tools/shadow/launchd/com.zeta.shadow-observer.plist + install-launchagent.ts).

  </pre>

### Thread 3 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6EVc8x`
- **Path**: `tools/kiro/launchd/com.lucent.zeta.kiro-loop.plist`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-23T21:08:51Z on `tools/kiro/launchd/com.lucent.zeta.kiro-loop.plist`:34 (association: NONE)

  <pre>
  P0: Log + working-directory paths are hard-coded to /Users/acehack and don’t match the install.sh-created $LOG_DIR/$STATE_DIR. This will write logs to the wrong place (or fail) for other users. Prefer using template placeholders and/or environment variables (e.g., ZETA_KIRO_LOOP_LOG_DIR / _STATE_DIR / _WORKTREE) so install.sh controls the actual locations.

  </pre>

### Thread 4 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6EVc8z`
- **Path**: `tools/kiro/launchd/com.lucent.zeta.kiro-loop.plist`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-23T21:08:52Z on `tools/kiro/launchd/com.lucent.zeta.kiro-loop.plist`:6 (association: NONE)

  <pre>
  P1: Label prefix "com.lucent.zeta.*" is inconsistent with other launchd services in this repo (e.g., com.zeta.shadow-observer, com.zeta.claude-loop-*). Unless there’s a specific need for the extra segment, align the label + filename to the established com.zeta.* namespace to keep service discovery/cleanup consistent.

  </pre>

### Thread 5 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6EVc82`
- **Path**: `tools/kiro/launchd/com.lucent.zeta.kiro-loop.plist`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-23T21:08:52Z on `tools/kiro/launchd/com.lucent.zeta.kiro-loop.plist`:27 (association: NONE)

  <pre>
  P1: This plist enables ZETA_KIRO_LOOP_FORWARD_ACTIONS=1 by default, which (per the tick script) can arm auto-merge on CLEAN PRs. That’s a high-impact behavior to ship as the default for an install script. Consider defaulting this to disabled (or wiring it to an explicit installer flag / documented opt-in) so a first-time install can’t unexpectedly start merging PRs.

  </pre>

### Thread 6 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6EVc89`
- **Path**: `tools/kiro/launchd/install.sh`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-23T21:08:52Z on `tools/kiro/launchd/install.sh`:16 (association: NONE)

  <pre>
  P0: install.sh currently just copies the plist verbatim, but the plist is machine-specific (hard-coded HOME/repo/bun/log paths). The install script should be responsible for rendering a portable template (placeholders → actual paths), validating it with plutil -lint, and then writing to ~/Library/LaunchAgents (pattern already exists in tools/shadow/launchd/install-launchagent.ts).

  </pre>

### Thread 7 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6EVc8_`
- **Path**: `tools/kiro/launchd/install.sh`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-23T21:08:52Z on `tools/kiro/launchd/install.sh`:4 (association: NONE)

  <pre>
  P2: Most repo shell scripts use `set -euo pipefail`; this script uses only `set -e`, which won’t catch unset variables and can mask failures in pipelines if any are added later. Consider switching to `set -euo pipefail` for consistency with tools/setup/*.sh and tools/kiro/kiro-loop-wrapper.sh.

  </pre>

### Thread 8 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6EVc9A`
- **Path**: `tools/kiro/kiro-loop-wrapper.sh`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-23T21:08:52Z on `tools/kiro/kiro-loop-wrapper.sh`:25 (association: NONE)

  <pre>
  P1: tools/kiro/kiro-loop-wrapper.sh isn’t referenced by the plist or install script (ProgramArguments directly invoke bun + kiro-loop-tick.ts). If the wrapper is intended to provide PATH/shellenv setup for launchd, wire the plist to call it (with absolute paths) and/or pass the needed env vars in the plist; otherwise consider removing the wrapper to avoid dead substrate.
  </pre>

### Thread 9 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6EZPmi`
- **Path**: `tools/kiro/launchd/install.sh`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-24T14:21:37Z on `tools/kiro/launchd/install.sh`:6 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Point installer at committed plist template**

  `install.sh` references `com.google.zeta.kiro-loop.plist`, but this commit only adds `tools/kiro/launchd/com.lucent.zeta.kiro-loop.plist`. On a fresh checkout, `sed ... "$PLIST_SRC"` fails with “No such file or directory”, so the launch agent never gets installed. This makes the new install path unusable unless users manually rename files first.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 10 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6EZPx6`
- **Path**: `tools/kiro/launchd/install.sh`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-24T14:22:25Z on `tools/kiro/launchd/install.sh`:7 (association: NONE)

  <pre>
  P0: install.sh references a plist filename that does not exist in this PR (com.google.zeta.kiro-loop.plist) and will fail when it tries to read/copy it. Update PLIST_SRC/PLIST_DST_NAME to match the actual plist file/label shipped (tools/kiro/launchd/com.lucent.zeta.kiro-loop.plist) so the installer works.

  </pre>

### Thread 11 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6EZPyA`
- **Path**: `tools/kiro/launchd/install.sh`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-24T14:22:26Z on `tools/kiro/launchd/install.sh`:16 (association: NONE)

  <pre>
  P0: `launchctl bootout "gui/$(id -u)/$PLIST_DST_NAME"` uses the plist *filename* (with .plist) as the service target; `launchctl bootout gui/$uid/$label` expects the Label (no .plist), or you can pass the domain plus the plist path. With the current value this likely won’t unload the running job on reinstall.
  </pre>

### Thread 12 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6EZPyH`
- **Path**: `tools/kiro/launchd/install.sh`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-24T14:22:26Z on `tools/kiro/launchd/install.sh`:19 (association: NONE)

  <pre>
  P1: The `sed` placeholder substitution is not safe for `$HOME` containing characters like `&amp;`, `|`, `\`, or newlines, and can silently generate an invalid plist. The repo already ships a safe placeholder substitution pattern for launchd templates (tools/shadow/launchd/install-launchagent.ts + README) that avoids shell metacharacter issues; consider reusing that approach here (string replacement in a script, plus leftover-placeholder detection).

  </pre>

### Thread 13 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6EZPyM`
- **Path**: `tools/kiro/launchd/com.lucent.zeta.kiro-loop.plist`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-24T14:22:26Z on `tools/kiro/launchd/com.lucent.zeta.kiro-loop.plist`:6 (association: NONE)

  <pre>
  P0: The plist `Label` is `com.google.zeta.kiro-loop`, but the file is named `com.lucent.zeta.kiro-loop.plist` and the installer is expected to target that label. The label/name mismatch will break `launchctl print/bootout` workflows and makes the service hard to manage; align the Label with the intended domain/name.

  </pre>

### Thread 14 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6EZPyO`
- **Path**: `tools/kiro/launchd/com.lucent.zeta.kiro-loop.plist`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-24T14:22:26Z on `tools/kiro/launchd/com.lucent.zeta.kiro-loop.plist`:25 (association: NONE)

  <pre>
  P1: ProgramArguments and WorkingDirectory hard-code a specific mise bun install path (bun/1.3/...) and a specific checkout location under `Documents/src/repos/Zeta`. This will break on machines with different bun versions or repo locations; follow the existing launchd-template convention (e.g., `{{BUN_PATH}}` and `{{REPO_ROOT}}` placeholders rendered by an installer) or invoke a wrapper script that resolves bun + repo root dynamically.

  </pre>

### Thread 15 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6EZPyV`
- **Path**: `tools/kiro/launchd/com.lucent.zeta.kiro-loop.plist`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-24T14:22:27Z on `tools/kiro/launchd/com.lucent.zeta.kiro-loop.plist`:24 (association: NONE)

  <pre>
  P1: The PATH in the plist is also pinned to a specific bun install directory (bun/1.3/...) and includes `__USER_HOME__` substitution. Since a wrapper script already exists (tools/kiro/kiro-loop-wrapper.sh) to source `~/.config/zeta/shellenv.sh` and set PATH, consider removing most of this PATH management from the plist and launching the wrapper instead (or ensure PATH is derived from the same single source of truth as the rest of the repo).
  </pre>

### Thread 16 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6EZPyb`
- **Path**: `tools/kiro/kiro-loop-wrapper.sh`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-24T14:22:27Z on `tools/kiro/kiro-loop-wrapper.sh`:25 (association: NONE)

  <pre>
  P1: tools/kiro/kiro-loop-wrapper.sh is added as a “launchd entry point” but the new plist does not use it (it invokes bun directly). Either update the plist to call the wrapper (so shellenv/PATH/repo-root resolution is centralized) or drop the wrapper to avoid dead/unused substrate.
  </pre>

### Thread 17 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6EgYQO`
- **Path**: `tools/kiro/kiro-loop-wrapper.sh`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-25T09:33:27Z on `tools/kiro/kiro-loop-wrapper.sh`:25 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Export loop worktree before invoking tick runner**

  This wrapper resolves and `cd`s into the checked-out repo, but it never sets `ZETA_KIRO_LOOP_WORKTREE` before executing `kiro-loop-tick.ts`; that tick script uses `process.env.ZETA_KIRO_LOOP_WORKTREE` and otherwise falls back to a user-specific default (`$HOME/Documents/src/repos/Zeta`) for all `git`/`gh` subprocess `cwd`s. On any machine where the repo is installed elsewhere, the launchd service starts but runs commands against a non-existent/wrong path, so the autonomous loop cannot operate correctly.

  Useful? React with 👍 / 👎.
  </pre>

## Fix commits (touching thread paths)

### `9a7824715234c498c0ad0e6e53fc37bf885a3a76` -- 2026-05-23T21:05:08Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/kiro/kiro-loop-wrapper.sh`
- `tools/kiro/launchd/com.lucent.zeta.kiro-loop.plist`
- `tools/kiro/launchd/install.sh`

**Message:**

```
feat(kiro): DECOMPOSED - add launchd plist + install script for Alexa…

… loop (from #4760)
```

### `e391b30783e093d26d86c075428881955ce594ad` -- 2026-05-24T01:10:16Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/kiro/kiro-loop-wrapper.sh`

**Message:**

```
fix(kiro): resolve shellcheck warning in wrapper script
```

### `7f489f5b9f0c7a34d0ef798ea5fd98e6f16e196c` -- 2026-05-24T14:19:16Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/kiro/launchd/com.lucent.zeta.kiro-loop.plist`
- `tools/kiro/launchd/install.sh`

**Message:**

```
fix(kiro): address review comments on launchd script
```

### `48a90465226c3de477e328297791624bb1513367` -- 2026-05-25T09:31:07Z -- `claude`

**Touched files (intersect with thread paths):**

- `tools/kiro/launchd/com.lucent.zeta.kiro-loop.plist`
- `tools/kiro/launchd/install.sh`

**Message:**

```
fix(kiro-launchd): align Label/filename + use wrapper + safe substitu…

…tion

Addresses unresolved review threads on PR #4768:

P0 — Label/filename/installer alignment
- Plist Label now `com.lucent.zeta.kiro-loop` (matches filename)
- install.sh references the correct plist filename
- `launchctl bootout` uses the Label, not the filename

P1 — Route plist through the existing wrapper script
- ProgramArguments now invoke `kiro-loop-wrapper.sh` (which already
  resolves PATH and repo root from $SCRIPT_DIR/../..)
- Removes hard-coded `bun/1.3` install path and pinned PATH entry
- install.sh substitutes `__REPO_ROOT__` computed from its own
  location, so the plist is portable across checkout locations

P1 — Safer placeholder substitution
- Replaces unsafe `sed` with python literal-string `.replace()` for
  `__USER_HOME__` and `__REPO_ROOT__` (handles `&`, `|`, `\\`,
  newlines in expansion values)

P2 — Stricter shell defaults
- install.sh: `set -e` → `set -euo pipefail`

Co-Authored-By: Claude <noreply@anthropic.com>
```

### `75fe1cc2fbcd5e57f98856eb63459d21dbafb6a5` -- 2026-05-25T11:02:37Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/kiro/kiro-loop-wrapper.sh`

**Message:**

```
fix(kiro/launchd): export ZETA_KIRO_LOOP_WORKTREE in wrapper.sh

The bash 'cd' in the wrapper does NOT propagate to Bun.spawn calls in
kiro-loop-tick.ts, which take explicit cwd: worktree from
process.env.ZETA_KIRO_LOOP_WORKTREE. Without this export the tick would
fall back to $HOME/Documents/src/repos/Zeta and break on machines with
different checkout locations.

Resolves chatgpt-codex-connector P1 finding on PR #4768.
```
