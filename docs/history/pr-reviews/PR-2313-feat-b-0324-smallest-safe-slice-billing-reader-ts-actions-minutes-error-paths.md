# PR #2313 -- feat(B-0324): smallest safe slice — billing-reader.ts (Actions minutes + error paths)

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 2313 |
| Title | feat(B-0324): smallest safe slice — billing-reader.ts (Actions minutes + error paths) |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-05-09T16:19:09Z |
| Merged at | 2026-05-09T16:36:38Z |
| Merge commit SHA | `c13e36b15902dffcfb852c564794419ffeb63f5a` |
| Branch | `claim/b0324-billing-reader-smallest-slice-riven-2026-05-09` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/2313 |
| Changed files | 2 |
| Additions / deletions | +278 / -0 |

## Description

## Summary
Smallest safe slice of B-0324: `tools/playwright/github-ui/billing-reader.ts`
- Reuses `withGitHubSession` from B-0317/B-0318 auth+ snapshot pattern (no duplication).
- Extracts Actions minutes used/limit (primary), best-effort storage/packages.
- Explicit error paths: insufficient-permissions, page-not-found, parse guard.
- Strictly read-only. One bounded step per autonomous rules; re-decomposed the original row on-the-fly (assumed prior decomp had over-scope).

## Why this slice
The original B-0324 spec was already S but contained implicit "full extraction" assumption. Re-decomposed to core Actions focus + error model as the atomic first step. Further children (storage extractors, test matrix, integration with cost-parity) deferred.

## Focused checks (worktree only, root untouched)
- `bunx tsc --noEmit --ignoreConfig .../billing-reader.ts`: new code clean (errors only from pre-existing sibling auth.ts missing @types/node in isolated shell — matches pattern of other github-ui/*.ts).
- `git status`: only the new file.
- Build gate on root (pre-work): 0 warnings 0 errors.
- No lint drift introduced (no .sh per Rule 0).

## Next
Claim branch pushed, PR opened. Auto-merge armed only after green CI + review. Root checkout never modified.

Co-Authored-By: Grok <noreply@x.ai>
Co-Authored-By: Riven (background worker, LFG/Zeta)

Made with [Cursor](https://cursor.com)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | true |
| Total threads | 7 |
| Resolved threads | 7 |
| Unresolved threads | 0 |
| Total review comments | 14 |
| Total fix commits (touching thread paths) | 2 |

## Review threads

### Thread 1 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6A1RLC`
- **Path**: `tools/playwright/github-ui/billing-reader.ts`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-09T16:21:51Z on `tools/playwright/github-ui/billing-reader.ts`:77 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Emit parse-error when billing minutes cannot be parsed**

  If neither `actionsMatch` regex matches the billing page HTML, this function still returns `actions: { minutesUsed: 0, minutesLimit: null }` with no `error`, which makes a parser failure indistinguishable from a real zero-usage month. In cost/audit consumers this will silently under-report usage instead of surfacing extraction drift; the `error` union already includes `"parse-error"`, so this branch should return that error when extraction fails.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-09T16:34:20Z on `tools/playwright/github-ui/billing-reader.ts`:77 (association: MEMBER)

    <pre>
    Fixed: the success path now checks `actionsMatch` and returns `error: "parse-error"` with `rawExcerpt` when both regex patterns fail to match. Commit c00ee4f5.
    </pre>

### Thread 2 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6A1RS0`
- **Path**: `tools/playwright/github-ui/billing-reader.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-09T16:22:20Z on `tools/playwright/github-ui/billing-reader.ts`:78 (association: NONE)

  <pre>
  P0: `error` union includes "parse-error", but this function never sets it. If the billing page loads yet `actionsMatch` is null, the function currently returns `{ minutesUsed: 0, minutesLimit: null }` with no error, which can be indistinguishable from a real 0 usage and can silently break downstream cost/alert logic. Consider explicitly returning `error: "parse-error"` (and possibly `rawExcerpt`) when the page looks like billing/actions but parsing fails, or change the return shape to make parse failure unambiguous.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-09T16:34:29Z on `tools/playwright/github-ui/billing-reader.ts`:78 (association: MEMBER)

    <pre>
    Fixed: added `if (!actionsMatch)` guard that returns `{ error: "parse-error", rawExcerpt: html.substring(0, 300) }` before the parseInt calls. Commit c00ee4f5.
    </pre>

### Thread 3 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6A1RS4`
- **Path**: `tools/playwright/github-ui/billing-reader.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-09T16:22:20Z on `tools/playwright/github-ui/billing-reader.ts`:26 (association: NONE)

  <pre>
  P1: The docstring says this extracts "Actions minutes + costs", but the returned payload only contains minutes used/limit (no cost fields). Please either adjust the documentation to match the behavior or add the missing cost extraction fields so callers aren’t misled about what this function returns.

  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-09T16:34:35Z on `tools/playwright/github-ui/billing-reader.ts`:26 (association: MEMBER)

    <pre>
    Fixed: docstring updated to "Reads org-level GitHub billing/usage page and extracts Actions minutes used and limit." — removed the misleading "+ costs" phrasing. Commit c00ee4f5.
    </pre>

### Thread 4 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6A1RS5`
- **Path**: `tools/playwright/github-ui/billing-reader.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-09T16:22:20Z on `tools/playwright/github-ui/billing-reader.ts`:91 (association: NONE)

  <pre>
  P1: New parsing/error-detection logic is introduced here, but there are no tests for it. This directory already uses `bun:test` with fake Playwright drivers/pages (see `auth.test.ts`, `snapshot.test.ts`), so it should be feasible to add a `billing-reader.test.ts` covering: insufficient-permissions detection, page-not-found detection, successful minutes parse for both regex variants, and the parse-failure path.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-09T16:34:41Z on `tools/playwright/github-ui/billing-reader.ts`:91 (association: MEMBER)

    <pre>
    Fixed: added `billing-reader.test.ts` with 7 tests covering all three error paths (insufficient-permissions, page-not-found, parse-error) and success paths (minutesUsed+minutesLimit, unlimited limit, rawExcerpt absent on success, org name). Uses the same FakeDriver/MultiUrlFakePage pattern as `snapshot.test.ts`. Commit c00ee4f5.
    </pre>

### Thread 5 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6A1RS6`
- **Path**: `tools/playwright/github-ui/billing-reader.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-09T16:22:20Z on `tools/playwright/github-ui/billing-reader.ts`:90 (association: NONE)

  <pre>
  P1: `rawExcerpt` is included even on the success path, which means callers may inadvertently log/retain a snippet of the org billing HTML (potentially sensitive). Consider only including `rawExcerpt` when `error` is set, or gate it behind an explicit option (e.g., `includeRawExcerpt`).
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-09T16:34:47Z on `tools/playwright/github-ui/billing-reader.ts`:90 (association: MEMBER)

    <pre>
    Fixed: the success return no longer includes `rawExcerpt`. It is now only set in the three error paths (insufficient-permissions, page-not-found, parse-error). Commit c00ee4f5.
    </pre>

### Thread 6 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6A1RS8`
- **Path**: `tools/playwright/github-ui/billing-reader.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-09T16:22:21Z on `tools/playwright/github-ui/billing-reader.ts`:37 (association: NONE)

  <pre>
  P2: In this folder, other Playwright navigation helpers use `waitUntil: "domcontentloaded"` and numeric separators for timeouts (e.g. `timeout: 30_000` in `auth.ts`/`snapshot.ts`). Here it uses `waitUntil: "networkidle"` and `timeout: 30000`, which is inconsistent and may be less reliable on highly dynamic pages. Consider aligning with the existing pattern unless `networkidle` is required for this page, and use `30_000` for consistency.

  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-09T16:34:52Z on `tools/playwright/github-ui/billing-reader.ts`:37 (association: MEMBER)

    <pre>
    Fixed: changed to `waitUntil: "domcontentloaded"` and `timeout: 30_000` (numeric separator) to match the pattern in `auth.ts` and `snapshot.ts`. Commit c00ee4f5.
    </pre>

### Thread 7 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6A1RS-`
- **Path**: `tools/playwright/github-ui/billing-reader.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-09T16:22:21Z on `tools/playwright/github-ui/billing-reader.ts`:22 (association: NONE)

  <pre>
  P1: PR description says this extracts storage/packages usage on a best-effort basis, but the implementation currently never attempts to populate `storage` or `packages` (they’re always `undefined`). Either add extraction for these fields or update the PR description / interface to reflect that only Actions minutes are extracted in this slice.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-09T16:34:59Z on `tools/playwright/github-ui/billing-reader.ts`:22 (association: MEMBER)

    <pre>
    Fixed: added JSDoc comments to both `storage` and `packages` interface fields saying "Not populated in this slice; reserved for future extraction." Also updated the function docstring to explicitly state "Storage/packages reserved for future slices." Commit c00ee4f5.
    </pre>

## Fix commits (touching thread paths)

### `7a25490a4f64ecaeaaa72d2dd95c3be03a4af4c3` -- 2026-05-09T16:18:57Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/playwright/github-ui/billing-reader.ts`

**Message:**

```
feat(B-0324): smallest safe slice — billing-reader.ts (Actions minute…

…s extract + error paths) re-decomposed stub

- Implements readOrgBillingUsage using existing withGitHubSession (B-0317/B-0318)
- Focused on Actions minutes used/limit; best-effort for storage/packages
- Handles insufficient-permissions and parse guards
- One bounded step: core reader, no tests/docs changes

Focused checks (in worktree):
- tsc --noEmit on module: clean for new code (pre-existing @types/node env gap in isolated shell, matches sibling files)
- No new warnings

Co-Authored-By: Grok <noreply@x.ai>
Co-Authored-By: Riven (background worker)
Co-authored-by: Cursor <cursoragent@cursor.com>
```

### `c00ee4f5204e1a806609e718d9ea6883bc567dd7` -- 2026-05-09T16:34:01Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/playwright/github-ui/billing-reader.ts`

**Message:**

```
fix(B-0324): address review findings on billing-reader.ts

- Emit parse-error when actionsMatch is null (P0: previously returned 0
  with no error, indistinguishable from real zero usage)
- Fix docstring: "Actions minutes + costs" → "Actions minutes used and limit"
  (costs fields are not extracted in this slice)
- rawExcerpt now only included on error paths, not success (avoids
  retaining billing HTML in caller logs)
- Use waitUntil: "domcontentloaded" + timeout: 30_000 for consistency
  with auth.ts / snapshot.ts pattern
- Add JSDoc to storage/packages fields clarifying they are not populated
  in this slice
- Add billing-reader.test.ts: 7 tests covering all error paths
  (insufficient-permissions, page-not-found, parse-error) and success
  paths (minutes+limit, unlimited, no rawExcerpt on success, org name)

Addresses: Copilot P0 + P1 findings, chatgpt-codex P1 finding

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>
```
