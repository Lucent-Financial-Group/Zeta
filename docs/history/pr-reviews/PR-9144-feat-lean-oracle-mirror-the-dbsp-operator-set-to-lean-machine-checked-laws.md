# PR #9144 -- feat(lean-oracle): mirror the DBSP operator set to Lean — machine-checked laws

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 9144 |
| Title | feat(lean-oracle): mirror the DBSP operator set to Lean — machine-checked laws |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-07-02T15:24:16Z |
| Merged at | 2026-07-02T15:24:27Z |
| Merge commit SHA | `3f73ceb9bf4c9547e7d3efb43a1bdeaf9df52581` |
| Branch | `otto/dbsp-operators-lean-oracle` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/9144 |
| Changed files | 2 |
| Additions / deletions | +131 / -0 |

## Description

Completes the oracle coverage: the DBSP operator set is now mirrored to the **Lean oracle**, where the laws are **proven** (Mathlib), not merely cross-verified (TS/Py/Go, #9137) or structurally grounded (Q#, #9142).

## What
`src/Core.Lean4/Lean4/DbspOperators.lean` (`namespace Zeta.DbspOperators`). A Z-set is `K → ℤ` (pointwise weights); the operator set with machine-checked theorems:

| Theorem | Law |
|---|---|
| `relay_id` | `relay = id` |
| `select_linear` | `select p (a+b) = select p a + select p b` |
| `join_bilinear_left` / `_right` | join distributes over add each side (bilinear) |
| `join_comm` | `join a b = join b a` |
| `integrate_assoc` / `integrate_comm` | integral fold assoc + comm (the algebraic root of DoP-invariance) |
| `retract_inverse` | `add a (neg a) = zero` (retractions cancel) |
| `distinctCross_no_cross` | the `H` boundary op emits 0 with no crossing |
| **`select_incremental`** | **`∑ select (δ i) = select (∑ δ i)`** — incremental ≡ recompute for a linear op, over any `Finset` |

## Verified
Registered in root `Lean4.lean`. Built locally: `lake build Lean4` → **2203 jobs, 0 errors, no `sorry`**; module warning-clean. `lean-proof.yml` (the Lean CI lane) covers it.

Now **every oracle surface** carries the DBSP operator set — cross-language IR, Q# reference, and Lean (machine-proven) — all over the same weight algebra. Complements the existing `DbspChainRule.lean`.

Anchor: Budiu et al. *DBSP* (VLDB 2023).

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 0 |
| Resolved threads | 0 |
| Unresolved threads | 0 |
| Total review comments | 0 |
| Total fix commits (touching thread paths) | 0 |

## Review threads

(no review threads on this PR)

## Fix commits (touching thread paths)

(no commits in this PR touched files anchored by review threads)
