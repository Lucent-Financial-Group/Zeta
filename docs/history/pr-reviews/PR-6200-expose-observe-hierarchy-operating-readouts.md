# PR #6200 -- Expose observe hierarchy operating readouts

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 6200 |
| Title | Expose observe hierarchy operating readouts |
| Author | `maximdolphin` (human) |
| State | MERGED |
| Created at | 2026-05-31T05:03:06Z |
| Merged at | 2026-05-31T14:37:01Z |
| Merge commit SHA | `959516e0238ed296d1540b5fba1f93afa6449cd5` |
| Branch | `docs/agentic-org-observability-lgtm-stack-2026-05-31` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/6200 |
| Changed files | 144 |
| Additions / deletions | +17257 / -225 |

## Description

## Summary

- Adds hierarchy operating readouts to `observe.ts` so executive, C-suite, director, TPM/manager, lead, and IC hats get scoped priority items, scoped metrics, and legal management actions.
- Extends hierarchy snapshots with work batches and work items so TPM workflows can prioritize initiative execution instead of only seeing projects/initiatives.
- Wires the operating readout through the agent CLI and observe-act worker lane, including JSON ingestion via `AGENTIC_ORG_HIERARCHY_JSON`.

## Workflow gaps covered

- Directors now see department initiative priority scope and legal actions such as priority decisions and staffing requests.
- TPMs now see initiative execution priority across work batches/work items plus meeting, scheduling, and escalation actions.
- CLI and worker tests cover the end-to-end observe surface so agents can consume the scoped workflow context.

## Test plan

- `npm run typecheck`
- `node --experimental-strip-types --test packages/application/test/observe.test.ts apps/agent-cli/test/agent-cli.test.ts apps/workers/test/org-cadence-lanes.test.ts`
- `npm test`

## Merge status

- Merged current `origin/main` into this branch before opening the PR.
- Checked existing PR #6192: still OPEN, `mergedAt: null`.


## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | true |
| Total threads | 5 |
| Resolved threads | 5 |
| Unresolved threads | 0 |
| Total review comments | 9 |
| Total fix commits (touching thread paths) | 7 |

## Review threads

### Thread 1 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F6roy`
- **Path**: `agentic-organization/apps/workers/src/org-cadence-lanes.ts`
- **Replies**: 1

**Initial comment:**

- **`github-code-quality[bot]` (bot)** at 2026-05-31T05:05:39Z on `agentic-organization/apps/workers/src/org-cadence-lanes.ts`:649 (association: NONE)

  <pre>
  ## Property access on null or undefined

  The base expression of this property access is always undefined.

  ---

  General fix: avoid property access on a value that may be &lt;code&gt;undefined&lt;/code&gt; at runtime. In TypeScript, this often means not dereferencing imported enum-like objects unless you are sure they are runtime values; use a stable literal accepted by the target API instead.&lt;/p&gt;
  &lt;p&gt;Best targeted fix here (without changing behavior): in &lt;code&gt;recordConformanceMetric&lt;/code&gt; within &lt;code&gt;agentic-organization/apps/workers/src/org-cadence-lanes.ts&lt;/code&gt;, replace &lt;code&gt;kind: TelemetryMetricKind.Gauge&lt;/code&gt; with the equivalent string literal (e.g., &lt;code&gt;"gauge"&lt;/code&gt;). This preserves the same metric semantics while removing the unsafe property access that can throw if &lt;code&gt;TelemetryMetricKind&lt;/code&gt; is undefined at runtime. Then remove the now-unused &lt;code&gt;TelemetryMetricKind&lt;/code&gt; import from the observability import block.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-31T06:01:44Z on `agentic-organization/apps/workers/src/org-cadence-lanes.ts`:649 (association: MEMBER)

    <pre>
    Verified **real, not a false positive.** `TelemetryMetricKind` is imported from `packages/observability/src/index.ts` but that barrel never exports it and no source file defines it anywhere on the PR head — so `TelemetryMetricKind.Gauge` dereferences `undefined` at runtime. Root cause: the LGTM telemetry module was not committed (see PR-level diagnosis comment). Leaving unresolved until the module lands.
    </pre>

### Thread 2 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F6roz`
- **Path**: `agentic-organization/apps/workers/test/org-cadence-lanes.test.ts`
- **Replies**: 1

**Initial comment:**

- **`github-code-quality[bot]` (bot)** at 2026-05-31T05:05:39Z on `agentic-organization/apps/workers/test/org-cadence-lanes.test.ts`:554 (association: NONE)

  <pre>
  ## Property access on null or undefined

  The base expression of this property access is always undefined.

  ---

  General fix: avoid dereferencing a possibly undefined imported value. In tests, prefer asserting against the concrete serialized value already produced by the telemetry recorder (a stable string/primitive), rather than referencing an enum/object that may not exist at runtime.&lt;/p&gt;
  &lt;p&gt;Best fix here: in &lt;code&gt;agentic-organization/apps/workers/test/org-cadence-lanes.test.ts&lt;/code&gt;, update the expected metric object in the &lt;code&gt;"conformance lane emits the pass-ratio SLI metric for Grafana alerts"&lt;/code&gt; test so &lt;code&gt;kind&lt;/code&gt; uses the literal gauge value instead of &lt;code&gt;TelemetryMetricKind.Gauge&lt;/code&gt;. Since this removes the only shown runtime property access causing the alert, it preserves test intent (assert gauge metric emitted) while eliminating null/undefined property access risk. Also remove the now-unused &lt;code&gt;TelemetryMetricKind&lt;/code&gt; import from line 40.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-31T06:01:46Z on `agentic-organization/apps/workers/test/org-cadence-lanes.test.ts`:554 (association: MEMBER)

    <pre>
    Verified **real** — same root cause via the test path: `TelemetryMetricKind` is imported but never exported/defined, so `TelemetryMetricKind.Gauge` is property access on `undefined`. See PR-level diagnosis comment. Unresolved until the missing telemetry module is committed.
    </pre>

### Thread 3 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F6ro0`
- **Path**: `agentic-organization/apps/agent-cli/src/agent-cli.ts`
- **Replies**: 1

**Initial comment:**

- **`github-code-quality[bot]` (bot)** at 2026-05-31T05:05:39Z on `agentic-organization/apps/agent-cli/src/agent-cli.ts`:203 (association: NONE)

  <pre>
  ## Invocation of non-function

  Callee is not a function: it has type undefined.

  ---

  General fix: guard calls to imported values that may be undefined/non-functions before invoking them, and fail fast with a clear error if the dependency is not callable.&lt;/p&gt;
  &lt;p&gt;Best single fix here: in &lt;code&gt;agentic-organization/apps/agent-cli/src/agent-cli.ts&lt;/code&gt;, inside &lt;code&gt;createAgentCliMetricAgentsFromEnv&lt;/code&gt;, assign &lt;code&gt;createLgtmTelemetryQueryPort&lt;/code&gt; to a local checked variable and verify &lt;code&gt;typeof ... === "function"&lt;/code&gt; before calling it. If not callable, throw an explicit error indicating the observability port factory is unavailable. This prevents the runtime “invocation of non-function” crash and provides actionable diagnostics, while preserving normal behavior when the import is valid. No new imports or dependencies are required.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-31T06:01:47Z on `agentic-organization/apps/agent-cli/src/agent-cli.ts`:203 (association: MEMBER)

    <pre>
    Verified **real.** `createLgtmTelemetryQueryPort` is imported from the observability barrel but is never exported there nor defined anywhere on the PR head — so this invokes `undefined`. Same missing-module root cause (see PR-level diagnosis comment). Unresolved until fixed.
    </pre>

### Thread 4 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F6ro1`
- **Path**: `agentic-organization/apps/workers/test/org-cadence-lanes.test.ts`
- **Replies**: 1

**Initial comment:**

- **`github-code-quality[bot]` (bot)** at 2026-05-31T05:05:39Z on `agentic-organization/apps/workers/test/org-cadence-lanes.test.ts`:537 (association: NONE)

  <pre>
  ## Invocation of non-function

  Callee is not a function: it has type undefined.

  ---

  General fix: when a constructor import may be undefined at runtime, avoid invoking it and use a locally defined object/class that implements only the required contract for the test.&lt;/p&gt;
  &lt;p&gt;Best fix in this file: in &lt;code&gt;agentic-organization/apps/workers/test/org-cadence-lanes.test.ts&lt;/code&gt;, replace &lt;code&gt;new RecordingTelemetry()&lt;/code&gt; with a local telemetry stub object that collects emitted metrics, and update the assertion to compare against that local &lt;code&gt;metrics&lt;/code&gt; array. Keep using &lt;code&gt;TelemetryMetricKind&lt;/code&gt; for expected values. Also remove the now-unused &lt;code&gt;RecordingTelemetry&lt;/code&gt; import to avoid dead/incorrect value import usage.&lt;/p&gt;
  &lt;p&gt;What to change:&lt;/p&gt;
  &lt;ul&gt;
  &lt;li&gt;Import line 40: remove &lt;code&gt;RecordingTelemetry&lt;/code&gt; from named imports.&lt;/li&gt;
  &lt;li&gt;In test &lt;code&gt;"conformance lane emits the pass-ratio SLI metric for Grafana alerts"&lt;/code&gt;:
  &lt;ul&gt;
  &lt;li&gt;Replace &lt;code&gt;const telemetry = new RecordingTelemetry();&lt;/code&gt;&lt;/li&gt;
  &lt;li&gt;Add local &lt;code&gt;const metrics: unknown[] = [];&lt;/code&gt; and &lt;code&gt;const telemetry = { recordMetric(...) { ... } }&lt;/code&gt; (or equivalent method name expected by lane; use broad typing via cast to avoid introducing external types).&lt;/li&gt;
  &lt;li&gt;Assert against &lt;code&gt;metrics&lt;/code&gt; instead of &lt;code&gt;telemetry.metrics&lt;/code&gt;.&lt;/li&gt;
  &lt;/ul&gt;
  &lt;/li&gt;
  &lt;/ul&gt;
  &lt;p&gt;No new external imports/dependencies required.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-31T06:01:48Z on `agentic-organization/apps/workers/test/org-cadence-lanes.test.ts`:537 (association: MEMBER)

    <pre>
    Verified **real.** `RecordingTelemetry` is imported but never defined/exported in the tree, so `new RecordingTelemetry()` constructs `undefined`. Same missing-module root cause (see PR-level diagnosis comment). Unresolved until fixed.
    </pre>

### Thread 5 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F8g9E`
- **Path**: `agentic-organization/apps/agent-cli/src/agent-cli.ts`
- **Replies**: 0

**Initial comment:**

- **`github-code-quality[bot]` (bot)** at 2026-05-31T14:26:28Z on `agentic-organization/apps/agent-cli/src/agent-cli.ts`:205 (association: NONE)

  <pre>
  ## Invocation of non-function

  Callee is not a function: it has type undefined.

  ---

  To fix invocation-of-non-function safely without changing behavior when wiring is correct, add a runtime guard before calling &lt;code&gt;createLgtmTelemetryQueryPort&lt;/code&gt;. In general, when a factory imported across module boundaries may be undefined, verify it is callable and fail fast with a clear error message rather than invoking blindly.&lt;/p&gt;
  &lt;p&gt;Best single fix in &lt;code&gt;agentic-organization/apps/agent-cli/src/agent-cli.ts&lt;/code&gt; (inside &lt;code&gt;createAgentCliMetricAgentsFromEnv&lt;/code&gt;): before line 200, check &lt;code&gt;typeof createLgtmTelemetryQueryPort !== "function"&lt;/code&gt; and throw an explicit error if not callable. Then proceed with the existing call unchanged. This preserves normal functionality when imports are correct, and converts a cryptic “is not a function” crash into a deterministic, diagnosable error.
  </pre>

## Fix commits (touching thread paths)

### `3ad65b7a600ed397d00e5be4b1ec8456b7bdc5e2` -- 2026-05-30T22:20:31Z -- `maximdolphin`

**Touched files (intersect with thread paths):**

- `agentic-organization/apps/workers/src/org-cadence-lanes.ts`
- `agentic-organization/apps/workers/test/org-cadence-lanes.test.ts`

**Message:**

```
feat(agentic-org): generic multi-provider work port + live flip, inte…

…gration CI, gastown moat analysis

Squashes this work-stream's agentic-organization delta onto current main (the branch's prior
slice landed via the squash-merged PR #6071; this carries everything since, scoped to
agentic-organization/ so main's other progress is untouched).

Generic provider-agnostic work port (GEN1–GEN5):
- One surface (project/pull/advance) over a WorkProviderKind DU (github|gitlab|jira|linear) split
  into families (code_review PR/MR vs work_item card); actionsForFamily is the translation table,
  assertProviderSupports the structural guard. Adding a provider = a translation, not a call site.
- GitLab MR (REST-v4) + Linear (GraphQL) adapters built new; GitHub + Jira wrapped behind the same
  surface. resolveWorkProvider builds the live client; token only ever a header, never logged.
  asChangeControlPort adapts a code-review provider to the kernel's port unchanged (open/closed).
- Live flip: resolveWorkProviderFromEnv (null-default, throw-on-partial, legacy back-compat);
  worker mounts an OPTIONAL work-provider Secret (absent → internal-only); proven over the real
  native-fetch wire (loopback, token absent from every call) AND in-cluster (deployed worker flips
  external:gitlab from a Secret, token leaked 0×, then restores internal-only).
- Subagent-reviewed: GitLab partials tightened to throw (no silent empty MR), changes-requested
  axis documented fail-safe; regression tests added.

Integration CI (INT1): the 7 env-gated integration tests run green against real Cockroach+NATS
(npm run test:integration + .github/workflows/integration.yml that fails if any test skips);
ci.yml runs the fast hermetic typecheck+unit suite.

Plus the earlier C-track (C0–C7 adaptive platform: autonomy policy, hat guardrails, org-intelligence,
onboarding/self-healing) carried in this delta where not already on main.

Strategy docs (for the next build phase):
- GASTOWN_FULL_IMPL_COMPARISON.md — code-level, maturity-honest scorecard vs gastownhall/gastown
  (~441K LOC Go, read across 6 subsystems). We out-architected them (enforced kernel, Cockroach+NATS,
  no-SPOF hats, native ports — their unbuilt Factory-Worker-API endgame is our start). They
  out-shipped us on specific build-on-top tooling (merge queue, model-eval, persistent pool,
  layered config, escalation ladder, ESTOP, durable/ephemeral comms split).
- ORCHESTRATION_MOAT_ROADMAP.md — close the gap + go miles ahead by exploiting the
  enforced+deterministic+replayable kernel (M1 conformance checker, M2 simulator/DST, M3
  self-optimizing loop, M4 clamp verification) + enforce the pattern unbypassably.
- HANDOFF_GOAL_ORCHESTRATION_MOAT.md — a paste-able cold-start /goal prompt for the next agent.

tsc 0; 845 unit/contract tests, 0 fail; 7 integration tests green vs real infra; proven in kind.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
```

### `8f774ba2a64c09356a3e891165d5a86ba45840d2` -- 2026-05-30T22:52:16Z -- `maximdolphin`

**Touched files (intersect with thread paths):**

- `agentic-organization/apps/workers/src/org-cadence-lanes.ts`
- `agentic-organization/apps/workers/test/org-cadence-lanes.test.ts`

**Message:**

```
feat(agentic-org): add conformance replay lane

Build the M1/M4 orchestration moat foundation: replay org_events through the legal-transition clamps, wire a live conformance lane, add clamp property tests, and add the KIND conformance proof.

Also fixes memory archive-at-floor drift by making archive legal from every non-terminal memory phase, and records the phase proof in NORTH_STAR.

Co-Authored-By: Codex <noreply@openai.com>
```

### `7bbad242975a4b60bb801730099dd10fe2140257` -- 2026-05-30T23:16:18Z -- `maximdolphin`

**Touched files (intersect with thread paths):**

- `agentic-organization/apps/workers/src/org-cadence-lanes.ts`
- `agentic-organization/apps/workers/test/org-cadence-lanes.test.ts`

**Message:**

```
feat(agentic-org): add recovery scanner lanes

Build the G3 orchestration moat recovery scanners: pure classifiers, bounded Cockroach lifecycle readers, four fail-open worker cadence lanes, and a KIND recovery proof.

Dead-letter evidence stores failure-message hashes rather than raw failure text, preserving forensic linkage without leaking durable payloads.

Verification: npm run typecheck; npm test; docker build agentic-org-worker:g3-recovery-final; kind load; worker pod worker-7489448c66-bxmnq; deploy/run-recovery-scanners.ts PROOF: PASS for org-recovery-02a002d1.

Co-Authored-By: Codex <noreply@openai.com>
```

### `3f70c17e7495a982ee3e218264858c55eafdba1f` -- 2026-05-30T23:55:56Z -- `maximdolphin`

**Touched files (intersect with thread paths):**

- `agentic-organization/apps/workers/src/org-cadence-lanes.ts`
- `agentic-organization/apps/workers/test/org-cadence-lanes.test.ts`

**Message:**

```
feat(agentic-org): add release queue lane

Build the G1 release queue: pure batch/bisect planner, approved ChangeSet cadence lane, explicit release-batch evaluator port, Cockroach transaction-bound persistence, and KIND proof.

The change-control lane now leaves approved ChangeSets for release; the release queue applies green batches and bounces isolated red culprits through the conformant approved-to-changes_requested transition. Post-review fixes make bisection evaluate against the accumulating accepted stack and prevent metadata-only production applies when no evaluator is wired.

Verification: npm run typecheck; npm test (882 tests, 875 pass, 7 skipped, 0 fail); docker build agentic-org-worker:g1-release-queue-atomic sha256:da47e79507bfc3690eb449c60a9a616916ad060d09a908d9d0a11b289749dc9f; kind load; worker pod worker-695b8dc895-lc8dv zero restarts; deploy/run-release-queue.ts PROOF: PASS for org-release-a8e06b67.

Co-Authored-By: Codex <noreply@openai.com>
```

### `45e637284ea1478ec8f4fabd4e6ac8046af36e57` -- 2026-05-31T05:02:02Z -- `maximdolphin`

**Touched files (intersect with thread paths):**

- `agentic-organization/apps/agent-cli/src/agent-cli.ts`
- `agentic-organization/apps/workers/src/org-cadence-lanes.ts`
- `agentic-organization/apps/workers/test/org-cadence-lanes.test.ts`

**Message:**

```
feat(agentic-org): expose hierarchy operating readouts

Add observe.ts hierarchy operating readouts for directors, TPMs, and other management hats so each level sees scoped priority items, metrics, and legal coordination actions.

Wire the readout through the agent CLI and observe-act worker lane, including JSON ingestion for hierarchy work batches and work items.

Co-Authored-By: Codex <noreply@openai.com>
```

### `bad7f0afd9f599066a21a5687c3fb633c66b024c` -- 2026-05-31T14:23:14Z -- `maximdolphin`

**Touched files (intersect with thread paths):**

- `agentic-organization/apps/agent-cli/src/agent-cli.ts`

**Message:**

```
feat(agentic-org): surface management missions in observe

Expose top-down hierarchy missions in observe.ts so management hats see the mission goal, timeframe, expected progress, lag signals, and tool-gated corrective actions inside the existing observe readout.

Co-Authored-By: Codex <noreply@openai.com>
```

### `908c1790db211308afbf76f10b5078a6182d15a1` -- 2026-05-31T14:33:42Z -- `maximdolphin`

**Touched files (intersect with thread paths):**

- `agentic-organization/apps/agent-cli/src/agent-cli.ts`
- `agentic-organization/apps/workers/src/org-cadence-lanes.ts`
- `agentic-organization/apps/workers/test/org-cadence-lanes.test.ts`

**Message:**

```
feat(agentic-org): land observability workflow stack

Commit the LGTM telemetry ports, observability deployment proof, DORA metrics, trace propagation, observe lifecycle flow, and review-thread lint fixes for PR 6200.

Co-Authored-By: Codex <noreply@openai.com>
```
