# PR #6424 -- docs(ace): slice 6 remote-registry design spec (081KT07NV0008QG0R000SJ34AK)

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 6424 |
| Title | docs(ace): slice 6 remote-registry design spec (081KT07NV0008QG0R000SJ34AK) |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-06-01T15:29:23Z |
| Merged at | 2026-06-01T15:31:03Z |
| Merge commit SHA | `3e4be3370ec7a00eb624f6ac65ee521c1e60ba82` |
| Branch | `otto-windows/ace-slice6-remote-registry-spec-2026-06-01` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/6424 |
| Changed files | 1 |
| Additions / deletions | +248 / -0 |

## Description

Design spec for slice 6 of the Ace package manager — **remote registry** (081KT07NV0008QG0R000SJ34AK).

Resolve `name@range` against a **hosted, signed** catalog fetched over HTTP(S), merged
**under** the local registry, with **all per-package verification unchanged**.

**Decisions locked with operator 2026-06-01:**

- **Full index trust now** — three gates on the index: ed25519 **signature** (trust store, optional per-registry pin) + monotonic-**sequence anti-rollback** + `issued_at` **freshness** (default 30-day max-staleness). A remote registry is never shipped unsigned; anti-rollback + freshness defend availability/version-selection where the per-package pin can't.
- **Cache-fallback + `--offline`** — unreachable remote → cached index or local-only with warning; `--offline` skips remotes (skips only the freshness gate on cache; sig + anti-rollback still enforced).
- **Conditional-GET caching** — ETag/Last-Modified revalidation, content-addressed body cache under `~/.ace/registry-cache/`.
- **`registries.json` + CLI** — `ace registry remote add/list/rm`; precedence user > bundled > remote[0] > … (local overrides remote).

**Solver/lockfile/install graph unchanged** — the remote merge happens once, up front, producing the same `Registry` map the solver already consumes.

New: `tools/ace/registry-remote.ts` + tests; additive to `signing.ts` (`signIndex`/`verifyIndexSignature`), `store.ts` (config + cache paths; sync `loadRegistry` untouched), `ace.ts` (CLI + async load).

Deferred (sub-rows): mirror/failover, incremental index, full TUF roles, `ace registry publish` tooling, key rotation.

Next: on approval → writing-plans → subagent-driven build.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 2 |
| Resolved threads | 0 |
| Unresolved threads | 2 |
| Total review comments | 2 |
| Total fix commits (touching thread paths) | 1 |

## Review threads

### Thread 1 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM6GKvaL`
- **Path**: `docs/agendas/ace-package-manager/2026-06-01-ace-cli-slice6-remote-registry-design.md`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-06-01T15:31:23Z on `docs/agendas/ace-package-manager/2026-06-01-ace-cli-slice6-remote-registry-design.md`:120 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Require a registry-scoped key for remote indexes**

  When a user has more than one trusted key and adds a remote without `--key`, this accepts an index signed by any key in the existing Ace trust store, which is currently the global package-signing keyring (`loadTrustStore` has no registry role separation). That lets an unrelated trusted package author or another registry key sign a catalog that controls version selection for this remote; make the registry key mandatory or add a registry-authority/role check instead of falling back to any trusted key.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 2 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM6GKvaN`
- **Path**: `docs/agendas/ace-package-manager/2026-06-01-ace-cli-slice6-remote-registry-design.md`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-06-01T15:31:23Z on `docs/agendas/ace-package-manager/2026-06-01-ace-cli-slice6-remote-registry-design.md`:125 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Reject future issued_at timestamps**

  With this freshness check, an index whose `issued_at` is far in the future always passes because `now - issued_at` is negative and therefore below the max staleness. In the scenario where a registry clock is misconfigured (or a compromised signer publishes one future-dated high-sequence index), clients can cache and continue accepting that index for years, defeating the intended freshness gate; add a maximum future-skew bound before accepting the timestamp.

  Useful? React with 👍 / 👎.
  </pre>

## Fix commits (touching thread paths)

### `16c0a6867d8fd465dd80f4509d240eafd4e4c234` -- 2026-06-01T15:29:07Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `docs/agendas/ace-package-manager/2026-06-01-ace-cli-slice6-remote-registry-design.md`

**Message:**

```
docs(ace): slice 6 remote-registry design spec (081KT07NV0008QG0R000SJ34AK)

Remote registry: fetch a SIGNED index over HTTP(S), verify it with three gates
(ed25519 signature against the trust store + per-registry monotonic-sequence
anti-rollback + issued_at freshness/max-staleness), cache it (conditional GET,
content-addressed), and merge it UNDER the local registry (user > bundled >
remote[0] > ...). registries.json + `ace registry remote add/list/rm`; --offline
+ cache-fallback. Solver/lockfile/install graph unchanged — the remote merge
produces the same Registry map the solver already consumes.

Decisions locked with operator 2026-06-01 (full index trust now; cache-fallback +
--offline; conditional-GET caching; registries.json + CLI). Per-package hash-pin +
signature gate unchanged — index trust is additive, defending availability +
version-selection where the per-package pin cannot.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```
