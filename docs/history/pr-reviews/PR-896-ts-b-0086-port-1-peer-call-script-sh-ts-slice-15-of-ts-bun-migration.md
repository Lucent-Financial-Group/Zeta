# PR #896 -- ts(B-0086): port 1 peer-call script (.sh→.ts) — slice 15 of TS/Bun migration

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 896 |
| Title | ts(B-0086): port 1 peer-call script (.sh→.ts) — slice 15 of TS/Bun migration |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-04-30T06:00:08Z |
| Merged at | 2026-04-30T06:03:18Z |
| Merge commit SHA | `0a258338afaef8d2131987bc1921a5014cf009e1` |
| Branch | `lane-b/ts-bun-slice-15-peer-call-grok-2026-04-30` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/896 |
| Changed files | 3 |
| Additions / deletions | +322 / -8 |

## Description

## Summary

Slice 15 of the TS/Bun migration — opens **peer-call cluster**:

- `tools/peer-call/grok.{sh→ts}` (Otto's harness-side caller for invoking Grok via cursor-agent as a peer reviewer)

Sibling ports (gemini.sh + codex.sh) follow the same shape; deferred to slice 16/17.

## Audit substrate

See [`docs/trajectories/typescript-bun-migration/slice-audits.md`](docs/trajectories/typescript-bun-migration/slice-audits.md) Slice 15 entry.

## Mechanical port notes

- Bash arg-parse loop → `classifyFlag` helper + `MutableArgState` (main `parseArgs` stays under cognitive-complexity 15).
- Bash `eval "$context_cmd" 2>&1 | head -c 20000` → `spawnSync("/bin/sh", ["-c", contextCmd])` + slice to `CTX_HEAD_BYTES`.
- Bash `head -c 20000 < "$file"` → `readFileSync` + `Buffer.subarray(0, FILE_HEAD_BYTES)`.
- Bash `cursor-agent --print ...` → `spawnSync` with `stdio: "inherit"` (preserves live LLM-streaming UX).
- PREAMBLE preserved verbatim (Otto's contribution to the four-ferry consensus protocol convention).
- eslint-disable for `no-os-command-from-path` placed on the literal next line per directive-placement pattern from #892 review.

## Test plan

- [x] `bun --bun tsc --noEmit` clean (the new `lint (tsc tools)` gate from #890 will validate this in CI).
- [x] `eslint` clean.
- [x] Argument-validation byte-equivalent: `--file` without value → exit 1; `--context-cmd` without value → exit 1; `--bogus` → exit 1; no prompt → exit 1.
- [ ] CI gate (gate.yml + CodeQL + lint-tsc-tools).
- LLM-response equivalence is non-deterministic (Grok produces different completions per invocation); only structural/UX equivalence verified.

## Behavioural note vs bash

- The eval/security boundary is preserved verbatim: user-supplied `--context-cmd` runs through `/bin/sh -c` (TS) which is structurally equivalent to bash `eval`. Same trust assumption: caller controls the script invocation, so caller controls the context command.

## Trajectory

- After-merge: 36 ports total, 6 Bucket B files remain (2 budget + 1 git/batch-resolve + 2 peer-call sibling + 1 pr-preservation/archive).
- Trajectory: see [`docs/trajectories/typescript-bun-migration/RESUME.md`](docs/trajectories/typescript-bun-migration/RESUME.md).

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 12 |
| Resolved threads | 1 |
| Unresolved threads | 11 |
| Total review comments | 12 |
| Total fix commits (touching thread paths) | 1 |

## Review threads

### Thread 1 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5-pRiE`
- **Path**: `tools/peer-call/grok.ts`
- **Replies**: 0

**Initial comment:**

- **`github-advanced-security[bot]` (bot)** at 2026-04-30T06:01:46Z on `tools/peer-call/grok.ts`:174 (association: NONE)

  <pre>
  ## CodeQL / Indirect uncontrolled command line

  This command depends on an unsanitized [command-line argument](1).

  [Show more details](https://github.com/Lucent-Financial-Group/Zeta/security/code-scanning/33)
  </pre>

### Thread 2 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM5-pSn9`
- **Path**: `tools/peer-call/grok.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-04-30T06:03:38Z on `tools/peer-call/grok.ts`:162 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Limit file attachment reads to the configured head size**

  The TS port now loads the entire `--file` input into memory before truncating, while the bash version streamed only the first 20,000 bytes via `head -c`. On large files this can cause unnecessary memory spikes or OOM failures and makes the script much slower for the common “attach a big log/diff file” workflow, even though only a small prefix is ever used in the prompt.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 3 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM5-pSn_`
- **Path**: `tools/peer-call/grok.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-04-30T06:03:38Z on `tools/peer-call/grok.ts`:176 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Truncate context output by bytes, not UTF-16 characters**

  This truncation uses `String.slice(0, CTX_HEAD_BYTES)`, which counts UTF-16 code units, not bytes. The original bash behavior (`head -c 20000`) was byte-based, so non-ASCII context output can now exceed the intended 20,000-byte cap by a large margin, inflating prompt size and potentially causing token/cost regressions when `--context-cmd` emits Unicode-heavy output.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 4 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM5-pTlD`
- **Path**: `tools/peer-call/grok.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-30T06:05:10Z on `tools/peer-call/grok.ts`:175 (association: NONE)

  <pre>
  runContextCmd() concatenates stdout/stderr via template literals, but spawnSync can return stdout/stderr as null when the child fails to start. Please coalesce to empty strings (and consider surfacing result.error) so the returned context doesn’t become "nullnull" and downstream formatting remains stable.

  </pre>

### Thread 5 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM5-pTlP`
- **Path**: `docs/trajectories/typescript-bun-migration/slice-audits.md`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-30T06:05:10Z on `docs/trajectories/typescript-bun-migration/slice-audits.md`:418 (association: NONE)

  <pre>
  This trajectory doc is a current-state surface, so named attribution like “Otto’s …” should be expressed as role-refs per repo convention. Please rephrase this bullet to avoid persona/human names (e.g., “the tester’s harness-side caller …”).

  </pre>

### Thread 6 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM5-pTlb`
- **Path**: `tools/peer-call/grok.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-30T06:05:10Z on `tools/peer-call/grok.ts`:22 (association: NONE)

  <pre>
  The header comment uses named attribution (e.g., “Otto”) in a non-history surface (code under tools/). Repo convention is to use role-refs outside the enumerated history/research surfaces; please rewrite this block using role-based references (e.g., “the tester”, “the harness-side caller”) to comply.

  </pre>

### Thread 7 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM5-pTlj`
- **Path**: `tools/peer-call/grok.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-30T06:05:10Z on `tools/peer-call/grok.ts`:27 (association: NONE)

  <pre>
  Exit-code docs say the non-zero case is “response captured to stderr”, but this implementation runs cursor-agent with stdio inherited and does not capture/replay output. Please update the exit-code comment to match actual behavior (or change implementation to capture if that’s intended).

  </pre>

### Thread 8 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM5-pTlt`
- **Path**: `tools/peer-call/grok.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-30T06:05:11Z on `tools/peer-call/grok.ts`:163 (association: NONE)

  <pre>
  readHead() reads the entire file into memory and can throw (e.g., permission error) which will crash the script with a stack trace. Please (1) read only the first N bytes (stream/open+read) and (2) catch read errors and return a typed/controlled invocation error message instead of throwing.
  </pre>

### Thread 9 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM5-pTl6`
- **Path**: `tools/peer-call/grok.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-30T06:05:11Z on `tools/peer-call/grok.ts`:186 (association: NONE)

  <pre>
  PREAMBLE embeds named attribution (Otto/Aaron/Amara/Gemini/etc.) in a current-state code surface. Repo convention is role-refs outside the history/research surfaces; please rephrase the preamble to use roles (e.g., “the proposer / critic / sharpener / tester”) while keeping the relationship-model intent.

  </pre>

### Thread 10 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM5-pTmH`
- **Path**: `tools/peer-call/grok.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-30T06:05:11Z on `tools/peer-call/grok.ts`:283 (association: NONE)

  <pre>
  The cursor-agent spawn result collapses all non-zero outcomes into exit code 2 and prints “exited with code …”. If spawnSync fails to launch (status === null, result.error set) this is an invocation error per the script’s own exit-code contract and should be reported distinctly (launch failure vs terminated by signal vs non-zero exit).

  </pre>

### Thread 11 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM5-pTmO`
- **Path**: `docs/trajectories/typescript-bun-migration/slice-audits.md`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-30T06:05:11Z on `docs/trajectories/typescript-bun-migration/slice-audits.md`:423 (association: NONE)

  <pre>
  Slice sections in this file are ordered by slice number (ascending). This Slice 15 section was inserted between Slice 6 and Slice 14, which breaks the document’s structure and makes later slices harder to find; please move Slice 15 to its correct position (after Slice 14).

  </pre>

### Thread 12 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM5-pTmT`
- **Path**: `tools/peer-call/grok.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-30T06:05:12Z on `tools/peer-call/grok.ts`:147 (association: NONE)

  <pre>
  commandAvailable() treats any failure as “not on PATH” later. spawnSync provides result.error (e.g., EACCES) and status can be null; please distinguish ENOENT (not found) from other spawn failures so the user gets an accurate error (and so later exit-code mapping can be correct).

  </pre>

## Fix commits (touching thread paths)

### `d262fde787ebb8e6cf660f1c44a0718a8af31b7d` -- 2026-04-30T05:59:40Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `docs/trajectories/typescript-bun-migration/slice-audits.md`
- `tools/peer-call/grok.ts`

**Message:**

```
ts(B-0086): port 1 peer-call script (.sh→.ts) — slice 15 of TS/Bun mi…

…gration

* ts(slice-15, wip 1/N): port peer-call/grok (.sh→.ts)

First script of slice 15. Otto's harness-side caller for invoking
Grok via cursor-agent as a peer reviewer. Opens the peer-call
cluster — sibling ports (gemini + codex) follow the same shape.

Byte-equivalent on argument-validation paths. LLM-response
equivalence is non-deterministic; only structural/UX equivalence
verified.

Mechanical changes:
- bash arg-parse loop → classifyFlag helper + MutableArgState
  (main parseArgs stays under cognitive-complexity 15)
- bash 'eval "$context_cmd" 2>&1 | head -c 20000' →
  spawnSync('/bin/sh', ['-c', contextCmd]) + slice to CTX_HEAD_BYTES
- bash file-head read → readFileSync + Buffer.subarray
- bash 'cursor-agent --print ...' → spawnSync with stdio:'inherit'
  (preserves live LLM-streaming UX)
- PREAMBLE preserved verbatim (Otto's contribution to the
  four-ferry consensus protocol convention)
- eslint-disable for no-os-command-from-path placed on the literal
  next line per directive-placement pattern from #892 review

Lint-clean: bun --bun tsc --noEmit + eslint strictTypeChecked +
sonarjs all pass. The lint (tsc tools) gate from #890 will validate
this in CI.

* trajectory(ts-bun): slice 15 audit substrate + RESUME tracker

EOF
```
