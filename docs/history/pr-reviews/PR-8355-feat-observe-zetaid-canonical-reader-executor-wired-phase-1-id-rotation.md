# PR #8355 -- feat(observe): ZetaId-canonical reader + executor wired (Phase 1 id-rotation)

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 8355 |
| Title | feat(observe): ZetaId-canonical reader + executor wired (Phase 1 id-rotation) |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-06-15T22:46:30Z |
| Merged at | 2026-06-15T22:46:40Z |
| Merge commit SHA | `d2c22522d7af8b5128ab8388d4497deecde02d87` |
| Branch | `alexa/zetaid-canonical-reader` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/8355 |
| Changed files | 8 |
| Additions / deletions | +1767 / -25 |

## Description

## What

Phase 1 of the zero-downtime id rotation (ADR: 2026-06-15-zero-downtime-id-rotation-pattern-overlap-window-dual-key.md).

### Changes

1. **autonomous-pickup.ts** — Reader uses `zetaid` frontmatter field as canonical `id`. Dual-lookup resolves both ZetaId and legacy B-xxxx for backward compat during the overlap window. The B-xxxx sequential counter is operationally dead — no agent needs to coordinate on next-number ever again.

2. **kiro-executor.ts** (new) — Real `just-bash` CommandExecutor for the observe loop. Creates claim branches and runs bounded work scripts. Phase 1: proof-of-execution (claim file); Phase 2: actual implementation.

3. **run-loop-real.ts** — Wired the executor + `buildDoItemSpec` so `do_item` actions execute end-to-end (was returning not-yet-executable).

4. **b-to-zetaid-map.json** — Persistent mapping table (1,131 entries) for the batched Phase 2 migration.

5. **ADR + spec** — Formal pattern documented for reuse: overlap-window dual-key rotation with safety/liveness properties.

6. **.kiro/** — Steering files + hooks for autonomous tick context.

### Verified

- 190 tests pass (observe + backlog suites)
- Observe loop picks items by ZetaId: `081KQNJ500008QG0R003SCWBDV` (was `B-0170`)
- Dry-run works end-to-end
- Real execution fails gracefully when not on main (expected: the launchd service runs on main)

### Next (Phase 2)

- Batched `depends_on` migration via mapping table
- Regenerate golden vectors through generators (not sed)
- Drop B-xxxx at quorum after in-flight branches land

Co-Authored-By: Kiro <noreply@kiro.dev>

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 0 |
| Resolved threads | 0 |
| Unresolved threads | 0 |
| Total review comments | 0 |
| Total fix commits (touching thread paths) | 0 |

## Review threads

(no review threads on this PR)

## Fix commits (touching thread paths)

(no commits in this PR touched files anchored by review threads)
