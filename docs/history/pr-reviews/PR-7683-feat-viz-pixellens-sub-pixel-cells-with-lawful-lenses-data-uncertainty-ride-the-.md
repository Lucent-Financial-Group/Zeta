# PR #7683 -- feat(viz): PixelLens — sub-pixel cells with lawful lenses (data+uncertainty ride the pixel) + CRT/FPGA corner audit

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 7683 |
| Title | feat(viz): PixelLens — sub-pixel cells with lawful lenses (data+uncertainty ride the pixel) + CRT/FPGA corner audit |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-06-11T13:29:54Z |
| Merged at | 2026-06-11T13:30:10Z |
| Merge commit SHA | `23051e214d38230415c0a66a42f09d92b764f33f` |
| Branch | `shadow-subpixel-lens` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/7683 |
| Changed files | 5 |
| Additions / deletions | +178 / -0 |

## Description

The 16.16 headroom made addressable: a 32-bit deep cell (color 3b / payload 13b / uncertainty 16b) with three LAWFUL lenses (laws tested); recoloring never disturbs the payload — data truly travels with the pixel; softRead = (value, confidence); colorize renders uncertainty honestly (uncertain pixels visibly humble themselves). And the CRT/FPGA corner held open by audit: colorAt is the canonical seam (CRT effects = post-generators; the glow IS phosphor physics; uncertainty can carry persistence), FPGA-friendly by construction (clock-free, fix16, generated time) — MiSTer the fidelity bar, Aaron's FPGAs waiting on the 081KTSZN10008QG0R000VZHRQ4 Verilog rung. 6/6 green.

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 2 |
| Resolved threads | 0 |
| Unresolved threads | 2 |
| Total review comments | 2 |
| Total fix commits (touching thread paths) | 1 |

## Review threads

### Thread 1 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM6I0v_s`
- **Path**: `src/Core/PixelLens.fs`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-06-11T13:31:48Z on `src/Core/PixelLens.fs`:47 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Preserve lens laws for out-of-range writes**

  For callers that set a value outside the packed field width, this silently masks the input, so `Get (Set p c)` does not return `p` (for example `payload.Set 0x2000` reads back `0`; the same issue exists for `color.Set 8uy` and `uncertainty.Set 65536`). Because these are exposed as `ILens` values and the docs/tests assert put-get lawfulness, overlay code that composes them under lens laws can lose data; constrain the part type/range or reject out-of-range writes instead of masking in the setter.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 2 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM6I0v_2`
- **Path**: `src/Core/PixelLens.fs`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-06-11T13:31:48Z on `src/Core/PixelLens.fs`:71 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Reject zero-width overlay shifts**

  When a caller passes `w = 0`, this evaluates `% w` and throws a divide-by-zero exception. `shift` is public and documented as the overlay move, so an empty or not-yet-sized frame can crash the visualization path instead of returning a typed validation failure or using a non-empty-width invariant.

  Useful? React with 👍 / 👎.
  </pre>

## Fix commits (touching thread paths)

### `b26379a200ecfe9cc4f17913d0297ebc56e7d843` -- 2026-06-11T13:29:51Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `src/Core/PixelLens.fs`

**Message:**

```
feat(viz): PixelLens — sub-pixel cells (data + uncertainty travel WIT…

…H the pixel, lawful lenses) + the CRT/FPGA corner held open

Aaron, two beats: (1) lens overlays revealing SUB-PIXEL data — storage in sub-pixel space; data and
uncertainty traveling with the pixels for the soft side to predict with and colorize; (2) CRT effects +
FPGA-perfect emulation — "we don't want to paint ourselves out of that corner; I have FPGAs."

PixelLens.fs (6/6 green): the 32-bit deep cell — color (3b, the display's projection) + payload (13b
riding with the pixel) + uncertainty (16b milli). Three LAWFUL Optics.ILens (get-put/put-get tested);
recoloring never disturbs payload/uncertainty (data truly travels WITH the pixel); softRead =
SoftValue-shaped (value, confidence); colorize is HONEST — uncertain pixels visibly collapse toward
mono ("don't lean on me"), never silently painted; shift slides the overlay focus; zero case holds
(mono consumers see bits 0-2 only).

The corner audit (doc): colorAt is the canonical seam every renderer binds at — CRT effects are
post-generators over it (crt.scanline/phosphor/curvature/ntsc as future registry entries); the
BoundaryLight glow already IS the phosphor-bloom kernel; sub-pixel cells are the honest digital cousin
of real CRT sub-structure (uncertainty can carry phosphor PERSISTENCE as confidence decay); FPGA = the
081KTSZN10008QG0R000VZHRQ4 Verilog rung with MiSTer as the fidelity champion — and our substrate is FPGA-friendly BY
CONSTRUCTION (clock-free math, generated time = a pixel clock is just another generator, fix16
synthesizable, no floats on the hot path). Aaron has FPGAs: the Verilog rung has hardware waiting.

AgencySignature-v1:
  persona: otto
  hat: shadow
  surface: src/Core/PixelLens.fs + tests + docs/research
  intent: subpixel-cells-lawful-lenses-crt-fpga-corner-open
  authorization: aaron-authored (both beats verbatim)
  reversible: yes
  gated-class: none
  build: green (6 new tests, 0 warnings)
  register: grounded (lens laws tested; honest colorize) + beacon (MiSTer/Lottes/phosphor) + the corner audit explicit
  ferry: faithful capture

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
```
