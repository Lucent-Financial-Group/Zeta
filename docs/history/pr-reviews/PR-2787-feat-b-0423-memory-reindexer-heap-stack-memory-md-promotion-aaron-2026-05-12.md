# PR #2787 -- feat(B-0423): memory/ reindexer — heap→stack MEMORY.md promotion (Aaron 2026-05-12)

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 2787 |
| Title | feat(B-0423): memory/ reindexer — heap→stack MEMORY.md promotion (Aaron 2026-05-12) |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-05-12T15:12:42Z |
| Merged at | 2026-05-12T15:20:02Z |
| Merge commit SHA | `5100604a34864a9abab6ea5663e87e9a11b654eb` |
| Branch | `feat/b-0423-memory-md-reindexer-2026-05-12` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/2787 |
| Changed files | 2 |
| Additions / deletions | +234 / -0 |

## Description

## Summary

Implements the architectural fix from B-0423: the MEMORY.md
serialization-point anti-pattern.

Aaron 2026-05-12 architectural input:
- memory.md is the stack; recent memory files are the heap; both accessible
- AutoDream does the reindexing
- needs to run more often than Anthropic's base allows → autonomous-loop tick

## What the tool does

- Scans memory/*.md, parses frontmatter from every file
- Sorts by created date (newest first)
- Renders top-100 as stack entries in MEMORY.md
- Acknowledges remaining N as heap (accessible by direct path)
- Idempotent via --check mode (exit 2 if stale)

## Architectural composition

Eliminates the MEMORY.md serialization-point. Memory files can
commit without synchronous MEMORY.md paired-edit. This tool catches
the index up on cadence via the autonomous-loop cron.

Matches the 4-property substrate test (scale-free / lock-free /
weight-free / DST).

## Initial run

```
Entries: 1060. Top-100 indexed, 960 in heap.
```

## Test plan

- [x] parseFrontmatter unit tests pass (4 cases)
- [x] Real-data run regenerates MEMORY.md cleanly
- [x] Idempotency check
- [x] Stack/heap framing preserved

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 4 |
| Resolved threads | 4 |
| Unresolved threads | 0 |
| Total review comments | 4 |
| Total fix commits (touching thread paths) | 1 |

## Review threads

### Thread 1 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6Bdw6S`
- **Path**: `tools/memory/reindex-memory-md.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-12T15:17:14Z on `tools/memory/reindex-memory-md.ts`:99 (association: NONE)

  <pre>
  P0: collectEntries() assumes every entry returned by readdir("memory") is a regular file. In this repo, memory/ contains subdirectories (e.g. architectural-intent-guesses/, persona/, observed-phenomena/), so readFile(join(MEMORY_DIR, filename)) will throw EISDIR and the reindexer will crash. Use readdir({ withFileTypes: true }) and skip non-files (or catch EISDIR/ENOENT per-file and continue with a warning).

  </pre>

### Thread 2 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6Bdw7g`
- **Path**: `tools/memory/reindex-memory-md.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-12T15:17:15Z on `tools/memory/reindex-memory-md.ts`:139 (association: NONE)

  <pre>
  renderIndex() hard-codes "[AutoDream last run: 2026-04-23]" while also emitting a dynamic "Last reindex: &lt;today&gt;" later in the file. This will go stale immediately and makes the generated MEMORY.md internally inconsistent. Consider emitting a single source of truth (e.g., use the computed `now` for the AutoDream line, or remove/derive the earlier line).
  </pre>

### Thread 3 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6Bdw74`
- **Path**: `tools/memory/reindex-memory-md.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-12T15:17:15Z on `tools/memory/reindex-memory-md.ts`:117 (association: NONE)

  <pre>
  project_memory_format_standard.md specifies that memory/MEMORY.md should be "One line per entry, under 150 chars" and "Capped at ~200 lines". This generator truncates descriptions to 240 chars (and doesn’t enforce a total line-length budget), so it will keep producing entries that violate the documented standard. Consider adjusting truncation to target the 150-char per-entry limit (accounting for the link/title prefix), and keep MAX_STACK_ENTRIES aligned with the documented cap.
  </pre>

### Thread 4 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6Bdw8Z`
- **Path**: `tools/memory/reindex-memory-md.test.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-12T15:17:16Z on `tools/memory/reindex-memory-md.test.ts`:3 (association: NONE)

  <pre>
  Only parseFrontmatter() is unit-tested here. The new behavior in collectEntries()/renderIndex() (notably: skipping non-files from memory/, deterministic ordering, truncation, and stack/heap marker rendering) isn’t covered, and we already have directory entries in memory/ that would currently break the scan. Add tests that exercise collectEntries()/renderIndex() with a fixture directory containing both .md files and subdirectories to prevent regressions.
  </pre>

## Fix commits (touching thread paths)

### `dc1c00019aeb37bc2ac35aaefaebfd9cc0e5157c` -- 2026-05-12T15:11:09Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/memory/reindex-memory-md.test.ts`
- `tools/memory/reindex-memory-md.ts`

**Message:**

```
feat(B-0423): memory/ reindexer — heap→stack promotion for MEMORY.md

Implements the architectural fix from B-0423 backlog item:
the MEMORY.md serialization-point anti-pattern.

What it does:
- Scans memory/*.md, parses frontmatter from every file
- Sorts by created date (newest first)
- Renders top-100 as stack entries in MEMORY.md
- Acknowledges remaining N as heap (accessible by direct path)
- Idempotent via --check mode

How it composes with the architecture:
- Memory files commit without synchronous MEMORY.md paired-edit
  (eliminates serialization point)
- This tool runs on cadence (callable from autonomous-loop)
  to keep MEMORY.md current without blocking commits
- Stack/heap framing in preamble tells readers what they're
  looking at — newest-N indexed, rest browsable by path

Aaron 2026-05-12 architectural input:
- "memory.md for me is like stack and floating memories not
  yet in there but with recent timestamps are heap both
  easily accesable"
- "also lets your autodream do the reindexing"
- "but needs to run more often than anthorpic alows on theri base"

The autonomous-loop cron (firing every minute) is the
high-cadence mechanism. AutoDream-via-autonomous-loop is the
architectural fix; this tool is the implementation.

Test coverage:
- parseFrontmatter handles simple key:value, folded scalars,
  quoted strings, missing frontmatter

Initial run on current state: 1060 memory files, top-100
indexed, 960 acknowledged as heap.

Composes with feedback_aaron_thousand_brains_theory_match...
(four-property substrate test — this tool's design is
lock-free / wait-free / weight-free / DST-compatible).

🤖 Generated with [Claude Code](https://claude.com/claude-code)

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
```
