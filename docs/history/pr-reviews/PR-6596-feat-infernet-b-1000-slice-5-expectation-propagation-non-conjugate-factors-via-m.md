# PR #6596 -- feat(infernet 081KT2T2J0008QG0R000S7GHQ8 slice 5): Expectation Propagation — non-conjugate factors via moment-matching (probit site), plugs into runToFixpoint

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 6596 |
| Title | feat(infernet 081KT2T2J0008QG0R000S7GHQ8 slice 5): Expectation Propagation — non-conjugate factors via moment-matching (probit site), plugs into runToFixpoint |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-06-02T16:52:16Z |
| Merged at | 2026-06-02T17:07:33Z |
| Merge commit SHA | `724a6e72c1f1c6845a13d251c260edd302d24e8f` |
| Branch | `otto-cli/b1000-slice5-ep-2026-06-02` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/6596 |
| Changed files | 4 |
| Additions / deletions | +220 / -0 |

## Description

## 081KT2T2J0008QG0R000S7GHQ8 slice 5 — Expectation Propagation (non-conjugate factors)

EP handles **non-conjugate** factors by **moment matching**. The structural win: **EP is not a new engine — it's a new FACTOR TYPE** that the existing `FactorGraph` + `runToFixpoint` (slice 4) drives.

### `src/Bayesian/Ep.fs`

- **`Normal.erf/pdf/cdf`** — standard normal Φ/φ (erf via Abramowitz–Stegun 7.1.26, max abs err ≈1.5e-7; .NET has no built-in erf).
- **`Ep.probitProject cavity`** — moment-match the tilted `N(m,v)·Φ(x)` back to a Gaussian (GPML eq 3.58): `z=m/√(1+v)`, `λ=φ(z)/Φ(z)`, `m̂=m+vλ/√(1+v)`, `v̂=v−v²λ(z+λ)/(1+v)`.
- **`Ep.probitFactor variable`** — a soft observation "x>0" (likelihood Φ(x)) as a `Factor<Gaussian>`: `ComputeMessages = project(cavity·Φ) / cavity`, where the incoming var→factor message **is** the cavity (the sum-product variable rule). Improper/uniform cavity → flat message (no NaN).

The EP cycle = **cavity (divide) → tilt (×factor) → project (moment-match) → divide** — cavity/divide are slice 2, project is the new bit, the loop is slice 4's `runToFixpoint`.

### Tests (`Ep.Tests.fs` — 4/4)

- `Normal` cdf/pdf accurate
- **probit projection matches NUMERICAL QUADRATURE** of `N(m,v)·Φ(x)` over 4 (m,v) cases — the formula is *verified*, not hardcoded
- **EP probit factor in `runToFixpoint` converges to the moment-matched posterior** (≈`N(0.564,0.682)`, verified vs quadrature; mean shifts positive on "x>0")
- improper-cavity → flat message (no NaN)

Spec (clean-room, papers with proofs): Minka 2001 (EP); Rasmussen–Williams *GPML* §3.6. `dotnet build -c Release`: **0 warnings**. `dotnet test`: **4/4**.

Slices: 1 Vector/Wall → 2 message algebra → 3 factor graph → 4 BP fixpoint (all merged) → **5 EP (this)** → 6 seed CE. Refs 081KT2T2J0008QG0R000S7GHQ8.

🤖 Generated with [Claude Code](https://claude.com/claude-code)


## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | true |
| Total threads | 4 |
| Resolved threads | 4 |
| Unresolved threads | 0 |
| Total review comments | 8 |
| Total fix commits (touching thread paths) | 3 |

## Review threads

### Thread 1 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GgMwA`
- **Path**: `src/Bayesian/Ep.fs`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-06-02T16:53:40Z on `src/Bayesian/Ep.fs`:66 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Stabilize inverse Mills ratio in tail cavities**

  For proper Gaussian cavities whose `z = m / sqrt(1+v)` is in the negative tail (around `z &lt;= -9` with this `Normal.cdf` implementation), `Normal.cdf z` rounds to `0.0`, so this division produces `infinity`/`NaN` and `Gaussian.ofMeanVariance` can throw. That makes `Ep.probitFactor` crash inside `runToFixpoint` for valid models such as a strong negative Gaussian prior plus a probit observation, even though the tilted distribution is well-defined; use a tail-stable inverse Mills ratio / log-CDF path instead of dividing by the rounded CDF.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-06-02T17:00:35Z on `src/Bayesian/Ep.fs`:66 (association: MEMBER)

    <pre>
    Fixed in a713034d3. Factored out `inverseMills z` with an asymptotic fallback: when the naive φ/Φ is non-finite (Φ(z) underflowed to 0 for z≪0), it uses the lower-tail hazard-rate series λ(z)≈−z−1/z+2/z³ (from Φ(z)=φ(z)·[1/w−1/w³+3/w⁵−…], w=−z). The unstable branch and the asymptotic branch are complementary by construction — Φ underflows exactly where the asymptotic is most accurate — so probitProject stays finite/proper/variance-reduced for arbitrarily extreme cavities. Regression test added (cavities −15/−40/−8). Verified: 5/5 EP tests pass, 0 warnings.
    </pre>

### Thread 2 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GgQTR`
- **Path**: `src/Bayesian/Ep.fs`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-02T16:56:36Z on `src/Bayesian/Ep.fs`:90 (association: NONE)

  <pre>
  `probitProject` can produce NaN/∞ (and then throw via `Gaussian.ofMeanVariance`) when `Normal.cdf z` underflows to 0 for large negative `z` or when intermediate terms overflow (e.g., very large cavity variance). This would crash `FactorGraph.runToFixpoint`. Add a numerically-stable inverse Mills ratio fallback for extreme tails and guard non-finite / non-positive `vHat`, falling back to `cavity` so the factor emits a uniform message instead of throwing.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-06-02T17:00:38Z on `src/Bayesian/Ep.fs`:90 (association: MEMBER)

    <pre>
    Fixed in a713034d3. Factored out `inverseMills z` with an asymptotic fallback: when the naive φ/Φ is non-finite (Φ(z) underflowed to 0 for z≪0), it uses the lower-tail hazard-rate series λ(z)≈−z−1/z+2/z³ (from Φ(z)=φ(z)·[1/w−1/w³+3/w⁵−…], w=−z). The unstable branch and the asymptotic branch are complementary by construction — Φ underflows exactly where the asymptotic is most accurate — so probitProject stays finite/proper/variance-reduced for arbitrarily extreme cavities. Regression test added (cavities −15/−40/−8). Verified: 5/5 EP tests pass, 0 warnings.
    </pre>

### Thread 3 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GgQTy`
- **Path**: `src/Bayesian/Ep.fs`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-02T16:56:37Z on `src/Bayesian/Ep.fs`:45 (association: NONE)

  <pre>
  The `Normal` docs claim ".NET has no built-in erf", but this repo targets `net10.0` and `System.Math.Erf` is available. Using the BCL implementation simplifies the code and avoids maintaining a custom approximation (or at least update the comment if you intentionally want the A–S approximation).
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-06-02T17:00:45Z on `src/Bayesian/Ep.fs`:45 (association: MEMBER)

    <pre>
    Verified against Microsoft Learn (net10.0 API surface): there is no `System.Math.Erf`. `System.Math`/`System.MathF` expose Exp/Log/Sqrt/Sin/… but the error function was never added to the BCL — which is exactly why scientific .NET code ships its own erf (Abramowitz–Stegun 7.1.26 here, max abs err ≈1.5e-7). The doc comment is accurate; the rational approximation is necessary, not a redundant reimplementation. Resolving as a verified false positive (suspect-by-default reviewer-hallucinated-API class). If a built-in lands in a future runtime we can swap behind `Normal.erf`.
    </pre>

### Thread 4 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GgWd2`
- **Path**: `src/Bayesian/Ep.fs`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-06-02T17:02:12Z on `src/Bayesian/Ep.fs`:85 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Avoid overflowing the variance update**

  For very broad but still valid Gaussian cavities, e.g. `Gaussian.ofMeanVariance 0.0 1e308`, this computes `v * v` before dividing by `1.0 + v`, so the intermediate overflows to infinity even though the algebraically equivalent update remains finite. That makes `probitProject` throw through `Gaussian.ofMeanVariance` for a proper cavity accepted by the public constructor; rewrite the term as something like `v * (1.0 - (v / (1.0 + v)) * lambda * (z + lambda))` to keep the intermediate in range.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-06-02T17:03:43Z on `src/Bayesian/Ep.fs`:85 (association: MEMBER)

    <pre>
    Fixed in edcadc0e3. Factored v̂ = v − v²λ(z+λ)/(1+v) → v·(1 − (v/(1+v))·λ(z+λ)) — the v/(1+v) factor stays ≤ 1 so the v² intermediate no longer overflows for a broad-but-valid cavity (v=1e308 is finite+positive → constructor-accepted). Algebraically identical; verified for m=0,v=1e308 (z=0, λ=φ(0)/Φ(0)≈0.798 → v̂≈0.363·v, finite/proper). Regression test added (0,1e308)/(±1e150,1e300). 6/6 EP tests pass, 0 warnings.
    </pre>

## Fix commits (touching thread paths)

### `b0ffa65ffadf92b49464254a0c44e285ce157069` -- 2026-06-02T16:52:04Z -- ``

**Touched files (intersect with thread paths):**

- `src/Bayesian/Ep.fs`

**Message:**

```
feat(infernet 081KT2T2J0008QG0R000S7GHQ8 slice 5): Expectation Propagation — non-conjugat…

…e factors via moment matching (probit site); plugs into runToFixpoint (Aaron 2026-06-02)

Slice 5 of the Zeta Infer.NET rewrite: EP handles NON-conjugate factors by
moment matching. The structural win — EP is NOT a new engine, it's a new
FACTOR TYPE that the existing FactorGraph + runToFixpoint (slice 4) drives.

src/Bayesian/Ep.fs:
- `Normal.erf/pdf/cdf` — standard normal Φ/φ (erf via Abramowitz-Stegun
  7.1.26, max abs err ~1.5e-7; .NET has no built-in erf).
- `Ep.probitProject cavity` — moment-match the tilted N(m,v)·Φ(x) back to
  a Gaussian (GPML eq 3.58): z=m/√(1+v), λ=φ(z)/Φ(z), m̂=m+vλ/√(1+v),
  v̂=v−v²λ(z+λ)/(1+v).
- `Ep.probitFactor variable` — a soft observation "x>0" (likelihood Φ(x))
  as a Factor<Gaussian>: ComputeMessages = project(cavity·Φ)/cavity, where
  the incoming var→factor message IS the cavity (sum-product variable rule).
  Improper/uniform cavity → flat message (no NaN before the prior propagates).

EP cycle = cavity (divide) → tilt (×factor) → project (moment-match) →
divide. cavity/divide are slice 2; project is the new bit; the loop is
slice 4's runToFixpoint.

tests/Bayesian.Tests/Ep.Tests.fs (4 tests, all pass):
- Normal cdf/pdf accurate (Φ(0)=.5, Φ(1.96)≈.975, φ(0)≈.39894)
- probit projection matches NUMERICAL QUADRATURE of N(m,v)·Φ(x) over 4
  (m,v) cases — the moment-match formula is verified, not hardcoded
- EP probit factor in runToFixpoint converges to the moment-matched
  posterior (≈N(0.564,0.682), verified vs quadrature; mean shifts positive)
- improper-cavity → flat message (no NaN)

Spec (clean-room, papers): Minka 2001 (EP); Rasmussen-Williams GPML §3.6.
dotnet build -c Release: 0 warnings. dotnet test: 4/4 pass.

Slices: 1 Vector/Wall → 2 message algebra → 3 factor graph → 4 BP fixpoint
(all merged) → 5 EP (this) → 6 seed CE. Refs 081KT2T2J0008QG0R000S7GHQ8.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `a713034d31a2545507afeda7ccdd0f77ffcec2ac` -- 2026-06-02T17:00:21Z -- ``

**Touched files (intersect with thread paths):**

- `src/Bayesian/Ep.fs`

**Message:**

```
fix(infernet 081KT2T2J0008QG0R000S7GHQ8 slice 5): stabilize the probit inverse Mills rati…

…o for extreme negative cavities (Codex+Copilot review)

probitProject computed λ = φ(z)/Φ(z) directly. For a strongly-negative
cavity (z ≪ 0) Φ(z) underflows to 0, so λ becomes ∞/NaN, m̂/v̂ go
non-finite, and Gaussian.ofMeanVariance throws. The fixpoint loop can
drift a cavity into that regime, so this is a real robustness bug.

Fix: factor out `inverseMills z` with an asymptotic fallback. When the
naive φ/Φ is non-finite (Φ underflowed), use the lower-tail hazard-rate
series λ(z) ≈ −z − 1/z + 2/z³ (from Φ(z) = φ(z)·[1/w − 1/w³ + 3/w⁵ − …],
w = −z). The unstable branch and the asymptotic branch are complementary
by construction — Φ underflows exactly where the asymptotic is most
accurate — so the projection stays finite, proper, and variance-reduced
for arbitrarily extreme cavities.

Regression test: cavities (-15,1)/(-40,0.25)/(-8,4) now project to a
proper, finite, variance-reduced Gaussian with mean pushed toward the
x>0 constraint (previously threw). Existing quadrature cross-checks
unchanged. 0 warnings; 5/5 EP tests pass.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `edcadc0e3d025a6445065f36c4c188e98c1ea693` -- 2026-06-02T17:03:40Z -- ``

**Touched files (intersect with thread paths):**

- `src/Bayesian/Ep.fs`

**Message:**

```
fix(infernet 081KT2T2J0008QG0R000S7GHQ8 slice 5): avoid v² overflow in the probit varianc…

…e update for very broad cavities (Codex review)

v̂ = v − v²λ(z+λ)/(1+v) computed v*v first, which overflows to ∞ for a
broad-but-valid cavity (e.g. v = 1e308 — finite + positive, so the public
Gaussian.ofMeanVariance constructor accepts it), making probitProject
throw on an input the public API admits. Factored to the algebraically
identical v·(1 − (v/(1+v))·λ(z+λ)); the v/(1+v) factor stays ≤ 1 so the
intermediate stays in range. Symmetric robustness to the underflow fix
(extreme-variance tail vs extreme-mean tail).

Regression: cavities (0,1e308)/(±1e150,1e300) now project finite/proper/
variance-reduced. 0 warnings; 6/6 EP tests pass.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```
