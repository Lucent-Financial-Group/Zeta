# PR #17001 -- docs(ops): findings register for the first metal bring-up

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 17001 |
| Title | docs(ops): findings register for the first metal bring-up |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-09-08T01:13:35Z |
| Merged at | 2026-09-08T01:32:55Z |
| Merge commit SHA | `8f73d046326fa2cedef41965d2907393e07562a2` |
| Branch | `shadow/first-metal-findings-register` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/17001 |
| Changed files | 3 |
| Additions / deletions | +148 / -0 |

## Description

Four adversarial reviews ran against `main` ahead of the first reformat-and-boot: USB/first-boot, chart coverage, secrets/custody, and an adversarial pass over the branch/tag census. This is the one place to work them off — otherwise they evaporate into a chat log.

**Register discipline**, stated in the file: a row leaves only when the defect is CLOSED **and** something would go red if it came back. A fix that landed with no guard stays, marked `FIXED-UNGUARDED`. Today produced both a resolved blocker still being reported *and* a stale claim outliving its measurement — both directions cost real work.

## The three that would cost the machine

1. **No ISO has ever been built from the current 26.05 flake** — lane red for 14 runs — and the freshness gate structurally cannot notice: it diffs a path that doesn't exist (a check that cannot fail) and omits the flake that changed.
2. **The ABORT default exits 0**, so a safe refusal becomes "Install complete" → reboot → loop. The trigger is `foreign-data`, which a reformat always takes.
3. **No UEFI preflight** — a legacy-boot mistake surfaces at `bootctl install`, after the wipe.

## The one that outranks them for the cluster

**Gatekeeper's namespace webhook is fail-CLOSED** against a file whose ten-line rationale documents the opposite, citing an incident that already bricked a cluster. Three webhooks ship; two are set to `Ignore`; the third gates `namespaces CREATE/UPDATE` cluster-wide and defaults to `Fail`.

## Branch cleanup is BLOCKED — and the blocker is the safety net

**The off-site mirror has been disabled since 2026-08-29.** Origin holds 442 archive tags, the fork 359 — and the two namespaces that matter most are at **zero** on the fork: the `2026-09-03` sweep (49 → 0) and `archive/experiments/*`, which running research code reads **by name** (16 → 0).

So the preservation net exists on **origin only**, and every ref created since 2026-08-28 has no off-site copy. Re-enabling doesn't fix it retroactively — the mirror pushes `--prune --force`, so it *propagates* deletions.

Five ordered preconditions are recorded before any ref is deleted, plus one wrong "merged-safe" call found (`lumen/fix-heartbeat-actionlint` is **not** on main) and two refs that must never appear in a deletion set.

Nothing was deleted, changed, or fixed by this commit. It is measurement.

Task: 081M1Z96YAE087G0R000G91WCA

Agency-Signature-Version: 1
Agent: shadow
Agent-Runtime: Claude Code
Agent-Model: Claude Opus 5
Credential-Identity: AceHack
Credential-Mode: shared
Human-Review: not-implied-by-credential
Human-Review-Evidence: none
Action-Mode: autonomous-fail-closed
Task: 081M1Z96YAE087G0R000G91WCA

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 0 |
| Resolved threads | 0 |
| Unresolved threads | 0 |
| Total review comments | 0 |
| Total fix commits (touching thread paths) | 0 |

## Review threads

(no review threads on this PR)

## Fix commits (touching thread paths)

(no commits in this PR touched files anchored by review threads)
