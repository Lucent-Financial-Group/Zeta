# PR #6416 -- feat(ace): slice 5.4 — lockfile ergonomics (ace update + --locked + leaf-lock)

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 6416 |
| Title | feat(ace): slice 5.4 — lockfile ergonomics (ace update + --locked + leaf-lock) |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-06-01T12:33:24Z |
| Merged at | 2026-06-01T12:43:19Z |
| Merge commit SHA | `3bd26e9831487ac87cd357ac9efed261b72307e3` |
| Branch | `otto-windows/ace-slice5.4-impl-2026-06-01` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/6416 |
| Changed files | 6 |
| Additions / deletions | +1004 / -34 |

## Description

## Ace slice 5.4 — lockfile ergonomics (081KT07NV0008QG0R002GV3MXW / 081KT07NV0008QG0R0028AAV0E / 081KT07NV0008QG0R003VDHWWG)

Builds on slice 5.3 (lockfile `--frozen`/`--lockfile`, merged #6410). All
**additive** — no solver change, no lock-format change (`format_version`
stays `1`). Spec #6412 (+ preflight fix-forward #6414, merged); plan
`0f2b0cff7`.

### What ships

1. **`ace update <root>`** (081KT07NV0008QG0R002GV3MXW core) — re-solve within ranges + rewrite
   `./ace.lock`, **lock-only** (never extracts / `installPackage`). Runs the
   **same integrity preflight as install** (content_hash + store-key
   collision + `validatePackagePaths` over the resolved graph) **before**
   writing the lock; lock write failure is a **hard error** (the whole point
   of `update` is the lock). Leaf root → writes a leaf lock.

2. **`ace install --locked`** (081KT07NV0008QG0R0028AAV0E) — solve fresh, build the lockfile,
   compare against the on-disk lock via `lockfilesEqual`; **drift → refuse
   installing nothing** (`run ace update`); match → proceed through the
   existing preflight + extract. Mutually exclusive with `--frozen`
   (parse-time error). Distinct from `--frozen`: `--locked` *verifies the
   lock matches a fresh solve*; `--frozen` *replays the lock
   registry-independently*.

3. **Leaf-install lockfiles** (081KT07NV0008QG0R003VDHWWG leaf piece) — leaf (no-dep) installs
   now write a `buildLeafLockfile` (`nodes: []`); `--frozen` leaf reads +
   drift-gates the root; `--locked` leaf compares. Default leaf write is a
   warning (install already succeeded), matching the graph path.

### Preflight-before-write (per #6414)

`resolve()` succeeding ≠ safe to materialize — unsafe-path / store-collision
graphs can solve. So `ace update` preflights the resolved graph **before**
writing the lock, refusing to hand `--frozen` a lock that replays into an
unsafe state. This is the corrected design from Codex's #6412 review,
shipped to the spec on main via **#6414**.

### Verification

- `bun test tools/ace/` → **210 pass / 0 fail** (17 new: 11 lockfile helper
  + 6 ace integration), `bun --bun tsc --noEmit` → **0**, markdownlint clean,
  commit canary `git ls-tree HEAD | wc -l` = **67** throughout.
- Subagent-driven build (7 TDD tasks); each group spec- + quality-reviewed;
  final holistic review clean on correctness + scope (format unchanged,
  update never extracts, `--locked` refuses-without-installing,
  mutual-exclusion enforced at parse).

### Deferred (backlog)

081KT07NV0008QG0R002GV3MXW `--package <name>` single-bump · 081KT07NV0008QG0R003VDHWWG alphabetical node ordering +
partial-merge · 081KT07NV0008QG0R000SJ34AK remote registry (slice 6) · 081KT07NV0008QG0R003659TWT single-fetch cache.

🤖 Generated with [Claude Code](https://claude.com/claude-code)


## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | true |
| Total threads | 7 |
| Resolved threads | 7 |
| Unresolved threads | 0 |
| Total review comments | 7 |
| Total fix commits (touching thread paths) | 9 |

## Review threads

### Thread 1 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GHZg3`
- **Path**: `tools/ace/ace.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-06-01T12:35:17Z on `tools/ace/ace.ts`:558 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Preflight leaf updates before writing locks**

  When `ace update` is run on a leaf package whose `files` contain an unsafe path such as `../escape` and the package otherwise passes the content/signature checks, this branch writes the lockfile without running `validatePackagePaths`/`preflightGraph`. The graph update path refuses these before writing, and `installPackage` would later reject the same package, so `update` can now commit an unreplayable leaf lock that `--frozen` cannot install; run the same preflight on leaf packages before `writeFileSync`.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 2 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GHamG`
- **Path**: `tools/ace/ace.ts`
- **Replies**: 0

**Initial comment:**

- **`github-advanced-security[bot]` (bot)** at 2026-06-01T12:36:24Z on `tools/ace/ace.ts`:548 (association: NONE)

  <pre>
  ## CodeQL / Network data written to file

  Write to file system depends on [Untrusted data](1).

  [Show more details](https://github.com/Lucent-Financial-Group/Zeta/security/code-scanning/140)
  </pre>

### Thread 3 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GHamJ`
- **Path**: `tools/ace/ace.ts`
- **Replies**: 0

**Initial comment:**

- **`github-advanced-security[bot]` (bot)** at 2026-06-01T12:36:24Z on `tools/ace/ace.ts`:558 (association: NONE)

  <pre>
  ## CodeQL / Network data written to file

  Write to file system depends on [Untrusted data](1).

  [Show more details](https://github.com/Lucent-Financial-Group/Zeta/security/code-scanning/141)
  </pre>

### Thread 4 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GHamU`
- **Path**: `tools/ace/ace.ts`
- **Replies**: 0

**Initial comment:**

- **`github-advanced-security[bot]` (bot)** at 2026-06-01T12:36:25Z on `tools/ace/ace.ts`:757 (association: NONE)

  <pre>
  ## CodeQL / Network data written to file

  Write to file system depends on [Untrusted data](1).

  [Show more details](https://github.com/Lucent-Financial-Group/Zeta/security/code-scanning/142)
  </pre>

### Thread 5 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GHcPy`
- **Path**: `tools/ace/lockfile.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-01T12:37:55Z on `tools/ace/lockfile.ts`:85 (association: NONE)

  <pre>
  Docstring is a bit misleading: canonical serialization normalizes *object key* order, but array order (e.g., `nodes`) is still significant. Clarifying this avoids readers assuming `lockfilesEqual` is order-insensitive across nodes.
  </pre>

### Thread 6 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GHcQ4`
- **Path**: `tools/ace/ace.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-01T12:37:56Z on `tools/ace/ace.ts`:545 (association: NONE)

  <pre>
  The inline comment references "Codex #6412", which is a review/tool provenance detail rather than a stable in-repo reference. Consider referencing the fix-forward PR/spec instead so the comment remains meaningful over time.
  </pre>

### Thread 7 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GHcRd`
- **Path**: `tools/ace/ace.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-01T12:37:56Z on `tools/ace/ace.ts`:536 (association: NONE)

  <pre>
  `dependencies` is untrusted JSON; using a truthy check + `.length` will treat non-array values (e.g., a string) as a dependency graph and route through solve/resolve unnecessarily. Restrict the graph path to an actual array.
  </pre>

## Fix commits (touching thread paths)

### `55d140c688625837d12b36e3cce74ebee1c23a17` -- 2026-06-01T12:19:38Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/ace/lockfile.ts`

**Message:**

```
feat(ace): lockfilesEqual + buildLeafLockfile (slice 5.4 task 1)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `3f8f945dc59d3301949270cb8f6f35c49ea351a1` -- 2026-06-01T12:22:13Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/ace/ace.ts`

**Message:**

```
refactor(ace): extract preflightGraph helper (slice 5.4 task 2)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `6385afb827ec9caf383674583cd57977f289563a` -- 2026-06-01T12:23:02Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/ace/ace.ts`

**Message:**

```
feat(ace): --locked flag + --frozen mutual-exclusion (slice 5.4 task 3)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `6b53d590ddca870a00aa53eda8dc0bc86f2bd127` -- 2026-06-01T12:24:14Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/ace/ace.ts`

**Message:**

```
feat(ace): install --locked compare-before-extract (slice 5.4 task 4)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `4b1d9efdae9e05a5cf809f4376001604b90be90f` -- 2026-06-01T12:26:13Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/ace/ace.ts`

**Message:**

```
feat(ace): leaf-install lockfiles + --frozen/--locked leaf handling (…

…slice 5.4 task 5)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `934d6f25e1a6487e84567802ac4cd4529b66c52b` -- 2026-06-01T12:27:22Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/ace/ace.ts`

**Message:**

```
feat(ace): ace update command — re-solve + rewrite lock, preflight be…

…fore write (slice 5.4 task 6)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `daf6dc69d1b46ab6bccb2829f6e1c802312ed360` -- 2026-06-01T12:28:36Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/ace/ace.ts`

**Message:**

```
docs(ace): document ace update + --locked + leaf-lock (slice 5.4 task 7)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `4bbf71f7bc04af2bc95d07363432be5020306895` -- 2026-06-01T12:37:41Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/ace/ace.ts`

**Message:**

```
fix(ace): preflight leaf updates before writing locks (Codex #6416 P2)

`ace update` on a leaf (no-dep) package wrote a leaf lockfile without
running validatePackagePaths, while the graph update path preflights
(preflightGraph). A leaf whose files carry an unsafe path (e.g. ../escape)
that otherwise passes content/signature checks could thus get a lock that
installPackage and --frozen would later reject — an unreplayable leaf lock.

Add the same path-safety guard to the leaf branch before the lock write,
matching the graph path's preflight (line 108) and the leaf install path's
own validatePackagePaths (line 644). +1 regression test (leaf unsafe path →
refuse, no lock written). 211 pass / 0 fail, tsc 0.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `33ef96b580657d8fbbc3b5d49535303c92aa21ae` -- 2026-06-01T12:40:08Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/ace/ace.ts`
- `tools/ace/lockfile.ts`

**Message:**

```
fix(ace): harden untrusted dependencies guard + doc/comment clarity (…

…Copilot #6416)

Copilot review on #6416:
- ace.ts: `dependencies` is untrusted JSON — a truthy + `.length` check treated
  non-array values (e.g. a string) as a dependency graph and routed through
  solve/resolve. Guard with Array.isArray. Applied to BOTH the new `update`
  path AND the identical pre-existing `install` guard (avoid leaving the same
  latent hole; same class-fix pattern as slice 5.2). +1 regression test
  (string dependencies → leaf path, empty-nodes lock, no crash).
- lockfile.ts: clarify `lockfilesEqual` docstring — canonical JSON normalizes
  object key order, but node array order IS significant.
- ace.ts: reword inline comments to stable spec/PR refs (spec #6412,
  fix-forward #6414) and drop a circular self-reference.

212 pass / 0 fail, tsc 0.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```
