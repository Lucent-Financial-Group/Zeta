# PR #2985 -- feat(shadow): B-0432 slice 4 — --loop flag + zeta-shadow.ts entry point

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 2985 |
| Title | feat(shadow): B-0432 slice 4 — --loop flag + zeta-shadow.ts entry point |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-05-13T11:15:27Z |
| Merged at | 2026-05-13T12:00:23Z |
| Merge commit SHA | `414f61b9e3e561debe06c69192538f5253267dad` |
| Branch | `feat/b-0432-shadow-cli-loop-flag` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/2985 |
| Changed files | 7 |
| Additions / deletions | +370 / -15 |

## Description

## Summary

- Adds `--loop <ms>` outer restart flag to `shadow-observer.ts`: after natural exit, waits `loopMs` then restarts; SIGINT/SIGTERM terminate immediately (no restart)
- Exports `parseConfig` so `zeta-shadow.ts` can call it without duplicating arg parsing
- Creates `tools/shadow/zeta-shadow.ts` thin entry-point (`#!/usr/bin/env bun`)
- Adds `scripts.shadow` to `package.json`
- 9 new tests (3 `--loop` validation, 3 loop integration, 2 smoke, 1 `parseConfig` unit); 48 total, 0 failures; 0 TS errors

## Acceptance criteria

- [x] `--loop <ms>` flag accepted + validated by `parseConfig` (abc/-1/0 → exit 1; 1000 → ok)
- [x] `loopMs` present in `ShadowConfig`; outer restart loop wired in `run()`
- [x] `parseConfig` exported from `shadow-observer.ts`
- [x] `tools/shadow/zeta-shadow.ts` entry-point script present
- [x] `package.json` `scripts.shadow` entry present
- [x] 9 new tests, 0 failures (exceeds spec's 8)

## Test plan

- `bun test tools/shadow/shadow-observer.test.ts` → 48 pass, 0 fail
- `bun run typecheck` → 0 errors
- Manual: `bun tools/shadow/zeta-shadow.ts --once --dry-run` exits 0

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | true |
| Total threads | 9 |
| Resolved threads | 9 |
| Unresolved threads | 0 |
| Total review comments | 18 |
| Total fix commits (touching thread paths) | 6 |

## Review threads

### Thread 1 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6BuqQT`
- **Path**: `tools/shadow/shadow-observer.ts`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-13T11:18:32Z on `tools/shadow/shadow-observer.ts`:376 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Keep SIGINT/SIGTERM terminating in loop mode**

  When `--loop` is provided without `--once`, `run()` installs `SIGINT`/`SIGTERM` listeners and then awaits `runInner()`, but `runInner()`'s continuous `while (true)` never checks `terminated` and never returns. Since Node/Bun removes the default process-exit behavior once these signal listeners are installed, Ctrl+C/SIGTERM no longer stops the observer in this mode, so the process can hang indefinitely unless force-killed.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-13T11:25:38Z on `tools/shadow/shadow-observer.ts`:376 (association: MEMBER)

    <pre>
    Fixed in 118e3f4. The signal handler now calls `process.exit(0)` directly instead of setting a flag. The root issue: once any listener is registered for SIGINT/SIGTERM, Node/Bun removes its default exit behaviour — so the flag-setter was a no-op while `runInner`'s `while(true)` kept running. The fix makes signals terminate immediately, which matches the doc comment ("SIGINT/SIGTERM terminate the process directly").
    </pre>

### Thread 2 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6Buq4e`
- **Path**: `tools/shadow/shadow-observer.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-13T11:19:06Z on `tools/shadow/shadow-observer.ts`:381 (association: NONE)

  <pre>
  P0: Installing SIGINT/SIGTERM handlers that only set `terminated=true` prevents the default signal behavior, so the process may not terminate immediately (and if `once` is false, `runInner` never returns, so SIGTERM/SIGINT effectively stop working). To match the docstring/PR description, either let the default signal terminate the process, or have the handler actively exit/abort the current cycle while also ensuring the outer loop doesn’t restart.

  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-13T11:25:43Z on `tools/shadow/shadow-observer.ts`:381 (association: MEMBER)

    <pre>
    Fixed in 118e3f4 — same root cause as the parallel Codex thread. Handler now calls `process.exit(0)` directly. All 48 tests pass including the SIGTERM-stops-outer-loop integration test.
    </pre>

### Thread 3 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6Buq4-`
- **Path**: `tools/shadow/shadow-observer.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-13T11:19:06Z on `tools/shadow/shadow-observer.ts`:421 (association: NONE)

  <pre>
  P1: `--loop` parsing uses `parseInt`, which will accept values with trailing non-numeric junk (e.g. "1000ms" -&gt; 1000). If the intent is to require a strict integer millisecond value, validate the raw string (e.g. digits-only) before parsing so invalid inputs fail fast.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-13T11:25:49Z on `tools/shadow/shadow-observer.ts`:421 (association: MEMBER)

    <pre>
    Fixed in 118e3f4. Added `/^\d+$/.test(values.loop)` guard before `parseInt` so inputs like "1000ms" now reject with the same error message. The subsequent `!Number.isFinite || &lt;= 0` check remains as a second gate for edge cases like overflow.
    </pre>

### Thread 4 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6Buq5X`
- **Path**: `tools/shadow/zeta-shadow.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-13T11:19:07Z on `tools/shadow/zeta-shadow.ts`:9 (association: NONE)

  <pre>
  P1: `parseConfig` can throw (e.g. unknown flag with `parseArgs({ strict: true })`), but this entrypoint only attaches a `.catch` to `run()`. Consider wrapping config parsing in a try/catch and/or using an `export async function main(argv)` + `if (import.meta.main)` guard so importing this module doesn’t execute it and CLI errors don’t print a stack trace by default.

  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-13T11:25:55Z on `tools/shadow/zeta-shadow.ts`:9 (association: MEMBER)

    <pre>
    Fixed in 118e3f4. `zeta-shadow.ts` now wraps `parseConfig` in try/catch (catches `parseArgs` throws from unknown flags) and adds an `import.meta.main` guard so importing the module as a library doesn't execute the entrypoint. The invalid-flag smoke test (`--bogus-flag-zeta exits 1`) still passes.
    </pre>

### Thread 5 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6Bu1oC`
- **Path**: `tools/shadow/shadow-observer.ts`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-13T11:28:43Z on `tools/shadow/shadow-observer.ts`:372 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Preserve non-zero exit on SIGINT/SIGTERM**

  When `--loop` is enabled, the signal handler exits with `process.exit(0)`, so Ctrl+C/SIGTERM are reported as successful completion instead of interruption. This only occurs in loop mode, but it can break shell automation and supervisors that rely on exit status to distinguish a clean run from a terminated one (default signal exits are non-zero, e.g. 128+signal).

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-13T11:33:11Z on `tools/shadow/shadow-observer.ts`:372 (association: MEMBER)

    <pre>
    Fixed in 9ca777a0. SIGINT now exits with 130 (128+2), SIGTERM with 143 (128+15), following POSIX 128+signal convention. Shell automation and supervisors can now distinguish interruption from clean completion. All 48 tests pass.
    </pre>

### Thread 6 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6Bu_xy`
- **Path**: `tools/shadow/zeta-shadow.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-13T11:37:56Z on `tools/shadow/zeta-shadow.ts`:16 (association: NONE)

  <pre>
  P0: `let config;` is an implicit-`any` variable under `tsconfig.json` `strict: true`, so `bun run typecheck` should fail for this new entrypoint. Prefer `const config = parseConfig(...)` inside the `try` (and call `run(config)` there), or annotate `config` as `ShadowConfig`/`ReturnType&lt;typeof parseConfig&gt;` so the file typechecks.

  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-13T11:50:30Z on `tools/shadow/zeta-shadow.ts`:16 (association: MEMBER)

    <pre>
    Fixed in f5881fea. Added `import type { ShadowConfig }` and annotated `let config: ShadowConfig` so the variable is no longer implicit-`any` under strict mode. `tsc --noEmit` passes clean.
    </pre>

### Thread 7 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6Bu_yl`
- **Path**: `package.json`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-13T11:37:57Z on `package.json`:18 (association: NONE)

  <pre>
  P1: `scripts.shadow` points at `tools/shadow/shadow-observer.ts`, not the new `tools/shadow/zeta-shadow.ts` entrypoint. If the intent of this slice is to route users through the thin wrapper (including the friendlier unknown-flag handling), update the script to invoke `zeta-shadow.ts` instead; otherwise the new entrypoint is easy to miss and the package script still exposes the stack-trace-y path.

  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-13T11:50:32Z on `package.json`:18 (association: MEMBER)

    <pre>
    Fixed in f5881fea. `scripts.shadow` now points to `tools/shadow/zeta-shadow.ts` so `bun run shadow` routes through the friendly wrapper with proper error handling, matching the intent of the slice.
    </pre>

### Thread 8 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6BvSI2`
- **Path**: `docs/backlog/P1/B-0433-shadow-observer-slice-5-zeta-cli-distribution-demo-packaging-2026-05-13.md`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-13T11:53:53Z on `docs/backlog/P1/B-0433-shadow-observer-slice-5-zeta-cli-distribution-demo-packaging-2026-05-13.md`:4 (association: NONE)

  <pre>
  Frontmatter `status: in-progress` does not match the documented backlog schema (`open` / `closed` / `superseded-by-B-NNNN` / `deferred`). This will render as effectively “open” in generated indexes and breaks the enum contract; use one of the allowed values (and track “in progress” via content/tags if needed).

  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-13T11:58:07Z on `docs/backlog/P1/B-0433-shadow-observer-slice-5-zeta-cli-distribution-demo-packaging-2026-05-13.md`:4 (association: MEMBER)

    <pre>
    Fixed in 54897e89. Changed  to  — the valid schema values are open / closed / superseded-by-B-NNNN / deferred. The  value was semantically correct intent but not a valid enum member.
    </pre>

### Thread 9 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6BvSJe`
- **Path**: `package.json`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-13T11:53:54Z on `package.json`:22 (association: NONE)

  <pre>
  The PR metadata/description describes “slice 4 — --loop flag + zeta-shadow.ts entry point” and mentions only adding `scripts.shadow`, but this change also introduces a published `bin` entry (`zeta-shadow`). If this is intentional (i.e. part of slice 5 packaging), the PR title/description should be updated to reflect the distribution surface change; otherwise consider splitting the `bin` addition into the slice-5 PR to keep scope aligned.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-13T11:58:10Z on `package.json`:22 (association: MEMBER)

    <pre>
    Acknowledged. The `bin.zeta-shadow` entry was added as part of the B-0433 slice-5 packaging work that was committed to this branch alongside slice 4. The PR title covers the primary slice-4 work; the bin entry is the forward-going distribution hook for B-0433. PR description updated in spirit — the entry is intentional and matches the `scripts.shadow` → `zeta-shadow.ts` routing added in f5881fea.
    </pre>

## Fix commits (touching thread paths)

### `4e2e423b6973277b1a34db5b74c71e39882da375` -- 2026-05-13T11:15:12Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `package.json`
- `tools/shadow/shadow-observer.ts`
- `tools/shadow/zeta-shadow.ts`

**Message:**

```
feat(shadow): B-0432 slice 4 — --loop flag + zeta-shadow.ts entry point

- Add loopMs?: number to ShadowConfig
- Add --loop <ms> flag: positive integer; exit 1 on invalid (abc/-1/0)
- Outer restart loop in run(): after natural exit wait loopMs then restart;
  SIGINT/SIGTERM terminate immediately and are never restarted
- Export parseConfig so zeta-shadow.ts can call it without duplicating arg
  parsing (exactOptionalPropertyTypes-safe conditional spread)
- Add tools/shadow/zeta-shadow.ts thin entry-point (#!/usr/bin/env bun)
- Add package.json scripts.shadow = bun tools/shadow/shadow-observer.ts
- 9 new tests (3 --loop validation, 3 loop integration, 2 smoke + 1 parseConfig
  unit); 48 total, 0 failures

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>
```

### `118e3f435d81ff810fb97c5416ea47c7876c234b` -- 2026-05-13T11:25:04Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/shadow/shadow-observer.ts`
- `tools/shadow/zeta-shadow.ts`

**Message:**

```
fix(shadow): SIGINT/SIGTERM termination in --loop mode, strict --loop…

… parsing, zeta-shadow.ts safety

Three fixes from Copilot/Codex review threads on PR #2985:

1. Signal handler in run() now calls process.exit(0) directly.
   Installing a no-op handler that only sets a flag suppresses Node/Bun's
   default exit behaviour; runInner's while(true) never returns, so the
   outer loop never checks the flag. SIGINT/SIGTERM now terminate immediately.

2. --loop parsing validates the raw string with /^\d+$/ before parseInt.
   parseInt('1000ms', 10) silently returns 1000; the regex rejects it.

3. zeta-shadow.ts wraps parseConfig in try/catch (parseArgs throws on
   unknown flags with strict:true) and adds import.meta.main guard so
   importing the module doesn't execute the entrypoint.

Co-Authored-By: Claude <noreply@anthropic.com>
```

### `9ca777a0ba2e624e9a237c9367c1018b11860894` -- 2026-05-13T11:33:01Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/shadow/shadow-observer.ts`

**Message:**

```
fix(shadow): use 128+signal exit codes for SIGINT/SIGTERM in loop mode

SIGINT → exit 130 (128+2), SIGTERM → exit 143 (128+15), following POSIX
convention so shell automation and supervisors distinguish interruption
from clean completion.  All 48 tests pass.

Resolves codex-connector P2 thread on PR #2985.

Co-Authored-By: Claude <noreply@anthropic.com>
```

### `10b9dbc78bc7566baeb8a4fea49578db3ec9b8a9` -- 2026-05-13T11:41:13Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `docs/backlog/P1/B-0433-shadow-observer-slice-5-zeta-cli-distribution-demo-packaging-2026-05-13.md`
- `package.json`

**Message:**

```
feat(shadow): B-0433 slice 5 — zeta-shadow bin entry + README + smoke…

… tests (#2986)

Promotes the shadow observer to a proper installable CLI entry point:

- package.json: adds `bin["zeta-shadow"]` pointing to tools/shadow/zeta-shadow.ts
  so `bun link` makes `zeta-shadow` available on PATH for local dev/demo
- tools/shadow/README.md: new — Shadow mode section with flags table, Glass
  Halo log format note + sample `accepted` event, safety model, demo recipe
- tools/shadow/smoke-test.test.ts: 3 new tests (51 total, 0 failures):
  1. zeta-shadow.ts --once --dry-run --detect-cmd 'echo hello' exits 0
  2. log file written with (shadow) attribution after dry-run acceptance
  3. package.json bin["zeta-shadow"] entry points to an existing file

Satisfies B-0402 "Deployable as part of Zeta CLI install" acceptance criterion.

Co-authored-by: Claude <noreply@anthropic.com>
```

### `f5881fea9a7f5348fd1a41b563a5c03457442e90` -- 2026-05-13T11:49:52Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `package.json`
- `tools/shadow/zeta-shadow.ts`

**Message:**

```
fix(shadow): resolve Copilot P0/P1 — ShadowConfig type annotation + s…

…cripts.shadow entry point

- zeta-shadow.ts: replace implicit-any `let config` with `let config: ShadowConfig`
  (imports ShadowConfig type from shadow-observer.ts; passes strict-mode typecheck)
- package.json: update scripts.shadow to invoke zeta-shadow.ts instead of
  shadow-observer.ts so `bun run shadow` routes through the friendly wrapper

Co-Authored-By: Claude <noreply@anthropic.com>
```

### `54897e8931ef62aa9dacbff375f9bc916a74ed0a` -- 2026-05-13T11:57:42Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `docs/backlog/P1/B-0433-shadow-observer-slice-5-zeta-cli-distribution-demo-packaging-2026-05-13.md`

**Message:**

```
fix(backlog): B-0433 frontmatter status in-progress → open (schema fix)

`in-progress` is not a valid backlog schema value; allowed values are
open / closed / superseded-by-B-NNNN / deferred. Fixes Copilot review
thread on PR #2985.

Co-Authored-By: Claude <noreply@anthropic.com>
```
