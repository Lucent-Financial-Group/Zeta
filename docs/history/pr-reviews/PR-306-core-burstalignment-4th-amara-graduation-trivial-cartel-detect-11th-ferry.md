# PR #306 -- core: burstAlignment — 4th Amara graduation (trivial-cartel-detect, 11th ferry)

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 306 |
| Title | core: burstAlignment — 4th Amara graduation (trivial-cartel-detect, 11th ferry) |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-04-24T06:06:33Z |
| Merged at | 2026-04-24T07:04:06Z |
| Merge commit SHA | `cdcc0d134598a4741bd5d7008e2883329484c283` |
| Branch | `feat/burst-alignment-amara-graduation-4` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/306 |
| Changed files | 2 |
| Additions / deletions | +149 / -0 |

## Description

## Summary

Fourth Amara graduation — ships the burst-detection primitive from Aaron's differentiable firefly network design (11th ferry PR #296). This is the direct answer to Aaron Otto-111: *"Are you able to import the ideas and concepts and math from the conversation at some point?"* — yes, continuously, this is the cadence.

## What lands

- `significantLags : (int * double option) array -> double -> int array` — filters profile to lags where `|corr| >= threshold`; skips `None` and non-finite
- `burstAlignment : (int * double option) array -> double -> (int * int) array` — groups significant lags into contiguous runs

## 11th-ferry definition operationalized

Amara §1: *"Firefly detection = identify clusters where ∃ S ⊂ N such that ∀ i,j ∈ S, corr(E_i, E_j) ≫ baseline"*.

This function implements the pair-wise case. Node-set generalization (clusters across many stream pairs) is a future graduation that composes over this one.

## SPOF note (per Otto-106)

Caller-supplied threshold is a sensitivity SPOF. Mitigation: threshold should come from null-hypothesis baseline percentile, not hard-coded. Documented in XML-doc.

## Test plan

- [x] 9 new tests pass (19 total in module)
- [x] Build 0 Warning / 0 Error
- [x] Handles negative-correlation significance (anti-phase coordination)
- [x] Handles None entries without breaking (filtered, contiguity broken)
- [x] Single-lag + multi-run cases tested

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 15 |
| Resolved threads | 13 |
| Unresolved threads | 2 |
| Total review comments | 15 |
| Total fix commits (touching thread paths) | 1 |

## Review threads

### Thread 1 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59TwTN`
- **Path**: `src/Core/TemporalCoordinationDetection.fs`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-04-24T06:09:19Z on `src/Core/TemporalCoordinationDetection.fs`:211 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Normalize lag order before detecting contiguous bursts**

  `burstAlignment` assumes `significant` is already sorted by lag, but the function accepts any `(int * double option) array` and never enforces or documents that precondition. If callers provide a profile assembled out of order (for example, merged from multiple windows), the `lag = runEnd + 1` check will split or merge bursts based on input order rather than actual lag adjacency, producing incorrect burst ranges and potentially corrupting downstream coordination metrics.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 2 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59TwmM`
- **Path**: `src/Core/TemporalCoordinationDetection.fs`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-24T06:09:45Z on `src/Core/TemporalCoordinationDetection.fs`:175 (association: NONE)

  <pre>
  P1: `significantLags` doesn’t validate `threshold`. If a caller passes a negative value, `abs c &gt;= threshold` becomes true for all finite correlations, which is likely an unintended “everything is significant” result. Consider guarding `threshold` (finite and &gt;= 0.0, optionally &lt;= 1.0) and returning an empty array (similar to `crossCorrelationProfile` with negative `maxLag`) or clamping to 0.0; add a test for the chosen behavior.
  </pre>

### Thread 3 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59Twmg`
- **Path**: `src/Core/TemporalCoordinationDetection.fs`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-24T06:09:45Z on `src/Core/TemporalCoordinationDetection.fs`:214 (association: NONE)

  <pre>
  P1: `burstAlignment` assumes `significant` lags are already sorted and unique (it uses `runEnd + 1` contiguity). Since `significantLags` preserves the input `profile` order, an unsorted profile (or duplicates) will produce incorrect run grouping. Consider sorting and de-duplicating `significant` before the loop (or explicitly documenting/encoding the ordering requirement in the API).
  </pre>

### Thread 4 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59Twmq`
- **Path**: `src/Core/TemporalCoordinationDetection.fs`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-24T06:09:45Z on `src/Core/TemporalCoordinationDetection.fs`:200 (association: NONE)

  <pre>
  P1: This new doc comment introduces direct name attribution (“Amara”). Repo standing rules require no contributor names in code/docs/skills (use role references instead) — see `docs/AGENT-BEST-PRACTICES.md` Operational standing rules. Please rewrite this reference to avoid personal names (e.g., refer to the “11th ferry” document/section without naming the author).
  </pre>

### Thread 5 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59UAjZ`
- **Path**: `src/Core/TemporalCoordinationDetection.fs`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-04-24T06:31:56Z on `src/Core/TemporalCoordinationDetection.fs`:174 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Validate threshold is finite and non-negative**

  `significantLags` uses `abs c &gt;= threshold` without validating `threshold`, so a negative threshold makes every finite correlation significant while a `NaN` threshold makes none significant. Because `burstAlignment` is built directly on this output, a misconfigured threshold (e.g., a baseline percentile accidentally computed on signed correlations) silently flips the detector into all-positives or all-negatives and can corrupt downstream coordination metrics. Please guard this path by validating/clamping threshold to a finite range before filtering.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 6 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59UBGN`
- **Path**: `src/Core/TemporalCoordinationDetection.fs`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-24T06:32:43Z on `src/Core/TemporalCoordinationDetection.fs`:175 (association: NONE)

  <pre>
  `significantLags` treats `threshold` as-is; a negative threshold will mark every finite correlation as “significant” (since `abs c &gt;= threshold` becomes trivially true), and `NaN` silently yields an empty result. Consider validating that `threshold` is finite and &gt;= 0.0 (and ideally &lt;= 1.0 for Pearson profiles), or explicitly clamping/normalizing it, to avoid surprising detector behavior.
  </pre>

### Thread 7 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59UBGd`
- **Path**: `src/Core/TemporalCoordinationDetection.fs`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-24T06:32:43Z on `src/Core/TemporalCoordinationDetection.fs`:213 (association: NONE)

  <pre>
  `burstAlignment` assumes the `significant` lags are already sorted ascending and unique; if a caller passes a profile not produced by `crossCorrelationProfile` (or otherwise unsorted), contiguous-run detection will be incorrect. Either sort/deduplicate the lag list before scanning, or document the ordering/uniqueness precondition in the XML docs.
  </pre>

### Thread 8 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59UBGm`
- **Path**: `tests/Tests.FSharp/Algebra/TemporalCoordinationDetection.Tests.fs`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-24T06:32:43Z on `tests/Tests.FSharp/Algebra/TemporalCoordinationDetection.Tests.fs`:206 (association: NONE)

  <pre>
  The implementation claims to skip non-finite correlations, but the new tests don’t cover `NaN`/`Infinity` correlation entries. Add a small test case that includes a non-finite `Some` value and asserts it’s filtered out, so the contract stays enforced.
  </pre>

### Thread 9 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59UBG1`
- **Path**: `src/Core/TemporalCoordinationDetection.fs`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-24T06:32:44Z on `src/Core/TemporalCoordinationDetection.fs`:201 (association: NONE)

  <pre>
  The XML docs here include direct contributor name attribution (e.g., “Amara, §1”). Per `docs/AGENT-BEST-PRACTICES.md` Operational standing rules (around lines 284-290), code/docs should use role references instead of contributor names. Please rewrite this attribution to role-refs (or move it to an allowed historical surface) to stay compliant.
  </pre>

### Thread 10 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59UBG-`
- **Path**: `src/Core/TemporalCoordinationDetection.fs`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-24T06:32:44Z on `src/Core/TemporalCoordinationDetection.fs`:160 (association: NONE)

  <pre>
  The module-level documentation earlier in this file states that only `crossCorrelation`/`crossCorrelationProfile` “ship here” and labels later primitives as future graduations, but this PR adds `significantLags`/`burstAlignment` (and the file already contains additional primitives). Please update the top-of-file/module docs so the exported surface and graduation status described there matches the actual functions present.
  </pre>

### Thread 11 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59ULsR`
- **Path**: `tests/Tests.FSharp/Algebra/TemporalCoordinationDetection.Tests.fs`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-24T06:46:47Z on `tests/Tests.FSharp/Algebra/TemporalCoordinationDetection.Tests.fs`:206 (association: NONE)

  <pre>
  P2: New behavior filters out non-finite correlations (`Double.IsFinite`). Consider adding a test case covering `Some Double.NaN` / `Some Double.PositiveInfinity` in the profile to lock in the expected filtering behavior (and similarly for `burstAlignment`, since it relies on `significantLags`).
  </pre>

### Thread 12 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59ULsq`
- **Path**: `src/Core/TemporalCoordinationDetection.fs`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-24T06:46:47Z on `src/Core/TemporalCoordinationDetection.fs`:175 (association: NONE)

  <pre>
  P1: `significantLags` treats `threshold` as-is; a negative threshold will mark (almost) every finite correlation as significant, and a non-finite threshold (NaN/±Inf) yields surprising results. Consider validating `threshold` (finite and &gt;= 0) or normalizing it (e.g., `abs threshold`) and documenting the behavior for invalid values.
  </pre>

### Thread 13 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59ULs1`
- **Path**: `src/Core/TemporalCoordinationDetection.fs`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-24T06:46:48Z on `src/Core/TemporalCoordinationDetection.fs`:212 (association: NONE)

  <pre>
  P1: `burstAlignment` assumes the `significant` lags are already sorted and unique (it only checks `lag = runEnd + 1`). Since `significantLags` preserves the input order of `profile`, passing an unsorted profile can split/merge runs incorrectly. Consider sorting (and optionally de-duplicating) the lags before scanning, or explicitly documenting/enforcing the sorted-by-lag precondition.
  </pre>

### Thread 14 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM59UbnD`
- **Path**: `src/Core/TemporalCoordinationDetection.fs`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-24T07:06:13Z on `src/Core/TemporalCoordinationDetection.fs`:175 (association: NONE)

  <pre>
  P1: `significantLags` treats `threshold` as an unconstrained double; if a caller passes a negative value, every finite correlation becomes “significant” because `abs c &gt;= threshold` is always true. Consider guarding the accepted range (e.g., require finite `threshold` and `threshold &gt;= 0.0` (and possibly `&lt;= 1.0` given normalized correlations)), and define the behavior for invalid thresholds (empty result vs. clamping) to avoid accidental false positives.
  </pre>

### Thread 15 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM59Ubnk`
- **Path**: `src/Core/TemporalCoordinationDetection.fs`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-24T07:06:13Z on `src/Core/TemporalCoordinationDetection.fs`:195 (association: NONE)

  <pre>
  P1: New XML-doc text introduces contributor name attribution ("Amara") in a non-exempt code surface. Repo operational rules require role-refs instead of contributor names in code/docs/skills (docs/AGENT-BEST-PRACTICES.md §Operational standing rules). Please rewrite this reference/quote intro to avoid personal names (e.g., refer to the source doc/path or a role), and keep attribution in the allowed surfaces only.
  </pre>

## Fix commits (touching thread paths)

### `e4397e4f420ac98695b523e98e753d915c217cab` -- 2026-04-24T06:06:19Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `src/Core/TemporalCoordinationDetection.fs`
- `tests/Tests.FSharp/Algebra/TemporalCoordinationDetection.Tests.fs`

**Message:**

```
core: TemporalCoordinationDetection.burstAlignment + significantLags …

…— 4th graduation

Ships the burst-detection primitive from Aaron's differentiable
firefly network design, completing the "trivial-cartel-detect"
first-order-signal tier from the 11th ferry (PR #296).

Fourth graduation under the Otto-105 cadence; composes on
crossCorrelationProfile (PR #297).

Aaron Otto-111 question: "Are you able to import the ideas and
concepts and math from the conversation at some point?" — yes,
that's exactly what the graduation cadence is for. This ship is
an explicit answer to that question.

Surface:
- TemporalCoordinationDetection.significantLags
    : (int * double option) array -> double -> int array
  Filters a crossCorrelationProfile down to the lags where |corr|
  meets or exceeds a caller-supplied threshold. None entries
  (undefined variance) never count as significant. Non-finite
  correlations filtered by System.Double.IsFinite check.
- TemporalCoordinationDetection.burstAlignment
    : (int * double option) array -> double -> (int * int) array
  Groups significant lags into contiguous runs. Each run reported
  as (startLag, endLag) inclusive. Isolated significant lag at
  n reports as (n, n).

Operational meaning: a run of lags [-2..3] suggests actors that
coordinate across a 5-step window, not a single-point coincidence.
The 11th-ferry signal-model definition (Amara §1):
"Firefly detection = identify clusters where exists S subset of N
such that for all i,j in S, corr(E_i, E_j) >> baseline". This
function operationalises the pair-wise case (two streams); node-
set generalisation (clustering across many stream pairs) is a
separate graduation candidate.

Attribution:
- Concept (differentiable firefly network, trivial-cartel-detect,
  burst-as-first-order-signal) = Aaron's design
- Technical formulation (cluster detection over correlation
  profile, threshold + contiguity semantics) = Amara's
  formalization in 11th ferry
- Implementation = Otto

SPOF (per Otto-106): pure function. The caller-supplied threshold
is a SPOF on detector sensitivity — too high misses real cartels,
too low catches noise. Mitigation: threshold should come from a
null-hypothesis baseline computation (baseline's profile
percentile), not hard-coded. Documented in the XML-doc comment.

Tests (9 new, 19 total in module, all passing):
- significantLags: above-threshold selection; abs value for neg
  correlation; None entries filtered; empty when threshold too high
- burstAlignment: contiguous grouping; non-contiguous split into
  multiple runs; empty when no significant lags; single lag as
  (n, n) run; None entries break contiguity

Composes with:
- PR #297 crossCorrelation / crossCorrelationProfile
- PR #298 (pending) phaseLockingValue — complementary detector
- PR #295 RobustStats.robustAggregate — combining across many
  stream pairs

Next graduation queue:
- antiConsensusGate (10th ferry)
- ModularitySpike (needs graph substrate)
- InfluenceSurface / CartelCostFunction (need multi-node)

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
```
