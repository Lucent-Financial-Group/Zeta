# PR #3040 -- feat(bg/missed-substrate): B-0442 slice 3 — real branch-vs-squash comparator

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 3040 |
| Title | feat(bg/missed-substrate): B-0442 slice 3 — real branch-vs-squash comparator |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-05-13T22:01:45Z |
| Merged at | 2026-05-13T22:11:25Z |
| Merge commit SHA | `e0515f847ddb66ea8ea56c8824142bd23eac0ae1` |
| Branch | `feat/b0442-slice-3-real-branch-vs-squash-comparator-2026-05-13` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/3040 |
| Changed files | 4 |
| Additions / deletions | +393 / -29 |

## Description

## Summary

Closes B-0442 slice 3: real branch-vs-squash comparator. Replaces the slice-4 stub `detectCascade: () => null` with a real comparator that detects the Otto-section-missed-PR-#2980-by-3-min failure class — commits landing on a feature branch AFTER its parent PR squash-merged.

## Algorithm

1. `gh pr view N --json headRefOid,mergeCommit` — fetches the branch HEAD SHA at merge time (the SHA that was squashed)
2. `git fetch --quiet origin <branchName>` — surfaces branch-deleted state (post-merge auto-delete window closed → null; unrecoverable)
3. `git merge-base --is-ancestor <headRefOid> origin/<branchName>` — guards against rebases / force-pushes that would produce a false-positive flood
4. `git log <headRefOid>..origin/<branchName>` — the post-squash drift commits

## Why this design

The cleanest signal for post-squash drift is `headRefOid`: gh returns the branch HEAD SHA at merge time, so anything on the branch beyond that SHA is by definition post-squash drift. Trees and merge-commit SHAs are noisier (other PRs land on main in between); `headRefOid` isolates the per-branch comparison.

Rebase-guard via `--is-ancestor` is important: if someone force-pushes the branch after merge (rare but possible), `headRefOid` is no longer reachable and `git log` would return ALL the new branch's commits → false-positive flood. We detect this case and return null with `branch-rebased` status; the situation needs manual review.

## Architecture

Composes with the existing slice-4 adapter-injection pattern. Tests inject the high-level `Adapters.detectCascade` adapter for bus-publish-path testing; slice 3 adds lower-level `CascadeDetectorAdapters` (`fetchPRRefs` + `compareBranchToMerged` + `now`) so the real-detector logic is also unit-testable without a real repo.

New exported surfaces:
- `realCascadeDetector(pr, adapters)` — pure function
- `REAL_CASCADE_SUB_ADAPTERS` — production gh + git wiring
- `classifyCascadeUrgency(count, mergedAtIso, nowMs)` — urgency classifier
- `MAX_REPORTED_MISSING_COMMITS = 50` — cap on reported commits

`REAL_ADAPTERS.detectCascade` now calls `realCascadeDetector(pr, REAL_CASCADE_SUB_ADAPTERS)`.

## Test plan

- [x] 24/24 pass on `tools/bg/missed-substrate-detector.test.ts` — 7 new tests for `realCascadeDetector` covering drift / no-drift / branch-deleted / branch-rebased / no-merge / gh-error / git-error
- [x] 5 new tests for `classifyCascadeUrgency` across all urgency-band boundaries
- [x] 1 sanity test for `MAX_REPORTED_MISSING_COMMITS`
- [x] 73/73 pass across `tools/bg/` suite
- [x] `bun --bun tsc --noEmit` clean

## What's still pending (per `tools/bg/README.md`)

- Slice 5 for all three bg-services — subscriber agents that react to bus envelopes (closes the foreground-optional gap)
- Slice 6 for all three — launchd / cron registration + integration tests

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 9 |
| Resolved threads | 9 |
| Unresolved threads | 0 |
| Total review comments | 9 |
| Total fix commits (touching thread paths) | 2 |

## Review threads

### Thread 1 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6B50SU`
- **Path**: `tools/bg/missed-substrate-detector.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-13T22:04:45Z on `tools/bg/missed-substrate-detector.ts`:280 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Accept valid Git branch names in branch validator**

  The new branch-name regex is stricter than Git's own branch rules, so valid refs (for example names containing `@` or `+`) are treated as errors and then dropped by `realCascadeDetector` as `null`, which silently misses real cascades for those PRs. This is reproducible with `git check-ref-format --branch` (e.g. `renovate/@scope/pkg` is valid) but the current pattern rejects it.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 2 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6B50SY`
- **Path**: `tools/bg/missed-substrate-detector.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-13T22:04:45Z on `tools/bg/missed-substrate-detector.ts`:297 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Classify only missing-ref errors as branch deletion**

  Using `stderr.includes("not found")` to detect deleted branches is too broad: auth or remote failures (for example `repository ... not found`) will be misclassified as `branch-deleted`, so the detector suppresses diagnostics and reports no cascade instead of surfacing an operational error. In a credential or remote misconfiguration incident this can hide all cascade detections.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 3 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6B523a`
- **Path**: `tools/bg/missed-substrate-detector.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-13T22:08:26Z on `tools/bg/missed-substrate-detector.ts`:298 (association: NONE)

  <pre>
  `git fetch origin &lt;branchName&gt;` fetches the ref into `FETCH_HEAD` and does not reliably update `refs/remotes/origin/&lt;branchName&gt;`, but the later `merge-base` and `log` commands read `origin/&lt;branchName&gt;`. This can compare against a stale or missing remote-tracking ref and miss the post-squash commits this detector is meant to find; fetch into the remote-tracking ref explicitly or compare against `FETCH_HEAD`.
  </pre>

### Thread 4 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6B5233`
- **Path**: `tools/bg/missed-substrate-detector.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-13T22:08:26Z on `tools/bg/missed-substrate-detector.ts`:159 (association: NONE)

  <pre>
  Returning `null` for every non-`ok` ref/compare status collapses sensor failures and manual-review states into the same result as “no drift.” `pollOnce` will then report “no cascades detected” even when `gh pr view`, `git fetch`, or the rebase guard failed, which hides outages instead of surfacing an indeterminate/error tick.

  </pre>

### Thread 5 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6B524S`
- **Path**: `tools/bg/missed-substrate-detector.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-13T22:08:27Z on `tools/bg/missed-substrate-detector.ts`:296 (association: NONE)

  <pre>
  The fallback `stderr.includes("not found")` is too broad for branch-deletion detection. Common remote/auth failures such as “Repository not found” would be classified as `branch-deleted`, causing the detector to suppress an infrastructure problem as if the feature branch was simply gone.

  </pre>

### Thread 6 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6B524y`
- **Path**: `tools/bg/README.md`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-13T22:08:27Z on `tools/bg/README.md`:92 (association: NONE)

  <pre>
  This current-state README is outside the history-surface and roster-mapping carve-outs, so the failure-class reference should not include a persona name. Use a role/generic failure-class label here and keep the named attribution only in backlog/history surfaces.
  </pre>

### Thread 7 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6B525l`
- **Path**: `docs/backlog/P1/B-0442-missed-substrate-cascade-detector-background-service-2026-05-13.md`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-13T22:08:27Z on `docs/backlog/P1/B-0442-missed-substrate-cascade-detector-background-service-2026-05-13.md`:61 (association: NONE)

  <pre>
  This wrapped bullet continuation starts with a literal `+`, which markdownlint can parse as a nested list item using the `+` style instead of the file’s `-` style. Reword so the plus sign is not the first non-space character on the continuation line.
  </pre>

### Thread 8 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6B5258`
- **Path**: `docs/backlog/P1/B-0442-missed-substrate-cascade-detector-background-service-2026-05-13.md`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-13T22:08:28Z on `docs/backlog/P1/B-0442-missed-substrate-cascade-detector-background-service-2026-05-13.md`:76 (association: NONE)

  <pre>
  Adding the slice-3 implementation summary here leaves the same backlog row internally inconsistent: the unchanged “Design sketch” and “Decomposition into implementation slices (TBD)” below still describe a different slice order and pre-implementation state. Update or retire the stale sections in the same doc edit so readers do not get conflicting instructions.
  </pre>

### Thread 9 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6B526c`
- **Path**: `tools/bg/missed-substrate-detector.test.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-13T22:08:28Z on `tools/bg/missed-substrate-detector.test.ts`:198 (association: NONE)

  <pre>
  This new slice-3 suite is nested under the existing top-level `describe` that still says “slice-3 detect is stub,” so the test output now contradicts the behavior being tested. Rename or split the parent suite when adding the real-detector tests.

  </pre>

## Fix commits (touching thread paths)

### `c98a35f3ecc49d492dfdb18b88ac935efbf912ab` -- 2026-05-13T22:01:23Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `docs/backlog/P1/B-0442-missed-substrate-cascade-detector-background-service-2026-05-13.md`
- `tools/bg/README.md`
- `tools/bg/missed-substrate-detector.test.ts`
- `tools/bg/missed-substrate-detector.ts`

**Message:**

```
feat(bg/missed-substrate): B-0442 slice 3 — real branch-vs-squash com…

…parator

Replaces the slice-4 stub `detectCascade: () => null` with a real branch-vs-squash
comparator that detects the Otto-section-missed-PR-#2980-by-3-min failure class:
commits landing on a feature branch AFTER its parent PR squash-merged.

Algorithm:
  1. `gh pr view N --json headRefOid,mergeCommit` — fetches the branch HEAD SHA
     at merge time (the SHA that was squashed)
  2. `git fetch --quiet origin <branchName>` — surfaces branch-deleted state
     (post-merge auto-delete window closed → return null; unrecoverable)
  3. `git merge-base --is-ancestor <headRefOid> origin/<branchName>` — guards
     against rebases / force-pushes that would produce a false-positive flood
  4. `git log <headRefOid>..origin/<branchName>` — the post-squash drift commits

Architecture composes with the existing slice-4 adapter-injection pattern:
- New types: `PRRefsResult`, `BranchCompareResult`, `CascadeDetectorAdapters`
- New exported function: `realCascadeDetector(pr, adapters)` — pure, testable
- New exported constant: `REAL_CASCADE_SUB_ADAPTERS` — production gh + git wiring
- New exported function: `classifyCascadeUrgency(count, mergedAtIso, nowMs)` —
  fresh (<1hr) OR 4+ commits → high; 2-3 commits OR <24hr → medium; else low
- New exported constant: `MAX_REPORTED_MISSING_COMMITS = 50` — caps false-positive
  flood when the branch is being reused for follow-up work (not a cascade)
- REAL_ADAPTERS.detectCascade now calls `realCascadeDetector` with real
  sub-adapters; tests still inject Adapters.detectCascade for the bus-publish path

Test coverage:
- 7 tests for `realCascadeDetector`: drift detected / no-drift / branch-deleted /
  branch-rebased / no-merge / gh-error / git-error
- 5 tests for `classifyCascadeUrgency`: covering all urgency-band boundaries
- 1 test for `MAX_REPORTED_MISSING_COMMITS` sanity bound
- Slice-4 stub-message expectation updated to match real-detector behavior
- 24/24 pass, 73/73 bg-test-suite pass, full tsc --noEmit clean

Updates `tools/bg/README.md` status table (slice 1+2+3+4 live) and the B-0442
backlog row (slice-3 acceptance criteria checked + implementation summary).
Slices 5 (subscriber agents) and 6 (launchd/cron + integration tests) remain
pending — the foreground-optional architectural claim still aspirational per
the README's substrate-honest disclaimer.

Co-Authored-By: Claude <noreply@anthropic.com>
```

### `7349d559aa7f63ec23560add35f50f0ae4939d0c` -- 2026-05-13T22:08:51Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/bg/missed-substrate-detector.ts`

**Message:**

```
fix(bg/missed-substrate): Codex P1 — widen branch-name regex + tighte…

…n branch-deletion stderr check

Two P1 findings from Codex on PR #3040:

1. Branch-name regex was too strict — `/^[A-Za-z0-9._/-]+$/` rejected
   valid git refs containing `@`, `+`, or `=`. Widened to
   `/^[A-Za-z0-9._/+@=\-]+$/` which covers the git-check-ref-format
   character set while remaining shell-safe (the args go through
   spawnSync as an explicit array — no shell interpolation).

2. Branch-deletion stderr check was too broad — `stderr.includes("not found")`
   matched auth / repository-level failures (e.g., "repository ... not found")
   AND deleted-branch errors. The miscategorisation silently dropped
   cascade-detection on real auth errors rather than surfacing them.
   Narrowed to the specific phrase `couldn't find remote ref`, which is
   git's actual output for missing refs and does not match auth failures.

Both fixes preserve test coverage — 24/24 pass.

Co-Authored-By: Claude <noreply@anthropic.com>
```
