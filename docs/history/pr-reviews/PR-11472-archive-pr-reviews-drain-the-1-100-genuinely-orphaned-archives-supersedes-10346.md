# PR #11472 -- archive(pr-reviews): drain the 1,100 genuinely-orphaned archives — supersedes #10346

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 11472 |
| Title | archive(pr-reviews): drain the 1,100 genuinely-orphaned archives — supersedes #10346 |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-08-17T13:42:42Z |
| Merged at | 2026-08-17T14:00:06Z |
| Merge commit SHA | `f3ffbc4b135205646838c44792ac8456a1e38f43` |
| Branch | `shadow/drain-orphaned-pr-review-archives` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/11472 |
| Changed files | 2200 |
| Additions / deletions | +86219 / -0 |

## Description

Supersedes #10346 (CONFLICTING since 2026-08-13). The stale branch's conflicts were not resolved — the set was **re-derived** against current `main`, twice (main moved under it mid-flight).

## The counts (measured, not assumed)

| | count |
|---|---|
| archives proposed by #10346 | **1,229** |
| already on `main`, **byte-identical** | **87** — not carried |
| already on `main`, **different content** | **42** — left alone, `main`'s copy wins |
| genuinely absent from `main` | **1,100** — landed here |
| PR numbers absent by path but archived under a different slug | **0** |

The brief for this work expected "many of the 1,229 may already be on main" and that closing #10346 as superseded might be right. **That is false**: 1,100 of 1,229 (89.5%) had never landed. The archive pipeline has been running continuously, but it archives PRs *going forward* — it never went back for the orphaned batch.

Method: compare blob SHAs of #10346's added paths against `HEAD`'s tree, then re-check the "absent" set by PR *number* to catch a same-PR-different-slug landing (none found).

## The 42 that are NOT carried

`main`'s copies are **later snapshots of the same PRs**, not corrupt ones. Spot-checked two:

- `PR-9105-…` — `main` has 93 more lines (a further review round captured after the orphan branch was cut).
- `PR-9028-…` — `main` lacks dependabot's *"Dependabot is rebasing this PR"* banner, which the older copy still carries.

Three of the 42 (`PR-9177`, `PR-9178`, `PR-9179`) crossed onto `main` **while this PR was open**, from a concurrent drain. Same rule applied on rebase: `main`'s newer capture wins, this branch dropped its older copies. That is why the orphan count moved 1,103 → 1,100.

These are other agents' review records. Overwriting a richer capture with an older one destroys substrate, so the older copy is **dropped**, not merged. None of these paths appears in this diff.

## Content is verbatim — from the drain commit, not #10346's tip

#10346 carries a second commit, `f822e843cb style: mechanical lint auto-heal (MD032/MD026 safe fixer)`, which rewrote **414** of the archives.

`docs/history/pr-reviews/**` is explicitly listed in `.markdownlint-cli2.jsonc`'s `ignores` under the verbatim-preservation carve-out — so that heal normalised content **no linter reads**, and it manufactured conflicts:

- measured against #10346's **tip**: 89 files differ from `main`
- measured against the **raw drain** (`30de1f3842`): **39** differed

50 of the 89 were the auto-heal's own doing. This PR takes content from `30de1f3842`, byte-identical to what the archive tooling produced.

## Shards, not the manifest

#10346 also appends **1,229 lines to `docs/github/prs/manifest.jsonl`**. That is stale by design now: `081KZYMY46P087G0R003S64V2B` demoted the manifest to a **derived index** over `docs/github/prs/shards/<NNN>/<zetaid>.json`, precisely because concurrent archive PRs appending to one file conflict pairwise — the failure that produced the orphan batch in the first place. `pr-archive-on-merge.yml` `git restore`s the manifest so an archive commit never carries it.

So this PR writes **1,100 shards** via the repo's own `writeShard` (upsert on `pr_number`) and leaves `manifest.jsonl` untouched for soraya's serialised writer. **0** of the 1,100 shard keys already existed — nothing was replaced. That is also why this PR did **not** re-conflict on the ordinary pipeline: the pipeline archives newly-merged PRs (11xxx) and this set is all ≤ 10345, so their shard paths are disjoint by construction.

## Verified (run after the final rebase, not claimed)

| check | result |
|---|---|
| `bun src/Core.TypeScript/lint/lint-typescript.ts` | `✓ TypeScript, Prettier, and style checks passed successfully!` |
| `bun src/Core.TypeScript/lint/no-empty-dirs.ts` | `no-empty-dirs: OK (0 allowlisted, 0 flagged)` |
| `bunx markdownlint-cli2 "docs/history/pr-reviews/**/*.md"` | 0 findings (directory is ignored) |
| `check-no-conflict-markers.ts` | `OK: no git merge-conflict markers found in committed files` |
| `bun test src/Core.TypeScript/forge-host/github/` | `168 pass, 0 fail` |
| `derive-pr-manifest.ts` | **exit 1** (derived-index drift, by design on an archive branch) — **not exit 2**, so no unreadable / misfiled / duplicate-keyed shard |
| `audit-agencysignature-main-tip.ts` | `PASS: no regressions detected` |

Passed before the rebase and unaffected by it: `check-archive-header-section33`, `audit-section-33-migration-xrefs --enforce`, `audit-dangling-symlinks` (164/164), `lint-no-manus-commit-leak`, `audit-proof-lineage-binaries`.

Fidelity re-checked mechanically after the final rebase: **1,100/1,100** added blobs byte-identical to `30de1f3842`, **0** of `main`'s 6,923 existing archives modified, and the diff is **add-only** (0 modified, 0 deleted).

## Known red that is not this branch's

`lint-b-refs-resolve.ts` fails on 4 dangling `B-NNNN` refs in `docs/research/2026-08-15-the-archive-tag-corpus-characterized-3209-paths-collapse-to-115-and-the-boundary-of-what-i-checked.md` — a file on `main` that this branch does not touch. None of the 1,100 added files appears in its output.

---

Recommend closing #10346 once this lands.

Agency-Signature-Version: 1
Agent: Otto
Agent-Runtime: Claude Code
Agent-Model: claude-opus-5
Credential-Identity: acehack00@gmail.com
Credential-Mode: shared
Human-Review: not-implied-by-credential
Human-Review-Evidence: none
Action-Mode: human-directed
Task: drain-orphaned-pr-review-archives-supersede-pr-10346
Co-authored-by: Otto <otto@zeta.local>


## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 0 |
| Resolved threads | 0 |
| Unresolved threads | 0 |
| Total review comments | 0 |
| Total fix commits (touching thread paths) | 0 |

## Review threads

(no review threads on this PR)

## Fix commits (touching thread paths)

(no commits in this PR touched files anchored by review threads)
