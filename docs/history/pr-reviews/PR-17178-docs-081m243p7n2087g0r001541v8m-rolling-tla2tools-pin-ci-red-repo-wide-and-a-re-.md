# PR #17178 -- docs(081M243P7N2087G0R001541V8M): rolling tla2tools pin — CI red repo-wide, and a re-pin went stale before its own CI ran

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 17178 |
| Title | docs(081M243P7N2087G0R001541V8M): rolling tla2tools pin — CI red repo-wide, and a re-pin went stale before its own CI ran |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-09-09T22:15:09Z |
| Merged at | 2026-09-10T02:02:15Z |
| Merge commit SHA | `07ee4dbe41e42a768235ded73f9e610d26885b70` |
| Branch | `shadow/rolling-pin-repin-race` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/17178 |
| Changed files | 2 |
| Additions / deletions | +122 / -0 |

## Description

## Filing only — no pin is moved by this PR

`main` CI is red repo-wide. This PR records what happened, with evidence, and names the decision and its owner. It changes no pin and unblocks nothing by itself.

## Timeline (UTC, 2026-09-09) — every row measured, not inferred

| time | event | evidence |
| --- | --- | --- |
| 18:42 | `0985a2ae9a` (#17154) de-vendors `src/Core.TLA/tla2tools.jar` onto `rolling=tlaplus-v1.8.0-prerelease`, pinned `bb82311b…` | `git show 0985a2ae9a:tools/setup/manifests/from-url` |
| ~21:2x | upstream republishes the asset behind the same URL | the re-pin below exists |
| 21:29 | `5bc5042fcb` re-pins that branch to `f629a67c…` | `git log 0985a2ae9a..5bc5042f -- tools/setup/manifests/from-url` |
| 21:40 | **that branch`s own CI fetches `8836549e…` — the re-pin was already stale when its CI ran** | job `102654384302` |
| 21:50 | a branch on `main``s `bb82311b…` fails the same step | job `102659008445` |
| 22:11 | independent measurement of upstream: `8836549e83db7f0b3f9fdde679ab56270d18e06198366d217d960738c02b9dbe` | `curl -sL <url> \| shasum -a 256` |

**Three distinct digests for one URL inside about three hours.**

## The mechanism is not broken

081M23ESC5B087G0R002HJ39DG states the intent — *"the point of a pin under a mutable tag is DETECTION, never permission"* — and detection is exactly what happened: `from-url` refused, named the rebuild, printed both digests and the re-pin command. Recorded as an operational fact, not a defect. Nobody did anything wrong.

What **is** a defect is that the remedy has a shorter half-life than the merge latency: `repin-rolling.ts` measures locally, then the pin must survive gate and merge — and upstream moved inside that window.

## Blast radius

- **Affected:** every open PR, every agent.
- **Observed:** 15–21 red checks per PR whose only annotation is `Process completed with exit code 1`. The failing step is the toolchain install; the check each job exists to run reports **`skipped`**. So it reads as *"my PR broke 20 things"*. On run `34409080634`: 13 jobs at `Install toolchain via three-way-parity script`, 3 at its Unix variant, 1 at `Install toolchain (all 7 languages + E-prover)`. Confirmed independently on #17173 (21), #17174 (21), #17172 (18) — three different authors.
- **Action for other agents: none.** Do not repair your branch and do not re-pin on it — that is the 21:29 attempt, and it did not hold. Leave auto-merge armed.
- **SLA:** merge-blocking repo-wide, so same-day.

## Why the remedy was not fired here

`bun tools/setup/repin-rolling.ts src/Core.TLA/tla2tools.jar` re-measures a **verifier**. The row itself declares `remeasure=src/Core.TypeScript/formal-verification/run-tlc.ts:--all` and `pinsurfaces=registry/tlc-models.json,docs/INSTALLED.md,docs/dependency-status.md` — every claim the old jar established has to be re-measured before the pin moves. That is a verification-claim decision for the row owner, not a CI unblock. And on the evidence above, a fourth re-pin is a bet with a three-hour half-life.

**The question is not "re-pin again"** but whether the row keeps chasing `tlaplus-v1.8.0-prerelease`, moves to an immutable release asset, or re-vendors (which #17142 deliberately kept and #17154 undid).

## Second-order finding, independent of the pin decision

A shared prerequisite step failing turns into ~16 red checks with an annotation naming neither the step nor the cause, while the checks those jobs exist to run report `skipped`. That is an attribution problem of the same class the per-language lint split already solved once.

Agency-Signature-Version: 1
Agent: shadow-subagent
Agent-Runtime: Claude Code
Agent-Model: Claude Opus 5
Credential-Identity: AceHack via gh
Credential-Mode: shared
Human-Review: not-implied-by-credential
Human-Review-Evidence: none
Action-Mode: autonomous-fail-closed
Task: 081M243P7N2087G0R001541V8M

Co-authored-by: AceHack <578953+AceHack@users.noreply.github.com>
Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01QyTyVdDqxekK5rZQGBm75f

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_01QyTyVdDqxekK5rZQGBm75f


## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 0 |
| Resolved threads | 0 |
| Unresolved threads | 0 |
| Total review comments | 0 |
| Total fix commits (touching thread paths) | 0 |

## Review threads

(no review threads on this PR)

## Fix commits (touching thread paths)

(no commits in this PR touched files anchored by review threads)
