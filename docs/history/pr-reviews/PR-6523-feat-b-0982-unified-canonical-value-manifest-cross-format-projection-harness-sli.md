# PR #6523 -- feat(081KT07NV0008QG0R0032MCYER): unified canonical value manifest + cross-format projection harness (slice 1)

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 6523 |
| Title | feat(081KT07NV0008QG0R0032MCYER): unified canonical value manifest + cross-format projection harness (slice 1) |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-06-02T02:01:56Z |
| Merged at | 2026-06-02T02:09:18Z |
| Merge commit SHA | `b42d565ecf5b623d96d33d5c2b95220af1d0fa52` |
| Branch | `otto-cli/b0982-value-manifest-2026-06-01` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/6523 |
| Changed files | 4 |
| Additions / deletions | +764 / -8 |

## Description

First slice of **081KT07NV0008QG0R0032MCYER** (multi-format seeds; *nothing is single source of truth*), per your choice of the **unified-manifest + projections** structure.

## What the check found
The two existing seeds are **independently authored** — only 12 of 31 (JSON) / 42 (CBOR) values are shared, with different naming (`string-*` vs `str-*`, `int-42` vs `int-1000`) and 2 same-named vectors that legitimately differ (CBOR's `array-mixed`/`array-nested` carry Float/Bytes that JSON can't). So there was no shared canonical value set — just two trusted files.

## This slice (zero merged-seed churn)
- **`golden-vectors-values.json`** — the single source for WHICH canonical `DynamicValue`s exist: **61 distinct values** (the value-keyed union; 12 shared, 19 JSON-only, 30 CBOR-only), each tagged with the per-format seeds that express it. Value-keyed matching sidesteps the naming divergence + the 2 drifts entirely.
- **`golden-vectors-manifest.test.ts`** — proves the existing CBOR + JSON seeds are faithful **projections**:
  1. no seed value drifts from the manifest (every seed value is listed, its format flagged) + each encoding decodes back to its manifest value (via the merged CBOR/JSON decoders);
  2. each projection is complete (every flagged manifest value appears in that seed);
  3. **CBOR and JSON agree on every value both express** — the heart of *nothing is single source of truth*.

**Touches no merged byte-locked seed files** (manifest + test are new). 5 tests / 476 assertions; eslint/tsc/prettier clean.

## Remaining (later slices)
YAML + XML codecs (encode+decode × 4 oracles) join the manifest as projections; the full 4-format cross-validation is the capstone. The manifest is now the spec they project from.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 1 |
| Resolved threads | 1 |
| Unresolved threads | 0 |
| Total review comments | 1 |
| Total fix commits (touching thread paths) | 2 |

## Review threads

### Thread 1 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GS1dl`
- **Path**: `src/Core.TypeScript/dynamic-value/golden-vectors-manifest.test.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-02T02:04:28Z on `src/Core.TypeScript/dynamic-value/golden-vectors-manifest.test.ts`:39 (association: NONE)

  <pre>
  P2 (maintainability): `hexToBytes` is duplicated from `golden-vectors-cbor-decode.test.ts`, which makes future changes (e.g., stricter hex validation) easy to miss. Consider extracting a shared helper (e.g., `hex.ts`) and importing it from both tests.
  </pre>

## Fix commits (touching thread paths)

### `9cef7d8c6fc9ba9097e7883e23e26c1dafe1da78` -- 2026-06-02T02:01:46Z -- ``

**Touched files (intersect with thread paths):**

- `src/Core.TypeScript/dynamic-value/golden-vectors-manifest.test.ts`

**Message:**

```
feat(081KT07NV0008QG0R0032MCYER): unified canonical value manifest + cross-format project…

…ion harness (slice 1)

Aaron 2026-06-01 directed 081KT07NV0008QG0R0032MCYER (multi-format seeds; 'nothing is single source of truth') and chose
the unified-manifest-plus-projections structure. The two existing seeds turned out to be independently
authored (12 of 31/42 values shared; different naming; 2 same-named vectors legitimately differ since
CBOR carries float/bytes JSON can't), so there was no shared canonical value set — only two trusted
files.

This slice introduces golden-vectors-values.json: the single source for WHICH canonical DynamicValues
exist (61 distinct values = the value-keyed union of both seeds; each tagged with the per-format seeds
that express it — 31 json, 42 cbor). The per-format seeds become PROJECTIONS, validated by
golden-vectors-manifest.test.ts:
  - no seed value drifts from the manifest (every seed value is listed + its format flagged), and each
    encoding decodes back to its manifest value (uses the merged CBOR+JSON decoders);
  - each format projection is complete (every flagged manifest value appears in that seed);
  - CBOR and JSON agree on every value both express (the heart of 'nothing is single source of truth').

Touches NO merged byte-locked seed files (manifest + test are new) — value-keyed matching sidesteps
the naming divergence entirely. 5 tests / 476 assertions; eslint/tsc/prettier clean. YAML + XML join
the manifest as their codecs land (slices 2-3); full 4-format cross-validation is the capstone.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `72c57a61ba89664f96993e688cbdb0a5cfba9646` -- 2026-06-02T02:06:00Z -- ``

**Touched files (intersect with thread paths):**

- `src/Core.TypeScript/dynamic-value/golden-vectors-manifest.test.ts`

**Message:**

```
refactor(DynamicValue): extract shared fromHex into cbor.ts (Copilot …

…#6523)

hexToBytes was duplicated in golden-vectors-cbor-decode.test.ts + golden-vectors-manifest.test.ts.
Exported fromHex (the inverse of the existing toHex) from cbor.ts — the natural home, since the seed
carries CBOR bytes as hex — and both tests import it. One definition; future hex-handling changes
can't be missed in one copy. 95 tests pass; eslint/tsc/prettier clean.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```
