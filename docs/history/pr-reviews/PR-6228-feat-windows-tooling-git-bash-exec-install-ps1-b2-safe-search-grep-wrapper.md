# PR #6228 -- feat(windows+tooling): git-bash exec->install.ps1 (b2) + safe-search grep wrapper

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 6228 |
| Title | feat(windows+tooling): git-bash exec->install.ps1 (b2) + safe-search grep wrapper |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-05-31T18:36:30Z |
| Merged at | 2026-05-31T19:15:05Z |
| Merge commit SHA | `fc21b1d62e15cfa3b59c8a7f1278e3a2a1a746e4` |
| Branch | `otto-windows/gitbash-exec-plus-grep-wrapper-2026-05-31` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/6228 |
| Changed files | 3 |
| Additions / deletions | +407 / -6 |

## Description

## Two operator-authorized items (2026-05-31)

### b2 — `install.sh` git-bash arm DRIVES the Windows install
Previously the `MINGW*|MSYS*|CYGWIN*` arm just printed "run `pwsh install.ps1`" and exited 2. Now it **execs `install.ps1`** via `pwsh` (or falls back to `powershell.exe`), with `cygpath` path conversion and a no-PowerShell fallback (message + exit 2). So `bash tools/setup/install.sh` from Git Bash installs Zeta on Windows in one shot — **parity with the Unix one-liner**.

- `exec` replaces the shell so `install.ps1`'s exit code propagates.
- Linux/macOS/NixOS routing unchanged; the Server-Core `install.ps1` CI test is unaffected (it runs `install.ps1` directly, not via the git-bash arm).
- `bash -n` clean.

### Safe-search grep wrapper — `tools/search/grep.ts`
Content-search wrapper with `references/upstreams` + `node_modules` + build-output excludes **baked in** — structural, not behavioral (per `architecture-is-safety-mechanism-not-discipline` + `references-upstreams-not-our-code-search-excludes`). Came from an operator catch: agents doing raw `Get-ChildItem -Recurse | Select-String` / `find | xargs grep` don't honor `.gitignore` and walk into the gigabytes of mirrored upstream repos.

- **Zero-dep Bun-native walk** so the excludes hold even where `rg` isn't on PATH (this laptop has none).
- Cross-repo aware (`--repo ../SQLSharp` still skips that repo's upstreams).
- Distinct from the concept-index (`tools/search/concept-index.ts`); this is the raw ad-hoc grep made correct-by-construction.
- **10 tests pass** (asserts the upstreams/node_modules/bin exclusion + match correctness + arg parsing).

## Note on diff
Branch is behind a fast-moving `main`; the 3-dot (real PR) diff is the focused 3-file set (grep.ts, grep.test.ts, install.sh). The `install.sh` change triggers the NixOS + Windows Docker install tests (paths-gated) — both expected green (b2 only touches the git-bash arm neither test exercises).

🤖 Generated with [Claude Code](https://claude.com/claude-code)


## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | true |
| Total threads | 8 |
| Resolved threads | 8 |
| Unresolved threads | 0 |
| Total review comments | 8 |
| Total fix commits (touching thread paths) | 5 |

## Review threads

### Thread 1 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F9nRL`
- **Path**: `tools/search/grep.ts`
- **Replies**: 0

**Initial comment:**

- **`github-advanced-security[bot]` (bot)** at 2026-05-31T18:39:10Z on `tools/search/grep.ts`:121 (association: NONE)

  <pre>
  ## CodeQL / Potential file system race condition

  The file may have changed since it [was checked](1).

  [Show more details](https://github.com/Lucent-Financial-Group/Zeta/security/code-scanning/129)
  </pre>

### Thread 2 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F9nRO`
- **Path**: `tools/search/grep.ts`
- **Replies**: 0

**Initial comment:**

- **`github-advanced-security[bot]` (bot)** at 2026-05-31T18:39:10Z on `tools/search/grep.ts`:192 (association: NONE)

  <pre>
  ## CodeQL / Regular expression injection

  This regular expression is constructed from a [command-line argument](1).

  [Show more details](https://github.com/Lucent-Financial-Group/Zeta/security/code-scanning/128)
  </pre>

### Thread 3 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F9n1k`
- **Path**: `tools/search/grep.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-31T18:40:47Z on `tools/search/grep.ts`:202 (association: NONE)

  <pre>
  P0 (compile): With `exactOptionalPropertyTypes` enabled, returning `{ exts }` when `exts` is `undefined` violates the `exts?: Set&lt;string&gt;` type (optional means “absent” not “present with undefined”). This will fail `tsc --noEmit` even though `bun test` passes at runtime. Build the return object without the `exts` property unless it’s defined.
  </pre>

### Thread 4 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F9n1s`
- **Path**: `tools/search/grep.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-31T18:40:47Z on `tools/search/grep.ts`:197 (association: NONE)

  <pre>
  P0 (compile): Passing `exts: parsed.exts` can still materialize as `exts: undefined` in the object literal, which violates `GrepOptions` under `exactOptionalPropertyTypes`. Only include the `exts` property when it’s defined.
  </pre>

### Thread 5 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F9n1u`
- **Path**: `tools/search/grep.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-31T18:40:47Z on `tools/search/grep.ts`:12 (association: NONE)

  <pre>
  P1 (xref integrity): This comment cites `.claude/rules/architecture-is-safety-mechanism-not-discipline-...`, but the repo file name is the full `architecture-is-safety-mechanism-not-discipline-structural-protections-vs-runtime-virtue-historical-innovation-parallel.md`. Using an ellipsis breaks greppability and makes the reference look like a path that doesn’t exist.
  </pre>

### Thread 6 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F9r-1`
- **Path**: `tools/search/grep.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-31T18:58:35Z on `tools/search/grep.ts`:55 (association: NONE)

  <pre>
  P1: grep.ts claims to exclude “build outputs”, but the exclude set currently misses several build/output directories that are already treated as noise elsewhere in the repo (tsconfig/eslint ignore patterns): `artifacts/`, `TestResults/`, `BenchmarkDotNet.Artifacts/`, `.lake/`. Without these, searches can still be slow/noisy and the tool’s guarantee is weaker than the repo’s established ignore list.
  </pre>

### Thread 7 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F9r-7`
- **Path**: `tools/search/grep.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-31T18:58:35Z on `tools/search/grep.ts`:218 (association: NONE)

  <pre>
  P1: If `--repo` points to a missing path or a non-directory, `grepTree` will return 0 matches and `main` exits 0, which can look like a successful “no hits” search (false negative). It’s safer to validate that `repo` is a readable directory in `parseArgs` and return a clear error so `main` exits non-zero.
  </pre>

### Thread 8 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F9r--`
- **Path**: `tools/setup/install.sh`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-31T18:58:35Z on `tools/setup/install.sh`:45 (association: NONE)

  <pre>
  Minor doc drift: the header says the Windows arm execs `setup/install.ps1`, but the script actually dispatches `tools/setup/install.ps1`. Keeping the path accurate helps cross-referencing and grepability.
  </pre>

## Fix commits (touching thread paths)

### `c605745583ed920ffe6659409e6566e2b79cf72c` -- 2026-05-31T18:36:14Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/search/grep.ts`
- `tools/setup/install.sh`

**Message:**

```
feat(windows+tooling): git-bash exec->install.ps1 (b2) + safe-search …

…grep wrapper

Two operator-authorized items (2026-05-31).

b2 - install.sh git-bash arm now DRIVES the Windows install instead of just
printing instructions: execs install.ps1 via pwsh (or powershell.exe), with
cygpath path conversion + a no-PowerShell fallback (message + exit 2). So
`bash install.sh` from Git Bash installs Zeta on Windows in one shot - parity
with the Unix one-liner. Linux/macOS/NixOS routing unchanged; the Server-Core
install.ps1 CI test is unaffected (it runs install.ps1 directly, not via the
git-bash arm).

grep wrapper - tools/search/grep.ts: a safe content-search wrapper with the
references/upstreams + node_modules + build-output excludes baked in
(structural not behavioral, per architecture-is-safety-mechanism-not-discipline
+ references-upstreams-not-our-code-search-excludes). Zero-dep Bun-native walk
so the excludes hold even where `rg` is not on PATH (this laptop, 2026-05-31).
Distinct from the concept-index; this is the raw ad-hoc grep made
correct-by-construction. 10 tests pass.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
```

### `d9a2d748b5e16fb929826d4941988538c88f320e` -- 2026-05-31T18:39:38Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/search/grep.ts`

**Message:**

```
fix(search): grep.ts exts optional accepts undefined (exactOptionalPr…

…opertyTypes)

tsc `lint (tsc tools)` failed: under tsconfig exactOptionalPropertyTypes:true an
optional `exts?: Set<string>` rejects an explicit `undefined` value. Widen both
GrepOptions.exts and ParsedArgs.exts to `Set<string> | undefined`. tsc clean +
10 tests still pass.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
```

### `df86e11b8c97cf95765923c363cd6ccc734b2336` -- 2026-05-31T18:49:51Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/search/grep.ts`

**Message:**

```
fix(search): address CodeQL findings in grep.ts (TOCTOU + regex-injec…

…tion)

CodeQL (non-required, but a search tool should not introduce its own alerts)
flagged two real findings:

- TOCTOU (grep.ts:123): statSync(path) then readFileSync(path) is a
  check-then-use race. Read via a single fd instead - openSync -> fstatSync(fd)
  -> readFileSync(fd) -> closeSync - so the size check and the read act on the
  same open handle (no second path stat).

- regex-from-CLI-arg (grep.ts:194): dropped new RegExp(argv) entirely. The tool
  now does literal substring matching (fixed-string, like grep -F). That is the
  better default for an ad-hoc safe-grep (faster, no ReDoS / regex-injection
  surface); when a regex is genuinely needed the harness Grep tool (ripgrep)
  serves it AND honors .gitignore (so it also skips references/upstreams). -i
  now case-folds both sides.

tsc clean; 11 tests pass (added a case-insensitive case).

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
```

### `393b7c2ea4c719aea96a24682d76e4e2cf652ead` -- 2026-05-31T18:54:39Z -- `claude`

**Touched files (intersect with thread paths):**

- `tools/search/grep.ts`

**Message:**

```
fix(search/grep): full greppable rule xref (no ellipsis) — PR #6228 t…

…hread

Addresses the remaining Copilot P1 xref thread on grep.ts:12. The comment
cited the architecture-is-safety-mechanism rule with an ellipsis, breaking
greppability + making it read like a non-existent path. Replaced with the
full filename, verified on disk.

The two CodeQL findings (TOCTOU + regex-injection) were already fixed by
peer commit df86e11b8 (single-fd read; literal substring matching) — this
commit only adds the xref fix on top, honoring the peer's stronger fixes.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `55e208c27bbecfdf9935a32470c9f49beb8b23ab` -- 2026-05-31T19:12:05Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/search/grep.ts`
- `tools/setup/install.sh`

**Message:**

```
fix(search+install): address Copilot review threads on #6228

Three valid findings from the PR review:

- grep.ts EXCLUDE_BASENAMES: add artifacts / TestResults /
  BenchmarkDotNet.Artifacts / .lake — .NET/Lean/bench build outputs the repo
  already treats as noise. The "excludes build outputs" claim now covers them.
- grep.ts parseArgs: validate --repo is an existing, readable directory; a
  missing/non-dir path now errors + exits non-zero instead of silently
  returning 0 matches (a false-negative that reads as "no hits").
- install.sh header: Windows arm path setup/install.ps1 -> tools/setup/install.ps1
  (greppable/accurate; matches what the arm actually dispatches).

(The other three threads were already addressed: exts exactOptionalPropertyTypes
is fixed + tsc-green; the ellipsis xref was fixed by the peer commit.)

tsc clean; 12 grep tests pass (added bad-repo case); bash -n install.sh OK.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
```
