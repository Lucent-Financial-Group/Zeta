# PR #9118 -- feat(core): CellScheduler slice 1 — DoP=1 deterministic cell multiplexer

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 9118 |
| Title | feat(core): CellScheduler slice 1 — DoP=1 deterministic cell multiplexer |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-07-02T05:13:06Z |
| Merged at | 2026-07-02T05:13:13Z |
| Merge commit SHA | `942c6c20d2a240a9e8f710f2a5d23a5c2bc13ff1` |
| Branch | `otto/cell-scheduler-slice1-dop1-multiplexer` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/9118 |
| Changed files | 5 |
| Additions / deletions | +248 / -4 |

## Description

Slice 1 of the cell scheduler (`081KTG5S0M9`) — the **DoP=1 deterministic multiplexer**.

## What
Thousands of cells step on one cooperative loop, deterministic FIFO order, communicating **only** through inboxes (the declared channel — noninterference §13). No wall-clock, no randomness, no shared mutable state ⇒ same `(cells, messages)` replays identically (DST; the FoundationDB run-loop at the cell layer).

- **Generic over the cell step** (`'St -> 'Msg -> 'St * (CellId*'Msg) list`) — the multiplexer is honestly separable from the cell's work.
- **`yinYangStep`** is the `DurableYinYang.evolve` instantiation, with the `"__outbox__"` emission convention (pure `routeOutbox`, testable without Bonsai).
- **Non-termination is a NAMED backstop** (`Error`), never a silent cap.

## Why this shape
Slice 2 feeds this exact ready-queue to a `FerryThrottler` for DoP=N and proves `run(1) == run(N)` (the scale-free law). Slice 1 is the deterministic core that invariant must match. Composition, not a fourth scheduler — per the [design note](docs/research/2026-07-02-cell-scheduler-cells-on-the-deterministic-soft-loop-dop1-to-dopn.md) (slice 1 now marked LANDED).

## Tests (8, green)
sum-to-quiescence · inter-cell routing · **DoP=1 determinism (byte-identical replay)** · parking (idle cells cost nothing, untouched) · unknown-target drop · runaway backstop fires · `routeOutbox` extract/strip + non-object passthrough.

Build 0 warnings.

Anchors: Zhou et al. *FoundationDB* (SIGMOD 2021); Will Wilson, *Deterministic Simulation Testing* (Strange Loop 2014); Erlang/BEAM reduction-counting fairness.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 1 |
| Resolved threads | 0 |
| Unresolved threads | 1 |
| Total review comments | 1 |
| Total fix commits (touching thread paths) | 1 |

## Review threads

### Thread 1 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM6NyayM`
- **Path**: `src/Core/CellScheduler.fs`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-07-02T05:16:00Z on `src/Core/CellScheduler.fs`:74 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Preserve per-cell FIFO when re-readying cells**

  When a cell still has pending input, this re-ready path removes the current head message and re-delivers it, which appends it to the tail. For any cell with more than one remaining message (or an older pending message plus a self-emitted one), the inbox rotates from `[m2; m3]` to `[m3; m2]`, so the cell no longer processes its FIFO event sequence even though the scheduler contract and the later DoP-invariance proof rely on that order. Re-ready the cell id without rewriting its inbox.

  Useful? React with 👍 / 👎.
  </pre>

## Fix commits (touching thread paths)

### `836fd31db2b2aa4d6a83b11f8a4e439c28d97f2a` -- 2026-07-02T05:13:02Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `src/Core/CellScheduler.fs`

**Message:**

```
feat(core): CellScheduler slice 1 — DoP=1 deterministic cell multiple…

…xer (shadow*)

Slice 1 of 081KTG5S0M9 (cell scheduler). The DoP=1 MULTIPLEXER: thousands of
cells step on one cooperative loop, deterministic FIFO order, communicating only
through inboxes (the declared channel — noninterference §13). No wall-clock, no
randomness, no shared mutable state ⇒ same (cells, messages) replays identically
(DST; the FoundationDB run-loop at the cell layer).

Generic over the cell STEP (`'St -> 'Msg -> 'St * (CellId*'Msg) list`) so the
multiplexer is honestly separable from the cell's work. The YinYang wiring
(`'St = DynamicValue` via DurableYinYang.evolve) is one instantiation — yinYangStep
+ the "__outbox__" emission convention (pure routeOutbox). Slice 2 feeds this exact
ready-queue to a FerryThrottler for DoP=N and proves run(1)==run(N); slice 1 is the
deterministic core it must match.

Non-termination is a NAMED backstop (Error), never a silent cap (scope-honesty).
8 tests: sum-to-quiescence, inter-cell routing, DoP=1 determinism (byte-identical
replay), parking (idle cells cost nothing/untouched), unknown-target drop, runaway
backstop, routeOutbox extract/strip + non-object. Build 0 warnings.

Design note: docs/research/2026-07-02-cell-scheduler-…-dop1-to-dopn.md (slice 1
marked LANDED).

Anchors: Zhou et al. FoundationDB (SIGMOD 2021); Will Wilson, Deterministic
Simulation Testing (Strange Loop 2014); Erlang/BEAM reduction-counting fairness.

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>

AgencySignature-v1:
  persona: otto
  actor: zeta-otto
  surface: src/core
  topology: shadow-clone
  intent: cell-scheduler-slice1-dop1-deterministic-multiplexer
  authorization: aaron-explicit-2026-07-02
  uncertainty: low
  measure: cellscheduler-dop1-multiplexer-generic-over-step-fifo-routing-parking-named-backstop-8-tests
  delta-u: scheduler-slice1-lands-deterministic-core-slice2-dopn-invariance-builds-on-it
  seed: S4
```
