# PR #2456 -- fix: also skip hooks 403 with insufficient-token-scope sentinel

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 2456 |
| Title | fix: also skip hooks 403 with insufficient-token-scope sentinel |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-05-10T10:01:20Z |
| Merged at | 2026-05-10T10:06:49Z |
| Merge commit SHA | `9af38c532c78770e789959a59d3f52792abd2cb5` |
| Branch | `fix/hooks-403-gate-scope-only` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/2456 |
| Changed files | 2 |
| Additions / deletions | +46 / -18 |

## Description

## Summary

- `/repos/:repo/hooks` returns HTTP 403 in CI when `GITHUB_TOKEN` lacks admin scope, crashing the snapshot with _\"Resource not accessible by integration\"_ and failing the `check drift` job (non-required, but noisy)
- `snapshot-github-settings.ts`: switch from `ghApi` → `ghApiOptional` for `/hooks`; when null, emit `{ _skipped: \"insufficient-token-scope\" }` for `counts.webhooks` (same sentinel pattern as #2448/#2449/#2450)
- `check-github-settings-drift.ts`: make the `_skipped` sentinel-stripping recursive so it handles nested fields like `counts.webhooks` inside the `counts` object, not just top-level keys

## Test plan

- [ ] `bun tools/hygiene/check-github-settings-drift.ts` — passes locally with no drift
- [ ] `dotnet build -c Release` — 0 warnings, 0 errors
- [ ] CI `check drift` job passes on this PR

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 2 |
| Resolved threads | 2 |
| Unresolved threads | 0 |
| Total review comments | 2 |
| Total fix commits (touching thread paths) | 1 |

## Review threads

### Thread 1 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6A4_Ku`
- **Path**: `tools/hygiene/snapshot-github-settings.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-10T10:03:17Z on `tools/hygiene/snapshot-github-settings.ts`:256 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Restrict webhook fallback to scope errors only**

  Switching `/repos/${repo}/hooks` to `ghApiOptional` makes **any** non-zero `gh api` exit (404, 5xx, rate-limit, transient network/auth failures) look like an `_skipped: "insufficient-token-scope"` case, and `check-github-settings-drift.ts` now strips that field before diffing. This can hide real snapshot failures and produce false "no drift" results when webhook data was not actually retrieved; the fallback should only trigger for the specific insufficient-scope failure mode, not for all errors.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 2 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6A4_gA`
- **Path**: `tools/hygiene/snapshot-github-settings.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-10T10:04:52Z on `tools/hygiene/snapshot-github-settings.ts`:261 (association: NONE)

  <pre>
  `ghApiOptional` returns `null` for any non-zero `gh api` exit (not just HTTP 403 insufficient-scope). Treating `null` as `{ _skipped: "insufficient-token-scope" }` here can silently mask transient failures (rate limit, network, 5xx) and cause the drift check to skip `counts.webhooks` when it should fail loudly. Consider making the optional path discriminate 403 (e.g., inspect stderr for HTTP 403 / integration-scope message) and throw on other failures, or use a distinct sentinel for "api-error" so CI noise doesn’t hide real outages.
  </pre>

## Fix commits (touching thread paths)

### `a8576b3d40b05b92b0c36e2b0ec708f7dc51773e` -- 2026-05-10T10:01:09Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/hygiene/snapshot-github-settings.ts`

**Message:**

```
fix: also skip hooks 403 with insufficient-token-scope sentinel

The /repos/:repo/hooks endpoint returns HTTP 403 in CI when the
GITHUB_TOKEN lacks admin scope ("Resource not accessible by
integration"), crashing the snapshot and failing the check-drift job.

Apply the same sentinel pattern used for actions/permissions (#2448),
actions/variables (#2449), and code-scanning/default-setup (#2450):
- snapshot-github-settings.ts: use ghApiOptional for /hooks; emit
  { _skipped: "insufficient-token-scope" } for counts.webhooks when
  the call returns null.
- check-github-settings-drift.ts: make the sentinel-stripping logic
  recursive so it handles nested fields (counts.webhooks inside the
  counts object) in addition to the existing top-level fields.

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>
```
