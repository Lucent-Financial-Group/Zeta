# PR #6504 -- fix(DynamicValue): Rust oracle returns Result (converge with F#/C#) — no panic on deferred variants

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 6504 |
| Title | fix(DynamicValue): Rust oracle returns Result (converge with F#/C#) — no panic on deferred variants |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-06-02T00:07:32Z |
| Merged at | 2026-06-02T00:17:11Z |
| Merge commit SHA | `0403537acddbe1c5ccc48de140962c55b1700b83` |
| Branch | `otto-cli/dynamicvalue-rust-result-converge-2026-06-01` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/6504 |
| Changed files | 2 |
| Additions / deletions | +82 / -19 |

## Description

Converges the merged Rust oracle (#6503) with the F# (#6500) + C# (#6502) reground, per AGENTS.md's **Result-over-exception hard rule**. The Rust `to_canonical_json` *panicked* on a legal `DynamicValue::Float`/`::Bytes` — the same exception-for-a-legal-value-path violation fixed in F#/C#. Now returns `Result<String, EncodeError>` (FloatDeferred | BytesDeferred), surfacing deferred variants as **Err data** via `?` propagation.

All four oracles now agree on the byte-lock bytes; the three with a production encoder (F#/C#/Rust) share the `Result` error-wrapper. (TS needs no change — its only encoder is the test's inline one over the six locked cases, no production deferred path.)

Verified **locally**: `cargo test` green + `RUSTFLAGS=-D warnings` clean. Rust is not in the CI build-and-test matrix (`gate.yml` is dotnet-only) — locally cargo-verified, like every `Core.Rust.*` oracle.

This completes the DynamicValue v1 byte-lock + Result-convergence across all oracles. Next (separate): Float/Bytes lock under CBOR (or tagged-JSON) if called.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | true |
| Total threads | 2 |
| Resolved threads | 2 |
| Unresolved threads | 0 |
| Total review comments | 4 |
| Total fix commits (touching thread paths) | 2 |

## Review threads

### Thread 1 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GR36Y`
- **Path**: `src/Core.Rust.DynamicValue/src/lib.rs`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-02T00:09:51Z on `src/Core.Rust.DynamicValue/src/lib.rs`:96 (association: NONE)

  <pre>
  P1 (test coverage): `to_canonical_json` now returns `Result` to surface deferred `Float`/`Bytes` as `Err`, but the test suite still only exercises the locked vectors (which never include these variants). Add a small unit test that constructs `DynamicValue::Float`/`::Bytes` and asserts this returns `Err(EncodeError::{FloatDeferred,BytesDeferred})` (and does not panic) to lock the new contract in place.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-06-02T00:14:48Z on `src/Core.Rust.DynamicValue/src/lib.rs`:96 (association: MEMBER)

    <pre>
    Addressed in 6ba89c337: added unit tests asserting Float/Bytes -&gt; Err(EncodeError) with no panic. The seed has no Float/Bytes vectors so cross-verify cannot catch this; the tests lock the contract directly (assert-dont-skip).
    </pre>

### Thread 2 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GR36z`
- **Path**: `src/Core.Rust.DynamicValue/src/lib.rs`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-02T00:09:51Z on `src/Core.Rust.DynamicValue/src/lib.rs`:145 (association: NONE)

  <pre>
  P2: Since `EncodeError` is now part of the public API surface (returned from `to_canonical_json`), implement `Display` (and `std::error::Error`) so callers can surface a stable human-readable error without relying on `Debug` formatting.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-06-02T00:14:50Z on `src/Core.Rust.DynamicValue/src/lib.rs`:145 (association: MEMBER)

    <pre>
    Addressed in 6ba89c337: impl Display + std::error::Error for EncodeError (it is public API now); callers get a stable message without leaning on Debug.
    </pre>

## Fix commits (touching thread paths)

### `07c93010b831080fda8f03ec22d2359a978bf073` -- 2026-06-02T00:07:21Z -- ``

**Touched files (intersect with thread paths):**

- `src/Core.Rust.DynamicValue/src/lib.rs`

**Message:**

```
fix(DynamicValue): Rust oracle returns Result (converge with F#/C#) —…

… no panic on deferred variants

Per AGENTS.md hard rule (user-visible errors are Result, not exceptions),
converging the merged Rust oracle (#6503) with the F# (#6500) + C# (#6502)
reground: to_canonical_json now returns Result<String, EncodeError> (FloatDeferred
| BytesDeferred). A legal DynamicValue::Float / ::Bytes is surfaced as Err DATA via
? propagation, never panicked. All four oracles agree on the byte-lock bytes AND
(for the three with a production encoder) share the Result error-wrapper. Adds the
EncodeError enum (mirrors F#/C#).

Verified locally: cargo test green + RUSTFLAGS=-D warnings clean. (Rust is not in
the CI matrix — locally cargo-verified, like the other Core.Rust.* oracles.)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `6ba89c337b5bbed8473a1a0dae235e45b08fe424` -- 2026-06-02T00:14:21Z -- ``

**Touched files (intersect with thread paths):**

- `src/Core.Rust.DynamicValue/src/lib.rs`

**Message:**

```
feat(DynamicValue): Display+Error for EncodeError + lock Float/Bytes …

…deferred-Err contract (Rust oracle)

Resolves two Copilot threads on #6504:
- P1: add unit tests asserting Float/Bytes -> Err(EncodeError) and never panic
  (seed has no Float/Bytes vectors, so cross-verify cannot catch this; assert
  the contract directly per assert-dont-skip).
- P2: EncodeError is public API -> impl Display + std::error::Error so callers
  surface a stable message without leaning on Debug.

Additive: no locked-vector bytes change; cross-verify still green.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```
