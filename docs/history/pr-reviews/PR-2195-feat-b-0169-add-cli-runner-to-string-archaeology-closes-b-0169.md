# PR #2195 -- feat(B-0169): add CLI runner to string-archaeology — closes B-0169

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 2195 |
| Title | feat(B-0169): add CLI runner to string-archaeology — closes B-0169 |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-05-09T06:02:43Z |
| Merged at | 2026-05-09T06:05:43Z |
| Merge commit SHA | `05524fd4951b43193a863002d3e7b9e4b57fa9ef` |
| Branch | `claim/b0169-cli-runner-2026-05-09` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/2195 |
| Changed files | 2 |
| Additions / deletions | +83 / -3 |

## Description

## Summary

- Adds a CLI entrypoint (`#!/usr/bin/env bun` shebang + `main()` + `import.meta.main` guard) to `tools/decision-archaeology/string-archaeology.ts`, enabling direct invocation as `bun tools/decision-archaeology/string-archaeology.ts "term"`.
- Adds `-w` to the `git log -S` call to suppress whitespace-only false positives (fixes gap vs SKILL.md anti-pattern #3).
- Adds the required pre-start checklist to `docs/backlog/P1/B-0169-*.md` and marks the item `done`.

## Prior slices that this closes out

| PR | Content |
|---|---|
| #2139 | `.claude/skills/decision-archaeology/SKILL.md` |
| #2167 | `string-archaeology.ts` library + 3 worked examples in `docs/research/` |
| This PR | CLI runner — makes the tool directly invocable |

## Test plan

- [x] `bun tools/decision-archaeology/string-archaeology.ts --help` → prints usage, exit 0
- [x] `bun tools/decision-archaeology/string-archaeology.ts` (no args) → prints usage, exit 1
- [x] `bun tools/decision-archaeology/string-archaeology.ts "double-hop"` → JSON with commits list and summary
- [x] `bun tools/decision-archaeology/string-archaeology.ts "decision-archaeology" ".claude/skills/decision-archaeology/SKILL.md"` → scoped search returns 1 commit
- [x] `dotnet build -c Release` → 0 warnings, 0 errors

## Checks

- No new files; one TS file extended, one backlog row updated.
- Rule 0 compliant: TS not bash.
- Import as library still works (exports unchanged; only new symbols added).

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 2 |
| Resolved threads | 0 |
| Unresolved threads | 2 |
| Total review comments | 2 |
| Total fix commits (touching thread paths) | 1 |

## Review threads

### Thread 1 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM6AyzCF`
- **Path**: `tools/decision-archaeology/string-archaeology.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-09T06:05:52Z on `tools/decision-archaeology/string-archaeology.ts`:88 (association: NONE)

  <pre>
  `main()` currently always returns 0 after running `findStringIntroduction()`, even when `git log` fails (the result summary is an error) or when the search string is empty/invalid. This contradicts the documented exit codes above (exit 1 on git error / bad args) and makes it hard to use the CLI in scripts. Consider propagating an error exit code when `findStringIntroduction()` reports an error, and treating an empty/whitespace search as a bad-args case (print help + exit 1).

  </pre>

### Thread 2 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM6AyzCN`
- **Path**: `tools/decision-archaeology/string-archaeology.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-09T06:05:52Z on `tools/decision-archaeology/string-archaeology.ts`:83 (association: NONE)

  <pre>
  Argument validation: the CLI help describes only `&lt;search&gt;` and optional `&lt;file&gt;`, but `main()` silently ignores any additional arguments beyond the second. This can mask user mistakes (e.g., accidentally passing an extra flag/path). Consider detecting `args.length &gt; 2` and treating it as bad arguments (print help + exit 1).

  </pre>

## Fix commits (touching thread paths)

### `eba1e00769df436ed95e5e31f2d5b03399e4f706` -- 2026-05-09T06:02:18Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/decision-archaeology/string-archaeology.ts`

**Message:**

```
feat(B-0169): add CLI runner to string-archaeology — closes B-0169

string-archaeology.ts was a library-only module with no shebang and no
import.meta.main guard. Contributors had no way to invoke it directly
as `bun tools/decision-archaeology/string-archaeology.ts "term"`.

This slice adds:
- `#!/usr/bin/env bun` shebang + header doc block with usage/exit codes
- `main()` function with --help / no-args / positional-arg parsing
- `import.meta.main` guard wiring `main()` to the CLI entry point
- Adds `-w` flag to the git log invocation (suppresses whitespace-only
  false positives per SKILL.md anti-pattern #3)
- Pre-start checklist + status=done in backlog row B-0169

Smoke tests: --help, no-args (exit 1), live search, file-scoped search
all pass. Build: 0 warnings, 0 errors.

Prior merged slices:
- PR #2139: .claude/skills/decision-archaeology/SKILL.md
- PR #2167: string-archaeology.ts library + 3 worked examples

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>
```
