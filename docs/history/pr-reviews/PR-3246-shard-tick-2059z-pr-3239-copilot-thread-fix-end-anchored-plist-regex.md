# PR #3246 -- shard(tick): 2059Z — PR #3239 Copilot thread fix (end-anchored plist regex)

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 3246 |
| Title | shard(tick): 2059Z — PR #3239 Copilot thread fix (end-anchored plist regex) |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-05-14T21:03:27Z |
| Merged at | 2026-05-14T21:14:31Z |
| Merge commit SHA | `20a528826fc9c22433e9c9927b946289de122a17` |
| Branch | `shard/tick-2059Z-pr3239-thread-fix-otto-cli-2026-05-14` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/3246 |
| Changed files | 1 |
| Additions / deletions | +89 / -0 |

## Description

## Summary

Tick 2026-05-14T20:59Z shard. Substantive work was a thread fix on [#3239](https://github.com/Lucent-Financial-Group/Zeta/pull/3239) — Copilot caught a real regex bug (un-anchored `.plist` would over-exclude `foo.plist.bak` / `foo.plist.md`). Fixed + property-tested + thread resolved.

## What landed

- **Commit `673fbd0` on #3239's branch** — tightened regex to `\.gemini\/(launchd|service)\/[^/]+\.plist$` (end-anchored, no subdir traversal). 8/8 property tests pass; audit total unchanged at 46.
- This shard.

## Pattern this tick

[`blocked-green-ci-investigate-threads.md`](.claude/rules/blocked-green-ci-investigate-threads.md) paid off. The gate-poller reported `UNKNOWN` once then `BLOCKED` with 0 failed checks + autoMerge armed. The block was 1 thread with an addressable finding, not a CI failure. Investigated immediately, fixed in ~3 minutes.

## Why the Copilot review was substantive

The flag was not a nit. `EXCLUDE_RE.test(path)` with `.*\.plist` matches `foo.plist.bak`, `foo.plist.md`, `foo.plist.anything` — over-exclusion. End-anchoring with `\.plist$` + `[^/]+` for the filename slot eliminates the over-match without changing behavior for real plist files.

## Prior-tick PRs status

- [#3240](https://github.com/Lucent-Financial-Group/Zeta/pull/3240) (shard 2046Z) — wait-ci, autoMerge armed
- [#3244](https://github.com/Lucent-Financial-Group/Zeta/pull/3244) (B-0488 close-out) — wait-ci, autoMerge armed
- [#3245](https://github.com/Lucent-Financial-Group/Zeta/pull/3245) (shard 2055Z) — wait-ci, autoMerge armed

## Test plan

- [x] Property test: 8/8 cases pass for tightened regex
- [x] Audit total: 46 (unchanged — real plists still excluded)
- [x] Thread resolved via GraphQL `resolveReviewThread` mutation
- [x] Composite branch-guard + `gh pr create --head` used
- [ ] CI clears on the new #3239 commit + auto-merges

🤖 Generated with [Claude Code](https://claude.com/claude-code)

Co-Authored-By: Claude <noreply@anthropic.com>

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 1 |
| Resolved threads | 1 |
| Unresolved threads | 0 |
| Total review comments | 1 |
| Total fix commits (touching thread paths) | 2 |

## Review threads

### Thread 1 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6CMH9c`
- **Path**: `docs/hygiene-history/ticks/2026/05/14/2059Z.md`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-14T21:05:24Z on `docs/hygiene-history/ticks/2026/05/14/2059Z.md`:48 (association: NONE)

  <pre>
  P1: `gh api graphql resolveReviewThread` isn’t a valid `gh api graphql` invocation; `resolveReviewThread` is a GraphQL mutation field and needs to be passed via `-f query=...` (and variables via `-f`). Suggest updating this line to the canonical pattern already documented elsewhere (e.g., `gh api graphql -f query='mutation($id:ID!){resolveReviewThread(input:{threadId:$id}){thread{isResolved}}}' -f id=$tid`) so future readers can actually run it.

  </pre>

## Fix commits (touching thread paths)

### `48c3d8b7a3719e358048e76e28db33f76e1431bc` -- 2026-05-14T21:03:03Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `docs/hygiene-history/ticks/2026/05/14/2059Z.md`

**Message:**

```
shard(tick): 2059Z — PR #3239 Copilot thread fix (end-anchored plist …

…regex)

Tick output:
- PR #3239 (audit hygiene) Copilot review caught real regex bug:
  `\.gemini\/(launchd|service)\/.*\.plist` wasn't end-anchored, would
  over-exclude foo.plist.bak / foo.plist.md. Fixed in new commit
  673fbd0 with end-anchored `[^/]+\.plist$`. Property-tested 8/8.
  Thread resolved via GraphQL mutation.
- This shard.

Rule blocked-green-ci-investigate-threads.md paid off: gate reported
UNKNOWN/BLOCKED with green CI + autoMerge armed; investigated threads
first, found the actionable Copilot finding.

Prior-tick PRs: #3240 / #3244 / #3245 all still wait-ci with autoMerge
armed.

Cron sentinel 12fb713e armed; recurring.

Co-Authored-By: Claude <noreply@anthropic.com>
```

### `a5b812b026e9ef2a02563b36d3c4a056a8399d01` -- 2026-05-14T21:11:59Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `docs/hygiene-history/ticks/2026/05/14/2059Z.md`

**Message:**

```
fix(pr3246): correct gh api graphql usage description in 2059Z shard

Copilot caught: the shard described `gh api graphql resolveReviewThread`
as if it were a subcommand. That's not valid `gh api graphql` invocation
— `resolveReviewThread` is a GraphQL MUTATION FIELD, not a CLI
subcommand.

Corrected to show the canonical pattern:
  gh api graphql -f query='mutation { resolveReviewThread(input:{threadId:"<id>"}) { thread { isResolved } } }'

The mutation is passed via `-f query=` (and parameters via `-f` for
variables). Future readers can copy-paste a runnable example.

The Verify section (line 57) already correctly said "GraphQL mutation"
— only the earlier narrative text was misleading.

Co-Authored-By: Claude <noreply@anthropic.com>
```
