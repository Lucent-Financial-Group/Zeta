# PR #14153 -- ci: pin the last actions/checkout leftover to v7.0.1 fleet SHA

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 14153 |
| Title | ci: pin the last actions/checkout leftover to v7.0.1 fleet SHA |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-08-23T12:19:49Z |
| Merged at | 2026-08-24T16:15:52Z |
| Merge commit SHA | `f345129a4e30a4be0753d46ac59e729ce6d519dd` |
| Branch | `cursor/checkout-v7-fleet-e06b` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/14153 |
| Changed files | 1 |
| Additions / deletions | +1 / -1 |

## Description

<!-- CURSOR_AGENT_PR_BODY_BEGIN -->
# Summary

Rework leftover UNIQUE #13987 so the `actions/checkout` fleet is one pin. Dependabot #13987 only bumps `.github/workflows/image-pull-measurement.yml` (`11bd7190` # v4.2.2 → `3d3c42e5` # v7.0.1) on a stale base (`62b7e053`, 93 commits behind current `main`). Rewriting that Dependabot branch would fight Dependabot, so this follow-up lands the same intended pin from current `main`.

# Spec alignment

No OpenSpec behaviour change. Workflow supply-chain pin only. Existing floor lint remains `gha-action-mutable-tag` (SHA vs mutable tag). No new lint invented.

# What this PR does

- Bumps the only live leftover pin in `.github/workflows`:
  `image-pull-measurement.yml`
  `actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683` # v4.2.2
  → `actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1` # v7.0.1
- Leaves Dependabot leftover UNIQUE #13987 open and untouched.
- Does not take 14-vs-70 (`k8s-lane-partition.yml` leftover-on-main #13784).

# Audit (`.github/workflows`)

After this change, every live `uses: actions/checkout@` pin in `.github/workflows` is `3d3c42e5aac5ba805825da76410c181273ba90b1` # v7.0.1 (115 pins).

Comment-only leftovers (not pins): `de0fac2...` examples in `backlog-index-integrity.yml`, `low-memory.yml`, `memory-index-drift.yml`, `memory-index-integrity.yml`.

Out of scope (not `.github/workflows`): `agentic-organization/.github/workflows/*` and scaffold templates still show `de0fac2e` # v6.0.2.

# Fork-PR checkout safety

v7 blocks fork-PR checkout on `pull_request_target` / `workflow_run`.

- No workflow uses `on: pull_request_target`. `codeql.yml` mentions it only in a comment.
- `rerun-cancelled-gate.yml` is `workflow_run` but checks out the default branch, never the PR head. Already on the fleet pin.
- `lint-autofix-apply.yml` is `workflow_run` and checks out `workflow_run.head_sha`. Already on the fleet pin. Same-repo `if:` guard means fork PRs never reach that step. v7's fork-PR block matches that guard; same-repo head checkout stays.

# Tests

Local inventory after the bump: zero live `uses: actions/checkout@` pins in `.github/workflows` remain off `3d3c42e5aac5ba805825da76410c181273ba90b1`. `actionlint .github/workflows/image-pull-measurement.yml` passed. Workflow YAML change only; no new lint.

CI on `edfb6acf5` (run 32639082331):

- `gate (required)`: **green**
- `test (TS hermetic)`: **red**, 10 fails / 15693 pass in `src/Core.TypeScript/hygiene/mutation-findings.test.ts` (`strayDraws` 3 not 0; sampling frame missing 3 observed draws). This job is explicitly absent from `gate-required.needs` (`RUNS, DOES NOT BLOCK`). It is leftover-on-main mutation-findings drift, not this pin. Not patched here.

# Validation

- [x] Fleet inventory: only `image-pull-measurement.yml` was off the target SHA on `origin/main`
- [x] After bump: 115 live pins, all `3d3c42e5` # v7.0.1
- [x] `actionlint` clean on the edited workflow
- [x] No `pull_request_target` / fork-PR checkout silently bumped
- [x] `gate (required)` green on this PR
- [x] No new lint invented

# Leftover UNIQUE #13987 (kept open)

- Head: `330a2f6d6a2b870739d9a74547702f51dfcc0ade`
- Branch: `dependabot/github_actions/actions/checkout-7.0.1`
- Base at open: `62b7e053` (stale vs current `main`)
- Divergence vs current `main`: 93 behind, 1 ahead
- GitHub `mergeable_state`: `clean` (no conflicts; still stale)
- Diff: the same one-file pin this PR lands from current `main`
- `gate (required)`: green on that head
- Reviews requested: `maximdolphin`, `Addisons820`. No reviews submitted
- CoS does not merge. This PR also does not merge and does not approve

# Remaining land blockers

- Reviews: this PR requests `maximdolphin` / `Addisons820` and has zero reviews. Same on leftover UNIQUE #13987
- Dirty: #13987 is 93 commits behind `main`. This PR is 0 behind / 1 ahead of current `origin/main` (`7e2f18b7f`)
- leftover HOLD leftover-on-main #12252: already merged 2026-08-19 (`a71b93f9` into `3465e2fc`). Still lists requested reviewers `maximdolphin` / `Addisons820`. Not a merge conflict for this pin. Windows/macOS `continue-on-error` policy from that PR is untouched here
- Non-blocking CI: `test (TS hermetic)` red on mutation-findings sampling-frame drift (`strayDraws=3`). Does not fail `gate (required)`

# Notes

Do not merge. Do not approve. Do not take 14-vs-70.

Agency-Signature-Version: 1
Agent: riven
Agent-Runtime: cursor-cloud
Agent-Model: grok-4.6
Credential-Identity: AceHack
Credential-Mode: shared
Human-Review: not-implied-by-credential
Human-Review-Evidence: none
Action-Mode: autonomous-fail-open
Task: #13987
Co-authored-by: Grok <noreply@x.ai>

<!-- CURSOR_AGENT_PR_BODY_END -->

<div><a href="https://cursor.com/agents/bc-843d6b2b-87f5-4b6f-a90d-95eb0e70e06b?cursor_ref=pr_footer&cursor_cta=open_in_web"><picture><source media="(prefers-color-scheme: dark)" srcset="https://cursor.com/assets/images/open-in-web-dark.png"><source media="(prefers-color-scheme: light)" srcset="https://cursor.com/assets/images/open-in-web-light.png"><img alt="Open in Web" width="114" height="28" src="https://cursor.com/assets/images/open-in-web-dark.png"></picture></a>&nbsp;<a href="https://cursor.com/background-agent?bcId=bc-843d6b2b-87f5-4b6f-a90d-95eb0e70e06b&cursor_ref=pr_footer&cursor_cta=open_in_cursor"><picture><source media="(prefers-color-scheme: dark)" srcset="https://cursor.com/assets/images/open-in-cursor-dark.png"><source media="(prefers-color-scheme: light)" srcset="https://cursor.com/assets/images/open-in-cursor-light.png"><img alt="Open in Cursor" width="131" height="28" src="https://cursor.com/assets/images/open-in-cursor-dark.png"></picture></a>&nbsp;</div>



## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 0 |
| Resolved threads | 0 |
| Unresolved threads | 0 |
| Total review comments | 0 |
| Total fix commits (touching thread paths) | 0 |

## Review threads

(no review threads on this PR)

## Fix commits (touching thread paths)

(no commits in this PR touched files anchored by review threads)
