# PR #6031 -- feat(081KR50HA0008QG0R001CNS20T.1): static structural validator for bootstrap CLAUDE.md

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 6031 |
| Title | feat(081KR50HA0008QG0R001CNS20T.1): static structural validator for bootstrap CLAUDE.md |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-05-29T18:54:34Z |
| Merged at | 2026-05-29T19:09:12Z |
| Merge commit SHA | `943b662069c1778bbf16b5bd34326127706f1c94` |
| Branch | `otto-cli/b0354.1-bootstrap-validator-harness-2026-05-29` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/6031 |
| Changed files | 3 |
| Additions / deletions | +409 / -1 |

## Description

## What

Smallest safe slice of **081KR50HA0008QG0R001CNS20T** (fresh-instance validation test for bootstrap CLAUDE.md) — child **081KR50HA0008QG0R001CNS20T.1**: the static structural-validation **harness skeleton**.

Ships `tools/bootstrap-validator/validate-bootstrap-claude-md.ts` + `.test.ts`. Validates the shape a fresh Claude Code instance depends on, with **no Claude spawn** (live execution is 081KR50HA0008QG0R001CNS20T.2; findings/gap-filing is 081KR50HA0008QG0R001CNS20T.3).

### Checks
| id | hard/soft | meaning |
|---|---|---|
| `claude-md-exists` | fail | CLAUDE.md present at repo root |
| `six-step-process` | fail | `## 1.`..`## 6.` bootstrap process present |
| `rules-auto-load` | fail | `.claude/rules/` auto-load surface non-empty (behavioral-doctrine discovery channel) |
| `conciseness` | **warn** | CLAUDE.md within soft `--max-lines` ceiling (default 150) |

CLI: `--json` / `--root <path>` / `--max-lines <n>` / `--help`. Exit codes **0** pass / **1** usage / **3** fail. Convention-matched to `tools/cold-start-check.ts` (shebang, leading-comment-as-help, ESM-safe self-path).

## Recalibration (assume-decomposition-has-mistakes)

The 081KR50HA0008QG0R001CNS20T child sketch specified `conciseness` as "CLAUDE.md length **<50**". That bound is **empirically wrong** — the live bootstrap CLAUDE.md is **~76 lines** and that *is* the correct bootstrap form (Sections 1-6 + a short Conventions block). A hard `<50` would fail a correct file. The load-bearing invariant is **structural** (6-step process + rules auto-load surface), so conciseness is a **soft `--max-lines` warn** (default 150), not a hard fail. Recorded as a Progress note on the 081KR50HA0008QG0R001CNS20T row.

## Focused checks (run on this branch)

- `bun test tools/bootstrap-validator/validate-bootstrap-claude-md.test.ts` → **15 pass / 0 fail** (21 expect() calls). Includes a self-validation against the live repo root and the 76-line recalibration anchor.
- CLI exit-code matrix verified: live repo `→ 0`, bad `--root` `→ 3`, `--help` `→ 1`, bad arg `→ 1`.
- `--json` output verified well-formed.

**Build gate:** TS + docs only; no F#/.NET sources touched, so `dotnet build -c Release` is unaffected by this slice (not re-run for a non-.NET change).

## Scope discipline

One bounded step, one PR. Claim `081KR50HA0008QG0R001CNS20T.1` acquired via `tools/bus/claim.ts` (otto-cli). Authored in an isolated worktree off fresh `origin/main`; contested root checkout untouched.

Remaining children: **081KR50HA0008QG0R001CNS20T.2** (execute minimal validation), **081KR50HA0008QG0R001CNS20T.3** (document findings + file gap children, update parent 081KR2E4K0008QG0R001F0YB5S).

🤖 Generated with [Claude Code](https://claude.com/claude-code)


## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | true |
| Total threads | 4 |
| Resolved threads | 4 |
| Unresolved threads | 0 |
| Total review comments | 8 |
| Total fix commits (touching thread paths) | 2 |

## Review threads

### Thread 1 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6FxFxD`
- **Path**: `tools/bootstrap-validator/validate-bootstrap-claude-md.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-29T18:57:43Z on `tools/bootstrap-validator/validate-bootstrap-claude-md.ts`:139 (association: NONE)

  <pre>
  `checkConciseness` counts lines via `claudeMd.split("\n").length`, which over-counts by 1 when the file ends with a trailing newline (common for POSIX-formatted text). That can trigger a false `warn` when CLAUDE.md is exactly `--max-lines` lines.

  Normalize newlines and trim a single trailing newline before counting so the ceiling behaves deterministically.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-29T19:06:52Z on `tools/bootstrap-validator/validate-bootstrap-claude-md.ts`:139 (association: MEMBER)

    <pre>
    Fixed in 90935a045 — checkConciseness now normalizes CRLF and strips a single trailing newline before counting, so a POSIX trailing newline no longer over-counts by 1 / trips a false warn at exactly --max-lines.
    </pre>

### Thread 2 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6FxFxb`
- **Path**: `tools/bootstrap-validator/validate-bootstrap-claude-md.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-29T18:57:43Z on `tools/bootstrap-validator/validate-bootstrap-claude-md.ts`:229 (association: NONE)

  <pre>
  `parseArgs` treats the next token as a value even if it's another flag (e.g. `--root --json`), which can turn a usage error into a validation failure (exit 3) against a bogus root. Also, `--max-lines` claims to require an integer but currently accepts floats (e.g. `1.5`).

  Reject dash-prefixed values and require an integer for `--max-lines` so bad invocations reliably return exit code 1.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-29T19:06:54Z on `tools/bootstrap-validator/validate-bootstrap-claude-md.ts`:229 (association: MEMBER)

    <pre>
    Fixed in 90935a045 — parseArgs now rejects dash-prefixed values for --root and --max-lines (no flag-eats-flag), and --max-lines requires Number.isInteger (rejects floats like 1.5). Bad invocations reliably return exit 1.
    </pre>

### Thread 3 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6FxFxr`
- **Path**: `docs/backlog/P1/081KR50HA0008QG0R001CNS20T-fresh-instance-validation-bootstrap-claude-md.md`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-29T18:57:43Z on `docs/backlog/P1/081KR50HA0008QG0R001CNS20T-fresh-instance-validation-bootstrap-claude-md.md`:70 (association: NONE)

  <pre>
  This backlog row was updated (new Progress section), but the YAML frontmatter `last_updated` is still `2026-05-09`. Per the backlog conventions, `last_updated` should be bumped on every content edit so downstream tooling and readers can trust recency.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-29T19:06:57Z on `docs/backlog/P1/081KR50HA0008QG0R001CNS20T-fresh-instance-validation-bootstrap-claude-md.md`:70 (association: MEMBER)

    <pre>
    Fixed in 90935a045 — bumped frontmatter last_updated 2026-05-09 -&gt; 2026-05-29.
    </pre>

### Thread 4 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6FxFx6`
- **Path**: `docs/backlog/P1/081KR50HA0008QG0R001CNS20T-fresh-instance-validation-bootstrap-claude-md.md`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-29T18:57:44Z on `docs/backlog/P1/081KR50HA0008QG0R001CNS20T-fresh-instance-validation-bootstrap-claude-md.md`:83 (association: NONE)

  <pre>
  Line 83 starts with `+` (`+ file gap children...`), which markdownlint will interpret as a list item marker. This repo has a known CI failure mode for wrapped lines starting with `+`.

  Reword so the continuation line doesn’t begin with `+` (e.g., use “and”).
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-29T19:06:58Z on `docs/backlog/P1/081KR50HA0008QG0R001CNS20T-fresh-instance-validation-bootstrap-claude-md.md`:83 (association: MEMBER)

    <pre>
    Fixed in 90935a045 — reworded so the wrapped continuation reads 'and file gap children' instead of '+ file gap children'. Resolves the MD032 markdownlint required-check failure too (verified locally, falsified against a known-bad fixture).
    </pre>

## Fix commits (touching thread paths)

### `5f7d3a1a3f286717f4bf577d65dfc84970db4655` -- 2026-05-29T18:53:56Z -- `claude`

**Touched files (intersect with thread paths):**

- `docs/backlog/P1/081KR50HA0008QG0R001CNS20T-fresh-instance-validation-bootstrap-claude-md.md`
- `tools/bootstrap-validator/validate-bootstrap-claude-md.ts`

**Message:**

```
feat(081KR50HA0008QG0R001CNS20T.1): static structural validator for bootstrap CLAUDE.md

Smallest safe slice of 081KR50HA0008QG0R001CNS20T. Ships the fresh-instance bootstrap
validator HARNESS SKELETON (no Claude spawn — that is 081KR50HA0008QG0R001CNS20T.2/.3):

- tools/bootstrap-validator/validate-bootstrap-claude-md.ts
  Checks: CLAUDE.md exists; 6-step bootstrap process present
  (## 1..## 6); .claude/rules/ auto-load surface non-empty;
  conciseness (soft warn). Flags --json/--root/--max-lines/--help;
  exit 0 pass / 1 usage / 3 fail. Convention-matched to
  tools/cold-start-check.ts.
- + .test.ts (15 tests pass) including a self-validation against the
  live repo root and the 76-line recalibration anchor.

Recalibration (assume-decomposition-has-mistakes): the child sketch's
"length <50" bound is empirically wrong — the live bootstrap CLAUDE.md
is ~76 lines and that IS correct. Load-bearing invariant is structural,
so conciseness is a SOFT --max-lines warn (default 150), not a hard fail.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `90935a04546b2410edf6a27bea2eb82b53f0cf50` -- 2026-05-29T19:06:03Z -- `claude`

**Touched files (intersect with thread paths):**

- `docs/backlog/P1/081KR50HA0008QG0R001CNS20T-fresh-instance-validation-bootstrap-claude-md.md`
- `tools/bootstrap-validator/validate-bootstrap-claude-md.ts`

**Message:**

```
fix(081KR50HA0008QG0R001CNS20T.1): markdownlint MD032 + 3 Copilot review findings

- docs/backlog/P1/081KR50HA0008QG0R001CNS20T: reword wrapped line so continuation no longer
  begins with `+` (markdownlint MD032 required-check failure + thread 4)
- docs/backlog/P1/081KR50HA0008QG0R001CNS20T: bump last_updated 2026-05-09 -> 2026-05-29 (thread 3)
- validate-bootstrap-claude-md.ts checkConciseness: normalize CRLF + strip
  single trailing newline before line count so a POSIX trailing newline
  cannot over-count by 1 and trip a false warn at exactly maxLines (thread 1)
- validate-bootstrap-claude-md.ts parseArgs: reject dash-prefixed values for
  --root/--max-lines (flag-eats-flag) and require an integer for --max-lines
  (reject floats like 1.5); bad invocations now reliably return exit 1 (thread 2)

Verified: 15/15 validator tests pass; markdownlint exit 0 on the backlog
file (falsified against a known-bad fixture); no tsc errors in tools/.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```
