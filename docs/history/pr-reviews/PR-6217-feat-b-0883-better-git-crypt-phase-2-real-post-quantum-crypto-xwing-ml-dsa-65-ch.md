# PR #6217 -- feat(081KSNY2Z0008QG0R002JKH50A): better-git-crypt Phase 2 — real post-quantum crypto (XWing + ML-DSA-65 + ChaCha20-Poly1305 + CBOR)

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 6217 |
| Title | feat(081KSNY2Z0008QG0R002JKH50A): better-git-crypt Phase 2 — real post-quantum crypto (XWing + ML-DSA-65 + ChaCha20-Poly1305 + CBOR) |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-05-31T16:07:16Z |
| Merged at | 2026-05-31T17:00:37Z |
| Merge commit SHA | `d62e6cbeb22729b940ee47b63cc64042811f4620` |
| Branch | `otto-cli/b0883-encryption-phase2-real-crypto-2026-05-31` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/6217 |
| Changed files | 8 |
| Additions / deletions | +1255 / -134 |

## Description

## 081KSNY2Z0008QG0R002JKH50A Phase 2 — real crypto (operator-authorized 2026-05-31)

Wires the v1 type scaffold (`types.ts`) to actual post-quantum primitives in new `crypto.ts`: `generateRecipientKeyPair` / `encrypt` / `decrypt` / `encodeEnvelope` / `decodeEnvelope`.

### What's implemented
- **XWing** (ML-KEM-768 + X25519 hybrid) KEM + **ML-DSA-65** signatures — `@noble/post-quantum`
- **HKDF-SHA256** — `@noble/hashes`; **ChaCha20-Poly1305** content + CEK-wrap AEAD — `@noble/ciphers`
- **canonical CBOR** envelope — `cborg` (deterministic, so sign-over-encoded-bytes / verify agree)
- signature-first **fail-closed** verify; FIPS-203 implicit-rejection → `KemFailure`
- per-recipient KEM encapsulation + single shared CEK for content; zero-nonce CEK-wrap with the single-use-wrapKey safety invariant documented inline
- `contentNonce` added to `FileEnvelope` (memo schema `content_nonce`)

### Empirical API correction (runtime = the oracle)
A probe against the **installed** packages caught two errors in the v1 design memo's pseudocode before they reached code:
- memo's `chacha20poly1305(key).encrypt(nonce, pt)` **fails** on `@noble/ciphers@2.2.0` → corrected to `chacha20poly1305(key, nonce).encrypt(pt)`
- memo's 3-arg `ml_dsa65.sign(msg, sk, context)` **fails** → 2-arg `sign(msg, sk)`; domain-separation `ctx` lives inside the signed CBOR instead

### Tests — 51 pass / 0 fail
23 new `crypto.test.ts`: keygen sizes, single + multi-recipient round-trip, empty + 8 KiB binary payloads, all encrypt/decrypt `TFeedback` failure modes (EmptyRecipientSet, SenderNotInRecipientSet, RecipientKeyInvalid, SeedSourceNotAvailable, RecipientNotInEnvelope, KemFailure, SignatureInvalid on tampered ciphertext/signature/wrong-key), and on-disk CBOR encode/decode round-trip + determinism.

typecheck clean (CI gate); eslint clean; prettier clean.

### Deps (pinned current-latest per dep-pin-search-first-authority, verified 2026-05-31)
`@noble/post-quantum@0.6.1` · `@noble/ciphers@2.2.0` · `@noble/hashes@2.2.0` · `cborg@5.1.1`

### ⚠️ crypto-don't-rush gate (operator decision, README §"Phase 2 operator decisions")
This lands the **library + round-trip tests only** — it is **not wired to any real secret** (no recipients.json, no git textconv, no real-data flow). Per the operator's gate, **KATs against Noble's published vectors + formal-verification + security-ops review of the envelope & key-handling are REQUIRED before this holds anything real.** Those are tracked follow-ups; do not use for real secrets until they're done.

### Still deferred (post-Phase-2)
git textconv filter · recipient management (`.zeta-crypt/recipients.json` + rotation) · multi-cipher hedge (081KSNY2Z0008QG0R002ZAVMEK) · metadata encryption (081KSNY2Z0008QG0R0020KXAPS)

Unblocks **081KSNY2Z0008QG0R0011XCT94** (zflash USB-bound crypto integration) + **081KSNY2Z0008QG0R0030V5ZVS** (agent private encrypted state).

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | true |
| Total threads | 20 |
| Resolved threads | 20 |
| Unresolved threads | 0 |
| Total review comments | 20 |
| Total fix commits (touching thread paths) | 10 |

## Review threads

### Thread 1 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F9B3w`
- **Path**: `tools/crypto/better-git-crypt/crypto.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-31T16:09:46Z on `tools/crypto/better-git-crypt/crypto.ts`:263 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Reject non-ML-DSA signature algorithms**

  When a caller supplies a sender `RecipientKey` with `sigAlgId: "SLH-DSA"` (which `ALG_REGISTRY` advertises as `ships-v1` and `determineEncryptionPath` accepts), `encrypt` records `algSig: "SLH-DSA"` in the signed view but still signs with `ml_dsa65`. The envelope can only round-trip because `decrypt` also ignores `algSig` and verifies with ML-DSA, so the persisted algorithm metadata lies and a real SLH-DSA key path will fail; either mark SLH-DSA deferred or dispatch/reject before signing.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 2 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F9Cmh`
- **Path**: `tools/crypto/better-git-crypt/crypto.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-31T16:12:06Z on `tools/crypto/better-git-crypt/crypto.ts`:358 (association: NONE)

  <pre>
  P1: `encrypt` only compares the secret-key identity string before signing. If callers pass a stale or wrong ML-DSA secret key with the same identity, this returns an envelope signed as `context.sender.identity` that immediately fails verification for the sender public key, creating an undecryptable artifact instead of surfacing `RecipientKeyInvalid` at encryption time.
  </pre>

### Thread 3 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F9Cmm`
- **Path**: `tools/crypto/better-git-crypt/cli/main.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-31T16:12:06Z on `tools/crypto/better-git-crypt/cli/main.ts`:161 (association: NONE)

  <pre>
  P1: the dry-run CLI still reports the Phase 2 crypto integrations as `integrationPending`, including the CBOR entry naming a placeholder library, even though this PR implements them in `crypto.ts` with `cborg`. This makes the CLI output contradict the new README/PR state.
  </pre>

### Thread 4 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F9Cmn`
- **Path**: `tools/crypto/better-git-crypt/crypto.test.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-31T16:12:07Z on `tools/crypto/better-git-crypt/crypto.test.ts`:223 (association: NONE)

  <pre>
  This failure-mode test is too permissive for the current decrypt contract. `decrypt` checks `envelope.context` before structural validation or signature verification, so allowing `EnvelopeMalformed` or `SignatureInvalid` would let a regression from the precise `ContextMismatch` feedback pass unnoticed.
  </pre>

### Thread 5 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F9Cmp`
- **Path**: `tools/crypto/better-git-crypt/crypto.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-31T16:12:07Z on `tools/crypto/better-git-crypt/crypto.ts`:578 (association: NONE)

  <pre>
  `decodeEnvelope` accepts any byte-string length for `contentNonce`, even though the new `FileEnvelope` field is defined as a 12-byte ChaCha20-Poly1305 nonce. A malformed encoded envelope with a signed but wrong-length nonce will decode as structurally valid and only fail later as a content decrypt error.
  </pre>

### Thread 6 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F9EwL`
- **Path**: `tools/crypto/better-git-crypt/crypto.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-31T16:19:59Z on `tools/crypto/better-git-crypt/crypto.ts`:285 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Guard malformed sender signature keys**

  When a caller supplies a sender `RecipientKey` whose `publicSigKey` is truncated or otherwise not an ML-DSA-65 public key, this self-verify can throw from Noble's key decoder instead of returning the advertised `EncryptResult`; `decrypt` already wraps the same verify call in `try/catch`, but `encrypt` does not, so malformed registry data escapes the Result-over-exception contract. Wrap this verify and convert it to `RecipientKeyInvalid`/`SignatureFailure` the same way the rest of `encrypt` handles bad crypto inputs.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 7 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F9EwP`
- **Path**: `tools/crypto/better-git-crypt/crypto.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-31T16:19:59Z on `tools/crypto/better-git-crypt/crypto.ts`:321 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Require the sender's actual KEM key in recipients**

  If `context.recipients` contains an entry with the sender identity but a stale/different `publicKemKey` than `context.sender` (a realistic rotation or registry-staleness case), planning has already accepted the context by identity and this loop wraps the CEK only to that stale recipient key. `encrypt` then returns `ok`, but the sender's supplied KEM secret cannot unwrap its own slot because `decrypt` also selects by identity, producing an envelope the sender cannot recover despite the self-recipient invariant; validate key equality for the sender entry or use `context.sender` as the sender slot.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 8 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F9E3j`
- **Path**: `tools/crypto/better-git-crypt/crypto.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-31T16:20:30Z on `tools/crypto/better-git-crypt/crypto.ts`:581 (association: NONE)

  <pre>
  P1: `decodeEnvelope` currently treats an unsupported-but-well-formed envelope version as a generic `EnvelopeMalformed`, so callers that decode on-disk bytes never receive the authored `VersionUnsupported` feedback that `decrypt` exposes for the same condition. Split the type/shape check from the v1 support check so future-version envelopes fail with the precise feedback.
  </pre>

### Thread 9 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F9E3v`
- **Path**: `tools/crypto/better-git-crypt/crypto.test.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-31T16:20:30Z on `tools/crypto/better-git-crypt/crypto.test.ts`:78 (association: NONE)

  <pre>
  P2: This adds person-name test identities in code, but the repository convention keeps current-state code/tests role-referenced rather than name-attributed outside the closed history surfaces. Please rename the new test fixtures to neutral role identities such as `sender@zeta`, `recipient-a@zeta`, and `recipient-b@zeta` throughout this test file.
  </pre>

### Thread 10 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F9E35`
- **Path**: `tools/crypto/better-git-crypt/types.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-31T16:20:30Z on `tools/crypto/better-git-crypt/types.ts`:130 (association: NONE)

  <pre>
  P1: `contentNonce` is documented as a fixed 12-byte nonce, but the shared `validateEnvelopeStructure` path was not updated to enforce that invariant. Callers that validate or decrypt a `FileEnvelope` without going through `decodeEnvelope` can pass a malformed nonce and get a later crypto failure instead of `EnvelopeMalformed`.
  </pre>

### Thread 11 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F9HKF`
- **Path**: `tools/crypto/better-git-crypt/crypto.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-31T16:30:04Z on `tools/crypto/better-git-crypt/crypto.ts`:462 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Handle duplicate recipient slots before failing KEM**

  If a recipient identity appears more than once, e.g. after a stale registry entry is kept before the current key, `encrypt` will emit both slots but `decrypt` only tries the first matching slot. In that case a valid later slot for the same identity is ignored and decryption returns `KemFailure`; either reject duplicate identities during encryption/decoding or try all matching slots before failing.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 12 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F9IkE`
- **Path**: `tools/crypto/better-git-crypt/crypto.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-31T16:36:13Z on `tools/crypto/better-git-crypt/crypto.ts`:542 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Reject non-canonical CBOR before verification**

  For envelopes loaded from git, a byte-level rewrite to another valid CBOR representation of the same fields (for example a different map ordering or other non-canonical encoding that `cborg` accepts) will be normalized by this decode and then `decrypt` verifies a freshly re-encoded signed view, so the signature still passes even though the on-disk bytes were tampered with. Since this format is documented as canonical/deterministic and used as a tamper-detection boundary, `decodeEnvelope` should reject non-canonical encodings, e.g. by strict decoding plus comparing the input bytes to `encodeEnvelope(envelope)` before accepting it.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 13 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F9I4v`
- **Path**: `tools/crypto/better-git-crypt/crypto.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-31T16:37:40Z on `tools/crypto/better-git-crypt/crypto.ts`:245 (association: NONE)

  <pre>
  P1: this rejection exposes a registry/planner inconsistency: `SLH-DSA` is still marked `ships-v1`, so callers can get a v1 plan that this implementation later rejects. Align the registry/status (or add real SLH-DSA dispatch) so `determineEncryptionPath` does not advertise an algorithm the crypto layer cannot produce.
  </pre>

### Thread 14 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F9I41`
- **Path**: `tools/crypto/better-git-crypt/crypto.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-31T16:37:41Z on `tools/crypto/better-git-crypt/crypto.ts`:205 (association: NONE)

  <pre>
  P1: the persistent CBOR shape written here uses camelCase keys, while the linked v1 design memo/README describe the on-disk memo schema with snake_case fields such as `content_nonce`, `kem_ct`, and `wrapped_cek`. Since this is the signed on-disk format, landing a different field shape without updating the schema will make future consumers implement the wrong format.
  </pre>

### Thread 15 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F9I44`
- **Path**: `tools/crypto/better-git-crypt/crypto.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-31T16:37:41Z on `tools/crypto/better-git-crypt/crypto.ts`:550 (association: NONE)

  <pre>
  P1: `decodeEnvelope` accepts unknown top-level CBOR fields and then verifies a reconstructed signed view that omits them. That permits unauthenticated data to be carried in an otherwise valid envelope; for a signed on-disk format, unknown fields should fail closed unless they are explicitly included in the signed schema.
  </pre>

### Thread 16 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F9Ket`
- **Path**: `tools/crypto/better-git-crypt/crypto.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-31T16:44:13Z on `tools/crypto/better-git-crypt/crypto.ts`:350 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Reject empty sender identities before encrypting**

  When `context.sender.identity` is empty, the planner accepts the context as long as the recipients list contains the same empty identity, and `generateRecipientKeyPair("")` can produce exactly that input. This assignment then signs and returns an envelope with `signerIdentity: ""`, but `validateEnvelopeStructure`/`decrypt` reject empty signer identities, so `encrypt` can report `ok` for an artifact the library itself treats as malformed. Please validate the sender identity and return `RecipientKeyInvalid` before building the signed view.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 17 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F9MCS`
- **Path**: `tools/crypto/better-git-crypt/crypto.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-31T16:50:53Z on `tools/crypto/better-git-crypt/crypto.ts`:398 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Verify the sender KEM secret before returning ok**

  If `senderSecretKeys.kemSecretKey` is stale or belongs to a different keypair while the signature secret is correct, the checks above still pass because encryption only compares public KEM keys and self-verifies the signature key. The sender slot is then wrapped to `context.sender.publicKemKey`, `encrypt` returns `ok`, and the required sender self-recipient cannot decrypt with the supplied KEM secret; add a sender-slot unwrap/self-decrypt check (or otherwise prove the KEM secret matches the public key) before returning the envelope.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 18 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F9MjF`
- **Path**: `tools/crypto/better-git-crypt/crypto.test.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-31T16:53:04Z on `tools/crypto/better-git-crypt/crypto.test.ts`:9 (association: NONE)

  <pre>
  P1: This comment overstates what the round-trip tests prove and contradicts the README/PR gate that KATs, formal verification, and security-ops review are still required before real use. A green wiring test does not prove cryptographic correctness against vectors or review the envelope/key-handling design.
  </pre>

### Thread 19 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F9MjN`
- **Path**: `tools/crypto/better-git-crypt/README.md`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-31T16:53:05Z on `tools/crypto/better-git-crypt/README.md`:22 (association: NONE)

  <pre>
  P1: The README now says Phase 2 is implemented, but the later “Phase 2 integration protocol” section still describes Phase 2 as future work (“When operator authorizes Phase 2”) and lists cbor-x/split-module steps that no longer match this PR’s cborg/crypto.ts implementation. Please update or replace that stale protocol section so the document has one current-state story.
  </pre>

### Thread 20 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F9MjR`
- **Path**: `tools/crypto/better-git-crypt/crypto.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-31T16:53:05Z on `tools/crypto/better-git-crypt/crypto.ts`:142 (association: NONE)

  <pre>
  P1: `generateRecipientKeyPair` is exported as part of the crypto surface and accepts the public `SeedSource` union, but unsupported seed sources throw instead of returning typed feedback. That breaks the Result-shaped error boundary used by `encrypt`/`decrypt` in this module and turns a user-selectable setup option into an exception path.
  </pre>

## Fix commits (touching thread paths)

### `5af41d41e0a12a47cbd194289bedae634c582a9b` -- 2026-05-31T16:06:51Z -- ``

**Touched files (intersect with thread paths):**

- `tools/crypto/better-git-crypt/README.md`
- `tools/crypto/better-git-crypt/cli/main.ts`
- `tools/crypto/better-git-crypt/crypto.test.ts`
- `tools/crypto/better-git-crypt/crypto.ts`
- `tools/crypto/better-git-crypt/types.ts`

**Message:**

```
feat(081KSNY2Z0008QG0R002JKH50A): better-git-crypt Phase 2 — real post-quantum crypto

Wires the v1 type scaffold to actual primitives (operator-authorized
2026-05-31). Implements generateRecipientKeyPair / encrypt / decrypt /
encodeEnvelope / decodeEnvelope:

- XWing (ML-KEM-768 + X25519) KEM + ML-DSA-65 signatures (@noble/post-quantum)
- HKDF-SHA256 (@noble/hashes) + ChaCha20-Poly1305 AEAD (@noble/ciphers)
- canonical CBOR envelope (cborg) — deterministic so sign-over-bytes / verify agree
- signature-first fail-closed verify; FIPS-203 implicit-rejection -> KemFailure
- per-recipient KEM + single-CEK content; zero-nonce CEK-wrap (single-use wrapKey invariant documented)
- contentNonce added to FileEnvelope (memo schema "content_nonce")

API shapes empirically verified against installed packages (the runtime is
the oracle): the v1 memo's chacha20poly1305(key).encrypt(nonce,pt) and 3-arg
ml_dsa.sign do NOT exist in these versions — corrected to
chacha20poly1305(key,nonce).encrypt(pt) + 2-arg sign with ctx-in-signed-CBOR.

Deps pinned current-latest (dep-pin-search-first-authority): @noble/post-quantum@0.6.1
@noble/ciphers@2.2.0 @noble/hashes@2.2.0 cborg@5.1.1.

Tests: 51 pass (23 new crypto round-trip + tamper-detection + CBOR codec).
typecheck clean; eslint clean; prettier clean. Unblocks 081KSNY2Z0008QG0R0011XCT94/081KSNY2Z0008QG0R0030V5ZVS.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `344296956bebbda2e9c3a4a2cc7833d2ad01ce26` -- 2026-05-31T16:12:45Z -- ``

**Touched files (intersect with thread paths):**

- `tools/crypto/better-git-crypt/crypto.test.ts`
- `tools/crypto/better-git-crypt/crypto.ts`

**Message:**

```
fix(081KSNY2Z0008QG0R002JKH50A): reject algs crypto.ts doesn't dispatch (no lying envelop…

…e metadata)

Codex P2 (PR #6217): ALG_REGISTRY advertises SLH-DSA as ships-v1 and
determineEncryptionPath accepts it, but crypto.ts hardcodes ml_dsa65 —
so an envelope with algSig "SLH-DSA" would record metadata that disagrees
with the actual signature bytes. encrypt + decrypt now guard
algKem===ML-KEM-768+X25519 and algSig===ML-DSA-65, returning AlgUnsupported
for anything this impl doesn't actually dispatch. +1 test (52 pass).

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `6055d81eba8ae98b0b2ea4fa1c5c7ca6b9a76692` -- 2026-05-31T16:15:24Z -- ``

**Touched files (intersect with thread paths):**

- `tools/crypto/better-git-crypt/cli/main.ts`
- `tools/crypto/better-git-crypt/crypto.test.ts`
- `tools/crypto/better-git-crypt/crypto.ts`

**Message:**

```
fix(081KSNY2Z0008QG0R002JKH50A): address Copilot review — encrypt self-verify, nonce-leng…

…th, CLI status, test precision

PR #6217 review fixes (all verified against the code):
- encrypt now self-verifies the signature against the sender's DECLARED public
  sig key after signing — catches a wrong/stale secret key (right identity) at
  encrypt time (RecipientKeyInvalid) instead of minting an undecryptable envelope
- decodeEnvelope rejects a wrong-length contentNonce (EnvelopeMalformed) rather
  than letting it surface late as ContentDecryptFailure
- dry-run CLI now reports phase2Implemented + stillDeferred + the before-real-use
  gate, instead of claiming Phase 2 is integrationPending (contradicted reality)
- context-mismatch test tightened to assert exactly ContextMismatch (decrypt
  checks context before structural/signature)
- +3 tests (54 pass): mismatched-key, wrong-nonce-length, SLH-DSA AlgUnsupported

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `8794612e064995eb3c40e13f118310e4f58c1a9a` -- 2026-05-31T16:16:16Z -- ``

**Touched files (intersect with thread paths):**

- `tools/crypto/better-git-crypt/crypto.ts`

**Message:**

```
fix(081KSNY2Z0008QG0R002JKH50A): String() wrap numbers in nonce-length error (restrict-te…

…mplate-expressions)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `9d5520104827fedb66081fb4540c39cf8787b05c` -- 2026-05-31T16:26:49Z -- ``

**Touched files (intersect with thread paths):**

- `tools/crypto/better-git-crypt/crypto.test.ts`
- `tools/crypto/better-git-crypt/crypto.ts`
- `tools/crypto/better-git-crypt/types.ts`

**Message:**

```
fix(081KSNY2Z0008QG0R002JKH50A): round-2 review — self-verify throw-guard, sender KEM con…

…sistency, decode VersionUnsupported, shared nonce-length, role-named tests

PR #6217 (Codex + Copilot):
- encrypt self-verify wrapped in try/catch (Noble decoder throws on malformed
  publicSigKey) -> RecipientKeyInvalid instead of breaking the Result contract
- encrypt guards the sender's recipients-entry publicKemKey == context.sender's
  (stale/rotated-key footgun -> RecipientKeyInvalid)
- decodeEnvelope splits version check -> precise VersionUnsupported for a
  well-formed future version (not generic EnvelopeMalformed)
- validateEnvelopeStructure (shared) now enforces the 12-byte contentNonce
- test fixtures renamed to neutral roles (sender/recipient-a/recipient-b)
- +2 tests (56 pass): malformed-sig-key no-throw, decode VersionUnsupported

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `af12173b787df0d072464feeab5f86a3acfe85c0` -- 2026-05-31T16:32:17Z -- ``

**Touched files (intersect with thread paths):**

- `tools/crypto/better-git-crypt/crypto.test.ts`
- `tools/crypto/better-git-crypt/crypto.ts`

**Message:**

```
fix(081KSNY2Z0008QG0R002JKH50A): reject duplicate recipient identities (Codex P2)

decrypt resolves a recipient by first-match identity, so two slots for one
identity (stale registry entry before the current key) would make a valid
later slot unreachable and surface a spurious KemFailure. encrypt now rejects
duplicate identities up front -> RecipientKeyInvalid. +1 test (57 pass).

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `76c18d4782d0f01fa2293db74078579df77702bd` -- 2026-05-31T16:33:07Z -- ``

**Touched files (intersect with thread paths):**

- `tools/crypto/better-git-crypt/crypto.ts`

**Message:**

```
refactor(081KSNY2Z0008QG0R002JKH50A): extract firstDuplicateIdentity helper (encrypt cogn…

…itive-complexity)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `10cbc8bffa351f381c74012da3c9afd2a8f4b954` -- 2026-05-31T16:41:47Z -- ``

**Touched files (intersect with thread paths):**

- `tools/crypto/better-git-crypt/README.md`
- `tools/crypto/better-git-crypt/crypto.test.ts`
- `tools/crypto/better-git-crypt/crypto.ts`
- `tools/crypto/better-git-crypt/types.ts`

**Message:**

```
fix(081KSNY2Z0008QG0R002JKH50A): crypto-hardening round — canonical bytes, registry consi…

…stency, wire schema

Deep-review findings (Codex + Copilot, PR #6217 — the formal-verification/
security-ops class the crypto-don't-rush gate wants):
- decodeEnvelope enforces canonical bytes (re-encode + compare): rejects
  non-canonical CBOR malleability AND unknown unauthenticated top-level fields,
  bit-locking the on-disk form to the signed content (closes 2 findings)
- SLH-DSA registry status ships-v1 -> deferred-alternate: the planner no longer
  advertises a signature crypto.ts can't dispatch (registry/planner consistency)
- README documents the canonical camelCase flat wire schema as authoritative,
  superseding the memo's pre-implementation sketch
- +1 test (58 pass): unknown-field/canonical rejection

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `da12cd0d2a11f399d059f70d8f968c9d89e30df4` -- 2026-05-31T16:48:34Z -- ``

**Touched files (intersect with thread paths):**

- `tools/crypto/better-git-crypt/crypto.test.ts`
- `tools/crypto/better-git-crypt/crypto.ts`

**Message:**

```
fix(081KSNY2Z0008QG0R002JKH50A): reject empty recipient/sender identity before encrypt

Codex P2 (PR #6217): generateRecipientKeyPair("") yields identity:"";
encrypting with it mints an envelope with signerIdentity:"" that decrypt
can't resolve later -> undecryptable artifact. firstDuplicateIdentity ->
firstInvalidIdentity now rejects empty + duplicate identities in one pass
(sender must be in the recipient set, so this also guards an empty sender).
+1 test.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `cd214ab14ddb6ea813cb62278d636edb0500b9e2` -- 2026-05-31T16:57:37Z -- ``

**Touched files (intersect with thread paths):**

- `tools/crypto/better-git-crypt/README.md`
- `tools/crypto/better-git-crypt/crypto.test.ts`
- `tools/crypto/better-git-crypt/crypto.ts`

**Message:**

```
fix(081KSNY2Z0008QG0R002JKH50A): round-7 review — sender KEM self-check, no-throw keygen,…

… doc scope-honesty

Codex P2 + Copilot P1 x3 on PR #6217:
- encrypt now self-decrypts the sender's own slot (kemSecretUnwrapsSlot) — a
  stale/mismatched sender KEM secret (right public key, wrong secret) was minting
  envelopes the sender couldn't decrypt. KEM-side analogue of the sig self-verify. +test
- generateRecipientKeyPair param narrowed to literal "random-bytes": unsupported
  seed source is now a COMPILE error, not a runtime throw (Result-boundary by
  construction). Removed the throw; replaced the throw-test with a @ts-expect-error
  compile-guarantee test. Dropped orphaned SeedSource import.
- crypto.test.ts header: scope-honest — wiring tests prove the API composes, NOT
  cryptographic correctness; KATs+formal-verification+security-ops still required
  (crypto-don't-rush gate).
- README: replaced stale 'Phase 2 integration protocol' (cbor-x/split-module sketch)
  with 'Phase 2 — as implemented' (single crypto.ts/cborg) — one current-state story.

29 tests pass, tsc clean.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```
