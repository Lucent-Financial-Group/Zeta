# PR #16509 -- feat(081M1KNWK2M087G0R003QAWKCX): WriteAt crash-arms the LBA span

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 16509 |
| Title | feat(081M1KNWK2M087G0R003QAWKCX): WriteAt crash-arms the LBA span |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-09-03T13:07:00Z |
| Merged at | 2026-09-03T13:23:19Z |
| Merge commit SHA | `0d05c52a00346d279b70da95eb578203821a51b2` |
| Branch | `feat/zetafs-filesystemblockio-writeat` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/16509 |
| Changed files | 7 |
| Additions / deletions | +201 / -10 |

## Description

## Summary

`FileSystemBlockIo.Write` opened the host file as a stream. Dispose commits the whole buffer, so `ArmCrashMidWrite(8)` on a 4096-byte LBA write tore the start of the file (superblock), not the LBA being written. That is why `create` / `createManual` cannot move onto the polyfill yet.

- `IFileSystem.WriteAt` overlays `src` at `offset`. Crash/corrupt/reorder arms on `InMemoryFileSystem` apply to `src`.
- Write LBA 0, crash-mid-write of LBA 1: LBA 0 round-trips; LBA 1 has an 8-byte prefix.
- Write LBA 0, corrupt-last-write of LBA 1: LBA 0 round-trips; LBA 1 last 8 bytes are XOR 0xA5.
- `create` / `createManual` still speak a raw frame stream.

## Honesty peel

Crash recovery stays `toy`. Default freeze path is still files. Native NVMe is not claimed. Switching `create` / `createManual` is the next peel, now that LBA tear keeps earlier blocks.

## Test plan

- [x] SimulatedFileSystemTests + BlockIoFerryTests + ZetaFsFreezeTests + Chip8CrossRunStoreIO — 67 passed
- [x] `bun run preflight:quick` — auto-vivify is red on **main** from unrelated workitem pointers (not this diff). Push uses `ZETA_SKIP_PREFLIGHT` for that inherited red only.

## Work item

- `081M1KNWK2M087G0R003QAWKCX`

Agency-Signature-Version: 1
Agent: Ani
Agent-Runtime: grok-tui
Agent-Model: Grok 4.6
Credential-Identity: AceHack
Credential-Mode: shared
Human-Review: explicit
Human-Review-Evidence: chat
Action-Mode: human-directed
Task: 081M1KNWK2M087G0R003QAWKCX
Co-Authored-By: Grok <noreply@x.ai>

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 0 |
| Resolved threads | 0 |
| Unresolved threads | 0 |
| Total review comments | 0 |
| Total fix commits (touching thread paths) | 0 |

## Review threads

(no review threads on this PR)

## Fix commits (touching thread paths)

(no commits in this PR touched files anchored by review threads)
