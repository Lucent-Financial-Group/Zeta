# PR #1424 -- feat(C3 partial): LWW-Register semilattice property tests (3 more)

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 1424 |
| Title | feat(C3 partial): LWW-Register semilattice property tests (3 more) |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-05-03T15:27:49Z |
| Merged at | 2026-05-03T15:38:48Z |
| Merge commit SHA | `321627866e778c39b649c77c6857fc5d56aac7ca` |
| Branch | `otto/c3-lwwregister-properties-2026-05-03` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/1424 |
| Changed files | 1 |
| Additions / deletions | +57 / -0 |

## Description

## Summary

Three FsCheck properties for `LwwRegister`, completing the third CRDT in the C3 list (after PN-Counter #1420 and OR-Set #1423):
- LWW-register merge is commutative
- LWW-register merge is idempotent
- LWW-register merge is associative

LwwRegister.Merge picks larger timestamp; ties broken by replica-id (lexicographic ordinal) for determinism. The semilattice laws hold because (timestamp, replica) is a total order with deterministic tie-breaking.

Math-proofs assessment matrix C3 row: running total 9 of ~15 target after this PR (G-Counter already had 3; +PN-Counter 3 +OR-Set 3 +LWW 3).

## Test plan

- [x] All 3 properties pass: `dotnet test --filter "FullyQualifiedName~LWW-register"` → 3 passed in 97ms.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 5 |
| Resolved threads | 3 |
| Unresolved threads | 2 |
| Total review comments | 5 |
| Total fix commits (touching thread paths) | 1 |

## Review threads

### Thread 1 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5_Mky3`
- **Path**: `tests/Tests.FSharp/Properties/Math.Invariants.Tests.fs`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-03T15:29:43Z on `tests/Tests.FSharp/Properties/Math.Invariants.Tests.fs`:120 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Constrain commutativity property to valid LWW writes**

  This property generates `a` and `b` independently, so it includes inputs where `tA = tB` and `rA = rB` but `vA &lt;&gt; vB`; for those states, `LwwRegister.Merge a b` returns `a` while `LwwRegister.Merge b a` returns `b` because equal replicas hit the left-biased `&gt;=` branch, so the law fails even though the test currently “passes” by chance. That makes this a flaky property rather than a stable semilattice check unless you either enforce the LWW invariant that `(timestamp, replica)` uniquely identifies a write or add a deterministic tie-breaker for fully equal keys.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 2 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5_MlNv`
- **Path**: `tests/Tests.FSharp/Properties/Math.Invariants.Tests.fs`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-03T15:31:01Z on `tests/Tests.FSharp/Properties/Math.Invariants.Tests.fs`:211 (association: NONE)

  <pre>
  These properties are very unlikely to exercise the tie-breaking path (equal timestamps and/or equal replica IDs) because `tA/tB` come from the full `int` range and `NonEmptyString` has a huge domain; FsCheck will almost never generate equal keys. Consider shrinking the domain (e.g., timestamps from a small range and replica IDs from a small fixed set) and/or adding a dedicated property that forces `tA = tB` to actually validate the determinism/tie-break behavior you describe.

  </pre>

### Thread 3 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5_MlN2`
- **Path**: `tests/Tests.FSharp/Properties/Math.Invariants.Tests.fs`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-03T15:31:02Z on `tests/Tests.FSharp/Properties/Math.Invariants.Tests.fs`:110 (association: NONE)

  <pre>
  The rationale comment claims commutativity/idempotence/associativity follow solely because (Timestamp, Replica) is a total order, but `LwwRegister` state also includes `Value`. If two distinct states can exist with the same `(Timestamp, Replica)` but different `Value` (which `Create` allows), `Merge a b` becomes order-dependent (it picks the left argument when the keys are equal), so the semilattice-law explanation is incomplete. Either document/enforce the invariant that `(Timestamp, Replica)` uniquely identifies a write/value, or strengthen `Merge`/state representation so the laws hold for all representable states.
  </pre>

### Thread 4 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM5_Mnkx`
- **Path**: `tests/Tests.FSharp/Properties/Math.Invariants.Tests.fs`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-03T15:39:21Z on `tests/Tests.FSharp/Properties/Math.Invariants.Tests.fs`:169 (association: NONE)

  <pre>
  P2 (nit): The block comment says the “separate property below” uses `lwwEqual` on the whole state, but the test below is a `[&lt;Fact&gt;]` and only asserts on `.Value`. Please either update the comment to match what’s actually asserted, or change the fact to use `lwwEqual` if full-state equality is what you intend to pin.

  </pre>

### Thread 5 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM5_Mnk3`
- **Path**: `tests/Tests.FSharp/Properties/Math.Invariants.Tests.fs`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-03T15:39:21Z on `tests/Tests.FSharp/Properties/Math.Invariants.Tests.fs`:204 (association: NONE)

  <pre>
  P2 (nit): The fact comment calls this a “documented determinism contract”, but `src/Core/Crdt.fs` only documents tie-breaking by replica-id and doesn’t mention the additional left-bias when `(Timestamp, Replica)` are equal. Consider either documenting that behavior on `LwwRegister.Merge` (so this test matches the docs) or rewording the test comment to avoid implying it’s already documented elsewhere.

  </pre>

## Fix commits (touching thread paths)

### `d56e6774e5878abbc6ca7f9e93204533e220cdc3` -- 2026-05-03T15:27:40Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tests/Tests.FSharp/Properties/Math.Invariants.Tests.fs`

**Message:**

```
feat(C3 partial): LWW-Register semilattice property tests (3 more)

Three FsCheck properties for LwwRegister, mirroring the G-Counter
/ PN-Counter / OR-Set pattern:
- LWW-register merge is commutative
- LWW-register merge is idempotent
- LWW-register merge is associative

LwwRegister.Merge picks larger timestamp; ties broken by replica-id
(lexicographic ordinal) for determinism. The semilattice laws hold
because (timestamp, replica) is a total order with deterministic
tie-breaking.

Math-proofs assessment matrix C3 row: closes 3 more after PN-Counter +
OR-Set (running total: 9 of ~15 target).

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
```
