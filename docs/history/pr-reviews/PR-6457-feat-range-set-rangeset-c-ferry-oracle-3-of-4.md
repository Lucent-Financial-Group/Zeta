# PR #6457 -- feat(range-set): RangeSet C# ferry (oracle #3 of 4)

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 6457 |
| Title | feat(range-set): RangeSet C# ferry (oracle #3 of 4) |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-06-01T19:16:59Z |
| Merged at | 2026-06-01T19:28:26Z |
| Merge commit SHA | `a3832f11d4a010663791778f48ff9d5208fbd94d` |
| Branch | `otto-cli/rangeset-csharp-ferry-2026-06-01` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/6457 |
| Changed files | 8 |
| Additions / deletions | +391 / -0 |

## Description

Ferries the RangeSet TS reference (#6454, merged) to **C# — oracle #3 of 4** (TS/F#/C#/Rust). New self-contained `Zeta.Core.CSharp.RangeSet` project (own minimal `Result` port; zero external deps), following the Bonsai/Resume oracle pattern. `Interval(Lo, Hi)` record + `RangeSets` static ops: `Parse` (lenient input, strict `NotInteger`/`InvertedRange`/`Malformed` rejection vectors) / `Render` (canonical: sorted, disjoint, non-adjacent — overlapping AND touching coalesce) / `Contains` / `Union` / `Add` / `Size`. Non-negative JS-safe integers.

Replays `src/Core.TypeScript/range-set/golden-vectors.json`: `Render(Parse input) == canonical` + contains probes + canonical fixed-point (11 cases) + the 7 rejection vectors decline the specific feedback variant. **3 tests pass; builds 0/0** under latest-recommended + Meziantou (`Parse` split into `ParseToken` for MA0051; `Interval` not `Range` to avoid the `System.Range` clash; `RangeSets` ops-class to avoid MA0049 type==namespace).

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | true |
| Total threads | 3 |
| Resolved threads | 3 |
| Unresolved threads | 0 |
| Total review comments | 6 |
| Total fix commits (touching thread paths) | 2 |

## Review threads

### Thread 1 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GOWIv`
- **Path**: `src/Core.CSharp.RangeSet/RangeSets.cs`
- **Replies**: 1

**Initial comment:**

- **`github-code-quality[bot]` (bot)** at 2026-06-01T19:19:29Z on `src/Core.CSharp.RangeSet/RangeSets.cs`:39 (association: NONE)

  <pre>
  ## Missed opportunity to use Where

  &lt;p&gt;This foreach loop &lt;a class="Link" href="/Lucent-Financial-Group/Zeta/blob/2852b62224bdb254066fd4243d8595cb07a3183a/src/Core.CSharp.RangeSet/RangeSets.cs#L35-L35"&gt;implicitly filters its target sequence&lt;/a&gt; - consider filtering the sequence explicitly using '.Where(...)'.&lt;/p&gt;
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-06-01T19:25:35Z on `src/Core.CSharp.RangeSet/RangeSets.cs`:39 (association: MEMBER)

    <pre>
    No-op resolve: L33-39 is a digit-validation **early-return** loop (`return null` on first non-digit) inside `ParseNat`, not a filter-then-process. `.Where(...)` would allocate an enumerator and lose the short-circuit; the explicit loop is clearer and faster. Advisory style finding declined.
    </pre>

### Thread 2 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GOWI6`
- **Path**: `tests/Tests.CSharp/RangeSet/RangeSetTests.cs`
- **Replies**: 1

**Initial comment:**

- **`github-code-quality[bot]` (bot)** at 2026-06-01T19:19:29Z on `tests/Tests.CSharp/RangeSet/RangeSetTests.cs`:63 (association: NONE)

  <pre>
  ## Missed opportunity to use Select

  This foreach loop immediately &lt;a class="Link" href="/Lucent-Financial-Group/Zeta/blob/2852b62224bdb254066fd4243d8595cb07a3183a/tests/Tests.CSharp/RangeSet/RangeSetTests.cs#L61-L61"&gt;maps its iteration variable to another variable&lt;/a&gt; - consider mapping the sequence explicitly using '.Select(...)'.

  ---

  &lt;p&gt;Refactor the loop at &lt;code&gt;tests/Tests.CSharp/RangeSet/RangeSetTests.cs&lt;/code&gt; in &lt;code&gt;ReplaysEverySharedGoldenCaseRenderEqualsCanonicalAndContainsAgrees&lt;/code&gt; so the mapping from JSON element (&lt;code&gt;probe&lt;/code&gt;) to list (&lt;code&gt;arr&lt;/code&gt;) is done in the enumerable pipeline with &lt;code&gt;.Select(...)&lt;/code&gt;, rather than immediately inside the loop body.&lt;/p&gt;
  &lt;p&gt;Best fix (single, behavior-preserving change):&lt;/p&gt;
  &lt;ul&gt;
  &lt;li&gt;Update the &lt;code&gt;foreach&lt;/code&gt; header on the inner loop (currently line 59) from iterating raw probes to iterating mapped arrays:
  &lt;ul&gt;
  &lt;li&gt;from: &lt;code&gt;foreach (var probe in c.GetProperty("contains").EnumerateArray())&lt;/code&gt;&lt;/li&gt;
  &lt;li&gt;to: &lt;code&gt;foreach (var arr in c.GetProperty("contains").EnumerateArray().Select(probe =&amp;gt; probe.EnumerateArray().ToList()))&lt;/code&gt;&lt;/li&gt;
  &lt;/ul&gt;
  &lt;/li&gt;
  &lt;li&gt;Remove the now-unnecessary &lt;code&gt;var arr = ...&lt;/code&gt; statement in the loop body.&lt;/li&gt;
  &lt;li&gt;No new imports are needed because &lt;code&gt;System.Linq&lt;/code&gt; is already present.&lt;/li&gt;
  &lt;/ul&gt;
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-06-01T19:25:35Z on `tests/Tests.CSharp/RangeSet/RangeSetTests.cs`:63 (association: MEMBER)

    <pre>
    No-op resolve: the loop asserts inside each iteration (probe → arr → two assertions), it does not merely map. `.Select(...)` would split the assert away from the iteration and read worse for a test. Advisory style finding declined.
    </pre>

### Thread 3 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GOWiZ`
- **Path**: `src/Core.CSharp.RangeSet/RangeSets.cs`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-01T19:19:54Z on `src/Core.CSharp.RangeSet/RangeSets.cs`:128 (association: NONE)

  <pre>
  P0: `RangeSets.Parse` will throw a `NullReferenceException` if called with `null` (because of `s.Trim()`), which breaks the established “result over throw / no exception crosses the boundary” pattern used by the other C# oracles (e.g., `BonsaiCodec.Parse` returns an `Err` on null input). Handle `null` explicitly and return a `Malformed` feedback instead.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-06-01T19:25:33Z on `src/Core.CSharp.RangeSet/RangeSets.cs`:128 (association: MEMBER)

    <pre>
    Fixed in c7613eac2 — `Parse` now guards `if (s is null) return Decline(Malformed("input was not a string"))` before `s.Trim()`, matching the cited `BonsaiCodec.Parse` precedent (result-over-throw / no-exception-crosses-boundary). Regression test `ParseNullDeclinesMalformedRatherThanThrowing` added. Verified: Release build 0/0, RangeSet tests 4 pass.
    </pre>

## Fix commits (touching thread paths)

### `2852b62224bdb254066fd4243d8595cb07a3183a` -- 2026-06-01T19:16:48Z -- ``

**Touched files (intersect with thread paths):**

- `src/Core.CSharp.RangeSet/RangeSets.cs`
- `tests/Tests.CSharp/RangeSet/RangeSetTests.cs`

**Message:**

```
feat(range-set): RangeSet C# ferry (oracle #3 of 4)

Ferries the RangeSet TS reference (#6454, merged) to C# — oracle #3 of 4 (TS/F#/C#/Rust).
New self-contained Zeta.Core.CSharp.RangeSet project (own minimal Result port; zero external
deps), following the Bonsai/Resume oracle pattern. Interval(Lo,Hi) record + RangeSets static
ops: Parse (lenient input, strict NotInteger/InvertedRange/Malformed rejection vectors) /
Render (canonical: sorted, disjoint, non-adjacent — overlapping AND touching coalesce) /
Contains / Union / Add / Size. Non-negative JS-safe integers.

Replays src/Core.TypeScript/range-set/golden-vectors.json: Render(Parse input) == canonical
+ contains probes + canonical fixed-point (11 cases) + the 7 rejection vectors decline the
specific feedback variant. 3 tests pass; project builds 0/0 under latest-recommended +
Meziantou (Parse split into ParseToken to satisfy MA0051; Interval not Range to avoid the
System.Range clash; RangeSets ops-class to avoid MA0049 type==namespace).

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `c7613eac2ddae42de5515bf1f404d81be6a926ff` -- 2026-06-01T19:25:20Z -- ``

**Touched files (intersect with thread paths):**

- `src/Core.CSharp.RangeSet/RangeSets.cs`
- `tests/Tests.CSharp/RangeSet/RangeSetTests.cs`

**Message:**

```
fix(rangeset-csharp): Parse declines Malformed on null instead of NRE…

… (P0; result-over-throw parity)

Copilot P0 on #6457: RangeSets.Parse(string) called s.Trim() with no null guard, so a
null input threw NullReferenceException — breaking the result-over-throw /
no-exception-crosses-boundary contract the other C# oracles hold. Matches the cited
sibling precedent BonsaiCodec.Parse, which returns Err(MalformedJson) on null.

Fix: `if (s is null) return Decline(Malformed("input was not a string"))` before s.Trim().
Regression test ParseNullDeclinesMalformedRatherThanThrowing asserts null → Err Malformed,
no throw (C#-specific parity; the TS/F#/Rust oracles are non-null by type).

Verified: dotnet build Release → 0 warnings / 0 errors (analyzers + TreatWarningsAsErrors);
dotnet test --filter RangeSet → 4 pass / 0 fail.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```
