# PR #2450 -- fix: also skip code-scanning/default-setup 403 with insufficient-token-scope sentinel

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 2450 |
| Title | fix: also skip code-scanning/default-setup 403 with insufficient-token-scope sentinel |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-05-10T09:47:21Z |
| Merged at | 2026-05-10T09:50:43Z |
| Merge commit SHA | `7bf23cfac73fcb86a491ce278ee2fc1b0594e7af` |
| Branch | `fix/snapshot-drift-codeql-403` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/2450 |
| Changed files | 1 |
| Additions / deletions | +4 / -3 |

## Description

## Summary

- PRs #2448 and #2449 fixed `actions/permissions` and `actions/variables` 403s respectively, but CI immediately revealed `code-scanning/default-setup` hits the same problem
- Applies the identical sentinel pattern: `ghApiOptional` + `{_skipped:"insufficient-token-scope"}` fallback
- `check-github-settings-drift.ts` already strips any top-level field with that sentinel before diffing — no changes needed there

## Root cause

`/repos/.../code-scanning/default-setup` requires admin token scope. The CI `GITHUB_TOKEN` lacks it, producing:
```
error: snapshot failed: gh api /repos/.../code-scanning/default-setup failed (exit 1): gh: Resource not accessible by integration (HTTP 403)
```

## Test plan

- [x] `bun tsc --noEmit` passes clean
- [x] Single pattern change: `ghApi` → `ghApiOptional` + sentinel for `code-scanning/default-setup`
- [x] No change needed to drift checker — sentinel-stripping loop already handles any skipped key

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 2 |
| Resolved threads | 2 |
| Unresolved threads | 0 |
| Total review comments | 2 |
| Total fix commits (touching thread paths) | 1 |

## Review threads

### Thread 1 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6A48HK`
- **Path**: `tools/hygiene/snapshot-github-settings.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-10T09:48:57Z on `tools/hygiene/snapshot-github-settings.ts`:252 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Restrict sentinel fallback to 403 scope errors**

  This change makes `codeql_default_setup` skippable for **any** `gh api` failure, not just the intended insufficient-scope 403, because `ghApiOptional` returns `null` on every non-zero exit and line 252 converts that to `_skipped`. In `check-github-settings-drift.ts`, skipped fields are removed before diffing, so transient API failures, endpoint regressions, or jq/query errors on `/code-scanning/default-setup` will be silently hidden instead of failing the snapshot. Please gate this fallback on the specific 403/token-scope failure and keep other errors fatal.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 2 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6A48TO`
- **Path**: `tools/hygiene/snapshot-github-settings.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-10T09:49:45Z on `tools/hygiene/snapshot-github-settings.ts`:252 (association: NONE)

  <pre>
  P1: `ghApiOptional` returns `null` for any non-zero `gh api` failure (not just HTTP 403). In this block that means transient API failures, jq filter errors, or other unexpected problems will be recorded as `{ _skipped: "insufficient-token-scope" }` and then stripped by the drift checker, potentially hiding real regressions. Consider distinguishing 403 "insufficient-token-scope" from other errors (e.g., parse stderr for `(HTTP 403)` / `Resource not accessible by integration` and otherwise fail the snapshot or emit a different sentinel that is not stripped).
  </pre>

## Fix commits (touching thread paths)

### `d25c09bf7b1e293036ca3f284c0e14bc24eee5a2` -- 2026-05-10T09:45:48Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/hygiene/snapshot-github-settings.ts`

**Message:**

```
fix: also skip code-scanning/default-setup 403 with insufficient-toke…

…n-scope sentinel

Applies the same ghApiOptional + sentinel pattern to code-scanning/default-setup
(HTTP 403 in CI) that PRs #2448 and the previous commit applied to
actions/permissions and actions/variables. The drift checker already strips
any top-level key carrying the sentinel before diffing, so no changes needed there.

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>
```
