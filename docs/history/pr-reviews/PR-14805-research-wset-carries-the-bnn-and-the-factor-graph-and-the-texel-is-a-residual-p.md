# PR #14805 -- research: WSet carries the BNN and the factor graph — and the texel is a residual program nobody generated

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 14805 |
| Title | research: WSet carries the BNN and the factor graph — and the texel is a residual program nobody generated |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-08-24T15:02:51Z |
| Merged at | 2026-08-24T15:25:00Z |
| Merge commit SHA | `fd1b181913f6c1c6d6523b0cb2e8404897771295` |
| Branch | `lumen-wset-futamura` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/14805 |
| Changed files | 8 |
| Additions / deletions | +1226 / -0 |

## Description

Aaron asked whether `WSet` can carry a BNN and a factor graph as a tensor, and whether the GPU-texture form stays a **specialisation of** the spec rather than a second implementation wearing a spec's name. Measured, not argued: **five probes, each with a sabotage control that had to go red before the result was allowed to count.** All five are committed and reproduce with `bun`.

## 1. Does `WSet` carry them? Yes — and the first correction is that "WSet" is two types

| type | file | weight constraint | ops |
|---|---|---|---|
| `WSet<'K,'W>` | `src/Core/WSet.fs` | `IStarRing` | `apply`, `tensor`, `consolidate`, `bornProb` |
| `WeightedSet<'K,'W>` | `src/Core/WeightedSet.fs` | `ISemiring`/`IRing` | `add`, `subtract`, `scale`, `inner`, **`ITensor`** |

`IMessage<'M>` = `{Uniform, Product, Divide}` is **exactly the additive commutative group** of `WeightedSet`. Not a resemblance — the shipped `FactorGraph.passOnce`, replicated operation-for-operation, runs over it:

```
C  max abs diff = 0.000e+0 over 5000 random 3-variable graphs x 4 BP rounds
C' sabotage control (non-identity uniform): 4.000e-3 — PROBE CAN FAIL
D  sum-product marginalisation == tensor -> mapKeys -> consolidate: 8.882e-16
D' sabotage control (marginalise wrong axis): 1.584e+0 — PROBE CAN FAIL
```

`'K` is the **sufficient-statistic index**, never the sample space. Five honest limits are stated in §1.3 (two carriers not one; the properness half-space; the type not carrying the interpretation; `WireWeight<float>` not existing so posteriors are local-only; `Sppf.fs` hardcoded to `float`).

## 2. Derivation or coincidence? A third thing, and it is the useful answer

```
A  ngFuse == WeightedSet.add over |K|=4 : max abs diff = 0.000e+0 over 20000 pairs
A' sabotage control (weight x1.0000001): 4.564e-5 — PROBE CAN FAIL
```

`ngFuse` **is** the residual program a first Futamura projection of `WeightedSet.add` against the static key set would emit — loop unrolled, lookups resolved to field offsets, Zero-prune branch statically dead. **And no specialiser produced it.** So the drift Aaron is worried about is not present; it is *unguarded* — the guarantee is a fact about today's bytes, not a property of a pipeline. Cheapest fix is not a specialiser: promote probe A to a test.

The arena doc §6 already cleared the `4 = RGBA = NG4` numerology on sufficiency + closure + operation-match, and I did not re-litigate it. This adds a fourth invariant it could not have had — **derivability** — while noting the counterweight: **RGBA's 4 does no work in the derivation.** `|'K|=4` comes from Normal-Gamma; the texel's 4 comes from a framebuffer. The structural claim is `ngFuse ≡ WeightedSet.add ∘ specialise`; the RGBA fit is *packing* riding on it.

## 3. The Futamura staging — two of three stages exist and are machine-checked

`src/Core/IsaSpec.fs` is the spec language; `src/Core/Residual.fs` already has the **residual-target knob** (`Code | Circuit`) with the law *"the residual target is a free choice of medium; the specialized program's semantics are invariant under it."* There are **zero** `.wgsl`/`.glsl`/`.frag`/`.comp` files in the tree, and every GPU row in the arena doc is `[proposed]`.

Said plainly, as asked: **the specialiser for the texture path is a human writing a codec by hand**, and a Futamura projection with a human in the middle is a design intent, not a projection. The gap is smaller than it sounds — the texture is a *third case of an existing knob*.

## 4. Transfer — measured, and it inverts the obvious intuition

```
operation                          law consumed          R    C    H      O      S
WeightedSet.add / subtract         Add comm group       hold hold hold   hold   hold
scale-CHAIN / inner                Mul ASSOCIATIVE      hold hold hold  1.1e0  3.8e0
order-free factor product          Mul COMMUTATIVE      hold hold 9.1e-1 1.8e0  3.0e0
Born / renormalise |w|             norm MULTIPLICATIVE  hold hold hold   hold  6.4e-1
```

The probe is **calibrated against the classical ladder** (ℂ loses order, ℍ commutativity, 𝕆 associativity, 𝕊 alternativity + zero divisors), so its verdicts on our ops are trustworthy. It independently reproduces `CayleyWeightedSet.Tests.fs` from the algebra side.

**The whole EP/BP message algebra transfers to every rung**, because it consumes only the additive group. Sum-product dies at ℍ/𝕆; Viterbi/tropical at ℂ. **The most transferable part of the soft regime is the part that looks least like tensor algebra.**

**Honest limit, stated before anyone builds:** algebraic survival is not Bayesian meaning. The ceiling is set by **normalisation** (Hurwitz — composition algebras end at 𝕆), not by associativity.

## 5. Second question (scoped): the evidence/observation gap is **not** where ℂ comes from

Answered because the falsifier was cheap and decisive.

```
E1 belief fold: cancellations to zero from nonzero inputs = 0 / 800000 weights
E2 belief fold: trials where support GREW after observing = 0
F  amplitude lane (C-ring): TWO paths gave LESS support than ONE = 50000 / 50000
```

`observe` is `Array.map2 (*)` over non-negative `int64` — **no path sum, so nothing can cancel.** Row F is the control and it is what makes row E1 mean anything.

Also settled: (1) our **phase is a total order**, QM's is an **angle in U(1)** — the shared word is a pun; (2) **`ZSet` retraction is NOT conjugation** — on ℝ conjugation is the *identity*, retraction flips the sign; being an involution identifies nothing; (3) the observable is a **ratio**, not a norm — the genuine Born rule lives only in the lane that already declared its ring to be ℂ. Competitors (signed measures — the closest, with a **ℤ/2** hidden part — split-complex, dual numbers, ℝ²) excluded by **Frobenius/Hurwitz**.

Aaron flagged this as *"I think"*; it is filed **refuted in the stated form**, with the surviving shape recorded as `toy`. On the adjacent metering claim the **weak/strong split is named in those words**: the mathematical-structure version is defensible; *"physical quantum effects occur"* is **not bought by metering** and must not be implied.

## Registers, and what is NOT claimed

Labelled throughout per `toy-is-free-metered-must-be-earned`. §3 is **unmetered**. §4 is metered as algebra, **unmetered as probability**. Explicitly not claimed: that a quaternionic BNN means anything statistically; that the texture path works (there is no shader); that `ngFuse` was *produced by* a specialiser; that "one substrate" implies composability; that any of this is byte-locked — `consolidateWSet` and `WSet.consolidate` **disagree on output order today** (two open workitems, cited).

## Verification

- All five probes exit 0 from their committed paths; probes A and D import the **shipped** functions rather than replicas.
- Both docs lint **clean** outside the `docs/research/2026-*` ignore glob, with a sabotage control that produced 4 errors — so the rc=0 is a check that ran.
- `lint-tsirelson-constant-caveat.ts` rc=0; the `1/(3√2)` **design-parameter-not-Tsirelson-bound** caveat is carried where cited.
- Two conjecture rows filed in §B (`B-doubling`, `B-evidence-observation`); nothing in §A touched.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_011YfBgr4pESuGMseMzU5FYJ

Agency-Signature-Version: 1
Agent: lumen
Agent-Runtime: claude-code
Agent-Model: claude-opus-5
Credential-Identity: AceHack
Credential-Mode: shared
Human-Review: not-implied-by-credential
Human-Review-Evidence: none
Action-Mode: human-directed
Task: none


## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 0 |
| Resolved threads | 0 |
| Unresolved threads | 0 |
| Total review comments | 0 |
| Total fix commits (touching thread paths) | 0 |

## Review threads

(no review threads on this PR)

## Fix commits (touching thread paths)

(no commits in this PR touched files anchored by review threads)
