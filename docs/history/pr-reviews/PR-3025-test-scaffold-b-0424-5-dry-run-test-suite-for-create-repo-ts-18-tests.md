# PR #3025 -- test(scaffold): B-0424.5 — dry-run test suite for create-repo.ts (18 tests)

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 3025 |
| Title | test(scaffold): B-0424.5 — dry-run test suite for create-repo.ts (18 tests) |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-05-13T20:07:06Z |
| Merged at | 2026-05-13T20:15:03Z |
| Merge commit SHA | `18d823c33b83212819d53c2c44dbb413c442c5f1` |
| Branch | `feat/B-0424.5-scaffold-dry-run-tests` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/3025 |
| Changed files | 2 |
| Additions / deletions | +305 / -0 |

## Description

## Summary

- Adds `tools/scaffold/create-repo.test.ts` — 18 subprocess-based tests for the B-0424 Stage 1 scaffold tool
- Tests verify the `--dry-run` output for both `forge` and `ace` before anyone runs `--apply` (the irreversible GitHub repo creation step)
- Closes the test gap between B-0424.4 (Scorecard workflow) and the eventual Stage 1 execution PR

## What the tests cover

| Scenario | Assertions |
|---|---|
| Both repos produce 12 planned operations | Step ID inventory, all status:"planned" |
| Step 01 repo settings | public, squash-merge, auto-merge, delete-branch; ace has no homepage |
| Step 02 branch protection | no force-push, linear history, empty required_status_checks (deadlock guard) |
| Step 04 CodeQL | default-setup (not advanced-only) — guards against code_scanning ruleset regression |
| Step 05 fork | targets AceHack org |
| Step 06 scaffold files | core governance files present (README, AGENTS, GOVERNANCE, SECURITY, LICENSE) |
| Step 07 manual steps | budget-cap + CodeQL verification items present |
| Error handling | unknown/missing --repo exits non-zero with usage message |

## Why subprocess-based tests

`create-repo.ts` calls `process.exit()` and runs logic at module load — both break import-time unit testing. Spawning as a subprocess mirrors the actual invocation path, requires zero refactoring, and is zero-network (no GitHub calls, no side effects).

## Checks

- `bun test tools/scaffold/create-repo.test.ts` → **18 pass, 0 fail**
- `dotnet build -c Release` → **0 warnings, 0 errors**

## Context

- B-0424 backlog: `docs/backlog/P1/B-0424-three-repo-split-stage1-create-forge-ace-with-scaffolding-aaron-2026-05-13.md`
- ADR: `docs/DECISIONS/2026-04-22-three-repo-split-zeta-forge-ace.md`
- Prior slices: B-0424.1 (#2994), .2 (#2996), .3 (#3003), .4 (#3019)

operative-authorization: aaron 2026-05-13: "Cooling period: TBD. The memory file IS the durable record"

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | true |
| Total threads | 3 |
| Resolved threads | 3 |
| Unresolved threads | 0 |
| Total review comments | 6 |
| Total fix commits (touching thread paths) | 2 |

## Review threads

### Thread 1 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6B4M3b`
- **Path**: `tools/scaffold/create-repo.test.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-13T20:09:17Z on `tools/scaffold/create-repo.test.ts`:95 (association: NONE)

  <pre>
  Computing `result` at `describe` evaluation time runs the subprocess during file load (before any test lifecycle hooks), which can make filtering/skipping tests surprising and can fail the suite before tests are even registered. Move the subprocess call into a `beforeAll` (or into each test) and store it in a variable so failures are reported as test failures with clearer attribution and so the suite only executes when it actually runs.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-13T20:12:28Z on `tools/scaffold/create-repo.test.ts`:95 (association: MEMBER)

    <pre>
    Fixed: moved `runDryRun("forge")` into a `beforeAll` callback so the subprocess runs in the test lifecycle rather than at describe-evaluation time. Subprocess failures now surface as test failures with attribution, and test filtering/skipping works as expected. Added `beforeAll` to the bun:test import.
    </pre>

### Thread 2 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6B4M3z`
- **Path**: `tools/scaffold/create-repo.test.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-13T20:09:17Z on `tools/scaffold/create-repo.test.ts`:243 (association: NONE)

  <pre>
  These assertions hard-code that the usage text is written to `stderr`. If the script prints usage to `stdout` (or if Bun changes buffering behavior), the tests will fail even though the CLI behavior is acceptable. Consider asserting against `(result.stderr + result.stdout)` (or checking either stream) while still requiring non-zero exit to keep the tests robust to output-stream differences.

  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-13T20:12:36Z on `tools/scaffold/create-repo.test.ts`:243 (association: MEMBER)

    <pre>
    Fixed: assertions now check `result.stderr + result.stdout` so they pass regardless of which stream Bun (or the script) uses for usage output. Non-zero exit is still asserted to keep the test substantive.
    </pre>

### Thread 3 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6B4M4C`
- **Path**: `tools/scaffold/create-repo.test.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-13T20:09:17Z on `tools/scaffold/create-repo.test.ts`:70 (association: NONE)

  <pre>
  `orgName` is being used as the repository name segment (`Forge` / `ace`) rather than an organization name (the org is always `Lucent-Financial-Group` here). Renaming the parameter to something like `repoName` (or `expectedRepoName`) would make the helper’s intent clearer and reduce confusion when reading failures.

  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-13T20:12:43Z on `tools/scaffold/create-repo.test.ts`:70 (association: MEMBER)

    <pre>
    Fixed: renamed `orgName` → `repoName` in `assertCommonInvariants`. The parameter receives values like `"Forge"` and `"ace"` (repo names), not org names — the new name matches what it actually holds.
    </pre>

## Fix commits (touching thread paths)

### `e767bbe9a9c9aefdf62a27158cf07816c83208a2` -- 2026-05-13T20:06:48Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/scaffold/create-repo.test.ts`

**Message:**

```
test(scaffold): B-0424.5 — dry-run test suite for create-repo.ts (18 …

…tests)

Adds tools/scaffold/create-repo.test.ts with 18 subprocess-based tests
that verify the dry-run output of create-repo.ts for both forge and ace
before anyone runs --apply (the irreversible GitHub repo creation step).

Tests cover:
- All 12 planned operations present for each repo
- Every operation has status:"planned" in dry-run mode
- Step 01: public+squash-merge+auto-merge settings; ace has no homepage
- Step 02: branch protection data (no force-push, linear history, empty
  required_status_checks to avoid deadlock before CI is wired)
- Step 04: CodeQL default-setup (not advanced-only; guards against the
  code_scanning ruleset rule regression)
- Step 05: fork targets AceHack org
- Step 06: scaffold file list includes core governance files
- Step 07: manual-step checklist includes budget-cap and CodeQL items
- Error handling: non-zero exit + usage message for unknown/missing --repo

Build gate: dotnet build -c Release → 0 warnings 0 errors.
Test gate:  bun test tools/scaffold/create-repo.test.ts → 18 pass 0 fail.

operative-authorization: aaron 2026-05-13: "Cooling period: TBD. The memory file IS the durable record"

Co-Authored-By: Claude <noreply@anthropic.com>
```

### `3131b96f44139e5f3609b7e2ba981e3c73125b8e` -- 2026-05-13T20:12:08Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/scaffold/create-repo.test.ts`

**Message:**

```
fix(scaffold): address Copilot review — beforeAll, repoName, stderr+s…

…tdout, MD032

- Move describe-level runDryRun calls into beforeAll so subprocess failures
  report as test failures with clear attribution (Copilot #3237144663)
- Rename `orgName` → `repoName` in assertCommonInvariants for clarity
  (Copilot #3237144697)
- Check `stderr + stdout` for Usage: to survive output-stream differences
  (Copilot #3237144717)
- Add blank line before list in tick shard to fix MD032 markdownlint error

All 18 tests still pass.

Co-Authored-By: Claude <noreply@anthropic.com>
```
