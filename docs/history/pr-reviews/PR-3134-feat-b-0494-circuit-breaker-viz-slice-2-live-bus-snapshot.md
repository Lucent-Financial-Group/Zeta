# PR #3134 -- feat(b-0494): circuit breaker viz — slice-2 live bus snapshot

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 3134 |
| Title | feat(b-0494): circuit breaker viz — slice-2 live bus snapshot |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-05-14T13:18:50Z |
| Merged at | 2026-05-14T13:34:18Z |
| Merge commit SHA | `fc996db1f4a4d1fd90334399b2bad0784ca66b1f` |
| Branch | `feat/b-0494-circuit-breaker-live-bus-snapshot` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/3134 |
| Changed files | 4 |
| Additions / deletions | +365 / -2 |

## Description

## Summary

- **`tools/bus/export-cb-snapshot.ts`** — new Bun script: reads non-expired `/tmp/zeta-bus/` envelopes, normalises surface-tagged sender IDs to identity level (`otto-cli` → `otto`), derives `CLOSED`/`HALF_OPEN`/`OPEN` state per agent from idle-heartbeat count, writes `demo/circuit-breaker-snapshot.json`
- **`demo/circuit-breaker-snapshot.json`** — committed snapshot from 73 live envelopes (Otto + Vera show active work; others healthy / no recent activity)
- **`demo/index.html`** — `renderCircuitBreakerTab()` is now `async`; tries `fetch('./circuit-breaker-snapshot.json', {cache:'no-cache'})` first, falls back to `buildCbMockData()` on error or missing file — no visible regression when snapshot absent
- **`docs/backlog/P1/B-0494-*.md`** — backlog row filed with pre-start checklist

## Acceptance criteria (B-0494)

- [x] `tools/bus/export-cb-snapshot.ts` exists and runs via `bun`
- [x] `demo/circuit-breaker-snapshot.json` committed (generated from live bus)
- [x] `renderCircuitBreakerTab()` tries snapshot first, falls back to mock
- [x] `dotnet build -c Release` → 0 warnings, 0 errors
- [x] `bun tsc --noEmit` clean

## How to refresh the snapshot

```bash
bun tools/bus/export-cb-snapshot.ts
# → Wrote 5 entries (73 envelopes) → demo/circuit-breaker-snapshot.json
```

Run, commit, push. Future slice: automate via CI or a scheduled script.

## Decomposition note (slice-3)

Next: wire a live relay that GitHub Pages visitors can hit to get fresh data without a commit cycle. Tracked as future decomposition of B-0494 / B-0401.

Closes B-0494 slice-2. Parent: B-0435, B-0401.

🤖 Generated with [Claude Code](https://claude.ai/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | true |
| Total threads | 5 |
| Resolved threads | 5 |
| Unresolved threads | 0 |
| Total review comments | 10 |
| Total fix commits (touching thread paths) | 3 |

## Review threads

### Thread 1 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6CEnoM`
- **Path**: `tools/bus/export-cb-snapshot.ts`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-14T13:21:11Z on `tools/bus/export-cb-snapshot.ts`:123 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Derive idle streak from consecutive heartbeats only**

  `consecutiveIdle` is computed as the total number of idle heartbeats in the window, not the current consecutive idle streak. That means an agent with interleaved statuses (e.g., `idle, working, idle, working, ...`) can be shown as `HALF_OPEN`/`OPEN` once total idle count crosses thresholds even though failures were not consecutive, which contradicts the `consecutiveFailures` semantics used by the UI and will produce false trip states. Compute the streak from newest heartbeat backward and stop at the first non-idle heartbeat.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-14T13:25:12Z on `tools/bus/export-cb-snapshot.ts`:123 (association: MEMBER)

    <pre>
    Fixed: replaced  (total count) with a newest-first walk that stops at the first non-idle envelope, giving the actual trailing consecutive streak. An agent with pattern idle→working→idle→idle now correctly reports 2, not 3.
    </pre>

### Thread 2 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6CEnql`
- **Path**: `tools/bus/export-cb-snapshot.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-14T13:21:13Z on `tools/bus/export-cb-snapshot.ts`:154 (association: NONE)

  <pre>
  The variable `consecutiveIdle` and the output field `consecutiveFailures` are misleading: the code simply counts all idle heartbeats in the non-expired window (`idleHeartbeats.length`) without verifying they are actually consecutive in time. If an agent emitted, say, 3 idle heartbeats, then a `working` heartbeat, then 2 more idle ones, this code would report `consecutiveFailures: 5` and trip to OPEN, even though the run was broken by a positive signal. Either compute the actual consecutive trailing run (walk `own` sorted newest-first and stop at the first non-idle), or rename to `idleHeartbeats` / `idleCount` and adjust the surrounding notes ("consecutive idle heartbeats exceeded threshold") to match the actual semantics. The same naming inconsistency also propagates into the committed snapshot and the UI contract.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-14T13:25:13Z on `tools/bus/export-cb-snapshot.ts`:154 (association: MEMBER)

    <pre>
    Fixed: the trailing-streak walk now drives both the computation and the output field. `consecutiveFailures` in the snapshot now reflects actual consecutive idle heartbeats (newest-first walk, stop at first non-idle), consistent with the field name and UI contract.
    </pre>

### Thread 3 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6CEnrI`
- **Path**: `tools/bus/export-cb-snapshot.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-14T13:21:14Z on `tools/bus/export-cb-snapshot.ts`:186 (association: NONE)

  <pre>
  This script runs top-level side effects (reads argv, reads envelopes, writes output, logs) unconditionally on import. The convention in `tools/**.ts` is to export a `main(...)` entrypoint and only execute when `import.meta.main` is true (e.g., `tools/backlog/generate-index.ts`, `tools/budget/daily-cost-report.ts`, `tools/archive/archive-pr-reviews.ts`). Wrapping the argv parsing + I/O in `async function main(...)` guarded by `if (import.meta.main)` keeps the module importable for tests and matches the rest of the toolchain.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-14T13:25:15Z on `tools/bus/export-cb-snapshot.ts`:186 (association: MEMBER)

    <pre>
    Fixed: argv parsing + all I/O moved into `async function main()` guarded by `if (import.meta.main)`. The module is now importable without side effects, matching `tools/backlog/generate-index.ts`, `tools/budget/daily-cost-report.ts`, etc.
    </pre>

### Thread 4 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6CEuWV`
- **Path**: `tools/bus/export-cb-snapshot.ts`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-14T13:27:22Z on `tools/bus/export-cb-snapshot.ts`:134 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Treat claim release events as non-work signals**

  `deriveEntry()` currently marks any `claim` envelope as a positive work signal, but claim payloads include both `action: "claim"` and `action: "release"` (`tools/bus/types.ts`). A recent release message will therefore keep an agent in `CLOSED`/"Active work detected" even after it has explicitly relinquished work, which makes the snapshot misreport breaker health for idle agents.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-14T13:32:06Z on `tools/bus/export-cb-snapshot.ts`:134 (association: MEMBER)

    <pre>
    Fixed: hasWorkSignal now checks e.payload.action === "claim" in addition to e.topic === "claim", so a release envelope is excluded. An agent relinquishing work is no longer treated as an active-work health signal. Also used discriminated-union narrowing (topic narrows the payload type) so no explicit cast is needed.
    </pre>

### Thread 5 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6CEuWb`
- **Path**: `tools/bus/export-cb-snapshot.ts`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-14T13:27:22Z on `tools/bus/export-cb-snapshot.ts`:63 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Fail fast when bus directory cannot be read**

  When `readdir(busDir)` throws (missing path, permissions, transient I/O), the script silently returns `[]` and then writes a "healthy/no recent activity" snapshot. This makes misconfiguration or runtime failures look like valid data, so operators can publish a stale/false circuit-breaker state without noticing. The export should surface the read error (or at least mark the snapshot as failed) instead of treating it as an empty bus.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-14T13:31:57Z on `tools/bus/export-cb-snapshot.ts`:63 (association: MEMBER)

    <pre>
    Fixed: removed the `try/catch` around `readdir` in `readEnvelopes` so the error propagates. `main()` now catches it and exits non-zero with a clear message (`Cannot read bus directory …: &lt;OS error&gt;`). Misconfiguration or permission problems are now visible to operators rather than silently producing a healthy empty-bus snapshot.
    </pre>

## Fix commits (touching thread paths)

### `9b94d25164d8d580ddcebbbd95b2e30f9387e80f` -- 2026-05-14T13:18:32Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/bus/export-cb-snapshot.ts`

**Message:**

```
feat(b-0494): circuit breaker viz — slice-2 live bus snapshot

- tools/bus/export-cb-snapshot.ts: reads non-expired /tmp/zeta-bus/
  envelopes, normalises surface-tagged sender IDs to identity level
  (otto-cli → otto), derives CLOSED/HALF_OPEN/OPEN state from idle
  heartbeat count, writes demo/circuit-breaker-snapshot.json
- demo/circuit-breaker-snapshot.json: committed snapshot from 73 live
  envelopes (Otto + Vera show active work; others healthy/no-activity)
- demo/index.html: renderCircuitBreakerTab() → async; tries fetching
  ./circuit-breaker-snapshot.json first (cache:no-cache), falls back
  to buildCbMockData() on error or absent file — no visible regression
  when snapshot not present
- docs/backlog/P1/B-0494-*.md: backlog row with pre-start checklist

dotnet build: 0 warnings, 0 errors ✓
bun tsc --noEmit: clean ✓
bun tools/bus/export-cb-snapshot.ts: 5 entries / 73 envelopes ✓

Co-Authored-By: Claude <noreply@anthropic.com>
```

### `66c422d1161a1aed20472414fac424a15f484bb2` -- 2026-05-14T13:24:00Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/bus/export-cb-snapshot.ts`

**Message:**

```
fix(b-0494): consecutive idle streak + import.meta.main guard

Three reviewer findings addressed:

1. Consecutive idle computation was counting all idle heartbeats in the
   non-expired window (idleHeartbeats.length). An interleaved pattern
   like idle→working→idle→idle would report 3 "consecutive" failures
   instead of the correct trailing streak of 2.  Fixed by walking `own`
   (already sorted newest-first) and stopping at the first non-idle
   envelope.

2. The `consecutiveFailures` output field now reflects the actual
   trailing streak, consistent with the field name and the UI contract.

3. argv parsing + I/O were running as unconditional top-level side
   effects. Wrapped in `async function main()` guarded by
   `if (import.meta.main)` to match the rest of the toolchain and keep
   the module importable for tests.

Co-Authored-By: Claude <noreply@anthropic.com>
```

### `7c37a37ee1e38de9047dba821bba96df34c498af` -- 2026-05-14T13:31:08Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/bus/export-cb-snapshot.ts`

**Message:**

```
fix(b-0494): claim-release not a work signal; fail-loud on unreadable…

… bus dir

- `hasWorkSignal` now filters claim envelopes to `action === "claim"` only;
  a release envelope no longer keeps an agent in CLOSED/"Active work detected"
  after it relinquishes work.
- `readEnvelopes` lets `readdir` throw instead of silently returning []; main()
  catches and exits non-zero so misconfiguration is visible rather than
  masquerading as a healthy empty-bus snapshot.
- Replaced unsafe `(e.payload as { … })` casts with discriminated-union
  narrowing (topic already narrows payload type).

Co-Authored-By: Claude <noreply@anthropic.com>
```
