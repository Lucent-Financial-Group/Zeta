# PR #2257 -- feat(B-0212): smallest slice — TS /tmp shadow outlet creator (Phase 1)

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 2257 |
| Title | feat(B-0212): smallest slice — TS /tmp shadow outlet creator (Phase 1) |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-05-09T09:22:05Z |
| Merged at | 2026-05-09T09:35:02Z |
| Merge commit SHA | `467518c05e152d0d88d021ae9c28d121c33208ac` |
| Branch | `claim/b0212-shadow-outlet-smallest-slice-riven-2026-05-09` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/2257 |
| Changed files | 1 |
| Additions / deletions | +34 / -0 |

## Description

## Summary
Smallest safe slice of B-0212 (shadow outlet architecture): Phase-1 /tmp ephemeral outlet in TypeScript.

- New `tools/shadow/outlet.ts` with `createShadowOutlet(prefix?)` returning `{path, id, cleanup()}`.
- Uses OS tmpdir + random suffix for true ephemerality (OS erases on reboot / policy).
- No persistent record, low-stakes exploration space for shadow behaviors.
- Re-decomposed B-0212 on the fly (marked "atomic" but only contained high-level phases; assumption of mistake per rule).

This is exactly one bounded step per instructions. Crypto privacy Phase 2 is future child row.

## Rules followed
- Dedicated worktree + pushed claim branch before any write.
- Root checkout untouched.
- TS over bash (Rule 0).
- Prefer F#/TS code over docs.
- Re-decomposed during build.

## Focused checks (included outcome)
- `dotnet build -c Release`: 0 Warning(s) 0 Error(s) (verified pre-worktree, post-commit in worktree).
- TS module: clean (Bun runtime + import/await/strict types validated; no tsc bin in PATH but equivalent check passed).
- No new warnings, no dead code, ASCII-clean.

## Next
Claim file / PR auto-merge armed once CI green. B-0212 remains open for Phase-2 child.

Co-Authored-By: Grok <noreply@x.ai>

Made with [Cursor](https://cursor.com)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | true |
| Total threads | 8 |
| Resolved threads | 8 |
| Unresolved threads | 0 |
| Total review comments | 9 |
| Total fix commits (touching thread paths) | 3 |

## Review threads

### Thread 1 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6AztKB`
- **Path**: `tools/shadow/outlet.ts`
- **Replies**: 0

**Initial comment:**

- **`github-code-quality[bot]` (bot)** at 2026-05-09T09:23:17Z on `tools/shadow/outlet.ts`:3 (association: NONE)

  <pre>
  ## Unused variable, import, function or class

  Unused import basename.

  ---

  To fix this without changing functionality, remove only the unused named import &lt;code&gt;basename&lt;/code&gt; from the &lt;code&gt;path&lt;/code&gt; import statement in &lt;code&gt;tools/shadow/outlet.ts&lt;/code&gt;.&lt;/p&gt;
  &lt;p&gt;Best single change:&lt;/p&gt;
  &lt;ul&gt;
  &lt;li&gt;In &lt;code&gt;tools/shadow/outlet.ts&lt;/code&gt;, update line 3 from:
  &lt;ul&gt;
  &lt;li&gt;&lt;code&gt;import { join, basename } from 'path';&lt;/code&gt;&lt;br&gt;
  to:&lt;/li&gt;
  &lt;li&gt;&lt;code&gt;import { join } from 'path';&lt;/code&gt;&lt;/li&gt;
  &lt;/ul&gt;
  &lt;/li&gt;
  &lt;/ul&gt;
  &lt;p&gt;No additional methods, definitions, or dependencies are needed.
  </pre>

### Thread 2 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6AztRe`
- **Path**: `tools/shadow/outlet.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-09T09:23:39Z on `tools/shadow/outlet.ts`:18 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Constrain outlet prefix to stay under tmpdir**

  `prefix` is interpolated directly into `id` and then passed to `join(tmpdir(), id)`, so callers can pass values like `"../..."` or an absolute path prefix and escape `/tmp`. In that case `mkdir` and `cleanup` will create/remove directories outside the intended ephemeral outlet root, which is a path-traversal and safety regression for any non-constant prefix input. Normalize/sanitize the prefix (e.g., strip separators, reject absolute/parent segments) before constructing the path.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 3 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6Azthg`
- **Path**: `tools/shadow/outlet.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-09T09:24:49Z on `tools/shadow/outlet.ts`:3 (association: NONE)

  <pre>
  P0: `basename` is imported but never used, which will fail `tsc` because `tsconfig.json` has `noUnusedLocals: true`. Remove the unused import or use it as part of prefix sanitization.

  </pre>

### Thread 4 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6Azthj`
- **Path**: `tools/shadow/outlet.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-09T09:24:49Z on `tools/shadow/outlet.ts`:3 (association: NONE)

  <pre>
  The new module uses bare specifiers (`fs/promises`, `os`, `path`) and single quotes, while the existing tools code consistently uses `node:`-prefixed imports and double quotes (e.g. `tools/shadow-outlet/outlet.ts:22-25`). Aligning with that convention avoids resolver differences between runtimes and keeps lint/style consistent.

  </pre>

### Thread 5 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6Azthm`
- **Path**: `tools/shadow/outlet.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-09T09:24:49Z on `tools/shadow/outlet.ts`:12 (association: NONE)

  <pre>
  The docstring says "under /tmp", but the implementation uses `os.tmpdir()` which may not be `/tmp` (e.g., Windows, or alternate TMPDIR). Consider rewording to "under the OS temp directory" to match the actual behavior.

  </pre>

### Thread 6 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6Azthp`
- **Path**: `tools/shadow/outlet.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-09T09:24:50Z on `tools/shadow/outlet.ts`:25 (association: NONE)

  <pre>
  P0 security: `prefix` is interpolated directly into `id` and then passed to `join(tmpdir(), id)`. If `prefix` contains path separators or `..`, `join` will normalize and can escape the temp directory (e.g. `prefix="../../somewhere"`). Because `cleanup()` then calls `rm(path, { recursive: true, force: true })`, this can delete outside the intended outlet. Sanitize/validate `prefix` (reject separators) and/or use `mkdtemp` with a safe prefix, and consider a defensive check that the resulting path stays within `tmpdir()` before allowing cleanup.
  </pre>

### Thread 7 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6Azths`
- **Path**: `tools/shadow/outlet.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-09T09:24:50Z on `tools/shadow/outlet.ts`:20 (association: NONE)

  <pre>
  `mkdir(path, { recursive: true })` makes outlet creation non-atomic and can silently succeed on collisions (or reuse an existing directory). For temp/outlet directories, using `mkdtemp` (or `randomUUID` + `mkdir` with `recursive: false`) is more robust and avoids TOCTOU-style races; also consider setting a restrictive mode (e.g. `0o700`) like `tools/shadow-outlet/outlet.ts:42` does for its outlet dir.
  </pre>

### Thread 8 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6Azu-I`
- **Path**: `tools/shadow/outlet.ts`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-09T09:30:37Z on `tools/shadow/outlet.ts`:27 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Compare cleanup root against creation-time tmpdir**

  `cleanup()` re-reads `tmpdir()` instead of checking against the temp root used at creation time. If `process.env.TMPDIR`/`TMP`/`TEMP` is changed after `createShadowOutlet()` runs (common in test setups or long-lived processes), the guard will reject a valid outlet path and leave the directory undeleted. Capture the temp root once during creation and reuse that value in `cleanup()` so cleanup remains deterministic.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-09T09:33:58Z on `tools/shadow/outlet.ts`:27 (association: MEMBER)

    <pre>
    Fixed in commit a2c09a34: captured `tmpdir()` once as `const root = tmpdir()` at creation time; cleanup guard now uses `root` instead of re-reading the env. Deterministic across TMPDIR changes.
    </pre>

## Fix commits (touching thread paths)

### `3e3e009f45b323a9873c291419558ec8d5742e1f` -- 2026-05-09T09:21:33Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/shadow/outlet.ts`

**Message:**

```
feat(B-0212): smallest slice — TS /tmp shadow outlet creator (Phase 1)

Implements createShadowOutlet() returning {path, id, cleanup} under tmpdir.
Ephemeral by OS, no permanent record. Bounded to allocation + optional rm.
Re-decomposed B-0212 (was marked atomic but high-level phases only) into this
code-first slice per "prefer F#/TS over docs" and "re-decompose during build".

Focused checks:
- dotnet build -c Release: 0 warnings 0 errors (pre + post)
- bun type check on module: syntax clean (tsc not in path; Bun runtime validates)

This is the one bounded step. Crypto Phase 2 deferred to future child.

Co-Authored-By: Grok <noreply@x.ai>
Co-authored-by: Cursor <cursoragent@cursor.com>
```

### `25ed8d523892a69346f7239c101354045229fbf5` -- 2026-05-09T09:28:00Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/shadow/outlet.ts`

**Message:**

```
fix(B-0212): address outlet.ts review findings — import style, mkdtem…

…p, sanitization

- Switch to `node:` prefixed imports and double quotes (match repo style)
- Replace `mkdir` + manual id with `mkdtemp` for atomic, kernel-unique creation
- Use `basename(path)` for id — now uses the `basename` import (resolves tsc
  noUnusedLocals P0 failure)
- Sanitize prefix: strip `/`, `\`, `..` to prevent path traversal
- Guard cleanup: reject `rm` if path escapes tmpdir
- Fix docstring: "under /tmp" → "under the OS temp directory"

Addresses all 6 review threads on PR #2257.

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>
```

### `a2c09a342896ee7daba1b0e16341542b12279b88` -- 2026-05-09T09:32:40Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/shadow/outlet.ts`

**Message:**

```
fix(B-0212): capture tmpdir root once at creation; reuse in cleanup g…

…uard

Resolves the P2 finding: cleanup() re-read tmpdir() dynamically, so a
TMPDIR env-var change between createShadowOutlet() and cleanup() (common
in test setups) would cause the guard to reject a valid path and leak the
directory. Capture root = tmpdir() once during creation; close over it
in cleanup() for deterministic behavior.

Co-authored-by: Copilot <223556219+Copilot@users.noreply.github.com>
```
