# PR #6119 -- research(081KRW63S0008QG0R000QJR08H #3): Adinkra [8,4,4] code-based-crypto constructive proof prototype (code → ECC → key)

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 6119 |
| Title | research(081KRW63S0008QG0R000QJR08H #3): Adinkra [8,4,4] code-based-crypto constructive proof prototype (code → ECC → key) |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-05-30T14:26:55Z |
| Merged at | 2026-05-30T14:42:16Z |
| Merge commit SHA | `8bfbc2c856a3b26b3a37ba0910e7249ad85baa41` |
| Branch | `otto/adinkra-encryption-primer-b0623-2026-05-30` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/6119 |
| Changed files | 2 |
| Additions / deletions | +188 / -0 |

## Description

## What

081KRW63S0008QG0R000QJR08H acceptance #3 — the **constructive proof prototype** for code-based (Adinkra) crypto. Toy scale, test-guarded (6 tests / 283 assertions, passing locally).

`tools/research/adinkra-ecc/adinkra-ecc-prototype.ts` constructs the **[8,4,4] extended Hamming code** (the unique doubly-even self-dual code of length 8 — the smallest interesting Adinkra code per the existing 2026-05-21 primer), verifies:
- **doubly-even** — all 16 codeword weights ≡ 0 mod 4
- **self-dual** — self-orthogonal under mod-2 inner product + dimension n/2

and derives a **deterministic key-seed** (SHA-256 of canonical code material). The dual-use claim made concrete: *one structure yields both the ECC (protect-from-errors) and key material (protect-from-being-seen)*.

## Grounding (this session)

verify-existing-substrate confirmed the primer (acceptance #1+#2) already exists — so this adds only the open slice (#3, the code), no duplicate. The broader crypto substrate this composes with:
- **081KSNY2Z0008QG0R001A431CN** — encryption thermal-cost / two-axis: Adinkra = the thermal/physical axis
- **081KSNY2Z0008QG0R002ZAVMEK** — multi-cipher PQ, hedge against NIST monoculture: Adinkra is **code-based** PQC, a *different family* from NIST lattices ⇒ a hedge family (Aaron: "don't lock to nist lattices, they are sus")
- **081KSNY2Z0008QG0R002JKH50A** — minimal/lattice gitcrypt (Noble / X-Wing / ML-DSA-65)

## Substrate-honest

Proof-OF-CONSTRUCTION, not a security claim. Production KDF needs blake3 (Lucent-KSK); the open security questions (indistinguishability / forward-secrecy / non-compromise of the ECC) remain 081KRW63S0008QG0R000QJR08H #3 research scope. Research code; reversible.

🤖 Generated with [Claude Code](https://claude.com/claude-code)


## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | true |
| Total threads | 5 |
| Resolved threads | 5 |
| Unresolved threads | 0 |
| Total review comments | 10 |
| Total fix commits (touching thread paths) | 2 |

## Review threads

### Thread 1 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F4FxR`
- **Path**: `tools/research/adinkra-ecc/adinkra-ecc-prototype.test.ts`
- **Replies**: 1

**Initial comment:**

- **`github-code-quality[bot]` (bot)** at 2026-05-30T14:28:31Z on `tools/research/adinkra-ecc/adinkra-ecc-prototype.test.ts`:5 (association: NONE)

  <pre>
  ## Unused variable, import, function or class

  Unused import canonicalCodeMaterial.

  ---

  &lt;p&gt;The best fix is to remove &lt;code&gt;canonicalCodeMaterial&lt;/code&gt; from the named import list in &lt;code&gt;tools/research/adinkra-ecc/adinkra-ecc-prototype.test.ts&lt;/code&gt;.&lt;/p&gt;
  &lt;ul&gt;
  &lt;li&gt;General approach: delete unused imports rather than adding artificial usage.&lt;/li&gt;
  &lt;li&gt;Best specific fix here: edit only the import block at the top of the file, removing &lt;code&gt;canonicalCodeMaterial&lt;/code&gt; and leaving all actually used imports unchanged.&lt;/li&gt;
  &lt;li&gt;No methods, definitions, or new imports are needed.&lt;/li&gt;
  &lt;/ul&gt;
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-30T14:39:29Z on `tools/research/adinkra-ecc/adinkra-ecc-prototype.test.ts`:5 (association: MEMBER)

    <pre>
    Fixed in 4fd0f2351 — removed `canonicalCodeMaterial` from the test import list (imported but never used). `tsc --noEmit -p tsconfig.json` now reports 0 errors (was TS6133 here).
    </pre>

### Thread 2 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F4GHb`
- **Path**: `tools/research/adinkra-ecc/adinkra-ecc-prototype.test.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-30T14:30:19Z on `tools/research/adinkra-ecc/adinkra-ecc-prototype.test.ts`:5 (association: NONE)

  <pre>
  P0: `canonicalCodeMaterial` is imported but never used. With `noUnusedLocals` enabled in tsconfig, this should fail TypeScript compilation for the test suite.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-30T14:39:30Z on `tools/research/adinkra-ecc/adinkra-ecc-prototype.test.ts`:5 (association: MEMBER)

    <pre>
    Confirmed real and fixed in 4fd0f2351 — `tsc` emitted `TS6133` here, which was the actual `lint (tsc tools)` gate failure. Dropped `canonicalCodeMaterial` from the import list; gate now green (0 errors, 6 tests / 283 assertions pass).
    </pre>

### Thread 3 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F4GHd`
- **Path**: `tools/research/adinkra-ecc/adinkra-ecc-prototype.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-30T14:30:19Z on `tools/research/adinkra-ecc/adinkra-ecc-prototype.ts`:112 (association: NONE)

  <pre>
  P0: This repo’s TS config uses `lib: ["esnext"]` (no DOM), and other code avoids the bare `crypto` identifier for that reason. Using `crypto.subtle` here is likely to be a type error (and is inconsistent with the established `globalThis.crypto` pattern).
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-30T14:39:31Z on `tools/research/adinkra-ecc/adinkra-ecc-prototype.ts`:112 (association: MEMBER)

    <pre>
    Verified FALSE POSITIVE on the type-error prediction: `tsc --noEmit -p tsconfig.json` reported 0 errors on this line — `@types/bun` types the `crypto` global (incl. `.subtle`) even under `lib:["esnext"]`, and `globalThis.crypto` appears nowhere in the repo (grep: 0 hits). Addressed on the merits anyway in 4fd0f2351 by switching to the repo-uniform `node:crypto` `createHash` pattern (8+ files use it), making `deriveKeySeed`/`adinkraEccProof` synchronous. SHA-256 hex is byte-identical, so the determinism + 64-char-length tests still pass.
    </pre>

### Thread 4 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F4GHg`
- **Path**: `tools/research/adinkra-ecc/adinkra-ecc-prototype.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-30T14:30:19Z on `tools/research/adinkra-ecc/adinkra-ecc-prototype.ts`:96 (association: NONE)

  <pre>
  P1: `enumerateCodewords` claims to enumerate the full code, but if the generator rows are not linearly independent it will emit duplicates (and downstream checks like `isSelfDual` can then produce false positives because they use `codewords.length`). Consider de-duplicating or failing fast on duplicates.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-30T14:39:33Z on `tools/research/adinkra-ecc/adinkra-ecc-prototype.ts`:96 (association: MEMBER)

    <pre>
    Fixed in 4fd0f2351 — `enumerateCodewords` now de-duplicates via a Set, so it honestly enumerates the code as a SET and `isSelfDual`'s `|C| = 2^(n/2)` dimension check cannot be inflated by a linearly-dependent generator set. For the [8,4,4] case the I_4 block guarantees independence ⇒ still exactly 16 distinct codewords (test unchanged). Also guards the row index (cleared an adjacent TS2345).
    </pre>

### Thread 5 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F4GHh`
- **Path**: `tools/research/adinkra-ecc/adinkra-ecc-prototype.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-30T14:30:20Z on `tools/research/adinkra-ecc/adinkra-ecc-prototype.ts`:52 (association: NONE)

  <pre>
  P2: `xor8` returns an `Array&lt;number&gt;` and then force-casts it to `Bits8` via `as unknown as Bits8`. That hides type/length issues and makes later refactors riskier. Prefer constructing the 8-tuple explicitly so the return type is honest without an unsafe cast.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-30T14:39:34Z on `tools/research/adinkra-ecc/adinkra-ecc-prototype.ts`:52 (association: MEMBER)

    <pre>
    Fixed in 4fd0f2351 — `xor8` now constructs the 8-tuple explicitly instead of `as unknown as Bits8`. Literal tuple indices are exact `number` (not `number | undefined`), so the return type is honest without the cast — this also cleared the TS2532 the cast was masking.
    </pre>

## Fix commits (touching thread paths)

### `e51c99d2ae7cf5f058ef4a743aeaff5dd8f5e5f7` -- 2026-05-30T14:26:43Z -- `claude`

**Touched files (intersect with thread paths):**

- `tools/research/adinkra-ecc/adinkra-ecc-prototype.test.ts`
- `tools/research/adinkra-ecc/adinkra-ecc-prototype.ts`

**Message:**

```
research(081KRW63S0008QG0R000QJR08H #3): Adinkra [8,4,4] code-based-crypto constructive p…

…roof prototype (code → ECC → key)

Closes 081KRW63S0008QG0R000QJR08H acceptance #3 ('one constructive proof path: small Adinkra → ECC code
→ cryptographic primitive') at toy scale, test-guarded (6 tests / 283 assertions).

Constructs the [8,4,4] extended Hamming code (the unique doubly-even self-dual code
of length 8 = the smallest interesting Adinkra code per the 2026-05-21 primer),
VERIFIES the two load-bearing properties — doubly-even (all codeword weights ≡ 0
mod 4) + self-dual (self-orthogonal + dim n/2) — and derives a deterministic
key-seed (SHA-256 of canonical code material). The dual-use claim made concrete:
ONE structure yields both the ECC (protect-from-errors) and key material
(protect-from-being-seen).

Composes with the broader crypto substrate grounded this session:
- 081KSNY2Z0008QG0R001A431CN (encryption thermal-cost / two-axis) — Adinkra = the thermal/physical axis
- 081KSNY2Z0008QG0R002ZAVMEK (multi-cipher PQ, hedge against NIST monoculture) — Adinkra is CODE-BASED
  PQC, a DIFFERENT family from NIST lattices ⇒ a hedge family per the don't-lock
  constraint (Aaron: 'don't want to lock to nist lattices, they are sus')
- 081KSNY2Z0008QG0R002JKH50A (minimal/lattice gitcrypt, Noble/X-Wing/ML-DSA-65) — the lattice family

Substrate-honest (per the primer razor note): this is the proof-OF-CONSTRUCTION,
not a security claim. A production KDF needs blake3 (Lucent-KSK) + the open security
questions (indistinguishability / forward-secrecy / non-compromise of the ECC)
remain 081KRW63S0008QG0R000QJR08H #3's research scope.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `4fd0f2351a978087826aa0d129a820ecdf6460db` -- 2026-05-30T14:38:00Z -- `claude`

**Touched files (intersect with thread paths):**

- `tools/research/adinkra-ecc/adinkra-ecc-prototype.test.ts`
- `tools/research/adinkra-ecc/adinkra-ecc-prototype.ts`

**Message:**

```
fix(081KRW63S0008QG0R000QJR08H #3): resolve PR #6119 review threads — tsc-tools gate now …

…green (0 errors)

Resolves the 5 review threads on PR #6119; `tsc --noEmit -p tsconfig.json` goes
from 4 errors → 0 (the `lint (tsc tools)` gate failure was real), 6 tests still
pass (283 assertions).

- Unused import (CodeQL + Copilot P0; TS6133): drop `canonicalCodeMaterial` from
  the test import list — it was imported but never used (`noUnusedLocals`).
- `xor8` unsafe cast (Copilot P2; TS2532): construct the 8-tuple explicitly
  instead of `as unknown as Bits8`; literal tuple indices are exact `number`,
  so the return type is honest without a cast.
- `dotMod2` (TS2532, not reviewer-flagged but real under noUncheckedIndexedAccess):
  same literal-index treatment for the GF(2) inner product.
- `enumerateCodewords` duplicates (Copilot P1; TS2345): guard the row index and
  de-duplicate via a Set, so the function honestly enumerates the code as a SET
  and `isSelfDual`'s `|C| = 2^(n/2)` dimension check can't be inflated by a
  dependent generator set. The [8,4,4] case (I_4 block ⇒ independent rows) still
  yields exactly 16 distinct codewords; the test is unchanged.
- `crypto.subtle` (Copilot P0): the predicted type error is a VERIFIED FALSE
  POSITIVE — `tsc` reported 0 errors on the line, `@types/bun` types the `crypto`
  global incl. `.subtle` under `lib:["esnext"]`, and the recommended
  `globalThis.crypto` pattern does not exist anywhere in the repo (grep: 0 hits).
  Addressed on the MERITS anyway by switching to the repo-uniform `node:crypto`
  `createHash` pattern (8+ files use it), which also makes `deriveKeySeed` /
  `adinkraEccProof` synchronous. SHA-256 hex output is byte-identical, so
  determinism + 64-char-length tests hold.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```
