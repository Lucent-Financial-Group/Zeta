# PR #6481 -- feat(algebra): Z-set C# generic-math — IWSAM abelian-group (IAdditiveIdentity + IAddition/ISubtraction/IUnaryNegation)

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 6481 |
| Title | feat(algebra): Z-set C# generic-math — IWSAM abelian-group (IAdditiveIdentity + IAddition/ISubtraction/IUnaryNegation) |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-06-01T21:54:28Z |
| Merged at | 2026-06-01T21:57:20Z |
| Merge commit SHA | `4ea2a64473bcfcc93733abf83065165451710593` |
| Branch | `otto-cli/zset-csharp-generic-math-2026-06-01` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/6481 |
| Changed files | 2 |
| Additions / deletions | +150 / -1 |

## Description

The **dotnet-native** form of "numerics like dotnet as our interface" (Aaron 2026-06-01) — the C# Z-set rung after F# (#6480). C# Z-set IS-A `IAdditiveIdentity` + `IAdditionOperators` (monoid) **plus** `ISubtractionOperators` + `IUnaryNegationOperators` (the abelian-group inverse). Same shape as the G-Set (#6468) / Bag (#6473) IWSAM twins. **NOT `INumber`** — no total order; the ring scalar is per-element, not a numeric product.

### What

- `operator +` / binary `operator -` short-circuit empty as a **comparer-agnostic identity** *before* `Union`'s `RequireSameComparer` guard — so `a + Zero = a` / `Zero + a = a` / `a - Zero = a` hold for Z-sets built with *any* comparer (the G-Set #6468 wrinkle: `Union` runs `RequireSameComparer` before its own empty short-circuit). Two **non-empty** operands with mismatched comparers still fail fast.
- unary `operator -` → `Negate`; `operator +` / binary `-` → `Union` / `Union(Negate)`. **Hot path unchanged.**
- `AdditiveIdentity` cached + `[SuppressMessage CA1000]` (IWSAM requires the static member on the generic type; CA1000 predates static abstract interface members).

### Tests (+3, **18/18** ZSet pass; Core builds 0 warn / 0 err)

- `(+)` == `Union`, `(-a)` == `Negate`, `(a - b)` == `Union(Negate)`
- identity + commutative + associative
- abelian-group inverse: `a + (-a) = empty`, `a - a = empty`
- `(+)` **NOT** idempotent (`a + a` doubles); subtraction retracts a shared key to 0
- comparer-agnostic identity, but non-empty mismatch still throws (`+` and `-`)

### Ladder
F# #6480 ✅ · **C# (this)** · Rust (`impl Sub`/`Neg`) next · TS (`AbelianGroup` record) next.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 1 |
| Resolved threads | 0 |
| Unresolved threads | 1 |
| Total review comments | 1 |
| Total fix commits (touching thread paths) | 1 |

## Review threads

### Thread 1 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM6GQpga`
- **Path**: `src/Core.CSharp/ZSet.cs`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-01T21:57:31Z on `src/Core.CSharp/ZSet.cs`:314 (association: NONE)

  <pre>
  This introduces direct name attribution ("Aaron") in a current-state code comment. Repo convention is to avoid contributor/persona names in code and use role references instead (see `docs/AGENT-BEST-PRACTICES.md` Operational standing rules, "No name attribution in code, docs, or skills"). Please rewrite this comment without a personal name (e.g., drop the parenthetical attribution or replace it with a role-ref like “the human maintainer”).
  </pre>

## Fix commits (touching thread paths)

### `4b3db344c1304c8bae58578a36623ed20fc3ed26` -- 2026-06-01T21:54:18Z -- ``

**Touched files (intersect with thread paths):**

- `src/Core.CSharp/ZSet.cs`

**Message:**

```
feat(algebra): Z-set C# generic-math — IWSAM abelian-group (IAdditive…

…Identity + IAddition/ISubtraction/IUnaryNegation)

The dotnet-native form of "numerics like dotnet as our interface" (Aaron
2026-06-01): the C# Z-set IS-A `IAdditiveIdentity` + `IAdditionOperators`
(monoid) PLUS `ISubtractionOperators` + `IUnaryNegationOperators` (the abelian-
group inverse). Matches the F# Zero/(+)/(~-)/(-) rung (#6480) and the G-Set
(#6468) / Bag (#6473) IWSAM twins. NOT INumber — no total order; the ring
scalar is per-element, not a numeric product.

- `operator +` / `operator -` (binary) short-circuit empty as a COMPARER-
  AGNOSTIC identity BEFORE Union's RequireSameComparer guard, so a + Zero = a /
  Zero + a = a / a - Zero = a hold for any comparer (the G-Set #6468 wrinkle:
  Union runs RequireSameComparer before its own empty short-circuit). Two
  NON-empty operands with mismatched comparers still fail fast.
- `operator -` (unary) delegates to Negate; `operator +`/binary `-` delegate to
  Union / Union(Negate). Hot path unchanged.
- `AdditiveIdentity` cached + `[SuppressMessage CA1000]` (IWSAM requires the
  static member on the generic type; CA1000 predates static abstract members).

Tests (+3, 18/18 ZSet pass; Core builds 0 warn / 0 err): (+) == Union, (-a) ==
Negate, (a-b) == Union(Negate); identity + commutative + associative;
a + (-a) = empty and a - a = empty; (+) NOT idempotent (a + a doubles);
subtraction retracts to-0; comparer-agnostic identity but non-empty mismatch
throws.

Next rungs: Rust (impl Sub/Neg) · TS (AbelianGroup record).

Co-Authored-By: Claude <noreply@anthropic.com>
```
