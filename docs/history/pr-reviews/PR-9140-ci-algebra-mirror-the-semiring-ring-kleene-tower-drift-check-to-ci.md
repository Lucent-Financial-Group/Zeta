# PR #9140 -- ci(algebra): mirror the semiring→ring→kleene tower drift-check to CI

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 9140 |
| Title | ci(algebra): mirror the semiring→ring→kleene tower drift-check to CI |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-07-02T14:52:32Z |
| Merged at | 2026-07-02T14:52:42Z |
| Merge commit SHA | `de9ef83071ea23e43550f24cf0bb094feb913cc2` |
| Branch | `otto/kleene-tower-drift-check-ci` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/9140 |
| Changed files | 4 |
| Additions / deletions | +140 / -0 |

## Description

Two gaps closed so the algebra-tower drift-check covers the full **semiring→ring→kleene** tower **and runs on every PR**.

## 1. Complete the tower (kleene was missing)
The `codegen-law-drift` check covered semiring/ring/star-ring but not kleene. Adds:
- **`src/Core.TypeScript/algebra/kleene.ts`** — a TS Kleene oracle instance: tropical `(min,+)`. Crucially its `add` is **idempotent** (`min(a,a)=a`), so `add-idempotent` genuinely holds — unlike the `numberSemiring` used for the ring tier, where it would fail.
- `IKleeneAlgebra` registered in `codegen-law-drift.test.ts` + the checked-in `generated-kleene-laws.test.ts`. Generated laws pass against the tropical instance (semiring axioms + `add-idempotent`; star/closure laws render as documented comments — no scalar encoding).

## 2. Wire the drift-check into CI (it was local-only)
Adds a step to `gate.yml`'s cross-verify job running the drift-check + all four generated law-property files (semiring, ring, star-ring, kleene). **Pure bun/arithmetic** — no go/python spawns, so the cross-language law cross-verify stays a separate lane (no new toolchain dependency in this job).

## Verified
- `26 tests / 0 fail` on the exact CI command locally;
- `gate.yml` valid YAML.

Anchors: Lehmann 1977; Kozen 1994; Vandiver 1934 / Golan 1999 (idempotent semirings are zerosumfree — why kleene needs its own idempotent oracle, not the ring's).

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 1 |
| Resolved threads | 0 |
| Unresolved threads | 1 |
| Total review comments | 1 |
| Total fix commits (touching thread paths) | 1 |

## Review threads

### Thread 1 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM6N7_dT`
- **Path**: `src/Core.TypeScript/algebra/kleene.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-07-02T14:56:46Z on `src/Core.TypeScript/algebra/kleene.ts`:28 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Close the Kleene carrier over negative cycles**

  For negative input this returns `-Infinity`, but this same instance defines `zero` as `+Infinity` and `mul` as plain JS `a + b`; any matrix-star update that combines an unreachable path with a negative-cycle closure, e.g. `mul(r.zero, r.star(-1))`, becomes `NaN`, and `Math.min` then propagates `NaN`. Since the added generated star/closure cases currently contain no assertions, CI still passes while the advertised tropical Kleene oracle is not closed for negative-cycle cases; handle the bottom sentinel explicitly in `mul` or choose a closed carrier representation.

  Useful? React with 👍 / 👎.
  </pre>

## Fix commits (touching thread paths)

### `d05069a28614ae90ff7398cad535c800455f833f` -- 2026-07-02T14:52:29Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `src/Core.TypeScript/algebra/kleene.ts`

**Message:**

```
ci(algebra): mirror the semiring→ring→kleene tower drift-check to CI …

…(shadow*)

Aaron-authorized. Two gaps closed:

1. The codegen-law-drift check covered semiring/ring/star-ring but NOT kleene —
   the tower was incomplete. Adds a TS Kleene oracle instance
   (src/Core.TypeScript/algebra/kleene.ts: tropical (min,+) — idempotent add, so
   a⊕a=a genuinely holds, unlike the numberSemiring used for the ring tier),
   registers IKleeneAlgebra in codegen-law-drift.test.ts, and checks in the
   regenerated generated-kleene-laws.test.ts. The generated laws pass against the
   tropical instance (semiring axioms + add-idempotent; star/closure laws render as
   documented comments — no scalar encoding).

2. The drift-check did NOT run in CI at all — it was local-only. Adds a step to
   gate.yml's cross-verify job running the drift-check + all four generated
   law-property files (semiring, ring, star-ring, kleene). Pure bun/arithmetic, no
   go/python spawns (the cross-language law cross-verify stays a separate lane, to
   avoid a toolchain dependency in this job).

Local: 26 tests / 0 fail on the exact CI command; gate.yml valid YAML.

Anchors: Lehmann 1977 (matrix star = closure); Kozen 1994 (Kleene axioms);
Vandiver 1934 / Golan 1999 (idempotent semirings are zerosumfree — why kleene needs
its own idempotent oracle, not the ring's).

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>

AgencySignature-v1:
  persona: otto
  actor: zeta-otto
  surface: ci
  topology: shadow-clone
  intent: mirror-semiring-ring-kleene-tower-drift-check-to-ci-add-kleene-oracle-instance
  authorization: aaron-explicit-2026-07-02
  uncertainty: low
  measure: kleene-added-to-codegen-law-drift-tropical-ts-instance-idempotent-add-holds-drift-check-wired-into-gate-yml-cross-verify-job-26-tests-0-fail
  delta-u: algebra-tower-drift-check-complete-through-kleene-and-now-enforced-on-every-pr
  seed: S4
```
