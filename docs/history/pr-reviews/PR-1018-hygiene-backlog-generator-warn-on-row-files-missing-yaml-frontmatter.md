# PR #1018 -- hygiene(backlog-generator): warn on row files missing YAML frontmatter

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 1018 |
| Title | hygiene(backlog-generator): warn on row files missing YAML frontmatter |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-05-01T04:23:08Z |
| Merged at | 2026-05-01T11:05:26Z |
| Merge commit SHA | `6827106e94c3df5a00dd8cc3d4154cc52cf1c2c3` |
| Branch | `hygiene/backlog-generator-warn-missing-frontmatter-2026-05-01` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/1018 |
| Changed files | 3 |
| Additions / deletions | +25 / -3 |

## Description

Class-level prevention for the bug surfaced by this session's BACKLOG.md drift. Adds a stderr WARN line per row file without id/title frontmatter; output unchanged so CI behaviour unchanged.

Composes with #1016 (the frontmatter backfill).

🤖 Posted by Claude Code on Aaron's behalf

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | true |
| Total threads | 6 |
| Resolved threads | 5 |
| Unresolved threads | 1 |
| Total review comments | 6 |
| Total fix commits (touching thread paths) | 5 |

## Review threads

### Thread 1 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5-6nX_`
- **Path**: `tools/backlog/generate-index.sh`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-01T04:25:12Z on `tools/backlog/generate-index.sh`:143 (association: NONE)

  <pre>
  The warning currently prints the absolute path from `find` (because `BACKLOG_DIR` is rooted at `REPO_ROOT`). In CI logs this becomes a long runner-specific path and is harder to act on. Consider printing a repo-relative path (e.g., strip `$REPO_ROOT/`) or reuse a `docs/backlog/...` relative path, and tweak the wording to “missing required frontmatter fields (id/title)” since the condition is field-level (not necessarily absence of the entire YAML block).
  </pre>

### Thread 2 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5-7iUG`
- **Path**: `docs/backlog/P1/B-0125-skip-fsharp-analyze-on-docs-only-prs-2026-05-01.md`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-01T06:56:40Z on `docs/backlog/P1/B-0125-skip-fsharp-analyze-on-docs-only-prs-2026-05-01.md`:5 (association: NONE)

  <pre>
  The added `title:` in frontmatter says "Analyze (csharp)", but the filename slug is `skip-fsharp-analyze-...`. Since the generator uses the frontmatter title in the aggregate index, this mismatch makes the row harder to search/understand. Either align the frontmatter/heading wording with the slug, or rename the file to match the actual subject (and regenerate docs/BACKLOG.md accordingly).
  </pre>

### Thread 3 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5-7iUP`
- **Path**: `tools/backlog/generate-index.sh`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-01T06:56:40Z on `tools/backlog/generate-index.sh`:142 (association: NONE)

  <pre>
  The WARN message says "missing YAML frontmatter", but the condition is actually "id or title missing". A file could have frontmatter but be missing one required field, so the message is slightly inaccurate. Consider rewording to something like "missing required frontmatter fields (id + title)" (or detect the absence of the `---` block separately if you want to keep this wording).
  ```suggestion
        # Surface missing required frontmatter fields to stderr —
        # the file is otherwise silently rendered as `**[](path)** `
        # (empty link text + empty title) and the integrity check
        # fails later with no hint at the cause. Warning only —
        # does not change generator output, so does not regress
        # existing CI behaviour. Operators / CI can grep stderr
        # for "WARN:" to surface the cause early.
        if [ -z "$id" ] || [ -z "$title" ]; then
          # Print path relative to REPO_ROOT so CI logs aren't
          # cluttered with runner-specific absolute paths
          # (Codex P2 finding: keep warnings portable).
          echo "WARN: ${file#"${REPO_ROOT}"/} — missing required frontmatter fields (need id + title)" &gt;&amp;2
  ```
  </pre>

### Thread 4 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5-8fB3`
- **Path**: `tools/backlog/generate-index.sh`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-01T09:23:46Z on `tools/backlog/generate-index.sh`:24 (association: NONE)

  <pre>
  P1: The documented warning format in the header comment ("missing YAML frontmatter (need id + title)") doesn’t match the actual emitted warning text below. Please align the header comment and the echo message so operators can reliably grep/recognize the warning in CI logs.
  ```suggestion
  #   `WARN: &lt;file&gt; — missing YAML frontmatter`
  ```
  </pre>

### Thread 5 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5-8fCR`
- **Path**: `docs/backlog/P1/B-0125-skip-fsharp-analyze-on-docs-only-prs-2026-05-01.md`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-01T09:23:46Z on `docs/backlog/P1/B-0125-skip-fsharp-analyze-on-docs-only-prs-2026-05-01.md`:8 (association: NONE)

  <pre>
  P1: The frontmatter `title:` was updated, but the H1 heading below still says "Skip Analyze (csharp) ...". Please update the heading to match the new title to avoid intra-file drift (frontmatter is what the index generator reads, but humans will read the H1).
  </pre>

### Thread 6 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM5-9Jb5`
- **Path**: `tools/backlog/generate-index.sh`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-01T11:05:52Z on `tools/backlog/generate-index.sh`:141 (association: NONE)

  <pre>
  P1 (repo convention): avoid name-attribution in current-state surfaces. This new comment adds "Codex" attribution; please rewrite it using a role-ref (e.g., "review finding") or drop the attribution entirely (see .github/copilot-instructions.md 'No name attribution in code, docs, or skills').
  ```suggestion
          # (Keep warnings portable.)
  ```
  </pre>

## Fix commits (touching thread paths)

### `eb12bc41b79c390494bf803442bf1cc507df5ad9` -- 2026-05-01T04:23:06Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/backlog/generate-index.sh`

**Message:**

```
hygiene(backlog-generator): warn on row files missing YAML frontmatter

Class-level fix from this session's BACKLOG.md drift incident
(2026-05-01). Three rows merged to main with heading-only format
(B-0124, B-0125, B-0126) silently rendered as empty
`**[](path)** ` entries; the integrity check failed downstream
on every new backlog PR with no hint at the cause.

This change adds a stderr `WARN:` line per row file without `id`
or `title` frontmatter. Doesn't change generator output —
existing CI behaviour is unaffected. Operators / CI can grep
stderr to surface the cause early; ideally before the row even
gets merged.

Tested locally: fires for the three pre-existing leaky-format
rows on main. Will go silent once #1016 (frontmatter backfill)
merges.

Composes with:
- PR #1016 — backfills the frontmatter on the three pre-existing
  rows; this PR is the prevention-layer for future repeats.
- `tools/backlog/generate-index.sh` schema (id/priority/status/title)
  established by `docs/research/backlog-split-design-otto-181.md`.

Layer 3 of the 4-layer pattern — encode the class (silent
empty-render is the bug; warning is the catch), not the instance
(fixing only the three rows that triggered this).

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
```

### `1df2dfe7b16acf18b3b7dcf05eb0c9d69a0981c0` -- 2026-05-01T05:01:25Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/backlog/generate-index.sh`

**Message:**

```
hygiene(backlog-generator): warn uses repo-relative path (Codex P2)

Codex P2: the WARN message printed `${file}` which is the
absolute path from `find` (rooted at REPO_ROOT). In CI logs
this becomes a long runner-specific absolute path that's
noise. Switch to repo-relative path via `${file#${REPO_ROOT}/}`
so warnings stay portable across runners + local invocations.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
```

### `853069c6ad9e610f20467a4c815b40592df57d7d` -- 2026-05-01T06:53:51Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/backlog/generate-index.sh`

**Message:**

```
hygiene(backlog-generator): fix CI failures — shellcheck SC2295 + dri…

…ft backfill

Two CI failures on PR #1018 root-caused and fixed:

(1) **shellcheck SC2295**: parameter expansion `${file#${REPO_ROOT}/}`
    needs the inner expansion quoted separately to prevent pattern
    matching. Changed to `${file#"${REPO_ROOT}"/}` per
    shellcheck's recommendation.

(2) **BACKLOG.md generated-index drift**: B-0124, B-0125, B-0126
    were filed without YAML frontmatter (only markdown headers),
    so the new generator (which now WARNs on missing frontmatter
    AND skips emit) produced different output than committed
    BACKLOG.md (which had empty entries for those files).
    Backfilled frontmatter on all three with id/priority/status/
    title/created/last_updated. Regenerated BACKLOG.md to match.

Composes with the verify-before-state-claim discipline being
mechanized via task #350 — these were existing files claiming
backlog-row status without satisfying the schema. The WARN
behavior in this PR is exactly the mechanization.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
```

### `88b3029c66da75b4362414257c82fe910a4e2c4c` -- 2026-05-01T09:21:05Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `docs/backlog/P1/B-0125-skip-fsharp-analyze-on-docs-only-prs-2026-05-01.md`
- `tools/backlog/generate-index.sh`

**Message:**

```
hygiene(backlog-generator): address PR #1018 review threads — title/f…

…ilename alignment + WARN-message accuracy

Two real fixes (Copilot xref):

1. **B-0125 title/filename alignment**: filename slug uses `fsharp`
   (the language) but title said "Analyze (csharp)" (the GitHub Actions
   step name; F# runs as language=csharp under CodeQL). The mismatch
   made search confusing. Title now reads "Skip F#/Analyze (csharp) on
   docs-only PRs..." — slash-pair makes the F#-runs-as-csharp distinction
   explicit and aligns with both filename slug and the actual workflow
   step name.

2. **WARN message accuracy in generate-index.sh**: condition is
   `[ -z "$id" ] || [ -z "$title" ]` (one OR the other missing) but
   message said "missing YAML frontmatter" which implies the whole block
   is absent. Reworded to "missing required frontmatter field (need both
   id and title)" — accurate to what the condition actually tests.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
```

### `2800fdf46915dd6d805d28fb4c4f05327f9b0c83` -- 2026-05-01T09:59:50Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `docs/backlog/P1/B-0125-skip-fsharp-analyze-on-docs-only-prs-2026-05-01.md`
- `tools/backlog/generate-index.sh`

**Message:**

```
hygiene(backlog-generator): address PR #1018 follow-up review threads…

… — header comment + H1 heading consistency
```
