# PR #6442 -- feat(081KT07NV0008QG0R003BE6MJ2): Rust Bonsai oracle (#4 of 4) — serialize/parse + accumulate, Result-native, zero-dep

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 6442 |
| Title | feat(081KT07NV0008QG0R003BE6MJ2): Rust Bonsai oracle (#4 of 4) — serialize/parse + accumulate, Result-native, zero-dep |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-06-01T17:26:14Z |
| Merged at | 2026-06-01T17:35:56Z |
| Merge commit SHA | `a90d54500ab1b6bfe979b25bd8e88dae7130b4df` |
| Branch | `otto-cli/b0976-rust-bonsai-oracle-2026-06-01` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/6442 |
| Changed files | 4 |
| Additions / deletions | +1408 / -0 |

## Description

The **final corner** of the four-oracle compiler-BFT for the Bonsai-subset expression-tree serializer (081KT07NV0008QG0R003BE6MJ2 slice 1). Replays the shared `src/Core.TypeScript/bonsai/golden-vectors.json` **byte-for-byte** (`serialize(parse(canonical)) == canonical`) — the cross-language byte lock with the TS (#6433), F# (#6429), and C# (#6440) oracles. "The compilers don't lie."

## Shape (matches TS/F#/C# error-for-error)
- **Result over throw**, `std::result::Result`-native (the BCL shape — no port needed); no panic crosses the boundary.
- **Production ZERO-DEP**: a minimal hand-rolled JSON reader covers the tiny canonical grammar — matching the TS oracle's zero-dep posture (JS's built-in `JSON.parse`) and the repo's explicit zero-prod-dep doctrine for `Core.Rust.*`. `serde_json` is **dev-only** (reads the golden fixture wrapper in the cross-verify test). `serialize` hand-rolls canonical bytes; UTF-8 strings emit astral chars literally — byte-parity with the UTF-16 oracles' literal surrogate-pair emission. (Lone surrogates aren't representable in a Rust `String`; the golden vectors don't contain any, so the byte-lock is unaffected — documented in the source.)
- `Expr` / `ConstValue` / `BinOp` enums with **derived structural `PartialEq`** (no custom `equals` needed, unlike the C# records-with-lists); `BonsaiFeedback` variant-for-variant parity.
- **accumulate-mode**: `parse_all` collects **every** per-node decline keyed by JSON-path; `to_problem_details` groups into the RFC-9457 `ProblemDetails` (`BTreeMap` path → messages).

## Tests — 15, all pass; clippy clean
Byte-exact golden replay (every case) + an independently-constructed tree → same canonical + rejection-by-variant (UnsupportedVersion / MalformedJson / UnknownKind / UnknownConstTag / UnknownOp / NonSafeInt / NonCanonical / TooDeep) + accumulate (3 independent declines with paths + `ProblemDetails` keys + single malformed / single non-canonical). `cargo test` green; `cargo clippy --all-targets -- -D warnings` clean (incl. `missing_docs`); `cargo fmt`.

> Note: Rust is **not in CI's gate** (the gate runs dotnet + bun + lint, no `cargo`), so local `cargo test` + `cargo clippy -D warnings` is the real verification bar — held here.

## Oracle family — COMPLETE
**TS ✅ · F# ✅ · C# (#6440) · Rust (this).** All four byte-locked on the shared golden vectors. Slice-1 four-oracle BFT done.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | true |
| Total threads | 6 |
| Resolved threads | 5 |
| Unresolved threads | 1 |
| Total review comments | 6 |
| Total fix commits (touching thread paths) | 2 |

## Review threads

### Thread 1 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GMpkq`
- **Path**: `src/Core.Rust.Bonsai/src/lib.rs`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-01T17:27:54Z on `src/Core.Rust.Bonsai/src/lib.rs`:708 (association: NONE)

  <pre>
  When the `t` tag is **missing** (not present in the object), the code falls through to `other` with `tag == None` and reports `UnknownConstTag("null")` — which is misleading: the tag wasn't "null", it was absent. The same misreport occurs in `push_const` (lines 855-858). Consider distinguishing the two cases (e.g., reporting `MalformedJson("const value missing 't' tag")` for `None`, and `UnknownConstTag(s)` only for actual unknown tag strings).
  </pre>

### Thread 2 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GMplQ`
- **Path**: `src/Core.Rust.Bonsai/src/lib.rs`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-01T17:27:55Z on `src/Core.Rust.Bonsai/src/lib.rs`:410 (association: NONE)

  <pre>
  The number scanner accepts `.`, `e`, `E`, `+`, `-` at any position after the start, so malformed tokens like `1-2`, `1.2.3`, or `1+2` are silently consumed as a single `Json::Float` instead of failing JSON parsing. In practice the canonical-only guard at the end of `parse` re-serializes and catches mismatches, so this is mostly hidden in `parse`, but it weakens the error reporting of the underlying JSON reader and could surface unexpectedly through `parse_all` (which calls into `push_*` paths before the canonical guard). Consider tightening the scanner to a proper JSON-number state machine (or at least requiring `.`/`e`/`E` to appear at most once and `+`/`-` only immediately after `e`/`E`).
  </pre>

### Thread 3 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GMplm`
- **Path**: `src/Core.Rust.Bonsai/src/lib.rs`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-01T17:27:55Z on `src/Core.Rust.Bonsai/src/lib.rs`:316 (association: NONE)

  <pre>
  `Reader::new` materializes the entire input into a `Vec&lt;char&gt;`, which is an O(n) extra allocation plus a 4× memory blowup (`char` is 4 bytes). For long canonical payloads (golden vectors and the depth-1024 test), this is wasteful. Consider holding `&amp;[u8]` or `&amp;str` with byte-position indexing — the canonical grammar is largely ASCII, and `string()` is the only place that needs UTF-8 decoding (which `str::char_indices` handles cleanly).
  </pre>

### Thread 4 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GMq3w`
- **Path**: `src/Core.Rust.Bonsai/src/lib.rs`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-06-01T17:29:07Z on `src/Core.Rust.Bonsai/src/lib.rs`:395 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Reject invalid JSON numbers before semantic validation**

  When the input contains an invalid JSON number such as a leading-zero integer (`"v":01`) or a dangling sign/exponent (`"v":1+`), this scanner consumes the bad token as `Int`/`Float` and lets `parse` return `NonCanonical` or `ExpectedInt` instead of `MalformedJson`. The TS/F#/C# oracles use real JSON parsers and reject the same bytes as malformed, so the Rust oracle is not error-for-error on rejection inputs.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 5 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GMq30`
- **Path**: `src/Core.Rust.Bonsai/src/lib.rs`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-06-01T17:29:07Z on `src/Core.Rust.Bonsai/src/lib.rs`:462 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Reject raw control characters in JSON strings**

  When a string contains an unescaped control character such as a raw newline, this branch accepts it as part of the string and the caller later reports `NonCanonical` after re-serialization. JSON forbids unescaped U+0000–U+001F characters, and the other oracles reject these bytes as malformed, so this breaks the stated cross-language malformed/rejection contract for any invalid string payload.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 6 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM6GMxUv`
- **Path**: `src/Core.Rust.Bonsai/src/lib.rs`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-06-01T17:35:58Z on `src/Core.Rust.Bonsai/src/lib.rs`:431 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Reject integral float spellings as non-canonical**

  When a valid but non-canonical JSON number such as `1.0` or `1e0` appears in an `int` literal (or the wrapper `v`), this branch classifies the token as `Json::Float`, so `parse` returns `ExpectedInt`/`MalformedJson` before the canonical round-trip check. The TS reference parses with `JSON.parse` and only then compares `serialize(parse(s))` to the original (`src/Core.TypeScript/bonsai/bonsai.ts` lines 317-331), so the same integral value is rejected as `NonCanonical`; this breaks the stated specific rejection-variant contract for integral decimal/exponent spellings.

  Useful? React with 👍 / 👎.
  </pre>

## Fix commits (touching thread paths)

### `dc4addc7d4563f25b67aa1eb47468cf017439dec` -- 2026-06-01T17:26:03Z -- ``

**Touched files (intersect with thread paths):**

- `src/Core.Rust.Bonsai/src/lib.rs`

**Message:**

```
feat(081KT07NV0008QG0R003BE6MJ2): Rust Bonsai oracle (#4 of 4) — serialize/parse + accumu…

…late, Result-native, zero-dep

The final corner of the four-oracle compiler-BFT for the Bonsai-subset expression
serializer (081KT07NV0008QG0R003BE6MJ2 slice 1). Replays the shared golden-vectors.json byte-for-byte
(serialize(parse(canonical)) == canonical) — the cross-language byte lock with the
TS (#6433), F# (#6429), and C# (#6440) oracles. The compilers don't lie.

- Result over throw, std::result::Result-native (BCL shape); no panic crosses the boundary.
- Production ZERO-DEP: a minimal hand-rolled JSON reader covers the tiny canonical grammar
  (matching the TS oracle's zero-dep posture + the repo's zero-prod-dep doctrine for
  Core.Rust.*); serde_json is DEV-ONLY (reads the golden fixture wrapper in the cross-verify).
  serialize hand-rolls canonical bytes; UTF-8 strings emit astral chars literally (parity
  with the UTF-16 oracles' literal surrogate-pair emission).
- Expr / ConstValue / BinOp enums with derived structural PartialEq (no custom equals needed);
  BonsaiFeedback variant-for-variant parity with F#/C#/TS.
- accumulate-mode: parse_all collects EVERY per-node decline keyed by JSON-path;
  to_problem_details groups into RFC-9457 ProblemDetails (BTreeMap path -> messages).
- 15 tests: byte-exact golden replay (all cases) + rejection-by-variant + accumulate.
  cargo test green; cargo clippy --all-targets -D warnings clean (incl missing_docs);
  cargo fmt. (Rust is not in CI's cargo-less gate, so local cargo IS the verification bar.)

081KT07NV0008QG0R003BE6MJ2 oracle-family COMPLETE: TS + F# + C# + Rust, all four byte-locked.

Co-Authored-By: Claude <noreply@anthropic.com>
```

### `35850fcc517793f0f90e0643985dd9b0a28ee8f9` -- 2026-06-01T17:32:49Z -- ``

**Touched files (intersect with thread paths):**

- `src/Core.Rust.Bonsai/src/lib.rs`

**Message:**

```
fix(081KT07NV0008QG0R003BE6MJ2): tighten Rust JSON reader to reject malformed bytes (review)

Codex P2 + Copilot review on #6442: the hand-rolled reader was lenient, so malformed
input returned NonCanonical/ExpectedInt where the TS/F#/C# real-JSON-parser oracles
return MalformedJson — weakening the error-for-error rejection contract. Tightened so
the Rust oracle is error-for-error with the siblings on these inputs:

- number scanner: strict RFC-8259 grammar (-? (0|[1-9][0-9]*) frac? exp?). Rejects
  leading zeros (01), dangling sign/point/exponent (1+, 1., 1e), double-punctuation
  (1.2.3, 1-2) as MalformedJson rather than consuming them.
- string scanner: reject unescaped control chars (U+0000–U+001F) as MalformedJson
  (JSON forbids them; the siblings reject them).
- +2 tests locking the new rejections; 17 tests green; clippy -D warnings clean; fmt.

Not changed (resolved with rationale on the threads): missing const 't' tag keeps
UnknownConstTag for variant-parity with the siblings; Vec<char> reader retained
(canonical trees are small; keeps the surrogate/char logic clean).

Co-Authored-By: Claude <noreply@anthropic.com>
```
