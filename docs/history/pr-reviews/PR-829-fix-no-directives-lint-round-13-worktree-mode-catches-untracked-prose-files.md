# PR #829 -- fix(no-directives-lint): round-13 — worktree mode catches untracked prose files

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 829 |
| Title | fix(no-directives-lint): round-13 — worktree mode catches untracked prose files |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-04-29T08:24:01Z |
| Merged at | 2026-04-29T08:26:22Z |
| Merge commit SHA | `72ddfca77d3d7e237272bd9392af54f928cf077d` |
| Branch | `lint-untracked-fix-round13-2026-04-29` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/829 |
| Changed files | 2 |
| Additions / deletions | +120 / -5 |

## Description

Round-13 follow-up to merged PR #825. Round-12 manual verification surfaced a real bug: \`SCOPE=worktree\` mode did not include UNTRACKED prose files. The pre-commit/worktree use case explicitly includes about-to-be-committed prose, but \`git diff --name-only\` only reports tracked file changes — a freshly-created \`memory/feedback_*.md\` file (the most common pre-commit case) was silently skipped with \"no Otto-prose surfaces changed\".

## Round-13 fixes

1. **Worktree CHANGED_FILES** now appends \`git ls-files --others --exclude-standard\`. Untracked-but-not-ignored files matching prose-surface globs are pulled in. PR mode is unchanged (BASE_REF...HEAD diff is committed-only).

2. **Per-file extraction loop** branches on tracked-vs-untracked. For untracked files in worktree mode, every line is prefixed with \`+\` and fed into the same awk pipeline as a tracked-file diff. Detection uses \`git ls-files --error-unmatch\`.

## Tests.md

- Reproduction commands no longer use \`git stash --include-untracked\` (it was hiding tracked .sh modifications during testing — the round-13 discovery itself).
- Fixture D (renamed-file violation) reframed as a \`git worktree add --detach\` flow rather than commits + reset --hard inside the active checkout.

## Verification

A/B/C all executed against this commit:

- **A. Real violation** (\`Aaron's directive\`) → flagged, exit 1.
- **B. Filename contains \`human\` token + clean content** → not flagged, exit 0.
- **C. Blockquote with violation string** → not flagged, exit 0.
- **D. Renamed file** → documented-only (requires worktree setup).

## Best rule (round-13)

\`\`\`text
PR mode checks committed diff.
Worktree mode checks staged, unstaged, AND untracked candidate prose.
\`\`\`

## Best tiny blade

\`\`\`text
The fixture did not fail.
It found the next bug.
\`\`\`

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 5 |
| Resolved threads | 0 |
| Unresolved threads | 5 |
| Total review comments | 5 |
| Total fix commits (touching thread paths) | 1 |

## Review threads

### Thread 1 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM5-XilK`
- **Path**: `tools/lint/no-directives-otto-prose.tests.md`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-29T08:27:42Z on `tools/lint/no-directives-otto-prose.tests.md`:143 (association: NONE)

  <pre>
  The added prose references persona/human first-names (e.g., “Amara” / “round-12 … called for”). Repo convention is to avoid name attribution on current-state surfaces like tools/ docs; use role-refs instead (e.g., “the UX engineer”, “the reviewer”, or omit the attribution).
  </pre>

### Thread 2 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM5-XilZ`
- **Path**: `tools/lint/no-directives-otto-prose.tests.md`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-29T08:27:43Z on `tools/lint/no-directives-otto-prose.tests.md`:155 (association: NONE)

  <pre>
  The doc says fixtures A/B/C were verified against commit `&lt;round-13&gt;`, but that placeholder isn’t actionable. Replace it with the actual commit SHA (or remove the claim) so readers can reproduce the stated verification point-in-time.
  ```suggestion
  A/B/C are executable reproductions that should return the
  expected results for the current lint. D (renamed-file)
  requires temporary commits and is documented-only.
  ```
  </pre>

### Thread 3 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM5-Xilj`
- **Path**: `tools/lint/no-directives-otto-prose.tests.md`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-29T08:27:43Z on `tools/lint/no-directives-otto-prose.tests.md`:196 (association: NONE)

  <pre>
  This section says the renamed-file case requires `git reset --hard HEAD~1`, but the documented worktree flow doesn’t do that. Update the prose to match the actual steps (or add the missing reset step if it’s still required).
  ```suggestion
  This case requires temporary commits, but they should live only
  inside a disposable `git worktree` (per round-13 review) rather
  than the active checkout, so cleanup is just removing the
  worktree and keeping the scar-testing away from in-flight work.
  The shape:
  ```
  </pre>

### Thread 4 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM5-Xilw`
- **Path**: `tools/lint/no-directives-otto-prose.tests.md`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-29T08:27:43Z on `tools/lint/no-directives-otto-prose.tests.md`:200 (association: NONE)

  <pre>
  `mktemp -d` without a template fails on BSD/macOS. Since the repo targets macOS bash, use a portable form (e.g., a `mktemp -d -t &lt;name&gt;.XXXXXX` template, or the common fallback pattern) so this copy/paste fixture works cross-platform.
  ```suggestion
  tmpdir=$(mktemp -d -t no-directives-otto-prose.XXXXXX)
  ```
  </pre>

### Thread 5 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM5-Xil_`
- **Path**: `tools/lint/no-directives-otto-prose.sh`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-29T08:27:43Z on `tools/lint/no-directives-otto-prose.sh`:112 (association: NONE)

  <pre>
  In worktree mode, `git ls-files --others --exclude-standard` enumerates all untracked files in the repo before you filter to prose surfaces, which can be slow in large worktrees. Consider adding a pathspec (or otherwise restricting the ls-files call) so only candidate prose-surface paths are listed.
  </pre>

## Fix commits (touching thread paths)

### `722828ec2c433c3df84eb8c94fcecb945a6a8898` -- 2026-04-29T08:23:09Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/lint/no-directives-otto-prose.sh`
- `tools/lint/no-directives-otto-prose.tests.md`

**Message:**

```
fix(no-directives-lint): round-13 — worktree mode now catches untrack…

…ed prose files

Round-13 manual verification (per round-12 reviewer ask: "documented
fixtures are coverage intent; executed fixtures are coverage proof")
exposed a real bug: SCOPE=worktree did not include UNTRACKED prose
files. The pre-commit/worktree use case explicitly includes about-to-
be-committed prose, but `git diff --name-only` only reports tracked
file changes — a freshly-created memory/feedback_*.md file (the most
common pre-commit case) was silently skipped with
"no Otto-prose surfaces changed".

Two fixes:

1. Worktree CHANGED_FILES collection now appends
   `git ls-files --others --exclude-standard` so untracked-but-not-
   ignored files matching the prose-surface globs are pulled in.
   PR mode is unchanged because its changed-file set is the
   BASE_REF...HEAD diff (committed content only).

2. Per-file extraction loop now branches on tracked-vs-untracked.
   For untracked files in worktree mode, every line of the file is
   prefixed with `+` and fed into the same awk pipeline as a
   tracked-file diff, so untracked content goes through identical
   pattern-match + blockquote-filter logic as added-in-diff content.
   Detection uses `git ls-files --error-unmatch` (exits 0 when path
   is in the index, non-zero otherwise).

Tests.md updated:

- Reproduction commands no longer use `git stash --include-untracked`
  before each fixture. The stash was hiding tracked modifications to
  the lint .sh itself during testing (round-13 discovery), defeating
  the purpose of testing the modified script. The commands now
  assume a clean working tree and document that explicitly.

- D fixture (renamed-file violation) reframed as a `git worktree
  add --detach` flow rather than running commits + reset --hard
  inside the active checkout. Per round-13 review: keep the
  scar-testing away from the patient.

- A/B/C verified executing against this commit:
  A. Real violation (`Aaron's directive`) → flagged (exit 1).
  B. Filename contains `human` token + clean content → not flagged.
  C. Blockquote with violation string → not flagged (whitelist).
  D. Renamed file → documented-only (requires worktree setup).

Best rule (round-13):

  PR mode checks committed diff.
  Worktree mode checks staged, unstaged, AND untracked candidate prose.

Best tiny blade:

  The fixture did not fail.
  It found the next bug.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
```
