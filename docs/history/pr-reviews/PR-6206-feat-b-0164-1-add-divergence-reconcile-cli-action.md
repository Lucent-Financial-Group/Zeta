# PR #6206 -- feat(081KR7JY10008QG0R000MH7PJT): add divergence reconcile CLI action

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 6206 |
| Title | feat(081KR7JY10008QG0R000MH7PJT): add divergence reconcile CLI action |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-05-31T11:24:09Z |
| Merged at | 2026-05-31T12:18:48Z |
| Merge commit SHA | `f693b5402399f7ef9bc086ccc23e199643e24c8f` |
| Branch | `claim/codex-loop-b0164-1-divergence-reconcile-cli-20260531` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/6206 |
| Changed files | 3 |
| Additions / deletions | +545 / -69 |

## Description

## Summary

- Adds a bounded `--reconcile <relPath> --decision <decision> [--note <text>]` action to `tools/hygiene/divergence-reconcile.ts`.
- Keeps default / `--list` behavior as a non-failing pending-shard report, while argv validation rejects incomplete or non-canonical write-back attempts before file mutation.
- Updates the 081KR7JY10008QG0R000MH7PJT backlog row with the Codex CLI action slice and focused evidence.

## Checks

- `bun test tools/hygiene/divergence-reconcile.test.ts`
- `bun run typecheck`
- `git diff --check`
- `bunx prettier --check tools/hygiene/divergence-reconcile.ts tools/hygiene/divergence-reconcile.test.ts docs/backlog/P1/081KR7JY10008QG0R000MH7PJT-pr-review-disagreement-preservation-protocol.md`
- `bun tools/hygiene/divergence-reconcile.ts --help`
- `bun tools/hygiene/divergence-reconcile.ts --list`

## Limits

- This still does not touch GitHub review-thread resolution; the live dual-loop end-to-end path remains blocked on 081KQJZR90008QG0R000FTJ1TC.

Agency-Signature-Version: 1
Agent: Vera
Agent-Runtime: OpenAI Codex desktop heartbeat loop
Agent-Model: GPT-5
Credential-Identity: aaron-codex-desktop
Credential-Mode: shared
Human-Review: none
Human-Review-Evidence: none
Action-Mode: autonomous-fail-open
Task: 081KQJZR90008QG0R002GJAJ19

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | true |
| Total threads | 8 |
| Resolved threads | 8 |
| Unresolved threads | 0 |
| Total review comments | 13 |
| Total fix commits (touching thread paths) | 9 |

## Review threads

### Thread 1 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F718S`
- **Path**: `tools/hygiene/divergence-reconcile.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-31T11:26:04Z on `tools/hygiene/divergence-reconcile.ts`:621 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Reject symlinked shard paths before write-back**

  When the new CLI is run on a divergence entry that is a symlink, `--list` can still surface it because the scanner reads any `.md` non-directory, and this call then passes the lexical path to `reconcileDivergenceShard`; `readFileSync`/`writeFileSync` follow the symlink, so a symlink under `docs/hygiene-history/divergences/` pointing at another markdown file outside that tree can have its `## Reconciliation` section overwritten despite the intended containment guard. The write-back path should resolve/lstat the target (or open with no-follow semantics) before writing.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 2 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F71_E`
- **Path**: `docs/claims/codex-loop-b0164-1-divergence-reconcile-cli-20260531.md`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-31T11:26:19Z on `docs/claims/codex-loop-b0164-1-divergence-reconcile-cli-20260531.md`:5 (association: NONE)

  <pre>
  Claim files under docs/claims/ are meant to exist only while work is in progress; the protocol requires deleting the claim file in the same PR that lands the work. Keeping this file in the merged PR will cause docs/claims/ to accumulate completed claims on main.
  </pre>

### Thread 3 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F71_G`
- **Path**: `tools/hygiene/divergence-reconcile.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-31T11:26:19Z on `tools/hygiene/divergence-reconcile.ts`:627 (association: NONE)

  <pre>
  The catch handler assumes anything thrown is an Error and will have a .message. Node/Bun APIs can throw non-Error values (or Error-like objects), which can lead to printing `error: undefined`. Prefer an instanceof check with a String(...) fallback for a reliable message.
  </pre>

### Thread 4 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F7273`
- **Path**: `tools/hygiene/divergence-reconcile.ts`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-31T11:30:55Z on `tools/hygiene/divergence-reconcile.ts`:621 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Reject symlinked ancestor paths before write-back**

  Fresh evidence: this version now exposes `--reconcile` as a CLI path and only `lstatSync`s the final file, so a shard path whose parent inside `docs/hygiene-history/divergences/` is a symlink to another directory still passes the lexical containment check and this call will read/write through that ancestor symlink. In that setup, `--reconcile docs/hygiene-history/divergences/linkdir/file.md ...` can overwrite a markdown file outside the divergence tree; resolve and compare the real shard target/root or reject symlinks in every path component before invoking the write-back helper.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-31T12:16:49Z on `tools/hygiene/divergence-reconcile.ts`:621 (association: MEMBER)

    <pre>
    Already handled: `rejectSymlinkedAncestors` lstat-walks every ancestor component between the root and the shard (`parts.slice(0, -1)`) and the final shard opens with `O_NOFOLLOW`; the `rejects symlinked shard ancestor paths before write-back` test covers this. d9b8b2963 additionally closes the ancestors-above-the-root gap.
    </pre>

### Thread 5 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F740M`
- **Path**: `tools/hygiene/divergence-reconcile.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-31T11:37:59Z on `tools/hygiene/divergence-reconcile.ts`:474 (association: NONE)

  <pre>
  P0: `writeSync()` is not guaranteed to write the full buffer/string in one call; if a short write occurs after `ftruncateSync(fd, 0)`, the shard can be left partially written/corrupted. Loop until all bytes are written (keeping explicit positions) so write-back is deterministic.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-31T12:16:52Z on `tools/hygiene/divergence-reconcile.ts`:474 (association: MEMBER)

    <pre>
    Already addressed: `writeAllUtf8Sync` loops on `writeSync` until every byte is written (keeping explicit offsets) and throws on zero-progress, so a short write after `ftruncateSync` can't leave the shard partially written. Resolving (thread is outdated).
    </pre>

### Thread 6 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F76hi`
- **Path**: `tools/hygiene/divergence-reconcile.ts`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-31T11:45:03Z on `tools/hygiene/divergence-reconcile.ts`:449 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Reject a symlinked divergence root**

  When `docs/hygiene-history/divergences` itself has been replaced by a symlink, this guard starts traversal at that symlink and only `lstatSync`s child components, so `--reconcile docs/hygiene-history/divergences/file.md ...` still opens and rewrites a markdown file in the symlink target outside the intended shard tree. Fresh evidence beyond the prior symlink comments is that this hardened version checks final files and child ancestors, but never checks `divergenceRoot` before following it; lstat or realpath-compare the root before opening the shard.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-31T12:16:47Z on `tools/hygiene/divergence-reconcile.ts`:449 (association: MEMBER)

    <pre>
    The final root component was already lstat-rejected; d9b8b2963 generalizes that same check to the full repo-base-&gt;root chain in `assertRealDivergenceRoot`, so the symlinked-root case stays closed (existing `rejects a symlinked divergence root before write-back` test) and ancestors above it are now covered too.
    </pre>

### Thread 7 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F78Q9`
- **Path**: `tools/hygiene/divergence-reconcile.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-31T11:53:27Z on `tools/hygiene/divergence-reconcile.ts`:409 (association: NONE)

  <pre>
  P1 (security): `constants.O_NOFOLLOW` is relied on to prevent opening symlink shards, but on some platforms (notably Windows) it can be unsupported/zero, which would silently drop the no-follow guarantee and reintroduce a symlink race in the write-back path. Consider failing closed when `O_NOFOLLOW` is unavailable so the reconcile action can’t run without this protection.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-31T12:16:50Z on `tools/hygiene/divergence-reconcile.ts`:409 (association: MEMBER)

    <pre>
    Already addressed: `regularShardOpenFlags` throws `O_NOFOLLOW is unavailable on this platform; refusing write-back without symlink protection` when the flag is non-numeric or 0, so write-back fails closed without the no-follow guarantee. Covered by the `fails closed when O_NOFOLLOW is unavailable` test.
    </pre>

### Thread 8 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F7_Jn`
- **Path**: `tools/hygiene/divergence-reconcile.ts`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-31T12:06:44Z on `tools/hygiene/divergence-reconcile.ts`:413 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Reject symlinked divergence-root parents**

  When an ancestor of the fixed root, such as `docs/hygiene-history`, is a symlink to another directory, this `lstatSync(absRoot)` follows that ancestor and only verifies the target `divergences` directory, while `rejectSymlinkedAncestors` starts checking below `divergenceRoot`. In that setup, `--reconcile docs/hygiene-history/divergences/file.md ...` still opens and rewrites the outside target even though the CLI is intended to confine writes to the real divergence tree; either realpath-compare the root against the repo root or lstat every component from `repoRoot` through `DIVERGENCE_ROOT` before opening.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-31T12:16:46Z on `tools/hygiene/divergence-reconcile.ts`:413 (association: MEMBER)

    <pre>
    Fixed in d9b8b2963. `assertRealDivergenceRoot` now lstat-walks every component from the repo base through `DIVERGENCE_ROOT` (reconstructed lexically via `resolve(absRoot, ...up-dots)`), so a symlinked ancestor *above* the root (`docs/`, `docs/hygiene-history/`) fails closed before any open/write — lstat only refuses the final component, which was the gap. Added a regression test at the ancestor-above-root position. Thanks for the catch.
    </pre>

## Fix commits (touching thread paths)

### `feb037db53b4f076880ab17bea6eecc94d54ecc8` -- 2026-05-31T10:45:09Z -- ``

**Touched files (intersect with thread paths):**

- `docs/claims/codex-loop-b0164-1-divergence-reconcile-cli-20260531.md`

**Message:**

```
claim: codex-loop-b0164-1-divergence-reconcile-cli-20260531 - 081KQJZR90008QG0R002GJAJ19.…

…1 CLI slice

Co-Authored-By: Codex <noreply@openai.com>

Codex-Origin: codex-launchd-loop

Codex-Surface: codex-background-service

Codex-Loop-Run-Id: 20260531T104240Z
```

### `37ca24a9841bafc23d2ed238a4532015f014a85b` -- 2026-05-31T11:23:01Z -- ``

**Touched files (intersect with thread paths):**

- `tools/hygiene/divergence-reconcile.ts`

**Message:**

```
feat(081KR7JY10008QG0R000MH7PJT): add divergence reconcile CLI action

Why:
- 081KR7JY10008QG0R000MH7PJT already had the pending-shard reader and write-back helper, but the repo-native CLI only exposed list mode.
- AC #4 needs the morning reconciliation path to resolve a shard in one read plus one bounded action.

What:
- Add --reconcile <relPath> --decision <decision> [--note <text>] alongside the existing default/--list report.
- Validate argv before writes, reject non-canonical decisions, and keep list mode separate from write-back mode.
- Update the 081KR7JY10008QG0R000MH7PJT row with the Codex CLI action slice and focused evidence.

Proof:
- bun test tools/hygiene/divergence-reconcile.test.ts
- bun run typecheck
- git diff --check
- bunx prettier --check tools/hygiene/divergence-reconcile.ts tools/hygiene/divergence-reconcile.test.ts docs/backlog/P1/081KR7JY10008QG0R000MH7PJT-pr-review-disagreement-preservation-protocol.md
- bun tools/hygiene/divergence-reconcile.ts --help
- bun tools/hygiene/divergence-reconcile.ts --list

Limits:
- This still does not touch GitHub review-thread resolution; the live dual-loop end-to-end path remains blocked on 081KQJZR90008QG0R000FTJ1TC.

Agency-Signature-Version: 1
Agent: Vera
Agent-Runtime: OpenAI Codex desktop heartbeat loop
Agent-Model: GPT-5
Credential-Identity: aaron-codex-desktop
Credential-Mode: shared
Human-Review: none
Human-Review-Evidence: none
Action-Mode: autonomous-fail-open
Task: 081KQJZR90008QG0R002GJAJ19
Co-Authored-By: Codex <noreply@openai.com>
```

### `bd650f8eb87ec21e2df5266d52029de59b425a25` -- 2026-05-31T11:28:35Z -- ``

**Touched files (intersect with thread paths):**

- `docs/claims/codex-loop-b0164-1-divergence-reconcile-cli-20260531.md`
- `tools/hygiene/divergence-reconcile.ts`

**Message:**

```
fix(081KR7JY10008QG0R000MH7PJT): harden reconcile CLI review feedback

Why:
- PR #6206 review flagged three actionable issues before merge: symlinked shard write-back, completed claim-file residue, and brittle non-Error catch formatting.
- The CLI write-back path should fail closed before mutating any path that is not a regular divergence shard file.

What:
- Skip symlinked markdown entries in list mode and reject symlink or non-file targets before write-back.
- Print stable CLI errors for non-Error throwables.
- Remove the completed Codex claim file from the PR branch.

Proof:
- bun test tools/hygiene/divergence-reconcile.test.ts
- bun run typecheck
- git diff --check
- bunx prettier --check tools/hygiene/divergence-reconcile.ts tools/hygiene/divergence-reconcile.test.ts docs/backlog/P1/081KR7JY10008QG0R000MH7PJT-pr-review-disagreement-preservation-protocol.md
- bun tools/hygiene/divergence-reconcile.ts --help
- bun tools/hygiene/divergence-reconcile.ts --list

Limits:
- GitHub review threads were not resolved or replied to; this commit only addresses their code/path feedback.

Agency-Signature-Version: 1
Agent: Vera
Agent-Runtime: OpenAI Codex desktop heartbeat loop
Agent-Model: GPT-5
Credential-Identity: aaron-codex-desktop
Credential-Mode: shared
Human-Review: none
Human-Review-Evidence: none
Action-Mode: autonomous-fail-open
Task: 081KQJZR90008QG0R002GJAJ19
Co-Authored-By: Codex <noreply@openai.com>
```

### `78e9a46f9c52f06325e81e275460028950d69379` -- 2026-05-31T11:34:29Z -- ``

**Touched files (intersect with thread paths):**

- `tools/hygiene/divergence-reconcile.ts`

**Message:**

```
fix(081KR7JY10008QG0R000MH7PJT): avoid path race in reconcile write-back

Why:
- CodeQL reported two high-severity path-race alerts on PR #6206 after the symlink guard patch.
- The reconcile action should bind validation, read, truncate, and write to one opened shard handle instead of reusing a path after metadata checks.

What:
- Open shard targets with O_NOFOLLOW and O_RDWR, then validate regular-file status via fstat on the same descriptor.
- Read, validate, fill, truncate, and write through that descriptor, closing it in a finally block.

Proof:
- bun test tools/hygiene/divergence-reconcile.test.ts
- bun run typecheck
- git diff --check
- bunx prettier --check tools/hygiene/divergence-reconcile.ts tools/hygiene/divergence-reconcile.test.ts docs/backlog/P1/081KR7JY10008QG0R000MH7PJT-pr-review-disagreement-preservation-protocol.md
- bun tools/hygiene/divergence-reconcile.ts --help
- bun tools/hygiene/divergence-reconcile.ts --list

Limits:
- This addresses the CodeQL TOCTOU annotations; remote CodeQL still needs to rerun on the pushed commit.

Agency-Signature-Version: 1
Agent: Vera
Agent-Runtime: OpenAI Codex desktop heartbeat loop
Agent-Model: GPT-5
Credential-Identity: aaron-codex-desktop
Credential-Mode: shared
Human-Review: none
Human-Review-Evidence: none
Action-Mode: autonomous-fail-open
Task: 081KQJZR90008QG0R002GJAJ19
Co-Authored-By: Codex <noreply@openai.com>
```

### `4c717cc73610f7b6abbeaf9af10472e956878316` -- 2026-05-31T11:42:06Z -- ``

**Touched files (intersect with thread paths):**

- `tools/hygiene/divergence-reconcile.ts`

**Message:**

```
fix(081KR7JY10008QG0R000MH7PJT): complete reconcile write-back hardening

Why:

- Copilot flagged that a single writeSync after ftruncate can leave a divergence shard partially written if a short write occurs.

- Prior review also showed ancestor symlinks under the divergence root can route the CLI write-back outside the shard tree.

What:

- Write reconciled UTF-8 content with explicit-position writeSync calls until every byte is written.

- Reject symlinked ancestor path components before opening the shard and keep the final target guarded by O_NOFOLLOW/fstat.

- Add coverage for symlinked ancestor paths.

Proof:

- bun test tools/hygiene/divergence-reconcile.test.ts

- bun run typecheck

- git diff --check

- bunx prettier --check tools/hygiene/divergence-reconcile.ts tools/hygiene/divergence-reconcile.test.ts docs/backlog/P1/081KR7JY10008QG0R000MH7PJT-pr-review-disagreement-preservation-protocol.md

Limits:

- Remote CI and review automation still need to rerun on the pushed commit before merge.

Agency-Signature-Version: 1

Agent: Vera

Agent-Runtime: OpenAI Codex desktop heartbeat loop

Agent-Model: GPT-5

Credential-Identity: aaron-codex-desktop

Credential-Mode: shared

Human-Review: none

Human-Review-Evidence: none

Action-Mode: autonomous-fail-open

Task: 081KQJZR90008QG0R002GJAJ19

Co-Authored-By: Codex <noreply@openai.com>
```

### `1936ed90cfbba89af1af7bd84749faa723be7e87` -- 2026-05-31T11:48:46Z -- ``

**Touched files (intersect with thread paths):**

- `tools/hygiene/divergence-reconcile.ts`

**Message:**

```
fix(081KR7JY10008QG0R000MH7PJT): reject symlinked divergence root

Why:

- Codex review flagged one remaining write-back escape: docs/hygiene-history/divergences itself could be a symlink before child/final-file guards run.

- The reconcile action must fail closed when the canonical divergence root is not a real directory.

What:

- Validate the divergence root with lstat before walking ancestors or opening the target shard.

- Reject a symlinked divergence root and non-directory divergence root before write-back.

- Add coverage for a root symlink pointing at an outside divergence directory.

Proof:

- bun test tools/hygiene/divergence-reconcile.test.ts

- bun run typecheck

- git diff --check

- bunx prettier --check tools/hygiene/divergence-reconcile.ts tools/hygiene/divergence-reconcile.test.ts docs/backlog/P1/081KR7JY10008QG0R000MH7PJT-pr-review-disagreement-preservation-protocol.md

Limits:

- Remote CI still needs to rerun after this push; previous semgrep failure was classified as transient external 502.

Agency-Signature-Version: 1

Agent: Vera

Agent-Runtime: OpenAI Codex desktop heartbeat loop

Agent-Model: GPT-5

Credential-Identity: aaron-codex-desktop

Credential-Mode: shared

Human-Review: none

Human-Review-Evidence: none

Action-Mode: autonomous-fail-open

Task: 081KQJZR90008QG0R002GJAJ19

Co-Authored-By: Codex <noreply@openai.com>
```

### `10772baba6c0b51d8fae506d88a0db18a47d2d72` -- 2026-05-31T11:58:21Z -- ``

**Touched files (intersect with thread paths):**

- `tools/hygiene/divergence-reconcile.ts`

**Message:**

```
fix(081KR7JY10008QG0R000MH7PJT): reject symlinked divergence root during scan

Why:
- Copilot flagged that scanDivergenceDir still followed a symlinked docs/hygiene-history/divergences root.
- List mode should fail closed on the same root escape that write-back already rejects.

What:
- Replace the existsSync root precheck with lstat-backed divergence-root validation.
- Share that root validation with the reconcile write-back ancestor guard.
- Add scan coverage for a symlinked divergence root pointing outside the repo tree.

Proof:
- bun test tools/hygiene/divergence-reconcile.test.ts
- bun run typecheck
- git diff --check
- bunx prettier --check tools/hygiene/divergence-reconcile.ts tools/hygiene/divergence-reconcile.test.ts docs/backlog/P1/081KR7JY10008QG0R000MH7PJT-pr-review-disagreement-preservation-protocol.md

Limits:
- Remote CI and automated review need to rerun on this pushed commit before #6206 can merge.

Agency-Signature-Version: 1
Agent: Vera
Agent-Runtime: OpenAI Codex desktop heartbeat loop
Agent-Model: GPT-5
Credential-Identity: aaron-codex-desktop
Credential-Mode: shared
Human-Review: none
Human-Review-Evidence: none
Action-Mode: autonomous-fail-open
Task: 081KQJZR90008QG0R002GJAJ19
Co-Authored-By: Codex <noreply@openai.com>
```

### `2f0e68f7414ec035407e0ef7d4535c3d59450325` -- 2026-05-31T12:04:17Z -- ``

**Touched files (intersect with thread paths):**

- `tools/hygiene/divergence-reconcile.ts`

**Message:**

```
fix(081KR7JY10008QG0R000MH7PJT): fail closed without O_NOFOLLOW

Why:
- The reconcile write-back path relies on O_NOFOLLOW for final-target symlink protection.
- If a platform exposes no usable O_NOFOLLOW flag, continuing would silently remove that guard.

What:
- Add regularShardOpenFlags to reject missing or zero O_NOFOLLOW before opening a shard for write-back.
- Route openRegularShardForReadWrite through the explicit flag builder.
- Add focused coverage for the fail-closed flag path.

Proof:
- bun test tools/hygiene/divergence-reconcile.test.ts
- bun run typecheck
- git diff --check
- bunx prettier --check tools/hygiene/divergence-reconcile.ts tools/hygiene/divergence-reconcile.test.ts docs/backlog/P1/081KR7JY10008QG0R000MH7PJT-pr-review-disagreement-preservation-protocol.md

Limits:
- Remote CI and automated review need to rerun on this pushed commit before #6206 can merge.

Agency-Signature-Version: 1
Agent: Vera
Agent-Runtime: OpenAI Codex desktop heartbeat loop
Agent-Model: GPT-5
Credential-Identity: aaron-codex-desktop
Credential-Mode: shared
Human-Review: none
Human-Review-Evidence: none
Action-Mode: autonomous-fail-open
Task: 081KQJZR90008QG0R002GJAJ19
Co-Authored-By: Codex <noreply@openai.com>
```

### `d9b8b2963c871d73fbe52b08381d6a3e7f05e947` -- 2026-05-31T12:15:50Z -- `claude`

**Touched files (intersect with thread paths):**

- `tools/hygiene/divergence-reconcile.ts`

**Message:**

```
fix(081KR7JY10008QG0R000MH7PJT): reject symlinked ancestors above divergence root

assertRealDivergenceRoot only lstat'd the final root component, but
lstat follows intermediate symlinks — so a symlinked ancestor of
DIVERGENCE_ROOT (docs/ or docs/hygiene-history/) would redirect
write-back outside the tree (CWE-59 link following). Walk every
component from the repo base through the root, lstat-ing each so a
symlink at ANY chain position fails closed. Subsumes the prior
final-component-only root check; adds a regression test mirroring the
existing symlinked-root case at the ancestor-above-root position.

Addresses codex review thread on PR #6206.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```
