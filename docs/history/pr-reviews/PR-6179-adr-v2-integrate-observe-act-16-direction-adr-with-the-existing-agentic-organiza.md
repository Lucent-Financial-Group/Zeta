# PR #6179 -- ADR v2: integrate observe->act/16-direction ADR with the existing Agentic Organization observe.ts keystone

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 6179 |
| Title | ADR v2: integrate observe->act/16-direction ADR with the existing Agentic Organization observe.ts keystone |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-05-31T01:25:33Z |
| Merged at | 2026-05-31T01:33:34Z |
| Merge commit SHA | `49fb6a4aee77d3d3408764e3bcc4db4fa08a91d4` |
| Branch | `otto-cli/adr-v2-integrate-agentic-organization-keystone-2026-05-31` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/6179 |
| Changed files | 1 |
| Additions / deletions | +143 / -20 |

## Description

Per your "look at the agentic organization docs + see how the ADR integrates + update it." I read `agentic-organization/docs/` and the ADR needed a real reframe.

## The key finding
The **`observe.ts` keystone already exists and is designed in depth** — `OBSERVE_COMPOSER_AND_RUN_STATE.md` (code anchor `agentic-organization/packages/application/src/observe.ts`): `observe()` (pure) → memoryless `composer.compose()` → `decide()` (rejects illegal picks), the `RunScope` / `RunLifecyclePhase` / `ObserveResult` / `ComposerSelection` DUs, `DeterministicRule` vetoes, and a **≥3-agent constitution ratification gate**. And the **"Universal Action Grammar" is already a named concept** (`AGENT_WORK_RHYTHM_AND_PROMPT_FLOWS.md`: *"reuse those ideas instead of inventing another unrelated action language ... the Universal Action Grammar becomes the shared action representation inside phases"*).

**v1 of the ADR proposed a parallel observe.ts + action language — that was wrong.** v2 reframes it to **render the existing keystone**, contributing only three things on top:

1. the **fixed 16-slot Xbox-controller rendering** of the keystone's per-`RunScope` legal options (the concrete fixed-slot form of the already-named Universal Action Grammar);
2. **tri-boolean (081KSV2WD0008QG0R00051XS0N) `Tri[16]` per-slot availability** wired to the keystone's `ObserveResult` `readout|feedback` (`Result<T,TFeedback>`) + `DeterministicRule` vetoes (`T`=legal, `F`=vetoed, `N`=held);
3. the **local-USB single-node (no-cloud) deployment** of the same keystone, alongside the cluster runtime — the 16-way constrained decode is exactly what makes a small local model a viable composer without the cluster.

## What changed in the ADR

- Added an **Integration section** mapping every ADR concept onto the existing keystone DUs.
- Replaced "git-only, 128-bit ids" with the precise **git-as-db ZetaId-CRDT G-Set + Cockroach rebuildable index** (`GIT_COCKROACH_SYNC` / `frontmatter-db`).
- Mapped the 16 slots → `RunScope` (Scope slots), `ComposerSelection.select/.hold` (Commit A/B), the constitution gate (escalate R3).
- Rewrote the **codeable first slice** as a thin *renderer + local-selector adapter* over the keystone (no parallel observe/compose/decide/act path; act goes through the keystone's `decide()` → `command-pipeline.ts`).
- Two deployment targets named: **cluster** (k3s/Temporal/Dapr/NATS/Cockroach) vs **local-USB single-node** (sovereign/offline) — same keystone.

Net-new ADR substrate narrowed to: the 16-slot controller rendering + `Tri[16]` availability + the single-node deployment. Status still **PROPOSED** — ready for you + Max.

🤖 Generated with [Claude Code](https://claude.com/claude-code)


## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 3 |
| Resolved threads | 3 |
| Unresolved threads | 0 |
| Total review comments | 3 |
| Total fix commits (touching thread paths) | 3 |

## Review threads

### Thread 1 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F6Iwq`
- **Path**: `docs/DECISIONS/2026-05-31-observe-act-16-direction-universal-action-grammar-local-no-cloud-llm.md`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-31T01:27:12Z on `docs/DECISIONS/2026-05-31-observe-act-16-direction-universal-action-grammar-local-no-cloud-llm.md`:244 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Point the first slice at the real frontmatter-db package**

  In the codeable implementation steps, this path points to `packages/frontmatter-db/`, but a repo-wide path search shows the package only exists under `agentic-organization/packages/frontmatter-db/`; there is no root-level `packages/frontmatter-db`. Because this ADR is in root `docs/DECISIONS` and is meant to guide the next coding slice, following this instruction would either fail path resolution or create a duplicate package outside the existing Agentic Organization keystone it says to reuse.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 2 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F6I1u`
- **Path**: `docs/DECISIONS/2026-05-31-observe-act-16-direction-universal-action-grammar-local-no-cloud-llm.md`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-31T01:27:48Z on `docs/DECISIONS/2026-05-31-observe-act-16-direction-universal-action-grammar-local-no-cloud-llm.md`:70 (association: NONE)

  <pre>
  P1: This mapping overstates what the current keystone exposes. `observe.ts` only returns surviving `readout.options` plus the names of rules applied; vetoed options and their veto reasons are dropped, and if no option survives the result is feedback rather than a per-option readout. A `Tri[16]` renderer cannot distinguish a deterministic-rule-vetoed slot (`F`) from an unmapped/held slot (`N`) without extending the keystone readout or changing this ADR's projection contract.
  </pre>

### Thread 3 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F6I11`
- **Path**: `docs/DECISIONS/2026-05-31-observe-act-16-direction-universal-action-grammar-local-no-cloud-llm.md`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-31T01:27:48Z on `docs/DECISIONS/2026-05-31-observe-act-16-direction-universal-action-grammar-local-no-cloud-llm.md`:74 (association: NONE)

  <pre>
  P1: The v2 integration says the ZetaId-CRDT + Cockroach model supersedes the earlier git-only/128-bit model, but the unchanged Decision/architecture/alternatives sections below still specify “no DB”, “128-bit IDs”, and direct git append writes. That leaves the ADR internally contradictory about the state substrate the first slice should implement.
  </pre>

## Fix commits (touching thread paths)

### `119385ae623203768088d81512472d87fc538d7c` -- 2026-05-31T01:25:20Z -- `claude`

**Touched files (intersect with thread paths):**

- `docs/DECISIONS/2026-05-31-observe-act-16-direction-universal-action-grammar-local-no-cloud-llm.md`

**Message:**

```
ADR v2: integrate the observe->act/16-direction ADR with the existing…

… Agentic Organization observe.ts keystone

Per operator: reviewed agentic-organization/docs/ + updated the ADR to integrate.
Key finding: the observe.ts keystone ALREADY EXISTS + is designed in depth
(OBSERVE_COMPOSER_AND_RUN_STATE.md; packages/application/src/observe.ts), and the
'Universal Action Grammar' is ALREADY a named concept (AGENT_WORK_RHYTHM: 'reuse
those ideas instead of inventing another unrelated action language'). v1 proposed a
parallel observe.ts + action language -- wrong. v2 reframes the ADR to RENDER the
existing keystone, contributing only three things on top:

1. the fixed 16-slot Xbox-controller rendering of the keystone's per-RunScope legal
   options (the concrete fixed-slot form of the already-named Universal Action Grammar);
2. tri-boolean (081KSV2WD0008QG0R00051XS0N) Tri[16] per-slot availability wired to the keystone's
   ObserveResult readout|feedback (Result<T,TFeedback>) + DeterministicRule vetoes;
3. the local-USB single-node (no-cloud) deployment of the same keystone, alongside the
   cluster runtime (16-way constrained-decode makes a small local model a viable composer).

Added the Integration section mapping every ADR concept onto the existing DUs (RunScope /
RunLifecyclePhase / ObserveResult / ComposerSelection / constitution-gate); replaced the
git-only state with the precise git-as-db ZetaId-CRDT G-Set + Cockroach rebuildable index
(GIT_COCKROACH_SYNC / frontmatter-db); rewrote the codeable first slice as a renderer +
local-selector adapter over the keystone (no parallel observe/compose/decide/act path);
updated composes-with + revision history (v2). Net-new ADR substrate narrowed to the
16-slot rendering + Tri[16] + single-node deployment.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
```

### `7887fe1cf3da5ae8c325c7a9908127b14fa3e6c6` -- 2026-05-31T01:27:52Z -- `claude`

**Touched files (intersect with thread paths):**

- `docs/DECISIONS/2026-05-31-observe-act-16-direction-universal-action-grammar-local-no-cloud-llm.md`

**Message:**

```
ADR v2: add self-recursive observe.ts (summoned local small-LLM sub-o…

…bserves = summonable BFT on observe itself)

Operator 2026-05-31: observe.ts can be composed of summons of many local small
LLMs that assemble its own pieces, recursively. Added a section: observe() (pure)
need not be one call -- the readout (phase + legal options at RunScope) can be
composed by summoning small local LLMs per piece + joining (BFT). Recursive scope
decomposition down the RunScope ladder (same 16-slot grammar at every rung);
per-piece summoning (16-way constrained decode keeps each summon tiny+local);
BFT join (>=N small LLMs agree = T/F, disagree = N held) = summonable BFT (081KSV2WD0008QG0R00051XS0N)
over local models instead of compilers; self-recursive (each summon is observe-
shaped, so observe builds observe); decide()/deterministic-rules/>=3-agent gate
bound the whole recursion. No-central-Rehoboam preserved (readout assembled
distributedly, never decreed). OPEN: summon/join protocol, depth budget, caching.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
```

### `04d9ef5fadda218fe139c0c94139e4334550b8ce` -- 2026-05-31T01:31:38Z -- `claude`

**Touched files (intersect with thread paths):**

- `docs/DECISIONS/2026-05-31-observe-act-16-direction-universal-action-grammar-local-no-cloud-llm.md`

**Message:**

```
fix(#6179): correct agentic-org package paths + Tri[16] overstatement…

… + state-model contradiction (Codex P2 + Copilot P1 x2)

- Package paths: packages/{application,governance,frontmatter-db} -> prefixed
  agentic-organization/packages/... (the packages live under agentic-organization/,
  not root) (Codex P2).
- Tri[16] mapping overstated: observe() returns only surviving readout.options +
  rule NAMES (vetoed options + reasons dropped; zero survivors -> feedback). A
  Tri[16] renderer can mark F but cannot yet distinguish vetoed-with-reason from
  unmapped -> flagged [OPEN/limitation] needing a small keystone enhancement
  (readout lists vetoed options + reasons) (Copilot P1).
- State-model contradiction: Decision #4 / layering / alternatives still said
  'no DB' / 'the only state store', contradicting the v2 git-as-db+Cockroach-index
  model. Reconciled: git-as-db canonical (ZetaId-CRDT G-Set) + Cockroach as
  rebuildable INDEX (not a separate source of truth); the rejected alternative is
  DB-as-canonical, NOT the derived index (Copilot P1).

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
```
