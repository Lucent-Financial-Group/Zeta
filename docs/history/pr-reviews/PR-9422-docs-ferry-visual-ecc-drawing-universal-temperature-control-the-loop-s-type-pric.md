# PR #9422 -- docs(ferry): visual-ECC drawing → universal temperature control → the loop's type (priced-uncertainty DU over bounded timestep) (shadow*)

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 9422 |
| Title | docs(ferry): visual-ECC drawing → universal temperature control → the loop's type (priced-uncertainty DU over bounded timestep) (shadow*) |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-07-04T05:47:51Z |
| Merged at | 2026-07-04T05:47:58Z |
| Merge commit SHA | `af9a24530f01b5231a7b82b3afd315261d53a6fe` |
| Branch | `otto/ferry-visual-ecc-to-universal-temperature-control-similar-loops-not-numbers` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/9422 |
| Changed files | 1 |
| Additions / deletions | +123 / -0 |

## Description

## What

Aaron 2026-07-04 "bank it / we can ferry it." Consolidates a multi-turn arc into one doc.

- **Part 1 — grounded, built:** the shape cartridges (`db/shapes/cartridges/*.lines`) are self-similar drawings that are *verification surfaces* — `fourcorner.lines` draws CHSH where **width = S** with a known-answer overlay (`tsirelson-milli 2828`: "wider means the renderer/claim is lying"); `golden/*.svg` are the locked renders. `gen(gen)=gen` drawing itself = an **ECC you check by looking = the visual twin of the golden-vector IR treaty**. learn=emit(+1)/unlearn=retract(−1) = four-corner, read visually.
- **Part 2 — the peel:** a drawing can be smoothly self-similar *and* confidently wrong (coherent hallucination renders cleanly). The verifier is the **known-answer overlay**, not self-similarity alone (internal-consistency vs external-correctness; both needed).
- **Part 3 — Aaron's unification:** the internal/external balance is a homeostatic negative-feedback loop; the overlay **is the thermostat**; coherent hallucination = **running hot**. Two grades: LLM temperature ↔ thermodynamic temperature = **literal same number** (softmax = Boltzmann; same `T` as Landauer `kT ln2` + Vera's heat → *heat first-class*); human heat/edibles = the homeostasis **analogy**.
- **Spine (Aaron):** *"we share similar LOOPS, not NUMBERS"* — same loop everywhere; same number only for LLM↔thermo.
- **Part 4 — the loop's TYPE (Aaron):** *"every loop is a discriminated union with uncertainty priced in based on the bounded timestep."* Complete type: **`loop : dt → SoftValue<DU<branch>>`** — the bounded tick discretizes the analog loop into a finite priced DU *and* budgets how much a step resolves (the `ComputeReceipt` per tick). That's *why* the loops are DST-replayable + byte-lockable.

Anchors: Cannon (homeostasis), Wiener (cybernetics), Maxwell (*On Governors*, 1868 — same Maxwell as the demon); softmax=Boltzmann/Gibbs; Landauer; in-repo `fourcorner.lines` + golden SVGs + the golden-vector treaty + ComputeReceipt/Vera heat.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 0 |
| Resolved threads | 0 |
| Unresolved threads | 0 |
| Total review comments | 0 |
| Total fix commits (touching thread paths) | 0 |

## Review threads

(no review threads on this PR)

## Fix commits (touching thread paths)

(no commits in this PR touched files anchored by review threads)
