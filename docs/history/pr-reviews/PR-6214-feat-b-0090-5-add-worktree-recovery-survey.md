# PR #6214 -- feat(081KDVJT3E008QG0R000SCFYN5): add worktree recovery survey

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 6214 |
| Title | feat(081KDVJT3E008QG0R000SCFYN5): add worktree recovery survey |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-05-31T14:40:08Z |
| Merged at | 2026-05-31T16:10:55Z |
| Merge commit SHA | `b3e7f21e3b559e02aad05ddfbefd4c4bc22d88fa` |
| Branch | `claim/backlog-0090-5-worktree-survey-codex-20260531` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/6214 |
| Changed files | 2 |
| Additions / deletions | +1321 / -0 |

## Description

## Summary

- Adds `tools/hygiene/audit-worktree-survey.ts` for 081KDVJT3E008QG0R000SCFYN5.
- Classifies `git worktree list --porcelain` entries into `ALREADY-COVERED`, `NEEDS-RECOVERY`, and `OBSOLETE`.
- Supports markdown output, `--json`, `--root`, and `--report`.
- Adds focused Bun tests for args, parsing, classification, and markdown rendering.

## Coordination

- Claim branch was pushed first as `claim/backlog-0090-5-worktree-survey-codex-20260531`.
- Claim file is released in this PR branch.
- `docs/BACKLOG.md` and the 081KDVJT3E008QG0R000SCFYN5 row are intentionally untouched because active claim refs still own the generated-index path.

## Checks

- `bun test tools/hygiene/audit-worktree-survey.test.ts`
- `git diff --check`
- `bun tools/hygiene/audit-worktree-survey.ts --json`
- `bunx prettier --check tools/hygiene/audit-worktree-survey.ts tools/hygiene/audit-worktree-survey.test.ts`

## Typecheck Note

- `bun run typecheck` could not run in this worktree because `tsc` is not installed.
- `bunx tsc --noEmit` could not find the configured `@types/bun` package.

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | true |
| Total threads | 28 |
| Resolved threads | 28 |
| Unresolved threads | 0 |
| Total review comments | 43 |
| Total fix commits (touching thread paths) | 17 |

## Review threads

### Thread 1 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F8k2M`
- **Path**: `tools/hygiene/audit-worktree-survey.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-31T14:42:31Z on `tools/hygiene/audit-worktree-survey.ts`:279 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Compare content before declaring recovery needed**

  When a clean worktree was squash-merged, cherry-picked, or forward-synced, its HEAD is not an ancestor of `origin/main` even though the substrate exists on main—the ALREADY-COVERED case 081KQ8P5D0008QG0R0002TN22C explicitly includes as a different commit path. This ancestry-only check marks those worktrees as NEEDS-RECOVERY, inflating the recovery queue and obscuring genuinely unique work; base coverage on content/tree/patch equivalence or report it as unknown before assigning the recovery bucket.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 2 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F8k85`
- **Path**: `tools/hygiene/audit-worktree-survey.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-31T14:43:00Z on `tools/hygiene/audit-worktree-survey.ts`:230 (association: NONE)

  <pre>
  P1: Markdown table rows can break if `path` or `reason` contains `|` (or if `reason` contains newlines from git stderr). Escaping these values before interpolating into the table keeps the report valid markdown.
  </pre>

### Thread 3 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F8k88`
- **Path**: `tools/hygiene/audit-worktree-survey.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-31T14:43:00Z on `tools/hygiene/audit-worktree-survey.ts`:332 (association: NONE)

  <pre>
  P2: When writing `--report`, the markdown output can be written without a trailing newline, while `--json` always includes one. Ensuring a trailing newline for report files avoids POSIX "no newline at end of file" churn in generated artifacts.
  </pre>

### Thread 4 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F8nNy`
- **Path**: `tools/hygiene/audit-worktree-survey.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-31T14:51:23Z on `tools/hygiene/audit-worktree-survey.ts`:140 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Filter the survey to locked worktrees**

  For the 081KQ8P5D0008QG0R0002TN22C/081KDVJT3E008QG0R000SCFYN5 worktree surface, only locked/stale worktrees are in scope; `git worktree list --porcelain` marks those with a `locked ...` record, but this parser only records HEAD/branch/prunable and the resulting list is classified wholesale. On shared machines this reports the main checkout or ordinary active feature worktrees—especially dirty in-progress ones—as `NEEDS-RECOVERY` even though they are not lost substrate. Record the locked annotation and exclude non-locked entries from the recovery survey.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 5 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F8nN0`
- **Path**: `tools/hygiene/audit-worktree-survey.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-31T14:51:23Z on `tools/hygiene/audit-worktree-survey.ts`:107 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Accept the documented dry-run flag**

  081KDVJT3E008QG0R000SCFYN5's focused check explicitly runs this tool with `--dry`, but the parser treats any unrecognized option as an argument error. In the documented validation path, `bun tools/hygiene/audit-worktree-survey.ts --dry` exits 64 before producing a survey, so the backlog item cannot satisfy its own acceptance check unless `--dry` is accepted as a no-op or the contract is updated separately.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 6 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F8nsT`
- **Path**: `tools/hygiene/audit-worktree-survey.ts`
- **Replies**: 0

**Initial comment:**

- **`github-advanced-security[bot]` (bot)** at 2026-05-31T14:53:16Z on `tools/hygiene/audit-worktree-survey.ts`:214 (association: NONE)

  <pre>
  ## CodeQL / Incomplete string escaping or encoding

  This does not escape backslash characters in the input.

  [Show more details](https://github.com/Lucent-Financial-Group/Zeta/security/code-scanning/125)
  </pre>

### Thread 7 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F8nsY`
- **Path**: `tools/hygiene/audit-worktree-survey.ts`
- **Replies**: 0

**Initial comment:**

- **`github-advanced-security[bot]` (bot)** at 2026-05-31T14:53:16Z on `tools/hygiene/audit-worktree-survey.ts`:218 (association: NONE)

  <pre>
  ## CodeQL / Incomplete string escaping or encoding

  This does not escape backslash characters in the input.

  [Show more details](https://github.com/Lucent-Financial-Group/Zeta/security/code-scanning/126)
  </pre>

### Thread 8 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F8nyy`
- **Path**: `tools/hygiene/audit-worktree-survey.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-31T14:53:41Z on `tools/hygiene/audit-worktree-survey.ts`:270 (association: NONE)

  <pre>
  `renderInlineCode()` attempts to escape backticks by inserting a backslash, but backslashes are treated as literal characters inside Markdown code spans; a value containing a backtick will still terminate the code span and break the table rendering. Use a code-span fence that is longer than any backtick run in the value, and reuse it for `renderBranch()` as well so both path/branch fields are safe.
  </pre>

### Thread 9 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F8pfw`
- **Path**: `tools/hygiene/audit-worktree-survey.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-31T15:00:08Z on `tools/hygiene/audit-worktree-survey.ts`:352 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Compare trees before trusting git-cherry coverage**

  For clean locked worktrees that were squash-merged from multiple commits, `git cherry origin/main &lt;head&gt;` still prints `+` for each original commit because `git cherry` checks per-commit patch equivalence (confirmed against the git-cherry docs: https://www.kernel.org/pub/software/scm/git/docs/git-cherry.html), even when the worktree tree is identical to `origin/main`. Fresh evidence after the earlier coverage comment is that the new `patchEquivalentToMain` check relies only on this per-commit output, so a fully covered squashed branch is still reported as `NEEDS-RECOVERY`; add a tree/content diff check before assigning the recovery bucket.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 10 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F8pqX`
- **Path**: `tools/hygiene/audit-worktree-survey.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-31T15:00:45Z on `tools/hygiene/audit-worktree-survey.ts`:263 (association: NONE)

  <pre>
  `makeSurvey` filters to only `locked || prunable` entries, but the markdown summary labels this as "Total worktrees", which is misleading (it’s not the full `git worktree list` total). Rename the label to reflect the scoped set so readers don’t misinterpret the counts.
  </pre>

### Thread 11 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F8pqa`
- **Path**: `tools/hygiene/audit-worktree-survey.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-31T15:00:46Z on `tools/hygiene/audit-worktree-survey.ts`:296 (association: NONE)

  <pre>
  The `Root:` line uses a raw backtick wrapper (``Root: `${survey.root}```), which will produce broken markdown if the root path contains backticks or leading/trailing spaces. Use the existing `renderInlineCode` helper here so markdown remains valid for edge-case paths.
  </pre>

### Thread 12 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F8tCp`
- **Path**: `tools/hygiene/audit-worktree-survey.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-31T15:07:31Z on `tools/hygiene/audit-worktree-survey.ts`:368 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Compare branch changes instead of whole trees**

  Fresh evidence in this revision is that the new coverage check uses `git diff --quiet origin/main &lt;head&gt;`, whose `git diff -h` form compares the two commit trees, so a stale locked worktree whose changes were squash/forward-synced to main is still marked `NEEDS-RECOVERY` as soon as `origin/main` has any later unrelated commit that the worktree lacks. In that scenario the substrate is already covered, but this whole-tree comparison returns different before the per-commit cherry check also misses the squash; compare the branch delta against its base/merge-base or otherwise test whether the worktree's changes are contained in main.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 13 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F8xFA`
- **Path**: `tools/hygiene/audit-worktree-survey.ts`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-31T15:15:09Z on `tools/hygiene/audit-worktree-survey.ts`:145 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Don't mark prunable worktrees obsolete before checking HEAD**

  When a linked worktree directory was deleted manually, `git worktree list --porcelain` marks the entry `prunable` but still reports its `HEAD` and `branch`; if that branch has unique committed substrate, this early return classifies it as `OBSOLETE` without any coverage check. 081KQ8P5D0008QG0R0002TN22C's OBSOLETE bucket requires proof that the substrate was retired or superseded, so this hides recoverable committed work from the recovery queue simply because the checkout path is gone.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-31T15:24:16Z on `tools/hygiene/audit-worktree-survey.ts`:145 (association: MEMBER)

    <pre>
    Addressed in `60b21a11a` (fix(081KDVJT3E008QG0R000SCFYN5): protect prunable worktree commits). `classify` now runs the coverage checks first and only buckets a missing prunable worktree as OBSOLETE when no HEAD is recorded; a prunable+missing worktree with a recorded HEAD that is not known covered by origin/main now goes to NEEDS-RECOVERY. `inspectWorktreeEntry` takes a `fallbackGitContext` (= survey root / cwd) so HEAD coverage (merge-base / merge-tree / cherry) is computed against the shared object store even when the checkout directory is gone. Verified: 22/22 focused tests pass (incl. new missing-prunable-with/without-recoverable-substrate cases), tool `--dry` smoke exits 0. Resolving.
    </pre>

### Thread 14 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F8yxl`
- **Path**: `tools/hygiene/audit-worktree-survey.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-31T15:21:49Z on `tools/hygiene/audit-worktree-survey.ts`:347 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Compare against the covered delta before recovery**

  Fresh evidence after the earlier coverage comments is that `merge-tree` still compares the stale head against today's `origin/main`: `git merge-tree -h` describes `--write-tree` as doing a real merge of `&lt;branch1&gt; &lt;branch2&gt;`. When a multi-commit worktree was squash-merged and main later edits the same files, this command can conflict or produce a different tree while `git cherry` still prints `+` for the original commits, so already-covered/superseded substrate falls through to `NEEDS-RECOVERY`; compare the branch delta to its merge-base or historical main coverage before assigning the recovery bucket.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 15 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F8zso`
- **Path**: `tools/hygiene/audit-worktree-survey.ts`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-31T15:26:00Z on `tools/hygiene/audit-worktree-survey.ts`:366 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Detect branch deltas contained in larger squash commits**

  When a locked worktree's change was squash-merged together with any unrelated edit in the same main commit, this exact patch-id comparison misses that the worktree substrate is already contained: `git diff -h` confirms `git diff &lt;commit&gt; &lt;commit&gt;` builds the whole base-to-commit patch, and `git patch-id -h` only hashes that whole patch, so `base..head` (`A`) will not equal the historical `base..mainCommit` (`A+B`). In that common squash-batch scenario `treeEquivalentToMain` is false and `git cherry` also prints `+`, causing an already-covered worktree to be reported as `NEEDS-RECOVERY`; compare containment of the branch delta rather than requiring one first-parent commit's entire accumulated diff to match exactly.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-31T15:31:29Z on `tools/hygiene/audit-worktree-survey.ts`:366 (association: MEMBER)

    <pre>
    Addressed in `88fe9fa80` (fix(081KDVJT3E008QG0R000SCFYN5): detect contained branch deltas). The historical coverage check now validates whether the branch patch can be reverse-applied to each first-parent `origin/main` commit through a temporary index, so a feature delta batched into a larger squash commit is treated as covered without mutating any worktree. Added a regression for a feature change squash-merged alongside unrelated work. Focused checks passed: 23/23 Bun tests, `git diff --check`, Prettier check, dry-run JSON smoke, and `bun run typecheck` after lockfile install.
    </pre>

### Thread 16 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F803S`
- **Path**: `tools/hygiene/audit-worktree-survey.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-31T15:29:55Z on `tools/hygiene/audit-worktree-survey.ts`:343 (association: NONE)

  <pre>
  `gitStdout()` runs `git diff` and captures stdout, but it doesn't set `maxBuffer`. Node/Bun default buffers are small (~1 MiB), so moderately large diffs will raise a spawnSync buffer error and silently collapse these checks to `null` (making coverage classification degrade to "unknown"). Other hygiene tools in this repo raise `maxBuffer` to avoid ENOBUFS / maxBuffer failures (e.g. tools/hygiene/audit-md032-plus-linestart.ts:94-106).
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-31T15:36:31Z on `tools/hygiene/audit-worktree-survey.ts`:343 (association: MEMBER)

    <pre>
    Addressed in `0878c14e5` (fix(081KDVJT3E008QG0R000SCFYN5): raise survey git output buffers). `gitStdout()` now uses an explicit 64 MiB `maxBuffer`, covering large `git diff`, `patch-id`, and `rev-list` output paths; the temporary-index helper, status, merge-base, and worktree-list paths use the same buffer. Added large-diff regressions that exceed the default spawn buffer and still classify covered branch deltas correctly. Focused checks passed: 25/25 Bun tests, `git diff --check`, Prettier check, dry-run JSON smoke, and `bun run typecheck`.
    </pre>

### Thread 17 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F803e`
- **Path**: `tools/hygiene/audit-worktree-survey.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-31T15:29:56Z on `tools/hygiene/audit-worktree-survey.ts`:424 (association: NONE)

  <pre>
  The `git cherry origin/main &lt;head&gt;` call can emit one line per commit and can exceed the default spawnSync output buffer on long-lived branches. When that happens, `patchEquivalentToMain` stays `null` without any explicit error signal. Setting a larger `maxBuffer` prevents false "unknown" classifications in large repos.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-31T15:36:32Z on `tools/hygiene/audit-worktree-survey.ts`:424 (association: MEMBER)

    <pre>
    Addressed in `0878c14e5` (fix(081KDVJT3E008QG0R000SCFYN5): raise survey git output buffers). The `git cherry origin/main &lt;head&gt;` path now goes through `gitStdout()`, so it gets the same explicit 64 MiB `maxBuffer` instead of Node/Bun defaults. Focused checks passed: 25/25 Bun tests, `git diff --check`, Prettier check, dry-run JSON smoke, and `bun run typecheck`.
    </pre>

### Thread 18 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F82ja`
- **Path**: `tools/hygiene/audit-worktree-survey.ts`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-31T15:34:56Z on `tools/hygiene/audit-worktree-survey.ts`:403 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Don't treat reverted historical deltas as covered**

  When a locked worktree's branch delta appeared on `origin/main` in an earlier first-parent commit but was later reverted or otherwise removed before current `origin/main`, this loop still returns `true` as soon as it sees the old containing commit. That makes `classify` report `ALREADY-COVERED` even though the substrate no longer exists on main, so the recovery survey can tell operators to discard the worktree that is now the only remaining copy; require the current main tip to still contain the delta, or bucket historical-but-removed cases separately/with explicit retirement evidence.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-31T15:41:36Z on `tools/hygiene/audit-worktree-survey.ts`:403 (association: MEMBER)

    <pre>
    Addressed in `60d0a5c8c` (fix(081KDVJT3E008QG0R000SCFYN5): reject reverted historical deltas). Historical first-parent containment now only counts as ALREADY-COVERED when the current `origin/main` tip still differs from the merge-base on every branch-changed path; reverted historical coverage therefore remains recoverable. The same current-tip guard prevents `git cherry` from marking reverted historical patches as patch-equivalent. Added a regression for a branch delta that was squash-covered and then reverted. Focused checks passed: 26/26 Bun tests, `git diff --check`, Prettier check, dry-run JSON smoke, and `bun run typecheck`.
    </pre>

### Thread 19 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F84G0`
- **Path**: `tools/hygiene/audit-worktree-survey.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-31T15:40:25Z on `tools/hygiene/audit-worktree-survey.ts`:493 (association: NONE)

  <pre>
  `branchDeltaCoveredByMainHistory()` compares the branch’s patch-id against `stableDiffPatchId(path, base, commit)` for each commit on `origin/main`. For commits after the first one, `git diff base..commit` is a cumulative delta, so this patch-id will almost never match a single “historical delta” commit and can miss true coverage when the later `git apply --reverse --check` path can’t validate (e.g., rename/context mismatches). Computing patch-id for the commit’s own first-parent delta aligns with the intent and is substantially cheaper than diffing from `base` each time.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-31T15:45:10Z on `tools/hygiene/audit-worktree-survey.ts`:493 (association: MEMBER)

    <pre>
    Addressed in `d37869f19` (fix(081KDVJT3E008QG0R000SCFYN5): compare first-parent patch ids). Historical exact patch-id matching now computes each candidate main commit patch-id from its own first-parent delta instead of cumulative `base..commit`; the reverse-apply containment path remains as the broader fallback. Added a regression for an exact later squash commit after unrelated main work. Focused checks passed: 27/27 Bun tests, `git diff --check`, Prettier check, dry-run JSON smoke, and `bun run typecheck`.
    </pre>

### Thread 20 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F85-f`
- **Path**: `tools/hygiene/audit-worktree-survey.ts`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-31T15:44:23Z on `tools/hygiene/audit-worktree-survey.ts`:393 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Include file modes when checking retained paths**

  When a locked worktree's only substrate is a mode/type change (for example making a script executable) and that change was already squash-merged to `origin/main`, this check still returns false because `treeObjectId` parses only the blob id from `git ls-tree` and ignores the leading mode/type fields; `git ls-tree -h` confirms object-only is a distinct option, so the default record carries more than the object id. That makes `branchDeltaCoveredByMainHistory` skip the historical patch check and the survey reports an already-covered mode-only worktree as `NEEDS-RECOVERY`.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-31T15:49:03Z on `tools/hygiene/audit-worktree-survey.ts`:393 (association: MEMBER)

    <pre>
    Addressed in `869a670eb`: retained-path comparison now uses full `git ls-tree` entry identity, including mode/type/object, and includes a mode-only executable-bit regression test. Focused checks passed: `bun test tools/hygiene/audit-worktree-survey.test.ts`, `git diff --check`, `bunx prettier --check tools/hygiene/audit-worktree-survey.ts tools/hygiene/audit-worktree-survey.test.ts`, `bun tools/hygiene/audit-worktree-survey.ts --dry --json`, `bun run typecheck`.
    </pre>

### Thread 21 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F863X`
- **Path**: `tools/hygiene/audit-worktree-survey.ts`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-31T15:47:08Z on `tools/hygiene/audit-worktree-survey.ts`:399 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Require current main to retain historical deltas**

  Fresh evidence after the addressed reverted case is that this retention guard only checks whether each changed path differs from the merge-base object, not whether it still matches the worktree's delta. If a locked worktree changed `tracked.txt` from A→B, main once contained B, and a later main commit overwrote it to C, this returns true (A != C) and the later history scan can report `ALREADY-COVERED` from the old B commit even though current `origin/main` no longer contains the substrate; compare the tip against the branch result or otherwise ensure the current tip still contains the historical delta before allowing the covered bucket.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-31T15:55:49Z on `tools/hygiene/audit-worktree-survey.ts`:399 (association: MEMBER)

    <pre>
    Addressed in `7ecae463f`: the current-tip guard now checks whether `origin/main` contains the branch delta itself by reverse-applying the branch patch, rather than only checking that changed paths differ from the merge-base. This prevents old historical B commits from marking A-&gt;B work covered after current main overwrites it to C. Added an overwritten-result regression. Focused checks passed: 30/30 Bun tests, `git diff --check`, Prettier check, dry-run JSON smoke, and `bun run typecheck`.
    </pre>

### Thread 22 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F87LW`
- **Path**: `tools/hygiene/audit-worktree-survey.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-31T15:48:34Z on `tools/hygiene/audit-worktree-survey.ts`:397 (association: NONE)

  <pre>
  `branchChangedPathsRetainedAtMainTip()` reads as if it returns true when origin/main *retains* the base version of the changed paths, but the predicate currently returns true when `treeObjectId(base) !== treeObjectId(origin/main)` (i.e., origin/main differs from the merge-base for every changed path). This name/behavior mismatch makes the later early-return logic (`if (mainTipRetainsBranchPaths !== true) return ...`) hard to reason about and easy to mis-maintain.

  Consider renaming the helper to reflect what it actually checks (e.g. `mainTipDiffersFromBaseOnChangedPaths`) or invert the predicate to match the current name, updating the call sites accordingly.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-31T15:55:50Z on `tools/hygiene/audit-worktree-survey.ts`:397 (association: MEMBER)

    <pre>
    Addressed in `7ecae463f`: removed `branchChangedPathsRetainedAtMainTip` and replaced it with explicit branch-delta containment helpers (`branchPatch`, `commitContainsPatch`, `commitContainsBranchDelta`). The old name/behavior mismatch is gone. Focused checks passed: 30/30 Bun tests, `git diff --check`, Prettier check, dry-run JSON smoke, and `bun run typecheck`.
    </pre>

### Thread 23 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F873R`
- **Path**: `tools/hygiene/audit-worktree-survey.ts`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-31T15:51:19Z on `tools/hygiene/audit-worktree-survey.ts`:410 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Detect contained hunks despite adjacent squash edits**

  When a locked worktree's change is squash-merged in the same commit as an adjacent edit to the same file, this reverse-apply probe can return false because the `base..head` patch context no longer matches the squash commit, even though the branch payload is present. Fresh evidence beyond the earlier larger-squash thread: a base file `a/b/c`, branch change `b→B`, and main squash `b→B` plus adjacent `c→C` makes `git apply --reverse --check` fail and the survey falls through to `NEEDS-RECOVERY`, inflating the recovery queue for already-covered substrate.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-31T15:55:51Z on `tools/hygiene/audit-worktree-survey.ts`:410 (association: MEMBER)

    <pre>
    Addressed in `7ecae463f`: branch-delta containment now retries reverse-apply with a zero-context patch when full-context apply fails, so adjacent squash edits that change nearby context still classify as covered when the branch hunk is present. Added the `a/b/c` -&gt; `a/B/C` regression. Focused checks passed: 30/30 Bun tests, `git diff --check`, Prettier check, dry-run JSON smoke, and `bun run typecheck`.
    </pre>

### Thread 24 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F89Tl`
- **Path**: `tools/hygiene/audit-worktree-survey.ts`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-31T15:57:32Z on `tools/hygiene/audit-worktree-survey.ts`:464 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Require context before marking deltas covered**

  When the branch change's replacement text appears elsewhere on current `origin/main`, this zero-context fallback can make an uncovered worktree look covered: e.g. base `a/b/c`, branch changes `b→B`, and main only appends an unrelated `B` while leaving `b` intact. `git apply -h` describes `--unidiff-zero` as not expecting context, and the reverse check can match that unrelated `B`, so `treeEquivalentToMain` becomes true and the survey reports `ALREADY-COVERED` even though the worktree is still the only copy of the actual edit.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-31T16:00:41Z on `tools/hygiene/audit-worktree-survey.ts`:464 (association: MEMBER)

    <pre>
    Addressed in `55e217b5b`: the zero-context fallback now first verifies that the branch-added lines are present at the expected target location in the candidate commit before accepting the reverse-apply probe. Added the reviewer case where current main appends an unrelated `B` while leaving the original `b` intact; it now remains recoverable instead of `ALREADY-COVERED`. Focused checks passed: 31/31 Bun tests, `git diff --check`, Prettier check, dry-run JSON smoke, and `bun run typecheck`.
    </pre>

### Thread 25 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F89_D`
- **Path**: `tools/hygiene/audit-worktree-survey.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-31T16:00:03Z on `tools/hygiene/audit-worktree-survey.ts`:122 (association: NONE)

  <pre>
  `git worktree list --porcelain` output can be CRLF (`\r\n`) on Windows. Splitting on `"\n\n"` and checking `line === "locked"` / `"prunable"` will fail when the output contains `\r`, causing the survey to miss entries or misclassify them.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-31T16:08:14Z on `tools/hygiene/audit-worktree-survey.ts`:122 (association: MEMBER)

    <pre>
    Addressed in `d857d83f4`: `parseWorktreePorcelain` now normalizes CRLF and lone CR to LF before block/line parsing, so `locked` and `prunable` markers are recognized consistently across Windows-style porcelain output. Focused checks passed: Bun survey tests, diff check, Prettier check, dry-run JSON smoke, and typecheck.
    </pre>

### Thread 26 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F89_H`
- **Path**: `tools/hygiene/audit-worktree-survey.test.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-31T16:00:03Z on `tools/hygiene/audit-worktree-survey.test.ts`:117 (association: NONE)

  <pre>
  This test only exercises LF-separated porcelain output. Switching to CRLF makes the regression for Windows line endings explicit (and will fail without newline normalization in `parseWorktreePorcelain`).
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-31T16:08:15Z on `tools/hygiene/audit-worktree-survey.test.ts`:117 (association: MEMBER)

    <pre>
    Addressed in `d857d83f4`: the porcelain parser test now feeds CRLF-separated output, making the Windows newline regression explicit. Focused checks passed: Bun survey tests, diff check, Prettier check, dry-run JSON smoke, and typecheck.
    </pre>

### Thread 27 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F89_I`
- **Path**: `tools/hygiene/audit-worktree-survey.test.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-31T16:00:03Z on `tools/hygiene/audit-worktree-survey.test.ts`:47 (association: NONE)

  <pre>
  `eslint-plugin-sonarjs` flags `spawnSync("git", ...)` with `sonarjs/no-os-command-from-path`. Other tooling/tests in this repo suppress it with an inline disable comment; without it, `eslint .` will fail CI for this new test file.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-31T16:08:16Z on `tools/hygiene/audit-worktree-survey.test.ts`:47 (association: MEMBER)

    <pre>
    Addressed in `d857d83f4`: added the inline `sonarjs/no-os-command-from-path` suppression on the test helper `spawnSync("git", ...)`. Targeted ESLint no longer reports that rule for the helper; full repo ESLint is still blocked by unrelated pre-existing parser/rule debt outside this change.
    </pre>

### Thread 28 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F8-4u`
- **Path**: `tools/hygiene/audit-worktree-survey.ts`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-31T16:02:29Z on `tools/hygiene/audit-worktree-survey.ts`:430 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Handle deletion-only deltas in zero-context fallback**

  When a locked worktree's only payload deletes lines and that deletion was squash-merged together with an adjacent edit, the full-context reverse-apply check can fail because the surrounding context changed, but the zero-context reverse apply would still prove the deletion is present. This guard returns false before trying it because deletion-only hunks have no `+` lines, so already-covered deletion-only worktrees fall through to `NEEDS-RECOVERY` instead of `ALREADY-COVERED`.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-31T16:08:17Z on `tools/hygiene/audit-worktree-survey.ts`:430 (association: MEMBER)

    <pre>
    Addressed in `d857d83f4`: the zero-context fallback now handles deletion-only hunks by conservatively checking that deleted lines are not still present at the expected local deletion window before accepting the reverse-apply probe. Added positive coverage for deletion-only squash plus adjacent edit and a negative regression where the deleted line remains. Focused checks passed: Bun survey tests, diff check, Prettier check, dry-run JSON smoke, and typecheck.
    </pre>

## Fix commits (touching thread paths)

### `342d9c1c30ac465e53eb8030514e752ed168b83e` -- 2026-05-31T14:37:10Z -- ``

**Touched files (intersect with thread paths):**

- `tools/hygiene/audit-worktree-survey.test.ts`
- `tools/hygiene/audit-worktree-survey.ts`

**Message:**

```
feat(081KDVJT3E008QG0R000SCFYN5): add worktree recovery survey

Why:
- 081KDVJT3E008QG0R000SCFYN5 needs a TypeScript survey pattern before the orphan-branch survey can land.
- Worktree remnants are a known lost-substrate surface, so the tool should classify entries instead of only listing them.

Scope:
- Add a git worktree porcelain survey with markdown and JSON output.
- Classify worktrees into ALREADY-COVERED, NEEDS-RECOVERY, and OBSOLETE buckets.
- Cover argument parsing, porcelain parsing, bucket decisions, and markdown rendering with focused Bun tests.

Checks:
- bun test tools/hygiene/audit-worktree-survey.test.ts
- git diff --check
- bun tools/hygiene/audit-worktree-survey.ts --json
- bunx prettier --check tools/hygiene/audit-worktree-survey.ts tools/hygiene/audit-worktree-survey.test.ts

Notes:
- bun run typecheck could not run in this worktree because tsc is not installed.
- bunx tsc --noEmit could not find the configured @types/bun package.

Agency-Signature-Version: 1
Agent: Vera
Agent-Runtime: OpenAI Codex desktop heartbeat loop
Agent-Model: GPT-5
Credential-Identity: aaron-codex-desktop
Credential-Mode: shared
Human-Review: not-requested
Action-Mode: autonomous-fail-open
Task: 081KDVJT3E008QG0R000SCFYN5
Co-Authored-By: Codex <noreply@openai.com>
```

### `04d9be8fb68ca3e99485de359ce20399ac6251ea` -- 2026-05-31T14:48:42Z -- ``

**Touched files (intersect with thread paths):**

- `tools/hygiene/audit-worktree-survey.test.ts`
- `tools/hygiene/audit-worktree-survey.ts`

**Message:**

```
fix(081KDVJT3E008QG0R000SCFYN5): handle patch-covered worktrees

Address PR review feedback for the worktree recovery survey.

The survey now treats clean worktrees whose patches are already present on origin/main as ALREADY-COVERED, escapes markdown table cells, and writes report files with a trailing newline.

Focused checks:

- bun test tools/hygiene/audit-worktree-survey.test.ts

- git diff --check

- bunx prettier --check tools/hygiene/audit-worktree-survey.ts tools/hygiene/audit-worktree-survey.test.ts

- bun tools/hygiene/audit-worktree-survey.ts --json

Agency-Signature-Version: 1
Agent: Vera
Agent-Runtime: OpenAI Codex desktop heartbeat loop
Agent-Model: GPT-5
Credential-Identity: aaron-codex-desktop
Credential-Mode: shared
Human-Review: not-requested
Action-Mode: autonomous-fail-open
Task: 081KDVJT3E008QG0R000SCFYN5
Co-Authored-By: Codex <noreply@openai.com>
```

### `44dd1d13aab36db973544343a5d8f6268e35c11a` -- 2026-05-31T14:50:07Z -- ``

**Touched files (intersect with thread paths):**

- `tools/hygiene/audit-worktree-survey.test.ts`
- `tools/hygiene/audit-worktree-survey.ts`

**Message:**

```
test(081KDVJT3E008QG0R000SCFYN5): cover survey output newline

Keep the report output newline behavior behind a small formatter helper and cover both markdown and JSON output paths.

Focused checks:

- bun test tools/hygiene/audit-worktree-survey.test.ts

- git diff --check

- bunx prettier --check tools/hygiene/audit-worktree-survey.ts tools/hygiene/audit-worktree-survey.test.ts

Agency-Signature-Version: 1
Agent: Vera
Agent-Runtime: OpenAI Codex desktop heartbeat loop
Agent-Model: GPT-5
Credential-Identity: aaron-codex-desktop
Credential-Mode: shared
Human-Review: not-requested
Action-Mode: autonomous-fail-open
Task: 081KDVJT3E008QG0R000SCFYN5
Co-Authored-By: Codex <noreply@openai.com>
```

### `a4ee1495c5a48e71c8ba02663f05b3b8ef524d04` -- 2026-05-31T14:54:32Z -- ``

**Touched files (intersect with thread paths):**

- `tools/hygiene/audit-worktree-survey.test.ts`
- `tools/hygiene/audit-worktree-survey.ts`

**Message:**

```
fix(081KDVJT3E008QG0R000SCFYN5): scope worktree survey to locked entries

Address follow-up PR review feedback for the 081KDVJT3E008QG0R000SCFYN5 survey contract.

The survey now parses git worktree locked annotations, limits recovery classification to locked or prunable worktrees, and accepts the documented --dry flag without writing report files.

Focused checks:

- bun test tools/hygiene/audit-worktree-survey.test.ts

- git diff --check

- bunx prettier --check tools/hygiene/audit-worktree-survey.ts tools/hygiene/audit-worktree-survey.test.ts

- bun tools/hygiene/audit-worktree-survey.ts --dry --json

Agency-Signature-Version: 1
Agent: Vera
Agent-Runtime: OpenAI Codex desktop heartbeat loop
Agent-Model: GPT-5
Credential-Identity: aaron-codex-desktop
Credential-Mode: shared
Human-Review: not-requested
Action-Mode: autonomous-fail-open
Task: 081KDVJT3E008QG0R000SCFYN5
Co-Authored-By: Codex <noreply@openai.com>
```

### `a0c7b44d9fa765c9d869089de951bc6718017257` -- 2026-05-31T14:57:43Z -- ``

**Touched files (intersect with thread paths):**

- `tools/hygiene/audit-worktree-survey.test.ts`
- `tools/hygiene/audit-worktree-survey.ts`

**Message:**

```
fix(081KDVJT3E008QG0R000SCFYN5): harden survey markdown code spans

Address PR #6214 markdown escaping review feedback.

The renderer now uses code-span fences longer than any backtick run, reuses that renderer for branches, and escapes backslashes before table pipes/newlines.

Focused checks:

- bun test tools/hygiene/audit-worktree-survey.test.ts

- git diff --check

- bunx prettier --check tools/hygiene/audit-worktree-survey.ts tools/hygiene/audit-worktree-survey.test.ts

- bun tools/hygiene/audit-worktree-survey.ts --dry --json

Agency-Signature-Version: 1
Agent: Vera
Agent-Runtime: OpenAI Codex desktop heartbeat loop
Agent-Model: GPT-5
Credential-Identity: aaron-codex-desktop
Credential-Mode: shared
Human-Review: not-requested
Action-Mode: autonomous-fail-open
Task: 081KDVJT3E008QG0R000SCFYN5
Co-Authored-By: Codex <noreply@openai.com>
```

### `faaeb07e586f116044e8acea2f217cafc927e7ac` -- 2026-05-31T15:04:24Z -- ``

**Touched files (intersect with thread paths):**

- `tools/hygiene/audit-worktree-survey.test.ts`
- `tools/hygiene/audit-worktree-survey.ts`

**Message:**

```
fix(081KDVJT3E008QG0R000SCFYN5): compare covered worktree trees

Address PR #6214 follow-up review feedback.

The survey now treats clean locked worktrees whose tree matches origin/main as already covered before falling back to git cherry, labels the scoped locked/prunable total accurately, and renders the root path with the markdown code-span helper.

Focused checks:

- bun test tools/hygiene/audit-worktree-survey.test.ts

- git diff --check

- bunx prettier --check tools/hygiene/audit-worktree-survey.ts tools/hygiene/audit-worktree-survey.test.ts

- bun tools/hygiene/audit-worktree-survey.ts --dry --json

Agency-Signature-Version: 1
Agent: Vera
Agent-Runtime: OpenAI Codex desktop heartbeat loop
Agent-Model: GPT-5
Credential-Identity: aaron-codex-desktop
Credential-Mode: shared
Human-Review: not-requested
Action-Mode: autonomous-fail-open
Task: 081KDVJT3E008QG0R000SCFYN5
Co-Authored-By: Codex <noreply@openai.com>
```

### `a11b5b95fe1c35001873d4ac762e5bfd8bd2631a` -- 2026-05-31T15:12:27Z -- ``

**Touched files (intersect with thread paths):**

- `tools/hygiene/audit-worktree-survey.test.ts`
- `tools/hygiene/audit-worktree-survey.ts`

**Message:**

```
fix(081KDVJT3E008QG0R000SCFYN5): compare worktree recovery by merge tree

Use git merge-tree to detect whether a clean locked worktree would change origin/main when merged. This covers squash-merged branches even when origin/main has later unrelated commits.

Add an integration test for a squashed branch followed by unrelated main work.

Agency-Signature-Version: 1
Agent: Vera
Agent-Runtime: OpenAI Codex desktop heartbeat loop
Agent-Model: GPT-5
Credential-Identity: aaron-codex-desktop
Credential-Mode: shared
Human-Review: not-requested
Action-Mode: autonomous-fail-open
Task: 081KDVJT3E008QG0R000SCFYN5
Co-Authored-By: Codex <noreply@openai.com>
```

### `60b21a11ab332570c082c960e20c022bf0e7dff9` -- 2026-05-31T15:19:00Z -- ``

**Touched files (intersect with thread paths):**

- `tools/hygiene/audit-worktree-survey.test.ts`
- `tools/hygiene/audit-worktree-survey.ts`

**Message:**

```
fix(081KDVJT3E008QG0R000SCFYN5): protect prunable worktree commits

Do not classify missing prunable worktrees as obsolete until their recorded HEAD is checked against origin/main. Use the survey repository context for HEAD coverage checks when the worktree path no longer exists.

Add focused tests for missing prunable worktrees with and without recoverable committed substrate.

Agency-Signature-Version: 1
Agent: Vera
Agent-Runtime: OpenAI Codex desktop heartbeat loop
Agent-Model: GPT-5
Credential-Identity: aaron-codex-desktop
Credential-Mode: shared
Human-Review: not-requested
Action-Mode: autonomous-fail-open
Task: 081KDVJT3E008QG0R000SCFYN5
Co-Authored-By: Codex <noreply@openai.com>
```

### `06692a69579f6d5d8f298c821d1db57256c61218` -- 2026-05-31T15:23:59Z -- ``

**Touched files (intersect with thread paths):**

- `tools/hygiene/audit-worktree-survey.test.ts`
- `tools/hygiene/audit-worktree-survey.ts`

**Message:**

```
fix(081KDVJT3E008QG0R000SCFYN5): match historical main deltas

Replace the merge-tree coverage check with a stable patch-id comparison of the branch delta against first-parent origin/main history. This covers squash-merged branches even when main later edits the same files.

Update the focused integration test to exercise that same-file post-squash edit case.

Agency-Signature-Version: 1
Agent: Vera
Agent-Runtime: OpenAI Codex desktop heartbeat loop
Agent-Model: GPT-5
Credential-Identity: aaron-codex-desktop
Credential-Mode: shared
Human-Review: not-requested
Action-Mode: autonomous-fail-open
Task: 081KDVJT3E008QG0R000SCFYN5
Co-Authored-By: Codex <noreply@openai.com>
```

### `88fe9fa80a691b69512534c4600961075d7baacf` -- 2026-05-31T15:31:03Z -- ``

**Touched files (intersect with thread paths):**

- `tools/hygiene/audit-worktree-survey.test.ts`
- `tools/hygiene/audit-worktree-survey.ts`

**Message:**

```
fix(081KDVJT3E008QG0R000SCFYN5): detect contained branch deltas

Teach the worktree survey to treat a branch delta as covered when its patch is contained inside a larger first-parent main commit. The check uses a temporary index loaded from historical main commits and reverse-apply validation, so it does not mutate linked worktrees.

Add a regression for a feature change squash-merged alongside unrelated work in the same main commit.

Agency-Signature-Version: 1

Agent: Vera

Agent-Runtime: OpenAI Codex desktop heartbeat loop

Agent-Model: GPT-5

Credential-Identity: aaron-codex-desktop

Credential-Mode: shared

Human-Review: not-requested

Action-Mode: autonomous-fail-open

Task: 081KDVJT3E008QG0R000SCFYN5

Co-Authored-By: Codex <noreply@openai.com>
```

### `0878c14e5570112d5c37e5d700790f814c0cbbdf` -- 2026-05-31T15:36:05Z -- ``

**Touched files (intersect with thread paths):**

- `tools/hygiene/audit-worktree-survey.test.ts`
- `tools/hygiene/audit-worktree-survey.ts`

**Message:**

```
fix(081KDVJT3E008QG0R000SCFYN5): raise survey git output buffers

Set an explicit 64 MiB maxBuffer for git output paths used by the worktree survey, including diff, patch-id, cherry, status, merge-base, worktree-list, and temporary-index apply checks.

Add large-diff regressions so already-covered branch deltas do not degrade to unknown when the substrate diff exceeds the default spawn buffer.

Agency-Signature-Version: 1

Agent: Vera

Agent-Runtime: OpenAI Codex desktop heartbeat loop

Agent-Model: GPT-5

Credential-Identity: aaron-codex-desktop

Credential-Mode: shared

Human-Review: not-requested

Action-Mode: autonomous-fail-open

Task: 081KDVJT3E008QG0R000SCFYN5

Co-Authored-By: Codex <noreply@openai.com>
```

### `60d0a5c8c082e6ca77bc026172f4d00554ba3686` -- 2026-05-31T15:41:20Z -- ``

**Touched files (intersect with thread paths):**

- `tools/hygiene/audit-worktree-survey.test.ts`
- `tools/hygiene/audit-worktree-survey.ts`

**Message:**

```
fix(081KDVJT3E008QG0R000SCFYN5): reject reverted historical deltas

Require the current origin/main tip to retain branch-changed paths before historical first-parent containment or git-cherry equivalence can mark a locked worktree ALREADY-COVERED.

Add a regression where a branch delta was squash-covered on main and later reverted, so the survey keeps the worktree in recovery instead of discarding the remaining copy.

Agency-Signature-Version: 1

Agent: Vera

Agent-Runtime: OpenAI Codex desktop heartbeat loop

Agent-Model: GPT-5

Credential-Identity: aaron-codex-desktop

Credential-Mode: shared

Human-Review: not-requested

Action-Mode: autonomous-fail-open

Task: 081KDVJT3E008QG0R000SCFYN5

Co-Authored-By: Codex <noreply@openai.com>
```

### `d37869f1982ae452c486037bfa52805fee69c2f3` -- 2026-05-31T15:44:44Z -- ``

**Touched files (intersect with thread paths):**

- `tools/hygiene/audit-worktree-survey.test.ts`
- `tools/hygiene/audit-worktree-survey.ts`

**Message:**

```
fix(081KDVJT3E008QG0R000SCFYN5): compare first-parent patch ids

Compare branch patch ids against each historical main commit's own first-parent delta instead of cumulative base-to-commit diffs.

Add a regression for an exact later squash commit after unrelated main work, preserving the intended already-covered classification.

Agency-Signature-Version: 1

Agent: Vera

Agent-Runtime: OpenAI Codex desktop heartbeat loop

Agent-Model: GPT-5

Credential-Identity: aaron-codex-desktop

Credential-Mode: shared

Human-Review: not-requested

Action-Mode: autonomous-fail-open

Task: 081KDVJT3E008QG0R000SCFYN5

Co-Authored-By: Codex <noreply@openai.com>
```

### `869a670eb2add04053b37620eeb5f2bdbf19b2e8` -- 2026-05-31T15:48:44Z -- ``

**Touched files (intersect with thread paths):**

- `tools/hygiene/audit-worktree-survey.test.ts`
- `tools/hygiene/audit-worktree-survey.ts`

**Message:**

```
fix(081KDVJT3E008QG0R000SCFYN5): retain tree entry modes

Compare retained changed paths by full ls-tree entry identity so mode-only and type-only changes are treated as retained when origin/main already carries them.

Agency-Signature-Version: 1
Agent: Vera
Agent-Runtime: OpenAI Codex desktop heartbeat loop
Agent-Model: GPT-5
Credential-Identity: aaron-codex-desktop
Credential-Mode: shared
Human-Review: not-requested
Action-Mode: autonomous-fail-open
Task: 081KDVJT3E008QG0R000SCFYN5
Co-Authored-By: Codex <noreply@openai.com>
```

### `7ecae463fabfb2846620e90d10e85b38ded79bf2` -- 2026-05-31T15:55:24Z -- ``

**Touched files (intersect with thread paths):**

- `tools/hygiene/audit-worktree-survey.test.ts`
- `tools/hygiene/audit-worktree-survey.ts`

**Message:**

```
fix(081KDVJT3E008QG0R000SCFYN5): require retained branch deltas

Validate historical coverage against the current origin/main tip by reverse-applying the branch delta itself, with zero-context fallback for adjacent squash edits.

Agency-Signature-Version: 1
Agent: Vera
Agent-Runtime: OpenAI Codex desktop heartbeat loop
Agent-Model: GPT-5
Credential-Identity: aaron-codex-desktop
Credential-Mode: shared
Human-Review: not-requested
Action-Mode: autonomous-fail-open
Task: 081KDVJT3E008QG0R000SCFYN5
Co-Authored-By: Codex <noreply@openai.com>
```

### `55e217b5b81edb235a0db638b99d94a3b4fd54ee` -- 2026-05-31T16:00:26Z -- ``

**Touched files (intersect with thread paths):**

- `tools/hygiene/audit-worktree-survey.test.ts`
- `tools/hygiene/audit-worktree-survey.ts`

**Message:**

```
fix(081KDVJT3E008QG0R000SCFYN5): guard zero-context containment

Require zero-context containment fallback to match the branch-added target lines at their expected location before accepting the reverse-apply probe.

Agency-Signature-Version: 1
Agent: Vera
Agent-Runtime: OpenAI Codex desktop heartbeat loop
Agent-Model: GPT-5
Credential-Identity: aaron-codex-desktop
Credential-Mode: shared
Human-Review: not-requested
Action-Mode: autonomous-fail-open
Task: 081KDVJT3E008QG0R000SCFYN5
Co-Authored-By: Codex <noreply@openai.com>
```

### `d857d83f401440f55db7ddb9c40d934520ca4360` -- 2026-05-31T16:07:43Z -- ``

**Touched files (intersect with thread paths):**

- `tools/hygiene/audit-worktree-survey.test.ts`
- `tools/hygiene/audit-worktree-survey.ts`

**Message:**

```
fix(081KDVJT3E008QG0R000SCFYN5): normalize survey parser edges

Normalize worktree porcelain newlines before parsing so CRLF output preserves locked/prunable records. Extend the zero-context containment fallback to deletion-only branch hunks with a conservative deleted-line guard, and cover the requested test helper lint suppression.

Focused checks: bun test tools/hygiene/audit-worktree-survey.test.ts; git diff --check; bunx prettier --check tools/hygiene/audit-worktree-survey.ts tools/hygiene/audit-worktree-survey.test.ts; bun tools/hygiene/audit-worktree-survey.ts --dry --json; bun run typecheck. Additional note: bun run lint:typescript is blocked by unrelated repo-wide ESLint parser/rule debt; targeted ESLint no longer reports sonarjs/no-os-command-from-path for the touched helper.

Agency-Signature-Version: 1

Agent: Vera

Agent-Runtime: OpenAI Codex desktop heartbeat loop

Agent-Model: GPT-5

Credential-Identity: aaron-codex-desktop

Credential-Mode: shared

Human-Review: not-requested

Action-Mode: autonomous-fail-open

Task: 081KDVJT3E008QG0R000SCFYN5

Co-Authored-By: Codex <noreply@openai.com>
```
