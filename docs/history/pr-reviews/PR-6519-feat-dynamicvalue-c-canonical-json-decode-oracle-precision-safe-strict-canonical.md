# PR #6519 -- feat(DynamicValue): C# canonical-JSON DECODE oracle — precision-safe + strict-canonical

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 6519 |
| Title | feat(DynamicValue): C# canonical-JSON DECODE oracle — precision-safe + strict-canonical |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-06-02T01:39:02Z |
| Merged at | 2026-06-02T01:41:47Z |
| Merge commit SHA | `f6f429607c854e464535a415c52a8bd46094c852` |
| Branch | `otto-cli/dynamicvalue-decode-json-csharp-2026-06-01` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/6519 |
| Changed files | 2 |
| Additions / deletions | +542 / -0 |

## Description

Second of the four JSON-decode oracles (TS #6518 landed; F#/Rust follow). `FromCanonicalJson` is the inverse of `ToCanonicalJson` for the 6 locked shapes (Float + Bytes DEFERRED in JSON — they lock under CBOR).

## What

- Adds `FromCanonicalJson` + private recursive-descent readers to `DynamicValues.cs`, mirroring the CBOR `TryReadX(ref pos): DecodeError?` pattern + the **shared `DecodeError` enum** + the fixed-point canonical check.
- `DynamicValueJsonDecodeTests.cs`: round-trip over the seed (re-encode + structural equality) + 23 rejection cases.

## Precision-safe + strict-canonical

- **int64 precision**: number token parsed as **text** via `long.TryParse` (never a lossy `double`); on a `-?[0-9]+` token the only failure is overflow → `IntegerOverflow`.
- **fixed-point**: `ToCanonicalJson(decoded) == input` → else `NonCanonical` (rejects whitespace, non-minimal escapes, leading zeros).
- **deferred**: a number with `.`/`e`/`E` is a Float → `Unsupported`.
- **carries the TS oracle's malformed-input lessons in from the start**: `\uXXXX` requires exactly 4 hex digits (`ushort.TryParse` on the 4-char span); `.`/exponent require digits (`ConsumeJsonDigits`) — so `1.`/`1e`/`1e+` → `UnexpectedEnd`, not `Unsupported`.
- never thrown: `Result<DynamicValue, DecodeError>` per the Result-over-exception rule.

**24 tests pass; Release build 0-warnings** (split `TryReadJsonPair` out of `TryReadJsonObject` for MA0051). The compilers don't lie.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 1 |
| Resolved threads | 0 |
| Unresolved threads | 1 |
| Total review comments | 1 |
| Total fix commits (touching thread paths) | 1 |

## Review threads

### Thread 1 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM6GSnB-`
- **Path**: `src/Core.CSharp.DynamicValue/DynamicValues.cs`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-06-02T01:41:49Z on `src/Core.CSharp.DynamicValue/DynamicValues.cs`:965 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Validate \u escapes before TryParse**

  Because `NumberStyles.HexNumber` permits leading/trailing whitespace, a malformed escape like `"\u000 "` or `"\u 001"` is accepted here as a decoded character and then reported as `NonCanonical` by the fixed-point check, instead of the malformed-input `UnexpectedEnd` promised by the JSON decoder and mirrored by the TS oracle. Check the four characters with an explicit hex-digit predicate before parsing so non-hex whitespace cannot be silently accepted.

  Useful? React with 👍 / 👎.
  </pre>

## Fix commits (touching thread paths)

### `e532313e8b6bd67714e6a61152f654f90400ebd0` -- 2026-06-02T01:38:53Z -- ``

**Touched files (intersect with thread paths):**

- `src/Core.CSharp.DynamicValue/DynamicValues.cs`

**Message:**

```
feat(DynamicValue): C# canonical-JSON DECODE oracle — precision-safe …

…+ strict-canonical

Second of the four JSON-decode oracles (TS landed #6518; F#/Rust follow). FromCanonicalJson
is the inverse of ToCanonicalJson for the 6 locked shapes (Float + Bytes DEFERRED in JSON —
they lock under CBOR). Mirrors the CBOR-decode TryReadX(ref pos): DecodeError? pattern +
the shared DecodeError enum + the fixed-point canonical check.

- precision-safe int64: number token parsed as TEXT via long.TryParse (never a lossy double);
  the only failure on a -?[0-9]+ token is overflow → IntegerOverflow.
- strict-canonical via fixed-point: ToCanonicalJson(decoded) == input → else NonCanonical
  (rejects whitespace, non-minimal escapes, leading zeros).
- Float-shaped number (.eE) → Unsupported (deferred in v1 JSON).
- carries the TS oracle's malformed-input lessons from the start: \uXXXX requires exactly
  4 hex digits (ushort.TryParse on the 4-char span); '.'/'e'/'e+' require digits
  (ConsumeJsonDigits fails UnexpectedEnd if none) — so 1./1e/1e+ → UnexpectedEnd, not Unsupported.
- never thrown: Result<DynamicValue, DecodeError> per the Result-over-exception rule.

DynamicValueJsonDecodeTests: round-trip over the seed (re-encode + structural) + 23 rejection
cases (malformed / deferred-float / oversized / non-canonical). 24 tests pass; Release build
0-warnings (split TryReadJsonPair out of TryReadJsonObject for MA0051). The compilers don't lie.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```
