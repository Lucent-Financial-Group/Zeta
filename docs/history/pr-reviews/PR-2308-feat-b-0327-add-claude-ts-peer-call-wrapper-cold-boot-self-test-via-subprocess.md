# PR #2308 -- feat(B-0327): add claude.ts peer-call wrapper -- cold-boot self-test via subprocess

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 2308 |
| Title | feat(B-0327): add claude.ts peer-call wrapper -- cold-boot self-test via subprocess |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-05-09T14:34:59Z |
| Merged at | 2026-05-09T14:57:53Z |
| Merge commit SHA | `94ce688d28af96ae5c27032e3bc79969207f5583` |
| Branch | `feat/B-0327-claude-ts-self-call` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/2308 |
| Changed files | 3 |
| Additions / deletions | +475 / -69 |

## Description

## Summary

- Adds `tools/peer-call/claude.ts`: spawns `claude --print --tools "Read,Glob,Grep" --no-session-persistence` as a child process for cold-boot self-verification
- Wires `CLAUDE_SUBSTANTIVE_TRIGGERS` from `_firewall.ts` (landed by B-0325); implements B-0327 (P1)
- Updates `tools/peer-call/README.md` table: adds missing entries for `kiro.ts` (B-0326, c4256fa4) and `claude.ts`
- Marks all pre-start checklist + done-criteria as complete on the backlog row

## Design decisions

**Read-only tool set**: `--tools "Read,Glob,Grep"` limits blast radius while enabling all three cold-boot verification scenarios from the spec. The `--print` flag automatically skips the workspace trust dialog (documented in `claude --help`).

**No `--bare`**: We intentionally omit `--bare` so the child discovers and loads CLAUDE.md from CWD, which is the cold-boot fidelity requirement. `--bare` skips CLAUDE.md auto-discovery, which would defeat the purpose.

**`--model` flag**: Added (not in original spec) since test scenarios may want to verify substrate loading across different model variants.

## Smoke test results

```
$ bun tools/peer-call/claude.ts --help
claude.ts -- agent-side caller for cold-boot self-testing via the Claude CLI.
...

$ bun tools/peer-call/claude.ts "tick heartbeat" ; echo exit:$?
[CLAUDE-FIREWALL] REJECTED:
  Reason: MISSING_PAYLOAD:rote-heartbeat-pattern ...
exit:3

$ bun tools/peer-call/claude.ts --allow-empty "tick heartbeat" ; echo exit:$?
[CLAUDE-FIREWALL] BYPASS via --allow-empty (testing-only; logged): ...
# spawned child independently read CLAUDE.md and verified implementation
OUTPUT-FILE: /tmp/peer-call-output/20260509T143227Z-claude-o05tmu.md
exit:0

$ bun run typecheck
$ tsc --noEmit
# 0 errors
```

## Cold-boot live test result (excerpt)

The `--allow-empty` test spawned a fresh Claude Code instance which independently verified the implementation:
> "Branch deliverable looks complete and ready to commit + PR. The code is clean, follows the existing peer-call patterns, and the firewall integration is wired correctly."
> "All 4 imports from `_firewall.ts` resolve. Exit codes follow the 0/1/2/3 convention."

The cold-boot instance also noted that kiro.ts and claude.ts were missing from the README table (now fixed in this PR).

## Test plan

- [x] `bun tools/peer-call/claude.ts --help` exits 0
- [x] Firewall rejects rote heartbeat with exit 3
- [x] `--allow-empty` bypass reaches Claude CLI (exit 0)
- [x] `bun run typecheck` passes with 0 errors
- [x] File is clean ASCII text (no binary data)

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | true |
| Total threads | 4 |
| Resolved threads | 4 |
| Unresolved threads | 0 |
| Total review comments | 8 |
| Total fix commits (touching thread paths) | 3 |

## Review threads

### Thread 1 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6A02gM`
- **Path**: `tools/peer-call/claude.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-09T14:38:18Z on `tools/peer-call/claude.ts`:248 (association: NONE)

  <pre>
  P1: The help text’s cold-boot scenario references `CURRENT-aaron.md`, but in this repo the file lives under `memory/CURRENT-aaron.md`. As written, this example is likely to fail/confuse users trying the scenario. Update the example (and any related docs/spec text) to use the correct path (or clarify that `memory/` is implied).

  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-09T14:55:42Z on `tools/peer-call/claude.ts`:248 (association: MEMBER)

    <pre>
    Fixed in 2610ea88 (review thread fix commit) and 1d1227c3 (backlog doc). The help text now reads `memory/CURRENT-aaron.md` matching the actual file location.
    </pre>

### Thread 2 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6A02gP`
- **Path**: `tools/peer-call/claude.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-09T14:38:18Z on `tools/peer-call/claude.ts`:297 (association: NONE)

  <pre>
  P1 (security hardening + consistency): `splitContextCmd` rejects several shell metacharacters but does not reject NUL (`\^@`). Other peer-call wrappers (e.g. `kiro.ts`) explicitly reject NUL to avoid passing it into `spawnSync` args. Consider matching that behavior here for consistency and to reduce weird-platform edge cases.

  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-09T14:55:50Z on `tools/peer-call/claude.ts`:297 (association: MEMBER)

    <pre>
    Fixed in 2610ea88. splitContextCmd now rejects NUL (matching kiro.ts security convention) — regex updated to /[\^@\r\n|&amp;;&lt;&gt;`$]/u
    </pre>

### Thread 3 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6A02gT`
- **Path**: `docs/backlog/P1/B-0327-peer-call-claude-ts-self-call-cold-boot.md`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-09T14:38:18Z on `docs/backlog/P1/B-0327-peer-call-claude-ts-self-call-cold-boot.md`:81 (association: NONE)

  <pre>
  P1: This scenario list references `CURRENT-aaron.md`, but the current file is `memory/CURRENT-aaron.md` in this repo. Using the correct path here keeps the spec executable and avoids xref drift between the backlog row and the script’s `--help` examples.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-09T14:55:52Z on `docs/backlog/P1/B-0327-peer-call-claude-ts-self-call-cold-boot.md`:81 (association: MEMBER)

    <pre>
    Fixed in 1d1227c3. Scenario 3 in the backlog doc now reads 'memory/CURRENT-aaron.md' — matching the actual file location and keeping the spec executable.
    </pre>

### Thread 4 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6A02gV`
- **Path**: `tools/peer-call/README.md`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-09T14:38:19Z on `tools/peer-call/README.md`:25 (association: NONE)

  <pre>
  P1: This table now lists 8 wrappers (`grok/gemini/codex/amara/ani/riven/kiro/claude`), but the README intro still says “Six sibling TypeScript wrappers”. Update the intro count (or rephrase to avoid a hard-coded number) so the doc stays consistent as new peers are added.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-09T14:55:55Z on `tools/peer-call/README.md`:25 (association: MEMBER)

    <pre>
    Fixed in 2610ea88. README intro now reads "Eight sibling TypeScript wrappers (Bun runtime: grok, gemini, codex, amara, ani, riven, kiro, claude)" — listing all 8 explicitly so no hard-coded count can drift again.
    </pre>

## Fix commits (touching thread paths)

### `c44bbf7c3ca1e381d518690785687a0f2334b2ba` -- 2026-05-09T14:34:37Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `docs/backlog/P1/B-0327-peer-call-claude-ts-self-call-cold-boot.md`
- `tools/peer-call/README.md`
- `tools/peer-call/claude.ts`

**Message:**

```
feat(B-0327): add claude.ts peer-call wrapper -- cold-boot self-test …

…via subprocess

Adds `tools/peer-call/claude.ts`: spawns `claude --print --tools
"Read,Glob,Grep" --no-session-persistence` as a child process so the
factory can invoke a fresh Claude Code instance for cold-boot
substrate verification.

The cold-boot guarantee holds because the child is a fresh OS process
with no inherited context; it discovers CLAUDE.md from CWD independently.
Read-only tool set (Read,Glob,Grep) limits blast radius to the three
verification scenarios in the spec:
  1. "Read CLAUDE.md and report the wake-time floor."
  2. "Verify file X exists and summarise its purpose."
  3. "Read CURRENT-aaron.md and report what is in force."

Wires CLAUDE_SUBSTANTIVE_TRIGGERS from _firewall.ts (landed by B-0325).
Adds optional --model flag for model override. Full pre-start checklist
and done-criteria marked complete on backlog row.

Also updates README.md table to include kiro.ts (B-0326, landed
c4256fa4) and claude.ts entries that were missing from the table.

Smoke tests run:
  bun tools/peer-call/claude.ts --help       # OK
  bun tools/peer-call/claude.ts "tick heartbeat"  # exit 3 (firewall)
  bun tools/peer-call/claude.ts --allow-empty "..."  # exit 0 + live cold-boot
  bun run typecheck                          # 0 errors

Cold-boot live test: spawned child independently read CLAUDE.md,
verified the claude.ts implementation, and reported its findings
without any inherited context from the calling session.

operative-authorization: aaron 2026-05-04: "it**, not just the output. Grinding through failures + recoveries"

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>
```

### `2610ea88a788b169185f6e6435221921b7935626` -- 2026-05-09T14:49:07Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/peer-call/README.md`
- `tools/peer-call/claude.ts`

**Message:**

```
fix(B-0327): address review threads on path, NUL, README count (P1s)

- Update help examples to memory/CURRENT-aaron.md (correct location)
- Reject NUL in splitContextCmd (match kiro.ts security)
- Rephrase README intro to list 8 wrappers (no hard-coded 'Six')
Co-Authored-By: Grok <noreply@x.ai>

Co-authored-by: Cursor <cursoragent@cursor.com>
```

### `1d1227c357115729dd605c691896f910e0692748` -- 2026-05-09T14:53:18Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `docs/backlog/P1/B-0327-peer-call-claude-ts-self-call-cold-boot.md`

**Message:**

```
fix(B-0327): fix CURRENT-aaron.md path in backlog spec scenarios

Copilot P1: scenario 3 referenced CURRENT-aaron.md but the file lives
at memory/CURRENT-aaron.md. Using the correct path keeps the spec
executable and avoids xref drift between the backlog row and the
deployed help text.

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>
```
