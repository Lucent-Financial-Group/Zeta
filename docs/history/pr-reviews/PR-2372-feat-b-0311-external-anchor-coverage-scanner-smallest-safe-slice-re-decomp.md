# PR #2372 -- feat(B-0311): external-anchor coverage scanner — smallest safe slice (re-decomp)

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 2372 |
| Title | feat(B-0311): external-anchor coverage scanner — smallest safe slice (re-decomp) |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-05-09T23:17:48Z |
| Merged at | 2026-05-09T23:23:40Z |
| Merge commit SHA | `3f9b99ca87c73050f8448cde91f7fa84ff4603ef` |
| Branch | `claim/b0311-external-anchor-coverage-scanner-slice-2026-05-09` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/2372 |
| Changed files | 3 |
| Additions / deletions | +562 / -1 |

## Description

## Summary
Smallest safe slice of B-0311: implemented `tools/alignment/audit_external_anchors.ts` (TS) + tests. Re-decomposed during build (original S-effort item treated as broad; sliced to core scanner + classification + window extract + emit paths). 

## One bounded step
- Full scanner per spec (load registry from B-0310, per-concept URL extract + classify, JSON/MD/human output, CLI flags).
- No docs changes; all in F#/TS-preferred TS tool.
- Dedicated worktree + pushed claim branch used; root checkout untouched.

## Focused checks (run in worktree)
- `dotnet build -c Release`: 0 warnings, 0 errors (gate passed).
- `bun test tools/alignment/audit_external_anchors.test.ts`: 31 pass, 0 fail.
- `bun tools/alignment/audit_external_anchors.ts --md`: emits table; 63 concepts, **2 anchored**, 61 anchor-pending (only Otto-329/357 have claude.com anchors).
- `bun ... --json`: valid schema, counts match.
- All CLI paths (help, bad flag, --out) exit correctly.

Outcome: scanner complete and green; surfaces the coverage gap (most alignment concepts lack external anchors — future slices can target specific concepts).

## Reviewers
- spec-zealot (schema)
- alignment-auditor (HC/SD/DIR signal)

Co-Authored-By: Grok <noreply@x.ai>

Made with [Cursor](https://cursor.com)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 3 |
| Resolved threads | 3 |
| Unresolved threads | 0 |
| Total review comments | 3 |
| Total fix commits (touching thread paths) | 2 |

## Review threads

### Thread 1 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6A3D2j`
- **Path**: `tools/alignment/audit_external_anchors.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-09T23:22:38Z on `tools/alignment/audit_external_anchors.ts`:106 (association: NONE)

  <pre>
  P1: `BARE_URL_RE` currently uses `(?&lt;!\()` which suppresses *any* URL preceded by `(`, including common bare URLs like `(https://example.com)` in prose. That means anchors can be missed and concepts incorrectly classified as `anchor-pending`. Consider narrowing the exclusion to the actual markdown-link prefix (e.g., exclude only `](` cases) or strip markdown links first and then scan for bare URLs.

  </pre>

### Thread 2 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6A3D2n`
- **Path**: `tools/alignment/audit_external_anchors.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-09T23:22:38Z on `tools/alignment/audit_external_anchors.ts`:185 (association: NONE)

  <pre>
  P2: `audit()` re-reads and re-splits the same source file once per concept (`readSource(concept.source)` inside the loop). With many concepts sharing a small set of source files, this is avoidable I/O and can become noticeable as the registry grows. Consider caching per `concept.source` (e.g., `Map&lt;string,string&gt;`) and reusing the loaded content when extracting anchors.

  </pre>

### Thread 3 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6A3D2p`
- **Path**: `tools/alignment/audit_external_anchors.test.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-09T23:22:38Z on `tools/alignment/audit_external_anchors.test.ts`:115 (association: NONE)

  <pre>
  P2: `extractUrlsFromWindow` has an edge case where bare URLs wrapped in parentheses (e.g. `(https://example.com)`) are currently excluded by `BARE_URL_RE`. After adjusting the extraction logic, it would be good to add a regression test for parenthesized bare URLs (and for not double-counting markdown-link URLs) so this doesn’t silently reappear.
  </pre>

## Fix commits (touching thread paths)

### `a74c8bfa68d9694857d8f5487c91ee93586c9740` -- 2026-05-09T17:25:53Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/alignment/audit_external_anchors.test.ts`
- `tools/alignment/audit_external_anchors.ts`

**Message:**

```
feat(B-0311): external-anchor coverage scanner — per-concept audit tool

Adds tools/alignment/audit_external_anchors.ts (+ test file) that:
- Loads all concepts from the B-0310 concept registry
- For each concept, locates its position in the source surface and
  extracts external URLs from a ±20-line context window
- Classifies each URL as paper/rfc/blog/so-se/talk/other by domain
- Emits per-concept coverage: anchored (with URL list) or anchor-pending
- Supports --json, --md, --out DIR flags; exit codes 0/2

Baseline result on current repo: 63 concepts total, 2 anchored,
61 anchor-pending — establishing the coverage floor for follow-on
anchor-addition work (B-0312+).

Tests: 31 passing (classifyUrl, extractUrlsFromWindow, audit(),
main() CLI). Build gate: 0 warnings, 0 errors.

operative-authorization: aaron 2026-05-04: "it**, not just the output. Grinding through failures + recoveries"

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>
```

### `8ab6f8564b4998150bb181cade825f2b9e5fe7bf` -- 2026-05-09T17:29:50Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/alignment/audit_external_anchors.test.ts`

**Message:**

```
fix(B-0311): use startsWith for host checks to satisfy CodeQL URL san…

…itization rule

- Changed .includes('doi.org') / .includes('arxiv.org') to .startsWith('https://.../') in test expectations
- Safer domain matching, resolves CodeQL incomplete URL substring alerts on lines 86/92/104

Co-Authored-By: Grok <noreply@x.ai>
Co-authored-by: Cursor <cursoragent@cursor.com>
```
