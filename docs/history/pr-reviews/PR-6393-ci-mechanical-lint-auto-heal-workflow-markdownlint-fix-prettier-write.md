# PR #6393 -- ci: mechanical lint auto-heal workflow (markdownlint --fix / prettier --write)

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 6393 |
| Title | ci: mechanical lint auto-heal workflow (markdownlint --fix / prettier --write) |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-06-01T10:07:10Z |
| Merged at | 2026-06-01T10:30:25Z |
| Merge commit SHA | `e6eeb5a631ac9df03276cd5643d86ffb4d55f49c` |
| Branch | `otto-cli/lint-autofix-mechanical-workflow-2026-06-01` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/6393 |
| Changed files | 20 |
| Additions / deletions | +533 / -196 |

## Description

Auto-**heals** mechanical lint on PRs — runs the deterministic `--fix` tools and commits the result back, so the required lint gate passes on the healed commit instead of blocking on a fixable nit. Auto-heal, **not a new gate** (per 2026-06-01: "mechanical fixes or auto format fixes ... it just being a workflow step makes sense") — the keep-remaining-gates-frictionless half of winding branch protection down.

### Mechanical only (zero intelligence)

- `markdownlint-cli2 --fix` → heals the **markdownlint** required gate cleanly
- `prettier --write` (TS/JSON) → format hygiene (not itself a gate)

Same mise-pinned tool versions as `gate.yml` (via `install.sh`) → **version parity**, so the heal can't "fix" into a state the gate rejects.

### Safety

- **same-repo PRs only** (forks excluded — no untrusted-ref checkout, no write-perms over untrusted code); `pull_request`, not `_target`.
- **loop-guarded** (skips `github-actions[bot]` commits; heal is idempotent regardless).
- `head.ref` via **env var + quoted** in `run:` (injection-safe per the GH Actions injection guide); `contents: write` scoped to the autofix job only (workflow level is `read`).
- prettier gated on `--frozen-lockfile` + `bun.lock` discarded pre-commit → **no lockfile churn** in the heal.

### Re-trigger wrinkle (documented in-file)
`GITHUB_TOKEN` pushes don't re-trigger workflows, so while a **required** gate exists, set an `AUTOFIX_TOKEN` PAT/App-token secret for the gate to auto-re-run on the healed commit. Without it the fix still commits; the loop/author nudges the gate. As gates are removed, plain `GITHUB_TOKEN` suffices.

### Intelligence step (separate, future)
actionlint-logic / semgrep-without-`fix:` / type-logic fixes are an **agent-in-Actions** step (accelerator / local-LLM direction) — this PR is the free zero-LLM subset; that escalates the rest.

actionlint clean locally.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | true |
| Total threads | 19 |
| Resolved threads | 19 |
| Unresolved threads | 0 |
| Total review comments | 19 |
| Total fix commits (touching thread paths) | 4 |

## Review threads

### Thread 1 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GE8Wn`
- **Path**: `.github/workflows/lint-autofix.yml`
- **Replies**: 0

**Initial comment:**

- **`github-advanced-security[bot]` (bot)** at 2026-06-01T10:08:50Z on `.github/workflows/lint-autofix.yml`:74 (association: NONE)

  <pre>
  ## CodeQL / Checkout of untrusted code in trusted context

  Potential unsafe checkout of untrusted pull request on privileged workflow.

  [Show more details](https://github.com/Lucent-Financial-Group/Zeta/security/code-scanning/135)
  </pre>

### Thread 2 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GE_VB`
- **Path**: `.github/workflows/lint-autofix.yml`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-01T10:11:45Z on `.github/workflows/lint-autofix.yml`:99 (association: NONE)

  <pre>
  `bun install --frozen-lockfile` runs under `working-directory: src/Core.TypeScript`, but that directory doesn't contain the repo’s `package.json`/`bun.lock` (they live at the repo root). As a result this `bun install` will fail and the workflow will always skip running prettier.
  </pre>

### Thread 3 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GE_VV`
- **Path**: `.github/workflows/lint-autofix.yml`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-01T10:11:45Z on `.github/workflows/lint-autofix.yml`:116 (association: NONE)

  <pre>
  This resets `src/Core.TypeScript/bun.lock`, but the lockfile is at the repo root (`bun.lock`). Also, because the prettier step uses `bun install --frozen-lockfile`, it shouldn’t be modifying the lockfile at all, so forcibly checking out a lockfile here is unnecessary and can unintentionally discard legitimate dependency updates if this path is ever corrected.
  </pre>

### Thread 4 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GFKeo`
- **Path**: `.github/workflows/lint-autofix.yml`
- **Replies**: 0

**Initial comment:**

- **`github-advanced-security[bot]` (bot)** at 2026-06-01T10:22:19Z on `.github/workflows/lint-autofix.yml`:53 (association: NONE)

  <pre>
  ## CodeQL / Checkout of untrusted code in trusted context

  Potential unsafe checkout of untrusted pull request on privileged workflow.

  [Show more details](https://github.com/Lucent-Financial-Group/Zeta/security/code-scanning/136)
  </pre>

### Thread 5 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GFM2A`
- **Path**: `.github/workflows/lint-autofix-apply.yml`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-01T10:24:41Z on `.github/workflows/lint-autofix-apply.yml`:46 (association: NONE)

  <pre>
  P0: `gh run download` calls the Actions API, but this job’s explicit `permissions` only grants `contents: write`. With omitted scopes set to `none`, the download step will 403 and the patch will never apply. Add `actions: read` to the job permissions.
  </pre>

### Thread 6 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GFM21`
- **Path**: `.github/workflows/lint-autofix-apply.yml`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-01T10:24:42Z on `.github/workflows/lint-autofix-apply.yml`:57 (association: NONE)

  <pre>
  P1: The apply job checks out the moving branch tip (`head_branch`), but the patch artifact was produced from a specific commit (`workflow_run.head_sha`). If new commits land between produce/apply, `git apply` can fail or (worse) apply to a different base than intended. Prefer checking out the immutable `head_sha` and then pushing back to `head_branch`.
  </pre>

### Thread 7 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GFM3S`
- **Path**: `.github/workflows/lint-autofix-apply.yml`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-01T10:24:42Z on `.github/workflows/lint-autofix-apply.yml`:86 (association: NONE)

  <pre>
  P2: The commit message claims both markdownlint and prettier ran, but the producer workflow explicitly defers prettier. This will mislead when auditing why a heal commit was created.
  </pre>

### Thread 8 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GFM3t`
- **Path**: `.github/workflows/lint-autofix.yml`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-01T10:24:42Z on `.github/workflows/lint-autofix.yml`:60 (association: NONE)

  <pre>
  P1: For determinism and to avoid TOCTOU with a moving branch ref, prefer checking out the immutable PR head SHA rather than `head.ref` when generating the patch artifact.
  </pre>

### Thread 9 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GFM4T`
- **Path**: `docs/DECISIONS/2026-06-01-secure-github-actions-autofix-privilege-separation-two-job-split.md`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-01T10:24:43Z on `docs/DECISIONS/2026-06-01-secure-github-actions-autofix-privilege-separation-two-job-split.md`:74 (association: NONE)

  <pre>
  P1 (xref): This ADR links to `2026-06-01-ci-mechanical-lint-autoheal-and-review-emission-to-the-git-native-bus.md`, but that file does not exist under docs/DECISIONS/. Either add the missing ADR or remove the broken link to avoid dead references in the decision record.
  </pre>

### Thread 10 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GFM4k`
- **Path**: `.github/workflows/lint-autofix-apply.yml`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-01T10:24:43Z on `.github/workflows/lint-autofix-apply.yml`:46 (association: NONE)

  <pre>
  P0: `gh run download` calls the Actions API, but this job’s explicit `permissions` only grants `contents: write`. With omitted scopes set to `none`, the download step will 403 and the patch will never apply. Add `actions: read` to the job permissions.
  </pre>

### Thread 11 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GFM40`
- **Path**: `.github/workflows/lint-autofix-apply.yml`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-01T10:24:43Z on `.github/workflows/lint-autofix-apply.yml`:57 (association: NONE)

  <pre>
  P1: The apply job checks out the moving branch tip (`head_branch`), but the patch artifact was produced from a specific commit (`workflow_run.head_sha`). If new commits land between produce/apply, `git apply` can fail or (worse) apply to a different base than intended. Prefer checking out the immutable `head_sha` and then pushing back to `head_branch`.
  </pre>

### Thread 12 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GFM5G`
- **Path**: `.github/workflows/lint-autofix-apply.yml`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-01T10:24:44Z on `.github/workflows/lint-autofix-apply.yml`:86 (association: NONE)

  <pre>
  P2: The commit message claims both markdownlint and prettier ran, but the producer workflow explicitly defers prettier. This will mislead when auditing why a heal commit was created.
  </pre>

### Thread 13 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GFM5Z`
- **Path**: `.github/workflows/lint-autofix.yml`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-01T10:24:44Z on `.github/workflows/lint-autofix.yml`:60 (association: NONE)

  <pre>
  P1: For determinism and to avoid TOCTOU with a moving branch ref, prefer checking out the immutable PR head SHA rather than `head.ref` when generating the patch artifact.
  </pre>

### Thread 14 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GFM5l`
- **Path**: `docs/DECISIONS/2026-06-01-secure-github-actions-autofix-privilege-separation-two-job-split.md`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-01T10:24:44Z on `docs/DECISIONS/2026-06-01-secure-github-actions-autofix-privilege-separation-two-job-split.md`:74 (association: NONE)

  <pre>
  P1 (xref): This ADR links to `2026-06-01-ci-mechanical-lint-autoheal-and-review-emission-to-the-git-native-bus.md`, but that file does not exist under docs/DECISIONS/. Either add the missing ADR or remove the broken link to avoid dead references in the decision record.
  </pre>

### Thread 15 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GFM5y`
- **Path**: `.github/workflows/lint-autofix-apply.yml`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-01T10:24:44Z on `.github/workflows/lint-autofix-apply.yml`:46 (association: NONE)

  <pre>
  P0: `gh run download` calls the Actions API, but this job’s explicit `permissions` only grants `contents: write`. With omitted scopes set to `none`, the download step will 403 and the patch will never apply. Add `actions: read` to the job permissions.
  </pre>

### Thread 16 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GFM5_`
- **Path**: `.github/workflows/lint-autofix-apply.yml`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-01T10:24:45Z on `.github/workflows/lint-autofix-apply.yml`:57 (association: NONE)

  <pre>
  P1: The apply job checks out the moving branch tip (`head_branch`), but the patch artifact was produced from a specific commit (`workflow_run.head_sha`). If new commits land between produce/apply, `git apply` can fail or (worse) apply to a different base than intended. Prefer checking out the immutable `head_sha` and then pushing back to `head_branch`.
  </pre>

### Thread 17 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GFM6d`
- **Path**: `.github/workflows/lint-autofix-apply.yml`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-01T10:24:45Z on `.github/workflows/lint-autofix-apply.yml`:86 (association: NONE)

  <pre>
  P2: The commit message claims both markdownlint and prettier ran, but the producer workflow explicitly defers prettier. This will mislead when auditing why a heal commit was created.
  </pre>

### Thread 18 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GFM6r`
- **Path**: `.github/workflows/lint-autofix.yml`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-01T10:24:45Z on `.github/workflows/lint-autofix.yml`:60 (association: NONE)

  <pre>
  P1: For determinism and to avoid TOCTOU with a moving branch ref, prefer checking out the immutable PR head SHA rather than `head.ref` when generating the patch artifact.
  </pre>

### Thread 19 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GFM7E`
- **Path**: `docs/DECISIONS/2026-06-01-secure-github-actions-autofix-privilege-separation-two-job-split.md`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-01T10:24:45Z on `docs/DECISIONS/2026-06-01-secure-github-actions-autofix-privilege-separation-two-job-split.md`:74 (association: NONE)

  <pre>
  P1 (xref): This ADR links to `2026-06-01-ci-mechanical-lint-autoheal-and-review-emission-to-the-git-native-bus.md`, but that file does not exist under docs/DECISIONS/. Either add the missing ADR or remove the broken link to avoid dead references in the decision record.
  </pre>

## Fix commits (touching thread paths)

### `d1f24f458b0ba573ee86fa90d313bc540c61f8dc` -- 2026-06-01T10:07:06Z -- ``

**Touched files (intersect with thread paths):**

- `.github/workflows/lint-autofix.yml`

**Message:**

```
ci: mechanical lint auto-heal workflow (markdownlint --fix / prettier…

… --write)

Adds a lint-autofix workflow that runs the DETERMINISTIC --fix tools on a PR and
commits the heal back to the branch — auto-heal, not a new gate (operator
2026-06-01: "mechanical fixes or auto format fixes ... it just being a workflow
step makes sense"). Keeps the remaining required gates frictionless while branch
protection is wound down.

Mechanical only (no intelligence):
- markdownlint-cli2 --fix  → heals the `markdownlint` required gate cleanly
- prettier --write (TS/JSON) → format hygiene (not itself a gate)
Same mise-pinned versions as gate.yml (via install.sh) so the heal can't "fix"
into a state the gate rejects (version parity).

Safety:
- same-repo PRs only (forks excluded — no untrusted-ref checkout, no
  write-perms over untrusted code); pull_request (not _target).
- loop-guarded (skips github-actions[bot] commits; heal is idempotent anyway).
- head.ref used via env var + quoted in run: (injection-safe); contents:write
  scoped to the autofix job only (workflow-level is read).
- prettier gated on --frozen-lockfile + bun.lock discarded before commit, so no
  lockfile churn lands in the heal.

Re-trigger note (documented in the file): GITHUB_TOKEN pushes don't re-trigger
workflows, so while a required gate exists, set an AUTOFIX_TOKEN PAT/App-token
secret for the gate to auto-re-run on the healed commit; without it the fix
still commits and the loop/author nudges the gate. As gates are removed, plain
GITHUB_TOKEN suffices.

Intelligence-requiring fixes (actionlint logic, semgrep-without-fix, type/logic)
are a SEPARATE agent-in-Actions step (accelerator / local-LLM direction) — this
does the free zero-LLM subset; that escalates the rest. actionlint clean.
```

### `19d0d50874e8f361a8f1242f198ce1f8596eaa06` -- 2026-06-01T10:18:42Z -- ``

**Touched files (intersect with thread paths):**

- `.github/workflows/lint-autofix-apply.yml`
- `.github/workflows/lint-autofix.yml`
- `docs/DECISIONS/2026-06-01-secure-github-actions-autofix-privilege-separation-two-job-split.md`

**Message:**

```
ci(security): split lint-autofix into privilege-separated two-job pat…

…tern + security ADR

Resolves the CodeQL "checkout of untrusted code in trusted context" finding on
this PR by removing the structural pwn-request shape (operator-approved
2026-06-01; written as a security teaching piece for Max).

The single privileged job (checkout head + execute fixers + commit-back, all
with contents:write) combined PRIVILEGE and UNTRUSTED EXECUTION — the pwn-request
trap (install.sh / bun install run the PR's own lifecycle scripts under write
perms). Split into two jobs so neither holds both halves:

- lint-autofix.yml (PRODUCE, contents: read, no secrets-to-abuse): checkout head,
  run the fixers, upload the diff as an artifact. Executes PR code but has no
  privilege.
- lint-autofix-apply.yml (APPLY, on: workflow_run, contents: write): download the
  patch + git-apply + commit + push. Has privilege but executes NO PR code (git
  only). INVARIANT: never add build/test/install to the apply job.

The artifact is the trust boundary — the read-only job emits DATA, the privileged
job consumes DATA, never code. Both actionlint-clean.

docs/DECISIONS/2026-06-01-secure-github-actions-autofix-privilege-separation-two-job-split.md
teaches the why (pwn-request anatomy, pull_request vs pull_request_target, the
"no single job holds privilege + untrusted execution" principle) and resolves OQ5
of the CI-review-emission ADR (#6394). markdownlint-clean.
```

### `506d6d121822f1b9559ff4c8a23a064469c78849` -- 2026-06-01T10:20:14Z -- ``

**Touched files (intersect with thread paths):**

- `.github/workflows/lint-autofix.yml`

**Message:**

```
ci: drop buggy prettier step from autofix V1 (Copilot: pkg/lockfile a…

…re at repo root, not src/Core.TypeScript)

V1 = markdownlint --fix only (the required-gate autofix). prettier --write is
deferred to a follow-up — package.json/bun.lock live at the repo root (not
src/Core.TypeScript), so the working-directory + lockfile-discard were wrong;
prettier-format needs a deliberate scoping pass (root run + .prettierignore).
Resolves both Copilot findings; narrows path triggers to **/*.md + the workflows.
```

### `71b1e7d39dc776063d412f8796ac12aae17cada0` -- 2026-06-01T10:27:07Z -- ``

**Touched files (intersect with thread paths):**

- `.github/workflows/lint-autofix-apply.yml`
- `.github/workflows/lint-autofix.yml`
- `docs/DECISIONS/2026-06-01-secure-github-actions-autofix-privilege-separation-two-job-split.md`

**Message:**

```
ci(security): address #6393 review — actions:read (P0), immutable hea…

…d_sha checkout + ff-only push (P1 TOCTOU), persist-credentials:false (CodeQL), commit-msg/xref fixes

- P0: apply job gets actions:read (gh run download reads the producer artifact via Actions API; was 403).
- P1 (TOCTOU): produce + apply checkout the immutable head_sha (not the moving head_branch); apply pushes ff-only to head_branch (skips if moved — never force).
- CodeQL 'checkout untrusted code': produce uses persist-credentials:false (PR code runs with no token exposed; read-only, patch-only).
- P2: apply commit message drops the stale 'prettier --write' (prettier deferred).
- xref: security ADR references the CI-review-emission ADR by PR #6394 (no dead link pre-merge).
All actionlint + markdownlint clean.
```
