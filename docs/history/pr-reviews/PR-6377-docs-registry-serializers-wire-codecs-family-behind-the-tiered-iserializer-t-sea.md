# PR #6377 -- docs(registry): serializers/wire-codecs family behind the tiered ISerializer<'T> seam

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 6377 |
| Title | docs(registry): serializers/wire-codecs family behind the tiered ISerializer<'T> seam |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-06-01T08:36:32Z |
| Merged at | 2026-06-01T08:38:58Z |
| Merge commit SHA | `07bddff8e3ab29a8f870a55446a252e80c50ed5b` |
| Branch | `otto-cli/registry-serializer-roster-2026-06-01` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/6377 |
| Changed files | 1 |
| Additions / deletions | +1 / -0 |

## Description

Your call (2026-06-01): _"start pulling in serializers too, more than JSON … any over time as we need them live"_ — Arrow / Avro / gRPC / protobuf / Cap'n Proto / Thrift / Bond / JSON Schema.

**Not greenfield** — captured as a wish-list family behind the **existing tiered `ISerializer<'T>` seam** (`src/Core/Serializer.fs`, "don't pick one protocol for everything"), with **Arrow already an adapter** (`src/Core/ArrowSerializer.fs` ✅, Apache.Arrow) and the **JSON port in-flight** (🚧, ZetaJsonParser + serde_json). The new formats are **on-demand adapters** behind that seam — pulled when a use case needs them, not all at once.

Captured inline:

- **Two axes** the formats split on: row vs **columnar** (Arrow → its own batch/`RecordBatch` shape) and self-describing vs **schema-required** (protobuf/Avro/Thrift/Bond → `Schema` port + compile-time codegen, same stance as the mixin line).
- **gRPC** = protobuf wire + HTTP/2 RPC framing (composes with the Messaging RPC/Bus lines); **JSON Schema** = validation/schema-_description_, not a wire codec.
- **Boundary** (BCL-interface-boundary rule): the wire format is the de-facto-standard **spec we depend on**; the vendor **library is an adapter we wrap** — own the `ISerializer<'T>` port (own-your-interfaces; vendor import in core = lint failure).
- **Pin** (pin-only-slow-movers): wire-format specs slow-moving → pin the spec; vendor libs faster → default + adapter.
- **Consensus**: byte-level codecs → canonical-encoding **byte-diff golden-vectors** + **cross-decode round-trip** as the four-oracle verification.

Docs-only wish-list capture; prettier + markdownlint clean; BACKLOG.md untouched.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 1 |
| Resolved threads | 0 |
| Unresolved threads | 1 |
| Total review comments | 1 |
| Total fix commits (touching thread paths) | 1 |

## Review threads

### Thread 1 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM6GDdWN`
- **Path**: `docs/PRIMITIVE-REGISTRY.md`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-01T08:39:16Z on `docs/PRIMITIVE-REGISTRY.md`:47 (association: NONE)

  <pre>
  This bullet has a couple of factual mismatches with the current codebase:
  - Arrow *is already* implemented behind `ISerializer&lt;'T&gt;` (`src/Core/ArrowSerializer.fs` implements `ISerializer&lt;int64&gt;`), so saying Arrow is “not the message-codec port” is misleading.
  - `ZetaJsonParser` appears to be a Rust implementation detail (no TS/F#/C# hits), so calling it “four-lang `ZetaJsonParser`” is inaccurate; it’s the JSON *port* that’s four-language.

  Recommend rewording to keep the “row vs columnar” axis while acknowledging Arrow is adapted behind `ISerializer&lt;'T&gt;` today, and to describe JSON as a four-language port with Rust’s `ZetaJsonParser` + `serde_json` adapter (matching the JSON row in the table below).
  </pre>

## Fix commits (touching thread paths)

### `ac35cfe3b3901141738747ff77cb6a324e83a53b` -- 2026-06-01T08:36:28Z -- ``

**Touched files (intersect with thread paths):**

- `docs/PRIMITIVE-REGISTRY.md`

**Message:**

```
docs(registry): serializers/wire-codecs family behind the tiered ISer…

…ializer<'T> seam (maintainer)

Maintainer 2026-06-01: 'start pulling in serializers too, more than JSON ... any over time
as we need them live' (Arrow/Avro/gRPC/protobuf/CapnProto/Thrift/Bond/JSON-Schema).

Not greenfield — captured as a wish-list family behind the EXISTING tiered ISerializer<'T>
seam (src/Core/Serializer.fs) with Arrow already an adapter (src/Core/ArrowSerializer.fs ✅)
and JSON port in-flight (🚧). New formats are on-demand adapters. Captured the two-axis split
(row vs columnar; self-describing vs schema-required), gRPC = protobuf + RPC framing, JSON
Schema = validation not wire codec, the BCL-boundary discipline (wire spec = depend; vendor
lib = wrap), pin-only-slow-movers (spec slow → pin; lib faster → default+adapter), and the
four-oracle byte-diff-golden-vector + cross-decode-round-trip consensus angle.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```
