# PR #2512 -- feat(B-0170): add ADR supersession convention checker

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 2512 |
| Title | feat(B-0170): add ADR supersession convention checker |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-05-10T16:40:18Z |
| Merged at | 2026-05-10T17:25:52Z |
| Merge commit SHA | `db1c70e33029666d0f17bf3cee9396e4446b6173` |
| Branch | `claim/task-b0170-convention-drift-check` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/2512 |
| Changed files | 3 |
| Additions / deletions | +943 / -5 |

## Description

## Summary
- add the B-0170 v0.9 convention-drift checker for ADR supersession claims
- require superseded ADRs to carry a reciprocal top-of-file `Superseded by` marker naming the superseding ADR
- add focused Bun tests and README wiring for the new checker

## Verification
- `bun test tools/substrate-claim-checker/check-convention.test.ts`
- `bun run typecheck`
- `bunx eslint tools/substrate-claim-checker/check-convention.ts tools/substrate-claim-checker/check-convention.test.ts`
- `bun tools/substrate-claim-checker/check-convention.ts docs/DECISIONS/2026-04-21-router-coherence-v2.md`
- `git diff --check`

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | true |
| Total threads | 20 |
| Resolved threads | 20 |
| Unresolved threads | 0 |
| Total review comments | 37 |
| Total fix commits (touching thread paths) | 14 |

## Review threads

### Thread 1 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6A6iuX`
- **Path**: `tools/substrate-claim-checker/check-convention.ts`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-10T16:42:43Z on `tools/substrate-claim-checker/check-convention.ts`:143 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Match supersession claims when Markdown wraps lines**

  This checker evaluates each line independently, so a valid ADR claim split across lines (for example, `Supersedes ADR` on one line and the target path on the next) is never parsed. In hard-wrapped Markdown, that causes the supersession claim to be missed entirely and the reciprocal `Superseded by` requirement is not enforced, creating a false-negative in the convention-drift gate.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-10T16:46:03Z on `tools/substrate-claim-checker/check-convention.ts`:143 (association: MEMBER)

    <pre>
    Fixed in b87ee894: the checker now scans adjacent non-fenced Markdown prose as logical lines, so hard-wrapped `Supersedes ADR` claims still reach the target parser. Added regression coverage for wrapped Markdown links and wrapped backtick targets.
    </pre>

### Thread 2 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6A6iuY`
- **Path**: `tools/substrate-claim-checker/check-convention.ts`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-10T16:42:43Z on `tools/substrate-claim-checker/check-convention.ts`:240 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Strip Markdown link titles from supersession targets**

  The link matcher captures the full link destination payload, so valid Markdown links with titles (e.g. `[v1](old.md "title")`) yield a target string that includes the title text. Because `normalizeTarget` does not remove titles, path resolution fails and the claim is treated as unresolved, which silently bypasses convention checking for that supersession claim.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-10T16:46:04Z on `tools/substrate-claim-checker/check-convention.ts`:240 (association: MEMBER)

    <pre>
    Fixed in b87ee894: `normalizeTarget` now strips Markdown link titles before query/anchor handling and path resolution. Added regression coverage for `[v1](old.md "title")`.
    </pre>

### Thread 3 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6A6i0Z`
- **Path**: `docs/claims/task-b0170-convention-drift-check.md`
- **Replies**: 2

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-10T16:43:09Z on `docs/claims/task-b0170-convention-drift-check.md`:19 (association: NONE)

  <pre>
  P1: Claim files under docs/claims are meant to be temporary and should be deleted in the same PR that lands the work (release step). Keeping this file in the PR would leave a completed claim on main; please remove it (git rm) as part of this PR.

  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-10T16:47:51Z on `docs/claims/task-b0170-convention-drift-check.md`:19 (association: MEMBER)

    <pre>
    Fixed in 84d8bc40: claim file deleted via git rm. Release step is now part of this PR.
    </pre>
  - **`AceHack` (human)** at 2026-05-10T16:48:58Z on `docs/claims/task-b0170-convention-drift-check.md`:19 (association: MEMBER)

    <pre>
    Fixed in 84d8bc40: the claim file was removed from the PR as the release step, so it will not land on main as a completed active claim.
    </pre>

### Thread 4 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6A6i0j`
- **Path**: `tools/substrate-claim-checker/check-convention.ts`
- **Replies**: 3

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-10T16:43:09Z on `tools/substrate-claim-checker/check-convention.ts`:78 (association: NONE)

  <pre>
  P1: Unlike check-existence.ts, this checker will follow absolute paths / Windows drive paths / URLs if they appear in a Supersedes link and will attempt to stat/read them. Since ADR supersession should only reference repo-local ADR files, consider filtering targets to repo-relative paths (and ignoring absolute/URL targets) to avoid surprising reads outside the repo and to match the other checkers’ path hygiene.

  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-10T16:50:01Z on `tools/substrate-claim-checker/check-convention.ts`:78 (association: MEMBER)

    <pre>
    Fixed in 84d8bc40: added `isRepoRelativeTarget()` to reject URLs (`http://`/`https://`) and absolute paths before resolution, and `isInsideRepo()` to verify the resolved path stays within the repo root — mirrors the pattern in `check-existence.ts`.
    </pre>
  - **`AceHack` (human)** at 2026-05-10T16:51:15Z on `tools/substrate-claim-checker/check-convention.ts`:78 (association: MEMBER)

    <pre>
    Leaving this open for one more pass. I verified 84d8bc40 adds a repo-root guard and filters POSIX absolute/http(s) targets, but the local B-0170 worktree is currently behind the remote branch and dirty in this same path set with overlapping path-hygiene edits. Next safe step is to reconcile that worktree without reset/checkout, then either tighten the target filter coverage or resolve this thread from a clean branch state.
    </pre>
  - **`AceHack` (human)** at 2026-05-10T16:57:57Z on `tools/substrate-claim-checker/check-convention.ts`:78 (association: MEMBER)

    <pre>
    Fixed in 4fc64d90: supersession target parsing now rejects empty targets, POSIX absolute paths, Windows drive paths, UNC paths, protocol-relative URLs, and any URI-scheme target before claim collection or target resolution; resolution uses `resolve(...)` plus a repo-root containment guard.
    </pre>

### Thread 5 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6A6kb7`
- **Path**: `tools/substrate-claim-checker/check-convention.ts`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-10T16:49:31Z on `tools/substrate-claim-checker/check-convention.ts`:236 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Match superseding ADR name exactly in marker lines**

  `markerNamesSupersedingAdr` currently uses `line.includes(basename)` so a wrong reciprocal link can be accepted when the wrong filename merely contains the expected basename as a substring (for example `renew.md` satisfies `new.md`). In that case the checker reports no convention drift even though the old ADR does not actually name the superseding ADR, which defeats the reciprocal-check guarantee for certain filename pairs.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-10T16:57:59Z on `tools/substrate-claim-checker/check-convention.ts`:236 (association: MEMBER)

    <pre>
    Fixed in 4fc64d90: reciprocal marker matching now treats the superseding ADR basename as an exact filename token instead of accepting substring matches.
    </pre>

### Thread 6 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6A6k1o`
- **Path**: `tools/substrate-claim-checker/README.md`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-10T16:50:52Z on `tools/substrate-claim-checker/README.md`:77 (association: NONE)

  <pre>
  The section lists check-types marked as “shipped v0.5/v0.7/v0.9” under a heading “What this does NOT do (v0)”, which is contradictory now that those checkers exist. Please rename/restructure this list so shipped check-types aren’t presented as unsupported (or clarify that the section is only about the original v0 count-drift checker).
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-10T16:57:59Z on `tools/substrate-claim-checker/README.md`:77 (association: MEMBER)

    <pre>
    Fixed in 4fc64d90: the README section is now `Drift classes`, separating shipped classes from v1 candidates instead of listing shipped checks under a v0 unsupported heading.
    </pre>

### Thread 7 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6A6k1w`
- **Path**: `tools/substrate-claim-checker/check-convention.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-10T16:50:52Z on `tools/substrate-claim-checker/check-convention.ts`:190 (association: NONE)

  <pre>
  `logicalSearchLines` assigns the claim’s `line` to the first line of the paragraph. In typical ADRs the “Date” and “Status” metadata lines are adjacent, so a `Supersedes ADR ...` on the Status line will be reported at the Date line number, which makes findings hard to locate. Consider preserving a mapping from merged text offsets back to physical line numbers, or scanning per-line (with a small lookahead for hard-wrapped cases) so the reported line is the actual claim line.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-10T16:58:00Z on `tools/substrate-claim-checker/check-convention.ts`:190 (association: MEMBER)

    <pre>
    Fixed in 4fc64d90: hard-wrap support now scans physical lines plus adjacent wrapped continuations, so findings retain the actual `Supersedes ADR` line rather than the first line of a metadata paragraph.
    </pre>

### Thread 8 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6A6k12`
- **Path**: `tools/substrate-claim-checker/check-convention.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-10T16:50:53Z on `tools/substrate-claim-checker/check-convention.ts`:249 (association: NONE)

  <pre>
  `markerNamesSupersedingAdr` uses `line.includes(basename)` which can produce false negatives/positives when the marker line contains a different filename that happens to include the basename as a substring (e.g., `new.md.bak`). Consider matching the basename as a complete path token (e.g., within backticks / markdown link destination) or using a word-boundary style regex to require an exact filename match.

  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-10T16:58:01Z on `tools/substrate-claim-checker/check-convention.ts`:249 (association: MEMBER)

    <pre>
    Fixed in 4fc64d90: marker matching now requires an exact basename token, so `renew.md` or `new.md.bak` no longer satisfy `new.md`. Added regression coverage for that case.
    </pre>

### Thread 9 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6A6lzM`
- **Path**: `tools/substrate-claim-checker/check-convention.ts`
- **Replies**: 2

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-10T16:54:58Z on `tools/substrate-claim-checker/check-convention.ts`:244 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Parse wrapped Superseded-by marker as one logical line**

  The reciprocal check only keeps lines that literally contain `superseded by` in the first 25 lines, so a valid top-of-file marker that is hard-wrapped (e.g., `Superseded by` on one line and the `new.md` link on the next) loses the target line before name matching runs. In repositories with wrapped Markdown, this produces false convention-drift findings and can fail the checker even when reciprocity is correctly documented.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-10T17:01:46Z on `tools/substrate-claim-checker/check-convention.ts`:244 (association: MEMBER)

    <pre>
    Fixed in fc21a0b3: `supersededByMarkerLines` now treats a `Superseded by` line plus its immediate nonblank continuation as one marker line, so hard-wrapped top-of-file reciprocal markers keep the target link during exact-name matching. Added a blockquote hard-wrap regression test.
    </pre>
  - **`AceHack` (human)** at 2026-05-10T17:03:06Z on `tools/substrate-claim-checker/check-convention.ts`:244 (association: MEMBER)

    <pre>
    Fixed in fc21a0b3: `supersededByMarkerLines` now looks ahead to the next non-empty line whenever it finds a 'Superseded by' line, so hard-wrapped reciprocal markers (phrase on one line, filename link on the next) are correctly recognized. Regression test added in the same commit ("accepts hard-wrapped reciprocal top-of-file marker").
    </pre>

### Thread 10 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6A6lzN`
- **Path**: `tools/substrate-claim-checker/check-convention.ts`
- **Replies**: 2

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-10T16:54:58Z on `tools/substrate-claim-checker/check-convention.ts`:251 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Remove substring fallback when matching superseding ADR**

  The reciprocal-name check falls back to `includes`, so a wrong marker can pass if it merely contains the expected basename as a substring (for example `renew.md` satisfies `new.md`). That creates false negatives where the old ADR points to the wrong superseding document but the checker still reports no drift.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-10T16:58:46Z on `tools/substrate-claim-checker/check-convention.ts`:251 (association: MEMBER)

    <pre>
    Fixed in 4fc64d90: the substring fallback was removed. Marker matching now requires an exact basename token, with regression coverage for `renew.md` / `new.md.bak` not satisfying `new.md`.
    </pre>
  - **`AceHack` (human)** at 2026-05-10T17:03:13Z on `tools/substrate-claim-checker/check-convention.ts`:251 (association: MEMBER)

    <pre>
    Fixed in 4fc64d90: `markerNamesSupersedingAdr` now uses a token-boundary regex `(^|[^A-Za-z0-9._-])basename($|[^A-Za-z0-9._-])` via `escapeRegExp` — dropping the old `.includes()` fallback. A dedicated test was added ("flags reciprocal marker that only contains the ADR basename as a substring") to cover the `renew.md`/`new.md.bak` false-positive case.
    </pre>

### Thread 11 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6A6nNf`
- **Path**: `tools/substrate-claim-checker/check-convention.ts`
- **Replies**: 2

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-10T17:01:02Z on `tools/substrate-claim-checker/check-convention.ts`:208 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Strip blockquote/list prefixes from wrapped claim continuations**

  The wrapped-line recovery appends `next.text` verbatim, so Markdown continuations that repeat structural prefixes (for example `&gt; [v1](old.md)` in blockquotes or `- [v1](old.md)` in lists) become `Supersedes ADR &gt; [v1](...)` and no longer match the claim regex. In those documents, valid supersession claims are silently missed and the reciprocal `Superseded by` check is skipped, producing false negatives for convention drift.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-10T17:05:39Z on `tools/substrate-claim-checker/check-convention.ts`:208 (association: MEMBER)

    <pre>
    Fixed in bb0b2037: added `stripMarkdownStructuralPrefix` (strips `&gt;`, `- `, `* `, `+ `, and ordered-list prefixes) and applied it to the continuation text in both `logicalSearchLines` and `supersededByMarkerLines` before joining. Blockquote-prefixed continuations now produce clean joined lines that match the claim regexes. Regression tests added for both paths.
    </pre>
  - **`AceHack` (human)** at 2026-05-10T17:07:22Z on `tools/substrate-claim-checker/check-convention.ts`:208 (association: MEMBER)

    <pre>
    Verified and resolved in bb0b2037: the branch now strips Markdown structural prefixes from wrapped Supersedes ADR continuations before joining, and uses the same helper for wrapped Superseded by markers. Focused checks passed locally on the remote head: bun test tools/substrate-claim-checker/check-convention.test.ts; bun run typecheck; bunx eslint tools/substrate-claim-checker/check-convention.ts tools/substrate-claim-checker/check-convention.test.ts; bun tools/substrate-claim-checker/check-convention.ts docs/DECISIONS/2026-04-21-router-coherence-v2.md; git diff --check.
    </pre>

### Thread 12 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6A6n9i`
- **Path**: `tools/substrate-claim-checker/check-convention.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-10T17:04:12Z on `tools/substrate-claim-checker/check-convention.ts`:217 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Parse backtick supersession targets with anchors**

  Extend the backtick claim regex to accept `*.md` targets that include `#...` or `?...` suffixes, because the current pattern only matches when the backtick closes immediately after `.md`. In ADRs that write `Supersedes ADR `old.md#section`` (or query variants), the claim is skipped entirely, so the reciprocal `Superseded by` check is never enforced even though `normalizeTarget` already strips anchors/queries.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 13 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6A6n9j`
- **Path**: `tools/substrate-claim-checker/check-convention.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-10T17:04:12Z on `tools/substrate-claim-checker/check-convention.ts`:282 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Exclude fenced blocks from Superseded-by marker scan**

  Filter out fenced-code regions when collecting top-of-file `Superseded by` markers, because this loop currently treats any matching text in the first 25 lines as authoritative marker content. If an old ADR has a code example near the top containing `superseded by ...new.md`, the checker can falsely accept reciprocity and miss convention drift even though no real marker exists in prose.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 14 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6A6orB`
- **Path**: `tools/substrate-claim-checker/check-convention.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-10T17:07:17Z on `tools/substrate-claim-checker/check-convention.ts`:221 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Parse anchored backtick supersession targets**

  Update the backtick matcher so it accepts `*.md` targets that include `#...` or `?...` before the closing backtick; as written it only matches when the backtick closes immediately after `.md`. In ADRs that write claims like ``Supersedes ADR `old.md#section``` the claim is skipped entirely, so the reciprocal `Superseded by` convention check never runs for that supersession.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 15 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6A6orD`
- **Path**: `tools/substrate-claim-checker/check-convention.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-10T17:07:17Z on `tools/substrate-claim-checker/check-convention.ts`:255 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Ignore fenced code when scanning Superseded-by markers**

  Exclude fenced code blocks in the top-of-file marker scan, because this loop currently treats any `superseded by` text in the first 25 lines as a valid reciprocal marker. If an ADR has an early code example containing that phrase and a filename, `markerNamesSupersedingAdr` can pass even though no real prose marker exists, causing a false negative in convention-drift detection.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 16 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6A6pOC`
- **Path**: `tools/substrate-claim-checker/check-convention.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-10T17:09:45Z on `tools/substrate-claim-checker/check-convention.ts`:288 (association: NONE)

  <pre>
  P1: `supersededByMarkerLines` treats any occurrence of the phrase “superseded by” in the first 25 lines as the required reciprocal marker, and it also pulls in the following line regardless of whether it’s a marker continuation. This can (a) accept non-marker prose (e.g., a Status line) as satisfying the convention, and (b) accidentally pass if the next unrelated line contains the superseding ADR filename. Consider tightening this to only recognize an explicit top-of-file `**Superseded by**` marker (optionally blockquoted) and only concatenate true continuation lines (e.g., adjacent blockquote lines).

  </pre>

### Thread 17 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6A6qvI`
- **Path**: `tools/substrate-claim-checker/check-convention.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-10T17:16:16Z on `tools/substrate-claim-checker/check-convention.ts`:271 (association: NONE)

  <pre>
  P1: `supersededByMarkerLines` treats any line containing "superseded by" in the first 25 lines as a valid reciprocal marker. This can silently accept ADRs that only mention “superseded by …” in a Status sentence (or other prose) without the canonical top-of-file marker form (e.g. `&gt; **Superseded by** ...`). Tighten the matcher to only count actual marker lines (optionally hard-wrapped) so the checker enforces the convention it claims to check.
  </pre>

### Thread 18 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6A6qvP`
- **Path**: `tools/substrate-claim-checker/check-convention.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-10T17:16:16Z on `tools/substrate-claim-checker/check-convention.ts`:324 (association: NONE)

  <pre>
  P1: When reading a referenced (superseded) ADR fails, `checkFile` currently prints an error in `readFile(...)` but then silently `continue`s and still returns `{ ok: true }`, so `main()` can exit 0 even though the run had I/O errors and skipped checks. Consider treating target-read failures as input errors (propagate `ok=false` / increment an error counter) so the exit code reliably signals an incomplete/failed check run.
  </pre>

### Thread 19 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6A6qvY`
- **Path**: `tools/substrate-claim-checker/check-convention.test.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-10T17:16:16Z on `tools/substrate-claim-checker/check-convention.test.ts`:188 (association: NONE)

  <pre>
  A regression test is missing for the intended convention: an ADR that only contains a Status-line mention like "... superseded by new.md" (and even names the file) but lacks the canonical top-of-file `&gt; **Superseded by** ...` marker should still be flagged. Adding this test will prevent the marker parser from drifting toward accepting non-marker prose as a valid reciprocal marker.
  </pre>

### Thread 20 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6A6rCE`
- **Path**: `tools/substrate-claim-checker/check-convention.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-10T17:17:41Z on `tools/substrate-claim-checker/check-convention.ts`:264 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Handle tilde fences in top-of-file marker pre-scan**

  `supersededByMarkerLines` only toggles `inFence` for lines starting with triple backticks, so `~~~` fenced blocks are treated as normal content during the initial 25-line collection. In ADRs that begin with a longer `~~~` code sample, those code lines can consume the 25-line budget and prevent a real prose `Superseded by` marker (just below the fence) from being seen, producing a false convention-drift failure even when reciprocity is correctly documented.

  Useful? React with 👍 / 👎.
  </pre>

## Fix commits (touching thread paths)

### `0749e66f6b9ef28e1bf9adcbea34068b3e95d2b3` -- 2026-05-10T07:22:12Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `docs/claims/task-b0170-convention-drift-check.md`

**Message:**

```
claim: task-b0170-convention-drift-check - B-0170 convention-drift ch…

…ecker for ADR supersession markers

Co-Authored-By: Codex <noreply@openai.com>
```

### `e3ed52558ac74e97aad3dc38cb93cff6aaf61e97` -- 2026-05-10T16:39:52Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/substrate-claim-checker/README.md`
- `tools/substrate-claim-checker/check-convention.test.ts`
- `tools/substrate-claim-checker/check-convention.ts`

**Message:**

```
feat(B-0170): add ADR supersession convention checker

Add the v0.9 convention-drift checker slice for ADR supersession claims. The checker requires superseded ADRs to carry a reciprocal top-of-file marker naming the superseding ADR, with focused Bun tests and README wiring.

Co-Authored-By: Codex <noreply@openai.com>
```

### `b87ee8943cced67c722da070a2f6d924adf7c5a9` -- 2026-05-10T16:45:22Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/substrate-claim-checker/check-convention.test.ts`
- `tools/substrate-claim-checker/check-convention.ts`

**Message:**

```
fix(B-0170): catch wrapped ADR supersession links

Handle hard-wrapped supersession prose when scanning ADR convention claims and strip Markdown link titles before target resolution.

Co-Authored-By: Codex <noreply@openai.com>
```

### `84d8bc403e18069e49b14e90f89d59af477dd659` -- 2026-05-10T16:47:10Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `docs/claims/task-b0170-convention-drift-check.md`
- `tools/substrate-claim-checker/check-convention.ts`

**Message:**

```
fix(B-0170): address P1 review findings — path hygiene + claim release

- filter absolute paths and URLs in resolveTarget so the checker never
  follows paths outside the repo (mirrors check-existence.ts pattern)
- add isInsideRepo guard to ensure resolved paths stay within repo root
- git rm the claim file (claim release step belongs in the landing PR)

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>
```

### `a4ce32414289ba9d9d7a51991b34f6b1eaacc63f` -- 2026-05-10T16:53:00Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/substrate-claim-checker/check-convention.ts`

**Message:**

```
fix(B-0170): improve markerNamesSupersedingAdr basename matching (wor…

…d-boundary + case-insensitive) to address P2 false-negative review finding

Co-Authored-By: Grok <noreply@x.ai>
Co-authored-by: Cursor <cursoragent@cursor.com>
```

### `4fc64d90e023ca52886b0d344926338755a80386` -- 2026-05-10T16:57:23Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/substrate-claim-checker/README.md`
- `tools/substrate-claim-checker/check-convention.test.ts`
- `tools/substrate-claim-checker/check-convention.ts`

**Message:**

```
fix(B-0170): close convention checker review gaps

Tighten ADR supersession target hygiene, preserve physical line numbers for hard-wrapped claims, match reciprocal marker filenames exactly, and clarify shipped drift classes in the checker README.

Co-Authored-By: Codex <noreply@openai.com>
```

### `fc21a0b3c36ff52a21d8e07dabc14ff27f825abd` -- 2026-05-10T17:01:29Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/substrate-claim-checker/check-convention.test.ts`
- `tools/substrate-claim-checker/check-convention.ts`

**Message:**

```
fix(B-0170): parse wrapped superseded-by markers

Treat a top-of-file Superseded by line plus its immediate nonblank continuation as one marker line so hard-wrapped reciprocal ADR links are recognized.

Co-Authored-By: Codex <noreply@openai.com>
```

### `bb0b20374353cdf41ad03f5a7659fe1ef11d0f79` -- 2026-05-10T17:05:25Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/substrate-claim-checker/check-convention.test.ts`
- `tools/substrate-claim-checker/check-convention.ts`

**Message:**

```
fix(B-0170): strip blockquote/list prefixes from wrapped claim contin…

…uations

When a Supersedes ADR claim is split across lines and the continuation
starts with a blockquote marker (>), list marker (-, *, +), or ordered-
list prefix (1.), the naïve join produced "Supersedes ADR > [v1](old.md)"
which the claim regex could not match. Strip structural Markdown prefixes
from the continuation before joining.

Same fix applied to supersededByMarkerLines so the reciprocal check is
consistent. Regression tests added for both paths.

Addresses PRRT_kwDOSF9kNM6A6nNf (P2 Codex finding, check-convention.ts:208).

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>
```

### `113a462c4a75ae215ccb314b368c2aceb5f5f80c` -- 2026-05-10T17:10:10Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/substrate-claim-checker/check-convention.test.ts`
- `tools/substrate-claim-checker/check-convention.ts`

**Message:**

```
fix(B-0170): parse anchored backtick supersession targets

Allow backtick Supersedes ADR claims to include #anchor or ?query suffixes before the closing backtick. normalizeTarget already strips those suffixes for repo-local resolution, so the reciprocal Superseded by check now runs for anchored claims.

Co-Authored-By: Codex <noreply@openai.com>
```

### `6307e49ba8502bdfb3eb466cbb8affaf39a485b7` -- 2026-05-10T17:10:57Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/substrate-claim-checker/check-convention.test.ts`
- `tools/substrate-claim-checker/check-convention.ts`

**Message:**

```
fix(B-0170): anchored backtick targets + fenced-code exclusion in mar…

…ker scan

- extend backtick claim regex to accept `.md#anchor` / `.md?query` suffixes
  before the closing backtick; `normalizeTarget` already strips anchors/queries
  so path resolution is unaffected
- add fence-state tracking to `supersededByMarkerLines` (reuses existing
  `updateFenceState`/`startsFenceDelimiter` helpers) so a `superseded by` text
  inside a fenced code block in the first 25 lines is not accepted as a valid
  reciprocal marker
- add regression tests: backtick-with-anchor, backtick-with-query,
  fenced-code false-positive in marker scan (21 tests, all pass)

Fixes threads from chatgpt-codex-connector + Copilot on PR #2512.

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>
```

### `1519d8e776fab5cc4511c432ac18361c232414b6` -- 2026-05-10T17:12:46Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/substrate-claim-checker/check-convention.ts`

**Message:**

```
fix(B-0170): exclude fenced code blocks from superseded-by marker sca…

…n (P2 review finding)

- supersededByMarkerLines now skips ``` fenced regions when collecting top 25 lines
- prevents false markers from code examples containing 'superseded by'
- addresses open P2 thread on fenced exclusion

Co-Authored-By: Grok <noreply@x.ai>
Co-authored-by: Cursor <cursoragent@cursor.com>
```

### `6daf777ae7e04b5b5170b0f4e73716bc7be5053d` -- 2026-05-10T17:14:40Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/substrate-claim-checker/check-convention.test.ts`
- `tools/substrate-claim-checker/check-convention.ts`

**Message:**

```
fix(B-0170): guard superseded-by continuation against unrelated prose…

… lines

`supersededByMarkerLines` previously joined any adjacent non-empty line when
the current line matched `\bsuperseded by\b/i`, which could accept natural
prose continuations (e.g., "was superseded by\nnew.md conventions...") as a
valid reciprocal marker.

Fix: pull in the continuation only when it looks like a hard-wrapped file
reference — i.e., after stripping structural prefixes (blockquote `>`, list
`-`/`*`/`+`) the line starts with `` ` ``, `[`, or `<`. This preserves the
intended hard-wrap pattern while excluding plain prose continuations.

Added regression test: prose continuation after "superseded by" no longer
satisfies the reciprocal check (23 tests, all pass).

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>
```

### `1f4cb06b24561f5a9e95b6b0af1ff714b983f141` -- 2026-05-10T17:17:57Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/substrate-claim-checker/check-convention.test.ts`
- `tools/substrate-claim-checker/check-convention.ts`

**Message:**

```
fix(B-0170): require explicit superseded-by markers

Tighten the reciprocal marker scan so only explicit **Superseded by** marker lines, optionally blockquoted, are accepted. Wrapped marker continuations are only joined across adjacent blockquote lines, preventing status/prose lines from satisfying the convention by accident.

Co-Authored-By: Codex <noreply@openai.com>
```

### `f415accfc51634bb9cf7050c923fc2bb03a67f37` -- 2026-05-10T17:21:24Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/substrate-claim-checker/check-convention.test.ts`
- `tools/substrate-claim-checker/check-convention.ts`

**Message:**

```
fix(B-0170): propagate target-read errors in checkFile + add P1 regre…

…ssion tests

Addresses 4 Copilot/Codex unresolved review threads on PR #2512:

Thread PRRT_kwDOSF9kNM6A6qvP (P1): checkFile silently continued and
returned ok:true when readFile(resolved) failed for a referenced target
ADR. hadTargetErrors now propagates to ok:false so the caller and main()
can exit non-zero. main() updated to emit findings even when ok:false so
partial results are not suppressed alongside the error.

Thread PRRT_kwDOSF9kNM6A6qvY (P1 test gap): regression test added for
ADR whose Status line says "Superseded by new.md" (names the file in
prose) but lacks the canonical top-of-file bold-blockquote marker form
— checker must still flag it. Also added EISDIR target-read test.

Thread PRRT_kwDOSF9kNM6A6rCE (P2): regression test added for tilde-
fenced (~~~) "Superseded by" text in the top-of-file marker scan —
updateFenceState already handles ~ fences; test confirms it.

Thread PRRT_kwDOSF9kNM6A6qvI (P1): isSupersededByMarkerLine already
requires **Superseded by** bold-prefix — existing tests cover it;
reply + resolve closes the thread.

28 tests pass, 0 fail.

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>
```
