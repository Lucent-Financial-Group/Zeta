# PR #9182 -- feat(llmtv): Reticulum broadcast wiring — the society's minds on the mesh, one-way (shadow*)

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 9182 |
| Title | feat(llmtv): Reticulum broadcast wiring — the society's minds on the mesh, one-way (shadow*) |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-07-02T18:41:48Z |
| Merged at | 2026-07-02T18:41:57Z |
| Merge commit SHA | `06a551e531e6bbc3a1a4c3aa486bd5025c879020` |
| Branch | `shadow/llmtv-reticulum-broadcast-protocol-core` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/9182 |
| Changed files | 3 |
| Additions / deletions | +349 / -2 |

## Description

Aaron: *"take the Reticulum broadcast wiring next (shadow*)"* — the live multi-source feed behind the still frame `darkhall-tv.ts` renders. Transport-agnostic **protocol core**, riding the same substrate as `discovery-beacon.ts` (injected port, TEXT wire, DST-clean).

## Two load-bearing properties LLMTV requires
- **Noninterference (§13)** — the picture is **one-way out**. The wire vocabulary has only source→mesh messages (`frame`, `dark`); there is **no viewer→source variant at all**, so watching a mind cannot steer it — structurally, not by convention. A test asserts `decode` rejects any `steer`/`request` shape; the DU cannot express a back-channel.
- **Frost at the membrane** (`privacy-budget-is-hard-money`) — `frostStrip` projects a `SourceMind` before it may be published: frosted personal predictions are **dropped** (only the public veil label crosses), and `BroadcastMind` has **no field** for frosted content, so the substrate holds nothing to leak. Open by default (unfrosted personal broadcasts); frost is the earned, inviolable exception. `publishFrame` is the only way to a frame message, and it always goes through the membrane.

## Convergence & reuse
- `observeBroadcast` is **LWW-by-seq** — newer supersedes, stale/duplicate no-op — idempotent (§12) and order-independent, so out-of-order + redelivered frames **converge to the same table** (DST §7). Tested with shuffled+duplicated delivery.
- `toLlmtvTranscript` bridges the live channel table back to `darkhall-tv`, so the **live feed reuses the still-frame generator** (one IR). The grid is `dwellers.map` — 1 source and N fold on the same path (scale-free §1).

## Verified
- **45 tests pass** across discovery + darkhall (12 new); TS lint clean; markdownlint clean.

**Remaining:** the injected Reticulum/UDP `BroadcastTransport` socket impl + a `DiscoveryNode`-style runner tying discovery + broadcast (same shape as discovery's pending UDP transport).

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 2 |
| Resolved threads | 0 |
| Unresolved threads | 2 |
| Total review comments | 2 |
| Total fix commits (touching thread paths) | 1 |

## Review threads

### Thread 1 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM6N_yc9`
- **Path**: `src/Core.TypeScript/discovery/llmtv-broadcast.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-07-02T18:44:26Z on `src/Core.TypeScript/discovery/llmtv-broadcast.ts`:150 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Preserve dark tombstones to keep delivery order-independent**

  When a `dark` message with seq N deletes the channel, the fold also forgets that N was observed, so any delayed frame for the same `zid` with seq &lt;= N is accepted later because `current` is absent. For example, `frame(1), dark(2), frame(1)` ends live, while `frame(1), frame(1), dark(2)` ends dark, contradicting the documented LWW/order-independent broadcast semantics and letting stale packets resurrect retired sources; keep a per-source tombstone/high-water mark or equivalent instead of deleting all state.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 2 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM6N_ydD`
- **Path**: `src/Core.TypeScript/discovery/llmtv-broadcast.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-07-02T18:44:27Z on `src/Core.TypeScript/discovery/llmtv-broadcast.ts`:124 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Reject malformed frame payloads before casting**

  With schema-correct JSON from the mesh such as `{"schema":"zeta.llmtv.broadcast.v1","msg":{"t":"frame"}}`, `decode` returns a non-null `BroadcastMessage` even though `source`, `seq`, `frameNo`, and `mind` are missing; the next `observeBroadcast` call then throws at `msg.source.zid`. Since this decoder is the boundary for arbitrary transport bytes and promises malformed input returns `null`, validate the full `frame`/`dark` shape instead of accepting any object with the right tag.

  Useful? React with 👍 / 👎.
  </pre>

## Fix commits (touching thread paths)

### `eeb4ca21792d192d89d4a1c2850918fdcfea35da` -- 2026-07-02T18:41:24Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `src/Core.TypeScript/discovery/llmtv-broadcast.ts`

**Message:**

```
feat(llmtv): Reticulum broadcast wiring — the society's minds on the …

…mesh, one-way (shadow*)

Aaron: "take the Reticulum broadcast wiring next (shadow*)" — the live multi-source
feed behind the still frame darkhall-tv.ts renders. Transport-agnostic protocol core,
riding the same substrate as discovery-beacon.ts (injected port, TEXT wire, DST-clean).

Two load-bearing properties LLMTV requires:

- NONINTERFERENCE (§13) — the picture is ONE-WAY OUT. The wire vocabulary has only
  source→mesh messages (frame, dark); there is NO viewer→source variant at all, so
  watching a mind cannot steer it — structurally, not by convention. A test asserts
  decode rejects any "steer"/"request" shape; the DU cannot express a back-channel.
- FROST AT THE MEMBRANE (privacy-budget-is-hard-money) — frostStrip projects a
  SourceMind before it may be published: frosted personal predictions are DROPPED
  (only the public veil label crosses), and BroadcastMind has no field for frosted
  content, so the substrate holds nothing to leak. Open by default (unfrosted personal
  broadcasts); frost is the earned, inviolable exception. publishFrame is the only way
  to a frame message, and it always goes through the membrane.

The viewer fold (observeBroadcast) is LWW-by-seq: newer supersedes, stale/duplicate
no-op — idempotent (§12) and order-independent, so out-of-order + redelivered frames
converge to the same table (DST §7). toLlmtvTranscript bridges the live channel table
back to darkhall-tv, so the live feed reuses the still-frame generator (one IR). The
grid is dwellers.map — 1 source and N fold on the same path (scale-free §1).

12 new tests (45 across discovery+darkhall) pass; TS lint clean; markdownlint clean.
Remaining: the injected Reticulum/UDP BroadcastTransport socket impl + a DiscoveryNode
runner tying discovery + broadcast (same shape as discovery's pending UDP transport).

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>

AgencySignature-v1:
  persona: otto
  actor: otto-claude-code
  surface: src/Core.TypeScript/discovery
  topology: main-lane
  intent: llmtv-reticulum-broadcast-protocol-core-one-way-frost-at-membrane
  authorization: aaron-explicit-take-the-reticulum-broadcast-wiring-next
  uncertainty: low
  measure: 45-tests-pass-noninterference-and-membrane-proven-ts-lint-clean-dst-convergence-tested
  delta-u: llmtv-has-a-live-one-way-frost-safe-mesh-feed-core-reusing-the-still-frame-generator
  seed: S4
```
