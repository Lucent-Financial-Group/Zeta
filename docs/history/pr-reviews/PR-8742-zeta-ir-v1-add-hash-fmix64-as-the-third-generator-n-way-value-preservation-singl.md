# PR #8742 -- zeta-ir-v1: add hash.fmix64 as the third generator (N-way + value-preservation + single-source)

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 8742 |
| Title | zeta-ir-v1: add hash.fmix64 as the third generator (N-way + value-preservation + single-source) |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-06-20T15:59:12Z |
| Merged at | 2026-06-20T15:59:21Z |
| Merge commit SHA | `b005396d8e580c3a49721c16a8efd17fb96cea89` |
| Branch | `lumen/third-generator-fmix64` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/8742 |
| Changed files | 28 |
| Additions / deletions | +751 / -9 |

## Description

## The third generator under `zeta-ir-v1`

Proves the frozen v1 envelope **generalises beyond the seed pair** (splitmix64, fmix32) by adding a genuinely new generator — **MurmurHash3's 64-bit finalizer `fmix64`** — that fits the existing `mul`/`xorshr` grammar with **no grammar extension**:

```
x ^= x >> 33;  x *= 0xff51afd7ed558ccd;  x ^= x >> 33;  x *= 0xc4ceb9fe1a85ec53;  x ^= x >> 33;
```

i.e. `XorShr 33 · Mul · XorShr 33 · Mul · XorShr 33` at **width 64**. Constants verified against the **public-domain smhasher** source (Austin Appleby), so the vectors are externally anchored, not self-asserted. It pairs naturally with the shipped `fmix32` (same family, different width), exercising the width-parametric fold the v1 envelope introduced.

### What's wired
| Layer | Change |
|---|---|
| `GeneratorIrRegistry` | `fmix64Ir` row on the live Z-set relation (derived id `a24500e8…e78d`) |
| `ZetaIrV1` | `fmix64` binding + `known` + legacy projection (`WidthNoZetaId` shape) |
| N-way oracle (`fmix64/`) | **6 independent language ports** — TS (generated-from-IR) + F#/C#/Rust/Python/Go hand-ports — agree with each other and **10 canonical vectors** |
| Phase-B value-preservation | the v1 fold reproduces the committed legacy golden — now **3 primitives / 30 vectors** |
| Single-source byte-locks | both F# (`ZetaIrV1.Tests`) and bun (`legacy-source.test.ts`) derive `fmix64.ir.json` from the v1 envelope |

### Honest tiering

- **PROVEN:** the frozen `mul`/`xorshr` v1 envelope expresses a third, independently-specified primitive; its IR is byte-locked across 6 language oracles + canonical; folding the v1 envelope is value-preserving against the committed golden; the legacy file is single-sourced from v1.
- **NOT claimed:** the Face-3 `gen(gen)=gen` theorem itself (math team's). This widens the *evidence* that the envelope is a stable, general substrate — it is not the theorem.

### Gate

- compare.ts N-way: **13/13** (fmix64 included; Phase-B oracle now 3 primitives)
- F# ZetaIrV1 + GeneratorIrRegistry: **32/32** · bun `zeta-ir-v1-gen`: **10/10** · fmix64 `gen-ir.test.ts`: **5/5**
- `bunx tsc --noEmit`: clean (exit 0)
- Pre-existing `BenPort` heap-allocation benchmark failure (`48 vs 80`) is **unrelated** and reproduces on clean main HEAD.


## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 0 |
| Resolved threads | 0 |
| Unresolved threads | 0 |
| Total review comments | 0 |
| Total fix commits (touching thread paths) | 0 |

## Review threads

(no review threads on this PR)

## Fix commits (touching thread paths)

(no commits in this PR touched files anchored by review threads)
