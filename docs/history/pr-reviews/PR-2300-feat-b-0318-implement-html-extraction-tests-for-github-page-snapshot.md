# PR #2300 -- feat(B-0318): implement HTML extraction + tests for GitHub page snapshot

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 2300 |
| Title | feat(B-0318): implement HTML extraction + tests for GitHub page snapshot |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-05-09T12:06:42Z |
| Merged at | 2026-05-09T12:26:52Z |
| Merge commit SHA | `a23930653cec34dbfd689bbbcb49949e9d94cbe5` |
| Branch | `claim/b0318-snapshot-extraction-2026-05-09` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/2300 |
| Changed files | 2 |
| Additions / deletions | +419 / -19 |

## Description

## Summary

- Implements real extraction logic in `tools/playwright/github-ui/snapshot.ts`, replacing the empty stub from the initial slice
- Three exported pure functions: `extractToggles`, `extractFormValues`, `extractVisibleFeatures` — all take `html: string`, fully unit-testable without Playwright
- Fixes `SnapshotOptions` to extend `Pick<GitHubSessionOptions, "storageStatePath"|"driver">` — eliminates the `as any` cast and enables driver injection for tests
- Extractor signatures changed from `(page)=>Promise<…>` to `(html)=>…` (pure sync); the caller awaits `page.content()` once before dispatching to extractors
- Adds `snapshot.test.ts` with 24 unit tests (3 extractor functions × ~8 each) + 5 integration tests via `FakeDriver`/`FakeContext`/`MultiUrlFakePage` (zero real Playwright)

## Done-criteria coverage

- [x] `tools/playwright/github-ui/snapshot.ts` exists
- [x] Can snapshot `github.com/<org>/<repo>/settings` and return structured toggle states (validated by fake-page integration tests)
- [x] Output is JSON-serializable (explicit JSON roundtrip test)

## Test plan

- [x] `bun test tools/playwright/github-ui/` → 47 pass, 0 fail
- [x] `dotnet build -c Release` → 0 Warning(s), 0 Error(s)
- [ ] Live smoke: requires real `ZETA_GITHUB_STORAGE_STATE` session file (not blocked by this PR; consistent with B-0317 done-criteria precedent)

## What this PR does NOT do

- No reconciliation against expected state (B-0319)
- No mutations (B-0321)
- No richer DOM query API (would require extending `GitHubSessionPage` interface beyond `goto`/`content`/`url`)

operative-authorization: aaron 2026-05-04: "it**, not just the output. Grinding through failures + recoveries"

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | true |
| Total threads | 7 |
| Resolved threads | 7 |
| Unresolved threads | 0 |
| Total review comments | 14 |
| Total fix commits (touching thread paths) | 3 |

## Review threads

### Thread 1 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6A0WO5`
- **Path**: `tools/playwright/github-ui/snapshot.ts`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-09T12:09:41Z on `tools/playwright/github-ui/snapshot.ts`:110 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Match exact attribute names in parseAttr**

  The attribute parser uses `\b${name}\s*=` and therefore matches suffixes in hyphenated attributes (for example `data-name`, `data-id`, or `data-type`) before the real attribute. In `extractToggles`/`extractFormValues`, this can silently produce wrong keys or types when GitHub markup includes `data-*` fields on inputs, so snapshots become structurally incorrect even though parsing appears to succeed.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-09T12:25:10Z on `tools/playwright/github-ui/snapshot.ts`:110 (association: MEMBER)

    <pre>
    Fixed: replaced `\b` with `(?:^|\s)` in `parseAttr` so only whitespace-delimited attribute names match. `data-id` no longer incorrectly matches when searching for `id`.
    </pre>

### Thread 2 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6A0WO6`
- **Path**: `tools/playwright/github-ui/snapshot.ts`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-09T12:09:41Z on `tools/playwright/github-ui/snapshot.ts`:36 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Treat boolean checked attribute as present-is-true**

  The checked-state regex only treats `checked`, `checked=""`, `checked="checked"`, `checked="true"`, and `checked="1"` as true, but HTML boolean attributes are true whenever the attribute is present. For markup like `&lt;input ... checked="false"&gt;` (commonly emitted by template engines), this reports `false` even though the control is checked, which can invert extracted toggle states.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-09T12:25:12Z on `tools/playwright/github-ui/snapshot.ts`:36 (association: MEMBER)

    <pre>
    Fixed: simplified `isChecked` to `(?:^|\s)checked(?:\s|=|$)`, which matches the `checked` attribute on presence regardless of value — per the HTML spec, boolean attributes are true when present, not based on their value.
    </pre>

### Thread 3 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6A0WW4`
- **Path**: `tools/playwright/github-ui/snapshot.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-09T12:10:18Z on `tools/playwright/github-ui/snapshot.ts`:116 (association: NONE)

  <pre>
  P0: `parseAttr` uses a `\\b${name}` regex, which can incorrectly match attribute-name suffixes like `data-id=` / `data-name=` (e.g., parsing `id` will match the `id` portion of `data-id`). On GitHub pages with many `data-*` attributes this can return the wrong value and break extraction. Tighten the regex to require start-of-string or whitespace before the attribute name (and ensure the match is for the full attribute name token) so only the exact attribute is matched.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-09T12:25:14Z on `tools/playwright/github-ui/snapshot.ts`:116 (association: MEMBER)

    <pre>
    Fixed: `parseAttr` now uses `(?:^|\s)` boundary instead of `\b`, ensuring only whitespace-delimited attribute names match. Added regression tests for `data-id`/`data-name` vs `id`/`name` in both `extractToggles` and `extractFormValues`.
    </pre>

### Thread 4 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6A0WXD`
- **Path**: `tools/playwright/github-ui/snapshot.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-09T12:10:19Z on `tools/playwright/github-ui/snapshot.ts`:78 (association: NONE)

  <pre>
  P2: `extractVisibleFeatures` claims to extract "visible" headings, but it just regexes all `&lt;h2&gt;/&lt;h3&gt;` tags and cannot evaluate visibility from raw HTML (e.g., `hidden`, `aria-hidden`, or CSS). Consider renaming to reflect that it extracts headings present in the HTML, or update the docstring/field name accordingly.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-09T12:25:20Z on `tools/playwright/github-ui/snapshot.ts`:78 (association: MEMBER)

    <pre>
    Fixed: updated the docstring to clarify this is a structural scan of the HTML markup — visibility (CSS/aria-hidden) cannot be determined from static HTML alone.
    </pre>

### Thread 5 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6A0WXG`
- **Path**: `tools/playwright/github-ui/snapshot.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-09T12:10:19Z on `tools/playwright/github-ui/snapshot.ts`:36 (association: NONE)

  <pre>
  P2: The checked-state regex includes an alternative `\\s*\\/?&gt;` (inside `attrs`) but the `tagPattern` capture excludes the closing `&gt;`/`/&gt;`, so that branch can never match. Removing the unreachable branch will make the intent clearer and reduce regex complexity.

  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-09T12:25:22Z on `tools/playwright/github-ui/snapshot.ts`:36 (association: MEMBER)

    <pre>
    Fixed: the entire checked detection was rewritten as `(?:^|\s)checked(?:\s|=|$)`, which removes all prior branches including the unreachable `\/?\&gt;` alternative.
    </pre>

### Thread 6 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6A0WXK`
- **Path**: `tools/playwright/github-ui/snapshot.test.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-09T12:10:19Z on `tools/playwright/github-ui/snapshot.test.ts`:147 (association: NONE)

  <pre>
  P1: Consider adding a regression test that includes `data-id`/`data-name` attributes alongside real `id`/`name` to ensure attribute parsing doesn't accidentally match the suffix portion (e.g., `data-id` being parsed as `id`). This would have caught the current `parseAttr` boundary issue.

  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-09T12:25:25Z on `tools/playwright/github-ui/snapshot.test.ts`:147 (association: MEMBER)

    <pre>
    Fixed: added two regression tests — `does not confuse data-name with name attribute` in `extractToggles` and `does not confuse data-name with name (regression: parseAttr boundary)` in `extractFormValues`. Both verify `data-id`/`data-name` are not confused with `id`/`name`.
    </pre>

### Thread 7 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6A0Y5x`
- **Path**: `tools/playwright/github-ui/snapshot.ts`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-09T12:22:54Z on `tools/playwright/github-ui/snapshot.ts`:35 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Match `checked` as a standalone attribute**

  The checked-state detection uses `\bchecked\b`, which also matches the `checked` portion of other attribute names (for example `aria-checked="false"` or `data-checked="0"`). In those cases this branch still sets `hasChecked` to true even when the checkbox is actually unchecked, so snapshot toggle output can be inverted for real GitHub markup that carries accessibility/data attributes.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-09T12:25:28Z on `tools/playwright/github-ui/snapshot.ts`:35 (association: MEMBER)

    <pre>
    Fixed: `isChecked` now uses `(?:^|\s)checked(?:\s|=|$)` — presence-based detection that handles `checked` at end-of-string, followed by whitespace, or with any value (including `checked="false"` which the HTML spec requires to be true).
    </pre>

## Fix commits (touching thread paths)

### `83686984c071006e5c9137f099de362daf5e380a` -- 2026-05-09T12:06:25Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/playwright/github-ui/snapshot.test.ts`
- `tools/playwright/github-ui/snapshot.ts`

**Message:**

```
feat(B-0318): implement HTML extraction + tests for GitHub page snapshot

Replace the empty stub in `snapshot.ts` with three exported pure functions:
- `extractToggles(html)` — checkbox name/id → boolean from type=checkbox inputs
- `extractFormValues(html)` — name/id → value from text-like inputs
- `extractVisibleFeatures(html)` — trimmed h2/h3 text content

Fixes `SnapshotOptions` to extend `Pick<GitHubSessionOptions, "storageStatePath"|"driver">`
so the `options as any` cast is gone and driver injection works for testing.

Extractor signatures changed from `(page)=>Promise<…>` to `(html)=>…` — pure sync
functions that are independently unit-testable without Playwright.

Add `snapshot.test.ts`: 24 tests covering the three extractors and five
integration tests via `FakeDriver`/`FakeContext`/`MultiUrlFakePage` (zero real
Playwright). All 47 tests in the `github-ui` suite pass; build: 0W 0E.

Closes B-0318 done-criteria: snapshot.ts exists, returns JSON-serializable
structured data including toggle states from settings-page HTML.

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>
```

### `4ac3b6fa8ab2ab9eeadf339de9ef319f6f0a6233` -- 2026-05-09T12:20:37Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/playwright/github-ui/snapshot.ts`

**Message:**

```
fix(snapshot): tighten parseAttr regex boundary + boolean checked pre…

…sence semantics (addresses P0/P1/P2 review threads)

- parseAttr now anchors on (^|\s) to prevent suffix matches like data-id for 'id'
- extractToggles treats presence of 'checked' attr as true per HTML boolean attr rules (fixes checked="false" false-negative)
- cleaned unreachable regex branch in checked detection
- updated extractVisibleFeatures docstring for accuracy (no CSS visibility from static HTML)

Co-Authored-By: Grok <noreply@x.ai>
Co-authored-by: Cursor <cursoragent@cursor.com>
```

### `36437981b4835a8de8f78f8a727e7beedd2a0c53` -- 2026-05-09T12:24:14Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/playwright/github-ui/snapshot.test.ts`
- `tools/playwright/github-ui/snapshot.ts`

**Message:**

```
fix(B-0318): tighten parseAttr boundary + checked boolean attr + add …

…data-* regression tests

- Replace `\b` with `(?:^|\s)` in parseAttr to avoid matching hyphenated attr
  suffixes (e.g. `data-id` when seeking `id`); closes P0 finding from Copilot/Codex
- Simplify `isChecked` to `(?:^|\s)checked(?:\s|=|$)` — matches HTML boolean
  attribute presence regardless of value (fixes `checked="false"` inversion)
- Update `extractVisibleFeatures` docstring to clarify static-HTML-only scope
- Add regression tests: checked="false" treated as true, data-name vs name
  boundary isolation for both extractToggles and extractFormValues

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>
```
