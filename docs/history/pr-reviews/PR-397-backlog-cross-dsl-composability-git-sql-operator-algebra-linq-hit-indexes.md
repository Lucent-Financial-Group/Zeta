# PR #397 -- backlog: cross-DSL composability — git/SQL/operator-algebra/LINQ hit indexes

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 397 |
| Title | backlog: cross-DSL composability — git/SQL/operator-algebra/LINQ hit indexes |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-04-25T00:03:10Z |
| Merged at | 2026-04-25T00:28:17Z |
| Merge commit SHA | `0c2452adf1147d0c2e1682e495562c2e49053a2a` |
| Branch | `backlog/cross-dsl-composability` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/397 |
| Changed files | 2 |
| Additions / deletions | +299 / -0 |

## Description

## Summary

Maintainer 2026-04-24 directive — every first-class interface (git, SQL, operator algebra, LINQ, future GraphQL/blockchain/WASM-RPC) must compose with every other interface, with **full index utilization** (no full-scan fallback for composed leaves) and **retraction-preservation** end-to-end.

## Key insight captured

This row is a direct application of the **2026-04-22 semiring-parameterized Zeta substrate research** (memory: `project_semiring_parameterized_zeta_regime_change_one_algebra_to_map_others_2026_04_22.md`). "One algebra to map the others" — if the operator algebra is parameterized by a semiring, every other DSL's semantics maps into the same one algebra by semiring-swap, and **cross-DSL composability falls out for free**.

## Phased

- Phase 0 — design proposal + canonical-algebra identification + small-workload empirical verification
- Phase 1 — pairwise adapters (git ↔ SQL, SQL ↔ operator-algebra, etc.)
- Phase 2 — unified planner / binder
- Phase 3 — index-utilization audit
- Phase 4 — retraction-preservation proof / property-based verification

## Composes with

- **Closure-table hardening** (#396) — the hierarchical index this layer hits
- **Native F# git impl** (#395) — git as first-class DSL
- **Mode 2 protocol-upgrade** (#395) — fast protocol carries cross-DSL queries
- **Ouroboros bootstrap meta-thesis** (#395) — cross-DSL composability IS an Ouroboros closure (interfaces compose with themselves through the same substrate)
- **Semiring-parameterized substrate** (2026-04-22 research) — load-bearing
- **Blockchain ingest** (#394) — chain queries compose via same substrate

## Test plan

- [x] Single BACKLOG row in P2 — research-grade.
- [x] Maintainer directive verbatim preserved.
- [x] Otto-275 log-don't-implement: research scope captured.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | true |
| Total threads | 5 |
| Resolved threads | 5 |
| Unresolved threads | 0 |
| Total review comments | 10 |
| Total fix commits (touching thread paths) | 2 |

## Review threads

### Thread 1 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59iQrr`
- **Path**: `docs/BACKLOG.md`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-04-25T00:04:58Z on `docs/BACKLOG.md`:5664 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Point to an existing backlog dependency**

  This dependency note says the work composes with a “Closure-table hardening” row in the same `docs/BACKLOG.md` section, but there is no such row anywhere in the file (repo-wide search in `docs/BACKLOG.md` only finds this mention). That leaves this item with an unresolvable prerequisite, which weakens planning/ordering for the research phases because readers cannot find the referenced index-hardening task to scope or schedule it.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-25T00:24:54Z on `docs/BACKLOG.md`:5664 (association: MEMBER)

    <pre>
    Fixed in 9f26823. Pointer rewritten to name two concrete surfaces: `src/Core/Hierarchy.fs` (live closure-table operator) and the "Closure-table over DBSP" research row under `## Research projects` in `docs/BACKLOG.md` (paper-grade target). The original "same section" wording was wrong — the dependency lives in two places (code + research row in a different section), not a single hardening row in this P2 section. Drain log at `docs/pr-preservation/397-drain-log.md`.
    </pre>

### Thread 2 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59iRTm`
- **Path**: `docs/BACKLOG.md`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-25T00:06:06Z on `docs/BACKLOG.md`:5604 (association: NONE)

  <pre>
  P1: The line break in the checklist title splits a hyphenated term (`operator-` / `algebra`), which will render as "operator- algebra" in Markdown. Rewrap so `operator-algebra` stays intact (or remove the hyphen).

  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-25T00:25:10Z on `docs/BACKLOG.md`:5604 (association: MEMBER)

    <pre>
    Fixed in 9f26823. Title rewrapped so `operator-algebra` is contiguous on a single line. Drain log at `docs/pr-preservation/397-drain-log.md`.
    </pre>

### Thread 3 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59iRT1`
- **Path**: `docs/BACKLOG.md`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-25T00:06:06Z on `docs/BACKLOG.md`:5626 (association: NONE)

  <pre>
  P1: This line wrap splits "closure-table-" from "hardening", which will render as "closure-table- hardening". Please rewrap without breaking the hyphenated token (or drop the trailing hyphen).

  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-25T00:25:12Z on `docs/BACKLOG.md`:5626 (association: MEMBER)

    <pre>
    Fixed in 9f26823. Bullet rewrapped so `closure-table-hardening` is contiguous on a single line; subsequent text reflowed. Drain log at `docs/pr-preservation/397-drain-log.md`.
    </pre>

### Thread 4 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59iRT8`
- **Path**: `docs/BACKLOG.md`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-25T00:06:07Z on `docs/BACKLOG.md`:5650 (association: NONE)

  <pre>
  P0: The inline-code reference to `query-optimizer-expert` is split across lines as `query-optimizer-` / `expert`, which renders incorrectly (and can break readability/search). Rewrap so the inline-code token stays contiguous on one line.

  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-25T00:25:13Z on `docs/BACKLOG.md`:5650 (association: MEMBER)

    <pre>
    Fixed in 9f26823. Bullet rewrapped so the inline-code token `query-optimizer-expert` is contiguous on a single line; surrounding text reflowed without breaking other inline-code tokens. Drain log at `docs/pr-preservation/397-drain-log.md`.
    </pre>

### Thread 5 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59iRUJ`
- **Path**: `docs/BACKLOG.md`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-25T00:06:07Z on `docs/BACKLOG.md`:5677 (association: NONE)

  <pre>
  P1: The memory reference is missing the `memory/` prefix (`project_semiring_parameterized_...`) unlike other BACKLOG entries (e.g. the existing semiring row uses `memory/project_semiring_parameterized_...`). Using the full `memory/...` path here avoids ambiguous references and makes the pointer unambiguous.

  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-25T00:25:15Z on `docs/BACKLOG.md`:5677 (association: MEMBER)

    <pre>
    Fixed in 9f26823. Added the `memory/` prefix to the pointer (`memory/project_semiring_parameterized_zeta_regime_change_one_algebra_to_map_others_2026_04_22.md`) to match the convention used by the existing semiring rows at lines 7316 and 7409 of `docs/BACKLOG.md`. Drain log at `docs/pr-preservation/397-drain-log.md`.
    </pre>

## Fix commits (touching thread paths)

### `c590edf4ad87db957e79d4a9723c99fc2e4c2ee4` -- 2026-04-25T00:02:55Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `docs/BACKLOG.md`

**Message:**

```
backlog: cross-DSL composability — git/SQL/operator-algebra/LINQ hit …

…indexes

Maintainer 2026-04-24 directive — every first-class interface on
Zeta's substrate (git, SQL, operator algebra, LINQ, future
GraphQL / blockchain query / WASM-RPC) must compose with every
other interface. Mixed-DSL queries must:
  (1) parse + bind through unified type system
  (2) plan through cost-based optimizer (full mixed AST)
  (3) hit indexes for each constituent DSL
  (4) preserve retraction semantics end-to-end

Architectural primitive captured: this is a direct application
of the 2026-04-22 semiring-parameterized Zeta substrate research
("one algebra to map the others"). With operator algebra
parameterized by a semiring, every other DSL's semantics maps
into the same one algebra by semiring-swap, and cross-DSL
composability falls out for free.

Phased: Phase 0 design proposal → pairwise adapters → unified
planner/binder → index-utilization audit → retraction-preservation
proof.

Composes with closure-table hardening (#396 — the hierarchical
index this layer hits), native F# git impl (#395), Ouroboros
bootstrap meta-thesis (cross-DSL composability IS an Ouroboros
closure), semiring-parameterized substrate, blockchain ingest
(#394 — chain queries compose via same substrate).

Otto-275 log-don't-implement: research scope captured; does NOT
authorize implementation start.
```

### `9f26823651b93c8a4720e96a40b49417bef49add` -- 2026-04-25T00:24:40Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `docs/BACKLOG.md`

**Message:**

```
drain(#397): fix 5 Copilot threads on cross-DSL composability row

P0/P1/P1/P1/P2 from late Copilot re-review on the freshly-opened
PR. All five fixes land as in-place edits to the new BACKLOG row
(the row itself was added by this PR, so this is not an
append-only-file violation).

- title: rewrap so `operator-algebra` stays contiguous (P1).
- body: rewrap `closure-table-hardening` contiguous (P1).
- body: rewrap inline-code `query-optimizer-expert` contiguous
  (P0 — inline-code split breaks rendering and grep).
- composes-with: closure-table dependency pointer made concrete
  — names `src/Core/Hierarchy.fs` and the "Closure-table over
  DBSP" research row under `## Research projects` instead of a
  non-existent "same section" hardening row (P2).
- semiring memory pointer: add `memory/` prefix to match the
  convention used at the existing semiring rows (P1).

Drain log at `docs/pr-preservation/397-drain-log.md` per
Otto-250.
```
