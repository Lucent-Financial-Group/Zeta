# PR #433 -- drain(#226 follow-up): priority tie-break + strip-paired-delimiters + dedup-by-source_path

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 433 |
| Title | drain(#226 follow-up): priority tie-break + strip-paired-delimiters + dedup-by-source_path |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-04-25T05:23:38Z |
| Merged at | 2026-04-25T05:25:53Z |
| Merge commit SHA | `ae9de60cf9570dc2bf760c6092142f1d5ae26521` |
| Branch | `drain/226-followup-priority-tiebreak-and-strip-and-dedup` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/433 |
| Changed files | 1 |
| Additions / deletions | +43 / -14 |

## Description

Codex post-merge review on #226 surfaced 3 substantive findings.

| Finding | Severity | Fix |
|---|---|---|
| chain_head as winner contradicts invariant 6 | P1 | Use priority tie-break (max by (priority, timestamp)); chain_head only determines liveness |
| Canonicalization strips raw chars (_, *, `) corrupting identifiers | P1 | Strip PAIRED formatting delimiters only; preserve single occurrences and unpaired |
| MEMORY.md emits per-fact not per-source_path | P2 | Dedup by source_path; pick highest-priority fact per file as representative |


## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 4 |
| Resolved threads | 0 |
| Unresolved threads | 4 |
| Total review comments | 4 |
| Total fix commits (touching thread paths) | 1 |

## Review threads

### Thread 1 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM59kCgx`
- **Path**: `docs/research/memory-reconciliation-algorithm-design-2026-04-24.md`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-04-25T05:26:07Z on `docs/research/memory-reconciliation-algorithm-design-2026-04-24.md`:240 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Add stable tertiary tie-break for conflict winner**

  Selecting `winner` with `(priority, timestamp_utc)` is still ambiguous when two active records share both values, so the chosen winner depends on list iteration order rather than a documented deterministic rule. In this conflict path that can cause flapping `ConflictRow` winners between runs when input order changes, which undermines the deterministic reconciliation guarantee; add a final stable key (for example `id`) for exact ties.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 2 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM59kCgy`
- **Path**: `docs/research/memory-reconciliation-algorithm-design-2026-04-24.md`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-04-25T05:26:07Z on `docs/research/memory-reconciliation-algorithm-design-2026-04-24.md`:321 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Define deterministic tie-break for source_path dedup**

  The new dedup rule picks the “highest-priority fact” per `source_path`, but Phase-3 backfill commonly creates multiple facts from one file with the same priority, leaving the representative undefined. Different implementations (or different input ordering) can then select different representative sentences for the same file, causing non-reproducible `MEMORY.md` output and avoidable generation diffs; specify secondary/tertiary keys (e.g., timestamp then id).

  Useful? React with 👍 / 👎.
  </pre>

### Thread 3 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM59kCh2`
- **Path**: `docs/research/memory-reconciliation-algorithm-design-2026-04-24.md`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-25T05:26:13Z on `docs/research/memory-reconciliation-algorithm-design-2026-04-24.md`:240 (association: NONE)

  <pre>
  P1: The text says this is “per invariant 6” / “priority tie-break”, but the pseudocode actually breaks ties by (priority, timestamp_utc). That makes the spec internally inconsistent (invariant 6 and the later “Winner (priority tiebreak)” wording imply priority-only). Please update the spec language to explicitly define the full deterministic ordering (e.g., priority DESC then timestamp_utc DESC, and what happens if those are equal).
  ```suggestion
        # ConflictRow. The winner is chosen by the full
        # deterministic ordering (priority DESC,
        # timestamp_utc DESC, id DESC), NOT chain-head
        # precedence — chain-head only determines liveness,
        # not winner-among-actives. If two active facts have
        # equal priority and equal timestamp_utc, the higher
        # id wins as the final deterministic tie-break.
        # The chain head is one candidate among the actives;
        # it wins only if it has the highest
        # (priority, timestamp_utc, id) tuple.
        actives = [chain_head] + [f for f in group
                                   if f.status == "active"
                                   and f.id != chain_head.id]
        if len(actives) &gt; 1:
          winner = max(actives, key=lambda f: (f.priority, f.timestamp_utc, f.id))
  ```
  </pre>

### Thread 4 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM59kCh9`
- **Path**: `docs/research/memory-reconciliation-algorithm-design-2026-04-24.md`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-25T05:26:14Z on `docs/research/memory-reconciliation-algorithm-design-2026-04-24.md`:328 (association: NONE)

  <pre>
  P1: In the MEMORY.md generation rules, “dedup picks the highest-priority fact per source_path” and then “emit a newest-first list” are underspecified after dedup (what timestamp is used for ordering? and what is the deterministic tie-break if multiple facts share the highest priority for a given source_path?). Please define the ordering and tie-break explicitly so output is deterministic across runs.
  ```suggestion
  per `source_path` as the row representative. If multiple
  facts for the same `source_path` share that highest
  priority, choose the one with the greatest fact timestamp;
  if there is still a tie, choose the lexicographically
  smallest fact id. The row's description text is that
  representative fact's first sentence, and the row's
  ordering timestamp is that same representative fact's
  timestamp. Emit a newest-first list of `(source_path,
  first-sentence-of-representative-fact, tags-union)` tuples,
  ordered by representative timestamp descending and then
  by `source_path` ascending as the deterministic final
  tie-break across rows. Cap at configurable size
  (default: 250 entries or 24,000 bytes — strictly under
  the FACTORY-HYGIENE row #11 24,976-byte hard cap, with
  ~1KB headroom for any header / index annotations the
  generator writes around the entry list).
  ```
  </pre>

## Fix commits (touching thread paths)

### `48a3f09e814ed9312083f7a6c6c4eb2bb792eb7b` -- 2026-04-25T05:23:36Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `docs/research/memory-reconciliation-algorithm-design-2026-04-24.md`

**Message:**

```
drain(#226 follow-up: 3 Codex post-merge findings)

P1 (line 222) — invariant-2 winner via priority tie-break:
The reconcile pseudocode picked chain_head as the winner
in the invariant-2 violation path. Per invariant 6,
simultaneous actives must use priority tie-break (max
priority, then max timestamp). chain_head determines
liveness but NOT winner-among-actives. Updated: actives
includes the chain_head + any sibling active records;
winner = max by (priority, timestamp_utc). chain_head wins
only if it has the highest tuple.

P1 (line 128) — markdown formatting delimiters not raw chars:
The canonicalization rule removed * _ ` as raw characters,
which corrupted identifiers like _internal_var or
__private. Reworded to 'strip formatting delimiters' —
unwrap text from PAIRED markdown spans, not remove every
occurrence. Specifically: **text** / *text* / _text_ /
`text` → text, where _text_ stripping requires the
contents to match [A-Za-z0-9-]+ so identifiers survive.
Single occurrences and unpaired delimiters preserved.

P2 (line 296) — MEMORY.md dedup by source_path:
Phase 3 backfills multiple typed facts from a single
prose memory file. Without dedup, each MEMORY.md index
row would emit per-fact rather than per-file, multiplying
the index. Updated: dedup by source_path, picking the
highest-priority fact per file as the row's
representative; row description = first-sentence of that
fact; tags = union of all facts from that source_path.
```
