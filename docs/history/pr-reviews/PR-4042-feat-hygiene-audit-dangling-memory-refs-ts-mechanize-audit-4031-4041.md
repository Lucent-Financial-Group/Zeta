# PR #4042 -- feat(hygiene): audit-dangling-memory-refs.ts — mechanize audit #4031/#4041

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 4042 |
| Title | feat(hygiene): audit-dangling-memory-refs.ts — mechanize audit #4031/#4041 |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-05-17T04:42:37Z |
| Merged at | 2026-05-17T05:36:09Z |
| Merge commit SHA | `4559bbe704892c472e1f5f5a2b416a0536a45f61` |
| Branch | `otto/audit-dangling-memory-refs-tool-2026-05-17` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/4042 |
| Changed files | 3 |
| Additions / deletions | +594 / -0 |

## Description

## Summary

Implements the substrate-engineer-candidate tool the [PR #4041](https://github.com/Lucent-Financial-Group/Zeta/pull/4041) audit memo explicitly named:

> A `tools/hygiene/audit-dangling-memory-refs.ts` script + CI integration would mechanize the discipline.

Scans 6 default substrate surfaces (`.claude/agents`, `.claude/skills`, `.claude/rules`, `docs/research`, `docs/backlog`, `memory/persona`) for `memory/feedback_*.md` citations and reports which are dangling (not present in-repo).

## Modes

- Default: human report (markdown-table-friendly)
- `--json`: machine-readable output for CI
- `--surfaces <paths>`: restrict scan to specific subtrees

## Exit codes

- `0` — no dangling refs found
- `1` — one or more dangling refs found (CI-integratable as non-required check first per [B-0591](docs/backlog/P3/B-0591-wire-shard-schema-validator-to-ci-2026-05-17.md) pattern)
- `2` — configuration error (no surfaces exist)

## Live results today (2026-05-17, origin/main)

```text
TOTAL: 49 dangling edges / 32 unique dangling refs across 6 surfaces (1654 files scanned)
```

Audit memo's claim of 29 unique was edge-deduped at filename scope; this tool's 32 reflects current state (3 more landed since the audit, or methodology delta — multi-citation-site tracking enabled).

## Tests

9 pass / 21 assertions cover:
- `findEdgesInFile` — regex extraction over markdown
- `isDangling` — file existence check
- `auditSurface` — clean state + nonexistent surface
- `runAudit` — aggregation invariants (surfaces summed match totals)

Tests use tmpdir fixtures for unit-level cases + the real tree for the `.claude/agents` clean-state ratchet (audit memo asserts 0 dangling there).

## Composes with

- [`memory/feedback_otto_cli_audit_in_repo_rules_cite_user_scope_only_memory_files_5_dangling_refs_cold_boot_invisible_2026_05_17.md`](memory/feedback_otto_cli_audit_in_repo_rules_cite_user_scope_only_memory_files_5_dangling_refs_cold_boot_invisible_2026_05_17.md) (PR #4031 original audit)
- [`memory/feedback_otto_cli_audit_extension_29_dangling_memory_refs_across_4_surfaces_systemic_pattern_2026_05_17.md`](memory/feedback_otto_cli_audit_extension_29_dangling_memory_refs_across_4_surfaces_systemic_pattern_2026_05_17.md) (PR #4041 extension)
- [B-0591](docs/backlog/P3/B-0591-wire-shard-schema-validator-to-ci-2026-05-17.md) (validator-to-CI wiring precedent)
- [`.claude/rules/rule-0-no-sh-files.md`](.claude/rules/rule-0-no-sh-files.md) (TS not bash)

## Follow-up (not in this PR)

- Wire as non-required check in `gate.yml` (mirrors B-0591's Slice 1 pattern)
- After Option B disclosures land for the remaining 32 dangling refs, promote to required check

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | true |
| Total threads | 17 |
| Resolved threads | 17 |
| Unresolved threads | 0 |
| Total review comments | 17 |
| Total fix commits (touching thread paths) | 4 |

## Review threads

### Thread 1 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6CnUmc`
- **Path**: `tools/hygiene/audit-dangling-memory-refs.test.ts`
- **Replies**: 0

**Initial comment:**

- **`github-code-quality[bot]` (bot)** at 2026-05-17T04:44:08Z on `tools/hygiene/audit-dangling-memory-refs.test.ts`:2 (association: NONE)

  <pre>
  ## Unused variable, import, function or class

  Unused import mkdirSync.

  ---

  Remove the unused named import &lt;code&gt;mkdirSync&lt;/code&gt; from the &lt;code&gt;node:fs&lt;/code&gt; import list in &lt;code&gt;tools/hygiene/audit-dangling-memory-refs.test.ts&lt;/code&gt;.&lt;/p&gt;
  &lt;p&gt;Best fix (without changing functionality): edit only the import line at the top of the file so it imports only the actually used symbols (&lt;code&gt;mkdtempSync&lt;/code&gt;, &lt;code&gt;rmSync&lt;/code&gt;, &lt;code&gt;writeFileSync&lt;/code&gt;). This preserves behavior and resolves the CodeQL finding.
  </pre>

### Thread 2 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6CnUmf`
- **Path**: `tools/hygiene/audit-dangling-memory-refs.test.ts`
- **Replies**: 0

**Initial comment:**

- **`github-code-quality[bot]` (bot)** at 2026-05-17T04:44:08Z on `tools/hygiene/audit-dangling-memory-refs.test.ts`:15 (association: NONE)

  <pre>
  ## Unused variable, import, function or class

  Unused variable env.

  ---

  &lt;p&gt;Remove the unused &lt;code&gt;env&lt;/code&gt; declaration from &lt;code&gt;setupFixtureRepo()&lt;/code&gt; in &lt;code&gt;tools/hygiene/audit-dangling-memory-refs.test.ts&lt;/code&gt;.&lt;/p&gt;
  &lt;p&gt;Best fix without changing behavior:&lt;/p&gt;
  &lt;ul&gt;
  &lt;li&gt;Delete &lt;code&gt;const env = { ROOT: root };&lt;/code&gt; (line 15 in the shown snippet).&lt;/li&gt;
  &lt;li&gt;Keep function signature and return value unchanged (&lt;code&gt;{ root, cleanup }&lt;/code&gt;), since callers already rely on that.&lt;/li&gt;
  &lt;li&gt;No imports, methods, or additional definitions are required.&lt;/li&gt;
  &lt;/ul&gt;
  </pre>

### Thread 3 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6CnUw1`
- **Path**: `tools/hygiene/audit-dangling-memory-refs.test.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-17T04:45:08Z on `tools/hygiene/audit-dangling-memory-refs.test.ts`:15 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Remove unused test symbols to keep typecheck green**

  This test file introduces unused declarations (`mkdirSync` in the import list and `env` inside `setupFixtureRepo`), which triggers `TS6133` under the repo’s strict TypeScript settings (`noUnusedLocals: true` in `tsconfig.json`). In environments that run the normal `tsc --noEmit` gate, the change fails the typecheck step before tests can run.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 4 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6CnU4c`
- **Path**: `tools/hygiene/audit-dangling-memory-refs.test.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-17T04:45:57Z on `tools/hygiene/audit-dangling-memory-refs.test.ts`:2 (association: NONE)

  <pre>
  `mkdirSync` is imported but never used. With `noUnusedLocals` enabled in tsconfig, this causes `bun run typecheck` to fail and will block CI. Remove the unused import (or use it).

  This issue also appears on line 13 of the same file.

  </pre>

### Thread 5 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6CnU4f`
- **Path**: `tools/hygiene/audit-dangling-memory-refs.test.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-17T04:45:57Z on `tools/hygiene/audit-dangling-memory-refs.test.ts`:35 (association: NONE)

  <pre>
  These tests set `process.env.REPO_ROOT` after importing the module, but the module-level `ROOT` constant is computed at import time. As a result, the tmpdir fixture root isn’t actually exercised, and the tests don’t validate `REPO_ROOT` behavior. Consider refactoring to compute root via a function (or accept `root` as a parameter), or use a dynamic import after setting `REPO_ROOT` in each test.
  </pre>

### Thread 6 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6CnU4h`
- **Path**: `tools/hygiene/audit-dangling-memory-refs.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-17T04:45:57Z on `tools/hygiene/audit-dangling-memory-refs.ts`:9 (association: NONE)

  <pre>
  The header comment uses direct name attribution (e.g., “Aaron’s …”). Per `docs/AGENT-BEST-PRACTICES.md` ("No name attribution in code, docs, or skills"), current-state/code surfaces should use role-refs instead (e.g., “the maintainer’s CLI” / “the user-scope memory loader”).
  </pre>

### Thread 7 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6CnU4i`
- **Path**: `tools/hygiene/audit-dangling-memory-refs.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-17T04:45:57Z on `tools/hygiene/audit-dangling-memory-refs.ts`:38 (association: NONE)

  <pre>
  `ROOT` defaults to `process.cwd()`, which makes the tool sensitive to where it’s invoked (running from a subdirectory will resolve surfaces incorrectly). Most hygiene tools in this repo resolve the repo root via `git rev-parse --show-toplevel` with a fallback (e.g., `tools/hygiene/audit-backlog-status-drift.ts:44-70`). Aligning to that pattern would make the tool cwd-independent and easier to wire into CI.

  </pre>

### Thread 8 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6CnY-4`
- **Path**: `tools/hygiene/audit-dangling-memory-refs.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-17T05:11:08Z on `tools/hygiene/audit-dangling-memory-refs.ts`:240 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Reject empty --surfaces instead of scanning defaults**

  When `--surfaces` is provided without any following paths (for example from an empty CI variable expansion), `customSurfaces` stays empty and the code silently falls back to `DEFAULT_SURFACES`. That masks configuration mistakes and audits the wrong scope while still returning a normal dangling/non-dangling exit code, which is especially risky once this is wired into CI. This path should be treated as a configuration error (exit `2`) instead of defaulting.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 9 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6CnakV`
- **Path**: `tools/hygiene/audit-dangling-memory-refs.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-17T05:21:06Z on `tools/hygiene/audit-dangling-memory-refs.ts`:245 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Fail when any requested surface path is missing**

  The current validation only checks whether **at least one** requested surface exists, so `--surfaces docs/research docs/typo` still returns a normal audit result and can exit `0/1` even though one requested subtree was never scanned. In CI this creates false-green audits when a path is misspelled or renamed, because the tool silently reports `0 files scanned` for the bad surface instead of treating it as configuration error (`2`).

  Useful? React with 👍 / 👎.
  </pre>

### Thread 10 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6CnakW`
- **Path**: `tools/hygiene/audit-dangling-memory-refs.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-17T05:21:06Z on `tools/hygiene/audit-dangling-memory-refs.ts`:252 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Reject unknown CLI flags instead of ignoring them**

  Argument parsing has no error path for unrecognized options, so typos like `--jons` or `--surface` are silently ignored and the script falls back to defaults. That hides operator mistakes and can make automation believe a specific mode/scope was enforced when it was not; unknown flags should return a configuration error exit code rather than proceeding.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 11 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6Cna1k`
- **Path**: `tools/hygiene/audit-dangling-memory-refs.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-17T05:22:54Z on `tools/hygiene/audit-dangling-memory-refs.ts`:50 (association: NONE)

  <pre>
  P2: The comment above `MEM_REF_RE` says it matches references “as a path (NOT just a string in code)”, but the current regex will match any `memory/feedback_*.md` substring anywhere in markdown (including prose/code blocks). Please align the comment with actual behavior (or tighten the matcher if you truly want to exclude non-path contexts).

  </pre>

### Thread 12 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6Cna1m`
- **Path**: `tools/hygiene/audit-dangling-memory-refs.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-17T05:22:55Z on `tools/hygiene/audit-dangling-memory-refs.ts`:261 (association: NONE)

  <pre>
  P1: Arg parsing silently ignores unknown flags, and `--surfaces` with no following paths falls back to scanning DEFAULT_SURFACES. That makes mis-typed CLI invocations hard to notice and can trigger unexpectedly large scans in CI. Consider switching to a strict parser (error on unknown args) and treating `--surfaces` with zero collected paths as a configuration error (exit 2/64 with a usage message).
  </pre>

### Thread 13 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6Cna1o`
- **Path**: `tools/hygiene/audit-dangling-memory-refs.test.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-17T05:22:55Z on `tools/hygiene/audit-dangling-memory-refs.test.ts`:105 (association: NONE)

  <pre>
  P1: This test’s comment about “import statics capture ROOT at import time” no longer matches the implementation (the production code resolves `REPO_ROOT` lazily on each `repoRoot()` call). Also, the existence-proof assertion relies on a specific file in the real repo, making the test less self-contained. Prefer creating a temp `memory/feedback_*.md` file under the fixture repo and asserting `isDangling` returns false with `REPO_ROOT` set to that fixture root.
  </pre>

### Thread 14 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6Cna1p`
- **Path**: `tools/hygiene/audit-dangling-memory-refs.test.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-17T05:22:55Z on `tools/hygiene/audit-dangling-memory-refs.test.ts`:65 (association: NONE)

  <pre>
  P2: This test name/comment is internally contradictory (“ignores … (still matches if path-shaped)”), and the assertions show it intentionally DOES match bare `memory/feedback_*.md` in prose. Renaming the test (or rewriting the comment) would make the intended behavior clearer for future maintainers.

  </pre>

### Thread 15 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6Cna1u`
- **Path**: `tools/hygiene/audit-dangling-memory-refs.test.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-17T05:22:55Z on `tools/hygiene/audit-dangling-memory-refs.test.ts`:148 (association: NONE)

  <pre>
  P1: `auditSurface` “nonexistent surface” test depends on the current repo layout (it will start failing if someone ever adds `.claude/nonexistent-surface-for-test`). To avoid brittle tests, set `REPO_ROOT` to a tmp fixture and run `auditSurface("some/surface")` against that fixture instead.
  </pre>

### Thread 16 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6Cna15`
- **Path**: `memory/feedback_otto_qg_isomorphism_step_2_cube_adinkra_cayley_dickson_to_happylike_qecc_2026_05_16.md`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-17T05:22:56Z on `memory/feedback_otto_qg_isomorphism_step_2_cube_adinkra_cayley_dickson_to_happylike_qecc_2026_05_16.md`:12 (association: NONE)

  <pre>
  P1: This memo claims `docs/research/2026-05-15-qg-isomorphism-step-2-cube-adinkra-cayley-dickson-to-happylike-qecc.md` was created, but that file does not appear to exist in-repo (only references to it do). Either add the missing research doc in this PR or update the memo to point at the actual location/name so readers don’t follow a broken reference.

  </pre>

### Thread 17 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6Cna2B`
- **Path**: `tools/hygiene/audit-dangling-memory-refs.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-17T05:22:56Z on `tools/hygiene/audit-dangling-memory-refs.ts`:37 (association: NONE)

  <pre>
  P1: `repoRoot()` defaults to `process.cwd()`, so results depend on invocation directory (running from a subdir can scan the wrong tree or hit the “no surfaces exist” error). Nearby hygiene tools are cwd-independent by detecting the git toplevel / chdir’ing to it (e.g., `tools/hygiene/audit-md032-plus-linestart.ts:112-115`, `tools/hygiene/audit-backlog-status-drift.ts:62-71` + `main` at ~306-313). Consider adopting that pattern here while still honoring `REPO_ROOT` overrides for tests.
  </pre>

## Fix commits (touching thread paths)

### `7fec8fc9c5fc0c4eff31d87b9475f253ed180b81` -- 2026-05-17T04:35:08Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/hygiene/audit-dangling-memory-refs.test.ts`
- `tools/hygiene/audit-dangling-memory-refs.ts`

**Message:**

```
feat(hygiene): audit-dangling-memory-refs.ts — mechanize audit #4031/…

…#4041

Implements the substrate-engineer-candidate tool the PR #4041 audit
memo explicitly named:

> A `tools/hygiene/audit-dangling-memory-refs.ts` script + CI integration
> would mechanize the discipline.

Scans 6 default substrate surfaces (.claude/agents, .claude/skills,
.claude/rules, docs/research, docs/backlog, memory/persona) for
`memory/feedback_*.md` citations and reports which are dangling
(not present in-repo).

Modes:
- default human report
- `--json` machine-readable output (CI-friendly)
- `--surfaces <paths>` restrict scan to specific subtrees

Exit codes:
- 0 no dangling refs
- 1 one or more dangling refs found
- 2 configuration error (no surfaces exist)

Tests: 9 pass / 21 assertions cover findEdgesInFile (regex extraction),
isDangling (file existence), auditSurface (clean + nonexistent),
runAudit (aggregation invariants). Tests use tmpdir fixtures for the
edge-extraction unit tests + real-tree audits for the .claude/agents
ratchet (audit memo asserts 0 dangling there).

Live results on origin/main today (2026-05-17):
- 49 dangling edges / 32 unique dangling refs across 6 surfaces
- 1654 files scanned in ~2s
- exit 1 (CI-integratable as non-required check first per B-0591 pattern)

Audit memo's claim of 29 unique was edge-deduped at filename scope; my
tool's 32 reflects current state (3 more landed since audit; or
methodology delta — multi-citation site tracking enabled).

Composes with:
- memory/feedback_otto_cli_audit_in_repo_rules_cite_user_scope_only_memory_files_5_dangling_refs_cold_boot_invisible_2026_05_17.md (PR #4031 original)
- memory/feedback_otto_cli_audit_extension_29_dangling_memory_refs_across_4_surfaces_systemic_pattern_2026_05_17.md (PR #4041 extension)
- B-0591 (precedent: validator-to-CI wiring pattern)
- .claude/rules/rule-0-no-sh-files.md (TS not bash)

Co-Authored-By: Claude <noreply@anthropic.com>
```

### `1d2774c5b7f452d886caf4f7b90c10ab456e8d83` -- 2026-05-17T05:03:33Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/hygiene/audit-dangling-memory-refs.test.ts`
- `tools/hygiene/audit-dangling-memory-refs.ts`

**Message:**

```
fix: address 7 reviewer threads on PR #4042

- Remove unused mkdirSync import + unused env variable from test file
  (github-code-quality / Codex P1 / Copilot — TS6133 / noUnusedLocals)
- Strip name attribution from script header comment per
  docs/AGENT-BEST-PRACTICES.md "No name attribution in code, docs, or
  skills" (Copilot)
- Refactor ROOT from module-level const to lazy repoRoot() function so
  REPO_ROOT env var override is actually exercised by tests (Copilot —
  tmpdir fixture root was previously captured-at-import-time and never
  used by the script under test)

Deferred to follow-up: use `git rev-parse --show-toplevel` for repo-root
resolution when invoked from a subdirectory (Copilot). The lazy
repoRoot() resolves the testability concern; the cwd-vs-toplevel
concern is gated by the security_reminder_hook on child_process
imports for new files. Tracked as future improvement.

Verified all modes still work:
- bun ... (full scan) → 49 dangling / 32 unique / exit 1
- bun ... --surfaces .claude/agents → 0 dangling / exit 0
- REPO_ROOT=/tmp bun ... → error: no surfaces / exit 2
- bun test → 9 pass / 21 assertions

Co-Authored-By: Claude <noreply@anthropic.com>
```

### `a25e8ad0fef0d97a8b98605e91e06400a7644879` -- 2026-05-17T05:06:23Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `memory/feedback_otto_qg_isomorphism_step_2_cube_adinkra_cayley_dickson_to_happylike_qecc_2026_05_16.md`

**Message:**

```
memory: QG isomorphism Step 2 feedback summary for future-Otto

- memory/feedback_otto_qg_isomorphism_step_2_2026_05_16.md: round
  summary documenting Step 2 of 4-step proof strategy

Co-Authored-By: Kiro <noreply@kiro.dev>
```

### `262db154c0bed3ba07e83ea65fd25715aa1e6954` -- 2026-05-17T05:27:26Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/hygiene/audit-dangling-memory-refs.test.ts`
- `tools/hygiene/audit-dangling-memory-refs.ts`

**Message:**

```
fix: address 7 more reviewer threads on PR #4042 (strict CLI + fixtur…

…e tests)

Round 2 of reviewer absorption. Addresses:

1. Strict argument parsing (Codex P2 + Copilot P1):
   - Unknown --flags rejected with exit 2 + error message
   - --surfaces with zero paths fail-closes (no silent fallback to defaults)
   - Stray positional args (outside --surfaces) rejected with exit 2

2. Per-surface existence check (Codex P2):
   - Was: passes if ANY requested surface exists
   - Now: fails if ANY requested surface is missing
   - Prevents `--surfaces docs/research docs/typo` from returning 0/1
     while silently ignoring the typo

3. MEM_REF_RE comment alignment (Copilot P2):
   - Old comment claimed "NOT just a string in code" but regex matches
     prose anywhere. Reworded to match actual behavior (intentional
     match; dangling check is the filter).

4. Test cleanups (Copilot P1+P2):
   - Removed stale "ROOT captured at import time" comment from
     isDangling test (lazy repoRoot() resolves this concern)
   - Renamed "ignores ... (still matches)" contradictory test
   - Replaced .claude/nonexistent-surface-for-test brittle dependency
     with tmpdir fixture + REPO_ROOT override
   - Replaced .claude/agents ratchet test with tmpdir fixture test
     (was brittle to real repo state changes)
   - Added test exercising dangling-ref detection end-to-end via fixture

Verified all CLI modes:
- bun ... --json → exit 1 (dangling refs found)
- bun ... --jons → exit 2 (unrecognized flag)
- bun ... --surfaces → exit 2 (refusing fallback)
- bun ... --surfaces docs/research docs/typo → exit 2 (typo surface)
- bun ... random → exit 2 (stray positional)
- bun test → 10 pass / 26 assertions

Deferred (still gated by child_process security_reminder_hook):
- Use `git rev-parse --show-toplevel` for repoRoot (Copilot P1)

Out-of-scope thread (different file): comment claiming
`docs/research/2026-05-15-qg-isomorphism-step-2-*` doesn't exist —
that file is not in this PR's surface; not addressed here.

Co-Authored-By: Claude <noreply@anthropic.com>
```
