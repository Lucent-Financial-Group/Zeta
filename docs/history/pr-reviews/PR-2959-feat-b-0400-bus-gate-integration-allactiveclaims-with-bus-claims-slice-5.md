# PR #2959 -- feat(B-0400): bus-gate integration — allActiveClaims + --with-bus-claims (slice 5)

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 2959 |
| Title | feat(B-0400): bus-gate integration — allActiveClaims + --with-bus-claims (slice 5) |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-05-13T06:52:48Z |
| Merged at | 2026-05-13T07:11:30Z |
| Merge commit SHA | `7c622dfa60f6960ab490c297d6c0e2b59bcb1ed5` |
| Branch | `feat/b-0400-slice5-bus-gate-integration` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/2959 |
| Changed files | 6 |
| Additions / deletions | +230 / -6 |

## Description

## Summary

- **`claim.ts`**: export `allActiveClaims()` — collects active claims across all backlog items by discovering unique `itemId`s from claim-topic messages, then delegating per-item to `activeClaims()` for correct claim/release semantics
- **`poll-pr-gate-batch.ts`**: `--with-bus-claims` flag appends active bus claims (`busClaims?: ClaimRecord[]`) to the `BatchReport` JSON, giving the autonomous loop PR-gate state + bus coordination state in one call
- **DST-clean injection**: `BusClaimsFn` injectable type + `pollFn` injection in `main()` — tests never spawn `gh` or touch the filesystem

## Slice context

Slices 1–4 shipped (types + CLI + watch + claim-coordinator + status subcommand). This is slice 5: the `poll-pr-gate-batch.ts` integration listed as deferred in the backlog item.

**Usage after this slice:**
```bash
bun tools/github/poll-pr-gate-batch.ts --with-bus-claims 2944 2953
# BatchReport now includes busClaims: [{ from: "otto", itemId: "B-0400", branch: "...", ... }]
```

## Test results

```
bun test tools/bus/           → 64 pass, 0 fail   (+3 new allActiveClaims tests)
bun test tools/github/poll-pr-gate-batch.test.ts → 14 pass, 0 fail   (+4 new main() tests)
dotnet build -c Release       → 0 warnings, 0 errors
bunx tsc --noEmit             → 0 errors in changed files
```

## Checklist

- [x] Rule 0: TypeScript only
- [x] Build gate: `dotnet build -c Release` → 0 warn / 0 err
- [x] Tests: 78 total pass, 0 fail
- [x] Bus claim acquired before work: `claim.ts acquire --from otto --item B-0400`
- [x] Backlog item updated with slice 5 scope
- [x] operative-authorization: aaron 2026-05-13: "cooling periods, harsh-critic reviewers, F# anchor sanity check"

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | true |
| Total threads | 5 |
| Resolved threads | 4 |
| Unresolved threads | 1 |
| Total review comments | 13 |
| Total fix commits (touching thread paths) | 2 |

## Review threads

### Thread 1 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6BqNys`
- **Path**: `tools/bus/claim.ts`
- **Replies**: 2

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-13T06:55:37Z on `tools/bus/claim.ts`:164 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Collapse allActiveClaims into a single pass**

  `allActiveClaims()` first scans all claim messages for unique item IDs, then calls `activeClaims()` once per item; because `activeClaims()` itself does `list({ topic: "claim" })` and per-message file metadata lookups, this turns one scan into O(items × messages) work. In busy sessions (many claim/release events across many backlog items), `--with-bus-claims` can become disproportionately slow and delay `poll-pr-gate-batch` output, even though the result can be derived from a single message pass using the same latest-action logic.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-13T07:00:37Z on `tools/bus/claim.ts`:164 (association: MEMBER)

    <pre>
    Fixed in commit f610a2b6: refactored allActiveClaims() to a single list() call with one pass grouping by (from:itemId) key — identical tiebreaker semantics as activeClaims(), no nested list() calls.
    </pre>
  - **`AceHack` (human)** at 2026-05-13T07:00:52Z on `tools/bus/claim.ts`:164 (association: MEMBER)

    <pre>
    Fixed in commit f610a2b6: refactored allActiveClaims() to a single list() call with one pass grouping by (from:itemId) key — same tiebreaker semantics as activeClaims(), no nested list() calls.
    </pre>

### Thread 2 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6BqPF4`
- **Path**: `tools/bus/claim.ts`
- **Replies**: 2

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-13T06:56:56Z on `tools/bus/claim.ts`:165 (association: NONE)

  <pre>
  P1 (performance): allActiveClaims() reads the full claim message set via list({topic:"claim"}) and then calls activeClaims(itemId) for each discovered itemId, which re-reads the full set again each time. This makes runtime/disk I/O scale roughly O(#items × #messages). Consider computing the latest action per (from,itemId) in a single pass (essentially activeClaims generalized across all itemIds) and returning only entries whose latest action is "claim".

  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-13T07:00:38Z on `tools/bus/claim.ts`:165 (association: MEMBER)

    <pre>
    Fixed in commit f610a2b6: same single-pass refactor — list() called once, Map&lt;string,Entry&gt; keyed by (from:itemId) accumulates latest action per pair in one iteration. O(messages) total.
    </pre>
  - **`AceHack` (human)** at 2026-05-13T07:00:59Z on `tools/bus/claim.ts`:165 (association: MEMBER)

    <pre>
    Fixed in commit f610a2b6: same single-pass refactor. list() called once; Map keyed by (from:itemId) accumulates the latest action per pair in one iteration. O(messages) total instead of O(items x messages).
    </pre>

### Thread 3 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6BqPGg`
- **Path**: `tools/github/poll-pr-gate-batch.test.ts`
- **Replies**: 2

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-13T06:56:57Z on `tools/github/poll-pr-gate-batch.test.ts`:303 (association: NONE)

  <pre>
  P1 (test correctness / coverage): This test name and comments say it exercises the empty-PR early-return path ("--all-open returns nothing"), but it actually calls main() with a non-empty PR list (["--with-bus-claims","2"]). As written it only verifies busClaims is serialized when the flag is set, not the empty-prs branch. Either rename the test to match what it asserts, or make listOpenPRs injectable so the true early-return path can be covered deterministically.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-13T07:00:40Z on `tools/github/poll-pr-gate-batch.test.ts`:303 (association: MEMBER)

    <pre>
    Fixed in commit f610a2b6: renamed test to 'busClaims serialized as empty array when --with-bus-claims and busClaimsFn returns nothing'. Updated comment to clarify the --all-open empty path is NOT exercised (listOpenPRs not injectable here) and document that path needs a separate integration test.
    </pre>
  - **`AceHack` (human)** at 2026-05-13T07:01:06Z on `tools/github/poll-pr-gate-batch.test.ts`:303 (association: MEMBER)

    <pre>
    Fixed in commit f610a2b6: renamed test to 'busClaims serialized as empty array when --with-bus-claims and busClaimsFn returns nothing'. Updated the comment to document that the --all-open empty path requires injectable listOpenPRs and is a separate integration test gap.
    </pre>

### Thread 4 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6BqPG4`
- **Path**: `docs/backlog/P1/B-0400-inter-agent-ephemeral-communication-bus-nats-protocol.md`
- **Replies**: 2

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-13T06:56:57Z on `docs/backlog/P1/B-0400-inter-agent-ephemeral-communication-bus-nats-protocol.md`:101 (association: NONE)

  <pre>
  P2 (doc accuracy): This line claims "4 new tests in poll-pr-gate-batch.test.ts", but this slice’s diff adds 3 tests in that file. Please update the count (or add the missing test) so the slice scope stays accurate.

  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-13T07:00:41Z on `docs/backlog/P1/B-0400-inter-agent-ephemeral-communication-bus-nats-protocol.md`:101 (association: MEMBER)

    <pre>
    Fixed in commit f610a2b6: corrected '4 new tests' → '3 new tests' in slice-5 scope section of the backlog doc.
    </pre>
  - **`AceHack` (human)** at 2026-05-13T07:01:13Z on `docs/backlog/P1/B-0400-inter-agent-ephemeral-communication-bus-nats-protocol.md`:101 (association: MEMBER)

    <pre>
    Fixed in commit f610a2b6: corrected '4 new tests' to '3 new tests' in the slice-5 scope section of the B-0400 backlog doc.
    </pre>

### Thread 5 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM6Bqdw3`
- **Path**: `tools/bus/claim.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-13T07:11:42Z on `tools/bus/claim.ts`:192 (association: NONE)

  <pre>
  P2 (DST/stability): allActiveClaims() returns records in Map insertion order, which ultimately depends on list() ordering. list() sorts only by timestamp; for messages with identical timestamps, the relative order can vary based on filesystem enumeration, so the resulting array order (and thus JSON output when used by --with-bus-claims) can be nondeterministic. Consider sorting the returned ClaimRecord[] explicitly (e.g., by itemId then from, or by timestamp then itemId/from) before returning so the output stays stable across runs and platforms.

  </pre>

## Fix commits (touching thread paths)

### `57c0c7430c1ff8de12a0bf8c027d8ca1cccc6620` -- 2026-05-13T06:52:31Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `docs/backlog/P1/B-0400-inter-agent-ephemeral-communication-bus-nats-protocol.md`
- `tools/bus/claim.ts`
- `tools/github/poll-pr-gate-batch.test.ts`

**Message:**

```
feat(B-0400): bus-gate integration — allActiveClaims + --with-bus-cla…

…ims (slice 5)

- claim.ts: export allActiveClaims() — collects active claims across all
  backlog items by discovering unique itemIds from claim-topic messages
  and delegating per-item to activeClaims() for correct release semantics
- poll-pr-gate-batch.ts: --with-bus-claims flag appends active bus claims
  to the BatchReport output (busClaims?: ClaimRecord[])
- BusClaimsFn injectable type + pollFn injection in main() for full DST
  coverage without gh subprocess in tests
- 3 new tests in claim.test.ts (allActiveClaims across items, with release)
- 4 new tests in poll-pr-gate-batch.test.ts (flag present/absent, empty list)
- bun test tools/bus/ → 64 pass; bun test poll-pr-gate-batch.test.ts → 14 pass
- dotnet build -c Release → 0 warnings, 0 errors

operative-authorization: aaron 2026-05-13: "cooling periods, harsh-critic reviewers, F# anchor sanity check"

Co-Authored-By: Claude <noreply@anthropic.com>
```

### `f610a2b6afc1920f136945cfa76a2fa2673c869c` -- 2026-05-13T07:00:08Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `docs/backlog/P1/B-0400-inter-agent-ephemeral-communication-bus-nats-protocol.md`
- `tools/bus/claim.ts`
- `tools/github/poll-pr-gate-batch.test.ts`

**Message:**

```
fix(B-0400/slice5): address review thread findings — single-pass allA…

…ctiveClaims, test rename, doc count

Three Codex/Copilot review findings resolved:

1. perf(claim.ts): refactor allActiveClaims() from O(items×messages) to single-pass.
   Previously: list() once to discover item IDs, then activeClaims(id) per item
   (each calling list() again). Now: one list() call, groupBy (from:itemId) in
   a single pass — identical tiebreaker logic to activeClaims().

2. test(poll-pr-gate-batch): rename misleading test — was 'empty-PR early-return
   path' but actually tests 'busClaimsFn returns empty list'. Updated comment to
   clarify that --all-open empty path requires injectable listOpenPRs (not done here).

3. docs(B-0400): correct test count 4→3 in slice-5 scope section.

All 33 claim tests + 14 poll-pr-gate-batch tests pass.

Co-Authored-By: Claude <noreply@anthropic.com>
```
