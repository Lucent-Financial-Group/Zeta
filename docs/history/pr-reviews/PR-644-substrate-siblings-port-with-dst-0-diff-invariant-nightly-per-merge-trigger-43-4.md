# PR #644 -- substrate(siblings): port-with-DST + 0-diff invariant + nightly→per-merge trigger (#43+#44)

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 644 |
| Title | substrate(siblings): port-with-DST + 0-diff invariant + nightly→per-merge trigger (#43+#44) |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-04-27T15:50:28Z |
| Merged at | 2026-04-27T16:17:32Z |
| Merge commit SHA | `de10e3adc9b9b10c3faf8bcc1cc62d154611bc2e` |
| Branch | `lfg/sibling-pr-43-44-port-with-dst-and-slim-trigger` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/644 |
| Changed files | 4 |
| Additions / deletions | +215 / -4 |

## Description

## Summary

LFG sibling sync of two AceHack substrate PRs landing today:

- **AceHack #43** — `substrate: port-with-DST discipline + AceHack-LFG diff-minimization invariant`
  - Aaron framing: ports/absorbs from `../scratch` + `../SQLSharp` MUST add DST + pinned seeds + flake-zero by default; not "replicate the no-DST shape."
  - Aaron softened framing: "DST = clean separation between side-effects and pure" (the earlier "I/O monad pushed to extreme is a bit strong").
  - 0-diff invariant: AceHack ↔ LFG should converge to 0 difference, with any drift rigorously accounted for.

- **AceHack #44** — `ci: trigger low-memory verification on every merge to main + nightly`
  - Aaron framing 2026-04-27: *"no reason we don't change that nightly job for slim to just trigger on every merge to main, it's free for open source projects"* (Otto-249).
  - Push-to-main + 06:00 UTC schedule + workflow_dispatch.
  - **Filename caveat (Aaron flagged 2026-04-27):** *"when it becomes per merge this won't be a good filename anymore"* — `nightly-low-memory.yml` is misleading once it triggers per-merge. Follow-up rename PR will land on AceHack first (homebase-first), then sync here.

## Sync direction

AceHack = homebase (where work originates). LFG = published canonical. This PR forward-syncs the AceHack siblings to keep the published surface at 0-diff.

## Test plan
- [ ] CI green on LFG (gate.yml + nightly-low-memory.yml first per-merge run)
- [ ] Build: `dotnet build -c Release` → 0 Warning(s) / 0 Error(s)
- [ ] Both AceHack siblings squash-merged; this PR carries the cherry-picks

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | true |
| Total threads | 8 |
| Resolved threads | 8 |
| Unresolved threads | 0 |
| Total review comments | 16 |
| Total fix commits (touching thread paths) | 5 |

## Review threads

### Thread 1 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM594r4H`
- **Path**: `.github/workflows/nightly-low-memory.yml`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-04-27T15:52:48Z on `.github/workflows/nightly-low-memory.yml`:66 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Avoid duplicate low-memory CI on main pushes**

  Adding the `push` trigger here makes every merge to `main` run the same ubuntu-slim build/test twice, because `gate.yml` already triggers on `push` to `main` and already includes `ubuntu-slim` in its matrix (checked `.github/workflows/gate.yml` lines 45-48 and 122-126). That doubles slow-runner workload and queue time without adding new coverage, which undermines the stated goal of moving this verification out of the PR bottleneck; either remove `ubuntu-slim` from `gate.yml` or keep this workflow for schedule/manual only.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-27T16:05:46Z on `.github/workflows/nightly-low-memory.yml`:66 (association: MEMBER)

    <pre>
    Good catch. LFG main still had `ubuntu-slim` in gate.yml's build-and-test matrix (line 126) — AceHack main had already removed it but the divergence hadn't synced yet. Removed here too, so the slim leg only runs once via this workflow. Exactly the kind of AceHack-LFG drift the 0-diff invariant from #43 is meant to catch.
    </pre>

### Thread 2 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM594sse`
- **Path**: `.github/workflows/nightly-low-memory.yml`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-27T15:53:34Z on `.github/workflows/nightly-low-memory.yml`:6 (association: NONE)

  <pre>
  P1: The header comment says this workflow runs "once per day", but the workflow is also triggered on every push to `main`. Update the description to match the actual trigger surface so readers don’t assume this is nightly-only.
  ```suggestion
  # 1-vCPU / 5GB-RAM `ubuntu-slim` runner class on every push to
  # `main`, on the daily 06:00 UTC schedule, and via manual
  # `workflow_dispatch`. Goal: detect drift that would break Zeta on
  # resource-constrained environments before contributors hit it.
  ```
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-27T16:05:49Z on `.github/workflows/nightly-low-memory.yml`:6 (association: MEMBER)

    <pre>
    Reworded header to describe the actual trigger surface — push to main (in practice every merge — direct pushes blocked by branch protection) + daily 06:00 UTC schedule + workflow_dispatch.
    </pre>

### Thread 3 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM594stY`
- **Path**: `.github/workflows/nightly-low-memory.yml`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-27T15:53:34Z on `.github/workflows/nightly-low-memory.yml`:30 (association: NONE)

  <pre>
  P1: In the "What this workflow does" section, the bullets list schedule + workflow_dispatch but omit the `push` trigger that’s now in `on:`. This doc block should include the push-to-main behavior (or be rewritten) to avoid drift between documentation and YAML.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-27T16:05:51Z on `.github/workflows/nightly-low-memory.yml`:30 (association: MEMBER)

    <pre>
    Added push-to-main bullet at the top of the "What this workflow does" section, plus a note that ubuntu-slim was removed from gate.yml's matrix in this same PR (per Codex P2) so we don't double-run the slim leg on every push.
    </pre>

### Thread 4 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM594stu`
- **Path**: `.github/workflows/nightly-low-memory.yml`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-27T15:53:34Z on `.github/workflows/nightly-low-memory.yml`:72 (association: NONE)

  <pre>
  P1: `timeout-minutes: 30` conflicts with the comment that the ubuntu-slim runner class has a hard 15-minute cap; setting a higher timeout doesn’t provide defense-in-depth and can mislead future edits. Suggest setting `timeout-minutes` to &lt;= 15 (often 14 to fail gracefully before the platform kill) and adjusting the comment accordingly.
  ```suggestion
      timeout-minutes: 14   # fail gracefully before the runner-class 15 min hard cap
  ```
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-27T16:05:53Z on `.github/workflows/nightly-low-memory.yml`:72 (association: MEMBER)

    <pre>
    Reduced to `timeout-minutes: 14` (one minute under the 15-min runner-class hard cap) so the job fails gracefully with a proper Actions log before the platform kills it. Updated the safe-pattern-compliance comment block to match.
    </pre>

### Thread 5 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM594suR`
- **Path**: `memory/MEMORY.md`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-27T15:53:35Z on `memory/MEMORY.md`:6 (association: NONE)

  <pre>
  P1: `MEMORY.md` is documented as a ~200-line, terse index (memory/README.md:56-58). This entry has grown into a full paragraph, which increases the risk that newer entries fall past the truncation window and become effectively unread. Suggest tightening this bullet to the minimum (title + key tags) and leaving the detailed clarifications inside the linked memory file.
  ```suggestion
  - [Install-script language strategy — pre-install bash+PowerShell / post-install TypeScript / Python only for AI-ML; tags: `.mise.toml` canonical pins, port-with-DST, diff-minimization](project_install_script_language_strategy_post_install_typescript_pre_install_bash_powershell_python_for_ai_ml_2026_04_27.md) — 2026-04-27.
  ```
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-27T16:05:55Z on `memory/MEMORY.md`:6 (association: MEMBER)

    <pre>
    Deferring as a broader MEMORY.md index-hygiene pass — multiple entries have grown long over time, not just this one, and tightening one in isolation creates inconsistency without addressing the systemic concern. Tracked as separate hygiene work; the truncation-window risk you flagged is the real concern and the right framing for the index-wide pass.
    </pre>

### Thread 6 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM595CT9`
- **Path**: `.github/workflows/nightly-low-memory.yml`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-04-27T16:12:27Z on `.github/workflows/nightly-low-memory.yml`:79 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Scope concurrency group to preserve every main push run**

  Using a fixed workflow-level concurrency group here means GitHub Actions keeps at most one running and one pending run for this group; if two or more merges land on `main` while one low-memory run is active, older pending runs are replaced and never execute. That breaks the stated “runs on every merge” behavior and can hide transient regressions introduced and reverted between merges, so this group should be keyed by ref/commit (or removed) when per-merge coverage is required.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-27T16:15:50Z on `.github/workflows/nightly-low-memory.yml`:79 (association: MEMBER)

    <pre>
    Fixed via per-commit concurrency group: `group: low-memory-${{ github.sha }}`. Every push to main now gets its own slot, no replacement. Drift detection on intermediate states preserved.
    </pre>

### Thread 7 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM595DC9`
- **Path**: `.github/workflows/nightly-low-memory.yml`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-27T16:13:03Z on `.github/workflows/nightly-low-memory.yml`:79 (association: NONE)

  <pre>
  P1: `concurrency.group` is constant and `cancel-in-progress` is `false`, so a burst of merges can create a long queue of low-memory runs (many of which become redundant once newer commits land). Consider enabling cancellation (or keying the group to the ref/SHA) so only the latest main state runs, keeping feedback timely and avoiding wasted runner time.
  ```suggestion
    group: ${{ github.workflow }}-${{ github.ref }}
    cancel-in-progress: true
  ```
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-27T16:15:52Z on `.github/workflows/nightly-low-memory.yml`:79 (association: MEMBER)

    <pre>
    Fixed via per-commit concurrency group keyed on github.sha (rather than enabling cancel-in-progress, which would drop intermediate signal — the workflow's purpose is drift detection on every commit, not just the latest). Standard runners are free for public repos (Otto-249) so parallel-runs cost is zero.
    </pre>

### Thread 8 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM595DDs`
- **Path**: `.github/workflows/gate.yml`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-27T16:13:03Z on `.github/workflows/gate.yml`:131 (association: NONE)

  <pre>
  P1: This comment says ubuntu-slim moved out of `gate.yml`, but the earlier header in this job still documents ubuntu-slim as an active matrix leg (including the 15-minute cap rationale). Update/remove that earlier “Active legs” section so the file’s documentation matches the actual matrix after this change.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-27T16:15:54Z on `.github/workflows/gate.yml`:131 (association: MEMBER)

    <pre>
    Fixed: gate.yml header line 9-13 now reflects the post-Codex-P2 matrix (macos-26, ubuntu-24.04, ubuntu-24.04-arm; ubuntu-slim moved to nightly-low-memory.yml). The earlier inconsistency was leftover from when ubuntu-slim was experimental in both files.
    </pre>

## Fix commits (touching thread paths)

### `c51b7bfa9d65691478131e7806579996ba1662c9` -- 2026-04-27T15:26:53Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `memory/MEMORY.md`

**Message:**

```
substrate: port-with-DST discipline + AceHack-LFG diff-minimization i…

…nvariant (#43)

Aaron 2026-04-27 two more clarifications absorbed:

(5) Port-with-DST discipline: ../scratch and ../SQLSharp predate
    DST discovery; their no-DST shape made debugging hard. When
    porting, ADD DST (Otto-272) + pinned seeds (Otto-273) +
    flake-zero (Otto-248). Don't replicate the bad-debugging
    experience.

(6) AceHack-LFG diff-minimization invariant: long-horizon goal
    is 0-diff between AceHack and LFG main, OR a small enumerated
    allowlist of expected differences (.github/funding.yml, repo-
    name references, org-specific tokens). 'Few' is part of the
    bar.

Operational: build a 0-diff verification check under tools/sync/
that emits a report classifying each diff as expected
(allowlisted) or surprise (needs investigation). Sequenced after
laptop-source-integration (which may temporarily widen the diff).

Per AgencySignature v1 (Otto-138):
Author: Otto (Anthropic-side AI agent on the Zeta factory)
Origin-substrate: AceHack/Zeta main

Co-authored-by: Otto <noreply@anthropic.com>
```

### `bc63fff40388a8f9005bb7efee9cd7080e517453` -- 2026-04-27T15:45:10Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `.github/workflows/nightly-low-memory.yml`

**Message:**

```
ci: trigger low-memory verification on every merge to main + nightly …

…(#44)

Per Aaron 2026-04-27: 'no reason we don't change that nightly
job for slim to just trigger on every merge to main, it's free
for open source projects.'

The ubuntu-slim leg moved off per-PR gate (PR #43 / earlier
moved it to nightly) because it's ~7x slower than the regular
ubuntu-24.04 leg and times out at the 15-min runner-class cap
during PR review cycles.

But for post-merge to main, the elapsed time doesn't bottleneck
anything — main has already landed; the slim run is a drift
detector. Free for public repos (Otto-249 + Otto-210), so
running on every merge gives faster drift detection than
purely-nightly without cost.

New trigger surface:
  1. push to main (every merge) — primary drift detection
  2. daily 06:00 UTC schedule — backstop for weekend drift
  3. workflow_dispatch — manual ad-hoc verification

Per AgencySignature v1 (Otto-138):
Author: Otto (Anthropic-side AI agent on the Zeta factory)
Origin-substrate: AceHack/Zeta main

Co-authored-by: Otto <noreply@anthropic.com>
```

### `827159c855d3fff5c8ef99ea3681308e40bdb002` -- 2026-04-27T16:05:27Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `.github/workflows/gate.yml`
- `.github/workflows/nightly-low-memory.yml`

**Message:**

```
fix(threads): address Copilot P1 + Codex P2 on LFG #644

Five unresolved threads on the AceHack #43+#44 sibling sync; four are
real findings, fifth is hygiene-class deferred.

1. Codex P2 (duplicate CI on main pushes): LFG main's gate.yml still
   has ubuntu-slim in the build-and-test matrix — adding push-to-main
   trigger to nightly-low-memory.yml would run ubuntu-slim TWICE on
   every merge. Remove ubuntu-slim from gate.yml matrix here, mirroring
   AceHack main (where it was already removed). This is the kind of
   AceHack-LFG drift the 0-diff invariant from #43 catches — without
   the sibling sync, this divergence would have shipped silently.

2. Copilot P1 (header says "once per day", workflow runs on push too):
   Reframe header to describe the actual trigger surface (push to main
   + daily schedule + workflow_dispatch).

3. Copilot P1 ("What this workflow does" omits push trigger): Add
   push-to-main bullet to the section.

4. Copilot P1 (timeout-minutes: 30 vs 15-min runner cap): Reduce to
   14 to fail gracefully one minute before the platform kill, with a
   proper Actions log. Update the safe-pattern-compliance comment to
   match.

5. Copilot P1 (MEMORY.md entry too paragraph-long): Deferred — a
   broader index-hygiene pass under the existing MEMORY.md audit
   cadence, since multiple entries have grown long over time. Tightening
   only this one entry would create inconsistency without addressing
   the systemic concern.

Filename note: AceHack #45 renames `nightly-low-memory.yml` to
`low-memory.yml`. That rename syncs to LFG via a follow-up PR after
#45 lands; preserved here to keep this sibling sync minimal-scope.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
```

### `22f30f18e8a447a07ea5e47f46ce6615727c5f24` -- 2026-04-27T16:09:10Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `memory/MEMORY.md`

**Message:**

```
Merge remote-tracking branch 'origin/main' into lfg/sibling-pr-43-44-…

…fixes

# Conflicts:
#	memory/MEMORY.md
#	memory/project_install_script_language_strategy_post_install_typescript_pre_install_bash_powershell_python_for_ai_ml_2026_04_27.md
```

### `40be4201556ca0f34a8f0a9a5d4bd2a1baa9ee51` -- 2026-04-27T16:15:35Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `.github/workflows/gate.yml`
- `.github/workflows/nightly-low-memory.yml`

**Message:**

```
fix(threads): per-commit concurrency + remove ubuntu-slim from gate.y…

…ml header (Copilot P1 x2 + Codex P2)

Three new threads on LFG #644 after the merge-commit + earlier fix-up:

1. Codex P2 + Copilot P1 (concurrency-group queueing): with constant
   group "nightly-low-memory" + cancel-in-progress: false, GitHub
   Actions queues at most 1 running + 1 pending — older pending runs
   get REPLACED, never executing. That breaks "runs on every merge"
   and hides drift on intermediate states.

   Fix: per-commit concurrency group (group: low-memory-${{ github.sha }}).
   Each push to main gets its own slot, runs in parallel, no
   replacement. On schedule/workflow_dispatch the SHA is whatever main
   points at when the run starts; identical-SHA runs queue (cancel-
   in-progress: false) — the right behaviour, since the second run
   has nothing new to check.

   Standard runners are free for public repos (Otto-249), so the
   parallel-runs cost is zero.

2. Copilot P1 (gate.yml stale "Active legs" header): the header
   comment around line 9-13 still listed ubuntu-slim as an active
   leg. Update to reflect the post-#43+#44 + post-Codex-P2 reality:
   matrix is macos-26, ubuntu-24.04, ubuntu-24.04-arm; ubuntu-slim
   moved to nightly-low-memory.yml.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
```
