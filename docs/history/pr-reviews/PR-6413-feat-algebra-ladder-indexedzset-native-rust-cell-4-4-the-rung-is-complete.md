# PR #6413 -- feat(algebra-ladder): IndexedZSet native Rust cell → 4/4 (the rung is complete)

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 6413 |
| Title | feat(algebra-ladder): IndexedZSet native Rust cell → 4/4 (the rung is complete) |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-06-01T12:13:32Z |
| Merged at | 2026-06-01T12:16:24Z |
| Merge commit SHA | `eef692c431db33ab765a5281c6a4a795eec5cdbe` |
| Branch | `otto-cli/indexed-zset-rust-cell-4of4-2026-06-01` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/6413 |
| Changed files | 4 |
| Additions / deletions | +440 / -10 |

## Description

The native Rust **IndexedZSet** (`src/Core.Rust.Algebra/src/indexed_zset.rs`) joins F#/TS/C# — the algebra ladder **G-Set ⊂ Bag ⊂ Z-set ⊂ IndexedZSet is now 4/4**.

## What

Mirrors the TS reference oracle op-for-op — `empty` / `is_empty` / `key_count` / `tuple_count` / `of_groups` / `index_with` / `get` / `to_zset` / `add` / `neg` / `sub` / `join` — built on the Rust `ZSet` primitive + **native `Ord`** (the type's `Ord` IS the comparer-as-identity, the structural-hash contract the four oracles agree on; no `IComparer` plumbing).

### The bilinear join (the headline)

```text
join : (k → va → vb → C) → IndexedZSet<K,VA> → IndexedZSet<K,VB> → ZSet<C>
```

Merge-join on the shared key × cross-product the value Z-sets, output weight = **PRODUCT** (`w_A · w_B`, overflow-checked, mirroring F# `Checked.(*)` / C# `checked`), consolidate (sum + drop-zero) → **flat `ZSet<C>`**, key passed to `combine`. `⋈` is kept orthogonal to re-index (a separate `index_with` step), per DBSP. Bilinear ⇒ negatives propagate (a tombstone joined yields a tombstone) — the CALM / coordination-free property, no special-case delete branch.

## Verification (the treaty)

- Cross-verifies **byte-for-byte** against the shared `src/Core.TypeScript/indexed-z-set/golden-vectors.json` via `tests/indexed_zset_cross_verify.rs` — `expectedA` / `add` / `neg` / `sub` / `join` / `toZSet` / `keyCount` / `tupleCount`.
- `cargo build` + `cargo clippy --all-targets -- -D warnings` clean (production crate stays zero-dep; `serde_json` dev-only).
- `cargo test` green: `indexed_zset_cross_verify` + bag/gset/zset regression all pass.

Per `m-acc-multi-oracle`: the compilers don't lie; **agreement IS the verification.**

## Registry

`docs/PRIMITIVE-REGISTRY.md`: IndexedZSet **3/4 → 4/4** (ladder complete) + footer.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 0 |
| Resolved threads | 0 |
| Unresolved threads | 0 |
| Total review comments | 0 |
| Total fix commits (touching thread paths) | 0 |

## Review threads

(no review threads on this PR)

## Fix commits (touching thread paths)

(no commits in this PR touched files anchored by review threads)
