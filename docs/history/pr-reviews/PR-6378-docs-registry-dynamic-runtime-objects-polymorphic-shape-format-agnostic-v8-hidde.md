# PR #6378 -- docs(registry): dynamic-runtime-objects / polymorphic shape (format-agnostic v8 hidden-shape) + XML/YAML

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 6378 |
| Title | docs(registry): dynamic-runtime-objects / polymorphic shape (format-agnostic v8 hidden-shape) + XML/YAML |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-06-01T08:41:03Z |
| Merged at | 2026-06-01T08:53:59Z |
| Merge commit SHA | `6fe3629f8102602cb40deb229ffb3551c20e07b9` |
| Branch | `otto-cli/registry-dynamic-objects-polymorphic-shape-2026-06-01` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/6378 |
| Changed files | 1 |
| Additions / deletions | +2 / -1 |

## Description

Your direction (2026-06-01, from the Itron `Platform.Dynamic` survey): the **dynamic-runtime-defined-object** primitive — _"not just json … works english too"_ — as the **format-agnostic generalization of our v8 hidden-shape** (the IUnknown/DCOM-like polymorphic-diplomacy primitive = **Eve Protocol** 081KRW63S0008QG0R0030F8ZXA).

**Itron `Platform.Dynamic` (concept-only / clean-room, IP constraint):** `DynamicValue` (tagged union over `DynamicValueType`) / `DynamicObject` / `DynamicArray` = a self-describing runtime value tree; `ISerializedObject.Deserialize(Type)` = lazy-bind a blob to a static type *or* keep it dynamic; `PropertyPath` = `"a.b[3].c"` navigation. **It's JSON-coupled** (the `*JsonConverter` files) — exactly what you flagged.

**The move:** ride our existing **`ISerializer<'T>` seam** (`src/Core/Serializer.fs`) so the SAME dynamic-object model works over **any** format — JSON / XML / YAML / protobuf / Arrow … **and English** (the v8 hidden-shape already runs over English + Bayesian-over-English). That's runtime `QueryInterface` — ask a value *"what shape do you support?"* with no compile-time type = polymorphic diplomacy across formats.

Captured as a wish-list family composing the serializer roster + Eve Protocol (081KRW63S0008QG0R0030F8ZXA) + the v8 substrate (`docs/research/2026-05-19-mika-lior-v8-system-architecture-...`). Also added **XML + YAML** to the serializer roster ('duh').

Docs-only; prettier + markdownlint clean; BACKLOG.md untouched.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 1 |
| Resolved threads | 1 |
| Unresolved threads | 0 |
| Total review comments | 1 |
| Total fix commits (touching thread paths) | 2 |

## Review threads

### Thread 1 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GDhwj`
- **Path**: `docs/PRIMITIVE-REGISTRY.md`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-01T08:43:43Z on `docs/PRIMITIVE-REGISTRY.md`:48 (association: NONE)

  <pre>
  This new reference prints a research filename that contains contributor/persona names in visible prose. Per the repo’s “No name attribution in code, docs, or skills” standing rule (docs/AGENT-BEST-PRACTICES.md, Operational standing rules), current-state docs like this should prefer role-refs and avoid showing names in the rendered text. Consider switching to a normal markdown link with neutral link text so the URL can stay the same but the displayed text doesn’t include names.
  </pre>

## Fix commits (touching thread paths)

### `af688fcde6f76b497498eb1be1a1e8ad7393e65e` -- 2026-06-01T08:40:59Z -- ``

**Touched files (intersect with thread paths):**

- `docs/PRIMITIVE-REGISTRY.md`

**Message:**

```
docs(registry): dynamic-runtime-objects/polymorphic-shape (format-agn…

…ostic v8 hidden-shape) + XML/YAML (maintainer)

Maintainer 2026-06-01 (Itron Platform.Dynamic survey): want the dynamic-runtime-defined-object
primitive — 'not just json … works english too' — as the format-agnostic generalization of our
v8 hidden-shape (IUnknown/DCOM-like polymorphic diplomacy, Eve Protocol 081KRW63S0008QG0R0030F8ZXA).

Captured (concept-only / clean-room from Itron Platform.Dynamic — DynamicValue tagged-union +
ISerialized<T> lazy-bind + PropertyPath): the SAME dynamic-object model riding the ISerializer<'T>
seam so it works over ANY format (JSON/XML/YAML/protobuf/Arrow … + English/Bayesian-over-English),
not JSON-coupled like theirs. = runtime QueryInterface / polymorphic diplomacy across formats.
Also added XML + YAML to the serializer roster ('duh').

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `a0b753f08946059e8757090d3b34b5bda601bafc` -- 2026-06-01T08:45:49Z -- ``

**Touched files (intersect with thread paths):**

- `docs/PRIMITIVE-REGISTRY.md`

**Message:**

```
docs(registry): neutral link text for v8 research-doc ref (no-name-at…

…tribution rule, Copilot #6378)

Copilot correctly flagged the bare filename ref printed persona names in rendered prose
(violates 'No name attribution in code, docs, or skills', AGENT-BEST-PRACTICES.md). Switched
to a markdown link with neutral link text — names stay in the href, out of the rendered text;
the precise pointer is preserved (location-pointer-index discipline).

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```
