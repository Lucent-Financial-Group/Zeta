# PR #9181 -- feat(core): SchedulerZeta weak fixed-point table - orbits dynamically loaded/unloaded (Shiva/GC)

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 9181 |
| Title | feat(core): SchedulerZeta weak fixed-point table - orbits dynamically loaded/unloaded (Shiva/GC) |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-07-02T18:41:32Z |
| Merged at | 2026-07-02T18:41:36Z |
| Merge commit SHA | `215e784ba3e5a0d89efe8fdea5314e8020e2b237` |
| Branch | `otto/scheduler-zeta-weak-fixed-point-table` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/9181 |
| Changed files | 2 |
| Additions / deletions | +85 / -0 |

## Description

You: *our weak reference tables so the fixed points can be dynamically loaded and unloaded.* The hard foundation (soft-GC-via-fingerprint is next).

The fixed points of a run (the states ON its orbit — the recurrence the zeta identifies) are **derived**: recomputable via the O(reachable) run-ahead. So they never need pinning — hold them **weakly**, let the GC (Shiva) reclaim them, regenerate on next access. cogen-as-memory-management, the `SpecializationCache` philosophy applied to the scheduler's recurrence.

## src/Core/SchedulerZeta.fs
- `orbitStates` — the recurrent set (fixed points), transient excluded;
- `FixedPointCache(key, step, start)` — a `WeakReference<'S[]>` table: `Orbit()` loads from the weak cache or regenerates if the GC unloaded it; `Unload()` is the explicit Shiva sweep; Hits/Misses. Regeneration is **lossless** (derived).

## Tests (7/7, +2)
- load → cache-hit (same instance) → **Unload (Shiva)** → reload regenerates the *same* fixed points exactly;
- `orbitStates` returns only the recurrent set (transient excluded).

**Next (your live extension):** tie into soft scheduling for **soft GC** — evict cached fixed points by fingerprint soft-selection weight (games not strongly selected in prediction mode get reclaimed), like the game fingerprints.

Anchors: WeakReference / SpecializationCache; Artin-Mazur 1965; #9175.

Generated with Claude Code

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 1 |
| Resolved threads | 0 |
| Unresolved threads | 1 |
| Total review comments | 1 |
| Total fix commits (touching thread paths) | 1 |

## Review threads

### Thread 1 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM6N_x6l`
- **Path**: `src/Core/SchedulerZeta.fs`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-07-02T18:43:49Z on `src/Core/SchedulerZeta.fs`:197 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Return a defensive copy from the weak cache**

  This matters when any caller mutates the returned array, e.g. sorting it, clearing it, or assigning an element: `Orbit()` returns the exact cached array instance, so that mutation corrupts the cache and a later hit returns the mutated orbit rather than regenerating the derived fixed points. The tests even keep this instance alive between calls, so the weak reference will hit and preserve the corruption; return an immutable/read-only shape or copy on the public boundary if the cache is meant to remain “always exact.”

  Useful? React with 👍 / 👎.
  </pre>

## Fix commits (touching thread paths)

### `d7b92454ab508dcc2818b0087066f9e551c8ec8c` -- 2026-07-02T18:41:29Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `src/Core/SchedulerZeta.fs`

**Message:**

```
feat(core): SchedulerZeta weak fixed-point table — orbits dynamically…

… loaded/unloaded (Shiva/GC) (shadow*)

Aaron: "our weak reference tables so the fixed points can be dynamically loaded and
unloaded." The hard foundation for soft-GC-via-fingerprint (next).

The fixed points of a run (the states ON its orbit — the DBSP/scheduler recurrence the
zeta identifies) are DERIVED: recomputable via the O(reachable) run-ahead. So they need
never be pinned — hold them WEAKLY and let the GC (Shiva) reclaim them; regenerate on
next access. cogen-as-memory-management, the SpecializationCache philosophy applied to
the scheduler's recurrence.

src/Core/SchedulerZeta.fs:
  • orbitStates key step start — the recurrent set (fixed points), transient excluded;
  • FixedPointCache(key, step, start) — a WeakReference<'S[]> table: Orbit() loads from
    the weak cache or regenerates (O(reachable)) if the GC unloaded it; Unload() is the
    explicit Shiva sweep; Hits/Misses metrics. Regeneration is LOSSLESS (derived).

tests/Tests.FSharp/SchedulerZeta.Tests.fs (7/7, +2): load → cache-hit (same instance) →
Unload (Shiva) → reload regenerates the SAME fixed points exactly; orbitStates returns
only the recurrent set (transient excluded).

Next (Aaron's live extension): tie into soft scheduling for SOFT GC — evict cached
fixed points by fingerprint soft-selection weight (games not strongly selected in
prediction mode get reclaimed), like the game fingerprints.

Anchors: WeakReference / cogen-as-memory-management (SpecializationCache); Artin–Mazur
1965 (the orbit = the fixed points); #9175 (runToHorizon regeneration).

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>

AgencySignature-v1:
  persona: otto
  actor: zeta-otto
  surface: src/core
  topology: shadow-clone
  intent: scheduler-zeta-weak-fixed-point-table-orbits-dynamically-loaded-unloaded-shiva-gc
  authorization: aaron-explicit-weak-reference-tables-fixed-points-loaded-unloaded-2026-07-02
  uncertainty: low
  measure: fixedpointcache-weakreference-orbit-load-hit-unload-shiva-regenerate-lossless-o-reachable-orbitstates-recurrent-set-transient-excluded-7-tests
  delta-u: scheduler-fixed-points-weakly-held-dynamically-loadable-unloadable-derived-lossless-foundation-for-soft-gc
  seed: S4
```
