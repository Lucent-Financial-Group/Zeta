# PR #2502 -- feat(B-0321): guarded UI mutation helpers with authorization + before/after snapshots

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 2502 |
| Title | feat(B-0321): guarded UI mutation helpers with authorization + before/after snapshots |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-05-10T14:19:50Z |
| Merged at | 2026-05-10T14:27:33Z |
| Merge commit SHA | `304a6e661adabdb9c917302bd0ce87cef7b9ffec` |
| Branch | `feat/B-0321-guarded-mutation-helpers-2026-05-10` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/2502 |
| Changed files | 4 |
| Additions / deletions | +855 / -7 |

## Description

## Summary

- Implements `tools/playwright/github-ui/mutate.ts` — Phase 2 of B-0064 (friction-reduction payload)
- All guardrails enforced **in code** with no bypass paths: authorization check, mandatory before/after snapshot pair, inverse action computation, selector-injection guard on `toggleKey`
- Drain log entry interface stubbed; write implemented by B-0322 (the next child)
- 25 unit tests with full fake driver covering authorization rejection, param validation, toggle round-trip, drain log entry shape, and snapshot pair invariant

## Guardrails detail

| Guardrail | Where enforced |
|---|---|
| Authorization (surfaceId + action in B-0320 list) | `authorizeRequest()` — returns `{ success: false }` with no session opened |
| Selector injection (`toggleKey` pattern) | `validateParams()` — validated before session opens |
| Before snapshot mandatory | `captureSnapshot()` called before `executeAction()` |
| After snapshot mandatory | `captureSnapshot()` called after `executeAction()` (page reload) |
| Inverse action required | `inverseOf()` computed before session opens — unknown actions fail fast |

## Test plan

- [x] `bun test tools/playwright/github-ui/mutate.test.ts` → 25 pass, 0 fail
- [x] `bun test tools/playwright/github-ui/` → 143 pass (no regressions in auth/snapshot/feature-diff/reconcile/billing/authorized-surfaces)
- [x] `bun tsc --noEmit` → 0 errors

## Dependencies

- B-0317 (auth helper) — closed ✓
- B-0318 (snapshot tool) — closed ✓
- B-0320 (authorized surfaces) — closed ✓

## What's next

- B-0322: drain-log.ts — writes MutationLogEntry to `docs/hygiene-history/playwright-mutations/log.jsonl`

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 6 |
| Resolved threads | 6 |
| Unresolved threads | 0 |
| Total review comments | 6 |
| Total fix commits (touching thread paths) | 1 |

## Review threads

### Thread 1 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6A58XI`
- **Path**: `tools/playwright/github-ui/mutate.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-10T14:22:10Z on `tools/playwright/github-ui/mutate.ts`:143 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Enforce surface URL constraints during authorization**

  `authorizeRequest` only checks `surfaceId` and `allowedActions`, but never validates `request.params.url` against the allow-listed surface definition, even though every mutation later navigates to `request.params.url`. This lets a caller use an approved `surfaceId`/action pair and still drive clicks on an arbitrary GitHub page, bypassing the intended surface-level guardrail from `authorized-surfaces.json`.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 2 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6A58XL`
- **Path**: `tools/playwright/github-ui/mutate.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-10T14:22:10Z on `tools/playwright/github-ui/mutate.ts`:223 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Make toggle-on/off honor target state before clicking**

  Both `toggle-on` and `toggle-off` execute an unconditional single click, so the helper behaves like `toggle` rather than a state-setting action. If the checkbox is already in the requested state, this flips it to the opposite value (e.g., `toggle-on` can disable a control), producing the inverse of what the action name promises and potentially applying unsafe settings changes.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 3 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6A58xX`
- **Path**: `tools/playwright/github-ui/mutate.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-10T14:23:49Z on `tools/playwright/github-ui/mutate.ts`:152 (association: NONE)

  <pre>
  P0 (security): Authorization currently checks only {surfaceId, action}. Since the request also supplies params.url (used for navigation), a caller can pass an authorized surfaceId/action but point url at an arbitrary page, effectively bypassing the authorized-surfaces allow-list’s intended scope (authorized-surfaces.json also includes urlPattern). Please validate that request.params.url is https://github.com/... and matches the authorized surface’s urlPattern (incl. &lt;org&gt;/&lt;repo&gt; placeholders), and fail closed if it doesn’t.
  </pre>

### Thread 4 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6A58xg`
- **Path**: `tools/playwright/github-ui/mutate.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-10T14:23:49Z on `tools/playwright/github-ui/mutate.ts`:229 (association: NONE)

  <pre>
  P0 (bug): toggle-on and toggle-off both unconditionally click the selector, so the mutation is not directionally correct or idempotent (e.g., toggle-on against an already-on toggle will actually turn it off). Please ensure the executor only clicks when the current toggle state differs from the desired state, and consider verifying the post-mutation state before proceeding/logging success.

  </pre>

### Thread 5 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6A58xm`
- **Path**: `tools/playwright/github-ui/mutate.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-10T14:23:50Z on `tools/playwright/github-ui/mutate.ts`:221 (association: NONE)

  <pre>
  P1 (bug/robustness): The selector uses `input#${toggleKey}`. CSS id selectors can require escaping (e.g., ids starting with digits), which can make otherwise-valid toggleKey values fail at runtime. Prefer an attribute selector for ids (e.g., input[id="..."]) to avoid CSS escaping pitfalls.

  </pre>

### Thread 6 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6A58xp`
- **Path**: `docs/backlog/P1/B-0321-playwright-github-guarded-mutation-helpers.md`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-10T14:23:50Z on `docs/backlog/P1/B-0321-playwright-github-guarded-mutation-helpers.md`:5 (association: NONE)

  <pre>
  P1 (codebase convention): This backlog row’s frontmatter `last_updated` must be updated on every content edit. The row was edited (status + new sections), but `last_updated` is still 2026-05-08; please bump it to today’s date.
  </pre>

## Fix commits (touching thread paths)

### `f7e23a11ec3224182c0cf722195d9b1c0afc6666` -- 2026-05-10T14:19:33Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `docs/backlog/P1/B-0321-playwright-github-guarded-mutation-helpers.md`
- `tools/playwright/github-ui/mutate.ts`

**Message:**

```
feat(B-0321): guarded UI mutation helpers with authorization + before…

…/after snapshots

Implements `tools/playwright/github-ui/mutate.ts` — Phase 2 of B-0064.

Guardrails enforced in code (no bypass paths):
- Authorization: surfaceId + action must appear in authorized-surfaces.json (B-0320)
- Snapshot pair: before AND after snapshots mandatory for every mutation
- Inverse action: computed and recorded in drain log entry before session opens
- Param validation: toggleKey validated against /^[a-zA-Z0-9_-]+$/ to prevent selector injection

Supported mutations: toggle-on / toggle-off (checkbox click).
Drain log entry shape defined (B-0322 implements the write).
Diff via diffPageSnapshots from feature-diff.ts (B-0323).

25 unit tests across:
- Authorization rejection (unlisted surface, unlisted action)
- Param validation (selector injection guard, unknown inverse)
- Successful toggle-on: before=false, after=true, diff.changedToggles populated
- Drain log entry: UUID, ISO timestamp, inverseAction, status "applied", JSON-serializable
- Snapshot pair invariant: both captured, distinct, included in entry

All 143 existing playwright/github-ui tests still pass. 0 TSC errors.

operative-authorization: aaron 2026-05-04: "it**, not just the output. Grinding through failures + recoveries"

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>
```
