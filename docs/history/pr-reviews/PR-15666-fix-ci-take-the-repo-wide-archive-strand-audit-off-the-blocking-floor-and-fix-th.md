# PR #15666 -- fix(ci): take the repo-wide archive-strand audit off the blocking floor — and fix the guard that went blind when cross-verify was split

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 15666 |
| Title | fix(ci): take the repo-wide archive-strand audit off the blocking floor — and fix the guard that went blind when cross-verify was split |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-08-26T20:20:36Z |
| Merged at | 2026-08-26T21:03:38Z |
| Merge commit SHA | `09bd8b85070bd1ce32cbe7d5698dd1025959b8d0` |
| Branch | `shadow/archive-strand-alarm-rescope` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/15666 |
| Changed files | 8 |
| Additions / deletions | +1208 / -32 |

## Description

Replaces #15552, which was written against a `cross-verify` that no longer exists.

## READ THE DIFF AGAIN — it now touches a file the approved version never did

Aaron approved the principle on #15552: *"yes, caught on schedule is fine, this is closer to our drift checks anyways."* **That approval does not cover this diff, and should not be treated as if it does.** Between #15552's merge base and today, `cross-verify` was restructured from 31 inline steps into a **31-leg matrix** driven by `src/Core.TypeScript/ci/cross-verify-roster.ts`. The 394-line region #15552 edited was replaced by one line, and the faithful translation of the same subtraction now edits **`cross-verify-roster.ts`** — a file the approved version never touched. Please re-read.

## What is unchanged from what was approved

The defect. `needs: cross-verify` waits on **all** matrix legs, so `cross-verify (orphaned-archive-refs)` still blocks every PR on repo-wide, time-varying stranded-ref state no PR author can remedy. Main's split fixed the *misdiagnosis* half — the red now names the audit instead of wearing the byte-lock's costume — but not the **blocking** half.

## Part 1 — the re-scope

- `- orphaned-archive-refs` leaves `gate.yml`'s `audit:` matrix **and** the `{ id: "orphaned-archive-refs", … }` entry leaves `CROSS_VERIFY_AUDITS`. **Both or neither**: `compareRosterToMatrix` compares the two lists **as sequences**, so one alone is a different and misleading red. Verified locally, not assumed: `--check-parity` → `roster and gate.yml agree on 30 audits. ✓`, rc=0.
- `.github/workflows/archive-strand-alarm.yml` — unchanged from #15552: `13,43 * * * *`, the audit's **exit code is the job's** (no `continue-on-error`, no `|| true`, no `| tee`), one deduplicated tracking issue, `secrets.GITHUB_TOKEN` with `contents: read` + `issues: write`, no `pull_request` trigger.
- `docs/ci/CROSS-VERIFY-CHECK-NAMES.md` drops the row and says **do not promote that string** — a required context that never reports does not fail a PR, it wedges it.
- The reasoning prose moves with the leg, to the removed roster entry.

`cross-verify-roster.test.ts` had `expect(ROSTER_IDS).toHaveLength(31)` and **went red on the removal** — which is that assertion doing its job. It is now 30 plus `not.toContain("orphaned-archive-refs")`, so this is recorded as a decision rather than a disappearance.

## Part 2 — the more important half: the guard had gone blind on `main`

`floor-live-remote-queries.ts` is #15552's falsifier, and it was **vacuously green on `main` right now**. It shipped resolving **one hop** (`bun x.ts` in a `run:` block, x.ts read). The split inserted a second hop — `gate.yml` → `cross-verify-roster.ts` → `audit-orphaned-archive-refs.ts` — and the guard went quiet. **The `ls-remote` never moved; only the detector did.** Nobody edited the guard, nobody edited the audit: this is the vacuity class arriving by *structural change* rather than by authoring, and a blind floor check is worse than the blocking problem #15552 was written to solve.

Resolution now walks the graph **transitively**, over `bun <path>.ts` invocations found in a `run:` block *and inside any already-visited file*, plus **relative** local imports/re-exports/static `import("./x")`. Bounded at `MAX_RESOLUTION_HOPS = 4`, visited-set cycle guard. Findings carry `via`, so the red names the chain.

**Still opaque, stated in the header rather than implied away:** shell indirection (`bash foo.sh`, `xargs`, a Makefile), dynamic `import()` with a computed specifier, runtime-built paths, and `spawnSync(bin, args)` with a computed command. A bounded guard that says where it stops is honest; one that implies completeness is not.

## Verification — every number measured, rc read directly, never through a pipe

**1. `gate-required.needs` as a set difference.** 8 jobs at the merge base, 8 on `main`, 8 here. `comm -23 main after` empty, `comm -13 main after` empty, `diff base after` rc=0. Nothing that could block before can fail to block now. The matrix job keeps the id `cross-verify`, so nothing about how the floor resolves changes.

**2. `actionlint .github/workflows/gate.yml .github/workflows/archive-strand-alarm.yml`** → **rc=0**.

**3. The guard, three separate runs, all with the FIXED code:**

| tree | rc | |
|---|---|---|
| merge base `d8a55190bf` | **1** | RED — the control. `cross-verify -> audit-orphaned-archive-refs.ts` |
| `origin/main` `06914eec9e`, **pre-removal** | **1** | RED — `cross-verify -> cross-verify-roster.ts -> audit-orphaned-archive-refs.ts`. **The same tree is rc=0 under the one-hop code.** |
| this branch, post-removal | **0** | GREEN |

**4. Roster/matrix parity** → rc=0, 30 audits — which is what proves *both* lists were edited.

**5. The regression is pinned, and the pin is mutation-checked.** Reverting `MAX_RESOLUTION_HOPS` to `1` turns exactly the three positive pins red (two-hops-away, behind-an-import, cycle) and leaves the rest green — measured, not asserted. The fixture block also carries a **removal control** (same tree, leg dropped from both lists → green), without which an always-firing detector would satisfy the positive tests.

**6. Also green locally:** `bun test` on both `ci/` suites (56 pass / 0 fail), the same test under `--config=bunfig.hermetic.toml` (so `test-typescript-hermetic` discovers it — it is in neither bunfig's ignore list), `tsc --noEmit` rc=0, `lint-typescript` rc=0, `markdownlint-cli2` on the edited doc rc=0, and the `cross-verify` legs `action-sha-roster`, `credential-role-separation`, `write-token-consistency`, `step-output-writers`, `heartbeat-lane-attestations` all rc=0.

**Named, and not mine.** `hygiene:check-arity-nonequality` is **rc=1 on `main`**: `tools/setup/persona-keys/frost-hsm-secrets.test.ts` raised its absence-under-taint count in #15642 without raising `registry/check-arity-nonequality-census.json`, last touched by #11501. No file in this diff is involved. Separately, `lint (bash retirement inventory + hygiene unit tests)` is a measured ~20% flake.

## What stops being caught, plainly

A merge can land while a record is stranded. Detection latency becomes at most 30 minutes instead of "the next pull request anyone opens" — a gain at 03:00, a loss only against a 120-minute grace window it already fits inside. In practice the strand was being cleared by re-running the PR, not by landing the record, and a check that clears itself on re-run is already flake.

Nothing else is added to or removed from the floor. No `required_status_checks` setting is touched.

Agency-Signature-Version: 1
Agent: shadow
Agent-Runtime: claude-code
Agent-Model: claude-opus-5
Credential-Identity: AceHack
Credential-Mode: shared
Human-Review: not-implied-by-credential
Human-Review-Evidence: none
Action-Mode: autonomous-fail-closed
Task: none
Co-authored-by: Claude Opus 5 <noreply@anthropic.com>


## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 0 |
| Resolved threads | 0 |
| Unresolved threads | 0 |
| Total review comments | 0 |
| Total fix commits (touching thread paths) | 0 |

## Review threads

(no review threads on this PR)

## Fix commits (touching thread paths)

(no commits in this PR touched files anchored by review threads)
