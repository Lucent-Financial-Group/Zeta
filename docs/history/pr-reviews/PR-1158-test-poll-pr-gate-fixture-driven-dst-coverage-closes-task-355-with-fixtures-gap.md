# PR #1158 -- test(poll-pr-gate): fixture-driven DST coverage — closes task #355 with-fixtures gap

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 1158 |
| Title | test(poll-pr-gate): fixture-driven DST coverage — closes task #355 with-fixtures gap |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-05-01T20:46:57Z |
| Merged at | 2026-05-01T20:49:05Z |
| Merge commit SHA | `31e4109f9847e33722cb3242d6a9febe0760269f` |
| Branch | `otto/poll-pr-gate-fixture-tests-2026-05-01` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/1158 |
| Changed files | 2 |
| Additions / deletions | +167 / -5 |

## Description

## Summary

Task #355 ("Poll-the-gate as executable script with fixtures — 5-AI convergent") promised both halves: the v1 single-PR script landed with the fixture set, but no test runner exercised the fixtures. The fixtures were unused infrastructure.

This PR closes the gap with a fixture-driven DST suite — 28 tests / 162 expects / 51ms / zero gh spawns.

## Changes

1. **`tools/github/poll-pr-gate.ts`**: pure additive `export` keyword on `PullRequestData`, `CheckCounts`, `GateReport`, `buildReport`, `loadFixture`. No behavior change; CLI surface unchanged.

2. **`tools/github/poll-pr-gate.test.ts`** (new): fixture-driven test suite. Per-fixture assertions for each of the 7 canonical scenarios (clean / blocked-by-threads / dirty / behind / non-required-failure / status-context-error / unknown-mergeability) + cross-fixture invariants (checks-counts integers, requiredChecks subset of checks, nextAction consistent with gate).

## Worked-example proof

Paired with the batch tool's DST suite landed in PR #1153, the v1 single-PR script now carries DST grade-A coverage. Both halves of task #355's deliverable (\"executable script\" + \"with fixtures\") are now substrate.

The `non-required-failure-warning.json` fixture's test is particularly load-bearing — it asserts the v1 hardening contract from peer-review 2026-04-30 (Codex + Copilot dual-review): non-required check failures must produce warnings, not gate failures, and `nextAction` must proceed based on required-only state.

## Test plan

- [x] `bun test tools/github/poll-pr-gate.test.ts` → 28 pass / 0 fail / 51ms
- [x] Pure additive export change preserves CLI surface (live-tested `bun tools/github/poll-pr-gate.ts --fixture <path>` for one fixture)
- [x] Cross-fixture invariants caught no violations across the 7-fixture set

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 1 |
| Resolved threads | 0 |
| Unresolved threads | 1 |
| Total review comments | 1 |
| Total fix commits (touching thread paths) | 1 |

## Review threads

### Thread 1 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM5_DULU`
- **Path**: `tools/github/poll-pr-gate.test.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-01T20:49:28Z on `tools/github/poll-pr-gate.test.ts`:158 (association: NONE)

  <pre>
  P2: In the cross-fixture invariant test, the `if` assertions for `gate === "DIRTY"`, `state === "MERGED"/"CLOSED"`, and `requiredChecks.failed &gt; 0` aren’t mutually exclusive. If a future fixture ever satisfies more than one condition (e.g., DIRTY + required failure), this can create contradictory expectations that don’t match `nextAction`’s precedence. Consider rewriting this as an explicit precedence chain (if/else-if or switch) mirroring `nextAction` so the invariant suite stays stable as fixtures evolve.

  </pre>

## Fix commits (touching thread paths)

### `062a0c621184fdc1c74ecc7982a2154f62e10d19` -- 2026-05-01T20:46:54Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/github/poll-pr-gate.test.ts`

**Message:**

```
test(poll-pr-gate): fixture-driven DST coverage — closes task #355 "w…

…ith fixtures" gap

Task #355 promised "Poll-the-gate as executable script with
fixtures" — the fixture set (7 JSON files under
tools/github/fixtures/) landed in v1, but no test runner
exercised them. The fixtures were unused infrastructure.

This commit closes the gap. Two minimal changes:

(1) tools/github/poll-pr-gate.ts: add `export` to PullRequestData,
    CheckCounts, GateReport, buildReport, and loadFixture so they
    can be imported into the test file. Pure additive — no
    behavior change, CLI surface unchanged.

(2) tools/github/poll-pr-gate.test.ts (new): fixture-driven
    DST suite. 28 tests / 162 expects / 51ms / zero gh spawns.

Per-fixture coverage:
- clean-armed-auto-merge.json → CLEAN gate, autoMerge armed
- blocked-by-threads.json → BLOCKED with resolve-threads (the
  exact scenario the BLOCKED-with-green-CI wake-time bullet targets)
- dirty-auto-merge-armed.json → DIRTY gate, rebase next action
- behind-needs-rebase.json → BEHIND collapses to DIRTY (Copilot
  P0 from v1 hardening)
- non-required-failure-warning.json → v1 hardening's load-bearing
  case: non-required check failure produces warning, not gate
  failure
- status-context-error.json → StatusContext ERROR maps to
  blocking conclusion (Codex P1 normalization)
- unknown-mergeability-pending-recompute.json → UNKNOWN
  mergeStateStatus surfaces honestly

Cross-fixture invariants (test.each across all 7):
- checks counts are non-negative integers
- requiredChecks counts bounded by total checks (subset
  invariant)
- nextAction consistent with gate state (DIRTY→rebase,
  MERGED→verify-merge, CLOSED→none, requiredChecks.failed>0
  → fix-failed-checks)

Worked-example proof — paired with the batch tool's DST suite
landed in PR #1153, the v1 single-PR script now also carries
DST grade-A coverage. Both halves of task #355's deliverable
("executable script" + "with fixtures") are now substrate.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
```
