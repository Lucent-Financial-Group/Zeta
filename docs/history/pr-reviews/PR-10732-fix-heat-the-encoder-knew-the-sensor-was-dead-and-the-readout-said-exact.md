# PR #10732 -- fix(heat): the encoder knew the sensor was dead and the readout said "exact"

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 10732 |
| Title | fix(heat): the encoder knew the sensor was dead and the readout said "exact" |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-08-14T21:52:53Z |
| Merged at | 2026-08-17T04:24:11Z |
| Merge commit SHA | `447ac0ab49ee18b3fa5365d9c102dede3eb5007d` |
| Branch | `shadow/fail-dangerous-nan-encoder-paths` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/10732 |
| Changed files | 9 |
| Additions / deletions | +576 / -15 |

## Description

## Re-measured first — two of the six reported defects were already fixed

The brief listed six fail-dangerous readings. Every one was re-measured against `origin/main@0cb3642eb` **before** anything was changed, because asserting a stale defect is the exact failure this work has spent the day cataloguing.

| reported | still reproduces? |
|---|---|
| `temperatureBand(NaN \| Infinity \| -1)` = `"cold"` | **partly, and by design** — the bare accessor still returns `cold` (four-oracle treaty tokens, cannot move), but `temperatureBandReading(NaN).verdict = "out-of-domain"` separates it from `in-range` |
| `heatReceiptPpm(NaN \| Infinity)` = `0` | **partly, and by design** — accessor still `0`; `heatReceiptScale(NaN).fidelity = "out-of-domain"` vs `heatReceiptScale(0).fidelity = "exact"` |
| F# `pct(nan)` → a confident 0% bar | **NO — fixed by #10722.** `pct` no longer exists |
| F# `pct(infinity)` → a confident 100% bar | **NO — fixed by #10722.** `UnitInterval.tryOf` rejects them; `render` emits a labelled broken bar |

```
$ dotnet test --filter "FullyQualifiedName~SocietalDoraSvg"
Passed!  - Failed: 0, Passed: 10, Skipped: 0, Total: 10
exit 0
```

## The live defect is worse than what was reported — and #10722 introduced it

#10722 gave each encoder a fidelity channel. It did not make that channel survive **composition**. `darkhall-room.ts` fed the lossy accessor straight into `temperatureReadout`:

```
heatRejected=0         scale.fidelity=exact          -> ppm=0 -> readout.fidelity=exact
heatRejected=NaN       scale.fidelity=out-of-domain  -> ppm=0 -> readout.fidelity=exact
heatRejected=Infinity  scale.fidelity=out-of-domain  -> ppm=0 -> readout.fidelity=exact
heatRejected=-1        scale.fidelity=out-of-domain  -> ppm=0 -> readout.fidelity=exact
```

**The encoder knew. The readout said `exact` anyway.**

That is strictly worse than the silence it replaced. An unwired fidelity field does not merely *omit* the fault — it **positively asserts** a faithfulness nothing measured, and an operator or an agent can now cite it. A field that can only ever say `exact` is the vacuity class: a check that cannot fail, wearing the fix's clothes.

## The fix

Extends the merged `fidelity` vehicle rather than inventing a parallel mechanism, as the brief asked.

- **`worstFidelity` + `FIDELITY_SEVERITY`** — declared order `exact < below-resolution < saturated < out-of-domain`. Idempotent, commutative, associative, so it folds over a *set*; arrival order cannot change a rendered verdict.
- **`temperatureReadout` takes `upstreamFidelity?`** — an **argument, not a schema field**. `TemperatureReadout`'s shape is deliberately unchanged: the sibling agent on `shadow/treaty-cannot-see-key-set-divergence` holds the key-set question, and adding a required field to a published `v1` is the very divergence `081M010WYE5087G0R003J89QVF` §2 is open about.
- **`darkhall-room.ts` encodes via `heatReceiptScale`**, not `heatReceiptPpm`, forwarding all four fidelities.
- **`data-temperature-fidelity`** on the room element, TV lane and dweller element; a worded suffix (`· SENSOR FAULT · no reading`, `· PINNED · at or above ceiling`, `· below resolution`); CSS repainting an out-of-domain lane. Words *as well as* colour, because `cold` already renders in the muted `--txt3` — colour alone cannot say whether muteness means *quiet* or *not measured*.

## Evidence the tests discriminate

Assertions expressible against the **pre-fix** API (`upstreamFidelity` is an unread extra property there, so the harness runs against both revisions):

```
check                                                    | pre-fix | post-fix
a NaN heat count does not report fidelity 'exact'        | FAIL    | PASS
blind room and idle room differ in fidelity              | FAIL    | PASS
an Infinity heat count does not report fidelity 'exact'  | FAIL    | PASS
blind room reports fidelity 'out-of-domain'              | FAIL    | PASS
an above-ceiling count reports 'saturated', not 'exact'  | FAIL    | PASS
--- controls, must pass BOTH ways ---
an idle room still reports 'exact'                       | PASS    | PASS
a healthy room still reports 'exact'                     | PASS    | PASS
a healthy room still reads band 'warm'                   | PASS    | PASS
```

5 of 5 discriminate; the 3 controls hold, so this is not "alarm on everything".

Renderer side — sources reverted to `origin/main` with the new tests kept:

```
✗ emits the fidelity as an attribute the CSS and an auditor can both read
✗ renders a blind room differently from an idle room (pre-fix: byte-identical)
✗ says the fault in words, not only in colour
✗ labels a pinned gauge as pinned rather than as a high reading
✗ leaves a healthy lane unmarked — no false alarm, and no chartjunk
✗ carries a CSS rule that repaints an out-of-domain lane
 17 pass, 6 fail    exit 1
```

With the fix: `246 pass, 0 fail`, exit `0` across all 21 test files that touch the changed modules.

## Owned errors and flagged corrections to the brief

1. **Correction — the F# half of the brief no longer reproduces.** `pct(nan)`/`pct(infinity)` were fixed by #10722. Reported rather than re-fixed.
2. **Correction — the TypeScript half reproduces only at the bare accessor, and deliberately.** The band token and the ppm value are treaty-locked; #10722 put the answer in a separable channel. Calling that unfixed would have been wrong.
3. **Owned limit on one test.** *"leaves a healthy lane unmarked"* also fails pre-fix, because it asserts an attribute pre-fix does not emit. It is a weaker control than the three attribute-independent ones, and is recorded as such rather than presented as a clean both-ways control.
4. **A gate that did not run to completion.** The full `bun test` run exceeds a 10-minute window and was killed. The 21 dependent test files were run instead. The gap is named rather than implied away — a check that did not run must not read as one that passed.
5. **Pre-existing formatting left alone.** `darkhall-browser-controller-input.ts` is unformatted on `main`; verified pre-existing by checking it on a stashed tree, not assumed.

## Gates

```
bun run typecheck          exit 0
bun run lint:typescript    exit 0   (TypeScript, Prettier and style checks passed)
bun run lint:markdown      exit 0
bun test <21 dependent>    exit 0   246 pass, 0 fail
dotnet test SocietalDoraSvg exit 0  10 passed
```

`hall/tv/index.html` is the committed generated artifact and is regenerated here.

## Residual filed, not hidden

**`081M01400RZ087G0R000PS3VJG`** — `HeatReceipt` has no fidelity channel, so the receipt rails still paint a blind counter as a genuine zero. **Not fixed here**: it needs a field on the published `zeta.heat.receipt.v1`, and adding one unilaterally is the same move that produced the §2 divergence. Checked and recorded: `HeatReceipt` has **no** F#, Q# or treaty counterpart, so no oracle disagrees today — the open question is the schema contract, not the values, and it should be settled together with `081M010WYE5087G0R003J89QVF` §2.

`thermalPpm` remains the non-injective fold of defect 4 (already tracked as §3).

## Coordination

Stays entirely out of the `TemperatureReadout` schema declaration — no field added, no `v1`/`v2` decision made — so it should not collide with `shadow/treaty-cannot-see-key-set-divergence`.

**Auto-merge deliberately not armed.**

🤖 Generated with [Claude Code](https://claude.com/claude-code)


## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 0 |
| Resolved threads | 0 |
| Unresolved threads | 0 |
| Total review comments | 0 |
| Total fix commits (touching thread paths) | 0 |

## Review threads

(no review threads on this PR)

## Fix commits (touching thread paths)

(no commits in this PR touched files anchored by review threads)
