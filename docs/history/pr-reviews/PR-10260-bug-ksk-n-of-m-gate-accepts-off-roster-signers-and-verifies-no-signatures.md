# PR #10260 -- bug(ksk): N-of-M gate accepts off-roster signers and verifies no signatures

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 10260 |
| Title | bug(ksk): N-of-M gate accepts off-roster signers and verifies no signatures |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-08-10T00:26:49Z |
| Merged at | 2026-08-10T00:27:08Z |
| Merge commit SHA | `c6f14e094177a033f97ef68929cfbb808fd67e02` |
| Branch | `shadow/ksk-threshold-gate-gaps` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/10260 |
| Changed files | 2 |
| Additions / deletions | +95 / -0 |

## Description

You asked to put n-of-m crypto with quantum resistance onto the DU-change gate. Surveying what exists first turned up **a gate that looks complete and currently authorizes on trust.**

## Inventory

| piece | status |
|---|---|
| N-of-M **threshold logic** | **real** — scope match, distinct count, duplicate detection |
| **signature verification** | **absent — and declared**: *"deferred to follow-up slice; this is threshold logic only"* |
| **roster membership check** | **absent — and NOT declared** ← the bug |
| `PqLattice` | an enum case marked *not yet implemented* (workitem `081KSNY2Z…`) |
| `Shamir.fs` | **toy field** — `Prime = 257`. Not a basis for a security gate |

## The bug

`checkKskAuthorization` uses `config.Signers` **only for its `.Length`**. Nothing checks that a signer in the request is *on the roster* — so a request with `Threshold` distinct but **entirely unknown** identities reaches `Authorized`, as long as the count doesn't exceed roster size.

With verification also deferred, the gate currently answers *"were enough distinct names supplied?"* — not *"did enough authorized parties consent?"*

The deferred-verification note is honest and declared. **The membership gap wasn't** — and it's the more surprising one, since a reader sees `config.Signers` referenced and reasonably assumes the roster is enforced.

## Why it matters more than it looks

This is the **emergency bypass of the consent gate for the KSK** — the kinetic safeguard, the highest-authority path in the system. It's also the mechanism you propose reusing for **k-of-n on DU/workflow changes**, where its job would be stopping a single party from widening the action space. **Reused as-is, it wouldn't stop that.**

One caution recorded: the in-tree **E8/adinkra lattice work is coding-theory** (Construction A over extended Hamming), *not* a cryptographic hardness assumption. Different uses of the word — worth not conflating when picking the PQ scheme. I'd suggest standardized **ML-DSA (FIPS 204)** or **SLH-DSA (FIPS 205)** over a bespoke construction.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 0 |
| Resolved threads | 0 |
| Unresolved threads | 0 |
| Total review comments | 0 |
| Total fix commits (touching thread paths) | 0 |

## Review threads

(no review threads on this PR)

## Fix commits (touching thread paths)

(no commits in this PR touched files anchored by review threads)
