# PR #14522 -- ci(twitch-ai): the app gets a CI job — its own tsc, the repo eslint, the real vite build (D0 of #14503)

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 14522 |
| Title | ci(twitch-ai): the app gets a CI job — its own tsc, the repo eslint, the real vite build (D0 of #14503) |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-08-23T22:41:12Z |
| Merged at | 2026-08-23T23:24:03Z |
| Merge commit SHA | `b1967172d8f9ed95b10751756f1e95102e2c5046` |
| Branch | `claude/otto-attention-density` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/14522 |
| Changed files | 14 |
| Additions / deletions | +962 / -241 |

## Description

D0 of the attention-density spec (docs/design/2026-08-23-twitch-ai-
attention-density-implementation-spec-*.md): "adding features to a
surface with no CI is exactly how the vacuity class gets in." The
twitch-ai Vite workspace had no CI job — its own tsconfig (DOM libs,
vite/client types) and its real `vite build` ran nowhere
(workitems/081M0QF7ZVY087G0R003Q4Q18D, moved to done/ here; two
main-is-red workitems already came out of the gap).

The job (`lint (twitch-ai app)`, drift check): frozen-lockfile installs
(root for the eslint stack, app for its own deps), `tsc --noEmit` under
the app's OWN settings, `bunx eslint src/apps/twitch-ai/src` with the
rc captured directly (not through a pipe — that class bit this session:
`eslint | tail` reported rc=0 over 145 live errors), and `bun run
build` — exactly what deploys to Pages. NOT added to the
`gate (required)` floor: that list is a treaty amendment per its own
comment, and stays with the consent path.

**Discrimination proof (D0 acceptance) — COMPLETE:**

- **RED** (deliberate `const d0DiscriminationProof: string = 42;` in
  main.ts, commit c4b55804): the job failed on exactly the planted
  error — `src/main.ts(216,7): error TS2322: Type 'number' is not
  assignable to type 'string'`, exit 2.
  https://github.com/Lucent-Financial-Group/Zeta/actions/runs/32671364129/job/97272744879
  (the run's overall conclusion reads `cancelled` because the restore
  push superseded it; the twitch-ai JOB completed failure before the
  cancellation, log on the link.)
- **GREEN** (break reverted, commit a9c1c850): same steps, success.
  https://github.com/Lucent-Financial-Group/Zeta/actions/runs/32671613542/job/97273336388

The RED run also caught a second real gap this PR then fixed: the
build-graph completeness audit (direction C) refused the UNCLAIMED new
job — "no target claims this job, so the affected-set query can never
name it." build-graph.json gains the declared target `ts:apps/twitch-ai`
(sources `src/apps/twitch-ai/**`) claiming `gate/lint-twitch-ai`;
`derive --write` re-canonicalised, drift gate in sync, audit reports
117 targets with every job claimed.

Making the job's eslint step green required paying down the app's
existing debt — the first narrow-path slice of
workitems/081M0RBXF6J087G0R0023EX9X2 ("eslint configured, pinned and
named in CI but never invoked"; its own prescription: "turn the tool on
for a narrow path first and widen"). 145 errors → 0:

- NEW src/protocol.ts — the typed worker ⟷ main message contract.
  84 of the 145 were unsafe-`any` from destructuring untyped `e.data`;
  the discriminated union removes the class, and it is the surface D1's
  attention field extends next.
- observe.ts: `HistoryEvent`/`NextAction` `actions?: any[]` becomes
  `readonly AgentAction[]` ({ tool; args?: Record<string, unknown> }) —
  typed at the source instead of casting at every consumer. Root tsc
  green across all consumers.
- swarm.worker.ts: typed handleMessage switch; loop() split
  (rebootIfCartSwitched / simulateTick / applySwarmKeyActions);
  onmessage no longer an async assignment (no-misused-promises).
- Chip8TvPlayer.ts: typed FramePayload/ArenaReadout params;
  updatePredictions split (resetButtons / applyPredictionGlow /
  applyCommittedGlow — cognitive complexity 32 → small helpers);
  numeric template expressions made explicit (toFixed/String).
- main.ts: mustGet() instead of non-null assertions; typed post()
  wrapper; upload handler's floating promise handled.
- counter.ts deleted — Vite scaffolding nothing imported.

Verified behavior-preserving: swarm/observe suites 1129 pass; the
15 s headless smoke of the rebuilt worker reproduces the pre-refactor
readouts exactly (priors registry hit "1200 trained ticks", mode
header "FLEE · self#1 · adv#4", OCR 0:0, 6 perception boxes).

Agency-Signature-Version: 1
Agent: otto (interactive Claude session)
Agent-Runtime: Claude Code (remote container)
Agent-Model: Claude
Credential-Identity: Claude via repo-scoped git proxy
Credential-Mode: unknown
Human-Review: none
Human-Review-Evidence: none
Action-Mode: human-directed
Task: 081M0QF7ZVY087G0R003Q4Q18D


Claude-Session: https://claude.ai/code/session_01UNRXBqHjAKNK6QzuLcem49

Co-authored-by: Claude

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_01UNRXBqHjAKNK6QzuLcem49

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | true |
| Total threads | 7 |
| Resolved threads | 7 |
| Unresolved threads | 0 |
| Total review comments | 7 |
| Total fix commits (touching thread paths) | 5 |

## Review threads

### Thread 1 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6bivPN`
- **Path**: `src/apps/twitch-ai/src/main.ts`
- **Replies**: 0

**Initial comment:**

- **`github-advanced-security[bot]` (bot)** at 2026-08-23T22:59:05Z on `src/apps/twitch-ai/src/main.ts`:86 (association: NONE)

  <pre>
  ## CodeQL / Clear text storage of sensitive information

  This stores sensitive data returned by [a call to getElementById](1) as clear text.
  This stores sensitive data returned by [a call to getItem](2) as clear text.
  This stores sensitive data returned by [an access to apiKeyInput](3) as clear text.

  [Show more details](https://github.com/Lucent-Financial-Group/Zeta/security/code-scanning/747)
  </pre>

### Thread 2 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6bivPO`
- **Path**: `src/apps/twitch-ai/src/swarm.worker.ts`
- **Replies**: 0

**Initial comment:**

- **`github-advanced-security[bot]` (bot)** at 2026-08-23T22:59:05Z on `src/apps/twitch-ai/src/swarm.worker.ts`:130 (association: NONE)

  <pre>
  ## CodeQL / Remote property injection

  A property name to write to depends on a [user-provided value](1).

  [Show more details](https://github.com/Lucent-Financial-Group/Zeta/security/code-scanning/761)
  </pre>

### Thread 3 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6bivPQ`
- **Path**: `src/apps/twitch-ai/src/swarm.worker.ts`
- **Replies**: 0

**Initial comment:**

- **`github-advanced-security[bot]` (bot)** at 2026-08-23T22:59:05Z on `src/apps/twitch-ai/src/swarm.worker.ts`:134 (association: NONE)

  <pre>
  ## CodeQL / Remote property injection

  A property name to write to depends on a [user-provided value](1).

  [Show more details](https://github.com/Lucent-Financial-Group/Zeta/security/code-scanning/762)
  </pre>

### Thread 4 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6bivPS`
- **Path**: `src/apps/twitch-ai/src/swarm.worker.ts`
- **Replies**: 0

**Initial comment:**

- **`github-advanced-security[bot]` (bot)** at 2026-08-23T22:59:05Z on `src/apps/twitch-ai/src/swarm.worker.ts`:140 (association: NONE)

  <pre>
  ## CodeQL / Missing origin verification in `postMessage` handler

  Postmessage handler has no origin check.

  [Show more details](https://github.com/Lucent-Financial-Group/Zeta/security/code-scanning/760)
  </pre>

### Thread 5 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6biyNO`
- **Path**: `src/apps/twitch-ai/src/swarm.worker.ts`
- **Replies**: 0

**Initial comment:**

- **`github-advanced-security[bot]` (bot)** at 2026-08-23T23:08:13Z on `src/apps/twitch-ai/src/swarm.worker.ts`:131 (association: NONE)

  <pre>
  ## CodeQL / Remote property injection

  A property name to write to depends on a [user-provided value](1).

  [Show more details](https://github.com/Lucent-Financial-Group/Zeta/security/code-scanning/765)
  </pre>

### Thread 6 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6biyNU`
- **Path**: `src/apps/twitch-ai/src/swarm.worker.ts`
- **Replies**: 0

**Initial comment:**

- **`github-advanced-security[bot]` (bot)** at 2026-08-23T23:08:13Z on `src/apps/twitch-ai/src/swarm.worker.ts`:136 (association: NONE)

  <pre>
  ## CodeQL / Remote property injection

  A property name to write to depends on a [user-provided value](1).

  [Show more details](https://github.com/Lucent-Financial-Group/Zeta/security/code-scanning/766)
  </pre>

### Thread 7 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6biyNW`
- **Path**: `src/apps/twitch-ai/src/swarm.worker.ts`
- **Replies**: 0

**Initial comment:**

- **`github-advanced-security[bot]` (bot)** at 2026-08-23T23:08:14Z on `src/apps/twitch-ai/src/swarm.worker.ts`:176 (association: NONE)

  <pre>
  ## CodeQL / Missing origin verification in `postMessage` handler

  Postmessage handler has no origin check.

  [Show more details](https://github.com/Lucent-Financial-Group/Zeta/security/code-scanning/764)
  </pre>

## Fix commits (touching thread paths)

### `4293dc2050d9c47ed489ed4f09508f47fe290090` -- 2026-08-23T22:40:42Z -- `claude`

**Touched files (intersect with thread paths):**

- `src/apps/twitch-ai/src/main.ts`
- `src/apps/twitch-ai/src/swarm.worker.ts`

**Message:**

```
ci(twitch-ai): the app gets a CI job — its own tsc, the repo eslint, …

…the real vite build (D0 of #14503)

D0 of the attention-density spec (docs/design/2026-08-23-twitch-ai-
attention-density-implementation-spec-*.md): "adding features to a
surface with no CI is exactly how the vacuity class gets in." The
twitch-ai Vite workspace had no CI job — its own tsconfig (DOM libs,
vite/client types) and its real `vite build` ran nowhere
(workitems/081M0QF7ZVY087G0R003Q4Q18D, moved to done/ here; two
main-is-red workitems already came out of the gap).

The job (`lint (twitch-ai app)`, drift check): frozen-lockfile installs
(root for the eslint stack, app for its own deps), `tsc --noEmit` under
the app's OWN settings, `bunx eslint src/apps/twitch-ai/src` with the
rc captured directly (not through a pipe — that class bit this session:
`eslint | tail` reported rc=0 over 145 live errors), and `bun run
build` — exactly what deploys to Pages. NOT added to the
`gate (required)` floor: that list is a treaty amendment per its own
comment, and stays with the consent path.

Making the job's eslint step green required paying down the app's
existing debt — the first narrow-path slice of
workitems/081M0RBXF6J087G0R0023EX9X2 ("eslint configured, pinned and
named in CI but never invoked"; its own prescription: "turn the tool on
for a narrow path first and widen"). 145 errors → 0:

- NEW src/protocol.ts — the typed worker ⟷ main message contract.
  84 of the 145 were unsafe-`any` from destructuring untyped `e.data`;
  the discriminated union removes the class, and it is the surface D1's
  attention field extends next.
- observe.ts: `HistoryEvent`/`NextAction` `actions?: any[]` becomes
  `readonly AgentAction[]` ({ tool; args?: Record<string, unknown> }) —
  typed at the source instead of casting at every consumer. Root tsc
  green across all consumers.
- swarm.worker.ts: typed handleMessage switch; loop() split
  (rebootIfCartSwitched / simulateTick / applySwarmKeyActions);
  onmessage no longer an async assignment (no-misused-promises).
- Chip8TvPlayer.ts: typed FramePayload/ArenaReadout params;
  updatePredictions split (resetButtons / applyPredictionGlow /
  applyCommittedGlow — cognitive complexity 32 → small helpers);
  numeric template expressions made explicit (toFixed/String).
- main.ts: mustGet() instead of non-null assertions; typed post()
  wrapper; upload handler's floating promise handled.
- counter.ts deleted — Vite scaffolding nothing imported.

Verified behavior-preserving: swarm/observe suites 1129 pass; the
15 s headless smoke of the rebuilt worker reproduces the pre-refactor
readouts exactly (priors registry hit "1200 trained ticks", mode
header "FLEE · self#1 · adv#4", OCR 0:0, 6 perception boxes).

Acceptance (per D0): the break-RED / restore-GREEN discrimination proof
runs on this PR as two follow-up commits; both run links are pasted in
the PR body before auto-merge is armed.

Agency-Signature-Version: 1
Agent: otto (interactive Claude session)
Agent-Runtime: Claude Code (remote container)
Agent-Model: Claude
Credential-Identity: Claude <noreply@anthropic.com> via repo-scoped git proxy
Credential-Mode: unknown
Human-Review: none
Human-Review-Evidence: none
Action-Mode: human-directed
Task: 081M0QF7ZVY087G0R003Q4Q18D

Claude-Session: https://claude.ai/code/session_01UNRXBqHjAKNK6QzuLcem49

Co-authored-by: Claude <noreply@anthropic.com>
```

### `c4b55804807eb2b65b95260572f955caaa4489f4` -- 2026-08-23T22:41:29Z -- `claude`

**Touched files (intersect with thread paths):**

- `src/apps/twitch-ai/src/main.ts`

**Message:**

```
test(ci): D0 discrimination proof — deliberate type break, revert fol…

…lows

The twitch-ai job must be SEEN red before it is believed to be a check.

Agency-Signature-Version: 1
Agent: otto (interactive Claude session)
Agent-Runtime: Claude Code (remote container)
Agent-Model: Claude
Credential-Identity: Claude <noreply@anthropic.com> via repo-scoped git proxy
Credential-Mode: unknown
Human-Review: none
Human-Review-Evidence: none
Action-Mode: human-directed
Task: 081M0QF7ZVY087G0R003Q4Q18D

Claude-Session: https://claude.ai/code/session_01UNRXBqHjAKNK6QzuLcem49

Co-authored-by: Claude <noreply@anthropic.com>
```

### `a9c1c850ad588e938bf0dbf9bdd269eb0a811d41` -- 2026-08-23T22:46:20Z -- `claude`

**Touched files (intersect with thread paths):**

- `src/apps/twitch-ai/src/main.ts`

**Message:**

```
test(ci): D0 discrimination proof restore — break reverted, GREEN hal…

…f; the graph claims the new leg

The deliberate TS2322 is removed (RED run:
https://github.com/Lucent-Financial-Group/Zeta/actions/runs/32671364129 —
the twitch-ai job failed on exactly the planted error,
'src/main.ts(216,7): error TS2322'). This commit is the GREEN half.

Also rides here because the RED run surfaced it: the build-graph
completeness audit (direction C) refused the unclaimed job — a job no
target claims can never be selected, so a wired selector would silently
stop running it. build-graph.json gains the declared target
ts:apps/twitch-ai (sources src/apps/twitch-ai/**) claiming
gate/lint-twitch-ai; derive --write re-canonicalised, drift gate in
sync, audit: 117 targets, every job claimed.

Agency-Signature-Version: 1
Agent: otto (interactive Claude session)
Agent-Runtime: Claude Code (remote container)
Agent-Model: Claude
Credential-Identity: Claude <noreply@anthropic.com> via repo-scoped git proxy
Credential-Mode: unknown
Human-Review: none
Human-Review-Evidence: none
Action-Mode: human-directed
Task: 081M0QF7ZVY087G0R003Q4Q18D

Claude-Session: https://claude.ai/code/session_01UNRXBqHjAKNK6QzuLcem49

Co-authored-by: Claude <noreply@anthropic.com>
```

### `44f7f580c4f031fbe9f43ea763d90ecd4d323245` -- 2026-08-23T23:01:21Z -- `claude`

**Touched files (intersect with thread paths):**

- `src/apps/twitch-ai/src/main.ts`
- `src/apps/twitch-ai/src/swarm.worker.ts`

**Message:**

```
fix(twitch-ai): address the CodeQL alerts the new scan surfaced

The D0 job's first real catch cascade — CodeQL re-scanned the refactored
files and raised 4 alerts (3 high, 1 medium). Each gets the honest
treatment, not a suppression:

- Remote property injection ×2 (swarm.worker KEY_DOWN/KEY_UP): the key
  index from the message now passes validKeyIndex (integer, 0..15)
  before any array write. The keypad has 16 keys; anything else is not
  a key.
- Missing origin verification (worker onmessage): a dedicated worker
  has no origin to verify — only the creating page holds its handle —
  so the available hardening is structural: isMainToWorkerMessage()
  drops unknown or malformed messages before any handler runs.
- Clear-text storage (main.ts settings): the saved API key is NO
  LONGER echoed back into the DOM when the modal opens. The field
  stays empty; the placeholder says whether a key exists; blank keeps
  it, typing replaces it, and an explicit 'Clear saved key' button
  removes it. The secret now lives in localStorage only (already
  disclosed on the page) instead of round-tripping through an input
  value on every settings open.

App tsc, repo eslint, and vite build all green locally.

Agency-Signature-Version: 1
Agent: otto (interactive Claude session)
Agent-Runtime: Claude Code (remote container)
Agent-Model: Claude
Credential-Identity: Claude <noreply@anthropic.com> via repo-scoped git proxy
Credential-Mode: unknown
Human-Review: none
Human-Review-Evidence: none
Action-Mode: human-directed
Task: 081M0QF7ZVY087G0R003Q4Q18D

Claude-Session: https://claude.ai/code/session_01UNRXBqHjAKNK6QzuLcem49

Co-authored-by: Claude <noreply@anthropic.com>
```

### `ba82a4caa49fb784510ef3372837c7b0d50f1915` -- 2026-08-23T23:09:12Z -- `claude`

**Touched files (intersect with thread paths):**

- `src/apps/twitch-ai/src/swarm.worker.ts`

**Message:**

```
fix(twitch-ai): CodeQL round 2 — loop-selected key writes, explicit e…

…mpty-origin assertion

The first round's guards were semantically right and taint-invisible:
validKeyIndex returned the SAME (tainted) value, so the property write
still read as message-derived; and shape validation is not an origin
check to the query. Round 2 uses the canonical forms:

- setManualKey: the written index is the LOOP variable over the sixteen
  fixed slots — the message value only selects which slot matches, so
  no message-derived value ever becomes a property name.
- onmessage now asserts e.origin === "" up front. On a dedicated
  worker the origin is ALWAYS the empty string (only the creating page
  holds the handle), so the check rejects a state that cannot occur on
  this channel — an invariant assertion, stated as such in the comment,
  not decoration.

Agency-Signature-Version: 1
Agent: otto (interactive Claude session)
Agent-Runtime: Claude Code (remote container)
Agent-Model: Claude
Credential-Identity: Claude <noreply@anthropic.com> via repo-scoped git proxy
Credential-Mode: unknown
Human-Review: none
Human-Review-Evidence: none
Action-Mode: human-directed
Task: 081M0QF7ZVY087G0R003Q4Q18D

Claude-Session: https://claude.ai/code/session_01UNRXBqHjAKNK6QzuLcem49

Co-authored-by: Claude <noreply@anthropic.com>
```
