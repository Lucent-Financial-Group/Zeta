# PR #1409 -- feat(B-0182): filter formal-verification tests to Linux on CI (Option A dual-axis)

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 1409 |
| Title | feat(B-0182): filter formal-verification tests to Linux on CI (Option A dual-axis) |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-05-03T13:37:50Z |
| Merged at | 2026-05-03T13:49:54Z |
| Merge commit SHA | `6b50da4af979daf45efaaffe6e04967f8a8a05ed` |
| Branch | `feat/b0182-linux-only-formal-verification-option-a` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/1409 |
| Changed files | 2 |
| Additions / deletions | +73 / -12 |

## Description

## Summary

Implements B-0182 Option A. Formal verification (TLC + Alloy) is pure-math computation; running on macOS / ARM / slim runners is duplicate work.

## Dual-axis filter

Both `Tlc.Runner.Tests.fs` + `Alloy.Runner.Tests.fs` `toolchainReady()` helpers now check:

1. **OS axis**: skip non-Linux on CI
2. **Runner-class axis**: skip `ubuntu-slim` via `GITHUB_WORKFLOW=low-memory` env-var

Combined: run when `Linux AND GITHUB_WORKFLOW != "low-memory"`; otherwise silent-no-op.

## Dev-local preserved

`CI=true` env var gates the filter. Dev laptops bypass; tests run normally.

## Tactical band-aid; strategic path noted

Per Aaron 2026-05-03: these test runners shell out to `java -cp ... tlc2.TLC` / `AlloyRunner` — no F# operator-algebra logic involved. A TS wrapper under `tools/` would be a more natural architectural fit. Filing that as B-0183 follow-up.

## Test plan

- [x] Build clean (0/0)
- [x] Closes B-0182 P2 backlog row

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 2 |
| Resolved threads | 2 |
| Unresolved threads | 0 |
| Total review comments | 2 |
| Total fix commits (touching thread paths) | 2 |

## Review threads

### Thread 1 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5_MKOE`
- **Path**: `tests/Tests.FSharp/Formal/Tlc.Runner.Tests.fs`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-03T13:40:33Z on `tests/Tests.FSharp/Formal/Tlc.Runner.Tests.fs`:92 (association: NONE)

  <pre>
  The doc comment says CI filtering avoids duplicate runs on “macOS / ARM / slim runners”, but the implemented gate only checks OS==Linux and GITHUB_WORKFLOW!=low-memory. On gate.yml this will still run on the ubuntu-24.04-arm leg (Linux/ARM). Either update the comment/PR intent to not mention ARM, or add an explicit architecture check if ARM is meant to be skipped as well.

  </pre>

### Thread 2 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5_MKOO`
- **Path**: `tests/Tests.FSharp/Formal/Alloy.Runner.Tests.fs`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-03T13:40:33Z on `tests/Tests.FSharp/Formal/Alloy.Runner.Tests.fs`:148 (association: NONE)

  <pre>
  This comment claims we’re filtering out duplicate runs on “macOS / ARM / slim runners”, but toolchainReady only filters non-Linux and the low-memory workflow. On Linux/ARM (ubuntu-24.04-arm in gate.yml) the Alloy suite will still run. Please either adjust the comment/PR description to match behavior, or include an architecture guard if ARM should be skipped too.

  </pre>

## Fix commits (touching thread paths)

### `a0e11242c7fe9627a71065c09e620c7d3cc5425f` -- 2026-05-03T13:37:36Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tests/Tests.FSharp/Formal/Alloy.Runner.Tests.fs`
- `tests/Tests.FSharp/Formal/Tlc.Runner.Tests.fs`

**Message:**

```
feat(B-0182): filter formal-verification tests to standard Linux on C…

…I (Option A dual-axis)

Per B-0182 + Aaron 2026-05-03: formal verification (TLC + Alloy) is
pure-math computation with no OS-specific behavior. Running on
macOS / ARM / slim runners alongside standard Linux is duplicate
work.

Implementation (Option A from B-0182 backlog row, dual-axis):

Both Tlc.Runner.Tests.fs + Alloy.Runner.Tests.fs toolchainReady()
helpers now check two axes BEFORE the existing toolchain-readiness
check:

1. OS axis: skip non-Linux on CI (catches macos-26, windows-2025,
   windows-11-arm)
2. Runner-class axis: skip the slim runner via GITHUB_WORKFLOW=
   low-memory env-var. ubuntu-slim is still Linux, so the OS check
   alone would not catch it.

Combined logic: run formal-verification tests when Linux AND
GITHUB_WORKFLOW != low-memory; otherwise silent-no-op (matches
existing skip-on-missing-toolchain pattern).

Dev-local preserved: CI=true env var gates the filter. Without it
(dev laptops), tests run normally on any OS where the toolchain
is present.

Closes B-0182 P2 backlog row.

Tactical band-aid; strategic path tracked separately: these test
runners are really shelling out to java -cp tla2tools.jar tlc2.TLC
+ java -cp alloy.jar AlloyRunner — no F# operator-algebra logic
involved. A TS wrapper under tools/ would be a more natural fit;
that migration is worth its own backlog row (filing as a follow-up
this same session per Aaron's "the fs should be a ts" observation).
```

### `9c8288969bad11fd32baec3c469dbaf9364e551d` -- 2026-05-03T13:46:16Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tests/Tests.FSharp/Formal/Alloy.Runner.Tests.fs`
- `tests/Tests.FSharp/Formal/Tlc.Runner.Tests.fs`

**Message:**

```
fix(B-0182): expand filter to 3-axis (OS + arch + workflow) — catches…

… ubuntu-24.04-arm

Reviewer #1409 caught: my 2-axis filter (OS + GITHUB_WORKFLOW) lets
ubuntu-24.04-arm through because it IS Linux, just on ARM CPU. The
PR description claimed "macOS / ARM / slim" coverage but the
implementation only caught macOS + slim.

Added third axis: RuntimeInformation.OSArchitecture == Architecture.X64.
Now filters out ARM-Linux (ubuntu-24.04-arm), windows-11-arm
(ARM Windows), and any future ARM matrix legs.

Combined logic on CI: run formal-verification when
Linux AND X64 AND GITHUB_WORKFLOW != low-memory; otherwise silent
no-op.

Comments updated in both Tlc.Runner.Tests.fs + Alloy.Runner.Tests.fs
to reflect the 3-axis check accurately.

Dev-local (no CI=true) bypasses all three filters; devs validate on
any OS+arch where the toolchain is present.
```
