# PR #9129 -- feat(core): DbspCellGraph — DBSP dataflow as a cell society (first CellScheduler consumer)

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 9129 |
| Title | feat(core): DbspCellGraph — DBSP dataflow as a cell society (first CellScheduler consumer) |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-07-02T12:39:55Z |
| Merged at | 2026-07-02T12:40:03Z |
| Merge commit SHA | `b054895c65f2eb6085534a9ded4b48d96d6c78bb` |
| Branch | `otto/dbsp-cell-graph-first-consumer` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/9129 |
| Changed files | 4 |
| Additions / deletions | +168 / -0 |

## Description

The **first real consumer** of the cell scheduler (`081KTG5S0M9`) — a DBSP incremental dataflow modelled as a cell society: each operator is a cell, **Z-set deltas are the messages**.

Makes the design note's thesis concrete — *"messages are Z-set deltas; a cell's inbox is a ZSetW"* — connecting the scheduler back to the base atom (`ZSet`).

## What
- `DbspOp = Filter | Rekey | Relay | Integrate` (`RequireQualifiedAccess` so the cases don't collide with the circuit operator types in `Operators.fs`).
- A cell holds its operator, downstream targets, and integrated output (`Acc` = DBSP `I`). The `step` is pure: apply operator to the incoming delta → integrate → emit the output delta downstream. That purity is exactly the noninterference the scheduler's DoP-invariance depends on.

## The law it demonstrates end-to-end
Streaming a sequence of input deltas (insertions **and** a retraction) through the cell graph yields the same integrated sink state as a **batch recompute** — DBSP's *incremental ≡ recompute*, now **across the scheduler** — and it's **DoP-invariant**.

## Tests (4, green)
- incremental ≡ recompute (`filter-even → ×10 → sink`);
- **retraction cancels at every stage** (key 2 and its image 20 vanish through the graph);
- `run(1) == run(4)` on the DBSP workload;
- fan-out then merge (partition by parity reunites at the sink).

Full suite **3848 green**, 0 warnings.

## Why it matters
Per the review-debt ledger (§6a), this is the **anchor consumer**: it gives the deferred perf pass an actual load to measure instead of a guessed hot path, and it ungates the oracle/Roslyn mirror follow-ups.

Anchor: Budiu et al. *DBSP* (VLDB 2023) — linear operators commute with the delta.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 2 |
| Resolved threads | 0 |
| Unresolved threads | 2 |
| Total review comments | 2 |
| Total fix commits (touching thread paths) | 1 |

## Review threads

### Thread 1 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM6N5Svt`
- **Path**: `src/Core/DbspCellGraph.fs`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-07-02T12:43:12Z on `src/Core/DbspCellGraph.fs`:71 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Drop empty Z-set deltas before routing**

  When an operator produces `ZSet.Empty` (for example, a `Filter 0` cell receives an odd-only delta), this still enqueues an empty message to every downstream cell. In a feedback topology such as a filter feeding itself, that zero delta is re-emitted forever and `runToQuiescence` hits `maxSteps`, even though DBSP's zero delta should be a no-op and quiesce. Since this graph fixes messages to `ZSet&lt;int64&gt;`, suppress empty `outDelta`s before delivery.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 2 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM6N5Svw`
- **Path**: `src/Core/DbspCellGraph.fs`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-07-02T12:43:12Z on `src/Core/DbspCellGraph.fs`:59 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Check rekey multiplication overflow**

  When a key/factor product exceeds the `int64` range, this multiplication wraps before `ZSet.ofSeq` canonicalizes the output, so a row can be rewritten to an unrelated key or collide with another row and corrupt the integrated sink state. Other Z-set arithmetic in core uses `Checked.(*)` for user-controlled products, so `DbspOp.Rekey` should not silently wrap large keys/factors.

  Useful? React with 👍 / 👎.
  </pre>

## Fix commits (touching thread paths)

### `525185a3a5ac4c48cf188403bf531f286ef7abce` -- 2026-07-02T12:39:52Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `src/Core/DbspCellGraph.fs`

**Message:**

```
feat(core): DbspCellGraph — DBSP dataflow as a cell society (first Ce…

…llScheduler consumer) (shadow*)

The first real CONSUMER of the cell scheduler (081KTG5S0M9): a DBSP incremental
dataflow modelled as a cell society — each operator is a cell, Z-set DELTAS are the
messages. Makes the design note's thesis concrete ("messages are Z-set deltas; a
cell's inbox is a ZSetW"), connecting the scheduler back to the base atom (ZSet).

DbspOp = Filter | Rekey | Relay | Integrate (RequireQualifiedAccess so the case
names don't collide with the circuit operator types in Operators.fs). A cell holds
its operator, downstream targets, and integrated output (Acc = DBSP `I`). The step
is pure — applies the operator to the incoming delta, integrates, emits the output
delta downstream — exactly the noninterference the scheduler's DoP-invariance needs.

Demonstrates the load-bearing law end-to-end: streaming a sequence of input deltas
(insertions AND a retraction) through the cell graph yields the same integrated sink
state as a batch recompute — DBSP incremental ≡ recompute, now ACROSS the scheduler
— and it is DoP-invariant.

4 tests: incremental ≡ recompute (filter-even -> *10 -> sink); retraction cancels at
every stage (key 2 and its image 20 vanish); run(1)==run(4) on the DBSP workload;
fan-out then merge (partition by parity reunites at the sink). Full suite 3848 green,
0 warnings.

Why it matters (per the review-debt ledger §6a): this is the anchor consumer that
gives the deferred perf pass an actual load to measure instead of a guessed hot path,
and it ungates the oracle/Roslyn mirror follow-ups.

Anchors: Budiu et al. DBSP (VLDB 2023) — linear operators commute with the delta;
incremental view maintenance (the `I`/`D` operators).

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>

AgencySignature-v1:
  persona: otto
  actor: zeta-otto
  surface: src/core
  topology: shadow-clone
  intent: dbsp-cell-graph-first-cellscheduler-consumer-deltas-as-messages-incremental-eq-recompute
  authorization: aaron-standing-24h-autonomous-2026-07-02
  uncertainty: low
  measure: dbsp-operators-as-cells-zset-deltas-as-messages-incremental-eq-recompute-across-scheduler-dop-invariant-retraction-cancels-4-tests
  delta-u: cell-scheduler-gains-first-consumer-ungates-perf-measurement-and-oracle-mirrors-proves-atom-scheduler-integration
  seed: S4
```
