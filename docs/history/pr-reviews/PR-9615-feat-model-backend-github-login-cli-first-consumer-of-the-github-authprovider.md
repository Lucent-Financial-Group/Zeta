# PR #9615 -- feat(model-backend): github-login-cli — first consumer of the GitHub AuthProvider

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 9615 |
| Title | feat(model-backend): github-login-cli — first consumer of the GitHub AuthProvider |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-07-09T14:12:16Z |
| Merged at | 2026-07-09T14:34:16Z |
| Merge commit SHA | `a27e1cb69af89b8d631f4d9e09c8b4182352660a` |
| Branch | `otto/github-login-cli` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/9615 |
| Changed files | 2 |
| Additions / deletions | +272 / -0 |

## Description

Closes the loop from #9554: wires `githubDeviceProvider` + `fetchTransport` + `fileTokenStore` through `deviceLogin`. Subcommands: `login` / `token` / `askpass` (GIT_ASKPASS mode — git push with no token in argv or env). Pure core, fake-transport tests, verified under node with tsc --strict --noUncheckedIndexedAccess. Cells stop hand-running device flows.

Agency-Signature-Version: 2
Agent: otto
Persona: otto
Cell: cowork/main
Agent-Runtime: claude-cowork
Agent-Model: fable-5
Credential-Identity: aaron
Credential-Mode: shared
Human-Review: pending
Human-Review-Evidence: this PR
Action-Mode: human-directed
Task: none

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | true |
| Total threads | 4 |
| Resolved threads | 4 |
| Unresolved threads | 0 |
| Total review comments | 6 |
| Total fix commits (touching thread paths) | 2 |

## Review threads

### Thread 1 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6PmPY0`
- **Path**: `src/Core.TypeScript/model-backend/github-login-cli.ts`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-07-09T14:16:07Z on `src/Core.TypeScript/model-backend/github-login-cli.ts`:123 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Provide a real Git askpass entrypoint**

  In GIT_ASKPASS mode, Git will not run `bun ... askpass` through a shell; `gitcredentials` documents that the `GIT_ASKPASS` value is the program Git invokes, with the prompt supplied as the command-line argument (https://git-scm.com/docs/gitcredentials#_requesting_credentials). Because this branch is reachable only when `argv[0] === "askpass"`, the real Git invocation either fails to exec the documented command string, or, if pointed at this script directly, treats `Username for ...` as `cmd` and falls through to usage, so HTTPS pushes still cannot use the stored token. Provide an executable wrapper or detect prompt-only invocation here.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-07-09T14:23:28Z on `src/Core.TypeScript/model-backend/github-login-cli.ts`:123 (association: MEMBER)

    <pre>
    Accepted and fixed in the follow-up commit — see commit message for the specifics (P1: bare credential-prompt argv routes to askpass so GIT_ASKPASS points directly at the script; P2: save/transport rejections become the exit-1 verdict, plus a .catch at the edge). Tests added for both.
    </pre>

### Thread 2 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6PmPY7`
- **Path**: `src/Core.TypeScript/model-backend/github-login-cli.ts`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-07-09T14:16:07Z on `src/Core.TypeScript/model-backend/github-login-cli.ts`:59 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Convert storage write failures into CLI verdicts**

  When the file-backed store cannot write the token after approval (for example a read-only `~/.config/zeta/auth` or a full disk), `deviceLogin` propagates the `store.save` rejection through this `await`, and the direct CLI only attaches `.then`, so the documented exit-code-1 storage failure path is bypassed in favor of an unhandled rejection/stack trace. Catch failures around the login runner and return the same clean `github login failed: ...` verdict as the other flow errors.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-07-09T14:23:30Z on `src/Core.TypeScript/model-backend/github-login-cli.ts`:59 (association: MEMBER)

    <pre>
    Accepted and fixed in the follow-up commit — see commit message for the specifics (P1: bare credential-prompt argv routes to askpass so GIT_ASKPASS points directly at the script; P2: save/transport rejections become the exit-1 verdict, plus a .catch at the edge). Tests added for both.
    </pre>

### Thread 3 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6PmdwK`
- **Path**: `src/Core.TypeScript/model-backend/github-login-cli.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-07-09T14:27:12Z on `src/Core.TypeScript/model-backend/github-login-cli.ts`:28 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Make the askpass entrypoint executable**

  Fresh evidence after the follow-up: `git ls-files -s` still records this script as mode `100644`, so the documented `GIT_ASKPASS=src/Core.TypeScript/model-backend/github-login-cli.ts git push …` path cannot be exec'd by Git. Git's `gitcredentials` docs say `GIT_ASKPASS` invokes the program named by the variable with the prompt as its command-line argument; with the current mode, that exact setup fails before `main()` runs (`Permission denied`), so HTTPS pushes still cannot use the stored token unless a separate executable wrapper or executable bit is provided.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 4 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6PmdwU`
- **Path**: `src/Core.TypeScript/model-backend/github-login-cli.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-07-09T14:27:12Z on `src/Core.TypeScript/model-backend/github-login-cli.ts`:45 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Tighten permissions when overwriting stored tokens**

  When `github.json` already exists with permissive permissions, `writeFile(..., { mode: 0o600 })` does not tighten the existing file; the mode is only applied on creation. In that scenario a re-login overwrites the file with a fresh GitHub access token while preserving something like `0644`, leaving the credential readable to other local users despite the CLI intending to store secrets under a private auth store.

  Useful? React with 👍 / 👎.
  </pre>

## Fix commits (touching thread paths)

### `064df18addf79e10272c9825e37862ae5ec77fdd` -- 2026-07-09T14:12:14Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `src/Core.TypeScript/model-backend/github-login-cli.ts`

**Message:**

```
feat(model-backend): github-login-cli — first consumer of the GitHub …

…AuthProvider

Closes the loop from #9554: the provider existed but nothing called
it (cells ran the device flow by hand on 2026-07-08). Wires
githubDeviceProvider + fetchTransport (the one membrane) +
fileTokenStore (~/.config/zeta/auth/github.json, 0600) through
login-runner's deviceLogin.

Subcommands: login (device flow, prints URL+code, polls, persists) ·
token (print stored token for scripts) · askpass (GIT_ASKPASS mode:
Username -> x-access-token, else the token — git push with no token
in argv/env).

Everything under main() is pure/injectable (noninterference 13):
fake-transport tests cover flow completion + persistence, askpass
prompt routing, not-logged-in exit 1, usage exit 2. Verified under
node (tsc --strict --noUncheckedIndexedAccess clean, 12 assertions);
bun test file mirrors the house style.

Co-Authored-By: Claude <noreply@anthropic.com>

Agency-Signature-Version: 2
Agent: otto
Persona: otto
Cell: cowork/main
Agent-Runtime: claude-cowork
Agent-Model: fable-5
Credential-Identity: aaron
Credential-Mode: shared
Human-Review: none
Human-Review-Evidence: none
Action-Mode: human-directed
Task: none
```

### `df7ed0a5f10f6e0aa3b621d0ad7eb7f1e1a854f2` -- 2026-07-09T14:23:27Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `src/Core.TypeScript/model-backend/github-login-cli.ts`

**Message:**

```
fix(model-backend): accept both #9615 reviews — real GIT_ASKPASS entr…

…ypoint + storage-failure verdicts

P1: git execs GIT_ASKPASS directly with the prompt as sole argv (no
shell) — main() now recognizes a bare credential-prompt argv as an
implicit askpass call, so GIT_ASKPASS points at the script itself.
P2: store.save/transport rejections become the clean exit-1 verdict
(try/catch in runLogin + .catch at the edge) instead of an unhandled
rejection. Tests cover both (prompt-shaped argv routing; EROFS save
rejection -> exit 1).

Co-Authored-By: Claude <noreply@anthropic.com>

Agency-Signature-Version: 2
Agent: otto
Persona: otto
Cell: cowork/main
Agent-Runtime: claude-cowork
Agent-Model: fable-5
Credential-Identity: aaron
Credential-Mode: shared
Human-Review: none
Human-Review-Evidence: this PR review (Vera P1 + P2)
Action-Mode: human-directed
Task: none
```
