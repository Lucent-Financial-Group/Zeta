# PR #14484 -- docs(research): the arena, the BNN, and the exact GPU mapping — belief update is alpha blending, and the RGBA coincidence that isn't

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 14484 |
| Title | docs(research): the arena, the BNN, and the exact GPU mapping — belief update is alpha blending, and the RGBA coincidence that isn't |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-08-23T21:38:37Z |
| Merged at | 2026-08-23T21:41:20Z |
| Merge commit SHA | `511e1a40e1fab9df79cca986f9d761178c2b91b3` |
| Branch | `claude/otto-arena-gpu-mapping-doc` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/14484 |
| Changed files | 1 |
| Additions / deletions | +235 / -0 |

## Description

The combined writeup Aaron asked me (Otto) to carry rather than route
through the shadow, with my own verification numbers and the
"think outside the box" GPU mapping made exact.

Three braided threads:

1. The arena as shipped after #14471 — the seven-layer perception
   ladder, the learned hunt/flee latch, and the catalogue of sensing
   defects the learner exposed by faithfully learning garbage (register
   space vs screen space, collision-undo wedges, costume-change debris,
   same-color merge poisoning, travelled-vs-wiggled agency, pursuer
   mimicry stealing self-identity, scoreboards as readout-not-agents,
   OCR debounce).

2. My verification numbers, register-labeled [measured]/[read]/
   [proposed] throughout: the live Pages worker bundle compares
   BYTE-IDENTICAL (cmp) to a local build of merged main — the one
   divergence anywhere is the index chunk's 15-byte --base path prefix;
   a 15 s headless run of the merged build (priors registry hit on the
   regenerated fingerprint, roles right, OCR reading both scoreboards);
   the NG4 bridge's convention-invariance (marginal scale independent
   of λ₀ by construction, ν moved only by α₀); the falsifier
   trajectory (INVERTED: one tag at t=333 flips the small buckets,
   ~2250 tag-free ticks after; NORMAL: catch at t=1633 strengthens the
   prior 0.2 → 0.44).

3. The exact GPU mapping, opcode for blend mode: planes = color
   channels; XOR draw = raster logic op; DRW collision = occlusion
   query; persistence-of-vision composite = MAX blend; CCL =
   jump-flooding (Rong &amp; Tan 2006); bbox/centroid/area = mip
   reductions; OCR = 20-bit window bit-pack into one 2^20 LUT
   texelFetch; one NG4 belief = one rgba32float texel; conjugate
   update = additive blend (Diaconis–Ylvisaker: posterior updating IS
   vector addition in natural coordinates — the load-bearing theorem);
   eligibility trace = constant-alpha feedback blend (the motion-blur
   accumulator); Amari's dually-flat coordinates as the reason no
   shader ALU is needed. Two non-exactnesses named so the table stays
   trustworthy (WebGPU lacks logic ops; the shipped trace is the list
   form, the blend maps the accumulating form).

§6 runs the numerology-vs-number-theory check on dim T(NG) = 4 = RGBA:
sufficiency, closure, and operation-match are the invariants that make
it a correspondence; a 5-dim family honestly fails to fit, which is
what an identification (unlike a count coincidence) must do — exclude
something.

Agency-Signature-Version: 1
Agent: otto (interactive Claude session)
Agent-Runtime: Claude Code (remote container)
Agent-Model: Claude
Credential-Identity: Claude via repo-scoped git proxy
Credential-Mode: unknown
Human-Review: none
Human-Review-Evidence: none
Action-Mode: human-directed
Task: none


Claude-Session: https://claude.ai/code/session_01UNRXBqHjAKNK6QzuLcem49

Co-authored-by: Claude

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_01UNRXBqHjAKNK6QzuLcem49

---
_Generated by [Claude Code](https://claude.ai/code/session_01UNRXBqHjAKNK6QzuLcem49)_

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 0 |
| Resolved threads | 0 |
| Unresolved threads | 0 |
| Total review comments | 0 |
| Total fix commits (touching thread paths) | 0 |

## Review threads

(no review threads on this PR)

## Fix commits (touching thread paths)

(no commits in this PR touched files anchored by review threads)
