# PR #8095 -- feat(zflash): migrate tools to src, add zeta flash router

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 8095 |
| Title | feat(zflash): migrate tools to src, add zeta flash router |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-06-13T23:27:59Z |
| Merged at | 2026-06-14T00:25:56Z |
| Merge commit SHA | `1d8ed47d9bb60b77493445ea61443b3fdb2c9093` |
| Branch | `riven/zflash-unify-zeta-flash-cli` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/8095 |
| Changed files | 7 |
| Additions / deletions | +31 / -48 |

## Description

## Summary

- Migrate `fat12-lib`, `flash-and-inject`, and `fat-inspect` from `full-ai-cluster/tools/` to `src/Core.TypeScript/zflash/` with shared `fat12-lib`, `raw-fat-esp` pure helpers, and `AlignedBlockDevice` port adapter
- Add `zeta-flash.ts` router and F# `zeta flash …` dispatch in `Program.fs` (`usb` / `inject` / `inspect` / mac passthrough)
- Refactor `argocd-health-test` cluster bootstrap to call `bootstrapKindClusterInProcess` / `bootstrapK3dClusterInProcess` instead of spawning `kind-up.ts` / `k3d-up.ts`
- Close **B-0844** (`--agent` already in `cli.ts`); add **B-1030** for MCP + skill follow-on
- CI path filters: `full-ai-cluster/tools/**` → `src/Core.TypeScript/zflash/**`

## Test plan

- [x] `bun test src/Core.TypeScript/zflash/fat12-lib.test.ts` (12 pass)
- [x] `dotnet build src/Core.FSharp.Cli/Zeta.Cli.fsproj -c Release`
- [ ] `bun src/Core.TypeScript/zflash/zeta-flash.ts --help`
- [ ] `dotnet run --project src/Core.FSharp.Cli -- flash --help` (from repo root, bun on PATH)
- [ ] CI argocd-health-test kind smoke (included scope)


Made with [Cursor](https://cursor.com)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 2 |
| Resolved threads | 2 |
| Unresolved threads | 0 |
| Total review comments | 2 |
| Total fix commits (touching thread paths) | 0 |

## Review threads

### Thread 1 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6JXLOr`
- **Path**: `src/Core.TypeScript/zflash/flash-and-inject.ts`
- **Replies**: 0

**Initial comment:**

- **`github-code-quality[bot]` (bot)** at 2026-06-13T23:32:33Z on `src/Core.TypeScript/zflash/flash-and-inject.ts`:232 (association: NONE)

  <pre>
  ## Unneeded defensive code

  This guard always evaluates to false.

  ---

  The best fix is to replace the &lt;code&gt;=== null&lt;/code&gt; check with a nullish check (&lt;code&gt;== null&lt;/code&gt;) and then narrow to a number before use. This removes the “always false” defensive branch while keeping safe failure behavior if the helper ever returns either &lt;code&gt;null&lt;/code&gt; or &lt;code&gt;undefined&lt;/code&gt; for “not found.”&lt;/p&gt;
  &lt;p&gt;In &lt;code&gt;src/Core.TypeScript/zflash/flash-and-inject.ts&lt;/code&gt;, update the guard right after:&lt;/p&gt;
  &lt;pre&gt;&lt;code&gt;const partOffset = detectIsohybridEspOffsetBytes(isoHead);
  &lt;/code&gt;&lt;/pre&gt;
  &lt;p&gt;Change:&lt;/p&gt;
  &lt;pre&gt;&lt;code&gt;if (partOffset === null) fail("no-esp");
  &lt;/code&gt;&lt;/pre&gt;
  &lt;p&gt;to:&lt;/p&gt;
  &lt;pre&gt;&lt;code&gt;if (partOffset == null) fail("no-esp");
  &lt;/code&gt;&lt;/pre&gt;
  &lt;p&gt;No imports, new methods, or dependencies are needed.
  </pre>

### Thread 2 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6JXLOu`
- **Path**: `src/Core.TypeScript/zflash/raw-fat-esp.ts`
- **Replies**: 0

**Initial comment:**

- **`github-code-quality[bot]` (bot)** at 2026-06-13T23:32:33Z on `src/Core.TypeScript/zflash/raw-fat-esp.ts`:70 (association: NONE)

  <pre>
  ## Unneeded defensive code

  This guard always evaluates to false.

  ---

  General fix: make the failure guard match the actual “not found” value that &lt;code&gt;detectIsohybridEspOffsetBytes&lt;/code&gt; can produce, instead of checking only &lt;code&gt;null&lt;/code&gt;.&lt;/p&gt;
  &lt;p&gt;Best minimal fix here (without changing functionality intent): replace the strict &lt;code&gt;null&lt;/code&gt; check with a nullish check (&lt;code&gt;== null&lt;/code&gt;), which safely handles both &lt;code&gt;null&lt;/code&gt; and &lt;code&gt;undefined&lt;/code&gt;. This removes the “always false” condition while preserving the intended error path for missing ESP offsets.&lt;/p&gt;
  &lt;p&gt;Change needed in:&lt;/p&gt;
  &lt;ul&gt;
  &lt;li&gt;&lt;code&gt;src/Core.TypeScript/zflash/raw-fat-esp.ts&lt;/code&gt;&lt;/li&gt;
  &lt;li&gt;Function &lt;code&gt;resolveEspGeometry&lt;/code&gt;, around line 70.&lt;/li&gt;
  &lt;/ul&gt;
  &lt;p&gt;No new imports, methods, or dependencies are needed.
  </pre>

## Fix commits (touching thread paths)

(no commits in this PR touched files anchored by review threads)
