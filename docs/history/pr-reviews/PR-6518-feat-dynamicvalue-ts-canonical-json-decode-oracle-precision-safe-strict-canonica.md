# PR #6518 -- feat(DynamicValue): TS canonical-JSON DECODE oracle — precision-safe + strict-canonical

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 6518 |
| Title | feat(DynamicValue): TS canonical-JSON DECODE oracle — precision-safe + strict-canonical |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-06-02T01:27:52Z |
| Merged at | 2026-06-02T01:34:52Z |
| Merge commit SHA | `9cf9e599bea94d53512d62d61ca2aa7ede8211c6` |
| Branch | `otto-cli/dynamicvalue-decode-json-ts-2026-06-01` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/6518 |
| Changed files | 3 |
| Additions / deletions | +397 / -75 |

## Description

Starts the **JSON decode set** (Aaron's "Both, CBOR then JSON"), mirroring the just-landed CBOR decode pattern. First of the four oracles (TS); C#/F#/Rust follow.

## What

- Extracts shared **`src/Core.TypeScript/dynamic-value/json.ts`** — encoder moved **verbatim** from `golden-vectors.test.ts` (encode byte-lock unchanged) + new **`fromCanonicalJson`** decoder + `DecodeError`/`DecodeResult` types.
- Repoints the encode test at the module; adds the decode test.

## Precision-safe + strict-canonical

- **int64 precision**: number tokens are read as **text** and parsed via `BigInt` (never a lossy JS `number`), with an i64 range check → `IntegerOverflow`. This is the whole reason a hand-rolled parser is needed (`JSON.parse` rounds `9223372036854775807`).
- **strict-canonical via fixed-point**: lenient recursive-descent parse, then one check — `canonicalJson(parsed) === input` → else `NonCanonical`. Rejects insignificant whitespace, non-minimal escapes (`\u0041` vs raw `A`), leading zeros, etc.
- **deferred shapes**: a number with `.`/`e`/`E` is a Float → `Unsupported` (Float + Bytes lock under CBOR, not JSON).
- never-throws: public API returns `DecodeResult`; internal `JsonDecodeError` caught at the boundary.

## Tests
Every seed vector decodes `ok` (fixed-point round-trip) + structural match incl. object key order; plus malformed (`UnexpectedEnd`/`TrailingData`), deferred-float (`Unsupported`), oversized (`IntegerOverflow`), and non-canonical rejections. **67 TS tests pass; tsc/eslint/prettier clean.**

`DecodeError`: `UnexpectedEnd | TrailingData | Unsupported | IntegerOverflow | NonCanonical` (no `NonTextKey` — JSON keys are syntactically strings). The compilers don't lie.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 2 |
| Resolved threads | 2 |
| Unresolved threads | 0 |
| Total review comments | 2 |
| Total fix commits (touching thread paths) | 2 |

## Review threads

### Thread 1 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GSfzH`
- **Path**: `src/Core.TypeScript/dynamic-value/json.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-02T01:30:09Z on `src/Core.TypeScript/dynamic-value/json.ts`:162 (association: NONE)

  <pre>
  `readEscape` uses `parseInt(hex, 16)` to validate `\uXXXX`, but `parseInt` will *partially* parse and silently accept non-hex digits (e.g. `"\\u00gg"` becomes code 0). That treats malformed JSON as a different string and typically surfaces as `NonCanonical` instead of `UnexpectedEnd`, contradicting the intended malformed-input behavior.

  Validate all 4 characters are hex before parsing.
  </pre>

### Thread 2 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GSfzW`
- **Path**: `src/Core.TypeScript/dynamic-value/json.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-02T01:30:09Z on `src/Core.TypeScript/dynamic-value/json.ts`:218 (association: NONE)

  <pre>
  `parseNumber` treats any token containing `.`/`e`/`E` as a deferred Float and returns `Unsupported`, but it doesn't enforce the JSON grammar that requires at least one digit after `.` and after the exponent (e.g. `"1."`, `"1e"`, `"1e+"`). Those inputs are malformed JSON and should report `UnexpectedEnd` (or another malformed-input error), not `Unsupported`.
  </pre>

## Fix commits (touching thread paths)

### `208b0299ae6daecab605540560bfde839237b97c` -- 2026-06-02T01:27:42Z -- ``

**Touched files (intersect with thread paths):**

- `src/Core.TypeScript/dynamic-value/json.ts`

**Message:**

```
feat(DynamicValue): TS canonical-JSON DECODE oracle — precision-safe …

…+ strict-canonical

Starts the JSON decode set (Aaron's 'Both, CBOR then JSON'), mirroring the CBOR decode
pattern. Extracts a shared src/Core.TypeScript/dynamic-value/json.ts module (encoder moved
verbatim from golden-vectors.test.ts so the encode byte-lock is unchanged; new
fromCanonicalJson decoder) and points the encode test at it.

fromCanonicalJson is the inverse of canonicalJson for the 6 locked shapes (Float + Bytes
DEFERRED in JSON — they lock under CBOR). A lenient recursive-descent parse, then one
fixed-point check (canonicalJson(parsed) === input) rejects every non-canonical form
(insignificant whitespace, non-minimal escapes, leading zeros / '+' signs) as NonCanonical.

int64 precision is preserved by reading the number token as TEXT and parsing via BigInt
(never a lossy JS number) + an i64 range check → IntegerOverflow. A number with a decimal
point or exponent is a Float → Unsupported (deferred in v1). Public API returns a
DecodeResult, never throws (internal JsonDecodeError caught at the boundary).

golden-vectors-json-decode.test.ts: every seed vector decodes ok (fixed-point round-trip)
+ structural match (incl. object key order), plus malformed / deferred-float / oversized /
non-canonical rejection cases. 67 TS tests pass; tsc/eslint/prettier clean.

DecodeError: UnexpectedEnd | TrailingData | Unsupported | IntegerOverflow | NonCanonical
(no NonTextKey — JSON keys are syntactically strings). The compilers don't lie.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `582bcfd0d7922aafcc4df7354d6b17215cb85a84` -- 2026-06-02T01:31:49Z -- ``

**Touched files (intersect with thread paths):**

- `src/Core.TypeScript/dynamic-value/json.ts`

**Message:**

```
fix(DynamicValue): TS JSON decoder rejects malformed \uXXXX + incompl…

…ete float grammar (Copilot #6518)

Two malformed-JSON cases the hand parser was mis-classifying:
- readEscape: parseInt(hex,16) PARTIALLY parses (e.g. '00gg' -> 0), silently accepting a
  malformed \uXXXX as a different char (surfacing as NonCanonical). Now requires exactly
  4 hex digits via /^[0-9a-fA-F]{4}$/ -> UnexpectedEnd otherwise.
- parseNumber: '1.', '1e', '1e+' (and '-', '-.5') lack the JSON-grammar-required digit
  after '.'/exponent/sign. Extracted consumeDigits() (fails UnexpectedEnd if no digit) and
  apply it to the integer part, fraction, and exponent -> malformed floats now report
  UnexpectedEnd, not Unsupported.

Added rejection tests for \u00gg / \q / 1. / 1e / 1e+ / -. 67 tests pass; tsc/eslint/prettier clean.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```
