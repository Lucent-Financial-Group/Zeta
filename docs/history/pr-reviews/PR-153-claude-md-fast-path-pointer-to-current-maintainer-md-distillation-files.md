# PR #153 -- CLAUDE.md: fast-path pointer to CURRENT-<maintainer>.md distillation files

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 153 |
| Title | CLAUDE.md: fast-path pointer to CURRENT-<maintainer>.md distillation files |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-04-23T14:55:04Z |
| Merged at | 2026-04-24T22:32:17Z |
| Merge commit SHA | `46f1ef4b6b8baa0fc3feebfc111c836d1d868006` |
| Branch | `docs/claude-md-current-maintainer-pointer` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/153 |
| Changed files | 2 |
| Additions / deletions | +35 / -0 |

## Description

## Summary
14-line addition to CLAUDE.md's memory section. Points Claude at the CURRENT-<maintainer>.md distillation layer (landed via ADR PR #152) as fast-path on wake, before the raw `feedback_*.md` / `project_*.md` log. Also codifies the same-tick-update discipline.

## Why now
The ADR in PR #152 noted CLAUDE.md update as optional follow-up. Doing it in the same session the pattern landed keeps the discipline coherent — no "ADR exists but CLAUDE.md doesn't reference it" divergence. Small, focused, factory-generic.

## Test plan
- [ ] Review the added paragraph fits CLAUDE.md's "harness — what this buys us" section tone
- [ ] Confirm the pointer to `docs/DECISIONS/2026-04-23-per-maintainer-current-memory-pattern.md` resolves (lands with PR #152)

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | true |
| Total threads | 8 |
| Resolved threads | 7 |
| Unresolved threads | 1 |
| Total review comments | 15 |
| Total fix commits (touching thread paths) | 3 |

## Review threads

### Thread 1 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59ITNx`
- **Path**: `CLAUDE.md`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-04-23T14:56:41Z on `CLAUDE.md`:94 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Replace dead references in fast-path memory guidance**

  The new fast-path paragraph points readers to `docs/DECISIONS/2026-04-23-per-maintainer-current-memory-pattern.md` and `memory/feedback_current_memory_per_maintainer_distillation_pattern_prefer_progress_2026_04_23.md`, but neither file exists in this commit tree (both paths fail lookup), so the guidance cannot be verified or followed by agents onboarding from `CLAUDE.md`. Please either land these documents in the same change or update the links to existing canonical docs.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-24T21:41:37Z on `CLAUDE.md`:94 (association: MEMBER)

    <pre>
    Fixed in 39ad190. Removed the dead references to `docs/DECISIONS/2026-04-23-per-maintainer-current-memory-pattern.md` and `memory/feedback_current_memory_per_maintainer_distillation_pattern_prefer_progress_2026_04_23.md` from the fast-path block; both files were never landed. The pattern is now described inline in CLAUDE.md with a citation of BP-284 (role-ref discipline). Proper ADR landing tracked as a P2 BACKLOG row.
    </pre>

### Thread 2 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59IeRV`
- **Path**: `CLAUDE.md`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-23T15:06:10Z on `CLAUDE.md`:90 (association: NONE)

  <pre>
  The instruction references “the memory folder” but doesn’t name the directory, and `CURRENT-&lt;maintainer&gt;.md` is a placeholder that may be interpreted differently by readers. To reduce ambiguity, explicitly state the expected path (e.g., `memory/`) and provide a concrete example filename so contributors know the exact convention to follow.
  ```suggestion
    `CURRENT-&lt;maintainer&gt;.md` files (for example,
    `CURRENT-jane-doe.md`) in
    `~/.claude/projects/&lt;slug&gt;/memory/` *before* the raw
    `feedback_*.md` / `project_*.md` log. CURRENT files
    are the distilled currently-in-force projection per
    maintainer (one per human / external-AI maintainer).
    They win on conflict with older raw
  ```
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-24T21:41:39Z on `CLAUDE.md`:90 (association: MEMBER)

    <pre>
    The path `~/.claude/projects/&lt;slug&gt;/memory/` is named explicitly in the same paragraph (line 88 in original, retained in 39ad190), and `CURRENT-&lt;maintainer&gt;.md` is intentionally a role-ref placeholder per BP-284 (no contributor names in code/docs/skill bodies — only inside `memory/persona/&lt;name&gt;/`). The 39ad190 rewrite tightens this and cites BP-284 directly. Resolving.
    </pre>

### Thread 3 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59IeR_`
- **Path**: `CLAUDE.md`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-23T15:06:11Z on `CLAUDE.md`:94 (association: NONE)

  <pre>
  These references are currently formatted as code spans rather than clickable links. In a navigation-oriented section like this, using standard Markdown links improves usability and reduces friction for readers trying to follow the fast-path documentation.
  ```suggestion
    [`memory/feedback_current_memory_per_maintainer_distillation_pattern_prefer_progress_2026_04_23.md`](memory/feedback_current_memory_per_maintainer_distillation_pattern_prefer_progress_2026_04_23.md).
  ```
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-24T21:41:40Z on `CLAUDE.md`:94 (association: MEMBER)

    <pre>
    Declining the markdown-link form: the targeted file `memory/feedback_current_memory_per_maintainer_distillation_pattern_prefer_progress_2026_04_23.md` does not exist in the tree (sibling thread caught the same dead-ref problem), so converting the code span to a clickable link would worsen the broken-link state, not improve it. 39ad190 removes the dead reference entirely and folds the substance inline; backlog row tracks landing the real ADR + feedback memory. Resolving.
    </pre>

### Thread 4 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59ImQ9`
- **Path**: `CLAUDE.md`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-04-23T15:12:29Z on `CLAUDE.md`:96 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Point fast-path guidance to an existing ADR**

  This new instruction references a `per-maintainer-current-memory-pattern` ADR entry in `docs/DECISIONS/`, but that entry does not exist in this commit tree, so readers cannot verify the rule source or follow the "grep for current filename" fallback. Fresh evidence in the current tree is that the only occurrence of `per-maintainer-current-memory-pattern` is this line itself, which leaves the fast-path policy effectively undocumented and unverifiable.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-24T21:41:41Z on `CLAUDE.md`:96 (association: MEMBER)

    <pre>
    Fixed in 39ad190. The `per-maintainer-current-memory-pattern` ADR pointer is removed from CLAUDE.md (the doc never landed). Substance now folded inline with a BP-284 citation. Proper ADR + companion feedback-memory landing tracked as P2 BACKLOG row in this same commit.
    </pre>

### Thread 5 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59hFl0`
- **Path**: `CLAUDE.md`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-04-24T21:43:08Z on `CLAUDE.md`:88 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Cite an existing BP rule for filename discipline**

  This new guidance makes `BP-284` the authority for `CURRENT-&lt;maintainer&gt;.md` naming, but that BP is not defined in the repo’s canonical BP list (`docs/AGENT-BEST-PRACTICES.md` currently enumerates only BP-07 through BP-24). Because `CLAUDE.md` is now using the citation as normative policy, an unresolved rule ID leaves the naming contract unverifiable and likely to be interpreted inconsistently by different agents; please reference an existing BP or land the cited BP rule first.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-24T22:21:57Z on `CLAUDE.md`:88 (association: MEMBER)

    <pre>
    Fixed in 65511fa. The BP-284 citation is replaced with the actual rule title + file pointer: `docs/AGENT-BEST-PRACTICES.md` "No name attribution in code, docs, or skills" (lines 284-292). Verified: that file enumerates BP-07..BP-24 only; the `284` reference was a confusion with the line number where the rule lives. Same fix folded into the BACKLOG row body. Resolving.
    </pre>

### Thread 6 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59hGeV`
- **Path**: `docs/BACKLOG.md`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-24T21:44:17Z on `docs/BACKLOG.md`:4087 (association: NONE)

  <pre>
  P2: The inline code spans for the referenced file paths are hard-wrapped across lines (e.g., `docs/DECISIONS/...-` then `current-memory-pattern.md`). In GitHub/CommonMark rendering, newlines inside inline code are normalized to spaces, which makes these paths non-copy/pasteable and visually incorrect. Consider keeping each path within a single line of code formatting (or moving the path to its own indented line) so the reference stays exact.
  ```suggestion
    draft cited (`docs/DECISIONS/2026-04-23-per-maintainer-current-memory-pattern.md`
    + `memory/feedback_current_memory_per_maintainer_distillation_pattern_prefer_progress_2026_04_23.md`)
    were never landed. PR #153 review left the pointer in
    CLAUDE.md but removed the dead links; this backlog row
    tracks landing the actual ADR (per-user vs in-repo split,
    role-ref filename discipline per BP-284, same-tick update
    rule, conflict-precedence over raw `feedback_*.md`) so
    the pattern has a citable substrate beyond CLAUDE.md
    prose. Companion feedback memory captures Aaron's
    original framing.
  ```
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-24T22:22:00Z on `docs/BACKLOG.md`:4087 (association: MEMBER)

    <pre>
    Fixed in 65511fa. Each full path is now on its own line of code formatting (prose wraps around the code spans, the spans themselves are unbroken), so CommonMark renders them correctly and they are copy-pasteable. Resolving.
    </pre>

### Thread 7 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59hGes`
- **Path**: `CLAUDE.md`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-24T21:44:17Z on `CLAUDE.md`:97 (association: NONE)

  <pre>
  P2: The PR description/test plan calls out verifying a pointer to `docs/DECISIONS/2026-04-23-per-maintainer-current-memory-pattern.md`, but this new CLAUDE.md paragraph doesn’t reference that ADR (and the repo currently has no such link here). Either add the intended cross-reference (once the ADR exists) or update the PR description/test plan so it matches the actual change.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-24T22:22:04Z on `CLAUDE.md`:97 (association: MEMBER)

    <pre>
    The CLAUDE.md change no longer references the unlanded ADR — commit 39ad190 removed those dead links and 65511fa cleaned up the role-ref citation. The PR description / test plan still mentions the ADR target as an aspirational pointer, which is captured in the P2 BACKLOG row tracking proper landing of the ADR + companion feedback memory. The substantive code change in this PR is consistent with itself; the PR-description / test-plan editorial alignment will follow the BACKLOG row once the ADR lands. Resolving.
    </pre>

### Thread 8 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM59hkE_`
- **Path**: `docs/BACKLOG.md`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-24T22:34:16Z on `docs/BACKLOG.md`:5603 (association: NONE)

  <pre>
  P1: This backlog item claims the ADR and companion feedback memory "were never landed", but both referenced files already exist in the repo (`docs/DECISIONS/2026-04-23-per-maintainer-current-memory-pattern.md` and `memory/feedback_current_memory_per_maintainer_distillation_pattern_prefer_progress_2026_04_23.md`). Update or remove this row (or mark it done) so BACKLOG doesn’t track already-completed work / spread stale history.
  ```suggestion
    distillation pattern. The supporting docs that draft cited
    have since landed as
    `docs/DECISIONS/2026-04-23-per-maintainer-current-memory-pattern.md`
    and
    `memory/feedback_current_memory_per_maintainer_distillation_pattern_prefer_progress_2026_04_23.md`.
    PR #153 review left the pointer in CLAUDE.md but removed
    the dead links; this row is therefore complete because the
    ADR (per-user vs in-repo split, role-ref filename
    discipline per the "No name attribution in code, docs, or
    skills" rule in `docs/AGENT-BEST-PRACTICES.md`, same-tick
    update rule, conflict-precedence over raw `feedback_*.md`)
    now provides a citable substrate beyond CLAUDE.md
  ```
  </pre>

## Fix commits (touching thread paths)

### `25441af338093a3ce2100cdb35f70187c0f773be` -- 2026-04-23T14:54:53Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `CLAUDE.md`

**Message:**

```
CLAUDE.md: fast-path pointer to CURRENT-<maintainer>.md distillation …

…files

The per-maintainer CURRENT.md pattern landed this session (ADR in PR
#152). CLAUDE.md is the session-bootstrap file Claude reads first;
adding a pointer to the CURRENT distillation layer makes future-me
use the fast-path rather than replaying the raw memory log from
scratch on every wake.

## What the addition says

Two things, minimal prose:

1. On wake, read CURRENT-<maintainer>.md files BEFORE the raw
   feedback/project files. CURRENT is the distilled currently-in-
   force projection per maintainer.
2. Same-tick update discipline: when a new memory lands that
   updates a CURRENT rule, edit CURRENT in the same tick. Skipping
   is lying-by-omission.

## Why I chose to add this now

The ADR I landed last tick (PR #152) explicitly noted the CLAUDE.md
update as optional follow-up. Per Aaron's "prefer progress over
quiet close" calibration, doing the follow-up in the same session
the pattern landed keeps the discipline coherent — not "ADR exists
but CLAUDE.md doesn't reference it" divergence.

Small, focused, factory-generic. Applies to any project adopting
the factory pattern.

## What this does NOT do

- Does NOT establish a new discipline — the CURRENT pattern is
  established by the ADR in PR #152 and the per-user memory
  entries. This is just making the discipline discoverable at
  session-bootstrap time.
- Does NOT touch CURRENT files themselves — they're per-user,
  outside this repo.
- Does NOT change the memory taxonomy (user_ / feedback_ /
  project_ / reference_) — CURRENT sits above the taxonomy as
  a projection layer.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
```

### `ef8bc306420938c2e50fe5134cec949e0184ea98` -- 2026-04-24T21:41:17Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `CLAUDE.md`
- `docs/BACKLOG.md`

**Message:**

```
drain: resolve 4 threads on #153

CLAUDE.md fast-path block: drop dead references to
unlanded ADR + feedback memory; tighten prose; cite
BP-284 role-ref discipline directly. Add P2 BACKLOG
row tracking the actual ADR landing.

Threads addressed:
- chatgpt-codex (dead-ref to ADR + feedback file): fixed,
  prose tightened to remove broken links.
- copilot (ambiguous "memory folder"): path is already
  named at `~/.claude/projects/<slug>/memory/`; outdated.
- copilot (markdown-link suggestion): would worsen the
  broken-link state — declined; resolved with reasoning
  in reply.
- chatgpt-codex (dead-ref to ADR, not outdated): fixed
  by removing the ADR pointer; backlog row tracks the
  proper landing as P2 research-grade.
```

### `aaae74c88d52ab72bb0b67d8ce1ceab3e6da66eb` -- 2026-04-24T22:21:32Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `CLAUDE.md`
- `docs/BACKLOG.md`

**Message:**

```
drain: resolve 3 threads on #153

CLAUDE.md fast-path block: replace BP-284 citation with the actual rule
title + file pointer (`docs/AGENT-BEST-PRACTICES.md` "No name attribution
in code, docs, or skills") since BP-284 is not in the canonical BP list.
BACKLOG row 4073-4087: reflow the two long paths onto their own lines so
inline-code spans no longer split across newlines (Copilot's CommonMark
correctness call); same role-ref-citation fix folded into the row body.

Threads addressed:
- chatgpt-codex P2 (CLAUDE.md:88, BP-284 not defined): fixed by replacing
  the unresolved BP citation with the rule title + file pointer to
  docs/AGENT-BEST-PRACTICES.md.
- copilot P2 (BACKLOG.md:4087, hard-wrapped inline code spans): fixed by
  moving each full backticked path onto its own line of code formatting
  with prose wrapping around it.
- copilot P2 (CLAUDE.md:95, PR description references unlanded ADR):
  the file no longer references that ADR (prior commit 39ad190 removed
  the dead link); updating the PR description to match is an editorial
  follow-up, the substantive code change is correct.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
```
