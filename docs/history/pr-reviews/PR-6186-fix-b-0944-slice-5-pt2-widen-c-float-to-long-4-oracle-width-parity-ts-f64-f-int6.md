# PR #6186 -- fix(081KSV2WD0008QG0R00051XS0N slice 5 pt2): widen C# float to long — 4-oracle width parity (TS f64 / F# int64 / C# long / Rust u64)

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 6186 |
| Title | fix(081KSV2WD0008QG0R00051XS0N slice 5 pt2): widen C# float to long — 4-oracle width parity (TS f64 / F# int64 / C# long / Rust u64) |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-05-31T02:48:45Z |
| Merged at | 2026-05-31T02:55:59Z |
| Merge commit SHA | `cf7f4dd4e3791e97bbacda1cabbb0664ad473e0c` |
| Branch | `otto-cli/b0944-slice5pt2-csharp-float-int64-parity-2026-05-30` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/6186 |
| Changed files | 2 |
| Additions / deletions | +36 / -13 |

## Description

## 081KSV2WD0008QG0R00051XS0N slice 5 pt2 — C# float width parity (32-bit → `long`)

Follow-up to the F# fix ([#6183](https://github.com/Lucent-Financial-Group/Zeta/pull/6183) commit `160173c80`). The C# float had the **same latent 32-bit `int` width** Copilot flagged P1 on F#:

- `IntOf` accumulated into `int` (wraps past 31 bits → silent wrong mode/V)
- `FromValue` computed `maxV`/`maxMode`/`bias` with 32-bit `int` (widths 31+ → negative/incorrect bounds instead of `Result` feedback)

Latent at the default 4/3/4 shape (8 value bits) but `FromTrits` + public `Decode` accept arbitrary widths.

### Fix — widen to `long`
`IntOf` accumulator · `IntToTrits` param · `FromValue` bounds (`maxV`/`maxMode`/`bias`) · the mode loop var → `long`.

### Closes the 4-oracle width-parity gap
| Oracle | Width | Exact to |
|---|---|---|
| TS | f64 (`number`) | 2^53 |
| F# | `int64` | 2^53 (then →f64) |
| C# | **`long`** (this PR) | 2^53 (then →f64) |
| Rust | `u64` | 2^53 (then →f64) |

All four now agree up to f64's 2^53 exact-integer range — the shared limit, since the decoded value is f64 in every oracle.

### Verification

- `dotnet build -c Release`: **0 warnings, 0 errors**.
- Tests: **13/13** (unchanged vectors; default-shape behavior identical).

Composes-with: 081KSV2WD0008QG0R00051XS0N (slice 5 pt2) · F# width fix (#6183) · C# float (#6184) · Rust float (#6185).

🤖 Generated with [Claude Code](https://claude.com/claude-code)


## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 2 |
| Resolved threads | 2 |
| Unresolved threads | 0 |
| Total review comments | 2 |
| Total fix commits (touching thread paths) | 1 |

## Review threads

### Thread 1 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F6VYt`
- **Path**: `src/Core.CSharp.TriBoolean/FloatOps.cs`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-31T02:50:36Z on `src/Core.CSharp.TriBoolean/FloatOps.cs`:139 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Bound the mode search for wide decoders**

  For public shapes with `DecoderWidth &gt;= 32`, this widened `long` loop can run for billions of iterations before finding ordinary values: e.g. `FromValue(1.0, new FloatShape(4, 32, 4))` has `bias = 2^31`, so the representable mode is around 2,147,483,648 while the loop starts at 0 and scans one-by-one. Since `FloatShape` is public and `FromTrits` also accepts arbitrary decoder widths, wide-shape calls now effectively hang instead of returning feedback; derive candidate modes analytically or reject/cap widths before entering the loop.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 2 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F6Vc6`
- **Path**: `src/Core.CSharp.TriBoolean/FloatOps.cs`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-31T02:51:00Z on `src/Core.CSharp.TriBoolean/FloatOps.cs`:137 (association: NONE)

  <pre>
  P1: The widened bounds are still untested at the width where the previous bug occurred. The current C# tests only exercise the default 4/3/4 shape, so a regression back to 32-bit arithmetic (or an incorrect long shift/cast) would not be caught; add cases around widths past 31 bits for `Decode`/`FromValue` before relying on this parity fix.
  </pre>

## Fix commits (touching thread paths)

### `fc53dd3a204d7bf53c809baeefcbe609cf17551e` -- 2026-05-31T02:48:35Z -- ``

**Touched files (intersect with thread paths):**

- `src/Core.CSharp.TriBoolean/FloatOps.cs`

**Message:**

```
fix(081KSV2WD0008QG0R00051XS0N slice 5 pt2): widen C# float reads to long (32-bit overflo…

…w + 4-oracle width parity)

Follow-up to the F# fix (#6183 commit 160173c80): the C# float had the same latent 32-bit
int width that Copilot flagged P1 on F# — IntOf accumulated into int (wraps past 31 bits ->
silent wrong mode/V) and FromValue computed maxV/maxMode/bias with 32-bit int (widths 31+
-> negative/incorrect bounds). Latent at the default 4/3/4 shape but FromTrits + public Decode
accept arbitrary widths.

Widen IntOf accumulator + IntToTrits param + FromValue bounds (maxV/maxMode/bias) + the mode
loop var from int to long. This aligns the C# oracle's width with Rust (u64) and F# (int64);
TS uses f64. All four now agree up to f64's 2^53 exact-integer range (the shared limit, since
the decoded value is f64 in every oracle). Class doc updated with the width rationale.

- Build: 0 warnings, 0 errors. Tests: 13/13 (unchanged vectors; default-shape behavior identical).

This closes the width-parity gap across the four-oracle BFT ballot: TS f64 / F# int64 / C# long
/ Rust u64 — consistent to 2^53.

Composes-with: 081KSV2WD0008QG0R00051XS0N (slice 5 pt2); F# width fix (#6183); C# float (#6184); Rust float (#6185).

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
```
