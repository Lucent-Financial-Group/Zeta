# PR #6459 -- feat(resume): byte-lock resume STATE wire — expectedStateAtSuspension golden (TS foundation)

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 6459 |
| Title | feat(resume): byte-lock resume STATE wire — expectedStateAtSuspension golden (TS foundation) |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-06-01T19:23:13Z |
| Merged at | 2026-06-01T19:29:49Z |
| Merge commit SHA | `a165b08603f6df89e34901ee0f83077b00377e57` |
| Branch | `otto-cli/resume-state-bytelock-golden-ts-2026-06-01` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/6459 |
| Changed files | 2 |
| Additions / deletions | +31 / -6 |

## Description

## What

Byte-locks the resume STATE wire across the four oracles. The resume slice (081KT07NV0008QG0R003BE6MJ2) was
byte-locked on suspension `fn`+`args` + final value, but the persisted `SagaState` bytes
were only behaviorally verified (per-oracle round-trip). This adds **`expectedStateAtSuspension`**
to `resume-golden.json` — the exact canonical `serializeState()` bytes the TS reference emits
at each suspension, in order — and asserts them in the TS oracle.

## Why this matters (the gap it closes)

State serialization was NOT byte-locked across oracles. The F# `kont` cons-list (top-first)
serializes **reversed** vs the TS array (top-last) on multi-frame konts. C#/Rust match TS by
construction (`Vec`/`IReadOnlyList` top-last). Behavioral tests passed anyway because each
oracle round-trips its own bytes — so the divergence was invisible until a saga is persisted
in one oracle and resumed in another.

The high-value golden case is **`cond_branch_on_activity`**: its 2-frame kont
(`branch` + `evalRight`) is exactly where the order diverges. The golden now pins **top-last**
(innermost frame first in the array), making it the single cross-oracle wire reference.

## Scope (foundation slice)

- `resume-golden.json`: `expectedStateAtSuspension` added to all 5 traces (canonical bytes
  authored by the TS reference engine) + description documents the byte-lock contract.
- `resume.test.ts`: `Trace.expectedStateAtSuspension` field + `expect(ser.value).toBe(...)`
  assertion at every suspension.

## Follow-ups (separate PRs)

1. Add the byte-equality assertion to the F#/C#/Rust resume cross-verify tests.
2. Align F# `Resume.fs` kont serialization order to the array reference (the actual fix).

## Verification

- `bun test bonsai/resume.test.ts` → **16 pass / 0 fail** (86 expect calls)
- root `tsc --noEmit` → **0 errors**

🤖 Generated with [Claude Code](https://claude.com/claude-code)


## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | true |
| Total threads | 1 |
| Resolved threads | 1 |
| Unresolved threads | 0 |
| Total review comments | 2 |
| Total fix commits (touching thread paths) | 2 |

## Review threads

### Thread 1 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GOcKS`
- **Path**: `src/Core.TypeScript/bonsai/resume.test.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-01T19:25:52Z on `src/Core.TypeScript/bonsai/resume.test.ts`:51 (association: NONE)

  <pre>
  P2 (nit): The test indexes `expectedStateAtSuspension[i]` but doesn’t assert that `expectedStateAtSuspension` (or `activityResults`) has the same length as `expectedSuspensions`. When the golden fixture is malformed, the failure can be a confusing `undefined` comparison rather than a clear schema mismatch. Consider adding explicit length equality checks before the loop.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-06-01T19:26:52Z on `src/Core.TypeScript/bonsai/resume.test.ts`:51 (association: MEMBER)

    <pre>
    Done in e5219d949 — added up-front `expect(tr.expectedStateAtSuspension.length).toBe(tr.expectedSuspensions.length)` + the same for `activityResults`, so a malformed golden fails as a clear schema mismatch rather than an undefined compare. 16 pass / 0 fail, tsc 0 errors.
    </pre>

## Fix commits (touching thread paths)

### `beb377b5873e84349bc81ffd5500dfb04d4b3e39` -- 2026-06-01T19:23:04Z -- ``

**Touched files (intersect with thread paths):**

- `src/Core.TypeScript/bonsai/resume.test.ts`

**Message:**

```
feat(resume): byte-lock the resume STATE wire — expectedStateAtSuspen…

…sion golden (TS foundation)

The resume slice (081KT07NV0008QG0R003BE6MJ2) was byte-locked on suspension fn+args + final value, but
NOT on the persisted SagaState bytes — only behavioral + per-oracle round-trip. This
adds `expectedStateAtSuspension` to resume-golden.json: the exact canonical
serializeState() bytes the TS reference emits at each suspension, in order. The TS
oracle now asserts ser.value === expectedStateAtSuspension[i] at every suspension.

This is the cross-oracle STATE-BYTE lock: every ferry (F#/C#/Rust) must reproduce
these bytes verbatim, not merely behave equivalently. The high-value case is
cond_branch_on_activity — its 2-frame kont (branch + evalRight) is exactly where the
F# cons-list serializes REVERSED vs the TS array; the golden pins top-last order
(innermost frame first), so the F# ferry's kont serialization must align to it.

Foundation slice: TS golden authored + TS assertion. Follow-ups (separate PRs):
add the byte-equality assertion to the F#/C#/Rust resume cross-verify tests, and
align F# Resume.fs kont serialization order to the array reference.

Verified: bun test bonsai/resume.test.ts → 16 pass / 0 fail (86 expect calls);
root tsc --noEmit → 0 errors.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `e5219d949450cfe436dea52dcc7062d2625d6c92` -- 2026-06-01T19:26:45Z -- ``

**Touched files (intersect with thread paths):**

- `src/Core.TypeScript/bonsai/resume.test.ts`

**Message:**

```
test(resume): assert golden fixture schema up front (Copilot P2 — cle…

…ar mismatch not undefined compare)

Copilot P2 nit on #6459: the byte-lock loop indexes expectedStateAtSuspension[i] without
asserting it has the same length as expectedSuspensions, so a malformed golden would surface
a confusing `ser.value === undefined` comparison instead of a clear schema mismatch.

Add up-front length-equality guards (expectedStateAtSuspension and activityResults each match
expectedSuspensions) per trace, so a fixture schema error fails loudly and obviously.

Verified: bun test → 16 pass / 0 fail (96 expect calls); tsc --noEmit → 0 errors.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```
