# PR #3003 -- feat(scaffold): B-0424.3 — UPSTREAM-RHYTHM.md templates for Forge + ace scaffold dirs

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 3003 |
| Title | feat(scaffold): B-0424.3 — UPSTREAM-RHYTHM.md templates for Forge + ace scaffold dirs |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-05-13T16:55:27Z |
| Merged at | 2026-05-13T17:32:57Z |
| Merge commit SHA | `41ba30d6bc40f15f790a5b50989bebb9da6a8c11` |
| Branch | `otto-b-0424-3-upstream-rhythm-templates-2026-05-13` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/3003 |
| Changed files | 3 |
| Additions / deletions | +320 / -0 |

## Description

## Summary

- Adds `docs/UPSTREAM-RHYTHM.md` to `tools/scaffold/forge/` — closes the ADR 2026-04-22 checklist item: *"docs/UPSTREAM-RHYTHM.md per repo (Zeta's serves as template)"*
- Adds `docs/UPSTREAM-RHYTHM.md` to `tools/scaffold/ace/` — same checklist item for ace
- Updates `tools/scaffold/README.md` to list the new files in the template tree

Both templates adapt Zeta's batched fork-first rhythm doc (referencing `AceHack/Forge` and `AceHack/ace` respectively) and cross-reference Zeta's canonical doc for the full cost-rationale section. No code changes; `collectFiles()` in `create-repo.ts` recursively enumerates the scaffold dir, so new template files are picked up automatically.

## Context

B-0424.1 (PR #2994) added governance templates + dry-run tool.
B-0424.2 (PR #2996) added the `workflow_dispatch` gate.
B-0424.3 (this PR) closes the `docs/UPSTREAM-RHYTHM.md` gap in the ADR checklist.

ADR: `docs/DECISIONS/2026-04-22-three-repo-split-zeta-forge-ace.md`

## Dry-run verification

```
bun tools/scaffold/create-repo.ts --repo forge --dry-run
```
```json
{
  "step": "06-push-scaffold-files",
  "description": "Push 11 scaffold files to Lucent-Financial-Group/Forge via git",
  "data": {
    "files": [
      "CODE_OF_CONDUCT.md",
      "LICENSE",
      "GOVERNANCE.md",
      "docs/UPSTREAM-RHYTHM.md",   ← new
      "docs/GITHUB-SETTINGS.md",
      "README.md",
      "CONTRIBUTING.md",
      ".github/copilot-instructions.md",
      "AGENTS.md",
      "CLAUDE.md",
      "SECURITY.md"
    ]
  }
}
```
Both repos: **12 operations planned, 0 failures**.

## What's still left for B-0424

After this PR, the remaining items before triggering the `workflow_dispatch` are:
1. `SCAFFOLD_STAGE1_PAT` secret created in LFG org settings (one-time Aaron action)
2. Social preview SVG/PNG (post-creation manual step)
3. OpenSSF Scorecard + Semgrep workflow copies (post-creation manual step)

The actual repo creation (`--apply`) is Aaron-triggered via the workflow in `.github/workflows/scaffold-stage1-create-repos.yml`.

## Test plan

- [x] `bun tools/scaffold/create-repo.ts --repo forge --dry-run` — 12 ops planned, 11 files including `docs/UPSTREAM-RHYTHM.md`
- [x] `bun tools/scaffold/create-repo.ts --repo ace --dry-run` — 12 ops planned, 11 files including `docs/UPSTREAM-RHYTHM.md`
- [x] No code changes; build gate satisfied trivially (markdown-only additions)

operative-authorization: aaron 2026-05-13: "Cooling period: TBD. The memory file IS the durable record"

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | true |
| Total threads | 9 |
| Resolved threads | 9 |
| Unresolved threads | 0 |
| Total review comments | 18 |
| Total fix commits (touching thread paths) | 6 |

## Review threads

### Thread 1 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6B1Mom`
- **Path**: `tools/scaffold/ace/docs/UPSTREAM-RHYTHM.md`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-13T16:58:59Z on `tools/scaffold/ace/docs/UPSTREAM-RHYTHM.md`:90 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Align bulk-sync merge method with scaffold settings**

  This template requires `gh pr merge ... --merge` for the upstream bulk-sync PR, but the scaffold creator configures new repos with `allow_merge_commit: false` in `tools/scaffold/create-repo.ts` (squash-only). On a freshly scaffolded `ace`/`Forge` repo, this documented command will fail because merge commits are disabled, which blocks the bulk-sync and subsequent forward-sync flow described here. Please either enable merge commits in scaffold defaults for these repos or update the cadence doc to a merge method that is actually allowed by default.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-13T17:02:20Z on `tools/scaffold/ace/docs/UPSTREAM-RHYTHM.md`:90 (association: MEMBER)

    <pre>
    Fixed in the follow-up commit. Changed both `ace` and `Forge` templates from `--merge` to `--squash`, matching the scaffold's squash-only default (`allow_merge_commit: false`). Updated the comment to explain the constraint. GitHub's `merge-upstream` API handles post-squash forward-sync without requiring a true fast-forward.
    </pre>

### Thread 2 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6B1NJ9`
- **Path**: `tools/scaffold/forge/docs/UPSTREAM-RHYTHM.md`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-13T16:59:25Z on `tools/scaffold/forge/docs/UPSTREAM-RHYTHM.md`:9 (association: NONE)

  <pre>
  The template references `.claude/skills/fork-pr-workflow/SKILL.md`, but the current scaffold directories for Forge don’t include a `.claude/` tree. When this file is pushed into the new repo, that path will be a broken reference. Either add the relevant `.claude/skills` content to the scaffold, or change this reference to point at the canonical skill location in Zeta (or otherwise document where the skill actually lives for Forge).

  This issue also appears on line 114 of the same file.

  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-13T17:03:37Z on `tools/scaffold/forge/docs/UPSTREAM-RHYTHM.md`:9 (association: MEMBER)

    <pre>
    Fixed: updated Forge template to reference `Lucent-Financial-Group/Zeta: .claude/skills/fork-pr-workflow/SKILL.md` (fully qualified) instead of the relative path. The skill lives in Zeta, not in scaffolded repos.
    </pre>

### Thread 3 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6B1NKd`
- **Path**: `tools/scaffold/ace/docs/UPSTREAM-RHYTHM.md`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-13T16:59:26Z on `tools/scaffold/ace/docs/UPSTREAM-RHYTHM.md`:8 (association: NONE)

  <pre>
  The template references `.claude/skills/fork-pr-workflow/SKILL.md`, but the current ace scaffold directory doesn’t include a `.claude/` tree. When this file is pushed into the new repo, that path will be a broken reference. Either add the relevant `.claude/skills` content to the scaffold, or change this reference to point at the canonical skill location in Zeta (or otherwise document where the skill actually lives for ace).

  This issue also appears on line 101 of the same file.

  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-13T17:03:42Z on `tools/scaffold/ace/docs/UPSTREAM-RHYTHM.md`:8 (association: MEMBER)

    <pre>
    Fixed: updated ace template to reference `Lucent-Financial-Group/Zeta: .claude/skills/fork-pr-workflow/SKILL.md` (fully qualified) instead of the relative path. The skill lives in Zeta, not in scaffolded repos.
    </pre>

### Thread 4 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6B1Unu`
- **Path**: `tools/scaffold/ace/docs/UPSTREAM-RHYTHM.md`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-13T17:06:30Z on `tools/scaffold/ace/docs/UPSTREAM-RHYTHM.md`:79 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Swap compare base/head in bulk-sync precondition check**

  The precondition check compares `main...Lucent-Financial-Group:main`, but GitHub’s compare API defines this as `BASE...HEAD` (starting point to endpoint, equivalent to `git log BASE..HEAD`), so this query reports commits on upstream that are not on the fork, not the other way around. In practice the `.status/.ahead_by/.behind_by` output is inverted relative to the stated precondition (“AceHack is ahead by ~10”), which can cause operators to run or skip bulk sync at the wrong time. The same reversed compare expression appears in the Forge template as well.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-13T17:12:01Z on `tools/scaffold/ace/docs/UPSTREAM-RHYTHM.md`:79 (association: MEMBER)

    <pre>
    Fixed: swapped compare direction from `main...Lucent-Financial-Group:main` to `Lucent-Financial-Group:main...main` in both ace and Forge templates. With LFG as BASE and AceHack as HEAD, `ahead_by` now correctly measures AceHack's lead over LFG — matching the stated precondition. Commit 9913c8b1.
    </pre>

### Thread 5 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6B1WA5`
- **Path**: `tools/scaffold/forge/docs/UPSTREAM-RHYTHM.md`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-13T17:07:52Z on `tools/scaffold/forge/docs/UPSTREAM-RHYTHM.md`:131 (association: NONE)

  <pre>
  P1: This template says the bulk sync PR must be squash-merged to match upstream merge settings, but it also instructs a `merge-upstream` forward-sync afterward. Squash-merging the bulk sync makes upstream history diverge from the fork and can turn the forward-sync into a non-fast-forward merge on the fork (and breaks the canonical “ahead/behind” expectations until the sync completes). Either (a) change the scaffolded repo settings/rhythm to allow merge commits for the bulk sync (as Zeta’s canonical `docs/UPSTREAM-RHYTHM.md` recommends), or (b) explicitly document the consequences/requirements of the squash-only approach (e.g., that the fork must allow merge commits and the compare output expectations differ before forward-sync).

  This issue also appears in the following locations of the same file:
  - line 114
  - line 136
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-13T17:12:09Z on `tools/scaffold/forge/docs/UPSTREAM-RHYTHM.md`:131 (association: MEMBER)

    <pre>
    Fixed: added a NOTE block explaining the squash-only divergence constraint. The note covers: (1) why --squash is required (scaffold sets allow_merge_commit: false), (2) how squash-merge rewrites history unlike Zeta's canonical --merge pattern, (3) that merge-upstream still works but creates a merge commit rather than a fast-forward, and (4) that compare showing ahead_by=1 after forward-sync is expected and not a new bulk-sync signal. Commit 02afbdcc.
    </pre>

### Thread 6 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6B1WBl`
- **Path**: `tools/scaffold/ace/docs/UPSTREAM-RHYTHM.md`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-13T17:07:52Z on `tools/scaffold/ace/docs/UPSTREAM-RHYTHM.md`:118 (association: NONE)

  <pre>
  P1: This template says the bulk sync PR must be squash-merged to match upstream merge settings, but it also instructs a `merge-upstream` forward-sync afterward. Squash-merging the bulk sync makes upstream history diverge from the fork and can turn the forward-sync into a non-fast-forward merge on the fork (and breaks the canonical “ahead/behind” expectations until the sync completes). Either (a) change the scaffolded repo settings/rhythm to allow merge commits for the bulk sync (as Zeta’s canonical `docs/UPSTREAM-RHYTHM.md` recommends), or (b) explicitly document the consequences/requirements of the squash-only approach (e.g., that the fork must allow merge commits and the compare output expectations differ before forward-sync).

  This issue also appears in the following locations of the same file:
  - line 101
  - line 123
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-13T17:12:15Z on `tools/scaffold/ace/docs/UPSTREAM-RHYTHM.md`:118 (association: MEMBER)

    <pre>
    Fixed: added a NOTE block explaining the squash-only divergence constraint. The note covers: (1) why --squash is required (scaffold sets allow_merge_commit: false), (2) how squash-merge rewrites history unlike Zeta's canonical --merge pattern, (3) that merge-upstream still works but creates a merge commit rather than a fast-forward, and (4) that compare showing ahead_by=1 after forward-sync is expected and not a new bulk-sync signal. Commit 02afbdcc.
    </pre>

### Thread 7 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6B1c5o`
- **Path**: `tools/scaffold/forge/docs/UPSTREAM-RHYTHM.md`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-13T17:14:53Z on `tools/scaffold/forge/docs/UPSTREAM-RHYTHM.md`:126 (association: NONE)

  <pre>
  P1: This template references `docs/BUGS.md`, but the scaffolded Forge repo doesn’t include that file (currently only `docs/GITHUB-SETTINGS.md` and this doc). Either add `docs/BUGS.md` to the scaffold, or change the exception wording to point at the actual security-tracking surface (e.g., GitHub Security advisories or `Lucent-Financial-Group/Zeta: docs/BUGS.md`).

  This issue also appears on line 146 of the same file.

  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-13T17:30:25Z on `tools/scaffold/forge/docs/UPSTREAM-RHYTHM.md`:126 (association: MEMBER)

    <pre>
    Fixed in commit 715bc958: rephrased the Security P0 exception to not reference `docs/BUGS.md` as if it exists on day one. New text points at GitHub Security advisories or a `docs/BUGS.md` once created, following the Zeta pattern.
    </pre>

### Thread 8 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6B1c6R`
- **Path**: `tools/scaffold/ace/docs/UPSTREAM-RHYTHM.md`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-13T17:14:54Z on `tools/scaffold/ace/docs/UPSTREAM-RHYTHM.md`:113 (association: NONE)

  <pre>
  P1: This template references `docs/BUGS.md`, but the scaffolded ace repo doesn’t include that file (currently only `docs/GITHUB-SETTINGS.md` and this doc). Either add `docs/BUGS.md` to the scaffold, or change the exception wording to point at the actual security-tracking surface (e.g., GitHub Security advisories or `Lucent-Financial-Group/Zeta: docs/BUGS.md`).

  This issue also appears on line 133 of the same file.

  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-13T17:30:32Z on `tools/scaffold/ace/docs/UPSTREAM-RHYTHM.md`:113 (association: MEMBER)

    <pre>
    Fixed in commit 715bc958: rephrased the Security P0 exception to not reference `docs/BUGS.md` as if it exists on day one. New text points at GitHub Security advisories or a `docs/BUGS.md` once created, following the Zeta pattern.
    </pre>

### Thread 9 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6B1d9L`
- **Path**: `tools/scaffold/forge/docs/UPSTREAM-RHYTHM.md`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-13T17:15:55Z on `tools/scaffold/forge/docs/UPSTREAM-RHYTHM.md`:119 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Fix forward-sync guidance for squash-based bulk sync**

  The forward-sync note says `merge-upstream` will create a merge commit after squash bulk sync, but GitHub’s sync semantics are fast-forward-only for this operation (`gh repo sync` documents “A fast forward update will be used…”, and the REST `merge-upstream` endpoint returns `409/422` when it cannot sync). After a squash merge into LFG, the histories diverge, so this command can fail instead of “handling” the divergence, which leaves the fork/upstream cadence stuck; the same incorrect guidance is duplicated in `tools/scaffold/ace/docs/UPSTREAM-RHYTHM.md`.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-13T17:30:40Z on `tools/scaffold/forge/docs/UPSTREAM-RHYTHM.md`:119 (association: MEMBER)

    <pre>
    Fixed in commit 715bc958 (both Forge and ace templates): removed the incorrect claim that `merge-upstream` creates a merge commit. Replaced with a CAUTION block that documents: (1) `merge-upstream` is fast-forward-only and returns 409/422 on a diverged fork, (2) forward-sync is optional — GitHub's 3-way PR diff correctly diffs only new commits against the common ancestor so the next bulk-sync PR works regardless, (3) the force-reset alternative for maintainers who want a clean AceHack history.
    </pre>

## Fix commits (touching thread paths)

### `8517999daf03db431d605580104eae1b11d46dda` -- 2026-05-13T16:55:07Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/scaffold/ace/docs/UPSTREAM-RHYTHM.md`
- `tools/scaffold/forge/docs/UPSTREAM-RHYTHM.md`

**Message:**

```
feat(scaffold): B-0424.3 — UPSTREAM-RHYTHM.md templates for Forge + a…

…ce scaffold dirs

Adds the missing docs/UPSTREAM-RHYTHM.md template to both
tools/scaffold/forge/ and tools/scaffold/ace/, closing the gap
in the ADR 2026-04-22 checklist:

  "docs/UPSTREAM-RHYTHM.md per repo (Zeta's serves as template)"

Both templates adapt Zeta's batched fork-first rhythm doc to
their respective repos (AceHack/Forge and AceHack/ace) while
cross-referencing Zeta's canonical doc for the full cost-
rationale section.

Also updates tools/scaffold/README.md to list the new files.

Dry-run verification:
  bun tools/scaffold/create-repo.ts --repo forge --dry-run
  → "Push 11 scaffold files" (was 10; docs/UPSTREAM-RHYTHM.md confirmed in list)

  bun tools/scaffold/create-repo.ts --repo ace --dry-run
  → "Push 11 scaffold files" (same; 12 operations planned total)

No code changes — collectFiles() recursively enumerates the
scaffold dir so new template files are picked up automatically.

operative-authorization: aaron 2026-05-13: "Cooling period: TBD. The memory file IS the durable record"

Co-Authored-By: Claude <noreply@anthropic.com>
```

### `f07b673b52a663ed82e8901ac262f877cf96a4cd` -- 2026-05-13T17:02:08Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/scaffold/ace/docs/UPSTREAM-RHYTHM.md`
- `tools/scaffold/forge/docs/UPSTREAM-RHYTHM.md`

**Message:**

```
fix(scaffold): align bulk-sync merge method with squash-only scaffold…

… settings

Both ace and Forge UPSTREAM-RHYTHM templates documented `--merge` for
the bulk-sync PR, but the scaffold creates LFG repos with
`allow_merge_commit: false` (squash-only). `--merge` would fail on a
freshly scaffolded repo.

Fix: change to `--squash` in both templates, matching the scaffold
default. GitHub's `merge-upstream` API handles post-squash forward-sync
without requiring a true fast-forward.

Addresses Vera (codex-connector) P1 finding on PR #3003.

Co-Authored-By: Claude <noreply@anthropic.com>
```

### `bc9dccdfee16ffc9b912b95744065861218d6607` -- 2026-05-13T17:03:17Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/scaffold/ace/docs/UPSTREAM-RHYTHM.md`
- `tools/scaffold/forge/docs/UPSTREAM-RHYTHM.md`

**Message:**

```
fix(scaffold): qualify fork-pr-workflow skill path to Zeta repo in te…

…mplates

The UPSTREAM-RHYTHM templates referenced `.claude/skills/fork-pr-workflow/SKILL.md`
as a relative path, but the scaffold doesn't include `.claude/skills/` in the
new repo. Updated both ace and Forge templates to qualify the path as
`Lucent-Financial-Group/Zeta: .claude/skills/fork-pr-workflow/SKILL.md`.

Addresses Vera (codex-connector) findings on threads 2 and 3, PR #3003.

Co-Authored-By: Claude <noreply@anthropic.com>
```

### `9913c8b17dd6fd08d325e2e820ab13342d97feff` -- 2026-05-13T17:09:15Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/scaffold/ace/docs/UPSTREAM-RHYTHM.md`
- `tools/scaffold/forge/docs/UPSTREAM-RHYTHM.md`

**Message:**

```
fix(scaffold): swap compare base/head in bulk-sync precondition (ace …

…+ Forge)

GitHub compare API is BASE...HEAD; `ahead_by` = commits in HEAD not in BASE.
Inverted form `main...LFG:main` measured LFG's lead over AceHack; correct
form `LFG:main...main` measures AceHack's lead — matching the stated
precondition comment.

Addresses Codex P2 review finding on PR #3003.

Co-Authored-By: Claude <noreply@anthropic.com>
```

### `02afbdcc4a40f8ede076e8aaf6f86312045ccfb8` -- 2026-05-13T17:11:46Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/scaffold/ace/docs/UPSTREAM-RHYTHM.md`
- `tools/scaffold/forge/docs/UPSTREAM-RHYTHM.md`

**Message:**

```
docs(scaffold): document squash-divergence in bulk-sync (ace + Forge)

Squash-only repos (scaffold default: allow_merge_commit: false) cannot
use --merge for the bulk-sync PR. Added NOTE explaining the consequence:
squash-merge rewrites history, the forward-sync via merge-upstream still
works but produces a merge commit (not a fast-forward), and the compare
check showing ahead_by=1 afterward is expected, not a new bulk-sync signal.

Follows Zeta canonical docs/UPSTREAM-RHYTHM.md §"Bulk sync" NOTE pattern,
adapted for squash-only constraint. Addresses Copilot P1 threads on PR #3003.

Co-Authored-By: Claude <noreply@anthropic.com>
```

### `715bc9580802f437a274fe754c85aee552320500` -- 2026-05-13T17:30:11Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/scaffold/ace/docs/UPSTREAM-RHYTHM.md`
- `tools/scaffold/forge/docs/UPSTREAM-RHYTHM.md`

**Message:**

```
fix(scaffold): address Copilot/Codex P1 findings in UPSTREAM-RHYTHM t…

…emplates

Three unresolved review threads on PR #3003 — all fixed in this commit:

1. docs/BUGS.md broken pointer (Copilot, forge line 126 + ace line 113):
   Scaffold does not include docs/BUGS.md on day one. Rephrased Security P0
   exception to reference GitHub Security advisories or docs/BUGS.md once
   created, following Lucent-Financial-Group/Zeta as pattern.

2. Forward-sync guidance incorrect (Codex P1, forge line 119 + ace mirror):
   merge-upstream is fast-forward-only and returns 409/422 on a diverged fork.
   Replaced the incorrect claim with a CAUTION block that: (a) documents the
   actual failure mode, (b) explains forward-sync is optional (3-way PR diff
   still correctly diffs only new commits), (c) provides the force-reset
   alternative for maintainers who want a clean AceHack history.

Co-Authored-By: Claude <noreply@anthropic.com>
```
