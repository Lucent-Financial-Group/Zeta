# PR #6445 -- Inventory: Phase 5 — Typed dynamic custom fields (CENTERPIECE)

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 6445 |
| Title | Inventory: Phase 5 — Typed dynamic custom fields (CENTERPIECE) |
| Author | `maximdolphin` (human) |
| State | MERGED |
| Created at | 2026-06-01T17:52:06Z |
| Merged at | 2026-06-03T20:26:32Z |
| Merge commit SHA | `31ea8bdd437ac85b20a863d9af2774806354ebf9` |
| Branch | `claude/festive-ritchie-NcXnW` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/6445 |
| Changed files | 10 |
| Additions / deletions | +1612 / -46 |

## Description

Implements **Phase 5 (Typed dynamic custom fields)** per `inventory/spec.md` + `inventory/PROGRESS.md`. Highest-risk phase; building in a 5a → 5b order with a checkpoint (owner-approved). **DRAFT until every gate proof lands with observed evidence.**

## Approach (owner-approved decisions)

- **Test arch:** shared logic extracted to same-origin `inventory/lib/custom-fields.js` (one source of truth; unit-tested; CSP `'self'`, no build step).
- **Admin proofs:** via a burnable admin test user (ENV creds at run time) → full add-field/deactivate flow exercised through real RLS.
- **Phasing:** one phase, checkpoint at the 5a boundary.

## The three named risks

1. **DB-side type validation (not app-only):** `sql/phase5.sql` adds a `BEFORE INSERT/UPDATE` trigger `validate_custom_fields()` (a trigger, not a CHECK constraint — a CHECK can't subquery `field_definitions`). Rejects malformed values + unknown keys **at the database**, even via direct PostgREST with the anon key. `SECURITY DEFINER` so validation doesn't depend on the writer's read policy. **No RLS/GRANT change.**
2. **Typed search & sort:** client-side (consistent with Phase 3), with a per-type comparator (number→numeric `9<10<100`, date→chronological, boolean→false<true, text/dropdown→locale; nulls/mixed safe). Proven 3 ways: unit test, DB cast proof (lexicographic `10,100,9` vs typed `9,10,100`), and Playwright in-app.
3. **XSS:** `textContent` at every custom render site (headers, cells, cards, form labels) — names *and* values. Proven inert with an `<img onerror>`/`<script>` sentinel payload.

## Status

- [x] **5a/1** schema migration + DB proof + shared lib + unit suite (20 pass; sabotaged comparator → 2 fail, demonstrating fail-on-broken).
- [ ] 5a/2 `index.html` wiring: load field defs, render custom columns/cards, typed form inputs, admin Manage-fields (add/deactivate).
- [ ] 5a/3 REST proof harness (`proofs/phase5-custom-fields-proofs.ts`).
- [ ] 5b search/sort wiring + Playwright XSS/search/sort browser proofs.
- [ ] Run all proofs live (needs owner-provided ENV creds: URL/anon are public; **admin + editor test-user passwords required** — not invented).
- [ ] PROGRESS.md evidence; owner runs `phase5.sql` + `phase5_proofs.sql` in the SQL editor.

**Security invariants preserved:** no `service_role` anywhere; no permissive/`USING(true)` policy; `change_log` still immutable; anon reads on all four sensitive tables must still return `[]` (re-checked in the REST proof). `required`-at-DB intentionally deferred (would break edits of the 210 existing items).

https://claude.ai/code/session_017GpaRnmUo2Nf8iyNbhQtJM

---
_Generated by [Claude Code](https://claude.ai/code/session_017GpaRnmUo2Nf8iyNbhQtJM)_

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 1 |
| Resolved threads | 1 |
| Unresolved threads | 0 |
| Total review comments | 1 |
| Total fix commits (touching thread paths) | 6 |

## Review threads

### Thread 1 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6G2Htm`
- **Path**: `.github/workflows/inventory-phase5-proof.yml`
- **Replies**: 0

**Initial comment:**

- **`github-advanced-security[bot]` (bot)** at 2026-06-03T18:07:00Z on `.github/workflows/inventory-phase5-proof.yml`:47 (association: NONE)

  <pre>
  ## CodeQL / Unpinned tag for a non-immutable Action in workflow or composite action

  Unpinned 3rd party Action 'inventory-phase5-proof' step [Uses Step](1) uses 'oven-sh/setup-bun' with ref 'v2', not a pinned commit hash

  [Show more details](https://github.com/Lucent-Financial-Group/Zeta/security/code-scanning/162)
  </pre>

## Fix commits (touching thread paths)

### `b8f7cdaa154e7a85666751ef0f86c4792782fe35` -- 2026-06-03T18:04:34Z -- `claude`

**Touched files (intersect with thread paths):**

- `.github/workflows/inventory-phase5-proof.yml`

**Message:**

```
Inventory Phase 5 (5a/5): CI workflow to run the live REST proof via …

…the admin secret

.github/workflows/inventory-phase5-proof.yml — repo secrets are write-only via
the API (only readable inside Actions), so the admin-session client-path proof
runs here, consuming secret `inventory_admin_login`.
- Gated: runs only on workflow_dispatch OR a push whose commit message contains
  [run-phase5-proof] — never auto-runs / never creates live data on a plain push.
- Public-repo safe: only the masked secret is a credential; SUPABASE_URL + anon
  key are already public; runs everything as ADMIN (no editor password in repo;
  validate_custom_fields() is SECURITY DEFINER so it validates all roles).
- Flexible secret parse (JSON | email:password | "email password"); split-out
  parts re-masked and written to $GITHUB_ENV without echo; harness logs no creds.
- contents: read only. PREREQ: owner runs inventory/sql/phase5.sql first.

https://claude.ai/code/session_017GpaRnmUo2Nf8iyNbhQtJM
```

### `56a0161236cea38fca044f54d5cf897b505d2061` -- 2026-06-03T18:07:47Z -- `claude`

**Touched files (intersect with thread paths):**

- `.github/workflows/inventory-phase5-proof.yml`

**Message:**

```
security(inventory ci): pin third-party actions to commit SHAs (suppl…

…y-chain)

Per the automated security review + .claude/rules dep-pin-search-first-authority:
pin both actions in inventory-phase5-proof.yml to verified 40-char commit SHAs
(tag comment kept for Dependabot):
  actions/checkout @11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
  oven-sh/setup-bun @735343b667d3e6f658f44d0eca948eb6282f2b76 # v2.0.2
SHAs verified via WebSearch (StepSecurity/community pin guides); current latest
of each major. A mutable tag could be silently re-pointed to a different payload.

https://claude.ai/code/session_017GpaRnmUo2Nf8iyNbhQtJM
```

### `8410c32e404e78c3364cb0d15bdee1ea24531eb3` -- 2026-06-03T18:51:17Z -- `claude`

**Touched files (intersect with thread paths):**

- `.github/workflows/inventory-phase5-proof.yml`

**Message:**

```
ci: broaden admin-secret delimiter parsing + retrigger [run-phase5-pr…

…oof]

Run #3 showed the secret is present (LOGIN: ***) but my parser couldn't split
email+password (JSON/colon/space only). Likely a newline/comma/pipe delimiter.
Normalize any of : , ; | = tab CR LF to a space before awk-splitting, and print
a non-leaking token count for diagnosis. No values are ever echoed.

https://claude.ai/code/session_017GpaRnmUo2Nf8iyNbhQtJM
```

### `7060494ee19edf5c4c8aa229836b520181392e74` -- 2026-06-03T18:54:22Z -- `claude`

**Touched files (intersect with thread paths):**

- `.github/workflows/inventory-phase5-proof.yml`

**Message:**

```
ci(inventory phase5): resolve admin creds from 2 dedicated secrets (J…

…SON fallback)

Run #4 diagnostic: inventory_admin_login normalized to 4 tokens (labeled/key=value
shape), so the whitespace split grabbed the wrong email/password -> sign-in 400.
Eliminate parsing ambiguity: prefer secrets INVENTORY_ADMIN_EMAIL +
INVENTORY_ADMIN_PASSWORD; fall back to inventory_admin_login as JSON
{"email","password"}; else fail with a clear fix message. No fragile whitespace
split; creds never echoed. (No [run-phase5-proof] flag here — won't run until the
secret form is set.)

https://claude.ai/code/session_017GpaRnmUo2Nf8iyNbhQtJM
```

### `aee8c9268bcebd1e77341b6aae87048e6bdd6532` -- 2026-06-03T19:38:40Z -- `claude`

**Touched files (intersect with thread paths):**

- `.github/workflows/inventory-phase5-proof.yml`

**Message:**

```
ci/proof: surface sign-in failure reason + trim secret CR/LF [run-pha…

…se5-proof]

Run #6 resolved creds (two-secret) but admin sign-in still 400. To end the
guessing without leaking values:
- harness signIn() now prints the Supabase auth error BODY (invalid_grant vs
  email_not_confirmed; the response never echoes the password).
- workflow trims accidental CR/LF from the secret values and prints a shape-only
  diagnostic (email length, has '@', password length) — no values.

https://claude.ai/code/session_017GpaRnmUo2Nf8iyNbhQtJM
```

### `7bb3dcfd2ab9d96b6317fceb247d8c2712147ecf` -- 2026-06-03T19:40:20Z -- `claude`

**Touched files (intersect with thread paths):**

- `.github/workflows/inventory-phase5-proof.yml`

**Message:**

```
ci(inventory phase5): trim surrounding whitespace on admin email secret

Run #7 returned invalid_credentials (the admin email+password don't match a real
Supabase user). Belt-and-suspenders: strip leading/trailing spaces from the email
secret too (emails have no interior spaces) so a paste artifact can't be the
cause. No trigger flag — the real fix is owner-side: the admin account must exist
with that exact email+password and role='admin'.

https://claude.ai/code/session_017GpaRnmUo2Nf8iyNbhQtJM
```
