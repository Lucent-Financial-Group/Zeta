# PR #9944 -- docs(prior-art): trim two over-claims — one verified absent, one self-corrected

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 9944 |
| Title | docs(prior-art): trim two over-claims — one verified absent, one self-corrected |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-08-01T23:40:22Z |
| Merged at | 2026-08-01T23:55:34Z |
| Merge commit SHA | `87b9e81d5ae3445415de10e71d25049b60d020f9` |
| Branch | `shadow/trim-two-prior-art-overclaims` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/9944 |
| Changed files | 1 |
| Additions / deletions | +16 / -5 |

## Description

Both surfaced by Soraya's whitewash ruling and the falsification round on it. Both **verified against the code**, not accepted on report.

## L179 — a primitive described but not present

The entry says the `Resolution`/unum primitive "tracks the meaningful bits of belief (`maxed`, `needsMoreBits`)". It doesn't — it isn't there.

```
rg 'maxed|needsMoreBits|Resolution' src/Core.TypeScript/tri-boolean-float/   -> 0 hits
fromValue(0)       -> isHeld=false decode.ok=true
fromValue(1)       -> isHeld=false decode.ok=true
fromValue(15.9375) -> isHeld=false decode.ok=true
fromValue(255)     -> isHeld=false decode.ok=true
```

The shipped TriBoolean Float's `decode` is **all-or-nothing** — `ok(number)` or one of two superposed states — so a carrier one trit short of resolution is indistinguishable from one with nothing resolved. There is no graded resolution quantity in the codomain.

**This is load-bearing, not cosmetic.** Aaron proposed a self-describing scalar as the escape from the whitewash impossibility, and that escape needs resolution to be *graded* and *non-forgeable*. It is neither: all-or-nothing decode, and `fromValue` mints a fully-resolved carrier for any representable value.

Now marked **INTENDED, NOT BUILT** so the next reader doesn't plan against an absent primitive — the exact slot→assumed-present drift named in today's template research, caught in the anchor list itself.

## L470 — Soraya trimming her own earlier claim

Coverage-at-τ "makes whitewashing unprofitable" → it makes **sandbagging** unprofitable (the `(u−l)` width term penalises `u=+∞`), which is real and shipped. It does **not** close the whitewash window: the interval score is still a function of history alone, so a fresh identity still lands at the prior.

Neither correction weakens an anchor. Both make the list say what's true.

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 0 |
| Resolved threads | 0 |
| Unresolved threads | 0 |
| Total review comments | 0 |
| Total fix commits (touching thread paths) | 0 |

## Review threads

(no review threads on this PR)

## Fix commits (touching thread paths)

(no commits in this PR touched files anchored by review threads)
