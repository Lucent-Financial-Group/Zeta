# PR #825 -- tools/lint: no-directives-otto-prose advisory lint (Amara round-7 lexeme guard, diff-based)

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 825 |
| Title | tools/lint: no-directives-otto-prose advisory lint (Amara round-7 lexeme guard, diff-based) |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-04-29T07:31:21Z |
| Merged at | 2026-04-29T08:16:25Z |
| Merge commit SHA | `654460f751aac5dc7c3cc1cb5429573781dee91f` |
| Branch | `tools/lint-no-directives-provenance-2026-04-29` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/825 |
| Changed files | 2 |
| Additions / deletions | +380 / -0 |

## Description

## Summary

Tiny enforcement patch under Amara's B-0105 carve-out: a diff-based advisory lint that flags Otto-authored prose using "directive" framing for maintainer input.

**Why now:** the directive-language drift has fired ~15 times throughout the session arc. Vigilance has failed; lint is the durable answer (Amara round-7).

**Per Amara's lexeme-guard naming distinction:** this is a lexeme guard (stops repeated wording drift), NOT a lane lock (stops classes of work).

## Design

- **Diff-based scope** (does not retrofit historical content): only checks files changed vs `origin/main`.
- **Otto-prose surfaces only**: `memory/*.md`, `docs/hygiene-history/ticks/**/*.md`, `docs/research/*.md`, `.github/copilot-instructions.md`.
- **Whitelist**:
  - blockquote-quoted text (`> ...` lines)
  - rule-documentation files where "directive" appears legitimately
- **Pattern** (Amara's narrowed regex): `\b(Aaron's|maintainer|QoL|human).*directive\b` etc.
- **Modes**: `advisory` (default, warn-only) and `--strict` (fail on hits).

## Future work (under follow-up consolidation)

- Wire into `.github/workflows/gate.yml` as advisory check.
- Promote to `--strict` after low-noise validation period.

## Test plan

- [x] Local advisory run on this branch: clean (only changed file is the lint itself, which is whitelisted).
- [x] Pattern verified by manual inspection.
- [x] Whitelist covers existing `directive`-using rule files.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | true |
| Total threads | 18 |
| Resolved threads | 18 |
| Unresolved threads | 0 |
| Total review comments | 18 |
| Total fix commits (touching thread paths) | 4 |

## Review threads

### Thread 1 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5-WwGg`
- **Path**: `tools/lint/no-directives-otto-prose.sh`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-04-29T07:34:13Z on `tools/lint/no-directives-otto-prose.sh`:91 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Limit scan to added diff lines**

  This loop greps entire files instead of the added/modified hunks, so any pre-existing phrase like `"Aaron's directive"` in a touched file is reported even when the commit did not introduce it. That contradicts the script’s own “does not retrofit historical content” behavior and will generate noisy/inactionable hits (and false failures in `--strict`) whenever a file with legacy text is edited for unrelated reasons.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 2 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5-WxPD`
- **Path**: `tools/lint/no-directives-otto-prose.sh`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-29T07:35:34Z on `tools/lint/no-directives-otto-prose.sh`:86 (association: NONE)

  <pre>
  `mktemp` is invoked without an explicit template. This is known to fail on BSD/macOS in some configurations (the repo already uses `mktemp -t &lt;name&gt;.XXXXXX` for portability). Consider switching both temp-file creations to an explicit template so the lint runs consistently on macOS and Linux.
  </pre>

### Thread 3 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5-WxPg`
- **Path**: `tools/lint/no-directives-otto-prose.sh`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-29T07:35:34Z on `tools/lint/no-directives-otto-prose.sh`:57 (association: NONE)

  <pre>
  `git diff` errors are currently suppressed (`2&gt;/dev/null || true`), which can silently turn “couldn’t resolve/fetch BASE_REF” into “no changed files; skipping” and make CI/local runs look clean when nothing was actually checked. It would be safer to fail (or at least warn loudly) when `git diff` can’t compute the range, and optionally include renames in the diff-filter so renamed prose files are still scanned.
  ```suggestion
  if ! CHANGED_FILES=$(git diff --name-only --diff-filter=AMR "$BASE_REF...HEAD"); then
    echo "no-directives-otto-prose: failed to compute changed files vs $BASE_REF; ensure the base ref exists locally or set BASE_REF correctly" &gt;&amp;2
    exit 1
  fi
  ```
  </pre>

### Thread 4 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5-WxP1`
- **Path**: `tools/lint/no-directives-otto-prose.sh`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-29T07:35:34Z on `tools/lint/no-directives-otto-prose.sh`:190 (association: NONE)

  <pre>
  The grep scan suppresses stderr and forces success (`2&gt;/dev/null ... || true`), which hides real failures like an invalid regex or an unsupported grep option. Consider treating grep exit status 2 as a hard error (and only allowing status 1 as “no matches”), so the lint can’t report “clean” when it didn’t actually run correctly.
  </pre>

### Thread 5 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5-WxQA`
- **Path**: `tools/lint/no-directives-otto-prose.sh`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-29T07:35:34Z on `tools/lint/no-directives-otto-prose.sh`:11 (association: NONE)

  <pre>
  This script introduces repeated named attribution (“Aaron”, “Amara”) in a current-state tooling surface. Repo convention is to use role-refs on current-state surfaces and reserve direct names for the documented history/research surfaces (docs/AGENT-BEST-PRACTICES.md). Please rephrase comments/user-facing strings to role-refs (e.g., “human maintainer”) to keep the surface-policy consistent.
  </pre>

### Thread 6 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5-WxQL`
- **Path**: `tools/lint/no-directives-otto-prose.sh`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-29T07:35:35Z on `tools/lint/no-directives-otto-prose.sh`:82 (association: NONE)

  <pre>
  PATTERN uses `\b` word-boundaries with `grep -E`, but BSD grep (macOS default) / POSIX ERE don’t honor `\b` (it’s treated as a literal backspace), so this lint can miss intended matches. Use the same portable boundary approach as `tools/lint/runner-version-freshness.sh` (explicit non-word start/end classes), or switch to a tool that guarantees PCRE word boundaries (with a portability plan).
  ```suggestion
  WORD_START='(^|[^[:alnum:]_])'
  WORD_END='($|[^[:alnum:]_])'
  PATTERN="${WORD_START}(Aaron'?s|maintainer|QoL|human).*directive${WORD_END}|${WORD_START}directive.*(Aaron|maintainer|QoL|human)${WORD_END}"
  ```
  </pre>

### Thread 7 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5-W2bf`
- **Path**: `tools/lint/no-directives-otto-prose.sh`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-04-29T07:41:38Z on `tools/lint/no-directives-otto-prose.sh`:89 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Fail when BASE_REF diff cannot be computed**

  If `git diff "$BASE_REF...HEAD"` fails (for example, shallow/local clones without `origin/main` or a typoed `BASE_REF`), the `|| true` here suppresses the error and leaves `CHANGED_FILES` empty, which then exits as a clean skip. That silently disables the lint in exactly the environments where ref wiring is broken, so CI/pre-commit can report success without having scanned anything.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 8 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5-W2bi`
- **Path**: `tools/lint/no-directives-otto-prose.sh`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-04-29T07:41:38Z on `tools/lint/no-directives-otto-prose.sh`:124 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Scan staged snapshot in worktree mode**

  In `SCOPE=worktree`, staged paths are included in `CHANGED_FILES`, but matches are searched from the current working-tree file contents. This misses violations when a bad phrase is staged and then edited out only in unstaged changes (a common partial-staging flow), so the hook can pass while the commit still contains prohibited text.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 9 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5-W6A2`
- **Path**: `tools/lint/no-directives-otto-prose.sh`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-04-29T07:45:58Z on `tools/lint/no-directives-otto-prose.sh`:196 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Fix blockquote whitelist parsing after grep -n**

  The blockquote whitelist is currently ineffective because `grep -n` prepends an extra `N:` field, but the filter only strips two fields (`file:line:`) before checking for a leading `&gt;`. As a result, a quoted line like `&gt; Aaron's directive ...` is left as `line:&gt; ...` and is still reported as a violation, creating persistent false positives on the exact quoted-text cases this whitelist is meant to skip.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 10 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5-W8OY`
- **Path**: `tools/lint/no-directives-otto-prose.sh`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-29T07:48:03Z on `tools/lint/no-directives-otto-prose.sh`:180 (association: NONE)

  <pre>
  P0: `grep -nE` prefixes each hit with the *ADDED_LINES_FILE line number*, but the subsequent blockquote filter assumes lines are formatted `path:line:content`. With `-n`, the awk `sub(/^[^:]*:[^:]*:/, "", content)` leaves `line:content`, so `&gt; ...` lines won’t be recognized/whitelisted correctly. Drop `-n` here (since you already embed `path:line:`), or adjust the downstream parsing to strip the extra field.
  ```suggestion
  grep -E "$PATTERN" "$ADDED_LINES_FILE" &gt; "$HITS_FILE"
  ```
  </pre>

### Thread 11 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5-W8O0`
- **Path**: `tools/lint/no-directives-otto-prose.sh`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-29T07:48:03Z on `tools/lint/no-directives-otto-prose.sh`:126 (association: NONE)

  <pre>
  P0: `grep` runs against lines formatted as `path:line:content`, but `PATTERN` isn’t anchored to the content field. If the *filename* contains `human`/`maintainer`/etc (common in `memory/feedback_human_...md`) and the added content contains an unrelated `directive` token (e.g., “compiler directive”), this can false-positive by matching across the `path:line:` prefix. Consider stripping the prefix before grepping, or anchoring the regex with something like `^[^:]+:[0-9]+:` and applying the directive-pattern only to the content portion.
  ```suggestion
  # Match only against the content field of `path:line:content`
  # records so filename tokens like `feedback_human_...` cannot
  # combine with unrelated added content to false-positive.
  CONTENT_PATTERN='(^|[^[:alnum:]_])(Aaron'\''?s|maintainer|QoL|human)[^|]*directive([^[:alnum:]_]|$)|(^|[^[:alnum:]_])directive[^|]*(Aaron|maintainer|QoL|human)([^[:alnum:]_]|$)'
  PATTERN="^[^:]+:[0-9]+:${CONTENT_PATTERN}"
  ```
  </pre>

### Thread 12 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5-W8O_`
- **Path**: `tools/lint/no-directives-otto-prose.sh`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-29T07:48:03Z on `tools/lint/no-directives-otto-prose.sh`:172 (association: NONE)

  <pre>
  P1: The awk diff parser increments `lineno` for any `^[^+-]` line. In `git diff` output this includes lines like `\ No newline at end of file`, which are not real file lines and will skew the computed line numbers for subsequent `+` lines in the hunk. It’d be safer to ignore `^\\` marker lines (and ideally only advance `lineno` on context lines, not all non +/- lines).
  </pre>

### Thread 13 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5-W8PK`
- **Path**: `tools/lint/no-directives-otto-prose.sh`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-29T07:48:04Z on `tools/lint/no-directives-otto-prose.sh`:34 (association: NONE)

  <pre>
  P2: The portability header says the script “Uses GNU-grep `\b` word boundaries”, but the implementation explicitly avoids `\b` (and even documents why). This comment block should be updated so it matches what the script actually does, otherwise future edits may “fix” the code back toward `\b` and reintroduce the portability issue.
  ```suggestion
  #   - Intentionally avoids grep `\b` word boundaries (not portable);
  #     uses a portable boundary pattern instead.
  ```
  </pre>

### Thread 14 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5-W8PX`
- **Path**: `tools/lint/no-directives-otto-prose.sh`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-29T07:48:04Z on `tools/lint/no-directives-otto-prose.sh`:194 (association: NONE)

  <pre>
  P0: The blockquote whitelist logic here still assumes `file:line:content`, but with the current `grep -nE` output it’s actually `addedFileLine:file:line:content`. Either remove `grep -n` upstream or change this to strip *three* `:`-separated fields before testing for `^[[:space:]]*&gt;` in the remaining content.
  ```suggestion
  # `grep -nE` prepends a hit line number to the existing
  # "path:N:content" records, so hits are formatted as
  # "addedFileLine:path:line:content". Match against the content
  # portion only by stripping the first three `:`-separated fields.
  awk -F: '{
    # Reconstruct content after the third colon
    # (addedFileLine:file:line:content).
    content = $0
    sub(/^[^:]*:[^:]*:[^:]*:/, "", content)
  ```
  </pre>

### Thread 15 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5-W8Pn`
- **Path**: `tools/lint/no-directives-otto-prose.sh`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-29T07:48:04Z on `tools/lint/no-directives-otto-prose.sh`:173 (association: NONE)

  <pre>
  P1: The `|| true` here suppresses awk/pipeline failures, which undermines the earlier goal of not silently skipping checks when something goes wrong. If awk exits non-zero (e.g., due to an unexpected hunk header format), this will currently be treated as success and the lint may report clean incorrectly.
  </pre>

### Thread 16 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5-W8Px`
- **Path**: `tools/lint/no-directives-otto-prose.sh`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-29T07:48:04Z on `tools/lint/no-directives-otto-prose.sh`:19 (association: NONE)

  <pre>
  P1: This tool source embeds contributor/persona first-names (e.g., in comments and user-facing output). Per `docs/AGENT-BEST-PRACTICES.md` “No name attribution in code, docs, or skills” (current-state surfaces use role-refs), tooling should avoid named attribution and use role-refs like “the human maintainer” / “boundary reviewer” instead (names are reserved for the documented history surfaces list).
  </pre>

### Thread 17 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5-W8QD`
- **Path**: `tools/lint/no-directives-otto-prose.tests.md`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-29T07:48:05Z on `tools/lint/no-directives-otto-prose.tests.md`:44 (association: NONE)

  <pre>
  P1: These fixtures include contributor/persona first-names on a current-state tooling surface. Per `docs/AGENT-BEST-PRACTICES.md`’s named-attribution rule (names reserved for history surfaces like `memory/**`, `docs/research/**`, etc.), consider rewriting the examples to use role-refs (“the human maintainer’s input …”) while still exercising the same regex/whitelist behavior.
  </pre>

### Thread 18 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM5-W8QS`
- **Path**: `tools/lint/no-directives-otto-prose.sh`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-29T07:48:05Z on `tools/lint/no-directives-otto-prose.sh`:98 (association: NONE)

  <pre>
  P1: `--diff-filter=AM` excludes renames (`R`). If a prose file is renamed *and* edited in the same change, it won’t be included in `CHANGED_FILES`, so new violations in the renamed file would be missed. Consider including `R` (e.g., `--diff-filter=AMR`) or omitting the filter and letting the later `-f`/path filters handle existence/scope.
  ```suggestion
        git diff --name-only --diff-filter=AMR
        git diff --cached --name-only --diff-filter=AMR
        git diff --name-only --diff-filter=AMR "$BASE_REF...HEAD"
      } | sort -u
    )
  else
    CHANGED_FILES=$(git diff --name-only --diff-filter=AMR "$BASE_REF...HEAD")
  ```
  </pre>

## Fix commits (touching thread paths)

### `1d86e0cf8da4c6f19fb8a71f51cd9f931a4aaf0c` -- 2026-04-29T07:31:02Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/lint/no-directives-otto-prose.sh`

**Message:**

```
tools/lint: no-directives-otto-prose advisory lint (Amara round-7 lex…

…eme guard)

Vigilance failed ~15 times to keep "directive" out of Otto-authored
prose framing for maintainer input. Per Amara's round-7 catch:

  "A word that slips fifteen times is not a word problem.
   It is a missing lint."

Per Amara's B-0105 carve-out:

  "Tiny enforcement patches are allowed when they directly prevent
   repeated consolidation-gate violations."

This is exactly that: tiny, targeted, advisory. Diff-based scope
(does not retrofit historical content) — only flags Otto-authored
prose changes vs origin/main on:
  - memory/*.md (top-level)
  - docs/hygiene-history/ticks/**/*.md
  - docs/research/*.md
  - .github/copilot-instructions.md

Pattern is narrowed (Amara's regex):
  "Aaron's directive" / "maintainer directive" / "QoL directive" /
  "human directive" / "directive" near maintainer-token

Whitelist:
  - blockquote-quoted text (`> ...` lines)
  - rule-documentation files where "directive" appears legitimately

Default mode is advisory (warn-only). --strict flag fails on hits.

Per the lexeme-guard naming distinction (Amara round-7):
  Lane locks stop classes of work.
  Lexeme guards stop repeated wording drift.
This is a lexeme guard, not a lane lock.

Future work (under follow-up consolidation):
  - wire into .github/workflows/gate.yml as advisory check
  - promote to --strict after low-noise validation period

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
```

### `5df5171d90e553c712bbc0990d4cb0ec9f401cad` -- 2026-04-29T07:37:56Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/lint/no-directives-otto-prose.sh`
- `tools/lint/no-directives-otto-prose.tests.md`

**Message:**

```
fix(no-directives-lint): add SCOPE=worktree mode + test fixtures + po…

…rtability honesty (Amara round-8)

Five Amara round-8 corrections applied:

1. Added SCOPE=worktree mode for local pre-commit use.
   Default SCOPE=pr is the original PR-diff mode. Worktree
   mode unions unstaged + staged + committed-not-pushed
   changes. Without this, the script could not catch local
   edits before commit (which is exactly what the prose
   originally claimed it could do).

2. Added test fixtures at tools/lint/no-directives-otto-prose.tests.md
   covering: must-flag cases (including the canonical
   paired-edit-HTML-comment proof case from PR #823); whitelist
   cases (blockquoted text, filename citations, historical-rule
   discussion); boundary cases (advisory-judgment territory).

3. Honesty about portability: this is Bash + GNU-grep
   oriented, NOT POSIX. Docs updated. Targets: Linux CI +
   the 4-shell developer target.

4. Confirmed HTML comments + paired-edit markers ARE in
   scope (they're the proof-case that motivated this lint).
   No exclusion for them.

5. Wiring into CI: still advisory-not-blocking, deferred
   to next-step backlog. Per Amara: "advisory CI before
   strict required check."

Best Amara keeper:
  "The regex was not the breakthrough.
   The scope was.
   The next breakthrough is wiring."

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
```

### `43b0139868f5b73be46427ac210ea6fb0efc3a69` -- 2026-04-29T07:42:00Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/lint/no-directives-otto-prose.sh`

**Message:**

```
fix(no-directives-lint): 6 reviewer fixes — diff-hunk scoping, mktemp…

… template, error propagation, portable boundary, role-refs

Six reviewer findings on PR #825 fixed:

1. Diff-hunk scoping (P2): replaced whole-file grep with parsing
   of git diff -U0 added-lines (^+ but not ^+++). Pre-existing
   "directive" prose in a touched file no longer flags; only
   newly-added/modified lines do.

2. mktemp portability: replaced bare mktemp with `mktemp -t
   no-directives.XXXXXX` (BSD/macOS-portable form, matches
   tools/lint/runner-version-freshness.sh precedent).

3. git-diff error propagation: removed `2>/dev/null || true`
   suppression. If BASE_REF can't be resolved, fail loudly
   instead of silently passing.

4. grep error propagation: distinguish exit status 2 (invalid
   regex / unsupported flag — hard error) from status 1 (no
   match — fine). Was suppressed and forced-success.

5. Portable word boundary: replaced `\b` (BSD grep treats as
   literal backspace; not POSIX-portable) with explicit
   `(^|[^[:alnum:]_])...([^[:alnum:]_]|$)` boundary. Matches
   tools/lint/runner-version-freshness.sh precedent.

6. Named-attribution carve-out: tooling-surface comments
   replaced direct names with role-refs ("the maintainer's
   directive" / "the no-directives autonomy rule" / cross-link
   to memory file). Persona-name attribution stays on history
   surfaces (memory files, commit messages, tick shards) per
   docs/AGENT-BEST-PRACTICES.md §named-attribution-carve-out.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
```

### `b30d5d0498d65b10f3acbee35dd763675e737f37` -- 2026-04-29T08:13:00Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/lint/no-directives-otto-prose.sh`
- `tools/lint/no-directives-otto-prose.tests.md`

**Message:**

```
fix(no-directives-lint): round-12 — diff-parser rewrite (FILE<TAB>CON…

…TENT) + missing fixtures

Round-12 review (12 unresolved threads, 3 P0 + 5 P1 + 4 P2/outdated)
addressed via two coordinated changes:

1. Diff-parser rewrite (.sh) — TAB-delimited FILE<TAB>CONTENT stream

   The prior implementation grepped against "path:line:content" output
   from `grep -nE`, which created two real bugs:

   - PATTERN matched filename substrings (e.g. `feedback_human_lineage_*.md`
     produced false positives on the "human" token in the regex even when
     content was clean).
   - The blockquote whitelist + awk lineno tracker assumed the wrong
     field-count/format after `-n` was added, silently breaking both
     filters.

   New shape: per-file `git diff -U0` is parsed by awk into a single
   `FILE\tCONTENT` line per added hunk-line. TAB is safe because no
   prose paths under memory/, docs/, .github/ contain literal TABs.
   Pattern-matching now runs against the CONTENT field only, killing
   the filename-substring false-positive class entirely. Diff metadata
   (\\ No newline, @@, ---, +++, -lines) is filtered before emission.
   `|| true` removed from awk so genuine awk failures propagate. PATTERN
   uses generic `[A-Z][a-z]+'s` rather than literal `Aaron's`.

   Filter changed from `--diff-filter=AM` to `--diff-filter=AMR` so that
   renamed prose files with new violations are included in CHANGED_FILES.

   Portability docstring updated to match implementation (no `\b`;
   POSIX-portable explicit non-alpha boundaries).

   Persona names removed from script comments per named-attribution
   carve-out (tooling-surface comments use role-refs; persona names
   belong on history surfaces — research notes, memory files, commit
   messages, tick shards).

2. Test fixtures (.tests.md) — framing note + 2 missing fixtures

   Added "Test-input vs authorial register" framing note explaining why
   fixtures retain canonical drift strings ("Aaron's directive") rather
   than substituting role-refs. Test-input register IS the data the lint
   detects; substituting "the maintainer's directive" would test a
   different regex alternative and silently lose coverage of the
   `[A-Z][a-z]+'s + directive` shape (the canonical real-world drift).

   Added fixture: renamed file with new violation in renamed copy
   (covers --diff-filter=AMR).

   Added fixture: filename contains regex token (`human`) but added
   content is clean — must NOT flag (covers the round-12 P0 false-
   positive fix).

Verified bash -n clean; both PR-mode and SCOPE=worktree lint runs
report "no Otto-prose surfaces changed; skipping" on this commit
(tooling-only — tests.md is whitelisted via the
`no-directives-otto-prose` substring match in the file-filter).

Closes-threads: 3 P0 (grep-format/anchoring/blockquote), 5 P1
(awk-lineno/error-suppression/role-refs/--diff-filter-AM/named-attribution),
4 P2/outdated (BASE_REF-fail/staged-snapshot/blockquote-whitelist-parsing/
docstring-portability).

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
```
