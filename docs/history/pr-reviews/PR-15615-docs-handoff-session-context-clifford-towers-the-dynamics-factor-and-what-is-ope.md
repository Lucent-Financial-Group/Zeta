# PR #15615 -- docs(handoff): session context — Clifford towers, the dynamics factor, and what is open

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 15615 |
| Title | docs(handoff): session context — Clifford towers, the dynamics factor, and what is open |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-08-26T12:15:45Z |
| Merged at | 2026-08-26T12:29:59Z |
| Merge commit SHA | `09f0d731518f91e04e5c4b5e365e33b63d50c4af` |
| Branch | `claude/tender-hawking-xmcs6m` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/15615 |
| Changed files | 1 |
| Additions / deletions | +249 / -0 |

## Description

# Summary

A handoff document for a session picking this thread up cold — written after
[#15517](https://github.com/Lucent-Financial-Group/Zeta/pull/15517) and
[#15565](https://github.com/Lucent-Financial-Group/Zeta/pull/15565) landed, so the next reader
starts from what is true rather than from the transcript.

Lands on `main` rather than living on a branch because a branch link is fragile — GitHub
auto-deleted this branch once already today when #15565 merged, which would have taken the link
with it.

One file. No code.

# Spec alignment

No capability under `openspec/specs/` changes. Documentation only.

# Tests

None applicable — a single markdown file, no code paths touched. The claims it makes are
restatements of results already merged and already carrying their own falsifiers:

| claim in the doc | where its falsifier lives |
|---|---|
| `Cl(4,1) ≅ M₂(Cl(3,0))`, `dim Cl(p,q,r)`, tower table | `src/Core.TypeScript/research/conformal-embedding-and-curvature-budget.test.ts` (30) |
| the dynamics factor's behaviour | `tests/Tests.FSharp/TravelerRankLedger.Tests.fs` TRL-34..41 |
| pr-archive 18/18 post-merge | `workitems/done/2026/08/081M0X93WA4087G0R0034C1A5Q-*` |

# Validation

- [x] Tests pass locally (0 warn, 0 err) — n/a, no code changed
- [x] Any new docstring claim has a falsifying test — n/a; the doc cites existing ones, adds none
- [x] `openspec/specs/**` updated if observable behaviour changed — n/a
- [ ] `docs/ROUND-HISTORY.md` entry if notable
- [x] Skill changes went through the `skill-creator` workflow — n/a

# Notes

**What it covers.** What landed and what it measured; the five open work items and which are
blocked (Q3 of the Clifford hold is the one that gates spatial belief in a BNN column); the
standing constraints; the register split — including the flat statement that
`grep -ril reservoir src/` finds **no reservoir implementation**, so the capacity claim stays
`toy`; and both coincidences kept labelled as coincidences.

**§6 is the part with the most value per line** — nine gotchas, eight of them mistakes made and
backed out of during this session:

- `Human-Review-Evidence` is an **enum**; 13 commits carried free prose.
- `Accountable-Party` / `Authority-Basis` do **not** reconcile across a squash.
- Validate the **squash preimage**, not just the PR body — the body passed while the commits were
  red.
- `if git push ... | tail -2` tests *tail's* exit status. A check that cannot fail.
- **A rebase drops merge commits, including fixes made inside them** — a ruff repair vanished
  that way and `lint (Python)` went red.
- Never run overlapping background jobs mutating one file: a break-red reported **41/41 with a
  mutation in place** because the compile raced the edit. Discarded, re-run synchronously.
- Mint work-item ids **before** writing them anywhere — a well-formed ZetaId that identifies
  nothing is the exact failure `never-assume-malice-where-mistake-is-possible.md` documents.
- `main` goes red sometimes and it is **not always yours**; check a clean worktree and look for an
  in-flight fix before duplicating one.
- Research tests under `src/Core.TypeScript/research/` are not auto-discovered. Three siblings are
  still unrun: `gromov-hyperbolicity`, `belief-manifold-curvature-sybil`,
  `causal-order-minkowski-embedding`.

**Also flagged, deliberately untouched:** the `agent-heartbeat` repair writer has stopped landing
(`manifest.jsonl` behind the shards; the workflow still reports success because the derive step is
`|| echo "::warning::..."`), and 5 Dependabot vulnerabilities sit on the default branch.

**Merge, not rebase.** The branch was one commit behind after
[#15612](https://github.com/Lucent-Financial-Group/Zeta/pull/15612); un-updated this PR would have
proposed **deleting 247 lines** of that work. A rebase needs a force-push, which is gated here, so
`origin/main` was merged in. That merge decides no content and therefore carries **no**
AgencySignature block — silence asserts nothing, which is the honest record
(`maintenance-commit-on-another-agents-branch-carries-no-block.md`). The squash preimage was
validated locally before pushing: 1 block, 0 invalid, disagreement `NONE`.

Agency-Signature-Version: 1
Agent: otto (interactive Claude session)
Agent-Runtime: Claude Code (remote container)
Agent-Model: Claude
Credential-Identity: Claude via repo-scoped git proxy
Credential-Mode: unknown
Human-Review: explicit
Human-Review-Evidence: chat
Action-Mode: human-directed
Task: none
Accountable-Party: acehack
Authority-Basis: standing-grant
Co-authored-by: Claude

---
_Generated by [Claude Code](https://claude.ai/code/session_01UNRXBqHjAKNK6QzuLcem49)_

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 0 |
| Resolved threads | 0 |
| Unresolved threads | 0 |
| Total review comments | 0 |
| Total fix commits (touching thread paths) | 0 |

## Review threads

(no review threads on this PR)

## Fix commits (touching thread paths)

(no commits in this PR touched files anchored by review threads)
