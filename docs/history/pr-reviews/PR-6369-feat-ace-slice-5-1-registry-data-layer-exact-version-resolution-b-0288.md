# PR #6369 -- feat(ace): slice 5.1 — registry data layer + exact-version resolution (081KR2E4K0008QG0R002YE3MMD)

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 6369 |
| Title | feat(ace): slice 5.1 — registry data layer + exact-version resolution (081KR2E4K0008QG0R002YE3MMD) |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-06-01T08:01:10Z |
| Merged at | 2026-06-01T08:20:33Z |
| Merge commit SHA | `908fa87d48e2d62e6bf1efe8c947dba566a9bc6f` |
| Branch | `otto-windows/ace-slice5.1-impl-2026-06-01` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/6369 |
| Changed files | 9 |
| Additions / deletions | +1041 / -53 |

## Description

## Ace slice 5.1 — registry data layer + exact-version resolution (081KR2E4K0008QG0R002YE3MMD)

A manifest dependency can now name a package by `name + version` and let a local **registry** supply `{url, package_hash}`, instead of inline-pinning them. `ace install` looks the dep up (exact version) and runs the **identical** slice-4 verify + atomic-install path.

### What changed
- **`AceDependency` is now an explicit DU** (per `implicit-not-explicit-in-DUs`): `{kind:"inline", name, version, url, package_hash}` (self-pinned, slice-4) `|` `{kind:"registry", name, version}` (resolved via registry).
- **Registry data layer** (`store.ts`): bundled (`tools/ace/registry.json`, ships `{}`) ∪ user (`~/.ace/registry.json`), user overrides — mirrors the trust-store union exactly. `loadRegistry` / `listRegistry` (source-tagged) / `addRegistryEntry` (dir `0o700` / file `0o600` + dedup by name+version, mirroring `addTrustedKey`).
- **Resolver** (`resolve.ts`): gains an injected `registry` param; per-edge `kind` dispatch — a registry edge looks up `{url, package_hash}` (exact-version; a miss is the new `registry-miss` refusal), then flows into the **same** content-hash + full-package-hash-pin + identity-keying + signature/trust checks as an inline edge. No verify bypass for registry edges.
- **CLI** (`ace.ts`): `ace registry add <name> <version> <url> [--hash <h>]` (fetches/reads + computes `packageHash` unless `--hash` given; flag-injection guards) and `ace registry list`; install passes `loadRegistry()` into `resolve`.
- **Docs**: `.claude/skills/ace/SKILL.md` registry verbs + registry-dep resolution.

### Verification
- 120 tests pass (`bun test tools/ace/`), 0 fail.
- Strict `tsc --noEmit` clean for `tools/ace/`.
- markdownlint clean on changed docs.
- Atomicity preserved: registry-miss / any verify failure → exit 1, store empty (e2e test).
- Final opus code review: **APPROVED — no P0/P1**; the one actionable P2 (`--hash` leading-dash guard) is fixed in this PR.

### Not in scope (deferred)
- Semver ranges + constraint solver → **slice 5.2** (Z3-SMT candidate + differential-test vs a reference solver).
- Lockfile (solved graph) → **slice 5.3**.

Spec: `docs/agendas/ace-package-manager/2026-06-01-ace-cli-slice5.1-registry-exact-version-design.md` (#6362).
Plan: `docs/agendas/ace-package-manager/2026-06-01-ace-cli-slice5.1-implementation-plan.md`.

🤖 Generated with [Claude Code](https://claude.com/claude-code)


## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | true |
| Total threads | 6 |
| Resolved threads | 6 |
| Unresolved threads | 0 |
| Total review comments | 12 |
| Total fix commits (touching thread paths) | 11 |

## Review threads

### Thread 1 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GC3tW`
- **Path**: `tools/ace/store.ts`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-06-01T08:03:44Z on `tools/ace/store.ts`:289 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Allow registry entries to be updated**

  When a user already has `~/.ace/registry.json` entry for the same `name@version`, rerunning `ace registry add` with a corrected URL or package hash silently takes this early return and leaves the stale pin in place. In that scenario exact-version registry dependencies keep resolving to the old URL/hash and continue to fail with fetch or `pin-mismatch`, even though the CLI reports only “already registered”; the helper comment also says this path should append/overwrite. Please overwrite the stored entry when the supplied `url` or `package_hash` differs, or fail loudly with instructions to edit/remove the entry.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-06-01T08:07:34Z on `tools/ace/store.ts`:289 (association: MEMBER)

    <pre>
    Fixed in 31c662ee0. `addRegistryEntry` now overwrites the stale pin when re-adding the same `name@version` with a differing `url`/`package_hash` (returns `updated:true`); an identical re-add stays an idempotent no-op (`added:false, updated:false`). The CLI reports registered / updated / already-registered accordingly, and the helper comment was corrected to match. New store.test covers both the overwrite-on-differ and identical-noop paths.
    </pre>

### Thread 2 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GC7te`
- **Path**: `tools/ace/store.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-01T08:07:13Z on `tools/ace/store.ts`:283 (association: NONE)

  <pre>
  The JSDoc says this function "Append/overwrite"s a registry entry, but the implementation dedups by (name, version) and does not overwrite an existing entry (it early-returns added:false). Please align the comment with actual behavior (or change the code to support overwrite).
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-06-01T08:11:32Z on `tools/ace/store.ts`:283 (association: MEMBER)

    <pre>
    Addressed in 31c662ee0 — addRegistryEntry now overwrites the stale pin on a differing url/package_hash (the JSDoc and behavior are aligned: identical re-add = idempotent no-op, differing = overwrite). This duplicates the Codex thread already resolved above.
    </pre>

### Thread 3 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GC7uP`
- **Path**: `tools/ace/resolve.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-01T08:07:13Z on `tools/ace/resolve.ts`:71 (association: NONE)

  <pre>
  `edge.url` / `edge.package_hash` are untrusted JSON fields. For malformed dependency edges (e.g., missing `url` on an inline edge, or an unknown `kind`), this code can currently pass `undefined` into `fetchPackage`, causing `ace install` to throw instead of returning an `invalid-package` refusal. Add a small runtime guard and reject unknown `kind` values while still allowing pre-kind inline edges (`kind` omitted) for back-compat.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-06-01T08:11:35Z on `tools/ace/resolve.ts`:71 (association: MEMBER)

    <pre>
    Fixed in 1574b1249. Added an explicit edge-shape guard at the top of the walk loop: an unknown `kind` now refuses with `invalid-package` instead of silently falling through to the inline branch, and an inline edge missing `url`/`package_hash` refuses with `invalid-package` (kind-omitted == inline back-compat preserved). New resolve.test cases cover both. (Note: the prior path didn't throw uncaught — the try/catch around fetchPackage refused it as fetch-failed — but the guard gives a precise reason + rejects unknown kinds.)
    </pre>

### Thread 4 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GC7ut`
- **Path**: `tools/ace/store.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-01T08:07:14Z on `tools/ace/store.ts`:291 (association: NONE)

  <pre>
  `addRegistryEntry` mutates a plain JS object keyed by user-controlled `name`/`version`. If `name` (or `version`) is `"__proto__"`/`"constructor"`, assigning into `obj[name]` can trigger prototype pollution semantics on normal objects. To avoid this, copy the parsed registry into null-prototype objects before mutating/writing.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-06-01T08:11:38Z on `tools/ace/store.ts`:291 (association: MEMBER)

    <pre>
    Fixed in 1574b1249. addRegistryEntry now builds a null-prototype working copy (Object.create(null) for the outer object and each inner versions object) before the bracket assignments, so a user-controlled `__proto__`/`constructor` name or version in an operator-edited/shared registry.json can't pollute Object.prototype. New store.test asserts no pollution for both `__proto__` and `constructor` names while the entries stay retrievable via the Map-based loader.
    </pre>

### Thread 5 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GDAQ7`
- **Path**: `tools/ace/ace.ts`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-06-01T08:11:15Z on `tools/ace/ace.ts`:395 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Store local registry paths as absolute paths**

  When `ace registry add` is run with a relative filesystem path, this stores that relative string in the user-global `~/.ace/registry.json`. The later install path resolves registry entries with `readFileSync(u, "utf8")` relative to the install command's current working directory, so a dependency registered from one directory fails with `fetch-failed` when installing from another. Normalize non-HTTP registry URLs to an absolute path before persisting them, or reject relative paths.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-06-01T08:13:48Z on `tools/ace/ace.ts`:395 (association: MEMBER)

    <pre>
    Fixed in 1f5ad50a3. `ace registry add` now normalizes a local (non-HTTP) path to absolute (`path.resolve`, aliased `toAbsolutePath` to avoid colliding with the package resolver) before computing the hash + persisting, so the user-global registry entry is cwd-independent; http(s) urls pass through unchanged. New ace.test asserts a relative add is stored as the absolute path.
    </pre>

### Thread 6 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GDETv`
- **Path**: `tools/ace/ace.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-01T08:14:40Z on `tools/ace/ace.ts`:394 (association: NONE)

  <pre>
  `ace registry add` computes `packageHash(pkg)` immediately after `JSON.parse`, but it doesn’t validate the parsed value is a well‑formed `AcePackage`. For parseable-but-malformed JSON (e.g., missing `manifest`/`files`), `packageHash` will throw and the CLI falls into the generic fatal handler instead of returning a clean error/exit code. Add a small runtime shape guard (and ideally verify the package’s declared name/version matches the CLI args) before hashing.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-06-01T08:16:03Z on `tools/ace/ace.ts`:394 (association: MEMBER)

    <pre>
    Fixed in the latest commit. registry add now shape-guards the parsed package before hashing (parseable-but-malformed package missing manifest/files → clean exit 65 instead of a bogus hash / fatal throw), and additionally verifies the package's declared name@version matches the CLI args (so a package can't be registered under the wrong name — mirrors the resolver's declared-identity check, caught here at add-time). New ace.test covers both refusals.
    </pre>

## Fix commits (touching thread paths)

### `c4b2ece9e9389651ce61d18ac3e8b848635e0e15` -- 2026-06-01T07:14:57Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/ace/store.ts`

**Message:**

```
feat(ace): AceDependency DU (inline|registry) + registry types/paths …

…+ bundled registry.json (slice 5.1 task 1)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `49454f71936a765ebf1ac309966f3df47d49328b` -- 2026-06-01T07:20:58Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/ace/store.ts`

**Message:**

```
feat(ace): loadRegistry union/override + listRegistry with source (sl…

…ice 5.1 task 2)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `681c6e17cc6ff5fa6172be2876e9aa56db3ab96a` -- 2026-06-01T07:26:01Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/ace/store.ts`

**Message:**

```
feat(ace): addRegistryEntry with trust-store perm discipline + dedup …

…(slice 5.1 task 3)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `9bd65edea770930fa15c5eeb35c111f65b3c6e38` -- 2026-06-01T07:37:26Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/ace/ace.ts`
- `tools/ace/resolve.ts`

**Message:**

```
feat(ace): resolver registry dispatch + registry-miss; migrate inline…

… edges to DU (slice 5.1 task 4)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `3a5c7e2989d9459b109af75e2bc6c4b1e8215080` -- 2026-06-01T07:50:02Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/ace/ace.ts`

**Message:**

```
feat(ace): ace registry add/list verbs + wire registry into install (…

…slice 5.1 task 5)

- Add RegistryArgs interface and extend ParsedArgs union with registry variant
- Add addRegistryEntry, listRegistry to store.ts import
- Implement registry parseArgs block (registry add <name> <version> <url> [--hash <h>], registry list)
- Implement registry handler in main: list shows rows, add fetches+hashes+stores
- Add kind:"inline" as const to all inline dep literals in ace.test.ts (tsc DU narrowing)
- Add loadRegistry import to test file
- Add three new registry tests: add+list, e2e registry-dep install, e2e missing registry dep
- loadRegistry() in resolve call was already present from Task 4 (verified)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `e1fe0a2b6d2dd60cbf94fa94e1e4064a3c7505ca` -- 2026-06-01T07:54:16Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/ace/ace.ts`

**Message:**

```
fix(ace): registry add parse — spread regHash only when defined (stri…

…ct tsc exactOptionalPropertyTypes)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `992be0385604a1e2612bf93e07c13ef5637713aa` -- 2026-06-01T07:58:44Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/ace/ace.ts`

**Message:**

```
fix(ace): guard registry add --hash value against leading-dash flag i…

…njection (P2 from final review)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `31c662ee0150747b6842baf0744c44eeb3749b03` -- 2026-06-01T08:07:15Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/ace/ace.ts`
- `tools/ace/store.ts`

**Message:**

```
fix(ace): registry add overwrites stale pin on corrected url/hash (Co…

…dex P2 #6369)

addRegistryEntry early-returned for any existing name@version, so re-running
'ace registry add' with a corrected url/package_hash silently no-op'd and left
the stale pin — the dep kept failing with fetch/pin-mismatch while the CLI said
'already registered'. Now: identical re-add stays an idempotent no-op
(added:false, updated:false); a differing url/hash overwrites the stale pin
(added:false, updated:true). CLI reports registered / updated / already-registered.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `1574b12497809aade2e1a2c00e14109613bcc0b5` -- 2026-06-01T08:11:18Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/ace/resolve.ts`
- `tools/ace/store.ts`

**Message:**

```
harden(ace): edge-shape guard + null-proto registry writes (Copilot #…

…6369)

- resolve.ts: untrusted dependency edges now get an explicit shape guard —
  unknown 'kind' is refused (invalid-package) instead of silently treated as
  inline; an inline edge missing url/package_hash refuses with invalid-package
  rather than surfacing later as a vague fetch-failed. kind-omitted == inline
  back-compat preserved.
- store.ts: addRegistryEntry builds a null-prototype working copy before its
  bracket assignments, so a user-controlled __proto__/constructor name or
  version in an operator-edited/shared registry.json cannot pollute
  Object.prototype. Entries stay retrievable via the Map-based loader.
- tests: unknown-kind + missing-url refusals; __proto__/constructor no-pollution.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `1f5ad50a33ff37f69fcdbb4d25f9b62a27335dee` -- 2026-06-01T08:13:37Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/ace/ace.ts`

**Message:**

```
fix(ace): registry add stores local paths absolute (cwd-independent i…

…nstall; Codex P2 #6369)

A relative filesystem path passed to 'ace registry add' was persisted verbatim
into the user-global ~/.ace/registry.json, so installing from a different cwd
resolved it against the wrong directory and failed with fetch-failed. Local
(non-HTTP) urls are now normalized to absolute (path.resolve, aliased
toAbsolutePath to avoid the package-resolver name) before fetch/hash + persist;
http(s) urls pass through. New ace.test asserts a relative add is stored absolute.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `df48073b1662db13d912d0ec13172747487fdbf1` -- 2026-06-01T08:15:58Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/ace/ace.ts`

**Message:**

```
harden(ace): registry add shape+identity guard before hashing (Copilo…

…t #6369)

packageHash was computed straight off JSON.parse output; a parseable-but-malformed
package (missing manifest/files) produced a bogus hash or threw into the generic
fatal handler. Added a shape guard (clean exit 65) plus a declared-identity check
(package name@version must match the CLI name/version, mirroring the resolver's
identity check but caught at add-time so a package can't be registered under the
wrong name). New ace.test cases cover both refusals.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```
