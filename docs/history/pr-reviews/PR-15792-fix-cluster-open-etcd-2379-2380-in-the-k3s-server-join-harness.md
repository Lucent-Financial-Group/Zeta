# PR #15792 -- fix(cluster): open etcd 2379/2380 in the k3s-server-join harness

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 15792 |
| Title | fix(cluster): open etcd 2379/2380 in the k3s-server-join harness |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-08-27T04:36:04Z |
| Merged at | 2026-08-27T04:54:45Z |
| Merge commit SHA | `cf4470266705d440f42690a77a5b641ddbb61f7e` |
| Branch | `cursor/k3s-server-join-etcd-fw-f618` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/15792 |
| Changed files | 1 |
| Additions / deletions | +98 / -2 |

## Description

<!-- CURSOR_AGENT_PR_BODY_BEGIN -->
# Summary

leftover UNIQUE leftover follow-up. leftover UNIQUE leftover #15746 leftover unique @ c1f0aff9 pinned `--node-ip` to the vlan so the two control planes stopped advertising the shared QEMU SLIRP `10.0.2.15`. That was necessary and not sufficient: `build-ai-cluster-iso` is still red on main. This PR opens etcd `2379`/`2380` in the **harness only** so the joiner can complete MemberAdd. Product `k3s-server.nix` firewall stays closed. leftover HOLD leftover-on-main #12252 is cited, not closed. Cite ≠ close.

# Diagnosis (measured)

Live: [build-ai-cluster-iso #1958](https://github.com/Lucent-Financial-Group/Zeta/actions/runs/33035015161) run `33035015161` @ `7424c15a` — job `build-iso` failed at step 13 (`NixOS test — a second CONTROL PLANE joins`); `build-iso-aarch64 + qemu-boot` green. Cascade "Locate ISO + capture metadata" is `find: 'result/iso': Not a directory` because the job already failed.

Verified, not assumed, from that run's logs (and the same shape after leftover UNIQUE leftover #15746 leftover unique @ c1f0aff9):

1. **`extraFlags` CONCATENATES**, it does not replace. Joiner kube-apiserver still carries `--cluster-cidr=10.143.0.0/17` and `--service-cluster-ip-range=10.99.192.0/19` from `k3s-server.nix`. This PR still switches the harness assignment to `lib.mkAfter` so that additive intent cannot drift back to a replace.

2. **`networking.primaryIPAddress` is the vlan**, not SLIRP. Measured:
   - `Adding member joiner-6aba2ae3=https://192.168.1.2:2380 to etcd cluster [founder-dce5ce45=https://192.168.1.1:2380]`
   - `Running kubelet … --node-ip=192.168.1.2`
   - founder `Serving HTTP bootstrap from datastore for 192.168.1.2:58276`
   - k3s helper text still mentions `https://10.0.2.15:6443` (default-route SLIRP); that is not the etcd peer URL after leftover UNIQUE leftover #15746 leftover unique @ c1f0aff9.

3. **The host firewall is the red.** Immediately after the MemberAdd line:
   ```
   refused connection: IN=eth1 SRC=192.168.1.2 DST=192.168.1.1 DPT=2379
   Retrying etcd cluster join: MemberAdd request timed out
   ```
   Repeated for the rest of the 420 s timeout. Joiner local `dial tcp 127.0.0.1:2379: connection refused` is the joiner's own etcd not up yet — not the founder refuse.

`full-ai-cluster/nixos/modules/k3s-server.nix` allows `6443`/`9345`/`10250` and INTENTIONALLY omits `2379`/`2380` ("embedded etcd binds 127.0.0.1 by default"). A `role=server` JOIN is etcd membership. After `--node-ip` points etcd at the vlan, the joiner must reach founder `:2379` (MemberAdd) and `:2380` (peer). Agent-join stays green because an agent never joins etcd.

# What this PR does

Harness-only, in `full-ai-cluster/nixos/tests/k3s-server-join.nix`:

- `lib.mkAfter [ 2379 2380 ]` on founder **and** joiner (`allowedTCPPorts`)
- `lib.mkAfter` for `--node-ip` so CIDR / `--tls-san` / `--disable` flags stay
- Non-vacuous assertion: founder is listening on `:2380`, and the joiner can `timeout 5 bash -c 'echo >/dev/tcp/$FOUNDER_IP/2379'` and `:2380` **before** the join starts — a closed firewall cannot look like a passing join
- Assert the two `--node-ip` values differ and are not `10.0.2.15`
- Pin `--node-ip` / `--cluster-cidr` / `--tls-san=control-plane` on the running cmdline so a replace cannot hide

# What this PR does not do

- Does **not** open `2379`/`2380` in product `k3s-server.nix`. The existing comment refuses a LAN-wide open. If hardware HA later needs those ports, they belong in a host-specific override that also scopes them.
- Does **not** decide the product multi-homed `--node-ip` question. That stays with `injected-cluster-address.nix` / `k3s-server.nix` as a named leftover.
- Does **not** touch `k3s-agent-join.nix` (agent-join is green; an agent never joins etcd).
- Does **not** add this check to `gate (required)` needs.
- Does **not** take 14-vs-70. Does **not** restore 14.
- Does **not** touch leftover HOLD leftover-on-main #12252. Cite ≠ close.

# Cite ≠ close

- leftover UNIQUE leftover #15746 leftover unique @ c1f0aff9 (`--node-ip` pin to vlan; did not green the lane)
- leftover UNIQUE leftover #15673 leftover unique @ 55bce4e8 leftover unique @ 2a083c9b (JOIN path + joining CP must not alias `control-plane` to `127.0.0.1`)
- leftover UNIQUE leftover #15668 leftover unique @ 4f7b95ee (injected-server-join + CIDRs + dirty-disk preflight; named leftover then: etcd 2379/2380 closed in `k3s-server.nix`)
- leftover HOLD leftover-on-main #12252

Sit leftover. Do not merge.

# Spec alignment

No `openspec/specs/` change. Test harness only; shipped module firewall and `--node-ip` product policy are unchanged.

# Tests

- `nix` evaluation of `full-ai-cluster` `k3s-server-join` (this environment; VM boot is CI's — no `/dev/kvm` assumed here)
- Falsifier: drop the harness `mkAfter [ 2379 2380 ]` and the `/dev/tcp` probes go red in seconds, the same refuse as run `33035015161`
- `k3s-agent-join` untouched

# Validation

- [x] Product `k3s-server.nix` firewall left closed
- [x] Harness-only `2379`/`2380` via `mkAfter`
- [x] Non-vacuous reachability assertion
- [x] extraFlags additive (`mkAfter`); merge already measured on leftover UNIQUE leftover #15746 leftover unique @ c1f0aff9 and again on run `33035015161`
- [x] primaryIP is vlan `192.168.1.1` / `192.168.1.2` (measured); asserted distinct and not SLIRP
- [ ] VM boot green is CI's to report (`build-iso` step 13)
- [x] No `openspec/specs/**` change
- [x] No skill-creator change

# Notes

`build-iso` step 13 is the lane. Do not promote to `gate (required)`. leftover HOLD leftover-on-main #12252 stays leftover HOLD.

Agency-Signature-Version: 1
Agent: riven
Agent-Runtime: cursor-cloud
Agent-Model: grok-4.6
Credential-Identity: AceHack
Credential-Mode: shared
Human-Review: explicit
Human-Review-Evidence: chat
Action-Mode: human-directed
Task: none
Co-authored-by: Grok <noreply@x.ai>
<!-- CURSOR_AGENT_PR_BODY_END -->

<div><a href="https://cursor.com/agents/bc-8394fe20-2c97-48ff-b343-037a94f6f618?cursor_ref=pr_footer&cursor_cta=open_in_web"><picture><source media="(prefers-color-scheme: dark)" srcset="https://cursor.com/assets/images/open-in-web-dark.png"><source media="(prefers-color-scheme: light)" srcset="https://cursor.com/assets/images/open-in-web-light.png"><img alt="Open in Web" width="114" height="28" src="https://cursor.com/assets/images/open-in-web-dark.png"></picture></a>&nbsp;<a href="https://cursor.com/background-agent?bcId=bc-8394fe20-2c97-48ff-b343-037a94f6f618&cursor_ref=pr_footer&cursor_cta=open_in_cursor"><picture><source media="(prefers-color-scheme: dark)" srcset="https://cursor.com/assets/images/open-in-cursor-dark.png"><source media="(prefers-color-scheme: light)" srcset="https://cursor.com/assets/images/open-in-cursor-light.png"><img alt="Open in Cursor" width="131" height="28" src="https://cursor.com/assets/images/open-in-cursor-dark.png"></picture></a>&nbsp;</div>



## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 0 |
| Resolved threads | 0 |
| Unresolved threads | 0 |
| Total review comments | 0 |
| Total fix commits (touching thread paths) | 0 |

## Review threads

(no review threads on this PR)

## Fix commits (touching thread paths)

(no commits in this PR touched files anchored by review threads)
