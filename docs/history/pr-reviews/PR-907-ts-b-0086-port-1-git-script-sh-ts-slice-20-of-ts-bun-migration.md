# PR #907 -- ts(B-0086): port 1 git script (.sh→.ts) — slice 20 of TS/Bun migration

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 907 |
| Title | ts(B-0086): port 1 git script (.sh→.ts) — slice 20 of TS/Bun migration |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-04-30T07:46:45Z |
| Merged at | 2026-04-30T07:49:38Z |
| Merge commit SHA | `a8e15f3d1c263024ade7da4d3c140ea9eb3d02c2` |
| Branch | `lane-b/ts-bun-slice-20-batch-resolve-pr-threads-2026-04-30` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/907 |
| Changed files | 3 |
| Additions / deletions | +578 / -3 |

## Description

## Summary

Slice 20 of the TS/Bun migration (B-0086). Ports `tools/git/batch-resolve-pr-threads.sh` (390 lines bash) → `tools/git/batch-resolve-pr-threads.ts` (415 lines TS). Last git-cluster port — slice 13 (push-with-retry) + slice 20 together complete the cluster.

## Behavioural improvements (deliberate, not drift)

1. **Drops jq from runtime deps** — JSON parsing is native (`JSON.parse` replaces all jq pipelines). Now only `gh` is probed.
2. **Pattern classification via TS arrays + `.some()`** — same semantics as bash `for pat in ; do [[ ]]; done`, with type-safe `Classification = "dangling-ref" | "name-attribution" | "unknown"` discriminated union.
3. **`ResolveError` discriminated record** — `stage: "reply" | "resolve"` makes failure mode visible in error messages.

All bash safety rails from PR #199 review-cycle preserved: positive-integer pr-number validation, exact-`--apply` second-arg check, GraphQL errors array inspection, null-pullRequest detection, paginated thread fetch (50 per page) + paginated per-thread comments (50 per thread with truncation warning), positional -F args, byte-equivalent reply templates.

## Verification

- 3 argument-validation paths byte-equivalent (no args / bad pr-number / unknown second arg) — same exit code + same message
- Live dry-run on PR #902 (4 unresolved threads) byte-equivalent — `diff <(bun ...) <(./...sh)` empty
- Apply mode not exercised (would mutate live PR state); code path verified by inspection
- `bun --bun tsc --noEmit -p tsconfig.json` clean

## Test plan

- [x] tsc --noEmit clean locally
- [x] Argument-validation paths byte-equivalent (3 sampled)
- [x] Live dry-run byte-equivalent on real PR
- [ ] CI: `lint (tsc tools)` gate passes
- [ ] CI: gate.yml matrix passes
- [ ] CodeQL clean

## Composes with

- `tools/git/push-with-retry.ts` (slice 13, #892) — git-cluster siblings
- `docs/trajectories/typescript-bun-migration/RESUME.md` — updated to reflect Bucket B 2 → 1
- `docs/trajectories/typescript-bun-migration/slice-audits.md` — slice 20 audit appended
- Slice 21 candidate: `tools/pr-preservation/archive-pr.sh` (674L, bash+Python mix — last Bucket B file)

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 6 |
| Resolved threads | 0 |
| Unresolved threads | 6 |
| Total review comments | 6 |
| Total fix commits (touching thread paths) | 1 |

## Review threads

### Thread 1 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM5-qpj6`
- **Path**: `tools/git/batch-resolve-pr-threads.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-04-30T07:51:33Z on `tools/git/batch-resolve-pr-threads.ts`:224 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Propagate gh stderr on GraphQL fetch failures**

  When `gh api graphql` exits non-zero, this path returns only a generic message and drops `result.stderr`. Since `spawnSync` pipes child stderr by default, auth/scope/rate-limit diagnostics from `gh` are swallowed here, which makes automation failures much harder to triage than the previous shell behavior that surfaced those errors directly. Include the captured stderr (or use inherited stderr) so operators can distinguish environment/token issues from script defects.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 2 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM5-qqUR`
- **Path**: `tools/git/batch-resolve-pr-threads.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-30T07:52:22Z on `tools/git/batch-resolve-pr-threads.ts`:127 (association: NONE)

  <pre>
  P0: spawnSync failures and JSON parsing aren’t handled defensively here. `spawnSync("gh", ...)` can return `status === null` with `result.error` set (e.g., EACCES/ENOENT), and `result.stdout` can be `null`/non-JSON; `JSON.parse(result.stdout)` would then throw and bypass the script’s documented exit-code contract. Please classify spawn failures (error/signal/status) and wrap `JSON.parse` in a try/catch that returns a typed `{ error, exitCode: 1 }` outcome with the underlying failure detail.
  </pre>

### Thread 3 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM5-qqUj`
- **Path**: `tools/git/batch-resolve-pr-threads.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-30T07:52:23Z on `tools/git/batch-resolve-pr-threads.ts`:228 (association: NONE)

  <pre>
  P0: `fetchOnePage` returns `JSON.parse(result.stdout)` without guarding against spawn/termination failures (`result.error`, `status === null`) or malformed/non-JSON stdout. A thrown exception here will crash the script with a stack trace instead of producing the intended `FetchError` (exit 1). Consider factoring a small `runGhJson&lt;T&gt;()` helper that (1) classifies spawn failures, (2) includes stderr in errors when `status !== 0`, and (3) catches JSON parse errors.
  </pre>

### Thread 4 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM5-qqUs`
- **Path**: `tools/git/batch-resolve-pr-threads.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-30T07:52:23Z on `tools/git/batch-resolve-pr-threads.ts`:135 (association: NONE)

  <pre>
  P1: The auto-reply text for name-attribution appears to conflict with the current “no name attribution” standing rule. It claims names in “config / collaborator registries” are legitimate, but `docs/AGENT-BEST-PRACTICES.md` limits names to the closed list of history/research surfaces (plus a roster-mapping carve-out), and config is a current-state surface. Also, it points to a “`memory/CURRENT-aaron.md` attribution table”, but that file doesn’t appear to contain an attribution table (it’s sectioned prose). Please update the reply to cite the correct policy and only endorse the actually-allowed surfaces, so the script doesn’t auto-resolve valid findings with an inaccurate justification.

  </pre>

### Thread 5 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM5-qqU_`
- **Path**: `docs/trajectories/typescript-bun-migration/slice-audits.md`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-30T07:52:23Z on `docs/trajectories/typescript-bun-migration/slice-audits.md`:424 (association: NONE)

  <pre>
  P1: The audit entry’s line counts look incorrect. `tools/git/batch-resolve-pr-threads.sh` is ~391 lines and the new TS port is ~542 lines, but this says “390 → 415 lines”. Please update the counts to match the current files so the slice audit remains mechanically trustworthy.

  </pre>

### Thread 6 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM5-qqVY`
- **Path**: `tools/git/batch-resolve-pr-threads.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-30T07:52:23Z on `tools/git/batch-resolve-pr-threads.ts`:116 (association: NONE)

  <pre>
  P1: This port loses the bash script’s “stderr preserved on failure” behavior for `gh repo view`. `spawnSync` captures stderr by default, but on non-zero exit you return a generic message and discard `result.stderr`, which makes auth/network failures much harder to diagnose. Please include `result.stderr` (trimmed) in the returned error (or write it through to stderr) so failures remain actionable.

  </pre>

## Fix commits (touching thread paths)

### `3c0b0c28acfe2d7035eb43f75c49c8db2725858e` -- 2026-04-30T07:46:29Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `docs/trajectories/typescript-bun-migration/slice-audits.md`
- `tools/git/batch-resolve-pr-threads.ts`

**Message:**

```
ts(B-0086): port 1 git script (.sh→.ts) — slice 20 of TS/Bun migration

Last git-cluster port: tools/git/batch-resolve-pr-threads.{sh→ts}.
Slice 13 (push-with-retry) + slice 20 (batch-resolve-pr-threads)
together complete the git-cluster.

Behavioural improvements over bash original (deliberate, not drift):

- jq pipelines replaced with native JSON parse + array operations;
  drops jq from runtime deps (only `gh` required now).
- Pattern classification via TypeScript pattern arrays + `.some()`
  rather than bash `for pat in ; do [[ ]]; done` — same semantics,
  more type-safe with `Classification = "dangling-ref" |
  "name-attribution" | "unknown"` discriminated union.
- ResolveError discriminated record with `stage: "reply" | "resolve"`
  so failure mode is visible in error messages.

Byte-equivalence verified on this repo state:
- Three argument-validation paths byte-equivalent (no args / bad
  pr-number / unknown second arg) — same exit code + same message.
- Live dry-run on PR #902 (4 unresolved threads) byte-equivalent —
  empty diff against bash original.
- Apply mode not exercised (would mutate live PR state); code path
  verified by inspection.

All bash safety rails from PR #199 (Copilot/Codex) preserved:
positive-integer validation, exact-`--apply` second-arg check,
GraphQL errors array inspection, null-pullRequest detection,
paginated thread fetch (50 per page) + paginated per-thread comments
(50 per thread with truncation warning), positional -F args
(avoids parameter-expansion-quote pitfall), printf-not-echo for
review-comment bodies (handled via TypeScript string operations).

Bucket B 2 → 1 (only tools/pr-preservation/archive-pr.sh 674L
remains; bash+Python mix — slice 21).

Composes with:
- tools/git/push-with-retry.ts (slice 13, #892) — git-cluster siblings
- docs/trajectories/typescript-bun-migration/RESUME.md updated
- docs/trajectories/typescript-bun-migration/slice-audits.md slice 20 audit

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
```
