# PR #2756 -- feat(dashboard): prefer metrics.json over GitHub API (Amara fix)

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 2756 |
| Title | feat(dashboard): prefer metrics.json over GitHub API (Amara fix) |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-05-11T21:51:29Z |
| Merged at | 2026-05-11T22:15:06Z |
| Merge commit SHA | `2e93daf5021bd608598766dd2a22954116dcdf2c` |
| Branch | `feat/b-0414-ci-generated-metrics-2026-05-11` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/2756 |
| Changed files | 2 |
| Additions / deletions | +275 / -141 |

## Description

## Summary

Addresses Amara's #2 critique (2026-05-11): GitHub's unauthenticated REST API has a 60 req/hr per IP limit. Public dashboard visitors burn through it within ~20 min at the current refresh cadence.

**Implementation:**
- `loadFromStaticMetrics()` reads `./metrics.json` directly — no auth, no rate limit, single fetch
- `loadDashboard()` tries static path first; falls back to GitHub API only if metrics.json is unavailable
- Refresh-info label distinguishes static (`Static metrics · generated Xm ago`) from live (`Sync: X · Connected`) modes

**Cadence:**
- `metrics.json` is regenerated every PR-touching commit (Otto's tick already runs `bun tools/dashboard/generate-metrics.ts`)
- For sustained-idle periods, stale-but-rendered beats rate-limited

**Composes with:**
- B-0414 (metrics.json generator — already shipped)
- B-0413 (OAuth tier — future authenticated path bypasses rate limit entirely)

## Test plan

- [ ] Local: `python3 -m http.server` in `demo/`, verify dashboard loads from metrics.json
- [ ] Force fallback: rename metrics.json temporarily, verify API path still works
- [ ] On deploy: visitor IPs no longer hit 60 req/hr limit

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | true |
| Total threads | 13 |
| Resolved threads | 9 |
| Unresolved threads | 4 |
| Total review comments | 13 |
| Total fix commits (touching thread paths) | 4 |

## Review threads

### Thread 1 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6BO99e`
- **Path**: `demo/index.html`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-11T21:54:47Z on `demo/index.html`:925 (association: NONE)

  <pre>
  P0 (security): `prList.innerHTML = ...` interpolates `pr.title` directly into both the element body and a `title="..."` attribute. PR titles are attacker-controlled via GitHub and can contain HTML/attribute-breaking characters, so this enables DOM XSS when rendering `metrics.json`. Prefer building DOM nodes and assigning `textContent`/`setAttribute`, or escape HTML/attribute values before inserting into `innerHTML`.

  </pre>

### Thread 2 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6BO99w`
- **Path**: `demo/index.html`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-11T21:54:47Z on `demo/index.html`:928 (association: NONE)

  <pre>
  P0 (security): Timeline rendering uses `timeline.innerHTML = ...` and interpolates `c.agent`, `c.sha`, `c.dateAgo`, and especially `c.message` into HTML and attributes without escaping. Commit messages and author names are untrusted input from Git and can include `&lt;`/`"` etc, leading to XSS when served publicly. Build elements with `textContent` (and `setAttribute` for `title`) or escape values before concatenation.

  </pre>

### Thread 3 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6BO99_`
- **Path**: `demo/index.html`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-11T21:54:48Z on `demo/index.html`:935 (association: NONE)

  <pre>
  P1 (bug/UI state): If a previous refresh hit the API error path, `#refresh-info` gets `style.color = var(--danger)` (line 1071) and is never reset on subsequent successful static loads. Consider explicitly resetting the color (and any other error styling) when setting the static-mode refresh label.

  </pre>

### Thread 4 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6BO9-Q`
- **Path**: `demo/index.html`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-11T21:54:48Z on `demo/index.html`:884 (association: NONE)

  <pre>
  P2 (maintainability): Comment says the static `metrics.json` path is "cache-friendly", but `fetch('./metrics.json', { cache: 'no-cache' })` forces revalidation each time. If the intent is to leverage browser caching and only refresh on your interval, consider using the default cache mode (or `force-cache` with cache-busting via querystring when needed) and update the comment to match the chosen behavior.

  </pre>

### Thread 5 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6BPDE3`
- **Path**: `demo/index.html`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-11T22:00:59Z on `demo/index.html`:980 (association: NONE)

  <pre>
  `loadDashboard()` falls back to the GitHub API, but the fallback rendering later in this function still interpolates untrusted strings (PR titles, commit messages, agent names) directly into `innerHTML` without using `esc()`/`textContent`. This keeps an XSS sink alive whenever `metrics.json` is missing or fails to parse; please apply the same escaping strategy to the API-rendered DOM updates too.
  </pre>

### Thread 6 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6BPDFk`
- **Path**: `demo/index.html`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-11T22:00:59Z on `demo/index.html`:977 (association: NONE)

  <pre>
  When `metrics.json` is unavailable, the fallback path still makes 3 unauthenticated GitHub API requests per refresh cycle; with the current auto-refresh cadence this can still exceed GitHub’s 60 req/hr/IP limit and leave the dashboard in a persistent failure loop. Consider reducing refresh frequency in fallback mode and/or adding rate-limit-aware backoff/caching when the API path is used.
  </pre>

### Thread 7 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6BPDGB`
- **Path**: `demo/index.html`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-11T22:00:59Z on `demo/index.html`:980 (association: NONE)

  <pre>
  `catch (staticErr)` logs `staticErr.message`, but the thrown value in JS isn’t guaranteed to be an `Error` (it can be any value), so `.message` may be undefined and the warning becomes less actionable. Prefer logging `staticErr instanceof Error ? staticErr.message : String(staticErr)` (or similar) to keep diagnostics robust.
  </pre>

### Thread 8 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6BPDGR`
- **Path**: `demo/metrics.json`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-11T22:01:00Z on `demo/metrics.json`:22 (association: NONE)

  <pre>
  This file embeds direct persona/human first-names in a current-state surface (`demo/`), which conflicts with the repo’s operational rule to use role-refs outside history/roster-mapping surfaces (see `docs/AGENT-BEST-PRACTICES.md` §Operational standing rules). If `demo/metrics.json` is intended to be public/consumable, consider emitting role-based identifiers instead of names.
  </pre>

### Thread 9 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6BPHy_`
- **Path**: `demo/index.html`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-11T22:06:31Z on `demo/index.html`:1108 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Restore 2-minute refresh cadence after recovery**

  When an API fallback request fails once, this block permanently replaces the 2-minute timer with a 10-minute timer for the rest of the session. There is no corresponding success path (static or API) that re-arms `window._dashboardInterval` back to `120000`, so a transient outage leaves the dashboard stale far longer than intended even after data sources recover.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 10 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM6BPP2x`
- **Path**: `demo/index.html`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-11T22:15:11Z on `demo/index.html`:1118 (association: NONE)

  <pre>
  P1 (codebase_conventions): The inline comment references a specific person by name ("Amara P1") in a current-state code surface (`demo/index.html`). Per `docs/AGENT-BEST-PRACTICES.md` Operational standing rules (“No name attribution in code, docs, or skills”, around lines 671-728), please rewrite this comment to use a role-ref / neutral phrasing (e.g., “review feedback P1”) instead of a contributor/persona name.
  </pre>

### Thread 11 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM6BPP29`
- **Path**: `demo/index.html`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-11T22:15:12Z on `demo/index.html`:1137 (association: NONE)

  <pre>
  P1 (bug): The backoff logic for API failures only adjusts the refresh interval when `window._dashboardInterval` already exists. Because the first `loadDashboard()` call happens before `_dashboardInterval` is assigned, an initial API failure won’t switch to the 10‑minute cadence until the next cycle. Consider initializing the interval before the first load (or have the catch block create/update the interval even when it doesn’t yet exist) so the intended backoff applies immediately on first failure.
  </pre>

### Thread 12 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM6BPP3U`
- **Path**: `demo/index.html`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-11T22:15:12Z on `demo/index.html`:897 (association: NONE)

  <pre>
  P2 (maintainability/accuracy): In static-metrics mode the dashboard displays `data.metrics.last_merge_ago`, which will drift and become incorrect as `metrics.json` ages (even though the UI also shows how long ago the file was generated). Since `metrics.json` also contains `data.metrics.last_merge` as a timestamp, consider computing the “ago” string at render time (via `timeAgo(last_merge)`) to keep the displayed value accurate even when the JSON is stale.

  </pre>

### Thread 13 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM6BPP3z`
- **Path**: `demo/metrics.json`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-11T22:15:12Z on `demo/metrics.json`:22 (association: NONE)

  <pre>
  P1 (codebase_conventions): `demo/metrics.json` includes direct contributor/persona names (e.g., the `agents[].name` field) in a current-state surface under `demo/`. This conflicts with the repo’s operational rule to use role-refs outside the explicitly allowlisted history surfaces (see `docs/AGENT-BEST-PRACTICES.md` “No name attribution in code, docs, or skills”, around lines 671-728). If this file is meant to be public/static, consider emitting role-based identifiers instead (and keep any name↔role mapping on an allowed history surface if needed).
  </pre>

## Fix commits (touching thread paths)

### `a2c0e1e02169920166ef40b2bebc195ce71ad9e3` -- 2026-05-11T21:51:13Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `demo/index.html`
- `demo/metrics.json`

**Message:**

```
feat(dashboard): prefer metrics.json over GitHub API — Amara rate-lim…

…it fix

Amara critique 2026-05-11: "GitHub's unauthenticated REST API
limit is 60 requests/hour per IP. Generate docs/demo-data.json
in CI every 2-5 minutes. Have the browser read that static file.
Keep GitHub API as local/dev fallback."

Implementation:
- `loadFromStaticMetrics()` reads `./metrics.json` directly (no
  auth, no rate limit, single fetch)
- `loadDashboard()` tries static path first; falls back to live
  GitHub API only if metrics.json is unavailable
- Refresh-info label distinguishes "Static metrics · generated
  Xm ago" from live API mode
- "Plant is resting" replaces "Array is resting" (Plant rename)

metrics.json regenerated to match current state. The static
file gets regenerated by every PR-touching commit (during
Otto's tick); for sustained-idle periods, stale-but-rendered
beats rate-limited.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
```

### `2839d2399adff7b5c07d83a175153f9bfd30aec1` -- 2026-05-11T21:57:21Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `demo/index.html`

**Message:**

```
fix(security): XSS escape in static-metrics renderer + UI state reset

P0 security fixes from Copilot review on PR #2756:
- PR titles, commit messages, agent names are attacker-controlled
  via GitHub. innerHTML interpolation without escaping is XSS.
- Added esc() helper using standard HTML entity escapes
  (&, <, >, ", ')
- Applied to all untrusted-source interpolations: agent list,
  PR queue, timeline (commit messages, hashes, authors)
- Numeric fields cast to Number for defense-in-depth

P1 UI fix:
- Reset refresh-info color on successful static load (was
  stuck on danger from prior API error)

P2 comment fix:
- Clarified cache: 'no-cache' purpose (revalidate freshness,
  not "cache-friendly")

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
```

### `24b482dfd6ed36292f5624ee9c3d5ea48b11a413` -- 2026-05-11T22:03:38Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `demo/index.html`

**Message:**

```
fix(security): XSS in API fallback path + defensive error logging + b…

…ackoff on rate-limit

Three more Copilot findings on PR #2756:

P0 — API fallback path also had unescaped innerHTML interpolation
of PR titles, commit messages, agent names, hashes. The static
path's esc() helper now applied throughout the fallback too.

P1 — On API failure, refresh interval extends from 2min to 10min
to avoid hammering the 60req/hr rate limit while it recovers.
Uses window._dashboardInterval so the catch block can clearInterval
and re-arm with the longer cadence.

P2 — Defensive error logging: `staticErr.message` is unsafe if
the thrown value isn't an Error. Now uses
`staticErr instanceof Error ? staticErr.message : String(staticErr)`.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
```

### `02b559d0a2f78109acb8a23d49b1804531d9d4bd` -- 2026-05-11T22:11:33Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `demo/index.html`

**Message:**

```
fix(dashboard): restore 2-min cadence after API recovery — P2

Copilot caught: once API failed, dashboard stayed at 10-min
refresh forever. Now:
- DASHBOARD_REFRESH_NORMAL_MS (2min) + DASHBOARD_REFRESH_BACKOFF_MS (10min) constants
- restoreNormalCadence() helper called after every successful load
- _dashboardCadence tracker prevents unnecessary clearInterval thrash
- Backoff only fires when not already in backoff

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
```
