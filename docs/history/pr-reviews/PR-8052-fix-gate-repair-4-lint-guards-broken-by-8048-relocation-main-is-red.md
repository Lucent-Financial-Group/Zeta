# PR #8052 -- fix(gate): repair 4 lint guards broken by #8048 relocation (main is RED)

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 8052 |
| Title | fix(gate): repair 4 lint guards broken by #8048 relocation (main is RED) |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-06-13T19:48:18Z |
| Merged at | 2026-06-13T19:48:30Z |
| Merge commit SHA | `61058375774752e020ad93aa95c6254428e406da` |
| Branch | `shadow/fix-gate-relocation-stale-paths` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/8052 |
| Changed files | 6 |
| Additions / deletions | +441 / -388 |

## Description

## Why (urgent)

**`main` is RED** — the batch-3 relocation (#8048, `tools/` → `src/Core.TypeScript/`) moved scripts + data but left stale `tools/` paths inside them. Four **required** gate jobs are failing on main, blocking every PR.

## Fixes (path-only, no behavior change)

| Gate job | Root cause | Fix |
|----------|-----------|-----|
| `no empty dirs` | `no-empty-dirs.ts` `ALLOWLIST_REL` → `tools/lint/` | repoint to `src/Core.TypeScript/lint/` |
| `no python files` | `no-python-files.ts` `ALLOWLIST_REL` + its test's `writeAllowlist()` both → `tools/lint/` (self-consistent in the test, so it passed while the *real* allowlist was unreachable) | repoint both |
| `tick-shard relative-paths` | 10 historical tick docs link (relative) to `tools/hygiene` & `tools/orchestrator-checks` files the relocation moved | grandfather the 10 into the baseline (1245→1255); history is append-only |
| `bash retirement inventory` | retained-shell allowlist + test list `tools/kiro/*.sh` | repoint to `src/Core.TypeScript/kiro/*.sh` |

## Verification

All 4 guards pass locally; **35 affected tests pass / 0 fail**.

## Note

The `no-python-files` guard is **transitional** — slated for removal/inversion when Python + Go join the existing 4 oracles (@AceHack 2026-06-13). Fixed minimally here only to restore green main until that lands.

Caught by the autonomous loop (keep-main-green mandate); regression from #8048.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 0 |
| Resolved threads | 0 |
| Unresolved threads | 0 |
| Total review comments | 0 |
| Total fix commits (touching thread paths) | 0 |

## Review threads

(no review threads on this PR)

## Fix commits (touching thread paths)

(no commits in this PR touched files anchored by review threads)
