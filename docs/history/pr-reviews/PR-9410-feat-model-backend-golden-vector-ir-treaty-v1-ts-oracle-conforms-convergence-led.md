# PR #9410 -- feat(model-backend): golden-vector IR treaty v1 — TS oracle conforms + convergence ledger (shadow*)

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 9410 |
| Title | feat(model-backend): golden-vector IR treaty v1 — TS oracle conforms + convergence ledger (shadow*) |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-07-04T04:36:14Z |
| Merged at | 2026-07-04T04:36:20Z |
| Merge commit SHA | `d2b436d43226e4b34ac43f4e5cde52c046579770` |
| Branch | `otto/golden-vector-ir-treaty-v1-ts-oracle` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/9410 |
| Changed files | 2 |
| Additions / deletions | +111 / -0 |

## Description

## What

Aaron 2026-07-04 *"start the golden-vector build."* The treaty: every oracle (TS `zeta-store`, F# `ZetaToolStore`, the other 5) must reproduce these observable results **identically** across space (7 langs) + time (DST replay) — making "all our langs agree" a **test**, not an aspiration.

**`zeta-store-golden-vectors.json`** — v1 locks the hash-**independent** fs semantics that already agree: link→resolve roundtrip, multi-parent dedup, `editEverywhere` shared-follow, `editLocal` COW, `unlink`. Text-only (no binary in the proof lineage). Addresses **not** locked yet — see `_convergence`.

## Starting the build did its job — it surfaced the disagreements the treaty must drive to zero
1. **content-hash function** — TS blake3 (`ace` `contentHash`) vs F# XxHash128 (`MerkleHash`): addresses diverge → pick **one canonical hash** (the first treaty **signature** — your call).
2. **tool_call event encoding** — TS object vs F# string → canonicalize.
3. **`editEverywhere` on an absent path** — TS links-if-absent vs F# no-op (DagFs = source of truth) → reconcile TS.
4. **db event type** — TS object vs F# string → canonicalize.

**`zeta-store-golden.test.ts`** — the TS oracle **conforms**: 6 tests (all vectors) + carries the ledger. The **F# replayer is next**; its failures *are* the convergence work list.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 0 |
| Resolved threads | 0 |
| Unresolved threads | 0 |
| Total review comments | 0 |
| Total fix commits (touching thread paths) | 0 |

## Review threads

(no review threads on this PR)

## Fix commits (touching thread paths)

(no commits in this PR touched files anchored by review threads)
