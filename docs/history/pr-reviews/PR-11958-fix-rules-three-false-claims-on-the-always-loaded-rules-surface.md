# PR #11958 -- fix(rules): three false claims on the always-loaded rules surface

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 11958 |
| Title | fix(rules): three false claims on the always-loaded rules surface |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-08-18T13:47:57Z |
| Merged at | 2026-08-18T14:03:01Z |
| Merge commit SHA | `846196e33a6ea02d465d8f7275b18e3ec6a32c2f` |
| Branch | `rules/correct-stale-factual-claims-in-startup-surfaces` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/11958 |
| Changed files | 5 |
| Additions / deletions | +108 / -3 |

## Description

`.claude/rules/` loads into every agent's context on every wake, so a false line there is paid
for repeatedly and propagates. Three corrections, each one line, each with the check that proved
the old text wrong. **Nothing about emphasis, ordering, or doctrine was touched.**

## 1. `every-bug-has-economic-value.md` — `sim` called an "unwired interface stub"

False since PR #11679 (merged 2026-08-17), work item `081M08VM385087G0R001DTM0K6`:

- `ls clis/` → `Zeta.Clis.fsproj` sits beside `Verbs.fs`
- `grep -n Clis Zeta.sln` → line 172, so CI's `dotnet build Zeta.sln -c Release` reads the file
- `ls tests/Tests.FSharp/Clis/` → `Verbs.Tests.fs`, the composition witness

"Working" would be equally wrong. `ISimVerb.Sim: ISeed * TimeSpan -> unit` and
`ICutVerb.Cut<'a>: TimeSpan * ISim<'a>` consumes the sim rather than the measurement — two
independent breaks, so nothing anywhere introduces an `ISim<'a>`. New text names that mechanically.
`db/uncertainty/README.md` (the satellite) had already been corrected; the hub had not.

## 2. `async-all-the-way-truthful-signatures.md` — named `Task.Run` sites that no longer exist

`grep -nE "Task\.(Run|Factory)" src/Core/Runtime.fs src/Core/SpineAsync.fs` → **no matches**.
Both are on the ferry (`Runtime.fs:48,68`; `SpineAsync.fs:28,42`), migrated in `1e012b7273`
*"Migrate raw Task.Run sites in DbspRuntime and SpineAsync to FerryThrottler"*. The rule was
pointing at its own worked instance as if it were outstanding work.

## 3. `toy-is-free-metered-must-be-earned.md` — `LagrangeCondorcet.fs` had no resolvable path

`find . -name 'LagrangeCondorcet*'` → `src/Bayesian/LagrangeCondorcet.fs`. (The reported defect
was "cites `src/Core/`" — it did not; it cited a bare filename, which the auditor cannot resolve
either.) The metered claims themselves check out and are untouched: `:32` carries
`μ_crit = (1 - √(23/27)) / 2`, and `tests/Bayesian.Tests/LagrangeCondorcet.Tests.fs:17` pins
`abs (jurySize - 25.96) < 0.1`.

**Measured effect:** `audit-rule-cross-refs.ts` resolved 113/127 before, 114/127 after.

## Swept and found sound (so untouched)

All 9 ZetaIds cited across the rules resolve to real rows; all 11 `rules.bak/` pointers exist;
`GOVERNANCE.md` §35 (line 903) and `AGENTS.md` line 25 both exist; `audit-proof-lineage-binaries.ts`
really does run inside gate.yml's `cross-verify` job (job at `:1720`, step at `:1810`); the six
`.wasm` files, `run-bytelock-ci.mjs` and `bytelock.yml` exist; `lint-no-new-bnnnn.ts` and
`lint-b-refs-resolve.ts` are wired in `backlog-index-integrity.yml`. Symbols verified live include
`GlassHalo.frost`, `RoomBoundary.frost`, `BeliefConvergence.observeAll`, `SpectrumMatch` /
`SameSourceAsKnown`, `AntiSybil.SourceOf` + `DistinctCount`, `AssertedOnly` / `supportsClaim` /
`Wall.Whitebox` / `whiteboxPermitted`, RC-2 / RC-3, and TravelerRankLedger's `0.5` prior + `≈0.35`
whitewash value.

## Left alone deliberately (listed, not guessed)

- **"`Collation.binary` in all four oracles"** (`culture-invariant-by-default.md`). F#/C#/TS have a
  named `binary` collation; no Rust collation module exists, but the treaty test says the canonical
  relation *is* Rust's native `Ord for String`. Module-vs-relation is a judgement call, not a fact check.
- **13 path-less pointers** the cross-ref auditor flags (`new-workitem.ts`, `dora-metrics.ts`, …).
  Each resolves uniquely; none is wrong, only path-less. Adding paths is a size decision on a
  cold-start surface.
- **Two work-item state inconsistencies** — `081KTF10R0108QG0R003P44BA2` is in `workitems/done/`
  with `state: backlog`; `081M08VM385087G0R001DTM0K6` is `state: backlog` while its body says
  "What this item shipped". Lifecycle bookkeeping, not rule text.
- **`dv2-data-split-discipline-activated.md` ordering** — it leads with change-rate partitioning
  while Aaron holds that *disagreement preservation* is what is unique to DV2. Emphasis, and his
  call; recorded so it is not lost, not touched.

Work item: `081M0AHW6CH087G0R001F1V9RY`.

Agency-Signature-Version: 1
Agent: shadow
Agent-Runtime: claude-code/agent-sdk
Agent-Model: claude-opus-5
Credential-Identity: AceHack
Credential-Mode: shared
Human-Review: not-implied-by-credential
Human-Review-Evidence: none
Action-Mode: autonomous-fail-open
Task: 081M0AHW6CH087G0R001F1V9RY
Co-authored-by: Claude Opus 5 <noreply@anthropic.com>


## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 0 |
| Resolved threads | 0 |
| Unresolved threads | 0 |
| Total review comments | 0 |
| Total fix commits (touching thread paths) | 0 |

## Review threads

(no review threads on this PR)

## Fix commits (touching thread paths)

(no commits in this PR touched files anchored by review threads)
