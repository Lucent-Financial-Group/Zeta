# PR #2159 -- feat(B-0266): Review Policy ruleset migration script

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 2159 |
| Title | feat(B-0266): Review Policy ruleset migration script |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-05-09T00:58:56Z |
| Merged at | 2026-05-09T01:15:45Z |
| Merge commit SHA | `1ec1137ae76d31648ef17466fc044de1fd73cfdf` |
| Branch | `b0266-review-policy-ruleset` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/2159 |
| Changed files | 2 |
| Additions / deletions | +250 / -1 |

## Description

## Summary

- Adds one-shot TS migration script at `tools/migrations/b0266-review-policy-ruleset.ts` that:
  1. Creates "Review Policy" ruleset with `pull_request` (thread resolution, squash-only) + `copilot_code_review` (draft + push review)
  2. Updates "Default" ruleset (id 15256879) to keep only `deletion`, `non_fast_forward`, `required_linear_history`
  3. Re-snapshots `tools/hygiene/github-settings.expected.json`
- Updates B-0266 backlog item with pre-start checklist and implementation docs

Second of three rulesets in the B-0155 split (after B-0265 CI Gate).

## To execute

```bash
# Dry run first
bun tools/migrations/b0266-review-policy-ruleset.ts --dry-run

# Then run for real
bun tools/migrations/b0266-review-policy-ruleset.ts

# Verify no drift
bun tools/hygiene/check-github-settings-drift.ts
```

After execution, commit the updated `github-settings.expected.json` and close B-0266.

## Test plan

- [x] `--dry-run` mode outputs correct payloads (verified locally)
- [x] `dotnet build -c Release` passes (0 warnings, 0 errors)
- [ ] Execute migration on merge, verify via drift check

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | true |
| Total threads | 11 |
| Resolved threads | 11 |
| Unresolved threads | 0 |
| Total review comments | 16 |
| Total fix commits (touching thread paths) | 5 |

## Review threads

### Thread 1 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6AxW1y`
- **Path**: `tools/migrations/b0266-review-policy-ruleset.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-09T01:01:48Z on `tools/migrations/b0266-review-policy-ruleset.ts`:193 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Snapshot the same repository mutated by the migration**

  This migration updates rulesets on a hardcoded repo (`Lucent-Financial-Group/Zeta`) in steps 1–2, but step 3 runs `snapshot-github-settings.ts` without `--repo`, so it falls back to `GH_REPO` or the current checkout context. If the operator has `GH_REPO` set (or is running from a fork worktree), the script will mutate one repository but overwrite `github-settings.expected.json` with a different repository’s settings, producing a corrupted baseline and misleading drift checks.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 2 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6AxXDD`
- **Path**: `tools/migrations/b0266-review-policy-ruleset.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-09T01:02:39Z on `tools/migrations/b0266-review-policy-ruleset.ts`:157 (association: NONE)

  <pre>
  Step 3 snapshots without passing `--repo`, so it may snapshot a different repo than the one mutated in steps 1–2 (e.g., if `GH_REPO` is set or the current `gh` context points elsewhere). Pass `--repo ${OWNER}/${REPO}` (or invoke `snapshot()` directly) so the expected snapshot always matches the target repo.

  </pre>

### Thread 3 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6AxXDM`
- **Path**: `tools/migrations/b0266-review-policy-ruleset.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-09T01:02:40Z on `tools/migrations/b0266-review-policy-ruleset.ts`:202 (association: NONE)

  <pre>
  This script assumes it is run from the repo root (relative paths for `tools/hygiene/snapshot-github-settings.ts` and `tools/hygiene/github-settings.expected.json`). Other tools typically resolve paths relative to `import.meta.url` (and/or `process.chdir` to repo root) to be robust when invoked from any working directory.
  </pre>

### Thread 4 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6AxXDO`
- **Path**: `tools/migrations/b0266-review-policy-ruleset.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-09T01:02:40Z on `tools/migrations/b0266-review-policy-ruleset.ts`:141 (association: NONE)

  <pre>
  Creating the "Review Policy" ruleset is not idempotent: re-running the script will create duplicate rulesets with the same name. Consider a preflight `GET /repos/{owner}/{repo}/rulesets` check to abort if a ruleset named "Review Policy" already exists (or update/replace it deterministically).
  </pre>

### Thread 5 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6AxXDR`
- **Path**: `tools/migrations/b0266-review-policy-ruleset.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-09T01:02:40Z on `tools/migrations/b0266-review-policy-ruleset.ts`:175 (association: NONE)

  <pre>
  Most `tools/**.ts` scripts export a `main(argv)` and only execute when `import.meta.main` is true; this file unconditionally runs `main().catch(...)` and calls `process.exit`. Aligning to the usual pattern makes it safer to import/reuse helpers (and keeps consistent tool structure across the repo).
  </pre>

### Thread 6 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6AxXDX`
- **Path**: `tools/migrations/b0266-review-policy-ruleset.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-09T01:02:40Z on `tools/migrations/b0266-review-policy-ruleset.ts`:203 (association: NONE)

  <pre>
  PR description says `tools/hygiene/github-settings.expected.json` was re-snapshotted, but that file isn't included in the PR changes shown here. If the migration is intended to update the checked-in snapshot as part of this PR, please include the snapshot diff (or update the PR description if snapshotting is deferred until after merge).
  </pre>

### Thread 7 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6AxZjS`
- **Path**: `tools/migrations/b0266-review-policy-ruleset.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-09T01:08:30Z on `tools/migrations/b0266-review-policy-ruleset.ts`:113 (association: NONE)

  <pre>
  `updatedDefaultPayload` omits `target`. The current "Default" ruleset in `tools/hygiene/github-settings.expected.json` includes `"target": "branch"`; depending on the rulesets API semantics for `PUT`, omitting `target` can cause the request to fail or to unintentionally clear/reset fields. Include `target: "branch"` (and, ideally, GET the existing ruleset first and preserve any other required/implicit fields) when updating the Default ruleset.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-09T01:11:12Z on `tools/migrations/b0266-review-policy-ruleset.ts`:113 (association: MEMBER)

    <pre>
    Fixed in 08fbeb05 — added `target: "branch"` to `updatedDefaultPayload`.
    </pre>

### Thread 8 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6AxZje`
- **Path**: `tools/migrations/b0266-review-policy-ruleset.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-09T01:08:31Z on `tools/migrations/b0266-review-policy-ruleset.ts`:186 (association: NONE)

  <pre>
  `main()` calls `process.exit(1)` on snapshot failure. In this repo’s tools scripts, the usual pattern is for `main(...)` to return an exit code (or throw) and only call `process.exit(...)` in the `if (import.meta.main)` guard. Consider returning a non-zero code or throwing here so imports/callers of `main()` don’t terminate the process unexpectedly.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-09T01:11:14Z on `tools/migrations/b0266-review-policy-ruleset.ts`:186 (association: MEMBER)

    <pre>
    Fixed in 08fbeb05 — replaced `process.exit(1)` with `throw new Error(...)` in `main()` so the `import.meta.main` guard handles process exit.
    </pre>

### Thread 9 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6AxZjk`
- **Path**: `tools/migrations/b0266-review-policy-ruleset.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-09T01:08:31Z on `tools/migrations/b0266-review-policy-ruleset.ts`:16 (association: NONE)

  <pre>
  This script hardcodes `OWNER`/`REPO`/`DEFAULT_RULESET_ID`. Since it performs repo-admin mutations, it’s easy to run it from a fork/alternate checkout and still have it target the upstream repo. Consider requiring an explicit `--repo OWNER/NAME` flag (or at least printing the resolved target and requiring confirmation) and deriving the default from `GH_REPO`/`gh repo view` like `snapshot-github-settings.ts` does.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-09T01:11:31Z on `tools/migrations/b0266-review-policy-ruleset.ts`:16 (association: MEMBER)

    <pre>
    Fixed in 08fbeb05 — added explicit target-repo echo (`Target: Lucent-Financial-Group/Zeta (hardcoded — this is a one-shot migration)`) before mutations. The hardcoding is intentional for this one-shot migration: it has `--dry-run` support, an idempotency check, and should only ever target this specific repo.
    </pre>

### Thread 10 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6Axa4j`
- **Path**: `tools/migrations/b0266-review-policy-ruleset.ts`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-09T01:10:55Z on `tools/migrations/b0266-review-policy-ruleset.ts`:138 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Allow reruns to complete remaining migration steps**

  This early return makes the migration non-resumable after partial failure: if step 1 succeeds but step 2 or step 3 fails (e.g., transient API/auth failure), rerunning the script exits as soon as it sees `Review Policy` and never updates the Default ruleset or refreshes `github-settings.expected.json`. In that state, the repository can be left half-migrated indefinitely unless someone performs manual API edits.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-09T01:12:59Z on `tools/migrations/b0266-review-policy-ruleset.ts`:138 (association: MEMBER)

    <pre>
    Fixed in bb4899f5 — the idempotency check now skips only step 1 (create); steps 2-3 (update Default + snapshot) still run on re-execution, making the migration resumable after partial failure.
    </pre>

### Thread 11 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6Axa4o`
- **Path**: `tools/migrations/b0266-review-policy-ruleset.ts`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-09T01:10:55Z on `tools/migrations/b0266-review-policy-ruleset.ts`:133 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Exclude parent rulesets from the idempotency check**

  The existence check calls `GET /repos/{owner}/{repo}/rulesets` without `includes_parents=false`, and GitHub includes inherited org-level rulesets by default. If an inherited ruleset is also named `Review Policy`, this check will treat the migration as already done and skip creating the repository-level ruleset, so the split never actually happens for this repo.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-09T01:13:02Z on `tools/migrations/b0266-review-policy-ruleset.ts`:133 (association: MEMBER)

    <pre>
    Fixed in bb4899f5 — added `?includes_parents=false` to the `GET /repos/{owner}/{repo}/rulesets` call so inherited org-level rulesets are excluded from the idempotency check.
    </pre>

## Fix commits (touching thread paths)

### `86ee44fb5dc271ec7ddf90b606e471e34032b2d9` -- 2026-05-09T00:58:15Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/migrations/b0266-review-policy-ruleset.ts`

**Message:**

```
feat(B-0266): Review Policy ruleset migration script

One-shot TS migration creates the "Review Policy" ruleset
(pull_request + copilot_code_review) and trims "Default" to
branch-safety-only rules (deletion, non_fast_forward,
required_linear_history). Dry-run verified. Second of three
rulesets in the B-0155 split.

Co-Authored-By: Claude Opus 4.6 (1M context) <noreply@anthropic.com>
```

### `5b68c57dc60c0c1b206ed5c404464c15129eee5f` -- 2026-05-09T01:03:57Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/migrations/b0266-review-policy-ruleset.ts`

**Message:**

```
fix(B-0266): address review threads — pass --repo to snapshot, add id…

…empotency preflight, align main() export pattern

- Snapshot now always targets Lucent-Financial-Group/Zeta via --repo flag
- Preflight GET /rulesets aborts duplicate create for idempotency
- Refactored to export async main() + import.meta.main guard (consistent with other tools)
- Dry-run message updated for clarity

Addresses all 6 unresolved review threads (P1 snapshot-mismatch + 5 others).
Co-Authored-By: Grok <noreply@x.ai>

Co-authored-by: Cursor <cursoragent@cursor.com>
```

### `33287512fc6a00443eaeac8a93df94ad99399cc3` -- 2026-05-09T01:05:13Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/migrations/b0266-review-policy-ruleset.ts`

**Message:**

```
fix(B-0266): address review findings — idempotency, repo-explicit sna…

…pshot, import.meta.main, path resolution

- Add preflight GET rulesets check to abort if "Review Policy" already exists
- Pass --repo explicitly to snapshot-github-settings.ts (fixes Codex P1 + Copilot finding)
- Resolve all paths relative to import.meta.url (robust from any working directory)
- Use import.meta.main guard per repo convention

Co-Authored-By: Claude Opus 4.6 (1M context) <noreply@anthropic.com>
```

### `08fbeb05bcecc1bb00ab507071ceca84a1784f5d` -- 2026-05-09T01:10:54Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/migrations/b0266-review-policy-ruleset.ts`

**Message:**

```
fix(B-0266): address Copilot review — add target field, fix process.e…

…xit, echo target repo

- Add `target: "branch"` to `updatedDefaultPayload` so the PUT doesn't
  reset/omit the field on the Default ruleset.
- Replace `process.exit(1)` in `main()` with `throw` to follow the
  repo's `import.meta.main` guard pattern.
- Echo the hardcoded target repo slug before mutations for operator
  visibility.

Co-Authored-By: Claude Opus 4.6 (1M context) <noreply@anthropic.com>
```

### `bb4899f57b5787a859bab0b53be6c9930269f6d8` -- 2026-05-09T01:12:42Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/migrations/b0266-review-policy-ruleset.ts`

**Message:**

```
fix(B-0266): make migration resumable + exclude parent rulesets from …

…idempotency check

- Skip only step 1 (create) when "Review Policy" already exists;
  steps 2-3 (update Default + snapshot) still run, making the
  migration resumable after partial failure.
- Add `?includes_parents=false` to the rulesets GET to avoid false
  positives from inherited org-level rulesets.

Co-Authored-By: Claude Opus 4.6 (1M context) <noreply@anthropic.com>
```
