# PR #2200 -- feat(B-0332): add memory load-bearing classifier

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 2200 |
| Title | feat(B-0332): add memory load-bearing classifier |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-05-09T06:20:52Z |
| Merged at | 2026-05-09T06:23:28Z |
| Merge commit SHA | `8ad8e19049ef79b0c995eb16f8950aff588cc74a` |
| Branch | `fix/b0332-memory-load-bearing-classifier` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/2200 |
| Changed files | 60 |
| Additions / deletions | +293 / -59 |

## Description

## Summary

- Add `tools/hygiene/classify-memory-load-bearing.ts` — traces citation graph from bootstrap surfaces to memory files
- BFS with depth tiers classifies files as load-bearing (reachable) vs decorative (unreachable)
- Handles `memory/` path refs, glob patterns, `../memory/` prefixes, memory-file-shaped bare links
- Supports `--memory-dir` to audit either user-scope or in-repo memory
- Mark B-0332 done

**Key findings from first run (user-scope, 627 files):**
| Metric | Value |
|--------|-------|
| Load-bearing (Tier 0) | 5 (0.8%) |
| Decorative | 622 (99.2%) |
| Broken refs in CLAUDE.md | 37 |
| Broken refs in ALIGNMENT.md | 4 |

Most CLAUDE.md memory references are phantom — files don't exist in user-scope memory dir but DO exist in-repo `memory/`. This suggests the two directories have diverged.

## Test plan

- [x] `bun tools/hygiene/classify-memory-load-bearing.ts` — default (user-scope memory)
- [x] `bun tools/hygiene/classify-memory-load-bearing.ts --memory-dir memory` — in-repo
- [x] `bun tools/hygiene/classify-memory-load-bearing.ts --json` — structured output
- [ ] CI gate (tsc)

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 2 |
| Resolved threads | 0 |
| Unresolved threads | 2 |
| Total review comments | 2 |
| Total fix commits (touching thread paths) | 1 |

## Review threads

### Thread 1 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM6Ay4Jn`
- **Path**: `tools/hygiene/classify-memory-load-bearing.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-09T06:23:44Z on `tools/hygiene/classify-memory-load-bearing.ts`:85 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Traverse memory tree recursively**

  Enumerating files with `readdirSync(memoryDir)` only captures immediate children, so markdown files in nested folders are excluded from `totalFiles`, `loadBearing`, and `decorative`. Because this repository already stores memory files under subdirectories (for example persona/ and observed-phenomena paths), the current implementation can materially undercount and misclassify the dataset, which breaks the accuracy of the load-bearing vs decorative report.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 2 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM6Ay4Jp`
- **Path**: `tools/hygiene/classify-memory-load-bearing.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-09T06:23:44Z on `tools/hygiene/classify-memory-load-bearing.ts`:17 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Derive default memory path from repo slug**

  The default path is hard-coded to a single machine-specific slug (`-Users-acehack-Documents-src-repos-Zeta`), so the documented default invocation fails on any environment where the repo lives at a different absolute path or username unless `--memory-dir` is always supplied. This makes the tool non-portable by default and can cause false “directory not found” failures for other contributors and CI-like environments.

  Useful? React with 👍 / 👎.
  </pre>

## Fix commits (touching thread paths)

### `7ffc3216956983c5daff55fdb60fa1604e4bc00b` -- 2026-05-09T06:20:44Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/hygiene/classify-memory-load-bearing.ts`

**Message:**

```
feat(B-0332): add memory load-bearing-vs-decorative classifier

Traces the citation graph from bootstrap surfaces (CLAUDE.md, AGENTS.md,
GOVERNANCE.md, docs/ALIGNMENT.md) to memory files. BFS with depth tiers.

Key findings from first run against user-scope memory (627 files):
- 5 load-bearing (directly cited, 0.8%)
- 622 decorative (not reachable, 99.2%)
- 41 broken references (CLAUDE.md cites files that don't exist)

Against in-repo memory/ (930 files): 100% reachable due to high
cross-reference density in memory files.

The 41 broken refs are the actionable finding — CLAUDE.md has phantom
pointers to memory files that were never created in the user-scope
directory or were deleted/renamed.

Co-Authored-By: Claude Opus 4.6 (1M context) <noreply@anthropic.com>
```
