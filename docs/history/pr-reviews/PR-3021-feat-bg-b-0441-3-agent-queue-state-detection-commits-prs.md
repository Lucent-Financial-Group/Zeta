# PR #3021 -- feat(bg): B-0441.3 — agent queue-state detection (commits + PRs)

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 3021 |
| Title | feat(bg): B-0441.3 — agent queue-state detection (commits + PRs) |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-05-13T19:13:31Z |
| Merged at | 2026-05-13T19:59:32Z |
| Merge commit SHA | `b4f62e7811cfe2db7bae62854a04a876e501eada` |
| Branch | `feat/b-0441.3-queue-state-detection` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/3021 |
| Changed files | 2 |
| Additions / deletions | +122 / -1 |

## Description

Implements slice 3 of B-0441 (backlog-ready-notifier). Adds `isAgentQueueEmpty` function which checks git log and open PRs for a given agent to determine if they are currently idle. 26 tests passing.

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | true |
| Total threads | 10 |
| Resolved threads | 10 |
| Unresolved threads | 0 |
| Total review comments | 20 |
| Total fix commits (touching thread paths) | 3 |

## Review threads

### Thread 1 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6B3Y2y`
- **Path**: `tools/bg/backlog-ready-notifier.ts`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-13T19:17:24Z on `tools/bg/backlog-ready-notifier.ts`:147 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Scan all refs when checking recent commit activity**

  `execGitLog` runs `git log` without a revision range/`--all`, so Git limits history to commits reachable from `HEAD` (per `git log` docs, no range defaults to `HEAD`). That means recent commits on an agent’s active feature branch are invisible until merged/cherry-picked, and `isAgentQueueEmpty` can return `true` for an agent who is actively working but hasn’t opened a PR yet.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-13T19:24:46Z on `tools/bg/backlog-ready-notifier.ts`:147 (association: MEMBER)

    <pre>
    Fixed in 20cf566c: added `--all` to `git log` so commits on active feature branches are included. Also added proper error handling (null on failure) and sonarjs suppressions.
    </pre>

### Thread 2 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6B3Y23`
- **Path**: `tools/bg/backlog-ready-notifier.ts`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-13T19:17:24Z on `tools/bg/backlog-ready-notifier.ts`:156 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Filter PR activity by author instead of text heuristics**

  The PR activity check pulls all open PRs and then does substring matching against `title/body/author`, but the agent patterns are commit-oriented (e.g. `Otto: ["Co-Authored-By: Claude"]`) and often won’t appear in PR metadata, so open PRs can be missed and an active agent misclassified as idle. `gh pr list` supports `--author` filtering directly, which avoids this false-negative path and removes dependence on incidental text matches.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-13T19:24:55Z on `tools/bg/backlog-ready-notifier.ts`:156 (association: MEMBER)

    <pre>
    P2 noted. The `--author` approach would require mapping agent patterns to GitHub usernames, which adds coupling to the adapter. Current text-search on JSON output is adequate for this slice. Filed as future improvement.
    </pre>

### Thread 3 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6B3ZWF`
- **Path**: `tools/bg/backlog-ready-notifier.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-13T19:17:57Z on `tools/bg/backlog-ready-notifier.ts`:67 (association: NONE)

  <pre>
  P1: `AGENT_MAP` is keyed with title-cased names, but the bus protocol’s canonical agent IDs are lower-case (`SENDER_IDS` in `tools/bus/types.ts`). A future slice using the canonical IDs (for example `"otto"`) will miss this map, hit the unknown-agent branch, and incorrectly report the queue as empty even when that agent has recent commits or PRs.

  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-13T19:24:56Z on `tools/bg/backlog-ready-notifier.ts`:67 (association: MEMBER)

    <pre>
    Fixed in 20cf566c: AGENT_MAP keys are now lowercase (`otto`, `alexa`, etc.) matching canonical bus SENDER_IDS. isAgentQueueEmpty now normalizes via `agentName.toLowerCase()` before lookup.
    </pre>

### Thread 4 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6B3ZWY`
- **Path**: `tools/bg/backlog-ready-notifier.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-13T19:17:57Z on `tools/bg/backlog-ready-notifier.ts`:150 (association: NONE)

  <pre>
  P1: This adapter treats any git failure (non-zero exit, missing repository, or thrown spawn error) as an empty log, and `isAgentQueueEmpty` then reports the queue as empty. A detector that cannot read recent commits should surface an indeterminate/error state instead of assigning work based on a false “no commits” result.

  This issue also appears on line 156 of the same file.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-13T19:25:05Z on `tools/bg/backlog-ready-notifier.ts`:150 (association: MEMBER)

    <pre>
    Fixed in 20cf566c: both adapters now return `null` on non-zero exit or spawn error. isAgentQueueEmpty treats null as conservative-busy (returns false) — sensor failure must not trigger work assignment.
    </pre>

### Thread 5 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6B3ZWq`
- **Path**: `tools/bg/backlog-ready-notifier.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-13T19:17:57Z on `tools/bg/backlog-ready-notifier.ts`:147 (association: NONE)

  <pre>
  P1: New TypeScript tool `spawnSync` calls for git need the established `sonarjs/no-os-command-from-path` suppression with a rationale; nearby tools such as `tools/bg/standing-by-detector.ts:69-70` do this so `npm run lint:typescript` does not fail on PATH-resolved git invocations.

  This issue also appears on line 156 of the same file.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-13T19:25:06Z on `tools/bg/backlog-ready-notifier.ts`:147 (association: MEMBER)

    <pre>
    Fixed in 20cf566c: added `// eslint-disable-next-line sonarjs/no-os-command-from-path -- git/gh invoked as explicit args array; no shell, no injection risk.` on both spawnSync calls, matching the pattern in standing-by-detector.ts.
    </pre>

### Thread 6 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6B36ZU`
- **Path**: `tools/bg/backlog-ready-notifier.ts`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-13T19:51:20Z on `tools/bg/backlog-ready-notifier.ts`:177 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Request all open PRs before inferring agent idleness**

  The `gh pr list` call does not set `--limit`, so it only inspects the default page of results; the GitHub CLI manual states `--limit &lt;int&gt; (default 30)`. In repos with more than 30 open PRs, an agent can have an open PR outside that first page and still be marked idle here, causing false work assignments. Add an explicit limit/pagination (or author-filtered query) so the activity check is complete.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-13T19:56:49Z on `tools/bg/backlog-ready-notifier.ts`:177 (association: MEMBER)

    <pre>
    Fixed in 5e2df432: added `--limit 1000` to the `gh pr list` invocation (same pattern as `tools/backlog/autonomous-pickup.ts:546`). Pagination beyond the default 30 is now covered.
    </pre>

### Thread 7 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6B37hz`
- **Path**: `tools/bg/backlog-ready-notifier.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-13T19:52:23Z on `tools/bg/backlog-ready-notifier.ts`:177 (association: NONE)

  <pre>
  P1: `gh pr list` defaults to a limited result set, so this can report an agent queue as empty when that agent has an open PR beyond the default page. Other repo tools that need all open PRs pass an explicit high `--limit` (for example `tools/backlog/autonomous-pickup.ts:546` uses `--limit 1000`, and `tools/backlog/empty-queue-pickup.ts:147` uses `--limit 200`).

  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-13T19:56:43Z on `tools/bg/backlog-ready-notifier.ts`:177 (association: MEMBER)

    <pre>
    Fixed in 5e2df432: added `--limit 1000` to `gh pr list`, matching the pattern in `tools/backlog/autonomous-pickup.ts` and `tools/backlog/empty-queue-pickup.ts`. Agents with more than 30 open PRs will no longer be falsely classified as idle.
    </pre>

### Thread 8 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6B37ii`
- **Path**: `tools/bg/backlog-ready-notifier.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-13T19:52:24Z on `tools/bg/backlog-ready-notifier.ts`:198 (association: NONE)

  <pre>
  P1: The new conservative-busy branch for a failed git sensor is untested. Because this behavior exists specifically to prevent false "queue empty" assignments when git is unavailable, add coverage with `execGitLog` returning `null` so regressions cannot silently reintroduce the prior failure mode.

  This issue also appears on line 204 of the same file.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-13T19:56:55Z on `tools/bg/backlog-ready-notifier.ts`:198 (association: MEMBER)

    <pre>
    Fixed in 5e2df432: added two new tests in `isAgentQueueEmpty` describe block — one for `execGitLog` returning `null` and one for `execGhPrList` returning `null`. Both assert `false` (conservative-busy), ensuring sensor failure cannot silently trigger a work assignment. 33 tests pass.
    </pre>

### Thread 9 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6B37i3`
- **Path**: `tools/bg/backlog-ready-notifier.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-13T19:52:24Z on `tools/bg/backlog-ready-notifier.ts`:91 (association: NONE)

  <pre>
  P1: This entry contradicts the stated invariant that the map is keyed by canonical bus sender IDs: `tools/bus/types.ts:112` defines `SENDER_IDS` as only `otto`, `alexa`, `riven`, `vera`, and `lior`, so `aaron` cannot be supplied via `--agent` and the map no longer mirrors the bus protocol.

  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-13T19:56:37Z on `tools/bg/backlog-ready-notifier.ts`:91 (association: MEMBER)

    <pre>
    Fixed in 5e2df432: removed `aaron` from AGENT_MAP. `SENDER_IDS` in `tools/bus/types.ts` is `[otto, alexa, riven, vera, lior]` — aaron is a human, not a bus sender, and cannot appear as the `--agent` value. Added a comment to the map making this invariant explicit.
    </pre>

### Thread 10 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6B37jK`
- **Path**: `tools/bg/backlog-ready-notifier.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-13T19:52:24Z on `tools/bg/backlog-ready-notifier.ts`:166 (association: NONE)

  <pre>
  P1: This git format omits the commit subject, so agent-tagged commits whose marker appears only in the subject (for example `alexa`, `vera`, or `riven`) are missed and the queue can be reported empty. The existing metrics detector scans the full commit message (`tools/dashboard/generate-metrics.ts:60-65`), not just `%b`, for these same agent patterns.

  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-13T19:56:31Z on `tools/bg/backlog-ready-notifier.ts`:166 (association: MEMBER)

    <pre>
    Fixed in 5e2df432: changed git log format from `%b` (body only) to `%B` (raw body = subject + blank + body), so agent markers in the commit subject line (e.g. `alexa`, `vera`, `riven`) are now included in the match scan.
    </pre>

## Fix commits (touching thread paths)

### `8fd5d77c71dcb45d70cdac6fa536b90a874d1054` -- 2026-05-13T19:13:27Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/bg/backlog-ready-notifier.ts`

**Message:**

```
feat(bg): B-0441.3 — agent queue-state detection (commits + PRs)
```

### `2d181de15a357f373ee1e1e2859b165b1832ac4d` -- 2026-05-13T19:24:05Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/bg/backlog-ready-notifier.ts`

**Message:**

```
fix(bg): B-0441.3 — address Copilot/Codex review findings

- Lowercase AGENT_MAP keys to match canonical bus IDs (SENDER_IDS in
  tools/bus/types.ts); add case-insensitive lookup in isAgentQueueEmpty
- execGitLog: add --all flag to include feature-branch commits; surface
  null on spawn failure instead of silently returning empty string
- execGhPrList: same error-surface fix; null on spawn failure
- isAgentQueueEmpty: treat null adapter output as "busy" (conservative
  failure — sensor down must not trigger work assignment)
- Add sonarjs/no-os-command-from-path suppressions on both spawnSync calls
- Update test fakeAdapters to use lowercase key matching new convention

Co-Authored-By: Claude <noreply@anthropic.com>
```

### `5e2df432dec73d8e60ae5e91e9e8c96712267bb3` -- 2026-05-13T19:56:18Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/bg/backlog-ready-notifier.ts`

**Message:**

```
fix(bg): B-0441.3 — close 5 Copilot/Codex review threads

- %b → %B in git log format so subject-line agent markers (alexa, vera,
  riven) are not missed by isAgentQueueEmpty
- Remove aaron from AGENT_MAP: not in SENDER_IDS; human, not a bus sender
- Add --limit 1000 to gh pr list so agents with >30 open PRs are not
  falsely classified as idle
- Add null-sensor tests: execGitLog and execGhPrList returning null must
  both return false (conservative-busy), not trigger work assignment

33 tests pass (2 new).

Co-Authored-By: Claude <noreply@anthropic.com>
```
