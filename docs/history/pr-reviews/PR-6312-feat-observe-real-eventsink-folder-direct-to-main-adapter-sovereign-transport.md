# PR #6312 -- feat(observe): real EventSink — folder-direct-to-main adapter (sovereign transport)

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 6312 |
| Title | feat(observe): real EventSink — folder-direct-to-main adapter (sovereign transport) |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-06-01T01:59:25Z |
| Merged at | 2026-06-01T02:41:45Z |
| Merge commit SHA | `863d929aac587087f034d46aa6fc702b58893c8e` |
| Branch | `otto-cli/observe-event-sink-folder-2026-05-31` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/6312 |
| Changed files | 2 |
| Additions / deletions | +511 / -0 |

## Description

## Real EventSink — folder-direct-to-main adapter (the sovereign transport)

operator: *"build the real EventSink folder-direct-to-main adapter next."* The production impl of the `EventSink` port (`execute.ts`).

`folderSink(opts)` writes the chosen action as a **ZetaId-named JSON file** into a git folder and commits it **direct to `main`, no-PR** (081KSNY2Z0008QG0R000E5KTPX folders-on-main; same mechanism as the agent-bus G-Set, 081KSXN940008QG0R00171YAZW). **Corporate batch-to-main (081KSNY2Z0008QG0R0017JSTGD) = same envelope, different `commit` fn.**

**Honors Amara's review:**
- **#2 stable identity + idempotency** — `EventEnvelope = { id, at, by, action }` is the durable **fact** ("at t, actor X recorded this action"); `id` = `mintObserveEventIdHex` → **`Category.WorkItem`** ZetaId; the filename IS the id, so EEXIST is a no-op (G-Set CRDT).
- **#1 fact, not command** — logs the action as a fact with identity+actor+time (toward the richer executed-event envelope, the next refinement).
- **#5/#6 conflict discipline** — mirrors the bus `gitPushEnvelope`: on-main guard + fetch + 0-ahead check + add only this file + `--no-verify` + `push HEAD:main` with rebase-retry (disjoint ZetaId files → G-Set union, never a real conflict).
- **never throws** — every failure → `AppendOutcome { ok:false; reason }` (the sink authors its outcome channel); `execute` surfaces it as feedback.

I/O injectable (`mint`/`now`/`commit`) → fully testable, no real git, temp dir. **Composes with `execute()` end-to-end** (tested). The read side over this log = the eventually-consistent indexes ([081KSXN940008QG0R000R76H45](https://github.com/Lucent-Financial-Group/Zeta/blob/main/docs/backlog/P2/081KSXN940008QG0R000R76H45-git-native-eventually-consistent-text-indexes-sorted-inverted-graph-plus-git-native-hindsight-storage-interface-aaron-2026-05-31.md)).

Verified: tsc 0, eslint clean, **8/8 tests pass**. Additive (new file; no change to `observe.ts`/`execute.ts`).

🤖 Generated with [Claude Code](https://claude.com/claude-code)


## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | true |
| Total threads | 18 |
| Resolved threads | 18 |
| Unresolved threads | 0 |
| Total review comments | 24 |
| Total fix commits (touching thread paths) | 10 |

## Review threads

### Thread 1 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F_WpP`
- **Path**: `tools/observe/event-sink-folder.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-06-01T02:01:09Z on `tools/observe/event-sink-folder.ts`:122 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Validate existing event content before accepting EEXIST**

  When the minted id already exists, this treats the append as successful without checking that the existing file contains the same envelope. In any retry/collision path where the same id is reused with a different `action`, `by`, or `at`, the sink can either commit the stale file or report success for an event that was never durably recorded; the agent-bus implementation this mirrors compares existing content and surfaces collisions instead of silently accepting them.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 2 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F_WpU`
- **Path**: `tools/observe/event-sink-folder.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-06-01T02:01:09Z on `tools/observe/event-sink-folder.ts`:166 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Abort failed rebases before returning**

  If a push is rejected and `git pull --rebase origin main` hits a same-path conflict, this returns `ok:false` while leaving the checkout in an in-progress rebase. That breaks the advertised Result-only failure mode for the direct-to-main sink because the next append or any unrelated git operation is blocked until someone manually aborts; the bus publisher handles the same path by running `git rebase --abort` before surfacing the failure.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 3 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F_WpY`
- **Path**: `tools/observe/event-sink-folder.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-06-01T02:01:09Z on `tools/observe/event-sink-folder.ts`:155 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Derive the co-author trailer from the acting agent**

  This default commit path hardcodes every observe event as authored by Claude even though `FolderSinkOptions.by` is the acting agent id and the repo requires harness-specific `Co-Authored-By` trailers for Codex, Kiro, Grok, Gemini, etc. When Vera/Riven/Alexa/Gemini use this shared sink, the direct-to-main commits become misattributed, undermining the audit/coordination trail that the commit discipline is meant to preserve.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 4 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F_YBC`
- **Path**: `tools/observe/event-sink-folder.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-01T02:03:43Z on `tools/observe/event-sink-folder.ts`:133 (association: NONE)

  <pre>
  P1: Treating every `EEXIST` as a successful append breaks the G-Set invariant. If the same id is reused with different envelope content, this silently accepts a collision; if it is the same already-committed file, `folderSink` still calls `git commit` on an unchanged path and the real default commit returns a failure instead of an idempotent ok. The bus implementation this mirrors compares existing content and distinguishes identical replays from same-id/different-content collisions (`tools/agent-bus/publish.ts:31-65`), then only publishes newly-created files.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-06-01T02:09:59Z on `tools/observe/event-sink-folder.ts`:133 (association: MEMBER)

    <pre>
    Addressed in commit 56263dcc9 (gitCommitToMain L182-193): after `git add`, `git diff --cached --quiet -- &lt;path&gt;` gates the commit — when the already-landed file is byte-identical nothing stages, so the function returns `{ok:true}` WITHOUT calling `git commit` (the failing-on-unchanged-path case is avoided; idempotent re-append = ok). Content-collision (same id, different content) is surfaced as `{ok:false, reason:'id collision'}` in writeEventFile L131-134 (commit 782568f64). Verified: 11/11 tests pass incl. the idempotent-re-append + collision cases. Resolving.
    </pre>

### Thread 5 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F_YBV`
- **Path**: `tools/observe/event-sink-folder.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-01T02:03:44Z on `tools/observe/event-sink-folder.ts`:167 (association: NONE)

  <pre>
  P1: If `git pull --rebase` hits a same-path collision, this returns while leaving the checkout in an in-progress rebase state. The next observe append will then fail or operate on a dirty repository until a human aborts the rebase; the bus path explicitly aborts before surfacing this collision (`tools/agent-bus/publish.ts:136-154`).
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-06-01T02:10:16Z on `tools/observe/event-sink-folder.ts`:167 (association: MEMBER)

    <pre>
    Addressed in commit 782568f64 (gitCommitToMain L213-217): on `git pull --rebase` failure the function now runs `git rebase --abort` (best-effort, try/catch — a no-op if no rebase is in progress) BEFORE returning `{ok:false, reason:'push rejected and rebase failed (peer contention); rebase aborted'}`, so the checkout is never left mid-rebase for the next append. Mirrors the bus abort discipline (publish.ts). Verified by the rebase-failure test. Resolving.
    </pre>

### Thread 6 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F_YBd`
- **Path**: `tools/observe/event-sink-folder.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-01T02:03:44Z on `tools/observe/event-sink-folder.ts`:126 (association: NONE)

  <pre>
  P1: `envelope.id` is used as a path segment without validation. Because `mint` is injectable and `FolderSinkOptions` is exported, a caller can return values like `../outside` or a Windows-reserved name and make the sink write outside `eventDir` or fail cross-platform; the bus path guards its id/persona segments before joining (`tools/agent-bus/types.ts:59-86`). Reject non-canonical observe ids before constructing the filename.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-06-01T02:10:17Z on `tools/observe/event-sink-folder.ts`:126 (association: MEMBER)

    <pre>
    Addressed in commit 56263dcc9: `isCanonicalEventId` (L86-88, `/^[0-9a-f]{32}$/`) is checked in folderSink.append (L244-246) BEFORE writeEventFile constructs the path — a non-canonical id (`../outside`, Windows-reserved, etc.) returns `{ok:false}` and never reaches `join(eventDir, ...)`. writeEventFile is module-private with that single guarded caller, so no path is built from an unvalidated id. Verified by the non-canonical-id rejection test. Resolving.
    </pre>

### Thread 7 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F_YBi`
- **Path**: `tools/observe/event-sink-folder.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-01T02:03:44Z on `tools/observe/event-sink-folder.ts`:21 (association: NONE)

  <pre>
  P2: This is a current-state code file, not one of the history/research surfaces where contributor/persona names are preserved. Per the repository's no-name-attribution convention for code/current-state docs, use the role reference instead of the reviewer name here.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-06-01T02:10:18Z on `tools/observe/event-sink-folder.ts`:21 (association: MEMBER)

    <pre>
    Addressed in commit 56263dcc9: the persona name was replaced with the role reference throughout the doc comment — L12 'Fact, not command (per the design review)', L15 'design-review caution #2', L21 'Conflict discipline (design-review cautions #5/#6)'. No contributor/persona name remains in this current-state code file. Resolving.
    </pre>

### Thread 8 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F_Yf4`
- **Path**: `tools/observe/event-sink-folder.ts`
- **Replies**: 2

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-06-01T02:05:06Z on `tools/observe/event-sink-folder.ts`:128 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Skip commit for matched duplicate events**

  When an event id already exists with identical content, this returns only `{ ok: true, path }`, so `folderSink.append` proceeds to call the default `gitCommitToMain`. In a real checkout where the original event already landed, `git add` has no changes and `git commit -- &lt;path&gt;` exits non-zero, causing an idempotent retry to report `ok:false`; the injected fake commit in the unit test masks this. Preserve whether the file was newly created and skip the commit for the matched-EEXIST no-op.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-06-01T02:07:34Z on `tools/observe/event-sink-folder.ts`:128 (association: MEMBER)

    <pre>
    Handled in 56263dcc9: gitCommitToMain now runs `git diff --cached --quiet` after `git add` and returns `{ok:true}` when nothing is staged (file already committed + unchanged) — so the idempotent re-append never reaches `git commit`, no non-zero exit. The writeEventFile content-match → ok path routes through that guard.
    </pre>
  - **`AceHack` (human)** at 2026-06-01T02:10:00Z on `tools/observe/event-sink-folder.ts`:128 (association: MEMBER)

    <pre>
    Addressed in commit 56263dcc9 (gitCommitToMain L182-193): after `git add`, `git diff --cached --quiet -- &lt;path&gt;` gates the commit — when the already-landed file is byte-identical nothing stages, so the function returns `{ok:true}` WITHOUT calling `git commit` (the failing-on-unchanged-path case is avoided; idempotent re-append = ok). Content-collision (same id, different content) is surfaced as `{ok:false, reason:'id collision'}` in writeEventFile L131-134 (commit 782568f64). Verified: 11/11 tests pass incl. the idempotent-re-append + collision cases. Resolving.
    </pre>

### Thread 9 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F_akX`
- **Path**: `tools/observe/event-sink-folder.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-06-01T02:10:41Z on `tools/observe/event-sink-folder.ts`:222 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Undo or preserve the local event commit on push failure**

  When the event commit is created successfully but the later push/rebase path returns `ok:false`, the commit remains on local `main` even though `execute()` treats the append as failed and does not advance the world. In a transient push/auth outage or repeated peer contention, the next run will hit the ahead-of-origin guard, and a later manual reconcile can publish an event that callers were told did not land, causing the log projection to include an unacknowledged action. Either surface this as a pending/successful durable append or cleanly park/undo the local commit before returning failure.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 10 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F_dXy`
- **Path**: `tools/observe/event-sink-folder.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-06-01T02:18:49Z on `tools/observe/event-sink-folder.ts`:207 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Clean the index after failed pushes**

  When a push ultimately fails after the event commit exists, `reset --soft HEAD~1` removes the commit but deliberately leaves the event file staged. In the next append, if `origin/main` has advanced (the peer-contention path this retry loop is meant to handle), `git pull --rebase origin main` refuses to run with that staged residue — verified error: `cannot pull with rebase: Your index contains uncommitted changes` — so the sink can keep returning failures while an unacknowledged event remains staged in the checkout. Undo or park the failed event without leaving the main worktree index dirty before returning `ok:false`.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 11 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F_ddJ`
- **Path**: `tools/observe/event-sink-folder.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-01T02:19:04Z on `tools/observe/event-sink-folder.ts`:264 (association: NONE)

  <pre>
  P1: `append` can still reject/throw before returning an `AppendOutcome`: an injected `now()` that returns `NaN` makes `toISOString()` throw, and an injected `commit` can throw despite the sink's documented Result-only contract. Wrap the append body so these failures are converted to `{ ok: false; reason }` like the rest of the adapter.
  </pre>

### Thread 12 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F_ddP`
- **Path**: `tools/observe/event-sink-folder.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-01T02:19:04Z on `tools/observe/event-sink-folder.ts`:156 (association: NONE)

  <pre>
  P2: This prefix check is broader than the agent-bus mapping it says it mirrors (`tools/agent-bus/publish.ts:76-85`), which only matches the exact sender id or a hyphen-delimited prefix. Values like `ottobot` or `liorx` would be mis-stamped as known surfaces instead of falling back to the sender trailer.
  </pre>

### Thread 13 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F_fqD`
- **Path**: `tools/observe/event-sink-folder.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-06-01T02:24:13Z on `tools/observe/event-sink-folder.ts`:218 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Avoid hard-resetting unrelated worktree edits**

  When a push/rebase ultimately fails, this `reset --hard HEAD~1` runs in the caller's main checkout and resets the entire index and tracked worktree, not just the observe event path. In the common context where an agent has uncommitted edits in other tracked files while the observe sink appends an event, a transient push failure or same-path rebase conflict will silently discard those unrelated edits; use a path-scoped cleanup/temporary worktree or first require a clean worktree before taking this destructive rollback path.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 14 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F_fqH`
- **Path**: `tools/observe/event-sink-folder.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-06-01T02:24:13Z on `tools/observe/event-sink-folder.ts`:279 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Remove the event file when append fails**

  If the default commit path returns `ok:false` before creating a commit, such as on a feature branch, local-ahead main, fetch failure, or git identity/commit failure, `writeEventFile` has already left `&lt;eventDir&gt;/&lt;id&gt;.json` in the folder while `append` reports failure and `execute` does not advance the world. Any local projection that scans the folder, or a later broad `git add`, can then observe/publish an action that callers were told did not land; clean up or preflight git before writing the event.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 15 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F_hP0`
- **Path**: `tools/observe/event-sink-folder.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-06-01T02:28:18Z on `tools/observe/event-sink-folder.ts`:231 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Preserve dirty-checkout retries during peer contention**

  When an agent has unrelated unstaged tracked edits and a peer advances `origin/main` after this event commit is created, this `git pull --rebase` aborts before replaying the disjoint event (checked `git pull -h`: `--autostash` is the option that automatically stashes/pops local changes, and this call omits it). The sink then treats ordinary peer contention as a failed append and removes the event file, so observe events can repeatedly fail to land whenever the main checkout is also being used for active edits; use `--autostash`, a clean temporary worktree, or preflight a clean tree before committing.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 16 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F_h1K`
- **Path**: `tools/observe/event-sink-folder.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-01T02:30:03Z on `tools/observe/event-sink-folder.ts`:286 (association: NONE)

  <pre>
  P0: This cleanup can delete a durable event that existed before this append. `writeEventFile` returns `{ ok:true, path }` both for a newly-created file and for an `EEXIST` byte-identical replay; if the subsequent commit path fails before the no-op staging check (for example not on `main`, fetch failure, or an injected failing commit), `rmSync` removes the pre-existing event file. Preserve the G-Set invariant by distinguishing `created` from `exists-identical` and only deleting files created by this append.
  </pre>

### Thread 17 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F_h1Z`
- **Path**: `tools/observe/event-sink-folder.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-01T02:30:04Z on `tools/observe/event-sink-folder.ts`:313 (association: NONE)

  <pre>
  P1: A throwing injected `commit` bypasses the failed-append cleanup path, leaving a newly written event file in `eventDir` even though the append failed. Because the function comments explicitly allow injected `commit` to throw and promise failed appends leave no half-readable file, the commit call needs to be caught and routed through the same cleanup logic, while still preserving any pre-existing idempotent replay file.
  </pre>

### Thread 18 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F_kTh`
- **Path**: `tools/observe/event-sink-folder.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-06-01T02:35:29Z on `tools/observe/event-sink-folder.ts`:273 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Unstage the event path when commit creation fails**

  Fresh evidence in this revision is that this catch still returns immediately after failures from the `git commit` call, while `append` only removes the worktree file via `rmSync`. In a checkout where commit creation fails after `git add` has staged the new event, such as missing `user.name`/`user.email`, I verified `git status --short` leaves `AD events/x.json` after the file is removed; that dirty index can block the next pull/rebase or let a later broad commit publish an event the caller was told failed. Unstage the event path in this pre-commit failure path before returning `ok:false`.

  Useful? React with 👍 / 👎.
  </pre>

## Fix commits (touching thread paths)

### `ff1ffb6c85ac283beda73f13ccb1081b8a8f5c73` -- 2026-06-01T01:59:21Z -- ``

**Touched files (intersect with thread paths):**

- `tools/observe/event-sink-folder.ts`

**Message:**

```
feat(observe): real EventSink — folder-direct-to-main adapter (the so…

…vereign transport)

operator: "build the real EventSink folder-direct-to-main adapter next." Implements the
EventSink port from execute.ts as the production sovereign transport.

- folderSink(opts): writes the chosen action as a ZetaId-named JSON file into a git folder and
  commits it DIRECT TO main, no-PR (081KSNY2Z0008QG0R000E5KTPX folders-on-main; same mechanism as the agent-bus
  G-Set, 081KSXN940008QG0R00171YAZW). Corporate batch-to-main (081KSNY2Z0008QG0R0017JSTGD) = same envelope, different commit fn.
- EventEnvelope = { id, at, by, action } — the durable FACT (Amara: replay folds facts not
  commands), carrying a stable ZetaId identity (mintObserveEventIdHex → Category.WorkItem) so the
  log is idempotent (filename IS the id; EEXIST is a no-op = G-Set CRDT).
- gitCommitToMain mirrors the bus gitPushEnvelope: on-main guard + fetch + 0-ahead check + add
  ONLY this file + --no-verify + push HEAD:main with rebase-retry (disjoint ZetaId files →
  G-Set union, never a real conflict — Amara cautions #5/#6 conflict discipline).
- Never throws: every failure (not-on-main / local-ahead / write / push) → AppendOutcome ok:false;
  the sink authors its outcome channel (asymmetric-authorship); execute surfaces it as feedback.
- I/O injectable (mint/now/commit) → fully testable, no real git, temp dir.

Honors Amara review: #2 stable ZetaId identity + idempotency; #5/#6 conflict discipline; fact-form
envelope (toward the executed-event envelope #1). Composes with execute() end-to-end (tested).

Verified: tsc 0, eslint clean, 8/8 tests pass.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `782568f647a469b8b3cbf6a6d8d30ecc220041aa` -- 2026-06-01T02:03:22Z -- ``

**Touched files (intersect with thread paths):**

- `tools/observe/event-sink-folder.ts`

**Message:**

```
fix(observe): EEXIST content-check + rebase-abort + actor-derived tra…

…iler (Codex #6312)

Three Codex P2 fixes on the folder-direct-to-main sink:
- EEXIST is idempotent ok ONLY if existing content matches; a same-id-different-content collision
  now returns ok:false instead of silently accepting stale (true G-Set property).
- on rebase-conflict, git rebase --abort before returning ok:false so no dangling rebase blocks
  the next append / unrelated git op (Result-only clean-state contract).
- Co-Authored-By trailer derived from the acting agent (coauthorFor) — the sink is shared
  (Vera/Riven/Alexa/Lior); hardcoded Claude mis-attributed every non-Otto surface.

+ tests: same-id-different-content collision → ok:false; coauthorFor per-surface mapping. 10/10.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `56263dcc909b9a103d939a8628a01988a8a9c802` -- 2026-06-01T02:07:04Z -- ``

**Touched files (intersect with thread paths):**

- `tools/observe/event-sink-folder.ts`

**Message:**

```
fix(observe): canonical-id path guard + idempotent-already-committed …

…+ role-ref + persona-name (Copilot #6312)

Four Copilot fixes on the folder-direct-to-main sink:
- isCanonicalEventId guard: mint is injectable + FolderSinkOptions exported, so a caller could
  return "../outside" or a Windows-reserved name → path traversal. Reject anything but a 32-hex
  ZetaId before joining a path (mirrors the bus segment guards). Security-relevant. +test.
- idempotent-already-committed: after git add, if nothing is staged (file already committed +
  unchanged), return ok (git commit would fail "nothing to commit"); re-append of a landed
  event is idempotent ok, not a failure (git diff --cached --quiet guard).
- EEXIST content-check confirmed (collision → ok:false, not silent stale-accept).
- rebase --abort confirmed present (no dangling rebase).
- name-attribution: replaced persona name in the code docstring with role references
  (no-name-attribution-in-code convention; names preserved only in history/research).

11/11 tests; tsc 0; eslint clean.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `ce086cc0541ac715210db298ebdbd0b740cae665` -- 2026-06-01T02:15:52Z -- ``

**Touched files (intersect with thread paths):**

- `tools/observe/event-sink-folder.ts`

**Message:**

```
fix(observe): undo the local commit on push failure so the sink canno…

…t wedge (Codex #6312)

When the commit lands locally but push/rebase ultimately fails, the commit was left on local main
→ the next append ahead-check sees local ahead → every future append fails (wedged). Now both
post-commit failure paths git reset --soft HEAD~1 (un-commit, preserve the event file staged for a
later retry) before returning ok:false. Result-only contract: a failed append leaves no local
residue (no dangling rebase, no ahead-of-origin commit).

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `92838396074e28d9e476453590dea23f7f38c43a` -- 2026-06-01T02:21:56Z -- ``

**Touched files (intersect with thread paths):**

- `tools/observe/event-sink-folder.ts`

**Message:**

```
fix(observe): hard-reset cleanup + Result-only append wrap + exact co…

…author match (Codex/Copilot #6312)

Three more review fixes on the folder-direct-to-main sink:
- undoLocalCommit: reset --soft left the event file STAGED, which makes the next append git pull
  --rebase refuse on a dirty index → reset --hard instead (drop commit + file; clean at
  origin/main; the loop re-appends a fresh event next tick so nothing is lost).
- append wrapped in try/catch: an injected now()=NaN (toISOString throws) or a throwing injected
  commit now converts to { ok:false } per the documented Result-only contract.
- coauthorFor: exact-or-hyphen match (otto / otto-cli) not bare startsWith, so ottobot/liorx fall
  back to the sender trailer (mirrors the agent-bus matching).

+tests: ottobot/liorx fallback; now()=NaN → ok:false. tsc 0, eslint clean, 12/12.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `cb9dd53ed219ced6935e5433248595b826f573e6` -- 2026-06-01T02:26:08Z -- ``

**Touched files (intersect with thread paths):**

- `tools/observe/event-sink-folder.ts`

**Message:**

```
fix(observe): targeted undo (no hard reset) + remove event file on an…

…y failure (Codex #6312)

Two fixes — the first reverts a data-loss bug I introduced last round:
- fqD (P1): reset --hard HEAD~1 reset the WHOLE worktree → would wipe an agent概s concurrent
  uncommitted edits in other files. Targeted undo instead: reset --soft HEAD~1 (stages only our
  commit diff) + restore --staged -- <our path> (unstage only our file). Nothing else touched.
- fqH (P2): a pre-commit failure (not-on-main / ahead / fetch) left the written <id>.json on disk
  while append reported failure → loadWorld (reads the folder) could half-read an event that never
  landed. append now rmSync(written.path) on ANY commit failure. +assert file removed on failure.

Net: a failed append leaves zero residue (no commit, no file, no staged change) and never touches
unrelated worktree files. tsc 0, eslint clean, 12/12.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `6208812b223f6cff4eb77a36d363e326354282de` -- 2026-06-01T02:29:24Z -- ``

**Touched files (intersect with thread paths):**

- `tools/observe/event-sink-folder.ts`

**Message:**

```
fix(observe): --autostash on rebase so peer contention resolves on a …

…dirty checkout (Codex #6312)

The rebase-retry refused if the shared main checkout had unrelated unstaged tracked edits. git
pull --rebase --autostash stashes them, replays our disjoint ZetaId event, pops them back — the
disjoint-file event lands even on a dirty checkout; the pop never conflicts with our file.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `31722545e10ff3c5f4ac42d3e3307e995389e1e2` -- 2026-06-01T02:32:13Z -- ``

**Touched files (intersect with thread paths):**

- `tools/observe/event-sink-folder.ts`

**Message:**

```
fix(observe): failure-cleanup removes ONLY our own write; throw route…

…s through it too (Copilot #6312 P0/P1)

- P0: the failure rmSync could delete a PRE-EXISTING durable event — writeEventFile returns ok for
  both a new write and an EEXIST byte-identical replay, so a same-id append whose commit fails was
  deleting a landed G-Set event. writeEventFile now reports `created`; append cleans up only a file
  it created this append (created===true), never a pre-existing replay. +test (durable event preserved).
- P1: a THROWING injected commit bypassed cleanup, leaving a half-written file. The outer catch now
  routes through the same removeOurFile cleanup. +test (throwing commit → ok:false + file removed).

tsc 0, eslint clean, 14/14.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `40eb6fb3c907f0b2085df1c7f498f2c70f3fa545` -- 2026-06-01T02:36:13Z -- ``

**Touched files (intersect with thread paths):**

- `tools/observe/event-sink-folder.ts`

**Message:**

```
fix(observe): unstage our path if git commit creation fails (Codex #6…

…312)

If git commit fails AFTER git add (missing identity, commit hook), our path is left staged;
append rmSync would then leave a staged-add of a deleted file = dirty index blocking the next
rebase. The outer catch now best-effort git restore --staged -- <our path> so the index is clean
too. No-op if nothing was staged.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `3d1f23c03af9793fa1e2f04dd97f9c6f76cabe10` -- 2026-06-01T02:37:09Z -- ``

**Touched files (intersect with thread paths):**

- `tools/observe/event-sink-folder.ts`

**Message:**

```
refactor(observe): extract pushWithRebaseRetry to keep gitCommitToMai…

…n under complexity 15

The catch-unstage addition pushed gitCommitToMain over cognitive-complexity 15. Extracted the
push/rebase/autostash/undo retry loop into pushWithRebaseRetry(run, undoLocalCommit). Behavior
unchanged; tsc 0, eslint clean, 14/14. (Fixes the lint error in the prior commit on this branch.)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```
