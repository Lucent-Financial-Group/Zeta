# PR #9902 -- feat(gate): markdownlint findings scope to the PR's own diff (081KX3KA3ES)

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 9902 |
| Title | feat(gate): markdownlint findings scope to the PR's own diff (081KX3KA3ES) |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-08-01T17:28:50Z |
| Merged at | 2026-08-01T17:39:15Z |
| Merge commit SHA | `67795bf24c452d9a87e3bf9ac603fc8d0ea6270e` |
| Branch | `otto/gate-scoped-markdownlint` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/9902 |
| Changed files | 1 |
| Additions / deletions | +24 / -2 |

## Description

The priority-inversion fix applied where it bites (drift-and-heal ADR item 4, workitem `081KX3KA3ES08QG0R003TW3XDE`'s last open scope):

On `pull_request` the markdownlint job now pipes through `hygiene/scoped-lint.ts` with the merge-ref diff (`HEAD^1..HEAD` — the merge commit's first parent is current main, so this is exactly the PR's changes; first-party context only, no user-controlled ref names). **In-scope findings fail the job; out-of-scope findings print as informational drift** — the sweep ledger owns them and the SLO auto-files work when they linger. Push/main keeps the whole-tree run: that run IS the detector.

The `|| true` inside the pipe group is load-bearing: default shell is `bash -e -o pipefail`, and the verdict must come from scoped-lint (rightmost stage), not the raw linter.

Pattern is additive — other lint jobs adopt the same three lines when their lane wants the same signal quality.

Verified: actionlint clean; pipe semantics proven under `bash -e -o pipefail` (out-of-scope→exit 0, in-scope→exit 1, clean→exit 0). Completion move for the workitem follows post-merge.

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 0 |
| Resolved threads | 0 |
| Unresolved threads | 0 |
| Total review comments | 0 |
| Total fix commits (touching thread paths) | 0 |

## Review threads

(no review threads on this PR)

## Fix commits (touching thread paths)

(no commits in this PR touched files anchored by review threads)
