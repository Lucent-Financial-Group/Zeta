# PR #2058 -- feat(B-0279): add FileCheckpointStore with fsync

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 2058 |
| Title | feat(B-0279): add FileCheckpointStore with fsync |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-05-08T11:16:09Z |
| Merged at | 2026-05-08T11:27:54Z |
| Merge commit SHA | `d0fbdde1a0351841fae1ab7669fa38949cd6a23a` |
| Branch | `feat/b-0279-file-checkpoint-store` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/2058 |
| Changed files | 2 |
| Additions / deletions | +414 / -0 |

## Description

## Summary

- Adds `FileCheckpointStore` to `src/Core/Checkpoint.fs` — a file-based `ICheckpointStore` implementation with crash-safe persistence via atomic-rename + fsync (`FileStream.Flush(true)`)
- Binary format: `[tick: int64][count: int32][per-op: opId: int32, version: int32, dataLen: int32, data: byte[]]`
- Follows existing codebase patterns: `[<Sealed>]` attribute, `Path.GetFullPath` canonicalization (same as `WitnessDurableBackingStore`), `Directory.CreateDirectory` on construction
- 5 new tests in `tests/Tests.FSharp/Storage/CheckpointStore.Tests.fs` covering roundtrip, empty-load, multi-operator, overwrite, and cross-instance survival

## Test plan

- [x] `dotnet build -c Release` passes with 0 warnings, 0 errors
- [x] `dotnet test Zeta.sln -c Release --filter "CheckpointStore"` — all 9 tests pass (4 existing InMemory + 5 new File)
- [ ] Review that atomic-rename pattern (write temp, fsync, `File.Move` with overwrite) provides crash-safety guarantees

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | true |
| Total threads | 6 |
| Resolved threads | 5 |
| Unresolved threads | 1 |
| Total review comments | 11 |
| Total fix commits (touching thread paths) | 2 |

## Review threads

### Thread 1 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6Amota`
- **Path**: `src/Core/Checkpoint.fs`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-08T11:18:58Z on `src/Core/Checkpoint.fs`:86 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Sanitize circuit IDs before building checkpoint path**

  `checkpointPath` concatenates `circuitId` directly into `Path.Combine`, so rooted IDs (for example, `/tmp/x`) bypass `canonicalRoot`, and relative traversal segments like `..` can escape it. In those cases `SaveCheckpointAsync`/`LoadCheckpointAsync` can write or read outside the checkpoint root, which breaks isolation and can overwrite unrelated files when circuit IDs are externally influenced. Validate IDs as safe filenames (or canonicalize and enforce that the resolved path stays under `canonicalRoot`) before any file operations.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-08T11:24:55Z on `src/Core/Checkpoint.fs`:86 (association: MEMBER)

    <pre>
    Fixed in f2b1478. `checkpointPath` now calls `Path.GetFullPath` on the combined path and validates the result stays under `canonicalRoot` (using platform-appropriate case comparison), matching the `DiskBackingStore` pattern. A circuit ID containing `../` or absolute paths raises `ArgumentException`.
    </pre>

### Thread 2 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6Amote`
- **Path**: `src/Core/Checkpoint.fs`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-08T11:18:58Z on `src/Core/Checkpoint.fs`:157 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Fsync the checkpoint directory after rename**

  The write path flushes the temp file and then calls `File.Move`, but it never flushes the parent directory metadata after the rename. Under power-loss crashes, this can leave the renamed entry non-durable on some filesystems, so the latest checkpoint may be missing after reboot even though file data was flushed. Add a directory fsync (or platform-equivalent durable rename sequence) after replacement to match the crash-safe guarantee.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-08T11:24:57Z on `src/Core/Checkpoint.fs`:157 (association: MEMBER)

    <pre>
    Fixed in f2b1478. Added best-effort directory fsync after `File.Move` — opens the parent directory as a `FileStream` and calls `Flush(true)`. On Windows (where directory-open-as-FileStream is unsupported), the `IOException` / `UnauthorizedAccessException` is caught silently. This is a known .NET limitation — there is no portable directory fsync API. The checkpoint is still recoverable via the temp-file pattern; worst case under power loss on Windows is reverting to the prior checkpoint.
    </pre>

### Thread 3 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6Ampq5`
- **Path**: `src/Core/Checkpoint.fs`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-08T11:19:54Z on `src/Core/Checkpoint.fs`:106 (association: NONE)

  <pre>
  P0: `checkpointPath` uses `Path.Combine(canonicalRoot, circuitId + ".checkpoint")` without validating that the resulting full path stays under `canonicalRoot`. A `circuitId` containing path separators / `..` (or on Windows, an absolute path) can escape `rootDir` and overwrite arbitrary files. Consider the same defense as `DiskSpine.pathFor` (canonicalize the combined path, then `StartsWith` the root-with-separator using platform-appropriate comparison) and reject unsafe IDs (also consider rejecting `:` to avoid NTFS ADS).

  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-08T11:24:58Z on `src/Core/Checkpoint.fs`:106 (association: MEMBER)

    <pre>
    Fixed in f2b1478. `checkpointPath` now calls `Path.GetFullPath` on the result of `Path.Combine` and validates the canonical path starts with `canonicalRoot + DirectorySeparatorChar`, using platform-appropriate case comparison (same pattern as `DiskBackingStore.pathFor`). A circuit ID with path separators or `../` sequences raises `ArgumentException`.
    </pre>

### Thread 4 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6AmprY`
- **Path**: `src/Core/Checkpoint.fs`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-08T11:19:54Z on `src/Core/Checkpoint.fs`:157 (association: NONE)

  <pre>
  P0: The current “fsync + rename” sequence (`Flush(true)` then `File.Move`) does not guarantee rename durability on POSIX filesystems unless the containing directory entry is also synced. If this type is intended to be crash-safe on host power loss, consider fsync’ing the directory after the rename (or otherwise documenting the weaker guarantee).
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-08T11:24:59Z on `src/Core/Checkpoint.fs`:157 (association: MEMBER)

    <pre>
    Fixed in f2b1478. Added best-effort directory fsync after `File.Move` via `FileStream(dirPath, ...).Flush(true)`. This is best-effort because .NET has no portable directory-fsync API — on POSIX (Linux/macOS) this works; on Windows it throws `IOException` which we catch silently. Documented as a known limitation in the code comment.
    </pre>

### Thread 5 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6Amprp`
- **Path**: `src/Core/Checkpoint.fs`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-08T11:19:55Z on `src/Core/Checkpoint.fs`:178 (association: NONE)

  <pre>
  P1: `LoadCheckpointAsync` assumes the on-disk file is well-formed; a truncated/corrupt file will throw from `BinaryReader`/`Array.init` and can take down recovery. Consider validating `count` (e.g., non-negative, sane upper bound) and catching `EndOfStreamException`/`IOException` to return `ValueNone` (or delete/quarantine the bad checkpoint) instead of crashing.

  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-08T11:25:00Z on `src/Core/Checkpoint.fs`:178 (association: MEMBER)

    <pre>
    Fixed in f2b1478. `LoadCheckpointAsync` now wraps the entire read in a `try/with` catching `EndOfStreamException` and `IOException`, returning `ValueNone` (treats corrupt checkpoint as missing). Also validates `count &gt;= 0` before `Array.init` to catch negative-length corruption. The circuit falls back to event-stream replay (Temporal model) when checkpoint is missing.
    </pre>

### Thread 6 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM6AmxBs`
- **Path**: `src/Core/Checkpoint.fs`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-08T11:28:00Z on `src/Core/Checkpoint.fs`:205 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Reject incomplete operator payloads during checkpoint load**

  The loader assumes each operator blob is fully present, but `BinaryReader.ReadBytes(dataLen)` can return fewer bytes than requested at EOF without throwing, and negative `dataLen` throws `ArgumentOutOfRangeException` (not caught here). In both cases a corrupt/truncated checkpoint can still return `ValueSome` (or crash with an uncaught exception), so recovery may fail later when `LoadState` reads the short buffer instead of cleanly falling back to replay as intended by the method comments.

  Useful? React with 👍 / 👎.
  </pre>

## Fix commits (touching thread paths)

### `b85ff355565acbc1c892661bb653e19916918a1c` -- 2026-05-08T11:15:58Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `src/Core/Checkpoint.fs`

**Message:**

```
feat(B-0279): add FileCheckpointStore — file-based checkpoint with fsync

Implements ICheckpointStore with atomic-rename + fsync for
crash-safe operator state persistence. StableStorage mode
for the checkpoint control plane.

Co-Authored-By: Claude Opus 4.6 (1M context) <noreply@anthropic.com>
```

### `f2b14782cf10e0a8bfa0574dd1012ae73e1b1ed2` -- 2026-05-08T11:24:46Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `src/Core/Checkpoint.fs`

**Message:**

```
fix(PR-2058): path traversal guard, corrupt-file resilience

- Path traversal: checkpointPath now calls Path.GetFullPath on
  the combined path and validates the result stays under
  canonicalRoot, matching DiskBackingStore's pattern. A circuitId
  containing "../" or absolute paths raises ArgumentException.

- Directory fsync after rename: best-effort directory flush after
  File.Move for POSIX rename durability. On Windows where
  directory-open-as-FileStream is unsupported, the IOException
  is caught and silently swallowed (documented limitation).

- Corrupt file handling: LoadCheckpointAsync wraps the read in
  try/catch for EndOfStreamException and IOException, returning
  ValueNone (treat corrupt checkpoint as missing). Also validates
  count is non-negative before Array.init.

Co-Authored-By: Claude Opus 4.6 (1M context) <noreply@anthropic.com>
```
