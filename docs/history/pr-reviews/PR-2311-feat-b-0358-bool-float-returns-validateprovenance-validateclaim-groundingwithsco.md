# PR #2311 -- feat(B-0358): bool→float returns — validateProvenance/validateClaim + groundingWithScore

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 2311 |
| Title | feat(B-0358): bool→float returns — validateProvenance/validateClaim + groundingWithScore |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-05-09T15:55:28Z |
| Merged at | 2026-05-09T16:03:39Z |
| Merge commit SHA | `c590adf3fed91752e6761183dfbe34b83094c79e` |
| Branch | `feat/b0358-float-returns-veridicality-2026-05-09` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/2311 |
| Changed files | 5 |
| Additions / deletions | +112 / -37 |

## Description

## Summary

- **B-0358** (P1) — Bool as degenerate distribution: replace binary API returns with confidence scores.
- `validateProvenance` and `validateClaim` now return `float [0.0, 1.0]` instead of `bool`.
- Adds `groundingWithScore (scorer: string -> float)` as gradient companion to `groundingWith`, parallel to `falsifiabilityWithScore` (PR #2205).
- 10 Veridicality tests updated (`true`/`false` → `1.0`/`0.0`); 3 new `groundingWithScore` tests added.

## Motivation

`bool` encodes an epistemic claim of certainty. `float` encodes the honest primitive: certainty is one point on a confidence spectrum. Aaron 2026-05-09: *"imagine we are redesigning — bools don't exist, they are just a special case of a distribution that is either 0 or 1."*

Current implementations return `{0.0, 1.0}`. Future scorers can return intermediate trust values (e.g. `0.7` for weakly-trusted root) without API change. Callers that need binary decisions own the threshold: `if score >= 0.5 then ...`

## Acceptance criteria satisfied

| Criterion | Status |
|---|---|
| `validateProvenance` returns `float` | ✓ |
| `validateClaim` returns `float` | ✓ |
| `falsifiabilityWith` gradient variant | ✓ (pre-existing `falsifiabilityWithScore`, PR #2205) |
| `groundingWith` gradient variant | ✓ (`groundingWithScore` added) |
| All callers updated (tests only — no non-test callers) | ✓ |
| `antiConsensusGate` remains `Result` | ✓ |

## Test run

```
Passed! — Failed: 0, Passed: 891, Skipped: 1
0 Warning(s), 0 Error(s)
```

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | true |
| Total threads | 3 |
| Resolved threads | 3 |
| Unresolved threads | 0 |
| Total review comments | 4 |
| Total fix commits (touching thread paths) | 2 |

## Review threads

### Thread 1 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6A1KZU`
- **Path**: `src/Core/SignalQuality.fs`
- **Replies**: 1

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-05-09T15:56:53Z on `src/Core/SignalQuality.fs`:310 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Weight grounding scores by claim weight**

  `groundingWithScore` currently computes a simple mean over positively asserted keys (`total &lt;- total + 1`), so high-confidence claims (larger positive weights) influence the result no more than low-confidence claims. In this module, weights are documented as evidentiary confidence, so this can materially distort grounding when residual weights differ (e.g., one strongly-supported claim and one weak claim produce ~0.5 instead of a near-strong score). Use residual positive weight as the averaging denominator/numerator contribution.

  Useful? React with 👍 / 👎.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-09T16:01:05Z on `src/Core/SignalQuality.fs`:310 (association: MEMBER)

    <pre>
    Fixed.  now uses residual positive weight as the averaging weight:

    ```fsharp
    weightSum &lt;- weightSum + w
    scoreSum  &lt;- scoreSum + clamped_score * float w
    // final: scoreSum / float weightSum
    ```

    A claim with weight 9 now contributes 9× to both numerator and denominator vs a weight-1 claim. New test added: weight-9 strong + weight-1 weak → 0.9 (not the erroneous 0.5 from the simple mean). 4/4 groundingWithScore tests pass.
    </pre>

### Thread 2 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6A1KyQ`
- **Path**: `src/Core/Veridicality.fs`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-09T15:58:18Z on `src/Core/Veridicality.fs`:120 (association: NONE)

  <pre>
  The doc comment introduces a named attribution ("Amara") in a non-history surface (`src/Core/**`). Repo guidance is to avoid persona/human first-name attribution in code/docs outside the explicitly allowed history surfaces; use a role-ref or a neutral spec reference instead (e.g., refer to the aurora report path without naming the author).
  </pre>

### Thread 3 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6A1KyU`
- **Path**: `src/Core/SignalQuality.fs`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-09T15:58:18Z on `src/Core/SignalQuality.fs`:298 (association: NONE)

  <pre>
  The comment says the aggregate is a "weighted average", but the implementation increments `total` by 1 per asserted claim and does not weight by `Weight` magnitude. Either adjust the wording (e.g., "average") or change the calculation to actually weight by claim weights (if that was the intent).

  </pre>

## Fix commits (touching thread paths)

### `d6a00cc4506ee630e147d3358aff612c19101516` -- 2026-05-09T15:55:14Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `src/Core/SignalQuality.fs`
- `src/Core/Veridicality.fs`

**Message:**

```
feat(B-0358): bool→float returns — validateProvenance/validateClaim +…

… groundingWithScore

validateProvenance and validateClaim now return float [0.0, 1.0]
rather than bool. Bool is a degenerate Bernoulli distribution;
float is the honest primitive. Current implementations return
{0.0, 1.0}; future scorers can return intermediate trust values
without breaking callers (Aaron 2026-05-09: "bools don't exist,
they are just a special case of a distribution that is either 0 or 1").

Also adds groundingWithScore (string -> float scorer, gradient
companion to groundingWith), parallel to the existing
falsifiabilityWithScore landed in PR #2205. 3 new tests for
groundingWithScore; 10 Veridicality tests updated to 1.0/0.0.

All 891 tests pass (1 pre-existing skip), 0 warnings.

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>
```

### `4bad9e151d8043f9dd48382b5f9f9de3cdb8c57c` -- 2026-05-09T15:59:56Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `src/Core/SignalQuality.fs`
- `src/Core/Veridicality.fs`

**Message:**

```
fix(B-0358): address review threads — weight groundingWithScore by re…

…sidual Weight; neutralise persona names in Veridicality docs

Co-Authored-By: Grok <noreply@x.ai>
Co-authored-by: Cursor <cursoragent@cursor.com>
```
