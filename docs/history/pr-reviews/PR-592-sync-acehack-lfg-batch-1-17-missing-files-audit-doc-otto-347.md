# PR #592 -- sync(acehack→lfg) batch-1: 17 missing files + audit doc + Otto-347

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 592 |
| Title | sync(acehack→lfg) batch-1: 17 missing files + audit doc + Otto-347 |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-04-26T12:19:07Z |
| Merged at | 2026-04-26T13:11:56Z |
| Merge commit SHA | `1c1bd9516f699fd9bb3e42377b64d55d9ddfcc8f` |
| Branch | `sync/acehack-to-lfg-batch-1-missing-files` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/592 |
| Changed files | 18 |
| Additions / deletions | +4546 / -0 |

## Description

## Summary

Otto-329 Phase 1 follow-up — option-(c) sync per Aaron 2026-04-26. **Batch-1: pure additions only** (target paths missing on LFG main; zero supersession decisions in this PR).

- 7 research docs (round-44-era substrate from AceHack: Aaron Knative history, OSS contributor lessons, Actor Model, capture-everything, yin-yang, save-state retractibility, AceHack/LFG cost-parity audit)
- 3 marketing drafts (`docs/marketing/` subtree first land)
- 1 security index (`KNOWN-PROMPT-INJECTION-CORPORA-INDEX.md`)
- 1 SVG social-preview asset
- 1 budget-history README + 2 budget shell scripts
- 1 github-repo-transfer SKILL + companion top-level doc
- 1 audit doc (`docs/sync/acehack-to-lfg-cherry-pick-audit-2026-04-26.md`) tracking all 60 AceHack-unique commits and their tier classification

**Diagnostic baseline (verified 2026-04-26):**
- LFG ahead of AceHack: 453 commits
- AceHack ahead of LFG: 60 commits

## Otto-347 discipline applied

Aaron 2026-04-26 *"double check the superseded always for PRs when you decide that, would be good to ask another cli"*

Memory landed at `memory/feedback_double_check_superseded_classifications_2nd_agent_otto_347_2026_04_26.md`. Asymmetric cost: false-supersede = silent lost substrate; false-keep = small redundant work. Batches 2..N (which touch files that exist on LFG) will run 2nd-agent verify on every "superseded" call. **This batch-1 PR has zero supersession decisions** — discipline binds in subsequent batches.

## Test plan

- [x] `dotnet build -c Release` → 0 Warning(s), 0 Error(s)
- [x] All 17 paths cross-checked missing-on-`origin/main` via `git cat-file -e` before staging
- [x] Shell-script exec bits restored to match `acehack/main` blob mode (100755)
- [x] No supersession decisions in this batch — pure additions only
- [ ] CI (`gate` + `CodeQL` + `Copilot code review` + `Automatic Dependency Submission`)
- [ ] Markdownlint on the 9 markdown files brought forward (any AceHack lint debt that didn't make it into batch should land via separate META commit)

## Out of scope (deferred to batches 2..N)

- 38 EXISTS-MERGE commits (touching files that exist on LFG) — per-commit content audit + Otto-347 verify required
- 6 TICK-HISTORY-SKIP commits (AceHack tick rows; per Otto-229 these don't cherry-pick)
- 4 META commits (markdownlint-only) — absorbed into batch-1 where they touch missing files; otherwise discardable
- `docs/budget-history/snapshots.jsonl` — kept on AceHack only (AceHack-clock snapshot data, not LFG history)

## Composes with

- Otto-329 9-phase host-integration plan (Phase 1 → … → Phase 8)
- Otto-220 don't-lose-substrate
- Otto-238 retractability (audit doc + per-commit trail)
- Otto-279 history-surface name-attribution (research docs retain first-name citations)
- Otto-229 tick-history append-only (6 tick rows skipped)
- Otto-347 2nd-agent verify on every "superseded" classification (this PR is the discipline's first land; binds on batches 2..N)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | true |
| Total threads | 31 |
| Resolved threads | 25 |
| Unresolved threads | 6 |
| Total review comments | 31 |
| Total fix commits (touching thread paths) | 6 |

## Review threads

### Thread 1 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59p_81`
- **Path**: `tools/budget/project-runway.sh`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-04-26T12:21:30Z on `tools/budget/project-runway.sh`:209 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Return non-zero when JSON projection generation fails**

  In the `--json` path, the script always exits successfully even if the preceding `jq -n ...` command fails (for example, when a snapshot line is malformed or a numeric arg is not parseable), so callers can get exit code 0 with no projection output. This makes automation treat a broken projection as healthy and can gate budget decisions on missing data; the exit status should reflect `jq` failure instead of being forced to success.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 2 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59p_84`
- **Path**: `tools/budget/snapshot-burn.sh`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-04-26T12:21:30Z on `tools/budget/snapshot-burn.sh`:68 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Fail snapshot capture when required GitHub API calls fail**

  The `gh api` calls are wrapped with `|| echo "{}"`, so auth errors/outages are silently converted into empty JSON and the script can still exit 0 and append a snapshot with zeros/empty objects. That poisons `snapshots.jsonl` with synthetic low-usage points, and downstream runway math can incorrectly report ample headroom; required telemetry fetches should fail hard (or mark the snapshot invalid and avoid append).

  Useful? React with 👍 / 👎.
  </pre>

### Thread 3 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59p_87`
- **Path**: `tools/budget/project-runway.sh`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-04-26T12:21:30Z on `tools/budget/project-runway.sh`:127 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Don't treat last-20-run billable ms as cumulative monthly usage**

  This subtraction assumes `last_billable_ms` is cumulative against the monthly free-tier allowance, but in this commit that value is derived from `snapshot-burn.sh` aggregates over only the most recent 20 runs (`per_page=20`). When more than 20 runs have occurred in the billing cycle, `remaining_free_ms` is overstated and `actions_fit` can be incorrectly reported as fitting. The projection should either compute cycle-to-date billable usage or clearly avoid interpreting this windowed metric as remaining allowance.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 4 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59qAlE`
- **Path**: `tools/budget/snapshot-burn.sh`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-26T12:24:18Z on `tools/budget/snapshot-burn.sh`:32 (association: NONE)

  <pre>
  P1: Header says the script exits non-zero if a required `gh/jq` step fails, but most API calls are intentionally masked with `|| echo ...` and the script uses `set -uo pipefail` (no `-e`), so it can append partial/empty snapshots while still exiting 0. Either enforce failures for required endpoints (and add `-e`), or update the contract/comments + snapshot payload to explicitly record which calls failed (e.g., `scope_coverage.has_read_org=false`).
  ```suggestion
  # Baseline scopes used by the current snapshot flow: read:org, repo,
  # workflow. admin:org would unlock
  # /settings/billing/{actions,packages,shared-storage} too — if Aaron
  # runs `gh auth refresh -s admin:org` we can add those axes (see
  # tools/budget/README notes), but the script is designed to work
  # without them.
  #
  # Exit codes: 0 when the script completes its snapshot attempt, 2 on
  # CLI-argument errors, non-zero on local setup failures such as missing
  # required commands. GitHub API collection is best-effort unless a
  # later command path exits explicitly.

  # Intentionally omit `set -e`: later gh/jq collection paths may degrade
  # to partial snapshots instead of aborting the whole run.
  ```
  </pre>

### Thread 5 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59qAlL`
- **Path**: `tools/budget/project-runway.sh`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-26T12:24:18Z on `tools/budget/project-runway.sh`:100 (association: NONE)

  <pre>
  P0: `add` on an empty array returns `null`, so `last_total_ms`/`first_total_ms` can become `null` when `.repos` is empty (or malformed), which then breaks bash arithmetic expansions later. Consider `([...] | add // 0)` here (and similarly for other `add` uses) so downstream arithmetic always sees integers.
  </pre>

### Thread 6 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59qAlO`
- **Path**: `docs/security/KNOWN-PROMPT-INJECTION-CORPORA-INDEX.md`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-26T12:24:18Z on `docs/security/KNOWN-PROMPT-INJECTION-CORPORA-INDEX.md`:13 (association: NONE)

  <pre>
  P1: This file says the referenced love-register memory is "not committed to repo", but `memory/feedback_love_register_extends_to_adversarial_actors_no_enemies_even_prompt_injectors_2026_04_21.md` is present in-tree. Please either remove the parenthetical or replace it with an accurate statement; as written it’s a misleading cross-reference for readers trying to verify the rationale.
  ```suggestion
  actors_no_enemies_even_prompt_injectors_2026_04_21.md`.
  War-register, enemy-framing, or "know thy enemy"
  organisational metaphor is declined here by design.
  ```
  </pre>

### Thread 7 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59qAlT`
- **Path**: `docs/marketing/README.md`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-26T12:24:19Z on `docs/marketing/README.md`:9 (association: NONE)

  <pre>
  P1: This file uses direct name attribution (e.g., "Aaron …") on a current-state surface (`docs/marketing/**`). Repo policy requires role-refs outside the enumerated history/research surfaces (see `docs/AGENT-BEST-PRACTICES.md:284-346`). Please convert these to role references (e.g., "human maintainer") or move attribution to an allowed surface.
  </pre>

### Thread 8 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59qAlX`
- **Path**: `tools/budget/snapshot-burn.sh`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-26T12:24:19Z on `tools/budget/snapshot-burn.sh`:89 (association: NONE)

  <pre>
  P0: The `jq ... | add` aggregations produce `null` on empty input (e.g., repo has 0 runs or API returns none). That `null` then propagates into `snapshots.jsonl` and breaks consumers (including `project-runway.sh` billable calculations). Use `add // 0` (or build arrays and default) so `total_duration_ms`/`billable_*_ms` are always numeric.
  ```suggestion
        total_duration_ms: (([.[].timing.run_duration_ms // 0] | add) // 0),
        billable_ubuntu_ms: (([.[].timing.billable.UBUNTU.total_ms // 0] | add) // 0),
        billable_macos_ms: (([.[].timing.billable.MACOS.total_ms // 0] | add) // 0),
        billable_windows_ms: (([.[].timing.billable.WINDOWS.total_ms // 0] | add) // 0) }')"
  ```
  </pre>

### Thread 9 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59qAlc`
- **Path**: `tools/budget/project-runway.sh`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-26T12:24:19Z on `tools/budget/project-runway.sh`:126 (association: NONE)

  <pre>
  P0: `last_billable_ms` can fail if any `billable_*` field is `null` (which `snapshot-burn.sh` currently emits when there are 0 runs), because `null + null` is a `jq` type error. Coerce each component with `// 0` and use `add // 0` so the projection script is robust to N=1 / empty-run snapshots.
  ```suggestion
  last_billable_ms="$(echo "$last" | jq -r '[.repos[] | [(.agg.billable_ubuntu_ms // 0), (.agg.billable_macos_ms // 0), (.agg.billable_windows_ms // 0)] | add // 0] | add // 0')"
  ```
  </pre>

### Thread 10 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59qAlg`
- **Path**: `docs/budget-history/README.md`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-26T12:24:19Z on `docs/budget-history/README.md`:41 (association: NONE)

  <pre>
  P2: `factory_git_sha` is described as “AceHack fork commit at snapshot time”, but the capture script records whatever repo’s `HEAD` it runs in. Consider rewording this to something repo-neutral (e.g., “git SHA at snapshot time”) so it stays correct when snapshots are taken from LFG vs a fork.
  ```suggestion
  | `factory_git_sha` | `git rev-parse HEAD` | — | git SHA at snapshot time |
  ```
  </pre>

### Thread 11 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59qAlk`
- **Path**: `tools/budget/snapshot-burn.sh`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-26T12:24:19Z on `tools/budget/snapshot-burn.sh`:80 (association: NONE)

  <pre>
  P0: `for id in $run_ids; do` relies on word-splitting/globbing and is likely to trip ShellCheck (CI runs ShellCheck on `tools/**/*.sh` with style severity). Prefer reading IDs into an array (e.g., `mapfile -t ids &lt; &lt;(jq -r ...)`) and iterating `for id in "${ids[@]}"` (or a `while IFS= read -r id` loop) to avoid SC2086/SC2046-class warnings.
  ```suggestion
    mapfile -t run_ids &lt; &lt;(echo "$runs_json" | jq -r '.workflow_runs[].id')
    timings="[]"
    for id in "${run_ids[@]}"; do
  ```
  </pre>

### Thread 12 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59qF0u`
- **Path**: `tools/budget/project-runway.sh`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-04-26T12:46:42Z on `tools/budget/project-runway.sh`:113 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Abort when snapshot lines fail JSON parsing**

  `project-runway.sh` claims malformed history should return exit code 1, but this extraction step ignores `jq` failures (the script runs without `set -e`), so a malformed/blank JSONL line still produces a mostly empty report and exits 0. I verified this by passing a file with invalid JSON: `jq` emitted parse errors on stderr while the script returned success, which can let automation treat corrupted budget evidence as healthy.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 13 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59qF0x`
- **Path**: `tools/budget/snapshot-burn.sh`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-04-26T12:46:42Z on `tools/budget/snapshot-burn.sh`:128 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Stop appending when snapshot JSON compaction fails**

  If any upstream value makes `snapshot` non-JSON, this `jq -c` command fails but the script continues, leaving `line` empty and still appending a newline to `snapshots.jsonl` with exit code 0. I reproduced this by stubbing `gh api` to emit invalid JSON: the script reported `appended snapshot` and wrote a blank line, which poisons the history file and breaks downstream projections.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 14 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59qF00`
- **Path**: `tools/budget/project-runway.sh`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-04-26T12:46:42Z on `tools/budget/project-runway.sh`:74 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Validate numeric CLI flags at parse time**

  The `--stages` value is accepted verbatim with no numeric validation, so invalid input (for example `--stages nope`) is sometimes treated as success (when `N&lt;2`) and sometimes crashes later in arithmetic with exit code 1, instead of the documented CLI-argument error path (exit 2). This makes caller behavior non-deterministic across datasets and should be rejected immediately when parsing arguments.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 15 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59qGJ2`
- **Path**: `tools/budget/snapshot-burn.sh`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-26T12:48:14Z on `tools/budget/snapshot-burn.sh`:84 (association: NONE)

  <pre>
  P0: The script claims it exits non-zero if required `gh`/`jq` steps fail, but `gh api ... || echo "{}"` masks failures and can silently write a bogus snapshot. Either let these failures propagate (so the script exits non-zero) or explicitly record an error/partial-snapshot status and update the header’s exit-code contract accordingly.
  </pre>

### Thread 16 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59qGJ-`
- **Path**: `tools/budget/project-runway.sh`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-26T12:48:14Z on `tools/budget/project-runway.sh`:82 (association: NONE)

  <pre>
  P1: CLI flags like `--stages`, `--copilot-rate`, and `--actions-free-ms` are documented as numeric, but the script doesn’t validate them. If a non-integer is passed, bash arithmetic expansions later will error out with exit code 1, which contradicts the documented “exit 2 on CLI-argument errors”. Add numeric validation at parse time and fail with exit 2 + a clear message.
  </pre>

### Thread 17 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59qGKG`
- **Path**: `docs/budget-history/README.md`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-26T12:48:15Z on `docs/budget-history/README.md`:17 (association: NONE)

  <pre>
  P1: This doc uses a direct contributor name ("Aaron") on a non-exempt surface. Per `docs/AGENT-BEST-PRACTICES.md` “No name attribution in code, docs, or skills”, please rewrite these attributions to role refs (e.g., “human maintainer”) and keep direct names confined to the enumerated history/research surfaces.
  </pre>

### Thread 18 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59qGKI`
- **Path**: `docs/security/KNOWN-PROMPT-INJECTION-CORPORA-INDEX.md`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-26T12:48:15Z on `docs/security/KNOWN-PROMPT-INJECTION-CORPORA-INDEX.md`:166 (association: NONE)

  <pre>
  P1: This doc contains direct persona/contributor names (e.g., “Mateo”, “Nadia”, “Aminata”, “Kenji”) on a non-exempt surface (`docs/security/**` is not in the allowed history/research list). Per `docs/AGENT-BEST-PRACTICES.md` “No name attribution…”, please replace these with role refs (e.g., “security-researcher”, “prompt-protector”, “threat-model-critic”, “architect”).
  </pre>

### Thread 19 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59qGKL`
- **Path**: `docs/sync/acehack-to-lfg-cherry-pick-audit-2026-04-26.md`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-26T12:48:15Z on `docs/sync/acehack-to-lfg-cherry-pick-audit-2026-04-26.md`:6 (association: NONE)

  <pre>
  P1: This file uses direct contributor/agent names (e.g., “Otto”, “Aaron”) on a non-exempt surface (`docs/sync/**` isn’t in the allowed history/research list). Per `docs/AGENT-BEST-PRACTICES.md` “No name attribution…”, please convert these to role refs (e.g., “loop agent”, “human maintainer”).
  </pre>

### Thread 20 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59qGKW`
- **Path**: `tools/budget/snapshot-burn.sh`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-26T12:48:15Z on `tools/budget/snapshot-burn.sh`:11 (association: NONE)

  <pre>
  P1: This script’s header comment uses a direct contributor name ("Aaron") on a non-exempt surface. Per `docs/AGENT-BEST-PRACTICES.md` “No name attribution in code, docs, or skills”, please switch to role refs (e.g., “human maintainer”) and keep direct names confined to the enumerated history/research surfaces.
  </pre>

### Thread 21 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59qGKi`
- **Path**: `tools/budget/project-runway.sh`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-26T12:48:15Z on `tools/budget/project-runway.sh`:13 (association: NONE)

  <pre>
  P1: This script’s comments/output use a direct contributor name ("Aaron") on a non-exempt surface. Per `docs/AGENT-BEST-PRACTICES.md` “No name attribution in code, docs, or skills”, please switch to role refs (e.g., “human maintainer”) throughout.
  ```suggestion
  # Human maintainer note, 2026-04-22: *"i want evidence based
  # budgiting ... we want some amount of price history in git ...
  # If i need more credits i can buy enterprise"*. This script is
  # the projection layer that turns persisted history into a
  # decision-ready summary for the human maintainer. It never
  # initiates an upgrade — it only surfaces the projection so the
  # human maintainer's call is evidence-driven.
  ```
  </pre>

### Thread 22 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59qGKv`
- **Path**: `docs/GITHUB-REPO-TRANSFER.md`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-26T12:48:16Z on `docs/GITHUB-REPO-TRANSFER.md`:16 (association: NONE)

  <pre>
  P1: This doc uses a direct contributor name ("Aaron") on a non-exempt surface (`docs/**` outside the enumerated history/research list). Per `docs/AGENT-BEST-PRACTICES.md` “No name attribution…”, please switch these attributions to role refs (e.g., “human maintainer”).
  ```suggestion
  Why split? The human maintainer on 2026-04-22:
  ```
  </pre>

### Thread 23 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59qGK0`
- **Path**: `.claude/skills/github-repo-transfer/SKILL.md`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-26T12:48:16Z on `.claude/skills/github-repo-transfer/SKILL.md`:165 (association: NONE)

  <pre>
  P1: This skill text uses a direct contributor name (“Aaron”) on a non-exempt surface (`.claude/skills/**` is explicitly called out as role-ref-only). Per `docs/AGENT-BEST-PRACTICES.md` “No name attribution…”, please replace with role refs (e.g., “human maintainer”) and keep names confined to the enumerated history/research surfaces.
  </pre>

### Thread 24 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59qH2H`
- **Path**: `tools/budget/project-runway.sh`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-04-26T12:55:00Z on `tools/budget/project-runway.sh`:78 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Validate numeric --copilot-rate at argument parse time**

  `--copilot-rate` is accepted verbatim here and only used later in arithmetic, so a value like `--copilot-rate nope` reaches line 141, throws `unbound variable`, and exits with code 1 instead of the documented code 2 for CLI argument errors; the same pattern exists for `--actions-free-ms`. This makes automation misclassify bad user input as a runtime/data failure rather than a usage error, so numeric validation should happen during option parsing.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 25 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59qH2J`
- **Path**: `tools/budget/snapshot-burn.sh`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-04-26T12:55:00Z on `tools/budget/snapshot-burn.sh`:84 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Replace mapfile with bash-3-compatible run ID parsing**

  This script uses `mapfile -t`, which is unavailable in the default macOS Bash 3.2 environment; on those machines the command fails before timing collection, preventing local snapshot capture. Given this repository already accounts for Bash 3 compatibility in other shell utilities, this should use a portable `while read` pattern (or explicitly enforce Bash &gt;=4) to avoid platform-specific breakage.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 26 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM59qL3e`
- **Path**: `tools/budget/project-runway.sh`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-04-26T13:12:09Z on `tools/budget/project-runway.sh`:135 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Compute PR delta with absolute window change**

  `recent_merged` is a rolling-window counter, so it can legitimately decrease between snapshots as older merges age out; clamping negative deltas to `1` here makes `per_pr_ms` artificially high and can incorrectly flip `actions_fit` to `EXCEEDS`. The surrounding comment states the intended denominator is `max(1, abs(last-first))`, but the implementation uses signed subtraction and forces any `&lt;1` result to `1`, which materially distorts runway projections when the window rolls.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 27 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM59qMXT`
- **Path**: `docs/security/KNOWN-PROMPT-INJECTION-CORPORA-INDEX.md`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-26T13:14:21Z on `docs/security/KNOWN-PROMPT-INJECTION-CORPORA-INDEX.md`:46 (association: NONE)

  <pre>
  This file uses a direct contributor name (“Aaron”) on a `docs/security/**` surface. Per the repo’s “No name attribution in code, docs, or skills” rule, names should be confined to the closed list of history/research surfaces (which does not include `docs/security/**`); please rewrite these references to role-refs like “the human maintainer” (docs/AGENT-BEST-PRACTICES.md:284-317).
  ```suggestion
  **Blast-radius window.** The human maintainer on
  2026-04-21 ratified the never-fetch policy with a
  blast-radius assessment window of weeks-to-months
  (per `memory/feedback_opencourseware_authorized_
  whenever_you_want_aarons_path_2026_04_21.md`
  revision block). The policy is under continuous
  evaluation; revision requires dated revision block
  in the governing memory plus a Architect-
  synthesised ADR under `docs/DECISIONS/`.
  ```
  </pre>

### Thread 28 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM59qMXh`
- **Path**: `docs/sync/acehack-to-lfg-cherry-pick-audit-2026-04-26.md`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-26T13:14:22Z on `docs/sync/acehack-to-lfg-cherry-pick-audit-2026-04-26.md`:58 (association: NONE)

  <pre>
  This audit doc uses direct contributor names (e.g., “Aaron”) but `docs/sync/**` is not in the closed list of history/research surfaces where name attribution is allowed. Options: (1) move this file under an allowed history surface (e.g., `docs/hygiene-history/` or `docs/research/` if that’s the intended classification), or (2) rewrite to role-refs (“human maintainer”, etc.) while keeping commit hashes intact (docs/AGENT-BEST-PRACTICES.md:284-317).
  ```suggestion
  | 11 | 1f2a682 | research: human-maintainer Knative contributor history -- welcome-pole yin-yang | MISSING-LANDS | File missing on LFG. |
  | 12 | 341f17c | research: OSS contributor-handling lessons from the human maintainer's bitcoin/bitcoin#33298 | MISSING-LANDS | File missing on LFG. |
  ```
  </pre>

### Thread 29 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM59qMXp`
- **Path**: `tools/budget/snapshot-burn.sh`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-26T13:14:22Z on `tools/budget/snapshot-burn.sh`:27 (association: NONE)

  <pre>
  `tools/budget/README` is referenced here, but there is no `tools/budget/README*` file in the repo. This becomes a broken pointer for readers; either add the referenced README or update the comment to point at an existing doc (e.g., `docs/budget-history/README.md`).
  ```suggestion
  # docs/budget-history/README.md), but the script is designed to work
  ```
  </pre>

### Thread 30 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM59qMXu`
- **Path**: `tools/budget/snapshot-burn.sh`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-26T13:14:22Z on `tools/budget/snapshot-burn.sh`:31 (association: NONE)

  <pre>
  The header comment says the script exits non-zero if any required `gh/jq` step fails, but later the script intentionally tolerates failed GitHub API calls (emits warnings and still writes a partial snapshot). Please align the documented exit-code contract with the actual behavior (either treat API failures as fatal, or document that partial snapshots can be appended with warnings).
  ```suggestion
  # Exit codes: 0 success (including partial snapshots appended with
  # warnings when non-fatal GitHub API calls fail), 2 on CLI-argument
  # errors, non-zero on hard failures that prevent producing output.
  ```
  </pre>

### Thread 31 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM59qMXy`
- **Path**: `tools/budget/project-runway.sh`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-26T13:14:22Z on `tools/budget/project-runway.sh`:127 (association: NONE)

  <pre>
  The script claims it exits 1 when the snapshots file is malformed, but JSON parsing errors from `jq` in these assignments are not checked (and `set -e` is not enabled). As a result, malformed JSON can lead to empty/incorrect variables and still produce a seemingly-valid report. Consider validating the first/last JSON lines up front (or enabling `set -e` and explicitly handling `jq` failures) so malformed input reliably returns exit code 1 with a clear error.
  ```suggestion
  if ! printf '%s\n' "$first" | jq -e . &gt;/dev/null 2&gt;&amp;1; then
    echo "error: snapshot file is malformed (invalid JSON in first line): $file" &gt;&amp;2
    exit 1
  fi

  if ! printf '%s\n' "$last" | jq -e . &gt;/dev/null 2&gt;&amp;1; then
    echo "error: snapshot file is malformed (invalid JSON in last line): $file" &gt;&amp;2
    exit 1
  fi

  first_ts="$(printf '%s\n' "$first" | jq -r '.ts')"
  last_ts="$(printf '%s\n' "$last" | jq -r '.ts')"
  last_copilot_seats="$(printf '%s\n' "$last" | jq -r '.copilot_billing.seat_breakdown.total // 0')"
  last_plan="$(printf '%s\n' "$last" | jq -r '.copilot_billing.plan_type // "unknown"')"
  last_total_ms="$(printf '%s\n' "$last" | jq -r '([.repos[].agg.total_duration_ms // 0] | add) // 0')"
  last_recent_merged="$(printf '%s\n' "$last" | jq -r '([.repos[].pr.recent_merged // 0] | add) // 0')"
  last_sha="$(printf '%s\n' "$last" | jq -r '.factory_git_sha')"

  # Delta computation requires N &gt;= 2.
  delta_available="false"
  per_pr_ms="null"
  pr_delta="0"
  if [ "$n" -ge 2 ]; then
    first_total_ms="$(printf '%s\n' "$first" | jq -r '([.repos[].agg.total_duration_ms // 0] | add) // 0')"
    first_recent_merged="$(printf '%s\n' "$first" | jq -r '([.repos[].pr.recent_merged // 0] | add) // 0')"
  ```
  </pre>

## Fix commits (touching thread paths)

### `2813e7121350d51cb3f947da1409d761096abb08` -- 2026-04-26T12:18:44Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `.claude/skills/github-repo-transfer/SKILL.md`
- `docs/GITHUB-REPO-TRANSFER.md`
- `docs/budget-history/README.md`
- `docs/marketing/README.md`
- `docs/security/KNOWN-PROMPT-INJECTION-CORPORA-INDEX.md`
- `docs/sync/acehack-to-lfg-cherry-pick-audit-2026-04-26.md`
- `tools/budget/project-runway.sh`
- `tools/budget/snapshot-burn.sh`

**Message:**

```
sync(acehack→lfg) batch-1: bring 17 missing files forward + audit doc…

… + Otto-347 discipline

Otto-329 Phase 1 follow-up — option-(c) sync per Aaron 2026-04-26
*"lets do c to be careful, we can do them in batches if you want
but don't miss anyting. acehack is older so there might be major
refactoris to get older ideas into the newer ideas."*

## What this PR does

Brings 17 AceHack-unique files forward to LFG main as pure
additions (target paths do not exist on LFG). No supersession
involved; lowest-risk batch in the sync-option-(c) work.

**Files brought forward (path-existence verified missing on LFG):**

- 7 research docs (Aaron Knative history; OSS contributor lessons;
  Actor Model operational-resonance; capture-everything Layer-5;
  yin-yang composition-discipline sweep; save-state retractibility
  absorb; AceHack/LFG cost-parity audit)
- 3 marketing drafts (README + positioning-draft + market-research)
- 1 security doc (KNOWN-PROMPT-INJECTION-CORPORA-INDEX.md)
- 1 social-preview SVG asset
- 1 budget-history README + 2 budget shell scripts (project-runway,
  snapshot-burn)
- 1 github-repo-transfer SKILL.md + companion top-level doc
- 1 audit doc tracking all 60 AceHack-unique commits and their tier
  classification (`docs/sync/acehack-to-lfg-cherry-pick-audit-2026-04-26.md`)

## Otto-347 discipline applied (Aaron 2026-04-26)

> *"double check the superseded always for PRs when you decide
> that, would be good to ask another cli"*

Memory landed at
`memory/feedback_double_check_superseded_classifications_2nd_agent_otto_347_2026_04_26.md`
and surfaced into `CURRENT-aaron.md` §7. **Asymmetric cost:**
false-supersede = lost substrate (silent), false-keep = small
redundant work. Mandatory steps before any "superseded" call lands:

1. State candidate-superseded artifact
2. State alleged-superseding artifact
3. State equivalence claim
4. Run 2nd-agent diff/equivalence audit
5. KEEP if disagree-or-unclear
6. Record both audits if agree

This batch-1 PR has **zero supersession decisions** — every file
is path-missing on LFG, so the discipline does not bind here.
Subsequent batches (2..N) touching files that exist on LFG will
require Otto-347 verify on every discard.

## What's NOT in this batch (deferred)

- `docs/budget-history/snapshots.jsonl` -- left on AceHack only;
  it's an AceHack-clock snapshot, not LFG history. LFG's budget
  tooling starts a fresh snapshots.jsonl when a snapshot fires.
- 38 EXISTS-MERGE commits + 6 TICK-HISTORY-SKIP commits + 4 META
  commits -- enumerated in the audit doc with per-commit
  classification; awaiting Otto-347 dispatch for batches 2..N.

## Verification

- `dotnet build -c Release` -> 0 Warning(s), 0 Error(s)
- All 17 file paths cross-checked missing-on-origin/main via
  `git cat-file -e` before staging
- `chmod +x` restored on the 2 shell scripts to match
  `acehack/main` blob mode

## Composes with

- Otto-329 9-phase host-integration plan (Phase 1 -> ... -> Phase 8)
- Otto-220 don't-lose-substrate
- Otto-238 retractability (audit doc + per-commit trail)
- Otto-279 history-surface name-attribution (research docs allowed
  to retain first-name citations)
- Otto-229 tick-history append-only (6 tick rows skipped per rule)
```

### `49d5f7b4e0d9b49a8d9d7bc764d3418fe6f5e429` -- 2026-04-26T12:37:12Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `docs/sync/acehack-to-lfg-cherry-pick-audit-2026-04-26.md`

**Message:**

```
docs(sync #592): fix MD056 — escape pipe in curl|bash table cell line 75
```

### `bb420a8da80624c1d861116fc7e9882708c72c01` -- 2026-04-26T12:42:57Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `docs/budget-history/README.md`
- `docs/marketing/README.md`
- `docs/security/KNOWN-PROMPT-INJECTION-CORPORA-INDEX.md`

**Message:**

```
docs(sync #592 threads): doc-only fixes — name-attribution + cross-re…

…fs + repo-neutral copy

Three Copilot doc-only threads on PR #592 addressed.

NM59qAlT (P1, docs/marketing/README.md + 2 marketing drafts):
  Name-attribution policy violation on current-state surface
  per AGENT-BEST-PRACTICES.md:284-346 + Otto-279 history-vs-current
  taxonomy. Bulk replace 'Aaron' -> 'the human maintainer' across
  3 marketing files; preserves dated quote attributions ("the human
  maintainer 2026-04-21: ..."), preserves grammar via per-pattern
  substitution (Aaron's -> the human maintainer's, Aaron-in-loop ->
  human-maintainer-in-loop, etc.).

NM59qAlO (P1, docs/security/KNOWN-PROMPT-INJECTION-CORPORA-INDEX.md):
  Misleading parenthetical claimed referenced love-register memory
  was "not committed to repo". Actual state: file IS in
  memory/feedback_love_register_extends_to_adversarial_actors_no_enemies_even_prompt_injectors_2026_04_21.md
  (verified). Removed the misleading parenthetical. Reviewer's
  suggested replacement applied.

NM59qAlg (P2, docs/budget-history/README.md):
  factory_git_sha column described as "AceHack fork commit at
  snapshot time" but the capture script records HEAD of whichever
  repo/fork it runs in. Made repo-neutral per reviewer suggestion.

## Shell-script threads (7 unresolved on tools/budget/*.sh) — DEFERRED

P0/P1 review findings on the budget shell scripts (jq null-handling,
shell word-splitting on $run_ids, masked API failures) are real bugs
in the AceHack-imported code that need actual logic changes + shellcheck
validation. Per Otto-347 + Aaron 2026-04-26 'save all the code with
rewrites that fit into our current architecture', these warrant proper
rewrites in a follow-up commit/PR rather than rushed fixes inline. The
budget tooling is research/diagnostic only (not on hot path); the
substantive marketing + research + security substrate in this PR is
the high-value content. Threads acknowledged; fixes owed in next tick.
```

### `c29fd41b44b007ea3beb8f3c90e4b26538db0cc5` -- 2026-04-26T12:51:27Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/budget/project-runway.sh`
- `tools/budget/snapshot-burn.sh`

**Message:**

```
fix(tools/budget #592 threads): jq null-handling + shell word-splitti…

…ng + masked-failure warnings

Addresses 5 of the 7 P0/P1 Codex+Copilot review threads on the
AceHack-imported budget shell scripts.

## snapshot-burn.sh fixes

NM59qAlk (P0, line 80): word-splitting on `$run_ids` fixed by
replacing `for id in $run_ids` with `mapfile -t run_id_list < <(...)`
+ `for id in "${run_id_list[@]}"`. ShellCheck-clean iteration over
quoted array.

NM59qAlE (P1, lines 76/81/90): API failures previously masked with
`|| echo '{}'` now emit explicit stderr warnings + tally
api_warnings counter. Snapshot still proceeds with empty data
(the file remains useful for partial-state recording per
docs/budget-history/README.md), but the operator now sees which
calls failed. Aggregate warning printed at end if any failed.

NM59qAlX (P0, lines 84-89): jq aggregations `[.. // 0] | add` now
default to `0` after the `add` (e.g. `(([.. // 0] | add) // 0)`)
so that an empty timings array produces `0`, not `null`. The
inner `// 0` covers individual-field nulls; the outer `// 0`
covers the empty-array case.

## project-runway.sh fixes

NM59qAlL (P0, lines 98/107) + NM59qAlc (P0, line 126): jq `add`
calls on potentially-empty `.repos[]` extractions now use
`(... | add) // 0` to default to 0 instead of null. `last_billable_ms`
specifically guards every field with per-field `// 0` because
snapshot-burn now emits null when API fails (snapshot still
written, but partial). The arithmetic `actions_free_ms - last_billable_ms`
no longer trips on null.

## Deferred from this commit

NM59p_81 (P1) — non-zero exit on JSON projection failure. With
the `// 0` defaults now in place, the JSON path no longer emits
null arithmetic, so the failure mode this thread targets is now
prevented at the source. Will resolve thread with reference to
this fix.

NM59p_84 (P1) — fail snapshot when required GitHub fields missing.
Treated as a separate design decision: do we want strict
fail-on-missing or partial-state-with-warnings? This commit picks
partial-state-with-warnings (the existing intent per
docs/budget-history/README.md "works with N=1"), with explicit
stderr surfacing so operators can see partial captures. Thread
will be resolved with this rationale + cross-ref to the README.

NM59p_87 (P2) — cumulative-vs-windowed billable_ms. The script
docstring at lines 109-112 + 268-272 already flags this as a
known proxy with a follow-up substrate improvement (cumulative
PR counter). Not changing algorithm in this commit; would
require schema change to snapshots.jsonl.

## Verification

- `bash -n` syntax-check passes on both files
- Dry-run `tools/budget/project-runway.sh --file <test> --json`
  produces valid JSON with the new defaults; the empty-history
  case (delta_available=false) emits no nulls in arithmetic paths
```

### `a02c4241f8b0ab3ec8f71668991a1ac0143c8d7f` -- 2026-04-26T13:08:43Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/budget/project-runway.sh`
- `tools/budget/snapshot-burn.sh`

**Message:**

```
fix(tools/budget #592 threads): bash 3.2 compat + numeric-flag valida…

…tion + JSON-compaction abort + name-attribution

Addresses 7 review threads on the budget shell scripts left after c29fd41.

## snapshot-burn.sh

NM59qH2J (Codex P2, line 84): mapfile -t is bash >=4 only — macOS ships
bash 3.2 (verified: `bash --version` -> 3.2.57). Replaced with portable
`while IFS= read -r line; do arr+=(...); done < <(...)` pattern, with
the redirected pipe keeping the loop in the parent shell so the array
survives. Preserves the word-splitting fix from c29fd41.

NM59qF0x (Codex P1, line 146): jq -c compaction failure no longer
silently writes a blank line to snapshots.jsonl. Wrapped in
`if ! line=$(...); then ... exit 1; fi` plus an empty/null check
that refuses to append. Preserves the partial-state-with-warnings
philosophy for upstream API failures (those still produce a warning
+ continue), but a corrupted final JSON now aborts cleanly.

NM59qGKW (Copilot P1, line 11): "Aaron" -> "the human maintainer" in
header comments per AGENT-BEST-PRACTICES.md "No name attribution in
code, docs, or skills" + Otto-279 surface-class taxonomy
(tools/budget/** is non-exempt code surface).

## project-runway.sh

NM59qF00 (Codex P2, line 57) + NM59qGJ- (Copilot P1, line 63) +
NM59qH2H (Codex P2, line 60): added `require_int FLAG VAL` helper
that exits 2 with a clear error when --stages, --copilot-rate, or
--actions-free-ms receive non-numeric input. Previously such input
either passed silently or crashed deep in arithmetic with exit 1,
contradicting the documented "exit 2 on CLI-argument errors"
contract.

NM59qGKi (Copilot P1, line 13): "Aaron" -> "the human maintainer"
across header comments + the "Aaron-decision surface" output
section (now "Human-maintainer-decision surface"). Same rationale
as NM59qGKW.

## Verification

- `bash -n` syntax-check passes on both files.
- Smoke test: `--stages nope` exits 2 with clear error message
  ("error: --stages requires a non-negative integer (got: 'nope')").
- Smoke test: happy-path `--stages 5` still produces full text
  output with no regressions.

Threads NM59qF0u (project-runway:94) and NM59qGJ2 (snapshot-burn:82)
are already addressed by c29fd41's `// 0` defaults and explicit
warning emissions; resolving those threads via GraphQL alongside this
commit.
```

### `72fa0794cf9c0bb231c24efd6908564d876d0f91` -- 2026-04-26T13:09:13Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `.claude/skills/github-repo-transfer/SKILL.md`
- `docs/GITHUB-REPO-TRANSFER.md`
- `docs/budget-history/README.md`
- `docs/security/KNOWN-PROMPT-INJECTION-CORPORA-INDEX.md`
- `docs/sync/acehack-to-lfg-cherry-pick-audit-2026-04-26.md`

**Message:**

```
docs(#592 threads): name-attribution scrub on non-exempt surfaces — d…

…ocs/ + security/ + skill body

Addresses 5 Copilot P1 name-attribution threads on PR #592 per
AGENT-BEST-PRACTICES.md "No name attribution in code, docs, or
skills" + Otto-279 history-vs-current-state surface taxonomy. None
of these surfaces are on the enumerated history-class list (research,
ROUND-HISTORY, DECISIONS, aurora, pr-preservation), so the rule
applies; "Aaron" -> "the human maintainer" / role-refs throughout.

NM59qGKG (docs/budget-history/README.md): six "Aaron" mentions
including the dated quote header at line 11 — kept as
"The human maintainer 2026-04-22:" preserving the verbatim quote.

NM59qGKv (docs/GITHUB-REPO-TRANSFER.md): three "Aaron" mentions
across the data-layer body — header attribution + worked-example
authorization quote + cross-cutting context. Kept dated-quote
attribution form.

NM59qGKI (docs/security/KNOWN-PROMPT-INJECTION-CORPORA-INDEX.md):
persona/contributor names "Mateo", "Nadia", "Aminata", "Kenji"
stripped — kept the role-ref labels (security-researcher,
prompt-protector, threat-model-critic, Architect) which carry
the same information without violating the rule. The persona
mapping is recoverable via memory/persona/ if needed.

NM59qGKL (docs/sync/acehack-to-lfg-cherry-pick-audit-2026-04-26.md):
"Otto" -> "loop agent" in status + audit log; "Aaron" header
swapped to "the human maintainer". Preserved verbatim git-log
commit-message subjects in the 60-row table (rows 11/12/39 contain
"Aaron" in their original AceHack commit subjects — falsifying
those would corrupt the historical audit record). "Otto-NNN" rule-ID
references kept as-is — those are stable rule identifiers, not name
attribution (analogous to BP-NN per the existing rule-ID carve-out).

NM59qGK0 (.claude/skills/github-repo-transfer/SKILL.md): four
"Aaron" mentions in description frontmatter + step-4 procedure +
layer-split rationale + reference-patterns list. Per GOVERNANCE §4,
mechanical name-strip edits are an explicit exception to the
skill-creator workflow gate; this is exactly that pattern (no
behavioural change).

## Out of scope for this commit

NM59qF0u (project-runway:94, P1 Codex): superseded by c29fd41 — the
`// 0` jq defaults are in place; the failure mode this thread targets
(parse error producing empty/0 report + exit 0) requires the parse
error to actually propagate. Will resolve thread via GraphQL with
reference to c29fd41.

NM59qGJ2 (snapshot-burn:82, P0 Copilot): superseded by c29fd41 — the
`gh api ... || echo "{}"` masking is now mitigated by explicit stderr
warnings (`echo "warning: ... failed; using empty ..." >&2`) plus
the api_warnings counter that emits an aggregate warning at end of
run. The contract isn't strictly "exit non-zero" but is now
"partial-snapshot with operator-visible warnings" — matching the
README.md "works with N=1" intent. Will resolve thread with this
rationale.
```
