# PR #9217 -- feat(llmtv): add live readout cadence

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 9217 |
| Title | feat(llmtv): add live readout cadence |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-07-02T23:33:52Z |
| Merged at | 2026-07-02T23:53:57Z |
| Merge commit SHA | `b4829ea244741b893ebaf80b97f7e668d7054ec6` |
| Branch | `codex/llmtv-live-readout-cadence` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/9217 |
| Changed files | 5 |
| Additions / deletions | +530 / -28 |

## Description

## Summary

- add `llmtv-live-readout`, a source-owned cadence sink that drains the live replay bridge into replay JSON and zero-JS LLMTV HTML through injected IO
- update the UDP LLMTV demo to run through the live replay bridge/readout path and emit both `out.html` and `out.replay.json`
- preserve the latest rendered page on empty ticks, and return typed write failures instead of throwing through the room loop
- repair current-main markdownlint drift in the quantum phase handoff

## Validation

- `bun test src/Core.TypeScript/discovery/llmtv-live-readout.test.ts src/Core.TypeScript/discovery/llmtv-live-replay-bridge.test.ts src/Core.TypeScript/discovery/llmtv-replay-render.test.ts src/Core.TypeScript/discovery/llmtv-node.test.ts src/Core.TypeScript/discovery/llmtv-replay-recorder.test.ts`
- `bun src/Core.TypeScript/lint/lint-typescript.ts`
- `bun run preflight:quick`
- `bun src/Core.TypeScript/discovery/llmtv-node.demo.ts 3 /tmp/llmtv-live-readout-smoke.html /tmp/llmtv-live-readout-smoke.replay.json`
- `bun src/Core.TypeScript/hygiene/audit-agencysignature-main-tip.ts`

Agency-Signature-Version: 1
Agent: Vera
Agent-Runtime: OpenAI Codex
Agent-Model: GPT-5
Credential-Identity: shared git credential
Credential-Mode: shared
Human-Review: explicit
Human-Review-Evidence: chat
Action-Mode: human-directed
Task: none
Co-Authored-By: Codex <noreply@openai.com>


## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 2 |
| Resolved threads | 2 |
| Unresolved threads | 0 |
| Total review comments | 2 |
| Total fix commits (touching thread paths) | 2 |

## Review threads

### Thread 1 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6ODLAc`
- **Path**: `src/Core.TypeScript/discovery/llmtv-live-readout.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-07-02T23:37:10Z on `src/Core.TypeScript/discovery/llmtv-live-readout.ts`:75 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Preserve captured frames until both writes succeed**

  In `flushNow`, `bridge.drain(...)` clears the recorder before either output write runs. When `writeText` throws for the replay or HTML path (disk full, permissions, or a transient filesystem error), the method returns `write-failed` but the captured batch has already been discarded, so a retry cannot write the missed frames and the replay has an unrecoverable gap. Snapshot first and clear only after both writes succeed.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 2 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6ODLAe`
- **Path**: `src/Core.TypeScript/discovery/llmtv-live-readout.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-07-02T23:37:10Z on `src/Core.TypeScript/discovery/llmtv-live-readout.ts`:82 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Keep prior dwellers across partial readout ticks**

  Because this renders from only the drained batch, any non-empty tick overwrites the previous page with just the sources that emitted during that interval. If `readoutEveryMs` is shorter than a source's `publishEveryMs`, or one source misses a cadence while another publishes, the quiet source disappears from the HTML even though the live channel/TTL should still keep it visible; the empty-tick guard does not cover these partial ticks. Maintain a rolling table or otherwise render current society state rather than only the per-drain delta.

  Useful? React with 👍 / 👎.
  </pre>

## Fix commits (touching thread paths)

### `9ba26f208e359abfe141e63ec4fcac7087d57d52` -- 2026-07-02T23:33:13Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `src/Core.TypeScript/discovery/llmtv-live-readout.ts`

**Message:**

```
feat(llmtv): add live readout cadence

Add a source-owned LLMTV live readout sink that drains the live replay bridge on an injected scheduler cadence, writes zeta.llmtv.replay.v1 JSON, and renders the zero-JS LLMTV page through injected IO. Empty ticks skip instead of erasing the last page, and write failures return typed feedback instead of throwing through the room loop.

Wire the UDP demo through the replay bridge/readout path so a physical multicast run now emits both replay JSON and HTML. Also fix current-main markdownlint drift in the quantum phase handoff.

Validation:

- bun test src/Core.TypeScript/discovery/llmtv-live-readout.test.ts src/Core.TypeScript/discovery/llmtv-live-replay-bridge.test.ts src/Core.TypeScript/discovery/llmtv-replay-render.test.ts src/Core.TypeScript/discovery/llmtv-node.test.ts src/Core.TypeScript/discovery/llmtv-replay-recorder.test.ts

- bun src/Core.TypeScript/lint/lint-typescript.ts

- bun run preflight:quick

- bun src/Core.TypeScript/discovery/llmtv-node.demo.ts 3 /tmp/llmtv-live-readout-smoke.html /tmp/llmtv-live-readout-smoke.replay.json

Agency-Signature-Version: 1

Agent: Vera

Agent-Runtime: OpenAI Codex

Agent-Model: GPT-5

Credential-Identity: shared git credential

Credential-Mode: shared

Human-Review: explicit

Human-Review-Evidence: chat

Action-Mode: human-directed

Task: none

Co-Authored-By: Codex <noreply@openai.com>
```

### `5cc52e8653840acb7558a2663341b172042f5a47` -- 2026-07-02T23:45:04Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `src/Core.TypeScript/discovery/llmtv-live-readout.ts`

**Message:**

```
fix(llmtv): preserve live readout state

Keep the LLMTV live readout recorder frames queued until both replay JSON and HTML writes succeed, then clear the bridge recorder only after the external state lands. Maintain a rolling channel table so sources remain visible across partial readout ticks until TTL expiry instead of blinking out when they miss one cadence.

Validation:\n- bun test src/Core.TypeScript/discovery/llmtv-live-readout.test.ts src/Core.TypeScript/discovery/llmtv-live-replay-bridge.test.ts src/Core.TypeScript/discovery/llmtv-replay-render.test.ts src/Core.TypeScript/discovery/llmtv-node.test.ts src/Core.TypeScript/discovery/llmtv-replay-recorder.test.ts\n- bun run preflight:quick

Agency-Signature-Version: 1\nAgent: Vera\nAgent-Runtime: OpenAI Codex\nAgent-Model: GPT-5\nCredential-Identity: shared git credential\nCredential-Mode: shared\nHuman-Review: explicit\nHuman-Review-Evidence: chat\nAction-Mode: human-directed\nTask: none\nCo-Authored-By: Codex <noreply@openai.com>
```
