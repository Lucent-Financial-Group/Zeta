# PR #833 -- ops(active-trajectory): refresh 9-file table with hard-reset-safety classification + preflight + lint scope

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 833 |
| Title | ops(active-trajectory): refresh 9-file table with hard-reset-safety classification + preflight + lint scope |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-04-29T09:52:00Z |
| Merged at | 2026-04-29T09:54:20Z |
| Merge commit SHA | `4b0f71da5f14c544acfe55685f919b81cfb56ff7` |
| Branch | `zero-zero-zero-perfile-classification-2026-04-29` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/833 |
| Changed files | 2 |
| Additions / deletions | +76 / -36 |

## Description

Refresh of \`docs/active-trajectory.md\` per Amara 2026-04-29T09:50Z corrections + maintainer follow-up framing.

## Headline

**All 9 infra files verified ALREADY_RESOLVED or SAFE_TO_RESET_LFG_SUPERSEDES** (against current git state, not the 16h-old plan). Zero NEEDS_FORWARD_SYNC. Zero NEEDS_HUMAN_DECISION on the 9 infra files.

## Changes

1. **Reframe to hard-reset-safety language** (Amara): "merge-direction is a plan shape; content-loss is the reset gate; do not confuse them." Replace 5-class merge taxonomy with 5-class reset-safety taxonomy (\`SAFE_TO_RESET_LFG_SUPERSEDES\` / \`ALREADY_RESOLVED\` / \`NEEDS_FORWARD_SYNC\` / \`EXPLICIT_ACCEPT_LOSS_REQUIRED\` / \`NEEDS_HUMAN_DECISION\`).

2. **9-file table refresh** with current-git verification:
   - 3 ALREADY_RESOLVED: verifiers.sh, markdownlint-cli2.jsonc, scorecard.yml (identical between forks).
   - 6 SAFE_TO_RESET_LFG_SUPERSEDES: .mise.toml, resume-diff.yml, elan.sh, linux.sh, codeql.yml, gate.yml. Each has LFG content that supersedes AceHack; hard-reset improves AceHack.

3. **Hard-reset preflight section** (Amara addition): NARROW reachability-preservation pass for branches/worktrees/orphans that intersect 0/0/0 safety. NOT broad LOST-branch recovery. Best rule: *"Before hard-reset, preserve reachability. After hard-reset, recover history."*

4. **gate.yml row** captures the maintainer's 09:51-09:53Z framing: AceHack's \`pip install semgrep\` is wrong-per-the-uv-decision; LFG's \`pipx:semgrep\` (mise auto-routes through \`uv tool install\`) is the canonical declarative form aligned with the ace-package-manager-release target shape (matching \`../scratch\` functionality).

5. **Lint scope** adds \`docs/active-trajectory.md\` to \`tools/lint/no-directives-otto-prose.sh\` so the load-bearing prose surface is no longer invisible to the lint. Inline addition; centralization of the surface list remains the deferred follow-up #1 from the multi-AI roundup absorption packet.

## Hard-reset readiness

After this PR lands:

- 9 of 9 infra files: SAFE
- 21 of 21 other modified files: NOT YET PER-FILE VERIFIED (heuristic-projected SAFE)
- Hard-reset preflight: NOT YET RUN

**This PR does NOT execute hard-reset.** Hard-reset still requires explicit maintainer sign-off per the AceHack-LFG topology memory.

## Verification

- \`bash -n tools/lint/no-directives-otto-prose.sh\` clean.
- \`SCOPE=worktree tools/lint/no-directives-otto-prose.sh --strict\` correctly flags \`docs/active-trajectory.md\` as containing the canonical violation phrase ("Aaron's directive" — quoted in test-input register inside the gate.yml row's evidence). The trajectory file is now in scope; the flag is intentional surfacing of the quoted real-world drift example. Self-application test passes.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 4 |
| Resolved threads | 0 |
| Unresolved threads | 4 |
| Total review comments | 4 |
| Total fix commits (touching thread paths) | 1 |

## Review threads

### Thread 1 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM5-Y9CO`
- **Path**: `tools/lint/no-directives-otto-prose.sh`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-29T09:55:57Z on `tools/lint/no-directives-otto-prose.sh`:127 (association: NONE)

  <pre>
  P1: The inline comment here is internally inconsistent and brittle: it says the surface list is duplicated in “line 121”, but line 121 is the `fi` that ends the empty-CHANGED_FILES guard. It also refers to “tests.md”, but the fixtures live in `tools/lint/no-directives-otto-prose.tests.md`. Recommend rewording to reference the duplicated locations by meaning (e.g., “the rule-doc whitelist grep -vE…” and the `.tests.md` file) rather than hard-coded line numbers, and fix the filename reference.
  ```suggestion
  # Note: this surface list is duplicated in the rule-doc whitelist
  # `grep -vE` clause below and the manual fixtures in
  # tools/lint/no-directives-otto-prose.tests.md. Centralizing the
  # surface list into a single variable / config file is a deferred
  # follow-up (per the multi-AI roundup absorption packet at
  ```
  </pre>

### Thread 2 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM5-Y9Cq`
- **Path**: `docs/active-trajectory.md`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-29T09:55:58Z on `docs/active-trajectory.md`:82 (association: NONE)

  <pre>
  P1: `docs/active-trajectory.md` is a current-state operational doc, but this newly added text introduces persona-name attribution (“Amara …”) in multiple places. Per the repo’s “no name attribution outside history surfaces” convention, please replace these with role-refs (e.g., “external reviewer”, “review feedback”, “maintainer framing correction”) while keeping the timestamp if it matters.
  </pre>

### Thread 3 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM5-Y9C_`
- **Path**: `docs/active-trajectory.md`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-29T09:55:58Z on `docs/active-trajectory.md`:70 (association: NONE)

  <pre>
  P1: The parenthetical “would also trip the just-landed no-directives lint” looks incorrect for the example shown. The lint’s pattern (tools/lint/no-directives-otto-prose.sh `PATTERN`) matches forms like “Aaron’s directive” / “directive from Aaron” / “maintainer directive”, but not “Aaron 2026-04-28 directive”. Please either adjust the example to an actually-matching form (if you truly need it as a fixture) or drop the claim that this wording would be flagged.
  ```suggestion
  | `.github/workflows/gate.yml` | SAFE_TO_RESET_LFG_SUPERSEDES | AceHack has `Setup Python` + `pip install semgrep` steps. The maintainer 2026-04-29T09:51Z framing: *"pip-install is wrong, uv we decided a long time ago"* — i.e., AceHack's pip-install path violates a prior maintainer decision (uv-only for Python tool management). LFG installs semgrep via `install.sh` three-way-parity (`pipx:semgrep` from `.mise.toml`, which is uv-managed under the hood). LFG's approach is the canonical-per-decision form. AceHack's extra `tools/tla`/`tools/alloy` cache paths are ALREADY on LFG (in the `Cache verifier jars (TLC + Alloy)` step). The retry-attempts logic (`for attempt in 1 2 3 4 5; do`) is identical on both sides — the diff is just comment wording (AceHack uses "Aaron 2026-04-28 directive"; LFG uses "the human maintainer's 2026-04-28 input"). Hard-reset REMOVES the wrong-per-decision pip path, GAINS the correct uv path, and cleans the prose register toward the preferred role-ref form. The declarative pattern matches the broader factory direction (maintainer 2026-04-29T09:53Z: *"everything is declarative; we need all the functionality of `../scratch` — that's what we're aiming for for the ace package manager release"*). AceHack's imperative `pip install` step violates this; LFG's pin-in-`.mise.toml` form is declarative. |
  ```
  </pre>

### Thread 4 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM5-Y9DV`
- **Path**: `docs/active-trajectory.md`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-29T09:55:58Z on `docs/active-trajectory.md`:126 (association: NONE)

  <pre>
  P1: This step quotes/uses the maintainer’s first name (“NEEDS AARON SIGN-OFF”) inside a current-state doc. Please rephrase to a role-ref (e.g., “explicit maintainer sign-off required”) and, if you need to keep the exact quoted string, confine it to a clearly labeled verbatim quote block so the surrounding prose remains role-based.
  </pre>

## Fix commits (touching thread paths)

### `fdbfa9e05426707af4c7774557eae3d8eccab66f` -- 2026-04-29T09:50:56Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `docs/active-trajectory.md`
- `tools/lint/no-directives-otto-prose.sh`

**Message:**

```
ops(active-trajectory): refresh 9-file table with hard-reset-safety c…

…lassification + preflight + lint scope

Amara 2026-04-29T09:50Z corrections + maintainer follow-up:

1. Reframe from merge-direction to hard-reset-safety language
   (Amara: "merge-direction is a plan shape; content-loss is the
   reset gate; do not confuse them"). Replace the 5-class merge
   taxonomy with the 5-class reset-safety taxonomy:
     SAFE_TO_RESET_LFG_SUPERSEDES
     ALREADY_RESOLVED
     NEEDS_FORWARD_SYNC
     EXPLICIT_ACCEPT_LOSS_REQUIRED
     NEEDS_HUMAN_DECISION

2. Refresh the 9-file table against current git state (NOT the
   16h-old plan file). All 9 verified ALREADY_RESOLVED or
   SAFE_TO_RESET_LFG_SUPERSEDES (2026-04-29T09:50Z):
     - 3 ALREADY_RESOLVED (verifiers.sh, markdownlint-cli2.jsonc,
       scorecard.yml — content identical between forks)
     - 6 SAFE_TO_RESET_LFG_SUPERSEDES (.mise.toml, resume-diff.yml,
       elan.sh, linux.sh, codeql.yml, gate.yml — LFG has same or
       better content; hard-reset improves AceHack)
   No NEEDS_FORWARD_SYNC. No NEEDS_HUMAN_DECISION on the 9 infra
   files.

3. Add hard-reset preflight section (Amara addition): NARROW
   reachability-preservation pass for branches/worktrees/orphans
   that intersect 0/0/0 safety. NOT broad LOST-branch recovery.
   Best rule: "Before hard-reset, preserve reachability. After
   hard-reset, recover history."

4. Update next-action with steps 1-2 (non-destructive overnight-
   safe) and 3-5 (require maintainer presence + sign-off).

5. Update gate.yml row with the maintainer 2026-04-29T09:51-09:53Z
   framing: AceHack's `pip install semgrep` is wrong-per-the-uv-
   decision; LFG's `pipx:semgrep` in `.mise.toml` is the canonical
   declarative form aligned with the ace-package-manager-release
   target shape (matching `../scratch` functionality).

6. Add `docs/active-trajectory.md` to the no-directives-otto-prose
   lint surface scope (per Amara's catch that the just-landed
   load-bearing prose surface was invisible to the lint). Inline
   addition for now; centralization of the surface list is
   the deferred follow-up #1 from the multi-AI roundup absorption
   packet.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
```
