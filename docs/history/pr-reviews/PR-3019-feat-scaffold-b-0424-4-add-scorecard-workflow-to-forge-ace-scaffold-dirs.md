# PR #3019 -- feat(scaffold): B-0424.4 — add Scorecard workflow to Forge + ace scaffold dirs

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 3019 |
| Title | feat(scaffold): B-0424.4 — add Scorecard workflow to Forge + ace scaffold dirs |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-05-13T18:44:23Z |
| Merged at | 2026-05-13T18:47:55Z |
| Merge commit SHA | `2bd94083ee339537d29a2cb4503975e8a8431aad` |
| Branch | `feat/b-0424-4-scaffold-scorecard-workflows-2026-05-13` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/3019 |
| Changed files | 5 |
| Additions / deletions | +172 / -10 |

## Description

## Summary

- Adds `.github/workflows/scorecard.yml` to both `tools/scaffold/forge/` and `tools/scaffold/ace/` template directories so that when `create-repo.ts --apply` runs, the Scorecard workflow is pushed to the new repos on day one instead of being a post-creation manual task.
- Removes "Wire OpenSSF Scorecard workflow" from the manual-steps list in `create-repo.ts` step 07, `tools/scaffold/README.md`, and the post-creation summary in `.github/workflows/scaffold-stage1-create-repos.yml`.
- Manual steps count: **7 → 6** (post-apply remainder).

## What this is

**B-0424.4** — the fourth bounded slice of [B-0424 (P1) Three-repo split Stage 1](../docs/backlog/P1/B-0424-three-repo-split-stage1-create-forge-ace-with-scaffolding-aaron-2026-05-13.md).

Previous slices:
- B-0424.1 (#2994) — governance file templates + dry-run `create-repo.ts` tool
- B-0424.2 (#2996) — `workflow_dispatch` GitHub Actions gate for `--apply`
- B-0424.3 (#3003) — `UPSTREAM-RHYTHM.md` templates

This slice is purely additive: two new scaffold files, three one-line deletions in supporting docs/code.

## Scorecard workflow provenance

`scorecard.yml` is copied verbatim from Zeta's existing reviewed workflow (same SHA-pinned actions, same security commentary). No new action SHAs were introduced; the same content-review that covers Zeta covers Forge and ace.

Workflows have **no `run:` blocks** — only action invocations with static inputs. The only `${{ ... }}` expressions are `github.workflow` and `github.ref` in the concurrency group (trusted contexts; the security hook's injection concern does not apply).

## Checks

```
bun tools/scaffold/create-repo.ts --repo forge --dry-run
→ step 06 files: 12 (was 11), includes .github/workflows/scorecard.yml
→ step 07 manualSteps: 6 (was 7), "Wire OpenSSF Scorecard" removed

bun tools/scaffold/create-repo.ts --repo ace --dry-run
→ same (ace also gets 12 files)

dotnet build -c Release
→ Build succeeded. 0 Warning(s). 0 Error(s).
```

## Test plan

- [x] Dry-run output shows `scorecard.yml` in step-06 file list for both repos
- [x] Dry-run step-07 `manualSteps` no longer includes "Wire OpenSSF Scorecard"
- [x] `dotnet build -c Release` passes 0 warnings 0 errors
- [ ] (After `--apply`) Verify `scorecard.yml` is present in created repos and the workflow appears in Actions tab

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 3 |
| Resolved threads | 0 |
| Unresolved threads | 3 |
| Total review comments | 3 |
| Total fix commits (touching thread paths) | 1 |

## Review threads

### Thread 1 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM6B26ny`
- **Path**: `tools/scaffold/README.md`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-13T18:48:04Z on `tools/scaffold/README.md`:76 (association: NONE)

  <pre>
  This manual-step list no longer matches `create-repo.ts` step 07: the tool still prints a sixth required step to add `bun-test`, `bun-lint`, `codeql`, and `scorecard` as required status checks after the CI workflows are wired. Dropping it from the README makes the documented post-apply checklist incomplete (and also contradicts the PR's 7→6 manual-step count).
  </pre>

### Thread 2 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM6B26oe`
- **Path**: `.github/workflows/scaffold-stage1-create-repos.yml`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-13T18:48:04Z on `.github/workflows/scaffold-stage1-create-repos.yml`:158 (association: NONE)

  <pre>
  This post-creation summary no longer mirrors `create-repo.ts` step 07: it omits the remaining required-status-checks step (`bun-test`, `bun-lint`, `codeql`, `scorecard`) that the tool still reports. Because this workflow is the operator-facing output after `--apply`, the missing item can leave the new repos with branch protection contexts never added.

  </pre>

### Thread 3 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM6B26pE`
- **Path**: `tools/scaffold/forge/.github/workflows/scorecard.yml`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-13T18:48:05Z on `tools/scaffold/forge/.github/workflows/scorecard.yml`:31 (association: NONE)

  <pre>
  Adding this workflow template increases each scaffold directory's file tree, but `tools/scaffold/README.md` still documents only `.github/copilot-instructions.md` under `.github/` and does not list `.github/workflows/scorecard.yml`. Please update the template-file tree so the scaffold documentation matches the files now shipped.
  </pre>

## Fix commits (touching thread paths)

### `a50553e54ef1aed7852fccc842ebda07e21bc6b3` -- 2026-05-13T18:44:01Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `.github/workflows/scaffold-stage1-create-repos.yml`
- `tools/scaffold/README.md`
- `tools/scaffold/forge/.github/workflows/scorecard.yml`

**Message:**

```
feat(scaffold): B-0424.4 — add Scorecard workflow to Forge + ace scaf…

…fold dirs

Moves "Wire OpenSSF Scorecard" from the manual-steps-after-apply list
into the day-one files pushed by step 06.  When --apply runs, the new
repos receive .github/workflows/scorecard.yml automatically — one fewer
post-creation manual task.

Changes:
- tools/scaffold/forge/.github/workflows/scorecard.yml  (new — SHA-pinned,
  copied verbatim from Zeta's reviewed workflow)
- tools/scaffold/ace/.github/workflows/scorecard.yml    (same)
- tools/scaffold/create-repo.ts step07: remove Scorecard from manualSteps
- tools/scaffold/README.md: manual steps 6 → 5
- .github/workflows/scaffold-stage1-create-repos.yml: echo list 6 → 5

Dry-run verified: step 06 now shows 12 scaffold files (was 11);
step 07 manual steps shrank from 7 to 6.
dotnet build -c Release: 0 warnings, 0 errors.

operative-authorization: aaron 2026-05-13: "Cooling period: TBD. The memory file IS the durable record"

Co-Authored-By: Claude <noreply@anthropic.com>
```
