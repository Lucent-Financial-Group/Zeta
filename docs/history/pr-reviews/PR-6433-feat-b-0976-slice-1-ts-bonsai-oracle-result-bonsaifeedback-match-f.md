# PR #6433 -- feat(081KT07NV0008QG0R003BE6MJ2 slice 1): TS Bonsai oracle → Result<_, BonsaiFeedback> (match F#)

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 6433 |
| Title | feat(081KT07NV0008QG0R003BE6MJ2 slice 1): TS Bonsai oracle → Result<_, BonsaiFeedback> (match F#) |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-06-01T16:41:59Z |
| Merged at | 2026-06-01T16:51:37Z |
| Merge commit SHA | `cd744c6982bf599c1517dd39be3fc70103abc687` |
| Branch | `otto-cli/b0976-bonsai-ts-result-2026-06-01` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/6433 |
| Changed files | 2 |
| Additions / deletions | +297 / -118 |

## Description

Converts the TS reference Bonsai oracle from throw to a pure-`Result` contract, **matching the F# oracle (#6429)** — "make them match." Same node-set, same canonical bytes, same `BonsaiFeedback` variant set, same error channel.

## What changed
- `serialize`/`parse` return `Result<_, BonsaiFeedback>` — no exception crosses the boundary (result over throw). TS owns a **minimal zero-dep `Result` port** (no BCL Result in JS; a consumer can adapt to neverthrow at the seam — hexagonal).
- `BonsaiFeedback` **mirrors the F# DU exactly** (the shared cross-oracle payload): `UnsupportedVersion / MalformedJson / UnknownKind / UnknownConstTag / UnknownOp / ExpectedString / ExpectedBool / ExpectedInt / NonSafeInt / TooDeep / NonCanonical`. `where` fields are JSON-pointer keys (ProblemDetails-ready for the accumulate-mode).
- Adopts the shared **`BONSAI_MAX_DEPTH = 1024`** cap (TS lacked one → now matches F#'s `TooDeep` + the cross-oracle depth contract).
- Constructors total (validation at the boundary, matching F#'s raw DU); `serialize`/`parse` **total even on `null`** (null string field → `ExpectedString`; null input → `MalformedJson`).
- Rejections asserted by **specific variant** (cross-oracle rejection-vector contract); canonical-only parse + byte-exact golden parity preserved.

**49 tests, `tsc --noEmit` 0 errors.** No external consumers of the slice-1 module, so the signature change breaks nothing.

Internal mechanism: a private `BonsaiFail` adapted to `Result` at the two boundaries (contract-vs-mechanism split; matches F#). Fail-fast (monadic); the **accumulate-mode (RFC-9457 ProblemDetails)** is the additive next slice per 081KT07NV0008QG0R003BE6MJ2 (the `where`-keyed payload is already designed for it).

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | true |
| Total threads | 6 |
| Resolved threads | 6 |
| Unresolved threads | 0 |
| Total review comments | 6 |
| Total fix commits (touching thread paths) | 3 |

## Review threads

### Thread 1 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GL-CS`
- **Path**: `src/Core.TypeScript/bonsai/bonsai.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-06-01T16:44:27Z on `src/Core.TypeScript/bonsai/bonsai.ts`:302 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Enforce the depth limit while parsing**

  When `parse` receives a deeply nested but otherwise valid Bonsai JSON document, it calls `parseNode` recursively with no depth counter and only checks `BONSAI_MAX_DEPTH` later by re-serializing the fully built tree. Inputs above the cap are therefore still recursively allocated first, and sufficiently deep inputs can throw a JS `RangeError` out of `parse` instead of returning a `Result` with `TooDeep`, defeating the new no-exceptions/depth-guard contract for untrusted parser input.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 2 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GMAHc`
- **Path**: `src/Core.TypeScript/bonsai/bonsai.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-01T16:46:32Z on `src/Core.TypeScript/bonsai/bonsai.ts`:286 (association: NONE)

  <pre>
  P0: `parseNode` recurses without any depth bound. A maliciously deep JSON input can cause a stack overflow before the later canonical round-trip check runs, defeating `BONSAI_MAX_DEPTH`'s DoS guard and the stated “serializer/parser bounded” contract. Add the same depth check used by `emitAt` to the parser recursion.
  </pre>

### Thread 3 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GMAHw`
- **Path**: `src/Core.TypeScript/bonsai/bonsai.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-01T16:46:32Z on `src/Core.TypeScript/bonsai/bonsai.ts`:314 (association: NONE)

  <pre>
  After changing `parseNode` to take a `depth` parameter (to enforce `BONSAI_MAX_DEPTH`), the top-level call in `parse` must pass the initial depth (e.g., 1) so the function compiles and the depth contract is applied consistently.
  </pre>

### Thread 4 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GMAH_`
- **Path**: `src/Core.TypeScript/bonsai/bonsai.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-01T16:46:33Z on `src/Core.TypeScript/bonsai/bonsai.ts`:216 (association: NONE)

  <pre>
  `serialize` should validate `binary.op` at the boundary the same way `parse` does. Right now `emitAt` will happily serialize an invalid operator string (possible from untyped JS callers via casts), producing canonical bytes that peer oracles will reject. Decline with `UnknownOp`/`ExpectedString` by reusing `asBinOp`.
  </pre>

### Thread 5 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GMAIU`
- **Path**: `src/Core.TypeScript/bonsai/bonsai.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-01T16:46:33Z on `src/Core.TypeScript/bonsai/bonsai.ts`:201 (association: NONE)

  <pre>
  `emitConst`'s `bool` case currently uses truthiness (`c.v ? ...`) without validating the runtime type. If an untyped caller constructs `{t:"bool", v: 1}` this will serialize as `true`, violating the cross-oracle contract (the parser would decline `ExpectedBool`). Add a `typeof` check and decline `ExpectedBool` to keep `serialize` a strict boundary.
  </pre>

### Thread 6 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6GMAIp`
- **Path**: `src/Core.TypeScript/bonsai/bonsai.test.ts`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-06-01T16:46:33Z on `src/Core.TypeScript/bonsai/bonsai.test.ts`:213 (association: NONE)

  <pre>
  The new depth cap is only asserted for `serialize`. Since `parse` is also supposed to be bounded by `BONSAI_MAX_DEPTH` (and the bugfix should add a parser-side depth guard), add a regression test that `parse` declines an over-deep *canonical* input with `TooDeep` (no stack overflow / no throw).
  </pre>

## Fix commits (touching thread paths)

### `cec550b539466e0d26e1923f46c777fcd0a5c26b` -- 2026-06-01T16:41:47Z -- ``

**Touched files (intersect with thread paths):**

- `src/Core.TypeScript/bonsai/bonsai.test.ts`
- `src/Core.TypeScript/bonsai/bonsai.ts`

**Message:**

```
feat(081KT07NV0008QG0R003BE6MJ2 slice 1): TS Bonsai oracle -> Result<_, BonsaiFeedback> (…

…match the F# oracle)

Operator: "make them match — it's ugly when they don't." This converts the TS
reference oracle from throw to a pure-Result contract, matching the F# oracle
(#6429) byte-for-byte AND error-shape-for-error-shape.

- serialize/parse return Result<_, BonsaiFeedback> — no exception crosses the
  boundary (result over throw). TS owns a minimal zero-dep Result port (no BCL
  Result in JS); a consumer can adapt to neverthrow at the seam (hexagonal).
- BonsaiFeedback mirrors the F# DU variant set exactly (the shared cross-oracle
  payload): UnsupportedVersion / MalformedJson / UnknownKind / UnknownConstTag /
  UnknownOp / ExpectedString / ExpectedBool / ExpectedInt / NonSafeInt / TooDeep /
  NonCanonical. `where` fields are JSON-pointer keys (ProblemDetails-ready).
- Adopts the shared BONSAI_MAX_DEPTH = 1024 cap (TS lacked one — now matches F#'s
  TooDeep + the cross-oracle depth contract; round-trips past JSON's default 64).
- Constructors are now total (validation at the serialize boundary, matching F#'s
  raw DU); serialize/parse stay total even on a null string field (ExpectedString)
  or null input (MalformedJson) — no exception escapes.
- Rejections asserted by SPECIFIC variant (cross-oracle rejection-vector contract);
  canonical-only parse + byte-exact golden parity preserved. 49 tests, tsc 0 errors.

Internal mechanism: a private BonsaiFail signal adapted to Result at the two
boundaries (contract-vs-mechanism split; matches F#). Fail-fast (monadic); the
accumulate-mode (RFC-9457 ProblemDetails) is the additive next slice per 081KT07NV0008QG0R003BE6MJ2.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `b0f7d9492151695b86f0c4a36d16b0d64931d1bf` -- 2026-06-01T16:45:40Z -- ``

**Touched files (intersect with thread paths):**

- `src/Core.TypeScript/bonsai/bonsai.test.ts`
- `src/Core.TypeScript/bonsai/bonsai.ts`

**Message:**

```
fix(bonsai-ts): enforce depth limit during parse (bounded recursion, …

…no RangeError escape) (#6433)

Codex P2: parseNode recursed with no depth counter, relying on the after-the-fact
serialize guard for the depth check. F# is bounded by its JsonDocument MaxDepth, but
JS's JSON.parse has no such bound — so a deeply-nested-but-valid document could
recurse parseNode toward a stack-overflow (RangeError) that escapes the Result
before the guard fires. Added a depth counter to parseNode: declines TooDeep during
parsing, bounding recursion. +1 test (deep document declines TooDeep via the parse
counter) -> 50; tsc 0.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `f25c94aebe6b000cd93e498801ca5db57c391af3` -- 2026-06-01T16:48:41Z -- ``

**Touched files (intersect with thread paths):**

- `src/Core.TypeScript/bonsai/bonsai.test.ts`
- `src/Core.TypeScript/bonsai/bonsai.ts`

**Message:**

```
fix(bonsai-ts): validate op + bool at the serialize boundary (parse/s…

…erialize symmetry) (#6433)

Two Copilot symmetry holes: serialize didn't validate binary.op or bool runtime
type, so an untyped JS caller (op:"div" as any, v:1 as any) could emit bytes a peer
oracle rejects, while parse correctly declines them.
- emitAt binary: decline UnknownOp if op not in BIN_OPS (mirrors parse's asBinOp).
- emitConst bool: decline ExpectedBool if v is not a boolean (mirrors parse).
serialize now declines exactly what parse declines. +2 tests -> 52; tsc 0.

(The depth-bound P0 + initial-depth + parse-depth-test findings were already fixed
in b0f7d9492 — Copilot reviewed the pre-fix diff.)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```
