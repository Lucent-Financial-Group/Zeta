# PR #7823 -- feat(zflash): 081KSNY2Z0008QG0R0008PN7RQ acceptance scenarios 1–4 harness + CI

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 7823 |
| Title | feat(zflash): 081KSNY2Z0008QG0R0008PN7RQ acceptance scenarios 1–4 harness + CI |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-06-11T22:52:51Z |
| Merged at | 2026-06-11T23:55:45Z |
| Merge commit SHA | `5afa2e729e6fc4d2aa26e2ae156a1046e4f10d51` |
| Branch | `feat/b-0891-acceptance-tests-all-green` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/7823 |
| Changed files | 16 |
| Additions / deletions | +702 / -69 |

## Description

## Summary
- Wire 081KSNY2Z0008QG0R0008PN7RQ acceptance scenarios 1–4 into the zflash/QEMU test harness with shared serial markers and boot-image preparation via `zflash-file-backed --test`.
- Scenario 1 runs audit → zflash bake → QEMU boot on every ISO workflow PR; scenarios 2–4 run on push/`workflow_dispatch` with execute env vars for retention and path-fork.
- Scenario 5 (cluster joining) remains explicitly skipped until multi-VM orchestration lands.

## Test plan
- [x] `bun test tools/zflash/test-harness/` — 81 pass
- [x] `bun --bun tsc --noEmit -p tsconfig.json`
- [ ] CI: `zflash-qemu-test.yml` dry-run job
- [ ] CI: `build-ai-cluster-iso` scenario 1 on PR
- [ ] CI: scenarios 2–4 on workflow_dispatch (long-running QEMU)


Made with [Cursor](https://cursor.com)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | true |
| Total threads | 3 |
| Resolved threads | 3 |
| Unresolved threads | 0 |
| Total review comments | 3 |
| Total fix commits (touching thread paths) | 3 |

## Review threads

### Thread 1 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6I9_V8`
- **Path**: `tools/zflash/test-harness/run.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-06-11T22:55:55Z on `tools/zflash/test-harness/run.ts`:293 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Boot the image that zflash produced**

  In scenario 1, the harness prepares a zflash-backed boot image but then invokes `qemu-boot-test.ts` with the original ISO path, so the produced image is never actually booted. Because `qemu-boot-test.ts` treats its argument as the CD-ROM/ISO media, this can let the new CI acceptance scenario pass even when `zflash-file-backed` creates an unbootable or malformed USB image, invalidating the “initial format” proof this commit adds.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 2 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6I-DKV`
- **Path**: `tools/zflash/test-harness/qemu-state.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-06-11T23:02:43Z on `tools/zflash/test-harness/qemu-state.ts`:386 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Require post-restore markers for retention proof**

  When scenario 3 auto-prepares a zflash boot image, this switches the restart success contract from the installed-OS restore markers to the early USB-copy markers. Those 081KSNY2Z0008QG0R0008PN7RQ lines are emitted by `zeta-install.sh` as soon as it notices and copies `/zeta-creds.enc` from the boot ESP, before the installed `zeta-creds-restore` service decrypts/restores credentials; in fact `prepare-boot-image.ts` encrypts the test blob with a fixed `DEFAULT_QEMU_USB_UUID`, while the installed restore path later uses the actual captured USB UUID, so the restore can fail even though these markers are present. For any run with `ZFLASH_QEMU_RETENTION_EXECUTE=1`, this can mark “reformat with retention” passed without proving retained credentials are usable after install.

  Useful? React with 👍 / 👎.
  </pre>

### Thread 3 -- resolved [collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6I-DKZ`
- **Path**: `tools/zflash/test-harness/prepare-boot-image.ts`
- **Replies**: 0

**Initial comment:**

- **`chatgpt-codex-connector[bot]` (bot)** at 2026-06-11T23:02:43Z on `tools/zflash/test-harness/prepare-boot-image.ts`:68 (association: NONE)

  <pre>
  **&lt;sub&gt;&lt;sub&gt;![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)&lt;/sub&gt;&lt;/sub&gt;  Use mcopy's supported version flag**

  In the new CI path, `prepareBootImage()` always calls `checkZflashToolchain()` before invoking zflash, so a false-negative probe blocks all file-backed boot-image scenarios after the workflow installs `mtools`. The GNU mtools manual documents the common version option as `-V` for all tools, while `mcopy --version` is not the documented probe; on runners where `mcopy` rejects that long option, scenario 1/3/4 fail before exercising zflash or QEMU. Use `mcopy -V` or a pure PATH existence check here.

  Useful? React with 👍 / 👎.
  </pre>

## Fix commits (touching thread paths)

### `bfda9c19b32d236da19da412be4654fe40b04da8` -- 2026-06-11T22:52:43Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/zflash/test-harness/prepare-boot-image.ts`
- `tools/zflash/test-harness/qemu-state.ts`
- `tools/zflash/test-harness/run.ts`

**Message:**

```
feat(zflash): wire 081KSNY2Z0008QG0R0008PN7RQ acceptance scenarios 1–4 into harness and CI

Scenario 1 now audits ISO, bakes a zflash test image, and boots in QEMU;
scenarios 3–4 auto-prepare boot images when executing with a real ISO; retention
serial markers are shared and aligned with zeta-install.sh and cred-restore idempotency.

Co-authored-by: Grok <noreply@x.ai>
Co-authored-by: Cursor <cursoragent@cursor.com>
```

### `f0bdb4a23fe8a8ce89389d84ad67ce9985be160f` -- 2026-06-11T22:59:03Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/zflash/test-harness/prepare-boot-image.ts`

**Message:**

```
fix(zflash): stabilize 081KSNY2Z0008QG0R0008PN7RQ CI — mtools, timeouts, lighter path-fork

Install mtools alongside QEMU for zflash file-backed bakes; split scenarios 3/4
into separate dispatch steps with serial-log artifacts; extend job timeout on
workflow_dispatch; path-fork fork boots now stop on 081KSNY2Z0008QG0R0008PN7RQ markers only.

Co-authored-by: Grok <noreply@x.ai>
Co-authored-by: Cursor <cursoragent@cursor.com>
```

### `9796f2344799fbe29fc8ed07567c894d842f2898` -- 2026-06-11T23:12:06Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `tools/zflash/test-harness/prepare-boot-image.ts`

**Message:**

```
fix(zflash): auto-detect isohybrid ESP offset for file-backed bakes

mcopy failed in CI because the harness used a 1MiB ESP offset; NixOS
isohybrid installers expose FAT at LBA 276 (141312 bytes). Detect from MBR
0xEF with the same fallback flash-and-inject uses.

Co-authored-by: Grok <noreply@x.ai>
Co-authored-by: Cursor <cursoragent@cursor.com>
```
