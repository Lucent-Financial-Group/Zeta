# PR #10398 -- docs(workitems): supersede the falsified orbital delta-max remedy; file the endpoint-speed envelope

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 10398 |
| Title | docs(workitems): supersede the falsified orbital delta-max remedy; file the endpoint-speed envelope |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-08-13T22:20:07Z |
| Merged at | 2026-08-13T22:33:06Z |
| Merge commit SHA | `27d28cc0c3687e0e5d4e78204bc277f66311f31b` |
| Branch | `shadow/orbital-endpoint-speed-envelope` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/10398 |
| Changed files | 4 |
| Additions / deletions | +382 / -6 |

## Description

## What this does

Records the outcome of the independent second check in #10387 against the Earth–Mars
light-time asymmetry bound. **No production file is touched** — `OrbitalAsymmetryBudget.fs`,
`BusRegime.fs`, workflows and credentials are all untouched. Two work-items and a prior-art
section.

### `081KZYK0Q8Z087G0R0010Z2Z2Q` (new) — the endpoint-speed envelope

- **The precise defect.** `δ_max` feeds `BusRegime.regimeOf`, and the error is one-directional
  in consequence: too *large* is a missed detection, too *small* is a **false `OutOfCone`
  conviction of an honest message**, which `Evidential` then promotes to evidence. So a
  **provably-conservative bound beats a usually-accurate one** — that is the selection
  criterion, stated as such.
- **Why both prior candidates are rejected**, with #10387's derivation **quoted rather than
  paraphrased**. The headline: under the stated light-time equations the first-order term
  carries the **sum** of the projected endpoint velocities, `|û·(v_A + v_B)|R/c²`, not the
  difference. And no projection of any sign works as a *bound* anyway — `û·v` cancels at
  perpendicular geometry, which is the structural hole behind the ≈22,297× under-budget factor.
- The **endpoint-speed envelope** `δ_max = δ_speed + δ_model`, marked **PROPOSED**, with its
  open questions kept open (the `1.2` multiplier is still unjustified; "never exceeded in a
  coarse scan" is an observation, not a theorem).
- **JPL Horizons golden vectors as a blocking dependency**, in its own H2, with both epochs
  named (2027-02-19 16:02:32 UTC / 101,417,205 km; conjunction near 2028-03-23 / ≈0.786°
  elongation). `depends_on:` is empty only because the prerequisite is an artifact, not a
  ZetaId — stated explicitly rather than left to look like an oversight.
- Every claim tagged **CHECKED** or **PROPOSED**, with a register table saying plainly that the
  filer did **not** re-derive the physics and that the transcription is a quotation under
  review, not a second independent confirmation.

### `081KZY5W6AJ087G0R003EE7PY6` (updated) — remedy struck, defects intact

D1/D2/D3 stand as filed. The remedy is struck in **both** places it appeared (the D2 prose and
"Fix shape" item 2), because a work-item stating a superseded remedy is worse than one stating
none — someone implements it from a stale read.

It also records the meta-finding plainly: **the second opinion refuted the first model's
proposed fix, not merely confirmed its defect.** A confirmation-only review would have passed
that fix; only an independent re-derivation caught it. The generalisation is the load-bearing
part — *finding a defect and prescribing its remedy are two different acts with two different
failure modes, and confidence in the first does not transfer to the second* — so the witness
quorum must cover the **remedy**, not stop at the finding.

### `docs/PRIOR-ART-LIST.md`

Seidelmann 1992 · Murray & Dermott 1999 · Park et al. 2021 (DE440/441) · Reasenberg et al. 1979
(Viking), each with **what it grounds**, not name-dropped. The checked-anchor debt is stated
out loud: these are cited from standing knowledge, not re-opened and page-checked, so the
`anchor-to-human-prior-art` bar is **not yet met** and is recorded as visible debt.

## Verification

- `lint-frontmatter.ts --strict` on both work-items: identical to the untouched-neighbour
  baseline (the two `state`/`slug` findings are emitted for *every* file `new-workitem.ts`
  mints — a schema drift between the legacy linter and the current minter, pre-existing).
- `lint-no-new-bnnnn.ts` · `lint-no-b-refs.ts`: pass.
- `audit-agencysignature-main-tip.ts --commit HEAD`: **CORRECT**, 0 regressions.
- ZetaId minted via `new-workitem.ts`; no hand-allocated `B-NNNN`.

## Corrections to the brief I was given

Recorded here because a clean execution of a wrong brief is worth less than the correction:

1. **#10387 confirms D3 as well as D1.** Its status line validates "the coordinate-rotation
   defect **and the missing solar-occlusion predicate**". The brief named only D1.
2. **It also strengthens D2's *finding*** (22,297× vs the 54× on file) while refuting D2's
   *remedy*. Both halves matter — the first model was right that the shipped budget is not
   conservative, and more right than it knew.
3. **The falsification is conditional on the stated equations**, and #10387 says so: the
   relative rate "corresponds to a different timing/convention and cannot be substituted into
   these two equations without changing the model." That is narrower than "the relative range
   rate is wrong physics."
4. **The positive result was omitted from the brief**: the correct leading term is the **sum**,
   `|û·(v_A + v_B)|R/c²`. That sign is the actual finding.
5. **`δ_model` was omitted.** The proposal is `δ_max = δ_speed + δ_model` with declared
   provenance, not `δ_speed` alone.
6. **Path:** the module is `src/Bayesian/OrbitalAsymmetryBudget.fs`, not `src/Core/`.

🤖 Generated with [Claude Code](https://claude.com/claude-code)


## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 0 |
| Resolved threads | 0 |
| Unresolved threads | 0 |
| Total review comments | 0 |
| Total fix commits (touching thread paths) | 0 |

## Review threads

(no review threads on this PR)

## Fix commits (touching thread paths)

(no commits in this PR touched files anchored by review threads)
