# PR #8715 -- test(formal): row 3 FsCheck cross-check for anti-mirror ρ_owe DPI/soundness

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 8715 |
| Title | test(formal): row 3 FsCheck cross-check for anti-mirror ρ_owe DPI/soundness |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-06-20T06:35:05Z |
| Merged at | 2026-06-20T06:35:13Z |
| Merge commit SHA | `4d0629ad035cb20593e96100b7deb591c61619ef` |
| Branch | `otto/row3-rho-owe-dpi-crosscheck` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/8715 |
| Changed files | 2 |
| Additions / deletions | +113 / -0 |

## Description

## Why

Math-team handoff **row 3** — the buildable **FsCheck CROSS-CHECK leg** for the anti-mirror `ρ_owe` estimator (`src/Core/Decorrelation.fs`, `ρ_owe = H(A|U,C)/H(A|C)`). The Lean 4 DPI soundness **proof** stays the math team's; this is the **empirical floor** that cross-checks the theorem. Honoring the handoff judgment call: **`ρ_owe` is evidence, not the lemma** — this leg never promotes the statistic to a proof.

Handoff: `docs/handoffs/2026-06-19-otto-to-math-team-nft-ntp-anti-mirror-societal-dora-formalization.md` (row 3).

## Prior-art gate (honored)

`DecorrelationEstimator.Tests.fs`'s 11 tests already cover: bounded `[0,1]` · no-context mirror→0 · product-grid own→1 · permutation-invariance · **DPI coarsen-U** (coarsening U cannot *lower* `ρ_owe`) · MI ≥0 / symmetric · stake-weighting guard. So this PR adds **net-new only** — no duplication.

## What (the property list)

New file `tests/Tests.FSharp/Formal/DecorrelationDpiCrossCheck.Tests.fs`:

1. **DPI DUAL (load-bearing):** refining U (more shared-cause info, `U ↦ (U, A)`) cannot **raise** `ρ_owe`. `I(A;U'|C) ≥ I(A;U|C) ⇒ H(A|U',C) ≤ H(A|U,C)`. The monotonicity direction the existing coarsen-U test does *not* assert.
2. **Soundness endpoint WITH context:** `A` determined by joint `(U,C)`, non-constant ⇒ `ρ_owe → 0` (existing mirror test is no-context `A=U`).
3. **Soundness endpoint WITH context:** `A ⫫ U | C` (per-cell `A×U` product) ⇒ `ρ_owe = 1` (existing own-test is context-agnostic).
4. **Determinism contract:** replication invariance + uniform-stake equivalence (`weighted == unweighted`) — the DST-replay floor.

**Could not assert (honest gap, documented in file header):** a "post-process A" DPI property. A probe confirmed coarsening `A` is **not monotone in either direction** against the plug-in estimator, so any such property would be **false** — not asserted. This is a faithful-not-forced outcome, not a missing test.

Every asserted property was **empirically pre-verified** against the real estimator (0 violations over thousands of random draws) before being encoded; none was weakened to force green.

## Gate

`dotnet test Zeta.sln -c Release` → **0 warnings / 0 errors**, Tests.FSharp **3516 passed** (5 new, 4 pre-existing manual smoke skips), all other projects green. No production code touched; no markdown touched.

**Beacon anchors:** Shannon 1948 (entropy / mutual information); Goguen–Meseguer 1982 (noninterference); the data-processing inequality (Cover & Thomas).

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 0 |
| Resolved threads | 0 |
| Unresolved threads | 0 |
| Total review comments | 0 |
| Total fix commits (touching thread paths) | 0 |

## Review threads

(no review threads on this PR)

## Fix commits (touching thread paths)

(no commits in this PR touched files anchored by review threads)
