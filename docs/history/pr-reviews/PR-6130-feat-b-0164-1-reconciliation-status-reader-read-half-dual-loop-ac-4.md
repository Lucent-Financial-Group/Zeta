# PR #6130 -- feat(081KR7JY10008QG0R000MH7PJT): reconciliation-status reader (read half, dual-loop AC #4)

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 6130 |
| Title | feat(081KR7JY10008QG0R000MH7PJT): reconciliation-status reader (read half, dual-loop AC #4) |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-05-30T17:09:36Z |
| Merged at | 2026-05-30T20:09:27Z |
| Merge commit SHA | `8a3b84f988886983cad9160ec8abaae2d4386e39` |
| Branch | `feat/b0164.1-reconciliation-status-reader-2026-05-30` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/6130 |
| Changed files | 4 |
| Additions / deletions | +219 / -1 |

## Description

## What

Adds the **read half** of the 081KR7JY10008QG0R000MH7PJT PR-review disagreement-preservation
protocol (dual-loop **AC #4**) to `tools/hygiene/divergence-shard.ts`.

The module already had the *write* half (detector -> builder -> fail-closed
writer -> `fileReviewThreadDisagreement` composer). The schema README's morning
workflow — *"Morning reconciliation reads all shards with empty `Reconciliation`
sections"* — and AC #4 (*"resolve the thread in one read + one action"*) both
depend on a primitive that distinguishes an **empty (awaiting)** Reconciliation
section from a **filled (decided)** one. None existed. This PR adds it.

New exports (pure, no I/O):

- `parseReconciliationStatus(markdown)` — classifies a filed shard's
  Reconciliation section. Round-trips with `buildDivergenceShard` (its
  placeholder output reads back as `unreconciled`).
- `ReconciliationStatus` — three explicit variants per the IMPLICIT-NOT-EXPLICIT
  discipline: `unreconciled` / `reconciled` (recognized keyword) /
  `reconciled-freeform` (filled but no keyword; morning tooling flags it to
  canonicalize).
- `RECONCILIATION_DECISIONS` + `ReconciliationDecision` — the README's
  four-option vocabulary (`accept-loop-a | accept-loop-b | accept-both |
  escalate`).

## Why this is the smallest safe slice

- **Below the blocked boundary.** Parsing an already-filed shard needs no GitHub
  call and no concurrent-loop harness (the 081KQJZR90008QG0R000FTJ1TC blocker), so it is fully
  testable in isolation — same posture as the Codex detector slice (2026-05-30).
- **Schema unchanged.** 081KQJZR90008QG0R002GJAJ19 AC #4 remains the source of truth; this slice only
  *reads* the schema (out-of-scope changes — GitHub submission, schema edits —
  untouched).
- **Load-bearing correctness detail:** the unreconciled placeholder itself lists
  the four decision keywords inside HTML comments, so comment-stripping is
  ordered *before* the empty-check + keyword scan. A test asserts the freshly
  built placeholder reads as `unreconciled` (not a spurious `accept-loop-a`).
- Matching is case-insensitive (consistent with `normalizedConclusion`);
  earliest-by-index wins when multiple keywords appear; `note` preserves original
  case. Section extraction terminates at the next `## ` heading or EOF (a later
  heading's `escalate` does not bleed in — tested). Malformed shard (missing
  `## Reconciliation`) throws, matching the eager-rejection discipline of
  `detectReviewThreadDisagreement`.

## Re-decomposition note

Per the build-time re-decompose discipline: Riven's 2026-05-11 "no substrate
mutation" framing is superseded by the Codex 2026-05-30 precedent of landing
real below-the-boundary tooling. The genuine remaining gap was the *read* half;
this PR lands exactly that and nothing more (one bounded step). Natural next
slice (still below the boundary, NOT here): an I/O scanner over
`docs/hygiene-history/divergences/**` that lists unreconciled shards by feeding
each through `parseReconciliationStatus`.

## Focused checks

- `bun test tools/hygiene/divergence-shard.test.ts` -> **41 pass / 0 fail**
  (11 new; baseline was 30).
- `bunx tsc --noEmit -p tsconfig.json` -> **clean** on the touched files (no
  `error TS` lines).
- Commit-canary: tree-size 66 == parent 66 (no commit-tree corruption).
- TS + docs only — no `.fs`/`.cs`/`.fsproj` touched, so the `dotnet build -c
  Release` gate is unaffected (determined entirely by `origin/main` state).

operative-authorization: aaron 2026-05-14: "- **Devil-pole** (edge-runner drive): keep pushing, discover, go hard, never-be-idle"

Closes a slice of 081KR7JY10008QG0R000MH7PJT (`docs/backlog/P1/081KR7JY10008QG0R000MH7PJT-pr-review-disagreement-preservation-protocol.md`). Parent 081KQJZR90008QG0R002GJAJ19; depends_on 081KQJZR90008QG0R000FTJ1TC (impl child remains blocked).

🤖 Generated with [Claude Code](https://claude.com/claude-code)


## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | true |
| Total threads | 5 |
| Resolved threads | 5 |
| Unresolved threads | 0 |
| Total review comments | 9 |
| Total fix commits (touching thread paths) | 2 |

## Review threads

### Thread 1 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F4o55`
- **Path**: `tools/hygiene/divergence-shard.ts`
- **Replies**: 0

**Initial comment:**

- **`github-advanced-security[bot]` (bot)** at 2026-05-30T17:11:57Z on `tools/hygiene/divergence-shard.ts`:460 (association: NONE)

  <pre>
  ## CodeQL / Incomplete multi-character sanitization

  This string may still contain [&lt;!--](1), which may cause an HTML element injection vulnerability.

  [Show more details](https://github.com/Lucent-Financial-Group/Zeta/security/code-scanning/123)
  </pre>

### Thread 2 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F4pC2`
- **Path**: `tools/hygiene/divergence-shard.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-30T17:12:40Z on `tools/hygiene/divergence-shard.ts`:453 (association: NONE)

  <pre>
  extractReconciliationSection locates the header via `indexOf("## Reconciliation")`, which can match occurrences in prose/code blocks before the actual heading and slice the wrong region. The repo already uses a heading-anchored regex in tools/hygiene/divergence-reconcile.ts; using the same approach here makes the section extraction deterministic and schema-faithful.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-30T20:09:18Z on `tools/hygiene/divergence-shard.ts`:453 (association: MEMBER)

    <pre>
    Resolved — already addressed on the current head. `extractReconciliationSection` now delegates to `reconciliationBody` in divergence-reconcile.ts, which anchors on the full-line heading `/^## Reconciliation[ \t]*$/m` instead of a bare `indexOf`. Finding was correct at filing time; fix has landed (thread is outdated).
    </pre>

### Thread 3 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F4pDA`
- **Path**: `tools/hygiene/divergence-shard.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-30T17:12:40Z on `tools/hygiene/divergence-shard.ts`:461 (association: NONE)

  <pre>
  stripHtmlComments does only a single global replace. Elsewhere in this codebase (tools/hygiene/divergence-reconcile.ts) this is implemented to a fixpoint because a single pass can leave residual `&lt;!--` sequences and can trip CodeQL's incomplete multi-character sanitization query. Using the fixpoint loop here avoids that class of false classification and keeps analyzers quiet.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-30T20:09:20Z on `tools/hygiene/divergence-shard.ts`:461 (association: MEMBER)

    <pre>
    Resolved — already addressed on the current head. `stripHtmlComments` is now imported from divergence-reconcile.ts, which runs the fixpoint `do { out = out.replace(/&lt;!--[\s\S]*?--&gt;/g, '') } while (out !== prev)` loop (the CodeQL js/incomplete-multi-character-sanitization-safe form). Single-sourced, no second pass carried here.
    </pre>

### Thread 4 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F4pDG`
- **Path**: `tools/hygiene/divergence-shard.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-30T17:12:41Z on `tools/hygiene/divergence-shard.ts`:486 (association: NONE)

  <pre>
  The docs say `note` preserves the maintainer’s original content, but the implementation builds `filled` by stripping HTML comments first. Either preserve comments in the returned note, or update the docstring to match the actual behavior (note is trimmed with HTML comments removed).
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-30T20:09:22Z on `tools/hygiene/divergence-shard.ts`:486 (association: MEMBER)

    <pre>
    Resolved — docstring now matches behavior. `parseReconciliationStatus` and the `ReconciliationStatus` type doc both state the returned `note` is the section text with placeholder HTML comments stripped (same fixpoint strip as the empty-check) then trimmed, case-preserving — explicitly 'not a byte-for-byte copy of the raw section'.
    </pre>

### Thread 5 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM6F4pDJ`
- **Path**: `tools/hygiene/divergence-shard.ts`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-05-30T17:12:41Z on `tools/hygiene/divergence-shard.ts`:416 (association: NONE)

  <pre>
  RECONCILIATION_DECISIONS and ReconciliationDecision are now defined/exported in both divergence-shard.ts and divergence-reconcile.ts. This duplication risks the two drifting (order/value changes, doc changes) and forces callers/tests to pick a source. Consider extracting these shared constants/types into a small shared module (or re-exporting from one place) so the vocabulary is single-sourced.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-05-30T20:09:24Z on `tools/hygiene/divergence-shard.ts`:416 (association: MEMBER)

    <pre>
    Resolved — single-sourced. `RECONCILIATION_DECISIONS` and `ReconciliationDecision` are now defined only in divergence-reconcile.ts; divergence-shard.ts imports and re-exports them so existing importers keep a stable surface while the vocabulary has one definition. Comment at the re-export cites this finding.
    </pre>

## Fix commits (touching thread paths)

### `1f5b105404d9c06ffa814580fdc1814f0a9bfe35` -- 2026-05-30T17:08:42Z -- `claude`

**Touched files (intersect with thread paths):**

- `tools/hygiene/divergence-shard.ts`

**Message:**

```
feat(081KR7JY10008QG0R000MH7PJT): reconciliation-status reader (read half, dual-loop AC…

… #4)

The divergence-shard module already had the write half of the disagreement-
preservation protocol (detector -> builder -> fail-closed writer -> compose).
The schema README's morning workflow ("reads all shards with empty
Reconciliation sections") and 081KR7JY10008QG0R000MH7PJT AC #4 ("resolve the thread in one read
+ one action") both depend on a read primitive that distinguishes an empty
(awaiting) Reconciliation section from a filled (decided) one. None existed.

This slice adds that pure read primitive to tools/hygiene/divergence-shard.ts:

- parseReconciliationStatus(markdown): classifies a filed shard's Reconciliation
  section. Consumes exactly the placeholder buildDivergenceShard writes
  (round-trip -> unreconciled). Comment-stripping is load-bearing and ordered
  first: the unreconciled placeholder itself lists the decision keywords inside
  HTML comments, so they must be stripped before the empty-check + keyword scan.
- ReconciliationStatus: three explicit variants per IMPLICIT-NOT-EXPLICIT --
  unreconciled / reconciled (recognized keyword) / reconciled-freeform (filled
  but no keyword; morning tooling flags it to canonicalize).
- RECONCILIATION_DECISIONS + ReconciliationDecision: the README's four-option
  vocabulary (accept-loop-a | accept-loop-b | accept-both | escalate). Matched
  case-insensitively (consistent with normalizedConclusion); earliest-by-index
  wins when multiple keywords appear; note preserves original case.
- Malformed shard (no "## Reconciliation" header) throws -- eager rejection over
  silent acceptance, matching detectReviewThreadDisagreement.

Stays below the blocked end-to-end boundary: pure, no GitHub, no concurrent-loop
harness; schema unchanged (081KQJZR90008QG0R002GJAJ19 AC #4 remains source of truth, this reads it).

Focused checks: bun test tools/hygiene/divergence-shard.test.ts -> 41 pass / 0
fail (11 new). tsc --noEmit -> clean on touched files. TS+docs only; no .NET
files touched, dotnet build gate unaffected.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

### `90813573784b55bd32193f6df29222f3046cf5e7` -- 2026-05-30T19:48:04Z -- `claude`

**Touched files (intersect with thread paths):**

- `tools/hygiene/divergence-shard.ts`

**Message:**

```
fix(081KR7JY10008QG0R000MH7PJT): single-source reconciliation parsing per Copilot revie…

…w (#6130)

Address the 5 review threads on tools/hygiene/divergence-shard.ts by
single-sourcing the reconciliation-section parsing against the canonical
implementations already on main in divergence-reconcile.ts:

- Dedup RECONCILIATION_DECISIONS + ReconciliationDecision: re-export from
  divergence-reconcile.ts instead of redefining (kills the drift risk the
  two modules carried; the order/values now have one definition).
- extractReconciliationSection: delegate to divergence-reconcile.ts's
  `reconciliationBody`, which anchors the heading as a full line
  (/^## Reconciliation[ \t]*$/m) rather than a bare indexOf — deterministic,
  no longer slices on a "## Reconciliation" mention inside prose/code.
- stripHtmlComments: import the canonical fixpoint impl rather than carrying
  a single-pass /g replace. This also resolves the CodeQL
  js/incomplete-multi-character-sanitization finding (L460) by construction:
  there is now exactly one fixpoint sanitizer across the divergence protocol.
- note docstrings: align to actual behavior — note is case-preserving and
  trimmed with the placeholder HTML comments stripped (not a byte-for-byte
  copy of the raw section).

divergence-reconcile.ts: export stripHtmlComments (additive; no behavior change).

Tests: 73 pass / 0 fail across divergence-shard.test.ts + divergence-reconcile.test.ts.

Co-Authored-By: Claude <noreply@anthropic.com>
```
