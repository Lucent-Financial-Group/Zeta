# PR #340 -- core: PLV mean phase offset — 19th graduation (Amara 18th-ferry #6)

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 340 |
| Title | core: PLV mean phase offset — 19th graduation (Amara 18th-ferry #6) |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-04-24T08:59:29Z |
| Merged at | 2026-04-24T12:10:22Z |
| Merge commit SHA | `da02e5d21837713b6ce536805c381bab0d542ddd` |
| Branch | `core/plv-mean-phase-offset-amara-18th-ferry` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/340 |
| Changed files | 2 |
| Additions / deletions | +248 / -9 |

## Description

## Summary

Addresses Amara 18th-ferry correction #6: **PLV = 1 can mean anti-phase locking, not same-time synchronization.** Detectors that rely on "PLV = 1 => synchronized" misread anti-phase coordinators as same-time coordinators.

Two new functions added to `TemporalCoordinationDetection`:

- **`meanPhaseOffset`** — returns the argument (angle) of the mean complex phase-difference vector whose magnitude is the PLV. Returns `None` when undefined (empty / mismatched / zero-magnitude).
- **`phaseLockingWithOffset`** — returns both magnitude and offset in one sequence pass; convenience for callers that need both.

Existing `phaseLockingValue` contract unchanged; new primitives are additive.

## Key test: anti-phase regression

```fsharp
// Anti-phase: magnitude = 1 AND offset = ±π
let a = [ 0.0; 0.5; 1.0; 1.5 ]
let b = a |> List.map (fun x -> x + Math.PI)
TemporalCoordinationDetection.meanPhaseOffset a b
// => Some(π)  (distinct from in-phase which is Some(0))
```

Without `meanPhaseOffset`, both cases return identical PLV = 1 and are indistinguishable. This is the regression Amara flagged.

## Test coverage

8 new xUnit tests: identical / constant-offset / anti-phase-vs-in-phase / zero-magnitude / empty / mismatched / single-element / magnitude-consistency / `phaseLockingWithOffset` variants. All 37 TemporalCoordinationDetection tests pass.

## Build

`dotnet build -c Release` — 0 Warnings / 0 Errors.

## 18th-ferry correction tracking

6th of 10 operationalized (after #338 parser-tech, #339 test-classification). Remaining: Wilson CIs in CartelToy tests (waits on #323), MAD=0 percentile-rank fallback (waits on #333), conductance-sign doc (waits on #331), artifact-output layout (Stage-2 calibration harness).

## Test plan

- [x] `dotnet build -c Release` clean.
- [x] `dotnet test` on TemporalCoordinationDetection passes (37/37).
- [x] Non-breaking — existing PLV contract unchanged.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | true |
| Total threads | 7 |
| Resolved threads | 7 |
| Unresolved threads | 0 |
| Total review comments | 14 |
| Total fix commits (touching thread paths) | 3 |

## Review threads

### Thread 1 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59WGi9`
- **Path**: `tests/Tests.FSharp/Algebra/TemporalCoordinationDetection.Tests.fs`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-24T09:02:35Z on `tests/Tests.FSharp/Algebra/TemporalCoordinationDetection.Tests.fs`:282 (association: NONE)

  <pre>
  In this test, using sentinel defaults (`-1.0`) for `Option.defaultValue` can mask the root cause if both results unexpectedly become `None` (the equality assertion would still pass). Prefer pattern-matching both options and failing explicitly when either is `None`, so the test reports the actual regression clearly.
  ```suggestion
      let magnitudeFromPair = TemporalCoordinationDetection.phaseLockingWithOffset a b
      let magnitudeFromPlv = TemporalCoordinationDetection.phaseLockingValue a b

      match magnitudeFromPair, magnitudeFromPlv with
      | Some (struct (magnitude, _)), Some plvMagnitude -&gt;
          Math.Round(magnitude, 12) |&gt; should equal (Math.Round(plvMagnitude, 12))
      | None, Some _ -&gt;
          failwith "expected phaseLockingWithOffset to return Some tuple"
      | Some _, None -&gt;
          failwith "expected phaseLockingValue to return Some magnitude"
      | None, None -&gt;
          failwith "expected both phaseLockingWithOffset and phaseLockingValue to return Some"
  ```
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-24T11:53:32Z on `tests/Tests.FSharp/Algebra/TemporalCoordinationDetection.Tests.fs`:282 (association: MEMBER)

    <pre>
    Fixed in 0c73a12: test rewritten to pattern-match both options and failwith explicitly on None. Sentinel -1.0 pattern removed; equality assertion now cannot silently pass when both primitives return None. Reviewer caught a real regression-masking bug.
    </pre>

### Thread 2 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59WGjn`
- **Path**: `src/Core/TemporalCoordinationDetection.fs`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-24T09:02:35Z on `src/Core/TemporalCoordinationDetection.fs`:186 (association: NONE)

  <pre>
  The provenance link in this doc comment references `docs/aurora/2026-04-24-amara-calibration-ci-hardening-deep-research-plus-5-5-corrections-18th-ferry.md`, but that file does not exist under `docs/aurora/` in the current tree. Please fix the path (or remove the link) so the cross-reference is valid.
  ```suggestion
      /// correction #6. Complements the original PLV
  ```
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-24T11:53:33Z on `src/Core/TemporalCoordinationDetection.fs`:186 (association: MEMBER)

    <pre>
    Fixed in 0c73a12: provenance rewritten to describe the 18th-ferry topically + cross-reference the related 19th-ferry DST audit doc that IS in the repo (docs/aurora/2026-04-24-amara-dst-audit-deep-research-plus-5-5-corrections-19th-ferry.md). 18th-ferry absorb doc lives in session memory + may land as its own docs/aurora/ file later. No dangling cross-reference.
    </pre>

### Thread 3 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59WGj4`
- **Path**: `src/Core/TemporalCoordinationDetection.fs`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-24T09:02:36Z on `src/Core/TemporalCoordinationDetection.fs`:160 (association: NONE)

  <pre>
  This doc comment says the zero-magnitude check "uses the same PLV-magnitude floor", but `phaseLockingValue` does not apply any floor (it will return values arbitrarily close to 0). Consider rewording to clarify that `epsilon` is only used to decide when the *offset* is undefined (to avoid `atan2` on a near-zero mean vector).
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-24T11:53:35Z on `src/Core/TemporalCoordinationDetection.fs`:160 (association: MEMBER)

    <pre>
    Fixed in 0c73a12: doc comment rewritten. The epsilon floor (phasePairEpsilon, 1e-12) applies ONLY to the offset-undefined decision in meanPhaseOffset / phaseLockingWithOffset. phaseLockingValue has no floor — it returns magnitude arbitrarily close to zero as normal output. Old wording conflated the two behaviors; corrected.
    </pre>

### Thread 4 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59WGkP`
- **Path**: `src/Core/TemporalCoordinationDetection.fs`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-24T09:02:36Z on `src/Core/TemporalCoordinationDetection.fs`:187 (association: NONE)

  <pre>
  The new provenance text introduces personal name attribution (e.g., "Aaron + Amara"). Repo guidance prefers role-based references (or a neutral provenance statement) in code/doc comments; please rephrase this section to avoid personal attribution while preserving the technical pointer.
  ```suggestion
      /// Provenance: 18th courier ferry, Part 2 correction
      /// #6 (`docs/aurora/2026-04-24-amara-calibration-
      /// ci-hardening-deep-research-plus-5-5-corrections-
      /// 18th-ferry.md`, §"Ten required corrections" #6).
      /// Complements the original PLV primitive from the
      /// foundational temporal-coordination design without
  ```
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-24T11:53:37Z on `src/Core/TemporalCoordinationDetection.fs`:187 (association: MEMBER)

    <pre>
    Fixed in 0c73a12: name attribution removed from doc comment. "Aaron + Amara 11th ferry" → "the 11th ferry" per factory role-reference convention. Audit-trail surfaces (commit messages, tick-history, memory) retain direct attribution; code/doc comments use role-neutral references.
    </pre>

### Thread 5 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59WGkn`
- **Path**: `src/Core/TemporalCoordinationDetection.fs`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-24T09:02:36Z on `src/Core/TemporalCoordinationDetection.fs`:208 (association: NONE)

  <pre>
  `meanPhaseOffset` and `phaseLockingWithOffset` duplicate the same sin/cos accumulation logic already present in `phaseLockingValue`, including repeating the `epsilon` constant. To reduce drift risk (e.g., one function later changes its accumulation or threshold), consider factoring the shared accumulation into a private helper (returning meanCos/meanSin/magnitude) and reusing it across all three functions.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-24T11:53:39Z on `src/Core/TemporalCoordinationDetection.fs`:208 (association: MEMBER)

    <pre>
    Fixed in 0c73a12: extracted shared helpers phasePairEpsilon + meanPhaseDiffVector. All three primitives (phaseLockingValue, meanPhaseOffset, phaseLockingWithOffset) now route through the shared accumulator. One-function-silently-diverges-from-others risk eliminated by construction. All 37 tests still pass.
    </pre>

### Thread 6 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59Yqkl`
- **Path**: `src/Core/TemporalCoordinationDetection.fs`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-24T11:56:21Z on `src/Core/TemporalCoordinationDetection.fs`:134 (association: NONE)

  <pre>
  P1: The provenance comment references `docs/aurora/2026-04-24-amara-temporal-coordination-detection-cartel-graph-influence-surface-11th-ferry.md`, but that file is not present under `docs/aurora/` in this repo. Please update the path to an existing doc (or remove/soften the link) so the cross-reference is valid.
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-24T12:08:57Z on `src/Core/TemporalCoordinationDetection.fs`:134 (association: MEMBER)

    <pre>
    Good catch — fixed in 5562c7d217cac7d1c0f31a5c91cf5a995b5cfa50. The 11th ferry file is real but hasn't landed on main yet (it's queued via PR #296 under the Otto-105 operationalize cadence), so the inline path was forward-looking. Replaced with an intent-preserving form that cites provenance by role + points at the MEMORY.md queue entry instead of a speculative path. When #296 merges, anyone following the breadcrumb still lands in the right material.
    </pre>

### Thread 7 -- resolved [outdated, collapsed]

- **Thread ID**: `PRRT_kwDOSF9kNM59YqlC`
- **Path**: `src/Core/TemporalCoordinationDetection.fs`
- **Replies**: 1

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-24T11:56:22Z on `src/Core/TemporalCoordinationDetection.fs`:178 (association: NONE)

  <pre>
  P2: The doc comment says the returned offset is in `(-pi, pi]`, but `atan2` can return `-pi` as well (range is `[-pi, pi]` per `System.Math.Atan2`). Please adjust the documented range (or explicitly normalize) so the contract matches the implementation.
  ```suggestion
      /// a value in `[-pi, pi]` when defined, or `None` when
  ```
  </pre>

**Replies:**

  - **`AceHack` (human)** at 2026-04-24T12:08:59Z on `src/Core/TemporalCoordinationDetection.fs`:178 (association: MEMBER)

    <pre>
    Correct — fixed in 5562c7d217cac7d1c0f31a5c91cf5a995b5cfa50. You're right that `System.Math.Atan2` documents `[-pi, pi]` (both endpoints reachable under IEEE-754 signed-zero semantics: `atan2(0.0, -1.0) = +pi`, `atan2(-0.0, -1.0) = -pi`). I chose to update the documented range rather than normalize at the boundary — callers that care about strict half-open bounds can normalize, and the observed behaviour is well-defined across runtimes. Behaviour unchanged.
    </pre>

## Fix commits (touching thread paths)

### `82e21de3ccb84e8e04ae99f5574b2a0f3e3fd007` -- 2026-04-24T08:59:08Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `src/Core/TemporalCoordinationDetection.fs`
- `tests/Tests.FSharp/Algebra/TemporalCoordinationDetection.Tests.fs`

**Message:**

```
core: PLV mean phase offset — 19th graduation (Amara 18th-ferry #6)

Addresses Amara 18th-ferry correction #6: PLV = 1 can mean
anti-phase locking, not same-time synchronization. Downstream
detectors that rely on "PLV = 1 => synchronized" misread
anti-phase coordinators as same-time coordinators.

Two new functions in `TemporalCoordinationDetection`:

- `meanPhaseOffset phasesA phasesB : double option`
  Returns the argument (angle) of the mean complex phase-
  difference vector whose magnitude is the PLV. Returns
  None when series are empty, mismatched-length, or when
  the mean vector has effectively zero magnitude (1e-12
  floor) — in which case direction is mathematically
  undefined.

- `phaseLockingWithOffset phasesA phasesB : struct (double * double) option`
  Returns both magnitude and offset in one sequence pass.
  Zero-magnitude case: magnitude near 0, offset = nan;
  near-zero magnitude is the caller's reliable "offset is
  undefined" signal.

Existing `phaseLockingValue` contract unchanged; new primitives
are additive. Downstream `Graph.coordinationRiskScore*` and any
other detector consuming PLV can now add a separate offset-
based term instead of collapsing both into one scalar (Amara's
explicit recommendation in correction #6).

8 new xUnit tests covering:
- Identical series (offset = 0)
- Constant pi/4 offset (observed = -pi/4, a-minus-b convention)
- Anti-phase series (magnitude 1, offset = pi) — the correction
  #6 regression test, contrasted against in-phase (offset 0)
  with identical magnitude
- Uniformly-distributed differences (zero-magnitude => None)
- Empty / mismatched-length / single-element edge cases
- phaseLockingWithOffset magnitude matches phaseLockingValue
  (consistency property preventing silent detector divergence)
- phaseLockingWithOffset zero-magnitude returns (near-zero, nan)
- phaseLockingWithOffset returns None on empty/mismatched

All 37 TemporalCoordinationDetection tests pass locally.
0 Warnings / 0 Errors build.

6th of the 10 18th-ferry corrections operationalized this
week (after test-classification doc in #339, parser-tech in
#338). Remaining: Wilson CIs in CartelToy tests (needs #323
landed), MAD=0 percentile-rank fallback (needs #333 landed),
conductance-sign doc (needs #331 landed), artifact-output
layout (Stage-2 with calibration harness).

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
```

### `7c31c8bccfc85ce5248f7859172a009af94c3aac` -- 2026-04-24T11:53:11Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `src/Core/TemporalCoordinationDetection.fs`
- `tests/Tests.FSharp/Algebra/TemporalCoordinationDetection.Tests.fs`

**Message:**

```
fix(#340): refactor shared accumulation + 5 review-thread fixes (Otto…

…-216)

Active PR-resolve-loop on #340 (PLV mean phase offset).

1. Sentinel-default in test (thread 59WGi9): replaced
   Option.defaultValue -1.0 pattern in the
   phaseLockingWithOffset-magnitude-matches-phaseLockingValue
   consistency test with explicit pattern-match + fail
   on None. Sentinel form would silently pass the
   equality assertion if BOTH primitives returned None,
   masking regressions.

2. Broken ferry cross-reference path (thread 59WGjn):
   doc comment referenced docs/aurora/2026-04-24-amara-
   calibration-ci-hardening-deep-research-plus-5-5-
   corrections-18th-ferry.md which doesn't exist on
   main (only 7th / 17th / 19th ferries landed as
   standalone docs). Rewrote provenance to describe the
   ferry topically + cross-reference the related 19th-
   ferry DST audit that IS in the repo.

3. Misleading "same PLV-magnitude floor" wording
   (thread 59WGj4): doc said meanPhaseOffset's
   zero-magnitude check uses "the same PLV-magnitude
   floor" — phaseLockingValue has NO floor (returns
   values arbitrarily close to 0). Fixed: clarified
   that the phasePairEpsilon floor applies ONLY to
   the offset-undefined decision; phaseLockingValue
   returns magnitude without threshold.

4. Name-attribution in doc comment (thread 59WGkP):
   "Aaron + Amara 11th ferry" replaced with "the 11th
   ferry" per factory role-reference convention. Audit-
   trail surfaces (commit messages, tick-history, memory)
   retain direct attribution; code/doc comments use
   role references.

5. Duplicate sin/cos accumulation across 3 functions
   (thread 59WGkn): extracted private helpers
   phasePairEpsilon + meanPhaseDiffVector. All three
   functions (phaseLockingValue, meanPhaseOffset,
   phaseLockingWithOffset) now route through the
   shared accumulator. Eliminates drift risk — one
   function can no longer silently diverge from the
   others on accumulation or threshold.

Build: 0 Warning(s) / 0 Error(s). All 37 TemporalCoordinationDetection
tests pass.

All 5 threads replied via GraphQL next step.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
```

### `5562c7d217cac7d1c0f31a5c91cf5a995b5cfa50` -- 2026-04-24T12:08:38Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `src/Core/TemporalCoordinationDetection.fs`

**Message:**

```
fix(#340): 2 review threads (stale ferry path + atan2 range)

Thread 59Yqkl (P1) — stale provenance reference:
  The doc cited `docs/aurora/2026-04-24-amara-temporal-
  coordination-detection-cartel-graph-influence-surface-
  11th-ferry.md`, but the 11th ferry has not yet landed
  under `docs/aurora/` (it's queued in the Otto-105
  operationalize cadence; PR #296 is its pending absorb).
  Replaced with the intent-preserving form: role references
  ("external AI collaborator's 11th courier ferry") plus a
  pointer at the MEMORY.md queue entry, so the provenance
  survives regardless of when the file-path question
  resolves. Also dropped the direct first-name so this
  factory-produced doc-comment tracks the name-attribution
  discipline.

Thread 59YqlC (P2) — atan2 range correction:
  Doc said `(-pi, pi]` but `System.Math.Atan2` is documented
  as `[-pi, pi]` (both endpoints reachable under IEEE-754
  signed-zero semantics: atan2(0, -1) = +pi,
  atan2(-0, -1) = -pi). Updated the doc to match the
  implementation. Behaviour unchanged.
```
