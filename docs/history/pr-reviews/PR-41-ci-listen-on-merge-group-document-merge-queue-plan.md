# PR #41 -- ci: listen on merge_group + document merge-queue plan

> Git-native PR-review archive. Generated by
> `tools/archive/archive-pr-reviews.ts`. Schema version: 1.
>
> Wall-clock generation timestamp deliberately omitted from the
> rendered file so deterministic re-runs against unchanged
> upstream substrate produce byte-identical content (manifest
> `fetched_at` carries the audit timestamp instead).

## Metadata

| Field | Value |
|---|---|
| Number | 41 |
| Title | ci: listen on merge_group + document merge-queue plan |
| Author | `AceHack` (human) |
| State | MERGED |
| Created at | 2026-04-21T12:30:45Z |
| Merged at | 2026-04-21T12:37:06Z |
| Merge commit SHA | `6ee2168cd2600a9b08b2d48c80b39bc975fda6dc` |
| Branch | `enable-merge-queue-prereq` |
| Base branch | `main` |
| URL | https://github.com/Lucent-Financial-Group/Zeta/pull/41 |
| Changed files | 3 |
| Additions / deletions | +192 / -0 |

## Description

## Summary

- Add `merge_group:` event trigger to `gate.yml` and `codeql.yml` — hard prerequisite for enabling GitHub's merge queue on `main`
- Document the merge-queue plan as §10 of `docs/research/parallel-worktree-safety-2026-04-22.md` per Aaron's 2026-04-22 insight
- `--auto` enabled as a dogfood of the auto-merge UX we're about to rely on

## Why this lands first (not the ruleset)

If the merge-queue ruleset is enabled before the required workflows listen to `merge_group` events, **every merge deadlocks** — the queue creates a merge-group branch, runs required checks, and the checks never fire. This PR lands the trigger first so the queue is safe to enable immediately after.

## Follow-up (same tick, after this merges)

1. Create merge-queue ruleset targeting `~DEFAULT_BRANCH` via `gh api` (config documented in research doc §10.3).
2. Test with the next PR by using `gh pr merge --auto --squash` instead of the current manual flow.
3. Observe queue latency + fall-through behavior; tune `min/max_entries_*` defaults if needed.

## Test plan

- [ ] CI on this PR — `gate.yml` still fires on `pull_request` (unchanged)
- [ ] After merge — confirm `merge_group` trigger is present on `main` HEAD
- [ ] After ruleset creation — open a follow-up PR, add to queue via `gh pr merge --auto --squash`, confirm merge-group CI fires and passes

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Outcome

| Field | Value |
|---|---|
| Merged | true |
| Re-reviewed post-fix | false |
| Total threads | 5 |
| Resolved threads | 0 |
| Unresolved threads | 5 |
| Total review comments | 5 |
| Total fix commits (touching thread paths) | 2 |

## Review threads

### Thread 1 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM58fsH6`
- **Path**: `docs/research/parallel-worktree-safety-2026-04-22.md`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-21T12:35:08Z on `docs/research/parallel-worktree-safety-2026-04-22.md`:257 (association: NONE)

  <pre>
  This section introduces repeated direct name attribution (e.g., in the heading and narrative). The repo’s documented convention is to avoid direct contributor names in docs and use role references instead (e.g., “human maintainer”). Please rewrite these attributions accordingly and keep any necessary verbatim attribution confined to the allowed persona-memory locations (see docs/AGENT-BEST-PRACTICES.md:284-290).

  </pre>

### Thread 2 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM58fsIW`
- **Path**: `docs/research/parallel-worktree-safety-2026-04-22.md`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-21T12:35:08Z on `docs/research/parallel-worktree-safety-2026-04-22.md`:372 (association: NONE)

  <pre>
  This doc claims the workflow-trigger change and the merge-queue ruleset config are already “landed” / “created”, but the PR description frames the ruleset as a follow-up after this PR merges. Please reconcile the doc with the PR plan (e.g., mark these as “proposed / pending” steps, or move completed actions to a post-merge update) so readers don’t think the ruleset already exists.
  </pre>

### Thread 3 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM58fsIm`
- **Path**: `docs/research/parallel-worktree-safety-2026-04-22.md`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-21T12:35:08Z on `docs/research/parallel-worktree-safety-2026-04-22.md`:408 (association: NONE)

  <pre>
  The referenced memory file `memory/feedback_kanban_factory_metaphor_blade_crystallize_materia_pipeline.md` doesn’t exist in the repository, so this cross-reference is currently broken. Either add the missing memory artifact, or update the reference to the correct existing file so the pointer is resolvable.

  </pre>

### Thread 4 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM58fsJA`
- **Path**: `.github/workflows/gate.yml`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-21T12:35:09Z on `.github/workflows/gate.yml`:37 (association: NONE)

  <pre>
  gate.yml now triggers on both `push` to `main` and `merge_group`. Once merge queue is enabled, every queued PR will run this workflow on the merge-group branch and then again after the squash lands on `main`, duplicating CI time and potentially increasing queue latency. Consider dropping the `push` trigger (if direct pushes to `main` are already blocked) or gating the heavy jobs so they don’t run twice for merge-queue merges.
  </pre>

### Thread 5 -- unresolved

- **Thread ID**: `PRRT_kwDOSF9kNM58fsJd`
- **Path**: `.github/workflows/codeql.yml`
- **Replies**: 0

**Initial comment:**

- **`Copilot` (bot)** at 2026-04-21T12:35:09Z on `.github/workflows/codeql.yml`:60 (association: NONE)

  <pre>
  Adding `merge_group` to CodeQL will run the (potentially slow) CodeQL database build on every merge-queue attempt, which can significantly increase merge queue cycle time and consume Actions minutes, even though CodeQL isn’t a required check. Consider keeping CodeQL on `push`/`schedule` only, or making the merge_group run a lighter/faster slice (or skip entirely) so merge-queue checks stay tight.

  </pre>

## Fix commits (touching thread paths)

### `d1632cc60caa3a7f1d76582fc703dc55c448e14f` -- 2026-04-21T12:30:28Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `.github/workflows/codeql.yml`
- `.github/workflows/gate.yml`
- `docs/research/parallel-worktree-safety-2026-04-22.md`

**Message:**

```
ci: listen on merge_group + document merge-queue plan

Add `merge_group:` event to the `on:` block of both required
workflows (`gate.yml`, `codeql.yml`) as a hard prerequisite for
enabling GitHub's merge queue on `main`. Without this trigger,
the required checks never run on the queue's merge-group branch
and merges deadlock.

Capture Aaron's 2026-04-22 merge-queue insight in §10 of
`docs/research/parallel-worktree-safety-2026-04-22.md`: pre-open
rebase-cost self-questioning discipline (§10.1), merge queue +
auto-merge as structural fix that largely replaces it (§10.2),
direct-admin enable path replacing the planned HB-001 (§10.3),
and §4 staging revision (§10.5) folding the enable into R45.

This PR lands only the workflow trigger change and the research
doc; the merge-queue ruleset itself is enabled via GitHub API
after this PR merges, so the merge-group trigger is already on
`main` before the queue can activate.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
```

### `c52e8c52bb00905eb7197d216c0f5683b175a8c5` -- 2026-04-21T12:33:21Z -- `AceHack`

**Touched files (intersect with thread paths):**

- `docs/research/parallel-worktree-safety-2026-04-22.md`

**Message:**

```
docs(research): fix MD056 — add missing 3rd column to §10.2 table

Two rows in the hazard-to-fix mapping table were missing their
"Still required separately" cell; markdownlint (MD056) rightly
flagged them. Fill with accurate content (stale-branch audit
continues; bouncing-between-PRs dance has no residual work).

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
```
