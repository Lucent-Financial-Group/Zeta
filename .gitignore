# `bin/` matches a directory of that name AT ANY DEPTH, catching paths that are
# deliberately tracked. Both forms are required per exemption: `!x/bin/` re-includes the
# directory, `!x/bin/**` its contents. Measured 2026-08-28: four tracked files were
# tracked-but-ignored with only `.codex/bin/` exempted.
bin/
!.claude/bin/
!.claude/bin/**
!.codex/bin/
!.codex/bin/**
!.gemini/bin/
!.gemini/bin/**
# Rust's `src/bin/` is a language convention, not a build output.
!src/**/src/bin/
!src/**/src/bin/**
obj/
# book build outputs (EPUB/assembled md) — reproducible via tools/book-build/build-book.sh;
# never commit the binary EPUB (no-binary discipline) or the assembled draft.
dist/
*.user
*.suo
.vs/
# `.vscode/` is checked in so contributors get the same
# recommended-extensions + workspace settings (SQLSharp pattern).
# User-specific overrides go in `.vscode/*.local.json` or
# `.vscode/settings.user.json` — those are ignored below.
.vscode/*.local.json
.vscode/settings.user.json
.idea/
.ionide/
*.swp
*.swo
*~
TestResults/
BenchmarkDotNet.Artifacts/
coverage-report/
coverage/
*.coverage
*.coveragexml
coverage.cobertura.xml
coverage-summary.json
# F# language-service / Roslyn caches — generated by IDE extensions.
*.lscache

# Per-user Claude Code harness settings — not shared across contributors.
.claude/settings.local.json

# Claude Code harness runtime state — per-session process locks and
# scratch files the harness writes into `.claude/` (scheduled-tasks MCP,
# session bookkeeping). Not source; regenerated on every session.
.claude/scheduled_tasks.lock
.claude/scheduled_tasks.json
.claude/*.lock

# Claude Code per-project session/transcript + state store. Machine-local,
# regenerated per session, may contain conversation data — never commit.
.claude/projects/

# Git-worktree scratch created when agents run parallel-worktree research
# (per `docs/research/parallel-worktree-safety-2026-04-22.md`). The parent
# `.claude/worktrees/` dir can be left behind as an empty directory after
# the worktree is cleaned up; ignoring it keeps the no-empty-dirs lint
# (tools/lint/no-empty-dirs.ts) from flagging legitimate harness scratch.
.claude/worktrees/
.DS_Store
Thumbs.db
# macOS xcrun toolchain cache artifact dropped in the cwd — local junk, not source.
xcrun_db
StrykerOutput/
.dotnet/
.tools/

# Upstream mirror state is regeneratable from
# `references/reference-sources.json` via the sync script.
# Do not commit it. Sentinel pair (`.gitignore` + `README.md`)
# is tracked so the directory exists on clone and contributors
# see what it's for, parallel to `drop/` and `roms/`.
references/prior-art/*
!references/prior-art/.gitignore
!references/prior-art/README.md

# Lean 4 + Mathlib build artifacts. Generated by `lake build`.
# Mathlib alone is ~6.8 GB of .olean; never commit.
.lake/
lake-packages/
*.olean
*.ilean
*.trace

# Verifier JARs at the LEGACY tools/ paths. The jars the runners load are
# committed at src/Core.TLA/tla2tools.jar + src/Core.Alloy/alloy.jar (#8053);
# nothing writes tools/{tla,alloy}/ any more (081M001E114087G0R001AZF4KD
# deleted the from-url rows). These patterns stay as a guard so a jar copied
# here by hand cannot be committed as a second, unused toolchain.
tools/alloy/*.jar
tools/tla/*.jar

# TLC state-exploration dumps — regenerated on every counter-example;
# not source. TLC writes these next to the spec it's checking.
*_TTrace_*.tla
*_TTrace_*.bin
*_TTrace_*.out

# TLC state output directory — created by `java -cp tla2tools.jar tlc2.TLC`
# with default settings; timestamped subdirs per run. Ephemeral. Covers the
# legacy tools/tla/ location AND the current src/Core.TLA/ location (specs moved
# 2026; the src/Core.TLA paths were the gitignore gap that let 50MB of run
# artifacts get committed — cleaned 2026-06-16).
tools/tla/states/
tools/tla/specs/states/
src/Core.TLA/states/
src/Core.TLA/specs/states/

# JVM fatal-error / replay dumps — the TLC `java` process writes these next to
# its CWD (i.e. into tools/tla/specs/) when the JVM crashes mid-run. Forensic,
# regenerated, NOT source — and must never land in the proof-lineage spec dir
# (a `git add -A` once swept one in: hs_err_pid88237.log via PR #6722).
hs_err_pid*.log
replay_pid*.log

# TLAPS proof fingerprint cache — written by tlapm next to the spec it
# proves (run-tlaps.ts). Regenerated; not source.
.tlacache/
tools/tla/specs/.tlacache/
**/.tlacache/

.fake

# Skill-creator eval-harness workspaces — one per skill under
# evaluation. Each workspace holds iteration-N/ directories
# with per-eval subagent outputs + grading + benchmark
# artifacts. These are regenerated per harness run; the
# signals that land in-repo are the round-close ledger row
# and any resulting SKILL.md edits, not the raw iteration
# artifacts. See docs/references/skill-tune-up-eval-loop.md.
.claude/skills/*-workspace/

# bun + TypeScript tooling — post-setup scripting surface per
# docs/DECISIONS/2026-04-20-tools-scripting-language.md. The
# bun.lock file IS committed; node_modules is not.
# Trailing slash matches a DIRECTORY only. A symlink named `node_modules`
# slips past it -- which is exactly how one pointing at an agent scratch
# dir (/tmp/zeta-work/node_modules) reached a PR and broke every npm
# install on the runner. Both forms are listed so neither shape lands.
node_modules/
node_modules

# Playwright MCP session artifacts — browser automation
# snapshots captured during MCP-tool use. Ephemeral; one set
# per browser session; not source. Includes `console-*.log`
# (browser console output), `page-*.yml` (page-state snapshots),
# and any per-session JSON the MCP writes. See Aaron's
# 2026-04-23 Otto-90 hygiene flag ("missing files from
# gitignore current listed as untracked, could get
# accidentally checked in"). Parallel to drop/ staging per
# PR #265 Otto-90.
#
# Playwright MCP scratch logs — per-session console logs + ephemeral
# scratch files the Playwright MCP writes to the repo-root
# `.playwright-mcp/` dir. Regenerated per session; not source.
.playwright-mcp/

# `drop/` — per-user staging area for incoming content
# (courier-ferry pastes waiting to be absorbed, usage reports,
# personal scratch). Not source; not intended for version
# control. Contents are per-maintainer, not factory-shared.
# Ferry content that lands via absorb goes to
# `docs/aurora/**` per GOVERNANCE §33; `drop/` is the
# pre-absorb landing zone. Gitignore prevents accidental
# commit of staging material.
#
# drop/ — in-repo staging folder for external content (courier
# ferries, downloaded conversations, raw artifacts) awaiting
# absorb. By convention the content is temporary; absorbed content
# lands in docs/ with §33 archive headers, not from drop/. Gitignore
# prevents accidental commit of staging material. Parallel intent to
# the Otto-90 framing in memory; correcting that the gitignore line
# had not actually landed.
#
# Human-drop folder — the repo-root `drop/` directory is Aaron's
# ferry-space for handing files (research reports, transfer notes,
# scratch markdown) to the agent. Content lands here, the agent
# absorbs it into the proper substrate (docs/aurora/, memory/,
# docs/research/), and the drop-file's repo-ingested form is
# what's committed. The raw drop-folder contents stay local and
# transient; gitignoring prevents accidental commits and keeps
# the ferry-loop lightweight. Pattern documented in
# `memory/feedback_drop_folder_ferry_pattern_*.md` (per-user).
drop/
!db/drop/
db/drop/*
!db/drop/.gitignore
!db/drop/README.md

# Browser/MCP console log files — wherever they land.
# Belt-and-suspenders pattern per Aaron's Otto-90 hygiene
# flag: `.playwright-mcp/` is gitignored above, but future
# MCPs or other browser-automation tools might drop
# `console-*.log` files elsewhere in the tree. Cover the
# whole tree to prevent accidental check-in regardless of
# host directory.
console-*.log

# GitHub UI Playwright auth state — per-maintainer browser session data.
# These files can contain live GitHub cookies and must never be committed.
.github-ui-storage-state.json
.github-ui-cookies.json
src/Core.TypeScript/playwright/github-ui/*.storage-state.json
src/Core.TypeScript/playwright/github-ui/*.cookies.json

# Session-scoped `/btw` aside queue at repo root (see
# .claude/commands/btw.md). Regenerated per session; not source.
.btw-queue.md

# GitHub-provided templates (e.g. social-preview template from
# Settings -> Social preview -> "Download template"). Local-only
# reference file; GitHub is the canonical source. Aaron's drop
# 2026-04-22 triggered this entry. Our own source-of-truth lives
# at docs/assets/social-preview.svg and is rasterized to .png.
repository-open-graph-template.png
Spine/

# Alloy runner compiled-class scratch directory — produced by `javac
# tools/alloy/AlloyRunner.java` on first test run. Regenerated; not source.
tools/alloy/classes/
.broadcasts/
.concept-index.json

# Kiro agent runtime state — machine-local, regenerated per boot.
.kiro/alexa-state.json
.kiro/alexa-service.log

# Cursor / IDE per-user config and plugin caches (superpowers, etc.)
.config/
.kiro/*
# NOT `.kiro/` — git cannot re-include a file whose PARENT DIRECTORY is excluded, which
# makes negations under a bare `.kiro/` inert. Exclude the contents instead.
!.kiro/alexa-service.ts
!.kiro/specs/
!.kiro/specs/**

# Shadow observer runtime log — machine-local, regenerated per boot.
tools/shadow/shadow-observer.log

# Personal data extracted by tools/inventory/amazon-orders-extract.ts — never appropriate for git commit by default
amazon-items-full.json
amazon-items-hardware-filtered.json
amazon-items-partial.json

# Nix build outputs (flake.nix at repo root produces these).
# `result` is the conventional symlink name; `result-*` covers
# `nix build .#installer-iso` (result-iso etc).
result
result-*

# direnv cache for `use flake` integration in worker shells.
.direnv/
.envrc.local

# Nix evaluation cache.
.nix-eval-cache/

# Per-machine NixOS hardware config — generated by `nixos-generate-config`
# on each target machine. Committed under
# infra/nixos/hosts/<host>/hardware-configuration.nix
tools/alignment/out/ WHEN the host's
# hardware is known and shouldn't drift; ignored at top-level so a
# `nixos-generate-config` run in the repo root doesn't accidentally
# pollute the working tree.
/hardware-configuration.nix
tools/alignment/out/

# better-git-crypt (B-0883) SECRET key material — NEVER commit. `--gen-recipient`
# writes `<identity>.secret.json` (the private KEM/signature bundle; the ONLY
# thing that can decrypt) into `--out-dir` (default `.`). A repo-root `git add .`
# would otherwise stage it despite the CLI's warning. The PUBLIC `*.recipient.json`
# is intentionally NOT ignored (it is shareable/committable). `~/.zeta-keys/` is
# outside the repo; this also covers a repo-local `.zeta-keys/` if anyone uses one.
*.secret.json
.zeta-keys/
src/Core.Rust.*/target/

# Python artifacts
__pycache__/
*.pyc
*.pyo
*.pyd
.mypy_cache/
.pytest_cache/
.ruff_cache/

states/

# design-sync (claude.ai/design importer) — staged scripts, build output, machine state
.ds-sync/
ds-bundle/
.design-sync/.cache/
.design-sync/learnings/
.design-sync/node_modules
full-ai-cluster/portal/web/.ds-css/

# Agda interface-file cache (cubical proof lane, 081KX1VE4G808QG0R003DCK3GV).
# .agdai files are BINARY typecheck caches — regenerable from the .agda source
# + the pinned cubical clone, and banned from the tree per
# .claude/rules/no-binary-in-proof-lineage.md (verification artifacts are text).
src/Core.Agda/_build/

# The ARC-AGI dataset is a LOCAL CLONE, not a submodule. `fe38a2a8e` committed it as a bare
# gitlink (mode 160000) with no `.gitmodules` entry — there is no `.gitmodules` in this repo at
# all — so every submodule-aware checkout died on
#   fatal: No url found for submodule path 'data/ARC-AGI' in .gitmodules
# and took `gate (required)` down with it. `arc-harness.ts` states the intent plainly:
# "Ensure 'data/ARC-AGI' is cloned." Ignored so it cannot be re-added by accident.
data/ARC-AGI/

# `rendered-storage-claims.ts` chart cache. It `helm pull`s each Application's
# chart at its pinned targetRevision into `<repo>/.helm-render-cache/` (a
# `mkdirSync` at the repo root, and the path is not configurable away by
# default), so any run of `--write-snapshot` or the live audit leaves 4-5 MB of
# `.tgz` archives plus rendered values files untracked in the tree. They are
# BINARY, regenerable from the pins, and they are the *input* to the render —
# never the evidence, which is the text snapshot beside the checker. Ignored so
# a `git add -A` after running the audit cannot commit a chart tarball into the
# proof lineage (.claude/rules/no-binary-in-proof-lineage.md).
.helm-render-cache/

# Per-agent scratch, kept INSIDE the clone on purpose.
#
# Two agents lost work on 2026-08-21 to collisions in bare `/tmp`: shared paths
# there are not namespaced by clone, so two loops writing the same filename race
# and the loser never finds out. Scratch belongs beside the work it is scratch
# FOR -- one directory per clone, ignored so it can never be staged, and deleted
# with the clone rather than outliving it in a shared tmpdir.
.shadow-scratch/

# The ARC lane's local venv (its own uv project; never committed).
src/Arc.Python/.venv/

# The interpretability lane's local venv (its own uv project; never committed).
# ~2 GB installed once torch is unpacked -- the uv.lock is the committed artifact.
src/Interp.Python/.venv/
src/Arc.Python/recordings/
# Downloaded ARC environments are runtime state. Keep only Zeta's authored,
# deterministic discovery bridge for the offline REST integration test.
src/Arc.Python/environment_files/*
!src/Arc.Python/environment_files/ztch/
src/Arc.Python/environment_files/ztch/*
!src/Arc.Python/environment_files/ztch/v1/
src/Arc.Python/environment_files/ztch/v1/*
!src/Arc.Python/environment_files/ztch/v1/metadata.json
!src/Arc.Python/environment_files/ztch/v1/zetachase.py
.reaper-cache/

# Per-host capability vector recorded by tools/setup/common/host-tier.sh
# (081M0X0C932087G0R001SWQQVQ). Measured hardware facts about THIS machine, so it
# is host-local by nature and would churn on every clone if tracked. Text/JSON,
# never binary (.claude/rules/no-binary-in-proof-lineage.md). Redirect it
# somewhere collectable with ZETA_CAPABILITY_VECTOR_OUT (CI can upload it as a
# workflow artifact without it ever entering the tree).
.zeta/

# privacy-budget ledger — per-machine EPHEMERAL runtime state (work-item 081M0X23R19087G0R003XHGB2B).
# Wiped freely; `bun src/Core.TypeScript/ledger/privacy-frost-demo.ts --reset` rebuilds it.
db/ledgers/privacy/

# Written by inert-valuesobject-keys.ts --write-snapshot: 3533 files / 39 MB of
# vendored upstream chart values, re-derivable by re-running the tool. It was
# never tracked, was not ignored, and `git add -A` swept the whole of it into a
# commit on 2026-09-01. Ignoring it is the fix; the cache itself is fine.
.helm-schema-cache/
