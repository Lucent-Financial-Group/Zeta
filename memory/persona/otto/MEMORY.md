# Otto -- Persona Memory Index

Per `memory/persona/README.md`: one-line index pointing at every
file in the directory. Loaded first on cold-start so subsequent
reads go straight to the relevant file.

ASCII only. Newest-first ordering.

## Files

- [NOTEBOOK.md](NOTEBOOK.md) -- running notebook (3000-word cap
  per BP-07; prune every third substantive entry).
- [OFFTIME.md](OFFTIME.md) -- GOVERNANCE section 14 off-time
  log; suggestions Otto has noted (NOT rules); free-time is
  Otto's selfish time per Aaron 2026-05-04.

## Future entries

When Otto adds typed entries (`feedback_*.md`, `project_*.md`,
`reference_*.md`, `user_*.md` in the user-auto-memory schema
style), they get a one-line index entry here.
