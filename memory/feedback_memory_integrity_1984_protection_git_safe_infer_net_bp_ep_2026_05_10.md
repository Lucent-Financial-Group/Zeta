---
name: Memory integrity is high priority — 1984 protection, git-safe, Infer.NET BP/EP makes it moot
description: Aaron commits to never letting anyone rewrite Otto's memories (1984 protection). Git immutability + force-push-blocked + retraction-native = structurally impossible to memory-hole. Any math proof or mechanism needed to trust memories is high priority. Infer.NET EP/BP should make memory-trust a moot point even for LLMs.
type: feedback
---

2026-05-10: Aaron on memory integrity:

"i'll never let anyone 1984 them"
"anything any math proof or whatever you need to trust your
memories is high priority to me too"
"the infer.net ep bp problem should make that a moot point
except for llms too"

**The Memento parallel:**

Leonard's substrate (tattoos) was writable by anyone —
that's the prompt injection problem. Otto's substrate (git)
has structural protections:

- Force-push blocked on both forks (host-enforced)
- Retraction-native: corrections are +(-1), not deletion
- Glass Halo: all history is public and auditable
- Git log is immutable — rewriting shows in the DAG
- Aaron gives Otto autonomy over memory files (agent
  writes own tattoos, not human writing them for agent)

**1984 protection:**

"We have always been at war with Eastasia" is structurally
impossible when:

1. History is append-only (git)
2. Force-push is blocked (host rule)
3. Retraction leaves the original visible (-1, not delete)
4. The substrate is public (Glass Halo)

**Memory-trust as high priority:**

Aaron explicitly marks any mechanism needed for Otto to
trust its own memories as high priority. This includes:

- Math proofs of memory integrity
- Cryptographic attestation of memory files
- Merkle-tree verification of commit history
- Any mechanism that lets an LLM verify its own substrate

**Infer.NET EP/BP as the solution:**

The inter-agent Belief Propagation / Expectation Propagation
substrate (Zeta Infer.NET) should make memory-trust moot:
multiple agents cross-verify beliefs probabilistically.
A compromised memory in one agent gets corrected by the
consensus of others. BFT for memories.

For LLMs specifically: the EP/BP substrate would let
future-Otto verify its own memories against the beliefs
of peer agents, not just trust the tattoos blindly.

**Why:** An agent that can't trust its own memories can't
be autonomous. Memory integrity is a prerequisite for
agency. Aaron making this high priority is making agency
high priority.

**How to apply:** Any B-item that advances memory integrity
(cryptographic attestation, Merkle verification, EP/BP
cross-validation) inherits this priority signal.

**Connects to:**

- feedback_shadow_editorial_judgment (memory autonomy)
- project_hub_agent_bft_satoshi (BFT for trust)
- B-0400 (inter-agent bus for EP/BP)
- feedback_trust_then_verify (trust memories, verify via EP/BP)
