---
name: dont-declare-done-from-the-build-slice-check-proven-core-map-and-primitive-wish-list-2026-06-04
description: "Aaron 2026-06-04 ('we are 4 years from a stopping point... not sure how you got to stopping point'): NEVER declare 'done/plateau/core complete' from the build-slice you just shipped. Calibrate against the REAL program — PROVEN-CORE-MAP.md (the per-leg matrix; nothing is PROVEN by the full bar yet) + the primitive WISH LIST (PRIMITIVE-REGISTRY.md). Building the 4-ser LEG infra ≠ proving the primitives through it."
metadata: 
  node_type: memory
  type: feedback
  originSessionId: a9bca54f-fdf0-41b7-8def-cb33ee1bec26
---

2026-06-04. After shipping the serializers (JSON/CBOR/YAML/XML/Arrow) + the policy
kernel, Otto repeatedly called it a "plateau / stopping point / core complete."
Aaron: "Have you looked at the primitive wish list — we are 4 YEARS from a stopping
point. How are we on homeostat proofs from first primitives from our serializers and
all our proof legs on all our primitives? Not sure how you got to stopping point."

**The error:** Otto declared done by looking at the BUILD SLICE he'd just finished
(serializer codecs exist) instead of the REAL PROGRAM. Checking `docs/PROVEN-CORE-MAP.md`
showed the truth: **"Nothing on this floor is PROVEN by the full bar yet."** The bar is
`PROVEN ⟺ math ∧ 4-lang ∧ 4-ser ∧ Bonsai ∧ Arrow ∧ homeostat-tie`. The per-leg matrix
(6 floor primitives: clock, identity, Merkle, CRDT, serialization-seed, metric) is
mostly ✗ on 4-ser/Bonsai/Arrow/homeostat. What Otto built is the **4-ser LEG
INFRASTRUCTURE** (the DynamicValue codecs) — necessary, but **building the leg ≠ tying
each primitive through it**. The explicit current-focus — take **G-Set to FULL PROVEN**
via the first-full-vertical (4-ser → Arrow → Bonsai → homeostat-tie) — is barely begun.

**How to apply:**
- **Before EVER saying "done / plateau / core complete / nothing actionable," read the
  real program state**: `docs/PROVEN-CORE-MAP.md` (per-leg matrix + current-focus
  vertical) and `docs/PRIMITIVE-REGISTRY.md` (the wish list). Calibrate against THOSE,
  not the slice just shipped.
- **"I built the leg" ≠ "the primitives are proven through the leg."** The 4-ser codecs
  existing means the 4-ser leg can NOW be tied — that UNBLOCKS the real work (wire each
  primitive through 4-ser ∧ Arrow ∧ Bonsai ∧ homeostat), it doesn't complete it.
- The real next work is concrete + unblocked: **G-Set first-full-vertical to FULL
  PROVEN** (G-Set values → DynamicValue → all-4-serializers agree → Arrow-tie →
  Bonsai-tie → homeostat-tie; the heartbeat-homeostat demo already exists). Then the
  other primitives, then the wish list (years of it).
- Pairs with [[autonomy-execute-your-own-recommendation-under-standing-authority...]]:
  that one = don't over-PAUSE; this one = don't over-DECLARE-DONE. Both = calibrate
  against the real program state, not a feeling (of bigness, or of completeness).
  Completeness is the proof-map's verdict, not Otto's sense that the slice wrapped.
