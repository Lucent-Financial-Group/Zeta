{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",

  "_comment": "Do NOT put real secrets here for production. Prefer environment variables (the keys below map to env vars by replacing ':' with '__', e.g. GitHub__ClientSecret). These placeholders document the full configuration surface.",

  "SelfBaseUrl": "https://CHANGE-ME-your-backend.example.com",
  "AllowedFrontendOrigins": "https://lucent-financial-group.github.io",

  "Jwt": {
    "Secret": "CHANGE-ME-generate-32+-random-bytes-base64-or-hex",
    "Issuer": "genesis-auth",
    "TtlMinutes": "30"
  },

  "GitHub": {
    "ClientId": "CHANGE-ME",
    "ClientSecret": "CHANGE-ME"
  },

  "GitLab": {
    "ClientId": "CHANGE-ME",
    "ClientSecret": "CHANGE-ME",
    "BaseUrl": "https://gitlab.com"
  }
}
